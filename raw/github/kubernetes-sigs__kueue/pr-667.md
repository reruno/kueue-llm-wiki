# PR #667: Integrate RayJob

**Summary**: Integrate RayJob

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/667

**Last updated**: 2023-05-25T09:14:37Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `feat/rayjob` → `main`
- **Created**: 2023-03-31T14:33:30Z
- **Updated**: 2023-05-25T09:14:37Z
- **Merged**: 2023-05-24T19:12:50Z
- **Closed**: 2023-05-24T19:12:50Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@kerthcet](https://github.com/kerthcet)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 18   **+1937 / -2**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Adds RayJob support into kueue.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #666 
#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Add support for Kuberay's RayJobs.
```
#### Special notes for your reviewer:

This implementation uses the not yet merged [kuberay job `suspend` feature](ray-project/kuberay#926) and is referencing the code from EPAM's fork.

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-03-31T14:33:38Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | f99df98191021dccda0c82f185186e10988198d8 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/646dab22e2890600081a262c |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-03-31T14:33:40Z

Hi @trasc. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mwielgus](https://github.com/mwielgus) — 2023-04-04T18:00:18Z

Please take a look at #668 - we now have a dedicated field to choose integrations to enable.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-05T14:39:54Z

/ok-to-test

### Comment by [@trasc](https://github.com/trasc) — 2023-04-05T17:25:45Z

/retest

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-05T18:02:29Z

could this be a flaky test in the existing suite?

### Comment by [@trasc](https://github.com/trasc) — 2023-04-06T04:40:46Z

> could this be a flaky test in the existing suite?

100%, this PR has nothing to do with the scheduler, and none of the new code  (controller or webhooks) is executing in that test suite.

### Comment by [@trasc](https://github.com/trasc) — 2023-04-06T04:41:07Z

/retest

### Comment by [@trasc](https://github.com/trasc) — 2023-04-06T06:28:37Z

/cc @alculquicondor 
/cc @mwielgus

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-06T12:29:32Z

Could you please open an issue and investigate?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-06T12:32:47Z

opened #678

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-11T20:23:00Z

Can you follow https://github.com/kubernetes-sigs/kueue/pull/668/files#diff-1dd4256189ca00ba75ebb5935fd7f7f403507b3d2e7033d8446c63b171715a52 to disable this integration by default?

### Comment by [@trasc](https://github.com/trasc) — 2023-04-12T06:16:50Z

> Can you follow https://github.com/kubernetes-sigs/kueue/pull/668/files#diff-1dd4256189ca00ba75ebb5935fd7f7f403507b3d2e7033d8446c63b171715a52 to disable this integration by default?

done

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-04-28T08:30:30Z

The ray pr is here https://github.com/ray-project/kuberay/pull/926

### Comment by [@trasc](https://github.com/trasc) — 2023-05-17T11:47:39Z

@alculquicondor  the PR is rebased and  references to epam's fork removed, ready for review.

I'll do some extra in cluster manual test but I don't expect to have a big impact on this.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-17T17:55:36Z

/retitle Integrate RayJob

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-17T17:56:40Z

/assign @kerthcet 
Can you do a pass?

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-05-18T06:44:26Z

/assign

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-05-18T11:17:27Z

I'll review this later today.

### Comment by [@trasc](https://github.com/trasc) — 2023-05-19T13:09:45Z

/test pull-kueue-test-e2e-main-1-24

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-21T16:16:46Z

Can we include `ray` to `KnwonWorkloadOwner`?

https://github.com/kubernetes-sigs/kueue/blob/74f45f69612a2d36ace91ae455f1e9fdbcd53037/pkg/controller/jobframework/known_frameworks.go#L25-L27

### Comment by [@trasc](https://github.com/trasc) — 2023-05-22T09:05:59Z

> Can we include `ray` to `KnwonWorkloadOwner`?

For now, we don't need it , ray is not creating batch/jobs , going forward, maybe we should rework https://github.com/kubernetes-sigs/kueue/blob/74f45f69612a2d36ace91ae455f1e9fdbcd53037/pkg/controller/jobframework/integrationmanager.go#L41 to cover this for all registers frameworks.

Also  `KnwonWorkloadOwner` might not be the best name for this, we are not interested if we know the owner, we are interested if that Owner has it's own Workload  managed by kueue.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-22T09:58:41Z

> Also `KnwonWorkloadOwner` might not be the best name for this, we are not interested if we know the owner, we are interested if that Owner has it's own Workload managed by kueue.

Agree. Let's work on this at #782.

cc: @kerthcet

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-05-24T18:57:30Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/667#pullrequestreview-1442563266" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/667#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-24T18:58:08Z

/lgtm

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-11T20:26:37Z

- **main.go:230** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-11T20:26:37Z
```diff
@@ -221,6 +227,26 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 			os.Exit(1)
 		}
 	}
+	if isFrameworkEnabled(cfg, rayjob.FrameworkName) {
```

  Before this starts growing too much, maybe we can replace with a loop.
  
  We can have some `RegisterIntegration` method that is called from the `init()` function of each integration's package.
  
  Then the main goes over all the registered integrations, checks if they are enabled before calling the constructor. Otherwise it creates the noop webhook.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-12T06:16:35Z

- **main.go:230** by [@trasc](https://github.com/trasc) — 2023-04-12T06:16:34Z
```diff
@@ -221,6 +227,26 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 			os.Exit(1)
 		}
 	}
+	if isFrameworkEnabled(cfg, rayjob.FrameworkName) {
```

  I did think of something like this but I don't think we should link it to the ray integration, so I've created #691.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-17T18:28:20Z

- **Makefile:291** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-17T17:34:15Z
```diff
@@ -286,3 +286,17 @@ mpi-operator-crd:
 	GOPATH=/tmp GO111MODULE=on $(GO_CMD) install github.com/kubeflow/mpi-operator/cmd/mpi-operator@$(MPI_VERSION)
 	mkdir -p $(shell pwd)/dep-crds/mpi-operator/
 	cp -f /tmp/pkg/mod/github.com/kubeflow/mpi-operator@$(MPI_VERSION)/manifests/base/* $(shell pwd)/dep-crds/mpi-operator/
+
+
+RAY_VERSOION := 9bc5d854bd316c46a79920bbd7b2887fb0c6d0af
```

  Can we get the Ray version using a way similar to MPIJob?
  
  https://github.com/kubernetes-sigs/kueue/blob/cd747c596b2b56929c71abe7c6f94ab5c73609f9/Makefile#L28-L29

- **Makefile:302** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-17T17:35:38Z
```diff
@@ -286,3 +286,17 @@ mpi-operator-crd:
 	GOPATH=/tmp GO111MODULE=on $(GO_CMD) install github.com/kubeflow/mpi-operator/cmd/mpi-operator@$(MPI_VERSION)
 	mkdir -p $(shell pwd)/dep-crds/mpi-operator/
 	cp -f /tmp/pkg/mod/github.com/kubeflow/mpi-operator@$(MPI_VERSION)/manifests/base/* $(shell pwd)/dep-crds/mpi-operator/
+
+
+RAY_VERSOION := 9bc5d854bd316c46a79920bbd7b2887fb0c6d0af
+RAYTEMP := /tmp/kubreay-$(RAY_VERSOION)
+.PHONY: ray-operator-crd-git
+ray-operator-crd: | $(RAYTEMP)
+	mkdir -p $(shell pwd)/dep-crds/ray-operator/
+	cp -f $(RAYTEMP)/ray-operator/config/crd/bases/* $(shell pwd)/dep-crds/ray-operator/
+
+$(RAYTEMP):
+	git clone --depth=1 --filter=blob:none --sparse https://github.com/ray-project/kuberay.git $(RAYTEMP)
+	git -C $(RAYTEMP) fetch --depth=1 origin $(RAY_VERSOION)
+	git -C $(RAYTEMP) reset --hard FETCH_HEAD
+	git -C $(RAYTEMP) sparse-checkout set ray-operator/config/crd/bases
```

  Are there any reasons why we use the git command instead of `go install`?

- **pkg/controller/jobs/rayjob/rayjob_controller.go:41** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-17T17:45:44Z
```diff
@@ -0,0 +1,282 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk = schema.GroupVersionKind{
+		Group:   rayjobapi.GroupVersion.Group,
+		Version: rayjobapi.GroupVersion.Version,
+		Kind:    "RayJob",
```

  ```suggestion
  	gvk =  rayjobapi.GroupVersion.Group.WithKind("RayJob")
  ```

- **pkg/controller/jobs/rayjob/rayjob_controller.go:105** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-17T17:51:06Z
```diff
@@ -0,0 +1,282 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk = schema.GroupVersionKind{
+		Group:   rayjobapi.GroupVersion.Group,
+		Version: rayjobapi.GroupVersion.Version,
+		Kind:    "RayJob",
+	}
+
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
```

  Does the kuberay-operator have parameters to represent priority between the head and workers?

- **pkg/controller/jobs/rayjob/rayjob_controller.go:148** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-17T18:04:24Z
```diff
@@ -0,0 +1,282 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk = schema.GroupVersionKind{
+		Group:   rayjobapi.GroupVersion.Group,
+		Version: rayjobapi.GroupVersion.Version,
+		Kind:    "RayJob",
+	}
+
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) == 0 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
```

  ```suggestion
  	if len(nodeSelectors[0].NodeSelector) != 0 {
  		headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
  	}
  ```

- **pkg/controller/jobs/rayjob/rayjob_controller.go:134** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-17T18:07:00Z
```diff
@@ -0,0 +1,282 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk = schema.GroupVersionKind{
+		Group:   rayjobapi.GroupVersion.Group,
+		Version: rayjobapi.GroupVersion.Version,
+		Kind:    "RayJob",
+	}
+
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
```

  ```suggestion
  	if dst == nil {
  		return src
  	}
  	for k, v := range src {
  ```

- **pkg/controller/jobs/rayjob/rayjob_controller.go:174** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-17T18:12:31Z
```diff
@@ -0,0 +1,282 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk = schema.GroupVersionKind{
+		Group:   rayjobapi.GroupVersion.Group,
+		Version: rayjobapi.GroupVersion.Version,
+		Kind:    "RayJob",
+	}
+
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) == 0 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if !equality.Semantic.DeepEqual(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector) {
+		headPodSpec.NodeSelector = cloneSelectors(nodeSelectors[0].NodeSelector)
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = cloneSelectors(nodeSelectors[index+1].NodeSelector)
```

  ```suggestion
  		if !equality.Semantic.DeepEqual(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector) {
  				workerPodSpec.NodeSelector = cloneSelectors(nodeSelectors[index+1].NodeSelector)
  		}
  ```

- **pkg/controller/jobs/rayjob/rayjob_controller.go:187** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-17T18:17:36Z
```diff
@@ -0,0 +1,282 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk = schema.GroupVersionKind{
+		Group:   rayjobapi.GroupVersion.Group,
+		Version: rayjobapi.GroupVersion.Version,
+		Kind:    "RayJob",
+	}
+
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) == 0 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if !equality.Semantic.DeepEqual(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector) {
+		headPodSpec.NodeSelector = cloneSelectors(nodeSelectors[0].NodeSelector)
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = cloneSelectors(nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func (j *RayJob) Finished() (metav1.Condition, bool) {
+	condition := metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  string(j.Status.JobStatus),
+		Message: j.Status.Message,
+	}
+	finished := false
+	switch j.Status.JobDeploymentStatus {
+	case rayjobapi.JobDeploymentStatusComplete, rayjobapi.JobDeploymentStatusFailedJobDeploy:
```

  I'm not sure how to handle the `FailedToGetOrCreateRayCluster` condition.
  
  If the condition indicates failed to create RayJob, we should treat it as `Finished`.
  
  wdyt?

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:107** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-17T18:27:33Z
```diff
@@ -0,0 +1,128 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+type RayJobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for rayjobapi RayJob.
+func SetupRayJobWebhook(mgr ctrl.Manager, opts ...jobframework.Option) error {
+	options := jobframework.DefaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &RayJobWebhook{
+		manageJobsWithoutQueueName: options.ManageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&rayjobapi.RayJob{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-ray-io-v1alpha1-rayjob,mutating=true,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=create,versions=v1alpha1,name=mrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &RayJobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *RayJobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
+	jobframework.ApplyDefaultForSuspend((*RayJob)(job), w.manageJobsWithoutQueueName)
+	return nil
+}
+
+// +kubebuilder:webhook:path=/validate-ray-io-v1alpha1-rayjob,mutating=false,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=update,versions=v1alpha1,name=vrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomValidator = &RayJobWebhook{}
+
+// ValidateCreate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *RayJobWebhook) ValidateCreate(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+	log.Info("Validating create", "job", klog.KObj(job))
+	return w.validateCreate(job).ToAggregate()
+}
+
+func (w *RayJobWebhook) validateCreate(job *rayjobapi.RayJob) field.ErrorList {
+	var allErrors field.ErrorList
+	kueueJob := (*RayJob)(job)
+
+	if w.manageJobsWithoutQueueName || jobframework.QueueName(kueueJob) != "" {
+		spec := &job.Spec
+		specPath := field.NewPath("spec")
+
+		if !spec.ShutdownAfterJobFinishes {
+			allErrors = append(allErrors, field.Invalid(specPath.Child("shutdownAfterJobFinishes"), spec.ShutdownAfterJobFinishes, "a kueue managed job should delete the cluster after finishing"))
+		}
+		// should not want existing cluster
+		if len(spec.ClusterSelector) > 0 {
+			allErrors = append(allErrors, field.Invalid(specPath.Child("clusterSelector"), spec.ClusterSelector, "a kueue managed job should not use an existing cluster"))
+		}
+
+		clusterSpec := spec.RayClusterSpec
+		clusterSpecPath := specPath.Child("rayClusterSpec")
+
+		// should not use auto scaler
+		if pointer.BoolDeref(clusterSpec.EnableInTreeAutoscaling, false) {
+			allErrors = append(allErrors, field.Invalid(clusterSpecPath.Child("enableInTreeAutoscaling"), clusterSpec.EnableInTreeAutoscaling, "a kueue managed job should not use autoscaling"))
+		}
+		// should limit the worker count to 8 - 1 (max podSets num - cluster head)
+		if len(clusterSpec.WorkerGroupSpecs) > 7 {
+			allErrors = append(allErrors, field.TooMany(clusterSpecPath.Child("workerGroupSpecs"), len(clusterSpec.WorkerGroupSpecs), 7))
+		}
+	}
+
+	allErrors = append(allErrors, jobframework.ValidateAnnotationAsCRDName(kueueJob, jobframework.QueueLabel)...)
```

  ```suggestion
  	allErrors = append(allErrors, jobframework.ValidateCreateForQueueName(job)...)
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-17T18:40:30Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:77** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-17T18:40:30Z
```diff
@@ -0,0 +1,282 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk = schema.GroupVersionKind{
+		Group:   rayjobapi.GroupVersion.Group,
+		Version: rayjobapi.GroupVersion.Version,
+		Kind:    "RayJob",
+	}
+
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
```

  ```suggestion
  type RayJob struct {
  	*rayjobapi.RayJob
  }
  
  func (j *RayJob) Object() client.Object {
  	return j.RayJob
  }
  ```
  
  Avoiding casting like this would be good.  If we do this, we can avoid casting in webhook.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-18T04:57:37Z

- **Makefile:302** by [@trasc](https://github.com/trasc) — 2023-05-18T04:57:37Z
```diff
@@ -286,3 +286,17 @@ mpi-operator-crd:
 	GOPATH=/tmp GO111MODULE=on $(GO_CMD) install github.com/kubeflow/mpi-operator/cmd/mpi-operator@$(MPI_VERSION)
 	mkdir -p $(shell pwd)/dep-crds/mpi-operator/
 	cp -f /tmp/pkg/mod/github.com/kubeflow/mpi-operator@$(MPI_VERSION)/manifests/base/* $(shell pwd)/dep-crds/mpi-operator/
+
+
+RAY_VERSOION := 9bc5d854bd316c46a79920bbd7b2887fb0c6d0af
+RAYTEMP := /tmp/kubreay-$(RAY_VERSOION)
+.PHONY: ray-operator-crd-git
+ray-operator-crd: | $(RAYTEMP)
+	mkdir -p $(shell pwd)/dep-crds/ray-operator/
+	cp -f $(RAYTEMP)/ray-operator/config/crd/bases/* $(shell pwd)/dep-crds/ray-operator/
+
+$(RAYTEMP):
+	git clone --depth=1 --filter=blob:none --sparse https://github.com/ray-project/kuberay.git $(RAYTEMP)
+	git -C $(RAYTEMP) fetch --depth=1 origin $(RAY_VERSOION)
+	git -C $(RAYTEMP) reset --hard FETCH_HEAD
+	git -C $(RAYTEMP) sparse-checkout set ray-operator/config/crd/bases
```

  The kuberay project is very big, Doing it like this is more than 10x faster on my machine.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-18T04:57:51Z

- **Makefile:291** by [@trasc](https://github.com/trasc) — 2023-05-18T04:57:51Z
```diff
@@ -286,3 +286,17 @@ mpi-operator-crd:
 	GOPATH=/tmp GO111MODULE=on $(GO_CMD) install github.com/kubeflow/mpi-operator/cmd/mpi-operator@$(MPI_VERSION)
 	mkdir -p $(shell pwd)/dep-crds/mpi-operator/
 	cp -f /tmp/pkg/mod/github.com/kubeflow/mpi-operator@$(MPI_VERSION)/manifests/base/* $(shell pwd)/dep-crds/mpi-operator/
+
+
+RAY_VERSOION := 9bc5d854bd316c46a79920bbd7b2887fb0c6d0af
```

  I'll give it a try

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-18T05:03:38Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:77** by [@trasc](https://github.com/trasc) — 2023-05-18T05:03:38Z
```diff
@@ -0,0 +1,282 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk = schema.GroupVersionKind{
+		Group:   rayjobapi.GroupVersion.Group,
+		Version: rayjobapi.GroupVersion.Version,
+		Kind:    "RayJob",
+	}
+
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
```

  I don't understand.  If we use the embedded  pointer version, we have an extra unnecessary allocation every time we instantiate a `kueue. RayJob`.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-18T06:27:42Z

- **Makefile:291** by [@trasc](https://github.com/trasc) — 2023-05-18T05:44:35Z
```diff
@@ -286,3 +286,17 @@ mpi-operator-crd:
 	GOPATH=/tmp GO111MODULE=on $(GO_CMD) install github.com/kubeflow/mpi-operator/cmd/mpi-operator@$(MPI_VERSION)
 	mkdir -p $(shell pwd)/dep-crds/mpi-operator/
 	cp -f /tmp/pkg/mod/github.com/kubeflow/mpi-operator@$(MPI_VERSION)/manifests/base/* $(shell pwd)/dep-crds/mpi-operator/
+
+
+RAY_VERSOION := 9bc5d854bd316c46a79920bbd7b2887fb0c6d0af
```

  done

- **Makefile:302** by [@trasc](https://github.com/trasc) — 2023-05-18T05:45:43Z
```diff
@@ -286,3 +286,17 @@ mpi-operator-crd:
 	GOPATH=/tmp GO111MODULE=on $(GO_CMD) install github.com/kubeflow/mpi-operator/cmd/mpi-operator@$(MPI_VERSION)
 	mkdir -p $(shell pwd)/dep-crds/mpi-operator/
 	cp -f /tmp/pkg/mod/github.com/kubeflow/mpi-operator@$(MPI_VERSION)/manifests/base/* $(shell pwd)/dep-crds/mpi-operator/
+
+
+RAY_VERSOION := 9bc5d854bd316c46a79920bbd7b2887fb0c6d0af
+RAYTEMP := /tmp/kubreay-$(RAY_VERSOION)
+.PHONY: ray-operator-crd-git
+ray-operator-crd: | $(RAYTEMP)
+	mkdir -p $(shell pwd)/dep-crds/ray-operator/
+	cp -f $(RAYTEMP)/ray-operator/config/crd/bases/* $(shell pwd)/dep-crds/ray-operator/
+
+$(RAYTEMP):
+	git clone --depth=1 --filter=blob:none --sparse https://github.com/ray-project/kuberay.git $(RAYTEMP)
+	git -C $(RAYTEMP) fetch --depth=1 origin $(RAY_VERSOION)
+	git -C $(RAYTEMP) reset --hard FETCH_HEAD
+	git -C $(RAYTEMP) sparse-checkout set ray-operator/config/crd/bases
```

  Looks that we can also get the yamls from go's mod dir

- **pkg/controller/jobs/rayjob/rayjob_controller.go:41** by [@trasc](https://github.com/trasc) — 2023-05-18T05:49:56Z
```diff
@@ -0,0 +1,282 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk = schema.GroupVersionKind{
+		Group:   rayjobapi.GroupVersion.Group,
+		Version: rayjobapi.GroupVersion.Version,
+		Kind:    "RayJob",
```

  thanks, done

- **pkg/controller/jobs/rayjob/rayjob_controller.go:134** by [@trasc](https://github.com/trasc) — 2023-05-18T05:52:05Z
```diff
@@ -0,0 +1,282 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk = schema.GroupVersionKind{
+		Group:   rayjobapi.GroupVersion.Group,
+		Version: rayjobapi.GroupVersion.Version,
+		Kind:    "RayJob",
+	}
+
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
```

  it's checked by len at line 130

- **pkg/controller/jobs/rayjob/rayjob_controller.go:148** by [@trasc](https://github.com/trasc) — 2023-05-18T06:01:52Z
```diff
@@ -0,0 +1,282 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk = schema.GroupVersionKind{
+		Group:   rayjobapi.GroupVersion.Group,
+		Version: rayjobapi.GroupVersion.Version,
+		Kind:    "RayJob",
+	}
+
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) == 0 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
```

  if `nodeSelectors[0].NodeSelector` is empty , applySelectors will just return without doing a single loop

- **pkg/controller/jobs/rayjob/rayjob_controller.go:174** by [@trasc](https://github.com/trasc) — 2023-05-18T06:04:46Z
```diff
@@ -0,0 +1,282 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk = schema.GroupVersionKind{
+		Group:   rayjobapi.GroupVersion.Group,
+		Version: rayjobapi.GroupVersion.Version,
+		Kind:    "RayJob",
+	}
+
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) == 0 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if !equality.Semantic.DeepEqual(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector) {
+		headPodSpec.NodeSelector = cloneSelectors(nodeSelectors[0].NodeSelector)
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = cloneSelectors(nodeSelectors[index+1].NodeSelector)
```

  done

- **pkg/controller/jobs/rayjob/rayjob_controller.go:187** by [@trasc](https://github.com/trasc) — 2023-05-18T06:08:09Z
```diff
@@ -0,0 +1,282 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk = schema.GroupVersionKind{
+		Group:   rayjobapi.GroupVersion.Group,
+		Version: rayjobapi.GroupVersion.Version,
+		Kind:    "RayJob",
+	}
+
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) == 0 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if !equality.Semantic.DeepEqual(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector) {
+		headPodSpec.NodeSelector = cloneSelectors(nodeSelectors[0].NodeSelector)
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = cloneSelectors(nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func (j *RayJob) Finished() (metav1.Condition, bool) {
+	condition := metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  string(j.Status.JobStatus),
+		Message: j.Status.Message,
+	}
+	finished := false
+	switch j.Status.JobDeploymentStatus {
+	case rayjobapi.JobDeploymentStatusComplete, rayjobapi.JobDeploymentStatusFailedJobDeploy:
```

  sure, done

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:107** by [@trasc](https://github.com/trasc) — 2023-05-18T06:18:22Z
```diff
@@ -0,0 +1,128 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+type RayJobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for rayjobapi RayJob.
+func SetupRayJobWebhook(mgr ctrl.Manager, opts ...jobframework.Option) error {
+	options := jobframework.DefaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &RayJobWebhook{
+		manageJobsWithoutQueueName: options.ManageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&rayjobapi.RayJob{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-ray-io-v1alpha1-rayjob,mutating=true,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=create,versions=v1alpha1,name=mrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &RayJobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *RayJobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
+	jobframework.ApplyDefaultForSuspend((*RayJob)(job), w.manageJobsWithoutQueueName)
+	return nil
+}
+
+// +kubebuilder:webhook:path=/validate-ray-io-v1alpha1-rayjob,mutating=false,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=update,versions=v1alpha1,name=vrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomValidator = &RayJobWebhook{}
+
+// ValidateCreate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *RayJobWebhook) ValidateCreate(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+	log.Info("Validating create", "job", klog.KObj(job))
+	return w.validateCreate(job).ToAggregate()
+}
+
+func (w *RayJobWebhook) validateCreate(job *rayjobapi.RayJob) field.ErrorList {
+	var allErrors field.ErrorList
+	kueueJob := (*RayJob)(job)
+
+	if w.manageJobsWithoutQueueName || jobframework.QueueName(kueueJob) != "" {
+		spec := &job.Spec
+		specPath := field.NewPath("spec")
+
+		if !spec.ShutdownAfterJobFinishes {
+			allErrors = append(allErrors, field.Invalid(specPath.Child("shutdownAfterJobFinishes"), spec.ShutdownAfterJobFinishes, "a kueue managed job should delete the cluster after finishing"))
+		}
+		// should not want existing cluster
+		if len(spec.ClusterSelector) > 0 {
+			allErrors = append(allErrors, field.Invalid(specPath.Child("clusterSelector"), spec.ClusterSelector, "a kueue managed job should not use an existing cluster"))
+		}
+
+		clusterSpec := spec.RayClusterSpec
+		clusterSpecPath := specPath.Child("rayClusterSpec")
+
+		// should not use auto scaler
+		if pointer.BoolDeref(clusterSpec.EnableInTreeAutoscaling, false) {
+			allErrors = append(allErrors, field.Invalid(clusterSpecPath.Child("enableInTreeAutoscaling"), clusterSpec.EnableInTreeAutoscaling, "a kueue managed job should not use autoscaling"))
+		}
+		// should limit the worker count to 8 - 1 (max podSets num - cluster head)
+		if len(clusterSpec.WorkerGroupSpecs) > 7 {
+			allErrors = append(allErrors, field.TooMany(clusterSpecPath.Child("workerGroupSpecs"), len(clusterSpec.WorkerGroupSpecs), 7))
+		}
+	}
+
+	allErrors = append(allErrors, jobframework.ValidateAnnotationAsCRDName(kueueJob, jobframework.QueueLabel)...)
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-18T06:37:47Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:105** by [@trasc](https://github.com/trasc) — 2023-05-18T06:37:47Z
```diff
@@ -0,0 +1,282 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk = schema.GroupVersionKind{
+		Group:   rayjobapi.GroupVersion.Group,
+		Version: rayjobapi.GroupVersion.Version,
+		Kind:    "RayJob",
+	}
+
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
```

  Nothing specific.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-05-18T15:58:54Z

Give a general look for the controller, will continue tomorrow.

- **pkg/controller/jobs/rayjob/rayjob_controller.go:52** by [@kerthcet](https://github.com/kerthcet) — 2023-05-18T14:50:02Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
```

  ```suggestion
  // RayJobReconciler reconciles a RayJob object
  ```

- **pkg/controller/jobs/rayjob/rayjob_controller.go:77** by [@kerthcet](https://github.com/kerthcet) — 2023-05-18T15:07:26Z
```diff
@@ -0,0 +1,282 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk = schema.GroupVersionKind{
+		Group:   rayjobapi.GroupVersion.Group,
+		Version: rayjobapi.GroupVersion.Version,
+		Kind:    "RayJob",
+	}
+
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
```

  This is more clear, we should update MPIJob the same pattern, not related to the PR.

- **pkg/controller/jobs/rayjob/rayjob_controller.go:72** by [@kerthcet](https://github.com/kerthcet) — 2023-05-18T15:09:19Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
```

  Add a `var _ jobframework.GenericJob = &RayJob{}` static check.

- **go.mod:14** by [@kerthcet](https://github.com/kerthcet) — 2023-05-18T15:11:48Z
```diff
@@ -11,6 +11,7 @@ require (
 	github.com/onsi/gomega v1.27.6
 	github.com/open-policy-agent/cert-controller v0.7.0
 	github.com/prometheus/client_golang v1.15.1
+	github.com/ray-project/kuberay/ray-operator v0.0.0-20230516192117-9bc5d854bd31
```

  This is not a released version, please open an issue to track this, when kuberay published a new release, we should bump to it.

- **pkg/controller/jobs/rayjob/rayjob_controller.go:84** by [@kerthcet](https://github.com/kerthcet) — 2023-05-18T15:14:08Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
```

  I haven't checked the kuberay operator's logic yet, but does `status==suspend` means all pods are cleaned up? I guess we update the status first, then cleanup the pods, then it's not right here.

- **pkg/controller/jobs/rayjob/rayjob_controller.go:105** by [@kerthcet](https://github.com/kerthcet) — 2023-05-18T15:22:18Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
```

  Can we use the raycluster name?

- **pkg/controller/jobs/rayjob/rayjob_controller.go:153** by [@kerthcet](https://github.com/kerthcet) — 2023-05-18T15:34:02Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) == 0 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
```

  Better to check whether nodeSelector[index+1] is valid here. It's more safe and we may support partial admission in the future.

- **pkg/controller/jobs/rayjob/rayjob_controller.go:179** by [@kerthcet](https://github.com/kerthcet) — 2023-05-18T15:36:18Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) == 0 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if !equality.Semantic.DeepEqual(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector) {
+		headPodSpec.NodeSelector = cloneSelectors(nodeSelectors[0].NodeSelector)
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector) {
```

  ditto

- **pkg/controller/jobs/rayjob/rayjob_controller.go:173** by [@kerthcet](https://github.com/kerthcet) — 2023-05-18T15:41:36Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) == 0 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if !equality.Semantic.DeepEqual(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector) {
+		headPodSpec.NodeSelector = cloneSelectors(nodeSelectors[0].NodeSelector)
```

  I think the logic is same across all Jobs e.g. MPIJob, so we can make it a util function, but it can be a follow-up, you can create an issue to track this in case you forget.

- **pkg/controller/jobs/rayjob/rayjob_controller.go:186** by [@kerthcet](https://github.com/kerthcet) — 2023-05-18T15:44:19Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) == 0 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if !equality.Semantic.DeepEqual(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector) {
+		headPodSpec.NodeSelector = cloneSelectors(nodeSelectors[0].NodeSelector)
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector) {
+			workerPodSpec.NodeSelector = cloneSelectors(nodeSelectors[index+1].NodeSelector)
+		}
+	}
+}
+
+func (j *RayJob) Finished() (metav1.Condition, bool) {
+	condition := metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  string(j.Status.JobStatus),
+		Message: j.Status.Message,
+	}
+	finished := false
+	switch j.Status.JobDeploymentStatus {
+	case rayjobapi.JobDeploymentStatusComplete, rayjobapi.JobDeploymentStatusFailedJobDeploy, rayjobapi.JobDeploymentStatusFailedToGetOrCreateRayCluster:
+		finished = true
+
```

  Remove the blank line

- **pkg/controller/jobs/rayjob/rayjob_controller.go:236** by [@kerthcet](https://github.com/kerthcet) — 2023-05-18T15:56:11Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) == 0 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if !equality.Semantic.DeepEqual(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector) {
+		headPodSpec.NodeSelector = cloneSelectors(nodeSelectors[0].NodeSelector)
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector) {
+			workerPodSpec.NodeSelector = cloneSelectors(nodeSelectors[index+1].NodeSelector)
+		}
+	}
+}
+
+func (j *RayJob) Finished() (metav1.Condition, bool) {
+	condition := metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  string(j.Status.JobStatus),
+		Message: j.Status.Message,
+	}
+	finished := false
+	switch j.Status.JobDeploymentStatus {
+	case rayjobapi.JobDeploymentStatusComplete, rayjobapi.JobDeploymentStatusFailedJobDeploy, rayjobapi.JobDeploymentStatusFailedToGetOrCreateRayCluster:
+		finished = true
+
+	default:
+		finished = false
+	}
+	return condition, finished
+}
+
+func (j *RayJob) EquivalentToWorkload(wl kueue.Workload) bool {
+	if len(wl.Spec.PodSets) != (len(j.Spec.RayClusterSpec.WorkerGroupSpecs) + 1) {
+		return false
+	}
+
+	podSets := wl.Spec.PodSets
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if podSets[0].Count != 1 {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.InitContainers, podSets[0].Template.Spec.InitContainers) {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.Containers, podSets[0].Template.Spec.Containers) {
+		return false
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		if podSets[index+1].Count != *j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Replicas {
+			return false
+		}
+
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.InitContainers, podSets[index+1].Template.Spec.InitContainers) {
+			return false
+		}
+		if !equality.Semantic.DeepEqual(workerPodSpec.Containers, podSets[index+1].Template.Spec.Containers) {
+			return false
+		}
+	}
+	return true
+}
+
+func (j *RayJob) PriorityClass() string {
+	if j.Spec.RayClusterSpec != nil {
+		rcs := j.Spec.RayClusterSpec
+		if len(rcs.HeadGroupSpec.Template.Spec.PriorityClassName) != 0 {
+			return rcs.HeadGroupSpec.Template.Spec.PriorityClassName
+		}
+		for wi := range rcs.WorkerGroupSpecs {
```

  I'm not sure, but maybe query the headGroup's priorityClassName is enough? For workers will start after the header.

- **pkg/controller/jobs/rayjob/rayjob_controller.go:251** by [@kerthcet](https://github.com/kerthcet) — 2023-05-18T15:57:23Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) == 0 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if !equality.Semantic.DeepEqual(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector) {
+		headPodSpec.NodeSelector = cloneSelectors(nodeSelectors[0].NodeSelector)
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector) {
+			workerPodSpec.NodeSelector = cloneSelectors(nodeSelectors[index+1].NodeSelector)
+		}
+	}
+}
+
+func (j *RayJob) Finished() (metav1.Condition, bool) {
+	condition := metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  string(j.Status.JobStatus),
+		Message: j.Status.Message,
+	}
+	finished := false
+	switch j.Status.JobDeploymentStatus {
+	case rayjobapi.JobDeploymentStatusComplete, rayjobapi.JobDeploymentStatusFailedJobDeploy, rayjobapi.JobDeploymentStatusFailedToGetOrCreateRayCluster:
+		finished = true
+
+	default:
+		finished = false
+	}
+	return condition, finished
+}
+
+func (j *RayJob) EquivalentToWorkload(wl kueue.Workload) bool {
+	if len(wl.Spec.PodSets) != (len(j.Spec.RayClusterSpec.WorkerGroupSpecs) + 1) {
+		return false
+	}
+
+	podSets := wl.Spec.PodSets
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if podSets[0].Count != 1 {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.InitContainers, podSets[0].Template.Spec.InitContainers) {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.Containers, podSets[0].Template.Spec.Containers) {
+		return false
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		if podSets[index+1].Count != *j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Replicas {
+			return false
+		}
+
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.InitContainers, podSets[index+1].Template.Spec.InitContainers) {
+			return false
+		}
+		if !equality.Semantic.DeepEqual(workerPodSpec.Containers, podSets[index+1].Template.Spec.Containers) {
+			return false
+		}
+	}
+	return true
+}
+
+func (j *RayJob) PriorityClass() string {
+	if j.Spec.RayClusterSpec != nil {
+		rcs := j.Spec.RayClusterSpec
+		if len(rcs.HeadGroupSpec.Template.Spec.PriorityClassName) != 0 {
+			return rcs.HeadGroupSpec.Template.Spec.PriorityClassName
+		}
+		for wi := range rcs.WorkerGroupSpecs {
+			w := &rcs.WorkerGroupSpecs[wi]
+			if len(w.Template.Spec.PriorityClassName) != 0 {
+				return w.Template.Spec.PriorityClassName
+			}
+		}
+	}
+	return ""
+}
+
+func (j *RayJob) PodsReady() bool {
+	return j.Status.RayClusterStatus.State == rayjobapi.Ready
+}
+
+func (j *RayJob) ReclaimablePods() []kueue.ReclaimablePod {
+	return nil
```

  Is this finished?

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-05-19T07:14:17Z

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:60** by [@kerthcet](https://github.com/kerthcet) — 2023-05-19T02:33:25Z
```diff
@@ -0,0 +1,128 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+type RayJobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for rayjobapi RayJob.
+func SetupRayJobWebhook(mgr ctrl.Manager, opts ...jobframework.Option) error {
+	options := jobframework.DefaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &RayJobWebhook{
+		manageJobsWithoutQueueName: options.ManageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&rayjobapi.RayJob{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-ray-io-v1alpha1-rayjob,mutating=true,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=create,versions=v1alpha1,name=mrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &RayJobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *RayJobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
```

  ```suggestion
  	log := ctrl.LoggerFrom(ctx).WithName("rayjob-webhook")
  ```

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:73** by [@kerthcet](https://github.com/kerthcet) — 2023-05-19T02:35:14Z
```diff
@@ -0,0 +1,128 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+type RayJobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for rayjobapi RayJob.
+func SetupRayJobWebhook(mgr ctrl.Manager, opts ...jobframework.Option) error {
+	options := jobframework.DefaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &RayJobWebhook{
+		manageJobsWithoutQueueName: options.ManageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&rayjobapi.RayJob{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-ray-io-v1alpha1-rayjob,mutating=true,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=create,versions=v1alpha1,name=mrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &RayJobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *RayJobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
+	jobframework.ApplyDefaultForSuspend((*RayJob)(job), w.manageJobsWithoutQueueName)
+	return nil
+}
+
+// +kubebuilder:webhook:path=/validate-ray-io-v1alpha1-rayjob,mutating=false,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=update,versions=v1alpha1,name=vrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomValidator = &RayJobWebhook{}
+
+// ValidateCreate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *RayJobWebhook) ValidateCreate(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
```

  ditto

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:88** by [@kerthcet](https://github.com/kerthcet) — 2023-05-19T02:36:17Z
```diff
@@ -0,0 +1,128 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+type RayJobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for rayjobapi RayJob.
+func SetupRayJobWebhook(mgr ctrl.Manager, opts ...jobframework.Option) error {
+	options := jobframework.DefaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &RayJobWebhook{
+		manageJobsWithoutQueueName: options.ManageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&rayjobapi.RayJob{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-ray-io-v1alpha1-rayjob,mutating=true,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=create,versions=v1alpha1,name=mrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &RayJobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *RayJobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
+	jobframework.ApplyDefaultForSuspend((*RayJob)(job), w.manageJobsWithoutQueueName)
+	return nil
+}
+
+// +kubebuilder:webhook:path=/validate-ray-io-v1alpha1-rayjob,mutating=false,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=update,versions=v1alpha1,name=vrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomValidator = &RayJobWebhook{}
+
+// ValidateCreate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *RayJobWebhook) ValidateCreate(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+	log.Info("Validating create", "job", klog.KObj(job))
+	return w.validateCreate(job).ToAggregate()
+}
+
+func (w *RayJobWebhook) validateCreate(job *rayjobapi.RayJob) field.ErrorList {
+	var allErrors field.ErrorList
+	kueueJob := (*RayJob)(job)
+
+	if w.manageJobsWithoutQueueName || jobframework.QueueName(kueueJob) != "" {
+		spec := &job.Spec
+		specPath := field.NewPath("spec")
+
+		if !spec.ShutdownAfterJobFinishes {
```

  Please add explanation here: why should this be enabled.

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:89** by [@kerthcet](https://github.com/kerthcet) — 2023-05-19T03:58:25Z
```diff
@@ -0,0 +1,128 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+type RayJobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for rayjobapi RayJob.
+func SetupRayJobWebhook(mgr ctrl.Manager, opts ...jobframework.Option) error {
+	options := jobframework.DefaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &RayJobWebhook{
+		manageJobsWithoutQueueName: options.ManageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&rayjobapi.RayJob{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-ray-io-v1alpha1-rayjob,mutating=true,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=create,versions=v1alpha1,name=mrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &RayJobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *RayJobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
+	jobframework.ApplyDefaultForSuspend((*RayJob)(job), w.manageJobsWithoutQueueName)
+	return nil
+}
+
+// +kubebuilder:webhook:path=/validate-ray-io-v1alpha1-rayjob,mutating=false,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=update,versions=v1alpha1,name=vrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomValidator = &RayJobWebhook{}
+
+// ValidateCreate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *RayJobWebhook) ValidateCreate(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+	log.Info("Validating create", "job", klog.KObj(job))
+	return w.validateCreate(job).ToAggregate()
+}
+
+func (w *RayJobWebhook) validateCreate(job *rayjobapi.RayJob) field.ErrorList {
+	var allErrors field.ErrorList
+	kueueJob := (*RayJob)(job)
+
+	if w.manageJobsWithoutQueueName || jobframework.QueueName(kueueJob) != "" {
+		spec := &job.Spec
+		specPath := field.NewPath("spec")
+
+		if !spec.ShutdownAfterJobFinishes {
+			allErrors = append(allErrors, field.Invalid(specPath.Child("shutdownAfterJobFinishes"), spec.ShutdownAfterJobFinishes, "a kueue managed job should delete the cluster after finishing"))
+		}
+		// should not want existing cluster
```

  Better to explain why rather than what

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:97** by [@kerthcet](https://github.com/kerthcet) — 2023-05-19T03:59:41Z
```diff
@@ -0,0 +1,128 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+type RayJobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for rayjobapi RayJob.
+func SetupRayJobWebhook(mgr ctrl.Manager, opts ...jobframework.Option) error {
+	options := jobframework.DefaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &RayJobWebhook{
+		manageJobsWithoutQueueName: options.ManageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&rayjobapi.RayJob{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-ray-io-v1alpha1-rayjob,mutating=true,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=create,versions=v1alpha1,name=mrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &RayJobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *RayJobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
+	jobframework.ApplyDefaultForSuspend((*RayJob)(job), w.manageJobsWithoutQueueName)
+	return nil
+}
+
+// +kubebuilder:webhook:path=/validate-ray-io-v1alpha1-rayjob,mutating=false,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=update,versions=v1alpha1,name=vrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomValidator = &RayJobWebhook{}
+
+// ValidateCreate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *RayJobWebhook) ValidateCreate(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+	log.Info("Validating create", "job", klog.KObj(job))
+	return w.validateCreate(job).ToAggregate()
+}
+
+func (w *RayJobWebhook) validateCreate(job *rayjobapi.RayJob) field.ErrorList {
+	var allErrors field.ErrorList
+	kueueJob := (*RayJob)(job)
+
+	if w.manageJobsWithoutQueueName || jobframework.QueueName(kueueJob) != "" {
+		spec := &job.Spec
+		specPath := field.NewPath("spec")
+
+		if !spec.ShutdownAfterJobFinishes {
+			allErrors = append(allErrors, field.Invalid(specPath.Child("shutdownAfterJobFinishes"), spec.ShutdownAfterJobFinishes, "a kueue managed job should delete the cluster after finishing"))
+		}
+		// should not want existing cluster
+		if len(spec.ClusterSelector) > 0 {
+			allErrors = append(allErrors, field.Invalid(specPath.Child("clusterSelector"), spec.ClusterSelector, "a kueue managed job should not use an existing cluster"))
+		}
+
+		clusterSpec := spec.RayClusterSpec
+		clusterSpecPath := specPath.Child("rayClusterSpec")
+
+		// should not use auto scaler
```

  I think this is fatal if we don't support auto scaler, I think we need to support this in the future.

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:115** by [@kerthcet](https://github.com/kerthcet) — 2023-05-19T06:03:03Z
```diff
@@ -0,0 +1,128 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+type RayJobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for rayjobapi RayJob.
+func SetupRayJobWebhook(mgr ctrl.Manager, opts ...jobframework.Option) error {
+	options := jobframework.DefaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &RayJobWebhook{
+		manageJobsWithoutQueueName: options.ManageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&rayjobapi.RayJob{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-ray-io-v1alpha1-rayjob,mutating=true,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=create,versions=v1alpha1,name=mrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &RayJobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *RayJobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
+	jobframework.ApplyDefaultForSuspend((*RayJob)(job), w.manageJobsWithoutQueueName)
+	return nil
+}
+
+// +kubebuilder:webhook:path=/validate-ray-io-v1alpha1-rayjob,mutating=false,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=update,versions=v1alpha1,name=vrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomValidator = &RayJobWebhook{}
+
+// ValidateCreate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *RayJobWebhook) ValidateCreate(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+	log.Info("Validating create", "job", klog.KObj(job))
+	return w.validateCreate(job).ToAggregate()
+}
+
+func (w *RayJobWebhook) validateCreate(job *rayjobapi.RayJob) field.ErrorList {
+	var allErrors field.ErrorList
+	kueueJob := (*RayJob)(job)
+
+	if w.manageJobsWithoutQueueName || jobframework.QueueName(kueueJob) != "" {
+		spec := &job.Spec
+		specPath := field.NewPath("spec")
+
+		if !spec.ShutdownAfterJobFinishes {
+			allErrors = append(allErrors, field.Invalid(specPath.Child("shutdownAfterJobFinishes"), spec.ShutdownAfterJobFinishes, "a kueue managed job should delete the cluster after finishing"))
+		}
+		// should not want existing cluster
+		if len(spec.ClusterSelector) > 0 {
+			allErrors = append(allErrors, field.Invalid(specPath.Child("clusterSelector"), spec.ClusterSelector, "a kueue managed job should not use an existing cluster"))
+		}
+
+		clusterSpec := spec.RayClusterSpec
+		clusterSpecPath := specPath.Child("rayClusterSpec")
+
+		// should not use auto scaler
+		if pointer.BoolDeref(clusterSpec.EnableInTreeAutoscaling, false) {
+			allErrors = append(allErrors, field.Invalid(clusterSpecPath.Child("enableInTreeAutoscaling"), clusterSpec.EnableInTreeAutoscaling, "a kueue managed job should not use autoscaling"))
+		}
+		// should limit the worker count to 8 - 1 (max podSets num - cluster head)
+		if len(clusterSpec.WorkerGroupSpecs) > 7 {
+			allErrors = append(allErrors, field.TooMany(clusterSpecPath.Child("workerGroupSpecs"), len(clusterSpec.WorkerGroupSpecs), 7))
+		}
+	}
+
+	allErrors = append(allErrors, jobframework.ValidateCreateForQueueName(kueueJob)...)
+	return allErrors
+}
+
+// ValidateUpdate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *RayJobWebhook) ValidateUpdate(ctx context.Context, oldObj, newObj runtime.Object) error {
+	oldJob := oldObj.(*rayjobapi.RayJob)
+	newJob := newObj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
```

  ```suggestion
  	log := ctrl.LoggerFrom(ctx).WithName("rayjob-webhook")
  ```

- **pkg/util/testingjobs/rayjob/wrappers.go:107** by [@kerthcet](https://github.com/kerthcet) — 2023-05-19T07:11:43Z
```diff
@@ -0,0 +1,146 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+// JobWrapper wraps a RayJob.
+type JobWrapper struct{ rayjobapi.RayJob }
+
+// MakeJob creates a wrapper for a suspended rayJob
+func MakeJob(name, ns string) *JobWrapper {
+	return &JobWrapper{rayjobapi.RayJob{
+		ObjectMeta: metav1.ObjectMeta{
+			Name:        name,
+			Namespace:   ns,
+			Annotations: make(map[string]string, 1),
+		},
+		Spec: rayjobapi.RayJobSpec{
+			ShutdownAfterJobFinishes: true,
+			RayClusterSpec: &rayjobapi.RayClusterSpec{
+				HeadGroupSpec: rayjobapi.HeadGroupSpec{
+					RayStartParams: map[string]string{"p1": "v1"},
+					Template: corev1.PodTemplateSpec{
+						Spec: corev1.PodSpec{
+							Containers: []corev1.Container{
+								{
+									Name: "head-container",
+								},
+							},
+						},
+					},
+				},
+				WorkerGroupSpecs: []rayjobapi.WorkerGroupSpec{
+					{
+						GroupName:      "workers-group-0",
+						Replicas:       pointer.Int32(1),
+						MinReplicas:    pointer.Int32(0),
+						MaxReplicas:    pointer.Int32(10),
+						RayStartParams: map[string]string{"p1": "v1"},
+						Template: corev1.PodTemplateSpec{
+							Spec: corev1.PodSpec{
+								Containers: []corev1.Container{
+									{
+										Name: "worker-container",
+									},
+								},
+							},
+						},
+					},
+				},
+			},
+			Suspend: true,
+		},
+	}}
+}
+
+// Obj returns the inner Job.
+func (j *JobWrapper) Obj() *rayjobapi.RayJob {
+	return &j.RayJob
+}
+
+// Suspend updates the suspend status of the job
+func (j *JobWrapper) Suspend(s bool) *JobWrapper {
+	j.Spec.Suspend = s
+	return j
+}
+
+// Queue updates the queue name of the job
+func (j *JobWrapper) Queue(queue string) *JobWrapper {
+	if j.Labels == nil {
+		j.Labels = make(map[string]string)
+	}
+	j.Labels[jobframework.QueueLabel] = queue
+	return j
+}
+
+func (j *JobWrapper) RequestWorkerGroup(name corev1.ResourceName, quantity string) *JobWrapper {
+	c := &j.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.Containers[0]
+	if c.Resources.Requests == nil {
+		c.Resources.Requests = corev1.ResourceList{name: resource.MustParse(quantity)}
+	} else {
+		c.Resources.Requests[name] = resource.MustParse(quantity)
+	}
+	return j
+}
```

  Chore: add a blank line below.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-19T09:03:55Z

- **go.mod:14** by [@trasc](https://github.com/trasc) — 2023-05-19T06:45:47Z
```diff
@@ -11,6 +11,7 @@ require (
 	github.com/onsi/gomega v1.27.6
 	github.com/open-policy-agent/cert-controller v0.7.0
 	github.com/prometheus/client_golang v1.15.1
+	github.com/ray-project/kuberay/ray-operator v0.0.0-20230516192117-9bc5d854bd31
```

  done #781

- **pkg/controller/jobs/rayjob/rayjob_controller.go:52** by [@trasc](https://github.com/trasc) — 2023-05-19T07:56:46Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
```

  done

- **pkg/controller/jobs/rayjob/rayjob_controller.go:72** by [@trasc](https://github.com/trasc) — 2023-05-19T07:58:10Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
```

  done

- **pkg/controller/jobs/rayjob/rayjob_controller.go:84** by [@trasc](https://github.com/trasc) — 2023-05-19T08:05:03Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
```

  In short , it should be.
  
  `j.Spec.Suspend` is used to trigger the suspend
  `j.Status.JobDeploymentStatus` is set to `rayjobapi.JobDeploymentStatusSuspended` after the job no longer has a cluster associated with it.

- **pkg/controller/jobs/rayjob/rayjob_controller.go:105** by [@trasc](https://github.com/trasc) — 2023-05-19T08:07:39Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
```

  I guess we could, but it will look strange, we already have the name of the workload that can do the connection to the job a log line is referring to.

- **pkg/controller/jobs/rayjob/rayjob_controller.go:153** by [@trasc](https://github.com/trasc) — 2023-05-19T08:13:02Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) == 0 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
```

  updated the initial partial len check

- **pkg/controller/jobs/rayjob/rayjob_controller.go:173** by [@trasc](https://github.com/trasc) — 2023-05-19T08:19:08Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) == 0 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if !equality.Semantic.DeepEqual(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector) {
+		headPodSpec.NodeSelector = cloneSelectors(nodeSelectors[0].NodeSelector)
```

  done #782

- **pkg/controller/jobs/rayjob/rayjob_controller.go:179** by [@trasc](https://github.com/trasc) — 2023-05-19T08:19:29Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) == 0 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if !equality.Semantic.DeepEqual(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector) {
+		headPodSpec.NodeSelector = cloneSelectors(nodeSelectors[0].NodeSelector)
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector) {
```

  if for the index, done

- **pkg/controller/jobs/rayjob/rayjob_controller.go:186** by [@trasc](https://github.com/trasc) — 2023-05-19T08:20:08Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) == 0 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if !equality.Semantic.DeepEqual(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector) {
+		headPodSpec.NodeSelector = cloneSelectors(nodeSelectors[0].NodeSelector)
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector) {
+			workerPodSpec.NodeSelector = cloneSelectors(nodeSelectors[index+1].NodeSelector)
+		}
+	}
+}
+
+func (j *RayJob) Finished() (metav1.Condition, bool) {
+	condition := metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  string(j.Status.JobStatus),
+		Message: j.Status.Message,
+	}
+	finished := false
+	switch j.Status.JobDeploymentStatus {
+	case rayjobapi.JobDeploymentStatusComplete, rayjobapi.JobDeploymentStatusFailedJobDeploy, rayjobapi.JobDeploymentStatusFailedToGetOrCreateRayCluster:
+		finished = true
+
```

  done

- **pkg/controller/jobs/rayjob/rayjob_controller.go:236** by [@trasc](https://github.com/trasc) — 2023-05-19T08:21:50Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) == 0 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if !equality.Semantic.DeepEqual(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector) {
+		headPodSpec.NodeSelector = cloneSelectors(nodeSelectors[0].NodeSelector)
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector) {
+			workerPodSpec.NodeSelector = cloneSelectors(nodeSelectors[index+1].NodeSelector)
+		}
+	}
+}
+
+func (j *RayJob) Finished() (metav1.Condition, bool) {
+	condition := metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  string(j.Status.JobStatus),
+		Message: j.Status.Message,
+	}
+	finished := false
+	switch j.Status.JobDeploymentStatus {
+	case rayjobapi.JobDeploymentStatusComplete, rayjobapi.JobDeploymentStatusFailedJobDeploy, rayjobapi.JobDeploymentStatusFailedToGetOrCreateRayCluster:
+		finished = true
+
+	default:
+		finished = false
+	}
+	return condition, finished
+}
+
+func (j *RayJob) EquivalentToWorkload(wl kueue.Workload) bool {
+	if len(wl.Spec.PodSets) != (len(j.Spec.RayClusterSpec.WorkerGroupSpecs) + 1) {
+		return false
+	}
+
+	podSets := wl.Spec.PodSets
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if podSets[0].Count != 1 {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.InitContainers, podSets[0].Template.Spec.InitContainers) {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.Containers, podSets[0].Template.Spec.Containers) {
+		return false
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		if podSets[index+1].Count != *j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Replicas {
+			return false
+		}
+
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.InitContainers, podSets[index+1].Template.Spec.InitContainers) {
+			return false
+		}
+		if !equality.Semantic.DeepEqual(workerPodSpec.Containers, podSets[index+1].Template.Spec.Containers) {
+			return false
+		}
+	}
+	return true
+}
+
+func (j *RayJob) PriorityClass() string {
+	if j.Spec.RayClusterSpec != nil {
+		rcs := j.Spec.RayClusterSpec
+		if len(rcs.HeadGroupSpec.Template.Spec.PriorityClassName) != 0 {
+			return rcs.HeadGroupSpec.Template.Spec.PriorityClassName
+		}
+		for wi := range rcs.WorkerGroupSpecs {
```

  It's the same approach as in MPIJob,  we need it try our bests to give the right priority to the workload.

- **pkg/controller/jobs/rayjob/rayjob_controller.go:251** by [@trasc](https://github.com/trasc) — 2023-05-19T08:25:52Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) == 0 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if !equality.Semantic.DeepEqual(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector) {
+		headPodSpec.NodeSelector = cloneSelectors(nodeSelectors[0].NodeSelector)
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector) {
+			workerPodSpec.NodeSelector = cloneSelectors(nodeSelectors[index+1].NodeSelector)
+		}
+	}
+}
+
+func (j *RayJob) Finished() (metav1.Condition, bool) {
+	condition := metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  string(j.Status.JobStatus),
+		Message: j.Status.Message,
+	}
+	finished := false
+	switch j.Status.JobDeploymentStatus {
+	case rayjobapi.JobDeploymentStatusComplete, rayjobapi.JobDeploymentStatusFailedJobDeploy, rayjobapi.JobDeploymentStatusFailedToGetOrCreateRayCluster:
+		finished = true
+
+	default:
+		finished = false
+	}
+	return condition, finished
+}
+
+func (j *RayJob) EquivalentToWorkload(wl kueue.Workload) bool {
+	if len(wl.Spec.PodSets) != (len(j.Spec.RayClusterSpec.WorkerGroupSpecs) + 1) {
+		return false
+	}
+
+	podSets := wl.Spec.PodSets
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if podSets[0].Count != 1 {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.InitContainers, podSets[0].Template.Spec.InitContainers) {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.Containers, podSets[0].Template.Spec.Containers) {
+		return false
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		if podSets[index+1].Count != *j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Replicas {
+			return false
+		}
+
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.InitContainers, podSets[index+1].Template.Spec.InitContainers) {
+			return false
+		}
+		if !equality.Semantic.DeepEqual(workerPodSpec.Containers, podSets[index+1].Template.Spec.Containers) {
+			return false
+		}
+	}
+	return true
+}
+
+func (j *RayJob) PriorityClass() string {
+	if j.Spec.RayClusterSpec != nil {
+		rcs := j.Spec.RayClusterSpec
+		if len(rcs.HeadGroupSpec.Template.Spec.PriorityClassName) != 0 {
+			return rcs.HeadGroupSpec.Template.Spec.PriorityClassName
+		}
+		for wi := range rcs.WorkerGroupSpecs {
+			w := &rcs.WorkerGroupSpecs[wi]
+			if len(w.Template.Spec.PriorityClassName) != 0 {
+				return w.Template.Spec.PriorityClassName
+			}
+		}
+	}
+	return ""
+}
+
+func (j *RayJob) PodsReady() bool {
+	return j.Status.RayClusterStatus.State == rayjobapi.Ready
+}
+
+func (j *RayJob) ReclaimablePods() []kueue.ReclaimablePod {
+	return nil
```

  Unfortunately looks like it, The RayCluster (actual worker) is design to stay up an potentially serve multiple jobs. Even if we allow it's scaling capabilities, there is now way of ensuring that after one worker group was scaled down it will not be scaled up again.

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:60** by [@trasc](https://github.com/trasc) — 2023-05-19T08:31:28Z
```diff
@@ -0,0 +1,128 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+type RayJobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for rayjobapi RayJob.
+func SetupRayJobWebhook(mgr ctrl.Manager, opts ...jobframework.Option) error {
+	options := jobframework.DefaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &RayJobWebhook{
+		manageJobsWithoutQueueName: options.ManageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&rayjobapi.RayJob{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-ray-io-v1alpha1-rayjob,mutating=true,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=create,versions=v1alpha1,name=mrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &RayJobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *RayJobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
```

  done

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:73** by [@trasc](https://github.com/trasc) — 2023-05-19T08:31:37Z
```diff
@@ -0,0 +1,128 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+type RayJobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for rayjobapi RayJob.
+func SetupRayJobWebhook(mgr ctrl.Manager, opts ...jobframework.Option) error {
+	options := jobframework.DefaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &RayJobWebhook{
+		manageJobsWithoutQueueName: options.ManageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&rayjobapi.RayJob{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-ray-io-v1alpha1-rayjob,mutating=true,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=create,versions=v1alpha1,name=mrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &RayJobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *RayJobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
+	jobframework.ApplyDefaultForSuspend((*RayJob)(job), w.manageJobsWithoutQueueName)
+	return nil
+}
+
+// +kubebuilder:webhook:path=/validate-ray-io-v1alpha1-rayjob,mutating=false,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=update,versions=v1alpha1,name=vrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomValidator = &RayJobWebhook{}
+
+// ValidateCreate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *RayJobWebhook) ValidateCreate(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
```

  done

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:97** by [@trasc](https://github.com/trasc) — 2023-05-19T08:32:51Z
```diff
@@ -0,0 +1,128 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+type RayJobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for rayjobapi RayJob.
+func SetupRayJobWebhook(mgr ctrl.Manager, opts ...jobframework.Option) error {
+	options := jobframework.DefaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &RayJobWebhook{
+		manageJobsWithoutQueueName: options.ManageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&rayjobapi.RayJob{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-ray-io-v1alpha1-rayjob,mutating=true,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=create,versions=v1alpha1,name=mrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &RayJobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *RayJobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
+	jobframework.ApplyDefaultForSuspend((*RayJob)(job), w.manageJobsWithoutQueueName)
+	return nil
+}
+
+// +kubebuilder:webhook:path=/validate-ray-io-v1alpha1-rayjob,mutating=false,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=update,versions=v1alpha1,name=vrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomValidator = &RayJobWebhook{}
+
+// ValidateCreate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *RayJobWebhook) ValidateCreate(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+	log.Info("Validating create", "job", klog.KObj(job))
+	return w.validateCreate(job).ToAggregate()
+}
+
+func (w *RayJobWebhook) validateCreate(job *rayjobapi.RayJob) field.ErrorList {
+	var allErrors field.ErrorList
+	kueueJob := (*RayJob)(job)
+
+	if w.manageJobsWithoutQueueName || jobframework.QueueName(kueueJob) != "" {
+		spec := &job.Spec
+		specPath := field.NewPath("spec")
+
+		if !spec.ShutdownAfterJobFinishes {
+			allErrors = append(allErrors, field.Invalid(specPath.Child("shutdownAfterJobFinishes"), spec.ShutdownAfterJobFinishes, "a kueue managed job should delete the cluster after finishing"))
+		}
+		// should not want existing cluster
+		if len(spec.ClusterSelector) > 0 {
+			allErrors = append(allErrors, field.Invalid(specPath.Child("clusterSelector"), spec.ClusterSelector, "a kueue managed job should not use an existing cluster"))
+		}
+
+		clusterSpec := spec.RayClusterSpec
+		clusterSpecPath := specPath.Child("rayClusterSpec")
+
+		// should not use auto scaler
```

  We should indeed revisit this after partial admission is merged. Follow-up issue #783.

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:115** by [@trasc](https://github.com/trasc) — 2023-05-19T08:37:21Z
```diff
@@ -0,0 +1,128 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+type RayJobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for rayjobapi RayJob.
+func SetupRayJobWebhook(mgr ctrl.Manager, opts ...jobframework.Option) error {
+	options := jobframework.DefaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &RayJobWebhook{
+		manageJobsWithoutQueueName: options.ManageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&rayjobapi.RayJob{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-ray-io-v1alpha1-rayjob,mutating=true,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=create,versions=v1alpha1,name=mrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &RayJobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *RayJobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
+	jobframework.ApplyDefaultForSuspend((*RayJob)(job), w.manageJobsWithoutQueueName)
+	return nil
+}
+
+// +kubebuilder:webhook:path=/validate-ray-io-v1alpha1-rayjob,mutating=false,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=update,versions=v1alpha1,name=vrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomValidator = &RayJobWebhook{}
+
+// ValidateCreate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *RayJobWebhook) ValidateCreate(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+	log.Info("Validating create", "job", klog.KObj(job))
+	return w.validateCreate(job).ToAggregate()
+}
+
+func (w *RayJobWebhook) validateCreate(job *rayjobapi.RayJob) field.ErrorList {
+	var allErrors field.ErrorList
+	kueueJob := (*RayJob)(job)
+
+	if w.manageJobsWithoutQueueName || jobframework.QueueName(kueueJob) != "" {
+		spec := &job.Spec
+		specPath := field.NewPath("spec")
+
+		if !spec.ShutdownAfterJobFinishes {
+			allErrors = append(allErrors, field.Invalid(specPath.Child("shutdownAfterJobFinishes"), spec.ShutdownAfterJobFinishes, "a kueue managed job should delete the cluster after finishing"))
+		}
+		// should not want existing cluster
+		if len(spec.ClusterSelector) > 0 {
+			allErrors = append(allErrors, field.Invalid(specPath.Child("clusterSelector"), spec.ClusterSelector, "a kueue managed job should not use an existing cluster"))
+		}
+
+		clusterSpec := spec.RayClusterSpec
+		clusterSpecPath := specPath.Child("rayClusterSpec")
+
+		// should not use auto scaler
+		if pointer.BoolDeref(clusterSpec.EnableInTreeAutoscaling, false) {
+			allErrors = append(allErrors, field.Invalid(clusterSpecPath.Child("enableInTreeAutoscaling"), clusterSpec.EnableInTreeAutoscaling, "a kueue managed job should not use autoscaling"))
+		}
+		// should limit the worker count to 8 - 1 (max podSets num - cluster head)
+		if len(clusterSpec.WorkerGroupSpecs) > 7 {
+			allErrors = append(allErrors, field.TooMany(clusterSpecPath.Child("workerGroupSpecs"), len(clusterSpec.WorkerGroupSpecs), 7))
+		}
+	}
+
+	allErrors = append(allErrors, jobframework.ValidateCreateForQueueName(kueueJob)...)
+	return allErrors
+}
+
+// ValidateUpdate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *RayJobWebhook) ValidateUpdate(ctx context.Context, oldObj, newObj runtime.Object) error {
+	oldJob := oldObj.(*rayjobapi.RayJob)
+	newJob := newObj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
```

  done

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:88** by [@trasc](https://github.com/trasc) — 2023-05-19T08:43:40Z
```diff
@@ -0,0 +1,128 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+type RayJobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for rayjobapi RayJob.
+func SetupRayJobWebhook(mgr ctrl.Manager, opts ...jobframework.Option) error {
+	options := jobframework.DefaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &RayJobWebhook{
+		manageJobsWithoutQueueName: options.ManageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&rayjobapi.RayJob{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-ray-io-v1alpha1-rayjob,mutating=true,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=create,versions=v1alpha1,name=mrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &RayJobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *RayJobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
+	jobframework.ApplyDefaultForSuspend((*RayJob)(job), w.manageJobsWithoutQueueName)
+	return nil
+}
+
+// +kubebuilder:webhook:path=/validate-ray-io-v1alpha1-rayjob,mutating=false,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=update,versions=v1alpha1,name=vrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomValidator = &RayJobWebhook{}
+
+// ValidateCreate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *RayJobWebhook) ValidateCreate(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+	log.Info("Validating create", "job", klog.KObj(job))
+	return w.validateCreate(job).ToAggregate()
+}
+
+func (w *RayJobWebhook) validateCreate(job *rayjobapi.RayJob) field.ErrorList {
+	var allErrors field.ErrorList
+	kueueJob := (*RayJob)(job)
+
+	if w.manageJobsWithoutQueueName || jobframework.QueueName(kueueJob) != "" {
+		spec := &job.Spec
+		specPath := field.NewPath("spec")
+
+		if !spec.ShutdownAfterJobFinishes {
```

  done

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:89** by [@trasc](https://github.com/trasc) — 2023-05-19T08:43:44Z
```diff
@@ -0,0 +1,128 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+type RayJobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for rayjobapi RayJob.
+func SetupRayJobWebhook(mgr ctrl.Manager, opts ...jobframework.Option) error {
+	options := jobframework.DefaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &RayJobWebhook{
+		manageJobsWithoutQueueName: options.ManageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&rayjobapi.RayJob{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-ray-io-v1alpha1-rayjob,mutating=true,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=create,versions=v1alpha1,name=mrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &RayJobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *RayJobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
+	jobframework.ApplyDefaultForSuspend((*RayJob)(job), w.manageJobsWithoutQueueName)
+	return nil
+}
+
+// +kubebuilder:webhook:path=/validate-ray-io-v1alpha1-rayjob,mutating=false,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=update,versions=v1alpha1,name=vrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomValidator = &RayJobWebhook{}
+
+// ValidateCreate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *RayJobWebhook) ValidateCreate(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+	log.Info("Validating create", "job", klog.KObj(job))
+	return w.validateCreate(job).ToAggregate()
+}
+
+func (w *RayJobWebhook) validateCreate(job *rayjobapi.RayJob) field.ErrorList {
+	var allErrors field.ErrorList
+	kueueJob := (*RayJob)(job)
+
+	if w.manageJobsWithoutQueueName || jobframework.QueueName(kueueJob) != "" {
+		spec := &job.Spec
+		specPath := field.NewPath("spec")
+
+		if !spec.ShutdownAfterJobFinishes {
+			allErrors = append(allErrors, field.Invalid(specPath.Child("shutdownAfterJobFinishes"), spec.ShutdownAfterJobFinishes, "a kueue managed job should delete the cluster after finishing"))
+		}
+		// should not want existing cluster
```

  done

- **pkg/util/testingjobs/rayjob/wrappers.go:107** by [@trasc](https://github.com/trasc) — 2023-05-19T08:44:39Z
```diff
@@ -0,0 +1,146 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+// JobWrapper wraps a RayJob.
+type JobWrapper struct{ rayjobapi.RayJob }
+
+// MakeJob creates a wrapper for a suspended rayJob
+func MakeJob(name, ns string) *JobWrapper {
+	return &JobWrapper{rayjobapi.RayJob{
+		ObjectMeta: metav1.ObjectMeta{
+			Name:        name,
+			Namespace:   ns,
+			Annotations: make(map[string]string, 1),
+		},
+		Spec: rayjobapi.RayJobSpec{
+			ShutdownAfterJobFinishes: true,
+			RayClusterSpec: &rayjobapi.RayClusterSpec{
+				HeadGroupSpec: rayjobapi.HeadGroupSpec{
+					RayStartParams: map[string]string{"p1": "v1"},
+					Template: corev1.PodTemplateSpec{
+						Spec: corev1.PodSpec{
+							Containers: []corev1.Container{
+								{
+									Name: "head-container",
+								},
+							},
+						},
+					},
+				},
+				WorkerGroupSpecs: []rayjobapi.WorkerGroupSpec{
+					{
+						GroupName:      "workers-group-0",
+						Replicas:       pointer.Int32(1),
+						MinReplicas:    pointer.Int32(0),
+						MaxReplicas:    pointer.Int32(10),
+						RayStartParams: map[string]string{"p1": "v1"},
+						Template: corev1.PodTemplateSpec{
+							Spec: corev1.PodSpec{
+								Containers: []corev1.Container{
+									{
+										Name: "worker-container",
+									},
+								},
+							},
+						},
+					},
+				},
+			},
+			Suspend: true,
+		},
+	}}
+}
+
+// Obj returns the inner Job.
+func (j *JobWrapper) Obj() *rayjobapi.RayJob {
+	return &j.RayJob
+}
+
+// Suspend updates the suspend status of the job
+func (j *JobWrapper) Suspend(s bool) *JobWrapper {
+	j.Spec.Suspend = s
+	return j
+}
+
+// Queue updates the queue name of the job
+func (j *JobWrapper) Queue(queue string) *JobWrapper {
+	if j.Labels == nil {
+		j.Labels = make(map[string]string)
+	}
+	j.Labels[jobframework.QueueLabel] = queue
+	return j
+}
+
+func (j *JobWrapper) RequestWorkerGroup(name corev1.ResourceName, quantity string) *JobWrapper {
+	c := &j.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.Containers[0]
+	if c.Resources.Requests == nil {
+		c.Resources.Requests = corev1.ResourceList{name: resource.MustParse(quantity)}
+	} else {
+		c.Resources.Requests[name] = resource.MustParse(quantity)
+	}
+	return j
+}
```

  done

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-05-19T09:35:56Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:119** by [@mwielgus](https://github.com/mwielgus) — 2023-05-19T09:35:55Z
```diff
@@ -0,0 +1,284 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a RayJob object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+var _ jobframework.GenericJob = (*RayJob)(nil)
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
```

  What about min and maxReplicas?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-05-19T09:45:17Z

- **pkg/controller/jobs/rayjob/rayjob_controller_test.go:34** by [@mwielgus](https://github.com/mwielgus) — 2023-05-19T09:45:17Z
```diff
@@ -0,0 +1,122 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+	testingrayutil "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+)
+
+func TestPodSets(t *testing.T) {
```

  Can we have some simple tests for other functions as well?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-05-19T09:52:33Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:119** by [@mwielgus](https://github.com/mwielgus) — 2023-05-19T09:52:33Z
```diff
@@ -0,0 +1,284 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a RayJob object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+var _ jobframework.GenericJob = (*RayJob)(nil)
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
```

  I see, the integration only works if autoscaling is off. Please just add a comment why these fields are ignored.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-19T10:14:22Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:119** by [@trasc](https://github.com/trasc) — 2023-05-19T10:14:22Z
```diff
@@ -0,0 +1,284 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a RayJob object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+var _ jobframework.GenericJob = (*RayJob)(nil)
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
```

  I've opened #783, they should be used then

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-19T10:15:45Z

- **pkg/controller/jobs/rayjob/rayjob_controller_test.go:34** by [@trasc](https://github.com/trasc) — 2023-05-19T10:15:45Z
```diff
@@ -0,0 +1,122 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+	testingrayutil "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+)
+
+func TestPodSets(t *testing.T) {
```

  they are not very complex, also we have integration test for ray

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-05-19T10:30:25Z

- **pkg/controller/jobs/rayjob/rayjob_controller_test.go:34** by [@mwielgus](https://github.com/mwielgus) — 2023-05-19T10:30:25Z
```diff
@@ -0,0 +1,122 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+	testingrayutil "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+)
+
+func TestPodSets(t *testing.T) {
```

  I understand that they are not complex, but still a bug can happen there ;).

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-19T10:35:41Z

- **pkg/controller/jobs/rayjob/rayjob_controller_test.go:34** by [@trasc](https://github.com/trasc) — 2023-05-19T10:35:41Z
```diff
@@ -0,0 +1,122 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+	testingrayutil "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+)
+
+func TestPodSets(t *testing.T) {
```

  I'll try to add some more

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-19T13:08:46Z

- **pkg/controller/jobs/rayjob/rayjob_controller_test.go:34** by [@trasc](https://github.com/trasc) — 2023-05-19T13:08:46Z
```diff
@@ -0,0 +1,122 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+	testingrayutil "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+)
+
+func TestPodSets(t *testing.T) {
```

  done

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-05-20T14:30:35Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:84** by [@kerthcet](https://github.com/kerthcet) — 2023-05-20T14:30:34Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
```

  Got it, so when raycluster is deleted and rayjob is suspend, we'll set the `JobDeploymentStatusSuspended`, sgtm.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-05-20T15:04:52Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:236** by [@kerthcet](https://github.com/kerthcet) — 2023-05-20T15:04:51Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) == 0 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if !equality.Semantic.DeepEqual(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector) {
+		headPodSpec.NodeSelector = cloneSelectors(nodeSelectors[0].NodeSelector)
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector) {
+			workerPodSpec.NodeSelector = cloneSelectors(nodeSelectors[index+1].NodeSelector)
+		}
+	}
+}
+
+func (j *RayJob) Finished() (metav1.Condition, bool) {
+	condition := metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  string(j.Status.JobStatus),
+		Message: j.Status.Message,
+	}
+	finished := false
+	switch j.Status.JobDeploymentStatus {
+	case rayjobapi.JobDeploymentStatusComplete, rayjobapi.JobDeploymentStatusFailedJobDeploy, rayjobapi.JobDeploymentStatusFailedToGetOrCreateRayCluster:
+		finished = true
+
+	default:
+		finished = false
+	}
+	return condition, finished
+}
+
+func (j *RayJob) EquivalentToWorkload(wl kueue.Workload) bool {
+	if len(wl.Spec.PodSets) != (len(j.Spec.RayClusterSpec.WorkerGroupSpecs) + 1) {
+		return false
+	}
+
+	podSets := wl.Spec.PodSets
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if podSets[0].Count != 1 {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.InitContainers, podSets[0].Template.Spec.InitContainers) {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.Containers, podSets[0].Template.Spec.Containers) {
+		return false
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		if podSets[index+1].Count != *j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Replicas {
+			return false
+		}
+
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.InitContainers, podSets[index+1].Template.Spec.InitContainers) {
+			return false
+		}
+		if !equality.Semantic.DeepEqual(workerPodSpec.Containers, podSets[index+1].Template.Spec.Containers) {
+			return false
+		}
+	}
+	return true
+}
+
+func (j *RayJob) PriorityClass() string {
+	if j.Spec.RayClusterSpec != nil {
+		rcs := j.Spec.RayClusterSpec
+		if len(rcs.HeadGroupSpec.Template.Spec.PriorityClassName) != 0 {
+			return rcs.HeadGroupSpec.Template.Spec.PriorityClassName
+		}
+		for wi := range rcs.WorkerGroupSpecs {
```

  See discussion here https://github.com/ray-project/kuberay/issues/1100, maybe we can query the `ray.io/priority-class-name` for the moment.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-05-21T04:12:29Z

- **pkg/controller/jobs/rayjob/rayjob_controller_test.go:138** by [@kerthcet](https://github.com/kerthcet) — 2023-05-20T15:43:38Z
```diff
@@ -0,0 +1,384 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	testingrayutil "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+)
+
+func TestPodSets(t *testing.T) {
+	job := testingrayutil.MakeJob("job", "ns").
+		WithHeadGroupSpec(
+			rayjobapi.HeadGroupSpec{
+				Template: corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name: "head_c",
+							},
+						},
+					},
+				},
+			},
+		).
+		WithWorkerGroups(
+			rayjobapi.WorkerGroupSpec{
+				GroupName: "group1",
+				Template: corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name: "group1_c",
+							},
+						},
+					},
+				},
+			},
+			rayjobapi.WorkerGroupSpec{
+				GroupName: "group2",
+				Replicas:  pointer.Int32(3),
+				Template: corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name: "group2_c",
+							},
+						},
+					},
+				},
+			},
+		).
+		Obj()
+
+	wantPodSets := []kueue.PodSet{
+		{
+			Name:  "head",
+			Count: 1,
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					Containers: []corev1.Container{
+						{
+							Name: "head_c",
+						},
+					},
+				},
+			},
+		},
+		{
+			Name:  "group1",
+			Count: 1,
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					Containers: []corev1.Container{
+						{
+							Name: "group1_c",
+						},
+					},
+				},
+			},
+		},
+		{
+			Name:  "group2",
+			Count: 3,
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					Containers: []corev1.Container{
+						{
+							Name: "group2_c",
+						},
+					},
+				},
+			},
+		},
+	}
+
+	result := ((*RayJob)(job)).PodSets()
+
+	if diff := cmp.Diff(wantPodSets, result); diff != "" {
+		t.Errorf("PodSets() mismatch (-want +got):\n%s", diff)
+	}
+}
+
+func TestEquivalentToWorkload(t *testing.T) {
+	testContainer := corev1.Container{
+		Name:  "ic1",
+		Image: "image1",
+		Resources: corev1.ResourceRequirements{
+			Requests: corev1.ResourceList{
+				corev1.ResourceCPU: resource.MustParse("1"),
+			},
+		},
+	}
+	job := (*RayJob)(testingrayutil.MakeJob("job", "ns").
+		RequestHead(corev1.ResourceCPU, "2").
```

  Is this invalid?

- **pkg/controller/jobs/rayjob/rayjob_controller_test.go:319** by [@kerthcet](https://github.com/kerthcet) — 2023-05-20T15:48:48Z
```diff
@@ -0,0 +1,384 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	testingrayutil "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+)
+
+func TestPodSets(t *testing.T) {
+	job := testingrayutil.MakeJob("job", "ns").
+		WithHeadGroupSpec(
+			rayjobapi.HeadGroupSpec{
+				Template: corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name: "head_c",
+							},
+						},
+					},
+				},
+			},
+		).
+		WithWorkerGroups(
+			rayjobapi.WorkerGroupSpec{
+				GroupName: "group1",
+				Template: corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name: "group1_c",
+							},
+						},
+					},
+				},
+			},
+			rayjobapi.WorkerGroupSpec{
+				GroupName: "group2",
+				Replicas:  pointer.Int32(3),
+				Template: corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name: "group2_c",
+							},
+						},
+					},
+				},
+			},
+		).
+		Obj()
+
+	wantPodSets := []kueue.PodSet{
+		{
+			Name:  "head",
+			Count: 1,
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					Containers: []corev1.Container{
+						{
+							Name: "head_c",
+						},
+					},
+				},
+			},
+		},
+		{
+			Name:  "group1",
+			Count: 1,
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					Containers: []corev1.Container{
+						{
+							Name: "group1_c",
+						},
+					},
+				},
+			},
+		},
+		{
+			Name:  "group2",
+			Count: 3,
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					Containers: []corev1.Container{
+						{
+							Name: "group2_c",
+						},
+					},
+				},
+			},
+		},
+	}
+
+	result := ((*RayJob)(job)).PodSets()
+
+	if diff := cmp.Diff(wantPodSets, result); diff != "" {
+		t.Errorf("PodSets() mismatch (-want +got):\n%s", diff)
+	}
+}
+
+func TestEquivalentToWorkload(t *testing.T) {
+	testContainer := corev1.Container{
+		Name:  "ic1",
+		Image: "image1",
+		Resources: corev1.ResourceRequirements{
+			Requests: corev1.ResourceList{
+				corev1.ResourceCPU: resource.MustParse("1"),
+			},
+		},
+	}
+	job := (*RayJob)(testingrayutil.MakeJob("job", "ns").
+		RequestHead(corev1.ResourceCPU, "2").
+		WithHeadGroupSpec(rayjobapi.HeadGroupSpec{
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					InitContainers: []corev1.Container{testContainer, testContainer},
+					Containers:     []corev1.Container{testContainer},
+				},
+			},
+		}).
+		WithWorkerGroups(rayjobapi.WorkerGroupSpec{
+			GroupName: "group1",
+			Replicas:  pointer.Int32(2),
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					InitContainers: []corev1.Container{testContainer},
+					Containers:     []corev1.Container{testContainer, testContainer, testContainer},
+				},
+			},
+		}, rayjobapi.WorkerGroupSpec{
+			GroupName: "group2",
+			Replicas:  pointer.Int32(3),
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					InitContainers: []corev1.Container{testContainer},
+					Containers:     []corev1.Container{testContainer, testContainer},
+				},
+			},
+		}).
+		Obj())
+	podSets := []kueue.PodSet{
+		*utiltesting.MakePodSet("head", 1).
+			InitContainers(testContainer, testContainer).
+			Containers(testContainer).
+			Obj(),
+		*utiltesting.MakePodSet("group1", 2).
+			InitContainers(testContainer).
+			Containers(testContainer, testContainer, testContainer).
+			Obj(),
+		*utiltesting.MakePodSet("group2", 3).
+			InitContainers(testContainer).
+			Containers(testContainer, testContainer).
+			Obj(),
+	}
+	basWorkload := utiltesting.MakeWorkload("wl", "ns").PodSets(podSets...).Obj()
+	cases := map[string]struct {
+		wl         kueue.Workload
+		wantResult bool
+	}{
+		"wrong podsets number": {
+			wl: *(&utiltesting.WorkloadWrapper{Workload: *basWorkload.DeepCopy()}).PodSets(podSets[:1]...).Obj(),
+		},
+		"bad head podSet count": {
+			wl: func() kueue.Workload {
+				wl := *basWorkload.DeepCopy()
+				wl.Spec.PodSets[0].Count = 3
+				return wl
+			}(),
+		},
+		"bad head podSet init container": {
+			wl: func() kueue.Workload {
+				wl := *basWorkload.DeepCopy()
+				wl.Spec.PodSets[0].Template.Spec.InitContainers[0].Image = "another-image"
+				return wl
+			}(),
+		},
+		"bad head podSet container": {
+			wl: func() kueue.Workload {
+				wl := *basWorkload.DeepCopy()
+				wl.Spec.PodSets[0].Template.Spec.Containers[0].Image = "another-image"
+				return wl
+			}(),
+		},
+		"bad workGroup podSet count": {
+			wl: func() kueue.Workload {
+				wl := *basWorkload.DeepCopy()
+				wl.Spec.PodSets[1].Count = 3
+				return wl
+			}(),
+		},
+		"bad workGroup podSet init container": {
+			wl: func() kueue.Workload {
+				wl := *basWorkload.DeepCopy()
+				wl.Spec.PodSets[2].Template.Spec.InitContainers[0].Image = "another-image"
+				return wl
+			}(),
+		},
+		"bad workGroup podSet container": {
+			wl: func() kueue.Workload {
+				wl := *basWorkload.DeepCopy()
+				wl.Spec.PodSets[1].Template.Spec.Containers[0].Image = "another-image"
+				return wl
+			}(),
+		},
+		"equivalent": {
+			wl:         *basWorkload.DeepCopy(),
+			wantResult: true,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			if job.EquivalentToWorkload(tc.wl) != tc.wantResult {
+				t.Fatalf("Unexpected result, wanted: %v", tc.wantResult)
+			}
+		})
+	}
+}
+
+func TestPriorityClass(t *testing.T) {
+	cases := map[string]struct {
+		job           *rayjobapi.RayJob
+		wantClassName string
+	}{
+		"none": {
+			job: testingrayutil.MakeJob("job", "ns").Obj(),
+		},
+		"from head": {
+			job:           testingrayutil.MakeJob("job", "ns").WithPriorityClassName("head-prio-class").WithWorkerPriorityClassName("worker-prio-class").Obj(),
+			wantClassName: "head-prio-class",
+		},
+		"from worker": {
+			job:           testingrayutil.MakeJob("job", "ns").WithWorkerPriorityClassName("worker-prio-class").Obj(),
+			wantClassName: "worker-prio-class",
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			got := ((*RayJob)(tc.job)).PriorityClass()
+			if diff := cmp.Diff(tc.wantClassName, got); diff != "" {
+				t.Errorf("PriorityClass() mismatch (-want +got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestNodeSelectors(t *testing.T) {
+
+	job := (*RayJob)(testingrayutil.MakeJob("job", "ns").
+		WithHeadGroupSpec(rayjobapi.HeadGroupSpec{
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					NodeSelector: map[string]string{},
+				},
+			},
+		}).
+		WithWorkerGroups(rayjobapi.WorkerGroupSpec{
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					NodeSelector: map[string]string{
+						"key-wg1": "value-wg1",
+					},
+				},
+			},
+		}, rayjobapi.WorkerGroupSpec{
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					NodeSelector: map[string]string{
+						"key-wg2": "value-wg2",
+					},
+				},
+			},
+		}).
+		Obj())
+
+	// run with Affinity should append or update the node selectors
+	job.RunWithNodeAffinity([]jobframework.PodSetNodeSelector{
+		{
+			NodeSelector: map[string]string{
+				"newKey": "newValue",
+			},
+		},
+		{
+			NodeSelector: map[string]string{
+				"key-wg1": "updated-value-wg1",
+			},
+		},
+		{
+			NodeSelector: map[string]string{
+				// don't add anything
+			},
+		},
+	})
+
+	if diff := cmp.Diff(
```

  Can we compare job directly? Like we have a wantedJob.

- **pkg/controller/jobs/rayjob/rayjob_controller.go:190** by [@kerthcet](https://github.com/kerthcet) — 2023-05-20T16:02:22Z
```diff
@@ -0,0 +1,284 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a RayJob object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+var _ jobframework.GenericJob = (*RayJob)(nil)
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	if len(nodeSelectors) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if !equality.Semantic.DeepEqual(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector) {
+		headPodSpec.NodeSelector = cloneSelectors(nodeSelectors[0].NodeSelector)
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector) {
+			workerPodSpec.NodeSelector = cloneSelectors(nodeSelectors[index+1].NodeSelector)
+		}
+	}
+}
+
+func (j *RayJob) Finished() (metav1.Condition, bool) {
+	condition := metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  string(j.Status.JobStatus),
+		Message: j.Status.Message,
+	}
+	finished := false
+	switch j.Status.JobDeploymentStatus {
+	case rayjobapi.JobDeploymentStatusComplete, rayjobapi.JobDeploymentStatusFailedJobDeploy, rayjobapi.JobDeploymentStatusFailedToGetOrCreateRayCluster:
```

  Maybe we should use jobStatus here, when `status == rayv1alpha1.JobStatusSucceeded || status == rayv1alpha1.JobStatusFailed`, how can you tell `JobDeploymentStatusFailedToGetOrCreateRayCluster` is finished, rayCluster is still retrying at that moment.

- **pkg/controller/jobs/rayjob/rayjob_controller.go:215** by [@kerthcet](https://github.com/kerthcet) — 2023-05-20T16:40:01Z
```diff
@@ -0,0 +1,284 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a RayJob object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+var _ jobframework.GenericJob = (*RayJob)(nil)
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	if len(nodeSelectors) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if !equality.Semantic.DeepEqual(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector) {
+		headPodSpec.NodeSelector = cloneSelectors(nodeSelectors[0].NodeSelector)
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector) {
+			workerPodSpec.NodeSelector = cloneSelectors(nodeSelectors[index+1].NodeSelector)
+		}
+	}
+}
+
+func (j *RayJob) Finished() (metav1.Condition, bool) {
+	condition := metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  string(j.Status.JobStatus),
+		Message: j.Status.Message,
+	}
+	finished := false
+	switch j.Status.JobDeploymentStatus {
+	case rayjobapi.JobDeploymentStatusComplete, rayjobapi.JobDeploymentStatusFailedJobDeploy, rayjobapi.JobDeploymentStatusFailedToGetOrCreateRayCluster:
+		finished = true
+	default:
+		finished = false
+	}
+	return condition, finished
+}
+
+func (j *RayJob) EquivalentToWorkload(wl kueue.Workload) bool {
+	if len(wl.Spec.PodSets) != (len(j.Spec.RayClusterSpec.WorkerGroupSpecs) + 1) {
+		return false
+	}
+
+	podSets := wl.Spec.PodSets
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if podSets[0].Count != 1 {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.InitContainers, podSets[0].Template.Spec.InitContainers) {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.Containers, podSets[0].Template.Spec.Containers) {
+		return false
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		if podSets[index+1].Count != *j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Replicas {
```

  Add a comment here like `When autoscaling is enabled, we should change this for replicas is changeable under that condition.` IIUC.

- **pkg/controller/jobs/rayjob/rayjob_controller.go:215** by [@kerthcet](https://github.com/kerthcet) — 2023-05-21T03:32:51Z
```diff
@@ -0,0 +1,284 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a RayJob object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+var _ jobframework.GenericJob = (*RayJob)(nil)
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	if len(nodeSelectors) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if !equality.Semantic.DeepEqual(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector) {
+		headPodSpec.NodeSelector = cloneSelectors(nodeSelectors[0].NodeSelector)
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector) {
+			workerPodSpec.NodeSelector = cloneSelectors(nodeSelectors[index+1].NodeSelector)
+		}
+	}
+}
+
+func (j *RayJob) Finished() (metav1.Condition, bool) {
+	condition := metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  string(j.Status.JobStatus),
+		Message: j.Status.Message,
+	}
+	finished := false
+	switch j.Status.JobDeploymentStatus {
+	case rayjobapi.JobDeploymentStatusComplete, rayjobapi.JobDeploymentStatusFailedJobDeploy, rayjobapi.JobDeploymentStatusFailedToGetOrCreateRayCluster:
+		finished = true
+	default:
+		finished = false
+	}
+	return condition, finished
+}
+
+func (j *RayJob) EquivalentToWorkload(wl kueue.Workload) bool {
+	if len(wl.Spec.PodSets) != (len(j.Spec.RayClusterSpec.WorkerGroupSpecs) + 1) {
+		return false
+	}
+
+	podSets := wl.Spec.PodSets
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if podSets[0].Count != 1 {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.InitContainers, podSets[0].Template.Spec.InitContainers) {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.Containers, podSets[0].Template.Spec.Containers) {
+		return false
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		if podSets[index+1].Count != *j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Replicas {
```

  And when replicas changes, we should reclaim or reserve the resources. But it won't happen until we enable the autoscaling.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-21T16:05:42Z

- **config/components/manager/controller_manager_config.yaml:34** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-21T16:05:41Z
```diff
@@ -31,3 +31,4 @@ integrations:
   frameworks:
   - "batch/job"
 # - "kubeflow.org/mpijob"
+# - "ray.io/rayjob"
```

  Can you mention `ray` in the following as well?
  
  https://github.com/kubernetes-sigs/kueue/blob/74f45f69612a2d36ace91ae455f1e9fdbcd53037/apis/config/v1beta1/configuration_types.go#L109-L115

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-21T16:20:56Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:122** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-21T16:20:56Z
```diff
@@ -0,0 +1,284 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a RayJob object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+var _ jobframework.GenericJob = (*RayJob)(nil)
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
```

  Why do we need to make `groupName`  lower?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-21T16:28:45Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:122** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-21T16:28:45Z
```diff
@@ -0,0 +1,284 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a RayJob object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+var _ jobframework.GenericJob = (*RayJob)(nil)
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
```

  Maybe, because the mpijob-controller uses lower cases?
  
  https://github.com/kubernetes-sigs/kueue/blob/74f45f69612a2d36ace91ae455f1e9fdbcd53037/pkg/controller/jobs/mpijob/mpijob_controller.go#L116

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-21T16:37:43Z

- **pkg/util/testingjobs/rayjob/wrappers.go:30** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-21T16:37:43Z
```diff
@@ -0,0 +1,152 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+// JobWrapper wraps a RayJob.
+type JobWrapper struct{ rayjobapi.RayJob }
```

  Uhm.. I think `RayJobWrapper` would be good.
  @trasc wdyt?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-21T16:59:34Z

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:83** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-21T16:59:34Z
```diff
@@ -0,0 +1,131 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+type RayJobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for rayjobapi RayJob.
+func SetupRayJobWebhook(mgr ctrl.Manager, opts ...jobframework.Option) error {
+	options := jobframework.DefaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &RayJobWebhook{
+		manageJobsWithoutQueueName: options.ManageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&rayjobapi.RayJob{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-ray-io-v1alpha1-rayjob,mutating=true,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=create,versions=v1alpha1,name=mrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &RayJobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *RayJobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("rayjob-webhook")
+	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
+	jobframework.ApplyDefaultForSuspend((*RayJob)(job), w.manageJobsWithoutQueueName)
+	return nil
+}
+
+// +kubebuilder:webhook:path=/validate-ray-io-v1alpha1-rayjob,mutating=false,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=update,versions=v1alpha1,name=vrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomValidator = &RayJobWebhook{}
+
+// ValidateCreate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *RayJobWebhook) ValidateCreate(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("rayjob-webhook")
+	log.Info("Validating create", "job", klog.KObj(job))
+	return w.validateCreate(job).ToAggregate()
+}
+
+func (w *RayJobWebhook) validateCreate(job *rayjobapi.RayJob) field.ErrorList {
+	var allErrors field.ErrorList
+	kueueJob := (*RayJob)(job)
+
+	if w.manageJobsWithoutQueueName || jobframework.QueueName(kueueJob) != "" {
```

  It is worth the commonizing process to check whether jobs are managed by kueue.
  
  We can work on that as a follow-up PR.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-21T17:03:29Z

- **pkg/util/pointer/pointer.go:32** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-21T17:03:29Z
```diff
@@ -26,8 +26,9 @@ func Quantity(q resource.Quantity) *resource.Quantity {
 }
 
 var (
-	Int32  = pointer.Int32
-	Int64  = pointer.Int64
-	Bool   = pointer.Bool
-	String = pointer.String
+	Int32     = pointer.Int32
+	Int64     = pointer.Int64
+	Bool      = pointer.Bool
+	BoolDeref = pointer.BoolDeref
```

  @alculquicondor I'm not sure whether we should add `BoolDeref` to this.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-21T17:06:44Z

- **test/integration/controller/rayjob/rayjob_controller_test.go:49** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-21T17:06:43Z
```diff
@@ -0,0 +1,564 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"fmt"
+
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/pointer"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	workloadrayjob "sigs.k8s.io/kueue/pkg/controller/jobs/rayjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	testingrayjob "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	jobName           = "test-job"
+	jobNamespace      = "default"
+	labelKey          = "cloud.provider.com/instance"
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
```

  ```suggestion
  	priorityValue     int32 = 10
  ```
  
  If we initialize this as `int32`, we don't need to cast to `int32` in actual test cases.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-21T17:23:03Z

- **test/integration/controller/rayjob/suite_test.go:49** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-21T17:08:34Z
```diff
@@ -0,0 +1,99 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"path/filepath"
+	"testing"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	"k8s.io/client-go/rest"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/rayjob"
+	"sigs.k8s.io/kueue/pkg/queue"
+	"sigs.k8s.io/kueue/pkg/scheduler"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	//+kubebuilder:scaffold:imports
+)
+
+var (
+	cfg        *rest.Config
+	k8sClient  client.Client
+	ctx        context.Context
+	fwk        *framework.Framework
+	crdPath    = filepath.Join("..", "..", "..", "..", "config", "components", "crd", "bases")
+	mpiCrdPath = filepath.Join("..", "..", "..", "..", "dep-crds", "ray-operator")
```

  ```suggestion
  	rayCrdPath = filepath.Join("..", "..", "..", "..", "dep-crds", "ray-operator")
  ```
  
  This is `ray` 🙃

- **test/integration/controller/rayjob/rayjob_controller_test.go:77** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-21T17:09:54Z
```diff
@@ -0,0 +1,564 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"fmt"
+
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/pointer"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	workloadrayjob "sigs.k8s.io/kueue/pkg/controller/jobs/rayjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	testingrayjob "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	jobName           = "test-job"
+	jobNamespace      = "default"
+	labelKey          = "cloud.provider.com/instance"
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+)
+
+var (
+	wlLookupKey               = types.NamespacedName{Name: workloadrayjob.GetWorkloadNameForRayJob(jobName), Namespace: jobNamespace}
+	ignoreConditionTimestamps = cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+func setInitStatus(name, namespace string) {
+	createdJob := &rayjobapi.RayJob{}
+	nsName := types.NamespacedName{Name: name, Namespace: namespace}
+	gomega.EventuallyWithOffset(1, func() error {
+		if err := k8sClient.Get(ctx, nsName, createdJob); err != nil {
+			return err
+		}
+		createdJob.Status.JobDeploymentStatus = rayjobapi.JobDeploymentStatusSuspended
+		return k8sClient.Status().Update(ctx, createdJob)
+	}, util.Timeout, util.Interval).Should(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Job controller", func() {
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(jobframework.WithManageJobsWithoutQueueName(true)),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
```

  ```suggestion
  			DepCRDPaths:  []string{rayCrdPath},
  ```

- **test/integration/controller/rayjob/rayjob_controller_test.go:177** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-21T17:18:03Z
```diff
@@ -0,0 +1,564 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"fmt"
+
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/pointer"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	workloadrayjob "sigs.k8s.io/kueue/pkg/controller/jobs/rayjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	testingrayjob "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	jobName           = "test-job"
+	jobNamespace      = "default"
+	labelKey          = "cloud.provider.com/instance"
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+)
+
+var (
+	wlLookupKey               = types.NamespacedName{Name: workloadrayjob.GetWorkloadNameForRayJob(jobName), Namespace: jobNamespace}
+	ignoreConditionTimestamps = cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+func setInitStatus(name, namespace string) {
+	createdJob := &rayjobapi.RayJob{}
+	nsName := types.NamespacedName{Name: name, Namespace: namespace}
+	gomega.EventuallyWithOffset(1, func() error {
+		if err := k8sClient.Get(ctx, nsName, createdJob); err != nil {
+			return err
+		}
+		createdJob.Status.JobDeploymentStatus = rayjobapi.JobDeploymentStatusSuspended
+		return k8sClient.Status().Update(ctx, createdJob)
+	}, util.Timeout, util.Interval).Should(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Job controller", func() {
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(jobframework.WithManageJobsWithoutQueueName(true)),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
+		}
+
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.It("Should reconcile RayJobs", func() {
+		ginkgo.By("checking the job gets suspended when created unsuspended")
+		priorityClass := testing.MakePriorityClass(priorityClassName).
+			PriorityValue(int32(priorityValue)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, priorityClass)).Should(gomega.Succeed())
+
+		job := testingrayjob.MakeJob(jobName, jobNamespace).
+			Suspend(false).
+			WithPriorityClassName(priorityClassName).
+			Obj()
+		err := k8sClient.Create(ctx, job)
+		gomega.Expect(err).To(gomega.Succeed())
+		createdJob := &rayjobapi.RayJob{}
+
+		setInitStatus(jobName, jobNamespace)
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, types.NamespacedName{Name: jobName, Namespace: jobNamespace}, createdJob); err != nil {
+				return false
+			}
+			return createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is created without queue assigned")
+		createdWorkload := &kueue.Workload{}
+		gomega.Eventually(func() error {
+			return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
+		gomega.Expect(metav1.IsControlledBy(createdWorkload, createdJob)).To(gomega.BeTrue(), "The Workload should be owned by the Job")
+
+		ginkgo.By("checking the workload is created with priority and priorityName")
+		gomega.Expect(createdWorkload.Spec.PriorityClassName).Should(gomega.Equal(priorityClassName))
+		gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
+
+		ginkgo.By("checking the workload is updated with queue name when the job does")
+		jobQueueName := "test-queue"
+		createdJob.Annotations = map[string]string{jobframework.QueueAnnotation: jobQueueName}
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return createdWorkload.Spec.QueueName == jobQueueName
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking a second non-matching workload is deleted")
+		secondWl := &kueue.Workload{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      workloadrayjob.GetWorkloadNameForRayJob("second-workload"),
+				Namespace: createdWorkload.Namespace,
+			},
+			Spec: *createdWorkload.Spec.DeepCopy(),
+		}
+		gomega.Expect(ctrl.SetControllerReference(createdJob, secondWl, scheme.Scheme)).Should(gomega.Succeed())
+		secondWl.Spec.PodSets[0].Count += 1
+
+		gomega.Expect(k8sClient.Create(ctx, secondWl)).Should(gomega.Succeed())
+		gomega.Eventually(func() error {
+			wl := &kueue.Workload{}
+			key := types.NamespacedName{Name: secondWl.Name, Namespace: secondWl.Namespace}
+			return k8sClient.Get(ctx, key, wl)
+		}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+		// check the original wl is still there
+		gomega.Consistently(func() bool {
+			err := k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+			return err == nil
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the job is unsuspended when workload is assigned")
+		onDemandFlavor := testing.MakeResourceFlavor("on-demand").Label(labelKey, "on-demand").Obj()
+		gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).Should(gomega.Succeed())
+		spotFlavor := testing.MakeResourceFlavor("spot").Label(labelKey, "spot").Obj()
+		gomega.Expect(k8sClient.Create(ctx, spotFlavor)).Should(gomega.Succeed())
+		clusterQueue := testing.MakeClusterQueue("cluster-queue").
+			ResourceGroup(
+				*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "5").Obj(),
+				*testing.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
+			).Obj()
+		admission := &kueue.Admission{
+			ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+			PodSetAssignments: []kueue.PodSetAssignment{{
+				Name: "Launcher",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "on-demand",
+				},
+			}, {
+				Name: "Worker",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "spot",
+				},
+			}},
+		}
```

  Can we use `testing.MakeAdmission()`?

- **test/integration/controller/rayjob/rayjob_controller_test.go:240** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-21T17:19:12Z
```diff
@@ -0,0 +1,564 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"fmt"
+
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/pointer"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	workloadrayjob "sigs.k8s.io/kueue/pkg/controller/jobs/rayjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	testingrayjob "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	jobName           = "test-job"
+	jobNamespace      = "default"
+	labelKey          = "cloud.provider.com/instance"
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+)
+
+var (
+	wlLookupKey               = types.NamespacedName{Name: workloadrayjob.GetWorkloadNameForRayJob(jobName), Namespace: jobNamespace}
+	ignoreConditionTimestamps = cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+func setInitStatus(name, namespace string) {
+	createdJob := &rayjobapi.RayJob{}
+	nsName := types.NamespacedName{Name: name, Namespace: namespace}
+	gomega.EventuallyWithOffset(1, func() error {
+		if err := k8sClient.Get(ctx, nsName, createdJob); err != nil {
+			return err
+		}
+		createdJob.Status.JobDeploymentStatus = rayjobapi.JobDeploymentStatusSuspended
+		return k8sClient.Status().Update(ctx, createdJob)
+	}, util.Timeout, util.Interval).Should(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Job controller", func() {
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(jobframework.WithManageJobsWithoutQueueName(true)),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
+		}
+
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.It("Should reconcile RayJobs", func() {
+		ginkgo.By("checking the job gets suspended when created unsuspended")
+		priorityClass := testing.MakePriorityClass(priorityClassName).
+			PriorityValue(int32(priorityValue)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, priorityClass)).Should(gomega.Succeed())
+
+		job := testingrayjob.MakeJob(jobName, jobNamespace).
+			Suspend(false).
+			WithPriorityClassName(priorityClassName).
+			Obj()
+		err := k8sClient.Create(ctx, job)
+		gomega.Expect(err).To(gomega.Succeed())
+		createdJob := &rayjobapi.RayJob{}
+
+		setInitStatus(jobName, jobNamespace)
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, types.NamespacedName{Name: jobName, Namespace: jobNamespace}, createdJob); err != nil {
+				return false
+			}
+			return createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is created without queue assigned")
+		createdWorkload := &kueue.Workload{}
+		gomega.Eventually(func() error {
+			return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
+		gomega.Expect(metav1.IsControlledBy(createdWorkload, createdJob)).To(gomega.BeTrue(), "The Workload should be owned by the Job")
+
+		ginkgo.By("checking the workload is created with priority and priorityName")
+		gomega.Expect(createdWorkload.Spec.PriorityClassName).Should(gomega.Equal(priorityClassName))
+		gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
+
+		ginkgo.By("checking the workload is updated with queue name when the job does")
+		jobQueueName := "test-queue"
+		createdJob.Annotations = map[string]string{jobframework.QueueAnnotation: jobQueueName}
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return createdWorkload.Spec.QueueName == jobQueueName
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking a second non-matching workload is deleted")
+		secondWl := &kueue.Workload{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      workloadrayjob.GetWorkloadNameForRayJob("second-workload"),
+				Namespace: createdWorkload.Namespace,
+			},
+			Spec: *createdWorkload.Spec.DeepCopy(),
+		}
+		gomega.Expect(ctrl.SetControllerReference(createdJob, secondWl, scheme.Scheme)).Should(gomega.Succeed())
+		secondWl.Spec.PodSets[0].Count += 1
+
+		gomega.Expect(k8sClient.Create(ctx, secondWl)).Should(gomega.Succeed())
+		gomega.Eventually(func() error {
+			wl := &kueue.Workload{}
+			key := types.NamespacedName{Name: secondWl.Name, Namespace: secondWl.Namespace}
+			return k8sClient.Get(ctx, key, wl)
+		}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+		// check the original wl is still there
+		gomega.Consistently(func() bool {
+			err := k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+			return err == nil
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the job is unsuspended when workload is assigned")
+		onDemandFlavor := testing.MakeResourceFlavor("on-demand").Label(labelKey, "on-demand").Obj()
+		gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).Should(gomega.Succeed())
+		spotFlavor := testing.MakeResourceFlavor("spot").Label(labelKey, "spot").Obj()
+		gomega.Expect(k8sClient.Create(ctx, spotFlavor)).Should(gomega.Succeed())
+		clusterQueue := testing.MakeClusterQueue("cluster-queue").
+			ResourceGroup(
+				*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "5").Obj(),
+				*testing.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
+			).Obj()
+		admission := &kueue.Admission{
+			ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+			PodSetAssignments: []kueue.PodSetAssignment{{
+				Name: "Launcher",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "on-demand",
+				},
+			}, {
+				Name: "Worker",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "spot",
+				},
+			}},
+		}
+		gomega.Expect(util.SetAdmission(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+		lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return !createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Eventually(func() bool {
+			ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "Started", corev1.EventTypeNormal, fmt.Sprintf("Admitted by clusterQueue %v", clusterQueue.Name))
+			return ok
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(onDemandFlavor.Name))
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(spotFlavor.Name))
+		gomega.Consistently(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the job gets suspended when parallelism changes and the added node selectors are removed")
+		parallelism := pointer.Int32Deref(job.Spec.RayClusterSpec.WorkerGroupSpecs[0].Replicas, 1)
+		newParallelism := int32(parallelism + 1)
+		createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Replicas = &newParallelism
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return createdJob.Spec.Suspend && len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector) == 0
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Eventually(func() bool {
+			ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "DeletedWorkload", corev1.EventTypeNormal, fmt.Sprintf("Deleted not matching Workload: %v", wlLookupKey.String()))
+			return ok
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is updated with new count")
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return createdWorkload.Spec.PodSets[1].Count == newParallelism
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(createdWorkload.Status.Admission).Should(gomega.BeNil())
+
+		ginkgo.By("checking the job is unsuspended and selectors added when workload is assigned again")
+		admission = &kueue.Admission{
+			ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+			PodSetAssignments: []kueue.PodSetAssignment{{
+				Name: createdWorkload.Spec.PodSets[0].Name,
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "on-demand",
+				},
+			}, {
+				Name: createdWorkload.Spec.PodSets[1].Name,
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "spot",
+				},
+			}},
+		}
```

  Can we use `testing.MakeAdmission()`?

- **test/integration/controller/rayjob/rayjob_controller_test.go:277** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-21T17:19:54Z
```diff
@@ -0,0 +1,564 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"fmt"
+
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/pointer"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	workloadrayjob "sigs.k8s.io/kueue/pkg/controller/jobs/rayjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	testingrayjob "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	jobName           = "test-job"
+	jobNamespace      = "default"
+	labelKey          = "cloud.provider.com/instance"
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+)
+
+var (
+	wlLookupKey               = types.NamespacedName{Name: workloadrayjob.GetWorkloadNameForRayJob(jobName), Namespace: jobNamespace}
+	ignoreConditionTimestamps = cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+func setInitStatus(name, namespace string) {
+	createdJob := &rayjobapi.RayJob{}
+	nsName := types.NamespacedName{Name: name, Namespace: namespace}
+	gomega.EventuallyWithOffset(1, func() error {
+		if err := k8sClient.Get(ctx, nsName, createdJob); err != nil {
+			return err
+		}
+		createdJob.Status.JobDeploymentStatus = rayjobapi.JobDeploymentStatusSuspended
+		return k8sClient.Status().Update(ctx, createdJob)
+	}, util.Timeout, util.Interval).Should(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Job controller", func() {
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(jobframework.WithManageJobsWithoutQueueName(true)),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
+		}
+
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.It("Should reconcile RayJobs", func() {
+		ginkgo.By("checking the job gets suspended when created unsuspended")
+		priorityClass := testing.MakePriorityClass(priorityClassName).
+			PriorityValue(int32(priorityValue)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, priorityClass)).Should(gomega.Succeed())
+
+		job := testingrayjob.MakeJob(jobName, jobNamespace).
+			Suspend(false).
+			WithPriorityClassName(priorityClassName).
+			Obj()
+		err := k8sClient.Create(ctx, job)
+		gomega.Expect(err).To(gomega.Succeed())
+		createdJob := &rayjobapi.RayJob{}
+
+		setInitStatus(jobName, jobNamespace)
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, types.NamespacedName{Name: jobName, Namespace: jobNamespace}, createdJob); err != nil {
+				return false
+			}
+			return createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is created without queue assigned")
+		createdWorkload := &kueue.Workload{}
+		gomega.Eventually(func() error {
+			return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
+		gomega.Expect(metav1.IsControlledBy(createdWorkload, createdJob)).To(gomega.BeTrue(), "The Workload should be owned by the Job")
+
+		ginkgo.By("checking the workload is created with priority and priorityName")
+		gomega.Expect(createdWorkload.Spec.PriorityClassName).Should(gomega.Equal(priorityClassName))
+		gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
+
+		ginkgo.By("checking the workload is updated with queue name when the job does")
+		jobQueueName := "test-queue"
+		createdJob.Annotations = map[string]string{jobframework.QueueAnnotation: jobQueueName}
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return createdWorkload.Spec.QueueName == jobQueueName
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking a second non-matching workload is deleted")
+		secondWl := &kueue.Workload{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      workloadrayjob.GetWorkloadNameForRayJob("second-workload"),
+				Namespace: createdWorkload.Namespace,
+			},
+			Spec: *createdWorkload.Spec.DeepCopy(),
+		}
+		gomega.Expect(ctrl.SetControllerReference(createdJob, secondWl, scheme.Scheme)).Should(gomega.Succeed())
+		secondWl.Spec.PodSets[0].Count += 1
+
+		gomega.Expect(k8sClient.Create(ctx, secondWl)).Should(gomega.Succeed())
+		gomega.Eventually(func() error {
+			wl := &kueue.Workload{}
+			key := types.NamespacedName{Name: secondWl.Name, Namespace: secondWl.Namespace}
+			return k8sClient.Get(ctx, key, wl)
+		}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+		// check the original wl is still there
+		gomega.Consistently(func() bool {
+			err := k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+			return err == nil
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the job is unsuspended when workload is assigned")
+		onDemandFlavor := testing.MakeResourceFlavor("on-demand").Label(labelKey, "on-demand").Obj()
+		gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).Should(gomega.Succeed())
+		spotFlavor := testing.MakeResourceFlavor("spot").Label(labelKey, "spot").Obj()
+		gomega.Expect(k8sClient.Create(ctx, spotFlavor)).Should(gomega.Succeed())
+		clusterQueue := testing.MakeClusterQueue("cluster-queue").
+			ResourceGroup(
+				*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "5").Obj(),
+				*testing.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
+			).Obj()
+		admission := &kueue.Admission{
+			ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+			PodSetAssignments: []kueue.PodSetAssignment{{
+				Name: "Launcher",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "on-demand",
+				},
+			}, {
+				Name: "Worker",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "spot",
+				},
+			}},
+		}
+		gomega.Expect(util.SetAdmission(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+		lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return !createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Eventually(func() bool {
+			ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "Started", corev1.EventTypeNormal, fmt.Sprintf("Admitted by clusterQueue %v", clusterQueue.Name))
+			return ok
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(onDemandFlavor.Name))
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(spotFlavor.Name))
+		gomega.Consistently(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the job gets suspended when parallelism changes and the added node selectors are removed")
+		parallelism := pointer.Int32Deref(job.Spec.RayClusterSpec.WorkerGroupSpecs[0].Replicas, 1)
+		newParallelism := int32(parallelism + 1)
+		createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Replicas = &newParallelism
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return createdJob.Spec.Suspend && len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector) == 0
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Eventually(func() bool {
+			ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "DeletedWorkload", corev1.EventTypeNormal, fmt.Sprintf("Deleted not matching Workload: %v", wlLookupKey.String()))
+			return ok
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is updated with new count")
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return createdWorkload.Spec.PodSets[1].Count == newParallelism
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(createdWorkload.Status.Admission).Should(gomega.BeNil())
+
+		ginkgo.By("checking the job is unsuspended and selectors added when workload is assigned again")
+		admission = &kueue.Admission{
+			ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+			PodSetAssignments: []kueue.PodSetAssignment{{
+				Name: createdWorkload.Spec.PodSets[0].Name,
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "on-demand",
+				},
+			}, {
+				Name: createdWorkload.Spec.PodSets[1].Name,
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "spot",
+				},
+			}},
+		}
+		gomega.Expect(util.SetAdmission(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return !createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(onDemandFlavor.Name))
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(spotFlavor.Name))
+		gomega.Consistently(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is finished when job is completed")
+		createdJob.Status.JobDeploymentStatus = rayjobapi.JobDeploymentStatusComplete
+		createdJob.Status.JobStatus = rayjobapi.JobStatusSucceeded
+		createdJob.Status.Message = "Job finished by test"
+
+		gomega.Expect(k8sClient.Status().Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadFinished)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	})
+})
+
+var _ = ginkgo.Describe("Job controller for workloads when only jobs with queue are managed", func() {
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
```

  ```suggestion
  			DepCRDPaths:  []string{rayCrdPath},
  ```

- **test/integration/controller/rayjob/rayjob_controller_test.go:326** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-21T17:20:17Z
```diff
@@ -0,0 +1,564 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"fmt"
+
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/pointer"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	workloadrayjob "sigs.k8s.io/kueue/pkg/controller/jobs/rayjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	testingrayjob "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	jobName           = "test-job"
+	jobNamespace      = "default"
+	labelKey          = "cloud.provider.com/instance"
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+)
+
+var (
+	wlLookupKey               = types.NamespacedName{Name: workloadrayjob.GetWorkloadNameForRayJob(jobName), Namespace: jobNamespace}
+	ignoreConditionTimestamps = cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+func setInitStatus(name, namespace string) {
+	createdJob := &rayjobapi.RayJob{}
+	nsName := types.NamespacedName{Name: name, Namespace: namespace}
+	gomega.EventuallyWithOffset(1, func() error {
+		if err := k8sClient.Get(ctx, nsName, createdJob); err != nil {
+			return err
+		}
+		createdJob.Status.JobDeploymentStatus = rayjobapi.JobDeploymentStatusSuspended
+		return k8sClient.Status().Update(ctx, createdJob)
+	}, util.Timeout, util.Interval).Should(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Job controller", func() {
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(jobframework.WithManageJobsWithoutQueueName(true)),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
+		}
+
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.It("Should reconcile RayJobs", func() {
+		ginkgo.By("checking the job gets suspended when created unsuspended")
+		priorityClass := testing.MakePriorityClass(priorityClassName).
+			PriorityValue(int32(priorityValue)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, priorityClass)).Should(gomega.Succeed())
+
+		job := testingrayjob.MakeJob(jobName, jobNamespace).
+			Suspend(false).
+			WithPriorityClassName(priorityClassName).
+			Obj()
+		err := k8sClient.Create(ctx, job)
+		gomega.Expect(err).To(gomega.Succeed())
+		createdJob := &rayjobapi.RayJob{}
+
+		setInitStatus(jobName, jobNamespace)
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, types.NamespacedName{Name: jobName, Namespace: jobNamespace}, createdJob); err != nil {
+				return false
+			}
+			return createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is created without queue assigned")
+		createdWorkload := &kueue.Workload{}
+		gomega.Eventually(func() error {
+			return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
+		gomega.Expect(metav1.IsControlledBy(createdWorkload, createdJob)).To(gomega.BeTrue(), "The Workload should be owned by the Job")
+
+		ginkgo.By("checking the workload is created with priority and priorityName")
+		gomega.Expect(createdWorkload.Spec.PriorityClassName).Should(gomega.Equal(priorityClassName))
+		gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
+
+		ginkgo.By("checking the workload is updated with queue name when the job does")
+		jobQueueName := "test-queue"
+		createdJob.Annotations = map[string]string{jobframework.QueueAnnotation: jobQueueName}
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return createdWorkload.Spec.QueueName == jobQueueName
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking a second non-matching workload is deleted")
+		secondWl := &kueue.Workload{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      workloadrayjob.GetWorkloadNameForRayJob("second-workload"),
+				Namespace: createdWorkload.Namespace,
+			},
+			Spec: *createdWorkload.Spec.DeepCopy(),
+		}
+		gomega.Expect(ctrl.SetControllerReference(createdJob, secondWl, scheme.Scheme)).Should(gomega.Succeed())
+		secondWl.Spec.PodSets[0].Count += 1
+
+		gomega.Expect(k8sClient.Create(ctx, secondWl)).Should(gomega.Succeed())
+		gomega.Eventually(func() error {
+			wl := &kueue.Workload{}
+			key := types.NamespacedName{Name: secondWl.Name, Namespace: secondWl.Namespace}
+			return k8sClient.Get(ctx, key, wl)
+		}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+		// check the original wl is still there
+		gomega.Consistently(func() bool {
+			err := k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+			return err == nil
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the job is unsuspended when workload is assigned")
+		onDemandFlavor := testing.MakeResourceFlavor("on-demand").Label(labelKey, "on-demand").Obj()
+		gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).Should(gomega.Succeed())
+		spotFlavor := testing.MakeResourceFlavor("spot").Label(labelKey, "spot").Obj()
+		gomega.Expect(k8sClient.Create(ctx, spotFlavor)).Should(gomega.Succeed())
+		clusterQueue := testing.MakeClusterQueue("cluster-queue").
+			ResourceGroup(
+				*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "5").Obj(),
+				*testing.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
+			).Obj()
+		admission := &kueue.Admission{
+			ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+			PodSetAssignments: []kueue.PodSetAssignment{{
+				Name: "Launcher",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "on-demand",
+				},
+			}, {
+				Name: "Worker",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "spot",
+				},
+			}},
+		}
+		gomega.Expect(util.SetAdmission(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+		lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return !createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Eventually(func() bool {
+			ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "Started", corev1.EventTypeNormal, fmt.Sprintf("Admitted by clusterQueue %v", clusterQueue.Name))
+			return ok
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(onDemandFlavor.Name))
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(spotFlavor.Name))
+		gomega.Consistently(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the job gets suspended when parallelism changes and the added node selectors are removed")
+		parallelism := pointer.Int32Deref(job.Spec.RayClusterSpec.WorkerGroupSpecs[0].Replicas, 1)
+		newParallelism := int32(parallelism + 1)
+		createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Replicas = &newParallelism
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return createdJob.Spec.Suspend && len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector) == 0
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Eventually(func() bool {
+			ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "DeletedWorkload", corev1.EventTypeNormal, fmt.Sprintf("Deleted not matching Workload: %v", wlLookupKey.String()))
+			return ok
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is updated with new count")
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return createdWorkload.Spec.PodSets[1].Count == newParallelism
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(createdWorkload.Status.Admission).Should(gomega.BeNil())
+
+		ginkgo.By("checking the job is unsuspended and selectors added when workload is assigned again")
+		admission = &kueue.Admission{
+			ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+			PodSetAssignments: []kueue.PodSetAssignment{{
+				Name: createdWorkload.Spec.PodSets[0].Name,
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "on-demand",
+				},
+			}, {
+				Name: createdWorkload.Spec.PodSets[1].Name,
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "spot",
+				},
+			}},
+		}
+		gomega.Expect(util.SetAdmission(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return !createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(onDemandFlavor.Name))
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(spotFlavor.Name))
+		gomega.Consistently(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is finished when job is completed")
+		createdJob.Status.JobDeploymentStatus = rayjobapi.JobDeploymentStatusComplete
+		createdJob.Status.JobStatus = rayjobapi.JobStatusSucceeded
+		createdJob.Status.Message = "Job finished by test"
+
+		gomega.Expect(k8sClient.Status().Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadFinished)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	})
+})
+
+var _ = ginkgo.Describe("Job controller for workloads when only jobs with queue are managed", func() {
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+	ginkgo.It("Should reconcile jobs only when queue is set", func() {
+		ginkgo.By("checking the workload is not created when queue name is not set")
+		job := testingrayjob.MakeJob(jobName, jobNamespace).Obj()
+		gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+		lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+		createdJob := &rayjobapi.RayJob{}
+		setInitStatus(jobName, jobNamespace)
+		gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+		createdWorkload := &kueue.Workload{}
+		gomega.Consistently(func() bool {
+			return apierrors.IsNotFound(k8sClient.Get(ctx, wlLookupKey, createdWorkload))
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is created when queue name is set")
+		jobQueueName := "test-queue"
+		if createdJob.Labels == nil {
+			createdJob.Labels = map[string]string{jobframework.QueueAnnotation: jobQueueName}
+		} else {
+			createdJob.Labels[jobframework.QueueLabel] = jobQueueName
+		}
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() error {
+			return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+
+})
+
+var _ = ginkgo.Describe("Job controller when waitForPodsReady enabled", func() {
+	type podsReadyTestSpec struct {
+		beforeJobStatus *rayjobapi.RayJobStatus
+		beforeCondition *metav1.Condition
+		jobStatus       rayjobapi.RayJobStatus
+		suspended       bool
+		wantCondition   *metav1.Condition
+	}
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(jobframework.WithWaitForPodsReady(true)),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
```

  ```suggestion
  			DepCRDPaths:  []string{rayCrdPath},
  ```

- **test/integration/controller/rayjob/rayjob_controller_test.go:372** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-21T17:21:07Z
```diff
@@ -0,0 +1,564 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"fmt"
+
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/pointer"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	workloadrayjob "sigs.k8s.io/kueue/pkg/controller/jobs/rayjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	testingrayjob "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	jobName           = "test-job"
+	jobNamespace      = "default"
+	labelKey          = "cloud.provider.com/instance"
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+)
+
+var (
+	wlLookupKey               = types.NamespacedName{Name: workloadrayjob.GetWorkloadNameForRayJob(jobName), Namespace: jobNamespace}
+	ignoreConditionTimestamps = cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+func setInitStatus(name, namespace string) {
+	createdJob := &rayjobapi.RayJob{}
+	nsName := types.NamespacedName{Name: name, Namespace: namespace}
+	gomega.EventuallyWithOffset(1, func() error {
+		if err := k8sClient.Get(ctx, nsName, createdJob); err != nil {
+			return err
+		}
+		createdJob.Status.JobDeploymentStatus = rayjobapi.JobDeploymentStatusSuspended
+		return k8sClient.Status().Update(ctx, createdJob)
+	}, util.Timeout, util.Interval).Should(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Job controller", func() {
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(jobframework.WithManageJobsWithoutQueueName(true)),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
+		}
+
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.It("Should reconcile RayJobs", func() {
+		ginkgo.By("checking the job gets suspended when created unsuspended")
+		priorityClass := testing.MakePriorityClass(priorityClassName).
+			PriorityValue(int32(priorityValue)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, priorityClass)).Should(gomega.Succeed())
+
+		job := testingrayjob.MakeJob(jobName, jobNamespace).
+			Suspend(false).
+			WithPriorityClassName(priorityClassName).
+			Obj()
+		err := k8sClient.Create(ctx, job)
+		gomega.Expect(err).To(gomega.Succeed())
+		createdJob := &rayjobapi.RayJob{}
+
+		setInitStatus(jobName, jobNamespace)
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, types.NamespacedName{Name: jobName, Namespace: jobNamespace}, createdJob); err != nil {
+				return false
+			}
+			return createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is created without queue assigned")
+		createdWorkload := &kueue.Workload{}
+		gomega.Eventually(func() error {
+			return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
+		gomega.Expect(metav1.IsControlledBy(createdWorkload, createdJob)).To(gomega.BeTrue(), "The Workload should be owned by the Job")
+
+		ginkgo.By("checking the workload is created with priority and priorityName")
+		gomega.Expect(createdWorkload.Spec.PriorityClassName).Should(gomega.Equal(priorityClassName))
+		gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
+
+		ginkgo.By("checking the workload is updated with queue name when the job does")
+		jobQueueName := "test-queue"
+		createdJob.Annotations = map[string]string{jobframework.QueueAnnotation: jobQueueName}
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return createdWorkload.Spec.QueueName == jobQueueName
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking a second non-matching workload is deleted")
+		secondWl := &kueue.Workload{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      workloadrayjob.GetWorkloadNameForRayJob("second-workload"),
+				Namespace: createdWorkload.Namespace,
+			},
+			Spec: *createdWorkload.Spec.DeepCopy(),
+		}
+		gomega.Expect(ctrl.SetControllerReference(createdJob, secondWl, scheme.Scheme)).Should(gomega.Succeed())
+		secondWl.Spec.PodSets[0].Count += 1
+
+		gomega.Expect(k8sClient.Create(ctx, secondWl)).Should(gomega.Succeed())
+		gomega.Eventually(func() error {
+			wl := &kueue.Workload{}
+			key := types.NamespacedName{Name: secondWl.Name, Namespace: secondWl.Namespace}
+			return k8sClient.Get(ctx, key, wl)
+		}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+		// check the original wl is still there
+		gomega.Consistently(func() bool {
+			err := k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+			return err == nil
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the job is unsuspended when workload is assigned")
+		onDemandFlavor := testing.MakeResourceFlavor("on-demand").Label(labelKey, "on-demand").Obj()
+		gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).Should(gomega.Succeed())
+		spotFlavor := testing.MakeResourceFlavor("spot").Label(labelKey, "spot").Obj()
+		gomega.Expect(k8sClient.Create(ctx, spotFlavor)).Should(gomega.Succeed())
+		clusterQueue := testing.MakeClusterQueue("cluster-queue").
+			ResourceGroup(
+				*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "5").Obj(),
+				*testing.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
+			).Obj()
+		admission := &kueue.Admission{
+			ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+			PodSetAssignments: []kueue.PodSetAssignment{{
+				Name: "Launcher",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "on-demand",
+				},
+			}, {
+				Name: "Worker",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "spot",
+				},
+			}},
+		}
+		gomega.Expect(util.SetAdmission(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+		lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return !createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Eventually(func() bool {
+			ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "Started", corev1.EventTypeNormal, fmt.Sprintf("Admitted by clusterQueue %v", clusterQueue.Name))
+			return ok
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(onDemandFlavor.Name))
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(spotFlavor.Name))
+		gomega.Consistently(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the job gets suspended when parallelism changes and the added node selectors are removed")
+		parallelism := pointer.Int32Deref(job.Spec.RayClusterSpec.WorkerGroupSpecs[0].Replicas, 1)
+		newParallelism := int32(parallelism + 1)
+		createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Replicas = &newParallelism
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return createdJob.Spec.Suspend && len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector) == 0
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Eventually(func() bool {
+			ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "DeletedWorkload", corev1.EventTypeNormal, fmt.Sprintf("Deleted not matching Workload: %v", wlLookupKey.String()))
+			return ok
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is updated with new count")
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return createdWorkload.Spec.PodSets[1].Count == newParallelism
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(createdWorkload.Status.Admission).Should(gomega.BeNil())
+
+		ginkgo.By("checking the job is unsuspended and selectors added when workload is assigned again")
+		admission = &kueue.Admission{
+			ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+			PodSetAssignments: []kueue.PodSetAssignment{{
+				Name: createdWorkload.Spec.PodSets[0].Name,
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "on-demand",
+				},
+			}, {
+				Name: createdWorkload.Spec.PodSets[1].Name,
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "spot",
+				},
+			}},
+		}
+		gomega.Expect(util.SetAdmission(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return !createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(onDemandFlavor.Name))
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(spotFlavor.Name))
+		gomega.Consistently(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is finished when job is completed")
+		createdJob.Status.JobDeploymentStatus = rayjobapi.JobDeploymentStatusComplete
+		createdJob.Status.JobStatus = rayjobapi.JobStatusSucceeded
+		createdJob.Status.Message = "Job finished by test"
+
+		gomega.Expect(k8sClient.Status().Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadFinished)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	})
+})
+
+var _ = ginkgo.Describe("Job controller for workloads when only jobs with queue are managed", func() {
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+	ginkgo.It("Should reconcile jobs only when queue is set", func() {
+		ginkgo.By("checking the workload is not created when queue name is not set")
+		job := testingrayjob.MakeJob(jobName, jobNamespace).Obj()
+		gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+		lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+		createdJob := &rayjobapi.RayJob{}
+		setInitStatus(jobName, jobNamespace)
+		gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+		createdWorkload := &kueue.Workload{}
+		gomega.Consistently(func() bool {
+			return apierrors.IsNotFound(k8sClient.Get(ctx, wlLookupKey, createdWorkload))
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is created when queue name is set")
+		jobQueueName := "test-queue"
+		if createdJob.Labels == nil {
+			createdJob.Labels = map[string]string{jobframework.QueueAnnotation: jobQueueName}
+		} else {
+			createdJob.Labels[jobframework.QueueLabel] = jobQueueName
+		}
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() error {
+			return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+
+})
+
+var _ = ginkgo.Describe("Job controller when waitForPodsReady enabled", func() {
+	type podsReadyTestSpec struct {
+		beforeJobStatus *rayjobapi.RayJobStatus
+		beforeCondition *metav1.Condition
+		jobStatus       rayjobapi.RayJobStatus
+		suspended       bool
+		wantCondition   *metav1.Condition
+	}
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(jobframework.WithWaitForPodsReady(true)),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.DescribeTable("Single job at different stages of progress towards completion",
+
+		func(podsReadyTestSpec podsReadyTestSpec) {
+			ginkgo.By("Create a resource flavor")
+			defaultFlavor := testing.MakeResourceFlavor("default").Label(labelKey, "default").Obj()
+			gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).Should(gomega.Succeed())
+
+			ginkgo.By("Create a job")
+			job := testingrayjob.MakeJob(jobName, jobNamespace).Obj()
+			jobQueueName := "test-queue"
+			job.Annotations = map[string]string{jobframework.QueueAnnotation: jobQueueName}
+			gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+			setInitStatus(jobName, jobNamespace)
+			createdJob := &rayjobapi.RayJob{}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+			ginkgo.By("Fetch the workload created for the job")
+			createdWorkload := &kueue.Workload{}
+			gomega.Eventually(func() error {
+				return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Admit the workload created for the job")
+			admission := &kueue.Admission{
+				ClusterQueue: kueue.ClusterQueueReference("foo"),
+				PodSetAssignments: []kueue.PodSetAssignment{{
+					Name: "Launcher",
+					Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+						corev1.ResourceCPU: "default",
+					},
+				}, {
+					Name: "Worker",
+					Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+						corev1.ResourceCPU: "default",
+					},
+				}},
+			}
```

  Can we use `testing.MakeAdmission()`?

- **test/integration/controller/rayjob/rayjob_controller_test.go:506** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-21T17:22:32Z
```diff
@@ -0,0 +1,564 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"fmt"
+
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/pointer"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	workloadrayjob "sigs.k8s.io/kueue/pkg/controller/jobs/rayjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	testingrayjob "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	jobName           = "test-job"
+	jobNamespace      = "default"
+	labelKey          = "cloud.provider.com/instance"
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+)
+
+var (
+	wlLookupKey               = types.NamespacedName{Name: workloadrayjob.GetWorkloadNameForRayJob(jobName), Namespace: jobNamespace}
+	ignoreConditionTimestamps = cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+func setInitStatus(name, namespace string) {
+	createdJob := &rayjobapi.RayJob{}
+	nsName := types.NamespacedName{Name: name, Namespace: namespace}
+	gomega.EventuallyWithOffset(1, func() error {
+		if err := k8sClient.Get(ctx, nsName, createdJob); err != nil {
+			return err
+		}
+		createdJob.Status.JobDeploymentStatus = rayjobapi.JobDeploymentStatusSuspended
+		return k8sClient.Status().Update(ctx, createdJob)
+	}, util.Timeout, util.Interval).Should(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Job controller", func() {
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(jobframework.WithManageJobsWithoutQueueName(true)),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
+		}
+
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.It("Should reconcile RayJobs", func() {
+		ginkgo.By("checking the job gets suspended when created unsuspended")
+		priorityClass := testing.MakePriorityClass(priorityClassName).
+			PriorityValue(int32(priorityValue)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, priorityClass)).Should(gomega.Succeed())
+
+		job := testingrayjob.MakeJob(jobName, jobNamespace).
+			Suspend(false).
+			WithPriorityClassName(priorityClassName).
+			Obj()
+		err := k8sClient.Create(ctx, job)
+		gomega.Expect(err).To(gomega.Succeed())
+		createdJob := &rayjobapi.RayJob{}
+
+		setInitStatus(jobName, jobNamespace)
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, types.NamespacedName{Name: jobName, Namespace: jobNamespace}, createdJob); err != nil {
+				return false
+			}
+			return createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is created without queue assigned")
+		createdWorkload := &kueue.Workload{}
+		gomega.Eventually(func() error {
+			return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
+		gomega.Expect(metav1.IsControlledBy(createdWorkload, createdJob)).To(gomega.BeTrue(), "The Workload should be owned by the Job")
+
+		ginkgo.By("checking the workload is created with priority and priorityName")
+		gomega.Expect(createdWorkload.Spec.PriorityClassName).Should(gomega.Equal(priorityClassName))
+		gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
+
+		ginkgo.By("checking the workload is updated with queue name when the job does")
+		jobQueueName := "test-queue"
+		createdJob.Annotations = map[string]string{jobframework.QueueAnnotation: jobQueueName}
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return createdWorkload.Spec.QueueName == jobQueueName
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking a second non-matching workload is deleted")
+		secondWl := &kueue.Workload{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      workloadrayjob.GetWorkloadNameForRayJob("second-workload"),
+				Namespace: createdWorkload.Namespace,
+			},
+			Spec: *createdWorkload.Spec.DeepCopy(),
+		}
+		gomega.Expect(ctrl.SetControllerReference(createdJob, secondWl, scheme.Scheme)).Should(gomega.Succeed())
+		secondWl.Spec.PodSets[0].Count += 1
+
+		gomega.Expect(k8sClient.Create(ctx, secondWl)).Should(gomega.Succeed())
+		gomega.Eventually(func() error {
+			wl := &kueue.Workload{}
+			key := types.NamespacedName{Name: secondWl.Name, Namespace: secondWl.Namespace}
+			return k8sClient.Get(ctx, key, wl)
+		}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+		// check the original wl is still there
+		gomega.Consistently(func() bool {
+			err := k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+			return err == nil
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the job is unsuspended when workload is assigned")
+		onDemandFlavor := testing.MakeResourceFlavor("on-demand").Label(labelKey, "on-demand").Obj()
+		gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).Should(gomega.Succeed())
+		spotFlavor := testing.MakeResourceFlavor("spot").Label(labelKey, "spot").Obj()
+		gomega.Expect(k8sClient.Create(ctx, spotFlavor)).Should(gomega.Succeed())
+		clusterQueue := testing.MakeClusterQueue("cluster-queue").
+			ResourceGroup(
+				*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "5").Obj(),
+				*testing.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
+			).Obj()
+		admission := &kueue.Admission{
+			ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+			PodSetAssignments: []kueue.PodSetAssignment{{
+				Name: "Launcher",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "on-demand",
+				},
+			}, {
+				Name: "Worker",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "spot",
+				},
+			}},
+		}
+		gomega.Expect(util.SetAdmission(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+		lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return !createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Eventually(func() bool {
+			ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "Started", corev1.EventTypeNormal, fmt.Sprintf("Admitted by clusterQueue %v", clusterQueue.Name))
+			return ok
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(onDemandFlavor.Name))
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(spotFlavor.Name))
+		gomega.Consistently(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the job gets suspended when parallelism changes and the added node selectors are removed")
+		parallelism := pointer.Int32Deref(job.Spec.RayClusterSpec.WorkerGroupSpecs[0].Replicas, 1)
+		newParallelism := int32(parallelism + 1)
+		createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Replicas = &newParallelism
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return createdJob.Spec.Suspend && len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector) == 0
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Eventually(func() bool {
+			ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "DeletedWorkload", corev1.EventTypeNormal, fmt.Sprintf("Deleted not matching Workload: %v", wlLookupKey.String()))
+			return ok
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is updated with new count")
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return createdWorkload.Spec.PodSets[1].Count == newParallelism
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(createdWorkload.Status.Admission).Should(gomega.BeNil())
+
+		ginkgo.By("checking the job is unsuspended and selectors added when workload is assigned again")
+		admission = &kueue.Admission{
+			ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+			PodSetAssignments: []kueue.PodSetAssignment{{
+				Name: createdWorkload.Spec.PodSets[0].Name,
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "on-demand",
+				},
+			}, {
+				Name: createdWorkload.Spec.PodSets[1].Name,
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "spot",
+				},
+			}},
+		}
+		gomega.Expect(util.SetAdmission(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return !createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(onDemandFlavor.Name))
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(spotFlavor.Name))
+		gomega.Consistently(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is finished when job is completed")
+		createdJob.Status.JobDeploymentStatus = rayjobapi.JobDeploymentStatusComplete
+		createdJob.Status.JobStatus = rayjobapi.JobStatusSucceeded
+		createdJob.Status.Message = "Job finished by test"
+
+		gomega.Expect(k8sClient.Status().Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadFinished)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	})
+})
+
+var _ = ginkgo.Describe("Job controller for workloads when only jobs with queue are managed", func() {
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+	ginkgo.It("Should reconcile jobs only when queue is set", func() {
+		ginkgo.By("checking the workload is not created when queue name is not set")
+		job := testingrayjob.MakeJob(jobName, jobNamespace).Obj()
+		gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+		lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+		createdJob := &rayjobapi.RayJob{}
+		setInitStatus(jobName, jobNamespace)
+		gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+		createdWorkload := &kueue.Workload{}
+		gomega.Consistently(func() bool {
+			return apierrors.IsNotFound(k8sClient.Get(ctx, wlLookupKey, createdWorkload))
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is created when queue name is set")
+		jobQueueName := "test-queue"
+		if createdJob.Labels == nil {
+			createdJob.Labels = map[string]string{jobframework.QueueAnnotation: jobQueueName}
+		} else {
+			createdJob.Labels[jobframework.QueueLabel] = jobQueueName
+		}
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() error {
+			return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+
+})
+
+var _ = ginkgo.Describe("Job controller when waitForPodsReady enabled", func() {
+	type podsReadyTestSpec struct {
+		beforeJobStatus *rayjobapi.RayJobStatus
+		beforeCondition *metav1.Condition
+		jobStatus       rayjobapi.RayJobStatus
+		suspended       bool
+		wantCondition   *metav1.Condition
+	}
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(jobframework.WithWaitForPodsReady(true)),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.DescribeTable("Single job at different stages of progress towards completion",
+
+		func(podsReadyTestSpec podsReadyTestSpec) {
+			ginkgo.By("Create a resource flavor")
+			defaultFlavor := testing.MakeResourceFlavor("default").Label(labelKey, "default").Obj()
+			gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).Should(gomega.Succeed())
+
+			ginkgo.By("Create a job")
+			job := testingrayjob.MakeJob(jobName, jobNamespace).Obj()
+			jobQueueName := "test-queue"
+			job.Annotations = map[string]string{jobframework.QueueAnnotation: jobQueueName}
+			gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+			setInitStatus(jobName, jobNamespace)
+			createdJob := &rayjobapi.RayJob{}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+			ginkgo.By("Fetch the workload created for the job")
+			createdWorkload := &kueue.Workload{}
+			gomega.Eventually(func() error {
+				return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Admit the workload created for the job")
+			admission := &kueue.Admission{
+				ClusterQueue: kueue.ClusterQueueReference("foo"),
+				PodSetAssignments: []kueue.PodSetAssignment{{
+					Name: "Launcher",
+					Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+						corev1.ResourceCPU: "default",
+					},
+				}, {
+					Name: "Worker",
+					Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+						corev1.ResourceCPU: "default",
+					},
+				}},
+			}
+			gomega.Expect(util.SetAdmission(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
+
+			ginkgo.By("Await for the job to be unsuspended")
+			gomega.Eventually(func() bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+				return createdJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.BeFalse())
+
+			if podsReadyTestSpec.beforeJobStatus != nil {
+				ginkgo.By("Update the job status to simulate its initial progress towards completion")
+				createdJob.Status = *podsReadyTestSpec.beforeJobStatus
+				gomega.Expect(k8sClient.Status().Update(ctx, createdJob)).Should(gomega.Succeed())
+				gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+			}
+
+			if podsReadyTestSpec.beforeCondition != nil {
+				ginkgo.By("Update the workload status")
+				gomega.Eventually(func() *metav1.Condition {
+					gomega.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
+					return apimeta.FindStatusCondition(createdWorkload.Status.Conditions, kueue.WorkloadPodsReady)
+				}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(podsReadyTestSpec.beforeCondition, ignoreConditionTimestamps))
+			}
+
+			ginkgo.By("Update the job status to simulate its progress towards completion")
+			createdJob.Status = podsReadyTestSpec.jobStatus
+			gomega.Expect(k8sClient.Status().Update(ctx, createdJob)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+			if podsReadyTestSpec.suspended {
+				ginkgo.By("Unset admission of the workload to suspend the job")
+				gomega.Eventually(func() error {
+					// the update may need to be retried due to a conflict as the workload gets
+					// also updated due to setting of the job status.
+					if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+						return err
+					}
+					return util.SetAdmission(ctx, k8sClient, createdWorkload, nil)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			}
+
+			ginkgo.By("Verify the PodsReady condition is added")
+			gomega.Eventually(func() *metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
+				return apimeta.FindStatusCondition(createdWorkload.Status.Conditions, kueue.WorkloadPodsReady)
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(podsReadyTestSpec.wantCondition, ignoreConditionTimestamps))
+		},
+		ginkgo.Entry("No progress", podsReadyTestSpec{
+			wantCondition: &metav1.Condition{
+				Type:    kueue.WorkloadPodsReady,
+				Status:  metav1.ConditionFalse,
+				Reason:  "PodsReady",
+				Message: "Not all pods are ready or succeeded",
+			},
+		}),
+		ginkgo.Entry("Running RayJob", podsReadyTestSpec{
+			jobStatus: rayjobapi.RayJobStatus{
+				JobDeploymentStatus: rayjobapi.JobDeploymentStatusRunning,
+				RayClusterStatus: rayjobapi.RayClusterStatus{
+					State: rayjobapi.Ready,
+				},
+			},
+			wantCondition: &metav1.Condition{
+				Type:    kueue.WorkloadPodsReady,
+				Status:  metav1.ConditionTrue,
+				Reason:  "PodsReady",
+				Message: "All pods were ready or succeeded since the workload admission",
+			},
+		}),
+		ginkgo.Entry("Running RayJob; PodsReady=False before", podsReadyTestSpec{
+			beforeCondition: &metav1.Condition{
+				Type:    kueue.WorkloadPodsReady,
+				Status:  metav1.ConditionFalse,
+				Reason:  "PodsReady",
+				Message: "Not all pods are ready or succeeded",
+			},
+			jobStatus: rayjobapi.RayJobStatus{
+				JobDeploymentStatus: rayjobapi.JobDeploymentStatusRunning,
+				RayClusterStatus: rayjobapi.RayClusterStatus{
+					State: rayjobapi.Ready,
+				},
+			},
+			wantCondition: &metav1.Condition{
+				Type:    kueue.WorkloadPodsReady,
+				Status:  metav1.ConditionTrue,
+				Reason:  "PodsReady",
+				Message: "All pods were ready or succeeded since the workload admission",
+			},
+		}),
+		ginkgo.Entry("Job suspended; PodsReady=True before", podsReadyTestSpec{
+			beforeJobStatus: &rayjobapi.RayJobStatus{
+				JobDeploymentStatus: rayjobapi.JobDeploymentStatusRunning,
+				RayClusterStatus: rayjobapi.RayClusterStatus{
+					State: rayjobapi.Ready,
+				},
+			},
+			beforeCondition: &metav1.Condition{
+				Type:    kueue.WorkloadPodsReady,
+				Status:  metav1.ConditionTrue,
+				Reason:  "PodsReady",
+				Message: "All pods were ready or succeeded since the workload admission",
+			},
+			jobStatus: rayjobapi.RayJobStatus{
+				JobDeploymentStatus: rayjobapi.JobDeploymentStatusSuspended,
+			},
+			suspended: true,
+			wantCondition: &metav1.Condition{
+				Type:    kueue.WorkloadPodsReady,
+				Status:  metav1.ConditionFalse,
+				Reason:  "PodsReady",
+				Message: "Not all pods are ready or succeeded",
+			},
+		}),
+	)
+})
+
+var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
+	const (
+		instanceKey = "cloud.provider.com/instance"
+	)
+
+	var (
+		ns                  *corev1.Namespace
+		onDemandFlavor      *kueue.ResourceFlavor
+		spotUntaintedFlavor *kueue.ResourceFlavor
+		clusterQueue        *kueue.ClusterQueue
+		localQueue          *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerAndSchedulerSetup(),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
```

  ```suggestion
  			DepCRDPaths:  []string{rayCrdPath},
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-22T08:56:15Z

- **config/components/manager/controller_manager_config.yaml:34** by [@trasc](https://github.com/trasc) — 2023-05-22T05:42:08Z
```diff
@@ -31,3 +31,4 @@ integrations:
   frameworks:
   - "batch/job"
 # - "kubeflow.org/mpijob"
+# - "ray.io/rayjob"
```

  done

- **pkg/controller/jobs/rayjob/rayjob_controller.go:122** by [@trasc](https://github.com/trasc) — 2023-05-22T05:42:58Z
```diff
@@ -0,0 +1,284 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a RayJob object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+var _ jobframework.GenericJob = (*RayJob)(nil)
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
```

  yes

- **pkg/controller/jobs/rayjob/rayjob_controller.go:215** by [@trasc](https://github.com/trasc) — 2023-05-22T05:51:46Z
```diff
@@ -0,0 +1,284 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a RayJob object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+var _ jobframework.GenericJob = (*RayJob)(nil)
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	if len(nodeSelectors) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if !equality.Semantic.DeepEqual(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector) {
+		headPodSpec.NodeSelector = cloneSelectors(nodeSelectors[0].NodeSelector)
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector) {
+			workerPodSpec.NodeSelector = cloneSelectors(nodeSelectors[index+1].NodeSelector)
+		}
+	}
+}
+
+func (j *RayJob) Finished() (metav1.Condition, bool) {
+	condition := metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  string(j.Status.JobStatus),
+		Message: j.Status.Message,
+	}
+	finished := false
+	switch j.Status.JobDeploymentStatus {
+	case rayjobapi.JobDeploymentStatusComplete, rayjobapi.JobDeploymentStatusFailedJobDeploy, rayjobapi.JobDeploymentStatusFailedToGetOrCreateRayCluster:
+		finished = true
+	default:
+		finished = false
+	}
+	return condition, finished
+}
+
+func (j *RayJob) EquivalentToWorkload(wl kueue.Workload) bool {
+	if len(wl.Spec.PodSets) != (len(j.Spec.RayClusterSpec.WorkerGroupSpecs) + 1) {
+		return false
+	}
+
+	podSets := wl.Spec.PodSets
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if podSets[0].Count != 1 {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.InitContainers, podSets[0].Template.Spec.InitContainers) {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.Containers, podSets[0].Template.Spec.Containers) {
+		return false
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		if podSets[index+1].Count != *j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Replicas {
```

  The auto-scaling dose not grantee that once it scaled down it will not scale up again, so in my opinion we cannot safely reclaim the resources if the count goes don at some point.

- **pkg/controller/jobs/rayjob/rayjob_controller.go:190** by [@trasc](https://github.com/trasc) — 2023-05-22T05:56:21Z
```diff
@@ -0,0 +1,284 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a RayJob object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+var _ jobframework.GenericJob = (*RayJob)(nil)
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	if len(nodeSelectors) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if !equality.Semantic.DeepEqual(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector) {
+		headPodSpec.NodeSelector = cloneSelectors(nodeSelectors[0].NodeSelector)
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector) {
+			workerPodSpec.NodeSelector = cloneSelectors(nodeSelectors[index+1].NodeSelector)
+		}
+	}
+}
+
+func (j *RayJob) Finished() (metav1.Condition, bool) {
+	condition := metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  string(j.Status.JobStatus),
+		Message: j.Status.Message,
+	}
+	finished := false
+	switch j.Status.JobDeploymentStatus {
+	case rayjobapi.JobDeploymentStatusComplete, rayjobapi.JobDeploymentStatusFailedJobDeploy, rayjobapi.JobDeploymentStatusFailedToGetOrCreateRayCluster:
```

  done

- **pkg/controller/jobs/rayjob/rayjob_controller.go:215** by [@trasc](https://github.com/trasc) — 2023-05-22T06:17:43Z
```diff
@@ -0,0 +1,284 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a RayJob object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+var _ jobframework.GenericJob = (*RayJob)(nil)
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	if len(nodeSelectors) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if !equality.Semantic.DeepEqual(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector) {
+		headPodSpec.NodeSelector = cloneSelectors(nodeSelectors[0].NodeSelector)
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector) {
+			workerPodSpec.NodeSelector = cloneSelectors(nodeSelectors[index+1].NodeSelector)
+		}
+	}
+}
+
+func (j *RayJob) Finished() (metav1.Condition, bool) {
+	condition := metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  string(j.Status.JobStatus),
+		Message: j.Status.Message,
+	}
+	finished := false
+	switch j.Status.JobDeploymentStatus {
+	case rayjobapi.JobDeploymentStatusComplete, rayjobapi.JobDeploymentStatusFailedJobDeploy, rayjobapi.JobDeploymentStatusFailedToGetOrCreateRayCluster:
+		finished = true
+	default:
+		finished = false
+	}
+	return condition, finished
+}
+
+func (j *RayJob) EquivalentToWorkload(wl kueue.Workload) bool {
+	if len(wl.Spec.PodSets) != (len(j.Spec.RayClusterSpec.WorkerGroupSpecs) + 1) {
+		return false
+	}
+
+	podSets := wl.Spec.PodSets
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if podSets[0].Count != 1 {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.InitContainers, podSets[0].Template.Spec.InitContainers) {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.Containers, podSets[0].Template.Spec.Containers) {
+		return false
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		if podSets[index+1].Count != *j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Replicas {
```

  done

- **pkg/controller/jobs/rayjob/rayjob_controller_test.go:138** by [@trasc](https://github.com/trasc) — 2023-05-22T07:10:17Z
```diff
@@ -0,0 +1,384 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	testingrayutil "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+)
+
+func TestPodSets(t *testing.T) {
+	job := testingrayutil.MakeJob("job", "ns").
+		WithHeadGroupSpec(
+			rayjobapi.HeadGroupSpec{
+				Template: corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name: "head_c",
+							},
+						},
+					},
+				},
+			},
+		).
+		WithWorkerGroups(
+			rayjobapi.WorkerGroupSpec{
+				GroupName: "group1",
+				Template: corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name: "group1_c",
+							},
+						},
+					},
+				},
+			},
+			rayjobapi.WorkerGroupSpec{
+				GroupName: "group2",
+				Replicas:  pointer.Int32(3),
+				Template: corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name: "group2_c",
+							},
+						},
+					},
+				},
+			},
+		).
+		Obj()
+
+	wantPodSets := []kueue.PodSet{
+		{
+			Name:  "head",
+			Count: 1,
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					Containers: []corev1.Container{
+						{
+							Name: "head_c",
+						},
+					},
+				},
+			},
+		},
+		{
+			Name:  "group1",
+			Count: 1,
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					Containers: []corev1.Container{
+						{
+							Name: "group1_c",
+						},
+					},
+				},
+			},
+		},
+		{
+			Name:  "group2",
+			Count: 3,
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					Containers: []corev1.Container{
+						{
+							Name: "group2_c",
+						},
+					},
+				},
+			},
+		},
+	}
+
+	result := ((*RayJob)(job)).PodSets()
+
+	if diff := cmp.Diff(wantPodSets, result); diff != "" {
+		t.Errorf("PodSets() mismatch (-want +got):\n%s", diff)
+	}
+}
+
+func TestEquivalentToWorkload(t *testing.T) {
+	testContainer := corev1.Container{
+		Name:  "ic1",
+		Image: "image1",
+		Resources: corev1.ResourceRequirements{
+			Requests: corev1.ResourceList{
+				corev1.ResourceCPU: resource.MustParse("1"),
+			},
+		},
+	}
+	job := (*RayJob)(testingrayutil.MakeJob("job", "ns").
+		RequestHead(corev1.ResourceCPU, "2").
```

  In which sense?

- **pkg/controller/jobs/rayjob/rayjob_controller_test.go:319** by [@trasc](https://github.com/trasc) — 2023-05-22T07:12:14Z
```diff
@@ -0,0 +1,384 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	testingrayutil "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+)
+
+func TestPodSets(t *testing.T) {
+	job := testingrayutil.MakeJob("job", "ns").
+		WithHeadGroupSpec(
+			rayjobapi.HeadGroupSpec{
+				Template: corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name: "head_c",
+							},
+						},
+					},
+				},
+			},
+		).
+		WithWorkerGroups(
+			rayjobapi.WorkerGroupSpec{
+				GroupName: "group1",
+				Template: corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name: "group1_c",
+							},
+						},
+					},
+				},
+			},
+			rayjobapi.WorkerGroupSpec{
+				GroupName: "group2",
+				Replicas:  pointer.Int32(3),
+				Template: corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name: "group2_c",
+							},
+						},
+					},
+				},
+			},
+		).
+		Obj()
+
+	wantPodSets := []kueue.PodSet{
+		{
+			Name:  "head",
+			Count: 1,
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					Containers: []corev1.Container{
+						{
+							Name: "head_c",
+						},
+					},
+				},
+			},
+		},
+		{
+			Name:  "group1",
+			Count: 1,
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					Containers: []corev1.Container{
+						{
+							Name: "group1_c",
+						},
+					},
+				},
+			},
+		},
+		{
+			Name:  "group2",
+			Count: 3,
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					Containers: []corev1.Container{
+						{
+							Name: "group2_c",
+						},
+					},
+				},
+			},
+		},
+	}
+
+	result := ((*RayJob)(job)).PodSets()
+
+	if diff := cmp.Diff(wantPodSets, result); diff != "" {
+		t.Errorf("PodSets() mismatch (-want +got):\n%s", diff)
+	}
+}
+
+func TestEquivalentToWorkload(t *testing.T) {
+	testContainer := corev1.Container{
+		Name:  "ic1",
+		Image: "image1",
+		Resources: corev1.ResourceRequirements{
+			Requests: corev1.ResourceList{
+				corev1.ResourceCPU: resource.MustParse("1"),
+			},
+		},
+	}
+	job := (*RayJob)(testingrayutil.MakeJob("job", "ns").
+		RequestHead(corev1.ResourceCPU, "2").
+		WithHeadGroupSpec(rayjobapi.HeadGroupSpec{
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					InitContainers: []corev1.Container{testContainer, testContainer},
+					Containers:     []corev1.Container{testContainer},
+				},
+			},
+		}).
+		WithWorkerGroups(rayjobapi.WorkerGroupSpec{
+			GroupName: "group1",
+			Replicas:  pointer.Int32(2),
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					InitContainers: []corev1.Container{testContainer},
+					Containers:     []corev1.Container{testContainer, testContainer, testContainer},
+				},
+			},
+		}, rayjobapi.WorkerGroupSpec{
+			GroupName: "group2",
+			Replicas:  pointer.Int32(3),
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					InitContainers: []corev1.Container{testContainer},
+					Containers:     []corev1.Container{testContainer, testContainer},
+				},
+			},
+		}).
+		Obj())
+	podSets := []kueue.PodSet{
+		*utiltesting.MakePodSet("head", 1).
+			InitContainers(testContainer, testContainer).
+			Containers(testContainer).
+			Obj(),
+		*utiltesting.MakePodSet("group1", 2).
+			InitContainers(testContainer).
+			Containers(testContainer, testContainer, testContainer).
+			Obj(),
+		*utiltesting.MakePodSet("group2", 3).
+			InitContainers(testContainer).
+			Containers(testContainer, testContainer).
+			Obj(),
+	}
+	basWorkload := utiltesting.MakeWorkload("wl", "ns").PodSets(podSets...).Obj()
+	cases := map[string]struct {
+		wl         kueue.Workload
+		wantResult bool
+	}{
+		"wrong podsets number": {
+			wl: *(&utiltesting.WorkloadWrapper{Workload: *basWorkload.DeepCopy()}).PodSets(podSets[:1]...).Obj(),
+		},
+		"bad head podSet count": {
+			wl: func() kueue.Workload {
+				wl := *basWorkload.DeepCopy()
+				wl.Spec.PodSets[0].Count = 3
+				return wl
+			}(),
+		},
+		"bad head podSet init container": {
+			wl: func() kueue.Workload {
+				wl := *basWorkload.DeepCopy()
+				wl.Spec.PodSets[0].Template.Spec.InitContainers[0].Image = "another-image"
+				return wl
+			}(),
+		},
+		"bad head podSet container": {
+			wl: func() kueue.Workload {
+				wl := *basWorkload.DeepCopy()
+				wl.Spec.PodSets[0].Template.Spec.Containers[0].Image = "another-image"
+				return wl
+			}(),
+		},
+		"bad workGroup podSet count": {
+			wl: func() kueue.Workload {
+				wl := *basWorkload.DeepCopy()
+				wl.Spec.PodSets[1].Count = 3
+				return wl
+			}(),
+		},
+		"bad workGroup podSet init container": {
+			wl: func() kueue.Workload {
+				wl := *basWorkload.DeepCopy()
+				wl.Spec.PodSets[2].Template.Spec.InitContainers[0].Image = "another-image"
+				return wl
+			}(),
+		},
+		"bad workGroup podSet container": {
+			wl: func() kueue.Workload {
+				wl := *basWorkload.DeepCopy()
+				wl.Spec.PodSets[1].Template.Spec.Containers[0].Image = "another-image"
+				return wl
+			}(),
+		},
+		"equivalent": {
+			wl:         *basWorkload.DeepCopy(),
+			wantResult: true,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			if job.EquivalentToWorkload(tc.wl) != tc.wantResult {
+				t.Fatalf("Unexpected result, wanted: %v", tc.wantResult)
+			}
+		})
+	}
+}
+
+func TestPriorityClass(t *testing.T) {
+	cases := map[string]struct {
+		job           *rayjobapi.RayJob
+		wantClassName string
+	}{
+		"none": {
+			job: testingrayutil.MakeJob("job", "ns").Obj(),
+		},
+		"from head": {
+			job:           testingrayutil.MakeJob("job", "ns").WithPriorityClassName("head-prio-class").WithWorkerPriorityClassName("worker-prio-class").Obj(),
+			wantClassName: "head-prio-class",
+		},
+		"from worker": {
+			job:           testingrayutil.MakeJob("job", "ns").WithWorkerPriorityClassName("worker-prio-class").Obj(),
+			wantClassName: "worker-prio-class",
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			got := ((*RayJob)(tc.job)).PriorityClass()
+			if diff := cmp.Diff(tc.wantClassName, got); diff != "" {
+				t.Errorf("PriorityClass() mismatch (-want +got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestNodeSelectors(t *testing.T) {
+
+	job := (*RayJob)(testingrayutil.MakeJob("job", "ns").
+		WithHeadGroupSpec(rayjobapi.HeadGroupSpec{
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					NodeSelector: map[string]string{},
+				},
+			},
+		}).
+		WithWorkerGroups(rayjobapi.WorkerGroupSpec{
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					NodeSelector: map[string]string{
+						"key-wg1": "value-wg1",
+					},
+				},
+			},
+		}, rayjobapi.WorkerGroupSpec{
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					NodeSelector: map[string]string{
+						"key-wg2": "value-wg2",
+					},
+				},
+			},
+		}).
+		Obj())
+
+	// run with Affinity should append or update the node selectors
+	job.RunWithNodeAffinity([]jobframework.PodSetNodeSelector{
+		{
+			NodeSelector: map[string]string{
+				"newKey": "newValue",
+			},
+		},
+		{
+			NodeSelector: map[string]string{
+				"key-wg1": "updated-value-wg1",
+			},
+		},
+		{
+			NodeSelector: map[string]string{
+				// don't add anything
+			},
+		},
+	})
+
+	if diff := cmp.Diff(
```

  The test is focusing on the node selectors only.

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:83** by [@trasc](https://github.com/trasc) — 2023-05-22T07:17:59Z
```diff
@@ -0,0 +1,131 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+type RayJobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for rayjobapi RayJob.
+func SetupRayJobWebhook(mgr ctrl.Manager, opts ...jobframework.Option) error {
+	options := jobframework.DefaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &RayJobWebhook{
+		manageJobsWithoutQueueName: options.ManageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&rayjobapi.RayJob{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-ray-io-v1alpha1-rayjob,mutating=true,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=create,versions=v1alpha1,name=mrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &RayJobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *RayJobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("rayjob-webhook")
+	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
+	jobframework.ApplyDefaultForSuspend((*RayJob)(job), w.manageJobsWithoutQueueName)
+	return nil
+}
+
+// +kubebuilder:webhook:path=/validate-ray-io-v1alpha1-rayjob,mutating=false,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=update,versions=v1alpha1,name=vrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomValidator = &RayJobWebhook{}
+
+// ValidateCreate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *RayJobWebhook) ValidateCreate(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("rayjob-webhook")
+	log.Info("Validating create", "job", klog.KObj(job))
+	return w.validateCreate(job).ToAggregate()
+}
+
+func (w *RayJobWebhook) validateCreate(job *rayjobapi.RayJob) field.ErrorList {
+	var allErrors field.ErrorList
+	kueueJob := (*RayJob)(job)
+
+	if w.manageJobsWithoutQueueName || jobframework.QueueName(kueueJob) != "" {
```

  #782 extended to cove this as well.

- **pkg/util/pointer/pointer.go:32** by [@trasc](https://github.com/trasc) — 2023-05-22T07:33:49Z
```diff
@@ -26,8 +26,9 @@ func Quantity(q resource.Quantity) *resource.Quantity {
 }
 
 var (
-	Int32  = pointer.Int32
-	Int64  = pointer.Int64
-	Bool   = pointer.Bool
-	String = pointer.String
+	Int32     = pointer.Int32
+	Int64     = pointer.Int64
+	Bool      = pointer.Bool
+	BoolDeref = pointer.BoolDeref
```

  If we can add a generic implementation in  k8s.io/utils/pointer like
  
  ```go
  func Pointer[T any](t T) *T {
     return &t
  }
  ``` 
  and maybe the defer, we can drop this package since we are only missing `Quantity`.
  
  Anyway removed.

- **pkg/util/testingjobs/rayjob/wrappers.go:30** by [@trasc](https://github.com/trasc) — 2023-05-22T07:36:58Z
```diff
@@ -0,0 +1,152 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+// JobWrapper wraps a RayJob.
+type JobWrapper struct{ rayjobapi.RayJob }
```

  The package already has `ray` it's redundant,

- **test/integration/controller/rayjob/suite_test.go:49** by [@trasc](https://github.com/trasc) — 2023-05-22T08:02:24Z
```diff
@@ -0,0 +1,99 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"path/filepath"
+	"testing"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	"k8s.io/client-go/rest"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/rayjob"
+	"sigs.k8s.io/kueue/pkg/queue"
+	"sigs.k8s.io/kueue/pkg/scheduler"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	//+kubebuilder:scaffold:imports
+)
+
+var (
+	cfg        *rest.Config
+	k8sClient  client.Client
+	ctx        context.Context
+	fwk        *framework.Framework
+	crdPath    = filepath.Join("..", "..", "..", "..", "config", "components", "crd", "bases")
+	mpiCrdPath = filepath.Join("..", "..", "..", "..", "dep-crds", "ray-operator")
```

  done

- **test/integration/controller/rayjob/rayjob_controller_test.go:49** by [@trasc](https://github.com/trasc) — 2023-05-22T08:09:10Z
```diff
@@ -0,0 +1,564 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"fmt"
+
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/pointer"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	workloadrayjob "sigs.k8s.io/kueue/pkg/controller/jobs/rayjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	testingrayjob "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	jobName           = "test-job"
+	jobNamespace      = "default"
+	labelKey          = "cloud.provider.com/instance"
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
```

  done

- **test/integration/controller/rayjob/rayjob_controller_test.go:77** by [@trasc](https://github.com/trasc) — 2023-05-22T08:34:49Z
```diff
@@ -0,0 +1,564 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"fmt"
+
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/pointer"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	workloadrayjob "sigs.k8s.io/kueue/pkg/controller/jobs/rayjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	testingrayjob "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	jobName           = "test-job"
+	jobNamespace      = "default"
+	labelKey          = "cloud.provider.com/instance"
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+)
+
+var (
+	wlLookupKey               = types.NamespacedName{Name: workloadrayjob.GetWorkloadNameForRayJob(jobName), Namespace: jobNamespace}
+	ignoreConditionTimestamps = cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+func setInitStatus(name, namespace string) {
+	createdJob := &rayjobapi.RayJob{}
+	nsName := types.NamespacedName{Name: name, Namespace: namespace}
+	gomega.EventuallyWithOffset(1, func() error {
+		if err := k8sClient.Get(ctx, nsName, createdJob); err != nil {
+			return err
+		}
+		createdJob.Status.JobDeploymentStatus = rayjobapi.JobDeploymentStatusSuspended
+		return k8sClient.Status().Update(ctx, createdJob)
+	}, util.Timeout, util.Interval).Should(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Job controller", func() {
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(jobframework.WithManageJobsWithoutQueueName(true)),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
```

  done

- **test/integration/controller/rayjob/rayjob_controller_test.go:177** by [@trasc](https://github.com/trasc) — 2023-05-22T08:35:00Z
```diff
@@ -0,0 +1,564 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"fmt"
+
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/pointer"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	workloadrayjob "sigs.k8s.io/kueue/pkg/controller/jobs/rayjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	testingrayjob "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	jobName           = "test-job"
+	jobNamespace      = "default"
+	labelKey          = "cloud.provider.com/instance"
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+)
+
+var (
+	wlLookupKey               = types.NamespacedName{Name: workloadrayjob.GetWorkloadNameForRayJob(jobName), Namespace: jobNamespace}
+	ignoreConditionTimestamps = cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+func setInitStatus(name, namespace string) {
+	createdJob := &rayjobapi.RayJob{}
+	nsName := types.NamespacedName{Name: name, Namespace: namespace}
+	gomega.EventuallyWithOffset(1, func() error {
+		if err := k8sClient.Get(ctx, nsName, createdJob); err != nil {
+			return err
+		}
+		createdJob.Status.JobDeploymentStatus = rayjobapi.JobDeploymentStatusSuspended
+		return k8sClient.Status().Update(ctx, createdJob)
+	}, util.Timeout, util.Interval).Should(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Job controller", func() {
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(jobframework.WithManageJobsWithoutQueueName(true)),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
+		}
+
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.It("Should reconcile RayJobs", func() {
+		ginkgo.By("checking the job gets suspended when created unsuspended")
+		priorityClass := testing.MakePriorityClass(priorityClassName).
+			PriorityValue(int32(priorityValue)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, priorityClass)).Should(gomega.Succeed())
+
+		job := testingrayjob.MakeJob(jobName, jobNamespace).
+			Suspend(false).
+			WithPriorityClassName(priorityClassName).
+			Obj()
+		err := k8sClient.Create(ctx, job)
+		gomega.Expect(err).To(gomega.Succeed())
+		createdJob := &rayjobapi.RayJob{}
+
+		setInitStatus(jobName, jobNamespace)
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, types.NamespacedName{Name: jobName, Namespace: jobNamespace}, createdJob); err != nil {
+				return false
+			}
+			return createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is created without queue assigned")
+		createdWorkload := &kueue.Workload{}
+		gomega.Eventually(func() error {
+			return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
+		gomega.Expect(metav1.IsControlledBy(createdWorkload, createdJob)).To(gomega.BeTrue(), "The Workload should be owned by the Job")
+
+		ginkgo.By("checking the workload is created with priority and priorityName")
+		gomega.Expect(createdWorkload.Spec.PriorityClassName).Should(gomega.Equal(priorityClassName))
+		gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
+
+		ginkgo.By("checking the workload is updated with queue name when the job does")
+		jobQueueName := "test-queue"
+		createdJob.Annotations = map[string]string{jobframework.QueueAnnotation: jobQueueName}
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return createdWorkload.Spec.QueueName == jobQueueName
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking a second non-matching workload is deleted")
+		secondWl := &kueue.Workload{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      workloadrayjob.GetWorkloadNameForRayJob("second-workload"),
+				Namespace: createdWorkload.Namespace,
+			},
+			Spec: *createdWorkload.Spec.DeepCopy(),
+		}
+		gomega.Expect(ctrl.SetControllerReference(createdJob, secondWl, scheme.Scheme)).Should(gomega.Succeed())
+		secondWl.Spec.PodSets[0].Count += 1
+
+		gomega.Expect(k8sClient.Create(ctx, secondWl)).Should(gomega.Succeed())
+		gomega.Eventually(func() error {
+			wl := &kueue.Workload{}
+			key := types.NamespacedName{Name: secondWl.Name, Namespace: secondWl.Namespace}
+			return k8sClient.Get(ctx, key, wl)
+		}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+		// check the original wl is still there
+		gomega.Consistently(func() bool {
+			err := k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+			return err == nil
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the job is unsuspended when workload is assigned")
+		onDemandFlavor := testing.MakeResourceFlavor("on-demand").Label(labelKey, "on-demand").Obj()
+		gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).Should(gomega.Succeed())
+		spotFlavor := testing.MakeResourceFlavor("spot").Label(labelKey, "spot").Obj()
+		gomega.Expect(k8sClient.Create(ctx, spotFlavor)).Should(gomega.Succeed())
+		clusterQueue := testing.MakeClusterQueue("cluster-queue").
+			ResourceGroup(
+				*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "5").Obj(),
+				*testing.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
+			).Obj()
+		admission := &kueue.Admission{
+			ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+			PodSetAssignments: []kueue.PodSetAssignment{{
+				Name: "Launcher",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "on-demand",
+				},
+			}, {
+				Name: "Worker",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "spot",
+				},
+			}},
+		}
```

  sure

- **test/integration/controller/rayjob/rayjob_controller_test.go:240** by [@trasc](https://github.com/trasc) — 2023-05-22T08:35:10Z
```diff
@@ -0,0 +1,564 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"fmt"
+
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/pointer"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	workloadrayjob "sigs.k8s.io/kueue/pkg/controller/jobs/rayjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	testingrayjob "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	jobName           = "test-job"
+	jobNamespace      = "default"
+	labelKey          = "cloud.provider.com/instance"
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+)
+
+var (
+	wlLookupKey               = types.NamespacedName{Name: workloadrayjob.GetWorkloadNameForRayJob(jobName), Namespace: jobNamespace}
+	ignoreConditionTimestamps = cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+func setInitStatus(name, namespace string) {
+	createdJob := &rayjobapi.RayJob{}
+	nsName := types.NamespacedName{Name: name, Namespace: namespace}
+	gomega.EventuallyWithOffset(1, func() error {
+		if err := k8sClient.Get(ctx, nsName, createdJob); err != nil {
+			return err
+		}
+		createdJob.Status.JobDeploymentStatus = rayjobapi.JobDeploymentStatusSuspended
+		return k8sClient.Status().Update(ctx, createdJob)
+	}, util.Timeout, util.Interval).Should(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Job controller", func() {
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(jobframework.WithManageJobsWithoutQueueName(true)),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
+		}
+
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.It("Should reconcile RayJobs", func() {
+		ginkgo.By("checking the job gets suspended when created unsuspended")
+		priorityClass := testing.MakePriorityClass(priorityClassName).
+			PriorityValue(int32(priorityValue)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, priorityClass)).Should(gomega.Succeed())
+
+		job := testingrayjob.MakeJob(jobName, jobNamespace).
+			Suspend(false).
+			WithPriorityClassName(priorityClassName).
+			Obj()
+		err := k8sClient.Create(ctx, job)
+		gomega.Expect(err).To(gomega.Succeed())
+		createdJob := &rayjobapi.RayJob{}
+
+		setInitStatus(jobName, jobNamespace)
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, types.NamespacedName{Name: jobName, Namespace: jobNamespace}, createdJob); err != nil {
+				return false
+			}
+			return createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is created without queue assigned")
+		createdWorkload := &kueue.Workload{}
+		gomega.Eventually(func() error {
+			return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
+		gomega.Expect(metav1.IsControlledBy(createdWorkload, createdJob)).To(gomega.BeTrue(), "The Workload should be owned by the Job")
+
+		ginkgo.By("checking the workload is created with priority and priorityName")
+		gomega.Expect(createdWorkload.Spec.PriorityClassName).Should(gomega.Equal(priorityClassName))
+		gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
+
+		ginkgo.By("checking the workload is updated with queue name when the job does")
+		jobQueueName := "test-queue"
+		createdJob.Annotations = map[string]string{jobframework.QueueAnnotation: jobQueueName}
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return createdWorkload.Spec.QueueName == jobQueueName
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking a second non-matching workload is deleted")
+		secondWl := &kueue.Workload{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      workloadrayjob.GetWorkloadNameForRayJob("second-workload"),
+				Namespace: createdWorkload.Namespace,
+			},
+			Spec: *createdWorkload.Spec.DeepCopy(),
+		}
+		gomega.Expect(ctrl.SetControllerReference(createdJob, secondWl, scheme.Scheme)).Should(gomega.Succeed())
+		secondWl.Spec.PodSets[0].Count += 1
+
+		gomega.Expect(k8sClient.Create(ctx, secondWl)).Should(gomega.Succeed())
+		gomega.Eventually(func() error {
+			wl := &kueue.Workload{}
+			key := types.NamespacedName{Name: secondWl.Name, Namespace: secondWl.Namespace}
+			return k8sClient.Get(ctx, key, wl)
+		}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+		// check the original wl is still there
+		gomega.Consistently(func() bool {
+			err := k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+			return err == nil
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the job is unsuspended when workload is assigned")
+		onDemandFlavor := testing.MakeResourceFlavor("on-demand").Label(labelKey, "on-demand").Obj()
+		gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).Should(gomega.Succeed())
+		spotFlavor := testing.MakeResourceFlavor("spot").Label(labelKey, "spot").Obj()
+		gomega.Expect(k8sClient.Create(ctx, spotFlavor)).Should(gomega.Succeed())
+		clusterQueue := testing.MakeClusterQueue("cluster-queue").
+			ResourceGroup(
+				*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "5").Obj(),
+				*testing.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
+			).Obj()
+		admission := &kueue.Admission{
+			ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+			PodSetAssignments: []kueue.PodSetAssignment{{
+				Name: "Launcher",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "on-demand",
+				},
+			}, {
+				Name: "Worker",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "spot",
+				},
+			}},
+		}
+		gomega.Expect(util.SetAdmission(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+		lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return !createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Eventually(func() bool {
+			ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "Started", corev1.EventTypeNormal, fmt.Sprintf("Admitted by clusterQueue %v", clusterQueue.Name))
+			return ok
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(onDemandFlavor.Name))
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(spotFlavor.Name))
+		gomega.Consistently(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the job gets suspended when parallelism changes and the added node selectors are removed")
+		parallelism := pointer.Int32Deref(job.Spec.RayClusterSpec.WorkerGroupSpecs[0].Replicas, 1)
+		newParallelism := int32(parallelism + 1)
+		createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Replicas = &newParallelism
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return createdJob.Spec.Suspend && len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector) == 0
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Eventually(func() bool {
+			ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "DeletedWorkload", corev1.EventTypeNormal, fmt.Sprintf("Deleted not matching Workload: %v", wlLookupKey.String()))
+			return ok
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is updated with new count")
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return createdWorkload.Spec.PodSets[1].Count == newParallelism
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(createdWorkload.Status.Admission).Should(gomega.BeNil())
+
+		ginkgo.By("checking the job is unsuspended and selectors added when workload is assigned again")
+		admission = &kueue.Admission{
+			ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+			PodSetAssignments: []kueue.PodSetAssignment{{
+				Name: createdWorkload.Spec.PodSets[0].Name,
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "on-demand",
+				},
+			}, {
+				Name: createdWorkload.Spec.PodSets[1].Name,
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "spot",
+				},
+			}},
+		}
```

  done

- **test/integration/controller/rayjob/rayjob_controller_test.go:277** by [@trasc](https://github.com/trasc) — 2023-05-22T08:35:17Z
```diff
@@ -0,0 +1,564 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"fmt"
+
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/pointer"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	workloadrayjob "sigs.k8s.io/kueue/pkg/controller/jobs/rayjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	testingrayjob "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	jobName           = "test-job"
+	jobNamespace      = "default"
+	labelKey          = "cloud.provider.com/instance"
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+)
+
+var (
+	wlLookupKey               = types.NamespacedName{Name: workloadrayjob.GetWorkloadNameForRayJob(jobName), Namespace: jobNamespace}
+	ignoreConditionTimestamps = cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+func setInitStatus(name, namespace string) {
+	createdJob := &rayjobapi.RayJob{}
+	nsName := types.NamespacedName{Name: name, Namespace: namespace}
+	gomega.EventuallyWithOffset(1, func() error {
+		if err := k8sClient.Get(ctx, nsName, createdJob); err != nil {
+			return err
+		}
+		createdJob.Status.JobDeploymentStatus = rayjobapi.JobDeploymentStatusSuspended
+		return k8sClient.Status().Update(ctx, createdJob)
+	}, util.Timeout, util.Interval).Should(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Job controller", func() {
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(jobframework.WithManageJobsWithoutQueueName(true)),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
+		}
+
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.It("Should reconcile RayJobs", func() {
+		ginkgo.By("checking the job gets suspended when created unsuspended")
+		priorityClass := testing.MakePriorityClass(priorityClassName).
+			PriorityValue(int32(priorityValue)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, priorityClass)).Should(gomega.Succeed())
+
+		job := testingrayjob.MakeJob(jobName, jobNamespace).
+			Suspend(false).
+			WithPriorityClassName(priorityClassName).
+			Obj()
+		err := k8sClient.Create(ctx, job)
+		gomega.Expect(err).To(gomega.Succeed())
+		createdJob := &rayjobapi.RayJob{}
+
+		setInitStatus(jobName, jobNamespace)
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, types.NamespacedName{Name: jobName, Namespace: jobNamespace}, createdJob); err != nil {
+				return false
+			}
+			return createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is created without queue assigned")
+		createdWorkload := &kueue.Workload{}
+		gomega.Eventually(func() error {
+			return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
+		gomega.Expect(metav1.IsControlledBy(createdWorkload, createdJob)).To(gomega.BeTrue(), "The Workload should be owned by the Job")
+
+		ginkgo.By("checking the workload is created with priority and priorityName")
+		gomega.Expect(createdWorkload.Spec.PriorityClassName).Should(gomega.Equal(priorityClassName))
+		gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
+
+		ginkgo.By("checking the workload is updated with queue name when the job does")
+		jobQueueName := "test-queue"
+		createdJob.Annotations = map[string]string{jobframework.QueueAnnotation: jobQueueName}
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return createdWorkload.Spec.QueueName == jobQueueName
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking a second non-matching workload is deleted")
+		secondWl := &kueue.Workload{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      workloadrayjob.GetWorkloadNameForRayJob("second-workload"),
+				Namespace: createdWorkload.Namespace,
+			},
+			Spec: *createdWorkload.Spec.DeepCopy(),
+		}
+		gomega.Expect(ctrl.SetControllerReference(createdJob, secondWl, scheme.Scheme)).Should(gomega.Succeed())
+		secondWl.Spec.PodSets[0].Count += 1
+
+		gomega.Expect(k8sClient.Create(ctx, secondWl)).Should(gomega.Succeed())
+		gomega.Eventually(func() error {
+			wl := &kueue.Workload{}
+			key := types.NamespacedName{Name: secondWl.Name, Namespace: secondWl.Namespace}
+			return k8sClient.Get(ctx, key, wl)
+		}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+		// check the original wl is still there
+		gomega.Consistently(func() bool {
+			err := k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+			return err == nil
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the job is unsuspended when workload is assigned")
+		onDemandFlavor := testing.MakeResourceFlavor("on-demand").Label(labelKey, "on-demand").Obj()
+		gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).Should(gomega.Succeed())
+		spotFlavor := testing.MakeResourceFlavor("spot").Label(labelKey, "spot").Obj()
+		gomega.Expect(k8sClient.Create(ctx, spotFlavor)).Should(gomega.Succeed())
+		clusterQueue := testing.MakeClusterQueue("cluster-queue").
+			ResourceGroup(
+				*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "5").Obj(),
+				*testing.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
+			).Obj()
+		admission := &kueue.Admission{
+			ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+			PodSetAssignments: []kueue.PodSetAssignment{{
+				Name: "Launcher",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "on-demand",
+				},
+			}, {
+				Name: "Worker",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "spot",
+				},
+			}},
+		}
+		gomega.Expect(util.SetAdmission(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+		lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return !createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Eventually(func() bool {
+			ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "Started", corev1.EventTypeNormal, fmt.Sprintf("Admitted by clusterQueue %v", clusterQueue.Name))
+			return ok
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(onDemandFlavor.Name))
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(spotFlavor.Name))
+		gomega.Consistently(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the job gets suspended when parallelism changes and the added node selectors are removed")
+		parallelism := pointer.Int32Deref(job.Spec.RayClusterSpec.WorkerGroupSpecs[0].Replicas, 1)
+		newParallelism := int32(parallelism + 1)
+		createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Replicas = &newParallelism
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return createdJob.Spec.Suspend && len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector) == 0
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Eventually(func() bool {
+			ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "DeletedWorkload", corev1.EventTypeNormal, fmt.Sprintf("Deleted not matching Workload: %v", wlLookupKey.String()))
+			return ok
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is updated with new count")
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return createdWorkload.Spec.PodSets[1].Count == newParallelism
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(createdWorkload.Status.Admission).Should(gomega.BeNil())
+
+		ginkgo.By("checking the job is unsuspended and selectors added when workload is assigned again")
+		admission = &kueue.Admission{
+			ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+			PodSetAssignments: []kueue.PodSetAssignment{{
+				Name: createdWorkload.Spec.PodSets[0].Name,
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "on-demand",
+				},
+			}, {
+				Name: createdWorkload.Spec.PodSets[1].Name,
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "spot",
+				},
+			}},
+		}
+		gomega.Expect(util.SetAdmission(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return !createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(onDemandFlavor.Name))
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(spotFlavor.Name))
+		gomega.Consistently(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is finished when job is completed")
+		createdJob.Status.JobDeploymentStatus = rayjobapi.JobDeploymentStatusComplete
+		createdJob.Status.JobStatus = rayjobapi.JobStatusSucceeded
+		createdJob.Status.Message = "Job finished by test"
+
+		gomega.Expect(k8sClient.Status().Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadFinished)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	})
+})
+
+var _ = ginkgo.Describe("Job controller for workloads when only jobs with queue are managed", func() {
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
```

  done

- **test/integration/controller/rayjob/rayjob_controller_test.go:326** by [@trasc](https://github.com/trasc) — 2023-05-22T08:35:25Z
```diff
@@ -0,0 +1,564 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"fmt"
+
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/pointer"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	workloadrayjob "sigs.k8s.io/kueue/pkg/controller/jobs/rayjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	testingrayjob "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	jobName           = "test-job"
+	jobNamespace      = "default"
+	labelKey          = "cloud.provider.com/instance"
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+)
+
+var (
+	wlLookupKey               = types.NamespacedName{Name: workloadrayjob.GetWorkloadNameForRayJob(jobName), Namespace: jobNamespace}
+	ignoreConditionTimestamps = cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+func setInitStatus(name, namespace string) {
+	createdJob := &rayjobapi.RayJob{}
+	nsName := types.NamespacedName{Name: name, Namespace: namespace}
+	gomega.EventuallyWithOffset(1, func() error {
+		if err := k8sClient.Get(ctx, nsName, createdJob); err != nil {
+			return err
+		}
+		createdJob.Status.JobDeploymentStatus = rayjobapi.JobDeploymentStatusSuspended
+		return k8sClient.Status().Update(ctx, createdJob)
+	}, util.Timeout, util.Interval).Should(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Job controller", func() {
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(jobframework.WithManageJobsWithoutQueueName(true)),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
+		}
+
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.It("Should reconcile RayJobs", func() {
+		ginkgo.By("checking the job gets suspended when created unsuspended")
+		priorityClass := testing.MakePriorityClass(priorityClassName).
+			PriorityValue(int32(priorityValue)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, priorityClass)).Should(gomega.Succeed())
+
+		job := testingrayjob.MakeJob(jobName, jobNamespace).
+			Suspend(false).
+			WithPriorityClassName(priorityClassName).
+			Obj()
+		err := k8sClient.Create(ctx, job)
+		gomega.Expect(err).To(gomega.Succeed())
+		createdJob := &rayjobapi.RayJob{}
+
+		setInitStatus(jobName, jobNamespace)
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, types.NamespacedName{Name: jobName, Namespace: jobNamespace}, createdJob); err != nil {
+				return false
+			}
+			return createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is created without queue assigned")
+		createdWorkload := &kueue.Workload{}
+		gomega.Eventually(func() error {
+			return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
+		gomega.Expect(metav1.IsControlledBy(createdWorkload, createdJob)).To(gomega.BeTrue(), "The Workload should be owned by the Job")
+
+		ginkgo.By("checking the workload is created with priority and priorityName")
+		gomega.Expect(createdWorkload.Spec.PriorityClassName).Should(gomega.Equal(priorityClassName))
+		gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
+
+		ginkgo.By("checking the workload is updated with queue name when the job does")
+		jobQueueName := "test-queue"
+		createdJob.Annotations = map[string]string{jobframework.QueueAnnotation: jobQueueName}
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return createdWorkload.Spec.QueueName == jobQueueName
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking a second non-matching workload is deleted")
+		secondWl := &kueue.Workload{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      workloadrayjob.GetWorkloadNameForRayJob("second-workload"),
+				Namespace: createdWorkload.Namespace,
+			},
+			Spec: *createdWorkload.Spec.DeepCopy(),
+		}
+		gomega.Expect(ctrl.SetControllerReference(createdJob, secondWl, scheme.Scheme)).Should(gomega.Succeed())
+		secondWl.Spec.PodSets[0].Count += 1
+
+		gomega.Expect(k8sClient.Create(ctx, secondWl)).Should(gomega.Succeed())
+		gomega.Eventually(func() error {
+			wl := &kueue.Workload{}
+			key := types.NamespacedName{Name: secondWl.Name, Namespace: secondWl.Namespace}
+			return k8sClient.Get(ctx, key, wl)
+		}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+		// check the original wl is still there
+		gomega.Consistently(func() bool {
+			err := k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+			return err == nil
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the job is unsuspended when workload is assigned")
+		onDemandFlavor := testing.MakeResourceFlavor("on-demand").Label(labelKey, "on-demand").Obj()
+		gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).Should(gomega.Succeed())
+		spotFlavor := testing.MakeResourceFlavor("spot").Label(labelKey, "spot").Obj()
+		gomega.Expect(k8sClient.Create(ctx, spotFlavor)).Should(gomega.Succeed())
+		clusterQueue := testing.MakeClusterQueue("cluster-queue").
+			ResourceGroup(
+				*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "5").Obj(),
+				*testing.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
+			).Obj()
+		admission := &kueue.Admission{
+			ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+			PodSetAssignments: []kueue.PodSetAssignment{{
+				Name: "Launcher",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "on-demand",
+				},
+			}, {
+				Name: "Worker",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "spot",
+				},
+			}},
+		}
+		gomega.Expect(util.SetAdmission(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+		lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return !createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Eventually(func() bool {
+			ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "Started", corev1.EventTypeNormal, fmt.Sprintf("Admitted by clusterQueue %v", clusterQueue.Name))
+			return ok
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(onDemandFlavor.Name))
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(spotFlavor.Name))
+		gomega.Consistently(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the job gets suspended when parallelism changes and the added node selectors are removed")
+		parallelism := pointer.Int32Deref(job.Spec.RayClusterSpec.WorkerGroupSpecs[0].Replicas, 1)
+		newParallelism := int32(parallelism + 1)
+		createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Replicas = &newParallelism
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return createdJob.Spec.Suspend && len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector) == 0
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Eventually(func() bool {
+			ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "DeletedWorkload", corev1.EventTypeNormal, fmt.Sprintf("Deleted not matching Workload: %v", wlLookupKey.String()))
+			return ok
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is updated with new count")
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return createdWorkload.Spec.PodSets[1].Count == newParallelism
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(createdWorkload.Status.Admission).Should(gomega.BeNil())
+
+		ginkgo.By("checking the job is unsuspended and selectors added when workload is assigned again")
+		admission = &kueue.Admission{
+			ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+			PodSetAssignments: []kueue.PodSetAssignment{{
+				Name: createdWorkload.Spec.PodSets[0].Name,
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "on-demand",
+				},
+			}, {
+				Name: createdWorkload.Spec.PodSets[1].Name,
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "spot",
+				},
+			}},
+		}
+		gomega.Expect(util.SetAdmission(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return !createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(onDemandFlavor.Name))
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(spotFlavor.Name))
+		gomega.Consistently(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is finished when job is completed")
+		createdJob.Status.JobDeploymentStatus = rayjobapi.JobDeploymentStatusComplete
+		createdJob.Status.JobStatus = rayjobapi.JobStatusSucceeded
+		createdJob.Status.Message = "Job finished by test"
+
+		gomega.Expect(k8sClient.Status().Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadFinished)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	})
+})
+
+var _ = ginkgo.Describe("Job controller for workloads when only jobs with queue are managed", func() {
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+	ginkgo.It("Should reconcile jobs only when queue is set", func() {
+		ginkgo.By("checking the workload is not created when queue name is not set")
+		job := testingrayjob.MakeJob(jobName, jobNamespace).Obj()
+		gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+		lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+		createdJob := &rayjobapi.RayJob{}
+		setInitStatus(jobName, jobNamespace)
+		gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+		createdWorkload := &kueue.Workload{}
+		gomega.Consistently(func() bool {
+			return apierrors.IsNotFound(k8sClient.Get(ctx, wlLookupKey, createdWorkload))
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is created when queue name is set")
+		jobQueueName := "test-queue"
+		if createdJob.Labels == nil {
+			createdJob.Labels = map[string]string{jobframework.QueueAnnotation: jobQueueName}
+		} else {
+			createdJob.Labels[jobframework.QueueLabel] = jobQueueName
+		}
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() error {
+			return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+
+})
+
+var _ = ginkgo.Describe("Job controller when waitForPodsReady enabled", func() {
+	type podsReadyTestSpec struct {
+		beforeJobStatus *rayjobapi.RayJobStatus
+		beforeCondition *metav1.Condition
+		jobStatus       rayjobapi.RayJobStatus
+		suspended       bool
+		wantCondition   *metav1.Condition
+	}
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(jobframework.WithWaitForPodsReady(true)),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
```

  done

- **test/integration/controller/rayjob/rayjob_controller_test.go:372** by [@trasc](https://github.com/trasc) — 2023-05-22T08:35:34Z
```diff
@@ -0,0 +1,564 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"fmt"
+
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/pointer"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	workloadrayjob "sigs.k8s.io/kueue/pkg/controller/jobs/rayjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	testingrayjob "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	jobName           = "test-job"
+	jobNamespace      = "default"
+	labelKey          = "cloud.provider.com/instance"
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+)
+
+var (
+	wlLookupKey               = types.NamespacedName{Name: workloadrayjob.GetWorkloadNameForRayJob(jobName), Namespace: jobNamespace}
+	ignoreConditionTimestamps = cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+func setInitStatus(name, namespace string) {
+	createdJob := &rayjobapi.RayJob{}
+	nsName := types.NamespacedName{Name: name, Namespace: namespace}
+	gomega.EventuallyWithOffset(1, func() error {
+		if err := k8sClient.Get(ctx, nsName, createdJob); err != nil {
+			return err
+		}
+		createdJob.Status.JobDeploymentStatus = rayjobapi.JobDeploymentStatusSuspended
+		return k8sClient.Status().Update(ctx, createdJob)
+	}, util.Timeout, util.Interval).Should(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Job controller", func() {
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(jobframework.WithManageJobsWithoutQueueName(true)),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
+		}
+
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.It("Should reconcile RayJobs", func() {
+		ginkgo.By("checking the job gets suspended when created unsuspended")
+		priorityClass := testing.MakePriorityClass(priorityClassName).
+			PriorityValue(int32(priorityValue)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, priorityClass)).Should(gomega.Succeed())
+
+		job := testingrayjob.MakeJob(jobName, jobNamespace).
+			Suspend(false).
+			WithPriorityClassName(priorityClassName).
+			Obj()
+		err := k8sClient.Create(ctx, job)
+		gomega.Expect(err).To(gomega.Succeed())
+		createdJob := &rayjobapi.RayJob{}
+
+		setInitStatus(jobName, jobNamespace)
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, types.NamespacedName{Name: jobName, Namespace: jobNamespace}, createdJob); err != nil {
+				return false
+			}
+			return createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is created without queue assigned")
+		createdWorkload := &kueue.Workload{}
+		gomega.Eventually(func() error {
+			return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
+		gomega.Expect(metav1.IsControlledBy(createdWorkload, createdJob)).To(gomega.BeTrue(), "The Workload should be owned by the Job")
+
+		ginkgo.By("checking the workload is created with priority and priorityName")
+		gomega.Expect(createdWorkload.Spec.PriorityClassName).Should(gomega.Equal(priorityClassName))
+		gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
+
+		ginkgo.By("checking the workload is updated with queue name when the job does")
+		jobQueueName := "test-queue"
+		createdJob.Annotations = map[string]string{jobframework.QueueAnnotation: jobQueueName}
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return createdWorkload.Spec.QueueName == jobQueueName
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking a second non-matching workload is deleted")
+		secondWl := &kueue.Workload{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      workloadrayjob.GetWorkloadNameForRayJob("second-workload"),
+				Namespace: createdWorkload.Namespace,
+			},
+			Spec: *createdWorkload.Spec.DeepCopy(),
+		}
+		gomega.Expect(ctrl.SetControllerReference(createdJob, secondWl, scheme.Scheme)).Should(gomega.Succeed())
+		secondWl.Spec.PodSets[0].Count += 1
+
+		gomega.Expect(k8sClient.Create(ctx, secondWl)).Should(gomega.Succeed())
+		gomega.Eventually(func() error {
+			wl := &kueue.Workload{}
+			key := types.NamespacedName{Name: secondWl.Name, Namespace: secondWl.Namespace}
+			return k8sClient.Get(ctx, key, wl)
+		}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+		// check the original wl is still there
+		gomega.Consistently(func() bool {
+			err := k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+			return err == nil
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the job is unsuspended when workload is assigned")
+		onDemandFlavor := testing.MakeResourceFlavor("on-demand").Label(labelKey, "on-demand").Obj()
+		gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).Should(gomega.Succeed())
+		spotFlavor := testing.MakeResourceFlavor("spot").Label(labelKey, "spot").Obj()
+		gomega.Expect(k8sClient.Create(ctx, spotFlavor)).Should(gomega.Succeed())
+		clusterQueue := testing.MakeClusterQueue("cluster-queue").
+			ResourceGroup(
+				*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "5").Obj(),
+				*testing.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
+			).Obj()
+		admission := &kueue.Admission{
+			ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+			PodSetAssignments: []kueue.PodSetAssignment{{
+				Name: "Launcher",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "on-demand",
+				},
+			}, {
+				Name: "Worker",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "spot",
+				},
+			}},
+		}
+		gomega.Expect(util.SetAdmission(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+		lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return !createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Eventually(func() bool {
+			ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "Started", corev1.EventTypeNormal, fmt.Sprintf("Admitted by clusterQueue %v", clusterQueue.Name))
+			return ok
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(onDemandFlavor.Name))
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(spotFlavor.Name))
+		gomega.Consistently(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the job gets suspended when parallelism changes and the added node selectors are removed")
+		parallelism := pointer.Int32Deref(job.Spec.RayClusterSpec.WorkerGroupSpecs[0].Replicas, 1)
+		newParallelism := int32(parallelism + 1)
+		createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Replicas = &newParallelism
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return createdJob.Spec.Suspend && len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector) == 0
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Eventually(func() bool {
+			ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "DeletedWorkload", corev1.EventTypeNormal, fmt.Sprintf("Deleted not matching Workload: %v", wlLookupKey.String()))
+			return ok
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is updated with new count")
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return createdWorkload.Spec.PodSets[1].Count == newParallelism
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(createdWorkload.Status.Admission).Should(gomega.BeNil())
+
+		ginkgo.By("checking the job is unsuspended and selectors added when workload is assigned again")
+		admission = &kueue.Admission{
+			ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+			PodSetAssignments: []kueue.PodSetAssignment{{
+				Name: createdWorkload.Spec.PodSets[0].Name,
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "on-demand",
+				},
+			}, {
+				Name: createdWorkload.Spec.PodSets[1].Name,
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "spot",
+				},
+			}},
+		}
+		gomega.Expect(util.SetAdmission(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return !createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(onDemandFlavor.Name))
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(spotFlavor.Name))
+		gomega.Consistently(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is finished when job is completed")
+		createdJob.Status.JobDeploymentStatus = rayjobapi.JobDeploymentStatusComplete
+		createdJob.Status.JobStatus = rayjobapi.JobStatusSucceeded
+		createdJob.Status.Message = "Job finished by test"
+
+		gomega.Expect(k8sClient.Status().Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadFinished)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	})
+})
+
+var _ = ginkgo.Describe("Job controller for workloads when only jobs with queue are managed", func() {
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+	ginkgo.It("Should reconcile jobs only when queue is set", func() {
+		ginkgo.By("checking the workload is not created when queue name is not set")
+		job := testingrayjob.MakeJob(jobName, jobNamespace).Obj()
+		gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+		lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+		createdJob := &rayjobapi.RayJob{}
+		setInitStatus(jobName, jobNamespace)
+		gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+		createdWorkload := &kueue.Workload{}
+		gomega.Consistently(func() bool {
+			return apierrors.IsNotFound(k8sClient.Get(ctx, wlLookupKey, createdWorkload))
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is created when queue name is set")
+		jobQueueName := "test-queue"
+		if createdJob.Labels == nil {
+			createdJob.Labels = map[string]string{jobframework.QueueAnnotation: jobQueueName}
+		} else {
+			createdJob.Labels[jobframework.QueueLabel] = jobQueueName
+		}
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() error {
+			return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+
+})
+
+var _ = ginkgo.Describe("Job controller when waitForPodsReady enabled", func() {
+	type podsReadyTestSpec struct {
+		beforeJobStatus *rayjobapi.RayJobStatus
+		beforeCondition *metav1.Condition
+		jobStatus       rayjobapi.RayJobStatus
+		suspended       bool
+		wantCondition   *metav1.Condition
+	}
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(jobframework.WithWaitForPodsReady(true)),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.DescribeTable("Single job at different stages of progress towards completion",
+
+		func(podsReadyTestSpec podsReadyTestSpec) {
+			ginkgo.By("Create a resource flavor")
+			defaultFlavor := testing.MakeResourceFlavor("default").Label(labelKey, "default").Obj()
+			gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).Should(gomega.Succeed())
+
+			ginkgo.By("Create a job")
+			job := testingrayjob.MakeJob(jobName, jobNamespace).Obj()
+			jobQueueName := "test-queue"
+			job.Annotations = map[string]string{jobframework.QueueAnnotation: jobQueueName}
+			gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+			setInitStatus(jobName, jobNamespace)
+			createdJob := &rayjobapi.RayJob{}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+			ginkgo.By("Fetch the workload created for the job")
+			createdWorkload := &kueue.Workload{}
+			gomega.Eventually(func() error {
+				return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Admit the workload created for the job")
+			admission := &kueue.Admission{
+				ClusterQueue: kueue.ClusterQueueReference("foo"),
+				PodSetAssignments: []kueue.PodSetAssignment{{
+					Name: "Launcher",
+					Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+						corev1.ResourceCPU: "default",
+					},
+				}, {
+					Name: "Worker",
+					Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+						corev1.ResourceCPU: "default",
+					},
+				}},
+			}
```

  done

- **test/integration/controller/rayjob/rayjob_controller_test.go:506** by [@trasc](https://github.com/trasc) — 2023-05-22T08:38:31Z
```diff
@@ -0,0 +1,564 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"fmt"
+
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/pointer"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	workloadrayjob "sigs.k8s.io/kueue/pkg/controller/jobs/rayjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	testingrayjob "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	jobName           = "test-job"
+	jobNamespace      = "default"
+	labelKey          = "cloud.provider.com/instance"
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+)
+
+var (
+	wlLookupKey               = types.NamespacedName{Name: workloadrayjob.GetWorkloadNameForRayJob(jobName), Namespace: jobNamespace}
+	ignoreConditionTimestamps = cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+func setInitStatus(name, namespace string) {
+	createdJob := &rayjobapi.RayJob{}
+	nsName := types.NamespacedName{Name: name, Namespace: namespace}
+	gomega.EventuallyWithOffset(1, func() error {
+		if err := k8sClient.Get(ctx, nsName, createdJob); err != nil {
+			return err
+		}
+		createdJob.Status.JobDeploymentStatus = rayjobapi.JobDeploymentStatusSuspended
+		return k8sClient.Status().Update(ctx, createdJob)
+	}, util.Timeout, util.Interval).Should(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Job controller", func() {
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(jobframework.WithManageJobsWithoutQueueName(true)),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
+		}
+
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.It("Should reconcile RayJobs", func() {
+		ginkgo.By("checking the job gets suspended when created unsuspended")
+		priorityClass := testing.MakePriorityClass(priorityClassName).
+			PriorityValue(int32(priorityValue)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, priorityClass)).Should(gomega.Succeed())
+
+		job := testingrayjob.MakeJob(jobName, jobNamespace).
+			Suspend(false).
+			WithPriorityClassName(priorityClassName).
+			Obj()
+		err := k8sClient.Create(ctx, job)
+		gomega.Expect(err).To(gomega.Succeed())
+		createdJob := &rayjobapi.RayJob{}
+
+		setInitStatus(jobName, jobNamespace)
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, types.NamespacedName{Name: jobName, Namespace: jobNamespace}, createdJob); err != nil {
+				return false
+			}
+			return createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is created without queue assigned")
+		createdWorkload := &kueue.Workload{}
+		gomega.Eventually(func() error {
+			return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
+		gomega.Expect(metav1.IsControlledBy(createdWorkload, createdJob)).To(gomega.BeTrue(), "The Workload should be owned by the Job")
+
+		ginkgo.By("checking the workload is created with priority and priorityName")
+		gomega.Expect(createdWorkload.Spec.PriorityClassName).Should(gomega.Equal(priorityClassName))
+		gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
+
+		ginkgo.By("checking the workload is updated with queue name when the job does")
+		jobQueueName := "test-queue"
+		createdJob.Annotations = map[string]string{jobframework.QueueAnnotation: jobQueueName}
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return createdWorkload.Spec.QueueName == jobQueueName
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking a second non-matching workload is deleted")
+		secondWl := &kueue.Workload{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      workloadrayjob.GetWorkloadNameForRayJob("second-workload"),
+				Namespace: createdWorkload.Namespace,
+			},
+			Spec: *createdWorkload.Spec.DeepCopy(),
+		}
+		gomega.Expect(ctrl.SetControllerReference(createdJob, secondWl, scheme.Scheme)).Should(gomega.Succeed())
+		secondWl.Spec.PodSets[0].Count += 1
+
+		gomega.Expect(k8sClient.Create(ctx, secondWl)).Should(gomega.Succeed())
+		gomega.Eventually(func() error {
+			wl := &kueue.Workload{}
+			key := types.NamespacedName{Name: secondWl.Name, Namespace: secondWl.Namespace}
+			return k8sClient.Get(ctx, key, wl)
+		}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+		// check the original wl is still there
+		gomega.Consistently(func() bool {
+			err := k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+			return err == nil
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the job is unsuspended when workload is assigned")
+		onDemandFlavor := testing.MakeResourceFlavor("on-demand").Label(labelKey, "on-demand").Obj()
+		gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).Should(gomega.Succeed())
+		spotFlavor := testing.MakeResourceFlavor("spot").Label(labelKey, "spot").Obj()
+		gomega.Expect(k8sClient.Create(ctx, spotFlavor)).Should(gomega.Succeed())
+		clusterQueue := testing.MakeClusterQueue("cluster-queue").
+			ResourceGroup(
+				*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "5").Obj(),
+				*testing.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
+			).Obj()
+		admission := &kueue.Admission{
+			ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+			PodSetAssignments: []kueue.PodSetAssignment{{
+				Name: "Launcher",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "on-demand",
+				},
+			}, {
+				Name: "Worker",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "spot",
+				},
+			}},
+		}
+		gomega.Expect(util.SetAdmission(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+		lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return !createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Eventually(func() bool {
+			ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "Started", corev1.EventTypeNormal, fmt.Sprintf("Admitted by clusterQueue %v", clusterQueue.Name))
+			return ok
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(onDemandFlavor.Name))
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(spotFlavor.Name))
+		gomega.Consistently(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the job gets suspended when parallelism changes and the added node selectors are removed")
+		parallelism := pointer.Int32Deref(job.Spec.RayClusterSpec.WorkerGroupSpecs[0].Replicas, 1)
+		newParallelism := int32(parallelism + 1)
+		createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Replicas = &newParallelism
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return createdJob.Spec.Suspend && len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector) == 0
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Eventually(func() bool {
+			ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "DeletedWorkload", corev1.EventTypeNormal, fmt.Sprintf("Deleted not matching Workload: %v", wlLookupKey.String()))
+			return ok
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is updated with new count")
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return createdWorkload.Spec.PodSets[1].Count == newParallelism
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(createdWorkload.Status.Admission).Should(gomega.BeNil())
+
+		ginkgo.By("checking the job is unsuspended and selectors added when workload is assigned again")
+		admission = &kueue.Admission{
+			ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+			PodSetAssignments: []kueue.PodSetAssignment{{
+				Name: createdWorkload.Spec.PodSets[0].Name,
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "on-demand",
+				},
+			}, {
+				Name: createdWorkload.Spec.PodSets[1].Name,
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "spot",
+				},
+			}},
+		}
+		gomega.Expect(util.SetAdmission(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.Get(ctx, lookupKey, createdJob); err != nil {
+				return false
+			}
+			return !createdJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(onDemandFlavor.Name))
+		gomega.Expect(len(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.Spec.RayClusterSpec.WorkerGroupSpecs[0].Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(spotFlavor.Name))
+		gomega.Consistently(func() bool {
+			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+				return false
+			}
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is finished when job is completed")
+		createdJob.Status.JobDeploymentStatus = rayjobapi.JobDeploymentStatusComplete
+		createdJob.Status.JobStatus = rayjobapi.JobStatusSucceeded
+		createdJob.Status.Message = "Job finished by test"
+
+		gomega.Expect(k8sClient.Status().Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadFinished)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	})
+})
+
+var _ = ginkgo.Describe("Job controller for workloads when only jobs with queue are managed", func() {
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+	ginkgo.It("Should reconcile jobs only when queue is set", func() {
+		ginkgo.By("checking the workload is not created when queue name is not set")
+		job := testingrayjob.MakeJob(jobName, jobNamespace).Obj()
+		gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+		lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+		createdJob := &rayjobapi.RayJob{}
+		setInitStatus(jobName, jobNamespace)
+		gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+		createdWorkload := &kueue.Workload{}
+		gomega.Consistently(func() bool {
+			return apierrors.IsNotFound(k8sClient.Get(ctx, wlLookupKey, createdWorkload))
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("checking the workload is created when queue name is set")
+		jobQueueName := "test-queue"
+		if createdJob.Labels == nil {
+			createdJob.Labels = map[string]string{jobframework.QueueAnnotation: jobQueueName}
+		} else {
+			createdJob.Labels[jobframework.QueueLabel] = jobQueueName
+		}
+		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+		gomega.Eventually(func() error {
+			return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+
+})
+
+var _ = ginkgo.Describe("Job controller when waitForPodsReady enabled", func() {
+	type podsReadyTestSpec struct {
+		beforeJobStatus *rayjobapi.RayJobStatus
+		beforeCondition *metav1.Condition
+		jobStatus       rayjobapi.RayJobStatus
+		suspended       bool
+		wantCondition   *metav1.Condition
+	}
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(jobframework.WithWaitForPodsReady(true)),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.DescribeTable("Single job at different stages of progress towards completion",
+
+		func(podsReadyTestSpec podsReadyTestSpec) {
+			ginkgo.By("Create a resource flavor")
+			defaultFlavor := testing.MakeResourceFlavor("default").Label(labelKey, "default").Obj()
+			gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).Should(gomega.Succeed())
+
+			ginkgo.By("Create a job")
+			job := testingrayjob.MakeJob(jobName, jobNamespace).Obj()
+			jobQueueName := "test-queue"
+			job.Annotations = map[string]string{jobframework.QueueAnnotation: jobQueueName}
+			gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+			setInitStatus(jobName, jobNamespace)
+			createdJob := &rayjobapi.RayJob{}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+			ginkgo.By("Fetch the workload created for the job")
+			createdWorkload := &kueue.Workload{}
+			gomega.Eventually(func() error {
+				return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Admit the workload created for the job")
+			admission := &kueue.Admission{
+				ClusterQueue: kueue.ClusterQueueReference("foo"),
+				PodSetAssignments: []kueue.PodSetAssignment{{
+					Name: "Launcher",
+					Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+						corev1.ResourceCPU: "default",
+					},
+				}, {
+					Name: "Worker",
+					Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+						corev1.ResourceCPU: "default",
+					},
+				}},
+			}
+			gomega.Expect(util.SetAdmission(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
+
+			ginkgo.By("Await for the job to be unsuspended")
+			gomega.Eventually(func() bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+				return createdJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.BeFalse())
+
+			if podsReadyTestSpec.beforeJobStatus != nil {
+				ginkgo.By("Update the job status to simulate its initial progress towards completion")
+				createdJob.Status = *podsReadyTestSpec.beforeJobStatus
+				gomega.Expect(k8sClient.Status().Update(ctx, createdJob)).Should(gomega.Succeed())
+				gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+			}
+
+			if podsReadyTestSpec.beforeCondition != nil {
+				ginkgo.By("Update the workload status")
+				gomega.Eventually(func() *metav1.Condition {
+					gomega.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
+					return apimeta.FindStatusCondition(createdWorkload.Status.Conditions, kueue.WorkloadPodsReady)
+				}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(podsReadyTestSpec.beforeCondition, ignoreConditionTimestamps))
+			}
+
+			ginkgo.By("Update the job status to simulate its progress towards completion")
+			createdJob.Status = podsReadyTestSpec.jobStatus
+			gomega.Expect(k8sClient.Status().Update(ctx, createdJob)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+			if podsReadyTestSpec.suspended {
+				ginkgo.By("Unset admission of the workload to suspend the job")
+				gomega.Eventually(func() error {
+					// the update may need to be retried due to a conflict as the workload gets
+					// also updated due to setting of the job status.
+					if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+						return err
+					}
+					return util.SetAdmission(ctx, k8sClient, createdWorkload, nil)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			}
+
+			ginkgo.By("Verify the PodsReady condition is added")
+			gomega.Eventually(func() *metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
+				return apimeta.FindStatusCondition(createdWorkload.Status.Conditions, kueue.WorkloadPodsReady)
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(podsReadyTestSpec.wantCondition, ignoreConditionTimestamps))
+		},
+		ginkgo.Entry("No progress", podsReadyTestSpec{
+			wantCondition: &metav1.Condition{
+				Type:    kueue.WorkloadPodsReady,
+				Status:  metav1.ConditionFalse,
+				Reason:  "PodsReady",
+				Message: "Not all pods are ready or succeeded",
+			},
+		}),
+		ginkgo.Entry("Running RayJob", podsReadyTestSpec{
+			jobStatus: rayjobapi.RayJobStatus{
+				JobDeploymentStatus: rayjobapi.JobDeploymentStatusRunning,
+				RayClusterStatus: rayjobapi.RayClusterStatus{
+					State: rayjobapi.Ready,
+				},
+			},
+			wantCondition: &metav1.Condition{
+				Type:    kueue.WorkloadPodsReady,
+				Status:  metav1.ConditionTrue,
+				Reason:  "PodsReady",
+				Message: "All pods were ready or succeeded since the workload admission",
+			},
+		}),
+		ginkgo.Entry("Running RayJob; PodsReady=False before", podsReadyTestSpec{
+			beforeCondition: &metav1.Condition{
+				Type:    kueue.WorkloadPodsReady,
+				Status:  metav1.ConditionFalse,
+				Reason:  "PodsReady",
+				Message: "Not all pods are ready or succeeded",
+			},
+			jobStatus: rayjobapi.RayJobStatus{
+				JobDeploymentStatus: rayjobapi.JobDeploymentStatusRunning,
+				RayClusterStatus: rayjobapi.RayClusterStatus{
+					State: rayjobapi.Ready,
+				},
+			},
+			wantCondition: &metav1.Condition{
+				Type:    kueue.WorkloadPodsReady,
+				Status:  metav1.ConditionTrue,
+				Reason:  "PodsReady",
+				Message: "All pods were ready or succeeded since the workload admission",
+			},
+		}),
+		ginkgo.Entry("Job suspended; PodsReady=True before", podsReadyTestSpec{
+			beforeJobStatus: &rayjobapi.RayJobStatus{
+				JobDeploymentStatus: rayjobapi.JobDeploymentStatusRunning,
+				RayClusterStatus: rayjobapi.RayClusterStatus{
+					State: rayjobapi.Ready,
+				},
+			},
+			beforeCondition: &metav1.Condition{
+				Type:    kueue.WorkloadPodsReady,
+				Status:  metav1.ConditionTrue,
+				Reason:  "PodsReady",
+				Message: "All pods were ready or succeeded since the workload admission",
+			},
+			jobStatus: rayjobapi.RayJobStatus{
+				JobDeploymentStatus: rayjobapi.JobDeploymentStatusSuspended,
+			},
+			suspended: true,
+			wantCondition: &metav1.Condition{
+				Type:    kueue.WorkloadPodsReady,
+				Status:  metav1.ConditionFalse,
+				Reason:  "PodsReady",
+				Message: "Not all pods are ready or succeeded",
+			},
+		}),
+	)
+})
+
+var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
+	const (
+		instanceKey = "cloud.provider.com/instance"
+	)
+
+	var (
+		ns                  *corev1.Namespace
+		onDemandFlavor      *kueue.ResourceFlavor
+		spotUntaintedFlavor *kueue.ResourceFlavor
+		clusterQueue        *kueue.ClusterQueue
+		localQueue          *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerAndSchedulerSetup(),
+			CRDPath:      crdPath,
+			DepCRDPaths:  []string{mpiCrdPath},
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-22T09:06:13Z

- **pkg/util/testingjobs/rayjob/wrappers.go:30** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-22T09:06:13Z
```diff
@@ -0,0 +1,152 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+// JobWrapper wraps a RayJob.
+type JobWrapper struct{ rayjobapi.RayJob }
```

  Makes sense.
  Maybe, we need to change `MakeMPIJob` to `MakeJob` as well.
  
  https://github.com/kubernetes-sigs/kueue/blob/74f45f69612a2d36ace91ae455f1e9fdbcd53037/pkg/util/testingjobs/mpijob/wrappers_mpijob.go#L34
  
  We can work on this at follow-up PRs.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-22T09:07:06Z

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:83** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-22T09:07:06Z
```diff
@@ -0,0 +1,131 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+type RayJobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for rayjobapi RayJob.
+func SetupRayJobWebhook(mgr ctrl.Manager, opts ...jobframework.Option) error {
+	options := jobframework.DefaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &RayJobWebhook{
+		manageJobsWithoutQueueName: options.ManageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&rayjobapi.RayJob{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-ray-io-v1alpha1-rayjob,mutating=true,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=create,versions=v1alpha1,name=mrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &RayJobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *RayJobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("rayjob-webhook")
+	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
+	jobframework.ApplyDefaultForSuspend((*RayJob)(job), w.manageJobsWithoutQueueName)
+	return nil
+}
+
+// +kubebuilder:webhook:path=/validate-ray-io-v1alpha1-rayjob,mutating=false,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=update,versions=v1alpha1,name=vrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomValidator = &RayJobWebhook{}
+
+// ValidateCreate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *RayJobWebhook) ValidateCreate(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("rayjob-webhook")
+	log.Info("Validating create", "job", klog.KObj(job))
+	return w.validateCreate(job).ToAggregate()
+}
+
+func (w *RayJobWebhook) validateCreate(job *rayjobapi.RayJob) field.ErrorList {
+	var allErrors field.ErrorList
+	kueueJob := (*RayJob)(job)
+
+	if w.manageJobsWithoutQueueName || jobframework.QueueName(kueueJob) != "" {
```

  Great!

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-22T09:07:48Z

- **pkg/util/testingjobs/rayjob/wrappers.go:30** by [@trasc](https://github.com/trasc) — 2023-05-22T09:07:48Z
```diff
@@ -0,0 +1,152 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+// JobWrapper wraps a RayJob.
+type JobWrapper struct{ rayjobapi.RayJob }
```

  sure

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-05-22T09:10:15Z

- **pkg/controller/jobs/rayjob/rayjob_controller_test.go:138** by [@kerthcet](https://github.com/kerthcet) — 2023-05-22T09:10:07Z
```diff
@@ -0,0 +1,384 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	testingrayutil "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+)
+
+func TestPodSets(t *testing.T) {
+	job := testingrayutil.MakeJob("job", "ns").
+		WithHeadGroupSpec(
+			rayjobapi.HeadGroupSpec{
+				Template: corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name: "head_c",
+							},
+						},
+					},
+				},
+			},
+		).
+		WithWorkerGroups(
+			rayjobapi.WorkerGroupSpec{
+				GroupName: "group1",
+				Template: corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name: "group1_c",
+							},
+						},
+					},
+				},
+			},
+			rayjobapi.WorkerGroupSpec{
+				GroupName: "group2",
+				Replicas:  pointer.Int32(3),
+				Template: corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name: "group2_c",
+							},
+						},
+					},
+				},
+			},
+		).
+		Obj()
+
+	wantPodSets := []kueue.PodSet{
+		{
+			Name:  "head",
+			Count: 1,
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					Containers: []corev1.Container{
+						{
+							Name: "head_c",
+						},
+					},
+				},
+			},
+		},
+		{
+			Name:  "group1",
+			Count: 1,
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					Containers: []corev1.Container{
+						{
+							Name: "group1_c",
+						},
+					},
+				},
+			},
+		},
+		{
+			Name:  "group2",
+			Count: 3,
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					Containers: []corev1.Container{
+						{
+							Name: "group2_c",
+						},
+					},
+				},
+			},
+		},
+	}
+
+	result := ((*RayJob)(job)).PodSets()
+
+	if diff := cmp.Diff(wantPodSets, result); diff != "" {
+		t.Errorf("PodSets() mismatch (-want +got):\n%s", diff)
+	}
+}
+
+func TestEquivalentToWorkload(t *testing.T) {
+	testContainer := corev1.Container{
+		Name:  "ic1",
+		Image: "image1",
+		Resources: corev1.ResourceRequirements{
+			Requests: corev1.ResourceList{
+				corev1.ResourceCPU: resource.MustParse("1"),
+			},
+		},
+	}
+	job := (*RayJob)(testingrayutil.MakeJob("job", "ns").
+		RequestHead(corev1.ResourceCPU, "2").
```

  We call RequestHead() to set the request for head then we call `WithHeadGroupSpec` to replace the head's pod spec?

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-05-22T09:13:11Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:236** by [@kerthcet](https://github.com/kerthcet) — 2023-05-22T09:13:10Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) == 0 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if !equality.Semantic.DeepEqual(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector) {
+		headPodSpec.NodeSelector = cloneSelectors(nodeSelectors[0].NodeSelector)
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector) {
+			workerPodSpec.NodeSelector = cloneSelectors(nodeSelectors[index+1].NodeSelector)
+		}
+	}
+}
+
+func (j *RayJob) Finished() (metav1.Condition, bool) {
+	condition := metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  string(j.Status.JobStatus),
+		Message: j.Status.Message,
+	}
+	finished := false
+	switch j.Status.JobDeploymentStatus {
+	case rayjobapi.JobDeploymentStatusComplete, rayjobapi.JobDeploymentStatusFailedJobDeploy, rayjobapi.JobDeploymentStatusFailedToGetOrCreateRayCluster:
+		finished = true
+
+	default:
+		finished = false
+	}
+	return condition, finished
+}
+
+func (j *RayJob) EquivalentToWorkload(wl kueue.Workload) bool {
+	if len(wl.Spec.PodSets) != (len(j.Spec.RayClusterSpec.WorkerGroupSpecs) + 1) {
+		return false
+	}
+
+	podSets := wl.Spec.PodSets
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if podSets[0].Count != 1 {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.InitContainers, podSets[0].Template.Spec.InitContainers) {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.Containers, podSets[0].Template.Spec.Containers) {
+		return false
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		if podSets[index+1].Count != *j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Replicas {
+			return false
+		}
+
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.InitContainers, podSets[index+1].Template.Spec.InitContainers) {
+			return false
+		}
+		if !equality.Semantic.DeepEqual(workerPodSpec.Containers, podSets[index+1].Template.Spec.Containers) {
+			return false
+		}
+	}
+	return true
+}
+
+func (j *RayJob) PriorityClass() string {
+	if j.Spec.RayClusterSpec != nil {
+		rcs := j.Spec.RayClusterSpec
+		if len(rcs.HeadGroupSpec.Template.Spec.PriorityClassName) != 0 {
+			return rcs.HeadGroupSpec.Template.Spec.PriorityClassName
+		}
+		for wi := range rcs.WorkerGroupSpecs {
```

  Seconding this?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-22T09:54:48Z

- **pkg/controller/jobs/rayjob/rayjob_controller_test.go:138** by [@trasc](https://github.com/trasc) — 2023-05-22T09:54:48Z
```diff
@@ -0,0 +1,384 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	testingrayutil "sigs.k8s.io/kueue/pkg/util/testingjobs/rayjob"
+)
+
+func TestPodSets(t *testing.T) {
+	job := testingrayutil.MakeJob("job", "ns").
+		WithHeadGroupSpec(
+			rayjobapi.HeadGroupSpec{
+				Template: corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name: "head_c",
+							},
+						},
+					},
+				},
+			},
+		).
+		WithWorkerGroups(
+			rayjobapi.WorkerGroupSpec{
+				GroupName: "group1",
+				Template: corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name: "group1_c",
+							},
+						},
+					},
+				},
+			},
+			rayjobapi.WorkerGroupSpec{
+				GroupName: "group2",
+				Replicas:  pointer.Int32(3),
+				Template: corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name: "group2_c",
+							},
+						},
+					},
+				},
+			},
+		).
+		Obj()
+
+	wantPodSets := []kueue.PodSet{
+		{
+			Name:  "head",
+			Count: 1,
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					Containers: []corev1.Container{
+						{
+							Name: "head_c",
+						},
+					},
+				},
+			},
+		},
+		{
+			Name:  "group1",
+			Count: 1,
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					Containers: []corev1.Container{
+						{
+							Name: "group1_c",
+						},
+					},
+				},
+			},
+		},
+		{
+			Name:  "group2",
+			Count: 3,
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					Containers: []corev1.Container{
+						{
+							Name: "group2_c",
+						},
+					},
+				},
+			},
+		},
+	}
+
+	result := ((*RayJob)(job)).PodSets()
+
+	if diff := cmp.Diff(wantPodSets, result); diff != "" {
+		t.Errorf("PodSets() mismatch (-want +got):\n%s", diff)
+	}
+}
+
+func TestEquivalentToWorkload(t *testing.T) {
+	testContainer := corev1.Container{
+		Name:  "ic1",
+		Image: "image1",
+		Resources: corev1.ResourceRequirements{
+			Requests: corev1.ResourceList{
+				corev1.ResourceCPU: resource.MustParse("1"),
+			},
+		},
+	}
+	job := (*RayJob)(testingrayutil.MakeJob("job", "ns").
+		RequestHead(corev1.ResourceCPU, "2").
```

  sure, thanks

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-05-22T15:18:05Z

One argue here https://github.com/kubernetes-sigs/kueue/pull/667/files#r1197996509, other parts generally LGTM. 
/assign @alculquicondor

- **pkg/controller/jobs/rayjob/rayjob_controller.go:166** by [@kerthcet](https://github.com/kerthcet) — 2023-05-22T15:05:33Z
```diff
@@ -0,0 +1,278 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a RayJob object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+var _ jobframework.GenericJob = (*RayJob)(nil)
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	if len(nodeSelectors) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
```

  If they're not match, we just return? maybe we should add an error to the interface. Same to `RunWithNodeAffinity`.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-22T17:51:10Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:166** by [@trasc](https://github.com/trasc) — 2023-05-22T17:51:10Z
```diff
@@ -0,0 +1,278 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a RayJob object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+var _ jobframework.GenericJob = (*RayJob)(nil)
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	if len(nodeSelectors) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
```

  Indeed it's not ideal, however the interface change is out of the scope of this PR, also it's very little that we can do in case of this error ....

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-22T18:01:46Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:236** by [@trasc](https://github.com/trasc) — 2023-05-22T18:01:45Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a Job object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) == 0 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if !equality.Semantic.DeepEqual(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector) {
+		headPodSpec.NodeSelector = cloneSelectors(nodeSelectors[0].NodeSelector)
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector) {
+			workerPodSpec.NodeSelector = cloneSelectors(nodeSelectors[index+1].NodeSelector)
+		}
+	}
+}
+
+func (j *RayJob) Finished() (metav1.Condition, bool) {
+	condition := metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  string(j.Status.JobStatus),
+		Message: j.Status.Message,
+	}
+	finished := false
+	switch j.Status.JobDeploymentStatus {
+	case rayjobapi.JobDeploymentStatusComplete, rayjobapi.JobDeploymentStatusFailedJobDeploy, rayjobapi.JobDeploymentStatusFailedToGetOrCreateRayCluster:
+		finished = true
+
+	default:
+		finished = false
+	}
+	return condition, finished
+}
+
+func (j *RayJob) EquivalentToWorkload(wl kueue.Workload) bool {
+	if len(wl.Spec.PodSets) != (len(j.Spec.RayClusterSpec.WorkerGroupSpecs) + 1) {
+		return false
+	}
+
+	podSets := wl.Spec.PodSets
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if podSets[0].Count != 1 {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.InitContainers, podSets[0].Template.Spec.InitContainers) {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.Containers, podSets[0].Template.Spec.Containers) {
+		return false
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		if podSets[index+1].Count != *j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Replicas {
+			return false
+		}
+
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.InitContainers, podSets[index+1].Template.Spec.InitContainers) {
+			return false
+		}
+		if !equality.Semantic.DeepEqual(workerPodSpec.Containers, podSets[index+1].Template.Spec.Containers) {
+			return false
+		}
+	}
+	return true
+}
+
+func (j *RayJob) PriorityClass() string {
+	if j.Spec.RayClusterSpec != nil {
+		rcs := j.Spec.RayClusterSpec
+		if len(rcs.HeadGroupSpec.Template.Spec.PriorityClassName) != 0 {
+			return rcs.HeadGroupSpec.Template.Spec.PriorityClassName
+		}
+		for wi := range rcs.WorkerGroupSpecs {
```

  `ray.io/priority-class-name` is very specific to the volcano integration of the RayCluster which will just force it in the pods spec.  
  
  I don't see the benefit of also checking that especially since it's not populated for RayJobs. On the other hand if  ray-project/kuberay#1100 adds a dedicated field in the spec we should definitely use it.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-23T10:45:30Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:265** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-23T10:45:29Z
```diff
@@ -0,0 +1,278 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a RayJob object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+var _ jobframework.GenericJob = (*RayJob)(nil)
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	if len(nodeSelectors) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if !equality.Semantic.DeepEqual(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector) {
+		headPodSpec.NodeSelector = cloneSelectors(nodeSelectors[0].NodeSelector)
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector) {
+			workerPodSpec.NodeSelector = cloneSelectors(nodeSelectors[index+1].NodeSelector)
+		}
+	}
+}
+
+func (j *RayJob) Finished() (metav1.Condition, bool) {
+	condition := metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  string(j.Status.JobStatus),
+		Message: j.Status.Message,
+	}
+	return condition, rayjobapi.IsJobTerminal(j.Status.JobStatus)
+}
+
+func (j *RayJob) EquivalentToWorkload(wl kueue.Workload) bool {
+	if len(wl.Spec.PodSets) != (len(j.Spec.RayClusterSpec.WorkerGroupSpecs) + 1) {
+		return false
+	}
+
+	podSets := wl.Spec.PodSets
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if podSets[0].Count != 1 {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.InitContainers, podSets[0].Template.Spec.InitContainers) {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.Containers, podSets[0].Template.Spec.Containers) {
+		return false
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		// MinReplicas and MaxReplicas should be taken into account for partial admission.
+		if podSets[index+1].Count != *j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Replicas {
+			return false
+		}
+
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.InitContainers, podSets[index+1].Template.Spec.InitContainers) {
+			return false
+		}
+		if !equality.Semantic.DeepEqual(workerPodSpec.Containers, podSets[index+1].Template.Spec.Containers) {
+			return false
+		}
+	}
+	return true
+}
+
+func (j *RayJob) PriorityClass() string {
+	if j.Spec.RayClusterSpec != nil {
+		rcs := j.Spec.RayClusterSpec
+		if len(rcs.HeadGroupSpec.Template.Spec.PriorityClassName) != 0 {
+			return rcs.HeadGroupSpec.Template.Spec.PriorityClassName
+		}
+		for wi := range rcs.WorkerGroupSpecs {
+			w := &rcs.WorkerGroupSpecs[wi]
+			if len(w.Template.Spec.PriorityClassName) != 0 {
+				return w.Template.Spec.PriorityClassName
+			}
+		}
+	}
+	return ""
+}
+
+func (j *RayJob) PodsReady() bool {
+	return j.Status.RayClusterStatus.State == rayjobapi.Ready
+}
+
+func (j *RayJob) ReclaimablePods() []kueue.ReclaimablePod {
+	return nil
+}
+
+// SetupWithManager sets up the controller with the Manager. It indexes workloads
+// based on the owning jobs.
+func (r *RayJobReconciler) SetupWithManager(mgr ctrl.Manager) error {
+	return ctrl.NewControllerManagedBy(mgr).
+		For(&rayjobapi.RayJob{}).
+		Owns(&kueue.Workload{}).
+		Complete(r)
+}
+
+func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
+	return jobframework.SetupWorkloadOwnerIndex(ctx, indexer, gvk)
+}
+
+//+kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+//+kubebuilder:rbac:groups=ray.io,resources=rayjobs,verbs=get;list;watch;update;patch
+//+kubebuilder:rbac:groups=ray.io,resources=rayjobs/status,verbs=get
```

  ```suggestion
  //+kubebuilder:rbac:groups=ray.io,resources=rayjobs/status,verbs=get;update
  ```
  
  I found we need to add permission to update statuses to framework job controllers.
  
  ref: #797

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-23T13:57:50Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:265** by [@trasc](https://github.com/trasc) — 2023-05-23T13:57:50Z
```diff
@@ -0,0 +1,278 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a RayJob object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+var _ jobframework.GenericJob = (*RayJob)(nil)
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	if len(nodeSelectors) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if !equality.Semantic.DeepEqual(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector) {
+		headPodSpec.NodeSelector = cloneSelectors(nodeSelectors[0].NodeSelector)
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector) {
+			workerPodSpec.NodeSelector = cloneSelectors(nodeSelectors[index+1].NodeSelector)
+		}
+	}
+}
+
+func (j *RayJob) Finished() (metav1.Condition, bool) {
+	condition := metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  string(j.Status.JobStatus),
+		Message: j.Status.Message,
+	}
+	return condition, rayjobapi.IsJobTerminal(j.Status.JobStatus)
+}
+
+func (j *RayJob) EquivalentToWorkload(wl kueue.Workload) bool {
+	if len(wl.Spec.PodSets) != (len(j.Spec.RayClusterSpec.WorkerGroupSpecs) + 1) {
+		return false
+	}
+
+	podSets := wl.Spec.PodSets
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	if podSets[0].Count != 1 {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.InitContainers, podSets[0].Template.Spec.InitContainers) {
+		return false
+	}
+	if !equality.Semantic.DeepEqual(headPodSpec.Containers, podSets[0].Template.Spec.Containers) {
+		return false
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		// MinReplicas and MaxReplicas should be taken into account for partial admission.
+		if podSets[index+1].Count != *j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Replicas {
+			return false
+		}
+
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		if !equality.Semantic.DeepEqual(workerPodSpec.InitContainers, podSets[index+1].Template.Spec.InitContainers) {
+			return false
+		}
+		if !equality.Semantic.DeepEqual(workerPodSpec.Containers, podSets[index+1].Template.Spec.Containers) {
+			return false
+		}
+	}
+	return true
+}
+
+func (j *RayJob) PriorityClass() string {
+	if j.Spec.RayClusterSpec != nil {
+		rcs := j.Spec.RayClusterSpec
+		if len(rcs.HeadGroupSpec.Template.Spec.PriorityClassName) != 0 {
+			return rcs.HeadGroupSpec.Template.Spec.PriorityClassName
+		}
+		for wi := range rcs.WorkerGroupSpecs {
+			w := &rcs.WorkerGroupSpecs[wi]
+			if len(w.Template.Spec.PriorityClassName) != 0 {
+				return w.Template.Spec.PriorityClassName
+			}
+		}
+	}
+	return ""
+}
+
+func (j *RayJob) PodsReady() bool {
+	return j.Status.RayClusterStatus.State == rayjobapi.Ready
+}
+
+func (j *RayJob) ReclaimablePods() []kueue.ReclaimablePod {
+	return nil
+}
+
+// SetupWithManager sets up the controller with the Manager. It indexes workloads
+// based on the owning jobs.
+func (r *RayJobReconciler) SetupWithManager(mgr ctrl.Manager) error {
+	return ctrl.NewControllerManagedBy(mgr).
+		For(&rayjobapi.RayJob{}).
+		Owns(&kueue.Workload{}).
+		Complete(r)
+}
+
+func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
+	return jobframework.SetupWorkloadOwnerIndex(ctx, indexer, gvk)
+}
+
+//+kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+//+kubebuilder:rbac:groups=ray.io,resources=rayjobs,verbs=get;list;watch;update;patch
+//+kubebuilder:rbac:groups=ray.io,resources=rayjobs/status,verbs=get
```

  thanks, done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-23T20:17:40Z

- **Makefile:286** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-23T20:04:20Z
```diff
@@ -286,3 +286,11 @@ mpi-operator-crd:
 	GOPATH=/tmp GO111MODULE=on $(GO_CMD) install github.com/kubeflow/mpi-operator/cmd/mpi-operator@$(MPI_VERSION)
```

  can you update this to use `go list` as well? In a separate PR

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:107** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-23T20:08:49Z
```diff
@@ -0,0 +1,131 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/pointer"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+type RayJobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for rayjobapi RayJob.
+func SetupRayJobWebhook(mgr ctrl.Manager, opts ...jobframework.Option) error {
+	options := jobframework.DefaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &RayJobWebhook{
+		manageJobsWithoutQueueName: options.ManageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&rayjobapi.RayJob{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-ray-io-v1alpha1-rayjob,mutating=true,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=create,versions=v1alpha1,name=mrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &RayJobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *RayJobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("rayjob-webhook")
+	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
+	jobframework.ApplyDefaultForSuspend((*RayJob)(job), w.manageJobsWithoutQueueName)
+	return nil
+}
+
+// +kubebuilder:webhook:path=/validate-ray-io-v1alpha1-rayjob,mutating=false,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=update,versions=v1alpha1,name=vrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomValidator = &RayJobWebhook{}
+
+// ValidateCreate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *RayJobWebhook) ValidateCreate(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("rayjob-webhook")
+	log.Info("Validating create", "job", klog.KObj(job))
+	return w.validateCreate(job).ToAggregate()
+}
+
+func (w *RayJobWebhook) validateCreate(job *rayjobapi.RayJob) field.ErrorList {
+	var allErrors field.ErrorList
+	kueueJob := (*RayJob)(job)
+
+	if w.manageJobsWithoutQueueName || jobframework.QueueName(kueueJob) != "" {
+		spec := &job.Spec
+		specPath := field.NewPath("spec")
+
+		// Should always delete the cluster after the sob has ended, otherwise it will continue to the queue's resources.
+		if !spec.ShutdownAfterJobFinishes {
+			allErrors = append(allErrors, field.Invalid(specPath.Child("shutdownAfterJobFinishes"), spec.ShutdownAfterJobFinishes, "a kueue managed job should delete the cluster after finishing"))
+		}
+
+		// Should not want existing cluster. Keuue (workload) should be able to control the admission of the actual work, not only the trigger.
+		if len(spec.ClusterSelector) > 0 {
+			allErrors = append(allErrors, field.Invalid(specPath.Child("clusterSelector"), spec.ClusterSelector, "a kueue managed job should not use an existing cluster"))
+		}
+
+		clusterSpec := spec.RayClusterSpec
+		clusterSpecPath := specPath.Child("rayClusterSpec")
+
+		// Should not use auto scaler. Once the resources are reserved by queue the cluster should do it's best to use them.
+		if pointer.BoolDeref(clusterSpec.EnableInTreeAutoscaling, false) {
+			allErrors = append(allErrors, field.Invalid(clusterSpecPath.Child("enableInTreeAutoscaling"), clusterSpec.EnableInTreeAutoscaling, "a kueue managed job should not use autoscaling"))
+		}
+
+		// Should limit the worker count to 8 - 1 (max podSets num - cluster head)
+		if len(clusterSpec.WorkerGroupSpecs) > 7 {
+			allErrors = append(allErrors, field.TooMany(clusterSpecPath.Child("workerGroupSpecs"), len(clusterSpec.WorkerGroupSpecs), 7))
```

  Does WorkerGroupSpecs have a limit? We could consider bumping the limit of Workload to match it.

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:106** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-23T20:09:18Z
```diff
@@ -0,0 +1,131 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/pointer"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+type RayJobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for rayjobapi RayJob.
+func SetupRayJobWebhook(mgr ctrl.Manager, opts ...jobframework.Option) error {
+	options := jobframework.DefaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &RayJobWebhook{
+		manageJobsWithoutQueueName: options.ManageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&rayjobapi.RayJob{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-ray-io-v1alpha1-rayjob,mutating=true,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=create,versions=v1alpha1,name=mrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &RayJobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *RayJobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("rayjob-webhook")
+	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
+	jobframework.ApplyDefaultForSuspend((*RayJob)(job), w.manageJobsWithoutQueueName)
+	return nil
+}
+
+// +kubebuilder:webhook:path=/validate-ray-io-v1alpha1-rayjob,mutating=false,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=update,versions=v1alpha1,name=vrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomValidator = &RayJobWebhook{}
+
+// ValidateCreate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *RayJobWebhook) ValidateCreate(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("rayjob-webhook")
+	log.Info("Validating create", "job", klog.KObj(job))
+	return w.validateCreate(job).ToAggregate()
+}
+
+func (w *RayJobWebhook) validateCreate(job *rayjobapi.RayJob) field.ErrorList {
+	var allErrors field.ErrorList
+	kueueJob := (*RayJob)(job)
+
+	if w.manageJobsWithoutQueueName || jobframework.QueueName(kueueJob) != "" {
+		spec := &job.Spec
+		specPath := field.NewPath("spec")
+
+		// Should always delete the cluster after the sob has ended, otherwise it will continue to the queue's resources.
+		if !spec.ShutdownAfterJobFinishes {
+			allErrors = append(allErrors, field.Invalid(specPath.Child("shutdownAfterJobFinishes"), spec.ShutdownAfterJobFinishes, "a kueue managed job should delete the cluster after finishing"))
+		}
+
+		// Should not want existing cluster. Keuue (workload) should be able to control the admission of the actual work, not only the trigger.
+		if len(spec.ClusterSelector) > 0 {
+			allErrors = append(allErrors, field.Invalid(specPath.Child("clusterSelector"), spec.ClusterSelector, "a kueue managed job should not use an existing cluster"))
+		}
+
+		clusterSpec := spec.RayClusterSpec
+		clusterSpecPath := specPath.Child("rayClusterSpec")
+
+		// Should not use auto scaler. Once the resources are reserved by queue the cluster should do it's best to use them.
+		if pointer.BoolDeref(clusterSpec.EnableInTreeAutoscaling, false) {
+			allErrors = append(allErrors, field.Invalid(clusterSpecPath.Child("enableInTreeAutoscaling"), clusterSpec.EnableInTreeAutoscaling, "a kueue managed job should not use autoscaling"))
+		}
+
+		// Should limit the worker count to 8 - 1 (max podSets num - cluster head)
+		if len(clusterSpec.WorkerGroupSpecs) > 7 {
```

  we should also validate that none of them are named "head"

- **pkg/controller/jobs/rayjob/rayjob_controller.go:166** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-23T20:13:17Z
```diff
@@ -0,0 +1,278 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a RayJob object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+var _ jobframework.GenericJob = (*RayJob)(nil)
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
+	dst := make(map[string]string, len(src))
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RestoreNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	if len(nodeSelectors) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
```

  I guess we wouldn't reach this code? Don't we check that the Workload matches the job before this?

- **pkg/controller/jobs/rayjob/rayjob_controller.go:157** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-23T20:14:16Z
```diff
@@ -0,0 +1,278 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a RayJob object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+var _ jobframework.GenericJob = (*RayJob)(nil)
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
```

  maybe this should go into a util package?
  
  it could even use generics.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-24T05:29:52Z

- **Makefile:286** by [@trasc](https://github.com/trasc) — 2023-05-24T05:29:52Z
```diff
@@ -286,3 +286,11 @@ mpi-operator-crd:
 	GOPATH=/tmp GO111MODULE=on $(GO_CMD) install github.com/kubeflow/mpi-operator/cmd/mpi-operator@$(MPI_VERSION)
```

  will do #801

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-24T06:14:25Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:157** by [@trasc](https://github.com/trasc) — 2023-05-24T05:48:57Z
```diff
@@ -0,0 +1,278 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"strings"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+var (
+	gvk           = rayjobapi.GroupVersion.WithKind("RayJob")
+	FrameworkName = "ray.io/rayjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupRayJobWebhook,
+		JobType:       &rayjobapi.RayJob{},
+		AddToScheme:   rayjobapi.AddToScheme,
+	}))
+}
+
+// RayJobReconciler reconciles a RayJob object
+type RayJobReconciler jobframework.JobReconciler
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	return (*RayJobReconciler)(jobframework.NewReconciler(scheme,
+		client,
+		record,
+		opts...,
+	))
+}
+
+type RayJob rayjobapi.RayJob
+
+var _ jobframework.GenericJob = (*RayJob)(nil)
+
+func (j *RayJob) Object() client.Object {
+	return (*rayjobapi.RayJob)(j)
+}
+
+func (j *RayJob) IsSuspended() bool {
+	return j.Spec.Suspend
+}
+
+func (j *RayJob) IsActive() bool {
+	return j.Status.JobDeploymentStatus != rayjobapi.JobDeploymentStatusSuspended
+}
+
+func (j *RayJob) Suspend() {
+	j.Spec.Suspend = true
+}
+
+func (j *RayJob) ResetStatus() bool {
+	if j.Status.StartTime == nil {
+		return false
+	}
+	j.Status.StartTime = nil
+	return true
+}
+
+func (j *RayJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *RayJob) PodSets() []kueue.PodSet {
+	// len = workerGroups + head
+	podSets := make([]kueue.PodSet, len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1)
+
+	// head
+	podSets[0] = kueue.PodSet{
+		Name:     "head",
+		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+		replicas := int32(1)
+		if wgs.Replicas != nil {
+			replicas = *wgs.Replicas
+		}
+		podSets[index+1] = kueue.PodSet{
+			Name:     strings.ToLower(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    replicas,
+		}
+	}
+	return podSets
+}
+
+func applySelectors(dst, src map[string]string) map[string]string {
+	if len(dst) == 0 {
+		return src
+	}
+	for k, v := range src {
+		dst[k] = v
+	}
+	return dst
+}
+
+func (j *RayJob) RunWithNodeAffinity(nodeSelectors []jobframework.PodSetNodeSelector) {
+	j.Spec.Suspend = false
+	if len(nodeSelectors) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
+		return
+	}
+
+	// head
+	headPodSpec := &j.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec
+	headPodSpec.NodeSelector = applySelectors(headPodSpec.NodeSelector, nodeSelectors[0].NodeSelector)
+
+	// workers
+	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
+		workerPodSpec := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index].Template.Spec
+		workerPodSpec.NodeSelector = applySelectors(workerPodSpec.NodeSelector, nodeSelectors[index+1].NodeSelector)
+	}
+}
+
+func cloneSelectors(src map[string]string) map[string]string {
```

  We should do it in #782

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:106** by [@trasc](https://github.com/trasc) — 2023-05-24T06:12:03Z
```diff
@@ -0,0 +1,131 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/pointer"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+type RayJobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for rayjobapi RayJob.
+func SetupRayJobWebhook(mgr ctrl.Manager, opts ...jobframework.Option) error {
+	options := jobframework.DefaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &RayJobWebhook{
+		manageJobsWithoutQueueName: options.ManageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&rayjobapi.RayJob{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-ray-io-v1alpha1-rayjob,mutating=true,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=create,versions=v1alpha1,name=mrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &RayJobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *RayJobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("rayjob-webhook")
+	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
+	jobframework.ApplyDefaultForSuspend((*RayJob)(job), w.manageJobsWithoutQueueName)
+	return nil
+}
+
+// +kubebuilder:webhook:path=/validate-ray-io-v1alpha1-rayjob,mutating=false,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=update,versions=v1alpha1,name=vrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomValidator = &RayJobWebhook{}
+
+// ValidateCreate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *RayJobWebhook) ValidateCreate(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("rayjob-webhook")
+	log.Info("Validating create", "job", klog.KObj(job))
+	return w.validateCreate(job).ToAggregate()
+}
+
+func (w *RayJobWebhook) validateCreate(job *rayjobapi.RayJob) field.ErrorList {
+	var allErrors field.ErrorList
+	kueueJob := (*RayJob)(job)
+
+	if w.manageJobsWithoutQueueName || jobframework.QueueName(kueueJob) != "" {
+		spec := &job.Spec
+		specPath := field.NewPath("spec")
+
+		// Should always delete the cluster after the sob has ended, otherwise it will continue to the queue's resources.
+		if !spec.ShutdownAfterJobFinishes {
+			allErrors = append(allErrors, field.Invalid(specPath.Child("shutdownAfterJobFinishes"), spec.ShutdownAfterJobFinishes, "a kueue managed job should delete the cluster after finishing"))
+		}
+
+		// Should not want existing cluster. Keuue (workload) should be able to control the admission of the actual work, not only the trigger.
+		if len(spec.ClusterSelector) > 0 {
+			allErrors = append(allErrors, field.Invalid(specPath.Child("clusterSelector"), spec.ClusterSelector, "a kueue managed job should not use an existing cluster"))
+		}
+
+		clusterSpec := spec.RayClusterSpec
+		clusterSpecPath := specPath.Child("rayClusterSpec")
+
+		// Should not use auto scaler. Once the resources are reserved by queue the cluster should do it's best to use them.
+		if pointer.BoolDeref(clusterSpec.EnableInTreeAutoscaling, false) {
+			allErrors = append(allErrors, field.Invalid(clusterSpecPath.Child("enableInTreeAutoscaling"), clusterSpec.EnableInTreeAutoscaling, "a kueue managed job should not use autoscaling"))
+		}
+
+		// Should limit the worker count to 8 - 1 (max podSets num - cluster head)
+		if len(clusterSpec.WorkerGroupSpecs) > 7 {
```

  done

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:107** by [@trasc](https://github.com/trasc) — 2023-05-24T06:12:20Z
```diff
@@ -0,0 +1,131 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/pointer"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+type RayJobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for rayjobapi RayJob.
+func SetupRayJobWebhook(mgr ctrl.Manager, opts ...jobframework.Option) error {
+	options := jobframework.DefaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &RayJobWebhook{
+		manageJobsWithoutQueueName: options.ManageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&rayjobapi.RayJob{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-ray-io-v1alpha1-rayjob,mutating=true,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=create,versions=v1alpha1,name=mrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &RayJobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *RayJobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("rayjob-webhook")
+	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
+	jobframework.ApplyDefaultForSuspend((*RayJob)(job), w.manageJobsWithoutQueueName)
+	return nil
+}
+
+// +kubebuilder:webhook:path=/validate-ray-io-v1alpha1-rayjob,mutating=false,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=update,versions=v1alpha1,name=vrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomValidator = &RayJobWebhook{}
+
+// ValidateCreate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *RayJobWebhook) ValidateCreate(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("rayjob-webhook")
+	log.Info("Validating create", "job", klog.KObj(job))
+	return w.validateCreate(job).ToAggregate()
+}
+
+func (w *RayJobWebhook) validateCreate(job *rayjobapi.RayJob) field.ErrorList {
+	var allErrors field.ErrorList
+	kueueJob := (*RayJob)(job)
+
+	if w.manageJobsWithoutQueueName || jobframework.QueueName(kueueJob) != "" {
+		spec := &job.Spec
+		specPath := field.NewPath("spec")
+
+		// Should always delete the cluster after the sob has ended, otherwise it will continue to the queue's resources.
+		if !spec.ShutdownAfterJobFinishes {
+			allErrors = append(allErrors, field.Invalid(specPath.Child("shutdownAfterJobFinishes"), spec.ShutdownAfterJobFinishes, "a kueue managed job should delete the cluster after finishing"))
+		}
+
+		// Should not want existing cluster. Keuue (workload) should be able to control the admission of the actual work, not only the trigger.
+		if len(spec.ClusterSelector) > 0 {
+			allErrors = append(allErrors, field.Invalid(specPath.Child("clusterSelector"), spec.ClusterSelector, "a kueue managed job should not use an existing cluster"))
+		}
+
+		clusterSpec := spec.RayClusterSpec
+		clusterSpecPath := specPath.Child("rayClusterSpec")
+
+		// Should not use auto scaler. Once the resources are reserved by queue the cluster should do it's best to use them.
+		if pointer.BoolDeref(clusterSpec.EnableInTreeAutoscaling, false) {
+			allErrors = append(allErrors, field.Invalid(clusterSpecPath.Child("enableInTreeAutoscaling"), clusterSpec.EnableInTreeAutoscaling, "a kueue managed job should not use autoscaling"))
+		}
+
+		// Should limit the worker count to 8 - 1 (max podSets num - cluster head)
+		if len(clusterSpec.WorkerGroupSpecs) > 7 {
+			allErrors = append(allErrors, field.TooMany(clusterSpecPath.Child("workerGroupSpecs"), len(clusterSpec.WorkerGroupSpecs), 7))
```

  no, no limit on the ray side

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-24T12:18:22Z

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:107** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-24T12:18:21Z
```diff
@@ -0,0 +1,131 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/pointer"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+)
+
+type RayJobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for rayjobapi RayJob.
+func SetupRayJobWebhook(mgr ctrl.Manager, opts ...jobframework.Option) error {
+	options := jobframework.DefaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &RayJobWebhook{
+		manageJobsWithoutQueueName: options.ManageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&rayjobapi.RayJob{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-ray-io-v1alpha1-rayjob,mutating=true,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=create,versions=v1alpha1,name=mrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &RayJobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *RayJobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("rayjob-webhook")
+	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
+	jobframework.ApplyDefaultForSuspend((*RayJob)(job), w.manageJobsWithoutQueueName)
+	return nil
+}
+
+// +kubebuilder:webhook:path=/validate-ray-io-v1alpha1-rayjob,mutating=false,failurePolicy=fail,sideEffects=None,groups=ray.io,resources=rayjobs,verbs=update,versions=v1alpha1,name=vrayjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomValidator = &RayJobWebhook{}
+
+// ValidateCreate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *RayJobWebhook) ValidateCreate(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*rayjobapi.RayJob)
+	log := ctrl.LoggerFrom(ctx).WithName("rayjob-webhook")
+	log.Info("Validating create", "job", klog.KObj(job))
+	return w.validateCreate(job).ToAggregate()
+}
+
+func (w *RayJobWebhook) validateCreate(job *rayjobapi.RayJob) field.ErrorList {
+	var allErrors field.ErrorList
+	kueueJob := (*RayJob)(job)
+
+	if w.manageJobsWithoutQueueName || jobframework.QueueName(kueueJob) != "" {
+		spec := &job.Spec
+		specPath := field.NewPath("spec")
+
+		// Should always delete the cluster after the sob has ended, otherwise it will continue to the queue's resources.
+		if !spec.ShutdownAfterJobFinishes {
+			allErrors = append(allErrors, field.Invalid(specPath.Child("shutdownAfterJobFinishes"), spec.ShutdownAfterJobFinishes, "a kueue managed job should delete the cluster after finishing"))
+		}
+
+		// Should not want existing cluster. Keuue (workload) should be able to control the admission of the actual work, not only the trigger.
+		if len(spec.ClusterSelector) > 0 {
+			allErrors = append(allErrors, field.Invalid(specPath.Child("clusterSelector"), spec.ClusterSelector, "a kueue managed job should not use an existing cluster"))
+		}
+
+		clusterSpec := spec.RayClusterSpec
+		clusterSpecPath := specPath.Child("rayClusterSpec")
+
+		// Should not use auto scaler. Once the resources are reserved by queue the cluster should do it's best to use them.
+		if pointer.BoolDeref(clusterSpec.EnableInTreeAutoscaling, false) {
+			allErrors = append(allErrors, field.Invalid(clusterSpecPath.Child("enableInTreeAutoscaling"), clusterSpec.EnableInTreeAutoscaling, "a kueue managed job should not use autoscaling"))
+		}
+
+		// Should limit the worker count to 8 - 1 (max podSets num - cluster head)
+		if len(clusterSpec.WorkerGroupSpecs) > 7 {
+			allErrors = append(allErrors, field.TooMany(clusterSpecPath.Child("workerGroupSpecs"), len(clusterSpec.WorkerGroupSpecs), 7))
```

  can you open an issue on their side? That's not a good API practice.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-24T18:57:19Z

/approve
