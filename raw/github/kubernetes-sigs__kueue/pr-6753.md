# PR #6753: feat: add exponential backoff for scheduler second pass

**Summary**: feat: add exponential backoff for scheduler second pass

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6753

**Last updated**: 2025-09-26T14:46:22Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mykysha](https://github.com/mykysha)
- **Branch**: `feat/tas-exponential-backoff` → `main`
- **Created**: 2025-09-08T11:25:46Z
- **Updated**: 2025-09-26T14:46:22Z
- **Merged**: 2025-09-26T13:18:19Z
- **Closed**: 2025-09-26T13:18:19Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 8   **+178 / -12**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
A follow-up for #5052

Add an exponential backoff for the scheduler second pass.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #5246

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Add an exponential backoff for the TAS scheduler second pass.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-09-08T11:27:20Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 4694cc4671c4a042a5e40b66306f7dfac01eb955 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68d68d337739190008dbe5e9 |

### Comment by [@mykysha](https://github.com/mykysha) — 2025-09-08T12:27:56Z

/retest-required

### Comment by [@mykysha](https://github.com/mykysha) — 2025-09-08T14:11:38Z

/retest-required

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-10T08:24:59Z

@mykysha can you think also of a reliable integration test? I know it is tricky for time-based issues, but we could control the parameters of backoff. I think it would be really helpful to make sure refactorings like proposed in [comment](https://github.com/kubernetes-sigs/kueue/pull/6753#discussion_r2333566369) don't break it.

### Comment by [@mykysha](https://github.com/mykysha) — 2025-09-10T08:41:10Z

I agree that an integration test would be helpful, however unless some larger backoff times are used they will flake, based on my local testing. I believe using large factor in a combination with relatively slow initial duration would be an option, but still requires some precision in design not to flake.

Will try to do and post the findings here.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-10T08:44:43Z

> I agree that an integration test would be helpful, however unless some larger backoff times are used they will flake, based on my local testing. 

Yes, same thoughts here. 

> I believe using large factor in a combination with relatively slow initial duration would be an option, but still requires some precision in design not to flake.

We can probably afford the test to be up to 30s, as long as we annotate it as "Slow", so that it runs in the dedicated CI job "kueue-test-integration-extended-main": https://prow.k8s.io/job-history/gs/kubernetes-ci-logs/pr-logs/directory/pull-kueue-test-integration-extended-main

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-10T08:51:12Z

Maybe we use ProvReq 1s, 2s, 4s, 8s, then trigger retry after 10s. Then, wait in test code only 5s for admission. If it wasn't reset, then the next attempt would be after 8s. 

Then the active phase of the test would be around 10s (to trigger retry) + 5s for the next wait, so around 15s. This is totally acceptable for a "slow" test. As I say, even 30s is ok to leave the margines bigger.

Or something along the lines (feel free to adjust the parametrization, that is just the idea).

### Comment by [@mykysha](https://github.com/mykysha) — 2025-09-17T14:01:48Z

Moved the backoff metadata storage to the workload.Info, and added an integration test focused on the testing of the backoff logic

### Comment by [@mykysha](https://github.com/mykysha) — 2025-09-23T08:45:22Z

/retest-required

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-25T08:16:47Z

cc @PBundyra 
in case you have a bit of time to be the second pair of eyes on this

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-26T12:36:20Z

LGTM label has been added.  <details>Git tree hash: 108b206900eff895622d1d89c68467fba0df3b9b</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-26T12:57:28Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-26T12:57:36Z

LGTM label has been added.  <details>Git tree hash: 556b8f537f97cc5b5666f77a14ff4300c54824a2</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-26T13:02:49Z

/approve
Thank you 👍

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-26T13:02:58Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6753#pullrequestreview-3272055106" title="LGTM">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6753#issuecomment-3338634972" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6753#" title="Author self-approved">mykysha</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-09T07:13:09Z

- **pkg/cache/queue/manager.go:94** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-09T07:13:09Z
```diff
@@ -88,6 +90,13 @@ func WithResourceTransformations(transforms []config.ResourceTransformation) Opt
 	}
 }
 
+// WithSecondPassBackoff overrides the exponential backoff used for second-pass requeues.
+func WithSecondPassBackoff(b wait.Backoff) Option {
```

  Is this used anywhere besides unit tests?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-09T07:18:41Z

- **pkg/cache/queue/manager.go:849** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-09T07:18:41Z
```diff
@@ -788,21 +810,52 @@ func (m *Manager) DeleteSnapshot(cq *kueue.ClusterQueue) {
 // pass queue.
 func (m *Manager) DeleteSecondPassWithoutLock(w *kueue.Workload) {
 	m.secondPassQueue.deleteByKey(workload.Key(w))
+	delete(m.secondPassDelayFns, workload.Key(w))
 }
 
-// QueueSecondPassIfNeeded queues for the second pass of scheduling with 1s
+// nextSecondPassDelay returns the next backoff duration for the workload's second pass,
+// creating a new backoff state if needed.
+func (m *Manager) nextSecondPassDelay(wl *kueue.Workload) time.Duration {
+	key := workload.Key(wl)
+
+	m.backoffMu.RLock()
+	fn, ok := m.secondPassDelayFns[key]
+	m.backoffMu.RUnlock()
+
+	if !ok {
+		fn = m.secondPassBackoff.DelayFunc()
+		m.backoffMu.Lock()
+		m.secondPassDelayFns[key] = fn
+		m.backoffMu.Unlock()
+	}
+	return fn()
+}
+
+// resetSecondPassBackoff forgets any backoff state for the workload so that
+// subsequent second-pass queuing starts from the initial delay again.
+func (m *Manager) resetSecondPassBackoff(wl *kueue.Workload) {
+	key := workload.Key(wl)
+
+	m.backoffMu.Lock()
+	delete(m.secondPassDelayFns, key)
+	m.backoffMu.Unlock()
+}
+
+// QueueSecondPassIfNeeded queues for the second pass of scheduling with exponential
 // delay.
 func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload) bool {
-	if workload.NeedsSecondPass(w) {
-		log := ctrl.LoggerFrom(ctx)
-		log.V(3).Info("Workload pre-queued for second pass", "workload", workload.Key(w))
-		m.secondPassQueue.prequeue(w)
-		m.clock.AfterFunc(time.Second, func() {
-			m.queueSecondPass(ctx, w)
-		})
-		return true
+	if !workload.NeedsSecondPass(w) {
+		m.resetSecondPassBackoff(w)
+		return false
```

  Could you please revert these changes to minimize the changelog for this PR?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-09T07:22:06Z

- **pkg/cache/queue/manager.go:94** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-09T07:22:06Z
```diff
@@ -88,6 +90,13 @@ func WithResourceTransformations(transforms []config.ResourceTransformation) Opt
 	}
 }
 
+// WithSecondPassBackoff overrides the exponential backoff used for second-pass requeues.
+func WithSecondPassBackoff(b wait.Backoff) Option {
```

  I’m wondering if we should also add this to the configuration.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-09-09T07:41:02Z

- **pkg/cache/queue/manager.go:849** by [@mykysha](https://github.com/mykysha) — 2025-09-09T07:41:02Z
```diff
@@ -788,21 +810,52 @@ func (m *Manager) DeleteSnapshot(cq *kueue.ClusterQueue) {
 // pass queue.
 func (m *Manager) DeleteSecondPassWithoutLock(w *kueue.Workload) {
 	m.secondPassQueue.deleteByKey(workload.Key(w))
+	delete(m.secondPassDelayFns, workload.Key(w))
 }
 
-// QueueSecondPassIfNeeded queues for the second pass of scheduling with 1s
+// nextSecondPassDelay returns the next backoff duration for the workload's second pass,
+// creating a new backoff state if needed.
+func (m *Manager) nextSecondPassDelay(wl *kueue.Workload) time.Duration {
+	key := workload.Key(wl)
+
+	m.backoffMu.RLock()
+	fn, ok := m.secondPassDelayFns[key]
+	m.backoffMu.RUnlock()
+
+	if !ok {
+		fn = m.secondPassBackoff.DelayFunc()
+		m.backoffMu.Lock()
+		m.secondPassDelayFns[key] = fn
+		m.backoffMu.Unlock()
+	}
+	return fn()
+}
+
+// resetSecondPassBackoff forgets any backoff state for the workload so that
+// subsequent second-pass queuing starts from the initial delay again.
+func (m *Manager) resetSecondPassBackoff(wl *kueue.Workload) {
+	key := workload.Key(wl)
+
+	m.backoffMu.Lock()
+	delete(m.secondPassDelayFns, key)
+	m.backoffMu.Unlock()
+}
+
+// QueueSecondPassIfNeeded queues for the second pass of scheduling with exponential
 // delay.
 func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload) bool {
-	if workload.NeedsSecondPass(w) {
-		log := ctrl.LoggerFrom(ctx)
-		log.V(3).Info("Workload pre-queued for second pass", "workload", workload.Key(w))
-		m.secondPassQueue.prequeue(w)
-		m.clock.AfterFunc(time.Second, func() {
-			m.queueSecondPass(ctx, w)
-		})
-		return true
+	if !workload.NeedsSecondPass(w) {
+		m.resetSecondPassBackoff(w)
+		return false
```

  Surely, reversed the check

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-09-09T07:45:36Z

- **pkg/cache/queue/manager.go:94** by [@mykysha](https://github.com/mykysha) — 2025-09-09T07:45:35Z
```diff
@@ -88,6 +90,13 @@ func WithResourceTransformations(transforms []config.ResourceTransformation) Opt
 	}
 }
 
+// WithSecondPassBackoff overrides the exponential backoff used for second-pass requeues.
+func WithSecondPassBackoff(b wait.Backoff) Option {
```

  Have thought about it as well, but unsure where to put it? As it's own `SchedulerSecondPassBackoff` in the config, or for it to be related to some existing configuration field?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-09T08:21:49Z

- **pkg/cache/queue/manager.go:94** by [@mimowo](https://github.com/mimowo) — 2025-09-09T08:21:49Z
```diff
@@ -88,6 +90,13 @@ func WithResourceTransformations(transforms []config.ResourceTransformation) Opt
 	}
 }
 
+// WithSecondPassBackoff overrides the exponential backoff used for second-pass requeues.
+func WithSecondPassBackoff(b wait.Backoff) Option {
```

  No need for configuration for now. It is very unlikely users would like to configure it themselves. We can add it if requested. 
  
  EDIT: at the same time it makes sense to expose for tests, but I would probably call it in the production code too.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-09T08:25:39Z

- **pkg/cache/queue/manager.go:132** by [@mimowo](https://github.com/mimowo) — 2025-09-09T08:25:39Z
```diff
@@ -117,6 +126,10 @@ type Manager struct {
 
 	afsEntryPenalties      *AfsEntryPenalties
 	workloadUpdateWatchers []WorkloadUpdateWatcher
+
+	secondPassDelayFns map[workload.Reference]wait.DelayFunc
+	secondPassBackoff  wait.Backoff
+	backoffMu          sync.RWMutex
```

  This seems like leaking implementation details of the secondPassQueue to manager. Can we instead encapsulate the structures into secondPassQueue? I'm not yet sure why we need the mutex at this level too. We already have one inside secondPassQueue.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-09-09T08:45:23Z

- **pkg/cache/queue/manager.go:132** by [@mykysha](https://github.com/mykysha) — 2025-09-09T08:45:23Z
```diff
@@ -117,6 +126,10 @@ type Manager struct {
 
 	afsEntryPenalties      *AfsEntryPenalties
 	workloadUpdateWatchers []WorkloadUpdateWatcher
+
+	secondPassDelayFns map[workload.Reference]wait.DelayFunc
+	secondPassBackoff  wait.Backoff
+	backoffMu          sync.RWMutex
```

  Can drop the mutex surely. 
  
  As for moving the logic to the secondPassQueue, can do, but for that I believe that the logic of moving the workload from the prequeue to the queue would be moved there as well, as that is where the timeout is.
  
  Or did you mean to just  move the `nextSecondPassDelay` with the related objects and call it from the manager as is?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-09T08:51:03Z

- **pkg/cache/queue/manager.go:132** by [@mimowo](https://github.com/mimowo) — 2025-09-09T08:51:02Z
```diff
@@ -117,6 +126,10 @@ type Manager struct {
 
 	afsEntryPenalties      *AfsEntryPenalties
 	workloadUpdateWatchers []WorkloadUpdateWatcher
+
+	secondPassDelayFns map[workload.Reference]wait.DelayFunc
+	secondPassBackoff  wait.Backoff
+	backoffMu          sync.RWMutex
```

  > Or did you mean to just move the nextSecondPassDelay with the related objects and call it from the manager as is?
  
  More like this. I would call the mechanics as is, just compute the delay from managed by calling the state of secondPassQueue

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-09-09T09:23:12Z

- **pkg/cache/queue/manager.go:132** by [@mykysha](https://github.com/mykysha) — 2025-09-09T09:23:12Z
```diff
@@ -117,6 +126,10 @@ type Manager struct {
 
 	afsEntryPenalties      *AfsEntryPenalties
 	workloadUpdateWatchers []WorkloadUpdateWatcher
+
+	secondPassDelayFns map[workload.Reference]wait.DelayFunc
+	secondPassBackoff  wait.Backoff
+	backoffMu          sync.RWMutex
```

  Removed the redundant mutex and moved the backoff logic to the secodPassQueue

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-09T10:14:23Z

- **pkg/cache/queue/manager.go:805** by [@mimowo](https://github.com/mimowo) — 2025-09-09T10:14:23Z
```diff
@@ -790,18 +789,20 @@ func (m *Manager) DeleteSecondPassWithoutLock(w *kueue.Workload) {
 	m.secondPassQueue.deleteByKey(workload.Key(w))
 }
 
-// QueueSecondPassIfNeeded queues for the second pass of scheduling with 1s
+// QueueSecondPassIfNeeded queues for the second pass of scheduling with exponential
 // delay.
 func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload) bool {
 	if workload.NeedsSecondPass(w) {
+		delay := m.secondPassQueue.nextSecondPassDelay(w)
 		log := ctrl.LoggerFrom(ctx)
-		log.V(3).Info("Workload pre-queued for second pass", "workload", workload.Key(w))
+		log.V(3).Info("Workload pre-queued for second pass (with backoff)", "workload", workload.Key(w), "delay", delay)
 		m.secondPassQueue.prequeue(w)
-		m.clock.AfterFunc(time.Second, func() {
+		m.clock.AfterFunc(delay, func() {
 			m.queueSecondPass(ctx, w)
 		})
 		return true
 	}
+	m.secondPassQueue.resetSecondPassBackoff(w)
```

  This should be eliminated. It means taking a lock even for workloads which don't use the second pass.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-09-09T11:58:07Z

- **pkg/cache/queue/manager.go:805** by [@mykysha](https://github.com/mykysha) — 2025-09-09T11:58:07Z
```diff
@@ -790,18 +789,20 @@ func (m *Manager) DeleteSecondPassWithoutLock(w *kueue.Workload) {
 	m.secondPassQueue.deleteByKey(workload.Key(w))
 }
 
-// QueueSecondPassIfNeeded queues for the second pass of scheduling with 1s
+// QueueSecondPassIfNeeded queues for the second pass of scheduling with exponential
 // delay.
 func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload) bool {
 	if workload.NeedsSecondPass(w) {
+		delay := m.secondPassQueue.nextSecondPassDelay(w)
 		log := ctrl.LoggerFrom(ctx)
-		log.V(3).Info("Workload pre-queued for second pass", "workload", workload.Key(w))
+		log.V(3).Info("Workload pre-queued for second pass (with backoff)", "workload", workload.Key(w), "delay", delay)
 		m.secondPassQueue.prequeue(w)
-		m.clock.AfterFunc(time.Second, func() {
+		m.clock.AfterFunc(delay, func() {
 			m.queueSecondPass(ctx, w)
 		})
 		return true
 	}
+	m.secondPassQueue.resetSecondPassBackoff(w)
```

  Can do, but I'm worried regarding leaving the workload backoff counters even if they are already admitted. If such a behavior is ok and the backoff counter should be reset only on workload deletion, will remove this altogether.
  
  In the other case, can tweak this to be `resetSecondPassBackoffIfNeeded` to first RLock, or store workload references separately in a slice so that it can be accessed for reading without having to lock first, and delete only if the workload reference is present in the counter

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-09T13:18:06Z

- **pkg/cache/queue/manager.go:805** by [@mimowo](https://github.com/mimowo) — 2025-09-09T13:18:06Z
```diff
@@ -790,18 +789,20 @@ func (m *Manager) DeleteSecondPassWithoutLock(w *kueue.Workload) {
 	m.secondPassQueue.deleteByKey(workload.Key(w))
 }
 
-// QueueSecondPassIfNeeded queues for the second pass of scheduling with 1s
+// QueueSecondPassIfNeeded queues for the second pass of scheduling with exponential
 // delay.
 func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload) bool {
 	if workload.NeedsSecondPass(w) {
+		delay := m.secondPassQueue.nextSecondPassDelay(w)
 		log := ctrl.LoggerFrom(ctx)
-		log.V(3).Info("Workload pre-queued for second pass", "workload", workload.Key(w))
+		log.V(3).Info("Workload pre-queued for second pass (with backoff)", "workload", workload.Key(w), "delay", delay)
 		m.secondPassQueue.prequeue(w)
-		m.clock.AfterFunc(time.Second, func() {
+		m.clock.AfterFunc(delay, func() {
 			m.queueSecondPass(ctx, w)
 		})
 		return true
 	}
+	m.secondPassQueue.resetSecondPassBackoff(w)
```

  > Can do, but I'm worried regarding leaving the workload backoff counters even if they are already admitted. If such a behavior is ok and the backoff counter should be reset only on workload deletion, will remove this altogether.
  
  Ah, yes. We certainly need to make sure the second pass counter is cleaned.
  
  The proposed code would work, but I'm thinking about a more clear approach:
  - we keep the "status" secondPassBackoff inside workload.Info structure on that object
  - then requeueAndUpdate will update the secondPassBackoff when calling RequeueSecondPassIfNeeded
  -  workload controller will call QueueSecondPassIfNeeded which calls RequeueSecondPassIfNeeded, but initalizing workload.Info to 0
  
  This way we don't need to reset the state at all - if scheduler succeeds then we don't need to requeue at all, and the workload.Info ends its life along with the state.  Let me know if there are gaps.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-25T08:10:23Z

- **pkg/cache/queue/second_pass_queue.go:36** by [@mimowo](https://github.com/mimowo) — 2025-09-25T08:10:23Z
```diff
@@ -30,12 +32,15 @@ type secondPassQueue struct {
 
 	prequeued sets.Set[workload.Reference]
 	queued    map[workload.Reference]*workload.Info
+
+	backoffFactor int
```

  Let's declare it as a const since it is never mutated

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-25T08:11:59Z

- **test/integration/singlecluster/tas/tas_test.go:2645** by [@mimowo](https://github.com/mimowo) — 2025-09-25T08:11:59Z
```diff
@@ -2552,6 +2553,141 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
 				})
 			})
+
+			ginkgo.It("resets exponential second-pass backoff when nodes become Ready", framework.SlowSpec, func() {
+				var (
+					wl1 *kueue.Workload
+				)
+
+				ginkgo.By("create workload", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0] = *testing.MakePodSet(kueue.DefaultPodSetName, 1).
+						Request(corev1.ResourceCPU, "1").
+						PreferredTopologyRequest(testing.DefaultRackTopologyLevel).
+						Image("image").
+						Obj()
+					util.MustCreate(ctx, k8sClient, wl1)
+				})
+
+				wlKey := client.ObjectKeyFromObject(wl1)
+
+				ginkgo.By("verify the workload reserves the quota and requires TAS assignment (second pass)", func() {
+					util.ExpectQuotaReservedWorkloadsTotalMetric(clusterQueue, "", 1)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+					util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, clusterQueue.Name, wl1)
+					gomega.Eventually(func(g gomega.Gomega) {
+						gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+						gomega.Expect(workload.HasTopologyAssignmentsPending(wl1)).Should(gomega.BeTrue())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				provReqKey := apitypes.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.ProvisioningRequestName(wlKey.Name, kueue.AdmissionCheckReference(ac.Name), 1),
+				}
+
+				ginkgo.By("await ProvisioningRequest creation", func() {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, provReqKey, &createdRequest)).Should(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("provision a node that is NOT Ready", func() {
+					nodes = []corev1.Node{
+						*testingnode.MakeNode("x1").
+							Label("node-group", "tas").
+							Label(testing.DefaultBlockTopologyLevel, "b1").
+							Label(testing.DefaultRackTopologyLevel, "r1").
+							Label(corev1.LabelHostname, "x1").
+							StatusAllocatable(corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+								corev1.ResourcePods:   resource.MustParse("10"),
+							}).
+							NotReady().
+							Obj(),
+					}
+					util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+				})
+
+				ginkgo.By("mark ProvisioningRequest as Provisioned (start of the backoff window)", func() {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, provReqKey, &createdRequest)).Should(gomega.Succeed())
+						apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+							Type:   autoscaling.Provisioned,
+							Status: metav1.ConditionTrue,
+							Reason: autoscaling.Provisioned,
+						})
+						g.Expect(k8sClient.Status().Update(ctx, &createdRequest)).Should(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				gomega.Consistently(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, wl1)).To(gomega.Succeed())
+					g.Expect(wl1.Status.Admission).ShouldNot(gomega.BeNil())
+					g.Expect(wl1.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
+					g.Expect(wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeNil())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+				ginkgo.By("observe at least three SecondPassFailed events while the node is NotReady (≈1s, 2s, 4s backoffs)", func() {
+					var evList corev1.EventList
+					gomega.Expect(k8sClient.List(ctx, &evList, &client.ListOptions{Namespace: ns.Name})).To(gomega.Succeed())
+					var count int32
+					for i := range evList.Items {
+						ev := evList.Items[i]
+						if ev.Reason == "SecondPassFailed" && ev.InvolvedObject.Name == wl1.Name {
+							if ev.Count > count {
+								count = ev.Count
+							}
+						}
+					}
+					gomega.Expect(count).To(gomega.BeNumerically("==", count))
```

  I think this should be wrapper in Eventually? Then, also we can use BeNumerically >= 3?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-25T08:13:30Z

- **test/integration/singlecluster/tas/tas_test.go:2557** by [@mimowo](https://github.com/mimowo) — 2025-09-25T08:13:30Z
```diff
@@ -2552,6 +2553,141 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
 				})
 			})
+
+			ginkgo.It("resets exponential second-pass backoff when nodes become Ready", framework.SlowSpec, func() {
```

  Could you please add a comment to the assert "It" which would fail if the backoff wasn't reset? 
  And explain why? It can be very useful when debugging. Please also confirm that experimentally.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-25T08:16:00Z

- **pkg/scheduler/scheduler.go:761** by [@mimowo](https://github.com/mimowo) — 2025-09-25T08:16:00Z
```diff
@@ -758,7 +758,8 @@ func (s *Scheduler) requeueAndUpdate(ctx context.Context, e entry) {
 		e.requeueReason = qcache.RequeueReasonFailedAfterNomination
 	}
 
-	if s.queues.QueueSecondPassIfNeeded(ctx, e.Obj) {
+	e.SecondPassIteration++
```

  Ah, we shouldn't do this here unconditionally. It may be bumed even if this is not second pass.
  
  The entire workload.Info should be passed inside `QueueSecondPassIfNeeded`, and bumped there.

### Review by [@PBundyra](https://github.com/PBundyra) (changes_requested) — 2025-09-25T08:32:18Z

- **pkg/workload/workload.go:193** by [@PBundyra](https://github.com/PBundyra) — 2025-09-25T08:22:32Z
```diff
@@ -189,6 +189,8 @@ type Info struct {
 	// LocalQueueFSUsage indicates the historical usage of resource in the LocalQueue, needed for the
 	// AdmissionFairSharing feature, it is only populated for Infos in cache.Snapshot (not in queue manager).
 	LocalQueueFSUsage *float64
+
```

  Please add a comment on what this field does

- **pkg/cache/queue/second_pass_queue.go:89** by [@PBundyra](https://github.com/PBundyra) — 2025-09-25T08:30:44Z
```diff
@@ -78,3 +83,8 @@ func (q *secondPassQueue) deleteByKey(key workload.Reference) {
 	delete(q.queued, key)
 	q.prequeued.Delete(key)
 }
+
+// nextSecondPassDelay returns the next backoff duration for the workload's second pass
+func (q *secondPassQueue) nextSecondPassDelay(iteration uint) time.Duration {
+	return time.Duration(math.Pow(float64(q.backoffFactor), float64(iteration))) * time.Second
```

  We should probably have some limit to this, like 10/30s, wdyt @mimowo

- **pkg/cache/queue/manager.go:796** by [@PBundyra](https://github.com/PBundyra) — 2025-09-25T08:31:46Z
```diff
@@ -790,27 +789,29 @@ func (m *Manager) DeleteSecondPassWithoutLock(w *kueue.Workload) {
 	m.secondPassQueue.deleteByKey(workload.Key(w))
 }
 
-// QueueSecondPassIfNeeded queues for the second pass of scheduling with 1s
+// QueueSecondPassIfNeeded queues for the second pass of scheduling with exponential
 // delay.
-func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload) bool {
+func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload, iteration uint) bool {
 	if workload.NeedsSecondPass(w) {
+		delay := m.secondPassQueue.nextSecondPassDelay(iteration)
```

  I don't think it need to be a `secondPassQueue` method, let's just use some util function for that

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-09-25T08:33:13Z

- **test/integration/singlecluster/tas/tas_test.go:2645** by [@mykysha](https://github.com/mykysha) — 2025-09-25T08:33:12Z
```diff
@@ -2552,6 +2553,141 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
 				})
 			})
+
+			ginkgo.It("resets exponential second-pass backoff when nodes become Ready", framework.SlowSpec, func() {
+				var (
+					wl1 *kueue.Workload
+				)
+
+				ginkgo.By("create workload", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0] = *testing.MakePodSet(kueue.DefaultPodSetName, 1).
+						Request(corev1.ResourceCPU, "1").
+						PreferredTopologyRequest(testing.DefaultRackTopologyLevel).
+						Image("image").
+						Obj()
+					util.MustCreate(ctx, k8sClient, wl1)
+				})
+
+				wlKey := client.ObjectKeyFromObject(wl1)
+
+				ginkgo.By("verify the workload reserves the quota and requires TAS assignment (second pass)", func() {
+					util.ExpectQuotaReservedWorkloadsTotalMetric(clusterQueue, "", 1)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+					util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, clusterQueue.Name, wl1)
+					gomega.Eventually(func(g gomega.Gomega) {
+						gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+						gomega.Expect(workload.HasTopologyAssignmentsPending(wl1)).Should(gomega.BeTrue())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				provReqKey := apitypes.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.ProvisioningRequestName(wlKey.Name, kueue.AdmissionCheckReference(ac.Name), 1),
+				}
+
+				ginkgo.By("await ProvisioningRequest creation", func() {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, provReqKey, &createdRequest)).Should(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("provision a node that is NOT Ready", func() {
+					nodes = []corev1.Node{
+						*testingnode.MakeNode("x1").
+							Label("node-group", "tas").
+							Label(testing.DefaultBlockTopologyLevel, "b1").
+							Label(testing.DefaultRackTopologyLevel, "r1").
+							Label(corev1.LabelHostname, "x1").
+							StatusAllocatable(corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+								corev1.ResourcePods:   resource.MustParse("10"),
+							}).
+							NotReady().
+							Obj(),
+					}
+					util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+				})
+
+				ginkgo.By("mark ProvisioningRequest as Provisioned (start of the backoff window)", func() {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, provReqKey, &createdRequest)).Should(gomega.Succeed())
+						apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+							Type:   autoscaling.Provisioned,
+							Status: metav1.ConditionTrue,
+							Reason: autoscaling.Provisioned,
+						})
+						g.Expect(k8sClient.Status().Update(ctx, &createdRequest)).Should(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				gomega.Consistently(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, wl1)).To(gomega.Succeed())
+					g.Expect(wl1.Status.Admission).ShouldNot(gomega.BeNil())
+					g.Expect(wl1.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
+					g.Expect(wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeNil())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+				ginkgo.By("observe at least three SecondPassFailed events while the node is NotReady (≈1s, 2s, 4s backoffs)", func() {
+					var evList corev1.EventList
+					gomega.Expect(k8sClient.List(ctx, &evList, &client.ListOptions{Namespace: ns.Name})).To(gomega.Succeed())
+					var count int32
+					for i := range evList.Items {
+						ev := evList.Items[i]
+						if ev.Reason == "SecondPassFailed" && ev.InvolvedObject.Name == wl1.Name {
+							if ev.Count > count {
+								count = ev.Count
+							}
+						}
+					}
+					gomega.Expect(count).To(gomega.BeNumerically("==", count))
```

  We can use BeNumerically to be larger than 3, surely, but my concern was that if we don't use exponential backoff we will pass this check, whereas using an exponential backoff should not update any second or so. Can add a check that it should be less than something like 5, which should both fail if exponential backoff is off and succeed if we somehow manage to fit in 4th run

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-25T08:40:25Z

- **test/integration/singlecluster/tas/tas_test.go:2645** by [@mimowo](https://github.com/mimowo) — 2025-09-25T08:40:25Z
```diff
@@ -2552,6 +2553,141 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
 				})
 			})
+
+			ginkgo.It("resets exponential second-pass backoff when nodes become Ready", framework.SlowSpec, func() {
+				var (
+					wl1 *kueue.Workload
+				)
+
+				ginkgo.By("create workload", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0] = *testing.MakePodSet(kueue.DefaultPodSetName, 1).
+						Request(corev1.ResourceCPU, "1").
+						PreferredTopologyRequest(testing.DefaultRackTopologyLevel).
+						Image("image").
+						Obj()
+					util.MustCreate(ctx, k8sClient, wl1)
+				})
+
+				wlKey := client.ObjectKeyFromObject(wl1)
+
+				ginkgo.By("verify the workload reserves the quota and requires TAS assignment (second pass)", func() {
+					util.ExpectQuotaReservedWorkloadsTotalMetric(clusterQueue, "", 1)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+					util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, clusterQueue.Name, wl1)
+					gomega.Eventually(func(g gomega.Gomega) {
+						gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+						gomega.Expect(workload.HasTopologyAssignmentsPending(wl1)).Should(gomega.BeTrue())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				provReqKey := apitypes.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.ProvisioningRequestName(wlKey.Name, kueue.AdmissionCheckReference(ac.Name), 1),
+				}
+
+				ginkgo.By("await ProvisioningRequest creation", func() {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, provReqKey, &createdRequest)).Should(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("provision a node that is NOT Ready", func() {
+					nodes = []corev1.Node{
+						*testingnode.MakeNode("x1").
+							Label("node-group", "tas").
+							Label(testing.DefaultBlockTopologyLevel, "b1").
+							Label(testing.DefaultRackTopologyLevel, "r1").
+							Label(corev1.LabelHostname, "x1").
+							StatusAllocatable(corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+								corev1.ResourcePods:   resource.MustParse("10"),
+							}).
+							NotReady().
+							Obj(),
+					}
+					util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+				})
+
+				ginkgo.By("mark ProvisioningRequest as Provisioned (start of the backoff window)", func() {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, provReqKey, &createdRequest)).Should(gomega.Succeed())
+						apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+							Type:   autoscaling.Provisioned,
+							Status: metav1.ConditionTrue,
+							Reason: autoscaling.Provisioned,
+						})
+						g.Expect(k8sClient.Status().Update(ctx, &createdRequest)).Should(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				gomega.Consistently(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, wl1)).To(gomega.Succeed())
+					g.Expect(wl1.Status.Admission).ShouldNot(gomega.BeNil())
+					g.Expect(wl1.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
+					g.Expect(wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeNil())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+				ginkgo.By("observe at least three SecondPassFailed events while the node is NotReady (≈1s, 2s, 4s backoffs)", func() {
+					var evList corev1.EventList
+					gomega.Expect(k8sClient.List(ctx, &evList, &client.ListOptions{Namespace: ns.Name})).To(gomega.Succeed())
+					var count int32
+					for i := range evList.Items {
+						ev := evList.Items[i]
+						if ev.Reason == "SecondPassFailed" && ev.InvolvedObject.Name == wl1.Name {
+							if ev.Count > count {
+								count = ev.Count
+							}
+						}
+					}
+					gomega.Expect(count).To(gomega.BeNumerically("==", count))
```

  Maybe then check if we are in a range? Say, not more than 5? Wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-25T08:40:50Z

- **pkg/cache/queue/second_pass_queue.go:89** by [@mimowo](https://github.com/mimowo) — 2025-09-25T08:40:50Z
```diff
@@ -78,3 +83,8 @@ func (q *secondPassQueue) deleteByKey(key workload.Reference) {
 	delete(q.queued, key)
 	q.prequeued.Delete(key)
 }
+
+// nextSecondPassDelay returns the next backoff duration for the workload's second pass
+func (q *secondPassQueue) nextSecondPassDelay(iteration uint) time.Duration {
+	return time.Duration(math.Pow(float64(q.backoffFactor), float64(iteration))) * time.Second
```

  Yes, let's at the cap at 30s

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-25T08:42:12Z

- **pkg/cache/queue/manager.go:796** by [@mimowo](https://github.com/mimowo) — 2025-09-25T08:42:12Z
```diff
@@ -790,27 +789,29 @@ func (m *Manager) DeleteSecondPassWithoutLock(w *kueue.Workload) {
 	m.secondPassQueue.deleteByKey(workload.Key(w))
 }
 
-// QueueSecondPassIfNeeded queues for the second pass of scheduling with 1s
+// QueueSecondPassIfNeeded queues for the second pass of scheduling with exponential
 // delay.
-func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload) bool {
+func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload, iteration uint) bool {
 	if workload.NeedsSecondPass(w) {
+		delay := m.secondPassQueue.nextSecondPassDelay(iteration)
```

  Where do you suggest the util to live? I think it makes sense to encapsulate the logic as much as possible inside the second_pass_queue file

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-09-25T08:49:44Z

- **pkg/cache/queue/manager.go:796** by [@PBundyra](https://github.com/PBundyra) — 2025-09-25T08:49:44Z
```diff
@@ -790,27 +789,29 @@ func (m *Manager) DeleteSecondPassWithoutLock(w *kueue.Workload) {
 	m.secondPassQueue.deleteByKey(workload.Key(w))
 }
 
-// QueueSecondPassIfNeeded queues for the second pass of scheduling with 1s
+// QueueSecondPassIfNeeded queues for the second pass of scheduling with exponential
 // delay.
-func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload) bool {
+func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload, iteration uint) bool {
 	if workload.NeedsSecondPass(w) {
+		delay := m.secondPassQueue.nextSecondPassDelay(iteration)
```

  The only thing this function does is doing some math calculations - it doesn't relate to the second pass queue itself. I image we could have a util function just for calculating backoff, it could also be used in different places. The function would be very similar to: https://github.com/kubernetes-sigs/kueue/blob/main/pkg/workload/workload.go#L610-L624

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-25T09:29:26Z

- **pkg/cache/queue/manager.go:796** by [@mimowo](https://github.com/mimowo) — 2025-09-25T09:29:26Z
```diff
@@ -790,27 +789,29 @@ func (m *Manager) DeleteSecondPassWithoutLock(w *kueue.Workload) {
 	m.secondPassQueue.deleteByKey(workload.Key(w))
 }
 
-// QueueSecondPassIfNeeded queues for the second pass of scheduling with 1s
+// QueueSecondPassIfNeeded queues for the second pass of scheduling with exponential
 // delay.
-func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload) bool {
+func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload, iteration uint) bool {
 	if workload.NeedsSecondPass(w) {
+		delay := m.secondPassQueue.nextSecondPassDelay(iteration)
```

  yes, I would suggest to use in this PR wait.Backoff and  similar code structure so that it is easy to commonize with the code in workload.go. In a small follow up I would suggest moving the commonized code to https://github.com/kubernetes-sigs/kueue/blob/main/pkg/util/wait/backoff.go, where actually yet another similar implementaiton is present :) 
  
  If not too much work commonizing in this PR is also ok.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-09-26T11:41:53Z

- **pkg/cache/queue/second_pass_queue.go:36** by [@mykysha](https://github.com/mykysha) — 2025-09-26T11:41:53Z
```diff
@@ -30,12 +32,15 @@ type secondPassQueue struct {
 
 	prequeued sets.Set[workload.Reference]
 	queued    map[workload.Reference]*workload.Info
+
+	backoffFactor int
```

  Moved this and added a cap as const

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-09-26T11:42:52Z

- **test/integration/singlecluster/tas/tas_test.go:2645** by [@mykysha](https://github.com/mykysha) — 2025-09-26T11:42:52Z
```diff
@@ -2552,6 +2553,141 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
 				})
 			})
+
+			ginkgo.It("resets exponential second-pass backoff when nodes become Ready", framework.SlowSpec, func() {
+				var (
+					wl1 *kueue.Workload
+				)
+
+				ginkgo.By("create workload", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0] = *testing.MakePodSet(kueue.DefaultPodSetName, 1).
+						Request(corev1.ResourceCPU, "1").
+						PreferredTopologyRequest(testing.DefaultRackTopologyLevel).
+						Image("image").
+						Obj()
+					util.MustCreate(ctx, k8sClient, wl1)
+				})
+
+				wlKey := client.ObjectKeyFromObject(wl1)
+
+				ginkgo.By("verify the workload reserves the quota and requires TAS assignment (second pass)", func() {
+					util.ExpectQuotaReservedWorkloadsTotalMetric(clusterQueue, "", 1)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+					util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, clusterQueue.Name, wl1)
+					gomega.Eventually(func(g gomega.Gomega) {
+						gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+						gomega.Expect(workload.HasTopologyAssignmentsPending(wl1)).Should(gomega.BeTrue())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				provReqKey := apitypes.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.ProvisioningRequestName(wlKey.Name, kueue.AdmissionCheckReference(ac.Name), 1),
+				}
+
+				ginkgo.By("await ProvisioningRequest creation", func() {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, provReqKey, &createdRequest)).Should(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("provision a node that is NOT Ready", func() {
+					nodes = []corev1.Node{
+						*testingnode.MakeNode("x1").
+							Label("node-group", "tas").
+							Label(testing.DefaultBlockTopologyLevel, "b1").
+							Label(testing.DefaultRackTopologyLevel, "r1").
+							Label(corev1.LabelHostname, "x1").
+							StatusAllocatable(corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+								corev1.ResourcePods:   resource.MustParse("10"),
+							}).
+							NotReady().
+							Obj(),
+					}
+					util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+				})
+
+				ginkgo.By("mark ProvisioningRequest as Provisioned (start of the backoff window)", func() {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, provReqKey, &createdRequest)).Should(gomega.Succeed())
+						apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+							Type:   autoscaling.Provisioned,
+							Status: metav1.ConditionTrue,
+							Reason: autoscaling.Provisioned,
+						})
+						g.Expect(k8sClient.Status().Update(ctx, &createdRequest)).Should(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				gomega.Consistently(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, wl1)).To(gomega.Succeed())
+					g.Expect(wl1.Status.Admission).ShouldNot(gomega.BeNil())
+					g.Expect(wl1.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
+					g.Expect(wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeNil())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+				ginkgo.By("observe at least three SecondPassFailed events while the node is NotReady (≈1s, 2s, 4s backoffs)", func() {
+					var evList corev1.EventList
+					gomega.Expect(k8sClient.List(ctx, &evList, &client.ListOptions{Namespace: ns.Name})).To(gomega.Succeed())
+					var count int32
+					for i := range evList.Items {
+						ev := evList.Items[i]
+						if ev.Reason == "SecondPassFailed" && ev.InvolvedObject.Name == wl1.Name {
+							if ev.Count > count {
+								count = ev.Count
+							}
+						}
+					}
+					gomega.Expect(count).To(gomega.BeNumerically("==", count))
```

  Added 5 as a limit to the check and made it to be >= 3

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-26T11:44:09Z

- **pkg/cache/queue/manager.go:57** by [@mimowo](https://github.com/mimowo) — 2025-09-26T11:44:09Z
```diff
@@ -51,6 +51,11 @@ var (
 	errClusterQueueAlreadyExists        = errors.New("clusterQueue already exists")
 )
 
+const (
+	backoffFactor = 2
+	backoffCap    = 30 * time.Second
+)
```

  Please move the consts to the second_pass_delay

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-09-26T11:44:30Z

- **pkg/scheduler/scheduler.go:761** by [@mykysha](https://github.com/mykysha) — 2025-09-26T11:44:30Z
```diff
@@ -758,7 +758,8 @@ func (s *Scheduler) requeueAndUpdate(ctx context.Context, e entry) {
 		e.requeueReason = qcache.RequeueReasonFailedAfterNomination
 	}
 
-	if s.queues.QueueSecondPassIfNeeded(ctx, e.Obj) {
+	e.SecondPassIteration++
```

  Moved the bump to the `QueueSecondPassIfNeeded `, however as it is the only Info-related data passed down, did not pass the entire object down. Iteration gets saved when the workload is being enqueued.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-09-26T11:49:17Z

- **test/integration/singlecluster/tas/tas_test.go:2557** by [@mykysha](https://github.com/mykysha) — 2025-09-26T11:49:17Z
```diff
@@ -2552,6 +2553,141 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
 				})
 			})
+
+			ginkgo.It("resets exponential second-pass backoff when nodes become Ready", framework.SlowSpec, func() {
```

  Tried to add a case there, where the workload would be first deleted and then readmitted, within a util.Timeout to signify that the next backoff would take 16s and it the timeout is 10s, however once per 80 or so runs 10s is not enough to readmit the workload, still investigating how to cover this in the test

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-26T11:53:31Z

- **test/integration/singlecluster/tas/tas_test.go:2582** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-26T11:53:31Z
```diff
@@ -2554,6 +2555,141 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
 				})
 			})
+
+			ginkgo.It("resets exponential second-pass backoff when nodes become Ready", framework.SlowSpec, func() {
+				var (
+					wl1 *kueue.Workload
+				)
+
+				ginkgo.By("create workload", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0] = *testing.MakePodSet(kueue.DefaultPodSetName, 1).
+						Request(corev1.ResourceCPU, "1").
+						PreferredTopologyRequest(testing.DefaultRackTopologyLevel).
+						Image("image").
+						Obj()
+					util.MustCreate(ctx, k8sClient, wl1)
+				})
+
+				wlKey := client.ObjectKeyFromObject(wl1)
+
+				ginkgo.By("verify the workload reserves the quota and requires TAS assignment (second pass)", func() {
+					util.ExpectQuotaReservedWorkloadsTotalMetric(clusterQueue, "", 1)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+					util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, clusterQueue.Name, wl1)
+					gomega.Eventually(func(g gomega.Gomega) {
+						gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
```

  ```suggestion
  						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-26T11:53:38Z

- **test/integration/singlecluster/tas/tas_test.go:2583** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-26T11:53:38Z
```diff
@@ -2554,6 +2555,141 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
 				})
 			})
+
+			ginkgo.It("resets exponential second-pass backoff when nodes become Ready", framework.SlowSpec, func() {
+				var (
+					wl1 *kueue.Workload
+				)
+
+				ginkgo.By("create workload", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0] = *testing.MakePodSet(kueue.DefaultPodSetName, 1).
+						Request(corev1.ResourceCPU, "1").
+						PreferredTopologyRequest(testing.DefaultRackTopologyLevel).
+						Image("image").
+						Obj()
+					util.MustCreate(ctx, k8sClient, wl1)
+				})
+
+				wlKey := client.ObjectKeyFromObject(wl1)
+
+				ginkgo.By("verify the workload reserves the quota and requires TAS assignment (second pass)", func() {
+					util.ExpectQuotaReservedWorkloadsTotalMetric(clusterQueue, "", 1)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+					util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, clusterQueue.Name, wl1)
+					gomega.Eventually(func(g gomega.Gomega) {
+						gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+						gomega.Expect(workload.HasTopologyAssignmentsPending(wl1)).Should(gomega.BeTrue())
```

  ```suggestion
  						g.Expect(workload.HasTopologyAssignmentsPending(wl1)).Should(gomega.BeTrue())
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-26T11:54:16Z

- **test/integration/singlecluster/tas/tas_test.go:2557** by [@mimowo](https://github.com/mimowo) — 2025-09-26T11:54:16Z
```diff
@@ -2552,6 +2553,141 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
 				})
 			})
+
+			ginkgo.It("resets exponential second-pass backoff when nodes become Ready", framework.SlowSpec, func() {
```

  I see that verifying the backoff is reset might be challenging in test. 
  
  I think as long as tested manually we should be good. the new design makes it more reliable to work properly.
  
  Still if you come up with a better way the feel free to notify and update, but it is not a blocker

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-26T11:54:18Z

- **test/integration/singlecluster/tas/tas_test.go:2571** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-26T11:54:18Z
```diff
@@ -2554,6 +2555,141 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
 				})
 			})
+
+			ginkgo.It("resets exponential second-pass backoff when nodes become Ready", framework.SlowSpec, func() {
+				var (
+					wl1 *kueue.Workload
+				)
+
+				ginkgo.By("create workload", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0] = *testing.MakePodSet(kueue.DefaultPodSetName, 1).
+						Request(corev1.ResourceCPU, "1").
+						PreferredTopologyRequest(testing.DefaultRackTopologyLevel).
+						Image("image").
+						Obj()
```

  ```suggestion
  					wl1 = testing.MakeWorkload("wl1", ns.Name).
  						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").
  						PodSets(*testing.MakePodSet(kueue.DefaultPodSetName, 1).
  							Request(corev1.ResourceCPU, "1").
  							PreferredTopologyRequest(testing.DefaultRackTopologyLevel).
  							Image("image").
  							Obj()).
  						Obj()
  ```

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-09-26T11:54:21Z

- **pkg/cache/queue/manager.go:57** by [@mykysha](https://github.com/mykysha) — 2025-09-26T11:54:21Z
```diff
@@ -51,6 +51,11 @@ var (
 	errClusterQueueAlreadyExists        = errors.New("clusterQueue already exists")
 )
 
+const (
+	backoffFactor = 2
+	backoffCap    = 30 * time.Second
+)
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-26T11:54:44Z

- **test/integration/singlecluster/tas/tas_test.go:2570** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-26T11:54:44Z
```diff
@@ -2554,6 +2555,141 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
 				})
 			})
+
+			ginkgo.It("resets exponential second-pass backoff when nodes become Ready", framework.SlowSpec, func() {
+				var (
+					wl1 *kueue.Workload
+				)
+
+				ginkgo.By("create workload", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0] = *testing.MakePodSet(kueue.DefaultPodSetName, 1).
+						Request(corev1.ResourceCPU, "1").
+						PreferredTopologyRequest(testing.DefaultRackTopologyLevel).
+						Image("image").
```

  Do we need this?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-26T12:14:06Z

- **pkg/cache/queue/manager.go:816** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-26T12:14:06Z
```diff
@@ -812,27 +811,30 @@ func (m *Manager) DeleteSecondPassWithoutLock(w *kueue.Workload) {
 	m.secondPassQueue.deleteByKey(workload.Key(w))
 }
 
-// QueueSecondPassIfNeeded queues for the second pass of scheduling with 1s
+// QueueSecondPassIfNeeded queues for the second pass of scheduling with exponential
 // delay.
-func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload) bool {
+func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload, iteration uint) bool {
```

  ```suggestion
  func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload, iteration int) bool {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-26T12:14:25Z

- **pkg/cache/queue/second_pass_queue.go:41** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-26T12:14:25Z
```diff
@@ -18,24 +18,34 @@ package queue
 
 import (
 	"sync"
+	"time"
 
 	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/apimachinery/pkg/util/wait"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
+const (
+	backoffFactor = 2
+	backoffCap    = 30 * time.Second
+)
+
 type secondPassQueue struct {
 	sync.RWMutex
 
 	prequeued sets.Set[workload.Reference]
 	queued    map[workload.Reference]*workload.Info
+
+	backoffFactor int
```

  ```suggestion
  ```

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-09-26T12:21:30Z

- **pkg/cache/queue/second_pass_queue.go:89** by [@mykysha](https://github.com/mykysha) — 2025-09-26T12:21:30Z
```diff
@@ -78,3 +83,8 @@ func (q *secondPassQueue) deleteByKey(key workload.Reference) {
 	delete(q.queued, key)
 	q.prequeued.Delete(key)
 }
+
+// nextSecondPassDelay returns the next backoff duration for the workload's second pass
+func (q *secondPassQueue) nextSecondPassDelay(iteration uint) time.Duration {
+	return time.Duration(math.Pow(float64(q.backoffFactor), float64(iteration))) * time.Second
```

  Done

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-09-26T12:22:20Z

- **test/integration/singlecluster/tas/tas_test.go:2582** by [@mykysha](https://github.com/mykysha) — 2025-09-26T12:22:20Z
```diff
@@ -2554,6 +2555,141 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
 				})
 			})
+
+			ginkgo.It("resets exponential second-pass backoff when nodes become Ready", framework.SlowSpec, func() {
+				var (
+					wl1 *kueue.Workload
+				)
+
+				ginkgo.By("create workload", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0] = *testing.MakePodSet(kueue.DefaultPodSetName, 1).
+						Request(corev1.ResourceCPU, "1").
+						PreferredTopologyRequest(testing.DefaultRackTopologyLevel).
+						Image("image").
+						Obj()
+					util.MustCreate(ctx, k8sClient, wl1)
+				})
+
+				wlKey := client.ObjectKeyFromObject(wl1)
+
+				ginkgo.By("verify the workload reserves the quota and requires TAS assignment (second pass)", func() {
+					util.ExpectQuotaReservedWorkloadsTotalMetric(clusterQueue, "", 1)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+					util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, clusterQueue.Name, wl1)
+					gomega.Eventually(func(g gomega.Gomega) {
+						gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
```

  Done

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-09-26T12:22:30Z

- **test/integration/singlecluster/tas/tas_test.go:2583** by [@mykysha](https://github.com/mykysha) — 2025-09-26T12:22:30Z
```diff
@@ -2554,6 +2555,141 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
 				})
 			})
+
+			ginkgo.It("resets exponential second-pass backoff when nodes become Ready", framework.SlowSpec, func() {
+				var (
+					wl1 *kueue.Workload
+				)
+
+				ginkgo.By("create workload", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0] = *testing.MakePodSet(kueue.DefaultPodSetName, 1).
+						Request(corev1.ResourceCPU, "1").
+						PreferredTopologyRequest(testing.DefaultRackTopologyLevel).
+						Image("image").
+						Obj()
+					util.MustCreate(ctx, k8sClient, wl1)
+				})
+
+				wlKey := client.ObjectKeyFromObject(wl1)
+
+				ginkgo.By("verify the workload reserves the quota and requires TAS assignment (second pass)", func() {
+					util.ExpectQuotaReservedWorkloadsTotalMetric(clusterQueue, "", 1)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+					util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, clusterQueue.Name, wl1)
+					gomega.Eventually(func(g gomega.Gomega) {
+						gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+						gomega.Expect(workload.HasTopologyAssignmentsPending(wl1)).Should(gomega.BeTrue())
```

  Done

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-09-26T12:22:54Z

- **test/integration/singlecluster/tas/tas_test.go:2571** by [@mykysha](https://github.com/mykysha) — 2025-09-26T12:22:54Z
```diff
@@ -2554,6 +2555,141 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
 				})
 			})
+
+			ginkgo.It("resets exponential second-pass backoff when nodes become Ready", framework.SlowSpec, func() {
+				var (
+					wl1 *kueue.Workload
+				)
+
+				ginkgo.By("create workload", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0] = *testing.MakePodSet(kueue.DefaultPodSetName, 1).
+						Request(corev1.ResourceCPU, "1").
+						PreferredTopologyRequest(testing.DefaultRackTopologyLevel).
+						Image("image").
+						Obj()
```

  Thanks, replaced with this approach

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-09-26T12:23:05Z

- **test/integration/singlecluster/tas/tas_test.go:2570** by [@mykysha](https://github.com/mykysha) — 2025-09-26T12:23:05Z
```diff
@@ -2554,6 +2555,141 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
 				})
 			})
+
+			ginkgo.It("resets exponential second-pass backoff when nodes become Ready", framework.SlowSpec, func() {
+				var (
+					wl1 *kueue.Workload
+				)
+
+				ginkgo.By("create workload", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0] = *testing.MakePodSet(kueue.DefaultPodSetName, 1).
+						Request(corev1.ResourceCPU, "1").
+						PreferredTopologyRequest(testing.DefaultRackTopologyLevel).
+						Image("image").
```

  Removed

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-09-26T12:27:59Z

- **pkg/cache/queue/manager.go:816** by [@mykysha](https://github.com/mykysha) — 2025-09-26T12:27:59Z
```diff
@@ -812,27 +811,30 @@ func (m *Manager) DeleteSecondPassWithoutLock(w *kueue.Workload) {
 	m.secondPassQueue.deleteByKey(workload.Key(w))
 }
 
-// QueueSecondPassIfNeeded queues for the second pass of scheduling with 1s
+// QueueSecondPassIfNeeded queues for the second pass of scheduling with exponential
 // delay.
-func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload) bool {
+func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload, iteration uint) bool {
```

  Changed to int everywhere

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-09-26T12:28:22Z

- **pkg/cache/queue/second_pass_queue.go:41** by [@mykysha](https://github.com/mykysha) — 2025-09-26T12:28:21Z
```diff
@@ -18,24 +18,34 @@ package queue
 
 import (
 	"sync"
+	"time"
 
 	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/apimachinery/pkg/util/wait"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
+const (
+	backoffFactor = 2
+	backoffCap    = 30 * time.Second
+)
+
 type secondPassQueue struct {
 	sync.RWMutex
 
 	prequeued sets.Set[workload.Reference]
 	queued    map[workload.Reference]*workload.Info
+
+	backoffFactor int
```

  Removed the leftovers

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (approved) — 2025-09-26T12:36:13Z

/lgtm
Thank you!

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-09-26T12:36:22Z

- **pkg/cache/queue/manager.go:796** by [@mykysha](https://github.com/mykysha) — 2025-09-26T12:36:21Z
```diff
@@ -790,27 +789,29 @@ func (m *Manager) DeleteSecondPassWithoutLock(w *kueue.Workload) {
 	m.secondPassQueue.deleteByKey(workload.Key(w))
 }
 
-// QueueSecondPassIfNeeded queues for the second pass of scheduling with 1s
+// QueueSecondPassIfNeeded queues for the second pass of scheduling with exponential
 // delay.
-func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload) bool {
+func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload, iteration uint) bool {
 	if workload.NeedsSecondPass(w) {
+		delay := m.secondPassQueue.nextSecondPassDelay(iteration)
```

  Made the backoff calculation to be like the one mentioned in the workload.go, with a goal to make a followup to move those parts to the util

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-09-26T12:39:04Z

- **pkg/workload/workload.go:193** by [@mykysha](https://github.com/mykysha) — 2025-09-26T12:39:03Z
```diff
@@ -189,6 +189,8 @@ type Info struct {
 	// LocalQueueFSUsage indicates the historical usage of resource in the LocalQueue, needed for the
 	// AdmissionFairSharing feature, it is only populated for Infos in cache.Snapshot (not in queue manager).
 	LocalQueueFSUsage *float64
+
```

  Done
