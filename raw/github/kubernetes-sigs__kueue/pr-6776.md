# PR #6776: KEP refreshAssignmentsDuringSchedulingCycle

**Summary**: KEP refreshAssignmentsDuringSchedulingCycle

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6776

**Last updated**: 2025-09-23T21:30:48Z

---

## Metadata

- **State**: closed
- **Author**: [@amy](https://github.com/amy)
- **Branch**: `overlappingKep` → `main`
- **Created**: 2025-09-09T19:51:21Z
- **Updated**: 2025-09-23T21:30:48Z
- **Merged**: —
- **Closed**: 2025-09-23T21:30:46Z
- **Labels**: `size/L`, `release-note-none`, `cncf-cla: yes`
- **Assignees**: _none_
- **Requested reviewers**: [@gabesaba](https://github.com/gabesaba), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 2   **+375 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
--> /kind feature

#### What this PR does / why we need it:
KEP proposal to refresh assignments during scheduling cycle. This makes scheduling a bit more resilient and helps mitigate issues where there's slow to terminate pods due to...
- Node health
- pod scoped issues like termination grace period or the bug in k8s where a pod gets stuck in termination when it was fetching a docker image https://github.com/kubernetes/kubernetes/issues/125050


#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:
I'm not tied to the implementation. But I think the context is useful here for us to start brainstorming solutions.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-09T19:51:27Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6776#" title="Author self-approved">amy</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [gabesaba](https://github.com/gabesaba) for approval. For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["gabesaba"]} -->

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-09-09T19:52:09Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 2725aaddeefe032ecd1be45e967fe7e48fcdf275 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68c0853cf27ec70008eb0360 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-6776--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-6776--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTY3NzYtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.B9u5JbPJrtTh2gWF4EJoX7EwYXe8qH2ANDt8hybMEQs)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-6776--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-09T20:02:25Z

@amy: The following test **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:

Test name | Commit | Details | Required | Rerun command
--- | --- | --- | --- | ---
pull-kueue-verify-main | 2725aaddeefe032ecd1be45e967fe7e48fcdf275 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/6776/pull-kueue-verify-main/1965503321166843904) | true | `/test pull-kueue-verify-main`

[Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=6776). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3Aamy). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository. I understand the commands that are listed [here](https://go.k8s.io/bot-commands).
</details>
<!-- test report -->

### Comment by [@amy](https://github.com/amy) — 2025-09-23T21:30:42Z

/close

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-23T21:30:48Z

@amy: Closed this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6776#issuecomment-3325619577):

>/close 


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-10T07:07:16Z

Left the first high-level review pass.

- **keps/4378-refresh-assignments-during-scheduling-cycle/README.md:125** by [@mimowo](https://github.com/mimowo) — 2025-09-10T06:51:23Z
```diff
@@ -0,0 +1,346 @@
+# KEP-4378: Refresh Assignments During Scheduling Cycle
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [KEP-4378: Refresh Assignments During Scheduling Cycle](#kep-4378-refresh-assignments-during-scheduling-cycle)
+  - [Summary](#summary)
+  - [Understanding the Scheduling problem today](#understanding-the-scheduling-problem-today)
+    - [Simplification of Kueue Scheduling today](#simplification-of-kueue-scheduling-today)
+    - [Pods slow to terminate](#pods-slow-to-terminate)
+    - [Scheduling slowness horizontally](#scheduling-slowness-horizontally)
+  - [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+  - [Proposal](#proposal)
+    - [Exclude Overlapping Preemption Targets](#exclude-overlapping-preemption-targets)
+    - [Issues with this approach](#issues-with-this-approach)
+    - [Allow Multiple Preemptors to claim same Preemption target](#allow-multiple-preemptors-to-claim-same-preemption-target)
+    - [User Stories (Optional)](#user-stories-optional)
+    - [Risks and Mitigations](#risks-and-mitigations)
+  - [Design Details](#design-details)
+    - [Test Plan](#test-plan)
+        - [Prerequisite testing updates](#prerequisite-testing-updates)
+      - [Unit Tests](#unit-tests)
+      - [Integration tests](#integration-tests)
+    - [Graduation Criteria](#graduation-criteria)
+  - [Implementation History](#implementation-history)
+  - [Drawbacks](#drawbacks)
+  - [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+In specific scenarios, Kueue scheduling is slow across 2 dimensions. Horizontally across clusterQueue heads and vertically
+down a single clusterQueue. The impact of this slowness results means workloads are not able to reclaim their nominalQuota (ie guarantees)
+within an acceptable period of time, ultimately resulting in what we consider a "guarantee violation".
+
+This KEP is a proposal to address scheduling issues in the horizontal dimension across CQ heads.
+
+## Understanding the Scheduling problem today
+
+### Simplification of Kueue Scheduling today
+
+1. nomination: For the admissible clusterqueue head workloads, assign preemption targets. This produces admission candidates.
+   - Preemption target assignment: actively terminating workloads are prioritized as preemption targets
+2. order admission candidates.
+3. evaluate admissibility of admission candidate. If the admission candidate has overlapping preemption targets, skip this workload.
+   - Ex: Preemptor A has preemption targets 1, 2. Preemptor B has preemption targets 2, 3. Preemptor B will be skipped in this `schedule()` invocation.
+4. If the admission candidate is admissible and its preemption target has not completed preemption, requeue admission candidate. 
+   (ie CQ heads will block its own CQ until it admits)
+
+### Pods slow to terminate
+
+Day to day, this algorithm is pretty effective. Where it causes scheduling slowness, is when the pod is slow to terminate for any reason. 
+There's 2 categories of reasons so far:
+
+1. Node health: we call these zombie pods where the pod gets stuck in termination
+2. Pod scoped:
+   - pod termination grace period
+   - pod gets stuck if it was pulling a docker image at time of termination
+
+### Scheduling slowness horizontally
+
+(While slowly terminating pods causes scheduling slowness vertically down 1 clusterQueue, that is out of scope for this document.)
+When there's pods slow to terminate, this causes scheduling slowness horizontally across clusterQueues. This is because:
+
+1. admission candidates (preemptor) will first prioritize workloads already marked for preemption to be selected as a preemption target.
+   - See: `CandidatesOrdering` function: `// 0. Workloads already marked for preemption first.`
+2. All CQ heads that select this slowly terminating pod as a preemption target will be skipped for the current `schedule()` invocation
+3. Because CQ heads are blocking, the skipped CQ head will continue to block admission for the rest of the CQ
+
+So a slowly terminating pod or pod stuck in termination can clog up scheduling across clusterQueues. 
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Isolate scheduling algorithm inefficiencies to 1 CQ instead of across CQs
```

  I'm not sure what it actually means.

- **keps/4378-refresh-assignments-during-scheduling-cycle/README.md:182** by [@mimowo](https://github.com/mimowo) — 2025-09-10T06:52:32Z
```diff
@@ -0,0 +1,346 @@
+# KEP-4378: Refresh Assignments During Scheduling Cycle
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [KEP-4378: Refresh Assignments During Scheduling Cycle](#kep-4378-refresh-assignments-during-scheduling-cycle)
+  - [Summary](#summary)
+  - [Understanding the Scheduling problem today](#understanding-the-scheduling-problem-today)
+    - [Simplification of Kueue Scheduling today](#simplification-of-kueue-scheduling-today)
+    - [Pods slow to terminate](#pods-slow-to-terminate)
+    - [Scheduling slowness horizontally](#scheduling-slowness-horizontally)
+  - [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+  - [Proposal](#proposal)
+    - [Exclude Overlapping Preemption Targets](#exclude-overlapping-preemption-targets)
+    - [Issues with this approach](#issues-with-this-approach)
+    - [Allow Multiple Preemptors to claim same Preemption target](#allow-multiple-preemptors-to-claim-same-preemption-target)
+    - [User Stories (Optional)](#user-stories-optional)
+    - [Risks and Mitigations](#risks-and-mitigations)
+  - [Design Details](#design-details)
+    - [Test Plan](#test-plan)
+        - [Prerequisite testing updates](#prerequisite-testing-updates)
+      - [Unit Tests](#unit-tests)
+      - [Integration tests](#integration-tests)
+    - [Graduation Criteria](#graduation-criteria)
+  - [Implementation History](#implementation-history)
+  - [Drawbacks](#drawbacks)
+  - [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+In specific scenarios, Kueue scheduling is slow across 2 dimensions. Horizontally across clusterQueue heads and vertically
+down a single clusterQueue. The impact of this slowness results means workloads are not able to reclaim their nominalQuota (ie guarantees)
+within an acceptable period of time, ultimately resulting in what we consider a "guarantee violation".
+
+This KEP is a proposal to address scheduling issues in the horizontal dimension across CQ heads.
+
+## Understanding the Scheduling problem today
+
+### Simplification of Kueue Scheduling today
+
+1. nomination: For the admissible clusterqueue head workloads, assign preemption targets. This produces admission candidates.
+   - Preemption target assignment: actively terminating workloads are prioritized as preemption targets
+2. order admission candidates.
+3. evaluate admissibility of admission candidate. If the admission candidate has overlapping preemption targets, skip this workload.
+   - Ex: Preemptor A has preemption targets 1, 2. Preemptor B has preemption targets 2, 3. Preemptor B will be skipped in this `schedule()` invocation.
+4. If the admission candidate is admissible and its preemption target has not completed preemption, requeue admission candidate. 
+   (ie CQ heads will block its own CQ until it admits)
+
+### Pods slow to terminate
+
+Day to day, this algorithm is pretty effective. Where it causes scheduling slowness, is when the pod is slow to terminate for any reason. 
+There's 2 categories of reasons so far:
+
+1. Node health: we call these zombie pods where the pod gets stuck in termination
+2. Pod scoped:
+   - pod termination grace period
+   - pod gets stuck if it was pulling a docker image at time of termination
+
+### Scheduling slowness horizontally
+
+(While slowly terminating pods causes scheduling slowness vertically down 1 clusterQueue, that is out of scope for this document.)
+When there's pods slow to terminate, this causes scheduling slowness horizontally across clusterQueues. This is because:
+
+1. admission candidates (preemptor) will first prioritize workloads already marked for preemption to be selected as a preemption target.
+   - See: `CandidatesOrdering` function: `// 0. Workloads already marked for preemption first.`
+2. All CQ heads that select this slowly terminating pod as a preemption target will be skipped for the current `schedule()` invocation
+3. Because CQ heads are blocking, the skipped CQ head will continue to block admission for the rest of the CQ
+
+So a slowly terminating pod or pod stuck in termination can clog up scheduling across clusterQueues. 
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Isolate scheduling algorithm inefficiencies to 1 CQ instead of across CQs
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+- Fix vertical scheduling algorithm inefficiency where preemption is slow and serialized down 1 CQ
+  if grace period termination is set
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+This KEP proposes to recalculate preemption targets when it detects overlapping preemption targets. 
+Due to how Kueue's scheduling algorithm works, this KEP also proposes to allow multiple preemptors to claim
+the same preemption target. Below is how we've come to that conclusion.
+
+### Exclude Overlapping Preemption Targets
+
+Naively, you could recalculate preemption targets and exclude workloads already selected as preemption targets. For this approach...
+
+- The preemptor's usage is added regardless of preemption target completion
+- As a result, you need to remove usage of preemption targets from the snapshot to properly account for resources being freed during the scheduling cycle. 
+  This is essential for RefreshAssignmentsDuringSchedulingCycle to work correctly. Without this, workloadFits() will fail on assignment retry because we add 
+  the admission candidate's usage in `AddUsage()` without removing the usage from the preemption target.
+
+### Issues with this approach
+
+This would introduce the following bug:
+
+- Preemptor 1 (1GPU) preempts a 2GPU target. The preemptor's assignment mode is correctly set as `Preempt`.
+- Preemptor 2 (2GPU) would be incorrectly admitted with assignment mode set incorrectly to `Fit`. 
+
+### Allow Multiple Preemptors to claim same Preemption target
+
+Instead, allow preemptors to claim only the usage they need, so that the preemption target could be attributable to more than 1 preemptor. This would allow:
+
+1. preemption target recalculation
+2. correctly set the preemptor's assignment mode to `Preempt` so that it requeues until the preemption target completes preemption
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+See [Pods slow to terminate](#pods-slow-to-terminate)
```

  Please add user stories specifically focused on the problems addressed in this KEP.

- **keps/4378-refresh-assignments-during-scheduling-cycle/README.md:93** by [@mimowo](https://github.com/mimowo) — 2025-09-10T06:58:33Z
```diff
@@ -0,0 +1,346 @@
+# KEP-4378: Refresh Assignments During Scheduling Cycle
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [KEP-4378: Refresh Assignments During Scheduling Cycle](#kep-4378-refresh-assignments-during-scheduling-cycle)
+  - [Summary](#summary)
+  - [Understanding the Scheduling problem today](#understanding-the-scheduling-problem-today)
+    - [Simplification of Kueue Scheduling today](#simplification-of-kueue-scheduling-today)
+    - [Pods slow to terminate](#pods-slow-to-terminate)
+    - [Scheduling slowness horizontally](#scheduling-slowness-horizontally)
+  - [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+  - [Proposal](#proposal)
+    - [Exclude Overlapping Preemption Targets](#exclude-overlapping-preemption-targets)
+    - [Issues with this approach](#issues-with-this-approach)
+    - [Allow Multiple Preemptors to claim same Preemption target](#allow-multiple-preemptors-to-claim-same-preemption-target)
+    - [User Stories (Optional)](#user-stories-optional)
+    - [Risks and Mitigations](#risks-and-mitigations)
+  - [Design Details](#design-details)
+    - [Test Plan](#test-plan)
+        - [Prerequisite testing updates](#prerequisite-testing-updates)
+      - [Unit Tests](#unit-tests)
+      - [Integration tests](#integration-tests)
+    - [Graduation Criteria](#graduation-criteria)
+  - [Implementation History](#implementation-history)
+  - [Drawbacks](#drawbacks)
+  - [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+In specific scenarios, Kueue scheduling is slow across 2 dimensions. Horizontally across clusterQueue heads and vertically
+down a single clusterQueue. The impact of this slowness results means workloads are not able to reclaim their nominalQuota (ie guarantees)
+within an acceptable period of time, ultimately resulting in what we consider a "guarantee violation".
+
+This KEP is a proposal to address scheduling issues in the horizontal dimension across CQ heads.
+
+## Understanding the Scheduling problem today
+
+### Simplification of Kueue Scheduling today
+
+1. nomination: For the admissible clusterqueue head workloads, assign preemption targets. This produces admission candidates.
+   - Preemption target assignment: actively terminating workloads are prioritized as preemption targets
+2. order admission candidates.
+3. evaluate admissibility of admission candidate. If the admission candidate has overlapping preemption targets, skip this workload.
+   - Ex: Preemptor A has preemption targets 1, 2. Preemptor B has preemption targets 2, 3. Preemptor B will be skipped in this `schedule()` invocation.
+4. If the admission candidate is admissible and its preemption target has not completed preemption, requeue admission candidate. 
+   (ie CQ heads will block its own CQ until it admits)
+
+### Pods slow to terminate
+
+Day to day, this algorithm is pretty effective. Where it causes scheduling slowness, is when the pod is slow to terminate for any reason. 
+There's 2 categories of reasons so far:
+
+1. Node health: we call these zombie pods where the pod gets stuck in termination
+2. Pod scoped:
+   - pod termination grace period
+   - pod gets stuck if it was pulling a docker image at time of termination
```

  I believe all the issues here could be mitigated by a small controller in Kueue which just transitions the Pods to Failed phase, as proposed in https://github.com/kubernetes-sigs/kueue/issues/6757. 
  
  I believe we should consider it as an alternative (for now preferred by me). 
  
  If we need to modify the scheduling algorithm itself it will be good to provide some User stories going beyond that category of problems.

- **keps/4378-refresh-assignments-during-scheduling-cycle/README.md:164** by [@mimowo](https://github.com/mimowo) — 2025-09-10T07:00:22Z
```diff
@@ -0,0 +1,346 @@
+# KEP-4378: Refresh Assignments During Scheduling Cycle
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [KEP-4378: Refresh Assignments During Scheduling Cycle](#kep-4378-refresh-assignments-during-scheduling-cycle)
+  - [Summary](#summary)
+  - [Understanding the Scheduling problem today](#understanding-the-scheduling-problem-today)
+    - [Simplification of Kueue Scheduling today](#simplification-of-kueue-scheduling-today)
+    - [Pods slow to terminate](#pods-slow-to-terminate)
+    - [Scheduling slowness horizontally](#scheduling-slowness-horizontally)
+  - [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+  - [Proposal](#proposal)
+    - [Exclude Overlapping Preemption Targets](#exclude-overlapping-preemption-targets)
+    - [Issues with this approach](#issues-with-this-approach)
+    - [Allow Multiple Preemptors to claim same Preemption target](#allow-multiple-preemptors-to-claim-same-preemption-target)
+    - [User Stories (Optional)](#user-stories-optional)
+    - [Risks and Mitigations](#risks-and-mitigations)
+  - [Design Details](#design-details)
+    - [Test Plan](#test-plan)
+        - [Prerequisite testing updates](#prerequisite-testing-updates)
+      - [Unit Tests](#unit-tests)
+      - [Integration tests](#integration-tests)
+    - [Graduation Criteria](#graduation-criteria)
+  - [Implementation History](#implementation-history)
+  - [Drawbacks](#drawbacks)
+  - [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+In specific scenarios, Kueue scheduling is slow across 2 dimensions. Horizontally across clusterQueue heads and vertically
+down a single clusterQueue. The impact of this slowness results means workloads are not able to reclaim their nominalQuota (ie guarantees)
+within an acceptable period of time, ultimately resulting in what we consider a "guarantee violation".
+
+This KEP is a proposal to address scheduling issues in the horizontal dimension across CQ heads.
+
+## Understanding the Scheduling problem today
+
+### Simplification of Kueue Scheduling today
+
+1. nomination: For the admissible clusterqueue head workloads, assign preemption targets. This produces admission candidates.
+   - Preemption target assignment: actively terminating workloads are prioritized as preemption targets
+2. order admission candidates.
+3. evaluate admissibility of admission candidate. If the admission candidate has overlapping preemption targets, skip this workload.
+   - Ex: Preemptor A has preemption targets 1, 2. Preemptor B has preemption targets 2, 3. Preemptor B will be skipped in this `schedule()` invocation.
+4. If the admission candidate is admissible and its preemption target has not completed preemption, requeue admission candidate. 
+   (ie CQ heads will block its own CQ until it admits)
+
+### Pods slow to terminate
+
+Day to day, this algorithm is pretty effective. Where it causes scheduling slowness, is when the pod is slow to terminate for any reason. 
+There's 2 categories of reasons so far:
+
+1. Node health: we call these zombie pods where the pod gets stuck in termination
+2. Pod scoped:
+   - pod termination grace period
+   - pod gets stuck if it was pulling a docker image at time of termination
+
+### Scheduling slowness horizontally
+
+(While slowly terminating pods causes scheduling slowness vertically down 1 clusterQueue, that is out of scope for this document.)
+When there's pods slow to terminate, this causes scheduling slowness horizontally across clusterQueues. This is because:
+
+1. admission candidates (preemptor) will first prioritize workloads already marked for preemption to be selected as a preemption target.
+   - See: `CandidatesOrdering` function: `// 0. Workloads already marked for preemption first.`
+2. All CQ heads that select this slowly terminating pod as a preemption target will be skipped for the current `schedule()` invocation
+3. Because CQ heads are blocking, the skipped CQ head will continue to block admission for the rest of the CQ
+
+So a slowly terminating pod or pod stuck in termination can clog up scheduling across clusterQueues. 
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Isolate scheduling algorithm inefficiencies to 1 CQ instead of across CQs
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+- Fix vertical scheduling algorithm inefficiency where preemption is slow and serialized down 1 CQ
+  if grace period termination is set
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+This KEP proposes to recalculate preemption targets when it detects overlapping preemption targets. 
+Due to how Kueue's scheduling algorithm works, this KEP also proposes to allow multiple preemptors to claim
+the same preemption target. Below is how we've come to that conclusion.
+
+### Exclude Overlapping Preemption Targets
+
+Naively, you could recalculate preemption targets and exclude workloads already selected as preemption targets. For this approach...
+
+- The preemptor's usage is added regardless of preemption target completion
+- As a result, you need to remove usage of preemption targets from the snapshot to properly account for resources being freed during the scheduling cycle. 
+  This is essential for RefreshAssignmentsDuringSchedulingCycle to work correctly. Without this, workloadFits() will fail on assignment retry because we add 
+  the admission candidate's usage in `AddUsage()` without removing the usage from the preemption target.
+
+### Issues with this approach
+
+This would introduce the following bug:
+
+- Preemptor 1 (1GPU) preempts a 2GPU target. The preemptor's assignment mode is correctly set as `Preempt`.
+- Preemptor 2 (2GPU) would be incorrectly admitted with assignment mode set incorrectly to `Fit`. 
```

  Actually, I don't understand the scenario. Could you maybe detail the case a bit more? 
  
  Also, why is "admitted with assignment mode set incorrectly to `Fit`. " is a problem? The mode is just an internal thing in Kueue, nothing user-facing, so as long as the user-facing behavior is correct I would be ok.

- **keps/4378-refresh-assignments-during-scheduling-cycle/README.md:214** by [@mimowo](https://github.com/mimowo) — 2025-09-10T07:06:55Z
```diff
@@ -0,0 +1,346 @@
+# KEP-4378: Refresh Assignments During Scheduling Cycle
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [KEP-4378: Refresh Assignments During Scheduling Cycle](#kep-4378-refresh-assignments-during-scheduling-cycle)
+  - [Summary](#summary)
+  - [Understanding the Scheduling problem today](#understanding-the-scheduling-problem-today)
+    - [Simplification of Kueue Scheduling today](#simplification-of-kueue-scheduling-today)
+    - [Pods slow to terminate](#pods-slow-to-terminate)
+    - [Scheduling slowness horizontally](#scheduling-slowness-horizontally)
+  - [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+  - [Proposal](#proposal)
+    - [Exclude Overlapping Preemption Targets](#exclude-overlapping-preemption-targets)
+    - [Issues with this approach](#issues-with-this-approach)
+    - [Allow Multiple Preemptors to claim same Preemption target](#allow-multiple-preemptors-to-claim-same-preemption-target)
+    - [User Stories (Optional)](#user-stories-optional)
+    - [Risks and Mitigations](#risks-and-mitigations)
+  - [Design Details](#design-details)
+    - [Test Plan](#test-plan)
+        - [Prerequisite testing updates](#prerequisite-testing-updates)
+      - [Unit Tests](#unit-tests)
+      - [Integration tests](#integration-tests)
+    - [Graduation Criteria](#graduation-criteria)
+  - [Implementation History](#implementation-history)
+  - [Drawbacks](#drawbacks)
+  - [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+In specific scenarios, Kueue scheduling is slow across 2 dimensions. Horizontally across clusterQueue heads and vertically
+down a single clusterQueue. The impact of this slowness results means workloads are not able to reclaim their nominalQuota (ie guarantees)
+within an acceptable period of time, ultimately resulting in what we consider a "guarantee violation".
+
+This KEP is a proposal to address scheduling issues in the horizontal dimension across CQ heads.
+
+## Understanding the Scheduling problem today
+
+### Simplification of Kueue Scheduling today
+
+1. nomination: For the admissible clusterqueue head workloads, assign preemption targets. This produces admission candidates.
+   - Preemption target assignment: actively terminating workloads are prioritized as preemption targets
+2. order admission candidates.
+3. evaluate admissibility of admission candidate. If the admission candidate has overlapping preemption targets, skip this workload.
+   - Ex: Preemptor A has preemption targets 1, 2. Preemptor B has preemption targets 2, 3. Preemptor B will be skipped in this `schedule()` invocation.
+4. If the admission candidate is admissible and its preemption target has not completed preemption, requeue admission candidate. 
+   (ie CQ heads will block its own CQ until it admits)
+
+### Pods slow to terminate
+
+Day to day, this algorithm is pretty effective. Where it causes scheduling slowness, is when the pod is slow to terminate for any reason. 
+There's 2 categories of reasons so far:
+
+1. Node health: we call these zombie pods where the pod gets stuck in termination
+2. Pod scoped:
+   - pod termination grace period
+   - pod gets stuck if it was pulling a docker image at time of termination
+
+### Scheduling slowness horizontally
+
+(While slowly terminating pods causes scheduling slowness vertically down 1 clusterQueue, that is out of scope for this document.)
+When there's pods slow to terminate, this causes scheduling slowness horizontally across clusterQueues. This is because:
+
+1. admission candidates (preemptor) will first prioritize workloads already marked for preemption to be selected as a preemption target.
+   - See: `CandidatesOrdering` function: `// 0. Workloads already marked for preemption first.`
+2. All CQ heads that select this slowly terminating pod as a preemption target will be skipped for the current `schedule()` invocation
+3. Because CQ heads are blocking, the skipped CQ head will continue to block admission for the rest of the CQ
+
+So a slowly terminating pod or pod stuck in termination can clog up scheduling across clusterQueues. 
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Isolate scheduling algorithm inefficiencies to 1 CQ instead of across CQs
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+- Fix vertical scheduling algorithm inefficiency where preemption is slow and serialized down 1 CQ
+  if grace period termination is set
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+This KEP proposes to recalculate preemption targets when it detects overlapping preemption targets. 
+Due to how Kueue's scheduling algorithm works, this KEP also proposes to allow multiple preemptors to claim
+the same preemption target. Below is how we've come to that conclusion.
+
+### Exclude Overlapping Preemption Targets
+
+Naively, you could recalculate preemption targets and exclude workloads already selected as preemption targets. For this approach...
+
+- The preemptor's usage is added regardless of preemption target completion
+- As a result, you need to remove usage of preemption targets from the snapshot to properly account for resources being freed during the scheduling cycle. 
+  This is essential for RefreshAssignmentsDuringSchedulingCycle to work correctly. Without this, workloadFits() will fail on assignment retry because we add 
+  the admission candidate's usage in `AddUsage()` without removing the usage from the preemption target.
+
+### Issues with this approach
+
+This would introduce the following bug:
+
+- Preemptor 1 (1GPU) preempts a 2GPU target. The preemptor's assignment mode is correctly set as `Preempt`.
+- Preemptor 2 (2GPU) would be incorrectly admitted with assignment mode set incorrectly to `Fit`. 
+
+### Allow Multiple Preemptors to claim same Preemption target
+
+Instead, allow preemptors to claim only the usage they need, so that the preemption target could be attributable to more than 1 preemptor. This would allow:
+
+1. preemption target recalculation
+2. correctly set the preemptor's assignment mode to `Preempt` so that it requeues until the preemption target completes preemption
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+See [Pods slow to terminate](#pods-slow-to-terminate)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+In `preempted_workloads.go` create a new PreemptedWorkloads type:
```

  This seems like a significant complication. Also, recomputing the preemption targets may increase computational complexity even more.
  
  I would like to take a step back and understand:
  1. does it cover some use-cases which are not handled by terminating the Pods?
  2. what is the issue with just allowing for preemption of multiple workloads with overlapping targets, but without recomputing the targets.

### Review by [@amy](https://github.com/amy) (commented) — 2025-09-10T14:21:32Z

- **keps/4378-refresh-assignments-during-scheduling-cycle/README.md:125** by [@amy](https://github.com/amy) — 2025-09-10T14:21:32Z
```diff
@@ -0,0 +1,346 @@
+# KEP-4378: Refresh Assignments During Scheduling Cycle
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [KEP-4378: Refresh Assignments During Scheduling Cycle](#kep-4378-refresh-assignments-during-scheduling-cycle)
+  - [Summary](#summary)
+  - [Understanding the Scheduling problem today](#understanding-the-scheduling-problem-today)
+    - [Simplification of Kueue Scheduling today](#simplification-of-kueue-scheduling-today)
+    - [Pods slow to terminate](#pods-slow-to-terminate)
+    - [Scheduling slowness horizontally](#scheduling-slowness-horizontally)
+  - [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+  - [Proposal](#proposal)
+    - [Exclude Overlapping Preemption Targets](#exclude-overlapping-preemption-targets)
+    - [Issues with this approach](#issues-with-this-approach)
+    - [Allow Multiple Preemptors to claim same Preemption target](#allow-multiple-preemptors-to-claim-same-preemption-target)
+    - [User Stories (Optional)](#user-stories-optional)
+    - [Risks and Mitigations](#risks-and-mitigations)
+  - [Design Details](#design-details)
+    - [Test Plan](#test-plan)
+        - [Prerequisite testing updates](#prerequisite-testing-updates)
+      - [Unit Tests](#unit-tests)
+      - [Integration tests](#integration-tests)
+    - [Graduation Criteria](#graduation-criteria)
+  - [Implementation History](#implementation-history)
+  - [Drawbacks](#drawbacks)
+  - [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+In specific scenarios, Kueue scheduling is slow across 2 dimensions. Horizontally across clusterQueue heads and vertically
+down a single clusterQueue. The impact of this slowness results means workloads are not able to reclaim their nominalQuota (ie guarantees)
+within an acceptable period of time, ultimately resulting in what we consider a "guarantee violation".
+
+This KEP is a proposal to address scheduling issues in the horizontal dimension across CQ heads.
+
+## Understanding the Scheduling problem today
+
+### Simplification of Kueue Scheduling today
+
+1. nomination: For the admissible clusterqueue head workloads, assign preemption targets. This produces admission candidates.
+   - Preemption target assignment: actively terminating workloads are prioritized as preemption targets
+2. order admission candidates.
+3. evaluate admissibility of admission candidate. If the admission candidate has overlapping preemption targets, skip this workload.
+   - Ex: Preemptor A has preemption targets 1, 2. Preemptor B has preemption targets 2, 3. Preemptor B will be skipped in this `schedule()` invocation.
+4. If the admission candidate is admissible and its preemption target has not completed preemption, requeue admission candidate. 
+   (ie CQ heads will block its own CQ until it admits)
+
+### Pods slow to terminate
+
+Day to day, this algorithm is pretty effective. Where it causes scheduling slowness, is when the pod is slow to terminate for any reason. 
+There's 2 categories of reasons so far:
+
+1. Node health: we call these zombie pods where the pod gets stuck in termination
+2. Pod scoped:
+   - pod termination grace period
+   - pod gets stuck if it was pulling a docker image at time of termination
+
+### Scheduling slowness horizontally
+
+(While slowly terminating pods causes scheduling slowness vertically down 1 clusterQueue, that is out of scope for this document.)
+When there's pods slow to terminate, this causes scheduling slowness horizontally across clusterQueues. This is because:
+
+1. admission candidates (preemptor) will first prioritize workloads already marked for preemption to be selected as a preemption target.
+   - See: `CandidatesOrdering` function: `// 0. Workloads already marked for preemption first.`
+2. All CQ heads that select this slowly terminating pod as a preemption target will be skipped for the current `schedule()` invocation
+3. Because CQ heads are blocking, the skipped CQ head will continue to block admission for the rest of the CQ
+
+So a slowly terminating pod or pod stuck in termination can clog up scheduling across clusterQueues. 
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Isolate scheduling algorithm inefficiencies to 1 CQ instead of across CQs
```

  So there's 2 dimensions to scheduling issue:
  1. Overlapping preemption problem where 1 indefinitely terminating pod can slow down scheduling across CQ heads. (ie the log where it skips preemption target due to slowly terminating pod)
  2. Serialized preemption problem. So lets say you want to admit a large batch of workloads in a CQ, all reclaiming guarantees, who all need preemptions. This is a scheduling algorithm issue vertically down a CQ bc each pod workload has a 15min grace period termination. So its 15min at worst between each admission despite nominalQuota reclamation. <- this is out of scope for this PR. I was planning on tackling it after the horizontal scheduling issue was resolved.

### Review by [@amy](https://github.com/amy) (commented) — 2025-09-10T14:23:27Z

- **keps/4378-refresh-assignments-during-scheduling-cycle/README.md:164** by [@amy](https://github.com/amy) — 2025-09-10T14:23:27Z
```diff
@@ -0,0 +1,346 @@
+# KEP-4378: Refresh Assignments During Scheduling Cycle
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [KEP-4378: Refresh Assignments During Scheduling Cycle](#kep-4378-refresh-assignments-during-scheduling-cycle)
+  - [Summary](#summary)
+  - [Understanding the Scheduling problem today](#understanding-the-scheduling-problem-today)
+    - [Simplification of Kueue Scheduling today](#simplification-of-kueue-scheduling-today)
+    - [Pods slow to terminate](#pods-slow-to-terminate)
+    - [Scheduling slowness horizontally](#scheduling-slowness-horizontally)
+  - [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+  - [Proposal](#proposal)
+    - [Exclude Overlapping Preemption Targets](#exclude-overlapping-preemption-targets)
+    - [Issues with this approach](#issues-with-this-approach)
+    - [Allow Multiple Preemptors to claim same Preemption target](#allow-multiple-preemptors-to-claim-same-preemption-target)
+    - [User Stories (Optional)](#user-stories-optional)
+    - [Risks and Mitigations](#risks-and-mitigations)
+  - [Design Details](#design-details)
+    - [Test Plan](#test-plan)
+        - [Prerequisite testing updates](#prerequisite-testing-updates)
+      - [Unit Tests](#unit-tests)
+      - [Integration tests](#integration-tests)
+    - [Graduation Criteria](#graduation-criteria)
+  - [Implementation History](#implementation-history)
+  - [Drawbacks](#drawbacks)
+  - [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+In specific scenarios, Kueue scheduling is slow across 2 dimensions. Horizontally across clusterQueue heads and vertically
+down a single clusterQueue. The impact of this slowness results means workloads are not able to reclaim their nominalQuota (ie guarantees)
+within an acceptable period of time, ultimately resulting in what we consider a "guarantee violation".
+
+This KEP is a proposal to address scheduling issues in the horizontal dimension across CQ heads.
+
+## Understanding the Scheduling problem today
+
+### Simplification of Kueue Scheduling today
+
+1. nomination: For the admissible clusterqueue head workloads, assign preemption targets. This produces admission candidates.
+   - Preemption target assignment: actively terminating workloads are prioritized as preemption targets
+2. order admission candidates.
+3. evaluate admissibility of admission candidate. If the admission candidate has overlapping preemption targets, skip this workload.
+   - Ex: Preemptor A has preemption targets 1, 2. Preemptor B has preemption targets 2, 3. Preemptor B will be skipped in this `schedule()` invocation.
+4. If the admission candidate is admissible and its preemption target has not completed preemption, requeue admission candidate. 
+   (ie CQ heads will block its own CQ until it admits)
+
+### Pods slow to terminate
+
+Day to day, this algorithm is pretty effective. Where it causes scheduling slowness, is when the pod is slow to terminate for any reason. 
+There's 2 categories of reasons so far:
+
+1. Node health: we call these zombie pods where the pod gets stuck in termination
+2. Pod scoped:
+   - pod termination grace period
+   - pod gets stuck if it was pulling a docker image at time of termination
+
+### Scheduling slowness horizontally
+
+(While slowly terminating pods causes scheduling slowness vertically down 1 clusterQueue, that is out of scope for this document.)
+When there's pods slow to terminate, this causes scheduling slowness horizontally across clusterQueues. This is because:
+
+1. admission candidates (preemptor) will first prioritize workloads already marked for preemption to be selected as a preemption target.
+   - See: `CandidatesOrdering` function: `// 0. Workloads already marked for preemption first.`
+2. All CQ heads that select this slowly terminating pod as a preemption target will be skipped for the current `schedule()` invocation
+3. Because CQ heads are blocking, the skipped CQ head will continue to block admission for the rest of the CQ
+
+So a slowly terminating pod or pod stuck in termination can clog up scheduling across clusterQueues. 
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Isolate scheduling algorithm inefficiencies to 1 CQ instead of across CQs
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+- Fix vertical scheduling algorithm inefficiency where preemption is slow and serialized down 1 CQ
+  if grace period termination is set
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+This KEP proposes to recalculate preemption targets when it detects overlapping preemption targets. 
+Due to how Kueue's scheduling algorithm works, this KEP also proposes to allow multiple preemptors to claim
+the same preemption target. Below is how we've come to that conclusion.
+
+### Exclude Overlapping Preemption Targets
+
+Naively, you could recalculate preemption targets and exclude workloads already selected as preemption targets. For this approach...
+
+- The preemptor's usage is added regardless of preemption target completion
+- As a result, you need to remove usage of preemption targets from the snapshot to properly account for resources being freed during the scheduling cycle. 
+  This is essential for RefreshAssignmentsDuringSchedulingCycle to work correctly. Without this, workloadFits() will fail on assignment retry because we add 
+  the admission candidate's usage in `AddUsage()` without removing the usage from the preemption target.
+
+### Issues with this approach
+
+This would introduce the following bug:
+
+- Preemptor 1 (1GPU) preempts a 2GPU target. The preemptor's assignment mode is correctly set as `Preempt`.
+- Preemptor 2 (2GPU) would be incorrectly admitted with assignment mode set incorrectly to `Fit`. 
```

  Fit mode is a problem. Because based off the algorithm, you want to block until the preemption target completes preemption when using `Preempt` mode. The second preemptor would just immediately admit in `Fit` mode instead of requeue until the target completes preemption.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-10T15:52:03Z

- **keps/4378-refresh-assignments-during-scheduling-cycle/README.md:125** by [@mimowo](https://github.com/mimowo) — 2025-09-10T15:52:03Z
```diff
@@ -0,0 +1,346 @@
+# KEP-4378: Refresh Assignments During Scheduling Cycle
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [KEP-4378: Refresh Assignments During Scheduling Cycle](#kep-4378-refresh-assignments-during-scheduling-cycle)
+  - [Summary](#summary)
+  - [Understanding the Scheduling problem today](#understanding-the-scheduling-problem-today)
+    - [Simplification of Kueue Scheduling today](#simplification-of-kueue-scheduling-today)
+    - [Pods slow to terminate](#pods-slow-to-terminate)
+    - [Scheduling slowness horizontally](#scheduling-slowness-horizontally)
+  - [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+  - [Proposal](#proposal)
+    - [Exclude Overlapping Preemption Targets](#exclude-overlapping-preemption-targets)
+    - [Issues with this approach](#issues-with-this-approach)
+    - [Allow Multiple Preemptors to claim same Preemption target](#allow-multiple-preemptors-to-claim-same-preemption-target)
+    - [User Stories (Optional)](#user-stories-optional)
+    - [Risks and Mitigations](#risks-and-mitigations)
+  - [Design Details](#design-details)
+    - [Test Plan](#test-plan)
+        - [Prerequisite testing updates](#prerequisite-testing-updates)
+      - [Unit Tests](#unit-tests)
+      - [Integration tests](#integration-tests)
+    - [Graduation Criteria](#graduation-criteria)
+  - [Implementation History](#implementation-history)
+  - [Drawbacks](#drawbacks)
+  - [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+In specific scenarios, Kueue scheduling is slow across 2 dimensions. Horizontally across clusterQueue heads and vertically
+down a single clusterQueue. The impact of this slowness results means workloads are not able to reclaim their nominalQuota (ie guarantees)
+within an acceptable period of time, ultimately resulting in what we consider a "guarantee violation".
+
+This KEP is a proposal to address scheduling issues in the horizontal dimension across CQ heads.
+
+## Understanding the Scheduling problem today
+
+### Simplification of Kueue Scheduling today
+
+1. nomination: For the admissible clusterqueue head workloads, assign preemption targets. This produces admission candidates.
+   - Preemption target assignment: actively terminating workloads are prioritized as preemption targets
+2. order admission candidates.
+3. evaluate admissibility of admission candidate. If the admission candidate has overlapping preemption targets, skip this workload.
+   - Ex: Preemptor A has preemption targets 1, 2. Preemptor B has preemption targets 2, 3. Preemptor B will be skipped in this `schedule()` invocation.
+4. If the admission candidate is admissible and its preemption target has not completed preemption, requeue admission candidate. 
+   (ie CQ heads will block its own CQ until it admits)
+
+### Pods slow to terminate
+
+Day to day, this algorithm is pretty effective. Where it causes scheduling slowness, is when the pod is slow to terminate for any reason. 
+There's 2 categories of reasons so far:
+
+1. Node health: we call these zombie pods where the pod gets stuck in termination
+2. Pod scoped:
+   - pod termination grace period
+   - pod gets stuck if it was pulling a docker image at time of termination
+
+### Scheduling slowness horizontally
+
+(While slowly terminating pods causes scheduling slowness vertically down 1 clusterQueue, that is out of scope for this document.)
+When there's pods slow to terminate, this causes scheduling slowness horizontally across clusterQueues. This is because:
+
+1. admission candidates (preemptor) will first prioritize workloads already marked for preemption to be selected as a preemption target.
+   - See: `CandidatesOrdering` function: `// 0. Workloads already marked for preemption first.`
+2. All CQ heads that select this slowly terminating pod as a preemption target will be skipped for the current `schedule()` invocation
+3. Because CQ heads are blocking, the skipped CQ head will continue to block admission for the rest of the CQ
+
+So a slowly terminating pod or pod stuck in termination can clog up scheduling across clusterQueues. 
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Isolate scheduling algorithm inefficiencies to 1 CQ instead of across CQs
```

  I see, but for 1. I would like to challenge the approach, and consider fixing the issue with Pods terminating indefinitely in the first place.
  
  Today I spent some time with @PBundyra discussing various variants of (1.), and all approaches we came up with (I think including the proposed here) have some drawbacks. 
  
  So I would like to take a step back and consider fixing the problem outside of the scheduling Algo. At least explore this path to the point where we see it wouldn't help.

### Review by [@amy](https://github.com/amy) (commented) — 2025-09-10T19:29:20Z

- **keps/4378-refresh-assignments-during-scheduling-cycle/README.md:93** by [@amy](https://github.com/amy) — 2025-09-10T19:29:20Z
```diff
@@ -0,0 +1,346 @@
+# KEP-4378: Refresh Assignments During Scheduling Cycle
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [KEP-4378: Refresh Assignments During Scheduling Cycle](#kep-4378-refresh-assignments-during-scheduling-cycle)
+  - [Summary](#summary)
+  - [Understanding the Scheduling problem today](#understanding-the-scheduling-problem-today)
+    - [Simplification of Kueue Scheduling today](#simplification-of-kueue-scheduling-today)
+    - [Pods slow to terminate](#pods-slow-to-terminate)
+    - [Scheduling slowness horizontally](#scheduling-slowness-horizontally)
+  - [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+  - [Proposal](#proposal)
+    - [Exclude Overlapping Preemption Targets](#exclude-overlapping-preemption-targets)
+    - [Issues with this approach](#issues-with-this-approach)
+    - [Allow Multiple Preemptors to claim same Preemption target](#allow-multiple-preemptors-to-claim-same-preemption-target)
+    - [User Stories (Optional)](#user-stories-optional)
+    - [Risks and Mitigations](#risks-and-mitigations)
+  - [Design Details](#design-details)
+    - [Test Plan](#test-plan)
+        - [Prerequisite testing updates](#prerequisite-testing-updates)
+      - [Unit Tests](#unit-tests)
+      - [Integration tests](#integration-tests)
+    - [Graduation Criteria](#graduation-criteria)
+  - [Implementation History](#implementation-history)
+  - [Drawbacks](#drawbacks)
+  - [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+In specific scenarios, Kueue scheduling is slow across 2 dimensions. Horizontally across clusterQueue heads and vertically
+down a single clusterQueue. The impact of this slowness results means workloads are not able to reclaim their nominalQuota (ie guarantees)
+within an acceptable period of time, ultimately resulting in what we consider a "guarantee violation".
+
+This KEP is a proposal to address scheduling issues in the horizontal dimension across CQ heads.
+
+## Understanding the Scheduling problem today
+
+### Simplification of Kueue Scheduling today
+
+1. nomination: For the admissible clusterqueue head workloads, assign preemption targets. This produces admission candidates.
+   - Preemption target assignment: actively terminating workloads are prioritized as preemption targets
+2. order admission candidates.
+3. evaluate admissibility of admission candidate. If the admission candidate has overlapping preemption targets, skip this workload.
+   - Ex: Preemptor A has preemption targets 1, 2. Preemptor B has preemption targets 2, 3. Preemptor B will be skipped in this `schedule()` invocation.
+4. If the admission candidate is admissible and its preemption target has not completed preemption, requeue admission candidate. 
+   (ie CQ heads will block its own CQ until it admits)
+
+### Pods slow to terminate
+
+Day to day, this algorithm is pretty effective. Where it causes scheduling slowness, is when the pod is slow to terminate for any reason. 
+There's 2 categories of reasons so far:
+
+1. Node health: we call these zombie pods where the pod gets stuck in termination
+2. Pod scoped:
+   - pod termination grace period
+   - pod gets stuck if it was pulling a docker image at time of termination
```

  What I wonder is this:
  We implement the external controller, and set the termination timeout to 15min. 
  
  So now, regardless of reason, termination is capped at 15min. Within that valid 15min termination window, we'll still have overlapping preemption targets.
  
  As an example when I run this query in splunk `index=* "Workload has overlapping preemption targets with another workload" | stats count by object.name | sort -count` in the last `24 hours`
  
  There's workloads that were skipped due to overlapping preemption targets 46K times. No reports of Zombie pods or pods failing to terminate outside of 15min grace period.

### Review by [@amy](https://github.com/amy) (commented) — 2025-09-10T19:30:40Z

- **keps/4378-refresh-assignments-during-scheduling-cycle/README.md:214** by [@amy](https://github.com/amy) — 2025-09-10T19:30:39Z
```diff
@@ -0,0 +1,346 @@
+# KEP-4378: Refresh Assignments During Scheduling Cycle
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [KEP-4378: Refresh Assignments During Scheduling Cycle](#kep-4378-refresh-assignments-during-scheduling-cycle)
+  - [Summary](#summary)
+  - [Understanding the Scheduling problem today](#understanding-the-scheduling-problem-today)
+    - [Simplification of Kueue Scheduling today](#simplification-of-kueue-scheduling-today)
+    - [Pods slow to terminate](#pods-slow-to-terminate)
+    - [Scheduling slowness horizontally](#scheduling-slowness-horizontally)
+  - [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+  - [Proposal](#proposal)
+    - [Exclude Overlapping Preemption Targets](#exclude-overlapping-preemption-targets)
+    - [Issues with this approach](#issues-with-this-approach)
+    - [Allow Multiple Preemptors to claim same Preemption target](#allow-multiple-preemptors-to-claim-same-preemption-target)
+    - [User Stories (Optional)](#user-stories-optional)
+    - [Risks and Mitigations](#risks-and-mitigations)
+  - [Design Details](#design-details)
+    - [Test Plan](#test-plan)
+        - [Prerequisite testing updates](#prerequisite-testing-updates)
+      - [Unit Tests](#unit-tests)
+      - [Integration tests](#integration-tests)
+    - [Graduation Criteria](#graduation-criteria)
+  - [Implementation History](#implementation-history)
+  - [Drawbacks](#drawbacks)
+  - [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+In specific scenarios, Kueue scheduling is slow across 2 dimensions. Horizontally across clusterQueue heads and vertically
+down a single clusterQueue. The impact of this slowness results means workloads are not able to reclaim their nominalQuota (ie guarantees)
+within an acceptable period of time, ultimately resulting in what we consider a "guarantee violation".
+
+This KEP is a proposal to address scheduling issues in the horizontal dimension across CQ heads.
+
+## Understanding the Scheduling problem today
+
+### Simplification of Kueue Scheduling today
+
+1. nomination: For the admissible clusterqueue head workloads, assign preemption targets. This produces admission candidates.
+   - Preemption target assignment: actively terminating workloads are prioritized as preemption targets
+2. order admission candidates.
+3. evaluate admissibility of admission candidate. If the admission candidate has overlapping preemption targets, skip this workload.
+   - Ex: Preemptor A has preemption targets 1, 2. Preemptor B has preemption targets 2, 3. Preemptor B will be skipped in this `schedule()` invocation.
+4. If the admission candidate is admissible and its preemption target has not completed preemption, requeue admission candidate. 
+   (ie CQ heads will block its own CQ until it admits)
+
+### Pods slow to terminate
+
+Day to day, this algorithm is pretty effective. Where it causes scheduling slowness, is when the pod is slow to terminate for any reason. 
+There's 2 categories of reasons so far:
+
+1. Node health: we call these zombie pods where the pod gets stuck in termination
+2. Pod scoped:
+   - pod termination grace period
+   - pod gets stuck if it was pulling a docker image at time of termination
+
+### Scheduling slowness horizontally
+
+(While slowly terminating pods causes scheduling slowness vertically down 1 clusterQueue, that is out of scope for this document.)
+When there's pods slow to terminate, this causes scheduling slowness horizontally across clusterQueues. This is because:
+
+1. admission candidates (preemptor) will first prioritize workloads already marked for preemption to be selected as a preemption target.
+   - See: `CandidatesOrdering` function: `// 0. Workloads already marked for preemption first.`
+2. All CQ heads that select this slowly terminating pod as a preemption target will be skipped for the current `schedule()` invocation
+3. Because CQ heads are blocking, the skipped CQ head will continue to block admission for the rest of the CQ
+
+So a slowly terminating pod or pod stuck in termination can clog up scheduling across clusterQueues. 
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Isolate scheduling algorithm inefficiencies to 1 CQ instead of across CQs
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+- Fix vertical scheduling algorithm inefficiency where preemption is slow and serialized down 1 CQ
+  if grace period termination is set
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+This KEP proposes to recalculate preemption targets when it detects overlapping preemption targets. 
+Due to how Kueue's scheduling algorithm works, this KEP also proposes to allow multiple preemptors to claim
+the same preemption target. Below is how we've come to that conclusion.
+
+### Exclude Overlapping Preemption Targets
+
+Naively, you could recalculate preemption targets and exclude workloads already selected as preemption targets. For this approach...
+
+- The preemptor's usage is added regardless of preemption target completion
+- As a result, you need to remove usage of preemption targets from the snapshot to properly account for resources being freed during the scheduling cycle. 
+  This is essential for RefreshAssignmentsDuringSchedulingCycle to work correctly. Without this, workloadFits() will fail on assignment retry because we add 
+  the admission candidate's usage in `AddUsage()` without removing the usage from the preemption target.
+
+### Issues with this approach
+
+This would introduce the following bug:
+
+- Preemptor 1 (1GPU) preempts a 2GPU target. The preemptor's assignment mode is correctly set as `Preempt`.
+- Preemptor 2 (2GPU) would be incorrectly admitted with assignment mode set incorrectly to `Fit`. 
+
+### Allow Multiple Preemptors to claim same Preemption target
+
+Instead, allow preemptors to claim only the usage they need, so that the preemption target could be attributable to more than 1 preemptor. This would allow:
+
+1. preemption target recalculation
+2. correctly set the preemptor's assignment mode to `Preempt` so that it requeues until the preemption target completes preemption
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+See [Pods slow to terminate](#pods-slow-to-terminate)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+In `preempted_workloads.go` create a new PreemptedWorkloads type:
```

  Yeah, not at all tied to the implementation and agree with your points around complexity. Let's scope high level discussions to whether we want to resolve scheduling slowness in the scheduler algo itself by calculating new preemption targets rather than my specific implementation.
  
  > 1. does it cover some use-cases which are not handled by terminating the Pods?
  
  See: https://github.com/kubernetes-sigs/kueue/pull/6776#discussion_r2337711054
  
  > 2. what is the issue with just allowing for preemption of multiple workloads with overlapping targets, but without recomputing the targets.
  
  I think this would be preferable and cleaner. But here's my clarifying question with a contrived example:
  - We have these targets: a=4, b=4, c=2 
  - Preemptor X (6) -> selects a and b
  - Preemptor Y (6) -> selects b and c
  - Admit X, Y; Preempt a, b, c
  - X + Y = 12
  - a + b + c = 10
  
  So you admit X and Y when their respective targets preempt. But the underlying cluster doesn't have enough resources for this schedule cycle. There would be no requeueing that happens.

### Review by [@amy](https://github.com/amy) (commented) — 2025-09-10T19:32:11Z

- **keps/4378-refresh-assignments-during-scheduling-cycle/README.md:125** by [@amy](https://github.com/amy) — 2025-09-10T19:32:11Z
```diff
@@ -0,0 +1,346 @@
+# KEP-4378: Refresh Assignments During Scheduling Cycle
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [KEP-4378: Refresh Assignments During Scheduling Cycle](#kep-4378-refresh-assignments-during-scheduling-cycle)
+  - [Summary](#summary)
+  - [Understanding the Scheduling problem today](#understanding-the-scheduling-problem-today)
+    - [Simplification of Kueue Scheduling today](#simplification-of-kueue-scheduling-today)
+    - [Pods slow to terminate](#pods-slow-to-terminate)
+    - [Scheduling slowness horizontally](#scheduling-slowness-horizontally)
+  - [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+  - [Proposal](#proposal)
+    - [Exclude Overlapping Preemption Targets](#exclude-overlapping-preemption-targets)
+    - [Issues with this approach](#issues-with-this-approach)
+    - [Allow Multiple Preemptors to claim same Preemption target](#allow-multiple-preemptors-to-claim-same-preemption-target)
+    - [User Stories (Optional)](#user-stories-optional)
+    - [Risks and Mitigations](#risks-and-mitigations)
+  - [Design Details](#design-details)
+    - [Test Plan](#test-plan)
+        - [Prerequisite testing updates](#prerequisite-testing-updates)
+      - [Unit Tests](#unit-tests)
+      - [Integration tests](#integration-tests)
+    - [Graduation Criteria](#graduation-criteria)
+  - [Implementation History](#implementation-history)
+  - [Drawbacks](#drawbacks)
+  - [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+In specific scenarios, Kueue scheduling is slow across 2 dimensions. Horizontally across clusterQueue heads and vertically
+down a single clusterQueue. The impact of this slowness results means workloads are not able to reclaim their nominalQuota (ie guarantees)
+within an acceptable period of time, ultimately resulting in what we consider a "guarantee violation".
+
+This KEP is a proposal to address scheduling issues in the horizontal dimension across CQ heads.
+
+## Understanding the Scheduling problem today
+
+### Simplification of Kueue Scheduling today
+
+1. nomination: For the admissible clusterqueue head workloads, assign preemption targets. This produces admission candidates.
+   - Preemption target assignment: actively terminating workloads are prioritized as preemption targets
+2. order admission candidates.
+3. evaluate admissibility of admission candidate. If the admission candidate has overlapping preemption targets, skip this workload.
+   - Ex: Preemptor A has preemption targets 1, 2. Preemptor B has preemption targets 2, 3. Preemptor B will be skipped in this `schedule()` invocation.
+4. If the admission candidate is admissible and its preemption target has not completed preemption, requeue admission candidate. 
+   (ie CQ heads will block its own CQ until it admits)
+
+### Pods slow to terminate
+
+Day to day, this algorithm is pretty effective. Where it causes scheduling slowness, is when the pod is slow to terminate for any reason. 
+There's 2 categories of reasons so far:
+
+1. Node health: we call these zombie pods where the pod gets stuck in termination
+2. Pod scoped:
+   - pod termination grace period
+   - pod gets stuck if it was pulling a docker image at time of termination
+
+### Scheduling slowness horizontally
+
+(While slowly terminating pods causes scheduling slowness vertically down 1 clusterQueue, that is out of scope for this document.)
+When there's pods slow to terminate, this causes scheduling slowness horizontally across clusterQueues. This is because:
+
+1. admission candidates (preemptor) will first prioritize workloads already marked for preemption to be selected as a preemption target.
+   - See: `CandidatesOrdering` function: `// 0. Workloads already marked for preemption first.`
+2. All CQ heads that select this slowly terminating pod as a preemption target will be skipped for the current `schedule()` invocation
+3. Because CQ heads are blocking, the skipped CQ head will continue to block admission for the rest of the CQ
+
+So a slowly terminating pod or pod stuck in termination can clog up scheduling across clusterQueues. 
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Isolate scheduling algorithm inefficiencies to 1 CQ instead of across CQs
```

  See: https://github.com/kubernetes-sigs/kueue/pull/6776#discussion_r2337711054

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-11T10:40:54Z

- **keps/4378-refresh-assignments-during-scheduling-cycle/README.md:214** by [@mimowo](https://github.com/mimowo) — 2025-09-11T10:40:54Z
```diff
@@ -0,0 +1,346 @@
+# KEP-4378: Refresh Assignments During Scheduling Cycle
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [KEP-4378: Refresh Assignments During Scheduling Cycle](#kep-4378-refresh-assignments-during-scheduling-cycle)
+  - [Summary](#summary)
+  - [Understanding the Scheduling problem today](#understanding-the-scheduling-problem-today)
+    - [Simplification of Kueue Scheduling today](#simplification-of-kueue-scheduling-today)
+    - [Pods slow to terminate](#pods-slow-to-terminate)
+    - [Scheduling slowness horizontally](#scheduling-slowness-horizontally)
+  - [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+  - [Proposal](#proposal)
+    - [Exclude Overlapping Preemption Targets](#exclude-overlapping-preemption-targets)
+    - [Issues with this approach](#issues-with-this-approach)
+    - [Allow Multiple Preemptors to claim same Preemption target](#allow-multiple-preemptors-to-claim-same-preemption-target)
+    - [User Stories (Optional)](#user-stories-optional)
+    - [Risks and Mitigations](#risks-and-mitigations)
+  - [Design Details](#design-details)
+    - [Test Plan](#test-plan)
+        - [Prerequisite testing updates](#prerequisite-testing-updates)
+      - [Unit Tests](#unit-tests)
+      - [Integration tests](#integration-tests)
+    - [Graduation Criteria](#graduation-criteria)
+  - [Implementation History](#implementation-history)
+  - [Drawbacks](#drawbacks)
+  - [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+In specific scenarios, Kueue scheduling is slow across 2 dimensions. Horizontally across clusterQueue heads and vertically
+down a single clusterQueue. The impact of this slowness results means workloads are not able to reclaim their nominalQuota (ie guarantees)
+within an acceptable period of time, ultimately resulting in what we consider a "guarantee violation".
+
+This KEP is a proposal to address scheduling issues in the horizontal dimension across CQ heads.
+
+## Understanding the Scheduling problem today
+
+### Simplification of Kueue Scheduling today
+
+1. nomination: For the admissible clusterqueue head workloads, assign preemption targets. This produces admission candidates.
+   - Preemption target assignment: actively terminating workloads are prioritized as preemption targets
+2. order admission candidates.
+3. evaluate admissibility of admission candidate. If the admission candidate has overlapping preemption targets, skip this workload.
+   - Ex: Preemptor A has preemption targets 1, 2. Preemptor B has preemption targets 2, 3. Preemptor B will be skipped in this `schedule()` invocation.
+4. If the admission candidate is admissible and its preemption target has not completed preemption, requeue admission candidate. 
+   (ie CQ heads will block its own CQ until it admits)
+
+### Pods slow to terminate
+
+Day to day, this algorithm is pretty effective. Where it causes scheduling slowness, is when the pod is slow to terminate for any reason. 
+There's 2 categories of reasons so far:
+
+1. Node health: we call these zombie pods where the pod gets stuck in termination
+2. Pod scoped:
+   - pod termination grace period
+   - pod gets stuck if it was pulling a docker image at time of termination
+
+### Scheduling slowness horizontally
+
+(While slowly terminating pods causes scheduling slowness vertically down 1 clusterQueue, that is out of scope for this document.)
+When there's pods slow to terminate, this causes scheduling slowness horizontally across clusterQueues. This is because:
+
+1. admission candidates (preemptor) will first prioritize workloads already marked for preemption to be selected as a preemption target.
+   - See: `CandidatesOrdering` function: `// 0. Workloads already marked for preemption first.`
+2. All CQ heads that select this slowly terminating pod as a preemption target will be skipped for the current `schedule()` invocation
+3. Because CQ heads are blocking, the skipped CQ head will continue to block admission for the rest of the CQ
+
+So a slowly terminating pod or pod stuck in termination can clog up scheduling across clusterQueues. 
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Isolate scheduling algorithm inefficiencies to 1 CQ instead of across CQs
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+- Fix vertical scheduling algorithm inefficiency where preemption is slow and serialized down 1 CQ
+  if grace period termination is set
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+This KEP proposes to recalculate preemption targets when it detects overlapping preemption targets. 
+Due to how Kueue's scheduling algorithm works, this KEP also proposes to allow multiple preemptors to claim
+the same preemption target. Below is how we've come to that conclusion.
+
+### Exclude Overlapping Preemption Targets
+
+Naively, you could recalculate preemption targets and exclude workloads already selected as preemption targets. For this approach...
+
+- The preemptor's usage is added regardless of preemption target completion
+- As a result, you need to remove usage of preemption targets from the snapshot to properly account for resources being freed during the scheduling cycle. 
+  This is essential for RefreshAssignmentsDuringSchedulingCycle to work correctly. Without this, workloadFits() will fail on assignment retry because we add 
+  the admission candidate's usage in `AddUsage()` without removing the usage from the preemption target.
+
+### Issues with this approach
+
+This would introduce the following bug:
+
+- Preemptor 1 (1GPU) preempts a 2GPU target. The preemptor's assignment mode is correctly set as `Preempt`.
+- Preemptor 2 (2GPU) would be incorrectly admitted with assignment mode set incorrectly to `Fit`. 
+
+### Allow Multiple Preemptors to claim same Preemption target
+
+Instead, allow preemptors to claim only the usage they need, so that the preemption target could be attributable to more than 1 preemptor. This would allow:
+
+1. preemption target recalculation
+2. correctly set the preemptor's assignment mode to `Preempt` so that it requeues until the preemption target completes preemption
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+See [Pods slow to terminate](#pods-slow-to-terminate)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+In `preempted_workloads.go` create a new PreemptedWorkloads type:
```

  The way I imagine this could work, is that we process X first, and preempt "a" and "b", this means we release 8 of quota, but we "assume usage" of X at the same time, so we are left with `delta=-a-b+X=-2`. Delta does not allow to schedule Y, because `X+Y > a + b + c`.
  
  For that , we don't need to track the "ClaimedUsage" per workload. Just simulate the preemption of "a" and "b", and scheduling of "X" on the snaphot. We can do it with the existing cluster_queue_snapshot.AddUsage, and cluster_queue_snapshot.RemoveUsage functions.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-11T10:44:56Z

- **keps/4378-refresh-assignments-during-scheduling-cycle/README.md:93** by [@mimowo](https://github.com/mimowo) — 2025-09-11T10:44:56Z
```diff
@@ -0,0 +1,346 @@
+# KEP-4378: Refresh Assignments During Scheduling Cycle
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [KEP-4378: Refresh Assignments During Scheduling Cycle](#kep-4378-refresh-assignments-during-scheduling-cycle)
+  - [Summary](#summary)
+  - [Understanding the Scheduling problem today](#understanding-the-scheduling-problem-today)
+    - [Simplification of Kueue Scheduling today](#simplification-of-kueue-scheduling-today)
+    - [Pods slow to terminate](#pods-slow-to-terminate)
+    - [Scheduling slowness horizontally](#scheduling-slowness-horizontally)
+  - [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+  - [Proposal](#proposal)
+    - [Exclude Overlapping Preemption Targets](#exclude-overlapping-preemption-targets)
+    - [Issues with this approach](#issues-with-this-approach)
+    - [Allow Multiple Preemptors to claim same Preemption target](#allow-multiple-preemptors-to-claim-same-preemption-target)
+    - [User Stories (Optional)](#user-stories-optional)
+    - [Risks and Mitigations](#risks-and-mitigations)
+  - [Design Details](#design-details)
+    - [Test Plan](#test-plan)
+        - [Prerequisite testing updates](#prerequisite-testing-updates)
+      - [Unit Tests](#unit-tests)
+      - [Integration tests](#integration-tests)
+    - [Graduation Criteria](#graduation-criteria)
+  - [Implementation History](#implementation-history)
+  - [Drawbacks](#drawbacks)
+  - [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+In specific scenarios, Kueue scheduling is slow across 2 dimensions. Horizontally across clusterQueue heads and vertically
+down a single clusterQueue. The impact of this slowness results means workloads are not able to reclaim their nominalQuota (ie guarantees)
+within an acceptable period of time, ultimately resulting in what we consider a "guarantee violation".
+
+This KEP is a proposal to address scheduling issues in the horizontal dimension across CQ heads.
+
+## Understanding the Scheduling problem today
+
+### Simplification of Kueue Scheduling today
+
+1. nomination: For the admissible clusterqueue head workloads, assign preemption targets. This produces admission candidates.
+   - Preemption target assignment: actively terminating workloads are prioritized as preemption targets
+2. order admission candidates.
+3. evaluate admissibility of admission candidate. If the admission candidate has overlapping preemption targets, skip this workload.
+   - Ex: Preemptor A has preemption targets 1, 2. Preemptor B has preemption targets 2, 3. Preemptor B will be skipped in this `schedule()` invocation.
+4. If the admission candidate is admissible and its preemption target has not completed preemption, requeue admission candidate. 
+   (ie CQ heads will block its own CQ until it admits)
+
+### Pods slow to terminate
+
+Day to day, this algorithm is pretty effective. Where it causes scheduling slowness, is when the pod is slow to terminate for any reason. 
+There's 2 categories of reasons so far:
+
+1. Node health: we call these zombie pods where the pod gets stuck in termination
+2. Pod scoped:
+   - pod termination grace period
+   - pod gets stuck if it was pulling a docker image at time of termination
```

  > As an example when I run this query in splunk index=* "Workload has overlapping preemption targets with another workload" | stats count by object.name | sort -count in the last 24 hours
  
  This looks quite bad indeed.
  
  > No reports of Zombie pods or pods failing to terminate outside of 15min grace period.
  
  How do you verify there were no zombie Pods?  Do you know what was actually blocking the preemption of the workloads for so long? I would really like to make sure we understand the scenario.

### Review by [@amy](https://github.com/amy) (commented) — 2025-09-11T16:06:32Z

- **keps/4378-refresh-assignments-during-scheduling-cycle/README.md:214** by [@amy](https://github.com/amy) — 2025-09-11T16:06:32Z
```diff
@@ -0,0 +1,346 @@
+# KEP-4378: Refresh Assignments During Scheduling Cycle
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [KEP-4378: Refresh Assignments During Scheduling Cycle](#kep-4378-refresh-assignments-during-scheduling-cycle)
+  - [Summary](#summary)
+  - [Understanding the Scheduling problem today](#understanding-the-scheduling-problem-today)
+    - [Simplification of Kueue Scheduling today](#simplification-of-kueue-scheduling-today)
+    - [Pods slow to terminate](#pods-slow-to-terminate)
+    - [Scheduling slowness horizontally](#scheduling-slowness-horizontally)
+  - [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+  - [Proposal](#proposal)
+    - [Exclude Overlapping Preemption Targets](#exclude-overlapping-preemption-targets)
+    - [Issues with this approach](#issues-with-this-approach)
+    - [Allow Multiple Preemptors to claim same Preemption target](#allow-multiple-preemptors-to-claim-same-preemption-target)
+    - [User Stories (Optional)](#user-stories-optional)
+    - [Risks and Mitigations](#risks-and-mitigations)
+  - [Design Details](#design-details)
+    - [Test Plan](#test-plan)
+        - [Prerequisite testing updates](#prerequisite-testing-updates)
+      - [Unit Tests](#unit-tests)
+      - [Integration tests](#integration-tests)
+    - [Graduation Criteria](#graduation-criteria)
+  - [Implementation History](#implementation-history)
+  - [Drawbacks](#drawbacks)
+  - [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+In specific scenarios, Kueue scheduling is slow across 2 dimensions. Horizontally across clusterQueue heads and vertically
+down a single clusterQueue. The impact of this slowness results means workloads are not able to reclaim their nominalQuota (ie guarantees)
+within an acceptable period of time, ultimately resulting in what we consider a "guarantee violation".
+
+This KEP is a proposal to address scheduling issues in the horizontal dimension across CQ heads.
+
+## Understanding the Scheduling problem today
+
+### Simplification of Kueue Scheduling today
+
+1. nomination: For the admissible clusterqueue head workloads, assign preemption targets. This produces admission candidates.
+   - Preemption target assignment: actively terminating workloads are prioritized as preemption targets
+2. order admission candidates.
+3. evaluate admissibility of admission candidate. If the admission candidate has overlapping preemption targets, skip this workload.
+   - Ex: Preemptor A has preemption targets 1, 2. Preemptor B has preemption targets 2, 3. Preemptor B will be skipped in this `schedule()` invocation.
+4. If the admission candidate is admissible and its preemption target has not completed preemption, requeue admission candidate. 
+   (ie CQ heads will block its own CQ until it admits)
+
+### Pods slow to terminate
+
+Day to day, this algorithm is pretty effective. Where it causes scheduling slowness, is when the pod is slow to terminate for any reason. 
+There's 2 categories of reasons so far:
+
+1. Node health: we call these zombie pods where the pod gets stuck in termination
+2. Pod scoped:
+   - pod termination grace period
+   - pod gets stuck if it was pulling a docker image at time of termination
+
+### Scheduling slowness horizontally
+
+(While slowly terminating pods causes scheduling slowness vertically down 1 clusterQueue, that is out of scope for this document.)
+When there's pods slow to terminate, this causes scheduling slowness horizontally across clusterQueues. This is because:
+
+1. admission candidates (preemptor) will first prioritize workloads already marked for preemption to be selected as a preemption target.
+   - See: `CandidatesOrdering` function: `// 0. Workloads already marked for preemption first.`
+2. All CQ heads that select this slowly terminating pod as a preemption target will be skipped for the current `schedule()` invocation
+3. Because CQ heads are blocking, the skipped CQ head will continue to block admission for the rest of the CQ
+
+So a slowly terminating pod or pod stuck in termination can clog up scheduling across clusterQueues. 
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Isolate scheduling algorithm inefficiencies to 1 CQ instead of across CQs
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+- Fix vertical scheduling algorithm inefficiency where preemption is slow and serialized down 1 CQ
+  if grace period termination is set
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+This KEP proposes to recalculate preemption targets when it detects overlapping preemption targets. 
+Due to how Kueue's scheduling algorithm works, this KEP also proposes to allow multiple preemptors to claim
+the same preemption target. Below is how we've come to that conclusion.
+
+### Exclude Overlapping Preemption Targets
+
+Naively, you could recalculate preemption targets and exclude workloads already selected as preemption targets. For this approach...
+
+- The preemptor's usage is added regardless of preemption target completion
+- As a result, you need to remove usage of preemption targets from the snapshot to properly account for resources being freed during the scheduling cycle. 
+  This is essential for RefreshAssignmentsDuringSchedulingCycle to work correctly. Without this, workloadFits() will fail on assignment retry because we add 
+  the admission candidate's usage in `AddUsage()` without removing the usage from the preemption target.
+
+### Issues with this approach
+
+This would introduce the following bug:
+
+- Preemptor 1 (1GPU) preempts a 2GPU target. The preemptor's assignment mode is correctly set as `Preempt`.
+- Preemptor 2 (2GPU) would be incorrectly admitted with assignment mode set incorrectly to `Fit`. 
+
+### Allow Multiple Preemptors to claim same Preemption target
+
+Instead, allow preemptors to claim only the usage they need, so that the preemption target could be attributable to more than 1 preemptor. This would allow:
+
+1. preemption target recalculation
+2. correctly set the preemptor's assignment mode to `Preempt` so that it requeues until the preemption target completes preemption
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+See [Pods slow to terminate](#pods-slow-to-terminate)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+In `preempted_workloads.go` create a new PreemptedWorkloads type:
```

  Let's also run through this issue with your example https://github.com/kubernetes-sigs/kueue/pull/6776#discussion_r2336949168. 
  
  So in a new example, lets say this:
  We have these targets: a=4, b=4, c=4
  Preemptor X (6) -> selects a and b
  Preemptor Y (6) -> selects b and c
  Admit X, Y; Preempt a, b, c
  X + Y = 12
  a + b + c = 12
  
  So:
  - process X, within simulation -> preempt a & b, add Y -> passes fit check
  - add X to snapshot, add a & b to existing `preemptedWorkloads`
  - process Y, within simulation -> preempt c, add Y -> passes fit check
  - Here, how do we attribute b as Y's preemption target?
  
  It could also be the case that Y is several heads downstream, and its original preemption targets are stale, but there's other valid preemption targets to still select that require recalculation.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-11T16:14:13Z

- **keps/4378-refresh-assignments-during-scheduling-cycle/README.md:214** by [@mimowo](https://github.com/mimowo) — 2025-09-11T16:14:13Z
```diff
@@ -0,0 +1,346 @@
+# KEP-4378: Refresh Assignments During Scheduling Cycle
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [KEP-4378: Refresh Assignments During Scheduling Cycle](#kep-4378-refresh-assignments-during-scheduling-cycle)
+  - [Summary](#summary)
+  - [Understanding the Scheduling problem today](#understanding-the-scheduling-problem-today)
+    - [Simplification of Kueue Scheduling today](#simplification-of-kueue-scheduling-today)
+    - [Pods slow to terminate](#pods-slow-to-terminate)
+    - [Scheduling slowness horizontally](#scheduling-slowness-horizontally)
+  - [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+  - [Proposal](#proposal)
+    - [Exclude Overlapping Preemption Targets](#exclude-overlapping-preemption-targets)
+    - [Issues with this approach](#issues-with-this-approach)
+    - [Allow Multiple Preemptors to claim same Preemption target](#allow-multiple-preemptors-to-claim-same-preemption-target)
+    - [User Stories (Optional)](#user-stories-optional)
+    - [Risks and Mitigations](#risks-and-mitigations)
+  - [Design Details](#design-details)
+    - [Test Plan](#test-plan)
+        - [Prerequisite testing updates](#prerequisite-testing-updates)
+      - [Unit Tests](#unit-tests)
+      - [Integration tests](#integration-tests)
+    - [Graduation Criteria](#graduation-criteria)
+  - [Implementation History](#implementation-history)
+  - [Drawbacks](#drawbacks)
+  - [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+In specific scenarios, Kueue scheduling is slow across 2 dimensions. Horizontally across clusterQueue heads and vertically
+down a single clusterQueue. The impact of this slowness results means workloads are not able to reclaim their nominalQuota (ie guarantees)
+within an acceptable period of time, ultimately resulting in what we consider a "guarantee violation".
+
+This KEP is a proposal to address scheduling issues in the horizontal dimension across CQ heads.
+
+## Understanding the Scheduling problem today
+
+### Simplification of Kueue Scheduling today
+
+1. nomination: For the admissible clusterqueue head workloads, assign preemption targets. This produces admission candidates.
+   - Preemption target assignment: actively terminating workloads are prioritized as preemption targets
+2. order admission candidates.
+3. evaluate admissibility of admission candidate. If the admission candidate has overlapping preemption targets, skip this workload.
+   - Ex: Preemptor A has preemption targets 1, 2. Preemptor B has preemption targets 2, 3. Preemptor B will be skipped in this `schedule()` invocation.
+4. If the admission candidate is admissible and its preemption target has not completed preemption, requeue admission candidate. 
+   (ie CQ heads will block its own CQ until it admits)
+
+### Pods slow to terminate
+
+Day to day, this algorithm is pretty effective. Where it causes scheduling slowness, is when the pod is slow to terminate for any reason. 
+There's 2 categories of reasons so far:
+
+1. Node health: we call these zombie pods where the pod gets stuck in termination
+2. Pod scoped:
+   - pod termination grace period
+   - pod gets stuck if it was pulling a docker image at time of termination
+
+### Scheduling slowness horizontally
+
+(While slowly terminating pods causes scheduling slowness vertically down 1 clusterQueue, that is out of scope for this document.)
+When there's pods slow to terminate, this causes scheduling slowness horizontally across clusterQueues. This is because:
+
+1. admission candidates (preemptor) will first prioritize workloads already marked for preemption to be selected as a preemption target.
+   - See: `CandidatesOrdering` function: `// 0. Workloads already marked for preemption first.`
+2. All CQ heads that select this slowly terminating pod as a preemption target will be skipped for the current `schedule()` invocation
+3. Because CQ heads are blocking, the skipped CQ head will continue to block admission for the rest of the CQ
+
+So a slowly terminating pod or pod stuck in termination can clog up scheduling across clusterQueues. 
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Isolate scheduling algorithm inefficiencies to 1 CQ instead of across CQs
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+- Fix vertical scheduling algorithm inefficiency where preemption is slow and serialized down 1 CQ
+  if grace period termination is set
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+This KEP proposes to recalculate preemption targets when it detects overlapping preemption targets. 
+Due to how Kueue's scheduling algorithm works, this KEP also proposes to allow multiple preemptors to claim
+the same preemption target. Below is how we've come to that conclusion.
+
+### Exclude Overlapping Preemption Targets
+
+Naively, you could recalculate preemption targets and exclude workloads already selected as preemption targets. For this approach...
+
+- The preemptor's usage is added regardless of preemption target completion
+- As a result, you need to remove usage of preemption targets from the snapshot to properly account for resources being freed during the scheduling cycle. 
+  This is essential for RefreshAssignmentsDuringSchedulingCycle to work correctly. Without this, workloadFits() will fail on assignment retry because we add 
+  the admission candidate's usage in `AddUsage()` without removing the usage from the preemption target.
+
+### Issues with this approach
+
+This would introduce the following bug:
+
+- Preemptor 1 (1GPU) preempts a 2GPU target. The preemptor's assignment mode is correctly set as `Preempt`.
+- Preemptor 2 (2GPU) would be incorrectly admitted with assignment mode set incorrectly to `Fit`. 
+
+### Allow Multiple Preemptors to claim same Preemption target
+
+Instead, allow preemptors to claim only the usage they need, so that the preemption target could be attributable to more than 1 preemptor. This would allow:
+
+1. preemption target recalculation
+2. correctly set the preemptor's assignment mode to `Preempt` so that it requeues until the preemption target completes preemption
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+See [Pods slow to terminate](#pods-slow-to-terminate)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+In `preempted_workloads.go` create a new PreemptedWorkloads type:
```

  > process X, within simulation -> preempt a & b, add Y -> passes fit check
  
  Is it the nomination phase? Why would Y pass the fit check? I imagine at this point for X and Y would require preeption, so are Preempt in the nominaion phase.
  
  Now during scheduling we process X and find a & b as preemption targets.
  
  We not, subract capactiy for a & b, and add add usage for X, so we in total released 2.
  
  Now, we re-run assignment for Y, we so we find it would fit if we preempt c (it already assumed a & b are targets). <- Indeed we would need to re-compute the preemption simulation here.
  
  We can see that Y could fit (still with Preemption mode), so we trigger the "parallel preemtions" for a, b, and c.
