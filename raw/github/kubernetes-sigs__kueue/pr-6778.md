# PR #6778: Modify updateAndPatch to return patched object

**Summary**: Modify updateAndPatch to return patched object

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6778

**Last updated**: 2025-09-10T14:53:44Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `feat/6158-modify-updateAndPatch` → `main`
- **Created**: 2025-09-10T07:56:49Z
- **Updated**: 2025-09-10T14:53:44Z
- **Merged**: 2025-09-10T14:06:00Z
- **Closed**: 2025-09-10T14:06:00Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mimowo](https://github.com/mimowo), [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 17   **+64 / -64**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Preparatory PR to introduce switch mechanism to alternate between Merge and Apply.
Changing the way we do the updateAndPatch allows to use Patch Merge in the future.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to #6158 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-10T07:56:52Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-09-10T07:56:57Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | d4e75e9a523af7d21af75906dc76790b1e290c35 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68c17fbff385370007617619 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-10T13:38:46Z

LGTM label has been added.  <details>Git tree hash: f0acea7d7657f21a0ac15ff442b969a754dc6b27</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-10T13:38:48Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6778#pullrequestreview-3206217598" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6778#" title="Author self-approved">mszadkow</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-09-10T13:40:52Z

@mbobrovskyi now lgtm from you ;)

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-10T13:41:54Z

/lgtm
Thank you so much 🙏

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-10T13:42:02Z

LGTM label has been added.  <details>Git tree hash: e43bec47942d979f31654a44eadf8d439f5d6a87</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-10T11:02:58Z

- **pkg/util/client/client.go:46** by [@mimowo](https://github.com/mimowo) — 2025-09-10T11:02:57Z
```diff
@@ -43,8 +43,8 @@ func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool,
 // PatchStatus applies the merge patch of client.Object status.
 // The resourceVersion will be part of the patch, make this call fail if
 // client.Object was changed.
-func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update func() (bool, error)) error {
-	return updateAndPatch(obj, true, update, func(patch client.Patch) error {
+func PatchStatus(ctx context.Context, c client.Client, strict bool, obj client.Object, update func() (client.Object, error)) error {
```

  Why introducing the `strict` param since always passed as true? 
  
  Is it needed in the follow up PRs?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-10T11:07:49Z

- **pkg/workload/workload.go:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-10T11:07:49Z
  It seems RemoveAnnotation() is unused. We can create a follow-up PR to clean it up.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-10T11:09:30Z

- **pkg/workloadslicing/workloadslicing.go:274** by [@mimowo](https://github.com/mimowo) — 2025-09-10T11:09:30Z
```diff
@@ -263,8 +266,12 @@ func StartWorkloadSlicePods(ctx context.Context, clnt client.Client, object clie
 		return fmt.Errorf("failed to list job pods: %w", err)
 	}
 	for i := range list.Items {
-		if err := clientutil.Patch(ctx, clnt, &list.Items[i], true, func() (bool, error) {
-			return pod.Ungate(&list.Items[i], kueue.ElasticJobSchedulingGate), nil
+		if err := clientutil.Patch(ctx, clnt, &list.Items[i], true, func() (client.Object, error) {
+			updated := pod.Ungate(&list.Items[i], kueue.ElasticJobSchedulingGate)
+			if updated {
+				return &list.Items[i], nil
+			}
+			return nil, nil
```

  It seems this pattern could simplify if we returned the bool, the updated object, and error.
  
  Yes, it is a bit redundant, but I think it makes the API easier to implement.
  
  wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-10T11:11:05Z

- **pkg/controller/jobs/pod/pod_controller.go:935** by [@mimowo](https://github.com/mimowo) — 2025-09-10T11:11:05Z
```diff
@@ -925,10 +932,13 @@ func (p *Pod) finalizePods(ctx context.Context, c client.Client, extraPods []cor
 	err := parallelize.Until(ctx, len(extraPods), func(i int) error {
 		pod := extraPods[i]
 		var removed bool
-		if err := clientutil.Patch(ctx, c, &pod, false, func() (bool, error) {
+		if err := clientutil.Patch(ctx, c, pod.DeepCopy(), false, func() (client.Object, error) {
```

  Why deepcopy the Pod? Isn't the Deep copy taken inside the updateAndPatch?
  
  Also, it feels inconsitent as there is no DeepCopy when calling `clientutil.Patch` above in the same file

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-10T11:32:38Z

- **pkg/controller/jobframework/reconciler.go:1090** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-10T11:32:38Z
```diff
@@ -1085,8 +1085,9 @@ func (r *JobReconciler) startJob(ctx context.Context, job GenericJob, object cli
 			return err
 		}
 	} else {
-		if err := clientutil.Patch(ctx, r.client, object, true, func() (bool, error) {
-			return true, job.RunWithPodSetsInfo(info)
+		if err := clientutil.Patch(ctx, r.client, object, true, func() (client.Object, error) {
+			err := job.RunWithPodSetsInfo(info)
+			return job.Object(), err
```

  ```suggestion
  			return object, job.RunWithPodSetsInfo(info)
  ```
  I’m wondering if we can do it like this.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-10T11:37:23Z

- **pkg/util/client/client.go:46** by [@mszadkow](https://github.com/mszadkow) — 2025-09-10T11:37:23Z
```diff
@@ -43,8 +43,8 @@ func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool,
 // PatchStatus applies the merge patch of client.Object status.
 // The resourceVersion will be part of the patch, make this call fail if
 // client.Object was changed.
-func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update func() (bool, error)) error {
-	return updateAndPatch(obj, true, update, func(patch client.Patch) error {
+func PatchStatus(ctx context.Context, c client.Client, strict bool, obj client.Object, update func() (client.Object, error)) error {
```

  Yes, it's needed later, I can introduce it separately in the next one

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-10T11:39:48Z

- **pkg/workload/workload.go:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-10T11:39:48Z
  https://github.com/kubernetes-sigs/kueue/pull/6780

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-10T11:41:04Z

- **pkg/workload/workload.go:1** by [@mszadkow](https://github.com/mszadkow) — 2025-09-10T11:41:04Z
  Will do one

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-10T11:43:23Z

- **pkg/workloadslicing/workloadslicing.go:274** by [@mszadkow](https://github.com/mszadkow) — 2025-09-10T11:43:23Z
```diff
@@ -263,8 +266,12 @@ func StartWorkloadSlicePods(ctx context.Context, clnt client.Client, object clie
 		return fmt.Errorf("failed to list job pods: %w", err)
 	}
 	for i := range list.Items {
-		if err := clientutil.Patch(ctx, clnt, &list.Items[i], true, func() (bool, error) {
-			return pod.Ungate(&list.Items[i], kueue.ElasticJobSchedulingGate), nil
+		if err := clientutil.Patch(ctx, clnt, &list.Items[i], true, func() (client.Object, error) {
+			updated := pod.Ungate(&list.Items[i], kueue.ElasticJobSchedulingGate)
+			if updated {
+				return &list.Items[i], nil
+			}
+			return nil, nil
```

  Yes, this will simplify couple of use cases

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-10T11:44:06Z

- **pkg/controller/jobs/pod/pod_controller.go:935** by [@mszadkow](https://github.com/mszadkow) — 2025-09-10T11:44:06Z
```diff
@@ -925,10 +932,13 @@ func (p *Pod) finalizePods(ctx context.Context, c client.Client, extraPods []cor
 	err := parallelize.Until(ctx, len(extraPods), func(i int) error {
 		pod := extraPods[i]
 		var removed bool
-		if err := clientutil.Patch(ctx, c, &pod, false, func() (bool, error) {
+		if err := clientutil.Patch(ctx, c, pod.DeepCopy(), false, func() (client.Object, error) {
```

  Good catch, this must be a leftover from the different attempts

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-10T11:44:47Z

- **pkg/controller/jobframework/reconciler.go:1090** by [@mszadkow](https://github.com/mszadkow) — 2025-09-10T11:44:47Z
```diff
@@ -1085,8 +1085,9 @@ func (r *JobReconciler) startJob(ctx context.Context, job GenericJob, object cli
 			return err
 		}
 	} else {
-		if err := clientutil.Patch(ctx, r.client, object, true, func() (bool, error) {
-			return true, job.RunWithPodSetsInfo(info)
+		if err := clientutil.Patch(ctx, r.client, object, true, func() (client.Object, error) {
+			err := job.RunWithPodSetsInfo(info)
+			return job.Object(), err
```

  it's quite easy to verify

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-10T11:45:07Z

- **pkg/util/client/client.go:46** by [@mimowo](https://github.com/mimowo) — 2025-09-10T11:45:07Z
```diff
@@ -43,8 +43,8 @@ func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool,
 // PatchStatus applies the merge patch of client.Object status.
 // The resourceVersion will be part of the patch, make this call fail if
 // client.Object was changed.
-func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update func() (bool, error)) error {
-	return updateAndPatch(obj, true, update, func(patch client.Patch) error {
+func PatchStatus(ctx context.Context, c client.Client, strict bool, obj client.Object, update func() (client.Object, error)) error {
```

  Let's do separately. This is not a simple change (as a whole), so I want to go step-by-step.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-10T12:01:09Z

- **pkg/workloadslicing/workloadslicing.go:274** by [@mimowo](https://github.com/mimowo) — 2025-09-10T12:01:09Z
```diff
@@ -263,8 +266,12 @@ func StartWorkloadSlicePods(ctx context.Context, clnt client.Client, object clie
 		return fmt.Errorf("failed to list job pods: %w", err)
 	}
 	for i := range list.Items {
-		if err := clientutil.Patch(ctx, clnt, &list.Items[i], true, func() (bool, error) {
-			return pod.Ungate(&list.Items[i], kueue.ElasticJobSchedulingGate), nil
+		if err := clientutil.Patch(ctx, clnt, &list.Items[i], true, func() (client.Object, error) {
+			updated := pod.Ungate(&list.Items[i], kueue.ElasticJobSchedulingGate)
+			if updated {
+				return &list.Items[i], nil
+			}
+			return nil, nil
```

  Please do then

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-10T12:19:04Z

- **pkg/workloadslicing/workloadslicing.go:274** by [@mszadkow](https://github.com/mszadkow) — 2025-09-10T12:19:03Z
```diff
@@ -263,8 +266,12 @@ func StartWorkloadSlicePods(ctx context.Context, clnt client.Client, object clie
 		return fmt.Errorf("failed to list job pods: %w", err)
 	}
 	for i := range list.Items {
-		if err := clientutil.Patch(ctx, clnt, &list.Items[i], true, func() (bool, error) {
-			return pod.Ungate(&list.Items[i], kueue.ElasticJobSchedulingGate), nil
+		if err := clientutil.Patch(ctx, clnt, &list.Items[i], true, func() (client.Object, error) {
+			updated := pod.Ungate(&list.Items[i], kueue.ElasticJobSchedulingGate)
+			if updated {
+				return &list.Items[i], nil
+			}
+			return nil, nil
```

  Done

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-10T12:19:17Z

- **pkg/util/client/client.go:46** by [@mszadkow](https://github.com/mszadkow) — 2025-09-10T12:19:17Z
```diff
@@ -43,8 +43,8 @@ func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool,
 // PatchStatus applies the merge patch of client.Object status.
 // The resourceVersion will be part of the patch, make this call fail if
 // client.Object was changed.
-func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update func() (bool, error)) error {
-	return updateAndPatch(obj, true, update, func(patch client.Patch) error {
+func PatchStatus(ctx context.Context, c client.Client, strict bool, obj client.Object, update func() (client.Object, error)) error {
```

  OK, will do, removing from here.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-10T12:19:46Z

- **pkg/controller/jobframework/reconciler.go:1090** by [@mszadkow](https://github.com/mszadkow) — 2025-09-10T12:19:46Z
```diff
@@ -1085,8 +1085,9 @@ func (r *JobReconciler) startJob(ctx context.Context, job GenericJob, object cli
 			return err
 		}
 	} else {
-		if err := clientutil.Patch(ctx, r.client, object, true, func() (bool, error) {
-			return true, job.RunWithPodSetsInfo(info)
+		if err := clientutil.Patch(ctx, r.client, object, true, func() (client.Object, error) {
+			err := job.RunWithPodSetsInfo(info)
+			return job.Object(), err
```

  Let's see the CI

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-10T13:24:40Z

- **pkg/controller/jobframework/reconciler.go:1089** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-10T13:24:40Z
```diff
@@ -1085,8 +1085,8 @@ func (r *JobReconciler) startJob(ctx context.Context, job GenericJob, object cli
 			return err
 		}
 	} else {
-		if err := clientutil.Patch(ctx, r.client, object, true, func() (bool, error) {
-			return true, job.RunWithPodSetsInfo(info)
+		if err := clientutil.Patch(ctx, r.client, object, true, func() (client.Object, bool, error) {
+			return job.Object(), true, job.RunWithPodSetsInfo(info)
```

  Since we already retrieve the object from job.Object(), can we reuse it?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-10T13:25:13Z

- **pkg/controller/jobframework/reconciler.go:1089** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-10T13:25:13Z
```diff
@@ -1085,8 +1085,8 @@ func (r *JobReconciler) startJob(ctx context.Context, job GenericJob, object cli
 			return err
 		}
 	} else {
-		if err := clientutil.Patch(ctx, r.client, object, true, func() (bool, error) {
-			return true, job.RunWithPodSetsInfo(info)
+		if err := clientutil.Patch(ctx, r.client, object, true, func() (client.Object, bool, error) {
+			return job.Object(), true, job.RunWithPodSetsInfo(info)
```

  Also in other places.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-10T13:29:19Z

- **pkg/controller/jobframework/reconciler.go:1089** by [@mszadkow](https://github.com/mszadkow) — 2025-09-10T13:29:19Z
```diff
@@ -1085,8 +1085,8 @@ func (r *JobReconciler) startJob(ctx context.Context, job GenericJob, object cli
 			return err
 		}
 	} else {
-		if err := clientutil.Patch(ctx, r.client, object, true, func() (bool, error) {
-			return true, job.RunWithPodSetsInfo(info)
+		if err := clientutil.Patch(ctx, r.client, object, true, func() (client.Object, bool, error) {
+			return job.Object(), true, job.RunWithPodSetsInfo(info)
```

  Yes, we can

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-10T13:37:07Z

- **pkg/controller/jobs/job/job_controller.go:188** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-10T13:37:06Z
```diff
@@ -183,18 +183,18 @@ func (j *Job) Stop(ctx context.Context, c client.Client, podSetsInfo []podset.Po
 
 	// Reset start time if necessary, so we can update the scheduling directives.
 	if j.Status.StartTime != nil {
-		if err := clientutil.PatchStatus(ctx, c, object, func() (bool, error) {
+		if err := clientutil.PatchStatus(ctx, c, object, func() (client.Object, bool, error) {
 			j.Status.StartTime = nil
-			return true, nil
+			return j.Object(), true, nil
```

  Also here 😉

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-10T13:38:38Z

Awesome,, thank you!
/lgtm
/approve

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-10T13:39:44Z

- **pkg/controller/jobs/job/job_controller.go:188** by [@mszadkow](https://github.com/mszadkow) — 2025-09-10T13:39:44Z
```diff
@@ -183,18 +183,18 @@ func (j *Job) Stop(ctx context.Context, c client.Client, podSetsInfo []podset.Po
 
 	// Reset start time if necessary, so we can update the scheduling directives.
 	if j.Status.StartTime != nil {
-		if err := clientutil.PatchStatus(ctx, c, object, func() (bool, error) {
+		if err := clientutil.PatchStatus(ctx, c, object, func() (client.Object, bool, error) {
 			j.Status.StartTime = nil
-			return true, nil
+			return j.Object(), true, nil
```

  hopefully last place

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-10T13:40:07Z

- **pkg/controller/jobs/pod/pod_controller.go:890** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-10T13:40:07Z
```diff
@@ -885,10 +885,10 @@ func (p *Pod) removeExcessPods(ctx context.Context, c client.Client, r record.Ev
 	// Finalize and delete the active pods created last
 	err := parallelize.Until(ctx, len(extraPods), func(i int) error {
 		pod := extraPods[i]
-		if err := clientutil.Patch(ctx, c, &pod, false, func() (bool, error) {
+		if err := clientutil.Patch(ctx, c, &pod, false, func() (client.Object, bool, error) {
 			removed := controllerutil.RemoveFinalizer(&pod, podconstants.PodFinalizer)
 			log.V(3).Info("Finalizing excess pod in group", "excessPod", klog.KObj(&pod))
```

  I’m wondering if we should log only when it’s removed.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-10T13:40:46Z

- **pkg/controller/jobs/pod/pod_controller.go:890** by [@mszadkow](https://github.com/mszadkow) — 2025-09-10T13:40:46Z
```diff
@@ -885,10 +885,10 @@ func (p *Pod) removeExcessPods(ctx context.Context, c client.Client, r record.Ev
 	// Finalize and delete the active pods created last
 	err := parallelize.Until(ctx, len(extraPods), func(i int) error {
 		pod := extraPods[i]
-		if err := clientutil.Patch(ctx, c, &pod, false, func() (bool, error) {
+		if err := clientutil.Patch(ctx, c, &pod, false, func() (client.Object, bool, error) {
 			removed := controllerutil.RemoveFinalizer(&pod, podconstants.PodFinalizer)
 			log.V(3).Info("Finalizing excess pod in group", "excessPod", klog.KObj(&pod))
```

  If so then it's outside of this PR scope

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-10T13:41:17Z

- **pkg/controller/jobs/pod/pod_controller.go:890** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-10T13:41:10Z
```diff
@@ -885,10 +885,10 @@ func (p *Pod) removeExcessPods(ctx context.Context, c client.Client, r record.Ev
 	// Finalize and delete the active pods created last
 	err := parallelize.Until(ctx, len(extraPods), func(i int) error {
 		pod := extraPods[i]
-		if err := clientutil.Patch(ctx, c, &pod, false, func() (bool, error) {
+		if err := clientutil.Patch(ctx, c, &pod, false, func() (client.Object, bool, error) {
 			removed := controllerutil.RemoveFinalizer(&pod, podconstants.PodFinalizer)
 			log.V(3).Info("Finalizing excess pod in group", "excessPod", klog.KObj(&pod))
```

  This is not related for current PR. we can change it in follow up PR.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-10T13:44:59Z

- **pkg/controller/jobs/pod/pod_controller.go:930** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-10T13:44:59Z
```diff
@@ -925,10 +925,10 @@ func (p *Pod) finalizePods(ctx context.Context, c client.Client, extraPods []cor
 	err := parallelize.Until(ctx, len(extraPods), func(i int) error {
 		pod := extraPods[i]
 		var removed bool
-		if err := clientutil.Patch(ctx, c, &pod, false, func() (bool, error) {
+		if err := clientutil.Patch(ctx, c, &pod, false, func() (client.Object, bool, error) {
 			removed = controllerutil.RemoveFinalizer(&pod, podconstants.PodFinalizer)
 			log.V(3).Info("Finalizing pod in group", "Pod", klog.KObj(&pod))
```

  Also here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-10T14:53:39Z

- **pkg/controller/jobs/pod/pod_controller.go:930** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-10T14:53:39Z
```diff
@@ -925,10 +925,10 @@ func (p *Pod) finalizePods(ctx context.Context, c client.Client, extraPods []cor
 	err := parallelize.Until(ctx, len(extraPods), func(i int) error {
 		pod := extraPods[i]
 		var removed bool
-		if err := clientutil.Patch(ctx, c, &pod, false, func() (bool, error) {
+		if err := clientutil.Patch(ctx, c, &pod, false, func() (client.Object, bool, error) {
 			removed = controllerutil.RemoveFinalizer(&pod, podconstants.PodFinalizer)
 			log.V(3).Info("Finalizing pod in group", "Pod", klog.KObj(&pod))
```

  https://github.com/kubernetes-sigs/kueue/pull/6783

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-10T14:53:44Z

- **pkg/controller/jobs/pod/pod_controller.go:890** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-10T14:53:44Z
```diff
@@ -885,10 +885,10 @@ func (p *Pod) removeExcessPods(ctx context.Context, c client.Client, r record.Ev
 	// Finalize and delete the active pods created last
 	err := parallelize.Until(ctx, len(extraPods), func(i int) error {
 		pod := extraPods[i]
-		if err := clientutil.Patch(ctx, c, &pod, false, func() (bool, error) {
+		if err := clientutil.Patch(ctx, c, &pod, false, func() (client.Object, bool, error) {
 			removed := controllerutil.RemoveFinalizer(&pod, podconstants.PodFinalizer)
 			log.V(3).Info("Finalizing excess pod in group", "excessPod", klog.KObj(&pod))
```

  https://github.com/kubernetes-sigs/kueue/pull/6783
