# PR #6785: Populate admissionChecks with all available flavors rather than implicitly assume empty list implies all flavors.

**Summary**: Populate admissionChecks with all available flavors rather than implicitly assume empty list implies all flavors.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6785

**Last updated**: 2025-10-14T14:21:38Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ganczak-commits](https://github.com/ganczak-commits)
- **Branch**: `all-flav-2` → `main`
- **Created**: 2025-09-10T16:56:39Z
- **Updated**: 2025-10-14T14:21:38Z
- **Merged**: 2025-10-14T14:21:36Z
- **Closed**: 2025-10-14T14:21:36Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@gabesaba](https://github.com/gabesaba), [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 10   **+105 / -27**

## Description

#### What type of PR is this?

/kind cleanup

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
Fixes #5475

#### Special notes for your reviewer:
The code change requested in [the issue](https://github.com/kubernetes-sigs/kueue/issues/5475) had a side-effect in some places in code where it was assumed "all flavors apply if the flavor list is empty". This assumption was no longer true after the requested update, so I changed the check to be "all flavors apply if the flavor list is of equal length to the number of possible flavors".
Some caveats related to this:
- I tried to cover all the places in code where such checks are made - however it was not entirely trivial to identify them. Preferably someone more knowledgeable about the code base should double check.
- I wasn't sure about the cleanest way to check whether all flavors were requested. I thought about adding a util e.g. in admissioncheck.go, however importing ResourceGroup from resource.go in that file would result in a cyclic dependency. So instead, I opted to calculate the number inline using a `slices.reduce()` method I added in utils.
- I updated workload_test.go, so that the mocked resource groups contain 2 flavors, so that there can be a distinction between an admission check applied to '1' flavor vs 'all' flavors.

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-09-10T16:56:46Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 3881e73f30e9904aede8e3a66d9b2eee53e8d40e |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68ee40a2368af300082c4384 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-10T16:56:49Z

Hi @ganczak-commits. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-18T15:24:49Z

/ok-to-test
/assign @PBundyra 
can you give it a pass?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-18T16:01:59Z

@ganczak-commits seems like the e2e multiKueue tests are failing for a reason. It would be good to understand and add some unit tests for early catching of the issues.

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-09-22T08:22:32Z

> @ganczak-commits seems like the e2e multiKueue tests are failing for a reason. It would be good to understand and add some unit tests for early catching of the issues.

Once you resolve it, please ping me so I can proceed with the review

### Comment by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-09-30T06:21:04Z

/retest

### Comment by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-09-30T06:27:05Z

@mimowo @PBundyra Sorry, when testing locally I missed that there's multiple varieties of e2e tests and only ran `make test-e2e`. The tests should be passing now, PTAL

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-30T07:11:02Z

@ganczak-commits since the issue was dicovered late by MultiKueue e2e tests I would like to make sure we now have lower level tests like unit or integration which would fail earlier if not for the fix. if this is much work we can consider in a follow up

### Comment by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-09-30T07:27:10Z

@mimowo Makes sense. I'll start working on a test, though - should that be a part of this PR or a separate one? Feels to me more like the latter, but let me know what you think.

### Comment by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-09-30T07:45:14Z

@mimowo Ok, actually maybe first I'll try to come up with a test and we can see how related it is to the rest of this PR

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-30T07:48:32Z

Thank you for the changes, it really makes it simpler conceptually. Left some nit comments

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-30T07:49:59Z

> @mimowo Makes sense. I'll start working on a test, though - should that be a part of this PR or a separate one? Feels to me more like the latter, but let me know what you think.

That has no obvious answer. I'm leaning to say part of the PR, but I'm ok to revisit this opinion in some contexts. Could you explain what specifically was the issue in the initial implemantation?

### Comment by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-09-30T10:12:24Z

> > @mimowo Makes sense. I'll start working on a test, though - should that be a part of this PR or a separate one? Feels to me more like the latter, but let me know what you think.
> 
> That has no obvious answer. I'm leaning to say part of the PR, but I'm ok to revisit this opinion in some contexts. Could you explain what specifically was the issue in the initial implemantation?

AFAICT we don't have any unit test that covers [`AdmissionChecksForClusterQueue`](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/cache/scheduler/cache.go#L309) method being called with `AdmissionCheckStrategy` rather than `AdmissionChecks`. This additional case does pick up on my previous bug PTAL: https://github.com/kubernetes-sigs/kueue/commit/690611511a5b1aaef7db07d68bb51895a061602d

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-30T11:03:21Z

> AFAICT we don't have any unit test that covers AdmissionChecksForClusterQueue method being called with AdmissionCheckStrategy rather than AdmissionChecks. This additional case does pick up on my previous bug PTAL: 6906115

The new test looks ok, but I'm not clear this would indeed capture the MultiKueue e2e test failures reported in https://github.com/kubernetes-sigs/kueue/pull/6785#issuecomment-3308303197. I think this is the build: https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/6785/pull-kueue-test-e2e-multikueue-main/1972665234590011392 

I have a doubt becuase the new unit test is testing AdmissionCheckStrategy, but the MultiKueue e2e tests were failing using regular AdmissionChecks. I was checking here: 
https://github.com/kubernetes-sigs/kueue/blob/main/test/e2e/multikueue/e2e_test.go

cc @PBundyra

### Comment by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-09-30T11:30:59Z

> The new test looks ok, but I'm not clear this would indeed capture the MultiKueue e2e test failures reported in [#6785 (comment)](https://github.com/kubernetes-sigs/kueue/pull/6785#issuecomment-3308303197).

FWIW I ran the new test case against the commit from before my fix, and it failed like so:
```
--- FAIL: TestDefault (0.02s)
    --- FAIL: TestDefault/should_set_managedBy_to_multiKueue_if_the_user_didn't_specify_any_(with_admission_strategy) (0.00s)
        /Users/ganczak/Code/kueue/pkg/controller/jobs/kubeflow/trainjob/trainjob_webhook_test.go:225: Default() mismatch (-want,+got):
              &v1alpha1.TrainJob{
              	TypeMeta:   {},
              	ObjectMeta: {Name: "trainjob", Namespace: "ns", Labels: {"kueue.x-k8s.io/queue-name": "local-queue"}},
              	Spec: v1alpha1.TrainJobSpec{
              		... // 5 identical fields
              		PodSpecOverrides: nil,
              		Suspend:          &true,
            - 		ManagedBy:        &"kueue.x-k8s.io/multikueue",
            + 		ManagedBy:        nil,
              	},
              	Status: {},
              }
FAIL
FAIL	sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/trainjob	0.764s
FAIL
```

### Comment by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-10-07T12:10:51Z

/retest

### Comment by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-10-14T05:25:33Z

/retest

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-10-14T07:35:16Z

/lgtm
Thanks @ganczak-commits 
cc @mimowo

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-14T07:35:24Z

LGTM label has been added.  <details>Git tree hash: 093c47948b535c6c69a6de79aa8e1f149f461ba8</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-14T08:48:13Z

/hold
Let's make sure we understand the impact of the PR first, as it does seem to have user-facing impact (not just cleanup), the [thread](https://github.com/kubernetes-sigs/kueue/pull/6785#discussion_r2428241242)

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-14T13:20:54Z

LGTM label has been added.  <details>Git tree hash: 2dda219163de3d6d592c6c1c4222e680e3c77dd7</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-14T13:20:56Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6785#" title="Author self-approved">ganczak-commits</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6785#pullrequestreview-3335610612" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-14T13:55:19Z

/unhold

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-30T07:47:34Z

- **pkg/util/admissioncheck/admissioncheck.go:180** by [@mimowo](https://github.com/mimowo) — 2025-09-30T07:47:34Z
```diff
@@ -177,6 +177,17 @@ func NewAdmissionChecks(cq *kueue.ClusterQueue) map[kueue.AdmissionCheckReferenc
 			checks[checkName] = sets.New[kueue.ResourceFlavorReference]()
 		}
 	}
+	// In case an AdmissionCheck empty, it should apply to all ResourceFlavors.
```

  This could be moved under the "else" branch only? Then we could eliminate the `if len(check) != 0 {` check?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-30T07:47:49Z

- **pkg/controller/core/workload_controller.go:551** by [@mimowo](https://github.com/mimowo) — 2025-09-30T07:47:49Z
```diff
@@ -548,7 +548,8 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 
 func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, wl *kueue.Workload, cq *kueue.ClusterQueue) (bool, error) {
 	log := ctrl.LoggerFrom(ctx)
-	admissionChecks := workload.AdmissionChecksForWorkload(log, wl, admissioncheck.NewAdmissionChecks(cq))
+	numAllFlavors := utilslices.Reduce(cq.Spec.ResourceGroups, func(acc int, rg kueue.ResourceGroup) int { return acc + len(rg.Flavors) }, 0)
```

  Nice trick, but I think we should have a helper function to better capture the intention, and reuse it from all places

### Review by [@ganczak-commits](https://github.com/ganczak-commits) (commented) — 2025-09-30T11:47:11Z

- **pkg/controller/core/workload_controller.go:551** by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-09-30T11:47:11Z
```diff
@@ -548,7 +548,8 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 
 func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, wl *kueue.Workload, cq *kueue.ClusterQueue) (bool, error) {
 	log := ctrl.LoggerFrom(ctx)
-	admissionChecks := workload.AdmissionChecksForWorkload(log, wl, admissioncheck.NewAdmissionChecks(cq))
+	numAllFlavors := utilslices.Reduce(cq.Spec.ResourceGroups, func(acc int, rg kueue.ResourceGroup) int { return acc + len(rg.Flavors) }, 0)
```

  Can you suggest the best place for it? I did try that, but ran into some circular reference issues

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-30T11:57:45Z

- **pkg/controller/core/workload_controller.go:551** by [@mimowo](https://github.com/mimowo) — 2025-09-30T11:57:45Z
```diff
@@ -548,7 +548,8 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 
 func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, wl *kueue.Workload, cq *kueue.ClusterQueue) (bool, error) {
 	log := ctrl.LoggerFrom(ctx)
-	admissionChecks := workload.AdmissionChecksForWorkload(log, wl, admissioncheck.NewAdmissionChecks(cq))
+	numAllFlavors := utilslices.Reduce(cq.Spec.ResourceGroups, func(acc int, rg kueue.ResourceGroup) int { return acc + len(rg.Flavors) }, 0)
```

  Maybe https://github.com/kubernetes-sigs/kueue/tree/main/pkg/util/queue, just new file cluster_queue.go
  , wdyt?

### Review by [@ganczak-commits](https://github.com/ganczak-commits) (commented) — 2025-09-30T13:30:49Z

- **pkg/controller/core/workload_controller.go:551** by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-09-30T13:30:49Z
```diff
@@ -548,7 +548,8 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 
 func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, wl *kueue.Workload, cq *kueue.ClusterQueue) (bool, error) {
 	log := ctrl.LoggerFrom(ctx)
-	admissionChecks := workload.AdmissionChecksForWorkload(log, wl, admissioncheck.NewAdmissionChecks(cq))
+	numAllFlavors := utilslices.Reduce(cq.Spec.ResourceGroups, func(acc int, rg kueue.ResourceGroup) int { return acc + len(rg.Flavors) }, 0)
```

  It's also causing an import cycle AFAICT.
  
  For the file pkg/util/queue/cluster_queue.go:
  ```
  package queue
  
  import (
  	"sigs.k8s.io/kueue/pkg/cache/scheduler"
  	utilslices "sigs.k8s.io/kueue/pkg/util/slices"
  )
  
  func AllFlavorsCount(resourceGroups []scheduler.ResourceGroup) int {
  	return utilslices.Reduce(resourceGroups, func(acc int, rg scheduler.ResourceGroup) int { return acc + len(rg.Flavors) }, 0)
  }
  ```
  
  Importing `"sigs.k8s.io/kueue/pkg/cache/scheduler"` results in an error `import cycle not allowed`

### Review by [@ganczak-commits](https://github.com/ganczak-commits) (commented) — 2025-09-30T13:31:06Z

- **pkg/util/admissioncheck/admissioncheck.go:180** by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-09-30T13:31:06Z
```diff
@@ -177,6 +177,17 @@ func NewAdmissionChecks(cq *kueue.ClusterQueue) map[kueue.AdmissionCheckReferenc
 			checks[checkName] = sets.New[kueue.ResourceFlavorReference]()
 		}
 	}
+	// In case an AdmissionCheck empty, it should apply to all ResourceFlavors.
```

  Hmm but aren't we supposed to cover all flavors if the list is empty for admission check _strategy_, too? That's how I interpret [this comment](https://github.com/kubernetes-sigs/kueue/blob/50b7ed542f9aef732cdbf95eb798f18308a577cb/apis/kueue/v1beta1/clusterqueue_types.go#L161).
  
  If I move this code to `else` branch only, test cases with an empty admission check strategy start failing, e.g.:
  ```
  === FAIL: pkg/workload TestAdmissionCheckStrategy/AdmissionCheckStrategy_without_a_flavor (0.00s)
      workload_test.go:916: Unexpected AdmissionChecks, (want-/got+):
            sets.Set[sigs.k8s.io/kueue/apis/kueue/v1beta1.AdmissionCheckReference](
          - 	{"ac1": {}},
          + 	{},
            )
  
  === FAIL: pkg/workload TestAdmissionCheckStrategy/Two_AdmissionCheckStrategies,_one_with_flavor,_one_without_flavor (0.00s)
      workload_test.go:916: Unexpected AdmissionChecks, (want-/got+):
            sets.Set[sigs.k8s.io/kueue/apis/kueue/v1beta1.AdmissionCheckReference](
          - 	{"ac1": {}, "ac2": {}},
          + 	{"ac1": {}},
            )
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (changes_requested) — 2025-10-01T11:11:20Z

- **pkg/util/admissioncheck/admissioncheck.go:180** by [@PBundyra](https://github.com/PBundyra) — 2025-10-01T11:09:03Z
```diff
@@ -177,6 +177,17 @@ func NewAdmissionChecks(cq *kueue.ClusterQueue) map[kueue.AdmissionCheckReferenc
 			checks[checkName] = sets.New[kueue.ResourceFlavorReference]()
 		}
 	}
+	// In case an AdmissionCheck empty, it should apply to all ResourceFlavors.
```

  > Hmm but aren't we supposed to cover all flavors if the list is empty for admission check _strategy_, too? That's how I interpret [this comment](https://github.com/kubernetes-sigs/kueue/blob/50b7ed542f9aef732cdbf95eb798f18308a577cb/apis/kueue/v1beta1/clusterqueue_types.go#L161).
  
  That's correct. Can we move the code responsible for populating the check with all flavors, somewhere around here: https://github.com/kubernetes-sigs/kueue/pull/6785/files#diff-494d796452c1aecae4f66bb2cb2e9ad94396b0c348168969fc1b4639ad6c768fR171-R173 ?
  
  So the code is easier to follow and we have AdmissionCheckStrategy case handled in one place?

- **pkg/workload/workload.go:1191** by [@PBundyra](https://github.com/PBundyra) — 2025-10-01T11:11:07Z
  I wonder if we need this code at all, to me it's seems like a bit of micro optimization which we could avoid for the sake of better readability, wdyt @mimowo?
  
  @ganczak-commits Can you verify if tests pass with this part deleted?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-10-01T11:19:20Z

- **pkg/util/admissioncheck/admissioncheck.go:180** by [@PBundyra](https://github.com/PBundyra) — 2025-10-01T11:19:20Z
```diff
@@ -177,6 +177,17 @@ func NewAdmissionChecks(cq *kueue.ClusterQueue) map[kueue.AdmissionCheckReferenc
 			checks[checkName] = sets.New[kueue.ResourceFlavorReference]()
 		}
 	}
+	// In case an AdmissionCheck empty, it should apply to all ResourceFlavors.
```

  Oh, actually we want to cover both AdmissionCheck and AdmissionCheckStrategy cases so it makes sense to keep it here

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-10-01T11:24:41Z

- **pkg/util/admissioncheck/admissioncheck.go:180** by [@PBundyra](https://github.com/PBundyra) — 2025-10-01T11:24:41Z
```diff
@@ -177,6 +177,17 @@ func NewAdmissionChecks(cq *kueue.ClusterQueue) map[kueue.AdmissionCheckReferenc
 			checks[checkName] = sets.New[kueue.ResourceFlavorReference]()
 		}
 	}
+	// In case an AdmissionCheck empty, it should apply to all ResourceFlavors.
```

  I think the cleanest solution would be to have a list that stores all the flavors, and modify this function in following manner:
  
  
  ```
  if admissionCheckStrategy != nil {
      iterate over checks from admissionChecksStrategy and assign either OnFlavors or if it's empty, then assign all flavors
  }
  
  iterate over checks from admissionChecks and assign all flavors to all checks
  ```

### Review by [@ganczak-commits](https://github.com/ganczak-commits) (commented) — 2025-10-02T08:57:51Z

- **pkg/workload/workload.go:1191** by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-10-02T08:57:50Z
  Interestingly, 3 integration test cases fail:
  ```
    [FAIL] Workload controller when the queue has admission checks [It] the workload should get the AdditionalChecks added
    /usr/local/google/home/ganczak/Code/kueue/test/integration/singlecluster/controller/core/workload_controller_test.go:219
    [FAIL] Workload controller when the queue has admission checks [It] should finish an unadmitted workload with failure when a check is rejected
    /usr/local/google/home/ganczak/Code/kueue/test/integration/singlecluster/controller/core/workload_controller_test.go:276
    [FAIL] Workload controller when the queue has admission checks [It] should evict then finish with failure an admitted workload when a check is rejected
    /usr/local/google/home/ganczak/Code/kueue/test/integration/singlecluster/controller/core/workload_controller_test.go:330
  ```
  
  To be sure it's not due to my changes in this PR, I double checked, and the same thing seems to happen on a clean branch, too.
  
  I'm attaching two logs of `the workload should get the AdditionalChecks added`: the failing run with the part deleted and, for comparison, a log of a passing run with the part intact:
  
  - [bad log.txt](https://github.com/user-attachments/files/22656320/bad.log.txt)
  - [good log.txt](https://github.com/user-attachments/files/22656321/good.log.txt)

### Review by [@ganczak-commits](https://github.com/ganczak-commits) (commented) — 2025-10-02T09:14:19Z

- **pkg/util/admissioncheck/admissioncheck.go:180** by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-10-02T09:14:19Z
```diff
@@ -177,6 +177,17 @@ func NewAdmissionChecks(cq *kueue.ClusterQueue) map[kueue.AdmissionCheckReferenc
 			checks[checkName] = sets.New[kueue.ResourceFlavorReference]()
 		}
 	}
+	// In case an AdmissionCheck empty, it should apply to all ResourceFlavors.
```

  Do you mean like so?
  ```
  func NewAdmissionChecks(cq *kueue.ClusterQueue) map[kueue.AdmissionCheckReference]sets.Set[kueue.ResourceFlavorReference] {
  	var checks map[kueue.AdmissionCheckReference]sets.Set[kueue.ResourceFlavorReference]
  	if cq.Spec.AdmissionChecksStrategy != nil {
  		checks = make(map[kueue.AdmissionCheckReference]sets.Set[kueue.ResourceFlavorReference], len(cq.Spec.AdmissionChecksStrategy.AdmissionChecks))
  		for _, check := range cq.Spec.AdmissionChecksStrategy.AdmissionChecks {
  			if len(check.OnFlavors) > 0 {
  				checks[check.Name] = sets.New(check.OnFlavors...)
  			} else {
  				checks[check.Name] = allFlavors(cq)
  			}
  		}
  	} else {
  		checks = make(map[kueue.AdmissionCheckReference]sets.Set[kueue.ResourceFlavorReference], len(cq.Spec.AdmissionChecks))
  		for _, checkName := range cq.Spec.AdmissionChecks {
  			checks[checkName] = allFlavors(cq)
  		}
  	}
  	return checks
  }
  
  func allFlavors(cq *kueue.ClusterQueue) sets.Set[kueue.ResourceFlavorReference] {
  	flavors := sets.New[kueue.ResourceFlavorReference]()
  	for _, rg := range cq.Spec.ResourceGroups {
  		for _, fv := range rg.Flavors {
  			flavors.Insert(fv.Name)
  		}
  	}
  	return flavors
  }
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-02T09:18:26Z

- **pkg/workload/workload.go:1191** by [@mimowo](https://github.com/mimowo) — 2025-10-02T09:18:26Z
  > I wonder if we need this code at all, to me it's seems like a bit of micro optimization which we could avoid for the sake of better readability, wdyt @mimowo?
  
  +1, it feels like code to simplify. It feels the original code returns all-or-nothing depending if enabled for all flavors.
  
  > To be sure it's not due to my changes in this PR, I double checked, and the same thing seems to happen on a clean branch, too.
  
  Do you mean these tests are flaking on the main branch?

### Review by [@ganczak-commits](https://github.com/ganczak-commits) (commented) — 2025-10-02T09:21:03Z

- **pkg/workload/workload.go:1191** by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-10-02T09:21:03Z
  > Do you mean these tests are flaking on the main branch?
  
  Sorry, I meant that if I delete this part, then the 3 test cases fail - both for this PR, and on the main branch. Meaning that this code seems to be impactful and not just a microoptimization, and also not because of any changes I'm making here.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-02T09:56:05Z

- **pkg/controller/core/workload_controller.go:551** by [@mimowo](https://github.com/mimowo) — 2025-10-02T09:56:05Z
```diff
@@ -548,7 +548,8 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 
 func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, wl *kueue.Workload, cq *kueue.ClusterQueue) (bool, error) {
 	log := ctrl.LoggerFrom(ctx)
-	admissionChecks := workload.AdmissionChecksForWorkload(log, wl, admissioncheck.NewAdmissionChecks(cq))
+	numAllFlavors := utilslices.Reduce(cq.Spec.ResourceGroups, func(acc int, rg kueue.ResourceGroup) int { return acc + len(rg.Flavors) }, 0)
```

  I'm surprised the `ResourceGroups` type is under the scheduler package, kueue.ResourceGroup.
  
  In that case we may need to have two helpers: one in the pkg/util/queue for the consumers using kueue.ResourceGroup, and inside scheduler for users of scheduler.ResourceGroup.

### Review by [@ganczak-commits](https://github.com/ganczak-commits) (commented) — 2025-10-04T11:17:53Z

- **pkg/controller/core/workload_controller.go:551** by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-10-04T11:17:53Z
```diff
@@ -548,7 +548,8 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 
 func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, wl *kueue.Workload, cq *kueue.ClusterQueue) (bool, error) {
 	log := ctrl.LoggerFrom(ctx)
-	admissionChecks := workload.AdmissionChecksForWorkload(log, wl, admissioncheck.NewAdmissionChecks(cq))
+	numAllFlavors := utilslices.Reduce(cq.Spec.ResourceGroups, func(acc int, rg kueue.ResourceGroup) int { return acc + len(rg.Flavors) }, 0)
```

  SG, defined 2 helpers, PTAL

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-10-06T08:18:01Z

- **pkg/cache/scheduler/resource.go:86** by [@PBundyra](https://github.com/PBundyra) — 2025-10-06T07:51:00Z
```diff
@@ -73,3 +74,7 @@ func createResourceQuotas(kueueRgs []kueue.ResourceGroup) map[resources.FlavorRe
 	}
 	return quotas
 }
+
+func NumAllFlavors(rgs []ResourceGroup) int {
+	return utilslices.Reduce(rgs, func(acc int, rg ResourceGroup) int { return acc + len(rg.Flavors) }, 0)
+}
```

  Is this the same function as in `util/queue/cluster_queue.go`? Can we not repeat ourselves and keep only one replica of the function?

- **pkg/util/admissioncheck/admissioncheck.go:180** by [@PBundyra](https://github.com/PBundyra) — 2025-10-06T08:08:42Z
```diff
@@ -177,6 +177,17 @@ func NewAdmissionChecks(cq *kueue.ClusterQueue) map[kueue.AdmissionCheckReferenc
 			checks[checkName] = sets.New[kueue.ResourceFlavorReference]()
 		}
 	}
+	// In case an AdmissionCheck empty, it should apply to all ResourceFlavors.
```

  Exactly

- **pkg/workload/workload.go:1191** by [@PBundyra](https://github.com/PBundyra) — 2025-10-06T08:17:53Z
  Can we get to the bottom on why those tests fail with the changes proposed in this PR? It feels like they shouldn't, so I'm afraid we have some bug

### Review by [@ganczak-commits](https://github.com/ganczak-commits) (commented) — 2025-10-06T08:54:44Z

- **pkg/cache/scheduler/resource.go:86** by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-10-06T08:54:43Z
```diff
@@ -73,3 +74,7 @@ func createResourceQuotas(kueueRgs []kueue.ResourceGroup) map[resources.FlavorRe
 	}
 	return quotas
 }
+
+func NumAllFlavors(rgs []ResourceGroup) int {
+	return utilslices.Reduce(rgs, func(acc int, rg ResourceGroup) int { return acc + len(rg.Flavors) }, 0)
+}
```

  It is; PTAL the discussion: https://github.com/kubernetes-sigs/kueue/pull/6785#discussion_r2391478805

### Review by [@ganczak-commits](https://github.com/ganczak-commits) (commented) — 2025-10-06T08:58:04Z

- **pkg/workload/workload.go:1191** by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-10-06T08:58:04Z
  > why those tests fail with the changes proposed in this PR
  
  To be clear, the same tests also fail _on a clean branch from main_ if I remove this code. But still, I'll try to understand why this code is needed

### Review by [@ganczak-commits](https://github.com/ganczak-commits) (commented) — 2025-10-06T09:59:03Z

- **pkg/workload/workload.go:1191** by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-10-06T09:59:03Z
  Ah, so it's because without the preceding code, the [`if wl.Status.Admission == nil` condition](https://github.com/kubernetes-sigs/kueue/blob/8c55e04b6736bff70d3bde70289bf4b95eb7af3c/pkg/workload/workload.go#L1196) "wins", and the admission checks are never updated in the workload.
  
  The 3 tests in `Workload controller when the queue has admission checks` _start passing_ again if I comment out even more code in `func AdmissionChecksForWorkload`:
  ```
  	/*allFlavors := true
  	for _, flavors := range admissionChecks {
  		if len(flavors) != 0 {
  			allFlavors = false
  		}
  	}
  	if allFlavors {
  		return sets.New(slices.Collect(maps.Keys(admissionChecks))...)
  	}*/
  
  	/*if wl.Status.Admission == nil {
  		log.V(2).Info("Workload has no Admission", "Workload", klog.KObj(wl))
  		return nil
  	}*/
  
  	var assignedFlavors []kueue.ResourceFlavorReference
  	/*for _, podSet := range wl.Status.Admission.PodSetAssignments {
  		for _, flavor := range podSet.Flavors {
  			assignedFlavors = append(assignedFlavors, flavor)
  		}
  	}*/
  ```
  
  So, this seems to me like a bug? Shouldn't the line `if wl.Status.Admission == nil` happen before `if allFlavors`?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-10-06T10:07:54Z

- **pkg/cache/scheduler/resource.go:86** by [@PBundyra](https://github.com/PBundyra) — 2025-10-06T09:19:58Z
```diff
@@ -73,3 +74,7 @@ func createResourceQuotas(kueueRgs []kueue.ResourceGroup) map[resources.FlavorRe
 	}
 	return quotas
 }
+
+func NumAllFlavors(rgs []ResourceGroup) int {
+	return utilslices.Reduce(rgs, func(acc int, rg ResourceGroup) int { return acc + len(rg.Flavors) }, 0)
+}
```

  IIUC from the discussion, the functions actually differ since they accept a parameter with different type. In that case I'm fine with the change, feel free to resolve

- **pkg/workload/workload.go:1191** by [@PBundyra](https://github.com/PBundyra) — 2025-10-06T10:07:30Z
  Thanks for the investigation, so it seems like the issue boils down to the test structure - we should only check admission checks once the Workload has a flavor assigned. Could you delete this code and update the failing integration tests?

### Review by [@ganczak-commits](https://github.com/ganczak-commits) (commented) — 2025-10-07T14:46:22Z

- **pkg/workload/workload.go:1191** by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-10-07T14:46:22Z
  Ok, I deleted the microoptimization and updated the tests. Currently I'm down to 3 failing, and one in particular seems tricky to me: [test run](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/6785/pull-kueue-test-integration-multikueue-main/1975539260094156800). To be clear, the same failure is happening if I delete the microoptimization _on the main branch_.
  
  What's happening is that the admissionCheck [here](https://github.com/kubernetes-sigs/kueue/blob/dc263e01760ce118ee580a4a6816dae6fbba962b/test/integration/multikueue/jobs_test.go#L1442) is `Pending` just as the test expects - but then it changes to `nil` because [this code](https://github.com/kubernetes-sigs/kueue/blob/dc263e01760ce118ee580a4a6816dae6fbba962b/pkg/workload/workload.go#L1196-L1199) is now executed (previously the microoptimization was returning the function a few lines above). The test is failing, because its check happens already after `Pending` has changed into `nil`.
  
  What's worse, even if I comment out [this expectation](https://github.com/kubernetes-sigs/kueue/blob/dc263e01760ce118ee580a4a6816dae6fbba962b/test/integration/multikueue/jobs_test.go#L1441-L1450), the test is failing a [couple lines below](https://github.com/kubernetes-sigs/kueue/blob/dc263e01760ce118ee580a4a6816dae6fbba962b/test/integration/multikueue/jobs_test.go#L1478), as `worker2 wl` is never removed, and this seems like a genuine change in functionality.
  
  I wonder, is that expected? It seems like losing the connection sets the `Admission` to nil. I can see that [`UnsetQuotaReservationWithCondition`](https://github.com/kubernetes-sigs/kueue/blob/dc263e01760ce118ee580a4a6816dae6fbba962b/pkg/controller/jobframework/reconciler.go#L540) is getting triggered. Is this WAI?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-10-08T13:48:13Z

- **pkg/workload/workload.go:1191** by [@PBundyra](https://github.com/PBundyra) — 2025-10-08T13:48:13Z
  Thanks for the explanation, I think we're facing a bug with MultiKueue:
  One of the MultiKueue constraints is that it always applies to all RFs in a CQ. In theory a Workload submitted to such a CQ doesn't need admission (flavor assignment) because regardless of the flavor it would always use the MultiKueue AdmissionCheck - this is what happens [here](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/workload/workload.go#L1180-L1191) - before we check for admission, if all AdmissionChecks applies to all RFs in a CQ it returns a list of the AdmissionChecks. It works correctly if MultiKueue is the only AdmissionCheck in a CQ, but iiuc it would break if there was combination of MultiKueue and an AdmissionCheck that should be run only on part of the flavors. Then the code would return an empty list of AdmissionChecks, even there should be the MultiKueue one (according to its assumption)
  
  Since this is a separate issue I'd like to tackle it in a separate PR by. Let's revert the changes related to the linked part of code and only update [this if](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/workload/workload.go#L1185) as it was done in previous commits.
  
  The follow up PR should consist of:
  1) Create an integration test similar to the one that currently fails (with deletion of the linked part of the code) and add AdmissionCheckStrategy to it, with an AdmissionCheck defined only for a one out of two flavors in the CQ - this should prove that there's indeed a bug. No need to change anything in the production code, let's just add another integration test.
  2) When we prove that there's indeed a bug, fix the linked part of the code so that whenever there's a MultiKueue AdmissionCheck it should always be returned, regardless of different AdmissionChecks
  
  @ganczak-commits Can you please create a ticket for what I've just described and follow up on that in a separate PR?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-10-09T08:26:33Z

- **pkg/workload/workload.go:1191** by [@PBundyra](https://github.com/PBundyra) — 2025-10-09T08:26:33Z
  Please investigate also what's the flow between losing a connection to worker cluster and reseting Workload's admission. Does MultiKueue controller sets the AdmissionCheck to Rejected or maybe it directly evicts the code? Those question might be helpful to understand/confirm a potential bug

### Review by [@ganczak-commits](https://github.com/ganczak-commits) (commented) — 2025-10-14T06:15:20Z

- **pkg/workload/workload.go:1191** by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-10-14T06:15:20Z
  I filed github.com/kubernetes-sigs/kueue/pull/7117 PTAL

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T07:53:43Z

- **pkg/workload/workload_test.go:848** by [@mimowo](https://github.com/mimowo) — 2025-10-14T07:53:43Z
```diff
@@ -844,6 +845,7 @@ func TestAdmissionCheckStrategy(t *testing.T) {
 					Obj()).
 				Obj(),
 			cq: utiltesting.MakeClusterQueue("cq").
+				ResourceGroup(*utiltesting.MakeFlavorQuotas("flavor1").Obj(), *utiltesting.MakeFlavorQuotas("flavor2").Obj()).
```

  This one is interesting, it looks like user-facing change actually, is it now required, or you just added for "clarity"?
  
  It seems we didn't have validation on `AdmissionCheckStrategy.OnFlavors` to be listed in the CQ. It looks like omission, wdyt @PBundyra ?
  
  I'm wondering if we should still aim for adding the validation. To guide the decision I would like to understand better how Kueue would behave if the flavors were not specified in 0.14, and after this PR.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-10-14T08:21:30Z

- **pkg/workload/workload_test.go:848** by [@PBundyra](https://github.com/PBundyra) — 2025-10-14T08:21:25Z
```diff
@@ -844,6 +845,7 @@ func TestAdmissionCheckStrategy(t *testing.T) {
 					Obj()).
 				Obj(),
 			cq: utiltesting.MakeClusterQueue("cq").
+				ResourceGroup(*utiltesting.MakeFlavorQuotas("flavor1").Obj(), *utiltesting.MakeFlavorQuotas("flavor2").Obj()).
```

  @ganczak-commits Are those changes still necessary after we reverted deletion of this code: https://github.com/kubernetes-sigs/kueue/pull/6785/files#diff-a7490e7f5efa2a59ab508f592b67dd6e7530b9d2e1b19a1b6a7438e6c2386905L1183-L1191 ?
  I think they might not be necessary

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T08:36:44Z

- **pkg/workload/workload_test.go:848** by [@mimowo](https://github.com/mimowo) — 2025-10-14T08:36:43Z
```diff
@@ -844,6 +845,7 @@ func TestAdmissionCheckStrategy(t *testing.T) {
 					Obj()).
 				Obj(),
 			cq: utiltesting.MakeClusterQueue("cq").
+				ResourceGroup(*utiltesting.MakeFlavorQuotas("flavor1").Obj(), *utiltesting.MakeFlavorQuotas("flavor2").Obj()).
```

  I see, in that case we may or may not revert them from this PR (secondary issue). 
  
  However, it seems it reveals another issue: that we allow ACs in admissionCheckStrategy.OnFlavors which may not be configured for the CQ. I think it is much better to "fail fast" in such configuration issues by validation.
  
  Let me open an issue

### Review by [@ganczak-commits](https://github.com/ganczak-commits) (commented) — 2025-10-14T08:38:21Z

- **pkg/workload/workload_test.go:848** by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-10-14T08:38:21Z
```diff
@@ -844,6 +845,7 @@ func TestAdmissionCheckStrategy(t *testing.T) {
 					Obj()).
 				Obj(),
 			cq: utiltesting.MakeClusterQueue("cq").
+				ResourceGroup(*utiltesting.MakeFlavorQuotas("flavor1").Obj(), *utiltesting.MakeFlavorQuotas("flavor2").Obj()).
```

  They _are_ still necessary due to my other change - I removed the `len(flavors) == 0` check ([link to code](https://github.com/kubernetes-sigs/kueue/blob/2878f390ae692feef21b8e15d6fa349e90f439f3/pkg/workload/workload.go#L1210-L1213)).
  
  Without this additional setup, the case `Two AdmissionCheckStrategies, one with flavor, one without flavor` fails - only ac1 check is present instead of the wanted two: ac1 and ac2.
  
  This is because, with the `len(flavors) == 0` check removed, if there are no RFs defined for the CQ, `flavors` for ac2 at [this point](https://github.com/kubernetes-sigs/kueue/blob/2878f390ae692feef21b8e15d6fa349e90f439f3/pkg/workload/workload.go#L1214) will be empty and will not match any of `assignedFlavors`. I've modified the setup so that flavors exist and `allFlavors` in admissioncheck.go actually produce a non-empty set.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T08:42:18Z

- **pkg/workload/workload_test.go:848** by [@mimowo](https://github.com/mimowo) — 2025-10-14T08:42:18Z
```diff
@@ -844,6 +845,7 @@ func TestAdmissionCheckStrategy(t *testing.T) {
 					Obj()).
 				Obj(),
 			cq: utiltesting.MakeClusterQueue("cq").
+				ResourceGroup(*utiltesting.MakeFlavorQuotas("flavor1").Obj(), *utiltesting.MakeFlavorQuotas("flavor2").Obj()).
```

  ptal: https://github.com/kubernetes-sigs/kueue/issues/7259

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T08:45:07Z

- **pkg/workload/workload_test.go:848** by [@mimowo](https://github.com/mimowo) — 2025-10-14T08:45:07Z
```diff
@@ -844,6 +845,7 @@ func TestAdmissionCheckStrategy(t *testing.T) {
 					Obj()).
 				Obj(),
 			cq: utiltesting.MakeClusterQueue("cq").
+				ResourceGroup(*utiltesting.MakeFlavorQuotas("flavor1").Obj(), *utiltesting.MakeFlavorQuotas("flavor2").Obj()).
```

  Ok, so the new configuration looks much better, but it shows this PR has user-facing impact. 
  
  Before we merge, could you please investigate and summarize what happens in case of such mis-configuration (when AdmissionCheckStrategy.OnFlavors references a non-existing flavor), both on the current main, and on this PR?
  
  Ideally, I would like to add validation, as in the issue, but to be on the safe side we need to understand the semantics before and after this PR.

### Review by [@ganczak-commits](https://github.com/ganczak-commits) (commented) — 2025-10-14T09:05:13Z

- **pkg/workload/workload_test.go:848** by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-10-14T09:05:13Z
```diff
@@ -844,6 +845,7 @@ func TestAdmissionCheckStrategy(t *testing.T) {
 					Obj()).
 				Obj(),
 			cq: utiltesting.MakeClusterQueue("cq").
+				ResourceGroup(*utiltesting.MakeFlavorQuotas("flavor1").Obj(), *utiltesting.MakeFlavorQuotas("flavor2").Obj()).
```

  > when AdmissionCheckStrategy.OnFlavors references a non-existing flavor
  
  Hmmm, but just to be clear, doesn't this particular test case fail due to a different issue? I.e. AdmissionCheckStrategy.OnFlavors being _empty_, and then [`allFlavors`](https://github.com/kubernetes-sigs/kueue/pull/6785/files#diff-494d796452c1aecae4f66bb2cb2e9ad94396b0c348168969fc1b4639ad6c768fR187-R196) producing an empty set if there are no flavors defined for the ClusterQueue?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T09:39:45Z

- **test/integration/multikueue/jobs_test.go:1761** by [@mimowo](https://github.com/mimowo) — 2025-10-14T09:39:45Z
```diff
@@ -1758,6 +1758,7 @@ var _ = ginkgo.Describe("MultiKueue", ginkgo.Ordered, ginkgo.ContinueOnFailure,
 		})
 
 		ginkgo.By("admit the new workload and finish the old workload in the manager cluster", func() {
+			util.SetQuotaReservation(manager.ctx, manager.client, newWorkloadKey, utiltesting.MakeAdmission(managerCq.Name).Obj())
```

  Why is this needed? Actually if needed this could be problematic for the functionality, because for ElasticWorkloads the old slice is finished first, and only then the new one is admitted. I think the ginkgo.By comment does not reflect this well. 
  
  For reference see this block of code: https://github.com/kubernetes-sigs/kueue/blob/173ac84f52ba9740ccb296d516e1409e4ae66689/pkg/scheduler/scheduler.go#L337-L348, here the Finish is first, anly only then Admit.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T09:58:25Z

- **pkg/workload/workload.go:1185** by [@mimowo](https://github.com/mimowo) — 2025-10-14T09:58:25Z
```diff
@@ -1176,13 +1176,13 @@ func RemoveFinalizer(ctx context.Context, c client.Client, wl *kueue.Workload) e
 
 // AdmissionChecksForWorkload returns AdmissionChecks that should be assigned to a specific Workload based on
 // ClusterQueue configuration and ResourceFlavors
-func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionChecks map[kueue.AdmissionCheckReference]sets.Set[kueue.ResourceFlavorReference]) sets.Set[kueue.AdmissionCheckReference] {
+func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionChecks map[kueue.AdmissionCheckReference]sets.Set[kueue.ResourceFlavorReference], numAllFlavors int) sets.Set[kueue.AdmissionCheckReference] {
 	// If all admissionChecks should be run for all flavors we don't need to wait for Workload's Admission to be set.
 	// This is also the case if admissionChecks are specified with ClusterQueue.Spec.AdmissionChecks instead of
 	// ClusterQueue.Spec.AdmissionCheckStrategy
 	allFlavors := true
 	for _, flavors := range admissionChecks {
-		if len(flavors) != 0 {
+		if len(flavors) != numAllFlavors {
```

  Relying just on just is tricky, because maybe the AdmissionCheckStrategy.OnFlavors lists two flavors which aren't mentioned in the CQ. Can we instead of checking counts compare if one set if actually contained in the other? I think this would be correct if we had the validation as proposed in https://github.com/kubernetes-sigs/kueue/issues/7259, but until we have it, for the purpose of the refactoring I think it will be safer to compare the containment on sets.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T09:58:55Z

- **pkg/workload/workload_test.go:848** by [@mimowo](https://github.com/mimowo) — 2025-10-14T09:58:55Z
```diff
@@ -844,6 +845,7 @@ func TestAdmissionCheckStrategy(t *testing.T) {
 					Obj()).
 				Obj(),
 			cq: utiltesting.MakeClusterQueue("cq").
+				ResourceGroup(*utiltesting.MakeFlavorQuotas("flavor1").Obj(), *utiltesting.MakeFlavorQuotas("flavor2").Obj()).
```

  Hm, I see, I think you are right. It got me thinking about one more possible issue: https://github.com/kubernetes-sigs/kueue/pull/6785/files/a66997e9585641ea24f9690c725604749291c02d#r2428589460
  with comparing just sizes of the lists

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T09:59:34Z

- **pkg/workload/workload_test.go:848** by [@mimowo](https://github.com/mimowo) — 2025-10-14T09:59:34Z
```diff
@@ -844,6 +845,7 @@ func TestAdmissionCheckStrategy(t *testing.T) {
 					Obj()).
 				Obj(),
 			cq: utiltesting.MakeClusterQueue("cq").
+				ResourceGroup(*utiltesting.MakeFlavorQuotas("flavor1").Obj(), *utiltesting.MakeFlavorQuotas("flavor2").Obj()).
```

  I'm happy to close this thread, the unit test is not exercising the e2e logic anyway.

### Review by [@ganczak-commits](https://github.com/ganczak-commits) (commented) — 2025-10-14T10:26:55Z

- **test/integration/multikueue/jobs_test.go:1761** by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-10-14T10:26:55Z
```diff
@@ -1758,6 +1758,7 @@ var _ = ginkgo.Describe("MultiKueue", ginkgo.Ordered, ginkgo.ContinueOnFailure,
 		})
 
 		ginkgo.By("admit the new workload and finish the old workload in the manager cluster", func() {
+			util.SetQuotaReservation(manager.ctx, manager.client, newWorkloadKey, utiltesting.MakeAdmission(managerCq.Name).Obj())
```

  Oops, this is not needed actually (I just ran it locally and it passed without this line). I think I somehow failed to revert this one line along with other changes after we've decided to tackle https://github.com/kubernetes-sigs/kueue/issues/7254 in a separate PR. I'll revert this..

### Review by [@ganczak-commits](https://github.com/ganczak-commits) (commented) — 2025-10-14T11:00:26Z

- **pkg/workload/workload.go:1185** by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-10-14T11:00:26Z
```diff
@@ -1176,13 +1176,13 @@ func RemoveFinalizer(ctx context.Context, c client.Client, wl *kueue.Workload) e
 
 // AdmissionChecksForWorkload returns AdmissionChecks that should be assigned to a specific Workload based on
 // ClusterQueue configuration and ResourceFlavors
-func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionChecks map[kueue.AdmissionCheckReference]sets.Set[kueue.ResourceFlavorReference]) sets.Set[kueue.AdmissionCheckReference] {
+func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionChecks map[kueue.AdmissionCheckReference]sets.Set[kueue.ResourceFlavorReference], numAllFlavors int) sets.Set[kueue.AdmissionCheckReference] {
 	// If all admissionChecks should be run for all flavors we don't need to wait for Workload's Admission to be set.
 	// This is also the case if admissionChecks are specified with ClusterQueue.Spec.AdmissionChecks instead of
 	// ClusterQueue.Spec.AdmissionCheckStrategy
 	allFlavors := true
 	for _, flavors := range admissionChecks {
-		if len(flavors) != 0 {
+		if len(flavors) != numAllFlavors {
```

  Ah, good catch Michał. Yes, it makes more sense to just compare the two sets. Updated the PR.

### Review by [@ganczak-commits](https://github.com/ganczak-commits) (commented) — 2025-10-14T11:00:31Z

- **test/integration/multikueue/jobs_test.go:1761** by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-10-14T11:00:31Z
```diff
@@ -1758,6 +1758,7 @@ var _ = ginkgo.Describe("MultiKueue", ginkgo.Ordered, ginkgo.ContinueOnFailure,
 		})
 
 		ginkgo.By("admit the new workload and finish the old workload in the manager cluster", func() {
+			util.SetQuotaReservation(manager.ctx, manager.client, newWorkloadKey, utiltesting.MakeAdmission(managerCq.Name).Obj())
```

  Reverted

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T11:26:33Z

- **pkg/util/queue/cluster_queue.go:31** by [@mimowo](https://github.com/mimowo) — 2025-10-14T11:26:33Z
```diff
@@ -15,10 +15,20 @@
 package queue
 
 import (
+	"k8s.io/apimachinery/pkg/util/sets"
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	utilslices "sigs.k8s.io/kueue/pkg/util/slices"
 )
 
-func NumAllFlavors(rgs []kueue.ResourceGroup) int {
-	return utilslices.Reduce(rgs, func(acc int, rg kueue.ResourceGroup) int { return acc + len(rg.Flavors) }, 0)
+func AllFlavors(rgs []kueue.ResourceGroup) sets.Set[kueue.ResourceFlavorReference] {
+	return utilslices.Reduce(
+		rgs,
+		func(acc sets.Set[kueue.ResourceFlavorReference], rg kueue.ResourceGroup) sets.Set[kueue.ResourceFlavorReference] {
+			for _, flavor := range rg.Flavors {
+				acc.Insert(flavor.Name)
+			}
+			return acc
```

  ```suggestion
  			return acc.Insert(rg.Flavors...)
  ```
  ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T11:28:16Z

- **pkg/workload/workload.go:1185** by [@mimowo](https://github.com/mimowo) — 2025-10-14T11:28:16Z
```diff
@@ -1176,13 +1176,13 @@ func RemoveFinalizer(ctx context.Context, c client.Client, wl *kueue.Workload) e
 
 // AdmissionChecksForWorkload returns AdmissionChecks that should be assigned to a specific Workload based on
 // ClusterQueue configuration and ResourceFlavors
-func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionChecks map[kueue.AdmissionCheckReference]sets.Set[kueue.ResourceFlavorReference]) sets.Set[kueue.AdmissionCheckReference] {
+func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionChecks map[kueue.AdmissionCheckReference]sets.Set[kueue.ResourceFlavorReference], numAllFlavors int) sets.Set[kueue.AdmissionCheckReference] {
 	// If all admissionChecks should be run for all flavors we don't need to wait for Workload's Admission to be set.
 	// This is also the case if admissionChecks are specified with ClusterQueue.Spec.AdmissionChecks instead of
 	// ClusterQueue.Spec.AdmissionCheckStrategy
 	allFlavors := true
 	for _, flavors := range admissionChecks {
-		if len(flavors) != 0 {
+		if len(flavors) != numAllFlavors {
```

  Can you extend the set of unit tests we discussed in the other thread for this case?

### Review by [@ganczak-commits](https://github.com/ganczak-commits) (commented) — 2025-10-14T12:14:23Z

- **pkg/workload/workload.go:1185** by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-10-14T12:14:23Z
```diff
@@ -1176,13 +1176,13 @@ func RemoveFinalizer(ctx context.Context, c client.Client, wl *kueue.Workload) e
 
 // AdmissionChecksForWorkload returns AdmissionChecks that should be assigned to a specific Workload based on
 // ClusterQueue configuration and ResourceFlavors
-func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionChecks map[kueue.AdmissionCheckReference]sets.Set[kueue.ResourceFlavorReference]) sets.Set[kueue.AdmissionCheckReference] {
+func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionChecks map[kueue.AdmissionCheckReference]sets.Set[kueue.ResourceFlavorReference], numAllFlavors int) sets.Set[kueue.AdmissionCheckReference] {
 	// If all admissionChecks should be run for all flavors we don't need to wait for Workload's Admission to be set.
 	// This is also the case if admissionChecks are specified with ClusterQueue.Spec.AdmissionChecks instead of
 	// ClusterQueue.Spec.AdmissionCheckStrategy
 	allFlavors := true
 	for _, flavors := range admissionChecks {
-		if len(flavors) != 0 {
+		if len(flavors) != numAllFlavors {
```

  Would this work? (in [workload_test.go](https://github.com/kubernetes-sigs/kueue/blob/6a1f89a58334b282f0c820b889d4137a4bdd6249/pkg/workload/workload_test.go#L903))
  ```
  "AdmissionCheckStrategy with a non-existent flavor": {
  	wl: utiltesting.MakeWorkload("wl", "ns").
  		ReserveQuota(utiltesting.MakeAdmission("cq").
  			PodSets(utiltesting.MakePodSetAssignment(kueue.DefaultPodSetName).
  				Assignment("cpu", "flavor1", "1").
  				Obj()).
  			Obj()).
  		Obj(),
  	cq: utiltesting.MakeClusterQueue("cq").
  		ResourceGroup(*utiltesting.MakeFlavorQuotas("flavor1").Obj()).
  		AdmissionCheckStrategy(
  			*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "flavor-nonexistent").Obj()).
  		Obj(),
  	wantAdmissionChecks: sets.New[kueue.AdmissionCheckReference](),
  },
  ```

### Review by [@ganczak-commits](https://github.com/ganczak-commits) (commented) — 2025-10-14T12:18:56Z

- **pkg/util/queue/cluster_queue.go:31** by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-10-14T12:18:56Z
```diff
@@ -15,10 +15,20 @@
 package queue
 
 import (
+	"k8s.io/apimachinery/pkg/util/sets"
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	utilslices "sigs.k8s.io/kueue/pkg/util/slices"
 )
 
-func NumAllFlavors(rgs []kueue.ResourceGroup) int {
-	return utilslices.Reduce(rgs, func(acc int, rg kueue.ResourceGroup) int { return acc + len(rg.Flavors) }, 0)
+func AllFlavors(rgs []kueue.ResourceGroup) sets.Set[kueue.ResourceFlavorReference] {
+	return utilslices.Reduce(
+		rgs,
+		func(acc sets.Set[kueue.ResourceFlavorReference], rg kueue.ResourceGroup) sets.Set[kueue.ResourceFlavorReference] {
+			for _, flavor := range rg.Flavors {
+				acc.Insert(flavor.Name)
+			}
+			return acc
```

  Humm I intended to also compare flavor _names_ here, same as I'm doing in the other `AllFlavors` method located in pkg/cache/scheduler/resource.go. But here, `Flavors` in `kueue.ResourceGroup` are of `FlavorQuotas` type, which contains `Name` and `Resources` (unlike the other location where they're just a string). So I'm plucking the `Name` here. WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T12:20:25Z

- **pkg/workload/workload.go:1185** by [@mimowo](https://github.com/mimowo) — 2025-10-14T12:20:25Z
```diff
@@ -1176,13 +1176,13 @@ func RemoveFinalizer(ctx context.Context, c client.Client, wl *kueue.Workload) e
 
 // AdmissionChecksForWorkload returns AdmissionChecks that should be assigned to a specific Workload based on
 // ClusterQueue configuration and ResourceFlavors
-func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionChecks map[kueue.AdmissionCheckReference]sets.Set[kueue.ResourceFlavorReference]) sets.Set[kueue.AdmissionCheckReference] {
+func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionChecks map[kueue.AdmissionCheckReference]sets.Set[kueue.ResourceFlavorReference], numAllFlavors int) sets.Set[kueue.AdmissionCheckReference] {
 	// If all admissionChecks should be run for all flavors we don't need to wait for Workload's Admission to be set.
 	// This is also the case if admissionChecks are specified with ClusterQueue.Spec.AdmissionChecks instead of
 	// ClusterQueue.Spec.AdmissionCheckStrategy
 	allFlavors := true
 	for _, flavors := range admissionChecks {
-		if len(flavors) != 0 {
+		if len(flavors) != numAllFlavors {
```

  Yeah, I think so, but to be sure please run experiment: check if the test fails on a branch with the commit to compare sets equality fails

### Review by [@ganczak-commits](https://github.com/ganczak-commits) (commented) — 2025-10-14T12:23:36Z

- **pkg/workload/workload.go:1185** by [@ganczak-commits](https://github.com/ganczak-commits) — 2025-10-14T12:23:36Z
```diff
@@ -1176,13 +1176,13 @@ func RemoveFinalizer(ctx context.Context, c client.Client, wl *kueue.Workload) e
 
 // AdmissionChecksForWorkload returns AdmissionChecks that should be assigned to a specific Workload based on
 // ClusterQueue configuration and ResourceFlavors
-func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionChecks map[kueue.AdmissionCheckReference]sets.Set[kueue.ResourceFlavorReference]) sets.Set[kueue.AdmissionCheckReference] {
+func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionChecks map[kueue.AdmissionCheckReference]sets.Set[kueue.ResourceFlavorReference], numAllFlavors int) sets.Set[kueue.AdmissionCheckReference] {
 	// If all admissionChecks should be run for all flavors we don't need to wait for Workload's Admission to be set.
 	// This is also the case if admissionChecks are specified with ClusterQueue.Spec.AdmissionChecks instead of
 	// ClusterQueue.Spec.AdmissionCheckStrategy
 	allFlavors := true
 	for _, flavors := range admissionChecks {
-		if len(flavors) != 0 {
+		if len(flavors) != numAllFlavors {
```

  I checked and the test fails with the code version where we compare the flavor counts rather than the sets.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T12:27:27Z

- **pkg/workload/workload.go:1185** by [@mimowo](https://github.com/mimowo) — 2025-10-14T12:27:27Z
```diff
@@ -1176,13 +1176,13 @@ func RemoveFinalizer(ctx context.Context, c client.Client, wl *kueue.Workload) e
 
 // AdmissionChecksForWorkload returns AdmissionChecks that should be assigned to a specific Workload based on
 // ClusterQueue configuration and ResourceFlavors
-func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionChecks map[kueue.AdmissionCheckReference]sets.Set[kueue.ResourceFlavorReference]) sets.Set[kueue.AdmissionCheckReference] {
+func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionChecks map[kueue.AdmissionCheckReference]sets.Set[kueue.ResourceFlavorReference], numAllFlavors int) sets.Set[kueue.AdmissionCheckReference] {
 	// If all admissionChecks should be run for all flavors we don't need to wait for Workload's Admission to be set.
 	// This is also the case if admissionChecks are specified with ClusterQueue.Spec.AdmissionChecks instead of
 	// ClusterQueue.Spec.AdmissionCheckStrategy
 	allFlavors := true
 	for _, flavors := range admissionChecks {
-		if len(flavors) != 0 {
+		if len(flavors) != numAllFlavors {
```

  awesome, in the future we may validate against that configuration as invalid, but lets scope the PR to be cleanup only

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T12:28:22Z

- **pkg/util/queue/cluster_queue.go:31** by [@mimowo](https://github.com/mimowo) — 2025-10-14T12:28:22Z
```diff
@@ -15,10 +15,20 @@
 package queue
 
 import (
+	"k8s.io/apimachinery/pkg/util/sets"
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	utilslices "sigs.k8s.io/kueue/pkg/util/slices"
 )
 
-func NumAllFlavors(rgs []kueue.ResourceGroup) int {
-	return utilslices.Reduce(rgs, func(acc int, rg kueue.ResourceGroup) int { return acc + len(rg.Flavors) }, 0)
+func AllFlavors(rgs []kueue.ResourceGroup) sets.Set[kueue.ResourceFlavorReference] {
+	return utilslices.Reduce(
+		rgs,
+		func(acc sets.Set[kueue.ResourceFlavorReference], rg kueue.ResourceGroup) sets.Set[kueue.ResourceFlavorReference] {
+			for _, flavor := range rg.Flavors {
+				acc.Insert(flavor.Name)
+			}
+			return acc
```

  Ah, Yeah it is ok to just grab name

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T13:20:46Z

Thank you 👍 
/lgtm
/approve
