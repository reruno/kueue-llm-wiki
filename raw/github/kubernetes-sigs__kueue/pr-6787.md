# PR #6787: Use PatchAdmissionStatus for eviction

**Summary**: Use PatchAdmissionStatus for eviction

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6787

**Last updated**: 2025-09-19T13:58:19Z

---

## Metadata

- **State**: closed
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `feat/6158-use-patch-admission-status-for-eviction` → `main`
- **Created**: 2025-09-10T19:11:35Z
- **Updated**: 2025-09-19T13:58:19Z
- **Merged**: —
- **Closed**: 2025-09-19T13:58:19Z
- **Labels**: `kind/feature`, `size/L`, `release-note-none`, `needs-rebase`, `cncf-cla: yes`
- **Assignees**: _none_
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 5   **+123 / -7**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Use `PatchAdmissionStatus` instead of `ApplyAdmissionStatus` as a step towards replacing SSA with Merge patch.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to  #6158 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-09-10T19:11:42Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 927db1dfb4a1f087382b6a0933f4fc5ca7d81e8f |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68c1cd6a3987020008d3c257 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-6787--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-6787--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTY3ODctLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.B_xnZLPuSvz4nRepY4yfC2A4UzdaEzH0WqzMVIf7Y4E)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-6787--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-10T19:11:42Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6787#" title="Author self-approved">mszadkow</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [gabesaba](https://github.com/gabesaba) for approval. For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["gabesaba"]} -->

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-09-11T08:50:36Z

This PR will focus on Eviction, as a base plz review this one: 
https://github.com/kubernetes-sigs/kueue/pull/6765
I have applied all of the comments from here

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-17T18:46:33Z

PR needs rebase.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-11T00:34:01Z

- **pkg/workload/workload_test.go:1305** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-11T00:34:01Z
```diff
@@ -1299,3 +1301,85 @@ func TestNeedsSecondPass(t *testing.T) {
 		})
 	}
 }
+func TestPatchAdmissionStatus(t *testing.T) {
+	var realClock clock.RealClock
```

  Use fake clock.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-11T00:36:06Z

- **pkg/features/kube_features.go:290** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-11T00:36:06Z
```diff
@@ -281,6 +286,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	FlavorFungibilityImplicitPreferenceDefault: {
 		{Version: version.MustParse("0.13"), Default: false, PreRelease: featuregate.Alpha},
 	},
+	WorkloadRequestUseMergePatch: {
+		{Version: version.MustParse("0.14"), Default: false, PreRelease: featuregate.Alpha},
```

  All tests pass if the feature is enabled by default, correct?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-11T01:07:27Z

- **pkg/workload/workload.go:862** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-11T01:07:27Z
```diff
@@ -855,6 +856,24 @@ func ApplyAdmissionStatusPatch(ctx context.Context, c client.Client, patch *kueu
 	return c.Status().Patch(ctx, patch, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
 }
 
+// PatchAdmissionStatus updates the admission status of a workload.
+// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
+// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
+func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, strict bool, clk clock.Clock, update func() (client.Object, bool, error)) error {
```

  ```suggestion
  func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, strict bool, clk clock.Clock, update func() (*kueue.Workload, bool, error)) error {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-11T01:11:08Z

- **pkg/workload/workload.go:864** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-11T01:11:08Z
```diff
@@ -855,6 +856,24 @@ func ApplyAdmissionStatusPatch(ctx context.Context, c client.Client, patch *kueu
 	return c.Status().Patch(ctx, patch, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
 }
 
+// PatchAdmissionStatus updates the admission status of a workload.
+// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
+// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
+func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, strict bool, clk clock.Clock, update func() (client.Object, bool, error)) error {
+	if features.Enabled(features.WorkloadRequestUseMergePatch) {
+		return clientutil.PatchStatus(ctx, c, w, update)
```

  ```suggestion
  		return clientutil.PatchStatus(ctx, c, w, strict, func() (client.Object, bool, error) {
  			return update()
  		})
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-11T01:12:39Z

- **pkg/workload/workload.go:873** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-11T01:12:39Z
```diff
@@ -855,6 +856,24 @@ func ApplyAdmissionStatusPatch(ctx context.Context, c client.Client, patch *kueu
 	return c.Status().Patch(ctx, patch, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
 }
 
+// PatchAdmissionStatus updates the admission status of a workload.
+// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
+// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
+func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, strict bool, clk clock.Clock, update func() (client.Object, bool, error)) error {
+	if features.Enabled(features.WorkloadRequestUseMergePatch) {
+		return clientutil.PatchStatus(ctx, c, w, update)
+	}
+	objPatched, updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	wPatched, ok := objPatched.(*kueue.Workload)
+	if !ok {
+		return fmt.Errorf("expected a Workload but got a %T", objPatched)
+	}
```

  ```suggestion
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-11T01:12:54Z

- **pkg/workload/workload.go:866** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-11T01:12:54Z
```diff
@@ -855,6 +856,24 @@ func ApplyAdmissionStatusPatch(ctx context.Context, c client.Client, patch *kueu
 	return c.Status().Patch(ctx, patch, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
 }
 
+// PatchAdmissionStatus updates the admission status of a workload.
+// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
+// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
+func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, strict bool, clk clock.Clock, update func() (client.Object, bool, error)) error {
+	if features.Enabled(features.WorkloadRequestUseMergePatch) {
+		return clientutil.PatchStatus(ctx, c, w, update)
+	}
+	objPatched, updated, err := update()
```

  ```suggestion
  	wPatched, updated, err := update()
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-11T01:19:55Z

- **pkg/workload/workload_test.go:1375** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-11T01:19:55Z
```diff
@@ -1299,3 +1301,85 @@ func TestNeedsSecondPass(t *testing.T) {
 		})
 	}
 }
+func TestPatchAdmissionStatus(t *testing.T) {
+	var realClock clock.RealClock
+	type patchCall struct {
+		updated bool
+		err     error
+	}
+
+	errUpdate := errors.New("update error")
+
+	tests := map[string]struct {
+		featureEnabled bool
+		patchCall      patchCall
+		wantPatched    bool
+		wantErr        error
+	}{
+		"feature disabled, update returns true": {
+			featureEnabled: false,
+			patchCall:      patchCall{updated: true, err: nil},
+			wantPatched:    true,
+			wantErr:        nil,
+		},
+		"feature disabled, update returns false": {
+			featureEnabled: false,
+			patchCall:      patchCall{updated: false, err: nil},
+			wantPatched:    false,
+			wantErr:        nil,
+		},
+		"feature disabled, update returns error": {
+			featureEnabled: false,
+			patchCall:      patchCall{updated: false, err: errUpdate},
+			wantPatched:    false,
+			wantErr:        errUpdate,
+		},
+		"feature enabled, update returns true": {
+			featureEnabled: true,
+			patchCall:      patchCall{updated: true, err: nil},
+			wantPatched:    true,
+			wantErr:        nil,
+		},
+		"feature enabled, update returns false": {
+			featureEnabled: true,
+			patchCall:      patchCall{updated: false, err: nil},
+			wantPatched:    true,
+			wantErr:        nil,
+		},
+		"feature enabled, update returns error": {
+			featureEnabled: true,
+			patchCall:      patchCall{updated: false, err: errUpdate},
+			wantPatched:    false,
+			wantErr:        errUpdate,
+		},
+	}
+	for name, tc := range tests {
+		t.Run(name, func(t *testing.T) {
+			features.SetFeatureGateDuringTest(t, features.WorkloadRequestUseMergePatch, tc.featureEnabled)
+			ctx, _ := utiltesting.ContextWithLog(t)
+			wl := utiltesting.MakeWorkload("foo", "default").Obj()
+			called := false
+			cl := utiltesting.NewFakeClientSSAAsSM(wl)
+			gotErr := PatchAdmissionStatus(
+				ctx,
+				cl,
+				wl,
+				false,
+				realClock,
+				func() (client.Object, bool, error) {
+					called = true
+					return wl, tc.patchCall.updated, tc.patchCall.err
+				},
+			)
+			if cmp.Diff(tc.wantErr, gotErr, cmpopts.EquateErrors()) != "" {
+				t.Errorf("PatchAdmissionStatus() error = %v, wantErr %v", gotErr, tc.wantErr)
```

  ```suggestion
  			if diff := cmp.Diff(tc.wantErr, gotErr, cmpopts.EquateErrors()); diff != "" {
  				t.Errorf("Unexpected error (-want/+got)\n%s", diff)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-11T01:25:00Z

- **pkg/workload/workload_test.go:1316** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-11T01:24:59Z
```diff
@@ -1299,3 +1301,85 @@ func TestNeedsSecondPass(t *testing.T) {
 		})
 	}
 }
+func TestPatchAdmissionStatus(t *testing.T) {
+	var realClock clock.RealClock
+	type patchCall struct {
+		updated bool
+		err     error
+	}
+
+	errUpdate := errors.New("update error")
+
+	tests := map[string]struct {
+		featureEnabled bool
+		patchCall      patchCall
+		wantPatched    bool
```

  It seems unnecessary.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-11T01:26:28Z

- **pkg/workload/workload_test.go:1316** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-11T01:26:28Z
```diff
@@ -1299,3 +1301,85 @@ func TestNeedsSecondPass(t *testing.T) {
 		})
 	}
 }
+func TestPatchAdmissionStatus(t *testing.T) {
+	var realClock clock.RealClock
+	type patchCall struct {
+		updated bool
+		err     error
+	}
+
+	errUpdate := errors.New("update error")
+
+	tests := map[string]struct {
+		featureEnabled bool
+		patchCall      patchCall
+		wantPatched    bool
```

  Or maybe compare with `called`?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-11T01:29:02Z

- **pkg/workload/workload.go:864** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-11T01:29:02Z
```diff
@@ -855,6 +856,24 @@ func ApplyAdmissionStatusPatch(ctx context.Context, c client.Client, patch *kueu
 	return c.Status().Patch(ctx, patch, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
 }
 
+// PatchAdmissionStatus updates the admission status of a workload.
+// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
+// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
+func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, strict bool, clk clock.Clock, update func() (client.Object, bool, error)) error {
+	if features.Enabled(features.WorkloadRequestUseMergePatch) {
+		return clientutil.PatchStatus(ctx, c, w, update)
```

  Don't forget to propagate strict here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-11T01:33:50Z

- **pkg/workload/workload_test.go:1362** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-11T01:33:49Z
```diff
@@ -1299,3 +1301,85 @@ func TestNeedsSecondPass(t *testing.T) {
 		})
 	}
 }
+func TestPatchAdmissionStatus(t *testing.T) {
+	var realClock clock.RealClock
+	type patchCall struct {
+		updated bool
+		err     error
+	}
+
+	errUpdate := errors.New("update error")
+
+	tests := map[string]struct {
+		featureEnabled bool
+		patchCall      patchCall
+		wantPatched    bool
+		wantErr        error
+	}{
+		"feature disabled, update returns true": {
+			featureEnabled: false,
+			patchCall:      patchCall{updated: true, err: nil},
+			wantPatched:    true,
+			wantErr:        nil,
+		},
+		"feature disabled, update returns false": {
+			featureEnabled: false,
+			patchCall:      patchCall{updated: false, err: nil},
+			wantPatched:    false,
+			wantErr:        nil,
+		},
+		"feature disabled, update returns error": {
+			featureEnabled: false,
+			patchCall:      patchCall{updated: false, err: errUpdate},
+			wantPatched:    false,
+			wantErr:        errUpdate,
+		},
+		"feature enabled, update returns true": {
+			featureEnabled: true,
+			patchCall:      patchCall{updated: true, err: nil},
+			wantPatched:    true,
+			wantErr:        nil,
+		},
+		"feature enabled, update returns false": {
+			featureEnabled: true,
+			patchCall:      patchCall{updated: false, err: nil},
+			wantPatched:    true,
+			wantErr:        nil,
+		},
+		"feature enabled, update returns error": {
+			featureEnabled: true,
+			patchCall:      patchCall{updated: false, err: errUpdate},
+			wantPatched:    false,
+			wantErr:        errUpdate,
+		},
+	}
+	for name, tc := range tests {
+		t.Run(name, func(t *testing.T) {
+			features.SetFeatureGateDuringTest(t, features.WorkloadRequestUseMergePatch, tc.featureEnabled)
+			ctx, _ := utiltesting.ContextWithLog(t)
+			wl := utiltesting.MakeWorkload("foo", "default").Obj()
+			called := false
+			cl := utiltesting.NewFakeClientSSAAsSM(wl)
```

  Not sure if this is needed for SMP.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-11T07:27:34Z

- **pkg/workload/workload.go:1072** by [@mimowo](https://github.com/mimowo) — 2025-09-11T07:27:34Z
```diff
@@ -1051,13 +1070,16 @@ func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionCh
 }
 
 func Evict(ctx context.Context, c client.Client, recorder record.EventRecorder, wl *kueue.Workload, reason, underlyingCause, msg string, clock clock.Clock) error {
```

  I'm surprised if this actually works, I suppose it doesn't in case of, for example, workload_controller deactivation, when there are updates done to the wl object.
  
  This is because in that case the "wl" is not the original object, so the pass will not contain the fields.
  
  The ways I see to make it work:
  1. pass the origWl as proposed before
  2. refactor the code to pass all changes done to "wl" as the "update" function
  
  I think (1.) was attempted before, but I didn't understand it as there were many changes. 
  
  Ideally, I would like to have a consistent interface (2.) but now I see this might be a significant amount of work.
  
  I will think more if I can find a good compromise approach, let me know if I'm missing something in the meanwhile.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-11T07:38:12Z

- **pkg/workload/workload.go:1072** by [@mimowo](https://github.com/mimowo) — 2025-09-11T07:38:12Z
```diff
@@ -1051,13 +1070,16 @@ func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionCh
 }
 
 func Evict(ctx context.Context, c client.Client, recorder record.EventRecorder, wl *kueue.Workload, reason, underlyingCause, msg string, clock clock.Clock) error {
```

  Ok, to make the interface consistent we could also consider this pattern:
  ```
  wl  - we have some workload here, the origin
  copyWl := wl.DeepCopy()
  copyWl.Status.requeueState = nil
  copyWl.Status.Conditions = append(copyWl.Status.Conditions, Evicted) 
  
  wl, err = PatchOrApply(wl, copyWl)
  ```
  Then we don't really need to use the "update" function. This would also help to store any changes to the wl in case the `PatchOrApply` fails. I think what we do currently (storing the changes regardless of success / failure) is problematic on its own.
  
  Maybe I'm missing something, but trying to explore different ways. 
  
  wdyt?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-11T08:04:46Z

- **pkg/workload/workload.go:873** by [@mszadkow](https://github.com/mszadkow) — 2025-09-11T08:04:46Z
```diff
@@ -855,6 +856,24 @@ func ApplyAdmissionStatusPatch(ctx context.Context, c client.Client, patch *kueu
 	return c.Status().Patch(ctx, patch, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
 }
 
+// PatchAdmissionStatus updates the admission status of a workload.
+// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
+// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
+func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, strict bool, clk clock.Clock, update func() (client.Object, bool, error)) error {
+	if features.Enabled(features.WorkloadRequestUseMergePatch) {
+		return clientutil.PatchStatus(ctx, c, w, update)
+	}
+	objPatched, updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	wPatched, ok := objPatched.(*kueue.Workload)
+	if !ok {
+		return fmt.Errorf("expected a Workload but got a %T", objPatched)
+	}
```

  No need if I change signature of the update.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-11T08:27:38Z

- **pkg/features/kube_features.go:290** by [@mszadkow](https://github.com/mszadkow) — 2025-09-11T08:27:37Z
```diff
@@ -281,6 +286,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	FlavorFungibilityImplicitPreferenceDefault: {
 		{Version: version.MustParse("0.13"), Default: false, PreRelease: featuregate.Alpha},
 	},
+	WorkloadRequestUseMergePatch: {
+		{Version: version.MustParse("0.14"), Default: false, PreRelease: featuregate.Alpha},
```

  There is no use yet, but from the original PR I can say it does

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-11T08:55:45Z

- **pkg/workload/workload.go:1072** by [@mszadkow](https://github.com/mszadkow) — 2025-09-11T08:55:45Z
```diff
@@ -1051,13 +1070,16 @@ func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionCh
 }
 
 func Evict(ctx context.Context, c client.Client, recorder record.EventRecorder, wl *kueue.Workload, reason, underlyingCause, msg string, clock clock.Clock) error {
```

  We sometimes do want to store them in other situations we don't.
  The approach with 2 workloads might is more robust and works in the both situations, but might be harder to understand.
  Keeping changes that shouldn't last in next lines in update makes a clear cut imho

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-11T09:23:56Z

- **pkg/features/kube_features.go:290** by [@mimowo](https://github.com/mimowo) — 2025-09-11T09:23:55Z
```diff
@@ -281,6 +286,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	FlavorFungibilityImplicitPreferenceDefault: {
 		{Version: version.MustParse("0.13"), Default: false, PreRelease: featuregate.Alpha},
 	},
+	WorkloadRequestUseMergePatch: {
+		{Version: version.MustParse("0.14"), Default: false, PreRelease: featuregate.Alpha},
```

  Please enable to true for testing purposes, otherwise it is not clear we are reviewing something that works. 
  
  You may consider a separate PR [experiment], but I think we can enable it under this PR until we are ready to merge. TBH, I'm even ok to merge it as beta, but it hasn't been confirmed with Yuki or Gabe
