# PR #6805: [Cleanup] Use Eventually inside SetQuotaReservation

**Summary**: [Cleanup] Use Eventually inside SetQuotaReservation

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6805

**Last updated**: 2025-09-12T18:54:11Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `cleanup/6158-cleanup-set-quota-reserved` → `main`
- **Created**: 2025-09-12T12:10:41Z
- **Updated**: 2025-09-12T18:54:11Z
- **Merged**: 2025-09-12T18:22:08Z
- **Closed**: 2025-09-12T18:22:08Z
- **Labels**: `lgtm`, `release-note-none`, `size/XL`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mimowo](https://github.com/mimowo)
- **Changed files**: 21   **+193 / -448**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Simplifies many calls in the tests and preps for 6158 related changes.
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates #6158

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-09-12T12:10:50Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 55c7dc8ef487b3f048933da684ba986f1c9b0020 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68c4321c0af9ad0009f95019 |

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-09-12T12:27:42Z

Fixing tests, hold on for a minute

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-09-12T12:42:49Z

/cc @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-12T17:50:10Z

/lgtm
/approve
thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-12T17:50:19Z

LGTM label has been added.  <details>Git tree hash: 6716d0649a634b0a642d2700d6f2a0d27f7ad104</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-12T17:50:21Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6805#issuecomment-3286321432" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6805#" title="Author self-approved">mszadkow</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-12T12:30:18Z

- **test/util/util.go:680** by [@mszadkow](https://github.com/mszadkow) — 2025-09-12T12:30:18Z
```diff
@@ -677,14 +677,18 @@ func ExpectCQResourceReservations(cq *kueue.ClusterQueue, flavor, resource strin
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
-func SetQuotaReservation(ctx context.Context, k8sClient client.Client, wl *kueue.Workload, admission *kueue.Admission) error {
-	wl = wl.DeepCopy()
-	if admission == nil {
-		workload.UnsetQuotaReservationWithCondition(wl, "EvictedByTest", "Evicted By Test", time.Now())
-	} else {
-		workload.SetQuotaReservation(wl, admission, clock.RealClock{})
-	}
-	return workload.ApplyAdmissionStatus(ctx, k8sClient, wl, false, clock.RealClock{})
+func SetQuotaReservation(ctx context.Context, k8sClient client.Client, wl *kueue.Workload, admission *kueue.Admission) {
```

  I could pass wlKey instead of workload, that would simplify other things too
  wdyt @mbobrovskyi ?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-12T12:44:38Z

- **test/integration/multikueue/dispatcher_test.go:184** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-12T12:44:38Z
```diff
@@ -181,8 +181,8 @@ var _ = ginkgo.Describe("MultiKueueDispatcherIncremental", ginkgo.Ordered, ginkg
 			admission := utiltesting.MakeAdmission(managerCq.Name).Obj()
 			gomega.Eventually(func(g gomega.Gomega) {
 				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
-				g.Expect(util.SetQuotaReservation(managerTestCluster.ctx, managerTestCluster.client, createdWorkload, admission)).To(gomega.Succeed())
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
```

  I believe this is no longer needed.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-12T12:45:15Z

- **test/util/util.go:680** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-12T12:45:14Z
```diff
@@ -677,14 +677,18 @@ func ExpectCQResourceReservations(cq *kueue.ClusterQueue, flavor, resource strin
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
-func SetQuotaReservation(ctx context.Context, k8sClient client.Client, wl *kueue.Workload, admission *kueue.Admission) error {
-	wl = wl.DeepCopy()
-	if admission == nil {
-		workload.UnsetQuotaReservationWithCondition(wl, "EvictedByTest", "Evicted By Test", time.Now())
-	} else {
-		workload.SetQuotaReservation(wl, admission, clock.RealClock{})
-	}
-	return workload.ApplyAdmissionStatus(ctx, k8sClient, wl, false, clock.RealClock{})
+func SetQuotaReservation(ctx context.Context, k8sClient client.Client, wl *kueue.Workload, admission *kueue.Admission) {
```

  Yes, please pass wlKey instead.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-12T12:45:49Z

- **test/integration/multikueue/dispatcher_test.go:208** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-12T12:45:49Z
```diff
@@ -205,8 +205,8 @@ var _ = ginkgo.Describe("MultiKueueDispatcherIncremental", ginkgo.Ordered, ginkg
 			admission := utiltesting.MakeAdmission(managerCq.Name).Obj()
 			gomega.Eventually(func(g gomega.Gomega) {
 				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
-				g.Expect(util.SetQuotaReservation(worker1TestCluster.ctx, worker1TestCluster.client, createdWorkload, admission)).To(gomega.Succeed())
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
```

  I think here is too.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-12T12:46:33Z

- **test/integration/multikueue/dispatcher_test.go:208** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-12T12:46:33Z
```diff
@@ -205,8 +205,8 @@ var _ = ginkgo.Describe("MultiKueueDispatcherIncremental", ginkgo.Ordered, ginkg
 			admission := utiltesting.MakeAdmission(managerCq.Name).Obj()
 			gomega.Eventually(func(g gomega.Gomega) {
 				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
-				g.Expect(util.SetQuotaReservation(worker1TestCluster.ctx, worker1TestCluster.client, createdWorkload, admission)).To(gomega.Succeed())
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
```

  Please check other places as well.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-12T14:07:44Z

- **test/integration/singlecluster/controller/jobs/jaxjob/jaxjob_controller_test.go:555** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-12T14:07:44Z
```diff
@@ -548,10 +546,14 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 			})
 
 			ginkgo.By("clear the workload's admission to stop the job", func() {
-				wl := &kueue.Workload{}
 				wlKey := types.NamespacedName{Name: workloadjaxjob.GetWorkloadNameForJAXJob(job.Name, job.UID), Namespace: job.Namespace}
-				gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
-				gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, wl, nil)).Should(gomega.Succeed())
+				wl := &kueue.Workload{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:      wlKey.Name,
+						Namespace: wlKey.Namespace,
+					},
+				}
```

  Can we use MakeWorkload?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-12T14:11:40Z

- **test/util/util.go:681** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-12T14:11:40Z
```diff
@@ -677,14 +677,18 @@ func ExpectCQResourceReservations(cq *kueue.ClusterQueue, flavor, resource strin
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
-func SetQuotaReservation(ctx context.Context, k8sClient client.Client, wl *kueue.Workload, admission *kueue.Admission) error {
-	wl = wl.DeepCopy()
-	if admission == nil {
-		workload.UnsetQuotaReservationWithCondition(wl, "EvictedByTest", "Evicted By Test", time.Now())
-	} else {
-		workload.SetQuotaReservation(wl, admission, clock.RealClock{})
-	}
-	return workload.ApplyAdmissionStatus(ctx, k8sClient, wl, false, clock.RealClock{})
+func SetQuotaReservation(ctx context.Context, k8sClient client.Client, wlKey client.ObjectKey, admission *kueue.Admission) {
+	clk := clock.RealClock{}
```

  Use FakeClock.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-12T14:14:28Z

- **test/integration/singlecluster/controller/jobs/jaxjob/jaxjob_controller_test.go:555** by [@mszadkow](https://github.com/mszadkow) — 2025-09-12T14:14:27Z
```diff
@@ -548,10 +546,14 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 			})
 
 			ginkgo.By("clear the workload's admission to stop the job", func() {
-				wl := &kueue.Workload{}
 				wlKey := types.NamespacedName{Name: workloadjaxjob.GetWorkloadNameForJAXJob(job.Name, job.UID), Namespace: job.Namespace}
-				gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
-				gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, wl, nil)).Should(gomega.Succeed())
+				wl := &kueue.Workload{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:      wlKey.Name,
+						Namespace: wlKey.Namespace,
+					},
+				}
```

  maybe yes, I will check

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-12T15:06:12Z

- **test/integration/singlecluster/webhook/core/workload_test.go:316** by [@mszadkow](https://github.com/mszadkow) — 2025-09-12T15:06:11Z
```diff
@@ -313,10 +313,11 @@ var _ = ginkgo.Describe("Workload validating webhook", ginkgo.Ordered, func() {
 		)
 
 		ginkgo.DescribeTable("Should have valid values when setting Admission", func(w func() *kueue.Workload, a *kueue.Admission, errorType gomega.OmegaMatcher) {
-			workload := w()
-			util.MustCreate(ctx, k8sClient, workload)
-
-			err := util.SetQuotaReservation(ctx, k8sClient, workload, a)
+			wl := w()
```

  In case you wonder why, it's because this test wants an error, which we no longer return from the function

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-12T15:35:06Z

- **test/integration/singlecluster/webhook/core/workload_test.go:316** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-12T15:35:06Z
```diff
@@ -313,10 +313,11 @@ var _ = ginkgo.Describe("Workload validating webhook", ginkgo.Ordered, func() {
 		)
 
 		ginkgo.DescribeTable("Should have valid values when setting Admission", func(w func() *kueue.Workload, a *kueue.Admission, errorType gomega.OmegaMatcher) {
-			workload := w()
-			util.MustCreate(ctx, k8sClient, workload)
-
-			err := util.SetQuotaReservation(ctx, k8sClient, workload, a)
+			wl := w()
```

  To be honest, I don't understand why we have this function here, but it's outside the scope of this PR.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-12T15:38:35Z

- **test/integration/singlecluster/webhook/core/workload_test.go:320** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-12T15:38:34Z
```diff
@@ -313,10 +313,11 @@ var _ = ginkgo.Describe("Workload validating webhook", ginkgo.Ordered, func() {
 		)
 
 		ginkgo.DescribeTable("Should have valid values when setting Admission", func(w func() *kueue.Workload, a *kueue.Admission, errorType gomega.OmegaMatcher) {
-			workload := w()
-			util.MustCreate(ctx, k8sClient, workload)
-
-			err := util.SetQuotaReservation(ctx, k8sClient, workload, a)
+			wl := w()
+			util.MustCreate(ctx, k8sClient, wl)
+			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+			workload.SetQuotaReservation(wl, a, clock.RealClock{})
+			err := workload.ApplyAdmissionStatus(ctx, k8sClient, wl, false, clock.RealClock{})
```

  Can we create a helper function for this? It could be used here and in SetQuotaReservation.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-12T15:40:50Z

Overall LGTM.
Thank you so much!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-12T17:28:26Z

- **test/integration/singlecluster/webhook/core/workload_test.go:320** by [@mimowo](https://github.com/mimowo) — 2025-09-12T17:28:26Z
```diff
@@ -313,10 +313,11 @@ var _ = ginkgo.Describe("Workload validating webhook", ginkgo.Ordered, func() {
 		)
 
 		ginkgo.DescribeTable("Should have valid values when setting Admission", func(w func() *kueue.Workload, a *kueue.Admission, errorType gomega.OmegaMatcher) {
-			workload := w()
-			util.MustCreate(ctx, k8sClient, workload)
-
-			err := util.SetQuotaReservation(ctx, k8sClient, workload, a)
+			wl := w()
+			util.MustCreate(ctx, k8sClient, wl)
+			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+			workload.SetQuotaReservation(wl, a, clock.RealClock{})
+			err := workload.ApplyAdmissionStatus(ctx, k8sClient, wl, false, clock.RealClock{})
```

  seems like a potential follow up, the PR already improves the testing
