# PR #6849: [Doc] Add documentation about TAS fast hot swap

**Summary**: [Doc] Add documentation about TAS fast hot swap

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6849

**Last updated**: 2025-09-16T11:48:17Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `hotswap-doc` → `main`
- **Created**: 2025-09-16T08:22:56Z
- **Updated**: 2025-09-16T11:48:17Z
- **Merged**: 2025-09-16T11:36:12Z
- **Closed**: 2025-09-16T11:36:12Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mwielgus](https://github.com/mwielgus), [@pajakd](https://github.com/pajakd)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mimowo](https://github.com/mimowo), [@mwielgus](https://github.com/mwielgus)
- **Changed files**: 1   **+92 / -11**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-09-16T08:23:03Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c5d35db9cd2f8d2d582c7aecc6044c6345da4283 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68c93f4a75a75a00089e4e62 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-6849--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-6849--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTY4NDktLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.BQbR-mOe-gDej_CXOR-Av1yVlNqDbBR7W8xbCYtOP9g)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-6849--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-16T08:27:59Z

/assign @pajakd 
for the first pass

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-16T08:58:08Z

LGTM label has been added.  <details>Git tree hash: 4774b5d7ec75771c30aa1747fbfa6ad1608af6fd</details>

### Comment by [@mwielgus](https://github.com/mwielgus) — 2025-09-16T10:09:49Z

Please add a section where you mention all 3 (or whatever featue gates there are) in the same place, with brief description and recommendation what to set if you don't understand the description.

/hold

### Comment by [@mwielgus](https://github.com/mwielgus) — 2025-09-16T10:57:20Z

/unhold
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-16T10:57:26Z

LGTM label has been added.  <details>Git tree hash: f82be2dc177c13a3d48a45076aaba28d5d00a3ea</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-16T11:35:22Z

/approve
/unhold
based on https://github.com/kubernetes-sigs/kueue/pull/6849#issuecomment-3297768643. If @pajakd has some comments we can address post merge.

/cherrypick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-09-16T11:35:25Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `website` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6849#issuecomment-3298067194):

>/approve
>/unhold
>based on https://github.com/kubernetes-sigs/kueue/pull/6849#issuecomment-3297768643. If @pajakd has some comments we can address post merge.
>
>/cherrypick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-16T11:35:30Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6849#issuecomment-3298067194" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6849#pullrequestreview-3228528269" title="LGTM">mwielgus</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6849#" title="Author self-approved">PBundyra</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-09-16T11:36:52Z

@mimowo: new pull request created: #6853

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6849#issuecomment-3298067194):

>/approve
>/unhold
>based on https://github.com/kubernetes-sigs/kueue/pull/6849#issuecomment-3297768643. If @pajakd has some comments we can address post merge.
>
>/cherrypick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-09-16T11:48:17Z

Addressed all the comments in #6855

## Reviews

### Review by [@mwielgus](https://github.com/mwielgus) (approved) — 2025-09-16T08:57:59Z

/lgtm

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-09-16T11:37:52Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:147** by [@pajakd](https://github.com/pajakd) — 2025-09-16T08:54:12Z
```diff
@@ -139,13 +139,56 @@ of the entire TAS workload we introduce the node hot swap feature.
 With this feature, TAS tries to find a replacement of the failed or deleted node for
 all the affected workloads, without changing the rest of the topology assignment.
 Currently this works only for a single node failure at the time and in case of multiple failures,
-the workload gets evicted. The node is assumed to have failed if its `conditions.Status.Ready`
-is not `True` for at least 30 seconds or if the node is missing (removed from the cluster).
+the workload gets evicted.
 
-Note that finding a replacement node within the old domain (like rack) may not always
-be possible. Hence, we recommend using [WaitForPodsReady](/docs/tasks/manage/setup_wait_for_pods_ready/)
+By default, the node is assumed to have failed if its `conditions.Status.Ready`
+is not `True` for at least 30 seconds or if the node is missing (removed from the cluster).
+Since Kueue v0.13, you can enable the `TASReplaceNodeOnPodTermination` feature gate, which adds an additional heuristic:
+a node is also considered failed if it is `NotReady` and at least one of the workload's Pods scheduled on that node are terminated or terminating.
```

  After discussing with @mimowo on this "at least one terminating pod", we decided that this is risky (some old terminated pod might still hang in on a node). So I implemented this eviction if "all pods are either terminated or terminating".

- **site/content/en/docs/concepts/topology_aware_scheduling.md:149** by [@pajakd](https://github.com/pajakd) — 2025-09-16T09:46:55Z
```diff
@@ -139,13 +139,56 @@ of the entire TAS workload we introduce the node hot swap feature.
 With this feature, TAS tries to find a replacement of the failed or deleted node for
 all the affected workloads, without changing the rest of the topology assignment.
 Currently this works only for a single node failure at the time and in case of multiple failures,
-the workload gets evicted. The node is assumed to have failed if its `conditions.Status.Ready`
-is not `True` for at least 30 seconds or if the node is missing (removed from the cluster).
+the workload gets evicted.
 
-Note that finding a replacement node within the old domain (like rack) may not always
-be possible. Hence, we recommend using [WaitForPodsReady](/docs/tasks/manage/setup_wait_for_pods_ready/)
+By default, the node is assumed to have failed if its `conditions.Status.Ready`
+is not `True` for at least 30 seconds or if the node is missing (removed from the cluster).
+Since Kueue v0.13, you can enable the `TASReplaceNodeOnPodTermination` feature gate, which adds an additional heuristic:
+a node is also considered failed if it is `NotReady` and at least one of the workload's Pods scheduled on that node are terminated or terminating.
+If this happens Kueue will immediately look for replacement without waiting 30 seconds.
+Note that those two heuristic are mutually exclusive and depends on the value of the `TASReplaceNodeOnPodTermination` feature gate.
```

  ```suggestion
  Note that those two heuristics are mutually exclusive and depend on the value of the `TASReplaceNodeOnPodTermination` feature gate.
  ```

- **site/content/en/docs/concepts/topology_aware_scheduling.md:153** by [@pajakd](https://github.com/pajakd) — 2025-09-16T09:48:59Z
```diff
@@ -139,13 +139,56 @@ of the entire TAS workload we introduce the node hot swap feature.
 With this feature, TAS tries to find a replacement of the failed or deleted node for
 all the affected workloads, without changing the rest of the topology assignment.
 Currently this works only for a single node failure at the time and in case of multiple failures,
-the workload gets evicted. The node is assumed to have failed if its `conditions.Status.Ready`
-is not `True` for at least 30 seconds or if the node is missing (removed from the cluster).
+the workload gets evicted.
 
-Note that finding a replacement node within the old domain (like rack) may not always
-be possible. Hence, we recommend using [WaitForPodsReady](/docs/tasks/manage/setup_wait_for_pods_ready/)
+By default, the node is assumed to have failed if its `conditions.Status.Ready`
+is not `True` for at least 30 seconds or if the node is missing (removed from the cluster).
+Since Kueue v0.13, you can enable the `TASReplaceNodeOnPodTermination` feature gate, which adds an additional heuristic:
+a node is also considered failed if it is `NotReady` and at least one of the workload's Pods scheduled on that node are terminated or terminating.
+If this happens Kueue will immediately look for replacement without waiting 30 seconds.
+Note that those two heuristic are mutually exclusive and depends on the value of the `TASReplaceNodeOnPodTermination` feature gate.
+
+Note that finding a replacement node that meets all the requirements (e.g. the same type of machine placed in the rack that Kueue had previously assigned to the workload) may not always be possible.
+If a workload is big enough to cover the whole topology domain (e.g. block or rack) it's inevitable that there will be no replacement within the same domain.
+Hence, we recommend using FailFast mode describe below or [WaitForPodsReady](/docs/tasks/manage/setup_wait_for_pods_ready/)
```

  ```suggestion
  Hence, we recommend using FailFast mode described below or [WaitForPodsReady](/docs/tasks/manage/setup_wait_for_pods_ready/)
  ```

- **site/content/en/docs/concepts/topology_aware_scheduling.md:181** by [@pajakd](https://github.com/pajakd) — 2025-09-16T09:51:41Z
```diff
@@ -139,13 +139,56 @@ of the entire TAS workload we introduce the node hot swap feature.
 With this feature, TAS tries to find a replacement of the failed or deleted node for
 all the affected workloads, without changing the rest of the topology assignment.
 Currently this works only for a single node failure at the time and in case of multiple failures,
-the workload gets evicted. The node is assumed to have failed if its `conditions.Status.Ready`
-is not `True` for at least 30 seconds or if the node is missing (removed from the cluster).
+the workload gets evicted.
 
-Note that finding a replacement node within the old domain (like rack) may not always
-be possible. Hence, we recommend using [WaitForPodsReady](/docs/tasks/manage/setup_wait_for_pods_ready/)
+By default, the node is assumed to have failed if its `conditions.Status.Ready`
+is not `True` for at least 30 seconds or if the node is missing (removed from the cluster).
+Since Kueue v0.13, you can enable the `TASReplaceNodeOnPodTermination` feature gate, which adds an additional heuristic:
+a node is also considered failed if it is `NotReady` and at least one of the workload's Pods scheduled on that node are terminated or terminating.
+If this happens Kueue will immediately look for replacement without waiting 30 seconds.
+Note that those two heuristic are mutually exclusive and depends on the value of the `TASReplaceNodeOnPodTermination` feature gate.
+
+Note that finding a replacement node that meets all the requirements (e.g. the same type of machine placed in the rack that Kueue had previously assigned to the workload) may not always be possible.
+If a workload is big enough to cover the whole topology domain (e.g. block or rack) it's inevitable that there will be no replacement within the same domain.
+Hence, we recommend using FailFast mode describe below or [WaitForPodsReady](/docs/tasks/manage/setup_wait_for_pods_ready/)
 and configuring `waitForPodsReady.recoveryTimeout`, to prevent the workloads from
-waiting for the replacement indefinetly.
+waiting for the replacement indefinitely.
+
+#### Fast Hot swap
+{{% alert title="Note" color="primary" %}}
+To enable the feature, you have to set the [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/)
+`TASFailedNodeReplacementFailFast` to `true`. This feature was introduced to Kueue in version 0.13.
+{{% /alert %}}
+
+By default, Kueue tries to find a replacement for a failed node until it succeeds or until the workload is evicted (for example, by `waitForPodsReady.recoveryTimeout`). To prevent Kueue from retrying indefinitely, you can enable the `TASFailedNodeReplacementFailFast` feature gate. When enabled, Kueue will only attempt to find a replacement node once. If it fails, it will not try again, and the workload will get evicted and requeued.
+
+#### Usage Scenarios
+
+Here are a few scenarios that can happen when both `TASReplaceNodeOnPodTermination` and `TASFailedNodeReplacementFailFast` are enabled:
+
+1. **Node becomes `NotReady`, pods are terminated, and a replacement is found:**
+   - A node running a pod from a TAS workload becomes `NotReady`.
+   - The pods on that node are terminated.
+   - With `TASReplaceNodeOnPodTermination` enabled, Kueue immediately looks for a replacement.
+   - A replacement node is available, and Kueue successfully swaps the failed node.
+   - The workload continues running on the new node.
+
+2. **Node becomes `NotReady`, pods are terminated, and no replacement is found:**
+   - A node running a pod from a TAS workload becomes `NotReady`.
+   - The pods on that node are terminated.
+   - Kueue immediately looks for a replacement but cannot find one.
+   - With `TASFailedNodeReplacementFailFast` enabled, Kueue will not retry.
+   - The workload immediately gets evicted (doesn't wait 30s or until waits for pod ready) and requeued
```

  ```suggestion
     - The workload immediately gets evicted and requeued (doesn't wait 30s or until `waitForPodsReady.recoveryTimeout` expires)
  ```

- **site/content/en/docs/concepts/topology_aware_scheduling.md:191** by [@pajakd](https://github.com/pajakd) — 2025-09-16T09:54:20Z
```diff
@@ -139,13 +139,56 @@ of the entire TAS workload we introduce the node hot swap feature.
 With this feature, TAS tries to find a replacement of the failed or deleted node for
 all the affected workloads, without changing the rest of the topology assignment.
 Currently this works only for a single node failure at the time and in case of multiple failures,
-the workload gets evicted. The node is assumed to have failed if its `conditions.Status.Ready`
-is not `True` for at least 30 seconds or if the node is missing (removed from the cluster).
+the workload gets evicted.
 
-Note that finding a replacement node within the old domain (like rack) may not always
-be possible. Hence, we recommend using [WaitForPodsReady](/docs/tasks/manage/setup_wait_for_pods_ready/)
+By default, the node is assumed to have failed if its `conditions.Status.Ready`
+is not `True` for at least 30 seconds or if the node is missing (removed from the cluster).
+Since Kueue v0.13, you can enable the `TASReplaceNodeOnPodTermination` feature gate, which adds an additional heuristic:
+a node is also considered failed if it is `NotReady` and at least one of the workload's Pods scheduled on that node are terminated or terminating.
+If this happens Kueue will immediately look for replacement without waiting 30 seconds.
+Note that those two heuristic are mutually exclusive and depends on the value of the `TASReplaceNodeOnPodTermination` feature gate.
+
+Note that finding a replacement node that meets all the requirements (e.g. the same type of machine placed in the rack that Kueue had previously assigned to the workload) may not always be possible.
+If a workload is big enough to cover the whole topology domain (e.g. block or rack) it's inevitable that there will be no replacement within the same domain.
+Hence, we recommend using FailFast mode describe below or [WaitForPodsReady](/docs/tasks/manage/setup_wait_for_pods_ready/)
 and configuring `waitForPodsReady.recoveryTimeout`, to prevent the workloads from
-waiting for the replacement indefinetly.
+waiting for the replacement indefinitely.
+
+#### Fast Hot swap
+{{% alert title="Note" color="primary" %}}
+To enable the feature, you have to set the [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/)
+`TASFailedNodeReplacementFailFast` to `true`. This feature was introduced to Kueue in version 0.13.
+{{% /alert %}}
+
+By default, Kueue tries to find a replacement for a failed node until it succeeds or until the workload is evicted (for example, by `waitForPodsReady.recoveryTimeout`). To prevent Kueue from retrying indefinitely, you can enable the `TASFailedNodeReplacementFailFast` feature gate. When enabled, Kueue will only attempt to find a replacement node once. If it fails, it will not try again, and the workload will get evicted and requeued.
+
+#### Usage Scenarios
+
+Here are a few scenarios that can happen when both `TASReplaceNodeOnPodTermination` and `TASFailedNodeReplacementFailFast` are enabled:
+
+1. **Node becomes `NotReady`, pods are terminated, and a replacement is found:**
+   - A node running a pod from a TAS workload becomes `NotReady`.
+   - The pods on that node are terminated.
+   - With `TASReplaceNodeOnPodTermination` enabled, Kueue immediately looks for a replacement.
+   - A replacement node is available, and Kueue successfully swaps the failed node.
+   - The workload continues running on the new node.
+
+2. **Node becomes `NotReady`, pods are terminated, and no replacement is found:**
+   - A node running a pod from a TAS workload becomes `NotReady`.
+   - The pods on that node are terminated.
+   - Kueue immediately looks for a replacement but cannot find one.
+   - With `TASFailedNodeReplacementFailFast` enabled, Kueue will not retry.
+   - The workload immediately gets evicted (doesn't wait 30s or until waits for pod ready) and requeued
+
+3. **Node gets deleted**
+   - Same scenarios apply as in 1. and 2.
+
+4. **Node requires the whole rack and one of the Nodes becomes `NotRead`:**
+   - A node running a pod from a TAS workload becomes `NotReady`.
+   - The pods on that node are terminated.
+   - Kueue immediately looks for a replacement but since the workload asked for the whole rack in the first place, it cannot find one.
+   - With `TASFailedNodeReplacementFailFast` enabled, Kueue will not retry.
+   - The workload immediately gets evicted (doesn't wait 30s or until waits for pod ready) and requeued
```

  ```suggestion
     - The workload immediately gets evicted and requeued (doesn't wait 30s or until `waitForPodsReady.recoveryTimeout` expires)
  ```

- **site/content/en/docs/concepts/topology_aware_scheduling.md:171** by [@pajakd](https://github.com/pajakd) — 2025-09-16T09:55:56Z
```diff
@@ -139,13 +139,56 @@ of the entire TAS workload we introduce the node hot swap feature.
 With this feature, TAS tries to find a replacement of the failed or deleted node for
 all the affected workloads, without changing the rest of the topology assignment.
 Currently this works only for a single node failure at the time and in case of multiple failures,
-the workload gets evicted. The node is assumed to have failed if its `conditions.Status.Ready`
-is not `True` for at least 30 seconds or if the node is missing (removed from the cluster).
+the workload gets evicted.
 
-Note that finding a replacement node within the old domain (like rack) may not always
-be possible. Hence, we recommend using [WaitForPodsReady](/docs/tasks/manage/setup_wait_for_pods_ready/)
+By default, the node is assumed to have failed if its `conditions.Status.Ready`
+is not `True` for at least 30 seconds or if the node is missing (removed from the cluster).
+Since Kueue v0.13, you can enable the `TASReplaceNodeOnPodTermination` feature gate, which adds an additional heuristic:
+a node is also considered failed if it is `NotReady` and at least one of the workload's Pods scheduled on that node are terminated or terminating.
+If this happens Kueue will immediately look for replacement without waiting 30 seconds.
+Note that those two heuristic are mutually exclusive and depends on the value of the `TASReplaceNodeOnPodTermination` feature gate.
+
+Note that finding a replacement node that meets all the requirements (e.g. the same type of machine placed in the rack that Kueue had previously assigned to the workload) may not always be possible.
+If a workload is big enough to cover the whole topology domain (e.g. block or rack) it's inevitable that there will be no replacement within the same domain.
+Hence, we recommend using FailFast mode describe below or [WaitForPodsReady](/docs/tasks/manage/setup_wait_for_pods_ready/)
 and configuring `waitForPodsReady.recoveryTimeout`, to prevent the workloads from
-waiting for the replacement indefinetly.
+waiting for the replacement indefinitely.
+
+#### Fast Hot swap
+{{% alert title="Note" color="primary" %}}
+To enable the feature, you have to set the [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/)
+`TASFailedNodeReplacementFailFast` to `true`. This feature was introduced to Kueue in version 0.13.
+{{% /alert %}}
+
+By default, Kueue tries to find a replacement for a failed node until it succeeds or until the workload is evicted (for example, by `waitForPodsReady.recoveryTimeout`). To prevent Kueue from retrying indefinitely, you can enable the `TASFailedNodeReplacementFailFast` feature gate. When enabled, Kueue will only attempt to find a replacement node once. If it fails, it will not try again, and the workload will get evicted and requeued.
+
+#### Usage Scenarios
+
+Here are a few scenarios that can happen when both `TASReplaceNodeOnPodTermination` and `TASFailedNodeReplacementFailFast` are enabled:
+
+1. **Node becomes `NotReady`, pods are terminated, and a replacement is found:**
+   - A node running a pod from a TAS workload becomes `NotReady`.
+   - The pods on that node are terminated.
```

  ```suggestion
     - All pods on that node are terminated or terminating.
  ```

- **site/content/en/docs/concepts/topology_aware_scheduling.md:178** by [@pajakd](https://github.com/pajakd) — 2025-09-16T09:56:18Z
```diff
@@ -139,13 +139,56 @@ of the entire TAS workload we introduce the node hot swap feature.
 With this feature, TAS tries to find a replacement of the failed or deleted node for
 all the affected workloads, without changing the rest of the topology assignment.
 Currently this works only for a single node failure at the time and in case of multiple failures,
-the workload gets evicted. The node is assumed to have failed if its `conditions.Status.Ready`
-is not `True` for at least 30 seconds or if the node is missing (removed from the cluster).
+the workload gets evicted.
 
-Note that finding a replacement node within the old domain (like rack) may not always
-be possible. Hence, we recommend using [WaitForPodsReady](/docs/tasks/manage/setup_wait_for_pods_ready/)
+By default, the node is assumed to have failed if its `conditions.Status.Ready`
+is not `True` for at least 30 seconds or if the node is missing (removed from the cluster).
+Since Kueue v0.13, you can enable the `TASReplaceNodeOnPodTermination` feature gate, which adds an additional heuristic:
+a node is also considered failed if it is `NotReady` and at least one of the workload's Pods scheduled on that node are terminated or terminating.
+If this happens Kueue will immediately look for replacement without waiting 30 seconds.
+Note that those two heuristic are mutually exclusive and depends on the value of the `TASReplaceNodeOnPodTermination` feature gate.
+
+Note that finding a replacement node that meets all the requirements (e.g. the same type of machine placed in the rack that Kueue had previously assigned to the workload) may not always be possible.
+If a workload is big enough to cover the whole topology domain (e.g. block or rack) it's inevitable that there will be no replacement within the same domain.
+Hence, we recommend using FailFast mode describe below or [WaitForPodsReady](/docs/tasks/manage/setup_wait_for_pods_ready/)
 and configuring `waitForPodsReady.recoveryTimeout`, to prevent the workloads from
-waiting for the replacement indefinetly.
+waiting for the replacement indefinitely.
+
+#### Fast Hot swap
+{{% alert title="Note" color="primary" %}}
+To enable the feature, you have to set the [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/)
+`TASFailedNodeReplacementFailFast` to `true`. This feature was introduced to Kueue in version 0.13.
+{{% /alert %}}
+
+By default, Kueue tries to find a replacement for a failed node until it succeeds or until the workload is evicted (for example, by `waitForPodsReady.recoveryTimeout`). To prevent Kueue from retrying indefinitely, you can enable the `TASFailedNodeReplacementFailFast` feature gate. When enabled, Kueue will only attempt to find a replacement node once. If it fails, it will not try again, and the workload will get evicted and requeued.
+
+#### Usage Scenarios
+
+Here are a few scenarios that can happen when both `TASReplaceNodeOnPodTermination` and `TASFailedNodeReplacementFailFast` are enabled:
+
+1. **Node becomes `NotReady`, pods are terminated, and a replacement is found:**
+   - A node running a pod from a TAS workload becomes `NotReady`.
+   - The pods on that node are terminated.
+   - With `TASReplaceNodeOnPodTermination` enabled, Kueue immediately looks for a replacement.
+   - A replacement node is available, and Kueue successfully swaps the failed node.
+   - The workload continues running on the new node.
+
+2. **Node becomes `NotReady`, pods are terminated, and no replacement is found:**
+   - A node running a pod from a TAS workload becomes `NotReady`.
+   - The pods on that node are terminated.
```

  ```suggestion
     - All pods on that node are terminated or terminating.
  ```

- **site/content/en/docs/concepts/topology_aware_scheduling.md:186** by [@pajakd](https://github.com/pajakd) — 2025-09-16T09:59:24Z
```diff
@@ -139,13 +139,56 @@ of the entire TAS workload we introduce the node hot swap feature.
 With this feature, TAS tries to find a replacement of the failed or deleted node for
 all the affected workloads, without changing the rest of the topology assignment.
 Currently this works only for a single node failure at the time and in case of multiple failures,
-the workload gets evicted. The node is assumed to have failed if its `conditions.Status.Ready`
-is not `True` for at least 30 seconds or if the node is missing (removed from the cluster).
+the workload gets evicted.
 
-Note that finding a replacement node within the old domain (like rack) may not always
-be possible. Hence, we recommend using [WaitForPodsReady](/docs/tasks/manage/setup_wait_for_pods_ready/)
+By default, the node is assumed to have failed if its `conditions.Status.Ready`
+is not `True` for at least 30 seconds or if the node is missing (removed from the cluster).
+Since Kueue v0.13, you can enable the `TASReplaceNodeOnPodTermination` feature gate, which adds an additional heuristic:
+a node is also considered failed if it is `NotReady` and at least one of the workload's Pods scheduled on that node are terminated or terminating.
+If this happens Kueue will immediately look for replacement without waiting 30 seconds.
+Note that those two heuristic are mutually exclusive and depends on the value of the `TASReplaceNodeOnPodTermination` feature gate.
+
+Note that finding a replacement node that meets all the requirements (e.g. the same type of machine placed in the rack that Kueue had previously assigned to the workload) may not always be possible.
+If a workload is big enough to cover the whole topology domain (e.g. block or rack) it's inevitable that there will be no replacement within the same domain.
+Hence, we recommend using FailFast mode describe below or [WaitForPodsReady](/docs/tasks/manage/setup_wait_for_pods_ready/)
 and configuring `waitForPodsReady.recoveryTimeout`, to prevent the workloads from
-waiting for the replacement indefinetly.
+waiting for the replacement indefinitely.
+
+#### Fast Hot swap
+{{% alert title="Note" color="primary" %}}
+To enable the feature, you have to set the [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/)
+`TASFailedNodeReplacementFailFast` to `true`. This feature was introduced to Kueue in version 0.13.
+{{% /alert %}}
+
+By default, Kueue tries to find a replacement for a failed node until it succeeds or until the workload is evicted (for example, by `waitForPodsReady.recoveryTimeout`). To prevent Kueue from retrying indefinitely, you can enable the `TASFailedNodeReplacementFailFast` feature gate. When enabled, Kueue will only attempt to find a replacement node once. If it fails, it will not try again, and the workload will get evicted and requeued.
+
+#### Usage Scenarios
+
+Here are a few scenarios that can happen when both `TASReplaceNodeOnPodTermination` and `TASFailedNodeReplacementFailFast` are enabled:
+
+1. **Node becomes `NotReady`, pods are terminated, and a replacement is found:**
+   - A node running a pod from a TAS workload becomes `NotReady`.
+   - The pods on that node are terminated.
+   - With `TASReplaceNodeOnPodTermination` enabled, Kueue immediately looks for a replacement.
+   - A replacement node is available, and Kueue successfully swaps the failed node.
+   - The workload continues running on the new node.
+
+2. **Node becomes `NotReady`, pods are terminated, and no replacement is found:**
+   - A node running a pod from a TAS workload becomes `NotReady`.
+   - The pods on that node are terminated.
+   - Kueue immediately looks for a replacement but cannot find one.
+   - With `TASFailedNodeReplacementFailFast` enabled, Kueue will not retry.
+   - The workload immediately gets evicted (doesn't wait 30s or until waits for pod ready) and requeued
+
+3. **Node gets deleted**
+   - Same scenarios apply as in 1. and 2.
+
+4. **Node requires the whole rack and one of the Nodes becomes `NotRead`:**
```

  ```suggestion
  4. **The Workload requires the whole rack and one of the nodes becomes `NotReady`:**
  ```

- **site/content/en/docs/concepts/topology_aware_scheduling.md:190** by [@pajakd](https://github.com/pajakd) — 2025-09-16T10:01:43Z
```diff
@@ -139,13 +139,56 @@ of the entire TAS workload we introduce the node hot swap feature.
 With this feature, TAS tries to find a replacement of the failed or deleted node for
 all the affected workloads, without changing the rest of the topology assignment.
 Currently this works only for a single node failure at the time and in case of multiple failures,
-the workload gets evicted. The node is assumed to have failed if its `conditions.Status.Ready`
-is not `True` for at least 30 seconds or if the node is missing (removed from the cluster).
+the workload gets evicted.
 
-Note that finding a replacement node within the old domain (like rack) may not always
-be possible. Hence, we recommend using [WaitForPodsReady](/docs/tasks/manage/setup_wait_for_pods_ready/)
+By default, the node is assumed to have failed if its `conditions.Status.Ready`
+is not `True` for at least 30 seconds or if the node is missing (removed from the cluster).
+Since Kueue v0.13, you can enable the `TASReplaceNodeOnPodTermination` feature gate, which adds an additional heuristic:
+a node is also considered failed if it is `NotReady` and at least one of the workload's Pods scheduled on that node are terminated or terminating.
+If this happens Kueue will immediately look for replacement without waiting 30 seconds.
+Note that those two heuristic are mutually exclusive and depends on the value of the `TASReplaceNodeOnPodTermination` feature gate.
+
+Note that finding a replacement node that meets all the requirements (e.g. the same type of machine placed in the rack that Kueue had previously assigned to the workload) may not always be possible.
+If a workload is big enough to cover the whole topology domain (e.g. block or rack) it's inevitable that there will be no replacement within the same domain.
+Hence, we recommend using FailFast mode describe below or [WaitForPodsReady](/docs/tasks/manage/setup_wait_for_pods_ready/)
 and configuring `waitForPodsReady.recoveryTimeout`, to prevent the workloads from
-waiting for the replacement indefinetly.
+waiting for the replacement indefinitely.
+
+#### Fast Hot swap
+{{% alert title="Note" color="primary" %}}
+To enable the feature, you have to set the [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/)
+`TASFailedNodeReplacementFailFast` to `true`. This feature was introduced to Kueue in version 0.13.
+{{% /alert %}}
+
+By default, Kueue tries to find a replacement for a failed node until it succeeds or until the workload is evicted (for example, by `waitForPodsReady.recoveryTimeout`). To prevent Kueue from retrying indefinitely, you can enable the `TASFailedNodeReplacementFailFast` feature gate. When enabled, Kueue will only attempt to find a replacement node once. If it fails, it will not try again, and the workload will get evicted and requeued.
+
+#### Usage Scenarios
+
+Here are a few scenarios that can happen when both `TASReplaceNodeOnPodTermination` and `TASFailedNodeReplacementFailFast` are enabled:
+
+1. **Node becomes `NotReady`, pods are terminated, and a replacement is found:**
+   - A node running a pod from a TAS workload becomes `NotReady`.
+   - The pods on that node are terminated.
+   - With `TASReplaceNodeOnPodTermination` enabled, Kueue immediately looks for a replacement.
+   - A replacement node is available, and Kueue successfully swaps the failed node.
+   - The workload continues running on the new node.
+
+2. **Node becomes `NotReady`, pods are terminated, and no replacement is found:**
+   - A node running a pod from a TAS workload becomes `NotReady`.
+   - The pods on that node are terminated.
+   - Kueue immediately looks for a replacement but cannot find one.
+   - With `TASFailedNodeReplacementFailFast` enabled, Kueue will not retry.
+   - The workload immediately gets evicted (doesn't wait 30s or until waits for pod ready) and requeued
+
+3. **Node gets deleted**
+   - Same scenarios apply as in 1. and 2.
+
+4. **Node requires the whole rack and one of the Nodes becomes `NotRead`:**
+   - A node running a pod from a TAS workload becomes `NotReady`.
+   - The pods on that node are terminated.
+   - Kueue immediately looks for a replacement but since the workload asked for the whole rack in the first place, it cannot find one.
+   - With `TASFailedNodeReplacementFailFast` enabled, Kueue will not retry.
```

  ```suggestion
     - With `TASFailedNodeReplacementFailFast` enabled, Kueue does not retry the replacement search.
  ```

- **site/content/en/docs/concepts/topology_aware_scheduling.md:189** by [@pajakd](https://github.com/pajakd) — 2025-09-16T10:04:31Z
```diff
@@ -139,13 +139,56 @@ of the entire TAS workload we introduce the node hot swap feature.
 With this feature, TAS tries to find a replacement of the failed or deleted node for
 all the affected workloads, without changing the rest of the topology assignment.
 Currently this works only for a single node failure at the time and in case of multiple failures,
-the workload gets evicted. The node is assumed to have failed if its `conditions.Status.Ready`
-is not `True` for at least 30 seconds or if the node is missing (removed from the cluster).
+the workload gets evicted.
 
-Note that finding a replacement node within the old domain (like rack) may not always
-be possible. Hence, we recommend using [WaitForPodsReady](/docs/tasks/manage/setup_wait_for_pods_ready/)
+By default, the node is assumed to have failed if its `conditions.Status.Ready`
+is not `True` for at least 30 seconds or if the node is missing (removed from the cluster).
+Since Kueue v0.13, you can enable the `TASReplaceNodeOnPodTermination` feature gate, which adds an additional heuristic:
+a node is also considered failed if it is `NotReady` and at least one of the workload's Pods scheduled on that node are terminated or terminating.
+If this happens Kueue will immediately look for replacement without waiting 30 seconds.
+Note that those two heuristic are mutually exclusive and depends on the value of the `TASReplaceNodeOnPodTermination` feature gate.
+
+Note that finding a replacement node that meets all the requirements (e.g. the same type of machine placed in the rack that Kueue had previously assigned to the workload) may not always be possible.
+If a workload is big enough to cover the whole topology domain (e.g. block or rack) it's inevitable that there will be no replacement within the same domain.
+Hence, we recommend using FailFast mode describe below or [WaitForPodsReady](/docs/tasks/manage/setup_wait_for_pods_ready/)
 and configuring `waitForPodsReady.recoveryTimeout`, to prevent the workloads from
-waiting for the replacement indefinetly.
+waiting for the replacement indefinitely.
+
+#### Fast Hot swap
+{{% alert title="Note" color="primary" %}}
+To enable the feature, you have to set the [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/)
+`TASFailedNodeReplacementFailFast` to `true`. This feature was introduced to Kueue in version 0.13.
+{{% /alert %}}
+
+By default, Kueue tries to find a replacement for a failed node until it succeeds or until the workload is evicted (for example, by `waitForPodsReady.recoveryTimeout`). To prevent Kueue from retrying indefinitely, you can enable the `TASFailedNodeReplacementFailFast` feature gate. When enabled, Kueue will only attempt to find a replacement node once. If it fails, it will not try again, and the workload will get evicted and requeued.
+
+#### Usage Scenarios
+
+Here are a few scenarios that can happen when both `TASReplaceNodeOnPodTermination` and `TASFailedNodeReplacementFailFast` are enabled:
+
+1. **Node becomes `NotReady`, pods are terminated, and a replacement is found:**
+   - A node running a pod from a TAS workload becomes `NotReady`.
+   - The pods on that node are terminated.
+   - With `TASReplaceNodeOnPodTermination` enabled, Kueue immediately looks for a replacement.
+   - A replacement node is available, and Kueue successfully swaps the failed node.
+   - The workload continues running on the new node.
+
+2. **Node becomes `NotReady`, pods are terminated, and no replacement is found:**
+   - A node running a pod from a TAS workload becomes `NotReady`.
+   - The pods on that node are terminated.
+   - Kueue immediately looks for a replacement but cannot find one.
+   - With `TASFailedNodeReplacementFailFast` enabled, Kueue will not retry.
+   - The workload immediately gets evicted (doesn't wait 30s or until waits for pod ready) and requeued
+
+3. **Node gets deleted**
+   - Same scenarios apply as in 1. and 2.
+
+4. **Node requires the whole rack and one of the Nodes becomes `NotRead`:**
+   - A node running a pod from a TAS workload becomes `NotReady`.
+   - The pods on that node are terminated.
+   - Kueue immediately looks for a replacement but since the workload asked for the whole rack in the first place, it cannot find one.
```

  I would make it more boring but less confusing 😃 
  
  ```suggestion
     - Kueue immediately looks for a replacement but since the workload requires the whole rack, it cannot find the replacement.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-16T11:39:41Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:147** by [@mimowo](https://github.com/mimowo) — 2025-09-16T11:39:41Z
```diff
@@ -139,13 +139,56 @@ of the entire TAS workload we introduce the node hot swap feature.
 With this feature, TAS tries to find a replacement of the failed or deleted node for
 all the affected workloads, without changing the rest of the topology assignment.
 Currently this works only for a single node failure at the time and in case of multiple failures,
-the workload gets evicted. The node is assumed to have failed if its `conditions.Status.Ready`
-is not `True` for at least 30 seconds or if the node is missing (removed from the cluster).
+the workload gets evicted.
 
-Note that finding a replacement node within the old domain (like rack) may not always
-be possible. Hence, we recommend using [WaitForPodsReady](/docs/tasks/manage/setup_wait_for_pods_ready/)
+By default, the node is assumed to have failed if its `conditions.Status.Ready`
+is not `True` for at least 30 seconds or if the node is missing (removed from the cluster).
+Since Kueue v0.13, you can enable the `TASReplaceNodeOnPodTermination` feature gate, which adds an additional heuristic:
+a node is also considered failed if it is `NotReady` and at least one of the workload's Pods scheduled on that node are terminated or terminating.
```

  Yes, please fix the docs to align with the implementation. Unless we have a case for changing the impl.
