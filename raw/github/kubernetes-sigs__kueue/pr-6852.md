# PR #6852: [#6136] Integraton tests for ManagedJobsNamespaceSelectorAlwaysRespected

**Summary**: [#6136] Integraton tests for ManagedJobsNamespaceSelectorAlwaysRespected

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6852

**Last updated**: 2025-10-09T07:39:03Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@Singularity23x0](https://github.com/Singularity23x0)
- **Branch**: `jobs-namespace-selector-it` → `main`
- **Created**: 2025-09-16T11:10:57Z
- **Updated**: 2025-10-09T07:39:03Z
- **Merged**: 2025-10-09T07:39:01Z
- **Closed**: 2025-10-09T07:39:01Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@pajakd](https://github.com/pajakd)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92)
- **Changed files**: 1   **+122 / -0**

## Description

#### What type of PR is this?
/kind cleanup

#### What this PR does / why we need it:

Adds an integration test case for ManagedJobsNamespaceSelectorAlwaysRespected in test/integration/singlecluster/jobs/job/job_controller_test.go. It is as discussed in in https://github.com/kubernetes-sigs/kueue/pull/5638#discussion_r2216302030 - such tests were not implemented there but instead were planned for implementation in [#6136].

#### Which issue(s) this PR fixes:
Fixes #6136

#### Special notes for your reviewer:
NONE

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-09-16T11:11:03Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 5de05dcbcfe11953a7fa25cfd7a18aa4e89a9eee |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68e52b6c214ce000079809e4 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-16T11:11:07Z

Hi @Singularity23x0. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-16T11:48:05Z

/ok-to-test

### Comment by [@PannagaRao](https://github.com/PannagaRao) — 2025-09-16T16:37:33Z

LGTM!

### Comment by [@kannon92](https://github.com/kannon92) — 2025-09-16T21:00:52Z

Please fix the verify step.

Otherwise, LGTM!

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-17T05:18:02Z

/release-note-none

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-17T06:23:53Z

/hold
Please make sure the test is failing when the feature gate ManagedJobsNamespaceSelectorAlwaysRespected
 is disabled

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-17T10:16:07Z

/test pull-kueue-test-e2e-multikueue-main

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-17T13:44:59Z

/test pull-kueue-test-e2e-multikueue-main
unrelated infra failure

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-18T13:29:52Z

/approve
leaving lgtm to @mbobrovskyi

### Comment by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-09-19T08:53:49Z

> /hold Please make sure the test is failing when the feature gate ManagedJobsNamespaceSelectorAlwaysRespected is disabled

Seems the test is working even when the flag is disabled. Working on fixing this.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-26T13:28:21Z

@Singularity23x0 what is the status here? cc @mbobrovskyi

### Comment by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-09-29T14:53:10Z

> @Singularity23x0 what is the status here? cc @mbobrovskyi

Fixed and can confirm the test fails when feature disabled.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-29T14:56:44Z

> /hold
> Please make sure the test is failing when the feature gate ManagedJobsNamespaceSelectorAlwaysRespected
> is disabled

/unhold
After:

> Fixed and can confirm the test fails when feature disabled.

Thank you 👍

### Comment by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-09-29T15:30:05Z

> > /hold
> > Please make sure the test is failing when the feature gate ManagedJobsNamespaceSelectorAlwaysRespected
> > is disabled
> 
> /unhold After:
> 
> > Fixed and can confirm the test fails when feature disabled.
> 
> Thank you 👍

There seems to be some other issue still present regarding the setup - still investigating.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-29T15:36:55Z

https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/6852/pull-kueue-test-integration-baseline-main/1972675939150073856

looks like a flake? ptal

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-29T16:54:16Z

/test pull-kueue-test-integration-multikueue-main
looks like flake might be related to https://github.com/kubernetes-sigs/kueue/issues/7033

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-29T16:55:09Z

https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/6852/pull-kueue-test-integration-baseline-main/1972689399950872576
This looks still like a valid test failure which needs to be investigated as part of the PR

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-01T06:59:43Z

@Singularity23x0  @kannon92 I would like to graduate the feature gate to Beta in 0.15 (after two releases). To make this reliable adding this integration test is important. Let me know if you hit some complications.

cc @mbobrovskyi if you can spot some issues in the test code

### Comment by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-10-07T11:32:47Z

> @Singularity23x0 @kannon92 I would like to graduate the feature gate to Beta in 0.15 (after two releases). To make this reliable adding this integration test is important. Let me know if you hit some complications.
> 
> cc @mbobrovskyi if you can spot some issues in the test code

Apologies for the delay, I was OOO in the last week. I was able to finally fix the config paired with a new test covering the flag-off case to ensure test correctness/reliability. PTAL.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-07T12:27:42Z

@Singularity23x0 please set the release note in the release-note block as `NONE`

<img width="596" height="131" alt="image" src="https://github.com/user-attachments/assets/5aa77cfc-65e6-4795-a068-a51b087d2494" />

### Comment by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-10-07T14:48:44Z

/retest

### Comment by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-10-08T10:06:51Z

All test passing now - @mimowo @kannon92 @mbobrovskyi ptal at your leisure.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-08T14:50:04Z

/assign @pajakd 
for an additional pass

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-08T14:50:45Z

@Singularity23x0 can you confirm the latest version of the test fails if ManagedJobsNamespaceSelectorAlwaysRespected was disabled?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-08T14:53:27Z

/approve
Leaving lgtm to @pajakd

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-08T14:53:37Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6852#issuecomment-3381949048" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6852#" title="Author self-approved">Singularity23x0</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@pajakd](https://github.com/pajakd) — 2025-10-09T07:12:24Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-09T07:12:32Z

LGTM label has been added.  <details>Git tree hash: cceddaa7a84d3eebd4e28e4d59a46794d9856711</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-09T07:25:09Z

Thanks, I opened the follow up issue, I think we are ready for: https://github.com/kubernetes-sigs/kueue/issues/7213

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-16T11:48:49Z

cc @kannon92 @mbobrovskyi would you like to it a pass

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-17T01:21:26Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:40** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-17T01:21:25Z
```diff
@@ -37,7 +37,6 @@ import (
 	"k8s.io/utils/ptr"
 	ctrl "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/client"
-
```

  Revert this line to fix `make verify`.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-17T01:24:54Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:3400** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-17T01:24:54Z
```diff
@@ -3367,3 +3367,115 @@ var _ = ginkgo.Describe("Job with elastic jobs via workload-slices support", gin
 		util.ExpectWorkloadToFinish(ctx, k8sClient, client.ObjectKeyFromObject(lowPriorityWorkloadSlice))
 	})
 })
+
+var _ = ginkgo.Describe("Job reconciliation with ManagedJobsNamespaceSelectorAlwaysRespected enabled", ginkgo.Ordered, func() {
+	const (
+		cqName = "cluster-queue"
+		rfName = "default"
+		lqName = "user-queue"
+	)
+
+	var (
+		rf          *kueue.ResourceFlavor
+		lq          *kueue.LocalQueue
+		cq          *kueue.ClusterQueue
+		managedNs   *corev1.Namespace
+		unmanagedNs *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg,
+			managerSetup(
+				jobframework.WithManagedJobsNamespaceSelector(util.NewManagedNamespaceSelector()),
+			),
+		)
+
+		managedNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "managed-ns-",
+				Labels: map[string]string{
+					"managed-by-kueue": "true",
+				},
+			},
+		}
```

  ```suggestion
  		managedNs = testing.MakeNamespaceWrapper("").
  			GenerateName("managed-ns-").
  			Label("managed-by-kueue", "true").
  			Obj()
  ```
  Please use wrapper instead.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-17T01:26:00Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:3410** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-17T01:25:59Z
```diff
@@ -3367,3 +3367,115 @@ var _ = ginkgo.Describe("Job with elastic jobs via workload-slices support", gin
 		util.ExpectWorkloadToFinish(ctx, k8sClient, client.ObjectKeyFromObject(lowPriorityWorkloadSlice))
 	})
 })
+
+var _ = ginkgo.Describe("Job reconciliation with ManagedJobsNamespaceSelectorAlwaysRespected enabled", ginkgo.Ordered, func() {
+	const (
+		cqName = "cluster-queue"
+		rfName = "default"
+		lqName = "user-queue"
+	)
+
+	var (
+		rf          *kueue.ResourceFlavor
+		lq          *kueue.LocalQueue
+		cq          *kueue.ClusterQueue
+		managedNs   *corev1.Namespace
+		unmanagedNs *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg,
+			managerSetup(
+				jobframework.WithManagedJobsNamespaceSelector(util.NewManagedNamespaceSelector()),
+			),
+		)
+
+		managedNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "managed-ns-",
+				Labels: map[string]string{
+					"managed-by-kueue": "true",
+				},
+			},
+		}
+		util.MustCreate(ctx, k8sClient, managedNs)
+
+		unmanagedNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "unmanaged-ns-",
+				Labels: map[string]string{
+					"managed-by-kueue": "false",
+				},
+			},
+		}
```

  ```suggestion
  		unmanagedNs = testing.MakeNamespaceWrapper("").
  			GenerateName("unmanaged-ns-").
  			Label("managed-by-kueue", "false").
  			Obj()
  ```
  
  And here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-17T01:36:15Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:3389** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-17T01:36:15Z
```diff
@@ -3367,3 +3367,115 @@ var _ = ginkgo.Describe("Job with elastic jobs via workload-slices support", gin
 		util.ExpectWorkloadToFinish(ctx, k8sClient, client.ObjectKeyFromObject(lowPriorityWorkloadSlice))
 	})
 })
+
+var _ = ginkgo.Describe("Job reconciliation with ManagedJobsNamespaceSelectorAlwaysRespected enabled", ginkgo.Ordered, func() {
+	const (
+		cqName = "cluster-queue"
+		rfName = "default"
+		lqName = "user-queue"
+	)
+
+	var (
+		rf          *kueue.ResourceFlavor
+		lq          *kueue.LocalQueue
+		cq          *kueue.ClusterQueue
+		managedNs   *corev1.Namespace
+		unmanagedNs *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg,
+			managerSetup(
+				jobframework.WithManagedJobsNamespaceSelector(util.NewManagedNamespaceSelector()),
```

  ```suggestion
  				jobframework.WithManagedJobsNamespaceSelector(labels.SelectorFromSet(map[string]string{"managed-by-kueue": "true"})),
  ```
  I'm wondering if we can reuse SelectorFromSet() instead of creation new function.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-17T01:40:16Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:3475** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-17T01:40:16Z
```diff
@@ -3367,3 +3367,115 @@ var _ = ginkgo.Describe("Job with elastic jobs via workload-slices support", gin
 		util.ExpectWorkloadToFinish(ctx, k8sClient, client.ObjectKeyFromObject(lowPriorityWorkloadSlice))
 	})
 })
+
+var _ = ginkgo.Describe("Job reconciliation with ManagedJobsNamespaceSelectorAlwaysRespected enabled", ginkgo.Ordered, func() {
+	const (
+		cqName = "cluster-queue"
+		rfName = "default"
+		lqName = "user-queue"
+	)
+
+	var (
+		rf          *kueue.ResourceFlavor
+		lq          *kueue.LocalQueue
+		cq          *kueue.ClusterQueue
+		managedNs   *corev1.Namespace
+		unmanagedNs *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg,
+			managerSetup(
+				jobframework.WithManagedJobsNamespaceSelector(util.NewManagedNamespaceSelector()),
+			),
+		)
+
+		managedNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "managed-ns-",
+				Labels: map[string]string{
+					"managed-by-kueue": "true",
+				},
+			},
+		}
+		util.MustCreate(ctx, k8sClient, managedNs)
+
+		unmanagedNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "unmanaged-ns-",
+				Labels: map[string]string{
+					"managed-by-kueue": "false",
+				},
+			},
+		}
+		util.MustCreate(ctx, k8sClient, unmanagedNs)
+
+		rf = testing.MakeResourceFlavor(rfName).Obj()
+		util.MustCreate(ctx, k8sClient, rf)
+
+		cq = testing.MakeClusterQueue(cqName).
+			ResourceGroup(*testing.MakeFlavorQuotas(rfName).Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		util.MustCreate(ctx, k8sClient, cq)
+
+		lq = testing.MakeLocalQueue(lqName, managedNs.Name).ClusterQueue(cqName).Obj()
+		util.MustCreate(ctx, k8sClient, lq)
+	})
+
+	ginkgo.BeforeEach(func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.ManagedJobsNamespaceSelectorAlwaysRespected, true)
+	})
+
+	ginkgo.AfterAll(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, managedNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, unmanagedNs)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, rf, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, lq, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, cq, true, util.LongTimeout)
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.It("should not reconcile a job in an unmanaged namespace", func() {
+		job := testingjob.MakeJob("unmanaged-job", unmanagedNs.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Suspend(true).
+			Image(util.GetAgnHostImage(), util.BehaviorExitFast).
+			Obj()
+
+		gomega.Expect(k8sClient.Create(ctx, job)).To(gomega.Succeed())
+
+		wls := &kueue.WorkloadList{}
+		gomega.Expect(k8sClient.List(ctx, wls, client.InNamespace(unmanagedNs.Name))).To(gomega.Succeed())
+		gomega.Expect(wls.Items).To(gomega.BeEmpty(), "Expected no workload in unmanaged namespace")
+	})
+
+	ginkgo.It("should reconcile a job in managed namespace and create a workload", func() {
+		job := testingjob.MakeJob("managed-job", managedNs.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Suspend(true).
+			Image(util.GetAgnHostImage(), util.BehaviorExitFast).
+			Obj()
+
+		gomega.Expect(k8sClient.Create(ctx, job)).To(gomega.Succeed())
+
+		ginkgo.By("check that only one workload is created", func() {
+			createdWorkloads := &kueue.WorkloadList{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.List(ctx, createdWorkloads, client.InNamespace(managedNs.Name))).To(gomega.Succeed())
+				g.Expect(createdWorkloads.Items).To(gomega.HaveLen(1))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+	})
+
+	ginkgo.It("Verify existing behavior - should not reconcile a pod in an unmanaged namespace", func() {
+		testPod := testingpod.MakePod("test-pod", unmanagedNs.Name).
+			Queue(lq.Name).
+			Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, testPod)).To(gomega.Succeed())
```

  Why pod? Pod is not enabled here.

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2025-09-17T10:03:27Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:3475** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-09-17T10:03:27Z
```diff
@@ -3367,3 +3367,115 @@ var _ = ginkgo.Describe("Job with elastic jobs via workload-slices support", gin
 		util.ExpectWorkloadToFinish(ctx, k8sClient, client.ObjectKeyFromObject(lowPriorityWorkloadSlice))
 	})
 })
+
+var _ = ginkgo.Describe("Job reconciliation with ManagedJobsNamespaceSelectorAlwaysRespected enabled", ginkgo.Ordered, func() {
+	const (
+		cqName = "cluster-queue"
+		rfName = "default"
+		lqName = "user-queue"
+	)
+
+	var (
+		rf          *kueue.ResourceFlavor
+		lq          *kueue.LocalQueue
+		cq          *kueue.ClusterQueue
+		managedNs   *corev1.Namespace
+		unmanagedNs *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg,
+			managerSetup(
+				jobframework.WithManagedJobsNamespaceSelector(util.NewManagedNamespaceSelector()),
+			),
+		)
+
+		managedNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "managed-ns-",
+				Labels: map[string]string{
+					"managed-by-kueue": "true",
+				},
+			},
+		}
+		util.MustCreate(ctx, k8sClient, managedNs)
+
+		unmanagedNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "unmanaged-ns-",
+				Labels: map[string]string{
+					"managed-by-kueue": "false",
+				},
+			},
+		}
+		util.MustCreate(ctx, k8sClient, unmanagedNs)
+
+		rf = testing.MakeResourceFlavor(rfName).Obj()
+		util.MustCreate(ctx, k8sClient, rf)
+
+		cq = testing.MakeClusterQueue(cqName).
+			ResourceGroup(*testing.MakeFlavorQuotas(rfName).Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		util.MustCreate(ctx, k8sClient, cq)
+
+		lq = testing.MakeLocalQueue(lqName, managedNs.Name).ClusterQueue(cqName).Obj()
+		util.MustCreate(ctx, k8sClient, lq)
+	})
+
+	ginkgo.BeforeEach(func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.ManagedJobsNamespaceSelectorAlwaysRespected, true)
+	})
+
+	ginkgo.AfterAll(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, managedNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, unmanagedNs)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, rf, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, lq, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, cq, true, util.LongTimeout)
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.It("should not reconcile a job in an unmanaged namespace", func() {
+		job := testingjob.MakeJob("unmanaged-job", unmanagedNs.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Suspend(true).
+			Image(util.GetAgnHostImage(), util.BehaviorExitFast).
+			Obj()
+
+		gomega.Expect(k8sClient.Create(ctx, job)).To(gomega.Succeed())
+
+		wls := &kueue.WorkloadList{}
+		gomega.Expect(k8sClient.List(ctx, wls, client.InNamespace(unmanagedNs.Name))).To(gomega.Succeed())
+		gomega.Expect(wls.Items).To(gomega.BeEmpty(), "Expected no workload in unmanaged namespace")
+	})
+
+	ginkgo.It("should reconcile a job in managed namespace and create a workload", func() {
+		job := testingjob.MakeJob("managed-job", managedNs.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Suspend(true).
+			Image(util.GetAgnHostImage(), util.BehaviorExitFast).
+			Obj()
+
+		gomega.Expect(k8sClient.Create(ctx, job)).To(gomega.Succeed())
+
+		ginkgo.By("check that only one workload is created", func() {
+			createdWorkloads := &kueue.WorkloadList{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.List(ctx, createdWorkloads, client.InNamespace(managedNs.Name))).To(gomega.Succeed())
+				g.Expect(createdWorkloads.Items).To(gomega.HaveLen(1))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+	})
+
+	ginkgo.It("Verify existing behavior - should not reconcile a pod in an unmanaged namespace", func() {
+		testPod := testingpod.MakePod("test-pod", unmanagedNs.Name).
+			Queue(lq.Name).
+			Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, testPod)).To(gomega.Succeed())
```

  This test was a direct adaptation from /usr/local/google/home/dpastuszka/gitrepos/kueue/test/e2e/customconfigs/reconcile_test.go where the setup is identical. Is there a significant difference I am not aware of here?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-17T10:16:46Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:3475** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-17T10:16:46Z
```diff
@@ -3367,3 +3367,115 @@ var _ = ginkgo.Describe("Job with elastic jobs via workload-slices support", gin
 		util.ExpectWorkloadToFinish(ctx, k8sClient, client.ObjectKeyFromObject(lowPriorityWorkloadSlice))
 	})
 })
+
+var _ = ginkgo.Describe("Job reconciliation with ManagedJobsNamespaceSelectorAlwaysRespected enabled", ginkgo.Ordered, func() {
+	const (
+		cqName = "cluster-queue"
+		rfName = "default"
+		lqName = "user-queue"
+	)
+
+	var (
+		rf          *kueue.ResourceFlavor
+		lq          *kueue.LocalQueue
+		cq          *kueue.ClusterQueue
+		managedNs   *corev1.Namespace
+		unmanagedNs *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg,
+			managerSetup(
+				jobframework.WithManagedJobsNamespaceSelector(util.NewManagedNamespaceSelector()),
+			),
+		)
+
+		managedNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "managed-ns-",
+				Labels: map[string]string{
+					"managed-by-kueue": "true",
+				},
+			},
+		}
+		util.MustCreate(ctx, k8sClient, managedNs)
+
+		unmanagedNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "unmanaged-ns-",
+				Labels: map[string]string{
+					"managed-by-kueue": "false",
+				},
+			},
+		}
+		util.MustCreate(ctx, k8sClient, unmanagedNs)
+
+		rf = testing.MakeResourceFlavor(rfName).Obj()
+		util.MustCreate(ctx, k8sClient, rf)
+
+		cq = testing.MakeClusterQueue(cqName).
+			ResourceGroup(*testing.MakeFlavorQuotas(rfName).Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		util.MustCreate(ctx, k8sClient, cq)
+
+		lq = testing.MakeLocalQueue(lqName, managedNs.Name).ClusterQueue(cqName).Obj()
+		util.MustCreate(ctx, k8sClient, lq)
+	})
+
+	ginkgo.BeforeEach(func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.ManagedJobsNamespaceSelectorAlwaysRespected, true)
+	})
+
+	ginkgo.AfterAll(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, managedNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, unmanagedNs)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, rf, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, lq, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, cq, true, util.LongTimeout)
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.It("should not reconcile a job in an unmanaged namespace", func() {
+		job := testingjob.MakeJob("unmanaged-job", unmanagedNs.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Suspend(true).
+			Image(util.GetAgnHostImage(), util.BehaviorExitFast).
+			Obj()
+
+		gomega.Expect(k8sClient.Create(ctx, job)).To(gomega.Succeed())
+
+		wls := &kueue.WorkloadList{}
+		gomega.Expect(k8sClient.List(ctx, wls, client.InNamespace(unmanagedNs.Name))).To(gomega.Succeed())
+		gomega.Expect(wls.Items).To(gomega.BeEmpty(), "Expected no workload in unmanaged namespace")
+	})
+
+	ginkgo.It("should reconcile a job in managed namespace and create a workload", func() {
+		job := testingjob.MakeJob("managed-job", managedNs.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Suspend(true).
+			Image(util.GetAgnHostImage(), util.BehaviorExitFast).
+			Obj()
+
+		gomega.Expect(k8sClient.Create(ctx, job)).To(gomega.Succeed())
+
+		ginkgo.By("check that only one workload is created", func() {
+			createdWorkloads := &kueue.WorkloadList{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.List(ctx, createdWorkloads, client.InNamespace(managedNs.Name))).To(gomega.Succeed())
+				g.Expect(createdWorkloads.Items).To(gomega.HaveLen(1))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+	})
+
+	ginkgo.It("Verify existing behavior - should not reconcile a pod in an unmanaged namespace", func() {
+		testPod := testingpod.MakePod("test-pod", unmanagedNs.Name).
+			Queue(lq.Name).
+			Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, testPod)).To(gomega.Succeed())
```

  Yes, Pod integration is enabled in the e2e test, but it's disabled here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-17T10:18:14Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:3475** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-17T10:18:14Z
```diff
@@ -3367,3 +3367,115 @@ var _ = ginkgo.Describe("Job with elastic jobs via workload-slices support", gin
 		util.ExpectWorkloadToFinish(ctx, k8sClient, client.ObjectKeyFromObject(lowPriorityWorkloadSlice))
 	})
 })
+
+var _ = ginkgo.Describe("Job reconciliation with ManagedJobsNamespaceSelectorAlwaysRespected enabled", ginkgo.Ordered, func() {
+	const (
+		cqName = "cluster-queue"
+		rfName = "default"
+		lqName = "user-queue"
+	)
+
+	var (
+		rf          *kueue.ResourceFlavor
+		lq          *kueue.LocalQueue
+		cq          *kueue.ClusterQueue
+		managedNs   *corev1.Namespace
+		unmanagedNs *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg,
+			managerSetup(
+				jobframework.WithManagedJobsNamespaceSelector(util.NewManagedNamespaceSelector()),
+			),
+		)
+
+		managedNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "managed-ns-",
+				Labels: map[string]string{
+					"managed-by-kueue": "true",
+				},
+			},
+		}
+		util.MustCreate(ctx, k8sClient, managedNs)
+
+		unmanagedNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "unmanaged-ns-",
+				Labels: map[string]string{
+					"managed-by-kueue": "false",
+				},
+			},
+		}
+		util.MustCreate(ctx, k8sClient, unmanagedNs)
+
+		rf = testing.MakeResourceFlavor(rfName).Obj()
+		util.MustCreate(ctx, k8sClient, rf)
+
+		cq = testing.MakeClusterQueue(cqName).
+			ResourceGroup(*testing.MakeFlavorQuotas(rfName).Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		util.MustCreate(ctx, k8sClient, cq)
+
+		lq = testing.MakeLocalQueue(lqName, managedNs.Name).ClusterQueue(cqName).Obj()
+		util.MustCreate(ctx, k8sClient, lq)
+	})
+
+	ginkgo.BeforeEach(func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.ManagedJobsNamespaceSelectorAlwaysRespected, true)
+	})
+
+	ginkgo.AfterAll(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, managedNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, unmanagedNs)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, rf, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, lq, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, cq, true, util.LongTimeout)
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.It("should not reconcile a job in an unmanaged namespace", func() {
+		job := testingjob.MakeJob("unmanaged-job", unmanagedNs.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Suspend(true).
+			Image(util.GetAgnHostImage(), util.BehaviorExitFast).
+			Obj()
+
+		gomega.Expect(k8sClient.Create(ctx, job)).To(gomega.Succeed())
+
+		wls := &kueue.WorkloadList{}
+		gomega.Expect(k8sClient.List(ctx, wls, client.InNamespace(unmanagedNs.Name))).To(gomega.Succeed())
+		gomega.Expect(wls.Items).To(gomega.BeEmpty(), "Expected no workload in unmanaged namespace")
+	})
+
+	ginkgo.It("should reconcile a job in managed namespace and create a workload", func() {
+		job := testingjob.MakeJob("managed-job", managedNs.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Suspend(true).
+			Image(util.GetAgnHostImage(), util.BehaviorExitFast).
+			Obj()
+
+		gomega.Expect(k8sClient.Create(ctx, job)).To(gomega.Succeed())
+
+		ginkgo.By("check that only one workload is created", func() {
+			createdWorkloads := &kueue.WorkloadList{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.List(ctx, createdWorkloads, client.InNamespace(managedNs.Name))).To(gomega.Succeed())
+				g.Expect(createdWorkloads.Items).To(gomega.HaveLen(1))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+	})
+
+	ginkgo.It("Verify existing behavior - should not reconcile a pod in an unmanaged namespace", func() {
+		testPod := testingpod.MakePod("test-pod", unmanagedNs.Name).
+			Queue(lq.Name).
+			Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, testPod)).To(gomega.Succeed())
```

  https://github.com/kubernetes-sigs/kueue/blob/f6ec3017428a425b0b3e33a3417ed1f10c1bc8ae/test/integration/singlecluster/controller/jobs/job/suite_test.go#L68-L84

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-17T10:21:41Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:3475** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-17T10:21:41Z
```diff
@@ -3367,3 +3367,115 @@ var _ = ginkgo.Describe("Job with elastic jobs via workload-slices support", gin
 		util.ExpectWorkloadToFinish(ctx, k8sClient, client.ObjectKeyFromObject(lowPriorityWorkloadSlice))
 	})
 })
+
+var _ = ginkgo.Describe("Job reconciliation with ManagedJobsNamespaceSelectorAlwaysRespected enabled", ginkgo.Ordered, func() {
+	const (
+		cqName = "cluster-queue"
+		rfName = "default"
+		lqName = "user-queue"
+	)
+
+	var (
+		rf          *kueue.ResourceFlavor
+		lq          *kueue.LocalQueue
+		cq          *kueue.ClusterQueue
+		managedNs   *corev1.Namespace
+		unmanagedNs *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg,
+			managerSetup(
+				jobframework.WithManagedJobsNamespaceSelector(util.NewManagedNamespaceSelector()),
+			),
+		)
+
+		managedNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "managed-ns-",
+				Labels: map[string]string{
+					"managed-by-kueue": "true",
+				},
+			},
+		}
+		util.MustCreate(ctx, k8sClient, managedNs)
+
+		unmanagedNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "unmanaged-ns-",
+				Labels: map[string]string{
+					"managed-by-kueue": "false",
+				},
+			},
+		}
+		util.MustCreate(ctx, k8sClient, unmanagedNs)
+
+		rf = testing.MakeResourceFlavor(rfName).Obj()
+		util.MustCreate(ctx, k8sClient, rf)
+
+		cq = testing.MakeClusterQueue(cqName).
+			ResourceGroup(*testing.MakeFlavorQuotas(rfName).Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		util.MustCreate(ctx, k8sClient, cq)
+
+		lq = testing.MakeLocalQueue(lqName, managedNs.Name).ClusterQueue(cqName).Obj()
+		util.MustCreate(ctx, k8sClient, lq)
+	})
+
+	ginkgo.BeforeEach(func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.ManagedJobsNamespaceSelectorAlwaysRespected, true)
+	})
+
+	ginkgo.AfterAll(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, managedNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, unmanagedNs)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, rf, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, lq, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, cq, true, util.LongTimeout)
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.It("should not reconcile a job in an unmanaged namespace", func() {
+		job := testingjob.MakeJob("unmanaged-job", unmanagedNs.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Suspend(true).
+			Image(util.GetAgnHostImage(), util.BehaviorExitFast).
+			Obj()
+
+		gomega.Expect(k8sClient.Create(ctx, job)).To(gomega.Succeed())
+
+		wls := &kueue.WorkloadList{}
+		gomega.Expect(k8sClient.List(ctx, wls, client.InNamespace(unmanagedNs.Name))).To(gomega.Succeed())
+		gomega.Expect(wls.Items).To(gomega.BeEmpty(), "Expected no workload in unmanaged namespace")
+	})
+
+	ginkgo.It("should reconcile a job in managed namespace and create a workload", func() {
+		job := testingjob.MakeJob("managed-job", managedNs.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Suspend(true).
+			Image(util.GetAgnHostImage(), util.BehaviorExitFast).
+			Obj()
+
+		gomega.Expect(k8sClient.Create(ctx, job)).To(gomega.Succeed())
+
+		ginkgo.By("check that only one workload is created", func() {
+			createdWorkloads := &kueue.WorkloadList{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.List(ctx, createdWorkloads, client.InNamespace(managedNs.Name))).To(gomega.Succeed())
+				g.Expect(createdWorkloads.Items).To(gomega.HaveLen(1))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+	})
+
+	ginkgo.It("Verify existing behavior - should not reconcile a pod in an unmanaged namespace", func() {
+		testPod := testingpod.MakePod("test-pod", unmanagedNs.Name).
+			Queue(lq.Name).
+			Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, testPod)).To(gomega.Succeed())
```

  Are you sure we need to check this here? Could we create a similar test case in the Pod integration tests instead?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-17T10:24:46Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:3449** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-17T10:24:45Z
```diff
@@ -3367,3 +3368,107 @@ var _ = ginkgo.Describe("Job with elastic jobs via workload-slices support", gin
 		util.ExpectWorkloadToFinish(ctx, k8sClient, client.ObjectKeyFromObject(lowPriorityWorkloadSlice))
 	})
 })
+
+var _ = ginkgo.Describe("Job reconciliation with ManagedJobsNamespaceSelectorAlwaysRespected enabled", ginkgo.Ordered, func() {
+	const (
+		cqName = "cluster-queue"
+		rfName = "default"
+		lqName = "user-queue"
+	)
+
+	var (
+		rf          *kueue.ResourceFlavor
+		lq          *kueue.LocalQueue
+		cq          *kueue.ClusterQueue
+		managedNs   *corev1.Namespace
+		unmanagedNs *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg,
+			managerSetup(
+				jobframework.WithManagedJobsNamespaceSelector(util.NewManagedNamespaceSelector()),
+			),
+		)
+
+		managedNs = testing.MakeNamespaceWrapper("").
+			GenerateName("managed-ns-").
+			Label("managed-by-kueue", "true").
+			Obj()
+		util.MustCreate(ctx, k8sClient, managedNs)
+
+		unmanagedNs = testing.MakeNamespaceWrapper("").
+			GenerateName("unmanaged-ns-").
+			Label("managed-by-kueue", "false").
+			Obj()
+		util.MustCreate(ctx, k8sClient, unmanagedNs)
+
+		rf = testing.MakeResourceFlavor(rfName).Obj()
+		util.MustCreate(ctx, k8sClient, rf)
+
+		cq = testing.MakeClusterQueue(cqName).
+			ResourceGroup(*testing.MakeFlavorQuotas(rfName).Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		util.MustCreate(ctx, k8sClient, cq)
+
+		lq = testing.MakeLocalQueue(lqName, managedNs.Name).ClusterQueue(cqName).Obj()
+		util.MustCreate(ctx, k8sClient, lq)
+	})
+
+	ginkgo.BeforeEach(func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.ManagedJobsNamespaceSelectorAlwaysRespected, true)
+	})
+
+	ginkgo.AfterAll(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, managedNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, unmanagedNs)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, rf, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, lq, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, cq, true, util.LongTimeout)
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.It("should not reconcile a job in an unmanaged namespace", func() {
+		job := testingjob.MakeJob("unmanaged-job", unmanagedNs.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Suspend(true).
+			Image(util.GetAgnHostImage(), util.BehaviorExitFast).
+			Obj()
+
+		gomega.Expect(k8sClient.Create(ctx, job)).To(gomega.Succeed())
+
+		wls := &kueue.WorkloadList{}
+		gomega.Expect(k8sClient.List(ctx, wls, client.InNamespace(unmanagedNs.Name))).To(gomega.Succeed())
+		gomega.Expect(wls.Items).To(gomega.BeEmpty(), "Expected no workload in unmanaged namespace")
+	})
+
+	ginkgo.It("should reconcile a job in managed namespace and create a workload", func() {
+		job := testingjob.MakeJob("managed-job", managedNs.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Suspend(true).
+			Image(util.GetAgnHostImage(), util.BehaviorExitFast).
```

  ```suggestion
  			Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
  ```
  I think BehaviorExitFast will cause many flaky tests. Can we use BehaviorWaitForDeletion instead?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-17T10:27:09Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:3435** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-17T10:27:09Z
```diff
@@ -3367,3 +3368,107 @@ var _ = ginkgo.Describe("Job with elastic jobs via workload-slices support", gin
 		util.ExpectWorkloadToFinish(ctx, k8sClient, client.ObjectKeyFromObject(lowPriorityWorkloadSlice))
 	})
 })
+
+var _ = ginkgo.Describe("Job reconciliation with ManagedJobsNamespaceSelectorAlwaysRespected enabled", ginkgo.Ordered, func() {
+	const (
+		cqName = "cluster-queue"
+		rfName = "default"
+		lqName = "user-queue"
+	)
+
+	var (
+		rf          *kueue.ResourceFlavor
+		lq          *kueue.LocalQueue
+		cq          *kueue.ClusterQueue
+		managedNs   *corev1.Namespace
+		unmanagedNs *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg,
+			managerSetup(
+				jobframework.WithManagedJobsNamespaceSelector(util.NewManagedNamespaceSelector()),
+			),
+		)
+
+		managedNs = testing.MakeNamespaceWrapper("").
+			GenerateName("managed-ns-").
+			Label("managed-by-kueue", "true").
+			Obj()
+		util.MustCreate(ctx, k8sClient, managedNs)
+
+		unmanagedNs = testing.MakeNamespaceWrapper("").
+			GenerateName("unmanaged-ns-").
+			Label("managed-by-kueue", "false").
+			Obj()
+		util.MustCreate(ctx, k8sClient, unmanagedNs)
+
+		rf = testing.MakeResourceFlavor(rfName).Obj()
+		util.MustCreate(ctx, k8sClient, rf)
+
+		cq = testing.MakeClusterQueue(cqName).
+			ResourceGroup(*testing.MakeFlavorQuotas(rfName).Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		util.MustCreate(ctx, k8sClient, cq)
+
+		lq = testing.MakeLocalQueue(lqName, managedNs.Name).ClusterQueue(cqName).Obj()
+		util.MustCreate(ctx, k8sClient, lq)
+	})
+
+	ginkgo.BeforeEach(func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.ManagedJobsNamespaceSelectorAlwaysRespected, true)
+	})
+
+	ginkgo.AfterAll(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, managedNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, unmanagedNs)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, rf, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, lq, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, cq, true, util.LongTimeout)
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.It("should not reconcile a job in an unmanaged namespace", func() {
+		job := testingjob.MakeJob("unmanaged-job", unmanagedNs.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Suspend(true).
+			Image(util.GetAgnHostImage(), util.BehaviorExitFast).
```

  And here.

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2025-09-18T13:26:22Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:3475** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-09-18T13:26:22Z
```diff
@@ -3367,3 +3367,115 @@ var _ = ginkgo.Describe("Job with elastic jobs via workload-slices support", gin
 		util.ExpectWorkloadToFinish(ctx, k8sClient, client.ObjectKeyFromObject(lowPriorityWorkloadSlice))
 	})
 })
+
+var _ = ginkgo.Describe("Job reconciliation with ManagedJobsNamespaceSelectorAlwaysRespected enabled", ginkgo.Ordered, func() {
+	const (
+		cqName = "cluster-queue"
+		rfName = "default"
+		lqName = "user-queue"
+	)
+
+	var (
+		rf          *kueue.ResourceFlavor
+		lq          *kueue.LocalQueue
+		cq          *kueue.ClusterQueue
+		managedNs   *corev1.Namespace
+		unmanagedNs *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg,
+			managerSetup(
+				jobframework.WithManagedJobsNamespaceSelector(util.NewManagedNamespaceSelector()),
+			),
+		)
+
+		managedNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "managed-ns-",
+				Labels: map[string]string{
+					"managed-by-kueue": "true",
+				},
+			},
+		}
+		util.MustCreate(ctx, k8sClient, managedNs)
+
+		unmanagedNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "unmanaged-ns-",
+				Labels: map[string]string{
+					"managed-by-kueue": "false",
+				},
+			},
+		}
+		util.MustCreate(ctx, k8sClient, unmanagedNs)
+
+		rf = testing.MakeResourceFlavor(rfName).Obj()
+		util.MustCreate(ctx, k8sClient, rf)
+
+		cq = testing.MakeClusterQueue(cqName).
+			ResourceGroup(*testing.MakeFlavorQuotas(rfName).Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		util.MustCreate(ctx, k8sClient, cq)
+
+		lq = testing.MakeLocalQueue(lqName, managedNs.Name).ClusterQueue(cqName).Obj()
+		util.MustCreate(ctx, k8sClient, lq)
+	})
+
+	ginkgo.BeforeEach(func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.ManagedJobsNamespaceSelectorAlwaysRespected, true)
+	})
+
+	ginkgo.AfterAll(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, managedNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, unmanagedNs)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, rf, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, lq, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, cq, true, util.LongTimeout)
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.It("should not reconcile a job in an unmanaged namespace", func() {
+		job := testingjob.MakeJob("unmanaged-job", unmanagedNs.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Suspend(true).
+			Image(util.GetAgnHostImage(), util.BehaviorExitFast).
+			Obj()
+
+		gomega.Expect(k8sClient.Create(ctx, job)).To(gomega.Succeed())
+
+		wls := &kueue.WorkloadList{}
+		gomega.Expect(k8sClient.List(ctx, wls, client.InNamespace(unmanagedNs.Name))).To(gomega.Succeed())
+		gomega.Expect(wls.Items).To(gomega.BeEmpty(), "Expected no workload in unmanaged namespace")
+	})
+
+	ginkgo.It("should reconcile a job in managed namespace and create a workload", func() {
+		job := testingjob.MakeJob("managed-job", managedNs.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Suspend(true).
+			Image(util.GetAgnHostImage(), util.BehaviorExitFast).
+			Obj()
+
+		gomega.Expect(k8sClient.Create(ctx, job)).To(gomega.Succeed())
+
+		ginkgo.By("check that only one workload is created", func() {
+			createdWorkloads := &kueue.WorkloadList{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.List(ctx, createdWorkloads, client.InNamespace(managedNs.Name))).To(gomega.Succeed())
+				g.Expect(createdWorkloads.Items).To(gomega.HaveLen(1))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+	})
+
+	ginkgo.It("Verify existing behavior - should not reconcile a pod in an unmanaged namespace", func() {
+		testPod := testingpod.MakePod("test-pod", unmanagedNs.Name).
+			Queue(lq.Name).
+			Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, testPod)).To(gomega.Succeed())
```

  This test seems to just be there to verify existing behavior and I don't see it being vital for this test, as such I would opt to remove it.
  If that is acceptable feel free to resolve this conversation.

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2025-09-18T13:29:45Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:3389** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-09-18T13:29:45Z
```diff
@@ -3367,3 +3367,115 @@ var _ = ginkgo.Describe("Job with elastic jobs via workload-slices support", gin
 		util.ExpectWorkloadToFinish(ctx, k8sClient, client.ObjectKeyFromObject(lowPriorityWorkloadSlice))
 	})
 })
+
+var _ = ginkgo.Describe("Job reconciliation with ManagedJobsNamespaceSelectorAlwaysRespected enabled", ginkgo.Ordered, func() {
+	const (
+		cqName = "cluster-queue"
+		rfName = "default"
+		lqName = "user-queue"
+	)
+
+	var (
+		rf          *kueue.ResourceFlavor
+		lq          *kueue.LocalQueue
+		cq          *kueue.ClusterQueue
+		managedNs   *corev1.Namespace
+		unmanagedNs *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg,
+			managerSetup(
+				jobframework.WithManagedJobsNamespaceSelector(util.NewManagedNamespaceSelector()),
```

  Done.

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2025-09-18T13:40:22Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:3435** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-09-18T13:40:22Z
```diff
@@ -3367,3 +3368,107 @@ var _ = ginkgo.Describe("Job with elastic jobs via workload-slices support", gin
 		util.ExpectWorkloadToFinish(ctx, k8sClient, client.ObjectKeyFromObject(lowPriorityWorkloadSlice))
 	})
 })
+
+var _ = ginkgo.Describe("Job reconciliation with ManagedJobsNamespaceSelectorAlwaysRespected enabled", ginkgo.Ordered, func() {
+	const (
+		cqName = "cluster-queue"
+		rfName = "default"
+		lqName = "user-queue"
+	)
+
+	var (
+		rf          *kueue.ResourceFlavor
+		lq          *kueue.LocalQueue
+		cq          *kueue.ClusterQueue
+		managedNs   *corev1.Namespace
+		unmanagedNs *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg,
+			managerSetup(
+				jobframework.WithManagedJobsNamespaceSelector(util.NewManagedNamespaceSelector()),
+			),
+		)
+
+		managedNs = testing.MakeNamespaceWrapper("").
+			GenerateName("managed-ns-").
+			Label("managed-by-kueue", "true").
+			Obj()
+		util.MustCreate(ctx, k8sClient, managedNs)
+
+		unmanagedNs = testing.MakeNamespaceWrapper("").
+			GenerateName("unmanaged-ns-").
+			Label("managed-by-kueue", "false").
+			Obj()
+		util.MustCreate(ctx, k8sClient, unmanagedNs)
+
+		rf = testing.MakeResourceFlavor(rfName).Obj()
+		util.MustCreate(ctx, k8sClient, rf)
+
+		cq = testing.MakeClusterQueue(cqName).
+			ResourceGroup(*testing.MakeFlavorQuotas(rfName).Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		util.MustCreate(ctx, k8sClient, cq)
+
+		lq = testing.MakeLocalQueue(lqName, managedNs.Name).ClusterQueue(cqName).Obj()
+		util.MustCreate(ctx, k8sClient, lq)
+	})
+
+	ginkgo.BeforeEach(func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.ManagedJobsNamespaceSelectorAlwaysRespected, true)
+	})
+
+	ginkgo.AfterAll(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, managedNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, unmanagedNs)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, rf, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, lq, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, cq, true, util.LongTimeout)
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.It("should not reconcile a job in an unmanaged namespace", func() {
+		job := testingjob.MakeJob("unmanaged-job", unmanagedNs.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Suspend(true).
+			Image(util.GetAgnHostImage(), util.BehaviorExitFast).
```

  Done.

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2025-09-18T13:40:29Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:3449** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-09-18T13:40:29Z
```diff
@@ -3367,3 +3368,107 @@ var _ = ginkgo.Describe("Job with elastic jobs via workload-slices support", gin
 		util.ExpectWorkloadToFinish(ctx, k8sClient, client.ObjectKeyFromObject(lowPriorityWorkloadSlice))
 	})
 })
+
+var _ = ginkgo.Describe("Job reconciliation with ManagedJobsNamespaceSelectorAlwaysRespected enabled", ginkgo.Ordered, func() {
+	const (
+		cqName = "cluster-queue"
+		rfName = "default"
+		lqName = "user-queue"
+	)
+
+	var (
+		rf          *kueue.ResourceFlavor
+		lq          *kueue.LocalQueue
+		cq          *kueue.ClusterQueue
+		managedNs   *corev1.Namespace
+		unmanagedNs *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg,
+			managerSetup(
+				jobframework.WithManagedJobsNamespaceSelector(util.NewManagedNamespaceSelector()),
+			),
+		)
+
+		managedNs = testing.MakeNamespaceWrapper("").
+			GenerateName("managed-ns-").
+			Label("managed-by-kueue", "true").
+			Obj()
+		util.MustCreate(ctx, k8sClient, managedNs)
+
+		unmanagedNs = testing.MakeNamespaceWrapper("").
+			GenerateName("unmanaged-ns-").
+			Label("managed-by-kueue", "false").
+			Obj()
+		util.MustCreate(ctx, k8sClient, unmanagedNs)
+
+		rf = testing.MakeResourceFlavor(rfName).Obj()
+		util.MustCreate(ctx, k8sClient, rf)
+
+		cq = testing.MakeClusterQueue(cqName).
+			ResourceGroup(*testing.MakeFlavorQuotas(rfName).Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		util.MustCreate(ctx, k8sClient, cq)
+
+		lq = testing.MakeLocalQueue(lqName, managedNs.Name).ClusterQueue(cqName).Obj()
+		util.MustCreate(ctx, k8sClient, lq)
+	})
+
+	ginkgo.BeforeEach(func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.ManagedJobsNamespaceSelectorAlwaysRespected, true)
+	})
+
+	ginkgo.AfterAll(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, managedNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, unmanagedNs)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, rf, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, lq, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, cq, true, util.LongTimeout)
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.It("should not reconcile a job in an unmanaged namespace", func() {
+		job := testingjob.MakeJob("unmanaged-job", unmanagedNs.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Suspend(true).
+			Image(util.GetAgnHostImage(), util.BehaviorExitFast).
+			Obj()
+
+		gomega.Expect(k8sClient.Create(ctx, job)).To(gomega.Succeed())
+
+		wls := &kueue.WorkloadList{}
+		gomega.Expect(k8sClient.List(ctx, wls, client.InNamespace(unmanagedNs.Name))).To(gomega.Succeed())
+		gomega.Expect(wls.Items).To(gomega.BeEmpty(), "Expected no workload in unmanaged namespace")
+	})
+
+	ginkgo.It("should reconcile a job in managed namespace and create a workload", func() {
+		job := testingjob.MakeJob("managed-job", managedNs.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Suspend(true).
+			Image(util.GetAgnHostImage(), util.BehaviorExitFast).
```

  Done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-29T14:55:31Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:3693** by [@mimowo](https://github.com/mimowo) — 2025-09-29T14:55:31Z
```diff
@@ -3633,3 +3635,90 @@ var _ = ginkgo.Describe("Job with elastic jobs via workload-slices support", gin
 		util.ExpectEvictedWorkloadsOnceTotalMetric(clusterQueue.Name, kueue.WorkloadEvictedByPreemption, "", highPriorityClass.Name, 0)
 	})
 })
+
+var _ = ginkgo.Describe("Job reconciliation with ManagedJobsNamespaceSelectorAlwaysRespected enabled", ginkgo.Ordered, func() {
+	const (
+		cqName = "cluster-queue"
+		rfName = "default"
+		lqName = "user-queue"
+	)
+
+	var (
+		rf        *kueue.ResourceFlavor
+		lq        *kueue.LocalQueue
+		cq        *kueue.ClusterQueue
+		managedNs *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		var managedByQueueRequirement, err = labels.NewRequirement("managed-by-kueue", selection.In, []string{"true"})
+		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		fwk.StartManager(ctx, cfg,
+			managerSetup(
+				jobframework.WithManagedJobsNamespaceSelector(labels.NewSelector().Add(*managedByQueueRequirement)),
+			),
+		)
+
+		managedNs = testing.MakeNamespaceWrapper("").
+			GenerateName("managed-ns-").
+			Label("managed-by-kueue", "true").
+			Obj()
+		util.MustCreate(ctx, k8sClient, managedNs)
+
+		rf = testing.MakeResourceFlavor(rfName).Obj()
+		util.MustCreate(ctx, k8sClient, rf)
+
+		cq = testing.MakeClusterQueue(cqName).
+			ResourceGroup(*testing.MakeFlavorQuotas(rfName).Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		util.MustCreate(ctx, k8sClient, cq)
+
+		lq = testing.MakeLocalQueue(lqName, managedNs.Name).ClusterQueue(cqName).Obj()
+		util.MustCreate(ctx, k8sClient, lq)
+	})
+
+	ginkgo.BeforeEach(func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.ManagedJobsNamespaceSelectorAlwaysRespected, true)
+	})
+
+	ginkgo.AfterAll(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, managedNs)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, rf, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, lq, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, cq, true, util.LongTimeout)
+		fwk.StopManager(ctx)
```

  I think no need to StopManager explicitly

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2025-10-07T11:53:29Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:3693** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-10-07T11:53:29Z
```diff
@@ -3633,3 +3635,90 @@ var _ = ginkgo.Describe("Job with elastic jobs via workload-slices support", gin
 		util.ExpectEvictedWorkloadsOnceTotalMetric(clusterQueue.Name, kueue.WorkloadEvictedByPreemption, "", highPriorityClass.Name, 0)
 	})
 })
+
+var _ = ginkgo.Describe("Job reconciliation with ManagedJobsNamespaceSelectorAlwaysRespected enabled", ginkgo.Ordered, func() {
+	const (
+		cqName = "cluster-queue"
+		rfName = "default"
+		lqName = "user-queue"
+	)
+
+	var (
+		rf        *kueue.ResourceFlavor
+		lq        *kueue.LocalQueue
+		cq        *kueue.ClusterQueue
+		managedNs *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		var managedByQueueRequirement, err = labels.NewRequirement("managed-by-kueue", selection.In, []string{"true"})
+		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		fwk.StartManager(ctx, cfg,
+			managerSetup(
+				jobframework.WithManagedJobsNamespaceSelector(labels.NewSelector().Add(*managedByQueueRequirement)),
+			),
+		)
+
+		managedNs = testing.MakeNamespaceWrapper("").
+			GenerateName("managed-ns-").
+			Label("managed-by-kueue", "true").
+			Obj()
+		util.MustCreate(ctx, k8sClient, managedNs)
+
+		rf = testing.MakeResourceFlavor(rfName).Obj()
+		util.MustCreate(ctx, k8sClient, rf)
+
+		cq = testing.MakeClusterQueue(cqName).
+			ResourceGroup(*testing.MakeFlavorQuotas(rfName).Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		util.MustCreate(ctx, k8sClient, cq)
+
+		lq = testing.MakeLocalQueue(lqName, managedNs.Name).ClusterQueue(cqName).Obj()
+		util.MustCreate(ctx, k8sClient, lq)
+	})
+
+	ginkgo.BeforeEach(func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.ManagedJobsNamespaceSelectorAlwaysRespected, true)
+	})
+
+	ginkgo.AfterAll(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, managedNs)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, rf, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, lq, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, cq, true, util.LongTimeout)
+		fwk.StopManager(ctx)
```

  Removed.
