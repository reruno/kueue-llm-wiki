# PR #689: [workload] WaitForPodsReady: Requeue at the back of the queue after timeout

**Summary**: [workload] WaitForPodsReady: Requeue at the back of the queue after timeout

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/689

**Last updated**: 2023-04-28T13:00:50Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `evicted_to_end_of_queue` → `main`
- **Created**: 2023-04-07T12:32:52Z
- **Updated**: 2023-04-28T13:00:50Z
- **Merged**: 2023-04-27T14:54:18Z
- **Closed**: 2023-04-27T14:54:18Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `tide/merge-method-squash`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mwielgus](https://github.com/mwielgus), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 15   **+344 / -65**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation


Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

A new workload condition , `Evicted`, is added,  which gets set when:

1. The workload gets preempted
2. The workload hits the PodsReady timeout

In case of  PodsReady timeout, the condition transition timestamp will be used in scheduler sorting, therefore the workload will be moved at the end of the queue. 

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #599

#### Special notes for your reviewer:

The  preemption based `Eviction` will be used in solving #510.

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-04-07T12:32:59Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 468cc4177e8008c2ef932326ab7b76b3996fe39d |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/644a71841bc8490007698835 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-04-07T12:33:01Z

Hi @trasc. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@trasc](https://github.com/trasc) — 2023-04-11T06:49:14Z

/unhold
/cc @alculquicondor 
/cc @mwielgus

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-04-11T13:19:58Z

/ok-to-test

### Comment by [@trasc](https://github.com/trasc) — 2023-04-26T05:08:01Z

/hold cancel

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-26T19:56:58Z

/hold cancel
/lgtm
/label tide/merge-method-squash

I thought we had configuration that prevented merge commits.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-27T14:53:16Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-04-27T14:53:25Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/689#issuecomment-1525842952" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/689#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-12T20:53:38Z

cc @mimowo

- **pkg/controller/core/workload_controller.go:131** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-12T20:29:53Z
```diff
@@ -127,6 +127,21 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling Workload")
 
+	if eviction := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); eviction != nil && eviction.Status == metav1.ConditionTrue &&
+		eviction.Reason == kueue.WorkloadEvictedByPodsReadyTimeout {
```

  I don't think we need to differentiate the reason here.
  Can we just copy the eviction reason and message into the admission condition?

- **pkg/controller/core/workload_controller.go:139** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-12T20:30:54Z
```diff
@@ -127,6 +127,21 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling Workload")
 
+	if eviction := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); eviction != nil && eviction.Status == metav1.ConditionTrue &&
+		eviction.Reason == kueue.WorkloadEvictedByPodsReadyTimeout {
+		if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadAdmitted) {
+			log.V(2).Info("Cancelling admission of the workload due to exceeding the PodsReady timeout")
+			err := workload.UnsetAdmissionWithCondition(ctx, r.client, &wl, "Evicted", fmt.Sprintf("Exceeded the PodsReady timeout %s", req.NamespacedName.String()))
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		} else {
+			// finish up the eviction
+			log.V(2).Info("Reset eviction condition")
+			err := workload.UpdateStatus(ctx, r.client, &wl, kueue.WorkloadEvicted, metav1.ConditionFalse,
```

  We don't need to reset this at this point: the workload is still "evicted".
  It should be reset when the Workload is Admitted again, possibly in the same API call (to minimize load on API server)

- **pkg/controller/core/workload_controller.go:179** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-12T20:35:59Z
```diff
@@ -159,17 +174,18 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 }
 
 func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req ctrl.Request, wl *kueue.Workload) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
 	countingTowardsTimeout, recheckAfter := r.admittedNotReadyWorkload(wl, realClock)
 	if !countingTowardsTimeout {
 		return ctrl.Result{}, nil
 	}
 	if recheckAfter > 0 {
-		klog.V(4).InfoS("Workload not yet ready and did not exceed its timeout", "workload", req.NamespacedName.String(), "recheckAfter", recheckAfter)
+		log.V(4).Info("Workload not yet ready and did not exceed its timeout", "recheckAfter", recheckAfter)
```

  oops, thanks for fixing :)

- **pkg/controller/core/workload_controller.go:186** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-12T20:37:15Z
```diff
@@ -159,17 +174,18 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 }
 
 func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req ctrl.Request, wl *kueue.Workload) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
 	countingTowardsTimeout, recheckAfter := r.admittedNotReadyWorkload(wl, realClock)
 	if !countingTowardsTimeout {
 		return ctrl.Result{}, nil
 	}
 	if recheckAfter > 0 {
-		klog.V(4).InfoS("Workload not yet ready and did not exceed its timeout", "workload", req.NamespacedName.String(), "recheckAfter", recheckAfter)
+		log.V(4).Info("Workload not yet ready and did not exceed its timeout", "recheckAfter", recheckAfter)
 		return ctrl.Result{RequeueAfter: recheckAfter}, nil
 	} else {
-		klog.V(2).InfoS("Cancelling admission of the workload due to exceeding the PodsReady timeout", "workload", req.NamespacedName.String())
-		err := workload.UnsetAdmissionWithCondition(ctx, r.client, wl,
-			"Evicted", fmt.Sprintf("Exceeded the PodsReady timeout %s", req.NamespacedName.String()))
+		log.V(2).Info("Set evicted condition")
```

  This is not true at this point. We are about to set it, but it's not set yet. Either change the wording to talk about what's about to happen or move the log line after the `UpdateStatus` call.
  If you do so, I would only emit the log if there wasn't an error.

- **pkg/queue/cluster_queue_impl.go:96** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-12T20:41:15Z
```diff
@@ -90,7 +91,9 @@ func (c *clusterQueueBase) PushOrUpdate(wInfo *workload.Info) {
 	if oldInfo != nil {
 		// update in place if the workload was inadmissible and didn't change
 		// to potentially become admissible.
-		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) {
+		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) &&
+			equality.Semantic.DeepEqual(apimeta.FindStatusCondition(oldInfo.Obj.Status.Conditions, kueue.WorkloadEvicted),
```

  why does this condition matter here?

- **pkg/scheduler/preemption/preemption.go:145** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-12T20:45:50Z
```diff
@@ -141,6 +141,13 @@ func (p *Preemptor) issuePreemptions(ctx context.Context, targets []*workload.In
 			errCh.SendErrorWithCancel(err, cancel)
 			return
 		}
+
+		err = workload.UpdateStatus(ctx, p.client, target.Obj, kueue.WorkloadEvicted, metav1.ConditionTrue, kueue.WorkloadEvictedByPreemption, "Preempted to accommodate a higher priority Workload", "evict")
```

  Shouldn't `applyPreemption` change to add the Evicted condition only?
  
  Also, we should avoid separate API calls. Consider that the apiserver could accept the first API call, but somehow fail the 2nd one. Or maybe the scheduler could immediately readmit the workload.

- **pkg/workload/workload.go:301** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-12T20:49:20Z
```diff
@@ -294,3 +294,16 @@ func AdmissionPatch(w *kueue.Workload) *kueue.Workload {
 	wlCopy.Status.Admission = w.Status.Admission.DeepCopy()
 	return wlCopy
 }
+
+// GetSchedulingTimestamp return the timestamp to be used by the scheduler. It could
+// be the workload creation time or the last time a PodsReady timeout has occurred.
+func GetSchedulingTimestamp(w *kueue.Workload) *metav1.Time {
+	evictedConditionPosition := FindConditionIndex(&w.Status, kueue.WorkloadEvicted)
```

  we should have gotten rid of `FindConditionIndex`. Let's use the metav1 helpers.

- **pkg/workload/workload.go:304** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-12T20:49:39Z
```diff
@@ -294,3 +294,16 @@ func AdmissionPatch(w *kueue.Workload) *kueue.Workload {
 	wlCopy.Status.Admission = w.Status.Admission.DeepCopy()
 	return wlCopy
 }
+
+// GetSchedulingTimestamp return the timestamp to be used by the scheduler. It could
+// be the workload creation time or the last time a PodsReady timeout has occurred.
+func GetSchedulingTimestamp(w *kueue.Workload) *metav1.Time {
+	evictedConditionPosition := FindConditionIndex(&w.Status, kueue.WorkloadEvicted)
+	if evictedConditionPosition != -1 {
+		evictedCondition := w.Status.Conditions[evictedConditionPosition]
+		if evictedCondition.Reason == kueue.WorkloadEvictedByPodsReadyTimeout {
```

  Uhm.... we are making this specific to Eviction. Let's double check in the issue https://github.com/kubernetes-sigs/kueue/issues/599#issuecomment-1505922654

- **pkg/workload/workload_test.go:231** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-12T20:53:17Z
```diff
@@ -310,3 +311,51 @@ func containersForRequests(requests ...map[corev1.ResourceName]string) []corev1.
 	}
 	return containers
 }
+
+func TestSchedulingTimestamp(t *testing.T) {
+	creationTime := metav1.Now()
+	conditionTime := metav1.NewTime(time.Now().Add(time.Hour))
+	cases := map[string]struct {
+		wl   *kueue.Workload
+		want metav1.Time
+	}{
+		"no condition": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Obj(),
+			want: creationTime,
+		},
+		"evicted by preemption": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Condition(metav1.Condition{
+					Type:               kueue.WorkloadEvicted,
+					Status:             metav1.ConditionTrue,
+					LastTransitionTime: conditionTime,
+					Reason:             kueue.WorkloadEvictedByPreemption,
+				}).
+				Obj(),
+			want: creationTime,
+		},
+		"evicted by PodsReady timeout": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Condition(metav1.Condition{
+					Type:               kueue.WorkloadEvicted,
+					Status:             metav1.ConditionTrue,
```

  Also test False, which should behave the same as "no condition"

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-13T08:22:06Z

- **pkg/controller/core/workload_controller.go:131** by [@trasc](https://github.com/trasc) — 2023-04-13T07:05:43Z
```diff
@@ -127,6 +127,21 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling Workload")
 
+	if eviction := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); eviction != nil && eviction.Status == metav1.ConditionTrue &&
+		eviction.Reason == kueue.WorkloadEvictedByPodsReadyTimeout {
```

  We need it at least for canceling the admission, in case of Preemption we'll do it in the job framework when the job becomes inactive.

- **pkg/controller/core/workload_controller.go:139** by [@trasc](https://github.com/trasc) — 2023-04-13T07:10:40Z
```diff
@@ -127,6 +127,21 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling Workload")
 
+	if eviction := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); eviction != nil && eviction.Status == metav1.ConditionTrue &&
+		eviction.Reason == kueue.WorkloadEvictedByPodsReadyTimeout {
+		if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadAdmitted) {
+			log.V(2).Info("Cancelling admission of the workload due to exceeding the PodsReady timeout")
+			err := workload.UnsetAdmissionWithCondition(ctx, r.client, &wl, "Evicted", fmt.Sprintf("Exceeded the PodsReady timeout %s", req.NamespacedName.String()))
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		} else {
+			// finish up the eviction
+			log.V(2).Info("Reset eviction condition")
+			err := workload.UpdateStatus(ctx, r.client, &wl, kueue.WorkloadEvicted, metav1.ConditionFalse,
```

  Maybe we should change the condition name to `Evicting`, it will make better sense.
  This also makes better sense when the reason is preemption, in that case the `Evicted` is set by the scheduler and the admission in canceled if `Evicted == true && !job.IsActive()` in the job framework.

- **pkg/controller/core/workload_controller.go:179** by [@trasc](https://github.com/trasc) — 2023-04-13T07:11:49Z
```diff
@@ -159,17 +174,18 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 }
 
 func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req ctrl.Request, wl *kueue.Workload) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
 	countingTowardsTimeout, recheckAfter := r.admittedNotReadyWorkload(wl, realClock)
 	if !countingTowardsTimeout {
 		return ctrl.Result{}, nil
 	}
 	if recheckAfter > 0 {
-		klog.V(4).InfoS("Workload not yet ready and did not exceed its timeout", "workload", req.NamespacedName.String(), "recheckAfter", recheckAfter)
+		log.V(4).Info("Workload not yet ready and did not exceed its timeout", "recheckAfter", recheckAfter)
```

  no problem

- **pkg/controller/core/workload_controller.go:186** by [@trasc](https://github.com/trasc) — 2023-04-13T07:14:09Z
```diff
@@ -159,17 +174,18 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 }
 
 func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req ctrl.Request, wl *kueue.Workload) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
 	countingTowardsTimeout, recheckAfter := r.admittedNotReadyWorkload(wl, realClock)
 	if !countingTowardsTimeout {
 		return ctrl.Result{}, nil
 	}
 	if recheckAfter > 0 {
-		klog.V(4).InfoS("Workload not yet ready and did not exceed its timeout", "workload", req.NamespacedName.String(), "recheckAfter", recheckAfter)
+		log.V(4).Info("Workload not yet ready and did not exceed its timeout", "recheckAfter", recheckAfter)
 		return ctrl.Result{RequeueAfter: recheckAfter}, nil
 	} else {
-		klog.V(2).InfoS("Cancelling admission of the workload due to exceeding the PodsReady timeout", "workload", req.NamespacedName.String())
-		err := workload.UnsetAdmissionWithCondition(ctx, r.client, wl,
-			"Evicted", fmt.Sprintf("Exceeded the PodsReady timeout %s", req.NamespacedName.String()))
+		log.V(2).Info("Set evicted condition")
```

  done

- **pkg/queue/cluster_queue_impl.go:96** by [@trasc](https://github.com/trasc) — 2023-04-13T07:17:38Z
```diff
@@ -90,7 +91,9 @@ func (c *clusterQueueBase) PushOrUpdate(wInfo *workload.Info) {
 	if oldInfo != nil {
 		// update in place if the workload was inadmissible and didn't change
 		// to potentially become admissible.
-		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) {
+		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) &&
+			equality.Semantic.DeepEqual(apimeta.FindStatusCondition(oldInfo.Obj.Status.Conditions, kueue.WorkloadEvicted),
```

  The queue should be reordered if the `Eviction` condition changes. Without this the workload will keep it's place in the queue even when his  `Evicted` condition has changed

- **pkg/scheduler/preemption/preemption.go:145** by [@trasc](https://github.com/trasc) — 2023-04-13T07:19:09Z
```diff
@@ -141,6 +141,13 @@ func (p *Preemptor) issuePreemptions(ctx context.Context, targets []*workload.In
 			errCh.SendErrorWithCancel(err, cancel)
 			return
 		}
+
+		err = workload.UpdateStatus(ctx, p.client, target.Obj, kueue.WorkloadEvicted, metav1.ConditionTrue, kueue.WorkloadEvictedByPreemption, "Preempted to accommodate a higher priority Workload", "evict")
```

  It will , in the next PR #692

- **pkg/workload/workload.go:301** by [@trasc](https://github.com/trasc) — 2023-04-13T07:26:11Z
```diff
@@ -294,3 +294,16 @@ func AdmissionPatch(w *kueue.Workload) *kueue.Workload {
 	wlCopy.Status.Admission = w.Status.Admission.DeepCopy()
 	return wlCopy
 }
+
+// GetSchedulingTimestamp return the timestamp to be used by the scheduler. It could
+// be the workload creation time or the last time a PodsReady timeout has occurred.
+func GetSchedulingTimestamp(w *kueue.Workload) *metav1.Time {
+	evictedConditionPosition := FindConditionIndex(&w.Status, kueue.WorkloadEvicted)
```

  done

- **pkg/workload/workload_test.go:231** by [@trasc](https://github.com/trasc) — 2023-04-13T07:31:28Z
```diff
@@ -310,3 +311,51 @@ func containersForRequests(requests ...map[corev1.ResourceName]string) []corev1.
 	}
 	return containers
 }
+
+func TestSchedulingTimestamp(t *testing.T) {
+	creationTime := metav1.Now()
+	conditionTime := metav1.NewTime(time.Now().Add(time.Hour))
+	cases := map[string]struct {
+		wl   *kueue.Workload
+		want metav1.Time
+	}{
+		"no condition": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Obj(),
+			want: creationTime,
+		},
+		"evicted by preemption": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Condition(metav1.Condition{
+					Type:               kueue.WorkloadEvicted,
+					Status:             metav1.ConditionTrue,
+					LastTransitionTime: conditionTime,
+					Reason:             kueue.WorkloadEvictedByPreemption,
+				}).
+				Obj(),
+			want: creationTime,
+		},
+		"evicted by PodsReady timeout": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Condition(metav1.Condition{
+					Type:               kueue.WorkloadEvicted,
+					Status:             metav1.ConditionTrue,
```

  In my opinion, the status should not matter,  indeed the condition status is more relevant for preemption , but just because the eviction process is finished should not mean that the workload should go back at the head of the queue.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-14T19:22:42Z

- **pkg/controller/core/workload_controller.go:131** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-14T19:22:41Z
```diff
@@ -127,6 +127,21 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling Workload")
 
+	if eviction := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); eviction != nil && eviction.Status == metav1.ConditionTrue &&
+		eviction.Reason == kueue.WorkloadEvictedByPodsReadyTimeout {
```

  I don't think we should make a difference on how Preemption and Eviction is treated here. Both modes are not immediate and potentially have to the job to be inactive.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-14T19:23:31Z

- **pkg/queue/cluster_queue_impl.go:96** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-14T19:23:30Z
```diff
@@ -90,7 +91,9 @@ func (c *clusterQueueBase) PushOrUpdate(wInfo *workload.Info) {
 	if oldInfo != nil {
 		// update in place if the workload was inadmissible and didn't change
 		// to potentially become admissible.
-		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) {
+		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) &&
+			equality.Semantic.DeepEqual(apimeta.FindStatusCondition(oldInfo.Obj.Status.Conditions, kueue.WorkloadEvicted),
```

  ack. Please add it to the comment above.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-14T19:24:42Z

- **pkg/workload/workload_test.go:231** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-14T19:24:42Z
```diff
@@ -310,3 +311,51 @@ func containersForRequests(requests ...map[corev1.ResourceName]string) []corev1.
 	}
 	return containers
 }
+
+func TestSchedulingTimestamp(t *testing.T) {
+	creationTime := metav1.Now()
+	conditionTime := metav1.NewTime(time.Now().Add(time.Hour))
+	cases := map[string]struct {
+		wl   *kueue.Workload
+		want metav1.Time
+	}{
+		"no condition": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Obj(),
+			want: creationTime,
+		},
+		"evicted by preemption": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Condition(metav1.Condition{
+					Type:               kueue.WorkloadEvicted,
+					Status:             metav1.ConditionTrue,
+					LastTransitionTime: conditionTime,
+					Reason:             kueue.WorkloadEvictedByPreemption,
+				}).
+				Obj(),
+			want: creationTime,
+		},
+		"evicted by PodsReady timeout": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Condition(metav1.Condition{
+					Type:               kueue.WorkloadEvicted,
+					Status:             metav1.ConditionTrue,
```

  That's why I think we shouldn't change the Evicted condition back to False until the job is re-admitted again.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-14T19:26:03Z

- **pkg/scheduler/preemption/preemption.go:145** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-14T19:26:03Z
```diff
@@ -141,6 +141,13 @@ func (p *Preemptor) issuePreemptions(ctx context.Context, targets []*workload.In
 			errCh.SendErrorWithCancel(err, cancel)
 			return
 		}
+
+		err = workload.UpdateStatus(ctx, p.client, target.Obj, kueue.WorkloadEvicted, metav1.ConditionTrue, kueue.WorkloadEvictedByPreemption, "Preempted to accommodate a higher priority Workload", "evict")
```

  Let me know once #692 is ready for review. I think I want to see the whole picture before merging this PR.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-19T11:47:06Z

- **pkg/workload/workload.go:304** by [@trasc](https://github.com/trasc) — 2023-04-19T11:47:05Z
```diff
@@ -294,3 +294,16 @@ func AdmissionPatch(w *kueue.Workload) *kueue.Workload {
 	wlCopy.Status.Admission = w.Status.Admission.DeepCopy()
 	return wlCopy
 }
+
+// GetSchedulingTimestamp return the timestamp to be used by the scheduler. It could
+// be the workload creation time or the last time a PodsReady timeout has occurred.
+func GetSchedulingTimestamp(w *kueue.Workload) *metav1.Time {
+	evictedConditionPosition := FindConditionIndex(&w.Status, kueue.WorkloadEvicted)
+	if evictedConditionPosition != -1 {
+		evictedCondition := w.Status.Conditions[evictedConditionPosition]
+		if evictedCondition.Reason == kueue.WorkloadEvictedByPodsReadyTimeout {
```

  ok by issue comments

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-19T12:31:59Z

- **pkg/queue/cluster_queue_impl.go:96** by [@trasc](https://github.com/trasc) — 2023-04-19T11:56:44Z
```diff
@@ -90,7 +91,9 @@ func (c *clusterQueueBase) PushOrUpdate(wInfo *workload.Info) {
 	if oldInfo != nil {
 		// update in place if the workload was inadmissible and didn't change
 		// to potentially become admissible.
-		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) {
+		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) &&
+			equality.Semantic.DeepEqual(apimeta.FindStatusCondition(oldInfo.Obj.Status.Conditions, kueue.WorkloadEvicted),
```

  done

- **pkg/workload/workload_test.go:231** by [@trasc](https://github.com/trasc) — 2023-04-19T12:23:12Z
```diff
@@ -310,3 +311,51 @@ func containersForRequests(requests ...map[corev1.ResourceName]string) []corev1.
 	}
 	return containers
 }
+
+func TestSchedulingTimestamp(t *testing.T) {
+	creationTime := metav1.Now()
+	conditionTime := metav1.NewTime(time.Now().Add(time.Hour))
+	cases := map[string]struct {
+		wl   *kueue.Workload
+		want metav1.Time
+	}{
+		"no condition": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Obj(),
+			want: creationTime,
+		},
+		"evicted by preemption": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Condition(metav1.Condition{
+					Type:               kueue.WorkloadEvicted,
+					Status:             metav1.ConditionTrue,
+					LastTransitionTime: conditionTime,
+					Reason:             kueue.WorkloadEvictedByPreemption,
+				}).
+				Obj(),
+			want: creationTime,
+		},
+		"evicted by PodsReady timeout": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Condition(metav1.Condition{
+					Type:               kueue.WorkloadEvicted,
+					Status:             metav1.ConditionTrue,
```

  I'd prefer to keep it as it is, doing it at the time of admission, will either require to do two SSA updates or make the SSA status updates more complicated (having the admission field manager manage the eviction condition as well).

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-20T06:15:51Z

- **pkg/scheduler/preemption/preemption.go:145** by [@trasc](https://github.com/trasc) — 2023-04-20T06:15:51Z
```diff
@@ -141,6 +141,13 @@ func (p *Preemptor) issuePreemptions(ctx context.Context, targets []*workload.In
 			errCh.SendErrorWithCancel(err, cancel)
 			return
 		}
+
+		err = workload.UpdateStatus(ctx, p.client, target.Obj, kueue.WorkloadEvicted, metav1.ConditionTrue, kueue.WorkloadEvictedByPreemption, "Preempted to accommodate a higher priority Workload", "evict")
```

  I'm keeping #692 in draft jut to wait for this to be merged, you can have a look at any point.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-20T15:43:27Z

- **pkg/workload/workload_test.go:231** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-20T15:43:27Z
```diff
@@ -310,3 +311,51 @@ func containersForRequests(requests ...map[corev1.ResourceName]string) []corev1.
 	}
 	return containers
 }
+
+func TestSchedulingTimestamp(t *testing.T) {
+	creationTime := metav1.Now()
+	conditionTime := metav1.NewTime(time.Now().Add(time.Hour))
+	cases := map[string]struct {
+		wl   *kueue.Workload
+		want metav1.Time
+	}{
+		"no condition": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Obj(),
+			want: creationTime,
+		},
+		"evicted by preemption": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Condition(metav1.Condition{
+					Type:               kueue.WorkloadEvicted,
+					Status:             metav1.ConditionTrue,
+					LastTransitionTime: conditionTime,
+					Reason:             kueue.WorkloadEvictedByPreemption,
+				}).
+				Obj(),
+			want: creationTime,
+		},
+		"evicted by PodsReady timeout": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Condition(metav1.Condition{
+					Type:               kueue.WorkloadEvicted,
+					Status:             metav1.ConditionTrue,
```

  But this goes against the guidance of API conditions https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md#typical-status-properties
  
  Ideally False and "condition doesn't exist" should behave the same. We shouldn't use "Unknown" for this condition because we actually know whether there was an eviction.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-20T15:44:22Z

- **pkg/workload/workload_test.go:231** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-20T15:44:22Z
```diff
@@ -310,3 +311,51 @@ func containersForRequests(requests ...map[corev1.ResourceName]string) []corev1.
 	}
 	return containers
 }
+
+func TestSchedulingTimestamp(t *testing.T) {
+	creationTime := metav1.Now()
+	conditionTime := metav1.NewTime(time.Now().Add(time.Hour))
+	cases := map[string]struct {
+		wl   *kueue.Workload
+		want metav1.Time
+	}{
+		"no condition": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Obj(),
+			want: creationTime,
+		},
+		"evicted by preemption": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Condition(metav1.Condition{
+					Type:               kueue.WorkloadEvicted,
+					Status:             metav1.ConditionTrue,
+					LastTransitionTime: conditionTime,
+					Reason:             kueue.WorkloadEvictedByPreemption,
+				}).
+				Obj(),
+			want: creationTime,
+		},
+		"evicted by PodsReady timeout": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Condition(metav1.Condition{
+					Type:               kueue.WorkloadEvicted,
+					Status:             metav1.ConditionTrue,
```

  IMO, good API practices and performance comes before the complexity of the management.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-21T09:48:08Z

- **pkg/workload/workload_test.go:231** by [@trasc](https://github.com/trasc) — 2023-04-21T09:48:08Z
```diff
@@ -310,3 +311,51 @@ func containersForRequests(requests ...map[corev1.ResourceName]string) []corev1.
 	}
 	return containers
 }
+
+func TestSchedulingTimestamp(t *testing.T) {
+	creationTime := metav1.Now()
+	conditionTime := metav1.NewTime(time.Now().Add(time.Hour))
+	cases := map[string]struct {
+		wl   *kueue.Workload
+		want metav1.Time
+	}{
+		"no condition": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Obj(),
+			want: creationTime,
+		},
+		"evicted by preemption": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Condition(metav1.Condition{
+					Type:               kueue.WorkloadEvicted,
+					Status:             metav1.ConditionTrue,
+					LastTransitionTime: conditionTime,
+					Reason:             kueue.WorkloadEvictedByPreemption,
+				}).
+				Obj(),
+			want: creationTime,
+		},
+		"evicted by PodsReady timeout": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Condition(metav1.Condition{
+					Type:               kueue.WorkloadEvicted,
+					Status:             metav1.ConditionTrue,
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-04-24T09:04:22Z

- **pkg/queue/cluster_queue_strict_fifo.go:46** by [@mimowo](https://github.com/mimowo) — 2023-04-24T08:20:49Z
```diff
@@ -40,10 +40,10 @@ func newClusterQueueStrictFIFO(cq *kueue.ClusterQueue) (ClusterQueue, error) {
 	return cqStrict, err
 }
 
-// byCreationTime is the function used by the clusterQueue heap algorithm to sort
+// byCreationOrEvictionTime is the function used by the clusterQueue heap algorithm to sort
 // workloads. It sorts workloads based on their priority.
 // When priorities are equal, it uses workloads.creationTimestamp.
-func byCreationTime(a, b interface{}) bool {
+func byCreationOrEvictionTime(a, b interface{}) bool {
```

  Should we rename it to something more generic, such as `queueOrderFunction`? 
  
  Embedding all used properties looks awkward, and even currently the name does not include priority.

- **pkg/workload/workload.go:300** by [@mimowo](https://github.com/mimowo) — 2023-04-24T08:31:57Z
```diff
@@ -236,10 +243,73 @@ func BaseSSAWorkload(w *kueue.Workload) *kueue.Workload {
 	return wlCopy
 }
 
-// AdmissionPatch creates a new object based on the input workload that
-// contains the admission. The object can be used in Server-Side-Apply.
-func AdmissionPatch(w *kueue.Workload) *kueue.Workload {
+// SetAdmission applies the provided admission to the workload.
+// The WorkloadAdmitted and WorkloadEvicted are added or updated if necessary.
+func SetAdmission(w *kueue.Workload, admission *kueue.Admission) {
+	w.Status.Admission = admission
+	admittedCond := metav1.Condition{
+		Type:               kueue.WorkloadAdmitted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             "Admitted",
+		Message:            fmt.Sprintf("Admitted by ClusterQueue %s", w.Status.Admission.ClusterQueue),
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, admittedCond)
+
+	//reset Evicted condition if present.
+	if evictedCond := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadEvicted); evictedCond != nil {
+		evictedCond.Status = metav1.ConditionFalse
+		evictedCond.LastTransitionTime = metav1.Now()
+	}
+}
+
+func SetEvictedCondition(w *kueue.Workload, reason string, message string) {
+	condition := metav1.Condition{
+		Type:               kueue.WorkloadEvicted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             reason,
+		Message:            message,
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, condition)
+}
+
+// admissionPatch creates a new object based on the input workload that contains
+// the admission and related conditions. The object can be used in Server-Side-Apply.
+func admissionPatch(w *kueue.Workload) *kueue.Workload {
 	wlCopy := BaseSSAWorkload(w)
+
 	wlCopy.Status.Admission = w.Status.Admission.DeepCopy()
+	if existing := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadAdmitted); existing != nil {
+		wlCopy.Status.Conditions = append(wlCopy.Status.Conditions, *existing.DeepCopy())
+	}
+	if existing := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadEvicted); existing != nil {
+		wlCopy.Status.Conditions = append(wlCopy.Status.Conditions, *existing.DeepCopy())
+	}
 	return wlCopy
 }
+
+// PatchAdmission updated all the admission related status fields of a workload with SSA.
+func PatchAdmission(ctx context.Context, c client.Client, w *kueue.Workload) error {
+	patch := admissionPatch(w)
+	return c.Status().Patch(ctx, patch, client.Apply, client.FieldOwner(constants.AdmissionName))
+}
+
+// GetEvictedCondition returns the Evicted condition if present and a bool
+// indication if the eviction is ongoing
+func GetEvictidCondition(w *kueue.Workload) (*metav1.Condition, bool) {
```

  Two suggestions:
  1. do not to return the isOngoing from this function. Returning the flag along with the condition suggests it is some additional information, but in fact can be computed from the condition pointer.
  2. rename to `GetEvictedCondition`, it looks like a typo currently

- **test/integration/scheduler/podsready/scheduler_test.go:309** by [@mimowo](https://github.com/mimowo) — 2023-04-24T08:40:09Z
```diff
@@ -301,4 +302,85 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 		})
 	})
 
+	var _ = ginkgo.Context("One second PodsReady timeout", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 5 * time.Second
+		})
+
+		ginkgo.It("Should move the evicted workload at the end of the queue", func() {
+			localQueueName := "eviction-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time,
+			// letting them all to time out, we should see a circular buffer admission pattern
+			workloads := []*kueue.Workload{
```

  This test seems overly complicated. Is there anything that we cover with 4 workloads that we wouldn't just with 2? I think using 2 workloads would make it both faster and more readable.

- **test/integration/scheduler/podsready/scheduler_test.go:305** by [@mimowo](https://github.com/mimowo) — 2023-04-24T08:47:47Z
```diff
@@ -301,4 +302,85 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 		})
 	})
 
+	var _ = ginkgo.Context("One second PodsReady timeout", func() {
```

  It seems that the context name does not agree with implementation which uses 5s timeout.
  
  To me the test conceptually belongs under the "Short PodsReady timeout" context. In this file we have two contexts currently:
  1. "Long PodsReady timeout" (1min) - tests under this context don't hit the timeout
  2. "Short PodsReady timeout" (3s) - tests under this context hit the timeout
  
  So I think the new test conceptually belongs to (2.) and there is no need for a new context.
  Is the timeout of 3s too short and the test is flaky, but not under 5s? I guess we can increase the timeout then to 5s, but maybe we could simplify the test (as suggested by keeping only 2 workloads), so that it passes with 3s.

- **pkg/workload/workload_test.go:231** by [@mimowo](https://github.com/mimowo) — 2023-04-24T08:49:53Z
```diff
@@ -310,3 +311,51 @@ func containersForRequests(requests ...map[corev1.ResourceName]string) []corev1.
 	}
 	return containers
 }
+
+func TestSchedulingTimestamp(t *testing.T) {
+	creationTime := metav1.Now()
+	conditionTime := metav1.NewTime(time.Now().Add(time.Hour))
+	cases := map[string]struct {
+		wl   *kueue.Workload
+		want metav1.Time
+	}{
+		"no condition": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Obj(),
+			want: creationTime,
+		},
+		"evicted by preemption": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Condition(metav1.Condition{
+					Type:               kueue.WorkloadEvicted,
+					Status:             metav1.ConditionTrue,
+					LastTransitionTime: conditionTime,
+					Reason:             kueue.WorkloadEvictedByPreemption,
+				}).
+				Obj(),
+			want: creationTime,
+		},
+		"evicted by PodsReady timeout": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Condition(metav1.Condition{
+					Type:               kueue.WorkloadEvicted,
+					Status:             metav1.ConditionTrue,
```

  I cannot find the requested test case for condition=False.

- **pkg/workload/workload.go:310** by [@mimowo](https://github.com/mimowo) — 2023-04-24T08:56:02Z
```diff
@@ -236,10 +243,73 @@ func BaseSSAWorkload(w *kueue.Workload) *kueue.Workload {
 	return wlCopy
 }
 
-// AdmissionPatch creates a new object based on the input workload that
-// contains the admission. The object can be used in Server-Side-Apply.
-func AdmissionPatch(w *kueue.Workload) *kueue.Workload {
+// SetAdmission applies the provided admission to the workload.
+// The WorkloadAdmitted and WorkloadEvicted are added or updated if necessary.
+func SetAdmission(w *kueue.Workload, admission *kueue.Admission) {
+	w.Status.Admission = admission
+	admittedCond := metav1.Condition{
+		Type:               kueue.WorkloadAdmitted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             "Admitted",
+		Message:            fmt.Sprintf("Admitted by ClusterQueue %s", w.Status.Admission.ClusterQueue),
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, admittedCond)
+
+	//reset Evicted condition if present.
+	if evictedCond := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadEvicted); evictedCond != nil {
+		evictedCond.Status = metav1.ConditionFalse
+		evictedCond.LastTransitionTime = metav1.Now()
+	}
+}
+
+func SetEvictedCondition(w *kueue.Workload, reason string, message string) {
+	condition := metav1.Condition{
+		Type:               kueue.WorkloadEvicted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             reason,
+		Message:            message,
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, condition)
+}
+
+// admissionPatch creates a new object based on the input workload that contains
+// the admission and related conditions. The object can be used in Server-Side-Apply.
+func admissionPatch(w *kueue.Workload) *kueue.Workload {
 	wlCopy := BaseSSAWorkload(w)
+
 	wlCopy.Status.Admission = w.Status.Admission.DeepCopy()
+	if existing := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadAdmitted); existing != nil {
+		wlCopy.Status.Conditions = append(wlCopy.Status.Conditions, *existing.DeepCopy())
+	}
+	if existing := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadEvicted); existing != nil {
+		wlCopy.Status.Conditions = append(wlCopy.Status.Conditions, *existing.DeepCopy())
+	}
 	return wlCopy
 }
+
+// PatchAdmission updated all the admission related status fields of a workload with SSA.
+func PatchAdmission(ctx context.Context, c client.Client, w *kueue.Workload) error {
+	patch := admissionPatch(w)
+	return c.Status().Patch(ctx, patch, client.Apply, client.FieldOwner(constants.AdmissionName))
+}
+
+// GetEvictedCondition returns the Evicted condition if present and a bool
+// indication if the eviction is ongoing
+func GetEvictidCondition(w *kueue.Workload) (*metav1.Condition, bool) {
+	cond := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadEvicted)
+	if cond == nil || cond.Status != metav1.ConditionTrue {
+		return cond, false
+	}
+	return cond, true
+}
+
+// GetSchedulingTimestamp return the timestamp to be used by the scheduler. It could
+// be the workload creation time or the last time a PodsReady timeout has occurred.
+func GetSchedulingTimestamp(w *kueue.Workload) *metav1.Time {
```

  I think the `GetSchedulingTimestamp` suggests the time of the workload being scheduled (admitted). I would suggest `GetQueueOrderTimestamp`, or `GetQueuedTimestamp`.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-24T10:38:47Z

- **pkg/queue/cluster_queue_strict_fifo.go:46** by [@trasc](https://github.com/trasc) — 2023-04-24T09:22:29Z
```diff
@@ -40,10 +40,10 @@ func newClusterQueueStrictFIFO(cq *kueue.ClusterQueue) (ClusterQueue, error) {
 	return cqStrict, err
 }
 
-// byCreationTime is the function used by the clusterQueue heap algorithm to sort
+// byCreationOrEvictionTime is the function used by the clusterQueue heap algorithm to sort
 // workloads. It sorts workloads based on their priority.
 // When priorities are equal, it uses workloads.creationTimestamp.
-func byCreationTime(a, b interface{}) bool {
+func byCreationOrEvictionTime(a, b interface{}) bool {
```

  done

- **pkg/workload/workload.go:310** by [@trasc](https://github.com/trasc) — 2023-04-24T09:24:02Z
```diff
@@ -236,10 +243,73 @@ func BaseSSAWorkload(w *kueue.Workload) *kueue.Workload {
 	return wlCopy
 }
 
-// AdmissionPatch creates a new object based on the input workload that
-// contains the admission. The object can be used in Server-Side-Apply.
-func AdmissionPatch(w *kueue.Workload) *kueue.Workload {
+// SetAdmission applies the provided admission to the workload.
+// The WorkloadAdmitted and WorkloadEvicted are added or updated if necessary.
+func SetAdmission(w *kueue.Workload, admission *kueue.Admission) {
+	w.Status.Admission = admission
+	admittedCond := metav1.Condition{
+		Type:               kueue.WorkloadAdmitted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             "Admitted",
+		Message:            fmt.Sprintf("Admitted by ClusterQueue %s", w.Status.Admission.ClusterQueue),
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, admittedCond)
+
+	//reset Evicted condition if present.
+	if evictedCond := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadEvicted); evictedCond != nil {
+		evictedCond.Status = metav1.ConditionFalse
+		evictedCond.LastTransitionTime = metav1.Now()
+	}
+}
+
+func SetEvictedCondition(w *kueue.Workload, reason string, message string) {
+	condition := metav1.Condition{
+		Type:               kueue.WorkloadEvicted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             reason,
+		Message:            message,
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, condition)
+}
+
+// admissionPatch creates a new object based on the input workload that contains
+// the admission and related conditions. The object can be used in Server-Side-Apply.
+func admissionPatch(w *kueue.Workload) *kueue.Workload {
 	wlCopy := BaseSSAWorkload(w)
+
 	wlCopy.Status.Admission = w.Status.Admission.DeepCopy()
+	if existing := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadAdmitted); existing != nil {
+		wlCopy.Status.Conditions = append(wlCopy.Status.Conditions, *existing.DeepCopy())
+	}
+	if existing := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadEvicted); existing != nil {
+		wlCopy.Status.Conditions = append(wlCopy.Status.Conditions, *existing.DeepCopy())
+	}
 	return wlCopy
 }
+
+// PatchAdmission updated all the admission related status fields of a workload with SSA.
+func PatchAdmission(ctx context.Context, c client.Client, w *kueue.Workload) error {
+	patch := admissionPatch(w)
+	return c.Status().Patch(ctx, patch, client.Apply, client.FieldOwner(constants.AdmissionName))
+}
+
+// GetEvictedCondition returns the Evicted condition if present and a bool
+// indication if the eviction is ongoing
+func GetEvictidCondition(w *kueue.Workload) (*metav1.Condition, bool) {
+	cond := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadEvicted)
+	if cond == nil || cond.Status != metav1.ConditionTrue {
+		return cond, false
+	}
+	return cond, true
+}
+
+// GetSchedulingTimestamp return the timestamp to be used by the scheduler. It could
+// be the workload creation time or the last time a PodsReady timeout has occurred.
+func GetSchedulingTimestamp(w *kueue.Workload) *metav1.Time {
```

  done

- **pkg/workload/workload.go:300** by [@trasc](https://github.com/trasc) — 2023-04-24T09:33:05Z
```diff
@@ -236,10 +243,73 @@ func BaseSSAWorkload(w *kueue.Workload) *kueue.Workload {
 	return wlCopy
 }
 
-// AdmissionPatch creates a new object based on the input workload that
-// contains the admission. The object can be used in Server-Side-Apply.
-func AdmissionPatch(w *kueue.Workload) *kueue.Workload {
+// SetAdmission applies the provided admission to the workload.
+// The WorkloadAdmitted and WorkloadEvicted are added or updated if necessary.
+func SetAdmission(w *kueue.Workload, admission *kueue.Admission) {
+	w.Status.Admission = admission
+	admittedCond := metav1.Condition{
+		Type:               kueue.WorkloadAdmitted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             "Admitted",
+		Message:            fmt.Sprintf("Admitted by ClusterQueue %s", w.Status.Admission.ClusterQueue),
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, admittedCond)
+
+	//reset Evicted condition if present.
+	if evictedCond := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadEvicted); evictedCond != nil {
+		evictedCond.Status = metav1.ConditionFalse
+		evictedCond.LastTransitionTime = metav1.Now()
+	}
+}
+
+func SetEvictedCondition(w *kueue.Workload, reason string, message string) {
+	condition := metav1.Condition{
+		Type:               kueue.WorkloadEvicted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             reason,
+		Message:            message,
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, condition)
+}
+
+// admissionPatch creates a new object based on the input workload that contains
+// the admission and related conditions. The object can be used in Server-Side-Apply.
+func admissionPatch(w *kueue.Workload) *kueue.Workload {
 	wlCopy := BaseSSAWorkload(w)
+
 	wlCopy.Status.Admission = w.Status.Admission.DeepCopy()
+	if existing := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadAdmitted); existing != nil {
+		wlCopy.Status.Conditions = append(wlCopy.Status.Conditions, *existing.DeepCopy())
+	}
+	if existing := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadEvicted); existing != nil {
+		wlCopy.Status.Conditions = append(wlCopy.Status.Conditions, *existing.DeepCopy())
+	}
 	return wlCopy
 }
+
+// PatchAdmission updated all the admission related status fields of a workload with SSA.
+func PatchAdmission(ctx context.Context, c client.Client, w *kueue.Workload) error {
+	patch := admissionPatch(w)
+	return c.Status().Patch(ctx, patch, client.Apply, client.FieldOwner(constants.AdmissionName))
+}
+
+// GetEvictedCondition returns the Evicted condition if present and a bool
+// indication if the eviction is ongoing
+func GetEvictidCondition(w *kueue.Workload) (*metav1.Condition, bool) {
```

  removed

- **test/integration/scheduler/podsready/scheduler_test.go:305** by [@trasc](https://github.com/trasc) — 2023-04-24T10:12:31Z
```diff
@@ -301,4 +302,85 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 		})
 	})
 
+	var _ = ginkgo.Context("One second PodsReady timeout", func() {
```

  moved to 2, (I  needed 5 sec just in a earlier version of the test, before created the dedicated local queue)

- **test/integration/scheduler/podsready/scheduler_test.go:309** by [@trasc](https://github.com/trasc) — 2023-04-24T10:14:20Z
```diff
@@ -301,4 +302,85 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 		})
 	})
 
+	var _ = ginkgo.Context("One second PodsReady timeout", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 5 * time.Second
+		})
+
+		ginkgo.It("Should move the evicted workload at the end of the queue", func() {
+			localQueueName := "eviction-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time,
+			// letting them all to time out, we should see a circular buffer admission pattern
+			workloads := []*kueue.Workload{
```

  With only two , we cannot distinguish if the order is given by the eviction time or by the workload being added to the inadmissible queue, switch ch to 3.

- **pkg/workload/workload_test.go:231** by [@trasc](https://github.com/trasc) — 2023-04-24T10:32:01Z
```diff
@@ -310,3 +311,51 @@ func containersForRequests(requests ...map[corev1.ResourceName]string) []corev1.
 	}
 	return containers
 }
+
+func TestSchedulingTimestamp(t *testing.T) {
+	creationTime := metav1.Now()
+	conditionTime := metav1.NewTime(time.Now().Add(time.Hour))
+	cases := map[string]struct {
+		wl   *kueue.Workload
+		want metav1.Time
+	}{
+		"no condition": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Obj(),
+			want: creationTime,
+		},
+		"evicted by preemption": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Condition(metav1.Condition{
+					Type:               kueue.WorkloadEvicted,
+					Status:             metav1.ConditionTrue,
+					LastTransitionTime: conditionTime,
+					Reason:             kueue.WorkloadEvictedByPreemption,
+				}).
+				Obj(),
+			want: creationTime,
+		},
+		"evicted by PodsReady timeout": {
+			wl: utiltesting.MakeWorkload("name", "ns").
+				Creation(creationTime.Time).
+				Condition(metav1.Condition{
+					Type:               kueue.WorkloadEvicted,
+					Status:             metav1.ConditionTrue,
```

  added

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-04-24T10:51:28Z

- **test/integration/scheduler/podsready/scheduler_test.go:355** by [@mimowo](https://github.com/mimowo) — 2023-04-24T10:50:42Z
```diff
@@ -299,6 +300,79 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			ginkgo.By("delete the waiting 'prod' workload so that it does not get admitted during teardown")
 			gomega.Expect(k8sClient.Delete(ctx, prodWl)).Should(gomega.Succeed())
 		})
-	})
 
+		ginkgo.It("Should move the evicted workload at the end of the queue", func() {
+			localQueueName := "eviction-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time,
+			// letting them all to time out, we should see a circular buffer admission pattern
+			workloads := []*kueue.Workload{
+				testing.MakeWorkload("prod1", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj(),
+				testing.MakeWorkload("prod2", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj(),
+				testing.MakeWorkload("prod3", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj(),
+			}
+
+			ginkgo.By("create the workloads", func() {
+				tkr := time.NewTicker(time.Second)
+				for _, wl := range workloads {
+					ginkgo.By(fmt.Sprintf("create the '%s' workload", wl.Name), func() {
+						gomega.Expect(k8sClient.Create(ctx, wl)).Should(gomega.Succeed())
+						gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).Should(gomega.Succeed())
+						ginkgo.GinkgoLogr.Info(wl.Name+" created", "at", wl.CreationTimestamp)
+						// since metav1.Time has only second resolution, wait one second to avoid
+						// any potential creation time collision
+						<-tkr.C
+					})
+				}
+			})
+
+			ginkgo.By("create the local queue to start admission", func() {
+				lq := testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, lq)).Should(gomega.Succeed())
+			})
+
+			getAdmittedWorkloads := func() []int {
+				ret := make([]int, 0, len(workloads))
+				for i, wl := range workloads {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).Should(gomega.Succeed())
+					if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadAdmitted) {
+						ret = append(ret, i)
+					}
+				}
+				return ret
+			}
+
+			ginkgo.By("check admission order", func() {
+				prev := -1
+				gomega.Eventually(func() bool {
+					admitted := getAdmittedWorkloads()
+					if len(admitted) > 0 {
+						prev = admitted[0]
+					}
+					return prev != -1
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+				for i := 0; i < 2*len(workloads); i++ {
```

  Why do we need this many iterations? I would suggest to unfold the loop and explicitely say in the comments what transition is expected.  I think we should strive to make the test fast, while maintaining the coverage, but it isn't clear how the looped steps increase coverage.
  
  What I would suggest is to make use 3 workloads, the first one is timed out and moves at the end. Then, workloads 2 and 3 complete successfully. Then, workload 1 after requeing completes successfully too. It is important the workload can succeed after requeue, so to show that the timeout is computed from the most recent admission, and not from creation.

- **test/integration/scheduler/podsready/scheduler_test.go:309** by [@mimowo](https://github.com/mimowo) — 2023-04-24T10:50:51Z
```diff
@@ -301,4 +302,85 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 		})
 	})
 
+	var _ = ginkgo.Context("One second PodsReady timeout", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 5 * time.Second
+		})
+
+		ginkgo.It("Should move the evicted workload at the end of the queue", func() {
+			localQueueName := "eviction-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time,
+			// letting them all to time out, we should see a circular buffer admission pattern
+			workloads := []*kueue.Workload{
```

  sg

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-24T10:55:44Z

- **test/integration/scheduler/podsready/scheduler_test.go:355** by [@trasc](https://github.com/trasc) — 2023-04-24T10:55:44Z
```diff
@@ -299,6 +300,79 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			ginkgo.By("delete the waiting 'prod' workload so that it does not get admitted during teardown")
 			gomega.Expect(k8sClient.Delete(ctx, prodWl)).Should(gomega.Succeed())
 		})
-	})
 
+		ginkgo.It("Should move the evicted workload at the end of the queue", func() {
+			localQueueName := "eviction-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time,
+			// letting them all to time out, we should see a circular buffer admission pattern
+			workloads := []*kueue.Workload{
+				testing.MakeWorkload("prod1", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj(),
+				testing.MakeWorkload("prod2", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj(),
+				testing.MakeWorkload("prod3", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj(),
+			}
+
+			ginkgo.By("create the workloads", func() {
+				tkr := time.NewTicker(time.Second)
+				for _, wl := range workloads {
+					ginkgo.By(fmt.Sprintf("create the '%s' workload", wl.Name), func() {
+						gomega.Expect(k8sClient.Create(ctx, wl)).Should(gomega.Succeed())
+						gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).Should(gomega.Succeed())
+						ginkgo.GinkgoLogr.Info(wl.Name+" created", "at", wl.CreationTimestamp)
+						// since metav1.Time has only second resolution, wait one second to avoid
+						// any potential creation time collision
+						<-tkr.C
+					})
+				}
+			})
+
+			ginkgo.By("create the local queue to start admission", func() {
+				lq := testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, lq)).Should(gomega.Succeed())
+			})
+
+			getAdmittedWorkloads := func() []int {
+				ret := make([]int, 0, len(workloads))
+				for i, wl := range workloads {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).Should(gomega.Succeed())
+					if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadAdmitted) {
+						ret = append(ret, i)
+					}
+				}
+				return ret
+			}
+
+			ginkgo.By("check admission order", func() {
+				prev := -1
+				gomega.Eventually(func() bool {
+					admitted := getAdmittedWorkloads()
+					if len(admitted) > 0 {
+						prev = admitted[0]
+					}
+					return prev != -1
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+				for i := 0; i < 2*len(workloads); i++ {
```

  I'd prefer to keep the test as is.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-04-24T11:41:53Z

- **test/integration/scheduler/podsready/scheduler_test.go:355** by [@mimowo](https://github.com/mimowo) — 2023-04-24T11:41:53Z
```diff
@@ -299,6 +300,79 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			ginkgo.By("delete the waiting 'prod' workload so that it does not get admitted during teardown")
 			gomega.Expect(k8sClient.Delete(ctx, prodWl)).Should(gomega.Succeed())
 		})
-	})
 
+		ginkgo.It("Should move the evicted workload at the end of the queue", func() {
+			localQueueName := "eviction-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time,
+			// letting them all to time out, we should see a circular buffer admission pattern
+			workloads := []*kueue.Workload{
+				testing.MakeWorkload("prod1", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj(),
+				testing.MakeWorkload("prod2", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj(),
+				testing.MakeWorkload("prod3", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj(),
+			}
+
+			ginkgo.By("create the workloads", func() {
+				tkr := time.NewTicker(time.Second)
+				for _, wl := range workloads {
+					ginkgo.By(fmt.Sprintf("create the '%s' workload", wl.Name), func() {
+						gomega.Expect(k8sClient.Create(ctx, wl)).Should(gomega.Succeed())
+						gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).Should(gomega.Succeed())
+						ginkgo.GinkgoLogr.Info(wl.Name+" created", "at", wl.CreationTimestamp)
+						// since metav1.Time has only second resolution, wait one second to avoid
+						// any potential creation time collision
+						<-tkr.C
+					})
+				}
+			})
+
+			ginkgo.By("create the local queue to start admission", func() {
+				lq := testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, lq)).Should(gomega.Succeed())
+			})
+
+			getAdmittedWorkloads := func() []int {
+				ret := make([]int, 0, len(workloads))
+				for i, wl := range workloads {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).Should(gomega.Succeed())
+					if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadAdmitted) {
+						ret = append(ret, i)
+					}
+				}
+				return ret
+			}
+
+			ginkgo.By("check admission order", func() {
+				prev := -1
+				gomega.Eventually(func() bool {
+					admitted := getAdmittedWorkloads()
+					if len(admitted) > 0 {
+						prev = admitted[0]
+					}
+					return prev != -1
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+				for i := 0; i < 2*len(workloads); i++ {
```

  I would be ok with that if there are good reasons. In particular one question that puzzles me is why the number of iterations is 2*len(workloads). Please elaborate in a comment so that the reasoning it is clear to the future readers too.
  
  Also, how long does the take currently take?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-24T12:02:16Z

- **test/integration/scheduler/podsready/scheduler_test.go:355** by [@trasc](https://github.com/trasc) — 2023-04-24T12:02:15Z
```diff
@@ -299,6 +300,79 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			ginkgo.By("delete the waiting 'prod' workload so that it does not get admitted during teardown")
 			gomega.Expect(k8sClient.Delete(ctx, prodWl)).Should(gomega.Succeed())
 		})
-	})
 
+		ginkgo.It("Should move the evicted workload at the end of the queue", func() {
+			localQueueName := "eviction-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time,
+			// letting them all to time out, we should see a circular buffer admission pattern
+			workloads := []*kueue.Workload{
+				testing.MakeWorkload("prod1", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj(),
+				testing.MakeWorkload("prod2", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj(),
+				testing.MakeWorkload("prod3", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj(),
+			}
+
+			ginkgo.By("create the workloads", func() {
+				tkr := time.NewTicker(time.Second)
+				for _, wl := range workloads {
+					ginkgo.By(fmt.Sprintf("create the '%s' workload", wl.Name), func() {
+						gomega.Expect(k8sClient.Create(ctx, wl)).Should(gomega.Succeed())
+						gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).Should(gomega.Succeed())
+						ginkgo.GinkgoLogr.Info(wl.Name+" created", "at", wl.CreationTimestamp)
+						// since metav1.Time has only second resolution, wait one second to avoid
+						// any potential creation time collision
+						<-tkr.C
+					})
+				}
+			})
+
+			ginkgo.By("create the local queue to start admission", func() {
+				lq := testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, lq)).Should(gomega.Succeed())
+			})
+
+			getAdmittedWorkloads := func() []int {
+				ret := make([]int, 0, len(workloads))
+				for i, wl := range workloads {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).Should(gomega.Succeed())
+					if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadAdmitted) {
+						ret = append(ret, i)
+					}
+				}
+				return ret
+			}
+
+			ginkgo.By("check admission order", func() {
+				prev := -1
+				gomega.Eventually(func() bool {
+					admitted := getAdmittedWorkloads()
+					if len(admitted) > 0 {
+						prev = admitted[0]
+					}
+					return prev != -1
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+				for i := 0; i < 2*len(workloads); i++ {
```

  2*len(...) ensures that all workloads are queued at the back of the list and be considered for scheduling later on (not lost in the process)
  
  the test takes about 3 sec to create the workloads, and another @`2*3*3sec` to check the scheduling order

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-04-24T12:56:13Z

- **test/integration/scheduler/podsready/scheduler_test.go:355** by [@mimowo](https://github.com/mimowo) — 2023-04-24T12:56:12Z
```diff
@@ -299,6 +300,79 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			ginkgo.By("delete the waiting 'prod' workload so that it does not get admitted during teardown")
 			gomega.Expect(k8sClient.Delete(ctx, prodWl)).Should(gomega.Succeed())
 		})
-	})
 
+		ginkgo.It("Should move the evicted workload at the end of the queue", func() {
+			localQueueName := "eviction-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time,
+			// letting them all to time out, we should see a circular buffer admission pattern
+			workloads := []*kueue.Workload{
+				testing.MakeWorkload("prod1", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj(),
+				testing.MakeWorkload("prod2", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj(),
+				testing.MakeWorkload("prod3", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj(),
+			}
+
+			ginkgo.By("create the workloads", func() {
+				tkr := time.NewTicker(time.Second)
+				for _, wl := range workloads {
+					ginkgo.By(fmt.Sprintf("create the '%s' workload", wl.Name), func() {
+						gomega.Expect(k8sClient.Create(ctx, wl)).Should(gomega.Succeed())
+						gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).Should(gomega.Succeed())
+						ginkgo.GinkgoLogr.Info(wl.Name+" created", "at", wl.CreationTimestamp)
+						// since metav1.Time has only second resolution, wait one second to avoid
+						// any potential creation time collision
+						<-tkr.C
+					})
+				}
+			})
+
+			ginkgo.By("create the local queue to start admission", func() {
+				lq := testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, lq)).Should(gomega.Succeed())
+			})
+
+			getAdmittedWorkloads := func() []int {
+				ret := make([]int, 0, len(workloads))
+				for i, wl := range workloads {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).Should(gomega.Succeed())
+					if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadAdmitted) {
+						ret = append(ret, i)
+					}
+				}
+				return ret
+			}
+
+			ginkgo.By("check admission order", func() {
+				prev := -1
+				gomega.Eventually(func() bool {
+					admitted := getAdmittedWorkloads()
+					if len(admitted) > 0 {
+						prev = admitted[0]
+					}
+					return prev != -1
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+				for i := 0; i < 2*len(workloads); i++ {
```

  So it is roughly `21`s total vs `6-7`s (if we followed the suggestion to wait only for the first to timeout and then complete them all successfully). 
  
  > 2*len(...) ensures that all workloads are queued at the back of the list and be considered for scheduling later on (not lost in the process)
  
  In the scheme described above, we could check that the first workload is requeued properly.
  
  Is there a code path that is explored additionally by repeating the timeout?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-24T14:38:56Z

- **test/integration/scheduler/podsready/scheduler_test.go:355** by [@trasc](https://github.com/trasc) — 2023-04-24T14:38:55Z
```diff
@@ -299,6 +300,79 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			ginkgo.By("delete the waiting 'prod' workload so that it does not get admitted during teardown")
 			gomega.Expect(k8sClient.Delete(ctx, prodWl)).Should(gomega.Succeed())
 		})
-	})
 
+		ginkgo.It("Should move the evicted workload at the end of the queue", func() {
+			localQueueName := "eviction-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time,
+			// letting them all to time out, we should see a circular buffer admission pattern
+			workloads := []*kueue.Workload{
+				testing.MakeWorkload("prod1", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj(),
+				testing.MakeWorkload("prod2", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj(),
+				testing.MakeWorkload("prod3", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj(),
+			}
+
+			ginkgo.By("create the workloads", func() {
+				tkr := time.NewTicker(time.Second)
+				for _, wl := range workloads {
+					ginkgo.By(fmt.Sprintf("create the '%s' workload", wl.Name), func() {
+						gomega.Expect(k8sClient.Create(ctx, wl)).Should(gomega.Succeed())
+						gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).Should(gomega.Succeed())
+						ginkgo.GinkgoLogr.Info(wl.Name+" created", "at", wl.CreationTimestamp)
+						// since metav1.Time has only second resolution, wait one second to avoid
+						// any potential creation time collision
+						<-tkr.C
+					})
+				}
+			})
+
+			ginkgo.By("create the local queue to start admission", func() {
+				lq := testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, lq)).Should(gomega.Succeed())
+			})
+
+			getAdmittedWorkloads := func() []int {
+				ret := make([]int, 0, len(workloads))
+				for i, wl := range workloads {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).Should(gomega.Succeed())
+					if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadAdmitted) {
+						ret = append(ret, i)
+					}
+				}
+				return ret
+			}
+
+			ginkgo.By("check admission order", func() {
+				prev := -1
+				gomega.Eventually(func() bool {
+					admitted := getAdmittedWorkloads()
+					if len(admitted) > 0 {
+						prev = admitted[0]
+					}
+					return prev != -1
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+				for i := 0; i < 2*len(workloads); i++ {
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-04-24T15:00:43Z

- **test/util/util.go:199** by [@mimowo](https://github.com/mimowo) — 2023-04-24T14:59:25Z
```diff
@@ -194,6 +194,25 @@ func ExpectWorkloadsToBeWaiting(ctx context.Context, k8sClient client.Client, wl
 	}, Timeout, Interval).Should(gomega.Equal(len(wls)), "Not enough workloads are waiting")
 }
 
+func ExpectWorkloadsToBeEvicted(ctx context.Context, k8sClient client.Client, wls ...*kueue.Workload) {
+	gomega.EventuallyWithOffset(1, func() int {
+		pending := 0
```

  ```suggestion
  		evicted := 0
  ```

- **test/util/util.go:213** by [@mimowo](https://github.com/mimowo) — 2023-04-24T14:59:38Z
```diff
@@ -194,6 +194,25 @@ func ExpectWorkloadsToBeWaiting(ctx context.Context, k8sClient client.Client, wl
 	}, Timeout, Interval).Should(gomega.Equal(len(wls)), "Not enough workloads are waiting")
 }
 
+func ExpectWorkloadsToBeEvicted(ctx context.Context, k8sClient client.Client, wls ...*kueue.Workload) {
+	gomega.EventuallyWithOffset(1, func() int {
+		pending := 0
+		var updatedWorkload kueue.Workload
+		for _, wl := range wls {
+			gomega.ExpectWithOffset(1, k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWorkload)).To(gomega.Succeed())
+			idx := workload.FindConditionIndex(&updatedWorkload.Status, kueue.WorkloadAdmitted)
+			if idx == -1 {
+				continue
+			}
+			cond := updatedWorkload.Status.Conditions[idx]
+			if cond.Status == metav1.ConditionFalse && cond.Reason == "Evicted" && wl.Status.Admission == nil {
+				pending++
+			}
+		}
+		return pending
+	}, Timeout, Interval).Should(gomega.Equal(len(wls)), "Not enough workloads are waiting")
```

  ```suggestion
  	}, Timeout, Interval).Should(gomega.Equal(len(wls)), "Not enough workloads are evicted")
  ```

- **test/integration/scheduler/podsready/scheduler_test.go:346** by [@mimowo](https://github.com/mimowo) — 2023-04-24T15:00:23Z
```diff
@@ -299,6 +299,50 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			ginkgo.By("delete the waiting 'prod' workload so that it does not get admitted during teardown")
 			gomega.Expect(k8sClient.Delete(ctx, prodWl)).Should(gomega.Succeed())
 		})
-	})
 
+		ginkgo.It("Should move the evicted workload at the end of the queue", func() {
+			localQueueName := "eviction-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time,
+			// letting them all to time out, we should see a circular buffer admission pattern
+			wl1 := testing.MakeWorkload("prod1", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl2 := testing.MakeWorkload("prod2", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl3 := testing.MakeWorkload("prod3", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+
+			ginkgo.By("create the workloads", func() {
+				// since metav1.Time has only second resolution, wait one second between
+				// create calls to avoid any potential creation timestamp collision
+				gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
+				gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
+				gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("create the local queue to start admission", func() {
+				lq := testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, lq)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("waiting for the first workload to be admitted ", func() {
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, wl1)
+			})
+
+			ginkgo.By("waiting the timeout, the first workload should be evicted and the second one admitted ", func() {
+				time.Sleep(podsReadyTimeout)
+				util.ExpectWorkloadsToBeEvicted(ctx, k8sClient, wl1)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, wl2)
+			})
+
+			ginkgo.By("finishing the second workload, the third one should be admitted ", func() {
+				util.FinishWorkloads(ctx, k8sClient, wl2)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, wl3)
+			})
+
+			ginkgo.By("finishing the third workload, the first one should be admitted ", func() {
+				util.FinishWorkloads(ctx, k8sClient, wl3)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, wl1)
+			})
```

  I would suggest to add one more step to verify wl1 finishes successfully, ie. it does not timeout

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-24T15:36:03Z

- **test/integration/scheduler/podsready/scheduler_test.go:346** by [@trasc](https://github.com/trasc) — 2023-04-24T15:36:02Z
```diff
@@ -299,6 +299,50 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			ginkgo.By("delete the waiting 'prod' workload so that it does not get admitted during teardown")
 			gomega.Expect(k8sClient.Delete(ctx, prodWl)).Should(gomega.Succeed())
 		})
-	})
 
+		ginkgo.It("Should move the evicted workload at the end of the queue", func() {
+			localQueueName := "eviction-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time,
+			// letting them all to time out, we should see a circular buffer admission pattern
+			wl1 := testing.MakeWorkload("prod1", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl2 := testing.MakeWorkload("prod2", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl3 := testing.MakeWorkload("prod3", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+
+			ginkgo.By("create the workloads", func() {
+				// since metav1.Time has only second resolution, wait one second between
+				// create calls to avoid any potential creation timestamp collision
+				gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
+				gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
+				gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("create the local queue to start admission", func() {
+				lq := testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, lq)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("waiting for the first workload to be admitted ", func() {
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, wl1)
+			})
+
+			ginkgo.By("waiting the timeout, the first workload should be evicted and the second one admitted ", func() {
+				time.Sleep(podsReadyTimeout)
+				util.ExpectWorkloadsToBeEvicted(ctx, k8sClient, wl1)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, wl2)
+			})
+
+			ginkgo.By("finishing the second workload, the third one should be admitted ", func() {
+				util.FinishWorkloads(ctx, k8sClient, wl2)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, wl3)
+			})
+
+			ginkgo.By("finishing the third workload, the first one should be admitted ", func() {
+				util.FinishWorkloads(ctx, k8sClient, wl3)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, wl1)
+			})
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-24T15:36:12Z

- **test/util/util.go:213** by [@trasc](https://github.com/trasc) — 2023-04-24T15:36:12Z
```diff
@@ -194,6 +194,25 @@ func ExpectWorkloadsToBeWaiting(ctx context.Context, k8sClient client.Client, wl
 	}, Timeout, Interval).Should(gomega.Equal(len(wls)), "Not enough workloads are waiting")
 }
 
+func ExpectWorkloadsToBeEvicted(ctx context.Context, k8sClient client.Client, wls ...*kueue.Workload) {
+	gomega.EventuallyWithOffset(1, func() int {
+		pending := 0
+		var updatedWorkload kueue.Workload
+		for _, wl := range wls {
+			gomega.ExpectWithOffset(1, k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWorkload)).To(gomega.Succeed())
+			idx := workload.FindConditionIndex(&updatedWorkload.Status, kueue.WorkloadAdmitted)
+			if idx == -1 {
+				continue
+			}
+			cond := updatedWorkload.Status.Conditions[idx]
+			if cond.Status == metav1.ConditionFalse && cond.Reason == "Evicted" && wl.Status.Admission == nil {
+				pending++
+			}
+		}
+		return pending
+	}, Timeout, Interval).Should(gomega.Equal(len(wls)), "Not enough workloads are waiting")
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-24T15:36:26Z

- **test/util/util.go:199** by [@trasc](https://github.com/trasc) — 2023-04-24T15:36:26Z
```diff
@@ -194,6 +194,25 @@ func ExpectWorkloadsToBeWaiting(ctx context.Context, k8sClient client.Client, wl
 	}, Timeout, Interval).Should(gomega.Equal(len(wls)), "Not enough workloads are waiting")
 }
 
+func ExpectWorkloadsToBeEvicted(ctx context.Context, k8sClient client.Client, wls ...*kueue.Workload) {
+	gomega.EventuallyWithOffset(1, func() int {
+		pending := 0
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-24T16:01:46Z

- **pkg/queue/cluster_queue_impl.go:93** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-24T15:41:35Z
```diff
@@ -89,8 +90,10 @@ func (c *clusterQueueBase) PushOrUpdate(wInfo *workload.Info) {
 	oldInfo := c.inadmissibleWorkloads[key]
 	if oldInfo != nil {
 		// update in place if the workload was inadmissible and didn't change
-		// to potentially become admissible.
-		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) {
+		// to potentially become admissible, unless the Eviction status changed.
```

  because it could affect the order of the workload in the queue.

- **pkg/workload/workload.go:293** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-24T15:48:42Z
```diff
@@ -236,10 +243,63 @@ func BaseSSAWorkload(w *kueue.Workload) *kueue.Workload {
 	return wlCopy
 }
 
-// AdmissionPatch creates a new object based on the input workload that
-// contains the admission. The object can be used in Server-Side-Apply.
-func AdmissionPatch(w *kueue.Workload) *kueue.Workload {
+// SetAdmission applies the provided admission to the workload.
+// The WorkloadAdmitted and WorkloadEvicted are added or updated if necessary.
+func SetAdmission(w *kueue.Workload, admission *kueue.Admission) {
+	w.Status.Admission = admission
+	admittedCond := metav1.Condition{
+		Type:               kueue.WorkloadAdmitted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             "Admitted",
+		Message:            fmt.Sprintf("Admitted by ClusterQueue %s", w.Status.Admission.ClusterQueue),
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, admittedCond)
+
+	//reset Evicted condition if present.
+	if evictedCond := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadEvicted); evictedCond != nil {
+		evictedCond.Status = metav1.ConditionFalse
+		evictedCond.LastTransitionTime = metav1.Now()
+	}
+}
+
+func SetEvictedCondition(w *kueue.Workload, reason string, message string) {
+	condition := metav1.Condition{
+		Type:               kueue.WorkloadEvicted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             reason,
+		Message:            message,
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, condition)
+}
+
+// admissionPatch creates a new object based on the input workload that contains
+// the admission and related conditions. The object can be used in Server-Side-Apply.
+func admissionPatch(w *kueue.Workload) *kueue.Workload {
 	wlCopy := BaseSSAWorkload(w)
+
 	wlCopy.Status.Admission = w.Status.Admission.DeepCopy()
+	if existing := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadAdmitted); existing != nil {
+		wlCopy.Status.Conditions = append(wlCopy.Status.Conditions, *existing.DeepCopy())
+	}
+	if existing := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadEvicted); existing != nil {
+		wlCopy.Status.Conditions = append(wlCopy.Status.Conditions, *existing.DeepCopy())
+	}
 	return wlCopy
 }
+
+// PatchAdmission updated all the admission related status fields of a workload with SSA.
+func PatchAdmission(ctx context.Context, c client.Client, w *kueue.Workload) error {
```

  ```suggestion
  func ApplyAdmission(ctx context.Context, c client.Client, w *kueue.Workload) error {
  ```
  
  `Patch` makes me think of regular Patch.

- **pkg/workload/workload.go:235** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-24T15:50:48Z
```diff
@@ -236,10 +243,63 @@ func BaseSSAWorkload(w *kueue.Workload) *kueue.Workload {
 	return wlCopy
 }
 
-// AdmissionPatch creates a new object based on the input workload that
-// contains the admission. The object can be used in Server-Side-Apply.
-func AdmissionPatch(w *kueue.Workload) *kueue.Workload {
+// SetAdmission applies the provided admission to the workload.
+// The WorkloadAdmitted and WorkloadEvicted are added or updated if necessary.
+func SetAdmission(w *kueue.Workload, admission *kueue.Admission) {
+	w.Status.Admission = admission
+	admittedCond := metav1.Condition{
+		Type:               kueue.WorkloadAdmitted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             "Admitted",
+		Message:            fmt.Sprintf("Admitted by ClusterQueue %s", w.Status.Admission.ClusterQueue),
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, admittedCond)
+
+	//reset Evicted condition if present.
```

  No need to make it conditional. You can just set it to False in all cases.

- **pkg/workload/workload.go:283** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-24T15:52:28Z
```diff
@@ -236,10 +243,63 @@ func BaseSSAWorkload(w *kueue.Workload) *kueue.Workload {
 	return wlCopy
 }
 
-// AdmissionPatch creates a new object based on the input workload that
-// contains the admission. The object can be used in Server-Side-Apply.
-func AdmissionPatch(w *kueue.Workload) *kueue.Workload {
+// SetAdmission applies the provided admission to the workload.
+// The WorkloadAdmitted and WorkloadEvicted are added or updated if necessary.
+func SetAdmission(w *kueue.Workload, admission *kueue.Admission) {
+	w.Status.Admission = admission
+	admittedCond := metav1.Condition{
+		Type:               kueue.WorkloadAdmitted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             "Admitted",
+		Message:            fmt.Sprintf("Admitted by ClusterQueue %s", w.Status.Admission.ClusterQueue),
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, admittedCond)
+
+	//reset Evicted condition if present.
+	if evictedCond := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadEvicted); evictedCond != nil {
+		evictedCond.Status = metav1.ConditionFalse
+		evictedCond.LastTransitionTime = metav1.Now()
+	}
+}
+
+func SetEvictedCondition(w *kueue.Workload, reason string, message string) {
+	condition := metav1.Condition{
+		Type:               kueue.WorkloadEvicted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             reason,
+		Message:            message,
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, condition)
+}
+
+// admissionPatch creates a new object based on the input workload that contains
+// the admission and related conditions. The object can be used in Server-Side-Apply.
+func admissionPatch(w *kueue.Workload) *kueue.Workload {
 	wlCopy := BaseSSAWorkload(w)
+
 	wlCopy.Status.Admission = w.Status.Admission.DeepCopy()
+	if existing := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadAdmitted); existing != nil {
```

  ```golang
  for _, cond := range []string(kueue.WorkloadAdmitted, kueue.WorkloadEvicted) {
  ```

- **pkg/workload/workload.go:224** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-24T15:57:43Z
```diff
@@ -236,10 +243,63 @@ func BaseSSAWorkload(w *kueue.Workload) *kueue.Workload {
 	return wlCopy
 }
 
-// AdmissionPatch creates a new object based on the input workload that
-// contains the admission. The object can be used in Server-Side-Apply.
-func AdmissionPatch(w *kueue.Workload) *kueue.Workload {
+// SetAdmission applies the provided admission to the workload.
+// The WorkloadAdmitted and WorkloadEvicted are added or updated if necessary.
+func SetAdmission(w *kueue.Workload, admission *kueue.Admission) {
```

  The function `UnsetAdmissionWithCondition` above already calls patch. This one doesn't.
  
  This could cause confusion or bugs in the future. Pick one approach and apply it to both functions.

- **pkg/workload/workload.go:242** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-24T15:58:13Z
```diff
@@ -236,10 +243,63 @@ func BaseSSAWorkload(w *kueue.Workload) *kueue.Workload {
 	return wlCopy
 }
 
-// AdmissionPatch creates a new object based on the input workload that
-// contains the admission. The object can be used in Server-Side-Apply.
-func AdmissionPatch(w *kueue.Workload) *kueue.Workload {
+// SetAdmission applies the provided admission to the workload.
+// The WorkloadAdmitted and WorkloadEvicted are added or updated if necessary.
+func SetAdmission(w *kueue.Workload, admission *kueue.Admission) {
+	w.Status.Admission = admission
+	admittedCond := metav1.Condition{
+		Type:               kueue.WorkloadAdmitted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             "Admitted",
+		Message:            fmt.Sprintf("Admitted by ClusterQueue %s", w.Status.Admission.ClusterQueue),
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, admittedCond)
+
+	//reset Evicted condition if present.
+	if evictedCond := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadEvicted); evictedCond != nil {
+		evictedCond.Status = metav1.ConditionFalse
+		evictedCond.LastTransitionTime = metav1.Now()
+	}
+}
+
+func SetEvictedCondition(w *kueue.Workload, reason string, message string) {
```

  Also this doesn't call Patch.

- **test/integration/scheduler/podsready/scheduler_test.go:350** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-24T16:00:13Z
```diff
@@ -299,6 +299,55 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			ginkgo.By("delete the waiting 'prod' workload so that it does not get admitted during teardown")
 			gomega.Expect(k8sClient.Delete(ctx, prodWl)).Should(gomega.Succeed())
 		})
-	})
 
+		ginkgo.It("Should move the evicted workload at the end of the queue", func() {
+			localQueueName := "eviction-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time,
+			// letting them all to time out, we should see a circular buffer admission pattern
+			wl1 := testing.MakeWorkload("prod1", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl2 := testing.MakeWorkload("prod2", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl3 := testing.MakeWorkload("prod3", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+
+			ginkgo.By("create the workloads", func() {
+				// since metav1.Time has only second resolution, wait one second between
+				// create calls to avoid any potential creation timestamp collision
+				gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
+				gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
+				gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("create the local queue to start admission", func() {
+				lq := testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, lq)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("waiting for the first workload to be admitted ", func() {
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, wl1)
+			})
+
+			ginkgo.By("waiting the timeout, the first workload should be evicted and the second one admitted ", func() {
+				time.Sleep(podsReadyTimeout)
+				util.ExpectWorkloadsToBeEvicted(ctx, k8sClient, wl1)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, wl2)
+			})
+
+			ginkgo.By("finishing the second workload, the third one should be admitted ", func() {
+				util.FinishWorkloads(ctx, k8sClient, wl2)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, wl3)
+			})
+
+			ginkgo.By("finishing the third workload, the first one should be admitted ", func() {
+				util.FinishWorkloads(ctx, k8sClient, wl3)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, wl1)
+			})
+
+			ginkgo.By("finishing", func() {
+				util.FinishWorkloads(ctx, k8sClient, wl1)
+				util.ExpectWorkloadsToBeFinished(ctx, k8sClient, wl1, wl2, wl3)
+			})
```

  This doesn't need to be part of the test. You can remove it just to keep tests focused on one feature.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-24T17:43:18Z

- **pkg/workload/workload.go:293** by [@trasc](https://github.com/trasc) — 2023-04-24T17:43:18Z
```diff
@@ -236,10 +243,63 @@ func BaseSSAWorkload(w *kueue.Workload) *kueue.Workload {
 	return wlCopy
 }
 
-// AdmissionPatch creates a new object based on the input workload that
-// contains the admission. The object can be used in Server-Side-Apply.
-func AdmissionPatch(w *kueue.Workload) *kueue.Workload {
+// SetAdmission applies the provided admission to the workload.
+// The WorkloadAdmitted and WorkloadEvicted are added or updated if necessary.
+func SetAdmission(w *kueue.Workload, admission *kueue.Admission) {
+	w.Status.Admission = admission
+	admittedCond := metav1.Condition{
+		Type:               kueue.WorkloadAdmitted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             "Admitted",
+		Message:            fmt.Sprintf("Admitted by ClusterQueue %s", w.Status.Admission.ClusterQueue),
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, admittedCond)
+
+	//reset Evicted condition if present.
+	if evictedCond := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadEvicted); evictedCond != nil {
+		evictedCond.Status = metav1.ConditionFalse
+		evictedCond.LastTransitionTime = metav1.Now()
+	}
+}
+
+func SetEvictedCondition(w *kueue.Workload, reason string, message string) {
+	condition := metav1.Condition{
+		Type:               kueue.WorkloadEvicted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             reason,
+		Message:            message,
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, condition)
+}
+
+// admissionPatch creates a new object based on the input workload that contains
+// the admission and related conditions. The object can be used in Server-Side-Apply.
+func admissionPatch(w *kueue.Workload) *kueue.Workload {
 	wlCopy := BaseSSAWorkload(w)
+
 	wlCopy.Status.Admission = w.Status.Admission.DeepCopy()
+	if existing := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadAdmitted); existing != nil {
+		wlCopy.Status.Conditions = append(wlCopy.Status.Conditions, *existing.DeepCopy())
+	}
+	if existing := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadEvicted); existing != nil {
+		wlCopy.Status.Conditions = append(wlCopy.Status.Conditions, *existing.DeepCopy())
+	}
 	return wlCopy
 }
+
+// PatchAdmission updated all the admission related status fields of a workload with SSA.
+func PatchAdmission(ctx context.Context, c client.Client, w *kueue.Workload) error {
```

  It is a bit of a mess with the verbs used in this package Set, Unset, Update, Apply , Patch. For me `Patch` is the closest to what this function is doing, patching the status of the workload.
  
  If we can agree to a set of verbs for just changing the go var  and a set of verbs for doing the changes to the k8s object I'll comply, however I think that this refactoring should be done in another PR/Issue.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-24T20:17:35Z

- **pkg/workload/workload.go:224** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-24T20:17:34Z
```diff
@@ -236,10 +243,63 @@ func BaseSSAWorkload(w *kueue.Workload) *kueue.Workload {
 	return wlCopy
 }
 
-// AdmissionPatch creates a new object based on the input workload that
-// contains the admission. The object can be used in Server-Side-Apply.
-func AdmissionPatch(w *kueue.Workload) *kueue.Workload {
+// SetAdmission applies the provided admission to the workload.
+// The WorkloadAdmitted and WorkloadEvicted are added or updated if necessary.
+func SetAdmission(w *kueue.Workload, admission *kueue.Admission) {
```

  How about we keep the `Set` and `Unset` functions but make it so that they don't call` Patch`. Then the function that actually calls Patch with SSA can be called `Apply`.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-24T20:18:16Z

- **pkg/workload/workload.go:224** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-24T20:18:16Z
```diff
@@ -236,10 +243,63 @@ func BaseSSAWorkload(w *kueue.Workload) *kueue.Workload {
 	return wlCopy
 }
 
-// AdmissionPatch creates a new object based on the input workload that
-// contains the admission. The object can be used in Server-Side-Apply.
-func AdmissionPatch(w *kueue.Workload) *kueue.Workload {
+// SetAdmission applies the provided admission to the workload.
+// The WorkloadAdmitted and WorkloadEvicted are added or updated if necessary.
+func SetAdmission(w *kueue.Workload, admission *kueue.Admission) {
```

  See if you can accommodate this in this PR. If it's a lot of changes, then we can do it in a follow up.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-25T06:10:22Z

- **test/integration/scheduler/podsready/scheduler_test.go:350** by [@trasc](https://github.com/trasc) — 2023-04-25T06:10:22Z
```diff
@@ -299,6 +299,55 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			ginkgo.By("delete the waiting 'prod' workload so that it does not get admitted during teardown")
 			gomega.Expect(k8sClient.Delete(ctx, prodWl)).Should(gomega.Succeed())
 		})
-	})
 
+		ginkgo.It("Should move the evicted workload at the end of the queue", func() {
+			localQueueName := "eviction-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time,
+			// letting them all to time out, we should see a circular buffer admission pattern
+			wl1 := testing.MakeWorkload("prod1", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl2 := testing.MakeWorkload("prod2", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl3 := testing.MakeWorkload("prod3", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+
+			ginkgo.By("create the workloads", func() {
+				// since metav1.Time has only second resolution, wait one second between
+				// create calls to avoid any potential creation timestamp collision
+				gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
+				gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
+				gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("create the local queue to start admission", func() {
+				lq := testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, lq)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("waiting for the first workload to be admitted ", func() {
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, wl1)
+			})
+
+			ginkgo.By("waiting the timeout, the first workload should be evicted and the second one admitted ", func() {
+				time.Sleep(podsReadyTimeout)
+				util.ExpectWorkloadsToBeEvicted(ctx, k8sClient, wl1)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, wl2)
+			})
+
+			ginkgo.By("finishing the second workload, the third one should be admitted ", func() {
+				util.FinishWorkloads(ctx, k8sClient, wl2)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, wl3)
+			})
+
+			ginkgo.By("finishing the third workload, the first one should be admitted ", func() {
+				util.FinishWorkloads(ctx, k8sClient, wl3)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, wl1)
+			})
+
+			ginkgo.By("finishing", func() {
+				util.FinishWorkloads(ctx, k8sClient, wl1)
+				util.ExpectWorkloadsToBeFinished(ctx, k8sClient, wl1, wl2, wl3)
+			})
```

  @mimowo do you want to add something here?  or we should just drop this?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-04-25T06:44:45Z

- **test/integration/scheduler/podsready/scheduler_test.go:350** by [@mimowo](https://github.com/mimowo) — 2023-04-25T06:44:45Z
```diff
@@ -299,6 +299,55 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			ginkgo.By("delete the waiting 'prod' workload so that it does not get admitted during teardown")
 			gomega.Expect(k8sClient.Delete(ctx, prodWl)).Should(gomega.Succeed())
 		})
-	})
 
+		ginkgo.It("Should move the evicted workload at the end of the queue", func() {
+			localQueueName := "eviction-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time,
+			// letting them all to time out, we should see a circular buffer admission pattern
+			wl1 := testing.MakeWorkload("prod1", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl2 := testing.MakeWorkload("prod2", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl3 := testing.MakeWorkload("prod3", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+
+			ginkgo.By("create the workloads", func() {
+				// since metav1.Time has only second resolution, wait one second between
+				// create calls to avoid any potential creation timestamp collision
+				gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
+				gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
+				gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("create the local queue to start admission", func() {
+				lq := testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, lq)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("waiting for the first workload to be admitted ", func() {
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, wl1)
+			})
+
+			ginkgo.By("waiting the timeout, the first workload should be evicted and the second one admitted ", func() {
+				time.Sleep(podsReadyTimeout)
+				util.ExpectWorkloadsToBeEvicted(ctx, k8sClient, wl1)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, wl2)
+			})
+
+			ginkgo.By("finishing the second workload, the third one should be admitted ", func() {
+				util.FinishWorkloads(ctx, k8sClient, wl2)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, wl3)
+			})
+
+			ginkgo.By("finishing the third workload, the first one should be admitted ", func() {
+				util.FinishWorkloads(ctx, k8sClient, wl3)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, wl1)
+			})
+
+			ginkgo.By("finishing", func() {
+				util.FinishWorkloads(ctx, k8sClient, wl1)
+				util.ExpectWorkloadsToBeFinished(ctx, k8sClient, wl1, wl2, wl3)
+			})
```

  I'm fine with dropping.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-25T07:20:29Z

- **pkg/queue/cluster_queue_impl.go:93** by [@trasc](https://github.com/trasc) — 2023-04-25T06:23:26Z
```diff
@@ -89,8 +90,10 @@ func (c *clusterQueueBase) PushOrUpdate(wInfo *workload.Info) {
 	oldInfo := c.inadmissibleWorkloads[key]
 	if oldInfo != nil {
 		// update in place if the workload was inadmissible and didn't change
-		// to potentially become admissible.
-		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) {
+		// to potentially become admissible, unless the Eviction status changed.
```

  done

- **pkg/workload/workload.go:235** by [@trasc](https://github.com/trasc) — 2023-04-25T06:26:13Z
```diff
@@ -236,10 +243,63 @@ func BaseSSAWorkload(w *kueue.Workload) *kueue.Workload {
 	return wlCopy
 }
 
-// AdmissionPatch creates a new object based on the input workload that
-// contains the admission. The object can be used in Server-Side-Apply.
-func AdmissionPatch(w *kueue.Workload) *kueue.Workload {
+// SetAdmission applies the provided admission to the workload.
+// The WorkloadAdmitted and WorkloadEvicted are added or updated if necessary.
+func SetAdmission(w *kueue.Workload, admission *kueue.Admission) {
+	w.Status.Admission = admission
+	admittedCond := metav1.Condition{
+		Type:               kueue.WorkloadAdmitted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             "Admitted",
+		Message:            fmt.Sprintf("Admitted by ClusterQueue %s", w.Status.Admission.ClusterQueue),
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, admittedCond)
+
+	//reset Evicted condition if present.
```

  I prefer not having the condition at all vs having it with a irrelevant or made up `reason`.

- **pkg/workload/workload.go:293** by [@trasc](https://github.com/trasc) — 2023-04-25T07:09:40Z
```diff
@@ -236,10 +243,63 @@ func BaseSSAWorkload(w *kueue.Workload) *kueue.Workload {
 	return wlCopy
 }
 
-// AdmissionPatch creates a new object based on the input workload that
-// contains the admission. The object can be used in Server-Side-Apply.
-func AdmissionPatch(w *kueue.Workload) *kueue.Workload {
+// SetAdmission applies the provided admission to the workload.
+// The WorkloadAdmitted and WorkloadEvicted are added or updated if necessary.
+func SetAdmission(w *kueue.Workload, admission *kueue.Admission) {
+	w.Status.Admission = admission
+	admittedCond := metav1.Condition{
+		Type:               kueue.WorkloadAdmitted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             "Admitted",
+		Message:            fmt.Sprintf("Admitted by ClusterQueue %s", w.Status.Admission.ClusterQueue),
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, admittedCond)
+
+	//reset Evicted condition if present.
+	if evictedCond := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadEvicted); evictedCond != nil {
+		evictedCond.Status = metav1.ConditionFalse
+		evictedCond.LastTransitionTime = metav1.Now()
+	}
+}
+
+func SetEvictedCondition(w *kueue.Workload, reason string, message string) {
+	condition := metav1.Condition{
+		Type:               kueue.WorkloadEvicted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             reason,
+		Message:            message,
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, condition)
+}
+
+// admissionPatch creates a new object based on the input workload that contains
+// the admission and related conditions. The object can be used in Server-Side-Apply.
+func admissionPatch(w *kueue.Workload) *kueue.Workload {
 	wlCopy := BaseSSAWorkload(w)
+
 	wlCopy.Status.Admission = w.Status.Admission.DeepCopy()
+	if existing := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadAdmitted); existing != nil {
+		wlCopy.Status.Conditions = append(wlCopy.Status.Conditions, *existing.DeepCopy())
+	}
+	if existing := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadEvicted); existing != nil {
+		wlCopy.Status.Conditions = append(wlCopy.Status.Conditions, *existing.DeepCopy())
+	}
 	return wlCopy
 }
+
+// PatchAdmission updated all the admission related status fields of a workload with SSA.
+func PatchAdmission(ctx context.Context, c client.Client, w *kueue.Workload) error {
```

  done

- **pkg/workload/workload.go:283** by [@trasc](https://github.com/trasc) — 2023-04-25T07:09:57Z
```diff
@@ -236,10 +243,63 @@ func BaseSSAWorkload(w *kueue.Workload) *kueue.Workload {
 	return wlCopy
 }
 
-// AdmissionPatch creates a new object based on the input workload that
-// contains the admission. The object can be used in Server-Side-Apply.
-func AdmissionPatch(w *kueue.Workload) *kueue.Workload {
+// SetAdmission applies the provided admission to the workload.
+// The WorkloadAdmitted and WorkloadEvicted are added or updated if necessary.
+func SetAdmission(w *kueue.Workload, admission *kueue.Admission) {
+	w.Status.Admission = admission
+	admittedCond := metav1.Condition{
+		Type:               kueue.WorkloadAdmitted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             "Admitted",
+		Message:            fmt.Sprintf("Admitted by ClusterQueue %s", w.Status.Admission.ClusterQueue),
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, admittedCond)
+
+	//reset Evicted condition if present.
+	if evictedCond := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadEvicted); evictedCond != nil {
+		evictedCond.Status = metav1.ConditionFalse
+		evictedCond.LastTransitionTime = metav1.Now()
+	}
+}
+
+func SetEvictedCondition(w *kueue.Workload, reason string, message string) {
+	condition := metav1.Condition{
+		Type:               kueue.WorkloadEvicted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             reason,
+		Message:            message,
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, condition)
+}
+
+// admissionPatch creates a new object based on the input workload that contains
+// the admission and related conditions. The object can be used in Server-Side-Apply.
+func admissionPatch(w *kueue.Workload) *kueue.Workload {
 	wlCopy := BaseSSAWorkload(w)
+
 	wlCopy.Status.Admission = w.Status.Admission.DeepCopy()
+	if existing := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadAdmitted); existing != nil {
```

  done

- **pkg/workload/workload.go:224** by [@trasc](https://github.com/trasc) — 2023-04-25T07:13:44Z
```diff
@@ -236,10 +243,63 @@ func BaseSSAWorkload(w *kueue.Workload) *kueue.Workload {
 	return wlCopy
 }
 
-// AdmissionPatch creates a new object based on the input workload that
-// contains the admission. The object can be used in Server-Side-Apply.
-func AdmissionPatch(w *kueue.Workload) *kueue.Workload {
+// SetAdmission applies the provided admission to the workload.
+// The WorkloadAdmitted and WorkloadEvicted are added or updated if necessary.
+func SetAdmission(w *kueue.Workload, admission *kueue.Admission) {
```

  done , for the admissin related changes , only `ApplyAdmissionStatus` will do k8s object changes. 
  
  There is also `UpdateStatus` which is doing both, if needed this should be done in another PR.

- **test/integration/scheduler/podsready/scheduler_test.go:350** by [@trasc](https://github.com/trasc) — 2023-04-25T07:17:35Z
```diff
@@ -299,6 +299,55 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			ginkgo.By("delete the waiting 'prod' workload so that it does not get admitted during teardown")
 			gomega.Expect(k8sClient.Delete(ctx, prodWl)).Should(gomega.Succeed())
 		})
-	})
 
+		ginkgo.It("Should move the evicted workload at the end of the queue", func() {
+			localQueueName := "eviction-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time,
+			// letting them all to time out, we should see a circular buffer admission pattern
+			wl1 := testing.MakeWorkload("prod1", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl2 := testing.MakeWorkload("prod2", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl3 := testing.MakeWorkload("prod3", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+
+			ginkgo.By("create the workloads", func() {
+				// since metav1.Time has only second resolution, wait one second between
+				// create calls to avoid any potential creation timestamp collision
+				gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
+				gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
+				gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("create the local queue to start admission", func() {
+				lq := testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, lq)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("waiting for the first workload to be admitted ", func() {
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, wl1)
+			})
+
+			ginkgo.By("waiting the timeout, the first workload should be evicted and the second one admitted ", func() {
+				time.Sleep(podsReadyTimeout)
+				util.ExpectWorkloadsToBeEvicted(ctx, k8sClient, wl1)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, wl2)
+			})
+
+			ginkgo.By("finishing the second workload, the third one should be admitted ", func() {
+				util.FinishWorkloads(ctx, k8sClient, wl2)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, wl3)
+			})
+
+			ginkgo.By("finishing the third workload, the first one should be admitted ", func() {
+				util.FinishWorkloads(ctx, k8sClient, wl3)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, wl1)
+			})
+
+			ginkgo.By("finishing", func() {
+				util.FinishWorkloads(ctx, k8sClient, wl1)
+				util.ExpectWorkloadsToBeFinished(ctx, k8sClient, wl1, wl2, wl3)
+			})
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-25T20:01:33Z

- **pkg/workload/workload.go:235** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-25T20:01:33Z
```diff
@@ -236,10 +243,63 @@ func BaseSSAWorkload(w *kueue.Workload) *kueue.Workload {
 	return wlCopy
 }
 
-// AdmissionPatch creates a new object based on the input workload that
-// contains the admission. The object can be used in Server-Side-Apply.
-func AdmissionPatch(w *kueue.Workload) *kueue.Workload {
+// SetAdmission applies the provided admission to the workload.
+// The WorkloadAdmitted and WorkloadEvicted are added or updated if necessary.
+func SetAdmission(w *kueue.Workload, admission *kueue.Admission) {
+	w.Status.Admission = admission
+	admittedCond := metav1.Condition{
+		Type:               kueue.WorkloadAdmitted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             "Admitted",
+		Message:            fmt.Sprintf("Admitted by ClusterQueue %s", w.Status.Admission.ClusterQueue),
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, admittedCond)
+
+	//reset Evicted condition if present.
```

  From https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md#typical-status-properties:
  
  > Controllers should apply their conditions to a resource the first time they visit the resource, even if the status is Unknown.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-25T20:19:20Z

- **pkg/workload/workload.go:235** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-25T20:19:20Z
```diff
@@ -236,10 +243,63 @@ func BaseSSAWorkload(w *kueue.Workload) *kueue.Workload {
 	return wlCopy
 }
 
-// AdmissionPatch creates a new object based on the input workload that
-// contains the admission. The object can be used in Server-Side-Apply.
-func AdmissionPatch(w *kueue.Workload) *kueue.Workload {
+// SetAdmission applies the provided admission to the workload.
+// The WorkloadAdmitted and WorkloadEvicted are added or updated if necessary.
+func SetAdmission(w *kueue.Workload, admission *kueue.Admission) {
+	w.Status.Admission = admission
+	admittedCond := metav1.Condition{
+		Type:               kueue.WorkloadAdmitted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             "Admitted",
+		Message:            fmt.Sprintf("Admitted by ClusterQueue %s", w.Status.Admission.ClusterQueue),
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, admittedCond)
+
+	//reset Evicted condition if present.
```

  I opened this slack thread to learn more about the rationale for a required Reason https://kubernetes.slack.com/archives/C0EG7JC6T/p1682453786427459
  
  But we will still need a reason for now. Perhaps it could be: NotAdmittedYet

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-25T20:21:53Z

- **pkg/workload/workload.go:235** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-25T20:21:52Z
```diff
@@ -236,10 +243,63 @@ func BaseSSAWorkload(w *kueue.Workload) *kueue.Workload {
 	return wlCopy
 }
 
-// AdmissionPatch creates a new object based on the input workload that
-// contains the admission. The object can be used in Server-Side-Apply.
-func AdmissionPatch(w *kueue.Workload) *kueue.Workload {
+// SetAdmission applies the provided admission to the workload.
+// The WorkloadAdmitted and WorkloadEvicted are added or updated if necessary.
+func SetAdmission(w *kueue.Workload, admission *kueue.Admission) {
+	w.Status.Admission = admission
+	admittedCond := metav1.Condition{
+		Type:               kueue.WorkloadAdmitted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             "Admitted",
+		Message:            fmt.Sprintf("Admitted by ClusterQueue %s", w.Status.Admission.ClusterQueue),
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, admittedCond)
+
+	//reset Evicted condition if present.
```

  Let's leave it for a follow up
  
  /approve
  /lgtm

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-25T20:22:53Z

- **CHANGELOG/CHANGELOG-0.4.md:9** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-25T20:22:52Z
```diff
@@ -2,6 +2,13 @@
 
 Changes since `v0.3.0`:
 
+### Features
+
+- Move the workloads evicted due to pods ready timeout to the end of the queue. #689
+
+## Production Readiness
```

  uhm... this was a mistake... they should all be ###

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-25T20:22:58Z

- **CHANGELOG/CHANGELOG-0.4.md:9** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-25T20:22:58Z
```diff
@@ -2,6 +2,13 @@
 
 Changes since `v0.3.0`:
 
+### Features
+
+- Move the workloads evicted due to pods ready timeout to the end of the queue. #689
+
+## Production Readiness
```

  /hold

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-27T12:18:17Z

- **pkg/controller/core/workload_controller.go:132** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-27T12:13:47Z
```diff
@@ -127,6 +127,17 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling Workload")
 
+	// if a pods ready timeout eviction is ongoing.
+	if evictionCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); evictionCond != nil && evictionCond.Status == metav1.ConditionTrue &&
+		evictionCond.Reason == kueue.WorkloadEvictedByPodsReadyTimeout &&
```

  Maybe I said this before, but I think it shouldn't be conditional on the reason.
  But we can standardize with preemption in a follow up, and based on the outcome of #510
  
  Leaving this comment for future reference.

- **pkg/controller/core/workload_controller.go:184** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-27T12:16:59Z
```diff
@@ -136,40 +147,41 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 
 	if !r.queues.QueueForWorkloadExists(&wl) {
 		log.V(3).Info("Workload is inadmissible because of missing LocalQueue", "localQueue", klog.KRef(wl.Namespace, wl.Spec.QueueName))
-		err := workload.UnsetAdmissionWithCondition(ctx, r.client, &wl,
-			"Inadmissible", fmt.Sprintf("LocalQueue %s doesn't exist", wl.Spec.QueueName))
+		workload.UnsetAdmissionWithCondition(&wl, "Inadmissible", fmt.Sprintf("LocalQueue %s doesn't exist", wl.Spec.QueueName))
+		err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
 	if !cqOk {
 		log.V(3).Info("Workload is inadmissible because of missing ClusterQueue", "clusterQueue", klog.KRef("", cqName))
-		err := workload.UnsetAdmissionWithCondition(ctx, r.client, &wl,
-			"Inadmissible", fmt.Sprintf("ClusterQueue %s doesn't exist", cqName))
+		workload.UnsetAdmissionWithCondition(&wl, "Inadmissible", fmt.Sprintf("ClusterQueue %s doesn't exist", cqName))
+		err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	if !r.cache.ClusterQueueActive(cqName) {
 		log.V(3).Info("Workload is inadmissible because ClusterQueue is inactive", "clusterQueue", klog.KRef("", cqName))
-		err := workload.UnsetAdmissionWithCondition(ctx, r.client, &wl,
-			"Inadmissible", fmt.Sprintf("ClusterQueue %s is inactive", cqName))
+		workload.UnsetAdmissionWithCondition(&wl, "Inadmissible", fmt.Sprintf("ClusterQueue %s is inactive", cqName))
+		err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 	return ctrl.Result{}, nil
 }
 
 func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req ctrl.Request, wl *kueue.Workload) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
 	countingTowardsTimeout, recheckAfter := r.admittedNotReadyWorkload(wl, realClock)
 	if !countingTowardsTimeout {
 		return ctrl.Result{}, nil
 	}
 	if recheckAfter > 0 {
-		klog.V(4).InfoS("Workload not yet ready and did not exceed its timeout", "workload", req.NamespacedName.String(), "recheckAfter", recheckAfter)
+		log.V(4).Info("Workload not yet ready and did not exceed its timeout", "recheckAfter", recheckAfter)
 		return ctrl.Result{RequeueAfter: recheckAfter}, nil
 	} else {
-		klog.V(2).InfoS("Cancelling admission of the workload due to exceeding the PodsReady timeout", "workload", req.NamespacedName.String())
-		err := workload.UnsetAdmissionWithCondition(ctx, r.client, wl,
-			"Evicted", fmt.Sprintf("Exceeded the PodsReady timeout %s", req.NamespacedName.String()))
+		log.V(2).Info("Start the eviction of the workload due to exceeding the PodsReady timeout")
+		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByPodsReadyTimeout, fmt.Sprintf("Exceeded the PodsReady timeout %s", req.NamespacedName.String()))
+		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, false)
```

  sorry, I just saw this.
  Why not strict? There could be potential for race conditions after #610.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-27T12:49:11Z

- **pkg/controller/core/workload_controller.go:184** by [@trasc](https://github.com/trasc) — 2023-04-27T12:48:13Z
```diff
@@ -136,40 +147,41 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 
 	if !r.queues.QueueForWorkloadExists(&wl) {
 		log.V(3).Info("Workload is inadmissible because of missing LocalQueue", "localQueue", klog.KRef(wl.Namespace, wl.Spec.QueueName))
-		err := workload.UnsetAdmissionWithCondition(ctx, r.client, &wl,
-			"Inadmissible", fmt.Sprintf("LocalQueue %s doesn't exist", wl.Spec.QueueName))
+		workload.UnsetAdmissionWithCondition(&wl, "Inadmissible", fmt.Sprintf("LocalQueue %s doesn't exist", wl.Spec.QueueName))
+		err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
 	if !cqOk {
 		log.V(3).Info("Workload is inadmissible because of missing ClusterQueue", "clusterQueue", klog.KRef("", cqName))
-		err := workload.UnsetAdmissionWithCondition(ctx, r.client, &wl,
-			"Inadmissible", fmt.Sprintf("ClusterQueue %s doesn't exist", cqName))
+		workload.UnsetAdmissionWithCondition(&wl, "Inadmissible", fmt.Sprintf("ClusterQueue %s doesn't exist", cqName))
+		err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	if !r.cache.ClusterQueueActive(cqName) {
 		log.V(3).Info("Workload is inadmissible because ClusterQueue is inactive", "clusterQueue", klog.KRef("", cqName))
-		err := workload.UnsetAdmissionWithCondition(ctx, r.client, &wl,
-			"Inadmissible", fmt.Sprintf("ClusterQueue %s is inactive", cqName))
+		workload.UnsetAdmissionWithCondition(&wl, "Inadmissible", fmt.Sprintf("ClusterQueue %s is inactive", cqName))
+		err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 	return ctrl.Result{}, nil
 }
 
 func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req ctrl.Request, wl *kueue.Workload) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
 	countingTowardsTimeout, recheckAfter := r.admittedNotReadyWorkload(wl, realClock)
 	if !countingTowardsTimeout {
 		return ctrl.Result{}, nil
 	}
 	if recheckAfter > 0 {
-		klog.V(4).InfoS("Workload not yet ready and did not exceed its timeout", "workload", req.NamespacedName.String(), "recheckAfter", recheckAfter)
+		log.V(4).Info("Workload not yet ready and did not exceed its timeout", "recheckAfter", recheckAfter)
 		return ctrl.Result{RequeueAfter: recheckAfter}, nil
 	} else {
-		klog.V(2).InfoS("Cancelling admission of the workload due to exceeding the PodsReady timeout", "workload", req.NamespacedName.String())
-		err := workload.UnsetAdmissionWithCondition(ctx, r.client, wl,
-			"Evicted", fmt.Sprintf("Exceeded the PodsReady timeout %s", req.NamespacedName.String()))
+		log.V(2).Info("Start the eviction of the workload due to exceeding the PodsReady timeout")
+		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByPodsReadyTimeout, fmt.Sprintf("Exceeded the PodsReady timeout %s", req.NamespacedName.String()))
+		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, false)
```

  Using strict should be discouraged since by using it we louse most of the benefits of SSA. Regarding 610 , depending on how it will be implemented, but as I see it is that the workload can only be admitted and the only conflict scenario is when the workload is preempted at the same time as  PRTimeout. both resulting in the eviction.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-27T12:59:06Z

- **pkg/controller/core/workload_controller.go:184** by [@trasc](https://github.com/trasc) — 2023-04-27T12:59:05Z
```diff
@@ -136,40 +147,41 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 
 	if !r.queues.QueueForWorkloadExists(&wl) {
 		log.V(3).Info("Workload is inadmissible because of missing LocalQueue", "localQueue", klog.KRef(wl.Namespace, wl.Spec.QueueName))
-		err := workload.UnsetAdmissionWithCondition(ctx, r.client, &wl,
-			"Inadmissible", fmt.Sprintf("LocalQueue %s doesn't exist", wl.Spec.QueueName))
+		workload.UnsetAdmissionWithCondition(&wl, "Inadmissible", fmt.Sprintf("LocalQueue %s doesn't exist", wl.Spec.QueueName))
+		err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
 	if !cqOk {
 		log.V(3).Info("Workload is inadmissible because of missing ClusterQueue", "clusterQueue", klog.KRef("", cqName))
-		err := workload.UnsetAdmissionWithCondition(ctx, r.client, &wl,
-			"Inadmissible", fmt.Sprintf("ClusterQueue %s doesn't exist", cqName))
+		workload.UnsetAdmissionWithCondition(&wl, "Inadmissible", fmt.Sprintf("ClusterQueue %s doesn't exist", cqName))
+		err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	if !r.cache.ClusterQueueActive(cqName) {
 		log.V(3).Info("Workload is inadmissible because ClusterQueue is inactive", "clusterQueue", klog.KRef("", cqName))
-		err := workload.UnsetAdmissionWithCondition(ctx, r.client, &wl,
-			"Inadmissible", fmt.Sprintf("ClusterQueue %s is inactive", cqName))
+		workload.UnsetAdmissionWithCondition(&wl, "Inadmissible", fmt.Sprintf("ClusterQueue %s is inactive", cqName))
+		err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 	return ctrl.Result{}, nil
 }
 
 func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req ctrl.Request, wl *kueue.Workload) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
 	countingTowardsTimeout, recheckAfter := r.admittedNotReadyWorkload(wl, realClock)
 	if !countingTowardsTimeout {
 		return ctrl.Result{}, nil
 	}
 	if recheckAfter > 0 {
-		klog.V(4).InfoS("Workload not yet ready and did not exceed its timeout", "workload", req.NamespacedName.String(), "recheckAfter", recheckAfter)
+		log.V(4).Info("Workload not yet ready and did not exceed its timeout", "recheckAfter", recheckAfter)
 		return ctrl.Result{RequeueAfter: recheckAfter}, nil
 	} else {
-		klog.V(2).InfoS("Cancelling admission of the workload due to exceeding the PodsReady timeout", "workload", req.NamespacedName.String())
-		err := workload.UnsetAdmissionWithCondition(ctx, r.client, wl,
-			"Evicted", fmt.Sprintf("Exceeded the PodsReady timeout %s", req.NamespacedName.String()))
+		log.V(2).Info("Start the eviction of the workload due to exceeding the PodsReady timeout")
+		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByPodsReadyTimeout, fmt.Sprintf("Exceeded the PodsReady timeout %s", req.NamespacedName.String()))
+		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, false)
```

  done
