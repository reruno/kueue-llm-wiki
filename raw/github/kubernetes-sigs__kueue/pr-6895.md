# PR #6895: TAS: stop injecting the TAS Label into Pods

**Summary**: TAS: stop injecting the TAS Label into Pods

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6895

**Last updated**: 2026-02-21T15:47:14Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `tas-drop-label3` → `main`
- **Created**: 2025-09-17T16:47:23Z
- **Updated**: 2026-02-21T15:47:14Z
- **Merged**: 2025-09-18T13:01:45Z
- **Closed**: 2025-09-18T13:01:45Z
- **Labels**: `release-note`, `lgtm`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 9   **+61 / -19**

## Description

#### What type of PR is this?

/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #3450

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
TAS: stop setting the "kueue.x-k8s.io/tas" label on Pods. 

In case the implicit TAS mode is used, then the `kueue.x-k8s.io/podset-unconstrained-topology=true` annotation
is set on Pods.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-09-17T16:47:30Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | ad08a5642953ced1fe5f9725614b0de3dccaf33d |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68cbfc940630a6000862c1f4 |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-17T16:47:41Z

/assign @tenzen-y

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-17T17:39:57Z

/test pull-kueue-test-e2e-main-1-33
infra

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-18T08:12:33Z

cc @PBundyra @gabesaba could you give it a pass?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-18T09:42:31Z

/test pull-kueue-test-e2e-main-1-31
/test pull-kueue-test-e2e-multikueue-main
known flakes

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-18T10:51:35Z

/test pull-kueue-test-e2e-multikueue-main

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-18T12:22:44Z

LGTM label has been added.  <details>Git tree hash: 42daba1c548686a7e4f10eae9e9ef9203f3d97e4</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-18T12:22:47Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6895#" title="Author self-approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6895#pullrequestreview-3239266396" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-18T12:29:44Z

/hold
let me address them streight, maybe it is not rebased

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-18T12:30:02Z

> We may also think if we drop the label entirely or just deprecate, to handle gracefully upgrade for users from TAS Alpha to TAS Beta. We have some adoption of Alpha, so maybe safer to keep label for one-two releases anyway, wdyt?

Based on our offline discussion, we decided to keep the TAS label for 2 minor releases since immediate removal of this label could cause disruption in the existing user environment.

Indeed, TAS is alpha features. So, we do not have any commitment to keep backward compatibility for the alpha feature.
But, keeping this label is very trivial work, but the advantages for existing users are very high.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-18T12:35:48Z

/unhold
Just pushed to rename "kueuealpha" as "kueue".

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-18T12:36:56Z

Thanks!
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-18T12:37:03Z

LGTM label has been added.  <details>Git tree hash: b7d2358b7640ac1021e5863fec4e0dee9b4c8fcc</details>

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-18T12:22:37Z

Thank you!
I left follow-up comments.

/lgtm
/approve

- **pkg/util/tas/tas.go:40** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-18T12:12:07Z
```diff
@@ -30,6 +30,30 @@ func DomainID(levelValues []string) TopologyDomainID {
 	return TopologyDomainID(strings.Join(levelValues, ","))
 }
 
+func IsTAS(pod *corev1.Pod) bool {
+	if IsExplicitTAS(pod.Annotations) {
+		return true
+	}
+	if _, ok := pod.Annotations[kueuealpha.PodSetUnconstrainedTopologyAnnotation]; ok {
+		return true
+	}
+	// TODO: remove after 0.16
```

  Let's open a dedicated issue for this follow-up.

- **pkg/util/tas/tas.go:53** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-18T12:13:30Z
```diff
@@ -30,6 +30,30 @@ func DomainID(levelValues []string) TopologyDomainID {
 	return TopologyDomainID(strings.Join(levelValues, ","))
 }
 
+func IsTAS(pod *corev1.Pod) bool {
+	if IsExplicitTAS(pod.Annotations) {
+		return true
+	}
+	if _, ok := pod.Annotations[kueuealpha.PodSetUnconstrainedTopologyAnnotation]; ok {
+		return true
+	}
+	// TODO: remove after 0.16
+	if _, ok := pod.Labels[kueuealpha.TASLabel]; ok {
+		return true
+	}
+	return false
+}
+
+func IsExplicitTAS(annots map[string]string) bool {
+	if _, ok := annots[kueuealpha.PodSetPreferredTopologyAnnotation]; ok {
+		return true
+	}
+	if _, ok := annots[kueuealpha.PodSetRequiredTopologyAnnotation]; ok {
+		return true
+	}
```

  ```suggestion
  	if _, ok := annots[kueue.PodSetPreferredTopologyAnnotation]; ok {
  		return true
  	}
  	if _, ok := annots[kueue.PodSetRequiredTopologyAnnotation]; ok {
  		return true
  	}
  ```
  
  Shouldn't we use `kueue` as import name since Topology is already beta API?

- **pkg/util/tas/tas.go:37** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-18T12:13:39Z
```diff
@@ -30,6 +30,30 @@ func DomainID(levelValues []string) TopologyDomainID {
 	return TopologyDomainID(strings.Join(levelValues, ","))
 }
 
+func IsTAS(pod *corev1.Pod) bool {
+	if IsExplicitTAS(pod.Annotations) {
+		return true
+	}
+	if _, ok := pod.Annotations[kueuealpha.PodSetUnconstrainedTopologyAnnotation]; ok {
```

  ```suggestion
  	if _, ok := pod.Annotations[kueue.PodSetUnconstrainedTopologyAnnotation]; ok {
  ```

- **pkg/util/tas/tas.go:53** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-18T12:22:13Z
```diff
@@ -30,6 +30,30 @@ func DomainID(levelValues []string) TopologyDomainID {
 	return TopologyDomainID(strings.Join(levelValues, ","))
 }
 
+func IsTAS(pod *corev1.Pod) bool {
+	if IsExplicitTAS(pod.Annotations) {
+		return true
+	}
+	if _, ok := pod.Annotations[kueuealpha.PodSetUnconstrainedTopologyAnnotation]; ok {
+		return true
+	}
+	// TODO: remove after 0.16
+	if _, ok := pod.Labels[kueuealpha.TASLabel]; ok {
+		return true
+	}
+	return false
+}
+
+func IsExplicitTAS(annots map[string]string) bool {
+	if _, ok := annots[kueuealpha.PodSetPreferredTopologyAnnotation]; ok {
+		return true
+	}
+	if _, ok := annots[kueuealpha.PodSetRequiredTopologyAnnotation]; ok {
+		return true
+	}
```

  cc @mbobrovskyi

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-18T12:35:59Z

- **pkg/util/tas/tas.go:37** by [@mimowo](https://github.com/mimowo) — 2025-09-18T12:35:59Z
```diff
@@ -30,6 +30,30 @@ func DomainID(levelValues []string) TopologyDomainID {
 	return TopologyDomainID(strings.Join(levelValues, ","))
 }
 
+func IsTAS(pod *corev1.Pod) bool {
+	if IsExplicitTAS(pod.Annotations) {
+		return true
+	}
+	if _, ok := pod.Annotations[kueuealpha.PodSetUnconstrainedTopologyAnnotation]; ok {
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-18T12:36:03Z

- **pkg/util/tas/tas.go:53** by [@mimowo](https://github.com/mimowo) — 2025-09-18T12:36:03Z
```diff
@@ -30,6 +30,30 @@ func DomainID(levelValues []string) TopologyDomainID {
 	return TopologyDomainID(strings.Join(levelValues, ","))
 }
 
+func IsTAS(pod *corev1.Pod) bool {
+	if IsExplicitTAS(pod.Annotations) {
+		return true
+	}
+	if _, ok := pod.Annotations[kueuealpha.PodSetUnconstrainedTopologyAnnotation]; ok {
+		return true
+	}
+	// TODO: remove after 0.16
+	if _, ok := pod.Labels[kueuealpha.TASLabel]; ok {
+		return true
+	}
+	return false
+}
+
+func IsExplicitTAS(annots map[string]string) bool {
+	if _, ok := annots[kueuealpha.PodSetPreferredTopologyAnnotation]; ok {
+		return true
+	}
+	if _, ok := annots[kueuealpha.PodSetRequiredTopologyAnnotation]; ok {
+		return true
+	}
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-18T12:42:10Z

- **pkg/util/tas/tas.go:40** by [@mimowo](https://github.com/mimowo) — 2025-09-18T12:42:10Z
```diff
@@ -30,6 +30,30 @@ func DomainID(levelValues []string) TopologyDomainID {
 	return TopologyDomainID(strings.Join(levelValues, ","))
 }
 
+func IsTAS(pod *corev1.Pod) bool {
+	if IsExplicitTAS(pod.Annotations) {
+		return true
+	}
+	if _, ok := pod.Annotations[kueuealpha.PodSetUnconstrainedTopologyAnnotation]; ok {
+		return true
+	}
+	// TODO: remove after 0.16
```

  Opened: https://github.com/kubernetes-sigs/kueue/issues/6911
