# PR #6906: Implement more flexible limitations on the number of Flavors per ClusterQueue

**Summary**: Implement more flexible limitations on the number of Flavors per ClusterQueue

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6906

**Last updated**: 2025-09-22T14:43:24Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@LarsSven](https://github.com/LarsSven)
- **Branch**: `ls/relax-flavor-limit` → `main`
- **Created**: 2025-09-18T09:15:13Z
- **Updated**: 2025-09-22T14:43:24Z
- **Merged**: 2025-09-22T14:38:17Z
- **Closed**: 2025-09-22T14:38:17Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 10   **+250 / -64**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

#### What this PR does / why we need it:
Currently Kueue limits the number of resource groups per ClusterQueue to 16, and then the number of flavors per resource group to 16, with the intention to limit the maximum number of flavors per ClusterQueue, since it can become a performance bottleneck. However, certain setups have very few resourcegroups but many flavors per resourcegroup, in which case the performance is perfectly fine as the total number of flavors is still acceptable, but they are just distributed across fewer resource groups. It makes much more sense to have a global "flavors per clusterqueue" limit than a limit on both lists.

This PR implements the following limits:
- Bump resource groups per clusterqueue from 16 to 64
- Bump flavors per resource group from 16 to 64
- Impose a new limit that limits total flavors per clusterqueue to 256

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #6891 #6007

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
The following limits for ClusterQueue quota specification have been relaxed:
- the number of Flavors per ResourceGroup is increased from 16 to 64
- the number of Resources per Flavor, within a ResourceGroup, is increased from 16 to 64

We also provide the following additional limits:
- the total number of Flavors across all ResourceGroups is <= 256
- the total number of Resources across all ResourceGroups is <= 256
- the total number of (Flavor, Resource) pairs within a ResourceGroup is <= 512
```

## Discussion

### Comment by [@linux-foundation-easycla[bot]](https://github.com/apps/linux-foundation-easycla) — 2025-09-18T09:15:20Z

<a href="https://easycla.lfx.linuxfoundation.org/#/?version=2"><img src="https://s3.amazonaws.com/cla-project-logo-prod/cla-signed.svg?v=2" alt="CLA Signed" align="left" height="28" width="328" ></a><br/><br />The committers listed above are authorized under a signed CLA.<ul><li>:white_check_mark: login: LarsSven / name: Lars  (96a073b5304cfc008c54e168d69bbd79510fedb7, add149d5d09717365338274501ce4a49cfc13791, f5e2b84a3594badf664f72e3db88060d1f65357f, 0d41dc13fa39f929950e388e69fa572b9ec6b373, 4bf96259d957e7cf524e276e4936d97baf6d7afc, 6895b230d2c58854fae51d2088e1c4108d3fb432, 584be09ca0978ed5a1c11c668f9c0fb73192ad13, 709c59e30180030c52fdb6dd77571b761ca272ea, 1826f0f933562a9ca1cbda8f2bfcfe1acc52221e, 1e0e04a6cf08d2fac9c3798471a23c145dcb7b93, 161e9f37324a96ca54303472829104055254b484, 6efa0e29eb15706793f7f029d136b8c3e3e61bf2, eeeb7a9170817a7fbfa7f51e89d2f3f8f11b71e6)</li></ul><!-- Date Modified: 2025-09-22 14:12:22.039127 -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-18T09:15:22Z

Welcome @LarsSven! <br><br>It looks like this is your first PR to <a href='https://github.com/kubernetes-sigs/kueue'>kubernetes-sigs/kueue</a> 🎉. Please refer to our [pull request process documentation](https://www.kubernetes.dev/docs/guide/pull-requests/) to help your PR have a smooth ride to approval. <br><br>You will be prompted by a bot to use commands during the review process. Do not be afraid to follow the prompts! It is okay to experiment. [Here is the bot commands documentation](https://go.k8s.io/bot-commands). <br><br>You can also check if kubernetes-sigs/kueue has [its own contribution guidelines](https://github.com/kubernetes-sigs/kueue/tree/master/CONTRIBUTING.md). <br><br>You may want to refer to our [testing guide](https://git.k8s.io/community/contributors/devel/sig-testing/testing.md) if you run into trouble with your tests not passing. <br><br>If you are having difficulty getting your pull request seen, please follow the [recommended escalation practices](https://www.kubernetes.dev/docs/guide/pull-requests/#why-is-my-pull-request-not-getting-reviewed). Also, for tips and tricks in the contribution process you may want to read the [Kubernetes contributor cheat sheet](https://www.kubernetes.dev/docs/contributor-cheatsheet/). We want to make sure your contribution gets all the attention it needs! <br><br>Thank you, and welcome to Kubernetes. :smiley:

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-18T09:15:24Z

Hi @LarsSven. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-09-18T09:16:00Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 709c59e30180030c52fdb6dd77571b761ca272ea |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68d15944a08fa500080bf5a8 |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-18T09:43:03Z

/ok-to-test

### Comment by [@LarsSven](https://github.com/LarsSven) — 2025-09-18T14:31:50Z

@mimowo I updated the PR to reflect the summary of expected changes.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-18T14:34:32Z

> @mimowo I updated the PR to reflect the summary of expected changes.

Thank you! The new set of constraints looks good and fairly intuitive to me.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-19T11:34:57Z

Please regenerate the Helm charts using `make update-helm`.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-19T12:23:39Z

@LarsSven I've talked a little more on that with @tenzen-y (Yuki) and I'm leaning to request another constraint suggested by Yuki, that is on the total number of (Resource, Flavor) pairs within a group. This way we can better control the complexity of the algorithm in the worst case scenario.

We also discussed that instead of webhook we should use CEL rules. However, since we already have a long cluster_queue webhook, I think it is justified to use webhook, and move to CEL as a separate follow up work.

Let me know if you are up to rewrite the webhook checks into CEL, either now, or in a follow up work.

### Comment by [@LarsSven](https://github.com/LarsSven) — 2025-09-19T13:10:32Z

I feel like this PR is growing a bit out of scope with those changes. The worst case scenario of (Resource, Flavor) pairs was possible in the old solution anyways too, so this change should not make any situation possible that wasn't possible already. The same basic constraints of max covered resources and max flavors have not changed.

So I would really like to make the argument that the (Resource, Flavor) check would be a separate feature that is solving something outside the scope of what this PR is trying to do. If this PR was making new performance problems possible I would agree it would be in scope, but it doesn't. I would also prefer to do the CEL in another PR too. Especially if you want to migrate some older code that isn't part of this PR.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-19T13:23:19Z

I see, the issue is that we cannot "stenghten" validation later (post release). Once we release relaxed validation it needs to stay forever. This is why I'm careful with the additional checks. 

The motivation for verifying the total number of (Flavor, Resource) pairs is to control the unit of quota counting, as explained in this comment https://github.com/kubernetes-sigs/kueue/issues/6891#issuecomment-3307755638

Sure, I'm ok to drop the aspirations to use CEL for now. This can be separate PR for sure.

### Comment by [@LarsSven](https://github.com/LarsSven) — 2025-09-19T13:29:30Z

So what values are you looking for for this new limit?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-19T13:35:33Z

For each group:

total := 0
for each Flavor:
  total += len(Flavor.Resources)
 total <= 256

### Comment by [@LarsSven](https://github.com/LarsSven) — 2025-09-19T13:42:28Z

Isn't having 256 flavor-resource combinations very low if we allow 256 resources and 256 flavors? I feel like this is adding new strict requirements that weren't there before. My assumption was that this new limit was going to be a lot higher than this. If you now have 4 covered resources, you're suddenly bound to 64 flavors max.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-19T13:50:24Z

> Isn't having 256 flavor-resource combinations very low if we allow 256 resources and 256 flavors? I feel like this is adding new strict requirements that weren't there before.

Indeed, it might be on the conservative side. It is basically keeping the same number of (Resource, Flavor) pairs as before, since before you could have within group 16*16. 

> My assumption was that this new limit was going to be a lot higher than this. If you now have 4 covered resources, you're suddenly bound to 64 flavors max.

Is this proposed constraint prohibitive for your use-case? If so, then I'm ok to not impose it.

### Comment by [@LarsSven](https://github.com/LarsSven) — 2025-09-19T14:08:02Z

Wait, for my understanding, is this resource-flavor pair on a per-resource-group basis? My assumption was that it was 256 per clusterqueue, which made it feel very constraining. But if it is indeed on resource group level it would indeed be the same as max before.

Personally, I would like to see this limit a bit higher, maybe 512? Users should also have a bit of room themselves to sacrifice performance for scale. I'm sure there are plenty of usecases where they want to exceed 256 and the scheduler would still perform perfectly fine. If you're going to constrain this so aggressively, then you limit the users who do have the right infrastructure to be able to run this with great performance (like us for example).

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-19T14:15:38Z

> If you're going to constrain this so aggressively, then you limit the users who do have the right infrastructure to be able to run this with great performance (like us for example).

+1, I don't want to limit any business use cases, for sure. Just trying to make some limits to allow reasoning about the worst case scenarios.

> Wait, for my understanding, is this resource-flavor pair on a per-resource-group basis? My assumption was that it was 256 per clusterqueue, which made it feel very constraining. But if it is indeed on resource group level it would indeed be the same as max before.

Yes, that is the proposal for the new limit. Yes, the idea was to keep this invariant.

> Personally, I would like to see this limit a bit higher, maybe 512?

I'm ok with that.

### Comment by [@LarsSven](https://github.com/LarsSven) — 2025-09-19T14:24:21Z

Okay, I will get back to that next week 👍

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-19T14:29:01Z

Thank you. FYI, we are targeting the 0.14 release Sep 25: https://github.com/kubernetes-sigs/kueue/issues/6756

### Comment by [@LarsSven](https://github.com/LarsSven) — 2025-09-22T12:53:45Z

There is 1 more integration test to fix, which I will do tomorrow

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-22T13:10:24Z

LGTM overall, but the test requires a fix

### Comment by [@LarsSven](https://github.com/LarsSven) — 2025-09-22T14:12:39Z

All tests should be fixed now

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-22T14:36:31Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6906#" title="Author self-approved">LarsSven</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6906#pullrequestreview-3253219221" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-22T14:36:31Z

LGTM label has been added.  <details>Git tree hash: c2f76bf4f76fa0ba8619c4d8147c45e5540b6705</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-22T14:39:46Z

/release-note-edit
```release-note
The following limits for ClusterQueue quota specification have been updated:
- the number of Flavors per ResourceGroup is increased from 16 to 64
- the number of Resources within a Flavor is increased from 16 to 64

We also provide the following additional limits:
- the total number of Flavors across all ResourceGroups is <= 256
- the total number of Resources across all ResourceGroups is <= 256
- the total number of (Flavor, Resource) pairs across within a ResourceGroup is <= 512
```
cc @LarsSven ptal if accurate

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-22T14:40:41Z

/release-note-edit
```release-note
The following limits for ClusterQueue quota specification have been relaxed:
- the number of Flavors per ResourceGroup is increased from 16 to 64
- the number of Resources per Flavor, within a ResourceGroup, is increased from 16 to 64

We also provide the following additional limits:
- the total number of Flavors across all ResourceGroups is <= 256
- the total number of Resources across all ResourceGroups is <= 256
- the total number of (Flavor, Resource) pairs across within a ResourceGroup is <= 512
```

### Comment by [@LarsSven](https://github.com/LarsSven) — 2025-09-22T14:41:50Z

Yes that's accurate, though the last line is not grammatically correct (across within).

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-22T14:43:22Z

Thank you, adjusted.

/release-note-edit
```release-note
The following limits for ClusterQueue quota specification have been relaxed:
- the number of Flavors per ResourceGroup is increased from 16 to 64
- the number of Resources per Flavor, within a ResourceGroup, is increased from 16 to 64

We also provide the following additional limits:
- the total number of Flavors across all ResourceGroups is <= 256
- the total number of Resources across all ResourceGroups is <= 256
- the total number of (Flavor, Resource) pairs within a ResourceGroup is <= 512
```

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-18T10:04:10Z

- **pkg/webhooks/clusterqueue_webhook_test.go:429** by [@mimowo](https://github.com/mimowo) — 2025-09-18T10:04:10Z
```diff
@@ -369,3 +371,66 @@ func TestValidateClusterQueueUpdate(t *testing.T) {
 		})
 	}
 }
+
+func makeFlavor(name string) kueue.FlavorQuotas {
+	return kueue.FlavorQuotas{
+		Name: kueue.ResourceFlavorReference(name),
+		Resources: []kueue.ResourceQuota{{
+			Name:         corev1.ResourceCPU,
+			NominalQuota: resource.MustParse("1"),
+		}},
+	}
+}
+
+func TestValidateTotalFlavors(t *testing.T) {
+	// Construct >256 flavors
+	var flavors []kueue.FlavorQuotas
+	for i := 0; i < 257; i++ {
+		flavors = append(flavors, makeFlavor("f"+string(rune('a'+(i%26)))+string(rune('A'+(i/26)))))
+	}
+	cq := &kueue.ClusterQueue{
+		Spec: kueue.ClusterQueueSpec{
+			ResourceGroups: []kueue.ResourceGroup{{
+				CoveredResources: []corev1.ResourceName{corev1.ResourceCPU},
+				Flavors:          flavors,
+			}},
+		},
+	}
+
+	errs := ValidateClusterQueue(cq)
+	if len(errs) == 0 {
+		t.Fatalf("expected error for >256 flavors, got none")
+	}
+	found := false
+	for _, e := range errs {
+		if e.Type == field.ErrorTypeInvalid && 
+		strings.Contains(e.Error(), "total number of flavors across all resourceGroups") {
+			found = true
+			break
+		}
+	}
+	if !found {
+		t.Errorf("expected invalid error about total flavors, got %v", errs)
+	}
+}
+
+func TestValidateTotalFlavorsWithinLimit(t *testing.T) {
+	// Construct 10 flavors
+	var flavors []kueue.FlavorQuotas
+	for i := 0; i < 10; i++ {
+		flavors = append(flavors, makeFlavor("f"+string(rune('a'+i))))
+	}
+	cq := &kueue.ClusterQueue{
+		Spec: kueue.ClusterQueueSpec{
+			ResourceGroups: []kueue.ResourceGroup{{
+				CoveredResources: []corev1.ResourceName{corev1.ResourceCPU},
+				Flavors:          flavors,
+			}},
+		},
```

  nit, can we use Wrappers (builders) for the objects, I know it isn't decreasing the amount of code much here, but would be useful for consistency

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-18T10:04:35Z

- **pkg/webhooks/clusterqueue_webhook_test.go:388** by [@mimowo](https://github.com/mimowo) — 2025-09-18T10:04:35Z
```diff
@@ -369,3 +371,66 @@ func TestValidateClusterQueueUpdate(t *testing.T) {
 		})
 	}
 }
+
+func makeFlavor(name string) kueue.FlavorQuotas {
+	return kueue.FlavorQuotas{
+		Name: kueue.ResourceFlavorReference(name),
+		Resources: []kueue.ResourceQuota{{
+			Name:         corev1.ResourceCPU,
+			NominalQuota: resource.MustParse("1"),
+		}},
+	}
+}
+
+func TestValidateTotalFlavors(t *testing.T) {
+	// Construct >256 flavors
+	var flavors []kueue.FlavorQuotas
+	for i := 0; i < 257; i++ {
```

  ```suggestion
  	for i := range 257 {
  ```
  I think, to make linter happy

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-18T10:05:33Z

- **pkg/webhooks/clusterqueue_webhook_test.go:417** by [@mimowo](https://github.com/mimowo) — 2025-09-18T10:05:33Z
```diff
@@ -369,3 +371,66 @@ func TestValidateClusterQueueUpdate(t *testing.T) {
 		})
 	}
 }
+
+func makeFlavor(name string) kueue.FlavorQuotas {
+	return kueue.FlavorQuotas{
+		Name: kueue.ResourceFlavorReference(name),
+		Resources: []kueue.ResourceQuota{{
+			Name:         corev1.ResourceCPU,
+			NominalQuota: resource.MustParse("1"),
+		}},
+	}
+}
+
+func TestValidateTotalFlavors(t *testing.T) {
+	// Construct >256 flavors
+	var flavors []kueue.FlavorQuotas
+	for i := 0; i < 257; i++ {
+		flavors = append(flavors, makeFlavor("f"+string(rune('a'+(i%26)))+string(rune('A'+(i/26)))))
+	}
+	cq := &kueue.ClusterQueue{
+		Spec: kueue.ClusterQueueSpec{
+			ResourceGroups: []kueue.ResourceGroup{{
+				CoveredResources: []corev1.ResourceName{corev1.ResourceCPU},
+				Flavors:          flavors,
+			}},
+		},
+	}
+
+	errs := ValidateClusterQueue(cq)
+	if len(errs) == 0 {
+		t.Fatalf("expected error for >256 flavors, got none")
+	}
+	found := false
+	for _, e := range errs {
+		if e.Type == field.ErrorTypeInvalid && 
+		strings.Contains(e.Error(), "total number of flavors across all resourceGroups") {
+			found = true
+			break
+		}
+	}
+	if !found {
+		t.Errorf("expected invalid error about total flavors, got %v", errs)
+	}
+}
+
+func TestValidateTotalFlavorsWithinLimit(t *testing.T) {
```

  nit, instead of adding two tests for exceeding the threshold, and below threashold add one test (or exend the existing), and use the "test cases" pattern.

### Review by [@LarsSven](https://github.com/LarsSven) (commented) — 2025-09-18T11:04:49Z

- **pkg/webhooks/clusterqueue_webhook_test.go:429** by [@LarsSven](https://github.com/LarsSven) — 2025-09-18T11:04:49Z
```diff
@@ -369,3 +371,66 @@ func TestValidateClusterQueueUpdate(t *testing.T) {
 		})
 	}
 }
+
+func makeFlavor(name string) kueue.FlavorQuotas {
+	return kueue.FlavorQuotas{
+		Name: kueue.ResourceFlavorReference(name),
+		Resources: []kueue.ResourceQuota{{
+			Name:         corev1.ResourceCPU,
+			NominalQuota: resource.MustParse("1"),
+		}},
+	}
+}
+
+func TestValidateTotalFlavors(t *testing.T) {
+	// Construct >256 flavors
+	var flavors []kueue.FlavorQuotas
+	for i := 0; i < 257; i++ {
+		flavors = append(flavors, makeFlavor("f"+string(rune('a'+(i%26)))+string(rune('A'+(i/26)))))
+	}
+	cq := &kueue.ClusterQueue{
+		Spec: kueue.ClusterQueueSpec{
+			ResourceGroups: []kueue.ResourceGroup{{
+				CoveredResources: []corev1.ResourceName{corev1.ResourceCPU},
+				Flavors:          flavors,
+			}},
+		},
+	}
+
+	errs := ValidateClusterQueue(cq)
+	if len(errs) == 0 {
+		t.Fatalf("expected error for >256 flavors, got none")
+	}
+	found := false
+	for _, e := range errs {
+		if e.Type == field.ErrorTypeInvalid && 
+		strings.Contains(e.Error(), "total number of flavors across all resourceGroups") {
+			found = true
+			break
+		}
+	}
+	if !found {
+		t.Errorf("expected invalid error about total flavors, got %v", errs)
+	}
+}
+
+func TestValidateTotalFlavorsWithinLimit(t *testing.T) {
+	// Construct 10 flavors
+	var flavors []kueue.FlavorQuotas
+	for i := 0; i < 10; i++ {
+		flavors = append(flavors, makeFlavor("f"+string(rune('a'+i))))
+	}
+	cq := &kueue.ClusterQueue{
+		Spec: kueue.ClusterQueueSpec{
+			ResourceGroups: []kueue.ResourceGroup{{
+				CoveredResources: []corev1.ResourceName{corev1.ResourceCPU},
+				Flavors:          flavors,
+			}},
+		},
```

  It actually helped a bit in terms of size

### Review by [@LarsSven](https://github.com/LarsSven) (commented) — 2025-09-18T11:04:58Z

- **pkg/webhooks/clusterqueue_webhook_test.go:417** by [@LarsSven](https://github.com/LarsSven) — 2025-09-18T11:04:58Z
```diff
@@ -369,3 +371,66 @@ func TestValidateClusterQueueUpdate(t *testing.T) {
 		})
 	}
 }
+
+func makeFlavor(name string) kueue.FlavorQuotas {
+	return kueue.FlavorQuotas{
+		Name: kueue.ResourceFlavorReference(name),
+		Resources: []kueue.ResourceQuota{{
+			Name:         corev1.ResourceCPU,
+			NominalQuota: resource.MustParse("1"),
+		}},
+	}
+}
+
+func TestValidateTotalFlavors(t *testing.T) {
+	// Construct >256 flavors
+	var flavors []kueue.FlavorQuotas
+	for i := 0; i < 257; i++ {
+		flavors = append(flavors, makeFlavor("f"+string(rune('a'+(i%26)))+string(rune('A'+(i/26)))))
+	}
+	cq := &kueue.ClusterQueue{
+		Spec: kueue.ClusterQueueSpec{
+			ResourceGroups: []kueue.ResourceGroup{{
+				CoveredResources: []corev1.ResourceName{corev1.ResourceCPU},
+				Flavors:          flavors,
+			}},
+		},
+	}
+
+	errs := ValidateClusterQueue(cq)
+	if len(errs) == 0 {
+		t.Fatalf("expected error for >256 flavors, got none")
+	}
+	found := false
+	for _, e := range errs {
+		if e.Type == field.ErrorTypeInvalid && 
+		strings.Contains(e.Error(), "total number of flavors across all resourceGroups") {
+			found = true
+			break
+		}
+	}
+	if !found {
+		t.Errorf("expected invalid error about total flavors, got %v", errs)
+	}
+}
+
+func TestValidateTotalFlavorsWithinLimit(t *testing.T) {
```

  Incorporated the pattern

### Review by [@LarsSven](https://github.com/LarsSven) (commented) — 2025-09-18T11:05:08Z

- **pkg/webhooks/clusterqueue_webhook_test.go:388** by [@LarsSven](https://github.com/LarsSven) — 2025-09-18T11:05:08Z
```diff
@@ -369,3 +371,66 @@ func TestValidateClusterQueueUpdate(t *testing.T) {
 		})
 	}
 }
+
+func makeFlavor(name string) kueue.FlavorQuotas {
+	return kueue.FlavorQuotas{
+		Name: kueue.ResourceFlavorReference(name),
+		Resources: []kueue.ResourceQuota{{
+			Name:         corev1.ResourceCPU,
+			NominalQuota: resource.MustParse("1"),
+		}},
+	}
+}
+
+func TestValidateTotalFlavors(t *testing.T) {
+	// Construct >256 flavors
+	var flavors []kueue.FlavorQuotas
+	for i := 0; i < 257; i++ {
```

  Implemented that too :)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-18T12:19:01Z

- **pkg/webhooks/clusterqueue_webhook_test.go:34** by [@mimowo](https://github.com/mimowo) — 2025-09-18T12:19:01Z
```diff
@@ -29,6 +29,9 @@ import (
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/features"
 	testingutil "sigs.k8s.io/kueue/pkg/util/testing"
+	"k8s.io/apimachinery/pkg/api/resource"
+	"strings"
+	"fmt"
```

  You probably need to move them up to make linter happy

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-18T12:20:40Z

- **pkg/webhooks/clusterqueue_webhook_test.go:420** by [@mimowo](https://github.com/mimowo) — 2025-09-18T12:20:40Z
```diff
@@ -369,3 +372,52 @@ func TestValidateClusterQueueUpdate(t *testing.T) {
 		})
 	}
 }
+
+func makeFlavors(n int) []kueue.FlavorQuotas {
+	flavs := make([]kueue.FlavorQuotas, 0, n)
+	for i := range n {
+		flavs = append(flavs, kueue.FlavorQuotas{
+			Name: kueue.ResourceFlavorReference(fmt.Sprintf("f%03d", i)),
+			Resources: []kueue.ResourceQuota{{
+				Name:         corev1.ResourceCPU,
+				NominalQuota: resource.MustParse("1"),
+			}},
+		})
+	}
+	return flavs
+}
+
+func TestValidateTotalFlavors(t *testing.T) {
+	testcases := []struct {
+		name       string
+		numFlavors int
+		wantErr    bool
+	}{
+		{"within limit (10 flavors)", 10, false},
+		{"At limit (256 flavors)", 256, false},
+		{"over limit (257 flavors)", 257, true},
+	}
+
+	for _, tc := range testcases {
+		t.Run(tc.name, func(t *testing.T) {
+			cq := testingutil.MakeClusterQueue("cluster-queue").ResourceGroup(makeFlavors(tc.numFlavors)...).Obj()
+
+			errs := ValidateClusterQueue(cq)
+
+			if tc.wantErr {
+				if len(errs) == 0 {
+					t.Fatalf("expected error for >256 flavors, got none")
+				}
+				for _, e := range errs {
+					if e.Type == field.ErrorTypeInvalid &&
+						strings.Contains(e.Error(), "total number of flavors across all resourceGroups") {
+						return // success
+					}
+				}
+				t.Errorf("expected invalid error about total flavors, got %v", errs)
+			} else if len(errs) != 0 {
+				t.Errorf("expected no error, got %v", errs)
+			}
```

  Please assert errors like here: https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/validation_test.go#L327-L330
  
  Less code, and more consistent with codebase

### Review by [@LarsSven](https://github.com/LarsSven) (commented) — 2025-09-18T12:50:26Z

- **pkg/webhooks/clusterqueue_webhook_test.go:420** by [@LarsSven](https://github.com/LarsSven) — 2025-09-18T12:50:26Z
```diff
@@ -369,3 +372,52 @@ func TestValidateClusterQueueUpdate(t *testing.T) {
 		})
 	}
 }
+
+func makeFlavors(n int) []kueue.FlavorQuotas {
+	flavs := make([]kueue.FlavorQuotas, 0, n)
+	for i := range n {
+		flavs = append(flavs, kueue.FlavorQuotas{
+			Name: kueue.ResourceFlavorReference(fmt.Sprintf("f%03d", i)),
+			Resources: []kueue.ResourceQuota{{
+				Name:         corev1.ResourceCPU,
+				NominalQuota: resource.MustParse("1"),
+			}},
+		})
+	}
+	return flavs
+}
+
+func TestValidateTotalFlavors(t *testing.T) {
+	testcases := []struct {
+		name       string
+		numFlavors int
+		wantErr    bool
+	}{
+		{"within limit (10 flavors)", 10, false},
+		{"At limit (256 flavors)", 256, false},
+		{"over limit (257 flavors)", 257, true},
+	}
+
+	for _, tc := range testcases {
+		t.Run(tc.name, func(t *testing.T) {
+			cq := testingutil.MakeClusterQueue("cluster-queue").ResourceGroup(makeFlavors(tc.numFlavors)...).Obj()
+
+			errs := ValidateClusterQueue(cq)
+
+			if tc.wantErr {
+				if len(errs) == 0 {
+					t.Fatalf("expected error for >256 flavors, got none")
+				}
+				for _, e := range errs {
+					if e.Type == field.ErrorTypeInvalid &&
+						strings.Contains(e.Error(), "total number of flavors across all resourceGroups") {
+						return // success
+					}
+				}
+				t.Errorf("expected invalid error about total flavors, got %v", errs)
+			} else if len(errs) != 0 {
+				t.Errorf("expected no error, got %v", errs)
+			}
```

  That's indeed a lot smaller, thanks!

### Review by [@LarsSven](https://github.com/LarsSven) (commented) — 2025-09-18T12:50:47Z

- **pkg/webhooks/clusterqueue_webhook_test.go:34** by [@LarsSven](https://github.com/LarsSven) — 2025-09-18T12:50:47Z
```diff
@@ -29,6 +29,9 @@ import (
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/features"
 	testingutil "sigs.k8s.io/kueue/pkg/util/testing"
+	"k8s.io/apimachinery/pkg/api/resource"
+	"strings"
+	"fmt"
```

  I ran `go fmt`
  
  It's been some time since I used Go, so forgot that that's a thing.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-18T12:52:40Z

- **pkg/webhooks/clusterqueue_webhook_test.go:29** by [@mimowo](https://github.com/mimowo) — 2025-09-18T12:52:40Z
```diff
@@ -26,6 +26,8 @@ import (
 	"k8s.io/apimachinery/pkg/util/validation/field"
 	"k8s.io/utils/ptr"
 
+	"fmt"
```

  Yeah, and this must go up I think the linter will say

### Review by [@LarsSven](https://github.com/LarsSven) (commented) — 2025-09-18T12:56:23Z

- **pkg/webhooks/clusterqueue_webhook_test.go:29** by [@LarsSven](https://github.com/LarsSven) — 2025-09-18T12:56:22Z
```diff
@@ -26,6 +26,8 @@ import (
 	"k8s.io/apimachinery/pkg/util/validation/field"
 	"k8s.io/utils/ptr"
 
+	"fmt"
```

  Doesn't the linter just check the same style as `go fmt`?, because this file was go fmt'ed before I committed it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-18T13:02:10Z

- **pkg/webhooks/clusterqueue_webhook_test.go:29** by [@mimowo](https://github.com/mimowo) — 2025-09-18T13:02:09Z
```diff
@@ -26,6 +26,8 @@ import (
 	"k8s.io/apimachinery/pkg/util/validation/field"
 	"k8s.io/utils/ptr"
 
+	"fmt"
```

  I think we may have more extra checks by linter.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-18T13:03:00Z

- **pkg/webhooks/clusterqueue_webhook_test.go:29** by [@mimowo](https://github.com/mimowo) — 2025-09-18T13:02:59Z
```diff
@@ -26,6 +26,8 @@ import (
 	"k8s.io/apimachinery/pkg/util/validation/field"
 	"k8s.io/utils/ptr"
 
+	"fmt"
```

  FIY we have some custom config here: https://github.com/kubernetes-sigs/kueue/blob/main/.golangci.yaml
  
  I'm not totally sure this config is used / respected if you call just go fmt, I would suppose not.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-18T13:04:04Z

- **pkg/webhooks/clusterqueue_webhook_test.go:29** by [@mimowo](https://github.com/mimowo) — 2025-09-18T13:04:04Z
```diff
@@ -26,6 +26,8 @@ import (
 	"k8s.io/apimachinery/pkg/util/validation/field"
 	"k8s.io/utils/ptr"
 
+	"fmt"
```

  Yes, it is an annoying beast, but helps to keep the code consistent :)

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-09-18T13:22:53Z

- **pkg/webhooks/clusterqueue_webhook_test.go:29** by [@kannon92](https://github.com/kannon92) — 2025-09-18T13:22:53Z
```diff
@@ -26,6 +26,8 @@ import (
 	"k8s.io/apimachinery/pkg/util/validation/field"
 	"k8s.io/utils/ptr"
 
+	"fmt"
```

  The verify job is failing due to this.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-09-18T13:23:42Z

- **pkg/webhooks/clusterqueue_webhook_test.go:29** by [@kannon92](https://github.com/kannon92) — 2025-09-18T13:23:42Z
```diff
@@ -26,6 +26,8 @@ import (
 	"k8s.io/apimachinery/pkg/util/validation/field"
 	"k8s.io/utils/ptr"
 
+	"fmt"
```

  You can run `make lint-fix` to format with golangci settings.

### Review by [@LarsSven](https://github.com/LarsSven) (commented) — 2025-09-18T13:45:56Z

- **pkg/webhooks/clusterqueue_webhook_test.go:29** by [@LarsSven](https://github.com/LarsSven) — 2025-09-18T13:45:56Z
```diff
@@ -26,6 +26,8 @@ import (
 	"k8s.io/apimachinery/pkg/util/validation/field"
 	"k8s.io/utils/ptr"
 
+	"fmt"
```

  I ran `lint-fix`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-18T13:51:08Z

- **apis/kueue/v1beta1/clusterqueue_types.go:64** by [@mimowo](https://github.com/mimowo) — 2025-09-18T13:51:07Z
```diff
@@ -59,9 +59,9 @@ type ClusterQueueSpec struct {
 	// Each resource group defines the list of resources and a list of flavors
 	// that provide quotas for these resources.
 	// Each resource and each flavor can only form part of one resource group.
-	// resourceGroups can be up to 16.
+	// resourceGroups can be up to 64, with a max of 256 total flavors across all groups.
 	// +listType=atomic
-	// +kubebuilder:validation:MaxItems=16
+	// +kubebuilder:validation:MaxItems=64
```

  Hold on, we probably don't need to relax here, right?
  
  If no, please revert.

### Review by [@LarsSven](https://github.com/LarsSven) (commented) — 2025-09-18T14:11:04Z

- **apis/kueue/v1beta1/clusterqueue_types.go:64** by [@LarsSven](https://github.com/LarsSven) — 2025-09-18T14:11:04Z
```diff
@@ -59,9 +59,9 @@ type ClusterQueueSpec struct {
 	// Each resource group defines the list of resources and a list of flavors
 	// that provide quotas for these resources.
 	// Each resource and each flavor can only form part of one resource group.
-	// resourceGroups can be up to 16.
+	// resourceGroups can be up to 64, with a max of 256 total flavors across all groups.
 	// +listType=atomic
-	// +kubebuilder:validation:MaxItems=16
+	// +kubebuilder:validation:MaxItems=64
```

  Ah, I had assumed you wanted to relax both. I don't immediately need more here no.

### Review by [@LarsSven](https://github.com/LarsSven) (commented) — 2025-09-18T14:12:34Z

- **apis/kueue/v1beta1/clusterqueue_types.go:64** by [@LarsSven](https://github.com/LarsSven) — 2025-09-18T14:12:33Z
```diff
@@ -59,9 +59,9 @@ type ClusterQueueSpec struct {
 	// Each resource group defines the list of resources and a list of flavors
 	// that provide quotas for these resources.
 	// Each resource and each flavor can only form part of one resource group.
-	// resourceGroups can be up to 16.
+	// resourceGroups can be up to 64, with a max of 256 total flavors across all groups.
 	// +listType=atomic
-	// +kubebuilder:validation:MaxItems=16
+	// +kubebuilder:validation:MaxItems=64
```

  I reverted that relaxation

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-09-18T14:41:29Z

- **apis/kueue/v1beta1/clusterqueue_types.go:62** by [@kannon92](https://github.com/kannon92) — 2025-09-18T14:41:28Z
```diff
@@ -59,7 +59,7 @@ type ClusterQueueSpec struct {
 	// Each resource group defines the list of resources and a list of flavors
 	// that provide quotas for these resources.
 	// Each resource and each flavor can only form part of one resource group.
-	// resourceGroups can be up to 16.
+	// resourceGroups can be up to 16, with a max of 256 total flavors across all groups.
```

  You may need to generate a new crd (ie `make generate manifests`).

### Review by [@LarsSven](https://github.com/LarsSven) (commented) — 2025-09-18T14:43:14Z

- **apis/kueue/v1beta1/clusterqueue_types.go:62** by [@LarsSven](https://github.com/LarsSven) — 2025-09-18T14:43:14Z
```diff
@@ -59,7 +59,7 @@ type ClusterQueueSpec struct {
 	// Each resource group defines the list of resources and a list of flavors
 	// that provide quotas for these resources.
 	// Each resource and each flavor can only form part of one resource group.
-	// resourceGroups can be up to 16.
+	// resourceGroups can be up to 16, with a max of 256 total flavors across all groups.
```

  I generated them

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-22T14:36:23Z

Thank you 👍 
/lgtm
/approve
