# PR #6942: Handle node replacement for slices

**Summary**: Handle node replacement for slices

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6942

**Last updated**: 2025-09-24T13:58:43Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@pajakd](https://github.com/pajakd)
- **Branch**: `hotswap_slices` → `main`
- **Created**: 2025-09-22T11:28:09Z
- **Updated**: 2025-09-24T13:58:43Z
- **Merged**: 2025-09-24T07:42:15Z
- **Closed**: 2025-09-24T07:42:15Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi), [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 2   **+309 / -8**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
This PR has the correct handling of node replacement in the case of slices. 

It solves two cases:

1. If the failed node was hosting a part of some slice, it tries to find the replacement within the same slice domain.
2. If the failed node was hosting a full slice (or a number of slices), it searches for the replacement in the `Required` domain or in the whole topology. 

If the search starts at the slice level (Case 1) it uses sliceSize = 1 because we are not assigning slices any more but individual pods to nodes.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
TAS: Node Hot Swap allows replacing a node for workloads using PodSet slices, 
ie. when the `kueue.x-k8s.io/podset-slice-size` annotation is used.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-22T11:28:12Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-09-22T11:28:15Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 487d0213bd5af0a1fcdccfc1b87272dfa5165d44 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68d38ec7c9a68a0008efebf1 |

### Comment by [@pajakd](https://github.com/pajakd) — 2025-09-22T11:58:04Z

/test all

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-22T12:27:08Z

Does this imply that the current node host swap mechanism does not support PodSet slice?

### Comment by [@pajakd](https://github.com/pajakd) — 2025-09-22T12:48:29Z

> Does this imply that the current node host swap mechanism does not support PodSet slice?

Currently hot swap does not work with slices. This PR is intended to make sure that these two features work together.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-22T13:15:04Z

> > Does this imply that the current node host swap mechanism does not support PodSet slice?
> 
> Currently hot swap does not work with slices. This PR is intended to make sure that these two features work together.

Thank you for describing that. In that case, we should include this one in the next v0.14 since the PodSet slice is not guarded by dedicated FG, which is guarded by TopologyAwareScheduling FG. That means PodSet slice will be enabled by default after v0.14.

What about a combination of TAS hot swap and PodSet group?

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-09-22T13:50:20Z

> Does this imply that the current node host swap mechanism does not support PodSet slice?

Currently we correctly support case when there's no replacement possible, but we would also like to support the case when it is actually possible. This is what this PR introduces

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-22T13:56:57Z

> > Does this imply that the current node host swap mechanism does not support PodSet slice?
> 
> Currently we correctly support case when there's no replacement possible, but we would also like to support the case when it is actually possible. This is what this PR introduces

Yes, the problem is TAS and TAS hot swap will be enabled by default, but TAS PodSet slice is not correctly handled by TAS hot swap and we do not have any way to disable TAS PodSet slice. So, I would like to ship this PR as part of the coming new release.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-23T07:35:53Z

/release-note-edit
```release-note
TAS: Node Hot Swap allows replacing a node for workloads using PodSet slices, 
ie. when the `kueue.x-k8s.io/podset-slice-size` annotation is used.
```

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-09-23T10:25:15Z

LGTM on the current changes, great work!
Can we also add some integration/e2e tests that cover basic scenarios of hotswap+TAS slices?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-23T10:50:16Z

This is not a blocker, but let me ask again. What about the following question?

> What about a combination of TAS hot swap and PodSet group?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-23T10:53:33Z

> Can we also add some integration/e2e tests that cover basic scenarios of hotswap+TAS slices?

I think both are useful but I wouldnt couple to this  PR. We can cover scheduling changes with unit.

Long term integration and e2e, but they should have been added regardless of the bug even

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-23T10:56:55Z

> What about a combination of TAS hot swap and PodSet group?

Yeah Im not sure. It may not work well of the leader fails. Still I wouldnt couple to this PR as it is already complex. I think k we should fix bugs one by one as we are identifying them.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-23T11:36:26Z

> > What about a combination of TAS hot swap and PodSet group?
> 
> Yeah Im not sure. It may not work well of the leader fails. Still I wouldnt couple to this PR as it is already complex. I think k we should fix bugs one by one as we are identifying them.

That makes sense. Should we open an issue?

### Comment by [@pajakd](https://github.com/pajakd) — 2025-09-23T11:38:00Z

1. about PodSet group. I think LeaderWorkerSet (#5845) might not work at the moment with hotswap (if using slices).  EDIT: or rather hotswap might not work for LeaderWorkerSet
2. I'm happy to work on the e2e test for this.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-23T11:44:50Z

> 1. about PodSet group. I think LeaderWorkerSet ([Co-locate leader with workers in LeaderWorkerSet #5845](https://github.com/kubernetes-sigs/kueue/pull/5845)) might not work at the moment with hotswap (if using slices).  EDIT: or rather hotswap might not work for LeaderWorkerSet
> 2. I'm happy to work on the e2e test for this.

Thank you for quickly investigating that. Let us continue that in https://github.com/kubernetes-sigs/kueue/issues/6957

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-23T12:46:21Z

Promoting LGTM from @PBundyra on https://github.com/kubernetes-sigs/kueue/pull/6942#issuecomment-3323375911

Thank you 👍 
/lgtm
/approve
/cherrypick release-0.13

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-09-23T12:46:24Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.13` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6942#issuecomment-3323868805):

>Promoting LGTM from @PBundyra on https://github.com/kubernetes-sigs/kueue/pull/6942#issuecomment-3323375911
>
>Thank you 👍 
>/lgtm
>/approve
>/cherrypick release-0.13


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-23T12:46:29Z

LGTM label has been added.  <details>Git tree hash: 4b23e8c0f5d9f6fa313baef2437648062defaee8</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-23T12:46:32Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6942#issuecomment-3323868805" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6942#" title="Author self-approved">pajakd</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-23T13:05:17Z

/hold
to address

### Comment by [@pajakd](https://github.com/pajakd) — 2025-09-23T17:05:26Z

/test pull-kueue-test-integration-baseline-main

this seems to be a known issue (eg https://github.com/kubernetes-sigs/kueue/pull/6944#issuecomment-3323941073)
```
There were failures detected in the following suites:
  fairsharing ./test/integration/singlecluster/scheduler/fairsharing

Test Suite Failed
make: *** [Makefile-test.mk:72: test-integration] Error 1
```

### Comment by [@pajakd](https://github.com/pajakd) — 2025-09-23T17:25:25Z

/test pull-kueue-test-integration-baseline-main

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-24T07:05:54Z

/unhold
I believe all comments are addressed now.  Thank you folks 👍 
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-24T07:06:01Z

LGTM label has been added.  <details>Git tree hash: 0b96d8a24d0e5555cadda06a703621968947ad51</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-09-24T07:42:57Z

@mimowo: new pull request created: #6979

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6942#issuecomment-3323868805):

>Promoting LGTM from @PBundyra on https://github.com/kubernetes-sigs/kueue/pull/6942#issuecomment-3323375911
>
>Thank you 👍 
>/lgtm
>/approve
>/cherrypick release-0.13


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-22T11:33:54Z

- **pkg/scheduler/scheduler_test.go:5732** by [@mimowo](https://github.com/mimowo) — 2025-09-22T11:33:54Z
```diff
@@ -5687,6 +5687,179 @@ func TestScheduleForTAS(t *testing.T) {
 					Obj(),
 			},
 		},
+		"workload with unhealthyNode; second pass; slices; can find replacement in slice domain": {
+			nodes:           defaultNodes,
+			admissionChecks: []kueue.AdmissionCheck{defaultProvCheck},
+			topologies:      []kueue.Topology{defaultThreeLevelTopology},
+			resourceFlavors: []kueue.ResourceFlavor{defaultTASThreeLevelFlavor},
+			clusterQueues:   []kueue.ClusterQueue{defaultClusterQueue},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("foo", "default").
+					UnhealthyNodes("x2").
+					Queue("tas-main").
+					PodSets(*utiltesting.MakePodSet("one", 2).
+						PreferredTopologyRequest(tasBlockLabel).
+						SliceSizeTopologyRequest(2).
+						SliceRequiredTopologyRequest(tasRackLabel).
+						Request(corev1.ResourceCPU, "1").
+						Obj()).
+					ReserveQuota(
+						utiltesting.MakeAdmission("tas-main").
+							PodSets(utiltesting.MakePodSetAssignment("one").Count(2).
+								Assignment(corev1.ResourceCPU, "tas-default", "2000m").
+								TopologyAssignment(utiltesting.MakeTopologyAssignment(utiltas.Levels(&defaultSingleLevelTopology)).
+									Domain(utiltesting.MakeTopologyDomainAssignment([]string{"x2"}, 1).Obj()).
+									Domain(utiltesting.MakeTopologyDomainAssignment([]string{"x3"}, 1).Obj()).
+									Obj()).
+								Obj()).
+							Obj(),
+					).
+					Admitted(true).
+					Obj(),
+			},
+			wantNewAssignments: map[workload.Reference]kueue.Admission{
+				"default/foo": *utiltesting.MakeAdmission("tas-main").
+					PodSets(utiltesting.MakePodSetAssignment("one").Count(2).
+						Assignment(corev1.ResourceCPU, "tas-default", "2000m").
+						TopologyAssignment(utiltesting.MakeTopologyAssignment(utiltas.Levels(&defaultSingleLevelTopology)).
+							Domain(utiltesting.MakeTopologyDomainAssignment([]string{"x3"}, 1).Obj()).
+							Domain(utiltesting.MakeTopologyDomainAssignment([]string{"x4"}, 1).Obj()).
+							Obj()).
+						Obj()).
+					Obj(),
+			},
+		},
+		"workload with unhealthyNode; second pass; slices; cannot find replacement for slice": {
```

  Is it substantially different that the test already added in https://github.com/kubernetes-sigs/kueue/pull/6914?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-22T11:36:43Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:517** by [@mimowo](https://github.com/mimowo) — 2025-09-22T11:36:43Z
```diff
@@ -514,7 +514,12 @@ func (s *TASFlavorSnapshot) findReplacementAssignment(tr *TASPodSetRequests, exi
 		return nil, nil, fmt.Sprintf("Cannot replace the node, because the existing topologyAssignment is invalid, as it contains the stale domain %v", staleDomain)
 	}
 	requiredReplacementDomain := s.requiredReplacementDomain(tr, existingAssignment)
-	replacementAssignment, reason := s.findTopologyAssignment(*tr, nil, assumedUsage, false, requiredReplacementDomain)
+	tr_copy := *tr
```

  https://go.dev/doc/effective_go#mixed-caps
  
  "Finally, the convention in Go is to use MixedCaps or mixedCaps rather than underscores to write multiword names."

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-22T11:37:11Z

cc @PBundyra

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-09-22T11:58:25Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:517** by [@pajakd](https://github.com/pajakd) — 2025-09-22T11:58:25Z
```diff
@@ -514,7 +514,12 @@ func (s *TASFlavorSnapshot) findReplacementAssignment(tr *TASPodSetRequests, exi
 		return nil, nil, fmt.Sprintf("Cannot replace the node, because the existing topologyAssignment is invalid, as it contains the stale domain %v", staleDomain)
 	}
 	requiredReplacementDomain := s.requiredReplacementDomain(tr, existingAssignment)
-	replacementAssignment, reason := s.findTopologyAssignment(*tr, nil, assumedUsage, false, requiredReplacementDomain)
+	tr_copy := *tr
```

  Done.

### Review by [@PBundyra](https://github.com/PBundyra) (changes_requested) — 2025-09-22T11:59:23Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:517** by [@PBundyra](https://github.com/PBundyra) — 2025-09-22T11:44:14Z
```diff
@@ -514,7 +514,12 @@ func (s *TASFlavorSnapshot) findReplacementAssignment(tr *TASPodSetRequests, exi
 		return nil, nil, fmt.Sprintf("Cannot replace the node, because the existing topologyAssignment is invalid, as it contains the stale domain %v", staleDomain)
 	}
 	requiredReplacementDomain := s.requiredReplacementDomain(tr, existingAssignment)
-	replacementAssignment, reason := s.findTopologyAssignment(*tr, nil, assumedUsage, false, requiredReplacementDomain)
+	tr_copy := *tr
```

  Please use camelCase convention for naming

- **pkg/cache/scheduler/tas_flavor_snapshot.go:518** by [@PBundyra](https://github.com/PBundyra) — 2025-09-22T11:45:04Z
```diff
@@ -514,7 +514,12 @@ func (s *TASFlavorSnapshot) findReplacementAssignment(tr *TASPodSetRequests, exi
 		return nil, nil, fmt.Sprintf("Cannot replace the node, because the existing topologyAssignment is invalid, as it contains the stale domain %v", staleDomain)
 	}
 	requiredReplacementDomain := s.requiredReplacementDomain(tr, existingAssignment)
-	replacementAssignment, reason := s.findTopologyAssignment(*tr, nil, assumedUsage, false, requiredReplacementDomain)
+	tr_copy := *tr
+	if slicesConfigured(tr.PodSet.TopologyRequest) && requiredReplacementDomain != "" && tr.Count < *tr.PodSet.TopologyRequest.PodSetSliceSize {
```

  Why do we need to check if the count is less then PodSetSliceSize?

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1376** by [@PBundyra](https://github.com/PBundyra) — 2025-09-22T11:45:17Z
```diff
@@ -1357,3 +1372,7 @@ func (s *TASFlavorSnapshot) notFitMessage(slicesFitCount, totalRequestsSlicesCou
 	}
 	return fmt.Sprintf("topology %q allows to fit only %d out of %d slice(s)", s.topologyName, slicesFitCount, totalRequestsSlicesCount)
 }
+
+func slicesConfigured(tr *kueue.PodSetTopologyRequest) bool {
```

  ```suggestion
  func slicesRequested(tr *kueue.PodSetTopologyRequest) bool {
  ```

- **pkg/cache/scheduler/tas_flavor_snapshot.go:575** by [@PBundyra](https://github.com/PBundyra) — 2025-09-22T11:53:48Z
```diff
@@ -558,9 +562,19 @@ func (s *TASFlavorSnapshot) requiredReplacementDomain(tr *TASPodSetRequests, ta
 		return ""
 	}
 	required := isRequired(tr.PodSet.TopologyRequest)
-	if !required {
+	if !required && !slicesConfigured(tr.PodSet.TopologyRequest) {
 		return ""
 	}
+	targetLevel := levelIdx
+	if slicesConfigured(tr.PodSet.TopologyRequest) && tr.Count < *tr.PodSet.TopologyRequest.PodSetSliceSize {
+		sliceTopologyKey := s.sliceLevelKeyWithDefault(tr.PodSet.TopologyRequest, s.lowestLevel())
+		sliceLevelIdx, resolved := s.resolveLevelIdx(sliceTopologyKey)
+		if !resolved {
+			return ""
+		}
+		targetLevel = max(targetLevel, sliceLevelIdx)
```

  No need to max(levelIdx, sliceLevelIdx), we assume slice will be below required/preferred topologically. If that's not the case, then a different place will return an error

- **pkg/cache/scheduler/tas_flavor_snapshot.go:571** by [@PBundyra](https://github.com/PBundyra) — 2025-09-22T11:54:15Z
```diff
@@ -558,9 +562,19 @@ func (s *TASFlavorSnapshot) requiredReplacementDomain(tr *TASPodSetRequests, ta
 		return ""
 	}
 	required := isRequired(tr.PodSet.TopologyRequest)
-	if !required {
+	if !required && !slicesConfigured(tr.PodSet.TopologyRequest) {
 		return ""
 	}
+	targetLevel := levelIdx
+	if slicesConfigured(tr.PodSet.TopologyRequest) && tr.Count < *tr.PodSet.TopologyRequest.PodSetSliceSize {
+		sliceTopologyKey := s.sliceLevelKeyWithDefault(tr.PodSet.TopologyRequest, s.lowestLevel())
+		sliceLevelIdx, resolved := s.resolveLevelIdx(sliceTopologyKey)
```

  ```suggestion
  		sliceLevelIdx, found := s.resolveLevelIdx(sliceTopologyKey)
  ```

- **pkg/cache/scheduler/tas_flavor_snapshot.go:581** by [@PBundyra](https://github.com/PBundyra) — 2025-09-22T11:59:11Z
```diff
@@ -574,7 +588,8 @@ func (s *TASFlavorSnapshot) requiredReplacementDomain(tr *TASPodSetRequests, ta
 	nodeDomain := ta.Domains[0].Values[0]
```

  I think we need to be smarter about it when it comes to slices, as contrary to regular `required` TAS request where all domains belong to the same top domain, that's not the case for slices. We need to find out which slices is missing a node and pick it. In case no slice is missing any node, it means that the slice consisted of only node so we need to pick whichever, analogously to regular hot swap. Create a UT for that please

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-09-22T12:13:53Z

- **pkg/scheduler/scheduler_test.go:5732** by [@pajakd](https://github.com/pajakd) — 2025-09-22T12:13:53Z
```diff
@@ -5687,6 +5687,179 @@ func TestScheduleForTAS(t *testing.T) {
 					Obj(),
 			},
 		},
+		"workload with unhealthyNode; second pass; slices; can find replacement in slice domain": {
+			nodes:           defaultNodes,
+			admissionChecks: []kueue.AdmissionCheck{defaultProvCheck},
+			topologies:      []kueue.Topology{defaultThreeLevelTopology},
+			resourceFlavors: []kueue.ResourceFlavor{defaultTASThreeLevelFlavor},
+			clusterQueues:   []kueue.ClusterQueue{defaultClusterQueue},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("foo", "default").
+					UnhealthyNodes("x2").
+					Queue("tas-main").
+					PodSets(*utiltesting.MakePodSet("one", 2).
+						PreferredTopologyRequest(tasBlockLabel).
+						SliceSizeTopologyRequest(2).
+						SliceRequiredTopologyRequest(tasRackLabel).
+						Request(corev1.ResourceCPU, "1").
+						Obj()).
+					ReserveQuota(
+						utiltesting.MakeAdmission("tas-main").
+							PodSets(utiltesting.MakePodSetAssignment("one").Count(2).
+								Assignment(corev1.ResourceCPU, "tas-default", "2000m").
+								TopologyAssignment(utiltesting.MakeTopologyAssignment(utiltas.Levels(&defaultSingleLevelTopology)).
+									Domain(utiltesting.MakeTopologyDomainAssignment([]string{"x2"}, 1).Obj()).
+									Domain(utiltesting.MakeTopologyDomainAssignment([]string{"x3"}, 1).Obj()).
+									Obj()).
+								Obj()).
+							Obj(),
+					).
+					Admitted(true).
+					Obj(),
+			},
+			wantNewAssignments: map[workload.Reference]kueue.Admission{
+				"default/foo": *utiltesting.MakeAdmission("tas-main").
+					PodSets(utiltesting.MakePodSetAssignment("one").Count(2).
+						Assignment(corev1.ResourceCPU, "tas-default", "2000m").
+						TopologyAssignment(utiltesting.MakeTopologyAssignment(utiltas.Levels(&defaultSingleLevelTopology)).
+							Domain(utiltesting.MakeTopologyDomainAssignment([]string{"x3"}, 1).Obj()).
+							Domain(utiltesting.MakeTopologyDomainAssignment([]string{"x4"}, 1).Obj()).
+							Obj()).
+						Obj()).
+					Obj(),
+			},
+		},
+		"workload with unhealthyNode; second pass; slices; cannot find replacement for slice": {
```

  You are right, this would be a duplicate - removed

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-09-22T12:14:02Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:517** by [@pajakd](https://github.com/pajakd) — 2025-09-22T12:14:02Z
```diff
@@ -514,7 +514,12 @@ func (s *TASFlavorSnapshot) findReplacementAssignment(tr *TASPodSetRequests, exi
 		return nil, nil, fmt.Sprintf("Cannot replace the node, because the existing topologyAssignment is invalid, as it contains the stale domain %v", staleDomain)
 	}
 	requiredReplacementDomain := s.requiredReplacementDomain(tr, existingAssignment)
-	replacementAssignment, reason := s.findTopologyAssignment(*tr, nil, assumedUsage, false, requiredReplacementDomain)
+	tr_copy := *tr
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-09-22T12:28:57Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:518** by [@pajakd](https://github.com/pajakd) — 2025-09-22T12:28:57Z
```diff
@@ -514,7 +514,12 @@ func (s *TASFlavorSnapshot) findReplacementAssignment(tr *TASPodSetRequests, exi
 		return nil, nil, fmt.Sprintf("Cannot replace the node, because the existing topologyAssignment is invalid, as it contains the stale domain %v", staleDomain)
 	}
 	requiredReplacementDomain := s.requiredReplacementDomain(tr, existingAssignment)
-	replacementAssignment, reason := s.findTopologyAssignment(*tr, nil, assumedUsage, false, requiredReplacementDomain)
+	tr_copy := *tr
+	if slicesConfigured(tr.PodSet.TopologyRequest) && requiredReplacementDomain != "" && tr.Count < *tr.PodSet.TopologyRequest.PodSetSliceSize {
```

  If the number of pods that were running on the failed node is greater than the sliceSize then we don't have to place them in the same domain on the slice level (because its the whole slice or a number of slices).

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-09-22T14:42:56Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:518** by [@PBundyra](https://github.com/PBundyra) — 2025-09-22T14:42:56Z
```diff
@@ -514,7 +514,12 @@ func (s *TASFlavorSnapshot) findReplacementAssignment(tr *TASPodSetRequests, exi
 		return nil, nil, fmt.Sprintf("Cannot replace the node, because the existing topologyAssignment is invalid, as it contains the stale domain %v", staleDomain)
 	}
 	requiredReplacementDomain := s.requiredReplacementDomain(tr, existingAssignment)
-	replacementAssignment, reason := s.findTopologyAssignment(*tr, nil, assumedUsage, false, requiredReplacementDomain)
+	tr_copy := *tr
+	if slicesConfigured(tr.PodSet.TopologyRequest) && requiredReplacementDomain != "" && tr.Count < *tr.PodSet.TopologyRequest.PodSetSliceSize {
```

  I see, thank you

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-09-23T07:09:40Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:581** by [@pajakd](https://github.com/pajakd) — 2025-09-23T07:09:40Z
```diff
@@ -574,7 +588,8 @@ func (s *TASFlavorSnapshot) requiredReplacementDomain(tr *TASPodSetRequests, ta
 	nodeDomain := ta.Domains[0].Values[0]
```

  Implemented a function for the "find out which slices is missing a node" logic PTAL. Added a testcase `workload with unhealthyNode; second pass; slices; finds correct slice domain` to check that it is not taking the first domain but actually the one that is incomplete.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-09-23T07:10:06Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:575** by [@pajakd](https://github.com/pajakd) — 2025-09-23T07:10:06Z
```diff
@@ -558,9 +562,19 @@ func (s *TASFlavorSnapshot) requiredReplacementDomain(tr *TASPodSetRequests, ta
 		return ""
 	}
 	required := isRequired(tr.PodSet.TopologyRequest)
-	if !required {
+	if !required && !slicesConfigured(tr.PodSet.TopologyRequest) {
 		return ""
 	}
+	targetLevel := levelIdx
+	if slicesConfigured(tr.PodSet.TopologyRequest) && tr.Count < *tr.PodSet.TopologyRequest.PodSetSliceSize {
+		sliceTopologyKey := s.sliceLevelKeyWithDefault(tr.PodSet.TopologyRequest, s.lowestLevel())
+		sliceLevelIdx, resolved := s.resolveLevelIdx(sliceTopologyKey)
+		if !resolved {
+			return ""
+		}
+		targetLevel = max(targetLevel, sliceLevelIdx)
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-09-23T07:15:32Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:518** by [@pajakd](https://github.com/pajakd) — 2025-09-23T07:15:32Z
```diff
@@ -514,7 +514,12 @@ func (s *TASFlavorSnapshot) findReplacementAssignment(tr *TASPodSetRequests, exi
 		return nil, nil, fmt.Sprintf("Cannot replace the node, because the existing topologyAssignment is invalid, as it contains the stale domain %v", staleDomain)
 	}
 	requiredReplacementDomain := s.requiredReplacementDomain(tr, existingAssignment)
-	replacementAssignment, reason := s.findTopologyAssignment(*tr, nil, assumedUsage, false, requiredReplacementDomain)
+	tr_copy := *tr
+	if slicesConfigured(tr.PodSet.TopologyRequest) && requiredReplacementDomain != "" && tr.Count < *tr.PodSet.TopologyRequest.PodSetSliceSize {
```

  I was wrong. The correct condition should be count % sliceSize != 0 because:
  -  if the failed node was hosting (some number of) full slices then we use the original logic (with unchanged sliceSize because we allocate full slices)
  - if the failed node was hosting some incomplete slice then we have less freedom, we have to find the exact slice domain that "lost" this node and allocate only there (with sliceSize = 1 because at and below the slice level we allocate individual pods). Here I use the method as outlined in your comment below (https://github.com/kubernetes-sigs/kueue/pull/6942#discussion_r2367956915).

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-23T13:04:08Z

I left a bit comment to check some corner cases.

- **pkg/cache/scheduler/tas_flavor_snapshot.go:517** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-23T12:54:07Z
```diff
@@ -514,7 +514,12 @@ func (s *TASFlavorSnapshot) findReplacementAssignment(tr *TASPodSetRequests, exi
 		return nil, nil, fmt.Sprintf("Cannot replace the node, because the existing topologyAssignment is invalid, as it contains the stale domain %v", staleDomain)
 	}
 	requiredReplacementDomain := s.requiredReplacementDomain(tr, existingAssignment)
-	replacementAssignment, reason := s.findTopologyAssignment(*tr, nil, assumedUsage, false, requiredReplacementDomain)
+	trCopy := *tr
```

  Is this shallow copy enough?
  Don't we need a deep copy?

- **pkg/cache/scheduler/tas_flavor_snapshot.go:578** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-23T13:00:20Z
```diff
@@ -557,19 +561,23 @@ func (s *TASFlavorSnapshot) requiredReplacementDomain(tr *TASPodSetRequests, ta
 	if !found {
 		return ""
 	}
-	required := isRequired(tr.PodSet.TopologyRequest)
-	if !required {
-		return ""
-	}
+
 	// no domain to comply with so we don't require any domain at all
 	// this happens when the faulty node was the only one in the assignment
 	if len(ta.Domains) == 0 {
 		return ""
 	}
 
+	if slicesRequested(tr.PodSet.TopologyRequest) && (tr.Count%*tr.PodSet.TopologyRequest.PodSetSliceSize != 0) {
+		return s.findIncompleteSliceDomain(tr, ta, tr.Count)
+	}
+
+	required := isRequired(tr.PodSet.TopologyRequest)
+	if !required {
+		return ""
+	}
```

  ```suggestion
  	if !isRequired(tr.PodSet.TopologyRequest) {
  		return ""	
  	}
  ```
  
  It looks like this `required` is not needed anywhere.

- **pkg/cache/scheduler/tas_flavor_snapshot.go:638** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-23T13:03:13Z
```diff
@@ -607,6 +615,41 @@ func deleteDomain(currentTopologyAssignment *kueue.TopologyAssignment, unhealthy
 	return noAffectedPods
 }
 
+func (s *TASFlavorSnapshot) findIncompleteSliceDomain(tr *TASPodSetRequests, ta *kueue.TopologyAssignment, missingCount int32) utiltas.TopologyDomainID {
+	// this function assumes that all assignments are at the hostname level
+	sliceTopologyKey := s.sliceLevelKeyWithDefault(tr.PodSet.TopologyRequest, s.lowestLevel())
+	sliceLevelIdx, found := s.resolveLevelIdx(sliceTopologyKey)
+	if !found {
+		return ""
+	}
+
+	sliceSize := *tr.PodSet.TopologyRequest.PodSetSliceSize
+
+	// domainToUsage maps a domain at sliceLevel to the number of pods in it
+	domainToUsage := make(map[utiltas.TopologyDomainID]int32)
+	nodeLevel := len(s.levelKeys) - 1
+
+	for _, domain := range ta.Domains {
+		nodeDomain, ok := s.domainsPerLevel[nodeLevel][utiltas.DomainID(domain.Values)]
+		if !ok {
+			continue
+		}
+
+		sliceDomain := nodeDomain
```

  Does this shallow copy enough?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-09-23T13:46:15Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:638** by [@pajakd](https://github.com/pajakd) — 2025-09-23T13:46:15Z
```diff
@@ -607,6 +615,41 @@ func deleteDomain(currentTopologyAssignment *kueue.TopologyAssignment, unhealthy
 	return noAffectedPods
 }
 
+func (s *TASFlavorSnapshot) findIncompleteSliceDomain(tr *TASPodSetRequests, ta *kueue.TopologyAssignment, missingCount int32) utiltas.TopologyDomainID {
+	// this function assumes that all assignments are at the hostname level
+	sliceTopologyKey := s.sliceLevelKeyWithDefault(tr.PodSet.TopologyRequest, s.lowestLevel())
+	sliceLevelIdx, found := s.resolveLevelIdx(sliceTopologyKey)
+	if !found {
+		return ""
+	}
+
+	sliceSize := *tr.PodSet.TopologyRequest.PodSetSliceSize
+
+	// domainToUsage maps a domain at sliceLevel to the number of pods in it
+	domainToUsage := make(map[utiltas.TopologyDomainID]int32)
+	nodeLevel := len(s.levelKeys) - 1
+
+	for _, domain := range ta.Domains {
+		nodeDomain, ok := s.domainsPerLevel[nodeLevel][utiltas.DomainID(domain.Values)]
+		if !ok {
+			continue
+		}
+
+		sliceDomain := nodeDomain
```

  We are operating on pointers (*domain) so the actual objects in the cache. I'm making any changes so I think we are fine here.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-09-23T13:51:59Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:517** by [@pajakd](https://github.com/pajakd) — 2025-09-23T13:51:59Z
```diff
@@ -514,7 +514,12 @@ func (s *TASFlavorSnapshot) findReplacementAssignment(tr *TASPodSetRequests, exi
 		return nil, nil, fmt.Sprintf("Cannot replace the node, because the existing topologyAssignment is invalid, as it contains the stale domain %v", staleDomain)
 	}
 	requiredReplacementDomain := s.requiredReplacementDomain(tr, existingAssignment)
-	replacementAssignment, reason := s.findTopologyAssignment(*tr, nil, assumedUsage, false, requiredReplacementDomain)
+	trCopy := *tr
```

  Yes, this is a shallow copy of the struct TASPodSetRequests. The field (PodSetSliceSize) that I'm modifying is in the PodSet field and for that I'm making the deep copy. So, I claim that I don't modify the original object.
  
  (I'm not sure if something bad would happen if I do modify it. cc @PBundyra )

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-24T03:13:14Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:638** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-24T03:13:14Z
```diff
@@ -607,6 +615,41 @@ func deleteDomain(currentTopologyAssignment *kueue.TopologyAssignment, unhealthy
 	return noAffectedPods
 }
 
+func (s *TASFlavorSnapshot) findIncompleteSliceDomain(tr *TASPodSetRequests, ta *kueue.TopologyAssignment, missingCount int32) utiltas.TopologyDomainID {
+	// this function assumes that all assignments are at the hostname level
+	sliceTopologyKey := s.sliceLevelKeyWithDefault(tr.PodSet.TopologyRequest, s.lowestLevel())
+	sliceLevelIdx, found := s.resolveLevelIdx(sliceTopologyKey)
+	if !found {
+		return ""
+	}
+
+	sliceSize := *tr.PodSet.TopologyRequest.PodSetSliceSize
+
+	// domainToUsage maps a domain at sliceLevel to the number of pods in it
+	domainToUsage := make(map[utiltas.TopologyDomainID]int32)
+	nodeLevel := len(s.levelKeys) - 1
+
+	for _, domain := range ta.Domains {
+		nodeDomain, ok := s.domainsPerLevel[nodeLevel][utiltas.DomainID(domain.Values)]
+		if !ok {
+			continue
+		}
+
+		sliceDomain := nodeDomain
```

  Why do we need to copy from nodeDomain to sliceDomain?
  If we want to operate on the actual object in the cache, could we use in the following?
  Let me know if my understanding is not correct.
  
  ```go
  		for i := nodeLevel; i > sliceLevelIdx; i-- {
  			nodeDomain = nodeDomain.parent
  		}
  		domainToUsage[nodeDomain.id] += domain.Count
  ```

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-09-24T06:55:37Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:638** by [@pajakd](https://github.com/pajakd) — 2025-09-24T06:55:37Z
```diff
@@ -607,6 +615,41 @@ func deleteDomain(currentTopologyAssignment *kueue.TopologyAssignment, unhealthy
 	return noAffectedPods
 }
 
+func (s *TASFlavorSnapshot) findIncompleteSliceDomain(tr *TASPodSetRequests, ta *kueue.TopologyAssignment, missingCount int32) utiltas.TopologyDomainID {
+	// this function assumes that all assignments are at the hostname level
+	sliceTopologyKey := s.sliceLevelKeyWithDefault(tr.PodSet.TopologyRequest, s.lowestLevel())
+	sliceLevelIdx, found := s.resolveLevelIdx(sliceTopologyKey)
+	if !found {
+		return ""
+	}
+
+	sliceSize := *tr.PodSet.TopologyRequest.PodSetSliceSize
+
+	// domainToUsage maps a domain at sliceLevel to the number of pods in it
+	domainToUsage := make(map[utiltas.TopologyDomainID]int32)
+	nodeLevel := len(s.levelKeys) - 1
+
+	for _, domain := range ta.Domains {
+		nodeDomain, ok := s.domainsPerLevel[nodeLevel][utiltas.DomainID(domain.Values)]
+		if !ok {
+			continue
+		}
+
+		sliceDomain := nodeDomain
```

  Of course you are right, we don't need a new variable for that. I thought to use a second variable so that the names of the variables indicate what is happening: we start the walk on the node level and eventually reach to the domain level. But this probably introduces more confusion than it solves. Renamed both and used a single variable.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-24T13:04:42Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:517** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-24T13:04:39Z
```diff
@@ -514,7 +514,12 @@ func (s *TASFlavorSnapshot) findReplacementAssignment(tr *TASPodSetRequests, exi
 		return nil, nil, fmt.Sprintf("Cannot replace the node, because the existing topologyAssignment is invalid, as it contains the stale domain %v", staleDomain)
 	}
 	requiredReplacementDomain := s.requiredReplacementDomain(tr, existingAssignment)
-	replacementAssignment, reason := s.findTopologyAssignment(*tr, nil, assumedUsage, false, requiredReplacementDomain)
+	trCopy := *tr
```

  @pajakd  Yes, that's right.
  In that case, why don't we need to deep copy `PodSetUpdates` and `SinglePodRequests`?
  
  https://github.com/kubernetes-sigs/kueue/blob/77590bb5af3817b86b108b46ff26773c834efe44/pkg/cache/scheduler/tas_flavor_snapshot.go#L348-L356
  
  Both are also pointer which means `findTopologyAssignment` will change the origin.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-24T13:22:12Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:638** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-24T13:22:11Z
```diff
@@ -607,6 +615,41 @@ func deleteDomain(currentTopologyAssignment *kueue.TopologyAssignment, unhealthy
 	return noAffectedPods
 }
 
+func (s *TASFlavorSnapshot) findIncompleteSliceDomain(tr *TASPodSetRequests, ta *kueue.TopologyAssignment, missingCount int32) utiltas.TopologyDomainID {
+	// this function assumes that all assignments are at the hostname level
+	sliceTopologyKey := s.sliceLevelKeyWithDefault(tr.PodSet.TopologyRequest, s.lowestLevel())
+	sliceLevelIdx, found := s.resolveLevelIdx(sliceTopologyKey)
+	if !found {
+		return ""
+	}
+
+	sliceSize := *tr.PodSet.TopologyRequest.PodSetSliceSize
+
+	// domainToUsage maps a domain at sliceLevel to the number of pods in it
+	domainToUsage := make(map[utiltas.TopologyDomainID]int32)
+	nodeLevel := len(s.levelKeys) - 1
+
+	for _, domain := range ta.Domains {
+		nodeDomain, ok := s.domainsPerLevel[nodeLevel][utiltas.DomainID(domain.Values)]
+		if !ok {
+			continue
+		}
+
+		sliceDomain := nodeDomain
```

  Thank you for updating that. The new code looks good to me 👍

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-09-24T13:52:53Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:517** by [@pajakd](https://github.com/pajakd) — 2025-09-24T13:52:53Z
```diff
@@ -514,7 +514,12 @@ func (s *TASFlavorSnapshot) findReplacementAssignment(tr *TASPodSetRequests, exi
 		return nil, nil, fmt.Sprintf("Cannot replace the node, because the existing topologyAssignment is invalid, as it contains the stale domain %v", staleDomain)
 	}
 	requiredReplacementDomain := s.requiredReplacementDomain(tr, existingAssignment)
-	replacementAssignment, reason := s.findTopologyAssignment(*tr, nil, assumedUsage, false, requiredReplacementDomain)
+	trCopy := *tr
```

  Discussed this offline with @PBundyra as this call of the `findTopologyAssignment` was part of the original PR for the hotswap #5287.  
  
  - The topologyRequests object is re-generated in each scheduling cycle: https://github.com/kubernetes-sigs/kueue/blob/c87e0c77e4e043af15078ccbbe766af8b0ba0aa6/pkg/scheduler/flavorassigner/flavorassigner.go#L576
  - `findTopologyAssignment` is (at least in the current implementation) only reading from it
  
  So we think its fine to keep the shallow copy here.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-24T13:58:43Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:517** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-24T13:58:43Z
```diff
@@ -514,7 +514,12 @@ func (s *TASFlavorSnapshot) findReplacementAssignment(tr *TASPodSetRequests, exi
 		return nil, nil, fmt.Sprintf("Cannot replace the node, because the existing topologyAssignment is invalid, as it contains the stale domain %v", staleDomain)
 	}
 	requiredReplacementDomain := s.requiredReplacementDomain(tr, existingAssignment)
-	replacementAssignment, reason := s.findTopologyAssignment(*tr, nil, assumedUsage, false, requiredReplacementDomain)
+	trCopy := *tr
```

  Thank you for describing that.
  That makes sense, and it could reduce performance regressions.
  
  Indeed, the `findTopologyAssignment` function is not exposed to other packages, so it would be safe.
  If we want to expose, let's revisit here.
