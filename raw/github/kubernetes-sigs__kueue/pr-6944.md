# PR #6944: Add priority_class label for optional metrics (if waitForPodsReady is enabled)

**Summary**: Add priority_class label for optional metrics (if waitForPodsReady is enabled)

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6944

**Last updated**: 2025-09-24T08:15:40Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@IrvingMg](https://github.com/IrvingMg)
- **Branch**: `feature/metric-kueue-ready-wait-time-seconds-wl-priority-class` → `main`
- **Created**: 2025-09-22T12:59:00Z
- **Updated**: 2025-09-24T08:15:40Z
- **Merged**: 2025-09-24T08:10:24Z
- **Closed**: 2025-09-24T08:10:24Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mbobrovskyi](https://github.com/mbobrovskyi), [@vladikkuzn](https://github.com/vladikkuzn)
- **Changed files**: 6   **+174 / -36**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind feature

#### What this PR does / why we need it:
Add workload_priority_class label for optional metrics (if waitForPodsReady is enabled):

- kueue_ready_wait_time_seconds (Histogram)
- kueue_admitted_until_ready_wait_time_seconds (Histogram)
- kueue_local_queue_ready_wait_time_seconds (Histogram)
- kueue_local_queue_admitted_until_ready_wait_time_seconds (Histogram)

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #5989 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Added workload_priority_class label for optional metrics (if waitForPodsReady is enabled):

- kueue_ready_wait_time_seconds (Histogram)
- kueue_admitted_until_ready_wait_time_seconds (Histogram)
- kueue_local_queue_ready_wait_time_seconds (Histogram)
- kueue_local_queue_admitted_until_ready_wait_time_seconds (Histogram)
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-09-22T12:59:07Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 218af138e11c33c082fc962e855bf19817d5b0fd |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68d39708001ace0008d6d70e |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-6944--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-6944--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTY5NDQtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.Om_QMAps-J-104oRboOtOnsJfK5HmzdK4ZarVhNsXqw)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-6944--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2025-09-22T12:59:45Z

/cc @mbobrovskyi @vladikkuzn

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2025-09-23T13:06:31Z

/test pull-kueue-test-integration-baseline-main

The logs show this message, but I couldn't see any test actually failing:
```
There were failures detected in the following suites:
  fairsharing ./test/integration/singlecluster/scheduler/fairsharing

Test Suite Failed
make: *** [Makefile-test.mk:72: test-integration] Error 1
```

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-23T13:12:24Z

I saw it elsewhere too, cc @gabesaba

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-23T14:41:37Z

/lgtm
Thank you!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-23T14:41:45Z

LGTM label has been added.  <details>Git tree hash: bab223091644e4a186dc8bccd0f9146224346150</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-23T14:55:32Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6944#" title="Author self-approved">IrvingMg</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6944#pullrequestreview-3258421876" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-24T05:51:16Z

@IrvingMg could you please resolve conflicts?

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-24T07:02:26Z

/retitle Add priority_class label for optional metrics (if waitForPodsReady is enabled)

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-24T07:02:40Z

/lgtm
Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-24T07:02:46Z

LGTM label has been added.  <details>Git tree hash: 4eeb8442fa3a03727e9344788185af28dc0e6e2d</details>

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-22T13:23:48Z

- **pkg/metrics/metrics.go:219** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-22T13:23:46Z
```diff
@@ -216,7 +216,7 @@ The label 'underlying_cause' can have the following values:
 			Name:      "ready_wait_time_seconds",
 			Help:      "The time between a workload was created or requeued until ready, per 'cluster_queue'",
 			Buckets:   generateExponentialBuckets(14),
-		}, []string{"cluster_queue"},
+		}, []string{"cluster_queue", "workload_priority_class"},
```

  Why do we want to sore only WorkloadPriorityClass name?
  Indeed, the Pod PriorityClass is used for Kueue scheduling and preemptions when users select that rather than  WorkloadPriorityClass.
  
  Shouldn't we add `priority_class` and `priority_calss_source` labels?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-22T14:33:06Z

- **pkg/metrics/metrics.go:219** by [@mimowo](https://github.com/mimowo) — 2025-09-22T14:33:05Z
```diff
@@ -216,7 +216,7 @@ The label 'underlying_cause' can have the following values:
 			Name:      "ready_wait_time_seconds",
 			Help:      "The time between a workload was created or requeued until ready, per 'cluster_queue'",
 			Buckets:   generateExponentialBuckets(14),
-		}, []string{"cluster_queue"},
+		}, []string{"cluster_queue", "workload_priority_class"},
```

  Good point, I had the same hesitation here: https://github.com/kubernetes-sigs/kueue/issues/5989#issuecomment-3281937253
  
  So we decided that workload_priority_class should be recommended approach for Workloads priority mechanism. Yes, Pods and MPIJob read prioirity from Pod, but it was more of a hack before we've introduced the "workload_priority_class". wdyt? 
  
  Having said that, I'm ok with "priority_class" too.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-22T17:16:25Z

- **pkg/metrics/metrics.go:541** by [@mimowo](https://github.com/mimowo) — 2025-09-22T17:16:25Z
```diff
@@ -538,8 +538,8 @@ func LocalQueueAdmissionChecksWaitTime(lq LocalQueueReference, waitTime time.Dur
 	localQueueAdmissionChecksWaitTime.WithLabelValues(string(lq.Name), lq.Namespace).Observe(waitTime.Seconds())
 }
 
-func ReadyWaitTime(cqName kueue.ClusterQueueReference, waitTime time.Duration) {
-	queuedUntilReadyWaitTime.WithLabelValues(string(cqName)).Observe(waitTime.Seconds())
+func ReadyWaitTime(cqName kueue.ClusterQueueReference, workloadPriorityClass string, waitTime time.Duration) {
```

  @IrvingMg can you please add a test case?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-22T17:17:53Z

- **pkg/metrics/metrics.go:219** by [@mimowo](https://github.com/mimowo) — 2025-09-22T17:17:53Z
```diff
@@ -216,7 +216,7 @@ The label 'underlying_cause' can have the following values:
 			Name:      "ready_wait_time_seconds",
 			Help:      "The time between a workload was created or requeued until ready, per 'cluster_queue'",
 			Buckets:   generateExponentialBuckets(14),
-		}, []string{"cluster_queue"},
+		}, []string{"cluster_queue", "workload_priority_class"},
```

  @mbobrovskyi @IrvingMg how many metrics are we yet to go? I would like to have them all consistently updated or none in 0.14. If we cannot update all metrics, I think we may consider reverting them all.
  
  If we are close, I would probably just merge as is, and open a separate PR to align the naming.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-22T17:50:37Z

- **pkg/metrics/metrics.go:219** by [@mimowo](https://github.com/mimowo) — 2025-09-22T17:50:37Z
```diff
@@ -216,7 +216,7 @@ The label 'underlying_cause' can have the following values:
 			Name:      "ready_wait_time_seconds",
 			Help:      "The time between a workload was created or requeued until ready, per 'cluster_queue'",
 			Buckets:   generateExponentialBuckets(14),
-		}, []string{"cluster_queue"},
+		}, []string{"cluster_queue", "workload_priority_class"},
```

  Actually I thought about this more and I agree with the priority_class direction. 
  
  It is not ticketed yet but we are considering another priority mechanism, where the priority source would be a configMap or another CrD. This way some automation could be adjusting all priorities for all workloads by a single API request. This is a separate feature which is not for sure yet going to be worked on, but it shows we may have new priority mechanisms.
  
  So to make this more easy to extend I support rename to priority_clasass.
  
  Still it this PR can stay with old name. We need another rename PR anyway. as for the priority_class_source it may be added later imo

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-22T19:50:36Z

- **pkg/metrics/metrics.go:219** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-22T19:50:36Z
```diff
@@ -216,7 +216,7 @@ The label 'underlying_cause' can have the following values:
 			Name:      "ready_wait_time_seconds",
 			Help:      "The time between a workload was created or requeued until ready, per 'cluster_queue'",
 			Buckets:   generateExponentialBuckets(14),
-		}, []string{"cluster_queue"},
+		}, []string{"cluster_queue", "workload_priority_class"},
```

  For ClusterQueue, we only have one PR: https://github.com/kubernetes-sigs/kueue/pull/6886.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-09-23T07:00:02Z

- **pkg/metrics/metrics.go:541** by [@IrvingMg](https://github.com/IrvingMg) — 2025-09-23T07:00:02Z
```diff
@@ -538,8 +538,8 @@ func LocalQueueAdmissionChecksWaitTime(lq LocalQueueReference, waitTime time.Dur
 	localQueueAdmissionChecksWaitTime.WithLabelValues(string(lq.Name), lq.Namespace).Observe(waitTime.Seconds())
 }
 
-func ReadyWaitTime(cqName kueue.ClusterQueueReference, waitTime time.Duration) {
-	queuedUntilReadyWaitTime.WithLabelValues(string(cqName)).Observe(waitTime.Seconds())
+func ReadyWaitTime(cqName kueue.ClusterQueueReference, workloadPriorityClass string, waitTime time.Duration) {
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-09-23T07:01:37Z

- **pkg/metrics/metrics.go:219** by [@IrvingMg](https://github.com/IrvingMg) — 2025-09-23T07:01:37Z
```diff
@@ -216,7 +216,7 @@ The label 'underlying_cause' can have the following values:
 			Name:      "ready_wait_time_seconds",
 			Help:      "The time between a workload was created or requeued until ready, per 'cluster_queue'",
 			Buckets:   generateExponentialBuckets(14),
-		}, []string{"cluster_queue"},
+		}, []string{"cluster_queue", "workload_priority_class"},
```

  To speed up the process a bit, I've updated this PR to include all optional metrics at once.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-23T07:04:38Z

- **test/e2e/customconfigs/waitforpodsready_test.go:487** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-23T07:04:38Z
```diff
@@ -482,3 +483,179 @@ var _ = ginkgo.Describe("WaitForPodsReady with default Timeout and a long Recove
 		})
 	})
 })
+
+var _ = ginkgo.Describe("WaitForPodsReady metrics with workload priority class", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
```

  Can we move it to integration tests?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-23T07:22:05Z

- **test/e2e/customconfigs/waitforpodsready_test.go:487** by [@mimowo](https://github.com/mimowo) — 2025-09-23T07:22:05Z
```diff
@@ -482,3 +483,179 @@ var _ = ginkgo.Describe("WaitForPodsReady with default Timeout and a long Recove
 		})
 	})
 })
+
+var _ = ginkgo.Describe("WaitForPodsReady metrics with workload priority class", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
```

  yes, we don't need e2e tests for metrics. We do have some e2e tests for metics, but these are mostly sanity testing.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-09-23T10:01:30Z

- **test/e2e/customconfigs/waitforpodsready_test.go:487** by [@IrvingMg](https://github.com/IrvingMg) — 2025-09-23T10:01:30Z
```diff
@@ -482,3 +483,179 @@ var _ = ginkgo.Describe("WaitForPodsReady with default Timeout and a long Recove
 		})
 	})
 })
+
+var _ = ginkgo.Describe("WaitForPodsReady metrics with workload priority class", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-23T11:09:07Z

- **pkg/metrics/metrics.go:219** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-23T11:09:06Z
```diff
@@ -216,7 +216,7 @@ The label 'underlying_cause' can have the following values:
 			Name:      "ready_wait_time_seconds",
 			Help:      "The time between a workload was created or requeued until ready, per 'cluster_queue'",
 			Buckets:   generateExponentialBuckets(14),
-		}, []string{"cluster_queue"},
+		}, []string{"cluster_queue", "workload_priority_class"},
```

  Acutally, the Pod Priority Class is not hack and they can always select either Workload PriorityClass and Pod PriorityClass, then the scheduler uses either Priority to prioritize workloads in https://github.com/kubernetes-sigs/kueue/blob/b7cd74b0424928f1da26ae150abf1c0ca7e27902/pkg/cache/queue/cluster_queue.go#L485-L490.
  
  So, I think that bot PriorityClasses are still a center of our scheduling mechanism.
  Thus, I would propose using `priority_class` instead of `workload_priority_class` in metric.
  
  > Still it this PR can stay with old name. We need another rename PR anyway. as for the priority_class_source it may be added later imo
  
  I'm fine with delaying the `priority_class_source` decision since it is not matter here.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-23T11:09:33Z

- **pkg/metrics/metrics.go:219** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-23T11:09:33Z
```diff
@@ -216,7 +216,7 @@ The label 'underlying_cause' can have the following values:
 			Name:      "ready_wait_time_seconds",
 			Help:      "The time between a workload was created or requeued until ready, per 'cluster_queue'",
 			Buckets:   generateExponentialBuckets(14),
-		}, []string{"cluster_queue"},
+		}, []string{"cluster_queue", "workload_priority_class"},
```

  > To speed up the process a bit, I've updated this PR to include all optional metrics at once.
  
  Thanks, Awesome!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-23T11:17:18Z

- **test/util/util.go:603** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-23T11:17:18Z
```diff
@@ -591,6 +591,78 @@ func ExpectAdmissionChecksWaitTimeMetric(cq *kueue.ClusterQueue, workloadPriorit
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func ExpectReadyWaitTimeMetric(cq *kueue.ClusterQueue, priorityClass string, count int) {
+	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) {
+		metric := metrics.QueuedUntilReadyWaitTime.WithLabelValues(cq.Name, priorityClass)
+		v, err := testutil.GetHistogramMetricCount(metric)
+		g.Expect(err).ToNot(gomega.HaveOccurred())
+		g.Expect(int(v)).Should(gomega.Equal(count))
+	}, Timeout, Interval).Should(gomega.Succeed())
+}
+
+func ExpectAdmittedUntilReadyWaitTimeMetric(cq *kueue.ClusterQueue, priorityClass string, count int) {
+	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) {
+		metric := metrics.AdmittedUntilReadyWaitTime.WithLabelValues(cq.Name, priorityClass)
+		v, err := testutil.GetHistogramMetricCount(metric)
+		g.Expect(err).ToNot(gomega.HaveOccurred())
+		g.Expect(int(v)).Should(gomega.Equal(count))
+	}, Timeout, Interval).Should(gomega.Succeed())
+}
+
+func ExpectLocalQueueReadyWaitTimeMetric(lq *kueue.LocalQueue, priorityClass string, count int) {
+	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) {
+		metric := metrics.LocalQueueQueuedUntilReadyWaitTime.WithLabelValues(lq.Name, lq.Namespace, priorityClass)
+		v, err := testutil.GetHistogramMetricCount(metric)
+		g.Expect(err).ToNot(gomega.HaveOccurred())
+		g.Expect(int(v)).Should(gomega.Equal(count))
+	}, Timeout, Interval).Should(gomega.Succeed())
+}
+
+func ExpectLocalQueueAdmittedUntilReadyWaitTimeMetric(lq *kueue.LocalQueue, priorityClass string, count int) {
+	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) {
+		metric := metrics.LocalQueueAdmittedUntilReadyWaitTime.WithLabelValues(lq.Name, lq.Namespace, priorityClass)
+		v, err := testutil.GetHistogramMetricCount(metric)
+		g.Expect(err).ToNot(gomega.HaveOccurred())
+		g.Expect(int(v)).Should(gomega.Equal(count))
+	}, Timeout, Interval).Should(gomega.Succeed())
+}
+
+func ExpectReadyWaitTimeMetricAtLeast(cq *kueue.ClusterQueue, priorityClass string, minCount int) {
+	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) {
+		metric := metrics.QueuedUntilReadyWaitTime.WithLabelValues(cq.Name, priorityClass)
+		v, err := testutil.GetHistogramMetricCount(metric)
+		g.Expect(err).ToNot(gomega.HaveOccurred())
+		g.Expect(int(v)).Should(gomega.BeNumerically(">=", minCount))
+	}, Timeout, Interval).Should(gomega.Succeed())
+}
+
+func ExpectAdmittedUntilReadyWaitTimeMetricAtLeast(cq *kueue.ClusterQueue, priorityClass string, minCount int) {
```

  Why minCount? Isn’t it reset after the ClusterQueue is deleted?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-23T11:18:44Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2737** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-23T11:18:44Z
```diff
@@ -2723,6 +2723,111 @@ var _ = ginkgo.Describe("Job controller interacting with Workload controller whe
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("testing ready wait time metrics with priority classes", func() {
+		ginkgo.BeforeEach(func() {
+			waitForPodsReadyTimeout = &metav1.Duration{Duration: 5 * time.Minute}
+			waitForPodsReadyRecoveryTimeout = nil
+		})
+
+		ginkgo.It("should record ready wait time metrics with workload priority class label", func() {
+			gomega.Expect(features.SetEnable(features.LocalQueueMetrics, true)).To(gomega.Succeed())
+			ginkgo.DeferCleanup(func() {
+				gomega.Expect(features.SetEnable(features.LocalQueueMetrics, false)).To(gomega.Succeed())
+			})
```

  ```suggestion
  			features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.LocalQueueMetrics, true)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-23T11:19:33Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2740** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-23T11:19:33Z
```diff
@@ -2723,6 +2723,111 @@ var _ = ginkgo.Describe("Job controller interacting with Workload controller whe
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("testing ready wait time metrics with priority classes", func() {
+		ginkgo.BeforeEach(func() {
+			waitForPodsReadyTimeout = &metav1.Duration{Duration: 5 * time.Minute}
+			waitForPodsReadyRecoveryTimeout = nil
+		})
+
+		ginkgo.It("should record ready wait time metrics with workload priority class label", func() {
+			gomega.Expect(features.SetEnable(features.LocalQueueMetrics, true)).To(gomega.Succeed())
+			ginkgo.DeferCleanup(func() {
+				gomega.Expect(features.SetEnable(features.LocalQueueMetrics, false)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("creating workload priority classes")
+			lowPriorityClass := testing.MakeWorkloadPriorityClass("low-priority").PriorityValue(10).Obj()
```

  ```suggestion
  			lowPriorityClass := testing.MakeWorkloadPriorityClass("low-priority").PriorityValue(priorityValue).Obj()
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-23T11:19:51Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2746** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-23T11:19:51Z
```diff
@@ -2723,6 +2723,111 @@ var _ = ginkgo.Describe("Job controller interacting with Workload controller whe
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("testing ready wait time metrics with priority classes", func() {
+		ginkgo.BeforeEach(func() {
+			waitForPodsReadyTimeout = &metav1.Duration{Duration: 5 * time.Minute}
+			waitForPodsReadyRecoveryTimeout = nil
+		})
+
+		ginkgo.It("should record ready wait time metrics with workload priority class label", func() {
+			gomega.Expect(features.SetEnable(features.LocalQueueMetrics, true)).To(gomega.Succeed())
+			ginkgo.DeferCleanup(func() {
+				gomega.Expect(features.SetEnable(features.LocalQueueMetrics, false)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("creating workload priority classes")
+			lowPriorityClass := testing.MakeWorkloadPriorityClass("low-priority").PriorityValue(10).Obj()
+			util.MustCreate(ctx, k8sClient, lowPriorityClass)
+			ginkgo.DeferCleanup(func() {
+				gomega.Expect(k8sClient.Delete(ctx, lowPriorityClass)).To(gomega.Succeed())
+			})
+
+			highPriorityClass := testing.MakeWorkloadPriorityClass("high-priority").PriorityValue(20).Obj()
```

  ```suggestion
  			highPriorityClass := testing.MakeWorkloadPriorityClass("high-priority").PriorityValue(highPriorityValue).Obj()
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-23T11:20:51Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2749** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-23T11:20:50Z
```diff
@@ -2723,6 +2723,111 @@ var _ = ginkgo.Describe("Job controller interacting with Workload controller whe
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("testing ready wait time metrics with priority classes", func() {
+		ginkgo.BeforeEach(func() {
+			waitForPodsReadyTimeout = &metav1.Duration{Duration: 5 * time.Minute}
+			waitForPodsReadyRecoveryTimeout = nil
+		})
+
+		ginkgo.It("should record ready wait time metrics with workload priority class label", func() {
+			gomega.Expect(features.SetEnable(features.LocalQueueMetrics, true)).To(gomega.Succeed())
+			ginkgo.DeferCleanup(func() {
+				gomega.Expect(features.SetEnable(features.LocalQueueMetrics, false)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("creating workload priority classes")
+			lowPriorityClass := testing.MakeWorkloadPriorityClass("low-priority").PriorityValue(10).Obj()
+			util.MustCreate(ctx, k8sClient, lowPriorityClass)
+			ginkgo.DeferCleanup(func() {
+				gomega.Expect(k8sClient.Delete(ctx, lowPriorityClass)).To(gomega.Succeed())
+			})
+
+			highPriorityClass := testing.MakeWorkloadPriorityClass("high-priority").PriorityValue(20).Obj()
+			util.MustCreate(ctx, k8sClient, highPriorityClass)
+			ginkgo.DeferCleanup(func() {
+				gomega.Expect(k8sClient.Delete(ctx, highPriorityClass)).To(gomega.Succeed())
```

  ```suggestion
  				util.ExpectObjectToBeDeleted(ctx, k8sClient, highPriorityClass, true)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-23T11:21:05Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2743** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-23T11:21:04Z
```diff
@@ -2723,6 +2723,111 @@ var _ = ginkgo.Describe("Job controller interacting with Workload controller whe
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("testing ready wait time metrics with priority classes", func() {
+		ginkgo.BeforeEach(func() {
+			waitForPodsReadyTimeout = &metav1.Duration{Duration: 5 * time.Minute}
+			waitForPodsReadyRecoveryTimeout = nil
+		})
+
+		ginkgo.It("should record ready wait time metrics with workload priority class label", func() {
+			gomega.Expect(features.SetEnable(features.LocalQueueMetrics, true)).To(gomega.Succeed())
+			ginkgo.DeferCleanup(func() {
+				gomega.Expect(features.SetEnable(features.LocalQueueMetrics, false)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("creating workload priority classes")
+			lowPriorityClass := testing.MakeWorkloadPriorityClass("low-priority").PriorityValue(10).Obj()
+			util.MustCreate(ctx, k8sClient, lowPriorityClass)
+			ginkgo.DeferCleanup(func() {
+				gomega.Expect(k8sClient.Delete(ctx, lowPriorityClass)).To(gomega.Succeed())
```

  ```suggestion
  				util.ExpectObjectToBeDeleted(ctx, k8sClient, lowPriorityClass, true)
  ```

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-09-23T11:57:31Z

- **test/util/util.go:603** by [@IrvingMg](https://github.com/IrvingMg) — 2025-09-23T11:57:31Z
```diff
@@ -591,6 +591,78 @@ func ExpectAdmissionChecksWaitTimeMetric(cq *kueue.ClusterQueue, workloadPriorit
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func ExpectReadyWaitTimeMetric(cq *kueue.ClusterQueue, priorityClass string, count int) {
+	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) {
+		metric := metrics.QueuedUntilReadyWaitTime.WithLabelValues(cq.Name, priorityClass)
+		v, err := testutil.GetHistogramMetricCount(metric)
+		g.Expect(err).ToNot(gomega.HaveOccurred())
+		g.Expect(int(v)).Should(gomega.Equal(count))
+	}, Timeout, Interval).Should(gomega.Succeed())
+}
+
+func ExpectAdmittedUntilReadyWaitTimeMetric(cq *kueue.ClusterQueue, priorityClass string, count int) {
+	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) {
+		metric := metrics.AdmittedUntilReadyWaitTime.WithLabelValues(cq.Name, priorityClass)
+		v, err := testutil.GetHistogramMetricCount(metric)
+		g.Expect(err).ToNot(gomega.HaveOccurred())
+		g.Expect(int(v)).Should(gomega.Equal(count))
+	}, Timeout, Interval).Should(gomega.Succeed())
+}
+
+func ExpectLocalQueueReadyWaitTimeMetric(lq *kueue.LocalQueue, priorityClass string, count int) {
+	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) {
+		metric := metrics.LocalQueueQueuedUntilReadyWaitTime.WithLabelValues(lq.Name, lq.Namespace, priorityClass)
+		v, err := testutil.GetHistogramMetricCount(metric)
+		g.Expect(err).ToNot(gomega.HaveOccurred())
+		g.Expect(int(v)).Should(gomega.Equal(count))
+	}, Timeout, Interval).Should(gomega.Succeed())
+}
+
+func ExpectLocalQueueAdmittedUntilReadyWaitTimeMetric(lq *kueue.LocalQueue, priorityClass string, count int) {
+	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) {
+		metric := metrics.LocalQueueAdmittedUntilReadyWaitTime.WithLabelValues(lq.Name, lq.Namespace, priorityClass)
+		v, err := testutil.GetHistogramMetricCount(metric)
+		g.Expect(err).ToNot(gomega.HaveOccurred())
+		g.Expect(int(v)).Should(gomega.Equal(count))
+	}, Timeout, Interval).Should(gomega.Succeed())
+}
+
+func ExpectReadyWaitTimeMetricAtLeast(cq *kueue.ClusterQueue, priorityClass string, minCount int) {
+	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) {
+		metric := metrics.QueuedUntilReadyWaitTime.WithLabelValues(cq.Name, priorityClass)
+		v, err := testutil.GetHistogramMetricCount(metric)
+		g.Expect(err).ToNot(gomega.HaveOccurred())
+		g.Expect(int(v)).Should(gomega.BeNumerically(">=", minCount))
+	}, Timeout, Interval).Should(gomega.Succeed())
+}
+
+func ExpectAdmittedUntilReadyWaitTimeMetricAtLeast(cq *kueue.ClusterQueue, priorityClass string, minCount int) {
```

  Yeah... however, as I understand it, the reconciler might record the same metric multiple times. So instead of using exact counts, which makes the test flaky, it was changed to use a minimum count

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-23T12:10:06Z

- **test/util/util.go:628** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-23T12:10:06Z
```diff
@@ -591,6 +591,78 @@ func ExpectAdmissionChecksWaitTimeMetric(cq *kueue.ClusterQueue, workloadPriorit
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func ExpectReadyWaitTimeMetric(cq *kueue.ClusterQueue, priorityClass string, count int) {
+	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) {
+		metric := metrics.QueuedUntilReadyWaitTime.WithLabelValues(cq.Name, priorityClass)
+		v, err := testutil.GetHistogramMetricCount(metric)
+		g.Expect(err).ToNot(gomega.HaveOccurred())
+		g.Expect(int(v)).Should(gomega.Equal(count))
+	}, Timeout, Interval).Should(gomega.Succeed())
+}
+
+func ExpectAdmittedUntilReadyWaitTimeMetric(cq *kueue.ClusterQueue, priorityClass string, count int) {
+	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) {
+		metric := metrics.AdmittedUntilReadyWaitTime.WithLabelValues(cq.Name, priorityClass)
+		v, err := testutil.GetHistogramMetricCount(metric)
+		g.Expect(err).ToNot(gomega.HaveOccurred())
+		g.Expect(int(v)).Should(gomega.Equal(count))
+	}, Timeout, Interval).Should(gomega.Succeed())
+}
+
+func ExpectLocalQueueReadyWaitTimeMetric(lq *kueue.LocalQueue, priorityClass string, count int) {
+	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) {
+		metric := metrics.LocalQueueQueuedUntilReadyWaitTime.WithLabelValues(lq.Name, lq.Namespace, priorityClass)
+		v, err := testutil.GetHistogramMetricCount(metric)
+		g.Expect(err).ToNot(gomega.HaveOccurred())
+		g.Expect(int(v)).Should(gomega.Equal(count))
+	}, Timeout, Interval).Should(gomega.Succeed())
+}
+
+func ExpectLocalQueueAdmittedUntilReadyWaitTimeMetric(lq *kueue.LocalQueue, priorityClass string, count int) {
+	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) {
+		metric := metrics.LocalQueueAdmittedUntilReadyWaitTime.WithLabelValues(lq.Name, lq.Namespace, priorityClass)
+		v, err := testutil.GetHistogramMetricCount(metric)
+		g.Expect(err).ToNot(gomega.HaveOccurred())
+		g.Expect(int(v)).Should(gomega.Equal(count))
+	}, Timeout, Interval).Should(gomega.Succeed())
+}
```

  It looks like this isn’t needed anymore, right?

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-09-23T12:14:39Z

- **test/util/util.go:628** by [@IrvingMg](https://github.com/IrvingMg) — 2025-09-23T12:14:39Z
```diff
@@ -591,6 +591,78 @@ func ExpectAdmissionChecksWaitTimeMetric(cq *kueue.ClusterQueue, workloadPriorit
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func ExpectReadyWaitTimeMetric(cq *kueue.ClusterQueue, priorityClass string, count int) {
+	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) {
+		metric := metrics.QueuedUntilReadyWaitTime.WithLabelValues(cq.Name, priorityClass)
+		v, err := testutil.GetHistogramMetricCount(metric)
+		g.Expect(err).ToNot(gomega.HaveOccurred())
+		g.Expect(int(v)).Should(gomega.Equal(count))
+	}, Timeout, Interval).Should(gomega.Succeed())
+}
+
+func ExpectAdmittedUntilReadyWaitTimeMetric(cq *kueue.ClusterQueue, priorityClass string, count int) {
+	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) {
+		metric := metrics.AdmittedUntilReadyWaitTime.WithLabelValues(cq.Name, priorityClass)
+		v, err := testutil.GetHistogramMetricCount(metric)
+		g.Expect(err).ToNot(gomega.HaveOccurred())
+		g.Expect(int(v)).Should(gomega.Equal(count))
+	}, Timeout, Interval).Should(gomega.Succeed())
+}
+
+func ExpectLocalQueueReadyWaitTimeMetric(lq *kueue.LocalQueue, priorityClass string, count int) {
+	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) {
+		metric := metrics.LocalQueueQueuedUntilReadyWaitTime.WithLabelValues(lq.Name, lq.Namespace, priorityClass)
+		v, err := testutil.GetHistogramMetricCount(metric)
+		g.Expect(err).ToNot(gomega.HaveOccurred())
+		g.Expect(int(v)).Should(gomega.Equal(count))
+	}, Timeout, Interval).Should(gomega.Succeed())
+}
+
+func ExpectLocalQueueAdmittedUntilReadyWaitTimeMetric(lq *kueue.LocalQueue, priorityClass string, count int) {
+	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) {
+		metric := metrics.LocalQueueAdmittedUntilReadyWaitTime.WithLabelValues(lq.Name, lq.Namespace, priorityClass)
+		v, err := testutil.GetHistogramMetricCount(metric)
+		g.Expect(err).ToNot(gomega.HaveOccurred())
+		g.Expect(int(v)).Should(gomega.Equal(count))
+	}, Timeout, Interval).Should(gomega.Succeed())
+}
```

  Right, I've removed them. Thanks

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-23T13:48:45Z

- **site/content/en/docs/reference/metrics.md:89** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-23T13:48:45Z
```diff
@@ -86,7 +86,7 @@ For more details [see](/docs/tasks/manage/setup_wait_for_pods_ready).
 
 | Metric name                                                | Type      | Description                                                                        | Labels                                                                                            |
 | ---------------------------------------------------------- | --------- | ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
-| `kueue_ready_wait_time_seconds`                            | Histogram | The time between a workload was created or requeued until ready.                   | `cluster_queue`: the name of the ClusterQueue                                                     | 
-| `kueue_admitted_until_ready_wait_time_seconds`             | Histogram | The time between a workload was admitted until ready.                              | `cluster_queue`: the name of the ClusterQueue                                                     |
-| `kueue_local_queue_ready_wait_time_seconds`                | Histogram | The time between a workload was created or requeued until ready, per `local_queue` | `name`: the name of the LocalQueue<br />`namespace`: the namespace that the LocalQueue resides in |
-| `kueue_local_queue_admitted_until_ready_wait_time_seconds` | Histogram | The time between a workload was admitted until ready, per `local_queue`            | `name`: the name of the LocalQueue<br />`namespace`: the namespace that the LocalQueue resides in |
\ No newline at end of file
+| `kueue_ready_wait_time_seconds`                            | Histogram | The time between a workload was created or requeued until ready.                   | `cluster_queue`: the name of the ClusterQueue<br> `priority_class`: the workload priority class name |
```

  ```suggestion
  | `kueue_ready_wait_time_seconds`                            | Histogram | The time between a workload was created or requeued until ready.                   | `cluster_queue`: the name of the ClusterQueue<br> `priority_class`: the priority class name |
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-23T13:49:10Z

- **site/content/en/docs/reference/metrics.md:89** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-23T13:49:10Z
```diff
@@ -86,7 +86,7 @@ For more details [see](/docs/tasks/manage/setup_wait_for_pods_ready).
 
 | Metric name                                                | Type      | Description                                                                        | Labels                                                                                            |
 | ---------------------------------------------------------- | --------- | ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
-| `kueue_ready_wait_time_seconds`                            | Histogram | The time between a workload was created or requeued until ready.                   | `cluster_queue`: the name of the ClusterQueue                                                     | 
-| `kueue_admitted_until_ready_wait_time_seconds`             | Histogram | The time between a workload was admitted until ready.                              | `cluster_queue`: the name of the ClusterQueue                                                     |
-| `kueue_local_queue_ready_wait_time_seconds`                | Histogram | The time between a workload was created or requeued until ready, per `local_queue` | `name`: the name of the LocalQueue<br />`namespace`: the namespace that the LocalQueue resides in |
-| `kueue_local_queue_admitted_until_ready_wait_time_seconds` | Histogram | The time between a workload was admitted until ready, per `local_queue`            | `name`: the name of the LocalQueue<br />`namespace`: the namespace that the LocalQueue resides in |
\ No newline at end of file
+| `kueue_ready_wait_time_seconds`                            | Histogram | The time between a workload was created or requeued until ready.                   | `cluster_queue`: the name of the ClusterQueue<br> `priority_class`: the workload priority class name |
```

  And also other metrics.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-09-23T14:04:42Z

- **site/content/en/docs/reference/metrics.md:89** by [@IrvingMg](https://github.com/IrvingMg) — 2025-09-23T14:04:41Z
```diff
@@ -86,7 +86,7 @@ For more details [see](/docs/tasks/manage/setup_wait_for_pods_ready).
 
 | Metric name                                                | Type      | Description                                                                        | Labels                                                                                            |
 | ---------------------------------------------------------- | --------- | ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
-| `kueue_ready_wait_time_seconds`                            | Histogram | The time between a workload was created or requeued until ready.                   | `cluster_queue`: the name of the ClusterQueue                                                     | 
-| `kueue_admitted_until_ready_wait_time_seconds`             | Histogram | The time between a workload was admitted until ready.                              | `cluster_queue`: the name of the ClusterQueue                                                     |
-| `kueue_local_queue_ready_wait_time_seconds`                | Histogram | The time between a workload was created or requeued until ready, per `local_queue` | `name`: the name of the LocalQueue<br />`namespace`: the namespace that the LocalQueue resides in |
-| `kueue_local_queue_admitted_until_ready_wait_time_seconds` | Histogram | The time between a workload was admitted until ready, per `local_queue`            | `name`: the name of the LocalQueue<br />`namespace`: the namespace that the LocalQueue resides in |
\ No newline at end of file
+| `kueue_ready_wait_time_seconds`                            | Histogram | The time between a workload was created or requeued until ready.                   | `cluster_queue`: the name of the ClusterQueue<br> `priority_class`: the workload priority class name |
```

  Done. Thanks

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-23T14:55:19Z

/approve
