# PR #695: [jobframework] Integration Manager

**Summary**: [jobframework] Integration Manager

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/695

**Last updated**: 2023-04-28T13:00:40Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `jobframework_integration_manager` → `main`
- **Created**: 2023-04-13T12:30:17Z
- **Updated**: 2023-04-28T13:00:40Z
- **Merged**: 2023-04-27T20:04:15Z
- **Closed**: 2023-04-27T20:04:15Z
- **Labels**: `lgtm`, `size/XL`, `approved`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mwielgus](https://github.com/mwielgus), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 9   **+546 / -77**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Change the way job's frameworks are integrated into the controller manager.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #691 

#### Special notes for your reviewer:

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-04-13T12:30:26Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 616fdda37945686cad5e474f80ff4492c10ec0ce |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6449839ee0ba650008bd200f |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-04-13T12:30:28Z

Hi @trasc. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-13T20:15:54Z

/ok-to-test

### Comment by [@trasc](https://github.com/trasc) — 2023-04-19T09:43:49Z

/cc @alculquicondor 
/cc @mwielgus

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-26T19:33:25Z

/kind cleanup

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-26T20:04:48Z

/lgtm

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-27T19:45:43Z

@alculquicondor Probably, you forgot to add a `approve` label to this PR :)

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-27T19:51:00Z

/approve

oh yes :)

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-04-27T19:51:08Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/695#issuecomment-1526297904" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/695#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-04-20T11:32:40Z

- **main.go:85** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-20T09:55:46Z
```diff
@@ -70,11 +67,21 @@ func init() {
 
 	utilruntime.Must(kueue.AddToScheme(scheme))
 	utilruntime.Must(config.AddToScheme(scheme))
-	utilruntime.Must(kubeflow.AddToScheme(scheme))
 	// +kubebuilder:scaffold:scheme
 }
 
 func main() {
+
+	// integrations schemas
+	utilruntime.Must(
+		jobframework.ForEachIntegration(func(_ string, cb jobframework.IntegrationCallbacks) error {
+			if cb.AddToScheme != nil {
+				return cb.AddToScheme(scheme)
+			}
+			return nil
+		}),
+	)
+
```

  Why don't we run this initialization in the `init()` function?

- **main.go:303** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-20T11:02:13Z
```diff
@@ -287,17 +289,31 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 	}
 	if err != nil {
 		setupLog.Error(err, "unable to load the config")
-		os.Exit(1)
+		return options, cfg, err
+	}
+
+	if cfg.Integrations != nil {
+		var invalidFrameworks []string
+		for _, framework := range cfg.Integrations.Frameworks {
+			if _, found := jobframework.GetIntegration(framework); !found {
+				invalidFrameworks = append(invalidFrameworks, framework)
+			}
+		}
+		if len(invalidFrameworks) > 0 {
```

  ```suggestion
  		if invalidFrameworks != nil {
  ```
  
  IIUC, `invalidFrameworks` is initialized as a nil slice.

- **main.go:210** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-20T11:11:25Z
```diff
@@ -180,46 +193,35 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 		os.Exit(1)
 	}
 
-	if isFrameworkEnabled(cfg, job.FrameworkName) {
-		if err := job.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.JobControllerName),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "Job")
-			os.Exit(1)
-		}
-		if err := job.SetupWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, job.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
+	opts := []jobframework.Option{
+		jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
+		jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
 	}
-
-	if isFrameworkEnabled(cfg, mpijob.FrameworkName) {
-		if err := mpijob.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.KueueName+"-mpijob-controller"),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "MPIJob")
-			os.Exit(1)
-		}
-		if err := mpijob.SetupMPIJobWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, mpijob.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
+	err := jobframework.ForEachIntegration(func(name string, cb jobframework.IntegrationCallbacks) error {
+		log := setupLog.WithValues("integration", name)
+		if isFrameworkEnabled(cfg, name) {
+			if err := cb.NewReconciler(mgr.GetScheme(),
+				mgr.GetClient(),
+				mgr.GetEventRecorderFor(name+"-"+constants.KueueName+"-controller"),
+				opts...,
+			).SetupWithManager(mgr); err != nil {
+				log.Error(err, "unable to create controller")
```

  Can we add a framework name to the log like the following:
  
  ```suggestion
  				log.Error(err, "unable to create controller", "controller", frameworkName)
  ```

- **main.go:206** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-20T11:13:49Z
```diff
@@ -180,46 +193,35 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 		os.Exit(1)
 	}
 
-	if isFrameworkEnabled(cfg, job.FrameworkName) {
-		if err := job.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.JobControllerName),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "Job")
-			os.Exit(1)
-		}
-		if err := job.SetupWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, job.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
+	opts := []jobframework.Option{
+		jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
+		jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
 	}
-
-	if isFrameworkEnabled(cfg, mpijob.FrameworkName) {
-		if err := mpijob.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.KueueName+"-mpijob-controller"),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "MPIJob")
-			os.Exit(1)
-		}
-		if err := mpijob.SetupMPIJobWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, mpijob.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
+	err := jobframework.ForEachIntegration(func(name string, cb jobframework.IntegrationCallbacks) error {
+		log := setupLog.WithValues("integration", name)
+		if isFrameworkEnabled(cfg, name) {
+			if err := cb.NewReconciler(mgr.GetScheme(),
+				mgr.GetClient(),
+				mgr.GetEventRecorderFor(name+"-"+constants.KueueName+"-controller"),
```

  Why do we use `name+"-"+constants.KueueName+"-controller"` instead of `constants.KueueName+"-mpijob-controller"` as a recorder name?

- **main.go:217** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-20T11:16:01Z
```diff
@@ -180,46 +193,35 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 		os.Exit(1)
 	}
 
-	if isFrameworkEnabled(cfg, job.FrameworkName) {
-		if err := job.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.JobControllerName),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "Job")
-			os.Exit(1)
-		}
-		if err := job.SetupWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, job.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
+	opts := []jobframework.Option{
+		jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
+		jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
 	}
-
-	if isFrameworkEnabled(cfg, mpijob.FrameworkName) {
-		if err := mpijob.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.KueueName+"-mpijob-controller"),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "MPIJob")
-			os.Exit(1)
-		}
-		if err := mpijob.SetupMPIJobWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, mpijob.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
+	err := jobframework.ForEachIntegration(func(name string, cb jobframework.IntegrationCallbacks) error {
+		log := setupLog.WithValues("integration", name)
+		if isFrameworkEnabled(cfg, name) {
+			if err := cb.NewReconciler(mgr.GetScheme(),
+				mgr.GetClient(),
+				mgr.GetEventRecorderFor(name+"-"+constants.KueueName+"-controller"),
+				opts...,
+			).SetupWithManager(mgr); err != nil {
+				log.Error(err, "unable to create controller")
+				return err
+			}
+			if err := cb.SetupWebhook(mgr, opts...); err != nil {
+				setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
+				return err
+			}
+		} else {
+			if err := noop.SetupWebhook(mgr, cb.WebhookType()); err != nil {
+				setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
```

  ```suggestion
  				setupLog.Error(err, "Unable to create webhook", "webhook", "Noop")
  ```

- **main.go:213** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-20T11:16:18Z
```diff
@@ -180,46 +193,35 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 		os.Exit(1)
 	}
 
-	if isFrameworkEnabled(cfg, job.FrameworkName) {
-		if err := job.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.JobControllerName),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "Job")
-			os.Exit(1)
-		}
-		if err := job.SetupWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, job.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
+	opts := []jobframework.Option{
+		jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
+		jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
 	}
-
-	if isFrameworkEnabled(cfg, mpijob.FrameworkName) {
-		if err := mpijob.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.KueueName+"-mpijob-controller"),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "MPIJob")
-			os.Exit(1)
-		}
-		if err := mpijob.SetupMPIJobWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, mpijob.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
+	err := jobframework.ForEachIntegration(func(name string, cb jobframework.IntegrationCallbacks) error {
+		log := setupLog.WithValues("integration", name)
+		if isFrameworkEnabled(cfg, name) {
+			if err := cb.NewReconciler(mgr.GetScheme(),
+				mgr.GetClient(),
+				mgr.GetEventRecorderFor(name+"-"+constants.KueueName+"-controller"),
+				opts...,
+			).SetupWithManager(mgr); err != nil {
+				log.Error(err, "unable to create controller")
+				return err
+			}
+			if err := cb.SetupWebhook(mgr, opts...); err != nil {
+				setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
```

  ```suggestion
  				setupLog.Error(err, "Unable to create webhook", "webhook", frameworkName)
  ```

- **pkg/controller/jobframework/integrationmanager.go:140** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-20T11:21:28Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"sort"
+	"sync"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+var (
+	errDuplicateFrameworkName  = errors.New("duplicate framework name")
+	errMissingMadatoryCallback = errors.New("mandatory callback missing")
+)
+
+type JobReconcilerInterface interface {
+	SetupWithManager(mgr ctrl.Manager) error
+}
+
+// IntegrationCallbacks groups a set of callbacks used to integrate a new framework
+type IntegrationCallbacks struct {
+	// NewReconciler creates a new reconciler
+	NewReconciler func(scheme *runtime.Scheme, client client.Client, record record.EventRecorder, opts ...Option) JobReconcilerInterface
+	// SetupWebhook sets up the framework's webhook with the controllers manager
+	SetupWebhook func(mgr ctrl.Manager, opts ...Option) error
+	// WebhookType returns an object of the type managed by the webhook
+	WebhookType func() runtime.Object
+	// SetupIndexes registers any additional indexes with the controllers manager
+	// (this callback is optional)
+	SetupIndexes func(ctx context.Context, indexer client.FieldIndexer) error
+	// AddToScheme adds any additional types to the controllers manager's scheme
+	// (this callback is optional)
+	AddToScheme func(s *runtime.Scheme) error
+}
+
+type integrationManager struct {
+	lock sync.RWMutex
+
+	names        []string
+	integrations map[string]IntegrationCallbacks
+}
+
+var manager integrationManager
+
+func (m *integrationManager) register(name string, cb IntegrationCallbacks) error {
+	m.lock.Lock()
+	defer m.lock.Unlock()
+	if m.integrations == nil {
+		m.integrations = make(map[string]IntegrationCallbacks)
+	}
+	if _, exists := m.integrations[name]; exists {
+		return fmt.Errorf("%w \"%s\"", errDuplicateFrameworkName, name)
+	}
+
+	if cb.NewReconciler == nil {
+		return fmt.Errorf("%w \"NewReconciler\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	if cb.SetupWebhook == nil {
+		return fmt.Errorf("%w \"SetupWebhook\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	if cb.WebhookType == nil {
+		return fmt.Errorf("%w \"WebhookType\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	m.integrations[name] = cb
+	m.names = append(m.names, name)
+
+	return nil
+}
+
+func (m *integrationManager) forEach(f func(name string, cb IntegrationCallbacks) error) error {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	for _, name := range m.names {
+		if err := f(name, m.integrations[name]); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (m *integrationManager) get(name string) (IntegrationCallbacks, bool) {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	cb, f := m.integrations[name]
+	return cb, f
+}
+
+func (m *integrationManager) getList() []string {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	ret := make([]string, len(m.names))
+	copy(ret, m.names)
+	sort.Strings(ret)
+	return ret
+}
+
+// RegisterIntegration registeres a new framework, returns an error when
+// attempting to register multiple frameworks with the same name of if a
+// mandatory callback is missing
+func RegisterIntegration(name string, cb IntegrationCallbacks) error {
+	return manager.register(name, cb)
+}
+
+// ForEachIntegration loops through  the registered list of frameworks calling f,
+// if at any point f returns an error the loop is stopped and that error is returned
+func ForEachIntegration(f func(name string, cb IntegrationCallbacks) error) error {
+	return manager.forEach(f)
+}
+
+// GetIntegration looks-up the framework identified by name in the currently registered
+// list of frameworks returning it's callbacks and true if found
+func GetIntegration(name string) (IntegrationCallbacks, bool) {
+	return manager.get(name)
+}
+
+// GetIntegrationsList return the list of currently registered frameworks
```

  ```suggestion
  // GetIntegrationsList returns the list of currently registered frameworks.
  ```

- **pkg/controller/jobframework/integrationmanager.go:128** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-20T11:22:06Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"sort"
+	"sync"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+var (
+	errDuplicateFrameworkName  = errors.New("duplicate framework name")
+	errMissingMadatoryCallback = errors.New("mandatory callback missing")
+)
+
+type JobReconcilerInterface interface {
+	SetupWithManager(mgr ctrl.Manager) error
+}
+
+// IntegrationCallbacks groups a set of callbacks used to integrate a new framework
+type IntegrationCallbacks struct {
+	// NewReconciler creates a new reconciler
+	NewReconciler func(scheme *runtime.Scheme, client client.Client, record record.EventRecorder, opts ...Option) JobReconcilerInterface
+	// SetupWebhook sets up the framework's webhook with the controllers manager
+	SetupWebhook func(mgr ctrl.Manager, opts ...Option) error
+	// WebhookType returns an object of the type managed by the webhook
+	WebhookType func() runtime.Object
+	// SetupIndexes registers any additional indexes with the controllers manager
+	// (this callback is optional)
+	SetupIndexes func(ctx context.Context, indexer client.FieldIndexer) error
+	// AddToScheme adds any additional types to the controllers manager's scheme
+	// (this callback is optional)
+	AddToScheme func(s *runtime.Scheme) error
+}
+
+type integrationManager struct {
+	lock sync.RWMutex
+
+	names        []string
+	integrations map[string]IntegrationCallbacks
+}
+
+var manager integrationManager
+
+func (m *integrationManager) register(name string, cb IntegrationCallbacks) error {
+	m.lock.Lock()
+	defer m.lock.Unlock()
+	if m.integrations == nil {
+		m.integrations = make(map[string]IntegrationCallbacks)
+	}
+	if _, exists := m.integrations[name]; exists {
+		return fmt.Errorf("%w \"%s\"", errDuplicateFrameworkName, name)
+	}
+
+	if cb.NewReconciler == nil {
+		return fmt.Errorf("%w \"NewReconciler\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	if cb.SetupWebhook == nil {
+		return fmt.Errorf("%w \"SetupWebhook\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	if cb.WebhookType == nil {
+		return fmt.Errorf("%w \"WebhookType\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	m.integrations[name] = cb
+	m.names = append(m.names, name)
+
+	return nil
+}
+
+func (m *integrationManager) forEach(f func(name string, cb IntegrationCallbacks) error) error {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	for _, name := range m.names {
+		if err := f(name, m.integrations[name]); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (m *integrationManager) get(name string) (IntegrationCallbacks, bool) {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	cb, f := m.integrations[name]
+	return cb, f
+}
+
+func (m *integrationManager) getList() []string {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	ret := make([]string, len(m.names))
+	copy(ret, m.names)
+	sort.Strings(ret)
+	return ret
+}
+
+// RegisterIntegration registeres a new framework, returns an error when
+// attempting to register multiple frameworks with the same name of if a
+// mandatory callback is missing
+func RegisterIntegration(name string, cb IntegrationCallbacks) error {
+	return manager.register(name, cb)
+}
+
+// ForEachIntegration loops through  the registered list of frameworks calling f,
```

  ```suggestion
  // ForEachIntegration loops through the registered list of frameworks calling f,
  ```

- **pkg/controller/jobframework/integrationmanager.go:41** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-20T11:22:27Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"sort"
+	"sync"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+var (
+	errDuplicateFrameworkName  = errors.New("duplicate framework name")
+	errMissingMadatoryCallback = errors.New("mandatory callback missing")
+)
+
+type JobReconcilerInterface interface {
+	SetupWithManager(mgr ctrl.Manager) error
+}
+
+// IntegrationCallbacks groups a set of callbacks used to integrate a new framework
```

  ```suggestion
  // IntegrationCallbacks groups a set of callbacks used to integrate a new framework.
  ```

- **pkg/controller/jobframework/integrationmanager.go:123** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-20T11:22:35Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"sort"
+	"sync"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+var (
+	errDuplicateFrameworkName  = errors.New("duplicate framework name")
+	errMissingMadatoryCallback = errors.New("mandatory callback missing")
+)
+
+type JobReconcilerInterface interface {
+	SetupWithManager(mgr ctrl.Manager) error
+}
+
+// IntegrationCallbacks groups a set of callbacks used to integrate a new framework
+type IntegrationCallbacks struct {
+	// NewReconciler creates a new reconciler
+	NewReconciler func(scheme *runtime.Scheme, client client.Client, record record.EventRecorder, opts ...Option) JobReconcilerInterface
+	// SetupWebhook sets up the framework's webhook with the controllers manager
+	SetupWebhook func(mgr ctrl.Manager, opts ...Option) error
+	// WebhookType returns an object of the type managed by the webhook
+	WebhookType func() runtime.Object
+	// SetupIndexes registers any additional indexes with the controllers manager
+	// (this callback is optional)
+	SetupIndexes func(ctx context.Context, indexer client.FieldIndexer) error
+	// AddToScheme adds any additional types to the controllers manager's scheme
+	// (this callback is optional)
+	AddToScheme func(s *runtime.Scheme) error
+}
+
+type integrationManager struct {
+	lock sync.RWMutex
+
+	names        []string
+	integrations map[string]IntegrationCallbacks
+}
+
+var manager integrationManager
+
+func (m *integrationManager) register(name string, cb IntegrationCallbacks) error {
+	m.lock.Lock()
+	defer m.lock.Unlock()
+	if m.integrations == nil {
+		m.integrations = make(map[string]IntegrationCallbacks)
+	}
+	if _, exists := m.integrations[name]; exists {
+		return fmt.Errorf("%w \"%s\"", errDuplicateFrameworkName, name)
+	}
+
+	if cb.NewReconciler == nil {
+		return fmt.Errorf("%w \"NewReconciler\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	if cb.SetupWebhook == nil {
+		return fmt.Errorf("%w \"SetupWebhook\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	if cb.WebhookType == nil {
+		return fmt.Errorf("%w \"WebhookType\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	m.integrations[name] = cb
+	m.names = append(m.names, name)
+
+	return nil
+}
+
+func (m *integrationManager) forEach(f func(name string, cb IntegrationCallbacks) error) error {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	for _, name := range m.names {
+		if err := f(name, m.integrations[name]); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (m *integrationManager) get(name string) (IntegrationCallbacks, bool) {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	cb, f := m.integrations[name]
+	return cb, f
+}
+
+func (m *integrationManager) getList() []string {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	ret := make([]string, len(m.names))
+	copy(ret, m.names)
+	sort.Strings(ret)
+	return ret
+}
+
+// RegisterIntegration registeres a new framework, returns an error when
+// attempting to register multiple frameworks with the same name of if a
+// mandatory callback is missing
```

  ```suggestion
  // mandatory callback is missing.
  ```

- **pkg/controller/jobframework/integrationmanager.go:129** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-20T11:22:41Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"sort"
+	"sync"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+var (
+	errDuplicateFrameworkName  = errors.New("duplicate framework name")
+	errMissingMadatoryCallback = errors.New("mandatory callback missing")
+)
+
+type JobReconcilerInterface interface {
+	SetupWithManager(mgr ctrl.Manager) error
+}
+
+// IntegrationCallbacks groups a set of callbacks used to integrate a new framework
+type IntegrationCallbacks struct {
+	// NewReconciler creates a new reconciler
+	NewReconciler func(scheme *runtime.Scheme, client client.Client, record record.EventRecorder, opts ...Option) JobReconcilerInterface
+	// SetupWebhook sets up the framework's webhook with the controllers manager
+	SetupWebhook func(mgr ctrl.Manager, opts ...Option) error
+	// WebhookType returns an object of the type managed by the webhook
+	WebhookType func() runtime.Object
+	// SetupIndexes registers any additional indexes with the controllers manager
+	// (this callback is optional)
+	SetupIndexes func(ctx context.Context, indexer client.FieldIndexer) error
+	// AddToScheme adds any additional types to the controllers manager's scheme
+	// (this callback is optional)
+	AddToScheme func(s *runtime.Scheme) error
+}
+
+type integrationManager struct {
+	lock sync.RWMutex
+
+	names        []string
+	integrations map[string]IntegrationCallbacks
+}
+
+var manager integrationManager
+
+func (m *integrationManager) register(name string, cb IntegrationCallbacks) error {
+	m.lock.Lock()
+	defer m.lock.Unlock()
+	if m.integrations == nil {
+		m.integrations = make(map[string]IntegrationCallbacks)
+	}
+	if _, exists := m.integrations[name]; exists {
+		return fmt.Errorf("%w \"%s\"", errDuplicateFrameworkName, name)
+	}
+
+	if cb.NewReconciler == nil {
+		return fmt.Errorf("%w \"NewReconciler\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	if cb.SetupWebhook == nil {
+		return fmt.Errorf("%w \"SetupWebhook\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	if cb.WebhookType == nil {
+		return fmt.Errorf("%w \"WebhookType\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	m.integrations[name] = cb
+	m.names = append(m.names, name)
+
+	return nil
+}
+
+func (m *integrationManager) forEach(f func(name string, cb IntegrationCallbacks) error) error {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	for _, name := range m.names {
+		if err := f(name, m.integrations[name]); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (m *integrationManager) get(name string) (IntegrationCallbacks, bool) {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	cb, f := m.integrations[name]
+	return cb, f
+}
+
+func (m *integrationManager) getList() []string {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	ret := make([]string, len(m.names))
+	copy(ret, m.names)
+	sort.Strings(ret)
+	return ret
+}
+
+// RegisterIntegration registeres a new framework, returns an error when
+// attempting to register multiple frameworks with the same name of if a
+// mandatory callback is missing
+func RegisterIntegration(name string, cb IntegrationCallbacks) error {
+	return manager.register(name, cb)
+}
+
+// ForEachIntegration loops through  the registered list of frameworks calling f,
+// if at any point f returns an error the loop is stopped and that error is returned
```

  ```suggestion
  // if at any point f returns an error the loop is stopped and that error is returned.
  ```

- **pkg/controller/jobframework/integrationmanager.go:135** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-20T11:22:53Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"sort"
+	"sync"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+var (
+	errDuplicateFrameworkName  = errors.New("duplicate framework name")
+	errMissingMadatoryCallback = errors.New("mandatory callback missing")
+)
+
+type JobReconcilerInterface interface {
+	SetupWithManager(mgr ctrl.Manager) error
+}
+
+// IntegrationCallbacks groups a set of callbacks used to integrate a new framework
+type IntegrationCallbacks struct {
+	// NewReconciler creates a new reconciler
+	NewReconciler func(scheme *runtime.Scheme, client client.Client, record record.EventRecorder, opts ...Option) JobReconcilerInterface
+	// SetupWebhook sets up the framework's webhook with the controllers manager
+	SetupWebhook func(mgr ctrl.Manager, opts ...Option) error
+	// WebhookType returns an object of the type managed by the webhook
+	WebhookType func() runtime.Object
+	// SetupIndexes registers any additional indexes with the controllers manager
+	// (this callback is optional)
+	SetupIndexes func(ctx context.Context, indexer client.FieldIndexer) error
+	// AddToScheme adds any additional types to the controllers manager's scheme
+	// (this callback is optional)
+	AddToScheme func(s *runtime.Scheme) error
+}
+
+type integrationManager struct {
+	lock sync.RWMutex
+
+	names        []string
+	integrations map[string]IntegrationCallbacks
+}
+
+var manager integrationManager
+
+func (m *integrationManager) register(name string, cb IntegrationCallbacks) error {
+	m.lock.Lock()
+	defer m.lock.Unlock()
+	if m.integrations == nil {
+		m.integrations = make(map[string]IntegrationCallbacks)
+	}
+	if _, exists := m.integrations[name]; exists {
+		return fmt.Errorf("%w \"%s\"", errDuplicateFrameworkName, name)
+	}
+
+	if cb.NewReconciler == nil {
+		return fmt.Errorf("%w \"NewReconciler\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	if cb.SetupWebhook == nil {
+		return fmt.Errorf("%w \"SetupWebhook\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	if cb.WebhookType == nil {
+		return fmt.Errorf("%w \"WebhookType\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	m.integrations[name] = cb
+	m.names = append(m.names, name)
+
+	return nil
+}
+
+func (m *integrationManager) forEach(f func(name string, cb IntegrationCallbacks) error) error {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	for _, name := range m.names {
+		if err := f(name, m.integrations[name]); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (m *integrationManager) get(name string) (IntegrationCallbacks, bool) {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	cb, f := m.integrations[name]
+	return cb, f
+}
+
+func (m *integrationManager) getList() []string {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	ret := make([]string, len(m.names))
+	copy(ret, m.names)
+	sort.Strings(ret)
+	return ret
+}
+
+// RegisterIntegration registeres a new framework, returns an error when
+// attempting to register multiple frameworks with the same name of if a
+// mandatory callback is missing
+func RegisterIntegration(name string, cb IntegrationCallbacks) error {
+	return manager.register(name, cb)
+}
+
+// ForEachIntegration loops through  the registered list of frameworks calling f,
+// if at any point f returns an error the loop is stopped and that error is returned
+func ForEachIntegration(f func(name string, cb IntegrationCallbacks) error) error {
+	return manager.forEach(f)
+}
+
+// GetIntegration looks-up the framework identified by name in the currently registered
+// list of frameworks returning it's callbacks and true if found
```

  ```suggestion
  // list of frameworks returning it's callbacks and true if found.
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-20T13:23:41Z

- **main.go:85** by [@trasc](https://github.com/trasc) — 2023-04-20T12:33:55Z
```diff
@@ -70,11 +67,21 @@ func init() {
 
 	utilruntime.Must(kueue.AddToScheme(scheme))
 	utilruntime.Must(config.AddToScheme(scheme))
-	utilruntime.Must(kubeflow.AddToScheme(scheme))
 	// +kubebuilder:scaffold:scheme
 }
 
 func main() {
+
+	// integrations schemas
+	utilruntime.Must(
+		jobframework.ForEachIntegration(func(_ string, cb jobframework.IntegrationCallbacks) error {
+			if cb.AddToScheme != nil {
+				return cb.AddToScheme(scheme)
+			}
+			return nil
+		}),
+	)
+
```

  For some reason it looked more cleaner to me, anyway ,done.

- **main.go:206** by [@trasc](https://github.com/trasc) — 2023-04-20T12:46:46Z
```diff
@@ -180,46 +193,35 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 		os.Exit(1)
 	}
 
-	if isFrameworkEnabled(cfg, job.FrameworkName) {
-		if err := job.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.JobControllerName),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "Job")
-			os.Exit(1)
-		}
-		if err := job.SetupWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, job.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
+	opts := []jobframework.Option{
+		jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
+		jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
 	}
-
-	if isFrameworkEnabled(cfg, mpijob.FrameworkName) {
-		if err := mpijob.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.KueueName+"-mpijob-controller"),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "MPIJob")
-			os.Exit(1)
-		}
-		if err := mpijob.SetupMPIJobWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, mpijob.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
+	err := jobframework.ForEachIntegration(func(name string, cb jobframework.IntegrationCallbacks) error {
+		log := setupLog.WithValues("integration", name)
+		if isFrameworkEnabled(cfg, name) {
+			if err := cb.NewReconciler(mgr.GetScheme(),
+				mgr.GetClient(),
+				mgr.GetEventRecorderFor(name+"-"+constants.KueueName+"-controller"),
```

  to be specific for each job controller.

- **main.go:210** by [@trasc](https://github.com/trasc) — 2023-04-20T12:52:46Z
```diff
@@ -180,46 +193,35 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 		os.Exit(1)
 	}
 
-	if isFrameworkEnabled(cfg, job.FrameworkName) {
-		if err := job.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.JobControllerName),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "Job")
-			os.Exit(1)
-		}
-		if err := job.SetupWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, job.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
+	opts := []jobframework.Option{
+		jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
+		jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
 	}
-
-	if isFrameworkEnabled(cfg, mpijob.FrameworkName) {
-		if err := mpijob.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.KueueName+"-mpijob-controller"),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "MPIJob")
-			os.Exit(1)
-		}
-		if err := mpijob.SetupMPIJobWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, mpijob.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
+	err := jobframework.ForEachIntegration(func(name string, cb jobframework.IntegrationCallbacks) error {
+		log := setupLog.WithValues("integration", name)
+		if isFrameworkEnabled(cfg, name) {
+			if err := cb.NewReconciler(mgr.GetScheme(),
+				mgr.GetClient(),
+				mgr.GetEventRecorderFor(name+"-"+constants.KueueName+"-controller"),
+				opts...,
+			).SetupWithManager(mgr); err != nil {
+				log.Error(err, "unable to create controller")
```

  the framework name is already added when creating `log`, `log := setupLog.WithValues("integration", name)`. 
  
  changing `"integration"` to `"jobFrameworkName"`

- **main.go:213** by [@trasc](https://github.com/trasc) — 2023-04-20T12:54:00Z
```diff
@@ -180,46 +193,35 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 		os.Exit(1)
 	}
 
-	if isFrameworkEnabled(cfg, job.FrameworkName) {
-		if err := job.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.JobControllerName),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "Job")
-			os.Exit(1)
-		}
-		if err := job.SetupWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, job.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
+	opts := []jobframework.Option{
+		jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
+		jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
 	}
-
-	if isFrameworkEnabled(cfg, mpijob.FrameworkName) {
-		if err := mpijob.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.KueueName+"-mpijob-controller"),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "MPIJob")
-			os.Exit(1)
-		}
-		if err := mpijob.SetupMPIJobWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, mpijob.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
+	err := jobframework.ForEachIntegration(func(name string, cb jobframework.IntegrationCallbacks) error {
+		log := setupLog.WithValues("integration", name)
+		if isFrameworkEnabled(cfg, name) {
+			if err := cb.NewReconciler(mgr.GetScheme(),
+				mgr.GetClient(),
+				mgr.GetEventRecorderFor(name+"-"+constants.KueueName+"-controller"),
+				opts...,
+			).SetupWithManager(mgr); err != nil {
+				log.Error(err, "unable to create controller")
+				return err
+			}
+			if err := cb.SetupWebhook(mgr, opts...); err != nil {
+				setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
```

  the frameworkName is already part of the log.

- **main.go:217** by [@trasc](https://github.com/trasc) — 2023-04-20T12:54:41Z
```diff
@@ -180,46 +193,35 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 		os.Exit(1)
 	}
 
-	if isFrameworkEnabled(cfg, job.FrameworkName) {
-		if err := job.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.JobControllerName),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "Job")
-			os.Exit(1)
-		}
-		if err := job.SetupWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, job.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
+	opts := []jobframework.Option{
+		jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
+		jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
 	}
-
-	if isFrameworkEnabled(cfg, mpijob.FrameworkName) {
-		if err := mpijob.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.KueueName+"-mpijob-controller"),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "MPIJob")
-			os.Exit(1)
-		}
-		if err := mpijob.SetupMPIJobWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, mpijob.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
+	err := jobframework.ForEachIntegration(func(name string, cb jobframework.IntegrationCallbacks) error {
+		log := setupLog.WithValues("integration", name)
+		if isFrameworkEnabled(cfg, name) {
+			if err := cb.NewReconciler(mgr.GetScheme(),
+				mgr.GetClient(),
+				mgr.GetEventRecorderFor(name+"-"+constants.KueueName+"-controller"),
+				opts...,
+			).SetupWithManager(mgr); err != nil {
+				log.Error(err, "unable to create controller")
+				return err
+			}
+			if err := cb.SetupWebhook(mgr, opts...); err != nil {
+				setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
+				return err
+			}
+		} else {
+			if err := noop.SetupWebhook(mgr, cb.WebhookType()); err != nil {
+				setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
```

  done

- **main.go:303** by [@trasc](https://github.com/trasc) — 2023-04-20T13:16:03Z
```diff
@@ -287,17 +289,31 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 	}
 	if err != nil {
 		setupLog.Error(err, "unable to load the config")
-		os.Exit(1)
+		return options, cfg, err
+	}
+
+	if cfg.Integrations != nil {
+		var invalidFrameworks []string
+		for _, framework := range cfg.Integrations.Frameworks {
+			if _, found := jobframework.GetIntegration(framework); !found {
+				invalidFrameworks = append(invalidFrameworks, framework)
+			}
+		}
+		if len(invalidFrameworks) > 0 {
```

  semantically , we are interested to know if the slice is empty. From a performance point of view there could be a penalty but since this not on a performance critical path, it makes more sense to me checking the length.

- **pkg/controller/jobframework/integrationmanager.go:41** by [@trasc](https://github.com/trasc) — 2023-04-20T13:17:36Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"sort"
+	"sync"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+var (
+	errDuplicateFrameworkName  = errors.New("duplicate framework name")
+	errMissingMadatoryCallback = errors.New("mandatory callback missing")
+)
+
+type JobReconcilerInterface interface {
+	SetupWithManager(mgr ctrl.Manager) error
+}
+
+// IntegrationCallbacks groups a set of callbacks used to integrate a new framework
```

  done

- **pkg/controller/jobframework/integrationmanager.go:123** by [@trasc](https://github.com/trasc) — 2023-04-20T13:19:03Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"sort"
+	"sync"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+var (
+	errDuplicateFrameworkName  = errors.New("duplicate framework name")
+	errMissingMadatoryCallback = errors.New("mandatory callback missing")
+)
+
+type JobReconcilerInterface interface {
+	SetupWithManager(mgr ctrl.Manager) error
+}
+
+// IntegrationCallbacks groups a set of callbacks used to integrate a new framework
+type IntegrationCallbacks struct {
+	// NewReconciler creates a new reconciler
+	NewReconciler func(scheme *runtime.Scheme, client client.Client, record record.EventRecorder, opts ...Option) JobReconcilerInterface
+	// SetupWebhook sets up the framework's webhook with the controllers manager
+	SetupWebhook func(mgr ctrl.Manager, opts ...Option) error
+	// WebhookType returns an object of the type managed by the webhook
+	WebhookType func() runtime.Object
+	// SetupIndexes registers any additional indexes with the controllers manager
+	// (this callback is optional)
+	SetupIndexes func(ctx context.Context, indexer client.FieldIndexer) error
+	// AddToScheme adds any additional types to the controllers manager's scheme
+	// (this callback is optional)
+	AddToScheme func(s *runtime.Scheme) error
+}
+
+type integrationManager struct {
+	lock sync.RWMutex
+
+	names        []string
+	integrations map[string]IntegrationCallbacks
+}
+
+var manager integrationManager
+
+func (m *integrationManager) register(name string, cb IntegrationCallbacks) error {
+	m.lock.Lock()
+	defer m.lock.Unlock()
+	if m.integrations == nil {
+		m.integrations = make(map[string]IntegrationCallbacks)
+	}
+	if _, exists := m.integrations[name]; exists {
+		return fmt.Errorf("%w \"%s\"", errDuplicateFrameworkName, name)
+	}
+
+	if cb.NewReconciler == nil {
+		return fmt.Errorf("%w \"NewReconciler\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	if cb.SetupWebhook == nil {
+		return fmt.Errorf("%w \"SetupWebhook\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	if cb.WebhookType == nil {
+		return fmt.Errorf("%w \"WebhookType\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	m.integrations[name] = cb
+	m.names = append(m.names, name)
+
+	return nil
+}
+
+func (m *integrationManager) forEach(f func(name string, cb IntegrationCallbacks) error) error {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	for _, name := range m.names {
+		if err := f(name, m.integrations[name]); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (m *integrationManager) get(name string) (IntegrationCallbacks, bool) {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	cb, f := m.integrations[name]
+	return cb, f
+}
+
+func (m *integrationManager) getList() []string {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	ret := make([]string, len(m.names))
+	copy(ret, m.names)
+	sort.Strings(ret)
+	return ret
+}
+
+// RegisterIntegration registeres a new framework, returns an error when
+// attempting to register multiple frameworks with the same name of if a
+// mandatory callback is missing
```

  done

- **pkg/controller/jobframework/integrationmanager.go:128** by [@trasc](https://github.com/trasc) — 2023-04-20T13:19:10Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"sort"
+	"sync"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+var (
+	errDuplicateFrameworkName  = errors.New("duplicate framework name")
+	errMissingMadatoryCallback = errors.New("mandatory callback missing")
+)
+
+type JobReconcilerInterface interface {
+	SetupWithManager(mgr ctrl.Manager) error
+}
+
+// IntegrationCallbacks groups a set of callbacks used to integrate a new framework
+type IntegrationCallbacks struct {
+	// NewReconciler creates a new reconciler
+	NewReconciler func(scheme *runtime.Scheme, client client.Client, record record.EventRecorder, opts ...Option) JobReconcilerInterface
+	// SetupWebhook sets up the framework's webhook with the controllers manager
+	SetupWebhook func(mgr ctrl.Manager, opts ...Option) error
+	// WebhookType returns an object of the type managed by the webhook
+	WebhookType func() runtime.Object
+	// SetupIndexes registers any additional indexes with the controllers manager
+	// (this callback is optional)
+	SetupIndexes func(ctx context.Context, indexer client.FieldIndexer) error
+	// AddToScheme adds any additional types to the controllers manager's scheme
+	// (this callback is optional)
+	AddToScheme func(s *runtime.Scheme) error
+}
+
+type integrationManager struct {
+	lock sync.RWMutex
+
+	names        []string
+	integrations map[string]IntegrationCallbacks
+}
+
+var manager integrationManager
+
+func (m *integrationManager) register(name string, cb IntegrationCallbacks) error {
+	m.lock.Lock()
+	defer m.lock.Unlock()
+	if m.integrations == nil {
+		m.integrations = make(map[string]IntegrationCallbacks)
+	}
+	if _, exists := m.integrations[name]; exists {
+		return fmt.Errorf("%w \"%s\"", errDuplicateFrameworkName, name)
+	}
+
+	if cb.NewReconciler == nil {
+		return fmt.Errorf("%w \"NewReconciler\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	if cb.SetupWebhook == nil {
+		return fmt.Errorf("%w \"SetupWebhook\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	if cb.WebhookType == nil {
+		return fmt.Errorf("%w \"WebhookType\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	m.integrations[name] = cb
+	m.names = append(m.names, name)
+
+	return nil
+}
+
+func (m *integrationManager) forEach(f func(name string, cb IntegrationCallbacks) error) error {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	for _, name := range m.names {
+		if err := f(name, m.integrations[name]); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (m *integrationManager) get(name string) (IntegrationCallbacks, bool) {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	cb, f := m.integrations[name]
+	return cb, f
+}
+
+func (m *integrationManager) getList() []string {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	ret := make([]string, len(m.names))
+	copy(ret, m.names)
+	sort.Strings(ret)
+	return ret
+}
+
+// RegisterIntegration registeres a new framework, returns an error when
+// attempting to register multiple frameworks with the same name of if a
+// mandatory callback is missing
+func RegisterIntegration(name string, cb IntegrationCallbacks) error {
+	return manager.register(name, cb)
+}
+
+// ForEachIntegration loops through  the registered list of frameworks calling f,
```

  done

- **pkg/controller/jobframework/integrationmanager.go:129** by [@trasc](https://github.com/trasc) — 2023-04-20T13:19:24Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"sort"
+	"sync"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+var (
+	errDuplicateFrameworkName  = errors.New("duplicate framework name")
+	errMissingMadatoryCallback = errors.New("mandatory callback missing")
+)
+
+type JobReconcilerInterface interface {
+	SetupWithManager(mgr ctrl.Manager) error
+}
+
+// IntegrationCallbacks groups a set of callbacks used to integrate a new framework
+type IntegrationCallbacks struct {
+	// NewReconciler creates a new reconciler
+	NewReconciler func(scheme *runtime.Scheme, client client.Client, record record.EventRecorder, opts ...Option) JobReconcilerInterface
+	// SetupWebhook sets up the framework's webhook with the controllers manager
+	SetupWebhook func(mgr ctrl.Manager, opts ...Option) error
+	// WebhookType returns an object of the type managed by the webhook
+	WebhookType func() runtime.Object
+	// SetupIndexes registers any additional indexes with the controllers manager
+	// (this callback is optional)
+	SetupIndexes func(ctx context.Context, indexer client.FieldIndexer) error
+	// AddToScheme adds any additional types to the controllers manager's scheme
+	// (this callback is optional)
+	AddToScheme func(s *runtime.Scheme) error
+}
+
+type integrationManager struct {
+	lock sync.RWMutex
+
+	names        []string
+	integrations map[string]IntegrationCallbacks
+}
+
+var manager integrationManager
+
+func (m *integrationManager) register(name string, cb IntegrationCallbacks) error {
+	m.lock.Lock()
+	defer m.lock.Unlock()
+	if m.integrations == nil {
+		m.integrations = make(map[string]IntegrationCallbacks)
+	}
+	if _, exists := m.integrations[name]; exists {
+		return fmt.Errorf("%w \"%s\"", errDuplicateFrameworkName, name)
+	}
+
+	if cb.NewReconciler == nil {
+		return fmt.Errorf("%w \"NewReconciler\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	if cb.SetupWebhook == nil {
+		return fmt.Errorf("%w \"SetupWebhook\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	if cb.WebhookType == nil {
+		return fmt.Errorf("%w \"WebhookType\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	m.integrations[name] = cb
+	m.names = append(m.names, name)
+
+	return nil
+}
+
+func (m *integrationManager) forEach(f func(name string, cb IntegrationCallbacks) error) error {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	for _, name := range m.names {
+		if err := f(name, m.integrations[name]); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (m *integrationManager) get(name string) (IntegrationCallbacks, bool) {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	cb, f := m.integrations[name]
+	return cb, f
+}
+
+func (m *integrationManager) getList() []string {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	ret := make([]string, len(m.names))
+	copy(ret, m.names)
+	sort.Strings(ret)
+	return ret
+}
+
+// RegisterIntegration registeres a new framework, returns an error when
+// attempting to register multiple frameworks with the same name of if a
+// mandatory callback is missing
+func RegisterIntegration(name string, cb IntegrationCallbacks) error {
+	return manager.register(name, cb)
+}
+
+// ForEachIntegration loops through  the registered list of frameworks calling f,
+// if at any point f returns an error the loop is stopped and that error is returned
```

  done

- **pkg/controller/jobframework/integrationmanager.go:135** by [@trasc](https://github.com/trasc) — 2023-04-20T13:19:39Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"sort"
+	"sync"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+var (
+	errDuplicateFrameworkName  = errors.New("duplicate framework name")
+	errMissingMadatoryCallback = errors.New("mandatory callback missing")
+)
+
+type JobReconcilerInterface interface {
+	SetupWithManager(mgr ctrl.Manager) error
+}
+
+// IntegrationCallbacks groups a set of callbacks used to integrate a new framework
+type IntegrationCallbacks struct {
+	// NewReconciler creates a new reconciler
+	NewReconciler func(scheme *runtime.Scheme, client client.Client, record record.EventRecorder, opts ...Option) JobReconcilerInterface
+	// SetupWebhook sets up the framework's webhook with the controllers manager
+	SetupWebhook func(mgr ctrl.Manager, opts ...Option) error
+	// WebhookType returns an object of the type managed by the webhook
+	WebhookType func() runtime.Object
+	// SetupIndexes registers any additional indexes with the controllers manager
+	// (this callback is optional)
+	SetupIndexes func(ctx context.Context, indexer client.FieldIndexer) error
+	// AddToScheme adds any additional types to the controllers manager's scheme
+	// (this callback is optional)
+	AddToScheme func(s *runtime.Scheme) error
+}
+
+type integrationManager struct {
+	lock sync.RWMutex
+
+	names        []string
+	integrations map[string]IntegrationCallbacks
+}
+
+var manager integrationManager
+
+func (m *integrationManager) register(name string, cb IntegrationCallbacks) error {
+	m.lock.Lock()
+	defer m.lock.Unlock()
+	if m.integrations == nil {
+		m.integrations = make(map[string]IntegrationCallbacks)
+	}
+	if _, exists := m.integrations[name]; exists {
+		return fmt.Errorf("%w \"%s\"", errDuplicateFrameworkName, name)
+	}
+
+	if cb.NewReconciler == nil {
+		return fmt.Errorf("%w \"NewReconciler\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	if cb.SetupWebhook == nil {
+		return fmt.Errorf("%w \"SetupWebhook\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	if cb.WebhookType == nil {
+		return fmt.Errorf("%w \"WebhookType\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	m.integrations[name] = cb
+	m.names = append(m.names, name)
+
+	return nil
+}
+
+func (m *integrationManager) forEach(f func(name string, cb IntegrationCallbacks) error) error {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	for _, name := range m.names {
+		if err := f(name, m.integrations[name]); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (m *integrationManager) get(name string) (IntegrationCallbacks, bool) {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	cb, f := m.integrations[name]
+	return cb, f
+}
+
+func (m *integrationManager) getList() []string {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	ret := make([]string, len(m.names))
+	copy(ret, m.names)
+	sort.Strings(ret)
+	return ret
+}
+
+// RegisterIntegration registeres a new framework, returns an error when
+// attempting to register multiple frameworks with the same name of if a
+// mandatory callback is missing
+func RegisterIntegration(name string, cb IntegrationCallbacks) error {
+	return manager.register(name, cb)
+}
+
+// ForEachIntegration loops through  the registered list of frameworks calling f,
+// if at any point f returns an error the loop is stopped and that error is returned
+func ForEachIntegration(f func(name string, cb IntegrationCallbacks) error) error {
+	return manager.forEach(f)
+}
+
+// GetIntegration looks-up the framework identified by name in the currently registered
+// list of frameworks returning it's callbacks and true if found
```

  done

- **pkg/controller/jobframework/integrationmanager.go:140** by [@trasc](https://github.com/trasc) — 2023-04-20T13:20:04Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"sort"
+	"sync"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+var (
+	errDuplicateFrameworkName  = errors.New("duplicate framework name")
+	errMissingMadatoryCallback = errors.New("mandatory callback missing")
+)
+
+type JobReconcilerInterface interface {
+	SetupWithManager(mgr ctrl.Manager) error
+}
+
+// IntegrationCallbacks groups a set of callbacks used to integrate a new framework
+type IntegrationCallbacks struct {
+	// NewReconciler creates a new reconciler
+	NewReconciler func(scheme *runtime.Scheme, client client.Client, record record.EventRecorder, opts ...Option) JobReconcilerInterface
+	// SetupWebhook sets up the framework's webhook with the controllers manager
+	SetupWebhook func(mgr ctrl.Manager, opts ...Option) error
+	// WebhookType returns an object of the type managed by the webhook
+	WebhookType func() runtime.Object
+	// SetupIndexes registers any additional indexes with the controllers manager
+	// (this callback is optional)
+	SetupIndexes func(ctx context.Context, indexer client.FieldIndexer) error
+	// AddToScheme adds any additional types to the controllers manager's scheme
+	// (this callback is optional)
+	AddToScheme func(s *runtime.Scheme) error
+}
+
+type integrationManager struct {
+	lock sync.RWMutex
+
+	names        []string
+	integrations map[string]IntegrationCallbacks
+}
+
+var manager integrationManager
+
+func (m *integrationManager) register(name string, cb IntegrationCallbacks) error {
+	m.lock.Lock()
+	defer m.lock.Unlock()
+	if m.integrations == nil {
+		m.integrations = make(map[string]IntegrationCallbacks)
+	}
+	if _, exists := m.integrations[name]; exists {
+		return fmt.Errorf("%w \"%s\"", errDuplicateFrameworkName, name)
+	}
+
+	if cb.NewReconciler == nil {
+		return fmt.Errorf("%w \"NewReconciler\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	if cb.SetupWebhook == nil {
+		return fmt.Errorf("%w \"SetupWebhook\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	if cb.WebhookType == nil {
+		return fmt.Errorf("%w \"WebhookType\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	m.integrations[name] = cb
+	m.names = append(m.names, name)
+
+	return nil
+}
+
+func (m *integrationManager) forEach(f func(name string, cb IntegrationCallbacks) error) error {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	for _, name := range m.names {
+		if err := f(name, m.integrations[name]); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (m *integrationManager) get(name string) (IntegrationCallbacks, bool) {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	cb, f := m.integrations[name]
+	return cb, f
+}
+
+func (m *integrationManager) getList() []string {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	ret := make([]string, len(m.names))
+	copy(ret, m.names)
+	sort.Strings(ret)
+	return ret
+}
+
+// RegisterIntegration registeres a new framework, returns an error when
+// attempting to register multiple frameworks with the same name of if a
+// mandatory callback is missing
+func RegisterIntegration(name string, cb IntegrationCallbacks) error {
+	return manager.register(name, cb)
+}
+
+// ForEachIntegration loops through  the registered list of frameworks calling f,
+// if at any point f returns an error the loop is stopped and that error is returned
+func ForEachIntegration(f func(name string, cb IntegrationCallbacks) error) error {
+	return manager.forEach(f)
+}
+
+// GetIntegration looks-up the framework identified by name in the currently registered
+// list of frameworks returning it's callbacks and true if found
+func GetIntegration(name string) (IntegrationCallbacks, bool) {
+	return manager.get(name)
+}
+
+// GetIntegrationsList return the list of currently registered frameworks
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-04-20T16:43:13Z

- **main.go:303** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-20T16:43:13Z
```diff
@@ -287,17 +289,31 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 	}
 	if err != nil {
 		setupLog.Error(err, "unable to load the config")
-		os.Exit(1)
+		return options, cfg, err
+	}
+
+	if cfg.Integrations != nil {
+		var invalidFrameworks []string
+		for _, framework := range cfg.Integrations.Frameworks {
+			if _, found := jobframework.GetIntegration(framework); !found {
+				invalidFrameworks = append(invalidFrameworks, framework)
+			}
+		}
+		if len(invalidFrameworks) > 0 {
```

  Previously from @alculquicondor, I heard that we don't distinguish the empty slice from the nil slice in the kueue project, and we generally verify if the slice is nil in this case. However, since I may remember incorrectly, I would let me know what @alculquicondor thinks.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-04-20T16:50:49Z

- **main.go:213** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-20T16:48:36Z
```diff
@@ -180,46 +193,35 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 		os.Exit(1)
 	}
 
-	if isFrameworkEnabled(cfg, job.FrameworkName) {
-		if err := job.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.JobControllerName),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "Job")
-			os.Exit(1)
-		}
-		if err := job.SetupWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, job.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
+	opts := []jobframework.Option{
+		jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
+		jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
 	}
-
-	if isFrameworkEnabled(cfg, mpijob.FrameworkName) {
-		if err := mpijob.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.KueueName+"-mpijob-controller"),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "MPIJob")
-			os.Exit(1)
-		}
-		if err := mpijob.SetupMPIJobWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, mpijob.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
+	err := jobframework.ForEachIntegration(func(name string, cb jobframework.IntegrationCallbacks) error {
+		log := setupLog.WithValues("integration", name)
+		if isFrameworkEnabled(cfg, name) {
+			if err := cb.NewReconciler(mgr.GetScheme(),
+				mgr.GetClient(),
+				mgr.GetEventRecorderFor(name+"-"+constants.KueueName+"-controller"),
+				opts...,
+			).SetupWithManager(mgr); err != nil {
+				log.Error(err, "unable to create controller")
+				return err
+			}
+			if err := cb.SetupWebhook(mgr, opts...); err != nil {
+				setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
```

  Oh, I was missing :)
  
  If so, we don't need "Job". This means the below:
  
  ```suggestion
  				setupLog.Error(err, "Unable to create webhook", "webhook")
  ```

- **main.go:218** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-20T16:50:36Z
```diff
@@ -180,46 +194,35 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 		os.Exit(1)
 	}
 
-	if isFrameworkEnabled(cfg, job.FrameworkName) {
-		if err := job.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.JobControllerName),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "Job")
-			os.Exit(1)
-		}
-		if err := job.SetupWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, job.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
+	opts := []jobframework.Option{
+		jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
+		jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
 	}
-
-	if isFrameworkEnabled(cfg, mpijob.FrameworkName) {
-		if err := mpijob.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.KueueName+"-mpijob-controller"),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "MPIJob")
-			os.Exit(1)
-		}
-		if err := mpijob.SetupMPIJobWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, mpijob.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
+	err := jobframework.ForEachIntegration(func(name string, cb jobframework.IntegrationCallbacks) error {
+		log := setupLog.WithValues("jobFrameworkName", name)
+		if isFrameworkEnabled(cfg, name) {
+			if err := cb.NewReconciler(mgr.GetScheme(),
+				mgr.GetClient(),
+				mgr.GetEventRecorderFor(name+"-"+constants.KueueName+"-controller"),
+				opts...,
+			).SetupWithManager(mgr); err != nil {
+				log.Error(err, "unable to create controller")
+				return err
+			}
+			if err := cb.SetupWebhook(mgr, opts...); err != nil {
+				setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
+				return err
+			}
+		} else {
+			if err := noop.SetupWebhook(mgr, cb.WebhookType()); err != nil {
+				setupLog.Error(err, "Unable to create webhook", "webhook", "Noop")
```

  As you mentioned, the setupLog already has values, `"jobFrameworkName", "noop"`, right?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-21T05:58:27Z

- **main.go:218** by [@trasc](https://github.com/trasc) — 2023-04-21T05:58:27Z
```diff
@@ -180,46 +194,35 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 		os.Exit(1)
 	}
 
-	if isFrameworkEnabled(cfg, job.FrameworkName) {
-		if err := job.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.JobControllerName),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "Job")
-			os.Exit(1)
-		}
-		if err := job.SetupWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, job.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
+	opts := []jobframework.Option{
+		jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
+		jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
 	}
-
-	if isFrameworkEnabled(cfg, mpijob.FrameworkName) {
-		if err := mpijob.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.KueueName+"-mpijob-controller"),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "MPIJob")
-			os.Exit(1)
-		}
-		if err := mpijob.SetupMPIJobWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, mpijob.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
+	err := jobframework.ForEachIntegration(func(name string, cb jobframework.IntegrationCallbacks) error {
+		log := setupLog.WithValues("jobFrameworkName", name)
+		if isFrameworkEnabled(cfg, name) {
+			if err := cb.NewReconciler(mgr.GetScheme(),
+				mgr.GetClient(),
+				mgr.GetEventRecorderFor(name+"-"+constants.KueueName+"-controller"),
+				opts...,
+			).SetupWithManager(mgr); err != nil {
+				log.Error(err, "unable to create controller")
+				return err
+			}
+			if err := cb.SetupWebhook(mgr, opts...); err != nil {
+				setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
+				return err
+			}
+		} else {
+			if err := noop.SetupWebhook(mgr, cb.WebhookType()); err != nil {
+				setupLog.Error(err, "Unable to create webhook", "webhook", "Noop")
```

  only `log` has it, good catch

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-24T14:58:09Z

- **main.go:206** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-24T14:25:21Z
```diff
@@ -180,46 +194,35 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 		os.Exit(1)
 	}
 
-	if isFrameworkEnabled(cfg, job.FrameworkName) {
-		if err := job.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.JobControllerName),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "Job")
-			os.Exit(1)
-		}
-		if err := job.SetupWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, job.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
+	opts := []jobframework.Option{
+		jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
+		jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
 	}
-
-	if isFrameworkEnabled(cfg, mpijob.FrameworkName) {
-		if err := mpijob.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.KueueName+"-mpijob-controller"),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "MPIJob")
-			os.Exit(1)
-		}
-		if err := mpijob.SetupMPIJobWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, mpijob.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
+	err := jobframework.ForEachIntegration(func(name string, cb jobframework.IntegrationCallbacks) error {
+		log := setupLog.WithValues("jobFrameworkName", name)
+		if isFrameworkEnabled(cfg, name) {
+			if err := cb.NewReconciler(mgr.GetScheme(),
+				mgr.GetClient(),
+				mgr.GetEventRecorderFor(name+"-"+constants.KueueName+"-controller"),
```

  prefer to use `fmt.Sprintf`

- **main.go:210** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-24T14:25:59Z
```diff
@@ -180,46 +194,35 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 		os.Exit(1)
 	}
 
-	if isFrameworkEnabled(cfg, job.FrameworkName) {
-		if err := job.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.JobControllerName),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "Job")
-			os.Exit(1)
-		}
-		if err := job.SetupWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, job.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
+	opts := []jobframework.Option{
+		jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
+		jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
 	}
-
-	if isFrameworkEnabled(cfg, mpijob.FrameworkName) {
-		if err := mpijob.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.KueueName+"-mpijob-controller"),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "MPIJob")
-			os.Exit(1)
-		}
-		if err := mpijob.SetupMPIJobWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, mpijob.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
+	err := jobframework.ForEachIntegration(func(name string, cb jobframework.IntegrationCallbacks) error {
+		log := setupLog.WithValues("jobFrameworkName", name)
+		if isFrameworkEnabled(cfg, name) {
+			if err := cb.NewReconciler(mgr.GetScheme(),
+				mgr.GetClient(),
+				mgr.GetEventRecorderFor(name+"-"+constants.KueueName+"-controller"),
+				opts...,
+			).SetupWithManager(mgr); err != nil {
+				log.Error(err, "unable to create controller")
```

  ```suggestion
  				log.Error(err, "Unable to create controller")
  ```

- **main.go:303** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-24T14:28:26Z
```diff
@@ -287,17 +289,31 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 	}
 	if err != nil {
 		setupLog.Error(err, "unable to load the config")
-		os.Exit(1)
+		return options, cfg, err
+	}
+
+	if cfg.Integrations != nil {
+		var invalidFrameworks []string
+		for _, framework := range cfg.Integrations.Frameworks {
+			if _, found := jobframework.GetIntegration(framework); !found {
+				invalidFrameworks = append(invalidFrameworks, framework)
+			}
+		}
+		if len(invalidFrameworks) > 0 {
```

  Indeed, we don't distinguish between `nil` and empty slice in the kubernetes (not just kueue) project. For that reason, `len(x) > 0` is preferred, because `len(nil) == 0`

- **main.go:304** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-24T14:32:08Z
```diff
@@ -287,17 +290,31 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 	}
 	if err != nil {
 		setupLog.Error(err, "unable to load the config")
-		os.Exit(1)
+		return options, cfg, err
+	}
+
+	if cfg.Integrations != nil {
+		var invalidFrameworks []string
+		for _, framework := range cfg.Integrations.Frameworks {
+			if _, found := jobframework.GetIntegration(framework); !found {
+				invalidFrameworks = append(invalidFrameworks, framework)
+			}
+		}
+		if len(invalidFrameworks) > 0 {
+			err := fmt.Errorf("invalid integration framework(s) %v", invalidFrameworks)
```

  nit: use `field.NotSupported`. Maybe you can produce an error for each invalid value and aggregate them using `field.ErrorList`

- **pkg/controller/jobframework/integrationmanager.go:48** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-24T14:41:12Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"sort"
+	"sync"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+var (
+	errDuplicateFrameworkName  = errors.New("duplicate framework name")
+	errMissingMadatoryCallback = errors.New("mandatory callback missing")
+)
+
+type JobReconcilerInterface interface {
+	SetupWithManager(mgr ctrl.Manager) error
+}
+
+// IntegrationCallbacks groups a set of callbacks used to integrate a new framework.
+type IntegrationCallbacks struct {
+	// NewReconciler creates a new reconciler
+	NewReconciler func(scheme *runtime.Scheme, client client.Client, record record.EventRecorder, opts ...Option) JobReconcilerInterface
+	// SetupWebhook sets up the framework's webhook with the controllers manager
+	SetupWebhook func(mgr ctrl.Manager, opts ...Option) error
+	// WebhookType returns an object of the type managed by the webhook
+	WebhookType func() runtime.Object
```

  This is a bit confusing, let's do this:
  ```suggestion
  	// Type is an object of the job type.
  	Type runtime.Object
  ```
  
  Also, with this change, we can fully get rid of the functions `WebhookType` that exist today.

- **pkg/controller/jobframework/integrationmanager.go:58** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-24T14:43:45Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"sort"
+	"sync"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+var (
+	errDuplicateFrameworkName  = errors.New("duplicate framework name")
+	errMissingMadatoryCallback = errors.New("mandatory callback missing")
+)
+
+type JobReconcilerInterface interface {
+	SetupWithManager(mgr ctrl.Manager) error
+}
+
+// IntegrationCallbacks groups a set of callbacks used to integrate a new framework.
+type IntegrationCallbacks struct {
+	// NewReconciler creates a new reconciler
+	NewReconciler func(scheme *runtime.Scheme, client client.Client, record record.EventRecorder, opts ...Option) JobReconcilerInterface
+	// SetupWebhook sets up the framework's webhook with the controllers manager
+	SetupWebhook func(mgr ctrl.Manager, opts ...Option) error
+	// WebhookType returns an object of the type managed by the webhook
+	WebhookType func() runtime.Object
+	// SetupIndexes registers any additional indexes with the controllers manager
+	// (this callback is optional)
+	SetupIndexes func(ctx context.Context, indexer client.FieldIndexer) error
+	// AddToScheme adds any additional types to the controllers manager's scheme
+	// (this callback is optional)
+	AddToScheme func(s *runtime.Scheme) error
+}
+
+type integrationManager struct {
+	lock sync.RWMutex
```

  Are you sure this is necessary? I think init functions are called serially.

- **pkg/controller/jobframework/integrationmanager.go:73** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-24T14:46:45Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"sort"
+	"sync"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+var (
+	errDuplicateFrameworkName  = errors.New("duplicate framework name")
+	errMissingMadatoryCallback = errors.New("mandatory callback missing")
+)
+
+type JobReconcilerInterface interface {
+	SetupWithManager(mgr ctrl.Manager) error
+}
+
+// IntegrationCallbacks groups a set of callbacks used to integrate a new framework.
+type IntegrationCallbacks struct {
+	// NewReconciler creates a new reconciler
+	NewReconciler func(scheme *runtime.Scheme, client client.Client, record record.EventRecorder, opts ...Option) JobReconcilerInterface
+	// SetupWebhook sets up the framework's webhook with the controllers manager
+	SetupWebhook func(mgr ctrl.Manager, opts ...Option) error
+	// WebhookType returns an object of the type managed by the webhook
+	WebhookType func() runtime.Object
+	// SetupIndexes registers any additional indexes with the controllers manager
+	// (this callback is optional)
+	SetupIndexes func(ctx context.Context, indexer client.FieldIndexer) error
+	// AddToScheme adds any additional types to the controllers manager's scheme
+	// (this callback is optional)
+	AddToScheme func(s *runtime.Scheme) error
+}
+
+type integrationManager struct {
+	lock sync.RWMutex
+
+	names        []string
+	integrations map[string]IntegrationCallbacks
+}
+
+var manager integrationManager
+
+func (m *integrationManager) register(name string, cb IntegrationCallbacks) error {
+	m.lock.Lock()
+	defer m.lock.Unlock()
+	if m.integrations == nil {
+		m.integrations = make(map[string]IntegrationCallbacks)
+	}
+	if _, exists := m.integrations[name]; exists {
+		return fmt.Errorf("%w \"%s\"", errDuplicateFrameworkName, name)
```

  ```suggestion
  		return fmt.Errorf("%w %q", errDuplicateFrameworkName, name)
  ```

- **pkg/controller/jobframework/integrationmanager.go:77** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-24T14:47:22Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"sort"
+	"sync"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+var (
+	errDuplicateFrameworkName  = errors.New("duplicate framework name")
+	errMissingMadatoryCallback = errors.New("mandatory callback missing")
+)
+
+type JobReconcilerInterface interface {
+	SetupWithManager(mgr ctrl.Manager) error
+}
+
+// IntegrationCallbacks groups a set of callbacks used to integrate a new framework.
+type IntegrationCallbacks struct {
+	// NewReconciler creates a new reconciler
+	NewReconciler func(scheme *runtime.Scheme, client client.Client, record record.EventRecorder, opts ...Option) JobReconcilerInterface
+	// SetupWebhook sets up the framework's webhook with the controllers manager
+	SetupWebhook func(mgr ctrl.Manager, opts ...Option) error
+	// WebhookType returns an object of the type managed by the webhook
+	WebhookType func() runtime.Object
+	// SetupIndexes registers any additional indexes with the controllers manager
+	// (this callback is optional)
+	SetupIndexes func(ctx context.Context, indexer client.FieldIndexer) error
+	// AddToScheme adds any additional types to the controllers manager's scheme
+	// (this callback is optional)
+	AddToScheme func(s *runtime.Scheme) error
+}
+
+type integrationManager struct {
+	lock sync.RWMutex
+
+	names        []string
+	integrations map[string]IntegrationCallbacks
+}
+
+var manager integrationManager
+
+func (m *integrationManager) register(name string, cb IntegrationCallbacks) error {
+	m.lock.Lock()
+	defer m.lock.Unlock()
+	if m.integrations == nil {
+		m.integrations = make(map[string]IntegrationCallbacks)
+	}
+	if _, exists := m.integrations[name]; exists {
+		return fmt.Errorf("%w \"%s\"", errDuplicateFrameworkName, name)
+	}
+
+	if cb.NewReconciler == nil {
+		return fmt.Errorf("%w \"NewReconciler\" for \"%s\"", errMissingMadatoryCallback, name)
```

  ```suggestion
  		return fmt.Errorf("%w \"NewReconciler\" for %q", errMissingMadatoryCallback, name)
  ```

- **pkg/controller/jobframework/integrationmanager.go:105** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-24T14:52:30Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"sort"
+	"sync"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+var (
+	errDuplicateFrameworkName  = errors.New("duplicate framework name")
+	errMissingMadatoryCallback = errors.New("mandatory callback missing")
+)
+
+type JobReconcilerInterface interface {
+	SetupWithManager(mgr ctrl.Manager) error
+}
+
+// IntegrationCallbacks groups a set of callbacks used to integrate a new framework.
+type IntegrationCallbacks struct {
+	// NewReconciler creates a new reconciler
+	NewReconciler func(scheme *runtime.Scheme, client client.Client, record record.EventRecorder, opts ...Option) JobReconcilerInterface
+	// SetupWebhook sets up the framework's webhook with the controllers manager
+	SetupWebhook func(mgr ctrl.Manager, opts ...Option) error
+	// WebhookType returns an object of the type managed by the webhook
+	WebhookType func() runtime.Object
+	// SetupIndexes registers any additional indexes with the controllers manager
+	// (this callback is optional)
+	SetupIndexes func(ctx context.Context, indexer client.FieldIndexer) error
+	// AddToScheme adds any additional types to the controllers manager's scheme
+	// (this callback is optional)
+	AddToScheme func(s *runtime.Scheme) error
+}
+
+type integrationManager struct {
+	lock sync.RWMutex
+
+	names        []string
+	integrations map[string]IntegrationCallbacks
+}
+
+var manager integrationManager
+
+func (m *integrationManager) register(name string, cb IntegrationCallbacks) error {
+	m.lock.Lock()
+	defer m.lock.Unlock()
+	if m.integrations == nil {
+		m.integrations = make(map[string]IntegrationCallbacks)
+	}
+	if _, exists := m.integrations[name]; exists {
+		return fmt.Errorf("%w \"%s\"", errDuplicateFrameworkName, name)
+	}
+
+	if cb.NewReconciler == nil {
+		return fmt.Errorf("%w \"NewReconciler\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	if cb.SetupWebhook == nil {
+		return fmt.Errorf("%w \"SetupWebhook\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	if cb.WebhookType == nil {
+		return fmt.Errorf("%w \"WebhookType\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	m.integrations[name] = cb
+	m.names = append(m.names, name)
+
+	return nil
+}
+
+func (m *integrationManager) forEach(f func(name string, cb IntegrationCallbacks) error) error {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	for _, name := range m.names {
+		if err := f(name, m.integrations[name]); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (m *integrationManager) get(name string) (IntegrationCallbacks, bool) {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	cb, f := m.integrations[name]
+	return cb, f
+}
+
+func (m *integrationManager) getList() []string {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	ret := make([]string, len(m.names))
+	copy(ret, m.names)
```

  if we are doing a copy anyways, we can just maintain the map and iterate on the keys in this function.
  
  But this is a nit

- **pkg/controller/jobframework/integrationmanager_test.go:181** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-24T14:54:45Z
```diff
@@ -0,0 +1,259 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"reflect"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+func testNewReconciler(*runtime.Scheme, client.Client, record.EventRecorder, ...Option) JobReconcilerInterface {
+	return nil
+}
+
+func testSetupWebhook(ctrl.Manager, ...Option) error {
+	return nil
+}
+
+func testWebhookType() runtime.Object {
+	return nil
+}
+
+func testSetupIndexes(context.Context, client.FieldIndexer) error {
+	return nil
+}
+
+func testAddToScheme(*runtime.Scheme) error {
+	return nil
+}
+
+var (
+	testIntegrationCallbacks = IntegrationCallbacks{
+		NewReconciler: testNewReconciler,
+		SetupWebhook:  testSetupWebhook,
+		WebhookType:   testWebhookType,
+		SetupIndexes:  testSetupIndexes,
+		AddToScheme:   testAddToScheme,
+	}
+)
+
+func TestRegister(t *testing.T) {
+	cases := map[string]struct {
+		manager              *integrationManager
+		integrationName      string
+		integrationCallbacks IntegrationCallbacks
+		wantError            error
+		wantList             []string
+		wantCallbacks        IntegrationCallbacks
+	}{
+		"successful": {
+			manager: &integrationManager{
+				names: []string{"oldFramework"},
+				integrations: map[string]IntegrationCallbacks{
+					"oldFramework": testIntegrationCallbacks,
+				},
+			},
+			integrationName:      "newFramework",
+			integrationCallbacks: testIntegrationCallbacks,
+			wantError:            nil,
+			wantList:             []string{"newFramework", "oldFramework"},
+			wantCallbacks:        testIntegrationCallbacks,
+		},
+		"duplicate name": {
+			manager: &integrationManager{
+				names: []string{"newFramework"},
+				integrations: map[string]IntegrationCallbacks{
+					"newFramework": testIntegrationCallbacks,
+				},
+			},
+			integrationName:      "newFramework",
+			integrationCallbacks: IntegrationCallbacks{},
+			wantError:            errDuplicateFrameworkName,
+			wantList:             []string{"newFramework"},
+			wantCallbacks:        testIntegrationCallbacks,
+		},
+		"missing NewReconciler": {
+			manager:         &integrationManager{},
+			integrationName: "newFramework",
+			integrationCallbacks: IntegrationCallbacks{
+				SetupWebhook: testSetupWebhook,
+				WebhookType:  testWebhookType,
+				SetupIndexes: testSetupIndexes,
+				AddToScheme:  testAddToScheme,
+			},
+			wantError: errMissingMadatoryCallback,
+			wantList:  []string{},
+		},
+		"missing SetupWebhook": {
+			manager:         &integrationManager{},
+			integrationName: "newFramework",
+			integrationCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				WebhookType:   testWebhookType,
+				SetupIndexes:  testSetupIndexes,
+				AddToScheme:   testAddToScheme,
+			},
+			wantError: errMissingMadatoryCallback,
+			wantList:  []string{},
+		},
+		"missing WebhookType": {
+			manager:         &integrationManager{},
+			integrationName: "newFramework",
+			integrationCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				SetupWebhook:  testSetupWebhook,
+				SetupIndexes:  testSetupIndexes,
+				AddToScheme:   testAddToScheme,
+			},
+			wantError: errMissingMadatoryCallback,
+			wantList:  []string{},
+		},
+		"missing SetupIndexes": {
+			manager:         &integrationManager{},
+			integrationName: "newFramework",
+			integrationCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				SetupWebhook:  testSetupWebhook,
+				WebhookType:   testWebhookType,
+				AddToScheme:   testAddToScheme,
+			},
+			wantError: nil,
+			wantList:  []string{"newFramework"},
+			wantCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				SetupWebhook:  testSetupWebhook,
+				WebhookType:   testWebhookType,
+				AddToScheme:   testAddToScheme,
+			},
+		},
+		"missing AddToScheme": {
+			manager:         &integrationManager{},
+			integrationName: "newFramework",
+			integrationCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				SetupWebhook:  testSetupWebhook,
+				WebhookType:   testWebhookType,
+				SetupIndexes:  testSetupIndexes,
+			},
+			wantError: nil,
+			wantList:  []string{"newFramework"},
+			wantCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				SetupWebhook:  testSetupWebhook,
+				WebhookType:   testWebhookType,
+				SetupIndexes:  testSetupIndexes,
+			},
+		},
+	}
+
+	for tcName, tc := range cases {
+		t.Run(tcName, func(t *testing.T) {
+			gotError := tc.manager.register(tc.integrationName, tc.integrationCallbacks)
+			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); diff != "" {
+				t.Errorf("Unexpected error (-want +got):\n%s", diff)
+			}
+			gotList := tc.manager.getList()
+			if diff := cmp.Diff(tc.wantList, gotList); diff != "" {
+				t.Errorf("Unexpected frameworks list (-want +got):\n%s", diff)
+			}
+
+			if gotCallbacks, found := tc.manager.get(tc.integrationName); found {
+				if diff := cmp.Diff(tc.wantCallbacks, gotCallbacks, cmp.FilterValues(func(_, _ interface{}) bool { return true }, cmp.Comparer(compareCallbacks))); diff != "" {
```

  Is `FilterValues` necessary?

- **pkg/controller/jobframework/integrationmanager_test.go:181** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-24T14:58:02Z
```diff
@@ -0,0 +1,259 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"reflect"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+func testNewReconciler(*runtime.Scheme, client.Client, record.EventRecorder, ...Option) JobReconcilerInterface {
+	return nil
+}
+
+func testSetupWebhook(ctrl.Manager, ...Option) error {
+	return nil
+}
+
+func testWebhookType() runtime.Object {
+	return nil
+}
+
+func testSetupIndexes(context.Context, client.FieldIndexer) error {
+	return nil
+}
+
+func testAddToScheme(*runtime.Scheme) error {
+	return nil
+}
+
+var (
+	testIntegrationCallbacks = IntegrationCallbacks{
+		NewReconciler: testNewReconciler,
+		SetupWebhook:  testSetupWebhook,
+		WebhookType:   testWebhookType,
+		SetupIndexes:  testSetupIndexes,
+		AddToScheme:   testAddToScheme,
+	}
+)
+
+func TestRegister(t *testing.T) {
+	cases := map[string]struct {
+		manager              *integrationManager
+		integrationName      string
+		integrationCallbacks IntegrationCallbacks
+		wantError            error
+		wantList             []string
+		wantCallbacks        IntegrationCallbacks
+	}{
+		"successful": {
+			manager: &integrationManager{
+				names: []string{"oldFramework"},
+				integrations: map[string]IntegrationCallbacks{
+					"oldFramework": testIntegrationCallbacks,
+				},
+			},
+			integrationName:      "newFramework",
+			integrationCallbacks: testIntegrationCallbacks,
+			wantError:            nil,
+			wantList:             []string{"newFramework", "oldFramework"},
+			wantCallbacks:        testIntegrationCallbacks,
+		},
+		"duplicate name": {
+			manager: &integrationManager{
+				names: []string{"newFramework"},
+				integrations: map[string]IntegrationCallbacks{
+					"newFramework": testIntegrationCallbacks,
+				},
+			},
+			integrationName:      "newFramework",
+			integrationCallbacks: IntegrationCallbacks{},
+			wantError:            errDuplicateFrameworkName,
+			wantList:             []string{"newFramework"},
+			wantCallbacks:        testIntegrationCallbacks,
+		},
+		"missing NewReconciler": {
+			manager:         &integrationManager{},
+			integrationName: "newFramework",
+			integrationCallbacks: IntegrationCallbacks{
+				SetupWebhook: testSetupWebhook,
+				WebhookType:  testWebhookType,
+				SetupIndexes: testSetupIndexes,
+				AddToScheme:  testAddToScheme,
+			},
+			wantError: errMissingMadatoryCallback,
+			wantList:  []string{},
+		},
+		"missing SetupWebhook": {
+			manager:         &integrationManager{},
+			integrationName: "newFramework",
+			integrationCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				WebhookType:   testWebhookType,
+				SetupIndexes:  testSetupIndexes,
+				AddToScheme:   testAddToScheme,
+			},
+			wantError: errMissingMadatoryCallback,
+			wantList:  []string{},
+		},
+		"missing WebhookType": {
+			manager:         &integrationManager{},
+			integrationName: "newFramework",
+			integrationCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				SetupWebhook:  testSetupWebhook,
+				SetupIndexes:  testSetupIndexes,
+				AddToScheme:   testAddToScheme,
+			},
+			wantError: errMissingMadatoryCallback,
+			wantList:  []string{},
+		},
+		"missing SetupIndexes": {
+			manager:         &integrationManager{},
+			integrationName: "newFramework",
+			integrationCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				SetupWebhook:  testSetupWebhook,
+				WebhookType:   testWebhookType,
+				AddToScheme:   testAddToScheme,
+			},
+			wantError: nil,
+			wantList:  []string{"newFramework"},
+			wantCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				SetupWebhook:  testSetupWebhook,
+				WebhookType:   testWebhookType,
+				AddToScheme:   testAddToScheme,
+			},
+		},
+		"missing AddToScheme": {
+			manager:         &integrationManager{},
+			integrationName: "newFramework",
+			integrationCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				SetupWebhook:  testSetupWebhook,
+				WebhookType:   testWebhookType,
+				SetupIndexes:  testSetupIndexes,
+			},
+			wantError: nil,
+			wantList:  []string{"newFramework"},
+			wantCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				SetupWebhook:  testSetupWebhook,
+				WebhookType:   testWebhookType,
+				SetupIndexes:  testSetupIndexes,
+			},
+		},
+	}
+
+	for tcName, tc := range cases {
+		t.Run(tcName, func(t *testing.T) {
+			gotError := tc.manager.register(tc.integrationName, tc.integrationCallbacks)
+			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); diff != "" {
+				t.Errorf("Unexpected error (-want +got):\n%s", diff)
+			}
+			gotList := tc.manager.getList()
+			if diff := cmp.Diff(tc.wantList, gotList); diff != "" {
+				t.Errorf("Unexpected frameworks list (-want +got):\n%s", diff)
+			}
+
+			if gotCallbacks, found := tc.manager.get(tc.integrationName); found {
+				if diff := cmp.Diff(tc.wantCallbacks, gotCallbacks, cmp.FilterValues(func(_, _ interface{}) bool { return true }, cmp.Comparer(compareCallbacks))); diff != "" {
```

  Actually, we are not gaining anything by doing `cmp.Diff` here, given that we are comparing each field individually in `compareCallbacks`.
  
  I would just do `tc.wantCallbacks.NewReconciler != gotCallBacks.NewReconciler` and similarly for other fields

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-04-24T15:01:16Z

- **main.go:303** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-24T15:01:16Z
```diff
@@ -287,17 +289,31 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 	}
 	if err != nil {
 		setupLog.Error(err, "unable to load the config")
-		os.Exit(1)
+		return options, cfg, err
+	}
+
+	if cfg.Integrations != nil {
+		var invalidFrameworks []string
+		for _, framework := range cfg.Integrations.Frameworks {
+			if _, found := jobframework.GetIntegration(framework); !found {
+				invalidFrameworks = append(invalidFrameworks, framework)
+			}
+		}
+		if len(invalidFrameworks) > 0 {
```

  Oh, thank you for reminding me.
  sgtm

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-25T09:35:17Z

- **main.go:206** by [@trasc](https://github.com/trasc) — 2023-04-25T07:37:36Z
```diff
@@ -180,46 +194,35 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 		os.Exit(1)
 	}
 
-	if isFrameworkEnabled(cfg, job.FrameworkName) {
-		if err := job.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.JobControllerName),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "Job")
-			os.Exit(1)
-		}
-		if err := job.SetupWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, job.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
+	opts := []jobframework.Option{
+		jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
+		jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
 	}
-
-	if isFrameworkEnabled(cfg, mpijob.FrameworkName) {
-		if err := mpijob.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.KueueName+"-mpijob-controller"),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "MPIJob")
-			os.Exit(1)
-		}
-		if err := mpijob.SetupMPIJobWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, mpijob.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
+	err := jobframework.ForEachIntegration(func(name string, cb jobframework.IntegrationCallbacks) error {
+		log := setupLog.WithValues("jobFrameworkName", name)
+		if isFrameworkEnabled(cfg, name) {
+			if err := cb.NewReconciler(mgr.GetScheme(),
+				mgr.GetClient(),
+				mgr.GetEventRecorderFor(name+"-"+constants.KueueName+"-controller"),
```

  done

- **main.go:210** by [@trasc](https://github.com/trasc) — 2023-04-25T07:37:58Z
```diff
@@ -180,46 +194,35 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 		os.Exit(1)
 	}
 
-	if isFrameworkEnabled(cfg, job.FrameworkName) {
-		if err := job.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.JobControllerName),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "Job")
-			os.Exit(1)
-		}
-		if err := job.SetupWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, job.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "Job")
-			os.Exit(1)
-		}
+	opts := []jobframework.Option{
+		jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
+		jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
 	}
-
-	if isFrameworkEnabled(cfg, mpijob.FrameworkName) {
-		if err := mpijob.NewReconciler(mgr.GetScheme(),
-			mgr.GetClient(),
-			mgr.GetEventRecorderFor(constants.KueueName+"-mpijob-controller"),
-			jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-			jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
-		).SetupWithManager(mgr); err != nil {
-			setupLog.Error(err, "unable to create controller", "controller", "MPIJob")
-			os.Exit(1)
-		}
-		if err := mpijob.SetupMPIJobWebhook(mgr, jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName)); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
-		}
-	} else {
-		if err := noop.SetupWebhook(mgr, mpijob.WebhookType()); err != nil {
-			setupLog.Error(err, "Unable to create webhook", "webhook", "MPIJob")
-			os.Exit(1)
+	err := jobframework.ForEachIntegration(func(name string, cb jobframework.IntegrationCallbacks) error {
+		log := setupLog.WithValues("jobFrameworkName", name)
+		if isFrameworkEnabled(cfg, name) {
+			if err := cb.NewReconciler(mgr.GetScheme(),
+				mgr.GetClient(),
+				mgr.GetEventRecorderFor(name+"-"+constants.KueueName+"-controller"),
+				opts...,
+			).SetupWithManager(mgr); err != nil {
+				log.Error(err, "unable to create controller")
```

  done

- **main.go:304** by [@trasc](https://github.com/trasc) — 2023-04-25T08:38:28Z
```diff
@@ -287,17 +290,31 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 	}
 	if err != nil {
 		setupLog.Error(err, "unable to load the config")
-		os.Exit(1)
+		return options, cfg, err
+	}
+
+	if cfg.Integrations != nil {
+		var invalidFrameworks []string
+		for _, framework := range cfg.Integrations.Frameworks {
+			if _, found := jobframework.GetIntegration(framework); !found {
+				invalidFrameworks = append(invalidFrameworks, framework)
+			}
+		}
+		if len(invalidFrameworks) > 0 {
+			err := fmt.Errorf("invalid integration framework(s) %v", invalidFrameworks)
```

  done

- **pkg/controller/jobframework/integrationmanager.go:48** by [@trasc](https://github.com/trasc) — 2023-04-25T08:45:03Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"sort"
+	"sync"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+var (
+	errDuplicateFrameworkName  = errors.New("duplicate framework name")
+	errMissingMadatoryCallback = errors.New("mandatory callback missing")
+)
+
+type JobReconcilerInterface interface {
+	SetupWithManager(mgr ctrl.Manager) error
+}
+
+// IntegrationCallbacks groups a set of callbacks used to integrate a new framework.
+type IntegrationCallbacks struct {
+	// NewReconciler creates a new reconciler
+	NewReconciler func(scheme *runtime.Scheme, client client.Client, record record.EventRecorder, opts ...Option) JobReconcilerInterface
+	// SetupWebhook sets up the framework's webhook with the controllers manager
+	SetupWebhook func(mgr ctrl.Manager, opts ...Option) error
+	// WebhookType returns an object of the type managed by the webhook
+	WebhookType func() runtime.Object
```

  done

- **pkg/controller/jobframework/integrationmanager.go:58** by [@trasc](https://github.com/trasc) — 2023-04-25T08:49:21Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"sort"
+	"sync"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+var (
+	errDuplicateFrameworkName  = errors.New("duplicate framework name")
+	errMissingMadatoryCallback = errors.New("mandatory callback missing")
+)
+
+type JobReconcilerInterface interface {
+	SetupWithManager(mgr ctrl.Manager) error
+}
+
+// IntegrationCallbacks groups a set of callbacks used to integrate a new framework.
+type IntegrationCallbacks struct {
+	// NewReconciler creates a new reconciler
+	NewReconciler func(scheme *runtime.Scheme, client client.Client, record record.EventRecorder, opts ...Option) JobReconcilerInterface
+	// SetupWebhook sets up the framework's webhook with the controllers manager
+	SetupWebhook func(mgr ctrl.Manager, opts ...Option) error
+	// WebhookType returns an object of the type managed by the webhook
+	WebhookType func() runtime.Object
+	// SetupIndexes registers any additional indexes with the controllers manager
+	// (this callback is optional)
+	SetupIndexes func(ctx context.Context, indexer client.FieldIndexer) error
+	// AddToScheme adds any additional types to the controllers manager's scheme
+	// (this callback is optional)
+	AddToScheme func(s *runtime.Scheme) error
+}
+
+type integrationManager struct {
+	lock sync.RWMutex
```

  The way the code is now,  no, it's not necessary, however in theory , you could start routines in init (I think) or potentially call this from other places.  
  
  Anyway , let's drop it for now.

- **pkg/controller/jobframework/integrationmanager.go:73** by [@trasc](https://github.com/trasc) — 2023-04-25T09:12:44Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"sort"
+	"sync"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+var (
+	errDuplicateFrameworkName  = errors.New("duplicate framework name")
+	errMissingMadatoryCallback = errors.New("mandatory callback missing")
+)
+
+type JobReconcilerInterface interface {
+	SetupWithManager(mgr ctrl.Manager) error
+}
+
+// IntegrationCallbacks groups a set of callbacks used to integrate a new framework.
+type IntegrationCallbacks struct {
+	// NewReconciler creates a new reconciler
+	NewReconciler func(scheme *runtime.Scheme, client client.Client, record record.EventRecorder, opts ...Option) JobReconcilerInterface
+	// SetupWebhook sets up the framework's webhook with the controllers manager
+	SetupWebhook func(mgr ctrl.Manager, opts ...Option) error
+	// WebhookType returns an object of the type managed by the webhook
+	WebhookType func() runtime.Object
+	// SetupIndexes registers any additional indexes with the controllers manager
+	// (this callback is optional)
+	SetupIndexes func(ctx context.Context, indexer client.FieldIndexer) error
+	// AddToScheme adds any additional types to the controllers manager's scheme
+	// (this callback is optional)
+	AddToScheme func(s *runtime.Scheme) error
+}
+
+type integrationManager struct {
+	lock sync.RWMutex
+
+	names        []string
+	integrations map[string]IntegrationCallbacks
+}
+
+var manager integrationManager
+
+func (m *integrationManager) register(name string, cb IntegrationCallbacks) error {
+	m.lock.Lock()
+	defer m.lock.Unlock()
+	if m.integrations == nil {
+		m.integrations = make(map[string]IntegrationCallbacks)
+	}
+	if _, exists := m.integrations[name]; exists {
+		return fmt.Errorf("%w \"%s\"", errDuplicateFrameworkName, name)
```

  done

- **pkg/controller/jobframework/integrationmanager.go:77** by [@trasc](https://github.com/trasc) — 2023-04-25T09:12:52Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"sort"
+	"sync"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+var (
+	errDuplicateFrameworkName  = errors.New("duplicate framework name")
+	errMissingMadatoryCallback = errors.New("mandatory callback missing")
+)
+
+type JobReconcilerInterface interface {
+	SetupWithManager(mgr ctrl.Manager) error
+}
+
+// IntegrationCallbacks groups a set of callbacks used to integrate a new framework.
+type IntegrationCallbacks struct {
+	// NewReconciler creates a new reconciler
+	NewReconciler func(scheme *runtime.Scheme, client client.Client, record record.EventRecorder, opts ...Option) JobReconcilerInterface
+	// SetupWebhook sets up the framework's webhook with the controllers manager
+	SetupWebhook func(mgr ctrl.Manager, opts ...Option) error
+	// WebhookType returns an object of the type managed by the webhook
+	WebhookType func() runtime.Object
+	// SetupIndexes registers any additional indexes with the controllers manager
+	// (this callback is optional)
+	SetupIndexes func(ctx context.Context, indexer client.FieldIndexer) error
+	// AddToScheme adds any additional types to the controllers manager's scheme
+	// (this callback is optional)
+	AddToScheme func(s *runtime.Scheme) error
+}
+
+type integrationManager struct {
+	lock sync.RWMutex
+
+	names        []string
+	integrations map[string]IntegrationCallbacks
+}
+
+var manager integrationManager
+
+func (m *integrationManager) register(name string, cb IntegrationCallbacks) error {
+	m.lock.Lock()
+	defer m.lock.Unlock()
+	if m.integrations == nil {
+		m.integrations = make(map[string]IntegrationCallbacks)
+	}
+	if _, exists := m.integrations[name]; exists {
+		return fmt.Errorf("%w \"%s\"", errDuplicateFrameworkName, name)
+	}
+
+	if cb.NewReconciler == nil {
+		return fmt.Errorf("%w \"NewReconciler\" for \"%s\"", errMissingMadatoryCallback, name)
```

  done

- **pkg/controller/jobframework/integrationmanager.go:105** by [@trasc](https://github.com/trasc) — 2023-04-25T09:15:21Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"sort"
+	"sync"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+var (
+	errDuplicateFrameworkName  = errors.New("duplicate framework name")
+	errMissingMadatoryCallback = errors.New("mandatory callback missing")
+)
+
+type JobReconcilerInterface interface {
+	SetupWithManager(mgr ctrl.Manager) error
+}
+
+// IntegrationCallbacks groups a set of callbacks used to integrate a new framework.
+type IntegrationCallbacks struct {
+	// NewReconciler creates a new reconciler
+	NewReconciler func(scheme *runtime.Scheme, client client.Client, record record.EventRecorder, opts ...Option) JobReconcilerInterface
+	// SetupWebhook sets up the framework's webhook with the controllers manager
+	SetupWebhook func(mgr ctrl.Manager, opts ...Option) error
+	// WebhookType returns an object of the type managed by the webhook
+	WebhookType func() runtime.Object
+	// SetupIndexes registers any additional indexes with the controllers manager
+	// (this callback is optional)
+	SetupIndexes func(ctx context.Context, indexer client.FieldIndexer) error
+	// AddToScheme adds any additional types to the controllers manager's scheme
+	// (this callback is optional)
+	AddToScheme func(s *runtime.Scheme) error
+}
+
+type integrationManager struct {
+	lock sync.RWMutex
+
+	names        []string
+	integrations map[string]IntegrationCallbacks
+}
+
+var manager integrationManager
+
+func (m *integrationManager) register(name string, cb IntegrationCallbacks) error {
+	m.lock.Lock()
+	defer m.lock.Unlock()
+	if m.integrations == nil {
+		m.integrations = make(map[string]IntegrationCallbacks)
+	}
+	if _, exists := m.integrations[name]; exists {
+		return fmt.Errorf("%w \"%s\"", errDuplicateFrameworkName, name)
+	}
+
+	if cb.NewReconciler == nil {
+		return fmt.Errorf("%w \"NewReconciler\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	if cb.SetupWebhook == nil {
+		return fmt.Errorf("%w \"SetupWebhook\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	if cb.WebhookType == nil {
+		return fmt.Errorf("%w \"WebhookType\" for \"%s\"", errMissingMadatoryCallback, name)
+	}
+
+	m.integrations[name] = cb
+	m.names = append(m.names, name)
+
+	return nil
+}
+
+func (m *integrationManager) forEach(f func(name string, cb IntegrationCallbacks) error) error {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	for _, name := range m.names {
+		if err := f(name, m.integrations[name]); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (m *integrationManager) get(name string) (IntegrationCallbacks, bool) {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	cb, f := m.integrations[name]
+	return cb, f
+}
+
+func (m *integrationManager) getList() []string {
+	m.lock.RLock()
+	defer m.lock.RUnlock()
+	ret := make([]string, len(m.names))
+	copy(ret, m.names)
```

  the names slice is also used do have some consistency in the order of forEach calls, I prefer to keep it.

- **pkg/controller/jobframework/integrationmanager_test.go:181** by [@trasc](https://github.com/trasc) — 2023-04-25T09:25:46Z
```diff
@@ -0,0 +1,259 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"reflect"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+func testNewReconciler(*runtime.Scheme, client.Client, record.EventRecorder, ...Option) JobReconcilerInterface {
+	return nil
+}
+
+func testSetupWebhook(ctrl.Manager, ...Option) error {
+	return nil
+}
+
+func testWebhookType() runtime.Object {
+	return nil
+}
+
+func testSetupIndexes(context.Context, client.FieldIndexer) error {
+	return nil
+}
+
+func testAddToScheme(*runtime.Scheme) error {
+	return nil
+}
+
+var (
+	testIntegrationCallbacks = IntegrationCallbacks{
+		NewReconciler: testNewReconciler,
+		SetupWebhook:  testSetupWebhook,
+		WebhookType:   testWebhookType,
+		SetupIndexes:  testSetupIndexes,
+		AddToScheme:   testAddToScheme,
+	}
+)
+
+func TestRegister(t *testing.T) {
+	cases := map[string]struct {
+		manager              *integrationManager
+		integrationName      string
+		integrationCallbacks IntegrationCallbacks
+		wantError            error
+		wantList             []string
+		wantCallbacks        IntegrationCallbacks
+	}{
+		"successful": {
+			manager: &integrationManager{
+				names: []string{"oldFramework"},
+				integrations: map[string]IntegrationCallbacks{
+					"oldFramework": testIntegrationCallbacks,
+				},
+			},
+			integrationName:      "newFramework",
+			integrationCallbacks: testIntegrationCallbacks,
+			wantError:            nil,
+			wantList:             []string{"newFramework", "oldFramework"},
+			wantCallbacks:        testIntegrationCallbacks,
+		},
+		"duplicate name": {
+			manager: &integrationManager{
+				names: []string{"newFramework"},
+				integrations: map[string]IntegrationCallbacks{
+					"newFramework": testIntegrationCallbacks,
+				},
+			},
+			integrationName:      "newFramework",
+			integrationCallbacks: IntegrationCallbacks{},
+			wantError:            errDuplicateFrameworkName,
+			wantList:             []string{"newFramework"},
+			wantCallbacks:        testIntegrationCallbacks,
+		},
+		"missing NewReconciler": {
+			manager:         &integrationManager{},
+			integrationName: "newFramework",
+			integrationCallbacks: IntegrationCallbacks{
+				SetupWebhook: testSetupWebhook,
+				WebhookType:  testWebhookType,
+				SetupIndexes: testSetupIndexes,
+				AddToScheme:  testAddToScheme,
+			},
+			wantError: errMissingMadatoryCallback,
+			wantList:  []string{},
+		},
+		"missing SetupWebhook": {
+			manager:         &integrationManager{},
+			integrationName: "newFramework",
+			integrationCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				WebhookType:   testWebhookType,
+				SetupIndexes:  testSetupIndexes,
+				AddToScheme:   testAddToScheme,
+			},
+			wantError: errMissingMadatoryCallback,
+			wantList:  []string{},
+		},
+		"missing WebhookType": {
+			manager:         &integrationManager{},
+			integrationName: "newFramework",
+			integrationCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				SetupWebhook:  testSetupWebhook,
+				SetupIndexes:  testSetupIndexes,
+				AddToScheme:   testAddToScheme,
+			},
+			wantError: errMissingMadatoryCallback,
+			wantList:  []string{},
+		},
+		"missing SetupIndexes": {
+			manager:         &integrationManager{},
+			integrationName: "newFramework",
+			integrationCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				SetupWebhook:  testSetupWebhook,
+				WebhookType:   testWebhookType,
+				AddToScheme:   testAddToScheme,
+			},
+			wantError: nil,
+			wantList:  []string{"newFramework"},
+			wantCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				SetupWebhook:  testSetupWebhook,
+				WebhookType:   testWebhookType,
+				AddToScheme:   testAddToScheme,
+			},
+		},
+		"missing AddToScheme": {
+			manager:         &integrationManager{},
+			integrationName: "newFramework",
+			integrationCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				SetupWebhook:  testSetupWebhook,
+				WebhookType:   testWebhookType,
+				SetupIndexes:  testSetupIndexes,
+			},
+			wantError: nil,
+			wantList:  []string{"newFramework"},
+			wantCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				SetupWebhook:  testSetupWebhook,
+				WebhookType:   testWebhookType,
+				SetupIndexes:  testSetupIndexes,
+			},
+		},
+	}
+
+	for tcName, tc := range cases {
+		t.Run(tcName, func(t *testing.T) {
+			gotError := tc.manager.register(tc.integrationName, tc.integrationCallbacks)
+			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); diff != "" {
+				t.Errorf("Unexpected error (-want +got):\n%s", diff)
+			}
+			gotList := tc.manager.getList()
+			if diff := cmp.Diff(tc.wantList, gotList); diff != "" {
+				t.Errorf("Unexpected frameworks list (-want +got):\n%s", diff)
+			}
+
+			if gotCallbacks, found := tc.manager.get(tc.integrationName); found {
+				if diff := cmp.Diff(tc.wantCallbacks, gotCallbacks, cmp.FilterValues(func(_, _ interface{}) bool { return true }, cmp.Comparer(compareCallbacks))); diff != "" {
```

  it adds some consistency in the error reporting

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-26T19:41:58Z

A few nits

- **CHANGELOG/CHANGELOG-0.4.md:7** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-26T19:33:12Z
```diff
@@ -4,11 +4,11 @@ Changes since `v0.3.0`:
 
 ### Features
 
+- Change the way job's frameworks are integrated into the controller manager. #695
```

  I would not include this in the release note. This is a cleanup, not a feature.

- **main.go:299** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-26T19:36:32Z
```diff
@@ -287,17 +291,33 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 	}
 	if err != nil {
 		setupLog.Error(err, "unable to load the config")
-		os.Exit(1)
+		return options, cfg, err
+	}
+
+	if cfg.Integrations != nil {
+		var errorlist field.ErrorList
+		avaiableFrameworks := jobframework.GetIntegrationsList()
```

  ```suggestion
  		availableFrameworks := jobframework.GetIntegrationsList()
  ```

- **pkg/controller/jobframework/integrationmanager_test.go:181** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-26T19:38:17Z
```diff
@@ -0,0 +1,259 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"reflect"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+func testNewReconciler(*runtime.Scheme, client.Client, record.EventRecorder, ...Option) JobReconcilerInterface {
+	return nil
+}
+
+func testSetupWebhook(ctrl.Manager, ...Option) error {
+	return nil
+}
+
+func testWebhookType() runtime.Object {
+	return nil
+}
+
+func testSetupIndexes(context.Context, client.FieldIndexer) error {
+	return nil
+}
+
+func testAddToScheme(*runtime.Scheme) error {
+	return nil
+}
+
+var (
+	testIntegrationCallbacks = IntegrationCallbacks{
+		NewReconciler: testNewReconciler,
+		SetupWebhook:  testSetupWebhook,
+		WebhookType:   testWebhookType,
+		SetupIndexes:  testSetupIndexes,
+		AddToScheme:   testAddToScheme,
+	}
+)
+
+func TestRegister(t *testing.T) {
+	cases := map[string]struct {
+		manager              *integrationManager
+		integrationName      string
+		integrationCallbacks IntegrationCallbacks
+		wantError            error
+		wantList             []string
+		wantCallbacks        IntegrationCallbacks
+	}{
+		"successful": {
+			manager: &integrationManager{
+				names: []string{"oldFramework"},
+				integrations: map[string]IntegrationCallbacks{
+					"oldFramework": testIntegrationCallbacks,
+				},
+			},
+			integrationName:      "newFramework",
+			integrationCallbacks: testIntegrationCallbacks,
+			wantError:            nil,
+			wantList:             []string{"newFramework", "oldFramework"},
+			wantCallbacks:        testIntegrationCallbacks,
+		},
+		"duplicate name": {
+			manager: &integrationManager{
+				names: []string{"newFramework"},
+				integrations: map[string]IntegrationCallbacks{
+					"newFramework": testIntegrationCallbacks,
+				},
+			},
+			integrationName:      "newFramework",
+			integrationCallbacks: IntegrationCallbacks{},
+			wantError:            errDuplicateFrameworkName,
+			wantList:             []string{"newFramework"},
+			wantCallbacks:        testIntegrationCallbacks,
+		},
+		"missing NewReconciler": {
+			manager:         &integrationManager{},
+			integrationName: "newFramework",
+			integrationCallbacks: IntegrationCallbacks{
+				SetupWebhook: testSetupWebhook,
+				WebhookType:  testWebhookType,
+				SetupIndexes: testSetupIndexes,
+				AddToScheme:  testAddToScheme,
+			},
+			wantError: errMissingMadatoryCallback,
+			wantList:  []string{},
+		},
+		"missing SetupWebhook": {
+			manager:         &integrationManager{},
+			integrationName: "newFramework",
+			integrationCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				WebhookType:   testWebhookType,
+				SetupIndexes:  testSetupIndexes,
+				AddToScheme:   testAddToScheme,
+			},
+			wantError: errMissingMadatoryCallback,
+			wantList:  []string{},
+		},
+		"missing WebhookType": {
+			manager:         &integrationManager{},
+			integrationName: "newFramework",
+			integrationCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				SetupWebhook:  testSetupWebhook,
+				SetupIndexes:  testSetupIndexes,
+				AddToScheme:   testAddToScheme,
+			},
+			wantError: errMissingMadatoryCallback,
+			wantList:  []string{},
+		},
+		"missing SetupIndexes": {
+			manager:         &integrationManager{},
+			integrationName: "newFramework",
+			integrationCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				SetupWebhook:  testSetupWebhook,
+				WebhookType:   testWebhookType,
+				AddToScheme:   testAddToScheme,
+			},
+			wantError: nil,
+			wantList:  []string{"newFramework"},
+			wantCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				SetupWebhook:  testSetupWebhook,
+				WebhookType:   testWebhookType,
+				AddToScheme:   testAddToScheme,
+			},
+		},
+		"missing AddToScheme": {
+			manager:         &integrationManager{},
+			integrationName: "newFramework",
+			integrationCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				SetupWebhook:  testSetupWebhook,
+				WebhookType:   testWebhookType,
+				SetupIndexes:  testSetupIndexes,
+			},
+			wantError: nil,
+			wantList:  []string{"newFramework"},
+			wantCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				SetupWebhook:  testSetupWebhook,
+				WebhookType:   testWebhookType,
+				SetupIndexes:  testSetupIndexes,
+			},
+		},
+	}
+
+	for tcName, tc := range cases {
+		t.Run(tcName, func(t *testing.T) {
+			gotError := tc.manager.register(tc.integrationName, tc.integrationCallbacks)
+			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); diff != "" {
+				t.Errorf("Unexpected error (-want +got):\n%s", diff)
+			}
+			gotList := tc.manager.getList()
+			if diff := cmp.Diff(tc.wantList, gotList); diff != "" {
+				t.Errorf("Unexpected frameworks list (-want +got):\n%s", diff)
+			}
+
+			if gotCallbacks, found := tc.manager.get(tc.integrationName); found {
+				if diff := cmp.Diff(tc.wantCallbacks, gotCallbacks, cmp.FilterValues(func(_, _ interface{}) bool { return true }, cmp.Comparer(compareCallbacks))); diff != "" {
```

  Is `FilterValues` necessary?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-26T19:56:06Z

- **CHANGELOG/CHANGELOG-0.4.md:7** by [@trasc](https://github.com/trasc) — 2023-04-26T19:56:06Z
```diff
@@ -4,11 +4,11 @@ Changes since `v0.3.0`:
 
 ### Features
 
+- Change the way job's frameworks are integrated into the controller manager. #695
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-26T19:57:13Z

- **main.go:299** by [@trasc](https://github.com/trasc) — 2023-04-26T19:57:13Z
```diff
@@ -287,17 +291,33 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 	}
 	if err != nil {
 		setupLog.Error(err, "unable to load the config")
-		os.Exit(1)
+		return options, cfg, err
+	}
+
+	if cfg.Integrations != nil {
+		var errorlist field.ErrorList
+		avaiableFrameworks := jobframework.GetIntegrationsList()
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-26T20:04:21Z

- **pkg/controller/jobframework/integrationmanager_test.go:181** by [@trasc](https://github.com/trasc) — 2023-04-26T20:04:21Z
```diff
@@ -0,0 +1,259 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"reflect"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+func testNewReconciler(*runtime.Scheme, client.Client, record.EventRecorder, ...Option) JobReconcilerInterface {
+	return nil
+}
+
+func testSetupWebhook(ctrl.Manager, ...Option) error {
+	return nil
+}
+
+func testWebhookType() runtime.Object {
+	return nil
+}
+
+func testSetupIndexes(context.Context, client.FieldIndexer) error {
+	return nil
+}
+
+func testAddToScheme(*runtime.Scheme) error {
+	return nil
+}
+
+var (
+	testIntegrationCallbacks = IntegrationCallbacks{
+		NewReconciler: testNewReconciler,
+		SetupWebhook:  testSetupWebhook,
+		WebhookType:   testWebhookType,
+		SetupIndexes:  testSetupIndexes,
+		AddToScheme:   testAddToScheme,
+	}
+)
+
+func TestRegister(t *testing.T) {
+	cases := map[string]struct {
+		manager              *integrationManager
+		integrationName      string
+		integrationCallbacks IntegrationCallbacks
+		wantError            error
+		wantList             []string
+		wantCallbacks        IntegrationCallbacks
+	}{
+		"successful": {
+			manager: &integrationManager{
+				names: []string{"oldFramework"},
+				integrations: map[string]IntegrationCallbacks{
+					"oldFramework": testIntegrationCallbacks,
+				},
+			},
+			integrationName:      "newFramework",
+			integrationCallbacks: testIntegrationCallbacks,
+			wantError:            nil,
+			wantList:             []string{"newFramework", "oldFramework"},
+			wantCallbacks:        testIntegrationCallbacks,
+		},
+		"duplicate name": {
+			manager: &integrationManager{
+				names: []string{"newFramework"},
+				integrations: map[string]IntegrationCallbacks{
+					"newFramework": testIntegrationCallbacks,
+				},
+			},
+			integrationName:      "newFramework",
+			integrationCallbacks: IntegrationCallbacks{},
+			wantError:            errDuplicateFrameworkName,
+			wantList:             []string{"newFramework"},
+			wantCallbacks:        testIntegrationCallbacks,
+		},
+		"missing NewReconciler": {
+			manager:         &integrationManager{},
+			integrationName: "newFramework",
+			integrationCallbacks: IntegrationCallbacks{
+				SetupWebhook: testSetupWebhook,
+				WebhookType:  testWebhookType,
+				SetupIndexes: testSetupIndexes,
+				AddToScheme:  testAddToScheme,
+			},
+			wantError: errMissingMadatoryCallback,
+			wantList:  []string{},
+		},
+		"missing SetupWebhook": {
+			manager:         &integrationManager{},
+			integrationName: "newFramework",
+			integrationCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				WebhookType:   testWebhookType,
+				SetupIndexes:  testSetupIndexes,
+				AddToScheme:   testAddToScheme,
+			},
+			wantError: errMissingMadatoryCallback,
+			wantList:  []string{},
+		},
+		"missing WebhookType": {
+			manager:         &integrationManager{},
+			integrationName: "newFramework",
+			integrationCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				SetupWebhook:  testSetupWebhook,
+				SetupIndexes:  testSetupIndexes,
+				AddToScheme:   testAddToScheme,
+			},
+			wantError: errMissingMadatoryCallback,
+			wantList:  []string{},
+		},
+		"missing SetupIndexes": {
+			manager:         &integrationManager{},
+			integrationName: "newFramework",
+			integrationCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				SetupWebhook:  testSetupWebhook,
+				WebhookType:   testWebhookType,
+				AddToScheme:   testAddToScheme,
+			},
+			wantError: nil,
+			wantList:  []string{"newFramework"},
+			wantCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				SetupWebhook:  testSetupWebhook,
+				WebhookType:   testWebhookType,
+				AddToScheme:   testAddToScheme,
+			},
+		},
+		"missing AddToScheme": {
+			manager:         &integrationManager{},
+			integrationName: "newFramework",
+			integrationCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				SetupWebhook:  testSetupWebhook,
+				WebhookType:   testWebhookType,
+				SetupIndexes:  testSetupIndexes,
+			},
+			wantError: nil,
+			wantList:  []string{"newFramework"},
+			wantCallbacks: IntegrationCallbacks{
+				NewReconciler: testNewReconciler,
+				SetupWebhook:  testSetupWebhook,
+				WebhookType:   testWebhookType,
+				SetupIndexes:  testSetupIndexes,
+			},
+		},
+	}
+
+	for tcName, tc := range cases {
+		t.Run(tcName, func(t *testing.T) {
+			gotError := tc.manager.register(tc.integrationName, tc.integrationCallbacks)
+			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); diff != "" {
+				t.Errorf("Unexpected error (-want +got):\n%s", diff)
+			}
+			gotList := tc.manager.getList()
+			if diff := cmp.Diff(tc.wantList, gotList); diff != "" {
+				t.Errorf("Unexpected frameworks list (-want +got):\n%s", diff)
+			}
+
+			if gotCallbacks, found := tc.manager.get(tc.integrationName); found {
+				if diff := cmp.Diff(tc.wantCallbacks, gotCallbacks, cmp.FilterValues(func(_, _ interface{}) bool { return true }, cmp.Comparer(compareCallbacks))); diff != "" {
```

  unfortunately yes
