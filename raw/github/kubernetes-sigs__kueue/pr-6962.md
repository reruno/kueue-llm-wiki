# PR #6962: [Prep] Wrap ApplyAdmissionStatus with PatchAdmissionStatus

**Summary**: [Prep] Wrap ApplyAdmissionStatus with PatchAdmissionStatus

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6962

**Last updated**: 2025-09-24T13:10:32Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `feat/6158-introduce-patch-admission-status` → `main`
- **Created**: 2025-09-23T13:51:56Z
- **Updated**: 2025-09-24T13:10:32Z
- **Merged**: 2025-09-24T12:00:23Z
- **Closed**: 2025-09-24T12:00:23Z
- **Labels**: `lgtm`, `release-note-none`, `size/XL`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 18   **+334 / -178**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Preparatory PR to introduce a wrap around ApplyAdmissionStatus to later allow for switch between Apply and Merge.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to #6158

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-09-23T13:52:04Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c026f18def688bfdb8d3f0cc7b7a656191bedba2 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68d3b5b3cdd0b4000773aa67 |

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-09-23T13:59:40Z

/cc @mbobrovskyi @mimowo

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-24T09:14:13Z

/lgtm
Thank you so much 🙏!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-24T09:14:21Z

LGTM label has been added.  <details>Git tree hash: 9853d689306a249c07636ed2e6d2037682a9fbeb</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-24T09:14:27Z

@mimowo PTAL

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-24T11:31:54Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6962#pullrequestreview-3262452370" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6962#" title="Author self-approved">mszadkow</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-24T11:31:55Z

cc @mwysokin

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-23T14:02:09Z

Awesome! This PR will help reviewing the main one a lot 👍 

cc @mwysokin

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-23T14:14:51Z

- **cmd/importer/pod/import.go:204** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-23T14:14:51Z
```diff
@@ -201,7 +201,9 @@ func createWorkload(ctx context.Context, c client.Client, wl *kueue.Workload) er
 
 func admitWorkload(ctx context.Context, c client.Client, wl *kueue.Workload) error {
 	var realClock = clock.RealClock{}
-	err := workload.ApplyAdmissionStatus(ctx, c, wl, false, realClock)
+	err := workload.PatchAdmissionStatus(ctx, c, wl, false, realClock, func() (*kueue.Workload, bool, error) {
```

  I think here is potential flaky issues with strict=false after migration to merge patch.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-23T14:21:04Z

- **pkg/scheduler/scheduler.go:317** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-23T14:21:05Z
```diff
@@ -315,7 +315,9 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			// Block admission until all currently admitted workloads are in
 			// PodsReady condition if the waitForPodsReady is enabled
 			workload.UnsetQuotaReservationWithCondition(e.Obj, "Waiting", "waiting for all admitted workloads to be in PodsReady condition", s.clock.Now())
-			if err := workload.ApplyAdmissionStatus(ctx, s.client, e.Obj, false, s.clock); err != nil {
+			if err := workload.PatchAdmissionStatus(ctx, s.client, e.Obj, false, s.clock, func() (*kueue.Workload, bool, error) {
```

  Here as well — there’s a potential risk of overwriting Conditions. We should revisit this and maybe refactor the scheduler in a follow-up to use strict=true.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-23T14:23:50Z

- **pkg/scheduler/scheduler_test.go:3869** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-23T14:23:50Z
```diff
@@ -3687,7 +3687,7 @@ func TestSchedule(t *testing.T) {
 
 			gotScheduled := make(map[workload.Reference]kueue.Admission)
 			var mu sync.Mutex
-			scheduler.applyAdmission = func(ctx context.Context, w *kueue.Workload) error {
+			scheduler.patchAdmission = func(ctx context.Context, w *kueue.Workload) error {
```

  I propose avoid patchAdmission at all and use InterceptorFuncs instead. This could be optimized in a follow-up PR.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-23T14:29:17Z

- **pkg/controller/core/workload_controller.go:266** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-23T14:29:17Z
```diff
@@ -263,7 +263,9 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		}
 
 		if updated {
-			return ctrl.Result{}, client.IgnoreNotFound(workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock))
+			return ctrl.Result{}, client.IgnoreNotFound(workload.PatchAdmissionStatus(ctx, r.client, &wl, true, r.clock, func() (*kueue.Workload, bool, error) {
```

  Shouldn't we use origWl here?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-23T14:30:21Z

- **pkg/controller/core/workload_controller.go:266** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-23T14:30:21Z
```diff
@@ -263,7 +263,9 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		}
 
 		if updated {
-			return ctrl.Result{}, client.IgnoreNotFound(workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock))
+			return ctrl.Result{}, client.IgnoreNotFound(workload.PatchAdmissionStatus(ctx, r.client, &wl, true, r.clock, func() (*kueue.Workload, bool, error) {
```

  Or maybe move if condition inside callback function.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-23T14:31:16Z

- **pkg/controller/core/workload_controller.go:299** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-23T14:31:17Z
```diff
@@ -294,7 +296,9 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 					}
 				}
 			} else {
-				if err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock); err != nil {
+				if err := workload.PatchAdmissionStatus(ctx, r.client, &wl, true, r.clock, func() (*kueue.Workload, bool, error) {
```

  Also here.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-23T14:31:39Z

- **pkg/scheduler/scheduler.go:317** by [@mimowo](https://github.com/mimowo) — 2025-09-23T14:31:39Z
```diff
@@ -315,7 +315,9 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			// Block admission until all currently admitted workloads are in
 			// PodsReady condition if the waitForPodsReady is enabled
 			workload.UnsetQuotaReservationWithCondition(e.Obj, "Waiting", "waiting for all admitted workloads to be in PodsReady condition", s.clock.Now())
-			if err := workload.ApplyAdmissionStatus(ctx, s.client, e.Obj, false, s.clock); err != nil {
+			if err := workload.PatchAdmissionStatus(ctx, s.client, e.Obj, false, s.clock, func() (*kueue.Workload, bool, error) {
```

  We can use Strict=false in the old mode (apply) but strict=false in the new mode (Patch).
  
  For example StrictPatch(true) could be overrding only for patch.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-23T14:34:00Z

- **pkg/controller/jobframework/reconciler.go:539** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-23T14:34:00Z
```diff
@@ -537,7 +537,9 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 				setRequeued := evCond.Reason == kueue.WorkloadEvictedByPreemption
 				workload.SetRequeuedCondition(wl, evCond.Reason, evCond.Message, setRequeued)
 				_ = workload.UnsetQuotaReservationWithCondition(wl, "Pending", evCond.Message, r.clock.Now())
```

  Please also move it inside callback function.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-23T14:35:45Z

- **pkg/scheduler/scheduler.go:317** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-23T14:35:45Z
```diff
@@ -315,7 +315,9 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			// Block admission until all currently admitted workloads are in
 			// PodsReady condition if the waitForPodsReady is enabled
 			workload.UnsetQuotaReservationWithCondition(e.Obj, "Waiting", "waiting for all admitted workloads to be in PodsReady condition", s.clock.Now())
```

  I think this should be inside callback as well.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-23T14:38:09Z

- **pkg/scheduler/scheduler.go:774** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-23T14:38:09Z
```diff
@@ -768,13 +772,12 @@ func (s *Scheduler) requeueAndUpdate(ctx context.Context, e entry) {
 	log.V(2).Info("Workload re-queued", "workload", klog.KObj(e.Obj), "clusterQueue", klog.KRef("", string(e.ClusterQueue)), "queue", klog.KRef(e.Obj.Namespace, string(e.Obj.Spec.QueueName)), "requeueReason", e.requeueReason, "added", added, "status", e.status)
 
 	if e.status == notNominated || e.status == skipped {
-		patch := workload.PrepareWorkloadPatch(e.Obj, true, s.clock)
-		reservationIsChanged := workload.UnsetQuotaReservationWithCondition(patch, "Pending", e.inadmissibleMsg, s.clock.Now())
-		resourceRequestsIsChanged := workload.PropagateResourceRequests(patch, &e.Info)
-		if reservationIsChanged || resourceRequestsIsChanged {
-			if err := workload.ApplyAdmissionStatusPatch(ctx, s.client, patch); err != nil {
-				log.Error(err, "Could not update Workload status")
-			}
+		if err := workload.PatchAdmissionStatus(ctx, s.client, e.Obj, true, s.clock, func() (*kueue.Workload, bool, error) {
```

  Probably we should send original object as parameter.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-23T14:44:17Z

- **pkg/scheduler/scheduler.go:317** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-23T14:44:17Z
```diff
@@ -315,7 +315,9 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			// Block admission until all currently admitted workloads are in
 			// PodsReady condition if the waitForPodsReady is enabled
 			workload.UnsetQuotaReservationWithCondition(e.Obj, "Waiting", "waiting for all admitted workloads to be in PodsReady condition", s.clock.Now())
-			if err := workload.ApplyAdmissionStatus(ctx, s.client, e.Obj, false, s.clock); err != nil {
+			if err := workload.PatchAdmissionStatus(ctx, s.client, e.Obj, false, s.clock, func() (*kueue.Workload, bool, error) {
```

  You mean strict=true for Patch, right? The problem is that we’ll requeue the workload on each conflict. I don’t think that’s ideal, since it would hurt performance in that case.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-23T14:57:05Z

- **pkg/scheduler/scheduler.go:317** by [@mimowo](https://github.com/mimowo) — 2025-09-23T14:57:05Z
```diff
@@ -315,7 +315,9 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			// Block admission until all currently admitted workloads are in
 			// PodsReady condition if the waitForPodsReady is enabled
 			workload.UnsetQuotaReservationWithCondition(e.Obj, "Waiting", "waiting for all admitted workloads to be in PodsReady condition", s.clock.Now())
-			if err := workload.ApplyAdmissionStatus(ctx, s.client, e.Obj, false, s.clock); err != nil {
+			if err := workload.PatchAdmissionStatus(ctx, s.client, e.Obj, false, s.clock, func() (*kueue.Workload, bool, error) {
```

  > You mean strict=true for Patch, right? The problem is that we’ll requeue the workload on each conflict. I don’t think that’s ideal, since it would hurt performance in that case.
  
  Yes, but this is Alpha. We can see if this is a problem based on user feedback. I don't expect this to be a major problem TBH.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-23T14:59:00Z

- **pkg/scheduler/scheduler.go:317** by [@mimowo](https://github.com/mimowo) — 2025-09-23T14:59:00Z
```diff
@@ -315,7 +315,9 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			// Block admission until all currently admitted workloads are in
 			// PodsReady condition if the waitForPodsReady is enabled
 			workload.UnsetQuotaReservationWithCondition(e.Obj, "Waiting", "waiting for all admitted workloads to be in PodsReady condition", s.clock.Now())
-			if err := workload.ApplyAdmissionStatus(ctx, s.client, e.Obj, false, s.clock); err != nil {
+			if err := workload.PatchAdmissionStatus(ctx, s.client, e.Obj, false, s.clock, func() (*kueue.Workload, bool, error) {
```

  If this is an issue we can improve before Beta the performance if you have some refactoring idea for scheduler. "We should revisit this and maybe refactor the scheduler in a follow-up to use strict=true.". 
  
  However, for 0.14 the priority is (1.) make the build passing, (2.) allow using Patch, even if at the cost of some performance penalty. Improving perf is nice to have for Alpha.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-23T19:13:12Z

- **pkg/scheduler/scheduler.go:774** by [@mszadkow](https://github.com/mszadkow) — 2025-09-23T19:13:12Z
```diff
@@ -768,13 +772,12 @@ func (s *Scheduler) requeueAndUpdate(ctx context.Context, e entry) {
 	log.V(2).Info("Workload re-queued", "workload", klog.KObj(e.Obj), "clusterQueue", klog.KRef("", string(e.ClusterQueue)), "queue", klog.KRef(e.Obj.Namespace, string(e.Obj.Spec.QueueName)), "requeueReason", e.requeueReason, "added", added, "status", e.status)
 
 	if e.status == notNominated || e.status == skipped {
-		patch := workload.PrepareWorkloadPatch(e.Obj, true, s.clock)
-		reservationIsChanged := workload.UnsetQuotaReservationWithCondition(patch, "Pending", e.inadmissibleMsg, s.clock.Now())
-		resourceRequestsIsChanged := workload.PropagateResourceRequests(patch, &e.Info)
-		if reservationIsChanged || resourceRequestsIsChanged {
-			if err := workload.ApplyAdmissionStatusPatch(ctx, s.client, patch); err != nil {
-				log.Error(err, "Could not update Workload status")
-			}
+		if err := workload.PatchAdmissionStatus(ctx, s.client, e.Obj, true, s.clock, func() (*kueue.Workload, bool, error) {
```

  Yes, correct.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-24T05:39:45Z

- **pkg/scheduler/scheduler.go:317** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-24T05:39:45Z
```diff
@@ -315,7 +315,9 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			// Block admission until all currently admitted workloads are in
 			// PodsReady condition if the waitForPodsReady is enabled
 			workload.UnsetQuotaReservationWithCondition(e.Obj, "Waiting", "waiting for all admitted workloads to be in PodsReady condition", s.clock.Now())
-			if err := workload.ApplyAdmissionStatus(ctx, s.client, e.Obj, false, s.clock); err != nil {
+			if err := workload.PatchAdmissionStatus(ctx, s.client, e.Obj, false, s.clock, func() (*kueue.Workload, bool, error) {
```

  I also found an error:
  
  ```
  2025-09-23T10:22:18.788073478Z  ERROR   scheduler       scheduler/scheduler.go:776      Could not update Workload status        {"schedulingCycle": 395, "error": "Operation cannot be fulfilled on workloads.kueue.x-k8s.io \"jobset-jobset-1eba3\": the object has been modified; please apply your changes to the latest version and try again"}
  sigs.k8s.io/kueue/pkg/scheduler.(*Scheduler).requeueAndUpdate
          /workspace/pkg/scheduler/scheduler.go:776
  sigs.k8s.io/kueue/pkg/scheduler.(*Scheduler).schedule
          /workspace/pkg/scheduler/scheduler.go:350
  sigs.k8s.io/kueue/pkg/util/wait.untilWithBackoff.func1
          /workspace/pkg/util/wait/backoff.go:43
  k8s.io/apimachinery/pkg/util/wait.BackoffUntil.func1
          /workspace/vendor/k8s.io/apimachinery/pkg/util/wait/backoff.go:233
  k8s.io/apimachinery/pkg/util/wait.BackoffUntilWithContext.func1
          /workspace/vendor/k8s.io/apimachinery/pkg/util/wait/backoff.go:255
  k8s.io/apimachinery/pkg/util/wait.BackoffUntilWithContext
          /workspace/vendor/k8s.io/apimachinery/pkg/util/wait/backoff.go:256
  k8s.io/apimachinery/pkg/util/wait.BackoffUntil
          /workspace/vendor/k8s.io/apimachinery/pkg/util/wait/backoff.go:233
  sigs.k8s.io/kueue/pkg/util/wait.untilWithBackoff
          /workspace/pkg/util/wait/backoff.go:42
  sigs.k8s.io/kueue/pkg/util/wait.UntilWithBackoff
          /workspace/pkg/util/wait/backoff.go:34
  ```
  
  We’re trying to requeue in strict mode, but we hit a conflict error. This prevents us from updating the workload and sending an event.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-24T05:53:32Z

- **pkg/scheduler/scheduler.go:317** by [@mimowo](https://github.com/mimowo) — 2025-09-24T05:53:32Z
```diff
@@ -315,7 +315,9 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			// Block admission until all currently admitted workloads are in
 			// PodsReady condition if the waitForPodsReady is enabled
 			workload.UnsetQuotaReservationWithCondition(e.Obj, "Waiting", "waiting for all admitted workloads to be in PodsReady condition", s.clock.Now())
-			if err := workload.ApplyAdmissionStatus(ctx, s.client, e.Obj, false, s.clock); err != nil {
+			if err := workload.PatchAdmissionStatus(ctx, s.client, e.Obj, false, s.clock, func() (*kueue.Workload, bool, error) {
```

  I see, does it get ok after a while then workload_controller updates the in memory copy of the workload or it gets stuck forever?
  
  1. If this gets stuck then we have a bigger issue (probably not just for strict use because any request can fail)
  
  2. if the workload gets admitted after a while then we can measure the performance impact with the integration perf tests. 
  
  Please check which option this is and if 2. then what is the impact overall.
  
  Ideally, I think we should issue a Get to the workload from scheduler to get latest copy, we can do it only when the new feature gate is enabled. This can be done in a follow up PR depending on the investigation

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-24T06:05:34Z

- **pkg/scheduler/scheduler_test.go:3869** by [@mszadkow](https://github.com/mszadkow) — 2025-09-24T06:05:34Z
```diff
@@ -3687,7 +3687,7 @@ func TestSchedule(t *testing.T) {
 
 			gotScheduled := make(map[workload.Reference]kueue.Admission)
 			var mu sync.Mutex
-			scheduler.applyAdmission = func(ctx context.Context, w *kueue.Workload) error {
+			scheduler.patchAdmission = func(ctx context.Context, w *kueue.Workload) error {
```

  Yes, I remember we had the discussion, but I had closer look at the tests that use this "stub" and they already have the interceptor for something else.
  This change will require rethinking of scheduler tests imho

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-24T06:19:17Z

- **pkg/scheduler/scheduler.go:317** by [@mimowo](https://github.com/mimowo) — 2025-09-24T06:19:17Z
```diff
@@ -315,7 +315,9 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			// Block admission until all currently admitted workloads are in
 			// PodsReady condition if the waitForPodsReady is enabled
 			workload.UnsetQuotaReservationWithCondition(e.Obj, "Waiting", "waiting for all admitted workloads to be in PodsReady condition", s.clock.Now())
-			if err := workload.ApplyAdmissionStatus(ctx, s.client, e.Obj, false, s.clock); err != nil {
+			if err := workload.PatchAdmissionStatus(ctx, s.client, e.Obj, false, s.clock, func() (*kueue.Workload, bool, error) {
```

  In any case as Mike said it the first comment I believe it can be done in the main PR introducing the feature gate.
  
  I'm not sure there is a way to prevent the risk of overriding conditions other than just using strict unfortunately. Alternatively we just accept there is this risk, but this is tricky decision. 
  
  For now we are not graduating to Beta yet.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-24T06:35:07Z

- **pkg/controller/core/workload_controller.go:266** by [@mszadkow](https://github.com/mszadkow) — 2025-09-24T06:35:07Z
```diff
@@ -263,7 +263,9 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		}
 
 		if updated {
-			return ctrl.Result{}, client.IgnoreNotFound(workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock))
+			return ctrl.Result{}, client.IgnoreNotFound(workload.PatchAdmissionStatus(ctx, r.client, &wl, true, r.clock, func() (*kueue.Workload, bool, error) {
```

  rather wlOrig, but moving this inside would be refactor and actually wrong, cause we don't end the fn here if not update

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-24T07:05:29Z

- **cmd/importer/pod/import.go:204** by [@mszadkow](https://github.com/mszadkow) — 2025-09-24T07:05:29Z
```diff
@@ -201,7 +201,9 @@ func createWorkload(ctx context.Context, c client.Client, wl *kueue.Workload) er
 
 func admitWorkload(ctx context.Context, c client.Client, wl *kueue.Workload) error {
 	var realClock = clock.RealClock{}
-	err := workload.ApplyAdmissionStatus(ctx, c, wl, false, realClock)
+	err := workload.PatchAdmissionStatus(ctx, c, wl, false, realClock, func() (*kueue.Workload, bool, error) {
```

  I believe it should be the same as here: https://github.com/kubernetes-sigs/kueue/pull/6962#discussion_r2374490718

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-24T07:06:19Z

- **pkg/controller/core/workload_controller.go:299** by [@mszadkow](https://github.com/mszadkow) — 2025-09-24T07:06:19Z
```diff
@@ -294,7 +296,9 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 					}
 				}
 			} else {
-				if err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock); err != nil {
+				if err := workload.PatchAdmissionStatus(ctx, r.client, &wl, true, r.clock, func() (*kueue.Workload, bool, error) {
```

  same as above, moving it inside changes the logic and if we want to keep it is outside of the scope here

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-24T07:57:13Z

- **pkg/controller/core/workload_controller.go:266** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-24T07:57:13Z
```diff
@@ -263,7 +263,9 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		}
 
 		if updated {
-			return ctrl.Result{}, client.IgnoreNotFound(workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock))
+			return ctrl.Result{}, client.IgnoreNotFound(workload.PatchAdmissionStatus(ctx, r.client, &wl, true, r.clock, func() (*kueue.Workload, bool, error) {
```

  I can't see logic changes. You just need to move:
  
  https://github.com/kubernetes-sigs/kueue/pull/6962/files#diff-60dd240c20adbd6a189d018d1c216c2d296730f446c341d8bf449fa6657964ffR243-R264
  
  inside the callback function, return whether it was updated or not. If not, we won’t update it in PatchAdmissionStatus.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-24T07:58:14Z

- **pkg/controller/core/workload_controller.go:299** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-24T07:58:14Z
```diff
@@ -294,7 +296,9 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 					}
 				}
 			} else {
-				if err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock); err != nil {
+				if err := workload.PatchAdmissionStatus(ctx, r.client, &wl, true, r.clock, func() (*kueue.Workload, bool, error) {
```

  Here is I'm agree we can't move it inside callback function without refactoring.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-24T08:00:06Z

- **pkg/controller/core/workload_controller.go:299** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-24T08:00:06Z
```diff
@@ -294,7 +296,9 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 					}
 				}
 			} else {
-				if err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock); err != nil {
+				if err := workload.PatchAdmissionStatus(ctx, r.client, &wl, true, r.clock, func() (*kueue.Workload, bool, error) {
```

  But still can you use wlOrig?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-24T08:05:37Z

- **pkg/scheduler/scheduler.go:773** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-24T08:05:37Z
```diff
@@ -766,15 +770,15 @@ func (s *Scheduler) requeueAndUpdate(ctx context.Context, e entry) {
 
 	added := s.queues.RequeueWorkload(ctx, &e.Info, e.requeueReason)
 	log.V(2).Info("Workload re-queued", "workload", klog.KObj(e.Obj), "clusterQueue", klog.KRef("", string(e.ClusterQueue)), "queue", klog.KRef(e.Obj.Namespace, string(e.Obj.Spec.QueueName)), "requeueReason", e.requeueReason, "added", added, "status", e.status)
-
+	wl := e.Obj.DeepCopy()
```

  Can we move it inside callback function?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-24T08:06:27Z

- **pkg/scheduler/scheduler_test.go:3869** by [@mszadkow](https://github.com/mszadkow) — 2025-09-24T08:06:27Z
```diff
@@ -3687,7 +3687,7 @@ func TestSchedule(t *testing.T) {
 
 			gotScheduled := make(map[workload.Reference]kueue.Admission)
 			var mu sync.Mutex
-			scheduler.applyAdmission = func(ctx context.Context, w *kueue.Workload) error {
+			scheduler.patchAdmission = func(ctx context.Context, w *kueue.Workload) error {
```

  https://github.com/kubernetes-sigs/kueue/issues/6980

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-24T08:07:14Z

- **pkg/scheduler/scheduler.go:774** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-24T08:07:14Z
```diff
@@ -766,15 +770,15 @@ func (s *Scheduler) requeueAndUpdate(ctx context.Context, e entry) {
 
 	added := s.queues.RequeueWorkload(ctx, &e.Info, e.requeueReason)
 	log.V(2).Info("Workload re-queued", "workload", klog.KObj(e.Obj), "clusterQueue", klog.KRef("", string(e.ClusterQueue)), "queue", klog.KRef(e.Obj.Namespace, string(e.Obj.Spec.QueueName)), "requeueReason", e.requeueReason, "added", added, "status", e.status)
-
+	wl := e.Obj.DeepCopy()
+	wlOrig := e.Obj.DeepCopy()
```

  Not sure do we need this.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-24T08:10:38Z

- **test/integration/singlecluster/webhook/core/workload_test.go:321** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-24T08:10:38Z
```diff
@@ -315,14 +315,19 @@ var _ = ginkgo.Describe("Workload validating webhook", ginkgo.Ordered, func() {
 		ginkgo.DescribeTable("Should have valid values when setting Admission", func(w func() *kueue.Workload, a *kueue.Admission, errorType gomega.OmegaMatcher) {
 			wl := w()
 			util.MustCreate(ctx, k8sClient, wl)
-			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
-			workload.SetQuotaReservation(wl, a, clock.RealClock{})
-			err := workload.ApplyAdmissionStatus(ctx, k8sClient, wl, false, clock.RealClock{})
-			if errorType != nil {
-				gomega.Expect(err).Should(errorType)
-			} else {
-				gomega.Expect(err).ShouldNot(gomega.HaveOccurred())
-			}
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+				err := workload.PatchAdmissionStatus(ctx, k8sClient, wl, false, clock.RealClock{}, func() (*kueue.Workload, bool, error) {
```

  ```suggestion
  				err := workload.PatchAdmissionStatus(ctx, k8sClient, wl, true, clock.RealClock{}, func() (*kueue.Workload, bool, error) {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-24T08:10:58Z

- **test/integration/singlecluster/scheduler/podsready/scheduler_test.go:333** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-24T08:10:58Z
```diff
@@ -330,8 +330,10 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(prodWl), prodWl)).Should(gomega.Succeed())
 				g.Expect(workload.IsActive(prodWl)).Should(gomega.BeFalse())
 				g.Expect(prodWl.Status.RequeueState).Should(gomega.BeNil())
-				workload.SetRequeuedCondition(prodWl, kueue.WorkloadDeactivated, "by test", false)
-				g.Expect(workload.ApplyAdmissionStatus(ctx, k8sClient, prodWl, true, realClock)).Should(gomega.Succeed())
+				g.Expect(workload.PatchAdmissionStatus(ctx, k8sClient, prodWl, false, realClock, func() (*kueue.Workload, bool, error) {
```

  ```suggestion
  				g.Expect(workload.PatchAdmissionStatus(ctx, k8sClient, prodWl, true, realClock, func() (*kueue.Workload, bool, error) {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-24T08:11:12Z

- **test/integration/singlecluster/controller/jobs/pod/pod_controller_test.go:1395** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-24T08:11:12Z
```diff
@@ -1387,10 +1390,13 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 				})
 
 				ginkgo.By("setting evicted condition to true", func() {
-					workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByPreemption, "By test")
-					gomega.Expect(
-						workload.ApplyAdmissionStatus(ctx, k8sClient, wl, false, realClock),
-					).Should(gomega.Succeed())
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+						g.Expect(workload.PatchAdmissionStatus(ctx, k8sClient, wl, false, realClock, func() (*kueue.Workload, bool, error) {
```

  ```suggestion
  						g.Expect(workload.PatchAdmissionStatus(ctx, k8sClient, wl, true, realClock, func() (*kueue.Workload, bool, error) {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-24T08:11:24Z

- **test/integration/singlecluster/controller/jobs/pod/pod_controller_test.go:801** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-24T08:11:24Z
```diff
@@ -795,11 +795,14 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 
 				createdPod := &corev1.Pod{}
 				ginkgo.By("checking that the Pods get a deletion timestamp when the workload is evicted", func() {
-					gomega.Expect(func() error {
-						w := createdWorkload.DeepCopy()
-						workload.SetEvictedCondition(w, "ByTest", "by test")
-						return workload.ApplyAdmissionStatus(ctx, k8sClient, w, false, realClock)
-					}()).Should(gomega.Succeed())
+					createdWorkload := &kueue.Workload{}
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
+						g.Expect(workload.PatchAdmissionStatus(ctx, k8sClient, createdWorkload, false, realClock, func() (*kueue.Workload, bool, error) {
```

  ```suggestion
  						g.Expect(workload.PatchAdmissionStatus(ctx, k8sClient, createdWorkload, true, realClock, func() (*kueue.Workload, bool, error) {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-24T08:11:35Z

- **test/integration/singlecluster/controller/core/workload_controller_test.go:503** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-24T08:11:35Z
```diff
@@ -500,8 +500,10 @@ var _ = ginkgo.Describe("Workload controller", ginkgo.Ordered, ginkgo.ContinueOn
 			ginkgo.By("evicting the workload, the accumulated admission time is updated", func() {
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, key, wl)).To(gomega.Succeed())
-					workload.SetEvictedCondition(wl, "ByTest", "by test")
-					g.Expect(workload.ApplyAdmissionStatus(ctx, k8sClient, wl, false, realClock)).To(gomega.Succeed())
+					g.Expect(workload.PatchAdmissionStatus(ctx, k8sClient, wl, false, realClock, func() (*kueue.Workload, bool, error) {
```

  ```suggestion
  					g.Expect(workload.PatchAdmissionStatus(ctx, k8sClient, wl, true, realClock, func() (*kueue.Workload, bool, error) {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-24T08:13:15Z

- **test/util/util.go:730** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-24T08:13:15Z
```diff
@@ -721,10 +725,13 @@ func SetQuotaReservation(ctx context.Context, k8sClient client.Client, wlKey cli
 func SyncAdmittedConditionForWorkloads(ctx context.Context, k8sClient client.Client, wls ...*kueue.Workload) {
 	var updatedWorkload kueue.Workload
 	for _, wl := range wls {
-		gomega.ExpectWithOffset(1, k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWorkload)).To(gomega.Succeed())
-		if workload.SyncAdmittedCondition(&updatedWorkload, time.Now()) {
-			gomega.ExpectWithOffset(1, workload.ApplyAdmissionStatus(ctx, k8sClient, &updatedWorkload, false, clock.RealClock{})).To(gomega.Succeed())
-		}
+		gomega.EventuallyWithOffset(1, func(g gomega.Gomega) {
+			g.ExpectWithOffset(1, k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWorkload)).To(gomega.Succeed())
+			g.ExpectWithOffset(1, workload.PatchAdmissionStatus(ctx, k8sClient, &updatedWorkload, false, clock.RealClock{}, func() (*kueue.Workload, bool, error) {
```

  ```suggestion
  			g.ExpectWithOffset(1, workload.PatchAdmissionStatus(ctx, k8sClient, &updatedWorkload, true, clock.RealClock{}, func() (*kueue.Workload, bool, error) {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-24T08:14:35Z

- **pkg/scheduler/scheduler_test.go:3869** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-24T08:14:35Z
```diff
@@ -3687,7 +3687,7 @@ func TestSchedule(t *testing.T) {
 
 			gotScheduled := make(map[workload.Reference]kueue.Admission)
 			var mu sync.Mutex
-			scheduler.applyAdmission = func(ctx context.Context, w *kueue.Workload) error {
+			scheduler.patchAdmission = func(ctx context.Context, w *kueue.Workload) error {
```

  Thank you 🙏

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-24T08:20:01Z

- **cmd/importer/pod/import.go:204** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-24T08:20:01Z
```diff
@@ -201,7 +201,9 @@ func createWorkload(ctx context.Context, c client.Client, wl *kueue.Workload) er
 
 func admitWorkload(ctx context.Context, c client.Client, wl *kueue.Workload) error {
 	var realClock = clock.RealClock{}
-	err := workload.ApplyAdmissionStatus(ctx, c, wl, false, realClock)
+	err := workload.PatchAdmissionStatus(ctx, c, wl, false, realClock, func() (*kueue.Workload, bool, error) {
```

  Yeah, probably. But maybe we could just optimize the importer.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-24T08:24:00Z

- **pkg/scheduler/scheduler.go:774** by [@mszadkow](https://github.com/mszadkow) — 2025-09-24T08:23:59Z
```diff
@@ -766,15 +770,15 @@ func (s *Scheduler) requeueAndUpdate(ctx context.Context, e entry) {
 
 	added := s.queues.RequeueWorkload(ctx, &e.Info, e.requeueReason)
 	log.V(2).Info("Workload re-queued", "workload", klog.KObj(e.Obj), "clusterQueue", klog.KRef("", string(e.ClusterQueue)), "queue", klog.KRef(e.Obj.Namespace, string(e.Obj.Spec.QueueName)), "requeueReason", e.requeueReason, "added", added, "status", e.status)
-
+	wl := e.Obj.DeepCopy()
+	wlOrig := e.Obj.DeepCopy()
```

  I will check what can be done here, but with Merge Patch this will be required

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-24T08:32:50Z

- **pkg/controller/core/workload_controller.go:488** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-24T08:32:50Z
```diff
@@ -468,7 +486,10 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 			rejectedCheckNames = append(rejectedCheckNames, check.Name)
 		}
 		workload.SetDeactivationTarget(wl, kueue.WorkloadEvictedByAdmissionCheck, fmt.Sprintf("Admission check(s): %v, were rejected", stringsutils.Join(rejectedCheckNames, ",")))
```

  Also this one. Can we move it to callback function?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-24T08:41:43Z

- **cmd/importer/pod/import.go:204** by [@mszadkow](https://github.com/mszadkow) — 2025-09-24T08:41:43Z
```diff
@@ -201,7 +201,9 @@ func createWorkload(ctx context.Context, c client.Client, wl *kueue.Workload) er
 
 func admitWorkload(ctx context.Context, c client.Client, wl *kueue.Workload) error {
 	var realClock = clock.RealClock{}
-	err := workload.ApplyAdmissionStatus(ctx, c, wl, false, realClock)
+	err := workload.PatchAdmissionStatus(ctx, c, wl, false, realClock, func() (*kueue.Workload, bool, error) {
```

  Ok.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-24T08:44:03Z

- **pkg/controller/core/workload_controller.go:266** by [@mszadkow](https://github.com/mszadkow) — 2025-09-24T08:44:03Z
```diff
@@ -263,7 +263,9 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		}
 
 		if updated {
-			return ctrl.Result{}, client.IgnoreNotFound(workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock))
+			return ctrl.Result{}, client.IgnoreNotFound(workload.PatchAdmissionStatus(ctx, r.client, &wl, true, r.clock, func() (*kueue.Workload, bool, error) {
```

  I misunderstood you at first time, I will try

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-24T08:44:31Z

- **pkg/controller/core/workload_controller.go:356** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-24T08:44:31Z
```diff
@@ -337,7 +346,9 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	// If the workload is admitted, updating the status here would set the Admitted condition to
 	// false before the workloads eviction.
 	if !workload.IsAdmitted(&wl) && workload.SyncAdmittedCondition(&wl, r.clock.Now()) {
-		if err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock); err != nil {
+		if err := workload.PatchAdmissionStatus(ctx, r.client, &wl, true, r.clock, func() (*kueue.Workload, bool, error) {
+			return &wl, true, nil
```

  ```suggestion
  			return &wl, workload.SyncAdmittedCondition(&wl, r.clock.Now(), nil
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-24T08:44:48Z

- **pkg/controller/core/workload_controller.go:356** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-24T08:44:48Z
```diff
@@ -337,7 +346,9 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	// If the workload is admitted, updating the status here would set the Admitted condition to
 	// false before the workloads eviction.
 	if !workload.IsAdmitted(&wl) && workload.SyncAdmittedCondition(&wl, r.clock.Now()) {
-		if err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock); err != nil {
+		if err := workload.PatchAdmissionStatus(ctx, r.client, &wl, true, r.clock, func() (*kueue.Workload, bool, error) {
+			return &wl, true, nil
```

  Probably it should be like this, right?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-24T08:46:08Z

- **pkg/controller/core/workload_controller.go:356** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-24T08:46:08Z
```diff
@@ -337,7 +346,9 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	// If the workload is admitted, updating the status here would set the Admitted condition to
 	// false before the workloads eviction.
 	if !workload.IsAdmitted(&wl) && workload.SyncAdmittedCondition(&wl, r.clock.Now()) {
-		if err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock); err != nil {
+		if err := workload.PatchAdmissionStatus(ctx, r.client, &wl, true, r.clock, func() (*kueue.Workload, bool, error) {
+			return &wl, true, nil
```

  Ah, no, in that case we reconcile it every time.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-24T08:46:59Z

- **pkg/controller/core/workload_controller.go:356** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-24T08:46:59Z
```diff
@@ -337,7 +346,9 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	// If the workload is admitted, updating the status here would set the Admitted condition to
 	// false before the workloads eviction.
 	if !workload.IsAdmitted(&wl) && workload.SyncAdmittedCondition(&wl, r.clock.Now()) {
-		if err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock); err != nil {
+		if err := workload.PatchAdmissionStatus(ctx, r.client, &wl, true, r.clock, func() (*kueue.Workload, bool, error) {
+			return &wl, true, nil
```

  Than maybe it's easier to use wlOrig.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-24T08:47:27Z

- **pkg/controller/core/workload_controller.go:488** by [@mszadkow](https://github.com/mszadkow) — 2025-09-24T08:47:27Z
```diff
@@ -468,7 +486,10 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 			rejectedCheckNames = append(rejectedCheckNames, check.Name)
 		}
 		workload.SetDeactivationTarget(wl, kueue.WorkloadEvictedByAdmissionCheck, fmt.Sprintf("Admission check(s): %v, were rejected", stringsutils.Join(rejectedCheckNames, ",")))
```

  seems that yes

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-24T09:12:15Z

- **pkg/controller/core/workload_controller.go:356** by [@mszadkow](https://github.com/mszadkow) — 2025-09-24T09:12:15Z
```diff
@@ -337,7 +346,9 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	// If the workload is admitted, updating the status here would set the Admitted condition to
 	// false before the workloads eviction.
 	if !workload.IsAdmitted(&wl) && workload.SyncAdmittedCondition(&wl, r.clock.Now()) {
-		if err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock); err != nil {
+		if err := workload.PatchAdmissionStatus(ctx, r.client, &wl, true, r.clock, func() (*kueue.Workload, bool, error) {
+			return &wl, true, nil
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-24T11:31:46Z

Thank you 👍 This will make reviewing of the "main" PR much easier.
/lgtm
/approve

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-24T11:38:34Z

- **pkg/scheduler/scheduler.go:317** by [@mszadkow](https://github.com/mszadkow) — 2025-09-24T11:38:34Z
```diff
@@ -315,7 +315,9 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			// Block admission until all currently admitted workloads are in
 			// PodsReady condition if the waitForPodsReady is enabled
 			workload.UnsetQuotaReservationWithCondition(e.Obj, "Waiting", "waiting for all admitted workloads to be in PodsReady condition", s.clock.Now())
-			if err := workload.ApplyAdmissionStatus(ctx, s.client, e.Obj, false, s.clock); err != nil {
+			if err := workload.PatchAdmissionStatus(ctx, s.client, e.Obj, false, s.clock, func() (*kueue.Workload, bool, error) {
```

  @mbobrovskyi will you plz create an issue out of that?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-24T12:51:01Z

- **pkg/scheduler/scheduler.go:317** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-24T12:51:01Z
```diff
@@ -315,7 +315,9 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			// Block admission until all currently admitted workloads are in
 			// PodsReady condition if the waitForPodsReady is enabled
 			workload.UnsetQuotaReservationWithCondition(e.Obj, "Waiting", "waiting for all admitted workloads to be in PodsReady condition", s.clock.Now())
-			if err := workload.ApplyAdmissionStatus(ctx, s.client, e.Obj, false, s.clock); err != nil {
+			if err := workload.PatchAdmissionStatus(ctx, s.client, e.Obj, false, s.clock, func() (*kueue.Workload, bool, error) {
```

  @mbobrovskyi @mimowo I think that the conflict error is generic PATCH operation problem.
  So, if we replace APPLY with PATCH, the errors will significantly increase since many Kueue internal components are involved in many Resources in parallel. 
  
  So, I think that we should retry mechanism to our patch client similar to client-go retry client: https://github.com/kubernetes/client-go/blob/6cf045f1f42f05384b3439d6aea3d0bca204a96c/util/retry/util.go

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-24T12:53:24Z

- **pkg/scheduler/scheduler.go:317** by [@mimowo](https://github.com/mimowo) — 2025-09-24T12:53:23Z
```diff
@@ -315,7 +315,9 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			// Block admission until all currently admitted workloads are in
 			// PodsReady condition if the waitForPodsReady is enabled
 			workload.UnsetQuotaReservationWithCondition(e.Obj, "Waiting", "waiting for all admitted workloads to be in PodsReady condition", s.clock.Now())
-			if err := workload.ApplyAdmissionStatus(ctx, s.client, e.Obj, false, s.clock); err != nil {
+			if err := workload.PatchAdmissionStatus(ctx, s.client, e.Obj, false, s.clock, func() (*kueue.Workload, bool, error) {
```

  Yeah, I think so. However, I would not call it a blocker for Alpha at this point we are trying to make it running at all.
  
  cc @mwysokin

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-24T12:55:40Z

- **pkg/scheduler/scheduler.go:317** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-24T12:55:40Z
```diff
@@ -315,7 +315,9 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			// Block admission until all currently admitted workloads are in
 			// PodsReady condition if the waitForPodsReady is enabled
 			workload.UnsetQuotaReservationWithCondition(e.Obj, "Waiting", "waiting for all admitted workloads to be in PodsReady condition", s.clock.Now())
-			if err := workload.ApplyAdmissionStatus(ctx, s.client, e.Obj, false, s.clock); err != nil {
+			if err := workload.PatchAdmissionStatus(ctx, s.client, e.Obj, false, s.clock, func() (*kueue.Workload, bool, error) {
```

  Yes, I think that this is the Beta graduation criteria.
  But, when you introduce the feature to your customers, please note that scheduling performance might be decreased, specifically in large environments.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-24T13:05:19Z

- **pkg/scheduler/scheduler.go:317** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-24T13:05:19Z
```diff
@@ -315,7 +315,9 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			// Block admission until all currently admitted workloads are in
 			// PodsReady condition if the waitForPodsReady is enabled
 			workload.UnsetQuotaReservationWithCondition(e.Obj, "Waiting", "waiting for all admitted workloads to be in PodsReady condition", s.clock.Now())
-			if err := workload.ApplyAdmissionStatus(ctx, s.client, e.Obj, false, s.clock); err != nil {
+			if err := workload.PatchAdmissionStatus(ctx, s.client, e.Obj, false, s.clock, func() (*kueue.Workload, bool, error) {
```

  Let's open dedicated issue since it looks like a bit big problem for this feature.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-24T13:05:33Z

- **pkg/scheduler/scheduler.go:317** by [@mimowo](https://github.com/mimowo) — 2025-09-24T13:05:33Z
```diff
@@ -315,7 +315,9 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			// Block admission until all currently admitted workloads are in
 			// PodsReady condition if the waitForPodsReady is enabled
 			workload.UnsetQuotaReservationWithCondition(e.Obj, "Waiting", "waiting for all admitted workloads to be in PodsReady condition", s.clock.Now())
-			if err := workload.ApplyAdmissionStatus(ctx, s.client, e.Obj, false, s.clock); err != nil {
+			if err := workload.PatchAdmissionStatus(ctx, s.client, e.Obj, false, s.clock, func() (*kueue.Workload, bool, error) {
```

  Yes for sure

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-24T13:08:51Z

- **pkg/scheduler/scheduler.go:317** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-24T13:08:51Z
```diff
@@ -315,7 +315,9 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			// Block admission until all currently admitted workloads are in
 			// PodsReady condition if the waitForPodsReady is enabled
 			workload.UnsetQuotaReservationWithCondition(e.Obj, "Waiting", "waiting for all admitted workloads to be in PodsReady condition", s.clock.Now())
-			if err := workload.ApplyAdmissionStatus(ctx, s.client, e.Obj, false, s.clock); err != nil {
+			if err := workload.PatchAdmissionStatus(ctx, s.client, e.Obj, false, s.clock, func() (*kueue.Workload, bool, error) {
```

  https://github.com/kubernetes-sigs/kueue/issues/6992

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-24T13:10:32Z

- **pkg/scheduler/scheduler.go:317** by [@mimowo](https://github.com/mimowo) — 2025-09-24T13:10:32Z
```diff
@@ -315,7 +315,9 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			// Block admission until all currently admitted workloads are in
 			// PodsReady condition if the waitForPodsReady is enabled
 			workload.UnsetQuotaReservationWithCondition(e.Obj, "Waiting", "waiting for all admitted workloads to be in PodsReady condition", s.clock.Now())
-			if err := workload.ApplyAdmissionStatus(ctx, s.client, e.Obj, false, s.clock); err != nil {
+			if err := workload.PatchAdmissionStatus(ctx, s.client, e.Obj, false, s.clock, func() (*kueue.Workload, bool, error) {
```

  Thank you for tracking the issue
