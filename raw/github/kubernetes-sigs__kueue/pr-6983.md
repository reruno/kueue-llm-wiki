# PR #6983: parse fair weight into float64 upon update

**Summary**: parse fair weight into float64 upon update

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6983

**Last updated**: 2025-09-25T15:10:33Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@gabesaba](https://github.com/gabesaba)
- **Branch**: `parse_fs` → `main`
- **Created**: 2025-09-24T08:15:18Z
- **Updated**: 2025-09-25T15:10:33Z
- **Merged**: 2025-09-24T13:52:17Z
- **Closed**: 2025-09-24T13:52:17Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 8   **+131 / -132**

## Description

#### What type of PR is this?
/kind cleanup

#### What this PR does / why we need it:
Follow-up to #6963, to fix #6960

Upon update to ClusterQueue or Cohort, we convert the resource.Quantity representation of FairWeight into float64

#### Which issue(s) this PR fixes:
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-09-24T08:15:24Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | f8e220ff067f602ad3e276c3cc988f07bd3cfa62 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68d3f18a32f3f500083c3ad7 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-24T10:15:32Z

LGTM label has been added.  <details>Git tree hash: 8be2338feac5b59ca8e6e98b182d626edd94166e</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-24T10:23:05Z

/hold
looks like the issue returned in this PR: https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/6983/pull-kueue-test-integration-extended-main/1970791112079577088


```

Ginkgo ran 25 suites in 7m11.821768504s
There were failures detected in the following suites:
  fairsharing ./test/integration/singlecluster/scheduler/fairsharing
Test Suite Failed
make: *** [Makefile-test.mk:72: test-integration] Error 1
```

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-09-24T11:05:07Z

I couldn't find any indication that it is the same memory race. In fact, I looked at the logs and couldn't find any error..

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-24T11:21:43Z

> I couldn't find any indication that it is the same memory race. In fact, I looked at the logs and couldn't find any error..

It would not have been recorded before too. This was discovered by me when I set INTEGRATION_NPROC=1 in a commit

See here: https://github.com/kubernetes-sigs/kueue/pull/6963#issuecomment-3324409154

using this commit: https://github.com/kubernetes-sigs/kueue/commit/27571b476a6bc3ec239e91c80495aa693cbc8e0c

Without this commit, when it failed it reported exactly the same. And notably, it didn't report the data race to the console. I think this is becuase when the test runner fails with data race on panic it does not publish results from all threads.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-24T12:08:46Z

It failed the second time in a row with the same way, https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/6983/pull-kueue-test-integration-extended-main/1970806712730390528
which strongly suggests this is reverting the issue

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-09-24T12:53:42Z

Despite holding lock, we are racing with informer cache. I will make a deep copy before calling AsFloat64Slow

https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/6983/pull-kueue-test-integration-extended-main/1970828477141618688
```
WARNING: DATA RACE
Read at 0x00c0005d5dc0 by goroutine 498:
... 
  sigs.k8s.io/controller-runtime/pkg/cache.(*informerCache).Get()
      /home/prow/go/src/sigs.k8s.io/kueue/vendor/sigs.k8s.io/controller-runtime/pkg/cache/informer_cache.go:88 +0x29b
  sigs.k8s.io/controller-runtime/pkg/client.(*client).Get()
      /home/prow/go/src/sigs.k8s.io/kueue/vendor/sigs.k8s.io/controller-runtime/pkg/client/client.go:338 +0x244
  sigs.k8s.io/kueue/pkg/controller/core.(*LocalQueueReconciler).Reconcile()
      /home/prow/go/src/sigs.k8s.io/kueue/pkg/controller/core/localqueue_controller.go:160 +0x451
```

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-24T13:03:52Z

LGTM label has been added.  <details>Git tree hash: 19ac0d8587e26dbee96c09ef98de5be3729e7de6</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-24T13:29:13Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6983#" title="Author self-approved">gabesaba</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6983#discussion_r2375810637" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [gabesaba,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-24T13:29:13Z

LGTM label has been added.  <details>Git tree hash: cf8366c8686f31553b408bc5e514a617b824d8e3</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-24T08:28:31Z

- **pkg/cache/scheduler/fair_sharing.go:30** by [@mimowo](https://github.com/mimowo) — 2025-09-24T08:28:30Z
```diff
@@ -21,35 +21,34 @@ import (
 	"math"
 
 	corev1 "k8s.io/api/core/v1"
-	"k8s.io/apimachinery/pkg/api/resource"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/resources"
 )
 
-var (
-	oneQuantity = resource.MustParse("1")
+const (
+	oneQuantityFloat = 1.0
```

  If we are renaming anyway, I would propose `defaultWeight` to reflect the semantic, not type.
  
  I know some languages prefer adding type as variable / const suffix, but it is not idiomatic for golang.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-09-24T08:32:42Z

- **pkg/cache/scheduler/fair_sharing.go:30** by [@gabesaba](https://github.com/gabesaba) — 2025-09-24T08:32:42Z
```diff
@@ -21,35 +21,34 @@ import (
 	"math"
 
 	corev1 "k8s.io/api/core/v1"
-	"k8s.io/apimachinery/pkg/api/resource"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/resources"
 )
 
-var (
-	oneQuantity = resource.MustParse("1")
+const (
+	oneQuantityFloat = 1.0
```

  defaultWeight sounds good to me. However, I added the suffix since we still have a resource.Quantity oneQuantity.
  
  Does:
  defaultWeightFloat
  defaultWeightQuantity
  
  sounds fine to you? Otherwise, I can just inline all of the resource.Quantity instances

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-24T08:39:00Z

- **pkg/cache/scheduler/fair_sharing.go:30** by [@mimowo](https://github.com/mimowo) — 2025-09-24T08:38:59Z
```diff
@@ -21,35 +21,34 @@ import (
 	"math"
 
 	corev1 "k8s.io/api/core/v1"
-	"k8s.io/apimachinery/pkg/api/resource"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/resources"
 )
 
-var (
-	oneQuantity = resource.MustParse("1")
+const (
+	oneQuantityFloat = 1.0
```

  Maybe let's go with `defaultWeight`, and inline the uses of `resource.Quantity`. Assuming it does not blow the diff too much), but I would like to go where we want to be eventually. 
  
  If it blows the diff too much, I might change my mind, hard to tell without experiment.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-09-24T10:03:45Z

- **pkg/cache/scheduler/fair_sharing.go:30** by [@gabesaba](https://github.com/gabesaba) — 2025-09-24T10:03:45Z
```diff
@@ -21,35 +21,34 @@ import (
 	"math"
 
 	corev1 "k8s.io/api/core/v1"
-	"k8s.io/apimachinery/pkg/api/resource"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/resources"
 )
 
-var (
-	oneQuantity = resource.MustParse("1")
+const (
+	oneQuantityFloat = 1.0
```

  Done. I split into 2 commits, with the 2nd just being the rename from `oneQuantity` to `defaultWeight`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-24T10:15:24Z

/lgtm
Thanks, I think this is cleaner indeed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-24T13:03:45Z

- **pkg/cache/scheduler/fair_sharing.go:179** by [@mimowo](https://github.com/mimowo) — 2025-09-24T13:03:45Z
```diff
@@ -168,9 +166,15 @@ func calculateLendable(node hierarchicalResourceNode) map[corev1.ResourceName]in
 
 // parseFairWeight parses FairSharing.Weight if it exists,
 // or otherwise returns the default value of 1.
-func parseFairWeight(fs *kueue.FairSharing) resource.Quantity {
+func parseFairWeight(fs *kueue.FairSharing) float64 {
 	if fs == nil || fs.Weight == nil {
-		return oneQuantity
+		return defaultWeight
 	}
-	return *fs.Weight
+
+	// we make a deep copy to avoid any data race, as this is a
+	// mutating method.  even though parseFairWeight only called
+	// when we're holding the cache's write lock, we race with the
+	// informer cache.
+	weightDeepCopy := fs.Weight.DeepCopy()
```

  I think this is far from obvious to a reader why `AsFloat64Slow` is mutating, so keeping the link might be helpful.
  
  in any case this is a nit.
  
  /lgtm
  
  Setting hold in case you want to update the comment. Otherwise feel free to unhold
  /hold

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-09-24T13:17:58Z

- **pkg/cache/scheduler/fair_sharing.go:179** by [@gabesaba](https://github.com/gabesaba) — 2025-09-24T13:17:58Z
```diff
@@ -168,9 +166,15 @@ func calculateLendable(node hierarchicalResourceNode) map[corev1.ResourceName]in
 
 // parseFairWeight parses FairSharing.Weight if it exists,
 // or otherwise returns the default value of 1.
-func parseFairWeight(fs *kueue.FairSharing) resource.Quantity {
+func parseFairWeight(fs *kueue.FairSharing) float64 {
 	if fs == nil || fs.Weight == nil {
-		return oneQuantity
+		return defaultWeight
 	}
-	return *fs.Weight
+
+	// we make a deep copy to avoid any data race, as this is a
+	// mutating method.  even though parseFairWeight only called
+	// when we're holding the cache's write lock, we race with the
+	// informer cache.
+	weightDeepCopy := fs.Weight.DeepCopy()
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-24T13:18:23Z

Thanks, LGTM
/approve

- **pkg/cache/scheduler/fair_sharing.go:179** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-24T13:17:40Z
```diff
@@ -168,9 +166,15 @@ func calculateLendable(node hierarchicalResourceNode) map[corev1.ResourceName]in
 
 // parseFairWeight parses FairSharing.Weight if it exists,
 // or otherwise returns the default value of 1.
-func parseFairWeight(fs *kueue.FairSharing) resource.Quantity {
+func parseFairWeight(fs *kueue.FairSharing) float64 {
 	if fs == nil || fs.Weight == nil {
-		return oneQuantity
+		return defaultWeight
 	}
-	return *fs.Weight
+
+	// we make a deep copy to avoid any data race, as this is a
+	// mutating method.  even though parseFairWeight only called
+	// when we're holding the cache's write lock, we race with the
+	// informer cache.
+	weightDeepCopy := fs.Weight.DeepCopy()
```

  +1

- **pkg/cache/scheduler/fair_sharing.go:175** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-24T13:17:55Z
```diff
@@ -168,9 +166,16 @@ func calculateLendable(node hierarchicalResourceNode) map[corev1.ResourceName]in
 
 // parseFairWeight parses FairSharing.Weight if it exists,
 // or otherwise returns the default value of 1.
-func parseFairWeight(fs *kueue.FairSharing) resource.Quantity {
+func parseFairWeight(fs *kueue.FairSharing) float64 {
 	if fs == nil || fs.Weight == nil {
-		return oneQuantity
+		return defaultWeight
 	}
-	return *fs.Weight
+
+	// we make a deep copy to avoid any data race, as this is a
+	// mutating method.  even though parseFairWeight only called
```

  ```suggestion
  	// mutating method even though parseFairWeight only called
  ```
  
  nit

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-24T13:22:45Z

- **pkg/cache/scheduler/fair_sharing.go:179** by [@mimowo](https://github.com/mimowo) — 2025-09-24T13:22:44Z
```diff
@@ -168,9 +166,15 @@ func calculateLendable(node hierarchicalResourceNode) map[corev1.ResourceName]in
 
 // parseFairWeight parses FairSharing.Weight if it exists,
 // or otherwise returns the default value of 1.
-func parseFairWeight(fs *kueue.FairSharing) resource.Quantity {
+func parseFairWeight(fs *kueue.FairSharing) float64 {
 	if fs == nil || fs.Weight == nil {
-		return oneQuantity
+		return defaultWeight
 	}
-	return *fs.Weight
+
+	// we make a deep copy to avoid any data race, as this is a
+	// mutating method.  even though parseFairWeight only called
+	// when we're holding the cache's write lock, we race with the
+	// informer cache.
+	weightDeepCopy := fs.Weight.DeepCopy()
```

  /unhold
  as my comment is addressed

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-09-24T13:26:50Z

- **pkg/cache/scheduler/fair_sharing.go:175** by [@gabesaba](https://github.com/gabesaba) — 2025-09-24T13:26:50Z
```diff
@@ -168,9 +166,16 @@ func calculateLendable(node hierarchicalResourceNode) map[corev1.ResourceName]in
 
 // parseFairWeight parses FairSharing.Weight if it exists,
 // or otherwise returns the default value of 1.
-func parseFairWeight(fs *kueue.FairSharing) resource.Quantity {
+func parseFairWeight(fs *kueue.FairSharing) float64 {
 	if fs == nil || fs.Weight == nil {
-		return oneQuantity
+		return defaultWeight
 	}
-	return *fs.Weight
+
+	// we make a deep copy to avoid any data race, as this is a
+	// mutating method.  even though parseFairWeight only called
```

  I will leave as is. Without the period, it seems closer to a run-on sentence to me

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-24T13:29:05Z

- **pkg/cache/scheduler/fair_sharing.go:175** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-24T13:29:05Z
```diff
@@ -168,9 +166,16 @@ func calculateLendable(node hierarchicalResourceNode) map[corev1.ResourceName]in
 
 // parseFairWeight parses FairSharing.Weight if it exists,
 // or otherwise returns the default value of 1.
-func parseFairWeight(fs *kueue.FairSharing) resource.Quantity {
+func parseFairWeight(fs *kueue.FairSharing) float64 {
 	if fs == nil || fs.Weight == nil {
-		return oneQuantity
+		return defaultWeight
 	}
-	return *fs.Weight
+
+	// we make a deep copy to avoid any data race, as this is a
+	// mutating method.  even though parseFairWeight only called
```

  Thanks for updating that.
  /lgtm
  /approve
