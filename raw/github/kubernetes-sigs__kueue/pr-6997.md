# PR #6997: [Trainjob] Don't lookup the child JobSet object

**Summary**: [Trainjob] Don't lookup the child JobSet object

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6997

**Last updated**: 2025-09-29T12:52:20Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kaisoz](https://github.com/kaisoz)
- **Branch**: `6568-trainer-use-info` → `main`
- **Created**: 2025-09-24T21:02:15Z
- **Updated**: 2025-09-29T12:52:20Z
- **Merged**: 2025-09-29T12:52:18Z
- **Closed**: 2025-09-29T12:52:18Z
- **Labels**: `lgtm`, `size/XXL`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mimowo](https://github.com/mimowo), [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 96   **+19692 / -308**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind cleanup

#### What this PR does / why we need it:
The current implementation of the Kubeflow Trainer V2 integration looks up the child JobSet object at reconciliation time. This is undesirable because

a) JobSet should be an implementation detail of the Trainer which might be replaced in the future
b) it creates tricky race conditions to reason about, because the JobSet may not be immediately created
c) it couples the JobSet API version used for Trainer with the version we integrate with

This PR addresses this by using TrainJob helper functions to recreate the child JobSet in memory at runtime.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #6865

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-09-24T21:02:22Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | f767cbc5ac903d8e512612ca713d9e83e3cdc593 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68da74abbc28190008ada99b |

### Comment by [@kaisoz](https://github.com/kaisoz) — 2025-09-24T21:13:49Z

/assign @mimowo 

PTAL! Thanks!

### Comment by [@kaisoz](https://github.com/kaisoz) — 2025-09-25T04:49:54Z

/retest

### Comment by [@kaisoz](https://github.com/kaisoz) — 2025-09-25T05:00:27Z

from the failed `pull-kueue-test-integration-multikueue-main` test [details link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/6997/pull-kueue-test-integration-multikueue-main/1971074672816558080)

```
Ran 31 of 31 Specs in 168.227 seconds
SUCCESS! -- 31 Passed | 0 Failed | 0 Pending | 0 Skipped
Ginkgo ran 1 suite in 4m54.792923099s
Test Suite Failed
make: *** [Makefile-test.mk:90: test-multikueue-integration] Error 1
```

all tests passed but Make returns an error? I'm trying to reproduce that locally... 🫤 

EDIT: I rerun the test and it passed... :shrug:

### Comment by [@kaisoz](https://github.com/kaisoz) — 2025-09-25T05:00:38Z

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-25T07:46:36Z

> all tests passed but Make returns an error? I'm trying to reproduce that locally... 🫤

Yes, yesterday we had a flake which affected a couple of branches: https://github.com/kubernetes-sigs/kueue/issues/6960

Sorry for the inconvenience. It is solved now, please rebase.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-29T11:54:21Z

LGTM label has been added.  <details>Git tree hash: 3669500073cc6f04c57a0a642ae445f2d2569a3b</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-29T11:54:22Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6997#" title="Author self-approved">kaisoz</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6997#pullrequestreview-3279351977" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-29T11:55:14Z

Ups, @kaisoz take a look at the `pull-kueue-verify-main` failure. Looks like just ordering of imports

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-29T12:00:25Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-29T12:00:33Z

LGTM label has been added.  <details>Git tree hash: 8f2eb0803942a55ecbc8360f8165a0f8f9991b70</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-25T07:08:17Z

- **go.mod:12** by [@mimowo](https://github.com/mimowo) — 2025-09-25T07:08:17Z
```diff
@@ -9,7 +9,7 @@ require (
 	github.com/google/go-cmp v0.7.0
 	github.com/json-iterator/go v1.1.12
 	github.com/kubeflow/mpi-operator v0.6.0
-	github.com/kubeflow/trainer/v2 v2.0.0
+	github.com/kubeflow/trainer/v2 v2.0.1-0.20250924122344-56be1f8163fb
```

  This PR is using Trainer 2.0 branch and our integration tests continue to work: https://github.com/kubernetes-sigs/kueue/blob/main/test/integration/singlecluster/controller/jobs/trainjob/trainjob_controller_test.go
  
  So , I think we have a green light for 2.0.1 release.
  
  cc @andreyvelich @astefanutti @tenzen-y

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-25T07:14:15Z

- **pkg/controller/jobs/kubeflow/trainjob/trainjob_controller.go:189** by [@mimowo](https://github.com/mimowo) — 2025-09-25T07:14:14Z
```diff
@@ -150,10 +158,74 @@ func (t *TrainJob) PodLabelSelector() string {
 }
 
 func getChildJobSet(t *TrainJob) (*jobsetapi.JobSet, error) {
-	jobSet := jobsetapi.JobSet{}
-	// TODO: Ideally we should be using the parent context here
-	err := reconciler.client.Get(context.Background(), types.NamespacedName{Name: t.Name, Namespace: t.Namespace}, &jobSet)
-	return &jobSet, err
+	runtimeRefGK := kftrainerruntime.RuntimeRefToRuntimeRegistryKey(t.Spec.RuntimeRef)
+	runtime, ok := reconciler.runtimes[runtimeRefGK]
+	if !ok {
+		return nil, fmt.Errorf("unsupported runtime: %s", runtimeRefGK)
+	}
+
+	trainJob := (*kftrainerapi.TrainJob)(t)
+	trSpec, err := getRuntimeSpec(trainJob)
+	if err != nil {
+		return nil, fmt.Errorf("runtime '%s' not found", trainJob.Spec.RuntimeRef.Name)
+	}
+	info, err := runtime.RuntimeInfo(trainJob, trSpec.Template, trSpec.MLPolicy, trSpec.PodGroupPolicy)
+	if err != nil {
+		return nil, err
+	}
+
+	// Get the jobsetSpecApply and apply the TrainJob object overrides for the trainer and initializer jobs
+	jobSetSpec, ok := kftrainerruntime.TemplateSpecApply[jobsetapplyapi.JobSetSpecApplyConfiguration](info)
+	if !ok {
+		return nil, err
+	}
+	jobsetApply := kftrainerjobset.NewBuilder(jobsetapplyapi.JobSet(t.Name, t.Namespace).
+		WithSpec(jobSetSpec)).Initializer(trainJob).Trainer(info, trainJob).PodLabels(info.Scheduler.PodLabels).Build()
+
+	// convert to jobset with the defaults set
+	return jobsetApplyToJobset(jobsetApply)
```

  This remains JobSet-specific so it is not ideal long term. We need to ticket and work on resolving in the future.
  
  I guess some changes to the interfaces are still needed?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-25T07:17:09Z

- **pkg/controller/jobs/kubeflow/trainjob/trainjob_controller.go:189** by [@mimowo](https://github.com/mimowo) — 2025-09-25T07:17:09Z
```diff
@@ -150,10 +158,74 @@ func (t *TrainJob) PodLabelSelector() string {
 }
 
 func getChildJobSet(t *TrainJob) (*jobsetapi.JobSet, error) {
-	jobSet := jobsetapi.JobSet{}
-	// TODO: Ideally we should be using the parent context here
-	err := reconciler.client.Get(context.Background(), types.NamespacedName{Name: t.Name, Namespace: t.Namespace}, &jobSet)
-	return &jobSet, err
+	runtimeRefGK := kftrainerruntime.RuntimeRefToRuntimeRegistryKey(t.Spec.RuntimeRef)
+	runtime, ok := reconciler.runtimes[runtimeRefGK]
+	if !ok {
+		return nil, fmt.Errorf("unsupported runtime: %s", runtimeRefGK)
+	}
+
+	trainJob := (*kftrainerapi.TrainJob)(t)
+	trSpec, err := getRuntimeSpec(trainJob)
+	if err != nil {
+		return nil, fmt.Errorf("runtime '%s' not found", trainJob.Spec.RuntimeRef.Name)
+	}
+	info, err := runtime.RuntimeInfo(trainJob, trSpec.Template, trSpec.MLPolicy, trSpec.PodGroupPolicy)
+	if err != nil {
+		return nil, err
+	}
+
+	// Get the jobsetSpecApply and apply the TrainJob object overrides for the trainer and initializer jobs
+	jobSetSpec, ok := kftrainerruntime.TemplateSpecApply[jobsetapplyapi.JobSetSpecApplyConfiguration](info)
+	if !ok {
+		return nil, err
+	}
+	jobsetApply := kftrainerjobset.NewBuilder(jobsetapplyapi.JobSet(t.Name, t.Namespace).
+		WithSpec(jobSetSpec)).Initializer(trainJob).Trainer(info, trainJob).PodLabels(info.Scheduler.PodLabels).Build()
+
+	// convert to jobset with the defaults set
+	return jobsetApplyToJobset(jobsetApply)
```

  Oh, all the necessary information seems to be already in the "info" object, so maybe we could already stop constructing the JobSet. It looks weird.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-25T07:18:49Z

- **pkg/controller/jobs/kubeflow/trainjob/trainjob_controller.go:353** by [@mimowo](https://github.com/mimowo) — 2025-09-25T07:18:49Z
```diff
@@ -256,19 +328,48 @@ func (t *TrainJob) Finished() (message string, success, finished bool) {
 }
 
 func (t *TrainJob) PodsReady() bool {
+	var (
+		replicas      int32
+		readyReplicas int32
+	)
 	jobset, err := getChildJobSet(t)
 	if err != nil {
 		return false
 	}
-	return (*kJobset.JobSet)(jobset).PodsReady()
+
+	for _, replicatedJob := range jobset.Spec.ReplicatedJobs {
+		replicas += replicatedJob.Replicas
+	}
+	for _, jobStatus := range t.Status.JobsStatus {
+		readyReplicas += jobStatus.Ready + jobStatus.Succeeded
+	}
+	return replicas == readyReplicas
 }
 
 func (t *TrainJob) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
+	if len(t.Status.JobsStatus) == 0 {
+		return nil, nil
+	}
 	jobset, err := getChildJobSet(t)
```

  It looks like a mix of getting the jobset, by status from Trainer. I think we should just not use JobSet here, but rely solely on Trainer infos.
  
  If this is substantial work to do we can plan as a follow up

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-25T07:21:38Z

- **pkg/controller/jobframework/setup.go:113** by [@mimowo](https://github.com/mimowo) — 2025-09-25T07:21:37Z
```diff
@@ -109,20 +109,35 @@ func (m *integrationManager) setupControllers(ctx context.Context, mgr ctrl.Mana
 	})
 }
 
-func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
-	if err := cb.NewReconciler(
+func (m *integrationManager) setupControllerAndWebhook(ctx context.Context, mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	var (
```

  I think we don't need this state: err and r, please minimize changes which are distractions

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-25T07:22:57Z

- **pkg/controller/jobframework/integrationmanager.go:54** by [@mimowo](https://github.com/mimowo) — 2025-09-25T07:22:57Z
```diff
@@ -51,7 +51,7 @@ type JobReconcilerInterface interface {
 	SetupWithManager(mgr ctrl.Manager) error
 }
 
-type ReconcilerFactory func(client client.Client, record record.EventRecorder, opts ...Option) JobReconcilerInterface
+type ReconcilerFactory func(ctx context.Context, client client.Client, indexer client.FieldIndexer, eventRecorder record.EventRecorder, opts ...Option) (JobReconcilerInterface, error)
```

  ```suggestion
  type ReconcilerFactory func(ctx context.Context, client client.Client, indexer client.FieldIndexer, record record.EventRecorder, opts ...Option) (JobReconcilerInterface, error)
  ```
  Maybe the new name is ok, but please minimize distractions due to drive-by cleanups. The PR is already massive (same in other places).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-25T07:24:25Z

- **pkg/controller/jobframework/reconciler.go:1442** by [@mimowo](https://github.com/mimowo) — 2025-09-25T07:24:25Z
```diff
@@ -1437,12 +1437,12 @@ type ReconcilerSetup func(*builder.Builder, client.Client) *builder.Builder
 // NewGenericReconcilerFactory creates a new reconciler factory for a concrete GenericJob type.
 // newJob should return a new empty job.
 func NewGenericReconcilerFactory(newJob func() GenericJob, setup ...ReconcilerSetup) ReconcilerFactory {
-	return func(client client.Client, record record.EventRecorder, opts ...Option) JobReconcilerInterface {
+	return func(ctx context.Context, client client.Client, indexer client.FieldIndexer, eventRecorder record.EventRecorder, opts ...Option) (JobReconcilerInterface, error) {
 		return &genericReconciler{
-			jr:     NewReconciler(client, record, opts...),
+			jr:     NewReconciler(client, eventRecorder, opts...),
```

  Revert unnecessary driveby cleanups

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-25T07:26:04Z

- **pkg/controller/jobs/kubeflow/trainjob/trainjob_controller.go:90** by [@mimowo](https://github.com/mimowo) — 2025-09-25T07:26:04Z
```diff
@@ -68,30 +74,32 @@ func init() {
 // +kubebuilder:rbac:groups=trainer.kubeflow.org,resources=trainjobs,verbs=get;list;watch;update;patch
 // +kubebuilder:rbac:groups=trainer.kubeflow.org,resources=trainjobs/status,verbs=get;update;patch
 // +kubebuilder:rbac:groups=trainer.kubeflow.org,resources=trainjobs/finalizers,verbs=get;update
+// +kubebuilder:rbac:groups=trainer.kubeflow.org,resources=trainingruntimes,verbs=get;list;watch
+// +kubebuilder:rbac:groups=trainer.kubeflow.org,resources=clustertrainingruntimes,verbs=get;list;watch
 
 type trainJobReconciler struct {
-	jr     *jobframework.JobReconciler
-	client client.Client
+	jr       *jobframework.JobReconciler
+	runtimes map[string]kftrainerruntime.Runtime
+	client   client.Client
 }
 
 var reconciler trainJobReconciler
 var _ jobframework.JobReconcilerInterface = (*trainJobReconciler)(nil)
 
-func NewReconciler(client client.Client, eventRecorder record.EventRecorder, opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+func NewReconciler(ctx context.Context, client client.Client, indexer client.FieldIndexer, eventRecorder record.EventRecorder, opts ...jobframework.Option) (jobframework.JobReconcilerInterface, error) {
+	runtimes, err := kftrainerruntimecore.New(ctx, client, indexer)
+	if err != nil {
+		return nil, err
+	}
 	reconciler = trainJobReconciler{
-		jr:     jobframework.NewReconciler(client, eventRecorder, opts...),
-		client: client,
+		jr:       jobframework.NewReconciler(client, eventRecorder, opts...),
+		client:   client,
+		runtimes: runtimes,
 	}
-	return &reconciler
+	return &reconciler, nil
 }
 
 func (r *trainJobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	jobSet := jobsetapi.JobSet{}
```

  Nice!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-25T07:28:20Z

- **pkg/controller/jobs/kubeflow/trainjob/trainjob_controller.go:334** by [@mimowo](https://github.com/mimowo) — 2025-09-25T07:28:20Z
```diff
@@ -256,19 +328,48 @@ func (t *TrainJob) Finished() (message string, success, finished bool) {
 }
 
 func (t *TrainJob) PodsReady() bool {
+	var (
+		replicas      int32
+		readyReplicas int32
+	)
```

  Declare lower when needed, same as for JobSet.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-25T07:34:10Z

- **test/integration/multikueue/suite_test.go:129** by [@mimowo](https://github.com/mimowo) — 2025-09-25T07:34:09Z
```diff
@@ -109,13 +115,52 @@ func createCluster(setupFnc framework.ManagerSetup, apiFeatureGates ...string) c
 	}
 	c.cfg = c.fwk.Init()
 	c.ctx, c.client = c.fwk.SetupClient(c.cfg)
+	installClusterTrainerRuntimes(c)
+
 	// skip the manager setup if setup func is not provided
 	if setupFnc != nil {
 		c.fwk.StartManager(c.ctx, c.cfg, setupFnc)
 	}
+
 	return c
 }
 
+// Install ClusterTrainingRuntime manifests so that they can be referenced in the tests.
+func installClusterTrainerRuntimes(c cluster) {
```

  What is this doing? Is it installing the CRDs for the integration tests? Why cannot we follow the same path as for other CRDs?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-25T07:39:55Z

- **pkg/controller/jobs/kubeflow/trainjob/trainjob_controller.go:189** by [@mimowo](https://github.com/mimowo) — 2025-09-25T07:39:55Z
```diff
@@ -150,10 +158,74 @@ func (t *TrainJob) PodLabelSelector() string {
 }
 
 func getChildJobSet(t *TrainJob) (*jobsetapi.JobSet, error) {
-	jobSet := jobsetapi.JobSet{}
-	// TODO: Ideally we should be using the parent context here
-	err := reconciler.client.Get(context.Background(), types.NamespacedName{Name: t.Name, Namespace: t.Namespace}, &jobSet)
-	return &jobSet, err
+	runtimeRefGK := kftrainerruntime.RuntimeRefToRuntimeRegistryKey(t.Spec.RuntimeRef)
+	runtime, ok := reconciler.runtimes[runtimeRefGK]
+	if !ok {
+		return nil, fmt.Errorf("unsupported runtime: %s", runtimeRefGK)
+	}
+
+	trainJob := (*kftrainerapi.TrainJob)(t)
+	trSpec, err := getRuntimeSpec(trainJob)
+	if err != nil {
+		return nil, fmt.Errorf("runtime '%s' not found", trainJob.Spec.RuntimeRef.Name)
+	}
+	info, err := runtime.RuntimeInfo(trainJob, trSpec.Template, trSpec.MLPolicy, trSpec.PodGroupPolicy)
+	if err != nil {
+		return nil, err
+	}
+
+	// Get the jobsetSpecApply and apply the TrainJob object overrides for the trainer and initializer jobs
+	jobSetSpec, ok := kftrainerruntime.TemplateSpecApply[jobsetapplyapi.JobSetSpecApplyConfiguration](info)
+	if !ok {
+		return nil, err
+	}
+	jobsetApply := kftrainerjobset.NewBuilder(jobsetapplyapi.JobSet(t.Name, t.Namespace).
+		WithSpec(jobSetSpec)).Initializer(trainJob).Trainer(info, trainJob).PodLabels(info.Scheduler.PodLabels).Build()
+
+	// convert to jobset with the defaults set
+	return jobsetApplyToJobset(jobsetApply)
```

  Alternatively, it would be good if there was another helper in the Trainer which would return some struct (maybe StatusInfo or FullInfo) with all the details, but in Job-agnostic way.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-25T07:43:54Z

- **pkg/controller/jobs/kubeflow/trainjob/trainjob_controller.go:216** by [@mimowo](https://github.com/mimowo) — 2025-09-25T07:43:53Z
```diff
@@ -150,10 +158,74 @@ func (t *TrainJob) PodLabelSelector() string {
 }
 
 func getChildJobSet(t *TrainJob) (*jobsetapi.JobSet, error) {
-	jobSet := jobsetapi.JobSet{}
-	// TODO: Ideally we should be using the parent context here
-	err := reconciler.client.Get(context.Background(), types.NamespacedName{Name: t.Name, Namespace: t.Namespace}, &jobSet)
-	return &jobSet, err
+	runtimeRefGK := kftrainerruntime.RuntimeRefToRuntimeRegistryKey(t.Spec.RuntimeRef)
+	runtime, ok := reconciler.runtimes[runtimeRefGK]
+	if !ok {
+		return nil, fmt.Errorf("unsupported runtime: %s", runtimeRefGK)
+	}
+
+	trainJob := (*kftrainerapi.TrainJob)(t)
+	trSpec, err := getRuntimeSpec(trainJob)
+	if err != nil {
+		return nil, fmt.Errorf("runtime '%s' not found", trainJob.Spec.RuntimeRef.Name)
+	}
+	info, err := runtime.RuntimeInfo(trainJob, trSpec.Template, trSpec.MLPolicy, trSpec.PodGroupPolicy)
+	if err != nil {
+		return nil, err
+	}
+
+	// Get the jobsetSpecApply and apply the TrainJob object overrides for the trainer and initializer jobs
+	jobSetSpec, ok := kftrainerruntime.TemplateSpecApply[jobsetapplyapi.JobSetSpecApplyConfiguration](info)
+	if !ok {
+		return nil, err
+	}
+	jobsetApply := kftrainerjobset.NewBuilder(jobsetapplyapi.JobSet(t.Name, t.Namespace).
+		WithSpec(jobSetSpec)).Initializer(trainJob).Trainer(info, trainJob).PodLabels(info.Scheduler.PodLabels).Build()
+
+	// convert to jobset with the defaults set
+	return jobsetApplyToJobset(jobsetApply)
+}
+
+func jobsetApplyToJobset(jobsetApply *jobsetapplyapi.JobSetApplyConfiguration) (*jobsetapi.JobSet, error) {
+	jsonData, err := json.Marshal(jobsetApply)
+	if err != nil {
+		return nil, err
+	}
+
+	jobset := &jobsetapi.JobSet{}
+	if err := json.Unmarshal(jsonData, jobset); err != nil {
+		return nil, err
+	}
+
+	// Run a dry-run patch to set the jobset defaults
+	// Defaults must be applied here because Kueue later compares podsets to match workloads.
+	// Workloads coming from the API server are already defaulted, so without defaulting this JobSet, matching would fail.
+	if err = reconciler.client.Patch(context.Background(), jobset, client.Apply, &client.PatchOptions{
+		FieldManager: "defaulter",
+		DryRun:       []string{metav1.DryRunAll},
+	}); err != nil {
+		return nil, err
+	}
+	return jobset, nil
+}
+
+func getRuntimeSpec(trainJob *kftrainerapi.TrainJob) (*kftrainerapi.TrainingRuntimeSpec, error) {
+	// Defaults to ClusterTrainingRuntime according to trainjob_types.go
+	if trainJob.Spec.RuntimeRef.Kind == nil || *trainJob.Spec.RuntimeRef.Kind == "ClusterTrainingRuntime" {
+		var ctr kftrainerapi.ClusterTrainingRuntime
+		err := reconciler.client.Get(context.Background(), client.ObjectKey{Name: trainJob.Spec.RuntimeRef.Name}, &ctr)
```

  Since we already have the context in the reconciler (passed as NewReconciler), can we use this one?
  
  Yes, this is "globalCtx" rather than "request" ctx, but it is already better than Background. Background is not terminated when Kueue goroutine is shutting down, just terminated by the process as a whole. So, it might be leaking goroutines in integration tests for a while.
  
  Still, requestContext is better for debugging, but using the "globalContext" is already better than Background.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-25T07:44:38Z

The direction LGTM, it could make it into 0.14, so that the main aspects of the integration are settled. To make this happen it would be great to have released 2.0.1, and we could use it in Kueue as a preparatory PR to reduce vendor changes.

Aside from cosmetics, and removing drive-by cleanups, I have the comments:
1. we still use JobSet internally, I think it would be better to rely only on the Runtime.Info objects, and drop awareness of JobSet completely.
2. I'm unclear why the integration tests do some custom installing of CRDs, we have it already figured out for all integrations
3. we could already use the globalContext rather than Background

### Review by [@andreyvelich](https://github.com/andreyvelich) (commented) — 2025-09-25T12:50:15Z

Thank you for this @kaisoz!

- **pkg/controller/jobs/kubeflow/trainjob/trainjob_controller.go:214** by [@andreyvelich](https://github.com/andreyvelich) — 2025-09-25T12:39:56Z
```diff
@@ -150,10 +158,74 @@ func (t *TrainJob) PodLabelSelector() string {
 }
 
 func getChildJobSet(t *TrainJob) (*jobsetapi.JobSet, error) {
-	jobSet := jobsetapi.JobSet{}
-	// TODO: Ideally we should be using the parent context here
-	err := reconciler.client.Get(context.Background(), types.NamespacedName{Name: t.Name, Namespace: t.Namespace}, &jobSet)
-	return &jobSet, err
+	runtimeRefGK := kftrainerruntime.RuntimeRefToRuntimeRegistryKey(t.Spec.RuntimeRef)
+	runtime, ok := reconciler.runtimes[runtimeRefGK]
+	if !ok {
+		return nil, fmt.Errorf("unsupported runtime: %s", runtimeRefGK)
+	}
+
+	trainJob := (*kftrainerapi.TrainJob)(t)
+	trSpec, err := getRuntimeSpec(trainJob)
+	if err != nil {
+		return nil, fmt.Errorf("runtime '%s' not found", trainJob.Spec.RuntimeRef.Name)
+	}
+	info, err := runtime.RuntimeInfo(trainJob, trSpec.Template, trSpec.MLPolicy, trSpec.PodGroupPolicy)
+	if err != nil {
+		return nil, err
+	}
+
+	// Get the jobsetSpecApply and apply the TrainJob object overrides for the trainer and initializer jobs
+	jobSetSpec, ok := kftrainerruntime.TemplateSpecApply[jobsetapplyapi.JobSetSpecApplyConfiguration](info)
+	if !ok {
+		return nil, err
+	}
+	jobsetApply := kftrainerjobset.NewBuilder(jobsetapplyapi.JobSet(t.Name, t.Namespace).
+		WithSpec(jobSetSpec)).Initializer(trainJob).Trainer(info, trainJob).PodLabels(info.Scheduler.PodLabels).Build()
+
+	// convert to jobset with the defaults set
+	return jobsetApplyToJobset(jobsetApply)
+}
+
+func jobsetApplyToJobset(jobsetApply *jobsetapplyapi.JobSetApplyConfiguration) (*jobsetapi.JobSet, error) {
+	jsonData, err := json.Marshal(jobsetApply)
+	if err != nil {
+		return nil, err
+	}
+
+	jobset := &jobsetapi.JobSet{}
+	if err := json.Unmarshal(jsonData, jobset); err != nil {
+		return nil, err
+	}
+
+	// Run a dry-run patch to set the jobset defaults
+	// Defaults must be applied here because Kueue later compares podsets to match workloads.
+	// Workloads coming from the API server are already defaulted, so without defaulting this JobSet, matching would fail.
+	if err = reconciler.client.Patch(context.Background(), jobset, client.Apply, &client.PatchOptions{
+		FieldManager: "defaulter",
+		DryRun:       []string{metav1.DryRunAll},
+	}); err != nil {
+		return nil, err
+	}
+	return jobset, nil
+}
+
+func getRuntimeSpec(trainJob *kftrainerapi.TrainJob) (*kftrainerapi.TrainingRuntimeSpec, error) {
+	// Defaults to ClusterTrainingRuntime according to trainjob_types.go
+	if trainJob.Spec.RuntimeRef.Kind == nil || *trainJob.Spec.RuntimeRef.Kind == "ClusterTrainingRuntime" {
```

  Do we run the Kueue TrainJob controller after the TrainJob defaulter is running ?
  As you can see, we set the default value here: https://github.com/kubeflow/trainer/blob/master/pkg/apis/trainer/v1alpha1/trainjob_types.go#L151

- **pkg/controller/jobs/kubeflow/trainjob/trainjob_controller.go:214** by [@andreyvelich](https://github.com/andreyvelich) — 2025-09-25T12:41:55Z
```diff
@@ -150,10 +158,74 @@ func (t *TrainJob) PodLabelSelector() string {
 }
 
 func getChildJobSet(t *TrainJob) (*jobsetapi.JobSet, error) {
-	jobSet := jobsetapi.JobSet{}
-	// TODO: Ideally we should be using the parent context here
-	err := reconciler.client.Get(context.Background(), types.NamespacedName{Name: t.Name, Namespace: t.Namespace}, &jobSet)
-	return &jobSet, err
+	runtimeRefGK := kftrainerruntime.RuntimeRefToRuntimeRegistryKey(t.Spec.RuntimeRef)
+	runtime, ok := reconciler.runtimes[runtimeRefGK]
+	if !ok {
+		return nil, fmt.Errorf("unsupported runtime: %s", runtimeRefGK)
+	}
+
+	trainJob := (*kftrainerapi.TrainJob)(t)
+	trSpec, err := getRuntimeSpec(trainJob)
+	if err != nil {
+		return nil, fmt.Errorf("runtime '%s' not found", trainJob.Spec.RuntimeRef.Name)
+	}
+	info, err := runtime.RuntimeInfo(trainJob, trSpec.Template, trSpec.MLPolicy, trSpec.PodGroupPolicy)
+	if err != nil {
+		return nil, err
+	}
+
+	// Get the jobsetSpecApply and apply the TrainJob object overrides for the trainer and initializer jobs
+	jobSetSpec, ok := kftrainerruntime.TemplateSpecApply[jobsetapplyapi.JobSetSpecApplyConfiguration](info)
+	if !ok {
+		return nil, err
+	}
+	jobsetApply := kftrainerjobset.NewBuilder(jobsetapplyapi.JobSet(t.Name, t.Namespace).
+		WithSpec(jobSetSpec)).Initializer(trainJob).Trainer(info, trainJob).PodLabels(info.Scheduler.PodLabels).Build()
+
+	// convert to jobset with the defaults set
+	return jobsetApplyToJobset(jobsetApply)
+}
+
+func jobsetApplyToJobset(jobsetApply *jobsetapplyapi.JobSetApplyConfiguration) (*jobsetapi.JobSet, error) {
+	jsonData, err := json.Marshal(jobsetApply)
+	if err != nil {
+		return nil, err
+	}
+
+	jobset := &jobsetapi.JobSet{}
+	if err := json.Unmarshal(jsonData, jobset); err != nil {
+		return nil, err
+	}
+
+	// Run a dry-run patch to set the jobset defaults
+	// Defaults must be applied here because Kueue later compares podsets to match workloads.
+	// Workloads coming from the API server are already defaulted, so without defaulting this JobSet, matching would fail.
+	if err = reconciler.client.Patch(context.Background(), jobset, client.Apply, &client.PatchOptions{
+		FieldManager: "defaulter",
+		DryRun:       []string{metav1.DryRunAll},
+	}); err != nil {
+		return nil, err
+	}
+	return jobset, nil
+}
+
+func getRuntimeSpec(trainJob *kftrainerapi.TrainJob) (*kftrainerapi.TrainingRuntimeSpec, error) {
+	// Defaults to ClusterTrainingRuntime according to trainjob_types.go
+	if trainJob.Spec.RuntimeRef.Kind == nil || *trainJob.Spec.RuntimeRef.Kind == "ClusterTrainingRuntime" {
```

  You can get constant from here: https://github.com/kubeflow/trainer/blob/master/pkg/apis/trainer/v1alpha1/trainingruntime_types.go#L30

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-09-28T19:29:58Z

- **pkg/controller/jobs/kubeflow/trainjob/trainjob_controller.go:189** by [@kaisoz](https://github.com/kaisoz) — 2025-09-28T19:29:57Z
```diff
@@ -150,10 +158,74 @@ func (t *TrainJob) PodLabelSelector() string {
 }
 
 func getChildJobSet(t *TrainJob) (*jobsetapi.JobSet, error) {
-	jobSet := jobsetapi.JobSet{}
-	// TODO: Ideally we should be using the parent context here
-	err := reconciler.client.Get(context.Background(), types.NamespacedName{Name: t.Name, Namespace: t.Namespace}, &jobSet)
-	return &jobSet, err
+	runtimeRefGK := kftrainerruntime.RuntimeRefToRuntimeRegistryKey(t.Spec.RuntimeRef)
+	runtime, ok := reconciler.runtimes[runtimeRefGK]
+	if !ok {
+		return nil, fmt.Errorf("unsupported runtime: %s", runtimeRefGK)
+	}
+
+	trainJob := (*kftrainerapi.TrainJob)(t)
+	trSpec, err := getRuntimeSpec(trainJob)
+	if err != nil {
+		return nil, fmt.Errorf("runtime '%s' not found", trainJob.Spec.RuntimeRef.Name)
+	}
+	info, err := runtime.RuntimeInfo(trainJob, trSpec.Template, trSpec.MLPolicy, trSpec.PodGroupPolicy)
+	if err != nil {
+		return nil, err
+	}
+
+	// Get the jobsetSpecApply and apply the TrainJob object overrides for the trainer and initializer jobs
+	jobSetSpec, ok := kftrainerruntime.TemplateSpecApply[jobsetapplyapi.JobSetSpecApplyConfiguration](info)
+	if !ok {
+		return nil, err
+	}
+	jobsetApply := kftrainerjobset.NewBuilder(jobsetapplyapi.JobSet(t.Name, t.Namespace).
+		WithSpec(jobSetSpec)).Initializer(trainJob).Trainer(info, trainJob).PodLabels(info.Scheduler.PodLabels).Build()
+
+	// convert to jobset with the defaults set
+	return jobsetApplyToJobset(jobsetApply)
```

  > Alternatively, it would be good if there was another helper in the Trainer which would return some struct (maybe StatusInfo or FullInfo) with all the details, but in Job-agnostic way.
  
  yeah, that's the end goal, but unfortunately the info object doesn't contain all the information that ends up in the JobSet. For example, the `TrainJob.Spec.Trainer.ResourcesPerNode` property [overrides the `Trainer` `replicaJob` resources](https://github.com/kubeflow/trainer/blob/56be1f8163fb3a9b4bd999bb42e810fe429a31eb/pkg/runtime/framework/plugins/jobset/builder.go#L142) directly

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-09-28T20:20:54Z

- **pkg/controller/jobframework/setup.go:113** by [@kaisoz](https://github.com/kaisoz) — 2025-09-28T20:20:54Z
```diff
@@ -109,20 +109,35 @@ func (m *integrationManager) setupControllers(ctx context.Context, mgr ctrl.Mana
 	})
 }
 
-func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
-	if err := cb.NewReconciler(
+func (m *integrationManager) setupControllerAndWebhook(ctx context.Context, mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	var (
```

  I added both because of the `TrainJob` controller, since it's the only one whose creation can fail in case it can't instantiate the Trainer Runtimes

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-09-28T20:49:13Z

- **pkg/controller/jobs/kubeflow/trainjob/trainjob_controller.go:353** by [@kaisoz](https://github.com/kaisoz) — 2025-09-28T20:49:13Z
```diff
@@ -256,19 +328,48 @@ func (t *TrainJob) Finished() (message string, success, finished bool) {
 }
 
 func (t *TrainJob) PodsReady() bool {
+	var (
+		replicas      int32
+		readyReplicas int32
+	)
 	jobset, err := getChildJobSet(t)
 	if err != nil {
 		return false
 	}
-	return (*kJobset.JobSet)(jobset).PodsReady()
+
+	for _, replicatedJob := range jobset.Spec.ReplicatedJobs {
+		replicas += replicatedJob.Replicas
+	}
+	for _, jobStatus := range t.Status.JobsStatus {
+		readyReplicas += jobStatus.Ready + jobStatus.Succeeded
+	}
+	return replicas == readyReplicas
 }
 
 func (t *TrainJob) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
+	if len(t.Status.JobsStatus) == 0 {
+		return nil, nil
+	}
 	jobset, err := getChildJobSet(t)
```

  Unfortunately the Trainjob `info` object doesn't have the job `.spec.Completions` property, it just has the `.spec.Parallelism` as shown [here](https://github.com/kubeflow/trainer/blob/56be1f8163fb3a9b4bd999bb42e810fe429a31eb/pkg/runtime/core/trainingruntime.go#L168). 
  
  In case of the `trainer` job, its `.spec.Completions` is set to `.spec.Paralellism` (code [here](https://github.com/kubeflow/trainer/blob/01a076346a81ae68d8ba69ffba182134cf33803d/pkg/runtime/framework/plugins/jobset/builder.go#L117)), but this is not the case for any other jobs of the `TrainingRuntime`. A potential solution would be to get the referenced `TrainingRuntime` and extract such information, but this would end up being the same as what I do... (recreating the `JobSet`). IMO the best solution would be to have that information in the `info` object.

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-09-28T20:54:47Z

- **test/integration/multikueue/suite_test.go:129** by [@kaisoz](https://github.com/kaisoz) — 2025-09-28T20:54:47Z
```diff
@@ -109,13 +115,52 @@ func createCluster(setupFnc framework.ManagerSetup, apiFeatureGates ...string) c
 	}
 	c.cfg = c.fwk.Init()
 	c.ctx, c.client = c.fwk.SetupClient(c.cfg)
+	installClusterTrainerRuntimes(c)
+
 	// skip the manager setup if setup func is not provided
 	if setupFnc != nil {
 		c.fwk.StartManager(c.ctx, c.cfg, setupFnc)
 	}
+
 	return c
 }
 
+// Install ClusterTrainingRuntime manifests so that they can be referenced in the tests.
+func installClusterTrainerRuntimes(c cluster) {
```

  This is not installing CRDs but CRs (`Custom Resources`). These are the default TrainingRuntimes delivered by the Kubeflow project ([doc](https://www.kubeflow.org/docs/components/trainer/operator-guides/runtime/#supported-runtimes) and [repo](https://github.com/kubeflow/trainer/tree/master/manifests/base/runtimes)). I install them here because I need to reference a Runtime from the `TrainJob`.
  
  However, I could skip this and create a custom `TrainingRuntime` as I do for the unit tests. Do you prefer this? Do you want me to remove the default runtimes installation from the e2e tests as well?

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-09-28T20:59:15Z

- **pkg/controller/jobs/kubeflow/trainjob/trainjob_controller.go:214** by [@kaisoz](https://github.com/kaisoz) — 2025-09-28T20:59:15Z
```diff
@@ -150,10 +158,74 @@ func (t *TrainJob) PodLabelSelector() string {
 }
 
 func getChildJobSet(t *TrainJob) (*jobsetapi.JobSet, error) {
-	jobSet := jobsetapi.JobSet{}
-	// TODO: Ideally we should be using the parent context here
-	err := reconciler.client.Get(context.Background(), types.NamespacedName{Name: t.Name, Namespace: t.Namespace}, &jobSet)
-	return &jobSet, err
+	runtimeRefGK := kftrainerruntime.RuntimeRefToRuntimeRegistryKey(t.Spec.RuntimeRef)
+	runtime, ok := reconciler.runtimes[runtimeRefGK]
+	if !ok {
+		return nil, fmt.Errorf("unsupported runtime: %s", runtimeRefGK)
+	}
+
+	trainJob := (*kftrainerapi.TrainJob)(t)
+	trSpec, err := getRuntimeSpec(trainJob)
+	if err != nil {
+		return nil, fmt.Errorf("runtime '%s' not found", trainJob.Spec.RuntimeRef.Name)
+	}
+	info, err := runtime.RuntimeInfo(trainJob, trSpec.Template, trSpec.MLPolicy, trSpec.PodGroupPolicy)
+	if err != nil {
+		return nil, err
+	}
+
+	// Get the jobsetSpecApply and apply the TrainJob object overrides for the trainer and initializer jobs
+	jobSetSpec, ok := kftrainerruntime.TemplateSpecApply[jobsetapplyapi.JobSetSpecApplyConfiguration](info)
+	if !ok {
+		return nil, err
+	}
+	jobsetApply := kftrainerjobset.NewBuilder(jobsetapplyapi.JobSet(t.Name, t.Namespace).
+		WithSpec(jobSetSpec)).Initializer(trainJob).Trainer(info, trainJob).PodLabels(info.Scheduler.PodLabels).Build()
+
+	// convert to jobset with the defaults set
+	return jobsetApplyToJobset(jobsetApply)
+}
+
+func jobsetApplyToJobset(jobsetApply *jobsetapplyapi.JobSetApplyConfiguration) (*jobsetapi.JobSet, error) {
+	jsonData, err := json.Marshal(jobsetApply)
+	if err != nil {
+		return nil, err
+	}
+
+	jobset := &jobsetapi.JobSet{}
+	if err := json.Unmarshal(jsonData, jobset); err != nil {
+		return nil, err
+	}
+
+	// Run a dry-run patch to set the jobset defaults
+	// Defaults must be applied here because Kueue later compares podsets to match workloads.
+	// Workloads coming from the API server are already defaulted, so without defaulting this JobSet, matching would fail.
+	if err = reconciler.client.Patch(context.Background(), jobset, client.Apply, &client.PatchOptions{
+		FieldManager: "defaulter",
+		DryRun:       []string{metav1.DryRunAll},
+	}); err != nil {
+		return nil, err
+	}
+	return jobset, nil
+}
+
+func getRuntimeSpec(trainJob *kftrainerapi.TrainJob) (*kftrainerapi.TrainingRuntimeSpec, error) {
+	// Defaults to ClusterTrainingRuntime according to trainjob_types.go
+	if trainJob.Spec.RuntimeRef.Kind == nil || *trainJob.Spec.RuntimeRef.Kind == "ClusterTrainingRuntime" {
```

  yes, you are right, because the reconciler gets the object once it's gone through the API server, which is the one applying the defaults. I'll do the change 👍🏻

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-29T10:02:28Z

- **test/integration/multikueue/suite_test.go:129** by [@mimowo](https://github.com/mimowo) — 2025-09-29T10:02:28Z
```diff
@@ -109,13 +115,52 @@ func createCluster(setupFnc framework.ManagerSetup, apiFeatureGates ...string) c
 	}
 	c.cfg = c.fwk.Init()
 	c.ctx, c.client = c.fwk.SetupClient(c.cfg)
+	installClusterTrainerRuntimes(c)
+
 	// skip the manager setup if setup func is not provided
 	if setupFnc != nil {
 		c.fwk.StartManager(c.ctx, c.cfg, setupFnc)
 	}
+
 	return c
 }
 
+// Install ClusterTrainingRuntime manifests so that they can be referenced in the tests.
+func installClusterTrainerRuntimes(c cluster) {
```

  > This is not installing CRDs but CRs (Custom Resources). These are the default TrainingRuntimes delivered by the Kubeflow project ([doc](https://www.kubeflow.org/docs/components/trainer/operator-guides/runtime/#supported-runtimes) and [repo](https://github.com/kubeflow/trainer/tree/master/manifests/base/runtimes)). I install them here because I need to reference a Runtime from the TrainJob
  
  I see, thank you for clarifying.
  
  > However, I could skip this and create a custom TrainingRuntime as I do for the unit tests. Do you prefer this? Do you want me to remove the default runtimes installation from the e2e tests as well?
  
  My comment does not refer to e2e, but integration tests. In any case I think it is better to provide our testing Runtimes, especially for the integration tests.
  
  The runtime we use could be quite minimal.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-29T10:29:17Z

- **pkg/controller/jobs/kubeflow/trainjob/trainjob_controller.go:189** by [@mimowo](https://github.com/mimowo) — 2025-09-29T10:29:17Z
```diff
@@ -150,10 +158,74 @@ func (t *TrainJob) PodLabelSelector() string {
 }
 
 func getChildJobSet(t *TrainJob) (*jobsetapi.JobSet, error) {
-	jobSet := jobsetapi.JobSet{}
-	// TODO: Ideally we should be using the parent context here
-	err := reconciler.client.Get(context.Background(), types.NamespacedName{Name: t.Name, Namespace: t.Namespace}, &jobSet)
-	return &jobSet, err
+	runtimeRefGK := kftrainerruntime.RuntimeRefToRuntimeRegistryKey(t.Spec.RuntimeRef)
+	runtime, ok := reconciler.runtimes[runtimeRefGK]
+	if !ok {
+		return nil, fmt.Errorf("unsupported runtime: %s", runtimeRefGK)
+	}
+
+	trainJob := (*kftrainerapi.TrainJob)(t)
+	trSpec, err := getRuntimeSpec(trainJob)
+	if err != nil {
+		return nil, fmt.Errorf("runtime '%s' not found", trainJob.Spec.RuntimeRef.Name)
+	}
+	info, err := runtime.RuntimeInfo(trainJob, trSpec.Template, trSpec.MLPolicy, trSpec.PodGroupPolicy)
+	if err != nil {
+		return nil, err
+	}
+
+	// Get the jobsetSpecApply and apply the TrainJob object overrides for the trainer and initializer jobs
+	jobSetSpec, ok := kftrainerruntime.TemplateSpecApply[jobsetapplyapi.JobSetSpecApplyConfiguration](info)
+	if !ok {
+		return nil, err
+	}
+	jobsetApply := kftrainerjobset.NewBuilder(jobsetapplyapi.JobSet(t.Name, t.Namespace).
+		WithSpec(jobSetSpec)).Initializer(trainJob).Trainer(info, trainJob).PodLabels(info.Scheduler.PodLabels).Build()
+
+	// convert to jobset with the defaults set
+	return jobsetApplyToJobset(jobsetApply)
```

  cc @andreyvelich @tenzen-y we may need to revisit the helpers and the information exposed by the Trainer so that the integration code in Kueue has all the necessary data.
  
  @kaisoz would you like to open an issue in the Trainer repo and document what is needed to achieve fully "Job" agnostic code in Kueue?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-29T10:32:37Z

- **pkg/controller/jobs/kubeflow/trainjob/trainjob_controller.go:353** by [@mimowo](https://github.com/mimowo) — 2025-09-29T10:32:37Z
```diff
@@ -256,19 +328,48 @@ func (t *TrainJob) Finished() (message string, success, finished bool) {
 }
 
 func (t *TrainJob) PodsReady() bool {
+	var (
+		replicas      int32
+		readyReplicas int32
+	)
 	jobset, err := getChildJobSet(t)
 	if err != nil {
 		return false
 	}
-	return (*kJobset.JobSet)(jobset).PodsReady()
+
+	for _, replicatedJob := range jobset.Spec.ReplicatedJobs {
+		replicas += replicatedJob.Replicas
+	}
+	for _, jobStatus := range t.Status.JobsStatus {
+		readyReplicas += jobStatus.Ready + jobStatus.Succeeded
+	}
+	return replicas == readyReplicas
 }
 
 func (t *TrainJob) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
+	if len(t.Status.JobsStatus) == 0 {
+		return nil, nil
+	}
 	jobset, err := getChildJobSet(t)
```

  > IMO the best solution would be to have that information in the info object.
  
  sounds reasonable. Let me open an issue to inside Kueue first to track the effort.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-29T10:48:40Z

- **pkg/controller/jobs/kubeflow/trainjob/trainjob_controller.go:189** by [@mimowo](https://github.com/mimowo) — 2025-09-29T10:48:40Z
```diff
@@ -150,10 +158,74 @@ func (t *TrainJob) PodLabelSelector() string {
 }
 
 func getChildJobSet(t *TrainJob) (*jobsetapi.JobSet, error) {
-	jobSet := jobsetapi.JobSet{}
-	// TODO: Ideally we should be using the parent context here
-	err := reconciler.client.Get(context.Background(), types.NamespacedName{Name: t.Name, Namespace: t.Namespace}, &jobSet)
-	return &jobSet, err
+	runtimeRefGK := kftrainerruntime.RuntimeRefToRuntimeRegistryKey(t.Spec.RuntimeRef)
+	runtime, ok := reconciler.runtimes[runtimeRefGK]
+	if !ok {
+		return nil, fmt.Errorf("unsupported runtime: %s", runtimeRefGK)
+	}
+
+	trainJob := (*kftrainerapi.TrainJob)(t)
+	trSpec, err := getRuntimeSpec(trainJob)
+	if err != nil {
+		return nil, fmt.Errorf("runtime '%s' not found", trainJob.Spec.RuntimeRef.Name)
+	}
+	info, err := runtime.RuntimeInfo(trainJob, trSpec.Template, trSpec.MLPolicy, trSpec.PodGroupPolicy)
+	if err != nil {
+		return nil, err
+	}
+
+	// Get the jobsetSpecApply and apply the TrainJob object overrides for the trainer and initializer jobs
+	jobSetSpec, ok := kftrainerruntime.TemplateSpecApply[jobsetapplyapi.JobSetSpecApplyConfiguration](info)
+	if !ok {
+		return nil, err
+	}
+	jobsetApply := kftrainerjobset.NewBuilder(jobsetapplyapi.JobSet(t.Name, t.Namespace).
+		WithSpec(jobSetSpec)).Initializer(trainJob).Trainer(info, trainJob).PodLabels(info.Scheduler.PodLabels).Build()
+
+	// convert to jobset with the defaults set
+	return jobsetApplyToJobset(jobsetApply)
```

  Ok, I opened the issue at the Kueue side: https://github.com/kubernetes-sigs/kueue/issues/7043

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-29T10:58:31Z

- **pkg/controller/jobs/jobset/jobset_controller.go:226** by [@mimowo](https://github.com/mimowo) — 2025-09-29T10:58:31Z
```diff
@@ -223,7 +223,7 @@ func (j *JobSet) SetManagedBy(managedBy *string) {
 	j.Spec.ManagedBy = managedBy
 }
 
-func podsCountPerReplica(rj *jobsetapi.ReplicatedJob) int32 {
+func PodsCountPerReplica(rj *jobsetapi.ReplicatedJob) int32 {
```

  Hope we can revert that after https://github.com/kubernetes-sigs/kueue/pull/6997

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-09-29T11:42:41Z

- **pkg/controller/jobs/jobset/jobset_controller.go:226** by [@kaisoz](https://github.com/kaisoz) — 2025-09-29T11:42:41Z
```diff
@@ -223,7 +223,7 @@ func (j *JobSet) SetManagedBy(managedBy *string) {
 	j.Spec.ManagedBy = managedBy
 }
 
-func podsCountPerReplica(rj *jobsetapi.ReplicatedJob) int32 {
+func PodsCountPerReplica(rj *jobsetapi.ReplicatedJob) int32 {
```

  💯  it'll be gone once the infos expose all the required properties

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-09-29T11:43:25Z

- **pkg/controller/jobs/kubeflow/trainjob/trainjob_controller.go:189** by [@kaisoz](https://github.com/kaisoz) — 2025-09-29T11:43:25Z
```diff
@@ -150,10 +158,74 @@ func (t *TrainJob) PodLabelSelector() string {
 }
 
 func getChildJobSet(t *TrainJob) (*jobsetapi.JobSet, error) {
-	jobSet := jobsetapi.JobSet{}
-	// TODO: Ideally we should be using the parent context here
-	err := reconciler.client.Get(context.Background(), types.NamespacedName{Name: t.Name, Namespace: t.Namespace}, &jobSet)
-	return &jobSet, err
+	runtimeRefGK := kftrainerruntime.RuntimeRefToRuntimeRegistryKey(t.Spec.RuntimeRef)
+	runtime, ok := reconciler.runtimes[runtimeRefGK]
+	if !ok {
+		return nil, fmt.Errorf("unsupported runtime: %s", runtimeRefGK)
+	}
+
+	trainJob := (*kftrainerapi.TrainJob)(t)
+	trSpec, err := getRuntimeSpec(trainJob)
+	if err != nil {
+		return nil, fmt.Errorf("runtime '%s' not found", trainJob.Spec.RuntimeRef.Name)
+	}
+	info, err := runtime.RuntimeInfo(trainJob, trSpec.Template, trSpec.MLPolicy, trSpec.PodGroupPolicy)
+	if err != nil {
+		return nil, err
+	}
+
+	// Get the jobsetSpecApply and apply the TrainJob object overrides for the trainer and initializer jobs
+	jobSetSpec, ok := kftrainerruntime.TemplateSpecApply[jobsetapplyapi.JobSetSpecApplyConfiguration](info)
+	if !ok {
+		return nil, err
+	}
+	jobsetApply := kftrainerjobset.NewBuilder(jobsetapplyapi.JobSet(t.Name, t.Namespace).
+		WithSpec(jobSetSpec)).Initializer(trainJob).Trainer(info, trainJob).PodLabels(info.Scheduler.PodLabels).Build()
+
+	// convert to jobset with the defaults set
+	return jobsetApplyToJobset(jobsetApply)
```

  Thanks @mimowo, I'll gather all the requirements and open a issue in the Trainer repo 👍🏻

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-29T11:54:13Z

/lgtm
/approve

I reviewed the change and I'm happy as for the Alpha integration. 
I opened https://github.com/kubernetes-sigs/kueue/issues/7043 for the follow up. 
Hope we can have 2.0.1 full release before 0.14.0,  then the PR to update the dependency will be very small.
