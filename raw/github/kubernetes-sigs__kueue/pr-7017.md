# PR #7017: add kal linter

**Summary**: add kal linter

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7017

**Last updated**: 2025-10-03T14:05:31Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kannon92](https://github.com/kannon92)
- **Branch**: `enable-kai-kueue` → `main`
- **Created**: 2025-09-25T19:58:13Z
- **Updated**: 2025-10-03T14:05:31Z
- **Merged**: 2025-09-29T17:00:18Z
- **Closed**: 2025-09-29T17:00:18Z
- **Labels**: `lgtm`, `size/XXL`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo)
- **Changed files**: 40   **+568 / -434**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add KAI linter to kueue
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #7012 

#### Special notes for your reviewer:

I had to limit the scope of linter changes so I avoid breaking changes.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-09-25T19:58:19Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | e79aa2ece719e36a3dbd733d2ad1c7a9cda013af |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68daaf6d3890e00008eab5b2 |

### Comment by [@kannon92](https://github.com/kannon92) — 2025-09-26T16:12:35Z

@mimowo @mbobrovskyi @tenzen-y 

We may need to sync up on how to apply all these recommendations from the linter.

If I apply lint-api-fix, there are breaking API changes (changing pointers to non pointers based on serialization behavior).

I'm not entirely sure if we should fix all these recommendations atm. 

I could roll this out with a small subset of the linter and fix more as we go.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-26T16:22:07Z

> If I apply lint-api-fix, there are breaking API changes (changing pointers to non pointers based on serialization behavior).

Is it possible to configure the linter to avoid changing pointers?

### Comment by [@kannon92](https://github.com/kannon92) — 2025-09-27T03:36:28Z

> @mimowo @mbobrovskyi @tenzen-y
> 
> We may need to sync up on how to apply all these recommendations from the linter.
> 
> If I apply lint-api-fix, there are breaking API changes (changing pointers to non pointers based on serialization behavior).
> 
> I'm not entirely sure if we should fix all these recommendations atm.
> 
> I could roll this out with a small subset of the linter and fix more as we go.

I disabled quite a few linters but I think I got a good first patch for this. This is ready for review!

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-27T05:56:47Z

Looks great overall! Just one comment. This is an excellent feature, and I believe it will help us create a clearer API in the future. Thank you!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-29T07:21:36Z

LGTM label has been added.  <details>Git tree hash: d6f208e178e91104d4209128e6bade28494c81ef</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-29T07:21:40Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7017#" title="Author self-approved">kannon92</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7017#pullrequestreview-3278267857" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-29T07:22:40Z

> I'm not entirely sure if we should fix all these recommendations atm.
> 
> I could roll this out with a small subset of the linter and fix more as we go.

Makes sense to me to go in smaller steps, otherwise the diff would be not readable.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-29T16:11:22Z

/lgtm
Thank you!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-29T16:11:30Z

LGTM label has been added.  <details>Git tree hash: 0270d55e8f879970b2e20db2dd0a19dca0d6b5c6</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-29T16:11:48Z

@mimowo @tenzen-y can we unhold?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-29T16:17:11Z

> @mimowo @tenzen-y can we unhold?

I think so, this is mostly tweaking comments + some "required" fields which looks backwards-compatible.

It is nice to merge before 0.14 so that future diffs with v1beta2 are not too big.

/unhold

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-29T16:18:22Z

FYI @kannon92 you may need to rebase after https://github.com/kubernetes-sigs/kueue/pull/7056 (likely)

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-29T16:53:27Z

/test pull-kueue-test-integration-multikueue-main
looks like flake might be related to https://github.com/kubernetes-sigs/kueue/issues/7033

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-26T02:45:14Z

- **hack/kal-linter/.custom-gcl.yaml:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-26T02:45:14Z
  I'm wondering if we can use it as plugin https://github.com/kubernetes-sigs/kube-api-linter?tab=readme-ov-file#golangci-lint-plugin.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-09-26T13:03:00Z

- **hack/kal-linter/.custom-gcl.yaml:1** by [@kannon92](https://github.com/kannon92) — 2025-09-26T13:03:00Z
  I discussed this with @everettraven and his suggestion was to use the golangci custom instead of plugin.
  
  I tried to get plugin working but it was a bit harder to do. 
  
  It requires building a so file and loading that into golangci.

### Review by [@everettraven](https://github.com/everettraven) (commented) — 2025-09-26T13:25:41Z

- **hack/kal-linter/.custom-gcl.yaml:1** by [@everettraven](https://github.com/everettraven) — 2025-09-26T13:25:41Z
  The module approach is now the recommended approach by golangci-lint themselves. Going to the plugin documentation in https://golangci-lint.run/docs/plugins/go-plugins/ shows a warning banner that recommends the Go module approach instead :)
  
  It definitely is the easier path forward because you don't have to build the kube-api-linter `.so` plugin file and then pull it into golangci-lint.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-26T15:43:35Z

- **Makefile:234** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-26T15:43:35Z
```diff
@@ -229,13 +229,18 @@ vet: ## Run go vet against code.
 	$(GO_CMD) vet ./...
 
 .PHONY: ci-lint
-ci-lint: golangci-lint
+ci-lint: golangci-lint golangci-lint-kal
 	find . -path ./site -prune -false -o -name go.mod -exec dirname {} \; | xargs -I {} sh -c 'cd "{}" && $(GOLANGCI_LINT) run $(GOLANGCI_LINT_FIX) --timeout 15m0s --config "$(PROJECT_DIR)/.golangci.yaml"'
+	$(GOLANGCI_LINT_KAL) run -v --config $(PROJECT_DIR)/.golangci-kal.yml $(GOLANGCI_LINT_EXTRA_ARGS)
```

  Why this duplication?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-26T15:44:24Z

- **Makefile:232** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-26T15:44:24Z
```diff
@@ -229,13 +229,18 @@ vet: ## Run go vet against code.
 	$(GO_CMD) vet ./...
 
 .PHONY: ci-lint
-ci-lint: golangci-lint
+ci-lint: golangci-lint golangci-lint-kal
```

  Should we add golangci-lint-kal here? It might be better to move it to the verify step.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-26T15:45:15Z

- **Makefile:242** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-26T15:45:15Z
```diff
@@ -229,13 +229,18 @@ vet: ## Run go vet against code.
 	$(GO_CMD) vet ./...
 
 .PHONY: ci-lint
-ci-lint: golangci-lint
+ci-lint: golangci-lint golangci-lint-kal
 	find . -path ./site -prune -false -o -name go.mod -exec dirname {} \; | xargs -I {} sh -c 'cd "{}" && $(GOLANGCI_LINT) run $(GOLANGCI_LINT_FIX) --timeout 15m0s --config "$(PROJECT_DIR)/.golangci.yaml"'
+	$(GOLANGCI_LINT_KAL) run -v --config $(PROJECT_DIR)/.golangci-kal.yml $(GOLANGCI_LINT_EXTRA_ARGS)
 
 .PHONY: lint-fix
 lint-fix: GOLANGCI_LINT_FIX=--fix
 lint-fix: ci-lint
 
+.PHONY: lint-api
+lint-api: golangci-lint-kal
+	$(GOLANGCI_LINT_KAL) run -v --config $(PROJECT_DIR)/.golangci-kal.yml $(GOLANGCI_LINT_EXTRA_ARGS)
```

  What is GOLANGCI_LINT_EXTRA_ARGS?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-26T15:45:49Z

- **hack/kal-linter/.custom-gcl.yaml:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-26T15:45:49Z
  I see. Thank you for explanation.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-26T15:49:10Z

- **Makefile:239** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-26T15:49:10Z
```diff
@@ -229,13 +229,18 @@ vet: ## Run go vet against code.
 	$(GO_CMD) vet ./...
 
 .PHONY: ci-lint
-ci-lint: golangci-lint
+ci-lint: golangci-lint golangci-lint-kal
 	find . -path ./site -prune -false -o -name go.mod -exec dirname {} \; | xargs -I {} sh -c 'cd "{}" && $(GOLANGCI_LINT) run $(GOLANGCI_LINT_FIX) --timeout 15m0s --config "$(PROJECT_DIR)/.golangci.yaml"'
+	$(GOLANGCI_LINT_KAL) run -v --config $(PROJECT_DIR)/.golangci-kal.yml $(GOLANGCI_LINT_EXTRA_ARGS)
 
 .PHONY: lint-fix
 lint-fix: GOLANGCI_LINT_FIX=--fix
 lint-fix: ci-lint
 
+.PHONY: lint-api
```

  I'm wondering if we can also add `.PHONY: lint-api-fix` to automatically fix errors.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-09-26T15:50:38Z

- **Makefile:239** by [@kannon92](https://github.com/kannon92) — 2025-09-26T15:50:38Z
```diff
@@ -229,13 +229,18 @@ vet: ## Run go vet against code.
 	$(GO_CMD) vet ./...
 
 .PHONY: ci-lint
-ci-lint: golangci-lint
+ci-lint: golangci-lint golangci-lint-kal
 	find . -path ./site -prune -false -o -name go.mod -exec dirname {} \; | xargs -I {} sh -c 'cd "{}" && $(GOLANGCI_LINT) run $(GOLANGCI_LINT_FIX) --timeout 15m0s --config "$(PROJECT_DIR)/.golangci.yaml"'
+	$(GOLANGCI_LINT_KAL) run -v --config $(PROJECT_DIR)/.golangci-kal.yml $(GOLANGCI_LINT_EXTRA_ARGS)
 
 .PHONY: lint-fix
 lint-fix: GOLANGCI_LINT_FIX=--fix
 lint-fix: ci-lint
 
+.PHONY: lint-api
```

  Yea there was one but I didn't see it actually do anything.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-26T15:50:54Z

- **hack/kal-linter/.custom-gcl.yaml:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-26T15:50:54Z
  I'm not sure this is the right location for this file.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-26T16:00:21Z

- **Makefile:239** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-26T16:00:21Z
```diff
@@ -229,13 +229,18 @@ vet: ## Run go vet against code.
 	$(GO_CMD) vet ./...
 
 .PHONY: ci-lint
-ci-lint: golangci-lint
+ci-lint: golangci-lint golangci-lint-kal
 	find . -path ./site -prune -false -o -name go.mod -exec dirname {} \; | xargs -I {} sh -c 'cd "{}" && $(GOLANGCI_LINT) run $(GOLANGCI_LINT_FIX) --timeout 15m0s --config "$(PROJECT_DIR)/.golangci.yaml"'
+	$(GOLANGCI_LINT_KAL) run -v --config $(PROJECT_DIR)/.golangci-kal.yml $(GOLANGCI_LINT_EXTRA_ARGS)
 
 .PHONY: lint-fix
 lint-fix: GOLANGCI_LINT_FIX=--fix
 lint-fix: ci-lint
 
+.PHONY: lint-api
```

  I tested it, and the update is working for me, though not for all cases.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-26T16:07:23Z

- **.golangci-kal.yml:3** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-26T16:07:23Z
```diff
@@ -0,0 +1,76 @@
+version: "2"
+run:
+  go: "1.24"
```

  Can we avoid this configuration to get version from go.mod automatically?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-26T16:09:19Z

- **.golangci-kal.yml:68** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-26T16:09:19Z
```diff
@@ -0,0 +1,76 @@
+version: "2"
+run:
+  go: "1.24"
+  allow-parallel-runners: true
+linters:
+  default: none
+  enable:
+    - kubeapilinter # linter for Kube API conventions
+  settings:
+    custom:
+      kubeapilinter:
+        type: module
+        description: KAL is the Kube-API-Linter and lints Kube like APIs based on API conventions and best practices.
+        settings:
+          linters:
+            enable:
+              - "commentstart" # Ensure comments start with the serialized version of the field name.
+              - "conditions" # Ensure conditions have the correct json tags and markers.
+              - "conflictingmarkers"
+              - "duplicatemarkers" # Ensure there are no exact duplicate markers. for types and fields.
+              #- "forbiddenmarkers" # Ensure that types and fields do not contain any markers that are forbidden.
+              - "integers" # Ensure only int32 and int64 are used for integers.
+              - "jsontags" # Ensure every field has a json tag.
+              - "maxlength" # Ensure all strings and arrays have maximum lengths/maximum items.
+              - "nobools" # Bools do not evolve over time, should use enums instead.
+              - "nodurations" # Prevents usage of `Duration` types.
+              - "nofloats" # Ensure floats are not used.
+              - "nomaps" # Ensure maps are not used.
+              - "nonullable" # Ensure that types and fields do not have the nullable marker.
+              - "notimestamp" # Prevents usage of 'Timestamp' fields
+              - "optionalfields" # Ensure that all fields marked as optional adhere to being pointers and
+                                 # having the `omitempty` value in their `json` tag where appropriate.
+              - "optionalorrequired" # Every field should be marked as `+optional` or `+required`.
+              - "requiredfields" # Required fields should not be pointers, and should not have `omitempty`.
+              - "ssatags" # Ensure array fields have the appropriate listType markers
+              - "statusoptional" # Ensure all first children within status should be optional.
+              - "statussubresource" # All root objects that have a `status` field should have a status subresource.
+              - "uniquemarkers" # Ensure that types and fields do not contain more than a single definition of a marker that should only be present once.
+
+            # Per discussion in July 2024, we are keeping phase fields for now.
+            # See https://github.com/kubernetes-sigs/cluster-api/pull/10897#discussion_r1685929508
+            # and https://github.com/kubernetes-sigs/cluster-api/pull/10897#discussion_r1685919394.
+            # - "nophase" # Phase fields are discouraged by the Kube API conventions, use conditions instead.
+
+            # Linters below this line are disabled, pending conversation on how and when to enable them.
+            disable:
+            - "*" # We will manually enable new linters after understanding the impact. Disable all by default.
+          lintersConfig:
+            conflictingmarkers:
+              conflicts:
+              - name: "default_vs_required"
+                sets:
+                  - ["default", "kubebuilder:default"]
+                  - ["required", "kubebuilder:validation:Required", "k8s:required"]
+                description: "A field with a default value cannot be required"
+            conditions:
+              isFirstField: Warn # Require conditions to be the first field in the status struct.
+              usePatchStrategy: Forbid # Forbid patchStrategy markers on the Conditions field.
+              useProtobuf: Forbid # We don't use protobuf, so protobuf tags are not required.
+            optionalfields:
+              pointers:
+                preference: WhenRequired # Always | WhenRequired # Whether to always require pointers, or only when required. Defaults to `Always`.
+                policy: SuggestFix # SuggestFix | Warn # The policy for pointers in optional fields. Defaults to `SuggestFix`.
+              omitempty:
+                policy: SuggestFix # SuggestFix | Warn | Ignore # The policy for omitempty in optional fields. Defaults to `SuggestFix`.
+
+  exclusions:
+    generated: strict
+    rules:
+    ## KAL should only run on API folders.
+    - path-except: "apis/kueue/*"
+      linters:
+        - kubeapilinter
```

  Why this?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-09-26T16:13:18Z

- **Makefile:239** by [@kannon92](https://github.com/kannon92) — 2025-09-26T16:13:18Z
```diff
@@ -229,13 +229,18 @@ vet: ## Run go vet against code.
 	$(GO_CMD) vet ./...
 
 .PHONY: ci-lint
-ci-lint: golangci-lint
+ci-lint: golangci-lint golangci-lint-kal
 	find . -path ./site -prune -false -o -name go.mod -exec dirname {} \; | xargs -I {} sh -c 'cd "{}" && $(GOLANGCI_LINT) run $(GOLANGCI_LINT_FIX) --timeout 15m0s --config "$(PROJECT_DIR)/.golangci.yaml"'
+	$(GOLANGCI_LINT_KAL) run -v --config $(PROJECT_DIR)/.golangci-kal.yml $(GOLANGCI_LINT_EXTRA_ARGS)
 
 .PHONY: lint-fix
 lint-fix: GOLANGCI_LINT_FIX=--fix
 lint-fix: ci-lint
 
+.PHONY: lint-api
```

  I see. Yea it added a few breaking changes with pointers to non pointers. I'm not sure if we should apply those right now though.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-09-26T16:14:01Z

- **Makefile:242** by [@kannon92](https://github.com/kannon92) — 2025-09-26T16:14:01Z
```diff
@@ -229,13 +229,18 @@ vet: ## Run go vet against code.
 	$(GO_CMD) vet ./...
 
 .PHONY: ci-lint
-ci-lint: golangci-lint
+ci-lint: golangci-lint golangci-lint-kal
 	find . -path ./site -prune -false -o -name go.mod -exec dirname {} \; | xargs -I {} sh -c 'cd "{}" && $(GOLANGCI_LINT) run $(GOLANGCI_LINT_FIX) --timeout 15m0s --config "$(PROJECT_DIR)/.golangci.yaml"'
+	$(GOLANGCI_LINT_KAL) run -v --config $(PROJECT_DIR)/.golangci-kal.yml $(GOLANGCI_LINT_EXTRA_ARGS)
 
 .PHONY: lint-fix
 lint-fix: GOLANGCI_LINT_FIX=--fix
 lint-fix: ci-lint
 
+.PHONY: lint-api
+lint-api: golangci-lint-kal
+	$(GOLANGCI_LINT_KAL) run -v --config $(PROJECT_DIR)/.golangci-kal.yml $(GOLANGCI_LINT_EXTRA_ARGS)
```

  I think its a variable to pass more agruments to lint.
  
  I copied this code from cluster-api. It seems to be a common pattern to pass other arguments to linters. We don't necessarly need it right now.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-26T16:24:52Z

- **Makefile:239** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-26T16:24:52Z
```diff
@@ -229,13 +229,18 @@ vet: ## Run go vet against code.
 	$(GO_CMD) vet ./...
 
 .PHONY: ci-lint
-ci-lint: golangci-lint
+ci-lint: golangci-lint golangci-lint-kal
 	find . -path ./site -prune -false -o -name go.mod -exec dirname {} \; | xargs -I {} sh -c 'cd "{}" && $(GOLANGCI_LINT) run $(GOLANGCI_LINT_FIX) --timeout 15m0s --config "$(PROJECT_DIR)/.golangci.yaml"'
+	$(GOLANGCI_LINT_KAL) run -v --config $(PROJECT_DIR)/.golangci-kal.yml $(GOLANGCI_LINT_EXTRA_ARGS)
 
 .PHONY: lint-fix
 lint-fix: GOLANGCI_LINT_FIX=--fix
 lint-fix: ci-lint
 
+.PHONY: lint-api
```

  Can we enable only the linters we need?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-26T16:26:26Z

- **Makefile:242** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-26T16:26:26Z
```diff
@@ -229,13 +229,18 @@ vet: ## Run go vet against code.
 	$(GO_CMD) vet ./...
 
 .PHONY: ci-lint
-ci-lint: golangci-lint
+ci-lint: golangci-lint golangci-lint-kal
 	find . -path ./site -prune -false -o -name go.mod -exec dirname {} \; | xargs -I {} sh -c 'cd "{}" && $(GOLANGCI_LINT) run $(GOLANGCI_LINT_FIX) --timeout 15m0s --config "$(PROJECT_DIR)/.golangci.yaml"'
+	$(GOLANGCI_LINT_KAL) run -v --config $(PROJECT_DIR)/.golangci-kal.yml $(GOLANGCI_LINT_EXTRA_ARGS)
 
 .PHONY: lint-fix
 lint-fix: GOLANGCI_LINT_FIX=--fix
 lint-fix: ci-lint
 
+.PHONY: lint-api
+lint-api: golangci-lint-kal
+	$(GOLANGCI_LINT_KAL) run -v --config $(PROJECT_DIR)/.golangci-kal.yml $(GOLANGCI_LINT_EXTRA_ARGS)
```

  I don't think we need it right now. Alternatively, we could add this variable to ci-lint for consistency.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-09-26T16:39:12Z

- **.golangci-kal.yml:68** by [@kannon92](https://github.com/kannon92) — 2025-09-26T16:39:12Z
```diff
@@ -0,0 +1,76 @@
+version: "2"
+run:
+  go: "1.24"
+  allow-parallel-runners: true
+linters:
+  default: none
+  enable:
+    - kubeapilinter # linter for Kube API conventions
+  settings:
+    custom:
+      kubeapilinter:
+        type: module
+        description: KAL is the Kube-API-Linter and lints Kube like APIs based on API conventions and best practices.
+        settings:
+          linters:
+            enable:
+              - "commentstart" # Ensure comments start with the serialized version of the field name.
+              - "conditions" # Ensure conditions have the correct json tags and markers.
+              - "conflictingmarkers"
+              - "duplicatemarkers" # Ensure there are no exact duplicate markers. for types and fields.
+              #- "forbiddenmarkers" # Ensure that types and fields do not contain any markers that are forbidden.
+              - "integers" # Ensure only int32 and int64 are used for integers.
+              - "jsontags" # Ensure every field has a json tag.
+              - "maxlength" # Ensure all strings and arrays have maximum lengths/maximum items.
+              - "nobools" # Bools do not evolve over time, should use enums instead.
+              - "nodurations" # Prevents usage of `Duration` types.
+              - "nofloats" # Ensure floats are not used.
+              - "nomaps" # Ensure maps are not used.
+              - "nonullable" # Ensure that types and fields do not have the nullable marker.
+              - "notimestamp" # Prevents usage of 'Timestamp' fields
+              - "optionalfields" # Ensure that all fields marked as optional adhere to being pointers and
+                                 # having the `omitempty` value in their `json` tag where appropriate.
+              - "optionalorrequired" # Every field should be marked as `+optional` or `+required`.
+              - "requiredfields" # Required fields should not be pointers, and should not have `omitempty`.
+              - "ssatags" # Ensure array fields have the appropriate listType markers
+              - "statusoptional" # Ensure all first children within status should be optional.
+              - "statussubresource" # All root objects that have a `status` field should have a status subresource.
+              - "uniquemarkers" # Ensure that types and fields do not contain more than a single definition of a marker that should only be present once.
+
+            # Per discussion in July 2024, we are keeping phase fields for now.
+            # See https://github.com/kubernetes-sigs/cluster-api/pull/10897#discussion_r1685929508
+            # and https://github.com/kubernetes-sigs/cluster-api/pull/10897#discussion_r1685919394.
+            # - "nophase" # Phase fields are discouraged by the Kube API conventions, use conditions instead.
+
+            # Linters below this line are disabled, pending conversation on how and when to enable them.
+            disable:
+            - "*" # We will manually enable new linters after understanding the impact. Disable all by default.
+          lintersConfig:
+            conflictingmarkers:
+              conflicts:
+              - name: "default_vs_required"
+                sets:
+                  - ["default", "kubebuilder:default"]
+                  - ["required", "kubebuilder:validation:Required", "k8s:required"]
+                description: "A field with a default value cannot be required"
+            conditions:
+              isFirstField: Warn # Require conditions to be the first field in the status struct.
+              usePatchStrategy: Forbid # Forbid patchStrategy markers on the Conditions field.
+              useProtobuf: Forbid # We don't use protobuf, so protobuf tags are not required.
+            optionalfields:
+              pointers:
+                preference: WhenRequired # Always | WhenRequired # Whether to always require pointers, or only when required. Defaults to `Always`.
+                policy: SuggestFix # SuggestFix | Warn # The policy for pointers in optional fields. Defaults to `SuggestFix`.
+              omitempty:
+                policy: SuggestFix # SuggestFix | Warn | Ignore # The policy for omitempty in optional fields. Defaults to `SuggestFix`.
+
+  exclusions:
+    generated: strict
+    rules:
+    ## KAL should only run on API folders.
+    - path-except: "apis/kueue/*"
+      linters:
+        - kubeapilinter
```

  I'm honestly not sure. It seems to be required on other configs I saw.
  
  I find the config not very intuitive but path-expect seems to say we run this linter on that file.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-09-26T16:39:28Z

- **Makefile:232** by [@kannon92](https://github.com/kannon92) — 2025-09-26T16:39:28Z
```diff
@@ -229,13 +229,18 @@ vet: ## Run go vet against code.
 	$(GO_CMD) vet ./...
 
 .PHONY: ci-lint
-ci-lint: golangci-lint
+ci-lint: golangci-lint golangci-lint-kal
```

  Done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-09-26T16:39:41Z

- **Makefile:239** by [@kannon92](https://github.com/kannon92) — 2025-09-26T16:39:41Z
```diff
@@ -229,13 +229,18 @@ vet: ## Run go vet against code.
 	$(GO_CMD) vet ./...
 
 .PHONY: ci-lint
-ci-lint: golangci-lint
+ci-lint: golangci-lint golangci-lint-kal
 	find . -path ./site -prune -false -o -name go.mod -exec dirname {} \; | xargs -I {} sh -c 'cd "{}" && $(GOLANGCI_LINT) run $(GOLANGCI_LINT_FIX) --timeout 15m0s --config "$(PROJECT_DIR)/.golangci.yaml"'
+	$(GOLANGCI_LINT_KAL) run -v --config $(PROJECT_DIR)/.golangci-kal.yml $(GOLANGCI_LINT_EXTRA_ARGS)
 
 .PHONY: lint-fix
 lint-fix: GOLANGCI_LINT_FIX=--fix
 lint-fix: ci-lint
 
+.PHONY: lint-api
```

  The latest commit does this.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-09-26T16:40:43Z

- **hack/kal-linter/.custom-gcl.yaml:1** by [@kannon92](https://github.com/kannon92) — 2025-09-26T16:40:42Z
  Where should I put it?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-26T16:42:22Z

- **hack/kal-linter/.custom-gcl.yaml:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-26T16:42:22Z
  Yeah, this is a good question. Probably create separate directory for this. 
  
  @mimowo @tenzen-y WDYT?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-26T16:44:07Z

- **Makefile:245** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-26T16:44:07Z
```diff
@@ -229,13 +229,21 @@ vet: ## Run go vet against code.
 	$(GO_CMD) vet ./...
 
 .PHONY: ci-lint
-ci-lint: golangci-lint
+ci-lint: golangci-lint golangci-lint-kal
 	find . -path ./site -prune -false -o -name go.mod -exec dirname {} \; | xargs -I {} sh -c 'cd "{}" && $(GOLANGCI_LINT) run $(GOLANGCI_LINT_FIX) --timeout 15m0s --config "$(PROJECT_DIR)/.golangci.yaml"'
 
 .PHONY: lint-fix
 lint-fix: GOLANGCI_LINT_FIX=--fix
 lint-fix: ci-lint
 
+.PHONY: lint-api
+lint-api: golangci-lint-kal
+	$(GOLANGCI_LINT_KAL) run -v --config $(PROJECT_DIR)/.golangci-kal.yml
+
+.PHONY: lint-api-fix
+lint-api-fix: golangci-lint-kal
+	$(GOLANGCI_LINT_KAL) run -v --config $(PROJECT_DIR)/.golangci-kal.yml --fix
```

  Can we add a variable and reuse lint-api in the same way we use lint-fix?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-26T16:45:59Z

- **.golangci-kal.yml:68** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-26T16:45:59Z
```diff
@@ -0,0 +1,76 @@
+version: "2"
+run:
+  go: "1.24"
+  allow-parallel-runners: true
+linters:
+  default: none
+  enable:
+    - kubeapilinter # linter for Kube API conventions
+  settings:
+    custom:
+      kubeapilinter:
+        type: module
+        description: KAL is the Kube-API-Linter and lints Kube like APIs based on API conventions and best practices.
+        settings:
+          linters:
+            enable:
+              - "commentstart" # Ensure comments start with the serialized version of the field name.
+              - "conditions" # Ensure conditions have the correct json tags and markers.
+              - "conflictingmarkers"
+              - "duplicatemarkers" # Ensure there are no exact duplicate markers. for types and fields.
+              #- "forbiddenmarkers" # Ensure that types and fields do not contain any markers that are forbidden.
+              - "integers" # Ensure only int32 and int64 are used for integers.
+              - "jsontags" # Ensure every field has a json tag.
+              - "maxlength" # Ensure all strings and arrays have maximum lengths/maximum items.
+              - "nobools" # Bools do not evolve over time, should use enums instead.
+              - "nodurations" # Prevents usage of `Duration` types.
+              - "nofloats" # Ensure floats are not used.
+              - "nomaps" # Ensure maps are not used.
+              - "nonullable" # Ensure that types and fields do not have the nullable marker.
+              - "notimestamp" # Prevents usage of 'Timestamp' fields
+              - "optionalfields" # Ensure that all fields marked as optional adhere to being pointers and
+                                 # having the `omitempty` value in their `json` tag where appropriate.
+              - "optionalorrequired" # Every field should be marked as `+optional` or `+required`.
+              - "requiredfields" # Required fields should not be pointers, and should not have `omitempty`.
+              - "ssatags" # Ensure array fields have the appropriate listType markers
+              - "statusoptional" # Ensure all first children within status should be optional.
+              - "statussubresource" # All root objects that have a `status` field should have a status subresource.
+              - "uniquemarkers" # Ensure that types and fields do not contain more than a single definition of a marker that should only be present once.
+
+            # Per discussion in July 2024, we are keeping phase fields for now.
+            # See https://github.com/kubernetes-sigs/cluster-api/pull/10897#discussion_r1685929508
+            # and https://github.com/kubernetes-sigs/cluster-api/pull/10897#discussion_r1685919394.
+            # - "nophase" # Phase fields are discouraged by the Kube API conventions, use conditions instead.
+
+            # Linters below this line are disabled, pending conversation on how and when to enable them.
+            disable:
+            - "*" # We will manually enable new linters after understanding the impact. Disable all by default.
+          lintersConfig:
+            conflictingmarkers:
+              conflicts:
+              - name: "default_vs_required"
+                sets:
+                  - ["default", "kubebuilder:default"]
+                  - ["required", "kubebuilder:validation:Required", "k8s:required"]
+                description: "A field with a default value cannot be required"
+            conditions:
+              isFirstField: Warn # Require conditions to be the first field in the status struct.
+              usePatchStrategy: Forbid # Forbid patchStrategy markers on the Conditions field.
+              useProtobuf: Forbid # We don't use protobuf, so protobuf tags are not required.
+            optionalfields:
+              pointers:
+                preference: WhenRequired # Always | WhenRequired # Whether to always require pointers, or only when required. Defaults to `Always`.
+                policy: SuggestFix # SuggestFix | Warn # The policy for pointers in optional fields. Defaults to `SuggestFix`.
+              omitempty:
+                policy: SuggestFix # SuggestFix | Warn | Ignore # The policy for omitempty in optional fields. Defaults to `SuggestFix`.
+
+  exclusions:
+    generated: strict
+    rules:
+    ## KAL should only run on API folders.
+    - path-except: "apis/kueue/*"
+      linters:
+        - kubeapilinter
```

  What happens if remove this? It looks like we are skipping other apis.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-09-26T17:25:41Z

- **.golangci-kal.yml:68** by [@kannon92](https://github.com/kannon92) — 2025-09-26T17:25:41Z
```diff
@@ -0,0 +1,76 @@
+version: "2"
+run:
+  go: "1.24"
+  allow-parallel-runners: true
+linters:
+  default: none
+  enable:
+    - kubeapilinter # linter for Kube API conventions
+  settings:
+    custom:
+      kubeapilinter:
+        type: module
+        description: KAL is the Kube-API-Linter and lints Kube like APIs based on API conventions and best practices.
+        settings:
+          linters:
+            enable:
+              - "commentstart" # Ensure comments start with the serialized version of the field name.
+              - "conditions" # Ensure conditions have the correct json tags and markers.
+              - "conflictingmarkers"
+              - "duplicatemarkers" # Ensure there are no exact duplicate markers. for types and fields.
+              #- "forbiddenmarkers" # Ensure that types and fields do not contain any markers that are forbidden.
+              - "integers" # Ensure only int32 and int64 are used for integers.
+              - "jsontags" # Ensure every field has a json tag.
+              - "maxlength" # Ensure all strings and arrays have maximum lengths/maximum items.
+              - "nobools" # Bools do not evolve over time, should use enums instead.
+              - "nodurations" # Prevents usage of `Duration` types.
+              - "nofloats" # Ensure floats are not used.
+              - "nomaps" # Ensure maps are not used.
+              - "nonullable" # Ensure that types and fields do not have the nullable marker.
+              - "notimestamp" # Prevents usage of 'Timestamp' fields
+              - "optionalfields" # Ensure that all fields marked as optional adhere to being pointers and
+                                 # having the `omitempty` value in their `json` tag where appropriate.
+              - "optionalorrequired" # Every field should be marked as `+optional` or `+required`.
+              - "requiredfields" # Required fields should not be pointers, and should not have `omitempty`.
+              - "ssatags" # Ensure array fields have the appropriate listType markers
+              - "statusoptional" # Ensure all first children within status should be optional.
+              - "statussubresource" # All root objects that have a `status` field should have a status subresource.
+              - "uniquemarkers" # Ensure that types and fields do not contain more than a single definition of a marker that should only be present once.
+
+            # Per discussion in July 2024, we are keeping phase fields for now.
+            # See https://github.com/kubernetes-sigs/cluster-api/pull/10897#discussion_r1685929508
+            # and https://github.com/kubernetes-sigs/cluster-api/pull/10897#discussion_r1685919394.
+            # - "nophase" # Phase fields are discouraged by the Kube API conventions, use conditions instead.
+
+            # Linters below this line are disabled, pending conversation on how and when to enable them.
+            disable:
+            - "*" # We will manually enable new linters after understanding the impact. Disable all by default.
+          lintersConfig:
+            conflictingmarkers:
+              conflicts:
+              - name: "default_vs_required"
+                sets:
+                  - ["default", "kubebuilder:default"]
+                  - ["required", "kubebuilder:validation:Required", "k8s:required"]
+                description: "A field with a default value cannot be required"
+            conditions:
+              isFirstField: Warn # Require conditions to be the first field in the status struct.
+              usePatchStrategy: Forbid # Forbid patchStrategy markers on the Conditions field.
+              useProtobuf: Forbid # We don't use protobuf, so protobuf tags are not required.
+            optionalfields:
+              pointers:
+                preference: WhenRequired # Always | WhenRequired # Whether to always require pointers, or only when required. Defaults to `Always`.
+                policy: SuggestFix # SuggestFix | Warn # The policy for pointers in optional fields. Defaults to `SuggestFix`.
+              omitempty:
+                policy: SuggestFix # SuggestFix | Warn | Ignore # The policy for omitempty in optional fields. Defaults to `SuggestFix`.
+
+  exclusions:
+    generated: strict
+    rules:
+    ## KAL should only run on API folders.
+    - path-except: "apis/kueue/*"
+      linters:
+        - kubeapilinter
```

  We don't run the linter on the kueue api.
  
  I can apply this to config and visibility also but I didn't see a reason there. They are not CRDs.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-09-26T20:06:34Z

- **Makefile:245** by [@kannon92](https://github.com/kannon92) — 2025-09-26T20:06:33Z
```diff
@@ -229,13 +229,21 @@ vet: ## Run go vet against code.
 	$(GO_CMD) vet ./...
 
 .PHONY: ci-lint
-ci-lint: golangci-lint
+ci-lint: golangci-lint golangci-lint-kal
 	find . -path ./site -prune -false -o -name go.mod -exec dirname {} \; | xargs -I {} sh -c 'cd "{}" && $(GOLANGCI_LINT) run $(GOLANGCI_LINT_FIX) --timeout 15m0s --config "$(PROJECT_DIR)/.golangci.yaml"'
 
 .PHONY: lint-fix
 lint-fix: GOLANGCI_LINT_FIX=--fix
 lint-fix: ci-lint
 
+.PHONY: lint-api
+lint-api: golangci-lint-kal
+	$(GOLANGCI_LINT_KAL) run -v --config $(PROJECT_DIR)/.golangci-kal.yml
+
+.PHONY: lint-api-fix
+lint-api-fix: golangci-lint-kal
+	$(GOLANGCI_LINT_KAL) run -v --config $(PROJECT_DIR)/.golangci-kal.yml --fix
```

  good idea. fixed.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-09-26T20:06:56Z

- **.golangci-kal.yml:3** by [@kannon92](https://github.com/kannon92) — 2025-09-26T20:06:56Z
```diff
@@ -0,0 +1,76 @@
+version: "2"
+run:
+  go: "1.24"
```

  I didn't need this field.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-09-26T20:07:22Z

- **hack/kal-linter/.custom-gcl.yaml:1** by [@kannon92](https://github.com/kannon92) — 2025-09-26T20:07:22Z
  I moved it to hack/kal-linter.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-09-26T20:07:33Z

- **Makefile:242** by [@kannon92](https://github.com/kannon92) — 2025-09-26T20:07:33Z
```diff
@@ -229,13 +229,18 @@ vet: ## Run go vet against code.
 	$(GO_CMD) vet ./...
 
 .PHONY: ci-lint
-ci-lint: golangci-lint
+ci-lint: golangci-lint golangci-lint-kal
 	find . -path ./site -prune -false -o -name go.mod -exec dirname {} \; | xargs -I {} sh -c 'cd "{}" && $(GOLANGCI_LINT) run $(GOLANGCI_LINT_FIX) --timeout 15m0s --config "$(PROJECT_DIR)/.golangci.yaml"'
+	$(GOLANGCI_LINT_KAL) run -v --config $(PROJECT_DIR)/.golangci-kal.yml $(GOLANGCI_LINT_EXTRA_ARGS)
 
 .PHONY: lint-fix
 lint-fix: GOLANGCI_LINT_FIX=--fix
 lint-fix: ci-lint
 
+.PHONY: lint-api
+lint-api: golangci-lint-kal
+	$(GOLANGCI_LINT_KAL) run -v --config $(PROJECT_DIR)/.golangci-kal.yml $(GOLANGCI_LINT_EXTRA_ARGS)
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-09-26T20:07:41Z

- **Makefile:234** by [@kannon92](https://github.com/kannon92) — 2025-09-26T20:07:41Z
```diff
@@ -229,13 +229,18 @@ vet: ## Run go vet against code.
 	$(GO_CMD) vet ./...
 
 .PHONY: ci-lint
-ci-lint: golangci-lint
+ci-lint: golangci-lint golangci-lint-kal
 	find . -path ./site -prune -false -o -name go.mod -exec dirname {} \; | xargs -I {} sh -c 'cd "{}" && $(GOLANGCI_LINT) run $(GOLANGCI_LINT_FIX) --timeout 15m0s --config "$(PROJECT_DIR)/.golangci.yaml"'
+	$(GOLANGCI_LINT_KAL) run -v --config $(PROJECT_DIR)/.golangci-kal.yml $(GOLANGCI_LINT_EXTRA_ARGS)
```

  removed.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-27T05:47:36Z

- **apis/kueue/v1beta1/clusterqueue_types.go:328** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-27T05:47:36Z
```diff
@@ -322,22 +322,22 @@ type ClusterQueueStatus struct {
 }
 
 type ClusterQueuePendingWorkloadsStatus struct {
-	// Head contains the list of top pending workloads.
+	// clusterQueuePendingWorkload contains the list of top pending workloads.
 	// +listType=atomic
 	// +optional
 	Head []ClusterQueuePendingWorkload `json:"clusterQueuePendingWorkload"`
```

  ```suggestion
  	ClusterQueuePendingWorkloads []ClusterQueuePendingWorkload `json:"clusterQueuePendingWorkloads"`
  ```
  Interesting why do we use `Head` here? Also I think it should be pluralized to ClusterQueuePendingWorkloads.
  
  cc: @mimowo @tenzen-y 
  
  This is just comment not related to this PR.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-27T05:52:48Z

- **.golangci-kal.yml:68** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-27T05:52:48Z
```diff
@@ -0,0 +1,76 @@
+version: "2"
+run:
+  go: "1.24"
+  allow-parallel-runners: true
+linters:
+  default: none
+  enable:
+    - kubeapilinter # linter for Kube API conventions
+  settings:
+    custom:
+      kubeapilinter:
+        type: module
+        description: KAL is the Kube-API-Linter and lints Kube like APIs based on API conventions and best practices.
+        settings:
+          linters:
+            enable:
+              - "commentstart" # Ensure comments start with the serialized version of the field name.
+              - "conditions" # Ensure conditions have the correct json tags and markers.
+              - "conflictingmarkers"
+              - "duplicatemarkers" # Ensure there are no exact duplicate markers. for types and fields.
+              #- "forbiddenmarkers" # Ensure that types and fields do not contain any markers that are forbidden.
+              - "integers" # Ensure only int32 and int64 are used for integers.
+              - "jsontags" # Ensure every field has a json tag.
+              - "maxlength" # Ensure all strings and arrays have maximum lengths/maximum items.
+              - "nobools" # Bools do not evolve over time, should use enums instead.
+              - "nodurations" # Prevents usage of `Duration` types.
+              - "nofloats" # Ensure floats are not used.
+              - "nomaps" # Ensure maps are not used.
+              - "nonullable" # Ensure that types and fields do not have the nullable marker.
+              - "notimestamp" # Prevents usage of 'Timestamp' fields
+              - "optionalfields" # Ensure that all fields marked as optional adhere to being pointers and
+                                 # having the `omitempty` value in their `json` tag where appropriate.
+              - "optionalorrequired" # Every field should be marked as `+optional` or `+required`.
+              - "requiredfields" # Required fields should not be pointers, and should not have `omitempty`.
+              - "ssatags" # Ensure array fields have the appropriate listType markers
+              - "statusoptional" # Ensure all first children within status should be optional.
+              - "statussubresource" # All root objects that have a `status` field should have a status subresource.
+              - "uniquemarkers" # Ensure that types and fields do not contain more than a single definition of a marker that should only be present once.
+
+            # Per discussion in July 2024, we are keeping phase fields for now.
+            # See https://github.com/kubernetes-sigs/cluster-api/pull/10897#discussion_r1685929508
+            # and https://github.com/kubernetes-sigs/cluster-api/pull/10897#discussion_r1685919394.
+            # - "nophase" # Phase fields are discouraged by the Kube API conventions, use conditions instead.
+
+            # Linters below this line are disabled, pending conversation on how and when to enable them.
+            disable:
+            - "*" # We will manually enable new linters after understanding the impact. Disable all by default.
+          lintersConfig:
+            conflictingmarkers:
+              conflicts:
+              - name: "default_vs_required"
+                sets:
+                  - ["default", "kubebuilder:default"]
+                  - ["required", "kubebuilder:validation:Required", "k8s:required"]
+                description: "A field with a default value cannot be required"
+            conditions:
+              isFirstField: Warn # Require conditions to be the first field in the status struct.
+              usePatchStrategy: Forbid # Forbid patchStrategy markers on the Conditions field.
+              useProtobuf: Forbid # We don't use protobuf, so protobuf tags are not required.
+            optionalfields:
+              pointers:
+                preference: WhenRequired # Always | WhenRequired # Whether to always require pointers, or only when required. Defaults to `Always`.
+                policy: SuggestFix # SuggestFix | Warn # The policy for pointers in optional fields. Defaults to `SuggestFix`.
+              omitempty:
+                policy: SuggestFix # SuggestFix | Warn | Ignore # The policy for omitempty in optional fields. Defaults to `SuggestFix`.
+
+  exclusions:
+    generated: strict
+    rules:
+    ## KAL should only run on API folders.
+    - path-except: "apis/kueue/*"
+      linters:
+        - kubeapilinter
```

  Make sense.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-27T05:53:48Z

- **Makefile:232** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-27T05:53:48Z
```diff
@@ -229,13 +229,21 @@ vet: ## Run go vet against code.
 	$(GO_CMD) vet ./...
 
 .PHONY: ci-lint
-ci-lint: golangci-lint
+ci-lint: golangci-lint golangci-lint-kal
```

  ```suggestion
  ci-lint: golangci-lint
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-27T05:55:16Z

- **hack/kal-linter/.custom-gcl.yaml:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-27T05:55:16Z
  Thank you!

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-09-27T12:02:54Z

- **apis/kueue/v1beta1/clusterqueue_types.go:328** by [@kannon92](https://github.com/kannon92) — 2025-09-27T12:02:54Z
```diff
@@ -322,22 +322,22 @@ type ClusterQueueStatus struct {
 }
 
 type ClusterQueuePendingWorkloadsStatus struct {
-	// Head contains the list of top pending workloads.
+	// clusterQueuePendingWorkload contains the list of top pending workloads.
 	// +listType=atomic
 	// +optional
 	Head []ClusterQueuePendingWorkload `json:"clusterQueuePendingWorkload"`
```

  I can’t make this change as it’s breaking v1beta1.
  
  maybe this is a good change for v1beta2?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-29T07:17:16Z

- **apis/kueue/v1beta1/clusterqueue_types.go:328** by [@mimowo](https://github.com/mimowo) — 2025-09-29T07:17:15Z
```diff
@@ -322,22 +322,22 @@ type ClusterQueueStatus struct {
 }
 
 type ClusterQueuePendingWorkloadsStatus struct {
-	// Head contains the list of top pending workloads.
+	// clusterQueuePendingWorkload contains the list of top pending workloads.
 	// +listType=atomic
 	// +optional
 	Head []ClusterQueuePendingWorkload `json:"clusterQueuePendingWorkload"`
```

  Yes, it would be breaking. Also:
  - we are dropping this API in v1beta2, it is not longer functional
  - "head" is arguably better than `ClusterQueuePendingWorkloads`, "head" coveys it is just a couple of Top Workloads, not all. Maybe it could have been named: TopWorkloads, but we are dropping this feature anyway.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-29T07:21:28Z

/lgtm
/approve
/hold
in case @tenzen-y wants to comment

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-29T07:32:00Z

- **Makefile:232** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-29T07:31:59Z
```diff
@@ -229,13 +229,21 @@ vet: ## Run go vet against code.
 	$(GO_CMD) vet ./...
 
 .PHONY: ci-lint
-ci-lint: golangci-lint
+ci-lint: golangci-lint golangci-lint-kal
```

  /hold
  
  @kannon92 Could you please revert this changes?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-09-29T16:10:24Z

- **Makefile:232** by [@kannon92](https://github.com/kannon92) — 2025-09-29T16:10:24Z
```diff
@@ -229,13 +229,21 @@ vet: ## Run go vet against code.
 	$(GO_CMD) vet ./...
 
 .PHONY: ci-lint
-ci-lint: golangci-lint
+ci-lint: golangci-lint golangci-lint-kal
```

  done!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-29T16:11:11Z

- **Makefile:232** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-29T16:11:11Z
```diff
@@ -229,13 +229,21 @@ vet: ## Run go vet against code.
 	$(GO_CMD) vet ./...
 
 .PHONY: ci-lint
-ci-lint: golangci-lint
+ci-lint: golangci-lint golangci-lint-kal
```

  Thank you!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-30T07:53:13Z

- **apis/kueue/v1beta1/clusterqueue_types.go:328** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-30T07:46:45Z
```diff
@@ -322,22 +322,22 @@ type ClusterQueueStatus struct {
 }
 
 type ClusterQueuePendingWorkloadsStatus struct {
-	// Head contains the list of top pending workloads.
+	// clusterQueuePendingWorkload contains the list of top pending workloads.
 	// +listType=atomic
 	// +optional
 	Head []ClusterQueuePendingWorkload `json:"clusterQueuePendingWorkload"`
```

  This feature is no longer effective. So, we can just ignore those APIs.

- **apis/kueue/v1beta1/clusterqueue_types.go:461** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-30T07:48:17Z
```diff
@@ -456,6 +456,9 @@ type ClusterQueuePreemption struct {
 	// +kubebuilder:validation:Enum=Never;LowerPriority;Any
 	ReclaimWithinCohort PreemptionPolicy `json:"reclaimWithinCohort,omitempty"`
 
+	// borrowWithinCohort determines whether a pending Workload can preempt
+	// Workloads from other ClusterQueues in the cohort if the workload requires borrowing.
+	// May only be configured with Classical Preemption, and __not__ with Fair Sharing.
```

  IIRC, previously, @gabesaba removed this API comment to make API comments DRY as much as possible.
  If we add this API comments, it would be better to remove comments from `BorrowWithinCohort` typed.

- **hack/kal-linter/.custom-gcl.yaml:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-30T07:52:14Z
```diff
@@ -0,0 +1,6 @@
+version: v2.4.0
```

  how to automatically update this version by dependabot?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-09-30T18:49:29Z

- **hack/kal-linter/.custom-gcl.yaml:1** by [@kannon92](https://github.com/kannon92) — 2025-09-30T18:49:29Z
```diff
@@ -0,0 +1,6 @@
+version: v2.4.0
```

  I'm not sure.
  
  @JoelSpeed @everettraven any ideas on how projects can keep this up to date?
  
  I don't see that kal uses tags so not sure if dependabot would work here.

### Review by [@everettraven](https://github.com/everettraven) (commented) — 2025-09-30T19:08:43Z

- **hack/kal-linter/.custom-gcl.yaml:1** by [@everettraven](https://github.com/everettraven) — 2025-09-30T19:08:42Z
```diff
@@ -0,0 +1,6 @@
+version: v2.4.0
```

  At the moment, I'm not sure the best way to automatically keep this up to date. It is on our roadmap to get things to a good point for an initial tagged release soon, at which point shouldn't be difficult for dependabot to handle.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-10-02T14:04:38Z

- **hack/kal-linter/.custom-gcl.yaml:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-02T14:04:38Z
```diff
@@ -0,0 +1,6 @@
+version: v2.4.0
```

  Uhm, I see. I think here we have 2 perspectives, (1 is version management for KAL, (2 is version mangement for golangci-line in .custom-gcl.yaml.
  
  Let's see in the dedicate issue how we can manage those.

### Review by [@JoelSpeed](https://github.com/JoelSpeed) (commented) — 2025-10-03T09:31:04Z

- **hack/kal-linter/.custom-gcl.yaml:1** by [@JoelSpeed](https://github.com/JoelSpeed) — 2025-10-03T09:31:04Z
```diff
@@ -0,0 +1,6 @@
+version: v2.4.0
```

  If you were to use the [plugin approach](https://github.com/kubernetes-sigs/kube-api-linter?tab=readme-ov-file#golangci-lint-plugin) then the versions are based just on what's in your go.mod, which solves the version bumping for golangci-lint.
  
  I'm expecting to publish the first release of KAL within the next month, so we will start having releases going forward

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-10-03T14:05:31Z

- **hack/kal-linter/.custom-gcl.yaml:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-03T14:05:31Z
```diff
@@ -0,0 +1,6 @@
+version: v2.4.0
```

  Thank you for sharing that.
  We will track the KAL release and version management way in a dedicated issue.
