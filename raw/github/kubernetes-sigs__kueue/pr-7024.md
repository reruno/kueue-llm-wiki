# PR #7024: [Hierarchical Cohorts] Prefer Flavors with Local Quota

**Summary**: [Hierarchical Cohorts] Prefer Flavors with Local Quota

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7024

**Last updated**: 2025-09-30T13:17:34Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@gabesaba](https://github.com/gabesaba)
- **Branch**: `local` → `main`
- **Created**: 2025-09-26T14:51:02Z
- **Updated**: 2025-09-30T13:17:34Z
- **Merged**: 2025-09-29T06:26:16Z
- **Closed**: 2025-09-29T06:26:16Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@pajakd](https://github.com/pajakd)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 3   **+250 / -14**

## Description

#### What type of PR is this?
/kind bug

#### What this PR does / why we need it:
Flavor selection only took into account whether the workload fits within its ClusterQueue's quota, while we have information about whether it fits within any of its parent Cohorts' quotas. Using this information, we can prefer flavors where nominal quota is available, reducing likelihood of preemption from outside Cohort.

#### Which issue(s) this PR fixes:
Fixes #7016

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
```release-note
HC: When multiple borrowing flavors are available, prefer the flavor which
results in borrowing more locally (closer to the ClusterQueue, further from the root Cohort).

This fixes the scenario where a flavor would be selected which required borrowing
from the root Cohort in one flavor, while in a second flavor, quota was
available from the nearest parent Cohort.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-09-26T14:51:09Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | db7511149395261290e5fed55fa4a9ca6fc1b472 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68d6b4576ac0d400090fe304 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-26T14:51:09Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7024#" title="Author self-approved">gabesaba</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [gabesaba]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-09-26T14:51:40Z

Can you take a pass please, @varunsyal?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-26T16:00:47Z

Thank you @gabesaba , looks great to me, but I will leave the time to review by @varunsyal.

From me, could you please update the release note to make it a bit more clear what is the meaning of "Flavor Selection bug " in this context, so that users can better relate to their clusters.

### Comment by [@varunsyal](https://github.com/varunsyal) — 2025-09-26T16:51:32Z

Thanks @gabesaba 
This looks good to me. Does it also handle the case: when the cohort that has unused nominal quota in one flavor, and none in the other - would it prefer the first flavor even if it needs to preempt in the first flavor but not in the second?
(especially when the feature gate "FlavorFungibilityImplicitPreferenceDefault" is on)

### Comment by [@pajakd](https://github.com/pajakd) — 2025-09-29T05:57:42Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-29T05:57:49Z

LGTM label has been added.  <details>Git tree hash: ed18fe19001b848aa60cffec36ffdddd588030b7</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-29T07:32:30Z

Proposal, let me know @gabesaba if it captures the issue and the fix properly. My goal is to make the release notes relatable to users' environments as much as possible. I think emphasizing HierarchicalCohorts makes sense as this is a prerequisite for the issue (IIUC).
/release-note-edit
```release-note
HierarchicalCohorts: when multiple borrowing flavors are available, then prefer selecting the flavor which
needs to borrow on the shorter distance in the tree. 

This fixes the scenario where the aggregated nominal quota at the cohort level could be exceeded
for the selected flavor, while not necessary if another flavor was selected.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-29T07:35:06Z

/cherrypick release-0.13
We should cherrypick at least to 0.13. 0.12 is not necessary as HierarchicalCohorts were alpha back then. I'm ok to also go to 0.12 if requested.

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-09-29T07:35:42Z

@mimowo: #7024 failed to apply on top of branch "release-0.13":
```
Applying: Prefer Flavors with Local Quota
Using index info to reconstruct a base tree...
M	pkg/scheduler/flavorassigner/flavorassigner.go
M	pkg/scheduler/scheduler_test.go
M	test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go
Falling back to patching base and 3-way merge...
Auto-merging test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go
Auto-merging pkg/scheduler/scheduler_test.go
Auto-merging pkg/scheduler/flavorassigner/flavorassigner.go
CONFLICT (content): Merge conflict in pkg/scheduler/flavorassigner/flavorassigner.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 Prefer Flavors with Local Quota

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7024#issuecomment-3345457347):

>/cherrypick release-0.13
>We should cherrypick at least to 0.13. 0.12 is not necessary as HierarchicalCohorts were alpha back then. I'm ok to also go to 0.12 if requested.


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-09-29T09:29:35Z

> Proposal, let me know @gabesaba if it captures the issue and the fix properly.

Thanks @mimowo. I modified it slightly

/release-note-edit

```
[Hierarchical Cohorts] When multiple borrowing flavors are available, prefer the flavor which
results in borrowing more locally (closer to the ClusterQueue, further from the root Cohort).

This fixes the scenario where a flavor would be selected which required borrowing
from the root Cohort in one flavor, while in a second flavor, quota was
available from the nearest parent Cohort.
```

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-29T09:29:38Z

@gabesaba: /release-note-edit must be used with a release note block.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7024#issuecomment-3345938955):

>> Proposal, let me know @gabesaba if it captures the issue and the fix properly.
>
>Thanks @mimowo. I modified it slightly
>
>/release-note-edit
>
>```
>[Hierarchical Cohorts] When multiple borrowing flavors are available, prefer the flavor which
>results in borrowing more locally (closer to the ClusterQueue, further from the root Cohort).
>
>This fixes the scenario where a flavor would be selected which required borrowing
>from the root Cohort in one flavor, while in a second flavor, quota was
>available from the nearest parent Cohort.
>```


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-09-29T09:30:08Z

/release-note-edit

```release-note
[Hierarchical Cohorts] When multiple borrowing flavors are available, prefer the flavor which
results in borrowing more locally (closer to the ClusterQueue, further from the root Cohort).

This fixes the scenario where a flavor would be selected which required borrowing
from the root Cohort in one flavor, while in a second flavor, quota was
available from the nearest parent Cohort.
```

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-09-29T10:18:52Z

> Thanks @gabesaba This looks good to me. Does it also handle the case: when the cohort that has unused nominal quota in one flavor, and none in the other - would it prefer the first flavor even if it needs to preempt in the first flavor but not in the second? (especially when the feature gate "FlavorFungibilityImplicitPreferenceDefault" is on)

Yes. I added some new test cases to be sure: #7041

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-30T10:40:17Z

Just making the style consistent
/release-note-edit
```release-note
Hierarchical Cohorts: When multiple borrowing flavors are available, prefer the flavor which
results in borrowing more locally (closer to the ClusterQueue, further from the root Cohort).

This fixes the scenario where a flavor would be selected which required borrowing
from the root Cohort in one flavor, while in a second flavor, quota was
available from the nearest parent Cohort.
```

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-30T13:16:52Z

/release-note-edit

```release-note
HC: When multiple borrowing flavors are available, prefer the flavor which
results in borrowing more locally (closer to the ClusterQueue, further from the root Cohort).

This fixes the scenario where a flavor would be selected which required borrowing
from the root Cohort in one flavor, while in a second flavor, quota was
available from the nearest parent Cohort.
```

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-30T13:17:17Z

We used to use the `HC` prefix for the Hierarchical Cohort release note.

## Reviews

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-09-29T05:48:10Z

- **pkg/scheduler/scheduler_test.go:3793** by [@pajakd](https://github.com/pajakd) — 2025-09-29T05:48:09Z
```diff
@@ -3772,6 +3772,80 @@ func TestSchedule(t *testing.T) {
 				utiltesting.MakeEventRecord("sales", "foo-2", "Admitted", corev1.EventTypeNormal).Obj(),
 			},
 		},
+		"prefer flavor with most local capacity": {
+			enableFairSharing: true,
+			cohorts: []kueue.Cohort{
+				*utiltesting.MakeCohort("root").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("on-demand").Resource("gpu", "2").Obj(),
+						*utiltesting.MakeFlavorQuotas("spot").Resource("gpu", "0").Obj(),
+					).
+					Obj(),
+				*utiltesting.MakeCohort("cohort-a").
+					Parent("root").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("on-demand").Resource("gpu", "5").Obj(),
+						*utiltesting.MakeFlavorQuotas("spot").Resource("gpu", "7").Obj(),
+					).
+					Obj(),
+			},
+			additionalClusterQueues: []kueue.ClusterQueue{
+				*utiltesting.MakeClusterQueue("other-alpha").
```

  nit: CQ name `other-alpha` suggests that there is more than one queue that has some role in this test

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-09-29T05:57:29Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:615** by [@pajakd](https://github.com/pajakd) — 2025-09-29T05:57:29Z
```diff
@@ -482,6 +490,132 @@ var _ = ginkgo.Describe("Scheduler", ginkgo.Ordered, ginkgo.ContinueOnFailure, f
 		})
 	})
 
+	ginkgo.When("using hierarchical cohorts with several flavors", func() {
+		var (
+			cqp1 *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			createCohort(testing.MakeCohort("root-cohort").Obj())
+			createCohort(testing.MakeCohort("cohort-a").
+				Parent("root-cohort").
+				FairWeight(resource.MustParse("1")).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("flavor1").Resource(corev1.ResourceCPU, "0", "10").Obj(),
+					*testing.MakeFlavorQuotas("flavor2").Resource(corev1.ResourceCPU, "0", "10").Obj(),
+				).
+				Obj())
+			createCohort(testing.MakeCohort("cohort-b").
+				Parent("root-cohort").
+				FairWeight(resource.MustParse("1")).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("flavor1").Resource(corev1.ResourceCPU, "0", "10").Obj(),
+					*testing.MakeFlavorQuotas("flavor2").Resource(corev1.ResourceCPU, "0", "10").Obj(),
+				).
+				Obj())
+
+			fungibility := kueue.FlavorFungibility{
+				WhenCanBorrow:  kueue.TryNextFlavor,
+				WhenCanPreempt: kueue.TryNextFlavor,
+			}
+			preemption := kueue.ClusterQueuePreemption{
+				ReclaimWithinCohort: kueue.PreemptionPolicyAny,
+				WithinClusterQueue:  kueue.PreemptionPolicyLowerPriority,
+			}
+
+			cqp1 = createQueue(testing.MakeClusterQueue("cq-p1").
+				Cohort("cohort-a").
+				FairWeight(resource.MustParse("1")).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("flavor1").Resource(corev1.ResourceCPU, "3", "10").Obj(),
+					*testing.MakeFlavorQuotas("flavor2").Resource(corev1.ResourceCPU, "3", "10").Obj(),
+				).
+				FlavorFungibility(fungibility).
+				Preemption(preemption).
+				Obj())
+
+			createQueue(testing.MakeClusterQueue("cq-p2").
+				Cohort("cohort-a").
+				FairWeight(resource.MustParse("1")).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("flavor1").Resource(corev1.ResourceCPU, "3", "10").Obj(),
+					*testing.MakeFlavorQuotas("flavor2").Resource(corev1.ResourceCPU, "3", "10").Obj(),
+				).
+				FlavorFungibility(fungibility).
+				Preemption(preemption).
+				Obj())
+
+			createQueue(testing.MakeClusterQueue("cq-p3").
+				Cohort("cohort-a").
+				FairWeight(resource.MustParse("1")).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("flavor1").Resource(corev1.ResourceCPU, "2", "10").Obj(),
+					*testing.MakeFlavorQuotas("flavor2").Resource(corev1.ResourceCPU, "4", "10").Obj(),
+				).
+				FlavorFungibility(fungibility).
+				Preemption(preemption).
+				Obj())
+
+			createQueue(testing.MakeClusterQueue("cq-p4").
+				Cohort("cohort-b").
+				FairWeight(resource.MustParse("1")).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("flavor1").Resource(corev1.ResourceCPU, "2", "10").Obj(),
+					*testing.MakeFlavorQuotas("flavor2").Resource(corev1.ResourceCPU, "0", "10").Obj(),
+				).
+				FlavorFungibility(fungibility).
+				Preemption(preemption).
+				Obj())
+
+			createQueue(testing.MakeClusterQueue("cq-p5").
+				Cohort("cohort-b").
+				FairWeight(resource.MustParse("1")).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("flavor1").Resource(corev1.ResourceCPU, "0", "10").Obj(),
+					*testing.MakeFlavorQuotas("flavor2").Resource(corev1.ResourceCPU, "0", "10").Obj(),
+				).
+				FlavorFungibility(fungibility).
+				Preemption(preemption).
+				Obj())
+		})
+		ginkgo.AfterEach(func() {
+			_ = features.SetEnable(features.FlavorFungibilityImplicitPreferenceDefault, false)
+		})
+
+		// Since CohortA has 18CPU quota, we expect that
+		// all 18 CPUs scheduled are within this guarantee.
+		// On the otherhand, cq-p1 only has 6 quota, and
+		// has to borrow 12. There are 20 cpus are available from
+		// perspective of CohortA, and root, which serve as denominators.
+		//
+		// WeightedShare(CohortA) = 0/20 * 1000 = 0
+		// WeightedShare(cq-p1)  = 12/20 * 1000 = 600
+		ginkgo.It("Prefers flavor with remaining guarantees at Cohort level (FlavorFungibilityImplicitPreferenceDefault=false)", func() {
+			_ = features.SetEnable(features.FlavorFungibilityImplicitPreferenceDefault, false)
+			ginkgo.By("Creating workloads")
+			for range 18 {
+				createWorkload("cq-p1", "1")
+			}
+			ginkgo.By("Workloads active")
+			util.ExpectReservingActiveWorkloadsMetric(cqp1, 18)
+
+			ginkgo.By("Expected Weighted Shares")
+			util.ExpectClusterQueueWeightedShareMetric(cqp1, 600)
+			expectCohortWeightedShare("cohort-a", 0)
+		})
+		ginkgo.It("Prefers flavor with remaining guarantees at Cohort level (FlavorFungibilityImplicitPreferenceDefault=true)", func() {
+			_ = features.SetEnable(features.FlavorFungibilityImplicitPreferenceDefault, true)
+			for range 18 {
+				createWorkload("cq-p1", "1")
+			}
+			ginkgo.By("Workloads active")
+			util.ExpectReservingActiveWorkloadsMetric(cqp1, 18)
+
+			ginkgo.By("Expected Weighted Shares")
+			util.ExpectClusterQueueWeightedShareMetric(cqp1, 600)
+			expectCohortWeightedShare("cohort-a", 0)
```

  We don't check which flavors are selected for the workloads but only that `cohort-a` is not borrowing (and how much `cq-p1` is borrowing), right?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-09-29T09:31:12Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:615** by [@gabesaba](https://github.com/gabesaba) — 2025-09-29T09:31:12Z
```diff
@@ -482,6 +490,132 @@ var _ = ginkgo.Describe("Scheduler", ginkgo.Ordered, ginkgo.ContinueOnFailure, f
 		})
 	})
 
+	ginkgo.When("using hierarchical cohorts with several flavors", func() {
+		var (
+			cqp1 *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			createCohort(testing.MakeCohort("root-cohort").Obj())
+			createCohort(testing.MakeCohort("cohort-a").
+				Parent("root-cohort").
+				FairWeight(resource.MustParse("1")).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("flavor1").Resource(corev1.ResourceCPU, "0", "10").Obj(),
+					*testing.MakeFlavorQuotas("flavor2").Resource(corev1.ResourceCPU, "0", "10").Obj(),
+				).
+				Obj())
+			createCohort(testing.MakeCohort("cohort-b").
+				Parent("root-cohort").
+				FairWeight(resource.MustParse("1")).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("flavor1").Resource(corev1.ResourceCPU, "0", "10").Obj(),
+					*testing.MakeFlavorQuotas("flavor2").Resource(corev1.ResourceCPU, "0", "10").Obj(),
+				).
+				Obj())
+
+			fungibility := kueue.FlavorFungibility{
+				WhenCanBorrow:  kueue.TryNextFlavor,
+				WhenCanPreempt: kueue.TryNextFlavor,
+			}
+			preemption := kueue.ClusterQueuePreemption{
+				ReclaimWithinCohort: kueue.PreemptionPolicyAny,
+				WithinClusterQueue:  kueue.PreemptionPolicyLowerPriority,
+			}
+
+			cqp1 = createQueue(testing.MakeClusterQueue("cq-p1").
+				Cohort("cohort-a").
+				FairWeight(resource.MustParse("1")).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("flavor1").Resource(corev1.ResourceCPU, "3", "10").Obj(),
+					*testing.MakeFlavorQuotas("flavor2").Resource(corev1.ResourceCPU, "3", "10").Obj(),
+				).
+				FlavorFungibility(fungibility).
+				Preemption(preemption).
+				Obj())
+
+			createQueue(testing.MakeClusterQueue("cq-p2").
+				Cohort("cohort-a").
+				FairWeight(resource.MustParse("1")).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("flavor1").Resource(corev1.ResourceCPU, "3", "10").Obj(),
+					*testing.MakeFlavorQuotas("flavor2").Resource(corev1.ResourceCPU, "3", "10").Obj(),
+				).
+				FlavorFungibility(fungibility).
+				Preemption(preemption).
+				Obj())
+
+			createQueue(testing.MakeClusterQueue("cq-p3").
+				Cohort("cohort-a").
+				FairWeight(resource.MustParse("1")).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("flavor1").Resource(corev1.ResourceCPU, "2", "10").Obj(),
+					*testing.MakeFlavorQuotas("flavor2").Resource(corev1.ResourceCPU, "4", "10").Obj(),
+				).
+				FlavorFungibility(fungibility).
+				Preemption(preemption).
+				Obj())
+
+			createQueue(testing.MakeClusterQueue("cq-p4").
+				Cohort("cohort-b").
+				FairWeight(resource.MustParse("1")).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("flavor1").Resource(corev1.ResourceCPU, "2", "10").Obj(),
+					*testing.MakeFlavorQuotas("flavor2").Resource(corev1.ResourceCPU, "0", "10").Obj(),
+				).
+				FlavorFungibility(fungibility).
+				Preemption(preemption).
+				Obj())
+
+			createQueue(testing.MakeClusterQueue("cq-p5").
+				Cohort("cohort-b").
+				FairWeight(resource.MustParse("1")).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("flavor1").Resource(corev1.ResourceCPU, "0", "10").Obj(),
+					*testing.MakeFlavorQuotas("flavor2").Resource(corev1.ResourceCPU, "0", "10").Obj(),
+				).
+				FlavorFungibility(fungibility).
+				Preemption(preemption).
+				Obj())
+		})
+		ginkgo.AfterEach(func() {
+			_ = features.SetEnable(features.FlavorFungibilityImplicitPreferenceDefault, false)
+		})
+
+		// Since CohortA has 18CPU quota, we expect that
+		// all 18 CPUs scheduled are within this guarantee.
+		// On the otherhand, cq-p1 only has 6 quota, and
+		// has to borrow 12. There are 20 cpus are available from
+		// perspective of CohortA, and root, which serve as denominators.
+		//
+		// WeightedShare(CohortA) = 0/20 * 1000 = 0
+		// WeightedShare(cq-p1)  = 12/20 * 1000 = 600
+		ginkgo.It("Prefers flavor with remaining guarantees at Cohort level (FlavorFungibilityImplicitPreferenceDefault=false)", func() {
+			_ = features.SetEnable(features.FlavorFungibilityImplicitPreferenceDefault, false)
+			ginkgo.By("Creating workloads")
+			for range 18 {
+				createWorkload("cq-p1", "1")
+			}
+			ginkgo.By("Workloads active")
+			util.ExpectReservingActiveWorkloadsMetric(cqp1, 18)
+
+			ginkgo.By("Expected Weighted Shares")
+			util.ExpectClusterQueueWeightedShareMetric(cqp1, 600)
+			expectCohortWeightedShare("cohort-a", 0)
+		})
+		ginkgo.It("Prefers flavor with remaining guarantees at Cohort level (FlavorFungibilityImplicitPreferenceDefault=true)", func() {
+			_ = features.SetEnable(features.FlavorFungibilityImplicitPreferenceDefault, true)
+			for range 18 {
+				createWorkload("cq-p1", "1")
+			}
+			ginkgo.By("Workloads active")
+			util.ExpectReservingActiveWorkloadsMetric(cqp1, 18)
+
+			ginkgo.By("Expected Weighted Shares")
+			util.ExpectClusterQueueWeightedShareMetric(cqp1, 600)
+			expectCohortWeightedShare("cohort-a", 0)
```

  Correct. I don't think that we have any integration test assertions about flavor selection/usage, so the weighted share was a nice proxy

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-09-29T09:39:44Z

- **pkg/scheduler/scheduler_test.go:3793** by [@gabesaba](https://github.com/gabesaba) — 2025-09-29T09:39:44Z
```diff
@@ -3772,6 +3772,80 @@ func TestSchedule(t *testing.T) {
 				utiltesting.MakeEventRecord("sales", "foo-2", "Admitted", corev1.EventTypeNormal).Obj(),
 			},
 		},
+		"prefer flavor with most local capacity": {
+			enableFairSharing: true,
+			cohorts: []kueue.Cohort{
+				*utiltesting.MakeCohort("root").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("on-demand").Resource("gpu", "2").Obj(),
+						*utiltesting.MakeFlavorQuotas("spot").Resource("gpu", "0").Obj(),
+					).
+					Obj(),
+				*utiltesting.MakeCohort("cohort-a").
+					Parent("root").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("on-demand").Resource("gpu", "5").Obj(),
+						*utiltesting.MakeFlavorQuotas("spot").Resource("gpu", "7").Obj(),
+					).
+					Obj(),
+			},
+			additionalClusterQueues: []kueue.ClusterQueue{
+				*utiltesting.MakeClusterQueue("other-alpha").
```

  https://github.com/kubernetes-sigs/kueue/pull/7040
