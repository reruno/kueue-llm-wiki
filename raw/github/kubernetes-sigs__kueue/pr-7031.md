# PR #7031: Add ctx and indexer to the NewReconciler interface

**Summary**: Add ctx and indexer to the NewReconciler interface

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7031

**Last updated**: 2025-09-29T09:04:19Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kaisoz](https://github.com/kaisoz)
- **Branch**: `6568-update-newreconciler` → `main`
- **Created**: 2025-09-28T21:55:00Z
- **Updated**: 2025-09-29T09:04:19Z
- **Merged**: 2025-09-29T09:04:17Z
- **Closed**: 2025-09-29T09:04:17Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 40   **+274 / -114**

## Description

#### What type of PR is this?

/kind cleanup

#### What this PR does / why we need it:
This PR adds `context.Context` and `client.FieldIndexer` arguments to the `NewReconciler` interface. This is needed by the Trainjob integration to be able to instantiate the `TrainingRuntimes` of the trainer

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #6865

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-09-28T21:55:08Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 53164cccc5e1778d0132046ae48f14e75be744e2 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68da3cfdf6f46600082a9771 |

### Comment by [@kaisoz](https://github.com/kaisoz) — 2025-09-28T22:04:28Z

/assign @mimowo

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-29T08:04:30Z

LGTM label has been added.  <details>Git tree hash: 8aaf2986aef24336651c1c8d8d1060c1657a93cd</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-29T08:04:33Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7031#" title="Author self-approved">kaisoz</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7031#pullrequestreview-3278419589" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-29T07:00:10Z

- **pkg/controller/jobframework/integrationmanager.go:54** by [@mimowo](https://github.com/mimowo) — 2025-09-29T07:00:10Z
```diff
@@ -51,7 +51,7 @@ type JobReconcilerInterface interface {
 	SetupWithManager(mgr ctrl.Manager) error
 }
 
-type ReconcilerFactory func(client client.Client, record record.EventRecorder, opts ...Option) JobReconcilerInterface
+type ReconcilerFactory func(ctx context.Context, client client.Client, indexer client.FieldIndexer, record record.EventRecorder, opts ...Option) (JobReconcilerInterface, error)
```

  I was thinking if we could pass the ctx and inderex via Options to avoid interface change, but we anyway need to change the interface as we return the error. So, I'm ok with that.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-29T07:03:57Z

- **pkg/controller/jobframework/setup.go:113** by [@mimowo](https://github.com/mimowo) — 2025-09-29T07:03:57Z
```diff
@@ -109,20 +109,28 @@ func (m *integrationManager) setupControllers(ctx context.Context, mgr ctrl.Mana
 	})
 }
 
-func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
-	if err := cb.NewReconciler(
+func (m *integrationManager) setupControllerAndWebhook(ctx context.Context, mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	r, _ := cb.NewReconciler(
```

  I think we should use the error to fail fast rather than late. It makes debugging easier.
  
  What about the structure like:
  
  ```golang
  if r, err := vb.NewReconciler(); err != nil {
     return fmt.Errorf("%s: %w", fwkNamePrefix, err)
  } else  if err := r.SetupWithManager(mgr); err != nil {
     return fmt.Errorf("%s: %w", fwkNamePrefix, err)
  }
  ```
  if if does not work, then indeed it is better to declare the error on top.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-29T07:04:45Z

- **test/integration/singlecluster/webhook/jobs/raycluster_webhook_test.go:87** by [@mimowo](https://github.com/mimowo) — 2025-09-29T07:04:46Z
```diff
@@ -82,8 +84,10 @@ var _ = ginkgo.Describe("RayCluster Webhook", func() {
 				err = raycluster.SetupRayClusterWebhook(mgr, opts...)
 				gomega.Expect(err).NotTo(gomega.HaveOccurred())
 
-				reconciler = workloadrayjob.NewReconciler(
+				reconciler, _ = workloadrayjob.NewReconciler(
```

  Please don't ingore errors in tests but fail fast: `gomega.Expect(err).NotTo(gomega.HaveOccurred())`

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-09-29T07:36:45Z

- **pkg/controller/jobframework/setup.go:113** by [@kaisoz](https://github.com/kaisoz) — 2025-09-29T07:36:44Z
```diff
@@ -109,20 +109,28 @@ func (m *integrationManager) setupControllers(ctx context.Context, mgr ctrl.Mana
 	})
 }
 
-func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
-	if err := cb.NewReconciler(
+func (m *integrationManager) setupControllerAndWebhook(ctx context.Context, mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	r, _ := cb.NewReconciler(
```

  it makes sense, but it indeed needs the variables to be declared at the top

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-29T07:40:34Z

- **pkg/controller/jobframework/setup.go:113** by [@mimowo](https://github.com/mimowo) — 2025-09-29T07:40:33Z
```diff
@@ -109,20 +109,28 @@ func (m *integrationManager) setupControllers(ctx context.Context, mgr ctrl.Mana
 	})
 }
 
-func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
-	if err := cb.NewReconciler(
+func (m *integrationManager) setupControllerAndWebhook(ctx context.Context, mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	r, _ := cb.NewReconciler(
```

  ack, so 
  ```
  if r, err := vb.NewReconciler(); err != nil {
     return fmt.Errorf("%s: %w", fwkNamePrefix, err)
  } else  if err := r.SetupWithManager(mgr); err != nil {
     return fmt.Errorf("%s: %w", fwkNamePrefix, err)
  }
  ```
  does not compile?

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-09-29T08:02:24Z

- **pkg/controller/jobframework/setup.go:113** by [@kaisoz](https://github.com/kaisoz) — 2025-09-29T08:02:24Z
```diff
@@ -109,20 +109,28 @@ func (m *integrationManager) setupControllers(ctx context.Context, mgr ctrl.Mana
 	})
 }
 
-func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
-	if err := cb.NewReconciler(
+func (m *integrationManager) setupControllerAndWebhook(ctx context.Context, mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	r, _ := cb.NewReconciler(
```

  sorry I misread the original comment 🤦🏻  yeah, that does compile and works. Pushed the change 👍🏻

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-29T08:03:27Z

- **pkg/controller/jobframework/setup.go:113** by [@mimowo](https://github.com/mimowo) — 2025-09-29T08:03:27Z
```diff
@@ -109,20 +109,28 @@ func (m *integrationManager) setupControllers(ctx context.Context, mgr ctrl.Mana
 	})
 }
 
-func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
-	if err := cb.NewReconciler(
+func (m *integrationManager) setupControllerAndWebhook(ctx context.Context, mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	r, _ := cb.NewReconciler(
```

  Actually, I think the new version with declaring outside is also ok, please keep.

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-09-29T08:03:35Z

- **test/integration/singlecluster/webhook/jobs/raycluster_webhook_test.go:87** by [@kaisoz](https://github.com/kaisoz) — 2025-09-29T08:03:35Z
```diff
@@ -82,8 +84,10 @@ var _ = ginkgo.Describe("RayCluster Webhook", func() {
 				err = raycluster.SetupRayClusterWebhook(mgr, opts...)
 				gomega.Expect(err).NotTo(gomega.HaveOccurred())
 
-				reconciler = workloadrayjob.NewReconciler(
+				reconciler, _ = workloadrayjob.NewReconciler(
```

  Agree. I did ignore the errors for those reconcilers because currently they always return nil, but I think is a good practice to check the error to catch future potential regressions. I updated all the tests, including the unit tests

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-29T08:04:24Z

/lgtm
/approve
Thank you for the effort 👍

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-29T08:09:10Z

- **pkg/controller/jobframework/setup.go:113** by [@mimowo](https://github.com/mimowo) — 2025-09-29T08:09:10Z
```diff
@@ -109,20 +109,28 @@ func (m *integrationManager) setupControllers(ctx context.Context, mgr ctrl.Mana
 	})
 }
 
-func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
-	if err := cb.NewReconciler(
+func (m *integrationManager) setupControllerAndWebhook(ctx context.Context, mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	r, _ := cb.NewReconciler(
```

  Sorry, for the confusion. 
  
  I just wanted to say that I'm happy with both versions, my comment was to explore options.
  
  I'm ok to keep as is.
