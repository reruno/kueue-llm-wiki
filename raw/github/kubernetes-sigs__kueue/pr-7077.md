# PR #7077: Fix SimulateWorkloadRemoval bug

**Summary**: Fix SimulateWorkloadRemoval bug

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7077

**Last updated**: 2025-10-01T12:15:13Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@gabesaba](https://github.com/gabesaba)
- **Branch**: `simulate` → `main`
- **Created**: 2025-09-30T12:25:07Z
- **Updated**: 2025-10-01T12:15:13Z
- **Merged**: 2025-09-30T13:58:20Z
- **Closed**: 2025-09-30T13:58:20Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 5   **+65 / -27**

## Description

#### What type of PR is this?
/kind bug

#### What this PR does / why we need it:
During `SimulateWorkloadRemoval`, we were removing usage from the wrong ClusterQueue. I noticed this while debugging why we were assigning a suboptimal flavor in the context of #7015.

Because of this, in certain paths (notably [PreemptionOracle](https://github.com/kubernetes-sigs/kueue/blob/50b7ed542f9aef732cdbf95eb798f18308a577cb/pkg/scheduler/preemption/preemption_oracle.go#L60-L62)), we were claiming that [borrowingLevel=0](https://github.com/kubernetes-sigs/kueue/blob/50b7ed542f9aef732cdbf95eb798f18308a577cb/pkg/scheduler/flavorassigner/flavorassigner.go#L325-L330) for `Preempt` mode, but borrowingLevel>0 for `Fit` mode (after preemption successfully completed). In combination with `FlavorFungibilityImplicitPreferenceDefault=true`, this resulted in selecting the flavor with (`Preempt`, 0) mode incorrectly (because 0 is not accurate) over the flavor in which preemption already completed (`Fit`, 1).

#### Which issue(s) this PR fixes:
Fixes #7015

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
```release-note
Fix bug in workload usage removal simulation that results in inaccurate flavor assignment
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-09-30T12:25:14Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 34227554c1c3b9bfe6b6e6cb4c64922f1412d077 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68dbd5f51c94dc000870e39f |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-30T12:39:40Z

I think this failure is due to https://github.com/kubernetes-sigs/kueue/issues/7078

/test pull-kueue-test-integration-multikueue-main

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-30T12:43:00Z

> Fix bug in workload usage removal simulation during scheduling

@gabesaba please update the note in the user-observable terms.

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-09-30T12:51:05Z

> > Fix bug in workload usage removal simulation during scheduling
> 
> @gabesaba please update the note in the user-observable terms.

done.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-30T13:11:42Z

LGTM label has been added.  <details>Git tree hash: 2bd88c21a80ce5e051662c124fcb5a2e2f6bd64a</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-30T13:11:42Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7077#" title="Author self-approved">gabesaba</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7077#pullrequestreview-3284706673" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [gabesaba,mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-30T13:59:07Z

/cherrypick release-0.13

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-09-30T13:59:48Z

@mimowo: new pull request created: #7084

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7077#issuecomment-3352322390):

>/cherrypick release-0.13


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-30T14:03:57Z

/cherrypick release-0.12

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-09-30T14:04:35Z

@tenzen-y: #7077 failed to apply on top of branch "release-0.12":
```
Applying: Fix SimulateWorkloadRemoval
Using index info to reconstruct a base tree...
A	pkg/cache/scheduler/clusterqueue_snapshot.go
A	pkg/cache/scheduler/snapshot.go
M	pkg/scheduler/preemption/preemption_oracle.go
M	pkg/scheduler/scheduler.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/scheduler/scheduler.go
CONFLICT (content): Merge conflict in pkg/scheduler/scheduler.go
Auto-merging pkg/scheduler/preemption/preemption_oracle.go
CONFLICT (content): Merge conflict in pkg/scheduler/preemption/preemption_oracle.go
Auto-merging pkg/cache/snapshot.go
Auto-merging pkg/cache/clusterqueue_snapshot.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 Fix SimulateWorkloadRemoval

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7077#issuecomment-3352352017):

>/cherrypick release-0.12


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-30T14:06:07Z

> /cherrypick release-0.12

I think it is fair to skip in 0.12. Seems like the bug is tricky to repro (the scenario Gabe found is using FlavorFungibilityImplicitPreferenceDefault=true). And no user requested so far, this was discovered by Gabe during debugging.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-30T14:13:00Z

Based on discussion w/ @mimowo offline, we decided not to cherry-pick this PR to release-0.12 since it's hard to reproduce the observed bug in the situations w/o FlavorFungibilityImplicitPreferenceDefault FG.

The reproduced environments are only situations w/ FlavorFungibilityImplicitPreferenceDefault.

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-30T12:32:22Z

- **pkg/cache/scheduler/snapshot.go:67** by [@mimowo](https://github.com/mimowo) — 2025-09-30T12:32:22Z
```diff
@@ -52,14 +52,31 @@ func (s *Snapshot) RemoveWorkload(wl *workload.Info) {
 	cq.RemoveUsage(wl.Usage())
 }
 
-// AddWorkload adds a workload from its corresponding ClusterQueue and
+// AddWorkload adds a workload to its corresponding ClusterQueue and
 // updates resource usage.
 func (s *Snapshot) AddWorkload(wl *workload.Info) {
 	cq := s.ClusterQueue(wl.ClusterQueue)
 	cq.Workloads[workload.Key(wl.Obj)] = wl
 	cq.AddUsage(wl.Usage())
 }
 
+// SimulateWorkloadRemoval modifies the snapshot by removing the usage
+// corresponding to the list of workloads from workloads' respective
+// ClusterQueues. It returns a function which can be used to restore
+// this usage.
+func (s *Snapshot) SimulateWorkloadRemoval(workloads []*workload.Info) func() {
```

  Why moving at the same time? Is the move necessary or just a drive by cleanup?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-09-30T12:36:47Z

- **pkg/cache/scheduler/snapshot.go:67** by [@gabesaba](https://github.com/gabesaba) — 2025-09-30T12:36:47Z
```diff
@@ -52,14 +52,31 @@ func (s *Snapshot) RemoveWorkload(wl *workload.Info) {
 	cq.RemoveUsage(wl.Usage())
 }
 
-// AddWorkload adds a workload from its corresponding ClusterQueue and
+// AddWorkload adds a workload to its corresponding ClusterQueue and
 // updates resource usage.
 func (s *Snapshot) AddWorkload(wl *workload.Info) {
 	cq := s.ClusterQueue(wl.ClusterQueue)
 	cq.Workloads[workload.Key(wl.Obj)] = wl
 	cq.AddUsage(wl.Usage())
 }
 
+// SimulateWorkloadRemoval modifies the snapshot by removing the usage
+// corresponding to the list of workloads from workloads' respective
+// ClusterQueues. It returns a function which can be used to restore
+// this usage.
+func (s *Snapshot) SimulateWorkloadRemoval(workloads []*workload.Info) func() {
```

  since this is now a method on the snapshot, not CQ. If you prefer, I can leave there for smaller diff, and move later.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-30T12:52:43Z

- **pkg/cache/scheduler/snapshot.go:67** by [@mimowo](https://github.com/mimowo) — 2025-09-30T12:52:43Z
```diff
@@ -52,14 +52,31 @@ func (s *Snapshot) RemoveWorkload(wl *workload.Info) {
 	cq.RemoveUsage(wl.Usage())
 }
 
-// AddWorkload adds a workload from its corresponding ClusterQueue and
+// AddWorkload adds a workload to its corresponding ClusterQueue and
 // updates resource usage.
 func (s *Snapshot) AddWorkload(wl *workload.Info) {
 	cq := s.ClusterQueue(wl.ClusterQueue)
 	cq.Workloads[workload.Key(wl.Obj)] = wl
 	cq.AddUsage(wl.Usage())
 }
 
+// SimulateWorkloadRemoval modifies the snapshot by removing the usage
+// corresponding to the list of workloads from workloads' respective
+// ClusterQueues. It returns a function which can be used to restore
+// this usage.
+func (s *Snapshot) SimulateWorkloadRemoval(workloads []*workload.Info) func() {
```

  I see, not necessarily

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-30T12:53:30Z

- **pkg/cache/scheduler/snapshot.go:67** by [@mimowo](https://github.com/mimowo) — 2025-09-30T12:53:30Z
```diff
@@ -52,14 +52,31 @@ func (s *Snapshot) RemoveWorkload(wl *workload.Info) {
 	cq.RemoveUsage(wl.Usage())
 }
 
-// AddWorkload adds a workload from its corresponding ClusterQueue and
+// AddWorkload adds a workload to its corresponding ClusterQueue and
 // updates resource usage.
 func (s *Snapshot) AddWorkload(wl *workload.Info) {
 	cq := s.ClusterQueue(wl.ClusterQueue)
 	cq.Workloads[workload.Key(wl.Obj)] = wl
 	cq.AddUsage(wl.Usage())
 }
 
+// SimulateWorkloadRemoval modifies the snapshot by removing the usage
+// corresponding to the list of workloads from workloads' respective
+// ClusterQueues. It returns a function which can be used to restore
+// this usage.
+func (s *Snapshot) SimulateWorkloadRemoval(workloads []*workload.Info) func() {
```

  Actually, maybe better to move to make sure cherrypick is easier

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-30T12:53:41Z

- **pkg/cache/scheduler/snapshot.go:67** by [@mimowo](https://github.com/mimowo) — 2025-09-30T12:53:41Z
```diff
@@ -52,14 +52,31 @@ func (s *Snapshot) RemoveWorkload(wl *workload.Info) {
 	cq.RemoveUsage(wl.Usage())
 }
 
-// AddWorkload adds a workload from its corresponding ClusterQueue and
+// AddWorkload adds a workload to its corresponding ClusterQueue and
 // updates resource usage.
 func (s *Snapshot) AddWorkload(wl *workload.Info) {
 	cq := s.ClusterQueue(wl.ClusterQueue)
 	cq.Workloads[workload.Key(wl.Obj)] = wl
 	cq.AddUsage(wl.Usage())
 }
 
+// SimulateWorkloadRemoval modifies the snapshot by removing the usage
+// corresponding to the list of workloads from workloads' respective
+// ClusterQueues. It returns a function which can be used to restore
+// this usage.
+func (s *Snapshot) SimulateWorkloadRemoval(workloads []*workload.Info) func() {
```

  so that maybe we can include it today

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-30T12:55:02Z

- **pkg/cache/scheduler/snapshot.go:67** by [@mimowo](https://github.com/mimowo) — 2025-09-30T12:55:02Z
```diff
@@ -52,14 +52,31 @@ func (s *Snapshot) RemoveWorkload(wl *workload.Info) {
 	cq.RemoveUsage(wl.Usage())
 }
 
-// AddWorkload adds a workload from its corresponding ClusterQueue and
+// AddWorkload adds a workload to its corresponding ClusterQueue and
 // updates resource usage.
 func (s *Snapshot) AddWorkload(wl *workload.Info) {
 	cq := s.ClusterQueue(wl.ClusterQueue)
 	cq.Workloads[workload.Key(wl.Obj)] = wl
 	cq.AddUsage(wl.Usage())
 }
 
+// SimulateWorkloadRemoval modifies the snapshot by removing the usage
+// corresponding to the list of workloads from workloads' respective
+// ClusterQueues. It returns a function which can be used to restore
+// this usage.
+func (s *Snapshot) SimulateWorkloadRemoval(workloads []*workload.Info) func() {
```

  ah, ok you need entire snapshot to do `cq := s.ClusterQueue(w.ClusterQueue)`. Let's keep it where it is then

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-30T13:02:36Z

- **pkg/cache/scheduler/snapshot.go:73** by [@mimowo](https://github.com/mimowo) — 2025-09-30T13:02:36Z
```diff
@@ -52,14 +52,31 @@ func (s *Snapshot) RemoveWorkload(wl *workload.Info) {
 	cq.RemoveUsage(wl.Usage())
 }
 
-// AddWorkload adds a workload from its corresponding ClusterQueue and
+// AddWorkload adds a workload to its corresponding ClusterQueue and
 // updates resource usage.
 func (s *Snapshot) AddWorkload(wl *workload.Info) {
 	cq := s.ClusterQueue(wl.ClusterQueue)
 	cq.Workloads[workload.Key(wl.Obj)] = wl
 	cq.AddUsage(wl.Usage())
 }
 
+// SimulateWorkloadRemoval modifies the snapshot by removing the usage
+// corresponding to the list of workloads from workloads' respective
+// ClusterQueues. It returns a function which can be used to restore
+// this usage.
+func (s *Snapshot) SimulateWorkloadRemoval(workloads []*workload.Info) func() {
+	for _, w := range workloads {
```

  I think it is safer to revert this code. 
  ```
  	usage := make([]workload.Usage, 0, len(workloads))
  	for _, w := range workloads {
  		usage = append(usage, w.Usage())
  	}
  ```
  I synced with @gabesaba it is better to revert, because otherwise some controller doing eviction could change `w.Usage()` in the meanwhile. as w is a pointer

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-09-30T13:07:56Z

- **pkg/cache/scheduler/snapshot.go:73** by [@gabesaba](https://github.com/gabesaba) — 2025-09-30T13:07:56Z
```diff
@@ -52,14 +52,31 @@ func (s *Snapshot) RemoveWorkload(wl *workload.Info) {
 	cq.RemoveUsage(wl.Usage())
 }
 
-// AddWorkload adds a workload from its corresponding ClusterQueue and
+// AddWorkload adds a workload to its corresponding ClusterQueue and
 // updates resource usage.
 func (s *Snapshot) AddWorkload(wl *workload.Info) {
 	cq := s.ClusterQueue(wl.ClusterQueue)
 	cq.Workloads[workload.Key(wl.Obj)] = wl
 	cq.AddUsage(wl.Usage())
 }
 
+// SimulateWorkloadRemoval modifies the snapshot by removing the usage
+// corresponding to the list of workloads from workloads' respective
+// ClusterQueues. It returns a function which can be used to restore
+// this usage.
+func (s *Snapshot) SimulateWorkloadRemoval(workloads []*workload.Info) func() {
+	for _, w := range workloads {
```

  Done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-30T13:11:34Z

/lgtm 
/approve
/cherrypick release-0.13
/cherrypick release-0.12
