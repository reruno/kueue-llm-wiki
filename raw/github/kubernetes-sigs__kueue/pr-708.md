# PR #708: Allow parallel admissions when using waitForPodsReady

**Summary**: Allow parallel admissions when using waitForPodsReady

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/708

**Last updated**: 2023-05-11T12:31:18Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@KunWuLuan](https://github.com/KunWuLuan)
- **Branch**: `main` → `main`
- **Created**: 2023-04-20T02:22:50Z
- **Updated**: 2023-05-11T12:31:18Z
- **Merged**: 2023-05-11T12:31:18Z
- **Closed**: 2023-05-11T12:31:18Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 10   **+194 / -38**

## Description

#### What type of PR is this?
/kind feature

#### Which issue(s) this PR fixes:
to #536 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?

```release-note
Allow scheduler to admit more jobs when the head job have not reached the PodReady=true status.
```

## Discussion

### Comment by [@linux-foundation-easycla[bot]](https://github.com/apps/linux-foundation-easycla) — 2023-04-20T02:22:53Z

<a href="https://easycla.lfx.linuxfoundation.org/#/?version=2"><img src="https://s3.amazonaws.com/cla-project-logo-prod/cla-signed.svg" alt="CLA Signed" align="left" height="28" width="328" ></a><br/><br />The committers listed above are authorized under a signed CLA.<ul><li>:white_check_mark: login: KunWuLuan / name: GreenHand  (8fcf41a4f7d91709b6c94b3971c08e8c52b2dcd7, 955310a611ac1090deb97da66f85547815b65dc4)</li></ul>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-04-20T02:22:56Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | d895590545ab50b5da5ef2953ab67da5414e1fab |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/645c485e4de5cf00094f6587 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-708--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-708--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTcwOC0ta3ViZXJuZXRlcy1zaWdzLWt1ZXVlLm5ldGxpZnkuYXBwIn0.1_ZzpP7Ek8iB2N_vKJ6ZsMUTSQB7VNuI3A3VdnE35eA)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site settings](https://app.netlify.com/sites/kubernetes-sigs-kueue/settings/deploys#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-04-20T02:22:58Z

Welcome @KunWuLuan! <br><br>It looks like this is your first PR to <a href='https://github.com/kubernetes-sigs/kueue'>kubernetes-sigs/kueue</a> 🎉. Please refer to our [pull request process documentation](https://git.k8s.io/community/contributors/guide/pull-requests.md) to help your PR have a smooth ride to approval. <br><br>You will be prompted by a bot to use commands during the review process. Do not be afraid to follow the prompts! It is okay to experiment. [Here is the bot commands documentation](https://go.k8s.io/bot-commands). <br><br>You can also check if kubernetes-sigs/kueue has [its own contribution guidelines](https://github.com/kubernetes-sigs/kueue/tree/master/CONTRIBUTING.md). <br><br>You may want to refer to our [testing guide](https://git.k8s.io/community/contributors/devel/sig-testing/testing.md) if you run into trouble with your tests not passing. <br><br>If you are having difficulty getting your pull request seen, please follow the [recommended escalation practices](https://github.com/kubernetes/community/blob/master/contributors/guide/pull-requests.md#why-is-my-pull-request-not-getting-reviewed). Also, for tips and tricks in the contribution process you may want to read the [Kubernetes contributor cheat sheet](https://git.k8s.io/community/contributors/guide/contributor-cheatsheet/README.md). We want to make sure your contribution gets all the attention it needs! <br><br>Thank you, and welcome to Kubernetes. :smiley:

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-04-20T02:22:59Z

Hi @KunWuLuan. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-27T18:09:20Z

/assign @mimowo

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-27T18:15:00Z

I gave a quick pass. I'll do another review once @mimowo gives an LGTM

### Comment by [@mimowo](https://github.com/mimowo) — 2023-04-28T08:43:46Z

> I gave a quick pass. I'll do another review once @mimowo gives an LGTM

Thanks for heads up, will review today.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-03T19:26:51Z

Hi @KunWuLuan, could you handle the review comments above?

### Comment by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-05-04T07:15:22Z

@alculquicondor Of course yes! I am on vocation last few days and I will return to work tomorrow.

### Comment by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-05-04T07:15:52Z

@alculquicondor Of course yes! I am on vocation last few days and I will return to work tomorrow.

### Comment by [@mimowo](https://github.com/mimowo) — 2023-05-10T11:09:19Z

/lgtm

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-10T17:21:56Z

/retitle Allow parallel admissions when using waitForPodsReady

/kind feature

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-10T17:22:36Z

Could you add a release note?

### Comment by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-05-11T01:48:49Z

> Could you add a release note?

Done

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-11T12:30:26Z

/lgtm
/approve

Thanks a lot for your contribution!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-05-11T12:30:34Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/708#issuecomment-1543910183" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/708#" title="Author self-approved">KunWuLuan</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-04-20T12:09:06Z

Thank you for creating this PR!
/ok-to-test

- **apis/config/v1beta1/configuration_types.go:80** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-20T11:53:46Z
```diff
@@ -74,6 +74,10 @@ type WaitForPodsReady struct {
 	// is cancelled and requeued in the same cluster queue. Defaults to 5min.
 	// +optional
 	Timeout *metav1.Duration `json:"timeout,omitempty"`
+
+	// BlockAdmission when true, cluster queue will block admissions for all subsequent jobs
+	// until the jobs reach the PodsReady=true condition. It defaults to false.
+	BlockAdmission bool `json:"blockAdmission,omitempty" default:"false"`
```

  I'm not sure if setting the default value is possible as a marker.

- **pkg/scheduler/scheduler.go:76** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-20T11:55:59Z
```diff
@@ -62,23 +62,23 @@ type Scheduler struct {
 	recorder                record.EventRecorder
 	admissionRoutineWrapper routine.Wrapper
 	preemptor               *preemption.Preemptor
-	waitForPodsReady        bool
+	blockForPodsReady       bool
 	// Stubs.
 	applyAdmission func(context.Context, *kueue.Workload) error
 }
 
 type options struct {
-	waitForPodsReady bool
+	blockForPodsReady bool
 }
 
 // Option configures the reconciler.
 type Option func(*options)
 
 // WithWaitForPodsReady indicates if the controller should wait for the
 // PodsReady condition for all admitted workloads before admitting a new one.
```

  please update.

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-04-26T01:45:18Z

- **pkg/scheduler/scheduler.go:76** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-04-26T01:45:18Z
```diff
@@ -62,23 +62,23 @@ type Scheduler struct {
 	recorder                record.EventRecorder
 	admissionRoutineWrapper routine.Wrapper
 	preemptor               *preemption.Preemptor
-	waitForPodsReady        bool
+	blockForPodsReady       bool
 	// Stubs.
 	applyAdmission func(context.Context, *kueue.Workload) error
 }
 
 type options struct {
-	waitForPodsReady bool
+	blockForPodsReady bool
 }
 
 // Option configures the reconciler.
 type Option func(*options)
 
 // WithWaitForPodsReady indicates if the controller should wait for the
 // PodsReady condition for all admitted workloads before admitting a new one.
```

  Ok, I missed it, thanks. 👍

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-04-26T15:41:59Z

Uhm, it seems only that this PR adds a knob to make it configurable whether the users allow the kueue-scheduler can block a head of a workload. Hence, this PR doesn't seem to change the behavior of the kueue-scheduler.

So, I'm not sure whether it is worth merging this.

cc: @alculquicondor

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-27T18:14:39Z

- **apis/config/v1beta1/configuration_types.go:80** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-27T18:13:23Z
```diff
@@ -74,6 +74,10 @@ type WaitForPodsReady struct {
 	// is cancelled and requeued in the same cluster queue. Defaults to 5min.
 	// +optional
 	Timeout *metav1.Duration `json:"timeout,omitempty"`
+
+	// BlockAdmission when true, cluster queue will block admissions for all subsequent jobs
+	// until the jobs reach the PodsReady=true condition. It defaults to false.
+	BlockAdmission bool `json:"blockAdmission,omitempty"`
```

  The documentation of this field is contradicting the comment for `enable`.
  
  I think we can say that enable is about tracking the PodsReady condition, but not about blocking.
  
  Then this field is about blocking, but it should be true by default, because that's the existing behavior.
  To implement true as default, you either need to make it a pointer `*bool` and check for nil.
  
  Or you can change the name of the field to something like `admissionMode` with values `Sequential` or `Parallel`

- **test/integration/scheduler/podsready/suite_test.go:59** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-27T18:14:37Z
```diff
@@ -59,8 +59,9 @@ func TestSchedulerWithWaitForPodsReady(t *testing.T) {
 func managerAndSchedulerSetupWithTimeout(mgr manager.Manager, ctx context.Context, value time.Duration) {
```

  Missing integration test for the new behavior

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-04-28T08:37:44Z

- **test/integration/scheduler/podsready/suite_test.go:59** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-04-28T08:37:44Z
```diff
@@ -59,8 +59,9 @@ func TestSchedulerWithWaitForPodsReady(t *testing.T) {
 func managerAndSchedulerSetupWithTimeout(mgr manager.Manager, ctx context.Context, value time.Duration) {
```

  Thanks, I will add test later

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-04-28T10:25:55Z

Generally LGTM. Some nit comments left.

- **apis/config/v1beta1/configuration_types.go:80** by [@mimowo](https://github.com/mimowo) — 2023-04-28T09:33:15Z
```diff
@@ -74,6 +74,10 @@ type WaitForPodsReady struct {
 	// is cancelled and requeued in the same cluster queue. Defaults to 5min.
 	// +optional
 	Timeout *metav1.Duration `json:"timeout,omitempty"`
+
+	// BlockAdmission when true, cluster queue will block admissions for all subsequent jobs
+	// until the jobs reach the PodsReady=true condition. It defaults to false.
+	BlockAdmission bool `json:"blockAdmission,omitempty"`
```

  +1. I think that while the Kueue API is still Beta we could change the semantics of `Enable`, if people find it more useful to do not block admission, but we need to keep the comments consistent. For this PR I would suggest to use blockAdmission as *bool and set the default as `true`. Separately we can have a discussion on which mode should be default and adjust if needed.

- **test/integration/scheduler/podsready/suite_test.go:59** by [@mimowo](https://github.com/mimowo) — 2023-04-28T09:35:39Z
```diff
@@ -59,8 +59,9 @@ func TestSchedulerWithWaitForPodsReady(t *testing.T) {
 func managerAndSchedulerSetupWithTimeout(mgr manager.Manager, ctx context.Context, value time.Duration) {
```

  +1 for test

- **main.go:258** by [@mimowo](https://github.com/mimowo) — 2023-04-28T10:18:33Z
```diff
@@ -244,14 +244,18 @@ func setupScheduler(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manager
 		cCache,
 		mgr.GetClient(),
 		mgr.GetEventRecorderFor(constants.AdmissionName),
-		scheduler.WithWaitForPodsReady(waitForPodsReady(cfg)),
+		scheduler.WithBlockForPodsReady(blockForPodsReady(cfg)),
 	)
 	if err := mgr.Add(sched); err != nil {
 		setupLog.Error(err, "Unable to add scheduler to manager")
 		os.Exit(1)
 	}
 }
 
+func blockForPodsReady(cfg *config.Configuration) bool {
```

  nit: IIRC we could use this function when creating the cache (the `WithPodsReadyTracking` option), as the cache is used only for blocking, not for enforcing timeout.

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-05-06T01:13:55Z

- **apis/config/v1beta1/configuration_types.go:80** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-05-06T01:13:54Z
```diff
@@ -74,6 +74,10 @@ type WaitForPodsReady struct {
 	// is cancelled and requeued in the same cluster queue. Defaults to 5min.
 	// +optional
 	Timeout *metav1.Duration `json:"timeout,omitempty"`
+
+	// BlockAdmission when true, cluster queue will block admissions for all subsequent jobs
+	// until the jobs reach the PodsReady=true condition. It defaults to false.
+	BlockAdmission bool `json:"blockAdmission,omitempty"`
```

  Sure, I will use blockAdmission as *bool and set the default as true.

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-05-06T11:58:43Z

- **main.go:258** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-05-06T11:58:43Z
```diff
@@ -244,14 +244,18 @@ func setupScheduler(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manager
 		cCache,
 		mgr.GetClient(),
 		mgr.GetEventRecorderFor(constants.AdmissionName),
-		scheduler.WithWaitForPodsReady(waitForPodsReady(cfg)),
+		scheduler.WithBlockForPodsReady(blockForPodsReady(cfg)),
 	)
 	if err := mgr.Add(sched); err != nil {
 		setupLog.Error(err, "Unable to add scheduler to manager")
 		os.Exit(1)
 	}
 }
 
+func blockForPodsReady(cfg *config.Configuration) bool {
```

  Done~ 😄

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-06T19:55:41Z

- **apis/config/v1beta1/defaults.go:99** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-06T19:17:38Z
```diff
@@ -87,8 +87,14 @@ func SetDefaults_Configuration(cfg *Configuration) {
 	if cfg.ClientConnection.Burst == nil {
 		cfg.ClientConnection.Burst = pointer.Int32(DefaultClientConnectionBurst)
 	}
-	if cfg.WaitForPodsReady != nil && cfg.WaitForPodsReady.Timeout == nil {
-		cfg.WaitForPodsReady.Timeout = &metav1.Duration{Duration: defaultPodsReadyTimeout}
+	if cfg.WaitForPodsReady != nil {
+		if cfg.WaitForPodsReady.Timeout == nil {
+			cfg.WaitForPodsReady.Timeout = &metav1.Duration{Duration: defaultPodsReadyTimeout}
+		}
+		if cfg.WaitForPodsReady.BlockAdmission == nil {
+			defaultBlockAdmission := true
+			cfg.WaitForPodsReady.BlockAdmission = &defaultBlockAdmission
```

  In this implementation, wouldn't we enforce a `cfg.WaitForPodsReady.BlockAdmission=true` even if a `cfg.WaitForPodsReady.Enable` is false?

- **apis/config/v1beta1/defaults_test.go:63** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-06T19:27:43Z
```diff
@@ -59,6 +59,8 @@ func TestSetDefaults_Configuration(t *testing.T) {
 	podsReadyTimeoutTimeout := metav1.Duration{Duration: defaultPodsReadyTimeout}
 	podsReadyTimeoutOverwrite := metav1.Duration{Duration: time.Minute}
 
+	expectBlockAdmission := true
+
```

  ```suggestion
  ```

- **apis/config/v1beta1/defaults_test.go:286** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-06T19:28:01Z
```diff
@@ -280,8 +282,9 @@ func TestSetDefaults_Configuration(t *testing.T) {
 			},
 			want: &Configuration{
 				WaitForPodsReady: &WaitForPodsReady{
-					Enable:  true,
-					Timeout: &podsReadyTimeoutTimeout,
+					Enable:         true,
+					BlockAdmission: &expectBlockAdmission,
```

  ```suggestion
  					BlockAdmission: pointer.Bool(true),
  ```

- **apis/config/v1beta1/defaults_test.go:311** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-06T19:28:25Z
```diff
@@ -304,8 +307,9 @@ func TestSetDefaults_Configuration(t *testing.T) {
 			},
 			want: &Configuration{
 				WaitForPodsReady: &WaitForPodsReady{
-					Enable:  true,
-					Timeout: &podsReadyTimeoutOverwrite,
+					Enable:         true,
+					BlockAdmission: &expectBlockAdmission,
```

  ```suggestion
  					BlockAdmission: pointer.Bool(true),
  ```

- **main_test.go:225** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-06T19:28:46Z
```diff
@@ -221,6 +221,8 @@ integrations:
 		Frameworks: []string{job.FrameworkName},
 	}
 
+	defaultBlockAdmission := true
+
```

  ```suggestion
  ```

- **main_test.go:361** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-06T19:29:02Z
```diff
@@ -355,8 +357,9 @@ integrations:
 				ManageJobsWithoutQueueName: false,
 				InternalCertManagement:     enableDefaultInternalCertManagement,
 				WaitForPodsReady: &config.WaitForPodsReady{
-					Enable:  true,
-					Timeout: &metav1.Duration{Duration: 5 * time.Minute},
+					Enable:         true,
+					BlockAdmission: &defaultBlockAdmission,
```

  ```suggestion
  					BlockAdmission: pointer.Bool(true),
  ```

- **pkg/scheduler/scheduler.go:170** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-06T19:40:40Z
```diff
@@ -174,18 +163,16 @@ func (s *Scheduler) schedule(ctx context.Context) {
 			}
 			continue
 		}
-		if s.waitForPodsReady {
-			if !s.cache.PodsReadyForAllAdmittedWorkloads(ctx) {
-				log.V(5).Info("Waiting for all admitted workloads to be in the PodsReady condition")
-				// Block admission until all currently admitted workloads are in
-				// PodsReady condition if the waitForPodsReady is enabled
-				workload.UnsetAdmissionWithCondition(e.Obj, "Waiting", "waiting for all admitted workloads to be in PodsReady condition")
-				if err := workload.ApplyAdmissionStatus(ctx, s.client, e.Obj, true); err != nil {
-					log.Error(err, "Could not update Workload status")
-				}
-				s.cache.WaitForPodsReady(ctx)
-				log.V(5).Info("Finished waiting for all admitted workloads to be in the PodsReady condition")
+		if !s.cache.PodsReadyForAllAdmittedWorkloads(ctx) {
+			log.V(5).Info("Waiting for all admitted workloads to be in the PodsReady condition")
+			// Block admission until all currently admitted workloads are in
+			// PodsReady condition if the waitForPodsReady is enabled
```

  Updating this comment would be good.

- **pkg/scheduler/scheduler.go:71** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-06T19:53:58Z
```diff
@@ -60,26 +60,16 @@ type Scheduler struct {
 	recorder                record.EventRecorder
 	admissionRoutineWrapper routine.Wrapper
 	preemptor               *preemption.Preemptor
-	waitForPodsReady        bool
 	// Stubs.
 	applyAdmission func(context.Context, *kueue.Workload) error
 }
 
 type options struct {
-	waitForPodsReady bool
 }
 
 // Option configures the reconciler.
 type Option func(*options)
```

  Can we remove `options`? This does not seem to be used anywhere.

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-05-08T01:30:14Z

- **apis/config/v1beta1/defaults.go:99** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-05-08T01:30:14Z
```diff
@@ -87,8 +87,14 @@ func SetDefaults_Configuration(cfg *Configuration) {
 	if cfg.ClientConnection.Burst == nil {
 		cfg.ClientConnection.Burst = pointer.Int32(DefaultClientConnectionBurst)
 	}
-	if cfg.WaitForPodsReady != nil && cfg.WaitForPodsReady.Timeout == nil {
-		cfg.WaitForPodsReady.Timeout = &metav1.Duration{Duration: defaultPodsReadyTimeout}
+	if cfg.WaitForPodsReady != nil {
+		if cfg.WaitForPodsReady.Timeout == nil {
+			cfg.WaitForPodsReady.Timeout = &metav1.Duration{Duration: defaultPodsReadyTimeout}
+		}
+		if cfg.WaitForPodsReady.BlockAdmission == nil {
+			defaultBlockAdmission := true
+			cfg.WaitForPodsReady.BlockAdmission = &defaultBlockAdmission
```

  yes, but`cfg.WaitForPodsReady.BlockAdmission` won't work if `cfg.WaitForPodsReady.Enable` is false. 
  I can add some checking to set it false when `cfg.WaitForPodsReady.Enable` is false if needed

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-08T05:28:45Z

- **apis/config/v1beta1/defaults.go:99** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-08T05:28:44Z
```diff
@@ -87,8 +87,14 @@ func SetDefaults_Configuration(cfg *Configuration) {
 	if cfg.ClientConnection.Burst == nil {
 		cfg.ClientConnection.Burst = pointer.Int32(DefaultClientConnectionBurst)
 	}
-	if cfg.WaitForPodsReady != nil && cfg.WaitForPodsReady.Timeout == nil {
-		cfg.WaitForPodsReady.Timeout = &metav1.Duration{Duration: defaultPodsReadyTimeout}
+	if cfg.WaitForPodsReady != nil {
+		if cfg.WaitForPodsReady.Timeout == nil {
+			cfg.WaitForPodsReady.Timeout = &metav1.Duration{Duration: defaultPodsReadyTimeout}
+		}
+		if cfg.WaitForPodsReady.BlockAdmission == nil {
+			defaultBlockAdmission := true
+			cfg.WaitForPodsReady.BlockAdmission = &defaultBlockAdmission
```

  IIRC, a config after defaulting is shown in the kueue-controller log. 
  So, I think that users would confuse a bit with a `cfg.WaitForPodsReady.BlockAdmission=true` and a `cfg.WaitForPodsReady.Enable=false`.
  
  Hence, if a `cfg.WaitForPodsReady.Enable` is false, not defaulting a `cfg.WaitForPodsReady.BlockAdmission` to true would be good.
  
  cc: @mimowo

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-05-08T08:20:31Z

some more nits.

- **test/integration/scheduler/podsready/scheduler_test.go:428** by [@mimowo](https://github.com/mimowo) — 2023-05-08T08:04:05Z
```diff
@@ -355,3 +355,97 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
+
+	var (
+		defaultFlavor    *kueue.ResourceFlavor
+		podsReadyTimeout time.Duration
+		ns               *corev1.Namespace
+		prodClusterQ     *kueue.ClusterQueue
+		devClusterQ      *kueue.ClusterQueue
+		prodQueue        *kueue.LocalQueue
+		devQueue         *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: func(mgr manager.Manager, ctx context.Context) {
+				managerAndSchedulerSetupWithTimeoutAdmission(mgr, ctx, podsReadyTimeout, false)
+			},
+			CRDPath:     filepath.Join("..", "..", "..", "..", "config", "components", "crd", "bases"),
+			WebhookPath: filepath.Join("..", "..", "..", "..", "config", "components", "webhook"),
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+
+		defaultFlavor = utiltesting.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "podsready-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		prodClusterQ = testing.MakeClusterQueue("prod-cq").
+			Cohort("all").
+			ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, prodClusterQ)).Should(gomega.Succeed())
+
+		devClusterQ = testing.MakeClusterQueue("dev-cq").
+			Cohort("all").
+			ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, devClusterQ)).Should(gomega.Succeed())
+
+		prodQueue = testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, prodQueue)).Should(gomega.Succeed())
+
+		devQueue = testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, devQueue)).Should(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, prodClusterQ, true)
+		util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, devClusterQ, true)
+		fwk.Teardown()
+	})
+
+	ginkgo.Context("Long PodsReady timeout", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = time.Minute
+		})
+
+		ginkgo.It("Should not block admission of one new workload if two are considered in the same scheduling cycle", func() {
+			ginkgo.By("creating two workloads but delaying cluster queue creation which has enough capacity")
+			prodWl := testing.MakeWorkload("prod-wl", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "11").Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodWl)).Should(gomega.Succeed())
+			// wait a second to ensure the CreationTimestamps differ and scheduler picks the first created to be admitted
+			time.Sleep(time.Second)
```

  It does not seem needed here.

- **test/integration/scheduler/podsready/scheduler_test.go:386** by [@mimowo](https://github.com/mimowo) — 2023-05-08T08:11:31Z
```diff
@@ -355,3 +355,97 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
+
+	var (
+		defaultFlavor    *kueue.ResourceFlavor
+		podsReadyTimeout time.Duration
+		ns               *corev1.Namespace
+		prodClusterQ     *kueue.ClusterQueue
+		devClusterQ      *kueue.ClusterQueue
+		prodQueue        *kueue.LocalQueue
+		devQueue         *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: func(mgr manager.Manager, ctx context.Context) {
+				managerAndSchedulerSetupWithTimeoutAdmission(mgr, ctx, podsReadyTimeout, false)
+			},
+			CRDPath:     filepath.Join("..", "..", "..", "..", "config", "components", "crd", "bases"),
+			WebhookPath: filepath.Join("..", "..", "..", "..", "config", "components", "webhook"),
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+
+		defaultFlavor = utiltesting.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "podsready-",
```

  ```suggestion
  				GenerateName: "podsready-nonblocking-",
  ```

- **test/integration/scheduler/podsready/scheduler_test.go:417** by [@mimowo](https://github.com/mimowo) — 2023-05-08T08:14:45Z
```diff
@@ -355,3 +355,97 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
+
+	var (
+		defaultFlavor    *kueue.ResourceFlavor
+		podsReadyTimeout time.Duration
+		ns               *corev1.Namespace
+		prodClusterQ     *kueue.ClusterQueue
+		devClusterQ      *kueue.ClusterQueue
+		prodQueue        *kueue.LocalQueue
+		devQueue         *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: func(mgr manager.Manager, ctx context.Context) {
+				managerAndSchedulerSetupWithTimeoutAdmission(mgr, ctx, podsReadyTimeout, false)
+			},
+			CRDPath:     filepath.Join("..", "..", "..", "..", "config", "components", "crd", "bases"),
+			WebhookPath: filepath.Join("..", "..", "..", "..", "config", "components", "webhook"),
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+
+		defaultFlavor = utiltesting.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "podsready-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		prodClusterQ = testing.MakeClusterQueue("prod-cq").
+			Cohort("all").
+			ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, prodClusterQ)).Should(gomega.Succeed())
+
+		devClusterQ = testing.MakeClusterQueue("dev-cq").
+			Cohort("all").
+			ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, devClusterQ)).Should(gomega.Succeed())
+
+		prodQueue = testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, prodQueue)).Should(gomega.Succeed())
+
+		devQueue = testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, devQueue)).Should(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, prodClusterQ, true)
+		util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, devClusterQ, true)
+		fwk.Teardown()
+	})
+
+	ginkgo.Context("Long PodsReady timeout", func() {
```

  I think it makes sense to also cover the use case scenario for blockAdmission=false (which is to enforce the PodsReady timeout).

- **apis/config/v1beta1/configuration_types.go:79** by [@mimowo](https://github.com/mimowo) — 2023-05-08T08:15:15Z
```diff
@@ -74,6 +74,10 @@ type WaitForPodsReady struct {
 	// is cancelled and requeued in the same cluster queue. Defaults to 5min.
 	// +optional
 	Timeout *metav1.Duration `json:"timeout,omitempty"`
+
+	// BlockAdmission when true, cluster queue will block admissions for all subsequent jobs
+	// until the jobs reach the PodsReady=true condition. It defaults to false.
```

  ```suggestion
  	// until the jobs reach the PodsReady=true condition. It defaults to true.
  ```

- **apis/config/v1beta1/defaults.go:99** by [@mimowo](https://github.com/mimowo) — 2023-05-08T08:18:51Z
```diff
@@ -87,8 +87,14 @@ func SetDefaults_Configuration(cfg *Configuration) {
 	if cfg.ClientConnection.Burst == nil {
 		cfg.ClientConnection.Burst = pointer.Int32(DefaultClientConnectionBurst)
 	}
-	if cfg.WaitForPodsReady != nil && cfg.WaitForPodsReady.Timeout == nil {
-		cfg.WaitForPodsReady.Timeout = &metav1.Duration{Duration: defaultPodsReadyTimeout}
+	if cfg.WaitForPodsReady != nil {
+		if cfg.WaitForPodsReady.Timeout == nil {
+			cfg.WaitForPodsReady.Timeout = &metav1.Duration{Duration: defaultPodsReadyTimeout}
+		}
+		if cfg.WaitForPodsReady.BlockAdmission == nil {
+			defaultBlockAdmission := true
+			cfg.WaitForPodsReady.BlockAdmission = &defaultBlockAdmission
```

  Not defaulting when `.Enable=false` sounds good to me. But it is also ok for me to leave defaulting as true if the feature is disabled, to keep it simple (as we default timeout unconditionally).

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-05-08T11:28:20Z

- **pkg/scheduler/scheduler.go:71** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-05-08T11:28:11Z
```diff
@@ -60,26 +60,16 @@ type Scheduler struct {
 	recorder                record.EventRecorder
 	admissionRoutineWrapper routine.Wrapper
 	preemptor               *preemption.Preemptor
-	waitForPodsReady        bool
 	// Stubs.
 	applyAdmission func(context.Context, *kueue.Workload) error
 }
 
 type options struct {
-	waitForPodsReady bool
 }
 
 // Option configures the reconciler.
 type Option func(*options)
```

  Right, I will remove it

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-05-08T11:50:30Z

- **test/integration/scheduler/podsready/scheduler_test.go:428** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-05-08T11:50:30Z
```diff
@@ -355,3 +355,97 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
+
+	var (
+		defaultFlavor    *kueue.ResourceFlavor
+		podsReadyTimeout time.Duration
+		ns               *corev1.Namespace
+		prodClusterQ     *kueue.ClusterQueue
+		devClusterQ      *kueue.ClusterQueue
+		prodQueue        *kueue.LocalQueue
+		devQueue         *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: func(mgr manager.Manager, ctx context.Context) {
+				managerAndSchedulerSetupWithTimeoutAdmission(mgr, ctx, podsReadyTimeout, false)
+			},
+			CRDPath:     filepath.Join("..", "..", "..", "..", "config", "components", "crd", "bases"),
+			WebhookPath: filepath.Join("..", "..", "..", "..", "config", "components", "webhook"),
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+
+		defaultFlavor = utiltesting.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "podsready-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		prodClusterQ = testing.MakeClusterQueue("prod-cq").
+			Cohort("all").
+			ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, prodClusterQ)).Should(gomega.Succeed())
+
+		devClusterQ = testing.MakeClusterQueue("dev-cq").
+			Cohort("all").
+			ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, devClusterQ)).Should(gomega.Succeed())
+
+		prodQueue = testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, prodQueue)).Should(gomega.Succeed())
+
+		devQueue = testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, devQueue)).Should(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, prodClusterQ, true)
+		util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, devClusterQ, true)
+		fwk.Teardown()
+	})
+
+	ginkgo.Context("Long PodsReady timeout", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = time.Minute
+		})
+
+		ginkgo.It("Should not block admission of one new workload if two are considered in the same scheduling cycle", func() {
+			ginkgo.By("creating two workloads but delaying cluster queue creation which has enough capacity")
+			prodWl := testing.MakeWorkload("prod-wl", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "11").Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodWl)).Should(gomega.Succeed())
+			// wait a second to ensure the CreationTimestamps differ and scheduler picks the first created to be admitted
+			time.Sleep(time.Second)
```

  Testing block admission needs 2 tasks, we need a not ready task to ensure the second task not been blocked

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-05-08T11:51:11Z

- **test/integration/scheduler/podsready/scheduler_test.go:417** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-05-08T11:51:11Z
```diff
@@ -355,3 +355,97 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
+
+	var (
+		defaultFlavor    *kueue.ResourceFlavor
+		podsReadyTimeout time.Duration
+		ns               *corev1.Namespace
+		prodClusterQ     *kueue.ClusterQueue
+		devClusterQ      *kueue.ClusterQueue
+		prodQueue        *kueue.LocalQueue
+		devQueue         *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: func(mgr manager.Manager, ctx context.Context) {
+				managerAndSchedulerSetupWithTimeoutAdmission(mgr, ctx, podsReadyTimeout, false)
+			},
+			CRDPath:     filepath.Join("..", "..", "..", "..", "config", "components", "crd", "bases"),
+			WebhookPath: filepath.Join("..", "..", "..", "..", "config", "components", "webhook"),
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+
+		defaultFlavor = utiltesting.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "podsready-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		prodClusterQ = testing.MakeClusterQueue("prod-cq").
+			Cohort("all").
+			ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, prodClusterQ)).Should(gomega.Succeed())
+
+		devClusterQ = testing.MakeClusterQueue("dev-cq").
+			Cohort("all").
+			ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, devClusterQ)).Should(gomega.Succeed())
+
+		prodQueue = testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, prodQueue)).Should(gomega.Succeed())
+
+		devQueue = testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, devQueue)).Should(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, prodClusterQ, true)
+		util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, devClusterQ, true)
+		fwk.Teardown()
+	})
+
+	ginkgo.Context("Long PodsReady timeout", func() {
```

  I think the use case scenario for blockAdmission=false is tested is case `SchedulerWithWaitForPodsReady`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-05-08T12:02:15Z

- **test/integration/scheduler/podsready/scheduler_test.go:428** by [@mimowo](https://github.com/mimowo) — 2023-05-08T12:02:15Z
```diff
@@ -355,3 +355,97 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
+
+	var (
+		defaultFlavor    *kueue.ResourceFlavor
+		podsReadyTimeout time.Duration
+		ns               *corev1.Namespace
+		prodClusterQ     *kueue.ClusterQueue
+		devClusterQ      *kueue.ClusterQueue
+		prodQueue        *kueue.LocalQueue
+		devQueue         *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: func(mgr manager.Manager, ctx context.Context) {
+				managerAndSchedulerSetupWithTimeoutAdmission(mgr, ctx, podsReadyTimeout, false)
+			},
+			CRDPath:     filepath.Join("..", "..", "..", "..", "config", "components", "crd", "bases"),
+			WebhookPath: filepath.Join("..", "..", "..", "..", "config", "components", "webhook"),
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+
+		defaultFlavor = utiltesting.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "podsready-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		prodClusterQ = testing.MakeClusterQueue("prod-cq").
+			Cohort("all").
+			ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, prodClusterQ)).Should(gomega.Succeed())
+
+		devClusterQ = testing.MakeClusterQueue("dev-cq").
+			Cohort("all").
+			ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, devClusterQ)).Should(gomega.Succeed())
+
+		prodQueue = testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, prodQueue)).Should(gomega.Succeed())
+
+		devQueue = testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, devQueue)).Should(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, prodClusterQ, true)
+		util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, devClusterQ, true)
+		fwk.Teardown()
+	})
+
+	ginkgo.Context("Long PodsReady timeout", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = time.Minute
+		})
+
+		ginkgo.It("Should not block admission of one new workload if two are considered in the same scheduling cycle", func() {
+			ginkgo.By("creating two workloads but delaying cluster queue creation which has enough capacity")
+			prodWl := testing.MakeWorkload("prod-wl", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "11").Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodWl)).Should(gomega.Succeed())
+			// wait a second to ensure the CreationTimestamps differ and scheduler picks the first created to be admitted
+			time.Sleep(time.Second)
```

  yes, creating two tasks (or jobs) is fine, but I think we don't need to sleep to ensure they have different creation timestamps. The sleep was added in the original test to have control over which Job is admitted, so that the asserts are deterministic. Here you get both of them admitted, so the asserts don't make use of which one is admitted first.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-05-08T12:15:41Z

- **test/integration/scheduler/podsready/scheduler_test.go:417** by [@mimowo](https://github.com/mimowo) — 2023-05-08T12:15:41Z
```diff
@@ -355,3 +355,97 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
+
+	var (
+		defaultFlavor    *kueue.ResourceFlavor
+		podsReadyTimeout time.Duration
+		ns               *corev1.Namespace
+		prodClusterQ     *kueue.ClusterQueue
+		devClusterQ      *kueue.ClusterQueue
+		prodQueue        *kueue.LocalQueue
+		devQueue         *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: func(mgr manager.Manager, ctx context.Context) {
+				managerAndSchedulerSetupWithTimeoutAdmission(mgr, ctx, podsReadyTimeout, false)
+			},
+			CRDPath:     filepath.Join("..", "..", "..", "..", "config", "components", "crd", "bases"),
+			WebhookPath: filepath.Join("..", "..", "..", "..", "config", "components", "webhook"),
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+
+		defaultFlavor = utiltesting.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "podsready-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		prodClusterQ = testing.MakeClusterQueue("prod-cq").
+			Cohort("all").
+			ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, prodClusterQ)).Should(gomega.Succeed())
+
+		devClusterQ = testing.MakeClusterQueue("dev-cq").
+			Cohort("all").
+			ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, devClusterQ)).Should(gomega.Succeed())
+
+		prodQueue = testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, prodQueue)).Should(gomega.Succeed())
+
+		devQueue = testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, devQueue)).Should(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, prodClusterQ, true)
+		util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, devClusterQ, true)
+		fwk.Teardown()
+	})
+
+	ginkgo.Context("Long PodsReady timeout", func() {
```

  `SchedulerWithWaitForPodsReady` tests the `blockAdmission: true`, right? It is true that the timeout is covered by these tests. So the tests indeed cover it, but only based on an implicit assumption that setting `blockAdmission: false` does not interfere with the handling of exceeded timeout. 
  
  I guess we can assume that, but it isn't super obvious. One can imagine a subtle bug when setting `blockAdmission: false` disables the entire feature and then no test would flag it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-05-08T12:17:54Z

- **apis/config/v1beta1/configuration_types.go:80** by [@mimowo](https://github.com/mimowo) — 2023-05-08T12:17:54Z
```diff
@@ -74,6 +74,10 @@ type WaitForPodsReady struct {
 	// is cancelled and requeued in the same cluster queue. Defaults to 5min.
 	// +optional
 	Timeout *metav1.Duration `json:"timeout,omitempty"`
+
+	// BlockAdmission when true, cluster queue will block admissions for all subsequent jobs
+	// until the jobs reach the PodsReady=true condition. It defaults to false.
+	BlockAdmission bool `json:"blockAdmission,omitempty"`
```

  feel free to mark as resolved (along with other addressed comments)

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-05-09T01:22:56Z

- **test/integration/scheduler/podsready/scheduler_test.go:428** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-05-09T01:22:55Z
```diff
@@ -355,3 +355,97 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
+
+	var (
+		defaultFlavor    *kueue.ResourceFlavor
+		podsReadyTimeout time.Duration
+		ns               *corev1.Namespace
+		prodClusterQ     *kueue.ClusterQueue
+		devClusterQ      *kueue.ClusterQueue
+		prodQueue        *kueue.LocalQueue
+		devQueue         *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: func(mgr manager.Manager, ctx context.Context) {
+				managerAndSchedulerSetupWithTimeoutAdmission(mgr, ctx, podsReadyTimeout, false)
+			},
+			CRDPath:     filepath.Join("..", "..", "..", "..", "config", "components", "crd", "bases"),
+			WebhookPath: filepath.Join("..", "..", "..", "..", "config", "components", "webhook"),
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+
+		defaultFlavor = utiltesting.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "podsready-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		prodClusterQ = testing.MakeClusterQueue("prod-cq").
+			Cohort("all").
+			ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, prodClusterQ)).Should(gomega.Succeed())
+
+		devClusterQ = testing.MakeClusterQueue("dev-cq").
+			Cohort("all").
+			ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, devClusterQ)).Should(gomega.Succeed())
+
+		prodQueue = testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, prodQueue)).Should(gomega.Succeed())
+
+		devQueue = testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, devQueue)).Should(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, prodClusterQ, true)
+		util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, devClusterQ, true)
+		fwk.Teardown()
+	})
+
+	ginkgo.Context("Long PodsReady timeout", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = time.Minute
+		})
+
+		ginkgo.It("Should not block admission of one new workload if two are considered in the same scheduling cycle", func() {
+			ginkgo.By("creating two workloads but delaying cluster queue creation which has enough capacity")
+			prodWl := testing.MakeWorkload("prod-wl", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "11").Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodWl)).Should(gomega.Succeed())
+			// wait a second to ensure the CreationTimestamps differ and scheduler picks the first created to be admitted
+			time.Sleep(time.Second)
```

  You are right, I will remove the sleep action

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-05-09T06:55:16Z

- **test/integration/scheduler/podsready/scheduler_test.go:427** by [@mimowo](https://github.com/mimowo) — 2023-05-09T06:42:09Z
```diff
@@ -355,3 +355,198 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
+
+	var (
+		defaultFlavor    *kueue.ResourceFlavor
+		podsReadyTimeout time.Duration
+		ns               *corev1.Namespace
+		prodClusterQ     *kueue.ClusterQueue
+		devClusterQ      *kueue.ClusterQueue
+		prodQueue        *kueue.LocalQueue
+		devQueue         *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: func(mgr manager.Manager, ctx context.Context) {
+				managerAndSchedulerSetupWithTimeoutAdmission(mgr, ctx, podsReadyTimeout, false)
+			},
+			CRDPath:     filepath.Join("..", "..", "..", "..", "config", "components", "crd", "bases"),
+			WebhookPath: filepath.Join("..", "..", "..", "..", "config", "components", "webhook"),
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+
+		defaultFlavor = utiltesting.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "podsready-nonblocking-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		prodClusterQ = testing.MakeClusterQueue("prod-cq").
+			Cohort("all").
+			ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, prodClusterQ)).Should(gomega.Succeed())
+
+		devClusterQ = testing.MakeClusterQueue("dev-cq").
+			Cohort("all").
+			ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, devClusterQ)).Should(gomega.Succeed())
+
+		prodQueue = testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, prodQueue)).Should(gomega.Succeed())
+
+		devQueue = testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, devQueue)).Should(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, prodClusterQ, true)
+		util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, devClusterQ, true)
+		fwk.Teardown()
+	})
+
+	ginkgo.Context("Long PodsReady timeout", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = time.Minute
+		})
+
+		ginkgo.It("Should not block admission of one new workload if two are considered in the same scheduling cycle", func() {
+			ginkgo.By("creating two workloads but delaying cluster queue creation which has enough capacity")
+			prodWl := testing.MakeWorkload("prod-wl", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "11").Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodWl)).Should(gomega.Succeed())
+			// wait a second to ensure the CreationTimestamps differ and scheduler picks the first created to be admitted
```

  nit: remove the comment

- **test/integration/scheduler/podsready/scheduler_test.go:450** by [@mimowo](https://github.com/mimowo) — 2023-05-09T06:49:50Z
```diff
@@ -355,3 +355,198 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
+
+	var (
+		defaultFlavor    *kueue.ResourceFlavor
+		podsReadyTimeout time.Duration
+		ns               *corev1.Namespace
+		prodClusterQ     *kueue.ClusterQueue
+		devClusterQ      *kueue.ClusterQueue
+		prodQueue        *kueue.LocalQueue
+		devQueue         *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: func(mgr manager.Manager, ctx context.Context) {
+				managerAndSchedulerSetupWithTimeoutAdmission(mgr, ctx, podsReadyTimeout, false)
+			},
+			CRDPath:     filepath.Join("..", "..", "..", "..", "config", "components", "crd", "bases"),
+			WebhookPath: filepath.Join("..", "..", "..", "..", "config", "components", "webhook"),
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+
+		defaultFlavor = utiltesting.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "podsready-nonblocking-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		prodClusterQ = testing.MakeClusterQueue("prod-cq").
+			Cohort("all").
+			ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, prodClusterQ)).Should(gomega.Succeed())
+
+		devClusterQ = testing.MakeClusterQueue("dev-cq").
+			Cohort("all").
+			ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "5").Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, devClusterQ)).Should(gomega.Succeed())
+
+		prodQueue = testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, prodQueue)).Should(gomega.Succeed())
+
+		devQueue = testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, devQueue)).Should(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, prodClusterQ, true)
+		util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, devClusterQ, true)
+		fwk.Teardown()
+	})
+
+	ginkgo.Context("Long PodsReady timeout", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = time.Minute
+		})
+
+		ginkgo.It("Should not block admission of one new workload if two are considered in the same scheduling cycle", func() {
+			ginkgo.By("creating two workloads but delaying cluster queue creation which has enough capacity")
+			prodWl := testing.MakeWorkload("prod-wl", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "11").Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodWl)).Should(gomega.Succeed())
+			// wait a second to ensure the CreationTimestamps differ and scheduler picks the first created to be admitted
+			devWl := testing.MakeWorkload("dev-wl", ns.Name).Queue(devQueue.Name).Request(corev1.ResourceCPU, "11").Obj()
+			gomega.Expect(k8sClient.Create(ctx, devWl)).Should(gomega.Succeed())
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, prodWl, devWl)
+
+			ginkgo.By("creating the cluster queue")
+			// Delay cluster queue creation to make sure workloads are in the same
+			// scheduling cycle.
+			testCQ := testing.MakeClusterQueue("test-cq").
+				Cohort("all").
+				ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "25", "0").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, testCQ)).Should(gomega.Succeed())
+			defer func() {
+				gomega.Expect(util.DeleteClusterQueue(ctx, k8sClient, testCQ)).Should(gomega.Succeed())
+			}()
+
+			ginkgo.By("verifying that the first created workload is admitted and the second workload is admitted as the blockAdmission is false")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, prodWl)
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, devClusterQ.Name, devWl)
+		})
+
+	})
+
+	var _ = ginkgo.Context("Short PodsReady timeout", func() {
```

  nit: I suggest to only have one test in this context. Just to test that the timeout is respected.  "Should re-admit a timed out workload" seems the lightest to choose.

- **apis/config/v1beta1/defaults.go:97** by [@mimowo](https://github.com/mimowo) — 2023-05-09T06:53:40Z
```diff
@@ -87,8 +87,17 @@ func SetDefaults_Configuration(cfg *Configuration) {
 	if cfg.ClientConnection.Burst == nil {
 		cfg.ClientConnection.Burst = pointer.Int32(DefaultClientConnectionBurst)
 	}
-	if cfg.WaitForPodsReady != nil && cfg.WaitForPodsReady.Timeout == nil {
-		cfg.WaitForPodsReady.Timeout = &metav1.Duration{Duration: defaultPodsReadyTimeout}
+	if cfg.WaitForPodsReady != nil {
+		if cfg.WaitForPodsReady.Timeout == nil {
+			cfg.WaitForPodsReady.Timeout = &metav1.Duration{Duration: defaultPodsReadyTimeout}
+		}
+		if cfg.WaitForPodsReady.BlockAdmission == nil {
+			defaultBlockAdmission := true
+			if !cfg.WaitForPodsReady.Enable {
+				defaultBlockAdmission = false
```

  nit: add a test case for this in defaults_test.go

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-05-10T07:19:16Z

/lgtm
/assign @alculquicondor

- **apis/config/v1beta1/defaults_test.go:296** by [@mimowo](https://github.com/mimowo) — 2023-05-10T07:14:07Z
```diff
@@ -280,8 +280,33 @@ func TestSetDefaults_Configuration(t *testing.T) {
 			},
 			want: &Configuration{
 				WaitForPodsReady: &WaitForPodsReady{
-					Enable:  true,
-					Timeout: &podsReadyTimeoutTimeout,
+					Enable:         true,
+					BlockAdmission: pointer.Bool(true),
+					Timeout:        &podsReadyTimeoutTimeout,
+				},
+				Namespace:                          pointer.String(DefaultNamespace),
+				ControllerManagerConfigurationSpec: defaultCtrlManagerConfigurationSpec,
+				InternalCertManagement: &InternalCertManagement{
+					Enable: pointer.Bool(false),
+				},
+				ClientConnection: defaultClientConnection,
+				Integrations:     defaultIntegrations,
+			},
+		},
+		"set waitForPodsReady.blockAdmissibn to false when enable is false": {
```

  ```suggestion
  		"set waitForPodsReady.blockAdmission to false when enable is false": {
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-10T11:11:18Z

/lgtm
/assign @alculquicondor

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-10T11:11:21Z

/lgtm
/assign @alculquicondor

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-10T17:19:50Z

- **apis/config/v1beta1/defaults.go:99** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-10T17:09:31Z
```diff
@@ -87,8 +87,14 @@ func SetDefaults_Configuration(cfg *Configuration) {
 	if cfg.ClientConnection.Burst == nil {
 		cfg.ClientConnection.Burst = pointer.Int32(DefaultClientConnectionBurst)
 	}
-	if cfg.WaitForPodsReady != nil && cfg.WaitForPodsReady.Timeout == nil {
-		cfg.WaitForPodsReady.Timeout = &metav1.Duration{Duration: defaultPodsReadyTimeout}
+	if cfg.WaitForPodsReady != nil {
+		if cfg.WaitForPodsReady.Timeout == nil {
+			cfg.WaitForPodsReady.Timeout = &metav1.Duration{Duration: defaultPodsReadyTimeout}
+		}
+		if cfg.WaitForPodsReady.BlockAdmission == nil {
+			defaultBlockAdmission := true
+			cfg.WaitForPodsReady.BlockAdmission = &defaultBlockAdmission
```

  I also prefer we leave the default static as `true`.
  
  Then you can document that the value of this field is irrelevant/ignored if `enabled=false`.

- **main.go:259** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-10T17:14:07Z
```diff
@@ -248,14 +248,17 @@ func setupScheduler(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manager
 		cCache,
 		mgr.GetClient(),
 		mgr.GetEventRecorderFor(constants.AdmissionName),
-		scheduler.WithWaitForPodsReady(waitForPodsReady(cfg)),
 	)
 	if err := mgr.Add(sched); err != nil {
 		setupLog.Error(err, "Unable to add scheduler to manager")
 		os.Exit(1)
 	}
 }
 
+func blockForPodsReady(cfg *config.Configuration) bool {
+	return cfg.WaitForPodsReady != nil && cfg.WaitForPodsReady.Enable && cfg.WaitForPodsReady.BlockAdmission != nil && *cfg.WaitForPodsReady.BlockAdmission
```

  ```suggestion
  	return waitForPodsReady(cfg) && cfg.WaitForPodsReady.BlockAdmission != nil && *cfg.WaitForPodsReady.BlockAdmission
  ```

- **pkg/scheduler/scheduler.go:177** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-10T17:19:45Z
```diff
@@ -174,18 +163,17 @@ func (s *Scheduler) schedule(ctx context.Context) {
 			}
 			continue
 		}
-		if s.waitForPodsReady {
```

  why remove this check?
  
  sure, the behavior is the same, but `PodsReadyForAllAdmittedWorkloads` requires acquiring a lock and iterating over all clusterqueues, which we could avoid if waitForPodsReady is disabled.

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-05-11T01:39:17Z

- **pkg/scheduler/scheduler.go:177** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-05-11T01:39:17Z
```diff
@@ -174,18 +163,17 @@ func (s *Scheduler) schedule(ctx context.Context) {
 			}
 			continue
 		}
-		if s.waitForPodsReady {
```

  ok, I add a check in PodsReadyForAllAdmittedWorkload. I think there is no need to store `blockForPodsReady` or `podsReadyTracking` in two component
