# PR #709: add example for borrowing limit

**Summary**: add example for borrowing limit

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/709

**Last updated**: 2023-04-28T13:52:17Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@KunWuLuan](https://github.com/KunWuLuan)
- **Branch**: `add-borrowingLimit-sample` → `main`
- **Created**: 2023-04-20T09:03:01Z
- **Updated**: 2023-04-28T13:52:17Z
- **Merged**: 2023-04-28T13:52:17Z
- **Closed**: 2023-04-28T13:52:17Z
- **Labels**: `lgtm`, `tide/merge-method-squash`, `approved`, `size/M`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 1   **+43 / -1**

## Description

to #672

add example for borrowing limit

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-04-20T09:03:07Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c0419f9df853c5232c8c1abc44da434d9aa7e39b |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/644b6dbebf05490008450ffc |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-709--kubernetes-sigs-kueue.netlify.app/docs/concepts/cluster_queue](https://deploy-preview-709--kubernetes-sigs-kueue.netlify.app/docs/concepts/cluster_queue) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTcwOS0ta3ViZXJuZXRlcy1zaWdzLWt1ZXVlLm5ldGxpZnkuYXBwIn0.qlIWMA_AkQVvYm9JYA9Ewroxtx59dYs52oHYEstFp3s)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site settings](https://app.netlify.com/sites/kubernetes-sigs-kueue/settings/deploys#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-04-20T09:03:11Z

Hi @KunWuLuan. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-25T13:58:52Z

/assign @moficodes

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-04-25T13:58:54Z

@alculquicondor: GitHub didn't allow me to assign the following users: moficodes.

Note that only [kubernetes-sigs members](https://github.com/orgs/kubernetes-sigs/people) with read permissions, repo collaborators and people who have commented on this issue/PR can be assigned. Additionally, issues/PRs can only have 10 assignees at the same time.
For more information please see [the contributor guide](https://git.k8s.io/community/contributors/guide/first-contribution.md#issue-assignment-in-github)

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/709#issuecomment-1521839570):

>/assign @moficodes 


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@moficodes](https://github.com/moficodes) — 2023-04-26T16:15:25Z

@alculquicondor lgtm. Take a look.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-26T17:54:53Z

/ok-to-test

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-04-27T01:26:03Z

@KunWuLuan: The following tests **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:

Test name | Commit | Details | Required | Rerun command
--- | --- | --- | --- | ---
pull-kueue-test-integration-main | 99afc102b32393cfa80ed72efa1a4aeb783e81f8 | [link](https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/709/pull-kueue-test-integration-main/1651396231357796352) | true | `/test pull-kueue-test-integration-main`
pull-kueue-test-e2e-main-1-26 | 99afc102b32393cfa80ed72efa1a4aeb783e81f8 | [link](https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/709/pull-kueue-test-e2e-main-1-26/1651396231496208384) | true | `/test pull-kueue-test-e2e-main-1-26`
pull-kueue-test-e2e-main-1-24 | 99afc102b32393cfa80ed72efa1a4aeb783e81f8 | [link](https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/709/pull-kueue-test-e2e-main-1-24/1651396231399739392) | true | `/test pull-kueue-test-e2e-main-1-24`
pull-kueue-test-e2e-main-1-25 | 99afc102b32393cfa80ed72efa1a4aeb783e81f8 | [link](https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/709/pull-kueue-test-e2e-main-1-25/1651396231445876736) | true | `/test pull-kueue-test-e2e-main-1-25`

[Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=709). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3AKunWuLuan). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository. I understand the commands that are listed [here](https://go.k8s.io/bot-commands).
</details>
<!-- test report -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-28T13:51:36Z

/label tide/merge-method-squash
/approve
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-04-28T13:51:42Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/709#issuecomment-1527598077" title="LGTM">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/709#" title="Author self-approved">KunWuLuan</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/709#pullrequestreview-1402354102" title="Approved">moficodes</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@moficodes](https://github.com/moficodes) (commented) — 2023-04-25T14:44:00Z

- **site/content/en/docs/concepts/cluster_queue.md:249** by [@moficodes](https://github.com/moficodes) — 2023-04-25T14:44:00Z
```diff
@@ -237,6 +238,78 @@ scenarios:
   ClusterQueue `team-b-cq` before admitting any new Workloads in `team-a-cq`.
   Therefore, Kueue ensures the `nominalQuota` quota for `team-b-cq` is met.
 
+You can test borrowing limit by create the following two jobs and local queues:
+
+```
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: LocalQueue
+metadata:
+  namespace: "default"
+  name: "user-queue-1"
```

  In the example local-queue in `config/samples` it targets `cluster-queue-1` and `cluster-queue-2` here it seem to target `team-a-cq`. Why do we need the sample/borrowingLimitSamples?

### Review by [@moficodes](https://github.com/moficodes) (changes_requested) — 2023-04-25T14:55:20Z

Don't think we need the sample files in config/samples/borrowingLimitSamples

- **site/content/en/docs/concepts/cluster_queue.md:314** by [@moficodes](https://github.com/moficodes) — 2023-04-25T14:54:46Z
```diff
@@ -237,6 +238,78 @@ scenarios:
   ClusterQueue `team-b-cq` before admitting any new Workloads in `team-a-cq`.
   Therefore, Kueue ensures the `nominalQuota` quota for `team-b-cq` is met.
 
+You can test borrowing limit by create the following two jobs and local queues:
+
+```
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: LocalQueue
+metadata:
+  namespace: "default"
+  name: "user-queue-1"
+spec:
+  clusterQueue: "team-a-cq"
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job-team-a
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue-1
+spec:
+  parallelism: 1
+  completions: 1
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:v0.0.3
+        args: ["30s"]
+        resources:
+          requests:
+            cpu: 10
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+```
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: LocalQueue
+metadata:
+  namespace: "default"
+  name: "user-queue-2"
+spec:
+  clusterQueue: "team-b-cq"
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job-team-b
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue-2
+spec:
+  parallelism: 1
+  completions: 1
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:v0.0.3
+        args: ["30s"]
+        resources:
+          requests:
+            cpu: 12
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+You will see job `sample-job-team-a` is admitted because it borrowed 1 cpu from the cohort and `sample-job-team-b` can't be admitted because there is not enough cpu in the cohort.
+
 ### BorrowingLimit
 
```

  We want to add the example here. Not change the existing borrowing example. 
  
  There are two modes of borrowing. Without any limit mentioned it borrows the sum of quotas. So we can show a job that does that. Then set explicit borrowing limit to show it can no longer borrow the whole amount.

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-04-26T01:34:14Z

- **site/content/en/docs/concepts/cluster_queue.md:249** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-04-26T01:34:13Z
```diff
@@ -237,6 +238,78 @@ scenarios:
   ClusterQueue `team-b-cq` before admitting any new Workloads in `team-a-cq`.
   Therefore, Kueue ensures the `nominalQuota` quota for `team-b-cq` is met.
 
+You can test borrowing limit by create the following two jobs and local queues:
+
+```
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: LocalQueue
+metadata:
+  namespace: "default"
+  name: "user-queue-1"
```

  Got it. I will remove files in config/samples/borrowingLimitSamples

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-04-26T01:39:18Z

- **site/content/en/docs/concepts/cluster_queue.md:314** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-04-26T01:39:18Z
```diff
@@ -237,6 +238,78 @@ scenarios:
   ClusterQueue `team-b-cq` before admitting any new Workloads in `team-a-cq`.
   Therefore, Kueue ensures the `nominalQuota` quota for `team-b-cq` is met.
 
+You can test borrowing limit by create the following two jobs and local queues:
+
+```
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: LocalQueue
+metadata:
+  namespace: "default"
+  name: "user-queue-1"
+spec:
+  clusterQueue: "team-a-cq"
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job-team-a
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue-1
+spec:
+  parallelism: 1
+  completions: 1
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:v0.0.3
+        args: ["30s"]
+        resources:
+          requests:
+            cpu: 10
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+```
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: LocalQueue
+metadata:
+  namespace: "default"
+  name: "user-queue-2"
+spec:
+  clusterQueue: "team-b-cq"
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job-team-b
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue-2
+spec:
+  parallelism: 1
+  completions: 1
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:v0.0.3
+        args: ["30s"]
+        resources:
+          requests:
+            cpu: 12
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+You will see job `sample-job-team-a` is admitted because it borrowed 1 cpu from the cohort and `sample-job-team-b` can't be admitted because there is not enough cpu in the cohort.
+
 ### BorrowingLimit
 
```

  OK. I have moved the sample to borrowingLimit section

### Review by [@moficodes](https://github.com/moficodes) (approved) — 2023-04-26T16:15:45Z

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-26T17:57:10Z

- **site/content/en/docs/concepts/cluster_queue.md:246** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-26T17:55:47Z
```diff
@@ -243,9 +243,54 @@ To limit the amount of resources that a ClusterQueue can borrow from others,
 you can set the `.spec.resourcesGroup[*].flavors[*].resource[*].borrowingLimit`
 [quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/) field.
 
+Assume you created the following two ClusterQueues:
```

  ```suggestion
  As an example, assume you created the following two ClusterQueues:
  ```

- **site/content/en/docs/concepts/cluster_queue.md:293** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-26T17:57:08Z
```diff
@@ -243,9 +243,54 @@ To limit the amount of resources that a ClusterQueue can borrow from others,
 you can set the `.spec.resourcesGroup[*].flavors[*].resource[*].borrowingLimit`
 [quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/) field.
 
+Assume you created the following two ClusterQueues:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu", "memory"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9  
+        borrowingLimit: 1
+      - name: "memory"
+        nominalQuota: 36Gi
+```
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-b-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu", "memory"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 12  
+      - name: "memory"
+        nominalQuota: 48Gi
+```
+
+In this case, because we set borrowingLimit in ClusterQueue `team-a-cq`, if 
+ClusterQueue `team-b-cq` has no admitted Workloads, then ClusterQueue `team-a-cq` 
+can admit Workloads with resources adding up to `9+1=10` CPUs. 
+
 If, for a given flavor/resource, the `borrowingLimit` field is empty or null, 
 a ClusterQueue can borrow up to the sum of nominal quotas from all the 
-ClusterQueues in the cohort.
+ClusterQueues in the cohort. So `team-a-cq` can use up to `48+36=84Gi` memory.
```

  If we do it about `cpu`, then we can say that `team-b-cq` can borrow 12+9 CPUs. Then we can reduce the size of the samples to only account for cpu.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-26T17:57:48Z

- **site/content/en/docs/concepts/cluster_queue.md:293** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-26T17:57:47Z
```diff
@@ -243,9 +243,54 @@ To limit the amount of resources that a ClusterQueue can borrow from others,
 you can set the `.spec.resourcesGroup[*].flavors[*].resource[*].borrowingLimit`
 [quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/) field.
 
+Assume you created the following two ClusterQueues:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu", "memory"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9  
+        borrowingLimit: 1
+      - name: "memory"
+        nominalQuota: 36Gi
+```
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-b-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu", "memory"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 12  
+      - name: "memory"
+        nominalQuota: 48Gi
+```
+
+In this case, because we set borrowingLimit in ClusterQueue `team-a-cq`, if 
+ClusterQueue `team-b-cq` has no admitted Workloads, then ClusterQueue `team-a-cq` 
+can admit Workloads with resources adding up to `9+1=10` CPUs. 
+
 If, for a given flavor/resource, the `borrowingLimit` field is empty or null, 
 a ClusterQueue can borrow up to the sum of nominal quotas from all the 
-ClusterQueues in the cohort.
+ClusterQueues in the cohort. So `team-a-cq` can use up to `48+36=84Gi` memory.
```

  `For the yamls listed above`...

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-04-27T01:20:15Z

- **site/content/en/docs/concepts/cluster_queue.md:293** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-04-27T01:20:15Z
```diff
@@ -243,9 +243,54 @@ To limit the amount of resources that a ClusterQueue can borrow from others,
 you can set the `.spec.resourcesGroup[*].flavors[*].resource[*].borrowingLimit`
 [quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/) field.
 
+Assume you created the following two ClusterQueues:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu", "memory"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9  
+        borrowingLimit: 1
+      - name: "memory"
+        nominalQuota: 36Gi
+```
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-b-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu", "memory"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 12  
+      - name: "memory"
+        nominalQuota: 48Gi
+```
+
+In this case, because we set borrowingLimit in ClusterQueue `team-a-cq`, if 
+ClusterQueue `team-b-cq` has no admitted Workloads, then ClusterQueue `team-a-cq` 
+can admit Workloads with resources adding up to `9+1=10` CPUs. 
+
 If, for a given flavor/resource, the `borrowingLimit` field is empty or null, 
 a ClusterQueue can borrow up to the sum of nominal quotas from all the 
-ClusterQueues in the cohort.
+ClusterQueues in the cohort. So `team-a-cq` can use up to `48+36=84Gi` memory.
```

  You are right. Thanks for your suggestions. 😄

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-27T16:12:58Z

- **site/content/en/docs/concepts/cluster_queue.md:265** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-27T16:12:58Z
```diff
@@ -243,9 +243,55 @@ To limit the amount of resources that a ClusterQueue can borrow from others,
 you can set the `.spec.resourcesGroup[*].flavors[*].resource[*].borrowingLimit`
 [quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/) field.
 
+As an example, assume you created the following two ClusterQueues:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu", "memory"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9  
+        borrowingLimit: 1
+      - name: "memory"
+        nominalQuota: 36Gi
```

  let's remove this for simplicity of the example
