# PR #710: [preemption] Add LowerOrNewerEqualPriority preemption

**Summary**: [preemption] Add LowerOrNewerEqualPriority preemption

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/710

**Last updated**: 2023-05-08T19:04:57Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `prevent_starvation_of_large_jobs` → `main`
- **Created**: 2023-04-21T14:23:09Z
- **Updated**: 2023-05-08T19:04:57Z
- **Merged**: 2023-05-03T15:16:14Z
- **Closed**: 2023-05-03T15:16:14Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 6   **+154 / -22**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation


Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Add LowerOrNewerEqualPriority preemption in the same queue based on the workload's scheduling timestamp, allowing to prevent starvation of large jobs.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #534

#### Special notes for your reviewer:

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-04-21T14:23:18Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | b23405e82c85a3b07fb1a1a649e2bddbca075b49 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/645278041fc0cb0008923fa1 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-04-21T14:23:19Z

Hi @trasc. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-22T01:15:58Z

/ok-to-test

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-05-03T14:54:17Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/710#pullrequestreview-1410900538" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/710#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-03T15:05:50Z

/lgtm

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-27T20:29:37Z

- **apis/kueue/v1beta1/clusterqueue_types.go:277** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-27T20:17:28Z
```diff
@@ -270,7 +271,7 @@ type ClusterQueuePreemption struct {
 	//   lower priority than the pending Workload.
 	//
 	// +kubebuilder:default=Never
-	// +kubebuilder:validation:Enum=Never;LowerPriority
+	// +kubebuilder:validation:Enum=Never;LowerPriority;LowerOrEqualPriority
```

  would it make sense to also add to reclaimWithinCohort?

- **pkg/scheduler/preemption/preemption.go:245** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-27T20:21:21Z
```diff
@@ -240,32 +240,50 @@ func resourcesRequiringPreemption(assignment flavorassigner.Assignment) resource
 // preempting workload needs.
 func findCandidates(wl *kueue.Workload, cq *cache.ClusterQueue, resPerFlv resourcesPerFlavor) []*workload.Info {
 	var candidates []*workload.Info
-	cqs := sets.New(cq)
-	if cq.Cohort != nil && cq.Preemption.ReclaimWithinCohort != kueue.PreemptionPolicyNever {
-		cqs = cq.Cohort.Members
+	wlPriority := priority.Priority(wl)
+
+	if cq.Preemption.WithinClusterQueue != kueue.PreemptionPolicyNever {
```

  What's the reason to swap the order to check `ReclaimWithinCohort` and `WithinClusterQueue`?
  
  I'm not saying it would break something, but just want to understand if it's necessary. By swapping the order we get a more complex diff, so it's hard to tell if something changed.

- **apis/kueue/v1beta1/clusterqueue_types.go:271** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-27T20:22:29Z
```diff
@@ -270,7 +271,7 @@ type ClusterQueuePreemption struct {
 	//   lower priority than the pending Workload.
```

  Explain the semantics of LowerOrEqualPriority here

- **apis/kueue/v1beta1/clusterqueue_types.go:243** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-27T20:23:14Z
```diff
@@ -237,9 +237,10 @@ const (
 type PreemptionPolicy string
 
 const (
-	PreemptionPolicyNever         PreemptionPolicy = "Never"
-	PreemptionPolicyAny           PreemptionPolicy = "Any"
-	PreemptionPolicyLowerPriority PreemptionPolicy = "LowerPriority"
+	PreemptionPolicyNever                PreemptionPolicy = "Never"
+	PreemptionPolicyAny                  PreemptionPolicy = "Any"
+	PreemptionPolicyLowerPriority        PreemptionPolicy = "LowerPriority"
+	PreemptionPolicyLowerOrEqualPriority PreemptionPolicy = "LowerOrEqualPriority"
```

  How about this?
  
  ```suggestion
  	PreemptionPolicyLowerOrEqualPriority PreemptionPolicy = "LowerOrNewerEqualPriority"
  ```
  
  cc @ahg-g for sanity check.

- **pkg/scheduler/preemption/preemption_test.go:708** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-27T20:25:42Z
```diff
@@ -684,6 +693,44 @@ func TestPreemption(t *testing.T) {
 			},
 			wantPreempted: sets.New("/low-alpha", "/low-beta"),
 		},
+		"preempt newer workloads with the same priority": {
+			admitted: []kueue.Workload{
+				*utiltesting.MakeWorkload("wl1", "").
+					Priority(2).
+					Request(corev1.ResourceCPU, "3").
+					Admit(utiltesting.MakeAdmission("preventStarvation").Assignment(corev1.ResourceCPU, "default", "3").Obj()).
+					Obj(),
+				*utiltesting.MakeWorkload("wl2", "").
+					Priority(1).
+					Creation(time.Now()).
+					Request(corev1.ResourceCPU, "2").
+					Admit(utiltesting.MakeAdmission("preventStarvation").Assignment(corev1.ResourceCPU, "default", "2").Obj()).
+					Obj(),
```

  Add another workload with the same priority that has a time older than 15 seconds

- **test/integration/scheduler/preemption_test.go:159** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-27T20:26:34Z
```diff
@@ -129,6 +131,51 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, highWl2)
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, lowWl1, lowWl2)
 		})
+
+		ginkgo.It("Should preempt newer Workloads with the same priority when there is not enough quota", func() {
+			ginkgo.By("Creating initial Workloads with different priorities")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			wl2 := testing.MakeWorkload("wl-2", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			wl3 := testing.MakeWorkload("wl-3", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "3").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, wl2)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, wl3)).To(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, wl1, wl2)
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl3)
+
+			ginkgo.By("Waiting one second")
```

  explain why we need to wait

- **test/integration/scheduler/preemption_test.go:136** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-27T20:27:55Z
```diff
@@ -129,6 +131,51 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, highWl2)
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, lowWl1, lowWl2)
 		})
+
+		ginkgo.It("Should preempt newer Workloads with the same priority when there is not enough quota", func() {
+			ginkgo.By("Creating initial Workloads with different priorities")
```

  they all have the same priority

- **test/integration/scheduler/preemption_test.go:154** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-27T20:29:05Z
```diff
@@ -129,6 +131,51 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, highWl2)
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, lowWl1, lowWl2)
 		})
+
+		ginkgo.It("Should preempt newer Workloads with the same priority when there is not enough quota", func() {
+			ginkgo.By("Creating initial Workloads with different priorities")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			wl2 := testing.MakeWorkload("wl-2", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			wl3 := testing.MakeWorkload("wl-3", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "3").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, wl2)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, wl3)).To(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, wl1, wl2)
```

  better create wl3 after wl1 and wl2 are admitted.

- **test/integration/scheduler/preemption_test.go:140** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-27T20:29:33Z
```diff
@@ -129,6 +131,51 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, highWl2)
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, lowWl1, lowWl2)
 		})
+
+		ginkgo.It("Should preempt newer Workloads with the same priority when there is not enough quota", func() {
+			ginkgo.By("Creating initial Workloads with different priorities")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
```

  ```suggestion
  				Request(corev1.ResourceCPU, "2").
  ```
  
  Then we don't need wl2

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-28T06:29:20Z

- **apis/kueue/v1beta1/clusterqueue_types.go:271** by [@trasc](https://github.com/trasc) — 2023-04-28T04:14:27Z
```diff
@@ -270,7 +271,7 @@ type ClusterQueuePreemption struct {
 	//   lower priority than the pending Workload.
```

  done

- **apis/kueue/v1beta1/clusterqueue_types.go:277** by [@trasc](https://github.com/trasc) — 2023-04-28T04:17:32Z
```diff
@@ -270,7 +271,7 @@ type ClusterQueuePreemption struct {
 	//   lower priority than the pending Workload.
 	//
 	// +kubebuilder:default=Never
-	// +kubebuilder:validation:Enum=Never;LowerPriority
+	// +kubebuilder:validation:Enum=Never;LowerPriority;LowerOrEqualPriority
```

  for cohort we already have the option to preempt `Any`

- **pkg/scheduler/preemption/preemption.go:245** by [@trasc](https://github.com/trasc) — 2023-04-28T04:25:59Z
```diff
@@ -240,32 +240,50 @@ func resourcesRequiringPreemption(assignment flavorassigner.Assignment) resource
 // preempting workload needs.
 func findCandidates(wl *kueue.Workload, cq *cache.ClusterQueue, resPerFlv resourcesPerFlavor) []*workload.Info {
 	var candidates []*workload.Info
-	cqs := sets.New(cq)
-	if cq.Cohort != nil && cq.Preemption.ReclaimWithinCohort != kueue.PreemptionPolicyNever {
-		cqs = cq.Cohort.Members
+	wlPriority := priority.Priority(wl)
+
+	if cq.Preemption.WithinClusterQueue != kueue.PreemptionPolicyNever {
```

  The goal was just to treat the `WithinClusterQueue`  differently since we have an additional policy for it, otherwise the for each queue loop is getting to hard to read .
  
  Anyway, the order in which we walk the queues is not relevant here since the candidates are sorted after this.  L83-L88

- **test/integration/scheduler/preemption_test.go:140** by [@trasc](https://github.com/trasc) — 2023-04-28T05:05:43Z
```diff
@@ -129,6 +131,51 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, highWl2)
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, lowWl1, lowWl2)
 		})
+
+		ginkgo.It("Should preempt newer Workloads with the same priority when there is not enough quota", func() {
+			ginkgo.By("Creating initial Workloads with different priorities")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
```

  It will make no difference, I prefer to keep it as is since it was written to resemble the scenario described in the associated issue.

- **pkg/scheduler/preemption/preemption_test.go:708** by [@trasc](https://github.com/trasc) — 2023-04-28T05:07:28Z
```diff
@@ -684,6 +693,44 @@ func TestPreemption(t *testing.T) {
 			},
 			wantPreempted: sets.New("/low-alpha", "/low-beta"),
 		},
+		"preempt newer workloads with the same priority": {
+			admitted: []kueue.Workload{
+				*utiltesting.MakeWorkload("wl1", "").
+					Priority(2).
+					Request(corev1.ResourceCPU, "3").
+					Admit(utiltesting.MakeAdmission("preventStarvation").Assignment(corev1.ResourceCPU, "default", "3").Obj()).
+					Obj(),
+				*utiltesting.MakeWorkload("wl2", "").
+					Priority(1).
+					Creation(time.Now()).
+					Request(corev1.ResourceCPU, "2").
+					Admit(utiltesting.MakeAdmission("preventStarvation").Assignment(corev1.ResourceCPU, "default", "2").Obj()).
+					Obj(),
```

  why?

- **test/integration/scheduler/preemption_test.go:136** by [@trasc](https://github.com/trasc) — 2023-04-28T05:10:44Z
```diff
@@ -129,6 +131,51 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, highWl2)
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, lowWl1, lowWl2)
 		})
+
+		ginkgo.It("Should preempt newer Workloads with the same priority when there is not enough quota", func() {
+			ginkgo.By("Creating initial Workloads with different priorities")
```

  done

- **test/integration/scheduler/preemption_test.go:154** by [@trasc](https://github.com/trasc) — 2023-04-28T05:13:25Z
```diff
@@ -129,6 +131,51 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, highWl2)
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, lowWl1, lowWl2)
 		})
+
+		ginkgo.It("Should preempt newer Workloads with the same priority when there is not enough quota", func() {
+			ginkgo.By("Creating initial Workloads with different priorities")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			wl2 := testing.MakeWorkload("wl-2", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			wl3 := testing.MakeWorkload("wl-3", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "3").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, wl2)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, wl3)).To(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, wl1, wl2)
```

  done

- **test/integration/scheduler/preemption_test.go:159** by [@trasc](https://github.com/trasc) — 2023-04-28T05:13:32Z
```diff
@@ -129,6 +131,51 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, highWl2)
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, lowWl1, lowWl2)
 		})
+
+		ginkgo.It("Should preempt newer Workloads with the same priority when there is not enough quota", func() {
+			ginkgo.By("Creating initial Workloads with different priorities")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			wl2 := testing.MakeWorkload("wl-2", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			wl3 := testing.MakeWorkload("wl-3", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "3").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, wl2)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, wl3)).To(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, wl1, wl2)
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl3)
+
+			ginkgo.By("Waiting one second")
```

  done

- **apis/kueue/v1beta1/clusterqueue_types.go:243** by [@trasc](https://github.com/trasc) — 2023-04-28T05:17:16Z
```diff
@@ -237,9 +237,10 @@ const (
 type PreemptionPolicy string
 
 const (
-	PreemptionPolicyNever         PreemptionPolicy = "Never"
-	PreemptionPolicyAny           PreemptionPolicy = "Any"
-	PreemptionPolicyLowerPriority PreemptionPolicy = "LowerPriority"
+	PreemptionPolicyNever                PreemptionPolicy = "Never"
+	PreemptionPolicyAny                  PreemptionPolicy = "Any"
+	PreemptionPolicyLowerPriority        PreemptionPolicy = "LowerPriority"
+	PreemptionPolicyLowerOrEqualPriority PreemptionPolicy = "LowerOrEqualPriority"
```

  I guess that to fully describe it needs to be something like `LowerPriorityOrNewerWithEqualPriority`  too much to ask someone to write it in a yaml.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-28T13:49:49Z

- **pkg/scheduler/preemption/preemption_test.go:708** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-28T13:49:49Z
```diff
@@ -684,6 +693,44 @@ func TestPreemption(t *testing.T) {
 			},
 			wantPreempted: sets.New("/low-alpha", "/low-beta"),
 		},
+		"preempt newer workloads with the same priority": {
+			admitted: []kueue.Workload{
+				*utiltesting.MakeWorkload("wl1", "").
+					Priority(2).
+					Request(corev1.ResourceCPU, "3").
+					Admit(utiltesting.MakeAdmission("preventStarvation").Assignment(corev1.ResourceCPU, "default", "3").Obj()).
+					Obj(),
+				*utiltesting.MakeWorkload("wl2", "").
+					Priority(1).
+					Creation(time.Now()).
+					Request(corev1.ResourceCPU, "2").
+					Admit(utiltesting.MakeAdmission("preventStarvation").Assignment(corev1.ResourceCPU, "default", "2").Obj()).
+					Obj(),
```

  in order to have 2 workloads with the same priority where one is preempted (the newer one), but not the other.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-28T18:16:50Z

- **test/integration/scheduler/preemption_test.go:140** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-28T18:16:36Z
```diff
@@ -129,6 +131,51 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, highWl2)
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, lowWl1, lowWl2)
 		})
+
+		ginkgo.It("Should preempt newer Workloads with the same priority when there is not enough quota", func() {
+			ginkgo.By("Creating initial Workloads with different priorities")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
```

  Having less workloads could make the test marginally faster, but it's not a big deal.
  I'm ok either way.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2023-04-28T20:03:25Z

- **apis/kueue/v1beta1/clusterqueue_types.go:243** by [@ahg-g](https://github.com/ahg-g) — 2023-04-28T20:03:25Z
```diff
@@ -237,9 +237,10 @@ const (
 type PreemptionPolicy string
 
 const (
-	PreemptionPolicyNever         PreemptionPolicy = "Never"
-	PreemptionPolicyAny           PreemptionPolicy = "Any"
-	PreemptionPolicyLowerPriority PreemptionPolicy = "LowerPriority"
+	PreemptionPolicyNever                PreemptionPolicy = "Never"
+	PreemptionPolicyAny                  PreemptionPolicy = "Any"
+	PreemptionPolicyLowerPriority        PreemptionPolicy = "LowerPriority"
+	PreemptionPolicyLowerOrEqualPriority PreemptionPolicy = "LowerOrEqualPriority"
```

  sgtm, `LowerOrNewerEqualPriority` sounds long, but I have seen longer parameters :)

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-02T10:20:01Z

- **test/integration/scheduler/preemption_test.go:168** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-02T10:19:34Z
```diff
@@ -129,6 +131,51 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, highWl2)
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, lowWl1, lowWl2)
 		})
+
+		ginkgo.It("Should preempt newer Workloads with the same priority when there is not enough quota", func() {
+			ginkgo.By("Creating initial Workloads")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			wl2 := testing.MakeWorkload("wl-2", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			wl3 := testing.MakeWorkload("wl-3", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "3").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, wl2)).To(gomega.Succeed())
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, wl1, wl2)
+
+			gomega.Expect(k8sClient.Create(ctx, wl3)).To(gomega.Succeed())
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl3)
+
+			ginkgo.By("Waiting one second, to ensure that the new workload has a later creation time")
+			time.Sleep(time.Second)
+
+			ginkgo.By("Creating a new Workload")
+			wl4 := testing.MakeWorkload("wl-4", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl4)).To(gomega.Succeed())
```

  Uhm... Updating the `CreationTimestamp` in the wl4 might be better than creating the wl4 after waiting for a second. Since the key here is not to wait for other components (e.g., scheduler) but whether the `CreationTimestamp` is newer.
  
  @trasc @alculquicondor wdyt?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-03T07:25:59Z

- **test/integration/scheduler/preemption_test.go:168** by [@trasc](https://github.com/trasc) — 2023-05-03T07:25:58Z
```diff
@@ -129,6 +131,51 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, highWl2)
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, lowWl1, lowWl2)
 		})
+
+		ginkgo.It("Should preempt newer Workloads with the same priority when there is not enough quota", func() {
+			ginkgo.By("Creating initial Workloads")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			wl2 := testing.MakeWorkload("wl-2", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			wl3 := testing.MakeWorkload("wl-3", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "3").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, wl2)).To(gomega.Succeed())
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, wl1, wl2)
+
+			gomega.Expect(k8sClient.Create(ctx, wl3)).To(gomega.Succeed())
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl3)
+
+			ginkgo.By("Waiting one second, to ensure that the new workload has a later creation time")
+			time.Sleep(time.Second)
+
+			ginkgo.By("Creating a new Workload")
+			wl4 := testing.MakeWorkload("wl-4", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl4)).To(gomega.Succeed())
```

  Granted that waiting is not ideal, but I don't know if the control plane started by envtest will allow it, and even if it is allowed it's something that we don't expect to happen with a real api server.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-03T08:55:58Z

- **pkg/scheduler/preemption/preemption_test.go:708** by [@trasc](https://github.com/trasc) — 2023-05-03T08:46:12Z
```diff
@@ -684,6 +693,44 @@ func TestPreemption(t *testing.T) {
 			},
 			wantPreempted: sets.New("/low-alpha", "/low-beta"),
 		},
+		"preempt newer workloads with the same priority": {
+			admitted: []kueue.Workload{
+				*utiltesting.MakeWorkload("wl1", "").
+					Priority(2).
+					Request(corev1.ResourceCPU, "3").
+					Admit(utiltesting.MakeAdmission("preventStarvation").Assignment(corev1.ResourceCPU, "default", "3").Obj()).
+					Obj(),
+				*utiltesting.MakeWorkload("wl2", "").
+					Priority(1).
+					Creation(time.Now()).
+					Request(corev1.ResourceCPU, "2").
+					Admit(utiltesting.MakeAdmission("preventStarvation").Assignment(corev1.ResourceCPU, "default", "2").Obj()).
+					Obj(),
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-03T14:54:09Z

one minor nit

/approve

- **pkg/scheduler/preemption/preemption.go:270** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-03T13:28:49Z
```diff
@@ -240,32 +240,50 @@ func resourcesRequiringPreemption(assignment flavorassigner.Assignment) resource
 // preempting workload needs.
 func findCandidates(wl *kueue.Workload, cq *cache.ClusterQueue, resPerFlv resourcesPerFlavor) []*workload.Info {
 	var candidates []*workload.Info
-	cqs := sets.New(cq)
-	if cq.Cohort != nil && cq.Preemption.ReclaimWithinCohort != kueue.PreemptionPolicyNever {
-		cqs = cq.Cohort.Members
+	wlPriority := priority.Priority(wl)
+
+	if cq.Preemption.WithinClusterQueue != kueue.PreemptionPolicyNever {
+		considerSamePrio := (cq.Preemption.WithinClusterQueue == kueue.PreemptionPolicyLowerOrNewerEqualPriority)
+		preemptorTS := workload.GetQueueOrderTimestamp(wl)
+
+		for _, candidateWl := range cq.Workloads {
+			candidatePriority := priority.Priority(candidateWl.Obj)
+			if candidatePriority > wlPriority {
+				continue
+			}
+
+			if candidatePriority == wlPriority && !(considerSamePrio && preemptorTS.Before(workload.GetQueueOrderTimestamp(candidateWl.Obj))) {
+				continue
+			}
+
+			if !workloadUsesResources(candidateWl, resPerFlv) {
+				continue
+			}
+			candidates = append(candidates, candidateWl)
+		}
 	}
-	if cq.Preemption.WithinClusterQueue == kueue.PreemptionPolicyNever {
+
+	if cq.Cohort != nil && cq.Preemption.ReclaimWithinCohort != kueue.PreemptionPolicyNever {
+		cqs := cq.Cohort.Members
 		cqs.Delete(cq)
-	}
-	for cohortCQ := range cqs {
-		onlyLowerPrio := true
-		if cq != cohortCQ {
+		for cohortCQ := range cqs {
+			onlyLowerPrio := true
```

  a bit weird that this line is here. Move it closer to line 275

- **test/integration/scheduler/preemption_test.go:168** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-03T14:50:56Z
```diff
@@ -129,6 +131,51 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, highWl2)
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, lowWl1, lowWl2)
 		})
+
+		ginkgo.It("Should preempt newer Workloads with the same priority when there is not enough quota", func() {
+			ginkgo.By("Creating initial Workloads")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			wl2 := testing.MakeWorkload("wl-2", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			wl3 := testing.MakeWorkload("wl-3", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "3").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, wl2)).To(gomega.Succeed())
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, wl1, wl2)
+
+			gomega.Expect(k8sClient.Create(ctx, wl3)).To(gomega.Succeed())
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl3)
+
+			ginkgo.By("Waiting one second, to ensure that the new workload has a later creation time")
+			time.Sleep(time.Second)
+
+			ginkgo.By("Creating a new Workload")
+			wl4 := testing.MakeWorkload("wl-4", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl4)).To(gomega.Succeed())
```

  +1 on @trasc
  At least 1s is not that much for an integration test.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-03T15:05:16Z

- **pkg/scheduler/preemption/preemption.go:270** by [@trasc](https://github.com/trasc) — 2023-05-03T15:05:16Z
```diff
@@ -240,32 +240,50 @@ func resourcesRequiringPreemption(assignment flavorassigner.Assignment) resource
 // preempting workload needs.
 func findCandidates(wl *kueue.Workload, cq *cache.ClusterQueue, resPerFlv resourcesPerFlavor) []*workload.Info {
 	var candidates []*workload.Info
-	cqs := sets.New(cq)
-	if cq.Cohort != nil && cq.Preemption.ReclaimWithinCohort != kueue.PreemptionPolicyNever {
-		cqs = cq.Cohort.Members
+	wlPriority := priority.Priority(wl)
+
+	if cq.Preemption.WithinClusterQueue != kueue.PreemptionPolicyNever {
+		considerSamePrio := (cq.Preemption.WithinClusterQueue == kueue.PreemptionPolicyLowerOrNewerEqualPriority)
+		preemptorTS := workload.GetQueueOrderTimestamp(wl)
+
+		for _, candidateWl := range cq.Workloads {
+			candidatePriority := priority.Priority(candidateWl.Obj)
+			if candidatePriority > wlPriority {
+				continue
+			}
+
+			if candidatePriority == wlPriority && !(considerSamePrio && preemptorTS.Before(workload.GetQueueOrderTimestamp(candidateWl.Obj))) {
+				continue
+			}
+
+			if !workloadUsesResources(candidateWl, resPerFlv) {
+				continue
+			}
+			candidates = append(candidates, candidateWl)
+		}
 	}
-	if cq.Preemption.WithinClusterQueue == kueue.PreemptionPolicyNever {
+
+	if cq.Cohort != nil && cq.Preemption.ReclaimWithinCohort != kueue.PreemptionPolicyNever {
+		cqs := cq.Cohort.Members
 		cqs.Delete(cq)
-	}
-	for cohortCQ := range cqs {
-		onlyLowerPrio := true
-		if cq != cohortCQ {
+		for cohortCQ := range cqs {
+			onlyLowerPrio := true
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-03T18:18:33Z

- **test/integration/scheduler/preemption_test.go:168** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-03T18:18:33Z
```diff
@@ -129,6 +131,51 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, highWl2)
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, lowWl1, lowWl2)
 		})
+
+		ginkgo.It("Should preempt newer Workloads with the same priority when there is not enough quota", func() {
+			ginkgo.By("Creating initial Workloads")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			wl2 := testing.MakeWorkload("wl-2", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			wl3 := testing.MakeWorkload("wl-3", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "3").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, wl2)).To(gomega.Succeed())
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, wl1, wl2)
+
+			gomega.Expect(k8sClient.Create(ctx, wl3)).To(gomega.Succeed())
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl3)
+
+			ginkgo.By("Waiting one second, to ensure that the new workload has a later creation time")
+			time.Sleep(time.Second)
+
+			ginkgo.By("Creating a new Workload")
+			wl4 := testing.MakeWorkload("wl-4", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl4)).To(gomega.Succeed())
```

  >  we don't expect to happen with a real api server.
  
  You're right. thanks.
