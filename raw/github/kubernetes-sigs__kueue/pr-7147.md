# PR #7147: feat: add backoff util

**Summary**: feat: add backoff util

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7147

**Last updated**: 2025-10-14T12:19:48Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mykysha](https://github.com/mykysha)
- **Branch**: `feat/exponential-backoff-util` → `main`
- **Created**: 2025-10-03T08:04:00Z
- **Updated**: 2025-10-14T12:19:48Z
- **Merged**: 2025-10-14T12:19:36Z
- **Closed**: 2025-10-14T12:19:36Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: _none_
- **Changed files**: 5   **+153 / -42**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add a generalized backoff to the utility package to be used across kueue.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Follow-up for #6753 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-10-03T08:04:07Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | bd1f67dd6efb7703f52aa0f7a40be17151fbed77 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68ee37d5f0dd6b0008e3354a |

### Comment by [@mykysha](https://github.com/mykysha) — 2025-10-09T14:29:46Z

/test pull-kueue-test-e2e-main-1-31

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-09T14:42:08Z

LGTM overall, I will return to the PR tomorrow or Friday, but a pass from @gabesaba or @mszadkow would be great

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-14T12:18:10Z

/lgtm
/approve
I think this looks much better to have the logic commonized.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-14T12:18:19Z

LGTM label has been added.  <details>Git tree hash: 10bce1ea68bf7a030b790fe455897b0125e7cb7f</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-14T12:18:20Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7147#issuecomment-3401503052" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7147#" title="Author self-approved">mykysha</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-10-03T09:42:17Z

- **pkg/cache/queue/second_pass_queue.go:1** by [@gabesaba](https://github.com/gabesaba) — 2025-10-03T09:42:17Z
  What if we keep simple implementation, rather than depending on a library? And just expose the min/max backoff? It seems like jitter and factor are not necessary for now

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-10-03T09:49:58Z

- **pkg/cache/queue/second_pass_queue.go:1** by [@mykysha](https://github.com/mykysha) — 2025-10-03T09:49:58Z
  Can do, especially given the simple calculation nature of what is required. However, in #6753 it was discussed to keep the current implementation with the library, and to commonize it in util, so not sure if any specific future use-cases are planned that make the library usage more future-proof.
  
  @mimowo can we use a simple mathematical backoff calculation here?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-03T11:57:16Z

- **pkg/cache/queue/second_pass_queue.go:1** by [@mimowo](https://github.com/mimowo) — 2025-10-03T11:57:16Z
  I'm ok either way. Indeed I proposed to commonize the approach as we compute backoffs in a couple of places already. However, I don't have a strong view, we can only commonize the places which use the current approach.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-10-03T13:43:50Z

- **pkg/cache/queue/second_pass_queue.go:1** by [@mykysha](https://github.com/mykysha) — 2025-10-03T13:43:49Z
  Have simplified the backoff util logic, removing Factor and leaving Min and Max as inputs to the backoff. Also, removed the usage of the external wait library. Let me know if this approach works

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-09T11:21:12Z

- **pkg/cache/queue/second_pass_queue.go:1** by [@mimowo](https://github.com/mimowo) — 2025-10-09T11:21:12Z
  Actually, we may need to have jitter as this is already mentioned in the API field documentation for BackoffLimit: https://github.com/kubernetes-sigs/kueue/blob/baf4c2392c60c13780b554d375c8fdb87c1d2f59/apis/config/v1beta1/configuration_types.go#L318-L333
  
  So, by this subtle change we would break the specification.
  
  I would like to commonize the backoff code, to achieve that we have options:
  1. use the library instead, and just configure from different places 
  2. support jitter in the simple implementation too
  3. change the semantic of the backoff, which could be done in v1beta2
  
  I'm basically ok with all, but maybe it is safer to decouple (1.) or (2.) from (3.).
  
  My slight preference is actually for (1.). This "library" is the code k8s code, so pretty well tested. I'm not concerned about that. 
  
  wdyt @gabesaba ?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-10-09T11:34:08Z

- **pkg/cache/queue/second_pass_queue.go:1** by [@gabesaba](https://github.com/gabesaba) — 2025-10-09T11:34:08Z
  I'm fine with 1. When I wrote https://github.com/kubernetes-sigs/kueue/pull/7147/files#r2401382531, I was hoping we could reuse the existing code in backoff.go, but if that isn't possible and we need to write new code, I agree with @mimowo about reusing the well tested library for simplicity

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-10-09T11:35:34Z

- **pkg/cache/queue/second_pass_queue.go:1** by [@mykysha](https://github.com/mykysha) — 2025-10-09T11:35:33Z
  Will update the PR shortly

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-10-09T13:11:03Z

- **pkg/cache/queue/second_pass_queue.go:1** by [@mykysha](https://github.com/mykysha) — 2025-10-09T13:11:02Z
  Brought the lib back for the `Backoff` implementation, and also updated the existing `UntilWithBackoff`  to use the library so that they would be aligned and not one using k8s lib and another running off of a hand-written backoff calculator.
  @mimowo @gabesaba

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-09T14:19:09Z

- **pkg/util/wait/backoff.go:55** by [@mimowo](https://github.com/mimowo) — 2025-10-09T14:19:09Z
```diff
@@ -18,12 +18,53 @@ package wait
 
 import (
 	"context"
+	"math"
 	"time"
 
 	"k8s.io/apimachinery/pkg/util/wait"
 	"k8s.io/utils/clock"
 )
 
+type Backoff struct {
+	// Min is the initial backoff duration.
+	Min time.Duration
+
+	// Factor is the multiplication factor for each iteration.
+	Factor float64
+
+	// Jitter is the jitter factor applied to each backoff duration.
+	Jitter float64
+
+	// Max is the maximum backoff duration. If no value is provided, it defaults
+	// to math.MaxInt64 / Factor.
+	Max time.Duration
+}
+
+// WaitTime returns the backoff duration for the given iteration.
+func (b Backoff) WaitTime(iteration int) time.Duration {
+	backoff := &wait.Backoff{
+		Duration: b.Min,
+		Factor:   b.Factor,
+		Jitter:   b.Jitter,
+		Steps:    iteration,
+		Cap:      b.Max,
+	}
+
+	if backoff.Cap == 0 {
+		backoff.Cap = time.Duration(math.MaxInt64 / math.Ceil(backoff.Factor))
+	}
```

  can we inline into the wait.Backoff initialization using cmp.Or?

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-10-09T14:25:11Z

- **pkg/util/wait/backoff.go:55** by [@mykysha](https://github.com/mykysha) — 2025-10-09T14:25:11Z
```diff
@@ -18,12 +18,53 @@ package wait
 
 import (
 	"context"
+	"math"
 	"time"
 
 	"k8s.io/apimachinery/pkg/util/wait"
 	"k8s.io/utils/clock"
 )
 
+type Backoff struct {
+	// Min is the initial backoff duration.
+	Min time.Duration
+
+	// Factor is the multiplication factor for each iteration.
+	Factor float64
+
+	// Jitter is the jitter factor applied to each backoff duration.
+	Jitter float64
+
+	// Max is the maximum backoff duration. If no value is provided, it defaults
+	// to math.MaxInt64 / Factor.
+	Max time.Duration
+}
+
+// WaitTime returns the backoff duration for the given iteration.
+func (b Backoff) WaitTime(iteration int) time.Duration {
+	backoff := &wait.Backoff{
+		Duration: b.Min,
+		Factor:   b.Factor,
+		Jitter:   b.Jitter,
+		Steps:    iteration,
+		Cap:      b.Max,
+	}
+
+	if backoff.Cap == 0 {
+		backoff.Cap = time.Duration(math.MaxInt64 / math.Ceil(backoff.Factor))
+	}
```

  Done

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-10-10T07:56:38Z

- **pkg/util/wait/backoff.go:57** by [@mszadkow](https://github.com/mszadkow) — 2025-10-10T07:56:37Z
```diff
@@ -17,13 +17,51 @@ limitations under the License.
 package wait
 
 import (
+	"cmp"
 	"context"
+	"math"
 	"time"
 
 	"k8s.io/apimachinery/pkg/util/wait"
 	"k8s.io/utils/clock"
 )
 
+type Backoff struct {
+	// Min is the initial backoff duration.
+	Min time.Duration
+
+	// Factor is the multiplication factor for each iteration.
+	Factor float64
+
+	// Jitter is the jitter factor applied to each backoff duration.
+	Jitter float64
+
+	// Max is the maximum backoff duration. If no value is provided, it defaults
+	// to math.MaxInt64 / Factor.
+	Max time.Duration
+}
+
+// WaitTime returns the backoff duration for the given iteration.
+func (b Backoff) WaitTime(iteration int) time.Duration {
+	backoff := &wait.Backoff{
+		Duration: b.Min,
+		Factor:   b.Factor,
+		Jitter:   b.Jitter,
+		Steps:    iteration,
+		Cap:      cmp.Or(b.Max, time.Duration(math.MaxInt64/math.Ceil(b.Factor))),
+	}
+
+	var duration time.Duration
+	for range iteration {
+		duration = backoff.Step()
+		if duration >= backoff.Cap {
```

  maybe just for clarity before break
  `duration = backoff.Cap`
  I mean you have UT for the cap, so this should be ok, so treat it as nit

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-10-10T07:59:41Z

- **pkg/util/wait/backoff.go:57** by [@mykysha](https://github.com/mykysha) — 2025-10-10T07:59:41Z
```diff
@@ -17,13 +17,51 @@ limitations under the License.
 package wait
 
 import (
+	"cmp"
 	"context"
+	"math"
 	"time"
 
 	"k8s.io/apimachinery/pkg/util/wait"
 	"k8s.io/utils/clock"
 )
 
+type Backoff struct {
+	// Min is the initial backoff duration.
+	Min time.Duration
+
+	// Factor is the multiplication factor for each iteration.
+	Factor float64
+
+	// Jitter is the jitter factor applied to each backoff duration.
+	Jitter float64
+
+	// Max is the maximum backoff duration. If no value is provided, it defaults
+	// to math.MaxInt64 / Factor.
+	Max time.Duration
+}
+
+// WaitTime returns the backoff duration for the given iteration.
+func (b Backoff) WaitTime(iteration int) time.Duration {
+	backoff := &wait.Backoff{
+		Duration: b.Min,
+		Factor:   b.Factor,
+		Jitter:   b.Jitter,
+		Steps:    iteration,
+		Cap:      cmp.Or(b.Max, time.Duration(math.MaxInt64/math.Ceil(b.Factor))),
+	}
+
+	var duration time.Duration
+	for range iteration {
+		duration = backoff.Step()
+		if duration >= backoff.Cap {
```

  The library should handle the assignment, but will change to `==` here to avoid confusion

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-10T08:43:33Z

- **pkg/util/wait/backoff.go:52** by [@mimowo](https://github.com/mimowo) — 2025-10-10T08:43:33Z
```diff
@@ -17,13 +17,51 @@ limitations under the License.
 package wait
 
 import (
+	"cmp"
 	"context"
+	"math"
 	"time"
 
 	"k8s.io/apimachinery/pkg/util/wait"
 	"k8s.io/utils/clock"
 )
 
+type Backoff struct {
+	// Min is the initial backoff duration.
+	Min time.Duration
+
+	// Factor is the multiplication factor for each iteration.
+	Factor float64
+
+	// Jitter is the jitter factor applied to each backoff duration.
+	Jitter float64
+
+	// Max is the maximum backoff duration. If no value is provided, it defaults
+	// to math.MaxInt64 / Factor.
+	Max time.Duration
+}
+
+// WaitTime returns the backoff duration for the given iteration.
+func (b Backoff) WaitTime(iteration int) time.Duration {
+	backoff := &wait.Backoff{
+		Duration: b.Min,
+		Factor:   b.Factor,
+		Jitter:   b.Jitter,
+		Steps:    iteration,
+		Cap:      cmp.Or(b.Max, time.Duration(math.MaxInt64/math.Ceil(b.Factor))),
+	}
```

  Instead of instantiating that on each "WaitTime" call, can we keep it on a field. Then, instead of exposing the values like Max, Jitter, Min, I would suggest not exposing anything, just the WaitTime function.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-10T08:44:29Z

- **pkg/util/wait/backoff.go:57** by [@mimowo](https://github.com/mimowo) — 2025-10-10T08:44:29Z
```diff
@@ -17,13 +17,51 @@ limitations under the License.
 package wait
 
 import (
+	"cmp"
 	"context"
+	"math"
 	"time"
 
 	"k8s.io/apimachinery/pkg/util/wait"
 	"k8s.io/utils/clock"
 )
 
+type Backoff struct {
+	// Min is the initial backoff duration.
+	Min time.Duration
+
+	// Factor is the multiplication factor for each iteration.
+	Factor float64
+
+	// Jitter is the jitter factor applied to each backoff duration.
+	Jitter float64
+
+	// Max is the maximum backoff duration. If no value is provided, it defaults
+	// to math.MaxInt64 / Factor.
+	Max time.Duration
+}
+
+// WaitTime returns the backoff duration for the given iteration.
+func (b Backoff) WaitTime(iteration int) time.Duration {
+	backoff := &wait.Backoff{
+		Duration: b.Min,
+		Factor:   b.Factor,
+		Jitter:   b.Jitter,
+		Steps:    iteration,
+		Cap:      cmp.Or(b.Max, time.Duration(math.MaxInt64/math.Ceil(b.Factor))),
+	}
+
+	var duration time.Duration
+	for range iteration {
+		duration = backoff.Step()
+		if duration == backoff.Cap {
```

  I'm curious about these two lines, doesn't the apimachinery already have a built in function which would return the waitTime depending on the current iteration?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-10T08:46:19Z

- **pkg/util/wait/backoff.go:57** by [@mimowo](https://github.com/mimowo) — 2025-10-10T08:46:19Z
```diff
@@ -17,13 +17,51 @@ limitations under the License.
 package wait
 
 import (
+	"cmp"
 	"context"
+	"math"
 	"time"
 
 	"k8s.io/apimachinery/pkg/util/wait"
 	"k8s.io/utils/clock"
 )
 
+type Backoff struct {
+	// Min is the initial backoff duration.
+	Min time.Duration
+
+	// Factor is the multiplication factor for each iteration.
+	Factor float64
+
+	// Jitter is the jitter factor applied to each backoff duration.
+	Jitter float64
+
+	// Max is the maximum backoff duration. If no value is provided, it defaults
+	// to math.MaxInt64 / Factor.
+	Max time.Duration
+}
+
+// WaitTime returns the backoff duration for the given iteration.
+func (b Backoff) WaitTime(iteration int) time.Duration {
+	backoff := &wait.Backoff{
+		Duration: b.Min,
+		Factor:   b.Factor,
+		Jitter:   b.Jitter,
+		Steps:    iteration,
+		Cap:      cmp.Or(b.Max, time.Duration(math.MaxInt64/math.Ceil(b.Factor))),
+	}
+
+	var duration time.Duration
+	for range iteration {
+		duration = backoff.Step()
+		if duration == backoff.Cap {
```

  Also, it feels safer to do `if duration >= backoff.Cap {` to be absolutely sure. Otherwise if using `==` please provide some commentary to the code with links to the apimachinery code which demonstrates that is not needed.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-10-10T08:46:32Z

- **pkg/util/wait/backoff.go:52** by [@mykysha](https://github.com/mykysha) — 2025-10-10T08:46:32Z
```diff
@@ -17,13 +17,51 @@ limitations under the License.
 package wait
 
 import (
+	"cmp"
 	"context"
+	"math"
 	"time"
 
 	"k8s.io/apimachinery/pkg/util/wait"
 	"k8s.io/utils/clock"
 )
 
+type Backoff struct {
+	// Min is the initial backoff duration.
+	Min time.Duration
+
+	// Factor is the multiplication factor for each iteration.
+	Factor float64
+
+	// Jitter is the jitter factor applied to each backoff duration.
+	Jitter float64
+
+	// Max is the maximum backoff duration. If no value is provided, it defaults
+	// to math.MaxInt64 / Factor.
+	Max time.Duration
+}
+
+// WaitTime returns the backoff duration for the given iteration.
+func (b Backoff) WaitTime(iteration int) time.Duration {
+	backoff := &wait.Backoff{
+		Duration: b.Min,
+		Factor:   b.Factor,
+		Jitter:   b.Jitter,
+		Steps:    iteration,
+		Cap:      cmp.Or(b.Max, time.Duration(math.MaxInt64/math.Ceil(b.Factor))),
+	}
```

  Given the way the library works, it does not reset the wait.Backoff between calls, so previous Step iterations stay, and it breaks the code. So we would need a way to clean the wait.Backoff between runs, or to recreate it

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-10-10T08:54:22Z

- **pkg/util/wait/backoff.go:57** by [@mykysha](https://github.com/mykysha) — 2025-10-10T08:54:22Z
```diff
@@ -17,13 +17,51 @@ limitations under the License.
 package wait
 
 import (
+	"cmp"
 	"context"
+	"math"
 	"time"
 
 	"k8s.io/apimachinery/pkg/util/wait"
 	"k8s.io/utils/clock"
 )
 
+type Backoff struct {
+	// Min is the initial backoff duration.
+	Min time.Duration
+
+	// Factor is the multiplication factor for each iteration.
+	Factor float64
+
+	// Jitter is the jitter factor applied to each backoff duration.
+	Jitter float64
+
+	// Max is the maximum backoff duration. If no value is provided, it defaults
+	// to math.MaxInt64 / Factor.
+	Max time.Duration
+}
+
+// WaitTime returns the backoff duration for the given iteration.
+func (b Backoff) WaitTime(iteration int) time.Duration {
+	backoff := &wait.Backoff{
+		Duration: b.Min,
+		Factor:   b.Factor,
+		Jitter:   b.Jitter,
+		Steps:    iteration,
+		Cap:      cmp.Or(b.Max, time.Duration(math.MaxInt64/math.Ceil(b.Factor))),
+	}
+
+	var duration time.Duration
+	for range iteration {
+		duration = backoff.Step()
+		if duration == backoff.Cap {
```

  As for the function to calculate wait time per case, I have not been able to find it inside the `"k8s.io/apimachinery/pkg/util/wait"`
  
  And for the check, `backoff.Step()` should cap by itself, so can add the comment explaining that. And this check is not specifically needed as `Step()` will still return cap indefinitely, it is just a shortcircuit not to call any extra `Step()`'s than needed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-10T10:42:24Z

- **pkg/util/wait/backoff.go:52** by [@mimowo](https://github.com/mimowo) — 2025-10-10T10:42:24Z
```diff
@@ -17,13 +17,51 @@ limitations under the License.
 package wait
 
 import (
+	"cmp"
 	"context"
+	"math"
 	"time"
 
 	"k8s.io/apimachinery/pkg/util/wait"
 	"k8s.io/utils/clock"
 )
 
+type Backoff struct {
+	// Min is the initial backoff duration.
+	Min time.Duration
+
+	// Factor is the multiplication factor for each iteration.
+	Factor float64
+
+	// Jitter is the jitter factor applied to each backoff duration.
+	Jitter float64
+
+	// Max is the maximum backoff duration. If no value is provided, it defaults
+	// to math.MaxInt64 / Factor.
+	Max time.Duration
+}
+
+// WaitTime returns the backoff duration for the given iteration.
+func (b Backoff) WaitTime(iteration int) time.Duration {
+	backoff := &wait.Backoff{
+		Duration: b.Min,
+		Factor:   b.Factor,
+		Jitter:   b.Jitter,
+		Steps:    iteration,
+		Cap:      cmp.Or(b.Max, time.Duration(math.MaxInt64/math.Ceil(b.Factor))),
+	}
```

  Ah, ok. but if wait.Backoff has internal counter for "iteration", could we rely on it, and not use our "iteration" stored outside? It seems like two layers keep the interation counter currently

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-10T10:42:41Z

- **pkg/util/wait/backoff.go:57** by [@mimowo](https://github.com/mimowo) — 2025-10-10T10:42:41Z
```diff
@@ -17,13 +17,51 @@ limitations under the License.
 package wait
 
 import (
+	"cmp"
 	"context"
+	"math"
 	"time"
 
 	"k8s.io/apimachinery/pkg/util/wait"
 	"k8s.io/utils/clock"
 )
 
+type Backoff struct {
+	// Min is the initial backoff duration.
+	Min time.Duration
+
+	// Factor is the multiplication factor for each iteration.
+	Factor float64
+
+	// Jitter is the jitter factor applied to each backoff duration.
+	Jitter float64
+
+	// Max is the maximum backoff duration. If no value is provided, it defaults
+	// to math.MaxInt64 / Factor.
+	Max time.Duration
+}
+
+// WaitTime returns the backoff duration for the given iteration.
+func (b Backoff) WaitTime(iteration int) time.Duration {
+	backoff := &wait.Backoff{
+		Duration: b.Min,
+		Factor:   b.Factor,
+		Jitter:   b.Jitter,
+		Steps:    iteration,
+		Cap:      cmp.Or(b.Max, time.Duration(math.MaxInt64/math.Ceil(b.Factor))),
+	}
+
+	var duration time.Duration
+	for range iteration {
+		duration = backoff.Step()
+		if duration == backoff.Cap {
```

  sgtm, so just add a small commentary, because this is not obvious

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-10-10T10:57:58Z

- **pkg/util/wait/backoff.go:52** by [@mykysha](https://github.com/mykysha) — 2025-10-10T10:57:58Z
```diff
@@ -17,13 +17,51 @@ limitations under the License.
 package wait
 
 import (
+	"cmp"
 	"context"
+	"math"
 	"time"
 
 	"k8s.io/apimachinery/pkg/util/wait"
 	"k8s.io/utils/clock"
 )
 
+type Backoff struct {
+	// Min is the initial backoff duration.
+	Min time.Duration
+
+	// Factor is the multiplication factor for each iteration.
+	Factor float64
+
+	// Jitter is the jitter factor applied to each backoff duration.
+	Jitter float64
+
+	// Max is the maximum backoff duration. If no value is provided, it defaults
+	// to math.MaxInt64 / Factor.
+	Max time.Duration
+}
+
+// WaitTime returns the backoff duration for the given iteration.
+func (b Backoff) WaitTime(iteration int) time.Duration {
+	backoff := &wait.Backoff{
+		Duration: b.Min,
+		Factor:   b.Factor,
+		Jitter:   b.Jitter,
+		Steps:    iteration,
+		Cap:      cmp.Or(b.Max, time.Duration(math.MaxInt64/math.Ceil(b.Factor))),
+	}
```

  I can add something like that, for example in the case of the second pass scheduler we can save backoff in the `workload.Info`, and then just call something like `NextWaitTime()` or even the same `Step()`
  
  However in the workloads `UpdateRequeueState` which calculates requeueAt time it takes current iteration from the `wl.Status.RequeueState.Count`, and there we would either need to `Step()` in a loop, or have both `Step()` and the current implementation which would take iteration.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-10T13:23:35Z

- **pkg/util/wait/backoff.go:52** by [@mimowo](https://github.com/mimowo) — 2025-10-10T13:23:35Z
```diff
@@ -17,13 +17,51 @@ limitations under the License.
 package wait
 
 import (
+	"cmp"
 	"context"
+	"math"
 	"time"
 
 	"k8s.io/apimachinery/pkg/util/wait"
 	"k8s.io/utils/clock"
 )
 
+type Backoff struct {
+	// Min is the initial backoff duration.
+	Min time.Duration
+
+	// Factor is the multiplication factor for each iteration.
+	Factor float64
+
+	// Jitter is the jitter factor applied to each backoff duration.
+	Jitter float64
+
+	// Max is the maximum backoff duration. If no value is provided, it defaults
+	// to math.MaxInt64 / Factor.
+	Max time.Duration
+}
+
+// WaitTime returns the backoff duration for the given iteration.
+func (b Backoff) WaitTime(iteration int) time.Duration {
+	backoff := &wait.Backoff{
+		Duration: b.Min,
+		Factor:   b.Factor,
+		Jitter:   b.Jitter,
+		Steps:    iteration,
+		Cap:      cmp.Or(b.Max, time.Duration(math.MaxInt64/math.Ceil(b.Factor))),
+	}
```

  Ok, I'm ok to keep as is for simplicity for now.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-10-10T13:28:51Z

- **pkg/util/wait/backoff.go:57** by [@mykysha](https://github.com/mykysha) — 2025-10-10T13:28:51Z
```diff
@@ -17,13 +17,51 @@ limitations under the License.
 package wait
 
 import (
+	"cmp"
 	"context"
+	"math"
 	"time"
 
 	"k8s.io/apimachinery/pkg/util/wait"
 	"k8s.io/utils/clock"
 )
 
+type Backoff struct {
+	// Min is the initial backoff duration.
+	Min time.Duration
+
+	// Factor is the multiplication factor for each iteration.
+	Factor float64
+
+	// Jitter is the jitter factor applied to each backoff duration.
+	Jitter float64
+
+	// Max is the maximum backoff duration. If no value is provided, it defaults
+	// to math.MaxInt64 / Factor.
+	Max time.Duration
+}
+
+// WaitTime returns the backoff duration for the given iteration.
+func (b Backoff) WaitTime(iteration int) time.Duration {
+	backoff := &wait.Backoff{
+		Duration: b.Min,
+		Factor:   b.Factor,
+		Jitter:   b.Jitter,
+		Steps:    iteration,
+		Cap:      cmp.Or(b.Max, time.Duration(math.MaxInt64/math.Ceil(b.Factor))),
+	}
+
+	var duration time.Duration
+	for range iteration {
+		duration = backoff.Step()
+		if duration == backoff.Cap {
```

  added a short comment

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-10T13:39:18Z

- **pkg/util/wait/backoff.go:52** by [@mimowo](https://github.com/mimowo) — 2025-10-10T13:39:18Z
```diff
@@ -17,13 +17,51 @@ limitations under the License.
 package wait
 
 import (
+	"cmp"
 	"context"
+	"math"
 	"time"
 
 	"k8s.io/apimachinery/pkg/util/wait"
 	"k8s.io/utils/clock"
 )
 
+type Backoff struct {
+	// Min is the initial backoff duration.
+	Min time.Duration
+
+	// Factor is the multiplication factor for each iteration.
+	Factor float64
+
+	// Jitter is the jitter factor applied to each backoff duration.
+	Jitter float64
+
+	// Max is the maximum backoff duration. If no value is provided, it defaults
+	// to math.MaxInt64 / Factor.
+	Max time.Duration
+}
+
+// WaitTime returns the backoff duration for the given iteration.
+func (b Backoff) WaitTime(iteration int) time.Duration {
+	backoff := &wait.Backoff{
+		Duration: b.Min,
+		Factor:   b.Factor,
+		Jitter:   b.Jitter,
+		Steps:    iteration,
+		Cap:      cmp.Or(b.Max, time.Duration(math.MaxInt64/math.Ceil(b.Factor))),
+	}
```

  Actually, I think what is missing then is the "Reset" option on the wait.Backoff, because it internally updates the "initial" duration after each step here: https://github.com/kubernetes/apimachinery/blob/ff7ddf5d4ebcba6a72e612e6d21be431eb53b11b/pkg/util/wait/backoff.go#L63
  
  However, as you say what we need sometimes is the "Reset" option when `iteration=0`, so to facilitate that we could keep:
  
  ```golang
  type Backoff struct{
  	// Initial Delay 
  	initialDelay time.Duration
  
      backoff wait.Backoff // here are kept
  }
  Then we have following functions:
  
  func NewBackoff(options) Backoff {
     // initializing code
  }
  
  func WaitTime(iteration int) time.Duration {
    if iteration == 0 {
      backoff.Duration = inialDuration
      backoff.Steps = iteration
    }
     // the code as belo
  }
  ```
  Then we could avoid instanting a new backoff on every WaitTime. This is no differnece for WaitForPodsReady and second pass queue, but for scheduler backoff it is better to avoid unnecessary allocations.
  
  PTAL if this makes sense

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-13T07:21:24Z

- **pkg/util/wait/backoff.go:41** by [@mimowo](https://github.com/mimowo) — 2025-10-13T07:21:25Z
```diff
@@ -17,13 +17,51 @@ limitations under the License.
 package wait
 
 import (
+	"cmp"
 	"context"
+	"math"
 	"time"
 
 	"k8s.io/apimachinery/pkg/util/wait"
 	"k8s.io/utils/clock"
 )
 
+type Backoff struct {
+	// Min is the initial backoff duration.
+	Min time.Duration
+
+	// Factor is the multiplication factor for each iteration.
+	Factor float64
+
+	// Jitter is the jitter factor applied to each backoff duration.
+	Jitter float64
+
+	// Max is the maximum backoff duration. If no value is provided, it defaults
+	// to math.MaxInt64 / Factor.
+	Max time.Duration
```

  I would like to make the internal state of our backoff not exported, just the functions to facilitate refactoring, possibly as mentioned in https://github.com/kubernetes-sigs/kueue/pull/7147/files#r2420247951

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-10-14T11:49:57Z

- **pkg/util/wait/backoff.go:52** by [@mykysha](https://github.com/mykysha) — 2025-10-14T11:49:57Z
```diff
@@ -17,13 +17,51 @@ limitations under the License.
 package wait
 
 import (
+	"cmp"
 	"context"
+	"math"
 	"time"
 
 	"k8s.io/apimachinery/pkg/util/wait"
 	"k8s.io/utils/clock"
 )
 
+type Backoff struct {
+	// Min is the initial backoff duration.
+	Min time.Duration
+
+	// Factor is the multiplication factor for each iteration.
+	Factor float64
+
+	// Jitter is the jitter factor applied to each backoff duration.
+	Jitter float64
+
+	// Max is the maximum backoff duration. If no value is provided, it defaults
+	// to math.MaxInt64 / Factor.
+	Max time.Duration
+}
+
+// WaitTime returns the backoff duration for the given iteration.
+func (b Backoff) WaitTime(iteration int) time.Duration {
+	backoff := &wait.Backoff{
+		Duration: b.Min,
+		Factor:   b.Factor,
+		Jitter:   b.Jitter,
+		Steps:    iteration,
+		Cap:      cmp.Or(b.Max, time.Duration(math.MaxInt64/math.Ceil(b.Factor))),
+	}
```

  This way we would need to have a backoff on the per-backed-off-object instance, and `(iteration int)` would be redundant as `WaitTime(3)` followed by `WaitTime(4)` would return value that corresponds to what would be expected off of the `WaitTime(7)`.
  
  I've updated the backoff with another approach, which uses wait.Backoff as a value, hence all the internal changes are lost after the call meaning no need for resetting. And added a test for the consecutive backoff calculations to ensure that the WaitTime is not reliant on the previous calls. Let me know whether this approach works for you and solves the problem

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T12:09:20Z

- **pkg/util/wait/backoff.go:118** by [@mimowo](https://github.com/mimowo) — 2025-10-14T12:09:20Z
```diff
@@ -54,34 +87,34 @@ const (
 	// SlowDown signals to backoff.
 	SlowDown SpeedSignal = false
 
-	noBackoff      = time.Millisecond * 0
-	initialBackoff = time.Millisecond * 1
+	initialBackoff = time.Millisecond
 	maxBackoff     = time.Millisecond * 100
 )
 
 func (s *speedyBackoffManager) toggleBackoff(speedSignal SpeedSignal) {
 	switch speedSignal {
 	case KeepGoing:
-		s.backoff = noBackoff
+		s.backoff = nil
 	case SlowDown:
-		if s.backoff == noBackoff {
-			s.backoff = initialBackoff
+		if s.backoff == nil {
+			s.backoff = &wait.Backoff{
+				Duration: initialBackoff,
+				Factor:   2,
+				Steps:    math.MaxInt,
+				Cap:      maxBackoff,
+			}
 		}
 	}
 }
 
 type speedyBackoffManager struct {
-	backoff time.Duration
+	backoff *wait.Backoff
 	timer   clock.Timer
 }
 
 var _ wait.BackoffManager = (*speedyBackoffManager)(nil)
 
 func (s *speedyBackoffManager) Backoff() clock.Timer {
-	s.timer.Reset(s.backoff)
-	s.backoff *= 2
-	if s.backoff > maxBackoff {
-		s.backoff = maxBackoff
-	}
+	s.timer.Reset(s.backoff.Step())
```

  should the Step happen after the Reset() to match the previous logic?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T12:11:04Z

- **pkg/util/wait/backoff.go:52** by [@mimowo](https://github.com/mimowo) — 2025-10-14T12:11:03Z
```diff
@@ -17,13 +17,51 @@ limitations under the License.
 package wait
 
 import (
+	"cmp"
 	"context"
+	"math"
 	"time"
 
 	"k8s.io/apimachinery/pkg/util/wait"
 	"k8s.io/utils/clock"
 )
 
+type Backoff struct {
+	// Min is the initial backoff duration.
+	Min time.Duration
+
+	// Factor is the multiplication factor for each iteration.
+	Factor float64
+
+	// Jitter is the jitter factor applied to each backoff duration.
+	Jitter float64
+
+	// Max is the maximum backoff duration. If no value is provided, it defaults
+	// to math.MaxInt64 / Factor.
+	Max time.Duration
+}
+
+// WaitTime returns the backoff duration for the given iteration.
+func (b Backoff) WaitTime(iteration int) time.Duration {
+	backoff := &wait.Backoff{
+		Duration: b.Min,
+		Factor:   b.Factor,
+		Jitter:   b.Jitter,
+		Steps:    iteration,
+		Cap:      cmp.Or(b.Max, time.Duration(math.MaxInt64/math.Ceil(b.Factor))),
+	}
```

  Thank you, this looks great, I just left another small comment / question.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T12:11:16Z

- **pkg/util/wait/backoff.go:41** by [@mimowo](https://github.com/mimowo) — 2025-10-14T12:11:16Z
```diff
@@ -17,13 +17,51 @@ limitations under the License.
 package wait
 
 import (
+	"cmp"
 	"context"
+	"math"
 	"time"
 
 	"k8s.io/apimachinery/pkg/util/wait"
 	"k8s.io/utils/clock"
 )
 
+type Backoff struct {
+	// Min is the initial backoff duration.
+	Min time.Duration
+
+	// Factor is the multiplication factor for each iteration.
+	Factor float64
+
+	// Jitter is the jitter factor applied to each backoff duration.
+	Jitter float64
+
+	// Max is the maximum backoff duration. If no value is provided, it defaults
+	// to math.MaxInt64 / Factor.
+	Max time.Duration
```

  Thank you for dropping this.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-10-14T12:15:15Z

- **pkg/util/wait/backoff.go:118** by [@mykysha](https://github.com/mykysha) — 2025-10-14T12:15:15Z
```diff
@@ -54,34 +87,34 @@ const (
 	// SlowDown signals to backoff.
 	SlowDown SpeedSignal = false
 
-	noBackoff      = time.Millisecond * 0
-	initialBackoff = time.Millisecond * 1
+	initialBackoff = time.Millisecond
 	maxBackoff     = time.Millisecond * 100
 )
 
 func (s *speedyBackoffManager) toggleBackoff(speedSignal SpeedSignal) {
 	switch speedSignal {
 	case KeepGoing:
-		s.backoff = noBackoff
+		s.backoff = nil
 	case SlowDown:
-		if s.backoff == noBackoff {
-			s.backoff = initialBackoff
+		if s.backoff == nil {
+			s.backoff = &wait.Backoff{
+				Duration: initialBackoff,
+				Factor:   2,
+				Steps:    math.MaxInt,
+				Cap:      maxBackoff,
+			}
 		}
 	}
 }
 
 type speedyBackoffManager struct {
-	backoff time.Duration
+	backoff *wait.Backoff
 	timer   clock.Timer
 }
 
 var _ wait.BackoffManager = (*speedyBackoffManager)(nil)
 
 func (s *speedyBackoffManager) Backoff() clock.Timer {
-	s.timer.Reset(s.backoff)
-	s.backoff *= 2
-	if s.backoff > maxBackoff {
-		s.backoff = maxBackoff
-	}
+	s.timer.Reset(s.backoff.Step())
```

  Previously the s.backoff value was set to 1ms, and was updated after the call. Now the first call to Step returns 1ms and increases thereafter, so I think the logic is the same.
  Also the unit tests for the UntilWithBackoff are made in a way that test expected values between the calls, and the new approach passes those tests without tweaks

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T12:17:27Z

- **pkg/util/wait/backoff.go:118** by [@mimowo](https://github.com/mimowo) — 2025-10-14T12:17:26Z
```diff
@@ -54,34 +87,34 @@ const (
 	// SlowDown signals to backoff.
 	SlowDown SpeedSignal = false
 
-	noBackoff      = time.Millisecond * 0
-	initialBackoff = time.Millisecond * 1
+	initialBackoff = time.Millisecond
 	maxBackoff     = time.Millisecond * 100
 )
 
 func (s *speedyBackoffManager) toggleBackoff(speedSignal SpeedSignal) {
 	switch speedSignal {
 	case KeepGoing:
-		s.backoff = noBackoff
+		s.backoff = nil
 	case SlowDown:
-		if s.backoff == noBackoff {
-			s.backoff = initialBackoff
+		if s.backoff == nil {
+			s.backoff = &wait.Backoff{
+				Duration: initialBackoff,
+				Factor:   2,
+				Steps:    math.MaxInt,
+				Cap:      maxBackoff,
+			}
 		}
 	}
 }
 
 type speedyBackoffManager struct {
-	backoff time.Duration
+	backoff *wait.Backoff
 	timer   clock.Timer
 }
 
 var _ wait.BackoffManager = (*speedyBackoffManager)(nil)
 
 func (s *speedyBackoffManager) Backoff() clock.Timer {
-	s.timer.Reset(s.backoff)
-	s.backoff *= 2
-	if s.backoff > maxBackoff {
-		s.backoff = maxBackoff
-	}
+	s.timer.Reset(s.backoff.Step())
```

  Thanks for explaining, this makes sense
