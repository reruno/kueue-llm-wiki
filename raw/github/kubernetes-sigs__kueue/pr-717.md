# PR #717: Admission checking refactorization

**Summary**: Admission checking refactorization

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/717

**Last updated**: 2023-10-24T06:41:03Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mcariatm](https://github.com/mcariatm)
- **Branch**: `admitted_one_way` → `main`
- **Created**: 2023-04-24T14:23:29Z
- **Updated**: 2023-10-24T06:41:03Z
- **Merged**: 2023-04-26T19:18:14Z
- **Closed**: 2023-04-26T19:18:14Z
- **Labels**: `size/L`, `lgtm`, `approved`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 23   **+144 / -129**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

#### What this PR does / why we need it:
Now are used 2 different ways to check if workload is admitted:
wl.Status.Admission == nil
and
apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadAdmitted)
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #694

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-04-24T14:23:39Z

Hi @mcariatm. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-04-24T14:23:49Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 5706de34a4c9b55fc5e11756d1b0c424dd5d511b |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/644919c31d2b1c000868748e |

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-24T14:46:22Z

/ok-to-test

### Comment by [@mcariatm](https://github.com/mcariatm) — 2023-04-24T14:47:31Z

/remove-kind api-change
/remove-kind deprecation
/remove-kind failing-test
/remove-kind flake
/remove-kind regression

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-04-26T19:05:18Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/717#pullrequestreview-1402626205" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/717#" title="Author self-approved">mcariatm</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-04-24T15:51:41Z

- **pkg/workload/workload.go:303** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-24T15:22:04Z
```diff
@@ -292,5 +293,14 @@ func BaseSSAWorkload(w *kueue.Workload) *kueue.Workload {
 func AdmissionPatch(w *kueue.Workload) *kueue.Workload {
 	wlCopy := BaseSSAWorkload(w)
 	wlCopy.Status.Admission = w.Status.Admission.DeepCopy()
+	if admittedCondition := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadAdmitted); admittedCondition != nil {
+		wlCopy.Status.Conditions = []metav1.Condition{*admittedCondition}
+	}
+
 	return wlCopy
 }
+
+// IsWorkloadAdmitted check if workload is admitted based on conditions
```

  ```suggestion
  // IsWorkloadAdmitted checks if workload is admitted based on conditions
  ```

- **pkg/util/testing/wrappers.go:161** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-24T15:37:10Z
```diff
@@ -143,6 +151,15 @@ func (w *WorkloadWrapper) Condition(condition metav1.Condition) *WorkloadWrapper
 	return w
 }
 
+func (w *WorkloadWrapper) SetOrReplaceCondition(condition metav1.Condition) *WorkloadWrapper {
+	existingCondition := apimeta.FindStatusCondition(w.Status.Conditions, condition.Type)
+	if existingCondition != nil {
+		apimeta.RemoveStatusCondition(&w.Status.Conditions, condition.Type)
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, condition)
+	return w
+}
```

  Probably, we don't need this wrapper since the `apimeta.SetStatusCondition` checks whether the conditions already have the condition.
  
  https://github.com/kubernetes/apimachinery/blob/61b874e0403b27353daf90f9789c762610731ab6/pkg/api/meta/conditions.go#L25-L55

- **test/util/util.go:335** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-24T15:46:05Z
```diff
@@ -297,12 +315,47 @@ func AdmitWorkload(ctx context.Context, k8sClient client.Client, wl *kueue.Workl
 		return fmt.Errorf("admission can't be null")
 	}
 	wl.Status.Admission = admission
-	wl.Status.Conditions = []metav1.Condition{{
-		Type:               kueue.WorkloadAdmitted,
-		Status:             metav1.ConditionTrue,
-		LastTransitionTime: metav1.Now(),
-		Reason:             "AdmittedByTest",
-		Message:            fmt.Sprintf("Admitted by ClusterQueue %s", wl.Status.Admission.ClusterQueue),
-	}}
+	i := workload.FindConditionIndex(&wl.Status, kueue.WorkloadAdmitted)
+	if i == -1 {
+		wl.Status.Conditions = append(wl.Status.Conditions, metav1.Condition{
+			Type:               kueue.WorkloadAdmitted,
+			Status:             metav1.ConditionTrue,
+			LastTransitionTime: metav1.Now(),
+			Reason:             "AdmittedByTest",
+			Message:            fmt.Sprintf("Admitted by ClusterQueue %s", wl.Status.Admission.ClusterQueue),
+		})
+	} else {
+		wl.Status.Conditions[i] = metav1.Condition{
+			Type:               kueue.WorkloadAdmitted,
+			Status:             metav1.ConditionTrue,
+			LastTransitionTime: metav1.Now(),
+			Reason:             "AdmittedByTest",
+			Message:            fmt.Sprintf("Admitted by ClusterQueue %s", wl.Status.Admission.ClusterQueue),
+		}
+	}
```

  Probably, using the `apimeta.SetStatusCondition` is enough since the function checks whether the conditions already have the condition.
  
  https://github.com/kubernetes/apimachinery/blob/61b874e0403b27353daf90f9789c762610731ab6/pkg/api/meta/conditions.go#L25-L55

- **test/util/util.go:303** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-24T15:46:44Z
```diff
@@ -297,12 +315,47 @@ func AdmitWorkload(ctx context.Context, k8sClient client.Client, wl *kueue.Workl
 		return fmt.Errorf("admission can't be null")
 	}
 	wl.Status.Admission = admission
-	wl.Status.Conditions = []metav1.Condition{{
-		Type:               kueue.WorkloadAdmitted,
-		Status:             metav1.ConditionTrue,
-		LastTransitionTime: metav1.Now(),
-		Reason:             "AdmittedByTest",
-		Message:            fmt.Sprintf("Admitted by ClusterQueue %s", wl.Status.Admission.ClusterQueue),
-	}}
+	i := workload.FindConditionIndex(&wl.Status, kueue.WorkloadAdmitted)
+	if i == -1 {
+		wl.Status.Conditions = append(wl.Status.Conditions, metav1.Condition{
+			Type:               kueue.WorkloadAdmitted,
+			Status:             metav1.ConditionTrue,
+			LastTransitionTime: metav1.Now(),
+			Reason:             "AdmittedByTest",
+			Message:            fmt.Sprintf("Admitted by ClusterQueue %s", wl.Status.Admission.ClusterQueue),
+		})
+	} else {
+		wl.Status.Conditions[i] = metav1.Condition{
+			Type:               kueue.WorkloadAdmitted,
+			Status:             metav1.ConditionTrue,
+			LastTransitionTime: metav1.Now(),
+			Reason:             "AdmittedByTest",
+			Message:            fmt.Sprintf("Admitted by ClusterQueue %s", wl.Status.Admission.ClusterQueue),
+		}
+	}
+
+	return k8sClient.Status().Update(ctx, wl)
+}
+
+func UnAdmitWorkload(ctx context.Context, k8sClient client.Client, wl *kueue.Workload) error {
+	wl.Status.Admission = nil
+	i := workload.FindConditionIndex(&wl.Status, kueue.WorkloadAdmitted)
+	if i == -1 {
+		wl.Status.Conditions = append(wl.Status.Conditions, metav1.Condition{
+			Type:               kueue.WorkloadAdmitted,
+			Status:             metav1.ConditionFalse,
+			LastTransitionTime: metav1.Now(),
+			Reason:             "PreemptedByTest",
+			Message:            "Preempted by test",
+		})
+	} else {
+		wl.Status.Conditions[i] = metav1.Condition{
+			Type:               kueue.WorkloadAdmitted,
+			Status:             metav1.ConditionFalse,
+			LastTransitionTime: metav1.Now(),
+			Reason:             "PreemptedByTest",
+			Message:            "Preempted by test",
+		}
+	}
```

  same.

- **test/util/util.go:158** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-24T15:48:00Z
```diff
@@ -138,6 +138,24 @@ func SetAdmission(ctx context.Context, k8sClient client.Client, wl *kueue.Worklo
 		var newWL kueue.Workload
 		gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &newWL)).To(gomega.Succeed())
 		newWL.Status.Admission = admission
+		i := workload.FindConditionIndex(&newWL.Status, kueue.WorkloadAdmitted)
+		if i == -1 {
+			newWL.Status.Conditions = append(newWL.Status.Conditions, metav1.Condition{
+				Type:               kueue.WorkloadAdmitted,
+				Status:             metav1.ConditionTrue,
+				LastTransitionTime: metav1.Now(),
+				Reason:             "AdmittedByTest",
+				Message:            fmt.Sprintf("Admitted by ClusterQueue %s", newWL.Status.Admission.ClusterQueue),
+			})
+		} else {
+			newWL.Status.Conditions[i] = metav1.Condition{
+				Type:               kueue.WorkloadAdmitted,
+				Status:             metav1.ConditionTrue,
+				LastTransitionTime: metav1.Now(),
+				Reason:             "AdmittedByTest",
+				Message:            fmt.Sprintf("Admitted by ClusterQueue %s", newWL.Status.Admission.ClusterQueue),
+			}
+		}
```

  same.

- **pkg/controller/core/workload_controller_test.go:105** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-24T15:51:34Z
```diff
@@ -88,21 +88,6 @@ func TestAdmittedNotReadyWorkload(t *testing.T) {
 			podsReadyTimeout:           pointer.Duration(5 * time.Minute),
 			wantCountingTowardsTimeout: true,
 		},
-		"workload with Admitted=True, no PodsReady, but not admitted; not counting": {
-			workload: kueue.Workload{
-				Spec: kueue.WorkloadSpec{},
-				Status: kueue.WorkloadStatus{
-					Conditions: []metav1.Condition{
-						{
-							Type:               kueue.WorkloadAdmitted,
-							Status:             metav1.ConditionTrue,
-							LastTransitionTime: metav1.NewTime(minuteAgo),
-						},
-					},
-				},
-			},
-			podsReadyTimeout: pointer.Duration(5 * time.Minute),
-		},
```

  Why is this test case unnecessary?

### Review by [@mcariatm](https://github.com/mcariatm) (commented) — 2023-04-25T06:46:18Z

- **pkg/util/testing/wrappers.go:161** by [@mcariatm](https://github.com/mcariatm) — 2023-04-25T06:46:18Z
```diff
@@ -143,6 +151,15 @@ func (w *WorkloadWrapper) Condition(condition metav1.Condition) *WorkloadWrapper
 	return w
 }
 
+func (w *WorkloadWrapper) SetOrReplaceCondition(condition metav1.Condition) *WorkloadWrapper {
+	existingCondition := apimeta.FindStatusCondition(w.Status.Conditions, condition.Type)
+	if existingCondition != nil {
+		apimeta.RemoveStatusCondition(&w.Status.Conditions, condition.Type)
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, condition)
+	return w
+}
```

  I use it here https://github.com/kubernetes-sigs/kueue/pull/717/files#diff-2983f8941450021febb0b5013e359a5f344a84a6f6a9cfee8ecbdf3c3f92a9cfR756
  
  I need to replace LastTransitionTime from condition but SetOrReplaceCondition doesn't do that if !newCondition.LastTransitionTime.IsZero().

### Review by [@mcariatm](https://github.com/mcariatm) (commented) — 2023-04-25T06:51:03Z

- **pkg/controller/core/workload_controller_test.go:105** by [@mcariatm](https://github.com/mcariatm) — 2023-04-25T06:51:03Z
```diff
@@ -88,21 +88,6 @@ func TestAdmittedNotReadyWorkload(t *testing.T) {
 			podsReadyTimeout:           pointer.Duration(5 * time.Minute),
 			wantCountingTowardsTimeout: true,
 		},
-		"workload with Admitted=True, no PodsReady, but not admitted; not counting": {
-			workload: kueue.Workload{
-				Spec: kueue.WorkloadSpec{},
-				Status: kueue.WorkloadStatus{
-					Conditions: []metav1.Condition{
-						{
-							Type:               kueue.WorkloadAdmitted,
-							Status:             metav1.ConditionTrue,
-							LastTransitionTime: metav1.NewTime(minuteAgo),
-						},
-					},
-				},
-			},
-			podsReadyTimeout: pointer.Duration(5 * time.Minute),
-		},
```

  This situation is not anymore relevant. We must not have situations where admission condition is true and Admission is nil!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-04-25T09:26:07Z

- **pkg/util/testing/wrappers.go:161** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-25T09:26:06Z
```diff
@@ -143,6 +151,15 @@ func (w *WorkloadWrapper) Condition(condition metav1.Condition) *WorkloadWrapper
 	return w
 }
 
+func (w *WorkloadWrapper) SetOrReplaceCondition(condition metav1.Condition) *WorkloadWrapper {
+	existingCondition := apimeta.FindStatusCondition(w.Status.Conditions, condition.Type)
+	if existingCondition != nil {
+		apimeta.RemoveStatusCondition(&w.Status.Conditions, condition.Type)
+	}
+	apimeta.SetStatusCondition(&w.Status.Conditions, condition)
+	return w
+}
```

  Ah, you want to update the LastTransitionTime if the oldCondition.Status equals the newCondition.Status, right?
  If so, sgtm.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-04-25T09:32:29Z

- **pkg/controller/core/workload_controller_test.go:105** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-25T09:32:29Z
```diff
@@ -88,21 +88,6 @@ func TestAdmittedNotReadyWorkload(t *testing.T) {
 			podsReadyTimeout:           pointer.Duration(5 * time.Minute),
 			wantCountingTowardsTimeout: true,
 		},
-		"workload with Admitted=True, no PodsReady, but not admitted; not counting": {
-			workload: kueue.Workload{
-				Spec: kueue.WorkloadSpec{},
-				Status: kueue.WorkloadStatus{
-					Conditions: []metav1.Condition{
-						{
-							Type:               kueue.WorkloadAdmitted,
-							Status:             metav1.ConditionTrue,
-							LastTransitionTime: metav1.NewTime(minuteAgo),
-						},
-					},
-				},
-			},
-			podsReadyTimeout: pointer.Duration(5 * time.Minute),
-		},
```

  Oh, that's right. I remember that you created this PR to avoid such a case in the first place. :)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-25T18:35:23Z

- **pkg/workload/workload.go:304** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-25T18:35:22Z
```diff
@@ -292,5 +293,14 @@ func BaseSSAWorkload(w *kueue.Workload) *kueue.Workload {
 func AdmissionPatch(w *kueue.Workload) *kueue.Workload {
 	wlCopy := BaseSSAWorkload(w)
 	wlCopy.Status.Admission = w.Status.Admission.DeepCopy()
+	if admittedCondition := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadAdmitted); admittedCondition != nil {
+		wlCopy.Status.Conditions = []metav1.Condition{*admittedCondition}
+	}
+
 	return wlCopy
 }
+
+// IsWorkloadAdmitted checks if workload is admitted based on conditions
+func IsWorkloadAdmitted(w *kueue.Workload) bool {
```

  This is enough, because this is the workload package.
  
  ```suggestion
  func IsAdmitted(w *kueue.Workload) bool {
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-25T18:56:21Z

- **pkg/util/testing/wrappers.go:154** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-25T18:47:39Z
```diff
@@ -143,6 +151,15 @@ func (w *WorkloadWrapper) Condition(condition metav1.Condition) *WorkloadWrapper
 	return w
 }
 
+func (w *WorkloadWrapper) SetOrReplaceCondition(condition metav1.Condition) *WorkloadWrapper {
```

  `apimeta.SetStatusCondition` already implements this.

- **test/util/util.go:142** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-25T18:50:35Z
```diff
@@ -138,6 +139,14 @@ func SetAdmission(ctx context.Context, k8sClient client.Client, wl *kueue.Worklo
 		var newWL kueue.Workload
 		gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &newWL)).To(gomega.Succeed())
 		newWL.Status.Admission = admission
+		newWL.Status.Conditions = wl.Status.Conditions
```

  no need for this line. We are already getting the conditions from the `k8sClient.Get` call.

- **test/util/util.go:179** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-25T18:52:29Z
```diff
@@ -167,7 +176,7 @@ func ExpectWorkloadsToBePending(ctx context.Context, k8sClient client.Client, wl
 				continue
 			}
 			cond := updatedWorkload.Status.Conditions[idx]
-			if cond.Status == metav1.ConditionFalse && cond.Reason == "Pending" && wl.Status.Admission == nil {
+			if cond.Status == metav1.ConditionFalse && cond.Reason == "Pending" && !workload.IsWorkloadAdmitted(wl) {
```

  In theory, this should be enough, because we are checking the WorkloadAdmitted condition.
  
  ```suggestion
  			if cond.Status == metav1.ConditionFalse && cond.Reason == "Pending" {
  ```
  
  but this check is fine.

- **test/util/util.go:159** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-25T18:53:21Z
```diff
@@ -167,7 +176,7 @@ func ExpectWorkloadsToBePending(ctx context.Context, k8sClient client.Client, wl
 				continue
```

  For follow up: we shouldn't need the `FindConditionIndex` function. We can use the meta helpers.

- **test/util/util.go:145** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-25T18:55:14Z
```diff
@@ -138,6 +139,14 @@ func SetAdmission(ctx context.Context, k8sClient client.Client, wl *kueue.Worklo
 		var newWL kueue.Workload
 		gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &newWL)).To(gomega.Succeed())
 		newWL.Status.Admission = admission
+		newWL.Status.Conditions = wl.Status.Conditions
+		apimeta.SetStatusCondition(&newWL.Status.Conditions, metav1.Condition{
+			Type:               kueue.WorkloadAdmitted,
+			Status:             metav1.ConditionTrue,
```

  it's only true if `admission != nil`, right?

- **test/util/util.go:309** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-25T18:56:08Z
```diff
@@ -297,12 +306,25 @@ func AdmitWorkload(ctx context.Context, k8sClient client.Client, wl *kueue.Workl
 		return fmt.Errorf("admission can't be null")
 	}
 	wl.Status.Admission = admission
-	wl.Status.Conditions = []metav1.Condition{{
+	apimeta.SetStatusCondition(&wl.Status.Conditions, metav1.Condition{
```

  Oops, it looks like we have 3 functions that do roughly the same. Let's only keep one: `SetAdmission` and make it work for Admit and Evict.

### Review by [@mcariatm](https://github.com/mcariatm) (commented) — 2023-04-25T19:25:07Z

- **pkg/util/testing/wrappers.go:154** by [@mcariatm](https://github.com/mcariatm) — 2023-04-25T19:25:07Z
```diff
@@ -143,6 +151,15 @@ func (w *WorkloadWrapper) Condition(condition metav1.Condition) *WorkloadWrapper
 	return w
 }
 
+func (w *WorkloadWrapper) SetOrReplaceCondition(condition metav1.Condition) *WorkloadWrapper {
```

  I use it here https://github.com/kubernetes-sigs/kueue/pull/717/files#diff-2983f8941450021febb0b5013e359a5f344a84a6f6a9cfee8ecbdf3c3f92a9cfR756
  
  I need to replace LastTransitionTime from condition but apimeta.SetStatusCondition doesn't do that if !newCondition.LastTransitionTime.IsZero().

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-25T19:43:03Z

- **pkg/util/testing/wrappers.go:154** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-25T19:43:03Z
```diff
@@ -143,6 +151,15 @@ func (w *WorkloadWrapper) Condition(condition metav1.Condition) *WorkloadWrapper
 	return w
 }
 
+func (w *WorkloadWrapper) SetOrReplaceCondition(condition metav1.Condition) *WorkloadWrapper {
```

  I see, because `Admit` adds the condition...
  
  Sounds good.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-04-25T19:50:22Z

- **test/util/util.go:159** by [@tenzen-y](https://github.com/tenzen-y) — 2023-04-25T19:50:22Z
```diff
@@ -167,7 +176,7 @@ func ExpectWorkloadsToBePending(ctx context.Context, k8sClient client.Client, wl
 				continue
```

  I have already worked on that at #705.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-25T19:53:01Z

- **test/util/util.go:159** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-25T19:53:01Z
```diff
@@ -167,7 +176,7 @@ func ExpectWorkloadsToBePending(ctx context.Context, k8sClient client.Client, wl
 				continue
```

  ah, true. Let me check that one now.

### Review by [@mcariatm](https://github.com/mcariatm) (commented) — 2023-04-26T12:21:52Z

- **test/util/util.go:145** by [@mcariatm](https://github.com/mcariatm) — 2023-04-26T12:21:52Z
```diff
@@ -138,6 +139,14 @@ func SetAdmission(ctx context.Context, k8sClient client.Client, wl *kueue.Worklo
 		var newWL kueue.Workload
 		gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &newWL)).To(gomega.Succeed())
 		newWL.Status.Admission = admission
+		newWL.Status.Conditions = wl.Status.Conditions
+		apimeta.SetStatusCondition(&newWL.Status.Conditions, metav1.Condition{
+			Type:               kueue.WorkloadAdmitted,
+			Status:             metav1.ConditionTrue,
```

  right

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-04-26T15:20:15Z

/lgtm
from my side.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-26T19:05:10Z

/approve
