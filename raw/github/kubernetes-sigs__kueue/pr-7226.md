# PR #7226: Add validation for unsupported DRA features

**Summary**: Add validation for unsupported DRA features

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7226

**Last updated**: 2025-10-24T11:09:25Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@harche](https://github.com/harche)
- **Branch**: `dra_validation` → `main`
- **Created**: 2025-10-10T16:54:51Z
- **Updated**: 2025-10-24T11:09:25Z
- **Merged**: 2025-10-24T09:29:37Z
- **Closed**: 2025-10-24T09:29:37Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `size/XL`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alaypatel07](https://github.com/alaypatel07), [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@alaypatel07](https://github.com/alaypatel07), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 4   **+460 / -54**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind bug

#### What this PR does / why we need it:
 This PR adds validation to reject DRA (Dynamic Resource Allocation) v1 API features that are enabled
   by default in Kubernetes 1.34 but explicitly excluded from Kueue's DRA support scope per [KEP-2941](https://github.com/kubernetes-sigs/kueue/blob/main/keps/2941-DRA/README.md).


  Without validation, Kueue silently ignores unsupported DRA features, leading to:
  1. Silent admission failures - FirstAvailable workloads are admitted with 0 DRA resources, causing
  runtime failures
  2. Misleading errors - 5 out of 6 unsupported features produce confusing "DeviceClass not mapped"
  errors instead of clear feature-not-supported messages
  3. Poor UX - Users waste time debugging DeviceClass mappings when the actual issue is using
  unsupported features


Solution is to add early validation in pkg/dra/claims.go to explicitly reject all 6 enabled-by-default DRA features
   that Kueue doesn't support:

  GA Features:
  - AllocationMode 'All'
  - CEL Selectors
  - Device Constraints
  - Device Config

  Beta Features (enabled by default in K8s 1.34):
  - FirstAvailable (DRAPrioritizedList)
  - AdminAccess (DRAAdminAccess)

  Each feature now returns a clear, actionable error message like "FirstAvailable device selection is
  not supported" instead of misleading DeviceClass mapping errors.
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Kueue now properly validates and rejects unsupported DRA (Dynamic Resource Allocation) features with clear error messages instead of silently failing or producing misleading "DeviceClass not mapped" errors. Unsupported features include: AllocationMode 'All', CEL Selectors, Device Constraints, Device Config, FirstAvailable device selection, and AdminAccess.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-10-10T16:55:17Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 057dd223174be1b7788cbd2705df5f7d88267bca |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68fa302c29e15b0008adf648 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-7226--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-7226--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTcyMjYtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.yKXuavAT22fnP966tMUhC2yKzmORNfxGvwhK7FZ89A0)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-7226--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-10T16:55:20Z

Hi @harche. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-10T16:58:27Z

/ok-to-test
@alaypatel07 would you like to give it a pass?

### Comment by [@kannon92](https://github.com/kannon92) — 2025-10-10T17:00:45Z

/ok-to-test

/cc @alaypatel07

### Comment by [@alaypatel07](https://github.com/alaypatel07) — 2025-10-10T17:11:56Z

Yes I'll take a look.

### Comment by [@harche](https://github.com/harche) — 2025-10-10T19:10:44Z

/test pull-kueue-test-e2e-multikueue-main

### Comment by [@harche](https://github.com/harche) — 2025-10-10T19:13:08Z

@alaypatel07 I have updated the PR. Can you take another look? Thanks.

### Comment by [@kannon92](https://github.com/kannon92) — 2025-10-15T19:29:24Z

Please add a release note.

### Comment by [@harche](https://github.com/harche) — 2025-10-16T01:50:04Z

> Please add a release note.

Thanks, done.

### Comment by [@alaypatel07](https://github.com/alaypatel07) — 2025-10-16T01:58:57Z

@harche I'm falling behind on reviews due to the upcoming KEP freeze. I'll take a look first thing tomorrow morning.

### Comment by [@harche](https://github.com/harche) — 2025-10-16T09:04:42Z

> @harche I'm falling behind on reviews due to the upcoming KEP freeze. I'll take a look first thing tomorrow morning.

No worries @alaypatel07 , take your time. This can wait until the KEP freeze deadline passes. Thanks for your valuable feedback.

### Comment by [@harche](https://github.com/harche) — 2025-10-20T15:44:46Z

/hold just like unit tests, the integration tests should also check for exact error.

### Comment by [@harche](https://github.com/harche) — 2025-10-20T15:52:03Z

> /hold just like unit tests, the integration tests should also check for exact error.

updated. 

/unhold

### Comment by [@alaypatel07](https://github.com/alaypatel07) — 2025-10-22T06:55:34Z

Except the open comments, other things look good to me, feel free to push this forward once those are addressed.

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-22T06:55:44Z

LGTM label has been added.  <details>Git tree hash: a3b688937209641617394c226af10d437e70ec79</details>

### Comment by [@harche](https://github.com/harche) — 2025-10-22T16:37:53Z

> Except the open comments, other things look good to me, feel free to push this forward once those are addressed.
> 
> /lgtm

Thanks @alaypatel07 , during the testing I realized that [allocation mode is allowed to be empty ](https://github.com/kubernetes/kubernetes/blob/e1b996a4d3358b783a17e5f54a70e573e394a697/pkg/apis/resource/types.go#L846C61-L846C71), it just defaults to `ExactCount`. I made a small tweak to handle empty allocation mode at, https://github.com/kubernetes-sigs/kueue/pull/7226/files#diff-1817bbb9cab28ee6c2fb856446a0164731ee7fb63b3620c069d31c439750e2d1R85

### Comment by [@alaypatel07](https://github.com/alaypatel07) — 2025-10-22T16:42:53Z

@harche from what I can see, AllocationMode field itself can be `""` i.e. empty because it is not a pointer but it should always be defaulted to AllocationModeExact https://github.com/kubernetes/kubernetes/blob/e1b996a4d3358b783a17e5f54a70e573e394a697/pkg/apis/resource/v1beta1/defaults.go#L43

Are you seeing that this defaulting is not working?

### Comment by [@harche](https://github.com/harche) — 2025-10-22T16:52:57Z

> @harche from what I can see, AllocationMode field itself can be `""` i.e. empty because it is not a pointer but it should always be defaulted to AllocationModeExact https://github.com/kubernetes/kubernetes/blob/e1b996a4d3358b783a17e5f54a70e573e394a697/pkg/apis/resource/v1beta1/defaults.go#L43
> 
> Are you seeing that this defaulting is not working?

yeah, if I remove the `req.Exactly.AllocationMode == ""` check in the switch, the test that specifically sets AllocationMode to "" fails, but it passes if I bring back that check. 

```zsh
% go test ./pkg/dra/...
--- FAIL: Test_GetResourceRequests (0.05s)
    --- FAIL: Test_GetResourceRequests/Empty_allocation_mode_defaults_to_exact_count (0.00s)
        claims_test.go:392: unexpected error: unsupported DRA feature in ResourceClaimTemplate claim-tmpl-default in workload wl podset main: unsupported DRA feature: unsupported allocation mode: 
FAIL
FAIL	sigs.k8s.io/kueue/pkg/dra	0.402s
FAIL
```

but passes if I add `req.Exactly.AllocationMode == "" ` 
```zsh
go test ./pkg/dra/...
ok  	sigs.k8s.io/kueue/pkg/dra	(cached)
```

### Comment by [@alaypatel07](https://github.com/alaypatel07) — 2025-10-22T16:56:25Z

> > @harche from what I can see, AllocationMode field itself can be `""` i.e. empty because it is not a pointer but it should always be defaulted to AllocationModeExact https://github.com/kubernetes/kubernetes/blob/e1b996a4d3358b783a17e5f54a70e573e394a697/pkg/apis/resource/v1beta1/defaults.go#L43
> > Are you seeing that this defaulting is not working?
> 
> yeah, if I remove the `req.Exactly.AllocationMode == ""` check in the switch, the test that specifically sets AllocationMode to "" fails, but it passes if I bring back that check.
> 
> ```shell
> % go test ./pkg/dra/...
> --- FAIL: Test_GetResourceRequests (0.05s)
>     --- FAIL: Test_GetResourceRequests/Empty_allocation_mode_defaults_to_exact_count (0.00s)
>         claims_test.go:392: unexpected error: unsupported DRA feature in ResourceClaimTemplate claim-tmpl-default in workload wl podset main: unsupported DRA feature: unsupported allocation mode: 
> FAIL
> FAIL	sigs.k8s.io/kueue/pkg/dra	0.402s
> FAIL
> ```
> 
> but passes if I add `req.Exactly.AllocationMode == "" `
> 
> ```shell
> go test ./pkg/dra/...
> ok  	sigs.k8s.io/kueue/pkg/dra	(cached)
> ```

That's because this is a unit test and the resource claim is not going through the API server which has defaulting logic.  This test will not fail in the e2e since it goes through API server there. The correct thing IMO is to remove this unit test and let it just be an e2e test

### Comment by [@harche](https://github.com/harche) — 2025-10-22T17:00:23Z

> > > @harche from what I can see, AllocationMode field itself can be `""` i.e. empty because it is not a pointer but it should always be defaulted to AllocationModeExact https://github.com/kubernetes/kubernetes/blob/e1b996a4d3358b783a17e5f54a70e573e394a697/pkg/apis/resource/v1beta1/defaults.go#L43
> > > Are you seeing that this defaulting is not working?
> > 
> > 
> > yeah, if I remove the `req.Exactly.AllocationMode == ""` check in the switch, the test that specifically sets AllocationMode to "" fails, but it passes if I bring back that check.
> > ```shell
> > % go test ./pkg/dra/...
> > --- FAIL: Test_GetResourceRequests (0.05s)
> >     --- FAIL: Test_GetResourceRequests/Empty_allocation_mode_defaults_to_exact_count (0.00s)
> >         claims_test.go:392: unexpected error: unsupported DRA feature in ResourceClaimTemplate claim-tmpl-default in workload wl podset main: unsupported DRA feature: unsupported allocation mode: 
> > FAIL
> > FAIL	sigs.k8s.io/kueue/pkg/dra	0.402s
> > FAIL
> > ```
> > 
> > 
> >     
> >       
> >     
> > 
> >       
> >     
> > 
> >     
> >   
> > but passes if I add `req.Exactly.AllocationMode == "" `
> > ```shell
> > go test ./pkg/dra/...
> > ok  	sigs.k8s.io/kueue/pkg/dra	(cached)
> > ```
> 
> That's because this is a unit test and the resource claim is not going through the API server which has defaulting logic. This test will not fail in the e2e since it goes through API server there. The correct thing IMO is to remove this unit test and let it just be an e2e test

@alaypatel07 good point, removed it. Thanks.

### Comment by [@harche](https://github.com/harche) — 2025-10-22T18:25:42Z

/test pull-kueue-test-e2e-tas-main

### Comment by [@harche](https://github.com/harche) — 2025-10-22T18:27:04Z


> > That's because this is a unit test and the resource claim is not going through the API server which has defaulting logic. This test will not fail in the e2e since it goes through API server there. The correct thing IMO is to remove this unit test and let it just be an e2e test
> 
> @alaypatel07 good point, removed it. Thanks.

@alaypatel07 replaced with an integration test, https://github.com/kubernetes-sigs/kueue/pull/7226/files#diff-5dc66fc561764c1765de2f7abf5aca47f48c675141ab4c6c5fd6ce7a3a40b1a6R727

### Comment by [@harche](https://github.com/harche) — 2025-10-23T22:37:58Z

 @mimowo @alaypatel07  I have addressed all the review comments. Thanks.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-24T09:01:17Z

LGTM label has been added.  <details>Git tree hash: c89963c8bc4ae64e075c076acfad0c422a188c56</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-24T09:01:17Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7226#" title="Author self-approved">harche</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7226#pullrequestreview-3375256219" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-24T09:02:51Z

@harche let me know if:
1. you could follow up on https://github.com/kubernetes-sigs/kueue/issues/7343
2. you want to cherrypick to 0.14, this can be done but requires manual conflict resolution. Since the integration is an alpha feature it is not necessary

### Comment by [@harche](https://github.com/harche) — 2025-10-24T11:09:25Z

> @harche let me know if:
> 
> 1. you could follow up on [DRA cleanup: use structured assert on errors using cmp.Diff #7343](https://github.com/kubernetes-sigs/kueue/issues/7343)
> 2. you want to cherrypick to 0.14, this can be done but requires manual conflict resolution. Since the integration is an alpha feature it is not necessary

@mimowo thanks for your review. 

1. I have assigned https://github.com/kubernetes-sigs/kueue/issues/7343 to me. 
2. I am not looking for backporting it to 0.14, since DRA support is in Kueue is alpha anyway.

## Reviews

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-10-10T17:42:48Z

- **pkg/dra/claims.go:73** by [@alaypatel07](https://github.com/alaypatel07) — 2025-10-10T17:42:48Z
```diff
@@ -33,38 +33,78 @@ import (
 )
 
 var (
-	ErrDeviceClassNotMapped = errors.New("DeviceClass is not mapped in DRA configuration")
-	ErrResourceClaimInUse   = errors.New("ResourceClaim is in use")
-	ErrClaimSpecNotFound    = errors.New("failed to get claim spec")
+	ErrDeviceClassNotMapped          = errors.New("DeviceClass is not mapped in DRA configuration")
+	ErrResourceClaimInUse            = errors.New("ResourceClaim is in use")
+	ErrClaimSpecNotFound             = errors.New("failed to get claim spec")
+	ErrAllocationModeAllNotSupported = errors.New("AllocationMode 'All' is not supported")
+	ErrCELSelectorsNotSupported      = errors.New("CEL selectors are not supported")
+	ErrDeviceConstraintsNotSupported = errors.New("device constraints (MatchAttribute) are not supported")
+	ErrFirstAvailableNotSupported    = errors.New("FirstAvailable device selection is not supported")
+	ErrAdminAccessNotSupported       = errors.New("AdminAccess is not supported")
+	ErrDeviceConfigNotSupported      = errors.New("device config is not supported")
 )
 
 // countDevicesPerClass returns a resources.Requests representing the
 // total number of devices requested for each DeviceClass inside the provided
-// ResourceClaimSpec.
-func countDevicesPerClass(claimSpec *resourcev1.ResourceClaimSpec) resources.Requests {
+// ResourceClaimSpec. It validates that only supported DRA features are used
+// and returns an error if unsupported features are detected.
+func countDevicesPerClass(claimSpec *resourcev1.ResourceClaimSpec) (resources.Requests, error) {
 	out := resources.Requests{}
 	if claimSpec == nil {
-		return out
+		return out, nil
 	}
+
+	// Check for unsupported device constraints
+	if len(claimSpec.Devices.Constraints) > 0 {
+		return nil, ErrDeviceConstraintsNotSupported
+	}
+
+	// Check for unsupported device config
+	if len(claimSpec.Devices.Config) > 0 {
+		return nil, ErrDeviceConfigNotSupported
+	}
+
 	for _, req := range claimSpec.Devices.Requests {
-		// v1beta2 DeviceRequest has Exactly or FirstAvailable. For Step 1, we
+		// v1 DeviceRequest has Exactly or FirstAvailable. For Step 1, we
 		// preserve existing semantics by only supporting Exactly with Count.
 		var dcName string
 		var q int64
 		if req.Exactly != nil {
+			// Check for CEL selectors
```

  can you please convert the entire if condition into a switch case handling all the unsupported features first before considering the valid supported case?

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-10-10T17:45:55Z

- **pkg/dra/claims.go:36** by [@alaypatel07](https://github.com/alaypatel07) — 2025-10-10T17:45:55Z
```diff
@@ -33,38 +33,78 @@ import (
 )
 
 var (
-	ErrDeviceClassNotMapped = errors.New("DeviceClass is not mapped in DRA configuration")
-	ErrResourceClaimInUse   = errors.New("ResourceClaim is in use")
-	ErrClaimSpecNotFound    = errors.New("failed to get claim spec")
+	ErrDeviceClassNotMapped          = errors.New("DeviceClass is not mapped in DRA configuration")
```

  Nit: can we use a common error "UnsupportedDRAfeature" and then wrap a human readable error message to be returned from the function later.
  
  I think we only need to have the following concrete error types:
  1. ErrResourceClaimInUse and other
  1. ErrClaimSpecNotFound 
  1. ErrUnsupportedDRAfeature

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-10-10T17:47:38Z

- **pkg/dra/claims_test.go:219** by [@alaypatel07](https://github.com/alaypatel07) — 2025-10-10T17:47:38Z
```diff
@@ -253,6 +253,230 @@ func Test_GetResourceRequests(t *testing.T) {
 			},
 			want: map[kueuev1beta1.PodSetReference]corev1.ResourceList{"main": {"res-1": resource.MustParse("2")}},
 		},
+		{
```

  we should also update the integration tests for these errors at https://github.com/kubernetes-sigs/kueue/tree/main/test/integration/singlecluster/controller/dra

### Review by [@harche](https://github.com/harche) (commented) — 2025-10-10T18:54:23Z

- **pkg/dra/claims.go:73** by [@harche](https://github.com/harche) — 2025-10-10T18:54:23Z
```diff
@@ -33,38 +33,78 @@ import (
 )
 
 var (
-	ErrDeviceClassNotMapped = errors.New("DeviceClass is not mapped in DRA configuration")
-	ErrResourceClaimInUse   = errors.New("ResourceClaim is in use")
-	ErrClaimSpecNotFound    = errors.New("failed to get claim spec")
+	ErrDeviceClassNotMapped          = errors.New("DeviceClass is not mapped in DRA configuration")
+	ErrResourceClaimInUse            = errors.New("ResourceClaim is in use")
+	ErrClaimSpecNotFound             = errors.New("failed to get claim spec")
+	ErrAllocationModeAllNotSupported = errors.New("AllocationMode 'All' is not supported")
+	ErrCELSelectorsNotSupported      = errors.New("CEL selectors are not supported")
+	ErrDeviceConstraintsNotSupported = errors.New("device constraints (MatchAttribute) are not supported")
+	ErrFirstAvailableNotSupported    = errors.New("FirstAvailable device selection is not supported")
+	ErrAdminAccessNotSupported       = errors.New("AdminAccess is not supported")
+	ErrDeviceConfigNotSupported      = errors.New("device config is not supported")
 )
 
 // countDevicesPerClass returns a resources.Requests representing the
 // total number of devices requested for each DeviceClass inside the provided
-// ResourceClaimSpec.
-func countDevicesPerClass(claimSpec *resourcev1.ResourceClaimSpec) resources.Requests {
+// ResourceClaimSpec. It validates that only supported DRA features are used
+// and returns an error if unsupported features are detected.
+func countDevicesPerClass(claimSpec *resourcev1.ResourceClaimSpec) (resources.Requests, error) {
 	out := resources.Requests{}
 	if claimSpec == nil {
-		return out
+		return out, nil
 	}
+
+	// Check for unsupported device constraints
+	if len(claimSpec.Devices.Constraints) > 0 {
+		return nil, ErrDeviceConstraintsNotSupported
+	}
+
+	// Check for unsupported device config
+	if len(claimSpec.Devices.Config) > 0 {
+		return nil, ErrDeviceConfigNotSupported
+	}
+
 	for _, req := range claimSpec.Devices.Requests {
-		// v1beta2 DeviceRequest has Exactly or FirstAvailable. For Step 1, we
+		// v1 DeviceRequest has Exactly or FirstAvailable. For Step 1, we
 		// preserve existing semantics by only supporting Exactly with Count.
 		var dcName string
 		var q int64
 		if req.Exactly != nil {
+			// Check for CEL selectors
```

  Addressed, thanks.

### Review by [@harche](https://github.com/harche) (commented) — 2025-10-10T18:54:35Z

- **pkg/dra/claims.go:36** by [@harche](https://github.com/harche) — 2025-10-10T18:54:35Z
```diff
@@ -33,38 +33,78 @@ import (
 )
 
 var (
-	ErrDeviceClassNotMapped = errors.New("DeviceClass is not mapped in DRA configuration")
-	ErrResourceClaimInUse   = errors.New("ResourceClaim is in use")
-	ErrClaimSpecNotFound    = errors.New("failed to get claim spec")
+	ErrDeviceClassNotMapped          = errors.New("DeviceClass is not mapped in DRA configuration")
```

  Addressed, thanks.

### Review by [@harche](https://github.com/harche) (commented) — 2025-10-10T18:54:46Z

- **pkg/dra/claims_test.go:219** by [@harche](https://github.com/harche) — 2025-10-10T18:54:46Z
```diff
@@ -253,6 +253,230 @@ func Test_GetResourceRequests(t *testing.T) {
 			},
 			want: map[kueuev1beta1.PodSetReference]corev1.ResourceList{"main": {"res-1": resource.MustParse("2")}},
 		},
+		{
```

  Added integration tests.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-10-10T20:35:52Z

- **pkg/dra/claims.go:73** by [@alaypatel07](https://github.com/alaypatel07) — 2025-10-10T20:35:52Z
```diff
@@ -33,38 +33,78 @@ import (
 )
 
 var (
-	ErrDeviceClassNotMapped = errors.New("DeviceClass is not mapped in DRA configuration")
-	ErrResourceClaimInUse   = errors.New("ResourceClaim is in use")
-	ErrClaimSpecNotFound    = errors.New("failed to get claim spec")
+	ErrDeviceClassNotMapped          = errors.New("DeviceClass is not mapped in DRA configuration")
+	ErrResourceClaimInUse            = errors.New("ResourceClaim is in use")
+	ErrClaimSpecNotFound             = errors.New("failed to get claim spec")
+	ErrAllocationModeAllNotSupported = errors.New("AllocationMode 'All' is not supported")
+	ErrCELSelectorsNotSupported      = errors.New("CEL selectors are not supported")
+	ErrDeviceConstraintsNotSupported = errors.New("device constraints (MatchAttribute) are not supported")
+	ErrFirstAvailableNotSupported    = errors.New("FirstAvailable device selection is not supported")
+	ErrAdminAccessNotSupported       = errors.New("AdminAccess is not supported")
+	ErrDeviceConfigNotSupported      = errors.New("device config is not supported")
 )
 
 // countDevicesPerClass returns a resources.Requests representing the
 // total number of devices requested for each DeviceClass inside the provided
-// ResourceClaimSpec.
-func countDevicesPerClass(claimSpec *resourcev1.ResourceClaimSpec) resources.Requests {
+// ResourceClaimSpec. It validates that only supported DRA features are used
+// and returns an error if unsupported features are detected.
+func countDevicesPerClass(claimSpec *resourcev1.ResourceClaimSpec) (resources.Requests, error) {
 	out := resources.Requests{}
 	if claimSpec == nil {
-		return out
+		return out, nil
 	}
+
+	// Check for unsupported device constraints
+	if len(claimSpec.Devices.Constraints) > 0 {
+		return nil, ErrDeviceConstraintsNotSupported
+	}
+
+	// Check for unsupported device config
+	if len(claimSpec.Devices.Config) > 0 {
+		return nil, ErrDeviceConfigNotSupported
+	}
+
 	for _, req := range claimSpec.Devices.Requests {
-		// v1beta2 DeviceRequest has Exactly or FirstAvailable. For Step 1, we
+		// v1 DeviceRequest has Exactly or FirstAvailable. For Step 1, we
 		// preserve existing semantics by only supporting Exactly with Count.
 		var dcName string
 		var q int64
 		if req.Exactly != nil {
+			// Check for CEL selectors
```

  @harche why does this have to be nested ifs and switch cases? Can we work out a logic with one single switch case?
  
  For example:
  ```
  		switch {
  		case req.FirstAvailable != nil:
  		case req.Exactly != nil && len(req.Exactly.Selectors) > 0:
  		// error
  		case req.Exactly != nil && req.Exactly.AdminAccess != nil && *req.Exactly.AdminAccess:
  		// error
  		case req.Exactly != nil && req.Exactly.AllocationMode == DeviceAllocationModeAll:
  		// error
  		}
  ```

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-10-10T20:42:25Z

- **test/integration/singlecluster/controller/dra/dra_test.go:502** by [@alaypatel07](https://github.com/alaypatel07) — 2025-10-10T20:42:25Z
```diff
@@ -618,5 +619,385 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 					"DRA resource usage should be a multiple of pod count for webhook validation")
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
+
+		ginkgo.It("Should reject workload with AllocationMode 'All'", func() {
+			ginkgo.By("Creating a ResourceClaimTemplate with AllocationMode All")
+			rct := &resourcev1.ResourceClaimTemplate{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      "all-mode-template",
+					Namespace: ns.Name,
+				},
+				Spec: resourcev1.ResourceClaimTemplateSpec{
+					Spec: resourcev1.ResourceClaimSpec{
+						Devices: resourcev1.DeviceClaim{
+							Requests: []resourcev1.DeviceRequest{{
+								Name: "device-request",
+								Exactly: &resourcev1.ExactDeviceRequest{
+									DeviceClassName: "foo.example.com",
+									AllocationMode:  resourcev1.DeviceAllocationModeAll,
+								},
+							}},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating a workload with AllocationMode All")
+			wl := utiltesting.MakeWorkload("test-wl-all-mode", ns.Name).
+				Queue("test-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
```

  can we use this opportunity to start using, https://github.com/kubernetes-sigs/kueue/blob/main/pkg/util/testing/wrappers.go#L623 and https://github.com/kubernetes-sigs/kueue/blob/main/pkg/util/testing/wrappers.go#L637?
  
  I think this change is needed across all the test cases.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-10-10T20:44:33Z

- **test/integration/singlecluster/controller/dra/dra_test.go:625** by [@alaypatel07](https://github.com/alaypatel07) — 2025-10-10T20:44:33Z
```diff
@@ -618,5 +619,385 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 					"DRA resource usage should be a multiple of pod count for webhook validation")
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
+
+		ginkgo.It("Should reject workload with AllocationMode 'All'", func() {
+			ginkgo.By("Creating a ResourceClaimTemplate with AllocationMode All")
+			rct := &resourcev1.ResourceClaimTemplate{
```

  I wonder if we can create a ResourceClaimWrapper to produce these templates/claims in utiltesting: https://github.com/kubernetes-sigs/kueue/blob/5a6b10a710386852e5b57ce91896dd47c583cfcf/pkg/util/testing/wrappers.go#L422
  
  it will be helpful in unit testing as well as integration tests

### Review by [@harche](https://github.com/harche) (commented) — 2025-10-10T22:00:02Z

- **pkg/dra/claims.go:73** by [@harche](https://github.com/harche) — 2025-10-10T22:00:01Z
```diff
@@ -33,38 +33,78 @@ import (
 )
 
 var (
-	ErrDeviceClassNotMapped = errors.New("DeviceClass is not mapped in DRA configuration")
-	ErrResourceClaimInUse   = errors.New("ResourceClaim is in use")
-	ErrClaimSpecNotFound    = errors.New("failed to get claim spec")
+	ErrDeviceClassNotMapped          = errors.New("DeviceClass is not mapped in DRA configuration")
+	ErrResourceClaimInUse            = errors.New("ResourceClaim is in use")
+	ErrClaimSpecNotFound             = errors.New("failed to get claim spec")
+	ErrAllocationModeAllNotSupported = errors.New("AllocationMode 'All' is not supported")
+	ErrCELSelectorsNotSupported      = errors.New("CEL selectors are not supported")
+	ErrDeviceConstraintsNotSupported = errors.New("device constraints (MatchAttribute) are not supported")
+	ErrFirstAvailableNotSupported    = errors.New("FirstAvailable device selection is not supported")
+	ErrAdminAccessNotSupported       = errors.New("AdminAccess is not supported")
+	ErrDeviceConfigNotSupported      = errors.New("device config is not supported")
 )
 
 // countDevicesPerClass returns a resources.Requests representing the
 // total number of devices requested for each DeviceClass inside the provided
-// ResourceClaimSpec.
-func countDevicesPerClass(claimSpec *resourcev1.ResourceClaimSpec) resources.Requests {
+// ResourceClaimSpec. It validates that only supported DRA features are used
+// and returns an error if unsupported features are detected.
+func countDevicesPerClass(claimSpec *resourcev1.ResourceClaimSpec) (resources.Requests, error) {
 	out := resources.Requests{}
 	if claimSpec == nil {
-		return out
+		return out, nil
 	}
+
+	// Check for unsupported device constraints
+	if len(claimSpec.Devices.Constraints) > 0 {
+		return nil, ErrDeviceConstraintsNotSupported
+	}
+
+	// Check for unsupported device config
+	if len(claimSpec.Devices.Config) > 0 {
+		return nil, ErrDeviceConfigNotSupported
+	}
+
 	for _, req := range claimSpec.Devices.Requests {
-		// v1beta2 DeviceRequest has Exactly or FirstAvailable. For Step 1, we
+		// v1 DeviceRequest has Exactly or FirstAvailable. For Step 1, we
 		// preserve existing semantics by only supporting Exactly with Count.
 		var dcName string
 		var q int64
 		if req.Exactly != nil {
+			// Check for CEL selectors
```

  @alaypatel07 I didn't want to repeat `req.Exactly != nil`, would something like following do? 
  
  
  ```go
  switch {
  		case req.FirstAvailable != nil:
  			return nil, fmt.Errorf("%w: FirstAvailable device selection is not supported", ErrUnsupportedDRAFeature)
  		case req.Exactly == nil:
  			continue
  		case len(req.Exactly.Selectors) > 0:
  			return nil, fmt.Errorf("%w: CEL selectors are not supported", ErrUnsupportedDRAFeature)
  		case req.Exactly.AdminAccess != nil && *req.Exactly.AdminAccess:
  			return nil, fmt.Errorf("%w: AdminAccess is not supported", ErrUnsupportedDRAFeature)
  		case req.Exactly.AllocationMode == resourcev1.DeviceAllocationModeAll:
  			return nil, fmt.Errorf("%w: AllocationMode 'All' is not supported", ErrUnsupportedDRAFeature)
  		case req.Exactly.AllocationMode == resourcev1.DeviceAllocationModeExactCount:
  			dcName = req.Exactly.DeviceClassName
  			q = req.Exactly.Count
  		case req.Exactly.AllocationMode == "":
  			dcName = req.Exactly.DeviceClassName
  		default:
  			return nil, fmt.Errorf("%w: unsupported allocation mode: %s", ErrUnsupportedDRAFeature, req.Exactly.AllocationMode)
  		}
  ```

### Review by [@harche](https://github.com/harche) (commented) — 2025-10-10T22:26:47Z

- **pkg/dra/claims.go:73** by [@harche](https://github.com/harche) — 2025-10-10T22:26:47Z
```diff
@@ -33,38 +33,78 @@ import (
 )
 
 var (
-	ErrDeviceClassNotMapped = errors.New("DeviceClass is not mapped in DRA configuration")
-	ErrResourceClaimInUse   = errors.New("ResourceClaim is in use")
-	ErrClaimSpecNotFound    = errors.New("failed to get claim spec")
+	ErrDeviceClassNotMapped          = errors.New("DeviceClass is not mapped in DRA configuration")
+	ErrResourceClaimInUse            = errors.New("ResourceClaim is in use")
+	ErrClaimSpecNotFound             = errors.New("failed to get claim spec")
+	ErrAllocationModeAllNotSupported = errors.New("AllocationMode 'All' is not supported")
+	ErrCELSelectorsNotSupported      = errors.New("CEL selectors are not supported")
+	ErrDeviceConstraintsNotSupported = errors.New("device constraints (MatchAttribute) are not supported")
+	ErrFirstAvailableNotSupported    = errors.New("FirstAvailable device selection is not supported")
+	ErrAdminAccessNotSupported       = errors.New("AdminAccess is not supported")
+	ErrDeviceConfigNotSupported      = errors.New("device config is not supported")
 )
 
 // countDevicesPerClass returns a resources.Requests representing the
 // total number of devices requested for each DeviceClass inside the provided
-// ResourceClaimSpec.
-func countDevicesPerClass(claimSpec *resourcev1.ResourceClaimSpec) resources.Requests {
+// ResourceClaimSpec. It validates that only supported DRA features are used
+// and returns an error if unsupported features are detected.
+func countDevicesPerClass(claimSpec *resourcev1.ResourceClaimSpec) (resources.Requests, error) {
 	out := resources.Requests{}
 	if claimSpec == nil {
-		return out
+		return out, nil
 	}
+
+	// Check for unsupported device constraints
+	if len(claimSpec.Devices.Constraints) > 0 {
+		return nil, ErrDeviceConstraintsNotSupported
+	}
+
+	// Check for unsupported device config
+	if len(claimSpec.Devices.Config) > 0 {
+		return nil, ErrDeviceConfigNotSupported
+	}
+
 	for _, req := range claimSpec.Devices.Requests {
-		// v1beta2 DeviceRequest has Exactly or FirstAvailable. For Step 1, we
+		// v1 DeviceRequest has Exactly or FirstAvailable. For Step 1, we
 		// preserve existing semantics by only supporting Exactly with Count.
 		var dcName string
 		var q int64
 		if req.Exactly != nil {
+			// Check for CEL selectors
```

  Updated, let me know if that works, but I am open to changing it to repeated `req.Exactly != nil`.

### Review by [@harche](https://github.com/harche) (commented) — 2025-10-10T23:13:17Z

- **test/integration/singlecluster/controller/dra/dra_test.go:502** by [@harche](https://github.com/harche) — 2025-10-10T23:13:17Z
```diff
@@ -618,5 +619,385 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 					"DRA resource usage should be a multiple of pod count for webhook validation")
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
+
+		ginkgo.It("Should reject workload with AllocationMode 'All'", func() {
+			ginkgo.By("Creating a ResourceClaimTemplate with AllocationMode All")
+			rct := &resourcev1.ResourceClaimTemplate{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      "all-mode-template",
+					Namespace: ns.Name,
+				},
+				Spec: resourcev1.ResourceClaimTemplateSpec{
+					Spec: resourcev1.ResourceClaimSpec{
+						Devices: resourcev1.DeviceClaim{
+							Requests: []resourcev1.DeviceRequest{{
+								Name: "device-request",
+								Exactly: &resourcev1.ExactDeviceRequest{
+									DeviceClassName: "foo.example.com",
+									AllocationMode:  resourcev1.DeviceAllocationModeAll,
+								},
+							}},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating a workload with AllocationMode All")
+			wl := utiltesting.MakeWorkload("test-wl-all-mode", ns.Name).
+				Queue("test-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
```

  Updated, thanks.

### Review by [@harche](https://github.com/harche) (commented) — 2025-10-10T23:15:11Z

- **test/integration/singlecluster/controller/dra/dra_test.go:625** by [@harche](https://github.com/harche) — 2025-10-10T23:15:11Z
```diff
@@ -618,5 +619,385 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 					"DRA resource usage should be a multiple of pod count for webhook validation")
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
+
+		ginkgo.It("Should reject workload with AllocationMode 'All'", func() {
+			ginkgo.By("Creating a ResourceClaimTemplate with AllocationMode All")
+			rct := &resourcev1.ResourceClaimTemplate{
```

  Sounds like a great idea, but should we tackle this outside this PR? I can create a separate issue to look into it. I am afraid it might escalate the scope of this PR (which is just to harden the validation around unsupported DRA fields/features)

### Review by [@harche](https://github.com/harche) (commented) — 2025-10-11T00:13:36Z

- **test/integration/singlecluster/controller/dra/dra_test.go:625** by [@harche](https://github.com/harche) — 2025-10-11T00:13:36Z
```diff
@@ -618,5 +619,385 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 					"DRA resource usage should be a multiple of pod count for webhook validation")
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
+
+		ginkgo.It("Should reject workload with AllocationMode 'All'", func() {
+			ginkgo.By("Creating a ResourceClaimTemplate with AllocationMode All")
+			rct := &resourcev1.ResourceClaimTemplate{
```

  Added the wrapper, https://github.com/kubernetes-sigs/kueue/pull/7226/files#diff-8c600775ee9d35183196effef13d4df7be756e077046325d17b6214b080cba2bR1823

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-10-13T12:17:14Z

- **test/integration/singlecluster/controller/dra/dra_test.go:625** by [@alaypatel07](https://github.com/alaypatel07) — 2025-10-13T12:17:14Z
```diff
@@ -618,5 +619,385 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 					"DRA resource usage should be a multiple of pod count for webhook validation")
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
+
+		ginkgo.It("Should reject workload with AllocationMode 'All'", func() {
+			ginkgo.By("Creating a ResourceClaimTemplate with AllocationMode All")
+			rct := &resourcev1.ResourceClaimTemplate{
```

  +1. IMHO, since this PR is adding all the permutations of creating resource slices with different features, this is the right moment to add the wrapper. I dont really think it expands the scope. Thanks for tackling that.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-10-13T12:31:05Z

- **pkg/dra/claims.go:71** by [@alaypatel07](https://github.com/alaypatel07) — 2025-10-13T12:31:04Z
```diff
@@ -33,38 +33,65 @@ import (
 )
 
 var (
-	ErrDeviceClassNotMapped = errors.New("DeviceClass is not mapped in DRA configuration")
-	ErrResourceClaimInUse   = errors.New("ResourceClaim is in use")
-	ErrClaimSpecNotFound    = errors.New("failed to get claim spec")
+	ErrDeviceClassNotMapped  = errors.New("DeviceClass is not mapped in DRA configuration")
+	ErrResourceClaimInUse    = errors.New("ResourceClaim is in use")
+	ErrClaimSpecNotFound     = errors.New("failed to get claim spec")
+	ErrUnsupportedDRAFeature = errors.New("unsupported DRA feature")
 )
 
 // countDevicesPerClass returns a resources.Requests representing the
 // total number of devices requested for each DeviceClass inside the provided
-// ResourceClaimSpec.
-func countDevicesPerClass(claimSpec *resourcev1.ResourceClaimSpec) resources.Requests {
+// ResourceClaimSpec. It validates that only supported DRA features are used
+// and returns an error if unsupported features are detected.
+func countDevicesPerClass(claimSpec *resourcev1.ResourceClaimSpec) (resources.Requests, error) {
 	out := resources.Requests{}
 	if claimSpec == nil {
-		return out
+		return out, nil
 	}
+
+	// Check for unsupported device constraints
+	if len(claimSpec.Devices.Constraints) > 0 {
+		return nil, fmt.Errorf("%w: device constraints (MatchAttribute) are not supported", ErrUnsupportedDRAFeature)
+	}
+
+	// Check for unsupported device config
+	if len(claimSpec.Devices.Config) > 0 {
+		return nil, fmt.Errorf("%w: device config is not supported", ErrUnsupportedDRAFeature)
+	}
+
 	for _, req := range claimSpec.Devices.Requests {
-		// v1beta2 DeviceRequest has Exactly or FirstAvailable. For Step 1, we
+		// v1 DeviceRequest has Exactly or FirstAvailable. For Step 1, we
 		// preserve existing semantics by only supporting Exactly with Count.
 		var dcName string
 		var q int64
-		if req.Exactly != nil {
+
+		switch {
+		case req.FirstAvailable != nil:
+			return nil, fmt.Errorf("%w: FirstAvailable device selection is not supported", ErrUnsupportedDRAFeature)
+		case req.Exactly == nil:
```

  ```
      // Exactly specifies the details for a single request that must
  	// be met exactly for the request to be satisfied.
  	//
  	// One of Exactly or FirstAvailable must be set.
  	//
  	// +optional
  	// +oneOf=deviceRequestType
  	Exactly *[ExactDeviceRequest](https://pkg.go.dev/k8s.io/kubernetes@v1.34.0/pkg/apis/resource#ExactDeviceRequest)
  ```
  Is there any value check for this? The docs says that either of FirstAvailable or Exactly has to be set, and if we assert after line 71, FirsAvailable is not set then exactly has to be non-nil

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-10-13T12:35:03Z

- **pkg/dra/claims.go:82** by [@alaypatel07](https://github.com/alaypatel07) — 2025-10-13T12:35:03Z
```diff
@@ -33,38 +33,65 @@ import (
 )
 
 var (
-	ErrDeviceClassNotMapped = errors.New("DeviceClass is not mapped in DRA configuration")
-	ErrResourceClaimInUse   = errors.New("ResourceClaim is in use")
-	ErrClaimSpecNotFound    = errors.New("failed to get claim spec")
+	ErrDeviceClassNotMapped  = errors.New("DeviceClass is not mapped in DRA configuration")
+	ErrResourceClaimInUse    = errors.New("ResourceClaim is in use")
+	ErrClaimSpecNotFound     = errors.New("failed to get claim spec")
+	ErrUnsupportedDRAFeature = errors.New("unsupported DRA feature")
 )
 
 // countDevicesPerClass returns a resources.Requests representing the
 // total number of devices requested for each DeviceClass inside the provided
-// ResourceClaimSpec.
-func countDevicesPerClass(claimSpec *resourcev1.ResourceClaimSpec) resources.Requests {
+// ResourceClaimSpec. It validates that only supported DRA features are used
+// and returns an error if unsupported features are detected.
+func countDevicesPerClass(claimSpec *resourcev1.ResourceClaimSpec) (resources.Requests, error) {
 	out := resources.Requests{}
 	if claimSpec == nil {
-		return out
+		return out, nil
 	}
+
+	// Check for unsupported device constraints
+	if len(claimSpec.Devices.Constraints) > 0 {
+		return nil, fmt.Errorf("%w: device constraints (MatchAttribute) are not supported", ErrUnsupportedDRAFeature)
+	}
+
+	// Check for unsupported device config
+	if len(claimSpec.Devices.Config) > 0 {
+		return nil, fmt.Errorf("%w: device config is not supported", ErrUnsupportedDRAFeature)
+	}
+
 	for _, req := range claimSpec.Devices.Requests {
-		// v1beta2 DeviceRequest has Exactly or FirstAvailable. For Step 1, we
+		// v1 DeviceRequest has Exactly or FirstAvailable. For Step 1, we
 		// preserve existing semantics by only supporting Exactly with Count.
 		var dcName string
 		var q int64
-		if req.Exactly != nil {
+
+		switch {
+		case req.FirstAvailable != nil:
+			return nil, fmt.Errorf("%w: FirstAvailable device selection is not supported", ErrUnsupportedDRAFeature)
+		case req.Exactly == nil:
+			continue
+		case len(req.Exactly.Selectors) > 0:
+			return nil, fmt.Errorf("%w: CEL selectors are not supported", ErrUnsupportedDRAFeature)
+		case req.Exactly.AdminAccess != nil && *req.Exactly.AdminAccess:
+			return nil, fmt.Errorf("%w: AdminAccess is not supported", ErrUnsupportedDRAFeature)
+		case req.Exactly.AllocationMode == resourcev1.DeviceAllocationModeAll:
+			return nil, fmt.Errorf("%w: AllocationMode 'All' is not supported", ErrUnsupportedDRAFeature)
+		case req.Exactly.AllocationMode == resourcev1.DeviceAllocationModeExactCount:
 			dcName = req.Exactly.DeviceClassName
-			if req.Exactly.AllocationMode == resourcev1.DeviceAllocationModeExactCount {
-				q = req.Exactly.Count
-			}
+			q = req.Exactly.Count
+		case req.Exactly.AllocationMode == "":
```

  This case is not needed, Kueue should assume that the defaults are set appropriately during resource admission: https://github.com/kubernetes/kubernetes/blob/f306bb14b4a288cd04c6ddad65a5a91bc1f665f8/pkg/apis/resource/v1/defaults.go#L32-L37.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-10-13T12:38:04Z

- **pkg/util/testing/wrappers.go:450** by [@alaypatel07](https://github.com/alaypatel07) — 2025-10-13T12:38:04Z
```diff
@@ -1816,3 +1818,161 @@ func (p *PodSetAssignmentWrapper) DelayedTopologyRequest(state kueue.DelayedTopo
 func (p *PodSetAssignmentWrapper) Assignment(r corev1.ResourceName, f kueue.ResourceFlavorReference, value string) *PodSetAssignmentWrapper {
 	return p.Flavor(r, f).ResourceUsage(r, value)
 }
+
+// ResourceClaimTemplateWrapper wraps a resourcev1.ResourceClaimTemplate
+type ResourceClaimTemplateWrapper struct {
+	resourcev1.ResourceClaimTemplate
```

  nit: Instead of having separate structs for RCT and RC, there should be a common struct for ResourceClaimSpec and the With* functions use that struct to generate the spec. I think will remove a lot of duplication.

### Review by [@harche](https://github.com/harche) (commented) — 2025-10-14T15:13:02Z

- **pkg/dra/claims.go:71** by [@harche](https://github.com/harche) — 2025-10-14T15:13:02Z
```diff
@@ -33,38 +33,65 @@ import (
 )
 
 var (
-	ErrDeviceClassNotMapped = errors.New("DeviceClass is not mapped in DRA configuration")
-	ErrResourceClaimInUse   = errors.New("ResourceClaim is in use")
-	ErrClaimSpecNotFound    = errors.New("failed to get claim spec")
+	ErrDeviceClassNotMapped  = errors.New("DeviceClass is not mapped in DRA configuration")
+	ErrResourceClaimInUse    = errors.New("ResourceClaim is in use")
+	ErrClaimSpecNotFound     = errors.New("failed to get claim spec")
+	ErrUnsupportedDRAFeature = errors.New("unsupported DRA feature")
 )
 
 // countDevicesPerClass returns a resources.Requests representing the
 // total number of devices requested for each DeviceClass inside the provided
-// ResourceClaimSpec.
-func countDevicesPerClass(claimSpec *resourcev1.ResourceClaimSpec) resources.Requests {
+// ResourceClaimSpec. It validates that only supported DRA features are used
+// and returns an error if unsupported features are detected.
+func countDevicesPerClass(claimSpec *resourcev1.ResourceClaimSpec) (resources.Requests, error) {
 	out := resources.Requests{}
 	if claimSpec == nil {
-		return out
+		return out, nil
 	}
+
+	// Check for unsupported device constraints
+	if len(claimSpec.Devices.Constraints) > 0 {
+		return nil, fmt.Errorf("%w: device constraints (MatchAttribute) are not supported", ErrUnsupportedDRAFeature)
+	}
+
+	// Check for unsupported device config
+	if len(claimSpec.Devices.Config) > 0 {
+		return nil, fmt.Errorf("%w: device config is not supported", ErrUnsupportedDRAFeature)
+	}
+
 	for _, req := range claimSpec.Devices.Requests {
-		// v1beta2 DeviceRequest has Exactly or FirstAvailable. For Step 1, we
+		// v1 DeviceRequest has Exactly or FirstAvailable. For Step 1, we
 		// preserve existing semantics by only supporting Exactly with Count.
 		var dcName string
 		var q int64
-		if req.Exactly != nil {
+
+		switch {
+		case req.FirstAvailable != nil:
+			return nil, fmt.Errorf("%w: FirstAvailable device selection is not supported", ErrUnsupportedDRAFeature)
+		case req.Exactly == nil:
```

  Addressed, thanks.

### Review by [@harche](https://github.com/harche) (commented) — 2025-10-14T15:13:11Z

- **pkg/dra/claims.go:82** by [@harche](https://github.com/harche) — 2025-10-14T15:13:11Z
```diff
@@ -33,38 +33,65 @@ import (
 )
 
 var (
-	ErrDeviceClassNotMapped = errors.New("DeviceClass is not mapped in DRA configuration")
-	ErrResourceClaimInUse   = errors.New("ResourceClaim is in use")
-	ErrClaimSpecNotFound    = errors.New("failed to get claim spec")
+	ErrDeviceClassNotMapped  = errors.New("DeviceClass is not mapped in DRA configuration")
+	ErrResourceClaimInUse    = errors.New("ResourceClaim is in use")
+	ErrClaimSpecNotFound     = errors.New("failed to get claim spec")
+	ErrUnsupportedDRAFeature = errors.New("unsupported DRA feature")
 )
 
 // countDevicesPerClass returns a resources.Requests representing the
 // total number of devices requested for each DeviceClass inside the provided
-// ResourceClaimSpec.
-func countDevicesPerClass(claimSpec *resourcev1.ResourceClaimSpec) resources.Requests {
+// ResourceClaimSpec. It validates that only supported DRA features are used
+// and returns an error if unsupported features are detected.
+func countDevicesPerClass(claimSpec *resourcev1.ResourceClaimSpec) (resources.Requests, error) {
 	out := resources.Requests{}
 	if claimSpec == nil {
-		return out
+		return out, nil
 	}
+
+	// Check for unsupported device constraints
+	if len(claimSpec.Devices.Constraints) > 0 {
+		return nil, fmt.Errorf("%w: device constraints (MatchAttribute) are not supported", ErrUnsupportedDRAFeature)
+	}
+
+	// Check for unsupported device config
+	if len(claimSpec.Devices.Config) > 0 {
+		return nil, fmt.Errorf("%w: device config is not supported", ErrUnsupportedDRAFeature)
+	}
+
 	for _, req := range claimSpec.Devices.Requests {
-		// v1beta2 DeviceRequest has Exactly or FirstAvailable. For Step 1, we
+		// v1 DeviceRequest has Exactly or FirstAvailable. For Step 1, we
 		// preserve existing semantics by only supporting Exactly with Count.
 		var dcName string
 		var q int64
-		if req.Exactly != nil {
+
+		switch {
+		case req.FirstAvailable != nil:
+			return nil, fmt.Errorf("%w: FirstAvailable device selection is not supported", ErrUnsupportedDRAFeature)
+		case req.Exactly == nil:
+			continue
+		case len(req.Exactly.Selectors) > 0:
+			return nil, fmt.Errorf("%w: CEL selectors are not supported", ErrUnsupportedDRAFeature)
+		case req.Exactly.AdminAccess != nil && *req.Exactly.AdminAccess:
+			return nil, fmt.Errorf("%w: AdminAccess is not supported", ErrUnsupportedDRAFeature)
+		case req.Exactly.AllocationMode == resourcev1.DeviceAllocationModeAll:
+			return nil, fmt.Errorf("%w: AllocationMode 'All' is not supported", ErrUnsupportedDRAFeature)
+		case req.Exactly.AllocationMode == resourcev1.DeviceAllocationModeExactCount:
 			dcName = req.Exactly.DeviceClassName
-			if req.Exactly.AllocationMode == resourcev1.DeviceAllocationModeExactCount {
-				q = req.Exactly.Count
-			}
+			q = req.Exactly.Count
+		case req.Exactly.AllocationMode == "":
```

  Addressed, thanks.

### Review by [@harche](https://github.com/harche) (commented) — 2025-10-14T15:14:06Z

- **pkg/util/testing/wrappers.go:450** by [@harche](https://github.com/harche) — 2025-10-14T15:14:06Z
```diff
@@ -1816,3 +1818,161 @@ func (p *PodSetAssignmentWrapper) DelayedTopologyRequest(state kueue.DelayedTopo
 func (p *PodSetAssignmentWrapper) Assignment(r corev1.ResourceName, f kueue.ResourceFlavorReference, value string) *PodSetAssignmentWrapper {
 	return p.Flavor(r, f).ResourceUsage(r, value)
 }
+
+// ResourceClaimTemplateWrapper wraps a resourcev1.ResourceClaimTemplate
+type ResourceClaimTemplateWrapper struct {
+	resourcev1.ResourceClaimTemplate
```

  Between `ResourceClaimTemplateWrapper` and `ResourceClaimWrapper`, there's now a shared ResourceClaimSpecBuilder that both wrappers use, let me know if that aligns with what you had in mind.

### Review by [@harche](https://github.com/harche) (commented) — 2025-10-15T17:40:58Z

- **pkg/util/testing/wrappers.go:450** by [@harche](https://github.com/harche) — 2025-10-15T17:40:58Z
```diff
@@ -1816,3 +1818,161 @@ func (p *PodSetAssignmentWrapper) DelayedTopologyRequest(state kueue.DelayedTopo
 func (p *PodSetAssignmentWrapper) Assignment(r corev1.ResourceName, f kueue.ResourceFlavorReference, value string) *PodSetAssignmentWrapper {
 	return p.Flavor(r, f).ResourceUsage(r, value)
 }
+
+// ResourceClaimTemplateWrapper wraps a resourcev1.ResourceClaimTemplate
+type ResourceClaimTemplateWrapper struct {
+	resourcev1.ResourceClaimTemplate
```

  cc @alaypatel07

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-16T14:12:30Z

- **pkg/controller/core/workload_controller_test.go:436** by [@mimowo](https://github.com/mimowo) — 2025-10-16T14:12:30Z
```diff
@@ -426,20 +426,11 @@ func TestReconcile(t *testing.T) {
 					ResourceClaim("gpu", "rc1").
 					Obj()).
 				Obj(),
-			resourceClaims: []*resourcev1.ResourceClaim{{
-				ObjectMeta: metav1.ObjectMeta{Name: "rc1", Namespace: "ns"},
-				Spec: resourcev1.ResourceClaimSpec{
-					Devices: resourcev1.DeviceClaim{
-						Requests: []resourcev1.DeviceRequest{{
-							Exactly: &resourcev1.ExactDeviceRequest{
-								DeviceClassName: "gpu.example.com",
-								AllocationMode:  resourcev1.DeviceAllocationModeExactCount,
-								Count:           1,
-							},
-						}},
-					},
-				},
-			}},
+			resourceClaims: []*resourcev1.ResourceClaim{
+				utiltesting.MakeResourceClaim("rc1", "ns").
+					DeviceRequest("device-request", "gpu.example.com", 1).
+					Obj(),
+			},
```

  Actually could you please send a preaparatory non-functional PR just for the cleanup? 
  
  So that we don't mix cleanups with features or bugfixes.
  
  It will make the diff here smaller for the ease of reviewing / focusing on the user-changing experience.

### Review by [@harche](https://github.com/harche) (commented) — 2025-10-16T14:25:44Z

- **pkg/controller/core/workload_controller_test.go:436** by [@harche](https://github.com/harche) — 2025-10-16T14:25:44Z
```diff
@@ -426,20 +426,11 @@ func TestReconcile(t *testing.T) {
 					ResourceClaim("gpu", "rc1").
 					Obj()).
 				Obj(),
-			resourceClaims: []*resourcev1.ResourceClaim{{
-				ObjectMeta: metav1.ObjectMeta{Name: "rc1", Namespace: "ns"},
-				Spec: resourcev1.ResourceClaimSpec{
-					Devices: resourcev1.DeviceClaim{
-						Requests: []resourcev1.DeviceRequest{{
-							Exactly: &resourcev1.ExactDeviceRequest{
-								DeviceClassName: "gpu.example.com",
-								AllocationMode:  resourcev1.DeviceAllocationModeExactCount,
-								Count:           1,
-							},
-						}},
-					},
-				},
-			}},
+			resourceClaims: []*resourcev1.ResourceClaim{
+				utiltesting.MakeResourceClaim("rc1", "ns").
+					DeviceRequest("device-request", "gpu.example.com", 1).
+					Obj(),
+			},
```

  Thanks @mimowo,  I have brought this up earlier, https://github.com/kubernetes-sigs/kueue/pull/7226#discussion_r2422245521 
  
  @alaypatel07 are you okay with splitting the refactoring of tests utils from this PR?

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-10-16T14:32:23Z

- **pkg/controller/core/workload_controller_test.go:436** by [@alaypatel07](https://github.com/alaypatel07) — 2025-10-16T14:32:23Z
```diff
@@ -426,20 +426,11 @@ func TestReconcile(t *testing.T) {
 					ResourceClaim("gpu", "rc1").
 					Obj()).
 				Obj(),
-			resourceClaims: []*resourcev1.ResourceClaim{{
-				ObjectMeta: metav1.ObjectMeta{Name: "rc1", Namespace: "ns"},
-				Spec: resourcev1.ResourceClaimSpec{
-					Devices: resourcev1.DeviceClaim{
-						Requests: []resourcev1.DeviceRequest{{
-							Exactly: &resourcev1.ExactDeviceRequest{
-								DeviceClassName: "gpu.example.com",
-								AllocationMode:  resourcev1.DeviceAllocationModeExactCount,
-								Count:           1,
-							},
-						}},
-					},
-				},
-			}},
+			resourceClaims: []*resourcev1.ResourceClaim{
+				utiltesting.MakeResourceClaim("rc1", "ns").
+					DeviceRequest("device-request", "gpu.example.com", 1).
+					Obj(),
+			},
```

  @harche yes, I think what @mimowo is suggesting is little different from earlier conversation, instead of following up on the refactor, I think he wants the refactor to go in first as a separate PR, I am okay with that. Correct me if I am wrong.

### Review by [@harche](https://github.com/harche) (commented) — 2025-10-16T20:45:07Z

- **pkg/controller/core/workload_controller_test.go:436** by [@harche](https://github.com/harche) — 2025-10-16T20:45:06Z
```diff
@@ -426,20 +426,11 @@ func TestReconcile(t *testing.T) {
 					ResourceClaim("gpu", "rc1").
 					Obj()).
 				Obj(),
-			resourceClaims: []*resourcev1.ResourceClaim{{
-				ObjectMeta: metav1.ObjectMeta{Name: "rc1", Namespace: "ns"},
-				Spec: resourcev1.ResourceClaimSpec{
-					Devices: resourcev1.DeviceClaim{
-						Requests: []resourcev1.DeviceRequest{{
-							Exactly: &resourcev1.ExactDeviceRequest{
-								DeviceClassName: "gpu.example.com",
-								AllocationMode:  resourcev1.DeviceAllocationModeExactCount,
-								Count:           1,
-							},
-						}},
-					},
-				},
-			}},
+			resourceClaims: []*resourcev1.ResourceClaim{
+				utiltesting.MakeResourceClaim("rc1", "ns").
+					DeviceRequest("device-request", "gpu.example.com", 1).
+					Obj(),
+			},
```

  Thanks @alaypatel07 and @mimowo, I have raised https://github.com/kubernetes-sigs/kueue/pull/7300 to add wrapper for existing tests cases only. 
  
  We will deal with this PR once that one is merged. 
  
  /hold

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-20T07:22:13Z

- **pkg/controller/core/workload_controller_test.go:436** by [@mimowo](https://github.com/mimowo) — 2025-10-20T07:22:13Z
```diff
@@ -426,20 +426,11 @@ func TestReconcile(t *testing.T) {
 					ResourceClaim("gpu", "rc1").
 					Obj()).
 				Obj(),
-			resourceClaims: []*resourcev1.ResourceClaim{{
-				ObjectMeta: metav1.ObjectMeta{Name: "rc1", Namespace: "ns"},
-				Spec: resourcev1.ResourceClaimSpec{
-					Devices: resourcev1.DeviceClaim{
-						Requests: []resourcev1.DeviceRequest{{
-							Exactly: &resourcev1.ExactDeviceRequest{
-								DeviceClassName: "gpu.example.com",
-								AllocationMode:  resourcev1.DeviceAllocationModeExactCount,
-								Count:           1,
-							},
-						}},
-					},
-				},
-			}},
+			resourceClaims: []*resourcev1.ResourceClaim{
+				utiltesting.MakeResourceClaim("rc1", "ns").
+					DeviceRequest("device-request", "gpu.example.com", 1).
+					Obj(),
+			},
```

  I merged the other PR already, please rebase, let me unhold in the meanwhile, it will not merge anyway while not rebased.
  /unhold

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-20T14:57:21Z

- **pkg/dra/claims_test.go:240** by [@mimowo](https://github.com/mimowo) — 2025-10-20T14:57:21Z
```diff
@@ -220,6 +220,132 @@ func Test_GetResourceRequests(t *testing.T) {
 			},
 			want: map[kueue.PodSetReference]corev1.ResourceList{"main": {"res-1": resource.MustParse("2")}},
 		},
+		{
+			name: "AllocationMode All returns error",
+			extraObjects: []runtime.Object{
+				utiltesting.MakeResourceClaimTemplate("claim-tmpl-all", "ns1").
+					DeviceRequest("device-request", "test-deviceclass-1", 0).
+					AllocationModeAll().
+					Obj(),
+			},
+			modifyWL: func(w *kueue.Workload) {
+				w.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+					{Name: "req-all", ResourceClaimTemplateName: ptr.To("claim-tmpl-all")},
+				}
+			},
+			lookup: func(dc corev1.ResourceName) (corev1.ResourceName, bool) {
+				if dc == "test-deviceclass-1" {
+					return "res-1", true
+				}
+				return "", false
```

  This looks very verbose and repeatable, can we replace it somehow with some default lookups maybe?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-20T14:58:50Z

- **pkg/dra/claims_test.go:263** by [@mimowo](https://github.com/mimowo) — 2025-10-20T14:58:50Z
```diff
@@ -220,6 +220,132 @@ func Test_GetResourceRequests(t *testing.T) {
 			},
 			want: map[kueue.PodSetReference]corev1.ResourceList{"main": {"res-1": resource.MustParse("2")}},
 		},
+		{
+			name: "AllocationMode All returns error",
+			extraObjects: []runtime.Object{
+				utiltesting.MakeResourceClaimTemplate("claim-tmpl-all", "ns1").
+					DeviceRequest("device-request", "test-deviceclass-1", 0).
+					AllocationModeAll().
+					Obj(),
+			},
+			modifyWL: func(w *kueue.Workload) {
+				w.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+					{Name: "req-all", ResourceClaimTemplateName: ptr.To("claim-tmpl-all")},
+				}
+			},
+			lookup: func(dc corev1.ResourceName) (corev1.ResourceName, bool) {
+				if dc == "test-deviceclass-1" {
+					return "res-1", true
+				}
+				return "", false
+			},
+			wantErr: true,
+		},
+		{
+			name: "CEL selectors returns error",
+			extraObjects: []runtime.Object{
+				utiltesting.MakeResourceClaimTemplate("claim-tmpl-cel", "ns1").
+					DeviceRequest("req", "test-deviceclass-1", 1).
+					WithCELSelectors("device.driver == \"test-driver\"").
+					Obj(),
+			},
+			modifyWL: func(w *kueue.Workload) {
+				w.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+					{Name: "req-cel", ResourceClaimTemplateName: ptr.To("claim-tmpl-cel")},
+				}
+			},
+			lookup: func(dc corev1.ResourceName) (corev1.ResourceName, bool) {
+				if dc == "test-deviceclass-1" {
+					return "res-1", true
+				}
+				return "", false
+			},
+			wantErr: true,
```

  Instead of asserting true/false it is better to assert on specific validation error, please check https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobs/jobset/jobset_webhook_test.go#L49
  
  I know it was like this before, but I would like to use it as an occasion to cleanup

### Review by [@harche](https://github.com/harche) (commented) — 2025-10-20T15:39:00Z

- **pkg/dra/claims_test.go:240** by [@harche](https://github.com/harche) — 2025-10-20T15:39:00Z
```diff
@@ -220,6 +220,132 @@ func Test_GetResourceRequests(t *testing.T) {
 			},
 			want: map[kueue.PodSetReference]corev1.ResourceList{"main": {"res-1": resource.MustParse("2")}},
 		},
+		{
+			name: "AllocationMode All returns error",
+			extraObjects: []runtime.Object{
+				utiltesting.MakeResourceClaimTemplate("claim-tmpl-all", "ns1").
+					DeviceRequest("device-request", "test-deviceclass-1", 0).
+					AllocationModeAll().
+					Obj(),
+			},
+			modifyWL: func(w *kueue.Workload) {
+				w.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+					{Name: "req-all", ResourceClaimTemplateName: ptr.To("claim-tmpl-all")},
+				}
+			},
+			lookup: func(dc corev1.ResourceName) (corev1.ResourceName, bool) {
+				if dc == "test-deviceclass-1" {
+					return "res-1", true
+				}
+				return "", false
```

  Added a common `defaultLookup` function

### Review by [@harche](https://github.com/harche) (commented) — 2025-10-20T15:39:21Z

- **pkg/dra/claims_test.go:263** by [@harche](https://github.com/harche) — 2025-10-20T15:39:21Z
```diff
@@ -220,6 +220,132 @@ func Test_GetResourceRequests(t *testing.T) {
 			},
 			want: map[kueue.PodSetReference]corev1.ResourceList{"main": {"res-1": resource.MustParse("2")}},
 		},
+		{
+			name: "AllocationMode All returns error",
+			extraObjects: []runtime.Object{
+				utiltesting.MakeResourceClaimTemplate("claim-tmpl-all", "ns1").
+					DeviceRequest("device-request", "test-deviceclass-1", 0).
+					AllocationModeAll().
+					Obj(),
+			},
+			modifyWL: func(w *kueue.Workload) {
+				w.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+					{Name: "req-all", ResourceClaimTemplateName: ptr.To("claim-tmpl-all")},
+				}
+			},
+			lookup: func(dc corev1.ResourceName) (corev1.ResourceName, bool) {
+				if dc == "test-deviceclass-1" {
+					return "res-1", true
+				}
+				return "", false
+			},
+			wantErr: true,
+		},
+		{
+			name: "CEL selectors returns error",
+			extraObjects: []runtime.Object{
+				utiltesting.MakeResourceClaimTemplate("claim-tmpl-cel", "ns1").
+					DeviceRequest("req", "test-deviceclass-1", 1).
+					WithCELSelectors("device.driver == \"test-driver\"").
+					Obj(),
+			},
+			modifyWL: func(w *kueue.Workload) {
+				w.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+					{Name: "req-cel", ResourceClaimTemplateName: ptr.To("claim-tmpl-cel")},
+				}
+			},
+			lookup: func(dc corev1.ResourceName) (corev1.ResourceName, bool) {
+				if dc == "test-deviceclass-1" {
+					return "res-1", true
+				}
+				return "", false
+			},
+			wantErr: true,
```

  Thanks, updated to assert exact error.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-20T15:56:54Z

- **pkg/dra/claims.go:47** by [@mimowo](https://github.com/mimowo) — 2025-10-20T15:56:54Z
```diff
@@ -33,38 +33,69 @@ import (
 )
 
 var (
-	ErrDeviceClassNotMapped = errors.New("DeviceClass is not mapped in DRA configuration")
-	ErrResourceClaimInUse   = errors.New("ResourceClaim is in use")
-	ErrClaimSpecNotFound    = errors.New("failed to get claim spec")
+	ErrDeviceClassNotMapped  = errors.New("DeviceClass is not mapped in DRA configuration")
+	ErrResourceClaimInUse    = errors.New("ResourceClaim is in use")
+	ErrClaimSpecNotFound     = errors.New("failed to get claim spec")
+	ErrUnsupportedDRAFeature = errors.New("unsupported DRA feature")
+
+	// Specific unsupported DRA feature errors
+	ErrUnsupportedDRADeviceConstraints = errors.New("device constraints (MatchAttribute) are not supported")
+	ErrUnsupportedDRADeviceConfig      = errors.New("device config is not supported")
+	ErrUnsupportedDRAFirstAvailable    = errors.New("FirstAvailable device selection is not supported")
+	ErrUnsupportedDRACELSelectors      = errors.New("CEL selectors are not supported")
+	ErrUnsupportedDRAAdminAccess       = errors.New("AdminAccess is not supported")
+	ErrUnsupportedDRAAllocationModeAll = errors.New("AllocationMode 'All' is not supported")
```

  Do these need to be exposed? If not then unexport (lowercase)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-20T16:02:23Z

- **pkg/dra/claims_test.go:365** by [@mimowo](https://github.com/mimowo) — 2025-10-20T16:02:23Z
```diff
@@ -258,12 +352,18 @@ func Test_GetResourceRequests(t *testing.T) {
 			}
 
 			got, err := dra.GetResourceRequestsForResourceClaimTemplates(context.Background(), baseClient, wlCopy)
-			if (err != nil) != tc.wantErr {
-				t.Fatalf("unexpected error status: gotErr=%v wantErr=%v, err=%v", err != nil, tc.wantErr, err)
-			}
-			if tc.wantErr {
+			if tc.wantErr != nil {
+				if err == nil {
+					t.Fatalf("expected error but got none")
+				}
+				if !errors.Is(err, tc.wantErr) {
+					t.Fatalf("unexpected error: got=%v, want=%v", err, tc.wantErr)
+				}
 				return
 			}
+			if err != nil {
+				t.Fatalf("unexpected error: %v", err)
+			}
```

  I would prefer to assert errors as we do here, using cmp.Diff: https://github.com/kubernetes-sigs/kueue/blob/1fa14630e933358d77bed40e92bd105f6c101e7c/pkg/controller/jobs/jobset/jobset_webhook_test.go#L273-L291
  
  Would it work here? I'm not sure maybe it works better if the validation errors are using field.ErrorList type.

### Review by [@harche](https://github.com/harche) (commented) — 2025-10-20T16:32:11Z

- **pkg/dra/claims.go:47** by [@harche](https://github.com/harche) — 2025-10-20T16:32:11Z
```diff
@@ -33,38 +33,69 @@ import (
 )
 
 var (
-	ErrDeviceClassNotMapped = errors.New("DeviceClass is not mapped in DRA configuration")
-	ErrResourceClaimInUse   = errors.New("ResourceClaim is in use")
-	ErrClaimSpecNotFound    = errors.New("failed to get claim spec")
+	ErrDeviceClassNotMapped  = errors.New("DeviceClass is not mapped in DRA configuration")
+	ErrResourceClaimInUse    = errors.New("ResourceClaim is in use")
+	ErrClaimSpecNotFound     = errors.New("failed to get claim spec")
+	ErrUnsupportedDRAFeature = errors.New("unsupported DRA feature")
+
+	// Specific unsupported DRA feature errors
+	ErrUnsupportedDRADeviceConstraints = errors.New("device constraints (MatchAttribute) are not supported")
+	ErrUnsupportedDRADeviceConfig      = errors.New("device config is not supported")
+	ErrUnsupportedDRAFirstAvailable    = errors.New("FirstAvailable device selection is not supported")
+	ErrUnsupportedDRACELSelectors      = errors.New("CEL selectors are not supported")
+	ErrUnsupportedDRAAdminAccess       = errors.New("AdminAccess is not supported")
+	ErrUnsupportedDRAAllocationModeAll = errors.New("AllocationMode 'All' is not supported")
```

  They are used in integration tests, besides that, I am not sure having errors exported causes any harm, future callers might be able to verify if the errors belongs to any of the DRA related errors.

### Review by [@harche](https://github.com/harche) (commented) — 2025-10-20T16:37:25Z

- **pkg/dra/claims_test.go:365** by [@harche](https://github.com/harche) — 2025-10-20T16:37:25Z
```diff
@@ -258,12 +352,18 @@ func Test_GetResourceRequests(t *testing.T) {
 			}
 
 			got, err := dra.GetResourceRequestsForResourceClaimTemplates(context.Background(), baseClient, wlCopy)
-			if (err != nil) != tc.wantErr {
-				t.Fatalf("unexpected error status: gotErr=%v wantErr=%v, err=%v", err != nil, tc.wantErr, err)
-			}
-			if tc.wantErr {
+			if tc.wantErr != nil {
+				if err == nil {
+					t.Fatalf("expected error but got none")
+				}
+				if !errors.Is(err, tc.wantErr) {
+					t.Fatalf("unexpected error: got=%v, want=%v", err, tc.wantErr)
+				}
 				return
 			}
+			if err != nil {
+				t.Fatalf("unexpected error: %v", err)
+			}
```

  The current tests use `errors.Is`, this is exactly what we want. IMO, achieving this in any other way would not be more optimal than simply using `errors.Is`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-20T16:45:02Z

- **pkg/dra/claims.go:47** by [@mimowo](https://github.com/mimowo) — 2025-10-20T16:45:02Z
```diff
@@ -33,38 +33,69 @@ import (
 )
 
 var (
-	ErrDeviceClassNotMapped = errors.New("DeviceClass is not mapped in DRA configuration")
-	ErrResourceClaimInUse   = errors.New("ResourceClaim is in use")
-	ErrClaimSpecNotFound    = errors.New("failed to get claim spec")
+	ErrDeviceClassNotMapped  = errors.New("DeviceClass is not mapped in DRA configuration")
+	ErrResourceClaimInUse    = errors.New("ResourceClaim is in use")
+	ErrClaimSpecNotFound     = errors.New("failed to get claim spec")
+	ErrUnsupportedDRAFeature = errors.New("unsupported DRA feature")
+
+	// Specific unsupported DRA feature errors
+	ErrUnsupportedDRADeviceConstraints = errors.New("device constraints (MatchAttribute) are not supported")
+	ErrUnsupportedDRADeviceConfig      = errors.New("device config is not supported")
+	ErrUnsupportedDRAFirstAvailable    = errors.New("FirstAvailable device selection is not supported")
+	ErrUnsupportedDRACELSelectors      = errors.New("CEL selectors are not supported")
+	ErrUnsupportedDRAAdminAccess       = errors.New("AdminAccess is not supported")
+	ErrUnsupportedDRAAllocationModeAll = errors.New("AllocationMode 'All' is not supported")
```

  We can always export them later if needed. If they are unexported it is immediately obvious which are used or not outside the package for example during review in GH. Also, sometimes exported constants become unused but linters don't complain, so making them unused is easy to miss during review/ code changing.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-10-22T06:47:43Z

- **pkg/dra/claims.go:47** by [@alaypatel07](https://github.com/alaypatel07) — 2025-10-22T06:47:43Z
```diff
@@ -33,38 +33,69 @@ import (
 )
 
 var (
-	ErrDeviceClassNotMapped = errors.New("DeviceClass is not mapped in DRA configuration")
-	ErrResourceClaimInUse   = errors.New("ResourceClaim is in use")
-	ErrClaimSpecNotFound    = errors.New("failed to get claim spec")
+	ErrDeviceClassNotMapped  = errors.New("DeviceClass is not mapped in DRA configuration")
+	ErrResourceClaimInUse    = errors.New("ResourceClaim is in use")
+	ErrClaimSpecNotFound     = errors.New("failed to get claim spec")
+	ErrUnsupportedDRAFeature = errors.New("unsupported DRA feature")
+
+	// Specific unsupported DRA feature errors
+	ErrUnsupportedDRADeviceConstraints = errors.New("device constraints (MatchAttribute) are not supported")
+	ErrUnsupportedDRADeviceConfig      = errors.New("device config is not supported")
+	ErrUnsupportedDRAFirstAvailable    = errors.New("FirstAvailable device selection is not supported")
+	ErrUnsupportedDRACELSelectors      = errors.New("CEL selectors are not supported")
+	ErrUnsupportedDRAAdminAccess       = errors.New("AdminAccess is not supported")
+	ErrUnsupportedDRAAllocationModeAll = errors.New("AllocationMode 'All' is not supported")
```

  +1

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-10-22T06:52:22Z

- **pkg/dra/claims_test.go:365** by [@alaypatel07](https://github.com/alaypatel07) — 2025-10-22T06:52:22Z
```diff
@@ -258,12 +352,18 @@ func Test_GetResourceRequests(t *testing.T) {
 			}
 
 			got, err := dra.GetResourceRequestsForResourceClaimTemplates(context.Background(), baseClient, wlCopy)
-			if (err != nil) != tc.wantErr {
-				t.Fatalf("unexpected error status: gotErr=%v wantErr=%v, err=%v", err != nil, tc.wantErr, err)
-			}
-			if tc.wantErr {
+			if tc.wantErr != nil {
+				if err == nil {
+					t.Fatalf("expected error but got none")
+				}
+				if !errors.Is(err, tc.wantErr) {
+					t.Fatalf("unexpected error: got=%v, want=%v", err, tc.wantErr)
+				}
 				return
 			}
+			if err != nil {
+				t.Fatalf("unexpected error: %v", err)
+			}
```

  my 2c would be that when the exact error type is available, using `errors.Is` should be prefered. When errors are governed by outside libraries like `field.Invalid` it might make more sense to use cmp.Diff

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-10-22T06:54:33Z

- **pkg/dra/claims.go:47** by [@alaypatel07](https://github.com/alaypatel07) — 2025-10-22T06:54:32Z
```diff
@@ -33,38 +33,69 @@ import (
 )
 
 var (
-	ErrDeviceClassNotMapped = errors.New("DeviceClass is not mapped in DRA configuration")
-	ErrResourceClaimInUse   = errors.New("ResourceClaim is in use")
-	ErrClaimSpecNotFound    = errors.New("failed to get claim spec")
+	ErrDeviceClassNotMapped  = errors.New("DeviceClass is not mapped in DRA configuration")
+	ErrResourceClaimInUse    = errors.New("ResourceClaim is in use")
+	ErrClaimSpecNotFound     = errors.New("failed to get claim spec")
+	ErrUnsupportedDRAFeature = errors.New("unsupported DRA feature")
+
+	// Specific unsupported DRA feature errors
+	ErrUnsupportedDRADeviceConstraints = errors.New("device constraints (MatchAttribute) are not supported")
+	ErrUnsupportedDRADeviceConfig      = errors.New("device config is not supported")
+	ErrUnsupportedDRAFirstAvailable    = errors.New("FirstAvailable device selection is not supported")
+	ErrUnsupportedDRACELSelectors      = errors.New("CEL selectors are not supported")
+	ErrUnsupportedDRAAdminAccess       = errors.New("AdminAccess is not supported")
+	ErrUnsupportedDRAAllocationModeAll = errors.New("AllocationMode 'All' is not supported")
```

  @mimowo if the concern is support these variable as a public library because folks will start using these exported values, then an alternative would be to hide it behind an internal package such that the errors are only available to kueue

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-22T07:00:38Z

- **pkg/dra/claims.go:47** by [@mimowo](https://github.com/mimowo) — 2025-10-22T07:00:38Z
```diff
@@ -33,38 +33,69 @@ import (
 )
 
 var (
-	ErrDeviceClassNotMapped = errors.New("DeviceClass is not mapped in DRA configuration")
-	ErrResourceClaimInUse   = errors.New("ResourceClaim is in use")
-	ErrClaimSpecNotFound    = errors.New("failed to get claim spec")
+	ErrDeviceClassNotMapped  = errors.New("DeviceClass is not mapped in DRA configuration")
+	ErrResourceClaimInUse    = errors.New("ResourceClaim is in use")
+	ErrClaimSpecNotFound     = errors.New("failed to get claim spec")
+	ErrUnsupportedDRAFeature = errors.New("unsupported DRA feature")
+
+	// Specific unsupported DRA feature errors
+	ErrUnsupportedDRADeviceConstraints = errors.New("device constraints (MatchAttribute) are not supported")
+	ErrUnsupportedDRADeviceConfig      = errors.New("device config is not supported")
+	ErrUnsupportedDRAFirstAvailable    = errors.New("FirstAvailable device selection is not supported")
+	ErrUnsupportedDRACELSelectors      = errors.New("CEL selectors are not supported")
+	ErrUnsupportedDRAAdminAccess       = errors.New("AdminAccess is not supported")
+	ErrUnsupportedDRAAllocationModeAll = errors.New("AllocationMode 'All' is not supported")
```

  Im not concerned about external use at this point. As mentioned above just so that static analysis tools can help us find out when sone variables are not used as the code evolves.

### Review by [@harche](https://github.com/harche) (commented) — 2025-10-22T16:32:15Z

- **pkg/dra/claims.go:47** by [@harche](https://github.com/harche) — 2025-10-22T16:32:15Z
```diff
@@ -33,38 +33,69 @@ import (
 )
 
 var (
-	ErrDeviceClassNotMapped = errors.New("DeviceClass is not mapped in DRA configuration")
-	ErrResourceClaimInUse   = errors.New("ResourceClaim is in use")
-	ErrClaimSpecNotFound    = errors.New("failed to get claim spec")
+	ErrDeviceClassNotMapped  = errors.New("DeviceClass is not mapped in DRA configuration")
+	ErrResourceClaimInUse    = errors.New("ResourceClaim is in use")
+	ErrClaimSpecNotFound     = errors.New("failed to get claim spec")
+	ErrUnsupportedDRAFeature = errors.New("unsupported DRA feature")
+
+	// Specific unsupported DRA feature errors
+	ErrUnsupportedDRADeviceConstraints = errors.New("device constraints (MatchAttribute) are not supported")
+	ErrUnsupportedDRADeviceConfig      = errors.New("device config is not supported")
+	ErrUnsupportedDRAFirstAvailable    = errors.New("FirstAvailable device selection is not supported")
+	ErrUnsupportedDRACELSelectors      = errors.New("CEL selectors are not supported")
+	ErrUnsupportedDRAAdminAccess       = errors.New("AdminAccess is not supported")
+	ErrUnsupportedDRAAllocationModeAll = errors.New("AllocationMode 'All' is not supported")
```

  Thanks @mimowo and @alaypatel07 for that perspective. I have unexported those DRA related errors.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-22T16:57:28Z

- **pkg/dra/claims_test.go:365** by [@mimowo](https://github.com/mimowo) — 2025-10-22T16:57:28Z
```diff
@@ -258,12 +352,18 @@ func Test_GetResourceRequests(t *testing.T) {
 			}
 
 			got, err := dra.GetResourceRequestsForResourceClaimTemplates(context.Background(), baseClient, wlCopy)
-			if (err != nil) != tc.wantErr {
-				t.Fatalf("unexpected error status: gotErr=%v wantErr=%v, err=%v", err != nil, tc.wantErr, err)
-			}
-			if tc.wantErr {
+			if tc.wantErr != nil {
+				if err == nil {
+					t.Fatalf("expected error but got none")
+				}
+				if !errors.Is(err, tc.wantErr) {
+					t.Fatalf("unexpected error: got=%v, want=%v", err, tc.wantErr)
+				}
 				return
 			}
+			if err != nil {
+				t.Fatalf("unexpected error: %v", err)
+			}
```

  Yeah, I think errors.Is is the generic golang mechanism for generic errors. However, validation errors are structured, and so in kueue we prefer to assert on the in the structured manner, which generates a better diff using cmp.Diff. However, since this work is on top of preexisiting code then refactoring could be a separete follow up / or preparatory PR.
  
  Let me open an issue: https://github.com/kubernetes-sigs/kueue/issues/7343
  
  I don't see it as a blocker for this PR, but I would like to consider.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-24T09:01:09Z

/lgtm
/approve
Looks better than before, thank you!
