# PR #7246: Introduce WithLooseOnApply option for PatchAdmissionStatus

**Summary**: Introduce WithLooseOnApply option for PatchAdmissionStatus

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7246

**Last updated**: 2025-10-15T13:48:37Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `feat/7035-WorkloadRequestUseMergePatch-to-beta` → `main`
- **Created**: 2025-10-13T12:51:33Z
- **Updated**: 2025-10-15T13:48:37Z
- **Merged**: 2025-10-15T13:33:38Z
- **Closed**: 2025-10-15T13:33:38Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 3   **+44 / -12**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
As stated [here](https://github.com/kubernetes-sigs/kueue/issues/7035#issuecomment-3384483797)

> enabling the feature risks dropping some conditions set on the Workload by a controller, or user.

Thus to lower the risk to minimum we introduce an option that preserves strict setting for SSA.
That keeps the functionality unchanged when used with `WorkloadRequestUseMergePatch` disabled and for enabled let scheduler be more strict on patch and let it retry on the next scheduling cycle.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to #7035

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
WorkloadRequestUseMergePatch: use "strict" mode for admission patches during scheduling which
sends the ResourceVersion of the workload being admitted for comparing by kube-apiserver. 
This fixes the race-condition issue that Workload conditions added concurrently by other controllers
could be removed during scheduling.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-10-13T12:51:41Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | d64dff109c5b722d39f627ad994a9d015e8cb6b5 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68ee35ba2970260007cffd08 |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-13T12:57:43Z

Please also add a release note

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-15T13:05:28Z

Thanks 👍 
/lgtm
/approve
/cherrypick release-0.14

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-10-15T13:05:31Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.14` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7246#issuecomment-3406361703):

>Thanks 👍 
>/lgtm
>/approve
>/cherrypick release-0.14


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-15T13:05:39Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7246#issuecomment-3406361703" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7246#" title="Author self-approved">mszadkow</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-15T13:05:39Z

LGTM label has been added.  <details>Git tree hash: a15d5e4b1bb22cc8643db3e0d1042f3cca620f39</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-15T13:10:38Z

We need to adjust the release note to be user-oriented
/release-note-edit
```release-note
WorkloadRequestUseMergePatch: use "strict" for admission patches during scheduling by comparing 
ResourceVersion of the workload. This prevents occasional dropping of conditions added by other
controllers.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-15T13:11:50Z

/release-note-edit
```release-note
WorkloadRequestUseMergePatch: use "strict" mode for admission patches during scheduling which
sends the ResourceVersion of the workload being admitted for comparing by kube-apiserver. 
This prevents occasional dropping of conditions added by other controllers.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-15T13:12:38Z

/remove-kind feature
/kind bug

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-15T13:14:47Z

/release-note-edit
```release-note
WorkloadRequestUseMergePatch: use "strict" mode for admission patches during scheduling which
sends the ResourceVersion of the workload being admitted for comparing by kube-apiserver. 
This fixes the race-condition issue that Workload conditions added concurrently by other controllers
could be removed during scheduling.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-15T13:34:15Z

/cherrypick release-0.14

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-10-15T13:34:18Z

@mimowo: new pull request created: #7279

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7246#issuecomment-3406361703):

>Thanks 👍 
>/lgtm
>/approve
>/cherrypick release-0.14


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-10-15T13:34:53Z

@mimowo: new pull request could not be created: failed to create pull request against kubernetes-sigs/kueue#release-0.14 from head k8s-infra-cherrypick-robot:cherry-pick-7246-to-release-0.14: status code 422 not one of [201], body: {"message":"Validation Failed","errors":[{"resource":"PullRequest","code":"custom","message":"A pull request already exists for k8s-infra-cherrypick-robot:cherry-pick-7246-to-release-0.14."}],"documentation_url":"https://docs.github.com/rest/pulls/pulls#create-a-pull-request","status":"422"}

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7246#issuecomment-3406481199):

>/cherrypick release-0.14


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-15T13:35:13Z

@mszadkow please prepare the cherrypick manually

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-13T12:57:31Z

LGTM overall, please also open another "test" PR which turns the feature gate as "true" so that we can make sure it works. We could then merge that PR in a follow up.

- **pkg/scheduler/scheduler.go:647** by [@mimowo](https://github.com/mimowo) — 2025-10-13T12:56:33Z
```diff
@@ -644,7 +644,7 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, cq *schdcache.ClusterQu
 	s.admissionRoutineWrapper.Run(func() {
 		err := workload.PatchAdmissionStatus(ctx, s.client, origWorkload, s.clock, func() (*kueue.Workload, bool, error) {
 			return newWorkload, true, nil
-		}, workload.WithLoose())
+		}, workload.WithLooseOnApply())
```

  I think this should also be used here: https://github.com/kubernetes-sigs/kueue/blob/8749fd850a99904a423705b4f95337c3bef7467f/pkg/scheduler/scheduler.go#L317
  
  Do we have an integration or e2e test which covers that?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-10-13T13:04:55Z

- **pkg/scheduler/scheduler.go:647** by [@mszadkow](https://github.com/mszadkow) — 2025-10-13T13:04:55Z
```diff
@@ -644,7 +644,7 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, cq *schdcache.ClusterQu
 	s.admissionRoutineWrapper.Run(func() {
 		err := workload.PatchAdmissionStatus(ctx, s.client, origWorkload, s.clock, func() (*kueue.Workload, bool, error) {
 			return newWorkload, true, nil
-		}, workload.WithLoose())
+		}, workload.WithLooseOnApply())
```

  I am working on unit tests, but sure I can work on integration test as well.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-13T13:16:52Z

- **pkg/scheduler/scheduler.go:647** by [@mimowo](https://github.com/mimowo) — 2025-10-13T13:16:51Z
```diff
@@ -644,7 +644,7 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, cq *schdcache.ClusterQu
 	s.admissionRoutineWrapper.Run(func() {
 		err := workload.PatchAdmissionStatus(ctx, s.client, origWorkload, s.clock, func() (*kueue.Workload, bool, error) {
 			return newWorkload, true, nil
-		}, workload.WithLoose())
+		}, workload.WithLooseOnApply())
```

  Maybe we already have an integration test, you can check by enabling the feature gate, and say injecting a panic in that place.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-13T16:12:07Z

- **pkg/scheduler/scheduler_test.go:8197** by [@mimowo](https://github.com/mimowo) — 2025-10-13T16:12:07Z
```diff
@@ -8086,133 +8086,146 @@ func TestLastSchedulingContext(t *testing.T) {
 	}
 
 	for _, tc := range cases {
-		t.Run(tc.name, func(t *testing.T) {
-			ctx, log := utiltesting.ContextWithLog(t)
-			scheme := runtime.NewScheme()
+		for _, enabled := range []bool{false, true} {
+			t.Run(fmt.Sprintf("%s WorkloadRequestUseMergePatch enabled: %t", tc.name, enabled), func(t *testing.T) {
+				features.SetFeatureGateDuringTest(t, features.WorkloadRequestUseMergePatch, enabled)
+				ctx, log := utiltesting.ContextWithLog(t)
+				scheme := runtime.NewScheme()
 
-			clientBuilder := utiltesting.NewClientBuilder().
-				WithLists(
-					&kueue.WorkloadList{Items: tc.workloads},
-					&kueue.ClusterQueueList{Items: tc.cqs},
-					&kueue.LocalQueueList{Items: queues},
-				).
-				WithObjects(
-					utiltesting.MakeNamespace("default"),
-				).
-				WithStatusSubresource(&kueue.Workload{}).
-				WithInterceptorFuncs(interceptor.Funcs{SubResourcePatch: utiltesting.TreatSSAAsStrategicMerge})
+				testWls := make([]kueue.Workload, 0, len(tc.workloads))
+				for _, wl := range tc.workloads {
+					testWls = append(testWls, *wl.DeepCopy())
+				}
+				clientBuilder := utiltesting.NewClientBuilder().
+					WithLists(
+						&kueue.WorkloadList{Items: testWls},
+						&kueue.ClusterQueueList{Items: tc.cqs},
+						&kueue.LocalQueueList{Items: queues},
+					).
+					WithObjects(
+						utiltesting.MakeNamespace("default"),
+					).
+					WithStatusSubresource(&kueue.Workload{}).
+					WithInterceptorFuncs(interceptor.Funcs{SubResourcePatch: utiltesting.TreatSSAAsStrategicMerge})
 
-			cl := clientBuilder.Build()
-			broadcaster := record.NewBroadcaster()
-			recorder := broadcaster.NewRecorder(scheme,
-				corev1.EventSource{Component: constants.AdmissionName})
-			cqCache := schdcache.New(cl)
-			qManager := qcache.NewManager(cl, cqCache)
-			// Workloads are loaded into queues or clusterQueues as we add them.
-			for _, q := range queues {
-				if err := qManager.AddLocalQueue(ctx, &q); err != nil {
-					t.Fatalf("Inserting queue %s/%s in manager: %v", q.Namespace, q.Name, err)
+				cl := clientBuilder.Build()
+				broadcaster := record.NewBroadcaster()
+				recorder := broadcaster.NewRecorder(scheme,
+					corev1.EventSource{Component: constants.AdmissionName})
+				cqCache := schdcache.New(cl)
+				qManager := qcache.NewManager(cl, cqCache)
+				// Workloads are loaded into queues or clusterQueues as we add them.
+				for _, q := range queues {
+					if err := qManager.AddLocalQueue(ctx, &q); err != nil {
+						t.Fatalf("Inserting queue %s/%s in manager: %v", q.Namespace, q.Name, err)
+					}
 				}
-			}
-			for i := range resourceFlavors {
-				cqCache.AddOrUpdateResourceFlavor(log, resourceFlavors[i])
-			}
-			for _, cq := range tc.cqs {
-				if err := cqCache.AddClusterQueue(ctx, &cq); err != nil {
-					t.Fatalf("Inserting clusterQueue %s in cache: %v", cq.Name, err)
+				for i := range resourceFlavors {
+					cqCache.AddOrUpdateResourceFlavor(log, resourceFlavors[i])
 				}
-				if err := qManager.AddClusterQueue(ctx, &cq); err != nil {
-					t.Fatalf("Inserting clusterQueue %s in manager: %v", cq.Name, err)
+				for _, cq := range tc.cqs {
+					if err := cqCache.AddClusterQueue(ctx, &cq); err != nil {
+						t.Fatalf("Inserting clusterQueue %s in cache: %v", cq.Name, err)
+					}
+					if err := qManager.AddClusterQueue(ctx, &cq); err != nil {
+						t.Fatalf("Inserting clusterQueue %s in manager: %v", cq.Name, err)
+					}
 				}
-			}
-			scheduler := New(qManager, cqCache, cl, recorder, WithClock(t, fakeClock))
+				scheduler := New(qManager, cqCache, cl, recorder, WithClock(t, fakeClock))
 
-			wg := sync.WaitGroup{}
-			scheduler.setAdmissionRoutineWrapper(routine.NewWrapper(
-				func() { wg.Add(1) },
-				func() { wg.Done() },
-			))
+				wg := sync.WaitGroup{}
+				scheduler.setAdmissionRoutineWrapper(routine.NewWrapper(
+					func() { wg.Add(1) },
+					func() { wg.Done() },
+				))
 
-			var mu sync.Mutex
-			gotPreempted := sets.New[workload.Reference]()
-			scheduler.preemptor.OverrideApply(func(_ context.Context, w *kueue.Workload, _, _ string) error {
-				mu.Lock()
-				gotPreempted.Insert(workload.Key(w))
-				mu.Unlock()
-				return nil
-			})
+				var mu sync.Mutex
+				gotPreempted := sets.New[workload.Reference]()
+				scheduler.preemptor.OverrideApply(func(_ context.Context, w *kueue.Workload, _, _ string) error {
+					mu.Lock()
+					gotPreempted.Insert(workload.Key(w))
+					mu.Unlock()
+					return nil
+				})
 
-			ctx, cancel := context.WithTimeout(ctx, queueingTimeout)
-			go qManager.CleanUpOnContext(ctx)
-			defer cancel()
+				ctx, cancel := context.WithTimeout(ctx, queueingTimeout)
+				go qManager.CleanUpOnContext(ctx)
+				defer cancel()
 
-			scheduler.schedule(ctx)
-			wg.Wait()
+				scheduler.schedule(ctx)
+				wg.Wait()
 
-			if diff := cmp.Diff(tc.wantPreempted, gotPreempted); diff != "" {
-				t.Errorf("Unexpected preemptions (-want,+got):\n%s", diff)
-			}
+				if diff := cmp.Diff(tc.wantPreempted, gotPreempted); diff != "" {
+					t.Errorf("Unexpected preemptions (-want,+got):\n%s", diff)
+				}
 
-			gotWorkloads := &kueue.WorkloadList{}
-			err := cl.List(ctx, gotWorkloads)
-			if err != nil {
-				t.Fatalf("Unexpected list workloads error: %v", err)
-			}
+				gotWorkloads := &kueue.WorkloadList{}
+				err := cl.List(ctx, gotWorkloads)
+				if err != nil {
+					t.Fatalf("Unexpected list workloads error: %v", err)
+				}
 
-			defaultWorkloadCmpOpts := cmp.Options{
-				cmpopts.EquateEmpty(),
-				cmpopts.IgnoreFields(kueue.Workload{}, "ObjectMeta.ResourceVersion"),
-			}
+				defaultWorkloadCmpOpts := cmp.Options{
+					cmpopts.EquateEmpty(),
+					cmpopts.IgnoreFields(kueue.Workload{}, "ObjectMeta.ResourceVersion"),
+				}
 
-			if diff := cmp.Diff(tc.wantWorkloads, gotWorkloads.Items, defaultWorkloadCmpOpts); diff != "" {
-				t.Errorf("Unexpected scheduled workloads (-want,+got):\n%s", diff)
-			}
+				if diff := cmp.Diff(tc.wantWorkloads, gotWorkloads.Items, defaultWorkloadCmpOpts); diff != "" {
+					t.Errorf("Unexpected scheduled workloads (-want,+got):\n%s", diff)
+				}
 
-			for _, workloadReference := range tc.deleteWorkloads {
-				var workload kueue.Workload
-				err := cl.Get(ctx, workloadReference, &workload)
-				if err != nil {
-					t.Errorf("Unable to get workload: %v", err)
+				for _, workloadReference := range tc.deleteWorkloads {
+					var workload kueue.Workload
+					err := cl.Get(ctx, workloadReference, &workload)
+					if err != nil {
+						t.Errorf("Unable to get workload: %v", err)
+					}
+					err = cl.Delete(ctx, &workload)
+					if err != nil {
+						t.Errorf("Delete workload failed: %v", err)
+					}
+					err = cqCache.DeleteWorkload(log, &workload)
+					if err != nil {
+						t.Errorf("Delete workload failed: %v", err)
+					}
+					qManager.QueueAssociatedInadmissibleWorkloadsAfter(ctx, &workload, nil)
 				}
-				err = cl.Delete(ctx, &workload)
-				if err != nil {
-					t.Errorf("Delete workload failed: %v", err)
+
+				scheduler.schedule(ctx)
+				wg.Wait()
+
+				if features.Enabled(features.WorkloadRequestUseMergePatch) {
```

  This works well to me. However, could you please submit a preparatory cleanup PR to introduce the `for _, enabled := range []bool{false, true} {` loop. It would make the reading of the diff easier.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-10-14T06:46:40Z

- **pkg/scheduler/scheduler.go:647** by [@mszadkow](https://github.com/mszadkow) — 2025-10-14T06:46:40Z
```diff
@@ -644,7 +644,7 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, cq *schdcache.ClusterQu
 	s.admissionRoutineWrapper.Run(func() {
 		err := workload.PatchAdmissionStatus(ctx, s.client, origWorkload, s.clock, func() (*kueue.Workload, bool, error) {
 			return newWorkload, true, nil
-		}, workload.WithLoose())
+		}, workload.WithLooseOnApply())
```

  I think I found one that fails from time to time because of the change:
  `Scheduler Fair Sharing Suite: [It] Scheduler when ClusterQueue head has inadmissible workload sticky workload deleted, next workload can admit`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T06:59:47Z

- **pkg/scheduler/scheduler.go:647** by [@mimowo](https://github.com/mimowo) — 2025-10-14T06:59:47Z
```diff
@@ -644,7 +644,7 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, cq *schdcache.ClusterQu
 	s.admissionRoutineWrapper.Run(func() {
 		err := workload.PatchAdmissionStatus(ctx, s.client, origWorkload, s.clock, func() (*kueue.Workload, bool, error) {
 			return newWorkload, true, nil
-		}, workload.WithLoose())
+		}, workload.WithLooseOnApply())
```

  This one flakes not only on this PR: https://github.com/kubernetes-sigs/kueue/issues/7250
  
  I was asking if we have an integration test already which exercises the other use of PatchAdmissionStatus in schedule https://github.com/kubernetes-sigs/kueue/blob/8749fd850a99904a423705b4f95337c3bef7467f/pkg/scheduler/scheduler.go#L317 
  
  And in my first comment I also referenced the other place to update for consistency, as both are from scheduler.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-10-14T10:28:55Z

- **pkg/scheduler/scheduler.go:647** by [@mszadkow](https://github.com/mszadkow) — 2025-10-14T10:28:55Z
```diff
@@ -644,7 +644,7 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, cq *schdcache.ClusterQu
 	s.admissionRoutineWrapper.Run(func() {
 		err := workload.PatchAdmissionStatus(ctx, s.client, origWorkload, s.clock, func() (*kueue.Workload, bool, error) {
 			return newWorkload, true, nil
-		}, workload.WithLoose())
+		}, workload.WithLooseOnApply())
```

  Sorry for confusion, this part is tested here: `SchedulerWithWaitForPodsReady Long PodsReady timeout` and here:
  `SchedulerWithWaitForPodsReady Tiny PodsReady timeout`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T10:32:18Z

- **pkg/scheduler/scheduler.go:647** by [@mimowo](https://github.com/mimowo) — 2025-10-14T10:32:18Z
```diff
@@ -644,7 +644,7 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, cq *schdcache.ClusterQu
 	s.admissionRoutineWrapper.Run(func() {
 		err := workload.PatchAdmissionStatus(ctx, s.client, origWorkload, s.clock, func() (*kueue.Workload, bool, error) {
 			return newWorkload, true, nil
-		}, workload.WithLoose())
+		}, workload.WithLooseOnApply())
```

  Nice, does it flake if you run the tests in a loop with the changes in this PR, plus enabling the feature gate?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T10:34:41Z

- **pkg/workload/workload.go:932** by [@mimowo](https://github.com/mimowo) — 2025-10-14T10:34:41Z
```diff
@@ -921,11 +921,15 @@ type PatchAdmissionStatusOption func(*PatchAdmissionStatusOptions)
 //     from the "original" object to ensure its inclusion in the generated
 //     patch. Defaults to true. Setting Strict=false preserves the current
 //     ResourceVersion.
+//   - StrictWithApply: When using Patch Apply, controls whether to enforce
+//     strict version handling. Defaults to true. Setting StrictWithApply=false
+//     enforces looser version handling only for Patch Apply.
 //
 // Typically, PatchAdmissionStatusOptions are constructed via DefaultPatchAdmissionStatusOptions and
 // modified using PatchAdmissionStatusOption functions (e.g., WithLoose).
 type PatchAdmissionStatusOptions struct {
-	Strict bool
+	Strict          bool
+	StrictWithApply bool
```

  I think this names are misleading, I would never know what StrictWithApply means. 
  
  I would suggest to have 2 options actually.
  `StrictApply` and `StrictPatch`. Now, setting `WithLooseApply` only sets `StrictApply` to false.
  Also, `WithLoose` would set both to false.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T10:36:11Z

- **pkg/scheduler/scheduler_test.go:8197** by [@mimowo](https://github.com/mimowo) — 2025-10-14T10:36:11Z
```diff
@@ -8086,133 +8086,146 @@ func TestLastSchedulingContext(t *testing.T) {
 	}
 
 	for _, tc := range cases {
-		t.Run(tc.name, func(t *testing.T) {
-			ctx, log := utiltesting.ContextWithLog(t)
-			scheme := runtime.NewScheme()
+		for _, enabled := range []bool{false, true} {
+			t.Run(fmt.Sprintf("%s WorkloadRequestUseMergePatch enabled: %t", tc.name, enabled), func(t *testing.T) {
+				features.SetFeatureGateDuringTest(t, features.WorkloadRequestUseMergePatch, enabled)
+				ctx, log := utiltesting.ContextWithLog(t)
+				scheme := runtime.NewScheme()
 
-			clientBuilder := utiltesting.NewClientBuilder().
-				WithLists(
-					&kueue.WorkloadList{Items: tc.workloads},
-					&kueue.ClusterQueueList{Items: tc.cqs},
-					&kueue.LocalQueueList{Items: queues},
-				).
-				WithObjects(
-					utiltesting.MakeNamespace("default"),
-				).
-				WithStatusSubresource(&kueue.Workload{}).
-				WithInterceptorFuncs(interceptor.Funcs{SubResourcePatch: utiltesting.TreatSSAAsStrategicMerge})
+				testWls := make([]kueue.Workload, 0, len(tc.workloads))
+				for _, wl := range tc.workloads {
+					testWls = append(testWls, *wl.DeepCopy())
+				}
+				clientBuilder := utiltesting.NewClientBuilder().
+					WithLists(
+						&kueue.WorkloadList{Items: testWls},
+						&kueue.ClusterQueueList{Items: tc.cqs},
+						&kueue.LocalQueueList{Items: queues},
+					).
+					WithObjects(
+						utiltesting.MakeNamespace("default"),
+					).
+					WithStatusSubresource(&kueue.Workload{}).
+					WithInterceptorFuncs(interceptor.Funcs{SubResourcePatch: utiltesting.TreatSSAAsStrategicMerge})
 
-			cl := clientBuilder.Build()
-			broadcaster := record.NewBroadcaster()
-			recorder := broadcaster.NewRecorder(scheme,
-				corev1.EventSource{Component: constants.AdmissionName})
-			cqCache := schdcache.New(cl)
-			qManager := qcache.NewManager(cl, cqCache)
-			// Workloads are loaded into queues or clusterQueues as we add them.
-			for _, q := range queues {
-				if err := qManager.AddLocalQueue(ctx, &q); err != nil {
-					t.Fatalf("Inserting queue %s/%s in manager: %v", q.Namespace, q.Name, err)
+				cl := clientBuilder.Build()
+				broadcaster := record.NewBroadcaster()
+				recorder := broadcaster.NewRecorder(scheme,
+					corev1.EventSource{Component: constants.AdmissionName})
+				cqCache := schdcache.New(cl)
+				qManager := qcache.NewManager(cl, cqCache)
+				// Workloads are loaded into queues or clusterQueues as we add them.
+				for _, q := range queues {
+					if err := qManager.AddLocalQueue(ctx, &q); err != nil {
+						t.Fatalf("Inserting queue %s/%s in manager: %v", q.Namespace, q.Name, err)
+					}
 				}
-			}
-			for i := range resourceFlavors {
-				cqCache.AddOrUpdateResourceFlavor(log, resourceFlavors[i])
-			}
-			for _, cq := range tc.cqs {
-				if err := cqCache.AddClusterQueue(ctx, &cq); err != nil {
-					t.Fatalf("Inserting clusterQueue %s in cache: %v", cq.Name, err)
+				for i := range resourceFlavors {
+					cqCache.AddOrUpdateResourceFlavor(log, resourceFlavors[i])
 				}
-				if err := qManager.AddClusterQueue(ctx, &cq); err != nil {
-					t.Fatalf("Inserting clusterQueue %s in manager: %v", cq.Name, err)
+				for _, cq := range tc.cqs {
+					if err := cqCache.AddClusterQueue(ctx, &cq); err != nil {
+						t.Fatalf("Inserting clusterQueue %s in cache: %v", cq.Name, err)
+					}
+					if err := qManager.AddClusterQueue(ctx, &cq); err != nil {
+						t.Fatalf("Inserting clusterQueue %s in manager: %v", cq.Name, err)
+					}
 				}
-			}
-			scheduler := New(qManager, cqCache, cl, recorder, WithClock(t, fakeClock))
+				scheduler := New(qManager, cqCache, cl, recorder, WithClock(t, fakeClock))
 
-			wg := sync.WaitGroup{}
-			scheduler.setAdmissionRoutineWrapper(routine.NewWrapper(
-				func() { wg.Add(1) },
-				func() { wg.Done() },
-			))
+				wg := sync.WaitGroup{}
+				scheduler.setAdmissionRoutineWrapper(routine.NewWrapper(
+					func() { wg.Add(1) },
+					func() { wg.Done() },
+				))
 
-			var mu sync.Mutex
-			gotPreempted := sets.New[workload.Reference]()
-			scheduler.preemptor.OverrideApply(func(_ context.Context, w *kueue.Workload, _, _ string) error {
-				mu.Lock()
-				gotPreempted.Insert(workload.Key(w))
-				mu.Unlock()
-				return nil
-			})
+				var mu sync.Mutex
+				gotPreempted := sets.New[workload.Reference]()
+				scheduler.preemptor.OverrideApply(func(_ context.Context, w *kueue.Workload, _, _ string) error {
+					mu.Lock()
+					gotPreempted.Insert(workload.Key(w))
+					mu.Unlock()
+					return nil
+				})
 
-			ctx, cancel := context.WithTimeout(ctx, queueingTimeout)
-			go qManager.CleanUpOnContext(ctx)
-			defer cancel()
+				ctx, cancel := context.WithTimeout(ctx, queueingTimeout)
+				go qManager.CleanUpOnContext(ctx)
+				defer cancel()
 
-			scheduler.schedule(ctx)
-			wg.Wait()
+				scheduler.schedule(ctx)
+				wg.Wait()
 
-			if diff := cmp.Diff(tc.wantPreempted, gotPreempted); diff != "" {
-				t.Errorf("Unexpected preemptions (-want,+got):\n%s", diff)
-			}
+				if diff := cmp.Diff(tc.wantPreempted, gotPreempted); diff != "" {
+					t.Errorf("Unexpected preemptions (-want,+got):\n%s", diff)
+				}
 
-			gotWorkloads := &kueue.WorkloadList{}
-			err := cl.List(ctx, gotWorkloads)
-			if err != nil {
-				t.Fatalf("Unexpected list workloads error: %v", err)
-			}
+				gotWorkloads := &kueue.WorkloadList{}
+				err := cl.List(ctx, gotWorkloads)
+				if err != nil {
+					t.Fatalf("Unexpected list workloads error: %v", err)
+				}
 
-			defaultWorkloadCmpOpts := cmp.Options{
-				cmpopts.EquateEmpty(),
-				cmpopts.IgnoreFields(kueue.Workload{}, "ObjectMeta.ResourceVersion"),
-			}
+				defaultWorkloadCmpOpts := cmp.Options{
+					cmpopts.EquateEmpty(),
+					cmpopts.IgnoreFields(kueue.Workload{}, "ObjectMeta.ResourceVersion"),
+				}
 
-			if diff := cmp.Diff(tc.wantWorkloads, gotWorkloads.Items, defaultWorkloadCmpOpts); diff != "" {
-				t.Errorf("Unexpected scheduled workloads (-want,+got):\n%s", diff)
-			}
+				if diff := cmp.Diff(tc.wantWorkloads, gotWorkloads.Items, defaultWorkloadCmpOpts); diff != "" {
+					t.Errorf("Unexpected scheduled workloads (-want,+got):\n%s", diff)
+				}
 
-			for _, workloadReference := range tc.deleteWorkloads {
-				var workload kueue.Workload
-				err := cl.Get(ctx, workloadReference, &workload)
-				if err != nil {
-					t.Errorf("Unable to get workload: %v", err)
+				for _, workloadReference := range tc.deleteWorkloads {
+					var workload kueue.Workload
+					err := cl.Get(ctx, workloadReference, &workload)
+					if err != nil {
+						t.Errorf("Unable to get workload: %v", err)
+					}
+					err = cl.Delete(ctx, &workload)
+					if err != nil {
+						t.Errorf("Delete workload failed: %v", err)
+					}
+					err = cqCache.DeleteWorkload(log, &workload)
+					if err != nil {
+						t.Errorf("Delete workload failed: %v", err)
+					}
+					qManager.QueueAssociatedInadmissibleWorkloadsAfter(ctx, &workload, nil)
 				}
-				err = cl.Delete(ctx, &workload)
-				if err != nil {
-					t.Errorf("Delete workload failed: %v", err)
+
+				scheduler.schedule(ctx)
+				wg.Wait()
+
+				if features.Enabled(features.WorkloadRequestUseMergePatch) {
```

  The perapratory PR is merged, feel free to resolve

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T10:38:31Z

- **pkg/workload/workload.go:932** by [@mimowo](https://github.com/mimowo) — 2025-10-14T10:38:31Z
```diff
@@ -921,11 +921,15 @@ type PatchAdmissionStatusOption func(*PatchAdmissionStatusOptions)
 //     from the "original" object to ensure its inclusion in the generated
 //     patch. Defaults to true. Setting Strict=false preserves the current
 //     ResourceVersion.
+//   - StrictWithApply: When using Patch Apply, controls whether to enforce
+//     strict version handling. Defaults to true. Setting StrictWithApply=false
+//     enforces looser version handling only for Patch Apply.
 //
 // Typically, PatchAdmissionStatusOptions are constructed via DefaultPatchAdmissionStatusOptions and
 // modified using PatchAdmissionStatusOption functions (e.g., WithLoose).
 type PatchAdmissionStatusOptions struct {
-	Strict bool
+	Strict          bool
+	StrictWithApply bool
```

  Alternative sensible names are StrictOnApply & StrictOnPatch, but please don't introduce also StrictWithApply. Let's make the naming consistent

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-10-14T10:48:19Z

- **pkg/workload/workload.go:932** by [@mszadkow](https://github.com/mszadkow) — 2025-10-14T10:48:19Z
```diff
@@ -921,11 +921,15 @@ type PatchAdmissionStatusOption func(*PatchAdmissionStatusOptions)
 //     from the "original" object to ensure its inclusion in the generated
 //     patch. Defaults to true. Setting Strict=false preserves the current
 //     ResourceVersion.
+//   - StrictWithApply: When using Patch Apply, controls whether to enforce
+//     strict version handling. Defaults to true. Setting StrictWithApply=false
+//     enforces looser version handling only for Patch Apply.
 //
 // Typically, PatchAdmissionStatusOptions are constructed via DefaultPatchAdmissionStatusOptions and
 // modified using PatchAdmissionStatusOption functions (e.g., WithLoose).
 type PatchAdmissionStatusOptions struct {
-	Strict bool
+	Strict          bool
+	StrictWithApply bool
```

  Make sense, I will use first idea

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-10-14T10:51:01Z

- **pkg/scheduler/scheduler.go:647** by [@mszadkow](https://github.com/mszadkow) — 2025-10-14T10:51:00Z
```diff
@@ -644,7 +644,7 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, cq *schdcache.ClusterQu
 	s.admissionRoutineWrapper.Run(func() {
 		err := workload.PatchAdmissionStatus(ctx, s.client, origWorkload, s.clock, func() (*kueue.Workload, bool, error) {
 			return newWorkload, true, nil
-		}, workload.WithLoose())
+		}, workload.WithLooseOnApply())
```

  Testing now

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-10-14T10:52:20Z

- **pkg/workload/workload.go:932** by [@mszadkow](https://github.com/mszadkow) — 2025-10-14T10:52:20Z
```diff
@@ -921,11 +921,15 @@ type PatchAdmissionStatusOption func(*PatchAdmissionStatusOptions)
 //     from the "original" object to ensure its inclusion in the generated
 //     patch. Defaults to true. Setting Strict=false preserves the current
 //     ResourceVersion.
+//   - StrictWithApply: When using Patch Apply, controls whether to enforce
+//     strict version handling. Defaults to true. Setting StrictWithApply=false
+//     enforces looser version handling only for Patch Apply.
 //
 // Typically, PatchAdmissionStatusOptions are constructed via DefaultPatchAdmissionStatusOptions and
 // modified using PatchAdmissionStatusOption functions (e.g., WithLoose).
 type PatchAdmissionStatusOptions struct {
-	Strict bool
+	Strict          bool
+	StrictWithApply bool
```

  I decided in the end to keep strict and this another StrictWithApply, as the first one was "global" while the other applied only to patch, but sure, maybe it  doesn't sound good enough.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T10:58:39Z

- **pkg/workload/workload.go:932** by [@mimowo](https://github.com/mimowo) — 2025-10-14T10:58:38Z
```diff
@@ -921,11 +921,15 @@ type PatchAdmissionStatusOption func(*PatchAdmissionStatusOptions)
 //     from the "original" object to ensure its inclusion in the generated
 //     patch. Defaults to true. Setting Strict=false preserves the current
 //     ResourceVersion.
+//   - StrictWithApply: When using Patch Apply, controls whether to enforce
+//     strict version handling. Defaults to true. Setting StrictWithApply=false
+//     enforces looser version handling only for Patch Apply.
 //
 // Typically, PatchAdmissionStatusOptions are constructed via DefaultPatchAdmissionStatusOptions and
 // modified using PatchAdmissionStatusOption functions (e.g., WithLoose).
 type PatchAdmissionStatusOptions struct {
-	Strict bool
+	Strict          bool
+	StrictWithApply bool
```

  Please also open a separate PR (experimental only) has the code from this PR + enables the feature gate for Patch.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T10:59:51Z

- **pkg/workload/workload.go:985** by [@mimowo](https://github.com/mimowo) — 2025-10-14T10:59:51Z
```diff
@@ -958,6 +963,24 @@ func WithLoose() PatchAdmissionStatusOption {
 	}
 }
 
+// WithLooseOnApply returns a PatchAdmissionStatusOption that resets the StrictWithApply field on PatchAdmissionStatusOptions.
+//
+// When using Patch Apply, setting StrictWithApply to false enforces looser
+// version handling only for Patch Apply.
+// This is useful when the update function already handles version conflicts
+// and we want to avoid additional conflicts during Patch Apply.
+//
+// Example:
+//	patch := clientutil.Patch(ctx, c, w, clk, func() (bool, error) {
+//	    return updateFn(obj), nil
+//	}, WithLooseOnApply()) // disables strict mode for Patch Apply
+
+func WithLooseOnApply() PatchAdmissionStatusOption {
+	return func(o *PatchAdmissionStatusOptions) {
+		o.StrictWithApply = false
+	}
+}
```

  This reads very hard, as proposed in another comment, please split the "Strict" into "StrictOnApply" and "StringOnPatch".

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T11:31:12Z

- **pkg/workload/workload.go:1012** by [@mimowo](https://github.com/mimowo) — 2025-10-14T11:31:12Z
```diff
@@ -981,7 +1007,10 @@ func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 		return err
 	}
 
-	return ApplyAdmissionStatus(ctx, c, wPatched, opts.Strict, clk)
+	// When using Patch Apply, we might want to loosen strict version handling only for Patch Apply,
+	// regardless of the strictness used when calling the update function.
+	strict := opts.StrictPatch && opts.StrictApply
```

  ```suggestion
  	strict := opts.StrictApply
  ```
  In this code path we do Apply, so why &&? It does not seem to make sense.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-10-14T11:38:32Z

- **pkg/workload/workload.go:932** by [@mszadkow](https://github.com/mszadkow) — 2025-10-14T11:38:32Z
```diff
@@ -921,11 +921,15 @@ type PatchAdmissionStatusOption func(*PatchAdmissionStatusOptions)
 //     from the "original" object to ensure its inclusion in the generated
 //     patch. Defaults to true. Setting Strict=false preserves the current
 //     ResourceVersion.
+//   - StrictWithApply: When using Patch Apply, controls whether to enforce
+//     strict version handling. Defaults to true. Setting StrictWithApply=false
+//     enforces looser version handling only for Patch Apply.
 //
 // Typically, PatchAdmissionStatusOptions are constructed via DefaultPatchAdmissionStatusOptions and
 // modified using PatchAdmissionStatusOption functions (e.g., WithLoose).
 type PatchAdmissionStatusOptions struct {
-	Strict bool
+	Strict          bool
+	StrictWithApply bool
```

  Done - https://github.com/kubernetes-sigs/kueue/pull/7261

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-10-14T11:39:03Z

- **pkg/workload/workload.go:1012** by [@mszadkow](https://github.com/mszadkow) — 2025-10-14T11:39:02Z
```diff
@@ -981,7 +1007,10 @@ func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 		return err
 	}
 
-	return ApplyAdmissionStatus(ctx, c, wPatched, opts.Strict, clk)
+	// When using Patch Apply, we might want to loosen strict version handling only for Patch Apply,
+	// regardless of the strictness used when calling the update function.
+	strict := opts.StrictPatch && opts.StrictApply
```

  Yes, when they are completely separate like now it should be this way. Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-15T07:09:49Z

- **pkg/scheduler/scheduler.go:647** by [@mimowo](https://github.com/mimowo) — 2025-10-15T07:09:48Z
```diff
@@ -644,7 +644,7 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, cq *schdcache.ClusterQu
 	s.admissionRoutineWrapper.Run(func() {
 		err := workload.PatchAdmissionStatus(ctx, s.client, origWorkload, s.clock, func() (*kueue.Workload, bool, error) {
 			return newWorkload, true, nil
-		}, workload.WithLoose())
+		}, workload.WithLooseOnApply())
```

  Please summarize the results of the testing.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-10-15T12:02:23Z

- **pkg/scheduler/scheduler.go:647** by [@mszadkow](https://github.com/mszadkow) — 2025-10-15T12:02:22Z
```diff
@@ -644,7 +644,7 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, cq *schdcache.ClusterQu
 	s.admissionRoutineWrapper.Run(func() {
 		err := workload.PatchAdmissionStatus(ctx, s.client, origWorkload, s.clock, func() (*kueue.Workload, bool, error) {
 			return newWorkload, true, nil
-		}, workload.WithLoose())
+		}, workload.WithLooseOnApply())
```

  A successful with flag enabled run of:
  `ginkgo --json-report ./ginkgo.report -focus "SchedulerWithWaitForPodsReady"   -r --race --procs=4 --repeat=20`
  No failures.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-15T13:05:25Z

- **pkg/scheduler/scheduler.go:647** by [@mimowo](https://github.com/mimowo) — 2025-10-15T13:05:25Z
```diff
@@ -644,7 +644,7 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, cq *schdcache.ClusterQu
 	s.admissionRoutineWrapper.Run(func() {
 		err := workload.PatchAdmissionStatus(ctx, s.client, origWorkload, s.clock, func() (*kueue.Workload, bool, error) {
 			return newWorkload, true, nil
-		}, workload.WithLoose())
+		}, workload.WithLooseOnApply())
```

  Awesome!
