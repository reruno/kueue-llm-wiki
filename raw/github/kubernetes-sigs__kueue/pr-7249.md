# PR #7249: Add TAS support to the Kubeflow Trainer integration

**Summary**: Add TAS support to the Kubeflow Trainer integration

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7249

**Last updated**: 2025-11-19T14:43:53Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kaisoz](https://github.com/kaisoz)
- **Branch**: `7156-trainer-TAS-support` → `main`
- **Created**: 2025-10-13T15:14:16Z
- **Updated**: 2025-11-19T14:43:53Z
- **Merged**: 2025-10-24T08:51:36Z
- **Closed**: 2025-10-24T08:51:36Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 67   **+8131 / -801**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

This PR adds TAS support for the Kubeflow Trainer integration

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #7156

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
TAS: It supports the Kubeflow TrainJob
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-10-13T15:14:23Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 3adcd869e0f62cc37391e143c8bdef7ab53e0845 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68f8aadab7fb3900087a51d4 |

### Comment by [@kaisoz](https://github.com/kaisoz) — 2025-10-13T15:14:35Z

/assign @mimowo 

Please PTAL! Thanks!

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-16T11:00:03Z

@kaisoz this requires rebase after a recent merge https://github.com/kubernetes-sigs/kueue/pull/7272

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-16T13:02:05Z

/lgtm
/approve
/hold
Let me think if we should wait for Kubeflow release, but IIUC this is planned soon, so we should still have planty of time to bump the dependency before 0.15 release of Kueue (second half November)
cc @andreyvelich

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-16T13:02:12Z

LGTM label has been added.  <details>Git tree hash: 296560afaf7039c160c3c5d93398983c38f8cb94</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-16T13:02:16Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7249#" title="Author self-approved">kaisoz</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7249#issuecomment-3410792575" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-24T07:29:27Z

/unhold
We already have RC0, and full release of Kubeflow is expected in 1.5 week, so this should give us enough buffer to make adjustments if any. Let me merge to avoid maintaining conflicts for this PR.

There is also follow up work which can get unblocked by that, like removing the override-idx annotation.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-24T07:30:04Z

/lgtm
Thank you @kaisoz 👍

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-24T07:30:10Z

LGTM label has been added.  <details>Git tree hash: 9b1d9ad9392446080acfb6f09717e2c6a42d4e4d</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-24T07:30:41Z

cc @andreyvelich with whom I synced on the release plans for Kubeflow v2 wrt https://github.com/kubernetes-sigs/kueue/pull/7249#issuecomment-3441521558

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-24T08:31:43Z

https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7249/pull-kueue-test-e2e-multikueue-main/1981624596008275968
retest looks like flake, I will open issue

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-24T08:31:52Z

/test pull-kueue-test-e2e-multikueue-main

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-19T14:43:51Z

/release-note-edit

```release-note
TAS: It supports the Kubeflow TrainJob
```

## Reviews

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-10-13T15:18:06Z

- **pkg/controller/jobs/trainjob/trainjob_controller.go:1** by [@kaisoz](https://github.com/kaisoz) — 2025-10-13T15:18:05Z
  While implementing this, I realised that the only function that needs the `podset.Template`s to be defaulted is the `PodSets` function, so refactored the code to do it only there. The rest of the functions just need access to the generated jobset. This reduces the number of API server calls needed

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-10-13T15:25:48Z

- **pkg/controller/jobs/trainjob/trainjob_controller.go:252** by [@kaisoz](https://github.com/kaisoz) — 2025-10-13T15:25:47Z
```diff
@@ -243,7 +234,21 @@ func (t *TrainJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 	if err != nil {
 		return nil, err
 	}
-	return (*workloadjobset.JobSet)(jobset).PodSets(ctx)
+
+	podsets, err := (*workloadjobset.JobSet)(jobset).PodSets(ctx)
+	if err != nil {
+		return nil, err
+	}
+
+	// Run a dry-run patch of a throwaway workload to set the podset defaults
+	// Podsets must be defaulted because Kueue later uses them to match workloads.
+	// Workloads coming from the API server are already defaulted, so without defaulting these podsets, matching would fail.
+	wl := jobframework.NewWorkload(t.Name, t.Object(), podsets, []string{})
+	if err := reconciler.client.Create(ctx, wl, &client.CreateOptions{DryRun: []string{metav1.DryRunAll}}); err != nil {
```

  now there's no need to default the jobset object since it's only needed for podset creation. Therefore I preferred to default a workload object  to reduce the jobset object dependency and use a more kueue native object

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-10-13T15:26:46Z

- **pkg/controller/jobs/trainjob/trainjob_webhook.go:160** by [@kaisoz](https://github.com/kaisoz) — 2025-10-13T15:26:45Z
```diff
@@ -106,20 +118,64 @@ func (w *TrainJobWebhook) ValidateUpdate(ctx context.Context, oldObj, newObj run
 	newTrainJob := fromObject(newObj)
 	log := ctrl.LoggerFrom(ctx).WithName("trainjob-webhook")
 	log.Info("Validating update")
-	return nil, w.validateUpdate(oldTrainJob, newTrainJob).ToAggregate()
+	validationErrs, err := w.validateUpdate(ctx, oldTrainJob, newTrainJob)
+	if err != nil {
+		return nil, err
+	}
+	return nil, validationErrs.ToAggregate()
 }
 
-func (w *TrainJobWebhook) validateUpdate(oldTrainJob, newTrainJob *TrainJob) field.ErrorList {
+func (w *TrainJobWebhook) validateUpdate(ctx context.Context, oldTrainJob, newTrainJob *TrainJob) (field.ErrorList, error) {
 	var allErrs field.ErrorList
 	allErrs = append(allErrs, jobframework.ValidateJobOnUpdate(oldTrainJob, newTrainJob, w.queues.DefaultLocalQueueExist)...)
-	allErrs = append(allErrs, w.validateCreate(newTrainJob)...)
-	return allErrs
+	validationErrs, err := w.validateCreate(ctx, newTrainJob)
+	if err != nil {
+		return nil, err
+	}
+	allErrs = append(allErrs, validationErrs...)
+	return allErrs, nil
 }
 
-func (w *TrainJobWebhook) validateCreate(trainjob *TrainJob) field.ErrorList {
+func (w *TrainJobWebhook) validateCreate(ctx context.Context, trainjob *TrainJob) (field.ErrorList, error) {
 	var allErrs field.ErrorList
 	allErrs = append(allErrs, jobframework.ValidateJobOnCreate(trainjob)...)
-	return allErrs
+	if features.Enabled(features.TopologyAwareScheduling) {
+		validationErrs, err := w.validateTopologyRequest(ctx, trainjob)
+		if err != nil {
+			return nil, err
+		}
+		allErrs = append(allErrs, validationErrs...)
+	}
+	return allErrs, nil
+}
+
+func (w *TrainJobWebhook) validateTopologyRequest(ctx context.Context, trainJob *TrainJob) (field.ErrorList, error) {
+	var allErrs field.ErrorList
+
+	// TAS user-facing API annotations could be set either in the TrainingRuntime or the TrainJob and will be merged into the generated child jobset
+	childJobSet, err := getChildJobSet(ctx, trainJob)
+	if err != nil {
+		return nil, err
+	}
+	podSets, podSetsErr := (*workloadjobset.JobSet)(childJobSet).PodSets(ctx)
```

  The webhook doesn't need to default the podsets, so it can use the underlying jobset functions directly to avoid API server calls

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-13T15:28:39Z

- **pkg/controller/jobs/trainjob/trainjob_controller.go:252** by [@mimowo](https://github.com/mimowo) — 2025-10-13T15:28:39Z
```diff
@@ -243,7 +234,21 @@ func (t *TrainJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 	if err != nil {
 		return nil, err
 	}
-	return (*workloadjobset.JobSet)(jobset).PodSets(ctx)
+
+	podsets, err := (*workloadjobset.JobSet)(jobset).PodSets(ctx)
+	if err != nil {
+		return nil, err
+	}
+
+	// Run a dry-run patch of a throwaway workload to set the podset defaults
+	// Podsets must be defaulted because Kueue later uses them to match workloads.
+	// Workloads coming from the API server are already defaulted, so without defaulting these podsets, matching would fail.
+	wl := jobframework.NewWorkload(t.Name, t.Object(), podsets, []string{})
+	if err := reconciler.client.Create(ctx, wl, &client.CreateOptions{DryRun: []string{metav1.DryRunAll}}); err != nil {
```

  Making the code Job type agnostic is already ticketed : https://github.com/kubernetes-sigs/kueue/issues/7043
  
  For the ease of reviewing I would prefer not to mix the two efforts. We can send making the code Job agnosting as a preparatory PR, and close the mentioned issue, wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-13T15:37:49Z

- **pkg/controller/jobs/trainjob/trainjob_webhook.go:160** by [@mimowo](https://github.com/mimowo) — 2025-10-13T15:37:49Z
```diff
@@ -106,20 +118,64 @@ func (w *TrainJobWebhook) ValidateUpdate(ctx context.Context, oldObj, newObj run
 	newTrainJob := fromObject(newObj)
 	log := ctrl.LoggerFrom(ctx).WithName("trainjob-webhook")
 	log.Info("Validating update")
-	return nil, w.validateUpdate(oldTrainJob, newTrainJob).ToAggregate()
+	validationErrs, err := w.validateUpdate(ctx, oldTrainJob, newTrainJob)
+	if err != nil {
+		return nil, err
+	}
+	return nil, validationErrs.ToAggregate()
 }
 
-func (w *TrainJobWebhook) validateUpdate(oldTrainJob, newTrainJob *TrainJob) field.ErrorList {
+func (w *TrainJobWebhook) validateUpdate(ctx context.Context, oldTrainJob, newTrainJob *TrainJob) (field.ErrorList, error) {
 	var allErrs field.ErrorList
 	allErrs = append(allErrs, jobframework.ValidateJobOnUpdate(oldTrainJob, newTrainJob, w.queues.DefaultLocalQueueExist)...)
-	allErrs = append(allErrs, w.validateCreate(newTrainJob)...)
-	return allErrs
+	validationErrs, err := w.validateCreate(ctx, newTrainJob)
+	if err != nil {
+		return nil, err
+	}
+	allErrs = append(allErrs, validationErrs...)
+	return allErrs, nil
 }
 
-func (w *TrainJobWebhook) validateCreate(trainjob *TrainJob) field.ErrorList {
+func (w *TrainJobWebhook) validateCreate(ctx context.Context, trainjob *TrainJob) (field.ErrorList, error) {
 	var allErrs field.ErrorList
 	allErrs = append(allErrs, jobframework.ValidateJobOnCreate(trainjob)...)
-	return allErrs
+	if features.Enabled(features.TopologyAwareScheduling) {
+		validationErrs, err := w.validateTopologyRequest(ctx, trainjob)
+		if err != nil {
+			return nil, err
+		}
+		allErrs = append(allErrs, validationErrs...)
+	}
+	return allErrs, nil
+}
+
+func (w *TrainJobWebhook) validateTopologyRequest(ctx context.Context, trainJob *TrainJob) (field.ErrorList, error) {
+	var allErrs field.ErrorList
+
+	// TAS user-facing API annotations could be set either in the TrainingRuntime or the TrainJob and will be merged into the generated child jobset
+	childJobSet, err := getChildJobSet(ctx, trainJob)
+	if err != nil {
+		return nil, err
+	}
+	podSets, podSetsErr := (*workloadjobset.JobSet)(childJobSet).PodSets(ctx)
```

  Ok, but please encapsulate this bit, with a helper fuction like that:
  
  ```golang
  func podSets(ctx, trainJob) ([]kueue.PodSet, error)
  ```
  This way the code in webhook can already be Job agnostic. The same helper should be used in controller.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-13T16:52:28Z

- **pkg/controller/jobs/trainjob/trainjob_webhook.go:162** by [@mimowo](https://github.com/mimowo) — 2025-10-13T16:52:28Z
```diff
@@ -106,20 +118,64 @@ func (w *TrainJobWebhook) ValidateUpdate(ctx context.Context, oldObj, newObj run
 	newTrainJob := fromObject(newObj)
 	log := ctrl.LoggerFrom(ctx).WithName("trainjob-webhook")
 	log.Info("Validating update")
-	return nil, w.validateUpdate(oldTrainJob, newTrainJob).ToAggregate()
+	validationErrs, err := w.validateUpdate(ctx, oldTrainJob, newTrainJob)
+	if err != nil {
+		return nil, err
+	}
+	return nil, validationErrs.ToAggregate()
 }
 
-func (w *TrainJobWebhook) validateUpdate(oldTrainJob, newTrainJob *TrainJob) field.ErrorList {
+func (w *TrainJobWebhook) validateUpdate(ctx context.Context, oldTrainJob, newTrainJob *TrainJob) (field.ErrorList, error) {
 	var allErrs field.ErrorList
 	allErrs = append(allErrs, jobframework.ValidateJobOnUpdate(oldTrainJob, newTrainJob, w.queues.DefaultLocalQueueExist)...)
-	allErrs = append(allErrs, w.validateCreate(newTrainJob)...)
-	return allErrs
+	validationErrs, err := w.validateCreate(ctx, newTrainJob)
+	if err != nil {
+		return nil, err
+	}
+	allErrs = append(allErrs, validationErrs...)
+	return allErrs, nil
 }
 
-func (w *TrainJobWebhook) validateCreate(trainjob *TrainJob) field.ErrorList {
+func (w *TrainJobWebhook) validateCreate(ctx context.Context, trainjob *TrainJob) (field.ErrorList, error) {
 	var allErrs field.ErrorList
 	allErrs = append(allErrs, jobframework.ValidateJobOnCreate(trainjob)...)
-	return allErrs
+	if features.Enabled(features.TopologyAwareScheduling) {
+		validationErrs, err := w.validateTopologyRequest(ctx, trainjob)
+		if err != nil {
+			return nil, err
+		}
+		allErrs = append(allErrs, validationErrs...)
+	}
+	return allErrs, nil
+}
+
+func (w *TrainJobWebhook) validateTopologyRequest(ctx context.Context, trainJob *TrainJob) (field.ErrorList, error) {
+	var allErrs field.ErrorList
+
+	// TAS user-facing API annotations could be set either in the TrainingRuntime or the TrainJob and will be merged into the generated child jobset
+	childJobSet, err := getChildJobSet(ctx, trainJob)
+	if err != nil {
+		return nil, err
+	}
+	podSets, podSetsErr := (*workloadjobset.JobSet)(childJobSet).PodSets(ctx)
+
+	for i, rj := range childJobSet.Spec.ReplicatedJobs {
```

  Ah this is very tricky for TrainJob, because the validation paths are constructed as this was a JobSet, which is not the object a user constructs. 
  
  The idea of validation is to point user at the places which require adjusting, thus for other CRDs we return paths to the PodTemplate location. However, for the TrainJob the PodTemplate location is on the Runtime object actually. 
  
  In any case I would prefer to use the Runtime object paths rather than coming from JobSet.

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-10-13T20:15:17Z

- **pkg/controller/jobs/trainjob/trainjob_webhook.go:162** by [@kaisoz](https://github.com/kaisoz) — 2025-10-13T20:15:17Z
```diff
@@ -106,20 +118,64 @@ func (w *TrainJobWebhook) ValidateUpdate(ctx context.Context, oldObj, newObj run
 	newTrainJob := fromObject(newObj)
 	log := ctrl.LoggerFrom(ctx).WithName("trainjob-webhook")
 	log.Info("Validating update")
-	return nil, w.validateUpdate(oldTrainJob, newTrainJob).ToAggregate()
+	validationErrs, err := w.validateUpdate(ctx, oldTrainJob, newTrainJob)
+	if err != nil {
+		return nil, err
+	}
+	return nil, validationErrs.ToAggregate()
 }
 
-func (w *TrainJobWebhook) validateUpdate(oldTrainJob, newTrainJob *TrainJob) field.ErrorList {
+func (w *TrainJobWebhook) validateUpdate(ctx context.Context, oldTrainJob, newTrainJob *TrainJob) (field.ErrorList, error) {
 	var allErrs field.ErrorList
 	allErrs = append(allErrs, jobframework.ValidateJobOnUpdate(oldTrainJob, newTrainJob, w.queues.DefaultLocalQueueExist)...)
-	allErrs = append(allErrs, w.validateCreate(newTrainJob)...)
-	return allErrs
+	validationErrs, err := w.validateCreate(ctx, newTrainJob)
+	if err != nil {
+		return nil, err
+	}
+	allErrs = append(allErrs, validationErrs...)
+	return allErrs, nil
 }
 
-func (w *TrainJobWebhook) validateCreate(trainjob *TrainJob) field.ErrorList {
+func (w *TrainJobWebhook) validateCreate(ctx context.Context, trainjob *TrainJob) (field.ErrorList, error) {
 	var allErrs field.ErrorList
 	allErrs = append(allErrs, jobframework.ValidateJobOnCreate(trainjob)...)
-	return allErrs
+	if features.Enabled(features.TopologyAwareScheduling) {
+		validationErrs, err := w.validateTopologyRequest(ctx, trainjob)
+		if err != nil {
+			return nil, err
+		}
+		allErrs = append(allErrs, validationErrs...)
+	}
+	return allErrs, nil
+}
+
+func (w *TrainJobWebhook) validateTopologyRequest(ctx context.Context, trainJob *TrainJob) (field.ErrorList, error) {
+	var allErrs field.ErrorList
+
+	// TAS user-facing API annotations could be set either in the TrainingRuntime or the TrainJob and will be merged into the generated child jobset
+	childJobSet, err := getChildJobSet(ctx, trainJob)
+	if err != nil {
+		return nil, err
+	}
+	podSets, podSetsErr := (*workloadjobset.JobSet)(childJobSet).PodSets(ctx)
+
+	for i, rj := range childJobSet.Spec.ReplicatedJobs {
```

  I agree, makes sense and doing this is misleading. 
  
  However, in case of the Trainer, the TAS annotations can come either from the `TrainingRuntime` (`.spec.replicatedJobs[*].template.spec.template.metadata`) or the `TrainJob` (`.spec.PodTemplateOverrides[*].metadata`) , so using Runtime paths by default might be confusing for the user if the annotation came from the `TrainJob`? Anyway, I can think of three solutions so far:
  
  1. Use a generic error that doesn't point user to any specific place
  2. Process first the `Trainjob` overrides (since they have preference at merge time) and then the jobset metadata, adjusting the validation path in each case
  3. Default to the Runtime object path. At the end of the day, the `podTemplateOverrides` is a power user API and is very likely that the annotations will come from the Runtime
  
  WDYT?

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-10-13T20:18:15Z

- **pkg/controller/jobs/trainjob/trainjob_controller.go:252** by [@kaisoz](https://github.com/kaisoz) — 2025-10-13T20:18:15Z
```diff
@@ -243,7 +234,21 @@ func (t *TrainJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 	if err != nil {
 		return nil, err
 	}
-	return (*workloadjobset.JobSet)(jobset).PodSets(ctx)
+
+	podsets, err := (*workloadjobset.JobSet)(jobset).PodSets(ctx)
+	if err != nil {
+		return nil, err
+	}
+
+	// Run a dry-run patch of a throwaway workload to set the podset defaults
+	// Podsets must be defaulted because Kueue later uses them to match workloads.
+	// Workloads coming from the API server are already defaulted, so without defaulting these podsets, matching would fail.
+	wl := jobframework.NewWorkload(t.Name, t.Object(), podsets, []string{})
+	if err := reconciler.client.Create(ctx, wl, &client.CreateOptions{DryRun: []string{metav1.DryRunAll}}); err != nil {
```

  Sure! Although I think (and hope) that the job agnostic code won't need to perform any call to the API server to default the podsets. 
  
  Do you prefer me to revert this and keep the jobset defaulting by default to reduce the PR diff?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T07:59:50Z

- **pkg/controller/jobs/trainjob/trainjob_controller.go:252** by [@mimowo](https://github.com/mimowo) — 2025-10-14T07:59:50Z
```diff
@@ -243,7 +234,21 @@ func (t *TrainJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 	if err != nil {
 		return nil, err
 	}
-	return (*workloadjobset.JobSet)(jobset).PodSets(ctx)
+
+	podsets, err := (*workloadjobset.JobSet)(jobset).PodSets(ctx)
+	if err != nil {
+		return nil, err
+	}
+
+	// Run a dry-run patch of a throwaway workload to set the podset defaults
+	// Podsets must be defaulted because Kueue later uses them to match workloads.
+	// Workloads coming from the API server are already defaulted, so without defaulting these podsets, matching would fail.
+	wl := jobframework.NewWorkload(t.Name, t.Object(), podsets, []string{})
+	if err := reconciler.client.Create(ctx, wl, &client.CreateOptions{DryRun: []string{metav1.DryRunAll}}); err != nil {
```

  Can we extract to a separate PR to reduce the diff here, but still keep the improvement?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-14T08:05:07Z

- **pkg/controller/jobs/trainjob/trainjob_webhook.go:162** by [@mimowo](https://github.com/mimowo) — 2025-10-14T08:05:07Z
```diff
@@ -106,20 +118,64 @@ func (w *TrainJobWebhook) ValidateUpdate(ctx context.Context, oldObj, newObj run
 	newTrainJob := fromObject(newObj)
 	log := ctrl.LoggerFrom(ctx).WithName("trainjob-webhook")
 	log.Info("Validating update")
-	return nil, w.validateUpdate(oldTrainJob, newTrainJob).ToAggregate()
+	validationErrs, err := w.validateUpdate(ctx, oldTrainJob, newTrainJob)
+	if err != nil {
+		return nil, err
+	}
+	return nil, validationErrs.ToAggregate()
 }
 
-func (w *TrainJobWebhook) validateUpdate(oldTrainJob, newTrainJob *TrainJob) field.ErrorList {
+func (w *TrainJobWebhook) validateUpdate(ctx context.Context, oldTrainJob, newTrainJob *TrainJob) (field.ErrorList, error) {
 	var allErrs field.ErrorList
 	allErrs = append(allErrs, jobframework.ValidateJobOnUpdate(oldTrainJob, newTrainJob, w.queues.DefaultLocalQueueExist)...)
-	allErrs = append(allErrs, w.validateCreate(newTrainJob)...)
-	return allErrs
+	validationErrs, err := w.validateCreate(ctx, newTrainJob)
+	if err != nil {
+		return nil, err
+	}
+	allErrs = append(allErrs, validationErrs...)
+	return allErrs, nil
 }
 
-func (w *TrainJobWebhook) validateCreate(trainjob *TrainJob) field.ErrorList {
+func (w *TrainJobWebhook) validateCreate(ctx context.Context, trainjob *TrainJob) (field.ErrorList, error) {
 	var allErrs field.ErrorList
 	allErrs = append(allErrs, jobframework.ValidateJobOnCreate(trainjob)...)
-	return allErrs
+	if features.Enabled(features.TopologyAwareScheduling) {
+		validationErrs, err := w.validateTopologyRequest(ctx, trainjob)
+		if err != nil {
+			return nil, err
+		}
+		allErrs = append(allErrs, validationErrs...)
+	}
+	return allErrs, nil
+}
+
+func (w *TrainJobWebhook) validateTopologyRequest(ctx context.Context, trainJob *TrainJob) (field.ErrorList, error) {
+	var allErrs field.ErrorList
+
+	// TAS user-facing API annotations could be set either in the TrainingRuntime or the TrainJob and will be merged into the generated child jobset
+	childJobSet, err := getChildJobSet(ctx, trainJob)
+	if err != nil {
+		return nil, err
+	}
+	podSets, podSetsErr := (*workloadjobset.JobSet)(childJobSet).PodSets(ctx)
+
+	for i, rj := range childJobSet.Spec.ReplicatedJobs {
```

  (3.) would be awkward, because it would be reporting the path in a different object.
  
  (1.) and (2.) are so so. My slight preference is for generic `.spec.` as the path (1.)
  
  In either option (generic .spec, or .spec.podTemplateOverrides we need the message to append the information about both paths. Say, "adjust either "spec.podTemplateOverrides, or the metadata for the <> PodSet in the associated Runtime object", or along the lines.

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-10-14T14:27:12Z

- **pkg/controller/jobs/trainjob/trainjob_controller.go:252** by [@kaisoz](https://github.com/kaisoz) — 2025-10-14T14:27:11Z
```diff
@@ -243,7 +234,21 @@ func (t *TrainJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 	if err != nil {
 		return nil, err
 	}
-	return (*workloadjobset.JobSet)(jobset).PodSets(ctx)
+
+	podsets, err := (*workloadjobset.JobSet)(jobset).PodSets(ctx)
+	if err != nil {
+		return nil, err
+	}
+
+	// Run a dry-run patch of a throwaway workload to set the podset defaults
+	// Podsets must be defaulted because Kueue later uses them to match workloads.
+	// Workloads coming from the API server are already defaulted, so without defaulting these podsets, matching would fail.
+	wl := jobframework.NewWorkload(t.Name, t.Object(), podsets, []string{})
+	if err := reconciler.client.Create(ctx, wl, &client.CreateOptions{DryRun: []string{metav1.DryRunAll}}); err != nil {
```

  Fixed in https://github.com/kubernetes-sigs/kueue/pull/7262

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-10-15T16:15:12Z

- **pkg/controller/jobs/trainjob/trainjob_webhook.go:162** by [@kaisoz](https://github.com/kaisoz) — 2025-10-15T16:15:12Z
```diff
@@ -106,20 +118,64 @@ func (w *TrainJobWebhook) ValidateUpdate(ctx context.Context, oldObj, newObj run
 	newTrainJob := fromObject(newObj)
 	log := ctrl.LoggerFrom(ctx).WithName("trainjob-webhook")
 	log.Info("Validating update")
-	return nil, w.validateUpdate(oldTrainJob, newTrainJob).ToAggregate()
+	validationErrs, err := w.validateUpdate(ctx, oldTrainJob, newTrainJob)
+	if err != nil {
+		return nil, err
+	}
+	return nil, validationErrs.ToAggregate()
 }
 
-func (w *TrainJobWebhook) validateUpdate(oldTrainJob, newTrainJob *TrainJob) field.ErrorList {
+func (w *TrainJobWebhook) validateUpdate(ctx context.Context, oldTrainJob, newTrainJob *TrainJob) (field.ErrorList, error) {
 	var allErrs field.ErrorList
 	allErrs = append(allErrs, jobframework.ValidateJobOnUpdate(oldTrainJob, newTrainJob, w.queues.DefaultLocalQueueExist)...)
-	allErrs = append(allErrs, w.validateCreate(newTrainJob)...)
-	return allErrs
+	validationErrs, err := w.validateCreate(ctx, newTrainJob)
+	if err != nil {
+		return nil, err
+	}
+	allErrs = append(allErrs, validationErrs...)
+	return allErrs, nil
 }
 
-func (w *TrainJobWebhook) validateCreate(trainjob *TrainJob) field.ErrorList {
+func (w *TrainJobWebhook) validateCreate(ctx context.Context, trainjob *TrainJob) (field.ErrorList, error) {
 	var allErrs field.ErrorList
 	allErrs = append(allErrs, jobframework.ValidateJobOnCreate(trainjob)...)
-	return allErrs
+	if features.Enabled(features.TopologyAwareScheduling) {
+		validationErrs, err := w.validateTopologyRequest(ctx, trainjob)
+		if err != nil {
+			return nil, err
+		}
+		allErrs = append(allErrs, validationErrs...)
+	}
+	return allErrs, nil
+}
+
+func (w *TrainJobWebhook) validateTopologyRequest(ctx context.Context, trainJob *TrainJob) (field.ErrorList, error) {
+	var allErrs field.ErrorList
+
+	// TAS user-facing API annotations could be set either in the TrainingRuntime or the TrainJob and will be merged into the generated child jobset
+	childJobSet, err := getChildJobSet(ctx, trainJob)
+	if err != nil {
+		return nil, err
+	}
+	podSets, podSetsErr := (*workloadjobset.JobSet)(childJobSet).PodSets(ctx)
+
+	for i, rj := range childJobSet.Spec.ReplicatedJobs {
```

  After syncing up on Slack, I went with 
  
  ```
  job[node]: <error message>. Adjust either the "TrainJob.spec.podTemplateOverrides" or the "TrainingRuntime.Template" annotations for the corresponding Job
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-15T16:35:01Z

- **pkg/controller/jobs/trainjob/trainjob_webhook.go:155** by [@mimowo](https://github.com/mimowo) — 2025-10-15T16:35:02Z
```diff
@@ -106,20 +117,68 @@ func (w *TrainJobWebhook) ValidateUpdate(ctx context.Context, oldObj, newObj run
 	newTrainJob := fromObject(newObj)
 	log := ctrl.LoggerFrom(ctx).WithName("trainjob-webhook")
 	log.Info("Validating update")
-	return nil, w.validateUpdate(oldTrainJob, newTrainJob).ToAggregate()
+	validationErrs, err := w.validateUpdate(ctx, oldTrainJob, newTrainJob)
+	if err != nil {
+		return nil, err
+	}
+	return nil, validationErrs.ToAggregate()
 }
 
-func (w *TrainJobWebhook) validateUpdate(oldTrainJob, newTrainJob *TrainJob) field.ErrorList {
+func (w *TrainJobWebhook) validateUpdate(ctx context.Context, oldTrainJob, newTrainJob *TrainJob) (field.ErrorList, error) {
 	var allErrs field.ErrorList
 	allErrs = append(allErrs, jobframework.ValidateJobOnUpdate(oldTrainJob, newTrainJob, w.queues.DefaultLocalQueueExist)...)
-	allErrs = append(allErrs, w.validateCreate(newTrainJob)...)
-	return allErrs
+	validationErrs, err := w.validateCreate(ctx, newTrainJob)
+	if err != nil {
+		return nil, err
+	}
+	allErrs = append(allErrs, validationErrs...)
+	return allErrs, nil
 }
 
-func (w *TrainJobWebhook) validateCreate(trainjob *TrainJob) field.ErrorList {
+func (w *TrainJobWebhook) validateCreate(ctx context.Context, trainjob *TrainJob) (field.ErrorList, error) {
 	var allErrs field.ErrorList
 	allErrs = append(allErrs, jobframework.ValidateJobOnCreate(trainjob)...)
-	return allErrs
+	if features.Enabled(features.TopologyAwareScheduling) {
+		validationErrs, err := w.validateTopologyRequest(ctx, trainjob)
+		if err != nil {
+			return nil, err
+		}
+		allErrs = append(allErrs, validationErrs...)
+	}
+	return allErrs, nil
+}
+
+func (w *TrainJobWebhook) validateTopologyRequest(ctx context.Context, trainJob *TrainJob) (field.ErrorList, error) {
+	var allErrs field.ErrorList
+
+	// TAS user-facing API annotations could be set either in the TrainingRuntime or the TrainJob and will be merged into the generated child jobset
+	childJobSet, err := getChildJobSet(ctx, trainJob)
```

  Do we really need to use `getChildJobSet` here? I suppose we could just iterate over podSets returned below, please check? This would make this code job-agnostic.

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-10-16T10:01:18Z

- **pkg/controller/jobs/trainjob/trainjob_webhook.go:155** by [@kaisoz](https://github.com/kaisoz) — 2025-10-16T10:01:18Z
```diff
@@ -106,20 +117,68 @@ func (w *TrainJobWebhook) ValidateUpdate(ctx context.Context, oldObj, newObj run
 	newTrainJob := fromObject(newObj)
 	log := ctrl.LoggerFrom(ctx).WithName("trainjob-webhook")
 	log.Info("Validating update")
-	return nil, w.validateUpdate(oldTrainJob, newTrainJob).ToAggregate()
+	validationErrs, err := w.validateUpdate(ctx, oldTrainJob, newTrainJob)
+	if err != nil {
+		return nil, err
+	}
+	return nil, validationErrs.ToAggregate()
 }
 
-func (w *TrainJobWebhook) validateUpdate(oldTrainJob, newTrainJob *TrainJob) field.ErrorList {
+func (w *TrainJobWebhook) validateUpdate(ctx context.Context, oldTrainJob, newTrainJob *TrainJob) (field.ErrorList, error) {
 	var allErrs field.ErrorList
 	allErrs = append(allErrs, jobframework.ValidateJobOnUpdate(oldTrainJob, newTrainJob, w.queues.DefaultLocalQueueExist)...)
-	allErrs = append(allErrs, w.validateCreate(newTrainJob)...)
-	return allErrs
+	validationErrs, err := w.validateCreate(ctx, newTrainJob)
+	if err != nil {
+		return nil, err
+	}
+	allErrs = append(allErrs, validationErrs...)
+	return allErrs, nil
 }
 
-func (w *TrainJobWebhook) validateCreate(trainjob *TrainJob) field.ErrorList {
+func (w *TrainJobWebhook) validateCreate(ctx context.Context, trainjob *TrainJob) (field.ErrorList, error) {
 	var allErrs field.ErrorList
 	allErrs = append(allErrs, jobframework.ValidateJobOnCreate(trainjob)...)
-	return allErrs
+	if features.Enabled(features.TopologyAwareScheduling) {
+		validationErrs, err := w.validateTopologyRequest(ctx, trainjob)
+		if err != nil {
+			return nil, err
+		}
+		allErrs = append(allErrs, validationErrs...)
+	}
+	return allErrs, nil
+}
+
+func (w *TrainJobWebhook) validateTopologyRequest(ctx context.Context, trainJob *TrainJob) (field.ErrorList, error) {
+	var allErrs field.ErrorList
+
+	// TAS user-facing API annotations could be set either in the TrainingRuntime or the TrainJob and will be merged into the generated child jobset
+	childJobSet, err := getChildJobSet(ctx, trainJob)
```

  yeah! you are totally right, I changed the code to only iterate the podsets

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-16T12:55:06Z

- **pkg/controller/jobs/trainjob/trainjob_controller.go:1** by [@mimowo](https://github.com/mimowo) — 2025-10-16T12:55:06Z
  I think this is already addressed in a separate PR, and this PR does not contain that changes anyway now?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-16T12:56:35Z

- **pkg/controller/jobs/trainjob/trainjob_webhook.go:153** by [@mimowo](https://github.com/mimowo) — 2025-10-16T12:56:35Z
```diff
@@ -106,20 +111,55 @@ func (w *TrainJobWebhook) ValidateUpdate(ctx context.Context, oldObj, newObj run
 	newTrainJob := fromObject(newObj)
 	log := ctrl.LoggerFrom(ctx).WithName("trainjob-webhook")
 	log.Info("Validating update")
-	return nil, w.validateUpdate(oldTrainJob, newTrainJob).ToAggregate()
+	validationErrs, err := w.validateUpdate(ctx, oldTrainJob, newTrainJob)
+	if err != nil {
+		return nil, err
+	}
+	return nil, validationErrs.ToAggregate()
 }
 
-func (w *TrainJobWebhook) validateUpdate(oldTrainJob, newTrainJob *TrainJob) field.ErrorList {
+func (w *TrainJobWebhook) validateUpdate(ctx context.Context, oldTrainJob, newTrainJob *TrainJob) (field.ErrorList, error) {
 	var allErrs field.ErrorList
 	allErrs = append(allErrs, jobframework.ValidateJobOnUpdate(oldTrainJob, newTrainJob, w.queues.DefaultLocalQueueExist)...)
-	allErrs = append(allErrs, w.validateCreate(newTrainJob)...)
-	return allErrs
+	validationErrs, err := w.validateCreate(ctx, newTrainJob)
+	if err != nil {
+		return nil, err
+	}
+	allErrs = append(allErrs, validationErrs...)
+	return allErrs, nil
 }
 
-func (w *TrainJobWebhook) validateCreate(trainjob *TrainJob) field.ErrorList {
+func (w *TrainJobWebhook) validateCreate(ctx context.Context, trainjob *TrainJob) (field.ErrorList, error) {
 	var allErrs field.ErrorList
 	allErrs = append(allErrs, jobframework.ValidateJobOnCreate(trainjob)...)
-	return allErrs
+	if features.Enabled(features.TopologyAwareScheduling) {
+		validationErrs, err := w.validateTopologyRequest(ctx, trainjob)
+		if err != nil {
+			return nil, err
+		}
+		allErrs = append(allErrs, validationErrs...)
+	}
+	return allErrs, nil
+}
+
+func (w *TrainJobWebhook) validateTopologyRequest(ctx context.Context, trainJob *TrainJob) (field.ErrorList, error) {
+	var allErrs field.ErrorList
+
+	podSets, podSetsErr := podSets(ctx, trainJob)
+	for _, p := range podSets {
+		jobPath := field.NewPath("job").Key(string(p.Name))
+		allErrs = append(allErrs, jobframework.ValidateTASPodSetRequest(jobPath, &p.Template.ObjectMeta)...)
+		allErrs = append(allErrs, jobframework.ValidateSliceSizeAnnotationUpperBound(jobPath, &p.Template.ObjectMeta, &p)...)
+	}
```

  I love that, this is much simpler than what we do for other Jobs. I think we should follow this pattern
  
  Let me open an issue for that.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-16T13:00:40Z

- **pkg/controller/jobs/trainjob/trainjob_webhook.go:153** by [@mimowo](https://github.com/mimowo) — 2025-10-16T13:00:40Z
```diff
@@ -106,20 +111,55 @@ func (w *TrainJobWebhook) ValidateUpdate(ctx context.Context, oldObj, newObj run
 	newTrainJob := fromObject(newObj)
 	log := ctrl.LoggerFrom(ctx).WithName("trainjob-webhook")
 	log.Info("Validating update")
-	return nil, w.validateUpdate(oldTrainJob, newTrainJob).ToAggregate()
+	validationErrs, err := w.validateUpdate(ctx, oldTrainJob, newTrainJob)
+	if err != nil {
+		return nil, err
+	}
+	return nil, validationErrs.ToAggregate()
 }
 
-func (w *TrainJobWebhook) validateUpdate(oldTrainJob, newTrainJob *TrainJob) field.ErrorList {
+func (w *TrainJobWebhook) validateUpdate(ctx context.Context, oldTrainJob, newTrainJob *TrainJob) (field.ErrorList, error) {
 	var allErrs field.ErrorList
 	allErrs = append(allErrs, jobframework.ValidateJobOnUpdate(oldTrainJob, newTrainJob, w.queues.DefaultLocalQueueExist)...)
-	allErrs = append(allErrs, w.validateCreate(newTrainJob)...)
-	return allErrs
+	validationErrs, err := w.validateCreate(ctx, newTrainJob)
+	if err != nil {
+		return nil, err
+	}
+	allErrs = append(allErrs, validationErrs...)
+	return allErrs, nil
 }
 
-func (w *TrainJobWebhook) validateCreate(trainjob *TrainJob) field.ErrorList {
+func (w *TrainJobWebhook) validateCreate(ctx context.Context, trainjob *TrainJob) (field.ErrorList, error) {
 	var allErrs field.ErrorList
 	allErrs = append(allErrs, jobframework.ValidateJobOnCreate(trainjob)...)
-	return allErrs
+	if features.Enabled(features.TopologyAwareScheduling) {
+		validationErrs, err := w.validateTopologyRequest(ctx, trainjob)
+		if err != nil {
+			return nil, err
+		}
+		allErrs = append(allErrs, validationErrs...)
+	}
+	return allErrs, nil
+}
+
+func (w *TrainJobWebhook) validateTopologyRequest(ctx context.Context, trainJob *TrainJob) (field.ErrorList, error) {
+	var allErrs field.ErrorList
+
+	podSets, podSetsErr := podSets(ctx, trainJob)
+	for _, p := range podSets {
+		jobPath := field.NewPath("job").Key(string(p.Name))
+		allErrs = append(allErrs, jobframework.ValidateTASPodSetRequest(jobPath, &p.Template.ObjectMeta)...)
+		allErrs = append(allErrs, jobframework.ValidateSliceSizeAnnotationUpperBound(jobPath, &p.Template.ObjectMeta, &p)...)
+	}
```

  Done: https://github.com/kubernetes-sigs/kueue/issues/7297

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-10-16T13:04:36Z

- **pkg/controller/jobs/trainjob/trainjob_controller.go:1** by [@kaisoz](https://github.com/kaisoz) — 2025-10-16T13:04:36Z
  yes! This was addressed in #7249 and is not in this PR anymore

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-10-16T13:05:30Z

- **pkg/controller/jobs/trainjob/trainjob_webhook.go:153** by [@kaisoz](https://github.com/kaisoz) — 2025-10-16T13:05:29Z
```diff
@@ -106,20 +111,55 @@ func (w *TrainJobWebhook) ValidateUpdate(ctx context.Context, oldObj, newObj run
 	newTrainJob := fromObject(newObj)
 	log := ctrl.LoggerFrom(ctx).WithName("trainjob-webhook")
 	log.Info("Validating update")
-	return nil, w.validateUpdate(oldTrainJob, newTrainJob).ToAggregate()
+	validationErrs, err := w.validateUpdate(ctx, oldTrainJob, newTrainJob)
+	if err != nil {
+		return nil, err
+	}
+	return nil, validationErrs.ToAggregate()
 }
 
-func (w *TrainJobWebhook) validateUpdate(oldTrainJob, newTrainJob *TrainJob) field.ErrorList {
+func (w *TrainJobWebhook) validateUpdate(ctx context.Context, oldTrainJob, newTrainJob *TrainJob) (field.ErrorList, error) {
 	var allErrs field.ErrorList
 	allErrs = append(allErrs, jobframework.ValidateJobOnUpdate(oldTrainJob, newTrainJob, w.queues.DefaultLocalQueueExist)...)
-	allErrs = append(allErrs, w.validateCreate(newTrainJob)...)
-	return allErrs
+	validationErrs, err := w.validateCreate(ctx, newTrainJob)
+	if err != nil {
+		return nil, err
+	}
+	allErrs = append(allErrs, validationErrs...)
+	return allErrs, nil
 }
 
-func (w *TrainJobWebhook) validateCreate(trainjob *TrainJob) field.ErrorList {
+func (w *TrainJobWebhook) validateCreate(ctx context.Context, trainjob *TrainJob) (field.ErrorList, error) {
 	var allErrs field.ErrorList
 	allErrs = append(allErrs, jobframework.ValidateJobOnCreate(trainjob)...)
-	return allErrs
+	if features.Enabled(features.TopologyAwareScheduling) {
+		validationErrs, err := w.validateTopologyRequest(ctx, trainjob)
+		if err != nil {
+			return nil, err
+		}
+		allErrs = append(allErrs, validationErrs...)
+	}
+	return allErrs, nil
+}
+
+func (w *TrainJobWebhook) validateTopologyRequest(ctx context.Context, trainJob *TrainJob) (field.ErrorList, error) {
+	var allErrs field.ErrorList
+
+	podSets, podSetsErr := podSets(ctx, trainJob)
+	for _, p := range podSets {
+		jobPath := field.NewPath("job").Key(string(p.Name))
+		allErrs = append(allErrs, jobframework.ValidateTASPodSetRequest(jobPath, &p.Template.ObjectMeta)...)
+		allErrs = append(allErrs, jobframework.ValidateSliceSizeAnnotationUpperBound(jobPath, &p.Template.ObjectMeta, &p)...)
+	}
```

  yes, I realised that most of the integrations use that more complex pattern 😊

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-22T09:31:00Z

- **pkg/controller/jobs/trainjob/trainjob_controller.go:363** by [@mimowo](https://github.com/mimowo) — 2025-10-22T09:31:00Z
```diff
@@ -353,7 +359,8 @@ func (t *TrainJob) PodsReady(ctx context.Context) bool {
 	}
 	var readyReplicas int32
 	for _, jobStatus := range t.Status.JobsStatus {
-		readyReplicas += jobStatus.Ready + jobStatus.Succeeded
+		// No need to check for nil here since the Trainjob admission controller requires the JobStatus to be completely filled
+		readyReplicas += *jobStatus.Ready + *jobStatus.Succeeded
```

  Let's Deref with fallback to 0. I think we shouldn't really rely on the validation in TrainJob, this can change in the future versions.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-22T09:31:26Z

- **pkg/controller/jobs/trainjob/trainjob_controller.go:382** by [@mimowo](https://github.com/mimowo) — 2025-10-22T09:31:26Z
```diff
@@ -372,11 +379,11 @@ func (t *TrainJob) ReclaimablePods(ctx context.Context) ([]kueue.ReclaimablePod,
 
 	for i := range jobset.Spec.ReplicatedJobs {
 		spec := &jobset.Spec.ReplicatedJobs[i]
-		if status, found := statuses[spec.Name]; found && status.Succeeded > 0 {
-			if status.Succeeded > 0 && status.Succeeded <= spec.Replicas {
+		if status, found := statuses[spec.Name]; found && *status.Succeeded > 0 {
```

  Please don't assume status.Succeeded is always set, but use deref

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-22T09:31:38Z

- **pkg/controller/jobs/trainjob/trainjob_controller.go:141** by [@mimowo](https://github.com/mimowo) — 2025-10-22T09:31:38Z
```diff
@@ -137,8 +137,8 @@ func (t *TrainJob) IsSuspended() bool {
 }
 
 func (t *TrainJob) IsActive() bool {
-	for i := range t.Status.JobsStatus {
-		if t.Status.JobsStatus[i].Active > 0 {
+	for _, status := range t.Status.JobsStatus {
+		if status.Active != nil && *status.Active > 0 {
```

  Same here

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-10-22T09:41:54Z

- **pkg/controller/jobs/trainjob/trainjob_controller.go:363** by [@kaisoz](https://github.com/kaisoz) — 2025-10-22T09:41:54Z
```diff
@@ -353,7 +359,8 @@ func (t *TrainJob) PodsReady(ctx context.Context) bool {
 	}
 	var readyReplicas int32
 	for _, jobStatus := range t.Status.JobsStatus {
-		readyReplicas += jobStatus.Ready + jobStatus.Succeeded
+		// No need to check for nil here since the Trainjob admission controller requires the JobStatus to be completely filled
+		readyReplicas += *jobStatus.Ready + *jobStatus.Succeeded
```

  💯 , changing this!

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-10-22T09:43:27Z

- **pkg/controller/jobs/trainjob/trainjob_controller.go:382** by [@kaisoz](https://github.com/kaisoz) — 2025-10-22T09:43:26Z
```diff
@@ -372,11 +379,11 @@ func (t *TrainJob) ReclaimablePods(ctx context.Context) ([]kueue.ReclaimablePod,
 
 	for i := range jobset.Spec.ReplicatedJobs {
 		spec := &jobset.Spec.ReplicatedJobs[i]
-		if status, found := statuses[spec.Name]; found && status.Succeeded > 0 {
-			if status.Succeeded > 0 && status.Succeeded <= spec.Replicas {
+		if status, found := statuses[spec.Name]; found && *status.Succeeded > 0 {
```

  💯 !
