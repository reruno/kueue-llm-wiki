# PR #7266: Add ExpectAdmissionCheckState test util function and use it across e2e/integration tests

**Summary**: Add ExpectAdmissionCheckState test util function and use it across e2e/integration tests

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7266

**Last updated**: 2026-04-14T08:35:37Z

---

## Metadata

- **State**: open
- **Author**: [@aatia-commits](https://github.com/aatia-commits)
- **Branch**: `fix-7021` → `main`
- **Created**: 2025-10-14T15:27:30Z
- **Updated**: 2026-04-14T08:35:37Z
- **Merged**: —
- **Closed**: —
- **Labels**: `size/L`, `release-note-none`, `approved`, `needs-rebase`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: _none_
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 8   **+98 / -155**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Add a test utils function ExpectAdmissionCheckState and refactor tests which check for the admissionCheck state to use it 
To make test more readable and robust and have a common pattern when checking the AdmissionCheck state

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #7021 

#### Special notes for your reviewer:

1- For the `ExpectAdmissionCheckState` initially i wanted made it as wrapper for `gomega.Eventually` block but this to make this work in the test file i either need to call inside another gomega.Eventually block which is not recommended to have nested `gomega.Eventually` blocks because it makes the tests harder to debug and slower
and the other option is to separate  the AdmissionCheckState check logic outside of any gomega.Eventually and call the util function directly which i didn't like since it might have different behaviour and the function is not flexible to use 
so i opted to pass the `gomega.Eventually` `g reference` to the util function and use it like `g.ExpectWithOffset(1` inside the util function which is okay and error handling is working and firing to the parent  gomega.Eventually correctly and i can use it exactly as the current tests implementation 

2- few of the test files have a one liner which they call `FindAdmissionCheck` to just check if the workload has an AdmissionCheck. for those cases i didn't replace this with the util function since it does not add a value or reduce test code

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note

NONE

```

## Discussion

### Comment by [@linux-foundation-easycla[bot]](https://github.com/apps/linux-foundation-easycla) — 2025-10-14T15:27:36Z

<a href="https://easycla.lfx.linuxfoundation.org/#/?version=2"><img src="https://s3.amazonaws.com/cla-project-logo-prod/cla-signed.svg?v=2" alt="CLA Signed" align="left" height="28" width="328" ></a><br/><br />The committers listed above are authorized under a signed CLA.<ul><li>:white_check_mark: login: aatia-commits / name: Abdelrahman (Atia) B.  (083e1995e0d91d28ba5afdc4d545f5ab6d26eb18, 11bf55f97ffec2e3887c006c0ce280cfc0a276ca, 5e1350f41b81ebd89610ff686bba0990542c8d01, a3a5fe3fac32a471397d4168461863312be42ded, b407a48faf0c5b9cd242d934823022a56a3dc146, c01444c521b63607a1e972c93b035e244f8f4dc3, d62331ffa20b2987a5ec822fbd17268e756d04cb, dcb625371f12fa35cb8efadda421133d2ab35d0b, eb4b623b3dd8740ef68b8e089de3e1178e8da561, f0f091ef6f6a6013efdb1b6eedb60f65c0adb1c3)</li></ul>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-10-14T15:27:37Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 083e1995e0d91d28ba5afdc4d545f5ab6d26eb18 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/691d7ab76a38250007214c21 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-14T15:27:39Z

Welcome @aatia-commits! <br><br>It looks like this is your first PR to <a href='https://github.com/kubernetes-sigs/kueue'>kubernetes-sigs/kueue</a> 🎉. Please refer to our [pull request process documentation](https://www.kubernetes.dev/docs/guide/pull-requests/) to help your PR have a smooth ride to approval. <br><br>You will be prompted by a bot to use commands during the review process. Do not be afraid to follow the prompts! It is okay to experiment. [Here is the bot commands documentation](https://go.k8s.io/bot-commands). <br><br>You can also check if kubernetes-sigs/kueue has [its own contribution guidelines](https://github.com/kubernetes-sigs/kueue/tree/master/CONTRIBUTING.md). <br><br>You may want to refer to our [testing guide](https://git.k8s.io/community/contributors/devel/sig-testing/testing.md) if you run into trouble with your tests not passing. <br><br>If you are having difficulty getting your pull request seen, please follow the [recommended escalation practices](https://www.kubernetes.dev/docs/guide/pull-requests/#why-is-my-pull-request-not-getting-reviewed). Also, for tips and tricks in the contribution process you may want to read the [Kubernetes contributor cheat sheet](https://www.kubernetes.dev/docs/contributor-cheatsheet/). We want to make sure your contribution gets all the attention it needs! <br><br>Thank you, and welcome to Kubernetes. :smiley:

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-14T15:27:40Z

Hi @aatia-commits. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-14T15:50:31Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-15T07:06:26Z

cc @PBundyra @pajakd

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-27T06:26:46Z

PR needs rebase.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-03T10:53:53Z

@aatia-commits please rebase when you have a chance

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-03T10:55:27Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7266#" title="Author self-approved">aatia-commits</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7266#pullrequestreview-3744576753" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@kshalot](https://github.com/kshalot) — 2026-02-03T14:12:00Z

@mimowo From what I can see this should probably be extended to check more AdmissionCheck fields, e.g. `RetryCount`, because this was added in the meantime - https://github.com/kubernetes-sigs/kueue/commit/57f960e260c2ad2993e78309742d508062d6c427.

Somebody will have to take this over. I can rebase and align this myself, unless we decide it's a good fit for one of the newjoiners. TBD soon.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-03T15:21:26Z

> I can rebase and align this myself, unless we decide it's a good fit for one of the newjoiners. TBD soon.

I think it might be a good fit for onboarding new contributors indeed, but also feel free to rebase as you have all the context.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-05T12:04:35Z

@aatia-commits the PR looks almost ready, but it seems you are busy with other priorities. Would you like to still work on it, or we could transfer to someone?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-05T12:05:37Z

Oh, I see from https://github.com/kubernetes-sigs/kueue/pull/7266#issuecomment-3841569936 we already agree someone should take over. Let me look for an assignee.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-14T08:35:36Z

@aatia-commits: The following tests **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:

Test name | Commit | Details | Required | Rerun command
--- | --- | --- | --- | ---
pull-kueue-test-e2e-dra-main | 083e1995e0d91d28ba5afdc4d545f5ab6d26eb18 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7266/pull-kueue-test-e2e-dra-main/2009639986076323840) | true | `/test pull-kueue-test-e2e-dra-main`
pull-kueue-test-e2e-main-1-35 | 083e1995e0d91d28ba5afdc4d545f5ab6d26eb18 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7266/pull-kueue-test-e2e-main-1-35/2009664821091373056) | true | `/test pull-kueue-test-e2e-main-1-35`
pull-kueue-test-tas-scheduling-perf-main | 083e1995e0d91d28ba5afdc4d545f5ab6d26eb18 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7266/pull-kueue-test-tas-scheduling-perf-main/2019383404256038912) | true | `/test pull-kueue-test-tas-scheduling-perf-main`
pull-kueue-test-e2e-multikueue-dra-main | 083e1995e0d91d28ba5afdc4d545f5ab6d26eb18 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7266/pull-kueue-test-e2e-multikueue-dra-main/2021672229443473408) | true | `/test pull-kueue-test-e2e-multikueue-dra-main`
pull-kueue-test-e2e-multikueue-sequential-main | 083e1995e0d91d28ba5afdc4d545f5ab6d26eb18 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7266/pull-kueue-test-e2e-multikueue-sequential-main/2043971307170697216) | true | `/test pull-kueue-test-e2e-multikueue-sequential-main`

[Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=7266). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3Aaatia-commits). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository. I understand the commands that are listed [here](https://go.k8s.io/bot-commands).
</details>
<!-- test report -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-15T16:38:04Z

- **test/util/util.go:607** by [@mimowo](https://github.com/mimowo) — 2025-10-15T16:38:04Z
```diff
@@ -595,6 +595,18 @@ func ExpectAdmissionChecksWaitTimeMetric(cq *kueue.ClusterQueue, priorityClass s
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func ExpectAdmissionCheckState(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
+	check := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(admissionCheckName))
+	g.ExpectWithOffset(1, check).NotTo(gomega.BeNil())
+	g.ExpectWithOffset(1, check.State).To(gomega.Equal(expectedState))
+	if expectedMessage != "" {
+		g.ExpectWithOffset(1, check.Message).To(gomega.Equal(expectedMessage))
+	}
+	if len(podSetUpdates) > 0 {
+		g.ExpectWithOffset(1, check.PodSetUpdates).To(gomega.Equal(podSetUpdates))
+	}
```

  Could we assert here unconditionally?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-15T16:40:08Z

LGTM overall, just a nit question

### Review by [@aatia-commits](https://github.com/aatia-commits) (commented) — 2025-10-16T07:55:39Z

- **test/util/util.go:607** by [@aatia-commits](https://github.com/aatia-commits) — 2025-10-16T07:55:39Z
```diff
@@ -595,6 +595,18 @@ func ExpectAdmissionChecksWaitTimeMetric(cq *kueue.ClusterQueue, priorityClass s
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func ExpectAdmissionCheckState(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
+	check := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(admissionCheckName))
+	g.ExpectWithOffset(1, check).NotTo(gomega.BeNil())
+	g.ExpectWithOffset(1, check.State).To(gomega.Equal(expectedState))
+	if expectedMessage != "" {
+		g.ExpectWithOffset(1, check.Message).To(gomega.Equal(expectedMessage))
+	}
+	if len(podSetUpdates) > 0 {
+		g.ExpectWithOffset(1, check.PodSetUpdates).To(gomega.Equal(podSetUpdates))
+	}
```

  Not all test cases assert on podSetUpdates.
  most of them were actually ignoring it. If I remove the if condition here, it will assert an empty array against whatever value is in check.PodSetUpdates, which might cause many of the cases to fail.  I would need to send podSetUpdates in order for the assertion to work, which is not very flexible. what do you think?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-16T07:58:58Z

- **test/util/util.go:607** by [@mimowo](https://github.com/mimowo) — 2025-10-16T07:58:58Z
```diff
@@ -595,6 +595,18 @@ func ExpectAdmissionChecksWaitTimeMetric(cq *kueue.ClusterQueue, priorityClass s
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func ExpectAdmissionCheckState(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
+	check := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(admissionCheckName))
+	g.ExpectWithOffset(1, check).NotTo(gomega.BeNil())
+	g.ExpectWithOffset(1, check.State).To(gomega.Equal(expectedState))
+	if expectedMessage != "" {
+		g.ExpectWithOffset(1, check.Message).To(gomega.Equal(expectedMessage))
+	}
+	if len(podSetUpdates) > 0 {
+		g.ExpectWithOffset(1, check.PodSetUpdates).To(gomega.Equal(podSetUpdates))
+	}
```

  I think the tests don't assert on that for historical reasons - they were added before PodSetUpdates was introduced. Most of them would pass I expect if you just pass nil as PodSetUpdates anyway. 
  
  Worth running an experiment.

### Review by [@aatia-commits](https://github.com/aatia-commits) (commented) — 2025-10-16T09:10:21Z

- **test/util/util.go:607** by [@aatia-commits](https://github.com/aatia-commits) — 2025-10-16T09:10:21Z
```diff
@@ -595,6 +595,18 @@ func ExpectAdmissionChecksWaitTimeMetric(cq *kueue.ClusterQueue, priorityClass s
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func ExpectAdmissionCheckState(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
+	check := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(admissionCheckName))
+	g.ExpectWithOffset(1, check).NotTo(gomega.BeNil())
+	g.ExpectWithOffset(1, check.State).To(gomega.Equal(expectedState))
+	if expectedMessage != "" {
+		g.ExpectWithOffset(1, check.Message).To(gomega.Equal(expectedMessage))
+	}
+	if len(podSetUpdates) > 0 {
+		g.ExpectWithOffset(1, check.PodSetUpdates).To(gomega.Equal(podSetUpdates))
+	}
```

  A lot of the provisioning test are failing because Expected
        ```<[]v1beta1.PodSetUpdate | len:2, cap:2>: [
            {
                Name: "ps1",
                Labels: nil,
                Annotations: {
                    "autoscaling.x-k8s.io/consume-provisioning-request": "wl-ac-prov-1",
                    "autoscaling.x-k8s.io/provisioning-class-name": "provisioning-class",
                },
                NodeSelector: nil,
                Tolerations: nil,
            },
            {
                Name: "ps2",
                Labels: nil,
                Annotations: {
                    "autoscaling.x-k8s.io/consume-provisioning-request": "wl-ac-prov-1",
                    "autoscaling.x-k8s.io/provisioning-class-name": "provisioning-class",
                },
                NodeSelector: nil,
                Tolerations: nil,
            },
        ]
    to equal
        <[]v1beta1.PodSetUpdate | len:0, cap:0>: nil```
  do we need to assert on PodSetUpdate for each one ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-16T09:22:10Z

- **test/util/util.go:607** by [@mimowo](https://github.com/mimowo) — 2025-10-16T09:22:09Z
```diff
@@ -595,6 +595,18 @@ func ExpectAdmissionChecksWaitTimeMetric(cq *kueue.ClusterQueue, priorityClass s
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func ExpectAdmissionCheckState(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
+	check := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(admissionCheckName))
+	g.ExpectWithOffset(1, check).NotTo(gomega.BeNil())
+	g.ExpectWithOffset(1, check.State).To(gomega.Equal(expectedState))
+	if expectedMessage != "" {
+		g.ExpectWithOffset(1, check.Message).To(gomega.Equal(expectedMessage))
+	}
+	if len(podSetUpdates) > 0 {
+		g.ExpectWithOffset(1, check.PodSetUpdates).To(gomega.Equal(podSetUpdates))
+	}
```

  I see, still I think `if len(podSetUpdates) > 0 {`  is tricky because a reader may assume the podSetUpdates are empty (without reading the assert function), while they are not.  Also, there is no way to assert the podSetUpdates should be empty.
  
  I think it is reasonable to drop the special cases and asserts, and just assert on that, we can have a const like defaultPodSetUpdates used. 
  
  Alternative would be to pass somehow the gomega expectation / matcher for the AdmissionCheckState, but I'm not clear how this will look like in practice (I don't recall any other examples in the codebase).
  
  I would also like to wrap the waiting with Eventually into the helper.

### Review by [@aatia-commits](https://github.com/aatia-commits) (commented) — 2025-10-16T11:22:33Z

- **test/util/util.go:607** by [@aatia-commits](https://github.com/aatia-commits) — 2025-10-16T11:22:33Z
```diff
@@ -595,6 +595,18 @@ func ExpectAdmissionChecksWaitTimeMetric(cq *kueue.ClusterQueue, priorityClass s
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func ExpectAdmissionCheckState(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
+	check := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(admissionCheckName))
+	g.ExpectWithOffset(1, check).NotTo(gomega.BeNil())
+	g.ExpectWithOffset(1, check.State).To(gomega.Equal(expectedState))
+	if expectedMessage != "" {
+		g.ExpectWithOffset(1, check.Message).To(gomega.Equal(expectedMessage))
+	}
+	if len(podSetUpdates) > 0 {
+		g.ExpectWithOffset(1, check.PodSetUpdates).To(gomega.Equal(podSetUpdates))
+	}
```

  okay i like the idea of defaultPodSetUpdates, this should work and without adding a check in the util function
  will do it

### Review by [@aatia-commits](https://github.com/aatia-commits) (commented) — 2025-10-16T13:25:48Z

- **test/util/util.go:607** by [@aatia-commits](https://github.com/aatia-commits) — 2025-10-16T13:25:48Z
```diff
@@ -595,6 +595,18 @@ func ExpectAdmissionChecksWaitTimeMetric(cq *kueue.ClusterQueue, priorityClass s
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func ExpectAdmissionCheckState(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
+	check := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(admissionCheckName))
+	g.ExpectWithOffset(1, check).NotTo(gomega.BeNil())
+	g.ExpectWithOffset(1, check.State).To(gomega.Equal(expectedState))
+	if expectedMessage != "" {
+		g.ExpectWithOffset(1, check.Message).To(gomega.Equal(expectedMessage))
+	}
+	if len(podSetUpdates) > 0 {
+		g.ExpectWithOffset(1, check.PodSetUpdates).To(gomega.Equal(podSetUpdates))
+	}
```

  updated

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-17T06:32:55Z

- **test/util/util.go:79** by [@mimowo](https://github.com/mimowo) — 2025-10-17T06:32:55Z
```diff
@@ -77,6 +77,40 @@ import (
 	"sigs.k8s.io/kueue/pkg/workloadslicing"
 )
 
+var DefaultTestPodSetUpdates1 = []kueue.PodSetUpdate{
```

  Ah. these are dependent on the workload, ac and prov req names, so I don't think this belongs to util.go.
  
  Let's just define the consts in the provisioning_test.go, unexported. Think of the smallest possible scope of declaration.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-17T06:33:40Z

- **test/util/util.go:663** by [@mimowo](https://github.com/mimowo) — 2025-10-17T06:33:40Z
```diff
@@ -595,6 +629,16 @@ func ExpectAdmissionChecksWaitTimeMetric(cq *kueue.ClusterQueue, priorityClass s
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func ExpectAdmissionCheckState(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
```

  Wrap the assert inside with `gomega.EventuallyWithOffset(` (as in the function above). This will allow us to remove the Eventually code in the outer tests.

### Review by [@aatia-commits](https://github.com/aatia-commits) (commented) — 2025-10-17T06:39:57Z

- **test/util/util.go:663** by [@aatia-commits](https://github.com/aatia-commits) — 2025-10-17T06:39:57Z
```diff
@@ -595,6 +629,16 @@ func ExpectAdmissionChecksWaitTimeMetric(cq *kueue.ClusterQueue, priorityClass s
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func ExpectAdmissionCheckState(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
```

  I explained why i needed to do it without Eventually block here in the PR description 
  some the outer tests has other logic inside their  Eventually block , we would need to split them into multiple Eventually this way 
  and also one is using Consistently and not Eventually
  how do you suggest to handle that ?

### Review by [@aatia-commits](https://github.com/aatia-commits) (commented) — 2025-10-17T06:41:16Z

- **test/util/util.go:79** by [@aatia-commits](https://github.com/aatia-commits) — 2025-10-17T06:41:15Z
```diff
@@ -77,6 +77,40 @@ import (
 	"sigs.k8s.io/kueue/pkg/workloadslicing"
 )
 
+var DefaultTestPodSetUpdates1 = []kueue.PodSetUpdate{
```

  i noticed only 4 test case uses this DefaultTestPodSetUpdates1 and 2 uses DefaultTestPodSetUpdates2, what do you think about just remove it compelenty and use the values in the function call directly ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-17T06:52:54Z

- **test/util/util.go:663** by [@mimowo](https://github.com/mimowo) — 2025-10-17T06:52:54Z
```diff
@@ -595,6 +629,16 @@ func ExpectAdmissionChecksWaitTimeMetric(cq *kueue.ClusterQueue, priorityClass s
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func ExpectAdmissionCheckState(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
```

  I see, I think it still better to wrap in eventually to save complexity in the typical usage. 
  If the check is mixed with other asserts in the same Eventually just move it down to dedicated check outside of that Eventually.
  
  The consistently are tricker, let's see one by one:
  - https://github.com/kubernetes-sigs/kueue/pull/7266/files#diff-b524dd7e94e695c9007bf094b1ff9cd5a94952214c0db7de004c7ce5bea7ed1bR545-R547 - just move below as Eventually
  - https://github.com/kubernetes-sigs/kueue/pull/7266/files#diff-b524dd7e94e695c9007bf094b1ff9cd5a94952214c0db7de004c7ce5bea7ed1bR837-R840 - unfold using the helper, or introduce 2 helpers: ExpectAdmissionCheckState (waiting) , `VerifyAdmissionCheckState` (non waiting). The waiting would call the shared code with `VerifyAdmissionCheckState`

### Review by [@aatia-commits](https://github.com/aatia-commits) (commented) — 2025-11-19T08:16:21Z

- **test/util/util.go:663** by [@aatia-commits](https://github.com/aatia-commits) — 2025-11-19T08:16:14Z
```diff
@@ -595,6 +629,16 @@ func ExpectAdmissionChecksWaitTimeMetric(cq *kueue.ClusterQueue, priorityClass s
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func ExpectAdmissionCheckState(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
```

  hey sorry for the very late reply i was on vacation for the last 3 weeks 
  just to confirm what needs to be done here is to add a new ExpectAdmissionCheckState (waiting) util that uses gomega.Eventually and calls this none waiting new one and we use new waiting function for cases where it's simple and there is no other assertion inside the bigger block ?
  
  if this is the ask here so we don't have alot of cases where we just assert on the AdmissionCheck and it usually follow with some other checks so that is why i used the none waiting one

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-03T10:55:17Z

/approve
The PR looks quite good overall, and is aligned with my expectations. Leaving the final tagging (lgtm after rebase and addressing of remaining comments) to @kshalot or @PBundyra who are overwatching the related cleanup issues: https://github.com/kubernetes-sigs/kueue/issues/7021 and https://github.com/kubernetes-sigs/kueue/issues/6331
