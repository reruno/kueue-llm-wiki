# PR #73: Add support for affinity matching

**Summary**: Add support for affinity matching

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/73

**Last updated**: 2022-03-01T14:21:48Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ahg-g](https://github.com/ahg-g)
- **Branch**: `ahg-affinity` → `main`
- **Created**: 2022-02-25T20:29:48Z
- **Updated**: 2022-03-01T14:21:48Z
- **Merged**: 2022-03-01T14:21:47Z
- **Closed**: 2022-03-01T14:21:48Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 7   **+367 / -7**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:
Adds support for affinity matching with flavors

#### Which issue(s) this PR fixes:
Fixes #3

#### Special notes for your reviewer:

still need to add integration tests

## Discussion

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-02-25T20:29:57Z

/hold

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-02-25T20:30:01Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/73#" title="Author self-approved">ahg-g</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [ahg-g]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-02-28T17:29:56Z

/retest

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-28T19:59:00Z

/lgtm

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-02-28T20:14:48Z

squashed

/hold cancel

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-28T20:18:03Z

/lgtm

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-28T20:35:44Z

/retest

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-01T13:40:26Z

/retest

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-01T14:20:02Z

/retest

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-02-25T21:09:38Z

- **pkg/capacity/capacity.go:81** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-25T20:40:40Z
```diff
@@ -77,6 +78,7 @@ type Capacity struct {
 	RequestableResources map[corev1.ResourceName][]FlavorInfo
 	UsedResources        Resources
 	Workloads            map[string]*workload.Info
+	LabelKeys            map[corev1.ResourceName]sets.String
```

  worth a comment for the purpose of these.
  
  alternatively, is it worth combining it with RequestableResources map by putting `[]FlavorInfo` and `sets.String` into a struct?

- **pkg/capacity/capacity.go:373** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-25T20:44:46Z
```diff
@@ -351,12 +363,20 @@ func resourcesByName(in []kueue.Resource) map[corev1.ResourceName][]FlavorInfo {
 		flavors := make([]FlavorInfo, len(r.Flavors))
 		for i := range flavors {
 			f := &r.Flavors[i]
-			flavors[i] = FlavorInfo{
+			fInfo := FlavorInfo{
 				Name:       f.Name,
 				Guaranteed: workload.ResourceValue(r.Name, f.Quota.Guaranteed),
 				Ceiling:    workload.ResourceValue(r.Name, f.Quota.Ceiling),
 				Taints:     append([]corev1.Taint(nil), f.Taints...),
 			}
+			if len(f.Labels) != 0 {
+				fInfo.Labels = map[string]string{}
```

  use `make` with a size

- **pkg/scheduler/scheduler.go:290** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-25T20:54:32Z
```diff
@@ -256,6 +265,47 @@ func findFlavorForResource(name corev1.ResourceName, val int64, wUsed map[string
 	return "", 0
 }
 
+func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.RequiredNodeAffinity {
+	var specCopy corev1.PodSpec
+
+	// Remove affinity constraints with irrelevant keys.
+	if len(spec.NodeSelector) != 0 {
+		specCopy.NodeSelector = map[string]string{}
+		for k, v := range spec.NodeSelector {
+			if allowedKeys.Has(k) {
+				specCopy.NodeSelector[k] = v
+			}
+		}
+	}
+
+	affinity := spec.Affinity
+	if affinity != nil && affinity.NodeAffinity != nil && affinity.NodeAffinity.RequiredDuringSchedulingIgnoredDuringExecution != nil {
+		var termsCopy []corev1.NodeSelectorTerm
+		for _, t := range affinity.NodeAffinity.RequiredDuringSchedulingIgnoredDuringExecution.NodeSelectorTerms {
+			var expCopy []corev1.NodeSelectorRequirement
+			for _, e := range t.MatchExpressions {
+				if allowedKeys.Has(e.Key) {
+					expCopy = append(expCopy, e)
+				}
+			}
+			if len(expCopy) != 0 {
```

  if there are no requirements left, that means that this term should match ANY node. We should keep this term.

- **pkg/scheduler/scheduler.go:320** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-25T21:00:02Z
```diff
@@ -256,6 +265,47 @@ func findFlavorForResource(name corev1.ResourceName, val int64, wUsed map[string
 	return "", 0
 }
 
+func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.RequiredNodeAffinity {
+	var specCopy corev1.PodSpec
+
+	// Remove affinity constraints with irrelevant keys.
+	if len(spec.NodeSelector) != 0 {
+		specCopy.NodeSelector = map[string]string{}
+		for k, v := range spec.NodeSelector {
+			if allowedKeys.Has(k) {
+				specCopy.NodeSelector[k] = v
+			}
+		}
+	}
+
+	affinity := spec.Affinity
+	if affinity != nil && affinity.NodeAffinity != nil && affinity.NodeAffinity.RequiredDuringSchedulingIgnoredDuringExecution != nil {
+		var termsCopy []corev1.NodeSelectorTerm
+		for _, t := range affinity.NodeAffinity.RequiredDuringSchedulingIgnoredDuringExecution.NodeSelectorTerms {
+			var expCopy []corev1.NodeSelectorRequirement
+			for _, e := range t.MatchExpressions {
+				if allowedKeys.Has(e.Key) {
+					expCopy = append(expCopy, e)
+				}
+			}
+			if len(expCopy) != 0 {
+				termsCopy = append(termsCopy, corev1.NodeSelectorTerm{MatchExpressions: expCopy})
+			}
+		}
+		if len(termsCopy) != 0 {
+			specCopy.Affinity = &corev1.Affinity{
+				NodeAffinity: &corev1.NodeAffinity{
+					RequiredDuringSchedulingIgnoredDuringExecution: &corev1.NodeSelector{
+						NodeSelectorTerms: termsCopy,
+					},
+				},
+			}
+		}
+
+	}
+	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
```

  this function returns a LazyErrorNodeSelector, which was there just for backwards compatibility purposes.
  
  We should use a regular NodeSelector and fail assignment if there are parsing errors.

- **pkg/scheduler/scheduler.go:246** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-25T21:05:43Z
```diff
@@ -241,12 +243,19 @@ func (s *Scheduler) assign(ctx context.Context, e *entry) error {
 // If it finds a flavor, also returns any borrowing required.
 func findFlavorForResource(name corev1.ResourceName, val int64, wUsed map[string]int64, cap *capacity.Capacity, spec *corev1.PodSpec) (string, int64) {
 	for _, flavor := range cap.RequestableResources[name] {
+		// We will only check against the flavors labels.
```

  for the resource
  ```suggestion
  		// We will only check against the flavors labels for the resource.
  ```

- **pkg/scheduler/scheduler.go:247** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-25T21:09:24Z
```diff
@@ -241,12 +243,19 @@ func (s *Scheduler) assign(ctx context.Context, e *entry) error {
 // If it finds a flavor, also returns any borrowing required.
 func findFlavorForResource(name corev1.ResourceName, val int64, wUsed map[string]int64, cap *capacity.Capacity, spec *corev1.PodSpec) (string, int64) {
 	for _, flavor := range cap.RequestableResources[name] {
+		// We will only check against the flavors labels.
+		selector := flavorSelector(spec, cap.LabelKeys[name])
```

  optimization opportunity: build the selector before calling `findFlavorForResource`, once for every resource.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-02-25T22:31:00Z

- **pkg/capacity/capacity.go:81** by [@ahg-g](https://github.com/ahg-g) — 2022-02-25T22:20:26Z
```diff
@@ -77,6 +78,7 @@ type Capacity struct {
 	RequestableResources map[corev1.ResourceName][]FlavorInfo
 	UsedResources        Resources
 	Workloads            map[string]*workload.Info
+	LabelKeys            map[corev1.ResourceName]sets.String
```

  thought about merging, but it really doesn't solve any problem and one less type to care about.

- **pkg/scheduler/scheduler.go:247** by [@ahg-g](https://github.com/ahg-g) — 2022-02-25T22:23:17Z
```diff
@@ -241,12 +243,19 @@ func (s *Scheduler) assign(ctx context.Context, e *entry) error {
 // If it finds a flavor, also returns any borrowing required.
 func findFlavorForResource(name corev1.ResourceName, val int64, wUsed map[string]int64, cap *capacity.Capacity, spec *corev1.PodSpec) (string, int64) {
 	for _, flavor := range cap.RequestableResources[name] {
+		// We will only check against the flavors labels.
+		selector := flavorSelector(spec, cap.LabelKeys[name])
```

  well, I should just move it outside the loop :)

- **pkg/scheduler/scheduler.go:290** by [@ahg-g](https://github.com/ahg-g) — 2022-02-25T22:27:08Z
```diff
@@ -256,6 +265,47 @@ func findFlavorForResource(name corev1.ResourceName, val int64, wUsed map[string
 	return "", 0
 }
 
+func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.RequiredNodeAffinity {
+	var specCopy corev1.PodSpec
+
+	// Remove affinity constraints with irrelevant keys.
+	if len(spec.NodeSelector) != 0 {
+		specCopy.NodeSelector = map[string]string{}
+		for k, v := range spec.NodeSelector {
+			if allowedKeys.Has(k) {
+				specCopy.NodeSelector[k] = v
+			}
+		}
+	}
+
+	affinity := spec.Affinity
+	if affinity != nil && affinity.NodeAffinity != nil && affinity.NodeAffinity.RequiredDuringSchedulingIgnoredDuringExecution != nil {
+		var termsCopy []corev1.NodeSelectorTerm
+		for _, t := range affinity.NodeAffinity.RequiredDuringSchedulingIgnoredDuringExecution.NodeSelectorTerms {
+			var expCopy []corev1.NodeSelectorRequirement
+			for _, e := range t.MatchExpressions {
+				if allowedKeys.Has(e.Key) {
+					expCopy = append(expCopy, e)
+				}
+			}
+			if len(expCopy) != 0 {
```

  isn't that the same as no term?

- **pkg/scheduler/scheduler.go:320** by [@ahg-g](https://github.com/ahg-g) — 2022-02-25T22:29:44Z
```diff
@@ -256,6 +265,47 @@ func findFlavorForResource(name corev1.ResourceName, val int64, wUsed map[string
 	return "", 0
 }
 
+func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.RequiredNodeAffinity {
+	var specCopy corev1.PodSpec
+
+	// Remove affinity constraints with irrelevant keys.
+	if len(spec.NodeSelector) != 0 {
+		specCopy.NodeSelector = map[string]string{}
+		for k, v := range spec.NodeSelector {
+			if allowedKeys.Has(k) {
+				specCopy.NodeSelector[k] = v
+			}
+		}
+	}
+
+	affinity := spec.Affinity
+	if affinity != nil && affinity.NodeAffinity != nil && affinity.NodeAffinity.RequiredDuringSchedulingIgnoredDuringExecution != nil {
+		var termsCopy []corev1.NodeSelectorTerm
+		for _, t := range affinity.NodeAffinity.RequiredDuringSchedulingIgnoredDuringExecution.NodeSelectorTerms {
+			var expCopy []corev1.NodeSelectorRequirement
+			for _, e := range t.MatchExpressions {
+				if allowedKeys.Has(e.Key) {
+					expCopy = append(expCopy, e)
+				}
+			}
+			if len(expCopy) != 0 {
+				termsCopy = append(termsCopy, corev1.NodeSelectorTerm{MatchExpressions: expCopy})
+			}
+		}
+		if len(termsCopy) != 0 {
+			specCopy.Affinity = &corev1.Affinity{
+				NodeAffinity: &corev1.NodeAffinity{
+					RequiredDuringSchedulingIgnoredDuringExecution: &corev1.NodeSelector{
+						NodeSelectorTerms: termsCopy,
+					},
+				},
+			}
+		}
+
+	}
+	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
```

  I am replicating the scheduler code, I think we need to be consistent with that since it is what will be used eventually to match the pods against nodes.

- **pkg/scheduler/scheduler.go:290** by [@ahg-g](https://github.com/ahg-g) — 2022-02-25T22:30:36Z
```diff
@@ -256,6 +265,47 @@ func findFlavorForResource(name corev1.ResourceName, val int64, wUsed map[string
 	return "", 0
 }
 
+func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.RequiredNodeAffinity {
+	var specCopy corev1.PodSpec
+
+	// Remove affinity constraints with irrelevant keys.
+	if len(spec.NodeSelector) != 0 {
+		specCopy.NodeSelector = map[string]string{}
+		for k, v := range spec.NodeSelector {
+			if allowedKeys.Has(k) {
+				specCopy.NodeSelector[k] = v
+			}
+		}
+	}
+
+	affinity := spec.Affinity
+	if affinity != nil && affinity.NodeAffinity != nil && affinity.NodeAffinity.RequiredDuringSchedulingIgnoredDuringExecution != nil {
+		var termsCopy []corev1.NodeSelectorTerm
+		for _, t := range affinity.NodeAffinity.RequiredDuringSchedulingIgnoredDuringExecution.NodeSelectorTerms {
+			var expCopy []corev1.NodeSelectorRequirement
+			for _, e := range t.MatchExpressions {
+				if allowedKeys.Has(e.Key) {
+					expCopy = append(expCopy, e)
+				}
+			}
+			if len(expCopy) != 0 {
```

  if not, can you suggest a unit test (easier than explaining in detail what you have in mind :))?

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-02-28T13:11:57Z

- **pkg/scheduler/scheduler.go:280** by [@denkensk](https://github.com/denkensk) — 2022-02-28T13:06:29Z
```diff
@@ -256,6 +264,47 @@ func findFlavorForResource(name corev1.ResourceName, val int64, wUsed map[string
 	return "", 0
 }
 
+func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.RequiredNodeAffinity {
+	var specCopy corev1.PodSpec
```

  use `var requiredNodeAffinity nodeaffinity.RequiredNodeAffinity` directly?

- **pkg/scheduler/scheduler_test.go:878** by [@denkensk](https://github.com/denkensk) — 2022-02-28T13:09:31Z
```diff
@@ -875,6 +875,120 @@ func TestEntryAssignFlavors(t *testing.T) {
 				},
 			},
 		},
+		"multiple flavors, fits a node selector": {
```

  consider adding a case with `doesn't fit a node selector/affinity`?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-02-28T14:00:31Z

- **pkg/scheduler/scheduler.go:280** by [@ahg-g](https://github.com/ahg-g) — 2022-02-28T14:00:31Z
```diff
@@ -256,6 +264,47 @@ func findFlavorForResource(name corev1.ResourceName, val int64, wUsed map[string
 	return "", 0
 }
 
+func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.RequiredNodeAffinity {
+	var specCopy corev1.PodSpec
```

  we need the spec because we are also collecting NodeSelector (see line 275 below). It is also the type we pass in the matching function at the end.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-02-28T14:08:13Z

- **pkg/scheduler/scheduler_test.go:878** by [@ahg-g](https://github.com/ahg-g) — 2022-02-28T14:08:13Z
```diff
@@ -875,6 +875,120 @@ func TestEntryAssignFlavors(t *testing.T) {
 				},
 			},
 		},
+		"multiple flavors, fits a node selector": {
```

  added

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-02-28T14:10:24Z

- **pkg/scheduler/scheduler.go:280** by [@denkensk](https://github.com/denkensk) — 2022-02-28T14:10:24Z
```diff
@@ -256,6 +264,47 @@ func findFlavorForResource(name corev1.ResourceName, val int64, wUsed map[string
 	return "", 0
 }
 
+func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.RequiredNodeAffinity {
+	var specCopy corev1.PodSpec
```

  I know. I means if you can put the NodeSelector and the Affinity you collected in RequiredNodeAffinity directly instead of using `nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})` ?
  ```go
  return RequiredNodeAffinity{labelSelector: labels.SelectorFromSet(nodeSelector), nodeSelector: affinity}
  ```

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-02-28T14:25:13Z

- **pkg/scheduler/scheduler.go:280** by [@ahg-g](https://github.com/ahg-g) — 2022-02-28T14:25:13Z
```diff
@@ -256,6 +264,47 @@ func findFlavorForResource(name corev1.ResourceName, val int64, wUsed map[string
 	return "", 0
 }
 
+func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.RequiredNodeAffinity {
+	var specCopy corev1.PodSpec
```

  we can't, labelSelector and nodeSelector are not exported

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-02-28T15:27:01Z

- **pkg/scheduler/scheduler.go:290** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-28T15:23:40Z
```diff
@@ -256,6 +265,47 @@ func findFlavorForResource(name corev1.ResourceName, val int64, wUsed map[string
 	return "", 0
 }
 
+func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.RequiredNodeAffinity {
+	var specCopy corev1.PodSpec
+
+	// Remove affinity constraints with irrelevant keys.
+	if len(spec.NodeSelector) != 0 {
+		specCopy.NodeSelector = map[string]string{}
+		for k, v := range spec.NodeSelector {
+			if allowedKeys.Has(k) {
+				specCopy.NodeSelector[k] = v
+			}
+		}
+	}
+
+	affinity := spec.Affinity
+	if affinity != nil && affinity.NodeAffinity != nil && affinity.NodeAffinity.RequiredDuringSchedulingIgnoredDuringExecution != nil {
+		var termsCopy []corev1.NodeSelectorTerm
+		for _, t := range affinity.NodeAffinity.RequiredDuringSchedulingIgnoredDuringExecution.NodeSelectorTerms {
+			var expCopy []corev1.NodeSelectorRequirement
+			for _, e := range t.MatchExpressions {
+				if allowedKeys.Has(e.Key) {
+					expCopy = append(expCopy, e)
+				}
+			}
+			if len(expCopy) != 0 {
```

  Let's say you have:
  
  ```(gpu=a10) || (non_type_label=foo)```
  
  This should be interpreted as
  
  ```(gpu=a10) || true```
  
  instead of
  
  ```
  gpu=a10
  ```

- **pkg/scheduler/scheduler.go:254** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-28T15:24:53Z
```diff
@@ -240,13 +242,19 @@ func (s *Scheduler) assign(ctx context.Context, e *entry) error {
 // given that wUsed is the usage of flavors by previous podsets.
 // If it finds a flavor, also returns any borrowing required.
 func findFlavorForResource(name corev1.ResourceName, val int64, wUsed map[string]int64, cap *capacity.Capacity, spec *corev1.PodSpec) (string, int64) {
+	// We will only check against the flavors' labels for the resource.
+	selector := flavorSelector(spec, cap.LabelKeys[name])
 	for _, flavor := range cap.RequestableResources[name] {
 		_, untolerated := corev1helpers.FindMatchingUntoleratedTaint(flavor.Taints, spec.Tolerations, func(t *corev1.Taint) bool {
 			return t.Effect == corev1.TaintEffectNoSchedule || t.Effect == corev1.TaintEffectNoExecute
 		})
 		if untolerated {
 			continue
 		}
+		if match, _ := selector.Match(&corev1.Node{ObjectMeta: metav1.ObjectMeta{Labels: flavor.Labels}}); !match {
```

  Fail instead of ignoring the error. Scheduler would also fail.

- **pkg/scheduler/scheduler.go:320** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-28T15:25:58Z
```diff
@@ -256,6 +265,47 @@ func findFlavorForResource(name corev1.ResourceName, val int64, wUsed map[string
 	return "", 0
 }
 
+func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.RequiredNodeAffinity {
+	var specCopy corev1.PodSpec
+
+	// Remove affinity constraints with irrelevant keys.
+	if len(spec.NodeSelector) != 0 {
+		specCopy.NodeSelector = map[string]string{}
+		for k, v := range spec.NodeSelector {
+			if allowedKeys.Has(k) {
+				specCopy.NodeSelector[k] = v
+			}
+		}
+	}
+
+	affinity := spec.Affinity
+	if affinity != nil && affinity.NodeAffinity != nil && affinity.NodeAffinity.RequiredDuringSchedulingIgnoredDuringExecution != nil {
+		var termsCopy []corev1.NodeSelectorTerm
+		for _, t := range affinity.NodeAffinity.RequiredDuringSchedulingIgnoredDuringExecution.NodeSelectorTerms {
+			var expCopy []corev1.NodeSelectorRequirement
+			for _, e := range t.MatchExpressions {
+				if allowedKeys.Has(e.Key) {
+					expCopy = append(expCopy, e)
+				}
+			}
+			if len(expCopy) != 0 {
+				termsCopy = append(termsCopy, corev1.NodeSelectorTerm{MatchExpressions: expCopy})
+			}
+		}
+		if len(termsCopy) != 0 {
+			specCopy.Affinity = &corev1.Affinity{
+				NodeAffinity: &corev1.NodeAffinity{
+					RequiredDuringSchedulingIgnoredDuringExecution: &corev1.NodeSelector{
+						NodeSelectorTerms: termsCopy,
+					},
+				},
+			}
+		}
+
+	}
+	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
```

  Sg, please add a comment.

- **test/integration/scheduler/scheduler_test.go:28** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-28T15:26:14Z
```diff
@@ -25,6 +25,7 @@ import (
 	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	"k8s.io/apimachinery/pkg/types"
+	// kueue "sigs.k8s.io/kueue/api/v1alpha1"
```

  left over?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-02-28T17:17:01Z

- **pkg/scheduler/scheduler.go:254** by [@ahg-g](https://github.com/ahg-g) — 2022-02-28T16:22:55Z
```diff
@@ -240,13 +242,19 @@ func (s *Scheduler) assign(ctx context.Context, e *entry) error {
 // given that wUsed is the usage of flavors by previous podsets.
 // If it finds a flavor, also returns any borrowing required.
 func findFlavorForResource(name corev1.ResourceName, val int64, wUsed map[string]int64, cap *capacity.Capacity, spec *corev1.PodSpec) (string, int64) {
+	// We will only check against the flavors' labels for the resource.
+	selector := flavorSelector(spec, cap.LabelKeys[name])
 	for _, flavor := range cap.RequestableResources[name] {
 		_, untolerated := corev1helpers.FindMatchingUntoleratedTaint(flavor.Taints, spec.Tolerations, func(t *corev1.Taint) bool {
 			return t.Effect == corev1.TaintEffectNoSchedule || t.Effect == corev1.TaintEffectNoExecute
 		})
 		if untolerated {
 			continue
 		}
+		if match, _ := selector.Match(&corev1.Node{ObjectMeta: metav1.ObjectMeta{Labels: flavor.Labels}}); !match {
```

  logged an error, and returned with not flavor assigned

- **pkg/scheduler/scheduler.go:320** by [@ahg-g](https://github.com/ahg-g) — 2022-02-28T16:24:26Z
```diff
@@ -256,6 +265,47 @@ func findFlavorForResource(name corev1.ResourceName, val int64, wUsed map[string
 	return "", 0
 }
 
+func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.RequiredNodeAffinity {
+	var specCopy corev1.PodSpec
+
+	// Remove affinity constraints with irrelevant keys.
+	if len(spec.NodeSelector) != 0 {
+		specCopy.NodeSelector = map[string]string{}
+		for k, v := range spec.NodeSelector {
+			if allowedKeys.Has(k) {
+				specCopy.NodeSelector[k] = v
+			}
+		}
+	}
+
+	affinity := spec.Affinity
+	if affinity != nil && affinity.NodeAffinity != nil && affinity.NodeAffinity.RequiredDuringSchedulingIgnoredDuringExecution != nil {
+		var termsCopy []corev1.NodeSelectorTerm
+		for _, t := range affinity.NodeAffinity.RequiredDuringSchedulingIgnoredDuringExecution.NodeSelectorTerms {
+			var expCopy []corev1.NodeSelectorRequirement
+			for _, e := range t.MatchExpressions {
+				if allowedKeys.Has(e.Key) {
+					expCopy = append(expCopy, e)
+				}
+			}
+			if len(expCopy) != 0 {
+				termsCopy = append(termsCopy, corev1.NodeSelectorTerm{MatchExpressions: expCopy})
+			}
+		}
+		if len(termsCopy) != 0 {
+			specCopy.Affinity = &corev1.Affinity{
+				NodeAffinity: &corev1.NodeAffinity{
+					RequiredDuringSchedulingIgnoredDuringExecution: &corev1.NodeSelector{
+						NodeSelectorTerms: termsCopy,
+					},
+				},
+			}
+		}
+
+	}
+	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
```

  added

- **pkg/scheduler/scheduler.go:290** by [@ahg-g](https://github.com/ahg-g) — 2022-02-28T17:16:58Z
```diff
@@ -256,6 +265,47 @@ func findFlavorForResource(name corev1.ResourceName, val int64, wUsed map[string
 	return "", 0
 }
 
+func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.RequiredNodeAffinity {
+	var specCopy corev1.PodSpec
+
+	// Remove affinity constraints with irrelevant keys.
+	if len(spec.NodeSelector) != 0 {
+		specCopy.NodeSelector = map[string]string{}
+		for k, v := range spec.NodeSelector {
+			if allowedKeys.Has(k) {
+				specCopy.NodeSelector[k] = v
+			}
+		}
+	}
+
+	affinity := spec.Affinity
+	if affinity != nil && affinity.NodeAffinity != nil && affinity.NodeAffinity.RequiredDuringSchedulingIgnoredDuringExecution != nil {
+		var termsCopy []corev1.NodeSelectorTerm
+		for _, t := range affinity.NodeAffinity.RequiredDuringSchedulingIgnoredDuringExecution.NodeSelectorTerms {
+			var expCopy []corev1.NodeSelectorRequirement
+			for _, e := range t.MatchExpressions {
+				if allowedKeys.Has(e.Key) {
+					expCopy = append(expCopy, e)
+				}
+			}
+			if len(expCopy) != 0 {
```

  good catch, although empty term doesn't match any, but we can simply ignore all terms.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-02-28T19:55:56Z

- **pkg/scheduler/scheduler.go:290** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-28T19:55:56Z
```diff
@@ -256,6 +265,47 @@ func findFlavorForResource(name corev1.ResourceName, val int64, wUsed map[string
 	return "", 0
 }
 
+func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.RequiredNodeAffinity {
+	var specCopy corev1.PodSpec
+
+	// Remove affinity constraints with irrelevant keys.
+	if len(spec.NodeSelector) != 0 {
+		specCopy.NodeSelector = map[string]string{}
+		for k, v := range spec.NodeSelector {
+			if allowedKeys.Has(k) {
+				specCopy.NodeSelector[k] = v
+			}
+		}
+	}
+
+	affinity := spec.Affinity
+	if affinity != nil && affinity.NodeAffinity != nil && affinity.NodeAffinity.RequiredDuringSchedulingIgnoredDuringExecution != nil {
+		var termsCopy []corev1.NodeSelectorTerm
+		for _, t := range affinity.NodeAffinity.RequiredDuringSchedulingIgnoredDuringExecution.NodeSelectorTerms {
+			var expCopy []corev1.NodeSelectorRequirement
+			for _, e := range t.MatchExpressions {
+				if allowedKeys.Has(e.Key) {
+					expCopy = append(expCopy, e)
+				}
+			}
+			if len(expCopy) != 0 {
```

  right... once there is order support, we might want to do something else.
  
  I thought empty expressions meant match all. We can check later.
