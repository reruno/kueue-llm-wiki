# PR #7311: KEP: FailureRecoveryPolicy for gracefully handling "stuck" terminating Pods

**Summary**: KEP: FailureRecoveryPolicy for gracefully handling "stuck" terminating Pods

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7311

**Last updated**: 2025-11-19T14:40:02Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kshalot](https://github.com/kshalot)
- **Branch**: `6757-node-failure-recovery-kep` → `main`
- **Created**: 2025-10-17T10:03:33Z
- **Updated**: 2025-11-19T14:40:02Z
- **Merged**: 2025-11-19T14:40:00Z
- **Closed**: 2025-11-19T14:40:01Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 2   **+446 / -0**

## Description

#### What type of PR is this?
/kind documentation

#### What this PR does / why we need it:
This is a KEP for the feature requested in #6757. 

#### Which issue(s) this PR fixes:
Fixes #

#### Special notes for your reviewer:
~~This is a WIP~~, but reviewable version of the KEP, which contains the following working names:
* "FailureRecovery" as an umbrella term for the feature that is being implemented.
* "zombie pod" as a name for the pods that are "stuck" terminating. It's a little colloquial though, so maybe it should be renamed across the KEP/PoC.

I'm posting this KEP for review to get initial feedback and flag obvious issues (this is my first KEP).

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-10-17T10:03:42Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 2d918cf1853bcfa5dce3a0229c73b7c0f4b9db8f |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/691dc8b90594090008c7f369 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-17T10:03:43Z

Hi @kshalot. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-17T10:04:57Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-17T12:49:35Z

cc @sanposhiho @ichekrygin @mwysokin

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-17T13:13:41Z

/retitle KEP: FailureRecoveryPolicy for gracefully handling "stuck" terminating Pods
proposal

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-18T16:33:57Z

/lgtm
/approve
Let me put on hold in case @tenzen-y also wants to review 
/hold

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-18T16:34:06Z

LGTM label has been added.  <details>Git tree hash: 92216569ce0ed9f70aa34856da58ff4910a7afa1</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-19T14:38:11Z

LGTM label has been added.  <details>Git tree hash: fce3a23df36e797a46521a7ff913121f589c5425</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-19T14:38:11Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7311#" title="Author self-approved">kshalot</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7311#issuecomment-3548513310" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7311#pullrequestreview-3410407219" title="Approved">olekzabl</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7311#pullrequestreview-3483094377" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-19T14:39:38Z

/hold cancel

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-17T10:37:34Z

- **keps/6757-node-failure-recovery/README.md:108** by [@mimowo](https://github.com/mimowo) — 2025-10-17T10:37:34Z
```diff
@@ -0,0 +1,295 @@
+# KEP-6757: Node Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Managing the `node.kubernetes.io/out-of-service` taint on `Node`](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, coloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in it's behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `NodeFailureRecoveryStrategy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // NodeFailureRecoveryStrategy is used to enable automatic node failure recovery mechanisms.
+	// +optional
+  NodeFailureRecoveryStrategy *NodeFailureRecoveryStrategy `json:"nodeFailureRecoveryStrategy,omitempty"`
```

  Three high-level comments to the API proposal:
  1. let't generalize the concept of failure recovery, if anything we could pivot towards PodFailureRecovery, but I think we can keep generic "FailureRecovery".
  2. Let's make API which allows for multiple policies (array), you can get inspired by k8s API for podFailure Policy: https://kubernetes.io/docs/tasks/job/pod-failure-policy/
  3. Let's add Pod LabelSelector which will be a knob which allows both sides of the contract to opt-in, admin by enabling the feature, and users (ML researchers) by creating Jobs with matching the labelSelector.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-10-17T10:53:32Z

- **keps/6757-node-failure-recovery/README.md:108** by [@kshalot](https://github.com/kshalot) — 2025-10-17T10:53:32Z
```diff
@@ -0,0 +1,295 @@
+# KEP-6757: Node Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Managing the `node.kubernetes.io/out-of-service` taint on `Node`](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, coloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in it's behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `NodeFailureRecoveryStrategy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // NodeFailureRecoveryStrategy is used to enable automatic node failure recovery mechanisms.
+	// +optional
+  NodeFailureRecoveryStrategy *NodeFailureRecoveryStrategy `json:"nodeFailureRecoveryStrategy,omitempty"`
```

  1. Agreed.
  2. I preferred to make it an array, but I wanted to discuss how the different strategies might overlap/conflict first. I think with the addition of the label selector, an array is definitely the way to go - we'll allow to define different grace periods for different cases (e.g. with labels "no-risk", "low-risk" or something).
  3. Agreed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-17T11:34:58Z

- **keps/6757-node-failure-recovery/README.md:108** by [@mimowo](https://github.com/mimowo) — 2025-10-17T11:34:58Z
```diff
@@ -0,0 +1,295 @@
+# KEP-6757: Node Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Managing the `node.kubernetes.io/out-of-service` taint on `Node`](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, coloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in it's behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `NodeFailureRecoveryStrategy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // NodeFailureRecoveryStrategy is used to enable automatic node failure recovery mechanisms.
+	// +optional
+  NodeFailureRecoveryStrategy *NodeFailureRecoveryStrategy `json:"nodeFailureRecoveryStrategy,omitempty"`
```

  Drawing inspiration from podFailurePolicy we would just use the first "matching" strategy for a given Pod.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-17T12:47:34Z

- **keps/6757-failure-recovery/kep.yaml:20** by [@mimowo](https://github.com/mimowo) — 2025-10-17T12:36:31Z
```diff
@@ -0,0 +1,34 @@
+title: Failure Recovery
+kep-number: 6757
+authors:
+  - "@kshalot"
+status: provisional
+creation-date: 2025-10-17
+reviewers:
+  - "@mimowo"
+  - "@olekzabl"
+  - "@pbundyra"
+approvers:
+  - "@mimowo"
+  - "@tenzen-y"
+
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.1"
```

  ```suggestion
  latest-milestone: "v0.15"
  ```
  nit

- **keps/6757-failure-recovery/kep.yaml:24** by [@mimowo](https://github.com/mimowo) — 2025-10-17T12:36:48Z
```diff
@@ -0,0 +1,34 @@
+title: Failure Recovery
+kep-number: 6757
+authors:
+  - "@kshalot"
+status: provisional
+creation-date: 2025-10-17
+reviewers:
+  - "@mimowo"
+  - "@olekzabl"
+  - "@pbundyra"
+approvers:
+  - "@mimowo"
+  - "@tenzen-y"
+
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.1"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.2"
```

  ```suggestion
    alpha: "v0.15"
  ```
  nit

- **keps/6757-failure-recovery/kep.yaml:29** by [@mimowo](https://github.com/mimowo) — 2025-10-17T12:37:02Z
```diff
@@ -0,0 +1,34 @@
+title: Failure Recovery
+kep-number: 6757
+authors:
+  - "@kshalot"
+status: provisional
+creation-date: 2025-10-17
+reviewers:
+  - "@mimowo"
+  - "@olekzabl"
+  - "@pbundyra"
+approvers:
+  - "@mimowo"
+  - "@tenzen-y"
+
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.1"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.2"
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+feature-gates:
+  - name: NodeFailureRecovery
```

  ```suggestion
    - name: FailureRecoveryPolicy
  ```

- **keps/6757-failure-recovery/README.md:111** by [@mimowo](https://github.com/mimowo) — 2025-10-17T12:39:33Z
```diff
@@ -0,0 +1,307 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Managing the `node.kubernetes.io/out-of-service` taint on `Node`](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, coloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in it's behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
```

  Add that as a mitigation we make this behavior opt-in on two levels: admin needs to enable in the configuration, and users need to create Pods with labels matching the configured label selector by the admin.

- **keps/6757-failure-recovery/README.md:113** by [@mimowo](https://github.com/mimowo) — 2025-10-17T12:43:54Z
```diff
@@ -0,0 +1,307 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Managing the `node.kubernetes.io/out-of-service` taint on `Node`](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, coloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in it's behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicies`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+	// +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // FailureRecoveryRules specifies the rules to be enabled for failure recovery.  
+  FailureRecoveryRules []FailureRecoveryRule `json:"rules"`
```

  Just ` Rules []FailureRecoveryRule `json:"rules"``, same in other places. Both will woth, but this is the convention

- **keps/6757-failure-recovery/README.md:122** by [@mimowo](https://github.com/mimowo) — 2025-10-17T12:44:38Z
```diff
@@ -0,0 +1,307 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Managing the `node.kubernetes.io/out-of-service` taint on `Node`](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, coloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in it's behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicies`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+	// +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // FailureRecoveryRules specifies the rules to be enabled for failure recovery.  
+  FailureRecoveryRules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // FailureRecoveryAction specifies the action taken to recover from failures.  
+  FailureRecoveryAction FailureRecoveryAction `json:"action"`
+
+  // FailureRecoveryScopeSelector specifies the scope of resources covered by failure recovery -
+  // resources not matching the selector behave according to the default Kubernetes mechanism.
+  FailureRecoveryScopeSelector *metav1.LabelSelector `json:"recoveryScope,omitempty"`
```

  ```suggestion
    FailureRecoveryPodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
  ```
  This will be easier for users to understand I think as most are familiar witht he concept of LabelSelectors.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-17T12:48:54Z

- **keps/6757-failure-recovery/kep.yaml:29** by [@mimowo](https://github.com/mimowo) — 2025-10-17T12:48:54Z
```diff
@@ -0,0 +1,34 @@
+title: Failure Recovery
+kep-number: 6757
+authors:
+  - "@kshalot"
+status: provisional
+creation-date: 2025-10-17
+reviewers:
+  - "@mimowo"
+  - "@olekzabl"
+  - "@pbundyra"
+approvers:
+  - "@mimowo"
+  - "@tenzen-y"
+
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.1"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.2"
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+feature-gates:
+  - name: NodeFailureRecovery
```

  Maybe also retitle the KEP to FailureRecoveryPolicy, suggesting future extensibility by the use of the generic "policy" word.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-10-17T13:06:54Z

- **keps/6757-failure-recovery/kep.yaml:34** by [@kshalot](https://github.com/kshalot) — 2025-10-17T13:06:54Z
```diff
@@ -0,0 +1,34 @@
+title: Failure Recovery
+kep-number: 6757
+authors:
+  - "@kshalot"
+status: provisional
+creation-date: 2025-10-17
+reviewers:
+  - "@mimowo"
+  - "@olekzabl"
+  - "@pbundyra"
+approvers:
+  - "@mimowo"
+  - "@tenzen-y"
+
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.15"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.15"
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+feature-gates:
+  - name: FailureRecovery
+disable-supported: true
+
+# The following PRR answers are required at beta release
+metrics:
+  - my_feature_metric
```

  Wasn't sure what this is for, so I did not remove this yet.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-17T13:12:41Z

- **keps/6757-failure-recovery/kep.yaml:34** by [@mimowo](https://github.com/mimowo) — 2025-10-17T13:12:40Z
```diff
@@ -0,0 +1,34 @@
+title: Failure Recovery
+kep-number: 6757
+authors:
+  - "@kshalot"
+status: provisional
+creation-date: 2025-10-17
+reviewers:
+  - "@mimowo"
+  - "@olekzabl"
+  - "@pbundyra"
+approvers:
+  - "@mimowo"
+  - "@tenzen-y"
+
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.15"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.15"
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+feature-gates:
+  - name: FailureRecovery
+disable-supported: true
+
+# The following PRR answers are required at beta release
+metrics:
+  - my_feature_metric
```

  It is not needed for apha, just comment out

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-31T15:01:13Z

LGTM overall, would love to also get a pass from @tenzen-y

- **keps/6757-failure-recovery/README.md:38** by [@mimowo](https://github.com/mimowo) — 2025-10-31T14:40:15Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
```

  ```suggestion
  This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
  ```

- **keps/6757-failure-recovery/README.md:334** by [@mimowo](https://github.com/mimowo) — 2025-10-31T14:48:47Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicies`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+	// +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.  
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Action specifies the action taken to recover from failures.  
+  Action FailureRecoveryAction `json:"action"`
+
+  // PodLabelSelector specifies the scope of resources covered by failure recovery -
+  // resources not matching the selector behave according to the default Kubernetes mechanism.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+	
+  // PodTerminationConfig contains configuration for the `PodTerminationConfig` strategy.
+	// +optional
+  PodTerminationConfig *PodTerminationConfig `json:"podTerminationConfig,omitempty"`
+}
+
+type PodTerminationConfig struct {
+	// PodTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  ForcefulTerminationGracePeriod *time.Duration `json:"forcefulTerminationGracePeriod"`
+}
+
+type FailureRecoveryAction string
+
+const (
+	PodTermination FailureRecoveryAction = "PodTermination"
+)
+```
+
+This API requires opt-in both from the administrators and user's end, as the workloads would
+have to be labeled in order for them to be covered with failure recovery.
+
+An example failure recovery configuration:
+
+```yaml
+failureRecoveryPolicy:
+  rules:
+  - action: PodTermination
+    podLabelSelector:
+      safe-to-fail: true
+    podTerminationConfig:
+      forcefulTerminationGracePeriod: 5m
+```
+
+### Default Grace Period
+
+The proposal is to **not** set a default value for the forceful termination grace period.
+This puts configuration over convention, as it will make the user consciously think about the
+grace period the need for their specific use case, without risking a faulty default.
+
+Since enabling the feature is inherently risky, this serves as an additional explicit
+acknowledgement of this risk from the user's end.
+
+Alternatively, a default value can be inferred from the `terminationGracePeriodSeconds`.
+For example, it could be a multiple of that value. Nevertheless, this still runs the risk of
+setting a very small value or even 0, depending on the user's configuration.
+
+### Controller
+
+Setting the strategy type to `PodTermination` will enable a controller, which manages
+the failed nodes.
+
+The controller has to ignore any updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil || pod.DeletionGracePeriodSeconds == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not managed by Kueue.
+    * The `kueue.x-k8s.io/managed` label is set for pods with pod integration enabled.
+    * The presence of the `kueue.x-k8s.io/podset` label could be used in other cases.
+1. Match the label selector defined in the configured `FailureRecoveryPolicy`.
+
+
+For terminating pods matching the criteria, the controller schedules another reconciliation
+to happen after the remaining grace period elapses:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+	now := r.clock.Now()
+	gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+	totalGracePeriod := gracefulTerminationPeriod + zombiePodTerminationGracePeriod
+	if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+		gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+		return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+	}
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and deleted:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+	pod.Status.Phase = corev1.PodFailed
+	if err := r.client.Status().Update(ctx, pod); err != nil {
+		return ctrl.Result{}, err
+	}
+
+	return ctrl.Result{}, nil
+}
+```
+
+### Test Plan
+
+[ ] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+The proposal will be covered with unit tests for:
+1. Configuration parsing.
+1. Controller behavior:
+    1. Whether it ignores irrelevant pods (not terminating, not managed by Kueue, already failed/succeeded, not labeled).
+    1. Whether it correctly schedules a reconciliation for when the grace period elapses.
+    1. Whether it updates the pod's phase to `Failed` after the grace period elapses.
+
+#### Integration Tests
+
+The proposal will be covered with integrations tests that check whether:
+1. Adding a deletion timestamp to the pod requeues a reconciliation loop for when the grace period elapses.
+1. After the grace period elapses, the pod is marked as `Failed`.
+1. Replacement pods are scheduled in the place of the failed pod (optional, technically 1+2 is sufficient to prove this).
+
+
+Existing integration tests should prove that this feature does not impact Kueue during normal operation.
+
+### Graduation Criteria
+
+#### Alpha
+
+- Positive feedback from the users.
+
+## Implementation History
+
+2025-09-08: The [issue](https://github.com/kubernetes-sigs/kueue/issues/6757) is raised in Kueue.
+
+2025-09-12: The [issue](https://github.com/kubernetes/kubernetes/issues/134038) is raised in core Kubernetes.
+
+2025-10-17: First draft of the KEP.
+
+## Drawbacks
+
+* The same feature is discussed in core Kubernetes ([kubernetes/issues/134038](https://github.com/kubernetes/kubernetes/issues/134038)), so the underlying issue could potentially be fixed upstream. The timeline of an upstream change is long, but if the feature is deemed not time-critical, it could be fixed at the source instead of in Kueue.
+* This feature introduces a potential footgun to Kueue users and could turn out to be to volatile/risky to use in most affected cases.
+* It introduces yet another responsibility for Kueue - on top of quota management and scheduling, it will also start performing failure recovery.
+
+## Alternatives
+
+### Do Nothing
+
+Since this feature spawned a lengthy discussion about its riskiness and there are other controllers in the ecosystem which deal with
+node problem remediation (e.g. [medik8s](https://github.com/medik8s/self-node-remediation)), an alternative would be to do nothing,
+wait for the upstream conversations to be resolved and propose an alternative solution (external to Kueue) to the affected users.
+
+#### Advantages
+
+1. Requires no implementation effort.
+
+#### Drawbacks
+
+1. Forces users to run another system alongside Kueue, making deploying Kueue more complex those cases.
+
+### Hide The Controller Behind A Feature Gate
+
+Instead of introducing changes to the API, a feature gate could be used to get some initial feedback about the
+feature and its risks.
+
+#### Advantages
+
+1. Easy to implement, less moving parts.
+1. Allows to gather feedback quickly.
+
+#### Drawbacks
+
+1. It won't allow to implement the pod selectors and configurations for the failure recovery,
+limiting the implementation to defaults:
+  1. `podLabelSelector` - everything.
+  1. `forcefulTerminationGracePeriod` - a constant or a multiple of `terminationGracePeriodSeconds`.
+
+### Managing The `node.kubernetes.io/out-of-service` Taint On `Node`
+
+Core Kubernetes already contains logic for [automatic garbage collection](https://github.com/kubernetes/kubernetes/blob/4870d987d0a4aac2d9223d4c0b9f22858c0d1590/pkg/controller/podgc/gc_controller.go) of `Pod`s running on a `Node` with the `node.kubernetes.io/out-of-service` taint.
+It updates the `Pod`s status and deletes it from `etcd`. The recovery controller could use this fact to terminate stuck pods by automatically adding this taint to nodes which are not ready for some configurable time.
+
+Given the API proposal, this approach could potentially be implemented as an alternative
+ recovery strategy in the future.
+
+ ```go
+type OutOfServiceNodeTaintingConfig struct {
+  NotReadyNodeTimeout *time.Duration `json:"notReadyNodeTimeout"`
+}
+
+type FailureRecoveryAction string
+
+const (
+  PodTermination FailureRecoveryAction = "PodTermination"
+  TaintingNodeOutOfService FailureRecoveryAction = "TaintingNodeOutOfService"
+)
+ ```
+
+#### Advantages
+
+1. It uses an existing Kubernetes mechanism.
+2. Concerns are tidily separated - the recovery controller simply says the node is out of service, the garbage collector in the control plane can decide how and when to clean it up.
+
+
+#### Drawbacks
```

  nit: I would suggest not to use the "Drawbacks" subsection, because there is already such section referring to the selected approach. Just to avoid a reader getting tricked into counting the nesting levels. For the "Alterneatives" section we usually just have the "reasons to defer" paragraph (take a look at the TAS KEP).

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-10-31T16:25:44Z

- **keps/6757-failure-recovery/README.md:44** by [@olekzabl](https://github.com/olekzabl) — 2025-10-31T15:10:50Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
```

  ```suggestion
  When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
  ```

- **keps/6757-failure-recovery/README.md:69** by [@olekzabl](https://github.com/olekzabl) — 2025-10-31T15:11:07Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
```

  ```suggestion
  * Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
  ```

- **keps/6757-failure-recovery/README.md:96** by [@olekzabl](https://github.com/olekzabl) — 2025-10-31T15:13:40Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
```

  ```suggestion
  As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
  ```

- **keps/6757-failure-recovery/README.md:108** by [@olekzabl](https://github.com/olekzabl) — 2025-10-31T15:14:48Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicies`:
```

  ```suggestion
  The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
  ```

- **keps/6757-failure-recovery/README.md:146** by [@olekzabl](https://github.com/olekzabl) — 2025-10-31T15:17:36Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicies`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+	// +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.  
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Action specifies the action taken to recover from failures.  
+  Action FailureRecoveryAction `json:"action"`
+
+  // PodLabelSelector specifies the scope of resources covered by failure recovery -
+  // resources not matching the selector behave according to the default Kubernetes mechanism.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+	
+  // PodTerminationConfig contains configuration for the `PodTerminationConfig` strategy.
+	// +optional
+  PodTerminationConfig *PodTerminationConfig `json:"podTerminationConfig,omitempty"`
+}
+
+type PodTerminationConfig struct {
+	// PodTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  ForcefulTerminationGracePeriod *time.Duration `json:"forcefulTerminationGracePeriod"`
+}
+
+type FailureRecoveryAction string
+
+const (
+	PodTermination FailureRecoveryAction = "PodTermination"
```

  Nit: wouldn't we like an "action" to sound more "verb-like"? I mean, `TerminatePod`.
  (If this enum stays at all)

- **keps/6757-failure-recovery/README.md:132** by [@olekzabl](https://github.com/olekzabl) — 2025-10-31T15:32:38Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicies`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+	// +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.  
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Action specifies the action taken to recover from failures.  
+  Action FailureRecoveryAction `json:"action"`
+
+  // PodLabelSelector specifies the scope of resources covered by failure recovery -
+  // resources not matching the selector behave according to the default Kubernetes mechanism.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+	
+  // PodTerminationConfig contains configuration for the `PodTerminationConfig` strategy.
```

  "XConfig contains config for the `XConfig` strategy" - it reads puzzling, also a bit recurring.
  
  Did you mean that it contains config for the "pod termination" `Action`?
  (Am I guessing right that, should we ever have other supported `Action`s, this field may be ignored for them?)
  
  Assuming that "yes", another question: when `Action` is `PodTermination`, then is this field still `optional`? (Because below in KEP you say about deliberately not setting a default for it).
  
  ---
  
  From a distance: it seems to me that you need a "one-of" pattern (for `PodTermination`, use this config; for other future `Actions`, use another config), and - while I haven't seen much of K8s codebase - I have an impression that the standard way for doing this is like [here](https://github.com/kubernetes/kubernetes/blob/4ca91a6542802f1339caf6f2f8dfe917beb5ce9c/pkg/apis/core/types.go#L57). Which, in this case, would mean getting rid of `Action`, and instead having just
  
  ```
  PodLabelSelector *LabelSelector
  
  // Exactly one of the fields below must be specified.
  
  // +optional
  PodTermination *PodTerminationConfig `json: "omitempty"`
  
  // and in the future
  // +optional
  SomeOtherAction *SomeOtherActionConfig `json: "omitempty"`
  ```
  
  And then, you read the action from which config is set.
  But for each action, its config is clearly required (unless some sub-fields are explicitly optional).
  
  WDYT?

- **keps/6757-failure-recovery/README.md:203** by [@olekzabl](https://github.com/olekzabl) — 2025-10-31T15:37:59Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicies`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+	// +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.  
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Action specifies the action taken to recover from failures.  
+  Action FailureRecoveryAction `json:"action"`
+
+  // PodLabelSelector specifies the scope of resources covered by failure recovery -
+  // resources not matching the selector behave according to the default Kubernetes mechanism.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+	
+  // PodTerminationConfig contains configuration for the `PodTerminationConfig` strategy.
+	// +optional
+  PodTerminationConfig *PodTerminationConfig `json:"podTerminationConfig,omitempty"`
+}
+
+type PodTerminationConfig struct {
+	// PodTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  ForcefulTerminationGracePeriod *time.Duration `json:"forcefulTerminationGracePeriod"`
+}
+
+type FailureRecoveryAction string
+
+const (
+	PodTermination FailureRecoveryAction = "PodTermination"
+)
+```
+
+This API requires opt-in both from the administrators and user's end, as the workloads would
+have to be labeled in order for them to be covered with failure recovery.
+
+An example failure recovery configuration:
+
+```yaml
+failureRecoveryPolicy:
+  rules:
+  - action: PodTermination
+    podLabelSelector:
+      safe-to-fail: true
+    podTerminationConfig:
+      forcefulTerminationGracePeriod: 5m
+```
+
+### Default Grace Period
+
+The proposal is to **not** set a default value for the forceful termination grace period.
+This puts configuration over convention, as it will make the user consciously think about the
+grace period the need for their specific use case, without risking a faulty default.
+
+Since enabling the feature is inherently risky, this serves as an additional explicit
+acknowledgement of this risk from the user's end.
+
+Alternatively, a default value can be inferred from the `terminationGracePeriodSeconds`.
+For example, it could be a multiple of that value. Nevertheless, this still runs the risk of
+setting a very small value or even 0, depending on the user's configuration.
+
+### Controller
+
+Setting the strategy type to `PodTermination` will enable a controller, which manages
+the failed nodes.
+
+The controller has to ignore any updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil || pod.DeletionGracePeriodSeconds == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not managed by Kueue.
+    * The `kueue.x-k8s.io/managed` label is set for pods with pod integration enabled.
+    * The presence of the `kueue.x-k8s.io/podset` label could be used in other cases.
+1. Match the label selector defined in the configured `FailureRecoveryPolicy`.
+
+
+For terminating pods matching the criteria, the controller schedules another reconciliation
+to happen after the remaining grace period elapses:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+	now := r.clock.Now()
+	gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+	totalGracePeriod := gracefulTerminationPeriod + zombiePodTerminationGracePeriod
```

  Is `zombiePodTGP` same as `forcefulTGP` declared above?

- **keps/6757-failure-recovery/README.md:191** by [@olekzabl](https://github.com/olekzabl) — 2025-10-31T15:40:40Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicies`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+	// +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.  
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Action specifies the action taken to recover from failures.  
+  Action FailureRecoveryAction `json:"action"`
+
+  // PodLabelSelector specifies the scope of resources covered by failure recovery -
+  // resources not matching the selector behave according to the default Kubernetes mechanism.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+	
+  // PodTerminationConfig contains configuration for the `PodTerminationConfig` strategy.
+	// +optional
+  PodTerminationConfig *PodTerminationConfig `json:"podTerminationConfig,omitempty"`
+}
+
+type PodTerminationConfig struct {
+	// PodTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  ForcefulTerminationGracePeriod *time.Duration `json:"forcefulTerminationGracePeriod"`
+}
+
+type FailureRecoveryAction string
+
+const (
+	PodTermination FailureRecoveryAction = "PodTermination"
+)
+```
+
+This API requires opt-in both from the administrators and user's end, as the workloads would
+have to be labeled in order for them to be covered with failure recovery.
+
+An example failure recovery configuration:
+
+```yaml
+failureRecoveryPolicy:
+  rules:
+  - action: PodTermination
+    podLabelSelector:
+      safe-to-fail: true
+    podTerminationConfig:
+      forcefulTerminationGracePeriod: 5m
+```
+
+### Default Grace Period
+
+The proposal is to **not** set a default value for the forceful termination grace period.
+This puts configuration over convention, as it will make the user consciously think about the
+grace period the need for their specific use case, without risking a faulty default.
+
+Since enabling the feature is inherently risky, this serves as an additional explicit
+acknowledgement of this risk from the user's end.
+
+Alternatively, a default value can be inferred from the `terminationGracePeriodSeconds`.
+For example, it could be a multiple of that value. Nevertheless, this still runs the risk of
+setting a very small value or even 0, depending on the user's configuration.
+
+### Controller
+
+Setting the strategy type to `PodTermination` will enable a controller, which manages
+the failed nodes.
+
+The controller has to ignore any updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil || pod.DeletionGracePeriodSeconds == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not managed by Kueue.
+    * The `kueue.x-k8s.io/managed` label is set for pods with pod integration enabled.
+    * The presence of the `kueue.x-k8s.io/podset` label could be used in other cases.
+1. Match the label selector defined in the configured `FailureRecoveryPolicy`.
```

  Match -> don't match? (These are rules for _ignoring_)

- **keps/6757-failure-recovery/README.md:190** by [@olekzabl](https://github.com/olekzabl) — 2025-10-31T15:42:56Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicies`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+	// +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.  
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Action specifies the action taken to recover from failures.  
+  Action FailureRecoveryAction `json:"action"`
+
+  // PodLabelSelector specifies the scope of resources covered by failure recovery -
+  // resources not matching the selector behave according to the default Kubernetes mechanism.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+	
+  // PodTerminationConfig contains configuration for the `PodTerminationConfig` strategy.
+	// +optional
+  PodTerminationConfig *PodTerminationConfig `json:"podTerminationConfig,omitempty"`
+}
+
+type PodTerminationConfig struct {
+	// PodTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  ForcefulTerminationGracePeriod *time.Duration `json:"forcefulTerminationGracePeriod"`
+}
+
+type FailureRecoveryAction string
+
+const (
+	PodTermination FailureRecoveryAction = "PodTermination"
+)
+```
+
+This API requires opt-in both from the administrators and user's end, as the workloads would
+have to be labeled in order for them to be covered with failure recovery.
+
+An example failure recovery configuration:
+
+```yaml
+failureRecoveryPolicy:
+  rules:
+  - action: PodTermination
+    podLabelSelector:
+      safe-to-fail: true
+    podTerminationConfig:
+      forcefulTerminationGracePeriod: 5m
+```
+
+### Default Grace Period
+
+The proposal is to **not** set a default value for the forceful termination grace period.
+This puts configuration over convention, as it will make the user consciously think about the
+grace period the need for their specific use case, without risking a faulty default.
+
+Since enabling the feature is inherently risky, this serves as an additional explicit
+acknowledgement of this risk from the user's end.
+
+Alternatively, a default value can be inferred from the `terminationGracePeriodSeconds`.
+For example, it could be a multiple of that value. Nevertheless, this still runs the risk of
+setting a very small value or even 0, depending on the user's configuration.
+
+### Controller
+
+Setting the strategy type to `PodTermination` will enable a controller, which manages
+the failed nodes.
+
+The controller has to ignore any updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil || pod.DeletionGracePeriodSeconds == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not managed by Kueue.
+    * The `kueue.x-k8s.io/managed` label is set for pods with pod integration enabled.
+    * The presence of the `kueue.x-k8s.io/podset` label could be used in other cases.
```

  ```suggestion
  1. Are not managed by Kueue.
      * That means, have neither of the labels `kueue.x-k8s.io/managed` and `kueue.x-k8s.io/podset` set.
  ```
  
  This would be clearer to me - especially given your convention from the bullets above.
  (Or, if it sounds less clear to you, please at least tell me if I got it right :)

- **keps/6757-failure-recovery/README.md:221** by [@olekzabl](https://github.com/olekzabl) — 2025-10-31T15:46:59Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicies`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+	// +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.  
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Action specifies the action taken to recover from failures.  
+  Action FailureRecoveryAction `json:"action"`
+
+  // PodLabelSelector specifies the scope of resources covered by failure recovery -
+  // resources not matching the selector behave according to the default Kubernetes mechanism.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+	
+  // PodTerminationConfig contains configuration for the `PodTerminationConfig` strategy.
+	// +optional
+  PodTerminationConfig *PodTerminationConfig `json:"podTerminationConfig,omitempty"`
+}
+
+type PodTerminationConfig struct {
+	// PodTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  ForcefulTerminationGracePeriod *time.Duration `json:"forcefulTerminationGracePeriod"`
+}
+
+type FailureRecoveryAction string
+
+const (
+	PodTermination FailureRecoveryAction = "PodTermination"
+)
+```
+
+This API requires opt-in both from the administrators and user's end, as the workloads would
+have to be labeled in order for them to be covered with failure recovery.
+
+An example failure recovery configuration:
+
+```yaml
+failureRecoveryPolicy:
+  rules:
+  - action: PodTermination
+    podLabelSelector:
+      safe-to-fail: true
+    podTerminationConfig:
+      forcefulTerminationGracePeriod: 5m
+```
+
+### Default Grace Period
+
+The proposal is to **not** set a default value for the forceful termination grace period.
+This puts configuration over convention, as it will make the user consciously think about the
+grace period the need for their specific use case, without risking a faulty default.
+
+Since enabling the feature is inherently risky, this serves as an additional explicit
+acknowledgement of this risk from the user's end.
+
+Alternatively, a default value can be inferred from the `terminationGracePeriodSeconds`.
+For example, it could be a multiple of that value. Nevertheless, this still runs the risk of
+setting a very small value or even 0, depending on the user's configuration.
+
+### Controller
+
+Setting the strategy type to `PodTermination` will enable a controller, which manages
+the failed nodes.
+
+The controller has to ignore any updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil || pod.DeletionGracePeriodSeconds == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not managed by Kueue.
+    * The `kueue.x-k8s.io/managed` label is set for pods with pod integration enabled.
+    * The presence of the `kueue.x-k8s.io/podset` label could be used in other cases.
+1. Match the label selector defined in the configured `FailureRecoveryPolicy`.
+
+
+For terminating pods matching the criteria, the controller schedules another reconciliation
+to happen after the remaining grace period elapses:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+	now := r.clock.Now()
+	gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+	totalGracePeriod := gracefulTerminationPeriod + zombiePodTerminationGracePeriod
+	if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+		gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+		return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+	}
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and deleted:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+	pod.Status.Phase = corev1.PodFailed
+	if err := r.client.Status().Update(ctx, pod); err != nil {
```

  Here it's just updated, not deleted.
  Is some automation going to delete it? Which one?
  ([This doc](https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#pod-garbage-collection) suggests that a `Failed` pod will be cleaned up - but only eventually, "when the number of Pods exceeds the configured threshold")
  
  ---
  
  Also [this issue comment](https://github.com/kubernetes-sigs/kueue/issues/6757#issuecomment-3411678911) touches this topic - and seems not replied to - have you seen / considered it?

- **keps/6757-failure-recovery/README.md:290** by [@olekzabl](https://github.com/olekzabl) — 2025-10-31T15:52:07Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicies`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+	// +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.  
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Action specifies the action taken to recover from failures.  
+  Action FailureRecoveryAction `json:"action"`
+
+  // PodLabelSelector specifies the scope of resources covered by failure recovery -
+  // resources not matching the selector behave according to the default Kubernetes mechanism.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+	
+  // PodTerminationConfig contains configuration for the `PodTerminationConfig` strategy.
+	// +optional
+  PodTerminationConfig *PodTerminationConfig `json:"podTerminationConfig,omitempty"`
+}
+
+type PodTerminationConfig struct {
+	// PodTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  ForcefulTerminationGracePeriod *time.Duration `json:"forcefulTerminationGracePeriod"`
+}
+
+type FailureRecoveryAction string
+
+const (
+	PodTermination FailureRecoveryAction = "PodTermination"
+)
+```
+
+This API requires opt-in both from the administrators and user's end, as the workloads would
+have to be labeled in order for them to be covered with failure recovery.
+
+An example failure recovery configuration:
+
+```yaml
+failureRecoveryPolicy:
+  rules:
+  - action: PodTermination
+    podLabelSelector:
+      safe-to-fail: true
+    podTerminationConfig:
+      forcefulTerminationGracePeriod: 5m
+```
+
+### Default Grace Period
+
+The proposal is to **not** set a default value for the forceful termination grace period.
+This puts configuration over convention, as it will make the user consciously think about the
+grace period the need for their specific use case, without risking a faulty default.
+
+Since enabling the feature is inherently risky, this serves as an additional explicit
+acknowledgement of this risk from the user's end.
+
+Alternatively, a default value can be inferred from the `terminationGracePeriodSeconds`.
+For example, it could be a multiple of that value. Nevertheless, this still runs the risk of
+setting a very small value or even 0, depending on the user's configuration.
+
+### Controller
+
+Setting the strategy type to `PodTermination` will enable a controller, which manages
+the failed nodes.
+
+The controller has to ignore any updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil || pod.DeletionGracePeriodSeconds == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not managed by Kueue.
+    * The `kueue.x-k8s.io/managed` label is set for pods with pod integration enabled.
+    * The presence of the `kueue.x-k8s.io/podset` label could be used in other cases.
+1. Match the label selector defined in the configured `FailureRecoveryPolicy`.
+
+
+For terminating pods matching the criteria, the controller schedules another reconciliation
+to happen after the remaining grace period elapses:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+	now := r.clock.Now()
+	gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+	totalGracePeriod := gracefulTerminationPeriod + zombiePodTerminationGracePeriod
+	if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+		gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+		return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+	}
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and deleted:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+	pod.Status.Phase = corev1.PodFailed
+	if err := r.client.Status().Update(ctx, pod); err != nil {
+		return ctrl.Result{}, err
+	}
+
+	return ctrl.Result{}, nil
+}
+```
+
+### Test Plan
+
+[ ] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+The proposal will be covered with unit tests for:
+1. Configuration parsing.
+1. Controller behavior:
+    1. Whether it ignores irrelevant pods (not terminating, not managed by Kueue, already failed/succeeded, not labeled).
+    1. Whether it correctly schedules a reconciliation for when the grace period elapses.
+    1. Whether it updates the pod's phase to `Failed` after the grace period elapses.
+
+#### Integration Tests
+
+The proposal will be covered with integrations tests that check whether:
+1. Adding a deletion timestamp to the pod requeues a reconciliation loop for when the grace period elapses.
+1. After the grace period elapses, the pod is marked as `Failed`.
+1. Replacement pods are scheduled in the place of the failed pod (optional, technically 1+2 is sufficient to prove this).
+
+
+Existing integration tests should prove that this feature does not impact Kueue during normal operation.
+
+### Graduation Criteria
+
+#### Alpha
+
+- Positive feedback from the users.
+
+## Implementation History
+
+2025-09-08: The [issue](https://github.com/kubernetes-sigs/kueue/issues/6757) is raised in Kueue.
+
+2025-09-12: The [issue](https://github.com/kubernetes/kubernetes/issues/134038) is raised in core Kubernetes.
+
+2025-10-17: First draft of the KEP.
+
+## Drawbacks
+
+* The same feature is discussed in core Kubernetes ([kubernetes/issues/134038](https://github.com/kubernetes/kubernetes/issues/134038)), so the underlying issue could potentially be fixed upstream. The timeline of an upstream change is long, but if the feature is deemed not time-critical, it could be fixed at the source instead of in Kueue.
+* This feature introduces a potential footgun to Kueue users and could turn out to be to volatile/risky to use in most affected cases.
+* It introduces yet another responsibility for Kueue - on top of quota management and scheduling, it will also start performing failure recovery.
+
+## Alternatives
+
+### Do Nothing
+
+Since this feature spawned a lengthy discussion about its riskiness and there are other controllers in the ecosystem which deal with
+node problem remediation (e.g. [medik8s](https://github.com/medik8s/self-node-remediation)), an alternative would be to do nothing,
+wait for the upstream conversations to be resolved and propose an alternative solution (external to Kueue) to the affected users.
+
+#### Advantages
+
+1. Requires no implementation effort.
+
+#### Drawbacks
+
+1. Forces users to run another system alongside Kueue, making deploying Kueue more complex those cases.
+
+### Hide The Controller Behind A Feature Gate
```

  ```suggestion
  ### Hide The Controller Only Behind A Feature Gate
  ```
  
  This title puzzled me for a while - as your proposal does contain a feature gate, too.
  I needed to read further to realize that it's about "nothing more than a feature gate" (right?)

- **keps/6757-failure-recovery/README.md:305** by [@olekzabl](https://github.com/olekzabl) — 2025-10-31T15:52:48Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicies`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+	// +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.  
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Action specifies the action taken to recover from failures.  
+  Action FailureRecoveryAction `json:"action"`
+
+  // PodLabelSelector specifies the scope of resources covered by failure recovery -
+  // resources not matching the selector behave according to the default Kubernetes mechanism.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+	
+  // PodTerminationConfig contains configuration for the `PodTerminationConfig` strategy.
+	// +optional
+  PodTerminationConfig *PodTerminationConfig `json:"podTerminationConfig,omitempty"`
+}
+
+type PodTerminationConfig struct {
+	// PodTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  ForcefulTerminationGracePeriod *time.Duration `json:"forcefulTerminationGracePeriod"`
+}
+
+type FailureRecoveryAction string
+
+const (
+	PodTermination FailureRecoveryAction = "PodTermination"
+)
+```
+
+This API requires opt-in both from the administrators and user's end, as the workloads would
+have to be labeled in order for them to be covered with failure recovery.
+
+An example failure recovery configuration:
+
+```yaml
+failureRecoveryPolicy:
+  rules:
+  - action: PodTermination
+    podLabelSelector:
+      safe-to-fail: true
+    podTerminationConfig:
+      forcefulTerminationGracePeriod: 5m
+```
+
+### Default Grace Period
+
+The proposal is to **not** set a default value for the forceful termination grace period.
+This puts configuration over convention, as it will make the user consciously think about the
+grace period the need for their specific use case, without risking a faulty default.
+
+Since enabling the feature is inherently risky, this serves as an additional explicit
+acknowledgement of this risk from the user's end.
+
+Alternatively, a default value can be inferred from the `terminationGracePeriodSeconds`.
+For example, it could be a multiple of that value. Nevertheless, this still runs the risk of
+setting a very small value or even 0, depending on the user's configuration.
+
+### Controller
+
+Setting the strategy type to `PodTermination` will enable a controller, which manages
+the failed nodes.
+
+The controller has to ignore any updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil || pod.DeletionGracePeriodSeconds == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not managed by Kueue.
+    * The `kueue.x-k8s.io/managed` label is set for pods with pod integration enabled.
+    * The presence of the `kueue.x-k8s.io/podset` label could be used in other cases.
+1. Match the label selector defined in the configured `FailureRecoveryPolicy`.
+
+
+For terminating pods matching the criteria, the controller schedules another reconciliation
+to happen after the remaining grace period elapses:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+	now := r.clock.Now()
+	gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+	totalGracePeriod := gracefulTerminationPeriod + zombiePodTerminationGracePeriod
+	if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+		gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+		return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+	}
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and deleted:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+	pod.Status.Phase = corev1.PodFailed
+	if err := r.client.Status().Update(ctx, pod); err != nil {
+		return ctrl.Result{}, err
+	}
+
+	return ctrl.Result{}, nil
+}
+```
+
+### Test Plan
+
+[ ] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+The proposal will be covered with unit tests for:
+1. Configuration parsing.
+1. Controller behavior:
+    1. Whether it ignores irrelevant pods (not terminating, not managed by Kueue, already failed/succeeded, not labeled).
+    1. Whether it correctly schedules a reconciliation for when the grace period elapses.
+    1. Whether it updates the pod's phase to `Failed` after the grace period elapses.
+
+#### Integration Tests
+
+The proposal will be covered with integrations tests that check whether:
+1. Adding a deletion timestamp to the pod requeues a reconciliation loop for when the grace period elapses.
+1. After the grace period elapses, the pod is marked as `Failed`.
+1. Replacement pods are scheduled in the place of the failed pod (optional, technically 1+2 is sufficient to prove this).
+
+
+Existing integration tests should prove that this feature does not impact Kueue during normal operation.
+
+### Graduation Criteria
+
+#### Alpha
+
+- Positive feedback from the users.
+
+## Implementation History
+
+2025-09-08: The [issue](https://github.com/kubernetes-sigs/kueue/issues/6757) is raised in Kueue.
+
+2025-09-12: The [issue](https://github.com/kubernetes/kubernetes/issues/134038) is raised in core Kubernetes.
+
+2025-10-17: First draft of the KEP.
+
+## Drawbacks
+
+* The same feature is discussed in core Kubernetes ([kubernetes/issues/134038](https://github.com/kubernetes/kubernetes/issues/134038)), so the underlying issue could potentially be fixed upstream. The timeline of an upstream change is long, but if the feature is deemed not time-critical, it could be fixed at the source instead of in Kueue.
+* This feature introduces a potential footgun to Kueue users and could turn out to be to volatile/risky to use in most affected cases.
+* It introduces yet another responsibility for Kueue - on top of quota management and scheduling, it will also start performing failure recovery.
+
+## Alternatives
+
+### Do Nothing
+
+Since this feature spawned a lengthy discussion about its riskiness and there are other controllers in the ecosystem which deal with
+node problem remediation (e.g. [medik8s](https://github.com/medik8s/self-node-remediation)), an alternative would be to do nothing,
+wait for the upstream conversations to be resolved and propose an alternative solution (external to Kueue) to the affected users.
+
+#### Advantages
+
+1. Requires no implementation effort.
+
+#### Drawbacks
+
+1. Forces users to run another system alongside Kueue, making deploying Kueue more complex those cases.
+
+### Hide The Controller Behind A Feature Gate
+
+Instead of introducing changes to the API, a feature gate could be used to get some initial feedback about the
+feature and its risks.
+
+#### Advantages
+
+1. Easy to implement, less moving parts.
+1. Allows to gather feedback quickly.
+
+#### Drawbacks
+
+1. It won't allow to implement the pod selectors and configurations for the failure recovery,
+limiting the implementation to defaults:
+  1. `podLabelSelector` - everything.
+  1. `forcefulTerminationGracePeriod` - a constant or a multiple of `terminationGracePeriodSeconds`.
```

  This misrenders, please fix indents.

- **keps/6757-failure-recovery/README.md:305** by [@olekzabl](https://github.com/olekzabl) — 2025-10-31T15:54:04Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicies`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+	// +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.  
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Action specifies the action taken to recover from failures.  
+  Action FailureRecoveryAction `json:"action"`
+
+  // PodLabelSelector specifies the scope of resources covered by failure recovery -
+  // resources not matching the selector behave according to the default Kubernetes mechanism.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+	
+  // PodTerminationConfig contains configuration for the `PodTerminationConfig` strategy.
+	// +optional
+  PodTerminationConfig *PodTerminationConfig `json:"podTerminationConfig,omitempty"`
+}
+
+type PodTerminationConfig struct {
+	// PodTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  ForcefulTerminationGracePeriod *time.Duration `json:"forcefulTerminationGracePeriod"`
+}
+
+type FailureRecoveryAction string
+
+const (
+	PodTermination FailureRecoveryAction = "PodTermination"
+)
+```
+
+This API requires opt-in both from the administrators and user's end, as the workloads would
+have to be labeled in order for them to be covered with failure recovery.
+
+An example failure recovery configuration:
+
+```yaml
+failureRecoveryPolicy:
+  rules:
+  - action: PodTermination
+    podLabelSelector:
+      safe-to-fail: true
+    podTerminationConfig:
+      forcefulTerminationGracePeriod: 5m
+```
+
+### Default Grace Period
+
+The proposal is to **not** set a default value for the forceful termination grace period.
+This puts configuration over convention, as it will make the user consciously think about the
+grace period the need for their specific use case, without risking a faulty default.
+
+Since enabling the feature is inherently risky, this serves as an additional explicit
+acknowledgement of this risk from the user's end.
+
+Alternatively, a default value can be inferred from the `terminationGracePeriodSeconds`.
+For example, it could be a multiple of that value. Nevertheless, this still runs the risk of
+setting a very small value or even 0, depending on the user's configuration.
+
+### Controller
+
+Setting the strategy type to `PodTermination` will enable a controller, which manages
+the failed nodes.
+
+The controller has to ignore any updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil || pod.DeletionGracePeriodSeconds == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not managed by Kueue.
+    * The `kueue.x-k8s.io/managed` label is set for pods with pod integration enabled.
+    * The presence of the `kueue.x-k8s.io/podset` label could be used in other cases.
+1. Match the label selector defined in the configured `FailureRecoveryPolicy`.
+
+
+For terminating pods matching the criteria, the controller schedules another reconciliation
+to happen after the remaining grace period elapses:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+	now := r.clock.Now()
+	gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+	totalGracePeriod := gracefulTerminationPeriod + zombiePodTerminationGracePeriod
+	if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+		gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+		return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+	}
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and deleted:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+	pod.Status.Phase = corev1.PodFailed
+	if err := r.client.Status().Update(ctx, pod); err != nil {
+		return ctrl.Result{}, err
+	}
+
+	return ctrl.Result{}, nil
+}
+```
+
+### Test Plan
+
+[ ] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+The proposal will be covered with unit tests for:
+1. Configuration parsing.
+1. Controller behavior:
+    1. Whether it ignores irrelevant pods (not terminating, not managed by Kueue, already failed/succeeded, not labeled).
+    1. Whether it correctly schedules a reconciliation for when the grace period elapses.
+    1. Whether it updates the pod's phase to `Failed` after the grace period elapses.
+
+#### Integration Tests
+
+The proposal will be covered with integrations tests that check whether:
+1. Adding a deletion timestamp to the pod requeues a reconciliation loop for when the grace period elapses.
+1. After the grace period elapses, the pod is marked as `Failed`.
+1. Replacement pods are scheduled in the place of the failed pod (optional, technically 1+2 is sufficient to prove this).
+
+
+Existing integration tests should prove that this feature does not impact Kueue during normal operation.
+
+### Graduation Criteria
+
+#### Alpha
+
+- Positive feedback from the users.
+
+## Implementation History
+
+2025-09-08: The [issue](https://github.com/kubernetes-sigs/kueue/issues/6757) is raised in Kueue.
+
+2025-09-12: The [issue](https://github.com/kubernetes/kubernetes/issues/134038) is raised in core Kubernetes.
+
+2025-10-17: First draft of the KEP.
+
+## Drawbacks
+
+* The same feature is discussed in core Kubernetes ([kubernetes/issues/134038](https://github.com/kubernetes/kubernetes/issues/134038)), so the underlying issue could potentially be fixed upstream. The timeline of an upstream change is long, but if the feature is deemed not time-critical, it could be fixed at the source instead of in Kueue.
+* This feature introduces a potential footgun to Kueue users and could turn out to be to volatile/risky to use in most affected cases.
+* It introduces yet another responsibility for Kueue - on top of quota management and scheduling, it will also start performing failure recovery.
+
+## Alternatives
+
+### Do Nothing
+
+Since this feature spawned a lengthy discussion about its riskiness and there are other controllers in the ecosystem which deal with
+node problem remediation (e.g. [medik8s](https://github.com/medik8s/self-node-remediation)), an alternative would be to do nothing,
+wait for the upstream conversations to be resolved and propose an alternative solution (external to Kueue) to the affected users.
+
+#### Advantages
+
+1. Requires no implementation effort.
+
+#### Drawbacks
+
+1. Forces users to run another system alongside Kueue, making deploying Kueue more complex those cases.
+
+### Hide The Controller Behind A Feature Gate
+
+Instead of introducing changes to the API, a feature gate could be used to get some initial feedback about the
+feature and its risks.
+
+#### Advantages
+
+1. Easy to implement, less moving parts.
+1. Allows to gather feedback quickly.
+
+#### Drawbacks
+
+1. It won't allow to implement the pod selectors and configurations for the failure recovery,
+limiting the implementation to defaults:
+  1. `podLabelSelector` - everything.
+  1. `forcefulTerminationGracePeriod` - a constant or a multiple of `terminationGracePeriodSeconds`.
```

  How would you achieve multiple?

- **keps/6757-failure-recovery/README.md:49** by [@olekzabl](https://github.com/olekzabl) — 2025-10-31T16:24:41Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
```

  I'm reading this together with [PodGC docs](https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#pod-garbage-collection) and I'm puzzled.
  
  You're saying "pods are evicted from the node" - I'm not sure what it means, but for me it suggests that their assignment to that node is removed - but then such pods should be cleaned up by [PodGC, criterion # 2](https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#pod-garbage-collection).
  While I guess they aren't, because if they were, #6757 wouldn't have been created.
  
  So I'm guessing that these pods remain assigned to the failed node, and they fall short of being caught by PodGC's criterion # 3 due to a mismatch of taint names (`not-ready` != `out-of-service`). Is that right?
  
  If I'm correct, please consider rewording "evicted from the node". Or explaining what it means.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-10-31T16:37:25Z

- **keps/6757-failure-recovery/README.md:49** by [@olekzabl](https://github.com/olekzabl) — 2025-10-31T16:37:18Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
```

  Oh, and follow-up question: 
  
  *If* the affected pods remain assigned to the failed node, which has the `not-ready` taint - *then* why not make this a part of the rule for when your proposed controller acts?
  (Given that your intent seems to be rather to restrict its scope than to enlarge it)

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-10-31T17:03:21Z

- **keps/6757-failure-recovery/README.md:221** by [@olekzabl](https://github.com/olekzabl) — 2025-10-31T17:03:21Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicies`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+	// +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.  
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Action specifies the action taken to recover from failures.  
+  Action FailureRecoveryAction `json:"action"`
+
+  // PodLabelSelector specifies the scope of resources covered by failure recovery -
+  // resources not matching the selector behave according to the default Kubernetes mechanism.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+	
+  // PodTerminationConfig contains configuration for the `PodTerminationConfig` strategy.
+	// +optional
+  PodTerminationConfig *PodTerminationConfig `json:"podTerminationConfig,omitempty"`
+}
+
+type PodTerminationConfig struct {
+	// PodTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  ForcefulTerminationGracePeriod *time.Duration `json:"forcefulTerminationGracePeriod"`
+}
+
+type FailureRecoveryAction string
+
+const (
+	PodTermination FailureRecoveryAction = "PodTermination"
+)
+```
+
+This API requires opt-in both from the administrators and user's end, as the workloads would
+have to be labeled in order for them to be covered with failure recovery.
+
+An example failure recovery configuration:
+
+```yaml
+failureRecoveryPolicy:
+  rules:
+  - action: PodTermination
+    podLabelSelector:
+      safe-to-fail: true
+    podTerminationConfig:
+      forcefulTerminationGracePeriod: 5m
+```
+
+### Default Grace Period
+
+The proposal is to **not** set a default value for the forceful termination grace period.
+This puts configuration over convention, as it will make the user consciously think about the
+grace period the need for their specific use case, without risking a faulty default.
+
+Since enabling the feature is inherently risky, this serves as an additional explicit
+acknowledgement of this risk from the user's end.
+
+Alternatively, a default value can be inferred from the `terminationGracePeriodSeconds`.
+For example, it could be a multiple of that value. Nevertheless, this still runs the risk of
+setting a very small value or even 0, depending on the user's configuration.
+
+### Controller
+
+Setting the strategy type to `PodTermination` will enable a controller, which manages
+the failed nodes.
+
+The controller has to ignore any updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil || pod.DeletionGracePeriodSeconds == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not managed by Kueue.
+    * The `kueue.x-k8s.io/managed` label is set for pods with pod integration enabled.
+    * The presence of the `kueue.x-k8s.io/podset` label could be used in other cases.
+1. Match the label selector defined in the configured `FailureRecoveryPolicy`.
+
+
+For terminating pods matching the criteria, the controller schedules another reconciliation
+to happen after the remaining grace period elapses:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+	now := r.clock.Now()
+	gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+	totalGracePeriod := gracefulTerminationPeriod + zombiePodTerminationGracePeriod
+	if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+		gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+		return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+	}
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and deleted:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+	pod.Status.Phase = corev1.PodFailed
+	if err := r.client.Status().Update(ctx, pod); err != nil {
```

  > Also https://github.com/kubernetes-sigs/kueue/issues/6757#issuecomment-3411678911 touches this topic - and seems not replied to - have you seen / considered it?
  
  Ah I realized that the subsequent comments are related to that one.
  
  So, in the end, the outcome of your code snippet is that the pods becomes `Failed` and its Kueue quota is released - but it's _not_ necessarily deleted, right?
  
  (Perhaps I should clarify - I'm complaining about the word "deleted" a few lines above, [here](https://github.com/kubernetes-sigs/kueue/blob/282d3e4170a15fb53e8ef7417bd7ce2c49f78831/keps/6757-failure-recovery/README.md?plain=1#L214))

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-10-31T18:20:24Z

- **keps/6757-failure-recovery/README.md:132** by [@olekzabl](https://github.com/olekzabl) — 2025-10-31T18:20:23Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicies`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+	// +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.  
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Action specifies the action taken to recover from failures.  
+  Action FailureRecoveryAction `json:"action"`
+
+  // PodLabelSelector specifies the scope of resources covered by failure recovery -
+  // resources not matching the selector behave according to the default Kubernetes mechanism.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+	
+  // PodTerminationConfig contains configuration for the `PodTerminationConfig` strategy.
```

  (a [related section](https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md#unions) of API conventions doc)

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-03T09:25:09Z

- **keps/6757-failure-recovery/README.md:190** by [@kshalot](https://github.com/kshalot) — 2025-11-03T09:25:09Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicies`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+	// +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.  
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Action specifies the action taken to recover from failures.  
+  Action FailureRecoveryAction `json:"action"`
+
+  // PodLabelSelector specifies the scope of resources covered by failure recovery -
+  // resources not matching the selector behave according to the default Kubernetes mechanism.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+	
+  // PodTerminationConfig contains configuration for the `PodTerminationConfig` strategy.
+	// +optional
+  PodTerminationConfig *PodTerminationConfig `json:"podTerminationConfig,omitempty"`
+}
+
+type PodTerminationConfig struct {
+	// PodTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  ForcefulTerminationGracePeriod *time.Duration `json:"forcefulTerminationGracePeriod"`
+}
+
+type FailureRecoveryAction string
+
+const (
+	PodTermination FailureRecoveryAction = "PodTermination"
+)
+```
+
+This API requires opt-in both from the administrators and user's end, as the workloads would
+have to be labeled in order for them to be covered with failure recovery.
+
+An example failure recovery configuration:
+
+```yaml
+failureRecoveryPolicy:
+  rules:
+  - action: PodTermination
+    podLabelSelector:
+      safe-to-fail: true
+    podTerminationConfig:
+      forcefulTerminationGracePeriod: 5m
+```
+
+### Default Grace Period
+
+The proposal is to **not** set a default value for the forceful termination grace period.
+This puts configuration over convention, as it will make the user consciously think about the
+grace period the need for their specific use case, without risking a faulty default.
+
+Since enabling the feature is inherently risky, this serves as an additional explicit
+acknowledgement of this risk from the user's end.
+
+Alternatively, a default value can be inferred from the `terminationGracePeriodSeconds`.
+For example, it could be a multiple of that value. Nevertheless, this still runs the risk of
+setting a very small value or even 0, depending on the user's configuration.
+
+### Controller
+
+Setting the strategy type to `PodTermination` will enable a controller, which manages
+the failed nodes.
+
+The controller has to ignore any updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil || pod.DeletionGracePeriodSeconds == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not managed by Kueue.
+    * The `kueue.x-k8s.io/managed` label is set for pods with pod integration enabled.
+    * The presence of the `kueue.x-k8s.io/podset` label could be used in other cases.
```

  This is a good suggestion IMO. I reworded it to be closer to what you proposed.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-03T09:28:19Z

- **keps/6757-failure-recovery/README.md:146** by [@kshalot](https://github.com/kshalot) — 2025-11-03T09:28:18Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicies`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+	// +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.  
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Action specifies the action taken to recover from failures.  
+  Action FailureRecoveryAction `json:"action"`
+
+  // PodLabelSelector specifies the scope of resources covered by failure recovery -
+  // resources not matching the selector behave according to the default Kubernetes mechanism.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+	
+  // PodTerminationConfig contains configuration for the `PodTerminationConfig` strategy.
+	// +optional
+  PodTerminationConfig *PodTerminationConfig `json:"podTerminationConfig,omitempty"`
+}
+
+type PodTerminationConfig struct {
+	// PodTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  ForcefulTerminationGracePeriod *time.Duration `json:"forcefulTerminationGracePeriod"`
+}
+
+type FailureRecoveryAction string
+
+const (
+	PodTermination FailureRecoveryAction = "PodTermination"
```

  Good point

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-03T09:32:08Z

- **keps/6757-failure-recovery/README.md:305** by [@kshalot](https://github.com/kshalot) — 2025-11-03T09:32:07Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicies`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+	// +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.  
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Action specifies the action taken to recover from failures.  
+  Action FailureRecoveryAction `json:"action"`
+
+  // PodLabelSelector specifies the scope of resources covered by failure recovery -
+  // resources not matching the selector behave according to the default Kubernetes mechanism.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+	
+  // PodTerminationConfig contains configuration for the `PodTerminationConfig` strategy.
+	// +optional
+  PodTerminationConfig *PodTerminationConfig `json:"podTerminationConfig,omitempty"`
+}
+
+type PodTerminationConfig struct {
+	// PodTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  ForcefulTerminationGracePeriod *time.Duration `json:"forcefulTerminationGracePeriod"`
+}
+
+type FailureRecoveryAction string
+
+const (
+	PodTermination FailureRecoveryAction = "PodTermination"
+)
+```
+
+This API requires opt-in both from the administrators and user's end, as the workloads would
+have to be labeled in order for them to be covered with failure recovery.
+
+An example failure recovery configuration:
+
+```yaml
+failureRecoveryPolicy:
+  rules:
+  - action: PodTermination
+    podLabelSelector:
+      safe-to-fail: true
+    podTerminationConfig:
+      forcefulTerminationGracePeriod: 5m
+```
+
+### Default Grace Period
+
+The proposal is to **not** set a default value for the forceful termination grace period.
+This puts configuration over convention, as it will make the user consciously think about the
+grace period the need for their specific use case, without risking a faulty default.
+
+Since enabling the feature is inherently risky, this serves as an additional explicit
+acknowledgement of this risk from the user's end.
+
+Alternatively, a default value can be inferred from the `terminationGracePeriodSeconds`.
+For example, it could be a multiple of that value. Nevertheless, this still runs the risk of
+setting a very small value or even 0, depending on the user's configuration.
+
+### Controller
+
+Setting the strategy type to `PodTermination` will enable a controller, which manages
+the failed nodes.
+
+The controller has to ignore any updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil || pod.DeletionGracePeriodSeconds == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not managed by Kueue.
+    * The `kueue.x-k8s.io/managed` label is set for pods with pod integration enabled.
+    * The presence of the `kueue.x-k8s.io/podset` label could be used in other cases.
+1. Match the label selector defined in the configured `FailureRecoveryPolicy`.
+
+
+For terminating pods matching the criteria, the controller schedules another reconciliation
+to happen after the remaining grace period elapses:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+	now := r.clock.Now()
+	gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+	totalGracePeriod := gracefulTerminationPeriod + zombiePodTerminationGracePeriod
+	if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+		gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+		return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+	}
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and deleted:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+	pod.Status.Phase = corev1.PodFailed
+	if err := r.client.Status().Update(ctx, pod); err != nil {
+		return ctrl.Result{}, err
+	}
+
+	return ctrl.Result{}, nil
+}
+```
+
+### Test Plan
+
+[ ] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+The proposal will be covered with unit tests for:
+1. Configuration parsing.
+1. Controller behavior:
+    1. Whether it ignores irrelevant pods (not terminating, not managed by Kueue, already failed/succeeded, not labeled).
+    1. Whether it correctly schedules a reconciliation for when the grace period elapses.
+    1. Whether it updates the pod's phase to `Failed` after the grace period elapses.
+
+#### Integration Tests
+
+The proposal will be covered with integrations tests that check whether:
+1. Adding a deletion timestamp to the pod requeues a reconciliation loop for when the grace period elapses.
+1. After the grace period elapses, the pod is marked as `Failed`.
+1. Replacement pods are scheduled in the place of the failed pod (optional, technically 1+2 is sufficient to prove this).
+
+
+Existing integration tests should prove that this feature does not impact Kueue during normal operation.
+
+### Graduation Criteria
+
+#### Alpha
+
+- Positive feedback from the users.
+
+## Implementation History
+
+2025-09-08: The [issue](https://github.com/kubernetes-sigs/kueue/issues/6757) is raised in Kueue.
+
+2025-09-12: The [issue](https://github.com/kubernetes/kubernetes/issues/134038) is raised in core Kubernetes.
+
+2025-10-17: First draft of the KEP.
+
+## Drawbacks
+
+* The same feature is discussed in core Kubernetes ([kubernetes/issues/134038](https://github.com/kubernetes/kubernetes/issues/134038)), so the underlying issue could potentially be fixed upstream. The timeline of an upstream change is long, but if the feature is deemed not time-critical, it could be fixed at the source instead of in Kueue.
+* This feature introduces a potential footgun to Kueue users and could turn out to be to volatile/risky to use in most affected cases.
+* It introduces yet another responsibility for Kueue - on top of quota management and scheduling, it will also start performing failure recovery.
+
+## Alternatives
+
+### Do Nothing
+
+Since this feature spawned a lengthy discussion about its riskiness and there are other controllers in the ecosystem which deal with
+node problem remediation (e.g. [medik8s](https://github.com/medik8s/self-node-remediation)), an alternative would be to do nothing,
+wait for the upstream conversations to be resolved and propose an alternative solution (external to Kueue) to the affected users.
+
+#### Advantages
+
+1. Requires no implementation effort.
+
+#### Drawbacks
+
+1. Forces users to run another system alongside Kueue, making deploying Kueue more complex those cases.
+
+### Hide The Controller Behind A Feature Gate
+
+Instead of introducing changes to the API, a feature gate could be used to get some initial feedback about the
+feature and its risks.
+
+#### Advantages
+
+1. Easy to implement, less moving parts.
+1. Allows to gather feedback quickly.
+
+#### Drawbacks
+
+1. It won't allow to implement the pod selectors and configurations for the failure recovery,
+limiting the implementation to defaults:
+  1. `podLabelSelector` - everything.
+  1. `forcefulTerminationGracePeriod` - a constant or a multiple of `terminationGracePeriodSeconds`.
```

  It would be `N * terminationGracePeriodSeconds`, where N is a constant. It's one of the ideas discussed in https://github.com/kubernetes/kubernetes/issues/134038, but I did not expand on it here.
  
  Do you think it warrants more explanation?

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-11-03T09:57:01Z

- **keps/6757-failure-recovery/README.md:305** by [@olekzabl](https://github.com/olekzabl) — 2025-11-03T09:57:01Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicies`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+	// +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.  
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Action specifies the action taken to recover from failures.  
+  Action FailureRecoveryAction `json:"action"`
+
+  // PodLabelSelector specifies the scope of resources covered by failure recovery -
+  // resources not matching the selector behave according to the default Kubernetes mechanism.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+	
+  // PodTerminationConfig contains configuration for the `PodTerminationConfig` strategy.
+	// +optional
+  PodTerminationConfig *PodTerminationConfig `json:"podTerminationConfig,omitempty"`
+}
+
+type PodTerminationConfig struct {
+	// PodTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  ForcefulTerminationGracePeriod *time.Duration `json:"forcefulTerminationGracePeriod"`
+}
+
+type FailureRecoveryAction string
+
+const (
+	PodTermination FailureRecoveryAction = "PodTermination"
+)
+```
+
+This API requires opt-in both from the administrators and user's end, as the workloads would
+have to be labeled in order for them to be covered with failure recovery.
+
+An example failure recovery configuration:
+
+```yaml
+failureRecoveryPolicy:
+  rules:
+  - action: PodTermination
+    podLabelSelector:
+      safe-to-fail: true
+    podTerminationConfig:
+      forcefulTerminationGracePeriod: 5m
+```
+
+### Default Grace Period
+
+The proposal is to **not** set a default value for the forceful termination grace period.
+This puts configuration over convention, as it will make the user consciously think about the
+grace period the need for their specific use case, without risking a faulty default.
+
+Since enabling the feature is inherently risky, this serves as an additional explicit
+acknowledgement of this risk from the user's end.
+
+Alternatively, a default value can be inferred from the `terminationGracePeriodSeconds`.
+For example, it could be a multiple of that value. Nevertheless, this still runs the risk of
+setting a very small value or even 0, depending on the user's configuration.
+
+### Controller
+
+Setting the strategy type to `PodTermination` will enable a controller, which manages
+the failed nodes.
+
+The controller has to ignore any updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil || pod.DeletionGracePeriodSeconds == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not managed by Kueue.
+    * The `kueue.x-k8s.io/managed` label is set for pods with pod integration enabled.
+    * The presence of the `kueue.x-k8s.io/podset` label could be used in other cases.
+1. Match the label selector defined in the configured `FailureRecoveryPolicy`.
+
+
+For terminating pods matching the criteria, the controller schedules another reconciliation
+to happen after the remaining grace period elapses:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+	now := r.clock.Now()
+	gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+	totalGracePeriod := gracefulTerminationPeriod + zombiePodTerminationGracePeriod
+	if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+		gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+		return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+	}
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and deleted:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+	pod.Status.Phase = corev1.PodFailed
+	if err := r.client.Status().Update(ctx, pod); err != nil {
+		return ctrl.Result{}, err
+	}
+
+	return ctrl.Result{}, nil
+}
+```
+
+### Test Plan
+
+[ ] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+The proposal will be covered with unit tests for:
+1. Configuration parsing.
+1. Controller behavior:
+    1. Whether it ignores irrelevant pods (not terminating, not managed by Kueue, already failed/succeeded, not labeled).
+    1. Whether it correctly schedules a reconciliation for when the grace period elapses.
+    1. Whether it updates the pod's phase to `Failed` after the grace period elapses.
+
+#### Integration Tests
+
+The proposal will be covered with integrations tests that check whether:
+1. Adding a deletion timestamp to the pod requeues a reconciliation loop for when the grace period elapses.
+1. After the grace period elapses, the pod is marked as `Failed`.
+1. Replacement pods are scheduled in the place of the failed pod (optional, technically 1+2 is sufficient to prove this).
+
+
+Existing integration tests should prove that this feature does not impact Kueue during normal operation.
+
+### Graduation Criteria
+
+#### Alpha
+
+- Positive feedback from the users.
+
+## Implementation History
+
+2025-09-08: The [issue](https://github.com/kubernetes-sigs/kueue/issues/6757) is raised in Kueue.
+
+2025-09-12: The [issue](https://github.com/kubernetes/kubernetes/issues/134038) is raised in core Kubernetes.
+
+2025-10-17: First draft of the KEP.
+
+## Drawbacks
+
+* The same feature is discussed in core Kubernetes ([kubernetes/issues/134038](https://github.com/kubernetes/kubernetes/issues/134038)), so the underlying issue could potentially be fixed upstream. The timeline of an upstream change is long, but if the feature is deemed not time-critical, it could be fixed at the source instead of in Kueue.
+* This feature introduces a potential footgun to Kueue users and could turn out to be to volatile/risky to use in most affected cases.
+* It introduces yet another responsibility for Kueue - on top of quota management and scheduling, it will also start performing failure recovery.
+
+## Alternatives
+
+### Do Nothing
+
+Since this feature spawned a lengthy discussion about its riskiness and there are other controllers in the ecosystem which deal with
+node problem remediation (e.g. [medik8s](https://github.com/medik8s/self-node-remediation)), an alternative would be to do nothing,
+wait for the upstream conversations to be resolved and propose an alternative solution (external to Kueue) to the affected users.
+
+#### Advantages
+
+1. Requires no implementation effort.
+
+#### Drawbacks
+
+1. Forces users to run another system alongside Kueue, making deploying Kueue more complex those cases.
+
+### Hide The Controller Behind A Feature Gate
+
+Instead of introducing changes to the API, a feature gate could be used to get some initial feedback about the
+feature and its risks.
+
+#### Advantages
+
+1. Easy to implement, less moving parts.
+1. Allows to gather feedback quickly.
+
+#### Drawbacks
+
+1. It won't allow to implement the pod selectors and configurations for the failure recovery,
+limiting the implementation to defaults:
+  1. `podLabelSelector` - everything.
+  1. `forcefulTerminationGracePeriod` - a constant or a multiple of `terminationGracePeriodSeconds`.
```

  Oh, never mind, I imagined some non-existing problem.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-03T10:15:15Z

- **keps/6757-failure-recovery/README.md:49** by [@kshalot](https://github.com/kshalot) — 2025-11-03T10:15:15Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
```

  Good questions. Because you pointed this out, I noticed that the `unreachable` taint should be mentioned in the KEP, not `not-ready`.
  
  The node assignment is not removed when a pod is being deleted, so the pods are still "scheduled" i.e. do not fall under criterion # 2. You got it right - the `unreachable` taint is different than the `out-of-service` taint, so criterion # 3 does not apply.
  
  As for your follow-up - yes, I think we'd like to do that. Initially I wanted to base it off solely off pods, but now I noticed another benefit (apart from restricting scope) - it will explicitly ignore pods assigned to a node that is `not-ready` (for example due to pressure on some resource) and terminating for a long time because of that. If the node is `not-ready`, the kubelet is still running and communicating with the control plane.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-03T10:19:53Z

- **keps/6757-failure-recovery/README.md:203** by [@kshalot](https://github.com/kshalot) — 2025-11-03T10:19:53Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicies`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+	// +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.  
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Action specifies the action taken to recover from failures.  
+  Action FailureRecoveryAction `json:"action"`
+
+  // PodLabelSelector specifies the scope of resources covered by failure recovery -
+  // resources not matching the selector behave according to the default Kubernetes mechanism.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+	
+  // PodTerminationConfig contains configuration for the `PodTerminationConfig` strategy.
+	// +optional
+  PodTerminationConfig *PodTerminationConfig `json:"podTerminationConfig,omitempty"`
+}
+
+type PodTerminationConfig struct {
+	// PodTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  ForcefulTerminationGracePeriod *time.Duration `json:"forcefulTerminationGracePeriod"`
+}
+
+type FailureRecoveryAction string
+
+const (
+	PodTermination FailureRecoveryAction = "PodTermination"
+)
+```
+
+This API requires opt-in both from the administrators and user's end, as the workloads would
+have to be labeled in order for them to be covered with failure recovery.
+
+An example failure recovery configuration:
+
+```yaml
+failureRecoveryPolicy:
+  rules:
+  - action: PodTermination
+    podLabelSelector:
+      safe-to-fail: true
+    podTerminationConfig:
+      forcefulTerminationGracePeriod: 5m
+```
+
+### Default Grace Period
+
+The proposal is to **not** set a default value for the forceful termination grace period.
+This puts configuration over convention, as it will make the user consciously think about the
+grace period the need for their specific use case, without risking a faulty default.
+
+Since enabling the feature is inherently risky, this serves as an additional explicit
+acknowledgement of this risk from the user's end.
+
+Alternatively, a default value can be inferred from the `terminationGracePeriodSeconds`.
+For example, it could be a multiple of that value. Nevertheless, this still runs the risk of
+setting a very small value or even 0, depending on the user's configuration.
+
+### Controller
+
+Setting the strategy type to `PodTermination` will enable a controller, which manages
+the failed nodes.
+
+The controller has to ignore any updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil || pod.DeletionGracePeriodSeconds == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not managed by Kueue.
+    * The `kueue.x-k8s.io/managed` label is set for pods with pod integration enabled.
+    * The presence of the `kueue.x-k8s.io/podset` label could be used in other cases.
+1. Match the label selector defined in the configured `FailureRecoveryPolicy`.
+
+
+For terminating pods matching the criteria, the controller schedules another reconciliation
+to happen after the remaining grace period elapses:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+	now := r.clock.Now()
+	gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+	totalGracePeriod := gracefulTerminationPeriod + zombiePodTerminationGracePeriod
```

  Yes. Changed.

### Review by [@olekzabl](https://github.com/olekzabl) (approved) — 2025-11-03T10:39:55Z

- **keps/6757-failure-recovery/README.md:200** by [@olekzabl](https://github.com/olekzabl) — 2025-11-03T10:39:12Z
```diff
@@ -0,0 +1,332 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+	// +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.  
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Action specifies the action taken to recover from failures.  
+  Action FailureRecoveryAction `json:"action"`
+
+  // PodLabelSelector specifies the scope of resources covered by failure recovery -
+  // resources not matching the selector behave according to the default Kubernetes mechanism.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+	
+  // TerminatePodConfig contains configuration for the `TerminatePodConfig` strategy.
+	// +optional
+  TerminatePodConfig *TerminatePodConfig `json:"terminatePodConfig,omitempty"`
+}
+
+type TerminatePodConfig struct {
+	// ForcefulTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  ForcefulTerminationGracePeriod *time.Duration `json:"forcefulTerminationGracePeriod"`
+}
+
+type FailureRecoveryAction string
+
+const (
+	TerminatePod FailureRecoveryAction = "TerminatePod"
+)
+```
+
+This API requires opt-in both from the administrators and user's end, as the workloads would
+have to be labeled in order for them to be covered with failure recovery.
+
+An example failure recovery configuration:
+
+```yaml
+failureRecoveryPolicy:
+  rules:
+  - action: TerminatePod
+    podLabelSelector:
+      safe-to-fail: true
+    terminatePodConfig:
+      forcefulTerminationGracePeriod: 5m
+```
+
+### Default Grace Period
+
+The proposal is to **not** set a default value for the forceful termination grace period.
+This puts configuration over convention, as it will make the user consciously think about the
+grace period the need for their specific use case, without risking a faulty default.
+
+Since enabling the feature is inherently risky, this serves as an additional explicit
+acknowledgement of this risk from the user's end.
+
+Alternatively, a default value can be inferred from the `terminationGracePeriodSeconds`.
+For example, it could be a multiple of that value. Nevertheless, this still runs the risk of
+setting a very small value or even 0, depending on the user's configuration.
+
+### Controller
+
+Setting the strategy type to `TerminatePod` will enable a controller, which manages
+the failed nodes.
+
+The controller has to **ignore** updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil || pod.DeletionGracePeriodSeconds == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not managed by Kueue.
+    * Neither the `kueue.x-k8s.io/managed` (for pod integration) nor `kueue.x-k8s.io/podset` label is set.
+1. Don't match the label selector defined in the configured `FailureRecoveryPolicy`.
+1. Are **not** scheduled on a node tainted with `node.kubernetes.io/unreachable`.
+    * This explicitly ignores pods assigned to nodes that still have a running kubelet.
+    For example, nodes with the `node.kubernetes.io/not-ready` taint experiencing resource pressure
+    that makes pod termination take longer.
+
+
+For relevant (not ignored) terminating pods, the controller schedules another reconciliation
+to happen after the remaining grace period elapses:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+	now := r.clock.Now()
+	gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+  forcefulTerminationGracePeriod := time.Duration(*terminatePodConfig.ForcefulTerminationGracePeriod) * time.Second
```

  `*tPC.FTGP` already is a `time.Duration`.
  
  Also, nit: fix indent.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-03T10:42:05Z

- **keps/6757-failure-recovery/README.md:221** by [@kshalot](https://github.com/kshalot) — 2025-11-03T10:42:05Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicies`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+	// +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.  
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Action specifies the action taken to recover from failures.  
+  Action FailureRecoveryAction `json:"action"`
+
+  // PodLabelSelector specifies the scope of resources covered by failure recovery -
+  // resources not matching the selector behave according to the default Kubernetes mechanism.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+	
+  // PodTerminationConfig contains configuration for the `PodTerminationConfig` strategy.
+	// +optional
+  PodTerminationConfig *PodTerminationConfig `json:"podTerminationConfig,omitempty"`
+}
+
+type PodTerminationConfig struct {
+	// PodTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  ForcefulTerminationGracePeriod *time.Duration `json:"forcefulTerminationGracePeriod"`
+}
+
+type FailureRecoveryAction string
+
+const (
+	PodTermination FailureRecoveryAction = "PodTermination"
+)
+```
+
+This API requires opt-in both from the administrators and user's end, as the workloads would
+have to be labeled in order for them to be covered with failure recovery.
+
+An example failure recovery configuration:
+
+```yaml
+failureRecoveryPolicy:
+  rules:
+  - action: PodTermination
+    podLabelSelector:
+      safe-to-fail: true
+    podTerminationConfig:
+      forcefulTerminationGracePeriod: 5m
+```
+
+### Default Grace Period
+
+The proposal is to **not** set a default value for the forceful termination grace period.
+This puts configuration over convention, as it will make the user consciously think about the
+grace period the need for their specific use case, without risking a faulty default.
+
+Since enabling the feature is inherently risky, this serves as an additional explicit
+acknowledgement of this risk from the user's end.
+
+Alternatively, a default value can be inferred from the `terminationGracePeriodSeconds`.
+For example, it could be a multiple of that value. Nevertheless, this still runs the risk of
+setting a very small value or even 0, depending on the user's configuration.
+
+### Controller
+
+Setting the strategy type to `PodTermination` will enable a controller, which manages
+the failed nodes.
+
+The controller has to ignore any updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil || pod.DeletionGracePeriodSeconds == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not managed by Kueue.
+    * The `kueue.x-k8s.io/managed` label is set for pods with pod integration enabled.
+    * The presence of the `kueue.x-k8s.io/podset` label could be used in other cases.
+1. Match the label selector defined in the configured `FailureRecoveryPolicy`.
+
+
+For terminating pods matching the criteria, the controller schedules another reconciliation
+to happen after the remaining grace period elapses:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+	now := r.clock.Now()
+	gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+	totalGracePeriod := gracefulTerminationPeriod + zombiePodTerminationGracePeriod
+	if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+		gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+		return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+	}
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and deleted:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+	pod.Status.Phase = corev1.PodFailed
+	if err := r.client.Status().Update(ctx, pod); err != nil {
```

  I reworded "deleted", that was indeed incorrect.
  
  As for the second part of you comment - changing the status phase to `PodFailed`/`PodSucceeded` does not remove the pod object. It will remain in `etcd` until the number of pods with a final phase exceeds the threshold.
  
  I mentioned this in https://github.com/kubernetes-sigs/kueue/issues/6757#issuecomment-3411513222, where we opted for option (2) - mark the pods as failed, but do not remove them from `etcd`.
  
  My idea for the future would be to make this behavior configurable, for example by adding `deletePodObject: true` to the `TerminatePodConfig` API (I don't think this behavior warrants a separate `TerminateAndDeletePod` action since it's a superset of `TerminatePod`). It would be a backwards compatible change, so we don't have to add it immediately though (cc @mimowo).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-03T13:34:38Z

- **keps/6757-failure-recovery/README.md:221** by [@mimowo](https://github.com/mimowo) — 2025-11-03T13:34:38Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicies`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+	// +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.  
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Action specifies the action taken to recover from failures.  
+  Action FailureRecoveryAction `json:"action"`
+
+  // PodLabelSelector specifies the scope of resources covered by failure recovery -
+  // resources not matching the selector behave according to the default Kubernetes mechanism.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+	
+  // PodTerminationConfig contains configuration for the `PodTerminationConfig` strategy.
+	// +optional
+  PodTerminationConfig *PodTerminationConfig `json:"podTerminationConfig,omitempty"`
+}
+
+type PodTerminationConfig struct {
+	// PodTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  ForcefulTerminationGracePeriod *time.Duration `json:"forcefulTerminationGracePeriod"`
+}
+
+type FailureRecoveryAction string
+
+const (
+	PodTermination FailureRecoveryAction = "PodTermination"
+)
+```
+
+This API requires opt-in both from the administrators and user's end, as the workloads would
+have to be labeled in order for them to be covered with failure recovery.
+
+An example failure recovery configuration:
+
+```yaml
+failureRecoveryPolicy:
+  rules:
+  - action: PodTermination
+    podLabelSelector:
+      safe-to-fail: true
+    podTerminationConfig:
+      forcefulTerminationGracePeriod: 5m
+```
+
+### Default Grace Period
+
+The proposal is to **not** set a default value for the forceful termination grace period.
+This puts configuration over convention, as it will make the user consciously think about the
+grace period the need for their specific use case, without risking a faulty default.
+
+Since enabling the feature is inherently risky, this serves as an additional explicit
+acknowledgement of this risk from the user's end.
+
+Alternatively, a default value can be inferred from the `terminationGracePeriodSeconds`.
+For example, it could be a multiple of that value. Nevertheless, this still runs the risk of
+setting a very small value or even 0, depending on the user's configuration.
+
+### Controller
+
+Setting the strategy type to `PodTermination` will enable a controller, which manages
+the failed nodes.
+
+The controller has to ignore any updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil || pod.DeletionGracePeriodSeconds == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not managed by Kueue.
+    * The `kueue.x-k8s.io/managed` label is set for pods with pod integration enabled.
+    * The presence of the `kueue.x-k8s.io/podset` label could be used in other cases.
+1. Match the label selector defined in the configured `FailureRecoveryPolicy`.
+
+
+For terminating pods matching the criteria, the controller schedules another reconciliation
+to happen after the remaining grace period elapses:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+	now := r.clock.Now()
+	gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+	totalGracePeriod := gracefulTerminationPeriod + zombiePodTerminationGracePeriod
+	if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+		gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+		return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+	}
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and deleted:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+	pod.Status.Phase = corev1.PodFailed
+	if err := r.client.Status().Update(ctx, pod); err != nil {
```

  sgtm, we should aim for minimal support in the first iteration, but flexible API which will allow to introduce knobs like "delete" vs mark "Failed". I think it is reasonable to assume that the default in the first iteration is just "mark as Failed".

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-05T11:40:48Z

- **keps/6757-failure-recovery/README.md:132** by [@kshalot](https://github.com/kshalot) — 2025-11-05T11:40:48Z
```diff
@@ -0,0 +1,336 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+    - [Advantages](#advantages)
+    - [Drawbacks](#drawbacks-1)
+  - [Hide The Controller Behind A Feature Gate](#hide-the-controller-behind-a-feature-gate)
+    - [Advantages](#advantages-1)
+    - [Drawbacks](#drawbacks-2)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+    - [Advantages](#advantages-2)
+    - [Drawbacks](#drawbacks-3)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of unblocking the execution of zombie pods and recovering their quota,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/not-ready` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for to pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are evicted from the node and marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that manages moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themself, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicies`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+	// +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.  
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Action specifies the action taken to recover from failures.  
+  Action FailureRecoveryAction `json:"action"`
+
+  // PodLabelSelector specifies the scope of resources covered by failure recovery -
+  // resources not matching the selector behave according to the default Kubernetes mechanism.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+	
+  // PodTerminationConfig contains configuration for the `PodTerminationConfig` strategy.
```

  Indeed. I was considering this before going on vacation. There is no point in keeping the action field with how the API turned out, I'm going to remove it. It's simpler and better.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-18T10:03:13Z

- **keps/6757-failure-recovery/README.md:158** by [@mimowo](https://github.com/mimowo) — 2025-11-18T09:37:08Z
```diff
@@ -0,0 +1,341 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // +kubebuilder:validation:MinItems=1
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+// +kubebuilder:validation:ExactlyOneOf=["terminatePod"]
+type FailureRecoveryRule struct {
+  // Exactly one of the fields below must be specified.
+
+  // TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+  // This strategy recovers stuck pods by forcefully terminating them after a configured
+  // grace period elapses.
+  // +optional
+  TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+  // PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+  // resources not matching the selector are ignored by the controller.
+  // If a pod matches multiple configurations, the strictest one (with the shortest grace period)
+  // will be applied.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+
+  // ForcefulTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  // Represented using metav1.Duration (e.g. "10m", "1h30m").
+  // +required
+  ForcefulTerminationGracePeriod metav1.Duration `json:"forcefulTerminationGracePeriod"`
+}
+```
+
+This API requires opt-in both from the administrators and user's end, as the workloads would
+have to be labeled in order for them to be covered with failure recovery.
+
+An example failure recovery configuration:
+
+```yaml
+failureRecoveryPolicy:
+  rules:
+  - terminatePod:
+      podLabelSelector:
+        matchExpressions:
+        - key: safe-to-fail
+          operator: In
+          values: [ "true", "True", "yes" ]
```

  nit, let's just use one value
  ```suggestion
            values: [ "true" ]
  ```

- **keps/6757-failure-recovery/kep.yaml:29** by [@mimowo](https://github.com/mimowo) — 2025-11-18T09:37:30Z
```diff
@@ -0,0 +1,34 @@
+title: Failure Recovery
+kep-number: 6757
+authors:
+  - "@kshalot"
+status: provisional
+creation-date: 2025-10-17
+reviewers:
+  - "@mimowo"
+  - "@olekzabl"
+  - "@pbundyra"
+approvers:
+  - "@mimowo"
+  - "@tenzen-y"
+
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.15"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.15"
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+feature-gates:
+  - name: FailureRecovery
```

  ```suggestion
    - name: FailureRecoveryPolicy
  ```
  nit, just to make it consistent with the field name

- **keps/6757-failure-recovery/README.md:135** by [@mimowo](https://github.com/mimowo) — 2025-11-18T09:59:20Z
```diff
@@ -0,0 +1,341 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // +kubebuilder:validation:MinItems=1
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+// +kubebuilder:validation:ExactlyOneOf=["terminatePod"]
+type FailureRecoveryRule struct {
+  // Exactly one of the fields below must be specified.
+
+  // TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+  // This strategy recovers stuck pods by forcefully terminating them after a configured
+  // grace period elapses.
+  // +optional
+  TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+  // PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+  // resources not matching the selector are ignored by the controller.
+  // If a pod matches multiple configurations, the strictest one (with the shortest grace period)
+  // will be applied.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
```

  ```suggestion
    PodLabelSelector metav1.LabelSelector `json:"podLabelSelector,omitempty"`
  ```
  Why would that be a pointer? I think we can safely make it required. If someone wants to terminate all Pods they could do: `podLabelSelector: {}`

- **keps/6757-failure-recovery/README.md:134** by [@mimowo](https://github.com/mimowo) — 2025-11-18T09:59:43Z
```diff
@@ -0,0 +1,341 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // +kubebuilder:validation:MinItems=1
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+// +kubebuilder:validation:ExactlyOneOf=["terminatePod"]
+type FailureRecoveryRule struct {
+  // Exactly one of the fields below must be specified.
+
+  // TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+  // This strategy recovers stuck pods by forcefully terminating them after a configured
+  // grace period elapses.
+  // +optional
+  TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+  // PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+  // resources not matching the selector are ignored by the controller.
+  // If a pod matches multiple configurations, the strictest one (with the shortest grace period)
+  // will be applied.
```

  This is more of the description about evaluating the order of rules, so the comment belongs to the "Rules" field inside FailureRecoveryPolicy.
  
  However, I'm not certain this approach is the best. Alternatives I see:
  1. we choose whatever is the first matching rule
  2. we choose the least disruptive action first (say PodTermination over taining Node)
  3. we choose the first in time action (as proposed currently)
  
  I would suggest to avoid committing and for now only support a single rule.

- **keps/6757-failure-recovery/README.md:258** by [@mimowo](https://github.com/mimowo) — 2025-11-18T10:01:43Z
```diff
@@ -0,0 +1,341 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // +kubebuilder:validation:MinItems=1
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+// +kubebuilder:validation:ExactlyOneOf=["terminatePod"]
+type FailureRecoveryRule struct {
+  // Exactly one of the fields below must be specified.
+
+  // TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+  // This strategy recovers stuck pods by forcefully terminating them after a configured
+  // grace period elapses.
+  // +optional
+  TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+  // PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+  // resources not matching the selector are ignored by the controller.
+  // If a pod matches multiple configurations, the strictest one (with the shortest grace period)
+  // will be applied.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+
+  // ForcefulTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  // Represented using metav1.Duration (e.g. "10m", "1h30m").
+  // +required
+  ForcefulTerminationGracePeriod metav1.Duration `json:"forcefulTerminationGracePeriod"`
+}
+```
+
+This API requires opt-in both from the administrators and user's end, as the workloads would
+have to be labeled in order for them to be covered with failure recovery.
+
+An example failure recovery configuration:
+
+```yaml
+failureRecoveryPolicy:
+  rules:
+  - terminatePod:
+      podLabelSelector:
+        matchExpressions:
+        - key: safe-to-fail
+          operator: In
+          values: [ "true", "True", "yes" ]
+      forcefulTerminationGracePeriod: 5m
+```
+
+### Default Grace Period
+
+The proposal is to **not** set a default value for the forceful termination grace period.
+This puts configuration over convention, as it will make the user consciously think about the
+grace period the need for their specific use case, without risking a faulty default.
+
+Since enabling the feature is inherently risky, this serves as an additional explicit
+acknowledgement of this risk from the user's end.
+
+Alternatively, a default value can be inferred from the `terminationGracePeriodSeconds`.
+For example, it could be a multiple of that value. Nevertheless, this still runs the risk of
+setting a very small value or even 0, depending on the user's configuration.
+
+### Controller
+
+Setting the strategy type to `TerminatePod` will enable a controller, which manages
+the failed nodes.
+
+The controller has to **ignore** updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not managed by Kueue.
+    * Neither the `kueue.x-k8s.io/managed` (for pod integration) nor `kueue.x-k8s.io/podset` label is set.
+1. Don't match the label selector defined in the configured `FailureRecoveryPolicy`.
+1. Are **not** scheduled on a node tainted with `node.kubernetes.io/unreachable`.
+    * This explicitly ignores pods assigned to nodes that still have a running kubelet.
+    For example, nodes with the `node.kubernetes.io/not-ready` taint experiencing resource pressure
+    that makes pod termination take longer.
+
+
+For relevant (not ignored) terminating pods, the controller schedules another reconciliation
+to happen after the remaining grace period elapses.
+In case a pod is matched by multiple selectors defined in the policy (many actions with `TerminatePodConfig`),
+the controller will use the **lowest** `ForcefulTerminationGracePeriod` that was found.
+
+```go
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+  terminationCfg := strictestMatchingTerminationConfig(r.terminationCfgs, pod)
+  // ...
+
+  now := r.clock.Now()
+  gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+  totalGracePeriod := gracefulTerminationPeriod + *terminationCfg.ForcefulTerminationGracePeriod
+  if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+    gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+    return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+  }
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and transitioned into the `PodFailed` phase:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  pod.Status.Phase = corev1.PodFailed
+  if err := r.client.Status().Update(ctx, pod); err != nil {
+    return ctrl.Result{}, err
+  }
+
+  return ctrl.Result{}, nil
+}
+```
+
+### Test Plan
+
+[ ] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+The proposal will be covered with unit tests for:
+1. Configuration parsing.
+1. Controller behavior:
+    1. Whether it ignores irrelevant pods (not terminating, not managed by Kueue, already failed/succeeded, not labeled).
+    1. Whether it correctly schedules a reconciliation for when the grace period elapses.
+    1. Whether it updates the pod's phase to `Failed` after the grace period elapses.
+
+#### Integration Tests
+
+The proposal will be covered with integrations tests that check whether:
+1. Adding a deletion timestamp to the pod requeues a reconciliation loop for when the grace period elapses.
+1. After the grace period elapses, the pod is marked as `Failed`.
+1. Replacement pods are scheduled in the place of the failed pod (optional, technically 1+2 is sufficient to prove this).
+
+
+Existing integration tests should prove that this feature does not impact Kueue during normal operation.
+
+### Graduation Criteria
+
+#### Alpha
+
+- Positive feedback from the users.
+
```

  Add Also 
  
  #### Beta
  
  - feature gate enabled by default
  - re-evaluate allowing for multiple rules in failurePolicyRules

- **keps/6757-failure-recovery/README.md:257** by [@mimowo](https://github.com/mimowo) — 2025-11-18T10:01:46Z
```diff
@@ -0,0 +1,341 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // +kubebuilder:validation:MinItems=1
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+// +kubebuilder:validation:ExactlyOneOf=["terminatePod"]
+type FailureRecoveryRule struct {
+  // Exactly one of the fields below must be specified.
+
+  // TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+  // This strategy recovers stuck pods by forcefully terminating them after a configured
+  // grace period elapses.
+  // +optional
+  TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+  // PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+  // resources not matching the selector are ignored by the controller.
+  // If a pod matches multiple configurations, the strictest one (with the shortest grace period)
+  // will be applied.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+
+  // ForcefulTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  // Represented using metav1.Duration (e.g. "10m", "1h30m").
+  // +required
+  ForcefulTerminationGracePeriod metav1.Duration `json:"forcefulTerminationGracePeriod"`
+}
+```
+
+This API requires opt-in both from the administrators and user's end, as the workloads would
+have to be labeled in order for them to be covered with failure recovery.
+
+An example failure recovery configuration:
+
+```yaml
+failureRecoveryPolicy:
+  rules:
+  - terminatePod:
+      podLabelSelector:
+        matchExpressions:
+        - key: safe-to-fail
+          operator: In
+          values: [ "true", "True", "yes" ]
+      forcefulTerminationGracePeriod: 5m
+```
+
+### Default Grace Period
+
+The proposal is to **not** set a default value for the forceful termination grace period.
+This puts configuration over convention, as it will make the user consciously think about the
+grace period the need for their specific use case, without risking a faulty default.
+
+Since enabling the feature is inherently risky, this serves as an additional explicit
+acknowledgement of this risk from the user's end.
+
+Alternatively, a default value can be inferred from the `terminationGracePeriodSeconds`.
+For example, it could be a multiple of that value. Nevertheless, this still runs the risk of
+setting a very small value or even 0, depending on the user's configuration.
+
+### Controller
+
+Setting the strategy type to `TerminatePod` will enable a controller, which manages
+the failed nodes.
+
+The controller has to **ignore** updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not managed by Kueue.
+    * Neither the `kueue.x-k8s.io/managed` (for pod integration) nor `kueue.x-k8s.io/podset` label is set.
+1. Don't match the label selector defined in the configured `FailureRecoveryPolicy`.
+1. Are **not** scheduled on a node tainted with `node.kubernetes.io/unreachable`.
+    * This explicitly ignores pods assigned to nodes that still have a running kubelet.
+    For example, nodes with the `node.kubernetes.io/not-ready` taint experiencing resource pressure
+    that makes pod termination take longer.
+
+
+For relevant (not ignored) terminating pods, the controller schedules another reconciliation
+to happen after the remaining grace period elapses.
+In case a pod is matched by multiple selectors defined in the policy (many actions with `TerminatePodConfig`),
+the controller will use the **lowest** `ForcefulTerminationGracePeriod` that was found.
+
+```go
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+  terminationCfg := strictestMatchingTerminationConfig(r.terminationCfgs, pod)
+  // ...
+
+  now := r.clock.Now()
+  gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+  totalGracePeriod := gracefulTerminationPeriod + *terminationCfg.ForcefulTerminationGracePeriod
+  if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+    gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+    return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+  }
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and transitioned into the `PodFailed` phase:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  pod.Status.Phase = corev1.PodFailed
+  if err := r.client.Status().Update(ctx, pod); err != nil {
+    return ctrl.Result{}, err
+  }
+
+  return ctrl.Result{}, nil
+}
+```
+
+### Test Plan
+
+[ ] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+The proposal will be covered with unit tests for:
+1. Configuration parsing.
+1. Controller behavior:
+    1. Whether it ignores irrelevant pods (not terminating, not managed by Kueue, already failed/succeeded, not labeled).
+    1. Whether it correctly schedules a reconciliation for when the grace period elapses.
+    1. Whether it updates the pod's phase to `Failed` after the grace period elapses.
+
+#### Integration Tests
+
+The proposal will be covered with integrations tests that check whether:
+1. Adding a deletion timestamp to the pod requeues a reconciliation loop for when the grace period elapses.
+1. After the grace period elapses, the pod is marked as `Failed`.
+1. Replacement pods are scheduled in the place of the failed pod (optional, technically 1+2 is sufficient to prove this).
+
+
+Existing integration tests should prove that this feature does not impact Kueue during normal operation.
+
+### Graduation Criteria
+
+#### Alpha
+
+- Positive feedback from the users.
```

  Add feature gate disabled by default

- **keps/6757-failure-recovery/README.md:175** by [@mimowo](https://github.com/mimowo) — 2025-11-18T10:02:22Z
```diff
@@ -0,0 +1,341 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // +kubebuilder:validation:MinItems=1
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+// +kubebuilder:validation:ExactlyOneOf=["terminatePod"]
+type FailureRecoveryRule struct {
+  // Exactly one of the fields below must be specified.
+
+  // TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+  // This strategy recovers stuck pods by forcefully terminating them after a configured
+  // grace period elapses.
+  // +optional
+  TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+  // PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+  // resources not matching the selector are ignored by the controller.
+  // If a pod matches multiple configurations, the strictest one (with the shortest grace period)
+  // will be applied.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+
+  // ForcefulTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  // Represented using metav1.Duration (e.g. "10m", "1h30m").
+  // +required
+  ForcefulTerminationGracePeriod metav1.Duration `json:"forcefulTerminationGracePeriod"`
+}
+```
+
+This API requires opt-in both from the administrators and user's end, as the workloads would
+have to be labeled in order for them to be covered with failure recovery.
+
+An example failure recovery configuration:
+
+```yaml
+failureRecoveryPolicy:
+  rules:
+  - terminatePod:
+      podLabelSelector:
+        matchExpressions:
+        - key: safe-to-fail
+          operator: In
+          values: [ "true", "True", "yes" ]
+      forcefulTerminationGracePeriod: 5m
+```
+
+### Default Grace Period
+
+The proposal is to **not** set a default value for the forceful termination grace period.
+This puts configuration over convention, as it will make the user consciously think about the
+grace period the need for their specific use case, without risking a faulty default.
+
+Since enabling the feature is inherently risky, this serves as an additional explicit
+acknowledgement of this risk from the user's end.
+
+Alternatively, a default value can be inferred from the `terminationGracePeriodSeconds`.
+For example, it could be a multiple of that value. Nevertheless, this still runs the risk of
+setting a very small value or even 0, depending on the user's configuration.
+
+### Controller
```

  ```suggestion
  ### Implementation overview
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-18T10:39:48Z

- **keps/6757-failure-recovery/README.md:156** by [@mimowo](https://github.com/mimowo) — 2025-11-18T10:39:48Z
```diff
@@ -0,0 +1,346 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // +kubebuilder:validation:MinItems=1
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+// +kubebuilder:validation:ExactlyOneOf=["terminatePod"]
+type FailureRecoveryRule struct {
+  // Exactly one of the fields below must be specified.
+
+  // TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+  // This strategy recovers stuck pods by forcefully terminating them after a configured
+  // grace period elapses.
+  // +optional
+  TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+  // PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+  // resources not matching the selector are ignored by the controller.
+  // If a pod matches multiple configurations, the strictest one (with the shortest grace period)
+  // will be applied.
+  PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+
+  // ForcefulTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  // Represented using metav1.Duration (e.g. "10m", "1h30m").
+  // +required
+  ForcefulTerminationGracePeriod metav1.Duration `json:"forcefulTerminationGracePeriod"`
+}
+```
+
+This API requires opt-in both from the administrators and user's end, as the workloads would
+have to be labeled in order for them to be covered with failure recovery.
+
+An example failure recovery configuration:
+
+```yaml
+failureRecoveryPolicy:
+  rules:
+  - terminatePod:
+      podLabelSelector:
+        matchExpressions:
+        - key: safe-to-fail
```

  ```suggestion
          - key: example.com/pod-safe-to-fail
  ```
  nit, to make it a bit more realistic, as labels typically have the domain name

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-18T10:42:32Z

- **keps/6757-failure-recovery/README.md:134** by [@kshalot](https://github.com/kshalot) — 2025-11-18T10:42:32Z
```diff
@@ -0,0 +1,341 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // +kubebuilder:validation:MinItems=1
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+// +kubebuilder:validation:ExactlyOneOf=["terminatePod"]
+type FailureRecoveryRule struct {
+  // Exactly one of the fields below must be specified.
+
+  // TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+  // This strategy recovers stuck pods by forcefully terminating them after a configured
+  // grace period elapses.
+  // +optional
+  TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+  // PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+  // resources not matching the selector are ignored by the controller.
+  // If a pod matches multiple configurations, the strictest one (with the shortest grace period)
+  // will be applied.
```

  I purposefully added this comment under `TerminatePodConfig` since using the "strictest" one is equivalent to using all of the defined `TerminatePodConfig`s (since all they do is terminate the pod, it doesn't matter is all of them are executed or only the first in time one).
  
  In case there is a second `TaintNode` action, I don't think this would clash, since both could be taken independently. But I see your point about overcommitting, as I cannot really foresee how this API will evolve.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-18T10:51:41Z

- **keps/6757-failure-recovery/README.md:117** by [@mimowo](https://github.com/mimowo) — 2025-11-18T10:51:41Z
```diff
@@ -0,0 +1,345 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // In `alpha`, only a singular rule is supported.
+  // +kubebuilder:validation:MinItems=1
+  // +kubebuilder:validation:MaxItems=1
```

  Actually I think the kubebuilder markers are not relevant here because we don't generate schema for the Config API. 
  
  I would just replace it with:
  // Exactly one rule can be specified. We keep the API flexible to accommodate
  // setting more rules in the future.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-18T10:53:29Z

- **keps/6757-failure-recovery/README.md:121** by [@mimowo](https://github.com/mimowo) — 2025-11-18T10:53:29Z
```diff
@@ -0,0 +1,345 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // In `alpha`, only a singular rule is supported.
+  // +kubebuilder:validation:MinItems=1
+  // +kubebuilder:validation:MaxItems=1
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+// +kubebuilder:validation:ExactlyOneOf=["terminatePod"]
```

  Here also drop the kubebuilder marker as it is non-functional. Instead we can extend the comment to the TerminatePod field as:
  
  // Currently specifying the field is required. We keep the API flexible to allow
  // introducing other rule actions in the future.
  // TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-18T10:57:53Z

- **keps/6757-failure-recovery/README.md:117** by [@kshalot](https://github.com/kshalot) — 2025-11-18T10:57:53Z
```diff
@@ -0,0 +1,345 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // In `alpha`, only a singular rule is supported.
+  // +kubebuilder:validation:MinItems=1
+  // +kubebuilder:validation:MaxItems=1
```

  Yes, I noticed that in the implementation PR, but I wanted to wait for any other insights on that part. I'll remove them  and extend the Go-code validation.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-18T11:01:30Z

- **keps/6757-failure-recovery/README.md:121** by [@kshalot](https://github.com/kshalot) — 2025-11-18T11:01:30Z
```diff
@@ -0,0 +1,345 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Controller](#controller)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // In `alpha`, only a singular rule is supported.
+  // +kubebuilder:validation:MinItems=1
+  // +kubebuilder:validation:MaxItems=1
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+// +kubebuilder:validation:ExactlyOneOf=["terminatePod"]
```

  Removed, although I kept "optional" as I see it's used in `configuration_types.go`. I assume it's for self-documentation purposes.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-18T12:10:29Z

- **keps/6757-failure-recovery/README.md:139** by [@mimowo](https://github.com/mimowo) — 2025-11-18T12:10:29Z
```diff
@@ -0,0 +1,346 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // Exactly one rule can be specified. We keep the API flexible to accommodate
+  // setting more rules in the future.
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Exactly one of the fields below must be specified.
+
+  // TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+  // This strategy recovers stuck pods by forcefully terminating them after a configured
+  // grace period elapses.
+  // Currently specifying the field is required. We keep the API flexible to allow
+  // introducing other rule actions in the future.
+  // +optional
+  TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+  // PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+  // resources not matching the selector are ignored by the controller.
+  PodLabelSelector metav1.LabelSelector `json:"podLabelSelector"`
+
+  // ForcefulTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
```

  ```suggestion
    // elapses and when the pod should be terminated.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-18T12:13:10Z

- **keps/6757-failure-recovery/README.md:141** by [@mimowo](https://github.com/mimowo) — 2025-11-18T12:13:11Z
```diff
@@ -0,0 +1,346 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // Exactly one rule can be specified. We keep the API flexible to accommodate
+  // setting more rules in the future.
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Exactly one of the fields below must be specified.
+
+  // TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+  // This strategy recovers stuck pods by forcefully terminating them after a configured
+  // grace period elapses.
+  // Currently specifying the field is required. We keep the API flexible to allow
+  // introducing other rule actions in the future.
+  // +optional
+  TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+  // PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+  // resources not matching the selector are ignored by the controller.
+  PodLabelSelector metav1.LabelSelector `json:"podLabelSelector"`
+
+  // ForcefulTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  // Represented using metav1.Duration (e.g. "10m", "1h30m").
+  ForcefulTerminationGracePeriod metav1.Duration `json:"forcefulTerminationGracePeriod"`
```

  I'm still thinking about the following: 
  - make `PodLabelSelector: {}` by default, and `ForcefulTerminationGracePeriod=1min` by default
  
  These defaults look sensible to me, then we could for alpha return to the idea of using the feature gate as in https://github.com/kubernetes-sigs/kueue/pull/7311/files#diff-310c798c33bb04bf5909da6b51c0511f274dc3f3abfe9949bb248933d1d3a8eeR297
  
  Then we eliminate the need for introducing the API, and we can rely on the FG to collect user feedback. This will largely derisk the first iteration of the feature IMO.
  
  If you are ok with that I would move the API to the Alternatives so that we can return to that.
  
  cc @tenzen-y wdyt?

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-18T12:47:12Z

- **keps/6757-failure-recovery/README.md:141** by [@kshalot](https://github.com/kshalot) — 2025-11-18T12:47:12Z
```diff
@@ -0,0 +1,346 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // Exactly one rule can be specified. We keep the API flexible to accommodate
+  // setting more rules in the future.
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Exactly one of the fields below must be specified.
+
+  // TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+  // This strategy recovers stuck pods by forcefully terminating them after a configured
+  // grace period elapses.
+  // Currently specifying the field is required. We keep the API flexible to allow
+  // introducing other rule actions in the future.
+  // +optional
+  TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+  // PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+  // resources not matching the selector are ignored by the controller.
+  PodLabelSelector metav1.LabelSelector `json:"podLabelSelector"`
+
+  // ForcefulTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  // Represented using metav1.Duration (e.g. "10m", "1h30m").
+  ForcefulTerminationGracePeriod metav1.Duration `json:"forcefulTerminationGracePeriod"`
```

  Personally, I'm fine with both. A feature gate is easier to implement and there are fewer edge cases to think about. OTOH the disadvantage of defaulting to `PodLabelSelector: {}` is that it would make it harder for the users to test - they would have to commit all their pods to this new behavior instead of a selected control group limited by a label selector. Plus an alpha API could be removed, so the risk of introducing this feature as an API is small (or I am missing something). WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-18T13:07:32Z

- **keps/6757-failure-recovery/README.md:141** by [@mimowo](https://github.com/mimowo) — 2025-11-18T13:07:32Z
```diff
@@ -0,0 +1,346 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // Exactly one rule can be specified. We keep the API flexible to accommodate
+  // setting more rules in the future.
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Exactly one of the fields below must be specified.
+
+  // TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+  // This strategy recovers stuck pods by forcefully terminating them after a configured
+  // grace period elapses.
+  // Currently specifying the field is required. We keep the API flexible to allow
+  // introducing other rule actions in the future.
+  // +optional
+  TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+  // PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+  // resources not matching the selector are ignored by the controller.
+  PodLabelSelector metav1.LabelSelector `json:"podLabelSelector"`
+
+  // ForcefulTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  // Represented using metav1.Duration (e.g. "10m", "1h30m").
+  ForcefulTerminationGracePeriod metav1.Duration `json:"forcefulTerminationGracePeriod"`
```

  The API is Beta for config unfortunately. Unless we annotate the specific new field wirh Alpha level. 
  
  To mitigate the problem with the enablement for all Kueue pods we could introduce a reserved alpha level label called `kueue.x-k8s.io/pod-safe-to-forcefully-terminate`. Then the label selector could just be matching to it.
  
  So we have 3 variants:
  1. full blown API as currently 
  2. Alpha FG for all Kueue pods
  3. Alpha FG for all Kueue pods with the reserved label
  
  Im leaning to 2 or 3 given we are pretty late in the releasr cycle. No strong opinion between them. 
  
  Getting feedback about the feature can also help us influence the API for better versikn in (1.).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-18T13:11:35Z

- **keps/6757-failure-recovery/README.md:141** by [@mimowo](https://github.com/mimowo) — 2025-11-18T13:11:35Z
```diff
@@ -0,0 +1,346 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // Exactly one rule can be specified. We keep the API flexible to accommodate
+  // setting more rules in the future.
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Exactly one of the fields below must be specified.
+
+  // TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+  // This strategy recovers stuck pods by forcefully terminating them after a configured
+  // grace period elapses.
+  // Currently specifying the field is required. We keep the API flexible to allow
+  // introducing other rule actions in the future.
+  // +optional
+  TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+  // PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+  // resources not matching the selector are ignored by the controller.
+  PodLabelSelector metav1.LabelSelector `json:"podLabelSelector"`
+
+  // ForcefulTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  // Represented using metav1.Duration (e.g. "10m", "1h30m").
+  ForcefulTerminationGracePeriod metav1.Duration `json:"forcefulTerminationGracePeriod"`
```

  Actually Im slightly leaning to 3, bacause then it could even be enabled for Pods not managed by Kueue directly but those the user decides to annotate. It is a corner case but it was recently asked on wg-batch by @alaypatel07 as potentially useful outside of Kueue. cc @kannon92 also

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-18T13:18:08Z

- **keps/6757-failure-recovery/README.md:141** by [@kshalot](https://github.com/kshalot) — 2025-11-18T13:18:08Z
```diff
@@ -0,0 +1,346 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // Exactly one rule can be specified. We keep the API flexible to accommodate
+  // setting more rules in the future.
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Exactly one of the fields below must be specified.
+
+  // TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+  // This strategy recovers stuck pods by forcefully terminating them after a configured
+  // grace period elapses.
+  // Currently specifying the field is required. We keep the API flexible to allow
+  // introducing other rule actions in the future.
+  // +optional
+  TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+  // PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+  // resources not matching the selector are ignored by the controller.
+  PodLabelSelector metav1.LabelSelector `json:"podLabelSelector"`
+
+  // ForcefulTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  // Represented using metav1.Duration (e.g. "10m", "1h30m").
+  ForcefulTerminationGracePeriod metav1.Duration `json:"forcefulTerminationGracePeriod"`
```

  Good point.
  
  I'll rework the KEP towards option 2 and we can introduce option 3 if the discussion takes us there.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-18T13:27:12Z

- **keps/6757-failure-recovery/README.md:141** by [@kshalot](https://github.com/kshalot) — 2025-11-18T13:27:12Z
```diff
@@ -0,0 +1,346 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // Exactly one rule can be specified. We keep the API flexible to accommodate
+  // setting more rules in the future.
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Exactly one of the fields below must be specified.
+
+  // TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+  // This strategy recovers stuck pods by forcefully terminating them after a configured
+  // grace period elapses.
+  // Currently specifying the field is required. We keep the API flexible to allow
+  // introducing other rule actions in the future.
+  // +optional
+  TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+  // PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+  // resources not matching the selector are ignored by the controller.
+  PodLabelSelector metav1.LabelSelector `json:"podLabelSelector"`
+
+  // ForcefulTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  // Represented using metav1.Duration (e.g. "10m", "1h30m").
+  ForcefulTerminationGracePeriod metav1.Duration `json:"forcefulTerminationGracePeriod"`
```

  I missed your last comment - let's go for 3. then.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-18T14:17:59Z

- **keps/6757-failure-recovery/README.md:141** by [@mimowo](https://github.com/mimowo) — 2025-11-18T14:17:58Z
```diff
@@ -0,0 +1,346 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // Exactly one rule can be specified. We keep the API flexible to accommodate
+  // setting more rules in the future.
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Exactly one of the fields below must be specified.
+
+  // TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+  // This strategy recovers stuck pods by forcefully terminating them after a configured
+  // grace period elapses.
+  // Currently specifying the field is required. We keep the API flexible to allow
+  // introducing other rule actions in the future.
+  // +optional
+  TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+  // PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+  // resources not matching the selector are ignored by the controller.
+  PodLabelSelector metav1.LabelSelector `json:"podLabelSelector"`
+
+  // ForcefulTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  // Represented using metav1.Duration (e.g. "10m", "1h30m").
+  ForcefulTerminationGracePeriod metav1.Duration `json:"forcefulTerminationGracePeriod"`
```

  sgtm, let me know when ready

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-18T14:22:14Z

- **keps/6757-failure-recovery/README.md:141** by [@mimowo](https://github.com/mimowo) — 2025-11-18T14:22:14Z
```diff
@@ -0,0 +1,346 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // Exactly one rule can be specified. We keep the API flexible to accommodate
+  // setting more rules in the future.
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Exactly one of the fields below must be specified.
+
+  // TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+  // This strategy recovers stuck pods by forcefully terminating them after a configured
+  // grace period elapses.
+  // Currently specifying the field is required. We keep the API flexible to allow
+  // introducing other rule actions in the future.
+  // +optional
+  TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+  // PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+  // resources not matching the selector are ignored by the controller.
+  PodLabelSelector metav1.LabelSelector `json:"podLabelSelector"`
+
+  // ForcefulTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  // Represented using metav1.Duration (e.g. "10m", "1h30m").
+  ForcefulTerminationGracePeriod metav1.Duration `json:"forcefulTerminationGracePeriod"`
```

  Another benefit of avoiding (1.) is that we want to deprecate it anyway once the problem is solved in the core k8s, this way we avoid committing long term. We will just need to deprecate and remove support for the reserved label which is simpler than for the API.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-18T14:27:43Z

- **keps/6757-failure-recovery/README.md:141** by [@kshalot](https://github.com/kshalot) — 2025-11-18T14:27:43Z
```diff
@@ -0,0 +1,346 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // Exactly one rule can be specified. We keep the API flexible to accommodate
+  // setting more rules in the future.
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Exactly one of the fields below must be specified.
+
+  // TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+  // This strategy recovers stuck pods by forcefully terminating them after a configured
+  // grace period elapses.
+  // Currently specifying the field is required. We keep the API flexible to allow
+  // introducing other rule actions in the future.
+  // +optional
+  TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+  // PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+  // resources not matching the selector are ignored by the controller.
+  PodLabelSelector metav1.LabelSelector `json:"podLabelSelector"`
+
+  // ForcefulTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  // Represented using metav1.Duration (e.g. "10m", "1h30m").
+  ForcefulTerminationGracePeriod metav1.Duration `json:"forcefulTerminationGracePeriod"`
```

  I added https://github.com/kubernetes-sigs/kueue/pull/7311/commits/8a9628646235e913d44c0b52704d549e14cc32c5 with the change. I'm still looking for any leftovers, but it should be reviewable already.
  
  I'm keeping this discussion open.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-18T14:34:17Z

- **keps/6757-failure-recovery/README.md:141** by [@mimowo](https://github.com/mimowo) — 2025-11-18T14:34:17Z
```diff
@@ -0,0 +1,346 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // Exactly one rule can be specified. We keep the API flexible to accommodate
+  // setting more rules in the future.
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Exactly one of the fields below must be specified.
+
+  // TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+  // This strategy recovers stuck pods by forcefully terminating them after a configured
+  // grace period elapses.
+  // Currently specifying the field is required. We keep the API flexible to allow
+  // introducing other rule actions in the future.
+  // +optional
+  TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+  // PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+  // resources not matching the selector are ignored by the controller.
+  PodLabelSelector metav1.LabelSelector `json:"podLabelSelector"`
+
+  // ForcefulTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  // Represented using metav1.Duration (e.g. "10m", "1h30m").
+  ForcefulTerminationGracePeriod metav1.Duration `json:"forcefulTerminationGracePeriod"`
```

  I think we can make it an annotation also, annotations are considered more lightweight mechanism. I don't think we need label

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-18T14:46:36Z

- **keps/6757-failure-recovery/README.md:141** by [@kshalot](https://github.com/kshalot) — 2025-11-18T14:46:36Z
```diff
@@ -0,0 +1,346 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // Exactly one rule can be specified. We keep the API flexible to accommodate
+  // setting more rules in the future.
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Exactly one of the fields below must be specified.
+
+  // TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+  // This strategy recovers stuck pods by forcefully terminating them after a configured
+  // grace period elapses.
+  // Currently specifying the field is required. We keep the API flexible to allow
+  // introducing other rule actions in the future.
+  // +optional
+  TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+  // PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+  // resources not matching the selector are ignored by the controller.
+  PodLabelSelector metav1.LabelSelector `json:"podLabelSelector"`
+
+  // ForcefulTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  // Represented using metav1.Duration (e.g. "10m", "1h30m").
+  ForcefulTerminationGracePeriod metav1.Duration `json:"forcefulTerminationGracePeriod"`
```

  Yeah, I'm looking at the implementation and we don't need the pods to be indexed by that label. We'll have the relevant object in the reconciler, so we can just check whether the relevant key exists in the Annotations map.
  
  I'll make the change

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-18T14:55:47Z

- **keps/6757-failure-recovery/README.md:141** by [@kshalot](https://github.com/kshalot) — 2025-11-18T14:55:47Z
```diff
@@ -0,0 +1,346 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // Exactly one rule can be specified. We keep the API flexible to accommodate
+  // setting more rules in the future.
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Exactly one of the fields below must be specified.
+
+  // TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+  // This strategy recovers stuck pods by forcefully terminating them after a configured
+  // grace period elapses.
+  // Currently specifying the field is required. We keep the API flexible to allow
+  // introducing other rule actions in the future.
+  // +optional
+  TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+  // PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+  // resources not matching the selector are ignored by the controller.
+  PodLabelSelector metav1.LabelSelector `json:"podLabelSelector"`
+
+  // ForcefulTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  // Represented using metav1.Duration (e.g. "10m", "1h30m").
+  ForcefulTerminationGracePeriod metav1.Duration `json:"forcefulTerminationGracePeriod"`
```

  Done. I also changed the name from:
  
  `kueue.x-k8s.io/pod-safe-to-forcefully-terminate`
  to:
  `kueue.x-k8s.io/safe-to-forcefully-terminate`
  
  in https://github.com/kubernetes-sigs/kueue/pull/7311/commits/2b7bdec8f33fceec782a14ac8aaf4e2fae4d3a4c. I felt it's maybe better to keep the name more generic? I can revert that commit if the consensus is opposite.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-18T15:04:58Z

- **keps/6757-failure-recovery/README.md:141** by [@mimowo](https://github.com/mimowo) — 2025-11-18T15:04:57Z
```diff
@@ -0,0 +1,346 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Default Grace Period](#default-grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Hide The Controller Only Behind A Feature Gate](#hide-the-controller-only-behind-a-feature-gate)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+* Introduce the concept of failure recovery to the Kueue manager configuration API.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+* Introduce an API for the users to:
+  * Enable the controller.
+  * Configure the grace period between the `Pod`'s deletion time and when the transition to `Failed` happens.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Configuration API
+
+The `Configuration` struct is extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // Exactly one rule can be specified. We keep the API flexible to accommodate
+  // setting more rules in the future.
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Exactly one of the fields below must be specified.
+
+  // TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+  // This strategy recovers stuck pods by forcefully terminating them after a configured
+  // grace period elapses.
+  // Currently specifying the field is required. We keep the API flexible to allow
+  // introducing other rule actions in the future.
+  // +optional
+  TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+  // PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+  // resources not matching the selector are ignored by the controller.
+  PodLabelSelector metav1.LabelSelector `json:"podLabelSelector"`
+
+  // ForcefulTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully deleted.
+  // Represented using metav1.Duration (e.g. "10m", "1h30m").
+  ForcefulTerminationGracePeriod metav1.Duration `json:"forcefulTerminationGracePeriod"`
```

  new name sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-18T15:36:22Z

LGTM overall, mostly editorial nits

- **keps/6757-failure-recovery/README.md:101** by [@mimowo](https://github.com/mimowo) — 2025-11-18T15:29:03Z
```diff
@@ -0,0 +1,354 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Defaults](#defaults)
+    - [Affected Pods](#affected-pods)
+    - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+  * The controller is enabled via a feature gate.
+* Introduce a `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation that limits which pods are affected by the new controller.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Defaults
```

  nit, not sure what the "Defaults" parent section gives, let's just drop it and move children one level up.

- **keps/6757-failure-recovery/README.md:94** by [@mimowo](https://github.com/mimowo) — 2025-11-18T15:29:52Z
```diff
@@ -0,0 +1,354 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Defaults](#defaults)
+    - [Affected Pods](#affected-pods)
+    - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+  * The controller is enabled via a feature gate.
+* Introduce a `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation that limits which pods are affected by the new controller.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
```

  ```suggestion
  1. The Administrator - by enabling the feature globally 
  ```

- **keps/6757-failure-recovery/README.md:126** by [@mimowo](https://github.com/mimowo) — 2025-11-18T15:36:07Z
```diff
@@ -0,0 +1,354 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Defaults](#defaults)
+    - [Affected Pods](#affected-pods)
+    - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+  * The controller is enabled via a feature gate.
+* Introduce a `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation that limits which pods are affected by the new controller.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Defaults
+
+#### Affected Pods
+
+In order to allow the first adopters to control which pods are affected by the new controller, a new `Pod` annotation will be introduced:
+```yaml
+kueue.x-k8s.io/safe-to-forcefully-terminate: "true"
+```
+
```

  Not sure where is the best place, but I would like to also include a sample Job snippet using this mechanism, something like:
  
  ```yaml
  apiVersion: batch/v1
  kind: Job
  metadata:
    generateName: sample-job-
    namespace: default
    labels:
      kueue.x-k8s.io/queue-name: user-queue
  spec:
    parallelism: 3
    completions: 3
    podReplacementPolicy: Failed
    template:
      metadata:
        annotations:
          kueue.x-k8s.io/safe-to-forcefully-terminate: "true"         // <- new annotation
      spec:
        containers:
        - name: dummy-job
          image: registry.k8s.io/e2e-test-images/agnhost:2.53
          command: [ "/bin/sh" ]
          args: [ "-c", "sleep 600" ]
          resources:
            requests:
              cpu: "1"
              memory: "200Mi"
        restartPolicy: Never
  ```
  I think this could be either here or in user stories

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-18T16:18:45Z

- **keps/6757-failure-recovery/README.md:126** by [@kshalot](https://github.com/kshalot) — 2025-11-18T16:18:44Z
```diff
@@ -0,0 +1,354 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Defaults](#defaults)
+    - [Affected Pods](#affected-pods)
+    - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+  * The controller is enabled via a feature gate.
+* Introduce a `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation that limits which pods are affected by the new controller.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by defining the recovery rules and selectors in the Kueue config.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Defaults
+
+#### Affected Pods
+
+In order to allow the first adopters to control which pods are affected by the new controller, a new `Pod` annotation will be introduced:
+```yaml
+kueue.x-k8s.io/safe-to-forcefully-terminate: "true"
+```
+
```

  Added it here, below the annotation definition.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-11-19T05:16:59Z

Thank you for moving this forward @kshalot @mimowo

- **keps/6757-failure-recovery/README.md:48** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-19T04:42:26Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
```

  > It is especially relevant for stateful applications and might result in data corruption.
  For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
  
  This is just a confirmation.
  Does this assume that Node is healthy (containerd and runc are healthy), but kubelet is down?

- **keps/6757-failure-recovery/README.md:41** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-19T04:49:07Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
```

  ```suggestion
  When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, 
  the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint (after being marked with `node.kubernetes.io/not-ready` first).
  By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
  ```
  
  AFAIK, the node taints will be progressing N/A -> `node.kubernetes.io/not-ready` -> `node.kubernetes.io/unreachable`

- **keps/6757-failure-recovery/README.md:177** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-19T04:56:37Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+  * The controller is enabled via a feature gate.
+* Introduce a `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation that limits which pods are affected by the new controller.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by enabling the feature globally.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Affected Pods
+
+In order to allow the first adopters to control which pods are affected by the new controller, a new `Pod` annotation will be introduced:
+```yaml
+kueue.x-k8s.io/safe-to-forcefully-terminate: "true"
+```
+
+Only pods containing the new annotation will be affected by the controller, for example:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 3
+  completions: 3
+  podReplacementPolicy: Failed
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/safe-to-forcefully-terminate: "true"  // <- new annotation
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "sleep 600" ]
+        resources:
+          requests:
+            cpu: "1"
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+> The controller is not limited to pods managed by Kueue (i.e. having the `kueue.x-k8s.io/managed` or `kueue.x-k8s.io/podset` label). All pods in the cluster that have the new annotation will be affected.
+
+### Grace Period
+
+A default grace period of **1 minute** will be introduced to get initial feedback about the feature.
+This will help inform the decision on the structure of the API and setting defaults makes sense when graduating to beta.
+
+### Implementation Overview
+
+Setting the strategy type to `TerminatePod` will enable a controller, which manages
+the failed nodes.
+
+The controller has to **ignore** updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not annotated with the new `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation.
+1. Are **not** scheduled on a node tainted with `node.kubernetes.io/unreachable`.
+    * This explicitly ignores pods assigned to nodes that still have a running kubelet.
+    For example, nodes with the `node.kubernetes.io/not-ready` taint experiencing resource pressure
+    that makes pod termination take longer.
+
+For relevant (not ignored) terminating pods, the controller schedules another reconciliation
+to happen after the remaining grace period elapses.
+
+```go
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  now := r.clock.Now()
+  gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+  totalGracePeriod := gracefulTerminationPeriod + defaultForcefulTerminationGracePeriod
+  if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+    gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+    return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+  }
+
+  // ...
+}
+```
```

  ```suggestion
  ```
  
  We strongly recommend not to contain actual code in KEP.
  Such code will be outdated soon, then this code will bring us confusion, which will contribute to making understanding hard.
  
  Instead of that, it would be better to describe the flow step-by-step like:
  
  ```
  The TerminatePod controller behaves for target terminating Pods in the following: 
  1. Compute how much time Pod has been stuck since `.metadata.deletionGracePeriodSeconds`.
  2. If the computed stuck time exceeds 1 min, the controller acts in the forcefully terminating phase; otherwise, the reconciler will requeue the object once the stuck time reaches 1 min.
  
  After the TerinatePod controller triggers the ZombiePod controller, the zombie pod is added `Failed` phase. 
  ```

- **keps/6757-failure-recovery/README.md:193** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-19T05:04:53Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+  * The controller is enabled via a feature gate.
+* Introduce a `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation that limits which pods are affected by the new controller.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by enabling the feature globally.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Affected Pods
+
+In order to allow the first adopters to control which pods are affected by the new controller, a new `Pod` annotation will be introduced:
+```yaml
+kueue.x-k8s.io/safe-to-forcefully-terminate: "true"
+```
+
+Only pods containing the new annotation will be affected by the controller, for example:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 3
+  completions: 3
+  podReplacementPolicy: Failed
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/safe-to-forcefully-terminate: "true"  // <- new annotation
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "sleep 600" ]
+        resources:
+          requests:
+            cpu: "1"
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+> The controller is not limited to pods managed by Kueue (i.e. having the `kueue.x-k8s.io/managed` or `kueue.x-k8s.io/podset` label). All pods in the cluster that have the new annotation will be affected.
+
+### Grace Period
+
+A default grace period of **1 minute** will be introduced to get initial feedback about the feature.
+This will help inform the decision on the structure of the API and setting defaults makes sense when graduating to beta.
+
+### Implementation Overview
+
+Setting the strategy type to `TerminatePod` will enable a controller, which manages
+the failed nodes.
+
+The controller has to **ignore** updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not annotated with the new `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation.
+1. Are **not** scheduled on a node tainted with `node.kubernetes.io/unreachable`.
+    * This explicitly ignores pods assigned to nodes that still have a running kubelet.
+    For example, nodes with the `node.kubernetes.io/not-ready` taint experiencing resource pressure
+    that makes pod termination take longer.
+
+For relevant (not ignored) terminating pods, the controller schedules another reconciliation
+to happen after the remaining grace period elapses.
+
+```go
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  now := r.clock.Now()
+  gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+  totalGracePeriod := gracefulTerminationPeriod + defaultForcefulTerminationGracePeriod
+  if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+    gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+    return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+  }
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and transitioned into the `PodFailed` phase:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  pod.Status.Phase = corev1.PodFailed
+  if err := r.client.Status().Update(ctx, pod); err != nil {
+    return ctrl.Result{}, err
+  }
+
+  return ctrl.Result{}, nil
+}
+```
```

  ```suggestion
  ```
  
  As described above.

- **keps/6757-failure-recovery/README.md:262** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-19T05:05:20Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+  * The controller is enabled via a feature gate.
+* Introduce a `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation that limits which pods are affected by the new controller.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by enabling the feature globally.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Affected Pods
+
+In order to allow the first adopters to control which pods are affected by the new controller, a new `Pod` annotation will be introduced:
+```yaml
+kueue.x-k8s.io/safe-to-forcefully-terminate: "true"
+```
+
+Only pods containing the new annotation will be affected by the controller, for example:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 3
+  completions: 3
+  podReplacementPolicy: Failed
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/safe-to-forcefully-terminate: "true"  // <- new annotation
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "sleep 600" ]
+        resources:
+          requests:
+            cpu: "1"
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+> The controller is not limited to pods managed by Kueue (i.e. having the `kueue.x-k8s.io/managed` or `kueue.x-k8s.io/podset` label). All pods in the cluster that have the new annotation will be affected.
+
+### Grace Period
+
+A default grace period of **1 minute** will be introduced to get initial feedback about the feature.
+This will help inform the decision on the structure of the API and setting defaults makes sense when graduating to beta.
+
+### Implementation Overview
+
+Setting the strategy type to `TerminatePod` will enable a controller, which manages
+the failed nodes.
+
+The controller has to **ignore** updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not annotated with the new `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation.
+1. Are **not** scheduled on a node tainted with `node.kubernetes.io/unreachable`.
+    * This explicitly ignores pods assigned to nodes that still have a running kubelet.
+    For example, nodes with the `node.kubernetes.io/not-ready` taint experiencing resource pressure
+    that makes pod termination take longer.
+
+For relevant (not ignored) terminating pods, the controller schedules another reconciliation
+to happen after the remaining grace period elapses.
+
+```go
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  now := r.clock.Now()
+  gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+  totalGracePeriod := gracefulTerminationPeriod + defaultForcefulTerminationGracePeriod
+  if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+    gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+    return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+  }
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and transitioned into the `PodFailed` phase:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  pod.Status.Phase = corev1.PodFailed
+  if err := r.client.Status().Update(ctx, pod); err != nil {
+    return ctrl.Result{}, err
+  }
+
+  return ctrl.Result{}, nil
+}
+```
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+The proposal will be covered with unit tests for:
+1. Configuration parsing.
+1. Controller behavior:
+    1. Whether it ignores irrelevant pods (not terminating, already failed/succeeded, not annotated).
+    1. Whether it correctly schedules a reconciliation for when the grace period elapses.
+    1. Whether it updates the pod's phase to `Failed` after the grace period elapses.
+
+#### Integration Tests
+
+The proposal will be covered with integrations tests that check whether:
+1. Adding a deletion timestamp to the pod requeues a reconciliation loop for when the grace period elapses.
+1. After the grace period elapses, the pod is marked as `Failed`.
+1. Replacement pods are scheduled in the place of the failed pod (optional, technically 1+2 is sufficient to prove this).
+
+Existing integration tests should prove that this feature does not impact Kueue during normal operation.
+
+### Graduation Criteria
+
+#### Alpha
+
+- Feature gate disabled by default.
+- Positive feedback from the users.
+
+### Beta
+
+- Feature gate enabled by default.
+- Re-evaluate the introduction of the `FailureRecoveryPolicy` API.
```

  I agree. We should reevaluate that later.

- **keps/6757-failure-recovery/README.md:142** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-19T05:06:53Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+  * The controller is enabled via a feature gate.
+* Introduce a `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation that limits which pods are affected by the new controller.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by enabling the feature globally.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Affected Pods
+
+In order to allow the first adopters to control which pods are affected by the new controller, a new `Pod` annotation will be introduced:
+```yaml
+kueue.x-k8s.io/safe-to-forcefully-terminate: "true"
+```
+
+Only pods containing the new annotation will be affected by the controller, for example:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 3
+  completions: 3
+  podReplacementPolicy: Failed
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/safe-to-forcefully-terminate: "true"  // <- new annotation
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "sleep 600" ]
+        resources:
+          requests:
+            cpu: "1"
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+> The controller is not limited to pods managed by Kueue (i.e. having the `kueue.x-k8s.io/managed` or `kueue.x-k8s.io/podset` label). All pods in the cluster that have the new annotation will be affected.
+
+### Grace Period
+
+A default grace period of **1 minute** will be introduced to get initial feedback about the feature.
+This will help inform the decision on the structure of the API and setting defaults makes sense when graduating to beta.
```

  Can you mention that the grace period is counted based on Pod `.metadata.deletionGracePeriodSeconds`?

- **keps/6757-failure-recovery/README.md:180** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-19T05:15:10Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+  * The controller is enabled via a feature gate.
+* Introduce a `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation that limits which pods are affected by the new controller.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by enabling the feature globally.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Affected Pods
+
+In order to allow the first adopters to control which pods are affected by the new controller, a new `Pod` annotation will be introduced:
+```yaml
+kueue.x-k8s.io/safe-to-forcefully-terminate: "true"
+```
+
+Only pods containing the new annotation will be affected by the controller, for example:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 3
+  completions: 3
+  podReplacementPolicy: Failed
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/safe-to-forcefully-terminate: "true"  // <- new annotation
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "sleep 600" ]
+        resources:
+          requests:
+            cpu: "1"
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+> The controller is not limited to pods managed by Kueue (i.e. having the `kueue.x-k8s.io/managed` or `kueue.x-k8s.io/podset` label). All pods in the cluster that have the new annotation will be affected.
+
+### Grace Period
+
+A default grace period of **1 minute** will be introduced to get initial feedback about the feature.
+This will help inform the decision on the structure of the API and setting defaults makes sense when graduating to beta.
+
+### Implementation Overview
+
+Setting the strategy type to `TerminatePod` will enable a controller, which manages
+the failed nodes.
+
+The controller has to **ignore** updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not annotated with the new `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation.
+1. Are **not** scheduled on a node tainted with `node.kubernetes.io/unreachable`.
+    * This explicitly ignores pods assigned to nodes that still have a running kubelet.
+    For example, nodes with the `node.kubernetes.io/not-ready` taint experiencing resource pressure
+    that makes pod termination take longer.
+
+For relevant (not ignored) terminating pods, the controller schedules another reconciliation
+to happen after the remaining grace period elapses.
+
+```go
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  now := r.clock.Now()
+  gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+  totalGracePeriod := gracefulTerminationPeriod + defaultForcefulTerminationGracePeriod
+  if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+    gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+    return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+  }
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and transitioned into the `PodFailed` phase:
```

  It would be better to insert Pod Condition along with Pod phase for observability.
  I am assuming the following condition:
  
  ```yaml
  status:
    conditions:
    - type: FailureRecovery
       status: True
       reason: ForcefullyTerminated           <- Alternatively, ForcefullyTerminatedByKueue could be considered.
       message: "Pod forcefully terminated after 1234s grace period due to unreachable node %s (triggered by kueue.x-k8s.io/safe-to-forcefully-terminate annotation)"
       ...
  ```
  
  @mimowo WDYT?

- **keps/6757-failure-recovery/README.md:278** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-19T05:16:40Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+  * The controller is enabled via a feature gate.
+* Introduce a `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation that limits which pods are affected by the new controller.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by enabling the feature globally.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Affected Pods
+
+In order to allow the first adopters to control which pods are affected by the new controller, a new `Pod` annotation will be introduced:
+```yaml
+kueue.x-k8s.io/safe-to-forcefully-terminate: "true"
+```
+
+Only pods containing the new annotation will be affected by the controller, for example:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 3
+  completions: 3
+  podReplacementPolicy: Failed
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/safe-to-forcefully-terminate: "true"  // <- new annotation
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "sleep 600" ]
+        resources:
+          requests:
+            cpu: "1"
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+> The controller is not limited to pods managed by Kueue (i.e. having the `kueue.x-k8s.io/managed` or `kueue.x-k8s.io/podset` label). All pods in the cluster that have the new annotation will be affected.
+
+### Grace Period
+
+A default grace period of **1 minute** will be introduced to get initial feedback about the feature.
+This will help inform the decision on the structure of the API and setting defaults makes sense when graduating to beta.
+
+### Implementation Overview
+
+Setting the strategy type to `TerminatePod` will enable a controller, which manages
+the failed nodes.
+
+The controller has to **ignore** updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not annotated with the new `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation.
+1. Are **not** scheduled on a node tainted with `node.kubernetes.io/unreachable`.
+    * This explicitly ignores pods assigned to nodes that still have a running kubelet.
+    For example, nodes with the `node.kubernetes.io/not-ready` taint experiencing resource pressure
+    that makes pod termination take longer.
+
+For relevant (not ignored) terminating pods, the controller schedules another reconciliation
+to happen after the remaining grace period elapses.
+
+```go
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  now := r.clock.Now()
+  gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+  totalGracePeriod := gracefulTerminationPeriod + defaultForcefulTerminationGracePeriod
+  if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+    gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+    return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+  }
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and transitioned into the `PodFailed` phase:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  pod.Status.Phase = corev1.PodFailed
+  if err := r.client.Status().Update(ctx, pod); err != nil {
+    return ctrl.Result{}, err
+  }
+
+  return ctrl.Result{}, nil
+}
+```
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+The proposal will be covered with unit tests for:
+1. Configuration parsing.
+1. Controller behavior:
+    1. Whether it ignores irrelevant pods (not terminating, already failed/succeeded, not annotated).
+    1. Whether it correctly schedules a reconciliation for when the grace period elapses.
+    1. Whether it updates the pod's phase to `Failed` after the grace period elapses.
+
+#### Integration Tests
+
+The proposal will be covered with integrations tests that check whether:
+1. Adding a deletion timestamp to the pod requeues a reconciliation loop for when the grace period elapses.
+1. After the grace period elapses, the pod is marked as `Failed`.
+1. Replacement pods are scheduled in the place of the failed pod (optional, technically 1+2 is sufficient to prove this).
+
+Existing integration tests should prove that this feature does not impact Kueue during normal operation.
+
+### Graduation Criteria
+
+#### Alpha
+
+- Feature gate disabled by default.
+- Positive feedback from the users.
+
+### Beta
+
+- Feature gate enabled by default.
+- Re-evaluate the introduction of the `FailureRecoveryPolicy` API.
+
+## Implementation History
+
+2025-09-08: The [issue](https://github.com/kubernetes-sigs/kueue/issues/6757) is raised in Kueue.
+
+2025-09-12: The [issue](https://github.com/kubernetes/kubernetes/issues/134038) is raised in core Kubernetes.
+
+2025-10-17: First draft of the KEP.
+
+## Drawbacks
+
+* The same feature is discussed in core Kubernetes ([kubernetes/issues/134038](https://github.com/kubernetes/kubernetes/issues/134038)), so the underlying issue could potentially be fixed upstream. The timeline of an upstream change is long, but if the feature is deemed not time-critical, it could be fixed at the source instead of in Kueue.
+* This feature introduces a potential footgun to Kueue users and could turn out to be to volatile/risky to use in most affected cases.
+* It introduces yet another responsibility for Kueue - on top of quota management and scheduling, it will also start performing failure recovery.
+
+## Alternatives
```

  IIUC, Non Graceful Shutdown (https://kubernetes.io/docs/concepts/cluster-administration/node-shutdown/#non-graceful-node-shutdown) could be considered as an alternative.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-19T07:25:14Z

- **keps/6757-failure-recovery/README.md:180** by [@mimowo](https://github.com/mimowo) — 2025-11-19T07:25:13Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+  * The controller is enabled via a feature gate.
+* Introduce a `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation that limits which pods are affected by the new controller.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by enabling the feature globally.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Affected Pods
+
+In order to allow the first adopters to control which pods are affected by the new controller, a new `Pod` annotation will be introduced:
+```yaml
+kueue.x-k8s.io/safe-to-forcefully-terminate: "true"
+```
+
+Only pods containing the new annotation will be affected by the controller, for example:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 3
+  completions: 3
+  podReplacementPolicy: Failed
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/safe-to-forcefully-terminate: "true"  // <- new annotation
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "sleep 600" ]
+        resources:
+          requests:
+            cpu: "1"
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+> The controller is not limited to pods managed by Kueue (i.e. having the `kueue.x-k8s.io/managed` or `kueue.x-k8s.io/podset` label). All pods in the cluster that have the new annotation will be affected.
+
+### Grace Period
+
+A default grace period of **1 minute** will be introduced to get initial feedback about the feature.
+This will help inform the decision on the structure of the API and setting defaults makes sense when graduating to beta.
+
+### Implementation Overview
+
+Setting the strategy type to `TerminatePod` will enable a controller, which manages
+the failed nodes.
+
+The controller has to **ignore** updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not annotated with the new `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation.
+1. Are **not** scheduled on a node tainted with `node.kubernetes.io/unreachable`.
+    * This explicitly ignores pods assigned to nodes that still have a running kubelet.
+    For example, nodes with the `node.kubernetes.io/not-ready` taint experiencing resource pressure
+    that makes pod termination take longer.
+
+For relevant (not ignored) terminating pods, the controller schedules another reconciliation
+to happen after the remaining grace period elapses.
+
+```go
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  now := r.clock.Now()
+  gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+  totalGracePeriod := gracefulTerminationPeriod + defaultForcefulTerminationGracePeriod
+  if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+    gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+    return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+  }
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and transitioned into the `PodFailed` phase:
```

  sgtm, also sending an event would be helpful

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-19T07:27:41Z

- **keps/6757-failure-recovery/README.md:278** by [@mimowo](https://github.com/mimowo) — 2025-11-19T07:27:41Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+  * The controller is enabled via a feature gate.
+* Introduce a `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation that limits which pods are affected by the new controller.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by enabling the feature globally.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Affected Pods
+
+In order to allow the first adopters to control which pods are affected by the new controller, a new `Pod` annotation will be introduced:
+```yaml
+kueue.x-k8s.io/safe-to-forcefully-terminate: "true"
+```
+
+Only pods containing the new annotation will be affected by the controller, for example:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 3
+  completions: 3
+  podReplacementPolicy: Failed
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/safe-to-forcefully-terminate: "true"  // <- new annotation
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "sleep 600" ]
+        resources:
+          requests:
+            cpu: "1"
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+> The controller is not limited to pods managed by Kueue (i.e. having the `kueue.x-k8s.io/managed` or `kueue.x-k8s.io/podset` label). All pods in the cluster that have the new annotation will be affected.
+
+### Grace Period
+
+A default grace period of **1 minute** will be introduced to get initial feedback about the feature.
+This will help inform the decision on the structure of the API and setting defaults makes sense when graduating to beta.
+
+### Implementation Overview
+
+Setting the strategy type to `TerminatePod` will enable a controller, which manages
+the failed nodes.
+
+The controller has to **ignore** updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not annotated with the new `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation.
+1. Are **not** scheduled on a node tainted with `node.kubernetes.io/unreachable`.
+    * This explicitly ignores pods assigned to nodes that still have a running kubelet.
+    For example, nodes with the `node.kubernetes.io/not-ready` taint experiencing resource pressure
+    that makes pod termination take longer.
+
+For relevant (not ignored) terminating pods, the controller schedules another reconciliation
+to happen after the remaining grace period elapses.
+
+```go
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  now := r.clock.Now()
+  gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+  totalGracePeriod := gracefulTerminationPeriod + defaultForcefulTerminationGracePeriod
+  if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+    gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+    return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+  }
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and transitioned into the `PodFailed` phase:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  pod.Status.Phase = corev1.PodFailed
+  if err := r.client.Status().Update(ctx, pod); err != nil {
+    return ctrl.Result{}, err
+  }
+
+  return ctrl.Result{}, nil
+}
+```
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+The proposal will be covered with unit tests for:
+1. Configuration parsing.
+1. Controller behavior:
+    1. Whether it ignores irrelevant pods (not terminating, already failed/succeeded, not annotated).
+    1. Whether it correctly schedules a reconciliation for when the grace period elapses.
+    1. Whether it updates the pod's phase to `Failed` after the grace period elapses.
+
+#### Integration Tests
+
+The proposal will be covered with integrations tests that check whether:
+1. Adding a deletion timestamp to the pod requeues a reconciliation loop for when the grace period elapses.
+1. After the grace period elapses, the pod is marked as `Failed`.
+1. Replacement pods are scheduled in the place of the failed pod (optional, technically 1+2 is sufficient to prove this).
+
+Existing integration tests should prove that this feature does not impact Kueue during normal operation.
+
+### Graduation Criteria
+
+#### Alpha
+
+- Feature gate disabled by default.
+- Positive feedback from the users.
+
+### Beta
+
+- Feature gate enabled by default.
+- Re-evaluate the introduction of the `FailureRecoveryPolicy` API.
+
+## Implementation History
+
+2025-09-08: The [issue](https://github.com/kubernetes-sigs/kueue/issues/6757) is raised in Kueue.
+
+2025-09-12: The [issue](https://github.com/kubernetes/kubernetes/issues/134038) is raised in core Kubernetes.
+
+2025-10-17: First draft of the KEP.
+
+## Drawbacks
+
+* The same feature is discussed in core Kubernetes ([kubernetes/issues/134038](https://github.com/kubernetes/kubernetes/issues/134038)), so the underlying issue could potentially be fixed upstream. The timeline of an upstream change is long, but if the feature is deemed not time-critical, it could be fixed at the source instead of in Kueue.
+* This feature introduces a potential footgun to Kueue users and could turn out to be to volatile/risky to use in most affected cases.
+* It introduces yet another responsibility for Kueue - on top of quota management and scheduling, it will also start performing failure recovery.
+
+## Alternatives
```

  Yes as an alternative mechanism to terminate Pods, however this mechanism still requires third party, user or controller, to be triggered, by setting the OutOfService on the node.
  
  It was discussed but the downside is that it will also affect Pods which arent managed by Kueue, so it is less scoped,  e ause its scope is entire node. As a consequence some pods which arent safe to terminate could be marked as Finished by gc_controller in the core k8s

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-19T08:25:13Z

- **keps/6757-failure-recovery/README.md:177** by [@kshalot](https://github.com/kshalot) — 2025-11-19T08:25:13Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+  * The controller is enabled via a feature gate.
+* Introduce a `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation that limits which pods are affected by the new controller.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by enabling the feature globally.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Affected Pods
+
+In order to allow the first adopters to control which pods are affected by the new controller, a new `Pod` annotation will be introduced:
+```yaml
+kueue.x-k8s.io/safe-to-forcefully-terminate: "true"
+```
+
+Only pods containing the new annotation will be affected by the controller, for example:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 3
+  completions: 3
+  podReplacementPolicy: Failed
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/safe-to-forcefully-terminate: "true"  // <- new annotation
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "sleep 600" ]
+        resources:
+          requests:
+            cpu: "1"
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+> The controller is not limited to pods managed by Kueue (i.e. having the `kueue.x-k8s.io/managed` or `kueue.x-k8s.io/podset` label). All pods in the cluster that have the new annotation will be affected.
+
+### Grace Period
+
+A default grace period of **1 minute** will be introduced to get initial feedback about the feature.
+This will help inform the decision on the structure of the API and setting defaults makes sense when graduating to beta.
+
+### Implementation Overview
+
+Setting the strategy type to `TerminatePod` will enable a controller, which manages
+the failed nodes.
+
+The controller has to **ignore** updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not annotated with the new `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation.
+1. Are **not** scheduled on a node tainted with `node.kubernetes.io/unreachable`.
+    * This explicitly ignores pods assigned to nodes that still have a running kubelet.
+    For example, nodes with the `node.kubernetes.io/not-ready` taint experiencing resource pressure
+    that makes pod termination take longer.
+
+For relevant (not ignored) terminating pods, the controller schedules another reconciliation
+to happen after the remaining grace period elapses.
+
+```go
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  now := r.clock.Now()
+  gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+  totalGracePeriod := gracefulTerminationPeriod + defaultForcefulTerminationGracePeriod
+  if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+    gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+    return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+  }
+
+  // ...
+}
+```
```

  Agreed. Adding code did seem awkward, especially when I had to keep it in sync with the implementation. Removed.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-19T08:32:06Z

- **keps/6757-failure-recovery/README.md:48** by [@kshalot](https://github.com/kshalot) — 2025-11-19T08:32:06Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
```

  There's also the scenario of network partitions, where everything is running (including the kubelet) but there's no way for the control plane and kubelet to communicate.
  
  I'm wondering whether that's something that should be mentioned explicitly in the KEP as well.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-19T08:42:15Z

- **keps/6757-failure-recovery/README.md:48** by [@mimowo](https://github.com/mimowo) — 2025-11-19T08:42:14Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
```

  +1, we probably will never have an exhaustive list of scenarios, but mentioning the two will give us a better understanding. 
  
  Please also mention the flow from the taints and conditions point-of-view in both cases. Maybe it does not matter.  That could be part of the Notes section.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-19T09:36:01Z

- **keps/6757-failure-recovery/README.md:278** by [@kshalot](https://github.com/kshalot) — 2025-11-19T09:36:01Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+  * The controller is enabled via a feature gate.
+* Introduce a `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation that limits which pods are affected by the new controller.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by enabling the feature globally.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Affected Pods
+
+In order to allow the first adopters to control which pods are affected by the new controller, a new `Pod` annotation will be introduced:
+```yaml
+kueue.x-k8s.io/safe-to-forcefully-terminate: "true"
+```
+
+Only pods containing the new annotation will be affected by the controller, for example:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 3
+  completions: 3
+  podReplacementPolicy: Failed
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/safe-to-forcefully-terminate: "true"  // <- new annotation
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "sleep 600" ]
+        resources:
+          requests:
+            cpu: "1"
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+> The controller is not limited to pods managed by Kueue (i.e. having the `kueue.x-k8s.io/managed` or `kueue.x-k8s.io/podset` label). All pods in the cluster that have the new annotation will be affected.
+
+### Grace Period
+
+A default grace period of **1 minute** will be introduced to get initial feedback about the feature.
+This will help inform the decision on the structure of the API and setting defaults makes sense when graduating to beta.
+
+### Implementation Overview
+
+Setting the strategy type to `TerminatePod` will enable a controller, which manages
+the failed nodes.
+
+The controller has to **ignore** updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not annotated with the new `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation.
+1. Are **not** scheduled on a node tainted with `node.kubernetes.io/unreachable`.
+    * This explicitly ignores pods assigned to nodes that still have a running kubelet.
+    For example, nodes with the `node.kubernetes.io/not-ready` taint experiencing resource pressure
+    that makes pod termination take longer.
+
+For relevant (not ignored) terminating pods, the controller schedules another reconciliation
+to happen after the remaining grace period elapses.
+
+```go
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  now := r.clock.Now()
+  gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+  totalGracePeriod := gracefulTerminationPeriod + defaultForcefulTerminationGracePeriod
+  if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+    gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+    return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+  }
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and transitioned into the `PodFailed` phase:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  pod.Status.Phase = corev1.PodFailed
+  if err := r.client.Status().Update(ctx, pod); err != nil {
+    return ctrl.Result{}, err
+  }
+
+  return ctrl.Result{}, nil
+}
+```
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+The proposal will be covered with unit tests for:
+1. Configuration parsing.
+1. Controller behavior:
+    1. Whether it ignores irrelevant pods (not terminating, already failed/succeeded, not annotated).
+    1. Whether it correctly schedules a reconciliation for when the grace period elapses.
+    1. Whether it updates the pod's phase to `Failed` after the grace period elapses.
+
+#### Integration Tests
+
+The proposal will be covered with integrations tests that check whether:
+1. Adding a deletion timestamp to the pod requeues a reconciliation loop for when the grace period elapses.
+1. After the grace period elapses, the pod is marked as `Failed`.
+1. Replacement pods are scheduled in the place of the failed pod (optional, technically 1+2 is sufficient to prove this).
+
+Existing integration tests should prove that this feature does not impact Kueue during normal operation.
+
+### Graduation Criteria
+
+#### Alpha
+
+- Feature gate disabled by default.
+- Positive feedback from the users.
+
+### Beta
+
+- Feature gate enabled by default.
+- Re-evaluate the introduction of the `FailureRecoveryPolicy` API.
+
+## Implementation History
+
+2025-09-08: The [issue](https://github.com/kubernetes-sigs/kueue/issues/6757) is raised in Kueue.
+
+2025-09-12: The [issue](https://github.com/kubernetes/kubernetes/issues/134038) is raised in core Kubernetes.
+
+2025-10-17: First draft of the KEP.
+
+## Drawbacks
+
+* The same feature is discussed in core Kubernetes ([kubernetes/issues/134038](https://github.com/kubernetes/kubernetes/issues/134038)), so the underlying issue could potentially be fixed upstream. The timeline of an upstream change is long, but if the feature is deemed not time-critical, it could be fixed at the source instead of in Kueue.
+* This feature introduces a potential footgun to Kueue users and could turn out to be to volatile/risky to use in most affected cases.
+* It introduces yet another responsibility for Kueue - on top of quota management and scheduling, it will also start performing failure recovery.
+
+## Alternatives
```

  It's under the "Managing The `node.kubernetes.io/out-of-service` Taint On `Node`" alternative section

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-19T09:51:58Z

- **keps/6757-failure-recovery/README.md:278** by [@mimowo](https://github.com/mimowo) — 2025-11-19T09:51:58Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+  * The controller is enabled via a feature gate.
+* Introduce a `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation that limits which pods are affected by the new controller.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by enabling the feature globally.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Affected Pods
+
+In order to allow the first adopters to control which pods are affected by the new controller, a new `Pod` annotation will be introduced:
+```yaml
+kueue.x-k8s.io/safe-to-forcefully-terminate: "true"
+```
+
+Only pods containing the new annotation will be affected by the controller, for example:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 3
+  completions: 3
+  podReplacementPolicy: Failed
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/safe-to-forcefully-terminate: "true"  // <- new annotation
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "sleep 600" ]
+        resources:
+          requests:
+            cpu: "1"
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+> The controller is not limited to pods managed by Kueue (i.e. having the `kueue.x-k8s.io/managed` or `kueue.x-k8s.io/podset` label). All pods in the cluster that have the new annotation will be affected.
+
+### Grace Period
+
+A default grace period of **1 minute** will be introduced to get initial feedback about the feature.
+This will help inform the decision on the structure of the API and setting defaults makes sense when graduating to beta.
+
+### Implementation Overview
+
+Setting the strategy type to `TerminatePod` will enable a controller, which manages
+the failed nodes.
+
+The controller has to **ignore** updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not annotated with the new `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation.
+1. Are **not** scheduled on a node tainted with `node.kubernetes.io/unreachable`.
+    * This explicitly ignores pods assigned to nodes that still have a running kubelet.
+    For example, nodes with the `node.kubernetes.io/not-ready` taint experiencing resource pressure
+    that makes pod termination take longer.
+
+For relevant (not ignored) terminating pods, the controller schedules another reconciliation
+to happen after the remaining grace period elapses.
+
+```go
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  now := r.clock.Now()
+  gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+  totalGracePeriod := gracefulTerminationPeriod + defaultForcefulTerminationGracePeriod
+  if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+    gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+    return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+  }
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and transitioned into the `PodFailed` phase:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  pod.Status.Phase = corev1.PodFailed
+  if err := r.client.Status().Update(ctx, pod); err != nil {
+    return ctrl.Result{}, err
+  }
+
+  return ctrl.Result{}, nil
+}
+```
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+The proposal will be covered with unit tests for:
+1. Configuration parsing.
+1. Controller behavior:
+    1. Whether it ignores irrelevant pods (not terminating, already failed/succeeded, not annotated).
+    1. Whether it correctly schedules a reconciliation for when the grace period elapses.
+    1. Whether it updates the pod's phase to `Failed` after the grace period elapses.
+
+#### Integration Tests
+
+The proposal will be covered with integrations tests that check whether:
+1. Adding a deletion timestamp to the pod requeues a reconciliation loop for when the grace period elapses.
+1. After the grace period elapses, the pod is marked as `Failed`.
+1. Replacement pods are scheduled in the place of the failed pod (optional, technically 1+2 is sufficient to prove this).
+
+Existing integration tests should prove that this feature does not impact Kueue during normal operation.
+
+### Graduation Criteria
+
+#### Alpha
+
+- Feature gate disabled by default.
+- Positive feedback from the users.
+
+### Beta
+
+- Feature gate enabled by default.
+- Re-evaluate the introduction of the `FailureRecoveryPolicy` API.
+
+## Implementation History
+
+2025-09-08: The [issue](https://github.com/kubernetes-sigs/kueue/issues/6757) is raised in Kueue.
+
+2025-09-12: The [issue](https://github.com/kubernetes/kubernetes/issues/134038) is raised in core Kubernetes.
+
+2025-10-17: First draft of the KEP.
+
+## Drawbacks
+
+* The same feature is discussed in core Kubernetes ([kubernetes/issues/134038](https://github.com/kubernetes/kubernetes/issues/134038)), so the underlying issue could potentially be fixed upstream. The timeline of an upstream change is long, but if the feature is deemed not time-critical, it could be fixed at the source instead of in Kueue.
+* This feature introduces a potential footgun to Kueue users and could turn out to be to volatile/risky to use in most affected cases.
+* It introduces yet another responsibility for Kueue - on top of quota management and scheduling, it will also start performing failure recovery.
+
+## Alternatives
```

  Ok, add a reference to the on Non Graceful Shutdown (https://kubernetes.io/docs/concepts/cluster-administration/node-shutdown/#non-graceful-node-shutdown) from that section to use the more relatable naming. The OutOfService taint is more of technical detail

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-19T09:55:49Z

- **keps/6757-failure-recovery/README.md:41** by [@kshalot](https://github.com/kshalot) — 2025-11-19T09:55:48Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
```

  AFAIK the `node.kubernetes.io/not-ready` is assigned when the kubelet self-reports that it's not ready for running pods, for example due to resource pressure or some other issue (NotReady Ready condition)
  
  OTOH `node.kubernetes.io/unreachable` will be assigned when the kubelet fails to send a heartbeat so the control plane does not know it's status (Unknown Ready condition). I think we are only interested in this particular case.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-19T10:08:56Z

- **keps/6757-failure-recovery/README.md:278** by [@kshalot](https://github.com/kshalot) — 2025-11-19T10:08:55Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+  * The controller is enabled via a feature gate.
+* Introduce a `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation that limits which pods are affected by the new controller.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by enabling the feature globally.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Affected Pods
+
+In order to allow the first adopters to control which pods are affected by the new controller, a new `Pod` annotation will be introduced:
+```yaml
+kueue.x-k8s.io/safe-to-forcefully-terminate: "true"
+```
+
+Only pods containing the new annotation will be affected by the controller, for example:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 3
+  completions: 3
+  podReplacementPolicy: Failed
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/safe-to-forcefully-terminate: "true"  // <- new annotation
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "sleep 600" ]
+        resources:
+          requests:
+            cpu: "1"
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+> The controller is not limited to pods managed by Kueue (i.e. having the `kueue.x-k8s.io/managed` or `kueue.x-k8s.io/podset` label). All pods in the cluster that have the new annotation will be affected.
+
+### Grace Period
+
+A default grace period of **1 minute** will be introduced to get initial feedback about the feature.
+This will help inform the decision on the structure of the API and setting defaults makes sense when graduating to beta.
+
+### Implementation Overview
+
+Setting the strategy type to `TerminatePod` will enable a controller, which manages
+the failed nodes.
+
+The controller has to **ignore** updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not annotated with the new `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation.
+1. Are **not** scheduled on a node tainted with `node.kubernetes.io/unreachable`.
+    * This explicitly ignores pods assigned to nodes that still have a running kubelet.
+    For example, nodes with the `node.kubernetes.io/not-ready` taint experiencing resource pressure
+    that makes pod termination take longer.
+
+For relevant (not ignored) terminating pods, the controller schedules another reconciliation
+to happen after the remaining grace period elapses.
+
+```go
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  now := r.clock.Now()
+  gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+  totalGracePeriod := gracefulTerminationPeriod + defaultForcefulTerminationGracePeriod
+  if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+    gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+    return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+  }
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and transitioned into the `PodFailed` phase:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  pod.Status.Phase = corev1.PodFailed
+  if err := r.client.Status().Update(ctx, pod); err != nil {
+    return ctrl.Result{}, err
+  }
+
+  return ctrl.Result{}, nil
+}
+```
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+The proposal will be covered with unit tests for:
+1. Configuration parsing.
+1. Controller behavior:
+    1. Whether it ignores irrelevant pods (not terminating, already failed/succeeded, not annotated).
+    1. Whether it correctly schedules a reconciliation for when the grace period elapses.
+    1. Whether it updates the pod's phase to `Failed` after the grace period elapses.
+
+#### Integration Tests
+
+The proposal will be covered with integrations tests that check whether:
+1. Adding a deletion timestamp to the pod requeues a reconciliation loop for when the grace period elapses.
+1. After the grace period elapses, the pod is marked as `Failed`.
+1. Replacement pods are scheduled in the place of the failed pod (optional, technically 1+2 is sufficient to prove this).
+
+Existing integration tests should prove that this feature does not impact Kueue during normal operation.
+
+### Graduation Criteria
+
+#### Alpha
+
+- Feature gate disabled by default.
+- Positive feedback from the users.
+
+### Beta
+
+- Feature gate enabled by default.
+- Re-evaluate the introduction of the `FailureRecoveryPolicy` API.
+
+## Implementation History
+
+2025-09-08: The [issue](https://github.com/kubernetes-sigs/kueue/issues/6757) is raised in Kueue.
+
+2025-09-12: The [issue](https://github.com/kubernetes/kubernetes/issues/134038) is raised in core Kubernetes.
+
+2025-10-17: First draft of the KEP.
+
+## Drawbacks
+
+* The same feature is discussed in core Kubernetes ([kubernetes/issues/134038](https://github.com/kubernetes/kubernetes/issues/134038)), so the underlying issue could potentially be fixed upstream. The timeline of an upstream change is long, but if the feature is deemed not time-critical, it could be fixed at the source instead of in Kueue.
+* This feature introduces a potential footgun to Kueue users and could turn out to be to volatile/risky to use in most affected cases.
+* It introduces yet another responsibility for Kueue - on top of quota management and scheduling, it will also start performing failure recovery.
+
+## Alternatives
```

  Done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-19T10:09:04Z

- **keps/6757-failure-recovery/README.md:41** by [@mimowo](https://github.com/mimowo) — 2025-11-19T10:09:04Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
```

  I see, I'm fine with checking unreachable, sounds more robust.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-19T10:10:30Z

- **keps/6757-failure-recovery/README.md:48** by [@kshalot](https://github.com/kshalot) — 2025-11-19T10:10:30Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
```

  I updated the wording slightly in https://github.com/kubernetes-sigs/kueue/pull/7311/commits/f34063b8d17e6521172a271cbee4765fa18ab241, but since the behavior is the same in that case, I did not make any explicit changes to the flow description.
  
  I'm happy to expand upon that if you think it's necessary, I just did not want to be repetitive in the text.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-19T11:09:17Z

- **keps/6757-failure-recovery/README.md:41** by [@kshalot](https://github.com/kshalot) — 2025-11-19T11:09:17Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
```

  For reference, it's described [here](https://kubernetes.io/docs/reference/node/node-status/#condition) in the docs.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-19T11:35:00Z

- **keps/6757-failure-recovery/README.md:41** by [@kshalot](https://github.com/kshalot) — 2025-11-19T11:35:00Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
```

  I added a more streamlined description of the flow in https://github.com/kubernetes-sigs/kueue/pull/7311/commits/69a6e75dd1000913eaecf85d48d0e57bd3955915.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-19T11:35:16Z

- **keps/6757-failure-recovery/README.md:48** by [@kshalot](https://github.com/kshalot) — 2025-11-19T11:35:16Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
```

  Also added a more streamlined description of the flow in https://github.com/kubernetes-sigs/kueue/pull/7311/commits/69a6e75dd1000913eaecf85d48d0e57bd3955915.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-19T13:03:41Z

- **keps/6757-failure-recovery/README.md:180** by [@kshalot](https://github.com/kshalot) — 2025-11-19T13:03:41Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+  * The controller is enabled via a feature gate.
+* Introduce a `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation that limits which pods are affected by the new controller.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by enabling the feature globally.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Affected Pods
+
+In order to allow the first adopters to control which pods are affected by the new controller, a new `Pod` annotation will be introduced:
+```yaml
+kueue.x-k8s.io/safe-to-forcefully-terminate: "true"
+```
+
+Only pods containing the new annotation will be affected by the controller, for example:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 3
+  completions: 3
+  podReplacementPolicy: Failed
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/safe-to-forcefully-terminate: "true"  // <- new annotation
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "sleep 600" ]
+        resources:
+          requests:
+            cpu: "1"
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+> The controller is not limited to pods managed by Kueue (i.e. having the `kueue.x-k8s.io/managed` or `kueue.x-k8s.io/podset` label). All pods in the cluster that have the new annotation will be affected.
+
+### Grace Period
+
+A default grace period of **1 minute** will be introduced to get initial feedback about the feature.
+This will help inform the decision on the structure of the API and setting defaults makes sense when graduating to beta.
+
+### Implementation Overview
+
+Setting the strategy type to `TerminatePod` will enable a controller, which manages
+the failed nodes.
+
+The controller has to **ignore** updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not annotated with the new `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation.
+1. Are **not** scheduled on a node tainted with `node.kubernetes.io/unreachable`.
+    * This explicitly ignores pods assigned to nodes that still have a running kubelet.
+    For example, nodes with the `node.kubernetes.io/not-ready` taint experiencing resource pressure
+    that makes pod termination take longer.
+
+For relevant (not ignored) terminating pods, the controller schedules another reconciliation
+to happen after the remaining grace period elapses.
+
+```go
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  now := r.clock.Now()
+  gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+  totalGracePeriod := gracefulTerminationPeriod + defaultForcefulTerminationGracePeriod
+  if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+    gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+    return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+  }
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and transitioned into the `PodFailed` phase:
```

  Added `PodCondition` (https://github.com/kubernetes-sigs/kueue/pull/7311/commits/b9e7a08acafec9d2162af769b4b3680d44459515) and `Event` (https://github.com/kubernetes-sigs/kueue/pull/7311/commits/b76468ae5e5d3548f60e0d081880abc8e4d3b202)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-19T13:27:14Z

- **keps/6757-failure-recovery/README.md:180** by [@mimowo](https://github.com/mimowo) — 2025-11-19T13:27:14Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+  * The controller is enabled via a feature gate.
+* Introduce a `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation that limits which pods are affected by the new controller.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by enabling the feature globally.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Affected Pods
+
+In order to allow the first adopters to control which pods are affected by the new controller, a new `Pod` annotation will be introduced:
+```yaml
+kueue.x-k8s.io/safe-to-forcefully-terminate: "true"
+```
+
+Only pods containing the new annotation will be affected by the controller, for example:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 3
+  completions: 3
+  podReplacementPolicy: Failed
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/safe-to-forcefully-terminate: "true"  // <- new annotation
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "sleep 600" ]
+        resources:
+          requests:
+            cpu: "1"
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+> The controller is not limited to pods managed by Kueue (i.e. having the `kueue.x-k8s.io/managed` or `kueue.x-k8s.io/podset` label). All pods in the cluster that have the new annotation will be affected.
+
+### Grace Period
+
+A default grace period of **1 minute** will be introduced to get initial feedback about the feature.
+This will help inform the decision on the structure of the API and setting defaults makes sense when graduating to beta.
+
+### Implementation Overview
+
+Setting the strategy type to `TerminatePod` will enable a controller, which manages
+the failed nodes.
+
+The controller has to **ignore** updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not annotated with the new `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation.
+1. Are **not** scheduled on a node tainted with `node.kubernetes.io/unreachable`.
+    * This explicitly ignores pods assigned to nodes that still have a running kubelet.
+    For example, nodes with the `node.kubernetes.io/not-ready` taint experiencing resource pressure
+    that makes pod termination take longer.
+
+For relevant (not ignored) terminating pods, the controller schedules another reconciliation
+to happen after the remaining grace period elapses.
+
+```go
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  now := r.clock.Now()
+  gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+  totalGracePeriod := gracefulTerminationPeriod + defaultForcefulTerminationGracePeriod
+  if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+    gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+    return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+  }
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and transitioned into the `PodFailed` phase:
```

  lgtm, I woukd just prefer to include Kueue in condition type name. 
  
  FailueRecovery on its own feels generic that there could be another framwwork setting it. Also it would be unclear to the user who set it. Yes it could be inferred from message, but I think it might be better  calling it KueueFailureRecovery or FailureRecoveryByKueue

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-19T13:28:59Z

- **keps/6757-failure-recovery/README.md:180** by [@mimowo](https://github.com/mimowo) — 2025-11-19T13:28:59Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+  * The controller is enabled via a feature gate.
+* Introduce a `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation that limits which pods are affected by the new controller.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by enabling the feature globally.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Affected Pods
+
+In order to allow the first adopters to control which pods are affected by the new controller, a new `Pod` annotation will be introduced:
+```yaml
+kueue.x-k8s.io/safe-to-forcefully-terminate: "true"
+```
+
+Only pods containing the new annotation will be affected by the controller, for example:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 3
+  completions: 3
+  podReplacementPolicy: Failed
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/safe-to-forcefully-terminate: "true"  // <- new annotation
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "sleep 600" ]
+        resources:
+          requests:
+            cpu: "1"
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+> The controller is not limited to pods managed by Kueue (i.e. having the `kueue.x-k8s.io/managed` or `kueue.x-k8s.io/podset` label). All pods in the cluster that have the new annotation will be affected.
+
+### Grace Period
+
+A default grace period of **1 minute** will be introduced to get initial feedback about the feature.
+This will help inform the decision on the structure of the API and setting defaults makes sense when graduating to beta.
+
+### Implementation Overview
+
+Setting the strategy type to `TerminatePod` will enable a controller, which manages
+the failed nodes.
+
+The controller has to **ignore** updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not annotated with the new `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation.
+1. Are **not** scheduled on a node tainted with `node.kubernetes.io/unreachable`.
+    * This explicitly ignores pods assigned to nodes that still have a running kubelet.
+    For example, nodes with the `node.kubernetes.io/not-ready` taint experiencing resource pressure
+    that makes pod termination take longer.
+
+For relevant (not ignored) terminating pods, the controller schedules another reconciliation
+to happen after the remaining grace period elapses.
+
+```go
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  now := r.clock.Now()
+  gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+  totalGracePeriod := gracefulTerminationPeriod + defaultForcefulTerminationGracePeriod
+  if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+    gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+    return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+  }
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and transitioned into the `PodFailed` phase:
```

  I am also ok to park the condition idea for later based on user feedback. up to @tenzen-y

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-11-19T14:03:16Z

- **keps/6757-failure-recovery/README.md:180** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-19T14:03:16Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+  * The controller is enabled via a feature gate.
+* Introduce a `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation that limits which pods are affected by the new controller.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by enabling the feature globally.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Affected Pods
+
+In order to allow the first adopters to control which pods are affected by the new controller, a new `Pod` annotation will be introduced:
+```yaml
+kueue.x-k8s.io/safe-to-forcefully-terminate: "true"
+```
+
+Only pods containing the new annotation will be affected by the controller, for example:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 3
+  completions: 3
+  podReplacementPolicy: Failed
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/safe-to-forcefully-terminate: "true"  // <- new annotation
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "sleep 600" ]
+        resources:
+          requests:
+            cpu: "1"
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+> The controller is not limited to pods managed by Kueue (i.e. having the `kueue.x-k8s.io/managed` or `kueue.x-k8s.io/podset` label). All pods in the cluster that have the new annotation will be affected.
+
+### Grace Period
+
+A default grace period of **1 minute** will be introduced to get initial feedback about the feature.
+This will help inform the decision on the structure of the API and setting defaults makes sense when graduating to beta.
+
+### Implementation Overview
+
+Setting the strategy type to `TerminatePod` will enable a controller, which manages
+the failed nodes.
+
+The controller has to **ignore** updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not annotated with the new `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation.
+1. Are **not** scheduled on a node tainted with `node.kubernetes.io/unreachable`.
+    * This explicitly ignores pods assigned to nodes that still have a running kubelet.
+    For example, nodes with the `node.kubernetes.io/not-ready` taint experiencing resource pressure
+    that makes pod termination take longer.
+
+For relevant (not ignored) terminating pods, the controller schedules another reconciliation
+to happen after the remaining grace period elapses.
+
+```go
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  now := r.clock.Now()
+  gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+  totalGracePeriod := gracefulTerminationPeriod + defaultForcefulTerminationGracePeriod
+  if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+    gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+    return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+  }
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and transitioned into the `PodFailed` phase:
```

  > but I think it might be better calling it KueueFailureRecovery or FailureRecoveryByKueue
  
  As I proposed in the above comment, I'm ok with including `Kueue`. `KueueFailureRecovery` might be better since we typically use `ByABCD` for the underlying cause as a suffix. The `Kueue` prefix can decouple between those. 
  
  @mimowo @kshalot Which one do you prefer?
  
  > I am also ok to park the condition idea for later based on user feedback. up to @tenzen-y
  
  I think that the including Pod condition in Alpha phase would be better since users can leverage that for PodFailurePodlicy. When Pod gets this condition, researchers might want to ignore the failure count or prevent Job recreation for training data / model checkpoint consistency.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-19T14:06:22Z

- **keps/6757-failure-recovery/README.md:180** by [@mimowo](https://github.com/mimowo) — 2025-11-19T14:06:21Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+  * The controller is enabled via a feature gate.
+* Introduce a `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation that limits which pods are affected by the new controller.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by enabling the feature globally.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Affected Pods
+
+In order to allow the first adopters to control which pods are affected by the new controller, a new `Pod` annotation will be introduced:
+```yaml
+kueue.x-k8s.io/safe-to-forcefully-terminate: "true"
+```
+
+Only pods containing the new annotation will be affected by the controller, for example:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 3
+  completions: 3
+  podReplacementPolicy: Failed
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/safe-to-forcefully-terminate: "true"  // <- new annotation
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "sleep 600" ]
+        resources:
+          requests:
+            cpu: "1"
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+> The controller is not limited to pods managed by Kueue (i.e. having the `kueue.x-k8s.io/managed` or `kueue.x-k8s.io/podset` label). All pods in the cluster that have the new annotation will be affected.
+
+### Grace Period
+
+A default grace period of **1 minute** will be introduced to get initial feedback about the feature.
+This will help inform the decision on the structure of the API and setting defaults makes sense when graduating to beta.
+
+### Implementation Overview
+
+Setting the strategy type to `TerminatePod` will enable a controller, which manages
+the failed nodes.
+
+The controller has to **ignore** updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not annotated with the new `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation.
+1. Are **not** scheduled on a node tainted with `node.kubernetes.io/unreachable`.
+    * This explicitly ignores pods assigned to nodes that still have a running kubelet.
+    For example, nodes with the `node.kubernetes.io/not-ready` taint experiencing resource pressure
+    that makes pod termination take longer.
+
+For relevant (not ignored) terminating pods, the controller schedules another reconciliation
+to happen after the remaining grace period elapses.
+
+```go
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  now := r.clock.Now()
+  gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+  totalGracePeriod := gracefulTerminationPeriod + defaultForcefulTerminationGracePeriod
+  if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+    gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+    return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+  }
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and transitioned into the `PodFailed` phase:
```

  Thank you, I like KueueFailureRecovery more then we can follow the simple prefix pattern for other conditions if we introduce in the future

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-19T14:07:26Z

- **keps/6757-failure-recovery/README.md:180** by [@kshalot](https://github.com/kshalot) — 2025-11-19T14:07:25Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+  * The controller is enabled via a feature gate.
+* Introduce a `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation that limits which pods are affected by the new controller.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by enabling the feature globally.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Affected Pods
+
+In order to allow the first adopters to control which pods are affected by the new controller, a new `Pod` annotation will be introduced:
+```yaml
+kueue.x-k8s.io/safe-to-forcefully-terminate: "true"
+```
+
+Only pods containing the new annotation will be affected by the controller, for example:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 3
+  completions: 3
+  podReplacementPolicy: Failed
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/safe-to-forcefully-terminate: "true"  // <- new annotation
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "sleep 600" ]
+        resources:
+          requests:
+            cpu: "1"
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+> The controller is not limited to pods managed by Kueue (i.e. having the `kueue.x-k8s.io/managed` or `kueue.x-k8s.io/podset` label). All pods in the cluster that have the new annotation will be affected.
+
+### Grace Period
+
+A default grace period of **1 minute** will be introduced to get initial feedback about the feature.
+This will help inform the decision on the structure of the API and setting defaults makes sense when graduating to beta.
+
+### Implementation Overview
+
+Setting the strategy type to `TerminatePod` will enable a controller, which manages
+the failed nodes.
+
+The controller has to **ignore** updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not annotated with the new `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation.
+1. Are **not** scheduled on a node tainted with `node.kubernetes.io/unreachable`.
+    * This explicitly ignores pods assigned to nodes that still have a running kubelet.
+    For example, nodes with the `node.kubernetes.io/not-ready` taint experiencing resource pressure
+    that makes pod termination take longer.
+
+For relevant (not ignored) terminating pods, the controller schedules another reconciliation
+to happen after the remaining grace period elapses.
+
+```go
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  now := r.clock.Now()
+  gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+  totalGracePeriod := gracefulTerminationPeriod + defaultForcefulTerminationGracePeriod
+  if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+    gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+    return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+  }
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and transitioned into the `PodFailed` phase:
```

  personally, I prefer `KueueFailureRecovery` and that's what I added to the KEP

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-11-19T14:22:10Z

- **keps/6757-failure-recovery/README.md:180** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-19T14:22:10Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+  * The controller is enabled via a feature gate.
+* Introduce a `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation that limits which pods are affected by the new controller.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by enabling the feature globally.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Affected Pods
+
+In order to allow the first adopters to control which pods are affected by the new controller, a new `Pod` annotation will be introduced:
+```yaml
+kueue.x-k8s.io/safe-to-forcefully-terminate: "true"
+```
+
+Only pods containing the new annotation will be affected by the controller, for example:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 3
+  completions: 3
+  podReplacementPolicy: Failed
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/safe-to-forcefully-terminate: "true"  // <- new annotation
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "sleep 600" ]
+        resources:
+          requests:
+            cpu: "1"
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+> The controller is not limited to pods managed by Kueue (i.e. having the `kueue.x-k8s.io/managed` or `kueue.x-k8s.io/podset` label). All pods in the cluster that have the new annotation will be affected.
+
+### Grace Period
+
+A default grace period of **1 minute** will be introduced to get initial feedback about the feature.
+This will help inform the decision on the structure of the API and setting defaults makes sense when graduating to beta.
+
+### Implementation Overview
+
+Setting the strategy type to `TerminatePod` will enable a controller, which manages
+the failed nodes.
+
+The controller has to **ignore** updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not annotated with the new `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation.
+1. Are **not** scheduled on a node tainted with `node.kubernetes.io/unreachable`.
+    * This explicitly ignores pods assigned to nodes that still have a running kubelet.
+    For example, nodes with the `node.kubernetes.io/not-ready` taint experiencing resource pressure
+    that makes pod termination take longer.
+
+For relevant (not ignored) terminating pods, the controller schedules another reconciliation
+to happen after the remaining grace period elapses.
+
+```go
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  now := r.clock.Now()
+  gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+  totalGracePeriod := gracefulTerminationPeriod + defaultForcefulTerminationGracePeriod
+  if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+    gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+    return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+  }
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and transitioned into the `PodFailed` phase:
```

  > personally, I prefer KueueFailureRecovery and that's what I added to the KEP
  
  Awesome, thank you!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-11-19T14:23:51Z

- **keps/6757-failure-recovery/README.md:41** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-19T14:23:51Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
```

  > AFAIK the node.kubernetes.io/not-ready is assigned when the kubelet self-reports that it's not ready for running pods, for example due to resource pressure or some other issue (NotReady Ready condition)
  
  Ah, I see. You also assumed the situation under the network connectivity disruptions.
  That makes sense. I am checking a whole step in another thread.
  
  So, we could make this thread as a resolved. thanks.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-11-19T14:28:25Z

- **keps/6757-failure-recovery/README.md:278** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-19T14:28:25Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+  * The controller is enabled via a feature gate.
+* Introduce a `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation that limits which pods are affected by the new controller.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by enabling the feature globally.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Affected Pods
+
+In order to allow the first adopters to control which pods are affected by the new controller, a new `Pod` annotation will be introduced:
+```yaml
+kueue.x-k8s.io/safe-to-forcefully-terminate: "true"
+```
+
+Only pods containing the new annotation will be affected by the controller, for example:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 3
+  completions: 3
+  podReplacementPolicy: Failed
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/safe-to-forcefully-terminate: "true"  // <- new annotation
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "sleep 600" ]
+        resources:
+          requests:
+            cpu: "1"
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+> The controller is not limited to pods managed by Kueue (i.e. having the `kueue.x-k8s.io/managed` or `kueue.x-k8s.io/podset` label). All pods in the cluster that have the new annotation will be affected.
+
+### Grace Period
+
+A default grace period of **1 minute** will be introduced to get initial feedback about the feature.
+This will help inform the decision on the structure of the API and setting defaults makes sense when graduating to beta.
+
+### Implementation Overview
+
+Setting the strategy type to `TerminatePod` will enable a controller, which manages
+the failed nodes.
+
+The controller has to **ignore** updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not annotated with the new `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation.
+1. Are **not** scheduled on a node tainted with `node.kubernetes.io/unreachable`.
+    * This explicitly ignores pods assigned to nodes that still have a running kubelet.
+    For example, nodes with the `node.kubernetes.io/not-ready` taint experiencing resource pressure
+    that makes pod termination take longer.
+
+For relevant (not ignored) terminating pods, the controller schedules another reconciliation
+to happen after the remaining grace period elapses.
+
+```go
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  now := r.clock.Now()
+  gracefulTerminationPeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+  totalGracePeriod := gracefulTerminationPeriod + defaultForcefulTerminationGracePeriod
+  if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+    gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
+    return ctrl.Result{RequeueAfter: gracePeriodLeft}, nil
+  }
+
+  // ...
+}
+```
+
+In that scheduled reconciliation, unless the node recovered or the pod was deleted,
+the pod will be deemed "zombie" and transitioned into the `PodFailed` phase:
+
+```go
+func (r *ZombiePodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+  // ...
+
+  pod.Status.Phase = corev1.PodFailed
+  if err := r.client.Status().Update(ctx, pod); err != nil {
+    return ctrl.Result{}, err
+  }
+
+  return ctrl.Result{}, nil
+}
+```
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+The proposal will be covered with unit tests for:
+1. Configuration parsing.
+1. Controller behavior:
+    1. Whether it ignores irrelevant pods (not terminating, already failed/succeeded, not annotated).
+    1. Whether it correctly schedules a reconciliation for when the grace period elapses.
+    1. Whether it updates the pod's phase to `Failed` after the grace period elapses.
+
+#### Integration Tests
+
+The proposal will be covered with integrations tests that check whether:
+1. Adding a deletion timestamp to the pod requeues a reconciliation loop for when the grace period elapses.
+1. After the grace period elapses, the pod is marked as `Failed`.
+1. Replacement pods are scheduled in the place of the failed pod (optional, technically 1+2 is sufficient to prove this).
+
+Existing integration tests should prove that this feature does not impact Kueue during normal operation.
+
+### Graduation Criteria
+
+#### Alpha
+
+- Feature gate disabled by default.
+- Positive feedback from the users.
+
+### Beta
+
+- Feature gate enabled by default.
+- Re-evaluate the introduction of the `FailureRecoveryPolicy` API.
+
+## Implementation History
+
+2025-09-08: The [issue](https://github.com/kubernetes-sigs/kueue/issues/6757) is raised in Kueue.
+
+2025-09-12: The [issue](https://github.com/kubernetes/kubernetes/issues/134038) is raised in core Kubernetes.
+
+2025-10-17: First draft of the KEP.
+
+## Drawbacks
+
+* The same feature is discussed in core Kubernetes ([kubernetes/issues/134038](https://github.com/kubernetes/kubernetes/issues/134038)), so the underlying issue could potentially be fixed upstream. The timeline of an upstream change is long, but if the feature is deemed not time-critical, it could be fixed at the source instead of in Kueue.
+* This feature introduces a potential footgun to Kueue users and could turn out to be to volatile/risky to use in most affected cases.
+* It introduces yet another responsibility for Kueue - on top of quota management and scheduling, it will also start performing failure recovery.
+
+## Alternatives
```

  SGTM, thank you

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-11-19T14:31:08Z

- **keps/6757-failure-recovery/README.md:380** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-19T14:31:08Z
```diff
@@ -0,0 +1,412 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Observability](#observability)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Leveraging Core Kubernetes Non-Graceful Node Shutdown Handling](#leveraging-core-kubernetes-non-graceful-node-shutdown-handling)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a network partition or a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+The complete flow of pod termination in this case is as follows:
+1. `node-a` stops sending a heartbeat to the control plane.
+1. **After `node-monitor-grace-period` elapses ([50 seconds by default](https://kubernetes.io/docs/reference/node/node-status/#condition))**, the `node.kubernetes.io/unreachable` taint is added to `node-a`.
+1. A toleration ([5 minutes by default](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions)) for the `node.kubernetes.io/unreachable` is added to pods running on `node-a`.
+1. **After the toleration time elapses**, the control plane marks the pods for termination by setting their `deletionTimestamp`
+and `deletionGracePeriodSeconds` ([30 seconds by default](https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#pod-termination-flow)).
+1. **After the deletion grace period elapses**, the `kubelet` should send the `SIGKILL` signal to the pods to terminate them and set their phase accordingly.
+Regardless of whether this happens or not, the control plane will not see this update because of the lack of communication with the `kubelet`.
+1. The pod remains in a `Terminating` state.
+
+The pod can also become "stuck" if the loss of communication occurs **after** the pod was marked for termination:
+
+1. `pod-a` running on `node-a` is marked for termination. Its `deletionTimestamp` and `deletionGracePeriodSeconds` are set.
+1. `node-a` stops sending a heartbeat to the control plane.
+1. **After the deletion grace period elapses**, the `kubelet` should send the `SIGKILL` signal to the pods to terminate them and set their phase accordingly.
+Regardless of whether this happens or not, the control plane will not see this update because of the lack of communication with the `kubelet`.
+1. The pod remains in a `Terminating` state.
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started
+(which is likely in the case of network partitions, where the node is functioning correctly, just unable to communicate with the control plane).
+
+Nevertheless, it became clear that in some cases having a mechanism that would unblock replacement pods from starting would be beneficial.
+In particular, `Job`-based `Workload`s with `podReplacementPolicy: Failed` are unable to make progress when this issue occurs and require manual administration intervention.
+
+### Goals
+
+* Maximize the quota usage for `Workload`s by recovering from a common failure pattern that:
+  * Prevents `Job`s using `podReplacementPolicy: Failed` from making progress.
+
+### Non-Goals
+
+* Detecting whether a zombie pod was terminated properly and released all it's resources.
+
+## Proposal
+
+* Introduce a controller that moves zombie `Pod`s into the `Failed` phase.
+  * The controller is enabled via a feature gate.
+* Introduce a `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation that limits which pods are affected by the new controller.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I'm a user running a distributed Machine Learning workload based on `Job`.
+I set `podReplacementPolicy: Failed` to prevent duplicate task registration errors during node drains or preemptions,
+as my framework expects exactly one pod per worker index.
+
+Some nodes in my cluster often go offline for long periods of time, during which my job is unable to make progress -
+replacement pods will not be scheduled as long as the node remains offline. I'd like for my jobs to be automatically unblocked
+as soon as possible, so I can make best use of the available resources (since the training process is synchronized,
+there is no risk of data corruption in my case).
+
+### Risks and Mitigations
+
+#### Replacing Pods Which Are Still Running
+
+Starting a replacement pod without a guarantee that the original one was properly terminated carries an inherent risk
+and might result in inconsistencies, as discussed above.
+If this risk is not communicated to the user properly and the feature is not sufficiently explicit in its behavior,
+the users might mistake it for a general issue within Kueue/the broader Kubernetes ecosystem.
+
+As this risk stems from the behavior of the `kubelet` and control plane themselves, it cannot be fully mitigated without major changes in how Kubernetes operates.
+Instead, it should be adequately documented to prevent users without a compatible use-case from using it.
+Moreover, enabling this feature should require opt-in both from:
+1. The Administrator - by enabling the feature globally.
+2. The User - by opting specific workloads into the failure recovery mechanism.
+
+Controlling the covered workloads, instead of applying the recovery globally, will guarantee that only pods that were deemed "safe to forcefully terminate" are affected.
+
+## Design Details
+
+### Affected Pods
+
+In order to allow the first adopters to control which pods are affected by the new controller, a new `Pod` annotation will be introduced:
+```yaml
+kueue.x-k8s.io/safe-to-forcefully-terminate: "true"
+```
+
+Only pods containing the new annotation will be affected by the controller, for example:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 3
+  completions: 3
+  podReplacementPolicy: Failed
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/safe-to-forcefully-terminate: "true"  // <- new annotation
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "sleep 600" ]
+        resources:
+          requests:
+            cpu: "1"
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+> The controller is not limited to pods managed by Kueue (i.e. having the `kueue.x-k8s.io/managed` or `kueue.x-k8s.io/podset` label). All pods in the cluster that have the new annotation will be affected.
+
+### Grace Period
+
+When a pod is marked for termination, it's assigned with a `deletionGracePeriodSeconds`.
+This grace period defines the amount of time the pod has to terminate gracefully.
+This KEP defines "pods stuck terminating" as pods that were not terminated **1 minute** after the `deletionGracePeriodSeconds` elapsed.
+The value of 1 minute is chosen to get initial feedback about the feature and help inform the decision on the structure of the API and setting defaults makes sense when graduating to beta.
+
+### Observability
+
+Since this feature relies on interplay with existing Kubernetes mechanism, it should be possible for users to trace
+pod terminations back to the new controller. This will allow for easier troubleshooting and attribution, which is crucial
+when gathering initial user feedback.
+
+To address that, a new `ForcefullyTerminated` `PodCondition` object will be assigned to the pod when it is terminated by the new controller.
+
+```yaml
+apiVersion: v1
+kind: Pod
+spec:
+  # ...
+  terminationGracePeriodSeconds: 30
+status:
+  conditions:
+  # grace period = `terminationGracePeriodSeconds` + 60s (default threshold in the KEP)
+  - message: 'Pod forcefully terminated after 90s grace period due to unreachable node `node-a` (triggered by `kueue.x-k8s.io/safe-to-forcefully-terminate annotation`)'
+    reason: KueueForcefullyTerminated
+    status: "True"
+    type: KueueFailureRecovery
+    # ...
+```
+
+Additionally, an analogous `Warning` event will be emitted for the affected pod:
+
+```yaml
+apiVersion: v1
+kind: Event
+type: Warning
+involvedObject:
+  apiVersion: v1
+  kind: Pod
+  name: pod-a
+  namespace: default
+message: 'Pod forcefully terminated after 90s grace period due to unreachable node `node-a` (triggered by `kueue.x-k8s.io/safe-to-forcefully-terminate annotation`)'
+reason: KueueForcefullyTerminated
+reportingComponent: pod-termination-controller
+source:
+  component: pod-termination-controller
+```
+
+### Implementation Overview
+
+Enabling the `FailureRecoveryPolicy` feature gate will turn on a controller, which manages the failed nodes.
+
+The controller has to **ignore** updates to pods that:
+1. Are not terminating.
+    * `pod.DeletionTimestamp == nil`
+1. Are in a terminal phase.
+    * `pod.Status.Phase != corev1.PodRunning && pod.Status.Phase != corev1.PodPending`
+1. Are not annotated with the new `kueue.x-k8s.io/safe-to-forcefully-terminate` annotation.
+1. Are **not** scheduled on a node tainted with `node.kubernetes.io/unreachable`.
+    * This explicitly ignores pods assigned to nodes that still have a running kubelet.
+    For example, nodes with the `node.kubernetes.io/not-ready` taint experiencing resource pressure
+    that makes pod termination take longer.
+
+For relevant (not ignored) terminating pods, the reconciliation behaves in the following way:
+1. It computes the amount of time elapsed since the the pod's `gracefulTerminationGracePeriod` elapsed:
+    1. If it's below the default timeout of **1 minute**, the reconciler will requeue the object to be re-evaluated once the thershold is reached.
+    1. Otherwise, if the threshold of **1 minute** was reached, the pod will be deemed "zombie" and transitioned into the `PodFailed` phase.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+The proposal will be covered with unit tests for:
+1. Configuration parsing.
+1. Controller behavior:
+    1. Whether it ignores irrelevant pods (not terminating, already failed/succeeded, not annotated).
+    1. Whether it correctly schedules a reconciliation for when the grace period elapses.
+    1. Whether it updates the pod's phase to `Failed` after the grace period elapses.
+
+#### Integration Tests
+
+The proposal will be covered with integrations tests that check whether:
+1. Adding a deletion timestamp to the pod requeues a reconciliation loop for when the grace period elapses.
+1. After the grace period elapses, the pod is marked as `Failed`.
+1. Replacement pods are scheduled in the place of the failed pod (optional, technically 1+2 is sufficient to prove this).
+
+Existing integration tests should prove that this feature does not impact Kueue during normal operation.
+
+### Graduation Criteria
+
+#### Alpha
+
+- Feature gate disabled by default.
+- Positive feedback from the users.
+
+### Beta
+
+- Feature gate enabled by default.
+- Re-evaluate the introduction of the `FailureRecoveryPolicy` API.
+
+## Implementation History
+
+2025-09-08: The [issue](https://github.com/kubernetes-sigs/kueue/issues/6757) is raised in Kueue.
+
+2025-09-12: The [issue](https://github.com/kubernetes/kubernetes/issues/134038) is raised in core Kubernetes.
+
+2025-10-17: First draft of the KEP.
+
+## Drawbacks
+
+* The same feature is discussed in core Kubernetes ([kubernetes/issues/134038](https://github.com/kubernetes/kubernetes/issues/134038)), so the underlying issue could potentially be fixed upstream. The timeline of an upstream change is long, but if the feature is deemed not time-critical, it could be fixed at the source instead of in Kueue.
+* This feature introduces a potential footgun to Kueue users and could turn out to be to volatile/risky to use in most affected cases.
+* It introduces yet another responsibility for Kueue - on top of quota management and scheduling, it will also start performing failure recovery.
+
+## Alternatives
+
+### Do Nothing
+
+Since this feature spawned a lengthy discussion about its riskiness and there are other controllers in the ecosystem which deal with
+node problem remediation (e.g. [medik8s](https://github.com/medik8s/self-node-remediation)), an alternative would be to do nothing,
+wait for the upstream conversations to be resolved and propose an alternative solution (external to Kueue) to the affected users.
+
+The biggest benefit of this approach is that it requires no implementation effort.
+
+**Reasons for discarding/deferring**
+
+1. Forces users to run another system alongside Kueue, making deploying Kueue more complex those cases.
+
+### Control All Pods Managed By Kueue
+
+Instead of limiting the affected pods with the new annotation, the controlled could cover all pods managed by Kueue.
+
+**Reasons for discarding/deferring**
+
+1. It will make the feature harder to test for the early adopters, since they would have to commit all the pods to test
+an alpha feature.
+
+### Introduce a `FailureRecoveryPolicy` API
+
+The `Configuration` struct could be extended to add `FailureRecoveryPolicy`:
+
+```go
+type Configuration struct {
+  // ...
+
+  // FailureRecoveryPolicy is used to enable automatic failure recovery mechanisms.
+  // +optional
+  FailureRecoveryPolicy *FailureRecoveryPolicy `json:"failureRecoveryPolicy,omitempty"`
+}
+
+type FailureRecoveryPolicy struct {
+  // Rules specifies the rules to be enabled for failure recovery.
+  // Exactly one rule can be specified. We keep the API flexible to accommodate
+  // setting more rules in the future.
+  Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+  // Exactly one of the fields below must be specified.
+
+  // TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+  // This strategy recovers stuck pods by forcefully terminating them after a configured
+  // grace period elapses.
+  // Currently specifying the field is required. We keep the API flexible to allow
+  // introducing other rule actions in the future.
+  // +optional
+  TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+  // PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+  // resources not matching the selector are ignored by the controller.
+  PodLabelSelector metav1.LabelSelector `json:"podLabelSelector"`
+
+  // ForcefulTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+  // elapses and when the pod should be forcefully terminated.
+  // Represented using metav1.Duration (e.g. "10m", "1h30m").
+  ForcefulTerminationGracePeriod metav1.Duration `json:"forcefulTerminationGracePeriod"`
+}
+```
+
+This API requires opt-in both from the administrators and user's end, as the workloads would
+have to be labeled in order for them to be covered with failure recovery.
+
+An example failure recovery configuration:
+
+```yaml
+failureRecoveryPolicy:
+  rules:
+  - terminatePod:
+      podLabelSelector:
+        matchExpressions:
+        - key: example.com/pod-safe-to-fail
+          operator: In
+          values: [ "true" ]
+      forcefulTerminationGracePeriod: 5m
+```
+
+With the new API, the proposal is to **not** set a default value for the forceful termination grace period.
+This puts configuration over convention, as it will make the user consciously think about the
+grace period the need for their specific use case, without risking a faulty default.
+
+Since enabling the feature is inherently risky, this serves as an additional explicit
+acknowledgement of this risk from the user's end.
+
+Alternatively, a default value can be inferred from the `terminationGracePeriodSeconds`.
+For example, it could be a multiple of that value. Nevertheless, this still runs the risk of
+setting a very small value or even 0, depending on the user's configuration.
+
+**Reasons for discarding/deferring**
+
+1. `FailureRecoveryPolicy` would have to be added to the **beta** `Configuration` API.
+To avoid committing to a specific structure (and the feature in general), it is more prudent to gather initial feedback
+with a feature gate first, then decide how to best represent it in the API.
+1. As mentioned in the [Do Nothing](#do-nothing) alternative, the issue this KEP is trying to mitigate might be solved at the core Kubernetes level. If so, deprecating an annotation (proposed in the KEP) is simpler than deprecating an API.
+
+### Leveraging Core Kubernetes Non-Graceful Node Shutdown Handling
```

  Thank you for adding this. Let's consider those alternatives during Beta graduation, as we mentioned in this proposal.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-11-19T14:35:06Z

- **keps/6757-failure-recovery/README.md:48** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-19T14:35:06Z
```diff
@@ -0,0 +1,379 @@
+# KEP-6757: Failure Recovery
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Replacing Pods Which Are Still Running](#replacing-pods-which-are-still-running)
+- [Design Details](#design-details)
+  - [Affected Pods](#affected-pods)
+  - [Grace Period](#grace-period)
+  - [Implementation Overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+  - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Do Nothing](#do-nothing)
+  - [Control All Pods Managed By Kueue](#control-all-pods-managed-by-kueue)
+  - [Introduce a <code>FailureRecoveryPolicy</code> API](#introduce-a-failurerecoverypolicy-api)
+  - [Managing The <code>node.kubernetes.io/out-of-service</code> Taint On <code>Node</code>](#managing-the-nodekubernetesioout-of-service-taint-on-node)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces an opt-in mechanism of timeout-based graceful handling of zombie pods,
+i.e. pods that are stuck in the `Pending`/`Running` state due to a node-level failure.
+
+## Motivation
+
+Currently, a malfunction of the `kubelet` (or a more general node failure) results in the pods running on that node to become stuck.
+When the `kubelet` fails to send its regular heartbeat to the control plane within `node-monitor-grace-period`, the node is deemed unhealthy and is assigned the `node.kubernetes.io/unreachable` taint. By default, Kubernetes [automatically adds a 5-minute toleration](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions) for pods running in the affected node (this can be explicitly set by a different value by the user/controller).
+If the heartbeat is resumed within this toleration period, the control plane removes the taints and the pods continue running.
+On the other hand, if it is not resumed, then the pods are marked for termination (i.e. their `deletionTimestamp` is set in `etcd`).
+
+Without a functioning `kubelet`, the pods have no way of progressing beyond that point on their own.
+Such pods, colloquially called "zombie pods", remain terminating until the node is healed or they are manually removed.
+This is a crucial safety measure, as in that scenario the control plane has no way to confirm that the pods actually stopped
+and released all their resources. It is especially relevant for stateful applications and might result in data corruption.
+For example, the stuck pod might still be writing to a `PersistentVolume` when a replacement is started.
```

  The new flow looks comprehensive and great, thanks.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-11-19T14:38:02Z

LGTM 👍 
Thank you!

/lgtm
/approve
