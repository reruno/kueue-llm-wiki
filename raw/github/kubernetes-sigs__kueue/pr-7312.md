# PR #7312: Implement the `PodTermination` controller to gracefully handle "stuck" pods

**Summary**: Implement the `PodTermination` controller to gracefully handle "stuck" pods

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7312

**Last updated**: 2025-11-28T08:42:05Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kshalot](https://github.com/kshalot)
- **Branch**: `6757-gracefully-handle-zombie-pods` → `main`
- **Created**: 2025-10-17T13:19:42Z
- **Updated**: 2025-11-28T08:42:05Z
- **Merged**: 2025-11-21T08:16:34Z
- **Closed**: 2025-11-21T08:16:34Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XL`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92)
- **Changed files**: 10   **+796 / -6**

## Description

#### What type of PR is this?
/kind feature

#### What this PR does / why we need it:
Implements the `PodTermination` action controller described in #7311.

#### Which issue(s) this PR fixes:
Fixes #6757

#### Special notes for your reviewer:
~~This is a proof-of-concept, very rough sketch of the implementation that I used for testing. Leaving it here, as I'm going on vacation, in case this work needs to be urgently picked up for some reason. If not, it will be continued in 2 weeks.~~

#### Does this PR introduce a user-facing change?
```release-note
FailureRecovery: Introduce a mechanism to terminate Pods "stuck" in a terminating state due to node failures.
The feature is activated by enabling the alpha FailureRecoveryPolicy feature gate (disabled by default).
Only Pods with the kueue.x-k8s.io/safe-to-forcefully-terminate annotation are handled by the mechanism.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-10-17T13:19:50Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | d15bb80c4131b3f2c7b72a8a5d2bbd39ef9d0619 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/691f446ac33d9d000890ee8e |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-17T13:19:53Z

Hi @kshalot. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-17T13:21:40Z

/ok-to-test

### Comment by [@kshalot](https://github.com/kshalot) — 2025-11-13T10:50:04Z

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-19T15:05:51Z

@kshalot we need to update the release note, proposal:
/release-note-edit
```release-note
Introduce a mechanism to terminate Pods "stuck" in a terminating state due to node failures.
The feature is activated by enabling the alpha FailureRecoveryPolicy feature gate (disabled by default).
Only Pods with the kueue.x-k8s.io/safe-to-forcefully-terminate annotation are handled by the mechanism.
```
We will also need to follow up with documentation update.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-21T07:48:41Z

/lgtm
/approve
Thank you 👍 I think the remaining comments aren't really blocking for Alpha so merging to avoid conflicts with other PRs. I will still approve the follow ups before the release.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-21T07:48:48Z

LGTM label has been added.  <details>Git tree hash: 67abe4973c968439bea409980a53bde254d290fe</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-21T07:48:50Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7312#" title="Author self-approved">kshalot</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7312#issuecomment-3561840052" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7312#pullrequestreview-3459411576" title="Approved">olekzabl</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-21T08:04:11Z

https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7312/pull-kueue-test-unit-main/1991776223881072640

is not related I think

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-21T08:04:30Z

/test pull-kueue-test-unit-main

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-28T08:42:03Z

/release-note-edit
```release-note
FailureRecovery: Introduce a mechanism to terminate Pods "stuck" in a terminating state due to node failures.
The feature is activated by enabling the alpha FailureRecoveryPolicy feature gate (disabled by default).
Only Pods with the kueue.x-k8s.io/safe-to-forcefully-terminate annotation are handled by the mechanism.
```

## Reviews

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-11-12T10:40:16Z

- **apis/config/v1beta1/configuration_types.go:619** by [@olekzabl](https://github.com/olekzabl) — 2025-11-12T09:44:16Z
```diff
@@ -588,3 +592,29 @@ type WorkloadRetentionPolicy struct {
 	// +optional
 	AfterDeactivatedByKueue *metav1.Duration `json:"afterDeactivatedByKueue,omitempty"`
 }
+
+type FailureRecoveryPolicy struct {
+	// Rules specifies the rules to be enabled for failure recovery.
+	Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+	// Exactly one of the fields below must be specified.
+
+	// TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+	// This strategy recovers stuck pods by forcefully terminating them after a configured
+	// grace period elapses.
+	// +optional
+	TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+	// PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+	// resources not matching the selector are ignored by the controller.
+	PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+
+	// ForcefulTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+	// elapses and when the pod should be forcefully deleted.
+	// Represented using metav1.Duration (e.g. "10m", "1h30m").
+	ForcefulTerminationGracePeriod *metav1.Duration `json:"forcefulTerminationGracePeriod"`
```

  Quoting your KEP:
  
  _The proposal is to **not** set a default value for the forceful termination grace period. This puts configuration over convention, as it will make the user consciously think about the grace period the need for their specific use case, without risking a faulty default._
  
  This sounds like there should be some validation here against this being `nil`?
  (maybe also against 0 seconds?)
  
  BTW are there reasons for this to be a pointer? (non-pointer `Duration`s do exist in `_types.go`)

- **apis/config/v1beta1/configuration_types.go:598** by [@olekzabl](https://github.com/olekzabl) — 2025-11-12T09:49:01Z
```diff
@@ -588,3 +592,29 @@ type WorkloadRetentionPolicy struct {
 	// +optional
 	AfterDeactivatedByKueue *metav1.Duration `json:"afterDeactivatedByKueue,omitempty"`
 }
+
+type FailureRecoveryPolicy struct {
+	// Rules specifies the rules to be enabled for failure recovery.
+	Rules []FailureRecoveryRule `json:"rules"`
```

  How about `+kubebuilder:validation:MinItems=1`?
  (I guess no rules would make no sense?)

- **apis/config/v1beta1/configuration_types.go:602** by [@olekzabl](https://github.com/olekzabl) — 2025-11-12T10:14:36Z
```diff
@@ -588,3 +592,29 @@ type WorkloadRetentionPolicy struct {
 	// +optional
 	AfterDeactivatedByKueue *metav1.Duration `json:"afterDeactivatedByKueue,omitempty"`
 }
+
+type FailureRecoveryPolicy struct {
+	// Rules specifies the rules to be enabled for failure recovery.
+	Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+	// Exactly one of the fields below must be specified.
```

  This can be enforced with Kubebuilder annotations.
  (And then, I guess, you wouldn't need the corresponding code in `validation.go`).
  
  For now, perhaps just by just replacing `+optional` with `+required`?
  In the future (with more rule types), you could do some `+kubebuilder:validation:XValidation`, employing `has(self.X)` and potentially also `exists_one()`.

- **pkg/controller/core/core.go:74** by [@olekzabl](https://github.com/olekzabl) — 2025-11-12T10:17:12Z
```diff
@@ -63,6 +64,23 @@ func SetupControllers(mgr ctrl.Manager, qManager *qcache.Manager, cc *schdcache.
 		watchers = append(watchers, cohortRec)
 	}
 
+	if cfg.FailureRecoveryPolicy != nil {
+		terminationConfigs := make([]configapi.TerminatePodConfig, 0, len(cfg.FailureRecoveryPolicy.Rules))
+		for _, rule := range cfg.FailureRecoveryPolicy.Rules {
+			if rule.TerminatePod != nil {
+				terminationConfigs = append(terminationConfigs, *rule.TerminatePod)
+			}
+		}
+		zpRec, err := failurerecovery.NewTerminatingPodReconciler(mgr.GetClient(), terminationConfigs)
```

  `z` for `zombie`? :)
  I'd use `tpRec` as less puzzling.

- **pkg/controller/failurerecovery/pod_termination_controller.go:50** by [@olekzabl](https://github.com/olekzabl) — 2025-11-12T10:24:30Z
```diff
@@ -0,0 +1,168 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	utilnode "sigs.k8s.io/kueue/pkg/util/node"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=update;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var (
+	realClock = clock.RealClock{}
+)
+
+type TerminatingPodReconciler struct {
+	client          client.Client
+	terminationCfgs []*terminatePodConfigWithSelector
+	clock           clock.Clock
+}
+
+type terminatePodConfigWithSelector struct {
```

  Style nit: I'd rather name it `terminatePodConfigInternal` and let it contain just the `Selector` and the `Duration`.
  Then, for me, it'd be more clearly just an internal representation of a `TerminatePodConfig`.
  Right now, this contains the same label selector in 2 different formats, and the name doesn't help realizing how they relate.

- **pkg/controller/failurerecovery/pod_termination_controller.go:109** by [@olekzabl](https://github.com/olekzabl) — 2025-11-12T10:28:02Z
```diff
@@ -0,0 +1,168 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	utilnode "sigs.k8s.io/kueue/pkg/util/node"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=update;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var (
+	realClock = clock.RealClock{}
+)
+
+type TerminatingPodReconciler struct {
+	client          client.Client
+	terminationCfgs []*terminatePodConfigWithSelector
+	clock           clock.Clock
+}
+
+type terminatePodConfigWithSelector struct {
+	selector       labels.Selector
+	terminationCfg configapi.TerminatePodConfig
+}
+
+type TerminatingPodReconcilerOptions struct {
+	clock clock.Clock
+}
+
+type TerminatingPodReconcilerOption func(*TerminatingPodReconcilerOptions)
+
+func WithClock(c clock.Clock) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+var defaultOptions = TerminatingPodReconcilerOptions{
+	clock: realClock,
+}
+
+func NewTerminatingPodReconciler(
+	client client.Client,
+	cfgs []configapi.TerminatePodConfig,
+	opts ...TerminatingPodReconcilerOption,
+) (*TerminatingPodReconciler, error) {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	terminationCfgs := make([]*terminatePodConfigWithSelector, len(cfgs))
+	for i, cfg := range cfgs {
+		selector, err := metav1.LabelSelectorAsSelector(cfg.PodLabelSelector)
+		if err != nil {
+			return nil, err
+		}
+
+		terminationCfgs[i] = &terminatePodConfigWithSelector{
+			terminationCfg: cfg,
+			selector:       selector,
+		}
+	}
+
+	return &TerminatingPodReconciler{
+		client:          client,
+		terminationCfgs: terminationCfgs,
+		clock:           options.clock,
+	}, nil
+}
+
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	pod := &corev1.Pod{}
+	if err := r.client.Get(ctx, req.NamespacedName, pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	terminationCfg := firstMatchingTerminationConfig(r.terminationCfgs, pod)
+	// Pod does not match any of the configured pod label selector
+	if terminationCfg == nil {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod was not marked for termination
+	if pod.DeletionTimestamp.IsZero() {
```

  In the KEP, you also planned to ignore Pods with `pod.DeletionGracePeriodSeconds == nil`.

- **pkg/controller/failurerecovery/pod_termination_controller.go:107** by [@olekzabl](https://github.com/olekzabl) — 2025-11-12T10:29:57Z
```diff
@@ -0,0 +1,168 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	utilnode "sigs.k8s.io/kueue/pkg/util/node"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=update;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var (
+	realClock = clock.RealClock{}
+)
+
+type TerminatingPodReconciler struct {
+	client          client.Client
+	terminationCfgs []*terminatePodConfigWithSelector
+	clock           clock.Clock
+}
+
+type terminatePodConfigWithSelector struct {
+	selector       labels.Selector
+	terminationCfg configapi.TerminatePodConfig
+}
+
+type TerminatingPodReconcilerOptions struct {
+	clock clock.Clock
+}
+
+type TerminatingPodReconcilerOption func(*TerminatingPodReconcilerOptions)
+
+func WithClock(c clock.Clock) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+var defaultOptions = TerminatingPodReconcilerOptions{
+	clock: realClock,
+}
+
+func NewTerminatingPodReconciler(
+	client client.Client,
+	cfgs []configapi.TerminatePodConfig,
+	opts ...TerminatingPodReconcilerOption,
+) (*TerminatingPodReconciler, error) {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	terminationCfgs := make([]*terminatePodConfigWithSelector, len(cfgs))
+	for i, cfg := range cfgs {
+		selector, err := metav1.LabelSelectorAsSelector(cfg.PodLabelSelector)
+		if err != nil {
+			return nil, err
+		}
+
+		terminationCfgs[i] = &terminatePodConfigWithSelector{
+			terminationCfg: cfg,
+			selector:       selector,
+		}
+	}
+
+	return &TerminatingPodReconciler{
+		client:          client,
+		terminationCfgs: terminationCfgs,
+		clock:           options.clock,
+	}, nil
+}
+
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	pod := &corev1.Pod{}
+	if err := r.client.Get(ctx, req.NamespacedName, pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	terminationCfg := firstMatchingTerminationConfig(r.terminationCfgs, pod)
```

  So we just look at the first matching rule? How obvious is that?
  
  Alternatively, a user could expect that, if multiple selectors match, we take the least / most conservative action among all matches. (IDK if that'd make sense in general, though for now it'd boil down to a competition among durations, which feels well-defined).
  
  Is it worth mentioning in the KEP? In the comments in `types.go`?

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-12T11:15:25Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:107** by [@kshalot](https://github.com/kshalot) — 2025-11-12T11:15:25Z
```diff
@@ -0,0 +1,168 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	utilnode "sigs.k8s.io/kueue/pkg/util/node"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=update;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var (
+	realClock = clock.RealClock{}
+)
+
+type TerminatingPodReconciler struct {
+	client          client.Client
+	terminationCfgs []*terminatePodConfigWithSelector
+	clock           clock.Clock
+}
+
+type terminatePodConfigWithSelector struct {
+	selector       labels.Selector
+	terminationCfg configapi.TerminatePodConfig
+}
+
+type TerminatingPodReconcilerOptions struct {
+	clock clock.Clock
+}
+
+type TerminatingPodReconcilerOption func(*TerminatingPodReconcilerOptions)
+
+func WithClock(c clock.Clock) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+var defaultOptions = TerminatingPodReconcilerOptions{
+	clock: realClock,
+}
+
+func NewTerminatingPodReconciler(
+	client client.Client,
+	cfgs []configapi.TerminatePodConfig,
+	opts ...TerminatingPodReconcilerOption,
+) (*TerminatingPodReconciler, error) {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	terminationCfgs := make([]*terminatePodConfigWithSelector, len(cfgs))
+	for i, cfg := range cfgs {
+		selector, err := metav1.LabelSelectorAsSelector(cfg.PodLabelSelector)
+		if err != nil {
+			return nil, err
+		}
+
+		terminationCfgs[i] = &terminatePodConfigWithSelector{
+			terminationCfg: cfg,
+			selector:       selector,
+		}
+	}
+
+	return &TerminatingPodReconciler{
+		client:          client,
+		terminationCfgs: terminationCfgs,
+		clock:           options.clock,
+	}, nil
+}
+
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	pod := &corev1.Pod{}
+	if err := r.client.Get(ctx, req.NamespacedName, pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	terminationCfg := firstMatchingTerminationConfig(r.terminationCfgs, pod)
```

  Yes, I took the first one here to get something off the ground. I'd say the best way to go about this for now would be to just use the minimal matching grace period, i.e. the "least" conservative action. I think this behavior would be the most intuitive, since it would behave as if all the configured actions were ran (for now at least, since the only configurable part of an action is the grace period).

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-12T12:50:00Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:107** by [@kshalot](https://github.com/kshalot) — 2025-11-12T12:49:59Z
```diff
@@ -0,0 +1,168 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	utilnode "sigs.k8s.io/kueue/pkg/util/node"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=update;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var (
+	realClock = clock.RealClock{}
+)
+
+type TerminatingPodReconciler struct {
+	client          client.Client
+	terminationCfgs []*terminatePodConfigWithSelector
+	clock           clock.Clock
+}
+
+type terminatePodConfigWithSelector struct {
+	selector       labels.Selector
+	terminationCfg configapi.TerminatePodConfig
+}
+
+type TerminatingPodReconcilerOptions struct {
+	clock clock.Clock
+}
+
+type TerminatingPodReconcilerOption func(*TerminatingPodReconcilerOptions)
+
+func WithClock(c clock.Clock) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+var defaultOptions = TerminatingPodReconcilerOptions{
+	clock: realClock,
+}
+
+func NewTerminatingPodReconciler(
+	client client.Client,
+	cfgs []configapi.TerminatePodConfig,
+	opts ...TerminatingPodReconcilerOption,
+) (*TerminatingPodReconciler, error) {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	terminationCfgs := make([]*terminatePodConfigWithSelector, len(cfgs))
+	for i, cfg := range cfgs {
+		selector, err := metav1.LabelSelectorAsSelector(cfg.PodLabelSelector)
+		if err != nil {
+			return nil, err
+		}
+
+		terminationCfgs[i] = &terminatePodConfigWithSelector{
+			terminationCfg: cfg,
+			selector:       selector,
+		}
+	}
+
+	return &TerminatingPodReconciler{
+		client:          client,
+		terminationCfgs: terminationCfgs,
+		clock:           options.clock,
+	}, nil
+}
+
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	pod := &corev1.Pod{}
+	if err := r.client.Get(ctx, req.NamespacedName, pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	terminationCfg := firstMatchingTerminationConfig(r.terminationCfgs, pod)
```

  Added to the KEP: https://github.com/kubernetes-sigs/kueue/pull/7311/commits/af9c65e9e5b5e452c101a0f47029f4d8ecc5550a

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-12T12:51:09Z

- **pkg/controller/core/core.go:74** by [@kshalot](https://github.com/kshalot) — 2025-11-12T12:51:09Z
```diff
@@ -63,6 +64,23 @@ func SetupControllers(mgr ctrl.Manager, qManager *qcache.Manager, cc *schdcache.
 		watchers = append(watchers, cohortRec)
 	}
 
+	if cfg.FailureRecoveryPolicy != nil {
+		terminationConfigs := make([]configapi.TerminatePodConfig, 0, len(cfg.FailureRecoveryPolicy.Rules))
+		for _, rule := range cfg.FailureRecoveryPolicy.Rules {
+			if rule.TerminatePod != nil {
+				terminationConfigs = append(terminationConfigs, *rule.TerminatePod)
+			}
+		}
+		zpRec, err := failurerecovery.NewTerminatingPodReconciler(mgr.GetClient(), terminationConfigs)
```

  Leftover after renaming. Good catch.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-12T13:42:52Z

- **apis/config/v1beta1/configuration_types.go:602** by [@kshalot](https://github.com/kshalot) — 2025-11-12T13:42:52Z
```diff
@@ -588,3 +592,29 @@ type WorkloadRetentionPolicy struct {
 	// +optional
 	AfterDeactivatedByKueue *metav1.Duration `json:"afterDeactivatedByKueue,omitempty"`
 }
+
+type FailureRecoveryPolicy struct {
+	// Rules specifies the rules to be enabled for failure recovery.
+	Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+	// Exactly one of the fields below must be specified.
```

  Yes, I made the kubebuilder validations stricter.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-12T13:43:18Z

- **apis/config/v1beta1/configuration_types.go:602** by [@kshalot](https://github.com/kshalot) — 2025-11-12T13:43:18Z
```diff
@@ -588,3 +592,29 @@ type WorkloadRetentionPolicy struct {
 	// +optional
 	AfterDeactivatedByKueue *metav1.Duration `json:"afterDeactivatedByKueue,omitempty"`
 }
+
+type FailureRecoveryPolicy struct {
+	// Rules specifies the rules to be enabled for failure recovery.
+	Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+	// Exactly one of the fields below must be specified.
```

  I preferred to use the `ExactlyOneOf` validation immediately rather than using `required`. It serves as additional documentation.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-12T13:56:56Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:50** by [@kshalot](https://github.com/kshalot) — 2025-11-12T13:56:55Z
```diff
@@ -0,0 +1,168 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	utilnode "sigs.k8s.io/kueue/pkg/util/node"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=update;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var (
+	realClock = clock.RealClock{}
+)
+
+type TerminatingPodReconciler struct {
+	client          client.Client
+	terminationCfgs []*terminatePodConfigWithSelector
+	clock           clock.Clock
+}
+
+type terminatePodConfigWithSelector struct {
```

  Agreed.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-12T14:14:52Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:107** by [@kshalot](https://github.com/kshalot) — 2025-11-12T14:14:52Z
```diff
@@ -0,0 +1,168 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	utilnode "sigs.k8s.io/kueue/pkg/util/node"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=update;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var (
+	realClock = clock.RealClock{}
+)
+
+type TerminatingPodReconciler struct {
+	client          client.Client
+	terminationCfgs []*terminatePodConfigWithSelector
+	clock           clock.Clock
+}
+
+type terminatePodConfigWithSelector struct {
+	selector       labels.Selector
+	terminationCfg configapi.TerminatePodConfig
+}
+
+type TerminatingPodReconcilerOptions struct {
+	clock clock.Clock
+}
+
+type TerminatingPodReconcilerOption func(*TerminatingPodReconcilerOptions)
+
+func WithClock(c clock.Clock) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+var defaultOptions = TerminatingPodReconcilerOptions{
+	clock: realClock,
+}
+
+func NewTerminatingPodReconciler(
+	client client.Client,
+	cfgs []configapi.TerminatePodConfig,
+	opts ...TerminatingPodReconcilerOption,
+) (*TerminatingPodReconciler, error) {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	terminationCfgs := make([]*terminatePodConfigWithSelector, len(cfgs))
+	for i, cfg := range cfgs {
+		selector, err := metav1.LabelSelectorAsSelector(cfg.PodLabelSelector)
+		if err != nil {
+			return nil, err
+		}
+
+		terminationCfgs[i] = &terminatePodConfigWithSelector{
+			terminationCfg: cfg,
+			selector:       selector,
+		}
+	}
+
+	return &TerminatingPodReconciler{
+		client:          client,
+		terminationCfgs: terminationCfgs,
+		clock:           options.clock,
+	}, nil
+}
+
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	pod := &corev1.Pod{}
+	if err := r.client.Get(ctx, req.NamespacedName, pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	terminationCfg := firstMatchingTerminationConfig(r.terminationCfgs, pod)
```

  Implemented, mentioned in `_types.go`.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-12T14:26:13Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:109** by [@kshalot](https://github.com/kshalot) — 2025-11-12T14:26:13Z
```diff
@@ -0,0 +1,168 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	utilnode "sigs.k8s.io/kueue/pkg/util/node"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=update;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var (
+	realClock = clock.RealClock{}
+)
+
+type TerminatingPodReconciler struct {
+	client          client.Client
+	terminationCfgs []*terminatePodConfigWithSelector
+	clock           clock.Clock
+}
+
+type terminatePodConfigWithSelector struct {
+	selector       labels.Selector
+	terminationCfg configapi.TerminatePodConfig
+}
+
+type TerminatingPodReconcilerOptions struct {
+	clock clock.Clock
+}
+
+type TerminatingPodReconcilerOption func(*TerminatingPodReconcilerOptions)
+
+func WithClock(c clock.Clock) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+var defaultOptions = TerminatingPodReconcilerOptions{
+	clock: realClock,
+}
+
+func NewTerminatingPodReconciler(
+	client client.Client,
+	cfgs []configapi.TerminatePodConfig,
+	opts ...TerminatingPodReconcilerOption,
+) (*TerminatingPodReconciler, error) {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	terminationCfgs := make([]*terminatePodConfigWithSelector, len(cfgs))
+	for i, cfg := range cfgs {
+		selector, err := metav1.LabelSelectorAsSelector(cfg.PodLabelSelector)
+		if err != nil {
+			return nil, err
+		}
+
+		terminationCfgs[i] = &terminatePodConfigWithSelector{
+			terminationCfg: cfg,
+			selector:       selector,
+		}
+	}
+
+	return &TerminatingPodReconciler{
+		client:          client,
+		terminationCfgs: terminationCfgs,
+		clock:           options.clock,
+	}, nil
+}
+
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	pod := &corev1.Pod{}
+	if err := r.client.Get(ctx, req.NamespacedName, pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	terminationCfg := firstMatchingTerminationConfig(r.terminationCfgs, pod)
+	// Pod does not match any of the configured pod label selector
+	if terminationCfg == nil {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod was not marked for termination
+	if pod.DeletionTimestamp.IsZero() {
```

  I think it doesn't really matter whether this is checked or not, since AFAIK K8s always set this to a non-nil value for pods that are terminating. Since it has to be dereferenced later anyway, I went for `ptr.Deref(pod.DeletionGracePeriodSeconds, 0)` just to be on the safe side. We could do both.
  
  WDYT?

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-12T14:27:35Z

- **apis/config/v1beta1/configuration_types.go:619** by [@kshalot](https://github.com/kshalot) — 2025-11-12T14:27:34Z
```diff
@@ -588,3 +592,29 @@ type WorkloadRetentionPolicy struct {
 	// +optional
 	AfterDeactivatedByKueue *metav1.Duration `json:"afterDeactivatedByKueue,omitempty"`
 }
+
+type FailureRecoveryPolicy struct {
+	// Rules specifies the rules to be enabled for failure recovery.
+	Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+	// Exactly one of the fields below must be specified.
+
+	// TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+	// This strategy recovers stuck pods by forcefully terminating them after a configured
+	// grace period elapses.
+	// +optional
+	TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+	// PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+	// resources not matching the selector are ignored by the controller.
+	PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+
+	// ForcefulTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+	// elapses and when the pod should be forcefully deleted.
+	// Represented using metav1.Duration (e.g. "10m", "1h30m").
+	ForcefulTerminationGracePeriod *metav1.Duration `json:"forcefulTerminationGracePeriod"`
```

  I was worried about the value defaulting to a zero-value (which would be `0` for duration I think). But after a second thought, I think adding `+required` would address this issue.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-11-12T23:33:20Z

- **apis/config/v1beta1/configuration_types.go:602** by [@olekzabl](https://github.com/olekzabl) — 2025-11-12T23:33:20Z
```diff
@@ -588,3 +592,29 @@ type WorkloadRetentionPolicy struct {
 	// +optional
 	AfterDeactivatedByKueue *metav1.Duration `json:"afterDeactivatedByKueue,omitempty"`
 }
+
+type FailureRecoveryPolicy struct {
+	// Rules specifies the rules to be enabled for failure recovery.
+	Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+	// Exactly one of the fields below must be specified.
```

  So nice - I didn't know it exists.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-13T10:47:01Z

- **apis/config/v1beta1/configuration_types.go:602** by [@kshalot](https://github.com/kshalot) — 2025-11-13T10:47:00Z
```diff
@@ -588,3 +592,32 @@ type WorkloadRetentionPolicy struct {
 	// +optional
 	AfterDeactivatedByKueue *metav1.Duration `json:"afterDeactivatedByKueue,omitempty"`
 }
+
+type FailureRecoveryPolicy struct {
+	// Rules specifies the rules to be enabled for failure recovery.
+	// +kubebuilder:validation:MinItems=1
+	Rules []FailureRecoveryRule `json:"rules"`
+}
+
+// +kubebuilder:validation:ExactlyOneOf=["terminatePod"]
```

  Okay, from what I see now, we can't really use the kubebuilder validations for `Configuration`, since it's not being created via the API - it's being loaded by the manager from a configmap, which seems to bypass the validation.
  
  CC @olekzabl

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-13T10:57:32Z

- **apis/config/v1beta2/configuration_types.go:574** by [@kshalot](https://github.com/kshalot) — 2025-11-13T10:57:32Z
```diff
@@ -560,3 +564,32 @@ type WorkloadRetentionPolicy struct {
 	// +optional
 	AfterDeactivatedByKueue *metav1.Duration `json:"afterDeactivatedByKueue,omitempty"`
 }
+
+type FailureRecoveryPolicy struct {
+	// Rules specifies the rules to be enabled for failure recovery.
+	// +kubebuilder:validation:MinItems=1
+	Rules []FailureRecoveryRule `json:"rules"`
+}
+
+// +kubebuilder:validation:ExactlyOneOf=terminatePod
```

  I think the kubebuilder validations won't cut it after all, since the `Configuration` object is loaded as a YAML via a configmap.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-13T11:11:58Z

- **pkg/controller/jobs/pod/pod_controller_test.go:44** by [@mimowo](https://github.com/mimowo) — 2025-11-13T11:11:58Z
```diff
@@ -41,6 +41,7 @@ import (
 
 	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
```

  nit, the changes in aliases are ok, but let's don't distract the readers with them. Revert from this PR, but feel free to open a dedicated cleanup PR.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-11-13T11:48:46Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:109** by [@olekzabl](https://github.com/olekzabl) — 2025-11-13T11:48:46Z
```diff
@@ -0,0 +1,168 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	utilnode "sigs.k8s.io/kueue/pkg/util/node"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=update;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var (
+	realClock = clock.RealClock{}
+)
+
+type TerminatingPodReconciler struct {
+	client          client.Client
+	terminationCfgs []*terminatePodConfigWithSelector
+	clock           clock.Clock
+}
+
+type terminatePodConfigWithSelector struct {
+	selector       labels.Selector
+	terminationCfg configapi.TerminatePodConfig
+}
+
+type TerminatingPodReconcilerOptions struct {
+	clock clock.Clock
+}
+
+type TerminatingPodReconcilerOption func(*TerminatingPodReconcilerOptions)
+
+func WithClock(c clock.Clock) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+var defaultOptions = TerminatingPodReconcilerOptions{
+	clock: realClock,
+}
+
+func NewTerminatingPodReconciler(
+	client client.Client,
+	cfgs []configapi.TerminatePodConfig,
+	opts ...TerminatingPodReconcilerOption,
+) (*TerminatingPodReconciler, error) {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	terminationCfgs := make([]*terminatePodConfigWithSelector, len(cfgs))
+	for i, cfg := range cfgs {
+		selector, err := metav1.LabelSelectorAsSelector(cfg.PodLabelSelector)
+		if err != nil {
+			return nil, err
+		}
+
+		terminationCfgs[i] = &terminatePodConfigWithSelector{
+			terminationCfg: cfg,
+			selector:       selector,
+		}
+	}
+
+	return &TerminatingPodReconciler{
+		client:          client,
+		terminationCfgs: terminationCfgs,
+		clock:           options.clock,
+	}, nil
+}
+
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	pod := &corev1.Pod{}
+	if err := r.client.Get(ctx, req.NamespacedName, pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	terminationCfg := firstMatchingTerminationConfig(r.terminationCfgs, pod)
+	// Pod does not match any of the configured pod label selector
+	if terminationCfg == nil {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod was not marked for termination
+	if pod.DeletionTimestamp.IsZero() {
```

  I'm fine with the code as it is; I was just alerted by inconsistency with the KEP.
  
  Maybe just update the KEP accordingly?

### Review by [@olekzabl](https://github.com/olekzabl) (approved) — 2025-11-13T11:49:19Z

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-13T12:14:31Z

- **pkg/controller/jobs/pod/pod_controller_test.go:44** by [@kshalot](https://github.com/kshalot) — 2025-11-13T12:14:31Z
```diff
@@ -41,6 +41,7 @@ import (
 
 	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
```

  Done (https://github.com/kshalot/kueue/pull/1)
  
  I'll move forward with that PR after this in merged.
  
  Note: I kept the addition of `PodSetLabel` to `constants` in this PR so the new logic is migrated immediately. It temporarily "duplicates" the label but OTOH it avoids depending on the `controller` package (not to mention it was easier to extract this way). I can fully move back to `controllerconsts` if you prefer though.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-13T12:45:55Z

- **pkg/controller/jobs/pod/pod_controller_test.go:44** by [@mimowo](https://github.com/mimowo) — 2025-11-13T12:45:55Z
```diff
@@ -41,6 +41,7 @@ import (
 
 	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
```

  The preparatory PR is a good idea, but this particular one includes changes from this PR too
  
  When stripped from logic changes I can approve alias changes quickly in the prep PR.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-13T13:24:55Z

- **pkg/controller/jobs/pod/pod_controller_test.go:44** by [@kshalot](https://github.com/kshalot) — 2025-11-13T13:24:55Z
```diff
@@ -41,6 +41,7 @@ import (
 
 	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
```

  You are right. I instinctively did that because I thought there would be some co-dependency.
  
  Here's an independent PR in the upstream Kueue repo: https://github.com/kubernetes-sigs/kueue/pull/7644

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-13T14:43:03Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:109** by [@kshalot](https://github.com/kshalot) — 2025-11-13T14:43:03Z
```diff
@@ -0,0 +1,168 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	utilnode "sigs.k8s.io/kueue/pkg/util/node"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=update;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var (
+	realClock = clock.RealClock{}
+)
+
+type TerminatingPodReconciler struct {
+	client          client.Client
+	terminationCfgs []*terminatePodConfigWithSelector
+	clock           clock.Clock
+}
+
+type terminatePodConfigWithSelector struct {
+	selector       labels.Selector
+	terminationCfg configapi.TerminatePodConfig
+}
+
+type TerminatingPodReconcilerOptions struct {
+	clock clock.Clock
+}
+
+type TerminatingPodReconcilerOption func(*TerminatingPodReconcilerOptions)
+
+func WithClock(c clock.Clock) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+var defaultOptions = TerminatingPodReconcilerOptions{
+	clock: realClock,
+}
+
+func NewTerminatingPodReconciler(
+	client client.Client,
+	cfgs []configapi.TerminatePodConfig,
+	opts ...TerminatingPodReconcilerOption,
+) (*TerminatingPodReconciler, error) {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	terminationCfgs := make([]*terminatePodConfigWithSelector, len(cfgs))
+	for i, cfg := range cfgs {
+		selector, err := metav1.LabelSelectorAsSelector(cfg.PodLabelSelector)
+		if err != nil {
+			return nil, err
+		}
+
+		terminationCfgs[i] = &terminatePodConfigWithSelector{
+			terminationCfg: cfg,
+			selector:       selector,
+		}
+	}
+
+	return &TerminatingPodReconciler{
+		client:          client,
+		terminationCfgs: terminationCfgs,
+		clock:           options.clock,
+	}, nil
+}
+
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	pod := &corev1.Pod{}
+	if err := r.client.Get(ctx, req.NamespacedName, pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	terminationCfg := firstMatchingTerminationConfig(r.terminationCfgs, pod)
+	// Pod does not match any of the configured pod label selector
+	if terminationCfg == nil {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod was not marked for termination
+	if pod.DeletionTimestamp.IsZero() {
```

  Done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-18T15:46:35Z

- **apis/config/v1beta1/configuration_types.go:596** by [@mimowo](https://github.com/mimowo) — 2025-11-18T15:46:35Z
```diff
@@ -588,3 +592,31 @@ type WorkloadRetentionPolicy struct {
 	// +optional
 	AfterDeactivatedByKueue *metav1.Duration `json:"afterDeactivatedByKueue,omitempty"`
 }
+
+type FailureRecoveryPolicy struct {
```

  Let's take a backup of the work, but adapt the PR to align with the most recent KEP

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-18T17:00:24Z

- **apis/config/v1beta1/configuration_types.go:619** by [@kshalot](https://github.com/kshalot) — 2025-11-18T17:00:23Z
```diff
@@ -588,3 +592,29 @@ type WorkloadRetentionPolicy struct {
 	// +optional
 	AfterDeactivatedByKueue *metav1.Duration `json:"afterDeactivatedByKueue,omitempty"`
 }
+
+type FailureRecoveryPolicy struct {
+	// Rules specifies the rules to be enabled for failure recovery.
+	Rules []FailureRecoveryRule `json:"rules"`
+}
+
+type FailureRecoveryRule struct {
+	// Exactly one of the fields below must be specified.
+
+	// TerminatePod enables and contains configuration for the `TerminatePod` strategy.
+	// This strategy recovers stuck pods by forcefully terminating them after a configured
+	// grace period elapses.
+	// +optional
+	TerminatePod *TerminatePodConfig `json:"terminatePod,omitempty"`
+}
+
+type TerminatePodConfig struct {
+	// PodLabelSelector specifies the scope of resources covered by `TerminatePod` failure recovery -
+	// resources not matching the selector are ignored by the controller.
+	PodLabelSelector *metav1.LabelSelector `json:"podLabelSelector,omitempty"`
+
+	// ForcefulTerminationGracePeriod is the duration between when the pod's `deletionGracePeriodSeconds`
+	// elapses and when the pod should be forcefully deleted.
+	// Represented using metav1.Duration (e.g. "10m", "1h30m").
+	ForcefulTerminationGracePeriod *metav1.Duration `json:"forcefulTerminationGracePeriod"`
```

  Not relevant after removing the `FailureRecoveryPolicy` API.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-18T17:04:36Z

- **apis/config/v1beta1/configuration_types.go:596** by [@kshalot](https://github.com/kshalot) — 2025-11-18T17:04:36Z
```diff
@@ -588,3 +592,31 @@ type WorkloadRetentionPolicy struct {
 	// +optional
 	AfterDeactivatedByKueue *metav1.Duration `json:"afterDeactivatedByKueue,omitempty"`
 }
+
+type FailureRecoveryPolicy struct {
```

  Most of the code is realigned - should be ready for a first review (I added one TODO). I have a backup of this work in my fork repository.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-18T17:07:51Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:40** by [@mimowo](https://github.com/mimowo) — 2025-11-18T17:07:51Z
```diff
@@ -0,0 +1,136 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	utilnode "sigs.k8s.io/kueue/pkg/util/node"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=get;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var (
+	realClock                             = clock.RealClock{}
+	defaultForcefulTerminationGracePeriod = 1 * time.Second
```

  I should probably make it configurable as an option so that we can have 1min on prod, but a short value for testing

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-18T17:14:51Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:168** by [@mimowo](https://github.com/mimowo) — 2025-11-18T17:14:51Z
```diff
@@ -0,0 +1,136 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	utilnode "sigs.k8s.io/kueue/pkg/util/node"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=get;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var (
+	realClock                             = clock.RealClock{}
+	defaultForcefulTerminationGracePeriod = 1 * time.Second
+
+	// TODO: Move to API.
+	safeToForcefullyTerminateAnnotationName  = "kueue.x-k8s.io/safe-to-forcefully-terminate"
+	safeToForcefullyTerminateAnnotationValue = "true"
+)
+
+type TerminatingPodReconciler struct {
+	client client.Client
+	clock  clock.Clock
+}
+
+type TerminatingPodReconcilerOptions struct {
+	clock clock.Clock
+}
+
+type TerminatingPodReconcilerOption func(*TerminatingPodReconcilerOptions)
+
+func WithClock(c clock.Clock) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+var defaultOptions = TerminatingPodReconcilerOptions{
+	clock: realClock,
+}
+
+func NewTerminatingPodReconciler(
+	client client.Client,
+	opts ...TerminatingPodReconcilerOption,
+) (*TerminatingPodReconciler, error) {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	return &TerminatingPodReconciler{
+		client: client,
+		clock:  options.clock,
+	}, nil
+}
+
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	pod := &corev1.Pod{}
+	if err := r.client.Get(ctx, req.NamespacedName, pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	// Pod did not opt-in to be forcefully terminated
+	annotationValue, hasAnnotation := pod.Annotations[safeToForcefullyTerminateAnnotationName]
+	if !hasAnnotation || annotationValue != safeToForcefullyTerminateAnnotationValue {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod was not marked for termination
+	if pod.DeletionTimestamp.IsZero() {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod is not in a running phase
+	if utilpod.IsTerminated(pod) {
+		return ctrl.Result{}, nil
+	}
+
+	node := &corev1.Node{}
+	nodeKey := types.NamespacedName{Name: pod.Spec.NodeName}
+	if err := r.client.Get(ctx, nodeKey, node); err != nil {
+		return ctrl.Result{}, err
+	}
+	// Pod is not scheduled on an unreachable node
+	if !utilnode.HasTaint(node, corev1.TaintNodeUnreachable) {
+		return ctrl.Result{}, nil
+	}
```

  Actually, what is exactly the reason for checking Unreacheable, but not NotReady? 
  
  ```
  node.kubernetes.io/not-ready: Node is not ready. This corresponds to the NodeCondition Ready being "False".
  node.kubernetes.io/unreachable: Node is unreachable from the node controller. This corresponds to the NodeCondition Ready being "Unknown".
  ```
  Which of the taints is actually set when kubelet is disabled on a node (the most common scenario). 
  
  For example the GC controller in the core k8s checks the NotReady condition instead of the taints: https://github.com/kubernetes/kubernetes/blob/8d450ef773127374148abad4daaf28dac6cb2625/pkg/controller/podgc/gc_controller.go#L159-L165

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-18T17:15:53Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:119** by [@mimowo](https://github.com/mimowo) — 2025-11-18T17:15:53Z
```diff
@@ -0,0 +1,136 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	utilnode "sigs.k8s.io/kueue/pkg/util/node"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=get;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var (
+	realClock                             = clock.RealClock{}
+	defaultForcefulTerminationGracePeriod = 1 * time.Second
+
+	// TODO: Move to API.
+	safeToForcefullyTerminateAnnotationName  = "kueue.x-k8s.io/safe-to-forcefully-terminate"
+	safeToForcefullyTerminateAnnotationValue = "true"
+)
+
+type TerminatingPodReconciler struct {
+	client client.Client
+	clock  clock.Clock
+}
+
+type TerminatingPodReconcilerOptions struct {
+	clock clock.Clock
+}
+
+type TerminatingPodReconcilerOption func(*TerminatingPodReconcilerOptions)
+
+func WithClock(c clock.Clock) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+var defaultOptions = TerminatingPodReconcilerOptions{
+	clock: realClock,
+}
+
+func NewTerminatingPodReconciler(
+	client client.Client,
+	opts ...TerminatingPodReconcilerOption,
+) (*TerminatingPodReconciler, error) {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	return &TerminatingPodReconciler{
+		client: client,
+		clock:  options.clock,
+	}, nil
+}
+
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	pod := &corev1.Pod{}
+	if err := r.client.Get(ctx, req.NamespacedName, pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	// Pod did not opt-in to be forcefully terminated
+	annotationValue, hasAnnotation := pod.Annotations[safeToForcefullyTerminateAnnotationName]
+	if !hasAnnotation || annotationValue != safeToForcefullyTerminateAnnotationValue {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod was not marked for termination
+	if pod.DeletionTimestamp.IsZero() {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod is not in a running phase
+	if utilpod.IsTerminated(pod) {
+		return ctrl.Result{}, nil
+	}
+
+	node := &corev1.Node{}
+	nodeKey := types.NamespacedName{Name: pod.Spec.NodeName}
+	if err := r.client.Get(ctx, nodeKey, node); err != nil {
+		return ctrl.Result{}, err
+	}
+	// Pod is not scheduled on an unreachable node
+	if !utilnode.HasTaint(node, corev1.TaintNodeUnreachable) {
+		return ctrl.Result{}, nil
+	}
+
+	now := r.clock.Now()
+	gracefulTerminationPeriod := time.Duration(ptr.Deref(pod.DeletionGracePeriodSeconds, 0)) * time.Second
+	totalGracePeriod := gracefulTerminationPeriod + defaultForcefulTerminationGracePeriod
+	if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
+		gracePeriodLeft := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
```

  ```suggestion
  		remainingTime := pod.DeletionTimestamp.Add(totalGracePeriod).Sub(now)
  ```
  nit, but it is not really "grace period" we are tracking, but the time after that period

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-18T17:17:00Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:80** by [@mimowo](https://github.com/mimowo) — 2025-11-18T17:17:00Z
```diff
@@ -0,0 +1,108 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	unreachableNodeName = "unreachable-node"
+	reachableNodeName   = "reachable-node"
+
+	// The default termination grace period is set to 1 minute.
+	forcefulTerminationTimeout       = 80 * time.Second
+	forcefulTerminationCheckInterval = 15 * time.Second
+)
+
+func createTerminatingPod(p *corev1.Pod) {
+	ginkgo.GinkgoHelper()
+
+	util.MustCreate(ctx, k8sClient, p)
+	gomega.ExpectWithOffset(1, k8sClient.Delete(ctx, p)).To(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var ns *corev1.Namespace
+	var matchingPodWrapper *testingpod.PodWrapper
+
+	ginkgo.BeforeAll(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "pod-fr-namespace-")
+		nodes := []corev1.Node{
+			*testingnode.MakeNode(unreachableNodeName).
+				NotReady().
+				Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule}).
+				Obj(),
+			*testingnode.MakeNode(reachableNodeName).
+				Ready().
+				Obj(),
+		}
+		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+		matchingPodWrapper = testingpod.MakePod("matching-pod", ns.Name).
+			StatusPhase(corev1.PodPending).
+			TerminationGracePeriod(1).
+			NodeName(unreachableNodeName).
+			Annotation("kueue.x-k8s.io/safe-to-forcefully-terminate", "true")
+	})
+
+	ginkgo.It("forcefully terminates pods that opt-in, scheduled on unreachable nodes", func() {
+		matchingPod := matchingPodWrapper.Clone().Obj()
+		createTerminatingPod(matchingPod)
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: matchingPod.Name, Namespace: matchingPod.Namespace}, matchingPod)).
+				To(gomega.Succeed())
+			g.Expect(matchingPod.Status.Phase).Should(gomega.Equal(corev1.PodFailed))
+		}, forcefulTerminationTimeout, forcefulTerminationCheckInterval).Should(gomega.Succeed())
```

  Use default util.Timeout, and util.Interval for testing.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-19T09:51:41Z

- **pkg/util/node/node.go:25** by [@PBundyra](https://github.com/PBundyra) — 2025-11-19T09:51:42Z
```diff
@@ -0,0 +1,29 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package node
+
+import (
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+)
+
+func HasTaint(node *corev1.Node, taint string) bool {
```

  Shouldn't we also check the effect?

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-19T10:59:05Z

- **pkg/util/node/node.go:25** by [@kshalot](https://github.com/kshalot) — 2025-11-19T10:43:25Z
```diff
@@ -0,0 +1,29 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package node
+
+import (
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+)
+
+func HasTaint(node *corev1.Node, taint string) bool {
```

  Good question. I moved this to `taints` utils rather than `node` (https://github.com/kubernetes-sigs/kueue/pull/7312/commits/2faafed30cb21a01a8a82b67e2d8dc3608246024) to mimic this util from core k8s - https://github.com/kubernetes/kubernetes/blob/54900791c46f14fdd009102275e05e3510adc474/pkg/util/taints/taints.go#L225-L233.
  
  The existence of `TaintKeyExists` leads me to believe that there are other cases where you legitimately only care about the taint key being set. If required in the future, we can add `TaintExists` to have a more fine-grained taint matching function.

- **pkg/controller/failurerecovery/pod_termination_controller.go:168** by [@kshalot](https://github.com/kshalot) — 2025-11-19T10:48:58Z
```diff
@@ -0,0 +1,136 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	utilnode "sigs.k8s.io/kueue/pkg/util/node"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=get;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var (
+	realClock                             = clock.RealClock{}
+	defaultForcefulTerminationGracePeriod = 1 * time.Second
+
+	// TODO: Move to API.
+	safeToForcefullyTerminateAnnotationName  = "kueue.x-k8s.io/safe-to-forcefully-terminate"
+	safeToForcefullyTerminateAnnotationValue = "true"
+)
+
+type TerminatingPodReconciler struct {
+	client client.Client
+	clock  clock.Clock
+}
+
+type TerminatingPodReconcilerOptions struct {
+	clock clock.Clock
+}
+
+type TerminatingPodReconcilerOption func(*TerminatingPodReconcilerOptions)
+
+func WithClock(c clock.Clock) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+var defaultOptions = TerminatingPodReconcilerOptions{
+	clock: realClock,
+}
+
+func NewTerminatingPodReconciler(
+	client client.Client,
+	opts ...TerminatingPodReconcilerOption,
+) (*TerminatingPodReconciler, error) {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	return &TerminatingPodReconciler{
+		client: client,
+		clock:  options.clock,
+	}, nil
+}
+
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	pod := &corev1.Pod{}
+	if err := r.client.Get(ctx, req.NamespacedName, pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	// Pod did not opt-in to be forcefully terminated
+	annotationValue, hasAnnotation := pod.Annotations[safeToForcefullyTerminateAnnotationName]
+	if !hasAnnotation || annotationValue != safeToForcefullyTerminateAnnotationValue {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod was not marked for termination
+	if pod.DeletionTimestamp.IsZero() {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod is not in a running phase
+	if utilpod.IsTerminated(pod) {
+		return ctrl.Result{}, nil
+	}
+
+	node := &corev1.Node{}
+	nodeKey := types.NamespacedName{Name: pod.Spec.NodeName}
+	if err := r.client.Get(ctx, nodeKey, node); err != nil {
+		return ctrl.Result{}, err
+	}
+	// Pod is not scheduled on an unreachable node
+	if !utilnode.HasTaint(node, corev1.TaintNodeUnreachable) {
+		return ctrl.Result{}, nil
+	}
```

  In our case, we are interested in the `node.kubernetes.io/unreachable` taint. The code you sent checks both that the NodeCondition Ready is **not true** (covers both "false" and "unknown") and it does check the taint.
  
  Adding the condition check won't hurt, so we could add that.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-19T15:06:37Z

- **pkg/features/kube_features.go:349** by [@mimowo](https://github.com/mimowo) — 2025-11-19T15:06:37Z
```diff
@@ -339,6 +345,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	PropagateBatchJobLabelsToWorkload: {
 		{Version: version.MustParse("0.15"), Default: true, PreRelease: featuregate.Beta},
 	},
+	FailureRecoveryPolicy: {
+		{Version: version.MustParse("0.15"), Default: true, PreRelease: featuregate.Alpha},
```

  ```suggestion
  		{Version: version.MustParse("0.15"), Default: false, PreRelease: featuregate.Alpha},
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-19T15:07:20Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:81** by [@mimowo](https://github.com/mimowo) — 2025-11-19T15:07:20Z
```diff
@@ -0,0 +1,102 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	unreachableNodeName = "unreachable-node"
+	reachableNodeName   = "reachable-node"
+)
+
+func createTerminatingPod(p *corev1.Pod) {
+	ginkgo.GinkgoHelper()
+
+	util.MustCreate(ctx, k8sClient, p)
+	gomega.ExpectWithOffset(1, k8sClient.Delete(ctx, p)).To(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var ns *corev1.Namespace
+	var matchingPodWrapper *testingpod.PodWrapper
+
+	ginkgo.BeforeAll(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "pod-fr-namespace-")
+		nodes := []corev1.Node{
+			*testingnode.MakeNode(unreachableNodeName).
+				NotReady().
+				Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule}).
+				Obj(),
+			*testingnode.MakeNode(reachableNodeName).
+				Ready().
+				Obj(),
+		}
+		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+		matchingPodWrapper = testingpod.MakePod("matching-pod", ns.Name).
+			StatusPhase(corev1.PodPending).
+			TerminationGracePeriod(1).
+			NodeName(unreachableNodeName).
+			Annotation("kueue.x-k8s.io/safe-to-forcefully-terminate", "true")
+	})
+
+	ginkgo.It("forcefully terminates pods that opt-in, scheduled on unreachable nodes", func() {
+		matchingPod := matchingPodWrapper.Clone().Obj()
+		createTerminatingPod(matchingPod)
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: matchingPod.Name, Namespace: matchingPod.Namespace}, matchingPod)).
+				To(gomega.Succeed())
+			g.Expect(matchingPod.Status.Phase).Should(gomega.Equal(corev1.PodFailed))
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+
+	ginkgo.It("does not forcefully terminate pods that did not opt-in, scheduled on unreachable nodes", func() {
+		nonMatchingPod := matchingPodWrapper.
+			Clone().
+			Name("non-matching-pod").
+			Annotation("kueue.x-k8s.io/safe-to-forcefully-terminate", "false").
```

  Use constant rather than bare string.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-19T15:08:57Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:89** by [@mimowo](https://github.com/mimowo) — 2025-11-19T15:08:58Z
```diff
@@ -0,0 +1,102 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	unreachableNodeName = "unreachable-node"
+	reachableNodeName   = "reachable-node"
+)
+
+func createTerminatingPod(p *corev1.Pod) {
+	ginkgo.GinkgoHelper()
+
+	util.MustCreate(ctx, k8sClient, p)
+	gomega.ExpectWithOffset(1, k8sClient.Delete(ctx, p)).To(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var ns *corev1.Namespace
+	var matchingPodWrapper *testingpod.PodWrapper
+
+	ginkgo.BeforeAll(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "pod-fr-namespace-")
+		nodes := []corev1.Node{
+			*testingnode.MakeNode(unreachableNodeName).
+				NotReady().
+				Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule}).
+				Obj(),
+			*testingnode.MakeNode(reachableNodeName).
+				Ready().
+				Obj(),
+		}
+		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+		matchingPodWrapper = testingpod.MakePod("matching-pod", ns.Name).
+			StatusPhase(corev1.PodPending).
+			TerminationGracePeriod(1).
+			NodeName(unreachableNodeName).
+			Annotation("kueue.x-k8s.io/safe-to-forcefully-terminate", "true")
+	})
+
+	ginkgo.It("forcefully terminates pods that opt-in, scheduled on unreachable nodes", func() {
+		matchingPod := matchingPodWrapper.Clone().Obj()
+		createTerminatingPod(matchingPod)
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: matchingPod.Name, Namespace: matchingPod.Namespace}, matchingPod)).
+				To(gomega.Succeed())
+			g.Expect(matchingPod.Status.Phase).Should(gomega.Equal(corev1.PodFailed))
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+
+	ginkgo.It("does not forcefully terminate pods that did not opt-in, scheduled on unreachable nodes", func() {
+		nonMatchingPod := matchingPodWrapper.
+			Clone().
+			Name("non-matching-pod").
+			Annotation("kueue.x-k8s.io/safe-to-forcefully-terminate", "false").
+			Obj()
+		createTerminatingPod(nonMatchingPod)
+
+		gomega.Consistently(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: nonMatchingPod.Name, Namespace: nonMatchingPod.Namespace}, nonMatchingPod)).
+				To(gomega.Succeed())
+			g.Expect(nonMatchingPod.Status.Phase).Should(gomega.Equal(corev1.PodPending))
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
```

  Use util.ConsistentDuration for consistency checks. util.Timeout is 10s which would be too much time added.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-19T15:09:17Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:100** by [@mimowo](https://github.com/mimowo) — 2025-11-19T15:09:17Z
```diff
@@ -0,0 +1,102 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	unreachableNodeName = "unreachable-node"
+	reachableNodeName   = "reachable-node"
+)
+
+func createTerminatingPod(p *corev1.Pod) {
+	ginkgo.GinkgoHelper()
+
+	util.MustCreate(ctx, k8sClient, p)
+	gomega.ExpectWithOffset(1, k8sClient.Delete(ctx, p)).To(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var ns *corev1.Namespace
+	var matchingPodWrapper *testingpod.PodWrapper
+
+	ginkgo.BeforeAll(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "pod-fr-namespace-")
+		nodes := []corev1.Node{
+			*testingnode.MakeNode(unreachableNodeName).
+				NotReady().
+				Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule}).
+				Obj(),
+			*testingnode.MakeNode(reachableNodeName).
+				Ready().
+				Obj(),
+		}
+		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+		matchingPodWrapper = testingpod.MakePod("matching-pod", ns.Name).
+			StatusPhase(corev1.PodPending).
+			TerminationGracePeriod(1).
+			NodeName(unreachableNodeName).
+			Annotation("kueue.x-k8s.io/safe-to-forcefully-terminate", "true")
+	})
+
+	ginkgo.It("forcefully terminates pods that opt-in, scheduled on unreachable nodes", func() {
+		matchingPod := matchingPodWrapper.Clone().Obj()
+		createTerminatingPod(matchingPod)
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: matchingPod.Name, Namespace: matchingPod.Namespace}, matchingPod)).
+				To(gomega.Succeed())
+			g.Expect(matchingPod.Status.Phase).Should(gomega.Equal(corev1.PodFailed))
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+
+	ginkgo.It("does not forcefully terminate pods that did not opt-in, scheduled on unreachable nodes", func() {
+		nonMatchingPod := matchingPodWrapper.
+			Clone().
+			Name("non-matching-pod").
+			Annotation("kueue.x-k8s.io/safe-to-forcefully-terminate", "false").
+			Obj()
+		createTerminatingPod(nonMatchingPod)
+
+		gomega.Consistently(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: nonMatchingPod.Name, Namespace: nonMatchingPod.Namespace}, nonMatchingPod)).
+				To(gomega.Succeed())
+			g.Expect(nonMatchingPod.Status.Phase).Should(gomega.Equal(corev1.PodPending))
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+
+	ginkgo.It("does not forcefully terminate matching pods scheduled on healthy nodes", func() {
+		podOnHealthyNode := matchingPodWrapper.Clone().Name("healthy-pod").NodeName(reachableNodeName).Obj()
+		createTerminatingPod(podOnHealthyNode)
+
+		gomega.Consistently(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: podOnHealthyNode.Name, Namespace: podOnHealthyNode.Namespace}, podOnHealthyNode)).
+				To(gomega.Succeed())
+			g.Expect(podOnHealthyNode.Status.Phase).Should(gomega.Equal(corev1.PodPending))
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
```

  ```suggestion
  		}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-19T15:10:22Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:92** by [@mimowo](https://github.com/mimowo) — 2025-11-19T15:10:22Z
```diff
@@ -0,0 +1,102 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	unreachableNodeName = "unreachable-node"
+	reachableNodeName   = "reachable-node"
+)
+
+func createTerminatingPod(p *corev1.Pod) {
+	ginkgo.GinkgoHelper()
+
+	util.MustCreate(ctx, k8sClient, p)
+	gomega.ExpectWithOffset(1, k8sClient.Delete(ctx, p)).To(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var ns *corev1.Namespace
+	var matchingPodWrapper *testingpod.PodWrapper
+
+	ginkgo.BeforeAll(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "pod-fr-namespace-")
+		nodes := []corev1.Node{
+			*testingnode.MakeNode(unreachableNodeName).
+				NotReady().
+				Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule}).
+				Obj(),
+			*testingnode.MakeNode(reachableNodeName).
+				Ready().
+				Obj(),
+		}
+		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+		matchingPodWrapper = testingpod.MakePod("matching-pod", ns.Name).
+			StatusPhase(corev1.PodPending).
+			TerminationGracePeriod(1).
+			NodeName(unreachableNodeName).
+			Annotation("kueue.x-k8s.io/safe-to-forcefully-terminate", "true")
+	})
+
+	ginkgo.It("forcefully terminates pods that opt-in, scheduled on unreachable nodes", func() {
+		matchingPod := matchingPodWrapper.Clone().Obj()
+		createTerminatingPod(matchingPod)
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: matchingPod.Name, Namespace: matchingPod.Namespace}, matchingPod)).
+				To(gomega.Succeed())
+			g.Expect(matchingPod.Status.Phase).Should(gomega.Equal(corev1.PodFailed))
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+
+	ginkgo.It("does not forcefully terminate pods that did not opt-in, scheduled on unreachable nodes", func() {
+		nonMatchingPod := matchingPodWrapper.
+			Clone().
+			Name("non-matching-pod").
+			Annotation("kueue.x-k8s.io/safe-to-forcefully-terminate", "false").
+			Obj()
+		createTerminatingPod(nonMatchingPod)
+
+		gomega.Consistently(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: nonMatchingPod.Name, Namespace: nonMatchingPod.Namespace}, nonMatchingPod)).
+				To(gomega.Succeed())
+			g.Expect(nonMatchingPod.Status.Phase).Should(gomega.Equal(corev1.PodPending))
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+
+	ginkgo.It("does not forcefully terminate matching pods scheduled on healthy nodes", func() {
```

  Actually, I like separate test cases, but when Consitently is used we pay price of 2s total. I would prefer to combine the two "Its" and check both Pods within a Consistently, wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-19T18:40:33Z

- **pkg/util/node/node.go:25** by [@mimowo](https://github.com/mimowo) — 2025-11-19T18:40:33Z
```diff
@@ -0,0 +1,29 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package node
+
+import (
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+)
+
+func HasTaint(node *corev1.Node, taint string) bool {
```

  Yeah, I was also wondering about this, we probably should, but practically speaking I don't expect this taint to be added by users in different forms.
  
  The core k8s code also checks only the key in gc_controller for another reserved taint (very similar use-case): https://github.com/kubernetes/kubernetes/blob/54900791c46f14fdd009102275e05e3510adc474/pkg/controller/podgc/gc_controller.go#L162

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-19T19:52:35Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:89** by [@kshalot](https://github.com/kshalot) — 2025-11-19T19:52:35Z
```diff
@@ -0,0 +1,102 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	unreachableNodeName = "unreachable-node"
+	reachableNodeName   = "reachable-node"
+)
+
+func createTerminatingPod(p *corev1.Pod) {
+	ginkgo.GinkgoHelper()
+
+	util.MustCreate(ctx, k8sClient, p)
+	gomega.ExpectWithOffset(1, k8sClient.Delete(ctx, p)).To(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var ns *corev1.Namespace
+	var matchingPodWrapper *testingpod.PodWrapper
+
+	ginkgo.BeforeAll(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "pod-fr-namespace-")
+		nodes := []corev1.Node{
+			*testingnode.MakeNode(unreachableNodeName).
+				NotReady().
+				Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule}).
+				Obj(),
+			*testingnode.MakeNode(reachableNodeName).
+				Ready().
+				Obj(),
+		}
+		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+		matchingPodWrapper = testingpod.MakePod("matching-pod", ns.Name).
+			StatusPhase(corev1.PodPending).
+			TerminationGracePeriod(1).
+			NodeName(unreachableNodeName).
+			Annotation("kueue.x-k8s.io/safe-to-forcefully-terminate", "true")
+	})
+
+	ginkgo.It("forcefully terminates pods that opt-in, scheduled on unreachable nodes", func() {
+		matchingPod := matchingPodWrapper.Clone().Obj()
+		createTerminatingPod(matchingPod)
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: matchingPod.Name, Namespace: matchingPod.Namespace}, matchingPod)).
+				To(gomega.Succeed())
+			g.Expect(matchingPod.Status.Phase).Should(gomega.Equal(corev1.PodFailed))
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+
+	ginkgo.It("does not forcefully terminate pods that did not opt-in, scheduled on unreachable nodes", func() {
+		nonMatchingPod := matchingPodWrapper.
+			Clone().
+			Name("non-matching-pod").
+			Annotation("kueue.x-k8s.io/safe-to-forcefully-terminate", "false").
+			Obj()
+		createTerminatingPod(nonMatchingPod)
+
+		gomega.Consistently(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: nonMatchingPod.Name, Namespace: nonMatchingPod.Namespace}, nonMatchingPod)).
+				To(gomega.Succeed())
+			g.Expect(nonMatchingPod.Status.Phase).Should(gomega.Equal(corev1.PodPending))
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
```

  I used `2 * time.Second`. `util.ConsistentDuration` won't cut it because `TerminationGracePeriod(1)` is the lowest grace period that can be used when creating a pod (pods created with `TerminationGracePeriod(0)` are immediately removed) so `util.ConsistentDuration` (1 second) will not be enough to also catch the forceful termination (or will be flaky).
  
  We can potentially go lower, maybe `1500 * time.Millisecond` or smth. WDYT?

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-20T08:21:58Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:92** by [@kshalot](https://github.com/kshalot) — 2025-11-20T08:21:58Z
```diff
@@ -0,0 +1,102 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	unreachableNodeName = "unreachable-node"
+	reachableNodeName   = "reachable-node"
+)
+
+func createTerminatingPod(p *corev1.Pod) {
+	ginkgo.GinkgoHelper()
+
+	util.MustCreate(ctx, k8sClient, p)
+	gomega.ExpectWithOffset(1, k8sClient.Delete(ctx, p)).To(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var ns *corev1.Namespace
+	var matchingPodWrapper *testingpod.PodWrapper
+
+	ginkgo.BeforeAll(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "pod-fr-namespace-")
+		nodes := []corev1.Node{
+			*testingnode.MakeNode(unreachableNodeName).
+				NotReady().
+				Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule}).
+				Obj(),
+			*testingnode.MakeNode(reachableNodeName).
+				Ready().
+				Obj(),
+		}
+		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+		matchingPodWrapper = testingpod.MakePod("matching-pod", ns.Name).
+			StatusPhase(corev1.PodPending).
+			TerminationGracePeriod(1).
+			NodeName(unreachableNodeName).
+			Annotation("kueue.x-k8s.io/safe-to-forcefully-terminate", "true")
+	})
+
+	ginkgo.It("forcefully terminates pods that opt-in, scheduled on unreachable nodes", func() {
+		matchingPod := matchingPodWrapper.Clone().Obj()
+		createTerminatingPod(matchingPod)
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: matchingPod.Name, Namespace: matchingPod.Namespace}, matchingPod)).
+				To(gomega.Succeed())
+			g.Expect(matchingPod.Status.Phase).Should(gomega.Equal(corev1.PodFailed))
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+
+	ginkgo.It("does not forcefully terminate pods that did not opt-in, scheduled on unreachable nodes", func() {
+		nonMatchingPod := matchingPodWrapper.
+			Clone().
+			Name("non-matching-pod").
+			Annotation("kueue.x-k8s.io/safe-to-forcefully-terminate", "false").
+			Obj()
+		createTerminatingPod(nonMatchingPod)
+
+		gomega.Consistently(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: nonMatchingPod.Name, Namespace: nonMatchingPod.Namespace}, nonMatchingPod)).
+				To(gomega.Succeed())
+			g.Expect(nonMatchingPod.Status.Phase).Should(gomega.Equal(corev1.PodPending))
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+
+	ginkgo.It("does not forcefully terminate matching pods scheduled on healthy nodes", func() {
```

  Merging the test cases won't hurt. I'm just wondering - is 2s a lot? From what I can see, the integration tests are running in minutes (10-20). Obviously at some point such 1s changes will accumulate.
  
  I made the change as I don't have a strong preference on this matter (https://github.com/kubernetes-sigs/kueue/pull/7312/commits/b49414921ca0f66bf93ea0e9a502a47962330001)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-20T08:53:22Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:124** by [@mimowo](https://github.com/mimowo) — 2025-11-20T08:53:22Z
```diff
@@ -0,0 +1,142 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltaints "sigs.k8s.io/kueue/pkg/util/taints"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=get;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var (
+	realClock = clock.RealClock{}
+)
+
+type TerminatingPodReconciler struct {
+	client                         client.Client
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+}
+
+type TerminatingPodReconcilerOptions struct {
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+}
+
+type TerminatingPodReconcilerOption func(*TerminatingPodReconcilerOptions)
+
+func WithClock(c clock.Clock) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+func WithForcefulTerminationGracePeriod(t time.Duration) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.forcefulTerminationGracePeriod = t
+	}
+}
+
+var defaultOptions = TerminatingPodReconcilerOptions{
+	clock:                          realClock,
+	forcefulTerminationGracePeriod: time.Minute,
+}
+
+func NewTerminatingPodReconciler(
+	client client.Client,
+	opts ...TerminatingPodReconcilerOption,
+) (*TerminatingPodReconciler, error) {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	return &TerminatingPodReconciler{
+		client:                         client,
+		clock:                          options.clock,
+		forcefulTerminationGracePeriod: options.forcefulTerminationGracePeriod,
+	}, nil
+}
+
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	pod := &corev1.Pod{}
+	if err := r.client.Get(ctx, req.NamespacedName, pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	// Pod did not opt-in to be forcefully terminated
+	annotationValue, hasAnnotation := pod.Annotations[constants.SafeToForcefullyTerminateAnnotationKey]
+	if !hasAnnotation || annotationValue != constants.SafeToForcefullyTerminateAnnotationValue {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod was not marked for termination
+	if pod.DeletionTimestamp.IsZero() {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod is not in a running phase
+	if utilpod.IsTerminated(pod) {
+		return ctrl.Result{}, nil
+	}
+
+	node := &corev1.Node{}
+	nodeKey := types.NamespacedName{Name: pod.Spec.NodeName}
+	if err := r.client.Get(ctx, nodeKey, node); err != nil {
+		return ctrl.Result{}, err
+	}
+	// Pod is not scheduled on an unreachable node
+	if !utiltaints.TaintKeyExists(node.Spec.Taints, corev1.TaintNodeUnreachable) {
+		return ctrl.Result{}, nil
+	}
+
+	now := r.clock.Now()
+	gracefulTerminationPeriod := time.Duration(ptr.Deref(pod.DeletionGracePeriodSeconds, 0)) * time.Second
+	totalGracePeriod := gracefulTerminationPeriod + r.forcefulTerminationGracePeriod
+	if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
```

  Hm, I think Deletiontimestamp already accounts for the gracefultermination period. In other words, when gracefulTerminationPeriod is 30s, then the DeletionTimestamp will already point to the time in the future of when the time elapses. 
  
  see: https://github.com/kubernetes/kubernetes/blob/4cf195304caa519be476b367267f6c656bce19f7/staging/src/k8s.io/apimachinery/pkg/apis/meta/v1/types.go#L190-L209
  
  // The resource is expected to be deleted (no longer visible
  // from resource lists, and not reachable by name) **after** the time in this field, once the
  // finalizers list is empty. As long as the finalizers list contains items, deletion is blocked.
  
  So, IIUC the code is double counting `DeletionGracePeriodSeconds`. Please double check that.
  
  So, I think we should do `if now.Before(pod.DeletionTimestamp.Add(r.forcefulTerminationGracePeriod)) {`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-20T08:58:36Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:44** by [@mimowo](https://github.com/mimowo) — 2025-11-20T08:58:36Z
```diff
@@ -0,0 +1,104 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	unreachableNodeName        = "unreachable-node"
+	reachableNodeName          = "reachable-node"
+	forcefulTerminationTimeout = 2 * time.Second
+)
+
+func createTerminatingPod(p *corev1.Pod) {
+	ginkgo.GinkgoHelper()
+
+	util.MustCreate(ctx, k8sClient, p)
+	gomega.ExpectWithOffset(1, k8sClient.Delete(ctx, p)).To(gomega.Succeed())
+}
```

  remove the helper, it is saving just one line, and adds indirection layer

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-20T09:33:43Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:102** by [@mimowo](https://github.com/mimowo) — 2025-11-20T09:33:43Z
```diff
@@ -0,0 +1,104 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	unreachableNodeName        = "unreachable-node"
+	reachableNodeName          = "reachable-node"
+	forcefulTerminationTimeout = 2 * time.Second
+)
+
+func createTerminatingPod(p *corev1.Pod) {
+	ginkgo.GinkgoHelper()
+
+	util.MustCreate(ctx, k8sClient, p)
+	gomega.ExpectWithOffset(1, k8sClient.Delete(ctx, p)).To(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var ns *corev1.Namespace
+	var matchingPodWrapper *testingpod.PodWrapper
+
+	ginkgo.BeforeAll(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "pod-fr-namespace-")
+		nodes := []corev1.Node{
+			*testingnode.MakeNode(unreachableNodeName).
+				NotReady().
+				Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule}).
+				Obj(),
+			*testingnode.MakeNode(reachableNodeName).
+				Ready().
+				Obj(),
+		}
+		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+		matchingPodWrapper = testingpod.MakePod("matching-pod", ns.Name).
+			StatusPhase(corev1.PodPending).
+			TerminationGracePeriod(1).
+			NodeName(unreachableNodeName).
+			Annotation(constants.SafeToForcefullyTerminateAnnotationKey, constants.SafeToForcefullyTerminateAnnotationValue)
+	})
+
+	ginkgo.It("forcefully terminates pods that opt-in, scheduled on unreachable nodes", func() {
+		matchingPod := matchingPodWrapper.Clone().Obj()
+		createTerminatingPod(matchingPod)
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: matchingPod.Name, Namespace: matchingPod.Namespace}, matchingPod)).
+				To(gomega.Succeed())
+			g.Expect(matchingPod.Status.Phase).Should(gomega.Equal(corev1.PodFailed))
+		}, forcefulTerminationTimeout, util.Interval).Should(gomega.Succeed())
+	})
+
+	ginkgo.It("does not forcefully terminate matching pods that did not opt-in or are running on healthy nodes", func() {
+		nonMatchingPod := matchingPodWrapper.
+			Clone().
+			Name("non-matching-pod").
+			Annotation(constants.SafeToForcefullyTerminateAnnotationKey, constants.SafeToForcefullyTerminateAnnotationValue).
+			Obj()
+		createTerminatingPod(nonMatchingPod)
+
+		podOnHealthyNode := matchingPodWrapper.Clone().Name("healthy-pod").NodeName(reachableNodeName).Obj()
+		createTerminatingPod(podOnHealthyNode)
+
+		gomega.Consistently(func(g gomega.Gomega) {
+			// Non-matching pod is left untouched.
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: nonMatchingPod.Name, Namespace: nonMatchingPod.Namespace}, nonMatchingPod)).
+				To(gomega.Succeed())
+			g.Expect(nonMatchingPod.Status.Phase).Should(gomega.Equal(corev1.PodPending))
+
+			// Healthy pod is left untouched.
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: podOnHealthyNode.Name, Namespace: podOnHealthyNode.Namespace}, podOnHealthyNode)).
+				To(gomega.Succeed())
+			g.Expect(podOnHealthyNode.Status.Phase).Should(gomega.Equal(corev1.PodPending))
+		}, forcefulTerminationTimeout, util.Interval).Should(gomega.Succeed())
```

  I think this could flake potentially, if the test code starts a check just before forcefulTerminationTimeout elapses, but loses CPU for a while. Then the controller can make progress and transition to Failed, resulting in the test failing. 
  
  TBH I think we need two tests with different configuration:
  - (1.) forcefulTerminationTimeout=2s, and just wait to see we transition to Failed eventually
  - (2.) forcefulTerminationTimeout=10s, and verify that we stay in Pending for 1s or 2s

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-20T10:08:28Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:124** by [@kshalot](https://github.com/kshalot) — 2025-11-20T10:08:28Z
```diff
@@ -0,0 +1,142 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltaints "sigs.k8s.io/kueue/pkg/util/taints"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=get;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var (
+	realClock = clock.RealClock{}
+)
+
+type TerminatingPodReconciler struct {
+	client                         client.Client
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+}
+
+type TerminatingPodReconcilerOptions struct {
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+}
+
+type TerminatingPodReconcilerOption func(*TerminatingPodReconcilerOptions)
+
+func WithClock(c clock.Clock) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+func WithForcefulTerminationGracePeriod(t time.Duration) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.forcefulTerminationGracePeriod = t
+	}
+}
+
+var defaultOptions = TerminatingPodReconcilerOptions{
+	clock:                          realClock,
+	forcefulTerminationGracePeriod: time.Minute,
+}
+
+func NewTerminatingPodReconciler(
+	client client.Client,
+	opts ...TerminatingPodReconcilerOption,
+) (*TerminatingPodReconciler, error) {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	return &TerminatingPodReconciler{
+		client:                         client,
+		clock:                          options.clock,
+		forcefulTerminationGracePeriod: options.forcefulTerminationGracePeriod,
+	}, nil
+}
+
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	pod := &corev1.Pod{}
+	if err := r.client.Get(ctx, req.NamespacedName, pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	// Pod did not opt-in to be forcefully terminated
+	annotationValue, hasAnnotation := pod.Annotations[constants.SafeToForcefullyTerminateAnnotationKey]
+	if !hasAnnotation || annotationValue != constants.SafeToForcefullyTerminateAnnotationValue {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod was not marked for termination
+	if pod.DeletionTimestamp.IsZero() {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod is not in a running phase
+	if utilpod.IsTerminated(pod) {
+		return ctrl.Result{}, nil
+	}
+
+	node := &corev1.Node{}
+	nodeKey := types.NamespacedName{Name: pod.Spec.NodeName}
+	if err := r.client.Get(ctx, nodeKey, node); err != nil {
+		return ctrl.Result{}, err
+	}
+	// Pod is not scheduled on an unreachable node
+	if !utiltaints.TaintKeyExists(node.Spec.Taints, corev1.TaintNodeUnreachable) {
+		return ctrl.Result{}, nil
+	}
+
+	now := r.clock.Now()
+	gracefulTerminationPeriod := time.Duration(ptr.Deref(pod.DeletionGracePeriodSeconds, 0)) * time.Second
+	totalGracePeriod := gracefulTerminationPeriod + r.forcefulTerminationGracePeriod
+	if now.Before(pod.DeletionTimestamp.Add(totalGracePeriod)) {
```

  Yes, good catch. I got confused after all discussion about `DeletionGracePeriodSeconds` "elapsing" etc., but yeah - it is counted inside the `DeletionTimestamp` (I also tested it manually to be 100% sure).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-20T10:12:14Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:89** by [@mimowo](https://github.com/mimowo) — 2025-11-20T10:12:14Z
```diff
@@ -0,0 +1,102 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	unreachableNodeName = "unreachable-node"
+	reachableNodeName   = "reachable-node"
+)
+
+func createTerminatingPod(p *corev1.Pod) {
+	ginkgo.GinkgoHelper()
+
+	util.MustCreate(ctx, k8sClient, p)
+	gomega.ExpectWithOffset(1, k8sClient.Delete(ctx, p)).To(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var ns *corev1.Namespace
+	var matchingPodWrapper *testingpod.PodWrapper
+
+	ginkgo.BeforeAll(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "pod-fr-namespace-")
+		nodes := []corev1.Node{
+			*testingnode.MakeNode(unreachableNodeName).
+				NotReady().
+				Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule}).
+				Obj(),
+			*testingnode.MakeNode(reachableNodeName).
+				Ready().
+				Obj(),
+		}
+		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+		matchingPodWrapper = testingpod.MakePod("matching-pod", ns.Name).
+			StatusPhase(corev1.PodPending).
+			TerminationGracePeriod(1).
+			NodeName(unreachableNodeName).
+			Annotation("kueue.x-k8s.io/safe-to-forcefully-terminate", "true")
+	})
+
+	ginkgo.It("forcefully terminates pods that opt-in, scheduled on unreachable nodes", func() {
+		matchingPod := matchingPodWrapper.Clone().Obj()
+		createTerminatingPod(matchingPod)
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: matchingPod.Name, Namespace: matchingPod.Namespace}, matchingPod)).
+				To(gomega.Succeed())
+			g.Expect(matchingPod.Status.Phase).Should(gomega.Equal(corev1.PodFailed))
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+
+	ginkgo.It("does not forcefully terminate pods that did not opt-in, scheduled on unreachable nodes", func() {
+		nonMatchingPod := matchingPodWrapper.
+			Clone().
+			Name("non-matching-pod").
+			Annotation("kueue.x-k8s.io/safe-to-forcefully-terminate", "false").
+			Obj()
+		createTerminatingPod(nonMatchingPod)
+
+		gomega.Consistently(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: nonMatchingPod.Name, Namespace: nonMatchingPod.Namespace}, nonMatchingPod)).
+				To(gomega.Succeed())
+			g.Expect(nonMatchingPod.Status.Phase).Should(gomega.Equal(corev1.PodPending))
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
```

  I see, using 2s for Consistency check is fine.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-20T10:13:01Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:92** by [@mimowo](https://github.com/mimowo) — 2025-11-20T10:13:01Z
```diff
@@ -0,0 +1,102 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	unreachableNodeName = "unreachable-node"
+	reachableNodeName   = "reachable-node"
+)
+
+func createTerminatingPod(p *corev1.Pod) {
+	ginkgo.GinkgoHelper()
+
+	util.MustCreate(ctx, k8sClient, p)
+	gomega.ExpectWithOffset(1, k8sClient.Delete(ctx, p)).To(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var ns *corev1.Namespace
+	var matchingPodWrapper *testingpod.PodWrapper
+
+	ginkgo.BeforeAll(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "pod-fr-namespace-")
+		nodes := []corev1.Node{
+			*testingnode.MakeNode(unreachableNodeName).
+				NotReady().
+				Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule}).
+				Obj(),
+			*testingnode.MakeNode(reachableNodeName).
+				Ready().
+				Obj(),
+		}
+		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+		matchingPodWrapper = testingpod.MakePod("matching-pod", ns.Name).
+			StatusPhase(corev1.PodPending).
+			TerminationGracePeriod(1).
+			NodeName(unreachableNodeName).
+			Annotation("kueue.x-k8s.io/safe-to-forcefully-terminate", "true")
+	})
+
+	ginkgo.It("forcefully terminates pods that opt-in, scheduled on unreachable nodes", func() {
+		matchingPod := matchingPodWrapper.Clone().Obj()
+		createTerminatingPod(matchingPod)
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: matchingPod.Name, Namespace: matchingPod.Namespace}, matchingPod)).
+				To(gomega.Succeed())
+			g.Expect(matchingPod.Status.Phase).Should(gomega.Equal(corev1.PodFailed))
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+
+	ginkgo.It("does not forcefully terminate pods that did not opt-in, scheduled on unreachable nodes", func() {
+		nonMatchingPod := matchingPodWrapper.
+			Clone().
+			Name("non-matching-pod").
+			Annotation("kueue.x-k8s.io/safe-to-forcefully-terminate", "false").
+			Obj()
+		createTerminatingPod(nonMatchingPod)
+
+		gomega.Consistently(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: nonMatchingPod.Name, Namespace: nonMatchingPod.Namespace}, nonMatchingPod)).
+				To(gomega.Succeed())
+			g.Expect(nonMatchingPod.Status.Phase).Should(gomega.Equal(corev1.PodPending))
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+
+	ginkgo.It("does not forcefully terminate matching pods scheduled on healthy nodes", func() {
```

  Merged with 2s timeout sgtm

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-20T10:13:59Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:102** by [@kshalot](https://github.com/kshalot) — 2025-11-20T10:13:59Z
```diff
@@ -0,0 +1,104 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	unreachableNodeName        = "unreachable-node"
+	reachableNodeName          = "reachable-node"
+	forcefulTerminationTimeout = 2 * time.Second
+)
+
+func createTerminatingPod(p *corev1.Pod) {
+	ginkgo.GinkgoHelper()
+
+	util.MustCreate(ctx, k8sClient, p)
+	gomega.ExpectWithOffset(1, k8sClient.Delete(ctx, p)).To(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var ns *corev1.Namespace
+	var matchingPodWrapper *testingpod.PodWrapper
+
+	ginkgo.BeforeAll(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "pod-fr-namespace-")
+		nodes := []corev1.Node{
+			*testingnode.MakeNode(unreachableNodeName).
+				NotReady().
+				Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule}).
+				Obj(),
+			*testingnode.MakeNode(reachableNodeName).
+				Ready().
+				Obj(),
+		}
+		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+		matchingPodWrapper = testingpod.MakePod("matching-pod", ns.Name).
+			StatusPhase(corev1.PodPending).
+			TerminationGracePeriod(1).
+			NodeName(unreachableNodeName).
+			Annotation(constants.SafeToForcefullyTerminateAnnotationKey, constants.SafeToForcefullyTerminateAnnotationValue)
+	})
+
+	ginkgo.It("forcefully terminates pods that opt-in, scheduled on unreachable nodes", func() {
+		matchingPod := matchingPodWrapper.Clone().Obj()
+		createTerminatingPod(matchingPod)
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: matchingPod.Name, Namespace: matchingPod.Namespace}, matchingPod)).
+				To(gomega.Succeed())
+			g.Expect(matchingPod.Status.Phase).Should(gomega.Equal(corev1.PodFailed))
+		}, forcefulTerminationTimeout, util.Interval).Should(gomega.Succeed())
+	})
+
+	ginkgo.It("does not forcefully terminate matching pods that did not opt-in or are running on healthy nodes", func() {
+		nonMatchingPod := matchingPodWrapper.
+			Clone().
+			Name("non-matching-pod").
+			Annotation(constants.SafeToForcefullyTerminateAnnotationKey, constants.SafeToForcefullyTerminateAnnotationValue).
+			Obj()
+		createTerminatingPod(nonMatchingPod)
+
+		podOnHealthyNode := matchingPodWrapper.Clone().Name("healthy-pod").NodeName(reachableNodeName).Obj()
+		createTerminatingPod(podOnHealthyNode)
+
+		gomega.Consistently(func(g gomega.Gomega) {
+			// Non-matching pod is left untouched.
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: nonMatchingPod.Name, Namespace: nonMatchingPod.Namespace}, nonMatchingPod)).
+				To(gomega.Succeed())
+			g.Expect(nonMatchingPod.Status.Phase).Should(gomega.Equal(corev1.PodPending))
+
+			// Healthy pod is left untouched.
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: podOnHealthyNode.Name, Namespace: podOnHealthyNode.Namespace}, podOnHealthyNode)).
+				To(gomega.Succeed())
+			g.Expect(podOnHealthyNode.Status.Phase).Should(gomega.Equal(corev1.PodPending))
+		}, forcefulTerminationTimeout, util.Interval).Should(gomega.Succeed())
```

  Hm, but in this particular case, we want the phase to always be pending and never transition to failed. Where's the flakiness?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-20T10:16:09Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:72** by [@mimowo](https://github.com/mimowo) — 2025-11-20T10:16:09Z
```diff
@@ -0,0 +1,100 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	unreachableNodeName        = "unreachable-node"
+	reachableNodeName          = "reachable-node"
+	forcefulTerminationTimeout = 2 * time.Second
+)
+
+var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var ns *corev1.Namespace
+	var matchingPodWrapper *testingpod.PodWrapper
+
+	ginkgo.BeforeAll(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "pod-fr-namespace-")
+		nodes := []corev1.Node{
+			*testingnode.MakeNode(unreachableNodeName).
+				NotReady().
+				Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule}).
+				Obj(),
+			*testingnode.MakeNode(reachableNodeName).
+				Ready().
+				Obj(),
+		}
+		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+		matchingPodWrapper = testingpod.MakePod("matching-pod", ns.Name).
+			StatusPhase(corev1.PodPending).
+			TerminationGracePeriod(1).
+			NodeName(unreachableNodeName).
+			Annotation(constants.SafeToForcefullyTerminateAnnotationKey, constants.SafeToForcefullyTerminateAnnotationValue)
+	})
+
+	ginkgo.It("forcefully terminates pods that opt-in, scheduled on unreachable nodes", func() {
+		matchingPod := matchingPodWrapper.Clone().Obj()
+		util.MustCreate(ctx, k8sClient, matchingPod)
+		gomega.ExpectWithOffset(1, k8sClient.Delete(ctx, matchingPod)).To(gomega.Succeed())
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: matchingPod.Name, Namespace: matchingPod.Namespace}, matchingPod)).
+				To(gomega.Succeed())
+			g.Expect(matchingPod.Status.Phase).Should(gomega.Equal(corev1.PodFailed))
+		}, forcefulTerminationTimeout, util.Interval).Should(gomega.Succeed())
```

  Wouldn't this flake? because we expect the Pod to transition to failed after forcefulTerminationTimeout, at the same time this is only how long we are waiting.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-20T10:18:01Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:102** by [@mimowo](https://github.com/mimowo) — 2025-11-20T10:18:00Z
```diff
@@ -0,0 +1,104 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	unreachableNodeName        = "unreachable-node"
+	reachableNodeName          = "reachable-node"
+	forcefulTerminationTimeout = 2 * time.Second
+)
+
+func createTerminatingPod(p *corev1.Pod) {
+	ginkgo.GinkgoHelper()
+
+	util.MustCreate(ctx, k8sClient, p)
+	gomega.ExpectWithOffset(1, k8sClient.Delete(ctx, p)).To(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var ns *corev1.Namespace
+	var matchingPodWrapper *testingpod.PodWrapper
+
+	ginkgo.BeforeAll(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "pod-fr-namespace-")
+		nodes := []corev1.Node{
+			*testingnode.MakeNode(unreachableNodeName).
+				NotReady().
+				Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule}).
+				Obj(),
+			*testingnode.MakeNode(reachableNodeName).
+				Ready().
+				Obj(),
+		}
+		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+		matchingPodWrapper = testingpod.MakePod("matching-pod", ns.Name).
+			StatusPhase(corev1.PodPending).
+			TerminationGracePeriod(1).
+			NodeName(unreachableNodeName).
+			Annotation(constants.SafeToForcefullyTerminateAnnotationKey, constants.SafeToForcefullyTerminateAnnotationValue)
+	})
+
+	ginkgo.It("forcefully terminates pods that opt-in, scheduled on unreachable nodes", func() {
+		matchingPod := matchingPodWrapper.Clone().Obj()
+		createTerminatingPod(matchingPod)
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: matchingPod.Name, Namespace: matchingPod.Namespace}, matchingPod)).
+				To(gomega.Succeed())
+			g.Expect(matchingPod.Status.Phase).Should(gomega.Equal(corev1.PodFailed))
+		}, forcefulTerminationTimeout, util.Interval).Should(gomega.Succeed())
+	})
+
+	ginkgo.It("does not forcefully terminate matching pods that did not opt-in or are running on healthy nodes", func() {
+		nonMatchingPod := matchingPodWrapper.
+			Clone().
+			Name("non-matching-pod").
+			Annotation(constants.SafeToForcefullyTerminateAnnotationKey, constants.SafeToForcefullyTerminateAnnotationValue).
+			Obj()
+		createTerminatingPod(nonMatchingPod)
+
+		podOnHealthyNode := matchingPodWrapper.Clone().Name("healthy-pod").NodeName(reachableNodeName).Obj()
+		createTerminatingPod(podOnHealthyNode)
+
+		gomega.Consistently(func(g gomega.Gomega) {
+			// Non-matching pod is left untouched.
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: nonMatchingPod.Name, Namespace: nonMatchingPod.Namespace}, nonMatchingPod)).
+				To(gomega.Succeed())
+			g.Expect(nonMatchingPod.Status.Phase).Should(gomega.Equal(corev1.PodPending))
+
+			// Healthy pod is left untouched.
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: podOnHealthyNode.Name, Namespace: podOnHealthyNode.Namespace}, podOnHealthyNode)).
+				To(gomega.Succeed())
+			g.Expect(podOnHealthyNode.Status.Phase).Should(gomega.Equal(corev1.PodPending))
+		}, forcefulTerminationTimeout, util.Interval).Should(gomega.Succeed())
```

  Maybe I got confused about the intention of the test seeing the timeout. In that case, it could flake if there is a bug, rather than fail consistently as we expect.
  
  So, here I would imagine something like `forcefulTerminationTimeout + epsilon`, where `epsilon` is probably 1s or so.
  
  EDIT: Because if there is a bug that it transitions to Failed after forcefulTerminationTimeout, then we are just on the verge of catching the bug depending on the timing of the check.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-20T10:20:17Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:72** by [@kshalot](https://github.com/kshalot) — 2025-11-20T10:20:17Z
```diff
@@ -0,0 +1,100 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	unreachableNodeName        = "unreachable-node"
+	reachableNodeName          = "reachable-node"
+	forcefulTerminationTimeout = 2 * time.Second
+)
+
+var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var ns *corev1.Namespace
+	var matchingPodWrapper *testingpod.PodWrapper
+
+	ginkgo.BeforeAll(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "pod-fr-namespace-")
+		nodes := []corev1.Node{
+			*testingnode.MakeNode(unreachableNodeName).
+				NotReady().
+				Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule}).
+				Obj(),
+			*testingnode.MakeNode(reachableNodeName).
+				Ready().
+				Obj(),
+		}
+		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+		matchingPodWrapper = testingpod.MakePod("matching-pod", ns.Name).
+			StatusPhase(corev1.PodPending).
+			TerminationGracePeriod(1).
+			NodeName(unreachableNodeName).
+			Annotation(constants.SafeToForcefullyTerminateAnnotationKey, constants.SafeToForcefullyTerminateAnnotationValue)
+	})
+
+	ginkgo.It("forcefully terminates pods that opt-in, scheduled on unreachable nodes", func() {
+		matchingPod := matchingPodWrapper.Clone().Obj()
+		util.MustCreate(ctx, k8sClient, matchingPod)
+		gomega.ExpectWithOffset(1, k8sClient.Delete(ctx, matchingPod)).To(gomega.Succeed())
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: matchingPod.Name, Namespace: matchingPod.Namespace}, matchingPod)).
+				To(gomega.Succeed())
+			g.Expect(matchingPod.Status.Phase).Should(gomega.Equal(corev1.PodFailed))
+		}, forcefulTerminationTimeout, util.Interval).Should(gomega.Succeed())
```

  I think your question raises a valid point about naming, because `forcefulTerminationTimeout` is simply the timeout for eventually/consistently, not the "grace period" used in the feature.
  
  Since the tests don't seem to use the value of the forceful termination grace period, I just set it to `time.Millisecond` [here](https://github.com/kubernetes-sigs/kueue/pull/7312/files#diff-b30886bb4c8e05a117f3a8df12c424a5914ab298b0c402120f35046c1419b90aR63).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-20T10:20:35Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:179** by [@mimowo](https://github.com/mimowo) — 2025-11-20T10:20:35Z
```diff
@@ -0,0 +1,140 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltaints "sigs.k8s.io/kueue/pkg/util/taints"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=get;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var (
+	realClock = clock.RealClock{}
+)
+
+type TerminatingPodReconciler struct {
+	client                         client.Client
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+}
+
+type TerminatingPodReconcilerOptions struct {
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+}
+
+type TerminatingPodReconcilerOption func(*TerminatingPodReconcilerOptions)
+
+func WithClock(c clock.Clock) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+func WithForcefulTerminationGracePeriod(t time.Duration) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.forcefulTerminationGracePeriod = t
+	}
+}
+
+var defaultOptions = TerminatingPodReconcilerOptions{
+	clock:                          realClock,
+	forcefulTerminationGracePeriod: time.Minute,
+}
+
+func NewTerminatingPodReconciler(
+	client client.Client,
+	opts ...TerminatingPodReconcilerOption,
+) (*TerminatingPodReconciler, error) {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	return &TerminatingPodReconciler{
+		client:                         client,
+		clock:                          options.clock,
+		forcefulTerminationGracePeriod: options.forcefulTerminationGracePeriod,
+	}, nil
+}
+
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	pod := &corev1.Pod{}
+	if err := r.client.Get(ctx, req.NamespacedName, pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	// Pod did not opt-in to be forcefully terminated
+	annotationValue, hasAnnotation := pod.Annotations[constants.SafeToForcefullyTerminateAnnotationKey]
+	if !hasAnnotation || annotationValue != constants.SafeToForcefullyTerminateAnnotationValue {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod was not marked for termination
+	if pod.DeletionTimestamp.IsZero() {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod is not in a running phase
+	if utilpod.IsTerminated(pod) {
+		return ctrl.Result{}, nil
+	}
+
+	node := &corev1.Node{}
+	nodeKey := types.NamespacedName{Name: pod.Spec.NodeName}
+	if err := r.client.Get(ctx, nodeKey, node); err != nil {
+		return ctrl.Result{}, err
+	}
+	// Pod is not scheduled on an unreachable node
+	if !utiltaints.TaintKeyExists(node.Spec.Taints, corev1.TaintNodeUnreachable) {
+		return ctrl.Result{}, nil
+	}
+
+	now := r.clock.Now()
+	forcefulTerminationThreshold := pod.DeletionTimestamp.Add(r.forcefulTerminationGracePeriod)
+	if now.Before(forcefulTerminationThreshold) {
+		remainingTime := forcefulTerminationThreshold.Sub(now)
+		return ctrl.Result{RequeueAfter: remainingTime}, nil
+	}
+
+	podPatch := pod.DeepCopy()
+	podPatch.Status.Phase = corev1.PodFailed
```

  This also needs setting the condition KueueFailureRecovery as planned in the KEP

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-20T10:21:04Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:179** by [@kshalot](https://github.com/kshalot) — 2025-11-20T10:21:04Z
```diff
@@ -0,0 +1,140 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltaints "sigs.k8s.io/kueue/pkg/util/taints"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=get;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var (
+	realClock = clock.RealClock{}
+)
+
+type TerminatingPodReconciler struct {
+	client                         client.Client
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+}
+
+type TerminatingPodReconcilerOptions struct {
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+}
+
+type TerminatingPodReconcilerOption func(*TerminatingPodReconcilerOptions)
+
+func WithClock(c clock.Clock) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+func WithForcefulTerminationGracePeriod(t time.Duration) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.forcefulTerminationGracePeriod = t
+	}
+}
+
+var defaultOptions = TerminatingPodReconcilerOptions{
+	clock:                          realClock,
+	forcefulTerminationGracePeriod: time.Minute,
+}
+
+func NewTerminatingPodReconciler(
+	client client.Client,
+	opts ...TerminatingPodReconcilerOption,
+) (*TerminatingPodReconciler, error) {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	return &TerminatingPodReconciler{
+		client:                         client,
+		clock:                          options.clock,
+		forcefulTerminationGracePeriod: options.forcefulTerminationGracePeriod,
+	}, nil
+}
+
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	pod := &corev1.Pod{}
+	if err := r.client.Get(ctx, req.NamespacedName, pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	// Pod did not opt-in to be forcefully terminated
+	annotationValue, hasAnnotation := pod.Annotations[constants.SafeToForcefullyTerminateAnnotationKey]
+	if !hasAnnotation || annotationValue != constants.SafeToForcefullyTerminateAnnotationValue {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod was not marked for termination
+	if pod.DeletionTimestamp.IsZero() {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod is not in a running phase
+	if utilpod.IsTerminated(pod) {
+		return ctrl.Result{}, nil
+	}
+
+	node := &corev1.Node{}
+	nodeKey := types.NamespacedName{Name: pod.Spec.NodeName}
+	if err := r.client.Get(ctx, nodeKey, node); err != nil {
+		return ctrl.Result{}, err
+	}
+	// Pod is not scheduled on an unreachable node
+	if !utiltaints.TaintKeyExists(node.Spec.Taints, corev1.TaintNodeUnreachable) {
+		return ctrl.Result{}, nil
+	}
+
+	now := r.clock.Now()
+	forcefulTerminationThreshold := pod.DeletionTimestamp.Add(r.forcefulTerminationGracePeriod)
+	if now.Before(forcefulTerminationThreshold) {
+		remainingTime := forcefulTerminationThreshold.Sub(now)
+		return ctrl.Result{RequeueAfter: remainingTime}, nil
+	}
+
+	podPatch := pod.DeepCopy()
+	podPatch.Status.Phase = corev1.PodFailed
```

  Yes, and also send an event. WIP

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-20T10:23:24Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:108** by [@mimowo](https://github.com/mimowo) — 2025-11-20T10:23:24Z
```diff
@@ -0,0 +1,140 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltaints "sigs.k8s.io/kueue/pkg/util/taints"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=get;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var (
+	realClock = clock.RealClock{}
+)
+
+type TerminatingPodReconciler struct {
+	client                         client.Client
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+}
+
+type TerminatingPodReconcilerOptions struct {
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+}
+
+type TerminatingPodReconcilerOption func(*TerminatingPodReconcilerOptions)
+
+func WithClock(c clock.Clock) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+func WithForcefulTerminationGracePeriod(t time.Duration) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.forcefulTerminationGracePeriod = t
+	}
+}
+
+var defaultOptions = TerminatingPodReconcilerOptions{
+	clock:                          realClock,
+	forcefulTerminationGracePeriod: time.Minute,
+}
+
+func NewTerminatingPodReconciler(
+	client client.Client,
+	opts ...TerminatingPodReconcilerOption,
+) (*TerminatingPodReconciler, error) {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	return &TerminatingPodReconciler{
+		client:                         client,
+		clock:                          options.clock,
+		forcefulTerminationGracePeriod: options.forcefulTerminationGracePeriod,
+	}, nil
+}
+
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	pod := &corev1.Pod{}
+	if err := r.client.Get(ctx, req.NamespacedName, pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	// Pod did not opt-in to be forcefully terminated
+	annotationValue, hasAnnotation := pod.Annotations[constants.SafeToForcefullyTerminateAnnotationKey]
+	if !hasAnnotation || annotationValue != constants.SafeToForcefullyTerminateAnnotationValue {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod was not marked for termination
+	if pod.DeletionTimestamp.IsZero() {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod is not in a running phase
+	if utilpod.IsTerminated(pod) {
+		return ctrl.Result{}, nil
+	}
```

  Potential performance issue, running the Reconcile is relatively heavy, because the pool of workers is rather limited. 
  
  Thus, it is generally preferred to check preconditions in the event handler, which enqueues the reconcile if preconditions are met.
  
  Fetching the Node and checking node should be part of the Reconcile, but all checks above can be lightweight preconditions.
  
  I would say premature optimization when it was about rare objects, but for Pod you can have literally 200k of them in a cluster or more.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-20T10:26:26Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:72** by [@mimowo](https://github.com/mimowo) — 2025-11-20T10:26:26Z
```diff
@@ -0,0 +1,100 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	unreachableNodeName        = "unreachable-node"
+	reachableNodeName          = "reachable-node"
+	forcefulTerminationTimeout = 2 * time.Second
+)
+
+var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var ns *corev1.Namespace
+	var matchingPodWrapper *testingpod.PodWrapper
+
+	ginkgo.BeforeAll(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "pod-fr-namespace-")
+		nodes := []corev1.Node{
+			*testingnode.MakeNode(unreachableNodeName).
+				NotReady().
+				Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule}).
+				Obj(),
+			*testingnode.MakeNode(reachableNodeName).
+				Ready().
+				Obj(),
+		}
+		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+		matchingPodWrapper = testingpod.MakePod("matching-pod", ns.Name).
+			StatusPhase(corev1.PodPending).
+			TerminationGracePeriod(1).
+			NodeName(unreachableNodeName).
+			Annotation(constants.SafeToForcefullyTerminateAnnotationKey, constants.SafeToForcefullyTerminateAnnotationValue)
+	})
+
+	ginkgo.It("forcefully terminates pods that opt-in, scheduled on unreachable nodes", func() {
+		matchingPod := matchingPodWrapper.Clone().Obj()
+		util.MustCreate(ctx, k8sClient, matchingPod)
+		gomega.ExpectWithOffset(1, k8sClient.Delete(ctx, matchingPod)).To(gomega.Succeed())
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: matchingPod.Name, Namespace: matchingPod.Namespace}, matchingPod)).
+				To(gomega.Succeed())
+			g.Expect(matchingPod.Status.Phase).Should(gomega.Equal(corev1.PodFailed))
+		}, forcefulTerminationTimeout, util.Interval).Should(gomega.Succeed())
```

  Ah, yes, I interpreted the name as the constant as also used by the feature. Let me check again.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-20T11:51:08Z

- **pkg/controller/failurerecovery/pod_termination_controller_test.go:169** by [@mimowo](https://github.com/mimowo) — 2025-11-20T11:51:08Z
```diff
@@ -0,0 +1,177 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"fmt"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	testingclock "k8s.io/utils/clock/testing"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+)
+
+var (
+	podCmpOpts = cmp.Options{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreFields(
+			corev1.Pod{}, "ObjectMeta.ResourceVersion", "ObjectMeta.DeletionTimestamp",
+		),
+	}
+)
+
+func TestReconciler(t *testing.T) {
+	now := time.Now()
+	nowSecondPrecision := metav1.NewTime(now).Rfc3339Copy()
+	beforeGracePeriod := now.Add(-time.Minute * 3)
+	fakeClock := testingclock.NewFakeClock(now)
+
+	unreachableNode := testingnode.MakeNode("unreachable-node").
+		Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable}).Obj()
+	healthyNode := testingnode.MakeNode("healthy-node").Obj()
+	podToForcefullyTerminate := testingpod.MakePod("pod", "").
+		StatusPhase(corev1.PodRunning).
+		Annotation(constants.SafeToForcefullyTerminateAnnotationKey, constants.SafeToForcefullyTerminateAnnotationValue).
+		NodeName(unreachableNode.Name).
+		DeletionTimestamp(beforeGracePeriod).
+		KueueFinalizer()
+
+	cases := map[string]struct {
+		testPod    *corev1.Pod
```

  Let's split this into `pod` and `wantPod`. Always supplied to avoid tricks in https://github.com/kubernetes-sigs/kueue/pull/7312/files#diff-547b122e7c9dbc55a906f70d85ca16010acd456bb20e02158debbd9cd1b23a46R168-R171
  
  Yes, this is more lines of test code, but very declarative in nature

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-20T13:41:28Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:108** by [@kshalot](https://github.com/kshalot) — 2025-11-20T13:41:28Z
```diff
@@ -0,0 +1,140 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltaints "sigs.k8s.io/kueue/pkg/util/taints"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=get;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var (
+	realClock = clock.RealClock{}
+)
+
+type TerminatingPodReconciler struct {
+	client                         client.Client
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+}
+
+type TerminatingPodReconcilerOptions struct {
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+}
+
+type TerminatingPodReconcilerOption func(*TerminatingPodReconcilerOptions)
+
+func WithClock(c clock.Clock) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+func WithForcefulTerminationGracePeriod(t time.Duration) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.forcefulTerminationGracePeriod = t
+	}
+}
+
+var defaultOptions = TerminatingPodReconcilerOptions{
+	clock:                          realClock,
+	forcefulTerminationGracePeriod: time.Minute,
+}
+
+func NewTerminatingPodReconciler(
+	client client.Client,
+	opts ...TerminatingPodReconcilerOption,
+) (*TerminatingPodReconciler, error) {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	return &TerminatingPodReconciler{
+		client:                         client,
+		clock:                          options.clock,
+		forcefulTerminationGracePeriod: options.forcefulTerminationGracePeriod,
+	}, nil
+}
+
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	pod := &corev1.Pod{}
+	if err := r.client.Get(ctx, req.NamespacedName, pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	// Pod did not opt-in to be forcefully terminated
+	annotationValue, hasAnnotation := pod.Annotations[constants.SafeToForcefullyTerminateAnnotationKey]
+	if !hasAnnotation || annotationValue != constants.SafeToForcefullyTerminateAnnotationValue {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod was not marked for termination
+	if pod.DeletionTimestamp.IsZero() {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod is not in a running phase
+	if utilpod.IsTerminated(pod) {
+		return ctrl.Result{}, nil
+	}
```

  Good point. PTAL at the event filters in https://github.com/kubernetes-sigs/kueue/pull/7312/commits/3f11131e3c9cb666c8ab4627206ae1e3deaaa7c1.
  
  I'm wondering about the need for the "Create" filter. IIUC in theory in a pod is created and deleted very quickly, the filter will only see the create event, so in theory it's needed (easy to reproduce with integration tests).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-20T13:51:34Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:158** by [@mimowo](https://github.com/mimowo) — 2025-11-20T13:51:34Z
  It seems this check should be moved above the Node checking, because more lightweight

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-20T14:24:19Z

- **pkg/controller/failurerecovery/pod_termination_controller_test.go:169** by [@kshalot](https://github.com/kshalot) — 2025-11-20T14:24:19Z
```diff
@@ -0,0 +1,177 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"fmt"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	testingclock "k8s.io/utils/clock/testing"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+)
+
+var (
+	podCmpOpts = cmp.Options{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreFields(
+			corev1.Pod{}, "ObjectMeta.ResourceVersion", "ObjectMeta.DeletionTimestamp",
+		),
+	}
+)
+
+func TestReconciler(t *testing.T) {
+	now := time.Now()
+	nowSecondPrecision := metav1.NewTime(now).Rfc3339Copy()
+	beforeGracePeriod := now.Add(-time.Minute * 3)
+	fakeClock := testingclock.NewFakeClock(now)
+
+	unreachableNode := testingnode.MakeNode("unreachable-node").
+		Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable}).Obj()
+	healthyNode := testingnode.MakeNode("healthy-node").Obj()
+	podToForcefullyTerminate := testingpod.MakePod("pod", "").
+		StatusPhase(corev1.PodRunning).
+		Annotation(constants.SafeToForcefullyTerminateAnnotationKey, constants.SafeToForcefullyTerminateAnnotationValue).
+		NodeName(unreachableNode.Name).
+		DeletionTimestamp(beforeGracePeriod).
+		KueueFinalizer()
+
+	cases := map[string]struct {
+		testPod    *corev1.Pod
```

  `wantPod` was already in the struct, it was just optional because most cases expected `wantPod == testPod`. I made it explicit in https://github.com/kubernetes-sigs/kueue/pull/7312/commits/606dcc6d309252c2cdfecff15efc7950a4ba7dcd.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-20T14:39:35Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:108** by [@mimowo](https://github.com/mimowo) — 2025-11-20T14:39:35Z
```diff
@@ -0,0 +1,140 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltaints "sigs.k8s.io/kueue/pkg/util/taints"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=get;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var (
+	realClock = clock.RealClock{}
+)
+
+type TerminatingPodReconciler struct {
+	client                         client.Client
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+}
+
+type TerminatingPodReconcilerOptions struct {
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+}
+
+type TerminatingPodReconcilerOption func(*TerminatingPodReconcilerOptions)
+
+func WithClock(c clock.Clock) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+func WithForcefulTerminationGracePeriod(t time.Duration) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.forcefulTerminationGracePeriod = t
+	}
+}
+
+var defaultOptions = TerminatingPodReconcilerOptions{
+	clock:                          realClock,
+	forcefulTerminationGracePeriod: time.Minute,
+}
+
+func NewTerminatingPodReconciler(
+	client client.Client,
+	opts ...TerminatingPodReconcilerOption,
+) (*TerminatingPodReconciler, error) {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	return &TerminatingPodReconciler{
+		client:                         client,
+		clock:                          options.clock,
+		forcefulTerminationGracePeriod: options.forcefulTerminationGracePeriod,
+	}, nil
+}
+
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	pod := &corev1.Pod{}
+	if err := r.client.Get(ctx, req.NamespacedName, pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	// Pod did not opt-in to be forcefully terminated
+	annotationValue, hasAnnotation := pod.Annotations[constants.SafeToForcefullyTerminateAnnotationKey]
+	if !hasAnnotation || annotationValue != constants.SafeToForcefullyTerminateAnnotationValue {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod was not marked for termination
+	if pod.DeletionTimestamp.IsZero() {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod is not in a running phase
+	if utilpod.IsTerminated(pod) {
+		return ctrl.Result{}, nil
+	}
```

  We need to also add the code to Create, the reason is that this event is called on Kueue restart. This is always tricky because the event is called also for preexisting resources for which just the informer receives the event

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-20T14:54:10Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:108** by [@kshalot](https://github.com/kshalot) — 2025-11-20T14:54:10Z
```diff
@@ -0,0 +1,140 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltaints "sigs.k8s.io/kueue/pkg/util/taints"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=get;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var (
+	realClock = clock.RealClock{}
+)
+
+type TerminatingPodReconciler struct {
+	client                         client.Client
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+}
+
+type TerminatingPodReconcilerOptions struct {
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+}
+
+type TerminatingPodReconcilerOption func(*TerminatingPodReconcilerOptions)
+
+func WithClock(c clock.Clock) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+func WithForcefulTerminationGracePeriod(t time.Duration) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.forcefulTerminationGracePeriod = t
+	}
+}
+
+var defaultOptions = TerminatingPodReconcilerOptions{
+	clock:                          realClock,
+	forcefulTerminationGracePeriod: time.Minute,
+}
+
+func NewTerminatingPodReconciler(
+	client client.Client,
+	opts ...TerminatingPodReconcilerOption,
+) (*TerminatingPodReconciler, error) {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	return &TerminatingPodReconciler{
+		client:                         client,
+		clock:                          options.clock,
+		forcefulTerminationGracePeriod: options.forcefulTerminationGracePeriod,
+	}, nil
+}
+
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	pod := &corev1.Pod{}
+	if err := r.client.Get(ctx, req.NamespacedName, pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	// Pod did not opt-in to be forcefully terminated
+	annotationValue, hasAnnotation := pod.Annotations[constants.SafeToForcefullyTerminateAnnotationKey]
+	if !hasAnnotation || annotationValue != constants.SafeToForcefullyTerminateAnnotationValue {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod was not marked for termination
+	if pod.DeletionTimestamp.IsZero() {
+		return ctrl.Result{}, nil
+	}
+
+	// Pod is not in a running phase
+	if utilpod.IsTerminated(pod) {
+		return ctrl.Result{}, nil
+	}
```

  Good to know. Anyway, the Create is handled already

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-20T15:36:13Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:72** by [@kshalot](https://github.com/kshalot) — 2025-11-20T15:36:13Z
```diff
@@ -0,0 +1,100 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	unreachableNodeName        = "unreachable-node"
+	reachableNodeName          = "reachable-node"
+	forcefulTerminationTimeout = 2 * time.Second
+)
+
+var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var ns *corev1.Namespace
+	var matchingPodWrapper *testingpod.PodWrapper
+
+	ginkgo.BeforeAll(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "pod-fr-namespace-")
+		nodes := []corev1.Node{
+			*testingnode.MakeNode(unreachableNodeName).
+				NotReady().
+				Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule}).
+				Obj(),
+			*testingnode.MakeNode(reachableNodeName).
+				Ready().
+				Obj(),
+		}
+		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+		matchingPodWrapper = testingpod.MakePod("matching-pod", ns.Name).
+			StatusPhase(corev1.PodPending).
+			TerminationGracePeriod(1).
+			NodeName(unreachableNodeName).
+			Annotation(constants.SafeToForcefullyTerminateAnnotationKey, constants.SafeToForcefullyTerminateAnnotationValue)
+	})
+
+	ginkgo.It("forcefully terminates pods that opt-in, scheduled on unreachable nodes", func() {
+		matchingPod := matchingPodWrapper.Clone().Obj()
+		util.MustCreate(ctx, k8sClient, matchingPod)
+		gomega.ExpectWithOffset(1, k8sClient.Delete(ctx, matchingPod)).To(gomega.Succeed())
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: matchingPod.Name, Namespace: matchingPod.Namespace}, matchingPod)).
+				To(gomega.Succeed())
+			g.Expect(matchingPod.Status.Phase).Should(gomega.Equal(corev1.PodFailed))
+		}, forcefulTerminationTimeout, util.Interval).Should(gomega.Succeed())
```

  FYI I renamed the test timeout to `forcefulTerminationCheckTimeout`.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-20T15:39:05Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:102** by [@kshalot](https://github.com/kshalot) — 2025-11-20T15:39:05Z
```diff
@@ -0,0 +1,104 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	unreachableNodeName        = "unreachable-node"
+	reachableNodeName          = "reachable-node"
+	forcefulTerminationTimeout = 2 * time.Second
+)
+
+func createTerminatingPod(p *corev1.Pod) {
+	ginkgo.GinkgoHelper()
+
+	util.MustCreate(ctx, k8sClient, p)
+	gomega.ExpectWithOffset(1, k8sClient.Delete(ctx, p)).To(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var ns *corev1.Namespace
+	var matchingPodWrapper *testingpod.PodWrapper
+
+	ginkgo.BeforeAll(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "pod-fr-namespace-")
+		nodes := []corev1.Node{
+			*testingnode.MakeNode(unreachableNodeName).
+				NotReady().
+				Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule}).
+				Obj(),
+			*testingnode.MakeNode(reachableNodeName).
+				Ready().
+				Obj(),
+		}
+		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+		matchingPodWrapper = testingpod.MakePod("matching-pod", ns.Name).
+			StatusPhase(corev1.PodPending).
+			TerminationGracePeriod(1).
+			NodeName(unreachableNodeName).
+			Annotation(constants.SafeToForcefullyTerminateAnnotationKey, constants.SafeToForcefullyTerminateAnnotationValue)
+	})
+
+	ginkgo.It("forcefully terminates pods that opt-in, scheduled on unreachable nodes", func() {
+		matchingPod := matchingPodWrapper.Clone().Obj()
+		createTerminatingPod(matchingPod)
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: matchingPod.Name, Namespace: matchingPod.Namespace}, matchingPod)).
+				To(gomega.Succeed())
+			g.Expect(matchingPod.Status.Phase).Should(gomega.Equal(corev1.PodFailed))
+		}, forcefulTerminationTimeout, util.Interval).Should(gomega.Succeed())
+	})
+
+	ginkgo.It("does not forcefully terminate matching pods that did not opt-in or are running on healthy nodes", func() {
+		nonMatchingPod := matchingPodWrapper.
+			Clone().
+			Name("non-matching-pod").
+			Annotation(constants.SafeToForcefullyTerminateAnnotationKey, constants.SafeToForcefullyTerminateAnnotationValue).
+			Obj()
+		createTerminatingPod(nonMatchingPod)
+
+		podOnHealthyNode := matchingPodWrapper.Clone().Name("healthy-pod").NodeName(reachableNodeName).Obj()
+		createTerminatingPod(podOnHealthyNode)
+
+		gomega.Consistently(func(g gomega.Gomega) {
+			// Non-matching pod is left untouched.
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: nonMatchingPod.Name, Namespace: nonMatchingPod.Namespace}, nonMatchingPod)).
+				To(gomega.Succeed())
+			g.Expect(nonMatchingPod.Status.Phase).Should(gomega.Equal(corev1.PodPending))
+
+			// Healthy pod is left untouched.
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: podOnHealthyNode.Name, Namespace: podOnHealthyNode.Namespace}, podOnHealthyNode)).
+				To(gomega.Succeed())
+			g.Expect(podOnHealthyNode.Status.Phase).Should(gomega.Equal(corev1.PodPending))
+		}, forcefulTerminationTimeout, util.Interval).Should(gomega.Succeed())
```

  That's why I made the check timeout 2 seconds instead of lower:
  1. We have to use 1 second for the deletion grace period.
  2. I set the forceful termination grace period to 1 millisecond.
  
  So technically, there are `2s - 1s - 1ms = 999ms` for the forceful termination to occur and be seen in the test. Since the check interval is 250ms, there are going to be 2/3 checks "after" the termination should have occurred.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-21T07:08:33Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:117** by [@mimowo](https://github.com/mimowo) — 2025-11-21T07:08:34Z
```diff
@@ -0,0 +1,199 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"fmt"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltaints "sigs.k8s.io/kueue/pkg/util/taints"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=get;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var realClock = clock.RealClock{}
+
+const (
+	KueueFailureRecoveryConditionType = "KueueFailureRecovery"
+	KueueForcefulTerminationReason    = "KueueForcefullyTerminated"
+)
+
+type TerminatingPodReconciler struct {
+	client                         client.Client
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+	recorder                       record.EventRecorder
+}
+
+type TerminatingPodReconcilerOptions struct {
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+}
+
+type TerminatingPodReconcilerOption func(*TerminatingPodReconcilerOptions)
+
+func WithClock(c clock.Clock) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+func WithForcefulTerminationGracePeriod(t time.Duration) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.forcefulTerminationGracePeriod = t
+	}
+}
+
+var defaultOptions = TerminatingPodReconcilerOptions{
+	clock:                          realClock,
+	forcefulTerminationGracePeriod: time.Minute,
+}
+
+func NewTerminatingPodReconciler(
+	client client.Client,
+	recorder record.EventRecorder,
+	opts ...TerminatingPodReconcilerOption,
+) (*TerminatingPodReconciler, error) {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	return &TerminatingPodReconciler{
+		client:                         client,
+		clock:                          options.clock,
+		forcefulTerminationGracePeriod: options.forcefulTerminationGracePeriod,
+		recorder:                       recorder,
+	}, nil
+}
+
+func (r *TerminatingPodReconciler) Generic(event.GenericEvent) bool {
+	return false
+}
+
+func (r *TerminatingPodReconciler) Create(e event.CreateEvent) bool {
+	pod := e.Object.(*corev1.Pod)
+
+	if !podOptedInToFailurePolicy(pod) {
+		return false
+	}
+
+	if pod.DeletionTimestamp.IsZero() {
+		return false
+	}
+
+	return true
+}
+
+func (r *TerminatingPodReconciler) Update(u event.UpdateEvent) bool {
+	oldPod := u.ObjectOld.(*corev1.Pod)
```

  IIUC these castings are required for the oldschool registration of filtering, check how this is done in cohort controller to use strong typing which then does not require explicit casting in code: https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/core/cohort_controller.go#L109-L130

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-21T07:10:20Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:186** by [@mimowo](https://github.com/mimowo) — 2025-11-21T07:10:20Z
```diff
@@ -0,0 +1,199 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"fmt"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltaints "sigs.k8s.io/kueue/pkg/util/taints"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=get;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var realClock = clock.RealClock{}
+
+const (
+	KueueFailureRecoveryConditionType = "KueueFailureRecovery"
+	KueueForcefulTerminationReason    = "KueueForcefullyTerminated"
+)
+
+type TerminatingPodReconciler struct {
+	client                         client.Client
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+	recorder                       record.EventRecorder
+}
+
+type TerminatingPodReconcilerOptions struct {
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+}
+
+type TerminatingPodReconcilerOption func(*TerminatingPodReconcilerOptions)
+
+func WithClock(c clock.Clock) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+func WithForcefulTerminationGracePeriod(t time.Duration) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.forcefulTerminationGracePeriod = t
+	}
+}
+
+var defaultOptions = TerminatingPodReconcilerOptions{
+	clock:                          realClock,
+	forcefulTerminationGracePeriod: time.Minute,
+}
+
+func NewTerminatingPodReconciler(
+	client client.Client,
+	recorder record.EventRecorder,
+	opts ...TerminatingPodReconcilerOption,
+) (*TerminatingPodReconciler, error) {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	return &TerminatingPodReconciler{
+		client:                         client,
+		clock:                          options.clock,
+		forcefulTerminationGracePeriod: options.forcefulTerminationGracePeriod,
+		recorder:                       recorder,
+	}, nil
+}
+
+func (r *TerminatingPodReconciler) Generic(event.GenericEvent) bool {
+	return false
+}
+
+func (r *TerminatingPodReconciler) Create(e event.CreateEvent) bool {
+	pod := e.Object.(*corev1.Pod)
+
+	if !podOptedInToFailurePolicy(pod) {
+		return false
+	}
+
+	if pod.DeletionTimestamp.IsZero() {
+		return false
+	}
+
+	return true
+}
+
+func (r *TerminatingPodReconciler) Update(u event.UpdateEvent) bool {
+	oldPod := u.ObjectOld.(*corev1.Pod)
+	newPod := u.ObjectNew.(*corev1.Pod)
+
+	if !podOptedInToFailurePolicy(newPod) {
+		return false
+	}
+
+	// Pod was not marked for deletion in the update
+	if !oldPod.DeletionTimestamp.IsZero() || newPod.DeletionTimestamp.IsZero() {
+		return false
+	}
+
+	return true
+}
+
+func (r *TerminatingPodReconciler) Delete(event.DeleteEvent) bool {
+	return false
+}
+
+func podOptedInToFailurePolicy(p *corev1.Pod) bool {
+	annotationValue, hasAnnotation := p.Annotations[constants.SafeToForcefullyTerminateAnnotationKey]
+	return hasAnnotation && annotationValue == constants.SafeToForcefullyTerminateAnnotationValue
+}
+
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	pod := &corev1.Pod{}
+	if err := r.client.Get(ctx, req.NamespacedName, pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	// Pod was already terminated
+	if utilpod.IsTerminated(pod) {
+		return ctrl.Result{}, nil
+	}
+
+	// Forceful termination threshold not reached
+	now := r.clock.Now()
+	forcefulTerminationThreshold := pod.DeletionTimestamp.Add(r.forcefulTerminationGracePeriod)
+	if now.Before(forcefulTerminationThreshold) {
+		remainingTime := forcefulTerminationThreshold.Sub(now)
+		return ctrl.Result{RequeueAfter: remainingTime}, nil
+	}
+
+	node := &corev1.Node{}
+	nodeKey := types.NamespacedName{Name: pod.Spec.NodeName}
+	if err := r.client.Get(ctx, nodeKey, node); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	// Pod is not scheduled on an unreachable node
+	if !utiltaints.TaintKeyExists(node.Spec.Taints, corev1.TaintNodeUnreachable) {
+		return ctrl.Result{}, nil
+	}
+
+	totalDeletionGracePeriod := time.Duration(ptr.Deref(pod.DeletionGracePeriodSeconds, 0)) + r.forcefulTerminationGracePeriod
+	eventMessage := fmt.Sprintf(
+		"Pod forcefully terminated after %s grace period due to unreachable node `%s` (triggered by `%s` annotation)",
+		totalDeletionGracePeriod,
+		node.Name,
+		constants.SafeToForcefullyTerminateAnnotationKey,
+	)
+
+	podPatch := pod.DeepCopy()
+	podPatch.Status.Phase = corev1.PodFailed
+	podPatch.Status.Conditions = append(podPatch.Status.Conditions, corev1.PodCondition{
+		Type:    KueueFailureRecoveryConditionType,
+		Status:  corev1.ConditionTrue,
+		Reason:  KueueForcefulTerminationReason,
+		Message: eventMessage,
+	})
+	if err := r.client.Status().Patch(ctx, podPatch, client.MergeFrom(pod)); err != nil {
```

  This patch may potentially override some conditions or status changes done concurrently by another controller. To avoid that use our helper in clientutil which supports "strict" mode which compares the ResrouceVersion.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-21T07:11:09Z

- **pkg/controller/failurerecovery/pod_termination_controller_test.go:169** by [@mimowo](https://github.com/mimowo) — 2025-11-21T07:11:09Z
```diff
@@ -0,0 +1,177 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"fmt"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	testingclock "k8s.io/utils/clock/testing"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+)
+
+var (
+	podCmpOpts = cmp.Options{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreFields(
+			corev1.Pod{}, "ObjectMeta.ResourceVersion", "ObjectMeta.DeletionTimestamp",
+		),
+	}
+)
+
+func TestReconciler(t *testing.T) {
+	now := time.Now()
+	nowSecondPrecision := metav1.NewTime(now).Rfc3339Copy()
+	beforeGracePeriod := now.Add(-time.Minute * 3)
+	fakeClock := testingclock.NewFakeClock(now)
+
+	unreachableNode := testingnode.MakeNode("unreachable-node").
+		Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable}).Obj()
+	healthyNode := testingnode.MakeNode("healthy-node").Obj()
+	podToForcefullyTerminate := testingpod.MakePod("pod", "").
+		StatusPhase(corev1.PodRunning).
+		Annotation(constants.SafeToForcefullyTerminateAnnotationKey, constants.SafeToForcefullyTerminateAnnotationValue).
+		NodeName(unreachableNode.Name).
+		DeletionTimestamp(beforeGracePeriod).
+		KueueFinalizer()
+
+	cases := map[string]struct {
+		testPod    *corev1.Pod
```

  lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-21T07:11:49Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:72** by [@mimowo](https://github.com/mimowo) — 2025-11-21T07:11:49Z
```diff
@@ -0,0 +1,100 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	unreachableNodeName        = "unreachable-node"
+	reachableNodeName          = "reachable-node"
+	forcefulTerminationTimeout = 2 * time.Second
+)
+
+var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var ns *corev1.Namespace
+	var matchingPodWrapper *testingpod.PodWrapper
+
+	ginkgo.BeforeAll(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "pod-fr-namespace-")
+		nodes := []corev1.Node{
+			*testingnode.MakeNode(unreachableNodeName).
+				NotReady().
+				Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule}).
+				Obj(),
+			*testingnode.MakeNode(reachableNodeName).
+				Ready().
+				Obj(),
+		}
+		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+		matchingPodWrapper = testingpod.MakePod("matching-pod", ns.Name).
+			StatusPhase(corev1.PodPending).
+			TerminationGracePeriod(1).
+			NodeName(unreachableNodeName).
+			Annotation(constants.SafeToForcefullyTerminateAnnotationKey, constants.SafeToForcefullyTerminateAnnotationValue)
+	})
+
+	ginkgo.It("forcefully terminates pods that opt-in, scheduled on unreachable nodes", func() {
+		matchingPod := matchingPodWrapper.Clone().Obj()
+		util.MustCreate(ctx, k8sClient, matchingPod)
+		gomega.ExpectWithOffset(1, k8sClient.Delete(ctx, matchingPod)).To(gomega.Succeed())
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: matchingPod.Name, Namespace: matchingPod.Namespace}, matchingPod)).
+				To(gomega.Succeed())
+			g.Expect(matchingPod.Status.Phase).Should(gomega.Equal(corev1.PodFailed))
+		}, forcefulTerminationTimeout, util.Interval).Should(gomega.Succeed())
```

  sgtm, just check locally the tests don't flake by running them in a loop something like 50 times. We don't want introducing flakes before release

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-21T07:12:09Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:102** by [@mimowo](https://github.com/mimowo) — 2025-11-21T07:12:09Z
```diff
@@ -0,0 +1,104 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	unreachableNodeName        = "unreachable-node"
+	reachableNodeName          = "reachable-node"
+	forcefulTerminationTimeout = 2 * time.Second
+)
+
+func createTerminatingPod(p *corev1.Pod) {
+	ginkgo.GinkgoHelper()
+
+	util.MustCreate(ctx, k8sClient, p)
+	gomega.ExpectWithOffset(1, k8sClient.Delete(ctx, p)).To(gomega.Succeed())
+}
+
+var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var ns *corev1.Namespace
+	var matchingPodWrapper *testingpod.PodWrapper
+
+	ginkgo.BeforeAll(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "pod-fr-namespace-")
+		nodes := []corev1.Node{
+			*testingnode.MakeNode(unreachableNodeName).
+				NotReady().
+				Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule}).
+				Obj(),
+			*testingnode.MakeNode(reachableNodeName).
+				Ready().
+				Obj(),
+		}
+		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+		matchingPodWrapper = testingpod.MakePod("matching-pod", ns.Name).
+			StatusPhase(corev1.PodPending).
+			TerminationGracePeriod(1).
+			NodeName(unreachableNodeName).
+			Annotation(constants.SafeToForcefullyTerminateAnnotationKey, constants.SafeToForcefullyTerminateAnnotationValue)
+	})
+
+	ginkgo.It("forcefully terminates pods that opt-in, scheduled on unreachable nodes", func() {
+		matchingPod := matchingPodWrapper.Clone().Obj()
+		createTerminatingPod(matchingPod)
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: matchingPod.Name, Namespace: matchingPod.Namespace}, matchingPod)).
+				To(gomega.Succeed())
+			g.Expect(matchingPod.Status.Phase).Should(gomega.Equal(corev1.PodFailed))
+		}, forcefulTerminationTimeout, util.Interval).Should(gomega.Succeed())
+	})
+
+	ginkgo.It("does not forcefully terminate matching pods that did not opt-in or are running on healthy nodes", func() {
+		nonMatchingPod := matchingPodWrapper.
+			Clone().
+			Name("non-matching-pod").
+			Annotation(constants.SafeToForcefullyTerminateAnnotationKey, constants.SafeToForcefullyTerminateAnnotationValue).
+			Obj()
+		createTerminatingPod(nonMatchingPod)
+
+		podOnHealthyNode := matchingPodWrapper.Clone().Name("healthy-pod").NodeName(reachableNodeName).Obj()
+		createTerminatingPod(podOnHealthyNode)
+
+		gomega.Consistently(func(g gomega.Gomega) {
+			// Non-matching pod is left untouched.
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: nonMatchingPod.Name, Namespace: nonMatchingPod.Namespace}, nonMatchingPod)).
+				To(gomega.Succeed())
+			g.Expect(nonMatchingPod.Status.Phase).Should(gomega.Equal(corev1.PodPending))
+
+			// Healthy pod is left untouched.
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: podOnHealthyNode.Name, Namespace: podOnHealthyNode.Namespace}, podOnHealthyNode)).
+				To(gomega.Succeed())
+			g.Expect(podOnHealthyNode.Status.Phase).Should(gomega.Equal(corev1.PodPending))
+		}, forcefulTerminationTimeout, util.Interval).Should(gomega.Succeed())
```

  sgtm, just ask to verify no flakes https://github.com/kubernetes-sigs/kueue/pull/7312#discussion_r2548770248

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-21T07:47:26Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:148** by [@mimowo](https://github.com/mimowo) — 2025-11-21T07:47:26Z
```diff
@@ -0,0 +1,199 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"fmt"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltaints "sigs.k8s.io/kueue/pkg/util/taints"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=get;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var realClock = clock.RealClock{}
+
+const (
+	KueueFailureRecoveryConditionType = "KueueFailureRecovery"
+	KueueForcefulTerminationReason    = "KueueForcefullyTerminated"
+)
+
+type TerminatingPodReconciler struct {
+	client                         client.Client
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+	recorder                       record.EventRecorder
+}
+
+type TerminatingPodReconcilerOptions struct {
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+}
+
+type TerminatingPodReconcilerOption func(*TerminatingPodReconcilerOptions)
+
+func WithClock(c clock.Clock) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+func WithForcefulTerminationGracePeriod(t time.Duration) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.forcefulTerminationGracePeriod = t
+	}
+}
+
+var defaultOptions = TerminatingPodReconcilerOptions{
+	clock:                          realClock,
+	forcefulTerminationGracePeriod: time.Minute,
+}
+
+func NewTerminatingPodReconciler(
+	client client.Client,
+	recorder record.EventRecorder,
+	opts ...TerminatingPodReconcilerOption,
+) (*TerminatingPodReconciler, error) {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	return &TerminatingPodReconciler{
+		client:                         client,
+		clock:                          options.clock,
+		forcefulTerminationGracePeriod: options.forcefulTerminationGracePeriod,
+		recorder:                       recorder,
+	}, nil
+}
+
+func (r *TerminatingPodReconciler) Generic(event.GenericEvent) bool {
+	return false
+}
+
+func (r *TerminatingPodReconciler) Create(e event.CreateEvent) bool {
+	pod := e.Object.(*corev1.Pod)
+
+	if !podOptedInToFailurePolicy(pod) {
+		return false
+	}
+
+	if pod.DeletionTimestamp.IsZero() {
+		return false
+	}
+
+	return true
+}
+
+func (r *TerminatingPodReconciler) Update(u event.UpdateEvent) bool {
+	oldPod := u.ObjectOld.(*corev1.Pod)
+	newPod := u.ObjectNew.(*corev1.Pod)
+
+	if !podOptedInToFailurePolicy(newPod) {
+		return false
+	}
+
+	// Pod was not marked for deletion in the update
+	if !oldPod.DeletionTimestamp.IsZero() || newPod.DeletionTimestamp.IsZero() {
+		return false
+	}
+
+	return true
+}
+
+func (r *TerminatingPodReconciler) Delete(event.DeleteEvent) bool {
+	return false
+}
+
+func podOptedInToFailurePolicy(p *corev1.Pod) bool {
+	annotationValue, hasAnnotation := p.Annotations[constants.SafeToForcefullyTerminateAnnotationKey]
+	return hasAnnotation && annotationValue == constants.SafeToForcefullyTerminateAnnotationValue
+}
+
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	pod := &corev1.Pod{}
+	if err := r.client.Get(ctx, req.NamespacedName, pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	// Pod was already terminated
+	if utilpod.IsTerminated(pod) {
```

  Add the check to the event handlers too. And wrap all the Pod related checks to a helper
  `func preconditionsMet(p *corev1.Pod) bool`, feel free to change the naming.
  Then from Update call it for the `newPod`

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-21T13:30:14Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:72** by [@kshalot](https://github.com/kshalot) — 2025-11-21T13:30:14Z
```diff
@@ -0,0 +1,100 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	unreachableNodeName        = "unreachable-node"
+	reachableNodeName          = "reachable-node"
+	forcefulTerminationTimeout = 2 * time.Second
+)
+
+var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var ns *corev1.Namespace
+	var matchingPodWrapper *testingpod.PodWrapper
+
+	ginkgo.BeforeAll(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "pod-fr-namespace-")
+		nodes := []corev1.Node{
+			*testingnode.MakeNode(unreachableNodeName).
+				NotReady().
+				Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule}).
+				Obj(),
+			*testingnode.MakeNode(reachableNodeName).
+				Ready().
+				Obj(),
+		}
+		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+		matchingPodWrapper = testingpod.MakePod("matching-pod", ns.Name).
+			StatusPhase(corev1.PodPending).
+			TerminationGracePeriod(1).
+			NodeName(unreachableNodeName).
+			Annotation(constants.SafeToForcefullyTerminateAnnotationKey, constants.SafeToForcefullyTerminateAnnotationValue)
+	})
+
+	ginkgo.It("forcefully terminates pods that opt-in, scheduled on unreachable nodes", func() {
+		matchingPod := matchingPodWrapper.Clone().Obj()
+		util.MustCreate(ctx, k8sClient, matchingPod)
+		gomega.ExpectWithOffset(1, k8sClient.Delete(ctx, matchingPod)).To(gomega.Succeed())
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: matchingPod.Name, Namespace: matchingPod.Namespace}, matchingPod)).
+				To(gomega.Succeed())
+			g.Expect(matchingPod.Status.Phase).Should(gomega.Equal(corev1.PodFailed))
+		}, forcefulTerminationTimeout, util.Interval).Should(gomega.Succeed())
```

  I ran it 100 times and there is not flake. Resolving.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-21T14:57:07Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:117** by [@kshalot](https://github.com/kshalot) — 2025-11-21T14:56:53Z
```diff
@@ -0,0 +1,199 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"fmt"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltaints "sigs.k8s.io/kueue/pkg/util/taints"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=get;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var realClock = clock.RealClock{}
+
+const (
+	KueueFailureRecoveryConditionType = "KueueFailureRecovery"
+	KueueForcefulTerminationReason    = "KueueForcefullyTerminated"
+)
+
+type TerminatingPodReconciler struct {
+	client                         client.Client
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+	recorder                       record.EventRecorder
+}
+
+type TerminatingPodReconcilerOptions struct {
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+}
+
+type TerminatingPodReconcilerOption func(*TerminatingPodReconcilerOptions)
+
+func WithClock(c clock.Clock) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+func WithForcefulTerminationGracePeriod(t time.Duration) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.forcefulTerminationGracePeriod = t
+	}
+}
+
+var defaultOptions = TerminatingPodReconcilerOptions{
+	clock:                          realClock,
+	forcefulTerminationGracePeriod: time.Minute,
+}
+
+func NewTerminatingPodReconciler(
+	client client.Client,
+	recorder record.EventRecorder,
+	opts ...TerminatingPodReconcilerOption,
+) (*TerminatingPodReconciler, error) {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	return &TerminatingPodReconciler{
+		client:                         client,
+		clock:                          options.clock,
+		forcefulTerminationGracePeriod: options.forcefulTerminationGracePeriod,
+		recorder:                       recorder,
+	}, nil
+}
+
+func (r *TerminatingPodReconciler) Generic(event.GenericEvent) bool {
+	return false
+}
+
+func (r *TerminatingPodReconciler) Create(e event.CreateEvent) bool {
+	pod := e.Object.(*corev1.Pod)
+
+	if !podOptedInToFailurePolicy(pod) {
+		return false
+	}
+
+	if pod.DeletionTimestamp.IsZero() {
+		return false
+	}
+
+	return true
+}
+
+func (r *TerminatingPodReconciler) Update(u event.UpdateEvent) bool {
+	oldPod := u.ObjectOld.(*corev1.Pod)
```

  https://github.com/kubernetes-sigs/kueue/pull/7815

- **pkg/controller/failurerecovery/pod_termination_controller.go:148** by [@kshalot](https://github.com/kshalot) — 2025-11-21T14:56:58Z
```diff
@@ -0,0 +1,199 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"fmt"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltaints "sigs.k8s.io/kueue/pkg/util/taints"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=get;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var realClock = clock.RealClock{}
+
+const (
+	KueueFailureRecoveryConditionType = "KueueFailureRecovery"
+	KueueForcefulTerminationReason    = "KueueForcefullyTerminated"
+)
+
+type TerminatingPodReconciler struct {
+	client                         client.Client
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+	recorder                       record.EventRecorder
+}
+
+type TerminatingPodReconcilerOptions struct {
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+}
+
+type TerminatingPodReconcilerOption func(*TerminatingPodReconcilerOptions)
+
+func WithClock(c clock.Clock) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+func WithForcefulTerminationGracePeriod(t time.Duration) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.forcefulTerminationGracePeriod = t
+	}
+}
+
+var defaultOptions = TerminatingPodReconcilerOptions{
+	clock:                          realClock,
+	forcefulTerminationGracePeriod: time.Minute,
+}
+
+func NewTerminatingPodReconciler(
+	client client.Client,
+	recorder record.EventRecorder,
+	opts ...TerminatingPodReconcilerOption,
+) (*TerminatingPodReconciler, error) {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	return &TerminatingPodReconciler{
+		client:                         client,
+		clock:                          options.clock,
+		forcefulTerminationGracePeriod: options.forcefulTerminationGracePeriod,
+		recorder:                       recorder,
+	}, nil
+}
+
+func (r *TerminatingPodReconciler) Generic(event.GenericEvent) bool {
+	return false
+}
+
+func (r *TerminatingPodReconciler) Create(e event.CreateEvent) bool {
+	pod := e.Object.(*corev1.Pod)
+
+	if !podOptedInToFailurePolicy(pod) {
+		return false
+	}
+
+	if pod.DeletionTimestamp.IsZero() {
+		return false
+	}
+
+	return true
+}
+
+func (r *TerminatingPodReconciler) Update(u event.UpdateEvent) bool {
+	oldPod := u.ObjectOld.(*corev1.Pod)
+	newPod := u.ObjectNew.(*corev1.Pod)
+
+	if !podOptedInToFailurePolicy(newPod) {
+		return false
+	}
+
+	// Pod was not marked for deletion in the update
+	if !oldPod.DeletionTimestamp.IsZero() || newPod.DeletionTimestamp.IsZero() {
+		return false
+	}
+
+	return true
+}
+
+func (r *TerminatingPodReconciler) Delete(event.DeleteEvent) bool {
+	return false
+}
+
+func podOptedInToFailurePolicy(p *corev1.Pod) bool {
+	annotationValue, hasAnnotation := p.Annotations[constants.SafeToForcefullyTerminateAnnotationKey]
+	return hasAnnotation && annotationValue == constants.SafeToForcefullyTerminateAnnotationValue
+}
+
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	pod := &corev1.Pod{}
+	if err := r.client.Get(ctx, req.NamespacedName, pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	// Pod was already terminated
+	if utilpod.IsTerminated(pod) {
```

  https://github.com/kubernetes-sigs/kueue/pull/7815

- **pkg/controller/failurerecovery/pod_termination_controller.go:186** by [@kshalot](https://github.com/kshalot) — 2025-11-21T14:57:01Z
```diff
@@ -0,0 +1,199 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package failurerecovery
+
+import (
+	"context"
+	"fmt"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltaints "sigs.k8s.io/kueue/pkg/util/taints"
+)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+// +kubebuilder:rbac:groups="",resources=pods/status,verbs=get;patch
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+
+var realClock = clock.RealClock{}
+
+const (
+	KueueFailureRecoveryConditionType = "KueueFailureRecovery"
+	KueueForcefulTerminationReason    = "KueueForcefullyTerminated"
+)
+
+type TerminatingPodReconciler struct {
+	client                         client.Client
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+	recorder                       record.EventRecorder
+}
+
+type TerminatingPodReconcilerOptions struct {
+	clock                          clock.Clock
+	forcefulTerminationGracePeriod time.Duration
+}
+
+type TerminatingPodReconcilerOption func(*TerminatingPodReconcilerOptions)
+
+func WithClock(c clock.Clock) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+func WithForcefulTerminationGracePeriod(t time.Duration) TerminatingPodReconcilerOption {
+	return func(o *TerminatingPodReconcilerOptions) {
+		o.forcefulTerminationGracePeriod = t
+	}
+}
+
+var defaultOptions = TerminatingPodReconcilerOptions{
+	clock:                          realClock,
+	forcefulTerminationGracePeriod: time.Minute,
+}
+
+func NewTerminatingPodReconciler(
+	client client.Client,
+	recorder record.EventRecorder,
+	opts ...TerminatingPodReconcilerOption,
+) (*TerminatingPodReconciler, error) {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	return &TerminatingPodReconciler{
+		client:                         client,
+		clock:                          options.clock,
+		forcefulTerminationGracePeriod: options.forcefulTerminationGracePeriod,
+		recorder:                       recorder,
+	}, nil
+}
+
+func (r *TerminatingPodReconciler) Generic(event.GenericEvent) bool {
+	return false
+}
+
+func (r *TerminatingPodReconciler) Create(e event.CreateEvent) bool {
+	pod := e.Object.(*corev1.Pod)
+
+	if !podOptedInToFailurePolicy(pod) {
+		return false
+	}
+
+	if pod.DeletionTimestamp.IsZero() {
+		return false
+	}
+
+	return true
+}
+
+func (r *TerminatingPodReconciler) Update(u event.UpdateEvent) bool {
+	oldPod := u.ObjectOld.(*corev1.Pod)
+	newPod := u.ObjectNew.(*corev1.Pod)
+
+	if !podOptedInToFailurePolicy(newPod) {
+		return false
+	}
+
+	// Pod was not marked for deletion in the update
+	if !oldPod.DeletionTimestamp.IsZero() || newPod.DeletionTimestamp.IsZero() {
+		return false
+	}
+
+	return true
+}
+
+func (r *TerminatingPodReconciler) Delete(event.DeleteEvent) bool {
+	return false
+}
+
+func podOptedInToFailurePolicy(p *corev1.Pod) bool {
+	annotationValue, hasAnnotation := p.Annotations[constants.SafeToForcefullyTerminateAnnotationKey]
+	return hasAnnotation && annotationValue == constants.SafeToForcefullyTerminateAnnotationValue
+}
+
+func (r *TerminatingPodReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	pod := &corev1.Pod{}
+	if err := r.client.Get(ctx, req.NamespacedName, pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	// Pod was already terminated
+	if utilpod.IsTerminated(pod) {
+		return ctrl.Result{}, nil
+	}
+
+	// Forceful termination threshold not reached
+	now := r.clock.Now()
+	forcefulTerminationThreshold := pod.DeletionTimestamp.Add(r.forcefulTerminationGracePeriod)
+	if now.Before(forcefulTerminationThreshold) {
+		remainingTime := forcefulTerminationThreshold.Sub(now)
+		return ctrl.Result{RequeueAfter: remainingTime}, nil
+	}
+
+	node := &corev1.Node{}
+	nodeKey := types.NamespacedName{Name: pod.Spec.NodeName}
+	if err := r.client.Get(ctx, nodeKey, node); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	// Pod is not scheduled on an unreachable node
+	if !utiltaints.TaintKeyExists(node.Spec.Taints, corev1.TaintNodeUnreachable) {
+		return ctrl.Result{}, nil
+	}
+
+	totalDeletionGracePeriod := time.Duration(ptr.Deref(pod.DeletionGracePeriodSeconds, 0)) + r.forcefulTerminationGracePeriod
+	eventMessage := fmt.Sprintf(
+		"Pod forcefully terminated after %s grace period due to unreachable node `%s` (triggered by `%s` annotation)",
+		totalDeletionGracePeriod,
+		node.Name,
+		constants.SafeToForcefullyTerminateAnnotationKey,
+	)
+
+	podPatch := pod.DeepCopy()
+	podPatch.Status.Phase = corev1.PodFailed
+	podPatch.Status.Conditions = append(podPatch.Status.Conditions, corev1.PodCondition{
+		Type:    KueueFailureRecoveryConditionType,
+		Status:  corev1.ConditionTrue,
+		Reason:  KueueForcefulTerminationReason,
+		Message: eventMessage,
+	})
+	if err := r.client.Status().Patch(ctx, podPatch, client.MergeFrom(pod)); err != nil {
```

  https://github.com/kubernetes-sigs/kueue/pull/7815
