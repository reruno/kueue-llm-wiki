# PR #7318: Enable conversion webhooks for v1beta2: LocalQueue, ClusterQueue, Workload

**Summary**: Enable conversion webhooks for v1beta2: LocalQueue, ClusterQueue, Workload

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7318

**Last updated**: 2026-02-21T15:47:28Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `v1beta2-with-crds-update` → `main`
- **Created**: 2025-10-20T07:45:17Z
- **Updated**: 2026-02-21T15:47:28Z
- **Merged**: 2025-10-23T10:53:02Z
- **Closed**: 2025-10-23T10:53:02Z
- **Labels**: `lgtm`, `size/XXL`, `release-note-none`, `approved`, `kind/api-change`, `cncf-cla: yes`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 33   **+3595 / -146**

## Description

#### What type of PR is this?

/kind api-change

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #7113

#### Special notes for your reviewer:

This introduces just a subset of conversions, but we could consider merging this already so that the future diffs are easier to comprehend. Adding more conversions will not change the approach hopefully. 

Not adding any release notes because we already have in the previous PR introducing v1beta2. We will also refine the release notes at the end of the migration process.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-10-20T07:45:22Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 8a7e5e42b7cdc7f296deafd8d5599ea5a83907c3 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68f8aa84587bac0008d7f105 |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-20T12:16:29Z

/test pull-kueue-test-integration-baseline-main
https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7318/pull-kueue-test-integration-baseline-main/1980243581536833536
seems like a flake

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-20T12:30:48Z

cc @tenzen-y

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-20T13:09:35Z

cc @mbobrovskyi ptal

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-20T13:34:14Z

/test pull-kueue-test-e2e-main-1-34
infra failure: https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7318/pull-kueue-test-e2e-main-1-34/1980259608379265024

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-10-20T14:11:54Z

/lgtm
Thank you!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-20T14:12:03Z

LGTM label has been added.  <details>Git tree hash: 5797ddef8abb5814ed2679187df6124e0bb8ae03</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-10-20T14:13:22Z

/hold if @tenzen-y have some comments

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-20T16:12:30Z

rebased

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-20T19:25:16Z

/test pull-kueue-test-e2e-customconfigs-main
looks like unrelated flake

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-22T12:32:34Z

retesting e2e tests to check stability
/test pull-kueue-test-e2e-main-1-31
/test pull-kueue-test-e2e-main-1-32
/test pull-kueue-test-e2e-main-1-33
/test pull-kueue-test-e2e-main-1-34
/test pull-kueue-test-e2e-multikueue-main
/test pull-kueue-test-e2e-tas-main
/test pull-kueue-test-e2e-kueueviz-main
/test pull-kueue-test-e2e-customconfigs-main
/test pull-kueue-test-e2e-certmanager-main

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-22T13:15:13Z

3rd full run
/test pull-kueue-test-e2e-main-1-31
/test pull-kueue-test-e2e-main-1-32
/test pull-kueue-test-e2e-main-1-33
/test pull-kueue-test-e2e-main-1-34
/test pull-kueue-test-e2e-multikueue-main
/test pull-kueue-test-e2e-tas-main
/test pull-kueue-test-e2e-kueueviz-main
/test pull-kueue-test-e2e-customconfigs-main
/test pull-kueue-test-e2e-certmanager-main

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-22T14:06:05Z

4th full run
/test pull-kueue-test-e2e-main-1-31
/test pull-kueue-test-e2e-main-1-32
/test pull-kueue-test-e2e-main-1-33
/test pull-kueue-test-e2e-main-1-34
/test pull-kueue-test-e2e-multikueue-main
/test pull-kueue-test-e2e-tas-main
/test pull-kueue-test-e2e-kueueviz-main
/test pull-kueue-test-e2e-customconfigs-main
/test pull-kueue-test-e2e-certmanager-main

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-22T14:46:06Z

This could be follow-up PR, but let me say here.
We probably should follow object reference pattern for the Priority associated fields in the Workload object:

https://github.com/kubernetes-sigs/kueue/blob/fad03c3b01c97f4cab4faed979689af2aad742b1/apis/kueue/v1beta2/workload_types.go#L44-L67

https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md#multiple-resource-reference

Maybe, we can consider the following as new field constructs:


```yaml
spec:
  priority: 123456
  priorityClassRef:
    group: kueue.x-k8s.io | scheduling.k8s.io
    kind: PriorityClass | WorkloadPriorityClass
    name: high
```

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-22T14:54:02Z

Maybe, but I'm not sure if this is overkill, maybe this is a good topic to discuss in https://github.com/kubernetes-sigs/kueue/pull/7282?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-22T15:12:20Z

> Maybe, but I'm not sure if this is overkill, maybe this is a good topic to discuss in #7282?

We should consider this while v1beta2 graduation since the graduation from v1beta2 to v1 should not have any API changes.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-22T15:18:34Z

Yes, totally, I think we should consider it for v1beta2. I suggested maybe this issue to get some feedback from api reviewers (hopefully), but we can also consider the API separately. 

Still, I would prefer to decouple the discussion from this PR, because the objective here is to introduce the conversion webhooks. Maybe we can use this issue: https://github.com/kubernetes-sigs/kueue/issues/7342

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-22T15:42:53Z

```yaml
spec:
  priority: 123456
  priorityClassRef:
    group: kueue.x-k8s.io | scheduling.k8s.io
    kind: PriorityClass | WorkloadPriorityClass
    name: high
```
This API is actually richer than the current, because currently we don't have the information about the "name" of the priorityClass, just the "kind", and the numeric value. TBH, I think including the name is a good idea.

However since the API is richer it is not a simple conversion between the two.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-10-23T07:32:07Z

/lgtm
Thank you!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-23T07:32:15Z

LGTM label has been added.  <details>Git tree hash: ff2c4272567a06dbe8c4faaa2d68ba1c84b0745b</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-23T09:49:07Z

> ```yaml
> spec:
>   priority: 123456
>   priorityClassRef:
>     group: kueue.x-k8s.io | scheduling.k8s.io
>     kind: PriorityClass | WorkloadPriorityClass
>     name: high
> ```
> 
> This API is actually richer than the current, because currently we don't have the information about the "name" of the priorityClass, just the "kind", and the numeric value. TBH, I think including the name is a good idea.
> 
> However since the API is richer it is not a simple conversion between the two.

The v1beta1 API represents priorityClass name in .spec.priorityClassName. So, once we introduce objectReferenceRef pattern, we should remove the dedicated priorityClassName field.

Anyway, we should discuss it in a separate issue as I mentioned in the above.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-23T09:51:50Z

> > ```yaml
> > spec:
> >   priority: 123456
> >   priorityClassRef:
> >     group: kueue.x-k8s.io | scheduling.k8s.io
> >     kind: PriorityClass | WorkloadPriorityClass
> >     name: high
> > ```
> > 
> > 
> >     
> >       
> >     
> > 
> >       
> >     
> > 
> >     
> >   
> > This API is actually richer than the current, because currently we don't have the information about the "name" of the priorityClass, just the "kind", and the numeric value. TBH, I think including the name is a good idea.
> > However since the API is richer it is not a simple conversion between the two.
> 
> The v1beta1 API represents priorityClass name in .spec.priorityClassName. So, once we introduce objectReferenceRef pattern, we should remove the dedicated priorityClassName field.
> 
> Anyway, we should discuss it in a separate issue as I mentioned in the above.

I will open a dedicated issue, later

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-23T10:04:35Z

Excluding https://github.com/kubernetes-sigs/kueue/pull/7318#discussion_r2454592563, LGTM, thanks.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-23T10:25:58Z

/unhold
Seems like all comments are addressed, @tenzen-y ptal

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-23T10:26:51Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7318#" title="Author self-approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7318#discussion_r2454653710" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-28T09:30:37Z

/release-note-edit
```release-note
v1beta2: Expose the v1beta2 API for CRD serving. 

V1beta1 remains supported in this release and used as storage, but please plan for migration.

We plan to use v1beta2 for storage in 0.16, and discontinue the support for v1beta1 in 0.17.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-28T09:33:30Z

/release-note-edit
```release-note
NONE
```

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-22T08:26:40Z

- **apis/kueue/v1beta2/workload_types.go:767** by [@mimowo](https://github.com/mimowo) — 2025-10-22T08:26:40Z
```diff
@@ -764,7 +764,7 @@ const (
 
 // Workload is the Schema for the workloads API
 // +kubebuilder:validation:XValidation:rule="has(self.status) && has(self.status.conditions) && self.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True') && has(self.status.admission) ? size(self.spec.podSets) == size(self.status.admission.podSetAssignments) : true", message="podSetAssignments must have the same number of podSets as the spec"
-// +kubebuilder:validation:XValidation:rule="(has(oldSelf.status) && has(oldSelf.status.conditions) && oldSelf.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True')) ? (oldSelf.spec.priorityClassSource == self.spec.priorityClassSource) : true", message="field is immutable"
+// +kubebuilder:validation:XValidation:rule="(has(oldSelf.status) && has(oldSelf.status.conditions) && oldSelf.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True')) ? ((has(oldSelf.spec.priorityClassSource) ? oldSelf.spec.priorityClassSource : ”) == (has(self.spec.priorityClassSource) ? self.spec.priorityClassSource : ”)) : true", message="field is immutable"
```

  I had to relax here the validation to take into account possible `.spec.priorityClassSource` being null.
  
  Otherwise the following validation error blocks updates to the workload when conversion webhooks are enabled, see example [log](https://storage.googleapis.com/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7318/pull-kueue-test-e2e-main-1-34/1980577733733781504/artifacts/run-test-e2e-singlecluster-1.34.0/kind-control-plane/pods/kube-system_kube-controller-manager-kind-control-plane_59db7f9c8cb80cab934b711df6195f61/kube-controller-manager/0.log) a build:
  
  ```
  2025-10-21T10:27:16.577918448Z stderr F E1021 10:27:16.577774       1 garbagecollector.go:750] "Unhandled Error" err="orphanDependents for [batch/v1/Job, namespace: e2e-metrics-sb5mf, name: admission-checked-job, uid: f55b9d64-4733-4020-a6ea-15dec0ae9783] failed with failed to orphan dependents of owner [batch/v1/Job, namespace: e2e-metrics-sb5mf, name: admission-checked-job, uid: f55b9d64-4733-4020-a6ea-15dec0ae9783], got errors: orphaning [kueue.x-k8s.io/v1beta2/Workload, namespace: e2e-metrics-sb5mf, name: job-admission-checked-job-5e28d, uid: 53bf1434-d421-4b32-9da2-0cb4c335f3c7] failed, Workload.kueue.x-k8s.io \"job-admission-checked-job-5e28d\" is invalid: <nil>: Invalid value: \"object\": no such key: priorityClassSource evaluating rule: field is immutable" logger="UnhandledError"
  ```
  
  TBH this feels like a bug in the k8s api server, and I will work on posting the issue upstream. 
  
  However, in the meanwhile I can see two options:
  1. make the field as *string in v1beta2
  2. workaround the validation as currently in the CEL
  
  I'm ok with both, but (1.) seems to reflect the intention better. If no priority source is provided, then it is basically null, rather than empty string.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-22T08:26:50Z

- **apis/kueue/v1beta2/workload_types.go:767** by [@mimowo](https://github.com/mimowo) — 2025-10-22T08:26:49Z
```diff
@@ -764,7 +764,7 @@ const (
 
 // Workload is the Schema for the workloads API
 // +kubebuilder:validation:XValidation:rule="has(self.status) && has(self.status.conditions) && self.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True') && has(self.status.admission) ? size(self.spec.podSets) == size(self.status.admission.podSetAssignments) : true", message="podSetAssignments must have the same number of podSets as the spec"
-// +kubebuilder:validation:XValidation:rule="(has(oldSelf.status) && has(oldSelf.status.conditions) && oldSelf.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True')) ? (oldSelf.spec.priorityClassSource == self.spec.priorityClassSource) : true", message="field is immutable"
+// +kubebuilder:validation:XValidation:rule="(has(oldSelf.status) && has(oldSelf.status.conditions) && oldSelf.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True')) ? ((has(oldSelf.spec.priorityClassSource) ? oldSelf.spec.priorityClassSource : ”) == (has(self.spec.priorityClassSource) ? self.spec.priorityClassSource : ”)) : true", message="field is immutable"
```

  cc @tenzen-y wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-22T10:55:22Z

- **apis/kueue/v1beta2/workload_types.go:767** by [@mimowo](https://github.com/mimowo) — 2025-10-22T10:55:21Z
```diff
@@ -764,7 +764,7 @@ const (
 
 // Workload is the Schema for the workloads API
 // +kubebuilder:validation:XValidation:rule="has(self.status) && has(self.status.conditions) && self.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True') && has(self.status.admission) ? size(self.spec.podSets) == size(self.status.admission.podSetAssignments) : true", message="podSetAssignments must have the same number of podSets as the spec"
-// +kubebuilder:validation:XValidation:rule="(has(oldSelf.status) && has(oldSelf.status.conditions) && oldSelf.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True')) ? (oldSelf.spec.priorityClassSource == self.spec.priorityClassSource) : true", message="field is immutable"
+// +kubebuilder:validation:XValidation:rule="(has(oldSelf.status) && has(oldSelf.status.conditions) && oldSelf.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True')) ? ((has(oldSelf.spec.priorityClassSource) ? oldSelf.spec.priorityClassSource : ”) == (has(self.spec.priorityClassSource) ? self.spec.priorityClassSource : ”)) : true", message="field is immutable"
```

  Looking at this diagram: https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md#serialization-of-optionalrequired-fields
  
  I think we should really use ptr here and make the field optional to avoid problems, because "Is the zero value a valid user choice?" I think yes, because empty string is a valid value here, and so the answer should be "Use a pointer and omitempty".
  
  I can change that, but before we have a decision I think the current tweak to the CEL rules makes the tests happy.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-10-22T14:19:08Z

- **apis/kueue/v1beta2/workload_types.go:767** by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-22T14:19:08Z
```diff
@@ -764,7 +764,7 @@ const (
 
 // Workload is the Schema for the workloads API
 // +kubebuilder:validation:XValidation:rule="has(self.status) && has(self.status.conditions) && self.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True') && has(self.status.admission) ? size(self.spec.podSets) == size(self.status.admission.podSetAssignments) : true", message="podSetAssignments must have the same number of podSets as the spec"
-// +kubebuilder:validation:XValidation:rule="(has(oldSelf.status) && has(oldSelf.status.conditions) && oldSelf.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True')) ? (oldSelf.spec.priorityClassSource == self.spec.priorityClassSource) : true", message="field is immutable"
+// +kubebuilder:validation:XValidation:rule="(has(oldSelf.status) && has(oldSelf.status.conditions) && oldSelf.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True')) ? ((has(oldSelf.spec.priorityClassSource) ? oldSelf.spec.priorityClassSource : ”) == (has(self.spec.priorityClassSource) ? self.spec.priorityClassSource : ”)) : true", message="field is immutable"
```

  Thank you for investigating the errors.
  I'm also fine with (1) based on Kubernetes API conventions, but it would probably be better to default "" to the field only when the priorityClassSource is not specified (null).
  
  Because if the field is not specified, the Workload will be handled as priority=0 both in kube-scheduler and kueue-scheduler.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-22T14:29:32Z

- **apis/kueue/v1beta2/workload_types.go:767** by [@mimowo](https://github.com/mimowo) — 2025-10-22T14:29:32Z
```diff
@@ -764,7 +764,7 @@ const (
 
 // Workload is the Schema for the workloads API
 // +kubebuilder:validation:XValidation:rule="has(self.status) && has(self.status.conditions) && self.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True') && has(self.status.admission) ? size(self.spec.podSets) == size(self.status.admission.podSetAssignments) : true", message="podSetAssignments must have the same number of podSets as the spec"
-// +kubebuilder:validation:XValidation:rule="(has(oldSelf.status) && has(oldSelf.status.conditions) && oldSelf.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True')) ? (oldSelf.spec.priorityClassSource == self.spec.priorityClassSource) : true", message="field is immutable"
+// +kubebuilder:validation:XValidation:rule="(has(oldSelf.status) && has(oldSelf.status.conditions) && oldSelf.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True')) ? ((has(oldSelf.spec.priorityClassSource) ? oldSelf.spec.priorityClassSource : ”) == (has(self.spec.priorityClassSource) ? self.spec.priorityClassSource : ”)) : true", message="field is immutable"
```

  TBH I don't understand the motivation for defaulting priorityClassSource to "". I think keeping it null makes code simpler, eg CEL validation has fewer cases.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-10-22T14:30:41Z

Thank you for moving this forward. I left some comments.

- **apis/kueue/v1beta2/clusterqueue_types.go:552** by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-22T14:23:04Z
```diff
@@ -548,3 +548,5 @@ type ClusterQueueList struct {
 func init() {
 	SchemeBuilder.Register(&ClusterQueue{}, &ClusterQueueList{})
 }
+
+func (*ClusterQueue) Hub() {}
```

  Note in my mind: we should remove these implementations when we drop the conversion webhooks.

- **config/components/crd/kustomization.yaml:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-22T14:28:18Z
  This looks surprising to me.
  Why we can not use the markers like https://github.com/kubernetes-sigs/kueue/blob/f2aff8485835901e192932b1ffea90845d8a980c/pkg/webhooks/clusterqueue_webhook.go#L54?
  
  I guess we can add webhook markers to apis/kueue/v1beta1/xyz_conversion.go.

- **pkg/util/cert/cert.go:75** by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-22T14:29:28Z
```diff
@@ -63,6 +64,15 @@ func ManageCerts(mgr ctrl.Manager, cfg config.Configuration, setupFinished chan
 		}, {
 			Type: cert.Mutating,
 			Name: mutatingWebhookName,
+		}, {
+			Type: cert.CRDConversion,
+			Name: "localqueues.kueue.x-k8s.io",
+		}, {
+			Type: cert.CRDConversion,
+			Name: "clusterqueues.kueue.x-k8s.io",
+		}, {
+			Type: cert.CRDConversion,
+			Name: "workloads.kueue.x-k8s.io",
```

  Shouldn't we build the name like mutating and validating webhooks?
  If not, I guess that Helm charts do not work well.

- **test/integration/multikueue/suite_test.go:89** by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-22T14:30:17Z
```diff
@@ -82,6 +82,11 @@ var (
 	worker1TestCluster      cluster
 	worker2TestCluster      cluster
 	managersConfigNamespace *corev1.Namespace
+
+	// makes sure there is only one fwk.Init and setupClient at the same time
+	// since these functions are not thread safe due to adding to the common
+	// schema.
+	mu sync.Mutex
```

  Is this common issue? Or this PR specific one?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-22T14:30:48Z

- **apis/kueue/v1beta2/workload_types.go:767** by [@mimowo](https://github.com/mimowo) — 2025-10-22T14:30:48Z
```diff
@@ -764,7 +764,7 @@ const (
 
 // Workload is the Schema for the workloads API
 // +kubebuilder:validation:XValidation:rule="has(self.status) && has(self.status.conditions) && self.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True') && has(self.status.admission) ? size(self.spec.podSets) == size(self.status.admission.podSetAssignments) : true", message="podSetAssignments must have the same number of podSets as the spec"
-// +kubebuilder:validation:XValidation:rule="(has(oldSelf.status) && has(oldSelf.status.conditions) && oldSelf.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True')) ? (oldSelf.spec.priorityClassSource == self.spec.priorityClassSource) : true", message="field is immutable"
+// +kubebuilder:validation:XValidation:rule="(has(oldSelf.status) && has(oldSelf.status.conditions) && oldSelf.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True')) ? ((has(oldSelf.spec.priorityClassSource) ? oldSelf.spec.priorityClassSource : ”) == (has(self.spec.priorityClassSource) ? self.spec.priorityClassSource : ”)) : true", message="field is immutable"
```

  In any case since we are not sure, I would suggest separate Issue / PR for that. I think this API improvement can be done in a follow up. I opened: https://github.com/kubernetes-sigs/kueue/issues/7342. The main role of the PR is to introduce the conversion webhooks, not to introduce all API changes, we can make API adjustments in follow ups.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-22T14:33:49Z

- **apis/kueue/v1beta2/clusterqueue_types.go:552** by [@mimowo](https://github.com/mimowo) — 2025-10-22T14:33:49Z
```diff
@@ -548,3 +548,5 @@ type ClusterQueueList struct {
 func init() {
 	SchemeBuilder.Register(&ClusterQueue{}, &ClusterQueueList{})
 }
+
+func (*ClusterQueue) Hub() {}
```

  Yes

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-10-22T14:38:03Z

- **apis/kueue/v1beta2/workload_types.go:767** by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-22T14:38:03Z
```diff
@@ -764,7 +764,7 @@ const (
 
 // Workload is the Schema for the workloads API
 // +kubebuilder:validation:XValidation:rule="has(self.status) && has(self.status.conditions) && self.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True') && has(self.status.admission) ? size(self.spec.podSets) == size(self.status.admission.podSetAssignments) : true", message="podSetAssignments must have the same number of podSets as the spec"
-// +kubebuilder:validation:XValidation:rule="(has(oldSelf.status) && has(oldSelf.status.conditions) && oldSelf.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True')) ? (oldSelf.spec.priorityClassSource == self.spec.priorityClassSource) : true", message="field is immutable"
+// +kubebuilder:validation:XValidation:rule="(has(oldSelf.status) && has(oldSelf.status.conditions) && oldSelf.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True')) ? ((has(oldSelf.spec.priorityClassSource) ? oldSelf.spec.priorityClassSource : ”) == (has(self.spec.priorityClassSource) ? self.spec.priorityClassSource : ”)) : true", message="field is immutable"
```

  First of all, I would like to change my opinion. We can just make this field as *string.
  
  ---
  The motivation was visibility.
  For user (admin) PoV, they typically do not have enough knowledge for internal implementations.
  
  However, both PrioriryClass and WorkloadPriorityClass w/ 0 priority will not be created by default. 
  So, we do not need to default the"" value to this field.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-22T14:42:37Z

- **config/components/crd/kustomization.yaml:1** by [@mimowo](https://github.com/mimowo) — 2025-10-22T14:42:37Z
  These markers register admission webhooks, not conversion, so I think it makes sense to keep them where they are. The conversion webhooks are registered at two levels:
  - api server, based on the CRD spec.conversion field, thus the changes to manifests
  - controller-runtime based on the Convertible interface impemented (COnvertTo, and ConvertFrom functions)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-22T14:46:28Z

- **pkg/util/cert/cert.go:75** by [@mimowo](https://github.com/mimowo) — 2025-10-22T14:46:28Z
```diff
@@ -63,6 +64,15 @@ func ManageCerts(mgr ctrl.Manager, cfg config.Configuration, setupFinished chan
 		}, {
 			Type: cert.Mutating,
 			Name: mutatingWebhookName,
+		}, {
+			Type: cert.CRDConversion,
+			Name: "localqueues.kueue.x-k8s.io",
+		}, {
+			Type: cert.CRDConversion,
+			Name: "clusterqueues.kueue.x-k8s.io",
+		}, {
+			Type: cert.CRDConversion,
+			Name: "workloads.kueue.x-k8s.io",
```

  In case of conversion webhooks CRD name should be used: https://github.com/open-policy-agent/cert-controller/blob/94063fa2ac645e5cdfd907622e6aadcb993a314d/pkg/rotator/rotator.go#L96
  
  Without these changes e2e tests wouldn't work
  
  I will test helm manually

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-22T14:49:50Z

- **test/integration/multikueue/suite_test.go:89** by [@mimowo](https://github.com/mimowo) — 2025-10-22T14:49:50Z
```diff
@@ -82,6 +82,11 @@ var (
 	worker1TestCluster      cluster
 	worker2TestCluster      cluster
 	managersConfigNamespace *corev1.Namespace
+
+	// makes sure there is only one fwk.Init and setupClient at the same time
+	// since these functions are not thread safe due to adding to the common
+	// schema.
+	mu sync.Mutex
```

  Specific, because of these lines:
  https://github.com/kubernetes-sigs/kueue/pull/7318/files#diff-9be12d69f919437ea6abbe1fbcae80e1e97b5e28fdcbc515596187f25b3b6fe4R84-R90
  
  and calling ` kueue.AddToScheme(f.testEnv.Scheme)` is not thread safe, but it is called when creating 3 clusters in MultiKueue integration tests. It can make the initialization for the MultiKueue env slightly slower, but I didn't find a solution.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-10-22T15:11:09Z

- **config/components/crd/kustomization.yaml:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-22T15:11:09Z
  > api server, based on the CRD spec.conversion field, thus the changes to manifests
  
  That sounds interesting since I can not find such a field in `config/components/crd/bases/...`.
  Could you double-click that?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-22T15:24:37Z

- **config/components/crd/kustomization.yaml:1** by [@mimowo](https://github.com/mimowo) — 2025-10-22T15:24:37Z
  It is generated now under `config/components/crd/_output/` by combining the pre-existing patches from here: https://github.com/kubernetes-sigs/kueue/pull/7318/files#diff-ca91a436abee42dea1878b1cc972c9336ebdfb327859b6a4a500a4493b78a9eaL21-R23
  
  Here is the Makefile step to do so: https://github.com/kubernetes-sigs/kueue/pull/7318/files#diff-76ed074a9305c04054cdebb9e9aad2d818052b07091de1f20cad0bbac34ffb52R132-R135
  
  I ignore the file in _output (in .gitignore) because it is "derived", but if you think it is better we could check this in too.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-22T15:40:58Z

- **apis/kueue/v1beta2/workload_types.go:767** by [@mimowo](https://github.com/mimowo) — 2025-10-22T15:40:58Z
```diff
@@ -764,7 +764,7 @@ const (
 
 // Workload is the Schema for the workloads API
 // +kubebuilder:validation:XValidation:rule="has(self.status) && has(self.status.conditions) && self.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True') && has(self.status.admission) ? size(self.spec.podSets) == size(self.status.admission.podSetAssignments) : true", message="podSetAssignments must have the same number of podSets as the spec"
-// +kubebuilder:validation:XValidation:rule="(has(oldSelf.status) && has(oldSelf.status.conditions) && oldSelf.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True')) ? (oldSelf.spec.priorityClassSource == self.spec.priorityClassSource) : true", message="field is immutable"
+// +kubebuilder:validation:XValidation:rule="(has(oldSelf.status) && has(oldSelf.status.conditions) && oldSelf.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True')) ? ((has(oldSelf.spec.priorityClassSource) ? oldSelf.spec.priorityClassSource : ”) == (has(self.spec.priorityClassSource) ? self.spec.priorityClassSource : ”)) : true", message="field is immutable"
```

  ack, the question remains how the "ideal" API will look like: enum or  the priorityClassRef.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-22T17:14:57Z

- **pkg/util/cert/cert.go:75** by [@mimowo](https://github.com/mimowo) — 2025-10-22T17:14:57Z
```diff
@@ -63,6 +64,15 @@ func ManageCerts(mgr ctrl.Manager, cfg config.Configuration, setupFinished chan
 		}, {
 			Type: cert.Mutating,
 			Name: mutatingWebhookName,
+		}, {
+			Type: cert.CRDConversion,
+			Name: "localqueues.kueue.x-k8s.io",
+		}, {
+			Type: cert.CRDConversion,
+			Name: "clusterqueues.kueue.x-k8s.io",
+		}, {
+			Type: cert.CRDConversion,
+			Name: "workloads.kueue.x-k8s.io",
```

  I have tested helm locally by using `E2E_USE_HELM=true CREATE_KIND_CLUSTER=true make run-test-e2e-singlecluster-1.33.4` and the tests passed

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-10-23T09:52:02Z

- **test/integration/multikueue/suite_test.go:89** by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-23T09:52:02Z
```diff
@@ -82,6 +82,11 @@ var (
 	worker1TestCluster      cluster
 	worker2TestCluster      cluster
 	managersConfigNamespace *corev1.Namespace
+
+	// makes sure there is only one fwk.Init and setupClient at the same time
+	// since these functions are not thread safe due to adding to the common
+	// schema.
+	mu sync.Mutex
```

  That makes sense, thank you!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-10-23T09:52:41Z

- **apis/kueue/v1beta2/workload_types.go:767** by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-23T09:52:41Z
```diff
@@ -764,7 +764,7 @@ const (
 
 // Workload is the Schema for the workloads API
 // +kubebuilder:validation:XValidation:rule="has(self.status) && has(self.status.conditions) && self.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True') && has(self.status.admission) ? size(self.spec.podSets) == size(self.status.admission.podSetAssignments) : true", message="podSetAssignments must have the same number of podSets as the spec"
-// +kubebuilder:validation:XValidation:rule="(has(oldSelf.status) && has(oldSelf.status.conditions) && oldSelf.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True')) ? (oldSelf.spec.priorityClassSource == self.spec.priorityClassSource) : true", message="field is immutable"
+// +kubebuilder:validation:XValidation:rule="(has(oldSelf.status) && has(oldSelf.status.conditions) && oldSelf.status.conditions.exists(c, c.type == 'QuotaReserved' && c.status == 'True')) ? ((has(oldSelf.spec.priorityClassSource) ? oldSelf.spec.priorityClassSource : ”) == (has(self.spec.priorityClassSource) ? self.spec.priorityClassSource : ”)) : true", message="field is immutable"
```

  yes, exactly.
  Let's continue it in a dedicted issue.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-10-23T10:00:53Z

- **config/components/crd/kustomization.yaml:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-23T10:00:53Z
  Ah, I got it.
  I found the patch for `conversion` field in https://github.com/kubernetes-sigs/kueue/blob/8a7e5e42b7cdc7f296deafd8d5599ea5a83907c3/config/components/crd/patches/webhook_in_clusterqueues.yaml#L7-L16
  
  That makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-10-23T10:03:53Z

- **pkg/util/cert/cert.go:75** by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-23T10:03:53Z
```diff
@@ -63,6 +64,15 @@ func ManageCerts(mgr ctrl.Manager, cfg config.Configuration, setupFinished chan
 		}, {
 			Type: cert.Mutating,
 			Name: mutatingWebhookName,
+		}, {
+			Type: cert.CRDConversion,
+			Name: "localqueues.kueue.x-k8s.io",
+		}, {
+			Type: cert.CRDConversion,
+			Name: "clusterqueues.kueue.x-k8s.io",
+		}, {
+			Type: cert.CRDConversion,
+			Name: "workloads.kueue.x-k8s.io",
```

  Thank you for sharing the result.
  Did you check if using an arbitrary Helm application name works well, as reported in https://github.com/kubernetes-sigs/kueue/issues/6866?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-23T10:07:54Z

- **pkg/util/cert/cert.go:75** by [@mimowo](https://github.com/mimowo) — 2025-10-23T10:07:54Z
```diff
@@ -63,6 +64,15 @@ func ManageCerts(mgr ctrl.Manager, cfg config.Configuration, setupFinished chan
 		}, {
 			Type: cert.Mutating,
 			Name: mutatingWebhookName,
+		}, {
+			Type: cert.CRDConversion,
+			Name: "localqueues.kueue.x-k8s.io",
+		}, {
+			Type: cert.CRDConversion,
+			Name: "clusterqueues.kueue.x-k8s.io",
+		}, {
+			Type: cert.CRDConversion,
+			Name: "workloads.kueue.x-k8s.io",
```

  No I didn't I will check now, but I don't expect issues, because the new code in question does not have the name of the application in itself

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-23T10:21:41Z

- **pkg/util/cert/cert.go:75** by [@mimowo](https://github.com/mimowo) — 2025-10-23T10:21:40Z
```diff
@@ -63,6 +64,15 @@ func ManageCerts(mgr ctrl.Manager, cfg config.Configuration, setupFinished chan
 		}, {
 			Type: cert.Mutating,
 			Name: mutatingWebhookName,
+		}, {
+			Type: cert.CRDConversion,
+			Name: "localqueues.kueue.x-k8s.io",
+		}, {
+			Type: cert.CRDConversion,
+			Name: "clusterqueues.kueue.x-k8s.io",
+		}, {
+			Type: cert.CRDConversion,
+			Name: "workloads.kueue.x-k8s.io",
```

  Ok, I tested using `test-kueue` and it works properly:
  
  workload got admitted:
  ```
  ❯ k create -f cluster.yaml
  resourceflavor.kueue.x-k8s.io/default-flavor created
  clusterqueue.kueue.x-k8s.io/cluster-queue created
  localqueue.kueue.x-k8s.io/user-queue created
  localqueue.kueue.x-k8s.io/user-queue2 created
  ❯ k get lq
  NAME          CLUSTERQUEUE    PENDING WORKLOADS   ADMITTED WORKLOADS
  user-queue    cluster-queue   0                   0
  user-queue2   cluster-queue   0                   0
  ❯ k get cq
  NAME                            COHORT   PENDING WORKLOADS
  cluster-queue                            0
  test-admission-check-cq-fvfbp            0
  ❯ k create -f sample.yaml
  job.batch/sample-jobgczst created
  ❯ k get wl
  NAME                        QUEUE        RESERVED IN     ADMITTED   FINISHED   AGE
  job-sample-jobgczst-c109a   user-queue   cluster-queue   True                  3s
  ```
  
  Here is my deployment for Kueue, see `app.kubernetes.io/instance:test-kueue` :
  ```yaml
  ❯ k get deploy -nkueue-system -oyaml
  apiVersion: v1
  items:
  - apiVersion: apps/v1
    kind: Deployment
    metadata:
      annotations:
        deployment.kubernetes.io/revision: "1"
        meta.helm.sh/release-name: test-kueue
        meta.helm.sh/release-namespace: kueue-system
      creationTimestamp: "2025-10-23T10:17:00Z"
      generation: 1
      labels:
        app.kubernetes.io/instance: test-kueue
        app.kubernetes.io/managed-by: Helm
        app.kubernetes.io/name: kueue
        app.kubernetes.io/version: v0.14.1
        control-plane: controller-manager
        helm.sh/chart: kueue-0.14.1
      name: test-kueue-controller-manager
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-23T10:24:33Z

- **pkg/util/cert/cert.go:75** by [@mimowo](https://github.com/mimowo) — 2025-10-23T10:24:33Z
```diff
@@ -63,6 +64,15 @@ func ManageCerts(mgr ctrl.Manager, cfg config.Configuration, setupFinished chan
 		}, {
 			Type: cert.Mutating,
 			Name: mutatingWebhookName,
+		}, {
+			Type: cert.CRDConversion,
+			Name: "localqueues.kueue.x-k8s.io",
+		}, {
+			Type: cert.CRDConversion,
+			Name: "clusterqueues.kueue.x-k8s.io",
+		}, {
+			Type: cert.CRDConversion,
+			Name: "workloads.kueue.x-k8s.io",
```

  Also, I can see the caBundle is injected into the CRD properly - also here we have `meta.helm.sh/release-name: test-kueue`:
  
  ```
  ❯ k get crds/workloads.kueue.x-k8s.io -oyaml | head -n40
  apiVersion: apiextensions.k8s.io/v1
  kind: CustomResourceDefinition
  metadata:
    annotations:
      controller-gen.kubebuilder.io/version: v0.19.0
      meta.helm.sh/release-name: test-kueue
      meta.helm.sh/release-namespace: kueue-system
    creationTimestamp: "2025-10-23T10:16:59Z"
    generation: 2
    labels:
      app.kubernetes.io/instance: test-kueue
      app.kubernetes.io/managed-by: Helm
      app.kubernetes.io/name: kueue
      app.kubernetes.io/version: v0.14.1
      control-plane: controller-manager
      helm.sh/chart: kueue-0.14.1
    name: workloads.kueue.x-k8s.io
    resourceVersion: "2177"
    uid: f57642b8-aca7-43aa-bf11-c39cb0c35708
  spec:
    conversion:
      strategy: Webhook
      webhook:
        clientConfig:
          caBundle: LR0xtwSx...
          service:
            name: test-kueue-webhook-service
            namespace: kueue-system
            path: /convert
            port: 443
        conversionReviewVersions:
        - v1
    group: kueue.x-k8s.io
    names:
      kind: Workload
      listKind: WorkloadList
      plural: workloads
      shortNames:
      - wl
      singular: workload
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-10-23T10:26:43Z

- **pkg/util/cert/cert.go:75** by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-23T10:26:43Z
```diff
@@ -63,6 +64,15 @@ func ManageCerts(mgr ctrl.Manager, cfg config.Configuration, setupFinished chan
 		}, {
 			Type: cert.Mutating,
 			Name: mutatingWebhookName,
+		}, {
+			Type: cert.CRDConversion,
+			Name: "localqueues.kueue.x-k8s.io",
+		}, {
+			Type: cert.CRDConversion,
+			Name: "clusterqueues.kueue.x-k8s.io",
+		}, {
+			Type: cert.CRDConversion,
+			Name: "workloads.kueue.x-k8s.io",
```

  Thank you for checking that.
  LGTM
  /lgtm
  /approve
