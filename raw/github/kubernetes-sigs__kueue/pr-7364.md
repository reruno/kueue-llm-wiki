# PR #7364: don't update PodsReady condition timely

**Summary**: don't update PodsReady condition timely

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7364

**Last updated**: 2025-11-28T08:53:13Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@olderTaoist](https://github.com/olderTaoist)
- **Branch**: `timely-update` → `main`
- **Created**: 2025-10-23T11:51:02Z
- **Updated**: 2025-11-28T08:53:13Z
- **Merged**: 2025-11-07T11:18:57Z
- **Closed**: 2025-11-07T11:18:57Z
- **Labels**: `kind/bug`, `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 2   **+140 / -0**

## Description

#### What type of PR is this?

/kind bug
#### What this PR does / why we need it:

JobReconciler don't update PodsReady condition timely when updata status failed. no event triggers reconcile, need manually trigger the workload update.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #7363


#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-10-23T11:51:09Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 3bff38ff3f9e0ede497890b1f605b73022a0ac24 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/690dcf2fe66b1d0008778786 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-7364--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-7364--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTczNjQtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.rzXBwDknNrUMGFHSa_3TurW8x1R_JY3cs_QhxjhWe4E)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-7364--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-23T11:51:11Z

Welcome @olderTaoist! <br><br>It looks like this is your first PR to <a href='https://github.com/kubernetes-sigs/kueue'>kubernetes-sigs/kueue</a> 🎉. Please refer to our [pull request process documentation](https://www.kubernetes.dev/docs/guide/pull-requests/) to help your PR have a smooth ride to approval. <br><br>You will be prompted by a bot to use commands during the review process. Do not be afraid to follow the prompts! It is okay to experiment. [Here is the bot commands documentation](https://go.k8s.io/bot-commands). <br><br>You can also check if kubernetes-sigs/kueue has [its own contribution guidelines](https://github.com/kubernetes-sigs/kueue/tree/master/CONTRIBUTING.md). <br><br>You may want to refer to our [testing guide](https://git.k8s.io/community/contributors/devel/sig-testing/testing.md) if you run into trouble with your tests not passing. <br><br>If you are having difficulty getting your pull request seen, please follow the [recommended escalation practices](https://www.kubernetes.dev/docs/guide/pull-requests/#why-is-my-pull-request-not-getting-reviewed). Also, for tips and tricks in the contribution process you may want to read the [Kubernetes contributor cheat sheet](https://www.kubernetes.dev/docs/contributor-cheatsheet/). We want to make sure your contribution gets all the attention it needs! <br><br>Thank you, and welcome to Kubernetes. :smiley:

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-23T11:51:12Z

Hi @olderTaoist. Thanks for your PR.

I'm waiting for a [github.com](https://kubernetes-sigs/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-23T11:55:34Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-23T11:55:51Z

Thank you for the fix, lgtm, but can you think of adding a test for it?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-23T11:56:26Z

Please also update the "Does this PR introduce a user-facing change?" section to describe the fix. It will land in release notes for users.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-23T12:10:43Z

cc @PBundyra ptal

### Comment by [@olderTaoist](https://github.com/olderTaoist) — 2025-10-27T02:04:38Z

> Thank you for the fix, lgtm, but can you think of adding a test for it?

let me think about how to write a single test. I also accidentally discovered that in our k8s environment, accessing the webhook through the service (kube-proxy) failed probabilistically, resulting in the PodReady status not being updated timely and subsequent tasks being unable to be scheduled.

### Comment by [@olderTaoist](https://github.com/olderTaoist) — 2025-11-03T11:29:01Z

> Thank you for the fix, lgtm, but can you think of adding a test for it?

Done

### Comment by [@linux-foundation-easycla[bot]](https://github.com/apps/linux-foundation-easycla) — 2025-11-04T11:06:19Z

<a href="https://easycla.lfx.linuxfoundation.org/#/?version=2"><img src="https://s3.amazonaws.com/cla-project-logo-prod/cla-signed.svg?v=2" alt="CLA Signed" align="left" height="28" width="328" ></a><br/><br />The committers listed above are authorized under a signed CLA.<ul><li>:white_check_mark: login: olderTaoist / name: walker  (3bff38ff3f9e0ede497890b1f605b73022a0ac24)</li></ul>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-07T08:35:52Z

Looks great 👍 
/lgtm
/approve
/cherrypick release-0.14
/cherrypick release-0.13

In case @PBundyra has some additional comments. 
/hold

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-11-07T08:35:55Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.13`, `release-0.14` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7364#issuecomment-3501299058):

>Looks great 👍 
>/lgtm
>/approve
>/cherrypick release-0.14
>/cherrypick release-0.13
>
>In case @PBundyra has some additional comments. 
>/hold
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-07T08:36:00Z

LGTM label has been added.  <details>Git tree hash: 4e2d98879442a705cf7abe601ce21e64e43bc4e5</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-07T08:36:03Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7364#issuecomment-3501299058" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7364#" title="Author self-approved">olderTaoist</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-11-07T09:37:19Z

/hold cancel
Thank you @olderTaoist, great job!

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-07T09:55:34Z

```
=== Failed
=== FAIL: pkg/controller/jobframework TestReconcileGenericJobWithWaitForPodsReady/update_podready_condition_failed (0.06s)
    reconciler.go:316: "level"=2 "msg"="Namespace not found; skipping selector check" "job"="default/test-job-podready-fail" "gvk"="batch/v1, Kind=Job" "namespace"="default"
    reconciler_test.go:843: unexpected reconcile error want Internal error occurred: failed calling webhook got %!s(<nil>))
=== FAIL: pkg/controller/jobframework TestReconcileGenericJobWithWaitForPodsReady (0.11s)
=== FAIL: pkg/controller/jobframework TestReconcileGenericJobWithWaitForPodsReady/update_podready_condition_failed (0.05s)
    reconciler.go:316: "level"=2 "msg"="Namespace not found; skipping selector check" "job"="default/test-job-podready-fail" "gvk"="batch/v1, Kind=Job" "namespace"="default"
    reconciler_test.go:843: unexpected reconcile error want Internal error occurred: failed calling webhook got %!s(<nil>))
=== FAIL: pkg/controller/jobframework TestReconcileGenericJobWithWaitForPodsReady (0.09s)
=== FAIL: pkg/controller/jobframework TestReconcileGenericJobWithWaitForPodsReady/update_podready_condition_failed (0.04s)
    reconciler.go:316: "level"=2 "msg"="Namespace not found; skipping selector check" "job"="default/test-job-podready-fail" "gvk"="batch/v1, Kind=Job" "namespace"="default"
    reconciler_test.go:843: unexpected reconcile error want Internal error occurred: failed calling webhook got %!s(<nil>))
```
from https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7364/pull-kueue-test-unit-main/1986730061528567808
This looks related, ptal, putting on hold until investigated
/hold

### Comment by [@olderTaoist](https://github.com/olderTaoist) — 2025-11-07T10:59:35Z

> ```
> === Failed
> === FAIL: pkg/controller/jobframework TestReconcileGenericJobWithWaitForPodsReady/update_podready_condition_failed (0.06s)
>     reconciler.go:316: "level"=2 "msg"="Namespace not found; skipping selector check" "job"="default/test-job-podready-fail" "gvk"="batch/v1, Kind=Job" "namespace"="default"
>     reconciler_test.go:843: unexpected reconcile error want Internal error occurred: failed calling webhook got %!s(<nil>))
> === FAIL: pkg/controller/jobframework TestReconcileGenericJobWithWaitForPodsReady (0.11s)
> === FAIL: pkg/controller/jobframework TestReconcileGenericJobWithWaitForPodsReady/update_podready_condition_failed (0.05s)
>     reconciler.go:316: "level"=2 "msg"="Namespace not found; skipping selector check" "job"="default/test-job-podready-fail" "gvk"="batch/v1, Kind=Job" "namespace"="default"
>     reconciler_test.go:843: unexpected reconcile error want Internal error occurred: failed calling webhook got %!s(<nil>))
> === FAIL: pkg/controller/jobframework TestReconcileGenericJobWithWaitForPodsReady (0.09s)
> === FAIL: pkg/controller/jobframework TestReconcileGenericJobWithWaitForPodsReady/update_podready_condition_failed (0.04s)
>     reconciler.go:316: "level"=2 "msg"="Namespace not found; skipping selector check" "job"="default/test-job-podready-fail" "gvk"="batch/v1, Kind=Job" "namespace"="default"
>     reconciler_test.go:843: unexpected reconcile error want Internal error occurred: failed calling webhook got %!s(<nil>))
> ```
> 
> from https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7364/pull-kueue-test-unit-main/1986730061528567808 This looks related, ptal, putting on hold until investigated /hold

the [ManagedJobsNamespaceSelectorAlwaysRespected](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/features/kube_features.go#L294) feature is enabled by default(version 0.15) in the recent merge. need create namespace as fellow:
```
managedNamespace := utiltesting.MakeNamespaceWrapper(metav1.NamespaceDefault).
				Label("managed-by-kueue", "true").
				Obj()
builder := utiltesting.NewClientBuilder(batchv1.AddToScheme, kueue.AddToScheme).
				WithObjects(tc.workload, tc.job.Object(), managedNamespace)
```

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-07T11:02:46Z

This makes perfect sense, it will require a bit of tweaking when backporting, 
I think this PR classifies as a bugfix, so it would be good if you can prepare the backports, you can use the ./hack/cherry_pick_pull.sh script

/unhold
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-07T11:02:53Z

LGTM label has been added.  <details>Git tree hash: bf25873225fdd3b2efe7b439a6dfdb8451066cba</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-11-07T11:19:36Z

@mimowo: new pull request created: #7580

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7364#issuecomment-3501299058):

>Looks great 👍 
>/lgtm
>/approve
>/cherrypick release-0.14
>/cherrypick release-0.13
>
>In case @PBundyra has some additional comments. 
>/hold
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-11-07T11:20:11Z

@mimowo: #7364 failed to apply on top of branch "release-0.13":
```
Applying: JobReconciler don't update PodsReady condition timely
Using index info to reconstruct a base tree...
M	pkg/controller/jobframework/reconciler.go
M	pkg/controller/jobframework/reconciler_test.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/controller/jobframework/reconciler_test.go
CONFLICT (content): Merge conflict in pkg/controller/jobframework/reconciler_test.go
Auto-merging pkg/controller/jobframework/reconciler.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 JobReconciler don't update PodsReady condition timely

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7364#issuecomment-3501299058):

>Looks great 👍 
>/lgtm
>/approve
>/cherrypick release-0.14
>/cherrypick release-0.13
>
>In case @PBundyra has some additional comments. 
>/hold
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-28T08:53:06Z

I think this isn't really user observable thing. Or it is very hard to observe, removing from release notes.

/release-note-edit
```release-note
NONE
```

## Reviews

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-10-28T09:16:05Z

- **pkg/controller/jobframework/reconciler.go:519** by [@PBundyra](https://github.com/PBundyra) — 2025-10-28T09:16:05Z
```diff
@@ -514,6 +514,9 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 			err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName, r.clock)
 			if err != nil {
 				log.Error(err, "Updating workload status")
+				return ctrl.Result{
+					RequeueAfter: 0,
+				}, err
```

  ```suggestion
  				return ctrl.Result{}, client.IgnoreNotFound(err)
  ```

### Review by [@olderTaoist](https://github.com/olderTaoist) (commented) — 2025-11-03T11:28:43Z

- **pkg/controller/jobframework/reconciler.go:519** by [@olderTaoist](https://github.com/olderTaoist) — 2025-11-03T11:28:43Z
```diff
@@ -514,6 +514,9 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 			err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName, r.clock)
 			if err != nil {
 				log.Error(err, "Updating workload status")
+				return ctrl.Result{
+					RequeueAfter: 0,
+				}, err
```

  Done as you saied. because my use case is that when a job's podready status is false (no timeout), it will block all subsequent jobs, i wanted the Reconciler to reconcile immediately, so I didn't use the default behavior of controller-runtime.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-03T13:35:48Z

- **pkg/controller/jobframework/reconciler.go:519** by [@PBundyra](https://github.com/PBundyra) — 2025-11-03T13:35:48Z
```diff
@@ -514,6 +514,9 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 			err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName, r.clock)
 			if err != nil {
 				log.Error(err, "Updating workload status")
+				return ctrl.Result{
+					RequeueAfter: 0,
+				}, err
```

  IIUC, the `return ctrl.Result{}, client.IgnoreNotFound(err)` will make reconciler requeue the request right away. Am I missing something?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-03T14:02:43Z

- **pkg/controller/jobframework/reconciler_test.go:739** by [@PBundyra](https://github.com/PBundyra) — 2025-11-03T13:38:43Z
```diff
@@ -714,3 +718,280 @@ func TestProcessOptions(t *testing.T) {
 		})
 	}
 }
+
+func TestReconcileGenericJobWithWaitForPodsReady(t *testing.T) {
+	const (
+		testNS = metav1.NamespaceDefault
+	)
+
+	var (
+		testLocalQueueName = kueue.LocalQueueName("default")
+		testGVK            = batchv1.SchemeGroupVersion.WithKind("Job")
+	)
+	testCases := []struct {
+		name      string
+		workload  *kueue.Workload
+		job       jobframework.GenericJob
+		wantError error
+	}{
+		{
+			name: "update podready condition failed",
+			workload: &kueue.Workload{
```

  Thanks for coming up with this test. Could you please use the wrapper you can find in: https://github.com/kubernetes-sigs/kueue/blob/main/pkg/util/testing/v1beta2/wrappers.go#L63

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-03T14:03:30Z

- **pkg/controller/jobframework/reconciler_test.go:50** by [@PBundyra](https://github.com/PBundyra) — 2025-11-03T14:03:30Z
```diff
@@ -28,20 +30,26 @@ import (
 	appsv1 "k8s.io/api/apps/v1"
 	batchv1 "k8s.io/api/batch/v1"
 	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	"k8s.io/apimachinery/pkg/api/resource"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	"k8s.io/apimachinery/pkg/types"
 	"k8s.io/utils/clock"
 	testingclock "k8s.io/utils/clock/testing"
 	"k8s.io/utils/ptr"
 	controllerruntime "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/interceptor"
 	"sigs.k8s.io/jobset/api/jobset/v1alpha2"
-
 	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
 	mocks "sigs.k8s.io/kueue/internal/mocks/controller/jobframework"
 	"sigs.k8s.io/kueue/pkg/controller/constants"
 	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	. "sigs.k8s.io/kueue/pkg/controller/jobframework"
```

  Please remove the duplicate

### Review by [@olderTaoist](https://github.com/olderTaoist) (commented) — 2025-11-04T03:01:18Z

- **pkg/controller/jobframework/reconciler.go:519** by [@olderTaoist](https://github.com/olderTaoist) — 2025-11-04T03:01:18Z
```diff
@@ -514,6 +514,9 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 			err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName, r.clock)
 			if err != nil {
 				log.Error(err, "Updating workload status")
+				return ctrl.Result{
+					RequeueAfter: 0,
+				}, err
```

  when `Reconcile` return error,  controller-runtime default retry mechanism uses a rate limiter.

### Review by [@olderTaoist](https://github.com/olderTaoist) (commented) — 2025-11-04T03:02:28Z

- **pkg/controller/jobframework/reconciler_test.go:50** by [@olderTaoist](https://github.com/olderTaoist) — 2025-11-04T03:02:28Z
```diff
@@ -28,20 +30,26 @@ import (
 	appsv1 "k8s.io/api/apps/v1"
 	batchv1 "k8s.io/api/batch/v1"
 	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	"k8s.io/apimachinery/pkg/api/resource"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	"k8s.io/apimachinery/pkg/types"
 	"k8s.io/utils/clock"
 	testingclock "k8s.io/utils/clock/testing"
 	"k8s.io/utils/ptr"
 	controllerruntime "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/interceptor"
 	"sigs.k8s.io/jobset/api/jobset/v1alpha2"
-
 	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
 	mocks "sigs.k8s.io/kueue/internal/mocks/controller/jobframework"
 	"sigs.k8s.io/kueue/pkg/controller/constants"
 	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	. "sigs.k8s.io/kueue/pkg/controller/jobframework"
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-04T09:47:28Z

- **pkg/controller/jobframework/reconciler_test.go:741** by [@PBundyra](https://github.com/PBundyra) — 2025-11-04T09:47:28Z
```diff
@@ -714,3 +720,169 @@ func TestProcessOptions(t *testing.T) {
 		})
 	}
 }
+
+func TestReconcileGenericJobWithWaitForPodsReady(t *testing.T) {
+	var (
+		testLocalQueueName = kueue.LocalQueueName("default")
+		testGVK            = batchv1.SchemeGroupVersion.WithKind("Job")
+	)
+	testCases := []struct {
+		name      string
+		workload  *kueue.Workload
+		job       GenericJob
+		wantError error
+	}{
+		{
+			name: "update podready condition failed",
+			workload: utiltestingapi.MakeWorkload("job-test-job-podready-fail", metav1.NamespaceDefault).
+				Generation(1).UID("job-test-job-podready-fail").Finalizers(kueue.ResourceInUseFinalizerName).
+				Label(constants.JobUIDLabel, "test-job-podready-fail").ControllerReference(testGVK,
+				"test-job-podready-fail", "test-job-podready-fail").Queue(testLocalQueueName).
+				Priority(0).PodSets(kueue.PodSet{
```

  Can any of these fields be removed so the test is a minimal repro of the case we want to test? I'm pretty confident e.g. Priority is redundant, can we verify the rest?

### Review by [@olderTaoist](https://github.com/olderTaoist) (commented) — 2025-11-04T11:17:52Z

- **pkg/controller/jobframework/reconciler_test.go:741** by [@olderTaoist](https://github.com/olderTaoist) — 2025-11-04T11:17:52Z
```diff
@@ -714,3 +720,169 @@ func TestProcessOptions(t *testing.T) {
 		})
 	}
 }
+
+func TestReconcileGenericJobWithWaitForPodsReady(t *testing.T) {
+	var (
+		testLocalQueueName = kueue.LocalQueueName("default")
+		testGVK            = batchv1.SchemeGroupVersion.WithKind("Job")
+	)
+	testCases := []struct {
+		name      string
+		workload  *kueue.Workload
+		job       GenericJob
+		wantError error
+	}{
+		{
+			name: "update podready condition failed",
+			workload: utiltestingapi.MakeWorkload("job-test-job-podready-fail", metav1.NamespaceDefault).
+				Generation(1).UID("job-test-job-podready-fail").Finalizers(kueue.ResourceInUseFinalizerName).
+				Label(constants.JobUIDLabel, "test-job-podready-fail").ControllerReference(testGVK,
+				"test-job-podready-fail", "test-job-podready-fail").Queue(testLocalQueueName).
+				Priority(0).PodSets(kueue.PodSet{
```

  already remove generation、uid and priority. 
  
  - `OwnerReference`:  used to get workload of `GenericJob` 
  - `PodSets`:  used to determine if the workload belongs to a `GenericJob`
  -  the rest are all basic fields of the workload

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-04T12:28:41Z

- **pkg/controller/jobframework/reconciler_test.go:741** by [@PBundyra](https://github.com/PBundyra) — 2025-11-04T12:28:41Z
```diff
@@ -714,3 +720,169 @@ func TestProcessOptions(t *testing.T) {
 		})
 	}
 }
+
+func TestReconcileGenericJobWithWaitForPodsReady(t *testing.T) {
+	var (
+		testLocalQueueName = kueue.LocalQueueName("default")
+		testGVK            = batchv1.SchemeGroupVersion.WithKind("Job")
+	)
+	testCases := []struct {
+		name      string
+		workload  *kueue.Workload
+		job       GenericJob
+		wantError error
+	}{
+		{
+			name: "update podready condition failed",
+			workload: utiltestingapi.MakeWorkload("job-test-job-podready-fail", metav1.NamespaceDefault).
+				Generation(1).UID("job-test-job-podready-fail").Finalizers(kueue.ResourceInUseFinalizerName).
+				Label(constants.JobUIDLabel, "test-job-podready-fail").ControllerReference(testGVK,
+				"test-job-podready-fail", "test-job-podready-fail").Queue(testLocalQueueName).
+				Priority(0).PodSets(kueue.PodSet{
```

  Thanks, last nits I have are:
  1) Can each builder method be in a separate line so it reads better
  2) Can we transform the list of test cases into map where key is the name of the test and value is the test case itself?

### Review by [@olderTaoist](https://github.com/olderTaoist) (commented) — 2025-11-04T13:54:25Z

- **pkg/controller/jobframework/reconciler_test.go:741** by [@olderTaoist](https://github.com/olderTaoist) — 2025-11-04T13:54:25Z
```diff
@@ -714,3 +720,169 @@ func TestProcessOptions(t *testing.T) {
 		})
 	}
 }
+
+func TestReconcileGenericJobWithWaitForPodsReady(t *testing.T) {
+	var (
+		testLocalQueueName = kueue.LocalQueueName("default")
+		testGVK            = batchv1.SchemeGroupVersion.WithKind("Job")
+	)
+	testCases := []struct {
+		name      string
+		workload  *kueue.Workload
+		job       GenericJob
+		wantError error
+	}{
+		{
+			name: "update podready condition failed",
+			workload: utiltestingapi.MakeWorkload("job-test-job-podready-fail", metav1.NamespaceDefault).
+				Generation(1).UID("job-test-job-podready-fail").Finalizers(kueue.ResourceInUseFinalizerName).
+				Label(constants.JobUIDLabel, "test-job-podready-fail").ControllerReference(testGVK,
+				"test-job-podready-fail", "test-job-podready-fail").Queue(testLocalQueueName).
+				Priority(0).PodSets(kueue.PodSet{
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-04T14:11:59Z

- **pkg/controller/jobframework/reconciler_test.go:747** by [@PBundyra](https://github.com/PBundyra) — 2025-11-04T14:11:59Z
```diff
@@ -707,3 +719,136 @@ func TestProcessOptions(t *testing.T) {
 		})
 	}
 }
+
+func TestReconcileGenericJobWithWaitForPodsReady(t *testing.T) {
+	var (
+		testLocalQueueName = kueue.LocalQueueName("default")
+		testGVK            = batchv1.SchemeGroupVersion.WithKind("Job")
+	)
+	testCases := map[string]struct {
+		workload  *kueue.Workload
+		job       GenericJob
+		wantError error
+	}{
+		"update podready condition failed": {
+			workload: utiltestingapi.MakeWorkload("job-test-job-podready-fail", metav1.NamespaceDefault).
+				Finalizers(kueue.ResourceInUseFinalizerName).
+				Label(constants.JobUIDLabel, "test-job-podready-fail").
+				ControllerReference(testGVK, "test-job-podready-fail", "test-job-podready-fail").
+				Queue(testLocalQueueName).
+				PodSets(*utiltestingapi.MakePodSet("main", 1).Obj()).
+				Conditions(metav1.Condition{
+					Type:               kueue.WorkloadAdmitted,
+					Status:             metav1.ConditionTrue,
+					Reason:             "Admitted",
+					Message:            "The workload is admitted",
+					LastTransitionTime: metav1.NewTime(time.Now()),
+				}).
+				Condition(metav1.Condition{
+					Type:               kueue.WorkloadPodsReady,
+					Status:             metav1.ConditionFalse,
+					Reason:             kueue.WorkloadWaitForStart,
+					Message:            "Not all pods are ready or succeeded",
+					LastTransitionTime: metav1.NewTime(time.Now()),
+				}).
```

  ```suggestion
  				Conditions(metav1.Condition{
  					Type:               kueue.WorkloadAdmitted,
  					Status:             metav1.ConditionTrue,
  					Reason:             "Admitted",
  					Message:            "The workload is admitted",
  					LastTransitionTime: metav1.NewTime(time.Now()),
  				}, metav1.Condition{
  					Type:               kueue.WorkloadPodsReady,
  					Status:             metav1.ConditionFalse,
  					Reason:             kueue.WorkloadWaitForStart,
  					Message:            "Not all pods are ready or succeeded",
  					LastTransitionTime: metav1.NewTime(time.Now()),
  				}).
  ```
  
  `Conditions()` overwrites previous conditions while `Condition()` doesn't. Here it didn't lead to any mistakes but let's stick with one method

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-04T14:12:21Z

- **pkg/controller/jobframework/reconciler_test.go:786** by [@PBundyra](https://github.com/PBundyra) — 2025-11-04T14:12:21Z
```diff
@@ -707,3 +719,136 @@ func TestProcessOptions(t *testing.T) {
 		})
 	}
 }
+
+func TestReconcileGenericJobWithWaitForPodsReady(t *testing.T) {
+	var (
+		testLocalQueueName = kueue.LocalQueueName("default")
+		testGVK            = batchv1.SchemeGroupVersion.WithKind("Job")
+	)
+	testCases := map[string]struct {
+		workload  *kueue.Workload
+		job       GenericJob
+		wantError error
+	}{
+		"update podready condition failed": {
+			workload: utiltestingapi.MakeWorkload("job-test-job-podready-fail", metav1.NamespaceDefault).
+				Finalizers(kueue.ResourceInUseFinalizerName).
+				Label(constants.JobUIDLabel, "test-job-podready-fail").
+				ControllerReference(testGVK, "test-job-podready-fail", "test-job-podready-fail").
+				Queue(testLocalQueueName).
+				PodSets(*utiltestingapi.MakePodSet("main", 1).Obj()).
+				Conditions(metav1.Condition{
+					Type:               kueue.WorkloadAdmitted,
+					Status:             metav1.ConditionTrue,
+					Reason:             "Admitted",
+					Message:            "The workload is admitted",
+					LastTransitionTime: metav1.NewTime(time.Now()),
+				}).
+				Condition(metav1.Condition{
+					Type:               kueue.WorkloadPodsReady,
+					Status:             metav1.ConditionFalse,
+					Reason:             kueue.WorkloadWaitForStart,
+					Message:            "Not all pods are ready or succeeded",
+					LastTransitionTime: metav1.NewTime(time.Now()),
+				}).
+				Admission(&kueue.Admission{
+					ClusterQueue: "default-cq",
+				}).
+				Obj(),
+			job: (*job.Job)(testingjob.MakeJob("test-job-podready-fail", metav1.NamespaceDefault).
+				UID("test-job-podready-fail").
+				Label(constants.QueueLabel, string(testLocalQueueName)).
+				Parallelism(1).
+				Suspend(false).
+				Containers(corev1.Container{
+					Name: "c",
+					Resources: corev1.ResourceRequirements{
+						Requests: make(corev1.ResourceList),
+					},
+				}).
+				Ready(1).
+				Obj()),
+			wantError: apierrors.NewInternalError(errors.New("failed calling webhook")),
+		},
+		"update podready condition success": {
+			workload: utiltestingapi.MakeWorkload("job-test-job-podready-success", metav1.NamespaceDefault).
+				Finalizers(kueue.ResourceInUseFinalizerName).
+				Label(constants.JobUIDLabel, "job-test-job-podready-success").
+				ControllerReference(testGVK, "test-job-podready-success", "test-job-podready-success").
+				Queue(testLocalQueueName).
+				PodSets(*utiltestingapi.MakePodSet("main", 1).Obj()).
+				Conditions(metav1.Condition{
+					Type:               kueue.WorkloadAdmitted,
+					Status:             metav1.ConditionTrue,
+					Reason:             "Admitted",
+					Message:            "The workload is admitted",
+					LastTransitionTime: metav1.NewTime(time.Now()),
+				}).
+				Condition(metav1.Condition{
+					Type:               kueue.WorkloadPodsReady,
+					Status:             metav1.ConditionFalse,
+					Reason:             kueue.WorkloadWaitForStart,
+					Message:            "Not all pods are ready or succeeded",
+					LastTransitionTime: metav1.NewTime(time.Now()),
+				}).
```

  ditto

### Review by [@olderTaoist](https://github.com/olderTaoist) (commented) — 2025-11-05T01:35:50Z

- **pkg/controller/jobframework/reconciler_test.go:747** by [@olderTaoist](https://github.com/olderTaoist) — 2025-11-05T01:35:50Z
```diff
@@ -707,3 +719,136 @@ func TestProcessOptions(t *testing.T) {
 		})
 	}
 }
+
+func TestReconcileGenericJobWithWaitForPodsReady(t *testing.T) {
+	var (
+		testLocalQueueName = kueue.LocalQueueName("default")
+		testGVK            = batchv1.SchemeGroupVersion.WithKind("Job")
+	)
+	testCases := map[string]struct {
+		workload  *kueue.Workload
+		job       GenericJob
+		wantError error
+	}{
+		"update podready condition failed": {
+			workload: utiltestingapi.MakeWorkload("job-test-job-podready-fail", metav1.NamespaceDefault).
+				Finalizers(kueue.ResourceInUseFinalizerName).
+				Label(constants.JobUIDLabel, "test-job-podready-fail").
+				ControllerReference(testGVK, "test-job-podready-fail", "test-job-podready-fail").
+				Queue(testLocalQueueName).
+				PodSets(*utiltestingapi.MakePodSet("main", 1).Obj()).
+				Conditions(metav1.Condition{
+					Type:               kueue.WorkloadAdmitted,
+					Status:             metav1.ConditionTrue,
+					Reason:             "Admitted",
+					Message:            "The workload is admitted",
+					LastTransitionTime: metav1.NewTime(time.Now()),
+				}).
+				Condition(metav1.Condition{
+					Type:               kueue.WorkloadPodsReady,
+					Status:             metav1.ConditionFalse,
+					Reason:             kueue.WorkloadWaitForStart,
+					Message:            "Not all pods are ready or succeeded",
+					LastTransitionTime: metav1.NewTime(time.Now()),
+				}).
```

  it's  my silly mistake, the intention was to use two condition or conditions

### Review by [@olderTaoist](https://github.com/olderTaoist) (commented) — 2025-11-05T01:36:12Z

- **pkg/controller/jobframework/reconciler_test.go:786** by [@olderTaoist](https://github.com/olderTaoist) — 2025-11-05T01:36:12Z
```diff
@@ -707,3 +719,136 @@ func TestProcessOptions(t *testing.T) {
 		})
 	}
 }
+
+func TestReconcileGenericJobWithWaitForPodsReady(t *testing.T) {
+	var (
+		testLocalQueueName = kueue.LocalQueueName("default")
+		testGVK            = batchv1.SchemeGroupVersion.WithKind("Job")
+	)
+	testCases := map[string]struct {
+		workload  *kueue.Workload
+		job       GenericJob
+		wantError error
+	}{
+		"update podready condition failed": {
+			workload: utiltestingapi.MakeWorkload("job-test-job-podready-fail", metav1.NamespaceDefault).
+				Finalizers(kueue.ResourceInUseFinalizerName).
+				Label(constants.JobUIDLabel, "test-job-podready-fail").
+				ControllerReference(testGVK, "test-job-podready-fail", "test-job-podready-fail").
+				Queue(testLocalQueueName).
+				PodSets(*utiltestingapi.MakePodSet("main", 1).Obj()).
+				Conditions(metav1.Condition{
+					Type:               kueue.WorkloadAdmitted,
+					Status:             metav1.ConditionTrue,
+					Reason:             "Admitted",
+					Message:            "The workload is admitted",
+					LastTransitionTime: metav1.NewTime(time.Now()),
+				}).
+				Condition(metav1.Condition{
+					Type:               kueue.WorkloadPodsReady,
+					Status:             metav1.ConditionFalse,
+					Reason:             kueue.WorkloadWaitForStart,
+					Message:            "Not all pods are ready or succeeded",
+					LastTransitionTime: metav1.NewTime(time.Now()),
+				}).
+				Admission(&kueue.Admission{
+					ClusterQueue: "default-cq",
+				}).
+				Obj(),
+			job: (*job.Job)(testingjob.MakeJob("test-job-podready-fail", metav1.NamespaceDefault).
+				UID("test-job-podready-fail").
+				Label(constants.QueueLabel, string(testLocalQueueName)).
+				Parallelism(1).
+				Suspend(false).
+				Containers(corev1.Container{
+					Name: "c",
+					Resources: corev1.ResourceRequirements{
+						Requests: make(corev1.ResourceList),
+					},
+				}).
+				Ready(1).
+				Obj()),
+			wantError: apierrors.NewInternalError(errors.New("failed calling webhook")),
+		},
+		"update podready condition success": {
+			workload: utiltestingapi.MakeWorkload("job-test-job-podready-success", metav1.NamespaceDefault).
+				Finalizers(kueue.ResourceInUseFinalizerName).
+				Label(constants.JobUIDLabel, "job-test-job-podready-success").
+				ControllerReference(testGVK, "test-job-podready-success", "test-job-podready-success").
+				Queue(testLocalQueueName).
+				PodSets(*utiltestingapi.MakePodSet("main", 1).Obj()).
+				Conditions(metav1.Condition{
+					Type:               kueue.WorkloadAdmitted,
+					Status:             metav1.ConditionTrue,
+					Reason:             "Admitted",
+					Message:            "The workload is admitted",
+					LastTransitionTime: metav1.NewTime(time.Now()),
+				}).
+				Condition(metav1.Condition{
+					Type:               kueue.WorkloadPodsReady,
+					Status:             metav1.ConditionFalse,
+					Reason:             kueue.WorkloadWaitForStart,
+					Message:            "Not all pods are ready or succeeded",
+					LastTransitionTime: metav1.NewTime(time.Now()),
+				}).
```

  Done
