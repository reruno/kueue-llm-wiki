# PR #7370: feat(KEP-3258): implement delayed admission check retries

**Summary**: feat(KEP-3258): implement delayed admission check retries

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7370

**Last updated**: 2025-11-12T17:14:05Z

---

## Metadata

- **State**: closed
- **Author**: [@dhenkel92](https://github.com/dhenkel92)
- **Branch**: `09-09-feat_admissionchecks_implement_delayed_admission` → `main`
- **Created**: 2025-10-23T18:46:24Z
- **Updated**: 2025-11-12T17:14:05Z
- **Merged**: —
- **Closed**: 2025-11-12T17:14:03Z
- **Labels**: `kind/feature`, `release-note`, `size/XXL`, `kind/api-change`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: _none_
- **Requested reviewers**: [@kannon92](https://github.com/kannon92)
- **Changed files**: 22   **+1215 / -115**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
/kind api-change

#### What this PR does / why we need it:

Implements delayed retry mechanism [KEP-3258](https://github.com/DataDog/kueue/blob/main/keps/3258-delayed-admission-check-retries/README.md) for admission checks to prevent overwhelming external controllers and reduce control plane churn.

Problem:
Previously, when admission checks transitioned to Retry state, Kueue would immediately evict workloads and requeue them, causing excessive load on admission check controllers and unnecessary API server churn, particularly when retry conditions persisted predictably.

Solution:
This implementation adds two new fields to AdmissionCheckState:
- requeueAfterSeconds: Specifies minimum wait time before retry
- retryCount: Tracks retry attempts per admission check

Key Changes:

API Changes
- Added requeueAfterSeconds and retryCount fields to AdmissionCheckState
- Changed lastTransitionTime from required to optional (now pointer type) to enable webhook-managed timestamping and reduce clock skew issues

Controller Changes
- Workload controller now respects delayed retry times before resetting checks
- Prevents premature re-admission while delays are active
- Automatically sets lastTransitionTime when admission check state changes
- Auto-increments retryCount on transition to Retry state
- Calculates maximum retry time across all admission checks
- Updates workload.status.requeueState.requeueAt with the maximum delay
- Verbs extended to include status updates for timestamp management

Behavior:
When multiple admission checks specify different delays, Kueue uses the maximum delay across all checks. Workloads are evicted immediately to release quota, but admission check states aren't reset until all delays expire, preventing race conditions where fast-responding checks could block slower ones from registering their delays.

#### Which issue(s) this PR fixes:

Fixes #3258 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?

TBD

<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Support delayed admission check retries
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-23T18:46:31Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7370#" title="Author self-approved">dhenkel92</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [mimowo](https://github.com/mimowo) for approval. For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["mimowo"]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-23T18:46:35Z

Hi @dhenkel92. Thanks for your PR.

I'm waiting for a [github.com](https://kubernetes-sigs/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-10-23T18:47:28Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 0e6754305f4546df0e4c398fff04ed2ccc067fa7 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/690321f6c4669c000896747e |

### Comment by [@kannon92](https://github.com/kannon92) — 2025-10-23T20:00:48Z

/ok-to-test

### Comment by [@dhenkel92](https://github.com/dhenkel92) — 2025-10-24T06:21:44Z

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-24T06:50:55Z

@dhenkel92 thank you for the PR
@PBundyra would you like to give it the first pass?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-24T06:51:28Z

@dhenkel92 please update the release notes

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-10-28T12:23:59Z

/release-note-edit
```release-note
Support delayed admission check retries
```

### Comment by [@dhenkel92](https://github.com/dhenkel92) — 2025-10-30T08:55:53Z

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-30T14:57:23Z

Let's check if there are not flakes 1/5
/test pull-kueue-test-e2e-certmanager-main
/test pull-kueue-test-e2e-customconfigs-main
/test pull-kueue-test-e2e-main-1-31
/test pull-kueue-test-e2e-main-1-32
/test pull-kueue-test-e2e-main-1-33
/test pull-kueue-test-e2e-main-1-34
/test pull-kueue-test-integration-baseline-main 
/test pull-kueue-test-integration-extended-main
/test pull-kueue-test-integration-multikueue-main

### Comment by [@dhenkel92](https://github.com/dhenkel92) — 2025-10-31T09:01:01Z

Let's check if there are not flakes 2/5
/test pull-kueue-test-e2e-certmanager-main
/test pull-kueue-test-e2e-customconfigs-main
/test pull-kueue-test-e2e-main-1-31
/test pull-kueue-test-e2e-main-1-32
/test pull-kueue-test-e2e-main-1-33
/test pull-kueue-test-e2e-main-1-34
/test pull-kueue-test-integration-baseline-main
/test pull-kueue-test-integration-extended-main
/test pull-kueue-test-integration-multikueue-main

### Comment by [@dhenkel92](https://github.com/dhenkel92) — 2025-10-31T10:44:48Z

Let's check if there are not flakes 3/5
/test pull-kueue-test-e2e-certmanager-main
/test pull-kueue-test-e2e-customconfigs-main
/test pull-kueue-test-e2e-main-1-31
/test pull-kueue-test-e2e-main-1-32
/test pull-kueue-test-e2e-main-1-33
/test pull-kueue-test-e2e-main-1-34
/test pull-kueue-test-integration-baseline-main
/test pull-kueue-test-integration-extended-main
/test pull-kueue-test-integration-multikueue-main

### Comment by [@dhenkel92](https://github.com/dhenkel92) — 2025-10-31T11:16:54Z

Let's check if there are not flakes 4/5
/test pull-kueue-test-e2e-certmanager-main
/test pull-kueue-test-e2e-customconfigs-main
/test pull-kueue-test-e2e-main-1-31
/test pull-kueue-test-e2e-main-1-32
/test pull-kueue-test-e2e-main-1-33
/test pull-kueue-test-e2e-main-1-34
/test pull-kueue-test-integration-baseline-main
/test pull-kueue-test-integration-extended-main
/test pull-kueue-test-integration-multikueue-main

### Comment by [@dhenkel92](https://github.com/dhenkel92) — 2025-10-31T11:43:25Z

Let's check if there are not flakes 5/5
/test pull-kueue-test-e2e-certmanager-main
/test pull-kueue-test-e2e-customconfigs-main
/test pull-kueue-test-e2e-main-1-31
/test pull-kueue-test-e2e-main-1-32
/test pull-kueue-test-e2e-main-1-33
/test pull-kueue-test-e2e-main-1-34
/test pull-kueue-test-integration-baseline-main
/test pull-kueue-test-integration-extended-main
/test pull-kueue-test-integration-multikueue-main

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-31T11:56:51Z

@dhenkel92: The following test **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:

Test name | Commit | Details | Required | Rerun command
--- | --- | --- | --- | ---
pull-kueue-test-integration-extended-main | 0e6754305f4546df0e4c398fff04ed2ccc067fa7 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7370/pull-kueue-test-integration-extended-main/1984224752469807104) | true | `/test pull-kueue-test-integration-extended-main`

[Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=7370). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3Adhenkel92). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository. I understand the commands that are listed [here](https://go.k8s.io/bot-commands).
</details>
<!-- test report -->

### Comment by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-11-12T17:13:51Z

Closing this in favor of https://github.com/kubernetes-sigs/kueue/pull/7620

### Comment by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-11-12T17:13:58Z

/close

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-12T17:14:05Z

@sohankunkerkar: Closed this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7370#issuecomment-3523024212):

>/close


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@dhenkel92](https://github.com/dhenkel92) (commented) — 2025-10-23T19:04:56Z

- **apis/kueue/v1beta1/workload_types.go:1** by [@dhenkel92](https://github.com/dhenkel92) — 2025-10-23T19:03:27Z
  I saw that my branch is outdated and that there is a `v1beta2` version now, which I should probably use.
  However, I'm not sure how to do it. When rebasing the branch I get the following errors.
  
  ```
  E1023 20:59:26.882053   22422 conversion.go:801] Warning: could not find nor generate a final Conversion function for sigs.k8s.io/kueue/apis/kueue/v1beta2.AdmissionCheckState -> sigs.k8s.io/kueue/apis/kueue/v1beta1.AdmissionCheckState
  E1023 20:59:26.882057   22422 conversion.go:802]   the following fields need manual conversion:
  E1023 20:59:26.882060   22422 conversion.go:804]       - LastTransitionTime
  E1023 20:59:26.882061   22422 conversion.go:804]       - RequeueAfterSeconds
  E1023 20:59:26.882062   22422 conversion.go:804]       - RetryCount
  ```
  
  I'll keep it like this right now and wait for some guidance.

- **pkg/workload/admissionchecks.go:106** by [@dhenkel92](https://github.com/dhenkel92) — 2025-10-23T19:04:35Z
```diff
@@ -91,38 +93,33 @@ func resetChecksOnEviction(w *kueue.Workload, now time.Time) {
 			continue
 		}
 		checks[i] = kueue.AdmissionCheckState{
-			Name:               checks[i].Name,
-			State:              kueue.CheckStatePending,
-			LastTransitionTime: metav1.NewTime(now),
-			Message:            "Reset to Pending after eviction. Previously: " + string(checks[i].State),
+			Name:                checks[i].Name,
+			State:               kueue.CheckStatePending,
+			Message:             "Reset to Pending after eviction. Previously: " + string(checks[i].State),
+			RequeueAfterSeconds: checks[i].RequeueAfterSeconds,
+			RetryCount:          checks[i].RetryCount,
 		}
 	}
 }
 
 // SetAdmissionCheckState - adds or updates newCheck in the provided checks list.
+// TODO: remove clock
```

  This definitely needs to be done at some point, but it's touching so many files.
  I think I’d prefer to remove it in a separate PR to keep the reviews easier.
  
  [This is the PR](https://github.com/kubernetes-sigs/kueue/pull/3900/files) that introduced most of the clocks back then.

### Review by [@dhenkel92](https://github.com/dhenkel92) (commented) — 2025-10-23T19:36:52Z

- **apis/kueue/v1beta1/workload_types.go:1** by [@dhenkel92](https://github.com/dhenkel92) — 2025-10-23T19:36:52Z
  I've added my changes to both versions for now, so that my branch is up-2-date.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-24T06:56:23Z

- **apis/kueue/v1beta1/workload_types.go:533** by [@mimowo](https://github.com/mimowo) — 2025-10-24T06:56:23Z
```diff
@@ -525,19 +525,36 @@ type AdmissionCheckState struct {
 	// +kubebuilder:validation:Enum=Pending;Ready;Retry;Rejected
 	State CheckState `json:"state"`
 	// lastTransitionTime is the last time the condition transitioned from one status to another.
-	// This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
-	// +required
-	// +kubebuilder:validation:Required
+	// It will be set by kueue when the underlying condition changed
+	// +optional
+	// +kubebuilder:validation:Optional
 	// +kubebuilder:validation:Type=string
 	// +kubebuilder:validation:Format=date-time
-	LastTransitionTime metav1.Time `json:"lastTransitionTime"`
+	LastTransitionTime *metav1.Time `json:"lastTransitionTime,omitempty"`
```

  I think we cannot change it in v1beta1, only in v1beta2 and add conversion. For example it could make existing (generated) API clients no longer work for v1beta1.
  
  The new incrementally added fields below are fine.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-24T07:02:32Z

- **config/components/webhook/manifests.yaml:371** by [@mimowo](https://github.com/mimowo) — 2025-10-24T07:02:32Z
```diff
@@ -365,8 +365,10 @@ webhooks:
     - v1beta1
     operations:
     - CREATE
+    - UPDATE
     resources:
     - workloads
+    - workloads/status
```

  I see what you do so - to default the LastTransitionTime. enabling webhooks for this reason for updates feels like overkill. So, I'm rather leaning to keep it as is wrt to LastTransitionTime.
  
  In the future we will have MAP (mutating admission policies). This will be a better task for them.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-24T07:03:57Z

- **apis/kueue/v1beta1/workload_types.go:533** by [@mimowo](https://github.com/mimowo) — 2025-10-24T07:03:56Z
```diff
@@ -525,19 +525,36 @@ type AdmissionCheckState struct {
 	// +kubebuilder:validation:Enum=Pending;Ready;Retry;Rejected
 	State CheckState `json:"state"`
 	// lastTransitionTime is the last time the condition transitioned from one status to another.
-	// This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
-	// +required
-	// +kubebuilder:validation:Required
+	// It will be set by kueue when the underlying condition changed
+	// +optional
+	// +kubebuilder:validation:Optional
 	// +kubebuilder:validation:Type=string
 	// +kubebuilder:validation:Format=date-time
-	LastTransitionTime metav1.Time `json:"lastTransitionTime"`
+	LastTransitionTime *metav1.Time `json:"lastTransitionTime,omitempty"`
```

  After seeing the related change I would rather suggest to do not change the approach wrt LasttTransitionTime in v1beta2: https://github.com/kubernetes-sigs/kueue/pull/7370/files#r2459092243
  
  Out current approach is actually consistent with k8s conditions.
  
  Sure, defaulting feels nice, but I think we could revisit the topic when MAP graduates

### Review by [@dhenkel92](https://github.com/dhenkel92) (commented) — 2025-10-24T15:20:00Z

- **config/components/webhook/manifests.yaml:371** by [@dhenkel92](https://github.com/dhenkel92) — 2025-10-24T15:20:00Z
```diff
@@ -365,8 +365,10 @@ webhooks:
     - v1beta1
     operations:
     - CREATE
+    - UPDATE
     resources:
     - workloads
+    - workloads/status
```

  I see and it makes sense to me 👍 
  I would keep the new API fields, revert the LastTransitionTime update and move the other mutations to the reconciler.

### Review by [@dhenkel92](https://github.com/dhenkel92) (commented) — 2025-10-24T15:28:53Z

- **apis/kueue/v1beta1/workload_types.go:533** by [@dhenkel92](https://github.com/dhenkel92) — 2025-10-24T15:28:53Z
```diff
@@ -525,19 +525,36 @@ type AdmissionCheckState struct {
 	// +kubebuilder:validation:Enum=Pending;Ready;Retry;Rejected
 	State CheckState `json:"state"`
 	// lastTransitionTime is the last time the condition transitioned from one status to another.
-	// This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
-	// +required
-	// +kubebuilder:validation:Required
+	// It will be set by kueue when the underlying condition changed
+	// +optional
+	// +kubebuilder:validation:Optional
 	// +kubebuilder:validation:Type=string
 	// +kubebuilder:validation:Format=date-time
-	LastTransitionTime metav1.Time `json:"lastTransitionTime"`
+	LastTransitionTime *metav1.Time `json:"lastTransitionTime,omitempty"`
```

  🤔 That's true. I didn't notice that the LastTransitionTime of other conditions are not optional.
  I'll remove that change.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-27T18:50:51Z

- **pkg/controller/admissionchecks/provisioning/controller.go:63** by [@mimowo](https://github.com/mimowo) — 2025-10-27T18:50:51Z
```diff
@@ -60,13 +60,9 @@ const (
 	podTemplatesPrefix     = "ppt"
 )
 
-var (
-	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
-)
+var errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
```

  I'm considering if we could revert these provisioning changes and leave them to the follow up PR. 
  It would make review easier as the ProvReq might be tricky on upgrade of Kueue for already running workloads. It seems a big bulk of changes is just about ProvReq. I fully support migrating ProvReq to the new API, but I think it will be much safer in 2 PRs for the ease of reasoning about the changes

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-27T18:52:22Z

- **config/components/webhook/manifests.yaml:371** by [@mimowo](https://github.com/mimowo) — 2025-10-27T18:52:22Z
```diff
@@ -365,8 +365,10 @@ webhooks:
     - v1beta1
     operations:
     - CREATE
+    - UPDATE
     resources:
     - workloads
+    - workloads/status
```

  Yes, moving this code to reconcilers and minimizing the diff would help a lot to review. I will give another pass when this is addressed.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-10-28T11:21:01Z

- **apis/kueue/v1beta1/workload_types.go:542** by [@pajakd](https://github.com/pajakd) — 2025-10-28T11:21:01Z
```diff
@@ -537,7 +537,24 @@ type AdmissionCheckState struct {
 	// +kubebuilder:validation:Required
 	// +kubebuilder:validation:MaxLength=32768
 	Message string `json:"message" protobuf:"bytes,6,opt,name=message"`
-
+	// requeueAfterSeconds indicates how long to wait at least before
+	// retrying to admit the workload.
+	// When State=Retry, admission check controllers can set this
```

  It looks like the part `When State=Retry` should at the end of the previous sentence.

### Review by [@dhenkel92](https://github.com/dhenkel92) (commented) — 2025-10-28T12:12:24Z

- **apis/kueue/v1beta1/workload_types.go:542** by [@dhenkel92](https://github.com/dhenkel92) — 2025-10-28T12:12:24Z
```diff
@@ -537,7 +537,24 @@ type AdmissionCheckState struct {
 	// +kubebuilder:validation:Required
 	// +kubebuilder:validation:MaxLength=32768
 	Message string `json:"message" protobuf:"bytes,6,opt,name=message"`
-
+	// requeueAfterSeconds indicates how long to wait at least before
+	// retrying to admit the workload.
+	// When State=Retry, admission check controllers can set this
```

  👍 I updated the wording.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-10-28T12:25:36Z

- **pkg/controller/core/workload_controller.go:204** by [@pajakd](https://github.com/pajakd) — 2025-10-28T12:25:36Z
```diff
@@ -194,6 +194,26 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		return ctrl.Result{}, nil
 	}
 
+	maxTime := workload.GetMaxRetryTime(&wl)
+	if !maxTime.IsZero() {
+		var updated bool
+		err := workload.PatchAdmissionStatus(ctx, r.client, &wl, r.clock, func() (*kueue.Workload, bool, error) {
+			if wl.Status.RequeueState == nil {
+				wl.Status.RequeueState = &kueue.RequeueState{}
+			}
+			log.V(2).Info("At leas one admission check set a retry time", "maxTime", maxTime, "requeueState.RequeueAt", wl.Status.RequeueState.RequeueAt)
```

  "At leas" -> "At least"

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-10-28T13:02:32Z

- **apis/kueue/v1beta1/workload_types.go:542** by [@pajakd](https://github.com/pajakd) — 2025-10-28T13:02:32Z
```diff
@@ -537,7 +537,24 @@ type AdmissionCheckState struct {
 	// +kubebuilder:validation:Required
 	// +kubebuilder:validation:MaxLength=32768
 	Message string `json:"message" protobuf:"bytes,6,opt,name=message"`
-
+	// requeueAfterSeconds indicates how long to wait at least before
+	// retrying to admit the workload.
+	// The admission check controllers can set this field when State=Retry
```

  Just for me to understand. This field is set when the State transitions to Retry, right?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-28T17:37:09Z

- **pkg/controller/core/workload_controller.go:295** by [@mimowo](https://github.com/mimowo) — 2025-10-28T17:37:09Z
```diff
@@ -263,35 +283,56 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
+		var requeueAfter *time.Duration
+		if wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt != nil {
+			requeueAfter = ptr.To(wl.Status.RequeueState.RequeueAt.Sub(r.clock.Now()))
+		}
+		if requeueAfter != nil && *requeueAfter > 0 {
+			log.V(3).Info("Waiting for backoff to finish", "backoff", *requeueAfter)
+			return reconcile.Result{RequeueAfter: *requeueAfter}, nil
+		}
+
+		if requeueAfter != nil && workload.Status(&wl) == workload.StatusPending {
```

  ```suggestion
  		if wl.Status.RequeueState.RequeueAt != nil && workload.Status(&wl) == workload.StatusPending {
  ```
  to better capture the intention that the code is meant to clean the field

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-28T17:37:19Z

- **pkg/controller/core/workload_controller.go:301** by [@mimowo](https://github.com/mimowo) — 2025-10-28T17:37:19Z
```diff
@@ -263,35 +283,56 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
+		var requeueAfter *time.Duration
+		if wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt != nil {
+			requeueAfter = ptr.To(wl.Status.RequeueState.RequeueAt.Sub(r.clock.Now()))
+		}
+		if requeueAfter != nil && *requeueAfter > 0 {
+			log.V(3).Info("Waiting for backoff to finish", "backoff", *requeueAfter)
+			return reconcile.Result{RequeueAfter: *requeueAfter}, nil
+		}
+
+		if requeueAfter != nil && workload.Status(&wl) == workload.StatusPending {
+			log.V(3).Info("Pending workload requeued after backoff")
+
+			err := workload.PatchAdmissionStatus(ctx, r.client, &wl, r.clock, func() (*kueue.Workload, bool, error) {
+				if wl.Status.RequeueState == nil || wl.Status.RequeueState.RequeueAt == nil {
+					return nil, false, nil
+				}
```

  This code looks dead

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-28T17:44:49Z

- **pkg/controller/core/workload_controller.go:309** by [@mimowo](https://github.com/mimowo) — 2025-10-28T17:44:49Z
```diff
@@ -263,35 +283,56 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
+		var requeueAfter *time.Duration
+		if wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt != nil {
+			requeueAfter = ptr.To(wl.Status.RequeueState.RequeueAt.Sub(r.clock.Now()))
+		}
+		if requeueAfter != nil && *requeueAfter > 0 {
+			log.V(3).Info("Waiting for backoff to finish", "backoff", *requeueAfter)
+			return reconcile.Result{RequeueAfter: *requeueAfter}, nil
+		}
+
+		if requeueAfter != nil && workload.Status(&wl) == workload.StatusPending {
+			log.V(3).Info("Pending workload requeued after backoff")
+
+			err := workload.PatchAdmissionStatus(ctx, r.client, &wl, r.clock, func() (*kueue.Workload, bool, error) {
+				if wl.Status.RequeueState == nil || wl.Status.RequeueState.RequeueAt == nil {
+					return nil, false, nil
+				}
+				wl.Status.RequeueState.RequeueAt = nil
+				if wl.Status.RequeueState.Count == nil {
+					wl.Status.RequeueState = nil
+				}
+				workload.ResetAdmissionCheckRequeueState(&wl)
+				return &wl, true, nil
+			})
+			if err != nil && !apierrors.IsNotFound(err) {
```

  ```suggestion
  			if err != nil {
  ```
  or just ignore and exit early

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-28T17:47:37Z

- **pkg/controller/core/workload_controller.go:314** by [@mimowo](https://github.com/mimowo) — 2025-10-28T17:47:37Z
```diff
@@ -263,35 +283,56 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
+		var requeueAfter *time.Duration
+		if wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt != nil {
+			requeueAfter = ptr.To(wl.Status.RequeueState.RequeueAt.Sub(r.clock.Now()))
+		}
+		if requeueAfter != nil && *requeueAfter > 0 {
+			log.V(3).Info("Waiting for backoff to finish", "backoff", *requeueAfter)
+			return reconcile.Result{RequeueAfter: *requeueAfter}, nil
+		}
+
+		if requeueAfter != nil && workload.Status(&wl) == workload.StatusPending {
+			log.V(3).Info("Pending workload requeued after backoff")
+
+			err := workload.PatchAdmissionStatus(ctx, r.client, &wl, r.clock, func() (*kueue.Workload, bool, error) {
+				if wl.Status.RequeueState == nil || wl.Status.RequeueState.RequeueAt == nil {
+					return nil, false, nil
+				}
+				wl.Status.RequeueState.RequeueAt = nil
+				if wl.Status.RequeueState.Count == nil {
+					wl.Status.RequeueState = nil
+				}
+				workload.ResetAdmissionCheckRequeueState(&wl)
+				return &wl, true, nil
+			})
+			if err != nil && !apierrors.IsNotFound(err) {
+				return ctrl.Result{}, err
+			}
+
+			if err := r.queues.AddOrUpdateWorkload(wl.DeepCopy()); err != nil {
+				log.V(2).Info("ignored an error for now", "error", err)
```

  ```suggestion
  				log.V(2).Info("failed to put the workload back into queue", "error", err)
  ```
  Feel free to rephrase or drop the log
  this might actually be a bugfix because it could error and we would silence that. cc @PBundyra

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-28T17:48:04Z

- **pkg/controller/core/workload_controller.go:206** by [@mimowo](https://github.com/mimowo) — 2025-10-28T17:48:04Z
```diff
@@ -194,6 +194,26 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		return ctrl.Result{}, nil
 	}
 
+	maxTime := workload.GetMaxRetryTime(&wl)
+	if !maxTime.IsZero() {
+		var updated bool
+		err := workload.PatchAdmissionStatus(ctx, r.client, &wl, r.clock, func() (*kueue.Workload, bool, error) {
+			if wl.Status.RequeueState == nil {
+				wl.Status.RequeueState = &kueue.RequeueState{}
+			}
+			log.V(2).Info("At leas one admission check set a retry time", "maxTime", maxTime, "requeueState.RequeueAt", wl.Status.RequeueState.RequeueAt)
+			if wl.Status.RequeueState.RequeueAt != nil && (maxTime.Equal(wl.Status.RequeueState.RequeueAt) || maxTime.Before(wl.Status.RequeueState.RequeueAt)) {
+				log.V(2).Info("Ignoring AC retry time as it's before the current RequeueState time", "requeueState.RequeueAt", wl.Status.RequeueState.RequeueAt, "maxTime", maxTime)
```

  ```suggestion
  				log.V(3).Info("Ignoring AC retry time as it's before the current RequeueState time", "requeueState.RequeueAt", wl.Status.RequeueState.RequeueAt, "maxTime", maxTime)
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-28T17:49:20Z

- **pkg/workload/admissionchecks_test.go:417** by [@mimowo](https://github.com/mimowo) — 2025-10-28T17:49:20Z
```diff
@@ -412,60 +410,53 @@ func TestSetCheckState(t *testing.T) {
 		"add new check": {
 			origStates: []kueue.AdmissionCheckState{},
 			state: kueue.AdmissionCheckState{
-				Name:               "check1",
-				State:              kueue.CheckStatePending,
-				LastTransitionTime: *t0.DeepCopy(),
```

  Please check if this can be reverted after the recent changes

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-28T17:50:04Z

- **pkg/workload/workload.go:64** by [@mimowo](https://github.com/mimowo) — 2025-10-28T17:50:04Z
```diff
@@ -61,16 +61,14 @@ const (
 	StatusFinished      = "finished"
 )
 
-var (
```

  nit, I would suggest to remove the distractions, but feel free to keep if done by ide automatically

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-28T17:56:45Z

- **pkg/workload/workload.go:626** by [@mimowo](https://github.com/mimowo) — 2025-10-28T17:56:45Z
```diff
@@ -620,7 +619,13 @@ func UpdateRequeueState(wl *kueue.Workload, backoffBaseSeconds int32, backoffMax
 	backoff := wait.NewBackoff(time.Duration(backoffBaseSeconds)*time.Second, time.Duration(backoffMaxSeconds)*time.Second, 2, 0.0001)
 	waitDuration := backoff.WaitTime(int(requeuingCount))
 
-	wl.Status.RequeueState.RequeueAt = ptr.To(metav1.NewTime(clock.Now().Add(waitDuration)))
+	// The requeue state is shared between multiple components,
+	// so we have to ensure that we don't overwrite a future requeue.
+	currentRequeueAt := ptr.Deref(wl.Status.RequeueState.RequeueAt, metav1.NewTime(time.Time{}))
+	newRequeueAt := metav1.NewTime(clock.Now().Add(waitDuration))
+	if currentRequeueAt.Before(&newRequeueAt) {
```

  This feels a bit cumbersome and we have similar logic already in the workload_controller, consider if possible to extract a helper function and commonize the code.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-28T17:57:41Z

- **test/integration/singlecluster/scheduler/delayedadmission/delayed_admission_test.go:81** by [@mimowo](https://github.com/mimowo) — 2025-10-28T17:57:41Z
```diff
@@ -0,0 +1,218 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package delayedadmission
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithDelayedAdmissionChecks", func() {
+	var (
+		// Values referenced by tests:
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		clusterQ      *kueue.ClusterQueue
+		localQ        *kueue.LocalQueue
+		delayedCheck  *kueue.AdmissionCheck
+		realClock     = clock.RealClock{}
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(&configapi.Configuration{}))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		util.MustCreate(ctx, k8sClient, defaultFlavor)
+
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "delayed-retry-")
+
+		delayedCheck = utiltestingapi.MakeAdmissionCheck("delayed-check").ControllerName("ctrl").Obj()
+		util.MustCreate(ctx, k8sClient, delayedCheck)
+		util.SetAdmissionCheckActive(ctx, k8sClient, delayedCheck, metav1.ConditionTrue)
+
+		clusterQ = utiltestingapi.MakeClusterQueue("dev-cq").
+			ResourceGroup(*utiltestingapi.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "5").Obj()).
+			AdmissionChecks(kueue.AdmissionCheckReference(delayedCheck.Name)).
+			Obj()
+		util.MustCreate(ctx, k8sClient, clusterQ)
+
+		localQ = utiltestingapi.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(clusterQ.Name).Obj()
+		util.MustCreate(ctx, k8sClient, localQ)
+
+		util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQ)
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQ, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, delayedCheck, true)
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.Context("Delayed admission check", func() {
+		ginkgo.BeforeEach(func() {
+		})
```

  nir, drop

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-28T17:58:26Z

- **test/integration/singlecluster/scheduler/delayedadmission/delayed_admission_test.go:118** by [@mimowo](https://github.com/mimowo) — 2025-10-28T17:58:26Z
```diff
@@ -0,0 +1,218 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package delayedadmission
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithDelayedAdmissionChecks", func() {
+	var (
+		// Values referenced by tests:
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		clusterQ      *kueue.ClusterQueue
+		localQ        *kueue.LocalQueue
+		delayedCheck  *kueue.AdmissionCheck
+		realClock     = clock.RealClock{}
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(&configapi.Configuration{}))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		util.MustCreate(ctx, k8sClient, defaultFlavor)
+
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "delayed-retry-")
+
+		delayedCheck = utiltestingapi.MakeAdmissionCheck("delayed-check").ControllerName("ctrl").Obj()
+		util.MustCreate(ctx, k8sClient, delayedCheck)
+		util.SetAdmissionCheckActive(ctx, k8sClient, delayedCheck, metav1.ConditionTrue)
+
+		clusterQ = utiltestingapi.MakeClusterQueue("dev-cq").
+			ResourceGroup(*utiltestingapi.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "5").Obj()).
+			AdmissionChecks(kueue.AdmissionCheckReference(delayedCheck.Name)).
+			Obj()
+		util.MustCreate(ctx, k8sClient, clusterQ)
+
+		localQ = utiltestingapi.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(clusterQ.Name).Obj()
+		util.MustCreate(ctx, k8sClient, localQ)
+
+		util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQ)
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQ, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, delayedCheck, true)
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.Context("Delayed admission check", func() {
+		ginkgo.BeforeEach(func() {
+		})
+
+		ginkgo.It("should requeue workloads after a delayed retry", func() {
+			wl := utiltestingapi.MakeWorkload("delayed-ac-retry", ns.Name).
+				RequestAndLimit(corev1.ResourceCPU, "1").
+				Queue(kueue.LocalQueueName(localQ.Name)).
+				Obj()
+
+			ginkgo.By("Creating the job", func() {
+				util.MustCreate(ctx, k8sClient, wl)
+			})
+			wlLookupKey := client.ObjectKeyFromObject(wl)
+			createdWorkload := &kueue.Workload{}
+
+			ginkgo.By("Verifying workload is created but not admitted yet", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					g.Expect(workload.HasQuotaReservation(createdWorkload)).To(gomega.BeTrue())
+					g.Expect(workload.IsAdmitted(createdWorkload)).To(gomega.BeFalse())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Marking AC as Retry to trigger an immediate retry", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					workload.SetAdmissionCheckState(&createdWorkload.Status.AdmissionChecks, kueue.AdmissionCheckState{
+						Name:    kueue.AdmissionCheckReference(delayedCheck.Name),
+						State:   kueue.CheckStateRetry,
+						Message: "Retrying admission check",
+					}, realClock)
+					g.Expect(k8sClient.Status().Update(ctx, createdWorkload)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Finish eviction", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					util.FinishEvictionForWorkloads(ctx, k8sClient, createdWorkload)
```

  FinishEvictionForWorkloads already has eventually inside

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-28T18:08:17Z

- **test/integration/singlecluster/scheduler/delayedadmission/delayed_admission_test.go:153** by [@mimowo](https://github.com/mimowo) — 2025-10-28T18:08:18Z
```diff
@@ -0,0 +1,218 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package delayedadmission
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithDelayedAdmissionChecks", func() {
+	var (
+		// Values referenced by tests:
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		clusterQ      *kueue.ClusterQueue
+		localQ        *kueue.LocalQueue
+		delayedCheck  *kueue.AdmissionCheck
+		realClock     = clock.RealClock{}
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(&configapi.Configuration{}))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		util.MustCreate(ctx, k8sClient, defaultFlavor)
+
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "delayed-retry-")
+
+		delayedCheck = utiltestingapi.MakeAdmissionCheck("delayed-check").ControllerName("ctrl").Obj()
+		util.MustCreate(ctx, k8sClient, delayedCheck)
+		util.SetAdmissionCheckActive(ctx, k8sClient, delayedCheck, metav1.ConditionTrue)
+
+		clusterQ = utiltestingapi.MakeClusterQueue("dev-cq").
+			ResourceGroup(*utiltestingapi.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "5").Obj()).
+			AdmissionChecks(kueue.AdmissionCheckReference(delayedCheck.Name)).
+			Obj()
+		util.MustCreate(ctx, k8sClient, clusterQ)
+
+		localQ = utiltestingapi.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(clusterQ.Name).Obj()
+		util.MustCreate(ctx, k8sClient, localQ)
+
+		util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQ)
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQ, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, delayedCheck, true)
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.Context("Delayed admission check", func() {
+		ginkgo.BeforeEach(func() {
+		})
+
+		ginkgo.It("should requeue workloads after a delayed retry", func() {
+			wl := utiltestingapi.MakeWorkload("delayed-ac-retry", ns.Name).
+				RequestAndLimit(corev1.ResourceCPU, "1").
+				Queue(kueue.LocalQueueName(localQ.Name)).
+				Obj()
+
+			ginkgo.By("Creating the job", func() {
+				util.MustCreate(ctx, k8sClient, wl)
+			})
+			wlLookupKey := client.ObjectKeyFromObject(wl)
+			createdWorkload := &kueue.Workload{}
+
+			ginkgo.By("Verifying workload is created but not admitted yet", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					g.Expect(workload.HasQuotaReservation(createdWorkload)).To(gomega.BeTrue())
+					g.Expect(workload.IsAdmitted(createdWorkload)).To(gomega.BeFalse())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Marking AC as Retry to trigger an immediate retry", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					workload.SetAdmissionCheckState(&createdWorkload.Status.AdmissionChecks, kueue.AdmissionCheckState{
+						Name:    kueue.AdmissionCheckReference(delayedCheck.Name),
+						State:   kueue.CheckStateRetry,
+						Message: "Retrying admission check",
+					}, realClock)
+					g.Expect(k8sClient.Status().Update(ctx, createdWorkload)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Finish eviction", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					util.FinishEvictionForWorkloads(ctx, k8sClient, createdWorkload)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verifying retry counter is incremented after transition to Pending", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+
+					ac := admissioncheck.FindAdmissionCheck(createdWorkload.Status.AdmissionChecks, kueue.AdmissionCheckReference(delayedCheck.Name))
+					g.Expect(ac).ToNot(gomega.BeNil())
+					g.Expect(ac.State).To(gomega.Equal(kueue.CheckStatePending))
+					g.Expect(ac.LastTransitionTime).ToNot(gomega.BeNil())
+					g.Expect(ac.RetryCount).ToNot(gomega.BeNil())
+					g.Expect(*ac.RetryCount).To(gomega.Equal(int32(1)))
+
+					g.Expect(createdWorkload.Status.RequeueState).To(gomega.BeNil())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Wait for job to have quota again", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					g.Expect(workload.HasQuotaReservation(createdWorkload)).To(gomega.BeTrue())
+					g.Expect(workload.IsAdmitted(createdWorkload)).To(gomega.BeFalse())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Marking AC as Retry to trigger a delayed retry", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					workload.SetAdmissionCheckState(&createdWorkload.Status.AdmissionChecks, kueue.AdmissionCheckState{
+						Name:                kueue.AdmissionCheckReference(delayedCheck.Name),
+						State:               kueue.CheckStateRetry,
+						Message:             "Retrying admission check",
+						RequeueAfterSeconds: ptr.To(int32(5)),
```

  Add a comment why this is set to 5s and annotate the test case as "slow"

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-28T18:09:18Z

- **test/integration/singlecluster/scheduler/delayedadmission/delayed_admission_test.go:194** by [@mimowo](https://github.com/mimowo) — 2025-10-28T18:09:18Z
```diff
@@ -0,0 +1,218 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package delayedadmission
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithDelayedAdmissionChecks", func() {
+	var (
+		// Values referenced by tests:
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		clusterQ      *kueue.ClusterQueue
+		localQ        *kueue.LocalQueue
+		delayedCheck  *kueue.AdmissionCheck
+		realClock     = clock.RealClock{}
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(&configapi.Configuration{}))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		util.MustCreate(ctx, k8sClient, defaultFlavor)
+
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "delayed-retry-")
+
+		delayedCheck = utiltestingapi.MakeAdmissionCheck("delayed-check").ControllerName("ctrl").Obj()
+		util.MustCreate(ctx, k8sClient, delayedCheck)
+		util.SetAdmissionCheckActive(ctx, k8sClient, delayedCheck, metav1.ConditionTrue)
+
+		clusterQ = utiltestingapi.MakeClusterQueue("dev-cq").
+			ResourceGroup(*utiltestingapi.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "5").Obj()).
+			AdmissionChecks(kueue.AdmissionCheckReference(delayedCheck.Name)).
+			Obj()
+		util.MustCreate(ctx, k8sClient, clusterQ)
+
+		localQ = utiltestingapi.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(clusterQ.Name).Obj()
+		util.MustCreate(ctx, k8sClient, localQ)
+
+		util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQ)
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQ, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, delayedCheck, true)
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.Context("Delayed admission check", func() {
+		ginkgo.BeforeEach(func() {
+		})
+
+		ginkgo.It("should requeue workloads after a delayed retry", func() {
+			wl := utiltestingapi.MakeWorkload("delayed-ac-retry", ns.Name).
+				RequestAndLimit(corev1.ResourceCPU, "1").
+				Queue(kueue.LocalQueueName(localQ.Name)).
+				Obj()
+
+			ginkgo.By("Creating the job", func() {
+				util.MustCreate(ctx, k8sClient, wl)
+			})
+			wlLookupKey := client.ObjectKeyFromObject(wl)
+			createdWorkload := &kueue.Workload{}
+
+			ginkgo.By("Verifying workload is created but not admitted yet", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					g.Expect(workload.HasQuotaReservation(createdWorkload)).To(gomega.BeTrue())
+					g.Expect(workload.IsAdmitted(createdWorkload)).To(gomega.BeFalse())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Marking AC as Retry to trigger an immediate retry", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					workload.SetAdmissionCheckState(&createdWorkload.Status.AdmissionChecks, kueue.AdmissionCheckState{
+						Name:    kueue.AdmissionCheckReference(delayedCheck.Name),
+						State:   kueue.CheckStateRetry,
+						Message: "Retrying admission check",
+					}, realClock)
+					g.Expect(k8sClient.Status().Update(ctx, createdWorkload)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Finish eviction", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					util.FinishEvictionForWorkloads(ctx, k8sClient, createdWorkload)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verifying retry counter is incremented after transition to Pending", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+
+					ac := admissioncheck.FindAdmissionCheck(createdWorkload.Status.AdmissionChecks, kueue.AdmissionCheckReference(delayedCheck.Name))
+					g.Expect(ac).ToNot(gomega.BeNil())
+					g.Expect(ac.State).To(gomega.Equal(kueue.CheckStatePending))
+					g.Expect(ac.LastTransitionTime).ToNot(gomega.BeNil())
+					g.Expect(ac.RetryCount).ToNot(gomega.BeNil())
+					g.Expect(*ac.RetryCount).To(gomega.Equal(int32(1)))
+
+					g.Expect(createdWorkload.Status.RequeueState).To(gomega.BeNil())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Wait for job to have quota again", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					g.Expect(workload.HasQuotaReservation(createdWorkload)).To(gomega.BeTrue())
+					g.Expect(workload.IsAdmitted(createdWorkload)).To(gomega.BeFalse())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Marking AC as Retry to trigger a delayed retry", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					workload.SetAdmissionCheckState(&createdWorkload.Status.AdmissionChecks, kueue.AdmissionCheckState{
+						Name:                kueue.AdmissionCheckReference(delayedCheck.Name),
+						State:               kueue.CheckStateRetry,
+						Message:             "Retrying admission check",
+						RequeueAfterSeconds: ptr.To(int32(5)),
+					}, realClock)
+					g.Expect(k8sClient.Status().Update(ctx, createdWorkload)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Finish eviction", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					util.FinishEvictionForWorkloads(ctx, k8sClient, createdWorkload)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verifying retry counter is incremented after transition to Pending", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+
+					ac := admissioncheck.FindAdmissionCheck(createdWorkload.Status.AdmissionChecks, kueue.AdmissionCheckReference(delayedCheck.Name))
+					g.Expect(ac).ToNot(gomega.BeNil())
+					g.Expect(ac.State).To(gomega.Equal(kueue.CheckStatePending))
+					g.Expect(ac.LastTransitionTime).To(gomega.Not(gomega.BeNil()))
+					g.Expect(ac.RetryCount).ToNot(gomega.BeNil())
+					g.Expect(*ac.RetryCount).To(gomega.Equal(int32(2)))
+					g.Expect(ac.RequeueAfterSeconds).ToNot(gomega.BeNil())
+
+					g.Expect(createdWorkload.Status.RequeueState).ToNot(gomega.BeNil())
+					g.Expect(createdWorkload.Status.RequeueState.RequeueAt).ToNot(gomega.BeNil())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Wait for job to have quota again", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					g.Expect(workload.HasQuotaReservation(createdWorkload)).To(gomega.BeTrue())
+					g.Expect(workload.IsAdmitted(createdWorkload)).To(gomega.BeFalse())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
```

  IIUC in this block we could also check that the RequeueAt field is cleared

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-28T18:10:54Z

- **test/integration/singlecluster/scheduler/podsready/scheduler_test.go:313** by [@mimowo](https://github.com/mimowo) — 2025-10-28T18:10:54Z
```diff
@@ -309,8 +309,8 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			ginkgo.By("finish the eviction, and the workload is pending by backoff")
 			util.FinishEvictionForWorkloads(ctx, k8sClient, prodWl)
 			util.ExpectWorkloadToHaveRequeueState(ctx, k8sClient, client.ObjectKeyFromObject(prodWl), &kueue.RequeueState{
-				Count: ptr.To[int32](1),
-			}, false)
+				Count: ptr.To[int32](2),
+			}, true)
```

  Please verify why this change is needed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-28T18:11:13Z

- **test/integration/singlecluster/scheduler/podsready/scheduler_test.go:725** by [@mimowo](https://github.com/mimowo) — 2025-10-28T18:11:13Z
```diff
@@ -712,15 +712,14 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
 			util.ExpectQuotaReservedWorkloadsTotalMetric(prodClusterQ, "", 2)
 			util.ExpectAdmittedWorkloadsTotalMetric(prodClusterQ, "", 2)
 			util.AwaitWorkloadEvictionByPodsReadyTimeout(ctx, k8sClient, client.ObjectKeyFromObject(prodWl), podsReadyTimeout)
-			util.SetRequeuedConditionWithPodsReadyTimeout(ctx, k8sClient, client.ObjectKeyFromObject(prodWl))
 			util.ExpectWorkloadToHaveRequeueState(ctx, k8sClient, client.ObjectKeyFromObject(prodWl), &kueue.RequeueState{
 				Count: ptr.To[int32](2),
-			}, false)
+			}, true)
```

  Also check if this change is needed and why

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-28T18:15:55Z

- **pkg/workload/admissionchecks.go:216** by [@mimowo](https://github.com/mimowo) — 2025-10-28T18:15:55Z
```diff
@@ -186,3 +196,54 @@ func HasRejectedChecks(wl *kueue.Workload) bool {
 	}
 	return false
 }
+
+// GetMaxRetryTime returns the max retry time from all the admission checks.
+// It will return a zero time if no retry time is found.
+func GetMaxRetryTime(wl *kueue.Workload) metav1.Time {
+	var max time.Time
+	for i := range wl.Status.AdmissionChecks {
+		if wl.Status.AdmissionChecks[i].RequeueAfterSeconds == nil {
+			continue
+		}
+		// This should never happen, but prevents panics
+		if wl.Status.AdmissionChecks[i].LastTransitionTime.IsZero() {
+			continue
+		}
+
+		retryTime := wl.Status.AdmissionChecks[i].LastTransitionTime.Add(time.Duration(*wl.Status.AdmissionChecks[i].RequeueAfterSeconds) * time.Second)
```

  I think this function should check if the AC state=Retry as the guard.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-28T18:35:11Z

- **pkg/workload/admissionchecks.go:106** by [@mimowo](https://github.com/mimowo) — 2025-10-28T18:35:11Z
```diff
@@ -90,11 +92,18 @@ func resetChecksOnEviction(w *kueue.Workload, now time.Time) {
 		if checks[i].State == kueue.CheckStatePending {
 			continue
 		}
+		var retryCount *int32
+		if checks[i].State == kueue.CheckStateRetry {
+			tmpRetryCount := ptr.Deref(checks[i].RetryCount, 0) + 1
+			retryCount = ptr.To(tmpRetryCount)
+		}
 		checks[i] = kueue.AdmissionCheckState{
-			Name:               checks[i].Name,
-			State:              kueue.CheckStatePending,
-			LastTransitionTime: metav1.NewTime(now),
-			Message:            "Reset to Pending after eviction. Previously: " + string(checks[i].State),
+			Name:                checks[i].Name,
+			State:               kueue.CheckStatePending,
+			Message:             "Reset to Pending after eviction. Previously: " + string(checks[i].State),
+			LastTransitionTime:  metav1.NewTime(now),
+			RequeueAfterSeconds: checks[i].RequeueAfterSeconds,
+			RetryCount:          retryCount,
```

  Currently retryCount is only incremented, let's clear on deactivation for consistency with the RequeueState handling

### Review by [@dhenkel92](https://github.com/dhenkel92) (commented) — 2025-10-29T13:43:17Z

- **apis/kueue/v1beta1/workload_types.go:542** by [@dhenkel92](https://github.com/dhenkel92) — 2025-10-29T13:43:17Z
```diff
@@ -537,7 +537,24 @@ type AdmissionCheckState struct {
 	// +kubebuilder:validation:Required
 	// +kubebuilder:validation:MaxLength=32768
 	Message string `json:"message" protobuf:"bytes,6,opt,name=message"`
-
+	// requeueAfterSeconds indicates how long to wait at least before
+	// retrying to admit the workload.
+	// The admission check controllers can set this field when State=Retry
```

  Yes, that's right. It allows the admission check controller to set a delay when it updates the admission check state to Retry.

### Review by [@dhenkel92](https://github.com/dhenkel92) (commented) — 2025-10-29T20:55:15Z

- **test/integration/singlecluster/scheduler/podsready/scheduler_test.go:802** by [@dhenkel92](https://github.com/dhenkel92) — 2025-10-29T19:20:35Z
```diff
@@ -792,7 +791,7 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
 				}, true)
 				util.ExpectWorkloadToHaveRequeueState(ctx, k8sClient, client.ObjectKeyFromObject(wl2), &kueue.RequeueState{
 					Count: ptr.To[int32](1),
-				}, true)
+				}, false)
```

  This is a valid change. We were not resetting the RequeueState.ReqeueAfter for this workload state before.

- **test/integration/singlecluster/scheduler/podsready/scheduler_test.go:725** by [@dhenkel92](https://github.com/dhenkel92) — 2025-10-29T19:22:51Z
```diff
@@ -712,15 +712,14 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
 			util.ExpectQuotaReservedWorkloadsTotalMetric(prodClusterQ, "", 2)
 			util.ExpectAdmittedWorkloadsTotalMetric(prodClusterQ, "", 2)
 			util.AwaitWorkloadEvictionByPodsReadyTimeout(ctx, k8sClient, client.ObjectKeyFromObject(prodWl), podsReadyTimeout)
-			util.SetRequeuedConditionWithPodsReadyTimeout(ctx, k8sClient, client.ObjectKeyFromObject(prodWl))
 			util.ExpectWorkloadToHaveRequeueState(ctx, k8sClient, client.ObjectKeyFromObject(prodWl), &kueue.RequeueState{
 				Count: ptr.To[int32](2),
-			}, false)
+			}, true)
```

  I've added a comment explaining why the change is required and I'll open a ticket afterwards.

- **test/integration/singlecluster/scheduler/podsready/scheduler_test.go:313** by [@dhenkel92](https://github.com/dhenkel92) — 2025-10-29T19:24:11Z
```diff
@@ -309,8 +309,8 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			ginkgo.By("finish the eviction, and the workload is pending by backoff")
 			util.FinishEvictionForWorkloads(ctx, k8sClient, prodWl)
 			util.ExpectWorkloadToHaveRequeueState(ctx, k8sClient, client.ObjectKeyFromObject(prodWl), &kueue.RequeueState{
-				Count: ptr.To[int32](1),
-			}, false)
+				Count: ptr.To[int32](2),
+			}, true)
```

  It looks like it is a flaky test that depends on the timing. I've reverted the change and will see how it performs.

- **pkg/workload/admissionchecks.go:106** by [@dhenkel92](https://github.com/dhenkel92) — 2025-10-29T20:51:48Z
```diff
@@ -90,11 +92,18 @@ func resetChecksOnEviction(w *kueue.Workload, now time.Time) {
 		if checks[i].State == kueue.CheckStatePending {
 			continue
 		}
+		var retryCount *int32
+		if checks[i].State == kueue.CheckStateRetry {
+			tmpRetryCount := ptr.Deref(checks[i].RetryCount, 0) + 1
+			retryCount = ptr.To(tmpRetryCount)
+		}
 		checks[i] = kueue.AdmissionCheckState{
-			Name:               checks[i].Name,
-			State:              kueue.CheckStatePending,
-			LastTransitionTime: metav1.NewTime(now),
-			Message:            "Reset to Pending after eviction. Previously: " + string(checks[i].State),
+			Name:                checks[i].Name,
+			State:               kueue.CheckStatePending,
+			Message:             "Reset to Pending after eviction. Previously: " + string(checks[i].State),
+			LastTransitionTime:  metav1.NewTime(now),
+			RequeueAfterSeconds: checks[i].RequeueAfterSeconds,
+			RetryCount:          retryCount,
```

  Done in the reconciler function
  https://github.com/kubernetes-sigs/kueue/pull/7370/files#diff-60dd240c20adbd6a189d018d1c216c2d296730f446c341d8bf449fa6657964ffL316-R352

- **test/integration/singlecluster/scheduler/delayedadmission/delayed_admission_test.go:194** by [@dhenkel92](https://github.com/dhenkel92) — 2025-10-29T20:53:08Z
```diff
@@ -0,0 +1,218 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package delayedadmission
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithDelayedAdmissionChecks", func() {
+	var (
+		// Values referenced by tests:
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		clusterQ      *kueue.ClusterQueue
+		localQ        *kueue.LocalQueue
+		delayedCheck  *kueue.AdmissionCheck
+		realClock     = clock.RealClock{}
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(&configapi.Configuration{}))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		util.MustCreate(ctx, k8sClient, defaultFlavor)
+
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "delayed-retry-")
+
+		delayedCheck = utiltestingapi.MakeAdmissionCheck("delayed-check").ControllerName("ctrl").Obj()
+		util.MustCreate(ctx, k8sClient, delayedCheck)
+		util.SetAdmissionCheckActive(ctx, k8sClient, delayedCheck, metav1.ConditionTrue)
+
+		clusterQ = utiltestingapi.MakeClusterQueue("dev-cq").
+			ResourceGroup(*utiltestingapi.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "5").Obj()).
+			AdmissionChecks(kueue.AdmissionCheckReference(delayedCheck.Name)).
+			Obj()
+		util.MustCreate(ctx, k8sClient, clusterQ)
+
+		localQ = utiltestingapi.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(clusterQ.Name).Obj()
+		util.MustCreate(ctx, k8sClient, localQ)
+
+		util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQ)
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQ, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, delayedCheck, true)
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.Context("Delayed admission check", func() {
+		ginkgo.BeforeEach(func() {
+		})
+
+		ginkgo.It("should requeue workloads after a delayed retry", func() {
+			wl := utiltestingapi.MakeWorkload("delayed-ac-retry", ns.Name).
+				RequestAndLimit(corev1.ResourceCPU, "1").
+				Queue(kueue.LocalQueueName(localQ.Name)).
+				Obj()
+
+			ginkgo.By("Creating the job", func() {
+				util.MustCreate(ctx, k8sClient, wl)
+			})
+			wlLookupKey := client.ObjectKeyFromObject(wl)
+			createdWorkload := &kueue.Workload{}
+
+			ginkgo.By("Verifying workload is created but not admitted yet", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					g.Expect(workload.HasQuotaReservation(createdWorkload)).To(gomega.BeTrue())
+					g.Expect(workload.IsAdmitted(createdWorkload)).To(gomega.BeFalse())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Marking AC as Retry to trigger an immediate retry", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					workload.SetAdmissionCheckState(&createdWorkload.Status.AdmissionChecks, kueue.AdmissionCheckState{
+						Name:    kueue.AdmissionCheckReference(delayedCheck.Name),
+						State:   kueue.CheckStateRetry,
+						Message: "Retrying admission check",
+					}, realClock)
+					g.Expect(k8sClient.Status().Update(ctx, createdWorkload)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Finish eviction", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					util.FinishEvictionForWorkloads(ctx, k8sClient, createdWorkload)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verifying retry counter is incremented after transition to Pending", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+
+					ac := admissioncheck.FindAdmissionCheck(createdWorkload.Status.AdmissionChecks, kueue.AdmissionCheckReference(delayedCheck.Name))
+					g.Expect(ac).ToNot(gomega.BeNil())
+					g.Expect(ac.State).To(gomega.Equal(kueue.CheckStatePending))
+					g.Expect(ac.LastTransitionTime).ToNot(gomega.BeNil())
+					g.Expect(ac.RetryCount).ToNot(gomega.BeNil())
+					g.Expect(*ac.RetryCount).To(gomega.Equal(int32(1)))
+
+					g.Expect(createdWorkload.Status.RequeueState).To(gomega.BeNil())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Wait for job to have quota again", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					g.Expect(workload.HasQuotaReservation(createdWorkload)).To(gomega.BeTrue())
+					g.Expect(workload.IsAdmitted(createdWorkload)).To(gomega.BeFalse())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Marking AC as Retry to trigger a delayed retry", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					workload.SetAdmissionCheckState(&createdWorkload.Status.AdmissionChecks, kueue.AdmissionCheckState{
+						Name:                kueue.AdmissionCheckReference(delayedCheck.Name),
+						State:               kueue.CheckStateRetry,
+						Message:             "Retrying admission check",
+						RequeueAfterSeconds: ptr.To(int32(5)),
+					}, realClock)
+					g.Expect(k8sClient.Status().Update(ctx, createdWorkload)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Finish eviction", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					util.FinishEvictionForWorkloads(ctx, k8sClient, createdWorkload)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verifying retry counter is incremented after transition to Pending", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+
+					ac := admissioncheck.FindAdmissionCheck(createdWorkload.Status.AdmissionChecks, kueue.AdmissionCheckReference(delayedCheck.Name))
+					g.Expect(ac).ToNot(gomega.BeNil())
+					g.Expect(ac.State).To(gomega.Equal(kueue.CheckStatePending))
+					g.Expect(ac.LastTransitionTime).To(gomega.Not(gomega.BeNil()))
+					g.Expect(ac.RetryCount).ToNot(gomega.BeNil())
+					g.Expect(*ac.RetryCount).To(gomega.Equal(int32(2)))
+					g.Expect(ac.RequeueAfterSeconds).ToNot(gomega.BeNil())
+
+					g.Expect(createdWorkload.Status.RequeueState).ToNot(gomega.BeNil())
+					g.Expect(createdWorkload.Status.RequeueState.RequeueAt).ToNot(gomega.BeNil())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Wait for job to have quota again", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					g.Expect(workload.HasQuotaReservation(createdWorkload)).To(gomega.BeTrue())
+					g.Expect(workload.IsAdmitted(createdWorkload)).To(gomega.BeFalse())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
```

  I've added a comment why it's not possible right now and I'll create a ticket afterwards.

- **pkg/controller/core/workload_controller_test.go:2425** by [@dhenkel92](https://github.com/dhenkel92) — 2025-10-29T20:55:13Z
```diff
@@ -2414,6 +2409,168 @@ func TestReconcile(t *testing.T) {
 			},
 			wantError: nil,
 		},
+		"should reset requeueState.requeueAt when expired": {
+			cq: utiltestingapi.MakeClusterQueue("cq").Obj(),
+			lq: utiltestingapi.MakeLocalQueue("lq", "ns").ClusterQueue("cq").Obj(),
+			workload: utiltestingapi.MakeWorkload("wl", "ns").
+				Queue("lq").
+				RequeueState(nil, ptr.To(metav1.NewTime(time.Now().Add(-time.Second)))).
+				Obj(),
+			wantWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				Queue("lq").
+				Obj(),
+			wantWorkloadUseMergePatch: utiltestingapi.MakeWorkload("wl", "ns").
+				Queue("lq").
+				Obj(),
+		},
```

  For some reason, the `wantWorkload` part of the test is not applying the patch and I don't understand why.

### Review by [@dhenkel92](https://github.com/dhenkel92) (commented) — 2025-10-30T08:06:53Z

- **pkg/controller/core/workload_controller_test.go:2425** by [@dhenkel92](https://github.com/dhenkel92) — 2025-10-30T08:06:53Z
```diff
@@ -2414,6 +2409,168 @@ func TestReconcile(t *testing.T) {
 			},
 			wantError: nil,
 		},
+		"should reset requeueState.requeueAt when expired": {
+			cq: utiltestingapi.MakeClusterQueue("cq").Obj(),
+			lq: utiltestingapi.MakeLocalQueue("lq", "ns").ClusterQueue("cq").Obj(),
+			workload: utiltestingapi.MakeWorkload("wl", "ns").
+				Queue("lq").
+				RequeueState(nil, ptr.To(metav1.NewTime(time.Now().Add(-time.Second)))).
+				Obj(),
+			wantWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				Queue("lq").
+				Obj(),
+			wantWorkloadUseMergePatch: utiltestingapi.MakeWorkload("wl", "ns").
+				Queue("lq").
+				Obj(),
+		},
```

  It looks like it's a limitation of using SSA patches with the fake client. The only option i see right now is to remove the test.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-31T13:40:36Z

- **test/integration/singlecluster/scheduler/podsready/scheduler_test.go:802** by [@mimowo](https://github.com/mimowo) — 2025-10-31T13:40:36Z
```diff
@@ -792,7 +791,7 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
 				}, true)
 				util.ExpectWorkloadToHaveRequeueState(ctx, k8sClient, client.ObjectKeyFromObject(wl2), &kueue.RequeueState{
 					Count: ptr.To[int32](1),
-				}, true)
+				}, false)
```

  hm, maybe we don't need to clean it at all then (also related to the point above). I  think an old value should not cause user-observable problems anyway, wdyt? Also related to https://github.com/kubernetes-sigs/kueue/pull/7370/files#r2475037424
  
  EDIT: so my thinking is that if the workload s admitted, then the stale value is harmless (ignored) anyway, but when we are evicting we are going to update it anyway. So, I'm not clear we need to clear it at all. Especially if we didn't clear before this PR, then it seems orthogonal. 
  
  I agree it would be "ideal", but I want to minimize the scope of changes by the PR to bare minimum.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2025-11-07T14:49:54Z

- **apis/kueue/v1beta1/workload_types.go:550** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-11-07T14:24:49Z
```diff
@@ -537,7 +537,24 @@ type AdmissionCheckState struct {
 	// +kubebuilder:validation:Required
 	// +kubebuilder:validation:MaxLength=32768
 	Message string `json:"message" protobuf:"bytes,6,opt,name=message"`
-
+	// requeueAfterSeconds indicates how long to wait at least before
+	// retrying to admit the workload.
+	// The admission check controllers can set this field when State=Retry
+	// to implement delays between retry attempts.
+	//
+	// If nil when State=Retry, Kueue will retry immediately.
+	// If set, Kueue will add the workload back to the queue after
+	//   lastTransitionTime + RequeueAfterSeconds is over.
+	//
+	// +optional
+	// +kubebuilder:validation:Optional
```

  I think we should include the following validation here:
  ```suggestion
  	// +kubebuilder:validation:Minimum=0                        
  ```

- **apis/kueue/v1beta1/workload_types.go:556** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-11-07T14:25:30Z
```diff
@@ -537,7 +537,24 @@ type AdmissionCheckState struct {
 	// +kubebuilder:validation:Required
 	// +kubebuilder:validation:MaxLength=32768
 	Message string `json:"message" protobuf:"bytes,6,opt,name=message"`
-
+	// requeueAfterSeconds indicates how long to wait at least before
+	// retrying to admit the workload.
+	// The admission check controllers can set this field when State=Retry
+	// to implement delays between retry attempts.
+	//
+	// If nil when State=Retry, Kueue will retry immediately.
+	// If set, Kueue will add the workload back to the queue after
+	//   lastTransitionTime + RequeueAfterSeconds is over.
+	//
+	// +optional
+	// +kubebuilder:validation:Optional
+	RequeueAfterSeconds *int32 `json:"requeueAfterSeconds,omitempty"`
+	// retryCount tracks retry attempts for this admission check.
+	// Kueue automatically increments the counter whenever the
+	// state transitions to Retry.
+	// +optional
+	// +kubebuilder:validation:Optional
```

  ```suggestion
  	// +kubebuilder:validation:Minimum=0
  ```

- **pkg/workload/admissionchecks.go:233** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-11-07T14:47:02Z
```diff
@@ -186,3 +196,57 @@ func HasRejectedChecks(wl *kueue.Workload) bool {
 	}
 	return false
 }
+
+// GetMaxRetryTime returns the max retry time from all the admission checks.
+// It will return a zero time if no retry time is found.
+func GetMaxRetryTime(wl *kueue.Workload) metav1.Time {
+	var max time.Time
+	for i := range wl.Status.AdmissionChecks {
+		if wl.Status.AdmissionChecks[i].RequeueAfterSeconds == nil {
+			continue
+		}
+		// This should never happen, but prevents panics
+		if wl.Status.AdmissionChecks[i].LastTransitionTime.IsZero() {
+			continue
+		}
+		if wl.Status.AdmissionChecks[i].State != kueue.CheckStateRetry {
+			continue
+		}
+
+		retryTime := wl.Status.AdmissionChecks[i].LastTransitionTime.Add(time.Duration(*wl.Status.AdmissionChecks[i].RequeueAfterSeconds) * time.Second)
+		if max.Before(retryTime) {
+			max = retryTime
+		}
+	}
+	return metav1.NewTime(max)
+}
+
+// UpdateAdmissionCheckRequeueState calculates the RequeueAfterSeconds based on the backoff and requeuingCount
+func UpdateAdmissionCheckRequeueState(acState *kueue.AdmissionCheckState, backoffBaseSeconds int32, backoffMaxSeconds int32, clock clock.Clock) {
+	requeuingCount := ptr.Deref(acState.RetryCount, 0) + 1
+
+	// Every backoff duration is about "60s*2^(n-1)+Rand" where:
+	// - "n" represents the "requeuingCount",
+	// - "Rand" represents the random jitter.
+	// During this time, the workload is taken as an inadmissible and other
+	// workloads will have a chance to be admitted.
+	backoff := &wait.Backoff{
```

  I think we should follow the similar pattern used here: https://github.com/kubernetes-sigs/kueue/blob/main/pkg/workload/workload.go#L612 to maintain the consistency.
  ```suggestion
  	backoff := wait.NewBackoff(
    		time.Duration(backoffBaseSeconds)*time.Second,
    		time.Duration(backoffMaxSeconds)*time.Second,
    		2,
    		0.0001,
    	)
    	waitDuration := backoff.WaitTime(int(requeuingCount))
  ```
