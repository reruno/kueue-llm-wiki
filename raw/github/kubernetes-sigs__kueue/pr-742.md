# PR #742: KEP-78: Dynamically reclaiming resources

**Summary**: KEP-78: Dynamically reclaiming resources

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/742

**Last updated**: 2023-05-11T07:48:18Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `reclaim-KEP` → `main`
- **Created**: 2023-05-04T07:03:10Z
- **Updated**: 2023-05-11T07:48:18Z
- **Merged**: 2023-05-10T17:33:00Z
- **Closed**: 2023-05-10T17:33:00Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/api-change`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 2   **+178 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind documentation
/kind api-change
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
One-line PR description: This KEP proposes an approach to reclaim unused resources of an unfinished job of cluster-queue in an incremental manner, so that all the resources of the cluster-queue can used efficiently.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to: #78

#### Special notes for your reviewer:

This is the continuation of #331, with an adaptation to the current state of Kueue.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-05-04T07:03:13Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-05-04T07:06:19Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | beaceb8166fe94a354fb82439c4513183965bfd3 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/645b99c3d8e493000893525c |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-742--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-742--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTc0Mi0ta3ViZXJuZXRlcy1zaWdzLWt1ZXVlLm5ldGxpZnkuYXBwIn0.wynnTArgJkWd8pS7WHtE7Aipts4sP4i5GHSZ1VOmIfg)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site settings](https://app.netlify.com/sites/kubernetes-sigs-kueue/settings/deploys#deploy-notifications)._

### Comment by [@trasc](https://github.com/trasc) — 2023-05-04T07:26:53Z

/retest

### Comment by [@trasc](https://github.com/trasc) — 2023-05-09T10:46:33Z

/retest

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-05-10T17:31:40Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/742#pullrequestreview-1421104484" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/742#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-05T16:40:49Z

- **keps/78-dynamically-reclaiming-resources/README.md:34** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-05T15:39:57Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
```

  ```suggestion
  2. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
  ```

- **keps/78-dynamically-reclaiming-resources/README.md:74** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-05T15:50:48Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
```

  ```suggestion
  ```

- **keps/78-dynamically-reclaiming-resources/README.md:86** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-05T15:50:55Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
```

  ```suggestion
  ```

- **keps/78-dynamically-reclaiming-resources/README.md:104** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-05T15:51:34Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
+```
+
+#### Example (`batch/job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
+the expected result should be:
+
+```go
+map[string]int{
+    "main": P - max(P,(C-n))
+}
+```
+### API
+
+A new field `ReclaimablePods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
```

  ```suggestion
  // WorkloadStatus defines the observed state of Workload.
  ```

- **keps/78-dynamically-reclaiming-resources/README.md:109** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-05T15:51:43Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
+```
+
+#### Example (`batch/job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
+the expected result should be:
+
+```go
+map[string]int{
+    "main": P - max(P,(C-n))
+}
+```
+### API
+
+A new field `ReclaimablePods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+
+    // ...
+
+    // list of count of Pods of a PodSet with reclaimable resources 
```

  ```suggestion
      // list of count of Pods of a PodSet with reclaimable resources.
  ```

- **keps/78-dynamically-reclaiming-resources/README.md:131** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-05T15:52:03Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
+```
+
+#### Example (`batch/job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
+the expected result should be:
+
+```go
+map[string]int{
+    "main": P - max(P,(C-n))
+}
+```
+### API
+
+A new field `ReclaimablePods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+
+    // ...
+
+    // list of count of Pods of a PodSet with reclaimable resources 
+    // +optional
+    ReclaimablePods []ReclaimablePod `json:"reclaimablePod"`
+}
+
+// ReclaimedPodSetPod defines the PodSet name and count of Pods
+// belonging to the PodSet whose resources can be reclaimed.
+type ReclaimablePods struct{
+    Name string
+    Count int
+}
+```
+
+## Implementation
+
+### Workload
+#### API
+
+- Add the new field in the workload's status.
+- Validate the data in `status.ReclaimablePods`
+  1. The names must be found in the `PodSets`.
+  2. The cont should never exceed the `PodSets` count.
+  2. The cont should not decrease if the workload is admitted.
```

  ```suggestion
    3. The cont should not decrease if the workload is admitted.
  ```

- **keps/78-dynamically-reclaiming-resources/README.md:128** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-05T15:52:13Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
+```
+
+#### Example (`batch/job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
+the expected result should be:
+
+```go
+map[string]int{
+    "main": P - max(P,(C-n))
+}
+```
+### API
+
+A new field `ReclaimablePods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+
+    // ...
+
+    // list of count of Pods of a PodSet with reclaimable resources 
+    // +optional
+    ReclaimablePods []ReclaimablePod `json:"reclaimablePod"`
+}
+
+// ReclaimedPodSetPod defines the PodSet name and count of Pods
+// belonging to the PodSet whose resources can be reclaimed.
+type ReclaimablePods struct{
+    Name string
+    Count int
+}
+```
+
+## Implementation
+
+### Workload
+#### API
+
+- Add the new field in the workload's status.
+- Validate the data in `status.ReclaimablePods`
```

  ```suggestion
  - Validate the data in `status.ReclaimablePods`:
  ```

- **keps/78-dynamically-reclaiming-resources/README.md:52** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-05T15:55:40Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
```

  Will you include other framework jobs, such as MPIJob and RayJob? Or will you work on only batch/job?

- **keps/78-dynamically-reclaiming-resources/README.md:84** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-05T16:00:06Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
```

  ```suggestion
      // ...
  
      // Get reclaimable pods.
      Finished() (condition metav1.Condition, reclaimablePods map[string]int)
  ```

- **keps/78-dynamically-reclaiming-resources/README.md:72** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-05T16:00:59Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
```

  ```suggestion
      // Get reclaimable pods.
      ReclaimablePods() map[string]int
  ```

- **keps/78-dynamically-reclaiming-resources/README.md:72** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-05T16:17:52Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
```

  Is the key of this map a podSet name?

- **keps/78-dynamically-reclaiming-resources/README.md:54** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-05T16:40:13Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
```

  Can we available opt-out of this feature by setting it to `Configuration` like `waitForPodsReady`?
  
  Also, can we add a label or an annotation to prevent counting as reclaimable pods?
  We maybe want a label or an annotation per Job and podSet.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-05T16:45:26Z

- **keps/78-dynamically-reclaiming-resources/README.md:76** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-05T16:45:26Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
+```
+
+#### Example (`batch/job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
```

  Does the `number of completed pod executions` mean `.status.succeeded`?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-05T16:49:14Z

- **keps/78-dynamically-reclaiming-resources/README.md:76** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-05T16:49:14Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
+```
+
+#### Example (`batch/job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
```

  It would be good to clarify conditions to handle as `number of completed pod executions`.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-05T19:17:04Z

- **keps/78-dynamically-reclaiming-resources/README.md:39** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-05T17:29:59Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
```

  ```suggestion
  Currently, the quota assigned to a Job is reclaimed by Kueue only when the whole Job finishes.
  ```

- **keps/78-dynamically-reclaiming-resources/README.md:52** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-05T18:04:12Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
```

  MPIJob doesn't have partial termination semantics. Probably Ray doesn't either.
  
  Note that partial termination is independent from elasticity (resizing of the job)

- **keps/78-dynamically-reclaiming-resources/README.md:54** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-05T18:42:35Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
```

  why would you like to opt-out?
  Or are you thinking of a feature gate, while the feature is not mature?

- **keps/78-dynamically-reclaiming-resources/README.md:72** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-05T18:44:11Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
```

  Why `reclaimablePods` instead of the opposite: `activeReplicas` or `activeCount`?

- **keps/78-dynamically-reclaiming-resources/README.md:84** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-05T18:45:57Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
```

  I'm inclined towards a new method.

- **keps/78-dynamically-reclaiming-resources/README.md:89** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-05T18:46:52Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
+```
+
+#### Example (`batch/job`)
```

  More than an example, this is just the design for the Job integration.
  ```suggestion
  #### Design for `batch/Job`
  ```

- **keps/78-dynamically-reclaiming-resources/README.md:76** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-05T18:52:45Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
+```
+
+#### Example (`batch/job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
```

  As of today (k8s 1.27), it would be `.status.succeeded`. But starting from 1.28, it might need to include the number of failed indexes as well. kubernetes/enhancements#3967
  
  Check the proposal above to see if the design has enough information to quickly calculate the number of pods to reclaim.
  
  Additionally, this proposal for 1.28 will include a field for number of terminating pods (for which we can't reclaim the quota yet) https://github.com/kubernetes/enhancements/pull/3940
  
  You don't have to have a full design here, as the features are not released yet, but at least we should consider the possible expansions and cross-reference for future improvements.
  
  cc @mimowo  @kannon92

- **keps/78-dynamically-reclaiming-resources/README.md:111** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-05T19:08:43Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
+```
+
+#### Example (`batch/job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
+the expected result should be:
+
+```go
+map[string]int{
+    "main": P - max(P,(C-n))
+}
+```
+### API
+
+A new field `ReclaimablePods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+
+    // ...
+
+    // list of count of Pods of a PodSet with reclaimable resources 
+    // +optional
+    ReclaimablePods []ReclaimablePod `json:"reclaimablePod"`
```

  this would fail in the exceptions for using `map[string]int32`, given that there is no world where we would need another member of the map, is there?
  https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md#lists-of-named-subobjects-preferred-over-maps

- **keps/78-dynamically-reclaiming-resources/README.md:141** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-05T19:15:46Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
+```
+
+#### Example (`batch/job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
+the expected result should be:
+
+```go
+map[string]int{
+    "main": P - max(P,(C-n))
+}
+```
+### API
+
+A new field `ReclaimablePods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+
+    // ...
+
+    // list of count of Pods of a PodSet with reclaimable resources 
+    // +optional
+    ReclaimablePods []ReclaimablePod `json:"reclaimablePod"`
+}
+
+// ReclaimedPodSetPod defines the PodSet name and count of Pods
+// belonging to the PodSet whose resources can be reclaimed.
+type ReclaimablePods struct{
+    Name string
+    Count int
+}
+```
+
+## Implementation
+
+### Workload
+#### API
+
+- Add the new field in the workload's status.
+- Validate the data in `status.ReclaimablePods`
+  1. The names must be found in the `PodSets`.
+  2. The cont should never exceed the `PodSets` count.
+  2. The cont should not decrease if the workload is admitted.
+
+
+#### `pkg/workload`
+
+Rework the way `Info.TotalRequests` in computed in order to take the `ReclaimablePods` into account.
+
+#### Controller
+
+Should update the `status.Admission` when `status.ReclaimablePods` changes.
```

  uhm... that would be 2 API calls in the same object. Is it possible to do 1? Although that means that the kueue-job controller needs to know how to calculate usage, or have access to the workload.Info

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-05T19:26:37Z

- **keps/78-dynamically-reclaiming-resources/README.md:52** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-05T19:26:36Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
```

  > MPIJob doesn't have partial termination semantics. Probably Ray doesn't either.
  
  Yes, I agree.
  I asked about that because I couldn't find mention of other framework jobs.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-05T19:32:50Z

- **keps/78-dynamically-reclaiming-resources/README.md:54** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-05T19:32:50Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
```

  > Or are you thinking of a feature gate, while the feature is not mature?
  
  Yes, that's right. I'm ok with either setting Configuration or using feature gate.
  
  But I guess that we will take time to implement the feature gate. So I think we can just use the Configuration as a first step.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-05T19:35:56Z

- **keps/78-dynamically-reclaiming-resources/README.md:54** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-05T19:35:55Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
```

  I prefer a feature gate, because it implicitly has the semantics of being temporary.
  
  The configuration API is currently beta, meaning that if we remove a field, we would have to bump the API version.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-05T19:40:27Z

- **keps/78-dynamically-reclaiming-resources/README.md:54** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-05T19:40:26Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
```

  Yes, I prefer the feature gate, too.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-05T19:44:29Z

- **keps/78-dynamically-reclaiming-resources/README.md:76** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-05T19:44:28Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
+```
+
+#### Example (`batch/job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
```

  Thank you for pointing out that!

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-08T08:22:33Z

- **keps/78-dynamically-reclaiming-resources/README.md:34** by [@trasc](https://github.com/trasc) — 2023-05-08T08:22:33Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-08T09:23:01Z

- **keps/78-dynamically-reclaiming-resources/README.md:39** by [@trasc](https://github.com/trasc) — 2023-05-08T08:22:48Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
```

  done

- **keps/78-dynamically-reclaiming-resources/README.md:52** by [@trasc](https://github.com/trasc) — 2023-05-08T08:28:08Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
```

  The feature is related to the workload and I plan to do the `batc/job` integration as reference.
  
  (like MPI the Ray job doesn't look to have partial termination semantics )

- **keps/78-dynamically-reclaiming-resources/README.md:54** by [@trasc](https://github.com/trasc) — 2023-05-08T08:35:40Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
```

  I've added a [comment](https://github.com/kubernetes-sigs/kueue/issues/740#issuecomment-1534322235) in #740, let's see how the implementation of the feature gate framework goes.

- **keps/78-dynamically-reclaiming-resources/README.md:72** by [@trasc](https://github.com/trasc) — 2023-05-08T08:45:13Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
```

  > Is the key of this map a podSet name?
  
  changed to slice
  
  > Why reclaimablePods instead of the opposite: activeReplicas or activeCount?
  
  - `active...` could be misleading when the workload/job is suspended
  - I like this to be optional at podset level as well, and not mentioning a podset to mean that we still need all of it's count.

- **keps/78-dynamically-reclaiming-resources/README.md:74** by [@trasc](https://github.com/trasc) — 2023-05-08T08:45:38Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
```

  done

- **keps/78-dynamically-reclaiming-resources/README.md:86** by [@trasc](https://github.com/trasc) — 2023-05-08T08:46:15Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
```

  done

- **keps/78-dynamically-reclaiming-resources/README.md:84** by [@trasc](https://github.com/trasc) — 2023-05-08T08:46:53Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
```

  +1

- **keps/78-dynamically-reclaiming-resources/README.md:89** by [@trasc](https://github.com/trasc) — 2023-05-08T08:48:59Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
+```
+
+#### Example (`batch/job`)
```

  done

- **keps/78-dynamically-reclaiming-resources/README.md:76** by [@trasc](https://github.com/trasc) — 2023-05-08T09:05:05Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
+```
+
+#### Example (`batch/job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
```

  For `batch/Job` yes, for  workloads we only need to know the number of pods we no longer need to reserve quota for.

- **keps/78-dynamically-reclaiming-resources/README.md:104** by [@trasc](https://github.com/trasc) — 2023-05-08T09:05:34Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
+```
+
+#### Example (`batch/job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
+the expected result should be:
+
+```go
+map[string]int{
+    "main": P - max(P,(C-n))
+}
+```
+### API
+
+A new field `ReclaimablePods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
```

  done

- **keps/78-dynamically-reclaiming-resources/README.md:109** by [@trasc](https://github.com/trasc) — 2023-05-08T09:05:56Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
+```
+
+#### Example (`batch/job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
+the expected result should be:
+
+```go
+map[string]int{
+    "main": P - max(P,(C-n))
+}
+```
+### API
+
+A new field `ReclaimablePods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+
+    // ...
+
+    // list of count of Pods of a PodSet with reclaimable resources 
```

  done

- **keps/78-dynamically-reclaiming-resources/README.md:111** by [@trasc](https://github.com/trasc) — 2023-05-08T09:10:56Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
+```
+
+#### Example (`batch/job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
+the expected result should be:
+
+```go
+map[string]int{
+    "main": P - max(P,(C-n))
+}
+```
+### API
+
+A new field `ReclaimablePods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+
+    // ...
+
+    // list of count of Pods of a PodSet with reclaimable resources 
+    // +optional
+    ReclaimablePods []ReclaimablePod `json:"reclaimablePod"`
```

  We wold go with slice and
  - if a pod set is not mentioned , the reclaimable cont is considered 0
  - if an entry has a name not found in the `spec.podSets` is going to be ignored

- **keps/78-dynamically-reclaiming-resources/README.md:128** by [@trasc](https://github.com/trasc) — 2023-05-08T09:11:26Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
+```
+
+#### Example (`batch/job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
+the expected result should be:
+
+```go
+map[string]int{
+    "main": P - max(P,(C-n))
+}
+```
+### API
+
+A new field `ReclaimablePods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+
+    // ...
+
+    // list of count of Pods of a PodSet with reclaimable resources 
+    // +optional
+    ReclaimablePods []ReclaimablePod `json:"reclaimablePod"`
+}
+
+// ReclaimedPodSetPod defines the PodSet name and count of Pods
+// belonging to the PodSet whose resources can be reclaimed.
+type ReclaimablePods struct{
+    Name string
+    Count int
+}
+```
+
+## Implementation
+
+### Workload
+#### API
+
+- Add the new field in the workload's status.
+- Validate the data in `status.ReclaimablePods`
```

  done

- **keps/78-dynamically-reclaiming-resources/README.md:131** by [@trasc](https://github.com/trasc) — 2023-05-08T09:11:55Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
+```
+
+#### Example (`batch/job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
+the expected result should be:
+
+```go
+map[string]int{
+    "main": P - max(P,(C-n))
+}
+```
+### API
+
+A new field `ReclaimablePods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+
+    // ...
+
+    // list of count of Pods of a PodSet with reclaimable resources 
+    // +optional
+    ReclaimablePods []ReclaimablePod `json:"reclaimablePod"`
+}
+
+// ReclaimedPodSetPod defines the PodSet name and count of Pods
+// belonging to the PodSet whose resources can be reclaimed.
+type ReclaimablePods struct{
+    Name string
+    Count int
+}
+```
+
+## Implementation
+
+### Workload
+#### API
+
+- Add the new field in the workload's status.
+- Validate the data in `status.ReclaimablePods`
+  1. The names must be found in the `PodSets`.
+  2. The cont should never exceed the `PodSets` count.
+  2. The cont should not decrease if the workload is admitted.
```

  done

- **keps/78-dynamically-reclaiming-resources/README.md:141** by [@trasc](https://github.com/trasc) — 2023-05-08T09:15:05Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
+```
+
+#### Example (`batch/job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
+the expected result should be:
+
+```go
+map[string]int{
+    "main": P - max(P,(C-n))
+}
+```
+### API
+
+A new field `ReclaimablePods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+
+    // ...
+
+    // list of count of Pods of a PodSet with reclaimable resources 
+    // +optional
+    ReclaimablePods []ReclaimablePod `json:"reclaimablePod"`
+}
+
+// ReclaimedPodSetPod defines the PodSet name and count of Pods
+// belonging to the PodSet whose resources can be reclaimed.
+type ReclaimablePods struct{
+    Name string
+    Count int
+}
+```
+
+## Implementation
+
+### Workload
+#### API
+
+- Add the new field in the workload's status.
+- Validate the data in `status.ReclaimablePods`
+  1. The names must be found in the `PodSets`.
+  2. The cont should never exceed the `PodSets` count.
+  2. The cont should not decrease if the workload is admitted.
+
+
+#### `pkg/workload`
+
+Rework the way `Info.TotalRequests` in computed in order to take the `ReclaimablePods` into account.
+
+#### Controller
+
+Should update the `status.Admission` when `status.ReclaimablePods` changes.
```

  we might be able to to skip this step, I still need to check how the implementation will go.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-08T20:17:32Z

- **keps/78-dynamically-reclaiming-resources/README.md:76** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-08T20:17:31Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
+```
+
+#### Example (`batch/job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
```

  Could you add some notes about kubernetes/enhancements#3967 and kubernetes/enhancements#3940?

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-05-09T06:25:22Z

- **keps/78-dynamically-reclaiming-resources/README.md:90** by [@kerthcet](https://github.com/kerthcet) — 2023-05-09T00:45:41Z
```diff
@@ -0,0 +1,170 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Reference design for (<code>batch/Job</code>)](#reference-design-for-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+2. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the quota assigned to a Job is reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+    // Get reclaimable pods.
+    ReclaimablePods() []ReclaimablePod
+}
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+    // ...
+
+    // Get reclaimable pods.
+    Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+```
+
+#### Reference design for (`batch/Job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
+the expected result should be:
```

  Refer to that the result means reclaimablePods. And add an explanation of the map's kv pair.

- **keps/78-dynamically-reclaiming-resources/README.md:93** by [@kerthcet](https://github.com/kerthcet) — 2023-05-09T06:25:19Z
```diff
@@ -0,0 +1,170 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Reference design for (<code>batch/Job</code>)](#reference-design-for-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+2. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the quota assigned to a Job is reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+    // Get reclaimable pods.
+    ReclaimablePods() []ReclaimablePod
+}
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+    // ...
+
+    // Get reclaimable pods.
+    Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+```
+
+#### Reference design for (`batch/Job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
+the expected result should be:
+
+```go
+map[string]int{
+    "main": P - min(P,(C-n))
+}
+```
+### API
```

  At a high level, reclaiming resources makes sense for the most of the time, but not always that case like some jobs may want to restart all the pods when running into a pod failure, reclaiming all the resources may lead to the job end in failure because insufficient resources . So maybe we can make it an option, reclaim or not, default to reclaim.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-09T10:37:31Z

- **keps/78-dynamically-reclaiming-resources/README.md:90** by [@trasc](https://github.com/trasc) — 2023-05-09T10:29:26Z
```diff
@@ -0,0 +1,170 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Reference design for (<code>batch/Job</code>)](#reference-design-for-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+2. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the quota assigned to a Job is reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+    // Get reclaimable pods.
+    ReclaimablePods() []ReclaimablePod
+}
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+    // ...
+
+    // Get reclaimable pods.
+    Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+```
+
+#### Reference design for (`batch/Job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
+the expected result should be:
```

  done

- **keps/78-dynamically-reclaiming-resources/README.md:93** by [@trasc](https://github.com/trasc) — 2023-05-09T10:36:11Z
```diff
@@ -0,0 +1,170 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Reference design for (<code>batch/Job</code>)](#reference-design-for-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+2. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the quota assigned to a Job is reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+    // Get reclaimable pods.
+    ReclaimablePods() []ReclaimablePod
+}
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+    // ...
+
+    // Get reclaimable pods.
+    Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+```
+
+#### Reference design for (`batch/Job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
+the expected result should be:
+
+```go
+map[string]int{
+    "main": P - min(P,(C-n))
+}
+```
+### API
```

  For workload this should not be a problem, however this per-job opt-out needs to be done at job level, so the only way I can think of is  by using some "special" annotation/label.
  
  Should we do that?

- **keps/78-dynamically-reclaiming-resources/README.md:141** by [@trasc](https://github.com/trasc) — 2023-05-09T10:37:12Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
+```
+
+#### Example (`batch/job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
+the expected result should be:
+
+```go
+map[string]int{
+    "main": P - max(P,(C-n))
+}
+```
+### API
+
+A new field `ReclaimablePods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+
+    // ...
+
+    // list of count of Pods of a PodSet with reclaimable resources 
+    // +optional
+    ReclaimablePods []ReclaimablePod `json:"reclaimablePod"`
+}
+
+// ReclaimedPodSetPod defines the PodSet name and count of Pods
+// belonging to the PodSet whose resources can be reclaimed.
+type ReclaimablePods struct{
+    Name string
+    Count int
+}
+```
+
+## Implementation
+
+### Workload
+#### API
+
+- Add the new field in the workload's status.
+- Validate the data in `status.ReclaimablePods`
+  1. The names must be found in the `PodSets`.
+  2. The cont should never exceed the `PodSets` count.
+  2. The cont should not decrease if the workload is admitted.
+
+
+#### `pkg/workload`
+
+Rework the way `Info.TotalRequests` in computed in order to take the `ReclaimablePods` into account.
+
+#### Controller
+
+Should update the `status.Admission` when `status.ReclaimablePods` changes.
```

  we can do it without this.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-09T15:29:26Z

- **keps/78-dynamically-reclaiming-resources/README.md:93** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-09T15:29:25Z
```diff
@@ -0,0 +1,170 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Reference design for (<code>batch/Job</code>)](#reference-design-for-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+2. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the quota assigned to a Job is reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+    // Get reclaimable pods.
+    ReclaimablePods() []ReclaimablePod
+}
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+    // ...
+
+    // Get reclaimable pods.
+    Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+```
+
+#### Reference design for (`batch/Job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
+the expected result should be:
+
+```go
+map[string]int{
+    "main": P - min(P,(C-n))
+}
+```
+### API
```

  But the algorithm above is not actually considering failed pods. It's considering successfully completed pods. There are no scenarios where the job controller would recreate those.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-09T15:29:34Z

- **keps/78-dynamically-reclaiming-resources/README.md:76** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-09T15:29:34Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
+```
+
+#### Example (`batch/job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
```

  Not resolved yet.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-10T06:09:33Z

- **keps/78-dynamically-reclaiming-resources/README.md:76** by [@trasc](https://github.com/trasc) — 2023-05-10T06:09:33Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
+```
+
+#### Example (`batch/job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
```

  I can't say I see a very strong connection between them and this KEP, #kubernetes/enhancements/pull/3940 will improve the accuracy of `.status.succeeded` but it has no direct impact on on the current issue.  With #kubernetes/enhancements/pull/3967 we might go from running with the full umber of pods to `Finished` but also it has no direct impact. Am I missing something?
  
  I can add a new section with related issues and list them there, wold that be OK?

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-05-10T06:53:39Z

- **keps/78-dynamically-reclaiming-resources/README.md:93** by [@kerthcet](https://github.com/kerthcet) — 2023-05-10T06:53:38Z
```diff
@@ -0,0 +1,170 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Reference design for (<code>batch/Job</code>)](#reference-design-for-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+2. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the quota assigned to a Job is reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+    // Get reclaimable pods.
+    ReclaimablePods() []ReclaimablePod
+}
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+    // ...
+
+    // Get reclaimable pods.
+    Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+```
+
+#### Reference design for (`batch/Job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
+the expected result should be:
+
+```go
+map[string]int{
+    "main": P - min(P,(C-n))
+}
+```
+### API
```

  We can add when needed, not conflict with current API.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-10T12:19:47Z

- **keps/78-dynamically-reclaiming-resources/README.md:76** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-10T12:19:47Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
+```
+
+#### Example (`batch/job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
```

  Let's see:
  - 3940 adds a new field "terminating". When the job is actively running, your formula works fine. However, when one of the pods cause a job to fail, or when the job is suspended, there will be pods that are still terminating and using quota. Could we update the formula to account for those?
  - 3967 adds the ability for an index to fail. If an index fails, we could recover that quota, in a similar way to how we recover quota for succeeded indexes.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-05-10T12:23:10Z

- **keps/78-dynamically-reclaiming-resources/README.md:18** by [@kannon92](https://github.com/kannon92) — 2023-05-10T12:23:10Z
```diff
@@ -0,0 +1,155 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Reference design for (<code>batch/Job</code>)](#reference-design-for-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+  - [Jobframwork](#jobframwork)
```

  ```suggestion
    - [Jobframework](#jobframework)
  ```

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-05-10T12:23:48Z

- **keps/78-dynamically-reclaiming-resources/README.md:128** by [@kannon92](https://github.com/kannon92) — 2023-05-10T12:23:48Z
```diff
@@ -0,0 +1,155 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Reference design for (<code>batch/Job</code>)](#reference-design-for-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+2. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the quota assigned to a Job is reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+    // Get reclaimable pods.
+    ReclaimablePods() []ReclaimablePod
+}
+```
+
+#### Reference design for (`batch/Job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
+the expected reclaimablePods should be:
+
+```go
+[]ReclaimablePod{
+    {
+        Name: "main",
+        Count: P - min(P,(C-n)),
+    }
+}
+```
+### API
+
+A new field `ReclaimablePods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload.
+type WorkloadStatus struct {
+
+    // ...
+
+
+    // reclaimablePods keeps track of the number pods within a podset for which
+    // the resource reservation is no longer needed.
+    // +optional
+    ReclaimablePods []ReclaimablePod `json:"reclaimablePods,omitempty"`
+}
+
+type ReclaimablePod struct {
+    // name is the PodSet name.
+    Name string `json:"name"`
+
+    // count is the number of pods for which the requested resources are no longer needed.
+    Count int32 `json:"count"`
+}
+```
+
+## Implementation
+
+### Workload
+#### API
+
+- Add the new field in the workload's status.
+- Validate the data in `status.ReclaimablePods`:
+  1. The names must be found in the `PodSets`.
+  2. The cont should never exceed the `PodSets` count.
+  3. The cont should not decrease if the workload is admitted.
+
+
+#### `pkg/workload`
+
+Rework the way `Info.TotalRequests` in computed in order to take the `ReclaimablePods` into account.
+
+### Jobframwork
```

  ```suggestion
  ### Jobframework
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-10T12:29:39Z

- **keps/78-dynamically-reclaiming-resources/README.md:128** by [@trasc](https://github.com/trasc) — 2023-05-10T12:29:39Z
```diff
@@ -0,0 +1,155 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Reference design for (<code>batch/Job</code>)](#reference-design-for-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+2. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the quota assigned to a Job is reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+    // Get reclaimable pods.
+    ReclaimablePods() []ReclaimablePod
+}
+```
+
+#### Reference design for (`batch/Job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
+the expected reclaimablePods should be:
+
+```go
+[]ReclaimablePod{
+    {
+        Name: "main",
+        Count: P - min(P,(C-n)),
+    }
+}
+```
+### API
+
+A new field `ReclaimablePods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload.
+type WorkloadStatus struct {
+
+    // ...
+
+
+    // reclaimablePods keeps track of the number pods within a podset for which
+    // the resource reservation is no longer needed.
+    // +optional
+    ReclaimablePods []ReclaimablePod `json:"reclaimablePods,omitempty"`
+}
+
+type ReclaimablePod struct {
+    // name is the PodSet name.
+    Name string `json:"name"`
+
+    // count is the number of pods for which the requested resources are no longer needed.
+    Count int32 `json:"count"`
+}
+```
+
+## Implementation
+
+### Workload
+#### API
+
+- Add the new field in the workload's status.
+- Validate the data in `status.ReclaimablePods`:
+  1. The names must be found in the `PodSets`.
+  2. The cont should never exceed the `PodSets` count.
+  3. The cont should not decrease if the workload is admitted.
+
+
+#### `pkg/workload`
+
+Rework the way `Info.TotalRequests` in computed in order to take the `ReclaimablePods` into account.
+
+### Jobframwork
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-10T12:32:02Z

- **keps/78-dynamically-reclaiming-resources/README.md:18** by [@trasc](https://github.com/trasc) — 2023-05-10T12:32:02Z
```diff
@@ -0,0 +1,155 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Reference design for (<code>batch/Job</code>)](#reference-design-for-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+  - [Jobframwork](#jobframwork)
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-10T13:35:24Z

- **keps/78-dynamically-reclaiming-resources/README.md:76** by [@trasc](https://github.com/trasc) — 2023-05-10T13:35:24Z
```diff
@@ -0,0 +1,169 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Completion Accounting](#pod-completion-accounting)
+    - [Example (<code>batch/job</code>)](#example-)
+  - [API](#api)
+- [Implementation](#implementation)
+  - [Workload](#workload)
+    - [API](#api-1)
+    - [<code>pkg/workload</code>](#)
+    - [Controller](#controller)
+  - [Jobframwork](#jobframwork)
+  - [Batch/Job](#batchjob)
+- [Testing Plan](#testing-plan)
+  - [NonRegression](#nonregression)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document:
+1. *Job* refers to any kind job supported by Kueue including `batch/job`, `MPIJob`, `RayJob`, etc.
+1. *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the assigned to a Job are reclaimed by Kueue only when the whole Job finishes.
+For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes.
+This is not efficient as the Jobs might have different needs during execution, for instance the needs of a `batch/job` having the `parallelism` equal to `completions` will decrease with every Pod finishing it's execution.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+- Monitor the Job's pod execution.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimablePods` to the Workload API. `.status.ReclaimablePods` is a list that holds the count of Pods belonging to a PodSet whose resources are no longer needed and could be reclaimed by Kueue.
+
+### Pod Completion Accounting
+
+Since the ability of actively monitoring pods execution of a job is not in the scope of the core Kueue implementation, the number of pods for which the resources are no longer needed for each PodSet should be reported by each framework specific `GenericJob` implementation.
+
+For this purpose the `GenericJob` interface should be changed to either:
+
+1. Add an additional method able to report this
+
+```go
+type GenericJob interface {
+    // ...
+
+   // Get reclaimable pods.
+   ReclaimablePods() map[string]int
+}
+
+```
+
+2. Modify the current `Finished` method
+
+```go
+type GenericJob interface {
+   // ...
+
+   // Get reclaimable pods.
+   Finished() (condition metav1.Condition, reclaimablePods map[string]int)
+}
+
+```
+
+#### Example (`batch/job`)
+
+Having a job defined with **P** `parallelism` and **C** `completions`, and **n** number of completed pod executions,
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-10T17:31:30Z

/lgtm
/approve
