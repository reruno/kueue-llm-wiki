# PR #7425: Remove duplicate env variables in podSet template.

**Summary**: Remove duplicate env variables in podSet template.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7425

**Last updated**: 2025-10-30T07:59:22Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `fix/remove-duplicate-env-variables` → `main`
- **Created**: 2025-10-29T04:30:04Z
- **Updated**: 2025-10-30T07:59:22Z
- **Merged**: 2025-10-30T07:58:02Z
- **Closed**: 2025-10-30T07:58:02Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 15   **+215 / -25**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #3540

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Fixed the bug that prevented managing workloads with duplicated environment variable names in containers. This issue manifested when creating the Workload via the API.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-10-29T04:30:21Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 81396ab15cea96bc7690833dd3d3a88208bd393b |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6902da91b8341b0008ad023e |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-10-29T07:16:05Z

/retest

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-10-29T07:16:10Z

/cc @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-29T07:29:31Z

I would also suggest to make the release note a bit more generic like Fix the bug which prevented managing workloads with duplicated env. names in containers. This issue would manifest on creating the Workload API.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-29T10:56:42Z

@mbobrovskyi have you had a chance to also test e2e on a running cluster? I'm wondering if there is no issue that we would constantly try to update the Workload because EquivalentToWorkload responds to false. To verify that check the lists of events for the workload.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-10-30T06:44:20Z

@mimowo PTAL

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-30T07:56:53Z

LGTM label has been added.  <details>Git tree hash: d0d20651e5b0b594d2a0e0fc64200378ca78f9a8</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-30T07:56:54Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7425#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7425#pullrequestreview-3397856556" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-30T07:58:44Z

@mbobrovskyi please prepare the cherrypicks

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-29T07:26:54Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:968** by [@mimowo](https://github.com/mimowo) — 2025-10-29T07:23:48Z
```diff
@@ -964,6 +964,47 @@ var _ = ginkgo.Describe("Job controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 			})
 		})
 	})
+
+	ginkgo.It("should deduplicate env variables", func() {
```

  Extend the scenario or add a new test to demonstrate what happens on eviction

- **pkg/controller/jobframework/utils.go:63** by [@mimowo](https://github.com/mimowo) — 2025-10-29T07:26:14Z
```diff
@@ -0,0 +1,72 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"maps"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/features"
+)
+
+// JobPodSets retrieves the pod sets from a GenericJob and applies environment variable
+// deduplication if the WorkloadEnvDeduplication feature gate is enabled.
+func JobPodSets(ctx context.Context, job GenericJob) ([]kueue.PodSet, error) {
+	podSets, err := job.PodSets(ctx)
+	if err != nil {
+		return nil, err
+	}
+	if features.Enabled(features.WorkloadEnvDeduplication) {
+		DeduplicateEnvVariablesFromPodSets(podSets)
+	}
+	return podSets, nil
+}
+
+// DeduplicateEnvVariablesFromPodSets removes duplicate environment variables from all containers
+// in the given pod sets. Duplicates are identified by environment variable name;
+// only the last occurrence of each name is preserved.
+func DeduplicateEnvVariablesFromPodSets(podSets []kueue.PodSet) {
+	for podSetIndex := range podSets {
+		ps := &podSets[podSetIndex]
+		for containerIndex := range ps.Template.Spec.Containers {
+			container := &ps.Template.Spec.Containers[containerIndex]
+			envVars := make(map[string]corev1.EnvVar)
+			for _, envVar := range container.Env {
+				envVars[envVar.Name] = envVar
+			}
+			container.Env = slices.Collect(maps.Values(envVars))
+		}
+	}
+}
+
+// DeduplicateEnvVariablesFromPodSet removes duplicate environment variables from all containers
+// in the given pod set. Duplicates are identified by environment variable name;
+// only the last occurrence of each name is preserved.
+func DeduplicateEnvVariablesFromPodSet(podSet *kueue.PodSet) {
```

  This is too specific. We may need more tweaks also for ports for example. I would suggest a name like SanitizePodSets, it should also check the new feature gate inside

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-10-29T08:21:34Z

- **pkg/controller/jobframework/utils.go:63** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-10-29T08:21:34Z
```diff
@@ -0,0 +1,72 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"maps"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/features"
+)
+
+// JobPodSets retrieves the pod sets from a GenericJob and applies environment variable
+// deduplication if the WorkloadEnvDeduplication feature gate is enabled.
+func JobPodSets(ctx context.Context, job GenericJob) ([]kueue.PodSet, error) {
+	podSets, err := job.PodSets(ctx)
+	if err != nil {
+		return nil, err
+	}
+	if features.Enabled(features.WorkloadEnvDeduplication) {
+		DeduplicateEnvVariablesFromPodSets(podSets)
+	}
+	return podSets, nil
+}
+
+// DeduplicateEnvVariablesFromPodSets removes duplicate environment variables from all containers
+// in the given pod sets. Duplicates are identified by environment variable name;
+// only the last occurrence of each name is preserved.
+func DeduplicateEnvVariablesFromPodSets(podSets []kueue.PodSet) {
+	for podSetIndex := range podSets {
+		ps := &podSets[podSetIndex]
+		for containerIndex := range ps.Template.Spec.Containers {
+			container := &ps.Template.Spec.Containers[containerIndex]
+			envVars := make(map[string]corev1.EnvVar)
+			for _, envVar := range container.Env {
+				envVars[envVar.Name] = envVar
+			}
+			container.Env = slices.Collect(maps.Values(envVars))
+		}
+	}
+}
+
+// DeduplicateEnvVariablesFromPodSet removes duplicate environment variables from all containers
+// in the given pod set. Duplicates are identified by environment variable name;
+// only the last occurrence of each name is preserved.
+func DeduplicateEnvVariablesFromPodSet(podSet *kueue.PodSet) {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-10-29T08:21:40Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:968** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-10-29T08:21:40Z
```diff
@@ -964,6 +964,47 @@ var _ = ginkgo.Describe("Job controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 			})
 		})
 	})
+
+	ginkgo.It("should deduplicate env variables", func() {
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-29T08:26:34Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2360** by [@mimowo](https://github.com/mimowo) — 2025-10-29T08:26:34Z
```diff
@@ -2452,6 +2289,82 @@ var _ = ginkgo.Describe("Interacting with scheduler", ginkgo.Ordered, ginkgo.Con
 			util.ExpectLQAdmissionChecksWaitTimeMetric(localQueue, highPriorityClass.Name, 1)
 		})
 	})
+
+	ginkgo.It("should deduplicate env variables after preemption", func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.WorkloadEnvDeduplication, true)
+
+		highPriorityClass := testing.MakePriorityClass("high").PriorityValue(100).Obj()
+		util.MustCreate(ctx, k8sClient, highPriorityClass)
+		ginkgo.DeferCleanup(func() {
+			gomega.Expect(k8sClient.Delete(ctx, highPriorityClass)).To(gomega.Succeed())
+		})
+
+		lowJob := testingjob.MakeJob("low", ns.Name).
+			Queue(kueue.LocalQueueName(devLocalQ.Name)).
+			Parallelism(5).
+			NodeSelector(instanceKey, "spot-untainted").
+			Containers(
+				*testing.MakeContainer().
+					Name("c").
+					Image("sleep").
+					WithResourceReq(corev1.ResourceCPU, "1").
+					WithEnvVar(corev1.EnvVar{Name: "TEST_ENV", Value: "test1"}).
+					WithEnvVar(corev1.EnvVar{Name: "TEST_ENV", Value: "test2"}).
+					Obj(),
+			).
+			Obj()
+
+		ginkgo.By("Creating a low priority job with duplicated environment variables", func() {
+			util.MustCreate(ctx, k8sClient, lowJob)
+		})
+
+		lowCreatedWorkload := &kueue.Workload{}
+		lowWlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob.Name, lowJob.UID), Namespace: ns.Name}
+
+		ginkgo.By("Checking that the low priority workload is created with deduplicated environment variables", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, lowWlLookupKey, lowCreatedWorkload)).Should(gomega.Succeed())
+				g.Expect(lowCreatedWorkload.Spec.PodSets).Should(gomega.HaveLen(1))
+				g.Expect(lowCreatedWorkload.Spec.PodSets[0].Template.Spec.Containers).Should(gomega.HaveLen(1))
+				g.Expect(lowCreatedWorkload.Spec.PodSets[0].Template.Spec.Containers[0].Env).Should(gomega.BeComparableTo(
+					[]corev1.EnvVar{
+						{Name: "TEST_ENV", Value: "test2"},
+					},
+				))
+				g.Expect(workload.IsAdmitted(lowCreatedWorkload)).Should(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		highJob := testingjob.MakeJob("high", ns.Name).
+			Queue(kueue.LocalQueueName(devLocalQ.Name)).
+			Parallelism(5).
+			PriorityClass(highPriorityClass.Name).
+			Request(corev1.ResourceCPU, "1").
+			NodeSelector(instanceKey, "spot-untainted").
+			Obj()
+
+		ginkgo.By("Creating a high priority job", func() {
+			util.MustCreate(ctx, k8sClient, highJob)
+		})
+
+		highCreatedWorkload := &kueue.Workload{}
+		highWlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob.Name, lowJob.UID), Namespace: ns.Name}
+
+		ginkgo.By("Checking that the high priority workload is created and admitted", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, highWlLookupKey, highCreatedWorkload)).Should(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(highCreatedWorkload)).Should(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking that the low priority workload is successfully preempted", func() {
```

  I would like to also add a ginkgo assert on the low priority Job - does it still have the envs as originally, or sanitized.  That could be a separate ginkgo.By

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-10-29T08:28:32Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2360** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-10-29T08:28:31Z
```diff
@@ -2452,6 +2289,82 @@ var _ = ginkgo.Describe("Interacting with scheduler", ginkgo.Ordered, ginkgo.Con
 			util.ExpectLQAdmissionChecksWaitTimeMetric(localQueue, highPriorityClass.Name, 1)
 		})
 	})
+
+	ginkgo.It("should deduplicate env variables after preemption", func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.WorkloadEnvDeduplication, true)
+
+		highPriorityClass := testing.MakePriorityClass("high").PriorityValue(100).Obj()
+		util.MustCreate(ctx, k8sClient, highPriorityClass)
+		ginkgo.DeferCleanup(func() {
+			gomega.Expect(k8sClient.Delete(ctx, highPriorityClass)).To(gomega.Succeed())
+		})
+
+		lowJob := testingjob.MakeJob("low", ns.Name).
+			Queue(kueue.LocalQueueName(devLocalQ.Name)).
+			Parallelism(5).
+			NodeSelector(instanceKey, "spot-untainted").
+			Containers(
+				*testing.MakeContainer().
+					Name("c").
+					Image("sleep").
+					WithResourceReq(corev1.ResourceCPU, "1").
+					WithEnvVar(corev1.EnvVar{Name: "TEST_ENV", Value: "test1"}).
+					WithEnvVar(corev1.EnvVar{Name: "TEST_ENV", Value: "test2"}).
+					Obj(),
+			).
+			Obj()
+
+		ginkgo.By("Creating a low priority job with duplicated environment variables", func() {
+			util.MustCreate(ctx, k8sClient, lowJob)
+		})
+
+		lowCreatedWorkload := &kueue.Workload{}
+		lowWlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob.Name, lowJob.UID), Namespace: ns.Name}
+
+		ginkgo.By("Checking that the low priority workload is created with deduplicated environment variables", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, lowWlLookupKey, lowCreatedWorkload)).Should(gomega.Succeed())
+				g.Expect(lowCreatedWorkload.Spec.PodSets).Should(gomega.HaveLen(1))
+				g.Expect(lowCreatedWorkload.Spec.PodSets[0].Template.Spec.Containers).Should(gomega.HaveLen(1))
+				g.Expect(lowCreatedWorkload.Spec.PodSets[0].Template.Spec.Containers[0].Env).Should(gomega.BeComparableTo(
+					[]corev1.EnvVar{
+						{Name: "TEST_ENV", Value: "test2"},
+					},
+				))
+				g.Expect(workload.IsAdmitted(lowCreatedWorkload)).Should(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		highJob := testingjob.MakeJob("high", ns.Name).
+			Queue(kueue.LocalQueueName(devLocalQ.Name)).
+			Parallelism(5).
+			PriorityClass(highPriorityClass.Name).
+			Request(corev1.ResourceCPU, "1").
+			NodeSelector(instanceKey, "spot-untainted").
+			Obj()
+
+		ginkgo.By("Creating a high priority job", func() {
+			util.MustCreate(ctx, k8sClient, highJob)
+		})
+
+		highCreatedWorkload := &kueue.Workload{}
+		highWlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob.Name, lowJob.UID), Namespace: ns.Name}
+
+		ginkgo.By("Checking that the high priority workload is created and admitted", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, highWlLookupKey, highCreatedWorkload)).Should(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(highCreatedWorkload)).Should(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking that the low priority workload is successfully preempted", func() {
```

  Do we need to sanitize Job?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-29T08:30:08Z

- **pkg/controller/jobframework/utils.go:59** by [@mimowo](https://github.com/mimowo) — 2025-10-29T08:30:08Z
```diff
@@ -0,0 +1,64 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"maps"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/features"
+)
+
+// JobPodSets retrieves the pod sets from a GenericJob and applies environment variable
+// deduplication if the WorkloadEnvDeduplication feature gate is enabled.
+func JobPodSets(ctx context.Context, job GenericJob) ([]kueue.PodSet, error) {
+	podSets, err := job.PodSets(ctx)
+	if err != nil {
+		return nil, err
+	}
+	SanitizePodSets(podSets)
+	return podSets, nil
+}
+
+// SanitizePodSets sanitizes all PodSets in the given slice by removing duplicate
+// environment variables from each container when the WorkloadEnvDeduplication
+// feature is enabled. This function modifies the podSets slice in place.
+func SanitizePodSets(podSets []kueue.PodSet) {
+	for podSetIndex := range podSets {
+		SanitizePodSet(&podSets[podSetIndex])
+	}
+}
+
+// SanitizePodSet sanitizes a single PodSet by removing duplicate environment
+// variables from all containers in its pod template, but only if the
+// WorkloadEnvDeduplication feature gate is enabled.
+func SanitizePodSet(podSet *kueue.PodSet) {
+	if features.Enabled(features.WorkloadEnvDeduplication) {
+		for containerIndex := range podSet.Template.Spec.Containers {
+			container := &podSet.Template.Spec.Containers[containerIndex]
+			envVars := make(map[string]corev1.EnvVar)
+			for _, envVar := range container.Env {
+				envVars[envVar.Name] = envVar
```

  I think when containers execture then 
  PATH="var1"
  PATH="var1$PATH" 
  expands the TEST variable rather than override. This is actually common feature to use in .bashrc on linux to append a new path. It is less common on k8s, but maybe some programs ported from OS to k8s could use it, so I'm thinking that ideally we should resolve it. 
  
  Please check how much complication that would be, maybe there are some library resolvers

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-29T08:33:01Z

- **pkg/features/kube_features.go:208** by [@mimowo](https://github.com/mimowo) — 2025-10-29T08:33:01Z
```diff
@@ -198,6 +198,14 @@ const (
 	//
 	// Enable all updates to Workload objects to use Patch Merge instead of Patch Apply.
 	WorkloadRequestUseMergePatch featuregate.Feature = "WorkloadRequestUseMergePatch"
+
+	// owner: @mbobrovskyi
+	//
+	// WorkloadEnvDeduplication enables automatic deduplication of environment variables
+	// in PodSet templates within Workload objects. When enabled, duplicate env var entries
+	// are resolved by keeping only the last occurrence, allowing workload creation to succeed
+	// even when duplicates are present in the spec.
+	WorkloadEnvDeduplication featuregate.Feature = "WorkloadEnvDeduplication"
```

  ```suggestion
  	// owner: @mbobrovskyi
  	//
  	// SanitizePodSets enables automatic sanitization of PodSets when creating the Workload object.
  	// The main use case it deduplication of environment variables
  	// in PodSet templates within Workload objects. When enabled, duplicate env var entries
  	// are resolved by keeping only the last occurrence, allowing workload creation to succeed
  	// even when duplicates are present in the spec.
  	SanitizePodSets featuregate.Feature = "SanitizePodSets"
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-10-29T08:39:25Z

- **pkg/features/kube_features.go:208** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-10-29T08:39:25Z
```diff
@@ -198,6 +198,14 @@ const (
 	//
 	// Enable all updates to Workload objects to use Patch Merge instead of Patch Apply.
 	WorkloadRequestUseMergePatch featuregate.Feature = "WorkloadRequestUseMergePatch"
+
+	// owner: @mbobrovskyi
+	//
+	// WorkloadEnvDeduplication enables automatic deduplication of environment variables
+	// in PodSet templates within Workload objects. When enabled, duplicate env var entries
+	// are resolved by keeping only the last occurrence, allowing workload creation to succeed
+	// even when duplicates are present in the spec.
+	WorkloadEnvDeduplication featuregate.Feature = "WorkloadEnvDeduplication"
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-10-29T08:39:49Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2360** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-10-29T08:39:49Z
```diff
@@ -2452,6 +2289,82 @@ var _ = ginkgo.Describe("Interacting with scheduler", ginkgo.Ordered, ginkgo.Con
 			util.ExpectLQAdmissionChecksWaitTimeMetric(localQueue, highPriorityClass.Name, 1)
 		})
 	})
+
+	ginkgo.It("should deduplicate env variables after preemption", func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.WorkloadEnvDeduplication, true)
+
+		highPriorityClass := testing.MakePriorityClass("high").PriorityValue(100).Obj()
+		util.MustCreate(ctx, k8sClient, highPriorityClass)
+		ginkgo.DeferCleanup(func() {
+			gomega.Expect(k8sClient.Delete(ctx, highPriorityClass)).To(gomega.Succeed())
+		})
+
+		lowJob := testingjob.MakeJob("low", ns.Name).
+			Queue(kueue.LocalQueueName(devLocalQ.Name)).
+			Parallelism(5).
+			NodeSelector(instanceKey, "spot-untainted").
+			Containers(
+				*testing.MakeContainer().
+					Name("c").
+					Image("sleep").
+					WithResourceReq(corev1.ResourceCPU, "1").
+					WithEnvVar(corev1.EnvVar{Name: "TEST_ENV", Value: "test1"}).
+					WithEnvVar(corev1.EnvVar{Name: "TEST_ENV", Value: "test2"}).
+					Obj(),
+			).
+			Obj()
+
+		ginkgo.By("Creating a low priority job with duplicated environment variables", func() {
+			util.MustCreate(ctx, k8sClient, lowJob)
+		})
+
+		lowCreatedWorkload := &kueue.Workload{}
+		lowWlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob.Name, lowJob.UID), Namespace: ns.Name}
+
+		ginkgo.By("Checking that the low priority workload is created with deduplicated environment variables", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, lowWlLookupKey, lowCreatedWorkload)).Should(gomega.Succeed())
+				g.Expect(lowCreatedWorkload.Spec.PodSets).Should(gomega.HaveLen(1))
+				g.Expect(lowCreatedWorkload.Spec.PodSets[0].Template.Spec.Containers).Should(gomega.HaveLen(1))
+				g.Expect(lowCreatedWorkload.Spec.PodSets[0].Template.Spec.Containers[0].Env).Should(gomega.BeComparableTo(
+					[]corev1.EnvVar{
+						{Name: "TEST_ENV", Value: "test2"},
+					},
+				))
+				g.Expect(workload.IsAdmitted(lowCreatedWorkload)).Should(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		highJob := testingjob.MakeJob("high", ns.Name).
+			Queue(kueue.LocalQueueName(devLocalQ.Name)).
+			Parallelism(5).
+			PriorityClass(highPriorityClass.Name).
+			Request(corev1.ResourceCPU, "1").
+			NodeSelector(instanceKey, "spot-untainted").
+			Obj()
+
+		ginkgo.By("Creating a high priority job", func() {
+			util.MustCreate(ctx, k8sClient, highJob)
+		})
+
+		highCreatedWorkload := &kueue.Workload{}
+		highWlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob.Name, lowJob.UID), Namespace: ns.Name}
+
+		ginkgo.By("Checking that the high priority workload is created and admitted", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, highWlLookupKey, highCreatedWorkload)).Should(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(highCreatedWorkload)).Should(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking that the low priority workload is successfully preempted", func() {
```

  Added.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-29T08:43:33Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2360** by [@mimowo](https://github.com/mimowo) — 2025-10-29T08:43:33Z
```diff
@@ -2452,6 +2289,82 @@ var _ = ginkgo.Describe("Interacting with scheduler", ginkgo.Ordered, ginkgo.Con
 			util.ExpectLQAdmissionChecksWaitTimeMetric(localQueue, highPriorityClass.Name, 1)
 		})
 	})
+
+	ginkgo.It("should deduplicate env variables after preemption", func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.WorkloadEnvDeduplication, true)
+
+		highPriorityClass := testing.MakePriorityClass("high").PriorityValue(100).Obj()
+		util.MustCreate(ctx, k8sClient, highPriorityClass)
+		ginkgo.DeferCleanup(func() {
+			gomega.Expect(k8sClient.Delete(ctx, highPriorityClass)).To(gomega.Succeed())
+		})
+
+		lowJob := testingjob.MakeJob("low", ns.Name).
+			Queue(kueue.LocalQueueName(devLocalQ.Name)).
+			Parallelism(5).
+			NodeSelector(instanceKey, "spot-untainted").
+			Containers(
+				*testing.MakeContainer().
+					Name("c").
+					Image("sleep").
+					WithResourceReq(corev1.ResourceCPU, "1").
+					WithEnvVar(corev1.EnvVar{Name: "TEST_ENV", Value: "test1"}).
+					WithEnvVar(corev1.EnvVar{Name: "TEST_ENV", Value: "test2"}).
+					Obj(),
+			).
+			Obj()
+
+		ginkgo.By("Creating a low priority job with duplicated environment variables", func() {
+			util.MustCreate(ctx, k8sClient, lowJob)
+		})
+
+		lowCreatedWorkload := &kueue.Workload{}
+		lowWlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob.Name, lowJob.UID), Namespace: ns.Name}
+
+		ginkgo.By("Checking that the low priority workload is created with deduplicated environment variables", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, lowWlLookupKey, lowCreatedWorkload)).Should(gomega.Succeed())
+				g.Expect(lowCreatedWorkload.Spec.PodSets).Should(gomega.HaveLen(1))
+				g.Expect(lowCreatedWorkload.Spec.PodSets[0].Template.Spec.Containers).Should(gomega.HaveLen(1))
+				g.Expect(lowCreatedWorkload.Spec.PodSets[0].Template.Spec.Containers[0].Env).Should(gomega.BeComparableTo(
+					[]corev1.EnvVar{
+						{Name: "TEST_ENV", Value: "test2"},
+					},
+				))
+				g.Expect(workload.IsAdmitted(lowCreatedWorkload)).Should(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		highJob := testingjob.MakeJob("high", ns.Name).
+			Queue(kueue.LocalQueueName(devLocalQ.Name)).
+			Parallelism(5).
+			PriorityClass(highPriorityClass.Name).
+			Request(corev1.ResourceCPU, "1").
+			NodeSelector(instanceKey, "spot-untainted").
+			Obj()
+
+		ginkgo.By("Creating a high priority job", func() {
+			util.MustCreate(ctx, k8sClient, highJob)
+		})
+
+		highCreatedWorkload := &kueue.Workload{}
+		highWlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob.Name, lowJob.UID), Namespace: ns.Name}
+
+		ginkgo.By("Checking that the high priority workload is created and admitted", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, highWlLookupKey, highCreatedWorkload)).Should(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(highCreatedWorkload)).Should(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking that the low priority workload is successfully preempted", func() {
```

  > Do we need to sanitize Job?
  
  No, it is actually better to keep job as is, nice that it didn't require any additiional code changes 👍

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-10-29T08:44:39Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2360** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-10-29T08:44:39Z
```diff
@@ -2452,6 +2289,82 @@ var _ = ginkgo.Describe("Interacting with scheduler", ginkgo.Ordered, ginkgo.Con
 			util.ExpectLQAdmissionChecksWaitTimeMetric(localQueue, highPriorityClass.Name, 1)
 		})
 	})
+
+	ginkgo.It("should deduplicate env variables after preemption", func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.WorkloadEnvDeduplication, true)
+
+		highPriorityClass := testing.MakePriorityClass("high").PriorityValue(100).Obj()
+		util.MustCreate(ctx, k8sClient, highPriorityClass)
+		ginkgo.DeferCleanup(func() {
+			gomega.Expect(k8sClient.Delete(ctx, highPriorityClass)).To(gomega.Succeed())
+		})
+
+		lowJob := testingjob.MakeJob("low", ns.Name).
+			Queue(kueue.LocalQueueName(devLocalQ.Name)).
+			Parallelism(5).
+			NodeSelector(instanceKey, "spot-untainted").
+			Containers(
+				*testing.MakeContainer().
+					Name("c").
+					Image("sleep").
+					WithResourceReq(corev1.ResourceCPU, "1").
+					WithEnvVar(corev1.EnvVar{Name: "TEST_ENV", Value: "test1"}).
+					WithEnvVar(corev1.EnvVar{Name: "TEST_ENV", Value: "test2"}).
+					Obj(),
+			).
+			Obj()
+
+		ginkgo.By("Creating a low priority job with duplicated environment variables", func() {
+			util.MustCreate(ctx, k8sClient, lowJob)
+		})
+
+		lowCreatedWorkload := &kueue.Workload{}
+		lowWlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob.Name, lowJob.UID), Namespace: ns.Name}
+
+		ginkgo.By("Checking that the low priority workload is created with deduplicated environment variables", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, lowWlLookupKey, lowCreatedWorkload)).Should(gomega.Succeed())
+				g.Expect(lowCreatedWorkload.Spec.PodSets).Should(gomega.HaveLen(1))
+				g.Expect(lowCreatedWorkload.Spec.PodSets[0].Template.Spec.Containers).Should(gomega.HaveLen(1))
+				g.Expect(lowCreatedWorkload.Spec.PodSets[0].Template.Spec.Containers[0].Env).Should(gomega.BeComparableTo(
+					[]corev1.EnvVar{
+						{Name: "TEST_ENV", Value: "test2"},
+					},
+				))
+				g.Expect(workload.IsAdmitted(lowCreatedWorkload)).Should(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		highJob := testingjob.MakeJob("high", ns.Name).
+			Queue(kueue.LocalQueueName(devLocalQ.Name)).
+			Parallelism(5).
+			PriorityClass(highPriorityClass.Name).
+			Request(corev1.ResourceCPU, "1").
+			NodeSelector(instanceKey, "spot-untainted").
+			Obj()
+
+		ginkgo.By("Creating a high priority job", func() {
+			util.MustCreate(ctx, k8sClient, highJob)
+		})
+
+		highCreatedWorkload := &kueue.Workload{}
+		highWlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob.Name, lowJob.UID), Namespace: ns.Name}
+
+		ginkgo.By("Checking that the high priority workload is created and admitted", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, highWlLookupKey, highCreatedWorkload)).Should(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(highCreatedWorkload)).Should(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking that the low priority workload is successfully preempted", func() {
```

  OK. Than it is done.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-10-29T09:41:09Z

- **pkg/controller/jobframework/utils.go:59** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-10-29T09:41:09Z
```diff
@@ -0,0 +1,64 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"maps"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/features"
+)
+
+// JobPodSets retrieves the pod sets from a GenericJob and applies environment variable
+// deduplication if the WorkloadEnvDeduplication feature gate is enabled.
+func JobPodSets(ctx context.Context, job GenericJob) ([]kueue.PodSet, error) {
+	podSets, err := job.PodSets(ctx)
+	if err != nil {
+		return nil, err
+	}
+	SanitizePodSets(podSets)
+	return podSets, nil
+}
+
+// SanitizePodSets sanitizes all PodSets in the given slice by removing duplicate
+// environment variables from each container when the WorkloadEnvDeduplication
+// feature is enabled. This function modifies the podSets slice in place.
+func SanitizePodSets(podSets []kueue.PodSet) {
+	for podSetIndex := range podSets {
+		SanitizePodSet(&podSets[podSetIndex])
+	}
+}
+
+// SanitizePodSet sanitizes a single PodSet by removing duplicate environment
+// variables from all containers in its pod template, but only if the
+// WorkloadEnvDeduplication feature gate is enabled.
+func SanitizePodSet(podSet *kueue.PodSet) {
+	if features.Enabled(features.WorkloadEnvDeduplication) {
+		for containerIndex := range podSet.Template.Spec.Containers {
+			container := &podSet.Template.Spec.Containers[containerIndex]
+			envVars := make(map[string]corev1.EnvVar)
+			for _, envVar := range container.Env {
+				envVars[envVar.Name] = envVar
```

  I tired to run this on k8s:
  
  ```yaml
  apiVersion: batch/v1
  kind: Job
  metadata:
    name: sample-job
    labels:
      kueue.x-k8s.io/queue-name: user-queue
  spec:
    parallelism: 3
    completions: 3
    completionMode: Indexed
    suspend: false
    template:
      spec:
        containers:
          - name: dummy-job
            image: busybox
            command:
              - printenv
            env:
              - name: TEST
                value: "/some-path"
              - name: TEST
                value: "/another-path:$TEST"
            resources:
              requests:
                cpu: "1"
                memory: "1Mi"
        restartPolicy: Never
  ```
  
  And as a result I catch:
  
  ```
  kubectl logs sample-job-0-zkkbr                                                               
  PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
  HOSTNAME=sample-job-0
  TEST=/another-path:$TEST
  JOB_COMPLETION_INDEX=0
  KUBERNETES_PORT=tcp://10.96.0.1:443
  KUBERNETES_PORT_443_TCP=tcp://10.96.0.1:443
  KUBERNETES_PORT_443_TCP_PROTO=tcp
  KUBERNETES_PORT_443_TCP_PORT=443
  KUBERNETES_PORT_443_TCP_ADDR=10.96.0.1
  KUBERNETES_SERVICE_HOST=10.96.0.1
  KUBERNETES_SERVICE_PORT=443
  KUBERNETES_SERVICE_PORT_HTTPS=443
  HOME=/root
  ```
  
  So I don’t think we need to support this.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-29T10:51:16Z

- **pkg/controller/jobframework/utils.go:59** by [@mimowo](https://github.com/mimowo) — 2025-10-29T10:51:16Z
```diff
@@ -0,0 +1,64 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"maps"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/features"
+)
+
+// JobPodSets retrieves the pod sets from a GenericJob and applies environment variable
+// deduplication if the WorkloadEnvDeduplication feature gate is enabled.
+func JobPodSets(ctx context.Context, job GenericJob) ([]kueue.PodSet, error) {
+	podSets, err := job.PodSets(ctx)
+	if err != nil {
+		return nil, err
+	}
+	SanitizePodSets(podSets)
+	return podSets, nil
+}
+
+// SanitizePodSets sanitizes all PodSets in the given slice by removing duplicate
+// environment variables from each container when the WorkloadEnvDeduplication
+// feature is enabled. This function modifies the podSets slice in place.
+func SanitizePodSets(podSets []kueue.PodSet) {
+	for podSetIndex := range podSets {
+		SanitizePodSet(&podSets[podSetIndex])
+	}
+}
+
+// SanitizePodSet sanitizes a single PodSet by removing duplicate environment
+// variables from all containers in its pod template, but only if the
+// WorkloadEnvDeduplication feature gate is enabled.
+func SanitizePodSet(podSet *kueue.PodSet) {
+	if features.Enabled(features.WorkloadEnvDeduplication) {
+		for containerIndex := range podSet.Template.Spec.Containers {
+			container := &podSet.Template.Spec.Containers[containerIndex]
+			envVars := make(map[string]corev1.EnvVar)
+			for _, envVar := range container.Env {
+				envVars[envVar.Name] = envVar
```

  Awesome, thank you for the test, it makes our job easier

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-29T10:58:28Z

- **pkg/features/kube_features.go:325** by [@mimowo](https://github.com/mimowo) — 2025-10-29T10:58:28Z
```diff
@@ -312,6 +321,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	WorkloadRequestUseMergePatch: {
 		{Version: version.MustParse("0.14"), Default: false, PreRelease: featuregate.Alpha},
 	},
+	SanitizePodSets: {
+		{Version: version.MustParse("0.15"), Default: false, PreRelease: featuregate.Alpha},
```

  ```suggestion
  		{Version: version.MustParse("0.15"), Default: true, PreRelease: featuregate.Beta},
  ```
  I think this is a bugfix, we may introduce the FG just as a bailout option if something goes wrong, but I think this should be default behavior.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-10-29T11:11:44Z

- **pkg/features/kube_features.go:325** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-10-29T11:11:44Z
```diff
@@ -312,6 +321,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	WorkloadRequestUseMergePatch: {
 		{Version: version.MustParse("0.14"), Default: false, PreRelease: featuregate.Alpha},
 	},
+	SanitizePodSets: {
+		{Version: version.MustParse("0.15"), Default: false, PreRelease: featuregate.Alpha},
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-29T15:12:10Z

- **pkg/controller/jobframework/utils.go:60** by [@mimowo](https://github.com/mimowo) — 2025-10-29T15:12:10Z
```diff
@@ -0,0 +1,64 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"maps"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/features"
+)
+
+// JobPodSets retrieves the pod sets from a GenericJob and applies environment variable
+// deduplication if the SanitizePodSets feature gate is enabled.
+func JobPodSets(ctx context.Context, job GenericJob) ([]kueue.PodSet, error) {
+	podSets, err := job.PodSets(ctx)
+	if err != nil {
+		return nil, err
+	}
+	SanitizePodSets(podSets)
+	return podSets, nil
+}
+
+// SanitizePodSets sanitizes all PodSets in the given slice by removing duplicate
+// environment variables from each container when the SanitizePodSets
+// feature is enabled. This function modifies the podSets slice in place.
+func SanitizePodSets(podSets []kueue.PodSet) {
+	for podSetIndex := range podSets {
+		SanitizePodSet(&podSets[podSetIndex])
+	}
+}
+
+// SanitizePodSet sanitizes a single PodSet by removing duplicate environment
+// variables from all containers in its pod template, but only if the
+// SanitizePodSets feature gate is enabled.
+func SanitizePodSet(podSet *kueue.PodSet) {
+	if features.Enabled(features.SanitizePodSets) {
+		for containerIndex := range podSet.Template.Spec.Containers {
+			container := &podSet.Template.Spec.Containers[containerIndex]
+			envVars := make(map[string]corev1.EnvVar)
+			for _, envVar := range container.Env {
+				envVars[envVar.Name] = envVar
+			}
```

  Actually, this code may not be keeping the env variables in the same order. I think it is better to make sure they are in the same order as before.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-29T15:14:48Z

- **pkg/controller/jobframework/utils.go:60** by [@mimowo](https://github.com/mimowo) — 2025-10-29T15:14:48Z
```diff
@@ -0,0 +1,64 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"maps"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/features"
+)
+
+// JobPodSets retrieves the pod sets from a GenericJob and applies environment variable
+// deduplication if the SanitizePodSets feature gate is enabled.
+func JobPodSets(ctx context.Context, job GenericJob) ([]kueue.PodSet, error) {
+	podSets, err := job.PodSets(ctx)
+	if err != nil {
+		return nil, err
+	}
+	SanitizePodSets(podSets)
+	return podSets, nil
+}
+
+// SanitizePodSets sanitizes all PodSets in the given slice by removing duplicate
+// environment variables from each container when the SanitizePodSets
+// feature is enabled. This function modifies the podSets slice in place.
+func SanitizePodSets(podSets []kueue.PodSet) {
+	for podSetIndex := range podSets {
+		SanitizePodSet(&podSets[podSetIndex])
+	}
+}
+
+// SanitizePodSet sanitizes a single PodSet by removing duplicate environment
+// variables from all containers in its pod template, but only if the
+// SanitizePodSets feature gate is enabled.
+func SanitizePodSet(podSet *kueue.PodSet) {
+	if features.Enabled(features.SanitizePodSets) {
+		for containerIndex := range podSet.Template.Spec.Containers {
+			container := &podSet.Template.Spec.Containers[containerIndex]
+			envVars := make(map[string]corev1.EnvVar)
+			for _, envVar := range container.Env {
+				envVars[envVar.Name] = envVar
+			}
```

  I think we could reuse this structure in `pkg/util/orderedgroups/ordered_groups.go` which is esstinatilly what we need

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-10-29T16:41:26Z

- **pkg/controller/jobframework/utils.go:60** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-10-29T16:41:26Z
```diff
@@ -0,0 +1,64 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"maps"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/features"
+)
+
+// JobPodSets retrieves the pod sets from a GenericJob and applies environment variable
+// deduplication if the SanitizePodSets feature gate is enabled.
+func JobPodSets(ctx context.Context, job GenericJob) ([]kueue.PodSet, error) {
+	podSets, err := job.PodSets(ctx)
+	if err != nil {
+		return nil, err
+	}
+	SanitizePodSets(podSets)
+	return podSets, nil
+}
+
+// SanitizePodSets sanitizes all PodSets in the given slice by removing duplicate
+// environment variables from each container when the SanitizePodSets
+// feature is enabled. This function modifies the podSets slice in place.
+func SanitizePodSets(podSets []kueue.PodSet) {
+	for podSetIndex := range podSets {
+		SanitizePodSet(&podSets[podSetIndex])
+	}
+}
+
+// SanitizePodSet sanitizes a single PodSet by removing duplicate environment
+// variables from all containers in its pod template, but only if the
+// SanitizePodSets feature gate is enabled.
+func SanitizePodSet(podSet *kueue.PodSet) {
+	if features.Enabled(features.SanitizePodSets) {
+		for containerIndex := range podSet.Template.Spec.Containers {
+			container := &podSet.Template.Spec.Containers[containerIndex]
+			envVars := make(map[string]corev1.EnvVar)
+			for _, envVar := range container.Env {
+				envVars[envVar.Name] = envVar
+			}
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-30T07:56:47Z

/lgtm
/approve
Thank you 👍
