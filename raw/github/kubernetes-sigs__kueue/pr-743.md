# PR #743: [website/docs] Resource definition for workloads, queue `pods` quota

**Summary**: [website/docs] Resource definition for workloads, queue `pods` quota

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/743

**Last updated**: 2023-06-19T20:33:16Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `website-0.4-nanage-number-of-pods` → `website-release-0.4`
- **Created**: 2023-05-04T12:18:05Z
- **Updated**: 2023-06-19T20:33:16Z
- **Merged**: 2023-05-08T17:49:40Z
- **Closed**: 2023-05-08T17:49:40Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mwielgus](https://github.com/mwielgus), [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 2   **+40 / -5**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Adds a Resource section in  the workload description.
 Mention the ability to set up ClusteQueue `pods` quota.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #741

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@trasc](https://github.com/trasc) — 2023-05-04T12:18:56Z

/cc @mwielgus 
/cc @alculquicondor

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-05-08T17:47:54Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/743#pullrequestreview-1417235152" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/743#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/website-release-0.4/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-04T13:34:52Z

- **site/content/en/docs/concepts/cluster_queue.md:96** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-04T13:04:21Z
```diff
@@ -85,12 +85,16 @@ spec:
         nominalQuota: 9
       - name: "memory"
         nominalQuota: 36Gi
+      - name: "pods"
```

  needs to be listed in "coveredResources" above as well

- **site/content/en/docs/concepts/workload.md:67** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-04T13:05:54Z
```diff
@@ -62,6 +62,26 @@ the following fields:
 - `name` is a human-readable identifier for the pod set. You can use the role of
   the Pods in the Workload, like `driver`, `worker`, `parameter-server`, etc.
 
+### Resource requests
+
+A key role in deciding if and when a workload is played by the `podSets` resource requests.
```

  ```suggestion
  Kueue uses the `podSets` resources requests to calculate the quota used by a Workload and decide if and when to admit a Workload.
  ```

- **site/content/en/docs/concepts/workload.md:70** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-04T13:29:53Z
```diff
@@ -62,6 +62,26 @@ the following fields:
 - `name` is a human-readable identifier for the pod set. You can use the role of
   the Pods in the Workload, like `driver`, `worker`, `parameter-server`, etc.
 
+### Resource requests
+
+A key role in deciding if and when a workload is played by the `podSets` resource requests.
+
```

  Also add:
  Kueue calculates the total resources usage for a Workload as the sum of the resource requests for each podSet. The resource usage of a podSet is equal to the resource requests of the pod spec multiplied by the `count`.

- **site/content/en/docs/concepts/workload.md:71** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-04T13:30:45Z
```diff
@@ -62,6 +62,26 @@ the following fields:
 - `name` is a human-readable identifier for the pod set. You can use the role of
   the Pods in the Workload, like `driver`, `worker`, `parameter-server`, etc.
 
+### Resource requests
+
+A key role in deciding if and when a workload is played by the `podSets` resource requests.
+
+#### Requests values adjustment
+
+Depending on the cluster setup and the valued provided in the `spec` the values used in the scheduling process could be different if:
```

  ```suggestion
  Depending on the cluster setup, Kueue will adjust the resource usage of a Workload based on:
  ```

- **site/content/en/docs/concepts/workload.md:79** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-04T13:32:37Z
```diff
@@ -62,6 +62,26 @@ the following fields:
 - `name` is a human-readable identifier for the pod set. You can use the role of
   the Pods in the Workload, like `driver`, `worker`, `parameter-server`, etc.
 
+### Resource requests
+
+A key role in deciding if and when a workload is played by the `podSets` resource requests.
+
+#### Requests values adjustment
+
+Depending on the cluster setup and the valued provided in the `spec` the values used in the scheduling process could be different if:
+
+- The cluster defines default values in [Limit Ranges](https://kubernetes.io/docs/concepts/policy/limit-range/), the default values will be used if not provided in the `spec`.
+- The created pods are subject of a [Runtime Class Overhead](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-overhead/).
+- The spec defines only resource limits, case in which the limit values will be treated as requests.
+
+#### Requests values validation
+
+In cases when the cluster defines Limit Ranges, the values resulting from the adjustment above will be validated against the ranges. Failing the validation will mark the workload as `Inadmissible`.
```

  Use active voice:
  
  Kueue will mark the Workload as `Inadmissible` if...

- **site/content/en/docs/concepts/workload.md:83** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-04T13:34:40Z
```diff
@@ -62,6 +62,26 @@ the following fields:
 - `name` is a human-readable identifier for the pod set. You can use the role of
   the Pods in the Workload, like `driver`, `worker`, `parameter-server`, etc.
 
+### Resource requests
+
+A key role in deciding if and when a workload is played by the `podSets` resource requests.
+
+#### Requests values adjustment
+
+Depending on the cluster setup and the valued provided in the `spec` the values used in the scheduling process could be different if:
+
+- The cluster defines default values in [Limit Ranges](https://kubernetes.io/docs/concepts/policy/limit-range/), the default values will be used if not provided in the `spec`.
+- The created pods are subject of a [Runtime Class Overhead](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-overhead/).
+- The spec defines only resource limits, case in which the limit values will be treated as requests.
+
+#### Requests values validation
+
+In cases when the cluster defines Limit Ranges, the values resulting from the adjustment above will be validated against the ranges. Failing the validation will mark the workload as `Inadmissible`.
+
+#### Reserved resource names
+
+In addition to the usual resource naming restrictions, `pods` is reserved for internal Kueue use. And can only be used by a [ClusterQueue](/docs/concepts/cluster_queue#resources) to set quotas on the maximum number of pods.
```

  ```suggestion
  In addition to the usual resource naming restrictions, you cannot use the `pods` resource name in a Pod spec, as it is reserved for internal Kueue use. You can use the `pods` resource name in a [ClusterQueue](/docs/concepts/cluster_queue#resources) to set quotas on the maximum number of pods.
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-04T18:15:04Z

- **site/content/en/docs/concepts/workload.md:1** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-04T18:15:03Z
  IIRC, the kueue admits workloads with no resource requests/limits without any restrictions since the kueue can not assign any flavors to workloads.
  
  So, it might be better to mention the limitations and strongly suggest adding `pods` to ClusterQueue for the users who plan to deploy workloads with no resource requests/limits.
  
  @trasc What do you think?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-08T05:49:52Z

- **site/content/en/docs/concepts/cluster_queue.md:96** by [@trasc](https://github.com/trasc) — 2023-05-08T05:49:52Z
```diff
@@ -85,12 +85,16 @@ spec:
         nominalQuota: 9
       - name: "memory"
         nominalQuota: 36Gi
+      - name: "pods"
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-08T05:51:07Z

- **site/content/en/docs/concepts/workload.md:67** by [@trasc](https://github.com/trasc) — 2023-05-08T05:51:07Z
```diff
@@ -62,6 +62,26 @@ the following fields:
 - `name` is a human-readable identifier for the pod set. You can use the role of
   the Pods in the Workload, like `driver`, `worker`, `parameter-server`, etc.
 
+### Resource requests
+
+A key role in deciding if and when a workload is played by the `podSets` resource requests.
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-08T05:52:48Z

- **site/content/en/docs/concepts/workload.md:70** by [@trasc](https://github.com/trasc) — 2023-05-08T05:52:48Z
```diff
@@ -62,6 +62,26 @@ the following fields:
 - `name` is a human-readable identifier for the pod set. You can use the role of
   the Pods in the Workload, like `driver`, `worker`, `parameter-server`, etc.
 
+### Resource requests
+
+A key role in deciding if and when a workload is played by the `podSets` resource requests.
+
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-08T05:53:38Z

- **site/content/en/docs/concepts/workload.md:71** by [@trasc](https://github.com/trasc) — 2023-05-08T05:53:38Z
```diff
@@ -62,6 +62,26 @@ the following fields:
 - `name` is a human-readable identifier for the pod set. You can use the role of
   the Pods in the Workload, like `driver`, `worker`, `parameter-server`, etc.
 
+### Resource requests
+
+A key role in deciding if and when a workload is played by the `podSets` resource requests.
+
+#### Requests values adjustment
+
+Depending on the cluster setup and the valued provided in the `spec` the values used in the scheduling process could be different if:
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-08T05:56:05Z

- **site/content/en/docs/concepts/workload.md:79** by [@trasc](https://github.com/trasc) — 2023-05-08T05:56:05Z
```diff
@@ -62,6 +62,26 @@ the following fields:
 - `name` is a human-readable identifier for the pod set. You can use the role of
   the Pods in the Workload, like `driver`, `worker`, `parameter-server`, etc.
 
+### Resource requests
+
+A key role in deciding if and when a workload is played by the `podSets` resource requests.
+
+#### Requests values adjustment
+
+Depending on the cluster setup and the valued provided in the `spec` the values used in the scheduling process could be different if:
+
+- The cluster defines default values in [Limit Ranges](https://kubernetes.io/docs/concepts/policy/limit-range/), the default values will be used if not provided in the `spec`.
+- The created pods are subject of a [Runtime Class Overhead](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-overhead/).
+- The spec defines only resource limits, case in which the limit values will be treated as requests.
+
+#### Requests values validation
+
+In cases when the cluster defines Limit Ranges, the values resulting from the adjustment above will be validated against the ranges. Failing the validation will mark the workload as `Inadmissible`.
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-08T06:07:17Z

- **site/content/en/docs/concepts/workload.md:83** by [@trasc](https://github.com/trasc) — 2023-05-08T06:07:17Z
```diff
@@ -62,6 +62,26 @@ the following fields:
 - `name` is a human-readable identifier for the pod set. You can use the role of
   the Pods in the Workload, like `driver`, `worker`, `parameter-server`, etc.
 
+### Resource requests
+
+A key role in deciding if and when a workload is played by the `podSets` resource requests.
+
+#### Requests values adjustment
+
+Depending on the cluster setup and the valued provided in the `spec` the values used in the scheduling process could be different if:
+
+- The cluster defines default values in [Limit Ranges](https://kubernetes.io/docs/concepts/policy/limit-range/), the default values will be used if not provided in the `spec`.
+- The created pods are subject of a [Runtime Class Overhead](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-overhead/).
+- The spec defines only resource limits, case in which the limit values will be treated as requests.
+
+#### Requests values validation
+
+In cases when the cluster defines Limit Ranges, the values resulting from the adjustment above will be validated against the ranges. Failing the validation will mark the workload as `Inadmissible`.
+
+#### Reserved resource names
+
+In addition to the usual resource naming restrictions, `pods` is reserved for internal Kueue use. And can only be used by a [ClusterQueue](/docs/concepts/cluster_queue#resources) to set quotas on the maximum number of pods.
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-08T08:00:34Z

- **site/content/en/docs/concepts/workload.md:1** by [@trasc](https://github.com/trasc) — 2023-05-08T08:00:33Z
  added 
  ```
  Since pods resource name is reservedand it's value is computed by Kueue in the during admission, not provided by the batch user, it could be used by the cluster administrators to limit the number of zero or very small resource requesting workloads admitted at the same time.
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-08T08:54:49Z

Totally LGTM.

- **site/content/en/docs/concepts/cluster_queue.md:68** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-08T08:49:48Z
```diff
@@ -60,6 +63,11 @@ Kueue assigns the first flavor in the ClusterQueue's `.spec.resourceGroups[*].fl
 list that has enough unused `nominalQuota` quota in the ClusterQueue or the
 ClusterQueue's [cohort](#cohort).
 
+Since `pods` resource name is [reserved](/docs/conceps/workload#reserved-resource-names) and it's value
+is computed by Kueue in the during [admission](/docs/concepts#admission), not provided by the [batch user](/docs/tasks/#batch-user),
+it could be used by the [cluster administrators](/docs/tasks#batch-administrator) to limit the number of zero or very
```

  ```suggestion
  it could be used by the [batch administrators](/docs/tasks#batch-administrator) to limit the number of zero or very
  ```
  
  nit: Consistently using the `batch administrators` would be good throughout the whole of the docs.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-08T09:54:11Z

- **site/content/en/docs/concepts/cluster_queue.md:68** by [@trasc](https://github.com/trasc) — 2023-05-08T09:54:11Z
```diff
@@ -60,6 +63,11 @@ Kueue assigns the first flavor in the ClusterQueue's `.spec.resourceGroups[*].fl
 list that has enough unused `nominalQuota` quota in the ClusterQueue or the
 ClusterQueue's [cohort](#cohort).
 
+Since `pods` resource name is [reserved](/docs/conceps/workload#reserved-resource-names) and it's value
+is computed by Kueue in the during [admission](/docs/concepts#admission), not provided by the [batch user](/docs/tasks/#batch-user),
+it could be used by the [cluster administrators](/docs/tasks#batch-administrator) to limit the number of zero or very
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-08T09:57:44Z

/lgtm

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-08T17:47:47Z

/lgtm
/approve
