# PR #7488: enable optional fields, optionalorrequired and required fields

**Summary**: enable optional fields, optionalorrequired and required fields

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7488

**Last updated**: 2025-11-05T19:48:56Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kannon92](https://github.com/kannon92)
- **Branch**: `enable-all-lint-checks` → `main`
- **Created**: 2025-10-31T15:32:25Z
- **Updated**: 2025-11-05T19:48:56Z
- **Merged**: 2025-11-05T19:48:54Z
- **Closed**: 2025-11-05T19:48:54Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 23   **+276 / -189**

## Description

Enable all linter checks.

This is just to display the failures so we can decide if we should apply these.

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-10-31T15:32:46Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 3b009aaa56317dc6635d863505d6289cc34f40f1 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/690b8e970934080008711607 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-7488--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-7488--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTc0ODgtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.zENuFODihWO5o7kcK6T4g6XwqgoddAm8w7XTaRHyv84)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-7488--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@kannon92](https://github.com/kannon92) — 2025-10-31T15:44:33Z

cc @mimowo @tenzen-y

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-31T15:54:52Z

Thank you !

It does not look too bad, we have only two "nobools" violations for example:  

```
apis/kueue/v1beta2/workload_types.go:78:2: nobools: field Active pointer should not use a bool. Use a string type with meaningful constant values as an enum. (kubeapilinter)
	Active *bool `json:"active,omitempty"`
	^
apis/kueue/v1beta2/workload_types.go:116:2: nobools: field Unconstrained pointer should not use a bool. Use a string type with meaningful constant values as an enum. (kubeapilinter)
	Unconstrained *bool `json:"unconstrained,omitempty"`
```
TBH I think both are justified, I would suggest to just open a PR enabling the rule and disabling the check for these two fields. This will at least prompt us to think when adding new fields.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-31T15:57:24Z

Similarly for nomaps we have:

```
apis/kueue/v1beta2/workload_types.go:193:2: nomaps: ResourceUsage should not use a map type, use a list type with a unique name/identifier instead (kubeapilinter)
	ResourceUsage corev1.ResourceList `json:"resourceUsage,omitempty"`
	^
apis/kueue/v1beta2/workload_types.go:630:2: nomaps: Resources should not use a map type, use a list type with a unique name/identifier instead (kubeapilinter)
	Resources corev1.ResourceList `json:"resources,omitempty"`
	^
apis/kueue/v1beta2/fairsharing_types.go:68:2: nomaps: ConsumedResources should not use a map type, use a list type with a unique name/identifier instead (kubeapilinter)
	ConsumedResources corev1.ResourceList `json:"consumedResources"`
	^
```
I think all can be justified using `orev1.ResourceList. So I vote for enabling the linter check and adding the three fields to /nolint directive. Again, it will prompt us in the future to think about the API.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-31T15:59:37Z

For the optionalfields we have :

```
apis/kueue/v1beta2/clusterqueue_types.go:293:2: optionalfields: field FlavorsReservation should have the omitempty tag. (kubeapilinter)
	FlavorsReservation []FlavorUsage `json:"flavorsReservation"`
	^
apis/kueue/v1beta2/clusterqueue_types.go:301:2: optionalfields: field FlavorsUsage should have the omitempty tag. (kubeapilinter)
	FlavorsUsage []FlavorUsage `json:"flavorsUsage"`
	^
apis/kueue/v1beta2/clusterqueue_types.go:306:2: optionalfields: field PendingWorkloads should be a pointer. (kubeapilinter)
	PendingWorkloads int32 `json:"pendingWorkloads"`
	^
apis/kueue/v1beta2/clusterqueue_types.go:311:2: optionalfields: field ReservingWorkloads should be a pointer. (kubeapilinter)
	ReservingWorkloads int32 `json:"reservingWorkloads"`
	^
apis/kueue/v1beta2/clusterqueue_types.go:316:2: optionalfields: field AdmittedWorkloads should be a pointer. (kubeapilinter)
	AdmittedWorkloads int32 `json:"admittedWorkloads"`
	^
```
I would think they are all really required, and so shouldn't be pointers, but maybe I'm missing something. Adding "omitempty" also sounds like a good idea for some.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-31T16:00:46Z

Cases like this:
```
apis/kueue/v1beta2/admissioncheck_types.go:111:2: optionalorrequired: field Spec must be marked as optional or required (kubeapilinter)
	Spec AdmissionCheckSpec `json:"spec,omitempty"`
	^
apis/kueue/v1beta2/admissioncheck_types.go:114:2: optionalorrequired: field Status must be marked as optional or required (kubeapilinter)
	Status AdmissionCheckStatus `json:"status,omitempty"`
	^
```
seem like just adding required to spec and status. It feels weird we need to add required to spec, but if this makes linter happy  then ok.

### Comment by [@kannon92](https://github.com/kannon92) — 2025-11-02T19:30:58Z

/release-note-none

### Comment by [@kannon92](https://github.com/kannon92) — 2025-11-02T19:31:18Z

For nobools and nomaps, I opened up https://github.com/kubernetes-sigs/kueue/pull/7489.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-04T10:42:42Z

I have gone half through, the patterns seem to repeat. I will finish the entire review when we have an alignment on addressing the comments.

### Comment by [@kannon92](https://github.com/kannon92) — 2025-11-04T21:49:54Z

@mimowo @tenzen-y I got all the test failures fixed.

PTAL.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-05T19:48:15Z

LGTM label has been added.  <details>Git tree hash: 0e27d9e6567f4920acc1dc80894d9664ce13cd67</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-05T19:48:15Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7488#" title="Author self-approved">kannon92</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7488#pullrequestreview-3424028425" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-04T10:17:49Z

- **apis/kueue/v1beta2/admissioncheck_types.go:69** by [@mimowo](https://github.com/mimowo) — 2025-11-04T10:17:49Z
```diff
@@ -62,17 +62,23 @@ type AdmissionCheckSpec struct {
 
 type AdmissionCheckParametersReference struct {
 	// apiGroup is the group for the resource being referenced.
+	// +required
 	// +kubebuilder:validation:MaxLength=253
+	// +kubebuilder:validation:MinLength=1
 	// +kubebuilder:validation:Pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$"
-	APIGroup string `json:"apiGroup"`
+	APIGroup string `json:"apiGroup,omitempty"`
```

  Why adding omitempty here, since MinLength is 1? (also for Kind and Name fields) .

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-04T10:19:06Z

- **apis/kueue/v1beta2/admissioncheck_types.go:119** by [@mimowo](https://github.com/mimowo) — 2025-11-04T10:19:06Z
```diff
@@ -105,13 +111,16 @@ const (
 type AdmissionCheck struct {
 	metav1.TypeMeta `json:",inline"`
 	// metadata is the metadata of the AdmissionCheck.
+	// +optional
 	metav1.ObjectMeta `json:"metadata,omitempty"`
 
 	// spec is the specification of the AdmissionCheck.
-	Spec AdmissionCheckSpec `json:"spec,omitempty"`
+	// +optional
+	Spec AdmissionCheckSpec `json:"spec"` //nolint:kubeapilinter // spec should not be a pointer
```

  THB making spec optional looks weird to my, why not required? I've never seen an AdmissionCheck without spec, and it feels more would require it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-04T10:24:39Z

- **apis/kueue/v1beta2/clusterqueue_types.go:161** by [@mimowo](https://github.com/mimowo) — 2025-11-04T10:24:39Z
```diff
@@ -150,14 +156,16 @@ type AdmissionChecksStrategy struct {
 	// admissionChecks is a list of strategies for AdmissionChecks
 	// +listType=map
 	// +listMapKey=name
+	// +required
 	// +kubebuilder:validation:MaxItems=64
```

  Let's also add `+kubebuilder:validation:MinItems=1`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-04T10:30:37Z

- **apis/kueue/v1beta2/clusterqueue_types.go:204** by [@mimowo](https://github.com/mimowo) — 2025-11-04T10:30:37Z
```diff
@@ -191,7 +199,8 @@ type ResourceGroup struct {
 	// +kubebuilder:validation:MinItems=1
 	// +kubebuilder:validation:MaxItems=64
 	// +listType=atomic
-	CoveredResources []corev1.ResourceName `json:"coveredResources"`
+	// +required
+	CoveredResources []corev1.ResourceName `json:"coveredResources,omitempty"`
```

  omitempty looks weird for required values, especially when we have `MinItems=1`? Did you add it because the linter complained? TBH I would rather add a comment to disable linter (same below for the Flavors field).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-04T10:30:53Z

- **apis/kueue/v1beta2/clusterqueue_types.go:236** by [@mimowo](https://github.com/mimowo) — 2025-11-04T10:30:53Z
```diff
@@ -202,30 +211,34 @@ type ResourceGroup struct {
 	// The list cannot be empty and it can contain up to 64 flavors, with a max of
 	// 256 total flavors across all resource groups in the ClusterQueue.
 	// +listType=map
+	// +required
 	// +listMapKey=name
 	// +kubebuilder:validation:MinItems=1
 	// +kubebuilder:validation:MaxItems=64
-	Flavors []FlavorQuotas `json:"flavors"`
+	Flavors []FlavorQuotas `json:"flavors,omitempty"`
 }
 
 type FlavorQuotas struct {
 	// name of this flavor. The name should match the .metadata.name of a
 	// ResourceFlavor. If a matching ResourceFlavor does not exist, the
 	// ClusterQueue will have an Active condition set to False.
-	Name ResourceFlavorReference `json:"name"`
+	// +required
+	Name ResourceFlavorReference `json:"name"` //nolint:kubeapilinter // should not be a pointer
 
 	// resources is the list of quotas for this flavor per resource.
 	// There could be up to 64 resources.
 	// +listType=map
 	// +listMapKey=name
 	// +kubebuilder:validation:MinItems=1
 	// +kubebuilder:validation:MaxItems=64
-	Resources []ResourceQuota `json:"resources"`
+	// +required
+	Resources []ResourceQuota `json:"resources,omitempty"`
```

  same, why omitempty? And many other places to review like this

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-04T10:32:54Z

- **apis/kueue/v1beta2/clusterqueue_types.go:364** by [@mimowo](https://github.com/mimowo) — 2025-11-04T10:32:54Z
```diff
@@ -324,25 +338,30 @@ type ClusterQueueStatus struct {
 
 type FlavorUsage struct {
 	// name of the flavor.
-	Name ResourceFlavorReference `json:"name"`
+	// +required
+	Name ResourceFlavorReference `json:"name,omitempty"` //nolint:kubeapilinter // should not be a pointer
 
 	// resources lists the quota usage for the resources in this flavor.
 	// +listType=map
 	// +listMapKey=name
 	// +kubebuilder:validation:MaxItems=64
-	Resources []ResourceUsage `json:"resources"`
+	// +required
+	Resources []ResourceUsage `json:"resources,omitempty"`
 }
 
 type ResourceUsage struct {
 	// name of the resource
-	Name corev1.ResourceName `json:"name"`
+	// +required
+	Name corev1.ResourceName `json:"name,omitempty"`
 
 	// total is the total quantity of used quota, including the amount borrowed
 	// from the cohort.
+	// +required
 	Total resource.Quantity `json:"total,omitempty"`
 
 	// borrowed is quantity of quota that is borrowed from the cohort. In other
 	// words, it's the used quota that is over the nominalQuota.
+	// +required
```

  why not optional? If we are not Borrowing why to specifiy?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-04T10:33:02Z

- **apis/kueue/v1beta2/clusterqueue_types.go:359** by [@mimowo](https://github.com/mimowo) — 2025-11-04T10:33:02Z
```diff
@@ -324,25 +338,30 @@ type ClusterQueueStatus struct {
 
 type FlavorUsage struct {
 	// name of the flavor.
-	Name ResourceFlavorReference `json:"name"`
+	// +required
+	Name ResourceFlavorReference `json:"name,omitempty"` //nolint:kubeapilinter // should not be a pointer
 
 	// resources lists the quota usage for the resources in this flavor.
 	// +listType=map
 	// +listMapKey=name
 	// +kubebuilder:validation:MaxItems=64
-	Resources []ResourceUsage `json:"resources"`
+	// +required
+	Resources []ResourceUsage `json:"resources,omitempty"`
 }
 
 type ResourceUsage struct {
 	// name of the resource
-	Name corev1.ResourceName `json:"name"`
+	// +required
+	Name corev1.ResourceName `json:"name,omitempty"`
 
 	// total is the total quantity of used quota, including the amount borrowed
 	// from the cohort.
+	// +required
```

  I would say optional.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-04T10:34:07Z

- **apis/kueue/v1beta2/clusterqueue_types.go:542** by [@mimowo](https://github.com/mimowo) — 2025-11-04T10:34:07Z
```diff
@@ -509,12 +533,15 @@ type BorrowWithinCohort struct {
 type ClusterQueue struct {
 	metav1.TypeMeta `json:",inline"`
 	// metadata is the metadata of the ClusterQueue.
+	// +optional
 	metav1.ObjectMeta `json:"metadata,omitempty"`
 
 	// spec is the specification of the ClusterQueue.
-	Spec ClusterQueueSpec `json:"spec,omitempty"`
+	// +optional
+	Spec ClusterQueueSpec `json:"spec,omitempty"` //nolint:kubeapilinter // spec should not be a pointer
```

  I think spec for ClusterQueue should be required, never seen CQ without spec. It does not seem to have a functional meaning.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-04T10:37:13Z

- **apis/kueue/v1beta2/fairsharing_types.go:85** by [@mimowo](https://github.com/mimowo) — 2025-11-04T10:37:13Z
```diff
@@ -80,7 +82,7 @@ type AdmissionScope struct {
 	//
 	// +kubebuilder:validation:Enum=UsageBasedAdmissionFairSharing;NoAdmissionFairSharing
 	// +required
-	AdmissionMode AdmissionMode `json:"admissionMode"`
+	AdmissionMode AdmissionMode `json:"admissionMode,omitempty"`
```

  It cannot be empty, because the enum validation doesn't allow, I would just add a comment.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-04T12:02:46Z

- **apis/kueue/v1beta2/admissioncheck_types.go:119** by [@kannon92](https://github.com/kannon92) — 2025-11-04T12:02:46Z
```diff
@@ -105,13 +111,16 @@ const (
 type AdmissionCheck struct {
 	metav1.TypeMeta `json:",inline"`
 	// metadata is the metadata of the AdmissionCheck.
+	// +optional
 	metav1.ObjectMeta `json:"metadata,omitempty"`
 
 	// spec is the specification of the AdmissionCheck.
-	Spec AdmissionCheckSpec `json:"spec,omitempty"`
+	// +optional
+	Spec AdmissionCheckSpec `json:"spec"` //nolint:kubeapilinter // spec should not be a pointer
```

  I was mostly grasping at straws on fixing the tests. I can see about making these required again.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-04T12:03:07Z

- **apis/kueue/v1beta2/clusterqueue_types.go:161** by [@kannon92](https://github.com/kannon92) — 2025-11-04T12:03:07Z
```diff
@@ -150,14 +156,16 @@ type AdmissionChecksStrategy struct {
 	// admissionChecks is a list of strategies for AdmissionChecks
 	// +listType=map
 	// +listMapKey=name
+	// +required
 	// +kubebuilder:validation:MaxItems=64
```

  That broke some of the tests it seems.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-04T12:03:30Z

- **apis/kueue/v1beta2/clusterqueue_types.go:236** by [@kannon92](https://github.com/kannon92) — 2025-11-04T12:03:30Z
```diff
@@ -202,30 +211,34 @@ type ResourceGroup struct {
 	// The list cannot be empty and it can contain up to 64 flavors, with a max of
 	// 256 total flavors across all resource groups in the ClusterQueue.
 	// +listType=map
+	// +required
 	// +listMapKey=name
 	// +kubebuilder:validation:MinItems=1
 	// +kubebuilder:validation:MaxItems=64
-	Flavors []FlavorQuotas `json:"flavors"`
+	Flavors []FlavorQuotas `json:"flavors,omitempty"`
 }
 
 type FlavorQuotas struct {
 	// name of this flavor. The name should match the .metadata.name of a
 	// ResourceFlavor. If a matching ResourceFlavor does not exist, the
 	// ClusterQueue will have an Active condition set to False.
-	Name ResourceFlavorReference `json:"name"`
+	// +required
+	Name ResourceFlavorReference `json:"name"` //nolint:kubeapilinter // should not be a pointer
 
 	// resources is the list of quotas for this flavor per resource.
 	// There could be up to 64 resources.
 	// +listType=map
 	// +listMapKey=name
 	// +kubebuilder:validation:MinItems=1
 	// +kubebuilder:validation:MaxItems=64
-	Resources []ResourceQuota `json:"resources"`
+	// +required
+	Resources []ResourceQuota `json:"resources,omitempty"`
```

  The linter says to add it or it fails the check.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-04T12:05:23Z

- **apis/kueue/v1beta2/clusterqueue_types.go:204** by [@kannon92](https://github.com/kannon92) — 2025-11-04T12:05:23Z
```diff
@@ -191,7 +199,8 @@ type ResourceGroup struct {
 	// +kubebuilder:validation:MinItems=1
 	// +kubebuilder:validation:MaxItems=64
 	// +listType=atomic
-	CoveredResources []corev1.ResourceName `json:"coveredResources"`
+	// +required
+	CoveredResources []corev1.ResourceName `json:"coveredResources,omitempty"`
```

  Yes the linter requires it.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-04T16:09:24Z

- **apis/kueue/v1beta2/admissioncheck_types.go:69** by [@kannon92](https://github.com/kannon92) — 2025-11-04T16:09:23Z
```diff
@@ -62,17 +62,23 @@ type AdmissionCheckSpec struct {
 
 type AdmissionCheckParametersReference struct {
 	// apiGroup is the group for the resource being referenced.
+	// +required
 	// +kubebuilder:validation:MaxLength=253
+	// +kubebuilder:validation:MinLength=1
 	// +kubebuilder:validation:Pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$"
-	APIGroup string `json:"apiGroup"`
+	APIGroup string `json:"apiGroup,omitempty"`
```

  ```
  apis/kueue/v1beta2/admissioncheck_types.go:69:2: requiredfields: field APIGroup does not allow the zero value. It must have the omitempty tag. (kubeapilinter)
          APIGroup string `json:"apiGroup"`
  ```

### Review by [@everettraven](https://github.com/everettraven) (commented) — 2025-11-04T16:51:16Z

- **apis/kueue/v1beta2/admissioncheck_types.go:69** by [@everettraven](https://github.com/everettraven) — 2025-11-04T16:51:16Z
```diff
@@ -62,17 +62,23 @@ type AdmissionCheckSpec struct {
 
 type AdmissionCheckParametersReference struct {
 	// apiGroup is the group for the resource being referenced.
+	// +required
 	// +kubebuilder:validation:MaxLength=253
+	// +kubebuilder:validation:MinLength=1
 	// +kubebuilder:validation:Pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$"
-	APIGroup string `json:"apiGroup"`
+	APIGroup string `json:"apiGroup,omitempty"`
```

  It is to prevent serialization of the empty string when using a structured client.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-04T16:57:43Z

- **apis/kueue/v1beta2/admissioncheck_types.go:69** by [@mimowo](https://github.com/mimowo) — 2025-11-04T16:57:43Z
```diff
@@ -62,17 +62,23 @@ type AdmissionCheckSpec struct {
 
 type AdmissionCheckParametersReference struct {
 	// apiGroup is the group for the resource being referenced.
+	// +required
 	// +kubebuilder:validation:MaxLength=253
+	// +kubebuilder:validation:MinLength=1
 	// +kubebuilder:validation:Pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$"
-	APIGroup string `json:"apiGroup"`
+	APIGroup string `json:"apiGroup,omitempty"`
```

  Hm, but why would it matter, empty string is not an allowed value anyway because of the MinLength=1validation. 
  
  Setting required seems to reflect the intention better than optional. However I'm not an API expert so fine if this is the preferred way, it just looks surprising so I'm trying to understand a bit deeper.

### Review by [@JoelSpeed](https://github.com/JoelSpeed) (commented) — 2025-11-04T17:24:43Z

- **apis/kueue/v1beta2/admissioncheck_types.go:69** by [@JoelSpeed](https://github.com/JoelSpeed) — 2025-11-04T17:24:43Z
```diff
@@ -62,17 +62,23 @@ type AdmissionCheckSpec struct {
 
 type AdmissionCheckParametersReference struct {
 	// apiGroup is the group for the resource being referenced.
+	// +required
 	// +kubebuilder:validation:MaxLength=253
+	// +kubebuilder:validation:MinLength=1
 	// +kubebuilder:validation:Pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$"
-	APIGroup string `json:"apiGroup"`
+	APIGroup string `json:"apiGroup,omitempty"`
```

  It changes the error that gets returned.
  
  If you have `omitempty` here and the user doesn't set this to a non-empty string, then they get ~
  ```
  apiGroup is required
  ```
  If you _do not_ have `omitempty` here and the user doesn't set this to a non-empty string, then they get ~
  ```
  apiGroup must have length at least 1
  ```
  
  So by adding omitempty, the theory is that the error returned is more accurate for a user who has set no explicit opinion in their Go object

### Review by [@JoelSpeed](https://github.com/JoelSpeed) (commented) — 2025-11-04T17:49:23Z

- **apis/kueue/v1beta2/admissioncheck_types.go:119** by [@JoelSpeed](https://github.com/JoelSpeed) — 2025-11-04T17:49:23Z
```diff
@@ -105,13 +111,16 @@ const (
 type AdmissionCheck struct {
 	metav1.TypeMeta `json:",inline"`
 	// metadata is the metadata of the AdmissionCheck.
+	// +optional
 	metav1.ObjectMeta `json:"metadata,omitempty"`
 
 	// spec is the specification of the AdmissionCheck.
-	Spec AdmissionCheckSpec `json:"spec,omitempty"`
+	// +optional
+	Spec AdmissionCheckSpec `json:"spec"` //nolint:kubeapilinter // spec should not be a pointer
```

  What happens if you create an AdmissionCheck without a spec today? Do things actually blow up?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-04T17:53:58Z

- **apis/kueue/v1beta2/admissioncheck_types.go:119** by [@kannon92](https://github.com/kannon92) — 2025-11-04T17:53:58Z
```diff
@@ -105,13 +111,16 @@ const (
 type AdmissionCheck struct {
 	metav1.TypeMeta `json:",inline"`
 	// metadata is the metadata of the AdmissionCheck.
+	// +optional
 	metav1.ObjectMeta `json:"metadata,omitempty"`
 
 	// spec is the specification of the AdmissionCheck.
-	Spec AdmissionCheckSpec `json:"spec,omitempty"`
+	// +optional
+	Spec AdmissionCheckSpec `json:"spec"` //nolint:kubeapilinter // spec should not be a pointer
```

  I think most of our specs for v1beta1 are actually optional. So it is possible to create them with empty spec but I don't think that is a valid use case.
  
  I do know of a user that sets ClusterQueue with no spec but its not necessary something I would encourage.
  
  I originally shifted to optional so that I don't break any existing tests. But I wasn't entirely sure on all the failures.
  
  It turns out that omitempty was causing some problems in our integration tests and not anything to do with spec being required or optional.

### Review by [@JoelSpeed](https://github.com/JoelSpeed) (commented) — 2025-11-04T17:55:12Z

- **apis/kueue/v1beta2/clusterqueue_types.go:204** by [@JoelSpeed](https://github.com/JoelSpeed) — 2025-11-04T17:55:12Z
```diff
@@ -191,7 +199,8 @@ type ResourceGroup struct {
 	// +kubebuilder:validation:MinItems=1
 	// +kubebuilder:validation:MaxItems=64
 	// +listType=atomic
-	CoveredResources []corev1.ResourceName `json:"coveredResources"`
+	// +required
+	CoveredResources []corev1.ResourceName `json:"coveredResources,omitempty"`
```

  Similar story to https://github.com/kubernetes-sigs/kueue/pull/7488/files#r2491431494
  
  This is inline with current API conventions if you review the section about optional and required field serialization

### Review by [@JoelSpeed](https://github.com/JoelSpeed) (commented) — 2025-11-04T17:56:52Z

- **apis/kueue/v1beta2/admissioncheck_types.go:119** by [@JoelSpeed](https://github.com/JoelSpeed) — 2025-11-04T17:56:51Z
```diff
@@ -105,13 +111,16 @@ const (
 type AdmissionCheck struct {
 	metav1.TypeMeta `json:",inline"`
 	// metadata is the metadata of the AdmissionCheck.
+	// +optional
 	metav1.ObjectMeta `json:"metadata,omitempty"`
 
 	// spec is the specification of the AdmissionCheck.
-	Spec AdmissionCheckSpec `json:"spec,omitempty"`
+	// +optional
+	Spec AdmissionCheckSpec `json:"spec"` //nolint:kubeapilinter // spec should not be a pointer
```

  What's in the generated schema? Is this marked required today? If it's marked required I wouldn't change it

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-04T17:59:10Z

- **apis/kueue/v1beta2/admissioncheck_types.go:69** by [@mimowo](https://github.com/mimowo) — 2025-11-04T17:59:09Z
```diff
@@ -62,17 +62,23 @@ type AdmissionCheckSpec struct {
 
 type AdmissionCheckParametersReference struct {
 	// apiGroup is the group for the resource being referenced.
+	// +required
 	// +kubebuilder:validation:MaxLength=253
+	// +kubebuilder:validation:MinLength=1
 	// +kubebuilder:validation:Pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$"
-	APIGroup string `json:"apiGroup"`
+	APIGroup string `json:"apiGroup,omitempty"`
```

  Thank you for explaining 👍 This makes sense even though the difference is subtle.
  
  So +1 to use `omitempty` in those scenarios (applies to also other comments of mine which might have suggested to drop them).

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-04T18:05:59Z

- **apis/kueue/v1beta2/admissioncheck_types.go:119** by [@kannon92](https://github.com/kannon92) — 2025-11-04T18:05:58Z
```diff
@@ -105,13 +111,16 @@ const (
 type AdmissionCheck struct {
 	metav1.TypeMeta `json:",inline"`
 	// metadata is the metadata of the AdmissionCheck.
+	// +optional
 	metav1.ObjectMeta `json:"metadata,omitempty"`
 
 	// spec is the specification of the AdmissionCheck.
-	Spec AdmissionCheckSpec `json:"spec,omitempty"`
+	// +optional
+	Spec AdmissionCheckSpec `json:"spec"` //nolint:kubeapilinter // spec should not be a pointer
```

  Its not marked as either required or optional so I think it goes to optional.
  
  @mimowo @tenzen-y
  for v1beta2 do we want to keep spec as optional for objects? It is a breaking change.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-04T18:10:34Z

- **apis/kueue/v1beta2/admissioncheck_types.go:119** by [@mimowo](https://github.com/mimowo) — 2025-11-04T18:10:34Z
```diff
@@ -105,13 +111,16 @@ const (
 type AdmissionCheck struct {
 	metav1.TypeMeta `json:",inline"`
 	// metadata is the metadata of the AdmissionCheck.
+	// +optional
 	metav1.ObjectMeta `json:"metadata,omitempty"`
 
 	// spec is the specification of the AdmissionCheck.
-	Spec AdmissionCheckSpec `json:"spec,omitempty"`
+	// +optional
+	Spec AdmissionCheckSpec `json:"spec"` //nolint:kubeapilinter // spec should not be a pointer
```

  > I think most of our specs for v1beta1 are actually optional. So it is possible to create them with empty spec but I don't think that is a valid use case.
  
  This matches my understanding - technically we currently allow empty spec, but it is more of omission when designing the API rather than deliberate decision to support a use-case. Even if things don't blow up I don't think we have any integration or e2e tests for empty specs (maybe we have some unit tests).
  
  > I do know of a user that sets ClusterQueue with no spec but its not necessary something I would encourage.
  
  Interesting, even if we don't know the use case it would be a pity to break the user :(.
  
  > What's in the generated schema? Is this marked required today? If it's marked required I wouldn't change it
  
  I think schema allows :( 
  
  In Kueue we are now graduating v1beta1 to v1beta2 as part of the next 0.15 release, thus we are re-visiting the APIs. So, since this is going to be a new API strengthening validation seems sensible to consider.
  
  However, I agree it might not be worth it, especially if some users already use empty spec (as mentioned).

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-04T19:45:46Z

- **apis/kueue/v1beta2/clusterqueue_types.go:542** by [@kannon92](https://github.com/kannon92) — 2025-11-04T19:45:46Z
```diff
@@ -509,12 +533,15 @@ type BorrowWithinCohort struct {
 type ClusterQueue struct {
 	metav1.TypeMeta `json:",inline"`
 	// metadata is the metadata of the ClusterQueue.
+	// +optional
 	metav1.ObjectMeta `json:"metadata,omitempty"`
 
 	// spec is the specification of the ClusterQueue.
-	Spec ClusterQueueSpec `json:"spec,omitempty"`
+	// +optional
+	Spec ClusterQueueSpec `json:"spec,omitempty"` //nolint:kubeapilinter // spec should not be a pointer
```

  Leaving specs as optional.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-04T19:46:33Z

- **apis/kueue/v1beta2/clusterqueue_types.go:236** by [@kannon92](https://github.com/kannon92) — 2025-11-04T19:46:33Z
```diff
@@ -202,30 +211,34 @@ type ResourceGroup struct {
 	// The list cannot be empty and it can contain up to 64 flavors, with a max of
 	// 256 total flavors across all resource groups in the ClusterQueue.
 	// +listType=map
+	// +required
 	// +listMapKey=name
 	// +kubebuilder:validation:MinItems=1
 	// +kubebuilder:validation:MaxItems=64
-	Flavors []FlavorQuotas `json:"flavors"`
+	Flavors []FlavorQuotas `json:"flavors,omitempty"`
 }
 
 type FlavorQuotas struct {
 	// name of this flavor. The name should match the .metadata.name of a
 	// ResourceFlavor. If a matching ResourceFlavor does not exist, the
 	// ClusterQueue will have an Active condition set to False.
-	Name ResourceFlavorReference `json:"name"`
+	// +required
+	Name ResourceFlavorReference `json:"name"` //nolint:kubeapilinter // should not be a pointer
 
 	// resources is the list of quotas for this flavor per resource.
 	// There could be up to 64 resources.
 	// +listType=map
 	// +listMapKey=name
 	// +kubebuilder:validation:MinItems=1
 	// +kubebuilder:validation:MaxItems=64
-	Resources []ResourceQuota `json:"resources"`
+	// +required
+	Resources []ResourceQuota `json:"resources,omitempty"`
```

  resolving.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-04T21:44:37Z

- **apis/kueue/v1beta2/clusterqueue_types.go:359** by [@kannon92](https://github.com/kannon92) — 2025-11-04T21:44:37Z
```diff
@@ -324,25 +338,30 @@ type ClusterQueueStatus struct {
 
 type FlavorUsage struct {
 	// name of the flavor.
-	Name ResourceFlavorReference `json:"name"`
+	// +required
+	Name ResourceFlavorReference `json:"name,omitempty"` //nolint:kubeapilinter // should not be a pointer
 
 	// resources lists the quota usage for the resources in this flavor.
 	// +listType=map
 	// +listMapKey=name
 	// +kubebuilder:validation:MaxItems=64
-	Resources []ResourceUsage `json:"resources"`
+	// +required
+	Resources []ResourceUsage `json:"resources,omitempty"`
 }
 
 type ResourceUsage struct {
 	// name of the resource
-	Name corev1.ResourceName `json:"name"`
+	// +required
+	Name corev1.ResourceName `json:"name,omitempty"`
 
 	// total is the total quantity of used quota, including the amount borrowed
 	// from the cohort.
+	// +required
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-04T21:44:45Z

- **apis/kueue/v1beta2/clusterqueue_types.go:364** by [@kannon92](https://github.com/kannon92) — 2025-11-04T21:44:45Z
```diff
@@ -324,25 +338,30 @@ type ClusterQueueStatus struct {
 
 type FlavorUsage struct {
 	// name of the flavor.
-	Name ResourceFlavorReference `json:"name"`
+	// +required
+	Name ResourceFlavorReference `json:"name,omitempty"` //nolint:kubeapilinter // should not be a pointer
 
 	// resources lists the quota usage for the resources in this flavor.
 	// +listType=map
 	// +listMapKey=name
 	// +kubebuilder:validation:MaxItems=64
-	Resources []ResourceUsage `json:"resources"`
+	// +required
+	Resources []ResourceUsage `json:"resources,omitempty"`
 }
 
 type ResourceUsage struct {
 	// name of the resource
-	Name corev1.ResourceName `json:"name"`
+	// +required
+	Name corev1.ResourceName `json:"name,omitempty"`
 
 	// total is the total quantity of used quota, including the amount borrowed
 	// from the cohort.
+	// +required
 	Total resource.Quantity `json:"total,omitempty"`
 
 	// borrowed is quantity of quota that is borrowed from the cohort. In other
 	// words, it's the used quota that is over the nominalQuota.
+	// +required
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-04T21:45:06Z

- **apis/kueue/v1beta2/admissioncheck_types.go:119** by [@kannon92](https://github.com/kannon92) — 2025-11-04T21:45:06Z
```diff
@@ -105,13 +111,16 @@ const (
 type AdmissionCheck struct {
 	metav1.TypeMeta `json:",inline"`
 	// metadata is the metadata of the AdmissionCheck.
+	// +optional
 	metav1.ObjectMeta `json:"metadata,omitempty"`
 
 	// spec is the specification of the AdmissionCheck.
-	Spec AdmissionCheckSpec `json:"spec,omitempty"`
+	// +optional
+	Spec AdmissionCheckSpec `json:"spec"` //nolint:kubeapilinter // spec should not be a pointer
```

  Going to leave them as optional.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-05T08:44:01Z

- **apis/kueue/v1beta2/clusterqueue_types.go:161** by [@mimowo](https://github.com/mimowo) — 2025-11-05T08:44:01Z
```diff
@@ -150,14 +156,16 @@ type AdmissionChecksStrategy struct {
 	// admissionChecks is a list of strategies for AdmissionChecks
 	// +listType=map
 	// +listMapKey=name
+	// +required
 	// +kubebuilder:validation:MaxItems=64
```

  Unit tests only, or also integration or e2e? 
  
  We may revisit that for a follow up then. CQ without any declared resources looks useless.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-05T08:46:21Z

- **apis/kueue/v1beta2/localqueue_types.go:143** by [@mimowo](https://github.com/mimowo) — 2025-11-05T08:46:21Z
```diff
@@ -130,20 +129,26 @@ const (
 
 type LocalQueueFlavorUsage struct {
 	// name of the flavor.
-	Name ResourceFlavorReference `json:"name"`
+	// +required
+	Name ResourceFlavorReference `json:"name"` //nolint:kubeapilinter // should not be a pointer
 
 	// resources lists the quota usage for the resources in this flavor.
 	// +listType=map
 	// +listMapKey=name
 	// +kubebuilder:validation:MaxItems=16
-	Resources []LocalQueueResourceUsage `json:"resources"`
+	// +required
+	Resources []LocalQueueResourceUsage `json:"resources,omitempty"`
 }
 
+// +required
```

  What is the meaning of this - looks like a typo?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-05T08:47:41Z

- **apis/kueue/v1beta2/multikueue_types.go:68** by [@mimowo](https://github.com/mimowo) — 2025-11-05T08:47:41Z
```diff
@@ -50,18 +50,22 @@ type KubeConfig struct {
 	// If LocationType is Secret then Location is the name of the secret inside the namespace in
 	// which the kueue controller manager is running. The config should be stored in the "kubeconfig" key.
 	// +kubebuilder:validation:MaxLength=256
-	Location string `json:"location"`
+	// +kubebuilder:validation:MinLength=1
+	// +required
+	Location string `json:"location,omitempty"`
 
 	// locationType of the KubeConfig.
 	//
 	// +kubebuilder:default=Secret
+	// +optional
 	// +kubebuilder:validation:Enum=Secret;Path
-	LocationType LocationType `json:"locationType"`
+	LocationType LocationType `json:"locationType"` //nolint:kubeapilinter // should not be a pointer
 }
 
 type MultiKueueClusterSpec struct {
 	// kubeConfig is information on how to connect to the cluster.
-	KubeConfig KubeConfig `json:"kubeConfig"`
+	// +required
+	KubeConfig KubeConfig `json:"kubeConfig,omitzero,omitempty"`
```

  What is the meaning of omitzero, and why adding here?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-05T08:47:56Z

- **apis/kueue/v1beta2/multikueue_types.go:101** by [@mimowo](https://github.com/mimowo) — 2025-11-05T08:47:56Z
```diff
@@ -89,13 +93,16 @@ type MultiKueueClusterStatus struct {
 type MultiKueueCluster struct {
 	metav1.TypeMeta `json:",inline"`
 	// metadata is the metadata of the MultiKueueCluster.
+	// +optional
 	metav1.ObjectMeta `json:"metadata,omitempty"`
 
 	// spec is the specification of the MultiKueueCluster.
-	Spec MultiKueueClusterSpec `json:"spec,omitempty"`
+	// +optional
+	Spec MultiKueueClusterSpec `json:"spec,omitempty,omitzero"` //nolint:kubeapilinter // spec should not be a pointer
```

  Why adding omitzero here, but not for ClusterQueueSpec?

### Review by [@JoelSpeed](https://github.com/JoelSpeed) (commented) — 2025-11-05T08:57:59Z

- **config/components/crd/bases/kueue.x-k8s.io_clusterqueues.yaml:876** by [@JoelSpeed](https://github.com/JoelSpeed) — 2025-11-05T08:57:59Z
```diff
@@ -869,6 +871,8 @@ spec:
                     x-kubernetes-list-map-keys:
                     - name
                     x-kubernetes-list-type: map
+                required:
+                - admissionChecks
```

  Breaking change that doesn't ratchet, are you sure you want to do this?

### Review by [@JoelSpeed](https://github.com/JoelSpeed) (commented) — 2025-11-05T08:58:20Z

- **config/components/crd/bases/kueue.x-k8s.io_localqueues.yaml:660** by [@JoelSpeed](https://github.com/JoelSpeed) — 2025-11-05T08:58:20Z
```diff
@@ -657,6 +657,7 @@ spec:
                             x-kubernetes-int-or-string: true
                         required:
                         - name
+                        - total
```

  Breaking change that doesn't ratchet, are you sure you want to do this?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-05T09:08:56Z

- **apis/kueue/v1beta2/clusterqueue_types.go:161** by [@mimowo](https://github.com/mimowo) — 2025-11-05T09:08:56Z
```diff
@@ -150,14 +156,16 @@ type AdmissionChecksStrategy struct {
 	// admissionChecks is a list of strategies for AdmissionChecks
 	// +listType=map
 	// +listMapKey=name
+	// +required
 	// +kubebuilder:validation:MaxItems=64
```

  `AdmissionChecks` is the only field under `AdmissionChecksStrategy`, and so if `AdmissionChecks` is not specified, then setting `AdmissionChecksStrategy` is useless.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-05T09:10:59Z

- **config/components/crd/bases/kueue.x-k8s.io_clusterqueues.yaml:876** by [@mimowo](https://github.com/mimowo) — 2025-11-05T09:10:59Z
```diff
@@ -869,6 +871,8 @@ spec:
                     x-kubernetes-list-map-keys:
                     - name
                     x-kubernetes-list-type: map
+                required:
+                - admissionChecks
```

  I think this is related to here: https://github.com/kubernetes-sigs/kueue/pull/7488/files#r2489782960
  
  I think this is a desired strenghtening of the checks. Specifying AdmissionCheckStrategy without AdmissionChecks is useless (this is the only sub-field).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-05T09:13:09Z

- **config/components/crd/bases/kueue.x-k8s.io_localqueues.yaml:660** by [@mimowo](https://github.com/mimowo) — 2025-11-05T09:13:09Z
```diff
@@ -657,6 +657,7 @@ spec:
                             x-kubernetes-int-or-string: true
                         required:
                         - name
+                        - total
```

  This is just status - not supplied by users, so I'm ok to keep as optional as before - we don't need to be proactive with good messages here (I assume).

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-05T16:18:31Z

- **apis/kueue/v1beta2/multikueue_types.go:68** by [@kannon92](https://github.com/kannon92) — 2025-11-05T16:18:31Z
```diff
@@ -50,18 +50,22 @@ type KubeConfig struct {
 	// If LocationType is Secret then Location is the name of the secret inside the namespace in
 	// which the kueue controller manager is running. The config should be stored in the "kubeconfig" key.
 	// +kubebuilder:validation:MaxLength=256
-	Location string `json:"location"`
+	// +kubebuilder:validation:MinLength=1
+	// +required
+	Location string `json:"location,omitempty"`
 
 	// locationType of the KubeConfig.
 	//
 	// +kubebuilder:default=Secret
+	// +optional
 	// +kubebuilder:validation:Enum=Secret;Path
-	LocationType LocationType `json:"locationType"`
+	LocationType LocationType `json:"locationType"` //nolint:kubeapilinter // should not be a pointer
 }
 
 type MultiKueueClusterSpec struct {
 	// kubeConfig is information on how to connect to the cluster.
-	KubeConfig KubeConfig `json:"kubeConfig"`
+	// +required
+	KubeConfig KubeConfig `json:"kubeConfig,omitzero,omitempty"`
```

  https://www.bytesizego.com/blog/go-124-omitzero

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-05T16:19:47Z

- **apis/kueue/v1beta2/multikueue_types.go:68** by [@kannon92](https://github.com/kannon92) — 2025-11-05T16:19:47Z
```diff
@@ -50,18 +50,22 @@ type KubeConfig struct {
 	// If LocationType is Secret then Location is the name of the secret inside the namespace in
 	// which the kueue controller manager is running. The config should be stored in the "kubeconfig" key.
 	// +kubebuilder:validation:MaxLength=256
-	Location string `json:"location"`
+	// +kubebuilder:validation:MinLength=1
+	// +required
+	Location string `json:"location,omitempty"`
 
 	// locationType of the KubeConfig.
 	//
 	// +kubebuilder:default=Secret
+	// +optional
 	// +kubebuilder:validation:Enum=Secret;Path
-	LocationType LocationType `json:"locationType"`
+	LocationType LocationType `json:"locationType"` //nolint:kubeapilinter // should not be a pointer
 }
 
 type MultiKueueClusterSpec struct {
 	// kubeConfig is information on how to connect to the cluster.
-	KubeConfig KubeConfig `json:"kubeConfig"`
+	// +required
+	KubeConfig KubeConfig `json:"kubeConfig,omitzero,omitempty"`
```

  ```
  INFO [runner] linters took 607.623113ms with stages: goanalysis_metalinter: 589.846035ms 
  apis/kueue/v1beta2/multikueue_types.go:68:2: requiredfields: field KubeConfig does not allow the zero value. It must have the omitzero tag. (kubeapilinter)
          KubeConfig KubeConfig `json:"kubeConfig,omitempty"`
          ^
  ```

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-05T16:59:56Z

- **apis/kueue/v1beta2/multikueue_types.go:101** by [@kannon92](https://github.com/kannon92) — 2025-11-05T16:59:56Z
```diff
@@ -89,13 +93,16 @@ type MultiKueueClusterStatus struct {
 type MultiKueueCluster struct {
 	metav1.TypeMeta `json:",inline"`
 	// metadata is the metadata of the MultiKueueCluster.
+	// +optional
 	metav1.ObjectMeta `json:"metadata,omitempty"`
 
 	// spec is the specification of the MultiKueueCluster.
-	Spec MultiKueueClusterSpec `json:"spec,omitempty"`
+	// +optional
+	Spec MultiKueueClusterSpec `json:"spec,omitempty,omitzero"` //nolint:kubeapilinter // spec should not be a pointer
```

  I think its because we have optional fields for ClusterQueueSpec while from MultiKueueClusterSpec we have a required field in the spec.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-05T17:01:48Z

- **apis/kueue/v1beta2/localqueue_types.go:143** by [@kannon92](https://github.com/kannon92) — 2025-11-05T17:01:48Z
```diff
@@ -130,20 +129,26 @@ const (
 
 type LocalQueueFlavorUsage struct {
 	// name of the flavor.
-	Name ResourceFlavorReference `json:"name"`
+	// +required
+	Name ResourceFlavorReference `json:"name"` //nolint:kubeapilinter // should not be a pointer
 
 	// resources lists the quota usage for the resources in this flavor.
 	// +listType=map
 	// +listMapKey=name
 	// +kubebuilder:validation:MaxItems=16
-	Resources []LocalQueueResourceUsage `json:"resources"`
+	// +required
+	Resources []LocalQueueResourceUsage `json:"resources,omitempty"`
 }
 
+// +required
```

  yes it is. I'll remove.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-05T17:05:13Z

- **config/components/crd/bases/kueue.x-k8s.io_localqueues.yaml:660** by [@kannon92](https://github.com/kannon92) — 2025-11-05T17:05:13Z
```diff
@@ -657,6 +657,7 @@ spec:
                             x-kubernetes-int-or-string: true
                         required:
                         - name
+                        - total
```

  I'll set this back to optional.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-05T17:42:07Z

- **config/components/crd/bases/kueue.x-k8s.io_localqueues.yaml:660** by [@mimowo](https://github.com/mimowo) — 2025-11-05T17:42:07Z
```diff
@@ -657,6 +657,7 @@ spec:
                             x-kubernetes-int-or-string: true
                         required:
                         - name
+                        - total
```

  sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-05T17:43:09Z

- **apis/kueue/v1beta2/multikueue_types.go:101** by [@mimowo](https://github.com/mimowo) — 2025-11-05T17:43:08Z
```diff
@@ -89,13 +93,16 @@ type MultiKueueClusterStatus struct {
 type MultiKueueCluster struct {
 	metav1.TypeMeta `json:",inline"`
 	// metadata is the metadata of the MultiKueueCluster.
+	// +optional
 	metav1.ObjectMeta `json:"metadata,omitempty"`
 
 	// spec is the specification of the MultiKueueCluster.
-	Spec MultiKueueClusterSpec `json:"spec,omitempty"`
+	// +optional
+	Spec MultiKueueClusterSpec `json:"spec,omitempty,omitzero"` //nolint:kubeapilinter // spec should not be a pointer
```

  ack

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-05T18:11:02Z

- **config/components/crd/bases/kueue.x-k8s.io_localqueues.yaml:660** by [@kannon92](https://github.com/kannon92) — 2025-11-05T18:11:02Z
```diff
@@ -657,6 +657,7 @@ spec:
                             x-kubernetes-int-or-string: true
                         required:
                         - name
+                        - total
```

  updated.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-05T18:11:38Z

- **apis/kueue/v1beta2/localqueue_types.go:143** by [@kannon92](https://github.com/kannon92) — 2025-11-05T18:11:38Z
```diff
@@ -130,20 +129,26 @@ const (
 
 type LocalQueueFlavorUsage struct {
 	// name of the flavor.
-	Name ResourceFlavorReference `json:"name"`
+	// +required
+	Name ResourceFlavorReference `json:"name"` //nolint:kubeapilinter // should not be a pointer
 
 	// resources lists the quota usage for the resources in this flavor.
 	// +listType=map
 	// +listMapKey=name
 	// +kubebuilder:validation:MaxItems=16
-	Resources []LocalQueueResourceUsage `json:"resources"`
+	// +required
+	Resources []LocalQueueResourceUsage `json:"resources,omitempty"`
 }
 
+// +required
```

  removed.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-05T18:11:42Z

- **apis/kueue/v1beta2/localqueue_types.go:143** by [@kannon92](https://github.com/kannon92) — 2025-11-05T18:11:42Z
```diff
@@ -130,20 +129,26 @@ const (
 
 type LocalQueueFlavorUsage struct {
 	// name of the flavor.
-	Name ResourceFlavorReference `json:"name"`
+	// +required
+	Name ResourceFlavorReference `json:"name"` //nolint:kubeapilinter // should not be a pointer
 
 	// resources lists the quota usage for the resources in this flavor.
 	// +listType=map
 	// +listMapKey=name
 	// +kubebuilder:validation:MaxItems=16
-	Resources []LocalQueueResourceUsage `json:"resources"`
+	// +required
+	Resources []LocalQueueResourceUsage `json:"resources,omitempty"`
 }
 
+// +required
```

  removed.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-05T18:12:16Z

- **apis/kueue/v1beta2/clusterqueue_types.go:161** by [@kannon92](https://github.com/kannon92) — 2025-11-05T18:12:16Z
```diff
@@ -150,14 +156,16 @@ type AdmissionChecksStrategy struct {
 	// admissionChecks is a list of strategies for AdmissionChecks
 	// +listType=map
 	// +listMapKey=name
+	// +required
 	// +kubebuilder:validation:MaxItems=64
```

  I pushed up this change. Let's see if CI passes. It may have been the omitempty I was fighting with earliery.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-05T19:48:08Z

Thank you for the effort. Having the linter checks helping to guide future reviews is super useful. 

/lgtm
/approve
