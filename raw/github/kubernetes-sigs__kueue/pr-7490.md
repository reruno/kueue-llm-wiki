# PR #7490: Remove deprecated AdmissionChecks field from v1beta2 ClusterQueue API

**Summary**: Remove deprecated AdmissionChecks field from v1beta2 ClusterQueue API

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7490

**Last updated**: 2025-11-13T06:52:59Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@nerdeveloper](https://github.com/nerdeveloper)
- **Branch**: `remove-admissionchecks-field-v1beta2` → `main`
- **Created**: 2025-11-01T17:03:51Z
- **Updated**: 2025-11-13T06:52:59Z
- **Merged**: 2025-11-06T09:44:54Z
- **Closed**: 2025-11-06T09:44:54Z
- **Labels**: `release-note`, `size/L`, `lgtm`, `approved`, `kind/api-change`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 17   **+72 / -106**

## Description

## Summary

- Removes the deprecated `.spec.admissionChecks` field from v1beta2 ClusterQueue API
- Completes the migration to `.spec.admissionChecksStrategy` for v1beta2 stabilization

Fixes #7362

## Changes

- **API**: Removed `AdmissionChecks` field from v1beta2 `ClusterQueueSpec`
- **Conversion**: Added bidirectional conversion logic between v1beta1 ↔ v1beta2 to preserve compatibility
- **Controllers**: Updated `admissioncheck_controller` and `workload_controller` to use `AdmissionChecksStrategy` exclusively
- **Webhooks**: Removed validation for the deprecated field
- **Tests**: Updated integration tests and test utilities to use the new field structure
- **Generated files**: Regenerated CRDs, client-go, deepcopy, conversion, and documentation

## Testing

- All unit tests pass (3499 tests)
- All linting checks pass
- `make verify` passes

```release-note
v1beta2: remove deprecated .spec.admissionChecks field from ClusterQueue API in favor of .spec.admissionChecksStrategy.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-11-01T17:03:56Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 67d27a5c6aab3bce42c3fe0d19d73fff5b15f8b7 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/690c681b56479000085bc6cd |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-01T17:04:00Z

Hi @nerdeveloper. Thanks for your PR.

I'm waiting for a [github.com](https://kubernetes-sigs/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@nerdeveloper](https://github.com/nerdeveloper) — 2025-11-01T17:06:34Z

/release-note

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-01T17:06:36Z

@nerdeveloper: the `/release-note` and `/release-note-action-required` commands have been deprecated.
Please edit the `release-note` block in the PR body text to include the release note. If the release note requires additional action include the string `action required` in the release note. For example:
````
```release-note
Some release note with action required.
```
````

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7490#issuecomment-3476563239):

>/release-note


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@kannon92](https://github.com/kannon92) — 2025-11-01T20:30:43Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-04T09:37:42Z

@nerdeveloper let us know if you have time to address the remarks. It will be great to have it this week to leave some buffer before the release.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-06T09:34:24Z

LGTM label has been added.  <details>Git tree hash: 5cd4fe3585a466ea3ef748252af22a7b11b668b1</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-06T09:34:24Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7490#pullrequestreview-3427172383" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7490#" title="Author self-approved">nerdeveloper</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-13T06:51:44Z

/kind cleanup

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-13T06:51:52Z

/kind api-change

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-13T06:52:57Z

/release-note-edit
```release-note
v1beta2: remove deprecated .spec.admissionChecks field from ClusterQueue API in favor of .spec.admissionChecksStrategy.
```

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-02T17:21:12Z

- **apis/kueue/v1beta1/clusterqueue_conversion.go:39** by [@kannon92](https://github.com/kannon92) — 2025-11-02T17:21:12Z
```diff
@@ -38,12 +38,56 @@ func (dst *ClusterQueue) ConvertFrom(srcRaw conversion.Hub) error {
 
 func Convert_v1beta1_ClusterQueueSpec_To_v1beta2_ClusterQueueSpec(in *ClusterQueueSpec, out *v1beta2.ClusterQueueSpec, s conversionapi.Scope) error {
```

  I know we have #7394 but maybe we could consider writing some unit tests for this conversion.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-03T08:06:27Z

- **apis/kueue/v1beta1/clusterqueue_conversion.go:58** by [@mimowo](https://github.com/mimowo) — 2025-11-03T08:06:27Z
```diff
@@ -38,12 +38,56 @@ func (dst *ClusterQueue) ConvertFrom(srcRaw conversion.Hub) error {
 
 func Convert_v1beta1_ClusterQueueSpec_To_v1beta2_ClusterQueueSpec(in *ClusterQueueSpec, out *v1beta2.ClusterQueueSpec, s conversionapi.Scope) error {
 	out.CohortName = v1beta2.CohortReference(in.Cohort)
-	return autoConvert_v1beta1_ClusterQueueSpec_To_v1beta2_ClusterQueueSpec(in, out, s)
+	if err := autoConvert_v1beta1_ClusterQueueSpec_To_v1beta2_ClusterQueueSpec(in, out, s); err != nil {
+		return err
+	}
+
+	// Convert AdmissionChecks to AdmissionChecksStrategy
+	// If v1beta1 has AdmissionChecks but v1beta2 doesn't have AdmissionChecksStrategy,
+	// convert the old field to the new structure
+	if len(in.AdmissionChecks) > 0 && out.AdmissionChecksStrategy == nil {
+		out.AdmissionChecksStrategy = &v1beta2.AdmissionChecksStrategy{
+			AdmissionChecks: make([]v1beta2.AdmissionCheckStrategyRule, len(in.AdmissionChecks)),
+		}
+		for i, checkRef := range in.AdmissionChecks {
+			out.AdmissionChecksStrategy.AdmissionChecks[i] = v1beta2.AdmissionCheckStrategyRule{
+				Name:      v1beta2.AdmissionCheckReference(checkRef),
+				OnFlavors: []v1beta2.ResourceFlavorReference{}, // Empty means all flavors
+			}
+		}
+	}
```

  I think you can move this block before calling `return autoConvert_v1beta1_ClusterQueueSpec_To_v1beta2_ClusterQueueSpec(in, out, s)`. Then no need to handle errors.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-03T08:07:58Z

- **apis/kueue/v1beta1/clusterqueue_conversion.go:55** by [@mimowo](https://github.com/mimowo) — 2025-11-03T08:07:58Z
```diff
@@ -38,12 +38,56 @@ func (dst *ClusterQueue) ConvertFrom(srcRaw conversion.Hub) error {
 
 func Convert_v1beta1_ClusterQueueSpec_To_v1beta2_ClusterQueueSpec(in *ClusterQueueSpec, out *v1beta2.ClusterQueueSpec, s conversionapi.Scope) error {
 	out.CohortName = v1beta2.CohortReference(in.Cohort)
-	return autoConvert_v1beta1_ClusterQueueSpec_To_v1beta2_ClusterQueueSpec(in, out, s)
+	if err := autoConvert_v1beta1_ClusterQueueSpec_To_v1beta2_ClusterQueueSpec(in, out, s); err != nil {
+		return err
+	}
+
+	// Convert AdmissionChecks to AdmissionChecksStrategy
+	// If v1beta1 has AdmissionChecks but v1beta2 doesn't have AdmissionChecksStrategy,
+	// convert the old field to the new structure
+	if len(in.AdmissionChecks) > 0 && out.AdmissionChecksStrategy == nil {
+		out.AdmissionChecksStrategy = &v1beta2.AdmissionChecksStrategy{
+			AdmissionChecks: make([]v1beta2.AdmissionCheckStrategyRule, len(in.AdmissionChecks)),
+		}
+		for i, checkRef := range in.AdmissionChecks {
+			out.AdmissionChecksStrategy.AdmissionChecks[i] = v1beta2.AdmissionCheckStrategyRule{
+				Name:      v1beta2.AdmissionCheckReference(checkRef),
+				OnFlavors: []v1beta2.ResourceFlavorReference{}, // Empty means all flavors
```

  Is this line needed? I would suppose leaving it blank should result in the same handling.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-03T08:08:38Z

- **apis/kueue/v1beta1/clusterqueue_conversion.go:65** by [@mimowo](https://github.com/mimowo) — 2025-11-03T08:08:38Z
```diff
@@ -38,12 +38,56 @@ func (dst *ClusterQueue) ConvertFrom(srcRaw conversion.Hub) error {
 
 func Convert_v1beta1_ClusterQueueSpec_To_v1beta2_ClusterQueueSpec(in *ClusterQueueSpec, out *v1beta2.ClusterQueueSpec, s conversionapi.Scope) error {
 	out.CohortName = v1beta2.CohortReference(in.Cohort)
-	return autoConvert_v1beta1_ClusterQueueSpec_To_v1beta2_ClusterQueueSpec(in, out, s)
+	if err := autoConvert_v1beta1_ClusterQueueSpec_To_v1beta2_ClusterQueueSpec(in, out, s); err != nil {
+		return err
+	}
+
+	// Convert AdmissionChecks to AdmissionChecksStrategy
+	// If v1beta1 has AdmissionChecks but v1beta2 doesn't have AdmissionChecksStrategy,
+	// convert the old field to the new structure
+	if len(in.AdmissionChecks) > 0 && out.AdmissionChecksStrategy == nil {
+		out.AdmissionChecksStrategy = &v1beta2.AdmissionChecksStrategy{
+			AdmissionChecks: make([]v1beta2.AdmissionCheckStrategyRule, len(in.AdmissionChecks)),
+		}
+		for i, checkRef := range in.AdmissionChecks {
+			out.AdmissionChecksStrategy.AdmissionChecks[i] = v1beta2.AdmissionCheckStrategyRule{
+				Name:      v1beta2.AdmissionCheckReference(checkRef),
+				OnFlavors: []v1beta2.ResourceFlavorReference{}, // Empty means all flavors
+			}
+		}
+	}
+
+	return nil
 }
 
 func Convert_v1beta2_ClusterQueueSpec_To_v1beta1_ClusterQueueSpec(in *v1beta2.ClusterQueueSpec, out *ClusterQueueSpec, s conversionapi.Scope) error {
 	out.Cohort = CohortReference(in.CohortName)
-	return autoConvert_v1beta2_ClusterQueueSpec_To_v1beta1_ClusterQueueSpec(in, out, s)
+	if err := autoConvert_v1beta2_ClusterQueueSpec_To_v1beta1_ClusterQueueSpec(in, out, s); err != nil {
```

  I don't think this conversion requires any changes. v1beta1 supports both AdmissionChecks and AdmissionCheckStrategy.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-03T08:09:32Z

- **pkg/controller/core/admissioncheck_controller.go:212** by [@mimowo](https://github.com/mimowo) — 2025-11-03T08:09:32Z
```diff
@@ -196,14 +209,16 @@ func (h *acCqHandler) Generic(ctx context.Context, e event.GenericEvent, q workq
 	log := log.FromContext(ctx).WithValues("clusterQueue", klog.KObj(cq))
 	log.V(6).Info("Cluster queue generic event")
 
-	for _, ac := range cq.Spec.AdmissionChecks {
-		if cqs := h.cache.ClusterQueuesUsingAdmissionCheck(ac); len(cqs) == 0 {
-			req := reconcile.Request{
-				NamespacedName: types.NamespacedName{
-					Name: string(ac),
-				},
+	if cq.Spec.AdmissionChecksStrategy != nil {
```

  Omission of handling `cq.Spec.AdmissionChecksStrategy` here looks like a bug prior to the PR. I think it would be better to submit as a preparatory PR, or just cherrypick this bit. Let me know if I'm missing something.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-03T08:10:09Z

- **pkg/controller/core/admissioncheck_controller.go:212** by [@mimowo](https://github.com/mimowo) — 2025-11-03T08:10:09Z
```diff
@@ -196,14 +209,16 @@ func (h *acCqHandler) Generic(ctx context.Context, e event.GenericEvent, q workq
 	log := log.FromContext(ctx).WithValues("clusterQueue", klog.KObj(cq))
 	log.V(6).Info("Cluster queue generic event")
 
-	for _, ac := range cq.Spec.AdmissionChecks {
-		if cqs := h.cache.ClusterQueuesUsingAdmissionCheck(ac); len(cqs) == 0 {
-			req := reconcile.Request{
-				NamespacedName: types.NamespacedName{
-					Name: string(ac),
-				},
+	if cq.Spec.AdmissionChecksStrategy != nil {
```

  cc @mbobrovskyi

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-03T08:10:20Z

- **pkg/controller/core/admissioncheck_controller.go:212** by [@mimowo](https://github.com/mimowo) — 2025-11-03T08:10:20Z
```diff
@@ -196,14 +209,16 @@ func (h *acCqHandler) Generic(ctx context.Context, e event.GenericEvent, q workq
 	log := log.FromContext(ctx).WithValues("clusterQueue", klog.KObj(cq))
 	log.V(6).Info("Cluster queue generic event")
 
-	for _, ac := range cq.Spec.AdmissionChecks {
-		if cqs := h.cache.ClusterQueuesUsingAdmissionCheck(ac); len(cqs) == 0 {
-			req := reconcile.Request{
-				NamespacedName: types.NamespacedName{
-					Name: string(ac),
-				},
+	if cq.Spec.AdmissionChecksStrategy != nil {
```

  cc @IrvingMg @PBundyra

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-03T08:13:06Z

- **pkg/util/testing/v1beta2/wrappers.go:949** by [@mimowo](https://github.com/mimowo) — 2025-11-03T08:13:06Z
```diff
@@ -935,9 +935,26 @@ func (c *ClusterQueueWrapper) ResourceGroup(flavors ...kueue.FlavorQuotas) *Clus
 	return c
 }
 
-// AdmissionChecks replaces the queue additional checks
+// AdmissionChecks replaces the queue additional checks.
+// This is a convenience wrapper that converts to the AdmissionChecksStrategy format.
 func (c *ClusterQueueWrapper) AdmissionChecks(checks ...kueue.AdmissionCheckReference) *ClusterQueueWrapper {
-	c.Spec.AdmissionChecks = checks
+	if len(checks) == 0 {
+		c.Spec.AdmissionChecksStrategy = nil
+		return c
+	}
+
+	if c.Spec.AdmissionChecksStrategy == nil {
+		c.Spec.AdmissionChecksStrategy = &kueue.AdmissionChecksStrategy{}
+	}
+
```

  ```suggestion
    c.Spec.AdmissionChecksStrategy = &kueue.AdmissionChecksStrategy{}
  ```
  There should be no need to care about the corner cases in the testing wrappers. Also in the previous version we wouldn't care about them

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-06T09:15:52Z

- **apis/kueue/v1beta1/clusterqueue_conversion.go:56** by [@mimowo](https://github.com/mimowo) — 2025-11-06T09:15:53Z
```diff
@@ -38,6 +38,23 @@ func (dst *ClusterQueue) ConvertFrom(srcRaw conversion.Hub) error {
 
 func Convert_v1beta1_ClusterQueueSpec_To_v1beta2_ClusterQueueSpec(in *ClusterQueueSpec, out *v1beta2.ClusterQueueSpec, s conversionapi.Scope) error {
 	out.CohortName = v1beta2.CohortReference(in.Cohort)
+
+	// Convert AdmissionChecks to AdmissionChecksStrategy before autoConvert
+	if len(in.AdmissionChecks) > 0 {
+		out.AdmissionChecksStrategy = &v1beta2.AdmissionChecksStrategy{
+			AdmissionChecks: make([]v1beta2.AdmissionCheckStrategyRule, len(in.AdmissionChecks)),
+		}
+		for i, checkRef := range in.AdmissionChecks {
+			out.AdmissionChecksStrategy.AdmissionChecks[i] = v1beta2.AdmissionCheckStrategyRule{
+				Name: v1beta2.AdmissionCheckReference(checkRef),
+			}
+		}
+	}
+
+	if err := autoConvert_v1beta1_ClusterQueueSpec_To_v1beta2_ClusterQueueSpec(in, out, s); err != nil {
+		return err
+	}
```

  You can move this to the bottom just as before `return autoConvert_v1beta1_ClusterQueueSpec_To_v1beta2_ClusterQueueSpec(in, out, s)`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-06T09:18:31Z

- **pkg/controller/core/admissioncheck_controller.go:180** by [@mimowo](https://github.com/mimowo) — 2025-11-06T09:18:31Z
```diff
@@ -164,7 +164,20 @@ func (r *AdmissionCheckReconciler) Generic(e event.TypedGenericEvent[*kueue.Admi
 func (r *AdmissionCheckReconciler) NotifyClusterQueueUpdate(oldCq *kueue.ClusterQueue, newCq *kueue.ClusterQueue) {
 	log := r.log.WithValues("oldClusterQueue", klog.KObj(oldCq), "newClusterQueue", klog.KObj(newCq))
 	log.V(5).Info("Cluster queue notification")
-	noChange := newCq != nil && oldCq != nil && slices.CmpNoOrder(oldCq.Spec.AdmissionChecks, newCq.Spec.AdmissionChecks)
+
+	// Helper to extract admission check names from strategy
+	getAcNames := func(cq *kueue.ClusterQueue) []kueue.AdmissionCheckReference {
+		if cq.Spec.AdmissionChecksStrategy == nil {
+			return nil
+		}
+		names := make([]kueue.AdmissionCheckReference, len(cq.Spec.AdmissionChecksStrategy.AdmissionChecks))
+		for i, ac := range cq.Spec.AdmissionChecksStrategy.AdmissionChecks {
+			names[i] = ac.Name
+		}
+		return names
+	}
+
+	noChange := newCq != nil && oldCq != nil && slices.CmpNoOrder(getAcNames(oldCq), getAcNames(newCq))
```

  This change is great and a bugfix, we need also a separete PR we could cherrypick as a bugifx. Feel free to open. In any case keep the changes here as they are.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-06T09:19:43Z

- **apis/kueue/v1beta1/clusterqueue_conversion.go:39** by [@mimowo](https://github.com/mimowo) — 2025-11-06T09:19:43Z
```diff
@@ -38,12 +38,56 @@ func (dst *ClusterQueue) ConvertFrom(srcRaw conversion.Hub) error {
 
 func Convert_v1beta1_ClusterQueueSpec_To_v1beta2_ClusterQueueSpec(in *ClusterQueueSpec, out *v1beta2.ClusterQueueSpec, s conversionapi.Scope) error {
```

  Sure we need unit tests here, but I'm not too concerned about them in this PR. Every CQ goes via conversion as storage is still in v1beta1. I think we can follow up in next PR.

### Review by [@nerdeveloper](https://github.com/nerdeveloper) (commented) — 2025-11-06T09:26:11Z

- **pkg/controller/core/admissioncheck_controller.go:180** by [@nerdeveloper](https://github.com/nerdeveloper) — 2025-11-06T09:26:11Z
```diff
@@ -164,7 +164,20 @@ func (r *AdmissionCheckReconciler) Generic(e event.TypedGenericEvent[*kueue.Admi
 func (r *AdmissionCheckReconciler) NotifyClusterQueueUpdate(oldCq *kueue.ClusterQueue, newCq *kueue.ClusterQueue) {
 	log := r.log.WithValues("oldClusterQueue", klog.KObj(oldCq), "newClusterQueue", klog.KObj(newCq))
 	log.V(5).Info("Cluster queue notification")
-	noChange := newCq != nil && oldCq != nil && slices.CmpNoOrder(oldCq.Spec.AdmissionChecks, newCq.Spec.AdmissionChecks)
+
+	// Helper to extract admission check names from strategy
+	getAcNames := func(cq *kueue.ClusterQueue) []kueue.AdmissionCheckReference {
+		if cq.Spec.AdmissionChecksStrategy == nil {
+			return nil
+		}
+		names := make([]kueue.AdmissionCheckReference, len(cq.Spec.AdmissionChecksStrategy.AdmissionChecks))
+		for i, ac := range cq.Spec.AdmissionChecksStrategy.AdmissionChecks {
+			names[i] = ac.Name
+		}
+		return names
+	}
+
+	noChange := newCq != nil && oldCq != nil && slices.CmpNoOrder(getAcNames(oldCq), getAcNames(newCq))
```

  alright

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-06T09:29:53Z

- **pkg/controller/core/admissioncheck_controller.go:180** by [@mimowo](https://github.com/mimowo) — 2025-11-06T09:29:53Z
```diff
@@ -164,7 +164,20 @@ func (r *AdmissionCheckReconciler) Generic(e event.TypedGenericEvent[*kueue.Admi
 func (r *AdmissionCheckReconciler) NotifyClusterQueueUpdate(oldCq *kueue.ClusterQueue, newCq *kueue.ClusterQueue) {
 	log := r.log.WithValues("oldClusterQueue", klog.KObj(oldCq), "newClusterQueue", klog.KObj(newCq))
 	log.V(5).Info("Cluster queue notification")
-	noChange := newCq != nil && oldCq != nil && slices.CmpNoOrder(oldCq.Spec.AdmissionChecks, newCq.Spec.AdmissionChecks)
+
+	// Helper to extract admission check names from strategy
+	getAcNames := func(cq *kueue.ClusterQueue) []kueue.AdmissionCheckReference {
+		if cq.Spec.AdmissionChecksStrategy == nil {
+			return nil
+		}
+		names := make([]kueue.AdmissionCheckReference, len(cq.Spec.AdmissionChecksStrategy.AdmissionChecks))
+		for i, ac := range cq.Spec.AdmissionChecksStrategy.AdmissionChecks {
+			names[i] = ac.Name
+		}
+		return names
+	}
+
+	noChange := newCq != nil && oldCq != nil && slices.CmpNoOrder(getAcNames(oldCq), getAcNames(newCq))
```

  together with the changes in https://github.com/kubernetes-sigs/kueue/pull/7490/files#r2485619146

### Review by [@nerdeveloper](https://github.com/nerdeveloper) (commented) — 2025-11-06T09:31:24Z

- **pkg/controller/core/admissioncheck_controller.go:180** by [@nerdeveloper](https://github.com/nerdeveloper) — 2025-11-06T09:31:23Z
```diff
@@ -164,7 +164,20 @@ func (r *AdmissionCheckReconciler) Generic(e event.TypedGenericEvent[*kueue.Admi
 func (r *AdmissionCheckReconciler) NotifyClusterQueueUpdate(oldCq *kueue.ClusterQueue, newCq *kueue.ClusterQueue) {
 	log := r.log.WithValues("oldClusterQueue", klog.KObj(oldCq), "newClusterQueue", klog.KObj(newCq))
 	log.V(5).Info("Cluster queue notification")
-	noChange := newCq != nil && oldCq != nil && slices.CmpNoOrder(oldCq.Spec.AdmissionChecks, newCq.Spec.AdmissionChecks)
+
+	// Helper to extract admission check names from strategy
+	getAcNames := func(cq *kueue.ClusterQueue) []kueue.AdmissionCheckReference {
+		if cq.Spec.AdmissionChecksStrategy == nil {
+			return nil
+		}
+		names := make([]kueue.AdmissionCheckReference, len(cq.Spec.AdmissionChecksStrategy.AdmissionChecks))
+		for i, ac := range cq.Spec.AdmissionChecksStrategy.AdmissionChecks {
+			names[i] = ac.Name
+		}
+		return names
+	}
+
+	noChange := newCq != nil && oldCq != nil && slices.CmpNoOrder(getAcNames(oldCq), getAcNames(newCq))
```

  i will look into that next week

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-06T09:32:46Z

- **pkg/controller/core/admissioncheck_controller.go:212** by [@mimowo](https://github.com/mimowo) — 2025-11-06T09:32:46Z
```diff
@@ -196,14 +209,16 @@ func (h *acCqHandler) Generic(ctx context.Context, e event.GenericEvent, q workq
 	log := log.FromContext(ctx).WithValues("clusterQueue", klog.KObj(cq))
 	log.V(6).Info("Cluster queue generic event")
 
-	for _, ac := range cq.Spec.AdmissionChecks {
-		if cqs := h.cache.ClusterQueuesUsingAdmissionCheck(ac); len(cqs) == 0 {
-			req := reconcile.Request{
-				NamespacedName: types.NamespacedName{
-					Name: string(ac),
-				},
+	if cq.Spec.AdmissionChecksStrategy != nil {
```

  This looks actually as premature optimization with this check `if cqs := h.cache.ClusterQueuesUsingAdmissionCheck(ac.Name); len(cqs) == 0 {` I think we could consider dropping it in a follow up PR.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-06T09:34:16Z

/lgtm
/approve
Thank you 👍 

There are follow ups:
1. unit tests as requested in https://github.com/kubernetes-sigs/kueue/pull/7490/files#r2484917008
2. separete bugfix PR for 0.14 and 0.13: https://github.com/kubernetes-sigs/kueue/pull/7490/files#r2498163625
3. attempt to cleanup https://github.com/kubernetes-sigs/kueue/pull/7490/files#r2498212119 as it looks as a pre-existing premature optimization
