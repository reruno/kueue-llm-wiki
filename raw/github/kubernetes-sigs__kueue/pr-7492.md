# PR #7492: Add testing exclude resource prefixes

**Summary**: Add testing exclude resource prefixes

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7492

**Last updated**: 2025-11-26T17:30:40Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kannon92](https://github.com/kannon92)
- **Branch**: `add-testing-exclude-resource-prefixes` → `main`
- **Created**: 2025-11-02T17:19:44Z
- **Updated**: 2025-11-26T17:30:40Z
- **Merged**: 2025-11-26T17:30:38Z
- **Closed**: 2025-11-26T17:30:38Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi), [@gabesaba](https://github.com/gabesaba)
- **Requested reviewers**: [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 2   **+304 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add test cases for scheduler for excludeResourcePrefixes.

As I was working on #5800, I notice we don't really have a good test coverage of this excludeResourcePrefixes in the configuration.

There is one test in pod_controller in this but I think it make sense to have this for the scheduler.
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:
Assisted by Claude Code
#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-11-02T17:19:51Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c88a8c2a1c12cbb3be9d53f5343a55dd18b8c3ba |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/690f7a37a5e3720008c7dfe7 |

### Comment by [@kannon92](https://github.com/kannon92) — 2025-11-07T18:15:36Z

/assign @mbobrovskyi

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-09T07:10:38Z

/lgtm
Thank you!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-09T07:10:44Z

LGTM label has been added.  <details>Git tree hash: 8550b6e579893a14876fea306dd5051cca46c077</details>

### Comment by [@kannon92](https://github.com/kannon92) — 2025-11-18T20:18:28Z

/assign @gabesaba @mimowo

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-26T16:40:24Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7492#" title="Author self-approved">kannon92</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7492#pullrequestreview-3511815248" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-03T07:18:21Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:67** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-03T07:18:21Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource(corev1.ResourceName("networking.other.com/vpc"), "1000").
```

  ```suggestion
  					Resource("networking.other.com/vpc", "1000").
  ```
  nit

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-03T07:19:23Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:87** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-03T07:19:23Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource(corev1.ResourceName("networking.other.com/vpc"), "1000").
+					Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.It("should admit workload ignoring excluded resource", func() {
+		ginkgo.By("Creating a workload that requests an excluded resource")
+		wl := utiltestingapi.MakeWorkload("test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "100").
```

  ```suggestion
  			Request("networking.example.com/vpc", "100").
  ```
  nit

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-03T07:20:38Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:117** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-03T07:20:38Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource(corev1.ResourceName("networking.other.com/vpc"), "1000").
+					Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.It("should admit workload ignoring excluded resource", func() {
+		ginkgo.By("Creating a workload that requests an excluded resource")
+		wl := utiltestingapi.MakeWorkload("test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload gets admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Verifying excluded resource is not in admission resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		gomega.Expect(wl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("networking.example.com/vpc")))
+	})
+
+	ginkgo.It("should ignore multiple excluded resource prefixes", func() {
+		ginkgo.By("Creating a workload that requests resources from multiple excluded prefixes")
+		wl := utiltestingapi.MakeWorkload("multi-exclude-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "2").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "10").
+			Request(corev1.ResourceName("storage.example.com/disk"), "50").
+			Obj()
```

  ```suggestion
  			Request("networking.example.com/vpc", "10").
  			Request("storage.example.com/disk", "50").
  			Obj()
  ```
  nit

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-03T07:22:22Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:129** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-03T07:22:22Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource(corev1.ResourceName("networking.other.com/vpc"), "1000").
+					Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.It("should admit workload ignoring excluded resource", func() {
+		ginkgo.By("Creating a workload that requests an excluded resource")
+		wl := utiltestingapi.MakeWorkload("test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload gets admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Verifying excluded resource is not in admission resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		gomega.Expect(wl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("networking.example.com/vpc")))
+	})
+
+	ginkgo.It("should ignore multiple excluded resource prefixes", func() {
+		ginkgo.By("Creating a workload that requests resources from multiple excluded prefixes")
+		wl := utiltestingapi.MakeWorkload("multi-exclude-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "2").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "10").
+			Request(corev1.ResourceName("storage.example.com/disk"), "50").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload admission")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
```

  ```suggestion
  		gomega.Eventually(func(g gomega.Gomega) {
  			g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
  			g.Expect(workload.IsAdmitted(wl)).To(gomega.BeTrue())
  		}, util.Timeout, util.Interval).Should(gomega.Succeed())
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-03T07:23:47Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:144** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-03T07:23:47Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource(corev1.ResourceName("networking.other.com/vpc"), "1000").
+					Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.It("should admit workload ignoring excluded resource", func() {
+		ginkgo.By("Creating a workload that requests an excluded resource")
+		wl := utiltestingapi.MakeWorkload("test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload gets admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Verifying excluded resource is not in admission resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		gomega.Expect(wl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("networking.example.com/vpc")))
+	})
+
+	ginkgo.It("should ignore multiple excluded resource prefixes", func() {
+		ginkgo.By("Creating a workload that requests resources from multiple excluded prefixes")
+		wl := utiltestingapi.MakeWorkload("multi-exclude-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "2").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "10").
+			Request(corev1.ResourceName("storage.example.com/disk"), "50").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload admission")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Verifying both excluded resources are not in resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("networking.example.com/vpc")))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("storage.example.com/disk")))
+	})
+
+	ginkgo.It("should enforce quota for non-excluded resources", func() {
+		ginkgo.By("Creating a workload that consumes most of the CPU quota")
+		wl1 := utiltestingapi.MakeWorkload("quota-wl-1", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "8").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "1000").
```

  ```suggestion
  			Request("networking.example.com/vpc", "1000").
  ```
  nit

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-03T07:24:03Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:160** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-03T07:24:03Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource(corev1.ResourceName("networking.other.com/vpc"), "1000").
+					Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.It("should admit workload ignoring excluded resource", func() {
+		ginkgo.By("Creating a workload that requests an excluded resource")
+		wl := utiltestingapi.MakeWorkload("test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload gets admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Verifying excluded resource is not in admission resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		gomega.Expect(wl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("networking.example.com/vpc")))
+	})
+
+	ginkgo.It("should ignore multiple excluded resource prefixes", func() {
+		ginkgo.By("Creating a workload that requests resources from multiple excluded prefixes")
+		wl := utiltestingapi.MakeWorkload("multi-exclude-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "2").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "10").
+			Request(corev1.ResourceName("storage.example.com/disk"), "50").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload admission")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Verifying both excluded resources are not in resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("networking.example.com/vpc")))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("storage.example.com/disk")))
+	})
+
+	ginkgo.It("should enforce quota for non-excluded resources", func() {
+		ginkgo.By("Creating a workload that consumes most of the CPU quota")
+		wl1 := utiltestingapi.MakeWorkload("quota-wl-1", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "8").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "1000").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+
+		wl1Key := types.NamespacedName{Name: wl1.Name, Namespace: ns.Name}
+
+		ginkgo.By("Waiting for first workload to be admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wl1Key, wl1)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl1)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Creating a second workload that would exceed CPU quota")
+		wl2 := utiltestingapi.MakeWorkload("quota-wl-2", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "5").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "1").
```

  ```suggestion
  			Request("networking.example.com/vpc", "1").
  ```
  nit

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-03T07:24:44Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:178** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-03T07:24:44Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource(corev1.ResourceName("networking.other.com/vpc"), "1000").
+					Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.It("should admit workload ignoring excluded resource", func() {
+		ginkgo.By("Creating a workload that requests an excluded resource")
+		wl := utiltestingapi.MakeWorkload("test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload gets admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Verifying excluded resource is not in admission resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		gomega.Expect(wl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("networking.example.com/vpc")))
+	})
+
+	ginkgo.It("should ignore multiple excluded resource prefixes", func() {
+		ginkgo.By("Creating a workload that requests resources from multiple excluded prefixes")
+		wl := utiltestingapi.MakeWorkload("multi-exclude-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "2").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "10").
+			Request(corev1.ResourceName("storage.example.com/disk"), "50").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload admission")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Verifying both excluded resources are not in resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("networking.example.com/vpc")))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("storage.example.com/disk")))
+	})
+
+	ginkgo.It("should enforce quota for non-excluded resources", func() {
+		ginkgo.By("Creating a workload that consumes most of the CPU quota")
+		wl1 := utiltestingapi.MakeWorkload("quota-wl-1", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "8").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "1000").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+
+		wl1Key := types.NamespacedName{Name: wl1.Name, Namespace: ns.Name}
+
+		ginkgo.By("Waiting for first workload to be admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wl1Key, wl1)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl1)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Creating a second workload that would exceed CPU quota")
+		wl2 := utiltestingapi.MakeWorkload("quota-wl-2", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "5").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "1").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl2)).To(gomega.Succeed())
+
+		wl2Key := types.NamespacedName{Name: wl2.Name, Namespace: ns.Name}
+
+		ginkgo.By("Verifying second workload is not admitted due to CPU quota")
+		gomega.Consistently(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wl2Key, wl2)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl2)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
+	})
+
+	ginkgo.It("should use exact prefix matching", func() {
+		ginkgo.By("Creating a workload with a resource that doesn't match the excluded prefix")
+		wl := utiltestingapi.MakeWorkload("prefix-test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request(corev1.ResourceName("networking.other.com/vpc"), "100").
```

  ```suggestion
  			Request("networking.other.com/vpc", "100").
  ```
  nit

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-03T07:25:46Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:188** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-03T07:25:46Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource(corev1.ResourceName("networking.other.com/vpc"), "1000").
+					Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.It("should admit workload ignoring excluded resource", func() {
+		ginkgo.By("Creating a workload that requests an excluded resource")
+		wl := utiltestingapi.MakeWorkload("test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload gets admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Verifying excluded resource is not in admission resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		gomega.Expect(wl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("networking.example.com/vpc")))
+	})
+
+	ginkgo.It("should ignore multiple excluded resource prefixes", func() {
+		ginkgo.By("Creating a workload that requests resources from multiple excluded prefixes")
+		wl := utiltestingapi.MakeWorkload("multi-exclude-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "2").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "10").
+			Request(corev1.ResourceName("storage.example.com/disk"), "50").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload admission")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Verifying both excluded resources are not in resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("networking.example.com/vpc")))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("storage.example.com/disk")))
+	})
+
+	ginkgo.It("should enforce quota for non-excluded resources", func() {
+		ginkgo.By("Creating a workload that consumes most of the CPU quota")
+		wl1 := utiltestingapi.MakeWorkload("quota-wl-1", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "8").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "1000").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+
+		wl1Key := types.NamespacedName{Name: wl1.Name, Namespace: ns.Name}
+
+		ginkgo.By("Waiting for first workload to be admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wl1Key, wl1)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl1)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Creating a second workload that would exceed CPU quota")
+		wl2 := utiltestingapi.MakeWorkload("quota-wl-2", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "5").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "1").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl2)).To(gomega.Succeed())
+
+		wl2Key := types.NamespacedName{Name: wl2.Name, Namespace: ns.Name}
+
+		ginkgo.By("Verifying second workload is not admitted due to CPU quota")
+		gomega.Consistently(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wl2Key, wl2)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl2)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
+	})
+
+	ginkgo.It("should use exact prefix matching", func() {
+		ginkgo.By("Creating a workload with a resource that doesn't match the excluded prefix")
+		wl := utiltestingapi.MakeWorkload("prefix-test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request(corev1.ResourceName("networking.other.com/vpc"), "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{Name: wl.Name, Namespace: ns.Name}
+
+		ginkgo.By("Verifying workload is admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
```

  ```suggestion
  		gomega.Eventually(func(g gomega.Gomega) {
  			g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
  			g.Expect(workload.IsAdmitted(wl)).To(gomega.BeTrue())
  		}, util.Timeout, util.Interval).Should(gomega.Succeed())
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-03T07:27:36Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:154** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-03T07:27:36Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource(corev1.ResourceName("networking.other.com/vpc"), "1000").
+					Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.It("should admit workload ignoring excluded resource", func() {
+		ginkgo.By("Creating a workload that requests an excluded resource")
+		wl := utiltestingapi.MakeWorkload("test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload gets admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Verifying excluded resource is not in admission resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		gomega.Expect(wl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("networking.example.com/vpc")))
+	})
+
+	ginkgo.It("should ignore multiple excluded resource prefixes", func() {
+		ginkgo.By("Creating a workload that requests resources from multiple excluded prefixes")
+		wl := utiltestingapi.MakeWorkload("multi-exclude-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "2").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "10").
+			Request(corev1.ResourceName("storage.example.com/disk"), "50").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload admission")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Verifying both excluded resources are not in resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("networking.example.com/vpc")))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("storage.example.com/disk")))
+	})
+
+	ginkgo.It("should enforce quota for non-excluded resources", func() {
+		ginkgo.By("Creating a workload that consumes most of the CPU quota")
+		wl1 := utiltestingapi.MakeWorkload("quota-wl-1", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "8").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "1000").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+
+		wl1Key := types.NamespacedName{Name: wl1.Name, Namespace: ns.Name}
+
+		ginkgo.By("Waiting for first workload to be admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wl1Key, wl1)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl1)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
```

  ```suggestion
  		gomega.Eventually(func(g gomega.Gomega) {
  			g.Expect(k8sClient.Get(ctx, wl1Key, wl1)).To(gomega.Succeed())
  			g.Expect(workload.IsAdmitted(wl1)).To(gomega.BeTrue())
  		}, util.Timeout, util.Interval).Should(gomega.Succeed())
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-03T07:29:01Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:100** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-03T07:29:01Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource(corev1.ResourceName("networking.other.com/vpc"), "1000").
+					Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.It("should admit workload ignoring excluded resource", func() {
+		ginkgo.By("Creating a workload that requests an excluded resource")
+		wl := utiltestingapi.MakeWorkload("test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload gets admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
```

  ```suggestion
  		gomega.Eventually(func(g gomega.Gomega) {
  			g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
  			g.Expect(workload.IsAdmitted(wl)).To(gomega.BeTrue())
  		}, util.Timeout, util.Interval).Should(gomega.Succeed())
  ```

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-03T20:04:26Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:67** by [@kannon92](https://github.com/kannon92) — 2025-11-03T20:04:25Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource(corev1.ResourceName("networking.other.com/vpc"), "1000").
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-03T20:04:31Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:87** by [@kannon92](https://github.com/kannon92) — 2025-11-03T20:04:31Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource(corev1.ResourceName("networking.other.com/vpc"), "1000").
+					Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.It("should admit workload ignoring excluded resource", func() {
+		ginkgo.By("Creating a workload that requests an excluded resource")
+		wl := utiltestingapi.MakeWorkload("test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "100").
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-03T20:04:39Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:117** by [@kannon92](https://github.com/kannon92) — 2025-11-03T20:04:39Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource(corev1.ResourceName("networking.other.com/vpc"), "1000").
+					Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.It("should admit workload ignoring excluded resource", func() {
+		ginkgo.By("Creating a workload that requests an excluded resource")
+		wl := utiltestingapi.MakeWorkload("test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload gets admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Verifying excluded resource is not in admission resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		gomega.Expect(wl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("networking.example.com/vpc")))
+	})
+
+	ginkgo.It("should ignore multiple excluded resource prefixes", func() {
+		ginkgo.By("Creating a workload that requests resources from multiple excluded prefixes")
+		wl := utiltestingapi.MakeWorkload("multi-exclude-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "2").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "10").
+			Request(corev1.ResourceName("storage.example.com/disk"), "50").
+			Obj()
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-03T20:04:46Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:129** by [@kannon92](https://github.com/kannon92) — 2025-11-03T20:04:46Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource(corev1.ResourceName("networking.other.com/vpc"), "1000").
+					Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.It("should admit workload ignoring excluded resource", func() {
+		ginkgo.By("Creating a workload that requests an excluded resource")
+		wl := utiltestingapi.MakeWorkload("test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload gets admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Verifying excluded resource is not in admission resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		gomega.Expect(wl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("networking.example.com/vpc")))
+	})
+
+	ginkgo.It("should ignore multiple excluded resource prefixes", func() {
+		ginkgo.By("Creating a workload that requests resources from multiple excluded prefixes")
+		wl := utiltestingapi.MakeWorkload("multi-exclude-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "2").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "10").
+			Request(corev1.ResourceName("storage.example.com/disk"), "50").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload admission")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-03T20:04:53Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:144** by [@kannon92](https://github.com/kannon92) — 2025-11-03T20:04:52Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource(corev1.ResourceName("networking.other.com/vpc"), "1000").
+					Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.It("should admit workload ignoring excluded resource", func() {
+		ginkgo.By("Creating a workload that requests an excluded resource")
+		wl := utiltestingapi.MakeWorkload("test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload gets admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Verifying excluded resource is not in admission resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		gomega.Expect(wl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("networking.example.com/vpc")))
+	})
+
+	ginkgo.It("should ignore multiple excluded resource prefixes", func() {
+		ginkgo.By("Creating a workload that requests resources from multiple excluded prefixes")
+		wl := utiltestingapi.MakeWorkload("multi-exclude-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "2").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "10").
+			Request(corev1.ResourceName("storage.example.com/disk"), "50").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload admission")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Verifying both excluded resources are not in resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("networking.example.com/vpc")))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("storage.example.com/disk")))
+	})
+
+	ginkgo.It("should enforce quota for non-excluded resources", func() {
+		ginkgo.By("Creating a workload that consumes most of the CPU quota")
+		wl1 := utiltestingapi.MakeWorkload("quota-wl-1", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "8").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "1000").
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-03T20:05:00Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:160** by [@kannon92](https://github.com/kannon92) — 2025-11-03T20:04:59Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource(corev1.ResourceName("networking.other.com/vpc"), "1000").
+					Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.It("should admit workload ignoring excluded resource", func() {
+		ginkgo.By("Creating a workload that requests an excluded resource")
+		wl := utiltestingapi.MakeWorkload("test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload gets admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Verifying excluded resource is not in admission resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		gomega.Expect(wl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("networking.example.com/vpc")))
+	})
+
+	ginkgo.It("should ignore multiple excluded resource prefixes", func() {
+		ginkgo.By("Creating a workload that requests resources from multiple excluded prefixes")
+		wl := utiltestingapi.MakeWorkload("multi-exclude-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "2").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "10").
+			Request(corev1.ResourceName("storage.example.com/disk"), "50").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload admission")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Verifying both excluded resources are not in resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("networking.example.com/vpc")))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("storage.example.com/disk")))
+	})
+
+	ginkgo.It("should enforce quota for non-excluded resources", func() {
+		ginkgo.By("Creating a workload that consumes most of the CPU quota")
+		wl1 := utiltestingapi.MakeWorkload("quota-wl-1", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "8").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "1000").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+
+		wl1Key := types.NamespacedName{Name: wl1.Name, Namespace: ns.Name}
+
+		ginkgo.By("Waiting for first workload to be admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wl1Key, wl1)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl1)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Creating a second workload that would exceed CPU quota")
+		wl2 := utiltestingapi.MakeWorkload("quota-wl-2", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "5").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "1").
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-03T20:05:06Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:178** by [@kannon92](https://github.com/kannon92) — 2025-11-03T20:05:06Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource(corev1.ResourceName("networking.other.com/vpc"), "1000").
+					Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.It("should admit workload ignoring excluded resource", func() {
+		ginkgo.By("Creating a workload that requests an excluded resource")
+		wl := utiltestingapi.MakeWorkload("test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload gets admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Verifying excluded resource is not in admission resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		gomega.Expect(wl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("networking.example.com/vpc")))
+	})
+
+	ginkgo.It("should ignore multiple excluded resource prefixes", func() {
+		ginkgo.By("Creating a workload that requests resources from multiple excluded prefixes")
+		wl := utiltestingapi.MakeWorkload("multi-exclude-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "2").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "10").
+			Request(corev1.ResourceName("storage.example.com/disk"), "50").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload admission")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Verifying both excluded resources are not in resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("networking.example.com/vpc")))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("storage.example.com/disk")))
+	})
+
+	ginkgo.It("should enforce quota for non-excluded resources", func() {
+		ginkgo.By("Creating a workload that consumes most of the CPU quota")
+		wl1 := utiltestingapi.MakeWorkload("quota-wl-1", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "8").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "1000").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+
+		wl1Key := types.NamespacedName{Name: wl1.Name, Namespace: ns.Name}
+
+		ginkgo.By("Waiting for first workload to be admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wl1Key, wl1)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl1)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Creating a second workload that would exceed CPU quota")
+		wl2 := utiltestingapi.MakeWorkload("quota-wl-2", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "5").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "1").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl2)).To(gomega.Succeed())
+
+		wl2Key := types.NamespacedName{Name: wl2.Name, Namespace: ns.Name}
+
+		ginkgo.By("Verifying second workload is not admitted due to CPU quota")
+		gomega.Consistently(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wl2Key, wl2)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl2)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
+	})
+
+	ginkgo.It("should use exact prefix matching", func() {
+		ginkgo.By("Creating a workload with a resource that doesn't match the excluded prefix")
+		wl := utiltestingapi.MakeWorkload("prefix-test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request(corev1.ResourceName("networking.other.com/vpc"), "100").
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-03T20:05:22Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:188** by [@kannon92](https://github.com/kannon92) — 2025-11-03T20:05:22Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource(corev1.ResourceName("networking.other.com/vpc"), "1000").
+					Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.It("should admit workload ignoring excluded resource", func() {
+		ginkgo.By("Creating a workload that requests an excluded resource")
+		wl := utiltestingapi.MakeWorkload("test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload gets admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Verifying excluded resource is not in admission resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		gomega.Expect(wl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("networking.example.com/vpc")))
+	})
+
+	ginkgo.It("should ignore multiple excluded resource prefixes", func() {
+		ginkgo.By("Creating a workload that requests resources from multiple excluded prefixes")
+		wl := utiltestingapi.MakeWorkload("multi-exclude-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "2").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "10").
+			Request(corev1.ResourceName("storage.example.com/disk"), "50").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload admission")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Verifying both excluded resources are not in resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("networking.example.com/vpc")))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("storage.example.com/disk")))
+	})
+
+	ginkgo.It("should enforce quota for non-excluded resources", func() {
+		ginkgo.By("Creating a workload that consumes most of the CPU quota")
+		wl1 := utiltestingapi.MakeWorkload("quota-wl-1", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "8").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "1000").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+
+		wl1Key := types.NamespacedName{Name: wl1.Name, Namespace: ns.Name}
+
+		ginkgo.By("Waiting for first workload to be admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wl1Key, wl1)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl1)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Creating a second workload that would exceed CPU quota")
+		wl2 := utiltestingapi.MakeWorkload("quota-wl-2", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "5").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "1").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl2)).To(gomega.Succeed())
+
+		wl2Key := types.NamespacedName{Name: wl2.Name, Namespace: ns.Name}
+
+		ginkgo.By("Verifying second workload is not admitted due to CPU quota")
+		gomega.Consistently(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wl2Key, wl2)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl2)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
+	})
+
+	ginkgo.It("should use exact prefix matching", func() {
+		ginkgo.By("Creating a workload with a resource that doesn't match the excluded prefix")
+		wl := utiltestingapi.MakeWorkload("prefix-test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request(corev1.ResourceName("networking.other.com/vpc"), "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{Name: wl.Name, Namespace: ns.Name}
+
+		ginkgo.By("Verifying workload is admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-03T20:05:29Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:154** by [@kannon92](https://github.com/kannon92) — 2025-11-03T20:05:29Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource(corev1.ResourceName("networking.other.com/vpc"), "1000").
+					Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.It("should admit workload ignoring excluded resource", func() {
+		ginkgo.By("Creating a workload that requests an excluded resource")
+		wl := utiltestingapi.MakeWorkload("test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload gets admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Verifying excluded resource is not in admission resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		gomega.Expect(wl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("networking.example.com/vpc")))
+	})
+
+	ginkgo.It("should ignore multiple excluded resource prefixes", func() {
+		ginkgo.By("Creating a workload that requests resources from multiple excluded prefixes")
+		wl := utiltestingapi.MakeWorkload("multi-exclude-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "2").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "10").
+			Request(corev1.ResourceName("storage.example.com/disk"), "50").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload admission")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+		ginkgo.By("Verifying both excluded resources are not in resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("networking.example.com/vpc")))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("storage.example.com/disk")))
+	})
+
+	ginkgo.It("should enforce quota for non-excluded resources", func() {
+		ginkgo.By("Creating a workload that consumes most of the CPU quota")
+		wl1 := utiltestingapi.MakeWorkload("quota-wl-1", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "8").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "1000").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+
+		wl1Key := types.NamespacedName{Name: wl1.Name, Namespace: ns.Name}
+
+		ginkgo.By("Waiting for first workload to be admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wl1Key, wl1)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl1)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-03T20:05:34Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:100** by [@kannon92](https://github.com/kannon92) — 2025-11-03T20:05:34Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource(corev1.ResourceName("networking.other.com/vpc"), "1000").
+					Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.It("should admit workload ignoring excluded resource", func() {
+		ginkgo.By("Creating a workload that requests an excluded resource")
+		wl := utiltestingapi.MakeWorkload("test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request(corev1.ResourceName("networking.example.com/vpc"), "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload gets admitted")
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl)
+		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-07T17:33:46Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:194** by [@kannon92](https://github.com/kannon92) — 2025-11-07T17:33:46Z
```diff
@@ -0,0 +1,196 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource("networking.other.com/vpc", "1000").
+					Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.It("should admit workload ignoring excluded resource", func() {
+		ginkgo.By("Creating a workload that requests an excluded resource")
+		wl := utiltestingapi.MakeWorkload("test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request("networking.example.com/vpc", "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload gets admitted")
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			g.Expect(workload.IsAdmitted(wl)).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Verifying excluded resource is not in admission resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		gomega.Expect(wl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey("networking.example.com/vpc"))
+	})
+
+	ginkgo.It("should ignore multiple excluded resource prefixes", func() {
+		ginkgo.By("Creating a workload that requests resources from multiple excluded prefixes")
+		wl := utiltestingapi.MakeWorkload("multi-exclude-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "2").
+			Request("networking.example.com/vpc", "10").
+			Request("storage.example.com/disk", "50").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload admission")
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			g.Expect(workload.IsAdmitted(wl)).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Verifying both excluded resources are not in resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey("networking.example.com/vpc"))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey("storage.example.com/disk"))
+	})
+
+	ginkgo.It("should enforce quota for non-excluded resources", func() {
+		ginkgo.By("Creating a workload that consumes most of the CPU quota")
+		wl1 := utiltestingapi.MakeWorkload("quota-wl-1", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "8").
+			Request("networking.example.com/vpc", "1000").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+
+		wl1Key := types.NamespacedName{Name: wl1.Name, Namespace: ns.Name}
+
+		ginkgo.By("Waiting for first workload to be admitted")
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wl1Key, wl1)).To(gomega.Succeed())
+			g.Expect(workload.IsAdmitted(wl1)).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Creating a second workload that would exceed CPU quota")
+		wl2 := utiltestingapi.MakeWorkload("quota-wl-2", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "5").
+			Request("networking.example.com/vpc", "1").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl2)).To(gomega.Succeed())
+
+		wl2Key := types.NamespacedName{Name: wl2.Name, Namespace: ns.Name}
+
+		ginkgo.By("Verifying second workload is not admitted due to CPU quota")
+		gomega.Consistently(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wl2Key, wl2)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl2)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
+	})
+
+	ginkgo.It("should use exact prefix matching", func() {
+		ginkgo.By("Creating a workload with a resource that doesn't match the excluded prefix")
+		wl := utiltestingapi.MakeWorkload("prefix-test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request("networking.other.com/vpc", "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{Name: wl.Name, Namespace: ns.Name}
+
+		ginkgo.By("Verifying workload is admitted")
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			g.Expect(workload.IsAdmitted(wl)).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Verifying non-matching resource is NOT excluded (appears in resource usage)")
+		gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceName("networking.other.com/vpc")))
```

  Not entirely sure why but when I made the change to drop corev1.ResourceName here the integration test was failing.
  
  Had to revert the change here but I pushed @mbobrovskyi requests everywhere else.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-07T17:37:44Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:194** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-07T17:37:44Z
```diff
@@ -0,0 +1,196 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource("networking.other.com/vpc", "1000").
+					Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.It("should admit workload ignoring excluded resource", func() {
+		ginkgo.By("Creating a workload that requests an excluded resource")
+		wl := utiltestingapi.MakeWorkload("test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request("networking.example.com/vpc", "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload gets admitted")
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			g.Expect(workload.IsAdmitted(wl)).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Verifying excluded resource is not in admission resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		gomega.Expect(wl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey("networking.example.com/vpc"))
+	})
+
+	ginkgo.It("should ignore multiple excluded resource prefixes", func() {
+		ginkgo.By("Creating a workload that requests resources from multiple excluded prefixes")
+		wl := utiltestingapi.MakeWorkload("multi-exclude-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "2").
+			Request("networking.example.com/vpc", "10").
+			Request("storage.example.com/disk", "50").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload admission")
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			g.Expect(workload.IsAdmitted(wl)).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Verifying both excluded resources are not in resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey("networking.example.com/vpc"))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey("storage.example.com/disk"))
+	})
+
+	ginkgo.It("should enforce quota for non-excluded resources", func() {
+		ginkgo.By("Creating a workload that consumes most of the CPU quota")
+		wl1 := utiltestingapi.MakeWorkload("quota-wl-1", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "8").
+			Request("networking.example.com/vpc", "1000").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+
+		wl1Key := types.NamespacedName{Name: wl1.Name, Namespace: ns.Name}
+
+		ginkgo.By("Waiting for first workload to be admitted")
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wl1Key, wl1)).To(gomega.Succeed())
+			g.Expect(workload.IsAdmitted(wl1)).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Creating a second workload that would exceed CPU quota")
+		wl2 := utiltestingapi.MakeWorkload("quota-wl-2", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "5").
+			Request("networking.example.com/vpc", "1").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl2)).To(gomega.Succeed())
+
+		wl2Key := types.NamespacedName{Name: wl2.Name, Namespace: ns.Name}
+
+		ginkgo.By("Verifying second workload is not admitted due to CPU quota")
+		gomega.Consistently(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wl2Key, wl2)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl2)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
+	})
+
+	ginkgo.It("should use exact prefix matching", func() {
+		ginkgo.By("Creating a workload with a resource that doesn't match the excluded prefix")
+		wl := utiltestingapi.MakeWorkload("prefix-test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request("networking.other.com/vpc", "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{Name: wl.Name, Namespace: ns.Name}
+
+		ginkgo.By("Verifying workload is admitted")
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			g.Expect(workload.IsAdmitted(wl)).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Verifying non-matching resource is NOT excluded (appears in resource usage)")
+		gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceName("networking.other.com/vpc")))
```

  Yes. here it should be.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-08T03:23:03Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:41** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-08T03:23:03Z
```diff
@@ -0,0 +1,196 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
```

  ```suggestion
  	ginkgo.BeforeEach(func() {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-08T03:31:38Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:170** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-08T03:31:39Z
```diff
@@ -0,0 +1,196 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource("networking.other.com/vpc", "1000").
+					Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.It("should admit workload ignoring excluded resource", func() {
+		ginkgo.By("Creating a workload that requests an excluded resource")
+		wl := utiltestingapi.MakeWorkload("test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request("networking.example.com/vpc", "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload gets admitted")
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			g.Expect(workload.IsAdmitted(wl)).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Verifying excluded resource is not in admission resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		gomega.Expect(wl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey("networking.example.com/vpc"))
+	})
+
+	ginkgo.It("should ignore multiple excluded resource prefixes", func() {
+		ginkgo.By("Creating a workload that requests resources from multiple excluded prefixes")
+		wl := utiltestingapi.MakeWorkload("multi-exclude-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "2").
+			Request("networking.example.com/vpc", "10").
+			Request("storage.example.com/disk", "50").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload admission")
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			g.Expect(workload.IsAdmitted(wl)).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Verifying both excluded resources are not in resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey("networking.example.com/vpc"))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey("storage.example.com/disk"))
+	})
+
+	ginkgo.It("should enforce quota for non-excluded resources", func() {
+		ginkgo.By("Creating a workload that consumes most of the CPU quota")
+		wl1 := utiltestingapi.MakeWorkload("quota-wl-1", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "8").
+			Request("networking.example.com/vpc", "1000").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+
+		wl1Key := types.NamespacedName{Name: wl1.Name, Namespace: ns.Name}
+
+		ginkgo.By("Waiting for first workload to be admitted")
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wl1Key, wl1)).To(gomega.Succeed())
+			g.Expect(workload.IsAdmitted(wl1)).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Creating a second workload that would exceed CPU quota")
+		wl2 := utiltestingapi.MakeWorkload("quota-wl-2", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "5").
+			Request("networking.example.com/vpc", "1").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl2)).To(gomega.Succeed())
+
+		wl2Key := types.NamespacedName{Name: wl2.Name, Namespace: ns.Name}
+
+		ginkgo.By("Verifying second workload is not admitted due to CPU quota")
+		gomega.Consistently(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wl2Key, wl2)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl2)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
```

  ```suggestion
  		gomega.Consistently(func(g gomega.Gomega) {
  			g.Expect(k8sClient.Get(ctx, wl2Key, wl2)).To(gomega.Succeed())
  			g.Expect(workload.IsAdmitted(wl2)).Should(gomega.BeFalse())
  		}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-08T03:33:44Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:193** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-08T03:33:44Z
```diff
@@ -0,0 +1,196 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource("networking.other.com/vpc", "1000").
+					Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.It("should admit workload ignoring excluded resource", func() {
+		ginkgo.By("Creating a workload that requests an excluded resource")
+		wl := utiltestingapi.MakeWorkload("test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request("networking.example.com/vpc", "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload gets admitted")
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			g.Expect(workload.IsAdmitted(wl)).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Verifying excluded resource is not in admission resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		gomega.Expect(wl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey("networking.example.com/vpc"))
+	})
+
+	ginkgo.It("should ignore multiple excluded resource prefixes", func() {
+		ginkgo.By("Creating a workload that requests resources from multiple excluded prefixes")
+		wl := utiltestingapi.MakeWorkload("multi-exclude-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "2").
+			Request("networking.example.com/vpc", "10").
+			Request("storage.example.com/disk", "50").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload admission")
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			g.Expect(workload.IsAdmitted(wl)).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Verifying both excluded resources are not in resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey("networking.example.com/vpc"))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey("storage.example.com/disk"))
+	})
+
+	ginkgo.It("should enforce quota for non-excluded resources", func() {
+		ginkgo.By("Creating a workload that consumes most of the CPU quota")
+		wl1 := utiltestingapi.MakeWorkload("quota-wl-1", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "8").
+			Request("networking.example.com/vpc", "1000").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+
+		wl1Key := types.NamespacedName{Name: wl1.Name, Namespace: ns.Name}
+
+		ginkgo.By("Waiting for first workload to be admitted")
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wl1Key, wl1)).To(gomega.Succeed())
+			g.Expect(workload.IsAdmitted(wl1)).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Creating a second workload that would exceed CPU quota")
+		wl2 := utiltestingapi.MakeWorkload("quota-wl-2", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "5").
+			Request("networking.example.com/vpc", "1").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl2)).To(gomega.Succeed())
+
+		wl2Key := types.NamespacedName{Name: wl2.Name, Namespace: ns.Name}
+
+		ginkgo.By("Verifying second workload is not admitted due to CPU quota")
+		gomega.Consistently(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wl2Key, wl2)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl2)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
+	})
+
+	ginkgo.It("should use exact prefix matching", func() {
+		ginkgo.By("Creating a workload with a resource that doesn't match the excluded prefix")
+		wl := utiltestingapi.MakeWorkload("prefix-test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request("networking.other.com/vpc", "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{Name: wl.Name, Namespace: ns.Name}
+
+		ginkgo.By("Verifying workload is admitted")
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			g.Expect(workload.IsAdmitted(wl)).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Verifying non-matching resource is NOT excluded (appears in resource usage)")
+		gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
```

  ```suggestion
  		gomega.Expect(wl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
  		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
  ```
  nit

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-08T17:13:38Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:41** by [@kannon92](https://github.com/kannon92) — 2025-11-08T17:13:38Z
```diff
@@ -0,0 +1,196 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-08T17:13:46Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:170** by [@kannon92](https://github.com/kannon92) — 2025-11-08T17:13:46Z
```diff
@@ -0,0 +1,196 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource("networking.other.com/vpc", "1000").
+					Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.It("should admit workload ignoring excluded resource", func() {
+		ginkgo.By("Creating a workload that requests an excluded resource")
+		wl := utiltestingapi.MakeWorkload("test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request("networking.example.com/vpc", "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload gets admitted")
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			g.Expect(workload.IsAdmitted(wl)).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Verifying excluded resource is not in admission resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		gomega.Expect(wl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey("networking.example.com/vpc"))
+	})
+
+	ginkgo.It("should ignore multiple excluded resource prefixes", func() {
+		ginkgo.By("Creating a workload that requests resources from multiple excluded prefixes")
+		wl := utiltestingapi.MakeWorkload("multi-exclude-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "2").
+			Request("networking.example.com/vpc", "10").
+			Request("storage.example.com/disk", "50").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload admission")
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			g.Expect(workload.IsAdmitted(wl)).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Verifying both excluded resources are not in resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey("networking.example.com/vpc"))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey("storage.example.com/disk"))
+	})
+
+	ginkgo.It("should enforce quota for non-excluded resources", func() {
+		ginkgo.By("Creating a workload that consumes most of the CPU quota")
+		wl1 := utiltestingapi.MakeWorkload("quota-wl-1", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "8").
+			Request("networking.example.com/vpc", "1000").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+
+		wl1Key := types.NamespacedName{Name: wl1.Name, Namespace: ns.Name}
+
+		ginkgo.By("Waiting for first workload to be admitted")
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wl1Key, wl1)).To(gomega.Succeed())
+			g.Expect(workload.IsAdmitted(wl1)).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Creating a second workload that would exceed CPU quota")
+		wl2 := utiltestingapi.MakeWorkload("quota-wl-2", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "5").
+			Request("networking.example.com/vpc", "1").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl2)).To(gomega.Succeed())
+
+		wl2Key := types.NamespacedName{Name: wl2.Name, Namespace: ns.Name}
+
+		ginkgo.By("Verifying second workload is not admitted due to CPU quota")
+		gomega.Consistently(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wl2Key, wl2)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl2)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-08T17:14:30Z

- **test/integration/singlecluster/scheduler/excluderesources/scheduler_test.go:193** by [@kannon92](https://github.com/kannon92) — 2025-11-08T17:14:30Z
```diff
@@ -0,0 +1,196 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package excluderesources
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithExcludeResourcePrefixes", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		cq            *kueue.ClusterQueue
+		lq            *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		configuration := &config.Configuration{
+			Resources: &config.Resources{
+				ExcludeResourcePrefixes: []string{
+					"networking.example.com/",
+					"storage.example.com/",
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configuration))
+
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "exclude-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		cq = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource(corev1.ResourceCPU, "10").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Resource("networking.other.com/vpc", "1000").
+					Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.It("should admit workload ignoring excluded resource", func() {
+		ginkgo.By("Creating a workload that requests an excluded resource")
+		wl := utiltestingapi.MakeWorkload("test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request("networking.example.com/vpc", "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload gets admitted")
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			g.Expect(workload.IsAdmitted(wl)).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Verifying excluded resource is not in admission resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		gomega.Expect(wl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey("networking.example.com/vpc"))
+	})
+
+	ginkgo.It("should ignore multiple excluded resource prefixes", func() {
+		ginkgo.By("Creating a workload that requests resources from multiple excluded prefixes")
+		wl := utiltestingapi.MakeWorkload("multi-exclude-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "2").
+			Request("networking.example.com/vpc", "10").
+			Request("storage.example.com/disk", "50").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{
+			Name:      wl.Name,
+			Namespace: ns.Name,
+		}
+
+		ginkgo.By("Verifying workload admission")
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			g.Expect(workload.IsAdmitted(wl)).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Verifying both excluded resources are not in resource usage")
+		gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey("networking.example.com/vpc"))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey("storage.example.com/disk"))
+	})
+
+	ginkgo.It("should enforce quota for non-excluded resources", func() {
+		ginkgo.By("Creating a workload that consumes most of the CPU quota")
+		wl1 := utiltestingapi.MakeWorkload("quota-wl-1", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "8").
+			Request("networking.example.com/vpc", "1000").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+
+		wl1Key := types.NamespacedName{Name: wl1.Name, Namespace: ns.Name}
+
+		ginkgo.By("Waiting for first workload to be admitted")
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wl1Key, wl1)).To(gomega.Succeed())
+			g.Expect(workload.IsAdmitted(wl1)).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Creating a second workload that would exceed CPU quota")
+		wl2 := utiltestingapi.MakeWorkload("quota-wl-2", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "5").
+			Request("networking.example.com/vpc", "1").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl2)).To(gomega.Succeed())
+
+		wl2Key := types.NamespacedName{Name: wl2.Name, Namespace: ns.Name}
+
+		ginkgo.By("Verifying second workload is not admitted due to CPU quota")
+		gomega.Consistently(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, wl2Key, wl2)).To(gomega.Succeed())
+			return workload.IsAdmitted(wl2)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
+	})
+
+	ginkgo.It("should use exact prefix matching", func() {
+		ginkgo.By("Creating a workload with a resource that doesn't match the excluded prefix")
+		wl := utiltestingapi.MakeWorkload("prefix-test-wl", ns.Name).
+			Queue(kueue.LocalQueueName(lq.Name)).
+			Request(corev1.ResourceCPU, "1").
+			Request("networking.other.com/vpc", "100").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+		wlKey := types.NamespacedName{Name: wl.Name, Namespace: ns.Name}
+
+		ginkgo.By("Verifying workload is admitted")
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+			g.Expect(workload.IsAdmitted(wl)).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Verifying non-matching resource is NOT excluded (appears in resource usage)")
+		gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+		resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
```

  done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-26T16:40:13Z

/approve
Thank you folks 👍
