# PR #75: Add status report for kueue.Queue

**Summary**: Add status report for kueue.Queue

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/75

**Last updated**: 2022-03-02T16:21:14Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Branch**: `devel/queue_status` → `main`
- **Created**: 2022-02-25T22:01:53Z
- **Updated**: 2022-03-02T16:21:14Z
- **Merged**: 2022-03-02T16:21:14Z
- **Closed**: 2022-03-02T16:21:14Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `approved`, `kind/api-change`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 8   **+205 / -15**

## Description

Signed-off-by: Carlos Eduardo Arango Gutierrez <carangog@redhat.com>

#### What type of PR is this?
/kind feature
/kind api-change
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
initial implementation for  #5 

#### Special notes for your reviewer:

## Discussion

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-02-25T22:10:22Z

/assign @alculquicondor

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-03-02T16:19:33Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/75#pullrequestreview-897818231" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/75#" title="Author self-approved">ArangoGutierrez</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@jiwq](https://github.com/jiwq) (commented) — 2022-02-27T15:24:02Z

- **api/v1alpha1/queue_types.go:37** by [@jiwq](https://github.com/jiwq) — 2022-02-27T15:24:02Z
```diff
@@ -32,6 +32,9 @@ type CapacityReference string
 
 // QueueStatus defines the observed state of Queue
 type QueueStatus struct {
+	// PendingWorkloads is the number of workloads currently assigned to this
+	// queue not yet assigned to a Capacity.
+	PendingWorkloads int32 `json:"pendingWorkloads"`
```

  Missing the started workloads and usage, has another merge PR?

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-02-28T12:40:53Z

- **api/v1alpha1/queue_types.go:37** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-02-28T12:40:53Z
```diff
@@ -32,6 +32,9 @@ type CapacityReference string
 
 // QueueStatus defines the observed state of Queue
 type QueueStatus struct {
+	// PendingWorkloads is the number of workloads currently assigned to this
+	// queue not yet assigned to a Capacity.
+	PendingWorkloads int32 `json:"pendingWorkloads"`
```

  Will be another PR, I want this first one to lay down the status udpate to CR, with pending as the init example, a following PR will add more details to the kueue.queue.status field

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-02-28T20:35:02Z

- **pkg/controller/core/queue_controller.go:31** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-28T20:13:02Z
```diff
@@ -20,24 +20,31 @@ import (
 	"context"
 
 	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
 	"k8s.io/klog/v2"
 	ctrl "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
 
+	"sigs.k8s.io/controller-runtime/pkg/client"
```

  should be in the previous import block

- **pkg/controller/core/queue_controller.go:73** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-28T20:15:16Z
```diff
@@ -47,47 +54,119 @@ func NewQueueReconciler(queues *queue.Manager) *QueueReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=queues/finalizers,verbs=update
 
 func (r *QueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var queueObj kueue.Queue
+	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	log := ctrl.LoggerFrom(ctx).WithValues("queue", klog.KObj(&queueObj))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling Queue")
+
+	pendingJobs, err := r.queues.Status(&queueObj)
+	if err != nil {
+		r.log.Error(err, "Failed to retrieve queue status")
+		return ctrl.Result{}, err
+	}
+
+	queueObj.Status.PendingWorkloads = int32(pendingJobs)
+	if err := r.client.Status().Update(ctx, &queueObj); err != nil {
```

  check if there was a change in the object. We should not perform updates if there are no changes, to reduce load on apiserver

- **pkg/controller/core/queue_controller.go:83** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-28T20:16:22Z
```diff
@@ -47,47 +54,119 @@ func NewQueueReconciler(queues *queue.Manager) *QueueReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=queues/finalizers,verbs=update
 
 func (r *QueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var queueObj kueue.Queue
+	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	log := ctrl.LoggerFrom(ctx).WithValues("queue", klog.KObj(&queueObj))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling Queue")
+
+	pendingJobs, err := r.queues.Status(&queueObj)
+	if err != nil {
+		r.log.Error(err, "Failed to retrieve queue status")
+		return ctrl.Result{}, err
+	}
+
+	queueObj.Status.PendingWorkloads = int32(pendingJobs)
+	if err := r.client.Status().Update(ctx, &queueObj); err != nil {
+		r.log.Error(err, "Failed to Update queue")
+		return ctrl.Result{}, err
+	}
 	return ctrl.Result{}, nil
 }
 
 func (r *QueueReconciler) Create(e event.CreateEvent) bool {
-	q := e.Object.(*kueue.Queue)
+	q, match := e.Object.(*kueue.Queue)
+	if !match {
+		// No need to interact with the cache for other objects.
```

  ```suggestion
  		// No need to interact with the queue manager for other objects.
  ```
  
  same in all the instances below

- **pkg/controller/core/queue_controller.go:130** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-28T20:24:55Z
```diff
@@ -47,47 +54,119 @@ func NewQueueReconciler(queues *queue.Manager) *QueueReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=queues/finalizers,verbs=update
 
 func (r *QueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var queueObj kueue.Queue
+	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	log := ctrl.LoggerFrom(ctx).WithValues("queue", klog.KObj(&queueObj))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling Queue")
+
+	pendingJobs, err := r.queues.Status(&queueObj)
+	if err != nil {
+		r.log.Error(err, "Failed to retrieve queue status")
+		return ctrl.Result{}, err
+	}
+
+	queueObj.Status.PendingWorkloads = int32(pendingJobs)
+	if err := r.client.Status().Update(ctx, &queueObj); err != nil {
+		r.log.Error(err, "Failed to Update queue")
+		return ctrl.Result{}, err
+	}
 	return ctrl.Result{}, nil
 }
 
 func (r *QueueReconciler) Create(e event.CreateEvent) bool {
-	q := e.Object.(*kueue.Queue)
+	q, match := e.Object.(*kueue.Queue)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	log := r.log.WithValues("queue", klog.KObj(q))
 	log.V(2).Info("Queue create event")
 	ctx := logr.NewContext(context.Background(), log)
 	if err := r.queues.AddQueue(ctx, q); err != nil {
 		log.Error(err, "Failed to add queue to system")
 	}
-	return false
+	return true
 }
 
 func (r *QueueReconciler) Delete(e event.DeleteEvent) bool {
-	q := e.Object.(*kueue.Queue)
+	q, match := e.Object.(*kueue.Queue)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	r.log.V(2).Info("Queue delete event", "queue", klog.KObj(q))
 	r.queues.DeleteQueue(q)
-	return false
+	return true
 }
 
 func (r *QueueReconciler) Update(e event.UpdateEvent) bool {
-	q := e.ObjectNew.(*kueue.Queue)
+	q, match := e.ObjectNew.(*kueue.Queue)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	log := r.log.WithValues("queue", klog.KObj(q))
 	log.V(2).Info("Queue update event")
 	if err := r.queues.UpdateQueue(q); err != nil {
 		log.Error(err, "Failed to update queue in system")
 	}
-	return false
+	return true
 }
 
 func (r *QueueReconciler) Generic(e event.GenericEvent) bool {
 	r.log.V(3).Info("Ignore generic event", "obj", klog.KObj(e.Object), "kind", e.Object.GetObjectKind().GroupVersionKind())
-	return false
+	return true
+}
+
+type queueqWorkloadHandler struct{}
+
+func (h *queueqWorkloadHandler) Create(e event.CreateEvent, q workqueue.RateLimitingInterface) {
+	qw := e.Object.(*kueue.QueuedWorkload)
+	// Check that the wl has been assigned first
+	if workloadStatus(qw) != "" {
```

  ```suggestion
  	// Check that the wl has been assigned first
  	if qw.Spec.AssignedCapacity == "" {
  ```

- **pkg/controller/core/queue_controller.go:137** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-28T20:27:42Z
```diff
@@ -47,47 +54,119 @@ func NewQueueReconciler(queues *queue.Manager) *QueueReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=queues/finalizers,verbs=update
 
 func (r *QueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var queueObj kueue.Queue
+	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	log := ctrl.LoggerFrom(ctx).WithValues("queue", klog.KObj(&queueObj))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling Queue")
+
+	pendingJobs, err := r.queues.Status(&queueObj)
+	if err != nil {
+		r.log.Error(err, "Failed to retrieve queue status")
+		return ctrl.Result{}, err
+	}
+
+	queueObj.Status.PendingWorkloads = int32(pendingJobs)
+	if err := r.client.Status().Update(ctx, &queueObj); err != nil {
+		r.log.Error(err, "Failed to Update queue")
+		return ctrl.Result{}, err
+	}
 	return ctrl.Result{}, nil
 }
 
 func (r *QueueReconciler) Create(e event.CreateEvent) bool {
-	q := e.Object.(*kueue.Queue)
+	q, match := e.Object.(*kueue.Queue)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	log := r.log.WithValues("queue", klog.KObj(q))
 	log.V(2).Info("Queue create event")
 	ctx := logr.NewContext(context.Background(), log)
 	if err := r.queues.AddQueue(ctx, q); err != nil {
 		log.Error(err, "Failed to add queue to system")
 	}
-	return false
+	return true
 }
 
 func (r *QueueReconciler) Delete(e event.DeleteEvent) bool {
-	q := e.Object.(*kueue.Queue)
+	q, match := e.Object.(*kueue.Queue)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	r.log.V(2).Info("Queue delete event", "queue", klog.KObj(q))
 	r.queues.DeleteQueue(q)
-	return false
+	return true
 }
 
 func (r *QueueReconciler) Update(e event.UpdateEvent) bool {
-	q := e.ObjectNew.(*kueue.Queue)
+	q, match := e.ObjectNew.(*kueue.Queue)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	log := r.log.WithValues("queue", klog.KObj(q))
 	log.V(2).Info("Queue update event")
 	if err := r.queues.UpdateQueue(q); err != nil {
 		log.Error(err, "Failed to update queue in system")
 	}
-	return false
+	return true
 }
 
 func (r *QueueReconciler) Generic(e event.GenericEvent) bool {
 	r.log.V(3).Info("Ignore generic event", "obj", klog.KObj(e.Object), "kind", e.Object.GetObjectKind().GroupVersionKind())
-	return false
+	return true
+}
+
+type queueqWorkloadHandler struct{}
+
+func (h *queueqWorkloadHandler) Create(e event.CreateEvent, q workqueue.RateLimitingInterface) {
+	qw := e.Object.(*kueue.QueuedWorkload)
+	// Check that the wl has been assigned first
+	if workloadStatus(qw) != "" {
+		q.Add(requestForQueueStatus(qw))
+	}
+}
+
+func (h *queueqWorkloadHandler) Update(e event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldQW := e.ObjectOld.(*kueue.QueuedWorkload)
+	if workloadStatus(oldQW) != "" {
```

  If a workload transitioned from pending to assigned or finished, we need to Renconcile in all cases.
  
  If a workload switched queues, we need to update both queues.
  
  I don't think this function is doing that.

- **pkg/controller/core/queue_controller.go:148** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-28T20:28:40Z
```diff
@@ -47,47 +54,119 @@ func NewQueueReconciler(queues *queue.Manager) *QueueReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=queues/finalizers,verbs=update
 
 func (r *QueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var queueObj kueue.Queue
+	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	log := ctrl.LoggerFrom(ctx).WithValues("queue", klog.KObj(&queueObj))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling Queue")
+
+	pendingJobs, err := r.queues.Status(&queueObj)
+	if err != nil {
+		r.log.Error(err, "Failed to retrieve queue status")
+		return ctrl.Result{}, err
+	}
+
+	queueObj.Status.PendingWorkloads = int32(pendingJobs)
+	if err := r.client.Status().Update(ctx, &queueObj); err != nil {
+		r.log.Error(err, "Failed to Update queue")
+		return ctrl.Result{}, err
+	}
 	return ctrl.Result{}, nil
 }
 
 func (r *QueueReconciler) Create(e event.CreateEvent) bool {
-	q := e.Object.(*kueue.Queue)
+	q, match := e.Object.(*kueue.Queue)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	log := r.log.WithValues("queue", klog.KObj(q))
 	log.V(2).Info("Queue create event")
 	ctx := logr.NewContext(context.Background(), log)
 	if err := r.queues.AddQueue(ctx, q); err != nil {
 		log.Error(err, "Failed to add queue to system")
 	}
-	return false
+	return true
 }
 
 func (r *QueueReconciler) Delete(e event.DeleteEvent) bool {
-	q := e.Object.(*kueue.Queue)
+	q, match := e.Object.(*kueue.Queue)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	r.log.V(2).Info("Queue delete event", "queue", klog.KObj(q))
 	r.queues.DeleteQueue(q)
-	return false
+	return true
 }
 
 func (r *QueueReconciler) Update(e event.UpdateEvent) bool {
-	q := e.ObjectNew.(*kueue.Queue)
+	q, match := e.ObjectNew.(*kueue.Queue)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	log := r.log.WithValues("queue", klog.KObj(q))
 	log.V(2).Info("Queue update event")
 	if err := r.queues.UpdateQueue(q); err != nil {
 		log.Error(err, "Failed to update queue in system")
 	}
-	return false
+	return true
 }
 
 func (r *QueueReconciler) Generic(e event.GenericEvent) bool {
 	r.log.V(3).Info("Ignore generic event", "obj", klog.KObj(e.Object), "kind", e.Object.GetObjectKind().GroupVersionKind())
-	return false
+	return true
+}
+
+type queueqWorkloadHandler struct{}
+
+func (h *queueqWorkloadHandler) Create(e event.CreateEvent, q workqueue.RateLimitingInterface) {
+	qw := e.Object.(*kueue.QueuedWorkload)
+	// Check that the wl has been assigned first
+	if workloadStatus(qw) != "" {
+		q.Add(requestForQueueStatus(qw))
+	}
+}
+
+func (h *queueqWorkloadHandler) Update(e event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldQW := e.ObjectOld.(*kueue.QueuedWorkload)
+	if workloadStatus(oldQW) != "" {
+		q.Add(requestForQueueStatus(oldQW))
+	}
+	newQW := e.ObjectOld.(*kueue.QueuedWorkload)
+	if workloadStatus(newQW) != "" && workloadStatus(newQW) != workloadStatus(oldQW) {
+		q.Add(requestForQueueStatus(newQW))
+	}
+}
+
+func (h *queueqWorkloadHandler) Delete(e event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	qw := e.Object.(*kueue.QueuedWorkload)
+	if workloadStatus(qw) != "" {
```

  ```suggestion
  	if qw.Spec.AssignedCapacity == "" {
  ```

- **pkg/queue/manager.go:108** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-28T20:33:04Z
```diff
@@ -103,6 +103,13 @@ func (m *Manager) DeleteQueue(q *kueue.Queue) {
 	delete(m.queues, key)
 }
 
+func (m *Manager) Status(q *kueue.Queue) (int, error) {
```

  `m.RLock()`

- **pkg/queue/manager_test.go:227** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-28T20:34:54Z
```diff
@@ -153,6 +154,81 @@ func TestAddWorkload(t *testing.T) {
 	}
 }
 
+func TestStatus(t *testing.T) {
+	ctx := context.Background()
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %s", err)
+	}
+	now := time.Now().Truncate(time.Second)
+
+	queues := []kueue.Queue{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "foo"},
+			Spec: kueue.QueueSpec{
+				Capacity: "fooCap",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "bar"},
+			Spec: kueue.QueueSpec{
+				Capacity: "barCap",
+			},
+		},
+	}
+	workloads := []kueue.QueuedWorkload{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "a",
+				CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "foo"},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "b",
+				CreationTimestamp: metav1.NewTime(now),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "bar"},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "c",
+				CreationTimestamp: metav1.NewTime(now),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "foo"},
+		},
+	}
+	manager := NewManager(fake.NewClientBuilder().WithScheme(scheme).Build())
+	for _, q := range queues {
+		if err := manager.AddQueue(ctx, &q); err != nil {
+			t.Errorf("Failed adding queue: %s", err)
+		}
+	}
+	for _, wl := range workloads {
+		wl := wl
+		manager.AddOrUpdateWorkload(&wl)
+	}
+	wantStatus := make(map[string]int32)
+	wantStatus["foo"] = 2
+	wantStatus["bar"] = 1
+
+	status := make(map[string]int32)
+	statusFoo, err := manager.Status(&queues[0])
+	if err != nil {
+		t.Errorf("Failed retrieving queue status: %s", err)
+	}
+	status[queues[0].Name] = int32(statusFoo)
+	statusBar, err := manager.Status(&queues[1])
+	if err != nil {
+		t.Errorf("Failed retrieving queue status: %s", err)
+	}
+	status[queues[1].Name] = int32(statusBar)
+	if !reflect.DeepEqual(wantStatus, status) {
```

  Use `cmp.Diff` instead of `reflect.DeepEqual`

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-01T13:37:12Z

- **pkg/controller/core/queue_controller.go:31** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-01T13:37:12Z
```diff
@@ -20,24 +20,31 @@ import (
 	"context"
 
 	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
 	"k8s.io/klog/v2"
 	ctrl "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
 
+	"sigs.k8s.io/controller-runtime/pkg/client"
```

  curious why the vscode/go extension didn't fixed this, it usually does. fixing

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-01T14:14:21Z

- **pkg/queue/manager.go:108** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-01T14:14:20Z
```diff
@@ -103,6 +103,13 @@ func (m *Manager) DeleteQueue(q *kueue.Queue) {
 	delete(m.queues, key)
 }
 
+func (m *Manager) Status(q *kueue.Queue) (int, error) {
```

  since queue already has a defined mutex, and is needed for the cond to broadcast, I am going with it, let me know if that is not enough

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-01T17:05:36Z

- **pkg/controller/core/queue_controller.go:77** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-01T16:49:55Z
```diff
@@ -47,47 +55,124 @@ func NewQueueReconciler(queues *queue.Manager) *QueueReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=queues/finalizers,verbs=update
 
 func (r *QueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var queueObj kueue.Queue
+	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	log := ctrl.LoggerFrom(ctx).WithValues("queue", klog.KObj(&queueObj))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling Queue")
+
+	pendingJobs, err := r.queues.Status(&queueObj)
+	if err != nil {
+		r.log.Error(err, "Failed to retrieve queue status")
+		return ctrl.Result{}, err
+	}
+
+	oldStatus := queueObj.Status
+	queueObj.Status.PendingWorkloads = int32(pendingJobs)
+	if !equality.Semantic.DeepEqual(oldStatus, queueObj.Status) {
+		err := r.client.Status().Update(ctx, &queueObj)
+		r.log.Error(err, "Failed to Update queue")
```

  You can leave this out. The returned error is logged already.

- **pkg/controller/core/queue_controller.go:101** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-01T16:51:47Z
```diff
@@ -47,47 +55,124 @@ func NewQueueReconciler(queues *queue.Manager) *QueueReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=queues/finalizers,verbs=update
 
 func (r *QueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var queueObj kueue.Queue
+	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	log := ctrl.LoggerFrom(ctx).WithValues("queue", klog.KObj(&queueObj))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling Queue")
+
+	pendingJobs, err := r.queues.Status(&queueObj)
+	if err != nil {
+		r.log.Error(err, "Failed to retrieve queue status")
+		return ctrl.Result{}, err
+	}
+
+	oldStatus := queueObj.Status
+	queueObj.Status.PendingWorkloads = int32(pendingJobs)
+	if !equality.Semantic.DeepEqual(oldStatus, queueObj.Status) {
+		err := r.client.Status().Update(ctx, &queueObj)
+		r.log.Error(err, "Failed to Update queue")
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
 	return ctrl.Result{}, nil
 }
 
 func (r *QueueReconciler) Create(e event.CreateEvent) bool {
-	q := e.Object.(*kueue.Queue)
+	q, match := e.Object.(*kueue.Queue)
+	if !match {
+		// No need to interact with the queue manager for other objects.
+		return true
+	}
 	log := r.log.WithValues("queue", klog.KObj(q))
 	log.V(2).Info("Queue create event")
 	ctx := logr.NewContext(context.Background(), log)
 	if err := r.queues.AddQueue(ctx, q); err != nil {
 		log.Error(err, "Failed to add queue to system")
 	}
-	return false
+	return true
 }
 
 func (r *QueueReconciler) Delete(e event.DeleteEvent) bool {
-	q := e.Object.(*kueue.Queue)
+	q, match := e.Object.(*kueue.Queue)
+	if !match {
+		// No need to interact with the cache for other objects.
```

  queue manager :)

- **pkg/controller/core/queue_controller.go:112** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-01T16:51:54Z
```diff
@@ -47,47 +55,124 @@ func NewQueueReconciler(queues *queue.Manager) *QueueReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=queues/finalizers,verbs=update
 
 func (r *QueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var queueObj kueue.Queue
+	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	log := ctrl.LoggerFrom(ctx).WithValues("queue", klog.KObj(&queueObj))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling Queue")
+
+	pendingJobs, err := r.queues.Status(&queueObj)
+	if err != nil {
+		r.log.Error(err, "Failed to retrieve queue status")
+		return ctrl.Result{}, err
+	}
+
+	oldStatus := queueObj.Status
+	queueObj.Status.PendingWorkloads = int32(pendingJobs)
+	if !equality.Semantic.DeepEqual(oldStatus, queueObj.Status) {
+		err := r.client.Status().Update(ctx, &queueObj)
+		r.log.Error(err, "Failed to Update queue")
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
 	return ctrl.Result{}, nil
 }
 
 func (r *QueueReconciler) Create(e event.CreateEvent) bool {
-	q := e.Object.(*kueue.Queue)
+	q, match := e.Object.(*kueue.Queue)
+	if !match {
+		// No need to interact with the queue manager for other objects.
+		return true
+	}
 	log := r.log.WithValues("queue", klog.KObj(q))
 	log.V(2).Info("Queue create event")
 	ctx := logr.NewContext(context.Background(), log)
 	if err := r.queues.AddQueue(ctx, q); err != nil {
 		log.Error(err, "Failed to add queue to system")
 	}
-	return false
+	return true
 }
 
 func (r *QueueReconciler) Delete(e event.DeleteEvent) bool {
-	q := e.Object.(*kueue.Queue)
+	q, match := e.Object.(*kueue.Queue)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	r.log.V(2).Info("Queue delete event", "queue", klog.KObj(q))
 	r.queues.DeleteQueue(q)
-	return false
+	return true
 }
 
 func (r *QueueReconciler) Update(e event.UpdateEvent) bool {
-	q := e.ObjectNew.(*kueue.Queue)
+	q, match := e.ObjectNew.(*kueue.Queue)
+	if !match {
+		// No need to interact with the cache for other objects.
```

  here too

- **pkg/controller/core/queue_controller.go:132** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-01T16:52:31Z
```diff
@@ -47,47 +55,124 @@ func NewQueueReconciler(queues *queue.Manager) *QueueReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=queues/finalizers,verbs=update
 
 func (r *QueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var queueObj kueue.Queue
+	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	log := ctrl.LoggerFrom(ctx).WithValues("queue", klog.KObj(&queueObj))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling Queue")
+
+	pendingJobs, err := r.queues.Status(&queueObj)
+	if err != nil {
+		r.log.Error(err, "Failed to retrieve queue status")
+		return ctrl.Result{}, err
+	}
+
+	oldStatus := queueObj.Status
+	queueObj.Status.PendingWorkloads = int32(pendingJobs)
+	if !equality.Semantic.DeepEqual(oldStatus, queueObj.Status) {
+		err := r.client.Status().Update(ctx, &queueObj)
+		r.log.Error(err, "Failed to Update queue")
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
 	return ctrl.Result{}, nil
 }
 
 func (r *QueueReconciler) Create(e event.CreateEvent) bool {
-	q := e.Object.(*kueue.Queue)
+	q, match := e.Object.(*kueue.Queue)
+	if !match {
+		// No need to interact with the queue manager for other objects.
+		return true
+	}
 	log := r.log.WithValues("queue", klog.KObj(q))
 	log.V(2).Info("Queue create event")
 	ctx := logr.NewContext(context.Background(), log)
 	if err := r.queues.AddQueue(ctx, q); err != nil {
 		log.Error(err, "Failed to add queue to system")
 	}
-	return false
+	return true
 }
 
 func (r *QueueReconciler) Delete(e event.DeleteEvent) bool {
-	q := e.Object.(*kueue.Queue)
+	q, match := e.Object.(*kueue.Queue)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	r.log.V(2).Info("Queue delete event", "queue", klog.KObj(q))
 	r.queues.DeleteQueue(q)
-	return false
+	return true
 }
 
 func (r *QueueReconciler) Update(e event.UpdateEvent) bool {
-	q := e.ObjectNew.(*kueue.Queue)
+	q, match := e.ObjectNew.(*kueue.Queue)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	log := r.log.WithValues("queue", klog.KObj(q))
 	log.V(2).Info("Queue update event")
 	if err := r.queues.UpdateQueue(q); err != nil {
 		log.Error(err, "Failed to update queue in system")
 	}
-	return false
+	return true
 }
 
 func (r *QueueReconciler) Generic(e event.GenericEvent) bool {
 	r.log.V(3).Info("Ignore generic event", "obj", klog.KObj(e.Object), "kind", e.Object.GetObjectKind().GroupVersionKind())
-	return false
+	return true
+}
+
+type queueqWorkloadHandler struct{}
+
+func (h *queueqWorkloadHandler) Create(e event.CreateEvent, q workqueue.RateLimitingInterface) {
+	qw := e.Object.(*kueue.QueuedWorkload)
+	// Check that the wl has been assigned first
```

  hasn't

- **pkg/controller/core/queue_controller.go:142** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-01T17:01:48Z
```diff
@@ -47,47 +55,124 @@ func NewQueueReconciler(queues *queue.Manager) *QueueReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=queues/finalizers,verbs=update
 
 func (r *QueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var queueObj kueue.Queue
+	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	log := ctrl.LoggerFrom(ctx).WithValues("queue", klog.KObj(&queueObj))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling Queue")
+
+	pendingJobs, err := r.queues.Status(&queueObj)
+	if err != nil {
+		r.log.Error(err, "Failed to retrieve queue status")
+		return ctrl.Result{}, err
+	}
+
+	oldStatus := queueObj.Status
+	queueObj.Status.PendingWorkloads = int32(pendingJobs)
+	if !equality.Semantic.DeepEqual(oldStatus, queueObj.Status) {
+		err := r.client.Status().Update(ctx, &queueObj)
+		r.log.Error(err, "Failed to Update queue")
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
 	return ctrl.Result{}, nil
 }
 
 func (r *QueueReconciler) Create(e event.CreateEvent) bool {
-	q := e.Object.(*kueue.Queue)
+	q, match := e.Object.(*kueue.Queue)
+	if !match {
+		// No need to interact with the queue manager for other objects.
+		return true
+	}
 	log := r.log.WithValues("queue", klog.KObj(q))
 	log.V(2).Info("Queue create event")
 	ctx := logr.NewContext(context.Background(), log)
 	if err := r.queues.AddQueue(ctx, q); err != nil {
 		log.Error(err, "Failed to add queue to system")
 	}
-	return false
+	return true
 }
 
 func (r *QueueReconciler) Delete(e event.DeleteEvent) bool {
-	q := e.Object.(*kueue.Queue)
+	q, match := e.Object.(*kueue.Queue)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	r.log.V(2).Info("Queue delete event", "queue", klog.KObj(q))
 	r.queues.DeleteQueue(q)
-	return false
+	return true
 }
 
 func (r *QueueReconciler) Update(e event.UpdateEvent) bool {
-	q := e.ObjectNew.(*kueue.Queue)
+	q, match := e.ObjectNew.(*kueue.Queue)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	log := r.log.WithValues("queue", klog.KObj(q))
 	log.V(2).Info("Queue update event")
 	if err := r.queues.UpdateQueue(q); err != nil {
 		log.Error(err, "Failed to update queue in system")
 	}
-	return false
+	return true
 }
 
 func (r *QueueReconciler) Generic(e event.GenericEvent) bool {
 	r.log.V(3).Info("Ignore generic event", "obj", klog.KObj(e.Object), "kind", e.Object.GetObjectKind().GroupVersionKind())
-	return false
+	return true
+}
+
+type queueqWorkloadHandler struct{}
+
+func (h *queueqWorkloadHandler) Create(e event.CreateEvent, q workqueue.RateLimitingInterface) {
+	qw := e.Object.(*kueue.QueuedWorkload)
+	// Check that the wl has been assigned first
+	if qw.Spec.AssignedCapacity != "" {
+		q.Add(requestForQueueStatus(qw))
+	}
+}
+
+func (h *queueqWorkloadHandler) Update(e event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldQW := e.ObjectOld.(*kueue.QueuedWorkload)
+	newQW := e.ObjectOld.(*kueue.QueuedWorkload)
+	// If a workload transitioned from pending to assigned or finished, we need to Renconcile in all cases.
+	if newQW.Spec.AssignedCapacity != "" && workloadStatus(newQW) != workloadStatus(oldQW) {
+		q.Add(requestForQueueStatus(newQW))
+	}
+	// If a workload switched queues, we need to update both queues.
+	if newQW.Spec.QueueName != oldQW.Spec.QueueName {
+		q.Add(requestForQueueStatus(oldQW))
+		q.Add(requestForQueueStatus(newQW))
+	}
```

  I think something like this would be more accurate.
  
  ```go
  	if newQW.Spec.AssignedCapacity == "" || oldQW.Spec.AssignedCapacity == "" {
  		q.Add(requestForQueueStatus(newQW))
  		if newQW.Spec.QueueName != oldQW.Spec.QueueName {
  			q.Add(requestForQueueStatus(oldQW))
  		}
  	}
  ```
  
  But let's just Rencocile.... if nothing changed, there won't be any status update. Reconcile itself is somewhat cheap.
  
  ```go
  	q.Add(requestForQueueStatus(newQW))
  	if newQW.Spec.QueueName != oldQW.Spec.QueueName {
  		q.Add(requestForQueueStatus(oldQW))
  	}
  ```

- **pkg/queue/manager.go:108** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-01T17:03:58Z
```diff
@@ -103,6 +103,13 @@ func (m *Manager) DeleteQueue(q *kueue.Queue) {
 	delete(m.queues, key)
 }
 
+func (m *Manager) Status(q *kueue.Queue) (int, error) {
```

  yeah, we should definitely use the same lock, but can you swap it for a RWLock?

- **pkg/queue/manager_test.go:227** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-01T17:05:07Z
```diff
@@ -153,6 +154,93 @@ func TestAddWorkload(t *testing.T) {
 	}
 }
 
+func TestStatus(t *testing.T) {
+	ctx := context.Background()
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %s", err)
+	}
+	now := time.Now().Truncate(time.Second)
+
+	queues := []kueue.Queue{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "foo"},
+			Spec: kueue.QueueSpec{
+				Capacity: "fooCap",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "bar"},
+			Spec: kueue.QueueSpec{
+				Capacity: "barCap",
+			},
+		},
+	}
+	workloads := []kueue.QueuedWorkload{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "a",
+				CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "foo"},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "b",
+				CreationTimestamp: metav1.NewTime(now),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "bar"},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "c",
+				CreationTimestamp: metav1.NewTime(now),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "foo"},
+		},
+	}
+	manager := NewManager(fake.NewClientBuilder().WithScheme(scheme).Build())
+	for _, q := range queues {
+		if err := manager.AddQueue(ctx, &q); err != nil {
+			t.Errorf("Failed adding queue: %s", err)
+		}
+	}
+	for _, wl := range workloads {
+		wl := wl
+		manager.AddOrUpdateWorkload(&wl)
+	}
+	wantStatus := make(map[string]int32)
+	wantStatus["foo"] = 2
+	wantStatus["bar"] = 1
+
+	status := make(map[string]int32)
+	statusFoo, err := manager.Status(&queues[0])
+	if err != nil {
+		t.Errorf("Failed retrieving queue status: %s", err)
+	}
+	status[queues[0].Name] = int32(statusFoo)
+	statusBar, err := manager.Status(&queues[1])
+	if err != nil {
+		t.Errorf("Failed retrieving queue status: %s", err)
+	}
+	status[queues[1].Name] = int32(statusBar)
+	if diffReport := cmp.Diff(wantStatus, status); diffReport != "" {
```

  ```suggestion
  	if diff := cmp.Diff(wantStatus, status); diff != "" {
  ```
  
  Just to be consistent with the rest of the codebase

- **pkg/queue/manager_test.go:239** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-01T17:05:18Z
```diff
@@ -153,6 +154,93 @@ func TestAddWorkload(t *testing.T) {
 	}
 }
 
+func TestStatus(t *testing.T) {
+	ctx := context.Background()
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %s", err)
+	}
+	now := time.Now().Truncate(time.Second)
+
+	queues := []kueue.Queue{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "foo"},
+			Spec: kueue.QueueSpec{
+				Capacity: "fooCap",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "bar"},
+			Spec: kueue.QueueSpec{
+				Capacity: "barCap",
+			},
+		},
+	}
+	workloads := []kueue.QueuedWorkload{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "a",
+				CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "foo"},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "b",
+				CreationTimestamp: metav1.NewTime(now),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "bar"},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "c",
+				CreationTimestamp: metav1.NewTime(now),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "foo"},
+		},
+	}
+	manager := NewManager(fake.NewClientBuilder().WithScheme(scheme).Build())
+	for _, q := range queues {
+		if err := manager.AddQueue(ctx, &q); err != nil {
+			t.Errorf("Failed adding queue: %s", err)
+		}
+	}
+	for _, wl := range workloads {
+		wl := wl
+		manager.AddOrUpdateWorkload(&wl)
+	}
+	wantStatus := make(map[string]int32)
+	wantStatus["foo"] = 2
+	wantStatus["bar"] = 1
+
+	status := make(map[string]int32)
+	statusFoo, err := manager.Status(&queues[0])
+	if err != nil {
+		t.Errorf("Failed retrieving queue status: %s", err)
+	}
+	status[queues[0].Name] = int32(statusFoo)
+	statusBar, err := manager.Status(&queues[1])
+	if err != nil {
+		t.Errorf("Failed retrieving queue status: %s", err)
+	}
+	status[queues[1].Name] = int32(statusBar)
+	if diffReport := cmp.Diff(wantStatus, status); diffReport != "" {
+		t.Errorf("Status func returned wrong queue status: %s", diffReport)
+	}
+	// queueDoesntExist
+	fakeQueue := []kueue.Queue{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "fake"},
+			Spec: kueue.QueueSpec{
+				Capacity: "fakeCap",
+			},
+		},
+	}
+	if _, err = manager.Status(&fakeQueue[0]); reflect.DeepEqual(err, queueDoesntExist) {
```

  cmp.Diff

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-01T17:06:36Z

- **pkg/controller/core/queue_controller.go:77** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-01T17:06:35Z
```diff
@@ -47,47 +55,124 @@ func NewQueueReconciler(queues *queue.Manager) *QueueReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=queues/finalizers,verbs=update
 
 func (r *QueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var queueObj kueue.Queue
+	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	log := ctrl.LoggerFrom(ctx).WithValues("queue", klog.KObj(&queueObj))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling Queue")
+
+	pendingJobs, err := r.queues.Status(&queueObj)
+	if err != nil {
+		r.log.Error(err, "Failed to retrieve queue status")
+		return ctrl.Result{}, err
+	}
+
+	oldStatus := queueObj.Status
+	queueObj.Status.PendingWorkloads = int32(pendingJobs)
+	if !equality.Semantic.DeepEqual(oldStatus, queueObj.Status) {
+		err := r.client.Status().Update(ctx, &queueObj)
+		r.log.Error(err, "Failed to Update queue")
```

  a pues si ;P

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-01T17:06:46Z

- **pkg/controller/core/queue_controller.go:101** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-01T17:06:46Z
```diff
@@ -47,47 +55,124 @@ func NewQueueReconciler(queues *queue.Manager) *QueueReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=queues/finalizers,verbs=update
 
 func (r *QueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var queueObj kueue.Queue
+	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	log := ctrl.LoggerFrom(ctx).WithValues("queue", klog.KObj(&queueObj))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling Queue")
+
+	pendingJobs, err := r.queues.Status(&queueObj)
+	if err != nil {
+		r.log.Error(err, "Failed to retrieve queue status")
+		return ctrl.Result{}, err
+	}
+
+	oldStatus := queueObj.Status
+	queueObj.Status.PendingWorkloads = int32(pendingJobs)
+	if !equality.Semantic.DeepEqual(oldStatus, queueObj.Status) {
+		err := r.client.Status().Update(ctx, &queueObj)
+		r.log.Error(err, "Failed to Update queue")
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
 	return ctrl.Result{}, nil
 }
 
 func (r *QueueReconciler) Create(e event.CreateEvent) bool {
-	q := e.Object.(*kueue.Queue)
+	q, match := e.Object.(*kueue.Queue)
+	if !match {
+		// No need to interact with the queue manager for other objects.
+		return true
+	}
 	log := r.log.WithValues("queue", klog.KObj(q))
 	log.V(2).Info("Queue create event")
 	ctx := logr.NewContext(context.Background(), log)
 	if err := r.queues.AddQueue(ctx, q); err != nil {
 		log.Error(err, "Failed to add queue to system")
 	}
-	return false
+	return true
 }
 
 func (r *QueueReconciler) Delete(e event.DeleteEvent) bool {
-	q := e.Object.(*kueue.Queue)
+	q, match := e.Object.(*kueue.Queue)
+	if !match {
+		// No need to interact with the cache for other objects.
```

  grr fixing

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-01T17:07:08Z

- **pkg/queue/manager_test.go:239** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-01T17:07:07Z
```diff
@@ -153,6 +154,93 @@ func TestAddWorkload(t *testing.T) {
 	}
 }
 
+func TestStatus(t *testing.T) {
+	ctx := context.Background()
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %s", err)
+	}
+	now := time.Now().Truncate(time.Second)
+
+	queues := []kueue.Queue{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "foo"},
+			Spec: kueue.QueueSpec{
+				Capacity: "fooCap",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "bar"},
+			Spec: kueue.QueueSpec{
+				Capacity: "barCap",
+			},
+		},
+	}
+	workloads := []kueue.QueuedWorkload{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "a",
+				CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "foo"},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "b",
+				CreationTimestamp: metav1.NewTime(now),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "bar"},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "c",
+				CreationTimestamp: metav1.NewTime(now),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "foo"},
+		},
+	}
+	manager := NewManager(fake.NewClientBuilder().WithScheme(scheme).Build())
+	for _, q := range queues {
+		if err := manager.AddQueue(ctx, &q); err != nil {
+			t.Errorf("Failed adding queue: %s", err)
+		}
+	}
+	for _, wl := range workloads {
+		wl := wl
+		manager.AddOrUpdateWorkload(&wl)
+	}
+	wantStatus := make(map[string]int32)
+	wantStatus["foo"] = 2
+	wantStatus["bar"] = 1
+
+	status := make(map[string]int32)
+	statusFoo, err := manager.Status(&queues[0])
+	if err != nil {
+		t.Errorf("Failed retrieving queue status: %s", err)
+	}
+	status[queues[0].Name] = int32(statusFoo)
+	statusBar, err := manager.Status(&queues[1])
+	if err != nil {
+		t.Errorf("Failed retrieving queue status: %s", err)
+	}
+	status[queues[1].Name] = int32(statusBar)
+	if diffReport := cmp.Diff(wantStatus, status); diffReport != "" {
+		t.Errorf("Status func returned wrong queue status: %s", diffReport)
+	}
+	// queueDoesntExist
+	fakeQueue := []kueue.Queue{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "fake"},
+			Spec: kueue.QueueSpec{
+				Capacity: "fakeCap",
+			},
+		},
+	}
+	if _, err = manager.Status(&fakeQueue[0]); reflect.DeepEqual(err, queueDoesntExist) {
```

  ok, will change

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-01T22:15:37Z

- **pkg/controller/core/queue_controller.go:131** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-01T22:06:41Z
```diff
@@ -47,47 +55,118 @@ func NewQueueReconciler(queues *queue.Manager) *QueueReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=queues/finalizers,verbs=update
 
 func (r *QueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var queueObj kueue.Queue
+	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	log := ctrl.LoggerFrom(ctx).WithValues("queue", klog.KObj(&queueObj))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling Queue")
+
+	pendingJobs, err := r.queues.Status(&queueObj)
+	if err != nil {
+		r.log.Error(err, "Failed to retrieve queue status")
+		return ctrl.Result{}, err
+	}
+
+	oldStatus := queueObj.Status
+	queueObj.Status.PendingWorkloads = int32(pendingJobs)
+	if !equality.Semantic.DeepEqual(oldStatus, queueObj.Status) {
+		err := r.client.Status().Update(ctx, &queueObj)
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
 	return ctrl.Result{}, nil
 }
 
 func (r *QueueReconciler) Create(e event.CreateEvent) bool {
-	q := e.Object.(*kueue.Queue)
+	q, match := e.Object.(*kueue.Queue)
+	if !match {
+		// No need to interact with the queue manager for other objects.
+		return true
+	}
 	log := r.log.WithValues("queue", klog.KObj(q))
 	log.V(2).Info("Queue create event")
 	ctx := logr.NewContext(context.Background(), log)
 	if err := r.queues.AddQueue(ctx, q); err != nil {
 		log.Error(err, "Failed to add queue to system")
 	}
-	return false
+	return true
 }
 
 func (r *QueueReconciler) Delete(e event.DeleteEvent) bool {
-	q := e.Object.(*kueue.Queue)
+	q, match := e.Object.(*kueue.Queue)
+	if !match {
+		// No need to interact with the queue manager for other objects.
+		return true
+	}
 	r.log.V(2).Info("Queue delete event", "queue", klog.KObj(q))
 	r.queues.DeleteQueue(q)
-	return false
+	return true
 }
 
 func (r *QueueReconciler) Update(e event.UpdateEvent) bool {
-	q := e.ObjectNew.(*kueue.Queue)
+	q, match := e.ObjectNew.(*kueue.Queue)
+	if !match {
+		// No need to interact with the queue manager for other objects.
+		return true
+	}
 	log := r.log.WithValues("queue", klog.KObj(q))
 	log.V(2).Info("Queue update event")
 	if err := r.queues.UpdateQueue(q); err != nil {
 		log.Error(err, "Failed to update queue in system")
 	}
-	return false
+	return true
 }
 
 func (r *QueueReconciler) Generic(e event.GenericEvent) bool {
 	r.log.V(3).Info("Ignore generic event", "obj", klog.KObj(e.Object), "kind", e.Object.GetObjectKind().GroupVersionKind())
-	return false
+	return true
+}
+
+type queueqWorkloadHandler struct{}
+
+func (h *queueqWorkloadHandler) Create(e event.CreateEvent, q workqueue.RateLimitingInterface) {
+	qw := e.Object.(*kueue.QueuedWorkload)
+	// Check that the wl hasn't been assigned first
```

  I guess we can just remove the comment. The code is pretty clear.

- **pkg/queue/manager.go:108** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-01T22:07:16Z
```diff
@@ -103,6 +103,13 @@ func (m *Manager) DeleteQueue(q *kueue.Queue) {
 	delete(m.queues, key)
 }
 
+func (m *Manager) Status(q *kueue.Queue) (int, error) {
```

  Did you check if this was possible?

- **pkg/queue/manager.go:35** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-01T22:08:49Z
```diff
@@ -30,7 +30,10 @@ import (
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-const workloadQueueKey = "spec.queueName"
+const (
+	workloadQueueKey = "spec.queueName"
+	queueDoesntExist = "queue doesn't exist"
```

  you can store the error instead
  
  ```go
  	var queueDoesntExistErr = errors.New("queue doesn't exist")
  ```

- **pkg/queue/manager_test.go:213** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-01T22:12:13Z
```diff
@@ -153,6 +153,94 @@ func TestAddWorkload(t *testing.T) {
 	}
 }
 
+func TestStatus(t *testing.T) {
+	ctx := context.Background()
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %s", err)
+	}
+	now := time.Now().Truncate(time.Second)
+
+	queues := []kueue.Queue{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "foo"},
+			Spec: kueue.QueueSpec{
+				Capacity: "fooCap",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "bar"},
+			Spec: kueue.QueueSpec{
+				Capacity: "barCap",
+			},
+		},
+	}
+	workloads := []kueue.QueuedWorkload{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "a",
+				CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "foo"},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "b",
+				CreationTimestamp: metav1.NewTime(now),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "bar"},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "c",
+				CreationTimestamp: metav1.NewTime(now),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "foo"},
+		},
+	}
+	manager := NewManager(fake.NewClientBuilder().WithScheme(scheme).Build())
+	for _, q := range queues {
+		if err := manager.AddQueue(ctx, &q); err != nil {
+			t.Errorf("Failed adding queue: %s", err)
+		}
+	}
+	for _, wl := range workloads {
+		wl := wl
+		manager.AddOrUpdateWorkload(&wl)
+	}
+	wantStatus := make(map[string]int32)
+	wantStatus["foo"] = 2
+	wantStatus["bar"] = 1
```

  ```suggestion
  	wantStatus := map[string]int32{
  		"foo": 2,
  		"bar": 1,
  	}
  ```

- **pkg/queue/manager_test.go:225** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-01T22:12:49Z
```diff
@@ -153,6 +153,94 @@ func TestAddWorkload(t *testing.T) {
 	}
 }
 
+func TestStatus(t *testing.T) {
+	ctx := context.Background()
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %s", err)
+	}
+	now := time.Now().Truncate(time.Second)
+
+	queues := []kueue.Queue{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "foo"},
+			Spec: kueue.QueueSpec{
+				Capacity: "fooCap",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "bar"},
+			Spec: kueue.QueueSpec{
+				Capacity: "barCap",
+			},
+		},
+	}
+	workloads := []kueue.QueuedWorkload{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "a",
+				CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "foo"},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "b",
+				CreationTimestamp: metav1.NewTime(now),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "bar"},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "c",
+				CreationTimestamp: metav1.NewTime(now),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "foo"},
+		},
+	}
+	manager := NewManager(fake.NewClientBuilder().WithScheme(scheme).Build())
+	for _, q := range queues {
+		if err := manager.AddQueue(ctx, &q); err != nil {
+			t.Errorf("Failed adding queue: %s", err)
+		}
+	}
+	for _, wl := range workloads {
+		wl := wl
+		manager.AddOrUpdateWorkload(&wl)
+	}
+	wantStatus := make(map[string]int32)
+	wantStatus["foo"] = 2
+	wantStatus["bar"] = 1
+
+	status := make(map[string]int32)
+	statusFoo, err := manager.Status(&queues[0])
+	if err != nil {
+		t.Errorf("Failed retrieving queue status: %s", err)
+	}
+	status[queues[0].Name] = int32(statusFoo)
+	statusBar, err := manager.Status(&queues[1])
+	if err != nil {
+		t.Errorf("Failed retrieving queue status: %s", err)
+	}
+	status[queues[1].Name] = int32(statusBar)
```

  replace with a for loop, because it will be easier to read and to expand in the future.
  
  Although, alternatively, you can replace this with cases and do a `t.Run` for each. Similar to https://github.com/kubernetes-sigs/kueue/blob/6f36b63cf2d5765aff2f65e56fbb8f8fd34c1c69/pkg/capacity/capacity_test.go#L643-L716

- **pkg/queue/manager_test.go:235** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-01T22:13:34Z
```diff
@@ -153,6 +153,94 @@ func TestAddWorkload(t *testing.T) {
 	}
 }
 
+func TestStatus(t *testing.T) {
+	ctx := context.Background()
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %s", err)
+	}
+	now := time.Now().Truncate(time.Second)
+
+	queues := []kueue.Queue{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "foo"},
+			Spec: kueue.QueueSpec{
+				Capacity: "fooCap",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "bar"},
+			Spec: kueue.QueueSpec{
+				Capacity: "barCap",
+			},
+		},
+	}
+	workloads := []kueue.QueuedWorkload{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "a",
+				CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "foo"},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "b",
+				CreationTimestamp: metav1.NewTime(now),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "bar"},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "c",
+				CreationTimestamp: metav1.NewTime(now),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "foo"},
+		},
+	}
+	manager := NewManager(fake.NewClientBuilder().WithScheme(scheme).Build())
+	for _, q := range queues {
+		if err := manager.AddQueue(ctx, &q); err != nil {
+			t.Errorf("Failed adding queue: %s", err)
+		}
+	}
+	for _, wl := range workloads {
+		wl := wl
+		manager.AddOrUpdateWorkload(&wl)
+	}
+	wantStatus := make(map[string]int32)
+	wantStatus["foo"] = 2
+	wantStatus["bar"] = 1
+
+	status := make(map[string]int32)
+	statusFoo, err := manager.Status(&queues[0])
+	if err != nil {
+		t.Errorf("Failed retrieving queue status: %s", err)
+	}
+	status[queues[0].Name] = int32(statusFoo)
+	statusBar, err := manager.Status(&queues[1])
+	if err != nil {
+		t.Errorf("Failed retrieving queue status: %s", err)
+	}
+	status[queues[1].Name] = int32(statusBar)
+	if diff := cmp.Diff(wantStatus, status); diff != "" {
+		t.Errorf("Status func returned wrong queue status: %s", diff)
+	}
+	// queueDoesntExist
+	fakeQueue := []kueue.Queue{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "fake"},
+			Spec: kueue.QueueSpec{
+				Capacity: "fakeCap",
+			},
```

  you can omit this, as it doesn't really matter.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-02T14:14:20Z

- **pkg/controller/core/queue_controller.go:127** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-02T14:10:59Z
```diff
@@ -47,47 +55,117 @@ func NewQueueReconciler(queues *queue.Manager) *QueueReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=queues/finalizers,verbs=update
 
 func (r *QueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var queueObj kueue.Queue
+	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	log := ctrl.LoggerFrom(ctx).WithValues("queue", klog.KObj(&queueObj))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling Queue")
+
+	pendingJobs, err := r.queues.Status(&queueObj)
+	if err != nil {
+		r.log.Error(err, "Failed to retrieve queue status")
+		return ctrl.Result{}, err
+	}
+
+	oldStatus := queueObj.Status
+	queueObj.Status.PendingWorkloads = int32(pendingJobs)
+	if !equality.Semantic.DeepEqual(oldStatus, queueObj.Status) {
+		err := r.client.Status().Update(ctx, &queueObj)
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
 	return ctrl.Result{}, nil
 }
 
 func (r *QueueReconciler) Create(e event.CreateEvent) bool {
-	q := e.Object.(*kueue.Queue)
+	q, match := e.Object.(*kueue.Queue)
+	if !match {
+		// No need to interact with the queue manager for other objects.
+		return true
+	}
 	log := r.log.WithValues("queue", klog.KObj(q))
 	log.V(2).Info("Queue create event")
 	ctx := logr.NewContext(context.Background(), log)
 	if err := r.queues.AddQueue(ctx, q); err != nil {
 		log.Error(err, "Failed to add queue to system")
 	}
-	return false
+	return true
 }
 
 func (r *QueueReconciler) Delete(e event.DeleteEvent) bool {
-	q := e.Object.(*kueue.Queue)
+	q, match := e.Object.(*kueue.Queue)
+	if !match {
+		// No need to interact with the queue manager for other objects.
+		return true
+	}
 	r.log.V(2).Info("Queue delete event", "queue", klog.KObj(q))
 	r.queues.DeleteQueue(q)
-	return false
+	return true
 }
 
 func (r *QueueReconciler) Update(e event.UpdateEvent) bool {
-	q := e.ObjectNew.(*kueue.Queue)
+	q, match := e.ObjectNew.(*kueue.Queue)
+	if !match {
+		// No need to interact with the queue manager for other objects.
+		return true
+	}
 	log := r.log.WithValues("queue", klog.KObj(q))
 	log.V(2).Info("Queue update event")
 	if err := r.queues.UpdateQueue(q); err != nil {
 		log.Error(err, "Failed to update queue in system")
 	}
-	return false
+	return true
 }
 
 func (r *QueueReconciler) Generic(e event.GenericEvent) bool {
 	r.log.V(3).Info("Ignore generic event", "obj", klog.KObj(e.Object), "kind", e.Object.GetObjectKind().GroupVersionKind())
-	return false
+	return true
+}
+
+type queueqWorkloadHandler struct{}
```

  oops, just noticed the typo here :)
  ```suggestion
  type queuedWorkloadHandler struct{}
  ```

- **pkg/queue/manager_test.go:213** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-02T14:13:16Z
```diff
@@ -153,6 +153,98 @@ func TestAddWorkload(t *testing.T) {
 	}
 }
 
+func TestStatus(t *testing.T) {
+	ctx := context.Background()
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %s", err)
+	}
+	now := time.Now().Truncate(time.Second)
+
+	queues := []kueue.Queue{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "foo"},
+			Spec: kueue.QueueSpec{
+				Capacity: "fooCap",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "bar"},
+			Spec: kueue.QueueSpec{
+				Capacity: "barCap",
+			},
+		},
+	}
+	workloads := []kueue.QueuedWorkload{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "a",
+				CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "foo"},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "b",
+				CreationTimestamp: metav1.NewTime(now),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "bar"},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "c",
+				CreationTimestamp: metav1.NewTime(now),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "foo"},
+		},
+	}
+	manager := NewManager(fake.NewClientBuilder().WithScheme(scheme).Build())
+	for _, q := range queues {
+		if err := manager.AddQueue(ctx, &q); err != nil {
+			t.Errorf("Failed adding queue: %s", err)
+		}
+	}
+	for _, wl := range workloads {
+		wl := wl
+		manager.AddOrUpdateWorkload(&wl)
+	}
+
+	cases := map[string]struct {
+		queue      int
```

  (1/3) replace this with `*kueue.Queue`

- **pkg/queue/manager_test.go:217** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-02T14:13:31Z
```diff
@@ -153,6 +153,98 @@ func TestAddWorkload(t *testing.T) {
 	}
 }
 
+func TestStatus(t *testing.T) {
+	ctx := context.Background()
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %s", err)
+	}
+	now := time.Now().Truncate(time.Second)
+
+	queues := []kueue.Queue{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "foo"},
+			Spec: kueue.QueueSpec{
+				Capacity: "fooCap",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "bar"},
+			Spec: kueue.QueueSpec{
+				Capacity: "barCap",
+			},
+		},
+	}
+	workloads := []kueue.QueuedWorkload{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "a",
+				CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "foo"},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "b",
+				CreationTimestamp: metav1.NewTime(now),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "bar"},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "c",
+				CreationTimestamp: metav1.NewTime(now),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "foo"},
+		},
+	}
+	manager := NewManager(fake.NewClientBuilder().WithScheme(scheme).Build())
+	for _, q := range queues {
+		if err := manager.AddQueue(ctx, &q); err != nil {
+			t.Errorf("Failed adding queue: %s", err)
+		}
+	}
+	for _, wl := range workloads {
+		wl := wl
+		manager.AddOrUpdateWorkload(&wl)
+	}
+
+	cases := map[string]struct {
+		queue      int
+		wantStatus int
+	}{
+		"foo": {
+			queue:      0,
```

  (2/3) Then you can do `queue: &queues[0]`

- **pkg/queue/manager_test.go:238** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-02T14:14:15Z
```diff
@@ -153,6 +153,98 @@ func TestAddWorkload(t *testing.T) {
 	}
 }
 
+func TestStatus(t *testing.T) {
+	ctx := context.Background()
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %s", err)
+	}
+	now := time.Now().Truncate(time.Second)
+
+	queues := []kueue.Queue{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "foo"},
+			Spec: kueue.QueueSpec{
+				Capacity: "fooCap",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "bar"},
+			Spec: kueue.QueueSpec{
+				Capacity: "barCap",
+			},
+		},
+	}
+	workloads := []kueue.QueuedWorkload{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "a",
+				CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "foo"},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "b",
+				CreationTimestamp: metav1.NewTime(now),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "bar"},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:              "c",
+				CreationTimestamp: metav1.NewTime(now),
+			},
+			Spec: kueue.QueuedWorkloadSpec{QueueName: "foo"},
+		},
+	}
+	manager := NewManager(fake.NewClientBuilder().WithScheme(scheme).Build())
+	for _, q := range queues {
+		if err := manager.AddQueue(ctx, &q); err != nil {
+			t.Errorf("Failed adding queue: %s", err)
+		}
+	}
+	for _, wl := range workloads {
+		wl := wl
+		manager.AddOrUpdateWorkload(&wl)
+	}
+
+	cases := map[string]struct {
+		queue      int
+		wantStatus int
+	}{
+		"foo": {
+			queue:      0,
+			wantStatus: 2,
+		},
+		"bar": {
+			queue:      1,
+			wantStatus: 1,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			status, err := manager.Status(&queues[tc.queue])
+			if err != nil {
+				t.Errorf("Failed retrieving queue status: %s", err)
+			}
+			if diff := cmp.Diff(tc.wantStatus, status); diff != "" {
+				t.Errorf("Status func returned wrong queue status: %s", diff)
+			}
+		})
+	}
+
+	// queueDoesntExist
+	fakeQueue := []kueue.Queue{
```

  (3/3) and you can put this as part of the cases :)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-02T16:19:18Z

/lgtm
/approve


Please work on an integration test next
