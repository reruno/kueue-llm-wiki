# PR #7522: Add preemptor and preemptee paths to the Preemption message

**Summary**: Add preemptor and preemptee paths to the Preemption message

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7522

**Last updated**: 2025-11-28T08:37:40Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `feat/7067-more-details-infairsharing` → `main`
- **Created**: 2025-11-04T11:41:05Z
- **Updated**: 2025-11-28T08:37:40Z
- **Merged**: 2025-11-05T20:32:52Z
- **Closed**: 2025-11-05T20:32:52Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mbobrovskyi](https://github.com/mbobrovskyi), [@gabesaba](https://github.com/gabesaba), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 9   **+261 / -222**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #7067

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Observability: improve the message for the Preempted condition: include preemptor and preemptee object paths to make it easier to locate the objects involved in a preemption.
Before: "Preempted to accommodate a workload (UID: wl-in, JobUID: job-in) due to reclamation within the cohort"
After: "Preempted to accommodate a workload (UID: wl-in, JobUID: job-in) due to reclamation within the cohort; preemptor path: /r/c/q; preemptee path: /r/q_borrowing"
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-04T11:41:08Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-11-04T11:41:14Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | ebf561ffe26ac36621fa65436774c9093642d138 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/690b8a7b36ed31000800b194 |

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-11-04T13:23:00Z

/ok-to-test

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-11-05T11:29:47Z

/cc @mbobrovskyi

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-05T14:06:15Z

This needs release note as the change is user facing. Condition messages and events are more informative. feel free to even give an example

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-05T17:40:54Z

thank you 👍 
/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-05T17:41:02Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7522#issuecomment-3492538838" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7522#" title="Author self-approved">mszadkow</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-05T17:41:03Z

LGTM label has been added.  <details>Git tree hash: 2b224c66debd6f8eb3d7b89e05959b9a26d09652</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-05T19:42:41Z

/test pull-kueue-test-e2e-kueueviz-main
looks like unrelated

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-28T08:27:05Z

/release-note-edit
```release-note
Improve the message for the Preempted condition: include preemptor and preemptee object paths to make it easier to locate the objects involved in a preemption.
Before: "Preempted to accommodate a workload (UID: wl-in, JobUID: job-in) due to reclamation within the cohort"
After: "Preempted to accommodate a workload (UID: wl-in, JobUID: job-in) due to reclamation within the cohort; preemptor path: /r/c/q; preemptee path: /r/q_borrowing"
```

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-28T08:37:38Z

/release-note-edit
```release-note
Observability: improve the message for the Preempted condition: include preemptor and preemptee object paths to make it easier to locate the objects involved in a preemption.
Before: "Preempted to accommodate a workload (UID: wl-in, JobUID: job-in) due to reclamation within the cohort"
After: "Preempted to accommodate a workload (UID: wl-in, JobUID: job-in) due to reclamation within the cohort; preemptor path: /r/c/q; preemptee path: /r/q_borrowing"
```

## Reviews

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-11-04T13:43:44Z

- **pkg/scheduler/preemption/preemption.go:169** by [@mszadkow](https://github.com/mszadkow) — 2025-11-04T13:43:44Z
```diff
@@ -154,12 +155,22 @@ func preemptionMessage(preemptor *kueue.Workload, reason string) string {
 	} else {
 		jUID = uid
 	}
+	if preemptorPath == "" {
+		ptor = "UNKNOWN"
+	} else {
+		ptor = preemptorPath
+	}
+	if preempteePath == "" {
+		ptee = "UNKNOWN"
+	} else {
+		ptee = preempteePath
+	}
 
-	return fmt.Sprintf("Preempted to accommodate a workload (UID: %s, JobUID: %s) due to %s", wUID, jUID, HumanReadablePreemptionReasons[reason])
+	return fmt.Sprintf("Preempted to accommodate a workload (UID: %s, JobUID: %s) due to %s; preemptor path: %s; preemptee path: %s", wUID, jUID, HumanReadablePreemptionReasons[reason], ptor, ptee)
```

  @mimowo @mwysokin is that an acceptable solution?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-05T10:08:14Z

- **pkg/scheduler/preemption/preemption.go:197** by [@mimowo](https://github.com/mimowo) — 2025-11-05T10:08:14Z
```diff
@@ -168,7 +179,23 @@ func (p *Preemptor) IssuePreemptions(ctx context.Context, preemptor *workload.In
 	workqueue.ParallelizeUntil(ctx, parallelPreemptions, len(targets), func(i int) {
 		target := targets[i]
 		if !meta.IsStatusConditionTrue(target.WorkloadInfo.Obj.Status.Conditions, kueue.WorkloadEvicted) {
-			message := preemptionMessage(preemptor.Obj, target.Reason)
+			preemptorPath := "/" + string(preemptor.ClusterQueue)
+			if snap.HasParent() {
+				ancestors := snap.PathParentToRoot()
+				for a := range ancestors {
+					preemptorPath = "/" + string(a.GetName()) + preemptorPath
+				}
+			}
+
+			preempteePath := "/" + string(target.WorkloadInfo.ClusterQueue)
+			if target.WorkloadCq != nil && target.WorkloadCq.HasParent() {
+				ancestors := target.WorkloadCq.PathParentToRoot()
+				for a := range ancestors {
+					preempteePath = "/" + string(a.GetName()) + preempteePath
+				}
+			}
+
```

  Please de-duplicate the logic, and use something like strings.Join(parts, "/") for readability

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-05T10:08:46Z

- **pkg/scheduler/preemption/preemption.go:169** by [@mimowo](https://github.com/mimowo) — 2025-11-05T10:08:45Z
```diff
@@ -154,12 +155,22 @@ func preemptionMessage(preemptor *kueue.Workload, reason string) string {
 	} else {
 		jUID = uid
 	}
+	if preemptorPath == "" {
+		ptor = "UNKNOWN"
+	} else {
+		ptor = preemptorPath
+	}
+	if preempteePath == "" {
+		ptee = "UNKNOWN"
+	} else {
+		ptee = preempteePath
+	}
 
-	return fmt.Sprintf("Preempted to accommodate a workload (UID: %s, JobUID: %s) due to %s", wUID, jUID, HumanReadablePreemptionReasons[reason])
+	return fmt.Sprintf("Preempted to accommodate a workload (UID: %s, JobUID: %s) due to %s; preemptor path: %s; preemptee path: %s", wUID, jUID, HumanReadablePreemptionReasons[reason], ptor, ptee)
```

  LGTM

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-05T10:25:38Z

- **pkg/scheduler/preemption/preemption.go:167** by [@mimowo](https://github.com/mimowo) — 2025-11-05T10:25:38Z
```diff
@@ -154,12 +155,22 @@ func preemptionMessage(preemptor *kueue.Workload, reason string) string {
 	} else {
 		jUID = uid
 	}
+	if preemptorPath == "" {
+		ptor = "UNKNOWN"
+	} else {
+		ptor = preemptorPath
+	}
+	if preempteePath == "" {
+		ptee = "UNKNOWN"
+	} else {
+		ptee = preempteePath
+	}
```

  hm, can we compactify we `preemptorMsgPath := cmp.Or(preemptorPath, "UNKNOWN")`? 
  
  If this works please also submit a preparatory cleanup PR for the code above

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-11-05T11:09:43Z

- **pkg/scheduler/preemption/preemption.go:197** by [@mszadkow](https://github.com/mszadkow) — 2025-11-05T11:09:43Z
```diff
@@ -168,7 +179,23 @@ func (p *Preemptor) IssuePreemptions(ctx context.Context, preemptor *workload.In
 	workqueue.ParallelizeUntil(ctx, parallelPreemptions, len(targets), func(i int) {
 		target := targets[i]
 		if !meta.IsStatusConditionTrue(target.WorkloadInfo.Obj.Status.Conditions, kueue.WorkloadEvicted) {
-			message := preemptionMessage(preemptor.Obj, target.Reason)
+			preemptorPath := "/" + string(preemptor.ClusterQueue)
+			if snap.HasParent() {
+				ancestors := snap.PathParentToRoot()
+				for a := range ancestors {
+					preemptorPath = "/" + string(a.GetName()) + preemptorPath
+				}
+			}
+
+			preempteePath := "/" + string(target.WorkloadInfo.ClusterQueue)
+			if target.WorkloadCq != nil && target.WorkloadCq.HasParent() {
+				ancestors := target.WorkloadCq.PathParentToRoot()
+				for a := range ancestors {
+					preempteePath = "/" + string(a.GetName()) + preempteePath
+				}
+			}
+
```

  done

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-11-05T11:09:48Z

- **pkg/scheduler/preemption/preemption.go:167** by [@mszadkow](https://github.com/mszadkow) — 2025-11-05T11:09:48Z
```diff
@@ -154,12 +155,22 @@ func preemptionMessage(preemptor *kueue.Workload, reason string) string {
 	} else {
 		jUID = uid
 	}
+	if preemptorPath == "" {
+		ptor = "UNKNOWN"
+	} else {
+		ptor = preemptorPath
+	}
+	if preempteePath == "" {
+		ptee = "UNKNOWN"
+	} else {
+		ptee = preempteePath
+	}
```

  done

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-11-05T11:09:54Z

- **pkg/scheduler/preemption/preemption.go:169** by [@mszadkow](https://github.com/mszadkow) — 2025-11-05T11:09:54Z
```diff
@@ -154,12 +155,22 @@ func preemptionMessage(preemptor *kueue.Workload, reason string) string {
 	} else {
 		jUID = uid
 	}
+	if preemptorPath == "" {
+		ptor = "UNKNOWN"
+	} else {
+		ptor = preemptorPath
+	}
+	if preempteePath == "" {
+		ptee = "UNKNOWN"
+	} else {
+		ptee = preempteePath
+	}
 
-	return fmt.Sprintf("Preempted to accommodate a workload (UID: %s, JobUID: %s) due to %s", wUID, jUID, HumanReadablePreemptionReasons[reason])
+	return fmt.Sprintf("Preempted to accommodate a workload (UID: %s, JobUID: %s) due to %s; preemptor path: %s; preemptee path: %s", wUID, jUID, HumanReadablePreemptionReasons[reason], ptor, ptee)
```

  thanks

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-05T13:47:26Z

- **pkg/scheduler/preemption/preemption.go:533** by [@mimowo](https://github.com/mimowo) — 2025-11-05T13:47:26Z
```diff
@@ -512,3 +525,18 @@ func queueUnderNominalInResourcesNeedingPreemption(preemptionCtx *preemptionCtx)
 	}
 	return true
 }
+
+// buildCQPath constructs a path like "/parent/.../cq" for a given ClusterQueue snapshot.
+func buildCQPath(cqName string, cqSnap *schdcache.ClusterQueueSnapshot) string {
+	if cqSnap == nil || !cqSnap.HasParent() {
+		return "/" + cqName
+	}
```

  Is it really needed or just performance optimization?

### Review by [@mwysokin](https://github.com/mwysokin) (commented) — 2025-11-05T14:29:05Z

- **pkg/scheduler/preemption/preemption.go:169** by [@mwysokin](https://github.com/mwysokin) — 2025-11-05T14:29:04Z
```diff
@@ -154,12 +155,22 @@ func preemptionMessage(preemptor *kueue.Workload, reason string) string {
 	} else {
 		jUID = uid
 	}
+	if preemptorPath == "" {
+		ptor = "UNKNOWN"
+	} else {
+		ptor = preemptorPath
+	}
+	if preempteePath == "" {
+		ptee = "UNKNOWN"
+	} else {
+		ptee = preempteePath
+	}
 
-	return fmt.Sprintf("Preempted to accommodate a workload (UID: %s, JobUID: %s) due to %s", wUID, jUID, HumanReadablePreemptionReasons[reason])
+	return fmt.Sprintf("Preempted to accommodate a workload (UID: %s, JobUID: %s) due to %s; preemptor path: %s; preemptee path: %s", wUID, jUID, HumanReadablePreemptionReasons[reason], ptor, ptee)
```

  LGTM

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-11-05T17:33:21Z

- **pkg/scheduler/preemption/preemption.go:533** by [@mszadkow](https://github.com/mszadkow) — 2025-11-05T17:33:21Z
```diff
@@ -512,3 +525,18 @@ func queueUnderNominalInResourcesNeedingPreemption(preemptionCtx *preemptionCtx)
 	}
 	return true
 }
+
+// buildCQPath constructs a path like "/parent/.../cq" for a given ClusterQueue snapshot.
+func buildCQPath(cqName string, cqSnap *schdcache.ClusterQueueSnapshot) string {
+	if cqSnap == nil || !cqSnap.HasParent() {
+		return "/" + cqName
+	}
```

  optimisation, but well could be deleted to simplify

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-11-05T17:36:48Z

- **pkg/scheduler/preemption/preemption.go:533** by [@mszadkow](https://github.com/mszadkow) — 2025-11-05T17:36:48Z
```diff
@@ -512,3 +525,18 @@ func queueUnderNominalInResourcesNeedingPreemption(preemptionCtx *preemptionCtx)
 	}
 	return true
 }
+
+// buildCQPath constructs a path like "/parent/.../cq" for a given ClusterQueue snapshot.
+func buildCQPath(cqName string, cqSnap *schdcache.ClusterQueueSnapshot) string {
+	if cqSnap == nil || !cqSnap.HasParent() {
+		return "/" + cqName
+	}
```

  removed
