# PR #7533: Add documentation for Kubeflow Trainer v2 TrainJob integration with Kueue

**Summary**: Add documentation for Kubeflow Trainer v2 TrainJob integration with Kueue

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7533

**Last updated**: 2025-11-07T09:31:36Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@NarayanaSabari](https://github.com/NarayanaSabari)
- **Branch**: `kf-trainer-docs-v2` → `main`
- **Created**: 2025-11-05T05:16:48Z
- **Updated**: 2025-11-07T09:31:36Z
- **Merged**: 2025-11-07T09:30:56Z
- **Closed**: 2025-11-07T09:30:56Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `sig/scheduling`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 5   **+385 / -22**

## Description

#### What type of PR is this?

/kind documentation
/sig scheduling
/area batch

#### What this PR does / why we need it:

This PR adds comprehensive documentation for using Kubeflow Trainer v2 TrainJob with Kueue for workload management and scheduling. The documentation covers:

- Overview of TrainJob integration with Kueue
- Queue selection and suspend field configuration
- Using ClusterTrainingRuntime (cluster-scoped) with complete PyTorch DDP example
- Using TrainingRuntime (namespace-scoped) for team-specific custom configurations
- Priority classes with Kueue PriorityClass
- LLM fine-tuning use cases with TorchTune
- Gang scheduling configuration with ClusterQueue
- Monitoring TrainJob status with kubectl
- Troubleshooting common issues (admission, startup, pod creation)
- Best practices for production deployments
- Migration notes highlighting differences from Training Operator v1

The documentation is structured to guide users from basic setup to advanced scenarios, with verified examples that match the actual Trainer v2 API.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #7345 https://github.com/kubeflow/trainer/issues/2919

#### Special notes for your reviewer:

- All API versions, field names, and label formats have been verified against the actual Trainer v2 codebase
- Examples use the correct queue label: `kueue.x-k8s.io/queue-name`
- RuntimeRef structure matches the Go types defined in `pkg/apis/trainer/v1alpha1/trainjob_types.go`
- ClusterTrainingRuntime examples align with manifests in `manifests/base/runtimes/`
- Documentation follows the existing style and structure of other Kubeflow integration docs (TFJob, PyTorchJob, etc.)
- Updated the index page to prominently feature Trainer v2 as the recommended option

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-05T05:16:53Z

@NarayanaSabari: The label(s) `area/batch` cannot be applied, because the repository doesn't have them.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7533):

>#### What type of PR is this?
>
>/kind documentation
>/sig scheduling
>/area batch
>
>#### What this PR does / why we need it:
>
>This PR adds comprehensive documentation for using Kubeflow Trainer v2 TrainJob with Kueue for workload management and scheduling. The documentation covers:
>
>- Overview of TrainJob integration with Kueue
>- Queue selection and suspend field configuration
>- Using ClusterTrainingRuntime (cluster-scoped) with complete PyTorch DDP example
>- Using TrainingRuntime (namespace-scoped) for team-specific custom configurations
>- Priority classes with Kueue PriorityClass
>- LLM fine-tuning use cases with TorchTune
>- Gang scheduling configuration with ClusterQueue
>- Monitoring TrainJob status with kubectl
>- Troubleshooting common issues (admission, startup, pod creation)
>- Best practices for production deployments
>- Migration notes highlighting differences from Training Operator v1
>
>The documentation is structured to guide users from basic setup to advanced scenarios, with verified examples that match the actual Trainer v2 API.
>
>#### Which issue(s) this PR fixes:
>
>Fixes #7345 https://github.com/kubeflow/trainer/issues/2919
>
>#### Special notes for your reviewer:
>
>- All API versions, field names, and label formats have been verified against the actual Trainer v2 codebase
>- Examples use the correct queue label: `kueue.x-k8s.io/queue-name`
>- RuntimeRef structure matches the Go types defined in `pkg/apis/trainer/v1alpha1/trainjob_types.go`
>- ClusterTrainingRuntime examples align with manifests in `manifests/base/runtimes/`
>- Documentation follows the existing style and structure of other Kubeflow integration docs (TFJob, PyTorchJob, etc.)
>- Updated the index page to prominently feature Trainer v2 as the recommended option
>
>#### Does this PR introduce a user-facing change?
>
>```release-note
>Added documentation for using Kubeflow Trainer v2 TrainJob with Kueue, covering both ClusterTrainingRuntime and namespace-scoped TrainingRuntime configurations.


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-11-05T05:16:54Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 424142547e3f363670e3c590b5ed5fa96c8109a9 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/690cc26bb6a51c0007a13c4e |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-7533--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-7533--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTc1MzMtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.ZQwRfXXi9BsQEPKL9TW-gX0MrFq_ePoiVy5crZCtqLg)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-7533--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@linux-foundation-easycla[bot]](https://github.com/apps/linux-foundation-easycla) — 2025-11-05T05:16:56Z

<a href="https://easycla.lfx.linuxfoundation.org/#/?version=2"><img src="https://s3.amazonaws.com/cla-project-logo-prod/cla-signed.svg?v=2" alt="CLA Signed" align="left" height="28" width="328" ></a><br/><br />The committers listed above are authorized under a signed CLA.<ul><li>:white_check_mark: login: NarayanaSabari / name: Sabari Narayana  (0892cc70a85cf6de2b44c3ac1680e165f9f4eda6, 0ba1636b5b3f98326fa31b6a5cfd5d208edc0cc1, 11ca861a2517d4dda01f49008adfc6d95dbf0978, 3b5f544cd285191a03123087d68260a535280bcd, 424142547e3f363670e3c590b5ed5fa96c8109a9, 42c30c931cf8bfab13f2f3ad2974095cd12b19cf, a6a91773f5a434c27f4d244b3ab66a3a529d4c4a, c35c737f2aa352501e043464e8213a234a03c181, c6e7079ad904d0b7493809ada5dd43548b4ae7de)</li></ul>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-05T05:16:57Z

Welcome @NarayanaSabari! <br><br>It looks like this is your first PR to <a href='https://github.com/kubernetes-sigs/kueue'>kubernetes-sigs/kueue</a> 🎉. Please refer to our [pull request process documentation](https://www.kubernetes.dev/docs/guide/pull-requests/) to help your PR have a smooth ride to approval. <br><br>You will be prompted by a bot to use commands during the review process. Do not be afraid to follow the prompts! It is okay to experiment. [Here is the bot commands documentation](https://go.k8s.io/bot-commands). <br><br>You can also check if kubernetes-sigs/kueue has [its own contribution guidelines](https://github.com/kubernetes-sigs/kueue/tree/master/CONTRIBUTING.md). <br><br>You may want to refer to our [testing guide](https://git.k8s.io/community/contributors/devel/sig-testing/testing.md) if you run into trouble with your tests not passing. <br><br>If you are having difficulty getting your pull request seen, please follow the [recommended escalation practices](https://www.kubernetes.dev/docs/guide/pull-requests/#why-is-my-pull-request-not-getting-reviewed). Also, for tips and tricks in the contribution process you may want to read the [Kubernetes contributor cheat sheet](https://www.kubernetes.dev/docs/contributor-cheatsheet/). We want to make sure your contribution gets all the attention it needs! <br><br>Thank you, and welcome to Kubernetes. :smiley:

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-05T05:16:58Z

Hi @NarayanaSabari. Thanks for your PR.

I'm waiting for a [github.com](https://kubernetes-sigs/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-05T07:25:40Z

/ok-to-test

### Comment by [@NarayanaSabari](https://github.com/NarayanaSabari) — 2025-11-05T12:29:12Z

Thanks for the thorough review @mimowo! All excellent points about keeping the documentation DRY and focused on TrainJob-specific integration details. I'll make the following changes:

1. ✅ Replace detailed troubleshooting with reference to general guide + TrainJob-specific notes
2. ✅ Update priority section to reference workload priority class documentation
3. ✅ Remove generic monitoring section or replace with brief reference
4. ✅ Simplify gang scheduling section to just reference ClusterQueue docs
5. ✅ Update LLM fine-tuning to reference existing Kubeflow examples and show only the queue label addition

This will significantly reduce the doc size while making it more maintainable and focused on the unique aspects of using TrainJob with Kueue. I'll push the updates shortly.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-05T16:12:36Z

/release-note-edit
```release-note
NONE
```

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-05T16:12:38Z

@mimowo: /release-note-edit must be used with a single release note block.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7533#issuecomment-3492096449):

>/release-note-edit
>```release-note
>NONE
>```
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-05T16:13:03Z

/release-note-edit
```release-note
NONE
```

### Comment by [@NarayanaSabari](https://github.com/NarayanaSabari) — 2025-11-06T09:48:47Z

@andreyvelich Thank you for the comprehensive review! I've addressed all your comments:

**Summary of Changes:**
- ✅ Fixed "JobSet" → "jobs" wording  
- ✅ Updated installation link to operator guide
- ✅ Added ClusterTrainingRuntime guide cross-reference
- ✅ Updated LLM section to reference BuiltinTrainer guides
- ✅ Enhanced Additional Resources section

**Pending:**
- 🔍 Docker image verification - waiting for your recommendation
- 💬 Website restructuring - need guidance on preferred approach

Also addressed all of @mimowo's feedback (removed generic sections, added references to existing Kueue docs).

Thanks for helping improve this documentation!

### Comment by [@andreyvelich](https://github.com/andreyvelich) — 2025-11-06T13:52:41Z

@NarayanaSabari We might also want to introduce Kubeflow TrainJob page in the multi-cluster: https://deploy-preview-7533--kubernetes-sigs-kueue.netlify.app/docs/tasks/run/multikueue/
Since we support MultiKueue in TrainJob.

### Comment by [@NarayanaSabari](https://github.com/NarayanaSabari) — 2025-11-06T15:47:36Z

Hi @andreyvelich @mimowo  I have made all the changes mentioned, Let me know is there any thing else.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-07T09:06:08Z

/lgtm
/approve
Thank you 👍 
Since Kueue 0.14 already provides the support for TrainJob 2.1.0.

/cherrypick website
One additional thing we may improve in the follow up is the note that the integration does not support Topology-Aware Scheduling using Kueue 0.14 with TrainJob 2.0.0. However, this limitation will be solved in Kueue 0.15 used with TrainJob 2.1.0.

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-11-07T09:06:11Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `website` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7533#issuecomment-3501396435):

>/lgtm
>/approve
>Thank you 👍 
>Since Kueue 0.14 already provides the support for TrainJob 2.1.0.
>
>/cherrypick website
>One additional thing we may improve in the follow up is the note that the integration does not support Topology-Aware Scheduling using Kueue 0.14 with TrainJob 2.0.0. However, this limitation will be solved in Kueue 0.15 used with TrainJob 2.1.0.
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-07T09:06:16Z

LGTM label has been added.  <details>Git tree hash: abb9899e361b979ff73f896130008a3e7c22df95</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-07T09:06:17Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7533#issuecomment-3501396435" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7533#" title="Author self-approved">NarayanaSabari</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-11-07T09:31:36Z

@mimowo: new pull request created: #7578

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7533#issuecomment-3501396435):

>/lgtm
>/approve
>Thank you 👍 
>Since Kueue 0.14 already provides the support for TrainJob 2.1.0.
>
>/cherrypick website
>One additional thing we may improve in the follow up is the note that the integration does not support Topology-Aware Scheduling using Kueue 0.14 with TrainJob 2.0.0. However, this limitation will be solved in Kueue 0.15 used with TrainJob 2.1.0.
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-05T06:39:35Z

looks good , I added comments mostly to reduce the parts which are already mentioned elsewhere and should be familiar to the kueue user

- **site/content/en/docs/tasks/run/trainjobs.md:245** by [@mimowo](https://github.com/mimowo) — 2025-11-05T06:29:06Z
```diff
@@ -0,0 +1,374 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Kubeflow Trainer](https://www.kubeflow.org/docs/components/trainer/) TrainJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Overview
+
+Kubeflow Trainer v2 introduces the `TrainJob` API that works seamlessly with Kueue for batch scheduling and resource management. TrainJobs can be configured to use either:
+
+- **ClusterTrainingRuntime**: Cluster-scoped training runtimes that can be used across all namespaces
+- **TrainingRuntime**: Namespace-scoped training runtimes that are only available within a specific namespace
+
+Kueue manages TrainJobs by scheduling their underlying JobSets according to available quota and priority.
+
+## Before you begin
+
+1. Check [administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial cluster setup.
+
+2. Install Kubeflow Trainer v2. Check [the Trainer installation guide](https://www.kubeflow.org/docs/components/trainer/getting-started/).
+
+   **Note**: The minimum required Trainer version is v2.0.0.
+
+3. Enable TrainJob integration in Kueue. You can [modify kueue configurations from installed releases](/docs/installation#install-a-custom-configured-released-version) to include TrainJobs as an allowed workload.
+
+## TrainJob definition
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the TrainJob configuration:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Suspend field
+
+By default, Kueue will set `suspend` to true via webhook and unsuspend it when the TrainJob is admitted:
+
+```yaml
+spec:
+  suspend: true
+```
+
+## Using ClusterTrainingRuntime
+
+ClusterTrainingRuntimes are cluster-scoped resources that define training configurations accessible across all namespaces. They are typically created by platform administrators.
+
+### Example: PyTorch Distributed Training with ClusterTrainingRuntime
+
+First, create a ClusterTrainingRuntime for PyTorch distributed training:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: ClusterTrainingRuntime
+metadata:
+  name: torch-distributed
+  labels:
+    trainer.kubeflow.org/framework: torch
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+```
+
+Now, create a TrainJob that references this ClusterTrainingRuntime and will be scheduled by Kueue:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-distributed
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    command:
+      - torchrun
+      - /workspace/examples/mnist/mnist.py
+    numNodes: 2
+    resourcesPerNode:
+      requests:
+        cpu: "4"
+        memory: "8Gi"
+        nvidia.com/gpu: "1"
+```
+
+**Key Points:**
+- The `kueue.x-k8s.io/queue-name` label assigns this TrainJob to the `user-queue` LocalQueue
+- The `runtimeRef` points to the `ClusterTrainingRuntime` named `torch-distributed`
+- Kueue will manage the lifecycle and admission of this TrainJob based on available quota
+
+## Using TrainingRuntime (Namespace-scoped)
+
+TrainingRuntimes are namespace-scoped resources that provide more granular control per namespace. They are useful when different teams need customized training configurations.
+
+### Example: Custom PyTorch Training with TrainingRuntime
+
+Create a namespace-scoped TrainingRuntime:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainingRuntime
+metadata:
+  name: torch-custom
+  namespace: team-a
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+                      env:
+                        - name: CUSTOM_ENV
+                          value: "team-a-value"
+```
+
+Create a TrainJob that uses this namespace-scoped runtime:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-custom
+  namespace: team-a
+  labels:
+    kueue.x-k8s.io/queue-name: team-a-queue
+spec:
+  runtimeRef:
+    name: torch-custom
+    kind: TrainingRuntime
+    apiGroup: trainer.kubeflow.org
+  trainer:
+    image: docker.io/team-a/custom-training:latest
+    numNodes: 1
+    resourcesPerNode:
+      requests:
+        cpu: "2"
+        memory: "4Gi"
+```
+
+**Key Points:**
+- The TrainingRuntime is created in the same namespace as the TrainJob (`team-a`)
+- The `runtimeRef` specifies `kind: TrainingRuntime` to use the namespace-scoped runtime
+- Each namespace can have its own customized runtimes with different configurations
+
+## Using Kueue Priority
+
+You can set job priority using Kueue's PriorityClass:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: high-priority-training
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: high-priority
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    numNodes: 2
+    resourcesPerNode:
+      requests:
+        cpu: "4"
+        memory: "8Gi"
+        nvidia.com/gpu: "1"
+```
+
+## LLM Fine-Tuning with Kueue
+
+For Large Language Model fine-tuning with TorchTune, you can use specialized ClusterTrainingRuntimes:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: llama-finetuning
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: gpu-queue
+spec:
+  runtimeRef:
+    name: torchtune-llama3.2-1b
+    kind: ClusterTrainingRuntime
+  initializer:
+    dataset:
+      storageUri: hf://tatsu-lab/alpaca
+    model:
+      storageUri: hf://meta-llama/Llama-3.2-1B-Instruct
+      accessToken: <your-hf-token>
+  trainer:
+    numNodes: 1
+    resourcesPerNode:
+      requests:
+        cpu: "8"
+        memory: "32Gi"
+        nvidia.com/gpu: "2"
+```
+
+## Monitoring TrainJob Status
+
+You can check the status of your TrainJob:
+
+```bash
+kubectl get trainjob pytorch-distributed -n default
+```
+
+To see detailed information including Kueue admission status:
+
+```bash
+kubectl describe trainjob pytorch-distributed -n default
+```
+
+Check the workload status in Kueue:
+
+```bash
+kubectl get workload -n default
+```
+
+## Gang Scheduling with ClusterQueue
+
+For distributed training that requires all nodes to start simultaneously, configure your ClusterQueue appropriately:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: cluster-queue
+spec:
+  namespaceSelector: {}
+  resourceGroups:
+    - coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
+      flavors:
+        - name: default-flavor
+          resources:
+            - name: "cpu"
+              nominalQuota: 100
+            - name: "memory"
+              nominalQuota: 500Gi
+            - name: "nvidia.com/gpu"
+              nominalQuota: 8
+```
+
+## Cleanup
+
+To delete a TrainJob:
+
+```bash
+kubectl delete trainjob pytorch-distributed -n default
+```
+
+## Differences from Kubeflow Training Operator V1
+
+{{% alert title="Important" color="warning" %}}
+Kubeflow Trainer v2 introduces a new API that is not compatible with Training Operator v1 APIs (PyTorchJob, TFJob, etc.). The key differences are:
+
+- **Unified API**: TrainJob replaces framework-specific CRDs like PyTorchJob, TFJob
+- **Runtime-based**: Training configurations are defined in reusable Runtimes
+- **Built on JobSet**: Uses Kubernetes JobSet as the underlying infrastructure
+- **Better integration**: Native support for Kueue scheduling from the start
+
+For migration guidance, refer to the [Kubeflow Trainer documentation](https://www.kubeflow.org/docs/components/trainer/operator-guides/migration/).
+{{% /alert %}}
+
+## Best Practices
+
+1. **Use ClusterTrainingRuntimes for common patterns**: Create cluster-scoped runtimes for frequently used training configurations
+2. **Use TrainingRuntimes for team-specific needs**: Leverage namespace-scoped runtimes for customizations per team
+3. **Set appropriate resource requests**: Ensure your TrainJob resource requests match the ResourceFlavor in your ClusterQueue
+4. **Monitor quota usage**: Use `kubectl get clusterqueue` to track resource utilization
+5. **Use priority classes**: Assign priorities to TrainJobs to ensure critical workloads are scheduled first
+6. **Test with small configurations**: Before scaling up, test your TrainJob configuration with minimal resources
+
+## Additional Resources
+
+- [Kubeflow Trainer Documentation](https://www.kubeflow.org/docs/components/trainer/)
+- [Kueue Concepts](/docs/concepts/)
+- [Configure ClusterQueue](/docs/tasks/manage/setup_cluster_queue/)
+- [Kubeflow Python SDK](https://github.com/kubeflow/sdk/)
+
+## Troubleshooting
```

  We already have a troubleshooting page and the points here seem redundant with that, so I would prefer to avoid copy pasting for all job types. Feel free to refer to the generic guide. We can revisit that remark if there is something specific to TrainJobs

- **site/content/en/docs/tasks/run/kubeflow/trainjobs.md:198** by [@mimowo](https://github.com/mimowo) — 2025-11-05T06:32:25Z
```diff
@@ -0,0 +1,374 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Kubeflow Trainer](https://www.kubeflow.org/docs/components/trainer/) TrainJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Overview
+
+Kubeflow Trainer v2 introduces the `TrainJob` API that works seamlessly with Kueue for batch scheduling and resource management. TrainJobs can be configured to use either:
+
+- **ClusterTrainingRuntime**: Cluster-scoped training runtimes that can be used across all namespaces
+- **TrainingRuntime**: Namespace-scoped training runtimes that are only available within a specific namespace
+
+Kueue manages TrainJobs by scheduling their underlying JobSets according to available quota and priority.
+
+## Before you begin
+
+1. Check [administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial cluster setup.
+
+2. Install Kubeflow Trainer v2. Check [the Trainer installation guide](https://www.kubeflow.org/docs/components/trainer/getting-started/).
+
+   **Note**: The minimum required Trainer version is v2.0.0.
+
+3. Enable TrainJob integration in Kueue. You can [modify kueue configurations from installed releases](/docs/installation#install-a-custom-configured-released-version) to include TrainJobs as an allowed workload.
+
+## TrainJob definition
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the TrainJob configuration:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Suspend field
+
+By default, Kueue will set `suspend` to true via webhook and unsuspend it when the TrainJob is admitted:
+
+```yaml
+spec:
+  suspend: true
+```
+
+## Using ClusterTrainingRuntime
+
+ClusterTrainingRuntimes are cluster-scoped resources that define training configurations accessible across all namespaces. They are typically created by platform administrators.
+
+### Example: PyTorch Distributed Training with ClusterTrainingRuntime
+
+First, create a ClusterTrainingRuntime for PyTorch distributed training:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: ClusterTrainingRuntime
+metadata:
+  name: torch-distributed
+  labels:
+    trainer.kubeflow.org/framework: torch
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+```
+
+Now, create a TrainJob that references this ClusterTrainingRuntime and will be scheduled by Kueue:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-distributed
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    command:
+      - torchrun
+      - /workspace/examples/mnist/mnist.py
+    numNodes: 2
+    resourcesPerNode:
+      requests:
+        cpu: "4"
+        memory: "8Gi"
+        nvidia.com/gpu: "1"
+```
+
+**Key Points:**
+- The `kueue.x-k8s.io/queue-name` label assigns this TrainJob to the `user-queue` LocalQueue
+- The `runtimeRef` points to the `ClusterTrainingRuntime` named `torch-distributed`
+- Kueue will manage the lifecycle and admission of this TrainJob based on available quota
+
+## Using TrainingRuntime (Namespace-scoped)
+
+TrainingRuntimes are namespace-scoped resources that provide more granular control per namespace. They are useful when different teams need customized training configurations.
+
+### Example: Custom PyTorch Training with TrainingRuntime
+
+Create a namespace-scoped TrainingRuntime:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainingRuntime
+metadata:
+  name: torch-custom
+  namespace: team-a
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+                      env:
+                        - name: CUSTOM_ENV
+                          value: "team-a-value"
+```
+
+Create a TrainJob that uses this namespace-scoped runtime:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-custom
+  namespace: team-a
+  labels:
+    kueue.x-k8s.io/queue-name: team-a-queue
+spec:
+  runtimeRef:
+    name: torch-custom
+    kind: TrainingRuntime
+    apiGroup: trainer.kubeflow.org
+  trainer:
+    image: docker.io/team-a/custom-training:latest
+    numNodes: 1
+    resourcesPerNode:
+      requests:
+        cpu: "2"
+        memory: "4Gi"
+```
+
+**Key Points:**
+- The TrainingRuntime is created in the same namespace as the TrainJob (`team-a`)
+- The `runtimeRef` specifies `kind: TrainingRuntime` to use the namespace-scoped runtime
+- Each namespace can have its own customized runtimes with different configurations
+
+## Using Kueue Priority
+
+You can set job priority using Kueue's PriorityClass:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: high-priority-training
+  namespace: default
+  labels:
```

  we should rather promote using workload priority class. Also we have a page for that so instead I prefer to refer there

- **site/content/en/docs/tasks/run/trainjobs.md:211** by [@mimowo](https://github.com/mimowo) — 2025-11-05T06:34:58Z
```diff
@@ -0,0 +1,374 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Kubeflow Trainer](https://www.kubeflow.org/docs/components/trainer/) TrainJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Overview
+
+Kubeflow Trainer v2 introduces the `TrainJob` API that works seamlessly with Kueue for batch scheduling and resource management. TrainJobs can be configured to use either:
+
+- **ClusterTrainingRuntime**: Cluster-scoped training runtimes that can be used across all namespaces
+- **TrainingRuntime**: Namespace-scoped training runtimes that are only available within a specific namespace
+
+Kueue manages TrainJobs by scheduling their underlying JobSets according to available quota and priority.
+
+## Before you begin
+
+1. Check [administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial cluster setup.
+
+2. Install Kubeflow Trainer v2. Check [the Trainer installation guide](https://www.kubeflow.org/docs/components/trainer/getting-started/).
+
+   **Note**: The minimum required Trainer version is v2.0.0.
+
+3. Enable TrainJob integration in Kueue. You can [modify kueue configurations from installed releases](/docs/installation#install-a-custom-configured-released-version) to include TrainJobs as an allowed workload.
+
+## TrainJob definition
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the TrainJob configuration:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Suspend field
+
+By default, Kueue will set `suspend` to true via webhook and unsuspend it when the TrainJob is admitted:
+
+```yaml
+spec:
+  suspend: true
+```
+
+## Using ClusterTrainingRuntime
+
+ClusterTrainingRuntimes are cluster-scoped resources that define training configurations accessible across all namespaces. They are typically created by platform administrators.
+
+### Example: PyTorch Distributed Training with ClusterTrainingRuntime
+
+First, create a ClusterTrainingRuntime for PyTorch distributed training:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: ClusterTrainingRuntime
+metadata:
+  name: torch-distributed
+  labels:
+    trainer.kubeflow.org/framework: torch
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+```
+
+Now, create a TrainJob that references this ClusterTrainingRuntime and will be scheduled by Kueue:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-distributed
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    command:
+      - torchrun
+      - /workspace/examples/mnist/mnist.py
+    numNodes: 2
+    resourcesPerNode:
+      requests:
+        cpu: "4"
+        memory: "8Gi"
+        nvidia.com/gpu: "1"
+```
+
+**Key Points:**
+- The `kueue.x-k8s.io/queue-name` label assigns this TrainJob to the `user-queue` LocalQueue
+- The `runtimeRef` points to the `ClusterTrainingRuntime` named `torch-distributed`
+- Kueue will manage the lifecycle and admission of this TrainJob based on available quota
+
+## Using TrainingRuntime (Namespace-scoped)
+
+TrainingRuntimes are namespace-scoped resources that provide more granular control per namespace. They are useful when different teams need customized training configurations.
+
+### Example: Custom PyTorch Training with TrainingRuntime
+
+Create a namespace-scoped TrainingRuntime:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainingRuntime
+metadata:
+  name: torch-custom
+  namespace: team-a
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+                      env:
+                        - name: CUSTOM_ENV
+                          value: "team-a-value"
+```
+
+Create a TrainJob that uses this namespace-scoped runtime:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-custom
+  namespace: team-a
+  labels:
+    kueue.x-k8s.io/queue-name: team-a-queue
+spec:
+  runtimeRef:
+    name: torch-custom
+    kind: TrainingRuntime
+    apiGroup: trainer.kubeflow.org
+  trainer:
+    image: docker.io/team-a/custom-training:latest
+    numNodes: 1
+    resourcesPerNode:
+      requests:
+        cpu: "2"
+        memory: "4Gi"
+```
+
+**Key Points:**
+- The TrainingRuntime is created in the same namespace as the TrainJob (`team-a`)
+- The `runtimeRef` specifies `kind: TrainingRuntime` to use the namespace-scoped runtime
+- Each namespace can have its own customized runtimes with different configurations
+
+## Using Kueue Priority
+
+You can set job priority using Kueue's PriorityClass:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: high-priority-training
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: high-priority
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    numNodes: 2
+    resourcesPerNode:
+      requests:
+        cpu: "4"
+        memory: "8Gi"
+        nvidia.com/gpu: "1"
+```
+
+## LLM Fine-Tuning with Kueue
+
+For Large Language Model fine-tuning with TorchTune, you can use specialized ClusterTrainingRuntimes:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: llama-finetuning
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: gpu-queue
+spec:
+  runtimeRef:
+    name: torchtune-llama3.2-1b
+    kind: ClusterTrainingRuntime
+  initializer:
+    dataset:
+      storageUri: hf://tatsu-lab/alpaca
+    model:
+      storageUri: hf://meta-llama/Llama-3.2-1B-Instruct
+      accessToken: <your-hf-token>
+  trainer:
+    numNodes: 1
+    resourcesPerNode:
+      requests:
+        cpu: "8"
+        memory: "32Gi"
+        nvidia.com/gpu: "2"
+```
+
```

  This section seems very generic for all Job types supported in queue, drop unless we have something specific

- **site/content/en/docs/tasks/run/trainjobs.md:191** by [@mimowo](https://github.com/mimowo) — 2025-11-05T06:38:05Z
```diff
@@ -0,0 +1,374 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Kubeflow Trainer](https://www.kubeflow.org/docs/components/trainer/) TrainJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Overview
+
+Kubeflow Trainer v2 introduces the `TrainJob` API that works seamlessly with Kueue for batch scheduling and resource management. TrainJobs can be configured to use either:
+
+- **ClusterTrainingRuntime**: Cluster-scoped training runtimes that can be used across all namespaces
+- **TrainingRuntime**: Namespace-scoped training runtimes that are only available within a specific namespace
+
+Kueue manages TrainJobs by scheduling their underlying JobSets according to available quota and priority.
+
+## Before you begin
+
+1. Check [administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial cluster setup.
+
+2. Install Kubeflow Trainer v2. Check [the Trainer installation guide](https://www.kubeflow.org/docs/components/trainer/getting-started/).
+
+   **Note**: The minimum required Trainer version is v2.0.0.
+
+3. Enable TrainJob integration in Kueue. You can [modify kueue configurations from installed releases](/docs/installation#install-a-custom-configured-released-version) to include TrainJobs as an allowed workload.
+
+## TrainJob definition
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the TrainJob configuration:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Suspend field
+
+By default, Kueue will set `suspend` to true via webhook and unsuspend it when the TrainJob is admitted:
+
+```yaml
+spec:
+  suspend: true
+```
+
+## Using ClusterTrainingRuntime
+
+ClusterTrainingRuntimes are cluster-scoped resources that define training configurations accessible across all namespaces. They are typically created by platform administrators.
+
+### Example: PyTorch Distributed Training with ClusterTrainingRuntime
+
+First, create a ClusterTrainingRuntime for PyTorch distributed training:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: ClusterTrainingRuntime
+metadata:
+  name: torch-distributed
+  labels:
+    trainer.kubeflow.org/framework: torch
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+```
+
+Now, create a TrainJob that references this ClusterTrainingRuntime and will be scheduled by Kueue:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-distributed
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    command:
+      - torchrun
+      - /workspace/examples/mnist/mnist.py
+    numNodes: 2
+    resourcesPerNode:
+      requests:
+        cpu: "4"
+        memory: "8Gi"
+        nvidia.com/gpu: "1"
+```
+
+**Key Points:**
+- The `kueue.x-k8s.io/queue-name` label assigns this TrainJob to the `user-queue` LocalQueue
+- The `runtimeRef` points to the `ClusterTrainingRuntime` named `torch-distributed`
+- Kueue will manage the lifecycle and admission of this TrainJob based on available quota
+
+## Using TrainingRuntime (Namespace-scoped)
+
+TrainingRuntimes are namespace-scoped resources that provide more granular control per namespace. They are useful when different teams need customized training configurations.
+
+### Example: Custom PyTorch Training with TrainingRuntime
+
+Create a namespace-scoped TrainingRuntime:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainingRuntime
+metadata:
+  name: torch-custom
+  namespace: team-a
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+                      env:
+                        - name: CUSTOM_ENV
+                          value: "team-a-value"
+```
+
+Create a TrainJob that uses this namespace-scoped runtime:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-custom
+  namespace: team-a
+  labels:
+    kueue.x-k8s.io/queue-name: team-a-queue
+spec:
+  runtimeRef:
+    name: torch-custom
+    kind: TrainingRuntime
+    apiGroup: trainer.kubeflow.org
+  trainer:
+    image: docker.io/team-a/custom-training:latest
+    numNodes: 1
+    resourcesPerNode:
+      requests:
+        cpu: "2"
+        memory: "4Gi"
+```
+
+**Key Points:**
+- The TrainingRuntime is created in the same namespace as the TrainJob (`team-a`)
+- The `runtimeRef` specifies `kind: TrainingRuntime` to use the namespace-scoped runtime
+- Each namespace can have its own customized runtimes with different configurations
+
+## Using Kueue Priority
+
+You can set job priority using Kueue's PriorityClass:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: high-priority-training
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: high-priority
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    numNodes: 2
+    resourcesPerNode:
+      requests:
+        cpu: "4"
+        memory: "8Gi"
+        nvidia.com/gpu: "1"
+```
+
+## LLM Fine-Tuning with Kueue
```

  is there such an example I. the kubełet docs? if so we could refere there and mention that the only difference is the added queue name label

- **site/content/en/docs/tasks/run/kubeflow/trainjobs.md:290** by [@mimowo](https://github.com/mimowo) — 2025-11-05T06:38:36Z
```diff
@@ -0,0 +1,374 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Kubeflow Trainer](https://www.kubeflow.org/docs/components/trainer/) TrainJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Overview
+
+Kubeflow Trainer v2 introduces the `TrainJob` API that works seamlessly with Kueue for batch scheduling and resource management. TrainJobs can be configured to use either:
+
+- **ClusterTrainingRuntime**: Cluster-scoped training runtimes that can be used across all namespaces
+- **TrainingRuntime**: Namespace-scoped training runtimes that are only available within a specific namespace
+
+Kueue manages TrainJobs by scheduling their underlying JobSets according to available quota and priority.
+
+## Before you begin
+
+1. Check [administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial cluster setup.
+
+2. Install Kubeflow Trainer v2. Check [the Trainer installation guide](https://www.kubeflow.org/docs/components/trainer/getting-started/).
+
+   **Note**: The minimum required Trainer version is v2.0.0.
+
+3. Enable TrainJob integration in Kueue. You can [modify kueue configurations from installed releases](/docs/installation#install-a-custom-configured-released-version) to include TrainJobs as an allowed workload.
+
+## TrainJob definition
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the TrainJob configuration:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Suspend field
+
+By default, Kueue will set `suspend` to true via webhook and unsuspend it when the TrainJob is admitted:
+
+```yaml
+spec:
+  suspend: true
+```
+
+## Using ClusterTrainingRuntime
+
+ClusterTrainingRuntimes are cluster-scoped resources that define training configurations accessible across all namespaces. They are typically created by platform administrators.
+
+### Example: PyTorch Distributed Training with ClusterTrainingRuntime
+
+First, create a ClusterTrainingRuntime for PyTorch distributed training:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: ClusterTrainingRuntime
+metadata:
+  name: torch-distributed
+  labels:
+    trainer.kubeflow.org/framework: torch
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+```
+
+Now, create a TrainJob that references this ClusterTrainingRuntime and will be scheduled by Kueue:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-distributed
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    command:
+      - torchrun
+      - /workspace/examples/mnist/mnist.py
+    numNodes: 2
+    resourcesPerNode:
+      requests:
+        cpu: "4"
+        memory: "8Gi"
+        nvidia.com/gpu: "1"
+```
+
+**Key Points:**
+- The `kueue.x-k8s.io/queue-name` label assigns this TrainJob to the `user-queue` LocalQueue
+- The `runtimeRef` points to the `ClusterTrainingRuntime` named `torch-distributed`
+- Kueue will manage the lifecycle and admission of this TrainJob based on available quota
+
+## Using TrainingRuntime (Namespace-scoped)
+
+TrainingRuntimes are namespace-scoped resources that provide more granular control per namespace. They are useful when different teams need customized training configurations.
+
+### Example: Custom PyTorch Training with TrainingRuntime
+
+Create a namespace-scoped TrainingRuntime:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainingRuntime
+metadata:
+  name: torch-custom
+  namespace: team-a
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+                      env:
+                        - name: CUSTOM_ENV
+                          value: "team-a-value"
+```
+
+Create a TrainJob that uses this namespace-scoped runtime:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-custom
+  namespace: team-a
+  labels:
+    kueue.x-k8s.io/queue-name: team-a-queue
+spec:
+  runtimeRef:
+    name: torch-custom
+    kind: TrainingRuntime
+    apiGroup: trainer.kubeflow.org
+  trainer:
+    image: docker.io/team-a/custom-training:latest
+    numNodes: 1
+    resourcesPerNode:
+      requests:
+        cpu: "2"
+        memory: "4Gi"
+```
+
+**Key Points:**
+- The TrainingRuntime is created in the same namespace as the TrainJob (`team-a`)
+- The `runtimeRef` specifies `kind: TrainingRuntime` to use the namespace-scoped runtime
+- Each namespace can have its own customized runtimes with different configurations
+
+## Using Kueue Priority
+
+You can set job priority using Kueue's PriorityClass:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: high-priority-training
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: high-priority
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    numNodes: 2
+    resourcesPerNode:
+      requests:
+        cpu: "4"
+        memory: "8Gi"
+        nvidia.com/gpu: "1"
+```
+
+## LLM Fine-Tuning with Kueue
+
+For Large Language Model fine-tuning with TorchTune, you can use specialized ClusterTrainingRuntimes:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: llama-finetuning
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: gpu-queue
+spec:
+  runtimeRef:
+    name: torchtune-llama3.2-1b
+    kind: ClusterTrainingRuntime
+  initializer:
+    dataset:
+      storageUri: hf://tatsu-lab/alpaca
+    model:
+      storageUri: hf://meta-llama/Llama-3.2-1B-Instruct
+      accessToken: <your-hf-token>
+  trainer:
+    numNodes: 1
+    resourcesPerNode:
+      requests:
+        cpu: "8"
+        memory: "32Gi"
+        nvidia.com/gpu: "2"
+```
+
+## Monitoring TrainJob Status
+
+You can check the status of your TrainJob:
+
+```bash
+kubectl get trainjob pytorch-distributed -n default
+```
+
+To see detailed information including Kueue admission status:
+
+```bash
+kubectl describe trainjob pytorch-distributed -n default
+```
+
+Check the workload status in Kueue:
+
+```bash
+kubectl get workload -n default
+```
+
+## Gang Scheduling with ClusterQueue
+
+For distributed training that requires all nodes to start simultaneously, configure your ClusterQueue appropriately:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: cluster-queue
+spec:
+  namespaceSelector: {}
+  resourceGroups:
+    - coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
+      flavors:
+        - name: default-flavor
+          resources:
+            - name: "cpu"
+              nominalQuota: 100
+            - name: "memory"
+              nominalQuota: 500Gi
+            - name: "nvidia.com/gpu"
+              nominalQuota: 8
+```
+
+## Cleanup
```

  seems like generic too

### Review by [@NarayanaSabari](https://github.com/NarayanaSabari) (commented) — 2025-11-05T12:25:16Z

- **site/content/en/docs/tasks/run/trainjobs.md:245** by [@NarayanaSabari](https://github.com/NarayanaSabari) — 2025-11-05T12:25:16Z
```diff
@@ -0,0 +1,374 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Kubeflow Trainer](https://www.kubeflow.org/docs/components/trainer/) TrainJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Overview
+
+Kubeflow Trainer v2 introduces the `TrainJob` API that works seamlessly with Kueue for batch scheduling and resource management. TrainJobs can be configured to use either:
+
+- **ClusterTrainingRuntime**: Cluster-scoped training runtimes that can be used across all namespaces
+- **TrainingRuntime**: Namespace-scoped training runtimes that are only available within a specific namespace
+
+Kueue manages TrainJobs by scheduling their underlying JobSets according to available quota and priority.
+
+## Before you begin
+
+1. Check [administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial cluster setup.
+
+2. Install Kubeflow Trainer v2. Check [the Trainer installation guide](https://www.kubeflow.org/docs/components/trainer/getting-started/).
+
+   **Note**: The minimum required Trainer version is v2.0.0.
+
+3. Enable TrainJob integration in Kueue. You can [modify kueue configurations from installed releases](/docs/installation#install-a-custom-configured-released-version) to include TrainJobs as an allowed workload.
+
+## TrainJob definition
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the TrainJob configuration:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Suspend field
+
+By default, Kueue will set `suspend` to true via webhook and unsuspend it when the TrainJob is admitted:
+
+```yaml
+spec:
+  suspend: true
+```
+
+## Using ClusterTrainingRuntime
+
+ClusterTrainingRuntimes are cluster-scoped resources that define training configurations accessible across all namespaces. They are typically created by platform administrators.
+
+### Example: PyTorch Distributed Training with ClusterTrainingRuntime
+
+First, create a ClusterTrainingRuntime for PyTorch distributed training:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: ClusterTrainingRuntime
+metadata:
+  name: torch-distributed
+  labels:
+    trainer.kubeflow.org/framework: torch
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+```
+
+Now, create a TrainJob that references this ClusterTrainingRuntime and will be scheduled by Kueue:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-distributed
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    command:
+      - torchrun
+      - /workspace/examples/mnist/mnist.py
+    numNodes: 2
+    resourcesPerNode:
+      requests:
+        cpu: "4"
+        memory: "8Gi"
+        nvidia.com/gpu: "1"
+```
+
+**Key Points:**
+- The `kueue.x-k8s.io/queue-name` label assigns this TrainJob to the `user-queue` LocalQueue
+- The `runtimeRef` points to the `ClusterTrainingRuntime` named `torch-distributed`
+- Kueue will manage the lifecycle and admission of this TrainJob based on available quota
+
+## Using TrainingRuntime (Namespace-scoped)
+
+TrainingRuntimes are namespace-scoped resources that provide more granular control per namespace. They are useful when different teams need customized training configurations.
+
+### Example: Custom PyTorch Training with TrainingRuntime
+
+Create a namespace-scoped TrainingRuntime:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainingRuntime
+metadata:
+  name: torch-custom
+  namespace: team-a
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+                      env:
+                        - name: CUSTOM_ENV
+                          value: "team-a-value"
+```
+
+Create a TrainJob that uses this namespace-scoped runtime:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-custom
+  namespace: team-a
+  labels:
+    kueue.x-k8s.io/queue-name: team-a-queue
+spec:
+  runtimeRef:
+    name: torch-custom
+    kind: TrainingRuntime
+    apiGroup: trainer.kubeflow.org
+  trainer:
+    image: docker.io/team-a/custom-training:latest
+    numNodes: 1
+    resourcesPerNode:
+      requests:
+        cpu: "2"
+        memory: "4Gi"
+```
+
+**Key Points:**
+- The TrainingRuntime is created in the same namespace as the TrainJob (`team-a`)
+- The `runtimeRef` specifies `kind: TrainingRuntime` to use the namespace-scoped runtime
+- Each namespace can have its own customized runtimes with different configurations
+
+## Using Kueue Priority
+
+You can set job priority using Kueue's PriorityClass:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: high-priority-training
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: high-priority
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    numNodes: 2
+    resourcesPerNode:
+      requests:
+        cpu: "4"
+        memory: "8Gi"
+        nvidia.com/gpu: "1"
+```
+
+## LLM Fine-Tuning with Kueue
+
+For Large Language Model fine-tuning with TorchTune, you can use specialized ClusterTrainingRuntimes:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: llama-finetuning
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: gpu-queue
+spec:
+  runtimeRef:
+    name: torchtune-llama3.2-1b
+    kind: ClusterTrainingRuntime
+  initializer:
+    dataset:
+      storageUri: hf://tatsu-lab/alpaca
+    model:
+      storageUri: hf://meta-llama/Llama-3.2-1B-Instruct
+      accessToken: <your-hf-token>
+  trainer:
+    numNodes: 1
+    resourcesPerNode:
+      requests:
+        cpu: "8"
+        memory: "32Gi"
+        nvidia.com/gpu: "2"
+```
+
+## Monitoring TrainJob Status
+
+You can check the status of your TrainJob:
+
+```bash
+kubectl get trainjob pytorch-distributed -n default
+```
+
+To see detailed information including Kueue admission status:
+
+```bash
+kubectl describe trainjob pytorch-distributed -n default
+```
+
+Check the workload status in Kueue:
+
+```bash
+kubectl get workload -n default
+```
+
+## Gang Scheduling with ClusterQueue
+
+For distributed training that requires all nodes to start simultaneously, configure your ClusterQueue appropriately:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: cluster-queue
+spec:
+  namespaceSelector: {}
+  resourceGroups:
+    - coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
+      flavors:
+        - name: default-flavor
+          resources:
+            - name: "cpu"
+              nominalQuota: 100
+            - name: "memory"
+              nominalQuota: 500Gi
+            - name: "nvidia.com/gpu"
+              nominalQuota: 8
+```
+
+## Cleanup
+
+To delete a TrainJob:
+
+```bash
+kubectl delete trainjob pytorch-distributed -n default
+```
+
+## Differences from Kubeflow Training Operator V1
+
+{{% alert title="Important" color="warning" %}}
+Kubeflow Trainer v2 introduces a new API that is not compatible with Training Operator v1 APIs (PyTorchJob, TFJob, etc.). The key differences are:
+
+- **Unified API**: TrainJob replaces framework-specific CRDs like PyTorchJob, TFJob
+- **Runtime-based**: Training configurations are defined in reusable Runtimes
+- **Built on JobSet**: Uses Kubernetes JobSet as the underlying infrastructure
+- **Better integration**: Native support for Kueue scheduling from the start
+
+For migration guidance, refer to the [Kubeflow Trainer documentation](https://www.kubeflow.org/docs/components/trainer/operator-guides/migration/).
+{{% /alert %}}
+
+## Best Practices
+
+1. **Use ClusterTrainingRuntimes for common patterns**: Create cluster-scoped runtimes for frequently used training configurations
+2. **Use TrainingRuntimes for team-specific needs**: Leverage namespace-scoped runtimes for customizations per team
+3. **Set appropriate resource requests**: Ensure your TrainJob resource requests match the ResourceFlavor in your ClusterQueue
+4. **Monitor quota usage**: Use `kubectl get clusterqueue` to track resource utilization
+5. **Use priority classes**: Assign priorities to TrainJobs to ensure critical workloads are scheduled first
+6. **Test with small configurations**: Before scaling up, test your TrainJob configuration with minimal resources
+
+## Additional Resources
+
+- [Kubeflow Trainer Documentation](https://www.kubeflow.org/docs/components/trainer/)
+- [Kueue Concepts](/docs/concepts/)
+- [Configure ClusterQueue](/docs/tasks/manage/setup_cluster_queue/)
+- [Kubeflow Python SDK](https://github.com/kubeflow/sdk/)
+
+## Troubleshooting
```

  Great point! I'll replace the detailed troubleshooting section with a reference to the general Kueue troubleshooting guide and only include TrainJob-specific issues like verifying ClusterTrainingRuntime/TrainingRuntime existence. This keeps the documentation DRY and focused on what's unique to TrainJob integration.

### Review by [@NarayanaSabari](https://github.com/NarayanaSabari) (commented) — 2025-11-05T12:25:52Z

- **site/content/en/docs/tasks/run/kubeflow/trainjobs.md:198** by [@NarayanaSabari](https://github.com/NarayanaSabari) — 2025-11-05T12:25:52Z
```diff
@@ -0,0 +1,374 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Kubeflow Trainer](https://www.kubeflow.org/docs/components/trainer/) TrainJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Overview
+
+Kubeflow Trainer v2 introduces the `TrainJob` API that works seamlessly with Kueue for batch scheduling and resource management. TrainJobs can be configured to use either:
+
+- **ClusterTrainingRuntime**: Cluster-scoped training runtimes that can be used across all namespaces
+- **TrainingRuntime**: Namespace-scoped training runtimes that are only available within a specific namespace
+
+Kueue manages TrainJobs by scheduling their underlying JobSets according to available quota and priority.
+
+## Before you begin
+
+1. Check [administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial cluster setup.
+
+2. Install Kubeflow Trainer v2. Check [the Trainer installation guide](https://www.kubeflow.org/docs/components/trainer/getting-started/).
+
+   **Note**: The minimum required Trainer version is v2.0.0.
+
+3. Enable TrainJob integration in Kueue. You can [modify kueue configurations from installed releases](/docs/installation#install-a-custom-configured-released-version) to include TrainJobs as an allowed workload.
+
+## TrainJob definition
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the TrainJob configuration:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Suspend field
+
+By default, Kueue will set `suspend` to true via webhook and unsuspend it when the TrainJob is admitted:
+
+```yaml
+spec:
+  suspend: true
+```
+
+## Using ClusterTrainingRuntime
+
+ClusterTrainingRuntimes are cluster-scoped resources that define training configurations accessible across all namespaces. They are typically created by platform administrators.
+
+### Example: PyTorch Distributed Training with ClusterTrainingRuntime
+
+First, create a ClusterTrainingRuntime for PyTorch distributed training:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: ClusterTrainingRuntime
+metadata:
+  name: torch-distributed
+  labels:
+    trainer.kubeflow.org/framework: torch
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+```
+
+Now, create a TrainJob that references this ClusterTrainingRuntime and will be scheduled by Kueue:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-distributed
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    command:
+      - torchrun
+      - /workspace/examples/mnist/mnist.py
+    numNodes: 2
+    resourcesPerNode:
+      requests:
+        cpu: "4"
+        memory: "8Gi"
+        nvidia.com/gpu: "1"
+```
+
+**Key Points:**
+- The `kueue.x-k8s.io/queue-name` label assigns this TrainJob to the `user-queue` LocalQueue
+- The `runtimeRef` points to the `ClusterTrainingRuntime` named `torch-distributed`
+- Kueue will manage the lifecycle and admission of this TrainJob based on available quota
+
+## Using TrainingRuntime (Namespace-scoped)
+
+TrainingRuntimes are namespace-scoped resources that provide more granular control per namespace. They are useful when different teams need customized training configurations.
+
+### Example: Custom PyTorch Training with TrainingRuntime
+
+Create a namespace-scoped TrainingRuntime:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainingRuntime
+metadata:
+  name: torch-custom
+  namespace: team-a
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+                      env:
+                        - name: CUSTOM_ENV
+                          value: "team-a-value"
+```
+
+Create a TrainJob that uses this namespace-scoped runtime:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-custom
+  namespace: team-a
+  labels:
+    kueue.x-k8s.io/queue-name: team-a-queue
+spec:
+  runtimeRef:
+    name: torch-custom
+    kind: TrainingRuntime
+    apiGroup: trainer.kubeflow.org
+  trainer:
+    image: docker.io/team-a/custom-training:latest
+    numNodes: 1
+    resourcesPerNode:
+      requests:
+        cpu: "2"
+        memory: "4Gi"
+```
+
+**Key Points:**
+- The TrainingRuntime is created in the same namespace as the TrainJob (`team-a`)
+- The `runtimeRef` specifies `kind: TrainingRuntime` to use the namespace-scoped runtime
+- Each namespace can have its own customized runtimes with different configurations
+
+## Using Kueue Priority
+
+You can set job priority using Kueue's PriorityClass:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: high-priority-training
+  namespace: default
+  labels:
```

  Agreed! I'll update this section to reference the workload priority class documentation at `/docs/tasks/run/jobs_with_priority` instead of providing a full example. I'll keep just a brief note showing that TrainJobs use the same `kueue.x-k8s.io/priority-class` label as other workload types.

### Review by [@NarayanaSabari](https://github.com/NarayanaSabari) (commented) — 2025-11-05T12:26:24Z

- **site/content/en/docs/tasks/run/trainjobs.md:211** by [@NarayanaSabari](https://github.com/NarayanaSabari) — 2025-11-05T12:26:24Z
```diff
@@ -0,0 +1,374 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Kubeflow Trainer](https://www.kubeflow.org/docs/components/trainer/) TrainJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Overview
+
+Kubeflow Trainer v2 introduces the `TrainJob` API that works seamlessly with Kueue for batch scheduling and resource management. TrainJobs can be configured to use either:
+
+- **ClusterTrainingRuntime**: Cluster-scoped training runtimes that can be used across all namespaces
+- **TrainingRuntime**: Namespace-scoped training runtimes that are only available within a specific namespace
+
+Kueue manages TrainJobs by scheduling their underlying JobSets according to available quota and priority.
+
+## Before you begin
+
+1. Check [administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial cluster setup.
+
+2. Install Kubeflow Trainer v2. Check [the Trainer installation guide](https://www.kubeflow.org/docs/components/trainer/getting-started/).
+
+   **Note**: The minimum required Trainer version is v2.0.0.
+
+3. Enable TrainJob integration in Kueue. You can [modify kueue configurations from installed releases](/docs/installation#install-a-custom-configured-released-version) to include TrainJobs as an allowed workload.
+
+## TrainJob definition
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the TrainJob configuration:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Suspend field
+
+By default, Kueue will set `suspend` to true via webhook and unsuspend it when the TrainJob is admitted:
+
+```yaml
+spec:
+  suspend: true
+```
+
+## Using ClusterTrainingRuntime
+
+ClusterTrainingRuntimes are cluster-scoped resources that define training configurations accessible across all namespaces. They are typically created by platform administrators.
+
+### Example: PyTorch Distributed Training with ClusterTrainingRuntime
+
+First, create a ClusterTrainingRuntime for PyTorch distributed training:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: ClusterTrainingRuntime
+metadata:
+  name: torch-distributed
+  labels:
+    trainer.kubeflow.org/framework: torch
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+```
+
+Now, create a TrainJob that references this ClusterTrainingRuntime and will be scheduled by Kueue:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-distributed
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    command:
+      - torchrun
+      - /workspace/examples/mnist/mnist.py
+    numNodes: 2
+    resourcesPerNode:
+      requests:
+        cpu: "4"
+        memory: "8Gi"
+        nvidia.com/gpu: "1"
+```
+
+**Key Points:**
+- The `kueue.x-k8s.io/queue-name` label assigns this TrainJob to the `user-queue` LocalQueue
+- The `runtimeRef` points to the `ClusterTrainingRuntime` named `torch-distributed`
+- Kueue will manage the lifecycle and admission of this TrainJob based on available quota
+
+## Using TrainingRuntime (Namespace-scoped)
+
+TrainingRuntimes are namespace-scoped resources that provide more granular control per namespace. They are useful when different teams need customized training configurations.
+
+### Example: Custom PyTorch Training with TrainingRuntime
+
+Create a namespace-scoped TrainingRuntime:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainingRuntime
+metadata:
+  name: torch-custom
+  namespace: team-a
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+                      env:
+                        - name: CUSTOM_ENV
+                          value: "team-a-value"
+```
+
+Create a TrainJob that uses this namespace-scoped runtime:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-custom
+  namespace: team-a
+  labels:
+    kueue.x-k8s.io/queue-name: team-a-queue
+spec:
+  runtimeRef:
+    name: torch-custom
+    kind: TrainingRuntime
+    apiGroup: trainer.kubeflow.org
+  trainer:
+    image: docker.io/team-a/custom-training:latest
+    numNodes: 1
+    resourcesPerNode:
+      requests:
+        cpu: "2"
+        memory: "4Gi"
+```
+
+**Key Points:**
+- The TrainingRuntime is created in the same namespace as the TrainJob (`team-a`)
+- The `runtimeRef` specifies `kind: TrainingRuntime` to use the namespace-scoped runtime
+- Each namespace can have its own customized runtimes with different configurations
+
+## Using Kueue Priority
+
+You can set job priority using Kueue's PriorityClass:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: high-priority-training
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: high-priority
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    numNodes: 2
+    resourcesPerNode:
+      requests:
+        cpu: "4"
+        memory: "8Gi"
+        nvidia.com/gpu: "1"
+```
+
+## LLM Fine-Tuning with Kueue
+
+For Large Language Model fine-tuning with TorchTune, you can use specialized ClusterTrainingRuntimes:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: llama-finetuning
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: gpu-queue
+spec:
+  runtimeRef:
+    name: torchtune-llama3.2-1b
+    kind: ClusterTrainingRuntime
+  initializer:
+    dataset:
+      storageUri: hf://tatsu-lab/alpaca
+    model:
+      storageUri: hf://meta-llama/Llama-3.2-1B-Instruct
+      accessToken: <your-hf-token>
+  trainer:
+    numNodes: 1
+    resourcesPerNode:
+      requests:
+        cpu: "8"
+        memory: "32Gi"
+        nvidia.com/gpu: "2"
+```
+
```

  You're right, these kubectl commands are generic across all Kueue workloads. I'll remove this section entirely or replace it with a single sentence referencing the general monitoring documentation. There's nothing TrainJob-specific here.

### Review by [@NarayanaSabari](https://github.com/NarayanaSabari) (commented) — 2025-11-05T12:28:06Z

- **site/content/en/docs/tasks/run/kubeflow/trainjobs.md:290** by [@NarayanaSabari](https://github.com/NarayanaSabari) — 2025-11-05T12:28:06Z
```diff
@@ -0,0 +1,374 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Kubeflow Trainer](https://www.kubeflow.org/docs/components/trainer/) TrainJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Overview
+
+Kubeflow Trainer v2 introduces the `TrainJob` API that works seamlessly with Kueue for batch scheduling and resource management. TrainJobs can be configured to use either:
+
+- **ClusterTrainingRuntime**: Cluster-scoped training runtimes that can be used across all namespaces
+- **TrainingRuntime**: Namespace-scoped training runtimes that are only available within a specific namespace
+
+Kueue manages TrainJobs by scheduling their underlying JobSets according to available quota and priority.
+
+## Before you begin
+
+1. Check [administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial cluster setup.
+
+2. Install Kubeflow Trainer v2. Check [the Trainer installation guide](https://www.kubeflow.org/docs/components/trainer/getting-started/).
+
+   **Note**: The minimum required Trainer version is v2.0.0.
+
+3. Enable TrainJob integration in Kueue. You can [modify kueue configurations from installed releases](/docs/installation#install-a-custom-configured-released-version) to include TrainJobs as an allowed workload.
+
+## TrainJob definition
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the TrainJob configuration:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Suspend field
+
+By default, Kueue will set `suspend` to true via webhook and unsuspend it when the TrainJob is admitted:
+
+```yaml
+spec:
+  suspend: true
+```
+
+## Using ClusterTrainingRuntime
+
+ClusterTrainingRuntimes are cluster-scoped resources that define training configurations accessible across all namespaces. They are typically created by platform administrators.
+
+### Example: PyTorch Distributed Training with ClusterTrainingRuntime
+
+First, create a ClusterTrainingRuntime for PyTorch distributed training:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: ClusterTrainingRuntime
+metadata:
+  name: torch-distributed
+  labels:
+    trainer.kubeflow.org/framework: torch
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+```
+
+Now, create a TrainJob that references this ClusterTrainingRuntime and will be scheduled by Kueue:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-distributed
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    command:
+      - torchrun
+      - /workspace/examples/mnist/mnist.py
+    numNodes: 2
+    resourcesPerNode:
+      requests:
+        cpu: "4"
+        memory: "8Gi"
+        nvidia.com/gpu: "1"
+```
+
+**Key Points:**
+- The `kueue.x-k8s.io/queue-name` label assigns this TrainJob to the `user-queue` LocalQueue
+- The `runtimeRef` points to the `ClusterTrainingRuntime` named `torch-distributed`
+- Kueue will manage the lifecycle and admission of this TrainJob based on available quota
+
+## Using TrainingRuntime (Namespace-scoped)
+
+TrainingRuntimes are namespace-scoped resources that provide more granular control per namespace. They are useful when different teams need customized training configurations.
+
+### Example: Custom PyTorch Training with TrainingRuntime
+
+Create a namespace-scoped TrainingRuntime:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainingRuntime
+metadata:
+  name: torch-custom
+  namespace: team-a
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+                      env:
+                        - name: CUSTOM_ENV
+                          value: "team-a-value"
+```
+
+Create a TrainJob that uses this namespace-scoped runtime:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-custom
+  namespace: team-a
+  labels:
+    kueue.x-k8s.io/queue-name: team-a-queue
+spec:
+  runtimeRef:
+    name: torch-custom
+    kind: TrainingRuntime
+    apiGroup: trainer.kubeflow.org
+  trainer:
+    image: docker.io/team-a/custom-training:latest
+    numNodes: 1
+    resourcesPerNode:
+      requests:
+        cpu: "2"
+        memory: "4Gi"
+```
+
+**Key Points:**
+- The TrainingRuntime is created in the same namespace as the TrainJob (`team-a`)
+- The `runtimeRef` specifies `kind: TrainingRuntime` to use the namespace-scoped runtime
+- Each namespace can have its own customized runtimes with different configurations
+
+## Using Kueue Priority
+
+You can set job priority using Kueue's PriorityClass:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: high-priority-training
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: high-priority
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    numNodes: 2
+    resourcesPerNode:
+      requests:
+        cpu: "4"
+        memory: "8Gi"
+        nvidia.com/gpu: "1"
+```
+
+## LLM Fine-Tuning with Kueue
+
+For Large Language Model fine-tuning with TorchTune, you can use specialized ClusterTrainingRuntimes:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: llama-finetuning
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: gpu-queue
+spec:
+  runtimeRef:
+    name: torchtune-llama3.2-1b
+    kind: ClusterTrainingRuntime
+  initializer:
+    dataset:
+      storageUri: hf://tatsu-lab/alpaca
+    model:
+      storageUri: hf://meta-llama/Llama-3.2-1B-Instruct
+      accessToken: <your-hf-token>
+  trainer:
+    numNodes: 1
+    resourcesPerNode:
+      requests:
+        cpu: "8"
+        memory: "32Gi"
+        nvidia.com/gpu: "2"
+```
+
+## Monitoring TrainJob Status
+
+You can check the status of your TrainJob:
+
+```bash
+kubectl get trainjob pytorch-distributed -n default
+```
+
+To see detailed information including Kueue admission status:
+
+```bash
+kubectl describe trainjob pytorch-distributed -n default
+```
+
+Check the workload status in Kueue:
+
+```bash
+kubectl get workload -n default
+```
+
+## Gang Scheduling with ClusterQueue
+
+For distributed training that requires all nodes to start simultaneously, configure your ClusterQueue appropriately:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: cluster-queue
+spec:
+  namespaceSelector: {}
+  resourceGroups:
+    - coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
+      flavors:
+        - name: default-flavor
+          resources:
+            - name: "cpu"
+              nominalQuota: 100
+            - name: "memory"
+              nominalQuota: 500Gi
+            - name: "nvidia.com/gpu"
+              nominalQuota: 8
+```
+
+## Cleanup
```

  Agreed, this ClusterQueue configuration is not specific to TrainJobs. I'll replace this section with a brief mention and reference to the ClusterQueue setup documentation. Gang scheduling is a general Kueue feature that applies to all job types.

### Review by [@NarayanaSabari](https://github.com/NarayanaSabari) (commented) — 2025-11-05T12:28:24Z

- **site/content/en/docs/tasks/run/trainjobs.md:191** by [@NarayanaSabari](https://github.com/NarayanaSabari) — 2025-11-05T12:28:24Z
```diff
@@ -0,0 +1,374 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Kubeflow Trainer](https://www.kubeflow.org/docs/components/trainer/) TrainJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Overview
+
+Kubeflow Trainer v2 introduces the `TrainJob` API that works seamlessly with Kueue for batch scheduling and resource management. TrainJobs can be configured to use either:
+
+- **ClusterTrainingRuntime**: Cluster-scoped training runtimes that can be used across all namespaces
+- **TrainingRuntime**: Namespace-scoped training runtimes that are only available within a specific namespace
+
+Kueue manages TrainJobs by scheduling their underlying JobSets according to available quota and priority.
+
+## Before you begin
+
+1. Check [administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial cluster setup.
+
+2. Install Kubeflow Trainer v2. Check [the Trainer installation guide](https://www.kubeflow.org/docs/components/trainer/getting-started/).
+
+   **Note**: The minimum required Trainer version is v2.0.0.
+
+3. Enable TrainJob integration in Kueue. You can [modify kueue configurations from installed releases](/docs/installation#install-a-custom-configured-released-version) to include TrainJobs as an allowed workload.
+
+## TrainJob definition
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the TrainJob configuration:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Suspend field
+
+By default, Kueue will set `suspend` to true via webhook and unsuspend it when the TrainJob is admitted:
+
+```yaml
+spec:
+  suspend: true
+```
+
+## Using ClusterTrainingRuntime
+
+ClusterTrainingRuntimes are cluster-scoped resources that define training configurations accessible across all namespaces. They are typically created by platform administrators.
+
+### Example: PyTorch Distributed Training with ClusterTrainingRuntime
+
+First, create a ClusterTrainingRuntime for PyTorch distributed training:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: ClusterTrainingRuntime
+metadata:
+  name: torch-distributed
+  labels:
+    trainer.kubeflow.org/framework: torch
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+```
+
+Now, create a TrainJob that references this ClusterTrainingRuntime and will be scheduled by Kueue:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-distributed
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    command:
+      - torchrun
+      - /workspace/examples/mnist/mnist.py
+    numNodes: 2
+    resourcesPerNode:
+      requests:
+        cpu: "4"
+        memory: "8Gi"
+        nvidia.com/gpu: "1"
+```
+
+**Key Points:**
+- The `kueue.x-k8s.io/queue-name` label assigns this TrainJob to the `user-queue` LocalQueue
+- The `runtimeRef` points to the `ClusterTrainingRuntime` named `torch-distributed`
+- Kueue will manage the lifecycle and admission of this TrainJob based on available quota
+
+## Using TrainingRuntime (Namespace-scoped)
+
+TrainingRuntimes are namespace-scoped resources that provide more granular control per namespace. They are useful when different teams need customized training configurations.
+
+### Example: Custom PyTorch Training with TrainingRuntime
+
+Create a namespace-scoped TrainingRuntime:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainingRuntime
+metadata:
+  name: torch-custom
+  namespace: team-a
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+                      env:
+                        - name: CUSTOM_ENV
+                          value: "team-a-value"
+```
+
+Create a TrainJob that uses this namespace-scoped runtime:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-custom
+  namespace: team-a
+  labels:
+    kueue.x-k8s.io/queue-name: team-a-queue
+spec:
+  runtimeRef:
+    name: torch-custom
+    kind: TrainingRuntime
+    apiGroup: trainer.kubeflow.org
+  trainer:
+    image: docker.io/team-a/custom-training:latest
+    numNodes: 1
+    resourcesPerNode:
+      requests:
+        cpu: "2"
+        memory: "4Gi"
+```
+
+**Key Points:**
+- The TrainingRuntime is created in the same namespace as the TrainJob (`team-a`)
+- The `runtimeRef` specifies `kind: TrainingRuntime` to use the namespace-scoped runtime
+- Each namespace can have its own customized runtimes with different configurations
+
+## Using Kueue Priority
+
+You can set job priority using Kueue's PriorityClass:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: high-priority-training
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: high-priority
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    numNodes: 2
+    resourcesPerNode:
+      requests:
+        cpu: "4"
+        memory: "8Gi"
+        nvidia.com/gpu: "1"
+```
+
+## LLM Fine-Tuning with Kueue
```

  Yes! Kubeflow Trainer has comprehensive examples in their repo:
  - TorchTune Llama 3.2: https://github.com/kubeflow/trainer/blob/master/examples/torchtune/llama3_2/alpaca-trainjob-yaml.ipynb
  - TorchTune Qwen 2.5: https://github.com/kubeflow/trainer/blob/master/examples/torchtune/qwen2_5/qwen2.5-1.5B-with-alpaca.ipynb
  - DeepSpeed T5: https://github.com/kubeflow/trainer/blob/master/examples/deepspeed/text-summarization/T5-Fine-Tuning.ipynb
  
  I'll update this section to reference these examples and only show the Kueue-specific queue label addition, keeping it focused on what's unique to the integration.

### Review by [@andreyvelich](https://github.com/andreyvelich) (commented) — 2025-11-05T17:15:56Z

Thanks a lot for working on this @NarayanaSabari 🎉 
cc @tenzen-y @astefanutti @kaisoz @Electronic-Waste

- **site/content/en/docs/tasks/run/trainjobs.md:1** by [@andreyvelich](https://github.com/andreyvelich) — 2025-11-05T17:05:55Z
```diff
@@ -0,0 +1,258 @@
+---
```

  Given that TrainJob is replacement for all other Kubeflow Jobs, shall we have dedicated section at the website:
  Kubeflow TrainJob
  Similar to RayJob: https://kueue.sigs.k8s.io/docs/tasks/run/rayjobs/ and RayService: https://kueue.sigs.k8s.io/docs/tasks/run/rayservices/
  
  I would mark the rest of the Kubeflow Jobs (legacy), since we will deprecate them in the next year.

- **site/content/en/docs/tasks/run/kubeflow/trainjobs.md:20** by [@andreyvelich](https://github.com/andreyvelich) — 2025-11-05T17:08:49Z
```diff
@@ -0,0 +1,258 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Kubeflow Trainer](https://www.kubeflow.org/docs/components/trainer/) TrainJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Overview
+
+Kubeflow Trainer v2 introduces the `TrainJob` API that works seamlessly with Kueue for batch scheduling and resource management. TrainJobs can be configured to use either:
+
+- **ClusterTrainingRuntime**: Cluster-scoped training runtimes that can be used across all namespaces
+- **TrainingRuntime**: Namespace-scoped training runtimes that are only available within a specific namespace
+
+Kueue manages TrainJobs by scheduling their underlying JobSets according to available quota and priority.
```

  I wound't focus on JobSet here, since in the future we might also support Slurm and Flux jobs: https://github.com/kubeflow/trainer/pull/2909
  
  cc @vsoch 
  ```suggestion
  Kueue manages TrainJobs by scheduling their underlying jobs according to available quota and priority.
  ```

- **site/content/en/docs/tasks/run/kubeflow/trainjobs.md:26** by [@andreyvelich](https://github.com/andreyvelich) — 2025-11-05T17:09:27Z
```diff
@@ -0,0 +1,258 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Kubeflow Trainer](https://www.kubeflow.org/docs/components/trainer/) TrainJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Overview
+
+Kubeflow Trainer v2 introduces the `TrainJob` API that works seamlessly with Kueue for batch scheduling and resource management. TrainJobs can be configured to use either:
+
+- **ClusterTrainingRuntime**: Cluster-scoped training runtimes that can be used across all namespaces
+- **TrainingRuntime**: Namespace-scoped training runtimes that are only available within a specific namespace
+
+Kueue manages TrainJobs by scheduling their underlying JobSets according to available quota and priority.
+
+## Before you begin
+
+1. Check [administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial cluster setup.
+
+2. Install Kubeflow Trainer v2. Check [the Trainer installation guide](https://www.kubeflow.org/docs/components/trainer/getting-started/).
```

  Please redirect to this page
  ```suggestion
  2. Install Kubeflow Trainer v2. Check [the Trainer installation guide](https://www.kubeflow.org/docs/components/trainer/operator-guides/installation/).
  ```

- **site/content/en/docs/tasks/run/trainjob/trainjobs.md:107** by [@andreyvelich](https://github.com/andreyvelich) — 2025-11-05T17:11:09Z
```diff
@@ -0,0 +1,258 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Kubeflow Trainer](https://www.kubeflow.org/docs/components/trainer/) TrainJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Overview
+
+Kubeflow Trainer v2 introduces the `TrainJob` API that works seamlessly with Kueue for batch scheduling and resource management. TrainJobs can be configured to use either:
+
+- **ClusterTrainingRuntime**: Cluster-scoped training runtimes that can be used across all namespaces
+- **TrainingRuntime**: Namespace-scoped training runtimes that are only available within a specific namespace
+
+Kueue manages TrainJobs by scheduling their underlying JobSets according to available quota and priority.
+
+## Before you begin
+
+1. Check [administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial cluster setup.
+
+2. Install Kubeflow Trainer v2. Check [the Trainer installation guide](https://www.kubeflow.org/docs/components/trainer/getting-started/).
+
+   **Note**: The minimum required Trainer version is v2.0.0.
+
+3. Enable TrainJob integration in Kueue. You can [modify kueue configurations from installed releases](/docs/installation#install-a-custom-configured-released-version) to include TrainJobs as an allowed workload.
+
+## TrainJob definition
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the TrainJob configuration:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Suspend field
+
+By default, Kueue will set `suspend` to true via webhook and unsuspend it when the TrainJob is admitted:
+
+```yaml
+spec:
+  suspend: true
+```
+
+## Using ClusterTrainingRuntime
+
+ClusterTrainingRuntimes are cluster-scoped resources that define training configurations accessible across all namespaces. They are typically created by platform administrators.
+
+### Example: PyTorch Distributed Training with ClusterTrainingRuntime
+
+First, create a ClusterTrainingRuntime for PyTorch distributed training:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: ClusterTrainingRuntime
+metadata:
+  name: torch-distributed
+  labels:
+    trainer.kubeflow.org/framework: torch
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+```
+
+Now, create a TrainJob that references this ClusterTrainingRuntime and will be scheduled by Kueue:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-distributed
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    command:
+      - torchrun
+      - /workspace/examples/mnist/mnist.py
```

  Please can you verify that this image is still functional ?

- **site/content/en/docs/tasks/run/kubeflow/trainjobs.md:55** by [@andreyvelich](https://github.com/andreyvelich) — 2025-11-05T17:11:35Z
```diff
@@ -0,0 +1,258 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Kubeflow Trainer](https://www.kubeflow.org/docs/components/trainer/) TrainJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Overview
+
+Kubeflow Trainer v2 introduces the `TrainJob` API that works seamlessly with Kueue for batch scheduling and resource management. TrainJobs can be configured to use either:
+
+- **ClusterTrainingRuntime**: Cluster-scoped training runtimes that can be used across all namespaces
+- **TrainingRuntime**: Namespace-scoped training runtimes that are only available within a specific namespace
+
+Kueue manages TrainJobs by scheduling their underlying JobSets according to available quota and priority.
+
+## Before you begin
+
+1. Check [administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial cluster setup.
+
+2. Install Kubeflow Trainer v2. Check [the Trainer installation guide](https://www.kubeflow.org/docs/components/trainer/getting-started/).
+
+   **Note**: The minimum required Trainer version is v2.0.0.
+
+3. Enable TrainJob integration in Kueue. You can [modify kueue configurations from installed releases](/docs/installation#install-a-custom-configured-released-version) to include TrainJobs as an allowed workload.
+
+## TrainJob definition
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the TrainJob configuration:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Suspend field
+
+By default, Kueue will set `suspend` to true via webhook and unsuspend it when the TrainJob is admitted:
+
+```yaml
+spec:
+  suspend: true
+```
+
+## Using ClusterTrainingRuntime
+
+ClusterTrainingRuntimes are cluster-scoped resources that define training configurations accessible across all namespaces. They are typically created by platform administrators.
```

  Would be nice to cross-reference this guide: https://www.kubeflow.org/docs/components/trainer/operator-guides/runtime/

- **site/content/en/docs/tasks/run/trainjobs.md:211** by [@andreyvelich](https://github.com/andreyvelich) — 2025-11-05T17:14:43Z
```diff
@@ -0,0 +1,374 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Kubeflow Trainer](https://www.kubeflow.org/docs/components/trainer/) TrainJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Overview
+
+Kubeflow Trainer v2 introduces the `TrainJob` API that works seamlessly with Kueue for batch scheduling and resource management. TrainJobs can be configured to use either:
+
+- **ClusterTrainingRuntime**: Cluster-scoped training runtimes that can be used across all namespaces
+- **TrainingRuntime**: Namespace-scoped training runtimes that are only available within a specific namespace
+
+Kueue manages TrainJobs by scheduling their underlying JobSets according to available quota and priority.
+
+## Before you begin
+
+1. Check [administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial cluster setup.
+
+2. Install Kubeflow Trainer v2. Check [the Trainer installation guide](https://www.kubeflow.org/docs/components/trainer/getting-started/).
+
+   **Note**: The minimum required Trainer version is v2.0.0.
+
+3. Enable TrainJob integration in Kueue. You can [modify kueue configurations from installed releases](/docs/installation#install-a-custom-configured-released-version) to include TrainJobs as an allowed workload.
+
+## TrainJob definition
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the TrainJob configuration:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Suspend field
+
+By default, Kueue will set `suspend` to true via webhook and unsuspend it when the TrainJob is admitted:
+
+```yaml
+spec:
+  suspend: true
+```
+
+## Using ClusterTrainingRuntime
+
+ClusterTrainingRuntimes are cluster-scoped resources that define training configurations accessible across all namespaces. They are typically created by platform administrators.
+
+### Example: PyTorch Distributed Training with ClusterTrainingRuntime
+
+First, create a ClusterTrainingRuntime for PyTorch distributed training:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: ClusterTrainingRuntime
+metadata:
+  name: torch-distributed
+  labels:
+    trainer.kubeflow.org/framework: torch
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+```
+
+Now, create a TrainJob that references this ClusterTrainingRuntime and will be scheduled by Kueue:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-distributed
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    command:
+      - torchrun
+      - /workspace/examples/mnist/mnist.py
+    numNodes: 2
+    resourcesPerNode:
+      requests:
+        cpu: "4"
+        memory: "8Gi"
+        nvidia.com/gpu: "1"
+```
+
+**Key Points:**
+- The `kueue.x-k8s.io/queue-name` label assigns this TrainJob to the `user-queue` LocalQueue
+- The `runtimeRef` points to the `ClusterTrainingRuntime` named `torch-distributed`
+- Kueue will manage the lifecycle and admission of this TrainJob based on available quota
+
+## Using TrainingRuntime (Namespace-scoped)
+
+TrainingRuntimes are namespace-scoped resources that provide more granular control per namespace. They are useful when different teams need customized training configurations.
+
+### Example: Custom PyTorch Training with TrainingRuntime
+
+Create a namespace-scoped TrainingRuntime:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainingRuntime
+metadata:
+  name: torch-custom
+  namespace: team-a
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+                      env:
+                        - name: CUSTOM_ENV
+                          value: "team-a-value"
+```
+
+Create a TrainJob that uses this namespace-scoped runtime:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-custom
+  namespace: team-a
+  labels:
+    kueue.x-k8s.io/queue-name: team-a-queue
+spec:
+  runtimeRef:
+    name: torch-custom
+    kind: TrainingRuntime
+    apiGroup: trainer.kubeflow.org
+  trainer:
+    image: docker.io/team-a/custom-training:latest
+    numNodes: 1
+    resourcesPerNode:
+      requests:
+        cpu: "2"
+        memory: "4Gi"
+```
+
+**Key Points:**
+- The TrainingRuntime is created in the same namespace as the TrainJob (`team-a`)
+- The `runtimeRef` specifies `kind: TrainingRuntime` to use the namespace-scoped runtime
+- Each namespace can have its own customized runtimes with different configurations
+
+## Using Kueue Priority
+
+You can set job priority using Kueue's PriorityClass:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: high-priority-training
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: high-priority
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    numNodes: 2
+    resourcesPerNode:
+      requests:
+        cpu: "4"
+        memory: "8Gi"
+        nvidia.com/gpu: "1"
+```
+
+## LLM Fine-Tuning with Kueue
+
+For Large Language Model fine-tuning with TorchTune, you can use specialized ClusterTrainingRuntimes:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: llama-finetuning
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: gpu-queue
+spec:
+  runtimeRef:
+    name: torchtune-llama3.2-1b
+    kind: ClusterTrainingRuntime
+  initializer:
+    dataset:
+      storageUri: hf://tatsu-lab/alpaca
+    model:
+      storageUri: hf://meta-llama/Llama-3.2-1B-Instruct
+      accessToken: <your-hf-token>
+  trainer:
+    numNodes: 1
+    resourcesPerNode:
+      requests:
+        cpu: "8"
+        memory: "32Gi"
+        nvidia.com/gpu: "2"
+```
+
```

  Yes, probably, we can just at the beginning link to the BuiltinTrainer guides: https://www.kubeflow.org/docs/components/trainer/user-guides/builtin-trainer/torchtune/

### Review by [@NarayanaSabari](https://github.com/NarayanaSabari) (commented) — 2025-11-06T09:45:10Z

- **site/content/en/docs/tasks/run/trainjobs.md:1** by [@NarayanaSabari](https://github.com/NarayanaSabari) — 2025-11-06T09:45:10Z
```diff
@@ -0,0 +1,258 @@
+---
```

  Great idea! Having a dedicated "Kubeflow TrainJob" section similar to RayJob/RayService makes a lot of sense since TrainJob is replacing all legacy Kubeflow job types. 
  
  Should I:
  1. Create a new top-level "Kubeflow TrainJob" section in the navigation?
  2. Move this document to a new path like `/docs/tasks/run/trainjobs/` (without the kubeflow subfolder)?
  3. Update the index page to mark PyTorchJob/TFJob/etc. as "(legacy)"?
  
  I'm happy to implement this restructuring - just need guidance on your preferred approach for the site organization. Should I do this in this PR or a follow-up?

### Review by [@NarayanaSabari](https://github.com/NarayanaSabari) (commented) — 2025-11-06T09:45:32Z

- **site/content/en/docs/tasks/run/kubeflow/trainjobs.md:20** by [@NarayanaSabari](https://github.com/NarayanaSabari) — 2025-11-06T09:45:32Z
```diff
@@ -0,0 +1,258 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Kubeflow Trainer](https://www.kubeflow.org/docs/components/trainer/) TrainJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Overview
+
+Kubeflow Trainer v2 introduces the `TrainJob` API that works seamlessly with Kueue for batch scheduling and resource management. TrainJobs can be configured to use either:
+
+- **ClusterTrainingRuntime**: Cluster-scoped training runtimes that can be used across all namespaces
+- **TrainingRuntime**: Namespace-scoped training runtimes that are only available within a specific namespace
+
+Kueue manages TrainJobs by scheduling their underlying JobSets according to available quota and priority.
```

  ✅ Fixed! Changed "underlying JobSets" to "underlying jobs" to account for future Slurm/Flux support. Thanks for catching this!

### Review by [@NarayanaSabari](https://github.com/NarayanaSabari) (commented) — 2025-11-06T09:45:44Z

- **site/content/en/docs/tasks/run/kubeflow/trainjobs.md:26** by [@NarayanaSabari](https://github.com/NarayanaSabari) — 2025-11-06T09:45:43Z
```diff
@@ -0,0 +1,258 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Kubeflow Trainer](https://www.kubeflow.org/docs/components/trainer/) TrainJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Overview
+
+Kubeflow Trainer v2 introduces the `TrainJob` API that works seamlessly with Kueue for batch scheduling and resource management. TrainJobs can be configured to use either:
+
+- **ClusterTrainingRuntime**: Cluster-scoped training runtimes that can be used across all namespaces
+- **TrainingRuntime**: Namespace-scoped training runtimes that are only available within a specific namespace
+
+Kueue manages TrainJobs by scheduling their underlying JobSets according to available quota and priority.
+
+## Before you begin
+
+1. Check [administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial cluster setup.
+
+2. Install Kubeflow Trainer v2. Check [the Trainer installation guide](https://www.kubeflow.org/docs/components/trainer/getting-started/).
```

  ✅ Updated to point to the operator installation guide:
  `https://www.kubeflow.org/docs/components/trainer/operator-guides/installation/`
  
  Thanks for the correction!

### Review by [@NarayanaSabari](https://github.com/NarayanaSabari) (commented) — 2025-11-06T09:46:06Z

- **site/content/en/docs/tasks/run/kubeflow/trainjobs.md:55** by [@NarayanaSabari](https://github.com/NarayanaSabari) — 2025-11-06T09:46:06Z
```diff
@@ -0,0 +1,258 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Kubeflow Trainer](https://www.kubeflow.org/docs/components/trainer/) TrainJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Overview
+
+Kubeflow Trainer v2 introduces the `TrainJob` API that works seamlessly with Kueue for batch scheduling and resource management. TrainJobs can be configured to use either:
+
+- **ClusterTrainingRuntime**: Cluster-scoped training runtimes that can be used across all namespaces
+- **TrainingRuntime**: Namespace-scoped training runtimes that are only available within a specific namespace
+
+Kueue manages TrainJobs by scheduling their underlying JobSets according to available quota and priority.
+
+## Before you begin
+
+1. Check [administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial cluster setup.
+
+2. Install Kubeflow Trainer v2. Check [the Trainer installation guide](https://www.kubeflow.org/docs/components/trainer/getting-started/).
+
+   **Note**: The minimum required Trainer version is v2.0.0.
+
+3. Enable TrainJob integration in Kueue. You can [modify kueue configurations from installed releases](/docs/installation#install-a-custom-configured-released-version) to include TrainJobs as an allowed workload.
+
+## TrainJob definition
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the TrainJob configuration:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Suspend field
+
+By default, Kueue will set `suspend` to true via webhook and unsuspend it when the TrainJob is admitted:
+
+```yaml
+spec:
+  suspend: true
+```
+
+## Using ClusterTrainingRuntime
+
+ClusterTrainingRuntimes are cluster-scoped resources that define training configurations accessible across all namespaces. They are typically created by platform administrators.
```

  ✅ Added cross-reference to the runtime guide. The ClusterTrainingRuntime section now includes:
  
  > "For more details on ClusterTrainingRuntime configuration, see the [Kubeflow Runtime Guide](https://www.kubeflow.org/docs/components/trainer/operator-guides/runtime/)."

### Review by [@NarayanaSabari](https://github.com/NarayanaSabari) (commented) — 2025-11-06T09:46:31Z

- **site/content/en/docs/tasks/run/trainjob/trainjobs.md:107** by [@NarayanaSabari](https://github.com/NarayanaSabari) — 2025-11-06T09:46:31Z
```diff
@@ -0,0 +1,258 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Kubeflow Trainer](https://www.kubeflow.org/docs/components/trainer/) TrainJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Overview
+
+Kubeflow Trainer v2 introduces the `TrainJob` API that works seamlessly with Kueue for batch scheduling and resource management. TrainJobs can be configured to use either:
+
+- **ClusterTrainingRuntime**: Cluster-scoped training runtimes that can be used across all namespaces
+- **TrainingRuntime**: Namespace-scoped training runtimes that are only available within a specific namespace
+
+Kueue manages TrainJobs by scheduling their underlying JobSets according to available quota and priority.
+
+## Before you begin
+
+1. Check [administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial cluster setup.
+
+2. Install Kubeflow Trainer v2. Check [the Trainer installation guide](https://www.kubeflow.org/docs/components/trainer/getting-started/).
+
+   **Note**: The minimum required Trainer version is v2.0.0.
+
+3. Enable TrainJob integration in Kueue. You can [modify kueue configurations from installed releases](/docs/installation#install-a-custom-configured-released-version) to include TrainJobs as an allowed workload.
+
+## TrainJob definition
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the TrainJob configuration:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Suspend field
+
+By default, Kueue will set `suspend` to true via webhook and unsuspend it when the TrainJob is admitted:
+
+```yaml
+spec:
+  suspend: true
+```
+
+## Using ClusterTrainingRuntime
+
+ClusterTrainingRuntimes are cluster-scoped resources that define training configurations accessible across all namespaces. They are typically created by platform administrators.
+
+### Example: PyTorch Distributed Training with ClusterTrainingRuntime
+
+First, create a ClusterTrainingRuntime for PyTorch distributed training:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: ClusterTrainingRuntime
+metadata:
+  name: torch-distributed
+  labels:
+    trainer.kubeflow.org/framework: torch
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+```
+
+Now, create a TrainJob that references this ClusterTrainingRuntime and will be scheduled by Kueue:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-distributed
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    command:
+      - torchrun
+      - /workspace/examples/mnist/mnist.py
```

  Good catch! I need to verify that `docker.io/kubeflow/pytorch-dist-mnist-test:v1.0` still works. 
  
  Could you recommend a replacement if this image is deprecated? I noticed the Trainer repo has several training images - should I use one from the current examples like:
  - `docker.io/kubeflowkatib/pytorch-mnist:v1beta1-45c5727` 
  - Or another image from the Trainer manifests?
  
  I'll test and update accordingly.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-06T10:42:08Z

- **site/content/en/docs/tasks/run/trainjobs.md:7** by [@mimowo](https://github.com/mimowo) — 2025-11-06T10:42:08Z
```diff
@@ -0,0 +1,261 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
```

  Seems like some rendering issue to solve:
  
  <img width="1489" height="734" alt="Image" src="https://github.com/user-attachments/assets/2df3f552-720b-44fc-9898-4e936e344906" />

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-06T10:44:26Z

- **site/content/en/docs/tasks/run/trainjobs.md:1** by [@mimowo](https://github.com/mimowo) — 2025-11-06T10:44:26Z
```diff
@@ -0,0 +1,258 @@
+---
```

  Good point! I would suggest in this PR:
  "Kubeflow TrainJob (v2)"
  "Kubeflow Jobs (v1)" (with the sub-pages as currently)
   
   I think I would wait with calling it legacy, since v2 is still alpha, so v1 is what is going to be used on production for long. Calling legacy something which does not yet have out-of-alpha replacement is too early I think.

### Review by [@NarayanaSabari](https://github.com/NarayanaSabari) (commented) — 2025-11-06T11:29:10Z

- **site/content/en/docs/tasks/run/trainjobs.md:7** by [@NarayanaSabari](https://github.com/NarayanaSabari) — 2025-11-06T11:29:10Z
```diff
@@ -0,0 +1,261 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
```

  Thanks for the catch, i have remove the overview section and added the context on the above section

### Review by [@andreyvelich](https://github.com/andreyvelich) (commented) — 2025-11-06T11:30:42Z

- **site/content/en/docs/tasks/run/trainjobs.md:1** by [@andreyvelich](https://github.com/andreyvelich) — 2025-11-06T11:30:42Z
```diff
@@ -0,0 +1,258 @@
+---
```

  Sounds good, let's keep it v1 for now.

### Review by [@NarayanaSabari](https://github.com/NarayanaSabari) (commented) — 2025-11-06T11:47:12Z

- **site/content/en/docs/tasks/run/trainjobs.md:1** by [@NarayanaSabari](https://github.com/NarayanaSabari) — 2025-11-06T11:47:12Z
```diff
@@ -0,0 +1,258 @@
+---
```

  @mimowo @andreyvelich 
  
  Before implementing the website restructuring, I need your approval on the approach:
  
  ## Current File Structure
  ```
  docs/tasks/run/kubeflow/
  ├── _index.md (title: "Kubeflow Jobs")
  ├── trainjobs.md (NEW - TrainJob v2)
  ├── mpijobs.md
  ├── pytorchjobs.md
  ├── tfjobs.md
  ├── xgboostjobs.md
  ├── paddlejobs.md
  └── jaxjobs.md
  ```
  
  ## Proposed Approach
  
  **Keep current file locations**, update only `_index.md` to show this navigation tree:
  
  ```
  Kubeflow Training
  ├── Trainer v2
  │   └── Run a TrainJob
  ├── Trainer v1
  │   ├── Run a PyTorchJob
  │   ├── Run a TFJob
  │   ├── Run an XGBoostJob
  │   ├── Run a PaddleJob
  │   └── Run a JAXJob
  └── MPI Operator
      └── Run an MPIJob
  ```
  
  **Implementation:** Only modify `_index.md` content, keep all files in same location.
  
  ## Benefits
  - ✅ No file moves (existing links keep working)
  - ✅ Clear v2 vs v1 distinction
  - ✅ Easy to update when v2 goes stable
  
  ## Questions
  1. Is this structure acceptable?
  2. Should I implement in **this PR** or **follow-up PR**?
  
  Please confirm and I'll proceed. Thanks!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-06T11:50:20Z

- **site/content/en/docs/tasks/run/trainjobs.md:1** by [@mimowo](https://github.com/mimowo) — 2025-11-06T11:50:19Z
```diff
@@ -0,0 +1,258 @@
+---
```

  Proposed Approach sgtm

### Review by [@andreyvelich](https://github.com/andreyvelich) (commented) — 2025-11-06T11:52:57Z

- **site/content/en/docs/tasks/run/trainjobs.md:1** by [@andreyvelich](https://github.com/andreyvelich) — 2025-11-06T11:52:57Z
```diff
@@ -0,0 +1,258 @@
+---
```

  I suggested to move Kubeflow TrainJob (v2) to the root:
  ```
  # Name this page Kubeflow TrainJob (v2)
  /docs/tasks/run/trainjob/
  
  # Rename the below section to Kubeflow Jobs (v1)
  /docs/tasks/run/kubeflow/jaxjobs
  /docs/tasks/run/kubeflow/tfjob
  
  
  ```

### Review by [@andreyvelich](https://github.com/andreyvelich) (commented) — 2025-11-06T11:53:44Z

- **site/content/en/docs/tasks/run/trainjob/trainjobs.md:107** by [@andreyvelich](https://github.com/andreyvelich) — 2025-11-06T11:53:44Z
```diff
@@ -0,0 +1,258 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Kubeflow Trainer](https://www.kubeflow.org/docs/components/trainer/) TrainJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Overview
+
+Kubeflow Trainer v2 introduces the `TrainJob` API that works seamlessly with Kueue for batch scheduling and resource management. TrainJobs can be configured to use either:
+
+- **ClusterTrainingRuntime**: Cluster-scoped training runtimes that can be used across all namespaces
+- **TrainingRuntime**: Namespace-scoped training runtimes that are only available within a specific namespace
+
+Kueue manages TrainJobs by scheduling their underlying JobSets according to available quota and priority.
+
+## Before you begin
+
+1. Check [administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial cluster setup.
+
+2. Install Kubeflow Trainer v2. Check [the Trainer installation guide](https://www.kubeflow.org/docs/components/trainer/getting-started/).
+
+   **Note**: The minimum required Trainer version is v2.0.0.
+
+3. Enable TrainJob integration in Kueue. You can [modify kueue configurations from installed releases](/docs/installation#install-a-custom-configured-released-version) to include TrainJobs as an allowed workload.
+
+## TrainJob definition
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the TrainJob configuration:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Suspend field
+
+By default, Kueue will set `suspend` to true via webhook and unsuspend it when the TrainJob is admitted:
+
+```yaml
+spec:
+  suspend: true
+```
+
+## Using ClusterTrainingRuntime
+
+ClusterTrainingRuntimes are cluster-scoped resources that define training configurations accessible across all namespaces. They are typically created by platform administrators.
+
+### Example: PyTorch Distributed Training with ClusterTrainingRuntime
+
+First, create a ClusterTrainingRuntime for PyTorch distributed training:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: ClusterTrainingRuntime
+metadata:
+  name: torch-distributed
+  labels:
+    trainer.kubeflow.org/framework: torch
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+```
+
+Now, create a TrainJob that references this ClusterTrainingRuntime and will be scheduled by Kueue:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-distributed
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    command:
+      - torchrun
+      - /workspace/examples/mnist/mnist.py
```

  We don't have training image that we maintain, please try to run this image and see if that is working.

### Review by [@NarayanaSabari](https://github.com/NarayanaSabari) (commented) — 2025-11-06T12:11:14Z

- **site/content/en/docs/tasks/run/trainjobs.md:1** by [@NarayanaSabari](https://github.com/NarayanaSabari) — 2025-11-06T12:11:14Z
```diff
@@ -0,0 +1,258 @@
+---
```

  @andreyvelich @mimowo Updated are made, good to go

### Review by [@NarayanaSabari](https://github.com/NarayanaSabari) (commented) — 2025-11-06T12:21:32Z

- **site/content/en/docs/tasks/run/trainjob/trainjobs.md:107** by [@NarayanaSabari](https://github.com/NarayanaSabari) — 2025-11-06T12:21:32Z
```diff
@@ -0,0 +1,258 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Kubeflow Trainer](https://www.kubeflow.org/docs/components/trainer/) TrainJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Overview
+
+Kubeflow Trainer v2 introduces the `TrainJob` API that works seamlessly with Kueue for batch scheduling and resource management. TrainJobs can be configured to use either:
+
+- **ClusterTrainingRuntime**: Cluster-scoped training runtimes that can be used across all namespaces
+- **TrainingRuntime**: Namespace-scoped training runtimes that are only available within a specific namespace
+
+Kueue manages TrainJobs by scheduling their underlying JobSets according to available quota and priority.
+
+## Before you begin
+
+1. Check [administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial cluster setup.
+
+2. Install Kubeflow Trainer v2. Check [the Trainer installation guide](https://www.kubeflow.org/docs/components/trainer/getting-started/).
+
+   **Note**: The minimum required Trainer version is v2.0.0.
+
+3. Enable TrainJob integration in Kueue. You can [modify kueue configurations from installed releases](/docs/installation#install-a-custom-configured-released-version) to include TrainJobs as an allowed workload.
+
+## TrainJob definition
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the TrainJob configuration:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Suspend field
+
+By default, Kueue will set `suspend` to true via webhook and unsuspend it when the TrainJob is admitted:
+
+```yaml
+spec:
+  suspend: true
+```
+
+## Using ClusterTrainingRuntime
+
+ClusterTrainingRuntimes are cluster-scoped resources that define training configurations accessible across all namespaces. They are typically created by platform administrators.
+
+### Example: PyTorch Distributed Training with ClusterTrainingRuntime
+
+First, create a ClusterTrainingRuntime for PyTorch distributed training:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: ClusterTrainingRuntime
+metadata:
+  name: torch-distributed
+  labels:
+    trainer.kubeflow.org/framework: torch
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+```
+
+Now, create a TrainJob that references this ClusterTrainingRuntime and will be scheduled by Kueue:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-distributed
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    command:
+      - torchrun
+      - /workspace/examples/mnist/mnist.py
```

  ## Issue
  The image `docker.io/kubeflow/pytorch-dist-mnist-test:v1.0` no longer exists on Docker Hub.
  
  ### Verification Attempt
  ```bash
  $ docker run --rm -it docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
  docker: Error response from daemon: pull access denied for kubeflow/pytorch-dist-mnist-test, 
  repository does not exist or may require 'docker login'
  ```
  
  ## Proposed Solution
  
  Replace with a placeholder approach that shows users the correct pattern:
  
  ```yaml
  trainer:
    image: <your-registry>/pytorch-training:latest
    command:
      - torchrun
      - /workspace/train.py
    numNodes: 2
    resourcesPerNode:
      requests:
        cpu: "4"
        memory: "8Gi"
        nvidia.com/gpu: "1"
  ```
  
  **Add explanatory note:**
  > **Note**: Replace `<your-registry>/pytorch-training:latest` with your own training image containing your model code and dependencies.
  
  ### Rationale
  - Avoids dependency on external images that may break
  - Shows realistic usage pattern (users bring their own images)
  - Aligns with production TrainJob usage
  - Maintains focus on Kueue scheduling aspects
  
  ## Alternative Options
  
  1. **Use official PyTorch image** - `pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime` with inline script
  2. **Reference Kubeflow examples** - Link to maintained examples in kubeflow/trainer repository
  
  **Recommendation**: Placeholder approach (primary proposal) - clearest for documentation purposes.
  
  ---
  **Question for maintainer**: Does this approach work for you, or would you prefer one of the alternatives?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-06T12:27:27Z

- **site/content/en/docs/tasks/run/trainjob/trainjobs.md:107** by [@mimowo](https://github.com/mimowo) — 2025-11-06T12:27:27Z
```diff
@@ -0,0 +1,258 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Kubeflow Trainer](https://www.kubeflow.org/docs/components/trainer/) TrainJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Overview
+
+Kubeflow Trainer v2 introduces the `TrainJob` API that works seamlessly with Kueue for batch scheduling and resource management. TrainJobs can be configured to use either:
+
+- **ClusterTrainingRuntime**: Cluster-scoped training runtimes that can be used across all namespaces
+- **TrainingRuntime**: Namespace-scoped training runtimes that are only available within a specific namespace
+
+Kueue manages TrainJobs by scheduling their underlying JobSets according to available quota and priority.
+
+## Before you begin
+
+1. Check [administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial cluster setup.
+
+2. Install Kubeflow Trainer v2. Check [the Trainer installation guide](https://www.kubeflow.org/docs/components/trainer/getting-started/).
+
+   **Note**: The minimum required Trainer version is v2.0.0.
+
+3. Enable TrainJob integration in Kueue. You can [modify kueue configurations from installed releases](/docs/installation#install-a-custom-configured-released-version) to include TrainJobs as an allowed workload.
+
+## TrainJob definition
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the TrainJob configuration:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Suspend field
+
+By default, Kueue will set `suspend` to true via webhook and unsuspend it when the TrainJob is admitted:
+
+```yaml
+spec:
+  suspend: true
+```
+
+## Using ClusterTrainingRuntime
+
+ClusterTrainingRuntimes are cluster-scoped resources that define training configurations accessible across all namespaces. They are typically created by platform administrators.
+
+### Example: PyTorch Distributed Training with ClusterTrainingRuntime
+
+First, create a ClusterTrainingRuntime for PyTorch distributed training:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: ClusterTrainingRuntime
+metadata:
+  name: torch-distributed
+  labels:
+    trainer.kubeflow.org/framework: torch
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+```
+
+Now, create a TrainJob that references this ClusterTrainingRuntime and will be scheduled by Kueue:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-distributed
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    command:
+      - torchrun
+      - /workspace/examples/mnist/mnist.py
```

  I prefer that if we post examples then they are functional. 
  
  - "Reference Kubeflow examples " - this we need for sure to refer , however they examples there will not contain the "queue-name" label, so having a copy-paste and ready example following  "Use official PyTorch image" is great
  
  EDIT: in other words "we refer to Kubeflow examples", but having one or two examples, working as copy-paste and including queue-name label inside Kueue is also great

### Review by [@NarayanaSabari](https://github.com/NarayanaSabari) (commented) — 2025-11-06T12:53:43Z

- **site/content/en/docs/tasks/run/trainjob/trainjobs.md:107** by [@NarayanaSabari](https://github.com/NarayanaSabari) — 2025-11-06T12:53:43Z
```diff
@@ -0,0 +1,258 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Kubeflow Trainer](https://www.kubeflow.org/docs/components/trainer/) TrainJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Overview
+
+Kubeflow Trainer v2 introduces the `TrainJob` API that works seamlessly with Kueue for batch scheduling and resource management. TrainJobs can be configured to use either:
+
+- **ClusterTrainingRuntime**: Cluster-scoped training runtimes that can be used across all namespaces
+- **TrainingRuntime**: Namespace-scoped training runtimes that are only available within a specific namespace
+
+Kueue manages TrainJobs by scheduling their underlying JobSets according to available quota and priority.
+
+## Before you begin
+
+1. Check [administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial cluster setup.
+
+2. Install Kubeflow Trainer v2. Check [the Trainer installation guide](https://www.kubeflow.org/docs/components/trainer/getting-started/).
+
+   **Note**: The minimum required Trainer version is v2.0.0.
+
+3. Enable TrainJob integration in Kueue. You can [modify kueue configurations from installed releases](/docs/installation#install-a-custom-configured-released-version) to include TrainJobs as an allowed workload.
+
+## TrainJob definition
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the TrainJob configuration:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Suspend field
+
+By default, Kueue will set `suspend` to true via webhook and unsuspend it when the TrainJob is admitted:
+
+```yaml
+spec:
+  suspend: true
+```
+
+## Using ClusterTrainingRuntime
+
+ClusterTrainingRuntimes are cluster-scoped resources that define training configurations accessible across all namespaces. They are typically created by platform administrators.
+
+### Example: PyTorch Distributed Training with ClusterTrainingRuntime
+
+First, create a ClusterTrainingRuntime for PyTorch distributed training:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: ClusterTrainingRuntime
+metadata:
+  name: torch-distributed
+  labels:
+    trainer.kubeflow.org/framework: torch
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+```
+
+Now, create a TrainJob that references this ClusterTrainingRuntime and will be scheduled by Kueue:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-distributed
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    command:
+      - torchrun
+      - /workspace/examples/mnist/mnist.py
```

  @mimowo Thanks for the feedback! I've updated the example to use the official PyTorch image with a working copy-paste example.
  
  ## Changes Made
  
  1. **Replaced broken image** with `pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime` (official, maintained)
  2. **Added inline training script** that demonstrates PyTorch distributed training with DDP
  3. **Added info alert** referencing Kubeflow Trainer examples for advanced use cases
  
  The example now:
  - ✅ Works as copy-paste (includes the `kueue.x-k8s.io/queue-name` label)
  - ✅ Uses maintained official image
  - ✅ Shows distributed training concepts (DDP, NCCL backend)
  - ✅ References Kubeflow examples for production patterns
  
  ## ⚠️ Review Needed
  
  I used AI to generate the training code. **Please cross-check the PyTorch distributed training implementation** to ensure:
  - Correct DDP initialization and usage
  - Proper distributed backend setup
  - Any best practices I might have missed
  
  Let me know if the code needs adjustments or if you'd prefer a different approach!

### Review by [@andreyvelich](https://github.com/andreyvelich) (commented) — 2025-11-06T13:50:16Z

- **site/content/en/docs/tasks/run/trainjobs.md:1** by [@andreyvelich](https://github.com/andreyvelich) — 2025-11-06T13:50:16Z
```diff
@@ -0,0 +1,258 @@
+---
```

  @NarayanaSabari We don't really need to have subsection Kubeflow TrainJob (v2), since it only has one page.
  Can we create similar page as for RayJobs without nested structure: https://github.com/kubernetes-sigs/kueue/blob/main/site/content/en/docs/tasks/run/rayjobs.md ?
  
  Also, please can you update this page: https://kueue.sigs.k8s.io/docs/tasks/#batch-user ?

### Review by [@andreyvelich](https://github.com/andreyvelich) (commented) — 2025-11-06T13:57:48Z

- **site/content/en/docs/tasks/run/trainjob/trainjobs.md:107** by [@andreyvelich](https://github.com/andreyvelich) — 2025-11-06T13:57:48Z
```diff
@@ -0,0 +1,258 @@
+---
+title: "Run a TrainJob"
+date: 2024-11-05
+weight: 7
+description: >
+  Run a Kueue scheduled TrainJob from Kubeflow Trainer v2
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Kubeflow Trainer](https://www.kubeflow.org/docs/components/trainer/) TrainJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Overview
+
+Kubeflow Trainer v2 introduces the `TrainJob` API that works seamlessly with Kueue for batch scheduling and resource management. TrainJobs can be configured to use either:
+
+- **ClusterTrainingRuntime**: Cluster-scoped training runtimes that can be used across all namespaces
+- **TrainingRuntime**: Namespace-scoped training runtimes that are only available within a specific namespace
+
+Kueue manages TrainJobs by scheduling their underlying JobSets according to available quota and priority.
+
+## Before you begin
+
+1. Check [administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial cluster setup.
+
+2. Install Kubeflow Trainer v2. Check [the Trainer installation guide](https://www.kubeflow.org/docs/components/trainer/getting-started/).
+
+   **Note**: The minimum required Trainer version is v2.0.0.
+
+3. Enable TrainJob integration in Kueue. You can [modify kueue configurations from installed releases](/docs/installation#install-a-custom-configured-released-version) to include TrainJobs as an allowed workload.
+
+## TrainJob definition
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the TrainJob configuration:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Suspend field
+
+By default, Kueue will set `suspend` to true via webhook and unsuspend it when the TrainJob is admitted:
+
+```yaml
+spec:
+  suspend: true
+```
+
+## Using ClusterTrainingRuntime
+
+ClusterTrainingRuntimes are cluster-scoped resources that define training configurations accessible across all namespaces. They are typically created by platform administrators.
+
+### Example: PyTorch Distributed Training with ClusterTrainingRuntime
+
+First, create a ClusterTrainingRuntime for PyTorch distributed training:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: ClusterTrainingRuntime
+metadata:
+  name: torch-distributed
+  labels:
+    trainer.kubeflow.org/framework: torch
+spec:
+  mlPolicy:
+    numNodes: 1
+    torch:
+      numProcPerNode: auto
+  template:
+    spec:
+      replicatedJobs:
+        - name: node
+          template:
+            metadata:
+              labels:
+                trainer.kubeflow.org/trainjob-ancestor-step: trainer
+            spec:
+              template:
+                spec:
+                  containers:
+                    - name: trainer
+                      image: pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime
+```
+
+Now, create a TrainJob that references this ClusterTrainingRuntime and will be scheduled by Kueue:
+
+```yaml
+apiVersion: trainer.kubeflow.org/v1alpha1
+kind: TrainJob
+metadata:
+  name: pytorch-distributed
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  runtimeRef:
+    name: torch-distributed
+    kind: ClusterTrainingRuntime
+  trainer:
+    image: docker.io/kubeflow/pytorch-dist-mnist-test:v1.0
+    command:
+      - torchrun
+      - /workspace/examples/mnist/mnist.py
```

  IMO, you can just use the default PyTorch image from the `torch-distributed` runtime in your examples: https://github.com/kubeflow/trainer/blob/master/manifests/base/runtimes/torch_distributed.yaml#L25
  Since this image is executable, you can create TrainJob as this:
  ```
  apiVersion: trainer.kubeflow.org/v1alpha1
  kind: TrainJob
  metadata:
    name: pytorch-distributed
    labels:
      kueue.x-k8s.io/queue-name: user-queue
  spec:
    runtimeRef:
      name: torch-distributed
      kind: ClusterTrainingRuntime
  ```
  
  In the future, we can add more examples which shows how to use Kubeflow SDK, TrainJob, and Kueue.
