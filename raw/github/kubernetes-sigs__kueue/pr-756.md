# PR #756: Dynamically reclaiming resources

**Summary**: Dynamically reclaiming resources

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/756

**Last updated**: 2023-05-15T05:42:48Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `dyn-reclaim` → `main`
- **Created**: 2023-05-09T10:17:34Z
- **Updated**: 2023-05-15T05:42:48Z
- **Merged**: 2023-05-12T18:33:02Z
- **Closed**: 2023-05-12T18:33:02Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 24   **+1003 / -102**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Add the support for dynamic resources reclaim.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #78 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Add the support for dynamic resources reclaim.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-05-09T10:17:43Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | cf301762b65bc0d63cac45db52f7d32600b038ab |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/645e7cf2febc69000831fabc |

### Comment by [@trasc](https://github.com/trasc) — 2023-05-09T13:50:00Z

/cc @alculquicondor

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-12T18:06:37Z

Totally LGTM.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-05-12T18:30:59Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/756#pullrequestreview-1424981700" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/756#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-09T16:07:40Z

- **apis/kueue/v1beta1/workload_types.go:78** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-09T15:37:35Z
```diff
@@ -77,6 +77,10 @@ type PodSetAssignment struct {
 	// Beside what is provided in podSet's specs, this calculation takes into account
 	// the LimitRange defaults and RuntimeClass overheads at the moment of admission.
```

  still at the moment of admission? or is it the live usage now?

- **apis/kueue/webhooks/workload_webhook.go:252** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-09T15:41:13Z
```diff
@@ -200,3 +231,39 @@ func validateAdmissionUpdate(new, old *kueue.Admission, path *field.Path) field.
 	}
 	return apivalidation.ValidateImmutableField(new, old, path)
 }
+
+// validateReclaimablePodsUpdate validates that the reclaimable counts do not decrease, this should be checked
+// while the workload is admitted
+func validateReclaimablePodsUpdate(newObj, oldObj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	if workload.ReclaimablePodsAreEqual(newObj.Status.ReclaimablePods, oldObj.Status.ReclaimablePods) {
+		return nil
+	}
+
+	if len(oldObj.Status.ReclaimablePods) == 0 {
+		return nil
+	}
+
+	knowPodSets := make(map[string]*kueue.ReclaimablePod, len(oldObj.Status.ReclaimablePods))
+	for i := range oldObj.Status.ReclaimablePods {
+		name := oldObj.Status.ReclaimablePods[i].Name
+		knowPodSets[name] = &oldObj.Status.ReclaimablePods[i]
+	}
+
+	ret := field.ErrorList{}
```

  ```suggestion
  	var ret field.ErrorList
  ```

- **apis/kueue/v1beta1/workload_types.go:86** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-09T15:51:47Z
```diff
@@ -77,6 +77,10 @@ type PodSetAssignment struct {
 	// Beside what is provided in podSet's specs, this calculation takes into account
 	// the LimitRange defaults and RuntimeClass overheads at the moment of admission.
 	ResourceUsage corev1.ResourceList `json:"resourceUsage,omitempty"`
+
+	// count is the number of pods taken into account at admission time.
+	// +kubebuilder:validation:Minimum=1
+	Count int32 `json:"count"`
```

  What will happen if a user upgrades from 3.0 to 4.0? This would be zero. Would that affect quota calculations?
  
  Could updates fail, because the value is not set?

- **pkg/workload/workload.go:358** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-09T16:00:35Z
```diff
@@ -288,3 +341,36 @@ func GetQueueOrderTimestamp(w *kueue.Workload) *metav1.Time {
 func IsAdmitted(w *kueue.Workload) bool {
 	return apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadAdmitted)
 }
+
+// UpdateReclaimablePods updated the ReclaimablePods list for the workload wit SSA.
+func UpdateReclaimablePods(ctx context.Context, c client.Client, w *kueue.Workload, reclaimablePods []kueue.ReclaimablePod) error {
+	patch := BaseSSAWorkload(w)
+	patch.Status.ReclaimablePods = reclaimablePods
+	return c.Status().Patch(ctx, patch, client.Apply, client.FieldOwner(constants.ReclaimablePodsMgr))
+}
+
+// ReclaimablePodsAreEqual checks if two Reclaimable pods are semantically equal
+// having the same length and all keys have the same value.
+func ReclaimablePodsAreEqual(a, b []kueue.ReclaimablePod) bool {
+	if len(a) != len(b) {
+		return false
+	}
+	for ia := range a {
```

  any harm in sorting before comparing?

- **test/integration/controller/core/clusterqueue_controller_test.go:276** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-09T16:01:03Z
```diff
@@ -273,6 +273,182 @@ var _ = ginkgo.Describe("ClusterQueue controller", func() {
 			util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 0)
 			util.ExpectAdmittedActiveWorkloadsMetric(clusterQueue, 0)
 		})
+		ginkgo.It("Should update status when workloads have recalimable pods", func() {
```

  ```suggestion
  		ginkgo.It("Should update status when workloads have reclaimable pods", func() {
  ```

- **pkg/controller/jobframework/reconciler.go:152** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-09T16:03:21Z
```diff
@@ -149,6 +149,16 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
 
 	// 4. handle WaitForPodsReady only for a standalone job.
+	// 1.1 update reclaimable counts
```

  why is 1.1 after 4?

- **pkg/controller/jobframework/reconciler.go:149** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-09T16:04:14Z
```diff
@@ -149,6 +149,16 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
```

  if the job finishes, all pods are reclaimable. Should we update those values in the status?

- **test/integration/controller/job/job_controller_test.go:1005** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-09T16:06:22Z
```diff
@@ -975,4 +958,62 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 			})
 		})
 	})
+
+	ginkgo.It("Should allow reclaim of resources that are no longer needed", func() {
+		ginkgo.By("creating localQueue", func() {
+			prodLocalQ = testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodLocalQ)).Should(gomega.Succeed())
+		})
+
+		job1 := testingjob.MakeJob("job1", ns.Name).Queue(prodLocalQ.Name).
+			Request(corev1.ResourceCPU, "2").
+			Completions(5).
+			Parallelism(2).
+			Obj()
+		lookupKey1 := types.NamespacedName{Name: job1.Name, Namespace: job1.Namespace}
+
+		ginkgo.By("checking the first job starts", func() {
+			gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
+			createdJob1 := &batchv1.Job{}
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey1, createdJob1)).Should(gomega.Succeed())
+				return createdJob1.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
+			gomega.Expect(createdJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+			util.ExpectPendingWorkloadsMetric(prodClusterQ, 0, 0)
+			util.ExpectAdmittedActiveWorkloadsMetric(prodClusterQ, 1)
+		})
+
+		job2 := testingjob.MakeJob("job2", ns.Name).Queue(prodLocalQ.Name).Request(corev1.ResourceCPU, "3").Obj()
+		lookupKey2 := types.NamespacedName{Name: job2.Name, Namespace: job2.Namespace}
+
+		ginkgo.By("checking a second no-fit job does not start", func() {
+			gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
+			createdJob2 := &batchv1.Job{}
+			gomega.Consistently(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey2, createdJob2)).Should(gomega.Succeed())
+				return createdJob2.Spec.Suspend
+			}, util.ConsistentDuration, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			util.ExpectPendingWorkloadsMetric(prodClusterQ, 0, 1)
+			util.ExpectAdmittedActiveWorkloadsMetric(prodClusterQ, 1)
+		})
+
+		ginkgo.By("checking the second job starts when the first has less then to completions to go", func() {
+			createdJob1 := &batchv1.Job{}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey1, createdJob1)).Should(gomega.Succeed())
+			createdJob1.Status.Succeeded = 4
+			gomega.Expect(k8sClient.Status().Update(ctx, createdJob1)).Should(gomega.Succeed())
```

  check that the status of the Workload is updated

- **test/util/util.go:294** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-09T16:07:37Z
```diff
@@ -291,6 +291,10 @@ func SetAdmission(ctx context.Context, k8sClient client.Client, wl *kueue.Worklo
 	return workload.ApplyAdmissionStatus(ctx, k8sClient, wl, false)
 }
 
+func SetReclaimablePods(ctx context.Context, k8sClient client.Client, wl *kueue.Workload, reclaimablePods ...kueue.ReclaimablePod) error {
```

  just call workload.UpdateReclaimablePods where necessary.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-10T07:35:45Z

- **apis/kueue/v1beta1/workload_types.go:78** by [@trasc](https://github.com/trasc) — 2023-05-10T06:22:33Z
```diff
@@ -77,6 +77,10 @@ type PodSetAssignment struct {
 	// Beside what is provided in podSet's specs, this calculation takes into account
 	// the LimitRange defaults and RuntimeClass overheads at the moment of admission.
```

  still, the live usage is kept at `workload.Info` level.

- **apis/kueue/v1beta1/workload_types.go:86** by [@trasc](https://github.com/trasc) — 2023-05-10T06:24:50Z
```diff
@@ -77,6 +77,10 @@ type PodSetAssignment struct {
 	// Beside what is provided in podSet's specs, this calculation takes into account
 	// the LimitRange defaults and RuntimeClass overheads at the moment of admission.
 	ResourceUsage corev1.ResourceList `json:"resourceUsage,omitempty"`
+
+	// count is the number of pods taken into account at admission time.
+	// +kubebuilder:validation:Minimum=1
+	Count int32 `json:"count"`
```

  this is taken into account in `totalRequestsFromAdmission(wl *kueue.Workload)`

- **apis/kueue/webhooks/workload_webhook.go:252** by [@trasc](https://github.com/trasc) — 2023-05-10T06:25:28Z
```diff
@@ -200,3 +231,39 @@ func validateAdmissionUpdate(new, old *kueue.Admission, path *field.Path) field.
 	}
 	return apivalidation.ValidateImmutableField(new, old, path)
 }
+
+// validateReclaimablePodsUpdate validates that the reclaimable counts do not decrease, this should be checked
+// while the workload is admitted
+func validateReclaimablePodsUpdate(newObj, oldObj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	if workload.ReclaimablePodsAreEqual(newObj.Status.ReclaimablePods, oldObj.Status.ReclaimablePods) {
+		return nil
+	}
+
+	if len(oldObj.Status.ReclaimablePods) == 0 {
+		return nil
+	}
+
+	knowPodSets := make(map[string]*kueue.ReclaimablePod, len(oldObj.Status.ReclaimablePods))
+	for i := range oldObj.Status.ReclaimablePods {
+		name := oldObj.Status.ReclaimablePods[i].Name
+		knowPodSets[name] = &oldObj.Status.ReclaimablePods[i]
+	}
+
+	ret := field.ErrorList{}
```

  done

- **pkg/controller/jobframework/reconciler.go:149** by [@trasc](https://github.com/trasc) — 2023-05-10T06:31:01Z
```diff
@@ -149,6 +149,16 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
```

  .. from an execution point of view it is irrelevant ... for UX ... we can do a last update upon Finish

- **pkg/controller/jobframework/reconciler.go:152** by [@trasc](https://github.com/trasc) — 2023-05-10T06:32:35Z
```diff
@@ -149,6 +149,16 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
 
 	// 4. handle WaitForPodsReady only for a standalone job.
+	// 1.1 update reclaimable counts
```

  very good question :), done

- **pkg/workload/workload.go:358** by [@trasc](https://github.com/trasc) — 2023-05-10T06:53:59Z
```diff
@@ -288,3 +341,36 @@ func GetQueueOrderTimestamp(w *kueue.Workload) *metav1.Time {
 func IsAdmitted(w *kueue.Workload) bool {
 	return apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadAdmitted)
 }
+
+// UpdateReclaimablePods updated the ReclaimablePods list for the workload wit SSA.
+func UpdateReclaimablePods(ctx context.Context, c client.Client, w *kueue.Workload, reclaimablePods []kueue.ReclaimablePod) error {
+	patch := BaseSSAWorkload(w)
+	patch.Status.ReclaimablePods = reclaimablePods
+	return c.Status().Patch(ctx, patch, client.Apply, client.FieldOwner(constants.ReclaimablePodsMgr))
+}
+
+// ReclaimablePodsAreEqual checks if two Reclaimable pods are semantically equal
+// having the same length and all keys have the same value.
+func ReclaimablePodsAreEqual(a, b []kueue.ReclaimablePod) bool {
+	if len(a) != len(b) {
+		return false
+	}
+	for ia := range a {
```

  just the side effect on the provided parameters .. let's use a map to make this simpler.

- **test/integration/controller/core/clusterqueue_controller_test.go:276** by [@trasc](https://github.com/trasc) — 2023-05-10T06:55:51Z
```diff
@@ -273,6 +273,182 @@ var _ = ginkgo.Describe("ClusterQueue controller", func() {
 			util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 0)
 			util.ExpectAdmittedActiveWorkloadsMetric(clusterQueue, 0)
 		})
+		ginkgo.It("Should update status when workloads have recalimable pods", func() {
```

  done

- **test/integration/controller/job/job_controller_test.go:1005** by [@trasc](https://github.com/trasc) — 2023-05-10T07:17:38Z
```diff
@@ -975,4 +958,62 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 			})
 		})
 	})
+
+	ginkgo.It("Should allow reclaim of resources that are no longer needed", func() {
+		ginkgo.By("creating localQueue", func() {
+			prodLocalQ = testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodLocalQ)).Should(gomega.Succeed())
+		})
+
+		job1 := testingjob.MakeJob("job1", ns.Name).Queue(prodLocalQ.Name).
+			Request(corev1.ResourceCPU, "2").
+			Completions(5).
+			Parallelism(2).
+			Obj()
+		lookupKey1 := types.NamespacedName{Name: job1.Name, Namespace: job1.Namespace}
+
+		ginkgo.By("checking the first job starts", func() {
+			gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
+			createdJob1 := &batchv1.Job{}
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey1, createdJob1)).Should(gomega.Succeed())
+				return createdJob1.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
+			gomega.Expect(createdJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+			util.ExpectPendingWorkloadsMetric(prodClusterQ, 0, 0)
+			util.ExpectAdmittedActiveWorkloadsMetric(prodClusterQ, 1)
+		})
+
+		job2 := testingjob.MakeJob("job2", ns.Name).Queue(prodLocalQ.Name).Request(corev1.ResourceCPU, "3").Obj()
+		lookupKey2 := types.NamespacedName{Name: job2.Name, Namespace: job2.Namespace}
+
+		ginkgo.By("checking a second no-fit job does not start", func() {
+			gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
+			createdJob2 := &batchv1.Job{}
+			gomega.Consistently(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey2, createdJob2)).Should(gomega.Succeed())
+				return createdJob2.Spec.Suspend
+			}, util.ConsistentDuration, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			util.ExpectPendingWorkloadsMetric(prodClusterQ, 0, 1)
+			util.ExpectAdmittedActiveWorkloadsMetric(prodClusterQ, 1)
+		})
+
+		ginkgo.By("checking the second job starts when the first has less then to completions to go", func() {
+			createdJob1 := &batchv1.Job{}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey1, createdJob1)).Should(gomega.Succeed())
+			createdJob1.Status.Succeeded = 4
+			gomega.Expect(k8sClient.Status().Update(ctx, createdJob1)).Should(gomega.Succeed())
```

  done

- **test/util/util.go:294** by [@trasc](https://github.com/trasc) — 2023-05-10T07:35:40Z
```diff
@@ -291,6 +291,10 @@ func SetAdmission(ctx context.Context, k8sClient client.Client, wl *kueue.Worklo
 	return workload.ApplyAdmissionStatus(ctx, k8sClient, wl, false)
 }
 
+func SetReclaimablePods(ctx context.Context, k8sClient client.Client, wl *kueue.Workload, reclaimablePods ...kueue.ReclaimablePod) error {
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-10T14:38:52Z

- **apis/kueue/webhooks/workload_webhook.go:213** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-10T12:58:53Z
```diff
@@ -179,13 +181,42 @@ func validateAdmission(obj *kueue.Workload, path *field.Path) field.ErrorList {
 	return allErrs
 }
 
+func validateReclaimablePods(obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	if len(obj.Status.ReclaimablePods) == 0 {
+		return nil
+	}
+	knowPodSets := make(map[string]*kueue.PodSet, len(obj.Spec.PodSets))
+	knowPodSetNames := make([]string, len(obj.Spec.PodSets))
+	for i := range obj.Spec.PodSets {
+		name := obj.Spec.PodSets[i].Name
+		knowPodSets[name] = &obj.Spec.PodSets[i]
+		knowPodSetNames = append(knowPodSetNames, name)
+	}
+
+	var ret field.ErrorList
+	for i := range obj.Status.ReclaimablePods {
+		rps := &obj.Status.ReclaimablePods[i]
+		ps, found := knowPodSets[rps.Name]
+		rpsPath := basePath.Key(rps.Name)
+		if !found {
+			ret = append(ret, field.NotSupported(rpsPath.Child("name"), rps.Name, knowPodSetNames))
+		} else if rps.Count > ps.Count {
+			ret = append(ret, field.Invalid(rpsPath.Child("count"), rps.Count, fmt.Sprintf("should be less or equal to %d", ps.Count)))
+		} else if rps.Count < 0 {
+			ret = append(ret, field.Invalid(rpsPath.Child("count"), rps.Count, "should no be less than 0"))
+		}
```

  Should we add `// +kubebuilder:validation:Minimum=0` to `Status.ReclaimablePods[*].Count`, instead of this manual validation?

- **apis/kueue/webhooks/workload_webhook.go:236** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-10T13:08:09Z
```diff
@@ -200,3 +231,39 @@ func validateAdmissionUpdate(new, old *kueue.Admission, path *field.Path) field.
 	}
 	return apivalidation.ValidateImmutableField(new, old, path)
 }
+
+// validateReclaimablePodsUpdate validates that the reclaimable counts do not decrease, this should be checked
+// while the workload is admitted
```

  ```suggestion
  // while the workload is admitted.
  ```

- **apis/kueue/webhooks/workload_webhook.go:236** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-10T13:08:19Z
```diff
@@ -200,3 +231,39 @@ func validateAdmissionUpdate(new, old *kueue.Admission, path *field.Path) field.
 	}
 	return apivalidation.ValidateImmutableField(new, old, path)
 }
+
+// validateReclaimablePodsUpdate validates that the reclaimable counts do not decrease, this should be checked
+// while the workload is admitted
```

  ```suggestion
  // while the workload is admitted.
  ```

- **pkg/controller/jobframework/interface.go:53** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-10T13:20:40Z
```diff
@@ -50,6 +50,8 @@ type GenericJob interface {
 	PodsReady() bool
 	// GetGVK returns GVK (Group Version Kind) for the job.
 	GetGVK() schema.GroupVersionKind
+	// ReclaimablePods returns the list of reclaimable pods
```

  ```suggestion
  	// ReclaimablePods returns the list of reclaimable pods.
  ```

- **apis/kueue/webhooks/workload_webhook.go:266** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-10T13:44:45Z
```diff
@@ -200,3 +231,39 @@ func validateAdmissionUpdate(new, old *kueue.Admission, path *field.Path) field.
 	}
 	return apivalidation.ValidateImmutableField(new, old, path)
 }
+
+// validateReclaimablePodsUpdate validates that the reclaimable counts do not decrease, this should be checked
+// while the workload is admitted
+func validateReclaimablePodsUpdate(newObj, oldObj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	if workload.ReclaimablePodsAreEqual(newObj.Status.ReclaimablePods, oldObj.Status.ReclaimablePods) {
+		return nil
+	}
+
+	if len(oldObj.Status.ReclaimablePods) == 0 {
+		return nil
+	}
+
+	knowPodSets := make(map[string]*kueue.ReclaimablePod, len(oldObj.Status.ReclaimablePods))
+	for i := range oldObj.Status.ReclaimablePods {
+		name := oldObj.Status.ReclaimablePods[i].Name
+		knowPodSets[name] = &oldObj.Status.ReclaimablePods[i]
+	}
+
+	var ret field.ErrorList
+	newNames := sets.New[string]()
+	for i := range newObj.Status.ReclaimablePods {
+		newCount := &newObj.Status.ReclaimablePods[i]
+		newNames.Insert(newCount.Name)
+		oldCount, found := knowPodSets[newCount.Name]
+		if found && newCount.Count < oldCount.Count {
+			ret = append(ret, field.Invalid(basePath.Key(newCount.Name).Child("count"), newCount.Count, fmt.Sprintf("cannot be less then %d", oldCount.Count)))
+		}
```

  In the k8s v1.24 cluster without a `JobTrackingWithFinalizers` feature gate, we keep encountering this error if `.status.succeeded` decreases, right?
  
  So, mentioning that in https://kueue.sigs.k8s.io/docs/installation/#before-you-begin would be good.

- **pkg/controller/jobframework/reconciler.go:151** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-10T13:45:07Z
```diff
@@ -148,7 +148,17 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, err
 	}
 
-	// 4. handle WaitForPodsReady only for a standalone job.
+	// 4. update reclaimable counts
```

  ```suggestion
  	// 4. update reclaimable counts.
  ```

- **pkg/controller/jobs/job/job_controller.go:161** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-10T14:01:17Z
```diff
@@ -153,9 +157,26 @@ func (j *Job) GetGVK() schema.GroupVersionKind {
 	return gvk
 }
 
+func (j *Job) ReclaimablePods() []kueue.ReclaimablePod {
+	if j.Spec.Parallelism == nil || *j.Spec.Parallelism == 1 || j.Spec.Completions == nil || j.Status.Succeeded == 0 {
```

  ```suggestion
  	if *j.Spec.Parallelism == 1 || j.Status.Succeeded == 0 {
  ```
  
  IIUC, the job controller (k/k) sets 1 as a default if those fields are nil.

- **pkg/controller/jobs/job/job_controller.go:180** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-10T14:14:45Z
```diff
@@ -153,9 +157,26 @@ func (j *Job) GetGVK() schema.GroupVersionKind {
 	return gvk
 }
 
+func (j *Job) ReclaimablePods() []kueue.ReclaimablePod {
+	if j.Spec.Parallelism == nil || *j.Spec.Parallelism == 1 || j.Spec.Completions == nil || j.Status.Succeeded == 0 {
+		return nil
+	}
+
+	remaining := *j.Spec.Completions - j.Status.Succeeded
+	if remaining >= *j.Spec.Parallelism {
+		return nil
+	}
+
+	return []kueue.ReclaimablePod{{
+		Name:  podSetName,
+		Count: *j.Spec.Parallelism - remaining,
+	}}
+}
+
 func (j *Job) PodSets() []kueue.PodSet {
 	return []kueue.PodSet{
 		{
+			Name:     podSetName,
```

  ```suggestion
  ```
  
  We set Workload name in defaulting webhook.
  
  https://github.com/kubernetes-sigs/kueue/blob/f5bb4a263e92f92c5d483bcd4171778fac7b4510/apis/kueue/webhooks/workload_webhook.go#L57C56-L64

- **pkg/workload/workload.go:216** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-10T14:35:49Z
```diff
@@ -156,12 +203,18 @@ func ResourceQuantity(name corev1.ResourceName, v int64) resource.Quantity {
 	}
 }
 
-func (r Requests) scale(f int64) {
+func (r Requests) scaleUp(f int64) {
 	for name := range r {
 		r[name] *= f
 	}
 }
 
+func (r Requests) scaleDown(f int64) {
+	for name := range r {
+		r[name] /= f
+	}
+}
```

  Are there any potential issues? What happens if `r[name] /= f` isn't divisible?

- **pkg/workload/workload.go:345** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-10T14:37:08Z
```diff
@@ -288,3 +341,30 @@ func GetQueueOrderTimestamp(w *kueue.Workload) *metav1.Time {
 func IsAdmitted(w *kueue.Workload) bool {
 	return apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadAdmitted)
 }
+
+// UpdateReclaimablePods updated the ReclaimablePods list for the workload wit SSA.
```

  ```suggestion
  // UpdateReclaimablePods updates the ReclaimablePods list for the workload wit SSA.
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-10T17:38:14Z

- **apis/kueue/v1beta1/workload_types.go:78** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-10T17:38:13Z
```diff
@@ -77,6 +77,10 @@ type PodSetAssignment struct {
 	// Beside what is provided in podSet's specs, this calculation takes into account
 	// the LimitRange defaults and RuntimeClass overheads at the moment of admission.
```

  let's change the wording a little bit so it's clear that this field is not updated if some quota is reclaimed.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-10T17:40:35Z

- **apis/kueue/v1beta1/workload_types.go:86** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-10T17:40:35Z
```diff
@@ -77,6 +77,10 @@ type PodSetAssignment struct {
 	// Beside what is provided in podSet's specs, this calculation takes into account
 	// the LimitRange defaults and RuntimeClass overheads at the moment of admission.
 	ResourceUsage corev1.ResourceList `json:"resourceUsage,omitempty"`
+
+	// count is the number of pods taken into account at admission time.
+	// +kubebuilder:validation:Minimum=1
+	Count int32 `json:"count"`
```

  right it's calculated there.
  
  However, there is nothing trying to set this field again.
  So for example, if we try to add a condition to the Workload via SSA, and this field stays at zero, would that fail validation?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-10T17:41:23Z

- **pkg/controller/jobframework/reconciler.go:149** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-10T17:41:23Z
```diff
@@ -149,6 +149,16 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
```

  Right, I ask because of the UX. I wouldn't want to do it in a separate API call, but we can leave this as a follow up.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-10T17:42:32Z

- **apis/kueue/webhooks/workload_webhook.go:213** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-10T17:42:32Z
```diff
@@ -179,13 +181,42 @@ func validateAdmission(obj *kueue.Workload, path *field.Path) field.ErrorList {
 	return allErrs
 }
 
+func validateReclaimablePods(obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	if len(obj.Status.ReclaimablePods) == 0 {
+		return nil
+	}
+	knowPodSets := make(map[string]*kueue.PodSet, len(obj.Spec.PodSets))
+	knowPodSetNames := make([]string, len(obj.Spec.PodSets))
+	for i := range obj.Spec.PodSets {
+		name := obj.Spec.PodSets[i].Name
+		knowPodSets[name] = &obj.Spec.PodSets[i]
+		knowPodSetNames = append(knowPodSetNames, name)
+	}
+
+	var ret field.ErrorList
+	for i := range obj.Status.ReclaimablePods {
+		rps := &obj.Status.ReclaimablePods[i]
+		ps, found := knowPodSets[rps.Name]
+		rpsPath := basePath.Key(rps.Name)
+		if !found {
+			ret = append(ret, field.NotSupported(rpsPath.Child("name"), rps.Name, knowPodSetNames))
+		} else if rps.Count > ps.Count {
+			ret = append(ret, field.Invalid(rpsPath.Child("count"), rps.Count, fmt.Sprintf("should be less or equal to %d", ps.Count)))
+		} else if rps.Count < 0 {
+			ret = append(ret, field.Invalid(rpsPath.Child("count"), rps.Count, "should no be less than 0"))
+		}
```

  yes, prefer validation via API, rather than the webhook.
  
  In the future, we hope to replace all validations with cel #463

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-11T05:03:09Z

- **apis/kueue/v1beta1/workload_types.go:86** by [@trasc](https://github.com/trasc) — 2023-05-11T05:03:08Z
```diff
@@ -77,6 +77,10 @@ type PodSetAssignment struct {
 	// Beside what is provided in podSet's specs, this calculation takes into account
 	// the LimitRange defaults and RuntimeClass overheads at the moment of admission.
 	ResourceUsage corev1.ResourceList `json:"resourceUsage,omitempty"`
+
+	// count is the number of pods taken into account at admission time.
+	// +kubebuilder:validation:Minimum=1
+	Count int32 `json:"count"`
```

  yes ... I think it's safe to drop the minimum to 0, there is ,in theory, the possibility that all the pods in one of the pod sets to be completed, the workload evicted, and readmitted, in the second readmission the count for that pod set should be a real 0.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-11T05:08:09Z

- **apis/kueue/v1beta1/workload_types.go:78** by [@trasc](https://github.com/trasc) — 2023-05-11T05:08:09Z
```diff
@@ -77,6 +77,10 @@ type PodSetAssignment struct {
 	// Beside what is provided in podSet's specs, this calculation takes into account
 	// the LimitRange defaults and RuntimeClass overheads at the moment of admission.
```

  What is also keeping me thinking about this is if it's better to no keep the multiplied values here and record the needs o a single pod. Any thoughts on this?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-11T06:46:14Z

- **apis/kueue/webhooks/workload_webhook.go:213** by [@trasc](https://github.com/trasc) — 2023-05-11T05:54:09Z
```diff
@@ -179,13 +181,42 @@ func validateAdmission(obj *kueue.Workload, path *field.Path) field.ErrorList {
 	return allErrs
 }
 
+func validateReclaimablePods(obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	if len(obj.Status.ReclaimablePods) == 0 {
+		return nil
+	}
+	knowPodSets := make(map[string]*kueue.PodSet, len(obj.Spec.PodSets))
+	knowPodSetNames := make([]string, len(obj.Spec.PodSets))
+	for i := range obj.Spec.PodSets {
+		name := obj.Spec.PodSets[i].Name
+		knowPodSets[name] = &obj.Spec.PodSets[i]
+		knowPodSetNames = append(knowPodSetNames, name)
+	}
+
+	var ret field.ErrorList
+	for i := range obj.Status.ReclaimablePods {
+		rps := &obj.Status.ReclaimablePods[i]
+		ps, found := knowPodSets[rps.Name]
+		rpsPath := basePath.Key(rps.Name)
+		if !found {
+			ret = append(ret, field.NotSupported(rpsPath.Child("name"), rps.Name, knowPodSetNames))
+		} else if rps.Count > ps.Count {
+			ret = append(ret, field.Invalid(rpsPath.Child("count"), rps.Count, fmt.Sprintf("should be less or equal to %d", ps.Count)))
+		} else if rps.Count < 0 {
+			ret = append(ret, field.Invalid(rpsPath.Child("count"), rps.Count, "should no be less than 0"))
+		}
```

  done

- **apis/kueue/webhooks/workload_webhook.go:236** by [@trasc](https://github.com/trasc) — 2023-05-11T05:55:18Z
```diff
@@ -200,3 +231,39 @@ func validateAdmissionUpdate(new, old *kueue.Admission, path *field.Path) field.
 	}
 	return apivalidation.ValidateImmutableField(new, old, path)
 }
+
+// validateReclaimablePodsUpdate validates that the reclaimable counts do not decrease, this should be checked
+// while the workload is admitted
```

  done

- **apis/kueue/webhooks/workload_webhook.go:236** by [@trasc](https://github.com/trasc) — 2023-05-11T05:55:21Z
```diff
@@ -200,3 +231,39 @@ func validateAdmissionUpdate(new, old *kueue.Admission, path *field.Path) field.
 	}
 	return apivalidation.ValidateImmutableField(new, old, path)
 }
+
+// validateReclaimablePodsUpdate validates that the reclaimable counts do not decrease, this should be checked
+// while the workload is admitted
```

  done

- **apis/kueue/webhooks/workload_webhook.go:266** by [@trasc](https://github.com/trasc) — 2023-05-11T05:57:04Z
```diff
@@ -200,3 +231,39 @@ func validateAdmissionUpdate(new, old *kueue.Admission, path *field.Path) field.
 	}
 	return apivalidation.ValidateImmutableField(new, old, path)
 }
+
+// validateReclaimablePodsUpdate validates that the reclaimable counts do not decrease, this should be checked
+// while the workload is admitted
+func validateReclaimablePodsUpdate(newObj, oldObj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	if workload.ReclaimablePodsAreEqual(newObj.Status.ReclaimablePods, oldObj.Status.ReclaimablePods) {
+		return nil
+	}
+
+	if len(oldObj.Status.ReclaimablePods) == 0 {
+		return nil
+	}
+
+	knowPodSets := make(map[string]*kueue.ReclaimablePod, len(oldObj.Status.ReclaimablePods))
+	for i := range oldObj.Status.ReclaimablePods {
+		name := oldObj.Status.ReclaimablePods[i].Name
+		knowPodSets[name] = &oldObj.Status.ReclaimablePods[i]
+	}
+
+	var ret field.ErrorList
+	newNames := sets.New[string]()
+	for i := range newObj.Status.ReclaimablePods {
+		newCount := &newObj.Status.ReclaimablePods[i]
+		newNames.Insert(newCount.Name)
+		oldCount, found := knowPodSets[newCount.Name]
+		if found && newCount.Count < oldCount.Count {
+			ret = append(ret, field.Invalid(basePath.Key(newCount.Name).Child("count"), newCount.Count, fmt.Sprintf("cannot be less then %d", oldCount.Count)))
+		}
```

  This is only related to workload,  it's not specific to batch/job.

- **pkg/controller/jobframework/interface.go:53** by [@trasc](https://github.com/trasc) — 2023-05-11T05:57:53Z
```diff
@@ -50,6 +50,8 @@ type GenericJob interface {
 	PodsReady() bool
 	// GetGVK returns GVK (Group Version Kind) for the job.
 	GetGVK() schema.GroupVersionKind
+	// ReclaimablePods returns the list of reclaimable pods
```

  done

- **pkg/controller/jobframework/reconciler.go:151** by [@trasc](https://github.com/trasc) — 2023-05-11T05:59:18Z
```diff
@@ -148,7 +148,17 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, err
 	}
 
-	// 4. handle WaitForPodsReady only for a standalone job.
+	// 4. update reclaimable counts
```

  done

- **pkg/workload/workload.go:216** by [@trasc](https://github.com/trasc) — 2023-05-11T06:02:53Z
```diff
@@ -156,12 +203,18 @@ func ResourceQuantity(name corev1.ResourceName, v int64) resource.Quantity {
 	}
 }
 
-func (r Requests) scale(f int64) {
+func (r Requests) scaleUp(f int64) {
 	for name := range r {
 		r[name] *= f
 	}
 }
 
+func (r Requests) scaleDown(f int64) {
+	for name := range r {
+		r[name] /= f
+	}
+}
```

  The way it's used, the request sould always be a multiple f. 
  In theory if it's not divisible it will just round down the result.

- **pkg/workload/workload.go:345** by [@trasc](https://github.com/trasc) — 2023-05-11T06:03:41Z
```diff
@@ -288,3 +341,30 @@ func GetQueueOrderTimestamp(w *kueue.Workload) *metav1.Time {
 func IsAdmitted(w *kueue.Workload) bool {
 	return apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadAdmitted)
 }
+
+// UpdateReclaimablePods updated the ReclaimablePods list for the workload wit SSA.
```

  done

- **pkg/controller/jobs/job/job_controller.go:180** by [@trasc](https://github.com/trasc) — 2023-05-11T06:13:54Z
```diff
@@ -153,9 +157,26 @@ func (j *Job) GetGVK() schema.GroupVersionKind {
 	return gvk
 }
 
+func (j *Job) ReclaimablePods() []kueue.ReclaimablePod {
+	if j.Spec.Parallelism == nil || *j.Spec.Parallelism == 1 || j.Spec.Completions == nil || j.Status.Succeeded == 0 {
+		return nil
+	}
+
+	remaining := *j.Spec.Completions - j.Status.Succeeded
+	if remaining >= *j.Spec.Parallelism {
+		return nil
+	}
+
+	return []kueue.ReclaimablePod{{
+		Name:  podSetName,
+		Count: *j.Spec.Parallelism - remaining,
+	}}
+}
+
 func (j *Job) PodSets() []kueue.PodSet {
 	return []kueue.PodSet{
 		{
+			Name:     podSetName,
```

  Yes, but before the name was not relevant for this controller. If we don't specifically set it here and for some reason in the future the default name is changed, it will break the reporting of reclaimable pods .

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-11T07:00:30Z

- **pkg/controller/jobs/job/job_controller.go:161** by [@trasc](https://github.com/trasc) — 2023-05-11T07:00:30Z
```diff
@@ -153,9 +157,26 @@ func (j *Job) GetGVK() schema.GroupVersionKind {
 	return gvk
 }
 
+func (j *Job) ReclaimablePods() []kueue.ReclaimablePod {
+	if j.Spec.Parallelism == nil || *j.Spec.Parallelism == 1 || j.Spec.Completions == nil || j.Status.Succeeded == 0 {
```

  I'm not conformable to rely on that, but let's try to clean this a bit.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-11T08:57:26Z

- **apis/kueue/webhooks/workload_webhook.go:266** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-11T08:57:26Z
```diff
@@ -200,3 +231,39 @@ func validateAdmissionUpdate(new, old *kueue.Admission, path *field.Path) field.
 	}
 	return apivalidation.ValidateImmutableField(new, old, path)
 }
+
+// validateReclaimablePodsUpdate validates that the reclaimable counts do not decrease, this should be checked
+// while the workload is admitted
+func validateReclaimablePodsUpdate(newObj, oldObj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	if workload.ReclaimablePodsAreEqual(newObj.Status.ReclaimablePods, oldObj.Status.ReclaimablePods) {
+		return nil
+	}
+
+	if len(oldObj.Status.ReclaimablePods) == 0 {
+		return nil
+	}
+
+	knowPodSets := make(map[string]*kueue.ReclaimablePod, len(oldObj.Status.ReclaimablePods))
+	for i := range oldObj.Status.ReclaimablePods {
+		name := oldObj.Status.ReclaimablePods[i].Name
+		knowPodSets[name] = &oldObj.Status.ReclaimablePods[i]
+	}
+
+	var ret field.ErrorList
+	newNames := sets.New[string]()
+	for i := range newObj.Status.ReclaimablePods {
+		newCount := &newObj.Status.ReclaimablePods[i]
+		newNames.Insert(newCount.Name)
+		oldCount, found := knowPodSets[newCount.Name]
+		if found && newCount.Count < oldCount.Count {
+			ret = append(ret, field.Invalid(basePath.Key(newCount.Name).Child("count"), newCount.Count, fmt.Sprintf("cannot be less then %d", oldCount.Count)))
+		}
```

  If this validation fails, updating Workloads fails, right?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-11T08:57:45Z

- **pkg/controller/jobs/job/job_controller.go:161** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-11T08:57:44Z
```diff
@@ -153,9 +157,26 @@ func (j *Job) GetGVK() schema.GroupVersionKind {
 	return gvk
 }
 
+func (j *Job) ReclaimablePods() []kueue.ReclaimablePod {
+	if j.Spec.Parallelism == nil || *j.Spec.Parallelism == 1 || j.Spec.Completions == nil || j.Status.Succeeded == 0 {
```

  Sounds good.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-11T09:03:28Z

- **pkg/controller/jobs/job/job_controller.go:180** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-11T09:03:28Z
```diff
@@ -153,9 +157,26 @@ func (j *Job) GetGVK() schema.GroupVersionKind {
 	return gvk
 }
 
+func (j *Job) ReclaimablePods() []kueue.ReclaimablePod {
+	if j.Spec.Parallelism == nil || *j.Spec.Parallelism == 1 || j.Spec.Completions == nil || j.Status.Succeeded == 0 {
+		return nil
+	}
+
+	remaining := *j.Spec.Completions - j.Status.Succeeded
+	if remaining >= *j.Spec.Parallelism {
+		return nil
+	}
+
+	return []kueue.ReclaimablePod{{
+		Name:  podSetName,
+		Count: *j.Spec.Parallelism - remaining,
+	}}
+}
+
 func (j *Job) PodSets() []kueue.PodSet {
 	return []kueue.PodSet{
 		{
+			Name:     podSetName,
```

  SGTM

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-11T09:04:04Z

- **pkg/controller/jobs/job/job_controller.go:52** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-11T09:04:03Z
```diff
@@ -48,6 +48,10 @@ var (
 	FrameworkName = "batch/job"
 )
 
+const (
+	podSetName = "main"
```

  Using the following constant would be good: https://github.com/kubernetes-sigs/kueue/blob/f5bb4a263e92f92c5d483bcd4171778fac7b4510/apis/kueue/v1beta1/constants.go#L22

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-11T09:28:23Z

- **pkg/workload/workload.go:216** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-11T09:28:22Z
```diff
@@ -156,12 +203,18 @@ func ResourceQuantity(name corev1.ResourceName, v int64) resource.Quantity {
 	}
 }
 
-func (r Requests) scale(f int64) {
+func (r Requests) scaleUp(f int64) {
 	for name := range r {
 		r[name] *= f
 	}
 }
 
+func (r Requests) scaleDown(f int64) {
+	for name := range r {
+		r[name] /= f
+	}
+}
```

  Right. However, I think it is dangerous to assume that the receiver always has divisible values.
  
  So, I would suggest this function returns a bool indicating whether all results are correct.
  
  ```suggestion
  func (r Requests) scaleDown(f int64) bool {
  	incorrect := false
  	for name := range r {
  		res := r[name]
  		res /= f
  		incorrect = (!incorrect && res*f != r[name])
  		r[name] = res
  	}
  	return incorrect
  }
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-11T09:35:26Z

- **pkg/workload/workload.go:216** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-11T09:35:26Z
```diff
@@ -156,12 +203,18 @@ func ResourceQuantity(name corev1.ResourceName, v int64) resource.Quantity {
 	}
 }
 
-func (r Requests) scale(f int64) {
+func (r Requests) scaleUp(f int64) {
 	for name := range r {
 		r[name] *= f
 	}
 }
 
+func (r Requests) scaleDown(f int64) {
+	for name := range r {
+		r[name] /= f
+	}
+}
```

  This means I don't want to hide the fact that the receiver must have divisible values.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-11T10:55:49Z

- **apis/kueue/webhooks/workload_webhook.go:266** by [@trasc](https://github.com/trasc) — 2023-05-11T10:55:48Z
```diff
@@ -200,3 +231,39 @@ func validateAdmissionUpdate(new, old *kueue.Admission, path *field.Path) field.
 	}
 	return apivalidation.ValidateImmutableField(new, old, path)
 }
+
+// validateReclaimablePodsUpdate validates that the reclaimable counts do not decrease, this should be checked
+// while the workload is admitted
+func validateReclaimablePodsUpdate(newObj, oldObj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	if workload.ReclaimablePodsAreEqual(newObj.Status.ReclaimablePods, oldObj.Status.ReclaimablePods) {
+		return nil
+	}
+
+	if len(oldObj.Status.ReclaimablePods) == 0 {
+		return nil
+	}
+
+	knowPodSets := make(map[string]*kueue.ReclaimablePod, len(oldObj.Status.ReclaimablePods))
+	for i := range oldObj.Status.ReclaimablePods {
+		name := oldObj.Status.ReclaimablePods[i].Name
+		knowPodSets[name] = &oldObj.Status.ReclaimablePods[i]
+	}
+
+	var ret field.ErrorList
+	newNames := sets.New[string]()
+	for i := range newObj.Status.ReclaimablePods {
+		newCount := &newObj.Status.ReclaimablePods[i]
+		newNames.Insert(newCount.Name)
+		oldCount, found := knowPodSets[newCount.Name]
+		if found && newCount.Count < oldCount.Count {
+			ret = append(ret, field.Invalid(basePath.Key(newCount.Name).Child("count"), newCount.Count, fmt.Sprintf("cannot be less then %d", oldCount.Count)))
+		}
```

  right

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-11T11:03:05Z

- **apis/kueue/webhooks/workload_webhook.go:266** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-11T11:03:05Z
```diff
@@ -200,3 +231,39 @@ func validateAdmissionUpdate(new, old *kueue.Admission, path *field.Path) field.
 	}
 	return apivalidation.ValidateImmutableField(new, old, path)
 }
+
+// validateReclaimablePodsUpdate validates that the reclaimable counts do not decrease, this should be checked
+// while the workload is admitted
+func validateReclaimablePodsUpdate(newObj, oldObj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	if workload.ReclaimablePodsAreEqual(newObj.Status.ReclaimablePods, oldObj.Status.ReclaimablePods) {
+		return nil
+	}
+
+	if len(oldObj.Status.ReclaimablePods) == 0 {
+		return nil
+	}
+
+	knowPodSets := make(map[string]*kueue.ReclaimablePod, len(oldObj.Status.ReclaimablePods))
+	for i := range oldObj.Status.ReclaimablePods {
+		name := oldObj.Status.ReclaimablePods[i].Name
+		knowPodSets[name] = &oldObj.Status.ReclaimablePods[i]
+	}
+
+	var ret field.ErrorList
+	newNames := sets.New[string]()
+	for i := range newObj.Status.ReclaimablePods {
+		newCount := &newObj.Status.ReclaimablePods[i]
+		newNames.Insert(newCount.Name)
+		oldCount, found := knowPodSets[newCount.Name]
+		if found && newCount.Count < oldCount.Count {
+			ret = append(ret, field.Invalid(basePath.Key(newCount.Name).Child("count"), newCount.Count, fmt.Sprintf("cannot be less then %d", oldCount.Count)))
+		}
```

  I'd like to say there are cases of failing to update Workloads in k8s v1.24, although maybe I am overly concerned with details.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-11T11:06:24Z

- **pkg/workload/workload.go:216** by [@trasc](https://github.com/trasc) — 2023-05-11T11:06:24Z
```diff
@@ -156,12 +203,18 @@ func ResourceQuantity(name corev1.ResourceName, v int64) resource.Quantity {
 	}
 }
 
-func (r Requests) scale(f int64) {
+func (r Requests) scaleUp(f int64) {
 	for name := range r {
 		r[name] *= f
 	}
 }
 
+func (r Requests) scaleDown(f int64) {
+	for name := range r {
+		r[name] /= f
+	}
+}
```

  The values in admissions are computed by multiplying the values for one pod with the number of pods ,  we only use this to divide to the same count. Sure we can  detect this and return it but what should we do about it after that?
  
  ```suggestion
  func (r Requests) scaleDown(f int64) bool {
          incorrect := false
  	for name := range r {
  		r[name] /= f
  		incorrect |= (r[name] % f) > 0
  	}
  	return incorrect
  }
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-11T11:37:50Z

- **pkg/workload/workload.go:216** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-11T11:37:50Z
```diff
@@ -156,12 +203,18 @@ func ResourceQuantity(name corev1.ResourceName, v int64) resource.Quantity {
 	}
 }
 
-func (r Requests) scale(f int64) {
+func (r Requests) scaleUp(f int64) {
 	for name := range r {
 		r[name] *= f
 	}
 }
 
+func (r Requests) scaleDown(f int64) {
+	for name := range r {
+		r[name] /= f
+	}
+}
```

  My concern is the use case other than L151, although those cases don't exist in this PR. So, in this case (L151), we don't need to check whether the result is incorrect.
  
  This means for L151:
  
  ```go
  // ignore check
  _ = setRes.Requests.scaleDown(int64(setRes.Count))
  ```
  
  My concern is for the future if this function is used other than L151.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-11T11:40:31Z

- **pkg/workload/workload.go:216** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-11T11:40:31Z
```diff
@@ -156,12 +203,18 @@ func ResourceQuantity(name corev1.ResourceName, v int64) resource.Quantity {
 	}
 }
 
-func (r Requests) scale(f int64) {
+func (r Requests) scaleUp(f int64) {
 	for name := range r {
 		r[name] *= f
 	}
 }
 
+func (r Requests) scaleDown(f int64) {
+	for name := range r {
+		r[name] /= f
+	}
+}
```

  My suggestion might be too much. So let me know what others think. @alculquicondor

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-11T11:43:13Z

- **pkg/controller/jobs/job/job_controller.go:52** by [@trasc](https://github.com/trasc) — 2023-05-11T11:43:13Z
```diff
@@ -48,6 +48,10 @@ var (
 	FrameworkName = "batch/job"
 )
 
+const (
+	podSetName = "main"
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-11T11:49:47Z

- **pkg/workload/workload.go:216** by [@trasc](https://github.com/trasc) — 2023-05-11T11:49:47Z
```diff
@@ -156,12 +203,18 @@ func ResourceQuantity(name corev1.ResourceName, v int64) resource.Quantity {
 	}
 }
 
-func (r Requests) scale(f int64) {
+func (r Requests) scaleUp(f int64) {
 	for name := range r {
 		r[name] *= f
 	}
 }
 
+func (r Requests) scaleDown(f int64) {
+	for name := range r {
+		r[name] /= f
+	}
+}
```

  Depending on how we go with https://github.com/kubernetes-sigs/kueue/pull/756#discussion_r1190640633, the scale down might only be needed for workloads that ware admitted with v0.3 and updated with v0.4.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-11T15:39:16Z

- **apis/kueue/v1beta1/workload_types.go:78** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-11T15:39:15Z
```diff
@@ -77,6 +77,10 @@ type PodSetAssignment struct {
 	// Beside what is provided in podSet's specs, this calculation takes into account
 	// the LimitRange defaults and RuntimeClass overheads at the moment of admission.
```

  That could be useful, but the API is already beta, so we can't change it.
  We can drop it in a future v1beta2 though

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-11T15:40:30Z

- **apis/kueue/webhooks/workload_webhook.go:266** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-11T15:40:29Z
```diff
@@ -200,3 +231,39 @@ func validateAdmissionUpdate(new, old *kueue.Admission, path *field.Path) field.
 	}
 	return apivalidation.ValidateImmutableField(new, old, path)
 }
+
+// validateReclaimablePodsUpdate validates that the reclaimable counts do not decrease, this should be checked
+// while the workload is admitted
+func validateReclaimablePodsUpdate(newObj, oldObj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	if workload.ReclaimablePodsAreEqual(newObj.Status.ReclaimablePods, oldObj.Status.ReclaimablePods) {
+		return nil
+	}
+
+	if len(oldObj.Status.ReclaimablePods) == 0 {
+		return nil
+	}
+
+	knowPodSets := make(map[string]*kueue.ReclaimablePod, len(oldObj.Status.ReclaimablePods))
+	for i := range oldObj.Status.ReclaimablePods {
+		name := oldObj.Status.ReclaimablePods[i].Name
+		knowPodSets[name] = &oldObj.Status.ReclaimablePods[i]
+	}
+
+	var ret field.ErrorList
+	newNames := sets.New[string]()
+	for i := range newObj.Status.ReclaimablePods {
+		newCount := &newObj.Status.ReclaimablePods[i]
+		newNames.Insert(newCount.Name)
+		oldCount, found := knowPodSets[newCount.Name]
+		if found && newCount.Count < oldCount.Count {
+			ret = append(ret, field.Invalid(basePath.Key(newCount.Name).Child("count"), newCount.Count, fmt.Sprintf("cannot be less then %d", oldCount.Count)))
+		}
```

  That's a bug in job controller and 1.24 is almost reaching EoL, so I wouldn't be concerned about it https://kubernetes.io/releases/#release-v1-24

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-11T15:41:33Z

- **apis/kueue/webhooks/workload_webhook.go:266** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-11T15:41:33Z
```diff
@@ -200,3 +231,39 @@ func validateAdmissionUpdate(new, old *kueue.Admission, path *field.Path) field.
 	}
 	return apivalidation.ValidateImmutableField(new, old, path)
 }
+
+// validateReclaimablePodsUpdate validates that the reclaimable counts do not decrease, this should be checked
+// while the workload is admitted
+func validateReclaimablePodsUpdate(newObj, oldObj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	if workload.ReclaimablePodsAreEqual(newObj.Status.ReclaimablePods, oldObj.Status.ReclaimablePods) {
+		return nil
+	}
+
+	if len(oldObj.Status.ReclaimablePods) == 0 {
+		return nil
+	}
+
+	knowPodSets := make(map[string]*kueue.ReclaimablePod, len(oldObj.Status.ReclaimablePods))
+	for i := range oldObj.Status.ReclaimablePods {
+		name := oldObj.Status.ReclaimablePods[i].Name
+		knowPodSets[name] = &oldObj.Status.ReclaimablePods[i]
+	}
+
+	var ret field.ErrorList
+	newNames := sets.New[string]()
+	for i := range newObj.Status.ReclaimablePods {
+		newCount := &newObj.Status.ReclaimablePods[i]
+		newNames.Insert(newCount.Name)
+		oldCount, found := knowPodSets[newCount.Name]
+		if found && newCount.Count < oldCount.Count {
+			ret = append(ret, field.Invalid(basePath.Key(newCount.Name).Child("count"), newCount.Count, fmt.Sprintf("cannot be less then %d", oldCount.Count)))
+		}
```

  Although, in the implementation, we could take `max(oldvalue, newvalue)`, just to be on the safe side.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-11T17:12:33Z

- **apis/kueue/webhooks/workload_webhook.go:266** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-11T17:12:33Z
```diff
@@ -200,3 +231,39 @@ func validateAdmissionUpdate(new, old *kueue.Admission, path *field.Path) field.
 	}
 	return apivalidation.ValidateImmutableField(new, old, path)
 }
+
+// validateReclaimablePodsUpdate validates that the reclaimable counts do not decrease, this should be checked
+// while the workload is admitted
+func validateReclaimablePodsUpdate(newObj, oldObj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	if workload.ReclaimablePodsAreEqual(newObj.Status.ReclaimablePods, oldObj.Status.ReclaimablePods) {
+		return nil
+	}
+
+	if len(oldObj.Status.ReclaimablePods) == 0 {
+		return nil
+	}
+
+	knowPodSets := make(map[string]*kueue.ReclaimablePod, len(oldObj.Status.ReclaimablePods))
+	for i := range oldObj.Status.ReclaimablePods {
+		name := oldObj.Status.ReclaimablePods[i].Name
+		knowPodSets[name] = &oldObj.Status.ReclaimablePods[i]
+	}
+
+	var ret field.ErrorList
+	newNames := sets.New[string]()
+	for i := range newObj.Status.ReclaimablePods {
+		newCount := &newObj.Status.ReclaimablePods[i]
+		newNames.Insert(newCount.Name)
+		oldCount, found := knowPodSets[newCount.Name]
+		if found && newCount.Count < oldCount.Count {
+			ret = append(ret, field.Invalid(basePath.Key(newCount.Name).Child("count"), newCount.Count, fmt.Sprintf("cannot be less then %d", oldCount.Count)))
+		}
```

  > Although, in the implementation, we could take max(oldvalue, newvalue), just to be on the safe side.
  
  I prefer Aldo's suggestion.
  @trasc wdyt

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-11T18:09:52Z

- **apis/kueue/v1beta1/workload_types.go:82** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-11T17:45:28Z
```diff
@@ -76,7 +76,12 @@ type PodSetAssignment struct {
 	//
 	// Beside what is provided in podSet's specs, this calculation takes into account
 	// the LimitRange defaults and RuntimeClass overheads at the moment of admission.
+	// This field will not change in case of quota reclaim.
 	ResourceUsage corev1.ResourceList `json:"resourceUsage,omitempty"`
+
+	// count is the number of pods taken into account at admission time.
```

  Add: Value could be zero for Workloads created before this field was added.

- **apis/kueue/webhooks/workload_webhook.go:266** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-11T17:46:52Z
```diff
@@ -200,3 +231,39 @@ func validateAdmissionUpdate(new, old *kueue.Admission, path *field.Path) field.
 	}
 	return apivalidation.ValidateImmutableField(new, old, path)
 }
+
+// validateReclaimablePodsUpdate validates that the reclaimable counts do not decrease, this should be checked
+// while the workload is admitted
+func validateReclaimablePodsUpdate(newObj, oldObj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	if workload.ReclaimablePodsAreEqual(newObj.Status.ReclaimablePods, oldObj.Status.ReclaimablePods) {
+		return nil
+	}
+
+	if len(oldObj.Status.ReclaimablePods) == 0 {
+		return nil
+	}
+
+	knowPodSets := make(map[string]*kueue.ReclaimablePod, len(oldObj.Status.ReclaimablePods))
+	for i := range oldObj.Status.ReclaimablePods {
+		name := oldObj.Status.ReclaimablePods[i].Name
+		knowPodSets[name] = &oldObj.Status.ReclaimablePods[i]
+	}
+
+	var ret field.ErrorList
+	newNames := sets.New[string]()
+	for i := range newObj.Status.ReclaimablePods {
+		newCount := &newObj.Status.ReclaimablePods[i]
+		newNames.Insert(newCount.Name)
+		oldCount, found := knowPodSets[newCount.Name]
+		if found && newCount.Count < oldCount.Count {
+			ret = append(ret, field.Invalid(basePath.Key(newCount.Name).Child("count"), newCount.Count, fmt.Sprintf("cannot be less then %d", oldCount.Count)))
+		}
```

  To be clear: the Job implementation could change, but we can leave this validation as-is.

- **pkg/controller/jobframework/reconciler.go:153** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-11T17:59:24Z
```diff
@@ -148,7 +148,17 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, err
 	}
 
-	// 4. handle WaitForPodsReady only for a standalone job.
+	// 4. update reclaimable counts.
+	if rp := job.ReclaimablePods(); !workload.ReclaimablePodsAreEqual(rp, wl.Status.ReclaimablePods) {
+		err = workload.UpdateReclaimablePods(ctx, r.client, wl, rp)
```

  what if we update the podset usage in this function?
  
  For a follow up, but before the 0.4 release.

- **pkg/workload/workload.go:216** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-11T18:07:31Z
```diff
@@ -156,12 +203,18 @@ func ResourceQuantity(name corev1.ResourceName, v int64) resource.Quantity {
 	}
 }
 
-func (r Requests) scale(f int64) {
+func (r Requests) scaleUp(f int64) {
 	for name := range r {
 		r[name] *= f
 	}
 }
 
+func (r Requests) scaleDown(f int64) {
+	for name := range r {
+		r[name] /= f
+	}
+}
```

  Yeah, I get the concern... but Workload is an object fully controlled by Kueue. And the Quantity object is not a floating point. It's a decimal implementation. So this is quite safe.
  
  Maybe we can add in the Workload validation that the quantities are multiple of count?

- **apis/kueue/v1beta1/workload_types.go:140** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-11T18:09:49Z
```diff
@@ -124,6 +129,22 @@ type WorkloadStatus struct {
 	// +listType=map
 	// +listMapKey=type
 	Conditions []metav1.Condition `json:"conditions,omitempty"`
+
+	// reclaimablePods keeps track of the number pods within a podset for which
+	// the resource reservation is no longer needed.
+	// +optional
+	// +listType=map
+	// +listMapKey=name
+	ReclaimablePods []ReclaimablePod `json:"reclaimablePods,omitempty"`
```

  I'm having second thoughts here.
  
  If we assume that the admission section can be modified after admission (to update Count and usage), then I prefer we have `reclaimableCount` inside the admission struct.
  
  I think this might be a better idea as we move towards elastic jobs. @tenzen-y ?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-11T18:25:52Z

- **pkg/workload/workload.go:216** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-11T18:25:51Z
```diff
@@ -156,12 +203,18 @@ func ResourceQuantity(name corev1.ResourceName, v int64) resource.Quantity {
 	}
 }
 
-func (r Requests) scale(f int64) {
+func (r Requests) scaleUp(f int64) {
 	for name := range r {
 		r[name] *= f
 	}
 }
 
+func (r Requests) scaleDown(f int64) {
+	for name := range r {
+		r[name] /= f
+	}
+}
```

  > but Workload is an object fully controlled by Kueue. And the Quantity object is not a floating point. It's a decimal implementation. So this is quite safe.
  
  Ah, I see. Thank you for clarifying :)
  
  > Maybe we can add in the Workload validation that the quantities are multiple of count?
  
  That much more makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-11T18:49:24Z

- **apis/kueue/v1beta1/workload_types.go:140** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-11T18:49:23Z
```diff
@@ -124,6 +129,22 @@ type WorkloadStatus struct {
 	// +listType=map
 	// +listMapKey=type
 	Conditions []metav1.Condition `json:"conditions,omitempty"`
+
+	// reclaimablePods keeps track of the number pods within a podset for which
+	// the resource reservation is no longer needed.
+	// +optional
+	// +listType=map
+	// +listMapKey=name
+	ReclaimablePods []ReclaimablePod `json:"reclaimablePods,omitempty"`
```

  > then I prefer we have reclaimableCount inside the admission struct.
  
  Does this mean we have `reclaimableCount` inside the admission instead of having this `ReclaimablePods` (removing `ReclaimablePods `)?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-11T19:05:07Z

- **apis/kueue/v1beta1/workload_types.go:140** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-11T19:05:07Z
```diff
@@ -124,6 +129,22 @@ type WorkloadStatus struct {
 	// +listType=map
 	// +listMapKey=type
 	Conditions []metav1.Condition `json:"conditions,omitempty"`
+
+	// reclaimablePods keeps track of the number pods within a podset for which
+	// the resource reservation is no longer needed.
+	// +optional
+	// +listType=map
+	// +listMapKey=name
+	ReclaimablePods []ReclaimablePod `json:"reclaimablePods,omitempty"`
```

  yes, that's what I'm thinking.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-11T19:08:16Z

- **apis/kueue/v1beta1/workload_types.go:140** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-11T19:08:15Z
```diff
@@ -124,6 +129,22 @@ type WorkloadStatus struct {
 	// +listType=map
 	// +listMapKey=type
 	Conditions []metav1.Condition `json:"conditions,omitempty"`
+
+	// reclaimablePods keeps track of the number pods within a podset for which
+	// the resource reservation is no longer needed.
+	// +optional
+	// +listType=map
+	// +listMapKey=name
+	ReclaimablePods []ReclaimablePod `json:"reclaimablePods,omitempty"`
```

  IIRC, we selected `ReclaimablePods` instead of `reclaimableCount` since it could be misleading when the job is suspended.
  
  https://github.com/kubernetes-sigs/kueue/pull/742#discussion_r1187191726
  
  However, as @alculquicondor says, we can ignore that concern if we have `reclaimableCount` inside the `Admission`.
  
  > I think this might be a better idea as we move towards elastic jobs.
  
  right. The `Admission` has the `reclaimableCount` would be more natural for the elastic job.
  
  @trasc Are you concerned about having `reclaimableCount` inside the `Admission`?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-12T08:10:11Z

- **apis/kueue/v1beta1/workload_types.go:78** by [@trasc](https://github.com/trasc) — 2023-05-12T05:23:55Z
```diff
@@ -77,6 +77,10 @@ type PodSetAssignment struct {
 	// Beside what is provided in podSet's specs, this calculation takes into account
 	// the LimitRange defaults and RuntimeClass overheads at the moment of admission.
```

  Do we have a "wish list" for v1beta2? Should we create one?

- **apis/kueue/v1beta1/workload_types.go:140** by [@trasc](https://github.com/trasc) — 2023-05-12T05:27:43Z
```diff
@@ -124,6 +129,22 @@ type WorkloadStatus struct {
 	// +listType=map
 	// +listMapKey=type
 	Conditions []metav1.Condition `json:"conditions,omitempty"`
+
+	// reclaimablePods keeps track of the number pods within a podset for which
+	// the resource reservation is no longer needed.
+	// +optional
+	// +listType=map
+	// +listMapKey=name
+	ReclaimablePods []ReclaimablePod `json:"reclaimablePods,omitempty"`
```

  
  > If we assume that the admission section can be modified after admission (to update Count and usage), then I prefer we have `reclaimableCount` inside the admission struct.
  
  The admission is not changed when reclaimable changes.
  
  > @trasc Are you concerned about having reclaimableCount inside the Admission?
  
  SSA conflicts. We should keep them separated.

- **apis/kueue/webhooks/workload_webhook.go:266** by [@trasc](https://github.com/trasc) — 2023-05-12T05:29:17Z
```diff
@@ -200,3 +231,39 @@ func validateAdmissionUpdate(new, old *kueue.Admission, path *field.Path) field.
 	}
 	return apivalidation.ValidateImmutableField(new, old, path)
 }
+
+// validateReclaimablePodsUpdate validates that the reclaimable counts do not decrease, this should be checked
+// while the workload is admitted
+func validateReclaimablePodsUpdate(newObj, oldObj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	if workload.ReclaimablePodsAreEqual(newObj.Status.ReclaimablePods, oldObj.Status.ReclaimablePods) {
+		return nil
+	}
+
+	if len(oldObj.Status.ReclaimablePods) == 0 {
+		return nil
+	}
+
+	knowPodSets := make(map[string]*kueue.ReclaimablePod, len(oldObj.Status.ReclaimablePods))
+	for i := range oldObj.Status.ReclaimablePods {
+		name := oldObj.Status.ReclaimablePods[i].Name
+		knowPodSets[name] = &oldObj.Status.ReclaimablePods[i]
+	}
+
+	var ret field.ErrorList
+	newNames := sets.New[string]()
+	for i := range newObj.Status.ReclaimablePods {
+		newCount := &newObj.Status.ReclaimablePods[i]
+		newNames.Insert(newCount.Name)
+		oldCount, found := knowPodSets[newCount.Name]
+		if found && newCount.Count < oldCount.Count {
+			ret = append(ret, field.Invalid(basePath.Key(newCount.Name).Child("count"), newCount.Count, fmt.Sprintf("cannot be less then %d", oldCount.Count)))
+		}
```

  Let's keep it as-is.

- **pkg/controller/jobframework/reconciler.go:149** by [@trasc](https://github.com/trasc) — 2023-05-12T05:42:56Z
```diff
@@ -149,6 +149,16 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
```

  added a final update.

- **pkg/controller/jobframework/reconciler.go:153** by [@trasc](https://github.com/trasc) — 2023-05-12T05:45:18Z
```diff
@@ -148,7 +148,17 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, err
 	}
 
-	// 4. handle WaitForPodsReady only for a standalone job.
+	// 4. update reclaimable counts.
+	if rp := job.ReclaimablePods(); !workload.ReclaimablePodsAreEqual(rp, wl.Status.ReclaimablePods) {
+		err = workload.UpdateReclaimablePods(ctx, r.client, wl, rp)
```

  I think it's better not to change the admission info while the workload is admitted.

- **pkg/workload/workload.go:216** by [@trasc](https://github.com/trasc) — 2023-05-12T07:44:28Z
```diff
@@ -156,12 +203,18 @@ func ResourceQuantity(name corev1.ResourceName, v int64) resource.Quantity {
 	}
 }
 
-func (r Requests) scale(f int64) {
+func (r Requests) scaleUp(f int64) {
 	for name := range r {
 		r[name] *= f
 	}
 }
 
+func (r Requests) scaleDown(f int64) {
+	for name := range r {
+		r[name] /= f
+	}
+}
```

  done

- **apis/kueue/v1beta1/workload_types.go:82** by [@trasc](https://github.com/trasc) — 2023-05-12T07:47:19Z
```diff
@@ -76,7 +76,12 @@ type PodSetAssignment struct {
 	//
 	// Beside what is provided in podSet's specs, this calculation takes into account
 	// the LimitRange defaults and RuntimeClass overheads at the moment of admission.
+	// This field will not change in case of quota reclaim.
 	ResourceUsage corev1.ResourceList `json:"resourceUsage,omitempty"`
+
+	// count is the number of pods taken into account at admission time.
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-12T14:06:20Z

A few nits

- **apis/kueue/v1beta1/workload_types.go:78** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-12T13:55:38Z
```diff
@@ -77,6 +77,10 @@ type PodSetAssignment struct {
 	// Beside what is provided in podSet's specs, this calculation takes into account
 	// the LimitRange defaults and RuntimeClass overheads at the moment of admission.
```

  Good idea. Here it is https://github.com/kubernetes-sigs/kueue/issues/768
  
  For now, please add the clarification that this field won't change when reclaimable pods change.

- **apis/kueue/v1beta1/workload_types.go:140** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-12T13:58:01Z
```diff
@@ -124,6 +129,22 @@ type WorkloadStatus struct {
 	// +listType=map
 	// +listMapKey=type
 	Conditions []metav1.Condition `json:"conditions,omitempty"`
+
+	// reclaimablePods keeps track of the number pods within a podset for which
+	// the resource reservation is no longer needed.
+	// +optional
+	// +listType=map
+	// +listMapKey=name
+	ReclaimablePods []ReclaimablePod `json:"reclaimablePods,omitempty"`
```

  Let's say we have elastic jobs. Then the kueue scheduler+preemption could update `count`.
  
  However, this is generally independent from the concept of reclaiming pods.
  
  Ok, let's keep it like this.
  
  Any reason why not make this `map[string]int32`?

- **pkg/controller/jobframework/reconciler.go:153** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-12T14:01:42Z
```diff
@@ -148,7 +148,17 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, err
 	}
 
-	// 4. handle WaitForPodsReady only for a standalone job.
+	// 4. update reclaimable counts.
+	if rp := job.ReclaimablePods(); !workload.ReclaimablePodsAreEqual(rp, wl.Status.ReclaimablePods) {
+		err = workload.UpdateReclaimablePods(ctx, r.client, wl, rp)
```

  Sounds good for the concept of "reclaiming".
  
  We can revisit later when we do partial preemption or scale up.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-12T14:17:23Z

- **pkg/workload/workload.go:346** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-12T14:14:19Z
```diff
@@ -288,3 +341,30 @@ func GetQueueOrderTimestamp(w *kueue.Workload) *metav1.Time {
 func IsAdmitted(w *kueue.Workload) bool {
 	return apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadAdmitted)
 }
+
+// UpdateReclaimablePods updates the ReclaimablePods list for the workload wit SSA.
+func UpdateReclaimablePods(ctx context.Context, c client.Client, w *kueue.Workload, reclaimablePods []kueue.ReclaimablePod) error {
```

  ```suggestion
  func ApplyReclaimablePods(ctx context.Context, c client.Client, w *kueue.Workload, reclaimablePods []kueue.ReclaimablePod) error {
  ```

- **pkg/workload/workload.go:220** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-12T14:14:48Z
```diff
@@ -156,12 +203,18 @@ func ResourceQuantity(name corev1.ResourceName, v int64) resource.Quantity {
 	}
 }
 
-func (r Requests) scale(f int64) {
+func (r Requests) scaleUp(f int64) {
 	for name := range r {
 		r[name] *= f
 	}
 }
 
+func (r Requests) scaleDown(f int64) {
+	for name := range r {
+		r[name] /= f
+	}
+}
+
 // UpdateStatus updates the condition of a workload with ssa,
 // filelManager being set to managerPrefix + "-" + conditionType
 func UpdateStatus(ctx context.Context,
```

  Can you rename to ApplyStatus?

- **pkg/controller/jobframework/reconciler.go:136** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-12T14:17:20Z
```diff
@@ -129,6 +129,18 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		if wl == nil || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
 			return ctrl.Result{}, nil
 		}
+
+		// if the workload has reclaimable pods, do one last update.
+		if len(wl.Status.ReclaimablePods) > 0 {
+			if rp := job.ReclaimablePods(); !workload.ReclaimablePodsAreEqual(rp, wl.Status.ReclaimablePods) {
+				err = workload.UpdateReclaimablePods(ctx, r.client, wl, rp)
```

  Actually, let's not do this. It's a separate API call from the condition, which is not desirable.
  
  I would just force the last apply that sets the condition to also set the reclaimable pods.
  
  You can do it in a follow up, but for now, let's not add an extra API call.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-12T16:22:58Z

- **apis/kueue/v1beta1/workload_types.go:78** by [@trasc](https://github.com/trasc) — 2023-05-12T16:09:18Z
```diff
@@ -77,6 +77,10 @@ type PodSetAssignment struct {
 	// Beside what is provided in podSet's specs, this calculation takes into account
 	// the LimitRange defaults and RuntimeClass overheads at the moment of admission.
```

  it's done

- **apis/kueue/v1beta1/workload_types.go:140** by [@trasc](https://github.com/trasc) — 2023-05-12T16:14:42Z
```diff
@@ -124,6 +129,22 @@ type WorkloadStatus struct {
 	// +listType=map
 	// +listMapKey=type
 	Conditions []metav1.Condition `json:"conditions,omitempty"`
+
+	// reclaimablePods keeps track of the number pods within a podset for which
+	// the resource reservation is no longer needed.
+	// +optional
+	// +listType=map
+	// +listMapKey=name
+	ReclaimablePods []ReclaimablePod `json:"reclaimablePods,omitempty"`
```

  just to look similar to flavors and usage

- **pkg/controller/jobframework/reconciler.go:136** by [@trasc](https://github.com/trasc) — 2023-05-12T16:16:32Z
```diff
@@ -129,6 +129,18 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		if wl == nil || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
 			return ctrl.Result{}, nil
 		}
+
+		// if the workload has reclaimable pods, do one last update.
+		if len(wl.Status.ReclaimablePods) > 0 {
+			if rp := job.ReclaimablePods(); !workload.ReclaimablePodsAreEqual(rp, wl.Status.ReclaimablePods) {
+				err = workload.UpdateReclaimablePods(ctx, r.client, wl, rp)
```

  removed

- **pkg/workload/workload.go:220** by [@trasc](https://github.com/trasc) — 2023-05-12T16:18:47Z
```diff
@@ -156,12 +203,18 @@ func ResourceQuantity(name corev1.ResourceName, v int64) resource.Quantity {
 	}
 }
 
-func (r Requests) scale(f int64) {
+func (r Requests) scaleUp(f int64) {
 	for name := range r {
 		r[name] *= f
 	}
 }
 
+func (r Requests) scaleDown(f int64) {
+	for name := range r {
+		r[name] /= f
+	}
+}
+
 // UpdateStatus updates the condition of a workload with ssa,
 // filelManager being set to managerPrefix + "-" + conditionType
 func UpdateStatus(ctx context.Context,
```

  Let's keep "Apply" , for when we only push parts of a workload to the api server.

- **pkg/workload/workload.go:346** by [@trasc](https://github.com/trasc) — 2023-05-12T16:19:10Z
```diff
@@ -288,3 +341,30 @@ func GetQueueOrderTimestamp(w *kueue.Workload) *metav1.Time {
 func IsAdmitted(w *kueue.Workload) bool {
 	return apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadAdmitted)
 }
+
+// UpdateReclaimablePods updates the ReclaimablePods list for the workload wit SSA.
+func UpdateReclaimablePods(ctx context.Context, c client.Client, w *kueue.Workload, reclaimablePods []kueue.ReclaimablePod) error {
```

  Let's keep "Apply" , for when we only push parts of a workload to the api server.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-12T16:56:25Z

- **pkg/workload/workload.go:346** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-12T16:56:25Z
```diff
@@ -288,3 +341,30 @@ func GetQueueOrderTimestamp(w *kueue.Workload) *metav1.Time {
 func IsAdmitted(w *kueue.Workload) bool {
 	return apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadAdmitted)
 }
+
+// UpdateReclaimablePods updates the ReclaimablePods list for the workload wit SSA.
+func UpdateReclaimablePods(ctx context.Context, c client.Client, w *kueue.Workload, reclaimablePods []kueue.ReclaimablePod) error {
```

  Which this does?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-12T17:43:57Z

- **pkg/workload/workload.go:346** by [@trasc](https://github.com/trasc) — 2023-05-12T17:43:56Z
```diff
@@ -288,3 +341,30 @@ func GetQueueOrderTimestamp(w *kueue.Workload) *metav1.Time {
 func IsAdmitted(w *kueue.Workload) bool {
 	return apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadAdmitted)
 }
+
+// UpdateReclaimablePods updates the ReclaimablePods list for the workload wit SSA.
+func UpdateReclaimablePods(ctx context.Context, c client.Client, w *kueue.Workload, reclaimablePods []kueue.ReclaimablePod) error {
```

  `reclaimablePods []kueue.ReclaimablePod ` is not part of the  workload now, ` w *kueue.Workload` is used to identify the target.
  
  vs  `func ApplyAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, strict bool) error`  where w has the content set

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-12T17:46:28Z

- **pkg/workload/workload_test.go:162** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-12T17:46:28Z
```diff
@@ -110,6 +142,46 @@ func TestNewInfo(t *testing.T) {
 							corev1.ResourceMemory: 3 * 1024 * 1024,
 							"ex.com/gpu":          3,
 						},
+						Count: 3,
+					},
+				},
+			},
+		},
+		"admitted with reclaim": {
+			workload: *utiltesting.MakeWorkload("", "").
+				PodSets(
+					*utiltesting.MakePodSet("main", 5).
+						Request(corev1.ResourceCPU, "10m").
+						Request(corev1.ResourceMemory, "10Ki").
+						Obj(),
+				).
+				Admit(
+					utiltesting.MakeAdmission("").
+						Assignment(corev1.ResourceCPU, "f1", "40m").
+						Assignment(corev1.ResourceMemory, "f1", "40Ki").
+						AssignmentPodCount(4).
```

  In the real world, `AddignmentPodCount` is `3`, right?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-12T17:52:58Z

- **pkg/workload/workload_test.go:162** by [@trasc](https://github.com/trasc) — 2023-05-12T17:52:58Z
```diff
@@ -110,6 +142,46 @@ func TestNewInfo(t *testing.T) {
 							corev1.ResourceMemory: 3 * 1024 * 1024,
 							"ex.com/gpu":          3,
 						},
+						Count: 3,
+					},
+				},
+			},
+		},
+		"admitted with reclaim": {
+			workload: *utiltesting.MakeWorkload("", "").
+				PodSets(
+					*utiltesting.MakePodSet("main", 5).
+						Request(corev1.ResourceCPU, "10m").
+						Request(corev1.ResourceMemory, "10Ki").
+						Obj(),
+				).
+				Admit(
+					utiltesting.MakeAdmission("").
+						Assignment(corev1.ResourceCPU, "f1", "40m").
+						Assignment(corev1.ResourceMemory, "f1", "40Ki").
+						AssignmentPodCount(4).
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-12T18:05:04Z

- **test/integration/controller/job/job_controller_test.go:1012** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-12T18:04:32Z
```diff
@@ -975,4 +958,73 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 			})
 		})
 	})
+
+	ginkgo.It("Should allow reclaim of resources that are no longer needed", func() {
+		ginkgo.By("creating localQueue", func() {
+			prodLocalQ = testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodLocalQ)).Should(gomega.Succeed())
+		})
+
+		job1 := testingjob.MakeJob("job1", ns.Name).Queue(prodLocalQ.Name).
+			Request(corev1.ResourceCPU, "2").
+			Completions(5).
+			Parallelism(2).
+			Obj()
+		lookupKey1 := types.NamespacedName{Name: job1.Name, Namespace: job1.Namespace}
+
+		ginkgo.By("checking the first job starts", func() {
+			gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
+			createdJob1 := &batchv1.Job{}
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey1, createdJob1)).Should(gomega.Succeed())
+				return createdJob1.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
+			gomega.Expect(createdJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+			util.ExpectPendingWorkloadsMetric(prodClusterQ, 0, 0)
+			util.ExpectAdmittedActiveWorkloadsMetric(prodClusterQ, 1)
+		})
+
+		job2 := testingjob.MakeJob("job2", ns.Name).Queue(prodLocalQ.Name).Request(corev1.ResourceCPU, "3").Obj()
+		lookupKey2 := types.NamespacedName{Name: job2.Name, Namespace: job2.Namespace}
+
+		ginkgo.By("checking a second no-fit job does not start", func() {
+			gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
+			createdJob2 := &batchv1.Job{}
+			gomega.Consistently(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey2, createdJob2)).Should(gomega.Succeed())
+				return createdJob2.Spec.Suspend
+			}, util.ConsistentDuration, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			util.ExpectPendingWorkloadsMetric(prodClusterQ, 0, 1)
+			util.ExpectAdmittedActiveWorkloadsMetric(prodClusterQ, 1)
+		})
+
+		ginkgo.By("checking the second job starts when the first has less then to completions to go", func() {
+			createdJob1 := &batchv1.Job{}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey1, createdJob1)).Should(gomega.Succeed())
+			createdJob1.Status.Succeeded = 4
+			gomega.Expect(k8sClient.Status().Update(ctx, createdJob1)).Should(gomega.Succeed())
+
+			wl := &kueue.Workload{}
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job1.Name), Namespace: job1.Namespace}
+			gomega.Eventually(func() []kueue.ReclaimablePod {
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				return wl.Status.ReclaimablePods
+
```

  ```suggestion
  ```
  
  Reverting this empty line would be good.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-12T18:29:35Z

- **pkg/workload/workload.go:346** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-12T18:29:35Z
```diff
@@ -288,3 +341,30 @@ func GetQueueOrderTimestamp(w *kueue.Workload) *metav1.Time {
 func IsAdmitted(w *kueue.Workload) bool {
 	return apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadAdmitted)
 }
+
+// UpdateReclaimablePods updates the ReclaimablePods list for the workload wit SSA.
+func UpdateReclaimablePods(ctx context.Context, c client.Client, w *kueue.Workload, reclaimablePods []kueue.ReclaimablePod) error {
```

  I want to be able to quickly identify which function is using Update (PUT), versus SSA (PATCH). For that reason, I find `Apply` very obvious.
  
  But we can clean this up later.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-12T18:30:51Z

/lgtm
/approve
