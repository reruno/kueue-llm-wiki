# PR #7599: add field multiplyBy for ResourceTransformation

**Summary**: add field multiplyBy for ResourceTransformation

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7599

**Last updated**: 2025-12-12T16:35:47Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@calvin0327](https://github.com/calvin0327)
- **Branch**: `add-multiplyBy` → `main`
- **Created**: 2025-11-11T05:30:00Z
- **Updated**: 2025-12-12T16:35:47Z
- **Merged**: 2025-12-12T16:35:45Z
- **Closed**: 2025-12-12T16:35:45Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `kind/api-change`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@dgrove-oss](https://github.com/dgrove-oss)
- **Changed files**: 9   **+363 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
/kind api-change

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Ref: https://github.com/kubernetes-sigs/kueue/pull/7231

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
add field multiplyBy for ResourceTransformation
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-11-11T05:30:07Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | b618ad287e5153ebcd31757941981fb529654e9c |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/693bc3f3aa93280008dca729 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-11T05:30:11Z

Hi @calvin0327. Thanks for your PR.

I'm waiting for a [github.com](https://kubernetes-sigs/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@kannon92](https://github.com/kannon92) — 2025-11-11T14:24:06Z

/ok-to-test

/cc @dgrove-oss 

Could you take a look at this when you get a chance?

### Comment by [@kannon92](https://github.com/kannon92) — 2025-11-11T21:03:41Z

We should implement the feature in this PR also.

This PR just adds the API field but I imagine you may have to use this API somewhere in the code right?

### Comment by [@calvin0327](https://github.com/calvin0327) — 2025-11-12T01:58:33Z

> We should implement the feature in this PR also.
> 
> This PR just adds the API field but I imagine you may have to use this API somewhere in the code right?

Okay, I will implement this feature in this PR.

### Comment by [@calvin0327](https://github.com/calvin0327) — 2025-11-17T14:30:24Z

this is my test result:

I add the resource configuration in controller_manager_config.yaml:
<img width="831" height="604" alt="image" src="https://github.com/user-attachments/assets/b98089ad-bff2-493a-95b7-b3cfde78e722" />


The is my deployment,  I need 2 gpu, each with 10% cores and 500M gpumem:

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gpu-burn
  namespace: kueue-test
  labels:
    kueue.x-k8s.io/queue-name: user-queue
spec:
  replicas: 1
  selector:
    matchLabels:
      app-name: gpu-burn
  template:
    metadata:
      labels:
        app-name: gpu-burn
    spec:
      containers:
      - args:
        - while :; do /app/gpu_burn 300 || true; sleep 300; done
        command:
        - /bin/sh
        - -lc
        image: oguzpastirmaci/gpu-burn:latest
        imagePullPolicy: IfNotPresent
        name: main
        resources:
          limits:
            nvidia.com/gpu: "2"
            nvidia.com/gpucores: "10"
            nvidia.com/gpumem: "500"
          requests:
            nvidia.com/gpu: "2"
            nvidia.com/gpucores: "10"
            nvidia.com/gpumem: "500"
```

This is `clusterQueue` yaml, the `total-gpucores` and `total-gpumem` are calculated correctly.

<img width="547" height="732" alt="image" src="https://github.com/user-attachments/assets/1917db6c-a6a0-420b-aa27-a177a6b5d98e" />

### Comment by [@calvin0327](https://github.com/calvin0327) — 2025-11-25T02:13:26Z

@kannon92 @dgrove-oss PTAL, thanks

### Comment by [@calvin0327](https://github.com/calvin0327) — 2025-12-01T10:28:35Z

@kannon92 Hi, Do you have any other comments？

### Comment by [@kannon92](https://github.com/kannon92) — 2025-12-03T20:06:34Z

Change looks good to me.

I did have one question for @mimowo.

https://github.com/kubernetes-sigs/kueue/pull/7599/files#r2578032945

### Comment by [@mimowo](https://github.com/mimowo) — 2025-12-05T18:17:29Z

/assign 
I will be looking into this next week

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-12T07:25:36Z

@calvin0327: The following test **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:

Test name | Commit | Details | Required | Rerun command
--- | --- | --- | --- | ---
pull-kueue-test-e2e-main-1-31 | b0fcfd1bd0cf228f13d942722bd8bbf0b478defa | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7599/pull-kueue-test-e2e-main-1-31/1988251418611421184) | true | `/test pull-kueue-test-e2e-main-1-31`

[Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=7599). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3Acalvin0327). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository. I understand the commands that are listed [here](https://go.k8s.io/bot-commands).
</details>
<!-- test report -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-12-12T16:06:05Z

Thank you 👍 
/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-12T16:06:13Z

LGTM label has been added.  <details>Git tree hash: 7ff36f60724d3f8a43fdfc338876b4eba35c3506</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-12T16:06:15Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7599#" title="Author self-approved">calvin0327</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7599#issuecomment-3647191946" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-25T02:26:36Z

- **pkg/workload/workload.go:422** by [@kannon92](https://github.com/kannon92) — 2025-11-25T02:26:35Z
```diff
@@ -416,6 +416,16 @@ func applyResourceTransformations(input corev1.ResourceList, transforms map[core
 	output := make(corev1.ResourceList)
 	for inputName, inputQuantity := range input {
 		if mapping, ok := transforms[inputName]; ok {
+			// If MultiplyBy is specified, multiply the input quantity by
```

  Can you add some unit tests for this feature?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-25T02:26:57Z

- **pkg/workload/workload.go:422** by [@kannon92](https://github.com/kannon92) — 2025-11-25T02:26:57Z
```diff
@@ -416,6 +416,16 @@ func applyResourceTransformations(input corev1.ResourceList, transforms map[core
 	output := make(corev1.ResourceList)
 	for inputName, inputQuantity := range input {
 		if mapping, ok := transforms[inputName]; ok {
+			// If MultiplyBy is specified, multiply the input quantity by
```

  looks like ResourceTransformations already have some examples at a quick glance (https://github.com/kubernetes-sigs/kueue/blob/a10c41cd4e5153c12ee01c08ae18bb6e84c6991a/pkg/workload/workload_test.go#L375)

### Review by [@calvin0327](https://github.com/calvin0327) (commented) — 2025-11-26T08:59:31Z

- **pkg/workload/workload.go:422** by [@calvin0327](https://github.com/calvin0327) — 2025-11-26T08:59:31Z
```diff
@@ -416,6 +416,16 @@ func applyResourceTransformations(input corev1.ResourceList, transforms map[core
 	output := make(corev1.ResourceList)
 	for inputName, inputQuantity := range input {
 		if mapping, ok := transforms[inputName]; ok {
+			// If MultiplyBy is specified, multiply the input quantity by
```

  Sorry, I didn't notice the unit tests here earlier.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-12-01T17:38:20Z

- **pkg/workload/workload.go:421** by [@kannon92](https://github.com/kannon92) — 2025-12-01T17:38:20Z
```diff
@@ -416,6 +416,16 @@ func applyResourceTransformations(input corev1.ResourceList, transforms map[core
 	output := make(corev1.ResourceList)
 	for inputName, inputQuantity := range input {
 		if mapping, ok := transforms[inputName]; ok {
+			// If MultiplyBy is specified, multiply the input quantity by
+			// the value of the resource specified in MultiplyBy.
+			// Please see: https://github.com/kubernetes-sigs/kueue/pull/7231
```

  ```suggestion
  ```
  
  Please remove the Please see.
  
  The comment is self contained enough without having to link out to a PR.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-12-01T17:40:21Z

- **apis/config/v1beta2/configuration_types.go:464** by [@kannon92](https://github.com/kannon92) — 2025-12-01T17:40:21Z
```diff
@@ -461,6 +461,10 @@ type ResourceTransformation struct {
 	// Defaults to Retain
 	Strategy *ResourceTransformationStrategy `json:"strategy,omitempty"`
 
+	// MultiplyBy defines a resource name, which performs a multiplication
```

  @mimowo 
  should we add this field to v1beta1?

### Review by [@calvin0327](https://github.com/calvin0327) (commented) — 2025-12-02T03:56:27Z

- **pkg/workload/workload.go:421** by [@calvin0327](https://github.com/calvin0327) — 2025-12-02T03:56:27Z
```diff
@@ -416,6 +416,16 @@ func applyResourceTransformations(input corev1.ResourceList, transforms map[core
 	output := make(corev1.ResourceList)
 	for inputName, inputQuantity := range input {
 		if mapping, ok := transforms[inputName]; ok {
+			// If MultiplyBy is specified, multiply the input quantity by
+			// the value of the resource specified in MultiplyBy.
+			// Please see: https://github.com/kubernetes-sigs/kueue/pull/7231
```

  ok

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-08T12:22:44Z

- **apis/config/v1beta2/configuration_types.go:464** by [@mimowo](https://github.com/mimowo) — 2025-12-08T12:22:43Z
```diff
@@ -461,6 +461,10 @@ type ResourceTransformation struct {
 	// Defaults to Retain
 	Strategy *ResourceTransformationStrategy `json:"strategy,omitempty"`
 
+	// MultiplyBy defines a resource name, which performs a multiplication
```

  TBH I would support v1beta1, then we don't need any custom conversion code, we could just rely on auto-generated.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-08T12:24:50Z

- **pkg/workload/workload.go:423** by [@mimowo](https://github.com/mimowo) — 2025-12-08T12:24:50Z
```diff
@@ -418,6 +416,15 @@ func applyResourceTransformations(input corev1.ResourceList, transforms map[core
 	output := make(corev1.ResourceList)
 	for inputName, inputQuantity := range input {
 		if mapping, ok := transforms[inputName]; ok {
+			// If MultiplyBy is specified, multiply the input quantity by
+			// the value of the resource specified in MultiplyBy.
+			if mapping.MultiplyBy != "" {
+				q, ok := input[mapping.MultiplyBy]
+				if ok && !q.IsZero() {
```

  Why zero is a special case here? If it is 0, then I think we should multiply and get 0. I understand it may not be very useful,  but I think introducing the special case does not make it clearer. And also I didn't see any test case for that.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-08T12:25:17Z

- **pkg/workload/workload.go:424** by [@mimowo](https://github.com/mimowo) — 2025-12-08T12:25:17Z
```diff
@@ -418,6 +416,15 @@ func applyResourceTransformations(input corev1.ResourceList, transforms map[core
 	output := make(corev1.ResourceList)
 	for inputName, inputQuantity := range input {
 		if mapping, ok := transforms[inputName]; ok {
+			// If MultiplyBy is specified, multiply the input quantity by
+			// the value of the resource specified in MultiplyBy.
+			if mapping.MultiplyBy != "" {
```

  Please also add an integration test for it.

### Review by [@calvin0327](https://github.com/calvin0327) (commented) — 2025-12-09T12:51:16Z

- **pkg/workload/workload.go:424** by [@calvin0327](https://github.com/calvin0327) — 2025-12-09T12:51:16Z
```diff
@@ -418,6 +416,15 @@ func applyResourceTransformations(input corev1.ResourceList, transforms map[core
 	output := make(corev1.ResourceList)
 	for inputName, inputQuantity := range input {
 		if mapping, ok := transforms[inputName]; ok {
+			// If MultiplyBy is specified, multiply the input quantity by
+			// the value of the resource specified in MultiplyBy.
+			if mapping.MultiplyBy != "" {
```

  ok

### Review by [@calvin0327](https://github.com/calvin0327) (commented) — 2025-12-09T14:37:46Z

- **apis/config/v1beta2/configuration_types.go:464** by [@calvin0327](https://github.com/calvin0327) — 2025-12-09T14:37:46Z
```diff
@@ -461,6 +461,10 @@ type ResourceTransformation struct {
 	// Defaults to Retain
 	Strategy *ResourceTransformationStrategy `json:"strategy,omitempty"`
 
+	// MultiplyBy defines a resource name, which performs a multiplication
```

  Ok, I add the field to v1beta1.

### Review by [@calvin0327](https://github.com/calvin0327) (commented) — 2025-12-11T10:52:33Z

- **pkg/workload/workload.go:424** by [@calvin0327](https://github.com/calvin0327) — 2025-12-11T10:52:33Z
```diff
@@ -418,6 +416,15 @@ func applyResourceTransformations(input corev1.ResourceList, transforms map[core
 	output := make(corev1.ResourceList)
 	for inputName, inputQuantity := range input {
 		if mapping, ok := transforms[inputName]; ok {
+			// If MultiplyBy is specified, multiply the input quantity by
+			// the value of the resource specified in MultiplyBy.
+			if mapping.MultiplyBy != "" {
```

  I have added a integration test for `ResourceTransformation`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-11T16:03:07Z

- **test/integration/singlecluster/scheduler/resourcetransformations/resource_transformations_test.go:124** by [@mimowo](https://github.com/mimowo) — 2025-12-11T16:03:07Z
```diff
@@ -0,0 +1,192 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package resourcetransformations
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Resource Transformations", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		clusterQueue  *kueue.ClusterQueue
+		localQueue    *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		// Configure resource transformations for testing
+		transformations := []config.ResourceTransformation{
+			{
+				Input:    "nvidia.com/mig-1g.5gb",
+				Strategy: ptr.To(config.Replace),
+				Outputs: corev1.ResourceList{
+					"example.com/accelerator-memory": resource.MustParse("5Ki"),
+					"example.com/credits":            resource.MustParse("10"),
+				},
+			},
+			{
+				Input:      "nvidia.com/gpucores",
+				Strategy:   ptr.To(config.Replace),
+				MultiplyBy: "nvidia.com/gpu",
+				Outputs: corev1.ResourceList{
+					"nvidia.com/total-gpucores": resource.MustParse("1"),
+				},
+			},
+			{
+				Input:      "nvidia.com/gpumem",
+				Strategy:   ptr.To(config.Replace),
+				MultiplyBy: "nvidia.com/gpu",
+				Outputs: corev1.ResourceList{
+					"nvidia.com/total-gpumem": resource.MustParse("1"),
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(transformations))
+	})
+
+	ginkgo.BeforeEach(func() {
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		util.MustCreate(ctx, k8sClient, defaultFlavor)
+
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "resource-transformations-")
+
+		clusterQueue = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource("example.com/accelerator-memory", "100Gi").
+					Resource("example.com/credits", "1000").
+					Resource("nvidia.com/gpu", "50").
+					Resource("nvidia.com/total-gpucores", "1000").
+					Resource("nvidia.com/total-gpumem", "102400").
+					Obj(),
+			).Obj()
+		util.MustCreate(ctx, k8sClient, clusterQueue)
+
+		localQueue = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+		util.MustCreate(ctx, k8sClient, localQueue)
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.It("should transform resources with MultiplyBy", func() {
+		ginkgo.By("Creating a workload with vgpu resources")
+		wl := utiltestingapi.MakeWorkload("multiply-wl", ns.Name).
+			Queue(kueue.LocalQueueName(localQueue.Name)).
+			Request("nvidia.com/gpu", "2").
+			Request("nvidia.com/gpucores", "20").
+			Request("nvidia.com/gpumem", "1024").
+			Obj()
+		util.MustCreate(ctx, k8sClient, wl)
+
+		ginkgo.By("Waiting for workload to be admitted")
+		util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+
+		wlLookupKey := client.ObjectKeyFromObject(wl)
+		createdWorkload := &kueue.Workload{}
+
+		ginkgo.By("Verifying workload is created but not admitted yet", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
```

  I think this block is misleading, because above we already checked `ExpectWorkloadsToBeAdmitted`. Can we just remove it?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-11T16:03:27Z

- **test/integration/singlecluster/scheduler/resourcetransformations/resource_transformations_test.go:169** by [@mimowo](https://github.com/mimowo) — 2025-12-11T16:03:27Z
```diff
@@ -0,0 +1,192 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package resourcetransformations
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Resource Transformations", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		clusterQueue  *kueue.ClusterQueue
+		localQueue    *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		// Configure resource transformations for testing
+		transformations := []config.ResourceTransformation{
+			{
+				Input:    "nvidia.com/mig-1g.5gb",
+				Strategy: ptr.To(config.Replace),
+				Outputs: corev1.ResourceList{
+					"example.com/accelerator-memory": resource.MustParse("5Ki"),
+					"example.com/credits":            resource.MustParse("10"),
+				},
+			},
+			{
+				Input:      "nvidia.com/gpucores",
+				Strategy:   ptr.To(config.Replace),
+				MultiplyBy: "nvidia.com/gpu",
+				Outputs: corev1.ResourceList{
+					"nvidia.com/total-gpucores": resource.MustParse("1"),
+				},
+			},
+			{
+				Input:      "nvidia.com/gpumem",
+				Strategy:   ptr.To(config.Replace),
+				MultiplyBy: "nvidia.com/gpu",
+				Outputs: corev1.ResourceList{
+					"nvidia.com/total-gpumem": resource.MustParse("1"),
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(transformations))
+	})
+
+	ginkgo.BeforeEach(func() {
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		util.MustCreate(ctx, k8sClient, defaultFlavor)
+
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "resource-transformations-")
+
+		clusterQueue = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource("example.com/accelerator-memory", "100Gi").
+					Resource("example.com/credits", "1000").
+					Resource("nvidia.com/gpu", "50").
+					Resource("nvidia.com/total-gpucores", "1000").
+					Resource("nvidia.com/total-gpumem", "102400").
+					Obj(),
+			).Obj()
+		util.MustCreate(ctx, k8sClient, clusterQueue)
+
+		localQueue = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+		util.MustCreate(ctx, k8sClient, localQueue)
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.It("should transform resources with MultiplyBy", func() {
+		ginkgo.By("Creating a workload with vgpu resources")
+		wl := utiltestingapi.MakeWorkload("multiply-wl", ns.Name).
+			Queue(kueue.LocalQueueName(localQueue.Name)).
+			Request("nvidia.com/gpu", "2").
+			Request("nvidia.com/gpucores", "20").
+			Request("nvidia.com/gpumem", "1024").
+			Obj()
+		util.MustCreate(ctx, k8sClient, wl)
+
+		ginkgo.By("Waiting for workload to be admitted")
+		util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+
+		wlLookupKey := client.ObjectKeyFromObject(wl)
+		createdWorkload := &kueue.Workload{}
+
+		ginkgo.By("Verifying workload is created but not admitted yet", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Verifying MultiplyBy transformation")
+		gomega.Expect(createdWorkload.Status.Admission).NotTo(gomega.BeNil())
+		resourceUsage := createdWorkload.Status.Admission.PodSetAssignments[0].ResourceUsage
+
+		// Original resources should be replaced
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("nvidia.com/gpucores")))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("nvidia.com/gpumem")))
+
+		// Transformed resources: vgpucores * vgpu = 20 * 2 = 40
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceName("nvidia.com/total-gpucores")))
+		gomega.Expect(resourceUsage["nvidia.com/total-gpucores"]).To(gomega.Equal(resource.MustParse("40")))
+
+		// Transformed resources: vgpumem * vgpu = 1024 * 2 = 2048
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceName("nvidia.com/total-gpumem")))
+		gomega.Expect(resourceUsage["nvidia.com/total-gpumem"]).To(gomega.Equal(resource.MustParse("2048")))
+	})
+
+	ginkgo.It("should handle multiple PodSets with transformations", func() {
+		ginkgo.By("Creating a workload with multiple PodSets")
+		wl := utiltestingapi.MakeWorkload("multi-podset-wl", ns.Name).
+			Queue(kueue.LocalQueueName(localQueue.Name)).
+			PodSets(
+				*utiltestingapi.MakePodSet("ps01", 1).
+					Request("nvidia.com/mig-1g.5gb", "2").
+					Obj(),
+				*utiltestingapi.MakePodSet("ps02", 2).
+					Request("nvidia.com/gpu", "1").
+					Request("nvidia.com/gpucores", "10").
+					Obj(),
+			).
+			Obj()
+		util.MustCreate(ctx, k8sClient, wl)
+
+		ginkgo.By("Waiting for workload to be admitted")
+		util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+
+		wlLookupKey := client.ObjectKeyFromObject(wl)
+		createdWorkload := &kueue.Workload{}
+
+		ginkgo.By("Verifying workload is created but not admitted yet", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
```

  Same as in the other test, this looks unnecessary

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-11T16:05:46Z

- **test/integration/singlecluster/scheduler/resourcetransformations/resource_transformations_test.go:180** by [@mimowo](https://github.com/mimowo) — 2025-12-11T16:05:46Z
```diff
@@ -0,0 +1,192 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package resourcetransformations
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Resource Transformations", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		clusterQueue  *kueue.ClusterQueue
+		localQueue    *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		// Configure resource transformations for testing
+		transformations := []config.ResourceTransformation{
+			{
+				Input:    "nvidia.com/mig-1g.5gb",
+				Strategy: ptr.To(config.Replace),
+				Outputs: corev1.ResourceList{
+					"example.com/accelerator-memory": resource.MustParse("5Ki"),
+					"example.com/credits":            resource.MustParse("10"),
+				},
+			},
+			{
+				Input:      "nvidia.com/gpucores",
+				Strategy:   ptr.To(config.Replace),
+				MultiplyBy: "nvidia.com/gpu",
+				Outputs: corev1.ResourceList{
+					"nvidia.com/total-gpucores": resource.MustParse("1"),
+				},
+			},
+			{
+				Input:      "nvidia.com/gpumem",
+				Strategy:   ptr.To(config.Replace),
+				MultiplyBy: "nvidia.com/gpu",
+				Outputs: corev1.ResourceList{
+					"nvidia.com/total-gpumem": resource.MustParse("1"),
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(transformations))
+	})
+
+	ginkgo.BeforeEach(func() {
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		util.MustCreate(ctx, k8sClient, defaultFlavor)
+
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "resource-transformations-")
+
+		clusterQueue = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource("example.com/accelerator-memory", "100Gi").
+					Resource("example.com/credits", "1000").
+					Resource("nvidia.com/gpu", "50").
+					Resource("nvidia.com/total-gpucores", "1000").
+					Resource("nvidia.com/total-gpumem", "102400").
+					Obj(),
+			).Obj()
+		util.MustCreate(ctx, k8sClient, clusterQueue)
+
+		localQueue = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+		util.MustCreate(ctx, k8sClient, localQueue)
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.It("should transform resources with MultiplyBy", func() {
+		ginkgo.By("Creating a workload with vgpu resources")
+		wl := utiltestingapi.MakeWorkload("multiply-wl", ns.Name).
+			Queue(kueue.LocalQueueName(localQueue.Name)).
+			Request("nvidia.com/gpu", "2").
+			Request("nvidia.com/gpucores", "20").
+			Request("nvidia.com/gpumem", "1024").
+			Obj()
+		util.MustCreate(ctx, k8sClient, wl)
+
+		ginkgo.By("Waiting for workload to be admitted")
+		util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+
+		wlLookupKey := client.ObjectKeyFromObject(wl)
+		createdWorkload := &kueue.Workload{}
+
+		ginkgo.By("Verifying workload is created but not admitted yet", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Verifying MultiplyBy transformation")
+		gomega.Expect(createdWorkload.Status.Admission).NotTo(gomega.BeNil())
+		resourceUsage := createdWorkload.Status.Admission.PodSetAssignments[0].ResourceUsage
+
+		// Original resources should be replaced
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("nvidia.com/gpucores")))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("nvidia.com/gpumem")))
+
+		// Transformed resources: vgpucores * vgpu = 20 * 2 = 40
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceName("nvidia.com/total-gpucores")))
+		gomega.Expect(resourceUsage["nvidia.com/total-gpucores"]).To(gomega.Equal(resource.MustParse("40")))
+
+		// Transformed resources: vgpumem * vgpu = 1024 * 2 = 2048
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceName("nvidia.com/total-gpumem")))
+		gomega.Expect(resourceUsage["nvidia.com/total-gpumem"]).To(gomega.Equal(resource.MustParse("2048")))
+	})
+
+	ginkgo.It("should handle multiple PodSets with transformations", func() {
+		ginkgo.By("Creating a workload with multiple PodSets")
+		wl := utiltestingapi.MakeWorkload("multi-podset-wl", ns.Name).
+			Queue(kueue.LocalQueueName(localQueue.Name)).
+			PodSets(
+				*utiltestingapi.MakePodSet("ps01", 1).
+					Request("nvidia.com/mig-1g.5gb", "2").
+					Obj(),
+				*utiltestingapi.MakePodSet("ps02", 2).
+					Request("nvidia.com/gpu", "1").
+					Request("nvidia.com/gpucores", "10").
+					Obj(),
+			).
+			Obj()
+		util.MustCreate(ctx, k8sClient, wl)
+
+		ginkgo.By("Waiting for workload to be admitted")
+		util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+
+		wlLookupKey := client.ObjectKeyFromObject(wl)
+		createdWorkload := &kueue.Workload{}
+
+		ginkgo.By("Verifying workload is created but not admitted yet", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Verifying transformations for each PodSet")
+		gomega.Expect(createdWorkload.Status.Admission).NotTo(gomega.BeNil())
+		gomega.Expect(createdWorkload.Status.Admission.PodSetAssignments).To(gomega.HaveLen(2))
+
+		ps1Usage := createdWorkload.Status.Admission.PodSetAssignments[0].ResourceUsage
+		gomega.Expect(ps1Usage).NotTo(gomega.HaveKey(corev1.ResourceName("nvidia.com/mig-1g.5gb")))
+		gomega.Expect(ps1Usage).To(gomega.HaveKey(corev1.ResourceName("example.com/accelerator-memory")))
+		gomega.Expect(ps1Usage["example.com/accelerator-memory"]).To(gomega.Equal(resource.MustParse("10240")))
+		gomega.Expect(ps1Usage).To(gomega.HaveKey(corev1.ResourceName("example.com/credits")))
+		gomega.Expect(ps1Usage["example.com/credits"]).To(gomega.Equal(resource.MustParse("20"))) // 2 * 10
```

  This checks line by line are pretty cubmersome. Could you please try to rewrite them by asserting 
  `gomega.Expect(ps1Usage).To(gomega.Equal(expectedPs1Usage))`, same in other cases, wdyt?
  
  Please check if the diff is produced nicely, if not there are also helper functions in gomega we use for example for asserting on conditions like BeEquivalentTo or BeComparatableWith I think

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-11T16:06:24Z

- **test/integration/singlecluster/scheduler/resourcetransformations/resource_transformations_test.go:140** by [@mimowo](https://github.com/mimowo) — 2025-12-11T16:06:24Z
```diff
@@ -0,0 +1,192 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package resourcetransformations
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Resource Transformations", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		clusterQueue  *kueue.ClusterQueue
+		localQueue    *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		// Configure resource transformations for testing
+		transformations := []config.ResourceTransformation{
+			{
+				Input:    "nvidia.com/mig-1g.5gb",
+				Strategy: ptr.To(config.Replace),
+				Outputs: corev1.ResourceList{
+					"example.com/accelerator-memory": resource.MustParse("5Ki"),
+					"example.com/credits":            resource.MustParse("10"),
+				},
+			},
+			{
+				Input:      "nvidia.com/gpucores",
+				Strategy:   ptr.To(config.Replace),
+				MultiplyBy: "nvidia.com/gpu",
+				Outputs: corev1.ResourceList{
+					"nvidia.com/total-gpucores": resource.MustParse("1"),
+				},
+			},
+			{
+				Input:      "nvidia.com/gpumem",
+				Strategy:   ptr.To(config.Replace),
+				MultiplyBy: "nvidia.com/gpu",
+				Outputs: corev1.ResourceList{
+					"nvidia.com/total-gpumem": resource.MustParse("1"),
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(transformations))
+	})
+
+	ginkgo.BeforeEach(func() {
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		util.MustCreate(ctx, k8sClient, defaultFlavor)
+
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "resource-transformations-")
+
+		clusterQueue = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource("example.com/accelerator-memory", "100Gi").
+					Resource("example.com/credits", "1000").
+					Resource("nvidia.com/gpu", "50").
+					Resource("nvidia.com/total-gpucores", "1000").
+					Resource("nvidia.com/total-gpumem", "102400").
+					Obj(),
+			).Obj()
+		util.MustCreate(ctx, k8sClient, clusterQueue)
+
+		localQueue = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+		util.MustCreate(ctx, k8sClient, localQueue)
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.It("should transform resources with MultiplyBy", func() {
+		ginkgo.By("Creating a workload with vgpu resources")
+		wl := utiltestingapi.MakeWorkload("multiply-wl", ns.Name).
+			Queue(kueue.LocalQueueName(localQueue.Name)).
+			Request("nvidia.com/gpu", "2").
+			Request("nvidia.com/gpucores", "20").
+			Request("nvidia.com/gpumem", "1024").
+			Obj()
+		util.MustCreate(ctx, k8sClient, wl)
+
+		ginkgo.By("Waiting for workload to be admitted")
+		util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+
+		wlLookupKey := client.ObjectKeyFromObject(wl)
+		createdWorkload := &kueue.Workload{}
+
+		ginkgo.By("Verifying workload is created but not admitted yet", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Verifying MultiplyBy transformation")
+		gomega.Expect(createdWorkload.Status.Admission).NotTo(gomega.BeNil())
+		resourceUsage := createdWorkload.Status.Admission.PodSetAssignments[0].ResourceUsage
+
+		// Original resources should be replaced
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("nvidia.com/gpucores")))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("nvidia.com/gpumem")))
+
+		// Transformed resources: vgpucores * vgpu = 20 * 2 = 40
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceName("nvidia.com/total-gpucores")))
+		gomega.Expect(resourceUsage["nvidia.com/total-gpucores"]).To(gomega.Equal(resource.MustParse("40")))
+
+		// Transformed resources: vgpumem * vgpu = 1024 * 2 = 2048
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceName("nvidia.com/total-gpumem")))
+		gomega.Expect(resourceUsage["nvidia.com/total-gpumem"]).To(gomega.Equal(resource.MustParse("2048")))
```

  As commented in the other test I would prefer to assert on the entire structure: resourceUsage

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-11T16:10:02Z

- **apis/config/v1beta1/configuration_types.go:491** by [@mimowo](https://github.com/mimowo) — 2025-12-11T16:10:02Z
```diff
@@ -487,6 +487,10 @@ type ResourceTransformation struct {
 	// Defaults to Retain
 	Strategy *ResourceTransformationStrategy `json:"strategy,omitempty"`
 
+	// MultiplyBy defines a resource name, which performs a multiplication
+	// calculation on the resource name of the input.
```

  ```suggestion
  	// MultiplyBy indicates the resource name requested by a workload, if
  	// specified.
  	// The requested amount of the resource is used to multiply the requested
  	// amount of the resource indicated by the "input" field.
  	// +optional
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-11T16:10:38Z

LGTM, mostly editorial comments, and to improve readability of tests

### Review by [@calvin0327](https://github.com/calvin0327) (commented) — 2025-12-12T08:42:26Z

- **test/integration/singlecluster/scheduler/resourcetransformations/resource_transformations_test.go:140** by [@calvin0327](https://github.com/calvin0327) — 2025-12-12T08:42:26Z
```diff
@@ -0,0 +1,192 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package resourcetransformations
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Resource Transformations", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		clusterQueue  *kueue.ClusterQueue
+		localQueue    *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		// Configure resource transformations for testing
+		transformations := []config.ResourceTransformation{
+			{
+				Input:    "nvidia.com/mig-1g.5gb",
+				Strategy: ptr.To(config.Replace),
+				Outputs: corev1.ResourceList{
+					"example.com/accelerator-memory": resource.MustParse("5Ki"),
+					"example.com/credits":            resource.MustParse("10"),
+				},
+			},
+			{
+				Input:      "nvidia.com/gpucores",
+				Strategy:   ptr.To(config.Replace),
+				MultiplyBy: "nvidia.com/gpu",
+				Outputs: corev1.ResourceList{
+					"nvidia.com/total-gpucores": resource.MustParse("1"),
+				},
+			},
+			{
+				Input:      "nvidia.com/gpumem",
+				Strategy:   ptr.To(config.Replace),
+				MultiplyBy: "nvidia.com/gpu",
+				Outputs: corev1.ResourceList{
+					"nvidia.com/total-gpumem": resource.MustParse("1"),
+				},
+			},
+		}
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(transformations))
+	})
+
+	ginkgo.BeforeEach(func() {
+		defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+		util.MustCreate(ctx, k8sClient, defaultFlavor)
+
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "resource-transformations-")
+
+		clusterQueue = utiltestingapi.MakeClusterQueue("test-cq").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas("default").
+					Resource("example.com/accelerator-memory", "100Gi").
+					Resource("example.com/credits", "1000").
+					Resource("nvidia.com/gpu", "50").
+					Resource("nvidia.com/total-gpucores", "1000").
+					Resource("nvidia.com/total-gpumem", "102400").
+					Obj(),
+			).Obj()
+		util.MustCreate(ctx, k8sClient, clusterQueue)
+
+		localQueue = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+		util.MustCreate(ctx, k8sClient, localQueue)
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.It("should transform resources with MultiplyBy", func() {
+		ginkgo.By("Creating a workload with vgpu resources")
+		wl := utiltestingapi.MakeWorkload("multiply-wl", ns.Name).
+			Queue(kueue.LocalQueueName(localQueue.Name)).
+			Request("nvidia.com/gpu", "2").
+			Request("nvidia.com/gpucores", "20").
+			Request("nvidia.com/gpumem", "1024").
+			Obj()
+		util.MustCreate(ctx, k8sClient, wl)
+
+		ginkgo.By("Waiting for workload to be admitted")
+		util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+
+		wlLookupKey := client.ObjectKeyFromObject(wl)
+		createdWorkload := &kueue.Workload{}
+
+		ginkgo.By("Verifying workload is created but not admitted yet", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Verifying MultiplyBy transformation")
+		gomega.Expect(createdWorkload.Status.Admission).NotTo(gomega.BeNil())
+		resourceUsage := createdWorkload.Status.Admission.PodSetAssignments[0].ResourceUsage
+
+		// Original resources should be replaced
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("nvidia.com/gpucores")))
+		gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("nvidia.com/gpumem")))
+
+		// Transformed resources: vgpucores * vgpu = 20 * 2 = 40
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceName("nvidia.com/total-gpucores")))
+		gomega.Expect(resourceUsage["nvidia.com/total-gpucores"]).To(gomega.Equal(resource.MustParse("40")))
+
+		// Transformed resources: vgpumem * vgpu = 1024 * 2 = 2048
+		gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceName("nvidia.com/total-gpumem")))
+		gomega.Expect(resourceUsage["nvidia.com/total-gpumem"]).To(gomega.Equal(resource.MustParse("2048")))
```

  I agree with you.
