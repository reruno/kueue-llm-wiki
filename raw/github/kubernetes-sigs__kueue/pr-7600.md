# PR #7600: Introduce Retry mechanism to PATCH client

**Summary**: Introduce Retry mechanism to PATCH client

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7600

**Last updated**: 2025-11-26T12:05:45Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `feature/introduce-retry-mechanism-to-patch-client-2` → `main`
- **Created**: 2025-11-11T08:29:40Z
- **Updated**: 2025-11-26T12:05:45Z
- **Merged**: 2025-11-26T12:00:36Z
- **Closed**: 2025-11-26T12:00:36Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mszadkow](https://github.com/mszadkow), [@gabesaba](https://github.com/gabesaba), [@IrvingMg](https://github.com/IrvingMg), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 5   **+266 / -97**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Introduce Retry mechanism to PATCH client

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #6992

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-11-11T08:29:47Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 73f8290e55067cbf984a8bb8782c534972f3d3d8 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6925de77138d2e0008ea364e |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-11T10:06:57Z

/cc @IrvingMg @mszadkow

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-19T10:26:47Z

Before we introduce retry mechanism to workloads I want to solve this problem first: https://github.com/kubernetes-sigs/kueue/issues/7753

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-25T17:40:22Z

LGTM, but please run all integration and e2e tests with set as true for `WorkloadRequestUseMergePatch` https://github.com/kubernetes-sigs/kueue/blob/2546f4f3ef20c2436399163e6bba952c144412ec/pkg/features/kube_features.go#L340

Use another experiemental PR, not to risk merging that change.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-26T11:10:49Z

LGTM label has been added.  <details>Git tree hash: 3c4c97b1b49acb1e01700c7521aca5b0aaed2271</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-26T11:13:47Z

> Use another experiemental PR, not to risk merging that change.

Tested in https://github.com/kubernetes-sigs/kueue/pull/7891 (no related issue).

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-26T11:19:24Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7600#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7600#pullrequestreview-3510408736" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7600#pullrequestreview-3510436095" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-17T13:20:26Z

- **pkg/scheduler/scheduler.go:646** by [@mimowo](https://github.com/mimowo) — 2025-11-17T13:20:26Z
```diff
@@ -612,50 +612,50 @@ func updateAssignmentForTAS(snapshot *schdcache.Snapshot, cq *schdcache.ClusterQ
 // assuming it in the cache.
 func (s *Scheduler) admit(ctx context.Context, e *entry, cq *schdcache.ClusterQueueSnapshot) error {
 	log := ctrl.LoggerFrom(ctx)
-	newWorkload := e.Obj.DeepCopy()
-	origWorkload := e.Obj.DeepCopy()
 	admission := &kueue.Admission{
 		ClusterQueue:      e.ClusterQueue,
 		PodSetAssignments: e.assignment.ToAPI(),
 	}
 
-	workload.SetQuotaReservation(newWorkload, admission, s.clock)
-	if workload.HasAllChecks(newWorkload, workload.AdmissionChecksForWorkload(log, newWorkload, cq.AdmissionChecks, schdcache.AllFlavors(cq.ResourceGroups))) {
-		// sync Admitted, ignore the result since an API update is always done.
-		_ = workload.SyncAdmittedCondition(newWorkload, s.clock.Now())
-	}
-	if err := s.cache.AssumeWorkload(log, newWorkload); err != nil {
-		return err
-	}
-	e.status = assumed
-	log.V(2).Info("Workload assumed in the cache")
+	wl := e.Obj.DeepCopy()
 
-	if afs.Enabled(s.admissionFairSharing) {
-		s.updateEntryPenalty(log, e, add)
+	err := workload.PatchAdmissionStatus(ctx, s.client, wl, s.clock, func() (bool, error) {
+		workload.SetQuotaReservation(wl, admission, s.clock)
+		if workload.HasAllChecks(wl, workload.AdmissionChecksForWorkload(log, wl, cq.AdmissionChecks, schdcache.AllFlavors(cq.ResourceGroups))) {
+			// sync Admitted, ignore the result since an API update is always done.
+			_ = workload.SyncAdmittedCondition(wl, s.clock.Now())
+		}
+		if err := s.cache.AssumeWorkload(log, wl); err != nil {
+			return false, err
+		}
+		e.status = assumed
+		log.V(2).Info("Workload assumed in the cache")
 
-		// Trigger LocalQueue reconciler to apply any pending penalties
-		s.queues.NotifyWorkloadUpdateWatchers(e.Obj, newWorkload)
-	}
+		if afs.Enabled(s.admissionFairSharing) {
+			s.updateEntryPenalty(log, e, add)
 
-	if features.Enabled(features.TopologyAwareScheduling) && workload.HasUnhealthyNodes(e.Obj) {
-		log.V(5).Info("Clearing the topology assignment recovery field from the workload status after successful recovery")
-		newWorkload.Status.UnhealthyNodes = nil
+			// Trigger LocalQueue reconciler to apply any pending penalties
+			s.queues.NotifyWorkloadUpdateWatchers(e.Obj, wl)
+		}
+
+		if features.Enabled(features.TopologyAwareScheduling) && workload.HasUnhealthyNodes(e.Obj) {
+			log.V(5).Info("Clearing the topology assignment recovery field from the workload status after successful recovery")
+			wl.Status.UnhealthyNodes = nil
+		}
+		return true, nil
+	}, workload.WithLooseOnApply())
+	if err == nil {
+		// Record metrics and events for quota reservation and admission
+		s.recordWorkloadAdmissionMetrics(wl, e.Obj, admission)
+
+		log.V(2).Info("Workload successfully admitted and assigned flavors", "assignments", admission.PodSetAssignments)
+		return nil
 	}
 
 	s.admissionRoutineWrapper.Run(func() {
-		err := workload.PatchAdmissionStatus(ctx, s.client, origWorkload, s.clock, func() (*kueue.Workload, bool, error) {
```

  /hold
  We should not be moving the patch outside of the goroutine for the performance reasons

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-17T15:13:06Z

- **pkg/scheduler/scheduler.go:646** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-17T15:13:06Z
```diff
@@ -612,50 +612,50 @@ func updateAssignmentForTAS(snapshot *schdcache.Snapshot, cq *schdcache.ClusterQ
 // assuming it in the cache.
 func (s *Scheduler) admit(ctx context.Context, e *entry, cq *schdcache.ClusterQueueSnapshot) error {
 	log := ctrl.LoggerFrom(ctx)
-	newWorkload := e.Obj.DeepCopy()
-	origWorkload := e.Obj.DeepCopy()
 	admission := &kueue.Admission{
 		ClusterQueue:      e.ClusterQueue,
 		PodSetAssignments: e.assignment.ToAPI(),
 	}
 
-	workload.SetQuotaReservation(newWorkload, admission, s.clock)
-	if workload.HasAllChecks(newWorkload, workload.AdmissionChecksForWorkload(log, newWorkload, cq.AdmissionChecks, schdcache.AllFlavors(cq.ResourceGroups))) {
-		// sync Admitted, ignore the result since an API update is always done.
-		_ = workload.SyncAdmittedCondition(newWorkload, s.clock.Now())
-	}
-	if err := s.cache.AssumeWorkload(log, newWorkload); err != nil {
-		return err
-	}
-	e.status = assumed
-	log.V(2).Info("Workload assumed in the cache")
+	wl := e.Obj.DeepCopy()
 
-	if afs.Enabled(s.admissionFairSharing) {
-		s.updateEntryPenalty(log, e, add)
+	err := workload.PatchAdmissionStatus(ctx, s.client, wl, s.clock, func() (bool, error) {
+		workload.SetQuotaReservation(wl, admission, s.clock)
+		if workload.HasAllChecks(wl, workload.AdmissionChecksForWorkload(log, wl, cq.AdmissionChecks, schdcache.AllFlavors(cq.ResourceGroups))) {
+			// sync Admitted, ignore the result since an API update is always done.
+			_ = workload.SyncAdmittedCondition(wl, s.clock.Now())
+		}
+		if err := s.cache.AssumeWorkload(log, wl); err != nil {
+			return false, err
+		}
+		e.status = assumed
+		log.V(2).Info("Workload assumed in the cache")
 
-		// Trigger LocalQueue reconciler to apply any pending penalties
-		s.queues.NotifyWorkloadUpdateWatchers(e.Obj, newWorkload)
-	}
+		if afs.Enabled(s.admissionFairSharing) {
+			s.updateEntryPenalty(log, e, add)
 
-	if features.Enabled(features.TopologyAwareScheduling) && workload.HasUnhealthyNodes(e.Obj) {
-		log.V(5).Info("Clearing the topology assignment recovery field from the workload status after successful recovery")
-		newWorkload.Status.UnhealthyNodes = nil
+			// Trigger LocalQueue reconciler to apply any pending penalties
+			s.queues.NotifyWorkloadUpdateWatchers(e.Obj, wl)
+		}
+
+		if features.Enabled(features.TopologyAwareScheduling) && workload.HasUnhealthyNodes(e.Obj) {
+			log.V(5).Info("Clearing the topology assignment recovery field from the workload status after successful recovery")
+			wl.Status.UnhealthyNodes = nil
+		}
+		return true, nil
+	}, workload.WithLooseOnApply())
+	if err == nil {
+		// Record metrics and events for quota reservation and admission
+		s.recordWorkloadAdmissionMetrics(wl, e.Obj, admission)
+
+		log.V(2).Info("Workload successfully admitted and assigned flavors", "assignments", admission.PodSetAssignments)
+		return nil
 	}
 
 	s.admissionRoutineWrapper.Run(func() {
-		err := workload.PatchAdmissionStatus(ctx, s.client, origWorkload, s.clock, func() (*kueue.Workload, bool, error) {
```

  Otherwise we can't use retry mechanism here with current logic.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-25T15:17:07Z

- **pkg/util/client/client.go:155** by [@mimowo](https://github.com/mimowo) — 2025-11-25T15:17:07Z
```diff
@@ -146,12 +147,22 @@ func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update
 // It returns an error if the patch could not be applied.
 type patchFunc func(patch client.Patch) error
 
-func patchCommon(obj client.Object, updateFn UpdateFunc, patchFn patchFunc, options ...PatchOption) error {
+func patchWithRetry(ctx context.Context, c client.Client, obj client.Object, updateFn UpdateFunc, patchFn patchFunc, options ...PatchOption) error {
 	opts := DefaultPatchOptions()
 	for _, opt := range options {
 		opt(opts)
 	}
-	return executePatch(obj, opts, updateFn, patchFn)
+	fetch := false
```

  As part of the effort I would like to extend this test: https://github.com/kubernetes-sigs/kueue/blob/9e42c79320a853122908200bae1364db838957ad/pkg/scheduler/scheduler_test.go#L9114

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-25T15:18:36Z

- **pkg/util/client/client.go:150** by [@mimowo](https://github.com/mimowo) — 2025-11-25T15:18:36Z
```diff
@@ -146,12 +147,22 @@ func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update
 // It returns an error if the patch could not be applied.
 type patchFunc func(patch client.Patch) error
 
-func patchCommon(obj client.Object, updateFn UpdateFunc, patchFn patchFunc, options ...PatchOption) error {
+func patchWithRetry(ctx context.Context, c client.Client, obj client.Object, updateFn UpdateFunc, patchFn patchFunc, options ...PatchOption) error {
```

  no need to change the name. I don't think we should be retrying from other places than scheduler. There we have a better retry mechanism that is just to repeat reconcile. I would like to add a new PatchOption like WithRetry, and only use it from scheduler.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-25T15:33:45Z

- **pkg/util/client/client.go:150** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-25T15:33:45Z
```diff
@@ -146,12 +147,22 @@ func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update
 // It returns an error if the patch could not be applied.
 type patchFunc func(patch client.Patch) error
 
-func patchCommon(obj client.Object, updateFn UpdateFunc, patchFn patchFunc, options ...PatchOption) error {
+func patchWithRetry(ctx context.Context, c client.Client, obj client.Object, updateFn UpdateFunc, patchFn patchFunc, options ...PatchOption) error {
```

  Why not other places?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-25T15:35:43Z

- **pkg/util/client/client.go:150** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-25T15:35:43Z
```diff
@@ -146,12 +147,22 @@ func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update
 // It returns an error if the patch could not be applied.
 type patchFunc func(patch client.Patch) error
 
-func patchCommon(obj client.Object, updateFn UpdateFunc, patchFn patchFunc, options ...PatchOption) error {
+func patchWithRetry(ctx context.Context, c client.Client, obj client.Object, updateFn UpdateFunc, patchFn patchFunc, options ...PatchOption) error {
```

  Yeah, you’re probably right — reconcile is a better mechanism.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-25T15:37:07Z

- **pkg/util/client/client.go:150** by [@mimowo](https://github.com/mimowo) — 2025-11-25T15:37:07Z
```diff
@@ -146,12 +147,22 @@ func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update
 // It returns an error if the patch could not be applied.
 type patchFunc func(patch client.Patch) error
 
-func patchCommon(obj client.Object, updateFn UpdateFunc, patchFn patchFunc, options ...PatchOption) error {
+func patchWithRetry(ctx context.Context, c client.Client, obj client.Object, updateFn UpdateFunc, patchFn patchFunc, options ...PatchOption) error {
```

  Because maybe on another Reconcile the controller may take another action. For example, the scheduler may schedule a workload, and then the decision of Workload controller might be different. Delegating retry to reconciler is safer. I think scheduler should be the only exception.
  
  EDIT: it is also what we do for SSA - in case of conflicts from controllers we retry using reconciler mechanism.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-25T15:42:39Z

- **pkg/util/client/client.go:150** by [@mimowo](https://github.com/mimowo) — 2025-11-25T15:42:39Z
```diff
@@ -146,12 +147,22 @@ func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update
 // It returns an error if the patch could not be applied.
 type patchFunc func(patch client.Patch) error
 
-func patchCommon(obj client.Object, updateFn UpdateFunc, patchFn patchFunc, options ...PatchOption) error {
+func patchWithRetry(ctx context.Context, c client.Client, obj client.Object, updateFn UpdateFunc, patchFn patchFunc, options ...PatchOption) error {
```

  Let's call the new option RetryOnConflict rather than simply Retry to make it clear to the reader it is not a generic Retry of any error

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-25T16:03:28Z

- **pkg/util/client/client.go:155** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-25T16:03:28Z
```diff
@@ -146,12 +147,22 @@ func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update
 // It returns an error if the patch could not be applied.
 type patchFunc func(patch client.Patch) error
 
-func patchCommon(obj client.Object, updateFn UpdateFunc, patchFn patchFunc, options ...PatchOption) error {
+func patchWithRetry(ctx context.Context, c client.Client, obj client.Object, updateFn UpdateFunc, patchFn patchFunc, options ...PatchOption) error {
 	opts := DefaultPatchOptions()
 	for _, opt := range options {
 		opt(opts)
 	}
-	return executePatch(obj, opts, updateFn, patchFn)
+	fetch := false
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-25T16:03:57Z

- **pkg/util/client/client.go:150** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-25T16:03:57Z
```diff
@@ -146,12 +147,22 @@ func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update
 // It returns an error if the patch could not be applied.
 type patchFunc func(patch client.Patch) error
 
-func patchCommon(obj client.Object, updateFn UpdateFunc, patchFn patchFunc, options ...PatchOption) error {
+func patchWithRetry(ctx context.Context, c client.Client, obj client.Object, updateFn UpdateFunc, patchFn patchFunc, options ...PatchOption) error {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-25T16:04:58Z

- **pkg/scheduler/scheduler.go:581** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-25T16:04:58Z
```diff
@@ -578,7 +578,7 @@ func (s *Scheduler) getInitialAssignments(log logr.Logger, wl *workload.Info, sn
 func (s *Scheduler) evictWorkloadAfterFailedTASReplacement(ctx context.Context, log logr.Logger, wl *kueue.Workload) error {
 	log.V(3).Info("Evicting workload after failed try to find a node replacement; TASFailedNodeReplacementFailFast enabled")
 	msg := fmt.Sprintf("Workload was evicted as there was no replacement for a failed node: %s", wl.Status.UnhealthyNodes[0].Name)
-	if err := workload.Evict(ctx, s.client, s.recorder, wl, kueue.WorkloadEvictedDueToNodeFailures, msg, "", s.clock); err != nil {
+	if err := workload.Evict(ctx, s.client, s.recorder, wl, kueue.WorkloadEvictedDueToNodeFailures, msg, "", s.clock, workload.EvictWithRetryOnConflict()); err != nil {
```

  Probably we should add `workload.WithLooseOnApply()` here as well.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-25T16:13:29Z

- **pkg/workload/workload.go:1026** by [@mimowo](https://github.com/mimowo) — 2025-11-25T16:13:29Z
```diff
@@ -1020,6 +1021,14 @@ func WithLooseOnApply() PatchAdmissionStatusOption {
 	}
 }
 
+// PatchAdmissionStatusWithRetryOnConflict configures PatchAdmissionStatusOptions to enable retry logic on conflicts.
+// Note: This only works with merge patches.
+func PatchAdmissionStatusWithRetryOnConflict() PatchAdmissionStatusOption {
```

  ```suggestion
  func WithRetryOnConflictForPatch() PatchAdmissionStatusOption {
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-25T16:13:45Z

- **pkg/workload/workload.go:1028** by [@mimowo](https://github.com/mimowo) — 2025-11-25T16:13:45Z
```diff
@@ -1020,6 +1021,14 @@ func WithLooseOnApply() PatchAdmissionStatusOption {
 	}
 }
 
+// PatchAdmissionStatusWithRetryOnConflict configures PatchAdmissionStatusOptions to enable retry logic on conflicts.
+// Note: This only works with merge patches.
+func PatchAdmissionStatusWithRetryOnConflict() PatchAdmissionStatusOption {
+	return func(o *PatchAdmissionStatusOptions) {
+		o.RetryOnConflict = true
```

  ```suggestion
  		o.RetryOnConflictForPatch = true
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-25T16:14:13Z

- **pkg/workload/workload.go:1320** by [@mimowo](https://github.com/mimowo) — 2025-11-25T16:14:13Z
```diff
@@ -1302,6 +1315,14 @@ func WithCustomPrepare(customPrepare func(wl *kueue.Workload)) EvictOption {
 	}
 }
 
+// EvictWithRetryOnConflict sets the RetryOnConflict field in EvictOptions to true, enabling retrials on conflict during eviction.
+// Note: This only works with merge patches.
+func EvictWithRetryOnConflict() EvictOption {
```

  ```suggestion
  func WithRetryOnConflictForPatch() EvictOption {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-25T16:24:35Z

- **pkg/workload/workload.go:1320** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-25T16:24:35Z
```diff
@@ -1302,6 +1315,14 @@ func WithCustomPrepare(customPrepare func(wl *kueue.Workload)) EvictOption {
 	}
 }
 
+// EvictWithRetryOnConflict sets the RetryOnConflict field in EvictOptions to true, enabling retrials on conflict during eviction.
+// Note: This only works with merge patches.
+func EvictWithRetryOnConflict() EvictOption {
```

  We already have this name in this package. That's why I added Evict prefix.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-25T16:28:33Z

- **pkg/workload/workload.go:1320** by [@mimowo](https://github.com/mimowo) — 2025-11-25T16:28:33Z
```diff
@@ -1302,6 +1315,14 @@ func WithCustomPrepare(customPrepare func(wl *kueue.Workload)) EvictOption {
 	}
 }
 
+// EvictWithRetryOnConflict sets the RetryOnConflict field in EvictOptions to true, enabling retrials on conflict during eviction.
+// Note: This only works with merge patches.
+func EvictWithRetryOnConflict() EvictOption {
```

  Wait, but I thought we always do Evict in strict mode anyway, so why do we need the RetryOnConlict?
  
  I think we need RetryOnConflict only when `LooseOnApply=true`, `LooseOnPatch=false`. 
  
  I think using RetryOnConflictForPatch in other scenarios in other combinations will create unnecessary inconsistency between the two mechanisms

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-25T16:29:30Z

- **pkg/workload/workload.go:1320** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-25T16:29:30Z
```diff
@@ -1302,6 +1315,14 @@ func WithCustomPrepare(customPrepare func(wl *kueue.Workload)) EvictOption {
 	}
 }
 
+// EvictWithRetryOnConflict sets the RetryOnConflict field in EvictOptions to true, enabling retrials on conflict during eviction.
+// Note: This only works with merge patches.
+func EvictWithRetryOnConflict() EvictOption {
```

  Yes, but we are using Evict on scheduler as well. https://github.com/kubernetes-sigs/kueue/pull/7600#discussion_r2560570400.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-25T16:38:59Z

- **pkg/scheduler/scheduler.go:581** by [@mimowo](https://github.com/mimowo) — 2025-11-25T16:38:59Z
```diff
@@ -578,7 +578,7 @@ func (s *Scheduler) getInitialAssignments(log logr.Logger, wl *workload.Info, sn
 func (s *Scheduler) evictWorkloadAfterFailedTASReplacement(ctx context.Context, log logr.Logger, wl *kueue.Workload) error {
 	log.V(3).Info("Evicting workload after failed try to find a node replacement; TASFailedNodeReplacementFailFast enabled")
 	msg := fmt.Sprintf("Workload was evicted as there was no replacement for a failed node: %s", wl.Status.UnhealthyNodes[0].Name)
-	if err := workload.Evict(ctx, s.client, s.recorder, wl, kueue.WorkloadEvictedDueToNodeFailures, msg, "", s.clock); err != nil {
+	if err := workload.Evict(ctx, s.client, s.recorder, wl, kueue.WorkloadEvictedDueToNodeFailures, msg, "", s.clock, workload.EvictWithRetryOnConflict()); err != nil {
```

  TBH I don't know, let's don't mix this change with this PR.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-25T16:39:56Z

- **pkg/workload/workload.go:1320** by [@mimowo](https://github.com/mimowo) — 2025-11-25T16:39:55Z
```diff
@@ -1302,6 +1315,14 @@ func WithCustomPrepare(customPrepare func(wl *kueue.Workload)) EvictOption {
 	}
 }
 
+// EvictWithRetryOnConflict sets the RetryOnConflict field in EvictOptions to true, enabling retrials on conflict during eviction.
+// Note: This only works with merge patches.
+func EvictWithRetryOnConflict() EvictOption {
```

  Yes, but it is using strict mode, so for now I don't see a need to introduce the RetryOnConflict. 
  
  We can consider in the future. For now I see the goal of this PR to copy only to `LooseOnApply=true, LooseOnPatch=false.`. I think we could even throw "unsupported" error if the code tries to use RetryOnConlict in other scenario.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-25T16:40:18Z

- **pkg/scheduler/scheduler.go:581** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-25T16:40:18Z
```diff
@@ -578,7 +578,7 @@ func (s *Scheduler) getInitialAssignments(log logr.Logger, wl *workload.Info, sn
 func (s *Scheduler) evictWorkloadAfterFailedTASReplacement(ctx context.Context, log logr.Logger, wl *kueue.Workload) error {
 	log.V(3).Info("Evicting workload after failed try to find a node replacement; TASFailedNodeReplacementFailFast enabled")
 	msg := fmt.Sprintf("Workload was evicted as there was no replacement for a failed node: %s", wl.Status.UnhealthyNodes[0].Name)
-	if err := workload.Evict(ctx, s.client, s.recorder, wl, kueue.WorkloadEvictedDueToNodeFailures, msg, "", s.clock); err != nil {
+	if err := workload.Evict(ctx, s.client, s.recorder, wl, kueue.WorkloadEvictedDueToNodeFailures, msg, "", s.clock, workload.EvictWithRetryOnConflict()); err != nil {
```

  Yes, sure, this is question for follow-up.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-25T16:40:43Z

- **pkg/workload/workload.go:1320** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-25T16:40:43Z
```diff
@@ -1302,6 +1315,14 @@ func WithCustomPrepare(customPrepare func(wl *kueue.Workload)) EvictOption {
 	}
 }
 
+// EvictWithRetryOnConflict sets the RetryOnConflict field in EvictOptions to true, enabling retrials on conflict during eviction.
+// Note: This only works with merge patches.
+func EvictWithRetryOnConflict() EvictOption {
```

  OK. Let me revert it.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-25T16:51:01Z

- **pkg/workload/workload.go:1320** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-25T16:51:01Z
```diff
@@ -1302,6 +1315,14 @@ func WithCustomPrepare(customPrepare func(wl *kueue.Workload)) EvictOption {
 	}
 }
 
+// EvictWithRetryOnConflict sets the RetryOnConflict field in EvictOptions to true, enabling retrials on conflict during eviction.
+// Note: This only works with merge patches.
+func EvictWithRetryOnConflict() EvictOption {
```

  Reverted

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-25T16:51:15Z

- **pkg/workload/workload.go:1028** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-25T16:51:15Z
```diff
@@ -1020,6 +1021,14 @@ func WithLooseOnApply() PatchAdmissionStatusOption {
 	}
 }
 
+// PatchAdmissionStatusWithRetryOnConflict configures PatchAdmissionStatusOptions to enable retry logic on conflicts.
+// Note: This only works with merge patches.
+func PatchAdmissionStatusWithRetryOnConflict() PatchAdmissionStatusOption {
+	return func(o *PatchAdmissionStatusOptions) {
+		o.RetryOnConflict = true
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-25T16:51:21Z

- **pkg/workload/workload.go:1026** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-25T16:51:21Z
```diff
@@ -1020,6 +1021,14 @@ func WithLooseOnApply() PatchAdmissionStatusOption {
 	}
 }
 
+// PatchAdmissionStatusWithRetryOnConflict configures PatchAdmissionStatusOptions to enable retry logic on conflicts.
+// Note: This only works with merge patches.
+func PatchAdmissionStatusWithRetryOnConflict() PatchAdmissionStatusOption {
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-25T17:36:23Z

- **pkg/scheduler/scheduler.go:581** by [@mimowo](https://github.com/mimowo) — 2025-11-25T17:36:23Z
```diff
@@ -578,7 +578,7 @@ func (s *Scheduler) getInitialAssignments(log logr.Logger, wl *workload.Info, sn
 func (s *Scheduler) evictWorkloadAfterFailedTASReplacement(ctx context.Context, log logr.Logger, wl *kueue.Workload) error {
 	log.V(3).Info("Evicting workload after failed try to find a node replacement; TASFailedNodeReplacementFailFast enabled")
 	msg := fmt.Sprintf("Workload was evicted as there was no replacement for a failed node: %s", wl.Status.UnhealthyNodes[0].Name)
-	if err := workload.Evict(ctx, s.client, s.recorder, wl, kueue.WorkloadEvictedDueToNodeFailures, msg, "", s.clock); err != nil {
+	if err := workload.Evict(ctx, s.client, s.recorder, wl, kueue.WorkloadEvictedDueToNodeFailures, msg, "", s.clock, workload.EvictWithRetryOnConflict()); err != nil {
```

  I don't think we change that until we have a case for changing. It is always easier to change string -> loose, but not the other way round.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-25T17:38:40Z

- **pkg/scheduler/scheduler.go:646** by [@mimowo](https://github.com/mimowo) — 2025-11-25T17:38:39Z
```diff
@@ -612,50 +612,50 @@ func updateAssignmentForTAS(snapshot *schdcache.Snapshot, cq *schdcache.ClusterQ
 // assuming it in the cache.
 func (s *Scheduler) admit(ctx context.Context, e *entry, cq *schdcache.ClusterQueueSnapshot) error {
 	log := ctrl.LoggerFrom(ctx)
-	newWorkload := e.Obj.DeepCopy()
-	origWorkload := e.Obj.DeepCopy()
 	admission := &kueue.Admission{
 		ClusterQueue:      e.ClusterQueue,
 		PodSetAssignments: e.assignment.ToAPI(),
 	}
 
-	workload.SetQuotaReservation(newWorkload, admission, s.clock)
-	if workload.HasAllChecks(newWorkload, workload.AdmissionChecksForWorkload(log, newWorkload, cq.AdmissionChecks, schdcache.AllFlavors(cq.ResourceGroups))) {
-		// sync Admitted, ignore the result since an API update is always done.
-		_ = workload.SyncAdmittedCondition(newWorkload, s.clock.Now())
-	}
-	if err := s.cache.AssumeWorkload(log, newWorkload); err != nil {
-		return err
-	}
-	e.status = assumed
-	log.V(2).Info("Workload assumed in the cache")
+	wl := e.Obj.DeepCopy()
 
-	if afs.Enabled(s.admissionFairSharing) {
-		s.updateEntryPenalty(log, e, add)
+	err := workload.PatchAdmissionStatus(ctx, s.client, wl, s.clock, func() (bool, error) {
+		workload.SetQuotaReservation(wl, admission, s.clock)
+		if workload.HasAllChecks(wl, workload.AdmissionChecksForWorkload(log, wl, cq.AdmissionChecks, schdcache.AllFlavors(cq.ResourceGroups))) {
+			// sync Admitted, ignore the result since an API update is always done.
+			_ = workload.SyncAdmittedCondition(wl, s.clock.Now())
+		}
+		if err := s.cache.AssumeWorkload(log, wl); err != nil {
+			return false, err
+		}
+		e.status = assumed
+		log.V(2).Info("Workload assumed in the cache")
 
-		// Trigger LocalQueue reconciler to apply any pending penalties
-		s.queues.NotifyWorkloadUpdateWatchers(e.Obj, newWorkload)
-	}
+		if afs.Enabled(s.admissionFairSharing) {
+			s.updateEntryPenalty(log, e, add)
 
-	if features.Enabled(features.TopologyAwareScheduling) && workload.HasUnhealthyNodes(e.Obj) {
-		log.V(5).Info("Clearing the topology assignment recovery field from the workload status after successful recovery")
-		newWorkload.Status.UnhealthyNodes = nil
+			// Trigger LocalQueue reconciler to apply any pending penalties
+			s.queues.NotifyWorkloadUpdateWatchers(e.Obj, wl)
+		}
+
+		if features.Enabled(features.TopologyAwareScheduling) && workload.HasUnhealthyNodes(e.Obj) {
+			log.V(5).Info("Clearing the topology assignment recovery field from the workload status after successful recovery")
+			wl.Status.UnhealthyNodes = nil
+		}
+		return true, nil
+	}, workload.WithLooseOnApply())
+	if err == nil {
+		// Record metrics and events for quota reservation and admission
+		s.recordWorkloadAdmissionMetrics(wl, e.Obj, admission)
+
+		log.V(2).Info("Workload successfully admitted and assigned flavors", "assignments", admission.PodSetAssignments)
+		return nil
 	}
 
 	s.admissionRoutineWrapper.Run(func() {
-		err := workload.PatchAdmissionStatus(ctx, s.client, origWorkload, s.clock, func() (*kueue.Workload, bool, error) {
```

  /unhold

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-26T11:10:41Z

Thank you 👍 
/lgtm
/approve
I think with this PR we are ready to start recommending the Patch to some early testers, and consider it default mechanism for 0.16

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-11-26T11:19:17Z

Big thanks!
LGTM 💯 

/lgtm
/approve
