# PR #7604: Fix Scheduler when ClusterQueue head has inadmissible workload sticky workload deleted, next workload can admit integration test

**Summary**: Fix Scheduler when ClusterQueue head has inadmissible workload sticky workload deleted, next workload can admit integration test

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7604

**Last updated**: 2025-11-12T17:16:25Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `fix/scheduler-when-cluster-queue-head-has-inadmissible-workload-sticky-workload-deleted-next-workload-can-admit` → `main`
- **Created**: 2025-11-11T12:38:25Z
- **Updated**: 2025-11-12T17:16:25Z
- **Merged**: 2025-11-12T17:09:41Z
- **Closed**: 2025-11-12T17:09:41Z
- **Labels**: `kind/bug`, `lgtm`, `kind/failing-test`, `kind/flake`, `release-note-none`, `approved`, `cncf-cla: yes`, `size/S`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 1   **+13 / -10**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug
/kind failing-test
/kind flake

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Fix Scheduler when ClusterQueue head has inadmissible workload sticky workload deleted, next workload can admit integration test.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #7470

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-11-11T12:38:33Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 7533bcd799d6af46b4de58227ebb72910b97f573 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6914b946d7728b0007428f0f |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-12T14:01:48Z

LGTM label has been added.  <details>Git tree hash: db0874c512b2d923d893b7802d6fa6f3ec52acfc</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-12T14:29:30Z

/hold
https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7604/pull-kueue-test-integration-baseline-main/1988607604368084992
is related for sure

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-12T17:01:19Z

LGTM label has been added.  <details>Git tree hash: 81ec4db361123f8064755bf5d71288ece8b0f9f6</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-12T17:01:21Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7604#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7604#pullrequestreview-3454518853" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-12T17:15:42Z

/cherrypick release-0.14

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-12T17:15:48Z

/cherrypick release-0.13

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-11-12T17:16:18Z

@mbobrovskyi: #7604 failed to apply on top of branch "release-0.14":
```
Applying: Fix Scheduler when ClusterQueue head has inadmissible workload sticky workload deleted, next workload can admit.
Using index info to reconstruct a base tree...
M	test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go
Falling back to patching base and 3-way merge...
Auto-merging test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go
CONFLICT (content): Merge conflict in test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 Fix Scheduler when ClusterQueue head has inadmissible workload sticky workload deleted, next workload can admit.

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7604#issuecomment-3523030663):

>/cherrypick release-0.14
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-11-12T17:16:25Z

@mbobrovskyi: #7604 failed to apply on top of branch "release-0.13":
```
Applying: Fix Scheduler when ClusterQueue head has inadmissible workload sticky workload deleted, next workload can admit.
Using index info to reconstruct a base tree...
M	test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go
Falling back to patching base and 3-way merge...
Auto-merging test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go
CONFLICT (content): Merge conflict in test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 Fix Scheduler when ClusterQueue head has inadmissible workload sticky workload deleted, next workload can admit.

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7604#issuecomment-3523031054):

>/cherrypick release-0.13


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-12T10:11:32Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:977** by [@mimowo](https://github.com/mimowo) — 2025-11-12T10:11:32Z
```diff
@@ -953,34 +955,40 @@ var _ = ginkgo.Describe("Scheduler", ginkgo.Ordered, ginkgo.ContinueOnFailure, f
 
 		ginkgo.It("sticky workload deleted, next workload can admit", func() {
 			ginkgo.By("Creating borrowing workloads in queue2")
-			createWorkload("cq2", "1")
-			createWorkload("cq2", "1")
-			util.ExpectAdmittedWorkloadsTotalMetric(cq2, "", 2)
+			createWorkloadWithPriority("cq2", "2", 0)
+			util.ExpectAdmittedWorkloadsTotalMetric(cq2, "", 1)
 
 			ginkgo.By("Create inadmissible workload in queue1")
 			createWorkloadWithPriority("cq1", "4", 999)
 
 			ginkgo.By("Verify doesn't admit")
-			util.ExpectAdmittedWorkloadsTotalMetric(cq1, "", 0)
+			util.ExpectPendingWorkloadsMetric(cq1, 0, 1)
 
 			ginkgo.By("Create admissible workloads in queue1")
 			stickyWorkload := createWorkloadWithPriority("cq1", "3", 99)
 
+			ginkgo.By("Validate pending workloads")
+			util.ExpectPendingWorkloadsMetric(cq1, 2, 0)
+
 			ginkgo.By("Another admissible workload in queue1")
 			createWorkloadWithPriority("cq1", "3", 0)
 
+			ginkgo.By("Validate pending workloads")
+			util.ExpectPendingWorkloadsMetric(cq1, 3, 0)
```

  /hold
  The test asserts should not expect the inadmissible workload being counted as active, even if that happens for a short while.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-12T12:30:32Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:958** by [@mimowo](https://github.com/mimowo) — 2025-11-12T12:30:28Z
```diff
@@ -953,34 +955,40 @@ var _ = ginkgo.Describe("Scheduler", ginkgo.Ordered, ginkgo.ContinueOnFailure, f
 
 		ginkgo.It("sticky workload deleted, next workload can admit", func() {
 			ginkgo.By("Creating borrowing workloads in queue2")
-			createWorkload("cq2", "1")
-			createWorkload("cq2", "1")
-			util.ExpectAdmittedWorkloadsTotalMetric(cq2, "", 2)
+			createWorkloadWithPriority("cq2", "2", 0)
```

  is this priority change needed to make the test stable?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-12T12:54:05Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:977** by [@mimowo](https://github.com/mimowo) — 2025-11-12T12:54:05Z
```diff
@@ -953,34 +955,40 @@ var _ = ginkgo.Describe("Scheduler", ginkgo.Ordered, ginkgo.ContinueOnFailure, f
 
 		ginkgo.It("sticky workload deleted, next workload can admit", func() {
 			ginkgo.By("Creating borrowing workloads in queue2")
-			createWorkload("cq2", "1")
-			createWorkload("cq2", "1")
-			util.ExpectAdmittedWorkloadsTotalMetric(cq2, "", 2)
+			createWorkloadWithPriority("cq2", "2", 0)
+			util.ExpectAdmittedWorkloadsTotalMetric(cq2, "", 1)
 
 			ginkgo.By("Create inadmissible workload in queue1")
 			createWorkloadWithPriority("cq1", "4", 999)
 
 			ginkgo.By("Verify doesn't admit")
-			util.ExpectAdmittedWorkloadsTotalMetric(cq1, "", 0)
+			util.ExpectPendingWorkloadsMetric(cq1, 0, 1)
 
 			ginkgo.By("Create admissible workloads in queue1")
 			stickyWorkload := createWorkloadWithPriority("cq1", "3", 99)
 
+			ginkgo.By("Validate pending workloads")
+			util.ExpectPendingWorkloadsMetric(cq1, 2, 0)
+
 			ginkgo.By("Another admissible workload in queue1")
 			createWorkloadWithPriority("cq1", "3", 0)
 
+			ginkgo.By("Validate pending workloads")
+			util.ExpectPendingWorkloadsMetric(cq1, 3, 0)
```

  /unhold
  I think I understand, so after the sticky workload is created the inadmissible workload is requeued, but because the head is stick and so the inadmissible workload is not counted as inadmissible but active. this is a weird side effect. 
  
  I'm okish with that but it warrants a comment, otoh I'm thinking it might be easier to follow if we first create the sticky, and only then the inadmissible. then the test does not need to rely on this weird behavior. 
  
  I think ideally we would not move the inadmissible workload to active after sticky creation. but I'm not yet sure, it is not a high prio issue for now anyway

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-12T12:59:07Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:958** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-12T12:59:07Z
```diff
@@ -953,34 +955,40 @@ var _ = ginkgo.Describe("Scheduler", ginkgo.Ordered, ginkgo.ContinueOnFailure, f
 
 		ginkgo.It("sticky workload deleted, next workload can admit", func() {
 			ginkgo.By("Creating borrowing workloads in queue2")
-			createWorkload("cq2", "1")
-			createWorkload("cq2", "1")
-			util.ExpectAdmittedWorkloadsTotalMetric(cq2, "", 2)
+			createWorkloadWithPriority("cq2", "2", 0)
```

  Looks like there’s no need — I tested it with 50 repeats, and it works.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-12T13:50:59Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:977** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-12T13:50:59Z
```diff
@@ -953,34 +955,40 @@ var _ = ginkgo.Describe("Scheduler", ginkgo.Ordered, ginkgo.ContinueOnFailure, f
 
 		ginkgo.It("sticky workload deleted, next workload can admit", func() {
 			ginkgo.By("Creating borrowing workloads in queue2")
-			createWorkload("cq2", "1")
-			createWorkload("cq2", "1")
-			util.ExpectAdmittedWorkloadsTotalMetric(cq2, "", 2)
+			createWorkloadWithPriority("cq2", "2", 0)
+			util.ExpectAdmittedWorkloadsTotalMetric(cq2, "", 1)
 
 			ginkgo.By("Create inadmissible workload in queue1")
 			createWorkloadWithPriority("cq1", "4", 999)
 
 			ginkgo.By("Verify doesn't admit")
-			util.ExpectAdmittedWorkloadsTotalMetric(cq1, "", 0)
+			util.ExpectPendingWorkloadsMetric(cq1, 0, 1)
 
 			ginkgo.By("Create admissible workloads in queue1")
 			stickyWorkload := createWorkloadWithPriority("cq1", "3", 99)
 
+			ginkgo.By("Validate pending workloads")
+			util.ExpectPendingWorkloadsMetric(cq1, 2, 0)
+
 			ginkgo.By("Another admissible workload in queue1")
 			createWorkloadWithPriority("cq1", "3", 0)
 
+			ginkgo.By("Validate pending workloads")
+			util.ExpectPendingWorkloadsMetric(cq1, 3, 0)
```

  > I think ideally we would not move the inadmissible workload to active after sticky creation. but I'm not yet sure, it is not a high prio issue for now anyway
  
  The same issue:
  
  ```
  			ginkgo.By("Create admissible workloads in queue1")
  			stickyWorkload := createWorkloadWithPriority("cq1", "3", 99)
  
  			ginkgo.By("Validate pending workloads")
  			util.ExpectPendingWorkloadsMetric(cq1, 1, 0)
  
  			ginkgo.By("Create inadmissible workload in queue1")
  			createWorkloadWithPriority("cq1", "4", 999)
  
  			ginkgo.By("Verify doesn't admit")
  			util.ExpectPendingWorkloadsMetric(cq1, 2, 0)
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-12T14:00:16Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:977** by [@mimowo](https://github.com/mimowo) — 2025-11-12T14:00:15Z
```diff
@@ -953,34 +955,40 @@ var _ = ginkgo.Describe("Scheduler", ginkgo.Ordered, ginkgo.ContinueOnFailure, f
 
 		ginkgo.It("sticky workload deleted, next workload can admit", func() {
 			ginkgo.By("Creating borrowing workloads in queue2")
-			createWorkload("cq2", "1")
-			createWorkload("cq2", "1")
-			util.ExpectAdmittedWorkloadsTotalMetric(cq2, "", 2)
+			createWorkloadWithPriority("cq2", "2", 0)
+			util.ExpectAdmittedWorkloadsTotalMetric(cq2, "", 1)
 
 			ginkgo.By("Create inadmissible workload in queue1")
 			createWorkloadWithPriority("cq1", "4", 999)
 
 			ginkgo.By("Verify doesn't admit")
-			util.ExpectAdmittedWorkloadsTotalMetric(cq1, "", 0)
+			util.ExpectPendingWorkloadsMetric(cq1, 0, 1)
 
 			ginkgo.By("Create admissible workloads in queue1")
 			stickyWorkload := createWorkloadWithPriority("cq1", "3", 99)
 
+			ginkgo.By("Validate pending workloads")
+			util.ExpectPendingWorkloadsMetric(cq1, 2, 0)
+
 			ginkgo.By("Another admissible workload in queue1")
 			createWorkloadWithPriority("cq1", "3", 0)
 
+			ginkgo.By("Validate pending workloads")
+			util.ExpectPendingWorkloadsMetric(cq1, 3, 0)
```

  I wouldn't call it an issue, yes the inadmissible workload is counted as active, but I think in this order of adding it is more clear to the reader. The second workload is not evaluated despite priority because the head is already taken by the sticky workload.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-12T14:00:43Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:977** by [@mimowo](https://github.com/mimowo) — 2025-11-12T14:00:43Z
```diff
@@ -953,34 +955,40 @@ var _ = ginkgo.Describe("Scheduler", ginkgo.Ordered, ginkgo.ContinueOnFailure, f
 
 		ginkgo.It("sticky workload deleted, next workload can admit", func() {
 			ginkgo.By("Creating borrowing workloads in queue2")
-			createWorkload("cq2", "1")
-			createWorkload("cq2", "1")
-			util.ExpectAdmittedWorkloadsTotalMetric(cq2, "", 2)
+			createWorkloadWithPriority("cq2", "2", 0)
+			util.ExpectAdmittedWorkloadsTotalMetric(cq2, "", 1)
 
 			ginkgo.By("Create inadmissible workload in queue1")
 			createWorkloadWithPriority("cq1", "4", 999)
 
 			ginkgo.By("Verify doesn't admit")
-			util.ExpectAdmittedWorkloadsTotalMetric(cq1, "", 0)
+			util.ExpectPendingWorkloadsMetric(cq1, 0, 1)
 
 			ginkgo.By("Create admissible workloads in queue1")
 			stickyWorkload := createWorkloadWithPriority("cq1", "3", 99)
 
+			ginkgo.By("Validate pending workloads")
+			util.ExpectPendingWorkloadsMetric(cq1, 2, 0)
+
 			ginkgo.By("Another admissible workload in queue1")
 			createWorkloadWithPriority("cq1", "3", 0)
 
+			ginkgo.By("Validate pending workloads")
+			util.ExpectPendingWorkloadsMetric(cq1, 3, 0)
```

  I would propose to just make the comments more explanatory, maybe:
  
  ```
  ginkgo.By("Create admissible workloads in queue1")
  stickyWorkload := createWorkloadWithPriority("cq1", "3", 99)
  
  ginkgo.By("Verify the workload is counted as pending active")
  util.ExpectPendingWorkloadsMetric(cq1, 1, 0)
  
  ginkgo.By("Create inadmissible workload in queue1")
  createWorkloadWithPriority("cq1", "4", 999)
  
  ginkgo.By("Verify the inadmissible workload is also counted as pending active since the head is already taken by the sticky workload")
  util.ExpectPendingWorkloadsMetric(cq1, 2, 0)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-12T14:01:38Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:977** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-12T14:01:38Z
```diff
@@ -953,34 +955,40 @@ var _ = ginkgo.Describe("Scheduler", ginkgo.Ordered, ginkgo.ContinueOnFailure, f
 
 		ginkgo.It("sticky workload deleted, next workload can admit", func() {
 			ginkgo.By("Creating borrowing workloads in queue2")
-			createWorkload("cq2", "1")
-			createWorkload("cq2", "1")
-			util.ExpectAdmittedWorkloadsTotalMetric(cq2, "", 2)
+			createWorkloadWithPriority("cq2", "2", 0)
+			util.ExpectAdmittedWorkloadsTotalMetric(cq2, "", 1)
 
 			ginkgo.By("Create inadmissible workload in queue1")
 			createWorkloadWithPriority("cq1", "4", 999)
 
 			ginkgo.By("Verify doesn't admit")
-			util.ExpectAdmittedWorkloadsTotalMetric(cq1, "", 0)
+			util.ExpectPendingWorkloadsMetric(cq1, 0, 1)
 
 			ginkgo.By("Create admissible workloads in queue1")
 			stickyWorkload := createWorkloadWithPriority("cq1", "3", 99)
 
+			ginkgo.By("Validate pending workloads")
+			util.ExpectPendingWorkloadsMetric(cq1, 2, 0)
+
 			ginkgo.By("Another admissible workload in queue1")
 			createWorkloadWithPriority("cq1", "3", 0)
 
+			ginkgo.By("Validate pending workloads")
+			util.ExpectPendingWorkloadsMetric(cq1, 3, 0)
```

  The inadmissible workload is also counted as pending active since the head is already taken by the sticky workload. I’ve updated the ginkgo.By text accordingly.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-12T14:01:40Z

Thank you 👍 

/lgtm
/approve

/cherrypick release-0.14
/cherrypick release-0.13

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-12T17:01:13Z

/lgtm
/approve
/unhold
/cherrypick release-0.14
/cherrypick release-0.13
