# PR #7613: Add PropagateBatchJobLabelsToWorkload feature

**Summary**: Add PropagateBatchJobLabelsToWorkload feature

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7613

**Last updated**: 2025-11-17T12:31:41Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk)
- **Branch**: `provreq` → `main`
- **Created**: 2025-11-12T12:15:31Z
- **Updated**: 2025-11-17T12:31:41Z
- **Merged**: 2025-11-17T12:31:39Z
- **Closed**: 2025-11-17T12:31:39Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 5   **+79 / -10**

## Description

/kind bug
/kind feature

#### Which issue(s) this PR fixes:
Fixes #7597

#### Does this PR introduce a user-facing change?
<!--
-->
```release-note
Fix the bug that the kubernetes.io/job-name label was not propagated from the k8s Job to the PodTemplate in
the Workload object, and later to the pod template in the ProvisioningRequest. 

As a consequence the ClusterAutoscaler could not properly resolve pod affinities referring to that label,
via podAffinity.requiredDuringSchedulingIgnoredDuringExecution.labelSelector. For example, 
such pod affinities can be used to request ClusterAutoscaler to provision a single node which is large enough
to accommodate all Pods on a single Node.

We also introduce the PropagateBatchJobLabelsToWorkload feature gate to disable the new behavior in case of 
complications.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-11-12T12:15:38Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 73f96c36c82be58bb8dcaa5bc01d65455b9ba1a7 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/691b101e89d68200083d6bfa |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-17T12:18:35Z

/release-note-edit
```release-note
Fix the bug that the kubernetes.io/job-name label was not propagated from the k8s Job to the PodTemplate in
the Workload object, and later to the pod template in the ProvisioningRequest. 

As a consequence the ClusterAutoscaler could not properly resolve pod affinities referring to that label,
via podAffinity.requiredDuringSchedulingIgnoredDuringExecution.labelSelector. For example, 
such pod affinities can be used to request ClusterAutoscaler to provision a single node which is large enough
to accommodate all Pods on a single Node.

We also introduce the PropagateBatchJobLabelsToWorkload feature gate to disable the new behavior in case of 
complications.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-17T12:18:44Z

/kind bug

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-17T12:19:54Z

/lgtm
/approve
@yaroslava-serdiuk please prepare also the cherry-picks for the label propagation as this is a bug. 
The ./hack/cherry_pick_pull.sh script is helpful for that.
Just decrement the version counter in kube_features when backporting.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-17T12:20:01Z

LGTM label has been added.  <details>Git tree hash: 92ee720faac3b462bcf087fff978bc4947d53c3a</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-17T12:20:01Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7613#issuecomment-3541525100" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7613#" title="Author self-approved">yaroslava-serdiuk</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-12T16:35:00Z

- **pkg/controller/jobs/job/job_controller.go:236** by [@mimowo](https://github.com/mimowo) — 2025-11-12T16:35:00Z
```diff
@@ -233,13 +233,18 @@ var (
 	// the legacy names are no longer defined in the api, only in k/2/apis/batch
 	legacyJobNameLabel       = "job-name"
 	legacyControllerUIDLabel = "controller-uid"
-	ManagedLabels            = []string{legacyJobNameLabel, legacyControllerUIDLabel, batchv1.JobNameLabel, batchv1.ControllerUidLabel}
```

  IIUC if you remove `batchv1.JobNameLabel` from here, then the new code doing `delete(pt.Labels, batchv1.JobNameLabel)` is no-op. I think we should keep the label still in `ManagedLabels` just do the delete. 
  
  Please add unit tests, and an integration test.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2025-11-13T11:15:31Z

- **pkg/controller/jobs/job/job_controller.go:236** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2025-11-13T11:15:31Z
```diff
@@ -233,13 +233,18 @@ var (
 	// the legacy names are no longer defined in the api, only in k/2/apis/batch
 	legacyJobNameLabel       = "job-name"
 	legacyControllerUIDLabel = "controller-uid"
-	ManagedLabels            = []string{legacyJobNameLabel, legacyControllerUIDLabel, batchv1.JobNameLabel, batchv1.ControllerUidLabel}
```

  >if you remove batchv1.JobNameLabel from here, then the new code doing delete(pt.Labels, batchv1.JobNameLabel) is no-op.
  
  ManagedLabels are the list of labels that are removed from PodTemplate. The code is removing the managed labels and if the feature is disabled it removes job-name label as well `delete(pt.Labels, batchv1.JobNameLabel`. 
  
  However we use ManagedLabels in RestorePodSetsInfo, so I updated a code to include the label in the list in case the feature disabled. 
  I added a unit test and updated integration tests for multikueue. I see no reason to add a specific integration test.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-13T12:40:08Z

- **pkg/controller/jobs/job/job_controller.go:236** by [@mimowo](https://github.com/mimowo) — 2025-11-13T12:40:08Z
```diff
@@ -233,13 +233,18 @@ var (
 	// the legacy names are no longer defined in the api, only in k/2/apis/batch
 	legacyJobNameLabel       = "job-name"
 	legacyControllerUIDLabel = "controller-uid"
-	ManagedLabels            = []string{legacyJobNameLabel, legacyControllerUIDLabel, batchv1.JobNameLabel, batchv1.ControllerUidLabel}
```

  > I added a unit test and updated integration tests for multikueue. I see no reason to add a specific integration test.
  
  TBH I don't like extending random tests and overloading with responsibility.
  
  Let's add a dedicated integration test which will mimic the ProvisioningRequest scenario we encountered originally to show the ProvisioningRequest template contains the label.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-13T12:42:12Z

- **pkg/controller/jobs/job/job_controller.go:236** by [@mimowo](https://github.com/mimowo) — 2025-11-13T12:42:12Z
```diff
@@ -233,13 +233,18 @@ var (
 	// the legacy names are no longer defined in the api, only in k/2/apis/batch
 	legacyJobNameLabel       = "job-name"
 	legacyControllerUIDLabel = "controller-uid"
-	ManagedLabels            = []string{legacyJobNameLabel, legacyControllerUIDLabel, batchv1.JobNameLabel, batchv1.ControllerUidLabel}
```

  This looks like the proper test suite: https://github.com/kubernetes-sigs/kueue/blob/fd1b8121a52e6970dd3a4166bb293ae1b3b9cd06/test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go#L1518
  
  Or extend one of them with the assert that the label is propageted to the template.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-13T12:53:50Z

- **test/integration/multikueue/jobs_test.go:311** by [@mimowo](https://github.com/mimowo) — 2025-11-13T12:53:50Z
```diff
@@ -308,6 +308,7 @@ var _ = ginkgo.Describe("MultiKueue", ginkgo.Ordered, ginkgo.ContinueOnFailure,
 
 	ginkgo.It("Should run a job on worker if admitted (ManagedBy)", func() {
 		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.MultiKueueBatchJobWithManagedBy, true)
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.PropagateBatchJobLabelsToWorkload, true)
```

  Revert, as mentioned in https://github.com/kubernetes-sigs/kueue/pull/7613/files#r2523322303, let's have a test in the provReq suite which corresponds to the business use case for the issue
  
  EDIT: btw this is no-op as the feature is enabled by default anyway

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-13T15:14:33Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1680** by [@mimowo](https://github.com/mimowo) — 2025-11-13T15:07:32Z
```diff
@@ -1674,10 +1677,17 @@ var _ = ginkgo.Describe("Provisioning with scheduling", ginkgo.Ordered, ginkgo.C
 			})
 
 			ginkgo.By("await for wl1 to be Admitted", func() {
+				pdTemplate := &corev1.PodTemplate{}
```

  move the new assert to a dedicated ginkgo.by

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1690** by [@mimowo](https://github.com/mimowo) — 2025-11-13T15:09:05Z
```diff
@@ -1674,10 +1677,17 @@ var _ = ginkgo.Describe("Provisioning with scheduling", ginkgo.Ordered, ginkgo.C
 			})
 
 			ginkgo.By("await for wl1 to be Admitted", func() {
+				pdTemplate := &corev1.PodTemplate{}
+				templateKey := types.NamespacedName{
+					Namespace: wl1Key.Namespace,
+					Name:      createdRequest.Spec.PodSets[0].PodTemplateRef.Name,
+				}
 				gomega.Eventually(func(g gomega.Gomega) {
 					gomega.Expect(k8sClient.Get(ctx, wl1Key, &wlObj)).Should(gomega.Succeed())
 					g.Expect(workload.Status(&wlObj)).To(gomega.Equal(workload.StatusAdmitted))
+					g.Expect(k8sClient.Get(ctx, templateKey, pdTemplate)).Should(gomega.Succeed())
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(pdTemplate.ObjectMeta.GetLabels()).To(gomega.BeComparableTo(map[string]string{constants.ManagedByKueueLabelKey: constants.ManagedByKueueLabelValue}))
```

  nit, just check if the label is present. we can ignore checking other labels. I think gingko has some helpers like HasKeyWithValue

- **pkg/controller/jobs/job/job_controller.go:236** by [@mimowo](https://github.com/mimowo) — 2025-11-13T15:14:27Z
```diff
@@ -233,11 +233,14 @@ var (
 	// the legacy names are no longer defined in the api, only in k/2/apis/batch
 	legacyJobNameLabel       = "job-name"
 	legacyControllerUIDLabel = "controller-uid"
-	ManagedLabels            = []string{legacyJobNameLabel, legacyControllerUIDLabel, batchv1.JobNameLabel, batchv1.ControllerUidLabel}
```

  why the label is removed from ManagedLabels, this label is managed by the Kubernetes job controller, we just don't want to copy the label. when creating PodSets. Maybe we should rename the field like SkippedLabels if we want to remove this label from the set

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-13T16:13:19Z

- **pkg/controller/jobs/job/job_controller.go:236** by [@mimowo](https://github.com/mimowo) — 2025-11-13T16:13:19Z
```diff
@@ -233,11 +233,14 @@ var (
 	// the legacy names are no longer defined in the api, only in k/2/apis/batch
 	legacyJobNameLabel       = "job-name"
 	legacyControllerUIDLabel = "controller-uid"
-	ManagedLabels            = []string{legacyJobNameLabel, legacyControllerUIDLabel, batchv1.JobNameLabel, batchv1.ControllerUidLabel}
```

  TBH, I think it is much easier to reason about the code if we keep ManagedLabels as is, just skip the propagation, like:
  
  ```golang
  for _, managedLabel := range ManagedLabels {
    if features.Enabled(features.PropagateBatchJobLabelsToWorkload) {
      continue
    }
    delete(pt.Labels, managedLabel)
  }
  ```
  This code avoids using negation of the condition like `!features.Enabled(features.PropagateBatchJobLabelsToWorkload)`, and does not change the meaning of  ManagedLabels. Then we also can revert this line: https://github.com/kubernetes-sigs/kueue/pull/7613/files#diff-f278bdcf60c86f048f29eeef3fa2ee16447d577a7d3d6d0a3dce33626ffed260R252

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-13T16:17:28Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1690** by [@mimowo](https://github.com/mimowo) — 2025-11-13T16:17:28Z
```diff
@@ -1674,10 +1677,17 @@ var _ = ginkgo.Describe("Provisioning with scheduling", ginkgo.Ordered, ginkgo.C
 			})
 
 			ginkgo.By("await for wl1 to be Admitted", func() {
+				pdTemplate := &corev1.PodTemplate{}
+				templateKey := types.NamespacedName{
+					Namespace: wl1Key.Namespace,
+					Name:      createdRequest.Spec.PodSets[0].PodTemplateRef.Name,
+				}
 				gomega.Eventually(func(g gomega.Gomega) {
 					gomega.Expect(k8sClient.Get(ctx, wl1Key, &wlObj)).Should(gomega.Succeed())
 					g.Expect(workload.Status(&wlObj)).To(gomega.Equal(workload.StatusAdmitted))
+					g.Expect(k8sClient.Get(ctx, templateKey, pdTemplate)).Should(gomega.Succeed())
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(pdTemplate.ObjectMeta.GetLabels()).To(gomega.BeComparableTo(map[string]string{constants.ManagedByKueueLabelKey: constants.ManagedByKueueLabelValue}))
```

  wait, the assert is also on the wrong label "ManagedByKueueLabelKey", not the label in question. This suggests the label in question isn't really propagated all the way
  
  Found it:
  ```golang
  gomega.Expect(pdTemplate.ObjectMeta.GetLabels()).Should(gomega.HaveKeyWithValue(batchv1.JobNameLabel, batchv1.JobNameLabel))
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-14T18:32:16Z

- **pkg/controller/jobs/job/job_controller.go:236** by [@mimowo](https://github.com/mimowo) — 2025-11-14T18:32:16Z
```diff
@@ -233,11 +233,15 @@ var (
 	// the legacy names are no longer defined in the api, only in k/2/apis/batch
 	legacyJobNameLabel       = "job-name"
 	legacyControllerUIDLabel = "controller-uid"
-	ManagedLabels            = []string{legacyJobNameLabel, legacyControllerUIDLabel, batchv1.JobNameLabel, batchv1.ControllerUidLabel}
```

  nit, revert

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-14T18:32:23Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1688** by [@mimowo](https://github.com/mimowo) — 2025-11-14T18:32:23Z
```diff
@@ -1677,6 +1685,7 @@ var _ = ginkgo.Describe("Provisioning with scheduling", ginkgo.Ordered, ginkgo.C
 				gomega.Eventually(func(g gomega.Gomega) {
 					gomega.Expect(k8sClient.Get(ctx, wl1Key, &wlObj)).Should(gomega.Succeed())
 					g.Expect(workload.Status(&wlObj)).To(gomega.Equal(workload.StatusAdmitted))
+
```

  nit, revert

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-14T18:32:50Z

LGTM, just nits

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2025-11-17T11:48:42Z

- **pkg/controller/jobs/job/job_controller.go:236** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2025-11-17T11:48:42Z
```diff
@@ -233,11 +233,15 @@ var (
 	// the legacy names are no longer defined in the api, only in k/2/apis/batch
 	legacyJobNameLabel       = "job-name"
 	legacyControllerUIDLabel = "controller-uid"
-	ManagedLabels            = []string{legacyJobNameLabel, legacyControllerUIDLabel, batchv1.JobNameLabel, batchv1.ControllerUidLabel}
```

  With exported function CleanLabels we don't need to export managedLabels

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-17T11:49:58Z

- **pkg/controller/jobs/job/job_controller.go:236** by [@mimowo](https://github.com/mimowo) — 2025-11-17T11:49:58Z
```diff
@@ -233,11 +233,15 @@ var (
 	// the legacy names are no longer defined in the api, only in k/2/apis/batch
 	legacyJobNameLabel       = "job-name"
 	legacyControllerUIDLabel = "controller-uid"
-	ManagedLabels            = []string{legacyJobNameLabel, legacyControllerUIDLabel, batchv1.JobNameLabel, batchv1.ControllerUidLabel}
```

  sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-17T11:51:00Z

- **pkg/controller/jobs/job/job_controller.go:236** by [@mimowo](https://github.com/mimowo) — 2025-11-17T11:50:59Z
```diff
@@ -233,11 +233,15 @@ var (
 	// the legacy names are no longer defined in the api, only in k/2/apis/batch
 	legacyJobNameLabel       = "job-name"
 	legacyControllerUIDLabel = "controller-uid"
-	ManagedLabels            = []string{legacyJobNameLabel, legacyControllerUIDLabel, batchv1.JobNameLabel, batchv1.ControllerUidLabel}
```

  also CleanLabels is only used from the local package, so we can change to `cleanLabels`

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2025-11-17T12:06:45Z

- **pkg/controller/jobs/job/job_controller.go:236** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2025-11-17T12:06:45Z
```diff
@@ -233,11 +233,15 @@ var (
 	// the legacy names are no longer defined in the api, only in k/2/apis/batch
 	legacyJobNameLabel       = "job-name"
 	legacyControllerUIDLabel = "controller-uid"
-	ManagedLabels            = []string{legacyJobNameLabel, legacyControllerUIDLabel, batchv1.JobNameLabel, batchv1.ControllerUidLabel}
```

  Oh, right. Updated.
