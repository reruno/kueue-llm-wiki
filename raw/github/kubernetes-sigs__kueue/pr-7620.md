# PR #7620: feat(KEP-3258): implement delayed admission check retries

**Summary**: feat(KEP-3258): implement delayed admission check retries

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7620

**Last updated**: 2025-11-28T08:58:21Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@sohankunkerkar](https://github.com/sohankunkerkar)
- **Branch**: `pr-7370` → `main`
- **Created**: 2025-11-12T17:13:09Z
- **Updated**: 2025-11-28T08:58:21Z
- **Merged**: 2025-11-17T10:45:39Z
- **Closed**: 2025-11-17T10:45:39Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 24   **+1249 / -97**

## Description

Implements delayed retry mechanism for admission checks to prevent overwhelming external controllers and reduce control plane churn.

Problem:
Previously, when admission checks transitioned to Retry state, Kueue would immediately evict workloads and requeue them, causing excessive load on admission check controllers and unnecessary API server churn, particularly when retry conditions persisted predictably.

Solution:
This implementation adds two new fields to AdmissionCheckState:
- requeueAfterSeconds: Specifies minimum wait time before retry
- retryCount: Tracks retry attempts per admission check

Key Changes:

API Changes
- Added requeueAfterSeconds and retryCount fields to AdmissionCheckState
- Added +kubebuilder:validation:Minimum=0 to both fields (v1beta1 and v1beta2)

Controller Changes
- Auto-increments retryCount on transition to Retry state
- Calculates maximum retry time across all admission checks
- Updates workload.status.requeueState.requeueAt with the maximum delay
- Workload controller respects delayed retry times
- RequeueAfterSeconds values persist across evictions
- Refactored backoff calculation to use wait.NewBackoff() pattern

Behavior:
When multiple admission checks specify different delays, Kueue uses the maximum delay across all checks. Workloads are evicted immediately to release quota, but admission checks maintain their requeueAfterSeconds values, preventing race conditions where fast-responding checks could block slower ones from registering their delays.

Refs: KEP-3258
https://github.com/kubernetes-sigs/kueue/blob/main/keps/3258-delayed-admission-check-retries/README.md

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
/kind api-change

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes https://github.com/kubernetes-sigs/kueue/issues/3258

#### Special notes for your reviewer:

After discussing with @mimowo, taking over https://github.com/kubernetes-sigs/kueue/pull/7370 as the original author is on PTO.


#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
AdmissionChecks: introduce new optional fields in the workload status for admission checks to control the delay by 
external and internal admission check controllers:
* requeueAfterSeconds: specifies minimum wait time before retry
* retryCount: Tracks retry attempts per admission check
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-11-12T17:13:40Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 5a7d9a4cff38b802ca8beef379501651bc9b9753 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6918c5c31e1de600082b5d4d |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-17T10:19:03Z

LGTM label has been added.  <details>Git tree hash: 8081cc7c81df3b68b2123ffc41c4a3c9b508d39d</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-17T10:19:04Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7620#pullrequestreview-3472007319" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7620#" title="Author self-approved">sohankunkerkar</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-28T08:58:18Z

/remove-kind api-change
/kind feature

/release-note-edit
```release-note
AdmissionChecks: introduce new optional fields in the workload status for admission checks to control the delay by 
external and internal admission check controllers:
* requeueAfterSeconds: specifies minimum wait time before retry
* retryCount: Tracks retry attempts per admission check
```

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-13T19:31:11Z

LGTM overall, thank you 👍

- **pkg/controller/core/workload_controller.go:205** by [@mimowo](https://github.com/mimowo) — 2025-11-13T19:16:20Z
```diff
@@ -194,6 +194,25 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		return ctrl.Result{}, nil
 	}
 
+	maxTime := workload.GetMaxRetryTime(&wl)
+	if maxTime.After(r.clock.Now()) {
+		var updated bool
+		err := workload.PatchAdmissionStatus(ctx, r.client, &wl, r.clock, func() (*kueue.Workload, bool, error) {
+			if wl.Status.RequeueState == nil {
+				wl.Status.RequeueState = &kueue.RequeueState{}
+			}
+			log.V(2).Info("At least one admission check set a retry time", "maxTime", maxTime, "requeueState.RequeueAt", wl.Status.RequeueState.RequeueAt)
+			if updatedRequeueAt := workload.SetRequeueState(&wl, maxTime, false); !updatedRequeueAt {
```

  To improve readability, would it be feasible to move out the check if there is any update needed outside the PatchAdmissionStatus helper, I think even up to `if maxTime.After(r.clock.Now()) {`, replace  with more generic 
  ```golang
  if requeueAt := workload.NeedsRequeueAtUpdate(&wl); requeueAt != nil {
  ```
  That could internally call GetMaxRetryTime. Shifting the code to helper would also offload the complicated code in workload_controller.
  
  Then we don't need to track with `updated`.

- **pkg/controller/core/workload_controller.go:213** by [@mimowo](https://github.com/mimowo) — 2025-11-13T19:17:25Z
```diff
@@ -194,6 +194,25 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		return ctrl.Result{}, nil
 	}
 
+	maxTime := workload.GetMaxRetryTime(&wl)
+	if maxTime.After(r.clock.Now()) {
+		var updated bool
+		err := workload.PatchAdmissionStatus(ctx, r.client, &wl, r.clock, func() (*kueue.Workload, bool, error) {
+			if wl.Status.RequeueState == nil {
+				wl.Status.RequeueState = &kueue.RequeueState{}
+			}
+			log.V(2).Info("At least one admission check set a retry time", "maxTime", maxTime, "requeueState.RequeueAt", wl.Status.RequeueState.RequeueAt)
+			if updatedRequeueAt := workload.SetRequeueState(&wl, maxTime, false); !updatedRequeueAt {
+				log.V(3).Info("Ignoring AC retry time as it's before the current RequeueState time", "requeueState.RequeueAt", wl.Status.RequeueState.RequeueAt, "maxTime", maxTime)
+				return nil, false, nil
+			}
+			updated = true
+			return &wl, true, nil
+		})
+		if updated {
+			return reconcile.Result{Requeue: true}, client.IgnoreNotFound(err)
```

  ```suggestion
  			return reconcile.Result{}, client.IgnoreNotFound(err)
  ```
  nit, if we update workload object then the requeue for the controller is scheduled anyway

- **pkg/workload/workload.go:71** by [@mimowo](https://github.com/mimowo) — 2025-11-13T19:18:35Z
```diff
@@ -61,16 +61,14 @@ const (
 	StatusFinished      = "finished"
 )
 
-var (
-	admissionManagedConditions = []string{
-		kueue.WorkloadQuotaReserved,
-		kueue.WorkloadEvicted,
-		kueue.WorkloadAdmitted,
-		kueue.WorkloadPreempted,
-		kueue.WorkloadRequeued,
-		kueue.WorkloadDeactivationTarget,
-	}
-)
+var admissionManagedConditions = []string{
+	kueue.WorkloadQuotaReserved,
+	kueue.WorkloadEvicted,
+	kueue.WorkloadAdmitted,
+	kueue.WorkloadPreempted,
+	kueue.WorkloadRequeued,
+	kueue.WorkloadDeactivationTarget,
+}
```

  nit, revert to avoid distractions

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:139** by [@mimowo](https://github.com/mimowo) — 2025-11-13T19:22:32Z
```diff
@@ -135,7 +136,8 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 				).
 				Annotations(map[string]string{
 					"provreq.kueue.x-k8s.io/ValidUntilSeconds": "0",
-					"invalid-provreq-prefix/Foo":               "Bar"}).
+					"invalid-provreq-prefix/Foo":               "Bar",
```

  nit, revert unnecessary changes

- **pkg/controller/core/workload_controller_test.go:384** by [@mimowo](https://github.com/mimowo) — 2025-11-13T19:23:06Z
```diff
@@ -383,18 +381,16 @@ func TestSyncCheckStates(t *testing.T) {
 	}
 }
 
-var (
-	workloadCmpOpts = cmp.Options{
-		cmpopts.EquateEmpty(),
-		cmpopts.IgnoreFields(
-			kueue.Workload{}, "TypeMeta", "ObjectMeta.ResourceVersion",
-		),
-		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
-		cmpopts.IgnoreFields(kueue.AdmissionCheckState{}, "LastTransitionTime"),
-		cmpopts.IgnoreFields(kueue.RequeueState{}, "RequeueAt"),
-		cmpopts.SortSlices(func(a, b metav1.Condition) bool { return a.Type < b.Type }),
-	}
-)
+var workloadCmpOpts = cmp.Options{
```

  nit, revert, feel free to submit separete PR

- **pkg/workload/workload.go:568** by [@mimowo](https://github.com/mimowo) — 2025-11-13T19:23:18Z
```diff
@@ -567,7 +565,8 @@ func UpdateStatus(ctx context.Context,
 	conditionStatus metav1.ConditionStatus,
 	reason, message string,
 	managerPrefix string,
-	clock clock.Clock) error {
+	clock clock.Clock,
```

  nit, revert to avoid distractions, feel free to submit separate PR

- **test/integration/singlecluster/scheduler/podsready/scheduler_test.go:794** by [@mimowo](https://github.com/mimowo) — 2025-11-13T19:27:52Z
```diff
@@ -780,15 +780,21 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
 				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, standaloneClusterQ.Name, wl1)
 				// To avoid flakiness, we don't verify if the workload has a QuotaReserved=false with pending reason here.
 			})
+			ginkgo.By("waiting for the first workload to be evicted again (second eviction)", func() {
+				util.AwaitWorkloadEvictionByPodsReadyTimeout(ctx, k8sClient, client.ObjectKeyFromObject(wl1), podsReadyTimeout)
+				util.FinishEvictionForWorkloads(ctx, k8sClient, wl1)
+			})
```

  I have a question why adding this block. 
  
  I fully agree it is interesting to test after the second eviction, but it overwrites the previous asserts.
  So, I'm wondering it might be better to assert on requeueAt before this ginkgo.By block and after.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2025-11-13T20:43:04Z

- **test/integration/singlecluster/scheduler/podsready/scheduler_test.go:794** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-11-13T20:43:04Z
```diff
@@ -780,15 +780,21 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
 				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, standaloneClusterQ.Name, wl1)
 				// To avoid flakiness, we don't verify if the workload has a QuotaReserved=false with pending reason here.
 			})
+			ginkgo.By("waiting for the first workload to be evicted again (second eviction)", func() {
+				util.AwaitWorkloadEvictionByPodsReadyTimeout(ctx, k8sClient, client.ObjectKeyFromObject(wl1), podsReadyTimeout)
+				util.FinishEvictionForWorkloads(ctx, k8sClient, wl1)
+			})
```

  Thanks for the feedback! You're absolutely right about the assertion issue. I added the second eviction block to make this test behavior deterministic because in non-blocking mode, after the first eviction and back-off completion, the workload gets re-admitted automatically. However, since it still doesn't become ready (no pods are actually started in this test), it will timeout again and get evicted a second time. Without explicitly waiting for and finishing this second eviction, the test was becoming flaky because the timing was unpredictable.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2025-11-13T23:19:49Z

- **pkg/controller/core/workload_controller.go:205** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-11-13T23:19:49Z
```diff
@@ -194,6 +194,25 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		return ctrl.Result{}, nil
 	}
 
+	maxTime := workload.GetMaxRetryTime(&wl)
+	if maxTime.After(r.clock.Now()) {
+		var updated bool
+		err := workload.PatchAdmissionStatus(ctx, r.client, &wl, r.clock, func() (*kueue.Workload, bool, error) {
+			if wl.Status.RequeueState == nil {
+				wl.Status.RequeueState = &kueue.RequeueState{}
+			}
+			log.V(2).Info("At least one admission check set a retry time", "maxTime", maxTime, "requeueState.RequeueAt", wl.Status.RequeueState.RequeueAt)
+			if updatedRequeueAt := workload.SetRequeueState(&wl, maxTime, false); !updatedRequeueAt {
```

  Yeah, that makes sense! Let me rewrite this part.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2025-11-13T23:20:23Z

- **pkg/workload/workload.go:71** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-11-13T23:20:22Z
```diff
@@ -61,16 +61,14 @@ const (
 	StatusFinished      = "finished"
 )
 
-var (
-	admissionManagedConditions = []string{
-		kueue.WorkloadQuotaReserved,
-		kueue.WorkloadEvicted,
-		kueue.WorkloadAdmitted,
-		kueue.WorkloadPreempted,
-		kueue.WorkloadRequeued,
-		kueue.WorkloadDeactivationTarget,
-	}
-)
+var admissionManagedConditions = []string{
+	kueue.WorkloadQuotaReserved,
+	kueue.WorkloadEvicted,
+	kueue.WorkloadAdmitted,
+	kueue.WorkloadPreempted,
+	kueue.WorkloadRequeued,
+	kueue.WorkloadDeactivationTarget,
+}
```

  Yeah, I carried this change from the earlier PR.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2025-11-13T23:20:38Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:139** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-11-13T23:20:38Z
```diff
@@ -135,7 +136,8 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 				).
 				Annotations(map[string]string{
 					"provreq.kueue.x-k8s.io/ValidUntilSeconds": "0",
-					"invalid-provreq-prefix/Foo":               "Bar"}).
+					"invalid-provreq-prefix/Foo":               "Bar",
```

  Yeah, I carried this change from the earlier PR.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2025-11-13T23:20:51Z

- **pkg/controller/core/workload_controller_test.go:384** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-11-13T23:20:51Z
```diff
@@ -383,18 +381,16 @@ func TestSyncCheckStates(t *testing.T) {
 	}
 }
 
-var (
-	workloadCmpOpts = cmp.Options{
-		cmpopts.EquateEmpty(),
-		cmpopts.IgnoreFields(
-			kueue.Workload{}, "TypeMeta", "ObjectMeta.ResourceVersion",
-		),
-		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
-		cmpopts.IgnoreFields(kueue.AdmissionCheckState{}, "LastTransitionTime"),
-		cmpopts.IgnoreFields(kueue.RequeueState{}, "RequeueAt"),
-		cmpopts.SortSlices(func(a, b metav1.Condition) bool { return a.Type < b.Type }),
-	}
-)
+var workloadCmpOpts = cmp.Options{
```

  Yeah, I carried this change from the earlier PR.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-14T07:57:57Z

LGTM overall, left a couple comments, once this is done please follow up with refactoring of ProvisioningRequest to use the new API, this is already started by @dhenkel92 here: https://github.com/kubernetes-sigs/kueue/pull/7464/files#diff-d5b88e9a8af6b97ce61788f1307ec9ba1f4e3581a9c0634a151ce310c9ca3d91

- **pkg/controller/core/workload_controller.go:367** by [@mimowo](https://github.com/mimowo) — 2025-11-14T07:50:25Z
```diff
@@ -313,10 +355,10 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		wlOrig := wl.DeepCopy()
 		if dtCond != nil {
 			apimeta.RemoveStatusCondition(&wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
-		}
-		if wl.Status.RequeueState != nil {
-			wl.Status.RequeueState = nil
-			updated = true
+			// Clear RequeueState when deactivated due to requeuing limit exceeded.
+			if dtCond.Reason == kueue.WorkloadRequeuingLimitExceeded {
+				wl.Status.RequeueState = nil
+			}
```

  This change looks harmless, but otoh it does not seem needed. I ran all tests and they passed. I suggest to revert unless we have a good case why this is needed

- **pkg/controller/core/workload_controller.go:293** by [@mimowo](https://github.com/mimowo) — 2025-11-14T07:52:46Z
```diff
@@ -264,6 +275,37 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
+		if wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt != nil {
+			requeueAfter := ptr.To(wl.Status.RequeueState.RequeueAt.Sub(r.clock.Now()))
+			if requeueAfter != nil && *requeueAfter > 0 {
+				log.V(3).Info("Waiting for backoff to finish", "backoff", *requeueAfter)
+				return reconcile.Result{RequeueAfter: *requeueAfter}, nil
+			}
+
+			if workload.Status(&wl) == workload.StatusPending {
+				log.V(3).Info("Pending workload requeued after backoff")
+
+				// Clear RequeueAt since backoff has elapsed
+				err := workload.PatchAdmissionStatus(ctx, r.client, &wl, r.clock, func() (*kueue.Workload, bool, error) {
+					if wl.Status.RequeueState != nil {
+						wl.Status.RequeueState.RequeueAt = nil
+					}
+					return &wl, true, nil
```

  Now we can also add `workload.SetRequeuedCondition(&wl, kueue.WorkloadBackoffFinished, "The workload backoff was finished", true)`
  
  It is probably a follow up for later, but I would like to consolidate the two requests (here & pre-existing below)  into one. They are really the same thing conceptually. 
  
  Still, this would mean a bigger diff so I'm ok as is for now.

- **test/integration/singlecluster/scheduler/delayedadmission/delayed_admission_test.go:45** by [@mimowo](https://github.com/mimowo) — 2025-11-14T07:53:15Z
```diff
@@ -0,0 +1,216 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package delayedadmission
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("SchedulerWithDelayedAdmissionChecks", func() {
+	var (
+		// Values referenced by tests:
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+		clusterQ      *kueue.ClusterQueue
+		localQ        *kueue.LocalQueue
+		delayedCheck  *kueue.AdmissionCheck
+		realClock     = clock.RealClock{}
```

  nit, use util.RealClock intead

- **test/integration/singlecluster/scheduler/podsready/scheduler_test.go:794** by [@mimowo](https://github.com/mimowo) — 2025-11-14T07:55:03Z
```diff
@@ -780,15 +780,21 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
 				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, standaloneClusterQ.Name, wl1)
 				// To avoid flakiness, we don't verify if the workload has a QuotaReserved=false with pending reason here.
 			})
+			ginkgo.By("waiting for the first workload to be evicted again (second eviction)", func() {
+				util.AwaitWorkloadEvictionByPodsReadyTimeout(ctx, k8sClient, client.ObjectKeyFromObject(wl1), podsReadyTimeout)
+				util.FinishEvictionForWorkloads(ctx, k8sClient, wl1)
+			})
```

  I see, thanks for looking into this.

- **pkg/controller/core/workload_controller.go:293** by [@mimowo](https://github.com/mimowo) — 2025-11-14T07:56:02Z
```diff
@@ -264,6 +275,37 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
+		if wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt != nil {
+			requeueAfter := ptr.To(wl.Status.RequeueState.RequeueAt.Sub(r.clock.Now()))
+			if requeueAfter != nil && *requeueAfter > 0 {
+				log.V(3).Info("Waiting for backoff to finish", "backoff", *requeueAfter)
+				return reconcile.Result{RequeueAfter: *requeueAfter}, nil
+			}
+
+			if workload.Status(&wl) == workload.StatusPending {
+				log.V(3).Info("Pending workload requeued after backoff")
+
+				// Clear RequeueAt since backoff has elapsed
+				err := workload.PatchAdmissionStatus(ctx, r.client, &wl, r.clock, func() (*kueue.Workload, bool, error) {
+					if wl.Status.RequeueState != nil {
+						wl.Status.RequeueState.RequeueAt = nil
+					}
+					return &wl, true, nil
```

  Please also extend one of the test asserting to check that the Requeued=true condition is set.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2025-11-14T13:13:44Z

- **pkg/controller/core/workload_controller.go:367** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-11-14T13:13:44Z
```diff
@@ -313,10 +355,10 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		wlOrig := wl.DeepCopy()
 		if dtCond != nil {
 			apimeta.RemoveStatusCondition(&wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
-		}
-		if wl.Status.RequeueState != nil {
-			wl.Status.RequeueState = nil
-			updated = true
+			// Clear RequeueState when deactivated due to requeuing limit exceeded.
+			if dtCond.Reason == kueue.WorkloadRequeuingLimitExceeded {
+				wl.Status.RequeueState = nil
+			}
```

  Yeah, I'm leaning towards reverting this. I can't recollect any use case where preserving `RequeueState` during deactivation provides value.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-14T15:15:53Z

- **pkg/controller/core/workload_controller.go:599** by [@mimowo](https://github.com/mimowo) — 2025-11-14T15:15:53Z
```diff
@@ -543,7 +594,11 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 	}
 	// at this point we know a Workload has at least one Retry AdmissionCheck
 	message := "At least one admission check is false"
-	if err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByAdmissionCheck, message, "", r.clock); err != nil {
+	if err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByAdmissionCheck, message, "", r.clock, workload.WithCustomPrepare(func() (*kueue.Workload, error) {
+		// Set Requeued=false so that when the backoff elapses, we can set it to true
+		workload.SetRequeuedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "Evicted due to admission check retry", false)
```

  this makes sense. Did we set it before from another place?
  
  If no it sounds like it was a bug, surprising it didn't backfire more.
  
  if you also think this is a bug we could submit in separete PR we could backport.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2025-11-14T15:35:40Z

- **pkg/controller/core/workload_controller.go:599** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-11-14T15:35:39Z
```diff
@@ -543,7 +594,11 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 	}
 	// at this point we know a Workload has at least one Retry AdmissionCheck
 	message := "At least one admission check is false"
-	if err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByAdmissionCheck, message, "", r.clock); err != nil {
+	if err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByAdmissionCheck, message, "", r.clock, workload.WithCustomPrepare(func() (*kueue.Workload, error) {
+		// Set Requeued=false so that when the backoff elapses, we can set it to true
+		workload.SetRequeuedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "Evicted due to admission check retry", false)
```

  Yeah, this was a bug. The `Requeued=false` condition was never being set for admission check evictions in the workload controller itself. I looked into the codebase and found that for job-managed workloads, the jobframework reconciler sets `Requeued=false` when the job becomes inactive after eviction. However, for standalone workloads (those not managed by a job controller), there was no code setting this condition at all. This is why the bug existed but didn't cause widespread issues. I think the bug only affects the Requeued condition tracking; the actual retry/requeue functionality still works correctly via `RequeueState`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-14T15:40:22Z

- **pkg/controller/core/workload_controller.go:599** by [@mimowo](https://github.com/mimowo) — 2025-11-14T15:40:22Z
```diff
@@ -543,7 +594,11 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 	}
 	// at this point we know a Workload has at least one Retry AdmissionCheck
 	message := "At least one admission check is false"
-	if err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByAdmissionCheck, message, "", r.clock); err != nil {
+	if err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByAdmissionCheck, message, "", r.clock, workload.WithCustomPrepare(func() (*kueue.Workload, error) {
+		// Set Requeued=false so that when the backoff elapses, we can set it to true
+		workload.SetRequeuedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "Evicted due to admission check retry", false)
```

  I see, it will be good to try to isolate it as a preparatory PR then we could cherrypick. It will also lower the diff here.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2025-11-14T16:13:23Z

- **pkg/controller/core/workload_controller.go:599** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-11-14T16:13:23Z
```diff
@@ -543,7 +594,11 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 	}
 	// at this point we know a Workload has at least one Retry AdmissionCheck
 	message := "At least one admission check is false"
-	if err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByAdmissionCheck, message, "", r.clock); err != nil {
+	if err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByAdmissionCheck, message, "", r.clock, workload.WithCustomPrepare(func() (*kueue.Workload, error) {
+		// Set Requeued=false so that when the backoff elapses, we can set it to true
+		workload.SetRequeuedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "Evicted due to admission check retry", false)
```

  sure, here's the one: https://github.com/kubernetes-sigs/kueue/pull/7673

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-14T17:25:23Z

- **pkg/controller/core/workload_controller.go:369** by [@mimowo](https://github.com/mimowo) — 2025-11-14T17:25:23Z
```diff
@@ -314,8 +365,9 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		if dtCond != nil {
 			apimeta.RemoveStatusCondition(&wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
 		}
-		if wl.Status.RequeueState != nil {
-			wl.Status.RequeueState = nil
+		if wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt != nil {
+			// Clear the requeue schedule while preserving the Count for historical tracking
```

  I think we also want to clear the Count as before. This is because we re-activation is a manual process in spec, and if we don't clear the Count, then such a workload will be automatically put back as Deactivated, which is not good experience. So the user would need to clear the Count on their own.
  
  This makes me think we should also clear admissionChecks.RetryCount fields on deactivation

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-14T17:27:46Z

- **pkg/controller/core/workload_controller.go:613** by [@mimowo](https://github.com/mimowo) — 2025-11-14T17:27:46Z
```diff
@@ -543,7 +595,22 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 	}
 	// at this point we know a Workload has at least one Retry AdmissionCheck
 	message := "At least one admission check is false"
-	if err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByAdmissionCheck, message, "", r.clock); err != nil {
+	if err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByAdmissionCheck, message, "", r.clock, workload.WithCustomPrepare(func() (*kueue.Workload, error) {
+		// Only set Requeued=false if there's a delayed retry configured
+		// Check if any admission check has RequeueAfterSeconds set
+		hasDelayedRetry := false
+		for i := range wl.Status.AdmissionChecks {
+			if wl.Status.AdmissionChecks[i].State == kueue.CheckStateRetry && wl.Status.AdmissionChecks[i].RequeueAfterSeconds != nil {
+				hasDelayedRetry = true
+				break
+			}
+		}
+		if hasDelayedRetry {
+			// Set Requeued=false so that when the backoff elapses, we can set it to true
+			workload.SetRequeuedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "Evicted due to admission check retry", false)
+		}
+		return wl, nil
+	})); err != nil {
```

  Actually, this is getting complex, I'm no longer sure this is needed actually. 
  
  It think it is enough to set Requeued only from job_controller as before on removing QuotaReservation, because it is only at this moment when we consider the workload as Pending anyway.
  
  So, I would try to minimize the impact of the PR on pre-existing behavior actually if not needed.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2025-11-14T17:43:42Z

- **pkg/controller/core/workload_controller.go:369** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-11-14T17:43:42Z
```diff
@@ -314,8 +365,9 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		if dtCond != nil {
 			apimeta.RemoveStatusCondition(&wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
 		}
-		if wl.Status.RequeueState != nil {
-			wl.Status.RequeueState = nil
+		if wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt != nil {
+			// Clear the requeue schedule while preserving the Count for historical tracking
```

  Yeah, good catch! Just to sum up here-> we need to clear: `RequeueState.Count`, `RequeueState.RequeueAt`, all `AdmissionChecks[].RetryCount` fields, and all `AdmissionChecks[].RequeueAfterSeconds` fields.
  Having said that, we can't set `RequeueState = nil` because that causes the validation error with SSA. We need to
  clear the fields individually.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-14T18:01:06Z

- **pkg/controller/core/workload_controller.go:369** by [@mimowo](https://github.com/mimowo) — 2025-11-14T18:01:06Z
```diff
@@ -314,8 +365,9 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		if dtCond != nil {
 			apimeta.RemoveStatusCondition(&wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
 		}
-		if wl.Status.RequeueState != nil {
-			wl.Status.RequeueState = nil
+		if wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt != nil {
+			// Clear the requeue schedule while preserving the Count for historical tracking
```

  > Having said that, we can't set RequeueState = nil because that causes the validation error with SSA. We need to
  clear the fields individually.
  
  Hm, ok, but this is surprising because the pre-existing code would do 
  ```
  if wl.Status.RequeueState != nil {
     wl.Status.RequeueState = nil
  }
  ```
  didn't work on the current releases?

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2025-11-15T12:38:09Z

- **pkg/controller/core/workload_controller.go:613** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-11-15T12:38:09Z
```diff
@@ -543,7 +595,22 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 	}
 	// at this point we know a Workload has at least one Retry AdmissionCheck
 	message := "At least one admission check is false"
-	if err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByAdmissionCheck, message, "", r.clock); err != nil {
+	if err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByAdmissionCheck, message, "", r.clock, workload.WithCustomPrepare(func() (*kueue.Workload, error) {
+		// Only set Requeued=false if there's a delayed retry configured
+		// Check if any admission check has RequeueAfterSeconds set
+		hasDelayedRetry := false
+		for i := range wl.Status.AdmissionChecks {
+			if wl.Status.AdmissionChecks[i].State == kueue.CheckStateRetry && wl.Status.AdmissionChecks[i].RequeueAfterSeconds != nil {
+				hasDelayedRetry = true
+				break
+			}
+		}
+		if hasDelayedRetry {
+			// Set Requeued=false so that when the backoff elapses, we can set it to true
+			workload.SetRequeuedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "Evicted due to admission check retry", false)
+		}
+		return wl, nil
+	})); err != nil {
```

  Yeah, I removed that code.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2025-11-15T13:04:24Z

- **pkg/controller/core/workload_controller.go:369** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-11-15T13:04:24Z
```diff
@@ -314,8 +365,9 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		if dtCond != nil {
 			apimeta.RemoveStatusCondition(&wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
 		}
-		if wl.Status.RequeueState != nil {
-			wl.Status.RequeueState = nil
+		if wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt != nil {
+			// Clear the requeue schedule while preserving the Count for historical tracking
```

  @mimowo Yeah, I made minimal changes to make this work. Let me know how you feel about this one?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-17T10:13:46Z

- **pkg/controller/core/workload_controller.go:369** by [@mimowo](https://github.com/mimowo) — 2025-11-17T10:13:45Z
```diff
@@ -314,8 +365,9 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		if dtCond != nil {
 			apimeta.RemoveStatusCondition(&wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
 		}
-		if wl.Status.RequeueState != nil {
-			wl.Status.RequeueState = nil
+		if wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt != nil {
+			// Clear the requeue schedule while preserving the Count for historical tracking
```

  This looks ok. Could you please also prepare a small PR with just this fix, and dedicated test.
  I think if this didn't work in the past it is something where we should cherrypick the fix.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-17T10:14:59Z

- **pkg/controller/core/workload_controller.go:369** by [@mimowo](https://github.com/mimowo) — 2025-11-17T10:14:59Z
```diff
@@ -314,8 +365,9 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		if dtCond != nil {
 			apimeta.RemoveStatusCondition(&wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
 		}
-		if wl.Status.RequeueState != nil {
-			wl.Status.RequeueState = nil
+		if wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt != nil {
+			// Clear the requeue schedule while preserving the Count for historical tracking
```

  Ah, actually after you modified the ProvisioningRequest controller the fix is no longer needed. I think the fix was needed because the ProvRequest controller was changing the RequeueState using another admission manager.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-17T10:17:31Z

- **pkg/workload/admissionchecks.go:260** by [@mimowo](https://github.com/mimowo) — 2025-11-17T10:17:31Z
```diff
@@ -186,3 +196,66 @@ func HasRejectedChecks(wl *kueue.Workload) bool {
 	}
 	return false
 }
+
+// GetMaxRetryTime returns the max retry time from all the admission checks.
+// It will return a zero time if no retry time is found.
+func GetMaxRetryTime(wl *kueue.Workload) metav1.Time {
+	var max time.Time
+	for i := range wl.Status.AdmissionChecks {
+		if wl.Status.AdmissionChecks[i].RequeueAfterSeconds == nil {
+			continue
+		}
+		// This should never happen, but prevents panics
+		if wl.Status.AdmissionChecks[i].LastTransitionTime.IsZero() {
+			continue
+		}
+		if wl.Status.AdmissionChecks[i].State != kueue.CheckStateRetry {
+			continue
+		}
+
+		retryTime := wl.Status.AdmissionChecks[i].LastTransitionTime.Add(time.Duration(*wl.Status.AdmissionChecks[i].RequeueAfterSeconds) * time.Second)
+		if max.Before(retryTime) {
+			max = retryTime
+		}
+	}
+	return metav1.NewTime(max)
+}
+
+// NeedsRequeueAtUpdate checks if the workload needs its RequeueAt time updated
+// based on admission check retry times. It returns the target requeue time if an
+// update should be performed, or nil if no update is needed.
+func NeedsRequeueAtUpdate(wl *kueue.Workload, clock clock.Clock) *metav1.Time {
+	maxTime := GetMaxRetryTime(wl)
+	// No retry time set
+	if maxTime.IsZero() {
+		return nil
+	}
+	// Retry time is in the past
+	if !maxTime.After(clock.Now()) {
+		return nil
+	}
+	// Check if we need to update RequeueState
+	if wl.Status.RequeueState != nil {
+		currentRequeueAt := ptr.Deref(wl.Status.RequeueState.RequeueAt, metav1.NewTime(time.Time{}))
+		if !currentRequeueAt.Before(&maxTime) || currentRequeueAt.Equal(&maxTime) {
+			// Current time is already >= maxTime, no update needed
+			return nil
+		}
+	}
+	return &maxTime
+}
+
+// UpdateAdmissionCheckRequeueState calculates the RequeueAfterSeconds based on the backoff and requeuingCount
+func UpdateAdmissionCheckRequeueState(acState *kueue.AdmissionCheckState, backoffBaseSeconds int32, backoffMaxSeconds int32, clock clock.Clock) {
+	requeuingCount := ptr.Deref(acState.RetryCount, 0) + 1
+
+	// Every backoff duration is about "60s*2^(n-1)+Rand" where:
+	// - "n" represents the "requeuingCount",
+	// - "Rand" represents the random jitter.
+	// During this time, the workload is treated as inadmissible and other
+	// workloads will have a chance to be admitted.
+	backoff := wait.NewBackoff(time.Duration(backoffBaseSeconds)*time.Second, time.Duration(backoffMaxSeconds)*time.Second, 2, 0.0001)
+	waitDuration := backoff.WaitTime(int(requeuingCount))
+
+	acState.RequeueAfterSeconds = ptr.To(int32(waitDuration.Truncate(time.Second).Seconds()))
```

  We should also increment the RetryCount for ProvisioningRequest for informational purposes, even if we are not using that information directly. 
  
  EDIT: Ah I see it is already bumped here: https://github.com/kubernetes-sigs/kueue/pull/7620/files#diff-c703a3b5f20a31c414c5cafc7df771810f8e39ba1e8658d8b9f897bcb8af659cR95-R107
  
  And this assert shows it gets set: https://github.com/kubernetes-sigs/kueue/pull/7620/files#diff-b524dd7e94e695c9007bf094b1ff9cd5a94952214c0db7de004c7ce5bea7ed1bR924-L924
  
  And then incremented: https://github.com/kubernetes-sigs/kueue/pull/7620/files#diff-b524dd7e94e695c9007bf094b1ff9cd5a94952214c0db7de004c7ce5bea7ed1bL1351-R1368

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-17T10:18:56Z

/lgtm
/approve
Please also address the small follow ups:
- ~[thread to use RetryCount for ProvReq controller](https://github.com/kubernetes-sigs/kueue/pull/7620#discussion_r2533500741) for informational purposes~ 
- [thread to simplify the code after the ProvReq controller is updated](https://github.com/kubernetes-sigs/kueue/pull/7620#discussion_r2533493194)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-17T10:21:41Z

- **pkg/controller/core/workload_controller.go:599** by [@mimowo](https://github.com/mimowo) — 2025-11-17T10:21:41Z
```diff
@@ -543,7 +594,11 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 	}
 	// at this point we know a Workload has at least one Retry AdmissionCheck
 	message := "At least one admission check is false"
-	if err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByAdmissionCheck, message, "", r.clock); err != nil {
+	if err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByAdmissionCheck, message, "", r.clock, workload.WithCustomPrepare(func() (*kueue.Workload, error) {
+		// Set Requeued=false so that when the backoff elapses, we can set it to true
+		workload.SetRequeuedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "Evicted due to admission check retry", false)
```

  Thank you for de-coupling that
