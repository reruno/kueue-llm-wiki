# PR #7646: feat: display flavor assignment attempts in events

**Summary**: feat: display flavor assignment attempts in events

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7646

**Last updated**: 2025-11-28T08:38:14Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mykysha](https://github.com/mykysha)
- **Branch**: `feat/flavor-attempts` → `main`
- **Created**: 2025-11-13T13:56:43Z
- **Updated**: 2025-11-28T08:38:14Z
- **Merged**: 2025-11-26T17:02:37Z
- **Closed**: 2025-11-26T17:02:37Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@gabesaba](https://github.com/gabesaba), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 6   **+932 / -52**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add information about why some flavors were not chosen in favor of the other to the events at the admission.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #7137 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Observability: Summarize the list of flavors considered for admission in the release cycle, but not used eventually for a workload which reserved the quota. 
The summary is present in the message for the QuotaReserved condition, and in the event.
Before: "Quota reserved in ClusterQueue tas-main, wait time since queued was 9223372037s"
After: "Quota reserved in ClusterQueue tas-main, wait time since queued was 9223372037s; Flavors considered: one: default(NoFit;Flavor \"default\" does not support TopologyAwareScheduling)"
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-13T13:56:48Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-11-13T13:56:51Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | ed8ebeba8be105eb4300b0fc8835881dfd82fd21 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69270da887abba00082def9f |

### Comment by [@mykysha](https://github.com/mykysha) — 2025-11-20T16:24:13Z

This PR records flavor attempts tried, however as flavors get chosen on the first-to-fit basis, it may introduce some misleading results.

One example would be that not all flavors are tried, resulting in an incomplete list of the flavors that could be chosen. 
Another, and more serious one, would be that sometimes we might not reach the failure point for one of the flavors attempted, resulting in a message that is falsely marked as "Fit". As an example, in the test "multiple flavors, leader worker set, workers request GPU, leader does not request GPU, fits" in the flavorassigner_test.go there is one flavor that should be Fit, and another that should be NoFit because of the missing GPU resource. In fact, if another two resources get checked first, and then the fitting flavor is chosen on the gpu check, we do not get the missing resource message, hence do not fail the check.

One potential option would be to disregard attempts that have not been tried for all the requested resources, potentially severely limiting the information returned, but making it more true in the end.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-21T06:52:51Z

@mykysha please set release note

cc @mbobrovskyi @mszadkow @IrvingMg please review

### Comment by [@mykysha](https://github.com/mykysha) — 2025-11-26T14:50:55Z

/test pull-kueue-test-e2e-main-1-33

### Comment by [@mykysha](https://github.com/mykysha) — 2025-11-26T14:52:24Z

Looks like another case of #7903

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-26T16:38:13Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7646#pullrequestreview-3511807284" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7646#" title="Author self-approved">mykysha</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-26T16:38:14Z

LGTM label has been added.  <details>Git tree hash: 07a6bd6601de74c7fe3b5c221104eb09c9943a17</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-28T08:24:06Z

/release-note-edit
```release-note
Summarize the list of flavors considered for admission in the release cycle, but not used eventually for a workload
which reserved the quota. The summary is present in the message for the QuotaReserved condition, and in the event.
Before: "Quota reserved in ClusterQueue tas-main, wait time since queued was 9223372037s"
After: "Quota reserved in ClusterQueue tas-main, wait time since queued was 9223372037s; Flavors considered: one: default(NoFit;Flavor \"default\" does not support TopologyAwareScheduling)"
```

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-28T08:38:12Z

/release-note-edit
```release-note
Observability: Summarize the list of flavors considered for admission in the release cycle, but not used eventually for a workload which reserved the quota. 
The summary is present in the message for the QuotaReserved condition, and in the event.
Before: "Quota reserved in ClusterQueue tas-main, wait time since queued was 9223372037s"
After: "Quota reserved in ClusterQueue tas-main, wait time since queued was 9223372037s; Flavors considered: one: default(NoFit;Flavor \"default\" does not support TopologyAwareScheduling)"
```

## Reviews

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-11-21T08:16:06Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:601** by [@mszadkow](https://github.com/mszadkow) — 2025-11-21T08:16:06Z
```diff
@@ -586,6 +598,8 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			requests.Add(podset.podSet.Requests)
 		}
 
+		considerAcc := make(map[kueue.ResourceFlavorReference]FlavorAttempt)
```

  what does the `considerAcc` stand for?

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-11-21T08:22:33Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:601** by [@mykysha](https://github.com/mykysha) — 2025-11-21T08:22:32Z
```diff
@@ -586,6 +598,8 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			requests.Add(podset.podSet.Requests)
 		}
 
+		considerAcc := make(map[kueue.ResourceFlavorReference]FlavorAttempt)
```

  Will change to `consideredFlavors`, initially it was for considered accumulated

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-11-21T08:27:47Z

- **pkg/scheduler/scheduler_tas_test.go:278** by [@mszadkow](https://github.com/mszadkow) — 2025-11-21T08:27:47Z
```diff
@@ -274,6 +274,8 @@ func TestScheduleForTAS(t *testing.T) {
 			wantEvents: []utiltesting.EventRecord{
 				utiltesting.MakeEventRecord("default", "foo", "QuotaReserved", corev1.EventTypeNormal).Obj(),
 				utiltesting.MakeEventRecord("default", "foo", "Admitted", corev1.EventTypeNormal).Obj(),
+				utiltesting.MakeEventRecord("default", "foo", "FlavorAttempts", corev1.EventTypeNormal).
+					Message("Considered: launcher: tas-default(Fit) | worker: tas-default(Fit)").Obj(),
```

  The message is very concise, but I guess it's what @mimowo wanted and events are not the doc, rather short info. Message looks good imho

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-11-21T08:31:27Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:711** by [@mszadkow](https://github.com/mszadkow) — 2025-11-21T08:31:26Z
```diff
@@ -682,16 +708,17 @@ func (a *Assignment) append(requests resources.Requests, psAssignment *PodSetAss
 // Returns the chosen flavor, along with the information about resources that need to be borrowed.
 // If the flavor cannot be immediately assigned, it returns a status with
 // reasons or failure.
+// It also returns the list of Flavors tried.
```

  I guess it always returns considered flavors if no error

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-21T08:32:08Z

- **pkg/scheduler/scheduler_tas_test.go:278** by [@mimowo](https://github.com/mimowo) — 2025-11-21T08:32:08Z
```diff
@@ -274,6 +274,8 @@ func TestScheduleForTAS(t *testing.T) {
 			wantEvents: []utiltesting.EventRecord{
 				utiltesting.MakeEventRecord("default", "foo", "QuotaReserved", corev1.EventTypeNormal).Obj(),
 				utiltesting.MakeEventRecord("default", "foo", "Admitted", corev1.EventTypeNormal).Obj(),
+				utiltesting.MakeEventRecord("default", "foo", "FlavorAttempts", corev1.EventTypeNormal).
+					Message("Considered: launcher: tas-default(Fit) | worker: tas-default(Fit)").Obj(),
```

  Hm, yeah, but I don't think we need a new event for that. The event is "Admitted". I think it is enough to add after ";" the information like "Flavors consirered: ...)".
  
  However here, it looks very weird to have previously considered flavors as "Fit". I think it might happen if we had borrowing, right? However, if this is the case I would like to include that information.
  
  I think ideally we inform the user about both dimentions: `(borrowingDistance, preemptionMode)`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-21T08:32:40Z

- **pkg/scheduler/scheduler_tas_test.go:278** by [@mimowo](https://github.com/mimowo) — 2025-11-21T08:32:40Z
```diff
@@ -274,6 +274,8 @@ func TestScheduleForTAS(t *testing.T) {
 			wantEvents: []utiltesting.EventRecord{
 				utiltesting.MakeEventRecord("default", "foo", "QuotaReserved", corev1.EventTypeNormal).Obj(),
 				utiltesting.MakeEventRecord("default", "foo", "Admitted", corev1.EventTypeNormal).Obj(),
+				utiltesting.MakeEventRecord("default", "foo", "FlavorAttempts", corev1.EventTypeNormal).
+					Message("Considered: launcher: tas-default(Fit) | worker: tas-default(Fit)").Obj(),
```

  Just be careful to make sure the message is truncated to conform to the maximal length.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-11-21T08:33:24Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:711** by [@mykysha](https://github.com/mykysha) — 2025-11-21T08:33:24Z
```diff
@@ -682,16 +708,17 @@ func (a *Assignment) append(requests resources.Requests, psAssignment *PodSetAss
 // Returns the chosen flavor, along with the information about resources that need to be borrowed.
 // If the flavor cannot be immediately assigned, it returns a status with
 // reasons or failure.
+// It also returns the list of Flavors tried.
```

  Yes, that was the idea, but I see now how it might seem to be related to the `f the flavor cannot be immediately assigned` part, so will modify the order of the comment info

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-11-21T09:35:22Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:251** by [@pajakd](https://github.com/pajakd) — 2025-11-21T09:35:22Z
```diff
@@ -242,6 +242,17 @@ type PodSetAssignment struct {
 
 	TopologyAssignment     *tas.TopologyAssignment
 	DelayedTopologyRequest *kueue.DelayedTopologyRequestState
+
+	Considered []FlavorAttempt
+}
+
+// FlavorAttempt captures one attempted flavor and its worst-case outcome
+// across the requested resources.
+type FlavorAttempt struct {
```

  Perhaps `FlavorAttempt -> FlavorAssignmentAttempt`

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-11-21T09:35:37Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:246** by [@pajakd](https://github.com/pajakd) — 2025-11-21T09:35:36Z
```diff
@@ -242,6 +242,17 @@ type PodSetAssignment struct {
 
 	TopologyAssignment     *tas.TopologyAssignment
 	DelayedTopologyRequest *kueue.DelayedTopologyRequestState
+
+	Considered []FlavorAttempt
```

  Perhaps `Considered -> FlavorAssignmentAttempts`

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-21T09:37:30Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:565** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-21T09:37:30Z
```diff
@@ -547,10 +558,11 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 		}
 
 		psAssignment := PodSetAssignment{
-			Name:     podSet.Name,
-			Flavors:  make(ResourceAssignment, len(podSet.Requests)),
-			Requests: podSet.Requests.ToResourceList(),
-			Count:    podSet.Count,
+			Name:       podSet.Name,
+			Flavors:    make(ResourceAssignment, len(podSet.Requests)),
+			Requests:   podSet.Requests.ToResourceList(),
+			Count:      podSet.Count,
+			Considered: make([]FlavorAttempt, 0),
```

  ```suggestion
  ```
  
  I think it should be the same.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-11-21T10:02:31Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:253** by [@pajakd](https://github.com/pajakd) — 2025-11-21T10:02:30Z
```diff
@@ -242,6 +242,17 @@ type PodSetAssignment struct {
 
 	TopologyAssignment     *tas.TopologyAssignment
 	DelayedTopologyRequest *kueue.DelayedTopologyRequestState
+
+	Considered []FlavorAttempt
+}
+
+// FlavorAttempt captures one attempted flavor and its worst-case outcome
+// across the requested resources.
+type FlavorAttempt struct {
+	Flavor  kueue.ResourceFlavorReference
+	Mode    FlavorAssignmentMode
```

  `FlavorAssignmentMode` has less information than the internal `preemptionMode`. For example `preemptionMode` distinguishes reclaim from other preemption. 
  
  This PR will be already an improvement and will help in debugging, but if possible we should consider exposing the more granular information.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-11-24T15:10:59Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:601** by [@mykysha](https://github.com/mykysha) — 2025-11-24T15:10:59Z
```diff
@@ -586,6 +598,8 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			requests.Add(podset.podSet.Requests)
 		}
 
+		considerAcc := make(map[kueue.ResourceFlavorReference]FlavorAttempt)
```

  Have renamed the variable here

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-11-24T15:11:42Z

- **pkg/scheduler/scheduler_tas_test.go:278** by [@mykysha](https://github.com/mykysha) — 2025-11-24T15:11:42Z
```diff
@@ -274,6 +274,8 @@ func TestScheduleForTAS(t *testing.T) {
 			wantEvents: []utiltesting.EventRecord{
 				utiltesting.MakeEventRecord("default", "foo", "QuotaReserved", corev1.EventTypeNormal).Obj(),
 				utiltesting.MakeEventRecord("default", "foo", "Admitted", corev1.EventTypeNormal).Obj(),
+				utiltesting.MakeEventRecord("default", "foo", "FlavorAttempts", corev1.EventTypeNormal).
+					Message("Considered: launcher: tas-default(Fit) | worker: tas-default(Fit)").Obj(),
```

  Merged the event with "Admitted" and dropped empty Fit flavors that do not display any relevant borrowing info

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-11-24T15:11:57Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:711** by [@mykysha](https://github.com/mykysha) — 2025-11-24T15:11:57Z
```diff
@@ -682,16 +708,17 @@ func (a *Assignment) append(requests resources.Requests, psAssignment *PodSetAss
 // Returns the chosen flavor, along with the information about resources that need to be borrowed.
 // If the flavor cannot be immediately assigned, it returns a status with
 // reasons or failure.
+// It also returns the list of Flavors tried.
```

  Reworked the comment here

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-11-24T15:12:05Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:251** by [@mykysha](https://github.com/mykysha) — 2025-11-24T15:12:05Z
```diff
@@ -242,6 +242,17 @@ type PodSetAssignment struct {
 
 	TopologyAssignment     *tas.TopologyAssignment
 	DelayedTopologyRequest *kueue.DelayedTopologyRequestState
+
+	Considered []FlavorAttempt
+}
+
+// FlavorAttempt captures one attempted flavor and its worst-case outcome
+// across the requested resources.
+type FlavorAttempt struct {
```

  Renamed

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-11-24T15:12:12Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:246** by [@mykysha](https://github.com/mykysha) — 2025-11-24T15:12:11Z
```diff
@@ -242,6 +242,17 @@ type PodSetAssignment struct {
 
 	TopologyAssignment     *tas.TopologyAssignment
 	DelayedTopologyRequest *kueue.DelayedTopologyRequestState
+
+	Considered []FlavorAttempt
```

  Also renamed

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-11-24T15:13:00Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:565** by [@mykysha](https://github.com/mykysha) — 2025-11-24T15:12:59Z
```diff
@@ -547,10 +558,11 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 		}
 
 		psAssignment := PodSetAssignment{
-			Name:     podSet.Name,
-			Flavors:  make(ResourceAssignment, len(podSet.Requests)),
-			Requests: podSet.Requests.ToResourceList(),
-			Count:    podSet.Count,
+			Name:       podSet.Name,
+			Flavors:    make(ResourceAssignment, len(podSet.Requests)),
+			Requests:   podSet.Requests.ToResourceList(),
+			Count:      podSet.Count,
+			Considered: make([]FlavorAttempt, 0),
```

  Dropped

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-11-24T15:14:44Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:253** by [@mykysha](https://github.com/mykysha) — 2025-11-24T15:14:44Z
```diff
@@ -242,6 +242,17 @@ type PodSetAssignment struct {
 
 	TopologyAssignment     *tas.TopologyAssignment
 	DelayedTopologyRequest *kueue.DelayedTopologyRequestState
+
+	Considered []FlavorAttempt
+}
+
+// FlavorAttempt captures one attempted flavor and its worst-case outcome
+// across the requested resources.
+type FlavorAttempt struct {
+	Flavor  kueue.ResourceFlavorReference
+	Mode    FlavorAssignmentMode
```

  As this is the largest comment to tackle, I've decided to separate the other ones from this one and to push them now. I am continuing working on adding more info regarding preemption to the FlavorAssignmentAttempts, and will be pushing the changes as a part of this PR/as a followup once ready

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T16:42:14Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:636** by [@mimowo](https://github.com/mimowo) — 2025-11-24T16:42:14Z
```diff
@@ -611,12 +626,22 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 				groupStatus.reasons = append(groupStatus.reasons, status.reasons...)
 			}
 		}
+
+		finalConsidered := make([]FlavorAssignmentAttempt, 0, len(consideredFlavors))
+		for _, v := range consideredFlavors {
+			finalConsidered = append(finalConsidered, v)
+		}
+		slices.SortFunc(finalConsidered, func(a, b FlavorAssignmentAttempt) int {
+			return strings.Compare(string(a.Flavor), string(b.Flavor))
+		})
```

  can you wrap this in a helper function to offload the main body of the function?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T16:44:37Z

- **pkg/scheduler/scheduler.go:915** by [@mimowo](https://github.com/mimowo) — 2025-11-24T16:44:37Z
```diff
@@ -902,3 +911,45 @@ func (s *Scheduler) updateEntryPenalty(log logr.Logger, e *entry, op usageOp) {
 		log.V(3).Info("Entry penalty subtracted from localQueue", "localQueue", lqObjRef, "penalty", penalty)
 	}
 }
+
+func formatConsideredForEvent(a flavorassigner.Assignment) string {
```

  This needs to go to some helper file to offload the scheduler logic, maybe flavors_attempts_formatter.go

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T16:46:10Z

- **pkg/scheduler/scheduler_tas_test.go:1568** by [@mimowo](https://github.com/mimowo) — 2025-11-24T16:46:10Z
```diff
@@ -1559,10 +1561,11 @@ func TestScheduleForTAS(t *testing.T) {
 					).
 					Obj(),
 			},
-			eventCmpOpts: cmp.Options{eventIgnoreMessage},
 			wantEvents: []utiltesting.EventRecord{
-				utiltesting.MakeEventRecord("default", "foo", "QuotaReserved", corev1.EventTypeNormal).Obj(),
-				utiltesting.MakeEventRecord("default", "foo", "Admitted", corev1.EventTypeNormal).Obj(),
+				utiltesting.MakeEventRecord("default", "foo", "QuotaReserved", corev1.EventTypeNormal).
+					Message("Quota reserved in ClusterQueue tas-main, wait time since queued was 9223372037s").Obj(),
+				utiltesting.MakeEventRecord("default", "foo", "Admitted", corev1.EventTypeNormal).
+					Message("Admitted by ClusterQueue tas-main, wait time since reservation was 0s; Flavors considered: launcher: tas-default(NoFit;Flavor \"tas-default\" supports only TopologyAwareScheduling)").Obj(),
```

  Actually it makes me wonder, the information about the considered flavors should go to the QuotaResrvation condition, not necessarily Admitted. This is because in case we have admission checks we wouldn't be able to set the admitted condition by scheduler.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-11-24T16:47:22Z

- **pkg/scheduler/scheduler_tas_test.go:1568** by [@mykysha](https://github.com/mykysha) — 2025-11-24T16:47:21Z
```diff
@@ -1559,10 +1561,11 @@ func TestScheduleForTAS(t *testing.T) {
 					).
 					Obj(),
 			},
-			eventCmpOpts: cmp.Options{eventIgnoreMessage},
 			wantEvents: []utiltesting.EventRecord{
-				utiltesting.MakeEventRecord("default", "foo", "QuotaReserved", corev1.EventTypeNormal).Obj(),
-				utiltesting.MakeEventRecord("default", "foo", "Admitted", corev1.EventTypeNormal).Obj(),
+				utiltesting.MakeEventRecord("default", "foo", "QuotaReserved", corev1.EventTypeNormal).
+					Message("Quota reserved in ClusterQueue tas-main, wait time since queued was 9223372037s").Obj(),
+				utiltesting.MakeEventRecord("default", "foo", "Admitted", corev1.EventTypeNormal).
+					Message("Admitted by ClusterQueue tas-main, wait time since reservation was 0s; Flavors considered: launcher: tas-default(NoFit;Flavor \"tas-default\" supports only TopologyAwareScheduling)").Obj(),
```

  Makes sense, will move

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T16:48:35Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:756** by [@mimowo](https://github.com/mimowo) — 2025-11-24T16:48:35Z
```diff
@@ -717,16 +744,29 @@ func (a *FlavorAssigner) findFlavorForPodSets(
 		attemptedFlavorIdx = idx
 		fName := resourceGroup.Flavors[idx]
 
-		if fit, err := a.checkFlavorForPodSets(log, fName, psIDs, podSets, selectors, status); !fit {
+		ft := FlavorAssignmentAttempt{Flavor: fName}
+		flavorStatus := NewStatus()
+
+		if fit, err := a.checkFlavorForPodSets(log, fName, psIDs, podSets, selectors, flavorStatus); !fit {
+			ft.Mode = NoFit
+			if flavorStatus != nil {
+				ft.Reasons = append(ft.Reasons, flavorStatus.reasons...)
+				status.reasons = append(status.reasons, flavorStatus.reasons...)
+			}
+			considered = append(considered, ft)
```

  TBH this is a bit complicated, maybe we could introduce a helper type like "FlavorAssignmentAttempts". Then, in the "flavor_assignment_attempts.go" for example, and it could encapsulate the serialization logic which now is in the main scheduler.go, plus all the other helper logic

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T16:49:03Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:1024** by [@mimowo](https://github.com/mimowo) — 2025-11-24T16:49:03Z
```diff
@@ -967,3 +1020,85 @@ func filterRequestedResources(req resources.Requests, allowList sets.Set[corev1.
 	}
 	return filtered
 }
+
+func (a *FlavorAssigner) mergeFlavorAttemptsForResource(
```

  I hope these helper funtions can go to a new file, see another comment https://github.com/kubernetes-sigs/kueue/pull/7646/files#r2557034365

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T16:50:04Z

- **pkg/scheduler/scheduler_tas_test.go:1568** by [@mimowo](https://github.com/mimowo) — 2025-11-24T16:50:04Z
```diff
@@ -1559,10 +1561,11 @@ func TestScheduleForTAS(t *testing.T) {
 					).
 					Obj(),
 			},
-			eventCmpOpts: cmp.Options{eventIgnoreMessage},
 			wantEvents: []utiltesting.EventRecord{
-				utiltesting.MakeEventRecord("default", "foo", "QuotaReserved", corev1.EventTypeNormal).Obj(),
-				utiltesting.MakeEventRecord("default", "foo", "Admitted", corev1.EventTypeNormal).Obj(),
+				utiltesting.MakeEventRecord("default", "foo", "QuotaReserved", corev1.EventTypeNormal).
+					Message("Quota reserved in ClusterQueue tas-main, wait time since queued was 9223372037s").Obj(),
+				utiltesting.MakeEventRecord("default", "foo", "Admitted", corev1.EventTypeNormal).
+					Message("Admitted by ClusterQueue tas-main, wait time since reservation was 0s; Flavors considered: launcher: tas-default(NoFit;Flavor \"tas-default\" supports only TopologyAwareScheduling)").Obj(),
```

  And please add a test case which demonstrates that the information is not lost in this case - has some admission checks and needs to skip the first one

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-11-26T14:12:06Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:636** by [@mykysha](https://github.com/mykysha) — 2025-11-26T14:12:05Z
```diff
@@ -611,12 +626,22 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 				groupStatus.reasons = append(groupStatus.reasons, status.reasons...)
 			}
 		}
+
+		finalConsidered := make([]FlavorAssignmentAttempt, 0, len(consideredFlavors))
+		for _, v := range consideredFlavors {
+			finalConsidered = append(finalConsidered, v)
+		}
+		slices.SortFunc(finalConsidered, func(a, b FlavorAssignmentAttempt) int {
+			return strings.Compare(string(a.Flavor), string(b.Flavor))
+		})
```

  Moved this one outside of the function, into the helper file

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-11-26T14:12:15Z

- **pkg/scheduler/scheduler.go:915** by [@mykysha](https://github.com/mykysha) — 2025-11-26T14:12:15Z
```diff
@@ -902,3 +911,45 @@ func (s *Scheduler) updateEntryPenalty(log logr.Logger, e *entry, op usageOp) {
 		log.V(3).Info("Entry penalty subtracted from localQueue", "localQueue", lqObjRef, "penalty", penalty)
 	}
 }
+
+func formatConsideredForEvent(a flavorassigner.Assignment) string {
```

  Done

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-11-26T14:12:46Z

- **pkg/scheduler/scheduler_tas_test.go:1568** by [@mykysha](https://github.com/mykysha) — 2025-11-26T14:12:45Z
```diff
@@ -1559,10 +1561,11 @@ func TestScheduleForTAS(t *testing.T) {
 					).
 					Obj(),
 			},
-			eventCmpOpts: cmp.Options{eventIgnoreMessage},
 			wantEvents: []utiltesting.EventRecord{
-				utiltesting.MakeEventRecord("default", "foo", "QuotaReserved", corev1.EventTypeNormal).Obj(),
-				utiltesting.MakeEventRecord("default", "foo", "Admitted", corev1.EventTypeNormal).Obj(),
+				utiltesting.MakeEventRecord("default", "foo", "QuotaReserved", corev1.EventTypeNormal).
+					Message("Quota reserved in ClusterQueue tas-main, wait time since queued was 9223372037s").Obj(),
+				utiltesting.MakeEventRecord("default", "foo", "Admitted", corev1.EventTypeNormal).
+					Message("Admitted by ClusterQueue tas-main, wait time since reservation was 0s; Flavors considered: launcher: tas-default(NoFit;Flavor \"tas-default\" supports only TopologyAwareScheduling)").Obj(),
```

  Moved to the QuotaReservation as well as added a test case to the scheduler_test

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-11-26T14:13:15Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:756** by [@mykysha](https://github.com/mykysha) — 2025-11-26T14:13:15Z
```diff
@@ -717,16 +744,29 @@ func (a *FlavorAssigner) findFlavorForPodSets(
 		attemptedFlavorIdx = idx
 		fName := resourceGroup.Flavors[idx]
 
-		if fit, err := a.checkFlavorForPodSets(log, fName, psIDs, podSets, selectors, status); !fit {
+		ft := FlavorAssignmentAttempt{Flavor: fName}
+		flavorStatus := NewStatus()
+
+		if fit, err := a.checkFlavorForPodSets(log, fName, psIDs, podSets, selectors, flavorStatus); !fit {
+			ft.Mode = NoFit
+			if flavorStatus != nil {
+				ft.Reasons = append(ft.Reasons, flavorStatus.reasons...)
+				status.reasons = append(status.reasons, flavorStatus.reasons...)
+			}
+			considered = append(considered, ft)
```

  Done, and for other modes as well

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-11-26T14:13:28Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:1024** by [@mykysha](https://github.com/mykysha) — 2025-11-26T14:13:28Z
```diff
@@ -967,3 +1020,85 @@ func filterRequestedResources(req resources.Requests, allowList sets.Set[corev1.
 	}
 	return filtered
 }
+
+func (a *FlavorAssigner) mergeFlavorAttemptsForResource(
```

  Moved these as well

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-26T16:38:02Z

Thank you 👍 
/lgtm
/approve
