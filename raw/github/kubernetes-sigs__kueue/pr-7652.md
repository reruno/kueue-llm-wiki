# PR #7652: Document changing featureGates with configMap

**Summary**: Document changing featureGates with configMap

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7652

**Last updated**: 2025-11-14T23:29:42Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@MaysaMacedo](https://github.com/MaysaMacedo)
- **Branch**: `add-fg-cmg` → `main`
- **Created**: 2025-11-14T00:25:14Z
- **Updated**: 2025-11-14T23:29:42Z
- **Merged**: 2025-11-14T13:37:38Z
- **Closed**: 2025-11-14T13:37:38Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`, `size/S`
- **Assignees**: [@kannon92](https://github.com/kannon92), [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 1   **+19 / -1**

## Description

#### What type of PR is this?

/kind documentation

#### What this PR does / why we need it:

This commit increments the documentation on how
to modify the feature gates by adding an additional option, which is to modify the configMap.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-11-14T00:25:20Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | adfa966dac259ab8c6afb816720528aac8fee9f2 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/691725b3b99c6b00076fecb3 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-7652--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-7652--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTc2NTItLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.KjYmuH7rw6Xp9YnBoOBQK3u_Uy6Nr4Ugge0C89NKjaY)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-7652--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-14T00:34:13Z

LGTM label has been added.  <details>Git tree hash: 0fec8e5e5da58c5fea9c6b852a1b51c8379bd3b3</details>

### Comment by [@kannon92](https://github.com/kannon92) — 2025-11-14T00:35:14Z

This makes sense to me. I think modifying feature gates via config map is a good thing to call out in the documentation.

/assign @mimowo @tenzen-y

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-14T04:09:37Z

/cherry-pick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-11-14T04:09:40Z

@tenzen-y: once the present PR merges, I will cherry-pick it on top of `website` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7652#issuecomment-3530747352):

>/cherry-pick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-14T13:37:09Z

LGTM label has been added.  <details>Git tree hash: 4bd856f794c6b4833d417baadc0b2b99b8da54b9</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-14T13:37:11Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7652#" title="Author self-approved">MaysaMacedo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7652#pullrequestreview-3464989136" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-11-14T13:38:17Z

@tenzen-y: new pull request created: #7667

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7652#issuecomment-3530747352):

>/cherry-pick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-14T00:34:07Z

/lgtm

/cherry-pick website

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-11-14T04:09:23Z

Thank you for adding this documentation!

- **site/content/en/docs/installation/_index.md:240** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-14T04:06:02Z
```diff
@@ -235,9 +235,25 @@ To install and configure Kueue with [Helm](https://helm.sh/), follow the [instru
 
 ## Change the feature gates configuration
 
-Kueue uses a similar mechanism to configure features as described in [Kubernetes Feature Gates](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates).
+You can edit the `kueue-manager-config` `ConfigMap` and add the feature gate you would like to manage, for example:
 
-In order to change the default of a feature, you need to edit the `kueue-controller-manager` deployment within the kueue installation namespace and change the `manager` container arguments to include
+```
```

  ```suggestion
  ```yaml
  ```
  
  The laungauge color could be helpful to understand this code.

- **site/content/en/docs/installation/_index.md:248** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-14T04:07:07Z
```diff
@@ -235,9 +235,25 @@ To install and configure Kueue with [Helm](https://helm.sh/), follow the [instru
 
 ## Change the feature gates configuration
 
-Kueue uses a similar mechanism to configure features as described in [Kubernetes Feature Gates](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates).
+You can edit the `kueue-manager-config` `ConfigMap` and add the feature gate you would like to manage, for example:
 
-In order to change the default of a feature, you need to edit the `kueue-controller-manager` deployment within the kueue installation namespace and change the `manager` container arguments to include
+```
+apiVersion: v1
+data:
+  controller_manager_config.yaml: |
```

  ```suggestion
  apiVersion: v1
  kind: ConfigMap
  metadata:
    name: kueue-manager-config
    namespace: kueue-system
  data:
    controller_manager_config.yaml: |
  ```
  
  Can you follow the other block style?

- **site/content/en/docs/installation/_index.md:251** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-14T04:07:15Z
```diff
@@ -235,9 +235,25 @@ To install and configure Kueue with [Helm](https://helm.sh/), follow the [instru
 
 ## Change the feature gates configuration
 
-Kueue uses a similar mechanism to configure features as described in [Kubernetes Feature Gates](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates).
+You can edit the `kueue-manager-config` `ConfigMap` and add the feature gate you would like to manage, for example:
 
-In order to change the default of a feature, you need to edit the `kueue-controller-manager` deployment within the kueue installation namespace and change the `manager` container arguments to include
+```
+apiVersion: v1
+data:
+  controller_manager_config.yaml: |
+    apiVersion: config.kueue.x-k8s.io/v1beta1
+    kind: Configuration
+    featureGates:
+      ManagedJobsNamespaceSelectorAlwaysRespected: true
+  ...
+kind: ConfigMap
+  name: kueue-manager-config
+  namespace: kueue-system
```

  ```suggestion
  ```

- **site/content/en/docs/installation/_index.md:254** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-14T04:08:25Z
```diff
@@ -235,9 +235,25 @@ To install and configure Kueue with [Helm](https://helm.sh/), follow the [instru
 
 ## Change the feature gates configuration
 
-Kueue uses a similar mechanism to configure features as described in [Kubernetes Feature Gates](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates).
+You can edit the `kueue-manager-config` `ConfigMap` and add the feature gate you would like to manage, for example:
 
-In order to change the default of a feature, you need to edit the `kueue-controller-manager` deployment within the kueue installation namespace and change the `manager` container arguments to include
+```
+apiVersion: v1
+data:
+  controller_manager_config.yaml: |
+    apiVersion: config.kueue.x-k8s.io/v1beta1
+    kind: Configuration
+    featureGates:
+      ManagedJobsNamespaceSelectorAlwaysRespected: true
+  ...
+kind: ConfigMap
+  name: kueue-manager-config
+  namespace: kueue-system
+```
+
+After changing the `ConfigMap`, you need to delete the Pods related to `kueue-controller-manager` deployment to have the change enforced.
```

  ```suggestion
  After changing the `ConfigMap`, you need to rollout the Pods related to `kueue-controller-manager` deployment to have the change enforced.
  ```
  
  You do not need to delete Pods, you can roll out the kueue-controller-manager Pods.
  This is an important aspect in production.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-14T08:03:27Z

- **site/content/en/docs/installation/_index.md:238** by [@mimowo](https://github.com/mimowo) — 2025-11-14T08:03:27Z
```diff
@@ -235,9 +235,25 @@ To install and configure Kueue with [Helm](https://helm.sh/), follow the [instru
 
 ## Change the feature gates configuration
 
-Kueue uses a similar mechanism to configure features as described in [Kubernetes Feature Gates](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates).
```

  I think this sentence doesn't need removal. It is true regardless if the feature gate is enabled by config map, or via command line

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-14T08:06:09Z

- **site/content/en/docs/installation/_index.md:254** by [@mimowo](https://github.com/mimowo) — 2025-11-14T08:06:09Z
```diff
@@ -235,9 +235,25 @@ To install and configure Kueue with [Helm](https://helm.sh/), follow the [instru
 
 ## Change the feature gates configuration
 
-Kueue uses a similar mechanism to configure features as described in [Kubernetes Feature Gates](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates).
+You can edit the `kueue-manager-config` `ConfigMap` and add the feature gate you would like to manage, for example:
 
-In order to change the default of a feature, you need to edit the `kueue-controller-manager` deployment within the kueue installation namespace and change the `manager` container arguments to include
+```
+apiVersion: v1
+data:
+  controller_manager_config.yaml: |
+    apiVersion: config.kueue.x-k8s.io/v1beta1
+    kind: Configuration
+    featureGates:
+      ManagedJobsNamespaceSelectorAlwaysRespected: true
+  ...
+kind: ConfigMap
+  name: kueue-manager-config
+  namespace: kueue-system
+```
+
+After changing the `ConfigMap`, you need to delete the Pods related to `kueue-controller-manager` deployment to have the change enforced.
```

  Well, rollout deletes pods, just maintaining some availability.
  
  Maybe, "...you need to restart the `kueue-controller-manager` deployment to have the change enforced, for example using `kubectl rollout restart deploy kueue-controller-manager -nkueue-system`"

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-14T08:06:46Z

- **site/content/en/docs/installation/_index.md:256** by [@mimowo](https://github.com/mimowo) — 2025-11-14T08:06:46Z
```diff
@@ -235,9 +235,25 @@ To install and configure Kueue with [Helm](https://helm.sh/), follow the [instru
 
 ## Change the feature gates configuration
 
-Kueue uses a similar mechanism to configure features as described in [Kubernetes Feature Gates](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates).
+You can edit the `kueue-manager-config` `ConfigMap` and add the feature gate you would like to manage, for example:
 
-In order to change the default of a feature, you need to edit the `kueue-controller-manager` deployment within the kueue installation namespace and change the `manager` container arguments to include
+```
+apiVersion: v1
+data:
+  controller_manager_config.yaml: |
+    apiVersion: config.kueue.x-k8s.io/v1beta1
+    kind: Configuration
+    featureGates:
+      ManagedJobsNamespaceSelectorAlwaysRespected: true
+  ...
+kind: ConfigMap
+  name: kueue-manager-config
+  namespace: kueue-system
+```
+
+After changing the `ConfigMap`, you need to delete the Pods related to `kueue-controller-manager` deployment to have the change enforced.
+
+Alternatively, you can use a similar mechanism to configure features as described in [Kubernetes Feature Gates](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates). You need to edit the `kueue-controller-manager` deployment within the kueue installation namespace and change the `manager` container arguments to include
```

  ```suggestion
  Alternatively, you can edit the `kueue-controller-manager` deployment within the kueue installation namespace and change the `manager` container arguments to include
  ```

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2025-11-14T12:44:17Z

- **site/content/en/docs/installation/_index.md:238** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2025-11-14T12:44:17Z
```diff
@@ -235,9 +235,25 @@ To install and configure Kueue with [Helm](https://helm.sh/), follow the [instru
 
 ## Change the feature gates configuration
 
-Kueue uses a similar mechanism to configure features as described in [Kubernetes Feature Gates](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates).
```

  It was just moved to line 256

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-14T12:46:43Z

- **site/content/en/docs/installation/_index.md:238** by [@mimowo](https://github.com/mimowo) — 2025-11-14T12:46:43Z
```diff
@@ -235,9 +235,25 @@ To install and configure Kueue with [Helm](https://helm.sh/), follow the [instru
 
 ## Change the feature gates configuration
 
-Kueue uses a similar mechanism to configure features as described in [Kubernetes Feature Gates](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates).
```

  I prefer this sentence at the beginning TBH, in the current form. It is not alternative - we are using k8s mechanism of feature gates. There are just two ways to set them

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-14T13:37:03Z

/lgtm
/approve

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-11-14T23:29:08Z

- **site/content/en/docs/installation/_index.md:254** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-14T23:29:07Z
```diff
@@ -235,9 +235,25 @@ To install and configure Kueue with [Helm](https://helm.sh/), follow the [instru
 
 ## Change the feature gates configuration
 
-Kueue uses a similar mechanism to configure features as described in [Kubernetes Feature Gates](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates).
+You can edit the `kueue-manager-config` `ConfigMap` and add the feature gate you would like to manage, for example:
 
-In order to change the default of a feature, you need to edit the `kueue-controller-manager` deployment within the kueue installation namespace and change the `manager` container arguments to include
+```
+apiVersion: v1
+data:
+  controller_manager_config.yaml: |
+    apiVersion: config.kueue.x-k8s.io/v1beta1
+    kind: Configuration
+    featureGates:
+      ManagedJobsNamespaceSelectorAlwaysRespected: true
+  ...
+kind: ConfigMap
+  name: kueue-manager-config
+  namespace: kueue-system
+```
+
+After changing the `ConfigMap`, you need to delete the Pods related to `kueue-controller-manager` deployment to have the change enforced.
```

  Yes, `kubectl rollout restart` is what I assumed.
  So, LGTM on `restart`.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-11-14T23:29:42Z

LGTM, thanks!
/lgtm
/approve
