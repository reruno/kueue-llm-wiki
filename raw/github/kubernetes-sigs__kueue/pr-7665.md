# PR #7665: Unblock scheduler after failed preemption(s)

**Summary**: Unblock scheduler after failed preemption(s)

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7665

**Last updated**: 2025-11-24T10:50:48Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@olekzabl](https://github.com/olekzabl)
- **Branch**: `br20` → `main`
- **Created**: 2025-11-14T13:07:22Z
- **Updated**: 2025-11-24T10:50:48Z
- **Merged**: 2025-11-21T16:12:35Z
- **Closed**: 2025-11-21T16:12:35Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@pajakd](https://github.com/pajakd), [@mbobrovskyi](https://github.com/mbobrovskyi), [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 8   **+147 / -17**

## Description

#### What type of PR is this?

/kind bug

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:

Fixes #5590

#### Special notes for your reviewer:

This PR is currently split into 2 commits:

* The first commit is small, and fixes the issue. \
  (In short, we're roughly aligned with @mimowo that it does. \
  For details, see the discussion in #5590, starting from [this comment](https://github.com/kubernetes-sigs/kueue/issues/5590#issuecomment-3532620377))

* The second commit is my attempt to give this fix any reasonable test coverage. \
  I chose integration tests (to verify the whole path of "preemption error -> retry -> desired outcome"). \
  However, for that, I needed _selectively injecting_ fake errors into K8s client, in integration tests, which AFAICS has been never done in Kueue. \
  Hence, I needed some custom tweaking of the test setup to achieve that.
  I'm curious about reviewers' opinions whether this is nice enough.

The test coverage may be not yet as full as I'd wish (for example, I haven't cared for testing the "preemption failed" vs. "sticky workloads" interaction); yet, as I'm hearing this issue gained some urgency, I'm un-drafting this PR now, hoping that it may be good to go as it is. Then, further tests may come in a follow-up PR. (Tracked in issue #7806).

#### Does this PR introduce a user-facing change?

```release-note
Fix a bug that an error during workload preemption could leave the scheduler stuck without retrying.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-11-14T13:07:30Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | caf3b8b972ae04d114dd675816ed6ded7cd129c7 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69204ad5c33d9d0008be211f |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-14T13:07:32Z

Hi @olekzabl. Thanks for your PR.

I'm waiting for a [github.com](https://kubernetes-sigs/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-14T15:59:45Z

/ok-to-test

### Comment by [@olekzabl](https://github.com/olekzabl) — 2025-11-18T23:21:46Z

@gabesaba @mbobrovskyi May I ask for a review at this point?

While this is still technically a draft (due to non-perfect test coverage), I'd like to verify my testing approach with you before I continue work on that. And the code written so far I've already tried to "polish" reasonably.

Please see the PR description for more context.

### Comment by [@olekzabl](https://github.com/olekzabl) — 2025-11-19T07:08:29Z

/test

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-19T07:08:32Z

@olekzabl: The `/test` command needs one or more targets.
The following commands are available to trigger required jobs:
```
/test pull-kueue-build-image-main
```
```
/test pull-kueue-test-e2e-certmanager-main
```
```
/test pull-kueue-test-e2e-customconfigs-main
```
```
/test pull-kueue-test-e2e-kueueviz-main
```
```
/test pull-kueue-test-e2e-main-1-32
```
```
/test pull-kueue-test-e2e-main-1-33
```
```
/test pull-kueue-test-e2e-main-1-34
```
```
/test pull-kueue-test-e2e-multikueue-main
```
```
/test pull-kueue-test-e2e-tas-main
```
```
/test pull-kueue-test-integration-baseline-main
```
```
/test pull-kueue-test-integration-extended-main
```
```
/test pull-kueue-test-integration-multikueue-main
```
```
/test pull-kueue-test-scheduling-perf-main
```
```
/test pull-kueue-test-unit-main
```
```
/test pull-kueue-verify-main
```

Use `/test all` to run all jobs.


<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7665#issuecomment-3551120357):

>/test


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@olekzabl](https://github.com/olekzabl) — 2025-11-19T07:08:43Z

/test all

### Comment by [@olekzabl](https://github.com/olekzabl) — 2025-11-19T07:51:46Z

/retest

### Comment by [@olekzabl](https://github.com/olekzabl) — 2025-11-19T08:02:49Z

/retest

### Comment by [@olekzabl](https://github.com/olekzabl) — 2025-11-20T09:10:22Z

Un-drafting now (see comment on the bottom of PR description)

/assign mimowo
as I heard you'd like to review

### Comment by [@olekzabl](https://github.com/olekzabl) — 2025-11-20T09:11:24Z

/unassign mimowo
/assign PBundyra

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-20T09:26:29Z

Leaving review to @PBundyra, but please set the release note.

### Comment by [@olekzabl](https://github.com/olekzabl) — 2025-11-20T10:43:29Z

> please set the release note.

Done.

### Comment by [@olekzabl](https://github.com/olekzabl) — 2025-11-20T21:08:57Z

@Singularity23x0 

> _After further study of the code - the ManagerSetup field already does what I described, just in a single method.
So going a step further, we could use managerSetup ...ManagerSetup_

For some reason I'm technically unable to reply to this comment "at its place", so I'll reply here:

It's same story as in [this thread](https://github.com/kubernetes-sigs/kueue/pull/7665#discussion_r2546506257) - `ManagerSetup` is a function which takes an already-created `Manager` as an argument, and at that point AFAIU it's too late to pass a `NewClient` to it. So I'm not following what I could do here.

### Comment by [@pajakd](https://github.com/pajakd) — 2025-11-21T09:26:36Z

Left a few non-blocking comments but overall lgtm

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-11-21T11:57:08Z

Nice, thanks!
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-21T11:57:15Z

LGTM label has been added.  <details>Git tree hash: d3284f65ff732b0c8d8308bb31a8f4bf98bf36ad</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-21T15:44:00Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7665#pullrequestreview-3493325053" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7665#" title="Author self-approved">olekzabl</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7665#pullrequestreview-3492228468" title="Approved">Singularity23x0</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-21T15:44:43Z

@olekzabl please prepare CPs manually in case the automated cherrypick fails

### Comment by [@olekzabl](https://github.com/olekzabl) — 2025-11-21T20:38:40Z

> @olekzabl please prepare CPs manually in case the automated cherrypick fails

Hmm, I don't see any activity from [k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) following your earlier cherrypick slash-commands. No idea why.

I'll retry these commands below, though not sure if I have permissions for this.

### Comment by [@olekzabl](https://github.com/olekzabl) — 2025-11-21T20:38:57Z

/cherrypick release-0.14

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-11-21T20:39:34Z

@olekzabl: #7665 failed to apply on top of branch "release-0.14":
```
Applying: Add `RequeueReasonPreemptionFailed`
Using index info to reconstruct a base tree...
M	pkg/cache/queue/cluster_queue.go
M	pkg/scheduler/preemption/preemption.go
M	pkg/scheduler/preemption/preemption_hierarchical_test.go
M	pkg/scheduler/preemption/preemption_test.go
M	pkg/scheduler/scheduler.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/scheduler/scheduler.go
CONFLICT (content): Merge conflict in pkg/scheduler/scheduler.go
Auto-merging pkg/scheduler/preemption/preemption_test.go
CONFLICT (content): Merge conflict in pkg/scheduler/preemption/preemption_test.go
Auto-merging pkg/scheduler/preemption/preemption_hierarchical_test.go
CONFLICT (content): Merge conflict in pkg/scheduler/preemption/preemption_hierarchical_test.go
Auto-merging pkg/scheduler/preemption/preemption.go
CONFLICT (content): Merge conflict in pkg/scheduler/preemption/preemption.go
Auto-merging pkg/cache/queue/cluster_queue.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 Add `RequeueReasonPreemptionFailed`

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7665#issuecomment-3564526948):

>/cherrypick release-0.14


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@olekzabl](https://github.com/olekzabl) — 2025-11-21T20:39:59Z

/cherrypick release-0.13

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-11-21T20:40:35Z

@olekzabl: #7665 failed to apply on top of branch "release-0.13":
```
Applying: Add `RequeueReasonPreemptionFailed`
Using index info to reconstruct a base tree...
A	pkg/cache/queue/cluster_queue.go
M	pkg/scheduler/preemption/preemption.go
M	pkg/scheduler/preemption/preemption_hierarchical_test.go
M	pkg/scheduler/preemption/preemption_test.go
M	pkg/scheduler/scheduler.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/scheduler/scheduler.go
CONFLICT (content): Merge conflict in pkg/scheduler/scheduler.go
Auto-merging pkg/scheduler/preemption/preemption_test.go
CONFLICT (content): Merge conflict in pkg/scheduler/preemption/preemption_test.go
Auto-merging pkg/scheduler/preemption/preemption_hierarchical_test.go
CONFLICT (content): Merge conflict in pkg/scheduler/preemption/preemption_hierarchical_test.go
Auto-merging pkg/scheduler/preemption/preemption.go
CONFLICT (content): Merge conflict in pkg/scheduler/preemption/preemption.go
Auto-merging pkg/queue/cluster_queue.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 Add `RequeueReasonPreemptionFailed`

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7665#issuecomment-3564530392):

>/cherrypick release-0.13


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2025-11-20T14:22:36Z

- **pkg/scheduler/preemption/preemption_hierarchical_test.go:1859** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-20T13:30:29Z
```diff
@@ -1856,13 +1856,16 @@ func TestHierarchicalPreemptions(t *testing.T) {
 			wlInfo := workload.NewInfo(tc.incoming)
 			wlInfo.ClusterQueue = tc.targetCQ
 			targets := preemptor.GetTargets(log, *wlInfo, tc.assignment, snapshotWorkingCopy)
-			preempted, err := preemptor.IssuePreemptions(ctx, wlInfo, targets, snapshotWorkingCopy.ClusterQueue(wlInfo.ClusterQueue))
+			preempted, failed, err := preemptor.IssuePreemptions(ctx, wlInfo, targets, snapshotWorkingCopy.ClusterQueue(wlInfo.ClusterQueue))
```

  Nit: Is this a specific case where "failed" is equal to the amount of errors? Or do we always expect the amount of errors to be equal to the amount of failed preemptions? If yes I'd vouch for using "failed" instead of "errors" to resolve this ambiguity, otherwise ignore.

- **test/integration/framework/framework.go:175** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-20T13:59:33Z
```diff
@@ -160,9 +172,13 @@ func (f *Framework) SetupClient(cfg *rest.Config) (context.Context, client.Clien
 	return ctx, k8sClient
 }
 
-func (f *Framework) StartManager(ctx context.Context, cfg *rest.Config, managerSetup ManagerSetup) {
+func (f *Framework) StartManager(ctx context.Context, cfg *rest.Config, managerSetup ManagerSetup, opts ...ManagerSetupOption) {
```

  Why are we using `ManagerSetupOption` defined as `type ManagerSetupOption func(*managerSetupOptions)
  `?
  
  We could just use `type ManagerSetupOption func(*manager.Options)` and later directly call
  
  ```
  for _, opt := range opts {
  			opt(&mgrOpts)
  		}
  ```

- **test/integration/singlecluster/scheduler/suite_test.go:140** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-20T14:05:58Z
```diff
@@ -100,3 +115,44 @@ func managerAndSchedulerSetup(ctx context.Context, mgr manager.Manager) {
 	err = sched.Start(ctx)
 	gomega.Expect(err).NotTo(gomega.HaveOccurred())
 }
+
+func setupInterceptedClient() (context.Context, client.Client) {
+	ctx, baseClient := fwk.SetupClient(cfg)
+	funcs := interceptor.Funcs{
+		SubResourcePatch: wrapFakeSubResourcePatch(&fakeSubResourcePatch, baseClient),
+	}
+	client := interceptor.NewClient(baseClient, funcs)
+	return ctx, client
+}
+
+func newInterceptedClient(config *rest.Config, options client.Options) (client.Client, error) {
+	baseClient, err := client.NewWithWatch(config, options)
+	if err != nil {
+		return nil, err
+	}
+	funcs := interceptor.Funcs{
+		SubResourcePatch: wrapFakeSubResourcePatch(&fakeSubResourcePatch, baseClient),
+	}
+	client := interceptor.NewClient(baseClient, funcs)
+	return client, nil
+}
+
+func wrapFakeSubResourcePatch(f *fakeClientCallSpec, baseK8sClient client.Client) func(context.Context, client.Client, string, client.Object, client.Patch, ...client.SubResourcePatchOption) error {
```

  For readability I recommend aliasing `func(context.Context, client.Client, string, client.Object, client.Patch, ...client.SubResourcePatchOption) error`.
  
  Overall it seems like this method signature is what is actually a SubResourcePatch, and the field we call `fakeSubResourcePatch` (in newInterceptedClient) is not a SubResourcePatch but rather  a fakeClientCallSpec, which is confusing.
  
  Id propose renaming the parameter accordingly (e.g. `fakeClientSubResourcePatchSpec`), aliasing the signature as `subResourcePatch` and renaming the method to `createFakeSubResourcePatch`.
  
  Alternatively we could reorganize to make `fakeSubResourcePatch` already be the func created with `wrapFakeSubResourcePatch`/`createFakeSubResourcePatch ` and pass it here directly. Then we would call this method in the "Should retry on failed preemptions" test itself.
  
  Either way I think some refactor is needed to improve the readability of this part. Thoughts?

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-11-20T14:28:19Z

- **pkg/scheduler/preemption/preemption_hierarchical_test.go:1859** by [@olekzabl](https://github.com/olekzabl) — 2025-11-20T14:28:19Z
```diff
@@ -1856,13 +1856,16 @@ func TestHierarchicalPreemptions(t *testing.T) {
 			wlInfo := workload.NewInfo(tc.incoming)
 			wlInfo.ClusterQueue = tc.targetCQ
 			targets := preemptor.GetTargets(log, *wlInfo, tc.assignment, snapshotWorkingCopy)
-			preempted, err := preemptor.IssuePreemptions(ctx, wlInfo, targets, snapshotWorkingCopy.ClusterQueue(wlInfo.ClusterQueue))
+			preempted, failed, err := preemptor.IssuePreemptions(ctx, wlInfo, targets, snapshotWorkingCopy.ClusterQueue(wlInfo.ClusterQueue))
```

  What do you mean by "errors"?
  
  To clarify: `err` returned by `IssuePreemptions` is not a list of errors; it's arbitrarily chosen one of them (if any), else `nil`.
  
  As for `failed`, it's the number of all failed preemptions, so not deducible from `err`.
  However, in fact, I don't need this `failed` for anything else than nicer logging.
  I can drop it completely if it's controversial.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-11-20T14:31:33Z

- **test/integration/framework/framework.go:175** by [@olekzabl](https://github.com/olekzabl) — 2025-11-20T14:31:32Z
```diff
@@ -160,9 +172,13 @@ func (f *Framework) SetupClient(cfg *rest.Config) (context.Context, client.Clien
 	return ctx, k8sClient
 }
 
-func (f *Framework) StartManager(ctx context.Context, cfg *rest.Config, managerSetup ManagerSetup) {
+func (f *Framework) StartManager(ctx context.Context, cfg *rest.Config, managerSetup ManagerSetup, opts ...ManagerSetupOption) {
```

  Indeed, we could. (TBH I overlooked that possibility).
  
  I'm hesitating if we should. 
  One hand, this shortens the code.
  On the other hand, this gives more exposition of all the fields of `manager.Options` down to our test setup.
  With the current structure of types, we keep it more blackboxed: only `NewClient` is "explicitly vetted" to be manipulated with; other fields of `manager.Options` may be also included in the future, but only upon an explicit broadening of `managerSetupOptions` (which we'd have to peer-review). I see some value in that.
  
  So I'm not sure, and I'm fine with either way.
  
  Any other thoughts?

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2025-11-20T14:57:01Z

- **pkg/scheduler/preemption/preemption_hierarchical_test.go:1859** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-20T14:57:00Z
```diff
@@ -1856,13 +1856,16 @@ func TestHierarchicalPreemptions(t *testing.T) {
 			wlInfo := workload.NewInfo(tc.incoming)
 			wlInfo.ClusterQueue = tc.targetCQ
 			targets := preemptor.GetTargets(log, *wlInfo, tc.assignment, snapshotWorkingCopy)
-			preempted, err := preemptor.IssuePreemptions(ctx, wlInfo, targets, snapshotWorkingCopy.ClusterQueue(wlInfo.ClusterQueue))
+			preempted, failed, err := preemptor.IssuePreemptions(ctx, wlInfo, targets, snapshotWorkingCopy.ClusterQueue(wlInfo.ClusterQueue))
```

  This one boils down to the signature, where the return values are `(preempted int, errors int, exampleError error)` - the second one is denoted as 'errors' and is later used to populate the 'failed' field, hence my question.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-11-20T15:03:29Z

- **test/integration/singlecluster/scheduler/suite_test.go:140** by [@olekzabl](https://github.com/olekzabl) — 2025-11-20T15:03:29Z
```diff
@@ -100,3 +115,44 @@ func managerAndSchedulerSetup(ctx context.Context, mgr manager.Manager) {
 	err = sched.Start(ctx)
 	gomega.Expect(err).NotTo(gomega.HaveOccurred())
 }
+
+func setupInterceptedClient() (context.Context, client.Client) {
+	ctx, baseClient := fwk.SetupClient(cfg)
+	funcs := interceptor.Funcs{
+		SubResourcePatch: wrapFakeSubResourcePatch(&fakeSubResourcePatch, baseClient),
+	}
+	client := interceptor.NewClient(baseClient, funcs)
+	return ctx, client
+}
+
+func newInterceptedClient(config *rest.Config, options client.Options) (client.Client, error) {
+	baseClient, err := client.NewWithWatch(config, options)
+	if err != nil {
+		return nil, err
+	}
+	funcs := interceptor.Funcs{
+		SubResourcePatch: wrapFakeSubResourcePatch(&fakeSubResourcePatch, baseClient),
+	}
+	client := interceptor.NewClient(baseClient, funcs)
+	return client, nil
+}
+
+func wrapFakeSubResourcePatch(f *fakeClientCallSpec, baseK8sClient client.Client) func(context.Context, client.Client, string, client.Object, client.Patch, ...client.SubResourcePatchOption) error {
```

  Good points, thanks.
  
  I followed your suggested direction, though a bit differently:
  
  > renaming the parameter accordingly
  
  Do you mean `f`? This is local (to this function), and I recall Go style likes short local names.
  For now I kept `f`.
  
  Or do you mean the global var `fakeSubResourcePatch`? That would make sense.
  I renamed that, though slightly differently (dropping `Client`).
  
  > aliasing the signature as `subResourcePatch`
  
  Done, though I added `Fn`. (Because "patch" is often meant as "the object (part) to be patched". While here we mean the function).
  
  > renaming the method to `createFakeSubResourcePatch`
  
  Done, though I changed the name. (`create...` is Java style; Go likes nouns more).
  
  ---
  
  PTAL

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-11-20T15:12:04Z

- **pkg/scheduler/preemption/preemption_hierarchical_test.go:1859** by [@olekzabl](https://github.com/olekzabl) — 2025-11-20T15:12:04Z
```diff
@@ -1856,13 +1856,16 @@ func TestHierarchicalPreemptions(t *testing.T) {
 			wlInfo := workload.NewInfo(tc.incoming)
 			wlInfo.ClusterQueue = tc.targetCQ
 			targets := preemptor.GetTargets(log, *wlInfo, tc.assignment, snapshotWorkingCopy)
-			preempted, err := preemptor.IssuePreemptions(ctx, wlInfo, targets, snapshotWorkingCopy.ClusterQueue(wlInfo.ClusterQueue))
+			preempted, failed, err := preemptor.IssuePreemptions(ctx, wlInfo, targets, snapshotWorkingCopy.ClusterQueue(wlInfo.ClusterQueue))
```

  Ah I see :+1: 
  Changed to `failedPreemptions`. (`failed` could sound like "failed workloads").

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2025-11-20T15:22:18Z

- **test/integration/singlecluster/scheduler/suite_test.go:78** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-20T15:21:05Z
```diff
@@ -62,14 +74,18 @@ var _ = ginkgo.BeforeSuite(func() {
 		WebhookPath: util.WebhookPath,
 	}
 	cfg = fwk.Init()
-	ctx, k8sClient = fwk.SetupClient(cfg)
-	fwk.StartManager(ctx, cfg, managerAndSchedulerSetup)
+	ctx, k8sClient = setupInterceptedClient()
+	fwk.StartManager(ctx, cfg, managerAndSchedulerSetup, framework.WithNewClient(newInterceptedClient))
```

  Alternatively:
  
  ```suggestion
  	fwk.StartManager(ctx, cfg, func (ctx context.Context, mgr manager.Manager) {
  		mgr.NewClient = newInterceptedClient
  		managerAndSchedulerSetup(ctx, mgr)
  	})
  ```

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2025-11-20T15:29:40Z

- **test/integration/singlecluster/scheduler/suite_test.go:140** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-20T15:29:40Z
```diff
@@ -100,3 +115,44 @@ func managerAndSchedulerSetup(ctx context.Context, mgr manager.Manager) {
 	err = sched.Start(ctx)
 	gomega.Expect(err).NotTo(gomega.HaveOccurred())
 }
+
+func setupInterceptedClient() (context.Context, client.Client) {
+	ctx, baseClient := fwk.SetupClient(cfg)
+	funcs := interceptor.Funcs{
+		SubResourcePatch: wrapFakeSubResourcePatch(&fakeSubResourcePatch, baseClient),
+	}
+	client := interceptor.NewClient(baseClient, funcs)
+	return ctx, client
+}
+
+func newInterceptedClient(config *rest.Config, options client.Options) (client.Client, error) {
+	baseClient, err := client.NewWithWatch(config, options)
+	if err != nil {
+		return nil, err
+	}
+	funcs := interceptor.Funcs{
+		SubResourcePatch: wrapFakeSubResourcePatch(&fakeSubResourcePatch, baseClient),
+	}
+	client := interceptor.NewClient(baseClient, funcs)
+	return client, nil
+}
+
+func wrapFakeSubResourcePatch(f *fakeClientCallSpec, baseK8sClient client.Client) func(context.Context, client.Client, string, client.Object, client.Patch, ...client.SubResourcePatchOption) error {
```

  > Or do you mean the global var fakeSubResourcePatch?
  
  A: Yes.
  
  Overall: LGTM

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2025-11-20T15:30:11Z

- **pkg/scheduler/preemption/preemption_hierarchical_test.go:1859** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-20T15:30:11Z
```diff
@@ -1856,13 +1856,16 @@ func TestHierarchicalPreemptions(t *testing.T) {
 			wlInfo := workload.NewInfo(tc.incoming)
 			wlInfo.ClusterQueue = tc.targetCQ
 			targets := preemptor.GetTargets(log, *wlInfo, tc.assignment, snapshotWorkingCopy)
-			preempted, err := preemptor.IssuePreemptions(ctx, wlInfo, targets, snapshotWorkingCopy.ClusterQueue(wlInfo.ClusterQueue))
+			preempted, failed, err := preemptor.IssuePreemptions(ctx, wlInfo, targets, snapshotWorkingCopy.ClusterQueue(wlInfo.ClusterQueue))
```

  LGTM

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-11-20T21:04:59Z

- **test/integration/singlecluster/scheduler/suite_test.go:78** by [@olekzabl](https://github.com/olekzabl) — 2025-11-20T21:04:59Z
```diff
@@ -62,14 +74,18 @@ var _ = ginkgo.BeforeSuite(func() {
 		WebhookPath: util.WebhookPath,
 	}
 	cfg = fwk.Init()
-	ctx, k8sClient = fwk.SetupClient(cfg)
-	fwk.StartManager(ctx, cfg, managerAndSchedulerSetup)
+	ctx, k8sClient = setupInterceptedClient()
+	fwk.StartManager(ctx, cfg, managerAndSchedulerSetup, framework.WithNewClient(newInterceptedClient))
```

  This doesn't build. `mgr` has no field `NewClient`. (I just tried it, to make sure).
  
  `NewClient` is a field in `manager.Options`, not in `Manager` itself. AFAIU we need to pass it to the "constructor" of our `Manager` instance. Trying to set it on an already-built `Manager` is too late (unless we create some new hooks even deeper down the code - however, that deeper layer is outside Kueue codebase...)

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-11-21T08:42:55Z

- **pkg/scheduler/scheduler.go:308** by [@pajakd](https://github.com/pajakd) — 2025-11-21T08:42:55Z
```diff
@@ -296,13 +296,16 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 		if e.assignment.RepresentativeMode() == flavorassigner.Preempt {
 			// If preemptions are issued, the next attempt should try all the flavors.
 			e.LastAssignment = nil
-			preempted, err := s.preemptor.IssuePreemptions(ctx, &e.Info, preemptionTargets, e.clusterQueueSnapshot)
+			preempted, errors, err := s.preemptor.IssuePreemptions(ctx, &e.Info, preemptionTargets, e.clusterQueueSnapshot)
 			if err != nil {
 				log.Error(err, "Failed to preempt workloads")
 			}
 			if preempted != 0 {
 				e.inadmissibleMsg += fmt.Sprintf(". Pending the preemption of %d workload(s)", preempted)
 				e.requeueReason = qcache.RequeueReasonPendingPreemption
+			} else {
+				e.inadmissibleMsg += fmt.Sprintf(". Preempting %d workload(s) failed, will retry.", errors)
+				e.requeueReason = qcache.RequeueReasonPreemptionFailed
```

  `else` ? we cannot have both `preempted = 0` and `errors = 0`, right?

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-11-21T08:56:02Z

- **pkg/scheduler/scheduler.go:308** by [@olekzabl](https://github.com/olekzabl) — 2025-11-21T08:56:02Z
```diff
@@ -296,13 +296,16 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 		if e.assignment.RepresentativeMode() == flavorassigner.Preempt {
 			// If preemptions are issued, the next attempt should try all the flavors.
 			e.LastAssignment = nil
-			preempted, err := s.preemptor.IssuePreemptions(ctx, &e.Info, preemptionTargets, e.clusterQueueSnapshot)
+			preempted, errors, err := s.preemptor.IssuePreemptions(ctx, &e.Info, preemptionTargets, e.clusterQueueSnapshot)
 			if err != nil {
 				log.Error(err, "Failed to preempt workloads")
 			}
 			if preempted != 0 {
 				e.inadmissibleMsg += fmt.Sprintf(". Pending the preemption of %d workload(s)", preempted)
 				e.requeueReason = qcache.RequeueReasonPendingPreemption
+			} else {
+				e.inadmissibleMsg += fmt.Sprintf(". Preempting %d workload(s) failed, will retry.", errors)
+				e.requeueReason = qcache.RequeueReasonPreemptionFailed
```

  Good question. For some reason I thought we can't but, even if correct, it depends on other functions etc.
  
  So - it seems good to express our intent explicitly. Done.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-11-21T08:57:36Z

- **test/integration/framework/framework.go:175** by [@olekzabl](https://github.com/olekzabl) — 2025-11-21T08:57:36Z
```diff
@@ -160,9 +172,13 @@ func (f *Framework) SetupClient(cfg *rest.Config) (context.Context, client.Clien
 	return ctx, k8sClient
 }
 
-func (f *Framework) StartManager(ctx context.Context, cfg *rest.Config, managerSetup ManagerSetup) {
+func (f *Framework) StartManager(ctx context.Context, cfg *rest.Config, managerSetup ManagerSetup, opts ...ManagerSetupOption) {
```

  Ah, now I found a way to reply in-thread.
  See my earlier reply [here](https://github.com/kubernetes-sigs/kueue/pull/7665#issuecomment-3560057021).

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-11-21T09:17:43Z

- **pkg/scheduler/preemption/preemption_test.go:4156** by [@pajakd](https://github.com/pajakd) — 2025-11-21T09:17:43Z
```diff
@@ -4144,13 +4144,16 @@ func TestPreemption(t *testing.T) {
 			wlInfo := workload.NewInfo(tc.incoming)
 			wlInfo.ClusterQueue = tc.targetCQ
 			targets := preemptor.GetTargets(log, *wlInfo, tc.assignment, snapshotWorkingCopy)
-			preempted, err := preemptor.IssuePreemptions(ctx, wlInfo, targets, snapshotWorkingCopy.ClusterQueue(wlInfo.ClusterQueue))
+			preempted, failed, err := preemptor.IssuePreemptions(ctx, wlInfo, targets, snapshotWorkingCopy.ClusterQueue(wlInfo.ClusterQueue))
 			if err != nil {
 				t.Fatalf("Failed doing preemption")
 			}
 			if preempted != tc.wantPreempted {
 				t.Errorf("Reported %d preemptions, want %d", preempted, tc.wantPreempted)
 			}
+			if failed != 0 {
+				t.Errorf("Reported %d failed preemptions, want 0", failed)
+			}
```

  Have you considered adding unit test(s) for this change (here or in pkg/scheduler/scheduler_test.go)?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-11-21T09:23:58Z

- **test/integration/singlecluster/scheduler/preemption_test.go:159** by [@pajakd](https://github.com/pajakd) — 2025-11-21T09:23:57Z
```diff
@@ -144,6 +145,51 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, lowWl1, lowWl2)
 		})
 
+		ginkgo.It("Should retry on failed preemptions", func() {
+			attempt := 0
+			fakeSubResourcePatchSpec = func(obj client.Object) (fakeClientUsage, error) {
+				wl, ok := obj.(*kueue.Workload)
+				if !ok {
+					return fallThrough, nil
+				}
+				if cond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); cond == nil || cond.Status != metav1.ConditionTrue {
+					return fallThrough, nil
+				}
+				// Ignore patches triggered by util.FinishEvictionForWorkloads()
+				if cond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadQuotaReserved); cond != nil && cond.Status == metav1.ConditionFalse && cond.Message == "By test" {
```

  nit: if someone changes that "By test" message, we might start getting failures here? Perhaps add a constant to test/util/constants.go with this message.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-11-21T10:18:30Z

- **test/integration/singlecluster/scheduler/preemption_test.go:159** by [@olekzabl](https://github.com/olekzabl) — 2025-11-21T10:18:30Z
```diff
@@ -144,6 +145,51 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, lowWl1, lowWl2)
 		})
 
+		ginkgo.It("Should retry on failed preemptions", func() {
+			attempt := 0
+			fakeSubResourcePatchSpec = func(obj client.Object) (fakeClientUsage, error) {
+				wl, ok := obj.(*kueue.Workload)
+				if !ok {
+					return fallThrough, nil
+				}
+				if cond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); cond == nil || cond.Status != metav1.ConditionTrue {
+					return fallThrough, nil
+				}
+				// Ignore patches triggered by util.FinishEvictionForWorkloads()
+				if cond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadQuotaReserved); cond != nil && cond.Status == metav1.ConditionFalse && cond.Message == "By test" {
```

  I considered it - but then I noticed lots of `"by test"` literals spread across our tests.
  So I concluded adding such constant would go a bit against our current conventions.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-11-21T10:33:47Z

- **pkg/scheduler/preemption/preemption_test.go:4156** by [@olekzabl](https://github.com/olekzabl) — 2025-11-21T10:33:46Z
```diff
@@ -4144,13 +4144,16 @@ func TestPreemption(t *testing.T) {
 			wlInfo := workload.NewInfo(tc.incoming)
 			wlInfo.ClusterQueue = tc.targetCQ
 			targets := preemptor.GetTargets(log, *wlInfo, tc.assignment, snapshotWorkingCopy)
-			preempted, err := preemptor.IssuePreemptions(ctx, wlInfo, targets, snapshotWorkingCopy.ClusterQueue(wlInfo.ClusterQueue))
+			preempted, failed, err := preemptor.IssuePreemptions(ctx, wlInfo, targets, snapshotWorkingCopy.ClusterQueue(wlInfo.ClusterQueue))
 			if err != nil {
 				t.Fatalf("Failed doing preemption")
 			}
 			if preempted != tc.wantPreempted {
 				t.Errorf("Reported %d preemptions, want %d", preempted, tc.wantPreempted)
 			}
+			if failed != 0 {
+				t.Errorf("Reported %d failed preemptions, want 0", failed)
+			}
```

  I considered - but (as mentioned in description of this PR) I started with smaller coverage due to reported urgency of this PR.
  
  Since you ask about it, I opened a follow-up issue: #7806

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2025-11-21T10:47:35Z

- **test/integration/singlecluster/scheduler/suite_test.go:78** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-21T10:47:34Z
```diff
@@ -62,14 +74,18 @@ var _ = ginkgo.BeforeSuite(func() {
 		WebhookPath: util.WebhookPath,
 	}
 	cfg = fwk.Init()
-	ctx, k8sClient = fwk.SetupClient(cfg)
-	fwk.StartManager(ctx, cfg, managerAndSchedulerSetup)
+	ctx, k8sClient = setupInterceptedClient()
+	fwk.StartManager(ctx, cfg, managerAndSchedulerSetup, framework.WithNewClient(newInterceptedClient))
```

  Ahh I see.

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (approved) — 2025-11-21T10:52:07Z

- **test/integration/framework/framework.go:175** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-21T10:52:03Z
```diff
@@ -160,9 +172,13 @@ func (f *Framework) SetupClient(cfg *rest.Config) (context.Context, client.Clien
 	return ctx, k8sClient
 }
 
-func (f *Framework) StartManager(ctx context.Context, cfg *rest.Config, managerSetup ManagerSetup) {
+func (f *Framework) StartManager(ctx context.Context, cfg *rest.Config, managerSetup ManagerSetup, opts ...ManagerSetupOption) {
```

  Ok, i see the issue. 
  
  Well then as I stated - I think updating mgrOptions directly instead of using managerSetupOptions removes unnecessary busywork and the cost of direct access to options seems negligible given we are talking about tests.
  
  But leaving as is is also okay - leaving it up to you to decide.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-11-21T11:20:15Z

- **test/integration/framework/framework.go:175** by [@olekzabl](https://github.com/olekzabl) — 2025-11-21T11:20:15Z
```diff
@@ -160,9 +172,13 @@ func (f *Framework) SetupClient(cfg *rest.Config) (context.Context, client.Clien
 	return ctx, k8sClient
 }
 
-func (f *Framework) StartManager(ctx context.Context, cfg *rest.Config, managerSetup ManagerSetup) {
+func (f *Framework) StartManager(ctx context.Context, cfg *rest.Config, managerSetup ManagerSetup, opts ...ManagerSetupOption) {
```

  After thinking of it again - done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-21T15:43:49Z

Awesome, looks quite good 👍 
/approve
/cherrypick release-0.14
/cherrypick release-0.13

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T10:50:48Z

- **test/integration/singlecluster/scheduler/suite_test.go:120** by [@mimowo](https://github.com/mimowo) — 2025-11-24T10:50:48Z
```diff
@@ -100,3 +116,44 @@ func managerAndSchedulerSetup(ctx context.Context, mgr manager.Manager) {
 	err = sched.Start(ctx)
 	gomega.Expect(err).NotTo(gomega.HaveOccurred())
 }
+
+func setupInterceptedClient() (context.Context, client.Client) {
```

  btw, I think it is nice to see this PR paves the way for simulating API server errors. We have some older code which is not tested wrt error handling because we never added the mechanism. For example https://github.com/kubernetes-sigs/kueue/pull/7364, but many more in the past.
  
  Now I can refer to this PR as an example for covering error cases in integration tests. Thanks ! 
  
  cc @PBundyra @pajakd .
