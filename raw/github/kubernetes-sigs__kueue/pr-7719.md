# PR #7719: Use UpdateFunc type.

**Summary**: Use UpdateFunc type.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7719

**Last updated**: 2025-11-18T08:50:39Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `cleanup/use-function-types` → `main`
- **Created**: 2025-11-18T07:16:06Z
- **Updated**: 2025-11-18T08:50:39Z
- **Merged**: 2025-11-18T08:43:46Z
- **Closed**: 2025-11-18T08:43:47Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@gabesaba](https://github.com/gabesaba), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 2   **+35 / -30**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Use UpdateFunc type.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Prepare for #7709

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-11-18T07:16:13Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | f99e09c2874077e75c2627d8327aadb5638e1b22 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/691c2520fdac6e00084e69d8 |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-18T07:17:44Z

/cc @mimowo

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-18T07:51:34Z

LGTM label has been added.  <details>Git tree hash: f47135b380fc8c41cc8c3b4ce899634283870a57</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-18T07:51:35Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7719#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7719#pullrequestreview-3475971549" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-18T07:27:22Z

- **pkg/util/client/client.go:28** by [@mimowo](https://github.com/mimowo) — 2025-11-18T07:27:22Z
```diff
@@ -22,6 +22,11 @@ import (
 	"sigs.k8s.io/controller-runtime/pkg/client"
 )
 
+// UpdateFunc is a function that attempts to update a resource.
+// It returns the resulting object, a bool indicating whether an update was made,
+// and an error if any occurred.
+type UpdateFunc func() (client.Object, bool, error)
```

  ```suggestion
  type updateFunc func() (client.Object, bool, error)
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-18T07:28:25Z

- **pkg/util/client/client.go:161** by [@mimowo](https://github.com/mimowo) — 2025-11-18T07:28:25Z
```diff
@@ -143,30 +140,25 @@ func Patch(ctx context.Context, c client.Client, obj client.Object, update func(
 //   - By default, patches are generated in "strict" mode (Strict=true).
 //     This clears the ResourceVersion field in the original object to ensure
 //     it is always included in the generated patch.
-func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update func() (client.Object, bool, error), options ...PatchOption) error {
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update UpdateFunc, options ...PatchOption) error {
 	return patchCommon(obj, update, func(patch client.Patch) error {
 		return c.Status().Patch(ctx, obj, patch)
 	}, options...)
 }
 
-// getOriginalObject creates and returns a deep copy of the given client.Object.
-// The copy represents the "original" state of the object before applying any
-// modifications. This is typically used when generating a patch.
-//
-// If PatchOptions.Strict is set to true, the function clears the
-// ResourceVersion field on the copied object. This ensures that the
-// ResourceVersion is included in the generated patch, enforcing stricter
-// version handling during patch application.
-func getOriginalObject(obj client.Object, options PatchOptions) client.Object {
-	objOriginal := obj.DeepCopyObject().(client.Object)
-	if options.Strict {
-		// Clearing ResourceVersion from the original object to make sure it is included in the generated patch.
-		objOriginal.SetResourceVersion("")
+// patchFunc is a function that applies the given client.Patch to a resource.
+// It returns an error if the patch could not be applied.
+type patchFunc func(patch client.Patch) error
+
+func patchCommon(obj client.Object, updateFn UpdateFunc, patchFn patchFunc, options ...PatchOption) error {
+	opts := DefaultPatchOptions()
+	for _, opt := range options {
+		opt(opts)
 	}
-	return objOriginal
+	return executePatch(obj, opts, updateFn, patchFn)
 }
 
-func updateAndPatch(obj client.Object, options PatchOptions, update func() (client.Object, bool, error), patchFn func(client.Patch) error) error {
+func executePatch(obj client.Object, options *PatchOptions, update UpdateFunc, patchFn patchFunc) error {
```

  why the ptr for PatchOptions? It doesn't see related or required

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-18T07:39:43Z

- **pkg/util/client/client.go:161** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-18T07:39:43Z
```diff
@@ -143,30 +140,25 @@ func Patch(ctx context.Context, c client.Client, obj client.Object, update func(
 //   - By default, patches are generated in "strict" mode (Strict=true).
 //     This clears the ResourceVersion field in the original object to ensure
 //     it is always included in the generated patch.
-func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update func() (client.Object, bool, error), options ...PatchOption) error {
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update UpdateFunc, options ...PatchOption) error {
 	return patchCommon(obj, update, func(patch client.Patch) error {
 		return c.Status().Patch(ctx, obj, patch)
 	}, options...)
 }
 
-// getOriginalObject creates and returns a deep copy of the given client.Object.
-// The copy represents the "original" state of the object before applying any
-// modifications. This is typically used when generating a patch.
-//
-// If PatchOptions.Strict is set to true, the function clears the
-// ResourceVersion field on the copied object. This ensures that the
-// ResourceVersion is included in the generated patch, enforcing stricter
-// version handling during patch application.
-func getOriginalObject(obj client.Object, options PatchOptions) client.Object {
-	objOriginal := obj.DeepCopyObject().(client.Object)
-	if options.Strict {
-		// Clearing ResourceVersion from the original object to make sure it is included in the generated patch.
-		objOriginal.SetResourceVersion("")
+// patchFunc is a function that applies the given client.Patch to a resource.
+// It returns an error if the patch could not be applied.
+type patchFunc func(patch client.Patch) error
+
+func patchCommon(obj client.Object, updateFn UpdateFunc, patchFn patchFunc, options ...PatchOption) error {
+	opts := DefaultPatchOptions()
+	for _, opt := range options {
+		opt(opts)
 	}
-	return objOriginal
+	return executePatch(obj, opts, updateFn, patchFn)
 }
 
-func updateAndPatch(obj client.Object, options PatchOptions, update func() (client.Object, bool, error), patchFn func(client.Patch) error) error {
+func executePatch(obj client.Object, options *PatchOptions, update UpdateFunc, patchFn patchFunc) error {
```

  DefaultPatchOptions returns a pointer of PatchOptions which is allocated in heap, so we don’t need to allocate new memory on the stack in each propagation — we can just use that pointer directly.
  
  https://github.com/kubernetes-sigs/kueue/blob/0a58caf2eeae9e1602730bad6d842adc900caa9b/pkg/util/client/client.go#L51-L55
  
  https://github.com/kubernetes-sigs/kueue/blob/0a58caf2eeae9e1602730bad6d842adc900caa9b/pkg/util/client/client.go#L75-L81

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-18T07:42:17Z

- **pkg/util/client/client.go:161** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-18T07:42:16Z
```diff
@@ -143,30 +140,25 @@ func Patch(ctx context.Context, c client.Client, obj client.Object, update func(
 //   - By default, patches are generated in "strict" mode (Strict=true).
 //     This clears the ResourceVersion field in the original object to ensure
 //     it is always included in the generated patch.
-func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update func() (client.Object, bool, error), options ...PatchOption) error {
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update UpdateFunc, options ...PatchOption) error {
 	return patchCommon(obj, update, func(patch client.Patch) error {
 		return c.Status().Patch(ctx, obj, patch)
 	}, options...)
 }
 
-// getOriginalObject creates and returns a deep copy of the given client.Object.
-// The copy represents the "original" state of the object before applying any
-// modifications. This is typically used when generating a patch.
-//
-// If PatchOptions.Strict is set to true, the function clears the
-// ResourceVersion field on the copied object. This ensures that the
-// ResourceVersion is included in the generated patch, enforcing stricter
-// version handling during patch application.
-func getOriginalObject(obj client.Object, options PatchOptions) client.Object {
-	objOriginal := obj.DeepCopyObject().(client.Object)
-	if options.Strict {
-		// Clearing ResourceVersion from the original object to make sure it is included in the generated patch.
-		objOriginal.SetResourceVersion("")
+// patchFunc is a function that applies the given client.Patch to a resource.
+// It returns an error if the patch could not be applied.
+type patchFunc func(patch client.Patch) error
+
+func patchCommon(obj client.Object, updateFn UpdateFunc, patchFn patchFunc, options ...PatchOption) error {
+	opts := DefaultPatchOptions()
+	for _, opt := range options {
+		opt(opts)
 	}
-	return objOriginal
+	return executePatch(obj, opts, updateFn, patchFn)
 }
 
-func updateAndPatch(obj client.Object, options PatchOptions, update func() (client.Object, bool, error), patchFn func(client.Patch) error) error {
+func executePatch(obj client.Object, options *PatchOptions, update UpdateFunc, patchFn patchFunc) error {
```

  But you’re right — these changes aren’t related to this PR. I can move them to a separate one.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-18T07:43:44Z

- **pkg/util/client/client.go:161** by [@mimowo](https://github.com/mimowo) — 2025-11-18T07:43:44Z
```diff
@@ -143,30 +140,25 @@ func Patch(ctx context.Context, c client.Client, obj client.Object, update func(
 //   - By default, patches are generated in "strict" mode (Strict=true).
 //     This clears the ResourceVersion field in the original object to ensure
 //     it is always included in the generated patch.
-func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update func() (client.Object, bool, error), options ...PatchOption) error {
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update UpdateFunc, options ...PatchOption) error {
 	return patchCommon(obj, update, func(patch client.Patch) error {
 		return c.Status().Patch(ctx, obj, patch)
 	}, options...)
 }
 
-// getOriginalObject creates and returns a deep copy of the given client.Object.
-// The copy represents the "original" state of the object before applying any
-// modifications. This is typically used when generating a patch.
-//
-// If PatchOptions.Strict is set to true, the function clears the
-// ResourceVersion field on the copied object. This ensures that the
-// ResourceVersion is included in the generated patch, enforcing stricter
-// version handling during patch application.
-func getOriginalObject(obj client.Object, options PatchOptions) client.Object {
-	objOriginal := obj.DeepCopyObject().(client.Object)
-	if options.Strict {
-		// Clearing ResourceVersion from the original object to make sure it is included in the generated patch.
-		objOriginal.SetResourceVersion("")
+// patchFunc is a function that applies the given client.Patch to a resource.
+// It returns an error if the patch could not be applied.
+type patchFunc func(patch client.Patch) error
+
+func patchCommon(obj client.Object, updateFn UpdateFunc, patchFn patchFunc, options ...PatchOption) error {
+	opts := DefaultPatchOptions()
+	for _, opt := range options {
+		opt(opts)
 	}
-	return objOriginal
+	return executePatch(obj, opts, updateFn, patchFn)
 }
 
-func updateAndPatch(obj client.Object, options PatchOptions, update func() (client.Object, bool, error), patchFn func(client.Patch) error) error {
+func executePatch(obj client.Object, options *PatchOptions, update UpdateFunc, patchFn patchFunc) error {
```

  > DefaultPatchOptions returns a pointer of PatchOptions which is allocated in heap, so we don’t need to allocate new memory on the stack in each propagation — we can just use that pointer directly.
  
  I see, but I still think it is better to avoid premature optimization here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-18T07:43:57Z

- **pkg/util/client/client.go:28** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-18T07:43:57Z
```diff
@@ -22,6 +22,11 @@ import (
 	"sigs.k8s.io/controller-runtime/pkg/client"
 )
 
+// UpdateFunc is a function that attempts to update a resource.
+// It returns the resulting object, a bool indicating whether an update was made,
+// and an error if any occurred.
+type UpdateFunc func() (client.Object, bool, error)
```

  I think we can set the external type here, since it’s a library and a user-facing type.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-18T07:47:42Z

- **pkg/util/client/client.go:28** by [@mimowo](https://github.com/mimowo) — 2025-11-18T07:47:41Z
```diff
@@ -22,6 +22,11 @@ import (
 	"sigs.k8s.io/controller-runtime/pkg/client"
 )
 
+// UpdateFunc is a function that attempts to update a resource.
+// It returns the resulting object, a bool indicating whether an update was made,
+// and an error if any occurred.
+type UpdateFunc func() (client.Object, bool, error)
```

  I'm not sure, we can do that when needed later.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-18T07:51:26Z

/lgtm
/approve

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-18T07:51:47Z

- **pkg/util/client/client.go:28** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-18T07:51:46Z
```diff
@@ -22,6 +22,11 @@ import (
 	"sigs.k8s.io/controller-runtime/pkg/client"
 )
 
+// UpdateFunc is a function that attempts to update a resource.
+// It returns the resulting object, a bool indicating whether an update was made,
+// and an error if any occurred.
+type UpdateFunc func() (client.Object, bool, error)
```

  The same applies to `PatchOptions`:
  
  ```go
  type PatchOptions struct {
  	Strict bool
  }
  ```
  
  So we also need to set the internal type. We’re not using it externally anyway, but it’s still public.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-18T07:51:56Z

- **pkg/util/client/client.go:28** by [@mimowo](https://github.com/mimowo) — 2025-11-18T07:51:56Z
```diff
@@ -22,6 +22,11 @@ import (
 	"sigs.k8s.io/controller-runtime/pkg/client"
 )
 
+// UpdateFunc is a function that attempts to update a resource.
+// It returns the resulting object, a bool indicating whether an update was made,
+// and an error if any occurred.
+type UpdateFunc func() (client.Object, bool, error)
```

  Ok, merging as is, this is not that important anyway, no strong view here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-18T07:52:09Z

- **pkg/util/client/client.go:161** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-18T07:52:09Z
```diff
@@ -143,30 +140,25 @@ func Patch(ctx context.Context, c client.Client, obj client.Object, update func(
 //   - By default, patches are generated in "strict" mode (Strict=true).
 //     This clears the ResourceVersion field in the original object to ensure
 //     it is always included in the generated patch.
-func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update func() (client.Object, bool, error), options ...PatchOption) error {
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update UpdateFunc, options ...PatchOption) error {
 	return patchCommon(obj, update, func(patch client.Patch) error {
 		return c.Status().Patch(ctx, obj, patch)
 	}, options...)
 }
 
-// getOriginalObject creates and returns a deep copy of the given client.Object.
-// The copy represents the "original" state of the object before applying any
-// modifications. This is typically used when generating a patch.
-//
-// If PatchOptions.Strict is set to true, the function clears the
-// ResourceVersion field on the copied object. This ensures that the
-// ResourceVersion is included in the generated patch, enforcing stricter
-// version handling during patch application.
-func getOriginalObject(obj client.Object, options PatchOptions) client.Object {
-	objOriginal := obj.DeepCopyObject().(client.Object)
-	if options.Strict {
-		// Clearing ResourceVersion from the original object to make sure it is included in the generated patch.
-		objOriginal.SetResourceVersion("")
+// patchFunc is a function that applies the given client.Patch to a resource.
+// It returns an error if the patch could not be applied.
+type patchFunc func(patch client.Patch) error
+
+func patchCommon(obj client.Object, updateFn UpdateFunc, patchFn patchFunc, options ...PatchOption) error {
+	opts := DefaultPatchOptions()
+	for _, opt := range options {
+		opt(opts)
 	}
-	return objOriginal
+	return executePatch(obj, opts, updateFn, patchFn)
 }
 
-func updateAndPatch(obj client.Object, options PatchOptions, update func() (client.Object, bool, error), patchFn func(client.Patch) error) error {
+func executePatch(obj client.Object, options *PatchOptions, update UpdateFunc, patchFn patchFunc) error {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-18T08:05:39Z

- **pkg/util/client/client.go:161** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-18T08:05:38Z
```diff
@@ -143,30 +140,25 @@ func Patch(ctx context.Context, c client.Client, obj client.Object, update func(
 //   - By default, patches are generated in "strict" mode (Strict=true).
 //     This clears the ResourceVersion field in the original object to ensure
 //     it is always included in the generated patch.
-func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update func() (client.Object, bool, error), options ...PatchOption) error {
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update UpdateFunc, options ...PatchOption) error {
 	return patchCommon(obj, update, func(patch client.Patch) error {
 		return c.Status().Patch(ctx, obj, patch)
 	}, options...)
 }
 
-// getOriginalObject creates and returns a deep copy of the given client.Object.
-// The copy represents the "original" state of the object before applying any
-// modifications. This is typically used when generating a patch.
-//
-// If PatchOptions.Strict is set to true, the function clears the
-// ResourceVersion field on the copied object. This ensures that the
-// ResourceVersion is included in the generated patch, enforcing stricter
-// version handling during patch application.
-func getOriginalObject(obj client.Object, options PatchOptions) client.Object {
-	objOriginal := obj.DeepCopyObject().(client.Object)
-	if options.Strict {
-		// Clearing ResourceVersion from the original object to make sure it is included in the generated patch.
-		objOriginal.SetResourceVersion("")
+// patchFunc is a function that applies the given client.Patch to a resource.
+// It returns an error if the patch could not be applied.
+type patchFunc func(patch client.Patch) error
+
+func patchCommon(obj client.Object, updateFn UpdateFunc, patchFn patchFunc, options ...PatchOption) error {
+	opts := DefaultPatchOptions()
+	for _, opt := range options {
+		opt(opts)
 	}
-	return objOriginal
+	return executePatch(obj, opts, updateFn, patchFn)
 }
 
-func updateAndPatch(obj client.Object, options PatchOptions, update func() (client.Object, bool, error), patchFn func(client.Patch) error) error {
+func executePatch(obj client.Object, options *PatchOptions, update UpdateFunc, patchFn patchFunc) error {
```

  Created separate PR for this https://github.com/kubernetes-sigs/kueue/pull/7721.
