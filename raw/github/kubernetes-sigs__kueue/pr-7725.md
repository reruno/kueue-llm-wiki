# PR #7725: simplify requeueIfNotPresent logic

**Summary**: simplify requeueIfNotPresent logic

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7725

**Last updated**: 2025-11-20T11:10:03Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ErikJiang](https://github.com/ErikJiang)
- **Branch**: `refactor-requeue-logic` → `main`
- **Created**: 2025-11-18T09:35:03Z
- **Updated**: 2025-11-20T11:10:03Z
- **Merged**: 2025-11-20T11:10:02Z
- **Closed**: 2025-11-20T11:10:02Z
- **Labels**: `kind/feature`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`, `size/S`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 1   **+13 / -6**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

cache `inadmissibleWorkloads[key]` in `requeueIfNotPresent` to avoid duplicate map lookups.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-11-18T09:35:09Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | de13a90cf6dff320b67c89bf0a8f524e74712a63 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/691eeb1a2d5d580008420886 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-20T09:59:36Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7725#" title="Author self-approved">ErikJiang</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7725#pullrequestreview-3486780435" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-20T09:59:36Z

LGTM label has been added.  <details>Git tree hash: d3c4a2ba2d0ceddd77eb2a61f077fe2b670e92cd</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-20T10:25:27Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-20T10:25:33Z

LGTM label has been added.  <details>Git tree hash: 659fee177bae6ddf11294b4a4cd092bf9884a43d</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-18T14:06:21Z

- **pkg/cache/queue/cluster_queue.go:301** by [@mimowo](https://github.com/mimowo) — 2025-11-18T14:06:21Z
```diff
@@ -284,22 +284,24 @@ func (c *ClusterQueue) requeueIfNotPresent(wInfo *workload.Info, immediate bool)
 	defer c.rwm.Unlock()
 	key := workload.Key(wInfo.Obj)
 	c.forgetInflightByKey(key)
+
+	inadmissibleWl, existsInInadmissible := c.inadmissibleWorkloads[key]
+
 	if c.backoffWaitingTimeExpired(wInfo) &&
 		(immediate || c.queueInadmissibleCycle >= c.popCycle || wInfo.LastAssignment.PendingFlavors()) {
 		// If the workload was inadmissible, move it back into the queue.
-		inadmissibleWl := c.inadmissibleWorkloads[key]
-		if inadmissibleWl != nil {
+		if existsInInadmissible {
 			wInfo = inadmissibleWl
 			delete(c.inadmissibleWorkloads, key)
 		}
```

  Actually, this might have a small semantic change, because technicall inadmissibleWorkloads[key] may have a value equal nil. Then we may get `inadmissibleWl =nil`, but `existsInInadmissible` as true.
  So it may require extra investigation to make sure the change is safe (we never have inadmissibleWorkloads[key] stored as nil). Let me know if you investigated the change is safe.

### Review by [@ErikJiang](https://github.com/ErikJiang) (commented) — 2025-11-19T02:56:34Z

- **pkg/cache/queue/cluster_queue.go:301** by [@ErikJiang](https://github.com/ErikJiang) — 2025-11-19T02:56:33Z
```diff
@@ -284,22 +284,24 @@ func (c *ClusterQueue) requeueIfNotPresent(wInfo *workload.Info, immediate bool)
 	defer c.rwm.Unlock()
 	key := workload.Key(wInfo.Obj)
 	c.forgetInflightByKey(key)
+
+	inadmissibleWl, existsInInadmissible := c.inadmissibleWorkloads[key]
+
 	if c.backoffWaitingTimeExpired(wInfo) &&
 		(immediate || c.queueInadmissibleCycle >= c.popCycle || wInfo.LastAssignment.PendingFlavors()) {
 		// If the workload was inadmissible, move it back into the queue.
-		inadmissibleWl := c.inadmissibleWorkloads[key]
-		if inadmissibleWl != nil {
+		if existsInInadmissible {
 			wInfo = inadmissibleWl
 			delete(c.inadmissibleWorkloads, key)
 		}
```

  Good catch! 
  After reviewing all write operations to the `inadmissibleWorkloads` map, I found that the likelihood of storing nil values is low. 
  However, I agree with your concern about the potential semantic difference. 
  I've added the `inadmissibleWl != nil` defensive check to maintain the same behavior as the original code. WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-20T06:39:14Z

- **pkg/cache/queue/cluster_queue.go:301** by [@mimowo](https://github.com/mimowo) — 2025-11-20T06:39:14Z
```diff
@@ -284,22 +284,24 @@ func (c *ClusterQueue) requeueIfNotPresent(wInfo *workload.Info, immediate bool)
 	defer c.rwm.Unlock()
 	key := workload.Key(wInfo.Obj)
 	c.forgetInflightByKey(key)
+
+	inadmissibleWl, existsInInadmissible := c.inadmissibleWorkloads[key]
+
 	if c.backoffWaitingTimeExpired(wInfo) &&
 		(immediate || c.queueInadmissibleCycle >= c.popCycle || wInfo.LastAssignment.PendingFlavors()) {
 		// If the workload was inadmissible, move it back into the queue.
-		inadmissibleWl := c.inadmissibleWorkloads[key]
-		if inadmissibleWl != nil {
+		if existsInInadmissible {
 			wInfo = inadmissibleWl
 			delete(c.inadmissibleWorkloads, key)
 		}
```

  yeah, this will work, but Im not sure we want that. Ideally we would not spread the checks.
  
   I'm wondering it might be better to have a helper function to get the element which would return if not nil. We could even consider encapsulating inadmissible workloads in a struct in the future

### Review by [@ErikJiang](https://github.com/ErikJiang) (commented) — 2025-11-20T08:35:54Z

- **pkg/cache/queue/cluster_queue.go:301** by [@ErikJiang](https://github.com/ErikJiang) — 2025-11-20T08:35:53Z
```diff
@@ -284,22 +284,24 @@ func (c *ClusterQueue) requeueIfNotPresent(wInfo *workload.Info, immediate bool)
 	defer c.rwm.Unlock()
 	key := workload.Key(wInfo.Obj)
 	c.forgetInflightByKey(key)
+
+	inadmissibleWl, existsInInadmissible := c.inadmissibleWorkloads[key]
+
 	if c.backoffWaitingTimeExpired(wInfo) &&
 		(immediate || c.queueInadmissibleCycle >= c.popCycle || wInfo.LastAssignment.PendingFlavors()) {
 		// If the workload was inadmissible, move it back into the queue.
-		inadmissibleWl := c.inadmissibleWorkloads[key]
-		if inadmissibleWl != nil {
+		if existsInInadmissible {
 			wInfo = inadmissibleWl
 			delete(c.inadmissibleWorkloads, key)
 		}
```

  Good call!
  I've centralized the nil check in a `getInadmissibleWorkload` helper function and updated all callers to use it.
  This avoids spreading the checks across multiple places.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-20T08:42:56Z

- **pkg/cache/queue/cluster_queue.go:279** by [@mimowo](https://github.com/mimowo) — 2025-11-20T08:42:56Z
```diff
@@ -274,6 +274,16 @@ func (c *ClusterQueue) DeleteFromLocalQueue(log logr.Logger, q *LocalQueue) {
 	}
 }
 
+// getInadmissibleWorkload retrieves a workload from inadmissibleWorkloads.
+// Returns the workload and true if it exists and is non-nil, otherwise returns nil and false.
+func (c *ClusterQueue) getInadmissibleWorkload(key workload.Reference) (*workload.Info, bool) {
```

  I wouldn't even return the bool at all.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-20T08:44:54Z

- **pkg/cache/queue/cluster_queue.go:279** by [@mimowo](https://github.com/mimowo) — 2025-11-20T08:44:55Z
```diff
@@ -274,6 +274,16 @@ func (c *ClusterQueue) DeleteFromLocalQueue(log logr.Logger, q *LocalQueue) {
 	}
 }
 
+// getInadmissibleWorkload retrieves a workload from inadmissibleWorkloads.
+// Returns the workload and true if it exists and is non-nil, otherwise returns nil and false.
+func (c *ClusterQueue) getInadmissibleWorkload(key workload.Reference) (*workload.Info, bool) {
```

  ```suggestion
  func (c *ClusterQueue) inadmissibleWorkload(key workload.Reference) *workload.Info {
  ```

### Review by [@ErikJiang](https://github.com/ErikJiang) (commented) — 2025-11-20T09:29:26Z

- **pkg/cache/queue/cluster_queue.go:279** by [@ErikJiang](https://github.com/ErikJiang) — 2025-11-20T09:29:26Z
```diff
@@ -274,6 +274,16 @@ func (c *ClusterQueue) DeleteFromLocalQueue(log logr.Logger, q *LocalQueue) {
 	}
 }
 
+// getInadmissibleWorkload retrieves a workload from inadmissibleWorkloads.
+// Returns the workload and true if it exists and is non-nil, otherwise returns nil and false.
+func (c *ClusterQueue) getInadmissibleWorkload(key workload.Reference) (*workload.Info, bool) {
```

  Thanks for the suggestion! 
  Just to clarify: without the bool return, wouldn't the helper function be the same as directly accessing `c.inadmissibleWorkloads[key]`?
  perhaps the helper isn't needed? 😂

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-20T09:36:45Z

- **pkg/cache/queue/cluster_queue.go:279** by [@mimowo](https://github.com/mimowo) — 2025-11-20T09:36:45Z
```diff
@@ -274,6 +274,16 @@ func (c *ClusterQueue) DeleteFromLocalQueue(log logr.Logger, q *LocalQueue) {
 	}
 }
 
+// getInadmissibleWorkload retrieves a workload from inadmissibleWorkloads.
+// Returns the workload and true if it exists and is non-nil, otherwise returns nil and false.
+func (c *ClusterQueue) getInadmissibleWorkload(key workload.Reference) (*workload.Info, bool) {
```

  Yes, it will do the same, but it will make a clear convention of using the function. Otherwise some places in code will check  wl != nil, some found=true, making different semantics in different places.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-20T09:39:18Z

- **pkg/cache/queue/cluster_queue.go:279** by [@mimowo](https://github.com/mimowo) — 2025-11-20T09:39:18Z
```diff
@@ -274,6 +274,16 @@ func (c *ClusterQueue) DeleteFromLocalQueue(log logr.Logger, q *LocalQueue) {
 	}
 }
 
+// getInadmissibleWorkload retrieves a workload from inadmissibleWorkloads.
+// Returns the workload and true if it exists and is non-nil, otherwise returns nil and false.
+func (c *ClusterQueue) getInadmissibleWorkload(key workload.Reference) (*workload.Info, bool) {
```

  Ultimately (feel free for follow up), I would like to have a structure inadmissibleWorkloads which encapsulates the logic, and exposes the function. Then, programmers will not have direct access to the map, which can be confusing. We do it similarly for PreemptedWorkloads which is just a thin wrapper for similar reasons: https://github.com/kubernetes-sigs/kueue/blob/main/pkg/scheduler/preemption/preempted_workloads.go

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-20T09:54:39Z

- **pkg/cache/queue/cluster_queue.go:283** by [@mimowo](https://github.com/mimowo) — 2025-11-20T09:54:39Z
```diff
@@ -274,6 +273,16 @@ func (c *ClusterQueue) DeleteFromLocalQueue(log logr.Logger, q *LocalQueue) {
 	}
 }
 
+// inadmissibleWorkload retrieves a workload from inadmissibleWorkloads.
+// Returns the workload if it exists and is non-nil, otherwise returns nil.
+func (c *ClusterQueue) inadmissibleWorkload(key workload.Reference) *workload.Info {
+	wl, exists := c.inadmissibleWorkloads[key]
+	if exists && wl != nil {
+		return wl
+	}
+	return nil
```

  ```suggestion
  	wl, _ := c.inadmissibleWorkloads[key]
  	return wl
  ```
  nit

### Review by [@ErikJiang](https://github.com/ErikJiang) (commented) — 2025-11-20T09:55:34Z

- **pkg/cache/queue/cluster_queue.go:279** by [@ErikJiang](https://github.com/ErikJiang) — 2025-11-20T09:55:34Z
```diff
@@ -274,6 +274,16 @@ func (c *ClusterQueue) DeleteFromLocalQueue(log logr.Logger, q *LocalQueue) {
 	}
 }
 
+// getInadmissibleWorkload retrieves a workload from inadmissibleWorkloads.
+// Returns the workload and true if it exists and is non-nil, otherwise returns nil and false.
+func (c *ClusterQueue) getInadmissibleWorkload(key workload.Reference) (*workload.Info, bool) {
```

  Got it, thanks for the patience in explaining this!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-20T09:59:28Z

/lgtm
/approve
Thank you 👍

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-20T10:14:19Z

- **pkg/cache/queue/cluster_queue.go:283** by [@mimowo](https://github.com/mimowo) — 2025-11-20T10:14:19Z
```diff
@@ -274,6 +273,16 @@ func (c *ClusterQueue) DeleteFromLocalQueue(log logr.Logger, q *LocalQueue) {
 	}
 }
 
+// inadmissibleWorkload retrieves a workload from inadmissibleWorkloads.
+// Returns the workload if it exists and is non-nil, otherwise returns nil.
+func (c *ClusterQueue) inadmissibleWorkload(key workload.Reference) *workload.Info {
+	wl, exists := c.inadmissibleWorkloads[key]
+	if exists && wl != nil {
+		return wl
+	}
+	return nil
```

  ups, please make linter happy https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7725/pull-kueue-verify-main/1991445821610004480

### Review by [@ErikJiang](https://github.com/ErikJiang) (commented) — 2025-11-20T10:19:39Z

- **pkg/cache/queue/cluster_queue.go:283** by [@ErikJiang](https://github.com/ErikJiang) — 2025-11-20T10:19:39Z
```diff
@@ -274,6 +273,16 @@ func (c *ClusterQueue) DeleteFromLocalQueue(log logr.Logger, q *LocalQueue) {
 	}
 }
 
+// inadmissibleWorkload retrieves a workload from inadmissibleWorkloads.
+// Returns the workload if it exists and is non-nil, otherwise returns nil.
+func (c *ClusterQueue) inadmissibleWorkload(key workload.Reference) *workload.Info {
+	wl, exists := c.inadmissibleWorkloads[key]
+	if exists && wl != nil {
+		return wl
+	}
+	return nil
```

  done.
