# PR #7734: Fix RequeueState clearing when backoff finishes

**Summary**: Fix RequeueState clearing when backoff finishes

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7734

**Last updated**: 2025-11-24T09:23:01Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@sohankunkerkar](https://github.com/sohankunkerkar)
- **Branch**: `fix-requeue-state` → `main`
- **Created**: 2025-11-18T17:27:08Z
- **Updated**: 2025-11-24T09:23:01Z
- **Merged**: 2025-11-20T08:42:04Z
- **Closed**: 2025-11-20T08:42:04Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92)
- **Changed files**: 2   **+77 / -10**

## Description

The fix clears workload requeue metadata via merge patch when deactivating. Also, added a regression test covering manual deactivation scenario.

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind bug
/kind cleanup

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes https://github.com/kubernetes-sigs/kueue/pull/7620#discussion_r2533493194

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Fix the bug that a workload which was deactivated by setting the `spec.active=false` would not have the 
`wl.Status.RequeueState` cleared.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-11-18T17:27:15Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | f97d3bb2dedcbd91dcab18e0c5dbea2239f5d238 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/691e352f27354900081db534 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-7734--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-7734--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTc3MzQtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.ZzxZ7Ia6mBT62MqDPPnVlpiIvVf8Tf0vgOWNDAlRcF8)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-7734--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-19T18:04:24Z

This requires a release note, proposal:
/release-note-edit
```release-note
Fix the bug that a workload which was deactivated by setting the `spec.active=false` would not have the 
`wl.Status.RequeueState` cleared.
```
Please prepare the cherrypicks.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-20T08:14:02Z

LGTM label has been added.  <details>Git tree hash: ed74d7313eb6b7980ff02e3011db1bda24b07bf2</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-20T08:14:02Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7734#pullrequestreview-3486247230" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7734#" title="Author self-approved">sohankunkerkar</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-19T16:48:41Z

- **pkg/controller/core/workload_controller.go:367** by [@mimowo](https://github.com/mimowo) — 2025-11-19T16:48:41Z
```diff
@@ -360,14 +361,10 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 				apimeta.RemoveStatusCondition(&wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
 			}
 			if wl.Status.RequeueState != nil {
-				if features.Enabled(features.WorkloadRequestUseMergePatch) {
-					// When using a merge patch, we can set the entire RequeueState to nil.
-					wl.Status.RequeueState = nil
-				} else {
-					// When using SSA, we must clear individual fields instead of setting to nil.
-					wl.Status.RequeueState.Count = nil
-					wl.Status.RequeueState.RequeueAt = nil
-				}
+				// Clear RequeueState using Merge Patch instead of SSA.
+				// SSA cannot delete a pointer field with json:",omitempty" when it's owned
+				// by a different field manager. Merge Patch handles this correctly.
+				wl.Status.RequeueState = nil
```

  As I discussed with @sohankunkerkar this is likely also a bugfix for 0.14 so I'm considering backporting. 
  
  @sohankunkerkar would the tests you add demonstrate the issue exists on 0.14?

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2025-11-19T17:47:30Z

- **pkg/controller/core/workload_controller.go:367** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-11-19T17:47:30Z
```diff
@@ -360,14 +361,10 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 				apimeta.RemoveStatusCondition(&wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
 			}
 			if wl.Status.RequeueState != nil {
-				if features.Enabled(features.WorkloadRequestUseMergePatch) {
-					// When using a merge patch, we can set the entire RequeueState to nil.
-					wl.Status.RequeueState = nil
-				} else {
-					// When using SSA, we must clear individual fields instead of setting to nil.
-					wl.Status.RequeueState.Count = nil
-					wl.Status.RequeueState.RequeueAt = nil
-				}
+				// Clear RequeueState using Merge Patch instead of SSA.
+				// SSA cannot delete a pointer field with json:",omitempty" when it's owned
+				// by a different field manager. Merge Patch handles this correctly.
+				wl.Status.RequeueState = nil
```

  @mimowo Yup, that's the case. I ran the same test on the `release-0.14` branch and the test fails without [this](https://github.com/kubernetes-sigs/kueue/pull/7734/files#diff-60dd240c20adbd6a189d018d1c216c2d296730f446c341d8bf449fa6657964ffR378) change.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2025-11-19T17:48:28Z

- **pkg/controller/core/workload_controller.go:367** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-11-19T17:48:28Z
```diff
@@ -360,14 +361,10 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 				apimeta.RemoveStatusCondition(&wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
 			}
 			if wl.Status.RequeueState != nil {
-				if features.Enabled(features.WorkloadRequestUseMergePatch) {
-					// When using a merge patch, we can set the entire RequeueState to nil.
-					wl.Status.RequeueState = nil
-				} else {
-					// When using SSA, we must clear individual fields instead of setting to nil.
-					wl.Status.RequeueState.Count = nil
-					wl.Status.RequeueState.RequeueAt = nil
-				}
+				// Clear RequeueState using Merge Patch instead of SSA.
+				// SSA cannot delete a pointer field with json:",omitempty" when it's owned
+				// by a different field manager. Merge Patch handles this correctly.
+				wl.Status.RequeueState = nil
```

  We need to tweak the parameters for `clientutil.PatchStatus`, so it's going to be a manual backport.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-19T18:04:35Z

- **pkg/controller/core/workload_controller.go:367** by [@mimowo](https://github.com/mimowo) — 2025-11-19T18:04:35Z
```diff
@@ -360,14 +361,10 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 				apimeta.RemoveStatusCondition(&wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
 			}
 			if wl.Status.RequeueState != nil {
-				if features.Enabled(features.WorkloadRequestUseMergePatch) {
-					// When using a merge patch, we can set the entire RequeueState to nil.
-					wl.Status.RequeueState = nil
-				} else {
-					// When using SSA, we must clear individual fields instead of setting to nil.
-					wl.Status.RequeueState.Count = nil
-					wl.Status.RequeueState.RequeueAt = nil
-				}
+				// Clear RequeueState using Merge Patch instead of SSA.
+				// SSA cannot delete a pointer field with json:",omitempty" when it's owned
+				// by a different field manager. Merge Patch handles this correctly.
+				wl.Status.RequeueState = nil
```

  sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-19T18:18:37Z

- **pkg/controller/core/workload_controller.go:370** by [@mimowo](https://github.com/mimowo) — 2025-11-19T18:18:38Z
```diff
@@ -360,14 +361,10 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 				apimeta.RemoveStatusCondition(&wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
 			}
 			if wl.Status.RequeueState != nil {
-				if features.Enabled(features.WorkloadRequestUseMergePatch) {
-					// When using a merge patch, we can set the entire RequeueState to nil.
-					wl.Status.RequeueState = nil
-				} else {
-					// When using SSA, we must clear individual fields instead of setting to nil.
-					wl.Status.RequeueState.Count = nil
-					wl.Status.RequeueState.RequeueAt = nil
-				}
+				// Clear RequeueState using Merge Patch instead of SSA.
+				// SSA cannot delete a pointer field with json:",omitempty" when it's owned
+				// by a different field manager. Merge Patch handles this correctly.
+				wl.Status.RequeueState = nil
 			}
 		}
 		if updated || wl.Status.RequeueState != nil {
```

  Can we simplify this as a small follow up? I think this is an omission from the previous PR because here we set `updated: true`: https://github.com/kubernetes-sigs/kueue/pull/7734/files#diff-60dd240c20adbd6a189d018d1c216c2d296730f446c341d8bf449fa6657964ffR356-R358
  
  wdyt @mbobrovskyi ?

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2025-11-19T21:20:13Z

- **pkg/controller/core/workload_controller.go:370** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-11-19T21:20:12Z
```diff
@@ -360,14 +361,10 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 				apimeta.RemoveStatusCondition(&wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
 			}
 			if wl.Status.RequeueState != nil {
-				if features.Enabled(features.WorkloadRequestUseMergePatch) {
-					// When using a merge patch, we can set the entire RequeueState to nil.
-					wl.Status.RequeueState = nil
-				} else {
-					// When using SSA, we must clear individual fields instead of setting to nil.
-					wl.Status.RequeueState.Count = nil
-					wl.Status.RequeueState.RequeueAt = nil
-				}
+				// Clear RequeueState using Merge Patch instead of SSA.
+				// SSA cannot delete a pointer field with json:",omitempty" when it's owned
+				// by a different field manager. Merge Patch handles this correctly.
+				wl.Status.RequeueState = nil
 			}
 		}
 		if updated || wl.Status.RequeueState != nil {
```

  Yup, that makes sense!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-20T08:13:52Z

/lgtm
/approve

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-20T09:08:46Z

- **pkg/controller/core/workload_controller.go:378** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-20T09:08:46Z
```diff
@@ -360,25 +361,21 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 				apimeta.RemoveStatusCondition(&wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
 			}
 			if wl.Status.RequeueState != nil {
-				if features.Enabled(features.WorkloadRequestUseMergePatch) {
-					// When using a merge patch, we can set the entire RequeueState to nil.
-					wl.Status.RequeueState = nil
-				} else {
-					// When using SSA, we must clear individual fields instead of setting to nil.
-					wl.Status.RequeueState.Count = nil
-					wl.Status.RequeueState.RequeueAt = nil
-				}
+				// Clear RequeueState using Merge Patch instead of SSA.
+				// SSA cannot delete a pointer field with json:",omitempty" when it's owned
+				// by a different field manager. Merge Patch handles this correctly.
+				wl.Status.RequeueState = nil
 			}
 		}
-		if updated || wl.Status.RequeueState != nil {
+		if updated {
 			if evicted {
 				if err := workload.Evict(ctx, r.client, r.recorder, &wl, reason, message, underlyingCause, r.clock, workload.WithCustomPrepare(prepare)); err != nil {
 					if !apierrors.IsNotFound(err) {
 						return ctrl.Result{}, fmt.Errorf("setting eviction: %w", err)
 					}
 				}
 			} else {
-				if err := workload.PatchAdmissionStatus(ctx, r.client, &wl, r.clock, func() (bool, error) {
+				if err := clientutil.PatchStatus(ctx, r.client, &wl, func() (bool, error) {
```

  Sorry, didn't got why this changes?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-20T09:11:13Z

- **pkg/controller/core/workload_controller.go:378** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-20T09:11:13Z
```diff
@@ -360,25 +361,21 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 				apimeta.RemoveStatusCondition(&wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
 			}
 			if wl.Status.RequeueState != nil {
-				if features.Enabled(features.WorkloadRequestUseMergePatch) {
-					// When using a merge patch, we can set the entire RequeueState to nil.
-					wl.Status.RequeueState = nil
-				} else {
-					// When using SSA, we must clear individual fields instead of setting to nil.
-					wl.Status.RequeueState.Count = nil
-					wl.Status.RequeueState.RequeueAt = nil
-				}
+				// Clear RequeueState using Merge Patch instead of SSA.
+				// SSA cannot delete a pointer field with json:",omitempty" when it's owned
+				// by a different field manager. Merge Patch handles this correctly.
+				wl.Status.RequeueState = nil
 			}
 		}
-		if updated || wl.Status.RequeueState != nil {
+		if updated {
 			if evicted {
 				if err := workload.Evict(ctx, r.client, r.recorder, &wl, reason, message, underlyingCause, r.clock, workload.WithCustomPrepare(prepare)); err != nil {
 					if !apierrors.IsNotFound(err) {
 						return ctrl.Result{}, fmt.Errorf("setting eviction: %w", err)
 					}
 				}
 			} else {
-				if err := workload.PatchAdmissionStatus(ctx, r.client, &wl, r.clock, func() (bool, error) {
+				if err := clientutil.PatchStatus(ctx, r.client, &wl, func() (bool, error) {
```

  Shouldn't we keep field owner on SSA?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-20T09:18:49Z

- **pkg/controller/core/workload_controller.go:381** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-20T09:18:49Z
```diff
@@ -360,25 +361,21 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 				apimeta.RemoveStatusCondition(&wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
 			}
 			if wl.Status.RequeueState != nil {
-				if features.Enabled(features.WorkloadRequestUseMergePatch) {
-					// When using a merge patch, we can set the entire RequeueState to nil.
-					wl.Status.RequeueState = nil
-				} else {
-					// When using SSA, we must clear individual fields instead of setting to nil.
-					wl.Status.RequeueState.Count = nil
-					wl.Status.RequeueState.RequeueAt = nil
-				}
+				// Clear RequeueState using Merge Patch instead of SSA.
+				// SSA cannot delete a pointer field with json:",omitempty" when it's owned
+				// by a different field manager. Merge Patch handles this correctly.
+				wl.Status.RequeueState = nil
 			}
 		}
-		if updated || wl.Status.RequeueState != nil {
+		if updated {
 			if evicted {
 				if err := workload.Evict(ctx, r.client, r.recorder, &wl, reason, message, underlyingCause, r.clock, workload.WithCustomPrepare(prepare)); err != nil {
 					if !apierrors.IsNotFound(err) {
 						return ctrl.Result{}, fmt.Errorf("setting eviction: %w", err)
 					}
 				}
 			} else {
-				if err := workload.PatchAdmissionStatus(ctx, r.client, &wl, r.clock, func() (bool, error) {
```

  We intentionally keep PatchAdmissionStatus to allow user choose between SSA and merge patch. I think we should revert this changes before fully migrate to merge patch.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T09:21:32Z

- **pkg/controller/core/workload_controller.go:378** by [@mimowo](https://github.com/mimowo) — 2025-11-24T09:21:32Z
```diff
@@ -360,25 +361,21 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 				apimeta.RemoveStatusCondition(&wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
 			}
 			if wl.Status.RequeueState != nil {
-				if features.Enabled(features.WorkloadRequestUseMergePatch) {
-					// When using a merge patch, we can set the entire RequeueState to nil.
-					wl.Status.RequeueState = nil
-				} else {
-					// When using SSA, we must clear individual fields instead of setting to nil.
-					wl.Status.RequeueState.Count = nil
-					wl.Status.RequeueState.RequeueAt = nil
-				}
+				// Clear RequeueState using Merge Patch instead of SSA.
+				// SSA cannot delete a pointer field with json:",omitempty" when it's owned
+				// by a different field manager. Merge Patch handles this correctly.
+				wl.Status.RequeueState = nil
 			}
 		}
-		if updated || wl.Status.RequeueState != nil {
+		if updated {
 			if evicted {
 				if err := workload.Evict(ctx, r.client, r.recorder, &wl, reason, message, underlyingCause, r.clock, workload.WithCustomPrepare(prepare)); err != nil {
 					if !apierrors.IsNotFound(err) {
 						return ctrl.Result{}, fmt.Errorf("setting eviction: %w", err)
 					}
 				}
 			} else {
-				if err := workload.PatchAdmissionStatus(ctx, r.client, &wl, r.clock, func() (bool, error) {
+				if err := clientutil.PatchStatus(ctx, r.client, &wl, func() (bool, error) {
```

  These changes are needed to clear the status.RequeueState. Otherwise the status would keep the old counter for status.RequeueState.Count and so re-activation of a workload would require the user to first clear the status, then change spec.active.
  
  However with SSA clearing the fields is not working if another controller takes over the field (I think that was ProvisioningRequest controller).
  
  > Shouldn't we keep field owner on SSA?
  
  I don't see a need, but let me know if I'm missing something.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T09:23:01Z

- **pkg/controller/core/workload_controller.go:381** by [@mimowo](https://github.com/mimowo) — 2025-11-24T09:23:00Z
```diff
@@ -360,25 +361,21 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 				apimeta.RemoveStatusCondition(&wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
 			}
 			if wl.Status.RequeueState != nil {
-				if features.Enabled(features.WorkloadRequestUseMergePatch) {
-					// When using a merge patch, we can set the entire RequeueState to nil.
-					wl.Status.RequeueState = nil
-				} else {
-					// When using SSA, we must clear individual fields instead of setting to nil.
-					wl.Status.RequeueState.Count = nil
-					wl.Status.RequeueState.RequeueAt = nil
-				}
+				// Clear RequeueState using Merge Patch instead of SSA.
+				// SSA cannot delete a pointer field with json:",omitempty" when it's owned
+				// by a different field manager. Merge Patch handles this correctly.
+				wl.Status.RequeueState = nil
 			}
 		}
-		if updated || wl.Status.RequeueState != nil {
+		if updated {
 			if evicted {
 				if err := workload.Evict(ctx, r.client, r.recorder, &wl, reason, message, underlyingCause, r.clock, workload.WithCustomPrepare(prepare)); err != nil {
 					if !apierrors.IsNotFound(err) {
 						return ctrl.Result{}, fmt.Errorf("setting eviction: %w", err)
 					}
 				}
 			} else {
-				if err := workload.PatchAdmissionStatus(ctx, r.client, &wl, r.clock, func() (bool, error) {
```

  This patch/apply is not used for admission / eviction. It is just used to clear the status.RequeueState. The new feature gate you are referring two is meant for status.Admission IIUC. We already have had updates to workloads in the past using Update or Patch in some scenarios.
