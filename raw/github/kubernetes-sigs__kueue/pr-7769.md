# PR #7769: Support RayJob InTreeAutoscaling: update RayCluster.IsTopLevel() and RayJob.Skip() (0.14 branch)

**Summary**: Support RayJob InTreeAutoscaling: update RayCluster.IsTopLevel() and RayJob.Skip() (0.14 branch)

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7769

**Last updated**: 2025-12-05T18:05:35Z

---

## Metadata

- **State**: closed
- **Author**: [@hiboyang](https://github.com/hiboyang)
- **Branch**: `release-0.14-bo2-dev` → `release-0.14`
- **Created**: 2025-11-19T22:45:12Z
- **Updated**: 2025-12-05T18:05:35Z
- **Merged**: —
- **Closed**: 2025-12-05T18:05:35Z
- **Labels**: `kind/feature`, `size/XL`, `ok-to-test`, `do-not-merge/release-note-label-needed`, `cncf-cla: yes`
- **Assignees**: [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 15   **+826 / -22**

## Description

#### What type of PR is this?

/kind feature

**This PR is replaced** by https://github.com/kubernetes-sigs/kueue/pull/8082, which is created based on `main` branch. We will continue the discussion in that new PR (https://github.com/kubernetes-sigs/kueue/pull/8082),

#### What this PR does / why we need it:

This PR is to provide code change to help discussion in
[Support RayJob InTreeAutoscaling](https://github.com/kubernetes-sigs/kueue/issues/7605)

#### Which issue(s) this PR fixes:

[Support RayJob InTreeAutoscaling](https://github.com/kubernetes-sigs/kueue/issues/7605)

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?

Yes, this PR tries to support RayJob InTreeAutoscaling, which was not supported previously.

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-19T22:45:15Z

Adding the "do-not-merge/release-note-label-needed" label because no release-note block was detected, please follow our [release note process](https://git.k8s.io/community/contributors/guide/release-notes.md) to remove it.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-19T22:45:20Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7769#" title="Author self-approved">hiboyang</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [gabesaba](https://github.com/gabesaba) for approval. For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/release-0.14/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["gabesaba"]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-19T22:45:23Z

Hi @hiboyang. Thanks for your PR.

I'm waiting for a [github.com](https://kubernetes-sigs/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-11-19T22:45:57Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 1d862c8422c51dd8524e76f21f5ac4d815f04c11 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6931b845e073660007ba84e0 |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-20T06:35:27Z

/assign @yaroslava-serdiuk 
ptal

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-20T10:04:11Z

/ok-to-test

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-11-23T17:02:39Z

/ok-to-test

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-11-23T17:14:49Z

/test pull-kueue-test-unit-release-0-14

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-11-23T17:16:49Z

/ok-to-test

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-11-23T21:56:55Z

/ok-to-test

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-11-24T01:16:22Z

/ok-to-test

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-11-24T03:19:54Z

/ok-to-test

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-11-24T03:38:43Z

/ok-to-test

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-12-02T18:38:53Z

/ok-to-test

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-12-03T06:39:02Z

/ok-to-test

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-12-03T19:30:23Z

/ok-to-test

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-12-03T20:30:24Z

/ok-to-test

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-12-03T21:03:40Z

/ok-to-test

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-12-03T22:56:56Z

/ok-to-test

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-12-03T23:11:06Z

/ok-to-test

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-12-03T23:41:46Z

/ok-to-test

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-12-04T00:06:43Z

/ok-to-test

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-12-04T00:35:40Z

Hi @yaroslava-serdiuk  @mimowo, I added more tests. Would you help to review and check whether we can merge it?

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-04T03:42:45Z

You are attempting to merge into the release-0.14 branch. Please rebase your changes onto main and set main as the target branch.

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-12-04T06:16:29Z

> You are attempting to merge into the release-0.14 branch. Please rebase your changes onto main and set main as the target branch.

Sounds good, let me rebase onto main, and update this PR later.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-04T19:16:47Z

@hiboyang: The following tests **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:

Test name | Commit | Details | Required | Rerun command
--- | --- | --- | --- | ---
pull-kueue-test-integration-multikueue-main | 1d862c8422c51dd8524e76f21f5ac4d815f04c11 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7769/pull-kueue-test-integration-multikueue-main/1996659213106941952) | true | `/test pull-kueue-test-integration-multikueue-main`
pull-kueue-test-integration-extended-main | 1d862c8422c51dd8524e76f21f5ac4d815f04c11 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7769/pull-kueue-test-integration-extended-main/1996659213010472960) | true | `/test pull-kueue-test-integration-extended-main`
pull-kueue-verify-main | 1d862c8422c51dd8524e76f21f5ac4d815f04c11 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7769/pull-kueue-verify-main/1996659216911175680) | true | `/test pull-kueue-verify-main`
pull-kueue-test-unit-main | 1d862c8422c51dd8524e76f21f5ac4d815f04c11 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7769/pull-kueue-test-unit-main/1996659212867866624) | true | `/test pull-kueue-test-unit-main`
pull-kueue-test-scheduling-perf-main | 1d862c8422c51dd8524e76f21f5ac4d815f04c11 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7769/pull-kueue-test-scheduling-perf-main/1996659219150934016) | true | `/test pull-kueue-test-scheduling-perf-main`
pull-kueue-test-integration-baseline-main | 1d862c8422c51dd8524e76f21f5ac4d815f04c11 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7769/pull-kueue-test-integration-baseline-main/1996659212930781184) | true | `/test pull-kueue-test-integration-baseline-main`
pull-kueue-populator-test-integration-main | 1d862c8422c51dd8524e76f21f5ac4d815f04c11 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7769/pull-kueue-populator-test-integration-main/1996659221680099328) | true | `/test pull-kueue-populator-test-integration-main`
pull-kueue-populator-test-unit-main | 1d862c8422c51dd8524e76f21f5ac4d815f04c11 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7769/pull-kueue-populator-test-unit-main/1996659220841238528) | true | `/test pull-kueue-populator-test-unit-main`
pull-kueue-build-image-main | 1d862c8422c51dd8524e76f21f5ac4d815f04c11 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7769/pull-kueue-build-image-main/1996659217729064960) | true | `/test pull-kueue-build-image-main`
pull-kueue-populator-verify-main | 1d862c8422c51dd8524e76f21f5ac4d815f04c11 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7769/pull-kueue-populator-verify-main/1996659219985600512) | true | `/test pull-kueue-populator-verify-main`
pull-kueue-test-e2e-main-1-32 | 1d862c8422c51dd8524e76f21f5ac4d815f04c11 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7769/pull-kueue-test-e2e-main-1-32/1996659213182439424) | true | `/test pull-kueue-test-e2e-main-1-32`
pull-kueue-test-e2e-main-1-33 | 1d862c8422c51dd8524e76f21f5ac4d815f04c11 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7769/pull-kueue-test-e2e-main-1-33/1996659213283102720) | true | `/test pull-kueue-test-e2e-main-1-33`
pull-kueue-test-e2e-main-1-34 | 1d862c8422c51dd8524e76f21f5ac4d815f04c11 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7769/pull-kueue-test-e2e-main-1-34/1996659213375377408) | true | `/test pull-kueue-test-e2e-main-1-34`
pull-kueue-test-e2e-upgrade-main | 1d862c8422c51dd8524e76f21f5ac4d815f04c11 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7769/pull-kueue-test-e2e-upgrade-main/1996659215212482560) | true | `/test pull-kueue-test-e2e-upgrade-main`
pull-kueue-test-e2e-certmanager-main | 1d862c8422c51dd8524e76f21f5ac4d815f04c11 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7769/pull-kueue-test-e2e-certmanager-main/1996659213731893248) | true | `/test pull-kueue-test-e2e-certmanager-main`
pull-kueue-test-e2e-multikueue-main | 1d862c8422c51dd8524e76f21f5ac4d815f04c11 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7769/pull-kueue-test-e2e-multikueue-main/1996659213480235008) | true | `/test pull-kueue-test-e2e-multikueue-main`
pull-kueue-test-e2e-customconfigs-main | 1d862c8422c51dd8524e76f21f5ac4d815f04c11 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7769/pull-kueue-test-e2e-customconfigs-main/1996659213635424256) | true | `/test pull-kueue-test-e2e-customconfigs-main`
pull-kueue-test-e2e-kueueviz-main | 1d862c8422c51dd8524e76f21f5ac4d815f04c11 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7769/pull-kueue-test-e2e-kueueviz-main/1996659214365233152) | true | `/test pull-kueue-test-e2e-kueueviz-main`
pull-kueue-populator-test-e2e-main | 1d862c8422c51dd8524e76f21f5ac4d815f04c11 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7769/pull-kueue-populator-test-e2e-main/1996659222493794304) | true | `/test pull-kueue-populator-test-e2e-main`
pull-kueue-test-e2e-tas-main | 1d862c8422c51dd8524e76f21f5ac4d815f04c11 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7769/pull-kueue-test-e2e-tas-main/1996659213564121088) | true | `/test pull-kueue-test-e2e-tas-main`
pull-kueue-test-e2e-certmanager-upgrade-main | 1d862c8422c51dd8524e76f21f5ac4d815f04c11 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7769/pull-kueue-test-e2e-certmanager-upgrade-main/1996659216055537664) | true | `/test pull-kueue-test-e2e-certmanager-upgrade-main`

[Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=7769). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3Ahiboyang). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository. I understand the commands that are listed [here](https://go.k8s.io/bot-commands).
</details>
<!-- test report -->

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-12-04T19:33:24Z

/ok-to-test

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-12-04T19:40:36Z

@mbobrovskyi I got a lot of conflicting when trying to rebase this PR on main branch. Thus created another PR based on main branch: https://github.com/kubernetes-sigs/kueue/pull/8082. Would you help to review/merge the new PR: https://github.com/kubernetes-sigs/kueue/pull/8082?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-12-05T08:36:01Z

@hiboyang that is ok, but it means losing discussion history. I think it should also be possible to change the target branch for this PR from "release-0.14" to "main" here: 
<img width="1243" height="150" alt="image" src="https://github.com/user-attachments/assets/d3451f38-bf84-4ec6-a0f1-eaf7a3e96ec0" />

Let us know your preference. I'm ok to also move my comments there.

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-12-05T17:48:15Z

> @hiboyang that is ok, but it means losing discussion history. I think it should also be possible to change the target branch for this PR from "release-0.14" to "main" here: <img alt="image" width="1243" height="150" src="https://private-user-images.githubusercontent.com/10359181/522848292-d3451f38-bf84-4ec6-a0f1-eaf7a3e96ec0.png?jwt=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9..dqVfA72iP-9ZdksvlcSgvlIjvJ8Csb0-tGBS9EmA9Kc">
> 
> Let us know your preference. I'm ok to also move my comments there.

I tried changing the target branch to be `main`, then this PR was messed up with a lot of changes from different places. Thus I reverted it back to set the target branch back to be `release-0.14`. We can keep discussion in that new https://github.com/kubernetes-sigs/kueue/pull/8082. Let me copy unaddressed comments to there from this PR and continue the discussion there.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-12-05T17:56:43Z

sgtm, feel free to close this one

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-12-05T18:05:35Z

We will continue the discussion in a new PR https://github.com/kubernetes-sigs/kueue/pull/8082, which is based on `main` branch. Close this one.

## Reviews

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2025-11-20T17:33:01Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:112** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2025-11-20T17:33:01Z
```diff
@@ -104,7 +105,15 @@ func (j *RayJob) Suspend() {
 func (j *RayJob) Skip() bool {
 	// Skip reconciliation for RayJobs that use clusterSelector to reference existing clusters.
 	// These jobs are not managed by Kueue.
-	return len(j.Spec.ClusterSelector) > 0
+	if len(j.Spec.ClusterSelector) > 0 {
+		return true
+	}
+	// Short term solution to support RayJob InTreeAutoscaling: https://github.com/kubernetes-sigs/kueue/issues/7605
+	if ptr.Deref(j.Spec.RayClusterSpec.EnableInTreeAutoscaling, false) &&
```

  The integration test failed because j.Spec.RayClusterSpec is nil

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2025-11-21T11:04:43Z

- **pkg/controller/jobs/raycluster/raycluster_controller.go:108** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-20T14:27:38Z
```diff
@@ -97,6 +97,17 @@ func (j *RayCluster) GVK() schema.GroupVersionKind {
 	return gvk
 }
 
+func (j *RayCluster) IsTopLevel() bool {
+	// Short term solution to support RayJob InTreeAutoscaling: https://github.com/kubernetes-sigs/kueue/issues/7605
+	if ptr.Deref(j.Spec.EnableInTreeAutoscaling, false) &&
+		jobframework.WorkloadSliceEnabled(j) &&
+		j.Labels[rayutils.RayOriginatedFromCRDLabelKey] == string(rayutils.RayJobCRD) {
+		return true
+	} else {
+		return false
+	}
```

  Why not?:
  
  ```suggestion
  	return ptr.Deref(j.Spec.EnableInTreeAutoscaling, false) &&
  		jobframework.WorkloadSliceEnabled(j) &&
  		j.Labels[rayutils.RayOriginatedFromCRDLabelKey] == string(rayutils.RayJobCRD) 
  ```

- **pkg/controller/jobframework/reconciler.go:642** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-21T11:04:00Z
```diff
@@ -630,7 +630,16 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if workloadSliceEnabled(job) {
 		// Start workload-slice schedule-gated pods (if any).
 		log.V(3).Info("Job running with admitted workload slice, start pods.")
-		return ctrl.Result{}, workloadslicing.StartWorkloadSlicePods(ctx, r.client, object)
+		// Check if job implements JobWithPodLabelSelector interface
+		var podLabelSelector string
+		if jobWithPodLabelSelector, ok := job.(JobWithPodLabelSelector); ok {
+			podLabelSelector = jobWithPodLabelSelector.PodLabelSelector()
+			log.V(10).Info("Using PodLabelSelector from job", "podLabelSelector", podLabelSelector)
+		} else {
+			log.V(10).Info("Not using PodLabelSelector from job")
+		}
+
+		return ctrl.Result{}, workloadslicing.StartWorkloadSlicePods(ctx, r.client, object, podLabelSelector)
```

  Is it possible that the label selector we get here is empty by design? Meaning we would want to StartWorkloadSlicePods with any set of labels? 
  If yes then the current logic may clash with this case due to defaulting to using the owner reference UID if the selector is empty.

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2025-11-23T16:22:49Z

- **pkg/controller/jobs/raycluster/raycluster_controller.go:108** by [@hiboyang](https://github.com/hiboyang) — 2025-11-23T16:22:48Z
```diff
@@ -97,6 +97,17 @@ func (j *RayCluster) GVK() schema.GroupVersionKind {
 	return gvk
 }
 
+func (j *RayCluster) IsTopLevel() bool {
+	// Short term solution to support RayJob InTreeAutoscaling: https://github.com/kubernetes-sigs/kueue/issues/7605
+	if ptr.Deref(j.Spec.EnableInTreeAutoscaling, false) &&
+		jobframework.WorkloadSliceEnabled(j) &&
+		j.Labels[rayutils.RayOriginatedFromCRDLabelKey] == string(rayutils.RayJobCRD) {
+		return true
+	} else {
+		return false
+	}
```

  Good suggestion, will do

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2025-11-23T16:23:58Z

- **pkg/controller/jobframework/reconciler.go:642** by [@hiboyang](https://github.com/hiboyang) — 2025-11-23T16:23:58Z
```diff
@@ -630,7 +630,16 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if workloadSliceEnabled(job) {
 		// Start workload-slice schedule-gated pods (if any).
 		log.V(3).Info("Job running with admitted workload slice, start pods.")
-		return ctrl.Result{}, workloadslicing.StartWorkloadSlicePods(ctx, r.client, object)
+		// Check if job implements JobWithPodLabelSelector interface
+		var podLabelSelector string
+		if jobWithPodLabelSelector, ok := job.(JobWithPodLabelSelector); ok {
+			podLabelSelector = jobWithPodLabelSelector.PodLabelSelector()
+			log.V(10).Info("Using PodLabelSelector from job", "podLabelSelector", podLabelSelector)
+		} else {
+			log.V(10).Info("Not using PodLabelSelector from job")
+		}
+
+		return ctrl.Result{}, workloadslicing.StartWorkloadSlicePods(ctx, r.client, object, podLabelSelector)
```

  Updated PR, do not need logic using pod selector anymore, can still use owner reference UID.

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2025-11-23T16:24:25Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:112** by [@hiboyang](https://github.com/hiboyang) — 2025-11-23T16:24:25Z
```diff
@@ -104,7 +105,15 @@ func (j *RayJob) Suspend() {
 func (j *RayJob) Skip() bool {
 	// Skip reconciliation for RayJobs that use clusterSelector to reference existing clusters.
 	// These jobs are not managed by Kueue.
-	return len(j.Spec.ClusterSelector) > 0
+	if len(j.Spec.ClusterSelector) > 0 {
+		return true
+	}
+	// Short term solution to support RayJob InTreeAutoscaling: https://github.com/kubernetes-sigs/kueue/issues/7605
+	if ptr.Deref(j.Spec.RayClusterSpec.EnableInTreeAutoscaling, false) &&
```

  Working on it

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T08:44:31Z

- **pkg/controller/jobframework/reconciler.go:398** by [@mimowo](https://github.com/mimowo) — 2025-11-24T08:44:31Z
```diff
@@ -374,7 +374,28 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if !isTopLevelJob {
 		_, _, finished := job.Finished()
 		if !finished && !job.IsSuspended() {
-			if ancestorWorkload, err := r.getWorkloadForObject(ctx, ancestorJob); err != nil {
+			var ancestorWorkload *kueue.Workload
+			var err error
+			ancestorGenericJob := CreateGenericJobFromRuntimeObject(ancestorJob)
+			if ancestorGenericJob != nil {
+				log.V(10).Info("Ancestor job converted to generic job, trying to convert it again to JobWithCustomWorkloadRetriever")
+				jobWithCustomWorkloadRetriever, ok := ancestorGenericJob.(JobWithCustomWorkloadRetriever)
+				if ok {
+					log.V(10).Info("Ancestor job converted to JobWithCustomWorkloadRetriever")
+					ancestorWorkload, err = jobWithCustomWorkloadRetriever.GetWorkload(ctx, r.client)
+					if err != nil {
+						log.Error(err, "couldn't get an ancestor job workload")
+						return ctrl.Result{}, err
+					}
+				} else {
+					log.V(10).Info("Ancestor job not converted to JobWithCustomWorkloadRetriever")
+					ancestorWorkload, err = r.getWorkloadForObject(ctx, ancestorJob)
+				}
+			} else {
+				log.V(10).Info("Ancestor job not converted to generic job", "ancestorJobName", ancestorJob.GetName())
+				ancestorWorkload, err = r.getWorkloadForObject(ctx, ancestorJob)
+			}
+			if err != nil {
```

  Are these code changes needed? Let me know what is the reason for introducing. I would think implementing the IsTopLevel is enough here.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2025-11-24T13:40:06Z

- **pkg/controller/jobframework/reconciler.go:382** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2025-11-24T13:40:06Z
```diff
@@ -374,7 +374,28 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if !isTopLevelJob {
 		_, _, finished := job.Finished()
 		if !finished && !job.IsSuspended() {
-			if ancestorWorkload, err := r.getWorkloadForObject(ctx, ancestorJob); err != nil {
+			var ancestorWorkload *kueue.Workload
+			var err error
+			ancestorGenericJob := CreateGenericJobFromRuntimeObject(ancestorJob)
+			if ancestorGenericJob != nil {
+				log.V(10).Info("Ancestor job converted to generic job, trying to convert it again to JobWithCustomWorkloadRetriever")
+				jobWithCustomWorkloadRetriever, ok := ancestorGenericJob.(JobWithCustomWorkloadRetriever)
```

  Does it work? My understanding is that for RayJob the `ancestorJob == nil` so it will result in `isTopLevelJob = true`, so I suppose this loop was not entered.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2025-11-24T13:46:05Z

- **pkg/controller/jobframework/reconciler.go:382** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2025-11-24T13:46:05Z
```diff
@@ -374,7 +374,28 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if !isTopLevelJob {
 		_, _, finished := job.Finished()
 		if !finished && !job.IsSuspended() {
-			if ancestorWorkload, err := r.getWorkloadForObject(ctx, ancestorJob); err != nil {
+			var ancestorWorkload *kueue.Workload
+			var err error
+			ancestorGenericJob := CreateGenericJobFromRuntimeObject(ancestorJob)
+			if ancestorGenericJob != nil {
+				log.V(10).Info("Ancestor job converted to generic job, trying to convert it again to JobWithCustomWorkloadRetriever")
+				jobWithCustomWorkloadRetriever, ok := ancestorGenericJob.(JobWithCustomWorkloadRetriever)
```

  We may just Skip() RayJob in order to not manage Workload for the RayJob, since the RayCluster is created.

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2025-11-24T15:58:54Z

- **pkg/controller/jobframework/reconciler.go:398** by [@hiboyang](https://github.com/hiboyang) — 2025-11-24T15:58:53Z
```diff
@@ -374,7 +374,28 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if !isTopLevelJob {
 		_, _, finished := job.Finished()
 		if !finished && !job.IsSuspended() {
-			if ancestorWorkload, err := r.getWorkloadForObject(ctx, ancestorJob); err != nil {
+			var ancestorWorkload *kueue.Workload
+			var err error
+			ancestorGenericJob := CreateGenericJobFromRuntimeObject(ancestorJob)
+			if ancestorGenericJob != nil {
+				log.V(10).Info("Ancestor job converted to generic job, trying to convert it again to JobWithCustomWorkloadRetriever")
+				jobWithCustomWorkloadRetriever, ok := ancestorGenericJob.(JobWithCustomWorkloadRetriever)
+				if ok {
+					log.V(10).Info("Ancestor job converted to JobWithCustomWorkloadRetriever")
+					ancestorWorkload, err = jobWithCustomWorkloadRetriever.GetWorkload(ctx, r.client)
+					if err != nil {
+						log.Error(err, "couldn't get an ancestor job workload")
+						return ctrl.Result{}, err
+					}
+				} else {
+					log.V(10).Info("Ancestor job not converted to JobWithCustomWorkloadRetriever")
+					ancestorWorkload, err = r.getWorkloadForObject(ctx, ancestorJob)
+				}
+			} else {
+				log.V(10).Info("Ancestor job not converted to generic job", "ancestorJobName", ancestorJob.GetName())
+				ancestorWorkload, err = r.getWorkloadForObject(ctx, ancestorJob)
+			}
+			if err != nil {
```

  Yes, this is needed. RayJob will create a Submitter batch/Job: "The submitter is a Kubernetes Job that runs ray job submit to submit a Ray job to the RayCluster", see details in [its page](https://docs.ray.io/en/latest/cluster/kubernetes/getting-started/rayjob-quick-start.html). When `reconciler.go` reconciles the Submitter Job, its ancestor is RayJob and cannot get workload from it, since the workload is created on the underlying RayCluster. In this case, have to get the workload from that RayCluster. The `JobWithCustomWorkloadRetriever` interface provides a way to get workload in this scenario.

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2025-11-24T16:01:49Z

- **pkg/controller/jobframework/reconciler.go:382** by [@hiboyang](https://github.com/hiboyang) — 2025-11-24T16:01:49Z
```diff
@@ -374,7 +374,28 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if !isTopLevelJob {
 		_, _, finished := job.Finished()
 		if !finished && !job.IsSuspended() {
-			if ancestorWorkload, err := r.getWorkloadForObject(ctx, ancestorJob); err != nil {
+			var ancestorWorkload *kueue.Workload
+			var err error
+			ancestorGenericJob := CreateGenericJobFromRuntimeObject(ancestorJob)
+			if ancestorGenericJob != nil {
+				log.V(10).Info("Ancestor job converted to generic job, trying to convert it again to JobWithCustomWorkloadRetriever")
+				jobWithCustomWorkloadRetriever, ok := ancestorGenericJob.(JobWithCustomWorkloadRetriever)
```

  See [previous comment](https://github.com/kubernetes-sigs/kueue/pull/7769#discussion_r2556853080). This is to reconcile Submitter Job from Ray: [this page](https://docs.ray.io/en/latest/cluster/kubernetes/getting-started/rayjob-quick-start.html).

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2025-11-24T18:39:23Z

- **pkg/controller/jobframework/reconciler.go:382** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2025-11-24T18:39:23Z
```diff
@@ -374,7 +374,28 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if !isTopLevelJob {
 		_, _, finished := job.Finished()
 		if !finished && !job.IsSuspended() {
-			if ancestorWorkload, err := r.getWorkloadForObject(ctx, ancestorJob); err != nil {
+			var ancestorWorkload *kueue.Workload
+			var err error
+			ancestorGenericJob := CreateGenericJobFromRuntimeObject(ancestorJob)
+			if ancestorGenericJob != nil {
+				log.V(10).Info("Ancestor job converted to generic job, trying to convert it again to JobWithCustomWorkloadRetriever")
+				jobWithCustomWorkloadRetriever, ok := ancestorGenericJob.(JobWithCustomWorkloadRetriever)
```

  From what I read the submitter job is a mechanism to inject pods to the RayCluster.
  The RayJob is ancestor to the RayCluster, the RayJob usually doesn't have ancestor. Unless there is a specific scenario when user create a specific Job that creates RayJobs.
  
  So for the change you provided: 
  1) if the ReconcileGenericJob is run for RayCluster: RayCluster has isTopLevel() method that return true and so it matches with workload that kueue created for RayCluster or it will create a workload if the workload wasn't created before.
  2) if the ReconcileGenericJob is run for RayJob: RayJob doesn't implement isTopLevel(), so then ancestor is searched which is usually nil and so RayJob will be treated as topLevel. And so there willl be another workload created for RayJob.
  
  If we have different workloads created for RayJob and RayCluster it will lead to quota issues.

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2025-11-25T00:46:23Z

- **pkg/controller/jobframework/reconciler.go:382** by [@hiboyang](https://github.com/hiboyang) — 2025-11-25T00:46:23Z
```diff
@@ -374,7 +374,28 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if !isTopLevelJob {
 		_, _, finished := job.Finished()
 		if !finished && !job.IsSuspended() {
-			if ancestorWorkload, err := r.getWorkloadForObject(ctx, ancestorJob); err != nil {
+			var ancestorWorkload *kueue.Workload
+			var err error
+			ancestorGenericJob := CreateGenericJobFromRuntimeObject(ancestorJob)
+			if ancestorGenericJob != nil {
+				log.V(10).Info("Ancestor job converted to generic job, trying to convert it again to JobWithCustomWorkloadRetriever")
+				jobWithCustomWorkloadRetriever, ok := ancestorGenericJob.(JobWithCustomWorkloadRetriever)
```

  > From what I read the submitter job is a mechanism to inject pods to the RayCluster. The RayJob is ancestor to the RayCluster, the RayJob usually doesn't have ancestor. Unless there is a specific scenario when user create a specific Job that creates RayJobs.
  > 
  > So for the change you provided:
  > 
  > 1. if the ReconcileGenericJob is run for RayCluster: RayCluster has isTopLevel() method that return true and so it matches with workload that kueue created for RayCluster or it will create a workload if the workload wasn't created before.
  > 2. if the ReconcileGenericJob is run for RayJob: RayJob doesn't implement isTopLevel(), so then ancestor is searched which is usually nil and so RayJob will be treated as topLevel. And so there willl be another workload created for RayJob.
  > 
  > If we have different workloads created for RayJob and RayCluster it will lead to quota issues.
  
  @yaroslava-serdiuk are you raising questions or want me to clarify? The `JobWithCustomWorkloadRetriever` is used in following scenario:
  1. Submit a RayJob with InTreeAutoscaling:true
  2. RayJob is skipped due to Skip() method and does not create workload
  3. RayJob creates RayCluster, which creates workload
  4. RayJob creates submitter job (batch/Job). This is Ray internal logic.
  5. The submitter job (batch/Job) triggers reconcile in Kueue. The submitter's ancestor is RayJob. Now RayJob implements `JobWithCustomWorkloadRetriever` and will get workload correctly. Without it, the submitter job will get workload from its ancestor (RayJob) by `ownerReferences`, which is wrong, since the workload is owned by underlying RayCluster (not RayJob).

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2025-11-25T09:34:14Z

- **pkg/controller/jobframework/reconciler.go:382** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2025-11-25T09:34:14Z
```diff
@@ -374,7 +374,28 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if !isTopLevelJob {
 		_, _, finished := job.Finished()
 		if !finished && !job.IsSuspended() {
-			if ancestorWorkload, err := r.getWorkloadForObject(ctx, ancestorJob); err != nil {
+			var ancestorWorkload *kueue.Workload
+			var err error
+			ancestorGenericJob := CreateGenericJobFromRuntimeObject(ancestorJob)
+			if ancestorGenericJob != nil {
+				log.V(10).Info("Ancestor job converted to generic job, trying to convert it again to JobWithCustomWorkloadRetriever")
+				jobWithCustomWorkloadRetriever, ok := ancestorGenericJob.(JobWithCustomWorkloadRetriever)
```

  I see, thanks for clarification.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2025-11-25T13:30:53Z

- **pkg/controller/jobframework/reconciler.go:382** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2025-11-25T13:30:53Z
```diff
@@ -374,7 +374,28 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if !isTopLevelJob {
 		_, _, finished := job.Finished()
 		if !finished && !job.IsSuspended() {
-			if ancestorWorkload, err := r.getWorkloadForObject(ctx, ancestorJob); err != nil {
+			var ancestorWorkload *kueue.Workload
+			var err error
+			ancestorGenericJob := CreateGenericJobFromRuntimeObject(ancestorJob)
+			if ancestorGenericJob != nil {
+				log.V(10).Info("Ancestor job converted to generic job, trying to convert it again to JobWithCustomWorkloadRetriever")
+				jobWithCustomWorkloadRetriever, ok := ancestorGenericJob.(JobWithCustomWorkloadRetriever)
```

  So with this change we ignore submitter job requests, which is probably fine since they are very small. I'm thinking whether it possible to simplify the change by simply disable RayJob integration.

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2025-11-25T17:16:47Z

- **pkg/controller/jobframework/reconciler.go:382** by [@hiboyang](https://github.com/hiboyang) — 2025-11-25T17:16:47Z
```diff
@@ -374,7 +374,28 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if !isTopLevelJob {
 		_, _, finished := job.Finished()
 		if !finished && !job.IsSuspended() {
-			if ancestorWorkload, err := r.getWorkloadForObject(ctx, ancestorJob); err != nil {
+			var ancestorWorkload *kueue.Workload
+			var err error
+			ancestorGenericJob := CreateGenericJobFromRuntimeObject(ancestorJob)
+			if ancestorGenericJob != nil {
+				log.V(10).Info("Ancestor job converted to generic job, trying to convert it again to JobWithCustomWorkloadRetriever")
+				jobWithCustomWorkloadRetriever, ok := ancestorGenericJob.(JobWithCustomWorkloadRetriever)
```

  Yeah, totally disabling RayJob integration is another option, probably should be the long term solution, which may involve more changes. We can explore that after getting current PR working.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2025-11-25T19:47:07Z

- **pkg/controller/jobframework/reconciler.go:382** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2025-11-25T19:47:07Z
```diff
@@ -374,7 +374,28 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if !isTopLevelJob {
 		_, _, finished := job.Finished()
 		if !finished && !job.IsSuspended() {
-			if ancestorWorkload, err := r.getWorkloadForObject(ctx, ancestorJob); err != nil {
+			var ancestorWorkload *kueue.Workload
+			var err error
+			ancestorGenericJob := CreateGenericJobFromRuntimeObject(ancestorJob)
+			if ancestorGenericJob != nil {
+				log.V(10).Info("Ancestor job converted to generic job, trying to convert it again to JobWithCustomWorkloadRetriever")
+				jobWithCustomWorkloadRetriever, ok := ancestorGenericJob.(JobWithCustomWorkloadRetriever)
```

  I mean to exclude RayJob in config map. In this case the ancestorJob will be nil for submitter job.

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2025-11-26T06:32:36Z

- **pkg/controller/jobframework/reconciler.go:382** by [@hiboyang](https://github.com/hiboyang) — 2025-11-26T06:32:36Z
```diff
@@ -374,7 +374,28 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if !isTopLevelJob {
 		_, _, finished := job.Finished()
 		if !finished && !job.IsSuspended() {
-			if ancestorWorkload, err := r.getWorkloadForObject(ctx, ancestorJob); err != nil {
+			var ancestorWorkload *kueue.Workload
+			var err error
+			ancestorGenericJob := CreateGenericJobFromRuntimeObject(ancestorJob)
+			if ancestorGenericJob != nil {
+				log.V(10).Info("Ancestor job converted to generic job, trying to convert it again to JobWithCustomWorkloadRetriever")
+				jobWithCustomWorkloadRetriever, ok := ancestorGenericJob.(JobWithCustomWorkloadRetriever)
```

  If we exclude RayJob in config map and the ancestorJob is nil for submitter job, the submitter job will become top level job due code like `isTopLevelJob = ancestorJob == nil`, will this cause issue?

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2025-11-27T08:30:23Z

- **pkg/controller/jobframework/reconciler.go:382** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2025-11-27T08:30:23Z
```diff
@@ -374,7 +374,28 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if !isTopLevelJob {
 		_, _, finished := job.Finished()
 		if !finished && !job.IsSuspended() {
-			if ancestorWorkload, err := r.getWorkloadForObject(ctx, ancestorJob); err != nil {
+			var ancestorWorkload *kueue.Workload
+			var err error
+			ancestorGenericJob := CreateGenericJobFromRuntimeObject(ancestorJob)
+			if ancestorGenericJob != nil {
+				log.V(10).Info("Ancestor job converted to generic job, trying to convert it again to JobWithCustomWorkloadRetriever")
+				jobWithCustomWorkloadRetriever, ok := ancestorGenericJob.(JobWithCustomWorkloadRetriever)
```

  Actually I see that submitter job will never have a [kueue label](https://github.com/ray-project/kuberay/blob/master/ray-operator/controllers/ray/rayjob_controller.go#L673), so if the ManageJobsWithoutQueueName is false (which should be), than kueue won't manage this job. 
  
  So the changes you added to handle submitter job are not needed.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2025-11-27T08:39:03Z

- **pkg/controller/jobframework/reconciler.go:382** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2025-11-27T08:39:02Z
```diff
@@ -374,7 +374,28 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if !isTopLevelJob {
 		_, _, finished := job.Finished()
 		if !finished && !job.IsSuspended() {
-			if ancestorWorkload, err := r.getWorkloadForObject(ctx, ancestorJob); err != nil {
+			var ancestorWorkload *kueue.Workload
+			var err error
+			ancestorGenericJob := CreateGenericJobFromRuntimeObject(ancestorJob)
+			if ancestorGenericJob != nil {
+				log.V(10).Info("Ancestor job converted to generic job, trying to convert it again to JobWithCustomWorkloadRetriever")
+				jobWithCustomWorkloadRetriever, ok := ancestorGenericJob.(JobWithCustomWorkloadRetriever)
```

  If we exclude RayJob in config map, than there would be no need to implement isTopLevel() for RayCluster. However, since we do not currently plan for RayJob deprecation, I would leave the implementation as it is.

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2025-11-29T01:58:36Z

- **pkg/controller/jobframework/reconciler.go:382** by [@hiboyang](https://github.com/hiboyang) — 2025-11-29T01:58:36Z
```diff
@@ -374,7 +374,28 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if !isTopLevelJob {
 		_, _, finished := job.Finished()
 		if !finished && !job.IsSuspended() {
-			if ancestorWorkload, err := r.getWorkloadForObject(ctx, ancestorJob); err != nil {
+			var ancestorWorkload *kueue.Workload
+			var err error
+			ancestorGenericJob := CreateGenericJobFromRuntimeObject(ancestorJob)
+			if ancestorGenericJob != nil {
+				log.V(10).Info("Ancestor job converted to generic job, trying to convert it again to JobWithCustomWorkloadRetriever")
+				jobWithCustomWorkloadRetriever, ok := ancestorGenericJob.(JobWithCustomWorkloadRetriever)
```

  @yaroslava-serdiuk, in my testing, I did see Kueue reconciles/manages the submitter job. Without my change like `JobWithCustomWorkloadRetriever`, the submitter job is created and then suspended by Kueue. That is why I added `JobWithCustomWorkloadRetriever`.
  
  Inside [code](https://github.com/kubernetes-sigs/kueue/blob/6bbbda3efd700d398a037adc1cd048e4bf01b563/pkg/controller/jobframework/reconciler.go#L782) `FindAncestorJobManagedByKueue`, it checks queue name on parent object, guess that is why Kueue still reconciles/manages the submitter job even though the submitter job does not have [kueue label](https://github.com/ray-project/kuberay/blob/master/ray-operator/controllers/ray/rayjob_controller.go#L673):
  ```
  		if managed && (manageJobsWithoutQueueName || QueueNameForObject(parentObj) != "") {
  			topLevelJob = parentObj
  		}
  ```

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2025-12-01T13:24:00Z

- **pkg/controller/jobframework/reconciler.go:382** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2025-12-01T13:24:00Z
```diff
@@ -374,7 +374,28 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if !isTopLevelJob {
 		_, _, finished := job.Finished()
 		if !finished && !job.IsSuspended() {
-			if ancestorWorkload, err := r.getWorkloadForObject(ctx, ancestorJob); err != nil {
+			var ancestorWorkload *kueue.Workload
+			var err error
+			ancestorGenericJob := CreateGenericJobFromRuntimeObject(ancestorJob)
+			if ancestorGenericJob != nil {
+				log.V(10).Info("Ancestor job converted to generic job, trying to convert it again to JobWithCustomWorkloadRetriever")
+				jobWithCustomWorkloadRetriever, ok := ancestorGenericJob.(JobWithCustomWorkloadRetriever)
```

  Correct. My apology, I tested with disabled RayJob integration and in that case the submitter job does not managed by kueue.

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2025-12-01T18:18:50Z

- **pkg/controller/jobframework/reconciler.go:382** by [@hiboyang](https://github.com/hiboyang) — 2025-12-01T18:18:50Z
```diff
@@ -374,7 +374,28 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if !isTopLevelJob {
 		_, _, finished := job.Finished()
 		if !finished && !job.IsSuspended() {
-			if ancestorWorkload, err := r.getWorkloadForObject(ctx, ancestorJob); err != nil {
+			var ancestorWorkload *kueue.Workload
+			var err error
+			ancestorGenericJob := CreateGenericJobFromRuntimeObject(ancestorJob)
+			if ancestorGenericJob != nil {
+				log.V(10).Info("Ancestor job converted to generic job, trying to convert it again to JobWithCustomWorkloadRetriever")
+				jobWithCustomWorkloadRetriever, ok := ancestorGenericJob.(JobWithCustomWorkloadRetriever)
```

  Cool, np, thanks for testing it in your side! We do not want to disable RayJob integration when running this in production environment, since it will impact old RayJobs. The goal is to keep the impact minimal, i.e. keep RayJob integration to support normal RayJob which does not need autoscaling, and user can opt in autoscaling when needed.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-04T04:05:05Z

- **pkg/controller/jobframework/reconciler.go:743** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-04T04:05:05Z
```diff
@@ -717,6 +738,10 @@ func (r *JobReconciler) getWorkloadForObject(ctx context.Context, jobObj client.
 	return &wls.Items[0], nil
 }
 
+func (r *JobReconciler) getWorkloadForObject(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
+	return GetWorkloadForObject(ctx, jobObj, r.client)
+}
```

  ```suggestion
  ```
  
  Use GetWorkloadForObject().

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-04T04:09:39Z

- **pkg/controller/jobframework/reconciler.go:719** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-04T04:09:39Z
```diff
@@ -694,10 +715,10 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// getWorkloadForObject returns the Workload associated with the given job.
-func (r *JobReconciler) getWorkloadForObject(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
+// GetWorkloadForObject returns the Workload associated with the given job.
+func GetWorkloadForObject(ctx context.Context, jobObj client.Object, c client.Client) (*kueue.Workload, error) {
```

  ```suggestion
  func GetWorkloadForObject(ctx context.Context, c client.Client, jobObj client.Object) (*kueue.Workload, error) {
  ```
  nit for consistency with other places

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-04T04:13:24Z

- **pkg/controller/jobframework/utils_test.go:36** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-04T04:13:24Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"testing"
+
+	batchv1 "k8s.io/api/batch/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/podset"
+)
+
+// mockGenericJob is a simple mock implementation of GenericJob for testing
+type mockGenericJob struct {
+	obj client.Object
+	gvk schema.GroupVersionKind
+}
```

  Please use *mocks.MockGenericJob.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-04T12:30:39Z

- **pkg/controller/jobframework/reconciler.go:398** by [@mimowo](https://github.com/mimowo) — 2025-12-04T12:30:39Z
```diff
@@ -374,7 +374,28 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if !isTopLevelJob {
 		_, _, finished := job.Finished()
 		if !finished && !job.IsSuspended() {
-			if ancestorWorkload, err := r.getWorkloadForObject(ctx, ancestorJob); err != nil {
+			var ancestorWorkload *kueue.Workload
+			var err error
+			ancestorGenericJob := CreateGenericJobFromRuntimeObject(ancestorJob)
+			if ancestorGenericJob != nil {
+				log.V(10).Info("Ancestor job converted to generic job, trying to convert it again to JobWithCustomWorkloadRetriever")
+				jobWithCustomWorkloadRetriever, ok := ancestorGenericJob.(JobWithCustomWorkloadRetriever)
+				if ok {
+					log.V(10).Info("Ancestor job converted to JobWithCustomWorkloadRetriever")
+					ancestorWorkload, err = jobWithCustomWorkloadRetriever.GetWorkload(ctx, r.client)
+					if err != nil {
+						log.Error(err, "couldn't get an ancestor job workload")
+						return ctrl.Result{}, err
+					}
+				} else {
+					log.V(10).Info("Ancestor job not converted to JobWithCustomWorkloadRetriever")
+					ancestorWorkload, err = r.getWorkloadForObject(ctx, ancestorJob)
+				}
+			} else {
+				log.V(10).Info("Ancestor job not converted to generic job", "ancestorJobName", ancestorJob.GetName())
+				ancestorWorkload, err = r.getWorkloadForObject(ctx, ancestorJob)
+			}
+			if err != nil {
```

  This has some issues:
  1. the request for the submitter Job is not represented by Kueue Workload, so bypassing quota reservation, this may lead to subtle bugs for TAS
  2. it seems incompatible with LocalQueueDefaulting, because then Workload will be created for the submitter Job anyway, consequences are hard to predict
  3. it is incompatible with the configuration setting for `manageJobsWithoutQueue: true`, then it will just suspend the submitter Job
  
  I think it will be better to manager the submitter Job as another topLevelJob, and create a Workload for it. 
  
  To achieve that we can implement the IsTopLevel for Job to lookup if parent it RayJob with autoscaling enabled. 
  
  Long term I think integration for RayJob could work this way by default - to create two workloads, one for submitter Job, one for RayCluster.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2025-12-04T12:52:47Z

- **pkg/controller/jobframework/reconciler.go:398** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2025-12-04T12:52:47Z
```diff
@@ -374,7 +374,28 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if !isTopLevelJob {
 		_, _, finished := job.Finished()
 		if !finished && !job.IsSuspended() {
-			if ancestorWorkload, err := r.getWorkloadForObject(ctx, ancestorJob); err != nil {
+			var ancestorWorkload *kueue.Workload
+			var err error
+			ancestorGenericJob := CreateGenericJobFromRuntimeObject(ancestorJob)
+			if ancestorGenericJob != nil {
+				log.V(10).Info("Ancestor job converted to generic job, trying to convert it again to JobWithCustomWorkloadRetriever")
+				jobWithCustomWorkloadRetriever, ok := ancestorGenericJob.(JobWithCustomWorkloadRetriever)
+				if ok {
+					log.V(10).Info("Ancestor job converted to JobWithCustomWorkloadRetriever")
+					ancestorWorkload, err = jobWithCustomWorkloadRetriever.GetWorkload(ctx, r.client)
+					if err != nil {
+						log.Error(err, "couldn't get an ancestor job workload")
+						return ctrl.Result{}, err
+					}
+				} else {
+					log.V(10).Info("Ancestor job not converted to JobWithCustomWorkloadRetriever")
+					ancestorWorkload, err = r.getWorkloadForObject(ctx, ancestorJob)
+				}
+			} else {
+				log.V(10).Info("Ancestor job not converted to generic job", "ancestorJobName", ancestorJob.GetName())
+				ancestorWorkload, err = r.getWorkloadForObject(ctx, ancestorJob)
+			}
+			if err != nil {
```

  I think 2) and 3) comes only if RayJob integration is disabled, so the Submitter Job doesn't have ancestor.
  However the 1) is still valid, since the RayCluster doesn't include submitter Job requests. 
  
  When RayJob integration is enabled, in order for isTopLevel() works, the Submitter Job need to have kueue label. This could be added by webhook.

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2025-12-04T16:36:21Z

- **pkg/controller/jobframework/utils_test.go:36** by [@hiboyang](https://github.com/hiboyang) — 2025-12-04T16:36:20Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"testing"
+
+	batchv1 "k8s.io/api/batch/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/podset"
+)
+
+// mockGenericJob is a simple mock implementation of GenericJob for testing
+type mockGenericJob struct {
+	obj client.Object
+	gvk schema.GroupVersionKind
+}
```

  updated, using *mocks.MockGenericJob now.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-05T08:38:25Z

- **pkg/controller/jobframework/interface.go:125** by [@mimowo](https://github.com/mimowo) — 2025-12-05T08:38:25Z
```diff
@@ -119,6 +119,13 @@ type JobWithCustomValidation interface {
 	ValidateOnUpdate(oldJob GenericJob) (field.ErrorList, error)
 }
 
+// JobWithCustomWorkloadRetriever interface should be implemented by jobs which have
+// their own logic to get workload, e.g. for RayJob with InTreeAutoScaling, the workload
+// is attached on the underlying RayCluster, RayJob needs to get workload through that RayCluster
+type JobWithCustomWorkloadRetriever interface {
```

  I think this interface is no longer needed if we make the submitter Job top-level as mentioned here: https://github.com/kubernetes-sigs/kueue/pull/7769/files#r2588885609
