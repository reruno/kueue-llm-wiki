# PR #7780: [AFS] Implement cache for AFS consumed resources

**Summary**: [AFS] Implement cache for AFS consumed resources

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7780

**Last updated**: 2025-11-26T14:28:11Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@IrvingMg](https://github.com/IrvingMg)
- **Branch**: `afs-cache-implementation` → `main`
- **Created**: 2025-11-20T11:34:26Z
- **Updated**: 2025-11-26T14:28:11Z
- **Merged**: 2025-11-26T14:12:38Z
- **Closed**: 2025-11-26T14:12:38Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 15   **+508 / -263**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
Implements cache-based storage for AFS consumed resources, fixing race condition where consumed resources aren't visible to scheduler when entry penalties are deleted.

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

/kind bug

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #6710
Fixes #7688
Fixes #7693

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
AdmissionFairSharing: Fix the bug that occasionally a workload may get admitted from a busy LocalQueue,
bypassing the entry penalties.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-20T11:34:29Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-11-20T11:34:33Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 19b4c9198a57c66e7dc79ff40f05b169f7710440 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69270155998f2300086e9ebd |

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-20T11:38:30Z

I’ve opened  draft PR for early review. 

@PBundyra  PTAL.

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-24T14:23:35Z

/test pull-kueue-test-e2e-multikueue-main
/test pull-kueue-test-e2e-main-1-33

Look like network issue:

```
shasum: standard input: no properly formatted SHA checksum lines found
Failed to install helm-unittest
For support, go to https://github.com/kubernetes/helm
Error: plugin install hook for "unittest" exited with error
make: *** [Makefile-deps.mk:110: helm] Error 1
```

See: https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7780/pull-kueue-test-e2e-multikueue-main/1992955152935424000

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-11-25T10:52:35Z

I like how this PR converges into an LGTM, great job!
After you address my latest comments please run O(200) runs of integration tests that happened to be flaky in the past to ensure it addresses the issue. Please paste the results here

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-25T15:26:41Z

This is actually user facing (just unlikely), proposal release note, ptal @PBundyra 
/release-note-edit
```release-note
AdmissionFairSharing: Fix the bug that occasionally a workload may get admitted from a busy LocalQueue,
bypassing the entry penalties.
```

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-25T15:39:49Z

> After you address my latest comments please run O(200) runs of integration tests that happened to be flaky in the past to ensure it addresses the issue. Please paste the results here

I’m currently running repetitions of the test “admits one workload from each LocalQueue when quota is limited”. So far, over 100 have completed successfully. I’ll share the logs once all 200 are done.

UPDATE: 200 repetitions were completed successfully. Here are the logs, gzip-compressed due to GitHub’s file size constraints.

[out.log.gz](https://github.com/user-attachments/files/23751049/out.log.gz)

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-26T08:55:15Z

Looks much more solid now 👍 
/lgtm
/approve
Hold for:
1. manual test on a live cluster for Kueue restart: https://github.com/kubernetes-sigs/kueue/pull/7780#discussion_r2563832983
2. in case @PBundyra has some remaining comments
/hold

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-26T08:55:23Z

LGTM label has been added.  <details>Git tree hash: 2e5e2b2d75000123f886a0eb1f630e054f142054</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-26T08:55:26Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7780#" title="Author self-approved">IrvingMg</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7780#issuecomment-3580224343" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-26T10:42:46Z

> 1. manual test on a live cluster for Kueue restart

Here are the logs with the steps I followed for the manual restart test on a Kind cluster: 
[afs-restart-results.log](https://github.com/user-attachments/files/23768105/afs-restart-results.log)

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-26T11:07:14Z

Thank you, the raport lgtm

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-11-26T12:06:37Z

Just some nits, other than that lgtm! Thanks @IrvingMg great job!

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-11-26T12:06:49Z

Leaving unholding to @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-26T12:55:47Z

> Leaving unholding to @mimowo

sg, I think the remaining aspects of the PR: 
1. ensure the Delete is called when the LQ is deleted to make sure no memory leak.
2. potentially simplify handling of the corner case discussed in https://github.com/kubernetes-sigs/kueue/pull/7780#discussion_r2564722590 (to be checked)

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-26T14:11:16Z

Thank you 👍 
/unhold
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-26T14:11:23Z

LGTM label has been added.  <details>Git tree hash: 0672030c67b03082b7cb1c179a7bf82ce3674ceb</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-26T14:13:03Z

@IrvingMg please check if we can cherrypick. Since AFS is alpha on 0.13 and 0.14 this is not "must", but if we can, then it would be great. It would be great to try to backport to 0.14 at least.

## Reviews

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-20T11:35:24Z

I’ve added a new subpackage for Admission Fair Sharing under the queue package, with the intention of also moving the AfsEntryPenalties struct there. 

I think this will help tidy up the code and avoid the circular dependency in CalcLocalQueueFSUsage between the queue and workload packages when using AFS structs. This can be a follow-up.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-20T11:37:02Z

- **pkg/cache/queue/afs/consumed_resources.go:1** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-20T11:36:07Z
```diff
@@ -0,0 +1,70 @@
+/*
```

  I’ve added a new subpackage for Admission Fair Sharing under the queue package, with the intention of also moving the AfsEntryPenalties struct there. 
  
  I think this will help tidy up the code and avoid the circular dependency in CalcLocalQueueFSUsage between the queue and workload packages when using AFS structs. This can be a follow-up

- **pkg/controller/core/localqueue_controller.go:308** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-20T11:36:57Z
```diff
@@ -273,43 +274,49 @@ func (r *LocalQueueReconciler) initializeAdmissionFsStatus(lq *kueue.LocalQueue)
 	return false
 }
 
-func (r *LocalQueueReconciler) reconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue) error {
+func (r *LocalQueueReconciler) reconcileConsumedUsage(lq *kueue.LocalQueue) error {
+	lqKey := utilqueue.Key(lq)
 	halfLifeTime := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
 	// reset usage to 0 if halfLife is 0
 	if halfLifeTime == 0 {
-		return r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{})
+		r.queues.SetAfsConsumedResources(lqKey, corev1.ResourceList{}, r.clock.Now())
+		return nil
 	}
 	cacheLq, err := r.cache.GetCacheLocalQueue(lq.Spec.ClusterQueue, lq)
 	if err != nil {
 		return err
 	}
-	// calculate alpha rate
-	oldUsage := lq.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources
+
+	var oldUsage corev1.ResourceList
+	var lastUpdate time.Time
+	if entry, found := r.queues.GetAfsConsumedResources(lqKey); found {
+		oldUsage = entry.Resources
+		lastUpdate = entry.LastUpdate
+	} else {
+		oldUsage = corev1.ResourceList{}
+		lastUpdate = r.clock.Now()
+	}
 	newUsage := cacheLq.GetAdmittedUsage()
 	alpha := afs.CalculateAlphaRate(
-		r.clock.Now().Sub(lq.Status.FairSharing.AdmissionFairSharingStatus.LastUpdate.Time).Seconds(),
+		r.clock.Now().Sub(lastUpdate).Seconds(),
 		halfLifeTime,
 	)
 	// calculate weighted average of old and new usage
 	scaledNewUsage := resource.MulByFloat(newUsage, alpha)
 	scaledOldUsage := resource.MulByFloat(oldUsage, 1-alpha)
 	sum := resource.MergeResourceListKeepSum(scaledOldUsage, scaledNewUsage)
 	// Add penalty to the final usage
-	lqKey := utilqueue.Key(lq)
-	err = r.queues.UpdateWithPenalty(lqKey, func(penalty corev1.ResourceList) error {
+	if err = r.queues.UpdateWithPenalty(lqKey, func(penalty corev1.ResourceList) error {
 		sum = resource.MergeResourceListKeepSum(sum, penalty)
+		return nil
+	}); err != nil {
+		return err
+	}
 
-		// update status
-		return r.updateAdmissionFsStatus(ctx, lq, sum)
-	})
-	return err
-}
+	r.queues.SetAfsConsumedResources(lqKey, sum, r.clock.Now())
+	r.log.V(3).Info("Updated AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name, "consumedResources", sum)
 
-func (r *LocalQueueReconciler) updateAdmissionFsStatus(ctx context.Context, lq *kueue.LocalQueue, consumedResources corev1.ResourceList) error {
```

  Since we’ve added the cache, do we still need to update consumedResources in the LQ status? If we’re not updating the status anymore, then we might need to update the API to remove that field, right?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-11-20T20:30:06Z

- **pkg/cache/queue/afs/consumed_resources.go:37** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-20T20:30:02Z
```diff
@@ -0,0 +1,70 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package afs
+
+import (
+	"sync"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	utilqueue "sigs.k8s.io/kueue/pkg/util/queue"
+)
+
+// ConsumedResourcesEntry stores the consumed resources for a LocalQueue with timestamp
+type ConsumedResourcesEntry struct {
+	Resources  corev1.ResourceList
+	LastUpdate time.Time
+}
+
+// AfsConsumedResources manages the fair sharing consumed resources cache
+type AfsConsumedResources struct {
+	sync.RWMutex
```

  Why do we need this mutex?
  The `resources` typed is syncMap.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-21T11:37:33Z

- **pkg/cache/queue/afs/consumed_resources.go:37** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-21T11:37:32Z
```diff
@@ -0,0 +1,70 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package afs
+
+import (
+	"sync"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	utilqueue "sigs.k8s.io/kueue/pkg/util/queue"
+)
+
+// ConsumedResourcesEntry stores the consumed resources for a LocalQueue with timestamp
+type ConsumedResourcesEntry struct {
+	Resources  corev1.ResourceList
+	LastUpdate time.Time
+}
+
+// AfsConsumedResources manages the fair sharing consumed resources cache
+type AfsConsumedResources struct {
+	sync.RWMutex
```

  Right, I’ve removed it. Thanks!

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-21T11:40:39Z

- **pkg/controller/core/localqueue_controller.go:308** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-21T11:40:38Z
```diff
@@ -273,43 +274,49 @@ func (r *LocalQueueReconciler) initializeAdmissionFsStatus(lq *kueue.LocalQueue)
 	return false
 }
 
-func (r *LocalQueueReconciler) reconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue) error {
+func (r *LocalQueueReconciler) reconcileConsumedUsage(lq *kueue.LocalQueue) error {
+	lqKey := utilqueue.Key(lq)
 	halfLifeTime := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
 	// reset usage to 0 if halfLife is 0
 	if halfLifeTime == 0 {
-		return r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{})
+		r.queues.SetAfsConsumedResources(lqKey, corev1.ResourceList{}, r.clock.Now())
+		return nil
 	}
 	cacheLq, err := r.cache.GetCacheLocalQueue(lq.Spec.ClusterQueue, lq)
 	if err != nil {
 		return err
 	}
-	// calculate alpha rate
-	oldUsage := lq.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources
+
+	var oldUsage corev1.ResourceList
+	var lastUpdate time.Time
+	if entry, found := r.queues.GetAfsConsumedResources(lqKey); found {
+		oldUsage = entry.Resources
+		lastUpdate = entry.LastUpdate
+	} else {
+		oldUsage = corev1.ResourceList{}
+		lastUpdate = r.clock.Now()
+	}
 	newUsage := cacheLq.GetAdmittedUsage()
 	alpha := afs.CalculateAlphaRate(
-		r.clock.Now().Sub(lq.Status.FairSharing.AdmissionFairSharingStatus.LastUpdate.Time).Seconds(),
+		r.clock.Now().Sub(lastUpdate).Seconds(),
 		halfLifeTime,
 	)
 	// calculate weighted average of old and new usage
 	scaledNewUsage := resource.MulByFloat(newUsage, alpha)
 	scaledOldUsage := resource.MulByFloat(oldUsage, 1-alpha)
 	sum := resource.MergeResourceListKeepSum(scaledOldUsage, scaledNewUsage)
 	// Add penalty to the final usage
-	lqKey := utilqueue.Key(lq)
-	err = r.queues.UpdateWithPenalty(lqKey, func(penalty corev1.ResourceList) error {
+	if err = r.queues.UpdateWithPenalty(lqKey, func(penalty corev1.ResourceList) error {
 		sum = resource.MergeResourceListKeepSum(sum, penalty)
+		return nil
+	}); err != nil {
+		return err
+	}
 
-		// update status
-		return r.updateAdmissionFsStatus(ctx, lq, sum)
-	})
-	return err
-}
+	r.queues.SetAfsConsumedResources(lqKey, sum, r.clock.Now())
+	r.log.V(3).Info("Updated AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name, "consumedResources", sum)
 
-func (r *LocalQueueReconciler) updateAdmissionFsStatus(ctx context.Context, lq *kueue.LocalQueue, consumedResources corev1.ResourceList) error {
```

  I've reverted this change and kept the status update. I think for observability it's still useful to have it. However, I decreased its impact by making it non-blocking for the reconciliation, so if status update fails, we just log the error.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T06:16:59Z

- **pkg/controller/core/localqueue_controller.go:293** by [@mimowo](https://github.com/mimowo) — 2025-11-24T06:16:59Z
```diff
@@ -274,41 +281,63 @@ func (r *LocalQueueReconciler) initializeAdmissionFsStatus(lq *kueue.LocalQueue)
 }
 
 func (r *LocalQueueReconciler) reconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue) error {
+	lqKey := utilqueue.Key(lq)
 	halfLifeTime := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
 	// reset usage to 0 if halfLife is 0
 	if halfLifeTime == 0 {
-		return r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{})
+		now := r.clock.Now()
+		r.queues.SetAfsConsumedResources(lqKey, corev1.ResourceList{}, now)
+		r.log.V(3).Info("Reset AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name)
+		if err := r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{}, now); err != nil {
+			r.log.V(3).Info("Failed to reset LocalQueue status", "namespace", lq.Namespace, "name", lq.Name, "error", err)
+		}
```

  This looks suspicious to update the cache before the request succeeds. I guess we could just revert the order, but ideally I think we should follow the analogy to regular usage. Then we consolidate assumed usage it cached usage in workload_controller reconciler handlers.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-24T09:25:46Z

- **pkg/controller/core/localqueue_controller.go:293** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-24T09:25:46Z
```diff
@@ -274,41 +281,63 @@ func (r *LocalQueueReconciler) initializeAdmissionFsStatus(lq *kueue.LocalQueue)
 }
 
 func (r *LocalQueueReconciler) reconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue) error {
+	lqKey := utilqueue.Key(lq)
 	halfLifeTime := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
 	// reset usage to 0 if halfLife is 0
 	if halfLifeTime == 0 {
-		return r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{})
+		now := r.clock.Now()
+		r.queues.SetAfsConsumedResources(lqKey, corev1.ResourceList{}, now)
+		r.log.V(3).Info("Reset AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name)
+		if err := r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{}, now); err != nil {
+			r.log.V(3).Info("Failed to reset LocalQueue status", "namespace", lq.Namespace, "name", lq.Name, "error", err)
+		}
```

  Could you clarify what “consolidate assumed usage and cached usage in workload_controller reconciler handlers” means? Does it mean that we should track assumed vs. actual AFS values? 
  
  If so, I’m wondering whether the decay calculation would then need to happen twice, once during assumption and once in the LQ reconciler, to ensure that we use fresher data.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T09:35:52Z

- **pkg/controller/core/localqueue_controller.go:293** by [@mimowo](https://github.com/mimowo) — 2025-11-24T09:35:52Z
```diff
@@ -274,41 +281,63 @@ func (r *LocalQueueReconciler) initializeAdmissionFsStatus(lq *kueue.LocalQueue)
 }
 
 func (r *LocalQueueReconciler) reconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue) error {
+	lqKey := utilqueue.Key(lq)
 	halfLifeTime := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
 	// reset usage to 0 if halfLife is 0
 	if halfLifeTime == 0 {
-		return r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{})
+		now := r.clock.Now()
+		r.queues.SetAfsConsumedResources(lqKey, corev1.ResourceList{}, now)
+		r.log.V(3).Info("Reset AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name)
+		if err := r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{}, now); err != nil {
+			r.log.V(3).Info("Failed to reset LocalQueue status", "namespace", lq.Namespace, "name", lq.Name, "error", err)
+		}
```

  I mean to we have workload assumed usage is consiolidated into "usage", here: https://github.com/kubernetes-sigs/kueue/blob/3b7aee0eb10ccdd9c23ed8fe7e2318047dbc7939/pkg/controller/core/workload_controller.go#L898-L902
  
  I consider this should be analogy to consolidating the cached afsPenalties into the cached consumedResources. I think it could happen also inside the workload_controller.
  
  Let me know if I'm unclear, maybe I'm missing something here which is an important detail. My main point is to basically follow the same flow pattern as for usage, so the analogy I have in mind:
  - afsPenalties <-> usage for assumedWorkload
  - ConsumedResources <-> usage for admittedWorkload

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-24T10:16:14Z

- **pkg/controller/core/localqueue_controller.go:293** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-24T10:16:14Z
```diff
@@ -274,41 +281,63 @@ func (r *LocalQueueReconciler) initializeAdmissionFsStatus(lq *kueue.LocalQueue)
 }
 
 func (r *LocalQueueReconciler) reconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue) error {
+	lqKey := utilqueue.Key(lq)
 	halfLifeTime := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
 	// reset usage to 0 if halfLife is 0
 	if halfLifeTime == 0 {
-		return r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{})
+		now := r.clock.Now()
+		r.queues.SetAfsConsumedResources(lqKey, corev1.ResourceList{}, now)
+		r.log.V(3).Info("Reset AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name)
+		if err := r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{}, now); err != nil {
+			r.log.V(3).Info("Failed to reset LocalQueue status", "namespace", lq.Namespace, "name", lq.Name, "error", err)
+		}
```

  So the workflow would be:
  
  1. Scheduler: Push the entry penalty on admission (current behavior).
  2. workload_controller update when the workload gets admitted: Calculate the new consumed value (with decay + penalty), update the cache, and clear the penalty.
  3. LQ reconciler: Recalculate the consumed value (with decay to ensure fresh data), update the cache, and update the status from the cache.
  
  Is this correct?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T10:21:45Z

- **pkg/controller/core/localqueue_controller.go:293** by [@mimowo](https://github.com/mimowo) — 2025-11-24T10:21:44Z
```diff
@@ -274,41 +281,63 @@ func (r *LocalQueueReconciler) initializeAdmissionFsStatus(lq *kueue.LocalQueue)
 }
 
 func (r *LocalQueueReconciler) reconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue) error {
+	lqKey := utilqueue.Key(lq)
 	halfLifeTime := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
 	// reset usage to 0 if halfLife is 0
 	if halfLifeTime == 0 {
-		return r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{})
+		now := r.clock.Now()
+		r.queues.SetAfsConsumedResources(lqKey, corev1.ResourceList{}, now)
+		r.log.V(3).Info("Reset AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name)
+		if err := r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{}, now); err != nil {
+			r.log.V(3).Info("Failed to reset LocalQueue status", "namespace", lq.Namespace, "name", lq.Name, "error", err)
+		}
```

  > Scheduler: Push the entry penalty on admission (current behavior).
  
  sgtm
  
  > workload_controller update when the workload gets admitted: Calculate the new consumed value (with decay + penalty), update the cache, and clear the penalty.
  
  Yes, with a small caveat that it would not always mean "clear the penalty", because if we have another workload contributing to the penalty then we should just subtract from the penalty - then last workload will clear( yes). Instead of subtracting it would also work if you stamp each penalty with the workload name/namespace.
  
  > LQ reconciler: Recalculate the consumed value (with decay to ensure fresh data), update the cache, and update the status from the cache.
  
  sgtm

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-24T13:59:38Z

- **pkg/controller/core/localqueue_controller.go:293** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-24T13:59:38Z
```diff
@@ -274,41 +281,63 @@ func (r *LocalQueueReconciler) initializeAdmissionFsStatus(lq *kueue.LocalQueue)
 }
 
 func (r *LocalQueueReconciler) reconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue) error {
+	lqKey := utilqueue.Key(lq)
 	halfLifeTime := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
 	// reset usage to 0 if halfLife is 0
 	if halfLifeTime == 0 {
-		return r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{})
+		now := r.clock.Now()
+		r.queues.SetAfsConsumedResources(lqKey, corev1.ResourceList{}, now)
+		r.log.V(3).Info("Reset AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name)
+		if err := r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{}, now); err != nil {
+			r.log.V(3).Info("Failed to reset LocalQueue status", "namespace", lq.Namespace, "name", lq.Name, "error", err)
+		}
```

  I've applied the changes. PTAL.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-24T14:25:40Z

- **pkg/workload/workload.go:330** by [@PBundyra](https://github.com/PBundyra) — 2025-11-21T15:38:57Z
```diff
@@ -320,27 +321,25 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
-func (i *Info) CalcLocalQueueFSUsage(ctx context.Context, c client.Client, resWeights map[corev1.ResourceName]float64, afsEntryPenalties *utilmaps.SyncMap[utilqueue.LocalQueueReference, corev1.ResourceList]) (float64, error) {
-	var lq kueue.LocalQueue
-	lqKey := client.ObjectKey{Namespace: i.Obj.Namespace, Name: string(i.Obj.Spec.QueueName)}
-	if err := c.Get(ctx, lqKey, &lq); err != nil {
-		return 0, err
-	}
+func (i *Info) CalcLocalQueueFSUsage(ctx context.Context, c client.Client, resWeights map[corev1.ResourceName]float64, afsEntryPenalties *utilmaps.SyncMap[utilqueue.LocalQueueReference, corev1.ResourceList], afsConsumedResources *queueafs.AfsConsumedResources) (float64, error) {
 	var usage float64
-	if lq.Status.FairSharing == nil || lq.Status.FairSharing.AdmissionFairSharingStatus == nil {
-		// If FairSharing is not enabled or initialized, return 0 usage.
-		return 0, nil
-	}
-	for resName, resVal := range lq.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources {
-		weight, found := resWeights[resName]
-		if !found {
-			weight = 1
+	lqKey := utilqueue.KeyFromWorkload(i.Obj)
+
+	if afsConsumedResources != nil {
+		if entry, found := afsConsumedResources.Get(lqKey); found {
+			for resName, resVal := range entry.Resources {
```

  Can we avoid nesting if's and instead, check if afsConsumedResources == nil and then return 0 if that's the case. Same for if the values is not found in the map

- **pkg/workload/workload.go:350** by [@PBundyra](https://github.com/PBundyra) — 2025-11-21T15:40:45Z
```diff
@@ -350,6 +349,11 @@ func (i *Info) CalcLocalQueueFSUsage(ctx context.Context, c client.Client, resWe
 		usage += weight * penaltyVal.AsApproximateFloat64()
 	}
```

  Can we consolidate with the consumedResource above to avoid duplicating?

- **pkg/controller/core/localqueue_controller.go:293** by [@PBundyra](https://github.com/PBundyra) — 2025-11-24T14:23:58Z
```diff
@@ -274,41 +281,63 @@ func (r *LocalQueueReconciler) initializeAdmissionFsStatus(lq *kueue.LocalQueue)
 }
 
 func (r *LocalQueueReconciler) reconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue) error {
+	lqKey := utilqueue.Key(lq)
 	halfLifeTime := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
 	// reset usage to 0 if halfLife is 0
 	if halfLifeTime == 0 {
-		return r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{})
+		now := r.clock.Now()
+		r.queues.SetAfsConsumedResources(lqKey, corev1.ResourceList{}, now)
+		r.log.V(3).Info("Reset AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name)
+		if err := r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{}, now); err != nil {
+			r.log.V(3).Info("Failed to reset LocalQueue status", "namespace", lq.Namespace, "name", lq.Name, "error", err)
+		}
```

  Please do not update the cache until the update to status was successful

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-24T14:27:13Z

- **pkg/controller/core/localqueue_controller.go:288** by [@PBundyra](https://github.com/PBundyra) — 2025-11-24T14:27:13Z
```diff
@@ -274,41 +281,44 @@ func (r *LocalQueueReconciler) initializeAdmissionFsStatus(lq *kueue.LocalQueue)
 }
 
 func (r *LocalQueueReconciler) reconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue) error {
+	lqKey := utilqueue.Key(lq)
 	halfLifeTime := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
 	// reset usage to 0 if halfLife is 0
 	if halfLifeTime == 0 {
-		return r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{})
+		now := r.clock.Now()
```

  Move it to the top to avoid duplication, and keep consistency between cache and status

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-24T14:27:28Z

- **pkg/controller/core/localqueue_controller.go:290** by [@PBundyra](https://github.com/PBundyra) — 2025-11-24T14:27:29Z
```diff
@@ -274,41 +281,44 @@ func (r *LocalQueueReconciler) initializeAdmissionFsStatus(lq *kueue.LocalQueue)
 }
 
 func (r *LocalQueueReconciler) reconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue) error {
+	lqKey := utilqueue.Key(lq)
 	halfLifeTime := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
 	// reset usage to 0 if halfLife is 0
 	if halfLifeTime == 0 {
-		return r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{})
+		now := r.clock.Now()
+		r.queues.SetAfsConsumedResources(lqKey, corev1.ResourceList{}, now)
+		r.log.V(3).Info("Reset AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name)
```

  Please set verbosity to 2

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-24T14:27:37Z

- **pkg/controller/core/localqueue_controller.go:297** by [@PBundyra](https://github.com/PBundyra) — 2025-11-24T14:27:37Z
```diff
@@ -274,41 +281,44 @@ func (r *LocalQueueReconciler) initializeAdmissionFsStatus(lq *kueue.LocalQueue)
 }
 
 func (r *LocalQueueReconciler) reconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue) error {
+	lqKey := utilqueue.Key(lq)
 	halfLifeTime := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
 	// reset usage to 0 if halfLife is 0
 	if halfLifeTime == 0 {
-		return r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{})
+		now := r.clock.Now()
+		r.queues.SetAfsConsumedResources(lqKey, corev1.ResourceList{}, now)
+		r.log.V(3).Info("Reset AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name)
+		if err := r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{}, now); err != nil {
+			r.log.V(3).Info("Failed to reset LocalQueue status", "namespace", lq.Namespace, "name", lq.Name, "error", err)
+		}
+		return nil
+	}
+
+	now := r.clock.Now()
```

  ditto

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-24T14:28:55Z

- **pkg/controller/core/localqueue_controller.go:314** by [@PBundyra](https://github.com/PBundyra) — 2025-11-24T14:28:55Z
```diff
@@ -274,41 +281,44 @@ func (r *LocalQueueReconciler) initializeAdmissionFsStatus(lq *kueue.LocalQueue)
 }
 
 func (r *LocalQueueReconciler) reconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue) error {
+	lqKey := utilqueue.Key(lq)
 	halfLifeTime := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
 	// reset usage to 0 if halfLife is 0
 	if halfLifeTime == 0 {
-		return r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{})
+		now := r.clock.Now()
+		r.queues.SetAfsConsumedResources(lqKey, corev1.ResourceList{}, now)
+		r.log.V(3).Info("Reset AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name)
+		if err := r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{}, now); err != nil {
+			r.log.V(3).Info("Failed to reset LocalQueue status", "namespace", lq.Namespace, "name", lq.Name, "error", err)
+		}
+		return nil
+	}
+
+	now := r.clock.Now()
+	entry, found := r.queues.GetAfsConsumedResourcesFor(lqKey)
+	if !found {
+		entry.LastUpdate = now
 	}
+
 	cacheLq, err := r.cache.GetCacheLocalQueue(lq.Spec.ClusterQueue, lq)
 	if err != nil {
 		return err
 	}
-	// calculate alpha rate
-	oldUsage := lq.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources
+
+	oldUsage := entry.Resources
 	newUsage := cacheLq.GetAdmittedUsage()
-	alpha := afs.CalculateAlphaRate(
-		r.clock.Now().Sub(lq.Status.FairSharing.AdmissionFairSharingStatus.LastUpdate.Time).Seconds(),
-		halfLifeTime,
-	)
-	// calculate weighted average of old and new usage
-	scaledNewUsage := resource.MulByFloat(newUsage, alpha)
-	scaledOldUsage := resource.MulByFloat(oldUsage, 1-alpha)
-	sum := resource.MergeResourceListKeepSum(scaledOldUsage, scaledNewUsage)
-	// Add penalty to the final usage
-	lqKey := utilqueue.Key(lq)
-	err = r.queues.UpdateWithPenalty(lqKey, func(penalty corev1.ResourceList) error {
-		sum = resource.MergeResourceListKeepSum(sum, penalty)
+	elapsed := now.Sub(entry.LastUpdate).Seconds()
+	newConsumed := afs.CalculateDecayedConsumed(oldUsage, newUsage, elapsed, halfLifeTime)
+
+	r.queues.SetAfsConsumedResources(lqKey, newConsumed, now)
+	r.log.V(3).Info("Updated AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name, "consumedResources", newConsumed)
```

  Set verbosity to 2 please

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-24T14:29:35Z

- **pkg/controller/core/localqueue_controller.go:316** by [@PBundyra](https://github.com/PBundyra) — 2025-11-24T14:29:35Z
```diff
@@ -274,41 +281,44 @@ func (r *LocalQueueReconciler) initializeAdmissionFsStatus(lq *kueue.LocalQueue)
 }
 
 func (r *LocalQueueReconciler) reconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue) error {
+	lqKey := utilqueue.Key(lq)
 	halfLifeTime := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
 	// reset usage to 0 if halfLife is 0
 	if halfLifeTime == 0 {
-		return r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{})
+		now := r.clock.Now()
+		r.queues.SetAfsConsumedResources(lqKey, corev1.ResourceList{}, now)
+		r.log.V(3).Info("Reset AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name)
+		if err := r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{}, now); err != nil {
+			r.log.V(3).Info("Failed to reset LocalQueue status", "namespace", lq.Namespace, "name", lq.Name, "error", err)
+		}
+		return nil
+	}
+
+	now := r.clock.Now()
+	entry, found := r.queues.GetAfsConsumedResourcesFor(lqKey)
+	if !found {
+		entry.LastUpdate = now
 	}
+
 	cacheLq, err := r.cache.GetCacheLocalQueue(lq.Spec.ClusterQueue, lq)
 	if err != nil {
 		return err
 	}
-	// calculate alpha rate
-	oldUsage := lq.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources
+
+	oldUsage := entry.Resources
 	newUsage := cacheLq.GetAdmittedUsage()
-	alpha := afs.CalculateAlphaRate(
-		r.clock.Now().Sub(lq.Status.FairSharing.AdmissionFairSharingStatus.LastUpdate.Time).Seconds(),
-		halfLifeTime,
-	)
-	// calculate weighted average of old and new usage
-	scaledNewUsage := resource.MulByFloat(newUsage, alpha)
-	scaledOldUsage := resource.MulByFloat(oldUsage, 1-alpha)
-	sum := resource.MergeResourceListKeepSum(scaledOldUsage, scaledNewUsage)
-	// Add penalty to the final usage
-	lqKey := utilqueue.Key(lq)
-	err = r.queues.UpdateWithPenalty(lqKey, func(penalty corev1.ResourceList) error {
-		sum = resource.MergeResourceListKeepSum(sum, penalty)
+	elapsed := now.Sub(entry.LastUpdate).Seconds()
+	newConsumed := afs.CalculateDecayedConsumed(oldUsage, newUsage, elapsed, halfLifeTime)
+
+	r.queues.SetAfsConsumedResources(lqKey, newConsumed, now)
+	r.log.V(3).Info("Updated AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name, "consumedResources", newConsumed)
 
-		// update status
-		return r.updateAdmissionFsStatus(ctx, lq, sum)
-	})
-	return err
+	return r.updateAdmissionFsStatus(ctx, lq, newConsumed, now)
```

  Revert order so it first updates the status and then if there's no error updates the cache

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-24T14:58:20Z

- **pkg/cache/queue/manager.go:830** by [@PBundyra](https://github.com/PBundyra) — 2025-11-24T14:58:20Z
```diff
@@ -810,6 +812,22 @@ func (m *Manager) GetAfsEntryPenalties() *utilmaps.SyncMap[queue.LocalQueueRefer
 	return m.afsEntryPenalties.getPenalties()
 }
 
+func (m *Manager) SetAfsConsumedResources(lqKey queue.LocalQueueReference, resources corev1.ResourceList, lastUpdate time.Time) {
+	m.afsConsumedResources.Set(lqKey, resources, lastUpdate)
+}
+
+func (m *Manager) GetAfsConsumedResourcesFor(lqKey queue.LocalQueueReference) (queueafs.ConsumedResourcesEntry, bool) {
+	return m.afsConsumedResources.Get(lqKey)
+}
+
+func (m *Manager) DeleteAfsConsumedResources(lqKey queue.LocalQueueReference) {
+	m.afsConsumedResources.Delete(lqKey)
+}
+
+func (m *Manager) GetAfsConsumedResources() *queueafs.AfsConsumedResources {
+	return m.afsConsumedResources
+}
+
```

  Since there's no logic, I consider it redundant. I'd rather make this field in manager public to avoid such boilerplate

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-24T15:02:44Z

- **pkg/controller/core/localqueue_controller.go:177** by [@PBundyra](https://github.com/PBundyra) — 2025-11-24T15:02:44Z
```diff
@@ -174,8 +175,14 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 
 	if afs.Enabled(r.admissionFSConfig) {
 		updated := r.initializeAdmissionFsStatus(&queueObj)
```

  I think we should modify it here, and initialize. This would reduce the code added below

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-24T15:09:16Z

- **pkg/controller/core/localqueue_controller.go:301** by [@PBundyra](https://github.com/PBundyra) — 2025-11-24T15:09:17Z
```diff
@@ -274,41 +281,44 @@ func (r *LocalQueueReconciler) initializeAdmissionFsStatus(lq *kueue.LocalQueue)
 }
 
 func (r *LocalQueueReconciler) reconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue) error {
+	lqKey := utilqueue.Key(lq)
 	halfLifeTime := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
 	// reset usage to 0 if halfLife is 0
 	if halfLifeTime == 0 {
-		return r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{})
+		now := r.clock.Now()
+		r.queues.SetAfsConsumedResources(lqKey, corev1.ResourceList{}, now)
+		r.log.V(3).Info("Reset AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name)
+		if err := r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{}, now); err != nil {
+			r.log.V(3).Info("Failed to reset LocalQueue status", "namespace", lq.Namespace, "name", lq.Name, "error", err)
+		}
+		return nil
+	}
+
+	now := r.clock.Now()
+	entry, found := r.queues.GetAfsConsumedResourcesFor(lqKey)
+	if !found {
+		entry.LastUpdate = now
 	}
```

  IIUC this should never happen because of the code here https://github.com/kubernetes-sigs/kueue/pull/7780/files#diff-9177ca87118e92b78007b8e08ffc92f1a18afa6fd6b4168b464e497e09ec53ecR183-R184

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-24T15:10:15Z

- **pkg/controller/core/workload_controller.go:1017** by [@PBundyra](https://github.com/PBundyra) — 2025-11-24T15:10:15Z
```diff
@@ -967,6 +980,43 @@ func (r *WorkloadReconciler) Generic(e event.TypedGenericEvent[*kueue.Workload])
 	return false
 }
 
+func (r *WorkloadReconciler) updateAfsConsumedUsage(log logr.Logger, wl *kueue.Workload) {
+	lqKey := qutil.KeyFromWorkload(wl)
+	penalty := afs.CalculateEntryPenalty(workload.NewInfo(wl).SumTotalRequests(), r.admissionFSConfig)
+	now := r.clock.Now()
+
+	oldEntry, found := r.queues.GetAfsConsumedResourcesFor(lqKey)
+	if !found {
+		oldEntry.LastUpdate = now
+	}
+
+	lq := &kueue.LocalQueue{
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace: wl.Namespace,
+			Name:      string(wl.Spec.QueueName),
+		},
+		Spec: kueue.LocalQueueSpec{
+			ClusterQueue: wl.Status.Admission.ClusterQueue,
+		},
+	}
+	cacheLq, err := r.cache.GetCacheLocalQueue(wl.Status.Admission.ClusterQueue, lq)
+	if err != nil {
+		log.V(3).Info("Failed to get cache LocalQueue", "error", err)
+		return
+	}
+
+	oldUsage := oldEntry.Resources
+	newUsage := cacheLq.GetAdmittedUsage()
+	elapsed := now.Sub(oldEntry.LastUpdate).Seconds()
+	newConsumed := afs.CalculateDecayedConsumed(oldUsage, newUsage, elapsed, r.admissionFSConfig.UsageHalfLifeTime.Seconds())
+	newConsumed = resource.MergeResourceListKeepSum(newConsumed, penalty)
+
+	r.queues.SetAfsConsumedResources(lqKey, newConsumed, now)
+	r.queues.SubEntryPenalty(lqKey, penalty)
+
+	log.V(3).Info("Updated AFS consumed usage", "localQueue", klog.KRef(wl.Namespace, string(wl.Spec.QueueName)), "consumed", newConsumed)
```

  Please set verbosity to 2

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-24T15:15:43Z

- **pkg/controller/core/workload_controller.go:1001** by [@PBundyra](https://github.com/PBundyra) — 2025-11-24T15:15:43Z
```diff
@@ -967,6 +980,43 @@ func (r *WorkloadReconciler) Generic(e event.TypedGenericEvent[*kueue.Workload])
 	return false
 }
 
+func (r *WorkloadReconciler) updateAfsConsumedUsage(log logr.Logger, wl *kueue.Workload) {
+	lqKey := qutil.KeyFromWorkload(wl)
+	penalty := afs.CalculateEntryPenalty(workload.NewInfo(wl).SumTotalRequests(), r.admissionFSConfig)
+	now := r.clock.Now()
+
+	oldEntry, found := r.queues.GetAfsConsumedResourcesFor(lqKey)
+	if !found {
+		oldEntry.LastUpdate = now
+	}
+
+	lq := &kueue.LocalQueue{
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace: wl.Namespace,
+			Name:      string(wl.Spec.QueueName),
+		},
+		Spec: kueue.LocalQueueSpec{
+			ClusterQueue: wl.Status.Admission.ClusterQueue,
+		},
+	}
```

  Please refactor GetCacheLocalQueue function to use lqkey and reduce this

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-24T16:07:33Z

- **pkg/workload/workload.go:330** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-24T16:07:33Z
```diff
@@ -320,27 +321,25 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
-func (i *Info) CalcLocalQueueFSUsage(ctx context.Context, c client.Client, resWeights map[corev1.ResourceName]float64, afsEntryPenalties *utilmaps.SyncMap[utilqueue.LocalQueueReference, corev1.ResourceList]) (float64, error) {
-	var lq kueue.LocalQueue
-	lqKey := client.ObjectKey{Namespace: i.Obj.Namespace, Name: string(i.Obj.Spec.QueueName)}
-	if err := c.Get(ctx, lqKey, &lq); err != nil {
-		return 0, err
-	}
+func (i *Info) CalcLocalQueueFSUsage(ctx context.Context, c client.Client, resWeights map[corev1.ResourceName]float64, afsEntryPenalties *utilmaps.SyncMap[utilqueue.LocalQueueReference, corev1.ResourceList], afsConsumedResources *queueafs.AfsConsumedResources) (float64, error) {
 	var usage float64
-	if lq.Status.FairSharing == nil || lq.Status.FairSharing.AdmissionFairSharingStatus == nil {
-		// If FairSharing is not enabled or initialized, return 0 usage.
-		return 0, nil
-	}
-	for resName, resVal := range lq.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources {
-		weight, found := resWeights[resName]
-		if !found {
-			weight = 1
+	lqKey := utilqueue.KeyFromWorkload(i.Obj)
+
+	if afsConsumedResources != nil {
+		if entry, found := afsConsumedResources.Get(lqKey); found {
+			for resName, resVal := range entry.Resources {
```

  Done

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-24T16:07:43Z

- **pkg/workload/workload.go:350** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-24T16:07:43Z
```diff
@@ -350,6 +349,11 @@ func (i *Info) CalcLocalQueueFSUsage(ctx context.Context, c client.Client, resWe
 		usage += weight * penaltyVal.AsApproximateFloat64()
 	}
```

  Done

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-24T16:07:59Z

- **pkg/controller/core/localqueue_controller.go:288** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-24T16:07:59Z
```diff
@@ -274,41 +281,44 @@ func (r *LocalQueueReconciler) initializeAdmissionFsStatus(lq *kueue.LocalQueue)
 }
 
 func (r *LocalQueueReconciler) reconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue) error {
+	lqKey := utilqueue.Key(lq)
 	halfLifeTime := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
 	// reset usage to 0 if halfLife is 0
 	if halfLifeTime == 0 {
-		return r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{})
+		now := r.clock.Now()
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-24T16:08:11Z

- **pkg/controller/core/localqueue_controller.go:290** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-24T16:08:11Z
```diff
@@ -274,41 +281,44 @@ func (r *LocalQueueReconciler) initializeAdmissionFsStatus(lq *kueue.LocalQueue)
 }
 
 func (r *LocalQueueReconciler) reconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue) error {
+	lqKey := utilqueue.Key(lq)
 	halfLifeTime := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
 	// reset usage to 0 if halfLife is 0
 	if halfLifeTime == 0 {
-		return r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{})
+		now := r.clock.Now()
+		r.queues.SetAfsConsumedResources(lqKey, corev1.ResourceList{}, now)
+		r.log.V(3).Info("Reset AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name)
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-24T16:08:23Z

- **pkg/controller/core/localqueue_controller.go:314** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-24T16:08:23Z
```diff
@@ -274,41 +281,44 @@ func (r *LocalQueueReconciler) initializeAdmissionFsStatus(lq *kueue.LocalQueue)
 }
 
 func (r *LocalQueueReconciler) reconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue) error {
+	lqKey := utilqueue.Key(lq)
 	halfLifeTime := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
 	// reset usage to 0 if halfLife is 0
 	if halfLifeTime == 0 {
-		return r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{})
+		now := r.clock.Now()
+		r.queues.SetAfsConsumedResources(lqKey, corev1.ResourceList{}, now)
+		r.log.V(3).Info("Reset AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name)
+		if err := r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{}, now); err != nil {
+			r.log.V(3).Info("Failed to reset LocalQueue status", "namespace", lq.Namespace, "name", lq.Name, "error", err)
+		}
+		return nil
+	}
+
+	now := r.clock.Now()
+	entry, found := r.queues.GetAfsConsumedResourcesFor(lqKey)
+	if !found {
+		entry.LastUpdate = now
 	}
+
 	cacheLq, err := r.cache.GetCacheLocalQueue(lq.Spec.ClusterQueue, lq)
 	if err != nil {
 		return err
 	}
-	// calculate alpha rate
-	oldUsage := lq.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources
+
+	oldUsage := entry.Resources
 	newUsage := cacheLq.GetAdmittedUsage()
-	alpha := afs.CalculateAlphaRate(
-		r.clock.Now().Sub(lq.Status.FairSharing.AdmissionFairSharingStatus.LastUpdate.Time).Seconds(),
-		halfLifeTime,
-	)
-	// calculate weighted average of old and new usage
-	scaledNewUsage := resource.MulByFloat(newUsage, alpha)
-	scaledOldUsage := resource.MulByFloat(oldUsage, 1-alpha)
-	sum := resource.MergeResourceListKeepSum(scaledOldUsage, scaledNewUsage)
-	// Add penalty to the final usage
-	lqKey := utilqueue.Key(lq)
-	err = r.queues.UpdateWithPenalty(lqKey, func(penalty corev1.ResourceList) error {
-		sum = resource.MergeResourceListKeepSum(sum, penalty)
+	elapsed := now.Sub(entry.LastUpdate).Seconds()
+	newConsumed := afs.CalculateDecayedConsumed(oldUsage, newUsage, elapsed, halfLifeTime)
+
+	r.queues.SetAfsConsumedResources(lqKey, newConsumed, now)
+	r.log.V(3).Info("Updated AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name, "consumedResources", newConsumed)
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-24T16:08:31Z

- **pkg/controller/core/localqueue_controller.go:316** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-24T16:08:31Z
```diff
@@ -274,41 +281,44 @@ func (r *LocalQueueReconciler) initializeAdmissionFsStatus(lq *kueue.LocalQueue)
 }
 
 func (r *LocalQueueReconciler) reconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue) error {
+	lqKey := utilqueue.Key(lq)
 	halfLifeTime := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
 	// reset usage to 0 if halfLife is 0
 	if halfLifeTime == 0 {
-		return r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{})
+		now := r.clock.Now()
+		r.queues.SetAfsConsumedResources(lqKey, corev1.ResourceList{}, now)
+		r.log.V(3).Info("Reset AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name)
+		if err := r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{}, now); err != nil {
+			r.log.V(3).Info("Failed to reset LocalQueue status", "namespace", lq.Namespace, "name", lq.Name, "error", err)
+		}
+		return nil
+	}
+
+	now := r.clock.Now()
+	entry, found := r.queues.GetAfsConsumedResourcesFor(lqKey)
+	if !found {
+		entry.LastUpdate = now
 	}
+
 	cacheLq, err := r.cache.GetCacheLocalQueue(lq.Spec.ClusterQueue, lq)
 	if err != nil {
 		return err
 	}
-	// calculate alpha rate
-	oldUsage := lq.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources
+
+	oldUsage := entry.Resources
 	newUsage := cacheLq.GetAdmittedUsage()
-	alpha := afs.CalculateAlphaRate(
-		r.clock.Now().Sub(lq.Status.FairSharing.AdmissionFairSharingStatus.LastUpdate.Time).Seconds(),
-		halfLifeTime,
-	)
-	// calculate weighted average of old and new usage
-	scaledNewUsage := resource.MulByFloat(newUsage, alpha)
-	scaledOldUsage := resource.MulByFloat(oldUsage, 1-alpha)
-	sum := resource.MergeResourceListKeepSum(scaledOldUsage, scaledNewUsage)
-	// Add penalty to the final usage
-	lqKey := utilqueue.Key(lq)
-	err = r.queues.UpdateWithPenalty(lqKey, func(penalty corev1.ResourceList) error {
-		sum = resource.MergeResourceListKeepSum(sum, penalty)
+	elapsed := now.Sub(entry.LastUpdate).Seconds()
+	newConsumed := afs.CalculateDecayedConsumed(oldUsage, newUsage, elapsed, halfLifeTime)
+
+	r.queues.SetAfsConsumedResources(lqKey, newConsumed, now)
+	r.log.V(3).Info("Updated AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name, "consumedResources", newConsumed)
 
-		// update status
-		return r.updateAdmissionFsStatus(ctx, lq, sum)
-	})
-	return err
+	return r.updateAdmissionFsStatus(ctx, lq, newConsumed, now)
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-24T16:08:39Z

- **pkg/cache/queue/manager.go:830** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-24T16:08:39Z
```diff
@@ -810,6 +812,22 @@ func (m *Manager) GetAfsEntryPenalties() *utilmaps.SyncMap[queue.LocalQueueRefer
 	return m.afsEntryPenalties.getPenalties()
 }
 
+func (m *Manager) SetAfsConsumedResources(lqKey queue.LocalQueueReference, resources corev1.ResourceList, lastUpdate time.Time) {
+	m.afsConsumedResources.Set(lqKey, resources, lastUpdate)
+}
+
+func (m *Manager) GetAfsConsumedResourcesFor(lqKey queue.LocalQueueReference) (queueafs.ConsumedResourcesEntry, bool) {
+	return m.afsConsumedResources.Get(lqKey)
+}
+
+func (m *Manager) DeleteAfsConsumedResources(lqKey queue.LocalQueueReference) {
+	m.afsConsumedResources.Delete(lqKey)
+}
+
+func (m *Manager) GetAfsConsumedResources() *queueafs.AfsConsumedResources {
+	return m.afsConsumedResources
+}
+
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-24T16:08:49Z

- **pkg/controller/core/workload_controller.go:1017** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-24T16:08:49Z
```diff
@@ -967,6 +980,43 @@ func (r *WorkloadReconciler) Generic(e event.TypedGenericEvent[*kueue.Workload])
 	return false
 }
 
+func (r *WorkloadReconciler) updateAfsConsumedUsage(log logr.Logger, wl *kueue.Workload) {
+	lqKey := qutil.KeyFromWorkload(wl)
+	penalty := afs.CalculateEntryPenalty(workload.NewInfo(wl).SumTotalRequests(), r.admissionFSConfig)
+	now := r.clock.Now()
+
+	oldEntry, found := r.queues.GetAfsConsumedResourcesFor(lqKey)
+	if !found {
+		oldEntry.LastUpdate = now
+	}
+
+	lq := &kueue.LocalQueue{
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace: wl.Namespace,
+			Name:      string(wl.Spec.QueueName),
+		},
+		Spec: kueue.LocalQueueSpec{
+			ClusterQueue: wl.Status.Admission.ClusterQueue,
+		},
+	}
+	cacheLq, err := r.cache.GetCacheLocalQueue(wl.Status.Admission.ClusterQueue, lq)
+	if err != nil {
+		log.V(3).Info("Failed to get cache LocalQueue", "error", err)
+		return
+	}
+
+	oldUsage := oldEntry.Resources
+	newUsage := cacheLq.GetAdmittedUsage()
+	elapsed := now.Sub(oldEntry.LastUpdate).Seconds()
+	newConsumed := afs.CalculateDecayedConsumed(oldUsage, newUsage, elapsed, r.admissionFSConfig.UsageHalfLifeTime.Seconds())
+	newConsumed = resource.MergeResourceListKeepSum(newConsumed, penalty)
+
+	r.queues.SetAfsConsumedResources(lqKey, newConsumed, now)
+	r.queues.SubEntryPenalty(lqKey, penalty)
+
+	log.V(3).Info("Updated AFS consumed usage", "localQueue", klog.KRef(wl.Namespace, string(wl.Spec.QueueName)), "consumed", newConsumed)
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-24T16:08:56Z

- **pkg/controller/core/workload_controller.go:1001** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-24T16:08:56Z
```diff
@@ -967,6 +980,43 @@ func (r *WorkloadReconciler) Generic(e event.TypedGenericEvent[*kueue.Workload])
 	return false
 }
 
+func (r *WorkloadReconciler) updateAfsConsumedUsage(log logr.Logger, wl *kueue.Workload) {
+	lqKey := qutil.KeyFromWorkload(wl)
+	penalty := afs.CalculateEntryPenalty(workload.NewInfo(wl).SumTotalRequests(), r.admissionFSConfig)
+	now := r.clock.Now()
+
+	oldEntry, found := r.queues.GetAfsConsumedResourcesFor(lqKey)
+	if !found {
+		oldEntry.LastUpdate = now
+	}
+
+	lq := &kueue.LocalQueue{
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace: wl.Namespace,
+			Name:      string(wl.Spec.QueueName),
+		},
+		Spec: kueue.LocalQueueSpec{
+			ClusterQueue: wl.Status.Admission.ClusterQueue,
+		},
+	}
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-24T16:11:25Z

- **pkg/controller/core/localqueue_controller.go:177** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-24T16:11:25Z
```diff
@@ -174,8 +175,14 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 
 	if afs.Enabled(r.admissionFSConfig) {
 		updated := r.initializeAdmissionFsStatus(&queueObj)
```

  Do you mean we should also initialize the consumed cache inside initializeAdmissionFsStatus?

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-24T16:16:15Z

- **pkg/controller/core/localqueue_controller.go:301** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-24T16:16:15Z
```diff
@@ -274,41 +281,44 @@ func (r *LocalQueueReconciler) initializeAdmissionFsStatus(lq *kueue.LocalQueue)
 }
 
 func (r *LocalQueueReconciler) reconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue) error {
+	lqKey := utilqueue.Key(lq)
 	halfLifeTime := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
 	// reset usage to 0 if halfLife is 0
 	if halfLifeTime == 0 {
-		return r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{})
+		now := r.clock.Now()
+		r.queues.SetAfsConsumedResources(lqKey, corev1.ResourceList{}, now)
+		r.log.V(3).Info("Reset AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name)
+		if err := r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{}, now); err != nil {
+			r.log.V(3).Info("Failed to reset LocalQueue status", "namespace", lq.Namespace, "name", lq.Name, "error", err)
+		}
+		return nil
+	}
+
+	now := r.clock.Now()
+	entry, found := r.queues.GetAfsConsumedResourcesFor(lqKey)
+	if !found {
+		entry.LastUpdate = now
 	}
```

  Lines 183-184 check for the cache entry but don't create it. We still need the `!found` check to handle cases where the cache is missing.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-25T10:14:30Z

- **pkg/controller/core/localqueue_controller.go:177** by [@PBundyra](https://github.com/PBundyra) — 2025-11-25T10:14:30Z
```diff
@@ -174,8 +175,14 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 
 	if afs.Enabled(r.admissionFSConfig) {
 		updated := r.initializeAdmissionFsStatus(&queueObj)
```

  I would rename it into `initializeIfNeeded` and cover both cache and status there

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-25T10:15:20Z

- **pkg/controller/core/localqueue_controller.go:313** by [@PBundyra](https://github.com/PBundyra) — 2025-11-25T10:15:20Z
```diff
@@ -274,41 +281,47 @@ func (r *LocalQueueReconciler) initializeAdmissionFsStatus(lq *kueue.LocalQueue)
 }
 
 func (r *LocalQueueReconciler) reconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue) error {
+	lqKey := utilqueue.Key(lq)
 	halfLifeTime := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
-	// reset usage to 0 if halfLife is 0
+	now := r.clock.Now()
+
 	if halfLifeTime == 0 {
-		return r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{})
+		r.log.V(2).Info("Reset AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name)
+		if err := r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{}, now); err != nil {
+			r.log.V(2).Info("Failed to reset LocalQueue status", "namespace", lq.Namespace, "name", lq.Name, "error", err)
+			return err
+		}
+		r.queues.AfsConsumedResources.Set(lqKey, corev1.ResourceList{}, now)
+		return nil
+	}
+
+	entry, found := r.queues.AfsConsumedResources.Get(lqKey)
+	if !found {
+		entry.LastUpdate = now
 	}
-	cacheLq, err := r.cache.GetCacheLocalQueue(lq.Spec.ClusterQueue, lq)
+
+	cacheLq, err := r.cache.GetCacheLocalQueue(lq.Spec.ClusterQueue, lqKey)
 	if err != nil {
 		return err
 	}
-	// calculate alpha rate
-	oldUsage := lq.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources
+
+	oldUsage := entry.Resources
 	newUsage := cacheLq.GetAdmittedUsage()
-	alpha := afs.CalculateAlphaRate(
-		r.clock.Now().Sub(lq.Status.FairSharing.AdmissionFairSharingStatus.LastUpdate.Time).Seconds(),
-		halfLifeTime,
-	)
-	// calculate weighted average of old and new usage
-	scaledNewUsage := resource.MulByFloat(newUsage, alpha)
-	scaledOldUsage := resource.MulByFloat(oldUsage, 1-alpha)
-	sum := resource.MergeResourceListKeepSum(scaledOldUsage, scaledNewUsage)
-	// Add penalty to the final usage
-	lqKey := utilqueue.Key(lq)
-	err = r.queues.UpdateWithPenalty(lqKey, func(penalty corev1.ResourceList) error {
-		sum = resource.MergeResourceListKeepSum(sum, penalty)
+	elapsed := now.Sub(entry.LastUpdate).Seconds()
+	newConsumed := afs.CalculateDecayedConsumed(oldUsage, newUsage, elapsed, halfLifeTime)
 
-		// update status
-		return r.updateAdmissionFsStatus(ctx, lq, sum)
-	})
-	return err
+	r.log.V(2).Info("Updated AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name, "consumedResources", newConsumed)
```

  This log should be after the request (or even maybe cache to be more future-proof) success

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-25T10:15:33Z

- **pkg/controller/core/localqueue_controller.go:289** by [@PBundyra](https://github.com/PBundyra) — 2025-11-25T10:15:33Z
```diff
@@ -274,41 +281,47 @@ func (r *LocalQueueReconciler) initializeAdmissionFsStatus(lq *kueue.LocalQueue)
 }
 
 func (r *LocalQueueReconciler) reconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue) error {
+	lqKey := utilqueue.Key(lq)
 	halfLifeTime := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
-	// reset usage to 0 if halfLife is 0
+	now := r.clock.Now()
+
 	if halfLifeTime == 0 {
-		return r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{})
+		r.log.V(2).Info("Reset AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name)
```

  This log should be after the request (or even maybe cache to be more future-proof) success

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-25T10:24:07Z

- **pkg/controller/core/workload_controller.go:913** by [@PBundyra](https://github.com/PBundyra) — 2025-11-25T10:24:08Z
```diff
@@ -900,6 +910,9 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 		if !r.cache.AddOrUpdateWorkload(log, wlCopy) {
 			log.V(2).Info("ClusterQueue for workload didn't exist; ignored for now")
 		}
+		if afs.Enabled(r.admissionFSConfig) && status == workload.StatusAdmitted {
```

  This should also be guarded somehow by ClusterQueue's AdmissionMode

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-25T10:30:06Z

- **pkg/controller/core/localqueue_controller_test.go:1** by [@PBundyra](https://github.com/PBundyra) — 2025-11-25T10:30:06Z
  Can we add a test for the scenario where the cache hadn't been initialized earlier

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-25T10:30:27Z

- **pkg/controller/core/localqueue_controller.go:301** by [@PBundyra](https://github.com/PBundyra) — 2025-11-25T10:30:27Z
```diff
@@ -274,41 +281,44 @@ func (r *LocalQueueReconciler) initializeAdmissionFsStatus(lq *kueue.LocalQueue)
 }
 
 func (r *LocalQueueReconciler) reconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue) error {
+	lqKey := utilqueue.Key(lq)
 	halfLifeTime := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
 	// reset usage to 0 if halfLife is 0
 	if halfLifeTime == 0 {
-		return r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{})
+		now := r.clock.Now()
+		r.queues.SetAfsConsumedResources(lqKey, corev1.ResourceList{}, now)
+		r.log.V(3).Info("Reset AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name)
+		if err := r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{}, now); err != nil {
+			r.log.V(3).Info("Failed to reset LocalQueue status", "namespace", lq.Namespace, "name", lq.Name, "error", err)
+		}
+		return nil
+	}
+
+	now := r.clock.Now()
+	entry, found := r.queues.GetAfsConsumedResourcesFor(lqKey)
+	if !found {
+		entry.LastUpdate = now
 	}
```

  Please see my latest comment there

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-25T10:40:38Z

- **pkg/scheduler/scheduler_afs_test.go:1** by [@PBundyra](https://github.com/PBundyra) — 2025-11-25T10:40:38Z
  Same here, do we have a test that covers scenario if the LQ usage cache is not initailized

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-25T10:48:57Z

- **pkg/cache/queue/afs_entry_penalties.go:63** by [@PBundyra](https://github.com/PBundyra) — 2025-11-25T10:48:57Z
```diff
@@ -60,20 +60,6 @@ func (m *AfsEntryPenalties) peek(lqKey utilqueue.LocalQueueReference) corev1.Res
 	return penalty
 }
 
-func (m *AfsEntryPenalties) updateWithPenalty(lqKey utilqueue.LocalQueueReference, fn func(penalty corev1.ResourceList) error) error {
```

  After we deleted this function it seems the mutex is now redundant. This can be addressed in the follow up PR, but please create a GH issue and follow up on that after merge of this PR

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-25T15:24:08Z

- **pkg/cache/queue/afs_entry_penalties.go:63** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-25T15:24:08Z
```diff
@@ -60,20 +60,6 @@ func (m *AfsEntryPenalties) peek(lqKey utilqueue.LocalQueueReference) corev1.Res
 	return penalty
 }
 
-func (m *AfsEntryPenalties) updateWithPenalty(lqKey utilqueue.LocalQueueReference, fn func(penalty corev1.ResourceList) error) error {
```

  I've opened https://github.com/kubernetes-sigs/kueue/issues/7888

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-25T15:24:47Z

- **pkg/controller/core/localqueue_controller.go:313** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-25T15:24:47Z
```diff
@@ -274,41 +281,47 @@ func (r *LocalQueueReconciler) initializeAdmissionFsStatus(lq *kueue.LocalQueue)
 }
 
 func (r *LocalQueueReconciler) reconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue) error {
+	lqKey := utilqueue.Key(lq)
 	halfLifeTime := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
-	// reset usage to 0 if halfLife is 0
+	now := r.clock.Now()
+
 	if halfLifeTime == 0 {
-		return r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{})
+		r.log.V(2).Info("Reset AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name)
+		if err := r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{}, now); err != nil {
+			r.log.V(2).Info("Failed to reset LocalQueue status", "namespace", lq.Namespace, "name", lq.Name, "error", err)
+			return err
+		}
+		r.queues.AfsConsumedResources.Set(lqKey, corev1.ResourceList{}, now)
+		return nil
+	}
+
+	entry, found := r.queues.AfsConsumedResources.Get(lqKey)
+	if !found {
+		entry.LastUpdate = now
 	}
-	cacheLq, err := r.cache.GetCacheLocalQueue(lq.Spec.ClusterQueue, lq)
+
+	cacheLq, err := r.cache.GetCacheLocalQueue(lq.Spec.ClusterQueue, lqKey)
 	if err != nil {
 		return err
 	}
-	// calculate alpha rate
-	oldUsage := lq.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources
+
+	oldUsage := entry.Resources
 	newUsage := cacheLq.GetAdmittedUsage()
-	alpha := afs.CalculateAlphaRate(
-		r.clock.Now().Sub(lq.Status.FairSharing.AdmissionFairSharingStatus.LastUpdate.Time).Seconds(),
-		halfLifeTime,
-	)
-	// calculate weighted average of old and new usage
-	scaledNewUsage := resource.MulByFloat(newUsage, alpha)
-	scaledOldUsage := resource.MulByFloat(oldUsage, 1-alpha)
-	sum := resource.MergeResourceListKeepSum(scaledOldUsage, scaledNewUsage)
-	// Add penalty to the final usage
-	lqKey := utilqueue.Key(lq)
-	err = r.queues.UpdateWithPenalty(lqKey, func(penalty corev1.ResourceList) error {
-		sum = resource.MergeResourceListKeepSum(sum, penalty)
+	elapsed := now.Sub(entry.LastUpdate).Seconds()
+	newConsumed := afs.CalculateDecayedConsumed(oldUsage, newUsage, elapsed, halfLifeTime)
 
-		// update status
-		return r.updateAdmissionFsStatus(ctx, lq, sum)
-	})
-	return err
+	r.log.V(2).Info("Updated AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name, "consumedResources", newConsumed)
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-25T15:24:53Z

- **pkg/controller/core/localqueue_controller.go:289** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-25T15:24:53Z
```diff
@@ -274,41 +281,47 @@ func (r *LocalQueueReconciler) initializeAdmissionFsStatus(lq *kueue.LocalQueue)
 }
 
 func (r *LocalQueueReconciler) reconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue) error {
+	lqKey := utilqueue.Key(lq)
 	halfLifeTime := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
-	// reset usage to 0 if halfLife is 0
+	now := r.clock.Now()
+
 	if halfLifeTime == 0 {
-		return r.updateAdmissionFsStatus(ctx, lq, corev1.ResourceList{})
+		r.log.V(2).Info("Reset AFS consumed resources cache", "namespace", lq.Namespace, "name", lq.Name)
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-25T15:25:00Z

- **pkg/controller/core/workload_controller.go:913** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-25T15:25:00Z
```diff
@@ -900,6 +910,9 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 		if !r.cache.AddOrUpdateWorkload(log, wlCopy) {
 			log.V(2).Info("ClusterQueue for workload didn't exist; ignored for now")
 		}
+		if afs.Enabled(r.admissionFSConfig) && status == workload.StatusAdmitted {
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-25T15:25:09Z

- **pkg/controller/core/localqueue_controller_test.go:1** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-25T15:25:09Z
  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-25T15:25:16Z

- **pkg/scheduler/scheduler_afs_test.go:1** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-25T15:25:16Z
  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-25T15:25:36Z

- **pkg/controller/core/localqueue_controller.go:177** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-25T15:25:36Z
```diff
@@ -174,8 +175,14 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 
 	if afs.Enabled(r.admissionFSConfig) {
 		updated := r.initializeAdmissionFsStatus(&queueObj)
```

  Done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-26T08:03:52Z

- **pkg/controller/core/localqueue_controller_test.go:229** by [@mimowo](https://github.com/mimowo) — 2025-11-26T08:03:53Z
```diff
@@ -222,17 +225,14 @@ func TestLocalQueueReconcile(t *testing.T) {
 				FairSharing(&kueue.FairSharing{
 					Weight: ptr.To(resource.MustParse("1")),
 				}).
-				FairSharingStatus(
-					&kueue.LocalQueueFairSharingStatus{
-						AdmissionFairSharingStatus: &kueue.LocalQueueAdmissionFairSharingStatus{
-							LastUpdate: metav1.NewTime(clock.Now().Add(-5 * time.Minute)),
-							ConsumedResources: map[corev1.ResourceName]resource.Quantity{
-								corev1.ResourceCPU: resource.MustParse("8"),
-								"GPU":              resource.MustParse("16"),
-							},
-						},
-					}).
 				Obj(),
+			initialConsumedResources: queueafs.ConsumedResourcesEntry{
```

  Hm, actually here I have a doubt about restarts. Because I think when Kueue restarts we must populate the cache based on the status of the LQ. So seeing the status removed makes me wonder if we handle restarts properly. Dropping the afsPenalties is ok - the consequence is limited. However, we must double check if the consumedResources already accounted in the status are taken into account. 
  
  So:  please test on a running cluster what happens when Kueue is restarted, say kubectl delete --all pods -nkueue-system. We could also automate that test in the integration tests using StopManager / StartManager helpers.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-26T08:04:37Z

- **pkg/controller/core/localqueue_controller_test.go:229** by [@mimowo](https://github.com/mimowo) — 2025-11-26T08:04:37Z
```diff
@@ -222,17 +225,14 @@ func TestLocalQueueReconcile(t *testing.T) {
 				FairSharing(&kueue.FairSharing{
 					Weight: ptr.To(resource.MustParse("1")),
 				}).
-				FairSharingStatus(
-					&kueue.LocalQueueFairSharingStatus{
-						AdmissionFairSharingStatus: &kueue.LocalQueueAdmissionFairSharingStatus{
-							LastUpdate: metav1.NewTime(clock.Now().Add(-5 * time.Minute)),
-							ConsumedResources: map[corev1.ResourceName]resource.Quantity{
-								corev1.ResourceCPU: resource.MustParse("8"),
-								"GPU":              resource.MustParse("16"),
-							},
-						},
-					}).
 				Obj(),
+			initialConsumedResources: queueafs.ConsumedResourcesEntry{
```

  cc @PBundyra

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-26T08:30:42Z

- **pkg/controller/core/localqueue_controller_test.go:229** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-26T08:30:41Z
```diff
@@ -222,17 +225,14 @@ func TestLocalQueueReconcile(t *testing.T) {
 				FairSharing(&kueue.FairSharing{
 					Weight: ptr.To(resource.MustParse("1")),
 				}).
-				FairSharingStatus(
-					&kueue.LocalQueueFairSharingStatus{
-						AdmissionFairSharingStatus: &kueue.LocalQueueAdmissionFairSharingStatus{
-							LastUpdate: metav1.NewTime(clock.Now().Add(-5 * time.Minute)),
-							ConsumedResources: map[corev1.ResourceName]resource.Quantity{
-								corev1.ResourceCPU: resource.MustParse("8"),
-								"GPU":              resource.MustParse("16"),
-							},
-						},
-					}).
 				Obj(),
+			initialConsumedResources: queueafs.ConsumedResourcesEntry{
```

  Restart scenario should be handled by  `initializeAfsIfNeeded` function:
  https://github.com/kubernetes-sigs/kueue/blob/1c312e28b8dae582bf2617a0b4f9afffb475b648/pkg/controller/core/localqueue_controller.go#L290-L293
  
  where `getCurrentUsageForLocalQueue` is:
  https://github.com/kubernetes-sigs/kueue/blob/1c312e28b8dae582bf2617a0b4f9afffb475b648/pkg/controller/core/localqueue_controller.go#L298-L304
  
  A unit test for this scenario was added here:
  https://github.com/kubernetes-sigs/kueue/blob/1c312e28b8dae582bf2617a0b4f9afffb475b648/pkg/controller/core/localqueue_controller_test.go#L529-L571

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-26T08:33:12Z

- **pkg/controller/core/localqueue_controller_test.go:229** by [@mimowo](https://github.com/mimowo) — 2025-11-26T08:33:12Z
```diff
@@ -222,17 +225,14 @@ func TestLocalQueueReconcile(t *testing.T) {
 				FairSharing(&kueue.FairSharing{
 					Weight: ptr.To(resource.MustParse("1")),
 				}).
-				FairSharingStatus(
-					&kueue.LocalQueueFairSharingStatus{
-						AdmissionFairSharingStatus: &kueue.LocalQueueAdmissionFairSharingStatus{
-							LastUpdate: metav1.NewTime(clock.Now().Add(-5 * time.Minute)),
-							ConsumedResources: map[corev1.ResourceName]resource.Quantity{
-								corev1.ResourceCPU: resource.MustParse("8"),
-								"GPU":              resource.MustParse("16"),
-							},
-						},
-					}).
 				Obj(),
+			initialConsumedResources: queueafs.ConsumedResourcesEntry{
```

  > Restart scenario should be handled by initializeAfsIfNeeded function:
  
  Thank you for the pointers, I will read. In the meanwhile - have you had a chance to check on a live cluster?

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-26T08:33:40Z

- **pkg/controller/core/localqueue_controller_test.go:229** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-26T08:33:40Z
```diff
@@ -222,17 +225,14 @@ func TestLocalQueueReconcile(t *testing.T) {
 				FairSharing(&kueue.FairSharing{
 					Weight: ptr.To(resource.MustParse("1")),
 				}).
-				FairSharingStatus(
-					&kueue.LocalQueueFairSharingStatus{
-						AdmissionFairSharingStatus: &kueue.LocalQueueAdmissionFairSharingStatus{
-							LastUpdate: metav1.NewTime(clock.Now().Add(-5 * time.Minute)),
-							ConsumedResources: map[corev1.ResourceName]resource.Quantity{
-								corev1.ResourceCPU: resource.MustParse("8"),
-								"GPU":              resource.MustParse("16"),
-							},
-						},
-					}).
 				Obj(),
+			initialConsumedResources: queueafs.ConsumedResourcesEntry{
```

  I’ve removed the status from the tests to reduce verbosity, since it wasn’t required for most of them because the calculations use the cache.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-26T08:34:41Z

- **pkg/cache/queue/cluster_queue_test.go:163** by [@mimowo](https://github.com/mimowo) — 2025-11-26T08:34:41Z
```diff
@@ -158,7 +160,7 @@ func Test_PushOrUpdate(t *testing.T) {
 	for name, tc := range cases {
 		t.Run(name, func(t *testing.T) {
 			ctx, _ := utiltesting.ContextWithLog(t)
-			cq := newClusterQueueImpl(ctx, nil, defaultOrdering, fakeClock, nil, false, nil)
+			cq := newClusterQueueImpl(ctx, nil, defaultOrdering, fakeClock, nil, false, nil, nil)
```

  seeing so many params, and many "nil" passed I think it will be better to use the options pattern. However, this is for a technical follow up, don't mix it here.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-26T08:39:17Z

- **pkg/controller/core/workload_controller.go:996** by [@mimowo](https://github.com/mimowo) — 2025-11-26T08:39:17Z
```diff
@@ -967,6 +980,34 @@ func (r *WorkloadReconciler) Generic(e event.TypedGenericEvent[*kueue.Workload])
 	return false
 }
 
+func (r *WorkloadReconciler) updateAfsConsumedUsage(log logr.Logger, wl *kueue.Workload) {
+	lqKey := qutil.KeyFromWorkload(wl)
+	penalty := afs.CalculateEntryPenalty(workload.NewInfo(wl).SumTotalRequests(), r.admissionFSConfig)
+	now := r.clock.Now()
+
+	oldEntry, found := r.queues.AfsConsumedResources.Get(lqKey)
+	if !found {
+		oldEntry.LastUpdate = now
+	}
+
+	cacheLq, err := r.cache.GetCacheLocalQueue(wl.Status.Admission.ClusterQueue, lqKey)
+	if err != nil {
+		log.V(2).Info("Failed to get cache LocalQueue", "error", err)
+		return
```

  Longer term this error should not be silenced, but the operation would be retried in Reconciler, however this requires a bigger refactoring to move the logic from Update to Reconcile: https://github.com/kubernetes-sigs/kueue/issues/5310
  
  cc @Singularity23x0

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-26T08:41:09Z

- **pkg/controller/core/localqueue_controller_test.go:229** by [@mimowo](https://github.com/mimowo) — 2025-11-26T08:41:09Z
```diff
@@ -222,17 +225,14 @@ func TestLocalQueueReconcile(t *testing.T) {
 				FairSharing(&kueue.FairSharing{
 					Weight: ptr.To(resource.MustParse("1")),
 				}).
-				FairSharingStatus(
-					&kueue.LocalQueueFairSharingStatus{
-						AdmissionFairSharingStatus: &kueue.LocalQueueAdmissionFairSharingStatus{
-							LastUpdate: metav1.NewTime(clock.Now().Add(-5 * time.Minute)),
-							ConsumedResources: map[corev1.ResourceName]resource.Quantity{
-								corev1.ResourceCPU: resource.MustParse("8"),
-								"GPU":              resource.MustParse("16"),
-							},
-						},
-					}).
 				Obj(),
+			initialConsumedResources: queueafs.ConsumedResourcesEntry{
```

  Yeah, I read it and
  
  ```golang
   if !hasCache { 
   	currentUsage := r.getCurrentUsageForLocalQueue(lq.Spec.ClusterQueue, lqKey) 
   	r.queues.AfsConsumedResources.Set(lqKey, currentUsage, now) 
   } 
   ```
   makes sense to me covering this case

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-26T08:43:58Z

- **pkg/controller/core/localqueue_controller_test.go:229** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-26T08:43:58Z
```diff
@@ -222,17 +225,14 @@ func TestLocalQueueReconcile(t *testing.T) {
 				FairSharing(&kueue.FairSharing{
 					Weight: ptr.To(resource.MustParse("1")),
 				}).
-				FairSharingStatus(
-					&kueue.LocalQueueFairSharingStatus{
-						AdmissionFairSharingStatus: &kueue.LocalQueueAdmissionFairSharingStatus{
-							LastUpdate: metav1.NewTime(clock.Now().Add(-5 * time.Minute)),
-							ConsumedResources: map[corev1.ResourceName]resource.Quantity{
-								corev1.ResourceCPU: resource.MustParse("8"),
-								"GPU":              resource.MustParse("16"),
-							},
-						},
-					}).
 				Obj(),
+			initialConsumedResources: queueafs.ConsumedResourcesEntry{
```

  > In the meanwhile - have you had a chance to check on a live cluster?
  
  Not yet. I’ll try it on a Kind cluster and share the results.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-26T08:48:10Z

- **pkg/controller/core/localqueue_controller_test.go:229** by [@mimowo](https://github.com/mimowo) — 2025-11-26T08:48:10Z
```diff
@@ -222,17 +225,14 @@ func TestLocalQueueReconcile(t *testing.T) {
 				FairSharing(&kueue.FairSharing{
 					Weight: ptr.To(resource.MustParse("1")),
 				}).
-				FairSharingStatus(
-					&kueue.LocalQueueFairSharingStatus{
-						AdmissionFairSharingStatus: &kueue.LocalQueueAdmissionFairSharingStatus{
-							LastUpdate: metav1.NewTime(clock.Now().Add(-5 * time.Minute)),
-							ConsumedResources: map[corev1.ResourceName]resource.Quantity{
-								corev1.ResourceCPU: resource.MustParse("8"),
-								"GPU":              resource.MustParse("16"),
-							},
-						},
-					}).
 				Obj(),
+			initialConsumedResources: queueafs.ConsumedResourcesEntry{
```

  sgtm, I think it is enough to submit workloads to populate the consumedResources in status, then restart Kueue and make sure the status is not wiped out. Also, that it gets decaying properly.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-26T12:01:20Z

- **pkg/cache/queue/afs/consumed_resources.go:62** by [@PBundyra](https://github.com/PBundyra) — 2025-11-26T12:01:20Z
```diff
@@ -0,0 +1,62 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package afs
+
+import (
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	utilqueue "sigs.k8s.io/kueue/pkg/util/queue"
+)
+
+// ConsumedResourcesEntry stores the consumed resources for a LocalQueue with timestamp
+type ConsumedResourcesEntry struct {
+	Resources  corev1.ResourceList
+	LastUpdate time.Time
+}
+
+// AfsConsumedResources manages the fair sharing consumed resources cache
+type AfsConsumedResources struct {
+	resources *utilmaps.SyncMap[utilqueue.LocalQueueReference, ConsumedResourcesEntry]
+}
+
+// NewAfsConsumedResources creates a new AfsConsumedResources cache
+func NewAfsConsumedResources() *AfsConsumedResources {
+	return &AfsConsumedResources{
+		resources: utilmaps.NewSyncMap[utilqueue.LocalQueueReference, ConsumedResourcesEntry](0),
+	}
+}
+
+// Set updates the consumed resources for a LocalQueue
+func (a *AfsConsumedResources) Set(lqKey utilqueue.LocalQueueReference, resources corev1.ResourceList, lastUpdate time.Time) {
+	a.resources.Add(lqKey, ConsumedResourcesEntry{
+		Resources:  resources,
+		LastUpdate: lastUpdate,
+	})
+}
+
+// Get retrieves the consumed resources for a LocalQueue
+func (a *AfsConsumedResources) Get(lqKey utilqueue.LocalQueueReference) (ConsumedResourcesEntry, bool) {
+	return a.resources.Get(lqKey)
+}
+
+// Delete removes the consumed resources entry for a LocalQueue
+func (a *AfsConsumedResources) Delete(lqKey utilqueue.LocalQueueReference) {
+	a.resources.Delete(lqKey)
+}
```

  IIUC this is unused, please remove

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-26T12:02:27Z

- **pkg/controller/core/localqueue_controller.go:185** by [@PBundyra](https://github.com/PBundyra) — 2025-11-26T12:02:27Z
```diff
@@ -173,10 +174,16 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	}
 
 	if afs.Enabled(r.admissionFSConfig) {
-		updated := r.initializeAdmissionFsStatus(&queueObj)
-		sinceLastUpdate := r.clock.Now().Sub(queueObj.Status.FairSharing.AdmissionFairSharingStatus.LastUpdate.Time)
+		initNeeded := r.initializeAfsIfNeeded(&queueObj)
 		lqKey := utilqueue.Key(&queueObj)
-		if interval := r.admissionFSConfig.UsageSamplingInterval.Duration; !updated && sinceLastUpdate < interval && !r.queues.HasPendingPenaltyFor(lqKey) {
+		var sinceLastUpdate time.Duration
+		if entry, found := r.queues.AfsConsumedResources.Get(lqKey); found {
+			sinceLastUpdate = r.clock.Now().Sub(entry.LastUpdate)
+		} else {
+			// No cache entry, force reconciliation by setting time >= interval
+			sinceLastUpdate = r.admissionFSConfig.UsageSamplingInterval.Duration
+		}
```

  Isn't this redundant now, given we always initialize if there was no entry before? We never delete from the cache so I think it should always be there

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-26T12:09:46Z

- **pkg/controller/core/localqueue_controller.go:185** by [@mimowo](https://github.com/mimowo) — 2025-11-26T12:09:45Z
```diff
@@ -173,10 +174,16 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	}
 
 	if afs.Enabled(r.admissionFSConfig) {
-		updated := r.initializeAdmissionFsStatus(&queueObj)
-		sinceLastUpdate := r.clock.Now().Sub(queueObj.Status.FairSharing.AdmissionFairSharingStatus.LastUpdate.Time)
+		initNeeded := r.initializeAfsIfNeeded(&queueObj)
 		lqKey := utilqueue.Key(&queueObj)
-		if interval := r.admissionFSConfig.UsageSamplingInterval.Duration; !updated && sinceLastUpdate < interval && !r.queues.HasPendingPenaltyFor(lqKey) {
+		var sinceLastUpdate time.Duration
+		if entry, found := r.queues.AfsConsumedResources.Get(lqKey); found {
+			sinceLastUpdate = r.clock.Now().Sub(entry.LastUpdate)
+		} else {
+			// No cache entry, force reconciliation by setting time >= interval
+			sinceLastUpdate = r.admissionFSConfig.UsageSamplingInterval.Duration
+		}
```

  Hm, but the call to initializeAfsIfNeeded (L177) and L180 are not under a lock, so what guarantees there is not concurrent modifcation which would remove the key in-between (for example by WorkloadController)?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-26T12:21:02Z

- **pkg/controller/core/localqueue_controller.go:230** by [@PBundyra](https://github.com/PBundyra) — 2025-11-26T12:21:02Z
  I think we should also delete from afsConsumedResources here

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-26T12:22:17Z

- **pkg/cache/queue/afs/consumed_resources.go:62** by [@mimowo](https://github.com/mimowo) — 2025-11-26T12:22:16Z
```diff
@@ -0,0 +1,62 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package afs
+
+import (
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	utilqueue "sigs.k8s.io/kueue/pkg/util/queue"
+)
+
+// ConsumedResourcesEntry stores the consumed resources for a LocalQueue with timestamp
+type ConsumedResourcesEntry struct {
+	Resources  corev1.ResourceList
+	LastUpdate time.Time
+}
+
+// AfsConsumedResources manages the fair sharing consumed resources cache
+type AfsConsumedResources struct {
+	resources *utilmaps.SyncMap[utilqueue.LocalQueueReference, ConsumedResourcesEntry]
+}
+
+// NewAfsConsumedResources creates a new AfsConsumedResources cache
+func NewAfsConsumedResources() *AfsConsumedResources {
+	return &AfsConsumedResources{
+		resources: utilmaps.NewSyncMap[utilqueue.LocalQueueReference, ConsumedResourcesEntry](0),
+	}
+}
+
+// Set updates the consumed resources for a LocalQueue
+func (a *AfsConsumedResources) Set(lqKey utilqueue.LocalQueueReference, resources corev1.ResourceList, lastUpdate time.Time) {
+	a.resources.Add(lqKey, ConsumedResourcesEntry{
+		Resources:  resources,
+		LastUpdate: lastUpdate,
+	})
+}
+
+// Get retrieves the consumed resources for a LocalQueue
+func (a *AfsConsumedResources) Get(lqKey utilqueue.LocalQueueReference) (ConsumedResourcesEntry, bool) {
+	return a.resources.Get(lqKey)
+}
+
+// Delete removes the consumed resources entry for a LocalQueue
+func (a *AfsConsumedResources) Delete(lqKey utilqueue.LocalQueueReference) {
+	a.resources.Delete(lqKey)
+}
```

  I'm not sure we want to remove the fuction. I think we might be missing invoking the function from the LQ even handler for Delete. If we never call Delete I think we may have a memory leak that the entry stay in the map outliving the deleted LQ by user.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-26T12:54:26Z

- **pkg/controller/core/localqueue_controller.go:185** by [@mimowo](https://github.com/mimowo) — 2025-11-26T12:54:26Z
```diff
@@ -173,10 +174,16 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	}
 
 	if afs.Enabled(r.admissionFSConfig) {
-		updated := r.initializeAdmissionFsStatus(&queueObj)
-		sinceLastUpdate := r.clock.Now().Sub(queueObj.Status.FairSharing.AdmissionFairSharingStatus.LastUpdate.Time)
+		initNeeded := r.initializeAfsIfNeeded(&queueObj)
 		lqKey := utilqueue.Key(&queueObj)
-		if interval := r.admissionFSConfig.UsageSamplingInterval.Duration; !updated && sinceLastUpdate < interval && !r.queues.HasPendingPenaltyFor(lqKey) {
+		var sinceLastUpdate time.Duration
+		if entry, found := r.queues.AfsConsumedResources.Get(lqKey); found {
+			sinceLastUpdate = r.clock.Now().Sub(entry.LastUpdate)
+		} else {
+			// No cache entry, force reconciliation by setting time >= interval
+			sinceLastUpdate = r.admissionFSConfig.UsageSamplingInterval.Duration
+		}
```

  Even if we only call Delete from event handler for LQ, then I think it is possible:
  1. Update triggers Reconcile
  2. Reconcile starts
  3. User deletes object
  Now "Delete" event handler may be running concurrently with the ongoing Reconcile from point (2.), and thus call AfsConsumedResources.Delete, after initializeAfsIfNeeded, but before AfsConsumedResources.Get.
  
  So, I agree this is super unlikely, but we need to be careful about the potential crash here. 
  
  As long as we handle such unlikely scenario gracefully (no panic) I'm good.No need for anything sophisticated like: L183-L184.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-26T13:32:58Z

- **pkg/cache/queue/afs/consumed_resources.go:62** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-26T13:32:58Z
```diff
@@ -0,0 +1,62 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package afs
+
+import (
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	utilqueue "sigs.k8s.io/kueue/pkg/util/queue"
+)
+
+// ConsumedResourcesEntry stores the consumed resources for a LocalQueue with timestamp
+type ConsumedResourcesEntry struct {
+	Resources  corev1.ResourceList
+	LastUpdate time.Time
+}
+
+// AfsConsumedResources manages the fair sharing consumed resources cache
+type AfsConsumedResources struct {
+	resources *utilmaps.SyncMap[utilqueue.LocalQueueReference, ConsumedResourcesEntry]
+}
+
+// NewAfsConsumedResources creates a new AfsConsumedResources cache
+func NewAfsConsumedResources() *AfsConsumedResources {
+	return &AfsConsumedResources{
+		resources: utilmaps.NewSyncMap[utilqueue.LocalQueueReference, ConsumedResourcesEntry](0),
+	}
+}
+
+// Set updates the consumed resources for a LocalQueue
+func (a *AfsConsumedResources) Set(lqKey utilqueue.LocalQueueReference, resources corev1.ResourceList, lastUpdate time.Time) {
+	a.resources.Add(lqKey, ConsumedResourcesEntry{
+		Resources:  resources,
+		LastUpdate: lastUpdate,
+	})
+}
+
+// Get retrieves the consumed resources for a LocalQueue
+func (a *AfsConsumedResources) Get(lqKey utilqueue.LocalQueueReference) (ConsumedResourcesEntry, bool) {
+	return a.resources.Get(lqKey)
+}
+
+// Delete removes the consumed resources entry for a LocalQueue
+func (a *AfsConsumedResources) Delete(lqKey utilqueue.LocalQueueReference) {
+	a.resources.Delete(lqKey)
+}
```

  Right, I was missing this. Added, thanks!

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-26T13:35:07Z

- **pkg/controller/core/localqueue_controller.go:185** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-26T13:35:07Z
```diff
@@ -173,10 +174,16 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	}
 
 	if afs.Enabled(r.admissionFSConfig) {
-		updated := r.initializeAdmissionFsStatus(&queueObj)
-		sinceLastUpdate := r.clock.Now().Sub(queueObj.Status.FairSharing.AdmissionFairSharingStatus.LastUpdate.Time)
+		initNeeded := r.initializeAfsIfNeeded(&queueObj)
 		lqKey := utilqueue.Key(&queueObj)
-		if interval := r.admissionFSConfig.UsageSamplingInterval.Duration; !updated && sinceLastUpdate < interval && !r.queues.HasPendingPenaltyFor(lqKey) {
+		var sinceLastUpdate time.Duration
+		if entry, found := r.queues.AfsConsumedResources.Get(lqKey); found {
+			sinceLastUpdate = r.clock.Now().Sub(entry.LastUpdate)
+		} else {
+			// No cache entry, force reconciliation by setting time >= interval
+			sinceLastUpdate = r.admissionFSConfig.UsageSamplingInterval.Duration
+		}
```

  Done. I’ve added a check after init to ensure the entry is still there. If not, the reconciliation is skipped.
  
  https://github.com/kubernetes-sigs/kueue/blob/19b4c9198a57c66e7dc79ff40f05b169f7710440/pkg/controller/core/localqueue_controller.go#L176-L183

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-11-26T13:43:39Z

- **pkg/cache/queue/cluster_queue_test.go:163** by [@IrvingMg](https://github.com/IrvingMg) — 2025-11-26T13:43:38Z
```diff
@@ -158,7 +160,7 @@ func Test_PushOrUpdate(t *testing.T) {
 	for name, tc := range cases {
 		t.Run(name, func(t *testing.T) {
 			ctx, _ := utiltesting.ContextWithLog(t)
-			cq := newClusterQueueImpl(ctx, nil, defaultOrdering, fakeClock, nil, false, nil)
+			cq := newClusterQueueImpl(ctx, nil, defaultOrdering, fakeClock, nil, false, nil, nil)
```

  I've created https://github.com/kubernetes-sigs/kueue/issues/7912

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-26T14:11:02Z

- **pkg/controller/core/localqueue_controller.go:230** by [@mimowo](https://github.com/mimowo) — 2025-11-26T14:11:02Z
  I can see it done here: https://github.com/kubernetes-sigs/kueue/compare/1c312e28b8dae582bf2617a0b4f9afffb475b648..19b4c9198a57c66e7dc79ff40f05b169f7710440#diff-9177ca87118e92b78007b8e08ffc92f1a18afa6fd6b4168b464e497e09ec53ecR225-R227
