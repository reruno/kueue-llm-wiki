# PR #780: [site] Run RayJobs

**Summary**: [site] Run RayJobs

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/780

**Last updated**: 2023-05-25T09:13:14Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `ray-doc` → `website-release-0.4`
- **Created**: 2023-05-18T15:21:39Z
- **Updated**: 2023-05-25T09:13:14Z
- **Merged**: 2023-05-24T19:00:50Z
- **Closed**: 2023-05-24T19:00:50Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 3   **+149 / -2**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Adds documentation on how to run a RayJob

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Ralates to  #666

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-05-18T15:21:49Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 430990d6099d8c74622e6ee7851be6b3fd75d579 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/646713f85bdccd0008d940f2 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-780--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-780--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTc4MC0ta3ViZXJuZXRlcy1zaWdzLWt1ZXVlLm5ldGxpZnkuYXBwIn0.U2_9QggJhm5MoS8ID8X042LeLCSSzSezda1qMyNsm6I)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site settings](https://app.netlify.com/sites/kubernetes-sigs-kueue/settings/deploys#deploy-notifications)._

### Comment by [@trasc](https://github.com/trasc) — 2023-05-18T15:22:51Z

/cc @alculquicondor 

Should I target this to `main` or `website-v0.4.0` ?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-18T18:00:25Z

`website-v0.4.0`

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-22T14:14:05Z

/lgtm
/assign @alculquicondor

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-24T12:24:43Z

/approve
/hold

Let's wait for the implementation PR to merge (it's in my review queue)

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-24T14:49:20Z

/milestone v0.4

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-24T18:58:43Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-05-24T18:58:51Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/780#issuecomment-1561778665" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/780#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/website-release-0.4/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-24T18:58:52Z

/hold cancel

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-21T19:37:01Z

- **site/content/en/docs/installation/_index.md:117** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-21T19:15:32Z
```diff
@@ -110,6 +110,11 @@ data:
     waitForPodsReady:
       enable: true
       timeout: 10m
+    integrations:
+      frameworks:
+      - "batch/job"
+    # - "kubeflow.org/mpijob"
+    # - "ray.io/rayj
```

  ```suggestion
      integrations:
        frameworks:
        - "batch/job"
      # - "kubeflow.org/mpijob"
      # - "ray.io/rayjob
  ```

- **site/content/en/docs/tasks/run_rayjobs.md:60** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-21T19:26:30Z
```diff
@@ -0,0 +1,141 @@
+---
+title: "Run A RayJob"
+date: 2022-05-18
+weight: 6
+description: >
+  Run a Kueue scheduled RayJob.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [KubeRay's](https://ray-project.github.io/kuberay/) [RayJob](https://ray-project.github.io/kuberay/guidance/rayjob/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+By default the integration for `ray.io/rayjob` is not enabled, check the[Installation](/docs/installation/) for details on [Kueue's manger configuration](/docs/installation/#install-a-custom-configured-released-version) 
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+
+Check [KubeRay](https://ray-project.github.io/kuberay/) documentation for installation and configuration details.
+
+## RayJob definition
+
+Besides the usual RayJob configuration while using Kueue take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayJob configuration.
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.rayClusterSpec`.
+
+```yaml
+    headGroupSpec:
+      template:
+        spec:
+          containers:
+            - resources:
+                requests:
+                  cpu: "1"
+    workerGroupSpecs:
+      - template:
+          spec:
+            containers:
+              - resources:
+                  requests:
+                    cpu: "1"
+```
+
+### c. Limitations
+
+- A Kueue managed RayJob cannot use an existing cluster.
+- The RayJob's cluster should be deleted at the end of its execution, `spec.ShutdownAfterJobFinishes` should be `true`.
+- Because Kueue will reserve the RayJob resources, the cluster's `inTreeAutoscaling` should not be enabled.
```

  ```suggestion
  - Because Kueue will reserve the RayJob resources, `spec.rayClusterSpec.enableInTreeAutoscaling` should be `false`.
  ```

- **site/content/en/docs/tasks/run_rayjobs.md:61** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-21T19:27:21Z
```diff
@@ -0,0 +1,141 @@
+---
+title: "Run A RayJob"
+date: 2022-05-18
+weight: 6
+description: >
+  Run a Kueue scheduled RayJob.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [KubeRay's](https://ray-project.github.io/kuberay/) [RayJob](https://ray-project.github.io/kuberay/guidance/rayjob/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+By default the integration for `ray.io/rayjob` is not enabled, check the[Installation](/docs/installation/) for details on [Kueue's manger configuration](/docs/installation/#install-a-custom-configured-released-version) 
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+
+Check [KubeRay](https://ray-project.github.io/kuberay/) documentation for installation and configuration details.
+
+## RayJob definition
+
+Besides the usual RayJob configuration while using Kueue take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayJob configuration.
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.rayClusterSpec`.
+
+```yaml
+    headGroupSpec:
+      template:
+        spec:
+          containers:
+            - resources:
+                requests:
+                  cpu: "1"
+    workerGroupSpecs:
+      - template:
+          spec:
+            containers:
+              - resources:
+                  requests:
+                    cpu: "1"
+```
+
+### c. Limitations
+
+- A Kueue managed RayJob cannot use an existing cluster.
+- The RayJob's cluster should be deleted at the end of its execution, `spec.ShutdownAfterJobFinishes` should be `true`.
+- Because Kueue will reserve the RayJob resources, the cluster's `inTreeAutoscaling` should not be enabled.
+- Because a Kueue workload can have a maximum of 8 PodSets, the maximum number of `workerGroupSpecs` is 7.
```

  ```suggestion
  - Because a Kueue workload can have a maximum of 8 PodSets, the maximum number of `spec.rayClusterSpec.workerGroupSpecs` is 7.
  ```

- **site/content/en/docs/tasks/run_rayjobs.md:73** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-21T19:30:24Z
```diff
@@ -0,0 +1,141 @@
+---
+title: "Run A RayJob"
+date: 2022-05-18
+weight: 6
+description: >
+  Run a Kueue scheduled RayJob.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [KubeRay's](https://ray-project.github.io/kuberay/) [RayJob](https://ray-project.github.io/kuberay/guidance/rayjob/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+By default the integration for `ray.io/rayjob` is not enabled, check the[Installation](/docs/installation/) for details on [Kueue's manger configuration](/docs/installation/#install-a-custom-configured-released-version) 
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+
+Check [KubeRay](https://ray-project.github.io/kuberay/) documentation for installation and configuration details.
+
+## RayJob definition
+
+Besides the usual RayJob configuration while using Kueue take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayJob configuration.
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.rayClusterSpec`.
+
+```yaml
+    headGroupSpec:
+      template:
+        spec:
+          containers:
+            - resources:
+                requests:
+                  cpu: "1"
+    workerGroupSpecs:
+      - template:
+          spec:
+            containers:
+              - resources:
+                  requests:
+                    cpu: "1"
+```
+
+### c. Limitations
+
+- A Kueue managed RayJob cannot use an existing cluster.
+- The RayJob's cluster should be deleted at the end of its execution, `spec.ShutdownAfterJobFinishes` should be `true`.
+- Because Kueue will reserve the RayJob resources, the cluster's `inTreeAutoscaling` should not be enabled.
+- Because a Kueue workload can have a maximum of 8 PodSets, the maximum number of `workerGroupSpecs` is 7.
+
+## Sample RayJob
+
+```yaml
+apiVersion: ray.io/v1alpha1
+kind: RayJob
+metadata:
+  generateName: sample-rayjob-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  suspend: true
```

  Do you prefer explicitly set `suspend=true`?

- **site/content/en/docs/tasks/run_rayjobs.md:67** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-21T19:36:50Z
```diff
@@ -0,0 +1,141 @@
+---
+title: "Run A RayJob"
+date: 2022-05-18
+weight: 6
+description: >
+  Run a Kueue scheduled RayJob.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [KubeRay's](https://ray-project.github.io/kuberay/) [RayJob](https://ray-project.github.io/kuberay/guidance/rayjob/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+By default the integration for `ray.io/rayjob` is not enabled, check the[Installation](/docs/installation/) for details on [Kueue's manger configuration](/docs/installation/#install-a-custom-configured-released-version) 
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+
+Check [KubeRay](https://ray-project.github.io/kuberay/) documentation for installation and configuration details.
+
+## RayJob definition
+
+Besides the usual RayJob configuration while using Kueue take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayJob configuration.
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.rayClusterSpec`.
+
+```yaml
+    headGroupSpec:
+      template:
+        spec:
+          containers:
+            - resources:
+                requests:
+                  cpu: "1"
+    workerGroupSpecs:
+      - template:
+          spec:
+            containers:
+              - resources:
+                  requests:
+                    cpu: "1"
+```
+
+### c. Limitations
+
+- A Kueue managed RayJob cannot use an existing cluster.
+- The RayJob's cluster should be deleted at the end of its execution, `spec.ShutdownAfterJobFinishes` should be `true`.
+- Because Kueue will reserve the RayJob resources, the cluster's `inTreeAutoscaling` should not be enabled.
+- Because a Kueue workload can have a maximum of 8 PodSets, the maximum number of `workerGroupSpecs` is 7.
+
+## Sample RayJob
+
+```yaml
+apiVersion: ray.io/v1alpha1
+kind: RayJob
```

  Is this inspired by https://github.com/ray-project/kuberay/blob/82c925bb3fe72e056a21521c3b6fff3b172c2817/ray-operator/config/samples/ray_v1alpha1_rayjob.yaml?
  
  If so, it would be worth mentioning that as a comment.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2023-05-22T09:27:02Z

- **site/content/en/docs/tasks/run_rayjobs.md:3** by [@denkensk](https://github.com/denkensk) — 2023-05-22T09:03:56Z
```diff
@@ -0,0 +1,141 @@
+---
+title: "Run A RayJob"
+date: 2022-05-18
```

  ```suggestion
  date: 2023-05-18
  ```

- **site/content/en/docs/tasks/run_rayjobs.md:59** by [@denkensk](https://github.com/denkensk) — 2023-05-22T09:24:43Z
```diff
@@ -0,0 +1,141 @@
+---
+title: "Run A RayJob"
+date: 2022-05-18
+weight: 6
+description: >
+  Run a Kueue scheduled RayJob.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [KubeRay's](https://ray-project.github.io/kuberay/) [RayJob](https://ray-project.github.io/kuberay/guidance/rayjob/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+By default the integration for `ray.io/rayjob` is not enabled, check the[Installation](/docs/installation/) for details on [Kueue's manger configuration](/docs/installation/#install-a-custom-configured-released-version) 
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+
+Check [KubeRay](https://ray-project.github.io/kuberay/) documentation for installation and configuration details.
+
+## RayJob definition
+
+Besides the usual RayJob configuration while using Kueue take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayJob configuration.
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.rayClusterSpec`.
+
+```yaml
+    headGroupSpec:
+      template:
+        spec:
+          containers:
+            - resources:
+                requests:
+                  cpu: "1"
+    workerGroupSpecs:
+      - template:
+          spec:
+            containers:
+              - resources:
+                  requests:
+                    cpu: "1"
+```
+
+### c. Limitations
+
+- A Kueue managed RayJob cannot use an existing cluster.
+- The RayJob's cluster should be deleted at the end of its execution, `spec.ShutdownAfterJobFinishes` should be `true`.
```

  ```suggestion
  - The Ray cluster should be deleted at the end of its execution, `spec.ShutdownAfterJobFinishes` should be `true`.
  ```

- **site/content/en/docs/tasks/run_rayjobs.md:60** by [@denkensk](https://github.com/denkensk) — 2023-05-22T09:25:43Z
```diff
@@ -0,0 +1,141 @@
+---
+title: "Run A RayJob"
+date: 2022-05-18
+weight: 6
+description: >
+  Run a Kueue scheduled RayJob.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [KubeRay's](https://ray-project.github.io/kuberay/) [RayJob](https://ray-project.github.io/kuberay/guidance/rayjob/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+By default the integration for `ray.io/rayjob` is not enabled, check the[Installation](/docs/installation/) for details on [Kueue's manger configuration](/docs/installation/#install-a-custom-configured-released-version) 
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+
+Check [KubeRay](https://ray-project.github.io/kuberay/) documentation for installation and configuration details.
+
+## RayJob definition
+
+Besides the usual RayJob configuration while using Kueue take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayJob configuration.
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.rayClusterSpec`.
+
+```yaml
+    headGroupSpec:
+      template:
+        spec:
+          containers:
+            - resources:
+                requests:
+                  cpu: "1"
+    workerGroupSpecs:
+      - template:
+          spec:
+            containers:
+              - resources:
+                  requests:
+                    cpu: "1"
+```
+
+### c. Limitations
+
+- A Kueue managed RayJob cannot use an existing cluster.
+- The RayJob's cluster should be deleted at the end of its execution, `spec.ShutdownAfterJobFinishes` should be `true`.
+- Because Kueue will reserve the RayJob resources, the cluster's `inTreeAutoscaling` should not be enabled.
```

  how about `will reserve resources for the Ray cluster`

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-22T13:55:41Z

- **site/content/en/docs/installation/_index.md:117** by [@trasc](https://github.com/trasc) — 2023-05-22T13:14:14Z
```diff
@@ -110,6 +110,11 @@ data:
     waitForPodsReady:
       enable: true
       timeout: 10m
+    integrations:
+      frameworks:
+      - "batch/job"
+    # - "kubeflow.org/mpijob"
+    # - "ray.io/rayj
```

  done

- **site/content/en/docs/tasks/run_rayjobs.md:3** by [@trasc](https://github.com/trasc) — 2023-05-22T13:14:52Z
```diff
@@ -0,0 +1,141 @@
+---
+title: "Run A RayJob"
+date: 2022-05-18
```

  done

- **site/content/en/docs/tasks/run_rayjobs.md:59** by [@trasc](https://github.com/trasc) — 2023-05-22T13:16:28Z
```diff
@@ -0,0 +1,141 @@
+---
+title: "Run A RayJob"
+date: 2022-05-18
+weight: 6
+description: >
+  Run a Kueue scheduled RayJob.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [KubeRay's](https://ray-project.github.io/kuberay/) [RayJob](https://ray-project.github.io/kuberay/guidance/rayjob/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+By default the integration for `ray.io/rayjob` is not enabled, check the[Installation](/docs/installation/) for details on [Kueue's manger configuration](/docs/installation/#install-a-custom-configured-released-version) 
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+
+Check [KubeRay](https://ray-project.github.io/kuberay/) documentation for installation and configuration details.
+
+## RayJob definition
+
+Besides the usual RayJob configuration while using Kueue take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayJob configuration.
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.rayClusterSpec`.
+
+```yaml
+    headGroupSpec:
+      template:
+        spec:
+          containers:
+            - resources:
+                requests:
+                  cpu: "1"
+    workerGroupSpecs:
+      - template:
+          spec:
+            containers:
+              - resources:
+                  requests:
+                    cpu: "1"
+```
+
+### c. Limitations
+
+- A Kueue managed RayJob cannot use an existing cluster.
+- The RayJob's cluster should be deleted at the end of its execution, `spec.ShutdownAfterJobFinishes` should be `true`.
```

  done

- **site/content/en/docs/tasks/run_rayjobs.md:60** by [@trasc](https://github.com/trasc) — 2023-05-22T13:26:01Z
```diff
@@ -0,0 +1,141 @@
+---
+title: "Run A RayJob"
+date: 2022-05-18
+weight: 6
+description: >
+  Run a Kueue scheduled RayJob.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [KubeRay's](https://ray-project.github.io/kuberay/) [RayJob](https://ray-project.github.io/kuberay/guidance/rayjob/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+By default the integration for `ray.io/rayjob` is not enabled, check the[Installation](/docs/installation/) for details on [Kueue's manger configuration](/docs/installation/#install-a-custom-configured-released-version) 
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+
+Check [KubeRay](https://ray-project.github.io/kuberay/) documentation for installation and configuration details.
+
+## RayJob definition
+
+Besides the usual RayJob configuration while using Kueue take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayJob configuration.
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.rayClusterSpec`.
+
+```yaml
+    headGroupSpec:
+      template:
+        spec:
+          containers:
+            - resources:
+                requests:
+                  cpu: "1"
+    workerGroupSpecs:
+      - template:
+          spec:
+            containers:
+              - resources:
+                  requests:
+                    cpu: "1"
+```
+
+### c. Limitations
+
+- A Kueue managed RayJob cannot use an existing cluster.
+- The RayJob's cluster should be deleted at the end of its execution, `spec.ShutdownAfterJobFinishes` should be `true`.
+- Because Kueue will reserve the RayJob resources, the cluster's `inTreeAutoscaling` should not be enabled.
```

  done

- **site/content/en/docs/tasks/run_rayjobs.md:61** by [@trasc](https://github.com/trasc) — 2023-05-22T13:27:51Z
```diff
@@ -0,0 +1,141 @@
+---
+title: "Run A RayJob"
+date: 2022-05-18
+weight: 6
+description: >
+  Run a Kueue scheduled RayJob.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [KubeRay's](https://ray-project.github.io/kuberay/) [RayJob](https://ray-project.github.io/kuberay/guidance/rayjob/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+By default the integration for `ray.io/rayjob` is not enabled, check the[Installation](/docs/installation/) for details on [Kueue's manger configuration](/docs/installation/#install-a-custom-configured-released-version) 
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+
+Check [KubeRay](https://ray-project.github.io/kuberay/) documentation for installation and configuration details.
+
+## RayJob definition
+
+Besides the usual RayJob configuration while using Kueue take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayJob configuration.
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.rayClusterSpec`.
+
+```yaml
+    headGroupSpec:
+      template:
+        spec:
+          containers:
+            - resources:
+                requests:
+                  cpu: "1"
+    workerGroupSpecs:
+      - template:
+          spec:
+            containers:
+              - resources:
+                  requests:
+                    cpu: "1"
+```
+
+### c. Limitations
+
+- A Kueue managed RayJob cannot use an existing cluster.
+- The RayJob's cluster should be deleted at the end of its execution, `spec.ShutdownAfterJobFinishes` should be `true`.
+- Because Kueue will reserve the RayJob resources, the cluster's `inTreeAutoscaling` should not be enabled.
+- Because a Kueue workload can have a maximum of 8 PodSets, the maximum number of `workerGroupSpecs` is 7.
```

  done

- **site/content/en/docs/tasks/run_rayjobs.md:73** by [@trasc](https://github.com/trasc) — 2023-05-22T13:43:39Z
```diff
@@ -0,0 +1,141 @@
+---
+title: "Run A RayJob"
+date: 2022-05-18
+weight: 6
+description: >
+  Run a Kueue scheduled RayJob.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [KubeRay's](https://ray-project.github.io/kuberay/) [RayJob](https://ray-project.github.io/kuberay/guidance/rayjob/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+By default the integration for `ray.io/rayjob` is not enabled, check the[Installation](/docs/installation/) for details on [Kueue's manger configuration](/docs/installation/#install-a-custom-configured-released-version) 
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+
+Check [KubeRay](https://ray-project.github.io/kuberay/) documentation for installation and configuration details.
+
+## RayJob definition
+
+Besides the usual RayJob configuration while using Kueue take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayJob configuration.
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.rayClusterSpec`.
+
+```yaml
+    headGroupSpec:
+      template:
+        spec:
+          containers:
+            - resources:
+                requests:
+                  cpu: "1"
+    workerGroupSpecs:
+      - template:
+          spec:
+            containers:
+              - resources:
+                  requests:
+                    cpu: "1"
+```
+
+### c. Limitations
+
+- A Kueue managed RayJob cannot use an existing cluster.
+- The RayJob's cluster should be deleted at the end of its execution, `spec.ShutdownAfterJobFinishes` should be `true`.
+- Because Kueue will reserve the RayJob resources, the cluster's `inTreeAutoscaling` should not be enabled.
+- Because a Kueue workload can have a maximum of 8 PodSets, the maximum number of `workerGroupSpecs` is 7.
+
+## Sample RayJob
+
+```yaml
+apiVersion: ray.io/v1alpha1
+kind: RayJob
+metadata:
+  generateName: sample-rayjob-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  suspend: true
```

  no, is set by the default weeb-hook anyaway, removed

- **site/content/en/docs/tasks/run_rayjobs.md:67** by [@trasc](https://github.com/trasc) — 2023-05-22T13:47:37Z
```diff
@@ -0,0 +1,141 @@
+---
+title: "Run A RayJob"
+date: 2022-05-18
+weight: 6
+description: >
+  Run a Kueue scheduled RayJob.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [KubeRay's](https://ray-project.github.io/kuberay/) [RayJob](https://ray-project.github.io/kuberay/guidance/rayjob/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+By default the integration for `ray.io/rayjob` is not enabled, check the[Installation](/docs/installation/) for details on [Kueue's manger configuration](/docs/installation/#install-a-custom-configured-released-version) 
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+
+Check [KubeRay](https://ray-project.github.io/kuberay/) documentation for installation and configuration details.
+
+## RayJob definition
+
+Besides the usual RayJob configuration while using Kueue take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayJob configuration.
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.rayClusterSpec`.
+
+```yaml
+    headGroupSpec:
+      template:
+        spec:
+          containers:
+            - resources:
+                requests:
+                  cpu: "1"
+    workerGroupSpecs:
+      - template:
+          spec:
+            containers:
+              - resources:
+                  requests:
+                    cpu: "1"
+```
+
+### c. Limitations
+
+- A Kueue managed RayJob cannot use an existing cluster.
+- The RayJob's cluster should be deleted at the end of its execution, `spec.ShutdownAfterJobFinishes` should be `true`.
+- Because Kueue will reserve the RayJob resources, the cluster's `inTreeAutoscaling` should not be enabled.
+- Because a Kueue workload can have a maximum of 8 PodSets, the maximum number of `workerGroupSpecs` is 7.
+
+## Sample RayJob
+
+```yaml
+apiVersion: ray.io/v1alpha1
+kind: RayJob
```

  It kind of is, but the config map is added and it's `generateName` based  sou you can crete multiple jobs base in the same yaml.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-22T14:00:22Z

Mostly, LGTM. I left a few comments for nits.

- **site/content/en/docs/tasks/run_rayjobs.md:15** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-22T13:58:57Z
```diff
@@ -0,0 +1,142 @@
+---
+title: "Run A RayJob"
+date: 2023-05-18
+weight: 6
+description: >
+  Run a Kueue scheduled RayJob.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [KubeRay's](https://ray-project.github.io/kuberay/) [RayJob](https://ray-project.github.io/kuberay/guidance/rayjob/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+By default the integration for `ray.io/rayjob` is not enabled, check the[Installation](/docs/installation/) for details on [Kueue's manger configuration](/docs/installation/#install-a-custom-configured-released-version) 
```

  ```suggestion
  By default the integration for `ray.io/rayjob` is not enabled, check the [Installation](/docs/installation/) for details on [Kueue's manger configuration](/docs/installation/#install-a-custom-configured-released-version).
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-22T14:09:35Z

- **site/content/en/docs/tasks/run_rayjobs.md:15** by [@trasc](https://github.com/trasc) — 2023-05-22T14:09:35Z
```diff
@@ -0,0 +1,142 @@
+---
+title: "Run A RayJob"
+date: 2023-05-18
+weight: 6
+description: >
+  Run a Kueue scheduled RayJob.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [KubeRay's](https://ray-project.github.io/kuberay/) [RayJob](https://ray-project.github.io/kuberay/guidance/rayjob/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+By default the integration for `ray.io/rayjob` is not enabled, check the[Installation](/docs/installation/) for details on [Kueue's manger configuration](/docs/installation/#install-a-custom-configured-released-version) 
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-23T18:09:44Z

- **site/content/en/docs/tasks/run_rayjobs.md:58** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-23T18:03:20Z
```diff
@@ -0,0 +1,142 @@
+---
+title: "Run A RayJob"
+date: 2023-05-18
+weight: 6
+description: >
+  Run a Kueue scheduled RayJob.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [KubeRay's](https://ray-project.github.io/kuberay/) [RayJob](https://ray-project.github.io/kuberay/guidance/rayjob/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+By default the integration for `ray.io/rayjob` is not enabled, check the [Installation](/docs/installation/) for details on [Kueue's manger configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+
+Check [KubeRay](https://ray-project.github.io/kuberay/) documentation for installation and configuration details.
+
+## RayJob definition
+
+Besides the usual RayJob configuration while using Kueue take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayJob configuration.
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.rayClusterSpec`.
+
+```yaml
+    headGroupSpec:
+      template:
+        spec:
+          containers:
+            - resources:
+                requests:
+                  cpu: "1"
+    workerGroupSpecs:
+      - template:
+          spec:
+            containers:
+              - resources:
+                  requests:
+                    cpu: "1"
+```
+
+### c. Limitations
+
+- A Kueue managed RayJob cannot use an existing cluster.
```

  ```suggestion
  - A Kueue managed RayJob cannot use an existing RayCluster.
  ```
  
  Maybe also add a link to what a Ray cluster is. Otherwise it can be confused with kubernetes cluster.

- **site/content/en/docs/tasks/run_rayjobs.md:59** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-23T18:03:49Z
```diff
@@ -0,0 +1,142 @@
+---
+title: "Run A RayJob"
+date: 2023-05-18
+weight: 6
+description: >
+  Run a Kueue scheduled RayJob.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [KubeRay's](https://ray-project.github.io/kuberay/) [RayJob](https://ray-project.github.io/kuberay/guidance/rayjob/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+By default the integration for `ray.io/rayjob` is not enabled, check the [Installation](/docs/installation/) for details on [Kueue's manger configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+
+Check [KubeRay](https://ray-project.github.io/kuberay/) documentation for installation and configuration details.
+
+## RayJob definition
+
+Besides the usual RayJob configuration while using Kueue take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayJob configuration.
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.rayClusterSpec`.
+
+```yaml
+    headGroupSpec:
+      template:
+        spec:
+          containers:
+            - resources:
+                requests:
+                  cpu: "1"
+    workerGroupSpecs:
+      - template:
+          spec:
+            containers:
+              - resources:
+                  requests:
+                    cpu: "1"
+```
+
+### c. Limitations
+
+- A Kueue managed RayJob cannot use an existing cluster.
+- The RayCluster should be deleted at the end of the job execution, `spec.ShutdownAfterJobFinishes` should be `true`.
```

  Should we do this in the webhook?

- **site/content/en/docs/tasks/run_rayjobs.md:61** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-23T18:08:33Z
```diff
@@ -0,0 +1,142 @@
+---
+title: "Run A RayJob"
+date: 2023-05-18
+weight: 6
+description: >
+  Run a Kueue scheduled RayJob.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [KubeRay's](https://ray-project.github.io/kuberay/) [RayJob](https://ray-project.github.io/kuberay/guidance/rayjob/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+By default the integration for `ray.io/rayjob` is not enabled, check the [Installation](/docs/installation/) for details on [Kueue's manger configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+
+Check [KubeRay](https://ray-project.github.io/kuberay/) documentation for installation and configuration details.
+
+## RayJob definition
+
+Besides the usual RayJob configuration while using Kueue take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayJob configuration.
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.rayClusterSpec`.
+
+```yaml
+    headGroupSpec:
+      template:
+        spec:
+          containers:
+            - resources:
+                requests:
+                  cpu: "1"
+    workerGroupSpecs:
+      - template:
+          spec:
+            containers:
+              - resources:
+                  requests:
+                    cpu: "1"
+```
+
+### c. Limitations
+
+- A Kueue managed RayJob cannot use an existing cluster.
+- The RayCluster should be deleted at the end of the job execution, `spec.ShutdownAfterJobFinishes` should be `true`.
+- Because Kueue will reserve resources for the RayCluster, `spec.rayClusterSpec.enableInTreeAutoscaling` should be `false`.
+- Because a Kueue workload can have a maximum of 8 PodSets, the maximum number of `spec.rayClusterSpec.workerGroupSpecs` is 7.
```

  Does RayJob have any validation for maximum number of `WorkerGroupSpecs`?

- **site/content/en/docs/tasks/run_rayjobs.md:75** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-23T18:08:52Z
```diff
@@ -0,0 +1,142 @@
+---
+title: "Run A RayJob"
+date: 2023-05-18
+weight: 6
+description: >
+  Run a Kueue scheduled RayJob.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [KubeRay's](https://ray-project.github.io/kuberay/) [RayJob](https://ray-project.github.io/kuberay/guidance/rayjob/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+By default the integration for `ray.io/rayjob` is not enabled, check the [Installation](/docs/installation/) for details on [Kueue's manger configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+
+Check [KubeRay](https://ray-project.github.io/kuberay/) documentation for installation and configuration details.
+
+## RayJob definition
+
+Besides the usual RayJob configuration while using Kueue take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayJob configuration.
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.rayClusterSpec`.
+
+```yaml
+    headGroupSpec:
+      template:
+        spec:
+          containers:
+            - resources:
+                requests:
+                  cpu: "1"
+    workerGroupSpecs:
+      - template:
+          spec:
+            containers:
+              - resources:
+                  requests:
+                    cpu: "1"
+```
+
+### c. Limitations
+
+- A Kueue managed RayJob cannot use an existing cluster.
+- The RayCluster should be deleted at the end of the job execution, `spec.ShutdownAfterJobFinishes` should be `true`.
+- Because Kueue will reserve resources for the RayCluster, `spec.rayClusterSpec.enableInTreeAutoscaling` should be `false`.
+- Because a Kueue workload can have a maximum of 8 PodSets, the maximum number of `spec.rayClusterSpec.workerGroupSpecs` is 7.
+
+## Sample RayJob
+
+```yaml
+apiVersion: ray.io/v1alpha1
+kind: RayJob
+metadata:
+  generateName: sample-rayjob-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  shutdownAfterJobFinishes: true
+  entrypoint: python /home/ray/samples/sample_code.py
+  runtimeEnv: ewogICAgInBpcCI6IFsKICAgICAgICAicmVxdWVzdHM9PTIuMjYuMCIsCiAgICAgICAgInBlbmR1bHVtPT0yLjEuMiIKICAgIF0sCiAgICAiZW52X3ZhcnMiOiB7ImNvdW50ZXJfbmFtZSI6ICJ0ZXN0X2NvdW50ZXIifQp9Cg==
```

  is this something users need to provide?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-24T06:53:10Z

- **site/content/en/docs/tasks/run_rayjobs.md:59** by [@trasc](https://github.com/trasc) — 2023-05-24T06:53:09Z
```diff
@@ -0,0 +1,142 @@
+---
+title: "Run A RayJob"
+date: 2023-05-18
+weight: 6
+description: >
+  Run a Kueue scheduled RayJob.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [KubeRay's](https://ray-project.github.io/kuberay/) [RayJob](https://ray-project.github.io/kuberay/guidance/rayjob/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+By default the integration for `ray.io/rayjob` is not enabled, check the [Installation](/docs/installation/) for details on [Kueue's manger configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+
+Check [KubeRay](https://ray-project.github.io/kuberay/) documentation for installation and configuration details.
+
+## RayJob definition
+
+Besides the usual RayJob configuration while using Kueue take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayJob configuration.
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.rayClusterSpec`.
+
+```yaml
+    headGroupSpec:
+      template:
+        spec:
+          containers:
+            - resources:
+                requests:
+                  cpu: "1"
+    workerGroupSpecs:
+      - template:
+          spec:
+            containers:
+              - resources:
+                  requests:
+                    cpu: "1"
+```
+
+### c. Limitations
+
+- A Kueue managed RayJob cannot use an existing cluster.
+- The RayCluster should be deleted at the end of the job execution, `spec.ShutdownAfterJobFinishes` should be `true`.
```

  It's a bit too "magical", since the Ray default is ~false.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-24T06:53:22Z

- **site/content/en/docs/tasks/run_rayjobs.md:61** by [@trasc](https://github.com/trasc) — 2023-05-24T06:53:22Z
```diff
@@ -0,0 +1,142 @@
+---
+title: "Run A RayJob"
+date: 2023-05-18
+weight: 6
+description: >
+  Run a Kueue scheduled RayJob.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [KubeRay's](https://ray-project.github.io/kuberay/) [RayJob](https://ray-project.github.io/kuberay/guidance/rayjob/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+By default the integration for `ray.io/rayjob` is not enabled, check the [Installation](/docs/installation/) for details on [Kueue's manger configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+
+Check [KubeRay](https://ray-project.github.io/kuberay/) documentation for installation and configuration details.
+
+## RayJob definition
+
+Besides the usual RayJob configuration while using Kueue take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayJob configuration.
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.rayClusterSpec`.
+
+```yaml
+    headGroupSpec:
+      template:
+        spec:
+          containers:
+            - resources:
+                requests:
+                  cpu: "1"
+    workerGroupSpecs:
+      - template:
+          spec:
+            containers:
+              - resources:
+                  requests:
+                    cpu: "1"
+```
+
+### c. Limitations
+
+- A Kueue managed RayJob cannot use an existing cluster.
+- The RayCluster should be deleted at the end of the job execution, `spec.ShutdownAfterJobFinishes` should be `true`.
+- Because Kueue will reserve resources for the RayCluster, `spec.rayClusterSpec.enableInTreeAutoscaling` should be `false`.
+- Because a Kueue workload can have a maximum of 8 PodSets, the maximum number of `spec.rayClusterSpec.workerGroupSpecs` is 7.
```

  no

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-24T06:56:06Z

- **site/content/en/docs/tasks/run_rayjobs.md:75** by [@trasc](https://github.com/trasc) — 2023-05-24T06:56:06Z
```diff
@@ -0,0 +1,142 @@
+---
+title: "Run A RayJob"
+date: 2023-05-18
+weight: 6
+description: >
+  Run a Kueue scheduled RayJob.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [KubeRay's](https://ray-project.github.io/kuberay/) [RayJob](https://ray-project.github.io/kuberay/guidance/rayjob/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+By default the integration for `ray.io/rayjob` is not enabled, check the [Installation](/docs/installation/) for details on [Kueue's manger configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+
+Check [KubeRay](https://ray-project.github.io/kuberay/) documentation for installation and configuration details.
+
+## RayJob definition
+
+Besides the usual RayJob configuration while using Kueue take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayJob configuration.
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.rayClusterSpec`.
+
+```yaml
+    headGroupSpec:
+      template:
+        spec:
+          containers:
+            - resources:
+                requests:
+                  cpu: "1"
+    workerGroupSpecs:
+      - template:
+          spec:
+            containers:
+              - resources:
+                  requests:
+                    cpu: "1"
+```
+
+### c. Limitations
+
+- A Kueue managed RayJob cannot use an existing cluster.
+- The RayCluster should be deleted at the end of the job execution, `spec.ShutdownAfterJobFinishes` should be `true`.
+- Because Kueue will reserve resources for the RayCluster, `spec.rayClusterSpec.enableInTreeAutoscaling` should be `false`.
+- Because a Kueue workload can have a maximum of 8 PodSets, the maximum number of `spec.rayClusterSpec.workerGroupSpecs` is 7.
+
+## Sample RayJob
+
+```yaml
+apiVersion: ray.io/v1alpha1
+kind: RayJob
+metadata:
+  generateName: sample-rayjob-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  shutdownAfterJobFinishes: true
+  entrypoint: python /home/ray/samples/sample_code.py
+  runtimeEnv: ewogICAgInBpcCI6IFsKICAgICAgICAicmVxdWVzdHM9PTIuMjYuMCIsCiAgICAgICAgInBlbmR1bHVtPT0yLjEuMiIKICAgIF0sCiAgICAiZW52X3ZhcnMiOiB7ImNvdW50ZXJfbmFtZSI6ICJ0ZXN0X2NvdW50ZXIifQp9Cg==
```

  looks like it 
  
  ```json
  {
      "pip": [
          "requests==2.26.0",
          "pendulum==2.1.2"
      ],
      "env_vars": {"counter_name": "test_counter"}
  }
  ```
  anyway is part of  RayJob official sample.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-24T07:01:26Z

- **site/content/en/docs/tasks/run_rayjobs.md:58** by [@trasc](https://github.com/trasc) — 2023-05-24T07:01:26Z
```diff
@@ -0,0 +1,142 @@
+---
+title: "Run A RayJob"
+date: 2023-05-18
+weight: 6
+description: >
+  Run a Kueue scheduled RayJob.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [KubeRay's](https://ray-project.github.io/kuberay/) [RayJob](https://ray-project.github.io/kuberay/guidance/rayjob/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+By default the integration for `ray.io/rayjob` is not enabled, check the [Installation](/docs/installation/) for details on [Kueue's manger configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+
+Check [KubeRay](https://ray-project.github.io/kuberay/) documentation for installation and configuration details.
+
+## RayJob definition
+
+Besides the usual RayJob configuration while using Kueue take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayJob configuration.
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.rayClusterSpec`.
+
+```yaml
+    headGroupSpec:
+      template:
+        spec:
+          containers:
+            - resources:
+                requests:
+                  cpu: "1"
+    workerGroupSpecs:
+      - template:
+          spec:
+            containers:
+              - resources:
+                  requests:
+                    cpu: "1"
+```
+
+### c. Limitations
+
+- A Kueue managed RayJob cannot use an existing cluster.
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-24T12:24:11Z

- **site/content/en/docs/tasks/run_rayjobs.md:75** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-24T12:24:10Z
```diff
@@ -0,0 +1,142 @@
+---
+title: "Run A RayJob"
+date: 2023-05-18
+weight: 6
+description: >
+  Run a Kueue scheduled RayJob.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [KubeRay's](https://ray-project.github.io/kuberay/) [RayJob](https://ray-project.github.io/kuberay/guidance/rayjob/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+By default the integration for `ray.io/rayjob` is not enabled, check the [Installation](/docs/installation/) for details on [Kueue's manger configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+
+Check [KubeRay](https://ray-project.github.io/kuberay/) documentation for installation and configuration details.
+
+## RayJob definition
+
+Besides the usual RayJob configuration while using Kueue take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayJob configuration.
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.rayClusterSpec`.
+
+```yaml
+    headGroupSpec:
+      template:
+        spec:
+          containers:
+            - resources:
+                requests:
+                  cpu: "1"
+    workerGroupSpecs:
+      - template:
+          spec:
+            containers:
+              - resources:
+                  requests:
+                    cpu: "1"
+```
+
+### c. Limitations
+
+- A Kueue managed RayJob cannot use an existing cluster.
+- The RayCluster should be deleted at the end of the job execution, `spec.ShutdownAfterJobFinishes` should be `true`.
+- Because Kueue will reserve resources for the RayCluster, `spec.rayClusterSpec.enableInTreeAutoscaling` should be `false`.
+- Because a Kueue workload can have a maximum of 8 PodSets, the maximum number of `spec.rayClusterSpec.workerGroupSpecs` is 7.
+
+## Sample RayJob
+
+```yaml
+apiVersion: ray.io/v1alpha1
+kind: RayJob
+metadata:
+  generateName: sample-rayjob-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  shutdownAfterJobFinishes: true
+  entrypoint: python /home/ray/samples/sample_code.py
+  runtimeEnv: ewogICAgInBpcCI6IFsKICAgICAgICAicmVxdWVzdHM9PTIuMjYuMCIsCiAgICAgICAgInBlbmR1bHVtPT0yLjEuMiIKICAgIF0sCiAgICAiZW52X3ZhcnMiOiB7ImNvdW50ZXJfbmFtZSI6ICJ0ZXN0X2NvdW50ZXIifQp9Cg==
```

  oh, this is a HEX yaml?
  
  Odd

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-24T14:57:15Z

- **site/content/en/docs/tasks/run_rayjobs.md:75** by [@trasc](https://github.com/trasc) — 2023-05-24T14:57:14Z
```diff
@@ -0,0 +1,142 @@
+---
+title: "Run A RayJob"
+date: 2023-05-18
+weight: 6
+description: >
+  Run a Kueue scheduled RayJob.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [KubeRay's](https://ray-project.github.io/kuberay/) [RayJob](https://ray-project.github.io/kuberay/guidance/rayjob/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+By default the integration for `ray.io/rayjob` is not enabled, check the [Installation](/docs/installation/) for details on [Kueue's manger configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+
+Check [KubeRay](https://ray-project.github.io/kuberay/) documentation for installation and configuration details.
+
+## RayJob definition
+
+Besides the usual RayJob configuration while using Kueue take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayJob configuration.
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.rayClusterSpec`.
+
+```yaml
+    headGroupSpec:
+      template:
+        spec:
+          containers:
+            - resources:
+                requests:
+                  cpu: "1"
+    workerGroupSpecs:
+      - template:
+          spec:
+            containers:
+              - resources:
+                  requests:
+                    cpu: "1"
+```
+
+### c. Limitations
+
+- A Kueue managed RayJob cannot use an existing cluster.
+- The RayCluster should be deleted at the end of the job execution, `spec.ShutdownAfterJobFinishes` should be `true`.
+- Because Kueue will reserve resources for the RayCluster, `spec.rayClusterSpec.enableInTreeAutoscaling` should be `false`.
+- Because a Kueue workload can have a maximum of 8 PodSets, the maximum number of `spec.rayClusterSpec.workerGroupSpecs` is 7.
+
+## Sample RayJob
+
+```yaml
+apiVersion: ray.io/v1alpha1
+kind: RayJob
+metadata:
+  generateName: sample-rayjob-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  shutdownAfterJobFinishes: true
+  entrypoint: python /home/ray/samples/sample_code.py
+  runtimeEnv: ewogICAgInBpcCI6IFsKICAgICAgICAicmVxdWVzdHM9PTIuMjYuMCIsCiAgICAgICAgInBlbmR1bHVtPT0yLjEuMiIKICAgIF0sCiAgICAiZW52X3ZhcnMiOiB7ImNvdW50ZXJfbmFtZSI6ICJ0ZXN0X2NvdW50ZXIifQp9Cg==
```

  is just base64 , it might be a byte slice it dose not matter to much
