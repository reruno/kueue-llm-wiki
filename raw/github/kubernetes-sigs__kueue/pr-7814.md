# PR #7814: Add helm chart for `kueue-populator`

**Summary**: Add helm chart for `kueue-populator`

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7814

**Last updated**: 2025-11-24T16:56:47Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@j-skiba](https://github.com/j-skiba)
- **Branch**: `add-helm-chart-for-kueue-populator` → `main`
- **Created**: 2025-11-21T14:22:43Z
- **Updated**: 2025-11-24T16:56:47Z
- **Merged**: 2025-11-24T16:56:45Z
- **Closed**: 2025-11-24T16:56:45Z
- **Labels**: `kind/feature`, `lgtm`, `release-note-none`, `size/XL`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@gabesaba](https://github.com/gabesaba), [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 11   **+723 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

/kind feature

#### What this PR does / why we need it:

- add Helm chart for installing `kueue-populator` along with other default resources as described here - https://github.com/kubernetes-sigs/lws/issues/665

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #7347 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-11-21T14:22:49Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 59d1eda004ccb5252a8dd926c887a29ff6bf7e9f |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6924659857e9960008118a67 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-21T14:22:53Z

Hi @j-skiba. Thanks for your PR.

I'm waiting for a [github.com](https://kubernetes-sigs/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-21T14:24:42Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-21T15:03:48Z

cc @Edwinhr716
ptal since this is taking over the responsibility of https://github.com/kubernetes-sigs/kueue/pull/7368

### Comment by [@j-skiba](https://github.com/j-skiba) — 2025-11-24T06:50:56Z

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-24T10:24:50Z

@j-skiba plase rebase

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-24T16:30:10Z

/lgtm 
/approve
Let's follow up with publishing the images and end to end testing of the integration with lws

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-24T16:30:18Z

LGTM label has been added.  <details>Git tree hash: 201a8618b4503c61b3fc92f497e5f8c0877a050a</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-24T16:39:57Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7814#" title="Author self-approved">j-skiba</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7814#pullrequestreview-3501319721" title="Approved">kshalot</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7814#issuecomment-3571676744" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T11:00:48Z

- **cmd/experimental/kueue-populator/charts/kueue-populator/values.yaml:72** by [@mimowo](https://github.com/mimowo) — 2025-11-24T11:00:48Z
```diff
@@ -0,0 +1,81 @@
+# Configuration for the Kueue Prepopulator component
+kueuePopulator:
+  image:
+    # -- Image repository for the populator
+    # REQUIRED: Specify the repository where your kueue-populator image is pushed.
+    # Example: <YOUR_REGISTRY>/kueue-populator
+    repository: null
+    # -- Image tag for the populator
+    # REQUIRED: Specify the tag of the image you want to deploy.
+    # Example: latest
+    tag: null
+    # -- Image pull policy
+    pullPolicy: IfNotPresent
+  
+  # -- Image pull secrets for the populator
+  imagePullSecrets: []
+  
+  # Configuration for the controller
+  config:
+    # -- Name of the default LocalQueue to create in namespaces
+    localQueueName: "default"
+    # -- Name of the default ClusterQueue to create and reference in LocalQueues
+    clusterQueueName: "cluster-queue"
+    
+    # -- Resources to configure in the default ResourceFlavor and ClusterQueue
+    resources:
+      - name: "cpu"
+        nominalQuota: "10"
+      - name: "memory"
+        nominalQuota: "10Gi"
+      - name: "nvidia.com/gpu"
+        nominalQuota: "100"
+    
+    # -- List of topology levels. Enables Topology creation if not empty.
+    # Example:
+    # tasLevels:
+    #   - name: cloud.google.com/gke-nodepool
+    tasLevels: []
+    
+    # -- Node labels to associate with the default ResourceFlavor
+    # Example:
+    # nodeLabel:
+    #   cloud.google.com/gke-nodepool: "default-pool"
+    nodeLabel: {}
+
+    # -- Label selector to filter namespaces where the default LocalQueue will be created.
+    managedJobsNamespaceSelector:
+      matchExpressions:
+        - key: kubernetes.io/metadata.name
+          operator: NotIn
+          values: [ "kube-system", "kueue-system" ]
+
+  # -- Resource requests and limits for the populator deployment
+  resources:
+    limits:
+      cpu: 100m
+      memory: 100Mi
+    requests:
+      cpu: 100m
+      memory: 100Mi
+
+# Default values for the dependency 'kueue'
+kueue:
+  # -- Enable the Kueue subchart
+  enabled: true
+  managerConfig:
+    # -- Overrides for the controller manager config
+    controllerManagerConfigYaml: |-
+      apiVersion: config.kueue.x-k8s.io/v1beta1
+      kind: Configuration
+      # EXCLUDE kueue-system from webhooks to prevent deadlock with the post-install hook
+      managedJobsNamespaceSelector:
```

  Hm, I'm not sure if this project needs its own managedJobsNamespaceSelector. Would it be possible to read and parse Kueue's configMap instead?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T11:02:30Z

- **cmd/experimental/kueue-populator/charts/kueue-populator/README.md:88** by [@mimowo](https://github.com/mimowo) — 2025-11-24T11:02:30Z
```diff
@@ -0,0 +1,139 @@
+# Kueue Prepopulator
+
+This Helm chart installs the Kueue Prepopulator, a component designed to automatically create default LocalQueue resources in namespaces, and sets up initial Kueue resources like a default ClusterQueue and ResourceFlavor. It includes the official Kueue chart as a dependency.
+
+## Purpose
+
+-   Deploys the `kueue-populator` controller manager.
+-   Installs Kueue (via subchart dependency).
+-   Creates a default `ResourceFlavor` named `tas-gpu-default`.
+-   Creates a default `ClusterQueue` (name configurable).
+-   The populator then creates a default `LocalQueue` (name configurable) in namespaces matching the selector, pointing to the default ClusterQueue.
+
+## Prerequisites
+
+-   [Helm](https://helm.sh/docs/intro/quickstart/#install-helm)
+-   Kubernetes cluster
+-   (Optional) [Cert-manager](https://cert-manager.io/docs/installation/)
+-   Docker or a compatible container builder.
+-   A container registry to push the image to.
+
+## Building the Image
+
+You need to build and push the image to your own registry.
+ 
+From the `cmd/experimental/kueue-populator` directory:
+ 
+```bash
+# Build the image
+make image-build IMAGE_REGISTRY=<YOUR_REGISTRY>
+ 
+# Push the image
+make image-push IMAGE_REGISTRY=<YOUR_REGISTRY>
+```
+ 
+This will build and push an image with the tag `<YOUR_REGISTRY>/kueue-populator:<GIT_TAG>`.
+ 
+If you want to use a specific tag, you can override `GIT_TAG`:
+ 
+```bash
+make image-build image-push IMAGE_REGISTRY=<YOUR_REGISTRY> GIT_TAG=latest
+```
+
+## Installation
+
+To install the chart, you MUST override the image repository and tag with the image you built and pushed.
+
+The following commands assume you are in the `cmd/experimental/kueue-populator` directory.
+
+Example using `--set`:
+
+```bash
+helm install kueue-populator ./charts/kueue-populator --namespace kueue-system --create-namespace \
+  --set kueuePopulator.image.repository=<YOUR_REGISTRY>/kueue-populator \
+  --set kueuePopulator.image.tag=latest
+```
+
+Example using a custom `my-values.yaml`:
+
+```yaml
+# my-values.yaml
+kueuePopulator:
+  image:
+    repository: <YOUR_REGISTRY>/kueue-populator
+    tag: latest
+```
+
+```bash
+helm install kueue-populator ./charts/kueue-populator --namespace kueue-system --create-namespace -f my-values.yaml
+```
+
+## Configuration
+
+### Kueue Prepopulator Configuration
+
+The following table lists the configurable parameters under the `kueuePopulator` key in `values.yaml`:
+
+| Key                                                | Type     | Default           | Description                                                                                                |
+| -------------------------------------------------- | -------- | ----------------- | ---------------------------------------------------------------------------------------------------------- |
+| `image.repository`                                 | string   | `null`            | **Required.** Image repository for the populator (e.g., `<YOUR_REGISTRY>/kueue-populator`)            |
+| `image.tag`                                        | string   | `null`            | **Required.** Image tag for the populator (e.g., `latest`)                                                |
+| `image.pullPolicy`                                 | string   | `IfNotPresent`    | Image pull policy                                                                                          |
+| `config.localQueueName`                            | string   | `default`         | Name of the default LocalQueue to create in namespaces                                                     |
+| `config.clusterQueueName`                          | string   | `cluster-queue`   | Name of the default ClusterQueue to create and reference in LocalQueues                                    |
+| `config.resources`                                 | list     | (see values.yaml) | Resources to configure in the default ResourceFlavor and ClusterQueue                                      |
+| `config.tasLevels`                                 | list     | `[]`              | Optional list of node labels for Topology Aware Scheduling levels. Enables Topology creation.            |
+| `config.nodeLabel`                                 | object   | `{}`              | Node labels to associate with the default ResourceFlavor.                                                  |
+| `config.managedJobsNamespaceSelector`              | object   | (see values.yaml) | Label selector to filter namespaces where the default LocalQueue will be created. Excludes system namespaces. |
+| `resources`                                        | object   | (see values.yaml) | CPU/Memory resource requests and limits for the populator deployment.                                   |
```

  tbh, I don't see a need to parametrize it yet. We don't paramatrize it for Kueue either for now, iirc

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T11:04:00Z

- **cmd/experimental/kueue-populator/charts/kueue-populator/README.md:84** by [@mimowo](https://github.com/mimowo) — 2025-11-24T11:04:00Z
```diff
@@ -0,0 +1,139 @@
+# Kueue Prepopulator
+
+This Helm chart installs the Kueue Prepopulator, a component designed to automatically create default LocalQueue resources in namespaces, and sets up initial Kueue resources like a default ClusterQueue and ResourceFlavor. It includes the official Kueue chart as a dependency.
+
+## Purpose
+
+-   Deploys the `kueue-populator` controller manager.
+-   Installs Kueue (via subchart dependency).
+-   Creates a default `ResourceFlavor` named `tas-gpu-default`.
+-   Creates a default `ClusterQueue` (name configurable).
+-   The populator then creates a default `LocalQueue` (name configurable) in namespaces matching the selector, pointing to the default ClusterQueue.
+
+## Prerequisites
+
+-   [Helm](https://helm.sh/docs/intro/quickstart/#install-helm)
+-   Kubernetes cluster
+-   (Optional) [Cert-manager](https://cert-manager.io/docs/installation/)
+-   Docker or a compatible container builder.
+-   A container registry to push the image to.
+
+## Building the Image
+
+You need to build and push the image to your own registry.
+ 
+From the `cmd/experimental/kueue-populator` directory:
+ 
+```bash
+# Build the image
+make image-build IMAGE_REGISTRY=<YOUR_REGISTRY>
+ 
+# Push the image
+make image-push IMAGE_REGISTRY=<YOUR_REGISTRY>
+```
+ 
+This will build and push an image with the tag `<YOUR_REGISTRY>/kueue-populator:<GIT_TAG>`.
+ 
+If you want to use a specific tag, you can override `GIT_TAG`:
+ 
+```bash
+make image-build image-push IMAGE_REGISTRY=<YOUR_REGISTRY> GIT_TAG=latest
+```
+
+## Installation
+
+To install the chart, you MUST override the image repository and tag with the image you built and pushed.
+
+The following commands assume you are in the `cmd/experimental/kueue-populator` directory.
+
+Example using `--set`:
+
+```bash
+helm install kueue-populator ./charts/kueue-populator --namespace kueue-system --create-namespace \
+  --set kueuePopulator.image.repository=<YOUR_REGISTRY>/kueue-populator \
+  --set kueuePopulator.image.tag=latest
+```
+
+Example using a custom `my-values.yaml`:
+
+```yaml
+# my-values.yaml
+kueuePopulator:
+  image:
+    repository: <YOUR_REGISTRY>/kueue-populator
+    tag: latest
+```
+
+```bash
+helm install kueue-populator ./charts/kueue-populator --namespace kueue-system --create-namespace -f my-values.yaml
+```
+
+## Configuration
+
+### Kueue Prepopulator Configuration
+
+The following table lists the configurable parameters under the `kueuePopulator` key in `values.yaml`:
+
+| Key                                                | Type     | Default           | Description                                                                                                |
+| -------------------------------------------------- | -------- | ----------------- | ---------------------------------------------------------------------------------------------------------- |
+| `image.repository`                                 | string   | `null`            | **Required.** Image repository for the populator (e.g., `<YOUR_REGISTRY>/kueue-populator`)            |
+| `image.tag`                                        | string   | `null`            | **Required.** Image tag for the populator (e.g., `latest`)                                                |
+| `image.pullPolicy`                                 | string   | `IfNotPresent`    | Image pull policy                                                                                          |
+| `config.localQueueName`                            | string   | `default`         | Name of the default LocalQueue to create in namespaces                                                     |
+| `config.clusterQueueName`                          | string   | `cluster-queue`   | Name of the default ClusterQueue to create and reference in LocalQueues                                    |
+| `config.resources`                                 | list     | (see values.yaml) | Resources to configure in the default ResourceFlavor and ClusterQueue                                      |
```

  can we encapsulate the settings under config.clusterQueue? Then it will allow to easily skip clusterQueue creation by not setting the key. So I can imagine: config.clusterQueue.name and config.clusterQueue.resoruces.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T11:05:03Z

- **cmd/experimental/kueue-populator/charts/kueue-populator/README.md:86** by [@mimowo](https://github.com/mimowo) — 2025-11-24T11:05:03Z
```diff
@@ -0,0 +1,139 @@
+# Kueue Prepopulator
+
+This Helm chart installs the Kueue Prepopulator, a component designed to automatically create default LocalQueue resources in namespaces, and sets up initial Kueue resources like a default ClusterQueue and ResourceFlavor. It includes the official Kueue chart as a dependency.
+
+## Purpose
+
+-   Deploys the `kueue-populator` controller manager.
+-   Installs Kueue (via subchart dependency).
+-   Creates a default `ResourceFlavor` named `tas-gpu-default`.
+-   Creates a default `ClusterQueue` (name configurable).
+-   The populator then creates a default `LocalQueue` (name configurable) in namespaces matching the selector, pointing to the default ClusterQueue.
+
+## Prerequisites
+
+-   [Helm](https://helm.sh/docs/intro/quickstart/#install-helm)
+-   Kubernetes cluster
+-   (Optional) [Cert-manager](https://cert-manager.io/docs/installation/)
+-   Docker or a compatible container builder.
+-   A container registry to push the image to.
+
+## Building the Image
+
+You need to build and push the image to your own registry.
+ 
+From the `cmd/experimental/kueue-populator` directory:
+ 
+```bash
+# Build the image
+make image-build IMAGE_REGISTRY=<YOUR_REGISTRY>
+ 
+# Push the image
+make image-push IMAGE_REGISTRY=<YOUR_REGISTRY>
+```
+ 
+This will build and push an image with the tag `<YOUR_REGISTRY>/kueue-populator:<GIT_TAG>`.
+ 
+If you want to use a specific tag, you can override `GIT_TAG`:
+ 
+```bash
+make image-build image-push IMAGE_REGISTRY=<YOUR_REGISTRY> GIT_TAG=latest
+```
+
+## Installation
+
+To install the chart, you MUST override the image repository and tag with the image you built and pushed.
+
+The following commands assume you are in the `cmd/experimental/kueue-populator` directory.
+
+Example using `--set`:
+
+```bash
+helm install kueue-populator ./charts/kueue-populator --namespace kueue-system --create-namespace \
+  --set kueuePopulator.image.repository=<YOUR_REGISTRY>/kueue-populator \
+  --set kueuePopulator.image.tag=latest
+```
+
+Example using a custom `my-values.yaml`:
+
+```yaml
+# my-values.yaml
+kueuePopulator:
+  image:
+    repository: <YOUR_REGISTRY>/kueue-populator
+    tag: latest
+```
+
+```bash
+helm install kueue-populator ./charts/kueue-populator --namespace kueue-system --create-namespace -f my-values.yaml
+```
+
+## Configuration
+
+### Kueue Prepopulator Configuration
+
+The following table lists the configurable parameters under the `kueuePopulator` key in `values.yaml`:
+
+| Key                                                | Type     | Default           | Description                                                                                                |
+| -------------------------------------------------- | -------- | ----------------- | ---------------------------------------------------------------------------------------------------------- |
+| `image.repository`                                 | string   | `null`            | **Required.** Image repository for the populator (e.g., `<YOUR_REGISTRY>/kueue-populator`)            |
+| `image.tag`                                        | string   | `null`            | **Required.** Image tag for the populator (e.g., `latest`)                                                |
+| `image.pullPolicy`                                 | string   | `IfNotPresent`    | Image pull policy                                                                                          |
+| `config.localQueueName`                            | string   | `default`         | Name of the default LocalQueue to create in namespaces                                                     |
+| `config.clusterQueueName`                          | string   | `cluster-queue`   | Name of the default ClusterQueue to create and reference in LocalQueues                                    |
+| `config.resources`                                 | list     | (see values.yaml) | Resources to configure in the default ResourceFlavor and ClusterQueue                                      |
+| `config.tasLevels`                                 | list     | `[]`              | Optional list of node labels for Topology Aware Scheduling levels. Enables Topology creation.            |
+| `config.nodeLabel`                                 | object   | `{}`              | Node labels to associate with the default ResourceFlavor.                                                  |
```

  Here similarly I think it would be cleaner to encapsulate under config.topology, and config.resourceFlavor, respectively

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T11:55:44Z

- **cmd/experimental/kueue-populator/Makefile:29** by [@mimowo](https://github.com/mimowo) — 2025-11-24T11:55:44Z
```diff
@@ -26,6 +26,22 @@ BASE_BUILDER_IMAGE ?= golang:1.25.0
 BUILDER_IMAGE ?= $(BASE_BUILDER_IMAGE)
 CGO_ENABLED ?= 0
 
+# Integration tests
```

  Hm, any particular reason for moving this up? If this is just a drive-by cleanup, then send a dedicated PR for it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T11:56:01Z

- **cmd/experimental/kueue-populator/Makefile:110** by [@mimowo](https://github.com/mimowo) — 2025-11-24T11:56:01Z
```diff
@@ -90,3 +97,31 @@ test-e2e:
 test: ## Run unit tests.
 	GOWORK=off $(GOTESTSUM) --junitfile $(ARTIFACTS)/junit-unit.xml -- \
 		-race -coverpkg=./... -coverprofile $(ARTIFACTS)/cover-unit.out ./pkg/controller/...
+
+
+.PHONY: update-helm
+update-helm: manifests $(YAML_PROCESSOR)
+	$(YAML_PROCESSOR) -zap-log-level=$(YAML_PROCESSOR_LOG_LEVEL) hack/processing-plan.yaml
+
+$(YAML_PROCESSOR):
+	cd $(ROOT_DIR) && $(MAKE) yaml-processor
+
+
+
```

  nit, make sure only one new line

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T11:56:58Z

- **cmd/experimental/kueue-populator/Makefile:118** by [@mimowo](https://github.com/mimowo) — 2025-11-24T11:56:58Z
```diff
@@ -90,3 +97,31 @@ test-e2e:
 test: ## Run unit tests.
 	GOWORK=off $(GOTESTSUM) --junitfile $(ARTIFACTS)/junit-unit.xml -- \
 		-race -coverpkg=./... -coverprofile $(ARTIFACTS)/cover-unit.out ./pkg/controller/...
+
+
+.PHONY: update-helm
+update-helm: manifests $(YAML_PROCESSOR)
+	$(YAML_PROCESSOR) -zap-log-level=$(YAML_PROCESSOR_LOG_LEVEL) hack/processing-plan.yaml
+
+$(YAML_PROCESSOR):
+	cd $(ROOT_DIR) && $(MAKE) yaml-processor
+
+
+
+.PHONY: helm-lint
+helm-lint: ## Run Helm chart lint test.
+	$(HELM) lint charts/kueue-populator
+
+.PHONY: helm-verify
+helm-verify: helm-lint ## run helm template and detect any rendering failures
+	$(HELM) template charts/kueue-populator > /dev/null
+	$(HELM) template charts/kueue-populator --set image.repository=my-repo --set image.tag=v1.0.0 > /dev/null
```

  As for the version use `v0.1.0` rather than `v1.0.0`. We need it to be before Kueue, because if we unify one day (merge the project into Kueue), then we don't want the version to be downgraded.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-24T12:31:31Z

- **cmd/experimental/kueue-populator/charts/kueue-populator/README.md:1** by [@kshalot](https://github.com/kshalot) — 2025-11-24T12:30:26Z
```diff
@@ -0,0 +1,139 @@
+# Kueue Prepopulator
```

  Since it was renamed:
  ```suggestion
  # Kueue Populator
  ```

- **cmd/experimental/kueue-populator/charts/kueue-populator/Chart.yaml:3** by [@kshalot](https://github.com/kshalot) — 2025-11-24T12:30:44Z
```diff
@@ -0,0 +1,11 @@
+apiVersion: v2
+name: kueue-populator
+description: A Helm chart for Kueue Prepopulator setup including Kueue, LocalQueue Creator, and default resources.
```

  ```suggestion
  description: A Helm chart for Kueue Populator setup including Kueue, LocalQueue Creator, and default resources.
  ```

- **cmd/experimental/kueue-populator/charts/kueue-populator/README.md:3** by [@kshalot](https://github.com/kshalot) — 2025-11-24T12:30:56Z
```diff
@@ -0,0 +1,139 @@
+# Kueue Prepopulator
+
+This Helm chart installs the Kueue Prepopulator, a component designed to automatically create default LocalQueue resources in namespaces, and sets up initial Kueue resources like a default ClusterQueue and ResourceFlavor. It includes the official Kueue chart as a dependency.
```

  ```suggestion
  This Helm chart installs the Kueue Populator, a component designed to automatically create default LocalQueue resources in namespaces, and sets up initial Kueue resources like a default ClusterQueue and ResourceFlavor. It includes the official Kueue chart as a dependency.
  ```

- **cmd/experimental/kueue-populator/charts/kueue-populator/README.md:73** by [@kshalot](https://github.com/kshalot) — 2025-11-24T12:31:08Z
```diff
@@ -0,0 +1,139 @@
+# Kueue Prepopulator
+
+This Helm chart installs the Kueue Prepopulator, a component designed to automatically create default LocalQueue resources in namespaces, and sets up initial Kueue resources like a default ClusterQueue and ResourceFlavor. It includes the official Kueue chart as a dependency.
+
+## Purpose
+
+-   Deploys the `kueue-populator` controller manager.
+-   Installs Kueue (via subchart dependency).
+-   Creates a default `ResourceFlavor` named `tas-gpu-default`.
+-   Creates a default `ClusterQueue` (name configurable).
+-   The populator then creates a default `LocalQueue` (name configurable) in namespaces matching the selector, pointing to the default ClusterQueue.
+
+## Prerequisites
+
+-   [Helm](https://helm.sh/docs/intro/quickstart/#install-helm)
+-   Kubernetes cluster
+-   (Optional) [Cert-manager](https://cert-manager.io/docs/installation/)
+-   Docker or a compatible container builder.
+-   A container registry to push the image to.
+
+## Building the Image
+
+You need to build and push the image to your own registry.
+ 
+From the `cmd/experimental/kueue-populator` directory:
+ 
+```bash
+# Build the image
+make image-build IMAGE_REGISTRY=<YOUR_REGISTRY>
+ 
+# Push the image
+make image-push IMAGE_REGISTRY=<YOUR_REGISTRY>
+```
+ 
+This will build and push an image with the tag `<YOUR_REGISTRY>/kueue-populator:<GIT_TAG>`.
+ 
+If you want to use a specific tag, you can override `GIT_TAG`:
+ 
+```bash
+make image-build image-push IMAGE_REGISTRY=<YOUR_REGISTRY> GIT_TAG=latest
+```
+
+## Installation
+
+To install the chart, you MUST override the image repository and tag with the image you built and pushed.
+
+The following commands assume you are in the `cmd/experimental/kueue-populator` directory.
+
+Example using `--set`:
+
+```bash
+helm install kueue-populator ./charts/kueue-populator --namespace kueue-system --create-namespace \
+  --set kueuePopulator.image.repository=<YOUR_REGISTRY>/kueue-populator \
+  --set kueuePopulator.image.tag=latest
+```
+
+Example using a custom `my-values.yaml`:
+
+```yaml
+# my-values.yaml
+kueuePopulator:
+  image:
+    repository: <YOUR_REGISTRY>/kueue-populator
+    tag: latest
+```
+
+```bash
+helm install kueue-populator ./charts/kueue-populator --namespace kueue-system --create-namespace -f my-values.yaml
+```
+
+## Configuration
+
+### Kueue Prepopulator Configuration
```

  ```suggestion
  ### Kueue Populator Configuration
  ```

- **cmd/experimental/kueue-populator/charts/kueue-populator/values.yaml:1** by [@kshalot](https://github.com/kshalot) — 2025-11-24T12:31:18Z
```diff
@@ -0,0 +1,81 @@
+# Configuration for the Kueue Prepopulator component
```

  ```suggestion
  # Configuration for the Kueue Populator component
  ```

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-24T12:57:03Z

- **cmd/experimental/kueue-populator/charts/kueue-populator/Chart.yaml:9** by [@kshalot](https://github.com/kshalot) — 2025-11-24T12:57:03Z
```diff
@@ -0,0 +1,11 @@
+apiVersion: v2
+name: kueue-populator
+description: A Helm chart for Kueue Prepopulator setup including Kueue, LocalQueue Creator, and default resources.
+type: application
+version: 0.1.0
+appVersion: "0.1.0"
+dependencies:
+  - name: kueue
+    version: "0.14.4"
```

  What is the plan to keep this up-to-date?
  
  This could be bound to a version range. A "wide" range would be:
  ```suggestion
      version: ">= 0.14.4 < 1.0.0"
  ```
  
  I'd at least consider a patch-level bind:
  ```suggestion
      version: "~0.14.4"
  ```

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2025-11-24T13:29:55Z

- **cmd/experimental/kueue-populator/Makefile:29** by [@j-skiba](https://github.com/j-skiba) — 2025-11-24T13:29:54Z
```diff
@@ -26,6 +26,22 @@ BASE_BUILDER_IMAGE ?= golang:1.25.0
 BUILDER_IMAGE ?= $(BASE_BUILDER_IMAGE)
 CGO_ENABLED ?= 0
 
+# Integration tests
```

  it was moved by an accident, removed

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2025-11-24T13:38:00Z

- **cmd/experimental/kueue-populator/charts/kueue-populator/values.yaml:72** by [@j-skiba](https://github.com/j-skiba) — 2025-11-24T13:38:00Z
```diff
@@ -0,0 +1,81 @@
+# Configuration for the Kueue Prepopulator component
+kueuePopulator:
+  image:
+    # -- Image repository for the populator
+    # REQUIRED: Specify the repository where your kueue-populator image is pushed.
+    # Example: <YOUR_REGISTRY>/kueue-populator
+    repository: null
+    # -- Image tag for the populator
+    # REQUIRED: Specify the tag of the image you want to deploy.
+    # Example: latest
+    tag: null
+    # -- Image pull policy
+    pullPolicy: IfNotPresent
+  
+  # -- Image pull secrets for the populator
+  imagePullSecrets: []
+  
+  # Configuration for the controller
+  config:
+    # -- Name of the default LocalQueue to create in namespaces
+    localQueueName: "default"
+    # -- Name of the default ClusterQueue to create and reference in LocalQueues
+    clusterQueueName: "cluster-queue"
+    
+    # -- Resources to configure in the default ResourceFlavor and ClusterQueue
+    resources:
+      - name: "cpu"
+        nominalQuota: "10"
+      - name: "memory"
+        nominalQuota: "10Gi"
+      - name: "nvidia.com/gpu"
+        nominalQuota: "100"
+    
+    # -- List of topology levels. Enables Topology creation if not empty.
+    # Example:
+    # tasLevels:
+    #   - name: cloud.google.com/gke-nodepool
+    tasLevels: []
+    
+    # -- Node labels to associate with the default ResourceFlavor
+    # Example:
+    # nodeLabel:
+    #   cloud.google.com/gke-nodepool: "default-pool"
+    nodeLabel: {}
+
+    # -- Label selector to filter namespaces where the default LocalQueue will be created.
+    managedJobsNamespaceSelector:
+      matchExpressions:
+        - key: kubernetes.io/metadata.name
+          operator: NotIn
+          values: [ "kube-system", "kueue-system" ]
+
+  # -- Resource requests and limits for the populator deployment
+  resources:
+    limits:
+      cpu: 100m
+      memory: 100Mi
+    requests:
+      cpu: 100m
+      memory: 100Mi
+
+# Default values for the dependency 'kueue'
+kueue:
+  # -- Enable the Kueue subchart
+  enabled: true
+  managerConfig:
+    # -- Overrides for the controller manager config
+    controllerManagerConfigYaml: |-
+      apiVersion: config.kueue.x-k8s.io/v1beta1
+      kind: Configuration
+      # EXCLUDE kueue-system from webhooks to prevent deadlock with the post-install hook
+      managedJobsNamespaceSelector:
```

  Yes, I think it would be better to make the populator read Kueue's config. Should I create a separate issue for that?
  
  I removed that particular part of config as it wasn't necessary.

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2025-11-24T13:38:10Z

- **cmd/experimental/kueue-populator/charts/kueue-populator/README.md:88** by [@j-skiba](https://github.com/j-skiba) — 2025-11-24T13:38:09Z
```diff
@@ -0,0 +1,139 @@
+# Kueue Prepopulator
+
+This Helm chart installs the Kueue Prepopulator, a component designed to automatically create default LocalQueue resources in namespaces, and sets up initial Kueue resources like a default ClusterQueue and ResourceFlavor. It includes the official Kueue chart as a dependency.
+
+## Purpose
+
+-   Deploys the `kueue-populator` controller manager.
+-   Installs Kueue (via subchart dependency).
+-   Creates a default `ResourceFlavor` named `tas-gpu-default`.
+-   Creates a default `ClusterQueue` (name configurable).
+-   The populator then creates a default `LocalQueue` (name configurable) in namespaces matching the selector, pointing to the default ClusterQueue.
+
+## Prerequisites
+
+-   [Helm](https://helm.sh/docs/intro/quickstart/#install-helm)
+-   Kubernetes cluster
+-   (Optional) [Cert-manager](https://cert-manager.io/docs/installation/)
+-   Docker or a compatible container builder.
+-   A container registry to push the image to.
+
+## Building the Image
+
+You need to build and push the image to your own registry.
+ 
+From the `cmd/experimental/kueue-populator` directory:
+ 
+```bash
+# Build the image
+make image-build IMAGE_REGISTRY=<YOUR_REGISTRY>
+ 
+# Push the image
+make image-push IMAGE_REGISTRY=<YOUR_REGISTRY>
+```
+ 
+This will build and push an image with the tag `<YOUR_REGISTRY>/kueue-populator:<GIT_TAG>`.
+ 
+If you want to use a specific tag, you can override `GIT_TAG`:
+ 
+```bash
+make image-build image-push IMAGE_REGISTRY=<YOUR_REGISTRY> GIT_TAG=latest
+```
+
+## Installation
+
+To install the chart, you MUST override the image repository and tag with the image you built and pushed.
+
+The following commands assume you are in the `cmd/experimental/kueue-populator` directory.
+
+Example using `--set`:
+
+```bash
+helm install kueue-populator ./charts/kueue-populator --namespace kueue-system --create-namespace \
+  --set kueuePopulator.image.repository=<YOUR_REGISTRY>/kueue-populator \
+  --set kueuePopulator.image.tag=latest
+```
+
+Example using a custom `my-values.yaml`:
+
+```yaml
+# my-values.yaml
+kueuePopulator:
+  image:
+    repository: <YOUR_REGISTRY>/kueue-populator
+    tag: latest
+```
+
+```bash
+helm install kueue-populator ./charts/kueue-populator --namespace kueue-system --create-namespace -f my-values.yaml
+```
+
+## Configuration
+
+### Kueue Prepopulator Configuration
+
+The following table lists the configurable parameters under the `kueuePopulator` key in `values.yaml`:
+
+| Key                                                | Type     | Default           | Description                                                                                                |
+| -------------------------------------------------- | -------- | ----------------- | ---------------------------------------------------------------------------------------------------------- |
+| `image.repository`                                 | string   | `null`            | **Required.** Image repository for the populator (e.g., `<YOUR_REGISTRY>/kueue-populator`)            |
+| `image.tag`                                        | string   | `null`            | **Required.** Image tag for the populator (e.g., `latest`)                                                |
+| `image.pullPolicy`                                 | string   | `IfNotPresent`    | Image pull policy                                                                                          |
+| `config.localQueueName`                            | string   | `default`         | Name of the default LocalQueue to create in namespaces                                                     |
+| `config.clusterQueueName`                          | string   | `cluster-queue`   | Name of the default ClusterQueue to create and reference in LocalQueues                                    |
+| `config.resources`                                 | list     | (see values.yaml) | Resources to configure in the default ResourceFlavor and ClusterQueue                                      |
+| `config.tasLevels`                                 | list     | `[]`              | Optional list of node labels for Topology Aware Scheduling levels. Enables Topology creation.            |
+| `config.nodeLabel`                                 | object   | `{}`              | Node labels to associate with the default ResourceFlavor.                                                  |
+| `config.managedJobsNamespaceSelector`              | object   | (see values.yaml) | Label selector to filter namespaces where the default LocalQueue will be created. Excludes system namespaces. |
+| `resources`                                        | object   | (see values.yaml) | CPU/Memory resource requests and limits for the populator deployment.                                   |
```

  removed

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2025-11-24T13:48:26Z

- **cmd/experimental/kueue-populator/charts/kueue-populator/README.md:84** by [@j-skiba](https://github.com/j-skiba) — 2025-11-24T13:48:26Z
```diff
@@ -0,0 +1,139 @@
+# Kueue Prepopulator
+
+This Helm chart installs the Kueue Prepopulator, a component designed to automatically create default LocalQueue resources in namespaces, and sets up initial Kueue resources like a default ClusterQueue and ResourceFlavor. It includes the official Kueue chart as a dependency.
+
+## Purpose
+
+-   Deploys the `kueue-populator` controller manager.
+-   Installs Kueue (via subchart dependency).
+-   Creates a default `ResourceFlavor` named `tas-gpu-default`.
+-   Creates a default `ClusterQueue` (name configurable).
+-   The populator then creates a default `LocalQueue` (name configurable) in namespaces matching the selector, pointing to the default ClusterQueue.
+
+## Prerequisites
+
+-   [Helm](https://helm.sh/docs/intro/quickstart/#install-helm)
+-   Kubernetes cluster
+-   (Optional) [Cert-manager](https://cert-manager.io/docs/installation/)
+-   Docker or a compatible container builder.
+-   A container registry to push the image to.
+
+## Building the Image
+
+You need to build and push the image to your own registry.
+ 
+From the `cmd/experimental/kueue-populator` directory:
+ 
+```bash
+# Build the image
+make image-build IMAGE_REGISTRY=<YOUR_REGISTRY>
+ 
+# Push the image
+make image-push IMAGE_REGISTRY=<YOUR_REGISTRY>
+```
+ 
+This will build and push an image with the tag `<YOUR_REGISTRY>/kueue-populator:<GIT_TAG>`.
+ 
+If you want to use a specific tag, you can override `GIT_TAG`:
+ 
+```bash
+make image-build image-push IMAGE_REGISTRY=<YOUR_REGISTRY> GIT_TAG=latest
+```
+
+## Installation
+
+To install the chart, you MUST override the image repository and tag with the image you built and pushed.
+
+The following commands assume you are in the `cmd/experimental/kueue-populator` directory.
+
+Example using `--set`:
+
+```bash
+helm install kueue-populator ./charts/kueue-populator --namespace kueue-system --create-namespace \
+  --set kueuePopulator.image.repository=<YOUR_REGISTRY>/kueue-populator \
+  --set kueuePopulator.image.tag=latest
+```
+
+Example using a custom `my-values.yaml`:
+
+```yaml
+# my-values.yaml
+kueuePopulator:
+  image:
+    repository: <YOUR_REGISTRY>/kueue-populator
+    tag: latest
+```
+
+```bash
+helm install kueue-populator ./charts/kueue-populator --namespace kueue-system --create-namespace -f my-values.yaml
+```
+
+## Configuration
+
+### Kueue Prepopulator Configuration
+
+The following table lists the configurable parameters under the `kueuePopulator` key in `values.yaml`:
+
+| Key                                                | Type     | Default           | Description                                                                                                |
+| -------------------------------------------------- | -------- | ----------------- | ---------------------------------------------------------------------------------------------------------- |
+| `image.repository`                                 | string   | `null`            | **Required.** Image repository for the populator (e.g., `<YOUR_REGISTRY>/kueue-populator`)            |
+| `image.tag`                                        | string   | `null`            | **Required.** Image tag for the populator (e.g., `latest`)                                                |
+| `image.pullPolicy`                                 | string   | `IfNotPresent`    | Image pull policy                                                                                          |
+| `config.localQueueName`                            | string   | `default`         | Name of the default LocalQueue to create in namespaces                                                     |
+| `config.clusterQueueName`                          | string   | `cluster-queue`   | Name of the default ClusterQueue to create and reference in LocalQueues                                    |
+| `config.resources`                                 | list     | (see values.yaml) | Resources to configure in the default ResourceFlavor and ClusterQueue                                      |
```

  I separated the settings to `config.clusterQueue`, `config.topology`, `config.resourceFlavor` and `config.localQueue`.

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2025-11-24T13:48:35Z

- **cmd/experimental/kueue-populator/charts/kueue-populator/README.md:86** by [@j-skiba](https://github.com/j-skiba) — 2025-11-24T13:48:35Z
```diff
@@ -0,0 +1,139 @@
+# Kueue Prepopulator
+
+This Helm chart installs the Kueue Prepopulator, a component designed to automatically create default LocalQueue resources in namespaces, and sets up initial Kueue resources like a default ClusterQueue and ResourceFlavor. It includes the official Kueue chart as a dependency.
+
+## Purpose
+
+-   Deploys the `kueue-populator` controller manager.
+-   Installs Kueue (via subchart dependency).
+-   Creates a default `ResourceFlavor` named `tas-gpu-default`.
+-   Creates a default `ClusterQueue` (name configurable).
+-   The populator then creates a default `LocalQueue` (name configurable) in namespaces matching the selector, pointing to the default ClusterQueue.
+
+## Prerequisites
+
+-   [Helm](https://helm.sh/docs/intro/quickstart/#install-helm)
+-   Kubernetes cluster
+-   (Optional) [Cert-manager](https://cert-manager.io/docs/installation/)
+-   Docker or a compatible container builder.
+-   A container registry to push the image to.
+
+## Building the Image
+
+You need to build and push the image to your own registry.
+ 
+From the `cmd/experimental/kueue-populator` directory:
+ 
+```bash
+# Build the image
+make image-build IMAGE_REGISTRY=<YOUR_REGISTRY>
+ 
+# Push the image
+make image-push IMAGE_REGISTRY=<YOUR_REGISTRY>
+```
+ 
+This will build and push an image with the tag `<YOUR_REGISTRY>/kueue-populator:<GIT_TAG>`.
+ 
+If you want to use a specific tag, you can override `GIT_TAG`:
+ 
+```bash
+make image-build image-push IMAGE_REGISTRY=<YOUR_REGISTRY> GIT_TAG=latest
+```
+
+## Installation
+
+To install the chart, you MUST override the image repository and tag with the image you built and pushed.
+
+The following commands assume you are in the `cmd/experimental/kueue-populator` directory.
+
+Example using `--set`:
+
+```bash
+helm install kueue-populator ./charts/kueue-populator --namespace kueue-system --create-namespace \
+  --set kueuePopulator.image.repository=<YOUR_REGISTRY>/kueue-populator \
+  --set kueuePopulator.image.tag=latest
+```
+
+Example using a custom `my-values.yaml`:
+
+```yaml
+# my-values.yaml
+kueuePopulator:
+  image:
+    repository: <YOUR_REGISTRY>/kueue-populator
+    tag: latest
+```
+
+```bash
+helm install kueue-populator ./charts/kueue-populator --namespace kueue-system --create-namespace -f my-values.yaml
+```
+
+## Configuration
+
+### Kueue Prepopulator Configuration
+
+The following table lists the configurable parameters under the `kueuePopulator` key in `values.yaml`:
+
+| Key                                                | Type     | Default           | Description                                                                                                |
+| -------------------------------------------------- | -------- | ----------------- | ---------------------------------------------------------------------------------------------------------- |
+| `image.repository`                                 | string   | `null`            | **Required.** Image repository for the populator (e.g., `<YOUR_REGISTRY>/kueue-populator`)            |
+| `image.tag`                                        | string   | `null`            | **Required.** Image tag for the populator (e.g., `latest`)                                                |
+| `image.pullPolicy`                                 | string   | `IfNotPresent`    | Image pull policy                                                                                          |
+| `config.localQueueName`                            | string   | `default`         | Name of the default LocalQueue to create in namespaces                                                     |
+| `config.clusterQueueName`                          | string   | `cluster-queue`   | Name of the default ClusterQueue to create and reference in LocalQueues                                    |
+| `config.resources`                                 | list     | (see values.yaml) | Resources to configure in the default ResourceFlavor and ClusterQueue                                      |
+| `config.tasLevels`                                 | list     | `[]`              | Optional list of node labels for Topology Aware Scheduling levels. Enables Topology creation.            |
+| `config.nodeLabel`                                 | object   | `{}`              | Node labels to associate with the default ResourceFlavor.                                                  |
```

  done

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2025-11-24T13:51:24Z

- **cmd/experimental/kueue-populator/charts/kueue-populator/README.md:1** by [@j-skiba](https://github.com/j-skiba) — 2025-11-24T13:51:24Z
```diff
@@ -0,0 +1,139 @@
+# Kueue Prepopulator
```

  all fixed

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2025-11-24T13:57:45Z

- **cmd/experimental/kueue-populator/charts/kueue-populator/Chart.yaml:9** by [@j-skiba](https://github.com/j-skiba) — 2025-11-24T13:57:45Z
```diff
@@ -0,0 +1,11 @@
+apiVersion: v2
+name: kueue-populator
+description: A Helm chart for Kueue Prepopulator setup including Kueue, LocalQueue Creator, and default resources.
+type: application
+version: 0.1.0
+appVersion: "0.1.0"
+dependencies:
+  - name: kueue
+    version: "0.14.4"
```

  I would stay with `version: "~0.14.4"` as this is early experimental version

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T14:08:22Z

- **cmd/experimental/kueue-populator/charts/kueue-populator/values.yaml:72** by [@mimowo](https://github.com/mimowo) — 2025-11-24T14:08:21Z
```diff
@@ -0,0 +1,81 @@
+# Configuration for the Kueue Prepopulator component
+kueuePopulator:
+  image:
+    # -- Image repository for the populator
+    # REQUIRED: Specify the repository where your kueue-populator image is pushed.
+    # Example: <YOUR_REGISTRY>/kueue-populator
+    repository: null
+    # -- Image tag for the populator
+    # REQUIRED: Specify the tag of the image you want to deploy.
+    # Example: latest
+    tag: null
+    # -- Image pull policy
+    pullPolicy: IfNotPresent
+  
+  # -- Image pull secrets for the populator
+  imagePullSecrets: []
+  
+  # Configuration for the controller
+  config:
+    # -- Name of the default LocalQueue to create in namespaces
+    localQueueName: "default"
+    # -- Name of the default ClusterQueue to create and reference in LocalQueues
+    clusterQueueName: "cluster-queue"
+    
+    # -- Resources to configure in the default ResourceFlavor and ClusterQueue
+    resources:
+      - name: "cpu"
+        nominalQuota: "10"
+      - name: "memory"
+        nominalQuota: "10Gi"
+      - name: "nvidia.com/gpu"
+        nominalQuota: "100"
+    
+    # -- List of topology levels. Enables Topology creation if not empty.
+    # Example:
+    # tasLevels:
+    #   - name: cloud.google.com/gke-nodepool
+    tasLevels: []
+    
+    # -- Node labels to associate with the default ResourceFlavor
+    # Example:
+    # nodeLabel:
+    #   cloud.google.com/gke-nodepool: "default-pool"
+    nodeLabel: {}
+
+    # -- Label selector to filter namespaces where the default LocalQueue will be created.
+    managedJobsNamespaceSelector:
+      matchExpressions:
+        - key: kubernetes.io/metadata.name
+          operator: NotIn
+          values: [ "kube-system", "kueue-system" ]
+
+  # -- Resource requests and limits for the populator deployment
+  resources:
+    limits:
+      cpu: 100m
+      memory: 100Mi
+    requests:
+      cpu: 100m
+      memory: 100Mi
+
+# Default values for the dependency 'kueue'
+kueue:
+  # -- Enable the Kueue subchart
+  enabled: true
+  managerConfig:
+    # -- Overrides for the controller manager config
+    controllerManagerConfigYaml: |-
+      apiVersion: config.kueue.x-k8s.io/v1beta1
+      kind: Configuration
+      # EXCLUDE kueue-system from webhooks to prevent deadlock with the post-install hook
+      managedJobsNamespaceSelector:
```

  I can still see that here: https://github.com/kubernetes-sigs/kueue/pull/7814/files#diff-b7b767bb9a58608af105d38a912df1f01a420089f186dd193335ad54dfbfe2b8R89
  
  However, it can be a follow up PR. Repeating the conifuration in this project is also not too bad. I don't have a strong view.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T14:08:31Z

- **cmd/experimental/kueue-populator/charts/kueue-populator/README.md:84** by [@mimowo](https://github.com/mimowo) — 2025-11-24T14:08:31Z
```diff
@@ -0,0 +1,139 @@
+# Kueue Prepopulator
+
+This Helm chart installs the Kueue Prepopulator, a component designed to automatically create default LocalQueue resources in namespaces, and sets up initial Kueue resources like a default ClusterQueue and ResourceFlavor. It includes the official Kueue chart as a dependency.
+
+## Purpose
+
+-   Deploys the `kueue-populator` controller manager.
+-   Installs Kueue (via subchart dependency).
+-   Creates a default `ResourceFlavor` named `tas-gpu-default`.
+-   Creates a default `ClusterQueue` (name configurable).
+-   The populator then creates a default `LocalQueue` (name configurable) in namespaces matching the selector, pointing to the default ClusterQueue.
+
+## Prerequisites
+
+-   [Helm](https://helm.sh/docs/intro/quickstart/#install-helm)
+-   Kubernetes cluster
+-   (Optional) [Cert-manager](https://cert-manager.io/docs/installation/)
+-   Docker or a compatible container builder.
+-   A container registry to push the image to.
+
+## Building the Image
+
+You need to build and push the image to your own registry.
+ 
+From the `cmd/experimental/kueue-populator` directory:
+ 
+```bash
+# Build the image
+make image-build IMAGE_REGISTRY=<YOUR_REGISTRY>
+ 
+# Push the image
+make image-push IMAGE_REGISTRY=<YOUR_REGISTRY>
+```
+ 
+This will build and push an image with the tag `<YOUR_REGISTRY>/kueue-populator:<GIT_TAG>`.
+ 
+If you want to use a specific tag, you can override `GIT_TAG`:
+ 
+```bash
+make image-build image-push IMAGE_REGISTRY=<YOUR_REGISTRY> GIT_TAG=latest
+```
+
+## Installation
+
+To install the chart, you MUST override the image repository and tag with the image you built and pushed.
+
+The following commands assume you are in the `cmd/experimental/kueue-populator` directory.
+
+Example using `--set`:
+
+```bash
+helm install kueue-populator ./charts/kueue-populator --namespace kueue-system --create-namespace \
+  --set kueuePopulator.image.repository=<YOUR_REGISTRY>/kueue-populator \
+  --set kueuePopulator.image.tag=latest
+```
+
+Example using a custom `my-values.yaml`:
+
+```yaml
+# my-values.yaml
+kueuePopulator:
+  image:
+    repository: <YOUR_REGISTRY>/kueue-populator
+    tag: latest
+```
+
+```bash
+helm install kueue-populator ./charts/kueue-populator --namespace kueue-system --create-namespace -f my-values.yaml
+```
+
+## Configuration
+
+### Kueue Prepopulator Configuration
+
+The following table lists the configurable parameters under the `kueuePopulator` key in `values.yaml`:
+
+| Key                                                | Type     | Default           | Description                                                                                                |
+| -------------------------------------------------- | -------- | ----------------- | ---------------------------------------------------------------------------------------------------------- |
+| `image.repository`                                 | string   | `null`            | **Required.** Image repository for the populator (e.g., `<YOUR_REGISTRY>/kueue-populator`)            |
+| `image.tag`                                        | string   | `null`            | **Required.** Image tag for the populator (e.g., `latest`)                                                |
+| `image.pullPolicy`                                 | string   | `IfNotPresent`    | Image pull policy                                                                                          |
+| `config.localQueueName`                            | string   | `default`         | Name of the default LocalQueue to create in namespaces                                                     |
+| `config.clusterQueueName`                          | string   | `cluster-queue`   | Name of the default ClusterQueue to create and reference in LocalQueues                                    |
+| `config.resources`                                 | list     | (see values.yaml) | Resources to configure in the default ResourceFlavor and ClusterQueue                                      |
```

  sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T14:09:44Z

- **cmd/experimental/kueue-populator/charts/kueue-populator/values.yaml:72** by [@mimowo](https://github.com/mimowo) — 2025-11-24T14:09:44Z
```diff
@@ -0,0 +1,81 @@
+# Configuration for the Kueue Prepopulator component
+kueuePopulator:
+  image:
+    # -- Image repository for the populator
+    # REQUIRED: Specify the repository where your kueue-populator image is pushed.
+    # Example: <YOUR_REGISTRY>/kueue-populator
+    repository: null
+    # -- Image tag for the populator
+    # REQUIRED: Specify the tag of the image you want to deploy.
+    # Example: latest
+    tag: null
+    # -- Image pull policy
+    pullPolicy: IfNotPresent
+  
+  # -- Image pull secrets for the populator
+  imagePullSecrets: []
+  
+  # Configuration for the controller
+  config:
+    # -- Name of the default LocalQueue to create in namespaces
+    localQueueName: "default"
+    # -- Name of the default ClusterQueue to create and reference in LocalQueues
+    clusterQueueName: "cluster-queue"
+    
+    # -- Resources to configure in the default ResourceFlavor and ClusterQueue
+    resources:
+      - name: "cpu"
+        nominalQuota: "10"
+      - name: "memory"
+        nominalQuota: "10Gi"
+      - name: "nvidia.com/gpu"
+        nominalQuota: "100"
+    
+    # -- List of topology levels. Enables Topology creation if not empty.
+    # Example:
+    # tasLevels:
+    #   - name: cloud.google.com/gke-nodepool
+    tasLevels: []
+    
+    # -- Node labels to associate with the default ResourceFlavor
+    # Example:
+    # nodeLabel:
+    #   cloud.google.com/gke-nodepool: "default-pool"
+    nodeLabel: {}
+
+    # -- Label selector to filter namespaces where the default LocalQueue will be created.
+    managedJobsNamespaceSelector:
+      matchExpressions:
+        - key: kubernetes.io/metadata.name
+          operator: NotIn
+          values: [ "kube-system", "kueue-system" ]
+
+  # -- Resource requests and limits for the populator deployment
+  resources:
+    limits:
+      cpu: 100m
+      memory: 100Mi
+    requests:
+      cpu: 100m
+      memory: 100Mi
+
+# Default values for the dependency 'kueue'
+kueue:
+  # -- Enable the Kueue subchart
+  enabled: true
+  managerConfig:
+    # -- Overrides for the controller manager config
+    controllerManagerConfigYaml: |-
+      apiVersion: config.kueue.x-k8s.io/v1beta1
+      kind: Configuration
+      # EXCLUDE kueue-system from webhooks to prevent deadlock with the post-install hook
+      managedJobsNamespaceSelector:
```

  Ok, let's open a follow up issue to reconsider that, something like "kueue-populator: re-use Kueue configuration to restrict the namespaces"

### Review by [@kshalot](https://github.com/kshalot) (approved) — 2025-11-24T16:39:50Z
