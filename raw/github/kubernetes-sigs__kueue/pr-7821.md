# PR #7821: Add performance tests for v1beta2 TopologyAssignment encoding

**Summary**: Add performance tests for v1beta2 TopologyAssignment encoding

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7821

**Last updated**: 2025-11-27T13:26:43Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@olekzabl](https://github.com/olekzabl)
- **Branch**: `br24` → `main`
- **Created**: 2025-11-24T00:16:44Z
- **Updated**: 2025-11-27T13:26:43Z
- **Merged**: 2025-11-26T17:30:45Z
- **Closed**: 2025-11-26T17:30:45Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@gabesaba](https://github.com/gabesaba), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 1   **+282 / -0**

## Description

#### What type of PR is this?

/kind cleanup

#### What this PR does / why we need it:

As described in #7640, it adds 2 kinds of unit tests verifying the behavior of the v1beta2 TopologyAssignment encoding:

- a check that, for the most popular naming schemes, we can fit sufficiently many nodes;
- a benchmark of the latency spent on generating the "packed" v1beta2 representation.

This serves 2 purposes: 

- it improves test coverage for PR #7544 ;
- it lays ground for assessment of any alternative encoding strategies in the future.

#### Which issue(s) this PR fixes:

Fixes #7640

#### Special notes for your reviewer:

The specific performance results depend significantly on the "node naming scheme", which is heavily vendor-dependent. OTOH, I wanted to keep my tests possibly simple, without diving into implementation details of numerous vendors.

As a middle ground, I checked the most typical naming schemes used by ~10 top vendors, and defined 3 `namingScheme`s, intended to act as "upper bounds" for all those real-life schemes (in terms of the length of each functional part of the node name).
These schemes are parametrized by numeric parameters, which are explicitly set inside individual test cases, to make those test cases more self-describing.

Still, to get some concrete numbers, I needed to take additional assumptions, e.g.
- If "node pools" are a concept, then:
  - there's no more than 100 of them
  - if their names contribute to node names, than these names aren't longer than 8 characters
- If "regions" contribute to node names, then there's no more than 100 of them
- Cluster name isn't longer than 16 characters.

These assumptions _may not hold_ in some rare cases - but I simply had to assume something on this.

---

Under these assumptions, the worst test case shows a limit of **46.8k nodes** fitting within a single etcd entry. That's worse than my initially forecasted 60k, apologies for that. Note though, tiny details matter: for example, in the "pool-and-node-based" scheme, once we assume that node pools are named `pool-<number>` (as I did in my early experiments), the pool-based part gets effectively shortened by 5 characters (as `pool-` becomes part of a single common prefix), and that alone increases the limit from 46.8k to 55.5k. So, user naming patterns matter, and (as long as we stick to the "single prefix & suffix" encoding strategy) there's nothing we can do about that.

#### Does this PR introduce a user-facing change?

```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-11-24T00:16:49Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 6b32cf9ac768ac6b88ba76c751ea78d143dda576 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/692620dd3e792900086e336b |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-7821--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-7821--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTc4MjEtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.c2g2KWzGNNbT5eLOfYNngUkEntH2L6_DWTSXWLJT6f0)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-7821--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@olekzabl](https://github.com/olekzabl) — 2025-11-24T10:06:37Z

Converting this to draft, as I see a "remote" unit test failure which I'll need to debug.

(At a first flance, strange thing; the test works for me locally, and I tried to make everything deterministic)

### Comment by [@olekzabl](https://github.com/olekzabl) — 2025-11-24T10:22:37Z

/retest

### Comment by [@olekzabl](https://github.com/olekzabl) — 2025-11-24T10:48:49Z

/retest

### Comment by [@olekzabl](https://github.com/olekzabl) — 2025-11-24T12:27:59Z

Wow.
Initially I assumed the timeout was caused by a combination of a logic bug (causing an infinite loop for some random cases) + some non-determinism (triggering the former on Prow but not on my machine).

However, looking at detailed logs, this was *not* the case: everything seemed to progress well, except for that it was **~50x** slower on Prow than on my machine, and then adding up whole `pkg/util/tas` on Prow has "organically" exceeded the timeout of 10m.

I now made 2 hacks to speed these tests up; locally it accelerated by ~5x. Now I need to see how it goes on Prow.

/retest

### Comment by [@olekzabl](https://github.com/olekzabl) — 2025-11-24T12:46:19Z

Now it's 1.56s on my machine vs. 3m53s on Prow... which means **149x** difference.
But at least well below the 10m timeout.

_EDIT: OK, I'm discovering some important factors behind this "150x divergence of test latency":_

- _on my machine, I initially mesured "run test" in VSCode, now I see "make test" is ~20x slower_
  _(post-edit: and the game-changer here is the `-race` flag, enabled by default in `make test`)_
- _also Prow has `-count 3` which `make test` doesn't do; sounds like explaining another 3x slowdown_

Un-drafting.

### Comment by [@olekzabl](https://github.com/olekzabl) — 2025-11-24T13:08:55Z

This time, `Failed to install helm-unittest` - this doesn't even seem a test flake, rather a flake of the test infrastructure.

/retest

(And, in the meantime, in a separate draft PR #7847, I'm experimenting with further optimizations)

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-26T16:42:16Z

LGTM label has been added.  <details>Git tree hash: 5ca2fb83b3781315d896f9714cdc68bcb5ce7d35</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-26T16:42:18Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7821#pullrequestreview-3511821681" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7821#" title="Author self-approved">olekzabl</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-25T15:26:07Z

- **pkg/util/tas/tas_assignment_performance_test.go:216** by [@PBundyra](https://github.com/PBundyra) — 2025-11-25T15:26:08Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"encoding/hex"
+	"encoding/json"
+	"fmt"
+	"math/rand/v2"
+	"sort"
+	"strings"
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+const (
+	targetNodeCount = 40_000
+)
+
+// Generate n hex numbers of given length,
+// ensuring they're distinct (and otherwise quasi-random).
+// For the health of tests, this function behaves deterministically.
+func randomHexIDs(n, length int) []string {
+	chosen := map[string]bool{}
+	res := make([]string, n)
+	rnd := rand.NewChaCha8([32]byte{})
+	bytes := make([]byte, (length+1)/2)
+	for i := range n {
+		for {
+			var _, _ = rnd.Read(bytes)
+			id := hex.EncodeToString(bytes)[:length]
+			if !chosen[id] {
+				chosen[id] = true
+				res[i] = id
+				break
+			}
+		}
+	}
+	return res
+}
+
+func fixedID(length int) string {
+	// This is really whatever.
+	return strings.Repeat("a", length)
+}
+
+func consecutiveIPs(n int) []string {
+	res := make([]string, n)
+	for i := range n {
+		// Picking 100.10x.xxx.xxx (instead of traditional 10.x.xxx.xxx)
+		// to make the test scenario a bit more adverse.
+		res[i] = fmt.Sprintf("100.%d.%d.%d",
+			100+i/(1<<16),
+			i%(1<<16)/(1<<8),
+			i%(1<<8),
+		)
+	}
+	return res
+}
+
+// namingScheme generates a list of node names according to a specific recipe.
+//
+// The list should be "well-prefixing", in that namingScheme(n)[:m] should be
+// an "equally good representative" of the scheme as namingScheme(m).
+// (This convention helps test performance; it's used in "approxMaxNodesFor").
+// To achieve that, specific schemes should rotate node "properties"
+// (like node pool, region etc.) using "%" operator rather than in big fixed chunks.
+type namingScheme func(nodes int) []string
+
+type poolAndNodeBasedNamingConfig struct {
+	fixedPrefixAndSuffixLength int
+	pools                      int
+	nodeIDLength               int
+	poolIDLength               int
+}
+
+func poolAndNodeBasedNaming(config poolAndNodeBasedNamingConfig) namingScheme {
+	return func(nodes int) []string {
+		res := make([]string, nodes)
+		nodeIDs := randomHexIDs(nodes, config.nodeIDLength)
+		poolIDs := randomHexIDs(config.pools, config.poolIDLength)
+		for i := range nodes {
+			res[i] = fmt.Sprintf("%s-%s-%s-%s",
+				fixedID(config.fixedPrefixAndSuffixLength),
+				poolIDs[i%config.pools],
+				nodeIDs[i],
+				fixedID(config.fixedPrefixAndSuffixLength),
+			)
+		}
+		return res
+	}
+}
+
+type regionAndIPBasedNamingConfig struct {
+	fixedPrefixAndSuffixLength int
+	regions                    int
+	regionIDLength             int
+}
+
+func regionAndIPBasedNaming(config regionAndIPBasedNamingConfig) namingScheme {
+	return func(nodes int) []string {
+		res := make([]string, nodes)
+		nodeIPs := consecutiveIPs(nodes)
+		regionIDs := randomHexIDs(config.regions, config.regionIDLength)
+		for i := range nodes {
+			res[i] = fmt.Sprintf("%s-%s-%s-%s",
+				fixedID(config.fixedPrefixAndSuffixLength),
+				regionIDs[i%config.regions],
+				nodeIPs[i],
+				fixedID(config.fixedPrefixAndSuffixLength),
+			)
+		}
+		return res
+	}
+}
+
+type nodeBasedNamingConfig struct {
+	fixedPrefixAndSuffixLength int
+	nodeIDLength               int
+}
+
+func nodeBasedNaming(config nodeBasedNamingConfig) namingScheme {
+	return func(nodes int) []string {
+		res := make([]string, nodes)
+		nodeIDs := randomHexIDs(nodes, config.nodeIDLength)
+		for i := range nodes {
+			res[i] = fmt.Sprintf("%s-%s-%s",
+				fixedID(config.fixedPrefixAndSuffixLength),
+				nodeIDs[i],
+				fixedID(config.fixedPrefixAndSuffixLength),
+			)
+		}
+		return res
+	}
+}
+
+func internalSinglePodsOn(nodes []string) *TopologyAssignment {
+	res := &TopologyAssignment{
+		Levels:  []string{corev1.LabelHostname},
+		Domains: make([]TopologyDomainAssignment, len(nodes)),
+	}
+	for i, n := range nodes {
+		res.Domains[i] = TopologyDomainAssignment{
+			Values: []string{n},
+			Count:  1,
+		}
+	}
+	return res
+}
+
+func jsonStr(v any) string {
+	bytes, err := json.Marshal(v)
+	if err != nil {
+		panic(err)
+	}
+	return string(bytes)
+}
+
+func isTooLarge(ta *kueue.TopologyAssignment) bool {
+	return len(jsonStr(ta)) > 1_500_000
+}
+
+func approxMaxNodesFor(naming namingScheme) int {
+	step := 1_000 // We search with a reduced resolution, to speed up the test
+	ceiling := 300_000
+	nodeNames := naming(ceiling)
+	found := sort.Search(ceiling/step, func(n int) bool {
+		// Here we rely on "well-prefixing"; see the comment on "nodeNaming".
+		return isTooLarge(V1Beta2From(internalSinglePodsOn(nodeNames[:n*step])))
+	}) - 1
+	return found * step
+}
+
+type performanceTestCase struct {
+	name   string
+	naming namingScheme
+}
+
+var performanceTestCases = []performanceTestCase{
+	{
+		name: "pool-and-node-based naming (100 node pools)",
+		naming: poolAndNodeBasedNaming(poolAndNodeBasedNamingConfig{
+			pools:        100, // happens in practice, at least in GKE
+			nodeIDLength: 6,   // reached in AKS
+
+			// reachable in AKS (<pool-name>-<8-char-id>-vmss, then let <pool-name> have 8 chars)
+			poolIDLength: 22,
+
+			fixedPrefixAndSuffixLength: 20,
+		}),
+	},
+	{
+		name: "pool-and-node-based naming (1 node pool)",
+		naming: poolAndNodeBasedNaming(poolAndNodeBasedNamingConfig{
+			pools:                      1,
+			nodeIDLength:               6,
+			poolIDLength:               22,
+			fixedPrefixAndSuffixLength: 20,
+		}),
```

  Do we know about any vendor that could fit thousands of into a single nodepool (or equivalent concept)

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-11-25T15:30:00Z

- **pkg/util/tas/tas_assignment_performance_test.go:206** by [@PBundyra](https://github.com/PBundyra) — 2025-11-25T15:30:00Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"encoding/hex"
+	"encoding/json"
+	"fmt"
+	"math/rand/v2"
+	"sort"
+	"strings"
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+const (
+	targetNodeCount = 40_000
+)
+
+// Generate n hex numbers of given length,
+// ensuring they're distinct (and otherwise quasi-random).
+// For the health of tests, this function behaves deterministically.
+func randomHexIDs(n, length int) []string {
+	chosen := map[string]bool{}
+	res := make([]string, n)
+	rnd := rand.NewChaCha8([32]byte{})
+	bytes := make([]byte, (length+1)/2)
+	for i := range n {
+		for {
+			var _, _ = rnd.Read(bytes)
+			id := hex.EncodeToString(bytes)[:length]
+			if !chosen[id] {
+				chosen[id] = true
+				res[i] = id
+				break
+			}
+		}
+	}
+	return res
+}
+
+func fixedID(length int) string {
+	// This is really whatever.
+	return strings.Repeat("a", length)
+}
+
+func consecutiveIPs(n int) []string {
+	res := make([]string, n)
+	for i := range n {
+		// Picking 100.10x.xxx.xxx (instead of traditional 10.x.xxx.xxx)
+		// to make the test scenario a bit more adverse.
+		res[i] = fmt.Sprintf("100.%d.%d.%d",
+			100+i/(1<<16),
+			i%(1<<16)/(1<<8),
+			i%(1<<8),
+		)
+	}
+	return res
+}
+
+// namingScheme generates a list of node names according to a specific recipe.
+//
+// The list should be "well-prefixing", in that namingScheme(n)[:m] should be
+// an "equally good representative" of the scheme as namingScheme(m).
+// (This convention helps test performance; it's used in "approxMaxNodesFor").
+// To achieve that, specific schemes should rotate node "properties"
+// (like node pool, region etc.) using "%" operator rather than in big fixed chunks.
+type namingScheme func(nodes int) []string
+
+type poolAndNodeBasedNamingConfig struct {
+	fixedPrefixAndSuffixLength int
+	pools                      int
+	nodeIDLength               int
+	poolIDLength               int
+}
+
+func poolAndNodeBasedNaming(config poolAndNodeBasedNamingConfig) namingScheme {
+	return func(nodes int) []string {
+		res := make([]string, nodes)
+		nodeIDs := randomHexIDs(nodes, config.nodeIDLength)
+		poolIDs := randomHexIDs(config.pools, config.poolIDLength)
+		for i := range nodes {
+			res[i] = fmt.Sprintf("%s-%s-%s-%s",
+				fixedID(config.fixedPrefixAndSuffixLength),
+				poolIDs[i%config.pools],
+				nodeIDs[i],
+				fixedID(config.fixedPrefixAndSuffixLength),
+			)
+		}
+		return res
+	}
+}
+
+type regionAndIPBasedNamingConfig struct {
+	fixedPrefixAndSuffixLength int
+	regions                    int
+	regionIDLength             int
+}
+
+func regionAndIPBasedNaming(config regionAndIPBasedNamingConfig) namingScheme {
+	return func(nodes int) []string {
+		res := make([]string, nodes)
+		nodeIPs := consecutiveIPs(nodes)
+		regionIDs := randomHexIDs(config.regions, config.regionIDLength)
+		for i := range nodes {
+			res[i] = fmt.Sprintf("%s-%s-%s-%s",
+				fixedID(config.fixedPrefixAndSuffixLength),
+				regionIDs[i%config.regions],
+				nodeIPs[i],
+				fixedID(config.fixedPrefixAndSuffixLength),
+			)
+		}
+		return res
+	}
+}
+
+type nodeBasedNamingConfig struct {
+	fixedPrefixAndSuffixLength int
+	nodeIDLength               int
+}
+
+func nodeBasedNaming(config nodeBasedNamingConfig) namingScheme {
+	return func(nodes int) []string {
+		res := make([]string, nodes)
+		nodeIDs := randomHexIDs(nodes, config.nodeIDLength)
+		for i := range nodes {
+			res[i] = fmt.Sprintf("%s-%s-%s",
+				fixedID(config.fixedPrefixAndSuffixLength),
+				nodeIDs[i],
+				fixedID(config.fixedPrefixAndSuffixLength),
+			)
+		}
+		return res
+	}
+}
+
+func internalSinglePodsOn(nodes []string) *TopologyAssignment {
+	res := &TopologyAssignment{
+		Levels:  []string{corev1.LabelHostname},
+		Domains: make([]TopologyDomainAssignment, len(nodes)),
+	}
+	for i, n := range nodes {
+		res.Domains[i] = TopologyDomainAssignment{
+			Values: []string{n},
+			Count:  1,
+		}
+	}
+	return res
+}
+
+func jsonStr(v any) string {
+	bytes, err := json.Marshal(v)
+	if err != nil {
+		panic(err)
+	}
+	return string(bytes)
+}
+
+func isTooLarge(ta *kueue.TopologyAssignment) bool {
+	return len(jsonStr(ta)) > 1_500_000
+}
+
+func approxMaxNodesFor(naming namingScheme) int {
+	step := 1_000 // We search with a reduced resolution, to speed up the test
+	ceiling := 300_000
+	nodeNames := naming(ceiling)
+	found := sort.Search(ceiling/step, func(n int) bool {
+		// Here we rely on "well-prefixing"; see the comment on "nodeNaming".
+		return isTooLarge(V1Beta2From(internalSinglePodsOn(nodeNames[:n*step])))
+	}) - 1
+	return found * step
+}
+
+type performanceTestCase struct {
+	name   string
+	naming namingScheme
+}
+
+var performanceTestCases = []performanceTestCase{
+	{
+		name: "pool-and-node-based naming (100 node pools)",
+		naming: poolAndNodeBasedNaming(poolAndNodeBasedNamingConfig{
+			pools:        100, // happens in practice, at least in GKE
+			nodeIDLength: 6,   // reached in AKS
+
+			// reachable in AKS (<pool-name>-<8-char-id>-vmss, then let <pool-name> have 8 chars)
+			poolIDLength: 22,
+
+			fixedPrefixAndSuffixLength: 20,
```

  I think 20 is a bit too much, it this is added at the beginning and at the end of the name it means the name exceeds 63 characters which is impossible. Alternatively I would put it only as a prefix and omit suffix

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-11-25T21:33:48Z

- **pkg/util/tas/tas_assignment_performance_test.go:216** by [@olekzabl](https://github.com/olekzabl) — 2025-11-25T21:33:48Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"encoding/hex"
+	"encoding/json"
+	"fmt"
+	"math/rand/v2"
+	"sort"
+	"strings"
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+const (
+	targetNodeCount = 40_000
+)
+
+// Generate n hex numbers of given length,
+// ensuring they're distinct (and otherwise quasi-random).
+// For the health of tests, this function behaves deterministically.
+func randomHexIDs(n, length int) []string {
+	chosen := map[string]bool{}
+	res := make([]string, n)
+	rnd := rand.NewChaCha8([32]byte{})
+	bytes := make([]byte, (length+1)/2)
+	for i := range n {
+		for {
+			var _, _ = rnd.Read(bytes)
+			id := hex.EncodeToString(bytes)[:length]
+			if !chosen[id] {
+				chosen[id] = true
+				res[i] = id
+				break
+			}
+		}
+	}
+	return res
+}
+
+func fixedID(length int) string {
+	// This is really whatever.
+	return strings.Repeat("a", length)
+}
+
+func consecutiveIPs(n int) []string {
+	res := make([]string, n)
+	for i := range n {
+		// Picking 100.10x.xxx.xxx (instead of traditional 10.x.xxx.xxx)
+		// to make the test scenario a bit more adverse.
+		res[i] = fmt.Sprintf("100.%d.%d.%d",
+			100+i/(1<<16),
+			i%(1<<16)/(1<<8),
+			i%(1<<8),
+		)
+	}
+	return res
+}
+
+// namingScheme generates a list of node names according to a specific recipe.
+//
+// The list should be "well-prefixing", in that namingScheme(n)[:m] should be
+// an "equally good representative" of the scheme as namingScheme(m).
+// (This convention helps test performance; it's used in "approxMaxNodesFor").
+// To achieve that, specific schemes should rotate node "properties"
+// (like node pool, region etc.) using "%" operator rather than in big fixed chunks.
+type namingScheme func(nodes int) []string
+
+type poolAndNodeBasedNamingConfig struct {
+	fixedPrefixAndSuffixLength int
+	pools                      int
+	nodeIDLength               int
+	poolIDLength               int
+}
+
+func poolAndNodeBasedNaming(config poolAndNodeBasedNamingConfig) namingScheme {
+	return func(nodes int) []string {
+		res := make([]string, nodes)
+		nodeIDs := randomHexIDs(nodes, config.nodeIDLength)
+		poolIDs := randomHexIDs(config.pools, config.poolIDLength)
+		for i := range nodes {
+			res[i] = fmt.Sprintf("%s-%s-%s-%s",
+				fixedID(config.fixedPrefixAndSuffixLength),
+				poolIDs[i%config.pools],
+				nodeIDs[i],
+				fixedID(config.fixedPrefixAndSuffixLength),
+			)
+		}
+		return res
+	}
+}
+
+type regionAndIPBasedNamingConfig struct {
+	fixedPrefixAndSuffixLength int
+	regions                    int
+	regionIDLength             int
+}
+
+func regionAndIPBasedNaming(config regionAndIPBasedNamingConfig) namingScheme {
+	return func(nodes int) []string {
+		res := make([]string, nodes)
+		nodeIPs := consecutiveIPs(nodes)
+		regionIDs := randomHexIDs(config.regions, config.regionIDLength)
+		for i := range nodes {
+			res[i] = fmt.Sprintf("%s-%s-%s-%s",
+				fixedID(config.fixedPrefixAndSuffixLength),
+				regionIDs[i%config.regions],
+				nodeIPs[i],
+				fixedID(config.fixedPrefixAndSuffixLength),
+			)
+		}
+		return res
+	}
+}
+
+type nodeBasedNamingConfig struct {
+	fixedPrefixAndSuffixLength int
+	nodeIDLength               int
+}
+
+func nodeBasedNaming(config nodeBasedNamingConfig) namingScheme {
+	return func(nodes int) []string {
+		res := make([]string, nodes)
+		nodeIDs := randomHexIDs(nodes, config.nodeIDLength)
+		for i := range nodes {
+			res[i] = fmt.Sprintf("%s-%s-%s",
+				fixedID(config.fixedPrefixAndSuffixLength),
+				nodeIDs[i],
+				fixedID(config.fixedPrefixAndSuffixLength),
+			)
+		}
+		return res
+	}
+}
+
+func internalSinglePodsOn(nodes []string) *TopologyAssignment {
+	res := &TopologyAssignment{
+		Levels:  []string{corev1.LabelHostname},
+		Domains: make([]TopologyDomainAssignment, len(nodes)),
+	}
+	for i, n := range nodes {
+		res.Domains[i] = TopologyDomainAssignment{
+			Values: []string{n},
+			Count:  1,
+		}
+	}
+	return res
+}
+
+func jsonStr(v any) string {
+	bytes, err := json.Marshal(v)
+	if err != nil {
+		panic(err)
+	}
+	return string(bytes)
+}
+
+func isTooLarge(ta *kueue.TopologyAssignment) bool {
+	return len(jsonStr(ta)) > 1_500_000
+}
+
+func approxMaxNodesFor(naming namingScheme) int {
+	step := 1_000 // We search with a reduced resolution, to speed up the test
+	ceiling := 300_000
+	nodeNames := naming(ceiling)
+	found := sort.Search(ceiling/step, func(n int) bool {
+		// Here we rely on "well-prefixing"; see the comment on "nodeNaming".
+		return isTooLarge(V1Beta2From(internalSinglePodsOn(nodeNames[:n*step])))
+	}) - 1
+	return found * step
+}
+
+type performanceTestCase struct {
+	name   string
+	naming namingScheme
+}
+
+var performanceTestCases = []performanceTestCase{
+	{
+		name: "pool-and-node-based naming (100 node pools)",
+		naming: poolAndNodeBasedNaming(poolAndNodeBasedNamingConfig{
+			pools:        100, // happens in practice, at least in GKE
+			nodeIDLength: 6,   // reached in AKS
+
+			// reachable in AKS (<pool-name>-<8-char-id>-vmss, then let <pool-name> have 8 chars)
+			poolIDLength: 22,
+
+			fixedPrefixAndSuffixLength: 20,
+		}),
+	},
+	{
+		name: "pool-and-node-based naming (1 node pool)",
+		naming: poolAndNodeBasedNaming(poolAndNodeBasedNamingConfig{
+			pools:                      1,
+			nodeIDLength:               6,
+			poolIDLength:               22,
+			fixedPrefixAndSuffixLength: 20,
+		}),
```

  In GKE, the maximum node count in a node pool seems to be 4000.
  I failed to find a vendor (with "pools" reflected in node names) surpassing that.
  
  So indeed, as long as we care about 40k+ nodes, it seems 10+ node pools is a must.
  To have more realistic scenarios, I bumped 1 node pool -> 10.
  And BTW I also bumped 100 NPs -> 1000, as this also sometimes exists in GKE.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-11-25T21:44:57Z

- **pkg/util/tas/tas_assignment_performance_test.go:206** by [@olekzabl](https://github.com/olekzabl) — 2025-11-25T21:44:57Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"encoding/hex"
+	"encoding/json"
+	"fmt"
+	"math/rand/v2"
+	"sort"
+	"strings"
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+const (
+	targetNodeCount = 40_000
+)
+
+// Generate n hex numbers of given length,
+// ensuring they're distinct (and otherwise quasi-random).
+// For the health of tests, this function behaves deterministically.
+func randomHexIDs(n, length int) []string {
+	chosen := map[string]bool{}
+	res := make([]string, n)
+	rnd := rand.NewChaCha8([32]byte{})
+	bytes := make([]byte, (length+1)/2)
+	for i := range n {
+		for {
+			var _, _ = rnd.Read(bytes)
+			id := hex.EncodeToString(bytes)[:length]
+			if !chosen[id] {
+				chosen[id] = true
+				res[i] = id
+				break
+			}
+		}
+	}
+	return res
+}
+
+func fixedID(length int) string {
+	// This is really whatever.
+	return strings.Repeat("a", length)
+}
+
+func consecutiveIPs(n int) []string {
+	res := make([]string, n)
+	for i := range n {
+		// Picking 100.10x.xxx.xxx (instead of traditional 10.x.xxx.xxx)
+		// to make the test scenario a bit more adverse.
+		res[i] = fmt.Sprintf("100.%d.%d.%d",
+			100+i/(1<<16),
+			i%(1<<16)/(1<<8),
+			i%(1<<8),
+		)
+	}
+	return res
+}
+
+// namingScheme generates a list of node names according to a specific recipe.
+//
+// The list should be "well-prefixing", in that namingScheme(n)[:m] should be
+// an "equally good representative" of the scheme as namingScheme(m).
+// (This convention helps test performance; it's used in "approxMaxNodesFor").
+// To achieve that, specific schemes should rotate node "properties"
+// (like node pool, region etc.) using "%" operator rather than in big fixed chunks.
+type namingScheme func(nodes int) []string
+
+type poolAndNodeBasedNamingConfig struct {
+	fixedPrefixAndSuffixLength int
+	pools                      int
+	nodeIDLength               int
+	poolIDLength               int
+}
+
+func poolAndNodeBasedNaming(config poolAndNodeBasedNamingConfig) namingScheme {
+	return func(nodes int) []string {
+		res := make([]string, nodes)
+		nodeIDs := randomHexIDs(nodes, config.nodeIDLength)
+		poolIDs := randomHexIDs(config.pools, config.poolIDLength)
+		for i := range nodes {
+			res[i] = fmt.Sprintf("%s-%s-%s-%s",
+				fixedID(config.fixedPrefixAndSuffixLength),
+				poolIDs[i%config.pools],
+				nodeIDs[i],
+				fixedID(config.fixedPrefixAndSuffixLength),
+			)
+		}
+		return res
+	}
+}
+
+type regionAndIPBasedNamingConfig struct {
+	fixedPrefixAndSuffixLength int
+	regions                    int
+	regionIDLength             int
+}
+
+func regionAndIPBasedNaming(config regionAndIPBasedNamingConfig) namingScheme {
+	return func(nodes int) []string {
+		res := make([]string, nodes)
+		nodeIPs := consecutiveIPs(nodes)
+		regionIDs := randomHexIDs(config.regions, config.regionIDLength)
+		for i := range nodes {
+			res[i] = fmt.Sprintf("%s-%s-%s-%s",
+				fixedID(config.fixedPrefixAndSuffixLength),
+				regionIDs[i%config.regions],
+				nodeIPs[i],
+				fixedID(config.fixedPrefixAndSuffixLength),
+			)
+		}
+		return res
+	}
+}
+
+type nodeBasedNamingConfig struct {
+	fixedPrefixAndSuffixLength int
+	nodeIDLength               int
+}
+
+func nodeBasedNaming(config nodeBasedNamingConfig) namingScheme {
+	return func(nodes int) []string {
+		res := make([]string, nodes)
+		nodeIDs := randomHexIDs(nodes, config.nodeIDLength)
+		for i := range nodes {
+			res[i] = fmt.Sprintf("%s-%s-%s",
+				fixedID(config.fixedPrefixAndSuffixLength),
+				nodeIDs[i],
+				fixedID(config.fixedPrefixAndSuffixLength),
+			)
+		}
+		return res
+	}
+}
+
+func internalSinglePodsOn(nodes []string) *TopologyAssignment {
+	res := &TopologyAssignment{
+		Levels:  []string{corev1.LabelHostname},
+		Domains: make([]TopologyDomainAssignment, len(nodes)),
+	}
+	for i, n := range nodes {
+		res.Domains[i] = TopologyDomainAssignment{
+			Values: []string{n},
+			Count:  1,
+		}
+	}
+	return res
+}
+
+func jsonStr(v any) string {
+	bytes, err := json.Marshal(v)
+	if err != nil {
+		panic(err)
+	}
+	return string(bytes)
+}
+
+func isTooLarge(ta *kueue.TopologyAssignment) bool {
+	return len(jsonStr(ta)) > 1_500_000
+}
+
+func approxMaxNodesFor(naming namingScheme) int {
+	step := 1_000 // We search with a reduced resolution, to speed up the test
+	ceiling := 300_000
+	nodeNames := naming(ceiling)
+	found := sort.Search(ceiling/step, func(n int) bool {
+		// Here we rely on "well-prefixing"; see the comment on "nodeNaming".
+		return isTooLarge(V1Beta2From(internalSinglePodsOn(nodeNames[:n*step])))
+	}) - 1
+	return found * step
+}
+
+type performanceTestCase struct {
+	name   string
+	naming namingScheme
+}
+
+var performanceTestCases = []performanceTestCase{
+	{
+		name: "pool-and-node-based naming (100 node pools)",
+		naming: poolAndNodeBasedNaming(poolAndNodeBasedNamingConfig{
+			pools:        100, // happens in practice, at least in GKE
+			nodeIDLength: 6,   // reached in AKS
+
+			// reachable in AKS (<pool-name>-<8-char-id>-vmss, then let <pool-name> have 8 chars)
+			poolIDLength: 22,
+
+			fixedPrefixAndSuffixLength: 20,
```

  1. Per [K8s docs](https://kubernetes.io/docs/concepts/architecture/nodes/), node name "must be a valid DNS subdomain name", and the link says it allows up to 253 chars.
  
  2. 20 on one side is quite reachable, e.g. EKS has `.compute.internal` suffix which is 17 chars.
     Then, while I failed to find a real scheme where 20 on both sides would be possible, I just chose to have a "symmetric over-estimate" for best "conceptual simplicity" of the test config.
  
     > Alternatively I would put it only as a prefix and omit suffix
  
     This I'd rather not do - both "long prefix" and "long suffix" happen in practice (just not at the same time), and (from my experimenting experience :) it's actually quite tempting to produce asymmetric approaches which could perform badly in one of those cases. I'd like my tests to catch this.
  
  3. Actually, whether it's 10, 20 or 50 has almost no influence on the "size limit" tests (because the fixed prefix/suffix will be stored just once per the whole TopologyAssignment, both in the current approach and in any sound follow-up strategy).
     The only influence is on the benchmark. Reducing 20 to 10 reduces the encoding latency by 25-50%, depending on the scheme. (In absolute numbers: from 2.9-4.2 ms to 2.1-2.8 ms).
     Once we enter the space of "better-packing approaches", encoding latency will most likely grow enough to make such differences barely noticeable.
  
  But, if you wish, I can decrease to 10 or even (somehow) de-symmetrize. WDYT?

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2025-11-26T13:06:55Z

- **pkg/util/tas/tas_assignment_performance_test.go:47** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-25T10:43:02Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"encoding/hex"
+	"encoding/json"
+	"fmt"
+	"math/rand/v2"
+	"sort"
+	"strings"
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+const (
+	targetNodeCount = 40_000
+)
+
+// Generate n hex numbers of given length,
+// ensuring they're distinct (and otherwise quasi-random).
+// For the health of tests, this function behaves deterministically.
+func randomHexIDs(n, length int) []string {
+	chosen := map[string]bool{}
+	res := make([]string, n)
+	rnd := rand.NewChaCha8([32]byte{})
+	bytes := make([]byte, (length+1)/2)
+	for i := range n {
+		for {
+			var _, _ = rnd.Read(bytes)
```

  If you're not using the results at all, do you need the "_, _" part?

- **pkg/util/tas/tas_assignment_performance_test.go:94** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-26T13:02:25Z
```diff
@@ -0,0 +1,282 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"encoding/hex"
+	"encoding/json"
+	"fmt"
+	"math/rand/v2"
+	"sort"
+	"strings"
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+const (
+	targetNodeCount = 40_000
+)
+
+// Generate n hex numbers of given length,
+// ensuring they're distinct (and otherwise quasi-random).
+// For the health of tests, this function behaves deterministically.
+func randomHexIDs(n, length int) []string {
+	chosen := map[string]bool{}
+	res := make([]string, n)
+	rnd := rand.NewChaCha8([32]byte{})
+	bytes := make([]byte, (length+1)/2)
+	for i := range n {
+		for {
+			var _, _ = rnd.Read(bytes)
+			id := hex.EncodeToString(bytes)[:length]
+			if !chosen[id] {
+				chosen[id] = true
+				res[i] = id
+				break
+			}
+		}
+	}
+	return res
+}
+
+func fixedID(length int) string {
+	// This is really whatever.
+	return strings.Repeat("a", length)
+}
+
+func consecutiveIPs(n int) []string {
+	res := make([]string, n)
+	for i := range n {
+		// Picking 100.10x.xxx.xxx (instead of traditional 10.x.xxx.xxx)
+		// to make the test scenario a bit more adverse.
+		res[i] = fmt.Sprintf("100.%d.%d.%d",
+			100+i/(1<<16),
+			i%(1<<16)/(1<<8),
+			i%(1<<8),
+		)
+	}
+	return res
+}
+
+// namingScheme generates a list of node names according to a specific recipe.
+//
+// The list should be "well-prefixing", in that namingScheme(n)[:m] should be
+// an "equally good representative" of the scheme as namingScheme(m).
+// (This convention helps test performance; it's used in "approxMaxNodesFor").
+// To achieve that, specific schemes should rotate node "properties"
+// (like node pool, region etc.) using "%" operator rather than in big fixed chunks.
+type namingScheme func(nodes int) []string
+
+type poolAndNodeBasedNamingConfig struct {
+	fixedPrefixAndSuffixLength int
+	pools                      int
+	nodeIDLength               int
+	poolIDLength               int
+}
+
+func poolAndNodeBasedNaming(config poolAndNodeBasedNamingConfig) namingScheme {
```

  This and analogous methods could be declared simply as 
  `func (n * <sth>BasedNamingConfig) namingScheme(nodes int) []string {`
  Then you can pass the <sth>BasedNamingConfig.namingScheme method where required.
  
  This would improve readability of the test by reducing the amount of jumping in the code to figure out what part does what.

- **pkg/util/tas/tas_assignment_performance_test.go:94** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-26T13:05:31Z
```diff
@@ -0,0 +1,282 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"encoding/hex"
+	"encoding/json"
+	"fmt"
+	"math/rand/v2"
+	"sort"
+	"strings"
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+const (
+	targetNodeCount = 40_000
+)
+
+// Generate n hex numbers of given length,
+// ensuring they're distinct (and otherwise quasi-random).
+// For the health of tests, this function behaves deterministically.
+func randomHexIDs(n, length int) []string {
+	chosen := map[string]bool{}
+	res := make([]string, n)
+	rnd := rand.NewChaCha8([32]byte{})
+	bytes := make([]byte, (length+1)/2)
+	for i := range n {
+		for {
+			var _, _ = rnd.Read(bytes)
+			id := hex.EncodeToString(bytes)[:length]
+			if !chosen[id] {
+				chosen[id] = true
+				res[i] = id
+				break
+			}
+		}
+	}
+	return res
+}
+
+func fixedID(length int) string {
+	// This is really whatever.
+	return strings.Repeat("a", length)
+}
+
+func consecutiveIPs(n int) []string {
+	res := make([]string, n)
+	for i := range n {
+		// Picking 100.10x.xxx.xxx (instead of traditional 10.x.xxx.xxx)
+		// to make the test scenario a bit more adverse.
+		res[i] = fmt.Sprintf("100.%d.%d.%d",
+			100+i/(1<<16),
+			i%(1<<16)/(1<<8),
+			i%(1<<8),
+		)
+	}
+	return res
+}
+
+// namingScheme generates a list of node names according to a specific recipe.
+//
+// The list should be "well-prefixing", in that namingScheme(n)[:m] should be
+// an "equally good representative" of the scheme as namingScheme(m).
+// (This convention helps test performance; it's used in "approxMaxNodesFor").
+// To achieve that, specific schemes should rotate node "properties"
+// (like node pool, region etc.) using "%" operator rather than in big fixed chunks.
+type namingScheme func(nodes int) []string
+
+type poolAndNodeBasedNamingConfig struct {
+	fixedPrefixAndSuffixLength int
+	pools                      int
+	nodeIDLength               int
+	poolIDLength               int
+}
+
+func poolAndNodeBasedNaming(config poolAndNodeBasedNamingConfig) namingScheme {
```

  Another option would be to replace the `type namingScheme func(nodes int) []string` with an interface defining "namingScheme" and then use `<sth>BasedNamingConfig` structs directly.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-11-26T13:48:30Z

- **pkg/util/tas/tas_assignment_performance_test.go:47** by [@olekzabl](https://github.com/olekzabl) — 2025-11-26T13:48:30Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"encoding/hex"
+	"encoding/json"
+	"fmt"
+	"math/rand/v2"
+	"sort"
+	"strings"
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+const (
+	targetNodeCount = 40_000
+)
+
+// Generate n hex numbers of given length,
+// ensuring they're distinct (and otherwise quasi-random).
+// For the health of tests, this function behaves deterministically.
+func randomHexIDs(n, length int) []string {
+	chosen := map[string]bool{}
+	res := make([]string, n)
+	rnd := rand.NewChaCha8([32]byte{})
+	bytes := make([]byte, (length+1)/2)
+	for i := range n {
+		for {
+			var _, _ = rnd.Read(bytes)
```

  Sadly, yes. Otherwise something shouts at me that the function output is unused.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-11-26T13:55:34Z

- **pkg/util/tas/tas_assignment_performance_test.go:177** by [@kshalot](https://github.com/kshalot) — 2025-11-26T13:55:34Z
```diff
@@ -0,0 +1,282 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"encoding/hex"
+	"encoding/json"
+	"fmt"
+	"math/rand/v2"
+	"sort"
+	"strings"
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+const (
+	targetNodeCount = 40_000
+)
+
+// Generate n hex numbers of given length,
+// ensuring they're distinct (and otherwise quasi-random).
+// For the health of tests, this function behaves deterministically.
+func randomHexIDs(n, length int) []string {
+	chosen := map[string]bool{}
+	res := make([]string, n)
+	rnd := rand.NewChaCha8([32]byte{})
+	bytes := make([]byte, (length+1)/2)
+	for i := range n {
+		for {
+			var _, _ = rnd.Read(bytes)
+			id := hex.EncodeToString(bytes)[:length]
+			if !chosen[id] {
+				chosen[id] = true
+				res[i] = id
+				break
+			}
+		}
+	}
+	return res
+}
+
+func fixedID(length int) string {
+	// This is really whatever.
+	return strings.Repeat("a", length)
+}
+
+func consecutiveIPs(n int) []string {
+	res := make([]string, n)
+	for i := range n {
+		// Picking 100.10x.xxx.xxx (instead of traditional 10.x.xxx.xxx)
+		// to make the test scenario a bit more adverse.
+		res[i] = fmt.Sprintf("100.%d.%d.%d",
+			100+i/(1<<16),
+			i%(1<<16)/(1<<8),
+			i%(1<<8),
+		)
+	}
+	return res
+}
+
+// namingScheme generates a list of node names according to a specific recipe.
+//
+// The list should be "well-prefixing", in that namingScheme(n)[:m] should be
+// an "equally good representative" of the scheme as namingScheme(m).
+// (This convention helps test performance; it's used in "approxMaxNodesFor").
+// To achieve that, specific schemes should rotate node "properties"
+// (like node pool, region etc.) using "%" operator rather than in big fixed chunks.
+type namingScheme func(nodes int) []string
+
+type poolAndNodeBasedNamingConfig struct {
+	fixedPrefixAndSuffixLength int
+	pools                      int
+	nodeIDLength               int
+	poolIDLength               int
+}
+
+func poolAndNodeBasedNaming(config poolAndNodeBasedNamingConfig) namingScheme {
+	return func(nodes int) []string {
+		res := make([]string, nodes)
+		nodeIDs := randomHexIDs(nodes, config.nodeIDLength)
+		poolIDs := randomHexIDs(config.pools, config.poolIDLength)
+		for i := range nodes {
+			res[i] = fmt.Sprintf("%s-%s-%s-%s",
+				fixedID(config.fixedPrefixAndSuffixLength),
+				poolIDs[i%config.pools],
+				nodeIDs[i],
+				fixedID(config.fixedPrefixAndSuffixLength),
+			)
+		}
+		return res
+	}
+}
+
+type regionAndIPBasedNamingConfig struct {
+	fixedPrefixAndSuffixLength int
+	regions                    int
+	regionIDLength             int
+}
+
+func regionAndIPBasedNaming(config regionAndIPBasedNamingConfig) namingScheme {
+	return func(nodes int) []string {
+		res := make([]string, nodes)
+		nodeIPs := consecutiveIPs(nodes)
+		regionIDs := randomHexIDs(config.regions, config.regionIDLength)
+		for i := range nodes {
+			res[i] = fmt.Sprintf("%s-%s-%s-%s",
+				fixedID(config.fixedPrefixAndSuffixLength),
+				regionIDs[i%config.regions],
+				nodeIPs[i],
+				fixedID(config.fixedPrefixAndSuffixLength),
+			)
+		}
+		return res
+	}
+}
+
+type nodeBasedNamingConfig struct {
+	fixedPrefixAndSuffixLength int
+	nodeIDLength               int
+}
+
+func nodeBasedNaming(config nodeBasedNamingConfig) namingScheme {
+	return func(nodes int) []string {
+		res := make([]string, nodes)
+		nodeIDs := randomHexIDs(nodes, config.nodeIDLength)
+		for i := range nodes {
+			res[i] = fmt.Sprintf("%s-%s-%s",
+				fixedID(config.fixedPrefixAndSuffixLength),
+				nodeIDs[i],
+				fixedID(config.fixedPrefixAndSuffixLength),
+			)
+		}
+		return res
+	}
+}
+
+func internalSinglePodsOn(nodes []string) *TopologyAssignment {
+	res := &TopologyAssignment{
+		Levels:  []string{corev1.LabelHostname},
+		Domains: make([]TopologyDomainAssignment, len(nodes)),
+	}
+	for i, n := range nodes {
+		res.Domains[i] = TopologyDomainAssignment{
+			Values: []string{n},
+			Count:  1,
+		}
+	}
+	return res
+}
+
+func jsonStr(v any) string {
+	bytes, err := json.Marshal(v)
+	if err != nil {
+		panic(err)
+	}
+	return string(bytes)
+}
+
+func isTooLarge(ta *kueue.TopologyAssignment) bool {
+	return len(jsonStr(ta)) > 1_500_000
```

  I'd consider making `1_500_000` a named constant so it's clearer to the reader what the limit is. Is the limit bytes, characters or what?
  
  To know that it's the max number of bytes you have to check the type of `jsonStr(ta)`, know that `len(str)` in Go returns the number of bytes and not runes etc.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-26T14:17:53Z

- **pkg/util/tas/tas_assignment_performance_test.go:206** by [@mimowo](https://github.com/mimowo) — 2025-11-26T14:17:53Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"encoding/hex"
+	"encoding/json"
+	"fmt"
+	"math/rand/v2"
+	"sort"
+	"strings"
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+const (
+	targetNodeCount = 40_000
+)
+
+// Generate n hex numbers of given length,
+// ensuring they're distinct (and otherwise quasi-random).
+// For the health of tests, this function behaves deterministically.
+func randomHexIDs(n, length int) []string {
+	chosen := map[string]bool{}
+	res := make([]string, n)
+	rnd := rand.NewChaCha8([32]byte{})
+	bytes := make([]byte, (length+1)/2)
+	for i := range n {
+		for {
+			var _, _ = rnd.Read(bytes)
+			id := hex.EncodeToString(bytes)[:length]
+			if !chosen[id] {
+				chosen[id] = true
+				res[i] = id
+				break
+			}
+		}
+	}
+	return res
+}
+
+func fixedID(length int) string {
+	// This is really whatever.
+	return strings.Repeat("a", length)
+}
+
+func consecutiveIPs(n int) []string {
+	res := make([]string, n)
+	for i := range n {
+		// Picking 100.10x.xxx.xxx (instead of traditional 10.x.xxx.xxx)
+		// to make the test scenario a bit more adverse.
+		res[i] = fmt.Sprintf("100.%d.%d.%d",
+			100+i/(1<<16),
+			i%(1<<16)/(1<<8),
+			i%(1<<8),
+		)
+	}
+	return res
+}
+
+// namingScheme generates a list of node names according to a specific recipe.
+//
+// The list should be "well-prefixing", in that namingScheme(n)[:m] should be
+// an "equally good representative" of the scheme as namingScheme(m).
+// (This convention helps test performance; it's used in "approxMaxNodesFor").
+// To achieve that, specific schemes should rotate node "properties"
+// (like node pool, region etc.) using "%" operator rather than in big fixed chunks.
+type namingScheme func(nodes int) []string
+
+type poolAndNodeBasedNamingConfig struct {
+	fixedPrefixAndSuffixLength int
+	pools                      int
+	nodeIDLength               int
+	poolIDLength               int
+}
+
+func poolAndNodeBasedNaming(config poolAndNodeBasedNamingConfig) namingScheme {
+	return func(nodes int) []string {
+		res := make([]string, nodes)
+		nodeIDs := randomHexIDs(nodes, config.nodeIDLength)
+		poolIDs := randomHexIDs(config.pools, config.poolIDLength)
+		for i := range nodes {
+			res[i] = fmt.Sprintf("%s-%s-%s-%s",
+				fixedID(config.fixedPrefixAndSuffixLength),
+				poolIDs[i%config.pools],
+				nodeIDs[i],
+				fixedID(config.fixedPrefixAndSuffixLength),
+			)
+		}
+		return res
+	}
+}
+
+type regionAndIPBasedNamingConfig struct {
+	fixedPrefixAndSuffixLength int
+	regions                    int
+	regionIDLength             int
+}
+
+func regionAndIPBasedNaming(config regionAndIPBasedNamingConfig) namingScheme {
+	return func(nodes int) []string {
+		res := make([]string, nodes)
+		nodeIPs := consecutiveIPs(nodes)
+		regionIDs := randomHexIDs(config.regions, config.regionIDLength)
+		for i := range nodes {
+			res[i] = fmt.Sprintf("%s-%s-%s-%s",
+				fixedID(config.fixedPrefixAndSuffixLength),
+				regionIDs[i%config.regions],
+				nodeIPs[i],
+				fixedID(config.fixedPrefixAndSuffixLength),
+			)
+		}
+		return res
+	}
+}
+
+type nodeBasedNamingConfig struct {
+	fixedPrefixAndSuffixLength int
+	nodeIDLength               int
+}
+
+func nodeBasedNaming(config nodeBasedNamingConfig) namingScheme {
+	return func(nodes int) []string {
+		res := make([]string, nodes)
+		nodeIDs := randomHexIDs(nodes, config.nodeIDLength)
+		for i := range nodes {
+			res[i] = fmt.Sprintf("%s-%s-%s",
+				fixedID(config.fixedPrefixAndSuffixLength),
+				nodeIDs[i],
+				fixedID(config.fixedPrefixAndSuffixLength),
+			)
+		}
+		return res
+	}
+}
+
+func internalSinglePodsOn(nodes []string) *TopologyAssignment {
+	res := &TopologyAssignment{
+		Levels:  []string{corev1.LabelHostname},
+		Domains: make([]TopologyDomainAssignment, len(nodes)),
+	}
+	for i, n := range nodes {
+		res.Domains[i] = TopologyDomainAssignment{
+			Values: []string{n},
+			Count:  1,
+		}
+	}
+	return res
+}
+
+func jsonStr(v any) string {
+	bytes, err := json.Marshal(v)
+	if err != nil {
+		panic(err)
+	}
+	return string(bytes)
+}
+
+func isTooLarge(ta *kueue.TopologyAssignment) bool {
+	return len(jsonStr(ta)) > 1_500_000
+}
+
+func approxMaxNodesFor(naming namingScheme) int {
+	step := 1_000 // We search with a reduced resolution, to speed up the test
+	ceiling := 300_000
+	nodeNames := naming(ceiling)
+	found := sort.Search(ceiling/step, func(n int) bool {
+		// Here we rely on "well-prefixing"; see the comment on "nodeNaming".
+		return isTooLarge(V1Beta2From(internalSinglePodsOn(nodeNames[:n*step])))
+	}) - 1
+	return found * step
+}
+
+type performanceTestCase struct {
+	name   string
+	naming namingScheme
+}
+
+var performanceTestCases = []performanceTestCase{
+	{
+		name: "pool-and-node-based naming (100 node pools)",
+		naming: poolAndNodeBasedNaming(poolAndNodeBasedNamingConfig{
+			pools:        100, // happens in practice, at least in GKE
+			nodeIDLength: 6,   // reached in AKS
+
+			// reachable in AKS (<pool-name>-<8-char-id>-vmss, then let <pool-name> have 8 chars)
+			poolIDLength: 22,
+
+			fixedPrefixAndSuffixLength: 20,
```

  I'm not sure what is the best value, but I wouldn't like to de-symmetrize,

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-26T16:42:08Z

/lgtm
/approve
Thank you 👍

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-11-27T13:24:40Z

- **pkg/util/tas/tas_assignment_performance_test.go:177** by [@olekzabl](https://github.com/olekzabl) — 2025-11-27T13:24:40Z
```diff
@@ -0,0 +1,282 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"encoding/hex"
+	"encoding/json"
+	"fmt"
+	"math/rand/v2"
+	"sort"
+	"strings"
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+const (
+	targetNodeCount = 40_000
+)
+
+// Generate n hex numbers of given length,
+// ensuring they're distinct (and otherwise quasi-random).
+// For the health of tests, this function behaves deterministically.
+func randomHexIDs(n, length int) []string {
+	chosen := map[string]bool{}
+	res := make([]string, n)
+	rnd := rand.NewChaCha8([32]byte{})
+	bytes := make([]byte, (length+1)/2)
+	for i := range n {
+		for {
+			var _, _ = rnd.Read(bytes)
+			id := hex.EncodeToString(bytes)[:length]
+			if !chosen[id] {
+				chosen[id] = true
+				res[i] = id
+				break
+			}
+		}
+	}
+	return res
+}
+
+func fixedID(length int) string {
+	// This is really whatever.
+	return strings.Repeat("a", length)
+}
+
+func consecutiveIPs(n int) []string {
+	res := make([]string, n)
+	for i := range n {
+		// Picking 100.10x.xxx.xxx (instead of traditional 10.x.xxx.xxx)
+		// to make the test scenario a bit more adverse.
+		res[i] = fmt.Sprintf("100.%d.%d.%d",
+			100+i/(1<<16),
+			i%(1<<16)/(1<<8),
+			i%(1<<8),
+		)
+	}
+	return res
+}
+
+// namingScheme generates a list of node names according to a specific recipe.
+//
+// The list should be "well-prefixing", in that namingScheme(n)[:m] should be
+// an "equally good representative" of the scheme as namingScheme(m).
+// (This convention helps test performance; it's used in "approxMaxNodesFor").
+// To achieve that, specific schemes should rotate node "properties"
+// (like node pool, region etc.) using "%" operator rather than in big fixed chunks.
+type namingScheme func(nodes int) []string
+
+type poolAndNodeBasedNamingConfig struct {
+	fixedPrefixAndSuffixLength int
+	pools                      int
+	nodeIDLength               int
+	poolIDLength               int
+}
+
+func poolAndNodeBasedNaming(config poolAndNodeBasedNamingConfig) namingScheme {
+	return func(nodes int) []string {
+		res := make([]string, nodes)
+		nodeIDs := randomHexIDs(nodes, config.nodeIDLength)
+		poolIDs := randomHexIDs(config.pools, config.poolIDLength)
+		for i := range nodes {
+			res[i] = fmt.Sprintf("%s-%s-%s-%s",
+				fixedID(config.fixedPrefixAndSuffixLength),
+				poolIDs[i%config.pools],
+				nodeIDs[i],
+				fixedID(config.fixedPrefixAndSuffixLength),
+			)
+		}
+		return res
+	}
+}
+
+type regionAndIPBasedNamingConfig struct {
+	fixedPrefixAndSuffixLength int
+	regions                    int
+	regionIDLength             int
+}
+
+func regionAndIPBasedNaming(config regionAndIPBasedNamingConfig) namingScheme {
+	return func(nodes int) []string {
+		res := make([]string, nodes)
+		nodeIPs := consecutiveIPs(nodes)
+		regionIDs := randomHexIDs(config.regions, config.regionIDLength)
+		for i := range nodes {
+			res[i] = fmt.Sprintf("%s-%s-%s-%s",
+				fixedID(config.fixedPrefixAndSuffixLength),
+				regionIDs[i%config.regions],
+				nodeIPs[i],
+				fixedID(config.fixedPrefixAndSuffixLength),
+			)
+		}
+		return res
+	}
+}
+
+type nodeBasedNamingConfig struct {
+	fixedPrefixAndSuffixLength int
+	nodeIDLength               int
+}
+
+func nodeBasedNaming(config nodeBasedNamingConfig) namingScheme {
+	return func(nodes int) []string {
+		res := make([]string, nodes)
+		nodeIDs := randomHexIDs(nodes, config.nodeIDLength)
+		for i := range nodes {
+			res[i] = fmt.Sprintf("%s-%s-%s",
+				fixedID(config.fixedPrefixAndSuffixLength),
+				nodeIDs[i],
+				fixedID(config.fixedPrefixAndSuffixLength),
+			)
+		}
+		return res
+	}
+}
+
+func internalSinglePodsOn(nodes []string) *TopologyAssignment {
+	res := &TopologyAssignment{
+		Levels:  []string{corev1.LabelHostname},
+		Domains: make([]TopologyDomainAssignment, len(nodes)),
+	}
+	for i, n := range nodes {
+		res.Domains[i] = TopologyDomainAssignment{
+			Values: []string{n},
+			Count:  1,
+		}
+	}
+	return res
+}
+
+func jsonStr(v any) string {
+	bytes, err := json.Marshal(v)
+	if err != nil {
+		panic(err)
+	}
+	return string(bytes)
+}
+
+func isTooLarge(ta *kueue.TopologyAssignment) bool {
+	return len(jsonStr(ta)) > 1_500_000
```

  Addressed in follow-up PR #7958.
  
  I take the point about bytes <-> string confusion.
  (Even though it has no influence on results for these particular test data, and I'm not sure if it could influence for any other data - I mean, not sure if TopologyAssignment can possibly contain "Ą" or other weird chars _anywhere_).
  
  Still, better to clarify. So now I'm explicitly counting bytes.
  
  And then, I don't think a named constant will add value. But feel free to complain there :)

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-11-27T13:26:43Z

- **pkg/util/tas/tas_assignment_performance_test.go:94** by [@olekzabl](https://github.com/olekzabl) — 2025-11-27T13:26:42Z
```diff
@@ -0,0 +1,282 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"encoding/hex"
+	"encoding/json"
+	"fmt"
+	"math/rand/v2"
+	"sort"
+	"strings"
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+const (
+	targetNodeCount = 40_000
+)
+
+// Generate n hex numbers of given length,
+// ensuring they're distinct (and otherwise quasi-random).
+// For the health of tests, this function behaves deterministically.
+func randomHexIDs(n, length int) []string {
+	chosen := map[string]bool{}
+	res := make([]string, n)
+	rnd := rand.NewChaCha8([32]byte{})
+	bytes := make([]byte, (length+1)/2)
+	for i := range n {
+		for {
+			var _, _ = rnd.Read(bytes)
+			id := hex.EncodeToString(bytes)[:length]
+			if !chosen[id] {
+				chosen[id] = true
+				res[i] = id
+				break
+			}
+		}
+	}
+	return res
+}
+
+func fixedID(length int) string {
+	// This is really whatever.
+	return strings.Repeat("a", length)
+}
+
+func consecutiveIPs(n int) []string {
+	res := make([]string, n)
+	for i := range n {
+		// Picking 100.10x.xxx.xxx (instead of traditional 10.x.xxx.xxx)
+		// to make the test scenario a bit more adverse.
+		res[i] = fmt.Sprintf("100.%d.%d.%d",
+			100+i/(1<<16),
+			i%(1<<16)/(1<<8),
+			i%(1<<8),
+		)
+	}
+	return res
+}
+
+// namingScheme generates a list of node names according to a specific recipe.
+//
+// The list should be "well-prefixing", in that namingScheme(n)[:m] should be
+// an "equally good representative" of the scheme as namingScheme(m).
+// (This convention helps test performance; it's used in "approxMaxNodesFor").
+// To achieve that, specific schemes should rotate node "properties"
+// (like node pool, region etc.) using "%" operator rather than in big fixed chunks.
+type namingScheme func(nodes int) []string
+
+type poolAndNodeBasedNamingConfig struct {
+	fixedPrefixAndSuffixLength int
+	pools                      int
+	nodeIDLength               int
+	poolIDLength               int
+}
+
+func poolAndNodeBasedNaming(config poolAndNodeBasedNamingConfig) namingScheme {
```

  Nice, thanks. I followed up in PR #7958 & introduced the interface.
  
  It's just important for me that these structs are named `...Naming`, not `...Config`. Because, conceptually, I'm dealing with _namings_, "config" is an implementation detail which doesn't deserve to influence naming.
  And then, I got rid of pointers, to avoid debating "what's the proper naming - a struct or a pointer to it".
