# PR #7845: Fix race condition that prevents Workload from reflecting Inadmissible status.

**Summary**: Fix race condition that prevents Workload from reflecting Inadmissible status.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7845

**Last updated**: 2025-11-25T12:38:49Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `fix/Race-Condition-Prevents-Workload-from-Reflecting-Inadmissible-Status` → `main`
- **Created**: 2025-11-24T11:34:34Z
- **Updated**: 2025-11-25T12:38:49Z
- **Merged**: 2025-11-25T07:56:36Z
- **Closed**: 2025-11-25T07:56:36Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 2   **+188 / -1**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Fix race condition that prevents Workload from reflecting Inadmissible status.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #6789

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Fix the race condition which could result that the Kueue scheduler occasionally does not record the reason
for admission failure of a workload if the workload was modified in the meanwhile by another controller.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-11-24T11:34:44Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 394df1e233472b9682f8b7e527c46b273fdeaacc |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69255b2008e5a500087fbf23 |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-24T11:37:27Z

/kind bug

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-24T11:40:01Z

/remove-kind cleanup

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-24T11:43:04Z

/release-note-edit
```release-note
Fix the race condition which could result that the Kueue scheduler occasionally does not record the reason
for admission failure of a workload if the workload was modified in the meanwhile by another controller.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-24T11:49:32Z



@mbobrovskyi the change is rather easy so I'm ok to merge, but I'm considering if we could write a test for the change? 

Maybe it could be unit test, or integration we also recently following this example: https://github.com/kubernetes-sigs/kueue/pull/7665

We could then reuse the test for the retry when we implement the patch. wdyt?

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-25T06:40:05Z

> @mbobrovskyi the change is rather easy so I'm ok to merge, but I'm considering if we could write a test for the change?
> 
> Maybe it could be unit test, or integration we also recently following this example: #7665
> 
> We could then reuse the test for the retry when we implement the patch. wdyt?

Added unit test.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-25T07:31:32Z

/lgtm
/approve
/cherrypick release-0.14
/cherrypick release-0.13

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-11-25T07:31:34Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.13`, `release-0.14` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7845#issuecomment-3574115736):

>/lgtm
>/approve
>/cherrypick release-0.14
>/cherrypick release-0.13


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-25T07:31:39Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7845#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7845#issuecomment-3574115736" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-25T07:31:39Z

LGTM label has been added.  <details>Git tree hash: 581dcd728813445deba784cb6bb144b9bfc56661</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-25T07:36:22Z

I triggered tentatively cherrypicks to 0.13 and 0.14, but I'm aware there has been many changes to the SSA / patch mechanics recently, so if hard to cherrypick, then we can skip, and just recommend users updating to 0.15. Please check and let us know how tricky this is @mbobrovskyi

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-25T07:38:13Z

As I mentioned in https://github.com/kubernetes-sigs/kueue/pull/7845#discussion_r2558821167, it would be better to use strict mode when performing a PATCH operation.

Actually, we don't observe performance regression. So, I think we should respect atomic behavior in strict mode.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-25T07:47:21Z

Correct, when we enable PATCH we will need to use strict. We don't observe performance regression so we could just do it. However, for this specific issue, if we use strict PATCH without retry, then the condition will not be recorded. So, the task by @mbobrovskyi to retry when using PATCH is important for this case.

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-11-25T07:57:14Z

@mimowo: #7845 failed to apply on top of branch "release-0.13":
```
Applying: Fix race condition that prevents Workload from reflecting Inadmissible status.
Using index info to reconstruct a base tree...
M	pkg/scheduler/scheduler.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/scheduler/scheduler.go
CONFLICT (content): Merge conflict in pkg/scheduler/scheduler.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 Fix race condition that prevents Workload from reflecting Inadmissible status.

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7845#issuecomment-3574115736):

>/lgtm
>/approve
>/cherrypick release-0.14
>/cherrypick release-0.13


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-11-25T07:57:48Z

@mimowo: #7845 failed to apply on top of branch "release-0.14":
```
Applying: Fix race condition that prevents Workload from reflecting Inadmissible status.
Using index info to reconstruct a base tree...
M	pkg/scheduler/scheduler.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/scheduler/scheduler.go
CONFLICT (content): Merge conflict in pkg/scheduler/scheduler.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 Fix race condition that prevents Workload from reflecting Inadmissible status.

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7845#issuecomment-3574115736):

>/lgtm
>/approve
>/cherrypick release-0.14
>/cherrypick release-0.13


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-25T12:38:49Z

> This is why we have two separate options: LooseOnApply, and LooseOnPatch. We only change LooseOnApply in this PR.

> Correct, when we enable PATCH we will need to use strict. We don't observe performance regression so we could just do it. However, for this specific issue, if we use strict PATCH without retry, then the condition will not be recorded. So, the task by @mbobrovskyi to retry when using PATCH is important for this case.

That makes sense. For the Apply operation, that sounds reasonable to me.
For the Patch operation, let' revisit how to handle that in PATCH retry mechanism PR or PATCH graduation PR 👍

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-11-24T14:27:13Z

- **pkg/scheduler/scheduler.go:801** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-24T14:27:11Z
```diff
@@ -798,7 +798,7 @@ func (s *Scheduler) requeueAndUpdate(ctx context.Context, e entry) {
 				updated = true
 			}
 			return updated, nil
-		}); err != nil {
+		}, workload.WithLooseOnApply()); err != nil {
```

  Isn't this the cause that the old resource version will be patched?
  I suspect that we will lose the atomic Admission.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T14:45:14Z

- **pkg/scheduler/scheduler.go:801** by [@mimowo](https://github.com/mimowo) — 2025-11-24T14:45:13Z
```diff
@@ -798,7 +798,7 @@ func (s *Scheduler) requeueAndUpdate(ctx context.Context, e entry) {
 				updated = true
 			}
 			return updated, nil
-		}); err != nil {
+		}, workload.WithLooseOnApply()); err != nil {
```

  Yes it might mean that we will patch older version. However this is also what we do during admission from within scheduler. Since scheduler is the only entity which assigns admission I don't see it being problematic. Do you see some scenario Yuki?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-11-24T14:57:27Z

- **pkg/scheduler/scheduler.go:801** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-24T14:57:26Z
```diff
@@ -798,7 +798,7 @@ func (s *Scheduler) requeueAndUpdate(ctx context.Context, e entry) {
 				updated = true
 			}
 			return updated, nil
-		}); err != nil {
+		}, workload.WithLooseOnApply()); err != nil {
```

  IIRC, the admission used SSA previously. So, the resource versions could be ignored.
  But, now, we use the Patch operation for that (correct me if my understanding is incorrect).
  
  So, I am worried about introducing a non-strict operation for that. 
  The current resource version error -> requeue sounds reasonable for the atomic policy.
  
  AFAIK, you don't observe performance regressions for the requeueing due to conflict errors, right?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T15:00:39Z

- **pkg/scheduler/scheduler.go:801** by [@mimowo](https://github.com/mimowo) — 2025-11-24T15:00:39Z
```diff
@@ -798,7 +798,7 @@ func (s *Scheduler) requeueAndUpdate(ctx context.Context, e entry) {
 				updated = true
 			}
 			return updated, nil
-		}); err != nil {
+		}, workload.WithLooseOnApply()); err != nil {
```

  You are partly correct. We are not yet using Patch. The LooseOnApply is used specifically to use Loose semantics for SSA. The semantics for Patch (still alpha) remains strict. For Patch Mike is working on the retry

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-24T15:55:02Z

- **pkg/scheduler/scheduler.go:801** by [@mimowo](https://github.com/mimowo) — 2025-11-24T15:55:02Z
```diff
@@ -798,7 +798,7 @@ func (s *Scheduler) requeueAndUpdate(ctx context.Context, e entry) {
 				updated = true
 			}
 			return updated, nil
-		}); err != nil {
+		}, workload.WithLooseOnApply()); err != nil {
```

  > AFAIK, you don't observe performance regressions for the requeueing due to conflict errors, right?
  
  We don't observe performance regression for using Patch, yes. However, I think this change to use patch might be for 0.16 as we already have many changes in this release.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-25T07:23:13Z

- **pkg/scheduler/scheduler_test.go:9114** by [@mimowo](https://github.com/mimowo) — 2025-11-25T07:23:13Z
```diff
@@ -9109,3 +9110,189 @@ func TestResourcesToReserve(t *testing.T) {
 		})
 	}
 }
+
+func TestSchedulerWithRaceCondition(t *testing.T) {
```

  ```suggestion
  func TestSchedulerWhenWorkloadModifiedConcurrently(t *testing.T) {
  ```
  nit the previous name suggests the race condition is inside scheduler

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-25T07:23:36Z

- **pkg/scheduler/scheduler_test.go:9231** by [@mimowo](https://github.com/mimowo) — 2025-11-25T07:23:36Z
```diff
@@ -9109,3 +9110,189 @@ func TestResourcesToReserve(t *testing.T) {
 		})
 	}
 }
+
+func TestSchedulerWithRaceCondition(t *testing.T) {
+	now := time.Now().Truncate(time.Second)
+
+	ns := utiltesting.MakeNamespaceWrapper("default").Obj()
+	rf := utiltestingapi.MakeResourceFlavor("rf").Obj()
+	cq := utiltestingapi.MakeClusterQueue("cq").
+		ResourceGroup(
+			*utiltestingapi.MakeFlavorQuotas(rf.Name).
+				Resource(corev1.ResourceCPU, "1").
+				Obj(),
+		).Obj()
+	lq := utiltestingapi.MakeLocalQueue("lq", metav1.NamespaceDefault).ClusterQueue(cq.Name).Obj()
+
+	testCases := map[string]struct {
+		features      map[featuregate.Feature]bool
+		workload      *kueue.Workload
+		wantWorkloads []kueue.Workload
+		wantEvents    []utiltesting.EventRecord
+	}{
+		"use patch in admit when the WorkloadRequestUseMergePatch feature is disabled": {
+			features: map[featuregate.Feature]bool{
+				features.WorkloadRequestUseMergePatch: false,
+			},
+			workload: utiltestingapi.MakeWorkload("wl", metav1.NamespaceDefault).
+				ResourceVersion("1").
+				Queue(kueue.LocalQueueName(lq.Name)).
+				PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+					Request(corev1.ResourceCPU, "1").
+					Obj()).
+				Obj(),
+			wantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("wl", metav1.NamespaceDefault).
+					ResourceVersion("1").
+					Queue(kueue.LocalQueueName(lq.Name)).
+					PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+						Request(corev1.ResourceCPU, "1").
+						Obj()).
+					ResourceVersion("3").
+					Condition(metav1.Condition{
+						Type:               kueue.WorkloadQuotaReserved,
+						Status:             metav1.ConditionTrue,
+						Reason:             kueue.WorkloadQuotaReserved,
+						Message:            "Quota reserved in ClusterQueue cq",
+						LastTransitionTime: metav1.NewTime(now),
+					}).
+					Admission(
+						utiltestingapi.MakeAdmission(cq.Name).
+							PodSets(utiltestingapi.MakePodSetAssignment(kueue.DefaultPodSetName).
+								Assignment(corev1.ResourceCPU, kueue.ResourceFlavorReference(rf.Name), "1").
+								Obj()).
+							Obj(),
+					).
+					Condition(metav1.Condition{
+						Type:               kueue.WorkloadAdmitted,
+						Status:             metav1.ConditionTrue,
+						Reason:             kueue.WorkloadAdmitted,
+						Message:            "The workload is admitted",
+						LastTransitionTime: metav1.NewTime(now),
+					}).
+					Obj(),
+			},
+			wantEvents: []utiltesting.EventRecord{
+				utiltesting.MakeEventRecord(metav1.NamespaceDefault, "wl", "QuotaReserved", corev1.EventTypeNormal).Obj(),
+				utiltesting.MakeEventRecord(metav1.NamespaceDefault, "wl", "Admitted", corev1.EventTypeNormal).Obj(),
+			},
+		},
+		"use patch in requeueAndUpdate when the WorkloadRequestUseMergePatch feature is disabled": {
+			features: map[featuregate.Feature]bool{
+				features.WorkloadRequestUseMergePatch: false,
+			},
+			workload: utiltestingapi.MakeWorkload("wl", metav1.NamespaceDefault).
+				ResourceVersion("1").
+				Queue(kueue.LocalQueueName(lq.Name)).
+				PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 2).
+					Request(corev1.ResourceCPU, "1").
+					Obj()).
+				Obj(),
+			wantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("wl", metav1.NamespaceDefault).
+					ResourceVersion("1").
+					Queue(kueue.LocalQueueName(lq.Name)).
+					PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 2).
+						Request(corev1.ResourceCPU, "1").
+						Obj()).
+					ResourceVersion("3").
+					Condition(metav1.Condition{
+						Type:               kueue.WorkloadQuotaReserved,
+						Status:             metav1.ConditionFalse,
+						Reason:             "Pending",
+						Message:            "couldn't assign flavors to pod set main: insufficient quota for cpu in flavor rf, previously considered podsets requests (0) + current podset request (2) > maximum capacity (1)",
+						LastTransitionTime: metav1.NewTime(now),
+					}).
+					ResourceRequests(kueue.PodSetRequest{
+						Name: kueue.DefaultPodSetName,
+						Resources: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+					}).
+					Obj(),
+			},
+			wantEvents: []utiltesting.EventRecord{
+				utiltesting.MakeEventRecord(metav1.NamespaceDefault, "wl", "Pending", corev1.EventTypeWarning).Obj(),
+			},
+		},
+	}
+	for name, tc := range testCases {
+		t.Run(name, func(t *testing.T) {
+			for feature, enabled := range tc.features {
+				features.SetFeatureGateDuringTest(t, feature, enabled)
+			}
+			ctx, log := utiltesting.ContextWithLog(t)
+			clientBuilder := utiltesting.NewClientBuilder().
+				WithObjects(ns.DeepCopy(), rf.DeepCopy(), cq.DeepCopy(), lq.DeepCopy(), tc.workload).
+				WithStatusSubresource(&kueue.Workload{}).
+				WithInterceptorFuncs(interceptor.Funcs{
+					SubResourcePatch: func(ctx context.Context, c client.Client, subResourceName string, obj client.Object, patch client.Patch, opts ...client.SubResourcePatchOption) error {
+						if _, ok := obj.(*kueue.Workload); ok && subResourceName == "status" {
+							// Simulate race condition.
```

  ```suggestion
  							// Simulate concurrent modification by another controller
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-25T07:24:08Z

LGTM, just nits, thank you 👍

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-11-25T07:26:53Z

- **pkg/scheduler/scheduler.go:801** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-25T07:26:52Z
```diff
@@ -798,7 +798,7 @@ func (s *Scheduler) requeueAndUpdate(ctx context.Context, e entry) {
 				updated = true
 			}
 			return updated, nil
-		}); err != nil {
+		}, workload.WithLooseOnApply()); err != nil {
```

  > You are partly correct. We are not yet suing Patch. The LooseOnApply is used specifically to use Loose semantics for SSA. The semantics for Patch (still alpha) remains strict. For Patch Mike is working on the retry
  
  I think that PATCH with non-strict mode does not look safe since the Workload objects are accessed by multiple components like Kueue, external-manager (implement JobFramework), Dispatchers, and AdmissionChecks.
  
  So, I think that we should avoid this mode when we enable the PATCH operation.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-25T07:44:21Z

- **pkg/scheduler/scheduler.go:801** by [@mimowo](https://github.com/mimowo) — 2025-11-25T07:44:21Z
```diff
@@ -798,7 +798,7 @@ func (s *Scheduler) requeueAndUpdate(ctx context.Context, e entry) {
 				updated = true
 			}
 			return updated, nil
-		}); err != nil {
+		}, workload.WithLooseOnApply()); err != nil {
```

  This is why we have two separate options: LooseOnApply, and LooseOnPatch. We only change LooseOnApply in this PR.
