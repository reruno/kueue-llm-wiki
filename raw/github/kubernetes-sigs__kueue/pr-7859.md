# PR #7859: Add lightweight e2e tests for certs rotation

**Summary**: Add lightweight e2e tests for certs rotation

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7859

**Last updated**: 2026-02-21T15:46:27Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `cert-rotation-scale-tests` → `main`
- **Created**: 2025-11-24T18:08:26Z
- **Updated**: 2026-02-21T15:46:27Z
- **Merged**: 2025-11-25T16:36:46Z
- **Closed**: 2025-11-25T16:36:46Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@sohankunkerkar](https://github.com/sohankunkerkar)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 1   **+106 / -3**

## Description

#### What type of PR is this?

/kind cleanup

#### What this PR does / why we need it:

To have some lightweight tests for cert rotation on startup which can be easily looped for checking of flakes.

#### Which issue(s) this PR fixes:

Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-11-24T18:08:33Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 70df402d094c18fbbc486a1fea20c809da2b3cc2 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6925d4f6c982c70007460606 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-7859--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-7859--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTc4NTktLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.WMpjU855s-8eZLdFxnJH_Kx9ounc-RNqOVj0WZv1FGg)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-7859--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-24T18:08:35Z

cc @sohankunkerkar

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-24T18:11:31Z

LGTM label has been added.  <details>Git tree hash: 7148c193276936141b6b07f77508a926f57487b4</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-24T18:24:17Z

/hold 
let me drop the loopong

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-24T18:25:11Z

https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7859/pull-kueue-test-e2e-main-1-32/1993018902510047232

I will check that

EDIT: added the timeout to LongTimeout for the LQ to be active, because I found a long gap around that to get the lease, [log](https://gcsweb.k8s.io/gcs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7859/pull-kueue-test-e2e-main-1-32/1993018902510047232/artifacts/run-test-e2e-singlecluster-1.32.8/kind-worker2/pods/kueue-system_kueue-controller-manager-dbbb6fcd9-qtw2z_7f7616d4-4725-4c05-800e-f9018e1cb281/manager/):
```
2025-11-24T18:22:22.030343249Z stderr F 2025-11-24T18:22:22.030042836Z	LEVEL(-2)	localqueue-reconciler	core/localqueue_controller.go:229	Queue update event	{"localQueue": {"name":"main","namespace":"e2e-certs-7zzvl"}}
2025-11-24T18:22:22.048289382Z stderr F 2025-11-24T18:22:22.04809231Z	LEVEL(-2)	localqueue-reconciler	core/localqueue_controller.go:229	Queue update event	{"localQueue": {"name":"main","namespace":"e2e-certs-7zzvl"}}
2025-11-24T18:22:22.078075117Z stderr F 2025-11-24T18:22:22.077771024Z	LEVEL(-2)	localqueue-reconciler	core/localqueue_controller.go:221	LocalQueue delete event	{"localQueue": {"name":"main","namespace":"e2e-certs-7zzvl"}}
2025-11-24T18:22:22.13467158Z stderr F 2025-11-24T18:22:22.134404108Z	LEVEL(-2)	cluster-queue-reconciler	core/clusterqueue_controller.go:324	ClusterQueue update event	{"clusterQueue": {"name":"cluster-queue"}}
2025-11-24T18:22:35.604569143Z stderr F I1124 18:22:35.604366       1 leaderelection.go:271] successfully acquired lease kueue-system/c1f6bfd2.kueue.x-k8s.io
2025-11-24T18:22:35.605148069Z stderr F 2025-11-24T18:22:35.604868266Z	LEVEL(-2)	MultiKueueGC	multikueue/multikueuecluster.go:641	Starting Garbage Collector
2025-11-24T18:22:35.605171579Z stderr F 2025-11-24T18:22:35.605016358Z	DEBUG	events	recorder/recorder.go:104	kueue-controller-manager-dbbb6fcd9-qtw2z_36c2a25c-296a-4047-914a-96258eaec698 became leader	{"type": "Normal", "object": {"kind":"Lease","namespace":"kueue-system","name":"c1f6bfd2.kueue.x-k8s.io","uid":"b81ba0f0-e6aa-4956-b0a9-3fdf341debef","apiVersion":"coordination.k8s.io/v1","resourceVersion":"3376"}, "reason": "LeaderElection"}
```
not sure there is a better fix there. Testing with 100 repeats to see if that helps.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-25T10:21:17Z

`pull-kueue-test-e2e-main-1-33` failure is unrelated;
https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7859/pull-kueue-test-e2e-main-1-33/1993025952111661056
opened: https://github.com/kubernetes-sigs/kueue/issues/7875

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-25T12:26:56Z

The build of this commit passed https://github.com/kubernetes-sigs/kueue/pull/7859/commits/24c1439610198268d18a355def44c7ff88368908. Note that each e2e test (3 of them executed 100 repeats, so 300 repeats total).

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-25T12:33:48Z

ready for review now

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-25T13:01:45Z

/test pull-kueue-test-e2e-main-1-33
failed due to https://github.com/kubernetes-sigs/kueue/issues/7844

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-25T13:02:38Z

/test pull-kueue-test-e2e-multikueue-main
failed due to https://github.com/kubernetes-sigs/kueue/issues/7844

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-25T15:11:41Z

/hold
for testing after the change https://github.com/kubernetes-sigs/kueue/pull/7859#discussion_r2560229467 to make sure we don't have a flake after the test code is fixed

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-25T15:58:16Z

/unhold
@sohankunkerkar ptal, I have reverted the looping after testing under: https://github.com/kubernetes-sigs/kueue/pull/7859/commits/c9cca0aa37dd5f7a926d60f78b361f05c109492f. All 300 repeats passed, the tests were only marked "failed" because I used focus (Fit)

Links:
https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7859/pull-kueue-test-e2e-main-1-33/1993336545850953728
https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7859/pull-kueue-test-e2e-main-1-34/1993336545947422720
https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7859/pull-kueue-test-e2e-main-1-32/1993336545762873344

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-25T16:10:51Z

squashed into singe commit before merge

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-25T16:11:38Z

LGTM label has been added.  <details>Git tree hash: f268357c5cf6873f0da59450e3447ee7d8a4ac04</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-25T16:11:39Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7859#" title="Author self-approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7859#pullrequestreview-3505835920" title="LGTM">sohankunkerkar</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (approved) — 2025-11-24T18:11:23Z

one nit but looks good!
/lgtm

- **test/e2e/singlecluster/certs_test.go:153** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-11-24T18:09:57Z
```diff
@@ -144,4 +148,103 @@ var _ = ginkgo.Describe("Kueue Certs", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	for i := range 10 {
+		ginkgo.FIt(fmt.Sprintf("Should allow to bootstrap certificates %d", i), func() {
```

  ```suggestion
  		ginkgo.It(fmt.Sprintf("Should allow to bootstrap certificates %d", i), func() {
  ```

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2025-11-24T18:11:38Z

- **test/e2e/singlecluster/certs_test.go:153** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-11-24T18:11:38Z
```diff
@@ -144,4 +148,103 @@ var _ = ginkgo.Describe("Kueue Certs", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	for i := range 10 {
+		ginkgo.FIt(fmt.Sprintf("Should allow to bootstrap certificates %d", i), func() {
```

  /hold

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-25T12:33:27Z

- **test/e2e/singlecluster/certs_test.go:153** by [@mimowo](https://github.com/mimowo) — 2025-11-25T12:33:27Z
```diff
@@ -144,4 +148,103 @@ var _ = ginkgo.Describe("Kueue Certs", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	for i := range 10 {
+		ginkgo.FIt(fmt.Sprintf("Should allow to bootstrap certificates %d", i), func() {
```

  done
  /unhold

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2025-11-25T14:43:50Z

- **test/e2e/singlecluster/certs_test.go:189** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-11-25T14:36:42Z
```diff
@@ -144,4 +151,101 @@ var _ = ginkgo.Describe("Kueue Certs", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("Should allow to bootstrap certificates", func() {
+		oldReplicas := 0
+		deployment := &appsv1.Deployment{}
+		localQueueCRD := &apiextensionsv1.CustomResourceDefinition{}
+		key := types.NamespacedName{Namespace: kueueNS, Name: "kueue-controller-manager"}
+		ginkgo.By("scale down replicas to 0", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, key, deployment)).To(gomega.Succeed())
+				oldReplicas = max(oldReplicas, int(ptr.Deref(deployment.Spec.Replicas, 0)))
+				deployment.Spec.Replicas = ptr.To[int32](0)
+				g.Expect(k8sClient.Update(ctx, deployment)).Should(gomega.Succeed())
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("await for no replicas", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, key, deployment)).To(gomega.Succeed())
+				g.Expect(deployment.Status.Replicas).To(gomega.Equal(int32(0)))
+				g.Expect(deployment.Status.TerminatingReplicas).To(gomega.BeNil())
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("clear the caBundle field", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, localQueueCRDKey, localQueueCRD)).Should(gomega.Succeed())
+				localQueueCRD.Spec.Conversion.Webhook.ClientConfig.CABundle = nil
+				g.Expect(k8sClient.Update(ctx, localQueueCRD)).Should(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("clear the caBundle fields for webhooks", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				mwc := &admissionregistrationv1.MutatingWebhookConfiguration{}
+				g.Expect(k8sClient.Get(ctx, mvcKey, mwc)).To(gomega.Succeed())
+				for _, webhook := range mwc.Webhooks {
```

  I think webhook is a copy, not a reference. You might need to change like the below code else the nil assignment has no effect on the actual webhook configuration.
  ```suggestion
  				for i := range mwc.Webhooks {
  				        mwc.Webhooks[i].ClientConfig.CABundle = nil
  			      }
  ```

- **test/e2e/singlecluster/certs_test.go:219** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-11-25T14:38:44Z
```diff
@@ -144,4 +151,101 @@ var _ = ginkgo.Describe("Kueue Certs", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("Should allow to bootstrap certificates", func() {
+		oldReplicas := 0
+		deployment := &appsv1.Deployment{}
+		localQueueCRD := &apiextensionsv1.CustomResourceDefinition{}
+		key := types.NamespacedName{Namespace: kueueNS, Name: "kueue-controller-manager"}
+		ginkgo.By("scale down replicas to 0", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, key, deployment)).To(gomega.Succeed())
+				oldReplicas = max(oldReplicas, int(ptr.Deref(deployment.Spec.Replicas, 0)))
+				deployment.Spec.Replicas = ptr.To[int32](0)
+				g.Expect(k8sClient.Update(ctx, deployment)).Should(gomega.Succeed())
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("await for no replicas", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, key, deployment)).To(gomega.Succeed())
+				g.Expect(deployment.Status.Replicas).To(gomega.Equal(int32(0)))
+				g.Expect(deployment.Status.TerminatingReplicas).To(gomega.BeNil())
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("clear the caBundle field", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, localQueueCRDKey, localQueueCRD)).Should(gomega.Succeed())
+				localQueueCRD.Spec.Conversion.Webhook.ClientConfig.CABundle = nil
+				g.Expect(k8sClient.Update(ctx, localQueueCRD)).Should(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("clear the caBundle fields for webhooks", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				mwc := &admissionregistrationv1.MutatingWebhookConfiguration{}
+				g.Expect(k8sClient.Get(ctx, mvcKey, mwc)).To(gomega.Succeed())
+				for _, webhook := range mwc.Webhooks {
+					webhook.ClientConfig.CABundle = nil
+				}
+				g.Expect(k8sClient.Update(ctx, mwc)).Should(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("scale back replicas", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, key, deployment)).To(gomega.Succeed())
+				deployment.Spec.Replicas = ptr.To(int32(oldReplicas))
+				g.Expect(k8sClient.Update(ctx, deployment)).Should(gomega.Succeed())
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("await for ready replicas", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, key, deployment)).To(gomega.Succeed())
+				g.Expect(deployment.Status.ReadyReplicas).To(gomega.Equal(int32(oldReplicas)))
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("await for Kueue to be available", func() {
+			util.WaitForKueueAvailability(ctx, k8sClient)
+		})
+
+		ginkgo.By("verify the caBundle is set again for CRD", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, localQueueCRDKey, localQueueCRD)).To(gomega.Succeed())
+				caBundle := localQueueCRD.Spec.Conversion.Webhook.ClientConfig.CABundle
+				g.Expect(caBundle).NotTo(gomega.BeEmpty())
```

  You might want to do that same as described above.
  ```go
          for i := range mwc.Webhooks {
                g.Expect(mwc.Webhooks[i].ClientConfig.CABundle).ToNot(gomega.BeEmpty(),
                    "webhook %s should have CABundle set", mwc.Webhooks[i].Name)
            }
  
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-25T15:11:07Z

- **test/e2e/singlecluster/certs_test.go:189** by [@mimowo](https://github.com/mimowo) — 2025-11-25T15:11:07Z
```diff
@@ -144,4 +151,101 @@ var _ = ginkgo.Describe("Kueue Certs", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("Should allow to bootstrap certificates", func() {
+		oldReplicas := 0
+		deployment := &appsv1.Deployment{}
+		localQueueCRD := &apiextensionsv1.CustomResourceDefinition{}
+		key := types.NamespacedName{Namespace: kueueNS, Name: "kueue-controller-manager"}
+		ginkgo.By("scale down replicas to 0", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, key, deployment)).To(gomega.Succeed())
+				oldReplicas = max(oldReplicas, int(ptr.Deref(deployment.Spec.Replicas, 0)))
+				deployment.Spec.Replicas = ptr.To[int32](0)
+				g.Expect(k8sClient.Update(ctx, deployment)).Should(gomega.Succeed())
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("await for no replicas", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, key, deployment)).To(gomega.Succeed())
+				g.Expect(deployment.Status.Replicas).To(gomega.Equal(int32(0)))
+				g.Expect(deployment.Status.TerminatingReplicas).To(gomega.BeNil())
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("clear the caBundle field", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, localQueueCRDKey, localQueueCRD)).Should(gomega.Succeed())
+				localQueueCRD.Spec.Conversion.Webhook.ClientConfig.CABundle = nil
+				g.Expect(k8sClient.Update(ctx, localQueueCRD)).Should(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("clear the caBundle fields for webhooks", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				mwc := &admissionregistrationv1.MutatingWebhookConfiguration{}
+				g.Expect(k8sClient.Get(ctx, mvcKey, mwc)).To(gomega.Succeed())
+				for _, webhook := range mwc.Webhooks {
```

  Good call, let me adjust the code to make sure the webhook is updated

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-25T15:13:12Z

- **test/e2e/singlecluster/certs_test.go:219** by [@mimowo](https://github.com/mimowo) — 2025-11-25T15:13:12Z
```diff
@@ -144,4 +151,101 @@ var _ = ginkgo.Describe("Kueue Certs", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("Should allow to bootstrap certificates", func() {
+		oldReplicas := 0
+		deployment := &appsv1.Deployment{}
+		localQueueCRD := &apiextensionsv1.CustomResourceDefinition{}
+		key := types.NamespacedName{Namespace: kueueNS, Name: "kueue-controller-manager"}
+		ginkgo.By("scale down replicas to 0", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, key, deployment)).To(gomega.Succeed())
+				oldReplicas = max(oldReplicas, int(ptr.Deref(deployment.Spec.Replicas, 0)))
+				deployment.Spec.Replicas = ptr.To[int32](0)
+				g.Expect(k8sClient.Update(ctx, deployment)).Should(gomega.Succeed())
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("await for no replicas", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, key, deployment)).To(gomega.Succeed())
+				g.Expect(deployment.Status.Replicas).To(gomega.Equal(int32(0)))
+				g.Expect(deployment.Status.TerminatingReplicas).To(gomega.BeNil())
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("clear the caBundle field", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, localQueueCRDKey, localQueueCRD)).Should(gomega.Succeed())
+				localQueueCRD.Spec.Conversion.Webhook.ClientConfig.CABundle = nil
+				g.Expect(k8sClient.Update(ctx, localQueueCRD)).Should(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("clear the caBundle fields for webhooks", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				mwc := &admissionregistrationv1.MutatingWebhookConfiguration{}
+				g.Expect(k8sClient.Get(ctx, mvcKey, mwc)).To(gomega.Succeed())
+				for _, webhook := range mwc.Webhooks {
+					webhook.ClientConfig.CABundle = nil
+				}
+				g.Expect(k8sClient.Update(ctx, mwc)).Should(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("scale back replicas", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, key, deployment)).To(gomega.Succeed())
+				deployment.Spec.Replicas = ptr.To(int32(oldReplicas))
+				g.Expect(k8sClient.Update(ctx, deployment)).Should(gomega.Succeed())
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("await for ready replicas", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, key, deployment)).To(gomega.Succeed())
+				g.Expect(deployment.Status.ReadyReplicas).To(gomega.Equal(int32(oldReplicas)))
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("await for Kueue to be available", func() {
+			util.WaitForKueueAvailability(ctx, k8sClient)
+		})
+
+		ginkgo.By("verify the caBundle is set again for CRD", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, localQueueCRDKey, localQueueCRD)).To(gomega.Succeed())
+				caBundle := localQueueCRD.Spec.Conversion.Webhook.ClientConfig.CABundle
+				g.Expect(caBundle).NotTo(gomega.BeEmpty())
```

  I think a copy should be fine because we are just asserting - no modification, wdyt?

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2025-11-25T15:28:27Z

- **test/e2e/singlecluster/certs_test.go:219** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-11-25T15:28:27Z
```diff
@@ -144,4 +151,101 @@ var _ = ginkgo.Describe("Kueue Certs", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("Should allow to bootstrap certificates", func() {
+		oldReplicas := 0
+		deployment := &appsv1.Deployment{}
+		localQueueCRD := &apiextensionsv1.CustomResourceDefinition{}
+		key := types.NamespacedName{Namespace: kueueNS, Name: "kueue-controller-manager"}
+		ginkgo.By("scale down replicas to 0", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, key, deployment)).To(gomega.Succeed())
+				oldReplicas = max(oldReplicas, int(ptr.Deref(deployment.Spec.Replicas, 0)))
+				deployment.Spec.Replicas = ptr.To[int32](0)
+				g.Expect(k8sClient.Update(ctx, deployment)).Should(gomega.Succeed())
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("await for no replicas", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, key, deployment)).To(gomega.Succeed())
+				g.Expect(deployment.Status.Replicas).To(gomega.Equal(int32(0)))
+				g.Expect(deployment.Status.TerminatingReplicas).To(gomega.BeNil())
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("clear the caBundle field", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, localQueueCRDKey, localQueueCRD)).Should(gomega.Succeed())
+				localQueueCRD.Spec.Conversion.Webhook.ClientConfig.CABundle = nil
+				g.Expect(k8sClient.Update(ctx, localQueueCRD)).Should(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("clear the caBundle fields for webhooks", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				mwc := &admissionregistrationv1.MutatingWebhookConfiguration{}
+				g.Expect(k8sClient.Get(ctx, mvcKey, mwc)).To(gomega.Succeed())
+				for _, webhook := range mwc.Webhooks {
+					webhook.ClientConfig.CABundle = nil
+				}
+				g.Expect(k8sClient.Update(ctx, mwc)).Should(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("scale back replicas", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, key, deployment)).To(gomega.Succeed())
+				deployment.Spec.Replicas = ptr.To(int32(oldReplicas))
+				g.Expect(k8sClient.Update(ctx, deployment)).Should(gomega.Succeed())
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("await for ready replicas", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, key, deployment)).To(gomega.Succeed())
+				g.Expect(deployment.Status.ReadyReplicas).To(gomega.Equal(int32(oldReplicas)))
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("await for Kueue to be available", func() {
+			util.WaitForKueueAvailability(ctx, k8sClient)
+		})
+
+		ginkgo.By("verify the caBundle is set again for CRD", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, localQueueCRDKey, localQueueCRD)).To(gomega.Succeed())
+				caBundle := localQueueCRD.Spec.Conversion.Webhook.ClientConfig.CABundle
+				g.Expect(caBundle).NotTo(gomega.BeEmpty())
```

  Yeah, that makes sense!

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (approved) — 2025-11-25T16:11:32Z

/lgtm
