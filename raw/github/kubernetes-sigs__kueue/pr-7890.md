# PR #7890: Document opt-in namespace management for jobs

**Summary**: Document opt-in namespace management for jobs

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7890

**Last updated**: 2025-12-11T08:06:11Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PannagaRao](https://github.com/PannagaRao)
- **Branch**: `doc-managedJobsNamespaceSelector` → `main`
- **Created**: 2025-11-25T17:06:42Z
- **Updated**: 2025-12-11T08:06:11Z
- **Merged**: 2025-12-11T08:05:32Z
- **Closed**: 2025-12-11T08:05:32Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `ok-to-test`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@kannon92](https://github.com/kannon92), [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 1   **+76 / -0**

## Description

Signed-off-by: Pannaga Rao Bhoja Ramamanohara

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation


#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:

Fixes # https://github.com/kubernetes-sigs/kueue/issues/7534

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
None
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-11-25T17:06:48Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c113f77b2020d423fa5b1c14a045909dad89dbc4 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6939c792a1d29d00088865c9 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-7890--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-7890--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTc4OTAtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.PZjYBP5MUiL1-_8Rq4Je67uSNHzYhigFiGzGZ4xJvkE)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-7890--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-25T17:06:52Z

Hi @PannagaRao. Thanks for your PR.

I'm waiting for a [github.com](https://kubernetes-sigs/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@kannon92](https://github.com/kannon92) — 2025-11-25T17:12:27Z

/ok-to-test

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-26T02:09:50Z

LGTM label has been added.  <details>Git tree hash: 2c9d56b7989603fe100d480059aac844d42b210e</details>

### Comment by [@kannon92](https://github.com/kannon92) — 2025-11-26T02:13:47Z

Idk why I can't view the preview for the site.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-02T15:19:38Z

LGTM label has been added.  <details>Git tree hash: 748978fc777f05afaf4d4f4bec91e947264d49fa</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-10T19:32:08Z

LGTM label has been added.  <details>Git tree hash: 19090189954270d9fd3e19b6b8c1a5c9acc6ee21</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-12-11T07:47:39Z

/approve
/cherrypick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-12-11T07:47:42Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `website` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7890#issuecomment-3640665562):

>/approve
>/cherrypick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-11T07:47:47Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7890#issuecomment-3640665562" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7890#" title="Author self-approved">PannagaRao</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-12-11T08:06:11Z

@mimowo: new pull request created: #8177

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7890#issuecomment-3640665562):

>/approve
>/cherrypick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (changes_requested) — 2025-11-25T17:12:15Z

- **site/content/en/docs/tasks/manage/enforce_job_management/opt_in_namespace_management.md:3** by [@kannon92](https://github.com/kannon92) — 2025-11-25T17:09:16Z
```diff
@@ -0,0 +1,120 @@
+---
+title: "Opt-in Namespace Management"
+date: 2024-12-28
```

  ```suggestion
  date: 2025-11-25
  ```

- **site/content/en/docs/tasks/manage/enforce_job_management/opt_in_namespace_management.md:11** by [@kannon92](https://github.com/kannon92) — 2025-11-25T17:09:53Z
```diff
@@ -0,0 +1,120 @@
+---
+title: "Opt-in Namespace Management"
+date: 2024-12-28
+weight: 15
+description: >
+  Configure Kueue to only manage workloads in explicitly labeled namespaces using the ManagedJobsNamespaceSelectorAlwaysRespected feature gate.
+---
+
+This page describes how to configure Kueue to enforce opt-in namespace management, ensuring that only workloads in explicitly labeled namespaces are reconciled by Kueue.
+
+## Overview
+
+The `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate controls whether the `managedJobsNamespaceSelector` restricts the reconciliation of all workloads, regardless of whether they have a `kueue.x-k8s.io/queue-name` label.
+
+When this feature gate is **disabled** (default behavior prior to v0.13):
+- If `manageJobsWithoutQueueName` is false, `managedJobsNamespaceSelector` has no effect: Kueue will manage exactly those instances of supported Kinds that have a `queue-name` label.
+- If `manageJobsWithoutQueueName` is true, then Kueue will (a) manage all instances of supported Kinds that have a `queue-name` label and (b) will manage all instances of supported Kinds that do not have a `queue-name` label if they are in namespaces that match `managedJobsNamespaceSelector`.
+
+When this feature gate is **enabled**:
+- The namespace selector check happens **first**, before any other reconciliation logic.
+- If a workload's namespace does not match the `managedJobsNamespaceSelector` (and the selector is not nil), the workload will not be reconciled by Kueue — regardless of whether it has a `queue-name` label or the value of `manageJobsWithoutQueueName`.
+- If a workload's namespace matches the selector (or if `managedJobsNamespaceSelector` is nil), normal reconciliation logic applies:
+  - If `manageJobsWithoutQueueName=false`: Kueue will manage exactly those instances of supported Kinds that have a `queue-name` label.
+  - If `manageJobsWithoutQueueName=true`: Kueue will manage all instances of supported Kinds with or without `queue-name` label.
+
+This change brings the `batch/v1/Job` integration into consistent alignment with `Pod`, `Deployment`, and `StatefulSet` integrations, which already do not act on resources in non-opted-in namespaces.
+
+## Feature Gate Status
+
+- **Alpha**: v0.13
+- **Beta**: v0.15
+
+## Before you begin
```

  This should be the first section.

- **site/content/en/docs/tasks/manage/enforce_job_management/opt_in_namespace_management.md:13** by [@kannon92](https://github.com/kannon92) — 2025-11-25T17:11:35Z
```diff
@@ -0,0 +1,120 @@
+---
+title: "Opt-in Namespace Management"
+date: 2024-12-28
+weight: 15
+description: >
+  Configure Kueue to only manage workloads in explicitly labeled namespaces using the ManagedJobsNamespaceSelectorAlwaysRespected feature gate.
+---
+
+This page describes how to configure Kueue to enforce opt-in namespace management, ensuring that only workloads in explicitly labeled namespaces are reconciled by Kueue.
+
+## Overview
+
+The `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate controls whether the `managedJobsNamespaceSelector` restricts the reconciliation of all workloads, regardless of whether they have a `kueue.x-k8s.io/queue-name` label.
```

  This doesn't really read like an overview. 
  
  Why should we use opt-in namespace management?
  
  I think the feature gate information can be farther down the doc.But I'd like the first thing to be why you should use opt-in namespace management and how to configure it.
  
  You could hyperlink to feature gate info but its distracting in the first prat of this doc.

- **site/content/en/docs/tasks/manage/enforce_job_management/opt_in_namespace_management.md:75** by [@kannon92](https://github.com/kannon92) — 2025-11-25T17:12:08Z
```diff
@@ -0,0 +1,120 @@
+---
+title: "Opt-in Namespace Management"
+date: 2024-12-28
+weight: 15
+description: >
+  Configure Kueue to only manage workloads in explicitly labeled namespaces using the ManagedJobsNamespaceSelectorAlwaysRespected feature gate.
+---
+
+This page describes how to configure Kueue to enforce opt-in namespace management, ensuring that only workloads in explicitly labeled namespaces are reconciled by Kueue.
+
+## Overview
+
+The `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate controls whether the `managedJobsNamespaceSelector` restricts the reconciliation of all workloads, regardless of whether they have a `kueue.x-k8s.io/queue-name` label.
+
+When this feature gate is **disabled** (default behavior prior to v0.13):
+- If `manageJobsWithoutQueueName` is false, `managedJobsNamespaceSelector` has no effect: Kueue will manage exactly those instances of supported Kinds that have a `queue-name` label.
+- If `manageJobsWithoutQueueName` is true, then Kueue will (a) manage all instances of supported Kinds that have a `queue-name` label and (b) will manage all instances of supported Kinds that do not have a `queue-name` label if they are in namespaces that match `managedJobsNamespaceSelector`.
+
+When this feature gate is **enabled**:
+- The namespace selector check happens **first**, before any other reconciliation logic.
+- If a workload's namespace does not match the `managedJobsNamespaceSelector` (and the selector is not nil), the workload will not be reconciled by Kueue — regardless of whether it has a `queue-name` label or the value of `manageJobsWithoutQueueName`.
+- If a workload's namespace matches the selector (or if `managedJobsNamespaceSelector` is nil), normal reconciliation logic applies:
+  - If `manageJobsWithoutQueueName=false`: Kueue will manage exactly those instances of supported Kinds that have a `queue-name` label.
+  - If `manageJobsWithoutQueueName=true`: Kueue will manage all instances of supported Kinds with or without `queue-name` label.
+
+This change brings the `batch/v1/Job` integration into consistent alignment with `Pod`, `Deployment`, and `StatefulSet` integrations, which already do not act on resources in non-opted-in namespaces.
+
+## Feature Gate Status
+
+- **Alpha**: v0.13
+- **Beta**: v0.15
+
+## Before you begin
+
+- Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+- Understand how to configure [`manageJobsWithoutQueueName`](/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name/).
+
+## Enabling the Feature Gate
+
+To enable this feature gate, you need to modify the Kueue manager configuration to include the feature gate flag:
+
+```yaml
+apiVersion: v1
+kind: ConfigMap
+metadata:
+  name: kueue-manager-config
+  namespace: kueue-system
+data:
+  controller_manager_config.yaml: |
+    apiVersion: config.kueue.x-k8s.io/v1beta1
+    kind: ControllerManagerConfig
+    featureGates:
+      ManagedJobsNamespaceSelectorAlwaysRespected: true
+```
+
+Or if using command-line flags:
+
+```yaml
+args:
+  - "--feature-gates=ManagedJobsNamespaceSelectorAlwaysRespected=true"
+```
+
+## Configuration
+
+Once the feature gate is enabled, you need to configure the `managedJobsNamespaceSelector` to specify which namespaces should be managed by Kueue.
+
+### Labeling Namespaces
+
+Namespaces that should be managed by Kueue must be labeled appropriately. For example, you can use the `managed-by-kueue` label:
+
+```bash
+kubectl label namespace my-namespace managed-by-kueue=true
+```
+
+### Configuring the Selector
```

  It would be great if you move this to the top of the doc.
  
  This section is the meet of this page imo.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2025-11-25T18:36:27Z

- **site/content/en/docs/tasks/manage/enforce_job_management/opt_in_namespace_management.md:13** by [@PannagaRao](https://github.com/PannagaRao) — 2025-11-25T18:36:27Z
```diff
@@ -0,0 +1,120 @@
+---
+title: "Opt-in Namespace Management"
+date: 2024-12-28
+weight: 15
+description: >
+  Configure Kueue to only manage workloads in explicitly labeled namespaces using the ManagedJobsNamespaceSelectorAlwaysRespected feature gate.
+---
+
+This page describes how to configure Kueue to enforce opt-in namespace management, ensuring that only workloads in explicitly labeled namespaces are reconciled by Kueue.
+
+## Overview
+
+The `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate controls whether the `managedJobsNamespaceSelector` restricts the reconciliation of all workloads, regardless of whether they have a `kueue.x-k8s.io/queue-name` label.
```

  I have added the section explaining the need of opt-in namespace in the first part and moved the feature details to the end. Please let me know your thoughts!

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-26T02:09:42Z

/lgtm

/assign @mimowo @tenzen-y

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-11-26T09:46:14Z

Thank you for working on this documentation.

- **site/content/en/docs/tasks/manage/enforce_job_management/opt_in_namespace_management.md:22** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-26T09:41:19Z
```diff
@@ -0,0 +1,107 @@
+---
+title: "Opt-in Namespace Management"
+date: 2025-11-25
+weight: 15
+description: >
+  Configure Kueue to only manage workloads in explicitly labeled namespaces using the ManagedJobsNamespaceSelectorAlwaysRespected feature gate.
+---
+
+This page describes how to configure Kueue to enforce opt-in namespace management, ensuring that only workloads in explicitly labeled namespaces are reconciled by Kueue.
+
+## Before you begin
+
+- Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+- Understand how to configure [`manageJobsWithoutQueueName`](/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name/).
+
+## Why Use Opt-in Namespace Management?
+
+Opt-in namespace management allows cluster administrators to enforce strict quota controls by ensuring that Kueue only manages workloads in explicitly designated namespaces. This provides several key benefits:
+
+- **Prevents quota bypass**: Users cannot bypass Kueue's quota system by manually adding `queue-name` labels to workloads in unmanaged namespaces. Only workloads in opted-in namespaces are ever reconciled by Kueue, regardless of how they are labeled.
+- **Explicit control**: Cluster administrators have full control over which namespaces are subject to Kueue management by simply labeling namespaces.
+- **Consistent behavior**: This brings `batch/v1/Job` integration into alignment with `Pod`, `Deployment`, and `StatefulSet` integrations, which already enforce namespace-based filtering.
```

  ```suggestion
  - **Consistent behavior**: This brings `batch/v1.Job` integration into alignment with `Pod`, `Deployment`, and `StatefulSet` integrations, which already enforce namespace-based filtering.
  ```
  
  It would be better to keep the consistency across documentation.

- **site/content/en/docs/tasks/manage/enforce_job_management/opt_in_namespace_management.md:50** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-26T09:43:34Z
```diff
@@ -0,0 +1,107 @@
+---
+title: "Opt-in Namespace Management"
+date: 2025-11-25
+weight: 15
+description: >
+  Configure Kueue to only manage workloads in explicitly labeled namespaces using the ManagedJobsNamespaceSelectorAlwaysRespected feature gate.
+---
+
+This page describes how to configure Kueue to enforce opt-in namespace management, ensuring that only workloads in explicitly labeled namespaces are reconciled by Kueue.
+
+## Before you begin
+
+- Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+- Understand how to configure [`manageJobsWithoutQueueName`](/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name/).
+
+## Why Use Opt-in Namespace Management?
+
+Opt-in namespace management allows cluster administrators to enforce strict quota controls by ensuring that Kueue only manages workloads in explicitly designated namespaces. This provides several key benefits:
+
+- **Prevents quota bypass**: Users cannot bypass Kueue's quota system by manually adding `queue-name` labels to workloads in unmanaged namespaces. Only workloads in opted-in namespaces are ever reconciled by Kueue, regardless of how they are labeled.
+- **Explicit control**: Cluster administrators have full control over which namespaces are subject to Kueue management by simply labeling namespaces.
+- **Consistent behavior**: This brings `batch/v1/Job` integration into alignment with `Pod`, `Deployment`, and `StatefulSet` integrations, which already enforce namespace-based filtering.
+
+## Configuration
+
+### Step 1: Label Namespaces
+
+Label the namespaces that should be managed by Kueue. For example, use the `managed-by-kueue` label:
+
+```bash
+kubectl label namespace my-namespace managed-by-kueue=true
+```
+
+### Step 2: Configure the Selector
+
+Configure the `managedJobsNamespaceSelector` in your Kueue Configuration to match the labeled namespaces. Use `matchLabels` to select namespaces that you have explicitly labeled:
+
+```yaml
+apiVersion: config.kueue.x-k8s.io/v1beta1
+kind: Configuration
+metadata:
+  name: config
+  namespace: kueue-system
+manageJobsWithoutQueueName: true
+managedJobsNamespaceSelector:
+  matchLabels:
+    managed-by-kueue: "true"
+```
+
+**Note**: When the `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate is enabled, only namespaces that match the selector will be reconciled. Namespaces without the required labels will not be managed by Kueue, regardless of whether workloads in those namespaces have a `queue-name` label.
```

  Is this needed?
  It looks like this is duplicated with step 3.

- **site/content/en/docs/tasks/manage/enforce_job_management/opt_in_namespace_management.md:54** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-26T09:44:05Z
```diff
@@ -0,0 +1,107 @@
+---
+title: "Opt-in Namespace Management"
+date: 2025-11-25
+weight: 15
+description: >
+  Configure Kueue to only manage workloads in explicitly labeled namespaces using the ManagedJobsNamespaceSelectorAlwaysRespected feature gate.
+---
+
+This page describes how to configure Kueue to enforce opt-in namespace management, ensuring that only workloads in explicitly labeled namespaces are reconciled by Kueue.
+
+## Before you begin
+
+- Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+- Understand how to configure [`manageJobsWithoutQueueName`](/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name/).
+
+## Why Use Opt-in Namespace Management?
+
+Opt-in namespace management allows cluster administrators to enforce strict quota controls by ensuring that Kueue only manages workloads in explicitly designated namespaces. This provides several key benefits:
+
+- **Prevents quota bypass**: Users cannot bypass Kueue's quota system by manually adding `queue-name` labels to workloads in unmanaged namespaces. Only workloads in opted-in namespaces are ever reconciled by Kueue, regardless of how they are labeled.
+- **Explicit control**: Cluster administrators have full control over which namespaces are subject to Kueue management by simply labeling namespaces.
+- **Consistent behavior**: This brings `batch/v1/Job` integration into alignment with `Pod`, `Deployment`, and `StatefulSet` integrations, which already enforce namespace-based filtering.
+
+## Configuration
+
+### Step 1: Label Namespaces
+
+Label the namespaces that should be managed by Kueue. For example, use the `managed-by-kueue` label:
+
+```bash
+kubectl label namespace my-namespace managed-by-kueue=true
+```
+
+### Step 2: Configure the Selector
+
+Configure the `managedJobsNamespaceSelector` in your Kueue Configuration to match the labeled namespaces. Use `matchLabels` to select namespaces that you have explicitly labeled:
+
+```yaml
+apiVersion: config.kueue.x-k8s.io/v1beta1
+kind: Configuration
+metadata:
+  name: config
+  namespace: kueue-system
+manageJobsWithoutQueueName: true
+managedJobsNamespaceSelector:
+  matchLabels:
+    managed-by-kueue: "true"
+```
+
+**Note**: When the `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate is enabled, only namespaces that match the selector will be reconciled. Namespaces without the required labels will not be managed by Kueue, regardless of whether workloads in those namespaces have a `queue-name` label.
+
+### Step 3: Enable the Feature Gate
+
+Enable the `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate in your Kueue manager configuration. See [Feature Gate Details](#feature-gate-details) for configuration instructions.
```

  Isn't this optional, IIUC?

- **site/content/en/docs/tasks/manage/enforce_job_management/opt_in_namespace_management.md:63** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-26T09:44:52Z
```diff
@@ -0,0 +1,107 @@
+---
+title: "Opt-in Namespace Management"
+date: 2025-11-25
+weight: 15
+description: >
+  Configure Kueue to only manage workloads in explicitly labeled namespaces using the ManagedJobsNamespaceSelectorAlwaysRespected feature gate.
+---
+
+This page describes how to configure Kueue to enforce opt-in namespace management, ensuring that only workloads in explicitly labeled namespaces are reconciled by Kueue.
+
+## Before you begin
+
+- Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+- Understand how to configure [`manageJobsWithoutQueueName`](/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name/).
+
+## Why Use Opt-in Namespace Management?
+
+Opt-in namespace management allows cluster administrators to enforce strict quota controls by ensuring that Kueue only manages workloads in explicitly designated namespaces. This provides several key benefits:
+
+- **Prevents quota bypass**: Users cannot bypass Kueue's quota system by manually adding `queue-name` labels to workloads in unmanaged namespaces. Only workloads in opted-in namespaces are ever reconciled by Kueue, regardless of how they are labeled.
+- **Explicit control**: Cluster administrators have full control over which namespaces are subject to Kueue management by simply labeling namespaces.
+- **Consistent behavior**: This brings `batch/v1/Job` integration into alignment with `Pod`, `Deployment`, and `StatefulSet` integrations, which already enforce namespace-based filtering.
+
+## Configuration
+
+### Step 1: Label Namespaces
+
+Label the namespaces that should be managed by Kueue. For example, use the `managed-by-kueue` label:
+
+```bash
+kubectl label namespace my-namespace managed-by-kueue=true
+```
+
+### Step 2: Configure the Selector
+
+Configure the `managedJobsNamespaceSelector` in your Kueue Configuration to match the labeled namespaces. Use `matchLabels` to select namespaces that you have explicitly labeled:
+
+```yaml
+apiVersion: config.kueue.x-k8s.io/v1beta1
+kind: Configuration
+metadata:
+  name: config
+  namespace: kueue-system
+manageJobsWithoutQueueName: true
+managedJobsNamespaceSelector:
+  matchLabels:
+    managed-by-kueue: "true"
+```
+
+**Note**: When the `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate is enabled, only namespaces that match the selector will be reconciled. Namespaces without the required labels will not be managed by Kueue, regardless of whether workloads in those namespaces have a `queue-name` label.
+
+### Step 3: Enable the Feature Gate
+
+Enable the `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate in your Kueue manager configuration. See [Feature Gate Details](#feature-gate-details) for configuration instructions.
+
+## Feature Gate Details {#feature-gate-details}
+
+The `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate controls whether the `managedJobsNamespaceSelector` restricts the reconciliation of all workloads, regardless of whether they have a `kueue.x-k8s.io/queue-name` label.
+
+### Feature Gate Status
+
+- **Alpha**: v0.13
+- **Beta**: v0.15
```

  Can you mention which features are described by these?

- **site/content/en/docs/tasks/manage/enforce_job_management/opt_in_namespace_management.md:88** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-26T09:45:45Z
```diff
@@ -0,0 +1,107 @@
+---
+title: "Opt-in Namespace Management"
+date: 2025-11-25
+weight: 15
+description: >
+  Configure Kueue to only manage workloads in explicitly labeled namespaces using the ManagedJobsNamespaceSelectorAlwaysRespected feature gate.
+---
+
+This page describes how to configure Kueue to enforce opt-in namespace management, ensuring that only workloads in explicitly labeled namespaces are reconciled by Kueue.
+
+## Before you begin
+
+- Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+- Understand how to configure [`manageJobsWithoutQueueName`](/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name/).
+
+## Why Use Opt-in Namespace Management?
+
+Opt-in namespace management allows cluster administrators to enforce strict quota controls by ensuring that Kueue only manages workloads in explicitly designated namespaces. This provides several key benefits:
+
+- **Prevents quota bypass**: Users cannot bypass Kueue's quota system by manually adding `queue-name` labels to workloads in unmanaged namespaces. Only workloads in opted-in namespaces are ever reconciled by Kueue, regardless of how they are labeled.
+- **Explicit control**: Cluster administrators have full control over which namespaces are subject to Kueue management by simply labeling namespaces.
+- **Consistent behavior**: This brings `batch/v1/Job` integration into alignment with `Pod`, `Deployment`, and `StatefulSet` integrations, which already enforce namespace-based filtering.
+
+## Configuration
+
+### Step 1: Label Namespaces
+
+Label the namespaces that should be managed by Kueue. For example, use the `managed-by-kueue` label:
+
+```bash
+kubectl label namespace my-namespace managed-by-kueue=true
+```
+
+### Step 2: Configure the Selector
+
+Configure the `managedJobsNamespaceSelector` in your Kueue Configuration to match the labeled namespaces. Use `matchLabels` to select namespaces that you have explicitly labeled:
+
+```yaml
+apiVersion: config.kueue.x-k8s.io/v1beta1
+kind: Configuration
+metadata:
+  name: config
+  namespace: kueue-system
+manageJobsWithoutQueueName: true
+managedJobsNamespaceSelector:
+  matchLabels:
+    managed-by-kueue: "true"
+```
+
+**Note**: When the `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate is enabled, only namespaces that match the selector will be reconciled. Namespaces without the required labels will not be managed by Kueue, regardless of whether workloads in those namespaces have a `queue-name` label.
+
+### Step 3: Enable the Feature Gate
+
+Enable the `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate in your Kueue manager configuration. See [Feature Gate Details](#feature-gate-details) for configuration instructions.
+
+## Feature Gate Details {#feature-gate-details}
+
+The `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate controls whether the `managedJobsNamespaceSelector` restricts the reconciliation of all workloads, regardless of whether they have a `kueue.x-k8s.io/queue-name` label.
+
+### Feature Gate Status
+
+- **Alpha**: v0.13
+- **Beta**: v0.15
+
+### Enabling the Feature Gate
+
+To enable this feature gate, modify the Kueue manager configuration to include the feature gate flag:
+
+```yaml
+apiVersion: v1
+kind: ConfigMap
+metadata:
+  name: kueue-manager-config
+  namespace: kueue-system
+data:
+  controller_manager_config.yaml: |
+    apiVersion: config.kueue.x-k8s.io/v1beta1
+    kind: ControllerManagerConfig
+    featureGates:
+      ManagedJobsNamespaceSelectorAlwaysRespected: true
+```
+
+Or if using command-line flags:
+
+```yaml
+args:
+  - "--feature-gates=ManagedJobsNamespaceSelectorAlwaysRespected=true"
+```
```

  ```suggestion
  ```
  
  We already mention how to enable feature gate in `Before you begin` section.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-26T17:32:05Z

- **site/content/en/docs/tasks/manage/enforce_job_management/opt_in_namespace_management.md:6** by [@mimowo](https://github.com/mimowo) — 2025-11-26T17:32:05Z
```diff
@@ -0,0 +1,107 @@
+---
+title: "Opt-in Namespace Management"
+date: 2025-11-25
+weight: 15
+description: >
+  Configure Kueue to only manage workloads in explicitly labeled namespaces using the ManagedJobsNamespaceSelectorAlwaysRespected feature gate.
```

  ```suggestion
    Configure Kueue to only manage workloads in explicitly labeled namespaces.
  ```
  No need to mention the FG in the title

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-26T17:36:16Z

- **site/content/en/docs/tasks/manage/enforce_job_management/opt_in_namespace_management.md:65** by [@mimowo](https://github.com/mimowo) — 2025-11-26T17:36:16Z
```diff
@@ -0,0 +1,107 @@
+---
+title: "Opt-in Namespace Management"
+date: 2025-11-25
+weight: 15
+description: >
+  Configure Kueue to only manage workloads in explicitly labeled namespaces using the ManagedJobsNamespaceSelectorAlwaysRespected feature gate.
+---
+
+This page describes how to configure Kueue to enforce opt-in namespace management, ensuring that only workloads in explicitly labeled namespaces are reconciled by Kueue.
+
+## Before you begin
+
+- Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+- Understand how to configure [`manageJobsWithoutQueueName`](/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name/).
+
+## Why Use Opt-in Namespace Management?
+
+Opt-in namespace management allows cluster administrators to enforce strict quota controls by ensuring that Kueue only manages workloads in explicitly designated namespaces. This provides several key benefits:
+
+- **Prevents quota bypass**: Users cannot bypass Kueue's quota system by manually adding `queue-name` labels to workloads in unmanaged namespaces. Only workloads in opted-in namespaces are ever reconciled by Kueue, regardless of how they are labeled.
+- **Explicit control**: Cluster administrators have full control over which namespaces are subject to Kueue management by simply labeling namespaces.
+- **Consistent behavior**: This brings `batch/v1/Job` integration into alignment with `Pod`, `Deployment`, and `StatefulSet` integrations, which already enforce namespace-based filtering.
+
+## Configuration
+
+### Step 1: Label Namespaces
+
+Label the namespaces that should be managed by Kueue. For example, use the `managed-by-kueue` label:
+
+```bash
+kubectl label namespace my-namespace managed-by-kueue=true
+```
+
+### Step 2: Configure the Selector
+
+Configure the `managedJobsNamespaceSelector` in your Kueue Configuration to match the labeled namespaces. Use `matchLabels` to select namespaces that you have explicitly labeled:
+
+```yaml
+apiVersion: config.kueue.x-k8s.io/v1beta1
+kind: Configuration
+metadata:
+  name: config
+  namespace: kueue-system
+manageJobsWithoutQueueName: true
+managedJobsNamespaceSelector:
+  matchLabels:
+    managed-by-kueue: "true"
+```
+
+**Note**: When the `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate is enabled, only namespaces that match the selector will be reconciled. Namespaces without the required labels will not be managed by Kueue, regardless of whether workloads in those namespaces have a `queue-name` label.
+
+### Step 3: Enable the Feature Gate
+
+Enable the `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate in your Kueue manager configuration. See [Feature Gate Details](#feature-gate-details) for configuration instructions.
+
+## Feature Gate Details {#feature-gate-details}
+
+The `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate controls whether the `managedJobsNamespaceSelector` restricts the reconciliation of all workloads, regardless of whether they have a `kueue.x-k8s.io/queue-name` label.
+
+### Feature Gate Status
+
+- **Alpha**: v0.13
+- **Beta**: v0.15
+
+### Enabling the Feature Gate
```

  Instead of this section please refer to the generic section https://kueue.sigs.k8s.io/docs/installation/#change-the-feature-gates-configuration

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2025-11-26T18:07:55Z

- **site/content/en/docs/tasks/manage/enforce_job_management/opt_in_namespace_management.md:50** by [@PannagaRao](https://github.com/PannagaRao) — 2025-11-26T18:07:55Z
```diff
@@ -0,0 +1,107 @@
+---
+title: "Opt-in Namespace Management"
+date: 2025-11-25
+weight: 15
+description: >
+  Configure Kueue to only manage workloads in explicitly labeled namespaces using the ManagedJobsNamespaceSelectorAlwaysRespected feature gate.
+---
+
+This page describes how to configure Kueue to enforce opt-in namespace management, ensuring that only workloads in explicitly labeled namespaces are reconciled by Kueue.
+
+## Before you begin
+
+- Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+- Understand how to configure [`manageJobsWithoutQueueName`](/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name/).
+
+## Why Use Opt-in Namespace Management?
+
+Opt-in namespace management allows cluster administrators to enforce strict quota controls by ensuring that Kueue only manages workloads in explicitly designated namespaces. This provides several key benefits:
+
+- **Prevents quota bypass**: Users cannot bypass Kueue's quota system by manually adding `queue-name` labels to workloads in unmanaged namespaces. Only workloads in opted-in namespaces are ever reconciled by Kueue, regardless of how they are labeled.
+- **Explicit control**: Cluster administrators have full control over which namespaces are subject to Kueue management by simply labeling namespaces.
+- **Consistent behavior**: This brings `batch/v1/Job` integration into alignment with `Pod`, `Deployment`, and `StatefulSet` integrations, which already enforce namespace-based filtering.
+
+## Configuration
+
+### Step 1: Label Namespaces
+
+Label the namespaces that should be managed by Kueue. For example, use the `managed-by-kueue` label:
+
+```bash
+kubectl label namespace my-namespace managed-by-kueue=true
+```
+
+### Step 2: Configure the Selector
+
+Configure the `managedJobsNamespaceSelector` in your Kueue Configuration to match the labeled namespaces. Use `matchLabels` to select namespaces that you have explicitly labeled:
+
+```yaml
+apiVersion: config.kueue.x-k8s.io/v1beta1
+kind: Configuration
+metadata:
+  name: config
+  namespace: kueue-system
+manageJobsWithoutQueueName: true
+managedJobsNamespaceSelector:
+  matchLabels:
+    managed-by-kueue: "true"
+```
+
+**Note**: When the `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate is enabled, only namespaces that match the selector will be reconciled. Namespaces without the required labels will not be managed by Kueue, regardless of whether workloads in those namespaces have a `queue-name` label.
```

  You are right. Will remove the Note

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2025-11-26T18:13:06Z

- **site/content/en/docs/tasks/manage/enforce_job_management/opt_in_namespace_management.md:65** by [@PannagaRao](https://github.com/PannagaRao) — 2025-11-26T18:13:05Z
```diff
@@ -0,0 +1,107 @@
+---
+title: "Opt-in Namespace Management"
+date: 2025-11-25
+weight: 15
+description: >
+  Configure Kueue to only manage workloads in explicitly labeled namespaces using the ManagedJobsNamespaceSelectorAlwaysRespected feature gate.
+---
+
+This page describes how to configure Kueue to enforce opt-in namespace management, ensuring that only workloads in explicitly labeled namespaces are reconciled by Kueue.
+
+## Before you begin
+
+- Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+- Understand how to configure [`manageJobsWithoutQueueName`](/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name/).
+
+## Why Use Opt-in Namespace Management?
+
+Opt-in namespace management allows cluster administrators to enforce strict quota controls by ensuring that Kueue only manages workloads in explicitly designated namespaces. This provides several key benefits:
+
+- **Prevents quota bypass**: Users cannot bypass Kueue's quota system by manually adding `queue-name` labels to workloads in unmanaged namespaces. Only workloads in opted-in namespaces are ever reconciled by Kueue, regardless of how they are labeled.
+- **Explicit control**: Cluster administrators have full control over which namespaces are subject to Kueue management by simply labeling namespaces.
+- **Consistent behavior**: This brings `batch/v1/Job` integration into alignment with `Pod`, `Deployment`, and `StatefulSet` integrations, which already enforce namespace-based filtering.
+
+## Configuration
+
+### Step 1: Label Namespaces
+
+Label the namespaces that should be managed by Kueue. For example, use the `managed-by-kueue` label:
+
+```bash
+kubectl label namespace my-namespace managed-by-kueue=true
+```
+
+### Step 2: Configure the Selector
+
+Configure the `managedJobsNamespaceSelector` in your Kueue Configuration to match the labeled namespaces. Use `matchLabels` to select namespaces that you have explicitly labeled:
+
+```yaml
+apiVersion: config.kueue.x-k8s.io/v1beta1
+kind: Configuration
+metadata:
+  name: config
+  namespace: kueue-system
+manageJobsWithoutQueueName: true
+managedJobsNamespaceSelector:
+  matchLabels:
+    managed-by-kueue: "true"
+```
+
+**Note**: When the `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate is enabled, only namespaces that match the selector will be reconciled. Namespaces without the required labels will not be managed by Kueue, regardless of whether workloads in those namespaces have a `queue-name` label.
+
+### Step 3: Enable the Feature Gate
+
+Enable the `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate in your Kueue manager configuration. See [Feature Gate Details](#feature-gate-details) for configuration instructions.
+
+## Feature Gate Details {#feature-gate-details}
+
+The `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate controls whether the `managedJobsNamespaceSelector` restricts the reconciliation of all workloads, regardless of whether they have a `kueue.x-k8s.io/queue-name` label.
+
+### Feature Gate Status
+
+- **Alpha**: v0.13
+- **Beta**: v0.15
+
+### Enabling the Feature Gate
```

  Are you suggesting we remove showing an example of hoe to enable this specific feature gate and just add the reference?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-11-26T18:32:29Z

- **site/content/en/docs/tasks/manage/enforce_job_management/opt_in_namespace_management.md:65** by [@kannon92](https://github.com/kannon92) — 2025-11-26T18:32:29Z
```diff
@@ -0,0 +1,107 @@
+---
+title: "Opt-in Namespace Management"
+date: 2025-11-25
+weight: 15
+description: >
+  Configure Kueue to only manage workloads in explicitly labeled namespaces using the ManagedJobsNamespaceSelectorAlwaysRespected feature gate.
+---
+
+This page describes how to configure Kueue to enforce opt-in namespace management, ensuring that only workloads in explicitly labeled namespaces are reconciled by Kueue.
+
+## Before you begin
+
+- Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+- Understand how to configure [`manageJobsWithoutQueueName`](/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name/).
+
+## Why Use Opt-in Namespace Management?
+
+Opt-in namespace management allows cluster administrators to enforce strict quota controls by ensuring that Kueue only manages workloads in explicitly designated namespaces. This provides several key benefits:
+
+- **Prevents quota bypass**: Users cannot bypass Kueue's quota system by manually adding `queue-name` labels to workloads in unmanaged namespaces. Only workloads in opted-in namespaces are ever reconciled by Kueue, regardless of how they are labeled.
+- **Explicit control**: Cluster administrators have full control over which namespaces are subject to Kueue management by simply labeling namespaces.
+- **Consistent behavior**: This brings `batch/v1/Job` integration into alignment with `Pod`, `Deployment`, and `StatefulSet` integrations, which already enforce namespace-based filtering.
+
+## Configuration
+
+### Step 1: Label Namespaces
+
+Label the namespaces that should be managed by Kueue. For example, use the `managed-by-kueue` label:
+
+```bash
+kubectl label namespace my-namespace managed-by-kueue=true
+```
+
+### Step 2: Configure the Selector
+
+Configure the `managedJobsNamespaceSelector` in your Kueue Configuration to match the labeled namespaces. Use `matchLabels` to select namespaces that you have explicitly labeled:
+
+```yaml
+apiVersion: config.kueue.x-k8s.io/v1beta1
+kind: Configuration
+metadata:
+  name: config
+  namespace: kueue-system
+manageJobsWithoutQueueName: true
+managedJobsNamespaceSelector:
+  matchLabels:
+    managed-by-kueue: "true"
+```
+
+**Note**: When the `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate is enabled, only namespaces that match the selector will be reconciled. Namespaces without the required labels will not be managed by Kueue, regardless of whether workloads in those namespaces have a `queue-name` label.
+
+### Step 3: Enable the Feature Gate
+
+Enable the `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate in your Kueue manager configuration. See [Feature Gate Details](#feature-gate-details) for configuration instructions.
+
+## Feature Gate Details {#feature-gate-details}
+
+The `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate controls whether the `managedJobsNamespaceSelector` restricts the reconciliation of all workloads, regardless of whether they have a `kueue.x-k8s.io/queue-name` label.
+
+### Feature Gate Status
+
+- **Alpha**: v0.13
+- **Beta**: v0.15
+
+### Enabling the Feature Gate
```

  Something like https://kueue.sigs.k8s.io/docs/concepts/cluster_queue/#lendinglimit.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-28T14:14:06Z

- **site/content/en/docs/tasks/manage/enforce_job_management/opt_in_namespace_management.md:54** by [@mimowo](https://github.com/mimowo) — 2025-11-28T14:14:05Z
```diff
@@ -0,0 +1,107 @@
+---
+title: "Opt-in Namespace Management"
+date: 2025-11-25
+weight: 15
+description: >
+  Configure Kueue to only manage workloads in explicitly labeled namespaces using the ManagedJobsNamespaceSelectorAlwaysRespected feature gate.
+---
+
+This page describes how to configure Kueue to enforce opt-in namespace management, ensuring that only workloads in explicitly labeled namespaces are reconciled by Kueue.
+
+## Before you begin
+
+- Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+- Understand how to configure [`manageJobsWithoutQueueName`](/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name/).
+
+## Why Use Opt-in Namespace Management?
+
+Opt-in namespace management allows cluster administrators to enforce strict quota controls by ensuring that Kueue only manages workloads in explicitly designated namespaces. This provides several key benefits:
+
+- **Prevents quota bypass**: Users cannot bypass Kueue's quota system by manually adding `queue-name` labels to workloads in unmanaged namespaces. Only workloads in opted-in namespaces are ever reconciled by Kueue, regardless of how they are labeled.
+- **Explicit control**: Cluster administrators have full control over which namespaces are subject to Kueue management by simply labeling namespaces.
+- **Consistent behavior**: This brings `batch/v1/Job` integration into alignment with `Pod`, `Deployment`, and `StatefulSet` integrations, which already enforce namespace-based filtering.
+
+## Configuration
+
+### Step 1: Label Namespaces
+
+Label the namespaces that should be managed by Kueue. For example, use the `managed-by-kueue` label:
+
+```bash
+kubectl label namespace my-namespace managed-by-kueue=true
+```
+
+### Step 2: Configure the Selector
+
+Configure the `managedJobsNamespaceSelector` in your Kueue Configuration to match the labeled namespaces. Use `matchLabels` to select namespaces that you have explicitly labeled:
+
+```yaml
+apiVersion: config.kueue.x-k8s.io/v1beta1
+kind: Configuration
+metadata:
+  name: config
+  namespace: kueue-system
+manageJobsWithoutQueueName: true
+managedJobsNamespaceSelector:
+  matchLabels:
+    managed-by-kueue: "true"
+```
+
+**Note**: When the `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate is enabled, only namespaces that match the selector will be reconciled. Namespaces without the required labels will not be managed by Kueue, regardless of whether workloads in those namespaces have a `queue-name` label.
+
+### Step 3: Enable the Feature Gate
+
+Enable the `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate in your Kueue manager configuration. See [Feature Gate Details](#feature-gate-details) for configuration instructions.
```

  True since we now released 0.15 it is not really necessary, as it is enabled by default. We may still mention it needs to be done on older versions.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2025-12-01T19:24:01Z

- **site/content/en/docs/tasks/manage/enforce_job_management/opt_in_namespace_management.md:54** by [@PannagaRao](https://github.com/PannagaRao) — 2025-12-01T19:24:01Z
```diff
@@ -0,0 +1,107 @@
+---
+title: "Opt-in Namespace Management"
+date: 2025-11-25
+weight: 15
+description: >
+  Configure Kueue to only manage workloads in explicitly labeled namespaces using the ManagedJobsNamespaceSelectorAlwaysRespected feature gate.
+---
+
+This page describes how to configure Kueue to enforce opt-in namespace management, ensuring that only workloads in explicitly labeled namespaces are reconciled by Kueue.
+
+## Before you begin
+
+- Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+- Understand how to configure [`manageJobsWithoutQueueName`](/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name/).
+
+## Why Use Opt-in Namespace Management?
+
+Opt-in namespace management allows cluster administrators to enforce strict quota controls by ensuring that Kueue only manages workloads in explicitly designated namespaces. This provides several key benefits:
+
+- **Prevents quota bypass**: Users cannot bypass Kueue's quota system by manually adding `queue-name` labels to workloads in unmanaged namespaces. Only workloads in opted-in namespaces are ever reconciled by Kueue, regardless of how they are labeled.
+- **Explicit control**: Cluster administrators have full control over which namespaces are subject to Kueue management by simply labeling namespaces.
+- **Consistent behavior**: This brings `batch/v1/Job` integration into alignment with `Pod`, `Deployment`, and `StatefulSet` integrations, which already enforce namespace-based filtering.
+
+## Configuration
+
+### Step 1: Label Namespaces
+
+Label the namespaces that should be managed by Kueue. For example, use the `managed-by-kueue` label:
+
+```bash
+kubectl label namespace my-namespace managed-by-kueue=true
+```
+
+### Step 2: Configure the Selector
+
+Configure the `managedJobsNamespaceSelector` in your Kueue Configuration to match the labeled namespaces. Use `matchLabels` to select namespaces that you have explicitly labeled:
+
+```yaml
+apiVersion: config.kueue.x-k8s.io/v1beta1
+kind: Configuration
+metadata:
+  name: config
+  namespace: kueue-system
+manageJobsWithoutQueueName: true
+managedJobsNamespaceSelector:
+  matchLabels:
+    managed-by-kueue: "true"
+```
+
+**Note**: When the `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate is enabled, only namespaces that match the selector will be reconciled. Namespaces without the required labels will not be managed by Kueue, regardless of whether workloads in those namespaces have a `queue-name` label.
+
+### Step 3: Enable the Feature Gate
+
+Enable the `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate in your Kueue manager configuration. See [Feature Gate Details](#feature-gate-details) for configuration instructions.
```

  I have added that enabling the feature gate is optional for 0.15+ and needed for older versions. Please let me know if it looks good.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2025-12-01T19:24:11Z

- **site/content/en/docs/tasks/manage/enforce_job_management/opt_in_namespace_management.md:65** by [@PannagaRao](https://github.com/PannagaRao) — 2025-12-01T19:24:11Z
```diff
@@ -0,0 +1,107 @@
+---
+title: "Opt-in Namespace Management"
+date: 2025-11-25
+weight: 15
+description: >
+  Configure Kueue to only manage workloads in explicitly labeled namespaces using the ManagedJobsNamespaceSelectorAlwaysRespected feature gate.
+---
+
+This page describes how to configure Kueue to enforce opt-in namespace management, ensuring that only workloads in explicitly labeled namespaces are reconciled by Kueue.
+
+## Before you begin
+
+- Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+- Understand how to configure [`manageJobsWithoutQueueName`](/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name/).
+
+## Why Use Opt-in Namespace Management?
+
+Opt-in namespace management allows cluster administrators to enforce strict quota controls by ensuring that Kueue only manages workloads in explicitly designated namespaces. This provides several key benefits:
+
+- **Prevents quota bypass**: Users cannot bypass Kueue's quota system by manually adding `queue-name` labels to workloads in unmanaged namespaces. Only workloads in opted-in namespaces are ever reconciled by Kueue, regardless of how they are labeled.
+- **Explicit control**: Cluster administrators have full control over which namespaces are subject to Kueue management by simply labeling namespaces.
+- **Consistent behavior**: This brings `batch/v1/Job` integration into alignment with `Pod`, `Deployment`, and `StatefulSet` integrations, which already enforce namespace-based filtering.
+
+## Configuration
+
+### Step 1: Label Namespaces
+
+Label the namespaces that should be managed by Kueue. For example, use the `managed-by-kueue` label:
+
+```bash
+kubectl label namespace my-namespace managed-by-kueue=true
+```
+
+### Step 2: Configure the Selector
+
+Configure the `managedJobsNamespaceSelector` in your Kueue Configuration to match the labeled namespaces. Use `matchLabels` to select namespaces that you have explicitly labeled:
+
+```yaml
+apiVersion: config.kueue.x-k8s.io/v1beta1
+kind: Configuration
+metadata:
+  name: config
+  namespace: kueue-system
+manageJobsWithoutQueueName: true
+managedJobsNamespaceSelector:
+  matchLabels:
+    managed-by-kueue: "true"
+```
+
+**Note**: When the `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate is enabled, only namespaces that match the selector will be reconciled. Namespaces without the required labels will not be managed by Kueue, regardless of whether workloads in those namespaces have a `queue-name` label.
+
+### Step 3: Enable the Feature Gate
+
+Enable the `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate in your Kueue manager configuration. See [Feature Gate Details](#feature-gate-details) for configuration instructions.
+
+## Feature Gate Details {#feature-gate-details}
+
+The `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate controls whether the `managedJobsNamespaceSelector` restricts the reconciliation of all workloads, regardless of whether they have a `kueue.x-k8s.io/queue-name` label.
+
+### Feature Gate Status
+
+- **Alpha**: v0.13
+- **Beta**: v0.15
+
+### Enabling the Feature Gate
```

  Done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-12-02T15:19:31Z

/lgtm

/assign @tenzen-y @mimowo

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-02T15:23:09Z

- **site/content/en/docs/tasks/manage/enforce_job_management/opt_in_namespace_management.md:64** by [@mimowo](https://github.com/mimowo) — 2025-12-02T15:23:09Z
```diff
@@ -0,0 +1,83 @@
+---
+title: "Opt-in Namespace Management"
+date: 2025-11-25
+weight: 15
+description: >
+  Configure Kueue to only manage workloads in explicitly labeled namespaces.
+---
+
+This page describes how to configure Kueue to enforce opt-in namespace management, ensuring that only workloads in explicitly labeled namespaces are reconciled by Kueue.
+
+## Before you begin
+
+- Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+- Understand how to configure [`manageJobsWithoutQueueName`](/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name/).
+
+## Why Use Opt-in Namespace Management?
+
+Opt-in namespace management allows cluster administrators to enforce strict quota controls by ensuring that Kueue only manages workloads in explicitly designated namespaces. This provides several key benefits:
+
+- **Prevents quota bypass**: Users cannot bypass Kueue's quota system by manually adding `queue-name` labels to workloads in unmanaged namespaces. Only workloads in opted-in namespaces are ever reconciled by Kueue, regardless of how they are labeled.
+- **Explicit control**: Cluster administrators have full control over which namespaces are subject to Kueue management by simply labeling namespaces.
+- **Consistent behavior**: This brings `batch/v1.Job` integration into alignment with `Pod`, `Deployment`, and `StatefulSet` integrations, which already enforce namespace-based filtering.
+
+## Configuration
+
+### Step 1: Label Namespaces
+
+Label the namespaces that should be managed by Kueue. For example, use the `managed-by-kueue` label:
+
+```bash
+kubectl label namespace my-namespace managed-by-kueue=true
+```
+
+### Step 2: Configure the Selector
+
+Configure the `managedJobsNamespaceSelector` in your Kueue Configuration to match the labeled namespaces. Use `matchLabels` to select namespaces that you have explicitly labeled:
+
+```yaml
+apiVersion: config.kueue.x-k8s.io/v1beta1
+kind: Configuration
+metadata:
+  name: config
+  namespace: kueue-system
+manageJobsWithoutQueueName: true
+managedJobsNamespaceSelector:
+  matchLabels:
+    managed-by-kueue: "true"
+```
+
+### Step 3: Enable the Feature Gate
+
+This step is optional if you're using Kueue v0.15 or later, as the `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate is enabled by default. If you're using an older version of Kueue, you need to explicitly enable this feature gate. See [Feature Gate Details](#feature-gate-details) for configuration instructions.
+
+## Feature Gate Details {#feature-gate-details}
+
+The `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate controls whether the `managedJobsNamespaceSelector` restricts the reconciliation of all workloads, regardless of whether they have a `kueue.x-k8s.io/queue-name` label.
+
+### Feature Gate Status
+
+The `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate was introduced as **Alpha** in Kueue v0.13 and graduated to **Beta** in v0.15.
+
+### Enabling the Feature Gate
+
+Check the [Installation guide](/docs/installation/#change-the-feature-gates-configuration) for details on feature gate configuration.
```

  U think this is just unnecessary, as it repeats the informtion from https://kueue.sigs.k8s.io/docs/installation/#feature-gates-for-alpha-and-beta-features

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-02T15:23:37Z

- **site/content/en/docs/tasks/manage/enforce_job_management/opt_in_namespace_management.md:52** by [@mimowo](https://github.com/mimowo) — 2025-12-02T15:23:37Z
```diff
@@ -0,0 +1,83 @@
+---
+title: "Opt-in Namespace Management"
+date: 2025-11-25
+weight: 15
+description: >
+  Configure Kueue to only manage workloads in explicitly labeled namespaces.
+---
+
+This page describes how to configure Kueue to enforce opt-in namespace management, ensuring that only workloads in explicitly labeled namespaces are reconciled by Kueue.
+
+## Before you begin
+
+- Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+- Understand how to configure [`manageJobsWithoutQueueName`](/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name/).
+
+## Why Use Opt-in Namespace Management?
+
+Opt-in namespace management allows cluster administrators to enforce strict quota controls by ensuring that Kueue only manages workloads in explicitly designated namespaces. This provides several key benefits:
+
+- **Prevents quota bypass**: Users cannot bypass Kueue's quota system by manually adding `queue-name` labels to workloads in unmanaged namespaces. Only workloads in opted-in namespaces are ever reconciled by Kueue, regardless of how they are labeled.
+- **Explicit control**: Cluster administrators have full control over which namespaces are subject to Kueue management by simply labeling namespaces.
+- **Consistent behavior**: This brings `batch/v1.Job` integration into alignment with `Pod`, `Deployment`, and `StatefulSet` integrations, which already enforce namespace-based filtering.
+
+## Configuration
+
+### Step 1: Label Namespaces
+
+Label the namespaces that should be managed by Kueue. For example, use the `managed-by-kueue` label:
+
+```bash
+kubectl label namespace my-namespace managed-by-kueue=true
+```
+
+### Step 2: Configure the Selector
+
+Configure the `managedJobsNamespaceSelector` in your Kueue Configuration to match the labeled namespaces. Use `matchLabels` to select namespaces that you have explicitly labeled:
+
+```yaml
+apiVersion: config.kueue.x-k8s.io/v1beta1
+kind: Configuration
+metadata:
+  name: config
+  namespace: kueue-system
+manageJobsWithoutQueueName: true
+managedJobsNamespaceSelector:
+  matchLabels:
+    managed-by-kueue: "true"
+```
+
+### Step 3: Enable the Feature Gate
+
+This step is optional if you're using Kueue v0.15 or later, as the `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate is enabled by default. If you're using an older version of Kueue, you need to explicitly enable this feature gate. See [Feature Gate Details](#feature-gate-details) for configuration instructions.
```

  Let's reference https://kueue.sigs.k8s.io/docs/installation/#feature-gates-for-alpha-and-beta-features instead, and make sure the history for the feature gate is there.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-02T15:24:15Z

- **site/content/en/docs/tasks/manage/enforce_job_management/opt_in_namespace_management.md:66** by [@mimowo](https://github.com/mimowo) — 2025-12-02T15:24:16Z
```diff
@@ -0,0 +1,83 @@
+---
+title: "Opt-in Namespace Management"
+date: 2025-11-25
+weight: 15
+description: >
+  Configure Kueue to only manage workloads in explicitly labeled namespaces.
+---
+
+This page describes how to configure Kueue to enforce opt-in namespace management, ensuring that only workloads in explicitly labeled namespaces are reconciled by Kueue.
+
+## Before you begin
+
+- Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+- Understand how to configure [`manageJobsWithoutQueueName`](/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name/).
+
+## Why Use Opt-in Namespace Management?
+
+Opt-in namespace management allows cluster administrators to enforce strict quota controls by ensuring that Kueue only manages workloads in explicitly designated namespaces. This provides several key benefits:
+
+- **Prevents quota bypass**: Users cannot bypass Kueue's quota system by manually adding `queue-name` labels to workloads in unmanaged namespaces. Only workloads in opted-in namespaces are ever reconciled by Kueue, regardless of how they are labeled.
+- **Explicit control**: Cluster administrators have full control over which namespaces are subject to Kueue management by simply labeling namespaces.
+- **Consistent behavior**: This brings `batch/v1.Job` integration into alignment with `Pod`, `Deployment`, and `StatefulSet` integrations, which already enforce namespace-based filtering.
+
+## Configuration
+
+### Step 1: Label Namespaces
+
+Label the namespaces that should be managed by Kueue. For example, use the `managed-by-kueue` label:
+
+```bash
+kubectl label namespace my-namespace managed-by-kueue=true
+```
+
+### Step 2: Configure the Selector
+
+Configure the `managedJobsNamespaceSelector` in your Kueue Configuration to match the labeled namespaces. Use `matchLabels` to select namespaces that you have explicitly labeled:
+
+```yaml
+apiVersion: config.kueue.x-k8s.io/v1beta1
+kind: Configuration
+metadata:
+  name: config
+  namespace: kueue-system
+manageJobsWithoutQueueName: true
+managedJobsNamespaceSelector:
+  matchLabels:
+    managed-by-kueue: "true"
+```
+
+### Step 3: Enable the Feature Gate
+
+This step is optional if you're using Kueue v0.15 or later, as the `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate is enabled by default. If you're using an older version of Kueue, you need to explicitly enable this feature gate. See [Feature Gate Details](#feature-gate-details) for configuration instructions.
+
+## Feature Gate Details {#feature-gate-details}
+
+The `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate controls whether the `managedJobsNamespaceSelector` restricts the reconciliation of all workloads, regardless of whether they have a `kueue.x-k8s.io/queue-name` label.
+
+### Feature Gate Status
+
+The `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate was introduced as **Alpha** in Kueue v0.13 and graduated to **Beta** in v0.15.
+
+### Enabling the Feature Gate
+
+Check the [Installation guide](/docs/installation/#change-the-feature-gates-configuration) for details on feature gate configuration.
+
+### Behavior When Enabled
```

  ```suggestion
  ### How it works
  ```

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2025-12-03T19:36:16Z

- **site/content/en/docs/tasks/manage/enforce_job_management/opt_in_namespace_management.md:64** by [@PannagaRao](https://github.com/PannagaRao) — 2025-12-03T19:36:16Z
```diff
@@ -0,0 +1,83 @@
+---
+title: "Opt-in Namespace Management"
+date: 2025-11-25
+weight: 15
+description: >
+  Configure Kueue to only manage workloads in explicitly labeled namespaces.
+---
+
+This page describes how to configure Kueue to enforce opt-in namespace management, ensuring that only workloads in explicitly labeled namespaces are reconciled by Kueue.
+
+## Before you begin
+
+- Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+- Understand how to configure [`manageJobsWithoutQueueName`](/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name/).
+
+## Why Use Opt-in Namespace Management?
+
+Opt-in namespace management allows cluster administrators to enforce strict quota controls by ensuring that Kueue only manages workloads in explicitly designated namespaces. This provides several key benefits:
+
+- **Prevents quota bypass**: Users cannot bypass Kueue's quota system by manually adding `queue-name` labels to workloads in unmanaged namespaces. Only workloads in opted-in namespaces are ever reconciled by Kueue, regardless of how they are labeled.
+- **Explicit control**: Cluster administrators have full control over which namespaces are subject to Kueue management by simply labeling namespaces.
+- **Consistent behavior**: This brings `batch/v1.Job` integration into alignment with `Pod`, `Deployment`, and `StatefulSet` integrations, which already enforce namespace-based filtering.
+
+## Configuration
+
+### Step 1: Label Namespaces
+
+Label the namespaces that should be managed by Kueue. For example, use the `managed-by-kueue` label:
+
+```bash
+kubectl label namespace my-namespace managed-by-kueue=true
+```
+
+### Step 2: Configure the Selector
+
+Configure the `managedJobsNamespaceSelector` in your Kueue Configuration to match the labeled namespaces. Use `matchLabels` to select namespaces that you have explicitly labeled:
+
+```yaml
+apiVersion: config.kueue.x-k8s.io/v1beta1
+kind: Configuration
+metadata:
+  name: config
+  namespace: kueue-system
+manageJobsWithoutQueueName: true
+managedJobsNamespaceSelector:
+  matchLabels:
+    managed-by-kueue: "true"
+```
+
+### Step 3: Enable the Feature Gate
+
+This step is optional if you're using Kueue v0.15 or later, as the `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate is enabled by default. If you're using an older version of Kueue, you need to explicitly enable this feature gate. See [Feature Gate Details](#feature-gate-details) for configuration instructions.
+
+## Feature Gate Details {#feature-gate-details}
+
+The `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate controls whether the `managedJobsNamespaceSelector` restricts the reconciliation of all workloads, regardless of whether they have a `kueue.x-k8s.io/queue-name` label.
+
+### Feature Gate Status
+
+The `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate was introduced as **Alpha** in Kueue v0.13 and graduated to **Beta** in v0.15.
+
+### Enabling the Feature Gate
+
+Check the [Installation guide](/docs/installation/#change-the-feature-gates-configuration) for details on feature gate configuration.
```

  Removed this section and added the link to https://kueue.sigs.k8s.io/docs/installation/#change-the-feature-gates-configuration in the Before you begin section.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2025-12-03T19:37:39Z

- **site/content/en/docs/tasks/manage/enforce_job_management/opt_in_namespace_management.md:52** by [@PannagaRao](https://github.com/PannagaRao) — 2025-12-03T19:37:39Z
```diff
@@ -0,0 +1,83 @@
+---
+title: "Opt-in Namespace Management"
+date: 2025-11-25
+weight: 15
+description: >
+  Configure Kueue to only manage workloads in explicitly labeled namespaces.
+---
+
+This page describes how to configure Kueue to enforce opt-in namespace management, ensuring that only workloads in explicitly labeled namespaces are reconciled by Kueue.
+
+## Before you begin
+
+- Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+- Understand how to configure [`manageJobsWithoutQueueName`](/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name/).
+
+## Why Use Opt-in Namespace Management?
+
+Opt-in namespace management allows cluster administrators to enforce strict quota controls by ensuring that Kueue only manages workloads in explicitly designated namespaces. This provides several key benefits:
+
+- **Prevents quota bypass**: Users cannot bypass Kueue's quota system by manually adding `queue-name` labels to workloads in unmanaged namespaces. Only workloads in opted-in namespaces are ever reconciled by Kueue, regardless of how they are labeled.
+- **Explicit control**: Cluster administrators have full control over which namespaces are subject to Kueue management by simply labeling namespaces.
+- **Consistent behavior**: This brings `batch/v1.Job` integration into alignment with `Pod`, `Deployment`, and `StatefulSet` integrations, which already enforce namespace-based filtering.
+
+## Configuration
+
+### Step 1: Label Namespaces
+
+Label the namespaces that should be managed by Kueue. For example, use the `managed-by-kueue` label:
+
+```bash
+kubectl label namespace my-namespace managed-by-kueue=true
+```
+
+### Step 2: Configure the Selector
+
+Configure the `managedJobsNamespaceSelector` in your Kueue Configuration to match the labeled namespaces. Use `matchLabels` to select namespaces that you have explicitly labeled:
+
+```yaml
+apiVersion: config.kueue.x-k8s.io/v1beta1
+kind: Configuration
+metadata:
+  name: config
+  namespace: kueue-system
+manageJobsWithoutQueueName: true
+managedJobsNamespaceSelector:
+  matchLabels:
+    managed-by-kueue: "true"
+```
+
+### Step 3: Enable the Feature Gate
+
+This step is optional if you're using Kueue v0.15 or later, as the `ManagedJobsNamespaceSelectorAlwaysRespected` feature gate is enabled by default. If you're using an older version of Kueue, you need to explicitly enable this feature gate. See [Feature Gate Details](#feature-gate-details) for configuration instructions.
```

  Added the reference to the doc and removed the section that explicitly explained the status of the feature gate. PTAL if it looks okay now.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-12-10T13:18:44Z

- **site/content/en/docs/tasks/manage/enforce_job_management/opt_in_namespace_management.md:40** by [@kannon92](https://github.com/kannon92) — 2025-12-10T13:18:43Z
```diff
@@ -0,0 +1,76 @@
+---
+title: "Opt-in Namespace Management"
+date: 2025-11-25
+weight: 15
+description: >
+  Configure Kueue to only manage workloads in explicitly labeled namespaces.
+---
+
+This page describes how to configure Kueue to enforce opt-in namespace management, ensuring that only workloads in explicitly labeled namespaces are reconciled by Kueue.
+
+## Before you begin
+
+- Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+- Understand how to configure [`manageJobsWithoutQueueName`](/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name/).
+- Learn how to [change the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration).
+
+## Why Use Opt-in Namespace Management?
+
+Opt-in namespace management allows cluster administrators to enforce strict quota controls by ensuring that Kueue only manages workloads in explicitly designated namespaces. This provides several key benefits:
+
+- **Prevents quota bypass**: Users cannot bypass Kueue's quota system by manually adding `queue-name` labels to workloads in unmanaged namespaces. Only workloads in opted-in namespaces are ever reconciled by Kueue, regardless of how they are labeled.
+- **Explicit control**: Cluster administrators have full control over which namespaces are subject to Kueue management by simply labeling namespaces.
+- **Consistent behavior**: This brings `batch/v1.Job` integration into alignment with `Pod`, `Deployment`, and `StatefulSet` integrations, which already enforce namespace-based filtering.
+
+## Configuration
+
+### Step 1: Label Namespaces
+
+Label the namespaces that should be managed by Kueue. For example, use the `managed-by-kueue` label:
+
+```bash
+kubectl label namespace my-namespace managed-by-kueue=true
+```
+
+### Step 2: Configure the Selector
+
+Configure the `managedJobsNamespaceSelector` in your Kueue Configuration to match the labeled namespaces. Use `matchLabels` to select namespaces that you have explicitly labeled:
+
+```yaml
+apiVersion: config.kueue.x-k8s.io/v1beta1
```

  ```suggestion
  apiVersion: config.kueue.x-k8s.io/v1beta2
  ```

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2025-12-10T19:19:11Z

- **site/content/en/docs/tasks/manage/enforce_job_management/opt_in_namespace_management.md:40** by [@PannagaRao](https://github.com/PannagaRao) — 2025-12-10T19:19:10Z
```diff
@@ -0,0 +1,76 @@
+---
+title: "Opt-in Namespace Management"
+date: 2025-11-25
+weight: 15
+description: >
+  Configure Kueue to only manage workloads in explicitly labeled namespaces.
+---
+
+This page describes how to configure Kueue to enforce opt-in namespace management, ensuring that only workloads in explicitly labeled namespaces are reconciled by Kueue.
+
+## Before you begin
+
+- Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+- Understand how to configure [`manageJobsWithoutQueueName`](/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name/).
+- Learn how to [change the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration).
+
+## Why Use Opt-in Namespace Management?
+
+Opt-in namespace management allows cluster administrators to enforce strict quota controls by ensuring that Kueue only manages workloads in explicitly designated namespaces. This provides several key benefits:
+
+- **Prevents quota bypass**: Users cannot bypass Kueue's quota system by manually adding `queue-name` labels to workloads in unmanaged namespaces. Only workloads in opted-in namespaces are ever reconciled by Kueue, regardless of how they are labeled.
+- **Explicit control**: Cluster administrators have full control over which namespaces are subject to Kueue management by simply labeling namespaces.
+- **Consistent behavior**: This brings `batch/v1.Job` integration into alignment with `Pod`, `Deployment`, and `StatefulSet` integrations, which already enforce namespace-based filtering.
+
+## Configuration
+
+### Step 1: Label Namespaces
+
+Label the namespaces that should be managed by Kueue. For example, use the `managed-by-kueue` label:
+
+```bash
+kubectl label namespace my-namespace managed-by-kueue=true
+```
+
+### Step 2: Configure the Selector
+
+Configure the `managedJobsNamespaceSelector` in your Kueue Configuration to match the labeled namespaces. Use `matchLabels` to select namespaces that you have explicitly labeled:
+
+```yaml
+apiVersion: config.kueue.x-k8s.io/v1beta1
```

  Done

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-12-10T19:32:02Z

/lgtm

/assign @mimowo @tenzen-y
