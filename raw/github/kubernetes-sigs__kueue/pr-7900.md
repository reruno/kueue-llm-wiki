# PR #7900: Fix integration tests when WorkloadRequestUseMergePatch feature enabled.

**Summary**: Fix integration tests when WorkloadRequestUseMergePatch feature enabled.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7900

**Last updated**: 2025-11-26T11:09:38Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `fix/integration-tests-for-WorkloadRequestUseMergePatch` → `main`
- **Created**: 2025-11-26T09:42:22Z
- **Updated**: 2025-11-26T11:09:38Z
- **Merged**: 2025-11-26T11:02:36Z
- **Closed**: 2025-11-26T11:02:36Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`, `size/S`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 1   **+10 / -8**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Fix integration tests when WorkloadRequestUseMergePatch feature enabled.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:
Based on https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7891/pull-kueue-test-integration-baseline-main/1993595378267590656l

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-11-26T09:42:29Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 2493e75c47b4371981ef6cff4449aa300fbfad8c |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6926d2f1cae4510008ea9287 |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-26T10:33:46Z

I see this flake first time: https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7900/pull-kueue-test-e2e-main-1-34/1993616322499448832

please check what is here, maybe this is realated to using Patch actualy

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-26T10:34:16Z

/lgtm
/approve
regardless if  https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/7900/pull-kueue-test-e2e-main-1-34/1993616322499448832 is related or not, this lgtm. It might still require us to check before enabling Patch by default as Beta, But I'm not concerned while in Alpha.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-26T10:34:24Z

LGTM label has been added.  <details>Git tree hash: f31f9a9e1379181d5281b7838baa2f2de7ec1338</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-26T10:34:26Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7900#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/7900#issuecomment-3580679723" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-26T10:34:49Z

> please check what is here, maybe this is realated to using Patch actualy

We’re not using patch here at all — this isn’t related to the flake.
The flake is in the e2e tests, but the changes we made are only in the integration tests.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-26T10:37:50Z

Created an issue https://github.com/kubernetes-sigs/kueue/issues/7903.

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-26T09:46:55Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1102** by [@mimowo](https://github.com/mimowo) — 2025-11-26T09:46:55Z
```diff
@@ -1091,14 +1092,15 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			ginkgo.By("Checking if the workload is deactivated, once had Rejected status in the status.admissionCheck[*] field, an event is emitted and a metric is increased", func() {
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
-
-					g.Expect(updatedWl.Status.AdmissionChecks).To(gomega.ContainElement(gomega.BeComparableTo(
-						kueue.AdmissionCheckState{
-							Name:       kueue.AdmissionCheckReference(ac.Name),
-							State:      kueue.CheckStatePending,
-							Message:    "Reset to Pending after eviction. Previously: Rejected",
-							RetryCount: ptr.To(int32(1)),
-						},
+					wantState := kueue.AdmissionCheckState{
+						Name:    kueue.AdmissionCheckReference(ac.Name),
+						State:   kueue.CheckStatePending,
+						Message: "Reset to Pending after eviction. Previously: Rejected",
+					}
+					if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+						wantState.RetryCount = ptr.To(int32(1))
+					}
```

  Can you clarify why this depends on the WorkloadRequestUseMergePatch? When deactivating we should clear `RetryCount`. So that when admin re-activates it is not de-activated immediately. Otherwise it will require 2 requests from admin - first to change the status, then spec

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-26T10:04:47Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1102** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-26T10:04:47Z
```diff
@@ -1091,14 +1092,15 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			ginkgo.By("Checking if the workload is deactivated, once had Rejected status in the status.admissionCheck[*] field, an event is emitted and a metric is increased", func() {
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
-
-					g.Expect(updatedWl.Status.AdmissionChecks).To(gomega.ContainElement(gomega.BeComparableTo(
-						kueue.AdmissionCheckState{
-							Name:       kueue.AdmissionCheckReference(ac.Name),
-							State:      kueue.CheckStatePending,
-							Message:    "Reset to Pending after eviction. Previously: Rejected",
-							RetryCount: ptr.To(int32(1)),
-						},
+					wantState := kueue.AdmissionCheckState{
+						Name:    kueue.AdmissionCheckReference(ac.Name),
+						State:   kueue.CheckStatePending,
+						Message: "Reset to Pending after eviction. Previously: Rejected",
+					}
+					if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+						wantState.RetryCount = ptr.To(int32(1))
+					}
```

  Apply patch does not reset this field. With merge patch it works as expected. It happens here:
  
  https://github.com/epam/kubernetes-kueue/blob/86f8780c0039ad720b23d58eccd9f435355c1d5f/pkg/workload/admissionchecks.go#L100-L112

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-26T10:11:17Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1102** by [@mimowo](https://github.com/mimowo) — 2025-11-26T10:11:17Z
```diff
@@ -1091,14 +1092,15 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			ginkgo.By("Checking if the workload is deactivated, once had Rejected status in the status.admissionCheck[*] field, an event is emitted and a metric is increased", func() {
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
-
-					g.Expect(updatedWl.Status.AdmissionChecks).To(gomega.ContainElement(gomega.BeComparableTo(
-						kueue.AdmissionCheckState{
-							Name:       kueue.AdmissionCheckReference(ac.Name),
-							State:      kueue.CheckStatePending,
-							Message:    "Reset to Pending after eviction. Previously: Rejected",
-							RetryCount: ptr.To(int32(1)),
-						},
+					wantState := kueue.AdmissionCheckState{
+						Name:    kueue.AdmissionCheckReference(ac.Name),
+						State:   kueue.CheckStatePending,
+						Message: "Reset to Pending after eviction. Previously: Rejected",
+					}
+					if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+						wantState.RetryCount = ptr.To(int32(1))
+					}
```

  I see, which might be another point for Patch, the inability to reset fields in SSA is really tricky

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-26T10:11:34Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1102** by [@mimowo](https://github.com/mimowo) — 2025-11-26T10:11:34Z
```diff
@@ -1091,14 +1092,15 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			ginkgo.By("Checking if the workload is deactivated, once had Rejected status in the status.admissionCheck[*] field, an event is emitted and a metric is increased", func() {
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
-
-					g.Expect(updatedWl.Status.AdmissionChecks).To(gomega.ContainElement(gomega.BeComparableTo(
-						kueue.AdmissionCheckState{
-							Name:       kueue.AdmissionCheckReference(ac.Name),
-							State:      kueue.CheckStatePending,
-							Message:    "Reset to Pending after eviction. Previously: Rejected",
-							RetryCount: ptr.To(int32(1)),
-						},
+					wantState := kueue.AdmissionCheckState{
+						Name:    kueue.AdmissionCheckReference(ac.Name),
+						State:   kueue.CheckStatePending,
+						Message: "Reset to Pending after eviction. Previously: Rejected",
+					}
+					if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+						wantState.RetryCount = ptr.To(int32(1))
+					}
```

  For now, in this PR the change makes sense to me

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-11-26T10:57:09Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1102** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-26T10:57:06Z
```diff
@@ -1091,14 +1092,15 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			ginkgo.By("Checking if the workload is deactivated, once had Rejected status in the status.admissionCheck[*] field, an event is emitted and a metric is increased", func() {
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
-
-					g.Expect(updatedWl.Status.AdmissionChecks).To(gomega.ContainElement(gomega.BeComparableTo(
-						kueue.AdmissionCheckState{
-							Name:       kueue.AdmissionCheckReference(ac.Name),
-							State:      kueue.CheckStatePending,
-							Message:    "Reset to Pending after eviction. Previously: Rejected",
-							RetryCount: ptr.To(int32(1)),
-						},
+					wantState := kueue.AdmissionCheckState{
+						Name:    kueue.AdmissionCheckReference(ac.Name),
+						State:   kueue.CheckStatePending,
+						Message: "Reset to Pending after eviction. Previously: Rejected",
+					}
+					if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+						wantState.RetryCount = ptr.To(int32(1))
+					}
```

  AFAIK, in SSA, we need to explicitly `nil` when we reset fields.
  What if we specify `retryCount = nil` in https://github.com/epam/kubernetes-kueue/blob/86f8780c0039ad720b23d58eccd9f435355c1d5f/pkg/workload/admissionchecks.go#L100-L112?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-26T11:07:35Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1102** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-26T11:07:34Z
```diff
@@ -1091,14 +1092,15 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			ginkgo.By("Checking if the workload is deactivated, once had Rejected status in the status.admissionCheck[*] field, an event is emitted and a metric is increased", func() {
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
-
-					g.Expect(updatedWl.Status.AdmissionChecks).To(gomega.ContainElement(gomega.BeComparableTo(
-						kueue.AdmissionCheckState{
-							Name:       kueue.AdmissionCheckReference(ac.Name),
-							State:      kueue.CheckStatePending,
-							Message:    "Reset to Pending after eviction. Previously: Rejected",
-							RetryCount: ptr.To(int32(1)),
-						},
+					wantState := kueue.AdmissionCheckState{
+						Name:    kueue.AdmissionCheckReference(ac.Name),
+						State:   kueue.CheckStatePending,
+						Message: "Reset to Pending after eviction. Previously: Rejected",
+					}
+					if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+						wantState.RetryCount = ptr.To(int32(1))
+					}
```

  Nope. It doesn't work.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-11-26T11:09:34Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1102** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-11-26T11:09:34Z
```diff
@@ -1091,14 +1092,15 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			ginkgo.By("Checking if the workload is deactivated, once had Rejected status in the status.admissionCheck[*] field, an event is emitted and a metric is increased", func() {
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
-
-					g.Expect(updatedWl.Status.AdmissionChecks).To(gomega.ContainElement(gomega.BeComparableTo(
-						kueue.AdmissionCheckState{
-							Name:       kueue.AdmissionCheckReference(ac.Name),
-							State:      kueue.CheckStatePending,
-							Message:    "Reset to Pending after eviction. Previously: Rejected",
-							RetryCount: ptr.To(int32(1)),
-						},
+					wantState := kueue.AdmissionCheckState{
+						Name:    kueue.AdmissionCheckReference(ac.Name),
+						State:   kueue.CheckStatePending,
+						Message: "Reset to Pending after eviction. Previously: Rejected",
+					}
+					if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+						wantState.RetryCount = ptr.To(int32(1))
+					}
```

  It should be the owner of this field — only then can SSA reset it to nil.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-11-26T11:09:38Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1102** by [@tenzen-y](https://github.com/tenzen-y) — 2025-11-26T11:09:38Z
```diff
@@ -1091,14 +1092,15 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			ginkgo.By("Checking if the workload is deactivated, once had Rejected status in the status.admissionCheck[*] field, an event is emitted and a metric is increased", func() {
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
-
-					g.Expect(updatedWl.Status.AdmissionChecks).To(gomega.ContainElement(gomega.BeComparableTo(
-						kueue.AdmissionCheckState{
-							Name:       kueue.AdmissionCheckReference(ac.Name),
-							State:      kueue.CheckStatePending,
-							Message:    "Reset to Pending after eviction. Previously: Rejected",
-							RetryCount: ptr.To(int32(1)),
-						},
+					wantState := kueue.AdmissionCheckState{
+						Name:    kueue.AdmissionCheckReference(ac.Name),
+						State:   kueue.CheckStatePending,
+						Message: "Reset to Pending after eviction. Previously: Rejected",
+					}
+					if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+						wantState.RetryCount = ptr.To(int32(1))
+					}
```

  That sounds interesting. We might need to use the Apply object rather than the typed object if we really carefully handle those situations.
