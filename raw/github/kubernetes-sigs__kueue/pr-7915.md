# PR #7915: Queue cache - add reverse workload mapping to stop relying on previous workload state during update/delete

**Summary**: Queue cache - add reverse workload mapping to stop relying on previous workload state during update/delete

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/7915

**Last updated**: 2025-11-28T16:10:59Z

---

## Metadata

- **State**: closed
- **Author**: [@Singularity23x0](https://github.com/Singularity23x0)
- **Branch**: `5310-queue-stash-reverse-mapping` → `main`
- **Created**: 2025-11-26T15:01:13Z
- **Updated**: 2025-11-28T16:10:59Z
- **Merged**: —
- **Closed**: 2025-11-28T16:09:23Z
- **Labels**: `size/L`, `release-note-none`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: _none_
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@pajakd](https://github.com/pajakd), [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 9   **+246 / -119**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
The PR introduces changes allowing for O(1) time retrieval of the current state of the workload assumed into the queue cache. This allows making the update/deletion methods independent from an object representing the state of the workload before the update.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part 1 of addressing: #5310 

#### Special notes for your reviewer:
Continued in https://github.com/kubernetes-sigs/kueue/pull/8001

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-26T15:01:22Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/7915#" title="Author self-approved">Singularity23x0</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [tenzen-y](https://github.com/tenzen-y) for approval. For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["tenzen-y"]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-26T15:01:24Z

Hi @Singularity23x0. Thanks for your PR.

I'm waiting for a [github.com](https://kubernetes-sigs/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-11-26T15:01:58Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 4ebd77e782c282d40fad4d3f1b869d40103c8e55 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69286843c17dce0008dcf2b6 |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-11-26T16:19:28Z

/ok-to-test

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-27T10:40:52Z

@Singularity23x0: The `/test` command needs one or more targets.
The following commands are available to trigger required jobs:
```
/test pull-kueue-build-image-main
```
```
/test pull-kueue-populator-test-e2e-main
```
```
/test pull-kueue-populator-test-integration-main
```
```
/test pull-kueue-populator-test-unit-main
```
```
/test pull-kueue-populator-verify-main
```
```
/test pull-kueue-test-e2e-certmanager-main
```
```
/test pull-kueue-test-e2e-certmanager-upgrade-main
```
```
/test pull-kueue-test-e2e-customconfigs-main
```
```
/test pull-kueue-test-e2e-kueueviz-main
```
```
/test pull-kueue-test-e2e-main-1-32
```
```
/test pull-kueue-test-e2e-main-1-33
```
```
/test pull-kueue-test-e2e-main-1-34
```
```
/test pull-kueue-test-e2e-multikueue-main
```
```
/test pull-kueue-test-e2e-tas-main
```
```
/test pull-kueue-test-e2e-upgrade-main
```
```
/test pull-kueue-test-integration-baseline-main
```
```
/test pull-kueue-test-integration-extended-main
```
```
/test pull-kueue-test-integration-multikueue-main
```
```
/test pull-kueue-test-scheduling-perf-main
```
```
/test pull-kueue-test-unit-main
```
```
/test pull-kueue-verify-main
```

Use `/test all` to run all jobs.


<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7915#issuecomment-3585182283):

>/test


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-27T10:41:06Z

/test all

### Comment by [@olekzabl](https://github.com/olekzabl) — 2025-11-27T11:05:08Z

This PR involves a refactor "take `*Workload` as argument -> take `Workload.Reference` instead" in a number of functions.

That refactor is very nice in itself - however, is it _necessary_ for the main purpose of this PR (namely, introducing the reverse mapping)?

I didn't find such a link. I have an impression that you could freely split this into 2 separate PRs (say, first introduce the mapping, then refactor as follow up). Which would be then easier to review for everyone.

Or have I missed sth?

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-11-27T13:44:14Z

@Singularity23x0: The `/test` command needs one or more targets.
The following commands are available to trigger required jobs:
```
/test pull-kueue-build-image-main
```
```
/test pull-kueue-populator-test-e2e-main
```
```
/test pull-kueue-populator-test-integration-main
```
```
/test pull-kueue-populator-test-unit-main
```
```
/test pull-kueue-populator-verify-main
```
```
/test pull-kueue-test-e2e-certmanager-main
```
```
/test pull-kueue-test-e2e-certmanager-upgrade-main
```
```
/test pull-kueue-test-e2e-customconfigs-main
```
```
/test pull-kueue-test-e2e-kueueviz-main
```
```
/test pull-kueue-test-e2e-main-1-32
```
```
/test pull-kueue-test-e2e-main-1-33
```
```
/test pull-kueue-test-e2e-main-1-34
```
```
/test pull-kueue-test-e2e-multikueue-main
```
```
/test pull-kueue-test-e2e-tas-main
```
```
/test pull-kueue-test-e2e-upgrade-main
```
```
/test pull-kueue-test-integration-baseline-main
```
```
/test pull-kueue-test-integration-extended-main
```
```
/test pull-kueue-test-integration-multikueue-main
```
```
/test pull-kueue-test-scheduling-perf-main
```
```
/test pull-kueue-test-unit-main
```
```
/test pull-kueue-verify-main
```

Use `/test all` to run all jobs.


<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/7915#issuecomment-3585943514):

>/test


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-27T14:03:17Z

> This PR involves a refactor "take `*Workload` as argument -> take `Workload.Reference` instead" in a number of functions.
> 
> That refactor is very nice in itself - however, is it _necessary_ for the main purpose of this PR (namely, introducing the reverse mapping)?
> 
> I didn't find such a link. I have an impression that you could freely split this into 2 separate PRs (say, first introduce the mapping, then refactor as follow up). Which would be then easier to review for everyone.
> 
> Or have I missed sth?

Given that the only reason for keeping the reverse-mapping is to use it for those deletions I feel like separating the logic into two pr-s would be very artificial. I can however rename the pr to reflect the contents better.

### Comment by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-27T15:03:42Z

/test all

### Comment by [@olekzabl](https://github.com/olekzabl) — 2025-11-27T15:57:54Z

> > This PR involves a refactor "take `*Workload` as argument -> take `Workload.Reference` instead" in a number of functions.
> > 
> > That refactor is very nice in itself - however, is it necessary for the main purpose of this PR (namely, introducing the reverse mapping)?
> >
> > I didn't find such a link. I have an impression that you could freely split this into 2 separate PRs (say, first introduce the mapping, then refactor as follow up). Which would be then easier to review for everyone.
> >
> > Or have I missed sth?
> 
> Given that the only reason for keeping the reverse-mapping is to use it for those deletions I feel like separating the logic into two pr-s would be very artificial. I can however rename the pr to reflect the contents better.

I don't follow.

* I understand that your goal is to "stop relying on previous workload state during update/delete".
  On that front, I notice one place where you stop taking `oldW` as an argument, because - with the new `assignedWorkloads` map - you can now take the new `w` and use `assignedWorkloads` to get the old queue.
  And that's nice.

* However, (unless I'm missing sth), this feels completely _orthogonal_ to whether you take _workload key_ or _full workload_ in plenty other methods.
  In most of the code under concern, _full workload_ is not needed; _workload key_ is enough, and hence I happily welcome your refactor because it black-boxes the code better.
  However, I think this refactor is _not the essence_ of this PR, rather a nice follow-up.
  And that splitting it may help in reviewing - as I still can't say I fully understand the whole flow :)

WDYT?

### Comment by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-28T08:49:05Z

> I don't follow.
> 
> * I understand that your goal is to "stop relying on previous workload state during update/delete".
>   On that front, I notice one place where you stop taking `oldW` as an argument, because - with the new `assignedWorkloads` map - you can now take the new `w` and use `assignedWorkloads` to get the old queue.
>   And that's nice.
> * However, (unless I'm missing sth), this feels completely _orthogonal_ to whether you take _workload key_ or _full workload_ in plenty other methods.
>   In most of the code under concern, _full workload_ is not needed; _workload key_ is enough, and hence I happily welcome your refactor because it black-boxes the code better.
>   However, I think this refactor is _not the essence_ of this PR, rather a nice follow-up.
>   And that splitting it may help in reviewing - as I still can't say I fully understand the whole flow :)
> 
> WDYT?

There are two exported methods which get the update - the update method, where we remove the old state and thedelete method, wehre we replace workload with just the key. Remaining changes are internal and accomodate this update. Outside of this refactor, any changes to the controller are to correct the invocation of the updated methods.

As such the only split I can make is to have 1 pr adding the reverse mapping tracking and another which then removes the reliance on old workload and using wlKey instead of wl in the delete, which I would be opposed to as the second part is the whole reason for which the former change is done.

### Comment by [@olekzabl](https://github.com/olekzabl) — 2025-11-28T11:22:05Z

> There are two exported methods which get the update - the update method, where we remove the old state and thedelete method, wehre we replace workload with just the key. Remaining changes are internal and accomodate this update. Outside of this refactor, any changes to the controller are to correct the invocation of the updated methods.
> 
> As such the only split I can make is to have 1 pr adding the reverse mapping tracking and another which then removes the reliance on old workload and using wlKey instead of wl in the delete, which I would be opposed to as the second part is the whole reason for which the former change is done.

We synced offline and, IIUC :), we agreed that it is _in principle_ possible to split this PR into 2 parts, as follows:

1. Introduce the reverse map, _and_ remove the actual reliance on the old workload data.
    (Specifically, `oldW` in `UpdateWorkload` is gone. And as for `DeleteWorkload`, while it technically takes a `*Workload` as an argument, nothing besides its key is used downstream).
2. Refactor function arguments from "workload" to "workload key".

Knowing that, I do _not_ insist that this should be necessarily done. (Doing so might delay development).
I mostly wanted to confirm if my understanding of this code is correct.

### Comment by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-28T16:09:57Z

Continued in a new PR created on the basis of this one: https://github.com/kubernetes-sigs/kueue/pull/8001

## Reviews

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-11-27T10:43:04Z

- **pkg/cache/queue/cluster_queue_test.go:712** by [@olekzabl](https://github.com/olekzabl) — 2025-11-27T02:48:58Z
```diff
@@ -706,9 +706,10 @@ func TestFIFOClusterQueue(t *testing.T) {
 		t.Errorf("Popped workload %q want %q", got.Obj.Name, "after")
 	}
 
-	q.Delete(log, &kueue.Workload{
+	wl := &kueue.Workload{
 		ObjectMeta: metav1.ObjectMeta{Name: "now"},
-	})
+	}
+	q.Delete(&log, workload.Key(wl))
```

  Nit: how about just
  
  ```golang
  q.Delete(workload.NewReference("", "now"))
  ```

- **pkg/cache/queue/manager.go:138** by [@olekzabl](https://github.com/olekzabl) — 2025-11-27T10:16:17Z
```diff
@@ -130,10 +131,11 @@ type Manager struct {
 
 func NewManager(client client.Client, checker StatusChecker, options ...Option) *Manager {
 	m := &Manager{
-		clock:         realClock,
-		client:        client,
-		statusChecker: checker,
-		localQueues:   make(map[queue.LocalQueueReference]*LocalQueue),
+		clock:            realClock,
+		client:           client,
+		statusChecker:    checker,
+		localQueues:      make(map[queue.LocalQueueReference]*LocalQueue),
+		assumedWorkloads: make(map[workload.Reference]queue.LocalQueueReference),
```

  Is it really about _assumed_ workloads?
  
  I'm not sure what "assumed" exactly means, but looking through the code, I see just 1 path how a workload can get "assumed", and it's while being _admitted_:
  
  https://github.com/kubernetes-sigs/kueue/blob/cc40ec1776dcb6fe98a8cd6741059ecfa1ea0496/pkg/scheduler/scheduler.go#L617-L625
  
  So, certainly _not every_ workload added to an LQ can be called "assumed" - while your code below seems like "keep all LQ workloads in this map".
  
  ---
  
  Did you mean "assigned workloads", perhaps?
  (Looking down through the code, I think that's an alternative which could play well)

- **pkg/cache/queue/manager.go:536** by [@olekzabl](https://github.com/olekzabl) — 2025-11-27T10:24:53Z
```diff
@@ -509,24 +526,43 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, reas
 	return added
 }
 
-func (m *Manager) DeleteWorkload(log logr.Logger, w *kueue.Workload) {
+func (m *Manager) DeleteWorkload(log logr.Logger, wlKey workload.Reference) {
 	m.Lock()
 	defer m.Unlock()
-	m.deleteWorkloadFromQueueAndClusterQueue(log, w, queue.KeyFromWorkload(w))
-	m.DeleteSecondPassWithoutLock(w)
+	m.deleteWorkloadFromAssumedQueueAndClusterQueue(&log, wlKey)
+	m.DeleteSecondPassWithoutLock(wlKey)
 }
 
-func (m *Manager) deleteWorkloadFromQueueAndClusterQueue(log logr.Logger, w *kueue.Workload, qKey queue.LocalQueueReference) {
+func (m *Manager) deleteWorkloadFromObsoleteQueues(log *logr.Logger, wlKey workload.Reference, actualQueue queue.LocalQueueReference) {
```

  1. Why does it say "queues", plural? AFAICS you'll always delete from 0 or 1 queues here.
     Is the plural intended to cover the "0" case? :)
  
  2. I'm also hesitating about the word "obsolete".
     It sounds a bit as if the queue itself was "obsolete", while it's just "obsolete for this workload".
     How about "previous" instead?

- **pkg/cache/queue/manager.go:544** by [@olekzabl](https://github.com/olekzabl) — 2025-11-27T10:30:10Z
```diff
@@ -509,24 +526,43 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, reas
 	return added
 }
 
-func (m *Manager) DeleteWorkload(log logr.Logger, w *kueue.Workload) {
+func (m *Manager) DeleteWorkload(log logr.Logger, wlKey workload.Reference) {
 	m.Lock()
 	defer m.Unlock()
-	m.deleteWorkloadFromQueueAndClusterQueue(log, w, queue.KeyFromWorkload(w))
-	m.DeleteSecondPassWithoutLock(w)
+	m.deleteWorkloadFromAssumedQueueAndClusterQueue(&log, wlKey)
+	m.DeleteSecondPassWithoutLock(wlKey)
 }
 
-func (m *Manager) deleteWorkloadFromQueueAndClusterQueue(log logr.Logger, w *kueue.Workload, qKey queue.LocalQueueReference) {
+func (m *Manager) deleteWorkloadFromObsoleteQueues(log *logr.Logger, wlKey workload.Reference, actualQueue queue.LocalQueueReference) {
+	assumedQueue, assumed := m.assumedWorkloads[wlKey]
+	if assumed && assumedQueue != actualQueue {
+		m.deleteWorkloadFromAssumedQueueAndClusterQueue(log, wlKey)
+	}
+
+}
+
+func (m *Manager) deleteWorkloadFromAssumedQueueAndClusterQueue(log *logr.Logger, wlKey workload.Reference) {
```

  Here assumed -> assigned would play well I guess.

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2025-11-27T10:53:38Z

- **pkg/cache/queue/cluster_queue_test.go:712** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-27T10:53:38Z
```diff
@@ -706,9 +706,10 @@ func TestFIFOClusterQueue(t *testing.T) {
 		t.Errorf("Popped workload %q want %q", got.Obj.Name, "after")
 	}
 
-	q.Delete(log, &kueue.Workload{
+	wl := &kueue.Workload{
 		ObjectMeta: metav1.ObjectMeta{Name: "now"},
-	})
+	}
+	q.Delete(&log, workload.Key(wl))
```

  Are you suggesting defining a new method that does not take the log parameter?

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2025-11-27T10:54:28Z

- **pkg/cache/queue/manager.go:138** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-27T10:54:28Z
```diff
@@ -130,10 +131,11 @@ type Manager struct {
 
 func NewManager(client client.Client, checker StatusChecker, options ...Option) *Manager {
 	m := &Manager{
-		clock:         realClock,
-		client:        client,
-		statusChecker: checker,
-		localQueues:   make(map[queue.LocalQueueReference]*LocalQueue),
+		clock:            realClock,
+		client:           client,
+		statusChecker:    checker,
+		localQueues:      make(map[queue.LocalQueueReference]*LocalQueue),
+		assumedWorkloads: make(map[workload.Reference]queue.LocalQueueReference),
```

  Fair point, though the wording of "assumed" was used in the context of lq and cq before in here. 
  Still, I can switch to using assigned instead of assumed.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-11-27T10:56:57Z

- **pkg/cache/queue/manager.go:447** by [@olekzabl](https://github.com/olekzabl) — 2025-11-27T10:56:19Z
```diff
@@ -441,24 +444,32 @@ func (m *Manager) ClusterQueueForWorkload(wl *kueue.Workload) (kueue.ClusterQueu
 func (m *Manager) AddOrUpdateWorkload(w *kueue.Workload, opts ...workload.InfoOption) error {
 	m.Lock()
 	defer m.Unlock()
-	return m.AddOrUpdateWorkloadWithoutLock(w, opts...)
+	var nullLogger *logr.Logger = nil
```

  This feels a quite unusual hack.
  How far are we from not needing it? I think not very far.
  AFAICS all usages of this method are in `workload_controller.go`, and in a context which already has a `log` object defined. It'd be enough to pass it to here.
  
  (And then, we'd no longer need pointer-type argument, nil checks etc.)
  
  WDYT?

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-11-27T10:58:33Z

- **pkg/cache/queue/cluster_queue_test.go:712** by [@olekzabl](https://github.com/olekzabl) — 2025-11-27T10:58:33Z
```diff
@@ -706,9 +706,10 @@ func TestFIFOClusterQueue(t *testing.T) {
 		t.Errorf("Popped workload %q want %q", got.Obj.Name, "after")
 	}
 
-	q.Delete(log, &kueue.Workload{
+	wl := &kueue.Workload{
 		ObjectMeta: metav1.ObjectMeta{Name: "now"},
-	})
+	}
+	q.Delete(&log, workload.Key(wl))
```

  Oh, no, I missed the `&log,` part, apologies.
  
  I just meant to build the reference directly, without constructing a (very incomplete) workload on the way.

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2025-11-27T11:00:53Z

- **pkg/cache/queue/manager.go:536** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-27T11:00:52Z
```diff
@@ -509,24 +526,43 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, reas
 	return added
 }
 
-func (m *Manager) DeleteWorkload(log logr.Logger, w *kueue.Workload) {
+func (m *Manager) DeleteWorkload(log logr.Logger, wlKey workload.Reference) {
 	m.Lock()
 	defer m.Unlock()
-	m.deleteWorkloadFromQueueAndClusterQueue(log, w, queue.KeyFromWorkload(w))
-	m.DeleteSecondPassWithoutLock(w)
+	m.deleteWorkloadFromAssumedQueueAndClusterQueue(&log, wlKey)
+	m.DeleteSecondPassWithoutLock(wlKey)
 }
 
-func (m *Manager) deleteWorkloadFromQueueAndClusterQueue(log logr.Logger, w *kueue.Workload, qKey queue.LocalQueueReference) {
+func (m *Manager) deleteWorkloadFromObsoleteQueues(log *logr.Logger, wlKey workload.Reference, actualQueue queue.LocalQueueReference) {
```

  1. Before the name was "...AssumedQueueAndClusterQueue", so queues (pl.) came from QueueAndClusterQueue.
  2. It only deletes if the queue has changed, so I propose "...FromQueuesIfReassigned".

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2025-11-27T11:06:38Z

- **pkg/cache/queue/manager.go:447** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-27T11:06:38Z
```diff
@@ -441,24 +444,32 @@ func (m *Manager) ClusterQueueForWorkload(wl *kueue.Workload) (kueue.ClusterQueu
 func (m *Manager) AddOrUpdateWorkload(w *kueue.Workload, opts ...workload.InfoOption) error {
 	m.Lock()
 	defer m.Unlock()
-	return m.AddOrUpdateWorkloadWithoutLock(w, opts...)
+	var nullLogger *logr.Logger = nil
```

  It is here solely because of "cq.Delete(log, wlKey)" - i was trying to keep the changes as non-invaisve as I could, but your point is fair - on it.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-11-27T11:30:29Z

- **pkg/cache/queue/manager.go:112** by [@olekzabl](https://github.com/olekzabl) — 2025-11-27T11:30:29Z
```diff
@@ -105,10 +105,11 @@ type Manager struct {
 	sync.RWMutex
 	cond sync.Cond
 
-	clock         clock.WithDelayedExecution
-	client        client.Client
-	statusChecker StatusChecker
-	localQueues   map[queue.LocalQueueReference]*LocalQueue
+	clock             clock.WithDelayedExecution
+	client            client.Client
+	statusChecker     StatusChecker
+	localQueues       map[queue.LocalQueueReference]*LocalQueue
+	assignedWorkloads map[workload.Reference]queue.LocalQueueReference
```

  If possible, please decide whether you want it consistent:
  * (A) with `q.items` (in entries of `localQueues`) or
  * (B) with `.Spec.LocalQueue` for _all_ workloads that exist.
  
  Currently, IIUC you follow a mixed approach:
  
  * Here you follow (A):
  
     https://github.com/kubernetes-sigs/kueue/blob/05bc3b206bb2bb1fe2df4f4e2de7b1138e33a2fd/pkg/cache/queue/manager.go#L468-L471
     Because you update `assignedWorkloads` if and only if `q.AddOrUpdate()` will be called (which is responsible for updating `q.items`).
  
     And some workloads (inactive, or those with quota reserved) had been already filtered out before:
  
     https://github.com/kubernetes-sigs/kueue/blob/05bc3b206bb2bb1fe2df4f4e2de7b1138e33a2fd/pkg/cache/queue/manager.go#L452-L457
  
  * OTOH here you follow (B):
  
     https://github.com/kubernetes-sigs/kueue/blob/05bc3b206bb2bb1fe2df4f4e2de7b1138e33a2fd/pkg/cache/queue/manager.go#L326-L329
  
     Your map is updated _before_ the filter, and `q.items` is updated only after it:
  
     https://github.com/kubernetes-sigs/kueue/blob/05bc3b206bb2bb1fe2df4f4e2de7b1138e33a2fd/pkg/cache/queue/manager.go#L341

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2025-11-27T12:16:28Z

- **pkg/cache/queue/manager.go:112** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-27T12:16:27Z
```diff
@@ -105,10 +105,11 @@ type Manager struct {
 	sync.RWMutex
 	cond sync.Cond
 
-	clock         clock.WithDelayedExecution
-	client        client.Client
-	statusChecker StatusChecker
-	localQueues   map[queue.LocalQueueReference]*LocalQueue
+	clock             clock.WithDelayedExecution
+	client            client.Client
+	statusChecker     StatusChecker
+	localQueues       map[queue.LocalQueueReference]*LocalQueue
+	assignedWorkloads map[workload.Reference]queue.LocalQueueReference
```

  B is an error on my end - fixed and refactored so that addition to the assigned workloads happens only when we expect to add thing to the queues.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-11-27T15:58:05Z

- **pkg/cache/queue/manager.go:723** by [@olekzabl](https://github.com/olekzabl) — 2025-11-27T15:44:05Z
```diff
@@ -685,15 +720,20 @@ func (m *Manager) heads() []workload.Info {
 			continue
 		}
 		wl := cq.Pop()
+		m.reportPendingWorkloads(cqName, cq)
```

  You moved this line above the `wl == nil` check. Why?
  Looking at the code, it seems this _may_ matter for behavior. (I'm not sure yet).
  And it feels unrelated to the rest of this PR.

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2025-11-28T08:41:15Z

- **pkg/cache/queue/manager.go:723** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-28T08:41:14Z
```diff
@@ -685,15 +720,20 @@ func (m *Manager) heads() []workload.Info {
 			continue
 		}
 		wl := cq.Pop()
+		m.reportPendingWorkloads(cqName, cq)
```

  One of the tests had an issue with reporting pending workloads, where the existence of the "inflight" workload would make it think there is one pending. WIth the reporting being set after the nil check, this remained the case, whereas now, if the inflight is set back to nil, the metric will be corrected accordingly.

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2025-11-28T11:05:11Z

- **pkg/cache/queue/manager.go:723** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-28T11:05:10Z
```diff
@@ -685,15 +720,20 @@ func (m *Manager) heads() []workload.Info {
 			continue
 		}
 		wl := cq.Pop()
+		m.reportPendingWorkloads(cqName, cq)
```

  The test in question is:
  `test/integration/singlecluster/controller/jobs/appwrapper/appwrapper_controller_test.go` -> `Should schedule AppWrappers as they fit in their ClusterQueue`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-28T14:20:49Z

- **pkg/cache/queue/manager.go:727** by [@mimowo](https://github.com/mimowo) — 2025-11-28T14:20:49Z
```diff
@@ -685,15 +720,20 @@ func (m *Manager) heads() []workload.Info {
 			continue
 		}
 		wl := cq.Pop()
+		m.reportPendingWorkloads(cqName, cq)
 		if wl == nil {
 			continue
 		}
-		m.reportPendingWorkloads(cqName, cq)
+		wlKey := workload.Key(wl.Obj)
```

  The PR looks like the right direction, but it feels like mixing functional changes (like here, maybe a fix) with some very mechanical changes like propagation of logs or using workload.Reference.
  
  I would like to consider separation of another preparetory PR for the mechanicak changes. It will make reviewing much easier, and will allow me to easily cherrypick the mechanical changes with confidence.

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2025-11-28T15:42:14Z

- **pkg/cache/queue/manager.go:727** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-28T15:42:14Z
```diff
@@ -685,15 +720,20 @@ func (m *Manager) heads() []workload.Info {
 			continue
 		}
 		wl := cq.Pop()
+		m.reportPendingWorkloads(cqName, cq)
 		if wl == nil {
 			continue
 		}
-		m.reportPendingWorkloads(cqName, cq)
+		wlKey := workload.Key(wl.Obj)
```

  The issue with separating the propagation of logs is that it calls for code duplication, as we require essentially the same logic but once requiring the log, and once not. To simplify the code of the functional changes log propagation is required  before or alongside it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-11-28T15:56:25Z

- **pkg/cache/queue/manager.go:727** by [@mimowo](https://github.com/mimowo) — 2025-11-28T15:56:25Z
```diff
@@ -685,15 +720,20 @@ func (m *Manager) heads() []workload.Info {
 			continue
 		}
 		wl := cq.Pop()
+		m.reportPendingWorkloads(cqName, cq)
 		if wl == nil {
 			continue
 		}
-		m.reportPendingWorkloads(cqName, cq)
+		wlKey := workload.Key(wl.Obj)
```

  > The issue with separating the propagation of logs is that it calls for code duplication, as we require essentially the same logic but once requiring the log, and once not. 
  
  Hm I didn't know that. Why can't we just always propagate the log?

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2025-11-28T16:10:30Z

- **pkg/cache/queue/manager.go:727** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-11-28T16:10:30Z
```diff
@@ -685,15 +720,20 @@ func (m *Manager) heads() []workload.Info {
 			continue
 		}
 		wl := cq.Pop()
+		m.reportPendingWorkloads(cqName, cq)
 		if wl == nil {
 			continue
 		}
-		m.reportPendingWorkloads(cqName, cq)
+		wlKey := workload.Key(wl.Obj)
```

  Extracted the changes to the logic (with the necessary log propagation) to https://github.com/kubernetes-sigs/kueue/pull/8001
