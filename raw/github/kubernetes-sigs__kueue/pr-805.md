# PR #805: Fix concurrent admission in cohort when borrowing

**Summary**: Fix concurrent admission in cohort when borrowing

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/805

**Last updated**: 2023-06-09T07:21:51Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `concurrent-cohort-preemption` → `main`
- **Created**: 2023-05-24T12:39:49Z
- **Updated**: 2023-06-09T07:21:51Z
- **Merged**: 2023-05-30T19:05:48Z
- **Closed**: 2023-05-30T19:05:48Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `tide/merge-method-squash`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 5   **+238 / -14**

## Description


#### What type of PR is this?

/kind bug

#### What this PR does / why we need it:

Giving a situation with a cohort composed of more than two queues, in which at least one queue is borrowing resources , when two new workloads are evaluated for admission within the same scheduling cycle, since the two workloads are not aware of each other,   they both can be evaluated as fit leading to over-admitting workloads within the cohort.

A simple solution could be to make sure that only one workload within a cohort is evaluated for admission within a single scheduling cycle, however since this can be a b drastic, we are limiting this limit to cohorts in which any of  its queues is borrowing.

#### Which issue(s) this PR fixes:

Fixes #804

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?

```release-note
Fix: Potential over-admission within cohort when borrowing.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-05-24T12:39:58Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 48713edf7282f38162b625e2eb9f408f0247dc96 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/64744ea0b6affe00085c3cc6 |

### Comment by [@trasc](https://github.com/trasc) — 2023-05-24T14:34:31Z

/retest

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-24T15:01:46Z

/milestone v0.4

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-05-26T18:28:50Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/805#pullrequestreview-1446757254" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/805#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-29T13:25:07Z

tests failing due to https://github.com/kubernetes/test-infra/issues/29622

### Comment by [@trasc](https://github.com/trasc) — 2023-05-29T17:08:22Z

/retest

### Comment by [@trasc](https://github.com/trasc) — 2023-05-30T17:29:49Z

/retest

### Comment by [@trasc](https://github.com/trasc) — 2023-05-30T18:51:12Z

/test pull-kueue-test-integration-main

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-30T19:53:05Z

/cherry-pick release-0.3

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2023-05-30T19:53:40Z

@alculquicondor: #805 failed to apply on top of branch "release-0.3":
```
Applying: Concurrent preemption within cohort
Applying: Fix over-admission in concurrent preemption
Using index info to reconstruct a base tree...
M	pkg/cache/cache.go
M	pkg/scheduler/scheduler.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/scheduler/scheduler.go
Auto-merging pkg/cache/cache.go
CONFLICT (content): Merge conflict in pkg/cache/cache.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
Patch failed at 0002 Fix over-admission in concurrent preemption
When you have resolved this problem, run "git am --continue".
If you prefer to skip this patch, run "git am --skip" instead.
To restore the original branch and stop patching, run "git am --abort".

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/805#issuecomment-1569001377):

>/cherry-pick release-0.3


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-30T20:06:33Z

@trasc can you create a manual cherry-pick for the release-0.3 branch?

### Comment by [@trasc](https://github.com/trasc) — 2023-05-30T20:08:24Z

will do

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-24T18:41:02Z

- **pkg/scheduler/scheduler.go:144** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-24T18:07:27Z
```diff
@@ -140,14 +140,15 @@ func (s *Scheduler) schedule(ctx context.Context) {
 			continue
 		}
 		cq := snapshot.ClusterQueues[e.ClusterQueue]
-		if e.assignment.Borrows() && cq.Cohort != nil && usedCohorts.Has(cq.Cohort.Name) {
-			e.status = skipped
-			e.inadmissibleMsg = "workloads in the cohort that don't require borrowing were prioritized and admitted first"
-			continue
-		}
-		// Even if there was a failure, we shouldn't admit other workloads to this
-		// cohort.
 		if cq.Cohort != nil {
+			e.assignment.Borrows()
```

  it looks like you forgot to remove this line

- **pkg/scheduler/scheduler.go:150** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-24T18:13:36Z
```diff
@@ -140,14 +140,15 @@ func (s *Scheduler) schedule(ctx context.Context) {
 			continue
 		}
 		cq := snapshot.ClusterQueues[e.ClusterQueue]
-		if e.assignment.Borrows() && cq.Cohort != nil && usedCohorts.Has(cq.Cohort.Name) {
-			e.status = skipped
-			e.inadmissibleMsg = "workloads in the cohort that don't require borrowing were prioritized and admitted first"
-			continue
-		}
-		// Even if there was a failure, we shouldn't admit other workloads to this
-		// cohort.
 		if cq.Cohort != nil {
+			e.assignment.Borrows()
+			if usedCohorts.Has(cq.Cohort.Name) && (e.assignment.Borrows() || cq.Cohort.HasBorrowingQueues()) {
```

  can you explain why this new check solves the bug in the PR description and what other alternatives you considered and why they didn't work?
  
  I'm wondering whether this check is excessive.
  
  IIUC, the over-admission would happen in the second scheduling cycle:
  - one workload was already preempted
  - two workloads try to use the space, the first one fits.
  - The second one is not aware that the first one was just scheduled. Could we just call `snapshot.AddWorkload` when we call `AssumeWorkload`?
  
  And this also makes me think that there is a bug even without preemption, in an scenario very similar to the integration test you have below. Disable preemption and just mark one of the beta workloads as finished after you have already created the workloads for alpha and gamma.

- **test/integration/scheduler/preemption_test.go:324** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-24T18:16:56Z
```diff
@@ -289,6 +302,60 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, alphaCQ.Name, alphaHighWl1)
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, betaCQ.Name, betaLowWl)
 		})
+
+		ginkgo.It("Should preempt all necessary workloads in concurrent scheduling", func() {
+			ginkgo.By("Creating workloads in beta-cq that borrow quota")
+
+			betaMidWl := testing.MakeWorkload("beta-mid", ns.Name).
+				Queue(betaQ.Name).
+				Priority(midPriority).
+				Request(corev1.ResourceCPU, "3").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, betaMidWl)).To(gomega.Succeed())
+			betaHighWl := testing.MakeWorkload("beta-high", ns.Name).
+				Queue(betaQ.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "3").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, betaHighWl)).To(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, betaCQ.Name, betaMidWl, betaHighWl)
+
+			ginkgo.By("Creating workload in alpha-cq and game-cq that need to preempt")
```

  ```suggestion
  			ginkgo.By("Creating workload in alpha-cq and gamma-cq that need to preempt")
  ```

- **test/integration/scheduler/preemption_test.go:188** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-24T18:17:29Z
```diff
@@ -185,8 +185,8 @@ var _ = ginkgo.Describe("Preemption", func() {
 
 	ginkgo.Context("In a ClusterQueue that is part of a cohort", func() {
 		var (
-			alphaCQ, betaCQ *kueue.ClusterQueue
-			alphaQ, betaQ   *kueue.LocalQueue
+			alphaCQ, betaCQ, gamaCQ *kueue.ClusterQueue
```

  ```suggestion
  			alphaCQ, betaCQ, gammaCQ *kueue.ClusterQueue
  ```

- **test/integration/scheduler/preemption_test.go:351** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-24T18:23:57Z
```diff
@@ -289,6 +302,60 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, alphaCQ.Name, alphaHighWl1)
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, betaCQ.Name, betaLowWl)
 		})
+
+		ginkgo.It("Should preempt all necessary workloads in concurrent scheduling", func() {
+			ginkgo.By("Creating workloads in beta-cq that borrow quota")
+
+			betaMidWl := testing.MakeWorkload("beta-mid", ns.Name).
+				Queue(betaQ.Name).
+				Priority(midPriority).
+				Request(corev1.ResourceCPU, "3").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, betaMidWl)).To(gomega.Succeed())
+			betaHighWl := testing.MakeWorkload("beta-high", ns.Name).
+				Queue(betaQ.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "3").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, betaHighWl)).To(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, betaCQ.Name, betaMidWl, betaHighWl)
+
+			ginkgo.By("Creating workload in alpha-cq and game-cq that need to preempt")
+			alphaMidWl := testing.MakeWorkload("alpha-mid", ns.Name).
+				Queue(alphaQ.Name).
+				Priority(midPriority).
+				Request(corev1.ResourceCPU, "2").
+				Obj()
+
+			gamaMidWl := testing.MakeWorkload("gama-mid", ns.Name).
+				Queue(gamaQ.Name).
+				Priority(midPriority).
+				Request(corev1.ResourceCPU, "2").
+				Obj()
+
+			gomega.Expect(k8sClient.Create(ctx, alphaMidWl)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, gamaMidWl)).To(gomega.Succeed())
+
+			// since the two pending workloads are not aware of each other both of them
+			// will request the eviction of betaMidWl only
+			util.FinishEvictionForWorkloads(ctx, k8sClient, betaMidWl)
+
+			// one of alpha-mid and gama-mid should be admitted
+			var firstAdmitted []*kueue.Workload
+			gomega.Eventually(func() []*kueue.Workload {
+				firstAdmitted = util.FilterAdmittedWorkloads(ctx, k8sClient, alphaMidWl, gamaMidWl)
+				return firstAdmitted
+			}, util.Timeout, util.Interval).Should(gomega.HaveLen(1))
+			// and stay that way
+			gomega.Consistently(util.FilterAdmittedWorkloads(ctx, k8sClient, alphaMidWl, gamaMidWl), util.ConsistentDuration, util.Interval).Should(gomega.Equal(firstAdmitted))
```

  we shouldn't need this check. We should avoid calling `Consistently` as it's pretty much a busy wait.
  
  We can instead confirm that both workloads are admitted at the end, and verify that the preempted workloads are not admitted anymore.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-25T09:19:22Z

- **pkg/scheduler/scheduler.go:144** by [@trasc](https://github.com/trasc) — 2023-05-25T07:16:10Z
```diff
@@ -140,14 +140,15 @@ func (s *Scheduler) schedule(ctx context.Context) {
 			continue
 		}
 		cq := snapshot.ClusterQueues[e.ClusterQueue]
-		if e.assignment.Borrows() && cq.Cohort != nil && usedCohorts.Has(cq.Cohort.Name) {
-			e.status = skipped
-			e.inadmissibleMsg = "workloads in the cohort that don't require borrowing were prioritized and admitted first"
-			continue
-		}
-		// Even if there was a failure, we shouldn't admit other workloads to this
-		// cohort.
 		if cq.Cohort != nil {
+			e.assignment.Borrows()
```

  done

- **test/integration/scheduler/preemption_test.go:188** by [@trasc](https://github.com/trasc) — 2023-05-25T07:18:57Z
```diff
@@ -185,8 +185,8 @@ var _ = ginkgo.Describe("Preemption", func() {
 
 	ginkgo.Context("In a ClusterQueue that is part of a cohort", func() {
 		var (
-			alphaCQ, betaCQ *kueue.ClusterQueue
-			alphaQ, betaQ   *kueue.LocalQueue
+			alphaCQ, betaCQ, gamaCQ *kueue.ClusterQueue
```

  done

- **test/integration/scheduler/preemption_test.go:324** by [@trasc](https://github.com/trasc) — 2023-05-25T07:19:40Z
```diff
@@ -289,6 +302,60 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, alphaCQ.Name, alphaHighWl1)
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, betaCQ.Name, betaLowWl)
 		})
+
+		ginkgo.It("Should preempt all necessary workloads in concurrent scheduling", func() {
+			ginkgo.By("Creating workloads in beta-cq that borrow quota")
+
+			betaMidWl := testing.MakeWorkload("beta-mid", ns.Name).
+				Queue(betaQ.Name).
+				Priority(midPriority).
+				Request(corev1.ResourceCPU, "3").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, betaMidWl)).To(gomega.Succeed())
+			betaHighWl := testing.MakeWorkload("beta-high", ns.Name).
+				Queue(betaQ.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "3").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, betaHighWl)).To(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, betaCQ.Name, betaMidWl, betaHighWl)
+
+			ginkgo.By("Creating workload in alpha-cq and game-cq that need to preempt")
```

  done

- **test/integration/scheduler/preemption_test.go:351** by [@trasc](https://github.com/trasc) — 2023-05-25T07:25:25Z
```diff
@@ -289,6 +302,60 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, alphaCQ.Name, alphaHighWl1)
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, betaCQ.Name, betaLowWl)
 		})
+
+		ginkgo.It("Should preempt all necessary workloads in concurrent scheduling", func() {
+			ginkgo.By("Creating workloads in beta-cq that borrow quota")
+
+			betaMidWl := testing.MakeWorkload("beta-mid", ns.Name).
+				Queue(betaQ.Name).
+				Priority(midPriority).
+				Request(corev1.ResourceCPU, "3").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, betaMidWl)).To(gomega.Succeed())
+			betaHighWl := testing.MakeWorkload("beta-high", ns.Name).
+				Queue(betaQ.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "3").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, betaHighWl)).To(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, betaCQ.Name, betaMidWl, betaHighWl)
+
+			ginkgo.By("Creating workload in alpha-cq and game-cq that need to preempt")
+			alphaMidWl := testing.MakeWorkload("alpha-mid", ns.Name).
+				Queue(alphaQ.Name).
+				Priority(midPriority).
+				Request(corev1.ResourceCPU, "2").
+				Obj()
+
+			gamaMidWl := testing.MakeWorkload("gama-mid", ns.Name).
+				Queue(gamaQ.Name).
+				Priority(midPriority).
+				Request(corev1.ResourceCPU, "2").
+				Obj()
+
+			gomega.Expect(k8sClient.Create(ctx, alphaMidWl)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, gamaMidWl)).To(gomega.Succeed())
+
+			// since the two pending workloads are not aware of each other both of them
+			// will request the eviction of betaMidWl only
+			util.FinishEvictionForWorkloads(ctx, k8sClient, betaMidWl)
+
+			// one of alpha-mid and gama-mid should be admitted
+			var firstAdmitted []*kueue.Workload
+			gomega.Eventually(func() []*kueue.Workload {
+				firstAdmitted = util.FilterAdmittedWorkloads(ctx, k8sClient, alphaMidWl, gamaMidWl)
+				return firstAdmitted
+			}, util.Timeout, util.Interval).Should(gomega.HaveLen(1))
+			// and stay that way
+			gomega.Consistently(util.FilterAdmittedWorkloads(ctx, k8sClient, alphaMidWl, gamaMidWl), util.ConsistentDuration, util.Interval).Should(gomega.Equal(firstAdmitted))
```

  I still  wold have like  to check that one and only one is admitted after the initial successful preemption and the eventually condition is not just a transition state.   However since the second eviction request can only be done in another scheduler cycle ... , 
  
  done

- **pkg/scheduler/scheduler.go:150** by [@trasc](https://github.com/trasc) — 2023-05-25T07:32:15Z
```diff
@@ -140,14 +140,15 @@ func (s *Scheduler) schedule(ctx context.Context) {
 			continue
 		}
 		cq := snapshot.ClusterQueues[e.ClusterQueue]
-		if e.assignment.Borrows() && cq.Cohort != nil && usedCohorts.Has(cq.Cohort.Name) {
-			e.status = skipped
-			e.inadmissibleMsg = "workloads in the cohort that don't require borrowing were prioritized and admitted first"
-			continue
-		}
-		// Even if there was a failure, we shouldn't admit other workloads to this
-		// cohort.
 		if cq.Cohort != nil {
+			e.assignment.Borrows()
+			if usedCohorts.Has(cq.Cohort.Name) && (e.assignment.Borrows() || cq.Cohort.HasBorrowingQueues()) {
```

  Yes this can happen without premonition,  all that is needed are two workloads from the same cohort to be the object of the same scheduling cycle, while while one of the queues is borrowing resources.

- **pkg/scheduler/scheduler.go:150** by [@trasc](https://github.com/trasc) — 2023-05-25T07:35:51Z
```diff
@@ -140,14 +140,15 @@ func (s *Scheduler) schedule(ctx context.Context) {
 			continue
 		}
 		cq := snapshot.ClusterQueues[e.ClusterQueue]
-		if e.assignment.Borrows() && cq.Cohort != nil && usedCohorts.Has(cq.Cohort.Name) {
-			e.status = skipped
-			e.inadmissibleMsg = "workloads in the cohort that don't require borrowing were prioritized and admitted first"
-			continue
-		}
-		// Even if there was a failure, we shouldn't admit other workloads to this
-		// cohort.
 		if cq.Cohort != nil {
+			e.assignment.Borrows()
+			if usedCohorts.Has(cq.Cohort.Name) && (e.assignment.Borrows() || cq.Cohort.HasBorrowingQueues()) {
```

  > Could we just call snapshot.AddWorkload when we call AssumeWorkload?
  
  Not really, the fit is checked in nominate, before any assume is done and before ordering the heads.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-25T15:24:46Z

- **pkg/scheduler/scheduler.go:150** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-25T15:24:46Z
```diff
@@ -140,14 +140,15 @@ func (s *Scheduler) schedule(ctx context.Context) {
 			continue
 		}
 		cq := snapshot.ClusterQueues[e.ClusterQueue]
-		if e.assignment.Borrows() && cq.Cohort != nil && usedCohorts.Has(cq.Cohort.Name) {
-			e.status = skipped
-			e.inadmissibleMsg = "workloads in the cohort that don't require borrowing were prioritized and admitted first"
-			continue
-		}
-		// Even if there was a failure, we shouldn't admit other workloads to this
-		// cohort.
 		if cq.Cohort != nil {
+			e.assignment.Borrows()
+			if usedCohorts.Has(cq.Cohort.Name) && (e.assignment.Borrows() || cq.Cohort.HasBorrowingQueues()) {
```

  True

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-25T15:25:44Z

- **pkg/scheduler/scheduler.go:150** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-25T15:25:44Z
```diff
@@ -140,14 +140,15 @@ func (s *Scheduler) schedule(ctx context.Context) {
 			continue
 		}
 		cq := snapshot.ClusterQueues[e.ClusterQueue]
-		if e.assignment.Borrows() && cq.Cohort != nil && usedCohorts.Has(cq.Cohort.Name) {
-			e.status = skipped
-			e.inadmissibleMsg = "workloads in the cohort that don't require borrowing were prioritized and admitted first"
-			continue
-		}
-		// Even if there was a failure, we shouldn't admit other workloads to this
-		// cohort.
 		if cq.Cohort != nil {
+			e.assignment.Borrows()
+			if usedCohorts.Has(cq.Cohort.Name) && (e.assignment.Borrows() || cq.Cohort.HasBorrowingQueues()) {
```

  Please add a comment to this check to summarize why we are doing it.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-25T15:28:08Z

- **test/integration/scheduler/preemption_test.go:186** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-25T15:27:05Z
```diff
@@ -185,8 +185,8 @@ var _ = ginkgo.Describe("Preemption", func() {
 
 	ginkgo.Context("In a ClusterQueue that is part of a cohort", func() {
```

  Since we can reproduce this even without preemption, could you add a unit test for the scenario in `scheduler_test.go`?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-26T07:14:27Z

- **test/integration/scheduler/preemption_test.go:186** by [@trasc](https://github.com/trasc) — 2023-05-26T07:14:27Z
```diff
@@ -185,8 +185,8 @@ var _ = ginkgo.Describe("Preemption", func() {
 
 	ginkgo.Context("In a ClusterQueue that is part of a cohort", func() {
```

  We besides using the preemption,  I don't think we have a way to ensure the workloads will be evaluated in the same scheduling cycle, I've added a unit test for it.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-26T18:28:42Z

just some nits

/approve

- **pkg/scheduler/scheduler.go:150** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-26T18:19:58Z
```diff
@@ -140,14 +140,15 @@ func (s *Scheduler) schedule(ctx context.Context) {
 			continue
 		}
 		cq := snapshot.ClusterQueues[e.ClusterQueue]
-		if e.assignment.Borrows() && cq.Cohort != nil && usedCohorts.Has(cq.Cohort.Name) {
-			e.status = skipped
-			e.inadmissibleMsg = "workloads in the cohort that don't require borrowing were prioritized and admitted first"
-			continue
-		}
-		// Even if there was a failure, we shouldn't admit other workloads to this
-		// cohort.
 		if cq.Cohort != nil {
+			e.assignment.Borrows()
+			if usedCohorts.Has(cq.Cohort.Name) && (e.assignment.Borrows() || cq.Cohort.HasBorrowingQueues()) {
```

  Please add the comment

- **pkg/scheduler/scheduler_test.go:709** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-26T18:26:28Z
```diff
@@ -605,6 +609,110 @@ func TestSchedule(t *testing.T) {
 				"flavor-nonexistent-cq": sets.New("sales/foo"),
 			},
 		},
+		"only one workload is admitted in a cohort while borrowing": {
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("new", "eng-beta").
+					Queue("main").
+					PodSets(*utiltesting.MakePodSet("one", 50).
+						Request(corev1.ResourceCPU, "1").
+						Obj()).
+					Obj(),
+				*utiltesting.MakeWorkload("new-gamma", "eng-beta").
+					Queue("gamma").
+					PodSets(*utiltesting.MakePodSet("one", 50).
+						Request(corev1.ResourceCPU, "1").
+						Obj()).
+					Obj(),
+				*utiltesting.MakeWorkload("existing", "eng-alpha").
+					PodSets(
+						*utiltesting.MakePodSet("borrow-on-demand", 51).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+						*utiltesting.MakePodSet("use-all-spot", 100).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Admit(utiltesting.MakeAdmission("eng-alpha").
+						PodSets(
+							kueue.PodSetAssignment{
+								Name: "borrow-on-demand",
+								Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+									corev1.ResourceCPU: "on-demand",
+								},
+								ResourceUsage: corev1.ResourceList{
+									corev1.ResourceCPU: resource.MustParse("51000m"),
+								},
+								Count: 51,
+							},
+							kueue.PodSetAssignment{
+								Name: "use-all-spot",
+								Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+									corev1.ResourceCPU: "spot",
+								},
+								ResourceUsage: corev1.ResourceList{
+									corev1.ResourceCPU: resource.MustParse("100000m"),
+								},
+								Count: 100,
+							},
+						).
+						Obj()).
+					Obj(),
+			},
+			additionalClusterQueues: []kueue.ClusterQueue{
+				*utiltesting.MakeClusterQueue("eng-gamma").
+					Cohort("eng").
+					Preemption(kueue.ClusterQueuePreemption{
+						ReclaimWithinCohort: kueue.PreemptionPolicyAny,
+						WithinClusterQueue:  kueue.PreemptionPolicyLowerPriority,
+					}).
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("on-demand").
+							Resource(corev1.ResourceCPU, "50", "10").Obj(),
+					).
+					Obj(),
+			},
+			additionalLocalQueues: []kueue.LocalQueue{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Namespace: "eng-beta",
+						Name:      "gamma",
+					},
+					Spec: kueue.LocalQueueSpec{
+						ClusterQueue: "eng-gamma",
+					},
+				},
+			},
+			wantAssignments: map[string]kueue.Admission{
+				"eng-alpha/existing": *utiltesting.MakeAdmission("eng-alpha").
+					PodSets(
+						kueue.PodSetAssignment{
+							Name: "borrow-on-demand",
+							Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+								corev1.ResourceCPU: "on-demand",
+							},
+							ResourceUsage: corev1.ResourceList{
+								corev1.ResourceCPU: resource.MustParse("51000m"),
+							},
+							Count: 51,
+						},
+						kueue.PodSetAssignment{
+							Name: "use-all-spot",
+							Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+								corev1.ResourceCPU: "spot",
+							},
+							ResourceUsage: corev1.ResourceList{
+								corev1.ResourceCPU: resource.MustParse("100000m"),
+							},
+							Count: 100,
+						},
+					).Obj(),
+				"eng-beta/new": *utiltesting.MakeAdmission("eng-beta", "one").Assignment(corev1.ResourceCPU, "on-demand", "50000m").AssignmentPodCount(50).Obj(),
```

  it looks like `eng-beta/new` and `eng-beta/new-gamma` could have the same chance of being scheduled?
  
  Could this unit test flake depending on the randomness of sort?

- **pkg/scheduler/scheduler_test.go:643** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-26T18:27:04Z
```diff
@@ -605,6 +609,110 @@ func TestSchedule(t *testing.T) {
 				"flavor-nonexistent-cq": sets.New("sales/foo"),
 			},
 		},
+		"only one workload is admitted in a cohort while borrowing": {
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("new", "eng-beta").
+					Queue("main").
+					PodSets(*utiltesting.MakePodSet("one", 50).
+						Request(corev1.ResourceCPU, "1").
+						Obj()).
+					Obj(),
+				*utiltesting.MakeWorkload("new-gamma", "eng-beta").
+					Queue("gamma").
+					PodSets(*utiltesting.MakePodSet("one", 50).
+						Request(corev1.ResourceCPU, "1").
+						Obj()).
+					Obj(),
+				*utiltesting.MakeWorkload("existing", "eng-alpha").
+					PodSets(
+						*utiltesting.MakePodSet("borrow-on-demand", 51).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+						*utiltesting.MakePodSet("use-all-spot", 100).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Admit(utiltesting.MakeAdmission("eng-alpha").
+						PodSets(
+							kueue.PodSetAssignment{
+								Name: "borrow-on-demand",
+								Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+									corev1.ResourceCPU: "on-demand",
+								},
+								ResourceUsage: corev1.ResourceList{
+									corev1.ResourceCPU: resource.MustParse("51000m"),
```

  ```suggestion
  									corev1.ResourceCPU: resource.MustParse("51"),
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-26T18:28:44Z

just some nits

/approve

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-29T06:55:11Z

- **pkg/scheduler/scheduler.go:150** by [@trasc](https://github.com/trasc) — 2023-05-29T06:42:53Z
```diff
@@ -140,14 +140,15 @@ func (s *Scheduler) schedule(ctx context.Context) {
 			continue
 		}
 		cq := snapshot.ClusterQueues[e.ClusterQueue]
-		if e.assignment.Borrows() && cq.Cohort != nil && usedCohorts.Has(cq.Cohort.Name) {
-			e.status = skipped
-			e.inadmissibleMsg = "workloads in the cohort that don't require borrowing were prioritized and admitted first"
-			continue
-		}
-		// Even if there was a failure, we shouldn't admit other workloads to this
-		// cohort.
 		if cq.Cohort != nil {
+			e.assignment.Borrows()
+			if usedCohorts.Has(cq.Cohort.Name) && (e.assignment.Borrows() || cq.Cohort.HasBorrowingQueues()) {
```

  done

- **pkg/scheduler/scheduler_test.go:643** by [@trasc](https://github.com/trasc) — 2023-05-29T06:47:57Z
```diff
@@ -605,6 +609,110 @@ func TestSchedule(t *testing.T) {
 				"flavor-nonexistent-cq": sets.New("sales/foo"),
 			},
 		},
+		"only one workload is admitted in a cohort while borrowing": {
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("new", "eng-beta").
+					Queue("main").
+					PodSets(*utiltesting.MakePodSet("one", 50).
+						Request(corev1.ResourceCPU, "1").
+						Obj()).
+					Obj(),
+				*utiltesting.MakeWorkload("new-gamma", "eng-beta").
+					Queue("gamma").
+					PodSets(*utiltesting.MakePodSet("one", 50).
+						Request(corev1.ResourceCPU, "1").
+						Obj()).
+					Obj(),
+				*utiltesting.MakeWorkload("existing", "eng-alpha").
+					PodSets(
+						*utiltesting.MakePodSet("borrow-on-demand", 51).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+						*utiltesting.MakePodSet("use-all-spot", 100).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Admit(utiltesting.MakeAdmission("eng-alpha").
+						PodSets(
+							kueue.PodSetAssignment{
+								Name: "borrow-on-demand",
+								Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+									corev1.ResourceCPU: "on-demand",
+								},
+								ResourceUsage: corev1.ResourceList{
+									corev1.ResourceCPU: resource.MustParse("51000m"),
```

  done

- **pkg/scheduler/scheduler_test.go:709** by [@trasc](https://github.com/trasc) — 2023-05-29T06:53:04Z
```diff
@@ -605,6 +609,110 @@ func TestSchedule(t *testing.T) {
 				"flavor-nonexistent-cq": sets.New("sales/foo"),
 			},
 		},
+		"only one workload is admitted in a cohort while borrowing": {
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("new", "eng-beta").
+					Queue("main").
+					PodSets(*utiltesting.MakePodSet("one", 50).
+						Request(corev1.ResourceCPU, "1").
+						Obj()).
+					Obj(),
+				*utiltesting.MakeWorkload("new-gamma", "eng-beta").
+					Queue("gamma").
+					PodSets(*utiltesting.MakePodSet("one", 50).
+						Request(corev1.ResourceCPU, "1").
+						Obj()).
+					Obj(),
+				*utiltesting.MakeWorkload("existing", "eng-alpha").
+					PodSets(
+						*utiltesting.MakePodSet("borrow-on-demand", 51).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+						*utiltesting.MakePodSet("use-all-spot", 100).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Admit(utiltesting.MakeAdmission("eng-alpha").
+						PodSets(
+							kueue.PodSetAssignment{
+								Name: "borrow-on-demand",
+								Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+									corev1.ResourceCPU: "on-demand",
+								},
+								ResourceUsage: corev1.ResourceList{
+									corev1.ResourceCPU: resource.MustParse("51000m"),
+								},
+								Count: 51,
+							},
+							kueue.PodSetAssignment{
+								Name: "use-all-spot",
+								Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+									corev1.ResourceCPU: "spot",
+								},
+								ResourceUsage: corev1.ResourceList{
+									corev1.ResourceCPU: resource.MustParse("100000m"),
+								},
+								Count: 100,
+							},
+						).
+						Obj()).
+					Obj(),
+			},
+			additionalClusterQueues: []kueue.ClusterQueue{
+				*utiltesting.MakeClusterQueue("eng-gamma").
+					Cohort("eng").
+					Preemption(kueue.ClusterQueuePreemption{
+						ReclaimWithinCohort: kueue.PreemptionPolicyAny,
+						WithinClusterQueue:  kueue.PreemptionPolicyLowerPriority,
+					}).
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("on-demand").
+							Resource(corev1.ResourceCPU, "50", "10").Obj(),
+					).
+					Obj(),
+			},
+			additionalLocalQueues: []kueue.LocalQueue{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Namespace: "eng-beta",
+						Name:      "gamma",
+					},
+					Spec: kueue.LocalQueueSpec{
+						ClusterQueue: "eng-gamma",
+					},
+				},
+			},
+			wantAssignments: map[string]kueue.Admission{
+				"eng-alpha/existing": *utiltesting.MakeAdmission("eng-alpha").
+					PodSets(
+						kueue.PodSetAssignment{
+							Name: "borrow-on-demand",
+							Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+								corev1.ResourceCPU: "on-demand",
+							},
+							ResourceUsage: corev1.ResourceList{
+								corev1.ResourceCPU: resource.MustParse("51000m"),
+							},
+							Count: 51,
+						},
+						kueue.PodSetAssignment{
+							Name: "use-all-spot",
+							Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+								corev1.ResourceCPU: "spot",
+							},
+							ResourceUsage: corev1.ResourceList{
+								corev1.ResourceCPU: resource.MustParse("100000m"),
+							},
+							Count: 100,
+						},
+					).Obj(),
+				"eng-beta/new": *utiltesting.MakeAdmission("eng-beta", "one").Assignment(corev1.ResourceCPU, "on-demand", "50000m").AssignmentPodCount(50).Obj(),
```

  at this point no, `new` will always have a timestamp <= to the one in of `new-gamma` .. but let'm make sure is always smaller.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-29T13:14:37Z

/lgtm
/label tide/merge-method-squash
