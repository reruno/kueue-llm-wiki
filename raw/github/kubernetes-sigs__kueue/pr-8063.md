# PR #8063: Introduce workload.PatchStatus function to commonize workload status updates

**Summary**: Introduce workload.PatchStatus function to commonize workload status updates

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8063

**Last updated**: 2025-12-18T07:02:15Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `cleanup/use-patch-status-2` → `main`
- **Created**: 2025-12-03T18:33:20Z
- **Updated**: 2025-12-18T07:02:15Z
- **Merged**: 2025-12-05T19:06:58Z
- **Closed**: 2025-12-05T19:06:58Z
- **Labels**: `lgtm`, `release-note-none`, `size/XL`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mykysha](https://github.com/mykysha), [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mszadkow](https://github.com/mszadkow), [@mykysha](https://github.com/mykysha), [@IrvingMg](https://github.com/IrvingMg), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 2   **+451 / -81**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Use PatchStatus().

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #7035

#### Special notes for your reviewer:
As we discussed here https://github.com/kubernetes-sigs/kueue/pull/8031#discussion_r2585031759 we should comminize patch logic.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-12-03T18:33:27Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | dbaa8e1fc2f203dc8f85c44012421a9f96abd837 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6932b9e09a207800080113f4 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-8063--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-8063--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTgwNjMtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.JbiknJtprYFQSEf1l6dwYJm_cH8P0CBkErCuTiCqA7g)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-8063--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-03T18:34:16Z

/cc @mszadkow @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2025-12-04T06:45:11Z

/retitle Introduce workload.PatchStatus function to commonize workload status updates

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-12-04T12:08:54Z

unit tests look good, no comments from my side

### Comment by [@mykysha](https://github.com/mykysha) — 2025-12-04T12:36:06Z

/lgtm
Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-04T12:36:14Z

LGTM label has been added.  <details>Git tree hash: 3870fd8964c77a0fced0c3e42252b3783c45979c</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-04T13:55:59Z

> https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8063/pull-kueue-test-integration-baseline-main/1996576852675661824

Looks like this is flaky issue. Created fix https://github.com/kubernetes-sigs/kueue/pull/8073.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-04T14:01:05Z

@IrvingMg @mszadkow @mykysha thank you for review 🙏

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-04T14:01:12Z

cc: @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2025-12-04T15:03:01Z

https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8063/pull-kueue-test-e2e-customconfigs-main/1996583380052873216
do you think this might be related? I think we have this as a known flake, but worth to check

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-05T03:14:49Z

> https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8063/pull-kueue-test-e2e-customconfigs-main/1996583380052873216 do you think this might be related? I think we have this as a known flake, but worth to check

I think this is a flake https://github.com/kubernetes-sigs/kueue/issues/5106. But after introducing https://github.com/kubernetes-sigs/kueue/commit/4573156a2ddac906e4490b0c5515dbef2a0ee7c7, we’re seeing this flake more often.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-05T07:17:34Z

/retest 

Due to https://github.com/kubernetes-sigs/kueue/pull/8073 merged.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-05T18:40:12Z

LGTM label has been added.  <details>Git tree hash: b8778772b523d5eb46ffc7b84fbf00b48da3a5d5</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-05T18:40:14Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8063#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8063#pullrequestreview-3545950133" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-18T07:01:39Z

/cherrypick release-0.15

We need it for https://github.com/kubernetes-sigs/kueue/pull/8311.

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-12-18T07:02:15Z

@mbobrovskyi: #8063 failed to apply on top of branch "release-0.15":
```
Applying: Introduce workload.PatchStatus function to commonize workload status updates.
Using index info to reconstruct a base tree...
M	pkg/workload/workload.go
M	pkg/workload/workload_test.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/workload/workload_test.go
CONFLICT (content): Merge conflict in pkg/workload/workload_test.go
Auto-merging pkg/workload/workload.go
CONFLICT (content): Merge conflict in pkg/workload/workload.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 Introduce workload.PatchStatus function to commonize workload status updates.

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8063#issuecomment-3668686612):

>/cherrypick release-0.15
>
>We need it for https://github.com/kubernetes-sigs/kueue/pull/8311.


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-03T18:55:29Z

- **pkg/workload/workload.go:1056** by [@mimowo](https://github.com/mimowo) — 2025-12-03T18:53:59Z
```diff
@@ -1055,15 +1041,39 @@ func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
+		err = c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
 	}
 	if err == nil {
 		wlCopy.DeepCopyInto(w)
 	}
 	return err
 }
 
+func PatchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update func(*kueue.Workload) (bool, error), options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, w, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictPatch)
```

  this looks weird, why StrictPatch is Patch disabled.

- **pkg/workload/workload.go:1061** by [@mimowo](https://github.com/mimowo) — 2025-12-03T18:55:26Z
```diff
@@ -1055,15 +1041,39 @@ func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
+		err = c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
 	}
 	if err == nil {
 		wlCopy.DeepCopyInto(w)
 	}
 	return err
 }
 
+func PatchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update func(*kueue.Workload) (bool, error), options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, w, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictPatch)
+			wlPatch.DeepCopyInto(wl)
```

  I think we should DeeopCopy only on success. It looks unconditional, and Im not clear if this is the case here. Same for the other invocation.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-04T03:17:44Z

- **pkg/workload/workload.go:1061** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-04T03:17:44Z
```diff
@@ -1055,15 +1041,39 @@ func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
+		err = c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
 	}
 	if err == nil {
 		wlCopy.DeepCopyInto(w)
 	}
 	return err
 }
 
+func PatchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update func(*kueue.Workload) (bool, error), options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, w, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictPatch)
+			wlPatch.DeepCopyInto(wl)
```

  This DeepCopy is applied to the patch object, not to the original workload object.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-04T03:18:11Z

- **pkg/workload/workload.go:1061** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-04T03:18:11Z
```diff
@@ -1055,15 +1041,39 @@ func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
+		err = c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
 	}
 	if err == nil {
 		wlCopy.DeepCopyInto(w)
 	}
 	return err
 }
 
+func PatchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update func(*kueue.Workload) (bool, error), options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, w, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictPatch)
+			wlPatch.DeepCopyInto(wl)
```

  On success, we currently perform a DeepCopy in patchStatus(). However, for SSA, we should also DeepCopy the object here to strip out unnecessary fields that would otherwise interfere with SSA. Please review the implementation in patchStatus().

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-04T03:19:05Z

- **pkg/workload/workload.go:1056** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-04T03:19:05Z
```diff
@@ -1055,15 +1041,39 @@ func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
+		err = c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
 	}
 	if err == nil {
 		wlCopy.DeepCopyInto(w)
 	}
 	return err
 }
 
+func PatchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update func(*kueue.Workload) (bool, error), options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, w, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictPatch)
```

  Not sure I got what you mean.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-04T05:11:11Z

- **pkg/workload/workload.go:1056** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-04T05:11:11Z
```diff
@@ -1055,15 +1041,39 @@ func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
+		err = c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
 	}
 	if err == nil {
 		wlCopy.DeepCopyInto(w)
 	}
 	return err
 }
 
+func PatchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update func(*kueue.Workload) (bool, error), options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, w, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictPatch)
```

  When WorkloadRequestUseMergePatch is disabled, we are using SSA. In this mode, we should exclude unnecessary fields, so the DeepCopy (or equivalent filtering) should be performed here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-04T05:19:13Z

- **pkg/workload/workload.go:1073** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-04T05:19:13Z
```diff
@@ -1055,15 +1041,39 @@ func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
+		err = c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
 	}
 	if err == nil {
 		wlCopy.DeepCopyInto(w)
 	}
 	return err
 }
 
+func PatchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update func(*kueue.Workload) (bool, error), options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, w, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictPatch)
+			wlPatch.DeepCopyInto(wl)
+		}
+		return update(wl)
+	}, options...)
+}
+
+func PatchAdmissionStatus(ctx context.Context, c client.Client, wl *kueue.Workload, clk clock.Clock, update func(*kueue.Workload) (bool, error), options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, wl, constants.AdmissionName, func(wl *kueue.Workload) (bool, error) {
+		if updated, err := update(wl); err != nil || !updated {
+			return updated, err
+		}
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
```

  In PatchAdmissionStatus, we should first apply the desired changes and then execute PrepareWorkloadPatch. But in PatchStatus, we should start with BaseSSAWorkload patch and apply the changes afterward.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-04T06:26:09Z

- **pkg/workload/workload.go:1056** by [@mimowo](https://github.com/mimowo) — 2025-12-04T06:26:09Z
```diff
@@ -1055,15 +1041,39 @@ func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
+		err = c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
 	}
 	if err == nil {
 		wlCopy.DeepCopyInto(w)
 	}
 	return err
 }
 
+func PatchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update func(*kueue.Workload) (bool, error), options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, w, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictPatch)
```

  I'm thinking we may be using not the right param, because this branch of code serves SSA, but the code passes StrictPatch option. I'm wondering if StrictApply is the correct option here?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-04T06:26:44Z

- **pkg/workload/workload.go:1061** by [@mimowo](https://github.com/mimowo) — 2025-12-04T06:26:44Z
```diff
@@ -1055,15 +1041,39 @@ func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
+		err = c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
 	}
 	if err == nil {
 		wlCopy.DeepCopyInto(w)
 	}
 	return err
 }
 
+func PatchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update func(*kueue.Workload) (bool, error), options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, w, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictPatch)
+			wlPatch.DeepCopyInto(wl)
```

  Ok do we have unit tests which confirm the workload status changes are not recorded on error?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-04T06:27:14Z

- **pkg/workload/workload.go:1056** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-04T06:27:14Z
```diff
@@ -1055,15 +1041,39 @@ func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
+		err = c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
 	}
 	if err == nil {
 		wlCopy.DeepCopyInto(w)
 	}
 	return err
 }
 
+func PatchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update func(*kueue.Workload) (bool, error), options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, w, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictPatch)
```

  Ah, you right, this is my mistake. Let fix it to StrictApply.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-04T06:27:24Z

- **pkg/workload/workload.go:1056** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-04T06:27:24Z
```diff
@@ -1055,15 +1041,39 @@ func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
+		err = c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
 	}
 	if err == nil {
 		wlCopy.DeepCopyInto(w)
 	}
 	return err
 }
 
+func PatchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update func(*kueue.Workload) (bool, error), options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, w, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictPatch)
```

  Thank you for catch this.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-04T06:29:10Z

- **pkg/workload/workload.go:1056** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-04T06:29:09Z
```diff
@@ -1055,15 +1041,39 @@ func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
+		err = c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
 	}
 	if err == nil {
 		wlCopy.DeepCopyInto(w)
 	}
 	return err
 }
 
+func PatchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update func(*kueue.Workload) (bool, error), options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, w, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictPatch)
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-04T06:39:35Z

- **pkg/workload/workload.go:1056** by [@mimowo](https://github.com/mimowo) — 2025-12-04T06:39:35Z
```diff
@@ -1055,15 +1041,39 @@ func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
+		err = c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
 	}
 	if err == nil {
 		wlCopy.DeepCopyInto(w)
 	}
 	return err
 }
 
+func PatchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update func(*kueue.Workload) (bool, error), options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, w, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictPatch)
```

  Yeah it would be good to have a test for that then.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-12-04T08:18:00Z

- **pkg/workload/workload.go:1022** by [@mszadkow](https://github.com/mszadkow) — 2025-12-04T08:18:00Z
```diff
@@ -1016,28 +997,33 @@ func DefaultPatchAdmissionStatusOptions() *PatchAdmissionStatusOptions {
 //	    return updateFn(obj), nil
 //	}, WithLooseOnApply()) // disables strict mode for Patch Apply
 
-func WithLooseOnApply() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithLooseOnApply() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.StrictApply = false
 	}
 }
 
-// WithRetryOnConflictForPatch configures PatchAdmissionStatusOptions to enable retry logic on conflicts.
+// WithRetryOnConflictForPatch configures PatchStatusOptions to enable retry logic on conflicts.
 // Note: This only works with merge patches.
-func WithRetryOnConflictForPatch() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithRetryOnConflictForPatch() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.RetryOnConflictForPatch = true
 	}
 }
 
-// PatchAdmissionStatus updates the admission status of a workload.
-// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
-// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
-func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, clk clock.Clock, update func(*kueue.Workload) (bool, error), options ...PatchAdmissionStatusOption) error {
-	opts := DefaultPatchAdmissionStatusOptions()
+func patchStatusOptions(options []PatchStatusOption) *PatchStatusOptions {
+	opts := DefaultPatchStatusOptions()
 	for _, opt := range options {
 		opt(opts)
 	}
+	return opts
+}
+
+// PatchAdmissionStatus updates the status of a workload.
```

  The comment should be updated too

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-12-04T08:28:16Z

- **pkg/workload/workload.go:1061** by [@mszadkow](https://github.com/mszadkow) — 2025-12-04T08:28:16Z
```diff
@@ -1055,15 +1041,39 @@ func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
+		err = c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
 	}
 	if err == nil {
 		wlCopy.DeepCopyInto(w)
 	}
 	return err
 }
 
+func PatchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update func(*kueue.Workload) (bool, error), options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, w, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictPatch)
+			wlPatch.DeepCopyInto(wl)
```

  That seems to be the only difference in behaviour, let's have test for that

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-04T09:21:05Z

- **pkg/workload/workload.go:1022** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-04T09:21:05Z
```diff
@@ -1016,28 +997,33 @@ func DefaultPatchAdmissionStatusOptions() *PatchAdmissionStatusOptions {
 //	    return updateFn(obj), nil
 //	}, WithLooseOnApply()) // disables strict mode for Patch Apply
 
-func WithLooseOnApply() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithLooseOnApply() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.StrictApply = false
 	}
 }
 
-// WithRetryOnConflictForPatch configures PatchAdmissionStatusOptions to enable retry logic on conflicts.
+// WithRetryOnConflictForPatch configures PatchStatusOptions to enable retry logic on conflicts.
 // Note: This only works with merge patches.
-func WithRetryOnConflictForPatch() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithRetryOnConflictForPatch() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.RetryOnConflictForPatch = true
 	}
 }
 
-// PatchAdmissionStatus updates the admission status of a workload.
-// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
-// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
-func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, clk clock.Clock, update func(*kueue.Workload) (bool, error), options ...PatchAdmissionStatusOption) error {
-	opts := DefaultPatchAdmissionStatusOptions()
+func patchStatusOptions(options []PatchStatusOption) *PatchStatusOptions {
+	opts := DefaultPatchStatusOptions()
 	for _, opt := range options {
 		opt(opts)
 	}
+	return opts
+}
+
+// PatchAdmissionStatus updates the status of a workload.
```

  Whats wrong with this?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-04T09:21:20Z

- **pkg/workload/workload.go:1022** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-04T09:21:20Z
```diff
@@ -1016,28 +997,33 @@ func DefaultPatchAdmissionStatusOptions() *PatchAdmissionStatusOptions {
 //	    return updateFn(obj), nil
 //	}, WithLooseOnApply()) // disables strict mode for Patch Apply
 
-func WithLooseOnApply() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithLooseOnApply() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.StrictApply = false
 	}
 }
 
-// WithRetryOnConflictForPatch configures PatchAdmissionStatusOptions to enable retry logic on conflicts.
+// WithRetryOnConflictForPatch configures PatchStatusOptions to enable retry logic on conflicts.
 // Note: This only works with merge patches.
-func WithRetryOnConflictForPatch() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithRetryOnConflictForPatch() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.RetryOnConflictForPatch = true
 	}
 }
 
-// PatchAdmissionStatus updates the admission status of a workload.
-// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
-// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
-func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, clk clock.Clock, update func(*kueue.Workload) (bool, error), options ...PatchAdmissionStatusOption) error {
-	opts := DefaultPatchAdmissionStatusOptions()
+func patchStatusOptions(options []PatchStatusOption) *PatchStatusOptions {
+	opts := DefaultPatchStatusOptions()
 	for _, opt := range options {
 		opt(opts)
 	}
+	return opts
+}
+
+// PatchAdmissionStatus updates the status of a workload.
```

  Ah, I see prefix

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-04T09:28:55Z

- **pkg/workload/workload.go:1022** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-04T09:28:55Z
```diff
@@ -1016,28 +997,33 @@ func DefaultPatchAdmissionStatusOptions() *PatchAdmissionStatusOptions {
 //	    return updateFn(obj), nil
 //	}, WithLooseOnApply()) // disables strict mode for Patch Apply
 
-func WithLooseOnApply() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithLooseOnApply() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.StrictApply = false
 	}
 }
 
-// WithRetryOnConflictForPatch configures PatchAdmissionStatusOptions to enable retry logic on conflicts.
+// WithRetryOnConflictForPatch configures PatchStatusOptions to enable retry logic on conflicts.
 // Note: This only works with merge patches.
-func WithRetryOnConflictForPatch() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithRetryOnConflictForPatch() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.RetryOnConflictForPatch = true
 	}
 }
 
-// PatchAdmissionStatus updates the admission status of a workload.
-// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
-// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
-func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, clk clock.Clock, update func(*kueue.Workload) (bool, error), options ...PatchAdmissionStatusOption) error {
-	opts := DefaultPatchAdmissionStatusOptions()
+func patchStatusOptions(options []PatchStatusOption) *PatchStatusOptions {
+	opts := DefaultPatchStatusOptions()
 	for _, opt := range options {
 		opt(opts)
 	}
+	return opts
+}
+
+// PatchAdmissionStatus updates the status of a workload.
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-04T09:29:29Z

- **pkg/workload/workload.go:1061** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-04T09:29:29Z
```diff
@@ -1055,15 +1041,39 @@ func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
+		err = c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
 	}
 	if err == nil {
 		wlCopy.DeepCopyInto(w)
 	}
 	return err
 }
 
+func PatchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update func(*kueue.Workload) (bool, error), options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, w, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictPatch)
+			wlPatch.DeepCopyInto(wl)
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-04T09:29:45Z

- **pkg/workload/workload.go:1056** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-04T09:29:45Z
```diff
@@ -1055,15 +1041,39 @@ func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
+		err = c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
 	}
 	if err == nil {
 		wlCopy.DeepCopyInto(w)
 	}
 	return err
 }
 
+func PatchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update func(*kueue.Workload) (bool, error), options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, w, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictPatch)
```

  Done

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-12-04T11:25:09Z

- **pkg/workload/workload.go:1062** by [@IrvingMg](https://github.com/IrvingMg) — 2025-12-04T11:18:09Z
```diff
@@ -1055,15 +1042,39 @@ func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
+		err = c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
 	}
 	if err == nil {
 		wlCopy.DeepCopyInto(w)
 	}
 	return err
 }
 
+func PatchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update UpdateFunc, options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, w, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictApply)
+			wlPatch.DeepCopyInto(wl)
+		}
+		return update(wl)
+	}, opts)
+}
```

  Would it be worth to extract the wrapped-update logic? For example, we could introduce something like:
  
  ```go
  type preparationStrategy func(wl *kueue.Workload, opts *PatchStatusOptions, update UpdateFunc) (bool, error)
  ```
  
  Then we could extract a helper such as:
  
  ```go
  func patchWithStrategy(ctx, c, w, owner, strategy, update, options...) error {
    	opts := patchStatusOptions(options)
    	return patchStatus(ctx, c, w, owner, func(wl *kueue.Workload) (bool, error) {
    		return strategy(wl, opts, update)
    	}, opts)
    }
  ```
  
  Where the strategies would be something like:
  
  ```go
  func wrapWithBaseSSA() preparationStrategy
  func wrapWithAdmissionPatch(clk clock.Clock) preparationStrategy  
  ```
  
  And finally, they could be invoked as:
  
  ```go
  func PatchStatus(...) error {
      return patchWithStrategy(..., wrapWithBaseSSA, ...)
  }
  
  func PatchAdmissionStatus(...) error {
    	return patchWithStrategy(..., wrapWithAdmissionPatch(clk), ...)
    }
  ```
  
  WDYT? Since there would only be two strategies, I'm not sure it's worth it. I'm also okay leaving it as is.

- **pkg/workload/workload.go:1027** by [@IrvingMg](https://github.com/IrvingMg) — 2025-12-04T11:25:00Z
```diff
@@ -1016,32 +999,36 @@ func DefaultPatchAdmissionStatusOptions() *PatchAdmissionStatusOptions {
 //	    return updateFn(obj), nil
 //	}, WithLooseOnApply()) // disables strict mode for Patch Apply
 
-func WithLooseOnApply() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithLooseOnApply() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.StrictApply = false
 	}
 }
 
-// WithRetryOnConflictForPatch configures PatchAdmissionStatusOptions to enable retry logic on conflicts.
+// WithRetryOnConflictForPatch configures PatchStatusOptions to enable retry logic on conflicts.
 // Note: This only works with merge patches.
-func WithRetryOnConflictForPatch() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithRetryOnConflictForPatch() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.RetryOnConflictForPatch = true
 	}
 }
 
-// PatchAdmissionStatus updates the admission status of a workload.
-// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
-// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
-func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, clk clock.Clock, update func(*kueue.Workload) (bool, error), options ...PatchAdmissionStatusOption) error {
-	opts := DefaultPatchAdmissionStatusOptions()
+func patchStatusOptions(options []PatchStatusOption) *PatchStatusOptions {
+	opts := DefaultPatchStatusOptions()
 	for _, opt := range options {
 		opt(opts)
 	}
+	return opts
+}
+
+// patchStatus updates the status of a workload.
+// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
+// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
+func patchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update UpdateFunc, opts *PatchStatusOptions) error {
```

  Maybe we can simplify this by extracting the two different patch logics, resulting in something like:
  
  ```go
  func patchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update UpdateFunc, opts *PatchStatusOptions) error {
    	wlCopy := w.DeepCopy()
  
    	var err error
    	if features.Enabled(features.WorkloadRequestUseMergePatch) {
    		err = applyMergePatch(ctx, c, wlCopy, update, opts)
    	} else {
    		err = applySSAPatch(ctx, c, wlCopy, owner, update)
    	}
  
    	if err == nil {
    		wlCopy.DeepCopyInto(w)
    	}
    	return err
    }
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-04T11:32:01Z

- **pkg/workload/workload.go:1062** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-04T11:32:01Z
```diff
@@ -1055,15 +1042,39 @@ func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
+		err = c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
 	}
 	if err == nil {
 		wlCopy.DeepCopyInto(w)
 	}
 	return err
 }
 
+func PatchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update UpdateFunc, options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, w, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictApply)
+			wlPatch.DeepCopyInto(wl)
+		}
+		return update(wl)
+	}, opts)
+}
```

  Agreed, this is a reasonable approach. And we discussed it with @mykysha. While it could be split further, doing so would hurt readability. Maintaining just two focused functions is clearer and easier to manage than introducing four. WDYT?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-04T11:49:05Z

- **pkg/workload/workload.go:1027** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-04T11:49:05Z
```diff
@@ -1016,32 +999,36 @@ func DefaultPatchAdmissionStatusOptions() *PatchAdmissionStatusOptions {
 //	    return updateFn(obj), nil
 //	}, WithLooseOnApply()) // disables strict mode for Patch Apply
 
-func WithLooseOnApply() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithLooseOnApply() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.StrictApply = false
 	}
 }
 
-// WithRetryOnConflictForPatch configures PatchAdmissionStatusOptions to enable retry logic on conflicts.
+// WithRetryOnConflictForPatch configures PatchStatusOptions to enable retry logic on conflicts.
 // Note: This only works with merge patches.
-func WithRetryOnConflictForPatch() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithRetryOnConflictForPatch() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.RetryOnConflictForPatch = true
 	}
 }
 
-// PatchAdmissionStatus updates the admission status of a workload.
-// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
-// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
-func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, clk clock.Clock, update func(*kueue.Workload) (bool, error), options ...PatchAdmissionStatusOption) error {
-	opts := DefaultPatchAdmissionStatusOptions()
+func patchStatusOptions(options []PatchStatusOption) *PatchStatusOptions {
+	opts := DefaultPatchStatusOptions()
 	for _, opt := range options {
 		opt(opts)
 	}
+	return opts
+}
+
+// patchStatus updates the status of a workload.
+// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
+// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
+func patchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update UpdateFunc, opts *PatchStatusOptions) error {
```

  Done

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-12-04T12:47:10Z

- **pkg/workload/workload.go:1062** by [@IrvingMg](https://github.com/IrvingMg) — 2025-12-04T12:47:10Z
```diff
@@ -1055,15 +1042,39 @@ func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
+		err = c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
 	}
 	if err == nil {
 		wlCopy.DeepCopyInto(w)
 	}
 	return err
 }
 
+func PatchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update UpdateFunc, options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, w, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictApply)
+			wlPatch.DeepCopyInto(wl)
+		}
+		return update(wl)
+	}, opts)
+}
```

  SGTM.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-05T03:24:23Z

- **pkg/workload/workload.go:1027** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-05T03:24:23Z
```diff
@@ -1016,32 +999,36 @@ func DefaultPatchAdmissionStatusOptions() *PatchAdmissionStatusOptions {
 //	    return updateFn(obj), nil
 //	}, WithLooseOnApply()) // disables strict mode for Patch Apply
 
-func WithLooseOnApply() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithLooseOnApply() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.StrictApply = false
 	}
 }
 
-// WithRetryOnConflictForPatch configures PatchAdmissionStatusOptions to enable retry logic on conflicts.
+// WithRetryOnConflictForPatch configures PatchStatusOptions to enable retry logic on conflicts.
 // Note: This only works with merge patches.
-func WithRetryOnConflictForPatch() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithRetryOnConflictForPatch() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.RetryOnConflictForPatch = true
 	}
 }
 
-// PatchAdmissionStatus updates the admission status of a workload.
-// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
-// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
-func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, clk clock.Clock, update func(*kueue.Workload) (bool, error), options ...PatchAdmissionStatusOption) error {
-	opts := DefaultPatchAdmissionStatusOptions()
+func patchStatusOptions(options []PatchStatusOption) *PatchStatusOptions {
+	opts := DefaultPatchStatusOptions()
 	for _, opt := range options {
 		opt(opts)
 	}
+	return opts
+}
+
+// patchStatus updates the status of a workload.
+// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
+// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
+func patchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update UpdateFunc, opts *PatchStatusOptions) error {
```

  It’s very interesting that after introducing these changes https://github.com/kubernetes-sigs/kueue/pull/8063/commits/4573156a2ddac906e4490b0c5515dbef2a0ee7c7, we started seeing flakes more often. It looks like nothing special but the new function requires Go to execute additional function and allocate a new stack frame, which takes some time – not much, but apparently enough to introduce flakes. After reverting it, the flakes are gone.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-05T03:29:49Z

- **pkg/workload/workload.go:1027** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-05T03:29:48Z
```diff
@@ -1016,32 +999,36 @@ func DefaultPatchAdmissionStatusOptions() *PatchAdmissionStatusOptions {
 //	    return updateFn(obj), nil
 //	}, WithLooseOnApply()) // disables strict mode for Patch Apply
 
-func WithLooseOnApply() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithLooseOnApply() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.StrictApply = false
 	}
 }
 
-// WithRetryOnConflictForPatch configures PatchAdmissionStatusOptions to enable retry logic on conflicts.
+// WithRetryOnConflictForPatch configures PatchStatusOptions to enable retry logic on conflicts.
 // Note: This only works with merge patches.
-func WithRetryOnConflictForPatch() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithRetryOnConflictForPatch() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.RetryOnConflictForPatch = true
 	}
 }
 
-// PatchAdmissionStatus updates the admission status of a workload.
-// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
-// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
-func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, clk clock.Clock, update func(*kueue.Workload) (bool, error), options ...PatchAdmissionStatusOption) error {
-	opts := DefaultPatchAdmissionStatusOptions()
+func patchStatusOptions(options []PatchStatusOption) *PatchStatusOptions {
+	opts := DefaultPatchStatusOptions()
 	for _, opt := range options {
 		opt(opts)
 	}
+	return opts
+}
+
+// patchStatus updates the status of a workload.
+// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
+// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
+func patchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update UpdateFunc, opts *PatchStatusOptions) error {
```

  @IrvingMg WDYT about reverting these changes? Of course, we still need to fix the flakes, but I think it’s better to keep the performance high. I don’t think we’ll need to reuse mergePatchStatus and applyPatchStatus, but if we ever do, we can always reintroduce them.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-12-05T10:31:58Z

- **pkg/workload/workload.go:1027** by [@IrvingMg](https://github.com/IrvingMg) — 2025-12-05T10:31:57Z
```diff
@@ -1016,32 +999,36 @@ func DefaultPatchAdmissionStatusOptions() *PatchAdmissionStatusOptions {
 //	    return updateFn(obj), nil
 //	}, WithLooseOnApply()) // disables strict mode for Patch Apply
 
-func WithLooseOnApply() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithLooseOnApply() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.StrictApply = false
 	}
 }
 
-// WithRetryOnConflictForPatch configures PatchAdmissionStatusOptions to enable retry logic on conflicts.
+// WithRetryOnConflictForPatch configures PatchStatusOptions to enable retry logic on conflicts.
 // Note: This only works with merge patches.
-func WithRetryOnConflictForPatch() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithRetryOnConflictForPatch() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.RetryOnConflictForPatch = true
 	}
 }
 
-// PatchAdmissionStatus updates the admission status of a workload.
-// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
-// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
-func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, clk clock.Clock, update func(*kueue.Workload) (bool, error), options ...PatchAdmissionStatusOption) error {
-	opts := DefaultPatchAdmissionStatusOptions()
+func patchStatusOptions(options []PatchStatusOption) *PatchStatusOptions {
+	opts := DefaultPatchStatusOptions()
 	for _, opt := range options {
 		opt(opts)
 	}
+	return opts
+}
+
+// patchStatus updates the status of a workload.
+// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
+// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
+func patchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update UpdateFunc, opts *PatchStatusOptions) error {
```

  Interesting… yeah, if it’s impacting performance, then I agree with reverting these changes.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-05T10:54:44Z

- **pkg/workload/workload.go:1027** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-05T10:54:44Z
```diff
@@ -1016,32 +999,36 @@ func DefaultPatchAdmissionStatusOptions() *PatchAdmissionStatusOptions {
 //	    return updateFn(obj), nil
 //	}, WithLooseOnApply()) // disables strict mode for Patch Apply
 
-func WithLooseOnApply() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithLooseOnApply() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.StrictApply = false
 	}
 }
 
-// WithRetryOnConflictForPatch configures PatchAdmissionStatusOptions to enable retry logic on conflicts.
+// WithRetryOnConflictForPatch configures PatchStatusOptions to enable retry logic on conflicts.
 // Note: This only works with merge patches.
-func WithRetryOnConflictForPatch() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithRetryOnConflictForPatch() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.RetryOnConflictForPatch = true
 	}
 }
 
-// PatchAdmissionStatus updates the admission status of a workload.
-// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
-// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
-func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, clk clock.Clock, update func(*kueue.Workload) (bool, error), options ...PatchAdmissionStatusOption) error {
-	opts := DefaultPatchAdmissionStatusOptions()
+func patchStatusOptions(options []PatchStatusOption) *PatchStatusOptions {
+	opts := DefaultPatchStatusOptions()
 	for _, opt := range options {
 		opt(opts)
 	}
+	return opts
+}
+
+// patchStatus updates the status of a workload.
+// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
+// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
+func patchStatus(ctx context.Context, c client.Client, w *kueue.Workload, owner client.FieldOwner, update UpdateFunc, opts *PatchStatusOptions) error {
```

  Done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-05T17:46:24Z

- **pkg/workload/workload.go:1074** by [@mimowo](https://github.com/mimowo) — 2025-12-05T17:46:24Z
```diff
@@ -1016,52 +999,83 @@ func DefaultPatchAdmissionStatusOptions() *PatchAdmissionStatusOptions {
 //	    return updateFn(obj), nil
 //	}, WithLooseOnApply()) // disables strict mode for Patch Apply
 
-func WithLooseOnApply() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithLooseOnApply() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.StrictApply = false
 	}
 }
 
-// WithRetryOnConflictForPatch configures PatchAdmissionStatusOptions to enable retry logic on conflicts.
+// WithRetryOnConflictForPatch configures PatchStatusOptions to enable retry logic on conflicts.
 // Note: This only works with merge patches.
-func WithRetryOnConflictForPatch() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithRetryOnConflictForPatch() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.RetryOnConflictForPatch = true
 	}
 }
 
-// PatchAdmissionStatus updates the admission status of a workload.
-// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
-// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
-func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, clk clock.Clock, update func(*kueue.Workload) (bool, error), options ...PatchAdmissionStatusOption) error {
-	opts := DefaultPatchAdmissionStatusOptions()
+func patchStatusOptions(options []PatchStatusOption) *PatchStatusOptions {
+	opts := DefaultPatchStatusOptions()
 	for _, opt := range options {
 		opt(opts)
 	}
-	var err error
-	wlCopy := w.DeepCopy()
+	return opts
+}
+
+// patchStatus updates the status of a workload.
+// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
+// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
+func patchStatus(ctx context.Context, c client.Client, wl *kueue.Workload, owner client.FieldOwner, update UpdateFunc, opts *PatchStatusOptions) error {
+	wlCopy := wl.DeepCopy()
 	if features.Enabled(features.WorkloadRequestUseMergePatch) {
-		var patchOptions []clientutil.PatchOption
+		patchOptions := make([]clientutil.PatchOption, 0, 2)
 		if !opts.StrictPatch {
 			patchOptions = append(patchOptions, clientutil.WithLoose())
 		}
 		if opts.RetryOnConflictForPatch {
 			patchOptions = append(patchOptions, clientutil.WithRetryOnConflict())
 		}
-		err = clientutil.PatchStatus(ctx, c, wlCopy, func() (bool, error) {
+		err := clientutil.PatchStatus(ctx, c, wlCopy, func() (bool, error) {
 			return update(wlCopy)
 		}, patchOptions...)
+		if err != nil {
+			return err
+		}
 	} else {
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
-	}
-	if err == nil {
-		wlCopy.DeepCopyInto(w)
+		err := c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
+		if err != nil {
+			return err
+		}
 	}
-	return err
+	wlCopy.DeepCopyInto(wl)
+	return nil
+}
+
+func PatchStatus(ctx context.Context, c client.Client, wl *kueue.Workload, owner client.FieldOwner, update UpdateFunc, options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, wl, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictApply)
+			wlPatch.DeepCopyInto(wl)
+		}
+		return update(wl)
+	}, opts)
+}
+
+func PatchAdmissionStatus(ctx context.Context, c client.Client, wl *kueue.Workload, clk clock.Clock, update UpdateFunc, options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, wl, constants.AdmissionName, func(wl *kueue.Workload) (bool, error) {
+		if updated, err := update(wl); err != nil || !updated {
+			return updated, err
+		}
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := PrepareWorkloadPatch(wl, opts.StrictApply, clk)
```

  I think we need to rename this function, or even remove and just use 
  ```golang
  wlPatch := BaseSSAWorkload(wl, opts.StrictApply)
  PrepareAdmissionPatch(w, wlCopy, clk)
  return wlCopy
  ```
  where PrepareAdmissionPatch encapsulates the functions:
  ```
  admissionStatusPatch
  admissionChecksStatusPatch
  ```
  It is rather a follow up

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-05T17:49:17Z

- **pkg/workload/workload.go:1063** by [@mimowo](https://github.com/mimowo) — 2025-12-05T17:49:17Z
```diff
@@ -1016,52 +999,83 @@ func DefaultPatchAdmissionStatusOptions() *PatchAdmissionStatusOptions {
 //	    return updateFn(obj), nil
 //	}, WithLooseOnApply()) // disables strict mode for Patch Apply
 
-func WithLooseOnApply() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithLooseOnApply() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.StrictApply = false
 	}
 }
 
-// WithRetryOnConflictForPatch configures PatchAdmissionStatusOptions to enable retry logic on conflicts.
+// WithRetryOnConflictForPatch configures PatchStatusOptions to enable retry logic on conflicts.
 // Note: This only works with merge patches.
-func WithRetryOnConflictForPatch() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithRetryOnConflictForPatch() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.RetryOnConflictForPatch = true
 	}
 }
 
-// PatchAdmissionStatus updates the admission status of a workload.
-// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
-// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
-func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, clk clock.Clock, update func(*kueue.Workload) (bool, error), options ...PatchAdmissionStatusOption) error {
-	opts := DefaultPatchAdmissionStatusOptions()
+func patchStatusOptions(options []PatchStatusOption) *PatchStatusOptions {
+	opts := DefaultPatchStatusOptions()
 	for _, opt := range options {
 		opt(opts)
 	}
-	var err error
-	wlCopy := w.DeepCopy()
+	return opts
+}
+
+// patchStatus updates the status of a workload.
+// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
+// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
+func patchStatus(ctx context.Context, c client.Client, wl *kueue.Workload, owner client.FieldOwner, update UpdateFunc, opts *PatchStatusOptions) error {
+	wlCopy := wl.DeepCopy()
 	if features.Enabled(features.WorkloadRequestUseMergePatch) {
-		var patchOptions []clientutil.PatchOption
+		patchOptions := make([]clientutil.PatchOption, 0, 2)
 		if !opts.StrictPatch {
 			patchOptions = append(patchOptions, clientutil.WithLoose())
 		}
 		if opts.RetryOnConflictForPatch {
 			patchOptions = append(patchOptions, clientutil.WithRetryOnConflict())
 		}
-		err = clientutil.PatchStatus(ctx, c, wlCopy, func() (bool, error) {
+		err := clientutil.PatchStatus(ctx, c, wlCopy, func() (bool, error) {
 			return update(wlCopy)
 		}, patchOptions...)
+		if err != nil {
+			return err
+		}
 	} else {
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
-	}
-	if err == nil {
-		wlCopy.DeepCopyInto(w)
+		err := c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
+		if err != nil {
+			return err
+		}
 	}
-	return err
+	wlCopy.DeepCopyInto(wl)
+	return nil
+}
+
+func PatchStatus(ctx context.Context, c client.Client, wl *kueue.Workload, owner client.FieldOwner, update UpdateFunc, options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, wl, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictApply)
+			wlPatch.DeepCopyInto(wl)
+		}
+		return update(wl)
```

  Why is the update here at the bottom, and for `PatchAdmissionStatus` it is at the top?
  
  I think it is actually slightly better to place it at the top, then it can make adjustments based on the original workload. 
  
  Also, I'm wondering if we could then commonize calling update and place it inside `patchStatus`, wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-05T17:50:55Z

LGTM overall, thank you for commonizing. It will make it easier to maintain over time. I have one comment to potentially commonize the code even more https://github.com/kubernetes-sigs/kueue/pull/8063/files#r2593474604
It could also be a follow up.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-05T17:52:12Z

- **pkg/workload/workload.go:1063** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-05T17:52:11Z
```diff
@@ -1016,52 +999,83 @@ func DefaultPatchAdmissionStatusOptions() *PatchAdmissionStatusOptions {
 //	    return updateFn(obj), nil
 //	}, WithLooseOnApply()) // disables strict mode for Patch Apply
 
-func WithLooseOnApply() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithLooseOnApply() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.StrictApply = false
 	}
 }
 
-// WithRetryOnConflictForPatch configures PatchAdmissionStatusOptions to enable retry logic on conflicts.
+// WithRetryOnConflictForPatch configures PatchStatusOptions to enable retry logic on conflicts.
 // Note: This only works with merge patches.
-func WithRetryOnConflictForPatch() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithRetryOnConflictForPatch() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.RetryOnConflictForPatch = true
 	}
 }
 
-// PatchAdmissionStatus updates the admission status of a workload.
-// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
-// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
-func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, clk clock.Clock, update func(*kueue.Workload) (bool, error), options ...PatchAdmissionStatusOption) error {
-	opts := DefaultPatchAdmissionStatusOptions()
+func patchStatusOptions(options []PatchStatusOption) *PatchStatusOptions {
+	opts := DefaultPatchStatusOptions()
 	for _, opt := range options {
 		opt(opts)
 	}
-	var err error
-	wlCopy := w.DeepCopy()
+	return opts
+}
+
+// patchStatus updates the status of a workload.
+// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
+// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
+func patchStatus(ctx context.Context, c client.Client, wl *kueue.Workload, owner client.FieldOwner, update UpdateFunc, opts *PatchStatusOptions) error {
+	wlCopy := wl.DeepCopy()
 	if features.Enabled(features.WorkloadRequestUseMergePatch) {
-		var patchOptions []clientutil.PatchOption
+		patchOptions := make([]clientutil.PatchOption, 0, 2)
 		if !opts.StrictPatch {
 			patchOptions = append(patchOptions, clientutil.WithLoose())
 		}
 		if opts.RetryOnConflictForPatch {
 			patchOptions = append(patchOptions, clientutil.WithRetryOnConflict())
 		}
-		err = clientutil.PatchStatus(ctx, c, wlCopy, func() (bool, error) {
+		err := clientutil.PatchStatus(ctx, c, wlCopy, func() (bool, error) {
 			return update(wlCopy)
 		}, patchOptions...)
+		if err != nil {
+			return err
+		}
 	} else {
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
-	}
-	if err == nil {
-		wlCopy.DeepCopyInto(w)
+		err := c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
+		if err != nil {
+			return err
+		}
 	}
-	return err
+	wlCopy.DeepCopyInto(wl)
+	return nil
+}
+
+func PatchStatus(ctx context.Context, c client.Client, wl *kueue.Workload, owner client.FieldOwner, update UpdateFunc, options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, wl, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictApply)
+			wlPatch.DeepCopyInto(wl)
+		}
+		return update(wl)
```

  It will not work. In some places we use other fields to construct the status (for example .spec.localQueue). If we move it before the update, we will get a lot of errors because the fields will be empty.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-05T17:55:19Z

- **pkg/workload/workload.go:1074** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-05T17:55:19Z
```diff
@@ -1016,52 +999,83 @@ func DefaultPatchAdmissionStatusOptions() *PatchAdmissionStatusOptions {
 //	    return updateFn(obj), nil
 //	}, WithLooseOnApply()) // disables strict mode for Patch Apply
 
-func WithLooseOnApply() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithLooseOnApply() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.StrictApply = false
 	}
 }
 
-// WithRetryOnConflictForPatch configures PatchAdmissionStatusOptions to enable retry logic on conflicts.
+// WithRetryOnConflictForPatch configures PatchStatusOptions to enable retry logic on conflicts.
 // Note: This only works with merge patches.
-func WithRetryOnConflictForPatch() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithRetryOnConflictForPatch() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.RetryOnConflictForPatch = true
 	}
 }
 
-// PatchAdmissionStatus updates the admission status of a workload.
-// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
-// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
-func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, clk clock.Clock, update func(*kueue.Workload) (bool, error), options ...PatchAdmissionStatusOption) error {
-	opts := DefaultPatchAdmissionStatusOptions()
+func patchStatusOptions(options []PatchStatusOption) *PatchStatusOptions {
+	opts := DefaultPatchStatusOptions()
 	for _, opt := range options {
 		opt(opts)
 	}
-	var err error
-	wlCopy := w.DeepCopy()
+	return opts
+}
+
+// patchStatus updates the status of a workload.
+// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
+// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
+func patchStatus(ctx context.Context, c client.Client, wl *kueue.Workload, owner client.FieldOwner, update UpdateFunc, opts *PatchStatusOptions) error {
+	wlCopy := wl.DeepCopy()
 	if features.Enabled(features.WorkloadRequestUseMergePatch) {
-		var patchOptions []clientutil.PatchOption
+		patchOptions := make([]clientutil.PatchOption, 0, 2)
 		if !opts.StrictPatch {
 			patchOptions = append(patchOptions, clientutil.WithLoose())
 		}
 		if opts.RetryOnConflictForPatch {
 			patchOptions = append(patchOptions, clientutil.WithRetryOnConflict())
 		}
-		err = clientutil.PatchStatus(ctx, c, wlCopy, func() (bool, error) {
+		err := clientutil.PatchStatus(ctx, c, wlCopy, func() (bool, error) {
 			return update(wlCopy)
 		}, patchOptions...)
+		if err != nil {
+			return err
+		}
 	} else {
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
-	}
-	if err == nil {
-		wlCopy.DeepCopyInto(w)
+		err := c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
+		if err != nil {
+			return err
+		}
 	}
-	return err
+	wlCopy.DeepCopyInto(wl)
+	return nil
+}
+
+func PatchStatus(ctx context.Context, c client.Client, wl *kueue.Workload, owner client.FieldOwner, update UpdateFunc, options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, wl, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictApply)
+			wlPatch.DeepCopyInto(wl)
+		}
+		return update(wl)
+	}, opts)
+}
+
+func PatchAdmissionStatus(ctx context.Context, c client.Client, wl *kueue.Workload, clk clock.Clock, update UpdateFunc, options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, wl, constants.AdmissionName, func(wl *kueue.Workload) (bool, error) {
+		if updated, err := update(wl); err != nil || !updated {
+			return updated, err
+		}
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := PrepareWorkloadPatch(wl, opts.StrictApply, clk)
```

  We have other places that use this function. After fully migrating to this approach, I think we can remove it entirely.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-05T18:01:09Z

- **pkg/workload/workload.go:1063** by [@mimowo](https://github.com/mimowo) — 2025-12-05T18:01:09Z
```diff
@@ -1016,52 +999,83 @@ func DefaultPatchAdmissionStatusOptions() *PatchAdmissionStatusOptions {
 //	    return updateFn(obj), nil
 //	}, WithLooseOnApply()) // disables strict mode for Patch Apply
 
-func WithLooseOnApply() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithLooseOnApply() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.StrictApply = false
 	}
 }
 
-// WithRetryOnConflictForPatch configures PatchAdmissionStatusOptions to enable retry logic on conflicts.
+// WithRetryOnConflictForPatch configures PatchStatusOptions to enable retry logic on conflicts.
 // Note: This only works with merge patches.
-func WithRetryOnConflictForPatch() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithRetryOnConflictForPatch() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.RetryOnConflictForPatch = true
 	}
 }
 
-// PatchAdmissionStatus updates the admission status of a workload.
-// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
-// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
-func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, clk clock.Clock, update func(*kueue.Workload) (bool, error), options ...PatchAdmissionStatusOption) error {
-	opts := DefaultPatchAdmissionStatusOptions()
+func patchStatusOptions(options []PatchStatusOption) *PatchStatusOptions {
+	opts := DefaultPatchStatusOptions()
 	for _, opt := range options {
 		opt(opts)
 	}
-	var err error
-	wlCopy := w.DeepCopy()
+	return opts
+}
+
+// patchStatus updates the status of a workload.
+// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
+// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
+func patchStatus(ctx context.Context, c client.Client, wl *kueue.Workload, owner client.FieldOwner, update UpdateFunc, opts *PatchStatusOptions) error {
+	wlCopy := wl.DeepCopy()
 	if features.Enabled(features.WorkloadRequestUseMergePatch) {
-		var patchOptions []clientutil.PatchOption
+		patchOptions := make([]clientutil.PatchOption, 0, 2)
 		if !opts.StrictPatch {
 			patchOptions = append(patchOptions, clientutil.WithLoose())
 		}
 		if opts.RetryOnConflictForPatch {
 			patchOptions = append(patchOptions, clientutil.WithRetryOnConflict())
 		}
-		err = clientutil.PatchStatus(ctx, c, wlCopy, func() (bool, error) {
+		err := clientutil.PatchStatus(ctx, c, wlCopy, func() (bool, error) {
 			return update(wlCopy)
 		}, patchOptions...)
+		if err != nil {
+			return err
+		}
 	} else {
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
-	}
-	if err == nil {
-		wlCopy.DeepCopyInto(w)
+		err := c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
+		if err != nil {
+			return err
+		}
 	}
-	return err
+	wlCopy.DeepCopyInto(wl)
+	return nil
+}
+
+func PatchStatus(ctx context.Context, c client.Client, wl *kueue.Workload, owner client.FieldOwner, update UpdateFunc, options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, wl, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictApply)
+			wlPatch.DeepCopyInto(wl)
+		}
+		return update(wl)
```

  Right but my point is to move calling the `update` before `BaseSSAWorkload` in both cases, would this not work either?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-05T18:05:27Z

- **pkg/workload/workload.go:1063** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-05T18:05:26Z
```diff
@@ -1016,52 +999,83 @@ func DefaultPatchAdmissionStatusOptions() *PatchAdmissionStatusOptions {
 //	    return updateFn(obj), nil
 //	}, WithLooseOnApply()) // disables strict mode for Patch Apply
 
-func WithLooseOnApply() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithLooseOnApply() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.StrictApply = false
 	}
 }
 
-// WithRetryOnConflictForPatch configures PatchAdmissionStatusOptions to enable retry logic on conflicts.
+// WithRetryOnConflictForPatch configures PatchStatusOptions to enable retry logic on conflicts.
 // Note: This only works with merge patches.
-func WithRetryOnConflictForPatch() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithRetryOnConflictForPatch() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.RetryOnConflictForPatch = true
 	}
 }
 
-// PatchAdmissionStatus updates the admission status of a workload.
-// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
-// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
-func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, clk clock.Clock, update func(*kueue.Workload) (bool, error), options ...PatchAdmissionStatusOption) error {
-	opts := DefaultPatchAdmissionStatusOptions()
+func patchStatusOptions(options []PatchStatusOption) *PatchStatusOptions {
+	opts := DefaultPatchStatusOptions()
 	for _, opt := range options {
 		opt(opts)
 	}
-	var err error
-	wlCopy := w.DeepCopy()
+	return opts
+}
+
+// patchStatus updates the status of a workload.
+// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
+// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
+func patchStatus(ctx context.Context, c client.Client, wl *kueue.Workload, owner client.FieldOwner, update UpdateFunc, opts *PatchStatusOptions) error {
+	wlCopy := wl.DeepCopy()
 	if features.Enabled(features.WorkloadRequestUseMergePatch) {
-		var patchOptions []clientutil.PatchOption
+		patchOptions := make([]clientutil.PatchOption, 0, 2)
 		if !opts.StrictPatch {
 			patchOptions = append(patchOptions, clientutil.WithLoose())
 		}
 		if opts.RetryOnConflictForPatch {
 			patchOptions = append(patchOptions, clientutil.WithRetryOnConflict())
 		}
-		err = clientutil.PatchStatus(ctx, c, wlCopy, func() (bool, error) {
+		err := clientutil.PatchStatus(ctx, c, wlCopy, func() (bool, error) {
 			return update(wlCopy)
 		}, patchOptions...)
+		if err != nil {
+			return err
+		}
 	} else {
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
-	}
-	if err == nil {
-		wlCopy.DeepCopyInto(w)
+		err := c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
+		if err != nil {
+			return err
+		}
 	}
-	return err
+	wlCopy.DeepCopyInto(wl)
+	return nil
+}
+
+func PatchStatus(ctx context.Context, c client.Client, wl *kueue.Workload, owner client.FieldOwner, update UpdateFunc, options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, wl, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictApply)
+			wlPatch.DeepCopyInto(wl)
+		}
+		return update(wl)
```

  Unfortunately no. 
  
  https://github.com/kubernetes-sigs/kueue/blob/98c941f944b9c316232f69a46bc2f66580f4ce35/pkg/workload/workload.go#L735-L757
  
  Base BaseSSAWorkload() creates almost empty patch.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-05T18:05:48Z

- **pkg/workload/workload.go:1063** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-05T18:05:48Z
```diff
@@ -1016,52 +999,83 @@ func DefaultPatchAdmissionStatusOptions() *PatchAdmissionStatusOptions {
 //	    return updateFn(obj), nil
 //	}, WithLooseOnApply()) // disables strict mode for Patch Apply
 
-func WithLooseOnApply() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithLooseOnApply() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.StrictApply = false
 	}
 }
 
-// WithRetryOnConflictForPatch configures PatchAdmissionStatusOptions to enable retry logic on conflicts.
+// WithRetryOnConflictForPatch configures PatchStatusOptions to enable retry logic on conflicts.
 // Note: This only works with merge patches.
-func WithRetryOnConflictForPatch() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithRetryOnConflictForPatch() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.RetryOnConflictForPatch = true
 	}
 }
 
-// PatchAdmissionStatus updates the admission status of a workload.
-// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
-// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
-func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, clk clock.Clock, update func(*kueue.Workload) (bool, error), options ...PatchAdmissionStatusOption) error {
-	opts := DefaultPatchAdmissionStatusOptions()
+func patchStatusOptions(options []PatchStatusOption) *PatchStatusOptions {
+	opts := DefaultPatchStatusOptions()
 	for _, opt := range options {
 		opt(opts)
 	}
-	var err error
-	wlCopy := w.DeepCopy()
+	return opts
+}
+
+// patchStatus updates the status of a workload.
+// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
+// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
+func patchStatus(ctx context.Context, c client.Client, wl *kueue.Workload, owner client.FieldOwner, update UpdateFunc, opts *PatchStatusOptions) error {
+	wlCopy := wl.DeepCopy()
 	if features.Enabled(features.WorkloadRequestUseMergePatch) {
-		var patchOptions []clientutil.PatchOption
+		patchOptions := make([]clientutil.PatchOption, 0, 2)
 		if !opts.StrictPatch {
 			patchOptions = append(patchOptions, clientutil.WithLoose())
 		}
 		if opts.RetryOnConflictForPatch {
 			patchOptions = append(patchOptions, clientutil.WithRetryOnConflict())
 		}
-		err = clientutil.PatchStatus(ctx, c, wlCopy, func() (bool, error) {
+		err := clientutil.PatchStatus(ctx, c, wlCopy, func() (bool, error) {
 			return update(wlCopy)
 		}, patchOptions...)
+		if err != nil {
+			return err
+		}
 	} else {
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
-	}
-	if err == nil {
-		wlCopy.DeepCopyInto(w)
+		err := c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
+		if err != nil {
+			return err
+		}
 	}
-	return err
+	wlCopy.DeepCopyInto(wl)
+	return nil
+}
+
+func PatchStatus(ctx context.Context, c client.Client, wl *kueue.Workload, owner client.FieldOwner, update UpdateFunc, options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, wl, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictApply)
+			wlPatch.DeepCopyInto(wl)
+		}
+		return update(wl)
```

  So after patch creation we will loose all changes.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-05T18:25:26Z

- **pkg/workload/workload.go:1063** by [@mimowo](https://github.com/mimowo) — 2025-12-05T18:25:26Z
```diff
@@ -1016,52 +999,83 @@ func DefaultPatchAdmissionStatusOptions() *PatchAdmissionStatusOptions {
 //	    return updateFn(obj), nil
 //	}, WithLooseOnApply()) // disables strict mode for Patch Apply
 
-func WithLooseOnApply() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithLooseOnApply() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.StrictApply = false
 	}
 }
 
-// WithRetryOnConflictForPatch configures PatchAdmissionStatusOptions to enable retry logic on conflicts.
+// WithRetryOnConflictForPatch configures PatchStatusOptions to enable retry logic on conflicts.
 // Note: This only works with merge patches.
-func WithRetryOnConflictForPatch() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithRetryOnConflictForPatch() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.RetryOnConflictForPatch = true
 	}
 }
 
-// PatchAdmissionStatus updates the admission status of a workload.
-// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
-// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
-func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, clk clock.Clock, update func(*kueue.Workload) (bool, error), options ...PatchAdmissionStatusOption) error {
-	opts := DefaultPatchAdmissionStatusOptions()
+func patchStatusOptions(options []PatchStatusOption) *PatchStatusOptions {
+	opts := DefaultPatchStatusOptions()
 	for _, opt := range options {
 		opt(opts)
 	}
-	var err error
-	wlCopy := w.DeepCopy()
+	return opts
+}
+
+// patchStatus updates the status of a workload.
+// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
+// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
+func patchStatus(ctx context.Context, c client.Client, wl *kueue.Workload, owner client.FieldOwner, update UpdateFunc, opts *PatchStatusOptions) error {
+	wlCopy := wl.DeepCopy()
 	if features.Enabled(features.WorkloadRequestUseMergePatch) {
-		var patchOptions []clientutil.PatchOption
+		patchOptions := make([]clientutil.PatchOption, 0, 2)
 		if !opts.StrictPatch {
 			patchOptions = append(patchOptions, clientutil.WithLoose())
 		}
 		if opts.RetryOnConflictForPatch {
 			patchOptions = append(patchOptions, clientutil.WithRetryOnConflict())
 		}
-		err = clientutil.PatchStatus(ctx, c, wlCopy, func() (bool, error) {
+		err := clientutil.PatchStatus(ctx, c, wlCopy, func() (bool, error) {
 			return update(wlCopy)
 		}, patchOptions...)
+		if err != nil {
+			return err
+		}
 	} else {
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
-	}
-	if err == nil {
-		wlCopy.DeepCopyInto(w)
+		err := c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
+		if err != nil {
+			return err
+		}
 	}
-	return err
+	wlCopy.DeepCopyInto(wl)
+	return nil
+}
+
+func PatchStatus(ctx context.Context, c client.Client, wl *kueue.Workload, owner client.FieldOwner, update UpdateFunc, options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, wl, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictApply)
+			wlPatch.DeepCopyInto(wl)
+		}
+		return update(wl)
```

  Do you mean that calling `wlPatch.DeepCopyInto(wl)` would zero all the workload fields in `wl` other than those in the `wlPatch`?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-05T18:28:30Z

- **pkg/workload/workload.go:1063** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-05T18:28:30Z
```diff
@@ -1016,52 +999,83 @@ func DefaultPatchAdmissionStatusOptions() *PatchAdmissionStatusOptions {
 //	    return updateFn(obj), nil
 //	}, WithLooseOnApply()) // disables strict mode for Patch Apply
 
-func WithLooseOnApply() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithLooseOnApply() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.StrictApply = false
 	}
 }
 
-// WithRetryOnConflictForPatch configures PatchAdmissionStatusOptions to enable retry logic on conflicts.
+// WithRetryOnConflictForPatch configures PatchStatusOptions to enable retry logic on conflicts.
 // Note: This only works with merge patches.
-func WithRetryOnConflictForPatch() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithRetryOnConflictForPatch() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.RetryOnConflictForPatch = true
 	}
 }
 
-// PatchAdmissionStatus updates the admission status of a workload.
-// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
-// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
-func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, clk clock.Clock, update func(*kueue.Workload) (bool, error), options ...PatchAdmissionStatusOption) error {
-	opts := DefaultPatchAdmissionStatusOptions()
+func patchStatusOptions(options []PatchStatusOption) *PatchStatusOptions {
+	opts := DefaultPatchStatusOptions()
 	for _, opt := range options {
 		opt(opts)
 	}
-	var err error
-	wlCopy := w.DeepCopy()
+	return opts
+}
+
+// patchStatus updates the status of a workload.
+// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
+// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
+func patchStatus(ctx context.Context, c client.Client, wl *kueue.Workload, owner client.FieldOwner, update UpdateFunc, opts *PatchStatusOptions) error {
+	wlCopy := wl.DeepCopy()
 	if features.Enabled(features.WorkloadRequestUseMergePatch) {
-		var patchOptions []clientutil.PatchOption
+		patchOptions := make([]clientutil.PatchOption, 0, 2)
 		if !opts.StrictPatch {
 			patchOptions = append(patchOptions, clientutil.WithLoose())
 		}
 		if opts.RetryOnConflictForPatch {
 			patchOptions = append(patchOptions, clientutil.WithRetryOnConflict())
 		}
-		err = clientutil.PatchStatus(ctx, c, wlCopy, func() (bool, error) {
+		err := clientutil.PatchStatus(ctx, c, wlCopy, func() (bool, error) {
 			return update(wlCopy)
 		}, patchOptions...)
+		if err != nil {
+			return err
+		}
 	} else {
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
-	}
-	if err == nil {
-		wlCopy.DeepCopyInto(w)
+		err := c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
+		if err != nil {
+			return err
+		}
 	}
-	return err
+	wlCopy.DeepCopyInto(wl)
+	return nil
+}
+
+func PatchStatus(ctx context.Context, c client.Client, wl *kueue.Workload, owner client.FieldOwner, update UpdateFunc, options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, wl, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictApply)
+			wlPatch.DeepCopyInto(wl)
+		}
+		return update(wl)
```

  Yes, exactly – we will replace all fields with zero values after running wlPatch.DeepCopyInto(wl).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-05T18:38:58Z

- **pkg/workload/workload.go:1063** by [@mimowo](https://github.com/mimowo) — 2025-12-05T18:38:58Z
```diff
@@ -1016,52 +999,83 @@ func DefaultPatchAdmissionStatusOptions() *PatchAdmissionStatusOptions {
 //	    return updateFn(obj), nil
 //	}, WithLooseOnApply()) // disables strict mode for Patch Apply
 
-func WithLooseOnApply() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithLooseOnApply() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.StrictApply = false
 	}
 }
 
-// WithRetryOnConflictForPatch configures PatchAdmissionStatusOptions to enable retry logic on conflicts.
+// WithRetryOnConflictForPatch configures PatchStatusOptions to enable retry logic on conflicts.
 // Note: This only works with merge patches.
-func WithRetryOnConflictForPatch() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithRetryOnConflictForPatch() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.RetryOnConflictForPatch = true
 	}
 }
 
-// PatchAdmissionStatus updates the admission status of a workload.
-// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
-// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
-func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, clk clock.Clock, update func(*kueue.Workload) (bool, error), options ...PatchAdmissionStatusOption) error {
-	opts := DefaultPatchAdmissionStatusOptions()
+func patchStatusOptions(options []PatchStatusOption) *PatchStatusOptions {
+	opts := DefaultPatchStatusOptions()
 	for _, opt := range options {
 		opt(opts)
 	}
-	var err error
-	wlCopy := w.DeepCopy()
+	return opts
+}
+
+// patchStatus updates the status of a workload.
+// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
+// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
+func patchStatus(ctx context.Context, c client.Client, wl *kueue.Workload, owner client.FieldOwner, update UpdateFunc, opts *PatchStatusOptions) error {
+	wlCopy := wl.DeepCopy()
 	if features.Enabled(features.WorkloadRequestUseMergePatch) {
-		var patchOptions []clientutil.PatchOption
+		patchOptions := make([]clientutil.PatchOption, 0, 2)
 		if !opts.StrictPatch {
 			patchOptions = append(patchOptions, clientutil.WithLoose())
 		}
 		if opts.RetryOnConflictForPatch {
 			patchOptions = append(patchOptions, clientutil.WithRetryOnConflict())
 		}
-		err = clientutil.PatchStatus(ctx, c, wlCopy, func() (bool, error) {
+		err := clientutil.PatchStatus(ctx, c, wlCopy, func() (bool, error) {
 			return update(wlCopy)
 		}, patchOptions...)
+		if err != nil {
+			return err
+		}
 	} else {
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
-	}
-	if err == nil {
-		wlCopy.DeepCopyInto(w)
+		err := c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
+		if err != nil {
+			return err
+		}
 	}
-	return err
+	wlCopy.DeepCopyInto(wl)
+	return nil
+}
+
+func PatchStatus(ctx context.Context, c client.Client, wl *kueue.Workload, owner client.FieldOwner, update UpdateFunc, options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, wl, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictApply)
+			wlPatch.DeepCopyInto(wl)
+		}
+		return update(wl)
```

  Gotcha. I think to make it more generic we may actually make the `update` function to accept two workloads `(origWorkload, patchWorkload)`. Then, if all fields are needed, then it would use `origWorkload`, but it would record changes to `patchWorkload`. However, this may not be very easy because many functions we have don't make this distinction.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-05T18:39:50Z

- **pkg/workload/workload.go:1063** by [@mimowo](https://github.com/mimowo) — 2025-12-05T18:39:50Z
```diff
@@ -1016,52 +999,83 @@ func DefaultPatchAdmissionStatusOptions() *PatchAdmissionStatusOptions {
 //	    return updateFn(obj), nil
 //	}, WithLooseOnApply()) // disables strict mode for Patch Apply
 
-func WithLooseOnApply() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithLooseOnApply() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.StrictApply = false
 	}
 }
 
-// WithRetryOnConflictForPatch configures PatchAdmissionStatusOptions to enable retry logic on conflicts.
+// WithRetryOnConflictForPatch configures PatchStatusOptions to enable retry logic on conflicts.
 // Note: This only works with merge patches.
-func WithRetryOnConflictForPatch() PatchAdmissionStatusOption {
-	return func(o *PatchAdmissionStatusOptions) {
+func WithRetryOnConflictForPatch() PatchStatusOption {
+	return func(o *PatchStatusOptions) {
 		o.RetryOnConflictForPatch = true
 	}
 }
 
-// PatchAdmissionStatus updates the admission status of a workload.
-// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
-// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
-func PatchAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workload, clk clock.Clock, update func(*kueue.Workload) (bool, error), options ...PatchAdmissionStatusOption) error {
-	opts := DefaultPatchAdmissionStatusOptions()
+func patchStatusOptions(options []PatchStatusOption) *PatchStatusOptions {
+	opts := DefaultPatchStatusOptions()
 	for _, opt := range options {
 		opt(opts)
 	}
-	var err error
-	wlCopy := w.DeepCopy()
+	return opts
+}
+
+// patchStatus updates the status of a workload.
+// If the WorkloadRequestUseMergePatch feature is enabled, it uses a Merge Patch with update function.
+// Otherwise, it runs the update function and, if updated, applies the SSA Patch status.
+func patchStatus(ctx context.Context, c client.Client, wl *kueue.Workload, owner client.FieldOwner, update UpdateFunc, opts *PatchStatusOptions) error {
+	wlCopy := wl.DeepCopy()
 	if features.Enabled(features.WorkloadRequestUseMergePatch) {
-		var patchOptions []clientutil.PatchOption
+		patchOptions := make([]clientutil.PatchOption, 0, 2)
 		if !opts.StrictPatch {
 			patchOptions = append(patchOptions, clientutil.WithLoose())
 		}
 		if opts.RetryOnConflictForPatch {
 			patchOptions = append(patchOptions, clientutil.WithRetryOnConflict())
 		}
-		err = clientutil.PatchStatus(ctx, c, wlCopy, func() (bool, error) {
+		err := clientutil.PatchStatus(ctx, c, wlCopy, func() (bool, error) {
 			return update(wlCopy)
 		}, patchOptions...)
+		if err != nil {
+			return err
+		}
 	} else {
 		if updated, err := update(wlCopy); err != nil || !updated {
 			return err
 		}
-		wlCopy = PrepareWorkloadPatch(wlCopy, opts.StrictApply, clk)
-		err = c.Status().Patch(ctx, wlCopy, client.Apply, client.FieldOwner(constants.AdmissionName), client.ForceOwnership)
-	}
-	if err == nil {
-		wlCopy.DeepCopyInto(w)
+		err := c.Status().Patch(ctx, wlCopy, client.Apply, owner, client.ForceOwnership)
+		if err != nil {
+			return err
+		}
 	}
-	return err
+	wlCopy.DeepCopyInto(wl)
+	return nil
+}
+
+func PatchStatus(ctx context.Context, c client.Client, wl *kueue.Workload, owner client.FieldOwner, update UpdateFunc, options ...PatchStatusOption) error {
+	opts := patchStatusOptions(options)
+	return patchStatus(ctx, c, wl, owner, func(wl *kueue.Workload) (bool, error) {
+		if !features.Enabled(features.WorkloadRequestUseMergePatch) {
+			wlPatch := BaseSSAWorkload(wl, opts.StrictApply)
+			wlPatch.DeepCopyInto(wl)
+		}
+		return update(wl)
```

  I'm ok for now, but this is something we may need to still rethink , but I still don't know the ideal state for now, so let's keep it evolving and see where we are.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-05T18:40:03Z

/lgtm
/approve
Thank you 👍
