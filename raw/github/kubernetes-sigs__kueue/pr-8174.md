# PR #8174: Improve RayJob InTreeAutoscaling e2e test

**Summary**: Improve RayJob InTreeAutoscaling e2e test

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8174

**Last updated**: 2025-12-16T17:46:12Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@hiboyang](https://github.com/hiboyang)
- **Branch**: `main-bo-dev-01` → `main`
- **Created**: 2025-12-11T00:04:16Z
- **Updated**: 2025-12-16T17:46:12Z
- **Merged**: 2025-12-16T17:46:10Z
- **Closed**: 2025-12-16T17:46:10Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi), [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi), [@gabesaba](https://github.com/gabesaba), [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 3   **+156 / -11**

## Description

#### What type of PR is this?

/kind cleanup

#### What this PR does / why we need it:

This is a follow-up for PR https://github.com/kubernetes-sigs/kueue/pull/8082, which adds support for RayJob with autoscaling. This PR improves e2e test to wait for scaling-up/down.

#### Which issue(s) this PR fixes:

Improve e2e test from PR https://github.com/kubernetes-sigs/kueue/pull/8082.

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?

```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-12-11T00:04:23Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 88b37dcc37bf6eeca1a9f9c7ca5b94426fdb5c7f |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6941946b5865b900088e1b6d |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-11T00:04:26Z

Hi @hiboyang. Thanks for your PR.

I'm waiting for a [github.com](https://kubernetes-sigs/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-12-11T00:05:26Z

@mimowo @yaroslava-serdiuk  this is the PR to improve e2e test for RayJob with autoscaling.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-12-11T06:31:05Z

Thank you! I will leave the first review pass to Yaroslava

### Comment by [@mimowo](https://github.com/mimowo) — 2025-12-11T07:33:15Z

/ok-to-test

### Comment by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2025-12-11T15:07:37Z

/release-note-none

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-12-11T16:52:04Z

/retest

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-12T11:25:53Z

LGTM label has been added.  <details>Git tree hash: f63009d2190179cca826c4fdb02319e3de6e04a8</details>

### Comment by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2025-12-15T12:01:15Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-15T12:01:22Z

LGTM label has been added.  <details>Git tree hash: 75ab8f8ffeeb3b75a05ff1df9bd42d9d1a06f386</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-12-15T13:13:01Z

LGTM overall, I would prefer to have another pass and approve from @mbobrovskyi who is our new approver for tests.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-15T18:14:34Z

/lgtm
/approve

Thank you!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-15T18:14:42Z

LGTM label has been added.  <details>Git tree hash: ef18a3a9696e8573dd5e180d9a14d4adaa1ae6c8</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-12-15T18:35:57Z

Actually, I have these two comments: https://github.com/kubernetes-sigs/kueue/pull/8174/changes#r2620439444 PTAL, and https://github.com/kubernetes-sigs/kueue/pull/8174#discussion_r2620450957

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-12-15T20:06:14Z

> Actually, I have these two comments: https://github.com/kubernetes-sigs/kueue/pull/8174/changes#r2620439444 PTAL, and [#8174 (comment)](https://github.com/kubernetes-sigs/kueue/pull/8174#discussion_r2620450957)

Thanks for the comments, I updated the PR according to them.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-16T16:22:53Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8174#" title="Author self-approved">hiboyang</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8174#issuecomment-3656975723" title="Approved">mbobrovskyi</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/util/testingjobs/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/util/testingjobs/OWNERS)~~ [mbobrovskyi]
- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [mbobrovskyi]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-16T16:27:11Z

LGTM label has been added.  <details>Git tree hash: f209c3591399228f9e97dbe9c96f49273f0dc9ea</details>

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-12-16T16:37:17Z

/retest

### Comment by [@hiboyang](https://github.com/hiboyang) — 2025-12-16T16:52:13Z

/retest

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-16T17:20:45Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-16T17:20:52Z

LGTM label has been added.  <details>Git tree hash: 4155b7e1abb71c7ad3890b8daa87e87b2d5bd033</details>

## Reviews

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2025-12-11T15:02:06Z

- **test/e2e/singlecluster/kuberay_test.go:299** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2025-12-11T14:47:14Z
```diff
@@ -205,6 +268,38 @@ var _ = ginkgo.Describe("Kuberay", func() {
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 
+		ginkgo.By("Waiting for 3 pods in rayjob namespace", func() {
+			// 3 rayjob pods: head, worker, submitter job
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				g.Expect(podList.Items).To(gomega.HaveLen(3), "Expected exactly 3 pods in rayjob namespace")
+				// Count pods that have "workers" in their name
+				workerPodCount := countWorkerPods(podList)
+				g.Expect(workerPodCount).To(gomega.Equal(1), "Expected exactly 1 pod with 'workers' in the name")
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for 5 workers due to scaling up", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				// Count pods that have "workers" in their name
+				workerPodCount := countWorkerPods(podList)
+				g.Expect(workerPodCount).To(gomega.Equal(5), "Expected exactly 5 pods with 'workers' in the name")
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for workers reduced to 1 due to scaling down", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				// Count pods that have "workers" in their name
+				workerPodCount := countWorkerPods(podList)
+				g.Expect(workerPodCount).To(gomega.Equal(1), "Expected exactly 5 pods with 'workers' in the name")
```

  ```suggestion
  				g.Expect(workerPodCount).To(gomega.Equal(1), "Expected exactly 1 pods with 'workers' in the name")
  ```

- **test/e2e/singlecluster/kuberay_test.go:183** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2025-12-11T14:57:36Z
```diff
@@ -140,19 +154,44 @@ var _ = ginkgo.Describe("Kuberay", func() {
 		})
 	})
 
-	// TODO enhance to test scale up / down operations
-	// See https://github.com/kubernetes-sigs/kueue/pull/8082#discussion_r2605582024
 	ginkgo.It("Should run a rayjob with InTreeAutoscaling", func() {
 		kuberayTestImage := util.GetKuberayTestImage()
 
+		// Create ConfigMap with Python script
+		configMap := &corev1.ConfigMap{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      "rayjob-autoscaling",
+				Namespace: ns.Name,
+			},
+			Data: map[string]string{
+				"sample_code.py": `import ray
+import os
+
+ray.init()
+
+@ray.remote
+def my_task(x, s):
+    import time
+    time.sleep(s)
+    return x * x
+
+# run tasks in sequence to avoid triggering autoscaling in the beginning
+print([ray.get(my_task.remote(i, 1)) for i in range(20)])
+# run tasks in parallel to trigger autoscaling (scaling up)
+print(ray.get([my_task.remote(i, 10) for i in range(10)]))
+# run tasks in sequence to trigger scaling down
+print([ray.get(my_task.remote(i, 1)) for i in range(40)])`,
```

  Is there a reason why you put such high numbers? I'd anticipate a maximum of around 5 being adequate here. If we want to run with 20 or 40 iterations, I suggest we reduce the sleep time accordingly to optimize the test's execution speed.

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2025-12-11T16:12:24Z

- **test/e2e/singlecluster/kuberay_test.go:299** by [@hiboyang](https://github.com/hiboyang) — 2025-12-11T16:12:24Z
```diff
@@ -205,6 +268,38 @@ var _ = ginkgo.Describe("Kuberay", func() {
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 
+		ginkgo.By("Waiting for 3 pods in rayjob namespace", func() {
+			// 3 rayjob pods: head, worker, submitter job
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				g.Expect(podList.Items).To(gomega.HaveLen(3), "Expected exactly 3 pods in rayjob namespace")
+				// Count pods that have "workers" in their name
+				workerPodCount := countWorkerPods(podList)
+				g.Expect(workerPodCount).To(gomega.Equal(1), "Expected exactly 1 pod with 'workers' in the name")
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for 5 workers due to scaling up", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				// Count pods that have "workers" in their name
+				workerPodCount := countWorkerPods(podList)
+				g.Expect(workerPodCount).To(gomega.Equal(5), "Expected exactly 5 pods with 'workers' in the name")
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for workers reduced to 1 due to scaling down", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				// Count pods that have "workers" in their name
+				workerPodCount := countWorkerPods(podList)
+				g.Expect(workerPodCount).To(gomega.Equal(1), "Expected exactly 5 pods with 'workers' in the name")
```

  updated

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2025-12-11T16:13:26Z

- **test/e2e/singlecluster/kuberay_test.go:183** by [@hiboyang](https://github.com/hiboyang) — 2025-12-11T16:13:26Z
```diff
@@ -140,19 +154,44 @@ var _ = ginkgo.Describe("Kuberay", func() {
 		})
 	})
 
-	// TODO enhance to test scale up / down operations
-	// See https://github.com/kubernetes-sigs/kueue/pull/8082#discussion_r2605582024
 	ginkgo.It("Should run a rayjob with InTreeAutoscaling", func() {
 		kuberayTestImage := util.GetKuberayTestImage()
 
+		// Create ConfigMap with Python script
+		configMap := &corev1.ConfigMap{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      "rayjob-autoscaling",
+				Namespace: ns.Name,
+			},
+			Data: map[string]string{
+				"sample_code.py": `import ray
+import os
+
+ray.init()
+
+@ray.remote
+def my_task(x, s):
+    import time
+    time.sleep(s)
+    return x * x
+
+# run tasks in sequence to avoid triggering autoscaling in the beginning
+print([ray.get(my_task.remote(i, 1)) for i in range(20)])
+# run tasks in parallel to trigger autoscaling (scaling up)
+print(ray.get([my_task.remote(i, 10) for i in range(10)]))
+# run tasks in sequence to trigger scaling down
+print([ray.get(my_task.remote(i, 1)) for i in range(40)])`,
```

  The high numbers are to make the rayjob have enough time to scale up and down. Let me tune these numbers to set them a bit lower.

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2025-12-11T20:21:40Z

- **test/e2e/singlecluster/kuberay_test.go:183** by [@hiboyang](https://github.com/hiboyang) — 2025-12-11T20:21:40Z
```diff
@@ -140,19 +154,44 @@ var _ = ginkgo.Describe("Kuberay", func() {
 		})
 	})
 
-	// TODO enhance to test scale up / down operations
-	// See https://github.com/kubernetes-sigs/kueue/pull/8082#discussion_r2605582024
 	ginkgo.It("Should run a rayjob with InTreeAutoscaling", func() {
 		kuberayTestImage := util.GetKuberayTestImage()
 
+		// Create ConfigMap with Python script
+		configMap := &corev1.ConfigMap{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      "rayjob-autoscaling",
+				Namespace: ns.Name,
+			},
+			Data: map[string]string{
+				"sample_code.py": `import ray
+import os
+
+ray.init()
+
+@ray.remote
+def my_task(x, s):
+    import time
+    time.sleep(s)
+    return x * x
+
+# run tasks in sequence to avoid triggering autoscaling in the beginning
+print([ray.get(my_task.remote(i, 1)) for i in range(20)])
+# run tasks in parallel to trigger autoscaling (scaling up)
+print(ray.get([my_task.remote(i, 10) for i in range(10)]))
+# run tasks in sequence to trigger scaling down
+print([ray.get(my_task.remote(i, 1)) for i in range(40)])`,
```

  @yaroslava-serdiuk I updated the tests to make waiting time shorter in rayjob, tried different values multiple times, if setting too short, the test is not stable and may fail randomly. Now get the final values. Would you check?

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2025-12-12T11:25:45Z

/lgtm

- **test/e2e/singlecluster/kuberay_test.go:285** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2025-12-12T11:18:47Z
```diff
@@ -205,6 +269,56 @@ var _ = ginkgo.Describe("Kuberay", func() {
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 
+		ginkgo.By("Waiting for 3 pods in rayjob namespace", func() {
+			// 3 rayjob pods: head, worker, submitter job
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				g.Expect(podList.Items).To(gomega.HaveLen(3), "Expected exactly 3 pods in rayjob namespace")
+				// Count pods that have "workers" in their name
+				workerPodCount := countRunningWorkerPods(podList)
+				g.Expect(workerPodCount).To(gomega.Equal(1), "Expected exactly 1 pod with 'workers' in the name")
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for 2 workloads", func() {
+			// 2 workloads: one for the ray job, another for the submitter job created by the ray job
```

  ```suggestion
  			// 2 workloads: one for the ray cluster, another for the submitter job created by the ray job
  ```

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2025-12-12T16:17:40Z

- **test/e2e/singlecluster/kuberay_test.go:183** by [@hiboyang](https://github.com/hiboyang) — 2025-12-12T16:17:40Z
```diff
@@ -140,19 +154,44 @@ var _ = ginkgo.Describe("Kuberay", func() {
 		})
 	})
 
-	// TODO enhance to test scale up / down operations
-	// See https://github.com/kubernetes-sigs/kueue/pull/8082#discussion_r2605582024
 	ginkgo.It("Should run a rayjob with InTreeAutoscaling", func() {
 		kuberayTestImage := util.GetKuberayTestImage()
 
+		// Create ConfigMap with Python script
+		configMap := &corev1.ConfigMap{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      "rayjob-autoscaling",
+				Namespace: ns.Name,
+			},
+			Data: map[string]string{
+				"sample_code.py": `import ray
+import os
+
+ray.init()
+
+@ray.remote
+def my_task(x, s):
+    import time
+    time.sleep(s)
+    return x * x
+
+# run tasks in sequence to avoid triggering autoscaling in the beginning
+print([ray.get(my_task.remote(i, 1)) for i in range(20)])
+# run tasks in parallel to trigger autoscaling (scaling up)
+print(ray.get([my_task.remote(i, 10) for i in range(10)]))
+# run tasks in sequence to trigger scaling down
+print([ray.get(my_task.remote(i, 1)) for i in range(40)])`,
```

  Thanks @yaroslava-serdiuk for checking! I updated the PR again due to git conflict. Would you help to approve again?

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2025-12-15T12:02:49Z

- **test/e2e/singlecluster/kuberay_test.go:183** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2025-12-15T12:02:49Z
```diff
@@ -140,19 +154,44 @@ var _ = ginkgo.Describe("Kuberay", func() {
 		})
 	})
 
-	// TODO enhance to test scale up / down operations
-	// See https://github.com/kubernetes-sigs/kueue/pull/8082#discussion_r2605582024
 	ginkgo.It("Should run a rayjob with InTreeAutoscaling", func() {
 		kuberayTestImage := util.GetKuberayTestImage()
 
+		// Create ConfigMap with Python script
+		configMap := &corev1.ConfigMap{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      "rayjob-autoscaling",
+				Namespace: ns.Name,
+			},
+			Data: map[string]string{
+				"sample_code.py": `import ray
+import os
+
+ray.init()
+
+@ray.remote
+def my_task(x, s):
+    import time
+    time.sleep(s)
+    return x * x
+
+# run tasks in sequence to avoid triggering autoscaling in the beginning
+print([ray.get(my_task.remote(i, 1)) for i in range(20)])
+# run tasks in parallel to trigger autoscaling (scaling up)
+print(ray.get([my_task.remote(i, 10) for i in range(10)]))
+# run tasks in sequence to trigger scaling down
+print([ray.get(my_task.remote(i, 1)) for i in range(40)])`,
```

  Sure, added lgtm. Passing to @mimowo for approval.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-15T13:21:26Z

- **test/e2e/singlecluster/kuberay_test.go:246** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-15T13:21:26Z
```diff
@@ -175,11 +216,35 @@ var _ = ginkgo.Describe("Kuberay", func() {
 			Image(rayv1.HeadNode, kuberayTestImage).
 			Image(rayv1.WorkerNode, kuberayTestImage).Obj()
 
+		// Add volume and volumeMount to head node for the ConfigMap
+		rayJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.Volumes = []corev1.Volume{
+			{
+				Name: "script-volume",
+				VolumeSource: corev1.VolumeSource{
+					ConfigMap: &corev1.ConfigMapVolumeSource{
+						LocalObjectReference: corev1.LocalObjectReference{
+							Name: "rayjob-autoscaling",
+						},
+					},
+				},
+			},
+		}
+		rayJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.Containers[0].VolumeMounts = []corev1.VolumeMount{
+			{
+				Name:      "script-volume",
+				MountPath: "/home/ray/samples",
+			},
+		}
+
+		ginkgo.By("Creating the ConfigMap", func() {
+			gomega.Expect(k8sClient.Create(ctx, configMap)).Should(gomega.Succeed())
+		})
```

  Do we have cleanup for this?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-15T13:51:16Z

- **test/e2e/singlecluster/kuberay_test.go:248** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-15T13:51:16Z
```diff
@@ -175,11 +216,35 @@ var _ = ginkgo.Describe("Kuberay", func() {
 			Image(rayv1.HeadNode, kuberayTestImage).
 			Image(rayv1.WorkerNode, kuberayTestImage).Obj()
 
+		// Add volume and volumeMount to head node for the ConfigMap
+		rayJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.Volumes = []corev1.Volume{
+			{
+				Name: "script-volume",
+				VolumeSource: corev1.VolumeSource{
+					ConfigMap: &corev1.ConfigMapVolumeSource{
+						LocalObjectReference: corev1.LocalObjectReference{
+							Name: "rayjob-autoscaling",
+						},
+					},
+				},
+			},
+		}
+		rayJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.Containers[0].VolumeMounts = []corev1.VolumeMount{
+			{
+				Name:      "script-volume",
+				MountPath: "/home/ray/samples",
+			},
+		}
+
+		ginkgo.By("Creating the ConfigMap", func() {
+			gomega.Expect(k8sClient.Create(ctx, configMap)).Should(gomega.Succeed())
+		})
+
 		ginkgo.By("Creating the rayJob", func() {
```

  I also can't see that we are clearing RayJobs. Probably we should add DeleteAllRayJobsInNamespace in DeleteNamespace to cleanup. But it could be fixed in follow-up.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-15T14:04:53Z

- **test/e2e/singlecluster/kuberay_test.go:281** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-15T14:04:53Z
```diff
@@ -204,6 +269,56 @@ var _ = ginkgo.Describe("Kuberay", func() {
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 
+		ginkgo.By("Waiting for 3 pods in rayjob namespace", func() {
+			// 3 rayjob pods: head, worker, submitter job
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				g.Expect(podList.Items).To(gomega.HaveLen(3), "Expected exactly 3 pods in rayjob namespace")
+				// Count pods that have "workers" in their name
+				workerPodCount := countRunningWorkerPods(podList)
+				g.Expect(workerPodCount).To(gomega.Equal(1), "Expected exactly 1 pod with 'workers' in the name")
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
```

  I also wondering why we are using VeryLongTimeout? Is it not enough LongTimeout?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-15T14:05:10Z

- **test/e2e/singlecluster/kuberay_test.go:281** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-15T14:05:10Z
```diff
@@ -204,6 +269,56 @@ var _ = ginkgo.Describe("Kuberay", func() {
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 
+		ginkgo.By("Waiting for 3 pods in rayjob namespace", func() {
+			// 3 rayjob pods: head, worker, submitter job
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				g.Expect(podList.Items).To(gomega.HaveLen(3), "Expected exactly 3 pods in rayjob namespace")
+				// Count pods that have "workers" in their name
+				workerPodCount := countRunningWorkerPods(podList)
+				g.Expect(workerPodCount).To(gomega.Equal(1), "Expected exactly 1 pod with 'workers' in the name")
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
```

  Also in other places.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-15T16:40:39Z

- **test/e2e/singlecluster/kuberay_test.go:248** by [@mimowo](https://github.com/mimowo) — 2025-12-15T16:40:38Z
```diff
@@ -175,11 +216,35 @@ var _ = ginkgo.Describe("Kuberay", func() {
 			Image(rayv1.HeadNode, kuberayTestImage).
 			Image(rayv1.WorkerNode, kuberayTestImage).Obj()
 
+		// Add volume and volumeMount to head node for the ConfigMap
+		rayJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.Volumes = []corev1.Volume{
+			{
+				Name: "script-volume",
+				VolumeSource: corev1.VolumeSource{
+					ConfigMap: &corev1.ConfigMapVolumeSource{
+						LocalObjectReference: corev1.LocalObjectReference{
+							Name: "rayjob-autoscaling",
+						},
+					},
+				},
+			},
+		}
+		rayJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.Containers[0].VolumeMounts = []corev1.VolumeMount{
+			{
+				Name:      "script-volume",
+				MountPath: "/home/ray/samples",
+			},
+		}
+
+		ginkgo.By("Creating the ConfigMap", func() {
+			gomega.Expect(k8sClient.Create(ctx, configMap)).Should(gomega.Succeed())
+		})
+
 		ginkgo.By("Creating the rayJob", func() {
```

  I think it is better to make sure the resources are cleaned in this PR TBH. Either adding to DeleteNamespace or in AfterEach make sense to me. I'm leaning to include in DeleteNamespace for consistency

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2025-12-15T17:58:24Z

- **test/e2e/singlecluster/kuberay_test.go:246** by [@hiboyang](https://github.com/hiboyang) — 2025-12-15T17:58:24Z
```diff
@@ -175,11 +216,35 @@ var _ = ginkgo.Describe("Kuberay", func() {
 			Image(rayv1.HeadNode, kuberayTestImage).
 			Image(rayv1.WorkerNode, kuberayTestImage).Obj()
 
+		// Add volume and volumeMount to head node for the ConfigMap
+		rayJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.Volumes = []corev1.Volume{
+			{
+				Name: "script-volume",
+				VolumeSource: corev1.VolumeSource{
+					ConfigMap: &corev1.ConfigMapVolumeSource{
+						LocalObjectReference: corev1.LocalObjectReference{
+							Name: "rayjob-autoscaling",
+						},
+					},
+				},
+			},
+		}
+		rayJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.Containers[0].VolumeMounts = []corev1.VolumeMount{
+			{
+				Name:      "script-volume",
+				MountPath: "/home/ray/samples",
+			},
+		}
+
+		ginkgo.By("Creating the ConfigMap", func() {
+			gomega.Expect(k8sClient.Create(ctx, configMap)).Should(gomega.Succeed())
+		})
```

  Good point, added cleanup for the configmap.

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2025-12-15T17:58:48Z

- **test/e2e/singlecluster/kuberay_test.go:248** by [@hiboyang](https://github.com/hiboyang) — 2025-12-15T17:58:48Z
```diff
@@ -175,11 +216,35 @@ var _ = ginkgo.Describe("Kuberay", func() {
 			Image(rayv1.HeadNode, kuberayTestImage).
 			Image(rayv1.WorkerNode, kuberayTestImage).Obj()
 
+		// Add volume and volumeMount to head node for the ConfigMap
+		rayJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.Volumes = []corev1.Volume{
+			{
+				Name: "script-volume",
+				VolumeSource: corev1.VolumeSource{
+					ConfigMap: &corev1.ConfigMapVolumeSource{
+						LocalObjectReference: corev1.LocalObjectReference{
+							Name: "rayjob-autoscaling",
+						},
+					},
+				},
+			},
+		}
+		rayJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.Containers[0].VolumeMounts = []corev1.VolumeMount{
+			{
+				Name:      "script-volume",
+				MountPath: "/home/ray/samples",
+			},
+		}
+
+		ginkgo.By("Creating the ConfigMap", func() {
+			gomega.Expect(k8sClient.Create(ctx, configMap)).Should(gomega.Succeed())
+		})
+
 		ginkgo.By("Creating the rayJob", func() {
```

  Good suggestion! Added DeleteAllRayJobsInNamespace in DeleteNamespace.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-15T18:01:58Z

- **test/util/util.go:207** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-15T18:01:58Z
```diff
@@ -198,6 +201,10 @@ func DeleteAllRayJobsInNamespace(ctx context.Context, c client.Client, ns *corev
 	return deleteAllObjectsInNamespace(ctx, c, ns, &rayv1.RayJob{})
 }
 
+func DeleteAllRayJobSetsInNamespace(ctx context.Context, c client.Client, ns *corev1.Namespace) error {
+	return deleteAllObjectsInNamespace(ctx, c, ns, &rayv1.RayJob{})
+}
+
```

  ```suggestion
  ```
  Looks like duplicate?

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2025-12-15T18:02:23Z

- **test/e2e/singlecluster/kuberay_test.go:281** by [@hiboyang](https://github.com/hiboyang) — 2025-12-15T18:02:23Z
```diff
@@ -204,6 +269,56 @@ var _ = ginkgo.Describe("Kuberay", func() {
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 
+		ginkgo.By("Waiting for 3 pods in rayjob namespace", func() {
+			// 3 rayjob pods: head, worker, submitter job
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				g.Expect(podList.Items).To(gomega.HaveLen(3), "Expected exactly 3 pods in rayjob namespace")
+				// Count pods that have "workers" in their name
+				workerPodCount := countRunningWorkerPods(podList)
+				g.Expect(workerPodCount).To(gomega.Equal(1), "Expected exactly 1 pod with 'workers' in the name")
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
```

  Using VeryLongTimeout helps making the test more stable, I tested with different sleeping time in scaling up/down, then come to around 5 - 10 seconds, adding extra time for pod startup time, VeryLongTimeout will make sure we have enough time to avoid random failure. I did another check, removed some VeryLongTimeout and still kept some VeryLongTimeout.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-15T18:35:28Z

- **test/e2e/singlecluster/kuberay_test.go:302** by [@mimowo](https://github.com/mimowo) — 2025-12-15T18:35:28Z
```diff
@@ -204,6 +272,56 @@ var _ = ginkgo.Describe("Kuberay", func() {
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 
+		ginkgo.By("Waiting for 3 pods in rayjob namespace", func() {
+			// 3 rayjob pods: head, worker, submitter job
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				g.Expect(podList.Items).To(gomega.HaveLen(3), "Expected exactly 3 pods in rayjob namespace")
+				// Count pods that have "workers" in their name
+				workerPodCount := countRunningWorkerPods(podList)
+				g.Expect(workerPodCount).To(gomega.Equal(1), "Expected exactly 1 pod with 'workers' in the name")
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for 2 workloads", func() {
+			// 2 workloads: one for the ray cluster, another for the submitter job created by the ray job
+			gomega.Eventually(func(g gomega.Gomega) {
+				workloadList := &kueue.WorkloadList{}
+				g.Expect(k8sClient.List(ctx, workloadList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				g.Expect(workloadList.Items).To(gomega.HaveLen(2), "Expected exactly 2 workloads")
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for 5 workers due to scaling up", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				// Count pods that have "workers" in their name
+				workerPodCount := countRunningWorkerPods(podList)
+				g.Expect(workerPodCount).To(gomega.Equal(5), "Expected exactly 5 pods with 'workers' in the name")
```

  Actually, it is better if this helper returns Pod names rather than just count.
  
  Then I think we should verify the new 5 pods is a superset of the old Pods. Otherwise maybe they were all re-created. Similarly, on scale down we would like to check that the new set of pods is a subset of previous Pods.
  
  Let me know what you think, but I expect this will make the tests proving the scaling really works. Otherwise it was possible also in the past with suspending / re admitting the Job.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-15T18:40:20Z

- **test/e2e/singlecluster/kuberay_test.go:281** by [@mimowo](https://github.com/mimowo) — 2025-12-15T18:40:20Z
```diff
@@ -204,6 +269,56 @@ var _ = ginkgo.Describe("Kuberay", func() {
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 
+		ginkgo.By("Waiting for 3 pods in rayjob namespace", func() {
+			// 3 rayjob pods: head, worker, submitter job
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				g.Expect(podList.Items).To(gomega.HaveLen(3), "Expected exactly 3 pods in rayjob namespace")
+				// Count pods that have "workers" in their name
+				workerPodCount := countRunningWorkerPods(podList)
+				g.Expect(workerPodCount).To(gomega.Equal(1), "Expected exactly 1 pod with 'workers' in the name")
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
```

  Oh, I think this is likely because the termination takes 30s as by default. I think we could mitigate this problem largely by setting int the PodTemplate: TerminationGracePeriodSeconds. PTAL as we have the `TerminationGracePeriod` helper introduced for many Job types already.
  
  With TerminationGracePeriodSeconds I expect LongTimeout is typically ok, but please check. I'm ok with VeryLongTimeout if needed.

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2025-12-15T18:43:27Z

- **test/e2e/singlecluster/kuberay_test.go:302** by [@hiboyang](https://github.com/hiboyang) — 2025-12-15T18:43:27Z
```diff
@@ -204,6 +272,56 @@ var _ = ginkgo.Describe("Kuberay", func() {
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 
+		ginkgo.By("Waiting for 3 pods in rayjob namespace", func() {
+			// 3 rayjob pods: head, worker, submitter job
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				g.Expect(podList.Items).To(gomega.HaveLen(3), "Expected exactly 3 pods in rayjob namespace")
+				// Count pods that have "workers" in their name
+				workerPodCount := countRunningWorkerPods(podList)
+				g.Expect(workerPodCount).To(gomega.Equal(1), "Expected exactly 1 pod with 'workers' in the name")
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for 2 workloads", func() {
+			// 2 workloads: one for the ray cluster, another for the submitter job created by the ray job
+			gomega.Eventually(func(g gomega.Gomega) {
+				workloadList := &kueue.WorkloadList{}
+				g.Expect(k8sClient.List(ctx, workloadList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				g.Expect(workloadList.Items).To(gomega.HaveLen(2), "Expected exactly 2 workloads")
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for 5 workers due to scaling up", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				// Count pods that have "workers" in their name
+				workerPodCount := countRunningWorkerPods(podList)
+				g.Expect(workerPodCount).To(gomega.Equal(5), "Expected exactly 5 pods with 'workers' in the name")
```

  Cool, like the suggestion here, let me add this!

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2025-12-15T20:01:55Z

- **test/e2e/singlecluster/kuberay_test.go:302** by [@hiboyang](https://github.com/hiboyang) — 2025-12-15T20:01:55Z
```diff
@@ -204,6 +272,56 @@ var _ = ginkgo.Describe("Kuberay", func() {
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 
+		ginkgo.By("Waiting for 3 pods in rayjob namespace", func() {
+			// 3 rayjob pods: head, worker, submitter job
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				g.Expect(podList.Items).To(gomega.HaveLen(3), "Expected exactly 3 pods in rayjob namespace")
+				// Count pods that have "workers" in their name
+				workerPodCount := countRunningWorkerPods(podList)
+				g.Expect(workerPodCount).To(gomega.Equal(1), "Expected exactly 1 pod with 'workers' in the name")
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for 2 workloads", func() {
+			// 2 workloads: one for the ray cluster, another for the submitter job created by the ray job
+			gomega.Eventually(func(g gomega.Gomega) {
+				workloadList := &kueue.WorkloadList{}
+				g.Expect(k8sClient.List(ctx, workloadList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				g.Expect(workloadList.Items).To(gomega.HaveLen(2), "Expected exactly 2 workloads")
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for 5 workers due to scaling up", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				// Count pods that have "workers" in their name
+				workerPodCount := countRunningWorkerPods(podList)
+				g.Expect(workerPodCount).To(gomega.Equal(5), "Expected exactly 5 pods with 'workers' in the name")
```

  Update PR to check pod names to be superset after scaling up/down.

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2025-12-15T20:04:56Z

- **test/e2e/singlecluster/kuberay_test.go:281** by [@hiboyang](https://github.com/hiboyang) — 2025-12-15T20:04:56Z
```diff
@@ -204,6 +269,56 @@ var _ = ginkgo.Describe("Kuberay", func() {
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 
+		ginkgo.By("Waiting for 3 pods in rayjob namespace", func() {
+			// 3 rayjob pods: head, worker, submitter job
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				g.Expect(podList.Items).To(gomega.HaveLen(3), "Expected exactly 3 pods in rayjob namespace")
+				// Count pods that have "workers" in their name
+				workerPodCount := countRunningWorkerPods(podList)
+				g.Expect(workerPodCount).To(gomega.Equal(1), "Expected exactly 1 pod with 'workers' in the name")
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
```

  Added TerminationGracePeriodSeconds to be 5 seconds in the code. Still feel VeryLongTimeout is safer, just in case the test running has some randomness and takes a bit long. Also VeryLongTimeout is the worst case, if the test finishes earlier, it will not really wait that long.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-16T07:39:46Z

- **test/e2e/singlecluster/kuberay_test.go:339** by [@mimowo](https://github.com/mimowo) — 2025-12-16T07:39:46Z
```diff
@@ -204,6 +301,72 @@ var _ = ginkgo.Describe("Kuberay", func() {
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 
+		ginkgo.By("Waiting for 3 pods in rayjob namespace", func() {
+			// 3 rayjob pods: head, worker, submitter job
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				g.Expect(podList.Items).To(gomega.HaveLen(3), "Expected exactly 3 pods in rayjob namespace")
+				// Get worker pod names and check count
+				workerPodNames := getRunningWorkerPodNames(podList)
+				g.Expect(workerPodNames).To(gomega.HaveLen(1), "Expected exactly 1 pod with 'workers' in the name")
+
+				// Store initial pod names for later verification
+				initialPodNames = workerPodNames
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for 2 workloads", func() {
+			// 2 workloads: one for the ray cluster, another for the submitter job created by the ray job
+			gomega.Eventually(func(g gomega.Gomega) {
+				workloadList := &kueue.WorkloadList{}
+				g.Expect(k8sClient.List(ctx, workloadList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				g.Expect(workloadList.Items).To(gomega.HaveLen(2), "Expected exactly 2 workloads")
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for 5 workers due to scaling up", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				// Get worker pod names and check count
+				currentPodNames := getRunningWorkerPodNames(podList)
+				g.Expect(currentPodNames).To(gomega.HaveLen(5), "Expected exactly 5 pods with 'workers' in the name")
+
+				// Verify that the current pod names are a superset of the initial pod names
+				g.Expect(verifyPodNamesAreSuperset(currentPodNames, initialPodNames)).To(gomega.BeTrue(),
+					"Current worker pod names should be a superset of initial pod names. "+
+						"Initial pods: %v, Current pods: %v", initialPodNames, currentPodNames)
```

  Nit, instead of the helper functions let's use the gomega way:
  
  ```
  g.Expect(currentPodNames.UnsortedList()).To(gomega.ContainElements(initialPodNames.UnsortedList()))
  ```
  This is less code, and better failure message when it happens

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-16T07:40:31Z

- **test/e2e/singlecluster/kuberay_test.go:364** by [@mimowo](https://github.com/mimowo) — 2025-12-16T07:40:31Z
```diff
@@ -204,6 +301,72 @@ var _ = ginkgo.Describe("Kuberay", func() {
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 
+		ginkgo.By("Waiting for 3 pods in rayjob namespace", func() {
+			// 3 rayjob pods: head, worker, submitter job
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				g.Expect(podList.Items).To(gomega.HaveLen(3), "Expected exactly 3 pods in rayjob namespace")
+				// Get worker pod names and check count
+				workerPodNames := getRunningWorkerPodNames(podList)
+				g.Expect(workerPodNames).To(gomega.HaveLen(1), "Expected exactly 1 pod with 'workers' in the name")
+
+				// Store initial pod names for later verification
+				initialPodNames = workerPodNames
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for 2 workloads", func() {
+			// 2 workloads: one for the ray cluster, another for the submitter job created by the ray job
+			gomega.Eventually(func(g gomega.Gomega) {
+				workloadList := &kueue.WorkloadList{}
+				g.Expect(k8sClient.List(ctx, workloadList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				g.Expect(workloadList.Items).To(gomega.HaveLen(2), "Expected exactly 2 workloads")
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for 5 workers due to scaling up", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				// Get worker pod names and check count
+				currentPodNames := getRunningWorkerPodNames(podList)
+				g.Expect(currentPodNames).To(gomega.HaveLen(5), "Expected exactly 5 pods with 'workers' in the name")
+
+				// Verify that the current pod names are a superset of the initial pod names
+				g.Expect(verifyPodNamesAreSuperset(currentPodNames, initialPodNames)).To(gomega.BeTrue(),
+					"Current worker pod names should be a superset of initial pod names. "+
+						"Initial pods: %v, Current pods: %v", initialPodNames, currentPodNames)
+
+				// Store scaled-up pod names for later verification during scaling down
+				scaledUpPodNames = currentPodNames
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for 3 workloads due to scaling up creating another workload", func() {
+			// 3 workloads now, after scaling up, a new workload will be created for the new resource request
+			gomega.Eventually(func(g gomega.Gomega) {
+				workloadList := &kueue.WorkloadList{}
+				g.Expect(k8sClient.List(ctx, workloadList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				g.Expect(workloadList.Items).To(gomega.HaveLen(3), "Expected exactly 3 workloads")
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for workers reduced to 1 due to scaling down", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				// Get worker pod names and check count
+				workerPodNames := getRunningWorkerPodNames(podList)
+				g.Expect(workerPodNames).To(gomega.HaveLen(1), "Expected exactly 1 pods with 'workers' in the name")
+
+				// Verify that the previous scaled-up pod names are a superset of the current pod names
+				g.Expect(verifyPodNamesAreSuperset(scaledUpPodNames, workerPodNames)).To(gomega.BeTrue(),
```

  here also let's replace that with the assert using gomega.ContainElements

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2025-12-16T16:23:17Z

- **test/e2e/singlecluster/kuberay_test.go:339** by [@hiboyang](https://github.com/hiboyang) — 2025-12-16T16:23:16Z
```diff
@@ -204,6 +301,72 @@ var _ = ginkgo.Describe("Kuberay", func() {
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 
+		ginkgo.By("Waiting for 3 pods in rayjob namespace", func() {
+			// 3 rayjob pods: head, worker, submitter job
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				g.Expect(podList.Items).To(gomega.HaveLen(3), "Expected exactly 3 pods in rayjob namespace")
+				// Get worker pod names and check count
+				workerPodNames := getRunningWorkerPodNames(podList)
+				g.Expect(workerPodNames).To(gomega.HaveLen(1), "Expected exactly 1 pod with 'workers' in the name")
+
+				// Store initial pod names for later verification
+				initialPodNames = workerPodNames
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for 2 workloads", func() {
+			// 2 workloads: one for the ray cluster, another for the submitter job created by the ray job
+			gomega.Eventually(func(g gomega.Gomega) {
+				workloadList := &kueue.WorkloadList{}
+				g.Expect(k8sClient.List(ctx, workloadList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				g.Expect(workloadList.Items).To(gomega.HaveLen(2), "Expected exactly 2 workloads")
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for 5 workers due to scaling up", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				// Get worker pod names and check count
+				currentPodNames := getRunningWorkerPodNames(podList)
+				g.Expect(currentPodNames).To(gomega.HaveLen(5), "Expected exactly 5 pods with 'workers' in the name")
+
+				// Verify that the current pod names are a superset of the initial pod names
+				g.Expect(verifyPodNamesAreSuperset(currentPodNames, initialPodNames)).To(gomega.BeTrue(),
+					"Current worker pod names should be a superset of initial pod names. "+
+						"Initial pods: %v, Current pods: %v", initialPodNames, currentPodNames)
```

  Good suggestion, updated PR!

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2025-12-16T16:23:28Z

- **test/e2e/singlecluster/kuberay_test.go:364** by [@hiboyang](https://github.com/hiboyang) — 2025-12-16T16:23:28Z
```diff
@@ -204,6 +301,72 @@ var _ = ginkgo.Describe("Kuberay", func() {
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 
+		ginkgo.By("Waiting for 3 pods in rayjob namespace", func() {
+			// 3 rayjob pods: head, worker, submitter job
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				g.Expect(podList.Items).To(gomega.HaveLen(3), "Expected exactly 3 pods in rayjob namespace")
+				// Get worker pod names and check count
+				workerPodNames := getRunningWorkerPodNames(podList)
+				g.Expect(workerPodNames).To(gomega.HaveLen(1), "Expected exactly 1 pod with 'workers' in the name")
+
+				// Store initial pod names for later verification
+				initialPodNames = workerPodNames
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for 2 workloads", func() {
+			// 2 workloads: one for the ray cluster, another for the submitter job created by the ray job
+			gomega.Eventually(func(g gomega.Gomega) {
+				workloadList := &kueue.WorkloadList{}
+				g.Expect(k8sClient.List(ctx, workloadList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				g.Expect(workloadList.Items).To(gomega.HaveLen(2), "Expected exactly 2 workloads")
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for 5 workers due to scaling up", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				// Get worker pod names and check count
+				currentPodNames := getRunningWorkerPodNames(podList)
+				g.Expect(currentPodNames).To(gomega.HaveLen(5), "Expected exactly 5 pods with 'workers' in the name")
+
+				// Verify that the current pod names are a superset of the initial pod names
+				g.Expect(verifyPodNamesAreSuperset(currentPodNames, initialPodNames)).To(gomega.BeTrue(),
+					"Current worker pod names should be a superset of initial pod names. "+
+						"Initial pods: %v, Current pods: %v", initialPodNames, currentPodNames)
+
+				// Store scaled-up pod names for later verification during scaling down
+				scaledUpPodNames = currentPodNames
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for 3 workloads due to scaling up creating another workload", func() {
+			// 3 workloads now, after scaling up, a new workload will be created for the new resource request
+			gomega.Eventually(func(g gomega.Gomega) {
+				workloadList := &kueue.WorkloadList{}
+				g.Expect(k8sClient.List(ctx, workloadList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				g.Expect(workloadList.Items).To(gomega.HaveLen(3), "Expected exactly 3 workloads")
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for workers reduced to 1 due to scaling down", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				podList := &corev1.PodList{}
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+				// Get worker pod names and check count
+				workerPodNames := getRunningWorkerPodNames(podList)
+				g.Expect(workerPodNames).To(gomega.HaveLen(1), "Expected exactly 1 pods with 'workers' in the name")
+
+				// Verify that the previous scaled-up pod names are a superset of the current pod names
+				g.Expect(verifyPodNamesAreSuperset(scaledUpPodNames, workerPodNames)).To(gomega.BeTrue(),
```

  Updated

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-16T16:27:03Z

Thank you 👍 
/lgtm
/cherrypick release-0.15

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-16T17:06:01Z

- **test/e2e/singlecluster/kuberay_test.go:248** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-16T17:06:01Z
```diff
@@ -175,11 +217,47 @@ var _ = ginkgo.Describe("Kuberay", func() {
 			Image(rayv1.HeadNode, kuberayTestImage).
 			Image(rayv1.WorkerNode, kuberayTestImage).Obj()
 
+		// Add volume and volumeMount to head node for the ConfigMap
+		rayJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.Volumes = []corev1.Volume{
+			{
+				Name: "script-volume",
+				VolumeSource: corev1.VolumeSource{
+					ConfigMap: &corev1.ConfigMapVolumeSource{
+						LocalObjectReference: corev1.LocalObjectReference{
+							Name: "rayjob-autoscaling",
+						},
+					},
+				},
+			},
+		}
+		rayJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.Containers[0].VolumeMounts = []corev1.VolumeMount{
+			{
+				Name:      "script-volume",
+				MountPath: "/home/ray/samples",
+			},
+		}
+		rayJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.TerminationGracePeriodSeconds = ptr.To(int64(5))
+		for i := range len(rayJob.Spec.RayClusterSpec.WorkerGroupSpecs) {
+			rayJob.Spec.RayClusterSpec.WorkerGroupSpecs[i].Template.Spec.TerminationGracePeriodSeconds = ptr.To(int64(5))
+		}
+
+		ginkgo.By("Creating the ConfigMap", func() {
+			gomega.Expect(k8sClient.Create(ctx, configMap)).Should(gomega.Succeed())
+		})
+		ginkgo.DeferCleanup(func() {
+			gomega.Expect(k8sClient.Delete(ctx, configMap)).Should(gomega.Succeed())
```

  ```suggestion
  			util.ExpectObjectToBeDeleted(ctx, k8sClient, configMap, true)
  ```

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2025-12-16T17:20:50Z

- **test/e2e/singlecluster/kuberay_test.go:248** by [@hiboyang](https://github.com/hiboyang) — 2025-12-16T17:20:50Z
```diff
@@ -175,11 +217,47 @@ var _ = ginkgo.Describe("Kuberay", func() {
 			Image(rayv1.HeadNode, kuberayTestImage).
 			Image(rayv1.WorkerNode, kuberayTestImage).Obj()
 
+		// Add volume and volumeMount to head node for the ConfigMap
+		rayJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.Volumes = []corev1.Volume{
+			{
+				Name: "script-volume",
+				VolumeSource: corev1.VolumeSource{
+					ConfigMap: &corev1.ConfigMapVolumeSource{
+						LocalObjectReference: corev1.LocalObjectReference{
+							Name: "rayjob-autoscaling",
+						},
+					},
+				},
+			},
+		}
+		rayJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.Containers[0].VolumeMounts = []corev1.VolumeMount{
+			{
+				Name:      "script-volume",
+				MountPath: "/home/ray/samples",
+			},
+		}
+		rayJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.TerminationGracePeriodSeconds = ptr.To(int64(5))
+		for i := range len(rayJob.Spec.RayClusterSpec.WorkerGroupSpecs) {
+			rayJob.Spec.RayClusterSpec.WorkerGroupSpecs[i].Template.Spec.TerminationGracePeriodSeconds = ptr.To(int64(5))
+		}
+
+		ginkgo.By("Creating the ConfigMap", func() {
+			gomega.Expect(k8sClient.Create(ctx, configMap)).Should(gomega.Succeed())
+		})
+		ginkgo.DeferCleanup(func() {
+			gomega.Expect(k8sClient.Delete(ctx, configMap)).Should(gomega.Succeed())
```

  I updated DeleteNamespace() to delete configmap, to make cleanup consistent with old code (inside DeleteNamespace).

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-16T17:21:37Z

- **test/e2e/singlecluster/kuberay_test.go:248** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-16T17:21:37Z
```diff
@@ -175,11 +217,47 @@ var _ = ginkgo.Describe("Kuberay", func() {
 			Image(rayv1.HeadNode, kuberayTestImage).
 			Image(rayv1.WorkerNode, kuberayTestImage).Obj()
 
+		// Add volume and volumeMount to head node for the ConfigMap
+		rayJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.Volumes = []corev1.Volume{
+			{
+				Name: "script-volume",
+				VolumeSource: corev1.VolumeSource{
+					ConfigMap: &corev1.ConfigMapVolumeSource{
+						LocalObjectReference: corev1.LocalObjectReference{
+							Name: "rayjob-autoscaling",
+						},
+					},
+				},
+			},
+		}
+		rayJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.Containers[0].VolumeMounts = []corev1.VolumeMount{
+			{
+				Name:      "script-volume",
+				MountPath: "/home/ray/samples",
+			},
+		}
+		rayJob.Spec.RayClusterSpec.HeadGroupSpec.Template.Spec.TerminationGracePeriodSeconds = ptr.To(int64(5))
+		for i := range len(rayJob.Spec.RayClusterSpec.WorkerGroupSpecs) {
+			rayJob.Spec.RayClusterSpec.WorkerGroupSpecs[i].Template.Spec.TerminationGracePeriodSeconds = ptr.To(int64(5))
+		}
+
+		ginkgo.By("Creating the ConfigMap", func() {
+			gomega.Expect(k8sClient.Create(ctx, configMap)).Should(gomega.Succeed())
+		})
+		ginkgo.DeferCleanup(func() {
+			gomega.Expect(k8sClient.Delete(ctx, configMap)).Should(gomega.Succeed())
```

  Thank you!
