# PR #818: [test/integration] Reuse envtest

**Summary**: [test/integration] Reuse envtest

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/818

**Last updated**: 2023-06-09T07:20:59Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `test-int-job-reuse-envtest-env` → `main`
- **Created**: 2023-05-29T15:00:37Z
- **Updated**: 2023-06-09T07:20:59Z
- **Merged**: 2023-05-31T14:39:46Z
- **Closed**: 2023-05-31T14:39:46Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 4   **+285 / -120**

## Description

#### What type of PR is this?

/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Reuse envtest in the integration tests for batch,mpi, and ray jobs.

Doing so we speed up the overall integration tests with @35%.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #767

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-05-29T15:00:42Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 25d39ad440f4861820927ac9f837a574b2fb6855 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6475d2ba45dd5700081c7dc6 |

### Comment by [@trasc](https://github.com/trasc) — 2023-05-30T18:52:33Z

/retest

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-30T19:34:20Z

/approve

Finally!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-05-30T19:34:27Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/818#issuecomment-1568975723" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/818#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-31T07:52:51Z

/test pull-kueue-test-integration-main

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-31T07:57:10Z

Since this PR, `pull-kueue-test-integration-main` consumes more capacity of memory...

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-31T08:00:11Z

Probably, the kube-apiserver crashes :(

```shell
{Unexpected error:
    <*fmt.wrapError | 0xc0001264a0>: 
    unable to install CRDs onto control plane: something went wrong waiting for CRDs to appear as API resources: timed out waiting for the condition
    {
        msg: "unable to install CRDs onto control plane: something went wrong waiting for CRDs to appear as API resources: timed out waiting for the condition",
        err: <*fmt.wrapError | 0xc000126480>{
            msg: "something went wrong waiting for CRDs to appear as API resources: timed out waiting for the condition",
            err: <*errors.errorString | 0xc00037f0d0>{
                s: "timed out waiting for the condition",
            },
        },
    }
occurred failed [FAILED] Unexpected error:
    <*fmt.wrapError | 0xc0001264a0>: 
    unable to install CRDs onto control plane: something went wrong waiting for CRDs to appear as API resources: timed out waiting for the condition
    {
        msg: "unable to install CRDs onto control plane: something went wrong waiting for CRDs to appear as API resources: timed out waiting for the condition",
        err: <*fmt.wrapError | 0xc000126480>{
            msg: "something went wrong waiting for CRDs to appear as API resources: timed out waiting for the condition",
            err: <*errors.errorString | 0xc00037f0d0>{
                s: "timed out waiting for the condition",
            },
        },
    }
occurred
In [BeforeSuite] at: /home/prow/go/src/sigs.k8s.io/kueue/test/integration/controller/core/suite_test.go:61 @ 05/31/23 07:55:40.17
}
```

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-31T11:56:18Z

/retest

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-31T12:40:33Z

/retest

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-31T13:06:57Z

/test pull-kueue-verify-main

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-31T13:09:18Z

The test was killed again...

```
/home/prow/go/src/sigs.k8s.io/kueue/bin/golangci-lint run --timeout 7m0s
make: *** [Makefile:162: ci-lint] Killed
```

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-31T14:27:50Z

/retest

after #823

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-31T15:27:08Z

/milestone v0.4

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-29T17:57:11Z

- **test/integration/controller/job/job_controller_test.go:452** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-29T17:50:43Z
```diff
@@ -429,17 +449,40 @@ var _ = ginkgo.Describe("Job controller", func() {
 	})
 })
 
-var _ = ginkgo.Describe("Job controller for workloads with no queue set", func() {
-	ginkgo.BeforeEach(func() {
+var _ = ginkgo.Describe("Job controller for workloads with no queue set", ginkgo.Ordered, func() {
```

  why not add `ContinueOnFailure` as well?

- **test/integration/controller/job/job_controller_test.go:79** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-29T17:53:45Z
```diff
@@ -46,31 +46,59 @@ import (
 const (
 	parallelism       = 4
 	jobName           = "test-job"
-	jobNamespace      = "default"
 	labelKey          = "cloud.provider.com/instance"
 	priorityClassName = "test-priority-class"
 	priorityValue     = 10
+	parentJobName     = jobName + "-parent"
+	childJobName      = jobName + "-child"
 )
 
 var (
-	wlLookupKey               = types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobName), Namespace: jobNamespace}
 	ignoreConditionTimestamps = cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
 )
 
 // +kubebuilder:docs-gen:collapse=Imports
 
-var _ = ginkgo.Describe("Job controller", func() {
+var _ = ginkgo.Describe("Job controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
 
-	ginkgo.BeforeEach(func() {
+	ginkgo.BeforeAll(func() {
 		fwk = &framework.Framework{
 			ManagerSetup: managerSetup(jobframework.WithManageJobsWithoutQueueName(true)),
 			CRDPath:      crdPath,
 		}
 		ctx, cfg, k8sClient = fwk.Setup()
 	})
-	ginkgo.AfterEach(func() {
+	ginkgo.AfterAll(func() {
 		fwk.Teardown()
 	})
+
+	var (
+		jobNamespace      string
+		ns                *corev1.Namespace
+		wlLookupKey       types.NamespacedName
+		parentWlLookupKey types.NamespacedName
+		childWlLookupKey  types.NamespacedName
+		childLookupKey    types.NamespacedName
```

  why move all of these variables to this level, if they are only used in one case?

- **test/integration/controller/job/job_controller_test.go:91** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-29T17:54:16Z
```diff
@@ -46,31 +46,59 @@ import (
 const (
 	parallelism       = 4
 	jobName           = "test-job"
-	jobNamespace      = "default"
 	labelKey          = "cloud.provider.com/instance"
 	priorityClassName = "test-priority-class"
 	priorityValue     = 10
+	parentJobName     = jobName + "-parent"
+	childJobName      = jobName + "-child"
 )
 
 var (
-	wlLookupKey               = types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobName), Namespace: jobNamespace}
 	ignoreConditionTimestamps = cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
 )
 
 // +kubebuilder:docs-gen:collapse=Imports
 
-var _ = ginkgo.Describe("Job controller", func() {
+var _ = ginkgo.Describe("Job controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
 
-	ginkgo.BeforeEach(func() {
+	ginkgo.BeforeAll(func() {
 		fwk = &framework.Framework{
 			ManagerSetup: managerSetup(jobframework.WithManageJobsWithoutQueueName(true)),
 			CRDPath:      crdPath,
 		}
 		ctx, cfg, k8sClient = fwk.Setup()
 	})
-	ginkgo.AfterEach(func() {
+	ginkgo.AfterAll(func() {
 		fwk.Teardown()
 	})
+
+	var (
+		jobNamespace      string
+		ns                *corev1.Namespace
+		wlLookupKey       types.NamespacedName
+		parentWlLookupKey types.NamespacedName
+		childWlLookupKey  types.NamespacedName
+		childLookupKey    types.NamespacedName
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns := &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "core-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+		jobNamespace = ns.Name
```

  get rid of this variable. Just use `ns.Name` were needed.

- **test/integration/controller/job/job_controller_test.go:468** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-29T17:55:15Z
```diff
@@ -429,17 +449,40 @@ var _ = ginkgo.Describe("Job controller", func() {
 	})
 })
 
-var _ = ginkgo.Describe("Job controller for workloads with no queue set", func() {
-	ginkgo.BeforeEach(func() {
+var _ = ginkgo.Describe("Job controller for workloads with no queue set", ginkgo.Ordered, func() {
+	ginkgo.BeforeAll(func() {
 		fwk = &framework.Framework{
 			ManagerSetup: managerSetup(),
 			CRDPath:      crdPath,
 		}
 		ctx, cfg, k8sClient = fwk.Setup()
 	})
-	ginkgo.AfterEach(func() {
+	ginkgo.AfterAll(func() {
 		fwk.Teardown()
 	})
+
+	var (
+		jobNamespace   string
+		ns             *corev1.Namespace
+		wlLookupKey    types.NamespacedName
+		childLookupKey types.NamespacedName
```

  only create were needed

- **test/integration/controller/rayjob/rayjob_controller_test.go:85** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-29T17:56:27Z
```diff
@@ -79,10 +76,30 @@ var _ = ginkgo.Describe("Job controller", func() {
 
 		ctx, cfg, k8sClient = fwk.Setup()
 	})
-	ginkgo.AfterEach(func() {
+	ginkgo.AfterAll(func() {
 		fwk.Teardown()
 	})
 
+	var (
+		ns           *corev1.Namespace
+		jobNamespace string
```

  use `ns.Name`

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-30T11:33:47Z

Thanks! Great improvement!
/lgtm

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-05-30T13:15:45Z

- **test/integration/controller/job/job_controller_test.go:79** by [@trasc](https://github.com/trasc) — 2023-05-30T10:09:06Z
```diff
@@ -46,31 +46,59 @@ import (
 const (
 	parallelism       = 4
 	jobName           = "test-job"
-	jobNamespace      = "default"
 	labelKey          = "cloud.provider.com/instance"
 	priorityClassName = "test-priority-class"
 	priorityValue     = 10
+	parentJobName     = jobName + "-parent"
+	childJobName      = jobName + "-child"
 )
 
 var (
-	wlLookupKey               = types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobName), Namespace: jobNamespace}
 	ignoreConditionTimestamps = cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
 )
 
 // +kubebuilder:docs-gen:collapse=Imports
 
-var _ = ginkgo.Describe("Job controller", func() {
+var _ = ginkgo.Describe("Job controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
 
-	ginkgo.BeforeEach(func() {
+	ginkgo.BeforeAll(func() {
 		fwk = &framework.Framework{
 			ManagerSetup: managerSetup(jobframework.WithManageJobsWithoutQueueName(true)),
 			CRDPath:      crdPath,
 		}
 		ctx, cfg, k8sClient = fwk.Setup()
 	})
-	ginkgo.AfterEach(func() {
+	ginkgo.AfterAll(func() {
 		fwk.Teardown()
 	})
+
+	var (
+		jobNamespace      string
+		ns                *corev1.Namespace
+		wlLookupKey       types.NamespacedName
+		parentWlLookupKey types.NamespacedName
+		childWlLookupKey  types.NamespacedName
+		childLookupKey    types.NamespacedName
```

  I did some cleanup but kept those that are used in more then one test

- **test/integration/controller/job/job_controller_test.go:91** by [@trasc](https://github.com/trasc) — 2023-05-30T10:09:16Z
```diff
@@ -46,31 +46,59 @@ import (
 const (
 	parallelism       = 4
 	jobName           = "test-job"
-	jobNamespace      = "default"
 	labelKey          = "cloud.provider.com/instance"
 	priorityClassName = "test-priority-class"
 	priorityValue     = 10
+	parentJobName     = jobName + "-parent"
+	childJobName      = jobName + "-child"
 )
 
 var (
-	wlLookupKey               = types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobName), Namespace: jobNamespace}
 	ignoreConditionTimestamps = cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
 )
 
 // +kubebuilder:docs-gen:collapse=Imports
 
-var _ = ginkgo.Describe("Job controller", func() {
+var _ = ginkgo.Describe("Job controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
 
-	ginkgo.BeforeEach(func() {
+	ginkgo.BeforeAll(func() {
 		fwk = &framework.Framework{
 			ManagerSetup: managerSetup(jobframework.WithManageJobsWithoutQueueName(true)),
 			CRDPath:      crdPath,
 		}
 		ctx, cfg, k8sClient = fwk.Setup()
 	})
-	ginkgo.AfterEach(func() {
+	ginkgo.AfterAll(func() {
 		fwk.Teardown()
 	})
+
+	var (
+		jobNamespace      string
+		ns                *corev1.Namespace
+		wlLookupKey       types.NamespacedName
+		parentWlLookupKey types.NamespacedName
+		childWlLookupKey  types.NamespacedName
+		childLookupKey    types.NamespacedName
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns := &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "core-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+		jobNamespace = ns.Name
```

  done

- **test/integration/controller/job/job_controller_test.go:452** by [@trasc](https://github.com/trasc) — 2023-05-30T10:17:57Z
```diff
@@ -429,17 +449,40 @@ var _ = ginkgo.Describe("Job controller", func() {
 	})
 })
 
-var _ = ginkgo.Describe("Job controller for workloads with no queue set", func() {
-	ginkgo.BeforeEach(func() {
+var _ = ginkgo.Describe("Job controller for workloads with no queue set", ginkgo.Ordered, func() {
```

  done

- **test/integration/controller/job/job_controller_test.go:468** by [@trasc](https://github.com/trasc) — 2023-05-30T10:18:22Z
```diff
@@ -429,17 +449,40 @@ var _ = ginkgo.Describe("Job controller", func() {
 	})
 })
 
-var _ = ginkgo.Describe("Job controller for workloads with no queue set", func() {
-	ginkgo.BeforeEach(func() {
+var _ = ginkgo.Describe("Job controller for workloads with no queue set", ginkgo.Ordered, func() {
+	ginkgo.BeforeAll(func() {
 		fwk = &framework.Framework{
 			ManagerSetup: managerSetup(),
 			CRDPath:      crdPath,
 		}
 		ctx, cfg, k8sClient = fwk.Setup()
 	})
-	ginkgo.AfterEach(func() {
+	ginkgo.AfterAll(func() {
 		fwk.Teardown()
 	})
+
+	var (
+		jobNamespace   string
+		ns             *corev1.Namespace
+		wlLookupKey    types.NamespacedName
+		childLookupKey types.NamespacedName
```

  done

- **test/integration/controller/rayjob/rayjob_controller_test.go:85** by [@trasc](https://github.com/trasc) — 2023-05-30T10:18:41Z
```diff
@@ -79,10 +76,30 @@ var _ = ginkgo.Describe("Job controller", func() {
 
 		ctx, cfg, k8sClient = fwk.Setup()
 	})
-	ginkgo.AfterEach(func() {
+	ginkgo.AfterAll(func() {
 		fwk.Teardown()
 	})
 
+	var (
+		ns           *corev1.Namespace
+		jobNamespace string
```

  done
