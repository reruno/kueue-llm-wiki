# PR #8207: Add unit tests for sortedDomainsWithLeader sorting behavior

**Summary**: Add unit tests for sortedDomainsWithLeader sorting behavior

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8207

**Last updated**: 2026-01-14T09:10:37Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@IrvingMg](https://github.com/IrvingMg)
- **Branch**: `investigate-algorithm-tas-pod-groups` → `main`
- **Created**: 2025-12-12T11:13:38Z
- **Updated**: 2026-01-14T09:10:37Z
- **Merged**: 2026-01-14T09:09:35Z
- **Closed**: 2026-01-14T09:09:35Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 2   **+374 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind cleanup

#### What this PR does / why we need it:
Adds unit tests for sortedDomainsWithLeader to document and verify its sorting behavior.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Closes #8178 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-12-12T11:13:45Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | be71fab3a8edfe18266f9f0245908204486873d4 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/696522cd0e51a70008ba46ab |

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2025-12-12T11:14:33Z

The current behavior seems to be correct. In the `sortedDomainsWithLeader` function, only `sliceStateWithLeader` changes depending on the algorithm. `stateWithLeader` is only used to break ties and is always sorted in ascending order. This matches how `sortedDomains` works, where the state is always sorted ascending to help prioritize domains that can fit slices with minimal leftover capacity.

https://github.com/kubernetes-sigs/kueue/blob/98e0af88ca368e2a50c52091365ffaf58fbeedd7/pkg/cache/scheduler/tas_flavor_snapshot.go#L1326-L1333

/cc @PBundyra @mimowo

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2025-12-18T13:52:31Z

@PBundyra PTAL

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-01-13T15:32:29Z

/lgtm
cc @mimowo

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-01-13T15:32:35Z

Thank you @IrvingMg!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-13T15:32:38Z

LGTM label has been added.  <details>Git tree hash: 7a450e0830052af4a153c2d181e03648d8198185</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-13T15:58:13Z

> /lgtm
> cc @mimowo 

Thank you @PBundyra for reviewing and letting me know. I can approve, but feel free to also use your new maintainer capability in this case.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-13T15:58:52Z

Thank you @IrvingMg for working on that and addressing all the comments!

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-01-14T08:42:24Z

Oh right, in that case!
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-14T08:42:34Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8207#" title="Author self-approved">IrvingMg</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8207#issuecomment-3748438803" title="Approved">PBundyra</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [PBundyra]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-16T07:47:30Z

- **pkg/cache/scheduler/tas_flavor_snapshot_test.go:438** by [@mimowo](https://github.com/mimowo) — 2025-12-16T07:47:30Z
```diff
@@ -429,3 +429,90 @@ func TestHasLevel(t *testing.T) {
 		})
 	}
 }
+
+// TestSortedDomainsWithLeader verifies the sorting criteria (in order of priority):
+// 1. leaderState - descending (always)
+// 2. sliceStateWithLeader - descending (BestFit) or ascending (LeastFreeCapacity)
+// 3. stateWithLeader - ascending (always, as tiebreaker)
+// 4. levelValues - ascending (always, as final tiebreaker)
+func TestSortedDomainsWithLeader(t *testing.T) {
```

  Hm instead of testing the unexperted function, could you maybe add a test which demonstrates the overall domain finding, I think TestFindTopologyAssignments in tas_cache would be the place to go.
  
  We may keep this test too, but the other one is more important to me

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-12-16T11:59:55Z

- **pkg/cache/scheduler/tas_flavor_snapshot_test.go:438** by [@IrvingMg](https://github.com/IrvingMg) — 2025-12-16T11:59:54Z
```diff
@@ -429,3 +429,90 @@ func TestHasLevel(t *testing.T) {
 		})
 	}
 }
+
+// TestSortedDomainsWithLeader verifies the sorting criteria (in order of priority):
+// 1. leaderState - descending (always)
+// 2. sliceStateWithLeader - descending (BestFit) or ascending (LeastFreeCapacity)
+// 3. stateWithLeader - ascending (always, as tiebreaker)
+// 4. levelValues - ascending (always, as final tiebreaker)
+func TestSortedDomainsWithLeader(t *testing.T) {
```

  I've added two new test cases in TestFindTopologyAssignments.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-01-08T10:08:45Z

- **pkg/cache/scheduler/tas_cache_test.go:5157** by [@PBundyra](https://github.com/PBundyra) — 2026-01-08T10:08:45Z
```diff
@@ -5072,6 +5072,157 @@ func TestFindTopologyAssignments(t *testing.T) {
 				},
 			},
 		},
+		// With LeastFreeCapacity (Unconstrained), domains are sorted by slice capacity ascending.
+		// b1: 8 GPU -> 4 slices, b2: 5 GPU -> 2 slices, b3: 4 GPU -> 2 slices
+		// b3 is selected because it has fewer slices, and as tiebreaker, less free worker capacity than b2.
+		"podset group with LeastFreeCapacity prefers domain with less slice capacity": {
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1").
+					Label(tasBlockLabel, "b1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("8"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b2").
+					Label(tasBlockLabel, "b2").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("5"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b3").
+					Label(tasBlockLabel, "b3").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("4"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+			},
+			levels: []string{tasBlockLabel},
+			podSets: []PodSetTestCase{
+				{
+					podSetName: "leader",
+					topologyRequest: &kueue.PodSetTopologyRequest{
+						Unconstrained: ptr.To(true),
+					},
+					requests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					podSetGroupName: ptr.To("sameGroup"),
+					count:           1,
+					wantAssignment: &tas.TopologyAssignment{
+						Levels: []string{tasBlockLabel},
+						Domains: []tas.TopologyDomainAssignment{
+							{
+								Count:  1,
+								Values: []string{"b3"},
+							},
+						},
+					},
+				},
+				{
+					podSetName: "workers",
+					topologyRequest: &kueue.PodSetTopologyRequest{
+						Unconstrained:               ptr.To(true),
+						PodSetSliceSize:             ptr.To(int32(2)),
+						PodSetSliceRequiredTopology: ptr.To(tasBlockLabel),
+					},
+					requests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+						"example.com/gpu":  1,
+					},
+					podSetGroupName: ptr.To("sameGroup"),
+					count:           4,
+					wantAssignment: &tas.TopologyAssignment{
+						Levels: []string{tasBlockLabel},
+						Domains: []tas.TopologyDomainAssignment{
+							{
+								Count:  4,
+								Values: []string{"b3"},
+							},
+						},
+					},
+				},
+			},
+		},
+		// When slice capacity is equal, the tiebreaker is free worker capacity (ascending).
+		// b1: 5 GPU -> 2 slices, 5 workers; b2: 4 GPU -> 2 slices, 4 workers
+		// b2 is selected because both have 2 slices, but b2 has less free worker capacity.
```

  How does it differ from the first case in this PR?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-01-08T10:19:31Z

- **pkg/cache/scheduler/tas_flavor_snapshot_test.go:472** by [@PBundyra](https://github.com/PBundyra) — 2026-01-08T10:19:31Z
```diff
@@ -429,3 +429,90 @@ func TestHasLevel(t *testing.T) {
 		})
 	}
 }
+
+// TestSortedDomainsWithLeader verifies the sorting criteria (in order of priority):
+// 1. leaderState - descending (always)
+// 2. sliceStateWithLeader - descending (BestFit) or ascending (LeastFreeCapacity)
+// 3. stateWithLeader - ascending (always, as tiebreaker)
+// 4. levelValues - ascending (always, as final tiebreaker)
+func TestSortedDomainsWithLeader(t *testing.T) {
+	levels := []string{"block"}
+
+	testCases := map[string]struct {
+		domains       []*domain
+		unconstrained bool
+		wantOrder     []string
+	}{
+		"leaderState descending: domains that can host leader come first": {
+			domains: []*domain{
+				{id: "no-leader", leaderState: 0, sliceStateWithLeader: 10, stateWithLeader: 10, levelValues: []string{"a"}},
+				{id: "has-leader", leaderState: 1, sliceStateWithLeader: 1, stateWithLeader: 1, levelValues: []string{"b"}},
+			},
+			unconstrained: false,
+			wantOrder:     []string{"has-leader", "no-leader"},
+		},
+		"BestFit: sliceStateWithLeader descending": {
+			domains: []*domain{
+				{id: "a", leaderState: 1, sliceStateWithLeader: 3, stateWithLeader: 1, levelValues: []string{"a"}},
+				{id: "b", leaderState: 1, sliceStateWithLeader: 1, stateWithLeader: 1, levelValues: []string{"b"}},
+				{id: "c", leaderState: 1, sliceStateWithLeader: 2, stateWithLeader: 1, levelValues: []string{"c"}},
+			},
+			unconstrained: false,
+			wantOrder:     []string{"a", "c", "b"},
+		},
+		"LeastFreeCapacity: sliceStateWithLeader ascending": {
+			domains: []*domain{
+				{id: "a", leaderState: 1, sliceStateWithLeader: 3, stateWithLeader: 1, levelValues: []string{"a"}},
+				{id: "b", leaderState: 1, sliceStateWithLeader: 1, stateWithLeader: 1, levelValues: []string{"b"}},
+				{id: "c", leaderState: 1, sliceStateWithLeader: 2, stateWithLeader: 1, levelValues: []string{"c"}},
+			},
+			unconstrained: true,
+			wantOrder:     []string{"b", "c", "a"},
+		},
+		"stateWithLeader ascending as tiebreaker for BestFit": {
```

  Let's keep the naming consistent
  ```suggestion
  		"BestFit: stateWithLeader ascending as tiebreaker": {
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-01-08T10:19:40Z

- **pkg/cache/scheduler/tas_flavor_snapshot_test.go:481** by [@PBundyra](https://github.com/PBundyra) — 2026-01-08T10:19:41Z
```diff
@@ -429,3 +429,90 @@ func TestHasLevel(t *testing.T) {
 		})
 	}
 }
+
+// TestSortedDomainsWithLeader verifies the sorting criteria (in order of priority):
+// 1. leaderState - descending (always)
+// 2. sliceStateWithLeader - descending (BestFit) or ascending (LeastFreeCapacity)
+// 3. stateWithLeader - ascending (always, as tiebreaker)
+// 4. levelValues - ascending (always, as final tiebreaker)
+func TestSortedDomainsWithLeader(t *testing.T) {
+	levels := []string{"block"}
+
+	testCases := map[string]struct {
+		domains       []*domain
+		unconstrained bool
+		wantOrder     []string
+	}{
+		"leaderState descending: domains that can host leader come first": {
+			domains: []*domain{
+				{id: "no-leader", leaderState: 0, sliceStateWithLeader: 10, stateWithLeader: 10, levelValues: []string{"a"}},
+				{id: "has-leader", leaderState: 1, sliceStateWithLeader: 1, stateWithLeader: 1, levelValues: []string{"b"}},
+			},
+			unconstrained: false,
+			wantOrder:     []string{"has-leader", "no-leader"},
+		},
+		"BestFit: sliceStateWithLeader descending": {
+			domains: []*domain{
+				{id: "a", leaderState: 1, sliceStateWithLeader: 3, stateWithLeader: 1, levelValues: []string{"a"}},
+				{id: "b", leaderState: 1, sliceStateWithLeader: 1, stateWithLeader: 1, levelValues: []string{"b"}},
+				{id: "c", leaderState: 1, sliceStateWithLeader: 2, stateWithLeader: 1, levelValues: []string{"c"}},
+			},
+			unconstrained: false,
+			wantOrder:     []string{"a", "c", "b"},
+		},
+		"LeastFreeCapacity: sliceStateWithLeader ascending": {
+			domains: []*domain{
+				{id: "a", leaderState: 1, sliceStateWithLeader: 3, stateWithLeader: 1, levelValues: []string{"a"}},
+				{id: "b", leaderState: 1, sliceStateWithLeader: 1, stateWithLeader: 1, levelValues: []string{"b"}},
+				{id: "c", leaderState: 1, sliceStateWithLeader: 2, stateWithLeader: 1, levelValues: []string{"c"}},
+			},
+			unconstrained: true,
+			wantOrder:     []string{"b", "c", "a"},
+		},
+		"stateWithLeader ascending as tiebreaker for BestFit": {
+			domains: []*domain{
+				{id: "large", leaderState: 1, sliceStateWithLeader: 5, stateWithLeader: 100, levelValues: []string{"a"}},
+				{id: "small", leaderState: 1, sliceStateWithLeader: 5, stateWithLeader: 10, levelValues: []string{"b"}},
+				{id: "medium", leaderState: 1, sliceStateWithLeader: 5, stateWithLeader: 50, levelValues: []string{"c"}},
+			},
+			unconstrained: false,
+			wantOrder:     []string{"small", "medium", "large"},
+		},
+		"stateWithLeader ascending as tiebreaker for LeastFreeCapacity": {
```

  ditto

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-01-08T10:32:15Z

- **pkg/cache/scheduler/tas_cache_test.go:5157** by [@IrvingMg](https://github.com/IrvingMg) — 2026-01-08T10:32:15Z
```diff
@@ -5072,6 +5072,157 @@ func TestFindTopologyAssignments(t *testing.T) {
 				},
 			},
 		},
+		// With LeastFreeCapacity (Unconstrained), domains are sorted by slice capacity ascending.
+		// b1: 8 GPU -> 4 slices, b2: 5 GPU -> 2 slices, b3: 4 GPU -> 2 slices
+		// b3 is selected because it has fewer slices, and as tiebreaker, less free worker capacity than b2.
+		"podset group with LeastFreeCapacity prefers domain with less slice capacity": {
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1").
+					Label(tasBlockLabel, "b1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("8"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b2").
+					Label(tasBlockLabel, "b2").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("5"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b3").
+					Label(tasBlockLabel, "b3").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("4"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+			},
+			levels: []string{tasBlockLabel},
+			podSets: []PodSetTestCase{
+				{
+					podSetName: "leader",
+					topologyRequest: &kueue.PodSetTopologyRequest{
+						Unconstrained: ptr.To(true),
+					},
+					requests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					podSetGroupName: ptr.To("sameGroup"),
+					count:           1,
+					wantAssignment: &tas.TopologyAssignment{
+						Levels: []string{tasBlockLabel},
+						Domains: []tas.TopologyDomainAssignment{
+							{
+								Count:  1,
+								Values: []string{"b3"},
+							},
+						},
+					},
+				},
+				{
+					podSetName: "workers",
+					topologyRequest: &kueue.PodSetTopologyRequest{
+						Unconstrained:               ptr.To(true),
+						PodSetSliceSize:             ptr.To(int32(2)),
+						PodSetSliceRequiredTopology: ptr.To(tasBlockLabel),
+					},
+					requests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+						"example.com/gpu":  1,
+					},
+					podSetGroupName: ptr.To("sameGroup"),
+					count:           4,
+					wantAssignment: &tas.TopologyAssignment{
+						Levels: []string{tasBlockLabel},
+						Domains: []tas.TopologyDomainAssignment{
+							{
+								Count:  4,
+								Values: []string{"b3"},
+							},
+						},
+					},
+				},
+			},
+		},
+		// When slice capacity is equal, the tiebreaker is free worker capacity (ascending).
+		// b1: 5 GPU -> 2 slices, 5 workers; b2: 4 GPU -> 2 slices, 4 workers
+		// b2 is selected because both have 2 slices, but b2 has less free worker capacity.
```

  First case demonstrates LeastFreeCapacity prefers domains with fewer slices (by excluding b1). Then a tiebreaker is applied between b2 and b3.
  
  Second case focuses on demonstrating the tiebreaker criterion: when slice capacity is equal, prefer domains with less free capacity.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-01-08T10:35:53Z

- **pkg/cache/scheduler/tas_flavor_snapshot_test.go:472** by [@IrvingMg](https://github.com/IrvingMg) — 2026-01-08T10:35:53Z
```diff
@@ -429,3 +429,90 @@ func TestHasLevel(t *testing.T) {
 		})
 	}
 }
+
+// TestSortedDomainsWithLeader verifies the sorting criteria (in order of priority):
+// 1. leaderState - descending (always)
+// 2. sliceStateWithLeader - descending (BestFit) or ascending (LeastFreeCapacity)
+// 3. stateWithLeader - ascending (always, as tiebreaker)
+// 4. levelValues - ascending (always, as final tiebreaker)
+func TestSortedDomainsWithLeader(t *testing.T) {
+	levels := []string{"block"}
+
+	testCases := map[string]struct {
+		domains       []*domain
+		unconstrained bool
+		wantOrder     []string
+	}{
+		"leaderState descending: domains that can host leader come first": {
+			domains: []*domain{
+				{id: "no-leader", leaderState: 0, sliceStateWithLeader: 10, stateWithLeader: 10, levelValues: []string{"a"}},
+				{id: "has-leader", leaderState: 1, sliceStateWithLeader: 1, stateWithLeader: 1, levelValues: []string{"b"}},
+			},
+			unconstrained: false,
+			wantOrder:     []string{"has-leader", "no-leader"},
+		},
+		"BestFit: sliceStateWithLeader descending": {
+			domains: []*domain{
+				{id: "a", leaderState: 1, sliceStateWithLeader: 3, stateWithLeader: 1, levelValues: []string{"a"}},
+				{id: "b", leaderState: 1, sliceStateWithLeader: 1, stateWithLeader: 1, levelValues: []string{"b"}},
+				{id: "c", leaderState: 1, sliceStateWithLeader: 2, stateWithLeader: 1, levelValues: []string{"c"}},
+			},
+			unconstrained: false,
+			wantOrder:     []string{"a", "c", "b"},
+		},
+		"LeastFreeCapacity: sliceStateWithLeader ascending": {
+			domains: []*domain{
+				{id: "a", leaderState: 1, sliceStateWithLeader: 3, stateWithLeader: 1, levelValues: []string{"a"}},
+				{id: "b", leaderState: 1, sliceStateWithLeader: 1, stateWithLeader: 1, levelValues: []string{"b"}},
+				{id: "c", leaderState: 1, sliceStateWithLeader: 2, stateWithLeader: 1, levelValues: []string{"c"}},
+			},
+			unconstrained: true,
+			wantOrder:     []string{"b", "c", "a"},
+		},
+		"stateWithLeader ascending as tiebreaker for BestFit": {
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-01-08T10:36:02Z

- **pkg/cache/scheduler/tas_flavor_snapshot_test.go:481** by [@IrvingMg](https://github.com/IrvingMg) — 2026-01-08T10:36:02Z
```diff
@@ -429,3 +429,90 @@ func TestHasLevel(t *testing.T) {
 		})
 	}
 }
+
+// TestSortedDomainsWithLeader verifies the sorting criteria (in order of priority):
+// 1. leaderState - descending (always)
+// 2. sliceStateWithLeader - descending (BestFit) or ascending (LeastFreeCapacity)
+// 3. stateWithLeader - ascending (always, as tiebreaker)
+// 4. levelValues - ascending (always, as final tiebreaker)
+func TestSortedDomainsWithLeader(t *testing.T) {
+	levels := []string{"block"}
+
+	testCases := map[string]struct {
+		domains       []*domain
+		unconstrained bool
+		wantOrder     []string
+	}{
+		"leaderState descending: domains that can host leader come first": {
+			domains: []*domain{
+				{id: "no-leader", leaderState: 0, sliceStateWithLeader: 10, stateWithLeader: 10, levelValues: []string{"a"}},
+				{id: "has-leader", leaderState: 1, sliceStateWithLeader: 1, stateWithLeader: 1, levelValues: []string{"b"}},
+			},
+			unconstrained: false,
+			wantOrder:     []string{"has-leader", "no-leader"},
+		},
+		"BestFit: sliceStateWithLeader descending": {
+			domains: []*domain{
+				{id: "a", leaderState: 1, sliceStateWithLeader: 3, stateWithLeader: 1, levelValues: []string{"a"}},
+				{id: "b", leaderState: 1, sliceStateWithLeader: 1, stateWithLeader: 1, levelValues: []string{"b"}},
+				{id: "c", leaderState: 1, sliceStateWithLeader: 2, stateWithLeader: 1, levelValues: []string{"c"}},
+			},
+			unconstrained: false,
+			wantOrder:     []string{"a", "c", "b"},
+		},
+		"LeastFreeCapacity: sliceStateWithLeader ascending": {
+			domains: []*domain{
+				{id: "a", leaderState: 1, sliceStateWithLeader: 3, stateWithLeader: 1, levelValues: []string{"a"}},
+				{id: "b", leaderState: 1, sliceStateWithLeader: 1, stateWithLeader: 1, levelValues: []string{"b"}},
+				{id: "c", leaderState: 1, sliceStateWithLeader: 2, stateWithLeader: 1, levelValues: []string{"c"}},
+			},
+			unconstrained: true,
+			wantOrder:     []string{"b", "c", "a"},
+		},
+		"stateWithLeader ascending as tiebreaker for BestFit": {
+			domains: []*domain{
+				{id: "large", leaderState: 1, sliceStateWithLeader: 5, stateWithLeader: 100, levelValues: []string{"a"}},
+				{id: "small", leaderState: 1, sliceStateWithLeader: 5, stateWithLeader: 10, levelValues: []string{"b"}},
+				{id: "medium", leaderState: 1, sliceStateWithLeader: 5, stateWithLeader: 50, levelValues: []string{"c"}},
+			},
+			unconstrained: false,
+			wantOrder:     []string{"small", "medium", "large"},
+		},
+		"stateWithLeader ascending as tiebreaker for LeastFreeCapacity": {
```

  Done.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-01-08T12:58:24Z

- **pkg/cache/scheduler/tas_cache_test.go:5157** by [@PBundyra](https://github.com/PBundyra) — 2026-01-08T12:58:24Z
```diff
@@ -5072,6 +5072,157 @@ func TestFindTopologyAssignments(t *testing.T) {
 				},
 			},
 		},
+		// With LeastFreeCapacity (Unconstrained), domains are sorted by slice capacity ascending.
+		// b1: 8 GPU -> 4 slices, b2: 5 GPU -> 2 slices, b3: 4 GPU -> 2 slices
+		// b3 is selected because it has fewer slices, and as tiebreaker, less free worker capacity than b2.
+		"podset group with LeastFreeCapacity prefers domain with less slice capacity": {
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1").
+					Label(tasBlockLabel, "b1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("8"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b2").
+					Label(tasBlockLabel, "b2").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("5"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b3").
+					Label(tasBlockLabel, "b3").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("4"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+			},
+			levels: []string{tasBlockLabel},
+			podSets: []PodSetTestCase{
+				{
+					podSetName: "leader",
+					topologyRequest: &kueue.PodSetTopologyRequest{
+						Unconstrained: ptr.To(true),
+					},
+					requests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					podSetGroupName: ptr.To("sameGroup"),
+					count:           1,
+					wantAssignment: &tas.TopologyAssignment{
+						Levels: []string{tasBlockLabel},
+						Domains: []tas.TopologyDomainAssignment{
+							{
+								Count:  1,
+								Values: []string{"b3"},
+							},
+						},
+					},
+				},
+				{
+					podSetName: "workers",
+					topologyRequest: &kueue.PodSetTopologyRequest{
+						Unconstrained:               ptr.To(true),
+						PodSetSliceSize:             ptr.To(int32(2)),
+						PodSetSliceRequiredTopology: ptr.To(tasBlockLabel),
+					},
+					requests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+						"example.com/gpu":  1,
+					},
+					podSetGroupName: ptr.To("sameGroup"),
+					count:           4,
+					wantAssignment: &tas.TopologyAssignment{
+						Levels: []string{tasBlockLabel},
+						Domains: []tas.TopologyDomainAssignment{
+							{
+								Count:  4,
+								Values: []string{"b3"},
+							},
+						},
+					},
+				},
+			},
+		},
+		// When slice capacity is equal, the tiebreaker is free worker capacity (ascending).
+		// b1: 5 GPU -> 2 slices, 5 workers; b2: 4 GPU -> 2 slices, 4 workers
+		// b2 is selected because both have 2 slices, but b2 has less free worker capacity.
```

  It seems like the second test is entirely covered within the first one.
  
  Additionally, I think with this setup BestFit would also return "b3" domain in the first test, and "b2" in the second one. Please add a scenario that would behave differently on BestFit and LeastFreeCapacity. The sliceStateWithLeader should differ on various nodes to observe the difference

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-08T13:36:43Z

- **pkg/cache/scheduler/tas_cache_test.go:5157** by [@mimowo](https://github.com/mimowo) — 2026-01-08T13:36:42Z
```diff
@@ -5072,6 +5072,157 @@ func TestFindTopologyAssignments(t *testing.T) {
 				},
 			},
 		},
+		// With LeastFreeCapacity (Unconstrained), domains are sorted by slice capacity ascending.
+		// b1: 8 GPU -> 4 slices, b2: 5 GPU -> 2 slices, b3: 4 GPU -> 2 slices
+		// b3 is selected because it has fewer slices, and as tiebreaker, less free worker capacity than b2.
+		"podset group with LeastFreeCapacity prefers domain with less slice capacity": {
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1").
+					Label(tasBlockLabel, "b1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("8"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b2").
+					Label(tasBlockLabel, "b2").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("5"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b3").
+					Label(tasBlockLabel, "b3").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("4"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+			},
+			levels: []string{tasBlockLabel},
+			podSets: []PodSetTestCase{
+				{
+					podSetName: "leader",
+					topologyRequest: &kueue.PodSetTopologyRequest{
+						Unconstrained: ptr.To(true),
+					},
+					requests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					podSetGroupName: ptr.To("sameGroup"),
+					count:           1,
+					wantAssignment: &tas.TopologyAssignment{
+						Levels: []string{tasBlockLabel},
+						Domains: []tas.TopologyDomainAssignment{
+							{
+								Count:  1,
+								Values: []string{"b3"},
+							},
+						},
+					},
+				},
+				{
+					podSetName: "workers",
+					topologyRequest: &kueue.PodSetTopologyRequest{
+						Unconstrained:               ptr.To(true),
+						PodSetSliceSize:             ptr.To(int32(2)),
+						PodSetSliceRequiredTopology: ptr.To(tasBlockLabel),
+					},
+					requests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+						"example.com/gpu":  1,
+					},
+					podSetGroupName: ptr.To("sameGroup"),
+					count:           4,
+					wantAssignment: &tas.TopologyAssignment{
+						Levels: []string{tasBlockLabel},
+						Domains: []tas.TopologyDomainAssignment{
+							{
+								Count:  4,
+								Values: []string{"b3"},
+							},
+						},
+					},
+				},
+			},
+		},
+		// When slice capacity is equal, the tiebreaker is free worker capacity (ascending).
+		// b1: 5 GPU -> 2 slices, 5 workers; b2: 4 GPU -> 2 slices, 4 workers
+		// b2 is selected because both have 2 slices, but b2 has less free worker capacity.
```

  > It seems like the second test is entirely covered within the first one.
  
  What do you mean here? That the execution paths for the second test case are subset of execution paths for the first one? I guess this could be easily verified.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-01-08T14:42:09Z

- **pkg/cache/scheduler/tas_cache_test.go:5157** by [@IrvingMg](https://github.com/IrvingMg) — 2026-01-08T14:42:09Z
```diff
@@ -5072,6 +5072,157 @@ func TestFindTopologyAssignments(t *testing.T) {
 				},
 			},
 		},
+		// With LeastFreeCapacity (Unconstrained), domains are sorted by slice capacity ascending.
+		// b1: 8 GPU -> 4 slices, b2: 5 GPU -> 2 slices, b3: 4 GPU -> 2 slices
+		// b3 is selected because it has fewer slices, and as tiebreaker, less free worker capacity than b2.
+		"podset group with LeastFreeCapacity prefers domain with less slice capacity": {
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1").
+					Label(tasBlockLabel, "b1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("8"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b2").
+					Label(tasBlockLabel, "b2").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("5"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b3").
+					Label(tasBlockLabel, "b3").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("4"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+			},
+			levels: []string{tasBlockLabel},
+			podSets: []PodSetTestCase{
+				{
+					podSetName: "leader",
+					topologyRequest: &kueue.PodSetTopologyRequest{
+						Unconstrained: ptr.To(true),
+					},
+					requests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					podSetGroupName: ptr.To("sameGroup"),
+					count:           1,
+					wantAssignment: &tas.TopologyAssignment{
+						Levels: []string{tasBlockLabel},
+						Domains: []tas.TopologyDomainAssignment{
+							{
+								Count:  1,
+								Values: []string{"b3"},
+							},
+						},
+					},
+				},
+				{
+					podSetName: "workers",
+					topologyRequest: &kueue.PodSetTopologyRequest{
+						Unconstrained:               ptr.To(true),
+						PodSetSliceSize:             ptr.To(int32(2)),
+						PodSetSliceRequiredTopology: ptr.To(tasBlockLabel),
+					},
+					requests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+						"example.com/gpu":  1,
+					},
+					podSetGroupName: ptr.To("sameGroup"),
+					count:           4,
+					wantAssignment: &tas.TopologyAssignment{
+						Levels: []string{tasBlockLabel},
+						Domains: []tas.TopologyDomainAssignment{
+							{
+								Count:  4,
+								Values: []string{"b3"},
+							},
+						},
+					},
+				},
+			},
+		},
+		// When slice capacity is equal, the tiebreaker is free worker capacity (ascending).
+		// b1: 5 GPU -> 2 slices, 5 workers; b2: 4 GPU -> 2 slices, 4 workers
+		// b2 is selected because both have 2 slices, but b2 has less free worker capacity.
```

  > Additionally, I think with this setup BestFit would also return "b3" domain in the first test, and "b2" in the second one. Please add a scenario that would behave differently on BestFit and LeastFreeCapacity. The sliceStateWithLeader should differ on various nodes to observe the difference
  
  IIUC, for podset groups, both algorithms behave the same in all setups. Since podset groups must be placed together in a single domain, the result is the same for both approaches. For single-domain selection, “minimum capacity that fits” = “first fit in ascending order.” Differences only appear when workloads can be split across multiple domains.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-01-08T14:49:31Z

- **pkg/cache/scheduler/tas_cache_test.go:5157** by [@PBundyra](https://github.com/PBundyra) — 2026-01-08T14:49:31Z
```diff
@@ -5072,6 +5072,157 @@ func TestFindTopologyAssignments(t *testing.T) {
 				},
 			},
 		},
+		// With LeastFreeCapacity (Unconstrained), domains are sorted by slice capacity ascending.
+		// b1: 8 GPU -> 4 slices, b2: 5 GPU -> 2 slices, b3: 4 GPU -> 2 slices
+		// b3 is selected because it has fewer slices, and as tiebreaker, less free worker capacity than b2.
+		"podset group with LeastFreeCapacity prefers domain with less slice capacity": {
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1").
+					Label(tasBlockLabel, "b1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("8"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b2").
+					Label(tasBlockLabel, "b2").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("5"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b3").
+					Label(tasBlockLabel, "b3").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("4"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+			},
+			levels: []string{tasBlockLabel},
+			podSets: []PodSetTestCase{
+				{
+					podSetName: "leader",
+					topologyRequest: &kueue.PodSetTopologyRequest{
+						Unconstrained: ptr.To(true),
+					},
+					requests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					podSetGroupName: ptr.To("sameGroup"),
+					count:           1,
+					wantAssignment: &tas.TopologyAssignment{
+						Levels: []string{tasBlockLabel},
+						Domains: []tas.TopologyDomainAssignment{
+							{
+								Count:  1,
+								Values: []string{"b3"},
+							},
+						},
+					},
+				},
+				{
+					podSetName: "workers",
+					topologyRequest: &kueue.PodSetTopologyRequest{
+						Unconstrained:               ptr.To(true),
+						PodSetSliceSize:             ptr.To(int32(2)),
+						PodSetSliceRequiredTopology: ptr.To(tasBlockLabel),
+					},
+					requests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+						"example.com/gpu":  1,
+					},
+					podSetGroupName: ptr.To("sameGroup"),
+					count:           4,
+					wantAssignment: &tas.TopologyAssignment{
+						Levels: []string{tasBlockLabel},
+						Domains: []tas.TopologyDomainAssignment{
+							{
+								Count:  4,
+								Values: []string{"b3"},
+							},
+						},
+					},
+				},
+			},
+		},
+		// When slice capacity is equal, the tiebreaker is free worker capacity (ascending).
+		// b1: 5 GPU -> 2 slices, 5 workers; b2: 4 GPU -> 2 slices, 4 workers
+		// b2 is selected because both have 2 slices, but b2 has less free worker capacity.
```

  I don't think we have a constraint that the whole podset group must be placed in a single domain? Where does this constraint come from?

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-01-08T15:29:47Z

- **pkg/cache/scheduler/tas_cache_test.go:5157** by [@IrvingMg](https://github.com/IrvingMg) — 2026-01-08T15:29:47Z
```diff
@@ -5072,6 +5072,157 @@ func TestFindTopologyAssignments(t *testing.T) {
 				},
 			},
 		},
+		// With LeastFreeCapacity (Unconstrained), domains are sorted by slice capacity ascending.
+		// b1: 8 GPU -> 4 slices, b2: 5 GPU -> 2 slices, b3: 4 GPU -> 2 slices
+		// b3 is selected because it has fewer slices, and as tiebreaker, less free worker capacity than b2.
+		"podset group with LeastFreeCapacity prefers domain with less slice capacity": {
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1").
+					Label(tasBlockLabel, "b1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("8"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b2").
+					Label(tasBlockLabel, "b2").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("5"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b3").
+					Label(tasBlockLabel, "b3").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("4"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+			},
+			levels: []string{tasBlockLabel},
+			podSets: []PodSetTestCase{
+				{
+					podSetName: "leader",
+					topologyRequest: &kueue.PodSetTopologyRequest{
+						Unconstrained: ptr.To(true),
+					},
+					requests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					podSetGroupName: ptr.To("sameGroup"),
+					count:           1,
+					wantAssignment: &tas.TopologyAssignment{
+						Levels: []string{tasBlockLabel},
+						Domains: []tas.TopologyDomainAssignment{
+							{
+								Count:  1,
+								Values: []string{"b3"},
+							},
+						},
+					},
+				},
+				{
+					podSetName: "workers",
+					topologyRequest: &kueue.PodSetTopologyRequest{
+						Unconstrained:               ptr.To(true),
+						PodSetSliceSize:             ptr.To(int32(2)),
+						PodSetSliceRequiredTopology: ptr.To(tasBlockLabel),
+					},
+					requests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+						"example.com/gpu":  1,
+					},
+					podSetGroupName: ptr.To("sameGroup"),
+					count:           4,
+					wantAssignment: &tas.TopologyAssignment{
+						Levels: []string{tasBlockLabel},
+						Domains: []tas.TopologyDomainAssignment{
+							{
+								Count:  4,
+								Values: []string{"b3"},
+							},
+						},
+					},
+				},
+			},
+		},
+		// When slice capacity is equal, the tiebreaker is free worker capacity (ascending).
+		// b1: 5 GPU -> 2 slices, 5 workers; b2: 4 GPU -> 2 slices, 4 workers
+		// b2 is selected because both have 2 slices, but b2 has less free worker capacity.
```

  I see, I thought podset groups had to be located together, but you’re right, they can be spread as well. However, for single-domain selection (when the entire podset group fits within one domain), it still looks like both algorithms produce the same result.
  
  So, I think the cases where we should see different behavior are multi-domain scenarios, where workers must be spread because no single domain has enough capacity. Is that right?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-01-09T14:02:04Z

- **pkg/cache/scheduler/tas_cache_test.go:5157** by [@PBundyra](https://github.com/PBundyra) — 2026-01-09T14:02:04Z
```diff
@@ -5072,6 +5072,157 @@ func TestFindTopologyAssignments(t *testing.T) {
 				},
 			},
 		},
+		// With LeastFreeCapacity (Unconstrained), domains are sorted by slice capacity ascending.
+		// b1: 8 GPU -> 4 slices, b2: 5 GPU -> 2 slices, b3: 4 GPU -> 2 slices
+		// b3 is selected because it has fewer slices, and as tiebreaker, less free worker capacity than b2.
+		"podset group with LeastFreeCapacity prefers domain with less slice capacity": {
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1").
+					Label(tasBlockLabel, "b1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("8"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b2").
+					Label(tasBlockLabel, "b2").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("5"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b3").
+					Label(tasBlockLabel, "b3").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("4"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+			},
+			levels: []string{tasBlockLabel},
+			podSets: []PodSetTestCase{
+				{
+					podSetName: "leader",
+					topologyRequest: &kueue.PodSetTopologyRequest{
+						Unconstrained: ptr.To(true),
+					},
+					requests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					podSetGroupName: ptr.To("sameGroup"),
+					count:           1,
+					wantAssignment: &tas.TopologyAssignment{
+						Levels: []string{tasBlockLabel},
+						Domains: []tas.TopologyDomainAssignment{
+							{
+								Count:  1,
+								Values: []string{"b3"},
+							},
+						},
+					},
+				},
+				{
+					podSetName: "workers",
+					topologyRequest: &kueue.PodSetTopologyRequest{
+						Unconstrained:               ptr.To(true),
+						PodSetSliceSize:             ptr.To(int32(2)),
+						PodSetSliceRequiredTopology: ptr.To(tasBlockLabel),
+					},
+					requests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+						"example.com/gpu":  1,
+					},
+					podSetGroupName: ptr.To("sameGroup"),
+					count:           4,
+					wantAssignment: &tas.TopologyAssignment{
+						Levels: []string{tasBlockLabel},
+						Domains: []tas.TopologyDomainAssignment{
+							{
+								Count:  4,
+								Values: []string{"b3"},
+							},
+						},
+					},
+				},
+			},
+		},
+		// When slice capacity is equal, the tiebreaker is free worker capacity (ascending).
+		// b1: 5 GPU -> 2 slices, 5 workers; b2: 4 GPU -> 2 slices, 4 workers
+		// b2 is selected because both have 2 slices, but b2 has less free worker capacity.
```

  That's right

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-01-12T08:52:36Z

- **pkg/cache/scheduler/tas_cache_test.go:5157** by [@IrvingMg](https://github.com/IrvingMg) — 2026-01-12T08:52:36Z
```diff
@@ -5072,6 +5072,157 @@ func TestFindTopologyAssignments(t *testing.T) {
 				},
 			},
 		},
+		// With LeastFreeCapacity (Unconstrained), domains are sorted by slice capacity ascending.
+		// b1: 8 GPU -> 4 slices, b2: 5 GPU -> 2 slices, b3: 4 GPU -> 2 slices
+		// b3 is selected because it has fewer slices, and as tiebreaker, less free worker capacity than b2.
+		"podset group with LeastFreeCapacity prefers domain with less slice capacity": {
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1").
+					Label(tasBlockLabel, "b1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("8"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b2").
+					Label(tasBlockLabel, "b2").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("5"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b3").
+					Label(tasBlockLabel, "b3").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("4"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+			},
+			levels: []string{tasBlockLabel},
+			podSets: []PodSetTestCase{
+				{
+					podSetName: "leader",
+					topologyRequest: &kueue.PodSetTopologyRequest{
+						Unconstrained: ptr.To(true),
+					},
+					requests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					podSetGroupName: ptr.To("sameGroup"),
+					count:           1,
+					wantAssignment: &tas.TopologyAssignment{
+						Levels: []string{tasBlockLabel},
+						Domains: []tas.TopologyDomainAssignment{
+							{
+								Count:  1,
+								Values: []string{"b3"},
+							},
+						},
+					},
+				},
+				{
+					podSetName: "workers",
+					topologyRequest: &kueue.PodSetTopologyRequest{
+						Unconstrained:               ptr.To(true),
+						PodSetSliceSize:             ptr.To(int32(2)),
+						PodSetSliceRequiredTopology: ptr.To(tasBlockLabel),
+					},
+					requests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+						"example.com/gpu":  1,
+					},
+					podSetGroupName: ptr.To("sameGroup"),
+					count:           4,
+					wantAssignment: &tas.TopologyAssignment{
+						Levels: []string{tasBlockLabel},
+						Domains: []tas.TopologyDomainAssignment{
+							{
+								Count:  4,
+								Values: []string{"b3"},
+							},
+						},
+					},
+				},
+			},
+		},
+		// When slice capacity is equal, the tiebreaker is free worker capacity (ascending).
+		// b1: 5 GPU -> 2 slices, 5 workers; b2: 4 GPU -> 2 slices, 4 workers
+		// b2 is selected because both have 2 slices, but b2 has less free worker capacity.
```

  Done. I've updated the tests, and they now show different behavior.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-01-12T13:45:20Z

- **pkg/cache/scheduler/tas_cache_test.go:5083** by [@PBundyra](https://github.com/PBundyra) — 2026-01-12T13:45:20Z
```diff
@@ -5074,6 +5074,275 @@ func TestFindTopologyAssignments(t *testing.T) {
 				},
 			},
 		},
+		// Podset group where workers must spread across multiple domains.
+		// Setup: 3 hosts with different GPU capacities, workers need more than any single host.
+		//        b1
+		//         |
+		//        r1
+		//    /   |    \
+		// x1:4  x2:3  x3:2  (GPU capacity)
```

  Could we make this test more explicit on the difference between BestFit and LeastFreeCapacity, so it's clear BestFit goes from the largest to the smallest node, and LeastFreeCapacity goes the other way around. I know this case shows this but I'd like to make it more explicit for the future maintenance. 
  
  I imagine something like this:
  
  Leader: CPU only
  Workers: As is currently
  
  x1: 4, x2: 2, x3: 2, x:4 :2
  
  Best fit picks: x1, x2
  LeastFreeCapacity picks: x2, x3, x4

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-01-12T13:48:14Z

- **pkg/cache/scheduler/tas_cache_test.go:5294** by [@PBundyra](https://github.com/PBundyra) — 2026-01-12T13:48:14Z
```diff
@@ -5074,6 +5074,275 @@ func TestFindTopologyAssignments(t *testing.T) {
 				},
 			},
 		},
+		// Podset group where workers must spread across multiple domains.
+		// Setup: 3 hosts with different GPU capacities, workers need more than any single host.
+		//        b1
+		//         |
+		//        r1
+		//    /   |    \
+		// x1:4  x2:3  x3:2  (GPU capacity)
+		//
+		// Leader: 1 pod, 1 GPU
+		// Workers: 6 pods, 1 GPU each, slice size=2 (need 3 slices)
+		// Total: 7 GPU needed, no single host fits all workers
+		//
+		// BestFit places leader in largest host (x1), workers spread evenly (x1:2, x2:2, x3:2).
+		"BestFit: podset group workers spread across hosts": {
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1-r1-x1").
+					Label(tasBlockLabel, "b1").
+					Label(tasRackLabel, "r1").
+					Label(corev1.LabelHostname, "x1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("4"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b1-r1-x2").
+					Label(tasBlockLabel, "b1").
+					Label(tasRackLabel, "r1").
+					Label(corev1.LabelHostname, "x2").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("3"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b1-r1-x3").
+					Label(tasBlockLabel, "b1").
+					Label(tasRackLabel, "r1").
+					Label(corev1.LabelHostname, "x3").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("2"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+			},
+			levels: defaultThreeLevels,
+			podSets: []PodSetTestCase{
+				{
+					podSetName: "leader",
+					topologyRequest: &kueue.PodSetTopologyRequest{
+						Preferred: ptr.To(tasBlockLabel),
+					},
+					requests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+						"example.com/gpu":  1,
+					},
+					podSetGroupName: ptr.To("sameGroup"),
+					count:           1,
+					wantAssignment: &tas.TopologyAssignment{
+						Levels: defaultOneLevel,
+						Domains: []tas.TopologyDomainAssignment{
+							{
+								Count:  1,
+								Values: []string{"x1"},
+							},
+						},
+					},
+				},
+				{
+					podSetName: "workers",
+					topologyRequest: &kueue.PodSetTopologyRequest{
+						Preferred:                   ptr.To(tasBlockLabel),
+						PodSetSliceSize:             ptr.To(int32(2)),
+						PodSetSliceRequiredTopology: ptr.To(corev1.LabelHostname),
+					},
+					requests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+						"example.com/gpu":  1,
+					},
+					podSetGroupName: ptr.To("sameGroup"),
+					count:           6,
+					wantAssignment: &tas.TopologyAssignment{
+						Levels: defaultOneLevel,
+						Domains: []tas.TopologyDomainAssignment{
+							{
+								Count:  2,
+								Values: []string{"x1"},
+							},
+							{
+								Count:  2,
+								Values: []string{"x2"},
+							},
+							{
+								Count:  2,
+								Values: []string{"x3"},
+							},
+						},
+					},
+				},
+			},
+		},
+		// Same setup as above with LeastFreeCapacity.
+		// LeastFreeCapacity places leader in smallest host (x3), leaving only 1 GPU there.
+		// Workers go to remaining hosts with capacity (x1:4, x2:2), x3 can't fit a 2-pod slice.
+		"LeastFreeCapacity: podset group workers spread across hosts": {
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1-r1-x1").
+					Label(tasBlockLabel, "b1").
+					Label(tasRackLabel, "r1").
+					Label(corev1.LabelHostname, "x1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("4"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b1-r1-x2").
+					Label(tasBlockLabel, "b1").
+					Label(tasRackLabel, "r1").
+					Label(corev1.LabelHostname, "x2").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("3"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b1-r1-x3").
+					Label(tasBlockLabel, "b1").
+					Label(tasRackLabel, "r1").
+					Label(corev1.LabelHostname, "x3").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("20"),
+						"example.com/gpu":   resource.MustParse("2"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+			},
+			levels: defaultThreeLevels,
+			podSets: []PodSetTestCase{
+				{
+					podSetName: "leader",
+					topologyRequest: &kueue.PodSetTopologyRequest{
+						Preferred: ptr.To(tasBlockLabel),
+					},
+					requests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+						"example.com/gpu":  1,
+					},
+					podSetGroupName: ptr.To("sameGroup"),
+					count:           1,
+					wantAssignment: &tas.TopologyAssignment{
+						Levels: defaultOneLevel,
+						Domains: []tas.TopologyDomainAssignment{
+							{
+								Count:  1,
+								Values: []string{"x3"},
+							},
+						},
+					},
+				},
+				{
+					podSetName: "workers",
+					topologyRequest: &kueue.PodSetTopologyRequest{
+						Preferred:                   ptr.To(tasBlockLabel),
+						PodSetSliceSize:             ptr.To(int32(2)),
+						PodSetSliceRequiredTopology: ptr.To(corev1.LabelHostname),
+					},
+					requests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+						"example.com/gpu":  1,
+					},
+					podSetGroupName: ptr.To("sameGroup"),
+					count:           6,
+					wantAssignment: &tas.TopologyAssignment{
+						Levels: defaultOneLevel,
+						Domains: []tas.TopologyDomainAssignment{
+							{
+								Count:  4,
+								Values: []string{"x1"},
+							},
+							{
+								Count:  2,
+								Values: []string{"x2"},
+							},
+						},
+					},
+				},
+			},
+			enableFeatureGates: []featuregate.Feature{features.TASProfileLeastFreeCapacity},
+		},
+		// LeastFreeCapacity: when slice capacity is equal, the tiebreaker is stateWithLeader (ascending).
+		// b1: 5 GPU -> 2 slices, 5 workers; b2: 4 GPU -> 2 slices, 4 workers
+		// b2 is selected because both have 2 slices, but b2 has less free worker capacity.
```

  👍

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-01-12T16:35:45Z

- **pkg/cache/scheduler/tas_cache_test.go:5083** by [@IrvingMg](https://github.com/IrvingMg) — 2026-01-12T16:35:45Z
```diff
@@ -5074,6 +5074,275 @@ func TestFindTopologyAssignments(t *testing.T) {
 				},
 			},
 		},
+		// Podset group where workers must spread across multiple domains.
+		// Setup: 3 hosts with different GPU capacities, workers need more than any single host.
+		//        b1
+		//         |
+		//        r1
+		//    /   |    \
+		// x1:4  x2:3  x3:2  (GPU capacity)
```

  Done. I've updated the tests.
