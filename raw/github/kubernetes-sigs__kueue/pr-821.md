# PR #821: Ignore reconciling against unmanaged child jobs in the jobframework

**Summary**: Ignore reconciling against unmanaged child jobs in the jobframework

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/821

**Last updated**: 2023-06-07T18:07:49Z

---

## Metadata

- **State**: closed
- **Author**: [@tenzen-y](https://github.com/tenzen-y)
- **Branch**: `framework-reconciler` → `main`
- **Created**: 2023-05-29T21:15:44Z
- **Updated**: 2023-06-07T18:07:49Z
- **Merged**: —
- **Closed**: 2023-06-07T18:01:05Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `approved`, `needs-rebase`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 19   **+311 / -80**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind bug

#### What this PR does / why we need it:
I fixed a bug that reconciles against unmanaged child batch/job.

#### Which issue(s) this PR fixes:
Fixes #800

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Fix a bug that reconciles against unmanaged child batch/job.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-05-29T21:15:50Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 658457ee6164315d232bebc6d3368fe19ff089a2 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/647f99c868a7df000824aebe |

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-29T21:18:06Z

/assign @alculquicondor

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-31T06:20:06Z

Maybe, the capacity of memory is insufficient :(

```shell
...
go fmt ./...
go vet ./...
golang.org/x/net/http2: /usr/local/go/pkg/tool/linux_amd64/compile: signal: killed
github.com/prometheus/procfs: /usr/local/go/pkg/tool/linux_amd64/compile: signal: killed
make: *** [Makefile:144: vet] Error 1
```

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-31T11:46:47Z

/retest

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-01T16:59:16Z

Rebased to resolve conflicts.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-01T17:02:35Z

@alculquicondor I've addressed your suggestions. Can you please take another look?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-02T21:12:47Z

@alculquicondor I addressed your comments. PTAL

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-06T20:19:59Z

@alculquicondor I'm sorry I didn't immediately understand your suggestion correctly. 
I updated this PR. Can you re-check this?

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-06-06T20:22:57Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/821#pullrequestreview-1466015651" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/821#" title="Author self-approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [alculquicondor]
- ~~[pkg/util/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/util/OWNERS)~~ [alculquicondor,tenzen-y]
- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [alculquicondor,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-06T20:42:32Z

@alculquicondor I fixed all nits and squashed all commit into one.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-07T12:00:32Z

Structured logging is typically output as JSON, so `camelCase` is preferred.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-07T16:29:38Z

> Structured logging is typically output as JSON, so `camelCase` is preferred.

I see. That makes sense.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-06-07T16:29:46Z

PR needs rebase.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-07T17:50:20Z

I have rebased and addressed all suggestions.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-07T17:52:20Z

GitHub is under disruption...
https://www.githubstatus.com/

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-07T17:57:31Z

rebase needed :sweat_smile:

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-07T18:00:04Z

> rebase needed 😅

Actually, I rebased and pushed the commit... But due to GitHub disruption, this PR isn'n updated...

```shell
$ git log --one-line
da57d98 (HEAD -> framework-reconciler, origin/framework-reconciler) Use camelCase for logs
4017e3e Ignore reconciling against unmanaged child jobs in the jobframework
97639b9 (origin/main, origin/HEAD) Merge pull request #831 from epam/kustomize-deprecated-fields
68825ad Merge pull request #833 from cpanato/migrate
7444b27 Partial admission (#771)
5f02e23 Migrate away from google.com gcp project k8s-testimages
```

https://github.com/tenzen-y/kueue/commit/da57d986a32dee286c76a4ffb6ae07ba03aeeb1a

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-07T18:01:17Z

/reopen

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-06-07T18:01:22Z

@tenzen-y: Failed to re-open PR: state cannot be changed. The framework-reconciler branch was force-pushed or recreated.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/821#issuecomment-1581277047):

>/reopen


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-07T18:02:56Z

This PR was broken :(

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-07T18:07:44Z

/reopen

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-06-07T18:07:49Z

@tenzen-y: Failed to re-open PR: state cannot be changed. The framework-reconciler branch was force-pushed or recreated.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/821#issuecomment-1581285858):

>/reopen


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-30T18:41:29Z

- **pkg/controller/jobframework/reconciler.go:251** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-30T18:14:09Z
```diff
@@ -236,6 +248,21 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	return ctrl.Result{}, nil
 }
 
+func (r *JobReconciler) ensureParentJob(ctx context.Context, job GenericJob, namespace string) (client.Object, error) {
```

  what are we ensuring? Either change to `ensureParentJobExists` or (my preference) `isParentJobManaged`, calling `QueueName` inside.

- **pkg/controller/jobframework/interface.go:61** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-30T18:37:27Z
```diff
@@ -58,10 +58,14 @@ func ParentWorkloadName(job GenericJob) string {
 	return job.Object().GetAnnotations()[ParentWorkloadAnnotation]
 }
 
-func QueueName(job GenericJob) string {
-	if queueLabel := job.Object().GetLabels()[QueueLabel]; queueLabel != "" {
+func QueueNameForGenericJob(job GenericJob) string {
```

  Since most of the functions in this package operate on GenericJob, I would instead let the existing function keep its name, and add QueueNameForObject.
  
  It will also reduce the diff size.

- **test/integration/controller/job/job_controller_test.go:500** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-30T18:40:17Z
```diff
@@ -480,6 +496,67 @@ var _ = ginkgo.Describe("Job controller for workloads with no queue set", func()
 			}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
 		})
 
+		ginkgo.It("Should not suspend a child job if the parent job doesn't have a queue name", func() {
+			ginkgo.By("Crating the child job without ownerReference which uses the parent workload annotation")
```

  ```suggestion
  			ginkgo.By("Creating the child job without ownerReference which uses the parent workload annotation")
  ```

- **test/integration/controller/job/job_controller_test.go:42** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-30T18:41:25Z
```diff
@@ -38,6 +39,7 @@ import (
 	"sigs.k8s.io/kueue/pkg/util/pointer"
 	"sigs.k8s.io/kueue/pkg/util/testing"
 	testingjob "sigs.k8s.io/kueue/pkg/util/testingjobs/job"
+	testingmpijob "sigs.k8s.io/kueue/pkg/util/testingjobs/mpijob"
```

  We should have these tests in the mpijob package instead

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-31T02:59:50Z

- **pkg/controller/jobframework/reconciler.go:251** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-31T02:59:50Z
```diff
@@ -236,6 +248,21 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	return ctrl.Result{}, nil
 }
 
+func (r *JobReconciler) ensureParentJob(ctx context.Context, job GenericJob, namespace string) (client.Object, error) {
```

  `isParentJobManaged ` is sounds good.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-31T03:00:26Z

- **pkg/controller/jobframework/interface.go:61** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-31T03:00:25Z
```diff
@@ -58,10 +58,14 @@ func ParentWorkloadName(job GenericJob) string {
 	return job.Object().GetAnnotations()[ParentWorkloadAnnotation]
 }
 
-func QueueName(job GenericJob) string {
-	if queueLabel := job.Object().GetLabels()[QueueLabel]; queueLabel != "" {
+func QueueNameForGenericJob(job GenericJob) string {
```

  That makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-31T03:00:36Z

- **test/integration/controller/job/job_controller_test.go:500** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-31T03:00:36Z
```diff
@@ -480,6 +496,67 @@ var _ = ginkgo.Describe("Job controller for workloads with no queue set", func()
 			}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
 		})
 
+		ginkgo.It("Should not suspend a child job if the parent job doesn't have a queue name", func() {
+			ginkgo.By("Crating the child job without ownerReference which uses the parent workload annotation")
```

  Thanks!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-31T03:14:11Z

- **pkg/controller/jobframework/interface.go:61** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-31T03:14:11Z
```diff
@@ -58,10 +58,14 @@ func ParentWorkloadName(job GenericJob) string {
 	return job.Object().GetAnnotations()[ParentWorkloadAnnotation]
 }
 
-func QueueName(job GenericJob) string {
-	if queueLabel := job.Object().GetLabels()[QueueLabel]; queueLabel != "" {
+func QueueNameForGenericJob(job GenericJob) string {
```

  done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-31T03:21:54Z

- **pkg/controller/jobframework/reconciler.go:251** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-31T03:21:53Z
```diff
@@ -236,6 +248,21 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	return ctrl.Result{}, nil
 }
 
+func (r *JobReconciler) ensureParentJob(ctx context.Context, job GenericJob, namespace string) (client.Object, error) {
```

  done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-31T03:28:17Z

- **test/integration/controller/job/job_controller_test.go:42** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-31T03:28:17Z
```diff
@@ -38,6 +39,7 @@ import (
 	"sigs.k8s.io/kueue/pkg/util/pointer"
 	"sigs.k8s.io/kueue/pkg/util/testing"
 	testingjob "sigs.k8s.io/kueue/pkg/util/testingjobs/job"
+	testingmpijob "sigs.k8s.io/kueue/pkg/util/testingjobs/mpijob"
```

  I put these tests on the job package since we haven't yet ways to start both mpijob-controller and job-controller in the integration test.
  
  But, we can put these tests on the mpijob package by expanding `managerSetup`.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-31T03:28:44Z

- **test/integration/controller/job/job_controller_test.go:42** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-31T03:28:43Z
```diff
@@ -38,6 +39,7 @@ import (
 	"sigs.k8s.io/kueue/pkg/util/pointer"
 	"sigs.k8s.io/kueue/pkg/util/testing"
 	testingjob "sigs.k8s.io/kueue/pkg/util/testingjobs/job"
+	testingmpijob "sigs.k8s.io/kueue/pkg/util/testingjobs/mpijob"
```

  Anyway, I will do that in this PR.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-31T05:16:17Z

- **test/integration/controller/job/job_controller_test.go:499** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-31T05:16:17Z
```diff
@@ -464,100 +461,6 @@ var _ = ginkgo.Describe("Job controller for workloads with no queue set", func()
 			return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
 		}, util.Timeout, util.Interval).Should(gomega.Succeed())
 	})
-	ginkgo.When("The parent-workload annotation is used", func() {
```

  @alculquicondor We can not run integration test cases related to parent-workload annotation since we must create a known workload owner (currently, only mpijobs) as a parent job.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-31T05:16:31Z

- **test/integration/controller/job/job_controller_test.go:42** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-31T05:16:31Z
```diff
@@ -38,6 +39,7 @@ import (
 	"sigs.k8s.io/kueue/pkg/util/pointer"
 	"sigs.k8s.io/kueue/pkg/util/testing"
 	testingjob "sigs.k8s.io/kueue/pkg/util/testingjobs/job"
+	testingmpijob "sigs.k8s.io/kueue/pkg/util/testingjobs/mpijob"
```

  done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-05-31T12:12:59Z

- **pkg/controller/jobframework/reconciler.go:123** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-31T12:04:47Z
```diff
@@ -109,9 +109,21 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && isStandaloneJob {
-		log.V(3).Info(fmt.Sprintf("Neither %s label, nor %s annotation is set, ignoring the job", QueueLabel, ParentWorkloadAnnotation))
-		return ctrl.Result{}, nil
+	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+		if isStandaloneJob {
+			log.V(3).Info(fmt.Sprintf("Neither %s label, nor %s annotation is set, ignoring the job", QueueLabel, ParentWorkloadAnnotation))
+			return ctrl.Result{}, nil
+		}
+		isParentJobManaged, err := r.isParentJobManaged(ctx, job, namespacedName.Namespace)
+		if err != nil {
+			log.Error(err, "couldn't check whether the parent job is managed by kueue")
+			return ctrl.Result{}, err
+		}
+		if isParentJobManaged {
```

  ```suggestion
  		if !isParentJobManaged {
  ```

- **pkg/controller/jobframework/reconciler.go:264** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-31T12:04:59Z
```diff
@@ -236,6 +248,22 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	return ctrl.Result{}, nil
 }
 
+// isParentJobManaged checks whether the parent job is managed by kueue.
+func (r *JobReconciler) isParentJobManaged(ctx context.Context, job GenericJob, namespace string) (bool, error) {
+	owner := metav1.GetControllerOf(job.Object())
+	if owner == nil {
+		return false, fmt.Errorf("owner isn't set even though %s annotation is set", ParentWorkloadAnnotation)
+	}
+	parentJob := KnownWorkloadOwnerObject(owner)
+	if parentJob == nil {
+		return false, fmt.Errorf("workload owner %v is unknown", owner)
+	}
+	if err := r.client.Get(ctx, client.ObjectKey{Name: owner.Name, Namespace: namespace}, parentJob); err != nil {
+		return false, err
+	}
+	return QueueNameForObject(parentJob) == "", nil
```

  ```suggestion
  	return QueueNameForObject(parentJob) != "", nil
  ```

- **test/integration/controller/mpijob/mpijob_controller_test.go:337** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-31T12:07:58Z
```diff
@@ -302,6 +309,91 @@ var _ = ginkgo.Describe("Job controller for workloads when only jobs with queue
 		}, util.Timeout, util.Interval).Should(gomega.Succeed())
 	})
 
+	ginkgo.It("Should suspend a job if the parent workload does not exist", func() {
+		ginkgo.By("Creating the parent job which has a queue name")
+		parentJob := testingmpijob.MakeMPIJob(parentJobName, jobNamespace).
+			UID(parentJobName).
+			Queue("test").
+			Suspend(false).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, parentJob)).Should(gomega.Succeed())
+
+		ginkgo.By("Creating the child job which uses the parent workload annotation")
+		childJob := testingjob.MakeJob(childJobName, jobNamespace).
+			OwnerReference(parentJobName, kubeflow.SchemeGroupVersionKind).
+			Suspend(false).
+			ParentWorkload("non-existing-parent-workload").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, childJob)).Should(gomega.Succeed())
+
+		ginkgo.By("checking that the child job is suspended")
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, childLookupKey, childJob)).Should(gomega.Succeed())
+			return childJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+	})
+
+	ginkgo.It("Should not suspend a child job if the parent job doesn't have a queue name", func() {
+		ginkgo.By("Creating the child job without ownerReference which uses the parent workload annotation")
```

  :thinking: maybe we should not allow a job to have the parent workload annotation if it doesn't have an owner reference?

- **test/integration/controller/mpijob/mpijob_controller_test.go:367** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-31T12:11:58Z
```diff
@@ -302,6 +309,91 @@ var _ = ginkgo.Describe("Job controller for workloads when only jobs with queue
 		}, util.Timeout, util.Interval).Should(gomega.Succeed())
 	})
 
+	ginkgo.It("Should suspend a job if the parent workload does not exist", func() {
+		ginkgo.By("Creating the parent job which has a queue name")
+		parentJob := testingmpijob.MakeMPIJob(parentJobName, jobNamespace).
+			UID(parentJobName).
+			Queue("test").
+			Suspend(false).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, parentJob)).Should(gomega.Succeed())
+
+		ginkgo.By("Creating the child job which uses the parent workload annotation")
+		childJob := testingjob.MakeJob(childJobName, jobNamespace).
+			OwnerReference(parentJobName, kubeflow.SchemeGroupVersionKind).
+			Suspend(false).
+			ParentWorkload("non-existing-parent-workload").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, childJob)).Should(gomega.Succeed())
+
+		ginkgo.By("checking that the child job is suspended")
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, childLookupKey, childJob)).Should(gomega.Succeed())
+			return childJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+	})
+
+	ginkgo.It("Should not suspend a child job if the parent job doesn't have a queue name", func() {
```

  It looks like you are mixing multiple cases here:
  - doesn't have an owner
  - unknown owner
  - MPIJob without queue name
  
  For the purpose of this integration test (in the mpi package) we should only have one test for the last case.
  The rest can be in unit tests.

- **test/integration/controller/mpijob/suite_test.go:75** by [@alculquicondor](https://github.com/alculquicondor) — 2023-05-31T12:12:55Z
```diff
@@ -70,6 +71,20 @@ func managerSetup(opts ...jobframework.Option) framework.ManagerSetup {
 		gomega.Expect(err).NotTo(gomega.HaveOccurred())
 		err = mpijob.SetupMPIJobWebhook(mgr, opts...)
 		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		if setupJobController {
```

  Can you also setup the webhook?
  I want to make sure the webhook is not setting the suspend field either.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-31T13:03:38Z

- **test/integration/controller/mpijob/suite_test.go:75** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-31T13:03:38Z
```diff
@@ -70,6 +71,20 @@ func managerSetup(opts ...jobframework.Option) framework.ManagerSetup {
 		gomega.Expect(err).NotTo(gomega.HaveOccurred())
 		err = mpijob.SetupMPIJobWebhook(mgr, opts...)
 		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		if setupJobController {
```

  ~~Sure.~~

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-31T13:06:15Z

- **test/integration/controller/mpijob/suite_test.go:75** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-31T13:06:15Z
```diff
@@ -70,6 +71,20 @@ func managerSetup(opts ...jobframework.Option) framework.ManagerSetup {
 		gomega.Expect(err).NotTo(gomega.HaveOccurred())
 		err = mpijob.SetupMPIJobWebhook(mgr, opts...)
 		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		if setupJobController {
```

  I already set up a webhook at Line 85.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-31T13:10:49Z

- **test/integration/controller/mpijob/mpijob_controller_test.go:367** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-31T13:10:48Z
```diff
@@ -302,6 +309,91 @@ var _ = ginkgo.Describe("Job controller for workloads when only jobs with queue
 		}, util.Timeout, util.Interval).Should(gomega.Succeed())
 	})
 
+	ginkgo.It("Should suspend a job if the parent workload does not exist", func() {
+		ginkgo.By("Creating the parent job which has a queue name")
+		parentJob := testingmpijob.MakeMPIJob(parentJobName, jobNamespace).
+			UID(parentJobName).
+			Queue("test").
+			Suspend(false).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, parentJob)).Should(gomega.Succeed())
+
+		ginkgo.By("Creating the child job which uses the parent workload annotation")
+		childJob := testingjob.MakeJob(childJobName, jobNamespace).
+			OwnerReference(parentJobName, kubeflow.SchemeGroupVersionKind).
+			Suspend(false).
+			ParentWorkload("non-existing-parent-workload").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, childJob)).Should(gomega.Succeed())
+
+		ginkgo.By("checking that the child job is suspended")
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, childLookupKey, childJob)).Should(gomega.Succeed())
+			return childJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+	})
+
+	ginkgo.It("Should not suspend a child job if the parent job doesn't have a queue name", func() {
```

  > For the purpose of this integration test (in the mpi package) we should only have one test for the last case.
  The rest can be in unit tests.
  
  That makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-31T13:15:46Z

- **test/integration/controller/mpijob/mpijob_controller_test.go:337** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-31T13:15:46Z
```diff
@@ -302,6 +309,91 @@ var _ = ginkgo.Describe("Job controller for workloads when only jobs with queue
 		}, util.Timeout, util.Interval).Should(gomega.Succeed())
 	})
 
+	ginkgo.It("Should suspend a job if the parent workload does not exist", func() {
+		ginkgo.By("Creating the parent job which has a queue name")
+		parentJob := testingmpijob.MakeMPIJob(parentJobName, jobNamespace).
+			UID(parentJobName).
+			Queue("test").
+			Suspend(false).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, parentJob)).Should(gomega.Succeed())
+
+		ginkgo.By("Creating the child job which uses the parent workload annotation")
+		childJob := testingjob.MakeJob(childJobName, jobNamespace).
+			OwnerReference(parentJobName, kubeflow.SchemeGroupVersionKind).
+			Suspend(false).
+			ParentWorkload("non-existing-parent-workload").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, childJob)).Should(gomega.Succeed())
+
+		ginkgo.By("checking that the child job is suspended")
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, childLookupKey, childJob)).Should(gomega.Succeed())
+			return childJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+	})
+
+	ginkgo.It("Should not suspend a child job if the parent job doesn't have a queue name", func() {
+		ginkgo.By("Creating the child job without ownerReference which uses the parent workload annotation")
```

  That makes sense. Actually, we verify the ownerReference at a defaulting webhook: 
  
  https://github.com/kubernetes-sigs/kueue/blob/f694062432ac3c9284d83fd603ba0431887b1c19/pkg/controller/jobs/job/job_webhook.go#L63
  
  However, we don't have validation webhooks for that.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-31T13:31:35Z

- **test/integration/controller/mpijob/mpijob_controller_test.go:337** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-31T13:31:35Z
```diff
@@ -302,6 +309,91 @@ var _ = ginkgo.Describe("Job controller for workloads when only jobs with queue
 		}, util.Timeout, util.Interval).Should(gomega.Succeed())
 	})
 
+	ginkgo.It("Should suspend a job if the parent workload does not exist", func() {
+		ginkgo.By("Creating the parent job which has a queue name")
+		parentJob := testingmpijob.MakeMPIJob(parentJobName, jobNamespace).
+			UID(parentJobName).
+			Queue("test").
+			Suspend(false).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, parentJob)).Should(gomega.Succeed())
+
+		ginkgo.By("Creating the child job which uses the parent workload annotation")
+		childJob := testingjob.MakeJob(childJobName, jobNamespace).
+			OwnerReference(parentJobName, kubeflow.SchemeGroupVersionKind).
+			Suspend(false).
+			ParentWorkload("non-existing-parent-workload").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, childJob)).Should(gomega.Succeed())
+
+		ginkgo.By("checking that the child job is suspended")
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, childLookupKey, childJob)).Should(gomega.Succeed())
+			return childJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+	})
+
+	ginkgo.It("Should not suspend a child job if the parent job doesn't have a queue name", func() {
+		ginkgo.By("Creating the child job without ownerReference which uses the parent workload annotation")
```

  I'm thinking of adding a validation at a follow-up PR 🤔 
  
  Either way, I will move this case to a unit test.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-05-31T13:46:49Z

- **test/integration/controller/mpijob/suite_test.go:75** by [@tenzen-y](https://github.com/tenzen-y) — 2023-05-31T13:46:49Z
```diff
@@ -70,6 +71,20 @@ func managerSetup(opts ...jobframework.Option) framework.ManagerSetup {
 		gomega.Expect(err).NotTo(gomega.HaveOccurred())
 		err = mpijob.SetupMPIJobWebhook(mgr, opts...)
 		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		if setupJobController {
```

  Maybe, I should replace `setupJobController` with `setupJobManager`.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-01T14:19:26Z

- **test/integration/controller/mpijob/suite_test.go:75** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-01T14:19:25Z
```diff
@@ -70,6 +71,20 @@ func managerSetup(opts ...jobframework.Option) framework.ManagerSetup {
 		gomega.Expect(err).NotTo(gomega.HaveOccurred())
 		err = mpijob.SetupMPIJobWebhook(mgr, opts...)
 		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		if setupJobController {
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-01T16:38:45Z

- **pkg/controller/constants/constants.go:17** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-01T16:38:45Z
```diff
@@ -14,7 +14,7 @@ See the License for the specific language governing permissions and
 limitations under the License.
 */
 
-package jobframework
+package constants
```

  Because of avoiding the import cycle.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-01T16:39:02Z

- **test/integration/controller/mpijob/mpijob_controller_test.go:367** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-01T16:39:02Z
```diff
@@ -302,6 +309,91 @@ var _ = ginkgo.Describe("Job controller for workloads when only jobs with queue
 		}, util.Timeout, util.Interval).Should(gomega.Succeed())
 	})
 
+	ginkgo.It("Should suspend a job if the parent workload does not exist", func() {
+		ginkgo.By("Creating the parent job which has a queue name")
+		parentJob := testingmpijob.MakeMPIJob(parentJobName, jobNamespace).
+			UID(parentJobName).
+			Queue("test").
+			Suspend(false).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, parentJob)).Should(gomega.Succeed())
+
+		ginkgo.By("Creating the child job which uses the parent workload annotation")
+		childJob := testingjob.MakeJob(childJobName, jobNamespace).
+			OwnerReference(parentJobName, kubeflow.SchemeGroupVersionKind).
+			Suspend(false).
+			ParentWorkload("non-existing-parent-workload").
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, childJob)).Should(gomega.Succeed())
+
+		ginkgo.By("checking that the child job is suspended")
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, childLookupKey, childJob)).Should(gomega.Succeed())
+			return childJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+	})
+
+	ginkgo.It("Should not suspend a child job if the parent job doesn't have a queue name", func() {
```

  done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-01T16:58:48Z

- **pkg/controller/jobframework/reconciler.go:264** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-01T16:58:48Z
```diff
@@ -236,6 +248,22 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	return ctrl.Result{}, nil
 }
 
+// isParentJobManaged checks whether the parent job is managed by kueue.
+func (r *JobReconciler) isParentJobManaged(ctx context.Context, job GenericJob, namespace string) (bool, error) {
+	owner := metav1.GetControllerOf(job.Object())
+	if owner == nil {
+		return false, fmt.Errorf("owner isn't set even though %s annotation is set", ParentWorkloadAnnotation)
+	}
+	parentJob := KnownWorkloadOwnerObject(owner)
+	if parentJob == nil {
+		return false, fmt.Errorf("workload owner %v is unknown", owner)
+	}
+	if err := r.client.Get(ctx, client.ObjectKey{Name: owner.Name, Namespace: namespace}, parentJob); err != nil {
+		return false, err
+	}
+	return QueueNameForObject(parentJob) == "", nil
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-01T16:58:52Z

- **pkg/controller/jobframework/reconciler.go:123** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-01T16:58:52Z
```diff
@@ -109,9 +109,21 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && isStandaloneJob {
-		log.V(3).Info(fmt.Sprintf("Neither %s label, nor %s annotation is set, ignoring the job", QueueLabel, ParentWorkloadAnnotation))
-		return ctrl.Result{}, nil
+	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+		if isStandaloneJob {
+			log.V(3).Info(fmt.Sprintf("Neither %s label, nor %s annotation is set, ignoring the job", QueueLabel, ParentWorkloadAnnotation))
+			return ctrl.Result{}, nil
+		}
+		isParentJobManaged, err := r.isParentJobManaged(ctx, job, namespacedName.Namespace)
+		if err != nil {
+			log.Error(err, "couldn't check whether the parent job is managed by kueue")
+			return ctrl.Result{}, err
+		}
+		if isParentJobManaged {
```

  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-06-01T20:33:13Z

- **pkg/cache/cache_test.go:1296** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-01T20:24:02Z
```diff
@@ -1293,7 +1293,7 @@ func TestCacheWorkloadOperations(t *testing.T) {
 			}
 
 			gotError := step.operation(cache)
-			if diff := cmp.Diff(step.wantError, messageOrEmpty(gotError)); diff != "" {
+			if diff := cmp.Diff(step.wantError, utiltesting.MessageOrEmpty(gotError)); diff != "" {
```

  This escaped my review lens in the past :monocle_face: 
  
  I'm not really a fan of checking errors by comparing strings. We should do the effort of implementing `Is` or exporting the relevant errors in the packages, so that we `cmp.Diff` can handle the rest.
  
  But it can be left for a follow up.

- **pkg/controller/jobframework/reconciler_test.go:74** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-01T20:25:43Z
```diff
@@ -0,0 +1,112 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"fmt"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	batchv1 "k8s.io/api/batch/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	testingjob "sigs.k8s.io/kueue/pkg/util/testingjobs/job"
+	testingmpijob "sigs.k8s.io/kueue/pkg/util/testingjobs/mpijob"
+)
+
+func TestIsParentJobManaged(t *testing.T) {
+	parentJobName := "test-job-parent"
+	childJobName := "test-job-child"
+	jobNamespace := "default"
+	cases := map[string]struct {
+		parentJob client.Object
+		job       client.Object
+		want      bool
+		wantErr   string
+	}{
+		"child job doesn't have ownerReference": {
+			parentJob: testingmpijob.MakeMPIJob(parentJobName, jobNamespace).
+				UID(parentJobName).
+				Obj(),
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				Obj(),
+			wantErr: "owner isn't set even though \"kueue.x-k8s.io/parent-workload\" annotation is set",
+		},
+		"child job has ownerReference with unknown workload owner": {
+			parentJob: testingjob.MakeJob(parentJobName, jobNamespace).
+				UID(parentJobName).
+				Obj(),
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				OwnerReference(parentJobName, batchv1.SchemeGroupVersion.WithKind("Job")).
+				Obj(),
+			wantErr: fmt.Sprintf("workload owner %v is unknown", &metav1.OwnerReference{
+				APIVersion: batchv1.SchemeGroupVersion.String(),
+				Kind:       "Job",
+				Name:       parentJobName,
+				UID:        types.UID(parentJobName),
+				Controller: pointer.Bool(true),
+			}),
+		},
+		"child job has ownerReference with known non-existing workload owner": {
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				OwnerReference(parentJobName, kubeflow.SchemeGroupVersionKind).
+				Obj(),
+			wantErr: "mpijobs.kubeflow.org \"test-job-parent\" not found",
```

  are these errors typed? Do they implement `Is`? If so, we should prefer comparing actual errors than strings.

- **pkg/controller/jobframework/reconciler.go:116** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-01T20:28:16Z
```diff
@@ -109,9 +110,21 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && isStandaloneJob {
-		log.V(3).Info(fmt.Sprintf("Neither %s label, nor %s annotation is set, ignoring the job", QueueLabel, ParentWorkloadAnnotation))
-		return ctrl.Result{}, nil
+	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+		if isStandaloneJob {
+			log.V(3).Info(fmt.Sprintf("Neither %s label, nor %s annotation is set, ignoring the job", controllerconsts.QueueLabel, controllerconsts.ParentWorkloadAnnotation))
```

  Do not use `Sprintf` for logging. Instead, use a constant message and add keys and values, so we get structured logging.

- **pkg/controller/jobframework/reconciler.go:125** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-01T20:28:23Z
```diff
@@ -109,9 +110,21 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && isStandaloneJob {
-		log.V(3).Info(fmt.Sprintf("Neither %s label, nor %s annotation is set, ignoring the job", QueueLabel, ParentWorkloadAnnotation))
-		return ctrl.Result{}, nil
+	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+		if isStandaloneJob {
+			log.V(3).Info(fmt.Sprintf("Neither %s label, nor %s annotation is set, ignoring the job", controllerconsts.QueueLabel, controllerconsts.ParentWorkloadAnnotation))
+			return ctrl.Result{}, nil
+		}
+		isParentJobManaged, err := r.isParentJobManaged(ctx, job.Object(), namespacedName.Namespace)
+		if err != nil {
+			log.Error(err, "couldn't check whether the parent job is managed by kueue")
+			return ctrl.Result{}, err
+		}
+		if !isParentJobManaged {
+			log.V(3).Info(fmt.Sprintf("%s annotation is set, and the parent job doesn't have a %s label, ignoring the job", controllerconsts.ParentWorkloadAnnotation, controllerconsts.QueueLabel))
```

  same here.

- **pkg/util/testing/units.go:25** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-01T20:32:05Z
```diff
@@ -21,3 +21,10 @@ const (
 	Mi = Ki * Ki
 	Gi = Ki * Mi
 )
+
+func MessageOrEmpty(err error) string {
```

  even if it's too much effort to get rid of string comparisons, I prefer we don't put this in the utils, otherwise people will keep using it instead of doing proper error comparisons.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-01T20:48:31Z

- **pkg/util/testing/units.go:25** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-01T20:48:31Z
```diff
@@ -21,3 +21,10 @@ const (
 	Mi = Ki * Ki
 	Gi = Ki * Mi
 )
+
+func MessageOrEmpty(err error) string {
```

  That makes sense.
  I will remove it from utils.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-01T20:48:59Z

- **pkg/controller/jobframework/reconciler.go:116** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-01T20:48:59Z
```diff
@@ -109,9 +110,21 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && isStandaloneJob {
-		log.V(3).Info(fmt.Sprintf("Neither %s label, nor %s annotation is set, ignoring the job", QueueLabel, ParentWorkloadAnnotation))
-		return ctrl.Result{}, nil
+	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+		if isStandaloneJob {
+			log.V(3).Info(fmt.Sprintf("Neither %s label, nor %s annotation is set, ignoring the job", controllerconsts.QueueLabel, controllerconsts.ParentWorkloadAnnotation))
```

  It makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-01T20:53:58Z

- **pkg/cache/cache_test.go:1296** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-01T20:53:58Z
```diff
@@ -1293,7 +1293,7 @@ func TestCacheWorkloadOperations(t *testing.T) {
 			}
 
 			gotError := step.operation(cache)
-			if diff := cmp.Diff(step.wantError, messageOrEmpty(gotError)); diff != "" {
+			if diff := cmp.Diff(step.wantError, utiltesting.MessageOrEmpty(gotError)); diff != "" {
```

  I see. I will work on cache pkg at follow-up PRs.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-01T21:00:35Z

- **pkg/controller/jobframework/reconciler_test.go:74** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-01T21:00:35Z
```diff
@@ -0,0 +1,112 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"fmt"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	batchv1 "k8s.io/api/batch/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	testingjob "sigs.k8s.io/kueue/pkg/util/testingjobs/job"
+	testingmpijob "sigs.k8s.io/kueue/pkg/util/testingjobs/mpijob"
+)
+
+func TestIsParentJobManaged(t *testing.T) {
+	parentJobName := "test-job-parent"
+	childJobName := "test-job-child"
+	jobNamespace := "default"
+	cases := map[string]struct {
+		parentJob client.Object
+		job       client.Object
+		want      bool
+		wantErr   string
+	}{
+		"child job doesn't have ownerReference": {
+			parentJob: testingmpijob.MakeMPIJob(parentJobName, jobNamespace).
+				UID(parentJobName).
+				Obj(),
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				Obj(),
+			wantErr: "owner isn't set even though \"kueue.x-k8s.io/parent-workload\" annotation is set",
+		},
+		"child job has ownerReference with unknown workload owner": {
+			parentJob: testingjob.MakeJob(parentJobName, jobNamespace).
+				UID(parentJobName).
+				Obj(),
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				OwnerReference(parentJobName, batchv1.SchemeGroupVersion.WithKind("Job")).
+				Obj(),
+			wantErr: fmt.Sprintf("workload owner %v is unknown", &metav1.OwnerReference{
+				APIVersion: batchv1.SchemeGroupVersion.String(),
+				Kind:       "Job",
+				Name:       parentJobName,
+				UID:        types.UID(parentJobName),
+				Controller: pointer.Bool(true),
+			}),
+		},
+		"child job has ownerReference with known non-existing workload owner": {
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				OwnerReference(parentJobName, kubeflow.SchemeGroupVersionKind).
+				Obj(),
+			wantErr: "mpijobs.kubeflow.org \"test-job-parent\" not found",
```

  > are these errors typed?
  
  No, that isn't errors typed. This error will return from https://github.com/kubernetes-sigs/kueue/blob/c44773e711779a6aaec49011a0f249b4fc61a49c/pkg/controller/jobframework/reconciler.go#L262C1-L263.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-01T21:04:08Z

- **pkg/controller/jobframework/reconciler.go:116** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-01T21:04:08Z
```diff
@@ -109,9 +110,21 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && isStandaloneJob {
-		log.V(3).Info(fmt.Sprintf("Neither %s label, nor %s annotation is set, ignoring the job", QueueLabel, ParentWorkloadAnnotation))
-		return ctrl.Result{}, nil
+	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+		if isStandaloneJob {
+			log.V(3).Info(fmt.Sprintf("Neither %s label, nor %s annotation is set, ignoring the job", controllerconsts.QueueLabel, controllerconsts.ParentWorkloadAnnotation))
```

  Actually, the log is copied from the main branch.
  
   https://github.com/kubernetes-sigs/kueue/blob/196db98f93f4f6c5f8c68be134b53fcdf101ee06/pkg/controller/jobframework/reconciler.go#L113.
  
  Anyway, we should fix that.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-01T21:10:07Z

- **pkg/controller/jobframework/reconciler.go:116** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-01T21:10:07Z
```diff
@@ -109,9 +110,21 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && isStandaloneJob {
-		log.V(3).Info(fmt.Sprintf("Neither %s label, nor %s annotation is set, ignoring the job", QueueLabel, ParentWorkloadAnnotation))
-		return ctrl.Result{}, nil
+	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+		if isStandaloneJob {
+			log.V(3).Info(fmt.Sprintf("Neither %s label, nor %s annotation is set, ignoring the job", controllerconsts.QueueLabel, controllerconsts.ParentWorkloadAnnotation))
```

  done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-01T21:10:15Z

- **pkg/controller/jobframework/reconciler.go:125** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-01T21:10:15Z
```diff
@@ -109,9 +110,21 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && isStandaloneJob {
-		log.V(3).Info(fmt.Sprintf("Neither %s label, nor %s annotation is set, ignoring the job", QueueLabel, ParentWorkloadAnnotation))
-		return ctrl.Result{}, nil
+	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+		if isStandaloneJob {
+			log.V(3).Info(fmt.Sprintf("Neither %s label, nor %s annotation is set, ignoring the job", controllerconsts.QueueLabel, controllerconsts.ParentWorkloadAnnotation))
+			return ctrl.Result{}, nil
+		}
+		isParentJobManaged, err := r.isParentJobManaged(ctx, job.Object(), namespacedName.Namespace)
+		if err != nil {
+			log.Error(err, "couldn't check whether the parent job is managed by kueue")
+			return ctrl.Result{}, err
+		}
+		if !isParentJobManaged {
+			log.V(3).Info(fmt.Sprintf("%s annotation is set, and the parent job doesn't have a %s label, ignoring the job", controllerconsts.ParentWorkloadAnnotation, controllerconsts.QueueLabel))
```

  done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-02T21:11:59Z

- **pkg/util/testing/units.go:25** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-02T21:11:59Z
```diff
@@ -21,3 +21,10 @@ const (
 	Mi = Ki * Ki
 	Gi = Ki * Mi
 )
+
+func MessageOrEmpty(err error) string {
```

  done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-02T21:12:05Z

- **pkg/controller/jobframework/reconciler_test.go:74** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-02T21:12:05Z
```diff
@@ -0,0 +1,112 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"fmt"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	batchv1 "k8s.io/api/batch/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	testingjob "sigs.k8s.io/kueue/pkg/util/testingjobs/job"
+	testingmpijob "sigs.k8s.io/kueue/pkg/util/testingjobs/mpijob"
+)
+
+func TestIsParentJobManaged(t *testing.T) {
+	parentJobName := "test-job-parent"
+	childJobName := "test-job-child"
+	jobNamespace := "default"
+	cases := map[string]struct {
+		parentJob client.Object
+		job       client.Object
+		want      bool
+		wantErr   string
+	}{
+		"child job doesn't have ownerReference": {
+			parentJob: testingmpijob.MakeMPIJob(parentJobName, jobNamespace).
+				UID(parentJobName).
+				Obj(),
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				Obj(),
+			wantErr: "owner isn't set even though \"kueue.x-k8s.io/parent-workload\" annotation is set",
+		},
+		"child job has ownerReference with unknown workload owner": {
+			parentJob: testingjob.MakeJob(parentJobName, jobNamespace).
+				UID(parentJobName).
+				Obj(),
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				OwnerReference(parentJobName, batchv1.SchemeGroupVersion.WithKind("Job")).
+				Obj(),
+			wantErr: fmt.Sprintf("workload owner %v is unknown", &metav1.OwnerReference{
+				APIVersion: batchv1.SchemeGroupVersion.String(),
+				Kind:       "Job",
+				Name:       parentJobName,
+				UID:        types.UID(parentJobName),
+				Controller: pointer.Bool(true),
+			}),
+		},
+		"child job has ownerReference with known non-existing workload owner": {
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				OwnerReference(parentJobName, kubeflow.SchemeGroupVersionKind).
+				Obj(),
+			wantErr: "mpijobs.kubeflow.org \"test-job-parent\" not found",
```

  done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-06-05T15:37:35Z

- **pkg/controller/jobframework/reconciler_test.go:100** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-05T15:37:35Z
```diff
@@ -0,0 +1,119 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"fmt"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	batchv1 "k8s.io/api/batch/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	testingjob "sigs.k8s.io/kueue/pkg/util/testingjobs/job"
+	testingmpijob "sigs.k8s.io/kueue/pkg/util/testingjobs/mpijob"
+)
+
+func TestIsParentJobManaged(t *testing.T) {
+	parentJobName := "test-job-parent"
+	childJobName := "test-job-child"
+	jobNamespace := "default"
+	cases := map[string]struct {
+		parentJob client.Object
+		job       client.Object
+		want      bool
+		wantErr   error
+	}{
+		"child job doesn't have ownerReference": {
+			parentJob: testingmpijob.MakeMPIJob(parentJobName, jobNamespace).
+				UID(parentJobName).
+				Obj(),
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				Obj(),
+			wantErr: errChildJobOwnerNotFound,
+		},
+		"child job has ownerReference with unknown workload owner": {
+			parentJob: testingjob.MakeJob(parentJobName, jobNamespace).
+				UID(parentJobName).
+				Obj(),
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				OwnerReference(parentJobName, batchv1.SchemeGroupVersion.WithKind("Job")).
+				Obj(),
+			wantErr: fmt.Errorf("workload owner %v is unknown", &metav1.OwnerReference{
+				APIVersion: batchv1.SchemeGroupVersion.String(),
+				Kind:       "Job",
+				Name:       parentJobName,
+				UID:        types.UID(parentJobName),
+				Controller: pointer.Bool(true),
+			}),
+		},
+		"child job has ownerReference with known non-existing workload owner": {
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				OwnerReference(parentJobName, kubeflow.SchemeGroupVersionKind).
+				Obj(),
+			wantErr: apierrors.NewNotFound(kubeflow.Resource("mpijobs"), parentJobName),
+		},
+		"child job has ownerReference with known existing workload owner, and the parent job has queue-name label": {
+			parentJob: testingmpijob.MakeMPIJob(parentJobName, jobNamespace).
+				UID(parentJobName).
+				Queue("test-q").
+				Obj(),
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				OwnerReference(parentJobName, kubeflow.SchemeGroupVersionKind).
+				Obj(),
+			want: true,
+		},
+		"child job has ownerReference with known existing workload owner, and the parent job doesn't has queue-name label": {
+			parentJob: testingmpijob.MakeMPIJob(parentJobName, jobNamespace).
+				UID(parentJobName).
+				Obj(),
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				OwnerReference(parentJobName, kubeflow.SchemeGroupVersionKind).
+				Obj(),
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			builder := utiltesting.NewClientBuilder(kubeflow.AddToScheme)
+			if tc.parentJob != nil {
+				builder = builder.WithObjects(tc.parentJob)
+			}
+			cl := builder.Build()
+			r := JobReconciler{client: cl}
+			got, gotErr := r.isParentJobManaged(context.Background(), tc.job, jobNamespace)
+			if tc.want != got {
+				t.Errorf("Unexpected response from isParentManaged want: %v,got: %v", tc.want, got)
+			}
+			if gotErr != nil && tc.wantErr == nil {
+				t.Errorf("Unexpected error got: %s", gotErr.Error())
+			} else if gotErr == nil && tc.wantErr != nil {
+				t.Errorf("Missing expected error: %s", tc.wantErr.Error())
+			} else if gotErr != nil && tc.wantErr != nil {
+				if diff := cmp.Diff(tc.wantErr.Error(), gotErr.Error()); len(diff) != 0 {
+					t.Errorf("Unexpected error (-want,+got):\n%s", diff)
+				}
+			}
```

  You can replace all of this with: `cmp.Diff(tc.wantErr, gotErr, cmpopts.EquateErrors)`

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-05T15:46:14Z

- **pkg/controller/jobframework/reconciler_test.go:100** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-05T15:46:14Z
```diff
@@ -0,0 +1,119 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"fmt"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	batchv1 "k8s.io/api/batch/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	testingjob "sigs.k8s.io/kueue/pkg/util/testingjobs/job"
+	testingmpijob "sigs.k8s.io/kueue/pkg/util/testingjobs/mpijob"
+)
+
+func TestIsParentJobManaged(t *testing.T) {
+	parentJobName := "test-job-parent"
+	childJobName := "test-job-child"
+	jobNamespace := "default"
+	cases := map[string]struct {
+		parentJob client.Object
+		job       client.Object
+		want      bool
+		wantErr   error
+	}{
+		"child job doesn't have ownerReference": {
+			parentJob: testingmpijob.MakeMPIJob(parentJobName, jobNamespace).
+				UID(parentJobName).
+				Obj(),
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				Obj(),
+			wantErr: errChildJobOwnerNotFound,
+		},
+		"child job has ownerReference with unknown workload owner": {
+			parentJob: testingjob.MakeJob(parentJobName, jobNamespace).
+				UID(parentJobName).
+				Obj(),
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				OwnerReference(parentJobName, batchv1.SchemeGroupVersion.WithKind("Job")).
+				Obj(),
+			wantErr: fmt.Errorf("workload owner %v is unknown", &metav1.OwnerReference{
+				APIVersion: batchv1.SchemeGroupVersion.String(),
+				Kind:       "Job",
+				Name:       parentJobName,
+				UID:        types.UID(parentJobName),
+				Controller: pointer.Bool(true),
+			}),
+		},
+		"child job has ownerReference with known non-existing workload owner": {
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				OwnerReference(parentJobName, kubeflow.SchemeGroupVersionKind).
+				Obj(),
+			wantErr: apierrors.NewNotFound(kubeflow.Resource("mpijobs"), parentJobName),
+		},
+		"child job has ownerReference with known existing workload owner, and the parent job has queue-name label": {
+			parentJob: testingmpijob.MakeMPIJob(parentJobName, jobNamespace).
+				UID(parentJobName).
+				Queue("test-q").
+				Obj(),
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				OwnerReference(parentJobName, kubeflow.SchemeGroupVersionKind).
+				Obj(),
+			want: true,
+		},
+		"child job has ownerReference with known existing workload owner, and the parent job doesn't has queue-name label": {
+			parentJob: testingmpijob.MakeMPIJob(parentJobName, jobNamespace).
+				UID(parentJobName).
+				Obj(),
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				OwnerReference(parentJobName, kubeflow.SchemeGroupVersionKind).
+				Obj(),
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			builder := utiltesting.NewClientBuilder(kubeflow.AddToScheme)
+			if tc.parentJob != nil {
+				builder = builder.WithObjects(tc.parentJob)
+			}
+			cl := builder.Build()
+			r := JobReconciler{client: cl}
+			got, gotErr := r.isParentJobManaged(context.Background(), tc.job, jobNamespace)
+			if tc.want != got {
+				t.Errorf("Unexpected response from isParentManaged want: %v,got: %v", tc.want, got)
+			}
+			if gotErr != nil && tc.wantErr == nil {
+				t.Errorf("Unexpected error got: %s", gotErr.Error())
+			} else if gotErr == nil && tc.wantErr != nil {
+				t.Errorf("Missing expected error: %s", tc.wantErr.Error())
+			} else if gotErr != nil && tc.wantErr != nil {
+				if diff := cmp.Diff(tc.wantErr.Error(), gotErr.Error()); len(diff) != 0 {
+					t.Errorf("Unexpected error (-want,+got):\n%s", diff)
+				}
+			}
```

  Actually, I tried the solution. But it seems that we can not ignore differences in memory addresses with `cmpopts.EquateErrors`.
  
  ```go
  $ go test ./pkg/controller/jobframework/...
  --- FAIL: TestIsParentJobManaged (0.01s)
      --- FAIL: TestIsParentJobManaged/child_job_has_ownerReference_with_unknown_workload_owner (0.00s)
          reconciler_test.go:110: Unexpected error (-want,+got):
                (*errors.errorString)(
              -   &⟪0x01400049f290⟫{
              -           s: "workload owner &OwnerReference{Kind:Job,Name:test-job-parent,UID:test-job-parent,APIVersion:batch/v1,Controller:*true,BlockOwnerDeletion:nil,} is unknown",
              -   },
              +   &⟪0x0140002ca0a0⟫{
              +           s: "workload owner &OwnerReference{Kind:Job,Name:test-job-parent,UID:test-job-parent,APIVersion:batch/v1,Controller:*true,BlockOwnerDeletion:nil,} is unknown",
              +   },
                )
  ...
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-06-05T15:53:35Z

- **pkg/controller/jobframework/reconciler.go:263** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-05T15:53:35Z
```diff
@@ -236,6 +252,22 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	return ctrl.Result{}, nil
 }
 
+// isParentJobManaged checks whether the parent job is managed by kueue.
+func (r *JobReconciler) isParentJobManaged(ctx context.Context, jobObj client.Object, namespace string) (bool, error) {
+	owner := metav1.GetControllerOf(jobObj)
+	if owner == nil {
+		return false, errChildJobOwnerNotFound
+	}
+	parentJob := KnownWorkloadOwnerObject(owner)
+	if parentJob == nil {
+		return false, fmt.Errorf("workload owner %v is unknown", owner)
```

  `fmt.Errorf("owner %v: %w", owner, unknownWorkloadOwner)`
  
  This will allow you to use `cmp` directly.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-05T16:16:32Z

- **pkg/controller/jobframework/reconciler.go:263** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-05T16:16:32Z
```diff
@@ -236,6 +252,22 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	return ctrl.Result{}, nil
 }
 
+// isParentJobManaged checks whether the parent job is managed by kueue.
+func (r *JobReconciler) isParentJobManaged(ctx context.Context, jobObj client.Object, namespace string) (bool, error) {
+	owner := metav1.GetControllerOf(jobObj)
+	if owner == nil {
+		return false, errChildJobOwnerNotFound
+	}
+	parentJob := KnownWorkloadOwnerObject(owner)
+	if parentJob == nil {
+		return false, fmt.Errorf("workload owner %v is unknown", owner)
```

  That way seems not to allow me to use `cmp`...
  
  https://github.com/kubernetes-sigs/kueue/pull/821/commits/defd1b510ffa72b8a5e8ae40a3ae89c3c2f69ebb
  
  ```shell
  $ go test ./pkg/controller/jobframework/...
  --- FAIL: TestIsParentJobManaged (0.01s)
      --- FAIL: TestIsParentJobManaged/child_job_has_ownerReference_with_unknown_workload_owner (0.00s)
          reconciler_test.go:112: Unexpected error (-want,+got):
                (*fmt.wrapError)(
              -   &⟪0x01400078e860⟫{
              -           msg: "workload owner &OwnerReference{Kind:Job,Name:test-job-parent,UID:test-job-parent,APIVersion:batch/v1,Controller:*true,BlockOwnerDeletion:nil,}: workload owner is unknown",
              -           err: &⟪0x0140005279a0⟫"errors".errorString{s: "workload owner is unknown"},
              -   },
              +   &⟪0x014000295a00⟫{
              +           msg: "workload owner &OwnerReference{Kind:Job,Name:test-job-parent,UID:test-job-parent,APIVersion:batch/v1,Controller:*true,BlockOwnerDeletion:nil,}: workload owner is unknown",
              +           err: &⟪0x0140005279a0⟫"errors".errorString{s: "workload owner is unknown"},
              +   },
                )
      --- FAIL: TestIsParentJobManaged/child_job_has_ownerReference_with_known_non-existing_workload_owner (0.00s)
          reconciler_test.go:112: Unexpected error (-want,+got):
                (*fmt.wrapError)(
              -   &⟪0x01400078e880⟫{
              -           msg: `workload owner not found: mpijobs.kubeflow.org "test-job-parent" not found`,
              -           err: &⟪0x014000782c80⟫"k8s.io/apimachinery/pkg/api/errors".StatusError{
              -                   ErrStatus: "k8s.io/apimachinery/pkg/apis/meta/v1".Status{
              -                           Status:  "Failure",
              -                           Message: `mpijobs.kubeflow.org "test-job-parent" not found`,
              -                           Reason:  "NotFound",
              -                           Details: &⟪0x01400060cc60⟫"k8s.io/apimachinery/pkg/apis/meta/v1".StatusDetails{Name: "test-job-parent", Group: "kubeflow.org", Kind: "mpijobs"},
              -                           Code:    404,
              -                   },
              -           },
              -   },
              +   &⟪0x01400078fc00⟫{
              +           msg: `workload owner not found: mpijobs.kubeflow.org "test-job-parent" not found`,
              +           err: &⟪0x014000630640⟫"k8s.io/apimachinery/pkg/api/errors".StatusError{
              +                   ErrStatus: "k8s.io/apimachinery/pkg/apis/meta/v1".Status{
              +                           Status:  "Failure",
              +                           Message: `mpijobs.kubeflow.org "test-job-parent" not found`,
              +                           Reason:  "NotFound",
              +                           Details: &⟪0x01400060ca20⟫"k8s.io/apimachinery/pkg/apis/meta/v1".StatusDetails{Name: "test-job-parent", Group: "kubeflow.org", Kind: "mpijobs"},
              +                           Code:    404,
              +                   },
              +           },
              +   },
                )
  FAIL
  FAIL    sigs.k8s.io/kueue/pkg/controller/jobframework   0.907s
  FAIL
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-06T18:24:39Z

- **pkg/controller/jobframework/reconciler.go:263** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-06T18:24:39Z
```diff
@@ -236,6 +252,22 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	return ctrl.Result{}, nil
 }
 
+// isParentJobManaged checks whether the parent job is managed by kueue.
+func (r *JobReconciler) isParentJobManaged(ctx context.Context, jobObj client.Object, namespace string) (bool, error) {
+	owner := metav1.GetControllerOf(jobObj)
+	if owner == nil {
+		return false, errChildJobOwnerNotFound
+	}
+	parentJob := KnownWorkloadOwnerObject(owner)
+	if parentJob == nil {
+		return false, fmt.Errorf("workload owner %v is unknown", owner)
```

  @alculquicondor As a result, I think we can not use `cmp` since the errors refer to other memory addresses in the `child job has ownerReference with unknown workload owner` and the `child job has ownerReference with known non-existing workload owner` case.
  
  So, I'd like to revert https://github.com/kubernetes-sigs/kueue/commit/defd1b510ffa72b8a5e8ae40a3ae89c3c2f69ebb. Any thought?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-06-06T19:00:56Z

- **pkg/controller/jobframework/reconciler.go:263** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-06T19:00:56Z
```diff
@@ -236,6 +252,22 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	return ctrl.Result{}, nil
 }
 
+// isParentJobManaged checks whether the parent job is managed by kueue.
+func (r *JobReconciler) isParentJobManaged(ctx context.Context, jobObj client.Object, namespace string) (bool, error) {
+	owner := metav1.GetControllerOf(jobObj)
+	if owner == nil {
+		return false, errChildJobOwnerNotFound
+	}
+	parentJob := KnownWorkloadOwnerObject(owner)
+	if parentJob == nil {
+		return false, fmt.Errorf("workload owner %v is unknown", owner)
```

  use the same memory address in `wantError`.
  
  If that doesn't work then, yes, you can revert.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-06T19:58:54Z

- **pkg/controller/jobframework/reconciler_test.go:60** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-06T19:58:54Z
```diff
@@ -0,0 +1,116 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"fmt"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	batchv1 "k8s.io/api/batch/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	testingjob "sigs.k8s.io/kueue/pkg/util/testingjobs/job"
+	testingmpijob "sigs.k8s.io/kueue/pkg/util/testingjobs/mpijob"
+)
+
+func TestIsParentJobManaged(t *testing.T) {
+	parentJobName := "test-job-parent"
+	childJobName := "test-job-child"
+	jobNamespace := "default"
+	unknwonWlOwner := &metav1.OwnerReference{
+		APIVersion: batchv1.SchemeGroupVersion.String(),
+		Kind:       "Job",
+		Name:       parentJobName,
+		UID:        types.UID(parentJobName),
+		Controller: pointer.Bool(true),
+	}
+
+	cases := map[string]struct {
+		parentJob client.Object
+		job       client.Object
+		want      bool
+		wantErr   error
+	}{
+		"child job doesn't have ownerReference": {
+			parentJob: testingmpijob.MakeMPIJob(parentJobName, jobNamespace).
+				UID(parentJobName).
+				Obj(),
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				Obj(),
+			wantErr: errChildJobOwnerNotFound,
+		},
+		"child job has ownerReference with unknown workload owner": {
+			parentJob: testingjob.MakeJob(parentJobName, jobNamespace).
+				UID(parentJobName).
+				Obj(),
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				OwnerReference(parentJobName, batchv1.SchemeGroupVersion.WithKind("Job")).
+				Obj(),
+			wantErr: fmt.Errorf("workload owner %s: %w", unknwonWlOwner.String(), errUnknownWorkloadOwner),
+		},
```

  @alculquicondor For example, we create a new error object in this case. So we faced the compare error.
  Hence, I guess that `cmp.Diff` doesn't work well...

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-06T19:59:51Z

- **pkg/controller/jobframework/reconciler_test.go:60** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-06T19:59:51Z
```diff
@@ -0,0 +1,116 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"fmt"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	batchv1 "k8s.io/api/batch/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	testingjob "sigs.k8s.io/kueue/pkg/util/testingjobs/job"
+	testingmpijob "sigs.k8s.io/kueue/pkg/util/testingjobs/mpijob"
+)
+
+func TestIsParentJobManaged(t *testing.T) {
+	parentJobName := "test-job-parent"
+	childJobName := "test-job-child"
+	jobNamespace := "default"
+	unknwonWlOwner := &metav1.OwnerReference{
+		APIVersion: batchv1.SchemeGroupVersion.String(),
+		Kind:       "Job",
+		Name:       parentJobName,
+		UID:        types.UID(parentJobName),
+		Controller: pointer.Bool(true),
+	}
+
+	cases := map[string]struct {
+		parentJob client.Object
+		job       client.Object
+		want      bool
+		wantErr   error
+	}{
+		"child job doesn't have ownerReference": {
+			parentJob: testingmpijob.MakeMPIJob(parentJobName, jobNamespace).
+				UID(parentJobName).
+				Obj(),
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				Obj(),
+			wantErr: errChildJobOwnerNotFound,
+		},
+		"child job has ownerReference with unknown workload owner": {
+			parentJob: testingjob.MakeJob(parentJobName, jobNamespace).
+				UID(parentJobName).
+				Obj(),
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				OwnerReference(parentJobName, batchv1.SchemeGroupVersion.WithKind("Job")).
+				Obj(),
+			wantErr: fmt.Errorf("workload owner %s: %w", unknwonWlOwner.String(), errUnknownWorkloadOwner),
+		},
```

  So if we use `wantErr:  errUnknownWorkloadOwner`, that should work well.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-06-06T20:03:37Z

- **pkg/controller/jobframework/reconciler_test.go:60** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-06T20:03:36Z
```diff
@@ -0,0 +1,116 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"fmt"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	batchv1 "k8s.io/api/batch/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	testingjob "sigs.k8s.io/kueue/pkg/util/testingjobs/job"
+	testingmpijob "sigs.k8s.io/kueue/pkg/util/testingjobs/mpijob"
+)
+
+func TestIsParentJobManaged(t *testing.T) {
+	parentJobName := "test-job-parent"
+	childJobName := "test-job-child"
+	jobNamespace := "default"
+	unknwonWlOwner := &metav1.OwnerReference{
+		APIVersion: batchv1.SchemeGroupVersion.String(),
+		Kind:       "Job",
+		Name:       parentJobName,
+		UID:        types.UID(parentJobName),
+		Controller: pointer.Bool(true),
+	}
+
+	cases := map[string]struct {
+		parentJob client.Object
+		job       client.Object
+		want      bool
+		wantErr   error
+	}{
+		"child job doesn't have ownerReference": {
+			parentJob: testingmpijob.MakeMPIJob(parentJobName, jobNamespace).
+				UID(parentJobName).
+				Obj(),
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				Obj(),
+			wantErr: errChildJobOwnerNotFound,
+		},
+		"child job has ownerReference with unknown workload owner": {
+			parentJob: testingjob.MakeJob(parentJobName, jobNamespace).
+				UID(parentJobName).
+				Obj(),
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				OwnerReference(parentJobName, batchv1.SchemeGroupVersion.WithKind("Job")).
+				Obj(),
+			wantErr: fmt.Errorf("workload owner %s: %w", unknwonWlOwner.String(), errUnknownWorkloadOwner),
+		},
```

  yes, that's what you should do :)

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-06T20:07:18Z

- **pkg/controller/jobframework/reconciler_test.go:60** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-06T20:07:17Z
```diff
@@ -0,0 +1,116 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"fmt"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	batchv1 "k8s.io/api/batch/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	testingjob "sigs.k8s.io/kueue/pkg/util/testingjobs/job"
+	testingmpijob "sigs.k8s.io/kueue/pkg/util/testingjobs/mpijob"
+)
+
+func TestIsParentJobManaged(t *testing.T) {
+	parentJobName := "test-job-parent"
+	childJobName := "test-job-child"
+	jobNamespace := "default"
+	unknwonWlOwner := &metav1.OwnerReference{
+		APIVersion: batchv1.SchemeGroupVersion.String(),
+		Kind:       "Job",
+		Name:       parentJobName,
+		UID:        types.UID(parentJobName),
+		Controller: pointer.Bool(true),
+	}
+
+	cases := map[string]struct {
+		parentJob client.Object
+		job       client.Object
+		want      bool
+		wantErr   error
+	}{
+		"child job doesn't have ownerReference": {
+			parentJob: testingmpijob.MakeMPIJob(parentJobName, jobNamespace).
+				UID(parentJobName).
+				Obj(),
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				Obj(),
+			wantErr: errChildJobOwnerNotFound,
+		},
+		"child job has ownerReference with unknown workload owner": {
+			parentJob: testingjob.MakeJob(parentJobName, jobNamespace).
+				UID(parentJobName).
+				Obj(),
+			job: testingjob.MakeJob(childJobName, jobNamespace).
+				OwnerReference(parentJobName, batchv1.SchemeGroupVersion.WithKind("Job")).
+				Obj(),
+			wantErr: fmt.Errorf("workload owner %s: %w", unknwonWlOwner.String(), errUnknownWorkloadOwner),
+		},
```

  Ah, I see what your suggestion means :)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-06-06T20:22:49Z

/approve

A couple of nits

- **pkg/controller/jobframework/reconciler.go:131** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-06T20:21:26Z
```diff
@@ -109,9 +114,23 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && isStandaloneJob {
-		log.V(3).Info(fmt.Sprintf("Neither %s label, nor %s annotation is set, ignoring the job", QueueLabel, ParentWorkloadAnnotation))
-		return ctrl.Result{}, nil
+	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+		if isStandaloneJob {
+			log.V(3).Info("Neither queue-name label, nor parent-workload annotation is set, ignoring the job",
+				controllerconsts.QueueLabel, QueueName(job), controllerconsts.ParentWorkloadAnnotation, ParentWorkloadName(job))
+			return ctrl.Result{}, nil
+		}
+		isParentJobManaged, err := r.isParentJobManaged(ctx, job.Object(), namespacedName.Namespace)
+		if err != nil {
+			log.Error(err, "couldn't check whether the parent job is managed by kueue")
+			return ctrl.Result{}, err
+		}
+		if !isParentJobManaged {
+			log.V(3).Info("parent-workload annotation is set, and the parent job doesn't have a queue-name label, ignoring the job",
+				controllerconsts.ParentWorkloadAnnotation, ParentWorkloadName(job))
```

  I would not use the same annotation, so that it's easier to write in a log query.
  
  ```suggestion
  				"parentWorkload", ParentWorkloadName(job))
  ```

- **pkg/controller/jobframework/reconciler.go:266** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-06T20:22:12Z
```diff
@@ -236,6 +255,22 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	return ctrl.Result{}, nil
 }
 
+// isParentJobManaged checks whether the parent job is managed by kueue.
+func (r *JobReconciler) isParentJobManaged(ctx context.Context, jobObj client.Object, namespace string) (bool, error) {
+	owner := metav1.GetControllerOf(jobObj)
+	if owner == nil {
+		return false, errChildJobOwnerNotFound
+	}
+	parentJob := KnownWorkloadOwnerObject(owner)
+	if parentJob == nil {
+		return false, fmt.Errorf("workload owner %s: %w", owner.String(), errUnknownWorkloadOwner)
```

  I think this should work?
  
  ```suggestion
  		return false, fmt.Errorf("workload owner %s: %w", owner, errUnknownWorkloadOwner)
  ```

- **pkg/controller/jobframework/reconciler_test.go:41** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-06T20:22:38Z
```diff
@@ -0,0 +1,103 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	batchv1 "k8s.io/api/batch/v1"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	testingjob "sigs.k8s.io/kueue/pkg/util/testingjobs/job"
+	testingmpijob "sigs.k8s.io/kueue/pkg/util/testingjobs/mpijob"
+)
+
+func TestIsParentJobManaged(t *testing.T) {
+	parentJobName := "test-job-parent"
+	childJobName := "test-job-child"
+	jobNamespace := "default"
+	cases := map[string]struct {
+		parentJob client.Object
+		job       client.Object
+		want      bool
```

  ```suggestion
  		wantManaged      bool
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-06T20:31:19Z

- **pkg/controller/jobframework/reconciler.go:131** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-06T20:31:19Z
```diff
@@ -109,9 +114,23 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && isStandaloneJob {
-		log.V(3).Info(fmt.Sprintf("Neither %s label, nor %s annotation is set, ignoring the job", QueueLabel, ParentWorkloadAnnotation))
-		return ctrl.Result{}, nil
+	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+		if isStandaloneJob {
+			log.V(3).Info("Neither queue-name label, nor parent-workload annotation is set, ignoring the job",
+				controllerconsts.QueueLabel, QueueName(job), controllerconsts.ParentWorkloadAnnotation, ParentWorkloadName(job))
+			return ctrl.Result{}, nil
+		}
+		isParentJobManaged, err := r.isParentJobManaged(ctx, job.Object(), namespacedName.Namespace)
+		if err != nil {
+			log.Error(err, "couldn't check whether the parent job is managed by kueue")
+			return ctrl.Result{}, err
+		}
+		if !isParentJobManaged {
+			log.V(3).Info("parent-workload annotation is set, and the parent job doesn't have a queue-name label, ignoring the job",
+				controllerconsts.ParentWorkloadAnnotation, ParentWorkloadName(job))
```

  I will fix L121 as well.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-06T20:34:55Z

- **pkg/controller/jobframework/reconciler.go:131** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-06T20:34:55Z
```diff
@@ -109,9 +114,23 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && isStandaloneJob {
-		log.V(3).Info(fmt.Sprintf("Neither %s label, nor %s annotation is set, ignoring the job", QueueLabel, ParentWorkloadAnnotation))
-		return ctrl.Result{}, nil
+	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+		if isStandaloneJob {
+			log.V(3).Info("Neither queue-name label, nor parent-workload annotation is set, ignoring the job",
+				controllerconsts.QueueLabel, QueueName(job), controllerconsts.ParentWorkloadAnnotation, ParentWorkloadName(job))
+			return ctrl.Result{}, nil
+		}
+		isParentJobManaged, err := r.isParentJobManaged(ctx, job.Object(), namespacedName.Namespace)
+		if err != nil {
+			log.Error(err, "couldn't check whether the parent job is managed by kueue")
+			return ctrl.Result{}, err
+		}
+		if !isParentJobManaged {
+			log.V(3).Info("parent-workload annotation is set, and the parent job doesn't have a queue-name label, ignoring the job",
+				controllerconsts.ParentWorkloadAnnotation, ParentWorkloadName(job))
```

  In L294, we use `parent-workload`. So I will replace the annotation with `parent-workload` for consistency
  
  ```golang
  			log.V(2).Info("job with no matching parent workload", "parent-workload", pwName)
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-06T20:40:49Z

- **pkg/controller/jobframework/reconciler.go:131** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-06T20:40:49Z
```diff
@@ -109,9 +114,23 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && isStandaloneJob {
-		log.V(3).Info(fmt.Sprintf("Neither %s label, nor %s annotation is set, ignoring the job", QueueLabel, ParentWorkloadAnnotation))
-		return ctrl.Result{}, nil
+	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+		if isStandaloneJob {
+			log.V(3).Info("Neither queue-name label, nor parent-workload annotation is set, ignoring the job",
+				controllerconsts.QueueLabel, QueueName(job), controllerconsts.ParentWorkloadAnnotation, ParentWorkloadName(job))
+			return ctrl.Result{}, nil
+		}
+		isParentJobManaged, err := r.isParentJobManaged(ctx, job.Object(), namespacedName.Namespace)
+		if err != nil {
+			log.Error(err, "couldn't check whether the parent job is managed by kueue")
+			return ctrl.Result{}, err
+		}
+		if !isParentJobManaged {
+			log.V(3).Info("parent-workload annotation is set, and the parent job doesn't have a queue-name label, ignoring the job",
+				controllerconsts.ParentWorkloadAnnotation, ParentWorkloadName(job))
```

  done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-06T20:40:57Z

- **pkg/controller/jobframework/reconciler_test.go:41** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-06T20:40:57Z
```diff
@@ -0,0 +1,103 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	batchv1 "k8s.io/api/batch/v1"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	testingjob "sigs.k8s.io/kueue/pkg/util/testingjobs/job"
+	testingmpijob "sigs.k8s.io/kueue/pkg/util/testingjobs/mpijob"
+)
+
+func TestIsParentJobManaged(t *testing.T) {
+	parentJobName := "test-job-parent"
+	childJobName := "test-job-child"
+	jobNamespace := "default"
+	cases := map[string]struct {
+		parentJob client.Object
+		job       client.Object
+		want      bool
```

  done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-06T20:41:04Z

- **pkg/controller/jobframework/reconciler.go:266** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-06T20:41:04Z
```diff
@@ -236,6 +255,22 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	return ctrl.Result{}, nil
 }
 
+// isParentJobManaged checks whether the parent job is managed by kueue.
+func (r *JobReconciler) isParentJobManaged(ctx context.Context, jobObj client.Object, namespace string) (bool, error) {
+	owner := metav1.GetControllerOf(jobObj)
+	if owner == nil {
+		return false, errChildJobOwnerNotFound
+	}
+	parentJob := KnownWorkloadOwnerObject(owner)
+	if parentJob == nil {
+		return false, fmt.Errorf("workload owner %s: %w", owner.String(), errUnknownWorkloadOwner)
```

  done.
