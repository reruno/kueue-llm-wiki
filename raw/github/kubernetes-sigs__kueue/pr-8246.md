# PR #8246: Define KEP for supporting modern tls settings

**Summary**: Define KEP for supporting modern tls settings

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8246

**Last updated**: 2026-01-16T18:02:28Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kannon92](https://github.com/kannon92)
- **Branch**: `tls-min-version-kep` → `main`
- **Created**: 2025-12-15T16:12:32Z
- **Updated**: 2026-01-16T18:02:28Z
- **Merged**: 2026-01-16T17:59:17Z
- **Closed**: 2026-01-16T17:59:17Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@kshalot](https://github.com/kshalot), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@pajakd](https://github.com/pajakd), [@everettraven](https://github.com/everettraven), [@kshalot](https://github.com/kshalot)
- **Changed files**: 2   **+410 / -0**

## Description


<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
KEP for #8190 

#### Special notes for your reviewer:

crypto tls: https://cs.opensource.google/go/go/+/refs/tags/go1.25.5:src/crypto/tls/cipher_suites.go;l=686


#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-12-15T16:12:39Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 5d30f58cf4bd54635f2866d2a19e852f51527171 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6967f0e0a9c8de00089cb811 |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-12-16T07:51:19Z

ack, I will try to give an initial pass this week. Otherwise after returning in January. Maybe @tenzen-y will be able to review in the meanwhile

### Comment by [@kannon92](https://github.com/kannon92) — 2025-12-17T20:53:53Z

/hold

As I was working on the implementation, I realize that Curves are not yet present in upstream kubernetes.

### Comment by [@kannon92](https://github.com/kannon92) — 2025-12-18T21:29:43Z

@mimowo @kshalot 

I did some updates to this. I realize that curves are a lot more complicated than I thought.

There is not yet support in upstream kubernetes so it would not be possible to implement this for visibility server. Visibility inherits settings for extended apiservers and curves are not an option yet.

So for now, I refactored this to focus on TLSMinVersion and CipherSuite. Both are options already present in Kubernetes in both kubelet and apiserver.

Kubelet: https://github.com/kubernetes/kubernetes/blob/master/staging/src/k8s.io/kubelet/config/v1beta1/types.go#L182
APIServer: https://github.com/kubernetes/kubernetes/blob/master/staging/src/k8s.io/apiserver/pkg/server/secure_serving.go#L60

### Comment by [@kannon92](https://github.com/kannon92) — 2025-12-19T20:21:25Z

/hold cancel

### Comment by [@kannon92](https://github.com/kannon92) — 2026-01-05T14:31:18Z

@kshalot @mimowo @tenzen-y I'm hoping to get this in for Kueue 0.16 so let me know if there is anything you think I need to update.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-07T16:29:07Z

> @kshalot @mimowo @tenzen-y I'm hoping to get this in for Kueue 0.16 so let me know if there is anything you think I need to update.

ACK

### Comment by [@kshalot](https://github.com/kshalot) — 2026-01-08T18:23:59Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-08T18:24:06Z

LGTM label has been added.  <details>Git tree hash: 51ffd9fbf820a8214738e5d03173c111db8f0767</details>

### Comment by [@kannon92](https://github.com/kannon92) — 2026-01-09T19:28:16Z

cc @everettraven 

as he is interested in helping out on API reviews for Kueue.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-12T14:22:59Z

/assign 
I'm going to review today or tomorrow so that this can still be included in 0.16.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-14T08:24:25Z

LGTM, just one nit comment for the validation part: https://github.com/kubernetes-sigs/kueue/pull/8246#discussion_r2689442954

Thank you for driving this 👍

### Comment by [@kannon92](https://github.com/kannon92) — 2026-01-14T16:14:02Z

Based on [this](https://github.com/kubernetes-sigs/kueue/pull/8246#issuecomment-3748382501),

I'm going forward with the implementation.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-14T16:49:55Z

> Based on https://github.com/kubernetes-sigs/kueue/pull/8246#issuecomment-3748382501,
> I'm going forward with the implementation.

Thank you, but be aware this is not approved yet, and I'm totally ok if @tenzen-y prefers to request some changes. For example, I don't have a strong view on the enum vs string  + validation myself. Just leaning towards string + validation, because: (1) this is the solution used in the core k8s (kubelet), (2)  enums require maintenance, but honestly I have no idea how much effort would be in the future to maintain those.

### Comment by [@kannon92](https://github.com/kannon92) — 2026-01-14T20:40:47Z

> > Based on [#8246 (comment)](https://github.com/kubernetes-sigs/kueue/pull/8246#issuecomment-3748382501),
> > I'm going forward with the implementation.
> 
> Thank you, but be aware this is not approved yet, and I'm totally ok if @tenzen-y prefers to request some changes. For example, I don't have a strong view on the enum vs string + validation myself. Just leaning towards string + validation, because: (1) this is the solution used in the core k8s (kubelet), (2) enums require maintenance, but honestly I have no idea how much effort would be in the future to maintain those.

Yea that is understood.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-15T20:01:45Z

Sorry for the delayed review. Let me try to revisit here again, tomorrow....

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-16T17:39:19Z

> > Based on [#8246 (comment)](https://github.com/kubernetes-sigs/kueue/pull/8246#issuecomment-3748382501),
> > I'm going forward with the implementation.
> 
> Thank you, but be aware this is not approved yet, and I'm totally ok if @tenzen-y prefers to request some changes. For example, I don't have a strong view on the enum vs string + validation myself. Just leaning towards string + validation, because: (1) this is the solution used in the core k8s (kubelet), (2) enums require maintenance, but honestly I have no idea how much effort would be in the future to maintain those.

I totally agree with @mimowo. I'm not sure how to track the policy updates in the TLS RFC when we select enums approach.
We might be able to find a better those management way even when using enum, but for now, I'm not sure.

If we find the way, we can always change the current string patameters to enum since those changes don't break CRD.
Anyway, at the first iteration, let's use string + validation pattern.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-16T17:40:03Z

LGTM label has been added.  <details>Git tree hash: 87e4a32a7e802a9e49f04d8ff2512d56954d163a</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-16T17:40:07Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8246#" title="Author self-approved">kannon92</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8246#pullrequestreview-3640654819" title="LGTM">kshalot</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8246#pullrequestreview-3671995811" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@everettraven](https://github.com/everettraven) — 2026-01-16T17:59:42Z

>If we find the way, we can always change the current string patameters to enum since those changes don't break CRD.

Is this non-breaking because it is not in a CRD? Going from a free-form string to one restricted by an enum is a breaking API change.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-16T18:02:28Z

> Is this non-breaking because it is not in a CRD? Going from a free-form string to one restricted by an enum is a breaking API change.

Sorry for your confusion. KueueConfiguration is not a CRD, which is a plain JSON configuration API.

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-16T15:13:49Z

- **keps/8190-tls-security-profiles/README.md:187** by [@mimowo](https://github.com/mimowo) — 2025-12-16T15:13:49Z
```diff
@@ -0,0 +1,424 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version, cipher suites, and elliptic curve preferences
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Allow configuration of elliptic curve preferences for key exchange
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+3. Elliptic curve preferences (for ECDHE key exchange)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites and elliptic curves approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_RSA_WITH_AES_128_CBC_SHA;TLS_RSA_WITH_AES_256_CBC_SHA;...
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSRSAWithAES128CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSRSAWithAES256CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	// ... additional cipher suites
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
+)
+
+// CurveID represents an elliptic curve identifier
+// +kubebuilder:validation:Enum=CurveP256;CurveP384;CurveP521;X25519
+type CurveID string
+
+const (
+	// CurveP256 represents the P-256 elliptic curve
+	CurveP256 CurveID = "CurveP256"
+	// CurveP384 represents the P-384 elliptic curve
+	CurveP384 CurveID = "CurveP384"
+	// CurveP521 represents the P-521 elliptic curve
+	CurveP521 CurveID = "CurveP521"
+	// X25519 represents the X25519 elliptic curve
+	CurveX25519 CurveID = "X25519"
```

  Is it constraining the options to only those listed here? Are we sure there will be no other new options?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-16T15:15:40Z

- **keps/8190-tls-security-profiles/README.md:172** by [@mimowo](https://github.com/mimowo) — 2025-12-16T15:15:40Z
```diff
@@ -0,0 +1,424 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version, cipher suites, and elliptic curve preferences
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Allow configuration of elliptic curve preferences for key exchange
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+3. Elliptic curve preferences (for ECDHE key exchange)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites and elliptic curves approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_RSA_WITH_AES_128_CBC_SHA;TLS_RSA_WITH_AES_256_CBC_SHA;...
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSRSAWithAES128CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSRSAWithAES256CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	// ... additional cipher suites
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
```

  Also, is it closed world, or open-world set? If closed, because I see `// +kubebuilder:validation:Enum=TLS_RSA_WITH_AES_128_CBC_SHA;TLS_RSA_WITH_AES_256_CBC_SHA;...
  ` then how sure we are no new options will be added. It feels better to keep it open world, but I'm not totally sure what are the policies here.
  
  Is there something analogous in the core k8s we could use as example?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-16T15:15:57Z

- **keps/8190-tls-security-profiles/kep.yaml:10** by [@mimowo](https://github.com/mimowo) — 2025-12-16T15:15:57Z
```diff
@@ -0,0 +1,25 @@
+title: TLS Security Profile Support
+kep-number: 8190
+authors:
+  - "@kannon92"
+status: implementable 
+creation-date: 2025-12-11
+reviewers:
+  - TBD
+approvers:
+  - TBD
```

  you can add me here

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-12-16T15:58:32Z

- **keps/8190-tls-security-profiles/README.md:187** by [@kshalot](https://github.com/kshalot) — 2025-12-16T15:58:32Z
```diff
@@ -0,0 +1,424 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version, cipher suites, and elliptic curve preferences
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Allow configuration of elliptic curve preferences for key exchange
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+3. Elliptic curve preferences (for ECDHE key exchange)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites and elliptic curves approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_RSA_WITH_AES_128_CBC_SHA;TLS_RSA_WITH_AES_256_CBC_SHA;...
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSRSAWithAES128CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSRSAWithAES256CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	// ... additional cipher suites
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
+)
+
+// CurveID represents an elliptic curve identifier
+// +kubebuilder:validation:Enum=CurveP256;CurveP384;CurveP521;X25519
+type CurveID string
+
+const (
+	// CurveP256 represents the P-256 elliptic curve
+	CurveP256 CurveID = "CurveP256"
+	// CurveP384 represents the P-384 elliptic curve
+	CurveP384 CurveID = "CurveP384"
+	// CurveP521 represents the P-521 elliptic curve
+	CurveP521 CurveID = "CurveP521"
+	// X25519 represents the X25519 elliptic curve
+	CurveX25519 CurveID = "X25519"
```

  Looking at the [TLS 1.3 RFC](https://datatracker.ietf.org/doc/html/rfc8446#section-4.2.7), curve488 is missing for TLS 1.3 at least, but that's not a comprehensive list (this is [here](https://www.iana.org/assignments/tls-parameters/tls-parameters.xhtml#tls-parameters-8) IIUC).

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-12-16T16:00:55Z

- **keps/8190-tls-security-profiles/README.md:172** by [@kannon92](https://github.com/kannon92) — 2025-12-16T16:00:55Z
```diff
@@ -0,0 +1,424 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version, cipher suites, and elliptic curve preferences
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Allow configuration of elliptic curve preferences for key exchange
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+3. Elliptic curve preferences (for ECDHE key exchange)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites and elliptic curves approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_RSA_WITH_AES_128_CBC_SHA;TLS_RSA_WITH_AES_256_CBC_SHA;...
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSRSAWithAES128CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSRSAWithAES256CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	// ... additional cipher suites
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
```

  I see some examples in our component-base / apiserver.
  
  https://github.com/kubernetes/kubernetes/blob/master/staging/src/k8s.io/apiserver/pkg/server/config.go#L346
  
  I see mostly configs accepting a raw string and then doing the validation in the code.
  
  I was thinking it would be better to provide a list of allowed values.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-12-16T16:35:09Z

- **keps/8190-tls-security-profiles/README.md:169** by [@kshalot](https://github.com/kshalot) — 2025-12-16T16:35:09Z
```diff
@@ -0,0 +1,424 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version, cipher suites, and elliptic curve preferences
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Allow configuration of elliptic curve preferences for key exchange
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+3. Elliptic curve preferences (for ECDHE key exchange)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites and elliptic curves approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_RSA_WITH_AES_128_CBC_SHA;TLS_RSA_WITH_AES_256_CBC_SHA;...
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSRSAWithAES128CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSRSAWithAES256CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	// ... additional cipher suites
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
+)
+
+// CurveID represents an elliptic curve identifier
+// +kubebuilder:validation:Enum=CurveP256;CurveP384;CurveP521;X25519
+type CurveID string
+
+const (
+	// CurveP256 represents the P-256 elliptic curve
+	CurveP256 CurveID = "CurveP256"
+	// CurveP384 represents the P-384 elliptic curve
+	CurveP384 CurveID = "CurveP384"
+	// CurveP521 represents the P-521 elliptic curve
+	CurveP521 CurveID = "CurveP521"
+	// X25519 represents the X25519 elliptic curve
+	CurveX25519 CurveID = "X25519"
+)
+
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// MinTLSVersion specifies the minimum TLS version that is acceptable.
+	// If not specified, defaults to "1.2" for backward compatibility.
+	// +optional
+	MinTLSVersion TLSVersion `json:"minTLSVersion,omitempty"`
+
+	// CipherSuites specifies the list of enabled TLS cipher suites.
+	// If not specified, a secure default list will be used.
+	// The available cipher suites are defined in Go's crypto/tls package.
+	// See https://golang.org/pkg/crypto/tls/#pkg-constants for valid values.
+	// +optional
+	CipherSuites []CipherSuite `json:"cipherSuites,omitempty"`
+
+	// CurvePreferences specifies the elliptic curves that will be used in
+	// an ECDHE handshake, in preference order.
+	// If not specified, a default set of curves will be used.
+	// +optional
+	CurvePreferences []CurveID `json:"curvePreferences,omitempty"`
+}
+
+// ControllerConfigurationSpec defines the desired state of ControllerConfiguration
+type ControllerConfigurationSpec struct {
+	// ... existing fields ...
+
+	// TLS contains TLS security settings for all Kueue API servers
+	// (webhooks, metrics, controller, and visibility).
+	// +optional
+	TLS *TLSOptions `json:"tls,omitempty"`
```

  Just flagging one thing, although I did not reflect deeply yet - it seems the "security"/"identity" fields for the endpoints is getting a bit scattered across the configuration, no?
  
  For example, on `main`, I can see:
  https://github.com/kubernetes-sigs/kueue/blob/441fe203453014175d04dba136e4e3de8d96e9b4/apis/config/v1beta2/configuration_types.go#L69-L70
  which is then present in the root `Configuration` struct:
  https://github.com/kubernetes-sigs/kueue/blob/441fe203453014175d04dba136e4e3de8d96e9b4/apis/config/v1beta2/configuration_types.go#L350-L371
  and also:
  https://github.com/kubernetes-sigs/kueue/blob/441fe203453014175d04dba136e4e3de8d96e9b4/apis/config/v1beta2/configuration_types.go#L154-L159
  in `Configuration > ControllerManager > ControllerWebhook` struct.
  
  Maybe we could consider some centralized "endpoint security" configuration? The API is already a mature-ish since it's in beta, but I'm thinking about anything security-related that might appear in the future

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-12-16T16:42:30Z

- **keps/8190-tls-security-profiles/README.md:172** by [@kannon92](https://github.com/kannon92) — 2025-12-16T16:42:30Z
```diff
@@ -0,0 +1,424 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version, cipher suites, and elliptic curve preferences
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Allow configuration of elliptic curve preferences for key exchange
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+3. Elliptic curve preferences (for ECDHE key exchange)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites and elliptic curves approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_RSA_WITH_AES_128_CBC_SHA;TLS_RSA_WITH_AES_256_CBC_SHA;...
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSRSAWithAES128CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSRSAWithAES256CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	// ... additional cipher suites
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
```

  We could switch to providing strings and validate.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-12-16T18:00:26Z

- **keps/8190-tls-security-profiles/README.md:169** by [@kannon92](https://github.com/kannon92) — 2025-12-16T18:00:26Z
```diff
@@ -0,0 +1,424 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version, cipher suites, and elliptic curve preferences
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Allow configuration of elliptic curve preferences for key exchange
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+3. Elliptic curve preferences (for ECDHE key exchange)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites and elliptic curves approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_RSA_WITH_AES_128_CBC_SHA;TLS_RSA_WITH_AES_256_CBC_SHA;...
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSRSAWithAES128CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSRSAWithAES256CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	// ... additional cipher suites
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
+)
+
+// CurveID represents an elliptic curve identifier
+// +kubebuilder:validation:Enum=CurveP256;CurveP384;CurveP521;X25519
+type CurveID string
+
+const (
+	// CurveP256 represents the P-256 elliptic curve
+	CurveP256 CurveID = "CurveP256"
+	// CurveP384 represents the P-384 elliptic curve
+	CurveP384 CurveID = "CurveP384"
+	// CurveP521 represents the P-521 elliptic curve
+	CurveP521 CurveID = "CurveP521"
+	// X25519 represents the X25519 elliptic curve
+	CurveX25519 CurveID = "X25519"
+)
+
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// MinTLSVersion specifies the minimum TLS version that is acceptable.
+	// If not specified, defaults to "1.2" for backward compatibility.
+	// +optional
+	MinTLSVersion TLSVersion `json:"minTLSVersion,omitempty"`
+
+	// CipherSuites specifies the list of enabled TLS cipher suites.
+	// If not specified, a secure default list will be used.
+	// The available cipher suites are defined in Go's crypto/tls package.
+	// See https://golang.org/pkg/crypto/tls/#pkg-constants for valid values.
+	// +optional
+	CipherSuites []CipherSuite `json:"cipherSuites,omitempty"`
+
+	// CurvePreferences specifies the elliptic curves that will be used in
+	// an ECDHE handshake, in preference order.
+	// If not specified, a default set of curves will be used.
+	// +optional
+	CurvePreferences []CurveID `json:"curvePreferences,omitempty"`
+}
+
+// ControllerConfigurationSpec defines the desired state of ControllerConfiguration
+type ControllerConfigurationSpec struct {
+	// ... existing fields ...
+
+	// TLS contains TLS security settings for all Kueue API servers
+	// (webhooks, metrics, controller, and visibility).
+	// +optional
+	TLS *TLSOptions `json:"tls,omitempty"`
```

  sorry this is a typo.  I'll update this to make it more clear.
  
  I added a field called tls.
  
  ```yaml
  tls:
    # Minimum TLS version: "1.2" or "1.3"
    minTLSVersion: "1.3"
    # List of enabled TLS cipher suites
    # For TLS 1.3, the following cipher suites are supported:
    cipherSuites:
      - TLS_AES_128_GCM_SHA256
      - TLS_AES_256_GCM_SHA384
      - TLS_CHACHA20_POLY1305_SHA256
    # Elliptic curves for ECDHE key exchange
    curvePreferences:
      - X25519
      - CurveP256
      - CurveP384
  
  ```

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-12-16T21:03:58Z

- **keps/8190-tls-security-profiles/README.md:172** by [@kannon92](https://github.com/kannon92) — 2025-12-16T21:03:58Z
```diff
@@ -0,0 +1,424 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version, cipher suites, and elliptic curve preferences
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Allow configuration of elliptic curve preferences for key exchange
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+3. Elliptic curve preferences (for ECDHE key exchange)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites and elliptic curves approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_RSA_WITH_AES_128_CBC_SHA;TLS_RSA_WITH_AES_256_CBC_SHA;...
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSRSAWithAES128CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSRSAWithAES256CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	// ... additional cipher suites
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
```

  I added this as an option in alternatives.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-12-16T21:23:02Z

- **keps/8190-tls-security-profiles/README.md:169** by [@kannon92](https://github.com/kannon92) — 2025-12-16T21:23:02Z
```diff
@@ -0,0 +1,424 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version, cipher suites, and elliptic curve preferences
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Allow configuration of elliptic curve preferences for key exchange
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+3. Elliptic curve preferences (for ECDHE key exchange)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites and elliptic curves approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_RSA_WITH_AES_128_CBC_SHA;TLS_RSA_WITH_AES_256_CBC_SHA;...
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSRSAWithAES128CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSRSAWithAES256CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	// ... additional cipher suites
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
+)
+
+// CurveID represents an elliptic curve identifier
+// +kubebuilder:validation:Enum=CurveP256;CurveP384;CurveP521;X25519
+type CurveID string
+
+const (
+	// CurveP256 represents the P-256 elliptic curve
+	CurveP256 CurveID = "CurveP256"
+	// CurveP384 represents the P-384 elliptic curve
+	CurveP384 CurveID = "CurveP384"
+	// CurveP521 represents the P-521 elliptic curve
+	CurveP521 CurveID = "CurveP521"
+	// X25519 represents the X25519 elliptic curve
+	CurveX25519 CurveID = "X25519"
+)
+
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// MinTLSVersion specifies the minimum TLS version that is acceptable.
+	// If not specified, defaults to "1.2" for backward compatibility.
+	// +optional
+	MinTLSVersion TLSVersion `json:"minTLSVersion,omitempty"`
+
+	// CipherSuites specifies the list of enabled TLS cipher suites.
+	// If not specified, a secure default list will be used.
+	// The available cipher suites are defined in Go's crypto/tls package.
+	// See https://golang.org/pkg/crypto/tls/#pkg-constants for valid values.
+	// +optional
+	CipherSuites []CipherSuite `json:"cipherSuites,omitempty"`
+
+	// CurvePreferences specifies the elliptic curves that will be used in
+	// an ECDHE handshake, in preference order.
+	// If not specified, a default set of curves will be used.
+	// +optional
+	CurvePreferences []CurveID `json:"curvePreferences,omitempty"`
+}
+
+// ControllerConfigurationSpec defines the desired state of ControllerConfiguration
+type ControllerConfigurationSpec struct {
+	// ... existing fields ...
+
+	// TLS contains TLS security settings for all Kueue API servers
+	// (webhooks, metrics, controller, and visibility).
+	// +optional
+	TLS *TLSOptions `json:"tls,omitempty"`
```

  Updated this.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-12-16T21:23:26Z

- **keps/8190-tls-security-profiles/kep.yaml:10** by [@kannon92](https://github.com/kannon92) — 2025-12-16T21:23:26Z
```diff
@@ -0,0 +1,25 @@
+title: TLS Security Profile Support
+kep-number: 8190
+authors:
+  - "@kannon92"
+status: implementable 
+creation-date: 2025-12-11
+reviewers:
+  - TBD
+approvers:
+  - TBD
```

  Thank you!
  
  I added you as approver and @kshalot as a reviewer.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-12-16T21:24:37Z

- **keps/8190-tls-security-profiles/README.md:187** by [@kannon92](https://github.com/kannon92) — 2025-12-16T21:24:37Z
```diff
@@ -0,0 +1,424 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version, cipher suites, and elliptic curve preferences
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Allow configuration of elliptic curve preferences for key exchange
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+3. Elliptic curve preferences (for ECDHE key exchange)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites and elliptic curves approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_RSA_WITH_AES_128_CBC_SHA;TLS_RSA_WITH_AES_256_CBC_SHA;...
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSRSAWithAES128CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSRSAWithAES256CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	// ... additional cipher suites
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
+)
+
+// CurveID represents an elliptic curve identifier
+// +kubebuilder:validation:Enum=CurveP256;CurveP384;CurveP521;X25519
+type CurveID string
+
+const (
+	// CurveP256 represents the P-256 elliptic curve
+	CurveP256 CurveID = "CurveP256"
+	// CurveP384 represents the P-384 elliptic curve
+	CurveP384 CurveID = "CurveP384"
+	// CurveP521 represents the P-521 elliptic curve
+	CurveP521 CurveID = "CurveP521"
+	// X25519 represents the X25519 elliptic curve
+	CurveX25519 CurveID = "X25519"
```

  https://github.com/golang/go/blob/release-branch.go1.25/src/crypto/tls/common.go#L145
  
  If there are new options we can add new enum fields. They should only change when we update golang so I don't think this is too burdensome.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-17T05:45:39Z

- **keps/8190-tls-security-profiles/README.md:187** by [@mimowo](https://github.com/mimowo) — 2025-12-17T05:45:39Z
```diff
@@ -0,0 +1,424 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version, cipher suites, and elliptic curve preferences
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Allow configuration of elliptic curve preferences for key exchange
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+3. Elliptic curve preferences (for ECDHE key exchange)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites and elliptic curves approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_RSA_WITH_AES_128_CBC_SHA;TLS_RSA_WITH_AES_256_CBC_SHA;...
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSRSAWithAES128CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSRSAWithAES256CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	// ... additional cipher suites
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
+)
+
+// CurveID represents an elliptic curve identifier
+// +kubebuilder:validation:Enum=CurveP256;CurveP384;CurveP521;X25519
+type CurveID string
+
+const (
+	// CurveP256 represents the P-256 elliptic curve
+	CurveP256 CurveID = "CurveP256"
+	// CurveP384 represents the P-384 elliptic curve
+	CurveP384 CurveID = "CurveP384"
+	// CurveP521 represents the P-521 elliptic curve
+	CurveP521 CurveID = "CurveP521"
+	// X25519 represents the X25519 elliptic curve
+	CurveX25519 CurveID = "X25519"
```

  Will we need to re-declare the constants in our code, or we could use the exposed constants?
  
  > They should only change when we update golang so I don't think this is too burdensome.
  
  Yes, but we may forget to do so, making sure we remember is another step in the process. So, it would be better avoiding listing of all options in our code (if feasible).

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-12-17T11:53:55Z

- **keps/8190-tls-security-profiles/README.md:187** by [@kannon92](https://github.com/kannon92) — 2025-12-17T11:53:55Z
```diff
@@ -0,0 +1,424 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version, cipher suites, and elliptic curve preferences
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Allow configuration of elliptic curve preferences for key exchange
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+3. Elliptic curve preferences (for ECDHE key exchange)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites and elliptic curves approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_RSA_WITH_AES_128_CBC_SHA;TLS_RSA_WITH_AES_256_CBC_SHA;...
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSRSAWithAES128CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSRSAWithAES256CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	// ... additional cipher suites
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
+)
+
+// CurveID represents an elliptic curve identifier
+// +kubebuilder:validation:Enum=CurveP256;CurveP384;CurveP521;X25519
+type CurveID string
+
+const (
+	// CurveP256 represents the P-256 elliptic curve
+	CurveP256 CurveID = "CurveP256"
+	// CurveP384 represents the P-384 elliptic curve
+	CurveP384 CurveID = "CurveP384"
+	// CurveP521 represents the P-521 elliptic curve
+	CurveP521 CurveID = "CurveP521"
+	// X25519 represents the X25519 elliptic curve
+	CurveX25519 CurveID = "X25519"
```

  So we could with the alternative and maybe just skip validation of this and let the server fail if someone specifies a wrong value.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-07T11:39:50Z

- **keps/8190-tls-security-profiles/README.md:172** by [@kshalot](https://github.com/kshalot) — 2026-01-07T11:39:50Z
```diff
@@ -0,0 +1,424 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version, cipher suites, and elliptic curve preferences
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Allow configuration of elliptic curve preferences for key exchange
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+3. Elliptic curve preferences (for ECDHE key exchange)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites and elliptic curves approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_RSA_WITH_AES_128_CBC_SHA;TLS_RSA_WITH_AES_256_CBC_SHA;...
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSRSAWithAES128CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSRSAWithAES256CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	// ... additional cipher suites
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
```

  There's also some opinionatedness in the Go stl. For example, for TLS 1.3 there are [five "official" cipher suites](https://datatracker.ietf.org/doc/html/rfc8446#appendix-B.4) but Go supports three (with an ongoing discussion to add support for the CCM ones at https://github.com/golang/go/issues/27484).
  
  One thing that I noticed is that our enum lists some ciphers which are listed as "insecure" in the Go crypto library. For example `TLS_RSA_WITH_AES_128_GCM_SHA256` is [here](https://github.com/golang/go/blob/d1d0fc7a97539206e33f04bac935f6450597137c/src/crypto/tls/cipher_suites.go#L89) in the `InsecureCipherSuites` list.
  
  @kannon92 Do you remember what was the source of the list of ciphers on our end? The 1.3 ones are the same, but we have the four extra `TLS_RSA_WITH...` in our enum for 1.2.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-07T20:51:40Z

- **keps/8190-tls-security-profiles/README.md:172** by [@kannon92](https://github.com/kannon92) — 2026-01-07T20:51:40Z
```diff
@@ -0,0 +1,424 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version, cipher suites, and elliptic curve preferences
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Allow configuration of elliptic curve preferences for key exchange
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+3. Elliptic curve preferences (for ECDHE key exchange)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites and elliptic curves approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_RSA_WITH_AES_128_CBC_SHA;TLS_RSA_WITH_AES_256_CBC_SHA;...
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSRSAWithAES128CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSRSAWithAES256CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	// ... additional cipher suites
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
```

  Yea for TLS 1.3 there is actually no configuration allowed AFAIK.
  
  The issue I got from the field is that there are still lots of folks that use their own ciphersuites so I think 1.2 configuration is still important.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-08T08:57:20Z

- **keps/8190-tls-security-profiles/README.md:172** by [@kshalot](https://github.com/kshalot) — 2026-01-08T08:57:20Z
```diff
@@ -0,0 +1,424 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version, cipher suites, and elliptic curve preferences
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Allow configuration of elliptic curve preferences for key exchange
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+3. Elliptic curve preferences (for ECDHE key exchange)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites and elliptic curves approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_RSA_WITH_AES_128_CBC_SHA;TLS_RSA_WITH_AES_256_CBC_SHA;...
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSRSAWithAES128CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSRSAWithAES256CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	// ... additional cipher suites
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
```

  What about the 4 insecure ciphers I mentioned though? Do we want to support them in Kueue? If so, do we want to add all the rest of the TLS 1.2 cipher suites that are insecure, but listed here https://pkg.go.dev/crypto/tls#pkg-constants.
  
  Also - the kubelet configuration [shows a warning when an insecure cipher suite is used](https://github.com/kubernetes/kubernetes/blob/97bd924010a39b71d3fd802e4284418c8b79545c/cmd/kubelet/app/server.go#L1144). Something to consider here as well maybe?

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-08T11:50:11Z

- **keps/8190-tls-security-profiles/README.md:172** by [@kshalot](https://github.com/kshalot) — 2026-01-08T11:50:10Z
```diff
@@ -0,0 +1,424 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version, cipher suites, and elliptic curve preferences
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Allow configuration of elliptic curve preferences for key exchange
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+3. Elliptic curve preferences (for ECDHE key exchange)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites and elliptic curves approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_RSA_WITH_AES_128_CBC_SHA;TLS_RSA_WITH_AES_256_CBC_SHA;...
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSRSAWithAES128CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSRSAWithAES256CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	// ... additional cipher suites
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
```

  Yeah, the cipher suite configuration is ignored for 1.3. Which makes me think this should probably be clear to the user via a comment. For example, someone might try to disable `TLS_CHACHA20_POLY1305_SHA256` for TLS 1.3 since it's not NIST approved.
  
  Also, if I'd like to set minVersion to 1.2 and limit the allowed cipher suites for 1.2, it's not clear from the API whether I should also include the 1.3 cipher suites, i.e. specify:
  ```
  [<allowlisted_tls_1.2_cipher_suites>, <all_tls_1.3_cipher_suites>]
  ```
  or just
  ```
  [<allowlisted_tls_1.2_cipher_suites>]
  ```
  (the result will be the same though).
  
  EDIT: [Here's](https://github.com/kubernetes/kubernetes/blob/97bd924010a39b71d3fd802e4284418c8b79545c/cmd/kubelet/app/server.go#L1155-L1159) how the kubelet seems to handle it.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-08T11:57:25Z

- **keps/8190-tls-security-profiles/README.md:402** by [@kshalot](https://github.com/kshalot) — 2026-01-08T11:57:25Z
```diff
@@ -0,0 +1,436 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_RSA_WITH_AES_128_CBC_SHA;TLS_RSA_WITH_AES_256_CBC_SHA;TLS_RSA_WITH_AES_128_GCM_SHA256;TLS_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSRSAWithAES128CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSRSAWithAES256CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSRSAWithAES128GCMSHA256               CipherSuite = "TLS_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSRSAWithAES256GCMSHA384               CipherSuite = "TLS_RSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES256CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES128CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES256CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithCHACHA20POLY1305SHA256   CipherSuite = "TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256"
+	CipherSuiteTLSECDHEECDSAWithCHACHA20POLY1305SHA256 CipherSuite = "TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256"
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
+)
+
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// MinTLSVersion specifies the minimum TLS version that is acceptable.
+	// If not specified, defaults to "1.2" for backward compatibility.
+	// +optional
+	MinTLSVersion TLSVersion `json:"minTLSVersion,omitempty"`
+
+	// CipherSuites specifies the list of enabled TLS cipher suites.
+	// If not specified, a secure default list will be used.
+	// The available cipher suites are defined in Go's crypto/tls package.
+	// See https://golang.org/pkg/crypto/tls/#pkg-constants for valid values.
+	// +optional
+	CipherSuites []CipherSuite `json:"cipherSuites,omitempty"`
+}
+
+// Configuration defines the desired state of Configuration
+type Configuration struct {
+	// ... existing fields ...
+
+	// TLS contains TLS security settings for all Kueue API servers
+	// (webhooks, metrics, controller, and visibility).
+	// +optional
+	TLS *TLSOptions `json:"tls,omitempty"`
+}
+```
+
+### Supported Servers
+
+The TLS configuration will be applied to the following Kueue servers:
+
+1. **Webhook Server**: Admission webhook endpoints for validating and mutating resources
+2. **Metrics Server**: Prometheus metrics endpoint (when secure metrics are enabled)
+3. **Visibility Server**: API server for workload visibility queries
+4. **Health Probe Server** (if applicable): Health check endpoints
+
+### Default Values
+
+To maintain backward compatibility and provide secure defaults:
+
+- **MinTLSVersion**: TLSVersion12 (value: "1.2")
+  - Rationale: TLS 1.2 is widely supported and required by most compliance frameworks
+  - TLS 1.0 and 1.1 are deprecated and will not be supported
+  - Valid enum values: TLSVersion12, TLSVersion13
+
+- **CipherSuites**: If not specified, Go's default secure cipher suite selection will be used
+  - For TLS 1.2, recommended cipher suite enums include:
+    - CipherSuiteTLSECDHERSAWithAES128GCMSHA256
+    - CipherSuiteTLSECDHERSAWithAES256GCMSHA384
+    - CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256
+    - CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384
+  - For TLS 1.3, cipher suites are not configurable (Go uses the built-in secure set)
+  - All available cipher suite enums are defined in the configuration_types.go file
+
+### Implementation Approach
+
+The implementation will follow these steps:
+
+1. **Configuration Loading**: Parse TLS options from the controller configuration
+2. **TLS Config Construction**: Build a `tls.Config` object with the specified options
+3. **Server Configuration**: Apply the TLS config to all relevant servers using `TLSOpts`
+
+Example implementation for applying TLS configuration:
+
+```golang
+func buildTLSConfig(cfg *config.TLSOptions) ([]func(*tls.Config), error) {
+	if cfg == nil {
+		return nil, nil
+	}
+
+	var tlsOpts []func(*tls.Config)
+
+	tlsOpts = append(tlsOpts, func(c *tls.Config) {
+		// Set minimum TLS version using enum
+		switch cfg.MinTLSVersion {
+		case config.TLSVersion12, "":
+			c.MinVersion = tls.VersionTLS12
+		case config.TLSVersion13:
+			c.MinVersion = tls.VersionTLS13
+		}
+
+		// Set cipher suites using enum values
+		if len(cfg.CipherSuites) > 0 {
+			c.CipherSuites = convertCipherSuites(cfg.CipherSuites)
+		}
+	})
+
+	return tlsOpts, nil
+}
+
+// Apply to webhook server
+webhookServer := webhook.NewServer(webhook.Options{
+	TLSOpts: tlsOpts,
+})
+
+// Apply to metrics server
+metricsServerOptions := metricsserver.Options{
+	TLSOpts: tlsOpts,
+}
+```
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+- Create test utilities for verifying TLS settings on running servers
+
+#### Unit Tests
+
+The following packages will have unit test coverage:
+
+- `pkg/controller/core`: Configuration loading
+  - Test TLS configurations are loaded correctly
+  - Test default values are applied correctly
+
+- `cmd/kueue`: TLS config application to servers
+  - Test TLS config is correctly applied to webhook server
+  - Test TLS config is correctly applied to metrics server
+  - Test TLS config is correctly applied to visibility server
+
+Coverage target: >80% for new TLS configuration code
+
+#### Integration tests
+
+Integration tests will verify:
+
+1. **TLS Version Enforcement**:
+   - Deploy Kueue with MinTLSVersion=1.3
+   - Verify TLS 1.2 connections are rejected
+   - Verify TLS 1.3 connections are accepted
+
+2. **Cipher Suite Restriction**:
+   - Deploy Kueue with specific cipher suites
+   - Verify only allowed cipher suites can establish connections
+   - Verify denied cipher suites are rejected
+
+3. **Backward Compatibility**:
+   - Deploy Kueue without TLS configuration
+   - Verify all servers start successfully with default settings
+   - Verify clients can connect using default TLS settings
+
+4. **Multi-Server Application**:
+   - Deploy Kueue with custom TLS settings
+   - Verify webhook server uses configured TLS settings
+   - Verify metrics server uses configured TLS settings
+   - Verify visibility server uses configured TLS settings
+
+### Graduation Criteria
+
+This feature can graduate directly to stable as it is:
+- An opt-in configuration feature
+- Uses well-established Go standard library APIs
+- Maintains backward compatibility through defaults
+- Follows existing patterns from KEP-4377 (metrics TLS)
+
+The feature will be considered stable when:
+- All unit and integration tests pass
+- Documentation is complete with examples and best practices
+- At least one release cycle of user feedback has been collected
+- No critical bugs or security issues have been identified
+
+## Implementation History
+
+- 2025-12-11: KEP proposed based on issues #8190 and OCPKUEUE-450
+- TBD: KEP approved
+- TBD: Implementation merged
+- TBD: Released in Kueue vX.Y
+
+## Drawbacks
+
+1. **Configuration Complexity**: Adds more configuration options that administrators need to understand
+   - Mitigation: Provide clear documentation and examples
+
+2. **Testing Burden**: Requires testing various TLS configuration combinations
+   - Mitigation: Focus on common scenarios and provide validation tools
+
+3. **Troubleshooting Complexity**: TLS issues can be difficult to diagnose
+   - Mitigation: Add comprehensive logging and error messages
+
+4. **Maintenance**: TLS standards evolve, requiring updates to documentation and recommendations
+   - Mitigation: Document the configuration in a way that's easy to update
+
+## Alternatives
+
+### Server-Specific Configuration
+
+Instead of applying TLS settings globally, we could allow per-server configuration:
+
+```golang
+type TLSOptions struct {
+	Webhooks   *TLSProfile `json:"webhooks,omitempty"`
+	Metrics    *TLSProfile `json:"metrics,omitempty"`
+	Visibility *TLSProfile `json:"visibility,omitempty"`
+}
+```
+
+**Rejected because**:
+- Adds unnecessary complexity for most use cases
+- Inconsistent TLS policies across servers could create security gaps
+- More difficult to validate and test
+- Most organizations want uniform TLS policies across all endpoints
+
+### Environment Variables
+
+We could expose TLS settings through environment variables instead of the configuration API:
+
+```
+KUEUE_MIN_TLS_VERSION=1.3
+KUEUE_TLS_CIPHER_SUITES=TLS_AES_128_GCM_SHA256,TLS_AES_256_GCM_SHA384
+```
+
+**Rejected because**:
+- Less discoverable than API-based configuration
+- Harder to validate at deployment time
+- Inconsistent with Kueue's configuration patterns
+- Environment variables are less suitable for complex list values
+- Cannot be managed through GitOps workflows as easily
+
+### String based api
```

  One thing to mention about this approach is that it seems to be used in the [`KubeletConfiguration` API](https://github.com/kubernetes/kubernetes/blob/97bd924010a39b71d3fd802e4284418c8b79545c/pkg/kubelet/apis/config/types.go#L130-L136) in core.
  
  There's also a [`InitializeTLS`](https://github.com/kubernetes/kubernetes/blob/97bd924010a39b71d3fd802e4284418c8b79545c/cmd/kubelet/app/server.go#L1102-L1182) function to take some inspiration from maybe? (handling insecure ciphers etc.)

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-08T15:56:59Z

- **keps/8190-tls-security-profiles/README.md:402** by [@kannon92](https://github.com/kannon92) — 2026-01-08T15:56:59Z
```diff
@@ -0,0 +1,436 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_RSA_WITH_AES_128_CBC_SHA;TLS_RSA_WITH_AES_256_CBC_SHA;TLS_RSA_WITH_AES_128_GCM_SHA256;TLS_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSRSAWithAES128CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSRSAWithAES256CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSRSAWithAES128GCMSHA256               CipherSuite = "TLS_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSRSAWithAES256GCMSHA384               CipherSuite = "TLS_RSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES256CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES128CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES256CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithCHACHA20POLY1305SHA256   CipherSuite = "TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256"
+	CipherSuiteTLSECDHEECDSAWithCHACHA20POLY1305SHA256 CipherSuite = "TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256"
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
+)
+
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// MinTLSVersion specifies the minimum TLS version that is acceptable.
+	// If not specified, defaults to "1.2" for backward compatibility.
+	// +optional
+	MinTLSVersion TLSVersion `json:"minTLSVersion,omitempty"`
+
+	// CipherSuites specifies the list of enabled TLS cipher suites.
+	// If not specified, a secure default list will be used.
+	// The available cipher suites are defined in Go's crypto/tls package.
+	// See https://golang.org/pkg/crypto/tls/#pkg-constants for valid values.
+	// +optional
+	CipherSuites []CipherSuite `json:"cipherSuites,omitempty"`
+}
+
+// Configuration defines the desired state of Configuration
+type Configuration struct {
+	// ... existing fields ...
+
+	// TLS contains TLS security settings for all Kueue API servers
+	// (webhooks, metrics, controller, and visibility).
+	// +optional
+	TLS *TLSOptions `json:"tls,omitempty"`
+}
+```
+
+### Supported Servers
+
+The TLS configuration will be applied to the following Kueue servers:
+
+1. **Webhook Server**: Admission webhook endpoints for validating and mutating resources
+2. **Metrics Server**: Prometheus metrics endpoint (when secure metrics are enabled)
+3. **Visibility Server**: API server for workload visibility queries
+4. **Health Probe Server** (if applicable): Health check endpoints
+
+### Default Values
+
+To maintain backward compatibility and provide secure defaults:
+
+- **MinTLSVersion**: TLSVersion12 (value: "1.2")
+  - Rationale: TLS 1.2 is widely supported and required by most compliance frameworks
+  - TLS 1.0 and 1.1 are deprecated and will not be supported
+  - Valid enum values: TLSVersion12, TLSVersion13
+
+- **CipherSuites**: If not specified, Go's default secure cipher suite selection will be used
+  - For TLS 1.2, recommended cipher suite enums include:
+    - CipherSuiteTLSECDHERSAWithAES128GCMSHA256
+    - CipherSuiteTLSECDHERSAWithAES256GCMSHA384
+    - CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256
+    - CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384
+  - For TLS 1.3, cipher suites are not configurable (Go uses the built-in secure set)
+  - All available cipher suite enums are defined in the configuration_types.go file
+
+### Implementation Approach
+
+The implementation will follow these steps:
+
+1. **Configuration Loading**: Parse TLS options from the controller configuration
+2. **TLS Config Construction**: Build a `tls.Config` object with the specified options
+3. **Server Configuration**: Apply the TLS config to all relevant servers using `TLSOpts`
+
+Example implementation for applying TLS configuration:
+
+```golang
+func buildTLSConfig(cfg *config.TLSOptions) ([]func(*tls.Config), error) {
+	if cfg == nil {
+		return nil, nil
+	}
+
+	var tlsOpts []func(*tls.Config)
+
+	tlsOpts = append(tlsOpts, func(c *tls.Config) {
+		// Set minimum TLS version using enum
+		switch cfg.MinTLSVersion {
+		case config.TLSVersion12, "":
+			c.MinVersion = tls.VersionTLS12
+		case config.TLSVersion13:
+			c.MinVersion = tls.VersionTLS13
+		}
+
+		// Set cipher suites using enum values
+		if len(cfg.CipherSuites) > 0 {
+			c.CipherSuites = convertCipherSuites(cfg.CipherSuites)
+		}
+	})
+
+	return tlsOpts, nil
+}
+
+// Apply to webhook server
+webhookServer := webhook.NewServer(webhook.Options{
+	TLSOpts: tlsOpts,
+})
+
+// Apply to metrics server
+metricsServerOptions := metricsserver.Options{
+	TLSOpts: tlsOpts,
+}
+```
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+- Create test utilities for verifying TLS settings on running servers
+
+#### Unit Tests
+
+The following packages will have unit test coverage:
+
+- `pkg/controller/core`: Configuration loading
+  - Test TLS configurations are loaded correctly
+  - Test default values are applied correctly
+
+- `cmd/kueue`: TLS config application to servers
+  - Test TLS config is correctly applied to webhook server
+  - Test TLS config is correctly applied to metrics server
+  - Test TLS config is correctly applied to visibility server
+
+Coverage target: >80% for new TLS configuration code
+
+#### Integration tests
+
+Integration tests will verify:
+
+1. **TLS Version Enforcement**:
+   - Deploy Kueue with MinTLSVersion=1.3
+   - Verify TLS 1.2 connections are rejected
+   - Verify TLS 1.3 connections are accepted
+
+2. **Cipher Suite Restriction**:
+   - Deploy Kueue with specific cipher suites
+   - Verify only allowed cipher suites can establish connections
+   - Verify denied cipher suites are rejected
+
+3. **Backward Compatibility**:
+   - Deploy Kueue without TLS configuration
+   - Verify all servers start successfully with default settings
+   - Verify clients can connect using default TLS settings
+
+4. **Multi-Server Application**:
+   - Deploy Kueue with custom TLS settings
+   - Verify webhook server uses configured TLS settings
+   - Verify metrics server uses configured TLS settings
+   - Verify visibility server uses configured TLS settings
+
+### Graduation Criteria
+
+This feature can graduate directly to stable as it is:
+- An opt-in configuration feature
+- Uses well-established Go standard library APIs
+- Maintains backward compatibility through defaults
+- Follows existing patterns from KEP-4377 (metrics TLS)
+
+The feature will be considered stable when:
+- All unit and integration tests pass
+- Documentation is complete with examples and best practices
+- At least one release cycle of user feedback has been collected
+- No critical bugs or security issues have been identified
+
+## Implementation History
+
+- 2025-12-11: KEP proposed based on issues #8190 and OCPKUEUE-450
+- TBD: KEP approved
+- TBD: Implementation merged
+- TBD: Released in Kueue vX.Y
+
+## Drawbacks
+
+1. **Configuration Complexity**: Adds more configuration options that administrators need to understand
+   - Mitigation: Provide clear documentation and examples
+
+2. **Testing Burden**: Requires testing various TLS configuration combinations
+   - Mitigation: Focus on common scenarios and provide validation tools
+
+3. **Troubleshooting Complexity**: TLS issues can be difficult to diagnose
+   - Mitigation: Add comprehensive logging and error messages
+
+4. **Maintenance**: TLS standards evolve, requiring updates to documentation and recommendations
+   - Mitigation: Document the configuration in a way that's easy to update
+
+## Alternatives
+
+### Server-Specific Configuration
+
+Instead of applying TLS settings globally, we could allow per-server configuration:
+
+```golang
+type TLSOptions struct {
+	Webhooks   *TLSProfile `json:"webhooks,omitempty"`
+	Metrics    *TLSProfile `json:"metrics,omitempty"`
+	Visibility *TLSProfile `json:"visibility,omitempty"`
+}
+```
+
+**Rejected because**:
+- Adds unnecessary complexity for most use cases
+- Inconsistent TLS policies across servers could create security gaps
+- More difficult to validate and test
+- Most organizations want uniform TLS policies across all endpoints
+
+### Environment Variables
+
+We could expose TLS settings through environment variables instead of the configuration API:
+
+```
+KUEUE_MIN_TLS_VERSION=1.3
+KUEUE_TLS_CIPHER_SUITES=TLS_AES_128_GCM_SHA256,TLS_AES_256_GCM_SHA384
+```
+
+**Rejected because**:
+- Less discoverable than API-based configuration
+- Harder to validate at deployment time
+- Inconsistent with Kueue's configuration patterns
+- Environment variables are less suitable for complex list values
+- Cannot be managed through GitOps workflows as easily
+
+### String based api
```

  Yea what option do you think we should do?
  
  Kubelet/APIServer have a lot of validation logic to make sure that people pass the right ciphers to the server.
  
  I was thinking it would be better to have it at the API level and avoid the validation.
  
  But I am open to whatever.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-08T17:17:48Z

- **keps/8190-tls-security-profiles/README.md:172** by [@kannon92](https://github.com/kannon92) — 2026-01-08T17:17:48Z
```diff
@@ -0,0 +1,424 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version, cipher suites, and elliptic curve preferences
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Allow configuration of elliptic curve preferences for key exchange
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+3. Elliptic curve preferences (for ECDHE key exchange)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites and elliptic curves approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_RSA_WITH_AES_128_CBC_SHA;TLS_RSA_WITH_AES_256_CBC_SHA;...
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSRSAWithAES128CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSRSAWithAES256CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	// ... additional cipher suites
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
```

  I will drop insurecure ciphers.
  
  I may have grabbed the wrong enums so thanks for reviewing this in detail.
  
  I was going to update our API to just allow:
  
  https://cs.opensource.google/go/go/+/refs/tags/go1.25.5:src/crypto/tls/cipher_suites.go;l=56

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-08T17:22:41Z

- **keps/8190-tls-security-profiles/README.md:172** by [@kannon92](https://github.com/kannon92) — 2026-01-08T17:22:41Z
```diff
@@ -0,0 +1,424 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version, cipher suites, and elliptic curve preferences
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Allow configuration of elliptic curve preferences for key exchange
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+3. Elliptic curve preferences (for ECDHE key exchange)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites and elliptic curves approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_RSA_WITH_AES_128_CBC_SHA;TLS_RSA_WITH_AES_256_CBC_SHA;...
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSRSAWithAES128CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSRSAWithAES256CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	// ... additional cipher suites
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
```

  I pushed up an update for this.

### Review by [@kshalot](https://github.com/kshalot) (approved) — 2026-01-08T18:21:49Z

/lgtm

One thing I'd also like to discuss further is [documenting the fact you cannot change which cipher suites are enabled for 1.3](https://github.com/kubernetes-sigs/kueue/pull/8246/changes#r2672048218), but I think this could be continued in the implementation PR.

I don't have a strong opinion on the strings/enum discussion, but I left my thoughts.

- **keps/8190-tls-security-profiles/README.md:402** by [@kshalot](https://github.com/kshalot) — 2026-01-08T18:18:43Z
```diff
@@ -0,0 +1,436 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_RSA_WITH_AES_128_CBC_SHA;TLS_RSA_WITH_AES_256_CBC_SHA;TLS_RSA_WITH_AES_128_GCM_SHA256;TLS_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSRSAWithAES128CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSRSAWithAES256CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSRSAWithAES128GCMSHA256               CipherSuite = "TLS_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSRSAWithAES256GCMSHA384               CipherSuite = "TLS_RSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES256CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES128CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES256CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithCHACHA20POLY1305SHA256   CipherSuite = "TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256"
+	CipherSuiteTLSECDHEECDSAWithCHACHA20POLY1305SHA256 CipherSuite = "TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256"
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
+)
+
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// MinTLSVersion specifies the minimum TLS version that is acceptable.
+	// If not specified, defaults to "1.2" for backward compatibility.
+	// +optional
+	MinTLSVersion TLSVersion `json:"minTLSVersion,omitempty"`
+
+	// CipherSuites specifies the list of enabled TLS cipher suites.
+	// If not specified, a secure default list will be used.
+	// The available cipher suites are defined in Go's crypto/tls package.
+	// See https://golang.org/pkg/crypto/tls/#pkg-constants for valid values.
+	// +optional
+	CipherSuites []CipherSuite `json:"cipherSuites,omitempty"`
+}
+
+// Configuration defines the desired state of Configuration
+type Configuration struct {
+	// ... existing fields ...
+
+	// TLS contains TLS security settings for all Kueue API servers
+	// (webhooks, metrics, controller, and visibility).
+	// +optional
+	TLS *TLSOptions `json:"tls,omitempty"`
+}
+```
+
+### Supported Servers
+
+The TLS configuration will be applied to the following Kueue servers:
+
+1. **Webhook Server**: Admission webhook endpoints for validating and mutating resources
+2. **Metrics Server**: Prometheus metrics endpoint (when secure metrics are enabled)
+3. **Visibility Server**: API server for workload visibility queries
+4. **Health Probe Server** (if applicable): Health check endpoints
+
+### Default Values
+
+To maintain backward compatibility and provide secure defaults:
+
+- **MinTLSVersion**: TLSVersion12 (value: "1.2")
+  - Rationale: TLS 1.2 is widely supported and required by most compliance frameworks
+  - TLS 1.0 and 1.1 are deprecated and will not be supported
+  - Valid enum values: TLSVersion12, TLSVersion13
+
+- **CipherSuites**: If not specified, Go's default secure cipher suite selection will be used
+  - For TLS 1.2, recommended cipher suite enums include:
+    - CipherSuiteTLSECDHERSAWithAES128GCMSHA256
+    - CipherSuiteTLSECDHERSAWithAES256GCMSHA384
+    - CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256
+    - CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384
+  - For TLS 1.3, cipher suites are not configurable (Go uses the built-in secure set)
+  - All available cipher suite enums are defined in the configuration_types.go file
+
+### Implementation Approach
+
+The implementation will follow these steps:
+
+1. **Configuration Loading**: Parse TLS options from the controller configuration
+2. **TLS Config Construction**: Build a `tls.Config` object with the specified options
+3. **Server Configuration**: Apply the TLS config to all relevant servers using `TLSOpts`
+
+Example implementation for applying TLS configuration:
+
+```golang
+func buildTLSConfig(cfg *config.TLSOptions) ([]func(*tls.Config), error) {
+	if cfg == nil {
+		return nil, nil
+	}
+
+	var tlsOpts []func(*tls.Config)
+
+	tlsOpts = append(tlsOpts, func(c *tls.Config) {
+		// Set minimum TLS version using enum
+		switch cfg.MinTLSVersion {
+		case config.TLSVersion12, "":
+			c.MinVersion = tls.VersionTLS12
+		case config.TLSVersion13:
+			c.MinVersion = tls.VersionTLS13
+		}
+
+		// Set cipher suites using enum values
+		if len(cfg.CipherSuites) > 0 {
+			c.CipherSuites = convertCipherSuites(cfg.CipherSuites)
+		}
+	})
+
+	return tlsOpts, nil
+}
+
+// Apply to webhook server
+webhookServer := webhook.NewServer(webhook.Options{
+	TLSOpts: tlsOpts,
+})
+
+// Apply to metrics server
+metricsServerOptions := metricsserver.Options{
+	TLSOpts: tlsOpts,
+}
+```
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+- Create test utilities for verifying TLS settings on running servers
+
+#### Unit Tests
+
+The following packages will have unit test coverage:
+
+- `pkg/controller/core`: Configuration loading
+  - Test TLS configurations are loaded correctly
+  - Test default values are applied correctly
+
+- `cmd/kueue`: TLS config application to servers
+  - Test TLS config is correctly applied to webhook server
+  - Test TLS config is correctly applied to metrics server
+  - Test TLS config is correctly applied to visibility server
+
+Coverage target: >80% for new TLS configuration code
+
+#### Integration tests
+
+Integration tests will verify:
+
+1. **TLS Version Enforcement**:
+   - Deploy Kueue with MinTLSVersion=1.3
+   - Verify TLS 1.2 connections are rejected
+   - Verify TLS 1.3 connections are accepted
+
+2. **Cipher Suite Restriction**:
+   - Deploy Kueue with specific cipher suites
+   - Verify only allowed cipher suites can establish connections
+   - Verify denied cipher suites are rejected
+
+3. **Backward Compatibility**:
+   - Deploy Kueue without TLS configuration
+   - Verify all servers start successfully with default settings
+   - Verify clients can connect using default TLS settings
+
+4. **Multi-Server Application**:
+   - Deploy Kueue with custom TLS settings
+   - Verify webhook server uses configured TLS settings
+   - Verify metrics server uses configured TLS settings
+   - Verify visibility server uses configured TLS settings
+
+### Graduation Criteria
+
+This feature can graduate directly to stable as it is:
+- An opt-in configuration feature
+- Uses well-established Go standard library APIs
+- Maintains backward compatibility through defaults
+- Follows existing patterns from KEP-4377 (metrics TLS)
+
+The feature will be considered stable when:
+- All unit and integration tests pass
+- Documentation is complete with examples and best practices
+- At least one release cycle of user feedback has been collected
+- No critical bugs or security issues have been identified
+
+## Implementation History
+
+- 2025-12-11: KEP proposed based on issues #8190 and OCPKUEUE-450
+- TBD: KEP approved
+- TBD: Implementation merged
+- TBD: Released in Kueue vX.Y
+
+## Drawbacks
+
+1. **Configuration Complexity**: Adds more configuration options that administrators need to understand
+   - Mitigation: Provide clear documentation and examples
+
+2. **Testing Burden**: Requires testing various TLS configuration combinations
+   - Mitigation: Focus on common scenarios and provide validation tools
+
+3. **Troubleshooting Complexity**: TLS issues can be difficult to diagnose
+   - Mitigation: Add comprehensive logging and error messages
+
+4. **Maintenance**: TLS standards evolve, requiring updates to documentation and recommendations
+   - Mitigation: Document the configuration in a way that's easy to update
+
+## Alternatives
+
+### Server-Specific Configuration
+
+Instead of applying TLS settings globally, we could allow per-server configuration:
+
+```golang
+type TLSOptions struct {
+	Webhooks   *TLSProfile `json:"webhooks,omitempty"`
+	Metrics    *TLSProfile `json:"metrics,omitempty"`
+	Visibility *TLSProfile `json:"visibility,omitempty"`
+}
+```
+
+**Rejected because**:
+- Adds unnecessary complexity for most use cases
+- Inconsistent TLS policies across servers could create security gaps
+- More difficult to validate and test
+- Most organizations want uniform TLS policies across all endpoints
+
+### Environment Variables
+
+We could expose TLS settings through environment variables instead of the configuration API:
+
+```
+KUEUE_MIN_TLS_VERSION=1.3
+KUEUE_TLS_CIPHER_SUITES=TLS_AES_128_GCM_SHA256,TLS_AES_256_GCM_SHA384
+```
+
+**Rejected because**:
+- Less discoverable than API-based configuration
+- Harder to validate at deployment time
+- Inconsistent with Kueue's configuration patterns
+- Environment variables are less suitable for complex list values
+- Cannot be managed through GitOps workflows as easily
+
+### String based api
```

  If I was the one implementing, I'd probably lean towards string + validation since it would make the dependency on the `tls` package a bit more explicit (i.e. we rely on the `tls` package to define the allowed values), but I don't see a huge difference in practice because the set of cipher suites is not changing in practice. We also wouldn't have to decide which suites to support and just defer to [`CipherSuites`](https://cs.opensource.google/go/go/+/refs/tags/go1.25.5:src/crypto/tls/cipher_suites.go;l=56) and [`InsecureCipherSuites`](https://cs.opensource.google/go/go/+/refs/tags/go1.25.5:src/crypto/tls/cipher_suites.go;l=80). And this approach is also used in KubeletConfiguration. 
  
  One hypothetical scenario where **maybe** this would make a difference is if the Go library deems some cipher insecure. For example the CBC ones in TLS 1.2 are now "weak" ([example](https://ciphersuite.info/cs/TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA/)) even though they not "insecure" in the `tls` library, and I think are being deprecated in some environments outside of Go. So if Go follows suit and moves those ciphers to `InsecureCiphers` in the `tls` lib, we would get different behaviour based on the implementation we choose here. Although I'm not sure how likely this is to happen, I'd wager it's not very likely so this might be moot.
  
  On the other hand, couldn't we move from enum to string in a  backwards compatible way in the future? So since right now we don't foresee any more validations other than expecting specific values, the enum seems like a good place to start.
  
  TL;DR - I don't have a super strong opinion. Maybe someone with more experience designing Kueue APIs can chime in. cc @mimowo

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-09T19:30:39Z

- **keps/8190-tls-security-profiles/README.md:402** by [@kannon92](https://github.com/kannon92) — 2026-01-09T19:30:39Z
```diff
@@ -0,0 +1,436 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_RSA_WITH_AES_128_CBC_SHA;TLS_RSA_WITH_AES_256_CBC_SHA;TLS_RSA_WITH_AES_128_GCM_SHA256;TLS_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSRSAWithAES128CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSRSAWithAES256CBCSHA                  CipherSuite = "TLS_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSRSAWithAES128GCMSHA256               CipherSuite = "TLS_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSRSAWithAES256GCMSHA384               CipherSuite = "TLS_RSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES256CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES128CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES256CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithCHACHA20POLY1305SHA256   CipherSuite = "TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256"
+	CipherSuiteTLSECDHEECDSAWithCHACHA20POLY1305SHA256 CipherSuite = "TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256"
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
+)
+
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// MinTLSVersion specifies the minimum TLS version that is acceptable.
+	// If not specified, defaults to "1.2" for backward compatibility.
+	// +optional
+	MinTLSVersion TLSVersion `json:"minTLSVersion,omitempty"`
+
+	// CipherSuites specifies the list of enabled TLS cipher suites.
+	// If not specified, a secure default list will be used.
+	// The available cipher suites are defined in Go's crypto/tls package.
+	// See https://golang.org/pkg/crypto/tls/#pkg-constants for valid values.
+	// +optional
+	CipherSuites []CipherSuite `json:"cipherSuites,omitempty"`
+}
+
+// Configuration defines the desired state of Configuration
+type Configuration struct {
+	// ... existing fields ...
+
+	// TLS contains TLS security settings for all Kueue API servers
+	// (webhooks, metrics, controller, and visibility).
+	// +optional
+	TLS *TLSOptions `json:"tls,omitempty"`
+}
+```
+
+### Supported Servers
+
+The TLS configuration will be applied to the following Kueue servers:
+
+1. **Webhook Server**: Admission webhook endpoints for validating and mutating resources
+2. **Metrics Server**: Prometheus metrics endpoint (when secure metrics are enabled)
+3. **Visibility Server**: API server for workload visibility queries
+4. **Health Probe Server** (if applicable): Health check endpoints
+
+### Default Values
+
+To maintain backward compatibility and provide secure defaults:
+
+- **MinTLSVersion**: TLSVersion12 (value: "1.2")
+  - Rationale: TLS 1.2 is widely supported and required by most compliance frameworks
+  - TLS 1.0 and 1.1 are deprecated and will not be supported
+  - Valid enum values: TLSVersion12, TLSVersion13
+
+- **CipherSuites**: If not specified, Go's default secure cipher suite selection will be used
+  - For TLS 1.2, recommended cipher suite enums include:
+    - CipherSuiteTLSECDHERSAWithAES128GCMSHA256
+    - CipherSuiteTLSECDHERSAWithAES256GCMSHA384
+    - CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256
+    - CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384
+  - For TLS 1.3, cipher suites are not configurable (Go uses the built-in secure set)
+  - All available cipher suite enums are defined in the configuration_types.go file
+
+### Implementation Approach
+
+The implementation will follow these steps:
+
+1. **Configuration Loading**: Parse TLS options from the controller configuration
+2. **TLS Config Construction**: Build a `tls.Config` object with the specified options
+3. **Server Configuration**: Apply the TLS config to all relevant servers using `TLSOpts`
+
+Example implementation for applying TLS configuration:
+
+```golang
+func buildTLSConfig(cfg *config.TLSOptions) ([]func(*tls.Config), error) {
+	if cfg == nil {
+		return nil, nil
+	}
+
+	var tlsOpts []func(*tls.Config)
+
+	tlsOpts = append(tlsOpts, func(c *tls.Config) {
+		// Set minimum TLS version using enum
+		switch cfg.MinTLSVersion {
+		case config.TLSVersion12, "":
+			c.MinVersion = tls.VersionTLS12
+		case config.TLSVersion13:
+			c.MinVersion = tls.VersionTLS13
+		}
+
+		// Set cipher suites using enum values
+		if len(cfg.CipherSuites) > 0 {
+			c.CipherSuites = convertCipherSuites(cfg.CipherSuites)
+		}
+	})
+
+	return tlsOpts, nil
+}
+
+// Apply to webhook server
+webhookServer := webhook.NewServer(webhook.Options{
+	TLSOpts: tlsOpts,
+})
+
+// Apply to metrics server
+metricsServerOptions := metricsserver.Options{
+	TLSOpts: tlsOpts,
+}
+```
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+- Create test utilities for verifying TLS settings on running servers
+
+#### Unit Tests
+
+The following packages will have unit test coverage:
+
+- `pkg/controller/core`: Configuration loading
+  - Test TLS configurations are loaded correctly
+  - Test default values are applied correctly
+
+- `cmd/kueue`: TLS config application to servers
+  - Test TLS config is correctly applied to webhook server
+  - Test TLS config is correctly applied to metrics server
+  - Test TLS config is correctly applied to visibility server
+
+Coverage target: >80% for new TLS configuration code
+
+#### Integration tests
+
+Integration tests will verify:
+
+1. **TLS Version Enforcement**:
+   - Deploy Kueue with MinTLSVersion=1.3
+   - Verify TLS 1.2 connections are rejected
+   - Verify TLS 1.3 connections are accepted
+
+2. **Cipher Suite Restriction**:
+   - Deploy Kueue with specific cipher suites
+   - Verify only allowed cipher suites can establish connections
+   - Verify denied cipher suites are rejected
+
+3. **Backward Compatibility**:
+   - Deploy Kueue without TLS configuration
+   - Verify all servers start successfully with default settings
+   - Verify clients can connect using default TLS settings
+
+4. **Multi-Server Application**:
+   - Deploy Kueue with custom TLS settings
+   - Verify webhook server uses configured TLS settings
+   - Verify metrics server uses configured TLS settings
+   - Verify visibility server uses configured TLS settings
+
+### Graduation Criteria
+
+This feature can graduate directly to stable as it is:
+- An opt-in configuration feature
+- Uses well-established Go standard library APIs
+- Maintains backward compatibility through defaults
+- Follows existing patterns from KEP-4377 (metrics TLS)
+
+The feature will be considered stable when:
+- All unit and integration tests pass
+- Documentation is complete with examples and best practices
+- At least one release cycle of user feedback has been collected
+- No critical bugs or security issues have been identified
+
+## Implementation History
+
+- 2025-12-11: KEP proposed based on issues #8190 and OCPKUEUE-450
+- TBD: KEP approved
+- TBD: Implementation merged
+- TBD: Released in Kueue vX.Y
+
+## Drawbacks
+
+1. **Configuration Complexity**: Adds more configuration options that administrators need to understand
+   - Mitigation: Provide clear documentation and examples
+
+2. **Testing Burden**: Requires testing various TLS configuration combinations
+   - Mitigation: Focus on common scenarios and provide validation tools
+
+3. **Troubleshooting Complexity**: TLS issues can be difficult to diagnose
+   - Mitigation: Add comprehensive logging and error messages
+
+4. **Maintenance**: TLS standards evolve, requiring updates to documentation and recommendations
+   - Mitigation: Document the configuration in a way that's easy to update
+
+## Alternatives
+
+### Server-Specific Configuration
+
+Instead of applying TLS settings globally, we could allow per-server configuration:
+
+```golang
+type TLSOptions struct {
+	Webhooks   *TLSProfile `json:"webhooks,omitempty"`
+	Metrics    *TLSProfile `json:"metrics,omitempty"`
+	Visibility *TLSProfile `json:"visibility,omitempty"`
+}
+```
+
+**Rejected because**:
+- Adds unnecessary complexity for most use cases
+- Inconsistent TLS policies across servers could create security gaps
+- More difficult to validate and test
+- Most organizations want uniform TLS policies across all endpoints
+
+### Environment Variables
+
+We could expose TLS settings through environment variables instead of the configuration API:
+
+```
+KUEUE_MIN_TLS_VERSION=1.3
+KUEUE_TLS_CIPHER_SUITES=TLS_AES_128_GCM_SHA256,TLS_AES_256_GCM_SHA384
+```
+
+**Rejected because**:
+- Less discoverable than API-based configuration
+- Harder to validate at deployment time
+- Inconsistent with Kueue's configuration patterns
+- Environment variables are less suitable for complex list values
+- Cannot be managed through GitOps workflows as easily
+
+### String based api
```

  IMO the main reason I want the enums is that the UX is a lot better to an admin who needs to configure the server.
  
  They can check the API docs and see what values are allowed. 
  
  If we provide a string or []string they have to reverse engineer from the validation logic to know what are the correct enum values.

### Review by [@everettraven](https://github.com/everettraven) (commented) — 2026-01-12T15:40:25Z

A few general comments on the API structure.

Taking lessons from previous iterations of APIs, I think taking the discriminated union approach here will buy you better long-term flexibility and admission-time validation enforcement - especially since Go supports configuration of TLS 1.2 settings but not TLS 1.3 for now.

- **keps/8190-tls-security-profiles/README.md:156** by [@everettraven](https://github.com/everettraven) — 2026-01-12T15:22:26Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
```

  If the TLS 1.2 and 1.3 cipher suites are not compatible with each other (i.e you cannot use a tls1.2 cipher suite with tls1.3 or vice-versa) it would probably make more sense to have a type alias for each TLS version so you can have distinction between the two and tighter validation.
  
  For example:
  ```go
  // {enums for valid TLS 1.2 cipher suites goes here}
  type TLS12CipherSuite string
  ...
  // {enums for valid TLS 1.3 cipher suites goes here}
  type TLS13CipherSuite string
  ```

- **keps/8190-tls-security-profiles/README.md:182** by [@everettraven](https://github.com/everettraven) — 2026-01-12T15:35:03Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES256CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES128CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES256CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithCHACHA20POLY1305SHA256   CipherSuite = "TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256"
+	CipherSuiteTLSECDHEECDSAWithCHACHA20POLY1305SHA256 CipherSuite = "TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256"
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
+)
+
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// MinTLSVersion specifies the minimum TLS version that is acceptable.
+	// If not specified, defaults to "1.2" for backward compatibility.
+	// +optional
+	MinTLSVersion TLSVersion `json:"minTLSVersion,omitempty"`
```

  In my experience, the TLS version is what dictates what configuration can and cannot be set.
  
  In my opinion, this makes the TLS version a good union discriminator value.
  
  I think a structure like:
  ```yaml
  minTLSVersion: TLS1.2
  tls12:
    ciphers:
      - ...
  ```
  
  Would make it so that you can have better admission time validation controls for varying versions of TLS configuration that you want to support. It also makes it so you can expand the configuration options you support for each TLS version independently. This is specifically useful for end-users that want to be able to configure things on TLS1.2 that you can allow configuration for because the Go library you use supports it, and on TLS1.3 you can allow no configuration until configuration is supported.

- **keps/8190-tls-security-profiles/README.md:182** by [@everettraven](https://github.com/everettraven) — 2026-01-12T15:36:55Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES256CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES128CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES256CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithCHACHA20POLY1305SHA256   CipherSuite = "TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256"
+	CipherSuiteTLSECDHEECDSAWithCHACHA20POLY1305SHA256 CipherSuite = "TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256"
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
+)
+
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// MinTLSVersion specifies the minimum TLS version that is acceptable.
+	// If not specified, defaults to "1.2" for backward compatibility.
+	// +optional
+	MinTLSVersion TLSVersion `json:"minTLSVersion,omitempty"`
```

  Also a note, the `tls12` field name isn't my favorite due to it reading as "tls twelve" but it avoids using `.` or `_` characters in the field name - both of which are allowed AFAIK.
  
  Regardless of naming, the example should at least be effective in getting the general idea across :)

- **keps/8190-tls-security-profiles/README.md:169** by [@everettraven](https://github.com/everettraven) — 2026-01-12T15:38:17Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES256CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES128CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES256CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithCHACHA20POLY1305SHA256   CipherSuite = "TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256"
+	CipherSuiteTLSECDHEECDSAWithCHACHA20POLY1305SHA256 CipherSuite = "TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256"
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
+)
+
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// MinTLSVersion specifies the minimum TLS version that is acceptable.
+	// If not specified, defaults to "1.2" for backward compatibility.
+	// +optional
+	MinTLSVersion TLSVersion `json:"minTLSVersion,omitempty"`
+
+	// CipherSuites specifies the list of enabled TLS cipher suites.
+	// If not specified, a secure default list will be used.
+	// The available cipher suites are defined in Go's crypto/tls package.
+	// See https://golang.org/pkg/crypto/tls/#pkg-constants for valid values.
+	// +optional
+	CipherSuites []CipherSuite `json:"cipherSuites,omitempty"`
+}
+
+// Configuration defines the desired state of Configuration
+type Configuration struct {
+	// ... existing fields ...
+
+	// TLS contains TLS security settings for all Kueue API servers
+	// (webhooks, metrics, controller, and visibility).
+	// +optional
+	TLS *TLSOptions `json:"tls,omitempty"`
```

  Would setting `tls: {}` be semantically any different than just not specifying the `tls` field?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-12T15:50:36Z

- **keps/8190-tls-security-profiles/README.md:407** by [@mimowo](https://github.com/mimowo) — 2026-01-12T15:50:36Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES256CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES128CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES256CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithCHACHA20POLY1305SHA256   CipherSuite = "TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256"
+	CipherSuiteTLSECDHEECDSAWithCHACHA20POLY1305SHA256 CipherSuite = "TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256"
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
+)
+
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// MinTLSVersion specifies the minimum TLS version that is acceptable.
+	// If not specified, defaults to "1.2" for backward compatibility.
+	// +optional
+	MinTLSVersion TLSVersion `json:"minTLSVersion,omitempty"`
+
+	// CipherSuites specifies the list of enabled TLS cipher suites.
+	// If not specified, a secure default list will be used.
+	// The available cipher suites are defined in Go's crypto/tls package.
+	// See https://golang.org/pkg/crypto/tls/#pkg-constants for valid values.
+	// +optional
+	CipherSuites []CipherSuite `json:"cipherSuites,omitempty"`
+}
+
+// Configuration defines the desired state of Configuration
+type Configuration struct {
+	// ... existing fields ...
+
+	// TLS contains TLS security settings for all Kueue API servers
+	// (webhooks, metrics, controller, and visibility).
+	// +optional
+	TLS *TLSOptions `json:"tls,omitempty"`
+}
+```
+
+We will take only the "secure" TLS options according to golang.
+The list is taken from [golang](https://cs.opensource.google/go/go/+/refs/tags/go1.25.5:src/crypto/tls/cipher_suites.go;l=56).
+
+### Supported Servers
+
+The TLS configuration will be applied to the following Kueue servers:
+
+1. **Webhook Server**: Admission webhook endpoints for validating and mutating resources
+2. **Metrics Server**: Prometheus metrics endpoint (when secure metrics are enabled)
+3. **Visibility Server**: API server for workload visibility queries
+4. **Health Probe Server** (if applicable): Health check endpoints
+
+### Default Values
+
+To maintain backward compatibility and provide secure defaults:
+
+- **MinTLSVersion**: TLSVersion12 (value: "1.2")
+  - Rationale: TLS 1.2 is widely supported and required by most compliance frameworks
+  - TLS 1.0 and 1.1 are deprecated and will not be supported
+  - Valid enum values: TLSVersion12, TLSVersion13
+
+- **CipherSuites**: If not specified, Go's default secure cipher suite selection will be used
+  - For TLS 1.2, recommended cipher suite enums include:
+    - CipherSuiteTLSECDHERSAWithAES128GCMSHA256
+    - CipherSuiteTLSECDHERSAWithAES256GCMSHA384
+    - CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256
+    - CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384
+  - For TLS 1.3, cipher suites are not configurable (Go uses the built-in secure set)
+  - All secure cipher suite enums are defined in the configuration_types.go file
+
+### Implementation Approach
+
+The implementation will follow these steps:
+
+1. **Configuration Loading**: Parse TLS options from the controller configuration
+2. **TLS Config Construction**: Build a `tls.Config` object with the specified options
+3. **Server Configuration**: Apply the TLS config to all relevant servers using `TLSOpts`
+
+Example implementation for applying TLS configuration:
+
+```golang
+func buildTLSConfig(cfg *config.TLSOptions) ([]func(*tls.Config), error) {
+	if cfg == nil {
+		return nil, nil
+	}
+
+	var tlsOpts []func(*tls.Config)
+
+	tlsOpts = append(tlsOpts, func(c *tls.Config) {
+		// Set minimum TLS version using enum
+		switch cfg.MinTLSVersion {
+		case config.TLSVersion12, "":
+			c.MinVersion = tls.VersionTLS12
+		case config.TLSVersion13:
+			c.MinVersion = tls.VersionTLS13
+		}
+
+		// Set cipher suites using enum values
+		if len(cfg.CipherSuites) > 0 {
+			c.CipherSuites = convertCipherSuites(cfg.CipherSuites)
+		}
+	})
+
+	return tlsOpts, nil
+}
+
+// Apply to webhook server
+webhookServer := webhook.NewServer(webhook.Options{
+	TLSOpts: tlsOpts,
+})
+
+// Apply to metrics server
+metricsServerOptions := metricsserver.Options{
+	TLSOpts: tlsOpts,
+}
+```
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+- Create test utilities for verifying TLS settings on running servers
+
+#### Unit Tests
+
+The following packages will have unit test coverage:
+
+- `pkg/controller/core`: Configuration loading
+  - Test TLS configurations are loaded correctly
+  - Test default values are applied correctly
+
+- `cmd/kueue`: TLS config application to servers
+  - Test TLS config is correctly applied to webhook server
+  - Test TLS config is correctly applied to metrics server
+  - Test TLS config is correctly applied to visibility server
+
+Coverage target: >80% for new TLS configuration code
+
+#### Integration tests
+
+Integration tests will verify:
+
+1. **TLS Version Enforcement**:
+   - Deploy Kueue with MinTLSVersion=1.3
+   - Verify TLS 1.2 connections are rejected
+   - Verify TLS 1.3 connections are accepted
+
+2. **Cipher Suite Restriction**:
+   - Deploy Kueue with specific cipher suites
+   - Verify only allowed cipher suites can establish connections
+   - Verify denied cipher suites are rejected
+
+3. **Backward Compatibility**:
+   - Deploy Kueue without TLS configuration
+   - Verify all servers start successfully with default settings
+   - Verify clients can connect using default TLS settings
+
+4. **Multi-Server Application**:
+   - Deploy Kueue with custom TLS settings
+   - Verify webhook server uses configured TLS settings
+   - Verify metrics server uses configured TLS settings
+   - Verify visibility server uses configured TLS settings
+
+### Graduation Criteria
+
+This feature can graduate directly to stable as it is:
+- An opt-in configuration feature
+- Uses well-established Go standard library APIs
+- Maintains backward compatibility through defaults
+- Follows existing patterns from KEP-4377 (metrics TLS)
+
+The feature will be considered stable when:
+- All unit and integration tests pass
+- Documentation is complete with examples and best practices
+- At least one release cycle of user feedback has been collected
+- No critical bugs or security issues have been identified
+
+## Implementation History
+
+- 2025-12-11: KEP proposed based on issues #8190 and OCPKUEUE-450
+- TBD: KEP approved
+- TBD: Implementation merged
+- TBD: Released in Kueue vX.Y
+
+## Drawbacks
+
+1. **Configuration Complexity**: Adds more configuration options that administrators need to understand
+   - Mitigation: Provide clear documentation and examples
+
+2. **Testing Burden**: Requires testing various TLS configuration combinations
+   - Mitigation: Focus on common scenarios and provide validation tools
+
+3. **Troubleshooting Complexity**: TLS issues can be difficult to diagnose
+   - Mitigation: Add comprehensive logging and error messages
+
+4. **Maintenance**: TLS standards evolve, requiring updates to documentation and recommendations
+   - Mitigation: Document the configuration in a way that's easy to update
+
+## Alternatives
+
+### Server-Specific Configuration
+
+Instead of applying TLS settings globally, we could allow per-server configuration:
+
+```golang
+type TLSOptions struct {
+	Webhooks   *TLSProfile `json:"webhooks,omitempty"`
+	Metrics    *TLSProfile `json:"metrics,omitempty"`
+	Visibility *TLSProfile `json:"visibility,omitempty"`
+}
+```
+
+**Rejected because**:
+- Adds unnecessary complexity for most use cases
+- Inconsistent TLS policies across servers could create security gaps
+- More difficult to validate and test
+- Most organizations want uniform TLS policies across all endpoints
+
+### Environment Variables
+
+We could expose TLS settings through environment variables instead of the configuration API:
+
+```
+KUEUE_MIN_TLS_VERSION=1.3
+KUEUE_TLS_CIPHER_SUITES=TLS_AES_128_GCM_SHA256,TLS_AES_256_GCM_SHA384
+```
+
+**Rejected because**:
+- Less discoverable than API-based configuration
+- Harder to validate at deployment time
+- Inconsistent with Kueue's configuration patterns
+- Environment variables are less suitable for complex list values
+- Cannot be managed through GitOps workflows as easily
+
+### String based api
+
+Do not provide enums but allow strings and validate the allowed values.
+
+This allows us to update our validation logic (TLS 1.4, etc) or new cipher suites are added.
+
+This does make it harder to configure as we will validate on startup rather than at the API.
```

  I'm not clear about this argument. This new API anyway is in the configmap, and so will be validated on startup - regardless if we use strings or enums

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-12T15:59:15Z

- **keps/8190-tls-security-profiles/README.md:160** by [@mimowo](https://github.com/mimowo) — 2026-01-12T15:59:15Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
```

  I'm a bit concerned with listing all the enums inside the Kueue code, because it requires a process to update the list whenever a new version of golang supports a new cypher. Given that maintainers of Kueue don't track the cyphers we are likely to miss if a new is added. I'm not sure if PQC could motivate introduction of new cyphers, but it feels possible.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-12T16:05:50Z

- **keps/8190-tls-security-profiles/README.md:156** by [@kshalot](https://github.com/kshalot) — 2026-01-12T16:05:49Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
```

  And going back to [this comment](https://github.com/kubernetes-sigs/kueue/pull/8246#discussion_r2672048218), the TLS 1.3 cipher suites are not configurable, so having them in the API might be confusing

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-12T16:10:51Z

- **keps/8190-tls-security-profiles/README.md:407** by [@kannon92](https://github.com/kannon92) — 2026-01-12T16:10:51Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES256CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES128CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES256CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithCHACHA20POLY1305SHA256   CipherSuite = "TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256"
+	CipherSuiteTLSECDHEECDSAWithCHACHA20POLY1305SHA256 CipherSuite = "TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256"
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
+)
+
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// MinTLSVersion specifies the minimum TLS version that is acceptable.
+	// If not specified, defaults to "1.2" for backward compatibility.
+	// +optional
+	MinTLSVersion TLSVersion `json:"minTLSVersion,omitempty"`
+
+	// CipherSuites specifies the list of enabled TLS cipher suites.
+	// If not specified, a secure default list will be used.
+	// The available cipher suites are defined in Go's crypto/tls package.
+	// See https://golang.org/pkg/crypto/tls/#pkg-constants for valid values.
+	// +optional
+	CipherSuites []CipherSuite `json:"cipherSuites,omitempty"`
+}
+
+// Configuration defines the desired state of Configuration
+type Configuration struct {
+	// ... existing fields ...
+
+	// TLS contains TLS security settings for all Kueue API servers
+	// (webhooks, metrics, controller, and visibility).
+	// +optional
+	TLS *TLSOptions `json:"tls,omitempty"`
+}
+```
+
+We will take only the "secure" TLS options according to golang.
+The list is taken from [golang](https://cs.opensource.google/go/go/+/refs/tags/go1.25.5:src/crypto/tls/cipher_suites.go;l=56).
+
+### Supported Servers
+
+The TLS configuration will be applied to the following Kueue servers:
+
+1. **Webhook Server**: Admission webhook endpoints for validating and mutating resources
+2. **Metrics Server**: Prometheus metrics endpoint (when secure metrics are enabled)
+3. **Visibility Server**: API server for workload visibility queries
+4. **Health Probe Server** (if applicable): Health check endpoints
+
+### Default Values
+
+To maintain backward compatibility and provide secure defaults:
+
+- **MinTLSVersion**: TLSVersion12 (value: "1.2")
+  - Rationale: TLS 1.2 is widely supported and required by most compliance frameworks
+  - TLS 1.0 and 1.1 are deprecated and will not be supported
+  - Valid enum values: TLSVersion12, TLSVersion13
+
+- **CipherSuites**: If not specified, Go's default secure cipher suite selection will be used
+  - For TLS 1.2, recommended cipher suite enums include:
+    - CipherSuiteTLSECDHERSAWithAES128GCMSHA256
+    - CipherSuiteTLSECDHERSAWithAES256GCMSHA384
+    - CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256
+    - CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384
+  - For TLS 1.3, cipher suites are not configurable (Go uses the built-in secure set)
+  - All secure cipher suite enums are defined in the configuration_types.go file
+
+### Implementation Approach
+
+The implementation will follow these steps:
+
+1. **Configuration Loading**: Parse TLS options from the controller configuration
+2. **TLS Config Construction**: Build a `tls.Config` object with the specified options
+3. **Server Configuration**: Apply the TLS config to all relevant servers using `TLSOpts`
+
+Example implementation for applying TLS configuration:
+
+```golang
+func buildTLSConfig(cfg *config.TLSOptions) ([]func(*tls.Config), error) {
+	if cfg == nil {
+		return nil, nil
+	}
+
+	var tlsOpts []func(*tls.Config)
+
+	tlsOpts = append(tlsOpts, func(c *tls.Config) {
+		// Set minimum TLS version using enum
+		switch cfg.MinTLSVersion {
+		case config.TLSVersion12, "":
+			c.MinVersion = tls.VersionTLS12
+		case config.TLSVersion13:
+			c.MinVersion = tls.VersionTLS13
+		}
+
+		// Set cipher suites using enum values
+		if len(cfg.CipherSuites) > 0 {
+			c.CipherSuites = convertCipherSuites(cfg.CipherSuites)
+		}
+	})
+
+	return tlsOpts, nil
+}
+
+// Apply to webhook server
+webhookServer := webhook.NewServer(webhook.Options{
+	TLSOpts: tlsOpts,
+})
+
+// Apply to metrics server
+metricsServerOptions := metricsserver.Options{
+	TLSOpts: tlsOpts,
+}
+```
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+- Create test utilities for verifying TLS settings on running servers
+
+#### Unit Tests
+
+The following packages will have unit test coverage:
+
+- `pkg/controller/core`: Configuration loading
+  - Test TLS configurations are loaded correctly
+  - Test default values are applied correctly
+
+- `cmd/kueue`: TLS config application to servers
+  - Test TLS config is correctly applied to webhook server
+  - Test TLS config is correctly applied to metrics server
+  - Test TLS config is correctly applied to visibility server
+
+Coverage target: >80% for new TLS configuration code
+
+#### Integration tests
+
+Integration tests will verify:
+
+1. **TLS Version Enforcement**:
+   - Deploy Kueue with MinTLSVersion=1.3
+   - Verify TLS 1.2 connections are rejected
+   - Verify TLS 1.3 connections are accepted
+
+2. **Cipher Suite Restriction**:
+   - Deploy Kueue with specific cipher suites
+   - Verify only allowed cipher suites can establish connections
+   - Verify denied cipher suites are rejected
+
+3. **Backward Compatibility**:
+   - Deploy Kueue without TLS configuration
+   - Verify all servers start successfully with default settings
+   - Verify clients can connect using default TLS settings
+
+4. **Multi-Server Application**:
+   - Deploy Kueue with custom TLS settings
+   - Verify webhook server uses configured TLS settings
+   - Verify metrics server uses configured TLS settings
+   - Verify visibility server uses configured TLS settings
+
+### Graduation Criteria
+
+This feature can graduate directly to stable as it is:
+- An opt-in configuration feature
+- Uses well-established Go standard library APIs
+- Maintains backward compatibility through defaults
+- Follows existing patterns from KEP-4377 (metrics TLS)
+
+The feature will be considered stable when:
+- All unit and integration tests pass
+- Documentation is complete with examples and best practices
+- At least one release cycle of user feedback has been collected
+- No critical bugs or security issues have been identified
+
+## Implementation History
+
+- 2025-12-11: KEP proposed based on issues #8190 and OCPKUEUE-450
+- TBD: KEP approved
+- TBD: Implementation merged
+- TBD: Released in Kueue vX.Y
+
+## Drawbacks
+
+1. **Configuration Complexity**: Adds more configuration options that administrators need to understand
+   - Mitigation: Provide clear documentation and examples
+
+2. **Testing Burden**: Requires testing various TLS configuration combinations
+   - Mitigation: Focus on common scenarios and provide validation tools
+
+3. **Troubleshooting Complexity**: TLS issues can be difficult to diagnose
+   - Mitigation: Add comprehensive logging and error messages
+
+4. **Maintenance**: TLS standards evolve, requiring updates to documentation and recommendations
+   - Mitigation: Document the configuration in a way that's easy to update
+
+## Alternatives
+
+### Server-Specific Configuration
+
+Instead of applying TLS settings globally, we could allow per-server configuration:
+
+```golang
+type TLSOptions struct {
+	Webhooks   *TLSProfile `json:"webhooks,omitempty"`
+	Metrics    *TLSProfile `json:"metrics,omitempty"`
+	Visibility *TLSProfile `json:"visibility,omitempty"`
+}
+```
+
+**Rejected because**:
+- Adds unnecessary complexity for most use cases
+- Inconsistent TLS policies across servers could create security gaps
+- More difficult to validate and test
+- Most organizations want uniform TLS policies across all endpoints
+
+### Environment Variables
+
+We could expose TLS settings through environment variables instead of the configuration API:
+
+```
+KUEUE_MIN_TLS_VERSION=1.3
+KUEUE_TLS_CIPHER_SUITES=TLS_AES_128_GCM_SHA256,TLS_AES_256_GCM_SHA384
+```
+
+**Rejected because**:
+- Less discoverable than API-based configuration
+- Harder to validate at deployment time
+- Inconsistent with Kueue's configuration patterns
+- Environment variables are less suitable for complex list values
+- Cannot be managed through GitOps workflows as easily
+
+### String based api
+
+Do not provide enums but allow strings and validate the allowed values.
+
+This allows us to update our validation logic (TLS 1.4, etc) or new cipher suites are added.
+
+This does make it harder to configure as we will validate on startup rather than at the API.
```

  Yea I meant its harder for users to configure.
  
  They have to know the enums for crypto/tls settings.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-12T16:11:19Z

- **keps/8190-tls-security-profiles/README.md:160** by [@kannon92](https://github.com/kannon92) — 2026-01-12T16:11:19Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
```

  We have to either list in the API or add them in validation?
  
  Or we blindly pass them into golang and hope things work?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-12T16:12:29Z

- **keps/8190-tls-security-profiles/README.md:156** by [@kannon92](https://github.com/kannon92) — 2026-01-12T16:12:29Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
```

  I wasn't as clear if TLS 1.3 ciphersuite can be used if you are using TLS 1.2.
  
  For 1.2 I think you are free to use what ciphersuites we want and my understanding is that many users still use 1.2.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-12T16:13:13Z

- **keps/8190-tls-security-profiles/README.md:156** by [@kannon92](https://github.com/kannon92) — 2026-01-12T16:13:13Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
```

  Code wise: I think its like if 1.3 we can't allow CipherSuite.
  
  If 1.2, we allow 1.2 or 1.3 settings. But I guess I need to check on this.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-12T16:15:27Z

- **keps/8190-tls-security-profiles/README.md:160** by [@kannon92](https://github.com/kannon92) — 2026-01-12T16:15:27Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
```

  Maybe I could consider using https://github.com/kubernetes/component-base/blob/master/cli/flag/ciphersuites_flag.go.
  
  Are you saying you'd prefer a raw string and we don't explictly document these options in our documentaiton?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-12T16:28:46Z

- **keps/8190-tls-security-profiles/README.md:160** by [@mimowo](https://github.com/mimowo) — 2026-01-12T16:28:45Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
```

  > Are you saying you'd prefer a raw string and we don't explictly document these options in our documentaiton?
  
  I'm not yet sure, but this is where I'm leaning to. If golang provides a validation function maybe the error would list to the user the list of available options, no? Is the list documented somewhere in golang?
  
  But first of all please let me know where you took TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA and other enum values from? Please also update the PR description in the section "Special notes for your reviewer:" with the source information. If we are going to maintain the list we need to know where to take the values from.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-12T16:30:18Z

- **keps/8190-tls-security-profiles/README.md:407** by [@mimowo](https://github.com/mimowo) — 2026-01-12T16:30:18Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES256CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES128CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES256CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithCHACHA20POLY1305SHA256   CipherSuite = "TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256"
+	CipherSuiteTLSECDHEECDSAWithCHACHA20POLY1305SHA256 CipherSuite = "TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256"
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
+)
+
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// MinTLSVersion specifies the minimum TLS version that is acceptable.
+	// If not specified, defaults to "1.2" for backward compatibility.
+	// +optional
+	MinTLSVersion TLSVersion `json:"minTLSVersion,omitempty"`
+
+	// CipherSuites specifies the list of enabled TLS cipher suites.
+	// If not specified, a secure default list will be used.
+	// The available cipher suites are defined in Go's crypto/tls package.
+	// See https://golang.org/pkg/crypto/tls/#pkg-constants for valid values.
+	// +optional
+	CipherSuites []CipherSuite `json:"cipherSuites,omitempty"`
+}
+
+// Configuration defines the desired state of Configuration
+type Configuration struct {
+	// ... existing fields ...
+
+	// TLS contains TLS security settings for all Kueue API servers
+	// (webhooks, metrics, controller, and visibility).
+	// +optional
+	TLS *TLSOptions `json:"tls,omitempty"`
+}
+```
+
+We will take only the "secure" TLS options according to golang.
+The list is taken from [golang](https://cs.opensource.google/go/go/+/refs/tags/go1.25.5:src/crypto/tls/cipher_suites.go;l=56).
+
+### Supported Servers
+
+The TLS configuration will be applied to the following Kueue servers:
+
+1. **Webhook Server**: Admission webhook endpoints for validating and mutating resources
+2. **Metrics Server**: Prometheus metrics endpoint (when secure metrics are enabled)
+3. **Visibility Server**: API server for workload visibility queries
+4. **Health Probe Server** (if applicable): Health check endpoints
+
+### Default Values
+
+To maintain backward compatibility and provide secure defaults:
+
+- **MinTLSVersion**: TLSVersion12 (value: "1.2")
+  - Rationale: TLS 1.2 is widely supported and required by most compliance frameworks
+  - TLS 1.0 and 1.1 are deprecated and will not be supported
+  - Valid enum values: TLSVersion12, TLSVersion13
+
+- **CipherSuites**: If not specified, Go's default secure cipher suite selection will be used
+  - For TLS 1.2, recommended cipher suite enums include:
+    - CipherSuiteTLSECDHERSAWithAES128GCMSHA256
+    - CipherSuiteTLSECDHERSAWithAES256GCMSHA384
+    - CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256
+    - CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384
+  - For TLS 1.3, cipher suites are not configurable (Go uses the built-in secure set)
+  - All secure cipher suite enums are defined in the configuration_types.go file
+
+### Implementation Approach
+
+The implementation will follow these steps:
+
+1. **Configuration Loading**: Parse TLS options from the controller configuration
+2. **TLS Config Construction**: Build a `tls.Config` object with the specified options
+3. **Server Configuration**: Apply the TLS config to all relevant servers using `TLSOpts`
+
+Example implementation for applying TLS configuration:
+
+```golang
+func buildTLSConfig(cfg *config.TLSOptions) ([]func(*tls.Config), error) {
+	if cfg == nil {
+		return nil, nil
+	}
+
+	var tlsOpts []func(*tls.Config)
+
+	tlsOpts = append(tlsOpts, func(c *tls.Config) {
+		// Set minimum TLS version using enum
+		switch cfg.MinTLSVersion {
+		case config.TLSVersion12, "":
+			c.MinVersion = tls.VersionTLS12
+		case config.TLSVersion13:
+			c.MinVersion = tls.VersionTLS13
+		}
+
+		// Set cipher suites using enum values
+		if len(cfg.CipherSuites) > 0 {
+			c.CipherSuites = convertCipherSuites(cfg.CipherSuites)
+		}
+	})
+
+	return tlsOpts, nil
+}
+
+// Apply to webhook server
+webhookServer := webhook.NewServer(webhook.Options{
+	TLSOpts: tlsOpts,
+})
+
+// Apply to metrics server
+metricsServerOptions := metricsserver.Options{
+	TLSOpts: tlsOpts,
+}
+```
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+- Create test utilities for verifying TLS settings on running servers
+
+#### Unit Tests
+
+The following packages will have unit test coverage:
+
+- `pkg/controller/core`: Configuration loading
+  - Test TLS configurations are loaded correctly
+  - Test default values are applied correctly
+
+- `cmd/kueue`: TLS config application to servers
+  - Test TLS config is correctly applied to webhook server
+  - Test TLS config is correctly applied to metrics server
+  - Test TLS config is correctly applied to visibility server
+
+Coverage target: >80% for new TLS configuration code
+
+#### Integration tests
+
+Integration tests will verify:
+
+1. **TLS Version Enforcement**:
+   - Deploy Kueue with MinTLSVersion=1.3
+   - Verify TLS 1.2 connections are rejected
+   - Verify TLS 1.3 connections are accepted
+
+2. **Cipher Suite Restriction**:
+   - Deploy Kueue with specific cipher suites
+   - Verify only allowed cipher suites can establish connections
+   - Verify denied cipher suites are rejected
+
+3. **Backward Compatibility**:
+   - Deploy Kueue without TLS configuration
+   - Verify all servers start successfully with default settings
+   - Verify clients can connect using default TLS settings
+
+4. **Multi-Server Application**:
+   - Deploy Kueue with custom TLS settings
+   - Verify webhook server uses configured TLS settings
+   - Verify metrics server uses configured TLS settings
+   - Verify visibility server uses configured TLS settings
+
+### Graduation Criteria
+
+This feature can graduate directly to stable as it is:
+- An opt-in configuration feature
+- Uses well-established Go standard library APIs
+- Maintains backward compatibility through defaults
+- Follows existing patterns from KEP-4377 (metrics TLS)
+
+The feature will be considered stable when:
+- All unit and integration tests pass
+- Documentation is complete with examples and best practices
+- At least one release cycle of user feedback has been collected
+- No critical bugs or security issues have been identified
+
+## Implementation History
+
+- 2025-12-11: KEP proposed based on issues #8190 and OCPKUEUE-450
+- TBD: KEP approved
+- TBD: Implementation merged
+- TBD: Released in Kueue vX.Y
+
+## Drawbacks
+
+1. **Configuration Complexity**: Adds more configuration options that administrators need to understand
+   - Mitigation: Provide clear documentation and examples
+
+2. **Testing Burden**: Requires testing various TLS configuration combinations
+   - Mitigation: Focus on common scenarios and provide validation tools
+
+3. **Troubleshooting Complexity**: TLS issues can be difficult to diagnose
+   - Mitigation: Add comprehensive logging and error messages
+
+4. **Maintenance**: TLS standards evolve, requiring updates to documentation and recommendations
+   - Mitigation: Document the configuration in a way that's easy to update
+
+## Alternatives
+
+### Server-Specific Configuration
+
+Instead of applying TLS settings globally, we could allow per-server configuration:
+
+```golang
+type TLSOptions struct {
+	Webhooks   *TLSProfile `json:"webhooks,omitempty"`
+	Metrics    *TLSProfile `json:"metrics,omitempty"`
+	Visibility *TLSProfile `json:"visibility,omitempty"`
+}
+```
+
+**Rejected because**:
+- Adds unnecessary complexity for most use cases
+- Inconsistent TLS policies across servers could create security gaps
+- More difficult to validate and test
+- Most organizations want uniform TLS policies across all endpoints
+
+### Environment Variables
+
+We could expose TLS settings through environment variables instead of the configuration API:
+
+```
+KUEUE_MIN_TLS_VERSION=1.3
+KUEUE_TLS_CIPHER_SUITES=TLS_AES_128_GCM_SHA256,TLS_AES_256_GCM_SHA384
+```
+
+**Rejected because**:
+- Less discoverable than API-based configuration
+- Harder to validate at deployment time
+- Inconsistent with Kueue's configuration patterns
+- Environment variables are less suitable for complex list values
+- Cannot be managed through GitOps workflows as easily
+
+### String based api
+
+Do not provide enums but allow strings and validate the allowed values.
+
+This allows us to update our validation logic (TLS 1.4, etc) or new cipher suites are added.
+
+This does make it harder to configure as we will validate on startup rather than at the API.
```

  Sure, we need to provide users the list, but I'm wondering if we need to list them all in code. Is there some golang documentation with the list? If so we could list there. Also, I imagine golang would provide a validation function which could list the available options in case of specifiying a wrong one, no?

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-12T16:45:41Z

- **keps/8190-tls-security-profiles/README.md:156** by [@kshalot](https://github.com/kshalot) — 2026-01-12T16:45:41Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
```

  As far as I understand:
  1. The TLS 1.3 cipher's cannot be used in TLS 1.2 and vice versa since they are structured differently AFAIK. I think there is some overlap in the available cipher suites for versions 1.1 and 1.2, but we don't support 1.1 so it doesn't matter.
  2. There's a bunch of references to the ciphers available for 1.3 not being configurable. See:
      * https://github.com/golang/go/issues/29349
      * https://github.com/golang/go/blob/fed3b0a298464457c58d1150bdb3942f22bd6220/src/crypto/tls/common.go#L709-L710
      
  So AFAIK the [`CipherSuites`](https://github.com/golang/go/blob/992ad55e3dcea4bd017d618d759cb9cd3529f288/src/crypto/tls/common.go#L709-L718) field is pretty much `CipherSuitesForTLS1.0-1.2`. I'm guessing the structure is confusing-ish because it was implemented before TLS 1.3 became a standard and kept this way for backwards compatibility.
  
  But someone should double check me.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-12T16:47:39Z

- **keps/8190-tls-security-profiles/README.md:156** by [@kshalot](https://github.com/kshalot) — 2026-01-12T16:47:39Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
```

  Also - that's why I mentioned it might not be clear which values the user should include here if it's not properly documented/structured so it's clear that for 1.3 it's ignored. See comment: https://github.com/kubernetes-sigs/kueue/pull/8246#discussion_r2672048218

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-12T20:55:18Z

- **keps/8190-tls-security-profiles/README.md:160** by [@kannon92](https://github.com/kannon92) — 2026-01-12T20:55:18Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
```

  Golang doesn't really provide a validation for this.
  
  In Kubernetes we validate this.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-12T21:59:43Z

- **keps/8190-tls-security-profiles/README.md:160** by [@kannon92](https://github.com/kannon92) — 2026-01-12T21:59:43Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
```

  https://cs.opensource.google/go/go/+/refs/tags/go1.25.5:src/crypto/tls/cipher_suites.go;l=686
  
  That was at least the values I took for golang 1.25.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-12T22:14:25Z

- **keps/8190-tls-security-profiles/README.md:407** by [@kannon92](https://github.com/kannon92) — 2026-01-12T22:14:25Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES256CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES128CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES256CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithCHACHA20POLY1305SHA256   CipherSuite = "TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256"
+	CipherSuiteTLSECDHEECDSAWithCHACHA20POLY1305SHA256 CipherSuite = "TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256"
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
+)
+
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// MinTLSVersion specifies the minimum TLS version that is acceptable.
+	// If not specified, defaults to "1.2" for backward compatibility.
+	// +optional
+	MinTLSVersion TLSVersion `json:"minTLSVersion,omitempty"`
+
+	// CipherSuites specifies the list of enabled TLS cipher suites.
+	// If not specified, a secure default list will be used.
+	// The available cipher suites are defined in Go's crypto/tls package.
+	// See https://golang.org/pkg/crypto/tls/#pkg-constants for valid values.
+	// +optional
+	CipherSuites []CipherSuite `json:"cipherSuites,omitempty"`
+}
+
+// Configuration defines the desired state of Configuration
+type Configuration struct {
+	// ... existing fields ...
+
+	// TLS contains TLS security settings for all Kueue API servers
+	// (webhooks, metrics, controller, and visibility).
+	// +optional
+	TLS *TLSOptions `json:"tls,omitempty"`
+}
+```
+
+We will take only the "secure" TLS options according to golang.
+The list is taken from [golang](https://cs.opensource.google/go/go/+/refs/tags/go1.25.5:src/crypto/tls/cipher_suites.go;l=56).
+
+### Supported Servers
+
+The TLS configuration will be applied to the following Kueue servers:
+
+1. **Webhook Server**: Admission webhook endpoints for validating and mutating resources
+2. **Metrics Server**: Prometheus metrics endpoint (when secure metrics are enabled)
+3. **Visibility Server**: API server for workload visibility queries
+4. **Health Probe Server** (if applicable): Health check endpoints
+
+### Default Values
+
+To maintain backward compatibility and provide secure defaults:
+
+- **MinTLSVersion**: TLSVersion12 (value: "1.2")
+  - Rationale: TLS 1.2 is widely supported and required by most compliance frameworks
+  - TLS 1.0 and 1.1 are deprecated and will not be supported
+  - Valid enum values: TLSVersion12, TLSVersion13
+
+- **CipherSuites**: If not specified, Go's default secure cipher suite selection will be used
+  - For TLS 1.2, recommended cipher suite enums include:
+    - CipherSuiteTLSECDHERSAWithAES128GCMSHA256
+    - CipherSuiteTLSECDHERSAWithAES256GCMSHA384
+    - CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256
+    - CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384
+  - For TLS 1.3, cipher suites are not configurable (Go uses the built-in secure set)
+  - All secure cipher suite enums are defined in the configuration_types.go file
+
+### Implementation Approach
+
+The implementation will follow these steps:
+
+1. **Configuration Loading**: Parse TLS options from the controller configuration
+2. **TLS Config Construction**: Build a `tls.Config` object with the specified options
+3. **Server Configuration**: Apply the TLS config to all relevant servers using `TLSOpts`
+
+Example implementation for applying TLS configuration:
+
+```golang
+func buildTLSConfig(cfg *config.TLSOptions) ([]func(*tls.Config), error) {
+	if cfg == nil {
+		return nil, nil
+	}
+
+	var tlsOpts []func(*tls.Config)
+
+	tlsOpts = append(tlsOpts, func(c *tls.Config) {
+		// Set minimum TLS version using enum
+		switch cfg.MinTLSVersion {
+		case config.TLSVersion12, "":
+			c.MinVersion = tls.VersionTLS12
+		case config.TLSVersion13:
+			c.MinVersion = tls.VersionTLS13
+		}
+
+		// Set cipher suites using enum values
+		if len(cfg.CipherSuites) > 0 {
+			c.CipherSuites = convertCipherSuites(cfg.CipherSuites)
+		}
+	})
+
+	return tlsOpts, nil
+}
+
+// Apply to webhook server
+webhookServer := webhook.NewServer(webhook.Options{
+	TLSOpts: tlsOpts,
+})
+
+// Apply to metrics server
+metricsServerOptions := metricsserver.Options{
+	TLSOpts: tlsOpts,
+}
+```
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+- Create test utilities for verifying TLS settings on running servers
+
+#### Unit Tests
+
+The following packages will have unit test coverage:
+
+- `pkg/controller/core`: Configuration loading
+  - Test TLS configurations are loaded correctly
+  - Test default values are applied correctly
+
+- `cmd/kueue`: TLS config application to servers
+  - Test TLS config is correctly applied to webhook server
+  - Test TLS config is correctly applied to metrics server
+  - Test TLS config is correctly applied to visibility server
+
+Coverage target: >80% for new TLS configuration code
+
+#### Integration tests
+
+Integration tests will verify:
+
+1. **TLS Version Enforcement**:
+   - Deploy Kueue with MinTLSVersion=1.3
+   - Verify TLS 1.2 connections are rejected
+   - Verify TLS 1.3 connections are accepted
+
+2. **Cipher Suite Restriction**:
+   - Deploy Kueue with specific cipher suites
+   - Verify only allowed cipher suites can establish connections
+   - Verify denied cipher suites are rejected
+
+3. **Backward Compatibility**:
+   - Deploy Kueue without TLS configuration
+   - Verify all servers start successfully with default settings
+   - Verify clients can connect using default TLS settings
+
+4. **Multi-Server Application**:
+   - Deploy Kueue with custom TLS settings
+   - Verify webhook server uses configured TLS settings
+   - Verify metrics server uses configured TLS settings
+   - Verify visibility server uses configured TLS settings
+
+### Graduation Criteria
+
+This feature can graduate directly to stable as it is:
+- An opt-in configuration feature
+- Uses well-established Go standard library APIs
+- Maintains backward compatibility through defaults
+- Follows existing patterns from KEP-4377 (metrics TLS)
+
+The feature will be considered stable when:
+- All unit and integration tests pass
+- Documentation is complete with examples and best practices
+- At least one release cycle of user feedback has been collected
+- No critical bugs or security issues have been identified
+
+## Implementation History
+
+- 2025-12-11: KEP proposed based on issues #8190 and OCPKUEUE-450
+- TBD: KEP approved
+- TBD: Implementation merged
+- TBD: Released in Kueue vX.Y
+
+## Drawbacks
+
+1. **Configuration Complexity**: Adds more configuration options that administrators need to understand
+   - Mitigation: Provide clear documentation and examples
+
+2. **Testing Burden**: Requires testing various TLS configuration combinations
+   - Mitigation: Focus on common scenarios and provide validation tools
+
+3. **Troubleshooting Complexity**: TLS issues can be difficult to diagnose
+   - Mitigation: Add comprehensive logging and error messages
+
+4. **Maintenance**: TLS standards evolve, requiring updates to documentation and recommendations
+   - Mitigation: Document the configuration in a way that's easy to update
+
+## Alternatives
+
+### Server-Specific Configuration
+
+Instead of applying TLS settings globally, we could allow per-server configuration:
+
+```golang
+type TLSOptions struct {
+	Webhooks   *TLSProfile `json:"webhooks,omitempty"`
+	Metrics    *TLSProfile `json:"metrics,omitempty"`
+	Visibility *TLSProfile `json:"visibility,omitempty"`
+}
+```
+
+**Rejected because**:
+- Adds unnecessary complexity for most use cases
+- Inconsistent TLS policies across servers could create security gaps
+- More difficult to validate and test
+- Most organizations want uniform TLS policies across all endpoints
+
+### Environment Variables
+
+We could expose TLS settings through environment variables instead of the configuration API:
+
+```
+KUEUE_MIN_TLS_VERSION=1.3
+KUEUE_TLS_CIPHER_SUITES=TLS_AES_128_GCM_SHA256,TLS_AES_256_GCM_SHA384
+```
+
+**Rejected because**:
+- Less discoverable than API-based configuration
+- Harder to validate at deployment time
+- Inconsistent with Kueue's configuration patterns
+- Environment variables are less suitable for complex list values
+- Cannot be managed through GitOps workflows as easily
+
+### String based api
+
+Do not provide enums but allow strings and validate the allowed values.
+
+This allows us to update our validation logic (TLS 1.4, etc) or new cipher suites are added.
+
+This does make it harder to configure as we will validate on startup rather than at the API.
```

  https://pkg.go.dev/crypto/tls#pkg-constants
  
  For Kubernetes, we could potential use https://github.com/kubernetes/kubernetes/blob/master/staging/src/k8s.io/component-base/cli/flag/ciphersuites_flag.go.
  
  This at least handles validation.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-12T22:18:18Z

- **keps/8190-tls-security-profiles/README.md:160** by [@kannon92](https://github.com/kannon92) — 2026-01-12T22:18:18Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
```

  So I guess I could drop the enums, use https://github.com/kubernetes/kubernetes/blob/master/staging/src/k8s.io/component-base/cli/flag/ciphersuites_flag.go to validate.
  
  That would make the "API":
  
  ```golang
  struct TLSOptions {
  MinTLSVersion string
  CipherSuite []string
  }
  ```
  
  And we use component base to validate?
  
  And maybe our docs just link to golang saying that these must be one of the following golang constants ie
  
  CipherSuite = []string{TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA, TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA}.
  
  Otherwise server fails to start.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-12T22:21:42Z

- **keps/8190-tls-security-profiles/README.md:160** by [@kannon92](https://github.com/kannon92) — 2026-01-12T22:21:42Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
```

  TBH this satifies my use case because we rely on APIServer following https://github.com/openshift/api/blob/master/config/v1/types_tlssecurityprofile.go.
  
  And users can use specific TLS Profiles that handles this complexity.
  
  But its a bit harder to configure this for someone without relying on some external entity to streamline this for you.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-12T22:24:25Z

- **keps/8190-tls-security-profiles/README.md:160** by [@kannon92](https://github.com/kannon92) — 2026-01-12T22:24:25Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
```

  The API, I believe, was motivated from https://ssl-config.mozilla.org/#server=go&version=1.25&config=modern&guideline=5.7.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-13T16:21:38Z

- **keps/8190-tls-security-profiles/README.md:160** by [@kannon92](https://github.com/kannon92) — 2026-01-13T16:21:38Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
```

  @mimowo @kshalot 
  
  I drafted a POC with this. https://github.com/kubernetes-sigs/kueue/pull/8563/files
  
  Are you happy with this? If so I can update the KEP to go this route.
  
  It feels that we are discussing implementation details at this point but the idea is sound imo.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-13T16:55:48Z

- **keps/8190-tls-security-profiles/README.md:160** by [@kshalot](https://github.com/kshalot) — 2026-01-13T16:55:48Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
```

  It looks good to me. IIUC the consensus is string > enum, so the KEP can be updated.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-13T17:18:50Z

- **keps/8190-tls-security-profiles/README.md:160** by [@kannon92](https://github.com/kannon92) — 2026-01-13T17:18:49Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
```

  Updated this KEP to reflect string api.
  
  I think it makes sense given the concerns about keeping up to date with CipherSuites.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-01-13T17:23:13Z

- **keps/8190-tls-security-profiles/kep.yaml:16** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-13T17:12:47Z
```diff
@@ -0,0 +1,25 @@
+title: TLS Security Profile Support
+kep-number: 8190
+authors:
+  - "@kannon92"
+status: implementable 
+creation-date: 2025-12-11
+reviewers:
+  - kshalot
+approvers:
+  - mimowo
+
+see-also:
+replaces:
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: stable
```

  ```suggestion
  stage: alpha
  ```
  
  I think we should start from Alpha or Beta since, as you described, this is a possible break to the existing environment. We should provide a better migration period and a way.
  
  > **Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
  
  Additionally, we should provide FG for a knob to enabling and disabling this feature.

- **keps/8190-tls-security-profiles/README.md:204** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-13T17:19:44Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES256CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES128CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES256CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithCHACHA20POLY1305SHA256   CipherSuite = "TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256"
+	CipherSuiteTLSECDHEECDSAWithCHACHA20POLY1305SHA256 CipherSuite = "TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256"
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
+)
+
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// MinTLSVersion specifies the minimum TLS version that is acceptable.
+	// If not specified, defaults to "1.2" for backward compatibility.
+	// +optional
+	MinTLSVersion TLSVersion `json:"minTLSVersion,omitempty"`
+
+	// CipherSuites specifies the list of enabled TLS cipher suites.
+	// If not specified, a secure default list will be used.
+	// The available cipher suites are defined in Go's crypto/tls package.
+	// See https://golang.org/pkg/crypto/tls/#pkg-constants for valid values.
+	// +optional
+	CipherSuites []CipherSuite `json:"cipherSuites,omitempty"`
+}
+
+// Configuration defines the desired state of Configuration
+type Configuration struct {
+	// ... existing fields ...
+
+	// TLS contains TLS security settings for all Kueue API servers
+	// (webhooks, metrics, controller, and visibility).
+	// +optional
+	TLS *TLSOptions `json:"tls,omitempty"`
+}
+```
+
+We will take only the "secure" TLS options according to golang.
+The list is taken from [golang](https://cs.opensource.google/go/go/+/refs/tags/go1.25.5:src/crypto/tls/cipher_suites.go;l=56).
```

  Any way to keep automatically synchronizing the Kueue API with the new list items?

- **keps/8190-tls-security-profiles/README.md:99** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-13T17:20:42Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
```

  Are there any use cases where they want to enforce different TLS policies for each webhook, metrics, ...?

- **keps/8190-tls-security-profiles/README.md:237** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-13T17:22:45Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Enum-Based API](#enum-based-api)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration using a string-based API:
+
+```golang
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// tlsMinVersion is the minimum TLS version supported.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// Valid values are: "VersionTLS12", "VersionTLS13"
+	// Default: "" (uses Go's default, currently TLS 1.2)
+	// +optional
+	TLSMinVersion string `json:"tlsMinVersion,omitempty"`
+
+	// tlsCipherSuites is the list of allowed cipher suites for the server.
+	// Note that TLS 1.3 ciphersuites are not configurable.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// Default: nil (uses Go's default cipher suite selection)
+	// +optional
+	TLSCipherSuites []string `json:"tlsCipherSuites,omitempty"`
+}
+
+// ControllerManager is added to the Configuration struct
+type ControllerManager struct {
+	// ... existing fields ...
+
+	// TLS contains TLS security settings for all Kueue API servers
+	// (webhooks, metrics, and visibility).
+	// +optional
+	TLS *TLSOptions `json:"tls,omitempty"`
+}
+```
+
+This approach uses string values that are validated at runtime against Go's crypto/tls package constants.
+This provides flexibility for future TLS versions and cipher suites without requiring API changes.
+
+### Supported Servers
+
+The TLS configuration will be applied to the following Kueue servers:
+
+1. **Webhook Server**: Admission webhook endpoints for validating and mutating resources
+2. **Metrics Server**: Prometheus metrics endpoint (when secure metrics are enabled)
+3. **Visibility Server**: API server for workload visibility queries
+4. **Health Probe Server** (if applicable): Health check endpoints
+
+### Default Values
+
+To maintain backward compatibility and provide secure defaults:
+
+- **TLSMinVersion**: "" (empty string, uses Go's default which is TLS 1.2)
+  - Rationale: TLS 1.2 is widely supported and required by most compliance frameworks
+  - TLS 1.0 and 1.1 are deprecated and will not be supported
+  - Valid values: "VersionTLS12", "VersionTLS13"
+
+- **TLSCipherSuites**: nil (uses Go's default secure cipher suite selection)
+  - For TLS 1.2, recommended cipher suites include:
+    - TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256
+    - TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384
+    - TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256
+    - TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384
+  - For TLS 1.3, cipher suites are not configurable (Go uses the built-in secure set)
+  - Valid cipher suite values are defined in Go's crypto/tls package constants
+
+### Implementation Approach
+
+The implementation will follow these steps:
+
+1. **Configuration Loading**: Parse TLS options from the controller configuration
+2. **Validation**: Validate TLS version and cipher suite strings against Go's crypto/tls package at startup
+3. **TLS Config Construction**: Build a `tls.Config` object with the specified options
+4. **Server Configuration**: Apply the TLS config to all relevant servers using `TLSOpts`
+
+Example implementation for applying TLS configuration:
+
+```golang
+func buildTLSConfig(cfg *config.TLSOptions) ([]func(*tls.Config), error) {
+	if cfg == nil {
+		return nil, nil
+	}
+
+	var tlsOpts []func(*tls.Config)
+
+	tlsOpts = append(tlsOpts, func(c *tls.Config) {
+		// Set minimum TLS version from string
+		if cfg.TLSMinVersion != "" {
+			version, err := tlsconfig.TLSVersion(cfg.TLSMinVersion)
+			if err == nil {
+				c.MinVersion = version
+			}
+		}
+
+		// Set cipher suites from string values
+		if len(cfg.TLSCipherSuites) > 0 {
+			suites, err := tlsconfig.TLSCipherSuites(cfg.TLSCipherSuites)
+			if err == nil {
+				c.CipherSuites = suites
+			}
+		}
+	})
+
```

  It would be better to remove actual implementations in the enhancement proposal to avoid confusion when this part is outdated.

- **keps/8190-tls-security-profiles/kep.yaml:10** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-13T17:23:09Z
```diff
@@ -0,0 +1,25 @@
+title: TLS Security Profile Support
+kep-number: 8190
+authors:
+  - "@kannon92"
+status: implementable 
+creation-date: 2025-12-11
+reviewers:
+  - kshalot
+approvers:
+  - mimowo
```

  ```suggestion
    - mimowo
    - tenzen-y
  ```

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-13T17:29:35Z

- **keps/8190-tls-security-profiles/README.md:204** by [@kannon92](https://github.com/kannon92) — 2026-01-13T17:29:35Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES256CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES128CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES256CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithCHACHA20POLY1305SHA256   CipherSuite = "TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256"
+	CipherSuiteTLSECDHEECDSAWithCHACHA20POLY1305SHA256 CipherSuite = "TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256"
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
+)
+
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// MinTLSVersion specifies the minimum TLS version that is acceptable.
+	// If not specified, defaults to "1.2" for backward compatibility.
+	// +optional
+	MinTLSVersion TLSVersion `json:"minTLSVersion,omitempty"`
+
+	// CipherSuites specifies the list of enabled TLS cipher suites.
+	// If not specified, a secure default list will be used.
+	// The available cipher suites are defined in Go's crypto/tls package.
+	// See https://golang.org/pkg/crypto/tls/#pkg-constants for valid values.
+	// +optional
+	CipherSuites []CipherSuite `json:"cipherSuites,omitempty"`
+}
+
+// Configuration defines the desired state of Configuration
+type Configuration struct {
+	// ... existing fields ...
+
+	// TLS contains TLS security settings for all Kueue API servers
+	// (webhooks, metrics, controller, and visibility).
+	// +optional
+	TLS *TLSOptions `json:"tls,omitempty"`
+}
+```
+
+We will take only the "secure" TLS options according to golang.
+The list is taken from [golang](https://cs.opensource.google/go/go/+/refs/tags/go1.25.5:src/crypto/tls/cipher_suites.go;l=56).
```

  sorry I pushed a large update so this is no longer needed.
  
  The general consesus was maintaing this list is too much of a headache so we will drop the enum api and just use strings. And rely on component-base to do the validation of the ciphersuite (like we have in Kubernetes).

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-13T17:32:06Z

- **keps/8190-tls-security-profiles/README.md:99** by [@kannon92](https://github.com/kannon92) — 2026-01-13T17:32:06Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
```

  The motivation factor for us is to have consistent settings for all components.
  
  ie we would like to make sure every component in our cluster is using TLS 1.3 (or TLS 1.2 with the same CipherSuite).
  
  I would strongly not recommend different TLS policies right now.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-13T17:33:45Z

- **keps/8190-tls-security-profiles/kep.yaml:16** by [@kannon92](https://github.com/kannon92) — 2026-01-13T17:33:45Z
```diff
@@ -0,0 +1,25 @@
+title: TLS Security Profile Support
+kep-number: 8190
+authors:
+  - "@kannon92"
+status: implementable 
+creation-date: 2025-12-11
+reviewers:
+  - kshalot
+approvers:
+  - mimowo
+
+see-also:
+replaces:
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: stable
```

  Well the api field provides the "Gate". 
  
  Its pretty important for my org that this goes in as beta on as we consider this a serious bug rather than a feature. I was thinking that since this is opt-in it should be fine to go in as stable. People that don't want this change don't need to set the API.
  
  But if you think a feature gate is needed I'm fine to add one

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-01-13T17:35:01Z

- **keps/8190-tls-security-profiles/README.md:204** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-13T17:35:01Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES256CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES128CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES256CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithCHACHA20POLY1305SHA256   CipherSuite = "TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256"
+	CipherSuiteTLSECDHEECDSAWithCHACHA20POLY1305SHA256 CipherSuite = "TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256"
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
+)
+
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// MinTLSVersion specifies the minimum TLS version that is acceptable.
+	// If not specified, defaults to "1.2" for backward compatibility.
+	// +optional
+	MinTLSVersion TLSVersion `json:"minTLSVersion,omitempty"`
+
+	// CipherSuites specifies the list of enabled TLS cipher suites.
+	// If not specified, a secure default list will be used.
+	// The available cipher suites are defined in Go's crypto/tls package.
+	// See https://golang.org/pkg/crypto/tls/#pkg-constants for valid values.
+	// +optional
+	CipherSuites []CipherSuite `json:"cipherSuites,omitempty"`
+}
+
+// Configuration defines the desired state of Configuration
+type Configuration struct {
+	// ... existing fields ...
+
+	// TLS contains TLS security settings for all Kueue API servers
+	// (webhooks, metrics, controller, and visibility).
+	// +optional
+	TLS *TLSOptions `json:"tls,omitempty"`
+}
+```
+
+We will take only the "secure" TLS options according to golang.
+The list is taken from [golang](https://cs.opensource.google/go/go/+/refs/tags/go1.25.5:src/crypto/tls/cipher_suites.go;l=56).
```

  Oh, I see. When I reviewed this KEP, I saw the items. No worries, I will try to review that again, thank you :)

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-01-13T17:38:16Z

- **keps/8190-tls-security-profiles/kep.yaml:16** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-13T17:38:16Z
```diff
@@ -0,0 +1,25 @@
+title: TLS Security Profile Support
+kep-number: 8190
+authors:
+  - "@kannon92"
+status: implementable 
+creation-date: 2025-12-11
+reviewers:
+  - kshalot
+approvers:
+  - mimowo
+
+see-also:
+replaces:
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: stable
```

  I think it would be worth introducing FG to enable and disable defaulting and verifying these mechanism as another point. Starting Beta is acceptable for me if you want, because adding APIs indicates that we will never remove this feature at all.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-13T17:44:58Z

- **keps/8190-tls-security-profiles/README.md:279** by [@kannon92](https://github.com/kannon92) — 2026-01-13T17:44:57Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Enum-Based API](#enum-based-api)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration using a string-based API:
+
+```golang
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// tlsMinVersion is the minimum TLS version supported.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// Valid values are: "VersionTLS12", "VersionTLS13"
+	// Default: "" (uses Go's default, currently TLS 1.2)
+	// +optional
+	TLSMinVersion string `json:"tlsMinVersion,omitempty"`
+
+	// tlsCipherSuites is the list of allowed cipher suites for the server.
+	// Note that TLS 1.3 ciphersuites are not configurable.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// Default: nil (uses Go's default cipher suite selection)
+	// +optional
+	TLSCipherSuites []string `json:"tlsCipherSuites,omitempty"`
+}
+
+// ControllerManager is added to the Configuration struct
+type ControllerManager struct {
+	// ... existing fields ...
+
+	// TLS contains TLS security settings for all Kueue API servers
+	// (webhooks, metrics, and visibility).
+	// +optional
+	TLS *TLSOptions `json:"tls,omitempty"`
+}
+```
+
+This approach uses string values that are validated at runtime against Go's crypto/tls package constants.
+This provides flexibility for future TLS versions and cipher suites without requiring API changes.
+
+### Supported Servers
+
+The TLS configuration will be applied to the following Kueue servers:
+
+1. **Webhook Server**: Admission webhook endpoints for validating and mutating resources
+2. **Metrics Server**: Prometheus metrics endpoint (when secure metrics are enabled)
+3. **Visibility Server**: API server for workload visibility queries
+4. **Health Probe Server** (if applicable): Health check endpoints
+
+### Default Values
+
+To maintain backward compatibility and provide secure defaults:
+
+- **TLSMinVersion**: "" (empty string, uses Go's default which is TLS 1.2)
+  - Rationale: TLS 1.2 is widely supported and required by most compliance frameworks
+  - TLS 1.0 and 1.1 are deprecated and will not be supported
+  - Valid values: "VersionTLS12", "VersionTLS13"
+
+- **TLSCipherSuites**: nil (uses Go's default secure cipher suite selection)
+  - For TLS 1.2, recommended cipher suites include:
+    - TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256
+    - TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384
+    - TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256
+    - TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384
+  - For TLS 1.3, cipher suites are not configurable (Go uses the built-in secure set)
+  - Valid cipher suite values are defined in Go's crypto/tls package constants
+
+### Implementation Approach
+
+The implementation will follow these steps:
+
+1. **Configuration Loading**: Parse TLS options from the controller configuration
+2. **Validation**: Validate TLS version and cipher suite strings against Go's crypto/tls package at startup
+3. **TLS Config Construction**: Build a `tls.Config` object with the specified options
+4. **Server Configuration**: Apply the TLS config to all relevant servers using `TLSOpts`
+
+Example implementation for applying TLS configuration:
+
+```golang
+func buildTLSConfig(cfg *config.TLSOptions) ([]func(*tls.Config), error) {
+	if cfg == nil {
+		return nil, nil
+	}
+
+	var tlsOpts []func(*tls.Config)
+
+	tlsOpts = append(tlsOpts, func(c *tls.Config) {
+		// Set minimum TLS version from string
+		if cfg.TLSMinVersion != "" {
+			version, err := tlsconfig.TLSVersion(cfg.TLSMinVersion)
+			if err == nil {
+				c.MinVersion = version
+			}
+		}
+
+		// Set cipher suites from string values
+		if len(cfg.TLSCipherSuites) > 0 {
+			suites, err := tlsconfig.TLSCipherSuites(cfg.TLSCipherSuites)
+			if err == nil {
+				c.CipherSuites = suites
+			}
+		}
+	})
+
+	return tlsOpts, nil
+}
+
+// Apply to webhook server
+webhookServer := webhook.NewServer(webhook.Options{
+	TLSOpts: tlsOpts,
+})
+
+// Apply to metrics server
+metricsServerOptions := metricsserver.Options{
+	TLSOpts: tlsOpts,
+}
+```
+
+The validation functions (`TLSVersion`, `TLSCipherSuites`) will use Go's crypto/tls package to validate the string values, ensuring only valid TLS versions and cipher suites are accepted.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+- Create test utilities for verifying TLS settings on running servers
+
+#### Unit Tests
+
+The following packages will have unit test coverage:
+
+- `pkg/controller/core`: Configuration loading
+  - Test TLS configurations are loaded correctly
+  - Test default values are applied correctly
+
+- `cmd/kueue`: TLS config application to servers
+  - Test TLS config is correctly applied to webhook server
+  - Test TLS config is correctly applied to metrics server
+  - Test TLS config is correctly applied to visibility server
+
+Coverage target: >80% for new TLS configuration code
+
+#### Integration tests
```

  @tenzen-y @mimowo @kshalot 
  
  I'm not entirely sure how much to test TLS versions?
  
  Since its not a default I think it has to be a CustomConfig test but its not really testing Kueue IMO.
  
  I did try changing the defaults and seeing if the CI would work. But there are a lot of different ways to deploy Kueue with TLS so curious on how much testing you think we need here.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-13T17:45:25Z

- **keps/8190-tls-security-profiles/README.md:279** by [@kannon92](https://github.com/kannon92) — 2026-01-13T17:45:25Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Enum-Based API](#enum-based-api)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration using a string-based API:
+
+```golang
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// tlsMinVersion is the minimum TLS version supported.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// Valid values are: "VersionTLS12", "VersionTLS13"
+	// Default: "" (uses Go's default, currently TLS 1.2)
+	// +optional
+	TLSMinVersion string `json:"tlsMinVersion,omitempty"`
+
+	// tlsCipherSuites is the list of allowed cipher suites for the server.
+	// Note that TLS 1.3 ciphersuites are not configurable.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// Default: nil (uses Go's default cipher suite selection)
+	// +optional
+	TLSCipherSuites []string `json:"tlsCipherSuites,omitempty"`
+}
+
+// ControllerManager is added to the Configuration struct
+type ControllerManager struct {
+	// ... existing fields ...
+
+	// TLS contains TLS security settings for all Kueue API servers
+	// (webhooks, metrics, and visibility).
+	// +optional
+	TLS *TLSOptions `json:"tls,omitempty"`
+}
+```
+
+This approach uses string values that are validated at runtime against Go's crypto/tls package constants.
+This provides flexibility for future TLS versions and cipher suites without requiring API changes.
+
+### Supported Servers
+
+The TLS configuration will be applied to the following Kueue servers:
+
+1. **Webhook Server**: Admission webhook endpoints for validating and mutating resources
+2. **Metrics Server**: Prometheus metrics endpoint (when secure metrics are enabled)
+3. **Visibility Server**: API server for workload visibility queries
+4. **Health Probe Server** (if applicable): Health check endpoints
+
+### Default Values
+
+To maintain backward compatibility and provide secure defaults:
+
+- **TLSMinVersion**: "" (empty string, uses Go's default which is TLS 1.2)
+  - Rationale: TLS 1.2 is widely supported and required by most compliance frameworks
+  - TLS 1.0 and 1.1 are deprecated and will not be supported
+  - Valid values: "VersionTLS12", "VersionTLS13"
+
+- **TLSCipherSuites**: nil (uses Go's default secure cipher suite selection)
+  - For TLS 1.2, recommended cipher suites include:
+    - TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256
+    - TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384
+    - TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256
+    - TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384
+  - For TLS 1.3, cipher suites are not configurable (Go uses the built-in secure set)
+  - Valid cipher suite values are defined in Go's crypto/tls package constants
+
+### Implementation Approach
+
+The implementation will follow these steps:
+
+1. **Configuration Loading**: Parse TLS options from the controller configuration
+2. **Validation**: Validate TLS version and cipher suite strings against Go's crypto/tls package at startup
+3. **TLS Config Construction**: Build a `tls.Config` object with the specified options
+4. **Server Configuration**: Apply the TLS config to all relevant servers using `TLSOpts`
+
+Example implementation for applying TLS configuration:
+
+```golang
+func buildTLSConfig(cfg *config.TLSOptions) ([]func(*tls.Config), error) {
+	if cfg == nil {
+		return nil, nil
+	}
+
+	var tlsOpts []func(*tls.Config)
+
+	tlsOpts = append(tlsOpts, func(c *tls.Config) {
+		// Set minimum TLS version from string
+		if cfg.TLSMinVersion != "" {
+			version, err := tlsconfig.TLSVersion(cfg.TLSMinVersion)
+			if err == nil {
+				c.MinVersion = version
+			}
+		}
+
+		// Set cipher suites from string values
+		if len(cfg.TLSCipherSuites) > 0 {
+			suites, err := tlsconfig.TLSCipherSuites(cfg.TLSCipherSuites)
+			if err == nil {
+				c.CipherSuites = suites
+			}
+		}
+	})
+
+	return tlsOpts, nil
+}
+
+// Apply to webhook server
+webhookServer := webhook.NewServer(webhook.Options{
+	TLSOpts: tlsOpts,
+})
+
+// Apply to metrics server
+metricsServerOptions := metricsserver.Options{
+	TLSOpts: tlsOpts,
+}
+```
+
+The validation functions (`TLSVersion`, `TLSCipherSuites`) will use Go's crypto/tls package to validate the string values, ensuring only valid TLS versions and cipher suites are accepted.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+- Create test utilities for verifying TLS settings on running servers
+
+#### Unit Tests
+
+The following packages will have unit test coverage:
+
+- `pkg/controller/core`: Configuration loading
+  - Test TLS configurations are loaded correctly
+  - Test default values are applied correctly
+
+- `cmd/kueue`: TLS config application to servers
+  - Test TLS config is correctly applied to webhook server
+  - Test TLS config is correctly applied to metrics server
+  - Test TLS config is correctly applied to visibility server
+
+Coverage target: >80% for new TLS configuration code
+
+#### Integration tests
```

  IMO I feel that the configuration can be handled via unit but the integration/e2e is really what I am curious on.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-13T17:51:27Z

- **keps/8190-tls-security-profiles/README.md:169** by [@kannon92](https://github.com/kannon92) — 2026-01-13T17:51:27Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+  - [String based api](#string-based-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration:
+
+```golang
+// TLSVersion represents a TLS version string
+// +kubebuilder:validation:Enum={"1.2","1.3"}
+type TLSVersion string
+
+const (
+	// TLSVersion12 represents TLS version 1.2
+	TLSVersion12 TLSVersion = "1.2"
+	// TLSVersion13 represents TLS version 1.3
+	TLSVersion13 TLSVersion = "1.3"
+)
+
+// CipherSuite represents a TLS cipher suite name
+// +kubebuilder:validation:Enum=TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA;TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA;TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384;TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256;TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256;TLS_AES_128_GCM_SHA256;TLS_AES_256_GCM_SHA384;TLS_CHACHA20_POLY1305_SHA256
+type CipherSuite string
+
+const (
+	// TLS 1.2 cipher suites
+	CipherSuiteTLSECDHEECDSAWithAES128CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES256CBCSHA           CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES128CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA"
+	CipherSuiteTLSECDHERSAWithAES256CBCSHA             CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA"
+	CipherSuiteTLSECDHEECDSAWithAES128GCMSHA256        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHEECDSAWithAES256GCMSHA384        CipherSuite = "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithAES128GCMSHA256          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
+	CipherSuiteTLSECDHERSAWithAES256GCMSHA384          CipherSuite = "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
+	CipherSuiteTLSECDHERSAWithCHACHA20POLY1305SHA256   CipherSuite = "TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256"
+	CipherSuiteTLSECDHEECDSAWithCHACHA20POLY1305SHA256 CipherSuite = "TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256"
+
+	// TLS 1.3 cipher suites
+	CipherSuiteTLSAES128GCMSHA256       CipherSuite = "TLS_AES_128_GCM_SHA256"
+	CipherSuiteTLSAES256GCMSHA384       CipherSuite = "TLS_AES_256_GCM_SHA384"
+	CipherSuiteTLSCHACHA20POLY1305SHA256 CipherSuite = "TLS_CHACHA20_POLY1305_SHA256"
+)
+
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// MinTLSVersion specifies the minimum TLS version that is acceptable.
+	// If not specified, defaults to "1.2" for backward compatibility.
+	// +optional
+	MinTLSVersion TLSVersion `json:"minTLSVersion,omitempty"`
+
+	// CipherSuites specifies the list of enabled TLS cipher suites.
+	// If not specified, a secure default list will be used.
+	// The available cipher suites are defined in Go's crypto/tls package.
+	// See https://golang.org/pkg/crypto/tls/#pkg-constants for valid values.
+	// +optional
+	CipherSuites []CipherSuite `json:"cipherSuites,omitempty"`
+}
+
+// Configuration defines the desired state of Configuration
+type Configuration struct {
+	// ... existing fields ...
+
+	// TLS contains TLS security settings for all Kueue API servers
+	// (webhooks, metrics, controller, and visibility).
+	// +optional
+	TLS *TLSOptions `json:"tls,omitempty"`
```

  hmm. No I don't see a reason to allow tls: {} would be the same as not specifying.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-13T17:51:44Z

- **keps/8190-tls-security-profiles/kep.yaml:10** by [@kannon92](https://github.com/kannon92) — 2026-01-13T17:51:44Z
```diff
@@ -0,0 +1,25 @@
+title: TLS Security Profile Support
+kep-number: 8190
+authors:
+  - "@kannon92"
+status: implementable 
+creation-date: 2025-12-11
+reviewers:
+  - kshalot
+approvers:
+  - mimowo
```

  updated.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-14T08:11:17Z

- **keps/8190-tls-security-profiles/README.md:279** by [@mimowo](https://github.com/mimowo) — 2026-01-14T08:11:17Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Enum-Based API](#enum-based-api)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration using a string-based API:
+
+```golang
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// tlsMinVersion is the minimum TLS version supported.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// Valid values are: "VersionTLS12", "VersionTLS13"
+	// Default: "" (uses Go's default, currently TLS 1.2)
+	// +optional
+	TLSMinVersion string `json:"tlsMinVersion,omitempty"`
+
+	// tlsCipherSuites is the list of allowed cipher suites for the server.
+	// Note that TLS 1.3 ciphersuites are not configurable.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// Default: nil (uses Go's default cipher suite selection)
+	// +optional
+	TLSCipherSuites []string `json:"tlsCipherSuites,omitempty"`
+}
+
+// ControllerManager is added to the Configuration struct
+type ControllerManager struct {
+	// ... existing fields ...
+
+	// TLS contains TLS security settings for all Kueue API servers
+	// (webhooks, metrics, and visibility).
+	// +optional
+	TLS *TLSOptions `json:"tls,omitempty"`
+}
+```
+
+This approach uses string values that are validated at runtime against Go's crypto/tls package constants.
+This provides flexibility for future TLS versions and cipher suites without requiring API changes.
+
+### Supported Servers
+
+The TLS configuration will be applied to the following Kueue servers:
+
+1. **Webhook Server**: Admission webhook endpoints for validating and mutating resources
+2. **Metrics Server**: Prometheus metrics endpoint (when secure metrics are enabled)
+3. **Visibility Server**: API server for workload visibility queries
+4. **Health Probe Server** (if applicable): Health check endpoints
+
+### Default Values
+
+To maintain backward compatibility and provide secure defaults:
+
+- **TLSMinVersion**: "" (empty string, uses Go's default which is TLS 1.2)
+  - Rationale: TLS 1.2 is widely supported and required by most compliance frameworks
+  - TLS 1.0 and 1.1 are deprecated and will not be supported
+  - Valid values: "VersionTLS12", "VersionTLS13"
+
+- **TLSCipherSuites**: nil (uses Go's default secure cipher suite selection)
+  - For TLS 1.2, recommended cipher suites include:
+    - TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256
+    - TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384
+    - TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256
+    - TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384
+  - For TLS 1.3, cipher suites are not configurable (Go uses the built-in secure set)
+  - Valid cipher suite values are defined in Go's crypto/tls package constants
+
+### Implementation Approach
+
+The implementation will follow these steps:
+
+1. **Configuration Loading**: Parse TLS options from the controller configuration
+2. **Validation**: Validate TLS version and cipher suite strings against Go's crypto/tls package at startup
+3. **TLS Config Construction**: Build a `tls.Config` object with the specified options
+4. **Server Configuration**: Apply the TLS config to all relevant servers using `TLSOpts`
+
+Example implementation for applying TLS configuration:
+
+```golang
+func buildTLSConfig(cfg *config.TLSOptions) ([]func(*tls.Config), error) {
+	if cfg == nil {
+		return nil, nil
+	}
+
+	var tlsOpts []func(*tls.Config)
+
+	tlsOpts = append(tlsOpts, func(c *tls.Config) {
+		// Set minimum TLS version from string
+		if cfg.TLSMinVersion != "" {
+			version, err := tlsconfig.TLSVersion(cfg.TLSMinVersion)
+			if err == nil {
+				c.MinVersion = version
+			}
+		}
+
+		// Set cipher suites from string values
+		if len(cfg.TLSCipherSuites) > 0 {
+			suites, err := tlsconfig.TLSCipherSuites(cfg.TLSCipherSuites)
+			if err == nil {
+				c.CipherSuites = suites
+			}
+		}
+	})
+
+	return tlsOpts, nil
+}
+
+// Apply to webhook server
+webhookServer := webhook.NewServer(webhook.Options{
+	TLSOpts: tlsOpts,
+})
+
+// Apply to metrics server
+metricsServerOptions := metricsserver.Options{
+	TLSOpts: tlsOpts,
+}
+```
+
+The validation functions (`TLSVersion`, `TLSCipherSuites`) will use Go's crypto/tls package to validate the string values, ensuring only valid TLS versions and cipher suites are accepted.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+- Create test utilities for verifying TLS settings on running servers
+
+#### Unit Tests
+
+The following packages will have unit test coverage:
+
+- `pkg/controller/core`: Configuration loading
+  - Test TLS configurations are loaded correctly
+  - Test default values are applied correctly
+
+- `cmd/kueue`: TLS config application to servers
+  - Test TLS config is correctly applied to webhook server
+  - Test TLS config is correctly applied to metrics server
+  - Test TLS config is correctly applied to visibility server
+
+Coverage target: >80% for new TLS configuration code
+
+#### Integration tests
```

  I think it would be helpful to have some integration or e2e tests. I think we have the customconfigs suite, maybe we could use it for changing the configuration and seeing to still works,  wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-14T08:22:46Z

- **keps/8190-tls-security-profiles/README.md:209** by [@mimowo](https://github.com/mimowo) — 2026-01-14T08:22:46Z
```diff
@@ -0,0 +1,398 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Enum-Based API](#enum-based-api)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration using a string-based API:
+
+```golang
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// tlsMinVersion is the minimum TLS version supported.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// Valid values are: "VersionTLS12", "VersionTLS13"
+	// Default: "" (uses Go's default, currently TLS 1.2)
+	// +optional
+	TLSMinVersion string `json:"tlsMinVersion,omitempty"`
+
+	// tlsCipherSuites is the list of allowed cipher suites for the server.
+	// Note that TLS 1.3 ciphersuites are not configurable.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// Default: nil (uses Go's default cipher suite selection)
+	// +optional
+	TLSCipherSuites []string `json:"tlsCipherSuites,omitempty"`
+}
+
+// ControllerManager is added to the Configuration struct
+type ControllerManager struct {
+	// ... existing fields ...
+
+	// TLS contains TLS security settings for all Kueue API servers
+	// (webhooks, metrics, and visibility).
+	// +optional
+	TLS *TLSOptions `json:"tls,omitempty"`
+}
+```
+
+This approach uses string values that are validated at runtime against Go's crypto/tls package constants.
+This provides flexibility for future TLS versions and cipher suites without requiring API changes.
+
+### Supported Servers
+
+The TLS configuration will be applied to the following Kueue servers:
+
+1. **Webhook Server**: Admission webhook endpoints for validating and mutating resources
+2. **Metrics Server**: Prometheus metrics endpoint (when secure metrics are enabled)
+3. **Visibility Server**: API server for workload visibility queries
+4. **Health Probe Server** (if applicable): Health check endpoints
+
+### Default Values
+
+To maintain backward compatibility and provide secure defaults:
+
+- **TLSMinVersion**: "" (empty string, uses Go's default which is TLS 1.2)
+  - Rationale: TLS 1.2 is widely supported and required by most compliance frameworks
+  - TLS 1.0 and 1.1 are deprecated and will not be supported
+  - Valid values: "VersionTLS12", "VersionTLS13"
+
+- **TLSCipherSuites**: nil (uses Go's default secure cipher suite selection)
+  - For TLS 1.2, recommended cipher suites include:
+    - TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256
+    - TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384
+    - TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256
+    - TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384
+  - For TLS 1.3, cipher suites are not configurable (Go uses the built-in secure set)
+  - Valid cipher suite values are defined in Go's crypto/tls package constants
+
+### Implementation Approach
+
+The implementation will follow these steps:
+
+1. **Configuration Loading**: Parse TLS options from the controller configuration
+2. **Validation**: Validate TLS version and cipher suite strings against Go's crypto/tls package at startup
```

  Maybe add do the validation point: ... similarly as in Kubelet (see [here](https://github.com/kubernetes/kubernetes/blob/97bd924010a39b71d3fd802e4284418c8b79545c/cmd/kubelet/app/server.go#L1134-L1148)), wdyt? 
  
  This would be useful to show how this can be done, and that we are following the pattern already used in k8s core.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-14T16:11:38Z

- **keps/8190-tls-security-profiles/README.md:209** by [@kannon92](https://github.com/kannon92) — 2026-01-14T16:11:38Z
```diff
@@ -0,0 +1,398 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Enum-Based API](#enum-based-api)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration using a string-based API:
+
+```golang
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// tlsMinVersion is the minimum TLS version supported.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// Valid values are: "VersionTLS12", "VersionTLS13"
+	// Default: "" (uses Go's default, currently TLS 1.2)
+	// +optional
+	TLSMinVersion string `json:"tlsMinVersion,omitempty"`
+
+	// tlsCipherSuites is the list of allowed cipher suites for the server.
+	// Note that TLS 1.3 ciphersuites are not configurable.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// Default: nil (uses Go's default cipher suite selection)
+	// +optional
+	TLSCipherSuites []string `json:"tlsCipherSuites,omitempty"`
+}
+
+// ControllerManager is added to the Configuration struct
+type ControllerManager struct {
+	// ... existing fields ...
+
+	// TLS contains TLS security settings for all Kueue API servers
+	// (webhooks, metrics, and visibility).
+	// +optional
+	TLS *TLSOptions `json:"tls,omitempty"`
+}
+```
+
+This approach uses string values that are validated at runtime against Go's crypto/tls package constants.
+This provides flexibility for future TLS versions and cipher suites without requiring API changes.
+
+### Supported Servers
+
+The TLS configuration will be applied to the following Kueue servers:
+
+1. **Webhook Server**: Admission webhook endpoints for validating and mutating resources
+2. **Metrics Server**: Prometheus metrics endpoint (when secure metrics are enabled)
+3. **Visibility Server**: API server for workload visibility queries
+4. **Health Probe Server** (if applicable): Health check endpoints
+
+### Default Values
+
+To maintain backward compatibility and provide secure defaults:
+
+- **TLSMinVersion**: "" (empty string, uses Go's default which is TLS 1.2)
+  - Rationale: TLS 1.2 is widely supported and required by most compliance frameworks
+  - TLS 1.0 and 1.1 are deprecated and will not be supported
+  - Valid values: "VersionTLS12", "VersionTLS13"
+
+- **TLSCipherSuites**: nil (uses Go's default secure cipher suite selection)
+  - For TLS 1.2, recommended cipher suites include:
+    - TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256
+    - TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384
+    - TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256
+    - TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384
+  - For TLS 1.3, cipher suites are not configurable (Go uses the built-in secure set)
+  - Valid cipher suite values are defined in Go's crypto/tls package constants
+
+### Implementation Approach
+
+The implementation will follow these steps:
+
+1. **Configuration Loading**: Parse TLS options from the controller configuration
+2. **Validation**: Validate TLS version and cipher suite strings against Go's crypto/tls package at startup
```

  I added some links for examples for secure serving for apiserver and the kubelet validation.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-14T16:12:54Z

- **keps/8190-tls-security-profiles/README.md:279** by [@kannon92](https://github.com/kannon92) — 2026-01-14T16:12:53Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Enum-Based API](#enum-based-api)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration using a string-based API:
+
+```golang
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// tlsMinVersion is the minimum TLS version supported.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// Valid values are: "VersionTLS12", "VersionTLS13"
+	// Default: "" (uses Go's default, currently TLS 1.2)
+	// +optional
+	TLSMinVersion string `json:"tlsMinVersion,omitempty"`
+
+	// tlsCipherSuites is the list of allowed cipher suites for the server.
+	// Note that TLS 1.3 ciphersuites are not configurable.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// Default: nil (uses Go's default cipher suite selection)
+	// +optional
+	TLSCipherSuites []string `json:"tlsCipherSuites,omitempty"`
+}
+
+// ControllerManager is added to the Configuration struct
+type ControllerManager struct {
+	// ... existing fields ...
+
+	// TLS contains TLS security settings for all Kueue API servers
+	// (webhooks, metrics, and visibility).
+	// +optional
+	TLS *TLSOptions `json:"tls,omitempty"`
+}
+```
+
+This approach uses string values that are validated at runtime against Go's crypto/tls package constants.
+This provides flexibility for future TLS versions and cipher suites without requiring API changes.
+
+### Supported Servers
+
+The TLS configuration will be applied to the following Kueue servers:
+
+1. **Webhook Server**: Admission webhook endpoints for validating and mutating resources
+2. **Metrics Server**: Prometheus metrics endpoint (when secure metrics are enabled)
+3. **Visibility Server**: API server for workload visibility queries
+4. **Health Probe Server** (if applicable): Health check endpoints
+
+### Default Values
+
+To maintain backward compatibility and provide secure defaults:
+
+- **TLSMinVersion**: "" (empty string, uses Go's default which is TLS 1.2)
+  - Rationale: TLS 1.2 is widely supported and required by most compliance frameworks
+  - TLS 1.0 and 1.1 are deprecated and will not be supported
+  - Valid values: "VersionTLS12", "VersionTLS13"
+
+- **TLSCipherSuites**: nil (uses Go's default secure cipher suite selection)
+  - For TLS 1.2, recommended cipher suites include:
+    - TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256
+    - TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384
+    - TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256
+    - TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384
+  - For TLS 1.3, cipher suites are not configurable (Go uses the built-in secure set)
+  - Valid cipher suite values are defined in Go's crypto/tls package constants
+
+### Implementation Approach
+
+The implementation will follow these steps:
+
+1. **Configuration Loading**: Parse TLS options from the controller configuration
+2. **Validation**: Validate TLS version and cipher suite strings against Go's crypto/tls package at startup
+3. **TLS Config Construction**: Build a `tls.Config` object with the specified options
+4. **Server Configuration**: Apply the TLS config to all relevant servers using `TLSOpts`
+
+Example implementation for applying TLS configuration:
+
+```golang
+func buildTLSConfig(cfg *config.TLSOptions) ([]func(*tls.Config), error) {
+	if cfg == nil {
+		return nil, nil
+	}
+
+	var tlsOpts []func(*tls.Config)
+
+	tlsOpts = append(tlsOpts, func(c *tls.Config) {
+		// Set minimum TLS version from string
+		if cfg.TLSMinVersion != "" {
+			version, err := tlsconfig.TLSVersion(cfg.TLSMinVersion)
+			if err == nil {
+				c.MinVersion = version
+			}
+		}
+
+		// Set cipher suites from string values
+		if len(cfg.TLSCipherSuites) > 0 {
+			suites, err := tlsconfig.TLSCipherSuites(cfg.TLSCipherSuites)
+			if err == nil {
+				c.CipherSuites = suites
+			}
+		}
+	})
+
+	return tlsOpts, nil
+}
+
+// Apply to webhook server
+webhookServer := webhook.NewServer(webhook.Options{
+	TLSOpts: tlsOpts,
+})
+
+// Apply to metrics server
+metricsServerOptions := metricsserver.Options{
+	TLSOpts: tlsOpts,
+}
+```
+
+The validation functions (`TLSVersion`, `TLSCipherSuites`) will use Go's crypto/tls package to validate the string values, ensuring only valid TLS versions and cipher suites are accepted.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+- Create test utilities for verifying TLS settings on running servers
+
+#### Unit Tests
+
+The following packages will have unit test coverage:
+
+- `pkg/controller/core`: Configuration loading
+  - Test TLS configurations are loaded correctly
+  - Test default values are applied correctly
+
+- `cmd/kueue`: TLS config application to servers
+  - Test TLS config is correctly applied to webhook server
+  - Test TLS config is correctly applied to metrics server
+  - Test TLS config is correctly applied to visibility server
+
+Coverage target: >80% for new TLS configuration code
+
+#### Integration tests
```

  Yea I added a note to add that.
  
  The only issue is that I want to run things like visibility or metrics but I don't want to duplicate the tests.
  
  I think I'd like to be able to configure kueue and then select a subset of tests to run. I'll look into how to do that.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-14T16:13:02Z

- **keps/8190-tls-security-profiles/README.md:237** by [@kannon92](https://github.com/kannon92) — 2026-01-14T16:13:02Z
```diff
@@ -0,0 +1,435 @@
+# KEP-8190: TLS Security Profile Support
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API Change](#configuration-api-change)
+  - [Supported Servers](#supported-servers)
+  - [Default Values](#default-values)
+  - [Implementation Approach](#implementation-approach)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Enum-Based API](#enum-based-api)
+  - [Server-Specific Configuration](#server-specific-configuration)
+  - [Environment Variables](#environment-variables)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes adding configurable TLS security profiles to Kueue's API servers (webhooks, metrics, controller, and visibility).
+This enhancement will allow administrators to explicitly configure the minimum TLS version and cipher suites
+for all TLS-enabled servers in Kueue, improving security posture, compliance, and compatibility with organizational security policies.
+
+## Motivation
+
+In Go-based projects, explicitly configuring TLS settings is crucial for security, compatibility, and compliance. Organizations
+often have strict security requirements that mandate specific TLS versions and cipher suites. Currently, Kueue relies on Go's
+default TLS configurations, which may not align with organizational security policies or compliance requirements (e.g., PCI-DSS,
+FIPS 140-2, SOC 2).
+
+The lack of configurable TLS settings creates several challenges:
+- Organizations cannot enforce minimum TLS version requirements (e.g., TLS 1.2 or 1.3 only)
+- Administrators cannot disable weak or deprecated cipher suites
+- Compliance audits may flag the use of default TLS configurations as a security concern
+- Integration with security scanning tools that require specific TLS configurations is difficult
+
+By providing explicit TLS configuration options, Kueue can better serve enterprise environments with stringent security requirements
+while maintaining backward compatibility through sensible defaults.
+
+### Goals
+
+- Provide configuration options for minimum TLS version across all Kueue API servers
+- Enable administrators to specify allowed cipher suites for TLS connections
+- Apply TLS settings uniformly across webhook, metrics, controller, and visibility servers
+- Maintain backward compatibility with existing deployments through sensible defaults
+- Document TLS configuration best practices and security implications
+
+### Non-Goals
+
+- Enforcing specific TLS configurations by default (the feature should be opt-in)
+- Providing automated certificate rotation beyond what already exists in Kueue
+- Supporting per-server TLS configuration (all servers will use the same TLS profile)
+- Implementing custom TLS handshake logic beyond Go's standard library capabilities
+- Supporting TLS versions below 1.2 (which are deprecated and insecure)
+- Providing a compatibility mode for legacy TLS 1.0/1.1 clients
+
+## Proposal
+
+We propose adding TLS configuration options to Kueue's configuration API that will be applied to all TLS-enabled servers.
+The configuration will be part of the controller configuration and will allow administrators to set:
+
+1. Minimum TLS version (1.2 or 1.3)
+2. Allowed cipher suites (based on Go's crypto/tls package)
+
+These settings will be applied uniformly across all Kueue servers (webhooks, metrics, controller, and visibility) to ensure
+consistent security posture across the entire Kueue deployment.
+
+### User Stories
+
+#### Story 1
+
+As a platform administrator in a regulated industry (financial services, healthcare), I need to ensure that all
+components in my Kubernetes cluster use TLS 1.3 with approved cipher suites to meet compliance requirements (PCI-DSS, HIPAA).
+I want to configure Kueue to enforce these requirements across all its API endpoints.
+
+#### Story 2
+
+As a security engineer, I need to disable weak cipher suites and enforce strong cryptographic algorithms across all
+services in my cluster. I want to configure Kueue's TLS settings to align with our organization's security baseline,
+which requires specific cipher suites approved by our security team.
+
+### Risks and Mitigations
+
+**Risk**: Misconfiguration of TLS settings could prevent clients from connecting to Kueue servers.
+
+**Mitigation**:
+- Provide comprehensive documentation with examples and best practices
+- Use sensible defaults that work for most environments
+- Log clear error messages when TLS configuration issues occur
+- Server will fail to start with clear error messages if TLS configuration is invalid
+
+**Risk**: Overly restrictive TLS settings could break compatibility with existing clients or monitoring tools.
+
+**Mitigation**:
+- Make the feature opt-in with backward-compatible defaults
+- Document the cipher suites and TLS versions supported by common clients (kubectl, Prometheus, etc.)
+- Provide migration guide for upgrading from default to custom TLS configurations
+- Include troubleshooting section in documentation for common TLS connectivity issues
+
+**Risk**: Performance impact from enforcing certain cipher suites or TLS versions.
+
+**Mitigation**:
+- Document performance characteristics of different cipher suites
+- Recommend modern, hardware-accelerated cipher suites (AES-GCM) as defaults
+- Provide benchmarking guidance for administrators to test performance impact
+
+## Design Details
+
+### Configuration API Change
+
+We will add a new `TLSOptions` structure to the controller configuration using a string-based API:
+
+```golang
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// tlsMinVersion is the minimum TLS version supported.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// Valid values are: "VersionTLS12", "VersionTLS13"
+	// Default: "" (uses Go's default, currently TLS 1.2)
+	// +optional
+	TLSMinVersion string `json:"tlsMinVersion,omitempty"`
+
+	// tlsCipherSuites is the list of allowed cipher suites for the server.
+	// Note that TLS 1.3 ciphersuites are not configurable.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// Default: nil (uses Go's default cipher suite selection)
+	// +optional
+	TLSCipherSuites []string `json:"tlsCipherSuites,omitempty"`
+}
+
+// ControllerManager is added to the Configuration struct
+type ControllerManager struct {
+	// ... existing fields ...
+
+	// TLS contains TLS security settings for all Kueue API servers
+	// (webhooks, metrics, and visibility).
+	// +optional
+	TLS *TLSOptions `json:"tls,omitempty"`
+}
+```
+
+This approach uses string values that are validated at runtime against Go's crypto/tls package constants.
+This provides flexibility for future TLS versions and cipher suites without requiring API changes.
+
+### Supported Servers
+
+The TLS configuration will be applied to the following Kueue servers:
+
+1. **Webhook Server**: Admission webhook endpoints for validating and mutating resources
+2. **Metrics Server**: Prometheus metrics endpoint (when secure metrics are enabled)
+3. **Visibility Server**: API server for workload visibility queries
+4. **Health Probe Server** (if applicable): Health check endpoints
+
+### Default Values
+
+To maintain backward compatibility and provide secure defaults:
+
+- **TLSMinVersion**: "" (empty string, uses Go's default which is TLS 1.2)
+  - Rationale: TLS 1.2 is widely supported and required by most compliance frameworks
+  - TLS 1.0 and 1.1 are deprecated and will not be supported
+  - Valid values: "VersionTLS12", "VersionTLS13"
+
+- **TLSCipherSuites**: nil (uses Go's default secure cipher suite selection)
+  - For TLS 1.2, recommended cipher suites include:
+    - TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256
+    - TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384
+    - TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256
+    - TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384
+  - For TLS 1.3, cipher suites are not configurable (Go uses the built-in secure set)
+  - Valid cipher suite values are defined in Go's crypto/tls package constants
+
+### Implementation Approach
+
+The implementation will follow these steps:
+
+1. **Configuration Loading**: Parse TLS options from the controller configuration
+2. **Validation**: Validate TLS version and cipher suite strings against Go's crypto/tls package at startup
+3. **TLS Config Construction**: Build a `tls.Config` object with the specified options
+4. **Server Configuration**: Apply the TLS config to all relevant servers using `TLSOpts`
+
+Example implementation for applying TLS configuration:
+
+```golang
+func buildTLSConfig(cfg *config.TLSOptions) ([]func(*tls.Config), error) {
+	if cfg == nil {
+		return nil, nil
+	}
+
+	var tlsOpts []func(*tls.Config)
+
+	tlsOpts = append(tlsOpts, func(c *tls.Config) {
+		// Set minimum TLS version from string
+		if cfg.TLSMinVersion != "" {
+			version, err := tlsconfig.TLSVersion(cfg.TLSMinVersion)
+			if err == nil {
+				c.MinVersion = version
+			}
+		}
+
+		// Set cipher suites from string values
+		if len(cfg.TLSCipherSuites) > 0 {
+			suites, err := tlsconfig.TLSCipherSuites(cfg.TLSCipherSuites)
+			if err == nil {
+				c.CipherSuites = suites
+			}
+		}
+	})
+
```

  updated.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-14T16:13:24Z

- **keps/8190-tls-security-profiles/kep.yaml:16** by [@kannon92](https://github.com/kannon92) — 2026-01-14T16:13:24Z
```diff
@@ -0,0 +1,25 @@
+title: TLS Security Profile Support
+kep-number: 8190
+authors:
+  - "@kannon92"
+status: implementable 
+creation-date: 2025-12-11
+reviewers:
+  - kshalot
+approvers:
+  - mimowo
+
+see-also:
+replaces:
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: stable
```

  updated with a feature gate.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-01-16T17:39:56Z

@kannon92 Awesome, thank you!
/lgtm
/approve
