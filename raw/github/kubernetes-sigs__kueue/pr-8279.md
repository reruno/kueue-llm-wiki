# PR #8279: TAS: fixed performance issue due to unncessary (empty) request by TopologyUngater

**Summary**: TAS: fixed performance issue due to unncessary (empty) request by TopologyUngater

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8279

**Last updated**: 2026-01-23T11:52:14Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `cleanup/remove-unncessary-request-by-TopologyUngater` → `main`
- **Created**: 2025-12-16T16:58:55Z
- **Updated**: 2026-01-23T11:52:14Z
- **Merged**: 2025-12-18T15:11:19Z
- **Closed**: 2025-12-18T15:11:19Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `approved`, `cncf-cla: yes`, `size/S`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 2   **+9 / -7**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Fix performance issue due to unncessary (empty) request by TopologyUngater

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #8277

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
TAS: fixed performance issue due to unnecessary (empty) request by TopologyUngater
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-12-16T16:59:03Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 41417f060bfe9bfbff1e372e02a254265c8d4331 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/694412268d4a5f0008c2e4b1 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-8279--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-8279--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTgyNzktLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.44cIgIoEQTGGINeFZBHZtE59o437R247ql_5tOdmfBI)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-8279--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@mimowo](https://github.com/mimowo) — 2025-12-18T15:00:57Z

/lgtm
/approve
/cherrypick release-0.15
/cherrypick release-0.14

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-12-18T15:01:00Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.14`, `release-0.15` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8279#issuecomment-3670740079):

>/lgtm
>/approve
>/cherrypick release-0.15
>/cherrypick release-0.14


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-18T15:01:06Z

LGTM label has been added.  <details>Git tree hash: bbf36747886ca325c12310f0d39ebee036ce9bb6</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-18T15:01:07Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8279#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8279#issuecomment-3670740079" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-12-18T15:11:57Z

@mimowo: #8279 failed to apply on top of branch "release-0.14":
```
Applying: TAS: Fix performance issue due to unncessary (empty) request by TopologyUngater
Using index info to reconstruct a base tree...
M	pkg/controller/tas/topology_ungater.go
M	pkg/controller/tas/topology_ungater_test.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/controller/tas/topology_ungater_test.go
CONFLICT (content): Merge conflict in pkg/controller/tas/topology_ungater_test.go
Auto-merging pkg/controller/tas/topology_ungater.go
CONFLICT (content): Merge conflict in pkg/controller/tas/topology_ungater.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 TAS: Fix performance issue due to unncessary (empty) request by TopologyUngater

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8279#issuecomment-3670740079):

>/lgtm
>/approve
>/cherrypick release-0.15
>/cherrypick release-0.14


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-12-18T15:12:32Z

@mimowo: new pull request created: #8333

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8279#issuecomment-3670740079):

>/lgtm
>/approve
>/cherrypick release-0.15
>/cherrypick release-0.14


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-23T11:52:11Z

/release-note-edit
```release-note
TAS: fixed performance issue due to unnecessary (empty) request by TopologyUngater
```

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-16T17:47:27Z

- **pkg/controller/tas/topology_ungater.go:248** by [@mimowo](https://github.com/mimowo) — 2025-12-16T17:47:27Z
```diff
@@ -243,13 +244,18 @@ func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request)
 		podWithUngateInfo := &allToUngate[i]
 		var ungated bool
 		e := utilclient.Patch(ctx, r.client, podWithUngateInfo.pod, func() (bool, error) {
-			log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
 			ungated = utilpod.Ungate(podWithUngateInfo.pod, kueue.TopologySchedulingGate)
-			if podWithUngateInfo.pod.Spec.NodeSelector == nil {
-				podWithUngateInfo.pod.Spec.NodeSelector = make(map[string]string)
+			if !equality.Semantic.DeepEqual(podWithUngateInfo.pod.Spec.NodeSelector, podWithUngateInfo.nodeLabels) {
```

  Please add test cases:
  1. NodeSelector is specified already, so `equality.Semantic.DeepEqual(podWithUngateInfo.pod.Spec.NodeSelector, podWithUngateInfo.nodeLabels)` is true, to verify that we still would ungate in this case
  2. to demonstrate the client does not send the request. I think this could be done in unit test for topology_ungater_test.go by interceptor which always errors.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-12-17T09:21:53Z

- **pkg/controller/tas/topology_ungater.go:259** by [@PBundyra](https://github.com/PBundyra) — 2025-12-17T09:21:53Z
```diff
@@ -243,13 +242,21 @@ func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request)
 		podWithUngateInfo := &allToUngate[i]
 		var ungated bool
 		e := utilclient.Patch(ctx, r.client, podWithUngateInfo.pod, func() (bool, error) {
-			log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
 			ungated = utilpod.Ungate(podWithUngateInfo.pod, kueue.TopologySchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
+			}
+			changed := ungated
 			if podWithUngateInfo.pod.Spec.NodeSelector == nil {
 				podWithUngateInfo.pod.Spec.NodeSelector = make(map[string]string)
 			}
-			maps.Copy(podWithUngateInfo.pod.Spec.NodeSelector, podWithUngateInfo.nodeLabels)
-			return true, nil
+			for k, v := range podWithUngateInfo.nodeLabels {
+				if current, exists := podWithUngateInfo.pod.Spec.NodeSelector[k]; !exists || current != v {
+					podWithUngateInfo.pod.Spec.NodeSelector[k] = v
+					changed = true
+				}
+			}
+			return changed, nil
```

  This wouldn't catch the case where there would be something present in `podWithUngateInfo.pod.Spec.NodeSelector` and not in `podWithUngateInfo.nodeLabels`. I suggest using `maps.Equal`

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-12-17T09:23:20Z

- **pkg/controller/tas/topology_ungater.go:248** by [@PBundyra](https://github.com/PBundyra) — 2025-12-17T09:23:20Z
```diff
@@ -243,13 +242,21 @@ func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request)
 		podWithUngateInfo := &allToUngate[i]
 		var ungated bool
 		e := utilclient.Patch(ctx, r.client, podWithUngateInfo.pod, func() (bool, error) {
-			log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
 			ungated = utilpod.Ungate(podWithUngateInfo.pod, kueue.TopologySchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
```

  If we want to log this after ensuring we indeed send the Patch then we should log it right before returning, depending on `changed` value

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-17T09:25:37Z

- **pkg/controller/tas/topology_ungater.go:259** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-17T09:25:36Z
```diff
@@ -243,13 +242,21 @@ func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request)
 		podWithUngateInfo := &allToUngate[i]
 		var ungated bool
 		e := utilclient.Patch(ctx, r.client, podWithUngateInfo.pod, func() (bool, error) {
-			log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
 			ungated = utilpod.Ungate(podWithUngateInfo.pod, kueue.TopologySchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
+			}
+			changed := ungated
 			if podWithUngateInfo.pod.Spec.NodeSelector == nil {
 				podWithUngateInfo.pod.Spec.NodeSelector = make(map[string]string)
 			}
-			maps.Copy(podWithUngateInfo.pod.Spec.NodeSelector, podWithUngateInfo.nodeLabels)
-			return true, nil
+			for k, v := range podWithUngateInfo.nodeLabels {
+				if current, exists := podWithUngateInfo.pod.Spec.NodeSelector[k]; !exists || current != v {
+					podWithUngateInfo.pod.Spec.NodeSelector[k] = v
+					changed = true
+				}
+			}
+			return changed, nil
```

  This is not working because the number of keys can be different.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-17T09:26:50Z

- **pkg/controller/tas/topology_ungater.go:259** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-17T09:26:49Z
```diff
@@ -243,13 +242,21 @@ func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request)
 		podWithUngateInfo := &allToUngate[i]
 		var ungated bool
 		e := utilclient.Patch(ctx, r.client, podWithUngateInfo.pod, func() (bool, error) {
-			log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
 			ungated = utilpod.Ungate(podWithUngateInfo.pod, kueue.TopologySchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
+			}
+			changed := ungated
 			if podWithUngateInfo.pod.Spec.NodeSelector == nil {
 				podWithUngateInfo.pod.Spec.NodeSelector = make(map[string]string)
 			}
-			maps.Copy(podWithUngateInfo.pod.Spec.NodeSelector, podWithUngateInfo.nodeLabels)
-			return true, nil
+			for k, v := range podWithUngateInfo.nodeLabels {
+				if current, exists := podWithUngateInfo.pod.Spec.NodeSelector[k]; !exists || current != v {
+					podWithUngateInfo.pod.Spec.NodeSelector[k] = v
+					changed = true
+				}
+			}
+			return changed, nil
```

  I tried this solution and unit tests failed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-17T09:45:31Z

- **pkg/controller/tas/topology_ungater.go:259** by [@mimowo](https://github.com/mimowo) — 2025-12-17T09:45:31Z
```diff
@@ -243,13 +242,21 @@ func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request)
 		podWithUngateInfo := &allToUngate[i]
 		var ungated bool
 		e := utilclient.Patch(ctx, r.client, podWithUngateInfo.pod, func() (bool, error) {
-			log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
 			ungated = utilpod.Ungate(podWithUngateInfo.pod, kueue.TopologySchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
+			}
+			changed := ungated
 			if podWithUngateInfo.pod.Spec.NodeSelector == nil {
 				podWithUngateInfo.pod.Spec.NodeSelector = make(map[string]string)
 			}
-			maps.Copy(podWithUngateInfo.pod.Spec.NodeSelector, podWithUngateInfo.nodeLabels)
-			return true, nil
+			for k, v := range podWithUngateInfo.nodeLabels {
+				if current, exists := podWithUngateInfo.pod.Spec.NodeSelector[k]; !exists || current != v {
+					podWithUngateInfo.pod.Spec.NodeSelector[k] = v
+					changed = true
+				}
+			}
+			return changed, nil
```

  ~why the number of keys would be different? We can create the Pod with the nodeSelector already as the TAS would add~

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-12-17T13:53:20Z

- **pkg/controller/tas/topology_ungater.go:259** by [@PBundyra](https://github.com/PBundyra) — 2025-12-17T13:53:20Z
```diff
@@ -243,13 +242,21 @@ func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request)
 		podWithUngateInfo := &allToUngate[i]
 		var ungated bool
 		e := utilclient.Patch(ctx, r.client, podWithUngateInfo.pod, func() (bool, error) {
-			log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
 			ungated = utilpod.Ungate(podWithUngateInfo.pod, kueue.TopologySchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
+			}
+			changed := ungated
 			if podWithUngateInfo.pod.Spec.NodeSelector == nil {
 				podWithUngateInfo.pod.Spec.NodeSelector = make(map[string]string)
 			}
-			maps.Copy(podWithUngateInfo.pod.Spec.NodeSelector, podWithUngateInfo.nodeLabels)
-			return true, nil
+			for k, v := range podWithUngateInfo.nodeLabels {
+				if current, exists := podWithUngateInfo.pod.Spec.NodeSelector[k]; !exists || current != v {
+					podWithUngateInfo.pod.Spec.NodeSelector[k] = v
+					changed = true
+				}
+			}
+			return changed, nil
```

  After second consideration this looks good to me, thanks

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-17T18:58:28Z

- **pkg/controller/tas/topology_ungater.go:259** by [@mimowo](https://github.com/mimowo) — 2025-12-17T18:58:28Z
```diff
@@ -243,13 +242,21 @@ func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request)
 		podWithUngateInfo := &allToUngate[i]
 		var ungated bool
 		e := utilclient.Patch(ctx, r.client, podWithUngateInfo.pod, func() (bool, error) {
-			log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
 			ungated = utilpod.Ungate(podWithUngateInfo.pod, kueue.TopologySchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
+			}
+			changed := ungated
 			if podWithUngateInfo.pod.Spec.NodeSelector == nil {
 				podWithUngateInfo.pod.Spec.NodeSelector = make(map[string]string)
 			}
-			maps.Copy(podWithUngateInfo.pod.Spec.NodeSelector, podWithUngateInfo.nodeLabels)
-			return true, nil
+			for k, v := range podWithUngateInfo.nodeLabels {
+				if current, exists := podWithUngateInfo.pod.Spec.NodeSelector[k]; !exists || current != v {
+					podWithUngateInfo.pod.Spec.NodeSelector[k] = v
+					changed = true
+				}
+			}
+			return changed, nil
```

   I think if the Pod is already ungated we can skip the node selectors update. 
  
  I think such a request would not even succeed because Kubernetes has validation against mutating a pod template for ungated pods.
  
  This would also simplify the code allowing to return to simple Copy

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-18T07:51:06Z

- **pkg/controller/tas/topology_ungater.go:259** by [@mimowo](https://github.com/mimowo) — 2025-12-18T07:51:06Z
```diff
@@ -243,13 +242,21 @@ func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request)
 		podWithUngateInfo := &allToUngate[i]
 		var ungated bool
 		e := utilclient.Patch(ctx, r.client, podWithUngateInfo.pod, func() (bool, error) {
-			log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
 			ungated = utilpod.Ungate(podWithUngateInfo.pod, kueue.TopologySchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
+			}
+			changed := ungated
 			if podWithUngateInfo.pod.Spec.NodeSelector == nil {
 				podWithUngateInfo.pod.Spec.NodeSelector = make(map[string]string)
 			}
-			maps.Copy(podWithUngateInfo.pod.Spec.NodeSelector, podWithUngateInfo.nodeLabels)
-			return true, nil
+			for k, v := range podWithUngateInfo.nodeLabels {
+				if current, exists := podWithUngateInfo.pod.Spec.NodeSelector[k]; !exists || current != v {
+					podWithUngateInfo.pod.Spec.NodeSelector[k] = v
+					changed = true
+				}
+			}
+			return changed, nil
```

  So here is the validation code https://github.com/kubernetes/kubernetes/blob/master/pkg/apis/core/validation/validation.go#L5788-L5794 in the code k8s which only accepts node selector updates if the pod is gated. In TAS we update the node selectors atomically in the same request which ungates the Pod. So, if the pod is already ungated, there is no need to change selectors.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-18T12:40:56Z

- **pkg/controller/tas/topology_ungater.go:259** by [@mimowo](https://github.com/mimowo) — 2025-12-18T12:40:56Z
```diff
@@ -243,13 +242,21 @@ func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request)
 		podWithUngateInfo := &allToUngate[i]
 		var ungated bool
 		e := utilclient.Patch(ctx, r.client, podWithUngateInfo.pod, func() (bool, error) {
-			log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
 			ungated = utilpod.Ungate(podWithUngateInfo.pod, kueue.TopologySchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
+			}
+			changed := ungated
 			if podWithUngateInfo.pod.Spec.NodeSelector == nil {
 				podWithUngateInfo.pod.Spec.NodeSelector = make(map[string]string)
 			}
-			maps.Copy(podWithUngateInfo.pod.Spec.NodeSelector, podWithUngateInfo.nodeLabels)
-			return true, nil
+			for k, v := range podWithUngateInfo.nodeLabels {
+				if current, exists := podWithUngateInfo.pod.Spec.NodeSelector[k]; !exists || current != v {
+					podWithUngateInfo.pod.Spec.NodeSelector[k] = v
+					changed = true
+				}
+			}
+			return changed, nil
```

  So I want to propose:
  
  ```golang
  ungated = utilpod.Ungate(podWithUngateInfo.pod, kueue.TopologySchedulingGate)
  if ungated {
  	log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
  	if podWithUngateInfo.pod.Spec.NodeSelector == nil {
  		podWithUngateInfo.pod.Spec.NodeSelector = make(map[string]string)
    	}
      maps.Copy(podWithUngateInfo.pod.Spec.NodeSelector, podWithUngateInfo.nodeLabels)
  }
  return ungated, nil
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-18T14:41:15Z

- **pkg/controller/tas/topology_ungater.go:259** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-18T14:41:15Z
```diff
@@ -243,13 +242,21 @@ func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request)
 		podWithUngateInfo := &allToUngate[i]
 		var ungated bool
 		e := utilclient.Patch(ctx, r.client, podWithUngateInfo.pod, func() (bool, error) {
-			log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
 			ungated = utilpod.Ungate(podWithUngateInfo.pod, kueue.TopologySchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
+			}
+			changed := ungated
 			if podWithUngateInfo.pod.Spec.NodeSelector == nil {
 				podWithUngateInfo.pod.Spec.NodeSelector = make(map[string]string)
 			}
-			maps.Copy(podWithUngateInfo.pod.Spec.NodeSelector, podWithUngateInfo.nodeLabels)
-			return true, nil
+			for k, v := range podWithUngateInfo.nodeLabels {
+				if current, exists := podWithUngateInfo.pod.Spec.NodeSelector[k]; !exists || current != v {
+					podWithUngateInfo.pod.Spec.NodeSelector[k] = v
+					changed = true
+				}
+			}
+			return changed, nil
```

  Done
