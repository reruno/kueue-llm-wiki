# PR #8335: cleanup: add ginkgo.By to the Consistently block in second-pass backoff test

**Summary**: cleanup: add ginkgo.By to the Consistently block in second-pass backoff test

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8335

**Last updated**: 2026-02-02T09:43:09Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mykysha](https://github.com/mykysha)
- **Branch**: `cleanup/backoff-test-consistently-explained` → `main`
- **Created**: 2025-12-18T15:49:18Z
- **Updated**: 2026-02-02T09:43:09Z
- **Merged**: 2025-12-22T09:04:33Z
- **Closed**: 2025-12-22T09:04:33Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 1   **+20 / -16**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add a ginkgo.By block around the Consistently in the second-pass backoff integration test to better communicate the test logic. Also use Eventually in the backoff event counting stage to help prevent failures when the event has not been added yet.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #8208 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-12-18T15:49:24Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 9188d165118d08fb8a72984eef52f11acc2647e1 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6945146ce1269900087618a2 |

### Comment by [@mykysha](https://github.com/mykysha) — 2025-12-18T15:51:49Z

I think using `ginkgo.By` block would help avoid future confusion about the duration the `Consistently` should run for in this test, and using 7 seconds instead of the previous 10 would help shorten the test execution time as the longer wait was not essential at all.

/cc @mbobrovskyi

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-18T15:51:53Z

@mykysha: GitHub didn't allow me to request PR reviews from the following users: PTAL.

Note that only [kubernetes-sigs members](https://github.com/orgs/kubernetes-sigs/people) and repo collaborators can review this PR, and authors cannot review their own PRs.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8335#issuecomment-3670946977):

>I think using `ginkgo.By` block would help avoid future confusion about the duration the `Consistently` should run for in this test, and using 7 seconds instead of the previous 10 would help shorten the test execution time as the longer wait was not essential at all.
>
>/cc @mbobrovskyi PTAL


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mykysha](https://github.com/mykysha) — 2025-12-18T15:54:02Z

Have also run the test locally for 100 times to ensure that the reduction of the `Consistently` time and the addition of `Eventually` does not introduce any flaky behavior

### Comment by [@mykysha](https://github.com/mykysha) — 2025-12-18T16:39:04Z

/retest-required

### Comment by [@mykysha](https://github.com/mykysha) — 2025-12-18T16:39:40Z

Added an issue #8338

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-19T10:08:54Z

LGTM label has been added.  <details>Git tree hash: d9d0968d52659e6c57854c01adbc3e7cc58369e9</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-19T10:08:56Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8335#pullrequestreview-3597923742" title="Approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8335#" title="Author self-approved">mykysha</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [mbobrovskyi]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-22T08:03:42Z

/unhold

We can make changes in a follow-up if needed.

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-18T15:59:56Z

- **test/integration/singlecluster/tas/tas_test.go:2863** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-18T15:59:56Z
```diff
@@ -2838,26 +2838,30 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					}, util.Timeout, util.Interval).Should(gomega.Succeed())
 				})
 
-				gomega.Consistently(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlKey, wl1)).To(gomega.Succeed())
-					g.Expect(wl1.Status.Admission).ShouldNot(gomega.BeNil())
-					g.Expect(wl1.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
-					g.Expect(wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeNil())
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				ginkgo.By("ensure no assignment during three backoff intervals (≈1s, 2s, 4s - total 7s)", func() {
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, wlKey, wl1)).To(gomega.Succeed())
+						g.Expect(wl1.Status.Admission).ShouldNot(gomega.BeNil())
+						g.Expect(wl1.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
+						g.Expect(wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeNil())
+					}, 7*time.Second, util.ShortInterval).Should(gomega.Succeed())
+				})
 
 				ginkgo.By("observe at least three SecondPassFailed events while the node is NotReady (≈1s, 2s, 4s backoffs)", func() {
 					var evList corev1.EventList
-					gomega.Expect(k8sClient.List(ctx, &evList, &client.ListOptions{Namespace: ns.Name})).To(gomega.Succeed())
-					var count int32
-					for i := range evList.Items {
-						ev := evList.Items[i]
-						if ev.Reason == "SecondPassFailed" && ev.InvolvedObject.Name == wl1.Name {
-							if ev.Count > count {
-								count = ev.Count
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.List(ctx, &evList, &client.ListOptions{Namespace: ns.Name})).To(gomega.Succeed())
+						var count int32
+						for i := range evList.Items {
+							ev := evList.Items[i]
+							if ev.Reason == "SecondPassFailed" && ev.InvolvedObject.Name == wl1.Name {
+								if ev.Count > count {
+									count = ev.Count
+								}
 							}
 						}
-					}
-					gomega.Expect(count).To(gomega.And(gomega.BeNumerically(">=", 3), gomega.BeNumerically("<=", 5)))
+						g.Expect(count).To(gomega.And(gomega.BeNumerically(">=", 3), gomega.BeNumerically("<=", 5)))
```

  We are using Eventually now. I'm wondering if we can set exactly count instead of >= and <=? Or it still not stable?

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-12-18T16:40:51Z

- **test/integration/singlecluster/tas/tas_test.go:2863** by [@mykysha](https://github.com/mykysha) — 2025-12-18T16:40:51Z
```diff
@@ -2838,26 +2838,30 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					}, util.Timeout, util.Interval).Should(gomega.Succeed())
 				})
 
-				gomega.Consistently(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlKey, wl1)).To(gomega.Succeed())
-					g.Expect(wl1.Status.Admission).ShouldNot(gomega.BeNil())
-					g.Expect(wl1.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
-					g.Expect(wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeNil())
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				ginkgo.By("ensure no assignment during three backoff intervals (≈1s, 2s, 4s - total 7s)", func() {
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, wlKey, wl1)).To(gomega.Succeed())
+						g.Expect(wl1.Status.Admission).ShouldNot(gomega.BeNil())
+						g.Expect(wl1.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
+						g.Expect(wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeNil())
+					}, 7*time.Second, util.ShortInterval).Should(gomega.Succeed())
+				})
 
 				ginkgo.By("observe at least three SecondPassFailed events while the node is NotReady (≈1s, 2s, 4s backoffs)", func() {
 					var evList corev1.EventList
-					gomega.Expect(k8sClient.List(ctx, &evList, &client.ListOptions{Namespace: ns.Name})).To(gomega.Succeed())
-					var count int32
-					for i := range evList.Items {
-						ev := evList.Items[i]
-						if ev.Reason == "SecondPassFailed" && ev.InvolvedObject.Name == wl1.Name {
-							if ev.Count > count {
-								count = ev.Count
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.List(ctx, &evList, &client.ListOptions{Namespace: ns.Name})).To(gomega.Succeed())
+						var count int32
+						for i := range evList.Items {
+							ev := evList.Items[i]
+							if ev.Reason == "SecondPassFailed" && ev.InvolvedObject.Name == wl1.Name {
+								if ev.Count > count {
+									count = ev.Count
+								}
 							}
 						}
-					}
-					gomega.Expect(count).To(gomega.And(gomega.BeNumerically(">=", 3), gomega.BeNumerically("<=", 5)))
+						g.Expect(count).To(gomega.And(gomega.BeNumerically(">=", 3), gomega.BeNumerically("<=", 5)))
```

  Yeah, surely, even if somehow the number of events would be larger than 3, it will fail later as the next timeout would not be able to pass. Will change to strict equal now

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-19T07:30:39Z

- **test/integration/singlecluster/tas/tas_test.go:2847** by [@mimowo](https://github.com/mimowo) — 2025-12-19T07:30:39Z
```diff
@@ -2838,26 +2838,30 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					}, util.Timeout, util.Interval).Should(gomega.Succeed())
 				})
 
-				gomega.Consistently(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlKey, wl1)).To(gomega.Succeed())
-					g.Expect(wl1.Status.Admission).ShouldNot(gomega.BeNil())
-					g.Expect(wl1.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
-					g.Expect(wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeNil())
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				ginkgo.By("ensure no assignment during three backoff intervals (≈1s, 2s, 4s - total 7s)", func() {
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, wlKey, wl1)).To(gomega.Succeed())
+						g.Expect(wl1.Status.Admission).ShouldNot(gomega.BeNil())
+						g.Expect(wl1.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
+						g.Expect(wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeNil())
+					}, 7*time.Second, util.ShortInterval).Should(gomega.Succeed())
```

  I'm wondering if this could flake, because it seems like exactly on the edge. Maybe it is safer to check for 6s, wdyt?

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-12-19T08:57:37Z

- **test/integration/singlecluster/tas/tas_test.go:2847** by [@mykysha](https://github.com/mykysha) — 2025-12-19T08:57:36Z
```diff
@@ -2838,26 +2838,30 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					}, util.Timeout, util.Interval).Should(gomega.Succeed())
 				})
 
-				gomega.Consistently(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlKey, wl1)).To(gomega.Succeed())
-					g.Expect(wl1.Status.Admission).ShouldNot(gomega.BeNil())
-					g.Expect(wl1.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
-					g.Expect(wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeNil())
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				ginkgo.By("ensure no assignment during three backoff intervals (≈1s, 2s, 4s - total 7s)", func() {
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, wlKey, wl1)).To(gomega.Succeed())
+						g.Expect(wl1.Status.Admission).ShouldNot(gomega.BeNil())
+						g.Expect(wl1.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
+						g.Expect(wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeNil())
+					}, 7*time.Second, util.ShortInterval).Should(gomega.Succeed())
```

  It is on the edge for three backoffs, but then we check to have three. The next attempt would be in 8s, should be enough time to assert events and mark nodes as ready in time for the scheduler attempt

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (approved) — 2025-12-19T10:08:47Z

/lgtm
/approve

/hold in case @mimowo want to add something.
