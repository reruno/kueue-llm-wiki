# PR #8390: Streamline the MultiKueue ClusterProfile API documentation

**Summary**: Streamline the MultiKueue ClusterProfile API documentation

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8390

**Last updated**: 2026-01-09T15:45:54Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kshalot](https://github.com/kshalot)
- **Branch**: `docs/update-multikueue-clusterprofile-docs` → `main`
- **Created**: 2025-12-22T11:22:47Z
- **Updated**: 2026-01-09T15:45:54Z
- **Merged**: 2026-01-09T15:01:40Z
- **Closed**: 2026-01-09T15:01:40Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92)
- **Changed files**: 1   **+157 / -51**

## Description

#### What type of PR is this?
/kind documentation

#### What this PR does / why we need it:
The documentation can be made more explicit to highlight the importance of the cloud provider/cluster manager in the ClusterProfile API integration.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-12-22T11:22:54Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 3ff3aec97f80c144a91b6a970476c0b4046e2f52 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69611410ab7b360008857c0a |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-8390--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-8390--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTgzOTAtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9._lNW6lRZiOMtjfIYlUPqkyxvf5q-v4uxxBitbashfjg)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-8390--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-09T14:52:42Z

LGTM label has been added.  <details>Git tree hash: 96d29e238d3519d0bbeed5fec13a4ae2b3ccea0d</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-09T14:52:44Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8390#" title="Author self-approved">kshalot</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8390#pullrequestreview-3644255459" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8390#pullrequestreview-3634982176" title="Approved">olekzabl</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8390#pullrequestreview-3603715796" title="Approved">Singularity23x0</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-09T15:45:15Z

/cherry-pick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-01-09T15:45:54Z

@tenzen-y: new pull request created: #8502

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8390#issuecomment-3729453898):

>/cherry-pick website
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (approved) — 2025-12-22T11:55:06Z

Looks ok, mind a small nit.

- **site/content/en/docs/tasks/manage/setup_multikueue.md:228** by [@Singularity23x0](https://github.com/Singularity23x0) — 2025-12-22T11:35:58Z
```diff
@@ -162,46 +213,31 @@ spec:
   ...
 status:
   accessProviders:
-  - name: ${PROVIDER_NAME}
+  - name: <access-provider-name>
     cluster:
-      server: https://${SERVER_ENDPOINT}
-      certificate-authority-data: ${CERTIFICATE_AUTHORITY_DATA}
+      ...
 ```
 
-### Configure Kueue Manager
+{{% alert title="Note" color="primary" %}}
 
-Next, configure the controller manager config map with the credentials providers.
+The `ClusterProfile` access providers are within the object's `status` subresource and, in principle, should be managed by the cluster manager
+(i.e. the entity provisioning the clusters, like a cloud provider) and not created manually.
 
-```yaml
-apiVersion: v1
-data:
-  controller_manager_config.yaml: |
-    ...
-    multiKueue:
-      clusterProfile:
-        credentialsProviders:
-        - name: ${PROVIDER_NAME}
-          execConfig:
-            apiVersion: client.authentication.k8s.io/v1beta1
-            command: /plugins/${PLUGIN_COMMAND}
-kind: ConfigMap
-metadata:
-  labels:
-    app.kubernetes.io/name: kueue
-    control-plane: controller-manager
-  name: kueue-manager-config
-  namespace: kueue-system
-```
+{{% /alert %}}
 
-### Install Required Plugins
+### Install the access provider plugin
```

  Are any links to an example available to place here?

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-12-22T12:45:35Z

- **site/content/en/docs/tasks/manage/setup_multikueue.md:235** by [@olekzabl](https://github.com/olekzabl) — 2025-12-22T12:45:26Z
```diff
@@ -229,18 +265,53 @@ spec:
         emptyDir: {}
 ```
 
-This patch does the following:
-1.  Adds an `initContainer` that copies the `${PLUGIN_COMMAND}` from its container image to the `/plugins` directory in the shared volume.
-2.  Adds an `emptyDir` volume named `clusterprofile-plugins` to the pod.
-3.  Mounts the `clusterprofile-plugins` volume to the `manager` container, making the plugin available at `/plugins/${PLUGIN_COMMAND}`.
```

  Elsewhere, you mostly _add_ explanations, but here, there's a piece of explanation which you seem to _remove_. Why?

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2025-12-22T13:07:44Z

- **site/content/en/docs/tasks/manage/setup_multikueue.md:235** by [@kshalot](https://github.com/kshalot) — 2025-12-22T13:07:44Z
```diff
@@ -229,18 +265,53 @@ spec:
         emptyDir: {}
 ```
 
-This patch does the following:
-1.  Adds an `initContainer` that copies the `${PLUGIN_COMMAND}` from its container image to the `/plugins` directory in the shared volume.
-2.  Adds an `emptyDir` volume named `clusterprofile-plugins` to the pod.
-3.  Mounts the `clusterprofile-plugins` volume to the `manager` container, making the plugin available at `/plugins/${PLUGIN_COMMAND}`.
```

  It seemed redudant to me - this is pretty much the YAML translated into text. I wasn't sure how much substance there was in this paragraph. Do you think it could be useful?

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2025-12-22T14:06:47Z

- **site/content/en/docs/tasks/manage/setup_multikueue.md:235** by [@olekzabl](https://github.com/olekzabl) — 2025-12-22T14:06:47Z
```diff
@@ -229,18 +265,53 @@ spec:
         emptyDir: {}
 ```
 
-This patch does the following:
-1.  Adds an `initContainer` that copies the `${PLUGIN_COMMAND}` from its container image to the `/plugins` directory in the shared volume.
-2.  Adds an `emptyDir` volume named `clusterprofile-plugins` to the pod.
-3.  Mounts the `clusterprofile-plugins` volume to the `manager` container, making the plugin available at `/plugins/${PLUGIN_COMMAND}`.
```

  Ok - I admit I focused on the diff, not so much on the (unchanged) YAML above.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-12-23T12:02:01Z

- **site/content/en/docs/tasks/manage/setup_multikueue.md:169** by [@mszadkow](https://github.com/mszadkow) — 2025-12-23T12:02:01Z
```diff
@@ -131,27 +130,79 @@ It provides a robust, modular, and extensible framework that helps other open so
 The integration with OCM is an optional solution that enables Kueue users to streamline the MultiKueue setup process, automate the generation of MultiKueue specific Kubeconfig, and enhance multicluster scheduling capabilities.
 For more details about this solution, please refer to this [link](https://github.com/open-cluster-management-io/ocm/tree/main/solutions/kueue-admission-check).
 
-## Setup MultiKueue with ClusterProfile API
+## Configure federated credential discovery via the ClusterProfile API
 
 {{< feature-state state="alpha" for_version="v0.15" >}}
 
-The [ClusterProfile API](https://multicluster.sigs.k8s.io/concepts/cluster-profile-api/) provides a standardized, vendor-neutral interface for presenting cluster information. It allows defining cluster access information in a standardized `ClusterProfile` object and using credential plugins for authentication.
+The [Cluster Inventory API](https://multicluster.sigs.k8s.io/) provides a standardized, vendor-neutral interface for representing a cluster's properties and status.
+It defines a `ClusterProfile` resource that encapsulates the identity, metadata, and status of a specific cluster within a multi-cluster environment.
+
+Kueue can leverage the configuration contained within a `ClusterProfile` object to obtain credentials and connect to the cluster it represents.
+In practice, this means that instead of using Kubeconfig files to specify the connection to its workers, the manager cluster uses the state and
+access providers contained in the existing `ClusterProfile`s. This eliminates a large portion of management required from the administrator and
+allows to delegate credentialing to the entity managing the clusters (like a cloud provider).
+
+{{% alert title="Note" color="primary" %}}
+
+Kueue itself does not manage the `ClusterProfile` resources, which is the responsibility of the cluster manager, for example a cloud provider or a dedicated controller.
+
+{{% /alert %}}
+
+### Enable the `MultiKueueClusterProfile` feature gate
+
+The `MultiKueueClusterProfile` feature gate has to be enabled for Kueue to consider `ClusterProfile` objects when connecting to clusters.
+Refer to the [Installation guide](/docs/installation/#change-the-feature-gates-configuration) for instructions on configuring feature gates.
+
+### Provision the `ClusterProfile` objects for your clusters
+
+#### Cloud provider managed cluster inventory
+
+When using a cloud provider that supports the automatic generation and synchronization of a cluster inventory, the `ClusterProfile` objects are
+managed directly by their platform and do not require that the user sets them up manually or uses a dedicated controller.
+
+To enable the generation of an inventory, refer to your cloud provider's documentation:
+* Google Cloud Platform:
+  * [Full walkthrough of MultiKueue setup with ClusterProfile](https://github.com/GoogleCloudPlatform/gke-fleet-management/blob/b9fe08386c48f84617cb8ab7b042f2790741e893/multikueue-clusterprofile/README.md).
+  * [Inventory generation documentation](https://docs.cloud.google.com/kubernetes-engine/fleet-management/docs/generate-inventory-for-integrations).
+
+When using cloud provider managed cluster inventory, you should expect a `ClusterProfile` object to be created in your MultiKueue manager cluster,
+one per cluster (including the manager itself).
```

  If I have 2 worker clusters I should have 3 cluster profiles then?

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-07T11:41:44Z

- **site/content/en/docs/tasks/manage/setup_multikueue.md:169** by [@kshalot](https://github.com/kshalot) — 2026-01-07T11:41:44Z
```diff
@@ -131,27 +130,79 @@ It provides a robust, modular, and extensible framework that helps other open so
 The integration with OCM is an optional solution that enables Kueue users to streamline the MultiKueue setup process, automate the generation of MultiKueue specific Kubeconfig, and enhance multicluster scheduling capabilities.
 For more details about this solution, please refer to this [link](https://github.com/open-cluster-management-io/ocm/tree/main/solutions/kueue-admission-check).
 
-## Setup MultiKueue with ClusterProfile API
+## Configure federated credential discovery via the ClusterProfile API
 
 {{< feature-state state="alpha" for_version="v0.15" >}}
 
-The [ClusterProfile API](https://multicluster.sigs.k8s.io/concepts/cluster-profile-api/) provides a standardized, vendor-neutral interface for presenting cluster information. It allows defining cluster access information in a standardized `ClusterProfile` object and using credential plugins for authentication.
+The [Cluster Inventory API](https://multicluster.sigs.k8s.io/) provides a standardized, vendor-neutral interface for representing a cluster's properties and status.
+It defines a `ClusterProfile` resource that encapsulates the identity, metadata, and status of a specific cluster within a multi-cluster environment.
+
+Kueue can leverage the configuration contained within a `ClusterProfile` object to obtain credentials and connect to the cluster it represents.
+In practice, this means that instead of using Kubeconfig files to specify the connection to its workers, the manager cluster uses the state and
+access providers contained in the existing `ClusterProfile`s. This eliminates a large portion of management required from the administrator and
+allows to delegate credentialing to the entity managing the clusters (like a cloud provider).
+
+{{% alert title="Note" color="primary" %}}
+
+Kueue itself does not manage the `ClusterProfile` resources, which is the responsibility of the cluster manager, for example a cloud provider or a dedicated controller.
+
+{{% /alert %}}
+
+### Enable the `MultiKueueClusterProfile` feature gate
+
+The `MultiKueueClusterProfile` feature gate has to be enabled for Kueue to consider `ClusterProfile` objects when connecting to clusters.
+Refer to the [Installation guide](/docs/installation/#change-the-feature-gates-configuration) for instructions on configuring feature gates.
+
+### Provision the `ClusterProfile` objects for your clusters
+
+#### Cloud provider managed cluster inventory
+
+When using a cloud provider that supports the automatic generation and synchronization of a cluster inventory, the `ClusterProfile` objects are
+managed directly by their platform and do not require that the user sets them up manually or uses a dedicated controller.
+
+To enable the generation of an inventory, refer to your cloud provider's documentation:
+* Google Cloud Platform:
+  * [Full walkthrough of MultiKueue setup with ClusterProfile](https://github.com/GoogleCloudPlatform/gke-fleet-management/blob/b9fe08386c48f84617cb8ab7b042f2790741e893/multikueue-clusterprofile/README.md).
+  * [Inventory generation documentation](https://docs.cloud.google.com/kubernetes-engine/fleet-management/docs/generate-inventory-for-integrations).
+
+When using cloud provider managed cluster inventory, you should expect a `ClusterProfile` object to be created in your MultiKueue manager cluster,
+one per cluster (including the manager itself).
```

  Yes

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-07T12:00:51Z

- **site/content/en/docs/tasks/manage/setup_multikueue.md:228** by [@kshalot](https://github.com/kshalot) — 2026-01-07T12:00:51Z
```diff
@@ -162,46 +213,31 @@ spec:
   ...
 status:
   accessProviders:
-  - name: ${PROVIDER_NAME}
+  - name: <access-provider-name>
     cluster:
-      server: https://${SERVER_ENDPOINT}
-      certificate-authority-data: ${CERTIFICATE_AUTHORITY_DATA}
+      ...
 ```
 
-### Configure Kueue Manager
+{{% alert title="Note" color="primary" %}}
 
-Next, configure the controller manager config map with the credentials providers.
+The `ClusterProfile` access providers are within the object's `status` subresource and, in principle, should be managed by the cluster manager
+(i.e. the entity provisioning the clusters, like a cloud provider) and not created manually.
 
-```yaml
-apiVersion: v1
-data:
-  controller_manager_config.yaml: |
-    ...
-    multiKueue:
-      clusterProfile:
-        credentialsProviders:
-        - name: ${PROVIDER_NAME}
-          execConfig:
-            apiVersion: client.authentication.k8s.io/v1beta1
-            command: /plugins/${PLUGIN_COMMAND}
-kind: ConfigMap
-metadata:
-  labels:
-    app.kubernetes.io/name: kueue
-    control-plane: controller-manager
-  name: kueue-manager-config
-  namespace: kueue-system
-```
+{{% /alert %}}
 
-### Install Required Plugins
+### Install the access provider plugin
```

  I added links with example plugins (the two that I know of).

### Review by [@olekzabl](https://github.com/olekzabl) (approved) — 2026-01-07T13:47:22Z

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-09T12:24:04Z

- **site/content/en/docs/tasks/manage/setup_multikueue.md:236** by [@mimowo](https://github.com/mimowo) — 2026-01-09T12:24:04Z
```diff
@@ -162,46 +213,33 @@ spec:
   ...
 status:
   accessProviders:
-  - name: ${PROVIDER_NAME}
+  - name: <access-provider-name>
     cluster:
-      server: https://${SERVER_ENDPOINT}
-      certificate-authority-data: ${CERTIFICATE_AUTHORITY_DATA}
+      ...
 ```
 
-### Configure Kueue Manager
+{{% alert title="Note" color="primary" %}}
 
-Next, configure the controller manager config map with the credentials providers.
+The `ClusterProfile` access providers are within the object's `status` subresource and, in principle, should be managed by the cluster manager
+(i.e. the entity provisioning the clusters, like a cloud provider) and not created manually.
 
-```yaml
-apiVersion: v1
-data:
-  controller_manager_config.yaml: |
-    ...
-    multiKueue:
-      clusterProfile:
-        credentialsProviders:
-        - name: ${PROVIDER_NAME}
-          execConfig:
-            apiVersion: client.authentication.k8s.io/v1beta1
-            command: /plugins/${PLUGIN_COMMAND}
-kind: ConfigMap
-metadata:
-  labels:
-    app.kubernetes.io/name: kueue
-    control-plane: controller-manager
-  name: kueue-manager-config
-  namespace: kueue-system
-```
+{{% /alert %}}
+
+### Install the access provider plugin
 
-### Install Required Plugins
+The access provider defined in `ClusterProfile` depends upon an executable plugin being available to the
+Kueue controller manager pods running within the MultiKueue manager cluster.
+It's the responsibility of the Kueue administrator to make sure that the required command is available.
 
-If your credentials provider requires an executable plugin, you must make it available to the Kueue manager.
+An example plugin can be found [here](https://github.com/kubernetes-sigs/cluster-inventory-api/tree/445319b6307a88778b930e154ed3e2f38d85a689/cmd/secretreader-plugin) (secret reader) or [here](https://cloud.google.com/blog/products/containers-kubernetes/kubectl-auth-changes-in-gke) (Google Cloud Platform).
 
-#### Add plugins via volume mounts
+#### Mount the executable with `initContainers`
```

  Can we please add a section for Mounting the executable using ImageVolume https://github.com/kubernetes-sigs/kueue/issues/8042#issuecomment-3612742046, this should work 1.35+ so we could add such a note. Over time, this could become the default way of doing it I think.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-09T14:47:01Z

- **site/content/en/docs/tasks/manage/setup_multikueue.md:236** by [@kshalot](https://github.com/kshalot) — 2026-01-09T14:47:01Z
```diff
@@ -162,46 +213,33 @@ spec:
   ...
 status:
   accessProviders:
-  - name: ${PROVIDER_NAME}
+  - name: <access-provider-name>
     cluster:
-      server: https://${SERVER_ENDPOINT}
-      certificate-authority-data: ${CERTIFICATE_AUTHORITY_DATA}
+      ...
 ```
 
-### Configure Kueue Manager
+{{% alert title="Note" color="primary" %}}
 
-Next, configure the controller manager config map with the credentials providers.
+The `ClusterProfile` access providers are within the object's `status` subresource and, in principle, should be managed by the cluster manager
+(i.e. the entity provisioning the clusters, like a cloud provider) and not created manually.
 
-```yaml
-apiVersion: v1
-data:
-  controller_manager_config.yaml: |
-    ...
-    multiKueue:
-      clusterProfile:
-        credentialsProviders:
-        - name: ${PROVIDER_NAME}
-          execConfig:
-            apiVersion: client.authentication.k8s.io/v1beta1
-            command: /plugins/${PLUGIN_COMMAND}
-kind: ConfigMap
-metadata:
-  labels:
-    app.kubernetes.io/name: kueue
-    control-plane: controller-manager
-  name: kueue-manager-config
-  namespace: kueue-system
-```
+{{% /alert %}}
+
+### Install the access provider plugin
 
-### Install Required Plugins
+The access provider defined in `ClusterProfile` depends upon an executable plugin being available to the
+Kueue controller manager pods running within the MultiKueue manager cluster.
+It's the responsibility of the Kueue administrator to make sure that the required command is available.
 
-If your credentials provider requires an executable plugin, you must make it available to the Kueue manager.
+An example plugin can be found [here](https://github.com/kubernetes-sigs/cluster-inventory-api/tree/445319b6307a88778b930e154ed3e2f38d85a689/cmd/secretreader-plugin) (secret reader) or [here](https://cloud.google.com/blog/products/containers-kubernetes/kubectl-auth-changes-in-gke) (Google Cloud Platform).
 
-#### Add plugins via volume mounts
+#### Mount the executable with `initContainers`
```

  Good point. I read the discussions on Slack just to be sure and it seems that the `noexec` mount option was indeed dropped for this feature. So the files should be executable.
  
  Note: I did not test it yet. I'm planning to do that to make sure, but in theory this should work.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-09T14:52:34Z

Thank you 👍 
/lgtm
/approve
/cherrypick website

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-09T14:54:14Z

- **site/content/en/docs/tasks/manage/setup_multikueue.md:236** by [@mimowo](https://github.com/mimowo) — 2026-01-09T14:54:14Z
```diff
@@ -162,46 +213,33 @@ spec:
   ...
 status:
   accessProviders:
-  - name: ${PROVIDER_NAME}
+  - name: <access-provider-name>
     cluster:
-      server: https://${SERVER_ENDPOINT}
-      certificate-authority-data: ${CERTIFICATE_AUTHORITY_DATA}
+      ...
 ```
 
-### Configure Kueue Manager
+{{% alert title="Note" color="primary" %}}
 
-Next, configure the controller manager config map with the credentials providers.
+The `ClusterProfile` access providers are within the object's `status` subresource and, in principle, should be managed by the cluster manager
+(i.e. the entity provisioning the clusters, like a cloud provider) and not created manually.
 
-```yaml
-apiVersion: v1
-data:
-  controller_manager_config.yaml: |
-    ...
-    multiKueue:
-      clusterProfile:
-        credentialsProviders:
-        - name: ${PROVIDER_NAME}
-          execConfig:
-            apiVersion: client.authentication.k8s.io/v1beta1
-            command: /plugins/${PLUGIN_COMMAND}
-kind: ConfigMap
-metadata:
-  labels:
-    app.kubernetes.io/name: kueue
-    control-plane: controller-manager
-  name: kueue-manager-config
-  namespace: kueue-system
-```
+{{% /alert %}}
+
+### Install the access provider plugin
 
-### Install Required Plugins
+The access provider defined in `ClusterProfile` depends upon an executable plugin being available to the
+Kueue controller manager pods running within the MultiKueue manager cluster.
+It's the responsibility of the Kueue administrator to make sure that the required command is available.
 
-If your credentials provider requires an executable plugin, you must make it available to the Kueue manager.
+An example plugin can be found [here](https://github.com/kubernetes-sigs/cluster-inventory-api/tree/445319b6307a88778b930e154ed3e2f38d85a689/cmd/secretreader-plugin) (secret reader) or [here](https://cloud.google.com/blog/products/containers-kubernetes/kubectl-auth-changes-in-gke) (Google Cloud Platform).
 
-#### Add plugins via volume mounts
+#### Mount the executable with `initContainers`
```

  Thanks, please test when you have a chance, we can follow up with a small update then if something pops up
