# PR #8396: KEP to support limiting the quota check to only resources declared in ClusterQueue

**Summary**: KEP to support limiting the quota check to only resources declared in ClusterQueue

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8396

**Last updated**: 2026-03-06T19:21:54Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@MaysaMacedo](https://github.com/MaysaMacedo)
- **Branch**: `kep-include-resource-prefixes` → `main`
- **Created**: 2025-12-23T13:34:04Z
- **Updated**: 2026-03-06T19:21:54Z
- **Merged**: 2026-03-06T19:20:19Z
- **Closed**: 2026-03-06T19:20:19Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`, `kind/kep`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@gabesaba](https://github.com/gabesaba), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 2   **+375 / -0**

## Description

#### What type of PR is this?
/kind feature
/kind documentation

#### What this PR does / why we need it:
This commit details the changes required to support a new configuration field, `quotaCheckStrategy`.

#### Which issue(s) this PR fixes:
KEP for https://github.com/kubernetes-sigs/kueue/issues/7513

#### Special notes for your reviewer:
Assisted with Claude

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-12-23T13:34:11Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 6a50dc5c2a0ae9bb6079bb5f92d9d3b90a7a4dd4 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69a990200ccbf10008db6455 |

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2025-12-23T13:40:16Z

/cc @amy @kannon92

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2025-12-23T13:46:11Z

/retest

### Comment by [@kannon92](https://github.com/kannon92) — 2026-01-13T15:46:26Z

/assign @mimowo @tenzen-y @gabesaba

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-13T16:14:09Z

I will try to get here this week

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-12T09:56:44Z

Hi @MaysaMacedo what is the status of the KEP? Are there any comments  that you have difficulty addressing? It would be great to move this forward.

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-02-12T13:09:30Z

> Hi @MaysaMacedo what is the status of the KEP? Are there any comments that you have difficulty addressing? It would be great to move this forward.

@mimowo it would be great to have more inputs on this scenario:
If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is defined in ClusterQueue:
    - Kueue does not evaluate the resource quota.
https://github.com/kubernetes-sigs/kueue/pull/8396/changes#diff-61a919e61d079bd8e05a912455b21b1d8ba4060079e2e6354e2183862f25f027R156
it's not clear to me what is the best approach.

It also would be good to have a confirmation to this https://github.com/kubernetes-sigs/kueue/pull/8396#discussion_r2722703256

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-12T18:07:39Z

Hi @MaysaMacedo so Iiuc the concern is that it can be misleading and tricky for admins that they have configured some prefix, but later when they need to add a resource to CQ they may forget that they made the global change which does not list the resource. is that the concern?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-12T18:10:20Z

If this is the concern then I'm considering another API, basically have a global knob in the configMap like 'QuotaCheck: OnlyDeclared'. By default we would use 'QuotaCheck: all's.

When OnlyDeclared is set then Kueue evaluates only resources declared in CQ, and will let workloads to schedule even if they request extra resources.

TBH I think this mode of operation would also eliminate the need for excludeResourcePrefixes. WDYT? cc @kannon92, @gabesaba @tenzen-y

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-02-12T18:37:15Z

@mimowo My concern is the following:
```
includeResourcePrefixes: ["memory"]
clusterQueue.resourceGroups.coveredResources: ["cpu"]
```
A Job is created with resources defined as "cpu", what should happen to this Job given "cpu" is defined on the clusterQueue, but not on includeResourcePrefixes? should Kueue evaluate the resource quota or not?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-12T18:42:23Z

> A Job is created with resources defined as "cpu", what should happen to this Job given "cpu" is defined on the clusterQueue, but not on includeResourcePrefixes? should Kueue evaluate the resource quota or not?

Again, I want to evaluate an alternative design:
```yaml
configuration.resources.quotaCheck: OnlyDeclared # in the config map
clusterQueue.resourceGroups.coveredResources: ["cpu"]
```
Then, when:
1. Job declares "cpu" -> check only "cpu"
2. Job declares "cpu" and "memory"  -> check only "cpu" (currently it would get blocked)

Does this cover your use-cases? If so, then I think the API eliminates the confusion

EDIT: And I think it also would nicely eliminates the original use-case for excludeResourcePrefiexes for network interfaces. Currently `excludeResourcePrefiexes` is used to exclude network interface prefixes, say `gke.networking.io/interface-` (or something like this). Then, admins could just say:`quotaCheck: OnlyDeclared` and the extra interfaces required by Jobs would be ignored.

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-02-12T19:12:50Z

@mimowo Yes, that is a great idea. It eliminates the confusions. Just to recap:
- QuotaCheck: OnlyDeclared' 
  - only resources defined on ClusterQueue are evaluated
  - any other resource defined on the job that doesn't match a resource in clusterQueue is ignored(doesn't have quota evaluated).
~any resource defined on ClusterQueue that is not present on job, would get the job suspended because of required asset not present?~
EDIT: I'm not sure it makes sense for the ClusterQueue to make a job be suspended if a resource from clusterQueue is not present on the Pod. I think this should be:
   - any resource defined on ClusterQueue that is not present on job, would get ignored.
- QuotaCheck: All': all resources on the job are evaluated, which is the default behavior currently.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-12T19:14:10Z

Exactly!

### Comment by [@kannon92](https://github.com/kannon92) — 2026-02-12T19:14:45Z

Should it be an API on ClusterQueue? Or we think it would always be all or nothing for all CQ?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-12T19:18:20Z

> Should it be an API on ClusterQueue? Or we think it would always be all or nothing for all CQ?

I would start with global, maybe just under `resources`. wdyt? Or in alpha we could even just go with global FG, and transform into API field later, but I'm also ok starting with configMap already. 

Unless you have use cases which warrant per-Cq.

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-02-12T19:27:26Z

If we go with this new API, should we plan to deprecate `excludeResourcesPrefixes`?
Given `excludeResourcesPrefixes` is a config, maybe we should go with that for `QuotaCheck`?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-12T19:30:39Z

i think so, but no strong view, depnds also on other maintainers.I would deprecate it. The only use case i know about is excluding the betwork interfaces from quota which will be covered now.

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-02-14T13:22:39Z

I'm moving this to WiP as I'm working on the new design suggested by @mimowo.
It would be great to hear the feedback from others [on the new design](https://github.com/kubernetes-sigs/kueue/pull/8396#issuecomment-3892742357) while I work on adapting the KEP.

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-02-21T21:20:43Z

@mimowo @kannon92 @amy @tenzen-y @gabesaba I just updated the KEP with the new design proposal.
It would be great if you could take a look. I added as a separate commit, which I can squash later if we decide to go with the new design.
The thing I'm not certain is if the place were I'm suggesting to add the filtering, couldn't affect other scenarios of Kueue unrelated to the KEP https://github.com/kubernetes-sigs/kueue/pull/8396/changes#diff-3d84ca7a028719ed8def7b1b2b03c5fda1bf0d962b27aeff5b093ae5430c65ceR161.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-23T15:41:04Z

/retitle KEP to support limiting the quota check to only resources declared in ClusterQueue
LGTM basically, just nits.

cc @tenzen-y @gabesaba wdyt?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-24T03:41:06Z

> /retitle KEP to support limiting the quota check to only resources declared in ClusterQueue
> 
> LGTM basically, just nits.
> 
> 
> 
> cc @tenzen-y @gabesaba wdyt?

ACK

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-27T16:39:27Z

/kind kep
Adding new label for the ease of filtering PRs which are KEPs  / designs are require special handling

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-05T09:02:04Z

LGTM label has been added.  <details>Git tree hash: 2baa4d9bb3d458f886d3678d8ec4afc595c813c6</details>

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-03-05T13:19:51Z

@mimowo thank you for the review!
I just squashed all commits together to have a clean history and lost your LGTM. If you could check it again, that would be great.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-05T14:18:20Z

LGTM label has been added.  <details>Git tree hash: 00f3ce1a7175999ac7a1ffa328bd40cc5e9021eb</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-06T18:50:40Z

/hold cancel

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-06T18:50:42Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8396#" title="Author self-approved">MaysaMacedo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8396#pullrequestreview-3896964882" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8396#pullrequestreview-3905410763" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2025-12-23T13:38:05Z

- **keps/7513-include-resource-prefixes/README.md:145** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2025-12-23T13:38:05Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
+
+### Implementation Details
+
+The resource filtering logic will be modified to:
+
+1. If `includeResourcePrefixes` is set and non-empty:
+   - For each resource in the pod's requests/limits, check if the resource name has a
+     prefix matching any entry in `includeResourcePrefixes`
+   - Only include the resource in quota calculations if it matches
+   - Pod resources not matching any prefix are ignored from quota calculations
+
+2. If `includeResourcePrefixes` is set and empty:
+    - Pod resources are ignored from quota calculations
```

  I would like to have additional opinions on this matter.

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2025-12-23T13:38:45Z

- **keps/7513-include-resource-prefixes/README.md:162** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2025-12-23T13:38:44Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
+
+### Implementation Details
+
+The resource filtering logic will be modified to:
+
+1. If `includeResourcePrefixes` is set and non-empty:
+   - For each resource in the pod's requests/limits, check if the resource name has a
+     prefix matching any entry in `includeResourcePrefixes`
+   - Only include the resource in quota calculations if it matches
+   - Pod resources not matching any prefix are ignored from quota calculations
+
+2. If `includeResourcePrefixes` is set and empty:
+    - Pod resources are ignored from quota calculations
+
+3. If `includeResourcePrefixes` and `excludeResourcePrefixes` are set:
+   - Reject the configuration during validation (mutually exclusive)
+
+4. If neither is set:
+   - All Pod resources are managed (existing default behavior)
+
+### Relation with ClusterQueue
+
+1. If Resource prefix is set in `includeResourcePrefixes` and its quota is defined in ClusterQueue:
+    - Workload quota is evaluated and workload is admitted
+2. If Resource prefix is set in `includeResourcePrefixes` and its quota is not defined in ClusterQueue:
+    - Mandatory Resource not included in ClusterQueue. Workload is suspended
+3. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is not defined in ClusterQueue:
+    - Kueue does not care about the resource quota and it should be admitted
+4. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is defined in ClusterQueue:
+    - Kueue does not evaluate the resource quota.
```

  I'm not sure if this should be the expected behavior. Does anyone have additional thoughts?

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2025-12-23T13:39:30Z

- **keps/7513-include-resource-prefixes/README.md:224** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2025-12-23T13:39:30Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
+
+### Implementation Details
+
+The resource filtering logic will be modified to:
+
+1. If `includeResourcePrefixes` is set and non-empty:
+   - For each resource in the pod's requests/limits, check if the resource name has a
+     prefix matching any entry in `includeResourcePrefixes`
+   - Only include the resource in quota calculations if it matches
+   - Pod resources not matching any prefix are ignored from quota calculations
+
+2. If `includeResourcePrefixes` is set and empty:
+    - Pod resources are ignored from quota calculations
+
+3. If `includeResourcePrefixes` and `excludeResourcePrefixes` are set:
+   - Reject the configuration during validation (mutually exclusive)
+
+4. If neither is set:
+   - All Pod resources are managed (existing default behavior)
+
+### Relation with ClusterQueue
+
+1. If Resource prefix is set in `includeResourcePrefixes` and its quota is defined in ClusterQueue:
+    - Workload quota is evaluated and workload is admitted
+2. If Resource prefix is set in `includeResourcePrefixes` and its quota is not defined in ClusterQueue:
+    - Mandatory Resource not included in ClusterQueue. Workload is suspended
+3. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is not defined in ClusterQueue:
+    - Kueue does not care about the resource quota and it should be admitted
+4. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is defined in ClusterQueue:
+    - Kueue does not evaluate the resource quota.
+
+### Interaction with excludeResourcePrefixes
+
+To maintain simplicity and avoid confusion, we can make `includeResourcePrefixes`
+and `excludeResourcePrefixes` mutually exclusive by making the configuration validation
+ reject configurations where both fields are set.
+
+### Test Plan
+
+[X] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+- In the test validation for configuration:
+  - Valid configurations with `includeResourcePrefixes` set
+  - Rejection when both `includeResourcePrefixes` and `excludeResourcePrefixes` are set
+  - Empty `includeResourcePrefixes` is treated as any Pod resource are exempt from quota management
+
+- In the workload test resource filtering logic:
+  - Resources matching include prefixes are counted
+  - Resources not matching include prefixes are ignored
+  - Edge cases: empty resource names, malformed resource names
+
+#### Integration tests
+
+- Scheduler tests with `includeResourcePrefixes` configured:
+  - Workloads requesting included resources with quota defined in the `ClusterQueue` are admitted normally
+  - Workloads requesting included resources without quota defined in the `ClusterQueue` are suspended
+  - Workloads requesting both included and non-included resources have only included resources counted
+  - Workloads requesting non-included resources without quota defined in the `ClusterQueue` moves to running as Kueue doesn't care about it
+
+### Graduation Criteria
+
+This feature will follow the standard Kueue graduation criteria:
+
+**Alpha**:
+- API changes implemented behind a feature gate, which is disabled by default
+- Basic functionality working with unit tests
+- Documentation covering the feature
+
+**Beta**:
+- Positive feedback from Alpha
+- Feature gate enabled by default
+- Integration tests added
+
+**Stable**:
+- Feature has been in beta for at least one release
+- No major bugs reported
+
+## Implementation History
+
+- 2025-12-22: First draft of the KEP
+
+## Drawbacks
+
+- Adds another configuration option
+- Users need to understand the difference between include and exclude approaches
+- Users who want to manage "all except a few" resources will find exclude approach
+  simpler than include approach
+- Requires documentation and potential migration for users who want to switch approaches
```

  Do we need to care/cover the scenario of users migration from `excludeResourcePrefixes` to `includeResourcePrefixes`?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-02T15:16:35Z

- **keps/7513-include-resource-prefixes/README.md:196** by [@kannon92](https://github.com/kannon92) — 2026-01-02T15:07:36Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
+
+### Implementation Details
+
+The resource filtering logic will be modified to:
+
+1. If `includeResourcePrefixes` is set and non-empty:
+   - For each resource in the pod's requests/limits, check if the resource name has a
+     prefix matching any entry in `includeResourcePrefixes`
+   - Only include the resource in quota calculations if it matches
+   - Pod resources not matching any prefix are ignored from quota calculations
+
+2. If `includeResourcePrefixes` is set and empty:
+    - Pod resources are ignored from quota calculations
+
+3. If `includeResourcePrefixes` and `excludeResourcePrefixes` are set:
+   - Reject the configuration during validation (mutually exclusive)
+
+4. If neither is set:
+   - All Pod resources are managed (existing default behavior)
+
+### Relation with ClusterQueue
+
+1. If Resource prefix is set in `includeResourcePrefixes` and its quota is defined in ClusterQueue:
+    - Workload quota is evaluated and workload is admitted
+2. If Resource prefix is set in `includeResourcePrefixes` and its quota is not defined in ClusterQueue:
+    - Mandatory Resource not included in ClusterQueue. Workload is suspended
+3. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is not defined in ClusterQueue:
+    - Kueue does not care about the resource quota and it should be admitted
+4. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is defined in ClusterQueue:
+    - Kueue does not evaluate the resource quota.
+
+### Interaction with excludeResourcePrefixes
+
+To maintain simplicity and avoid confusion, we can make `includeResourcePrefixes`
+and `excludeResourcePrefixes` mutually exclusive by making the configuration validation
+ reject configurations where both fields are set.
+
+### Test Plan
+
+[X] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+- In the test validation for configuration:
+  - Valid configurations with `includeResourcePrefixes` set
+  - Rejection when both `includeResourcePrefixes` and `excludeResourcePrefixes` are set
+  - Empty `includeResourcePrefixes` is treated as any Pod resource are exempt from quota management
+
+- In the workload test resource filtering logic:
+  - Resources matching include prefixes are counted
+  - Resources not matching include prefixes are ignored
+  - Edge cases: empty resource names, malformed resource names
+
+#### Integration tests
+
+- Scheduler tests with `includeResourcePrefixes` configured:
+  - Workloads requesting included resources with quota defined in the `ClusterQueue` are admitted normally
+  - Workloads requesting included resources without quota defined in the `ClusterQueue` are suspended
+  - Workloads requesting both included and non-included resources have only included resources counted
+  - Workloads requesting non-included resources without quota defined in the `ClusterQueue` moves to running as Kueue doesn't care about it
+
+### Graduation Criteria
```

  @mimowo @tenzen-y 
  
  Since this is an opt-in API field for configuration do we need to have alpha, beta criteria for this?
  
  Is it sufficient to "GA" this feature in 0.16 but its is only opt in for people?
  
  I just don't see a reason for feature gates for this since we can disable via configuration.

- **keps/7513-include-resource-prefixes/README.md:144** by [@kannon92](https://github.com/kannon92) — 2026-01-02T15:12:16Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
+
+### Implementation Details
+
+The resource filtering logic will be modified to:
+
+1. If `includeResourcePrefixes` is set and non-empty:
+   - For each resource in the pod's requests/limits, check if the resource name has a
+     prefix matching any entry in `includeResourcePrefixes`
+   - Only include the resource in quota calculations if it matches
+   - Pod resources not matching any prefix are ignored from quota calculations
+
+2. If `includeResourcePrefixes` is set and empty:
```

  I would like to disallow set and empty.
  
  it may be that we have to rethink the API field.
  
  Maybe we add something like
  
  ```
  struct config {
  
  //  +optional
     IncludeResources *IncludeResources `json:"includeResources,omitempty"`
  }
  
  struct IncludeResources {
    // minimum length of 1 if set
    // +required
    includeResourcesPrefixes []string `json:includeResourcePrefixes,omitempty"`
  }
  ```
  
  That could work.
  
  Or we could ignore if empty and assume its not set.

- **keps/7513-include-resource-prefixes/README.md:145** by [@kannon92](https://github.com/kannon92) — 2026-01-02T15:13:23Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
+
+### Implementation Details
+
+The resource filtering logic will be modified to:
+
+1. If `includeResourcePrefixes` is set and non-empty:
+   - For each resource in the pod's requests/limits, check if the resource name has a
+     prefix matching any entry in `includeResourcePrefixes`
+   - Only include the resource in quota calculations if it matches
+   - Pod resources not matching any prefix are ignored from quota calculations
+
+2. If `includeResourcePrefixes` is set and empty:
+    - Pod resources are ignored from quota calculations
```

  Above I give a suggestion. I don't like this option. I think empty should revert to behavior that this was never set.

- **keps/7513-include-resource-prefixes/README.md:158** by [@kannon92](https://github.com/kannon92) — 2026-01-02T15:14:24Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
+
+### Implementation Details
+
+The resource filtering logic will be modified to:
+
+1. If `includeResourcePrefixes` is set and non-empty:
+   - For each resource in the pod's requests/limits, check if the resource name has a
+     prefix matching any entry in `includeResourcePrefixes`
+   - Only include the resource in quota calculations if it matches
+   - Pod resources not matching any prefix are ignored from quota calculations
+
+2. If `includeResourcePrefixes` is set and empty:
+    - Pod resources are ignored from quota calculations
+
+3. If `includeResourcePrefixes` and `excludeResourcePrefixes` are set:
+   - Reject the configuration during validation (mutually exclusive)
+
+4. If neither is set:
+   - All Pod resources are managed (existing default behavior)
+
+### Relation with ClusterQueue
+
+1. If Resource prefix is set in `includeResourcePrefixes` and its quota is defined in ClusterQueue:
+    - Workload quota is evaluated and workload is admitted
+2. If Resource prefix is set in `includeResourcePrefixes` and its quota is not defined in ClusterQueue:
+    - Mandatory Resource not included in ClusterQueue. Workload is suspended
```

  For this option, what UX will we expose to users that this happened? Should we log an event or an error in logs?

- **keps/7513-include-resource-prefixes/README.md:162** by [@kannon92](https://github.com/kannon92) — 2026-01-02T15:16:01Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
+
+### Implementation Details
+
+The resource filtering logic will be modified to:
+
+1. If `includeResourcePrefixes` is set and non-empty:
+   - For each resource in the pod's requests/limits, check if the resource name has a
+     prefix matching any entry in `includeResourcePrefixes`
+   - Only include the resource in quota calculations if it matches
+   - Pod resources not matching any prefix are ignored from quota calculations
+
+2. If `includeResourcePrefixes` is set and empty:
+    - Pod resources are ignored from quota calculations
+
+3. If `includeResourcePrefixes` and `excludeResourcePrefixes` are set:
+   - Reject the configuration during validation (mutually exclusive)
+
+4. If neither is set:
+   - All Pod resources are managed (existing default behavior)
+
+### Relation with ClusterQueue
+
+1. If Resource prefix is set in `includeResourcePrefixes` and its quota is defined in ClusterQueue:
+    - Workload quota is evaluated and workload is admitted
+2. If Resource prefix is set in `includeResourcePrefixes` and its quota is not defined in ClusterQueue:
+    - Mandatory Resource not included in ClusterQueue. Workload is suspended
+3. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is not defined in ClusterQueue:
+    - Kueue does not care about the resource quota and it should be admitted
+4. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is defined in ClusterQueue:
+    - Kueue does not evaluate the resource quota.
```

  I disagree with this. I would like CQ to override all configuration options.
  
  So I think in this case, Kueue should care about resource quota if its specified in CQ.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-07T16:59:03Z

- **keps/7513-include-resource-prefixes/README.md:224** by [@kannon92](https://github.com/kannon92) — 2026-01-07T16:59:03Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
+
+### Implementation Details
+
+The resource filtering logic will be modified to:
+
+1. If `includeResourcePrefixes` is set and non-empty:
+   - For each resource in the pod's requests/limits, check if the resource name has a
+     prefix matching any entry in `includeResourcePrefixes`
+   - Only include the resource in quota calculations if it matches
+   - Pod resources not matching any prefix are ignored from quota calculations
+
+2. If `includeResourcePrefixes` is set and empty:
+    - Pod resources are ignored from quota calculations
+
+3. If `includeResourcePrefixes` and `excludeResourcePrefixes` are set:
+   - Reject the configuration during validation (mutually exclusive)
+
+4. If neither is set:
+   - All Pod resources are managed (existing default behavior)
+
+### Relation with ClusterQueue
+
+1. If Resource prefix is set in `includeResourcePrefixes` and its quota is defined in ClusterQueue:
+    - Workload quota is evaluated and workload is admitted
+2. If Resource prefix is set in `includeResourcePrefixes` and its quota is not defined in ClusterQueue:
+    - Mandatory Resource not included in ClusterQueue. Workload is suspended
+3. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is not defined in ClusterQueue:
+    - Kueue does not care about the resource quota and it should be admitted
+4. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is defined in ClusterQueue:
+    - Kueue does not evaluate the resource quota.
+
+### Interaction with excludeResourcePrefixes
+
+To maintain simplicity and avoid confusion, we can make `includeResourcePrefixes`
+and `excludeResourcePrefixes` mutually exclusive by making the configuration validation
+ reject configurations where both fields are set.
+
+### Test Plan
+
+[X] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+- In the test validation for configuration:
+  - Valid configurations with `includeResourcePrefixes` set
+  - Rejection when both `includeResourcePrefixes` and `excludeResourcePrefixes` are set
+  - Empty `includeResourcePrefixes` is treated as any Pod resource are exempt from quota management
+
+- In the workload test resource filtering logic:
+  - Resources matching include prefixes are counted
+  - Resources not matching include prefixes are ignored
+  - Edge cases: empty resource names, malformed resource names
+
+#### Integration tests
+
+- Scheduler tests with `includeResourcePrefixes` configured:
+  - Workloads requesting included resources with quota defined in the `ClusterQueue` are admitted normally
+  - Workloads requesting included resources without quota defined in the `ClusterQueue` are suspended
+  - Workloads requesting both included and non-included resources have only included resources counted
+  - Workloads requesting non-included resources without quota defined in the `ClusterQueue` moves to running as Kueue doesn't care about it
+
+### Graduation Criteria
+
+This feature will follow the standard Kueue graduation criteria:
+
+**Alpha**:
+- API changes implemented behind a feature gate, which is disabled by default
+- Basic functionality working with unit tests
+- Documentation covering the feature
+
+**Beta**:
+- Positive feedback from Alpha
+- Feature gate enabled by default
+- Integration tests added
+
+**Stable**:
+- Feature has been in beta for at least one release
+- No major bugs reported
+
+## Implementation History
+
+- 2025-12-22: First draft of the KEP
+
+## Drawbacks
+
+- Adds another configuration option
+- Users need to understand the difference between include and exclude approaches
+- Users who want to manage "all except a few" resources will find exclude approach
+  simpler than include approach
+- Requires documentation and potential migration for users who want to switch approaches
```

  I don't think so.
  
  But we should document includeResourcePrefixes and mention in our documentation that these options are mutually exclusive.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-07T16:59:21Z

- **keps/7513-include-resource-prefixes/README.md:162** by [@kannon92](https://github.com/kannon92) — 2026-01-07T16:59:21Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
+
+### Implementation Details
+
+The resource filtering logic will be modified to:
+
+1. If `includeResourcePrefixes` is set and non-empty:
+   - For each resource in the pod's requests/limits, check if the resource name has a
+     prefix matching any entry in `includeResourcePrefixes`
+   - Only include the resource in quota calculations if it matches
+   - Pod resources not matching any prefix are ignored from quota calculations
+
+2. If `includeResourcePrefixes` is set and empty:
+    - Pod resources are ignored from quota calculations
+
+3. If `includeResourcePrefixes` and `excludeResourcePrefixes` are set:
+   - Reject the configuration during validation (mutually exclusive)
+
+4. If neither is set:
+   - All Pod resources are managed (existing default behavior)
+
+### Relation with ClusterQueue
+
+1. If Resource prefix is set in `includeResourcePrefixes` and its quota is defined in ClusterQueue:
+    - Workload quota is evaluated and workload is admitted
+2. If Resource prefix is set in `includeResourcePrefixes` and its quota is not defined in ClusterQueue:
+    - Mandatory Resource not included in ClusterQueue. Workload is suspended
+3. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is not defined in ClusterQueue:
+    - Kueue does not care about the resource quota and it should be admitted
+4. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is defined in ClusterQueue:
+    - Kueue does not evaluate the resource quota.
```

  https://github.com/kubernetes-sigs/kueue/pull/8396#discussion_r2657888188

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-07T16:59:33Z

- **keps/7513-include-resource-prefixes/README.md:145** by [@kannon92](https://github.com/kannon92) — 2026-01-07T16:59:33Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
+
+### Implementation Details
+
+The resource filtering logic will be modified to:
+
+1. If `includeResourcePrefixes` is set and non-empty:
+   - For each resource in the pod's requests/limits, check if the resource name has a
+     prefix matching any entry in `includeResourcePrefixes`
+   - Only include the resource in quota calculations if it matches
+   - Pod resources not matching any prefix are ignored from quota calculations
+
+2. If `includeResourcePrefixes` is set and empty:
+    - Pod resources are ignored from quota calculations
```

  https://github.com/kubernetes-sigs/kueue/pull/8396#discussion_r2657882267

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-01-09T21:41:59Z

- **keps/7513-include-resource-prefixes/README.md:162** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-01-09T21:41:59Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
+
+### Implementation Details
+
+The resource filtering logic will be modified to:
+
+1. If `includeResourcePrefixes` is set and non-empty:
+   - For each resource in the pod's requests/limits, check if the resource name has a
+     prefix matching any entry in `includeResourcePrefixes`
+   - Only include the resource in quota calculations if it matches
+   - Pod resources not matching any prefix are ignored from quota calculations
+
+2. If `includeResourcePrefixes` is set and empty:
+    - Pod resources are ignored from quota calculations
+
+3. If `includeResourcePrefixes` and `excludeResourcePrefixes` are set:
+   - Reject the configuration during validation (mutually exclusive)
+
+4. If neither is set:
+   - All Pod resources are managed (existing default behavior)
+
+### Relation with ClusterQueue
+
+1. If Resource prefix is set in `includeResourcePrefixes` and its quota is defined in ClusterQueue:
+    - Workload quota is evaluated and workload is admitted
+2. If Resource prefix is set in `includeResourcePrefixes` and its quota is not defined in ClusterQueue:
+    - Mandatory Resource not included in ClusterQueue. Workload is suspended
+3. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is not defined in ClusterQueue:
+    - Kueue does not care about the resource quota and it should be admitted
+4. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is defined in ClusterQueue:
+    - Kueue does not evaluate the resource quota.
```

  Ok, let's see if we have additional inputs.

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-01-09T21:42:28Z

- **keps/7513-include-resource-prefixes/README.md:158** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-01-09T21:42:28Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
+
+### Implementation Details
+
+The resource filtering logic will be modified to:
+
+1. If `includeResourcePrefixes` is set and non-empty:
+   - For each resource in the pod's requests/limits, check if the resource name has a
+     prefix matching any entry in `includeResourcePrefixes`
+   - Only include the resource in quota calculations if it matches
+   - Pod resources not matching any prefix are ignored from quota calculations
+
+2. If `includeResourcePrefixes` is set and empty:
+    - Pod resources are ignored from quota calculations
+
+3. If `includeResourcePrefixes` and `excludeResourcePrefixes` are set:
+   - Reject the configuration during validation (mutually exclusive)
+
+4. If neither is set:
+   - All Pod resources are managed (existing default behavior)
+
+### Relation with ClusterQueue
+
+1. If Resource prefix is set in `includeResourcePrefixes` and its quota is defined in ClusterQueue:
+    - Workload quota is evaluated and workload is admitted
+2. If Resource prefix is set in `includeResourcePrefixes` and its quota is not defined in ClusterQueue:
+    - Mandatory Resource not included in ClusterQueue. Workload is suspended
```

  Could be both.

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-01-09T21:44:31Z

- **keps/7513-include-resource-prefixes/README.md:196** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-01-09T21:44:31Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
+
+### Implementation Details
+
+The resource filtering logic will be modified to:
+
+1. If `includeResourcePrefixes` is set and non-empty:
+   - For each resource in the pod's requests/limits, check if the resource name has a
+     prefix matching any entry in `includeResourcePrefixes`
+   - Only include the resource in quota calculations if it matches
+   - Pod resources not matching any prefix are ignored from quota calculations
+
+2. If `includeResourcePrefixes` is set and empty:
+    - Pod resources are ignored from quota calculations
+
+3. If `includeResourcePrefixes` and `excludeResourcePrefixes` are set:
+   - Reject the configuration during validation (mutually exclusive)
+
+4. If neither is set:
+   - All Pod resources are managed (existing default behavior)
+
+### Relation with ClusterQueue
+
+1. If Resource prefix is set in `includeResourcePrefixes` and its quota is defined in ClusterQueue:
+    - Workload quota is evaluated and workload is admitted
+2. If Resource prefix is set in `includeResourcePrefixes` and its quota is not defined in ClusterQueue:
+    - Mandatory Resource not included in ClusterQueue. Workload is suspended
+3. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is not defined in ClusterQueue:
+    - Kueue does not care about the resource quota and it should be admitted
+4. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is defined in ClusterQueue:
+    - Kueue does not evaluate the resource quota.
+
+### Interaction with excludeResourcePrefixes
+
+To maintain simplicity and avoid confusion, we can make `includeResourcePrefixes`
+and `excludeResourcePrefixes` mutually exclusive by making the configuration validation
+ reject configurations where both fields are set.
+
+### Test Plan
+
+[X] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+- In the test validation for configuration:
+  - Valid configurations with `includeResourcePrefixes` set
+  - Rejection when both `includeResourcePrefixes` and `excludeResourcePrefixes` are set
+  - Empty `includeResourcePrefixes` is treated as any Pod resource are exempt from quota management
+
+- In the workload test resource filtering logic:
+  - Resources matching include prefixes are counted
+  - Resources not matching include prefixes are ignored
+  - Edge cases: empty resource names, malformed resource names
+
+#### Integration tests
+
+- Scheduler tests with `includeResourcePrefixes` configured:
+  - Workloads requesting included resources with quota defined in the `ClusterQueue` are admitted normally
+  - Workloads requesting included resources without quota defined in the `ClusterQueue` are suspended
+  - Workloads requesting both included and non-included resources have only included resources counted
+  - Workloads requesting non-included resources without quota defined in the `ClusterQueue` moves to running as Kueue doesn't care about it
+
+### Graduation Criteria
```

  Fair point, maybe we don't need alpha, beta, criteria. I will leave it as is to hear additional thoughts.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-01-13T17:33:05Z

I didn't review all sections of this KEP, but I left very initial comments.

- **keps/7513-include-resource-prefixes/README.md:132** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-13T17:28:31Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
```

  We might need to another structure `ManageResource`

- **keps/7513-include-resource-prefixes/README.md:116** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-13T17:28:42Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
```

  ```suggestion
  type Resources struct {
  ```
  
  Do you meant that?

- **keps/7513-include-resource-prefixes/README.md:132** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-13T17:32:12Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
```

  We might need to use another structure, `prefixes` for better expressions, then deprecate exsiting ExcludeResourcePrefixes field. @mimowo What about deprecating the existing field?
  
  ```go
  resources:
    prefixes:
      excludes: ["a", "b", "c"]
      includes: ["d", "e", "f"]
  ```

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-01-16T15:07:49Z

- **keps/7513-include-resource-prefixes/README.md:116** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-01-16T15:07:49Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
```

  Yes. Will fix it

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-21T16:32:59Z

- **keps/7513-include-resource-prefixes/README.md:162** by [@gabesaba](https://github.com/gabesaba) — 2026-01-21T16:32:59Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
+
+### Implementation Details
+
+The resource filtering logic will be modified to:
+
+1. If `includeResourcePrefixes` is set and non-empty:
+   - For each resource in the pod's requests/limits, check if the resource name has a
+     prefix matching any entry in `includeResourcePrefixes`
+   - Only include the resource in quota calculations if it matches
+   - Pod resources not matching any prefix are ignored from quota calculations
+
+2. If `includeResourcePrefixes` is set and empty:
+    - Pod resources are ignored from quota calculations
+
+3. If `includeResourcePrefixes` and `excludeResourcePrefixes` are set:
+   - Reject the configuration during validation (mutually exclusive)
+
+4. If neither is set:
+   - All Pod resources are managed (existing default behavior)
+
+### Relation with ClusterQueue
+
+1. If Resource prefix is set in `includeResourcePrefixes` and its quota is defined in ClusterQueue:
+    - Workload quota is evaluated and workload is admitted
+2. If Resource prefix is set in `includeResourcePrefixes` and its quota is not defined in ClusterQueue:
+    - Mandatory Resource not included in ClusterQueue. Workload is suspended
+3. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is not defined in ClusterQueue:
+    - Kueue does not care about the resource quota and it should be admitted
+4. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is defined in ClusterQueue:
+    - Kueue does not evaluate the resource quota.
```

  Another option is to deactivate the CQ in this case, until the admin removes the resource from CQ (or adds to `includeResourcePrefixes`. This might be the easiest to understand UI since there's no interaction/overrides.
  
  @kannon92 do you have a use case in mind where this override is needed?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-21T16:48:49Z

- **keps/7513-include-resource-prefixes/README.md:162** by [@kannon92](https://github.com/kannon92) — 2026-01-21T16:48:48Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
+
+### Implementation Details
+
+The resource filtering logic will be modified to:
+
+1. If `includeResourcePrefixes` is set and non-empty:
+   - For each resource in the pod's requests/limits, check if the resource name has a
+     prefix matching any entry in `includeResourcePrefixes`
+   - Only include the resource in quota calculations if it matches
+   - Pod resources not matching any prefix are ignored from quota calculations
+
+2. If `includeResourcePrefixes` is set and empty:
+    - Pod resources are ignored from quota calculations
+
+3. If `includeResourcePrefixes` and `excludeResourcePrefixes` are set:
+   - Reject the configuration during validation (mutually exclusive)
+
+4. If neither is set:
+   - All Pod resources are managed (existing default behavior)
+
+### Relation with ClusterQueue
+
+1. If Resource prefix is set in `includeResourcePrefixes` and its quota is defined in ClusterQueue:
+    - Workload quota is evaluated and workload is admitted
+2. If Resource prefix is set in `includeResourcePrefixes` and its quota is not defined in ClusterQueue:
+    - Mandatory Resource not included in ClusterQueue. Workload is suspended
+3. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is not defined in ClusterQueue:
+    - Kueue does not care about the resource quota and it should be admitted
+4. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is defined in ClusterQueue:
+    - Kueue does not evaluate the resource quota.
```

  I guess my main concern is that people will think the quota is enforced but it is not.
  
  So if someone wants to limit ephemeral-storage or huge-pages or adds a new resource, we would allow the resource but Kueue would ignore it.
  
  Say we includeResourePrefix for GPUs and then someone runs a cpu job, do we just allow the cpu job to cluster? Or do we start the job as suspended?
  
  I guess we could put includeResourePrefix for cpu, memory, gpu to avoid this but I could see this being a common configuration.
  
  I like your idea of deactivating. I also thought maybe this could be a webhook check and writes would be rejected on the CQ if the required resource is not specified.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-21T16:48:51Z

Looks good to me. Just a couple of comments

- **keps/7513-include-resource-prefixes/README.md:74** by [@gabesaba](https://github.com/gabesaba) — 2026-01-21T16:43:23Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
```

  Please add a goal or non-goal for interaction with TAS.
  
  `includeResourcePrefixes` shouldn't affect TAS calculations, right? As TAS already [determines node capacity](https://github.com/kubernetes-sigs/kueue/blob/a85ab5729b123e958f680fa0715496582193cc1b/pkg/cache/scheduler/tas_flavor_snapshot.go#L174-L175) from status automatically, and ignoring some resources would reduce scheduling accuracy without any UX improvements.

- **keps/7513-include-resource-prefixes/README.md:122** by [@gabesaba](https://github.com/gabesaba) — 2026-01-21T16:47:13Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
```

  Since ```ExcludeResourcePrefixes``` already exists, could you either remove it from the KEP, or indicate clearly that this is an existing configuration, left here for comparison?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-21T20:45:46Z

- **keps/7513-include-resource-prefixes/README.md:196** by [@kannon92](https://github.com/kannon92) — 2026-01-21T20:45:45Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
+
+### Implementation Details
+
+The resource filtering logic will be modified to:
+
+1. If `includeResourcePrefixes` is set and non-empty:
+   - For each resource in the pod's requests/limits, check if the resource name has a
+     prefix matching any entry in `includeResourcePrefixes`
+   - Only include the resource in quota calculations if it matches
+   - Pod resources not matching any prefix are ignored from quota calculations
+
+2. If `includeResourcePrefixes` is set and empty:
+    - Pod resources are ignored from quota calculations
+
+3. If `includeResourcePrefixes` and `excludeResourcePrefixes` are set:
+   - Reject the configuration during validation (mutually exclusive)
+
+4. If neither is set:
+   - All Pod resources are managed (existing default behavior)
+
+### Relation with ClusterQueue
+
+1. If Resource prefix is set in `includeResourcePrefixes` and its quota is defined in ClusterQueue:
+    - Workload quota is evaluated and workload is admitted
+2. If Resource prefix is set in `includeResourcePrefixes` and its quota is not defined in ClusterQueue:
+    - Mandatory Resource not included in ClusterQueue. Workload is suspended
+3. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is not defined in ClusterQueue:
+    - Kueue does not care about the resource quota and it should be admitted
+4. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is defined in ClusterQueue:
+    - Kueue does not evaluate the resource quota.
+
+### Interaction with excludeResourcePrefixes
+
+To maintain simplicity and avoid confusion, we can make `includeResourcePrefixes`
+and `excludeResourcePrefixes` mutually exclusive by making the configuration validation
+ reject configurations where both fields are set.
+
+### Test Plan
+
+[X] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+- In the test validation for configuration:
+  - Valid configurations with `includeResourcePrefixes` set
+  - Rejection when both `includeResourcePrefixes` and `excludeResourcePrefixes` are set
+  - Empty `includeResourcePrefixes` is treated as any Pod resource are exempt from quota management
+
+- In the workload test resource filtering logic:
+  - Resources matching include prefixes are counted
+  - Resources not matching include prefixes are ignored
+  - Edge cases: empty resource names, malformed resource names
+
+#### Integration tests
+
+- Scheduler tests with `includeResourcePrefixes` configured:
+  - Workloads requesting included resources with quota defined in the `ClusterQueue` are admitted normally
+  - Workloads requesting included resources without quota defined in the `ClusterQueue` are suspended
+  - Workloads requesting both included and non-included resources have only included resources counted
+  - Workloads requesting non-included resources without quota defined in the `ClusterQueue` moves to running as Kueue doesn't care about it
+
+### Graduation Criteria
```

  Given all our back and forth on this one (via DMs) it may be worth going with a feature gate after all for alpha.

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-01-23T19:18:53Z

- **keps/7513-include-resource-prefixes/README.md:74** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-01-23T19:18:53Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
```

  @gabesaba can you detail more on how you thought having this field could impact the TAS calculations?
  `includeResourcePrefixes` is a matter of filtering which resource to be evaluated or not for quota admission. The actual verification if it should be admitted, I think is made at clusterqueue level.

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-01-23T20:14:18Z

- **keps/7513-include-resource-prefixes/README.md:132** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-01-23T20:14:18Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
```

  Should we go with the change you're proposing?
  Do you think it's easier to understand the purpose of those fields if we go with your proposal?

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-01-23T20:16:34Z

- **keps/7513-include-resource-prefixes/README.md:79** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-01-23T20:16:34Z
```diff
@@ -0,0 +1,235 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
```

  I would like additional inputs on the following:
  Not all the resources from `includeResourcePrefixes` needs to exist in the Resource list of the object for it to be included for quota calculation, right? If one of the resources from `includeResourcePrefixes` matches one of the resources listed in object, only that resource is used for quota validation and the rest is ignored.

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-01-23T20:18:55Z

- **keps/7513-include-resource-prefixes/README.md:162** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-01-23T20:18:55Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
+
+### Implementation Details
+
+The resource filtering logic will be modified to:
+
+1. If `includeResourcePrefixes` is set and non-empty:
+   - For each resource in the pod's requests/limits, check if the resource name has a
+     prefix matching any entry in `includeResourcePrefixes`
+   - Only include the resource in quota calculations if it matches
+   - Pod resources not matching any prefix are ignored from quota calculations
+
+2. If `includeResourcePrefixes` is set and empty:
+    - Pod resources are ignored from quota calculations
+
+3. If `includeResourcePrefixes` and `excludeResourcePrefixes` are set:
+   - Reject the configuration during validation (mutually exclusive)
+
+4. If neither is set:
+   - All Pod resources are managed (existing default behavior)
+
+### Relation with ClusterQueue
+
+1. If Resource prefix is set in `includeResourcePrefixes` and its quota is defined in ClusterQueue:
+    - Workload quota is evaluated and workload is admitted
+2. If Resource prefix is set in `includeResourcePrefixes` and its quota is not defined in ClusterQueue:
+    - Mandatory Resource not included in ClusterQueue. Workload is suspended
+3. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is not defined in ClusterQueue:
+    - Kueue does not care about the resource quota and it should be admitted
+4. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is defined in ClusterQueue:
+    - Kueue does not evaluate the resource quota.
```

  @gabesaba How you think we could deactivate the CQ?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-27T11:41:34Z

- **keps/7513-include-resource-prefixes/README.md:74** by [@gabesaba](https://github.com/gabesaba) — 2026-01-27T11:41:34Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
```

  I think that it shouldn't, though I wanted to confirm with you that you agree with this. In that case, including it in the non-goal would let future readers know that this does not change how TAS does calculations (e.g. ignoring some resource(s) when TAS does its math)

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-27T11:44:37Z

- **keps/7513-include-resource-prefixes/README.md:162** by [@gabesaba](https://github.com/gabesaba) — 2026-01-27T11:44:37Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
+
+### Implementation Details
+
+The resource filtering logic will be modified to:
+
+1. If `includeResourcePrefixes` is set and non-empty:
+   - For each resource in the pod's requests/limits, check if the resource name has a
+     prefix matching any entry in `includeResourcePrefixes`
+   - Only include the resource in quota calculations if it matches
+   - Pod resources not matching any prefix are ignored from quota calculations
+
+2. If `includeResourcePrefixes` is set and empty:
+    - Pod resources are ignored from quota calculations
+
+3. If `includeResourcePrefixes` and `excludeResourcePrefixes` are set:
+   - Reject the configuration during validation (mutually exclusive)
+
+4. If neither is set:
+   - All Pod resources are managed (existing default behavior)
+
+### Relation with ClusterQueue
+
+1. If Resource prefix is set in `includeResourcePrefixes` and its quota is defined in ClusterQueue:
+    - Workload quota is evaluated and workload is admitted
+2. If Resource prefix is set in `includeResourcePrefixes` and its quota is not defined in ClusterQueue:
+    - Mandatory Resource not included in ClusterQueue. Workload is suspended
+3. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is not defined in ClusterQueue:
+    - Kueue does not care about the resource quota and it should be admitted
+4. If Resource prefix is not set in `includeResourcePrefixes` and Resource quota is defined in ClusterQueue:
+    - Kueue does not evaluate the resource quota.
```

  Most likely [here](https://github.com/kubernetes-sigs/kueue/blob/c6d1e51cef7c939ecf37eb252cf1e419d5a53518/pkg/cache/scheduler/clusterqueue.go#L207-L230)

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-27T21:44:30Z

- **keps/7513-include-resource-prefixes/README.md:74** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-27T21:44:30Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
```

  This might be related to https://github.com/kubernetes-sigs/kueue/issues/8838

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-28T13:31:23Z

- **keps/7513-include-resource-prefixes/README.md:74** by [@gabesaba](https://github.com/gabesaba) — 2026-01-28T13:31:23Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
```

  > This might be related to #8838
  
  Yes, exactly.

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-02-02T19:14:59Z

- **keps/7513-include-resource-prefixes/README.md:158** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-02-02T19:14:59Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
+
+### Implementation Details
+
+The resource filtering logic will be modified to:
+
+1. If `includeResourcePrefixes` is set and non-empty:
+   - For each resource in the pod's requests/limits, check if the resource name has a
+     prefix matching any entry in `includeResourcePrefixes`
+   - Only include the resource in quota calculations if it matches
+   - Pod resources not matching any prefix are ignored from quota calculations
+
+2. If `includeResourcePrefixes` is set and empty:
+    - Pod resources are ignored from quota calculations
+
+3. If `includeResourcePrefixes` and `excludeResourcePrefixes` are set:
+   - Reject the configuration during validation (mutually exclusive)
+
+4. If neither is set:
+   - All Pod resources are managed (existing default behavior)
+
+### Relation with ClusterQueue
+
+1. If Resource prefix is set in `includeResourcePrefixes` and its quota is defined in ClusterQueue:
+    - Workload quota is evaluated and workload is admitted
+2. If Resource prefix is set in `includeResourcePrefixes` and its quota is not defined in ClusterQueue:
+    - Mandatory Resource not included in ClusterQueue. Workload is suspended
```

  I see that an event already exists for this scenario, for example:
  `116s        Warning   Pending                             workload/job-sample-job-rrrkm-e3a0b   couldn't assign flavors to pod set main: resource memory unavailable in ClusterQueue`

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-05T16:40:24Z

- **keps/7513-include-resource-prefixes/kep.yaml:19** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-05T16:31:14Z
```diff
@@ -1,32 +1,33 @@
-title: TLS Security Profile Support
-kep-number: 8190
+title: Include resource prefixes
+kep-number: 7513
 authors:
-  - "@kannon92"
-status: implementable 
-creation-date: 2025-12-11
+  - "@MaysaMacedo"
+status: provisional
+creation-date: 2025-12-23
 reviewers:
-  - kshalot
+  - "@kannon92"
+  - "@amy"
 approvers:
-  - mimowo
-  - tenzen-y
+  - "@mimowo"
+  - "@tenzen-y"
 
-see-also:
-replaces:
-
-# The target maturity stage in the current dev cycle for this KEP.
-stage: beta
+stage: alpha
 
 # The most recent milestone for which work toward delivery of this KEP has been
 # done. This can be the current (upcoming) milestone, if it is being actively
 # worked on.
-latest-milestone: "0.16"
+latest-milestone: "v0.16"
```

  ```suggestion
  latest-milestone: "v0.17"
  ```
  
  nit

- **keps/7513-include-resource-prefixes/README.md:116** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-05T16:33:41Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
```

  This has not resolved yet.

- **keps/7513-include-resource-prefixes/README.md:134** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-05T16:39:32Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
+
+### Implementation Details
```

  AFAIK, we observed that TAS doesn't care about `excludeResourcePrefixes`, which caused inaccurate Topology assignment. So we fixed the bugs so that we can account for excluded resources as well: https://github.com/kubernetes-sigs/kueue/issues/8838
  
  Does this new `inclusdeResourcePrefixes` follow the same principle in TAS?

- **keps/7513-include-resource-prefixes/README.md:132** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-05T16:40:19Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
```

  I'd like to reach an agreement with @mimowo before moving this forward.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-05T18:16:19Z

- **keps/7513-include-resource-prefixes/README.md:132** by [@mimowo](https://github.com/mimowo) — 2026-02-05T18:16:19Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
```

  I consider the "resources" word repetitive in the current API.
  
  However, in the proposal I also don't like making the "prefixes" like a legitimate subject. I think they are just a way of expressing the condition. Intuitively I think "resources" or "exclusion" is the subject.
  
  I'm thinking about something more like:
  ```yaml
  resources:
    excludeByPrefixes: 
    includeByPrefixes:
  ```
  For example, I can imagine extensions for "exact match", or  "regexes" for example, like:
  ```yaml
  resources:
    excludeByPrefixes: 
    includeByPrefixes:
    excludes:
    excludeByRegex: 
  ```
  Then likely "exludeByPrefixes" and "excludes" would not be mutually exclusive.
  
  In any case, I wouldn't rather refactor the existing API in this KEP. Maybe this is something we could consider for v1beta3 / v1. 
  
  For now `IncludeResourcePrefixes` seems reasonable for me, by symmetry. I'm also ok with something like `IncludeByPrefixes`, or `restrictByPrefixes`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T11:46:08Z

- **keps/7513-include-resource-prefixes/README.md:132** by [@mimowo](https://github.com/mimowo) — 2026-02-06T11:46:08Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
```

  Long story short  I'm not a fan of `resources.prefixes`, consider two options I find reasonable:
  1. support `IncludeResourcePrefixes` for now for consistency (as currently in the KEP)
  2. deprecate `ExcludeResourcePrefixes` and introduce `excludeByPrefixes` and  `includeByPrefixes` 
  
  Since the future evolution of the API is unclear, I'm leaning towards (1.) for simplicity and defer something like (2.) for v1beta3 or v1.
  
  wdyt @tenzen-y ?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-02-06T12:59:39Z

- **keps/7513-include-resource-prefixes/README.md:132** by [@kannon92](https://github.com/kannon92) — 2026-02-06T12:59:39Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
```

  So the term prefixes is a bit weird as cpu and memory are allow resources.
  
  But I'm fine with getting fields consistent but I wanted to call out that with our naming it isn't clear if cpu/memory are a prefix or just a resource.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T13:03:53Z

- **keps/7513-include-resource-prefixes/README.md:132** by [@mimowo](https://github.com/mimowo) — 2026-02-06T13:03:53Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
```

  Exactly, this is why I feel prefixes should not be a subject here, a more natural API in the future to me is:
  
  ```yaml
  resources:
    includeByPrefixes: [ nvidia.com/ ]
    include: [ CPU, memory]
  ```
  
  so introducing the prefixes concept will be too constraining

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-02-12T12:58:35Z

- **keps/7513-include-resource-prefixes/README.md:132** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-02-12T12:58:34Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
```

  I will keep `IncludeResourcePrefixes` for now and we can think about a refactor in the future.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-12T13:01:06Z

- **keps/7513-include-resource-prefixes/README.md:132** by [@mimowo](https://github.com/mimowo) — 2026-02-12T13:01:06Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
```

  Yes, let's see how the API evolves in practice. We will probably anyway need to adjust this before v1.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-12T13:04:25Z

- **keps/7513-include-resource-prefixes/README.md:132** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-12T13:04:25Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
```

  > I consider the "resources" word repetitive in the current API.
  > 
  > However, in the proposal I also don't like making the "prefixes" like a legitimate subject. I think they are just a way of expressing the condition. Intuitively I think "resources" or "exclusion" is the subject.
  > 
  > I'm thinking about something more like:
  > ```yaml
  > resources:
  >   excludeByPrefixes: 
  >   includeByPrefixes:
  > ```
  > For example, I can imagine extensions for "exact match", or  "regexes" for example, like:
  > ```yaml
  > resources:
  >   excludeByPrefixes: 
  >   includeByPrefixes:
  >   excludes:
  >   excludeByRegex: 
  > ```
  > Then likely "exludeByPrefixes" and "excludes" would not be mutually exclusive.
  > 
  > In any case, I wouldn't rather refactor the existing API in this KEP. Maybe this is something we could consider for v1beta3 / v1. 
  > 
  > For now `IncludeResourcePrefixes` seems reasonable for me, by symmetry. I'm also ok with something like `IncludeByPrefixes`, or `restrictByPrefixes`.
  > 
  > 
  
  That sounds reasonable to me. Let's revisit to break API with `.resources.excludeByPrefixes` and `.resources.includeByPrefixes` when we graduate API version to v1.
  
  Let's open a dedicated issue.

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-02-12T13:05:07Z

- **keps/7513-include-resource-prefixes/README.md:134** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-02-12T13:05:07Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
+
+### Implementation Details
```

  includeResourcesPrefixes won't affect TAS calculations, the fix for the bug you linked would cover include and exclude scenarios because it doesn't use filtered resources. I highlighted in the none-goals
  https://github.com/kubernetes-sigs/kueue/pull/8396/changes#diff-61a919e61d079bd8e05a912455b21b1d8ba4060079e2e6354e2183862f25f027R75

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-12T13:10:50Z

- **keps/7513-include-resource-prefixes/README.md:132** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-12T13:10:50Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
```

  > for simplicity and defer something like (2.) for v1beta3 or v1.
  
  I still think that one more nest under the resources since `include` and `exclude` are mutually exclusive. The additional level allows for representing the field selection as a structure.
  
  But, anyway, we will postpone the structure changes to the future API version.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-12T13:16:44Z

- **keps/7513-include-resource-prefixes/README.md:132** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-12T13:16:44Z
```diff
@@ -0,0 +1,241 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
+important resources from quota management.
+
+**Mitigation**:
+- Clear documentation with examples of common configurations
+- Validation error when both `includeResourcePrefixes` and `excludeResourcePrefixes`
+  are set
+
+## Design Details
+
+### API Changes
+
+Add a new field to the Kueue `Configuration`:
+
+```go
+type Configuration struct {
+    // ...
+    // ExcludeResourcePrefixes defines resource prefixes that will be excluded
+    // from quota management. Resources matching any of these prefixes won't
+    // be counted toward quota.
+    // Cannot be used together with IncludeResourcePrefixes.
+    ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+    // IncludeResourcePrefixes defines resource prefixes that will be included
+    // in quota management. When set, only resources matching one of these
+    // prefixes will be counted toward quota. Resources not matching any prefix
+    // will be ignored.
+    // Cannot be used together with ExcludeResourcePrefixes.
+    // +optional
+    IncludeResourcePrefixes []string `json:"includeResourcePrefixes,omitempty"`
+}
+```
```

  Opened: https://github.com/kubernetes-sigs/kueue/issues/9165

### Review by [@amy](https://github.com/amy) (commented) — 2026-02-12T23:16:53Z

- **keps/7513-include-resource-prefixes/README.md:79** by [@amy](https://github.com/amy) — 2026-02-12T23:16:53Z
```diff
@@ -0,0 +1,235 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
```

  Yes this sounds right. 
  
  So you basically just count quota for the resources that exist within `includeResourcePrefixes` on the pod. 
  
  So if you have: 
  includeResourcePrefixes: cpu, memory, gpu
  pod: 10 gpu, 100 foobar
  
  I would expect Kueue to just consider 10 gpu. Then foobar is ignored. And memory/gpu is 0.

### Review by [@amy](https://github.com/amy) (commented) — 2026-02-12T23:20:25Z

- **keps/7513-include-resource-prefixes/README.md:102** by [@amy](https://github.com/amy) — 2026-02-12T23:20:25Z
```diff
@@ -0,0 +1,236 @@
+# KEP-7513: Include resource prefixes
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Relation with ClusterQueue](#relation-with-clusterqueue)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `includeResourcePrefixes`
+that acts as an allow list of resources that Kueue will manage. Any resource not added
+to this list is exempt from quota management. This addresses the operational challenge
+of Kueue managing workloads in environments where mutating webhooks or controllers add
+unexpected resources to pod specifications.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhooks injects
+    resources into pod specifications these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue` doesn't have
+   the Quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a configuration option to specify an allow list of resource prefixes that Kueue
+  will manage for quota purposes
+- When configured, only resources matching the allow list prefixes will be counted toward
+  quota admission decisions
+- Maintain support for the existing `excludeResourcePrefixes` configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Removing or deprecating the existing `excludeResourcePrefixes` field
+- Automatically detecting which resources should be managed
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration field `includeResourcePrefixes` to the Kueue Configuration API.
+When this field is set, only resources whose names match one of the specified prefixes
+will be counted when making quota admission decisions. Resources not matching any prefix
+in the allow list will be ignored for quota purposes.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `includeResourcePrefixes`, I can configure:
+```yaml
+includeResourcePrefixes:
+  - nvidia.com/
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might misconfigure `includeResourcePrefixes` and accidentally not include
```

  This is not a flushed out idea, so I'm open to better ones. But... maybe we could float up somewhere the resources that appear that we are not counting. So that at least there's a place to do some accounting of what resources we're potentially not considering but should.
  
  Overall, I think this risk is fine, because the interruption without this is a bigger issue and requires a lot of business process / coordination.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-23T15:29:46Z

- **keps/7513-quota-check/README.md:36** by [@mimowo](https://github.com/mimowo) — 2026-02-23T15:29:46Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
```

  ```suggestion
  This KEP proposes to introduce a new configuration API which allows to restrict the
  quota check to only resources which are declared in the `ClusterQueue`. Other resources
  requested by the Pod are skipped from the quota checks.
  ```
  I think this is enough for the summary

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-23T15:32:54Z

- **keps/7513-quota-check/README.md:78** by [@mimowo](https://github.com/mimowo) — 2026-02-23T15:32:55Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
```

  ```suggestion
  Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
  When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions. 
  
  When the field has value `All`, which is the default reflecting the current semantics, all the resources 
  defined in the Workload's PodTemplate are taken into account for quota admission decisions.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-23T15:34:37Z

- **keps/7513-quota-check/README.md:147** by [@mimowo](https://github.com/mimowo) — 2026-02-23T15:34:37Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
+
+	// QuotaCheckOnlyDeclared means only resources declared in the ClusterQueue's
+	// coveredResources are checked against quota. Undeclared resources are ignored.
+	QuotaCheckOnlyDeclared QuotaCheckStrategy = "OnlyDeclared"
+)
+
+type Resources struct {
+    // ...
+    // QuotaCheck determines which resources are considered during quota admission.
+    // +kubebuilder:default="All"
+    // +optional
+    QuotaCheck QuotaCheckStrategy `json:"quotaCheck,omitempty"`
```

  ```suggestion
      // +optional
      QuotaCheck *QuotaCheckStrategy `json:"quotaCheck,omitempty"`
  ```
  Since this is option - and may not be set on the pre-existing clusters.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-23T15:35:36Z

- **keps/7513-quota-check/README.md:129** by [@mimowo](https://github.com/mimowo) — 2026-02-23T15:35:37Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
```

  I think these are ignored, aren't they, because this is Configuration API rather than CRD. So we will not have the generated valiadation rules.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-23T15:37:04Z

- **keps/7513-quota-check-strategy/README.md:236** by [@mimowo](https://github.com/mimowo) — 2026-02-23T15:37:04Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
+
+	// QuotaCheckOnlyDeclared means only resources declared in the ClusterQueue's
+	// coveredResources are checked against quota. Undeclared resources are ignored.
+	QuotaCheckOnlyDeclared QuotaCheckStrategy = "OnlyDeclared"
+)
+
+type Resources struct {
+    // ...
+    // QuotaCheck determines which resources are considered during quota admission.
+    // +kubebuilder:default="All"
+    // +optional
+    QuotaCheck QuotaCheckStrategy `json:"quotaCheck,omitempty"`
+}
+```
+
+### Feature Gate
+
+This feature is gated behind the `QuotaCheck` feature gate.
+
+- **Alpha**: The feature gate is disabled by default. Users must explicitly enable
+  it to use `quotaCheck: "OnlyDeclared"`. When the gate is disabled, the `quotaCheck`
+  configuration field is ignored and the behavior is equivalent to `All`.
+- **Beta**: The feature gate is enabled by default.
+- **GA**: The feature gate is locked to enabled and cannot be disabled.
+
+### Implementation Details
```

  TBH,, I think this section is too much details. If you want to keep it, then just keep the first paragraph.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-23T15:39:34Z

- **keps/7513-quota-check/README.md:194** by [@mimowo](https://github.com/mimowo) — 2026-02-23T15:39:34Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
+
+	// QuotaCheckOnlyDeclared means only resources declared in the ClusterQueue's
+	// coveredResources are checked against quota. Undeclared resources are ignored.
+	QuotaCheckOnlyDeclared QuotaCheckStrategy = "OnlyDeclared"
+)
+
+type Resources struct {
+    // ...
+    // QuotaCheck determines which resources are considered during quota admission.
+    // +kubebuilder:default="All"
+    // +optional
+    QuotaCheck QuotaCheckStrategy `json:"quotaCheck,omitempty"`
+}
+```
+
+### Feature Gate
+
+This feature is gated behind the `QuotaCheck` feature gate.
+
+- **Alpha**: The feature gate is disabled by default. Users must explicitly enable
+  it to use `quotaCheck: "OnlyDeclared"`. When the gate is disabled, the `quotaCheck`
+  configuration field is ignored and the behavior is equivalent to `All`.
+- **Beta**: The feature gate is enabled by default.
+- **GA**: The feature gate is locked to enabled and cannot be disabled.
+
+### Implementation Details
+
+The flavor assigner logic will be modified in the [`findFlavorForPodSets`](https://github.com/kubernetes-sigs/kueue/blob/fe64c96e32130b15e9e5898ffaa1e145761d49d8/pkg/scheduler/flavorassigner/flavorassigner.go#L751) method to:
+
+1. If `quotaCheck` is set with value `OnlyDeclared`:
+   - Only resources that match resources defined in the `ClusterQueue` are used for
+     quota check.
+   - Any resources defined in the Pod that are not defined in the `ClusterQueue` will
+     be skipped for flavor admission.
+   - Any resources defined in the `ClusterQueue` that are not in the Pod will be
+     skipped for quota check.
+
+2. If `quotaCheck` is set with value `All`, which would be the default:
+    - No change is necessary in the `findFlavorForPodSets` method.
+    - All resources defined in the Pod will be used for quota calculations.
+    - Any resources defined in the `excludeResourcePrefixes` remain being excluded
+      in the [`totalRequestsFromPodSets`](https://github.com/kubernetes-sigs/kueue/blob/fe64c96e32130b15e9e5898ffaa1e145761d49d8/pkg/workload/workload.go#L517).
+
+### Interaction with excludeResourcePrefixes
+
+The interaction between `quotaCheck` and `excludeResourcePrefixes` depends on the value
+of `quotaCheck`:
+
+#### When quotaCheck: All (default)
+
+This maintains **backward compatibility** with current Kueue behavior:
+
+- **With `excludeResourcePrefixes`**: All pod resources are considered for quota EXCEPT
+  those matching any prefix in `excludeResourcePrefixes`.
+- **Without `excludeResourcePrefixes`**: All pod resources are considered for quota.
+
+#### When quotaCheck: OnlyDeclared
+
+The `excludeResourcePrefixes` field is **ignored** and has no effect:
```

  I wouldn't ignore it, because this could be misleading. Let's choose one of the two options:
  1. prohibit setting by validation rules
  2. respect as it is respected for All - basically exludes the resources even if declared
  
  I would be leaning towards (1.)

### Review by [@amy](https://github.com/amy) (commented) — 2026-02-23T22:45:43Z

- **keps/7513-quota-check/README.md:87** by [@amy](https://github.com/amy) — 2026-02-23T22:45:43Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
```

  Want to make sure. `QuotaCheck` is global to this cohort right? 
  
  We're saying "this is an allow list of resources for the cohort that we care about". 
  
  We're not saying "this is an allow list specific to this CQ that we care about"

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-24T05:57:55Z

Thank you for updating the design detail.
I left some comments.

- **keps/7513-quota-check/README.md:147** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-24T03:47:19Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
+
+	// QuotaCheckOnlyDeclared means only resources declared in the ClusterQueue's
+	// coveredResources are checked against quota. Undeclared resources are ignored.
+	QuotaCheckOnlyDeclared QuotaCheckStrategy = "OnlyDeclared"
+)
+
+type Resources struct {
+    // ...
+    // QuotaCheck determines which resources are considered during quota admission.
+    // +kubebuilder:default="All"
+    // +optional
+    QuotaCheck QuotaCheckStrategy `json:"quotaCheck,omitempty"`
```

  Is this mutually exclusive with excludeResources? If yes, can you mention that in the API comment?

- **keps/7513-quota-check/README.md:147** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-24T03:51:37Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
+
+	// QuotaCheckOnlyDeclared means only resources declared in the ClusterQueue's
+	// coveredResources are checked against quota. Undeclared resources are ignored.
+	QuotaCheckOnlyDeclared QuotaCheckStrategy = "OnlyDeclared"
+)
+
+type Resources struct {
+    // ...
+    // QuotaCheck determines which resources are considered during quota admission.
+    // +kubebuilder:default="All"
+    // +optional
+    QuotaCheck QuotaCheckStrategy `json:"quotaCheck,omitempty"`
```

  I'm wondering if we should make this as "QuotaCheckStrategy" because this is not structured parameter for strategy and this is just a single-line parameter field.
  
  
  @mimowo Any thoughts?

- **keps/7513-quota-check/README.md:135** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-24T04:03:48Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
```

  @mimowo Shouldn't we replace this "All" with "AllPodRequests" or "AllPodResources" to explicitly represent that this indicates "Pod".
  
  Because cluster admin PoV, this strategy name is unclear which "All" does this indicate, "Pod" or "Node". Fore sure, once they read all our documentations, they can understand that, but I believe that the API field name should be intuitive.

- **keps/7513-quota-check/README.md:139** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-24T04:07:04Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
+
+	// QuotaCheckOnlyDeclared means only resources declared in the ClusterQueue's
+	// coveredResources are checked against quota. Undeclared resources are ignored.
+	QuotaCheckOnlyDeclared QuotaCheckStrategy = "OnlyDeclared"
```

  @mimowo As the same reason with "All" strategy, should't we explicitly say this as "OnlyDeclaredAtCoveredResources" or "OnlyCoveredResourcesDeclared"?

- **keps/7513-quota-check/README.md:66** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-24T05:57:39Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
```

  Could we have a derepacation-specific section?
  I guess that we will start deprecation after the QuotaCheck beta graduation, then, when we have the next API version, we will remove the `excludeResourcePrefixes`.
  
  @MaysaMacedo @mimowo Any deprecation plan?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-24T06:20:24Z

- **keps/7513-quota-check/README.md:147** by [@mimowo](https://github.com/mimowo) — 2026-02-24T06:20:24Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
+
+	// QuotaCheckOnlyDeclared means only resources declared in the ClusterQueue's
+	// coveredResources are checked against quota. Undeclared resources are ignored.
+	QuotaCheckOnlyDeclared QuotaCheckStrategy = "OnlyDeclared"
+)
+
+type Resources struct {
+    // ...
+    // QuotaCheck determines which resources are considered during quota admission.
+    // +kubebuilder:default="All"
+    // +optional
+    QuotaCheck QuotaCheckStrategy `json:"quotaCheck,omitempty"`
```

  QuotaCheckStrategy sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-24T06:43:40Z

- **keps/7513-quota-check/README.md:135** by [@mimowo](https://github.com/mimowo) — 2026-02-24T06:43:40Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
```

  I was hoping we can acoid that long names by the context field which is 'resources'.
  
  For example, the preexisting excludeResourcePrefixes:
  - does not say "Pod" either, and
  - "Resources" feels repetative as discussed before.
  
  So I like the simple All, we can explain it in the doc just for the field.
  
  When looking for alternatives Im rather thinking about
  - BlockUncoveredResources /BlockUndeclared
  - PermitUndeclaredResources / PermitUndeclared
  
  Because the real difference between these two modes is just handling the extra resources a workload requests if they are not covered by ClusterQueue. wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-24T06:44:13Z

- **keps/7513-quota-check/README.md:139** by [@mimowo](https://github.com/mimowo) — 2026-02-24T06:44:13Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
+
+	// QuotaCheckOnlyDeclared means only resources declared in the ClusterQueue's
+	// coveredResources are checked against quota. Undeclared resources are ignored.
+	QuotaCheckOnlyDeclared QuotaCheckStrategy = "OnlyDeclared"
```

  lets continue in the other umbrella thread https://github.com/kubernetes-sigs/kueue/pull/8396#discussion_r2844875876

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-24T06:46:06Z

- **keps/7513-quota-check/README.md:66** by [@mimowo](https://github.com/mimowo) — 2026-02-24T06:46:06Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
```

  I wouldnt call it a Goal of the KEP, we are not even sure yet we can. we need more time to determine that and user feedback.
  
  
  I would say - lets add it as a point for graduation to GA as  "Re-evaluate deprecation and dropping of the excludeResourcePrefixes"

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-24T15:30:54Z

- **keps/7513-quota-check/README.md:66** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-24T15:30:54Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
```

  > lets add it as a point for graduation to GA
  
  SGTM

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-24T15:32:28Z

- **keps/7513-quota-check/README.md:87** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-24T15:32:28Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
```

  IIUC, this is cluster-wide global configurations. So, once you specify `quotaCheck` here, the parameters will be applied to all Cohorts and ClusterQueues.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-24T15:40:41Z

- **keps/7513-quota-check/README.md:135** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-24T15:40:41Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
```

  > For example, the preexisting excludeResourcePrefixes:
  
  AFAIK, we didn't have TAS (since v0.9) when we introduce `excludeResource` feauture (since v0.7).
  So, we didn't have any reason to distinguish between quota and node because we didn't care Node capacity at all before incoming TAS.
  
  However, as you know, today, Kueue TAS carefully care Node capabilities. And actually, even if Kueue ignores some resources on quota checks, TAS will check all resources. There is a recognition gap from the user PoV, I think.
  
  For sure, as you mentioned, we can describe any semantics in API comments, but it would be better to make an effort for an intuitive API field name.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-24T15:45:48Z

- **keps/7513-quota-check/README.md:135** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-24T15:45:48Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
```

  > BlockUncoveredResources /BlockUndeclared
  PermitUndeclaredResources / PermitUndeclared
  
  I'm also fine with your proposal, which represents what action for quota checking.
  OTOH, if we select those action-based ENUM, we might want to replace QuotaCheckStrategy with QuotaReservationStrategy. Because I think that these actions are beyond the "check" and also represent what reservation happened. And "check" is also used in `AdmissionCheck`, but this field is only for QuotaReserved Workload state. @mimowo WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-24T16:34:08Z

- **keps/7513-quota-check/README.md:135** by [@mimowo](https://github.com/mimowo) — 2026-02-24T16:34:08Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
```

  The way I look at it is Admission is guarded by a sequence of checks in two phases:
  1. quota check & TAS (phase one)
  2. admission checks (phase two)
  
  So, QuotaCheck is just a check corresponding to "phase one", so it reads naturally to me. Maybe QuotaReservationStrategy is a bit more, and could also cover handling of TAS, but I think this is out of scope here.
  
  Regarding the enum value names, I slightly prefer `BlockUndeclared` / `PermitUndeclared` to avoid the redundant resources, but it is only slight preference. We could explain in the documentation text this is about declaration in the ClusterQueue. Alternatively: `BlockUndeclaredInCQ`, `PermitUndeclaredInCQ`, wdyt?

### Review by [@amy](https://github.com/amy) (commented) — 2026-02-24T17:35:58Z

- **keps/7513-quota-check/README.md:87** by [@amy](https://github.com/amy) — 2026-02-24T17:35:58Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
```

  solid. @MaysaMacedo can you confirm? Also if this is the case, can you make it more clear in the KEP / documentation. 🙏

### Review by [@amy](https://github.com/amy) (commented) — 2026-02-24T17:43:08Z

- **keps/7513-quota-check/README.md:171** by [@amy](https://github.com/amy) — 2026-02-24T17:43:09Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
+
+	// QuotaCheckOnlyDeclared means only resources declared in the ClusterQueue's
+	// coveredResources are checked against quota. Undeclared resources are ignored.
+	QuotaCheckOnlyDeclared QuotaCheckStrategy = "OnlyDeclared"
+)
+
+type Resources struct {
+    // ...
+    // QuotaCheck determines which resources are considered during quota admission.
+    // +kubebuilder:default="All"
+    // +optional
+    QuotaCheck QuotaCheckStrategy `json:"quotaCheck,omitempty"`
+}
+```
+
+### Feature Gate
+
+This feature is gated behind the `QuotaCheck` feature gate.
+
+- **Alpha**: The feature gate is disabled by default. Users must explicitly enable
+  it to use `quotaCheck: "OnlyDeclared"`. When the gate is disabled, the `quotaCheck`
+  configuration field is ignored and the behavior is equivalent to `All`.
+- **Beta**: The feature gate is enabled by default.
+- **GA**: The feature gate is locked to enabled and cannot be disabled.
+
+### Implementation Details
+
+The flavor assigner logic will be modified in the [`findFlavorForPodSets`](https://github.com/kubernetes-sigs/kueue/blob/fe64c96e32130b15e9e5898ffaa1e145761d49d8/pkg/scheduler/flavorassigner/flavorassigner.go#L751) method to:
+
+1. If `quotaCheck` is set with value `OnlyDeclared`:
+   - Only resources that match resources defined in the `ClusterQueue` are used for
+     quota check.
+   - Any resources defined in the Pod that are not defined in the `ClusterQueue` will
+     be skipped for flavor admission.
+   - Any resources defined in the `ClusterQueue` that are not in the Pod will be
+     skipped for quota check.
```

  [https://github.com/kubernetes-sigs/kueue/pull/8396/changes#r2843457589](https://github.com/kubernetes-sigs/kueue/pull/8396/changes#r2843457589)
  ^ Revisiting this
  
  Another clarification I have is... if quotaCheck is globally defined, lets say we have this:
  quotaCheck: CPU, Memory, GPU
  cohort-1, CQ-A: CPU, Memory
  cohort-1, CQ-B: GPU
  
  We can assume:
  CQ-A: if we submit a GPU workload, it will be blocked.
  CQ-B: if we submit a CPU/Memory workload, it will be blocked.
  @MaysaMacedo

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-25T02:28:05Z

- **keps/7513-quota-check/README.md:139** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-25T02:28:04Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
+
+	// QuotaCheckOnlyDeclared means only resources declared in the ClusterQueue's
+	// coveredResources are checked against quota. Undeclared resources are ignored.
+	QuotaCheckOnlyDeclared QuotaCheckStrategy = "OnlyDeclared"
```

  Closing this thread since we moved the discussions to https://github.com/kubernetes-sigs/kueue/pull/8396#discussion_r2844875876

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-25T02:45:29Z

- **keps/7513-quota-check-strategy/README.md:236** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-25T02:45:29Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
+
+	// QuotaCheckOnlyDeclared means only resources declared in the ClusterQueue's
+	// coveredResources are checked against quota. Undeclared resources are ignored.
+	QuotaCheckOnlyDeclared QuotaCheckStrategy = "OnlyDeclared"
+)
+
+type Resources struct {
+    // ...
+    // QuotaCheck determines which resources are considered during quota admission.
+    // +kubebuilder:default="All"
+    // +optional
+    QuotaCheck QuotaCheckStrategy `json:"quotaCheck,omitempty"`
+}
+```
+
+### Feature Gate
+
+This feature is gated behind the `QuotaCheck` feature gate.
+
+- **Alpha**: The feature gate is disabled by default. Users must explicitly enable
+  it to use `quotaCheck: "OnlyDeclared"`. When the gate is disabled, the `quotaCheck`
+  configuration field is ignored and the behavior is equivalent to `All`.
+- **Beta**: The feature gate is enabled by default.
+- **GA**: The feature gate is locked to enabled and cannot be disabled.
+
+### Implementation Details
```

  I agree with @mimowo 
  It would be better to remove implementation details (e.g, variable / typed / function names).
  Those implementation details will be outdated soon.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-25T02:49:30Z

- **keps/7513-quota-check/README.md:87** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-25T02:49:30Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
```

  > Also if this is the case, can you make it more clear in the KEP / documentation. 🙏
  
  I think that the lack of context is about which resource we will expand. So, @amy 's confusing happened.
  
  So, my proposal at line 88 - 91 is the following: 
  
  ```yaml
  apiVersion: config.x-k8s.io/v1beta2
  kind: Configuration
  ...
  resources:
    quotaCheck: "OnlyDeclared"
  ...
  ```
  
  This could represent that this is a global-level parameter.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-25T03:05:11Z

- **keps/7513-quota-check/README.md:135** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-25T03:05:11Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
```

  > So, QuotaCheck is just a check corresponding to "phase one", so it reads naturally to me. Maybe QuotaReservationStrategy is a bit more, and could also cover handling of TAS, but I think this is out of scope here.
  
  Good point, that sounds reasonable to me. I agree with QuotaCheckStrategy here. Thank you for pointing that out.
  
  > Regarding the enum value names, I slightly prefer BlockUndeclared / PermitUndeclared to avoid the redundant resources, but it is only slight preference. We could explain in the documentation text this is about declaration in the ClusterQueue. Alternatively: BlockUndeclaredInCQ, PermitUndeclaredInCQ, wdyt?
  
  `BlockUndeclared` sounds good to me, but I'd propose an alternative `IgnoreUndeclared` instead of `PermitUndeclared`. IIUC, even if we specify `PermitUndeclared`, flavor assigner doesn't do `Permit` action, and it just ignores evaluation, right? Let me know when my understanding is not correct.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-25T05:30:06Z

- **keps/7513-quota-check/README.md:135** by [@mimowo](https://github.com/mimowo) — 2026-02-25T05:30:06Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
```

  Yes, IgnoreUndeclared sounds better

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-25T05:45:39Z

- **keps/7513-quota-check/README.md:171** by [@mimowo](https://github.com/mimowo) — 2026-02-25T05:45:39Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
+
+	// QuotaCheckOnlyDeclared means only resources declared in the ClusterQueue's
+	// coveredResources are checked against quota. Undeclared resources are ignored.
+	QuotaCheckOnlyDeclared QuotaCheckStrategy = "OnlyDeclared"
+)
+
+type Resources struct {
+    // ...
+    // QuotaCheck determines which resources are considered during quota admission.
+    // +kubebuilder:default="All"
+    // +optional
+    QuotaCheck QuotaCheckStrategy `json:"quotaCheck,omitempty"`
+}
+```
+
+### Feature Gate
+
+This feature is gated behind the `QuotaCheck` feature gate.
+
+- **Alpha**: The feature gate is disabled by default. Users must explicitly enable
+  it to use `quotaCheck: "OnlyDeclared"`. When the gate is disabled, the `quotaCheck`
+  configuration field is ignored and the behavior is equivalent to `All`.
+- **Beta**: The feature gate is enabled by default.
+- **GA**: The feature gate is locked to enabled and cannot be disabled.
+
+### Implementation Details
+
+The flavor assigner logic will be modified in the [`findFlavorForPodSets`](https://github.com/kubernetes-sigs/kueue/blob/fe64c96e32130b15e9e5898ffaa1e145761d49d8/pkg/scheduler/flavorassigner/flavorassigner.go#L751) method to:
+
+1. If `quotaCheck` is set with value `OnlyDeclared`:
+   - Only resources that match resources defined in the `ClusterQueue` are used for
+     quota check.
+   - Any resources defined in the Pod that are not defined in the `ClusterQueue` will
+     be skipped for flavor admission.
+   - Any resources defined in the `ClusterQueue` that are not in the Pod will be
+     skipped for quota check.
```

  This is a very good question! One clarification, the quotaCheckStrategy is just an enum. The value we were considering to introduce is IgnorrUndeclared to ingore any resources requested by workload which are extra. However, I think in this mode we need to make surr the workload declares all resources listed in thr ClusterQueue flavors. 
  
  This is somethig we recently relaxed  in the default mode to count resources not requedted by workload as 0. Here I think we cannot do it. Otherwise we would let the workloads adkitted.
  
  Still as long as we ensure any workload listed in CQ covered resources is requested by workload it should result in blocking.
  
  I think it will be great to enrich rhe KEP with examples like these.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-25T05:45:55Z

- **keps/7513-quota-check/README.md:171** by [@mimowo](https://github.com/mimowo) — 2026-02-25T05:45:55Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
+
+	// QuotaCheckOnlyDeclared means only resources declared in the ClusterQueue's
+	// coveredResources are checked against quota. Undeclared resources are ignored.
+	QuotaCheckOnlyDeclared QuotaCheckStrategy = "OnlyDeclared"
+)
+
+type Resources struct {
+    // ...
+    // QuotaCheck determines which resources are considered during quota admission.
+    // +kubebuilder:default="All"
+    // +optional
+    QuotaCheck QuotaCheckStrategy `json:"quotaCheck,omitempty"`
+}
+```
+
+### Feature Gate
+
+This feature is gated behind the `QuotaCheck` feature gate.
+
+- **Alpha**: The feature gate is disabled by default. Users must explicitly enable
+  it to use `quotaCheck: "OnlyDeclared"`. When the gate is disabled, the `quotaCheck`
+  configuration field is ignored and the behavior is equivalent to `All`.
+- **Beta**: The feature gate is enabled by default.
+- **GA**: The feature gate is locked to enabled and cannot be disabled.
+
+### Implementation Details
+
+The flavor assigner logic will be modified in the [`findFlavorForPodSets`](https://github.com/kubernetes-sigs/kueue/blob/fe64c96e32130b15e9e5898ffaa1e145761d49d8/pkg/scheduler/flavorassigner/flavorassigner.go#L751) method to:
+
+1. If `quotaCheck` is set with value `OnlyDeclared`:
+   - Only resources that match resources defined in the `ClusterQueue` are used for
+     quota check.
+   - Any resources defined in the Pod that are not defined in the `ClusterQueue` will
+     be skipped for flavor admission.
+   - Any resources defined in the `ClusterQueue` that are not in the Pod will be
+     skipped for quota check.
```

  cc @tenzen-y

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-02-27T19:43:49Z

- **keps/7513-quota-check/README.md:135** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-02-27T19:43:49Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
```

  @mimowo `IgnoreUndeclared` would be a replacement for `OnlyDeclared`, right?
  would `BlockUndeclared` be a replacement for `All`?

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-02-27T19:52:24Z

- **keps/7513-quota-check/README.md:171** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-02-27T19:52:24Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
+
+	// QuotaCheckOnlyDeclared means only resources declared in the ClusterQueue's
+	// coveredResources are checked against quota. Undeclared resources are ignored.
+	QuotaCheckOnlyDeclared QuotaCheckStrategy = "OnlyDeclared"
+)
+
+type Resources struct {
+    // ...
+    // QuotaCheck determines which resources are considered during quota admission.
+    // +kubebuilder:default="All"
+    // +optional
+    QuotaCheck QuotaCheckStrategy `json:"quotaCheck,omitempty"`
+}
+```
+
+### Feature Gate
+
+This feature is gated behind the `QuotaCheck` feature gate.
+
+- **Alpha**: The feature gate is disabled by default. Users must explicitly enable
+  it to use `quotaCheck: "OnlyDeclared"`. When the gate is disabled, the `quotaCheck`
+  configuration field is ignored and the behavior is equivalent to `All`.
+- **Beta**: The feature gate is enabled by default.
+- **GA**: The feature gate is locked to enabled and cannot be disabled.
+
+### Implementation Details
+
+The flavor assigner logic will be modified in the [`findFlavorForPodSets`](https://github.com/kubernetes-sigs/kueue/blob/fe64c96e32130b15e9e5898ffaa1e145761d49d8/pkg/scheduler/flavorassigner/flavorassigner.go#L751) method to:
+
+1. If `quotaCheck` is set with value `OnlyDeclared`:
+   - Only resources that match resources defined in the `ClusterQueue` are used for
+     quota check.
+   - Any resources defined in the Pod that are not defined in the `ClusterQueue` will
+     be skipped for flavor admission.
+   - Any resources defined in the `ClusterQueue` that are not in the Pod will be
+     skipped for quota check.
```

  @amy My idea was the following:
     - Any resources defined in the worload that are not defined in the `ClusterQueue` will
       be **skipped**.
     - Any resources defined in the `ClusterQueue` that are not in the Pod will be
       **skipped**.
  
  So, if we have this:
  quotaCheck: IgnoreUndeclared
  cohort-1, CQ-A: CPU, Memory
  cohort-1, CQ-B: GPU
  
  I was assuming we would get:
  CQ-A: if we submit a GPU workload, it will not be blocked, GPU it's ignored.
  CQ-B: if we submit a CPU/Memory workload, it will not be blocked, CPU/Memory is ignored.
  
  Let me know if you have any concerns with this approach I'm suggesting.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-03-01T06:47:44Z

- **keps/7513-quota-check/README.md:171** by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-01T06:47:44Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
+
+	// QuotaCheckOnlyDeclared means only resources declared in the ClusterQueue's
+	// coveredResources are checked against quota. Undeclared resources are ignored.
+	QuotaCheckOnlyDeclared QuotaCheckStrategy = "OnlyDeclared"
+)
+
+type Resources struct {
+    // ...
+    // QuotaCheck determines which resources are considered during quota admission.
+    // +kubebuilder:default="All"
+    // +optional
+    QuotaCheck QuotaCheckStrategy `json:"quotaCheck,omitempty"`
+}
+```
+
+### Feature Gate
+
+This feature is gated behind the `QuotaCheck` feature gate.
+
+- **Alpha**: The feature gate is disabled by default. Users must explicitly enable
+  it to use `quotaCheck: "OnlyDeclared"`. When the gate is disabled, the `quotaCheck`
+  configuration field is ignored and the behavior is equivalent to `All`.
+- **Beta**: The feature gate is enabled by default.
+- **GA**: The feature gate is locked to enabled and cannot be disabled.
+
+### Implementation Details
+
+The flavor assigner logic will be modified in the [`findFlavorForPodSets`](https://github.com/kubernetes-sigs/kueue/blob/fe64c96e32130b15e9e5898ffaa1e145761d49d8/pkg/scheduler/flavorassigner/flavorassigner.go#L751) method to:
+
+1. If `quotaCheck` is set with value `OnlyDeclared`:
+   - Only resources that match resources defined in the `ClusterQueue` are used for
+     quota check.
+   - Any resources defined in the Pod that are not defined in the `ClusterQueue` will
+     be skipped for flavor admission.
+   - Any resources defined in the `ClusterQueue` that are not in the Pod will be
+     skipped for quota check.
```

  I can understand @amy 's confusion.
  @MaysaMacedo, could you expand the `User Stories` section? The story-2 is for IgnoreUndeclared, and the story-3 is for BlockUndeclared?

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-03-04T19:14:38Z

- **keps/7513-quota-check/README.md:171** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-03-04T19:14:38Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
+
+	// QuotaCheckOnlyDeclared means only resources declared in the ClusterQueue's
+	// coveredResources are checked against quota. Undeclared resources are ignored.
+	QuotaCheckOnlyDeclared QuotaCheckStrategy = "OnlyDeclared"
+)
+
+type Resources struct {
+    // ...
+    // QuotaCheck determines which resources are considered during quota admission.
+    // +kubebuilder:default="All"
+    // +optional
+    QuotaCheck QuotaCheckStrategy `json:"quotaCheck,omitempty"`
+}
+```
+
+### Feature Gate
+
+This feature is gated behind the `QuotaCheck` feature gate.
+
+- **Alpha**: The feature gate is disabled by default. Users must explicitly enable
+  it to use `quotaCheck: "OnlyDeclared"`. When the gate is disabled, the `quotaCheck`
+  configuration field is ignored and the behavior is equivalent to `All`.
+- **Beta**: The feature gate is enabled by default.
+- **GA**: The feature gate is locked to enabled and cannot be disabled.
+
+### Implementation Details
+
+The flavor assigner logic will be modified in the [`findFlavorForPodSets`](https://github.com/kubernetes-sigs/kueue/blob/fe64c96e32130b15e9e5898ffaa1e145761d49d8/pkg/scheduler/flavorassigner/flavorassigner.go#L751) method to:
+
+1. If `quotaCheck` is set with value `OnlyDeclared`:
+   - Only resources that match resources defined in the `ClusterQueue` are used for
+     quota check.
+   - Any resources defined in the Pod that are not defined in the `ClusterQueue` will
+     be skipped for flavor admission.
+   - Any resources defined in the `ClusterQueue` that are not in the Pod will be
+     skipped for quota check.
```

  I updated existent user story and added a new one. Let me know if you think we should go into more details. Note that in the examples, I didn't add cohorts to the `clusterQueue`, I'm assuming that wouldn't have an impact.

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-03-04T19:36:00Z

- **keps/7513-quota-check/README.md:147** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-03-04T19:35:59Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
+
+	// QuotaCheckOnlyDeclared means only resources declared in the ClusterQueue's
+	// coveredResources are checked against quota. Undeclared resources are ignored.
+	QuotaCheckOnlyDeclared QuotaCheckStrategy = "OnlyDeclared"
+)
+
+type Resources struct {
+    // ...
+    // QuotaCheck determines which resources are considered during quota admission.
+    // +kubebuilder:default="All"
+    // +optional
+    QuotaCheck QuotaCheckStrategy `json:"quotaCheck,omitempty"`
```

  I added in the commend of `QuotaCheckIgnoreUndeclared`. I'm assuming we want to still account for excludeResources on `QuotaCheckBlockUndeclared`?

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-03-04T19:37:58Z

- **keps/7513-quota-check/README.md:147** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-03-04T19:37:58Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
+
+	// QuotaCheckOnlyDeclared means only resources declared in the ClusterQueue's
+	// coveredResources are checked against quota. Undeclared resources are ignored.
+	QuotaCheckOnlyDeclared QuotaCheckStrategy = "OnlyDeclared"
+)
+
+type Resources struct {
+    // ...
+    // QuotaCheck determines which resources are considered during quota admission.
+    // +kubebuilder:default="All"
+    // +optional
+    QuotaCheck QuotaCheckStrategy `json:"quotaCheck,omitempty"`
```

  Not sure I got the suggestion proposed here.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-04T19:48:36Z

- **keps/7513-quota-check/README.md:147** by [@mimowo](https://github.com/mimowo) — 2026-03-04T19:48:36Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
+
+	// QuotaCheckOnlyDeclared means only resources declared in the ClusterQueue's
+	// coveredResources are checked against quota. Undeclared resources are ignored.
+	QuotaCheckOnlyDeclared QuotaCheckStrategy = "OnlyDeclared"
+)
+
+type Resources struct {
+    // ...
+    // QuotaCheck determines which resources are considered during quota admission.
+    // +kubebuilder:default="All"
+    // +optional
+    QuotaCheck QuotaCheckStrategy `json:"quotaCheck,omitempty"`
```

  The suggestion iiuc was to just rename the original `QuotaCheck` -> `QuotaCheckStrategy`. I see this is applied to the KEP already.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T09:01:56Z

/lgtm
/approve
Thank you, leaving on hold in case @tenzen-y has additional comments.
/hold

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T13:23:40Z

- **keps/7513-quota-check/README.md:214** by [@mimowo](https://github.com/mimowo) — 2026-03-05T13:23:40Z
```diff
@@ -0,0 +1,342 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Ignore resources not declared in clusterQueue](#story-1-ignore-resources-not-declared-in-clusterqueue)
+    - [Story 2: Block resource not declared in clusterQueue](#story-2-block-resource-not-declared-in-clusterqueue)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: BlockUndeclared (default)](#when-quotacheck-blockundeclared-default)
+    - [When quotaCheck: IgnoreUndeclared](#when-quotacheck-ignoreundeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration API which allows to restrict the
+quota check to only resources which are declared in the `ClusterQueue`. Other resources
+requested by the Pod are skipped from the quota checks.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API. When this field is
+set with `IgnoreUndeclared` value, resources declared in the Workload's PodTemplate that are not declared
+in the `ClusterQueue` are ignored for quota admission decisions. When the field has value `BlockUndeclared`,
+resources declared in the Workload's PodTemplate that are not declared in the `ClusterQueue` are taken into
+account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Ignore resources not declared in clusterQueue
+
+As a training platform operator, I primarily care about managing GPU and Memory quota across teams.
+While jobs request CPU and GPU, I want to focus on tracking and limiting GPU and Memory usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+apiVersion: config.x-k8s.io/v1beta2
+kind: Configuration
+...
+resources:
+  quotaCheck: "IgnoreUndeclared"
+...
+```
+
+Then the `clusterQueue` could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+     - name: "memory"
+       nominalQuota: 36Gi
+```
+
+This allows me to manage only GPU and Memory resources without worrying about other resources that
+might appear in job specifications. For example, the following job declares cpu and nvidia.com/gpu,
+but only nvidia.com/gpu is taken into account for quota as is the only declared in the `ClusterQueue`.
+The additional resource defined in the `ClusterQueue`, which is memory, won't have an impact on the quota
+as is not being requested by the Workload.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+...
+spec:
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        ...
+        resources:
+          requests:
+            cpu: "3"
+            nvidia.com/gpu: "10"
+```
+#### Story 2: Block resource not declared in clusterQueue
+
+I want for every workload resource quota to be managed by Kueue.
+
+With `quotaCheck`, I can configure:
+```yaml
+apiVersion: config.x-k8s.io/v1beta2
+kind: Configuration
+...
+resources:
+  quotaCheck: "BlockUndeclared"
+...
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+The following job declares cpu, memory and nvidia.com/gpu, and all resources are taken into account for
+quota, even the ones that are not declared in the `ClusterQueue`. This means that the workload will be
+suspended, as there is no flavors defined for cpu and memory in the `ClusterQueue`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+...
+spec:
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        ...
+        resources:
+          requests:
+            cpu: "3"
+            memory: "40Gi"
+            nvidia.com/gpu: "10"
+```
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckBlockUndeclared means all resources defined in the workload are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckBlockUndeclared QuotaCheckStrategy = "BlockUndeclared"
+
+	// QuotaCheckIgnoreUndeclared means only resources defined in the workload that are declared in the
+  // ClusterQueue's coveredResources are checked against quota. Resources undeclared in the clusterQueue
+  // are ignored. ExcludeResourcePrefixes is not allowed in combination with this field.
```

  ```suggestion
    // are ignored. ExcludeResourcePrefixes is not allowed in combination with this strategy.
  ```
  nit, or enum value.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T13:25:12Z

- **keps/7513-quota-check/README.md:147** by [@mimowo](https://github.com/mimowo) — 2026-03-05T13:25:12Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
+
+	// QuotaCheckOnlyDeclared means only resources declared in the ClusterQueue's
+	// coveredResources are checked against quota. Undeclared resources are ignored.
+	QuotaCheckOnlyDeclared QuotaCheckStrategy = "OnlyDeclared"
+)
+
+type Resources struct {
+    // ...
+    // QuotaCheck determines which resources are considered during quota admission.
+    // +kubebuilder:default="All"
+    // +optional
+    QuotaCheck QuotaCheckStrategy `json:"quotaCheck,omitempty"`
```

  Oh, I think @tenzen-y wanted to check the field name also as `quotaCheckStrategy`. I'm ok with both options, but it makes sense to align the field name with the enum name. So, I propose `quotaCheckStrategy` for now.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T14:18:13Z

/lgtm
/approve
Leaving on hold in case @tenzen-y has some comments
/hold

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-03-06T18:49:37Z

- **keps/7513-quota-check/README.md:147** by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-06T18:49:37Z
```diff
@@ -0,0 +1,269 @@
+# KEP-7513: Quota check
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Focus on GPU management](#story-1-focus-on-gpu-management)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Feature Gate](#feature-gate)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with excludeResourcePrefixes](#interaction-with-excluderesourceprefixes)
+    - [When quotaCheck: All (default)](#when-quotacheck-all-default)
+    - [When quotaCheck: OnlyDeclared](#when-quotacheck-onlydeclared)
+  - [Test Plan](#test-plan)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Continue using excludeResourcePrefixes](#continue-using-excluderesourceprefixes)
+  - [Add another API includeResourcePrefixes](#add-another-api-includeresourceprefixes)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new configuration field `quotaCheck`
+that acts as a way to control which resources should be managed by Kueue. The `quotaCheck`
+can have the value of `OnlyDeclared`, which means only resources declared in the `ClusterQueue`
+can have an impact in the admission of the Pod. It can also have the value of `All`, which
+means all the resources defined on the Pod requests will have an impact in the admission
+of the workload.
+
+## Motivation
+
+Currently, Kueue counts all resources in pod requests/limits during admission,
+except for those explicitly excluded via `excludeResourcePrefixes`. This works well when the
+set of resources used in a cluster doesn't change much. However, in practice, many Kueue users
+face operational challenges:
+
+1. **Mutating webhooks add unexpected resources**: When a mutating webhook injects
+    resources into pod specifications, these resources appear in pod requests/limits.
+
+2. **Workloads become blocked**: When a new resource isn't ignored in the
+   `excludeResourcePrefixes`, Kueue cannot admit workloads because the `ClusterQueue`
+   doesn't have the quota configured.
+
+3. **Reactive maintenance burden**: Platform operators must add entries to
+   `excludeResourcePrefixes` whenever a new resource appears that is not on `ClusterQueue`.
+
+4. **Most users care about a small set of resources**: In practice, most Kueue deployments
+   are primarily concerned with managing a small, well-known set of resources (CPU, memory,
+   and specific accelerators like GPUs), not all the resources that can appear in pod specs.
+
+The current exclude-based approach assumes users want to manage all resources by default and
+opt-out of specific ones. For many deployments, an include-based approach that assumes users
+want to manage only specific resources would be more appropriate and simpler to operate.
+
+### Goals
+
+- Provide a single configuration option to specify how Kueue should manage the resources for quota purposes
+- Deprecate existing `excludeResourcePrefixes` API configuration
+- Reduce the operational burden for cluster administrators in environments with dynamic
+  resource injection
+
+### Non-Goals
+
+- Modifying workload admission logic beyond resource filtering
+- Impact on TAS calculation for resources placement
+
+## Proposal
+
+Add a new configuration option `resources.quotaCheck` to the Kueue Configuration API.
+When this field is set with `OnlyDeclared` value, only resources declared in the `ClusterQueue` are taken into account for quota admission decisions, any resources not listed in the `ClusterQueue` are ignored. When the field has value `All`, which would be the default, all the resources defined in the Pod are taken into account for quota admission decisions.
+
+### User Stories
+
+#### Story 1: Focus on GPU management
+
+As a training platform operator, I primarily care about managing GPU quota across teams.
+While pods request CPU and memory, I want to focus on tracking and limiting GPU usage.
+
+With `quotaCheck`, I can configure:
+```yaml
+resources:
+  quotaCheck: "OnlyDeclared"
+```
+
+Then the clusterQueue could be configured like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["nvidia.com/gpu"]
+    flavors:
+    - name: "nvidia"
+      resources:
+      - name: "nvidia.com/gpu"
+        nominalQuota: 10
+```
+
+This allows me to manage only GPU resources without worrying about other resources that
+might appear in pod specifications.
+
+### Risks and Mitigations
+
+**Risk**: Users might get confused on what is being managed and what is not.
+
+**Mitigation**:
+- Provide logging with resources that are being skipped.
+
+## Design Details
+
+### API Changes
+
+Add a new type and field to the Kueue `Resources`:
+
+```go
+// QuotaCheckStrategy determines how Kueue checks resources against quota
+// during admission.
+// +kubebuilder:validation:Enum=All;OnlyDeclared
+type QuotaCheckStrategy string
+
+const (
+	// QuotaCheckAll means all resources in Pod requests are checked against quota,
+	// except those matching ExcludeResourcePrefixes.
+	QuotaCheckAll QuotaCheckStrategy = "All"
+
+	// QuotaCheckOnlyDeclared means only resources declared in the ClusterQueue's
+	// coveredResources are checked against quota. Undeclared resources are ignored.
+	QuotaCheckOnlyDeclared QuotaCheckStrategy = "OnlyDeclared"
+)
+
+type Resources struct {
+    // ...
+    // QuotaCheck determines which resources are considered during quota admission.
+    // +kubebuilder:default="All"
+    // +optional
+    QuotaCheck QuotaCheckStrategy `json:"quotaCheck,omitempty"`
```

  > Oh, I think @tenzen-y wanted to check the field name also as quotaCheckStrategy. I'm ok with both options, but it makes sense to align the field name with the enum name. So, I propose quotaCheckStrategy for now.
  
  Yes, I still want to propose the `quotaCheckStrategy` YAML field name to make the field name more intuitive, and the `strategy` suffix is used to select in various our Kueue API field names. So, I believe that the `strategy` suffix matches with our comprehensive API surfaces from user PoV.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-03-06T18:50:32Z

Awesome, KEP looks great, thank you!
/lgtm
/approve
