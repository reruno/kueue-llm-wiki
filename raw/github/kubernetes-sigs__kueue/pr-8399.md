# PR #8399: Add label to e2e tests in singlecluster

**Summary**: Add label to e2e tests in singlecluster

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8399

**Last updated**: 2026-01-12T19:19:07Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@MaysaMacedo](https://github.com/MaysaMacedo)
- **Branch**: `label-e2e-tests` → `main`
- **Created**: 2025-12-23T22:17:15Z
- **Updated**: 2026-01-12T19:19:07Z
- **Merged**: 2026-01-12T19:16:10Z
- **Closed**: 2026-01-12T19:16:10Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@pajakd](https://github.com/pajakd), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 22   **+81 / -45**

## Description

#### What type of PR is this?
/kind cleanup


#### What this PR does / why we need it:

This PR adds filters to the singlecluster e2e tests and also allow to only install the dependencies of each tests cases when are meant to run.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes https://github.com/kubernetes-sigs/kueue/issues/8100


#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-23T22:17:17Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-12-23T22:17:22Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 5a5130e45fcacd4e22c935fdc8af72ce8b6a25cc |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/696534b3503f1e000858d523 |

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-01-08T20:19:54Z

/hold

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-01-09T19:32:02Z

@kannon92 @mbobrovskyi 
I manually tested creating a kind cluster and running each test separately to ensure only the necessary dependencies are installed.
The only one that I had issues running was kuberay, maybe it's missing to set something in my kind cluster, but I saw kuberay was installed. Do you have any thoughts on how could I test just kuberay?

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-10T05:30:14Z

> The only one that I had issues running was kuberay, maybe it's missing to set something in my kind cluster, but I saw kuberay was installed. Do you have any thoughts on how could I test just kuberay?

I tested kuberay, and it looks like it works.

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-01-12T15:27:35Z

/hold cancel

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-12T15:56:26Z

Thank you so much!

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-12T15:56:33Z

LGTM label has been added.  <details>Git tree hash: a08c2e977ea1a7b36c0582caa3cfd40392e84bbf</details>

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-01-12T18:36:01Z

@mbobrovskyi I had to rebase the PR. Can you take another look? Thanks

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-12T18:42:14Z

LGTM label has been added.  <details>Git tree hash: bceed11d4ab69bf0a54353059b4a24c45a97058c</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-12T18:42:36Z

@mimowo need your approve for other files

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-12T18:50:23Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8399#" title="Author self-approved">MaysaMacedo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8399#pullrequestreview-3652191298" title="Approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8399#pullrequestreview-3652222883" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mbobrovskyi,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-24T02:51:32Z

- **test/e2e/singlecluster/e2e_test.go:45** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-24T02:51:32Z
```diff
@@ -42,7 +42,7 @@ import (
 	"sigs.k8s.io/kueue/test/util"
 )
 
-var _ = ginkgo.Describe("Kueue", func() {
+var _ = ginkgo.Describe("Kueue", ginkgo.Label("area:singlecluster", "feature:e2e"), func() {
```

  ```suggestion
  var _ = ginkgo.Describe("Kueue", ginkgo.Label("area:singlecluster", "feature:job"), func() {
  ```
  
  Maybe job?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-24T02:52:31Z

- **test/e2e/singlecluster/e2e_test.go:45** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-24T02:52:31Z
```diff
@@ -42,7 +42,7 @@ import (
 	"sigs.k8s.io/kueue/test/util"
 )
 
-var _ = ginkgo.Describe("Kueue", func() {
+var _ = ginkgo.Describe("Kueue", ginkgo.Label("area:singlecluster", "feature:e2e"), func() {
```

  Probably we need to rename this file.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-07T07:39:43Z

- **test/e2e/singlecluster/suite_test.go:84** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-07T07:39:43Z
```diff
@@ -69,14 +68,5 @@ var _ = ginkgo.BeforeSuite(func() {
 	gomega.Expect(err).NotTo(gomega.HaveOccurred())
 	ctx = ginkgo.GinkgoT().Context()
 
-	waitForAvailableStart := time.Now()
 	util.WaitForKueueAvailability(ctx, k8sClient)
-	util.WaitForJobSetAvailability(ctx, k8sClient)
-	util.WaitForLeaderWorkerSetAvailability(ctx, k8sClient)
-	util.WaitForAppWrapperAvailability(ctx, k8sClient)
-	util.WaitForKubeFlowTrainingOperatorAvailability(ctx, k8sClient)
-	ginkgo.GinkgoLogr.Info(
-		"Kueue and all required operators are available in the cluster",
-		"waitingTime", time.Since(waitForAvailableStart),
-	)
```

  Why this removed?

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-01-07T12:43:59Z

- **test/e2e/singlecluster/suite_test.go:84** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-01-07T12:43:59Z
```diff
@@ -69,14 +68,5 @@ var _ = ginkgo.BeforeSuite(func() {
 	gomega.Expect(err).NotTo(gomega.HaveOccurred())
 	ctx = ginkgo.GinkgoT().Context()
 
-	waitForAvailableStart := time.Now()
 	util.WaitForKueueAvailability(ctx, k8sClient)
-	util.WaitForJobSetAvailability(ctx, k8sClient)
-	util.WaitForLeaderWorkerSetAvailability(ctx, k8sClient)
-	util.WaitForAppWrapperAvailability(ctx, k8sClient)
-	util.WaitForKubeFlowTrainingOperatorAvailability(ctx, k8sClient)
-	ginkgo.GinkgoLogr.Info(
-		"Kueue and all required operators are available in the cluster",
-		"waitingTime", time.Since(waitForAvailableStart),
-	)
```

  @mbobrovskyi I moved each check to the respective scenario that needs it, for example [here](https://github.com/kubernetes-sigs/kueue/pull/8399/files#diff-10736532f9ae0b8f03a2a91dbb61866d01da2bd442426e073fa50fe035b892c0R52-R55).
  This helps when we need to run only jobset tests, for example, and we can avoid installing dependencies that are not needed for jobset. Only the jobset operator and Kueue are needed.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-07T12:55:14Z

- **test/e2e/singlecluster/suite_test.go:84** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-07T12:55:13Z
```diff
@@ -69,14 +68,5 @@ var _ = ginkgo.BeforeSuite(func() {
 	gomega.Expect(err).NotTo(gomega.HaveOccurred())
 	ctx = ginkgo.GinkgoT().Context()
 
-	waitForAvailableStart := time.Now()
 	util.WaitForKueueAvailability(ctx, k8sClient)
-	util.WaitForJobSetAvailability(ctx, k8sClient)
-	util.WaitForLeaderWorkerSetAvailability(ctx, k8sClient)
-	util.WaitForAppWrapperAvailability(ctx, k8sClient)
-	util.WaitForKubeFlowTrainingOperatorAvailability(ctx, k8sClient)
-	ginkgo.GinkgoLogr.Info(
-		"Kueue and all required operators are available in the cluster",
-		"waitingTime", time.Since(waitForAvailableStart),
-	)
```

  Ah, I see. Good idea. 👍

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-07T14:35:55Z

- **test/e2e/singlecluster/tas_test.go:48** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-07T14:35:55Z
```diff
@@ -45,7 +45,7 @@ import (
 	"sigs.k8s.io/kueue/test/util"
 )
 
-var _ = ginkgo.Describe("TopologyAwareScheduling", func() {
+var _ = ginkgo.Describe("TopologyAwareScheduling", ginkgo.Label("area:singlecluster", "feature:tas"), func() {
```

  Here we also using JobSet and TrainJob.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-07T14:39:44Z

- **site/content/en/docs/contribution_guidelines/testing.md:170** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-07T14:39:44Z
```diff
@@ -163,6 +163,21 @@ INTEGRATION_FILTERS="--label-filter=feature:tas" make test-integration
 INTEGRATION_FILTERS="--label-filter=feature:fairsharing" make test-integration
 ```
 
+### Use label filters for e2e singlecluster tests
+SingleCluster tests are labeled by feature and area. You can use `GINKGO_ARGS` with `--label-filter` to run specific tests:
+
+**Label Taxonomy:**
+- Features: `appwrapper, certs, deployment, job, fairsharing, jaxjob,jobset,kuberay,kueuectl,leaderworkerset,metrics,pod,pytorchjob,statefulset,tas,trainjob,visibility,e2e_v1beta1`
```

  ```suggestion
  - Features: `appwrapper,certs,deployment,job,fairsharing,jaxjob,jobset,kuberay,kueuectl,leaderworkerset,metrics,pod,pytorchjob,statefulset,tas,trainjob,visibility,e2e_v1beta1`
  ```
  nit: just for consistency

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-07T14:40:46Z

- **hack/e2e-common.sh:31** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-07T14:40:46Z
```diff
@@ -23,18 +23,18 @@ export KUEUE_NAMESPACE="${KUEUE_NAMESPACE:-kueue-system}"
 
 export KIND_VERSION="${E2E_KIND_VERSION/"kindest/node:v"/}"
 
-if [[ -n ${APPWRAPPER_VERSION:-} ]]; then
+if [[ -n ${APPWRAPPER_VERSION:-} && ("$GINKGO_ARGS" =~ "feature:appwrapper" || ! "$GINKGO_ARGS" =~ "--label-filter") ]]; then
     export APPWRAPPER_MANIFEST=${ROOT_DIR}/dep-crds/appwrapper/config/default
-    APPWRAPPER_IMAGE=quay.io/ibm/appwrapper:${APPWRAPPER_VERSION}
+    export APPWRAPPER_IMAGE=quay.io/ibm/appwrapper:${APPWRAPPER_VERSION}
 fi
 
-if [[ -n ${JOBSET_VERSION:-} ]]; then
+if [[ -n ${JOBSET_VERSION:-} && ("$GINKGO_ARGS" =~ "feature:jobset" || ! "$GINKGO_ARGS" =~ "--label-filter") ]]; then
```

  Probably we should also add for `feature:tas`.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-07T16:33:59Z

- **test/e2e/singlecluster/jobset_test.go:35** by [@kannon92](https://github.com/kannon92) — 2026-01-07T16:33:59Z
```diff
@@ -32,9 +32,13 @@ import (
 	"sigs.k8s.io/kueue/test/util"
 )
 
-var _ = ginkgo.Describe("JobSet", func() {
+var _ = ginkgo.Describe("JobSet", ginkgo.Label("area:singlecluster", "feature:jobset"), ginkgo.Ordered, func() {
```

  Why did you add ginkgo.Ordered?

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-01-08T20:29:34Z

- **test/e2e/singlecluster/tas_test.go:48** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-01-08T20:29:33Z
```diff
@@ -45,7 +45,7 @@ import (
 	"sigs.k8s.io/kueue/test/util"
 )
 
-var _ = ginkgo.Describe("TopologyAwareScheduling", func() {
+var _ = ginkgo.Describe("TopologyAwareScheduling", ginkgo.Label("area:singlecluster", "feature:tas"), func() {
```

  ack, I will update it here as well.

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-01-08T20:31:08Z

- **test/e2e/singlecluster/jobset_test.go:35** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-01-08T20:31:08Z
```diff
@@ -32,9 +32,13 @@ import (
 	"sigs.k8s.io/kueue/test/util"
 )
 
-var _ = ginkgo.Describe("JobSet", func() {
+var _ = ginkgo.Describe("JobSet", ginkgo.Label("area:singlecluster", "feature:jobset"), ginkgo.Ordered, func() {
```

  It was to be able to use BeforeAll, but I changed my approach to avoid doing that.
  the Ordered would make all the rest of the tests to be skipped if the first from the list fail.

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-01-09T18:12:30Z

- **test/e2e/singlecluster/tas_test.go:48** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-01-09T18:12:30Z
```diff
@@ -45,7 +45,7 @@ import (
 	"sigs.k8s.io/kueue/test/util"
 )
 
-var _ = ginkgo.Describe("TopologyAwareScheduling", func() {
+var _ = ginkgo.Describe("TopologyAwareScheduling", ginkgo.Label("area:singlecluster", "feature:tas"), func() {
```

  As the main feature here being tested is tas, do you think it's necessary to add the other 2?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-09T21:42:37Z

- **test/e2e/singlecluster/suite_test.go:85** by [@kannon92](https://github.com/kannon92) — 2026-01-09T21:42:37Z
```diff
@@ -71,12 +72,35 @@ var _ = ginkgo.BeforeSuite(func() {
 
 	waitForAvailableStart := time.Now()
 	util.WaitForKueueAvailability(ctx, k8sClient)
-	util.WaitForJobSetAvailability(ctx, k8sClient)
-	util.WaitForLeaderWorkerSetAvailability(ctx, k8sClient)
-	util.WaitForAppWrapperAvailability(ctx, k8sClient)
-	util.WaitForKubeFlowTrainingOperatorAvailability(ctx, k8sClient)
+	labelFilter := ginkgo.GinkgoLabelFilter()
+	if validateLabelFilter(labelFilter, []string{"feature:jobset", "feature:tas", "feature:trainjob"}) {
+		util.WaitForJobSetAvailability(ctx, k8sClient)
+	}
+	if validateLabelFilter(labelFilter, []string{"feature:leaderworkerset"}) {
+		util.WaitForLeaderWorkerSetAvailability(ctx, k8sClient)
+	}
+	if validateLabelFilter(labelFilter, []string{"feature:appwrapper"}) {
+		util.WaitForAppWrapperAvailability(ctx, k8sClient)
+	}
+	if validateLabelFilter(labelFilter, []string{"feature:jaxjob", "feature:pytorchjob"}) {
```

  This isn't quite correct

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-09T21:43:43Z

- **test/e2e/singlecluster/suite_test.go:85** by [@kannon92](https://github.com/kannon92) — 2026-01-09T21:43:43Z
```diff
@@ -71,12 +72,35 @@ var _ = ginkgo.BeforeSuite(func() {
 
 	waitForAvailableStart := time.Now()
 	util.WaitForKueueAvailability(ctx, k8sClient)
-	util.WaitForJobSetAvailability(ctx, k8sClient)
-	util.WaitForLeaderWorkerSetAvailability(ctx, k8sClient)
-	util.WaitForAppWrapperAvailability(ctx, k8sClient)
-	util.WaitForKubeFlowTrainingOperatorAvailability(ctx, k8sClient)
+	labelFilter := ginkgo.GinkgoLabelFilter()
+	if validateLabelFilter(labelFilter, []string{"feature:jobset", "feature:tas", "feature:trainjob"}) {
+		util.WaitForJobSetAvailability(ctx, k8sClient)
+	}
+	if validateLabelFilter(labelFilter, []string{"feature:leaderworkerset"}) {
+		util.WaitForLeaderWorkerSetAvailability(ctx, k8sClient)
+	}
+	if validateLabelFilter(labelFilter, []string{"feature:appwrapper"}) {
+		util.WaitForAppWrapperAvailability(ctx, k8sClient)
+	}
+	if validateLabelFilter(labelFilter, []string{"feature:jaxjob", "feature:pytorchjob"}) {
```

  It does look like these are the only two jobs we are testing though.
  
  cc @tenzen-y

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-10T04:52:38Z

- **test/e2e/singlecluster/tas_test.go:48** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-10T04:52:38Z
```diff
@@ -45,7 +45,7 @@ import (
 	"sigs.k8s.io/kueue/test/util"
 )
 
-var _ = ginkgo.Describe("TopologyAwareScheduling", func() {
+var _ = ginkgo.Describe("TopologyAwareScheduling", ginkgo.Label("area:singlecluster", "feature:tas"), func() {
```

  No, I mean that we need to WaitForJobSetAvailability and WaitForKubeFlowTrainingOperatorAvailability here as well.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-10T05:36:11Z

- **test/e2e/singlecluster/suite_test.go:87** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-10T05:36:11Z
```diff
@@ -71,12 +72,35 @@ var _ = ginkgo.BeforeSuite(func() {
 
 	waitForAvailableStart := time.Now()
 	util.WaitForKueueAvailability(ctx, k8sClient)
-	util.WaitForJobSetAvailability(ctx, k8sClient)
-	util.WaitForLeaderWorkerSetAvailability(ctx, k8sClient)
-	util.WaitForAppWrapperAvailability(ctx, k8sClient)
-	util.WaitForKubeFlowTrainingOperatorAvailability(ctx, k8sClient)
+	labelFilter := ginkgo.GinkgoLabelFilter()
+	if validateLabelFilter(labelFilter, []string{"feature:jobset", "feature:tas", "feature:trainjob"}) {
+		util.WaitForJobSetAvailability(ctx, k8sClient)
+	}
+	if validateLabelFilter(labelFilter, []string{"feature:leaderworkerset"}) {
+		util.WaitForLeaderWorkerSetAvailability(ctx, k8sClient)
+	}
+	if validateLabelFilter(labelFilter, []string{"feature:appwrapper"}) {
+		util.WaitForAppWrapperAvailability(ctx, k8sClient)
+	}
+	if validateLabelFilter(labelFilter, []string{"feature:jaxjob", "feature:pytorchjob"}) {
+		util.WaitForKubeFlowTrainingOperatorAvailability(ctx, k8sClient)
+	}
```

  ```suggestion
  	if validateLabelFilter(labelFilter, []string{"feature:jaxjob", "feature:pytorchjob"}) {
  		util.WaitForKubeFlowTrainingOperatorAvailability(ctx, k8sClient)
  	}
  	if validateLabelFilter(labelFilter, []string{"feature:kuberay"}) {
  		util.WaitForKubeRayOperatorAvailability(ctx, k8sClient)
  	}
  ```
  But I think we missed this.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-10T05:43:27Z

- **test/e2e/singlecluster/suite_test.go:76** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-10T05:43:27Z
```diff
@@ -71,12 +72,35 @@ var _ = ginkgo.BeforeSuite(func() {
 
 	waitForAvailableStart := time.Now()
 	util.WaitForKueueAvailability(ctx, k8sClient)
-	util.WaitForJobSetAvailability(ctx, k8sClient)
-	util.WaitForLeaderWorkerSetAvailability(ctx, k8sClient)
-	util.WaitForAppWrapperAvailability(ctx, k8sClient)
-	util.WaitForKubeFlowTrainingOperatorAvailability(ctx, k8sClient)
+	labelFilter := ginkgo.GinkgoLabelFilter()
+	if validateLabelFilter(labelFilter, []string{"feature:jobset", "feature:tas", "feature:trainjob"}) {
```

  ```suggestion
  	if  ginkgo.Label("feature:jobset", "feature:tas", "feature:trainjob").MatchesLabelFilter(labelFilter) {
  ```
  I'm wondering if we can go on this way. WDYT?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-10T05:44:28Z

- **test/e2e/singlecluster/tas_test.go:48** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-10T05:44:28Z
```diff
@@ -45,7 +45,7 @@ import (
 	"sigs.k8s.io/kueue/test/util"
 )
 
-var _ = ginkgo.Describe("TopologyAwareScheduling", func() {
+var _ = ginkgo.Describe("TopologyAwareScheduling", ginkgo.Label("area:singlecluster", "feature:tas"), func() {
```

  But since you’ve already added this, I think we can resolve it.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-10T05:45:22Z

Overall LGTM.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-01-10T06:23:11Z

- **test/e2e/singlecluster/suite_test.go:85** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-10T06:23:11Z
```diff
@@ -71,12 +72,35 @@ var _ = ginkgo.BeforeSuite(func() {
 
 	waitForAvailableStart := time.Now()
 	util.WaitForKueueAvailability(ctx, k8sClient)
-	util.WaitForJobSetAvailability(ctx, k8sClient)
-	util.WaitForLeaderWorkerSetAvailability(ctx, k8sClient)
-	util.WaitForAppWrapperAvailability(ctx, k8sClient)
-	util.WaitForKubeFlowTrainingOperatorAvailability(ctx, k8sClient)
+	labelFilter := ginkgo.GinkgoLabelFilter()
+	if validateLabelFilter(labelFilter, []string{"feature:jobset", "feature:tas", "feature:trainjob"}) {
+		util.WaitForJobSetAvailability(ctx, k8sClient)
+	}
+	if validateLabelFilter(labelFilter, []string{"feature:leaderworkerset"}) {
+		util.WaitForLeaderWorkerSetAvailability(ctx, k8sClient)
+	}
+	if validateLabelFilter(labelFilter, []string{"feature:appwrapper"}) {
+		util.WaitForAppWrapperAvailability(ctx, k8sClient)
+	}
+	if validateLabelFilter(labelFilter, []string{"feature:jaxjob", "feature:pytorchjob"}) {
```

  This looks expected one since KF job controllers are commonized as https://github.com/kubernetes-sigs/kueue/blob/971af82167bd5abecad9962c23684b190f47ae58/pkg/controller/jobs/kubeflow/kubeflowjob/kubeflowjob_controller.go#L36-L38

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-10T06:25:51Z

- **test/e2e/singlecluster/suite_test.go:87** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-10T06:25:51Z
```diff
@@ -71,12 +72,35 @@ var _ = ginkgo.BeforeSuite(func() {
 
 	waitForAvailableStart := time.Now()
 	util.WaitForKueueAvailability(ctx, k8sClient)
-	util.WaitForJobSetAvailability(ctx, k8sClient)
-	util.WaitForLeaderWorkerSetAvailability(ctx, k8sClient)
-	util.WaitForAppWrapperAvailability(ctx, k8sClient)
-	util.WaitForKubeFlowTrainingOperatorAvailability(ctx, k8sClient)
+	labelFilter := ginkgo.GinkgoLabelFilter()
+	if validateLabelFilter(labelFilter, []string{"feature:jobset", "feature:tas", "feature:trainjob"}) {
+		util.WaitForJobSetAvailability(ctx, k8sClient)
+	}
+	if validateLabelFilter(labelFilter, []string{"feature:leaderworkerset"}) {
+		util.WaitForLeaderWorkerSetAvailability(ctx, k8sClient)
+	}
+	if validateLabelFilter(labelFilter, []string{"feature:appwrapper"}) {
+		util.WaitForAppWrapperAvailability(ctx, k8sClient)
+	}
+	if validateLabelFilter(labelFilter, []string{"feature:jaxjob", "feature:pytorchjob"}) {
+		util.WaitForKubeFlowTrainingOperatorAvailability(ctx, k8sClient)
+	}
```

  I created prepare PR https://github.com/kubernetes-sigs/kueue/pull/8517.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-01-10T06:27:59Z

Basically, LGTM

- **hack/e2e-common.sh:56** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-10T06:27:04Z
  Oh, I'm surprised that MPIJob E2E tests do not exist.
  The MPIJob is executed by the mpi-operator separate from trainer v1 and trainer v2.
  
  So, we should implement that.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-01-10T06:29:36Z

- **hack/e2e-common.sh:56** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-10T06:29:36Z
  This is out of scope in this PR.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-10T06:39:50Z

- **hack/e2e-common.sh:56** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-10T06:39:50Z
  Created an [issue](https://github.com/kubernetes-sigs/kueue/issues/8518).

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-01-10T06:40:26Z

- **hack/e2e-common.sh:56** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-10T06:40:26Z
  Thank you!

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-01-12T14:23:24Z

- **test/e2e/singlecluster/suite_test.go:76** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-01-12T14:23:24Z
```diff
@@ -71,12 +72,35 @@ var _ = ginkgo.BeforeSuite(func() {
 
 	waitForAvailableStart := time.Now()
 	util.WaitForKueueAvailability(ctx, k8sClient)
-	util.WaitForJobSetAvailability(ctx, k8sClient)
-	util.WaitForLeaderWorkerSetAvailability(ctx, k8sClient)
-	util.WaitForAppWrapperAvailability(ctx, k8sClient)
-	util.WaitForKubeFlowTrainingOperatorAvailability(ctx, k8sClient)
+	labelFilter := ginkgo.GinkgoLabelFilter()
+	if validateLabelFilter(labelFilter, []string{"feature:jobset", "feature:tas", "feature:trainjob"}) {
```

  That is a good suggestion. However, we also we need to check if no "feature" is defined in the label filter, because this would mean that all the dependencies should be installed and all the tests run. So, I think we could keep the way it's now, having a separate function that handles all the necessary checks. WDYT?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-12T14:42:09Z

- **test/e2e/singlecluster/suite_test.go:76** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-12T14:42:09Z
```diff
@@ -71,12 +72,35 @@ var _ = ginkgo.BeforeSuite(func() {
 
 	waitForAvailableStart := time.Now()
 	util.WaitForKueueAvailability(ctx, k8sClient)
-	util.WaitForJobSetAvailability(ctx, k8sClient)
-	util.WaitForLeaderWorkerSetAvailability(ctx, k8sClient)
-	util.WaitForAppWrapperAvailability(ctx, k8sClient)
-	util.WaitForKubeFlowTrainingOperatorAvailability(ctx, k8sClient)
+	labelFilter := ginkgo.GinkgoLabelFilter()
+	if validateLabelFilter(labelFilter, []string{"feature:jobset", "feature:tas", "feature:trainjob"}) {
```

  I think this (no "feature") should work. I tested it locally and it worked for me. Could you please double-check?

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-01-12T14:52:56Z

- **test/e2e/singlecluster/suite_test.go:76** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-01-12T14:52:56Z
```diff
@@ -71,12 +72,35 @@ var _ = ginkgo.BeforeSuite(func() {
 
 	waitForAvailableStart := time.Now()
 	util.WaitForKueueAvailability(ctx, k8sClient)
-	util.WaitForJobSetAvailability(ctx, k8sClient)
-	util.WaitForLeaderWorkerSetAvailability(ctx, k8sClient)
-	util.WaitForAppWrapperAvailability(ctx, k8sClient)
-	util.WaitForKubeFlowTrainingOperatorAvailability(ctx, k8sClient)
+	labelFilter := ginkgo.GinkgoLabelFilter()
+	if validateLabelFilter(labelFilter, []string{"feature:jobset", "feature:tas", "feature:trainjob"}) {
```

  let me try it

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-01-12T14:54:53Z

- **test/e2e/singlecluster/tas_test.go:48** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-01-12T14:54:52Z
```diff
@@ -45,7 +45,7 @@ import (
 	"sigs.k8s.io/kueue/test/util"
 )
 
-var _ = ginkgo.Describe("TopologyAwareScheduling", func() {
+var _ = ginkgo.Describe("TopologyAwareScheduling", ginkgo.Label("area:singlecluster", "feature:tas"), func() {
```

  FYI, for tas we needed to wait for the kubeflow trainner controller manager instead of the training operator, so I added this check https://github.com/kubernetes-sigs/kueue/pull/8399/changes/d3747cacf63ff078637189be8b9b01c3b18b5c6e#diff-c735c4faf5e1601d0e6ce7c0c4ea6ecbe6900d853b716e44862c90093db6a058R91-R92

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-01-12T15:26:46Z

- **test/e2e/singlecluster/suite_test.go:76** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-01-12T15:26:46Z
```diff
@@ -71,12 +72,35 @@ var _ = ginkgo.BeforeSuite(func() {
 
 	waitForAvailableStart := time.Now()
 	util.WaitForKueueAvailability(ctx, k8sClient)
-	util.WaitForJobSetAvailability(ctx, k8sClient)
-	util.WaitForLeaderWorkerSetAvailability(ctx, k8sClient)
-	util.WaitForAppWrapperAvailability(ctx, k8sClient)
-	util.WaitForKubeFlowTrainingOperatorAvailability(ctx, k8sClient)
+	labelFilter := ginkgo.GinkgoLabelFilter()
+	if validateLabelFilter(labelFilter, []string{"feature:jobset", "feature:tas", "feature:trainjob"}) {
```

  It worked, thanks for the recommendation

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-12T15:32:36Z

- **test/util/e2e.go:353** by [@mimowo](https://github.com/mimowo) — 2026-01-12T15:32:36Z
```diff
@@ -350,6 +350,12 @@ func WaitForKubeRayOperatorAvailability(ctx context.Context, k8sClient client.Cl
 	verifyNoControllerRestarts(ctx, k8sClient, kroKey)
 }
 
+func WaitForKubeFlowTrainnerControllerManagerAvailability(ctx context.Context, k8sClient client.Client) {
```

  This looks like we missed introducing when introduced Kubeflow trainer support. This is very unlikely, but possible to flake if the Trainer is not yet ready. This is unlikely because typically Kueue and other controllers will give it enough time. Still, I would prefer to extract it to a separate PR and cherrypick to avoid any flakes here.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-12T15:33:09Z

LGTM, just left a comment to potentially extract the fix for flakes to separate PR

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-01-12T15:53:03Z

- **test/util/e2e.go:353** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-01-12T15:53:03Z
```diff
@@ -350,6 +350,12 @@ func WaitForKubeRayOperatorAvailability(ctx context.Context, k8sClient client.Cl
 	verifyNoControllerRestarts(ctx, k8sClient, kroKey)
 }
 
+func WaitForKubeFlowTrainnerControllerManagerAvailability(ctx context.Context, k8sClient client.Client) {
```

  https://github.com/kubernetes-sigs/kueue/pull/8547

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (approved) — 2026-01-12T18:42:07Z

Thank you!

/lgtm
/approve

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-01-12T18:50:13Z

Thank you 👍 
/approve
