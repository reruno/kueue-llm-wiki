# PR #8402: Automated cherry pick of #8151: MultiKueue: fix eviction initiated by the manager cluster

**Summary**: Automated cherry pick of #8151: MultiKueue: fix eviction initiated by the manager cluster

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8402

**Last updated**: 2025-12-24T16:47:39Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `automated-cherry-pick-of-#8151-upstream-release-0.15` → `release-0.15`
- **Created**: 2025-12-24T02:22:25Z
- **Updated**: 2025-12-24T16:47:39Z
- **Merged**: 2025-12-24T16:46:31Z
- **Closed**: 2025-12-24T16:46:31Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 6   **+1021 / -12**

## Description

Cherry pick of #8151 on release-0.15.

#8151: MultiKueue: fix eviction initiated by the manager cluster

For details on the cherry pick process, see the [cherry pick requests](https://git.k8s.io/community/contributors/devel/sig-release/cherry-picks.md) page.

```release-note
MultiKueue: fix the eviction when initiated by the manager cluster (due to eg. Preemption or WairForPodsReady timeout).
```

## Discussion

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-24T02:22:38Z

/kind bug

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-24T02:22:53Z

/cc @mimowo @tenzen-y

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-12-24T02:23:19Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | ceee8fa5ad4b27ea59101d49686bd9058222aa6e |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/694b5451c23e000007ce25fa |

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-12-24T16:09:57Z

This is a cherry-pick PR. So, I will merge this PR.
@mbobrovskyi Please address the above comments, thanks.

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-24T16:10:04Z

LGTM label has been added.  <details>Git tree hash: 1e2c0e5829426b6733be6b4dd59f6cde7b9d8c70</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-24T16:10:06Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8402#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8402#issuecomment-3690171975" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/release-0.15/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-12-24T16:08:11Z

- **pkg/controller/admissionchecks/multikueue/workload.go:356** by [@tenzen-y](https://github.com/tenzen-y) — 2025-12-24T16:03:04Z
```diff
@@ -345,7 +351,26 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 2. delete all workloads that are out of sync (other than scaled-down elastic workloads)
+	// 4. Handle workload evicted on manager cluster
+	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
+	if remoteEvictCond != nil && strings.Contains(remoteEvictCond.Reason, WorkloadEvictedOnManagerCluster) {
```

  ```suggestion
  	if remoteEvictCond != nil && remoteEvictCond.Reason == WorkloadEvictedOnManagerCluster {
  ```
  
  Why not exactly match? IIUC, we set only WorkloadEvictedOnManagerCluster as a reason in line 414 - 420.

- **pkg/controller/admissionchecks/multikueue/workload.go:58** by [@tenzen-y](https://github.com/tenzen-y) — 2025-12-24T16:07:10Z
```diff
@@ -53,6 +54,10 @@ import (
 	"sigs.k8s.io/kueue/pkg/workloadslicing"
 )
 
+const (
+	WorkloadEvictedOnManagerCluster = "EvictedOnManagerCluster"
```

  Could you move this reason to API: https://github.com/kubernetes-sigs/kueue/blob/5233f1b3204f293383f347e643bb113611fbccc3/apis/kueue/v1beta2/workload_types.go#L905?
  
  This is user-facing, and this should be managed as part of the API.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-24T16:21:03Z

- **pkg/controller/admissionchecks/multikueue/workload.go:356** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-24T16:21:02Z
```diff
@@ -345,7 +351,26 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 2. delete all workloads that are out of sync (other than scaled-down elastic workloads)
+	// 4. Handle workload evicted on manager cluster
+	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
+	if remoteEvictCond != nil && strings.Contains(remoteEvictCond.Reason, WorkloadEvictedOnManagerCluster) {
```

  Not exactly. After deactivation we have the reason of eviction like this `DeactivatedDueToEvictedOnManagerCluster`.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-24T16:25:56Z

- **pkg/controller/admissionchecks/multikueue/workload.go:58** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-24T16:25:56Z
```diff
@@ -53,6 +54,10 @@ import (
 	"sigs.k8s.io/kueue/pkg/workloadslicing"
 )
 
+const (
+	WorkloadEvictedOnManagerCluster = "EvictedOnManagerCluster"
```

  Done in #8417

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-12-24T16:27:08Z

- **pkg/controller/admissionchecks/multikueue/workload.go:356** by [@tenzen-y](https://github.com/tenzen-y) — 2025-12-24T16:27:08Z
```diff
@@ -345,7 +351,26 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 2. delete all workloads that are out of sync (other than scaled-down elastic workloads)
+	// 4. Handle workload evicted on manager cluster
+	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
+	if remoteEvictCond != nil && strings.Contains(remoteEvictCond.Reason, WorkloadEvictedOnManagerCluster) {
```

  So, does that mean we can not catch the temporary DeactivationTarget condition?
  So, we will check the Eviction condition instead of DeactivationTarget?
  
  In that case, couldn't we check eviction + DeactivatedDueToEvictedOnManagerCluster reason?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-24T16:33:40Z

- **pkg/controller/admissionchecks/multikueue/workload.go:356** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-24T16:33:40Z
```diff
@@ -345,7 +351,26 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 2. delete all workloads that are out of sync (other than scaled-down elastic workloads)
+	// 4. Handle workload evicted on manager cluster
+	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
+	if remoteEvictCond != nil && strings.Contains(remoteEvictCond.Reason, WorkloadEvictedOnManagerCluster) {
```

  > So, we will check the Eviction condition instead of DeactivationTarget?
  
  Unfortunately, we can't because we are removing it https://github.com/kubernetes-sigs/kueue/blob/8f7d4d945b88c733d21f5ec39777ff66e1f47ef7/pkg/controller/core/workload_controller.go#L388-L390 after eviction.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-24T16:35:52Z

- **pkg/controller/admissionchecks/multikueue/workload.go:356** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-24T16:35:51Z
```diff
@@ -345,7 +351,26 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 2. delete all workloads that are out of sync (other than scaled-down elastic workloads)
+	// 4. Handle workload evicted on manager cluster
+	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
+	if remoteEvictCond != nil && strings.Contains(remoteEvictCond.Reason, WorkloadEvictedOnManagerCluster) {
```

  > In that case, couldn't we check eviction + DeactivatedDueToEvictedOnManagerCluster reason?
  
  You mean we should check the full eviction reason `DeactivatedDueToEvictedOnManagerCluster` by `==` instead of using strings.Contains(), right?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-12-24T16:36:30Z

- **pkg/controller/admissionchecks/multikueue/workload.go:356** by [@tenzen-y](https://github.com/tenzen-y) — 2025-12-24T16:36:30Z
```diff
@@ -345,7 +351,26 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 2. delete all workloads that are out of sync (other than scaled-down elastic workloads)
+	// 4. Handle workload evicted on manager cluster
+	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
+	if remoteEvictCond != nil && strings.Contains(remoteEvictCond.Reason, WorkloadEvictedOnManagerCluster) {
```

  I meant why not `if remoteEvictCond != nil && remoteEvictCond.Reason == DeactivatedDueToEvictedOnManagerCluster {`?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-12-24T16:37:18Z

- **pkg/controller/admissionchecks/multikueue/workload.go:356** by [@tenzen-y](https://github.com/tenzen-y) — 2025-12-24T16:37:18Z
```diff
@@ -345,7 +351,26 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 2. delete all workloads that are out of sync (other than scaled-down elastic workloads)
+	// 4. Handle workload evicted on manager cluster
+	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
+	if remoteEvictCond != nil && strings.Contains(remoteEvictCond.Reason, WorkloadEvictedOnManagerCluster) {
```

  > In that case, couldn't we check eviction + DeactivatedDueToEvictedOnManagerCluster reason?
  > 
  > You mean we should check the full eviction reason DeactivatedDueToEvictedOnManagerCluster by == instead of using strings.Contains(), right?
  
  yes, exactly. why not? Any reason?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-24T16:38:15Z

- **pkg/controller/admissionchecks/multikueue/workload.go:356** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-24T16:38:15Z
```diff
@@ -345,7 +351,26 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 2. delete all workloads that are out of sync (other than scaled-down elastic workloads)
+	// 4. Handle workload evicted on manager cluster
+	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
+	if remoteEvictCond != nil && strings.Contains(remoteEvictCond.Reason, WorkloadEvictedOnManagerCluster) {
```

  We can. I just thought that checking with strings.Contains() is much easier.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-12-24T16:43:35Z

- **pkg/controller/admissionchecks/multikueue/workload.go:356** by [@tenzen-y](https://github.com/tenzen-y) — 2025-12-24T16:43:35Z
```diff
@@ -345,7 +351,26 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 2. delete all workloads that are out of sync (other than scaled-down elastic workloads)
+	// 4. Handle workload evicted on manager cluster
+	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
+	if remoteEvictCond != nil && strings.Contains(remoteEvictCond.Reason, WorkloadEvictedOnManagerCluster) {
```

  Yes, it is easier, but a lack of strict behavior guarantees. 
  So, I would propose an exact match.
  
  We can open follow-up PR separate from this one. Could you address that?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-12-24T16:47:02Z

- **pkg/controller/admissionchecks/multikueue/workload.go:356** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-24T16:47:02Z
```diff
@@ -345,7 +351,26 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 2. delete all workloads that are out of sync (other than scaled-down elastic workloads)
+	// 4. Handle workload evicted on manager cluster
+	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
+	if remoteEvictCond != nil && strings.Contains(remoteEvictCond.Reason, WorkloadEvictedOnManagerCluster) {
```

  Yes, surely.
  
  #8418
