# PR #8421: Add E2E tests for DRA

**Summary**: Add E2E tests for DRA

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8421

**Last updated**: 2026-01-09T15:17:21Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@sohankunkerkar](https://github.com/sohankunkerkar)
- **Branch**: `dra-e2e` → `main`
- **Created**: 2025-12-25T00:02:35Z
- **Updated**: 2026-01-09T15:17:21Z
- **Merged**: 2026-01-09T14:23:43Z
- **Closed**: 2026-01-09T14:23:43Z
- **Labels**: `lgtm`, `release-note-none`, `size/XL`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: _none_
- **Changed files**: 9   **+587 / -3**

## Description

This change adds end-to-end tests for the DRA feature, validating that Kueue correctly manages workloads using DRA.

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:
Part of https://github.com/kubernetes-sigs/kueue/issues/8243 and we need this before pushing the Kueue + DRA feature to Beta.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
None
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-12-25T00:02:42Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 4bff381d059f15f09c8ffcda3032c9bad2e6c4f5 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69605de451179000082fc811 |

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-08T16:47:17Z

/lgtm
/approve
Thank you 👍

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-08T16:47:26Z

LGTM label has been added.  <details>Git tree hash: 216035182a1ddefba2f6ff85c9c6ce443bd10afe</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-08T17:37:09Z

/hold
as https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8421/pull-kueue-test-e2e-main-1-34/2009311567799128064 looks relatwd. Lest make sure the tests are deflaked first

### Comment by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-08T20:06:56Z

> /hold as https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8421/pull-kueue-test-e2e-main-1-34/2009311567799128064 looks relatwd. Lest make sure the tests are deflaked first

Actually the tests run with `ParallelTotal: 2`, and both processes were racing to create cluster-scoped resources (dra-flavor, dra-cq) with fixed names, causing "already exists" errors when they started simultaneously. I fixed that part in the recent push.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-09T13:38:20Z

/unhold
Thanks 👍

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-09T13:39:48Z

LGTM label has been added.  <details>Git tree hash: fc32e23b245fca6154450905bfc1d4b1e726f3ad</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-09T13:39:52Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8421#pullrequestreview-3643973949" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8421#" title="Author self-approved">sohankunkerkar</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@copilot-pull-request-reviewer[bot]](https://github.com/apps/copilot-pull-request-reviewer) (commented) — 2025-12-25T00:12:43Z

## Pull request overview

This PR adds comprehensive end-to-end tests for the Dynamic Resource Allocation (DRA) feature in Kueue, validating that workloads using DRA resources are correctly managed. The changes include test infrastructure setup, DRA driver installation scripts, multiple test scenarios covering quota management, and helper functions for creating jobs with DRA resource claims.

### Key Changes
- Adds E2E test suite for DRA with 5 test scenarios covering admission, quota enforcement, and multi-pod jobs
- Implements infrastructure to build and deploy the DRA example driver from source in the test environment
- Adds configuration for enabling DRA feature gates in Kind clusters and Kueue controllers

### Reviewed changes

Copilot reviewed 9 out of 9 changed files in this pull request and generated 1 comment.

<details>
<summary>Show a summary per file</summary>

| File | Description |
| ---- | ----------- |
| test/e2e/dra/suite_test.go | Test suite setup with DRA driver availability checks |
| test/e2e/dra/dra_test.go | Five comprehensive test scenarios validating DRA resource management |
| test/e2e/config/dra/kustomization.yaml | Kustomization configuration for DRA E2E environment |
| test/e2e/config/dra/controller_manager_config.yaml | Controller configuration enabling DRA feature gate and device class mappings |
| test/e2e/config/dra/manager_e2e_patch.yaml | Image pull policy patch for E2E testing |
| pkg/util/testingjobs/job/wrappers.go | Helper function to add DRA resource claim templates to test jobs |
| hack/e2e-common.sh | Installation and configuration functions for DRA example driver |
| Makefile-test.mk | Make target for running DRA E2E tests |
| Makefile-deps.mk | DRA example driver version configuration |
</details>






---

💡 <a href="/kubernetes-sigs/kueue/new/main/.github/instructions?filename=*.instructions.md" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Add Copilot custom instructions</a> for smarter, more guided reviews. <a href="https://docs.github.com/en/copilot/customizing-copilot/adding-repository-custom-instructions-for-github-copilot" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Learn how to get started</a>.

- **hack/e2e-common.sh:451** by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer) — 2025-12-25T00:12:42Z
```diff
@@ -387,6 +431,44 @@ function install_multicluster {
     kubectl apply --kubeconfig="$1" --server-side -f "${CLUSTERPROFILE_CRD}"
 }
 
+# $1 cluster name
+# $2 kubeconfig option
+function install_dra_example_driver {
+    # Build from source because released versions don't support the stable resource.k8s.io/v1 API.
+    local dra_driver_temp_dir
+    dra_driver_temp_dir=$(mktemp -d)
+    trap "rm -rf \"$dra_driver_temp_dir\"" RETURN
+    git clone --depth 1 --branch main "${DRA_EXAMPLE_DRIVER_REPO}" "$dra_driver_temp_dir"
+
+    local image_repo="dra-example-driver"
+    local image_tag="local"
+
+    local go_version
+    go_version=$(grep '^go ' "$dra_driver_temp_dir/go.mod" | awk '{print $2}' | cut -d. -f1,2)
+
+    echo "Building dra-example-driver image from source (Go ${go_version})..."
+    # Patch Makefile to ensure static build with CGO_ENABLED=0
+    sed -i 's/CGO_LDFLAGS_ALLOW/CGO_ENABLED=0 CGO_LDFLAGS_ALLOW/' "$dra_driver_temp_dir/Makefile"
```

  The `sed -i` command has portability issues between Linux and macOS. On macOS (BSD sed), this requires an explicit backup extension argument. Consider using a portable approach like creating a temporary file or using the pattern from lines 548-549 where sed is used with pipes instead of in-place editing.
  ```suggestion
      local makefile_tmp="${dra_driver_temp_dir}/Makefile.tmp"
      sed 's/CGO_LDFLAGS_ALLOW/CGO_ENABLED=0 CGO_LDFLAGS_ALLOW/' "$dra_driver_temp_dir/Makefile" > "${makefile_tmp}"
      mv "${makefile_tmp}" "$dra_driver_temp_dir/Makefile"
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-06T08:05:14Z

- **Makefile-test.mk:164** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-06T08:05:14Z
```diff
@@ -152,6 +152,9 @@ test-e2e-upgrade: setup-e2e-env run-test-e2e-upgrade-$(E2E_KIND_VERSION:kindest/
 .PHONY: test-e2e-certmanager-upgrade
 test-e2e-certmanager-upgrade: setup-e2e-env run-test-e2e-certmanager-upgrade-$(E2E_KIND_VERSION:kindest/node:v%=%)
 
+.PHONY: test-e2e-dra
```

  It’s not running currently, right? Can you add this as part of the test-e2e target, just for testing before we add it to test-infra?

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-06T18:44:29Z

- **Makefile-test.mk:164** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-06T18:44:28Z
```diff
@@ -152,6 +152,9 @@ test-e2e-upgrade: setup-e2e-env run-test-e2e-upgrade-$(E2E_KIND_VERSION:kindest/
 .PHONY: test-e2e-certmanager-upgrade
 test-e2e-certmanager-upgrade: setup-e2e-env run-test-e2e-certmanager-upgrade-$(E2E_KIND_VERSION:kindest/node:v%=%)
 
+.PHONY: test-e2e-dra
```

  Yeah, that's a good idea. I made that change.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-07T08:19:16Z

- **hack/e2e-common.sh:443** by [@mimowo](https://github.com/mimowo) — 2026-01-07T08:19:16Z
```diff
@@ -387,6 +432,46 @@ function install_multicluster {
     kubectl apply --kubeconfig="$1" --server-side -f "${CLUSTERPROFILE_CRD}"
 }
 
+# $1 cluster name
+# $2 kubeconfig option
+function install_dra_example_driver {
+    # Build from source because released versions don't support the stable resource.k8s.io/v1 API.
+    local dra_driver_temp_dir
+    dra_driver_temp_dir=$(mktemp -d)
+    # shellcheck disable=SC2064 # Intentionally expand now to capture the temp dir path
+    trap "rm -rf '$dra_driver_temp_dir'" RETURN
+    git clone --depth 1 --branch main "${DRA_EXAMPLE_DRIVER_REPO}" "$dra_driver_temp_dir"
```

  This is using the main branch so it can potentially break our builds, also releases, if there is a bug upstream. I think it would be better to somehow depend on the released version, and ideally if this gets managed by dependabot. 
  
  cc @sohankunkerkar @mbobrovskyi is there a way to achieve it? 
  
  I'm ok temporarily to use "main" branch, but I would like to see a proper path forward.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-07T08:19:49Z

- **Makefile-test.mk:128** by [@mimowo](https://github.com/mimowo) — 2026-01-07T08:19:49Z
```diff
@@ -114,9 +114,18 @@ test-multikueue-integration: compile-crd-manifests gomod-download envtest ginkgo
 
 CREATE_KIND_CLUSTER ?= true
 
+SINGLECLUSTER-E2E_TARGETS := $(addprefix run-test-e2e-singlecluster-,$(E2E_KIND_VERSION:kindest/node:v%=%))
+
+# DRA e2e tests require k8s 1.34+
+DRA_MIN_K8S_VERSION := 1.34
+ifeq ($(shell printf '%s\n' "$(E2E_K8S_VERSION)" "$(DRA_MIN_K8S_VERSION)" | sort -V | head -n1),$(DRA_MIN_K8S_VERSION))
+DRA-E2E_TARGETS := $(addprefix run-test-e2e-dra-,$(E2E_KIND_VERSION:kindest/node:v%=%))
+else
+DRA-E2E_TARGETS :=
+endif
 
 .PHONY: test-e2e
-test-e2e: setup-e2e-env kueuectl kind-ray-project-mini-image-build run-test-e2e-singlecluster-$(E2E_KIND_VERSION:kindest/node:v%=%)
+test-e2e: setup-e2e-env kueuectl kind-ray-project-mini-image-build $(SINGLECLUSTER-E2E_TARGETS) $(DRA-E2E_TARGETS)
```

  IIUC this new target is going to be called by a dedicated job in a follow up PR when this gets merged?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-07T08:26:56Z

- **Makefile-test.mk:128** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-07T08:26:56Z
```diff
@@ -114,9 +114,18 @@ test-multikueue-integration: compile-crd-manifests gomod-download envtest ginkgo
 
 CREATE_KIND_CLUSTER ?= true
 
+SINGLECLUSTER-E2E_TARGETS := $(addprefix run-test-e2e-singlecluster-,$(E2E_KIND_VERSION:kindest/node:v%=%))
+
+# DRA e2e tests require k8s 1.34+
+DRA_MIN_K8S_VERSION := 1.34
+ifeq ($(shell printf '%s\n' "$(E2E_K8S_VERSION)" "$(DRA_MIN_K8S_VERSION)" | sort -V | head -n1),$(DRA_MIN_K8S_VERSION))
+DRA-E2E_TARGETS := $(addprefix run-test-e2e-dra-,$(E2E_KIND_VERSION:kindest/node:v%=%))
+else
+DRA-E2E_TARGETS :=
+endif
 
 .PHONY: test-e2e
-test-e2e: setup-e2e-env kueuectl kind-ray-project-mini-image-build run-test-e2e-singlecluster-$(E2E_KIND_VERSION:kindest/node:v%=%)
+test-e2e: setup-e2e-env kueuectl kind-ray-project-mini-image-build $(SINGLECLUSTER-E2E_TARGETS) $(DRA-E2E_TARGETS)
```

  Oh, this isn’t exactly what I meant. We shouldn’t disable the current tests. I think you could do it like this:
  
  ```suggestion
  test-e2e: test-e2e-dra setup-e2e-env kueuectl kind-ray-project-mini-image-build run-test-e2e-singlecluster-$(E2E_KIND_VERSION:kindest/node:v%=%)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-07T08:32:46Z

- **hack/e2e-common.sh:443** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-07T08:32:46Z
```diff
@@ -387,6 +432,46 @@ function install_multicluster {
     kubectl apply --kubeconfig="$1" --server-side -f "${CLUSTERPROFILE_CRD}"
 }
 
+# $1 cluster name
+# $2 kubeconfig option
+function install_dra_example_driver {
+    # Build from source because released versions don't support the stable resource.k8s.io/v1 API.
+    local dra_driver_temp_dir
+    dra_driver_temp_dir=$(mktemp -d)
+    # shellcheck disable=SC2064 # Intentionally expand now to capture the temp dir path
+    trap "rm -rf '$dra_driver_temp_dir'" RETURN
+    git clone --depth 1 --branch main "${DRA_EXAMPLE_DRIVER_REPO}" "$dra_driver_temp_dir"
```

  > cc @sohankunkerkar @mbobrovskyi is there a way to achieve it?
  
  I think so. The repository https://github.com/kubernetes-sigs/dra-example-driver.git
   is in Go, so we could probably add it as a dependency in hack/internal/tools. That way, we can bump it with each release.
  
  > I'm ok temporarily to use "main" branch, but I would like to see a proper path forward.
  
  👍

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-07T14:17:02Z

- **hack/e2e-common.sh:443** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-07T14:17:02Z
```diff
@@ -387,6 +432,46 @@ function install_multicluster {
     kubectl apply --kubeconfig="$1" --server-side -f "${CLUSTERPROFILE_CRD}"
 }
 
+# $1 cluster name
+# $2 kubeconfig option
+function install_dra_example_driver {
+    # Build from source because released versions don't support the stable resource.k8s.io/v1 API.
+    local dra_driver_temp_dir
+    dra_driver_temp_dir=$(mktemp -d)
+    # shellcheck disable=SC2064 # Intentionally expand now to capture the temp dir path
+    trap "rm -rf '$dra_driver_temp_dir'" RETURN
+    git clone --depth 1 --branch main "${DRA_EXAMPLE_DRIVER_REPO}" "$dra_driver_temp_dir"
```

  >This is using the main branch so it can potentially break our builds, also releases, if there is a bug upstream. I think it would be better to somehow depend on the released version, and ideally if this gets managed by dependabot.
  
  I can create an issue in https://github.com/kubernetes-sigs/dra-example-driver.git to ask for the new release.
  I've not see any activity for the last 3 months so I'm not sure if the repo is actively managed.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-07T14:17:15Z

- **Makefile-test.mk:128** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-07T14:17:15Z
```diff
@@ -114,9 +114,18 @@ test-multikueue-integration: compile-crd-manifests gomod-download envtest ginkgo
 
 CREATE_KIND_CLUSTER ?= true
 
+SINGLECLUSTER-E2E_TARGETS := $(addprefix run-test-e2e-singlecluster-,$(E2E_KIND_VERSION:kindest/node:v%=%))
+
+# DRA e2e tests require k8s 1.34+
+DRA_MIN_K8S_VERSION := 1.34
+ifeq ($(shell printf '%s\n' "$(E2E_K8S_VERSION)" "$(DRA_MIN_K8S_VERSION)" | sort -V | head -n1),$(DRA_MIN_K8S_VERSION))
+DRA-E2E_TARGETS := $(addprefix run-test-e2e-dra-,$(E2E_KIND_VERSION:kindest/node:v%=%))
+else
+DRA-E2E_TARGETS :=
+endif
 
 .PHONY: test-e2e
-test-e2e: setup-e2e-env kueuectl kind-ray-project-mini-image-build run-test-e2e-singlecluster-$(E2E_KIND_VERSION:kindest/node:v%=%)
+test-e2e: setup-e2e-env kueuectl kind-ray-project-mini-image-build $(SINGLECLUSTER-E2E_TARGETS) $(DRA-E2E_TARGETS)
```

  That's right.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-07T14:19:10Z

- **Makefile-test.mk:128** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-07T14:19:10Z
```diff
@@ -114,9 +114,18 @@ test-multikueue-integration: compile-crd-manifests gomod-download envtest ginkgo
 
 CREATE_KIND_CLUSTER ?= true
 
+SINGLECLUSTER-E2E_TARGETS := $(addprefix run-test-e2e-singlecluster-,$(E2E_KIND_VERSION:kindest/node:v%=%))
+
+# DRA e2e tests require k8s 1.34+
+DRA_MIN_K8S_VERSION := 1.34
+ifeq ($(shell printf '%s\n' "$(E2E_K8S_VERSION)" "$(DRA_MIN_K8S_VERSION)" | sort -V | head -n1),$(DRA_MIN_K8S_VERSION))
+DRA-E2E_TARGETS := $(addprefix run-test-e2e-dra-,$(E2E_KIND_VERSION:kindest/node:v%=%))
+else
+DRA-E2E_TARGETS :=
+endif
 
 .PHONY: test-e2e
-test-e2e: setup-e2e-env kueuectl kind-ray-project-mini-image-build run-test-e2e-singlecluster-$(E2E_KIND_VERSION:kindest/node:v%=%)
+test-e2e: setup-e2e-env kueuectl kind-ray-project-mini-image-build $(SINGLECLUSTER-E2E_TARGETS) $(DRA-E2E_TARGETS)
```

  Actually, the code does the same thing what you described above. I have done the similar thing for the cert-manager workflow as well. I can't use `test-e2e-dra` here bc it doesn't work for the 1.32/1.33 jobs.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-07T14:20:54Z

- **Makefile-test.mk:128** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-07T14:20:54Z
```diff
@@ -114,9 +114,18 @@ test-multikueue-integration: compile-crd-manifests gomod-download envtest ginkgo
 
 CREATE_KIND_CLUSTER ?= true
 
+SINGLECLUSTER-E2E_TARGETS := $(addprefix run-test-e2e-singlecluster-,$(E2E_KIND_VERSION:kindest/node:v%=%))
+
+# DRA e2e tests require k8s 1.34+
+DRA_MIN_K8S_VERSION := 1.34
+ifeq ($(shell printf '%s\n' "$(E2E_K8S_VERSION)" "$(DRA_MIN_K8S_VERSION)" | sort -V | head -n1),$(DRA_MIN_K8S_VERSION))
+DRA-E2E_TARGETS := $(addprefix run-test-e2e-dra-,$(E2E_KIND_VERSION:kindest/node:v%=%))
+else
+DRA-E2E_TARGETS :=
+endif
 
 .PHONY: test-e2e
-test-e2e: setup-e2e-env kueuectl kind-ray-project-mini-image-build run-test-e2e-singlecluster-$(E2E_KIND_VERSION:kindest/node:v%=%)
+test-e2e: setup-e2e-env kueuectl kind-ray-project-mini-image-build $(SINGLECLUSTER-E2E_TARGETS) $(DRA-E2E_TARGETS)
```

  Ah, I see. Thank you for explanation.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-07T14:38:16Z

- **hack/e2e-common.sh:443** by [@mimowo](https://github.com/mimowo) — 2026-01-07T14:38:16Z
```diff
@@ -387,6 +432,46 @@ function install_multicluster {
     kubectl apply --kubeconfig="$1" --server-side -f "${CLUSTERPROFILE_CRD}"
 }
 
+# $1 cluster name
+# $2 kubeconfig option
+function install_dra_example_driver {
+    # Build from source because released versions don't support the stable resource.k8s.io/v1 API.
+    local dra_driver_temp_dir
+    dra_driver_temp_dir=$(mktemp -d)
+    # shellcheck disable=SC2064 # Intentionally expand now to capture the temp dir path
+    trap "rm -rf '$dra_driver_temp_dir'" RETURN
+    git clone --depth 1 --branch main "${DRA_EXAMPLE_DRIVER_REPO}" "$dra_driver_temp_dir"
```

  I see, please do then. It would be better to test released version.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-07T15:18:01Z

- **hack/e2e-common.sh:443** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-07T15:18:01Z
```diff
@@ -387,6 +432,46 @@ function install_multicluster {
     kubectl apply --kubeconfig="$1" --server-side -f "${CLUSTERPROFILE_CRD}"
 }
 
+# $1 cluster name
+# $2 kubeconfig option
+function install_dra_example_driver {
+    # Build from source because released versions don't support the stable resource.k8s.io/v1 API.
+    local dra_driver_temp_dir
+    dra_driver_temp_dir=$(mktemp -d)
+    # shellcheck disable=SC2064 # Intentionally expand now to capture the temp dir path
+    trap "rm -rf '$dra_driver_temp_dir'" RETURN
+    git clone --depth 1 --branch main "${DRA_EXAMPLE_DRIVER_REPO}" "$dra_driver_temp_dir"
```

  I think we can switch to the release version when we get there. Are we going to block this PR until then?

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-07T16:22:28Z

- **hack/e2e-common.sh:443** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-07T16:22:28Z
```diff
@@ -387,6 +432,46 @@ function install_multicluster {
     kubectl apply --kubeconfig="$1" --server-side -f "${CLUSTERPROFILE_CRD}"
 }
 
+# $1 cluster name
+# $2 kubeconfig option
+function install_dra_example_driver {
+    # Build from source because released versions don't support the stable resource.k8s.io/v1 API.
+    local dra_driver_temp_dir
+    dra_driver_temp_dir=$(mktemp -d)
+    # shellcheck disable=SC2064 # Intentionally expand now to capture the temp dir path
+    trap "rm -rf '$dra_driver_temp_dir'" RETURN
+    git clone --depth 1 --branch main "${DRA_EXAMPLE_DRIVER_REPO}" "$dra_driver_temp_dir"
```

  https://github.com/kubernetes-sigs/dra-example-driver/issues/135

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-08T16:46:12Z

- **hack/e2e-common.sh:443** by [@mimowo](https://github.com/mimowo) — 2026-01-08T16:46:12Z
```diff
@@ -387,6 +432,46 @@ function install_multicluster {
     kubectl apply --kubeconfig="$1" --server-side -f "${CLUSTERPROFILE_CRD}"
 }
 
+# $1 cluster name
+# $2 kubeconfig option
+function install_dra_example_driver {
+    # Build from source because released versions don't support the stable resource.k8s.io/v1 API.
+    local dra_driver_temp_dir
+    dra_driver_temp_dir=$(mktemp -d)
+    # shellcheck disable=SC2064 # Intentionally expand now to capture the temp dir path
+    trap "rm -rf '$dra_driver_temp_dir'" RETURN
+    git clone --depth 1 --branch main "${DRA_EXAMPLE_DRIVER_REPO}" "$dra_driver_temp_dir"
```

  > I think we can switch to the release version when we get there. Are we going to block this PR until then?
  
  Thank you, no need to block on that since there is already consensus, and it should be done in coming days. 
  
  Let's merge as is so that we can in parallel:
  1. start running the new tests in another CI job
  2. update to released version when that is ready

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-09T13:39:23Z

- **Makefile-deps.mk:44** by [@mimowo](https://github.com/mimowo) — 2026-01-09T13:39:24Z
```diff
@@ -41,6 +41,7 @@ APPWRAPPER_VERSION = $(shell $(GO_CMD) list -m -f "{{.Version}}" github.com/proj
 LEADERWORKERSET_VERSION = $(shell $(GO_CMD) list -m -f "{{.Version}}" sigs.k8s.io/lws)
 CERTMANAGER_VERSION=$(shell $(GO_CMD) list -m -f "{{.Version}}" github.com/cert-manager/cert-manager)
 CLUSTERPROFILE_VERSION=$(shell $(GO_CMD) list -m -f "{{.Version}}" sigs.k8s.io/cluster-inventory-api)
+DRA_EXAMPLE_DRIVER_VERSION ?= v0.2.1
```

  As a follow up it would be better to read the version from go.mod as we do for other dependencies. 
  
  This way dependabot can maintain them

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-09T13:39:41Z

/lgtm
/approve
Thank you 👍

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-09T15:13:24Z

- **Makefile-deps.mk:44** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-09T15:13:24Z
```diff
@@ -41,6 +41,7 @@ APPWRAPPER_VERSION = $(shell $(GO_CMD) list -m -f "{{.Version}}" github.com/proj
 LEADERWORKERSET_VERSION = $(shell $(GO_CMD) list -m -f "{{.Version}}" sigs.k8s.io/lws)
 CERTMANAGER_VERSION=$(shell $(GO_CMD) list -m -f "{{.Version}}" github.com/cert-manager/cert-manager)
 CLUSTERPROFILE_VERSION=$(shell $(GO_CMD) list -m -f "{{.Version}}" sigs.k8s.io/cluster-inventory-api)
+DRA_EXAMPLE_DRIVER_VERSION ?= v0.2.1
```

  @mimowo Unfortunately `dra-example-driver` can't be tracked via go.mod like other dependencies. It's built as an external binary, not imported as a Go library, so go mod tidy removes it. We could add a blank import to pin it, but that might bring in unnecessary transitive dependencies. For now, keeping the hardcoded version with a comment to update it manually. Open to other suggestions!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-01-09T15:15:35Z

- **Makefile-deps.mk:44** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-09T15:15:35Z
```diff
@@ -41,6 +41,7 @@ APPWRAPPER_VERSION = $(shell $(GO_CMD) list -m -f "{{.Version}}" github.com/proj
 LEADERWORKERSET_VERSION = $(shell $(GO_CMD) list -m -f "{{.Version}}" sigs.k8s.io/lws)
 CERTMANAGER_VERSION=$(shell $(GO_CMD) list -m -f "{{.Version}}" github.com/cert-manager/cert-manager)
 CLUSTERPROFILE_VERSION=$(shell $(GO_CMD) list -m -f "{{.Version}}" sigs.k8s.io/cluster-inventory-api)
+DRA_EXAMPLE_DRIVER_VERSION ?= v0.2.1
```

  @sohankunkerkar Could we use https://github.com/kubernetes-sigs/kueue/blob/b7fdcd4e636c5cc15dd85289e75efa66f60a70a9/hack/internal/tools/pinversion.go?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-09T15:17:21Z

- **Makefile-deps.mk:44** by [@mimowo](https://github.com/mimowo) — 2026-01-09T15:17:20Z
```diff
@@ -41,6 +41,7 @@ APPWRAPPER_VERSION = $(shell $(GO_CMD) list -m -f "{{.Version}}" github.com/proj
 LEADERWORKERSET_VERSION = $(shell $(GO_CMD) list -m -f "{{.Version}}" sigs.k8s.io/lws)
 CERTMANAGER_VERSION=$(shell $(GO_CMD) list -m -f "{{.Version}}" github.com/cert-manager/cert-manager)
 CLUSTERPROFILE_VERSION=$(shell $(GO_CMD) list -m -f "{{.Version}}" sigs.k8s.io/cluster-inventory-api)
+DRA_EXAMPLE_DRIVER_VERSION ?= v0.2.1
```

  Yes, we use pingversion approach to keep the "unused" dependencies. 
  
  I'm not concerned about the extra dependencies because this is just for the testing modules, not the main binary of Kueue.
