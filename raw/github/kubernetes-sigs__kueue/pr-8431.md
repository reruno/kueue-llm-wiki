# PR #8431: Fix second-pass queue cleanup when workload no longer needs retry

**Summary**: Fix second-pass queue cleanup when workload no longer needs retry

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8431

**Last updated**: 2026-01-20T07:47:19Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@skools-here](https://github.com/skools-here)
- **Branch**: `fix/remove-second-pass-when-not-needed` → `main`
- **Created**: 2025-12-30T09:37:31Z
- **Updated**: 2026-01-20T07:47:19Z
- **Merged**: 2026-01-15T17:43:37Z
- **Closed**: 2026-01-15T17:43:37Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `approved`, `size/M`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92)
- **Changed files**: 2   **+40 / -15**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want faster PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug

#### What this PR does / why we need it:
Fixes cleanup of the second-pass scheduling queue by ensuring workloads are removed once they no longer require a retry, while preserving the initial backoff behavior.

#### Which issue(s) this PR fixes:
Fixes #8357

#### Special notes for your reviewer:
Cleanup is gated on retry iteration to avoid prematurely canceling the initial backoff window.

#### Does this PR introduce a user-facing change?

```release-note
TAS: Fixed an issue where workloads could remain in the second-pass scheduling queue (used for integration
or TAS with ProvisioningRequests, and for TAS Node Hot Swap) even if they no longer require to be in the queue.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-12-30T09:37:37Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 3cce5721c2c9cf049334670f2e1e40ccc9a718fa |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/696901d76976960008c43ad7 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-30T09:37:40Z

Welcome @skools-here! <br><br>It looks like this is your first PR to <a href='https://github.com/kubernetes-sigs/kueue'>kubernetes-sigs/kueue</a> 🎉. Please refer to our [pull request process documentation](https://www.kubernetes.dev/docs/guide/pull-requests/) to help your PR have a smooth ride to approval. <br><br>You will be prompted by a bot to use commands during the review process. Do not be afraid to follow the prompts! It is okay to experiment. [Here is the bot commands documentation](https://go.k8s.io/bot-commands). <br><br>You can also check if kubernetes-sigs/kueue has [its own contribution guidelines](https://github.com/kubernetes-sigs/kueue/tree/master/CONTRIBUTING.md). <br><br>You may want to refer to our [testing guide](https://git.k8s.io/community/contributors/devel/sig-testing/testing.md) if you run into trouble with your tests not passing. <br><br>If you are having difficulty getting your pull request seen, please follow the [recommended escalation practices](https://www.kubernetes.dev/docs/guide/pull-requests/#why-is-my-pull-request-not-getting-reviewed). Also, for tips and tricks in the contribution process you may want to read the [Kubernetes contributor cheat sheet](https://www.kubernetes.dev/docs/contributor-cheatsheet/). We want to make sure your contribution gets all the attention it needs! <br><br>Thank you, and welcome to Kubernetes. :smiley:

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-30T09:37:41Z

Hi @skools-here. Thanks for your PR.

I'm waiting for a [github.com](https://kubernetes-sigs/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-30T09:44:37Z

/ok-to-test

### Comment by [@skools-here](https://github.com/skools-here) — 2025-12-30T10:05:31Z

/retest-required

### Comment by [@gabesaba](https://github.com/gabesaba) — 2026-01-08T00:18:46Z

And please update the release-note

### Comment by [@skools-here](https://github.com/skools-here) — 2026-01-08T13:29:43Z

Hey @gabesaba I fixed the changes and added release notes also kindly review!

### Comment by [@gabesaba](https://github.com/gabesaba) — 2026-01-08T17:33:58Z

> Hey @gabesaba I fixed the changes and added release notes also kindly review!

thank you! will wait for @mimowo's comment to be addressed.

### Comment by [@skools-here](https://github.com/skools-here) — 2026-01-09T05:19:07Z

> > Hey @gabesaba I fixed the changes and added release notes also kindly review!
> 
> thank you! will wait for @mimowo's comment to be addressed.

I have completed what @mimowo said kindly take a look !

### Comment by [@skools-here](https://github.com/skools-here) — 2026-01-09T19:16:52Z

/test pull-kueue-test-e2e-main-1-35

### Comment by [@skools-here](https://github.com/skools-here) — 2026-01-09T19:31:19Z

The e2e job `pull-kueue-test-e2e-main-1-35 `failed twice without a test assertion failure. Unit tests and lint are passing locally. This seems like an infra flake.

### Comment by [@gabesaba](https://github.com/gabesaba) — 2026-01-10T06:04:39Z

> This seems like an infra flake.

I think so too

```
Error response from daemon: Head "https://registry.k8s.io/v2/jobset/jobset/manifests/v0.10.1": dial tcp 34.96.108.209:443: i/o timeout
```

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-13T13:04:05Z

@skools-here please let us know if you have cycles to work on completing that. I would be great to include in the next patch.

### Comment by [@skools-here](https://github.com/skools-here) — 2026-01-13T13:20:50Z

> @skools-here please let us know if you have cycles to work on completing that. I would be great to include in the next patch.

Thanks for asking! Yes, I do have cycles to work on this. I’m currently refactoring the test to cover the update transition as discussed, and I’ll push an updated patch shortly.

### Comment by [@skools-here](https://github.com/skools-here) — 2026-01-14T13:45:15Z

I have pushed the changes in test can you please take a look.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-15T16:38:01Z

/release-note-edit
```release-note
TAS: Fixed an issue where workloads could remain in the second-pass scheduling queue (used for integration
or TAS with ProvisioningRequests, and for TAS Node Hot Swap) even if they no longer require to be in the queue.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-15T16:38:48Z

Thank you 👍 
/lgtm
/approve
/cherrypick release-0.15
/cherrypick release-0.14

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-01-15T16:38:51Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.14`, `release-0.15` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8431#issuecomment-3755774253):

>/lgtm
>/approve
>/cherrypick release-0.15
>/cherrypick release-0.14


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-15T16:38:58Z

LGTM label has been added.  <details>Git tree hash: 14e0bca65ba1c46488aa86cdf790744bca3d58f0</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-15T16:38:58Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8431#pullrequestreview-3637252998" title="Approved">gabesaba</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8431#issuecomment-3755774253" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8431#" title="Author self-approved">skools-here</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [gabesaba,mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-01-15T17:44:17Z

@mimowo: #8431 failed to apply on top of branch "release-0.15":
```
Applying: Fix second-pass queue cleanup when workload no longer needs retry
Applying: Fix import formatting
Applying: Clarify second-pass queue cleanup logic
Applying: Add unit test for second-pass queue cleanup
Applying: Fix import order for manager tests
Applying: queue: fix second pass queue test handling deleted workloads
Using index info to reconstruct a base tree...
M	pkg/cache/queue/manager_test.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/cache/queue/manager_test.go
CONFLICT (content): Merge conflict in pkg/cache/queue/manager_test.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0006 queue: fix second pass queue test handling deleted workloads

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8431#issuecomment-3755774253):

>Thank you 👍 
>/lgtm
>/approve
>/cherrypick release-0.15
>/cherrypick release-0.14


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-01-15T17:44:53Z

@mimowo: #8431 failed to apply on top of branch "release-0.14":
```
Applying: Fix second-pass queue cleanup when workload no longer needs retry
.git/rebase-apply/patch:27: trailing whitespace.
	m.secondPassQueue.deleteByKey(workload.Key(w))	
warning: 1 line adds whitespace errors.
Using index info to reconstruct a base tree...
M	pkg/cache/queue/manager.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/cache/queue/manager.go
CONFLICT (content): Merge conflict in pkg/cache/queue/manager.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Fix second-pass queue cleanup when workload no longer needs retry

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8431#issuecomment-3755774253):

>Thank you 👍 
>/lgtm
>/approve
>/cherrypick release-0.15
>/cherrypick release-0.14


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-15T17:47:29Z

@skools-here could you please prepare cherrypicks manually? Usually the ./hack/cherry_pick_pull.sh script is handy, but you can also do it completely manually

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-16T09:46:01Z

Since we are approaching the release let me try to take care of the CPs here.

### Comment by [@skools-here](https://github.com/skools-here) — 2026-01-16T10:34:41Z

@tenzen-y thanks for the review I am working on your suggestions.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-16T10:41:33Z

Thank you @skools-here for taking those 👍

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-20T07:47:19Z

Ok, I opened https://github.com/kubernetes-sigs/kueue/issues/8682 to keep track of that

## Reviews

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-08T00:17:59Z

Thanks for the fix! I just have a small nitpick comment

/approve

- **pkg/cache/queue/manager.go:829** by [@gabesaba](https://github.com/gabesaba) — 2026-01-08T00:16:42Z
```diff
@@ -815,17 +815,21 @@ func (m *Manager) DeleteSecondPassWithoutLock(wl *kueue.Workload) {
 // QueueSecondPassIfNeeded queues for the second pass of scheduling with exponential
 // delay.
 func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload, iteration int) bool {
+	log := ctrl.LoggerFrom(ctx)
 	if workload.NeedsSecondPass(w) {
 		iteration++
 		delay := m.secondPassQueue.nextDelay(iteration)
-		log := ctrl.LoggerFrom(ctx)
 		log.V(3).Info("Workload pre-queued for second pass (with backoff)", "workload", workload.Key(w), "delay", delay)
 		m.secondPassQueue.prequeue(w)
 		m.clock.AfterFunc(delay, func() {
 			m.queueSecondPass(ctx, w, iteration)
 		})
 		return true
 	}
+	if !workload.NeedsSecondPass(w) && iteration > 0 {
```

  nit: else-if, and add a short comment to document this branch?
  ```suggestion
  	else if iteration > 0 {
  	  // some comment, including link to #8357
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-08T14:51:30Z

- **pkg/cache/queue/manager.go:818** by [@mimowo](https://github.com/mimowo) — 2026-01-08T14:51:30Z
```diff
@@ -815,16 +815,22 @@ func (m *Manager) DeleteSecondPassWithoutLock(wl *kueue.Workload) {
 // QueueSecondPassIfNeeded queues for the second pass of scheduling with exponential
 // delay.
 func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload, iteration int) bool {
+	log := ctrl.LoggerFrom(ctx)
```

  Could you please add some unit test for that changes in, probably in TestQueueSecondPassIfNeeded?

### Review by [@skools-here](https://github.com/skools-here) (commented) — 2026-01-08T15:09:54Z

- **pkg/cache/queue/manager.go:818** by [@skools-here](https://github.com/skools-here) — 2026-01-08T15:09:54Z
```diff
@@ -815,16 +815,22 @@ func (m *Manager) DeleteSecondPassWithoutLock(wl *kueue.Workload) {
 // QueueSecondPassIfNeeded queues for the second pass of scheduling with exponential
 // delay.
 func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload, iteration int) bool {
+	log := ctrl.LoggerFrom(ctx)
```

  Thanks! I’ll add unit coverage for this case and update the PR.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-10T06:09:43Z

- **pkg/cache/queue/manager_test.go:1465** by [@gabesaba](https://github.com/gabesaba) — 2026-01-10T06:09:42Z
```diff
@@ -1449,6 +1456,13 @@ func TestQueueSecondPassIfNeeded(t *testing.T) {
 			for _, wl := range tc.workloads {
 				manager.QueueSecondPassIfNeeded(ctx, wl, 0)
 			}
+			if name == "workload no longer needs second pass after first iteration" {
+				manager.QueueSecondPassIfNeeded(
+					ctx,
+					baseWorkloadNotNeedingSecondPass.DeepCopy(),
+					1,
+				)
+			}
```

  This is an anti-pattern - having a special case in the shared test logic for one of the test cases.
  
  Additionally, are we testing in this case that the new logic works? I don't see anything which changes whether the 2nd pass is needed, nor checks if it is removed from the queue.
  
  We might need another test fixture to test this. Let me know if you need help (though I'm OOO monday)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-12T15:46:18Z

- **pkg/cache/queue/manager_test.go:1465** by [@mimowo](https://github.com/mimowo) — 2026-01-12T15:46:18Z
```diff
@@ -1449,6 +1456,13 @@ func TestQueueSecondPassIfNeeded(t *testing.T) {
 			for _, wl := range tc.workloads {
 				manager.QueueSecondPassIfNeeded(ctx, wl, 0)
 			}
+			if name == "workload no longer needs second pass after first iteration" {
+				manager.QueueSecondPassIfNeeded(
+					ctx,
+					baseWorkloadNotNeedingSecondPass.DeepCopy(),
+					1,
+				)
+			}
```

  I can see this code adjusted to use `DeleteWorkload`, but I'm not clear this captures what we want to test. Using DeleteWorkload does not correspond to the original issue https://github.com/kubernetes-sigs/kueue/issues/8357.
  
  IIUC the intention is to show that `QueueSecondPassIfNeeded` would delete the workload if it was already added, but the update is no longer needed. 
  
  To better capture the intention, I would propose to add a new test case parameter like `update: func(wl *kueue.Workload)`. If specified it allows to update the workload, and if updated we call `QueueSecondPassIfNeeded` once again. This will make sure non of the pre-existing tests are affected, but we can test the new scenarios. wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-14T13:58:35Z

- **pkg/cache/queue/manager.go:844** by [@mimowo](https://github.com/mimowo) — 2026-01-14T13:58:35Z
```diff
@@ -833,6 +839,12 @@ func (m *Manager) queueSecondPass(ctx context.Context, w *kueue.Workload, iterat
 	m.Lock()
 	defer m.Unlock()
 
+	key := workload.Key(w)
+
+	if !m.secondPassQueue.prequeued.Has(key) {
```

  This does not seem necessary since we already check for the "prequeued" state inside secondpassqueue. So, this is generally better place as under locking.
  
  What we might be missing it to clear `wInfo.SecondPassIteration` if we don't queue. Do we have a test case for this scenario?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-15T13:39:18Z

- **pkg/cache/queue/manager.go:847** by [@mimowo](https://github.com/mimowo) — 2026-01-15T13:39:18Z
```diff
@@ -833,6 +839,13 @@ func (m *Manager) queueSecondPass(ctx context.Context, w *kueue.Workload, iterat
 	m.Lock()
 	defer m.Unlock()
 
+	key := workload.Key(w)
+
+	if !m.secondPassQueue.prequeued.Has(key) {
+		m.secondPassQueue.deleteByKey(key)
+		return
+	}
```

  Could you explain why this is for, and which testcase shows it is useful? Also, I think we should update `wInfo.SecondPassIteration = 0` in that case so that we start fresh if it hits second pass again.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-15T14:54:01Z

- **pkg/cache/queue/manager.go:847** by [@mimowo](https://github.com/mimowo) — 2026-01-15T14:54:01Z
```diff
@@ -833,6 +839,13 @@ func (m *Manager) queueSecondPass(ctx context.Context, w *kueue.Workload, iterat
 	m.Lock()
 	defer m.Unlock()
 
+	key := workload.Key(w)
+
+	if !m.secondPassQueue.prequeued.Has(key) {
+		m.secondPassQueue.deleteByKey(key)
+		return
+	}
```

  Oh sorry for misleading you maybe, I think this new code:
  
  ```
  		wInfo := workload.NewInfo(w, m.workloadInfoOptions...)
  		wInfo.SecondPassIteration = 0
  ```
  does not seem relevant, because the wInfo would be GCed quickly.
  
  
  Maybe to clarify I was thinking this entire block can be removed, because `prequeued` is arealdy checked inside `if m.secondPassQueue.queue(wInfo) {`

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-01-16T10:23:50Z

Thank you for creating this PR, I left some follow-up comments.

- **pkg/cache/queue/manager_test.go:1411** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-16T10:20:39Z
```diff
@@ -1395,22 +1396,26 @@ func TestQueueSecondPassIfNeeded(t *testing.T) {
 						DelayedTopologyRequest(kueue.DelayedTopologyRequestStatePending).
 						Obj(),
 				).
-				Obj(), now,
+				Obj(),
+			now,
 		).
 		AdmissionCheck(kueue.AdmissionCheckState{
 			Name:  "prov-check",
 			State: kueue.CheckStateReady,
 		})
+
 	baseWorkloadNotNeedingSecondPass := baseWorkloadBuilder.Clone()
 
 	cases := map[string]struct {
 		workloads []*kueue.Workload
-		deleted   sets.Set[workload.Reference]
+		update    func(*kueue.Workload)
```

  ```suggestion
  		updateWorkload    *kueue.Workload
  ```
  
  Such dynamic modification while UT should be avoided as much as possible.
  Those anonymous functions bring us surprises, maintenance PoV in the long-running future.

- **pkg/cache/queue/manager_test.go:1470** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-16T10:21:16Z
```diff
@@ -1420,43 +1425,57 @@ func TestQueueSecondPassIfNeeded(t *testing.T) {
 			passTime:  time.Second,
 			wantReady: sets.New(workload.Key(baseWorkloadNeedingSecondPass.Obj())),
 		},
-		"single queued workload deleted in the meanwhile": {
+		"workload stops needing second pass after being queued": {
 			workloads: []*kueue.Workload{
 				baseWorkloadNeedingSecondPass.DeepCopy(),
-				baseWorkloadNotNeedingSecondPass.DeepCopy(),
 			},
-			deleted:  sets.New(workload.Key(baseWorkloadNeedingSecondPass.Obj())),
-			passTime: time.Second,
+			update: func(wl *kueue.Workload) {
+				wl.Status.Admission = nil
+			},
+			passTime:  time.Second,
+			wantReady: nil,
 		},
-		"two queued workloads, one deleted in the meanwhile": {
+		"two queued workloads, one updated to no longer need second pass": {
 			workloads: []*kueue.Workload{
+				baseWorkloadNeedingSecondPass.Clone().Name("first").Obj(),
 				baseWorkloadNeedingSecondPass.Clone().Name("second").Obj(),
 			},
-			deleted:   sets.New(workload.Key(baseWorkloadNeedingSecondPass.Obj())),
+			update: func(wl *kueue.Workload) {
+				wl.Status.Admission = nil
+			},
 			passTime:  time.Second,
 			wantReady: sets.New(workload.NewReference("default", "second")),
 		},
 	}
+
 	for name, tc := range cases {
 		t.Run(name, func(t *testing.T) {
 			ctx, _ := utiltesting.ContextWithLog(t)
+
 			fakeClock := testingclock.NewFakeClock(now)
-			opts := []Option{
+			manager := NewManager(
+				utiltesting.NewFakeClient(),
+				nil,
 				WithClock(fakeClock),
-			}
-			manager := NewManager(utiltesting.NewFakeClient(), nil, opts...)
+			)
 
 			for _, wl := range tc.workloads {
 				manager.QueueSecondPassIfNeeded(ctx, wl, 0)
 			}
-			for _, wl := range tc.deleted.UnsortedList() {
-				manager.secondPassQueue.deleteByKey(wl)
+
+			if tc.update != nil {
+				wl := tc.workloads[0]
+				tc.update(wl)
+				manager.QueueSecondPassIfNeeded(ctx, wl, 1)
 			}
```

  ```suggestion
  			if tc.updateWorkload != nil {
  				manager.QueueSecondPassIfNeeded(ctx, tc.updateWorkload, 1)
  			}
  ```

- **pkg/cache/queue/manager.go:818** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-16T10:22:28Z
```diff
@@ -815,16 +815,22 @@ func (m *Manager) DeleteSecondPassWithoutLock(wl *kueue.Workload) {
 // QueueSecondPassIfNeeded queues for the second pass of scheduling with exponential
 // delay.
 func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload, iteration int) bool {
+	log := ctrl.LoggerFrom(ctx)
```

  ```suggestion
  	log := ctrl.LoggerFrom(ctx)
  	wlKey := workload.Key(w)
  ```
  
  `wlKey` is the same during this function.

- **pkg/cache/queue/manager.go:822** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-16T10:22:41Z
```diff
@@ -815,16 +815,22 @@ func (m *Manager) DeleteSecondPassWithoutLock(wl *kueue.Workload) {
 // QueueSecondPassIfNeeded queues for the second pass of scheduling with exponential
 // delay.
 func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload, iteration int) bool {
+	log := ctrl.LoggerFrom(ctx)
 	if workload.NeedsSecondPass(w) {
 		iteration++
 		delay := m.secondPassQueue.nextDelay(iteration)
-		log := ctrl.LoggerFrom(ctx)
 		log.V(3).Info("Workload pre-queued for second pass (with backoff)", "workload", workload.Key(w), "delay", delay)
```

  ```suggestion
  		log.V(3).Info("Workload pre-queued for second pass (with backoff)", "workload", wlKey, "delay", delay)
  ```

- **pkg/cache/queue/manager.go:832** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-16T10:22:50Z
```diff
@@ -815,16 +815,22 @@ func (m *Manager) DeleteSecondPassWithoutLock(wl *kueue.Workload) {
 // QueueSecondPassIfNeeded queues for the second pass of scheduling with exponential
 // delay.
 func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload, iteration int) bool {
+	log := ctrl.LoggerFrom(ctx)
 	if workload.NeedsSecondPass(w) {
 		iteration++
 		delay := m.secondPassQueue.nextDelay(iteration)
-		log := ctrl.LoggerFrom(ctx)
 		log.V(3).Info("Workload pre-queued for second pass (with backoff)", "workload", workload.Key(w), "delay", delay)
 		m.secondPassQueue.prequeue(w)
 		m.clock.AfterFunc(delay, func() {
 			m.queueSecondPass(ctx, w, iteration)
 		})
 		return true
+	} else if iteration > 0 {
+		// Remove the workload from the second-pass queue only after at least one
+		// retry iteration, to avoid canceling the initial backoff window.
+		// See #8357.
+		log.V(3).Info("Workload removed from second pass queue", "workload", workload.Key(w))
```

  ```suggestion
  		log.V(3).Info("Workload removed from second pass queue", "workload", wlKey)
  ```

- **pkg/cache/queue/manager.go:831** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-16T10:23:27Z
```diff
@@ -815,16 +815,22 @@ func (m *Manager) DeleteSecondPassWithoutLock(wl *kueue.Workload) {
 // QueueSecondPassIfNeeded queues for the second pass of scheduling with exponential
 // delay.
 func (m *Manager) QueueSecondPassIfNeeded(ctx context.Context, w *kueue.Workload, iteration int) bool {
+	log := ctrl.LoggerFrom(ctx)
 	if workload.NeedsSecondPass(w) {
 		iteration++
 		delay := m.secondPassQueue.nextDelay(iteration)
-		log := ctrl.LoggerFrom(ctx)
 		log.V(3).Info("Workload pre-queued for second pass (with backoff)", "workload", workload.Key(w), "delay", delay)
 		m.secondPassQueue.prequeue(w)
 		m.clock.AfterFunc(delay, func() {
 			m.queueSecondPass(ctx, w, iteration)
 		})
 		return true
+	} else if iteration > 0 {
+		// Remove the workload from the second-pass queue only after at least one
+		// retry iteration, to avoid canceling the initial backoff window.
+		// See #8357.
```

  ```suggestion
  		// See https://github.com/kubernetes-sigs/kueue/issues/8357.
  ```
  
  Could you use a link for visibility?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T10:44:20Z

- **pkg/cache/queue/manager_test.go:1411** by [@mimowo](https://github.com/mimowo) — 2026-01-16T10:44:20Z
```diff
@@ -1395,22 +1396,26 @@ func TestQueueSecondPassIfNeeded(t *testing.T) {
 						DelayedTopologyRequest(kueue.DelayedTopologyRequestStatePending).
 						Obj(),
 				).
-				Obj(), now,
+				Obj(),
+			now,
 		).
 		AdmissionCheck(kueue.AdmissionCheckState{
 			Name:  "prov-check",
 			State: kueue.CheckStateReady,
 		})
+
 	baseWorkloadNotNeedingSecondPass := baseWorkloadBuilder.Clone()
 
 	cases := map[string]struct {
 		workloads []*kueue.Workload
-		deleted   sets.Set[workload.Reference]
+		update    func(*kueue.Workload)
```

  +1, current implementation sets status.admission=nil to make the workload not needing the second pass, but I'm not sure we have a helper for that. We can alternatively Evict the workload for example (eg. using EvictedAt), which would also be more realistic case, because workload is first evicted, and only then the admission is removed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T12:04:24Z

- **pkg/cache/queue/manager_test.go:1411** by [@mimowo](https://github.com/mimowo) — 2026-01-16T12:04:24Z
```diff
@@ -1395,22 +1396,26 @@ func TestQueueSecondPassIfNeeded(t *testing.T) {
 						DelayedTopologyRequest(kueue.DelayedTopologyRequestStatePending).
 						Obj(),
 				).
-				Obj(), now,
+				Obj(),
+			now,
 		).
 		AdmissionCheck(kueue.AdmissionCheckState{
 			Name:  "prov-check",
 			State: kueue.CheckStateReady,
 		})
+
 	baseWorkloadNotNeedingSecondPass := baseWorkloadBuilder.Clone()
 
 	cases := map[string]struct {
 		workloads []*kueue.Workload
-		deleted   sets.Set[workload.Reference]
+		update    func(*kueue.Workload)
```

  I opened the potentially related issue https://github.com/kubernetes-sigs/kueue/issues/8633
