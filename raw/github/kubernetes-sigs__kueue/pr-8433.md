# PR #8433: Added workloadAssignedQueues logic handling it.

**Summary**: Added workloadAssignedQueues logic handling it.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8433

**Last updated**: 2026-01-14T15:15:42Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@Singularity23x0](https://github.com/Singularity23x0)
- **Branch**: `5310-v5-schedcache-assignedqueue` → `main`
- **Created**: 2025-12-30T15:57:26Z
- **Updated**: 2026-01-14T15:15:42Z
- **Merged**: 2026-01-14T15:15:39Z
- **Closed**: 2026-01-14T15:15:39Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@pajakd](https://github.com/pajakd), [@mbobrovskyi](https://github.com/mbobrovskyi), [@gabesaba](https://github.com/gabesaba), [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 4   **+199 / -106**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind feature

#### What this PR does / why we need it:
Adds workloadAssignedQueues tracking to the scheduler cache, by analogy to what 7915 and its successor have are implementing for the queue cache.
This change will be followed up by changing the assumedWorkloads map to a structure holding only boolean information on whether a wl has been assumed, to avoid data redundancy and conflicts, as workloadAssignedQueues will already be providing the queue assignment mappings.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->

Part 2.1 of addressing: https://github.com/kubernetes-sigs/kueue/issues/5310

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-12-30T15:57:34Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 55fa7ba9d2d97b7d37bd265268c3c8a3eeb94a2f |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/696668854633450008b03bef |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-12-30T15:57:37Z

Hi @Singularity23x0. Thanks for your PR.

I'm waiting for a [github.com](https://kubernetes-sigs/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-12-31T14:35:12Z

/ok-to-test

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-01-09T13:58:32Z

Please rebase on https://github.com/kubernetes-sigs/kueue/pull/8474 once it's merged

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-13T13:02:29Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8433#pullrequestreview-3655674413" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8433#" title="Author self-approved">Singularity23x0</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-01-14T14:48:19Z

/lgtm
Thanks a lot!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-14T14:48:27Z

LGTM label has been added.  <details>Git tree hash: 959d2a98b72427290e78419912ca72682d1c30b1</details>

## Reviews

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-01-08T13:56:05Z

- **pkg/cache/scheduler/cache.go:621** by [@PBundyra](https://github.com/PBundyra) — 2026-01-08T13:56:04Z
```diff
@@ -584,53 +587,90 @@ func (c *Cache) addOrUpdateWorkload(log logr.Logger, w *kueue.Workload) bool {
 		return false
 	}
 
-	c.cleanupAssumedState(log, w)
-
 	if c.podsReadyTracking {
 		c.podsReadyCond.Broadcast()
 	}
-	clusterQueue.addOrUpdateWorkload(log, w)
-	return true
+
+	err := c.addOrUpdateWorkloadToQueue(log, w, clusterQueue)
+	return err == nil
 }
 
-func (c *Cache) UpdateWorkload(log logr.Logger, oldWl, newWl *kueue.Workload) error {
+func (c *Cache) UpdateWorkload(log logr.Logger, wl *kueue.Workload) error {
 	c.Lock()
 	defer c.Unlock()
-	if workload.HasQuotaReservation(oldWl) {
-		cq := c.hm.ClusterQueue(oldWl.Status.Admission.ClusterQueue)
-		if cq == nil {
-			return errors.New("old ClusterQueue doesn't exist")
-		}
-		cq.deleteWorkload(log, oldWl)
-	}
-	c.cleanupAssumedState(log, oldWl)
 
-	if !workload.HasQuotaReservation(newWl) {
+	if !workload.HasQuotaReservation(wl) {
 		return nil
 	}
-	cq := c.hm.ClusterQueue(newWl.Status.Admission.ClusterQueue)
-	if cq == nil {
-		return errors.New("new ClusterQueue doesn't exist")
+
+	clusterQueue := c.hm.ClusterQueue(wl.Status.Admission.ClusterQueue)
+	if clusterQueue == nil {
+		return ErrCqNotFound
 	}
+
 	if c.podsReadyTracking {
 		c.podsReadyCond.Broadcast()
 	}
-	cq.addOrUpdateWorkload(log, newWl)
+
+	return c.addOrUpdateWorkloadToQueue(log, wl, clusterQueue)
+}
+
+func (c *Cache) addOrUpdateWorkloadToQueue(log logr.Logger, w *kueue.Workload, cq *clusterQueue) error {
+	wlKey := workload.Key(w)
+
+	if err := c.purgeSavedWorkloadData(log, wlKey); err != nil {
```

  I think we don't need to call it there. We'll just override the current values with new ones, and the `cq.addOrUpdateWorkload` has built-in delete

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-01-08T14:06:58Z

- **pkg/cache/scheduler/cache.go:640** by [@PBundyra](https://github.com/PBundyra) — 2026-01-08T14:06:58Z
```diff
@@ -584,53 +587,90 @@ func (c *Cache) addOrUpdateWorkload(log logr.Logger, w *kueue.Workload) bool {
 		return false
 	}
 
-	c.cleanupAssumedState(log, w)
-
 	if c.podsReadyTracking {
 		c.podsReadyCond.Broadcast()
 	}
-	clusterQueue.addOrUpdateWorkload(log, w)
-	return true
+
+	err := c.addOrUpdateWorkloadToQueue(log, w, clusterQueue)
+	return err == nil
 }
 
-func (c *Cache) UpdateWorkload(log logr.Logger, oldWl, newWl *kueue.Workload) error {
+func (c *Cache) UpdateWorkload(log logr.Logger, wl *kueue.Workload) error {
 	c.Lock()
 	defer c.Unlock()
-	if workload.HasQuotaReservation(oldWl) {
-		cq := c.hm.ClusterQueue(oldWl.Status.Admission.ClusterQueue)
-		if cq == nil {
-			return errors.New("old ClusterQueue doesn't exist")
-		}
-		cq.deleteWorkload(log, oldWl)
-	}
-	c.cleanupAssumedState(log, oldWl)
 
-	if !workload.HasQuotaReservation(newWl) {
+	if !workload.HasQuotaReservation(wl) {
 		return nil
 	}
-	cq := c.hm.ClusterQueue(newWl.Status.Admission.ClusterQueue)
-	if cq == nil {
-		return errors.New("new ClusterQueue doesn't exist")
+
+	clusterQueue := c.hm.ClusterQueue(wl.Status.Admission.ClusterQueue)
+	if clusterQueue == nil {
+		return ErrCqNotFound
 	}
+
 	if c.podsReadyTracking {
 		c.podsReadyCond.Broadcast()
 	}
-	cq.addOrUpdateWorkload(log, newWl)
+
+	return c.addOrUpdateWorkloadToQueue(log, wl, clusterQueue)
+}
+
+func (c *Cache) addOrUpdateWorkloadToQueue(log logr.Logger, w *kueue.Workload, cq *clusterQueue) error {
+	wlKey := workload.Key(w)
+
+	if err := c.purgeSavedWorkloadData(log, wlKey); err != nil {
+		return err
+	}
+
+	c.workloadAssignedQueues[wlKey] = cq.Name
+	cq.addOrUpdateWorkload(log, w)
+	return nil
+}
+
+func (c *Cache) purgeSavedWorkloadData(log logr.Logger, wlKey workload.Reference) error {
+	assignedCQ, assignded := c.workloadAssignedQueues[wlKey]
+	assumedCQ, assumed := c.assumedWorkloads[wlKey]
+
+	if assumed {
+		if !assignded {
+			return errors.New("workload assumed but not assigned to any queue")
+		}
+		if assumedCQ != assignedCQ {
+			return fmt.Errorf("workload assumed to %s but assigned to %s", assumedCQ, assignedCQ)
+		}
```

  I'm not sure if this is the right behavior. Why don't we delete the assumedCQ/assignedCQ anyway even if they differ/assignedCQ is not present?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-01-08T14:07:04Z

- **pkg/cache/scheduler/cache.go:631** by [@PBundyra](https://github.com/PBundyra) — 2026-01-08T14:07:05Z
```diff
@@ -584,53 +587,90 @@ func (c *Cache) addOrUpdateWorkload(log logr.Logger, w *kueue.Workload) bool {
 		return false
 	}
 
-	c.cleanupAssumedState(log, w)
-
 	if c.podsReadyTracking {
 		c.podsReadyCond.Broadcast()
 	}
-	clusterQueue.addOrUpdateWorkload(log, w)
-	return true
+
+	err := c.addOrUpdateWorkloadToQueue(log, w, clusterQueue)
+	return err == nil
 }
 
-func (c *Cache) UpdateWorkload(log logr.Logger, oldWl, newWl *kueue.Workload) error {
+func (c *Cache) UpdateWorkload(log logr.Logger, wl *kueue.Workload) error {
 	c.Lock()
 	defer c.Unlock()
-	if workload.HasQuotaReservation(oldWl) {
-		cq := c.hm.ClusterQueue(oldWl.Status.Admission.ClusterQueue)
-		if cq == nil {
-			return errors.New("old ClusterQueue doesn't exist")
-		}
-		cq.deleteWorkload(log, oldWl)
-	}
-	c.cleanupAssumedState(log, oldWl)
 
-	if !workload.HasQuotaReservation(newWl) {
+	if !workload.HasQuotaReservation(wl) {
 		return nil
 	}
-	cq := c.hm.ClusterQueue(newWl.Status.Admission.ClusterQueue)
-	if cq == nil {
-		return errors.New("new ClusterQueue doesn't exist")
+
+	clusterQueue := c.hm.ClusterQueue(wl.Status.Admission.ClusterQueue)
+	if clusterQueue == nil {
+		return ErrCqNotFound
 	}
+
 	if c.podsReadyTracking {
 		c.podsReadyCond.Broadcast()
 	}
-	cq.addOrUpdateWorkload(log, newWl)
+
+	return c.addOrUpdateWorkloadToQueue(log, wl, clusterQueue)
+}
+
+func (c *Cache) addOrUpdateWorkloadToQueue(log logr.Logger, w *kueue.Workload, cq *clusterQueue) error {
+	wlKey := workload.Key(w)
+
+	if err := c.purgeSavedWorkloadData(log, wlKey); err != nil {
+		return err
+	}
+
+	c.workloadAssignedQueues[wlKey] = cq.Name
+	cq.addOrUpdateWorkload(log, w)
+	return nil
+}
+
+func (c *Cache) purgeSavedWorkloadData(log logr.Logger, wlKey workload.Reference) error {
+	assignedCQ, assignded := c.workloadAssignedQueues[wlKey]
```

  ```suggestion
  	assignedCQ, assigned := c.workloadAssignedQueues[wlKey]
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-01-08T14:39:01Z

- **pkg/cache/scheduler/cache.go:581** by [@PBundyra](https://github.com/PBundyra) — 2026-01-08T14:39:01Z
```diff
@@ -584,53 +587,90 @@ func (c *Cache) addOrUpdateWorkload(log logr.Logger, w *kueue.Workload) bool {
 		return false
 	}
 
-	c.cleanupAssumedState(log, w)
-
 	if c.podsReadyTracking {
 		c.podsReadyCond.Broadcast()
 	}
-	clusterQueue.addOrUpdateWorkload(log, w)
-	return true
+
+	err := c.addOrUpdateWorkloadToQueue(log, w, clusterQueue)
+	return err == nil
 }
 
-func (c *Cache) UpdateWorkload(log logr.Logger, oldWl, newWl *kueue.Workload) error {
+func (c *Cache) UpdateWorkload(log logr.Logger, wl *kueue.Workload) error {
```

  After the changes UpdateWorkload looks very similar to AddOrUpdateWorkload. Do we have plans for consolidating those two in a follow-up?

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2026-01-12T15:29:54Z

- **pkg/cache/scheduler/cache.go:581** by [@Singularity23x0](https://github.com/Singularity23x0) — 2026-01-12T15:29:54Z
```diff
@@ -584,53 +587,90 @@ func (c *Cache) addOrUpdateWorkload(log logr.Logger, w *kueue.Workload) bool {
 		return false
 	}
 
-	c.cleanupAssumedState(log, w)
-
 	if c.podsReadyTracking {
 		c.podsReadyCond.Broadcast()
 	}
-	clusterQueue.addOrUpdateWorkload(log, w)
-	return true
+
+	err := c.addOrUpdateWorkloadToQueue(log, w, clusterQueue)
+	return err == nil
 }
 
-func (c *Cache) UpdateWorkload(log logr.Logger, oldWl, newWl *kueue.Workload) error {
+func (c *Cache) UpdateWorkload(log logr.Logger, wl *kueue.Workload) error {
```

  There is one key difference which makes it hard to reconcile these two: one returns whether the update happened, the other returns an error that was thrown. We cannot simply do a 
  `AddOrUpdateWorkload = (UpdateWorkload == nil)` since nil means "no error", not necessarily "update commenced". For example - we return **nil and true** when update happened correctly, but return **nil and false** when workload has quota reserved.

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2026-01-12T15:31:15Z

- **pkg/cache/scheduler/cache.go:640** by [@Singularity23x0](https://github.com/Singularity23x0) — 2026-01-12T15:31:15Z
```diff
@@ -584,53 +587,90 @@ func (c *Cache) addOrUpdateWorkload(log logr.Logger, w *kueue.Workload) bool {
 		return false
 	}
 
-	c.cleanupAssumedState(log, w)
-
 	if c.podsReadyTracking {
 		c.podsReadyCond.Broadcast()
 	}
-	clusterQueue.addOrUpdateWorkload(log, w)
-	return true
+
+	err := c.addOrUpdateWorkloadToQueue(log, w, clusterQueue)
+	return err == nil
 }
 
-func (c *Cache) UpdateWorkload(log logr.Logger, oldWl, newWl *kueue.Workload) error {
+func (c *Cache) UpdateWorkload(log logr.Logger, wl *kueue.Workload) error {
 	c.Lock()
 	defer c.Unlock()
-	if workload.HasQuotaReservation(oldWl) {
-		cq := c.hm.ClusterQueue(oldWl.Status.Admission.ClusterQueue)
-		if cq == nil {
-			return errors.New("old ClusterQueue doesn't exist")
-		}
-		cq.deleteWorkload(log, oldWl)
-	}
-	c.cleanupAssumedState(log, oldWl)
 
-	if !workload.HasQuotaReservation(newWl) {
+	if !workload.HasQuotaReservation(wl) {
 		return nil
 	}
-	cq := c.hm.ClusterQueue(newWl.Status.Admission.ClusterQueue)
-	if cq == nil {
-		return errors.New("new ClusterQueue doesn't exist")
+
+	clusterQueue := c.hm.ClusterQueue(wl.Status.Admission.ClusterQueue)
+	if clusterQueue == nil {
+		return ErrCqNotFound
 	}
+
 	if c.podsReadyTracking {
 		c.podsReadyCond.Broadcast()
 	}
-	cq.addOrUpdateWorkload(log, newWl)
+
+	return c.addOrUpdateWorkloadToQueue(log, wl, clusterQueue)
+}
+
+func (c *Cache) addOrUpdateWorkloadToQueue(log logr.Logger, w *kueue.Workload, cq *clusterQueue) error {
+	wlKey := workload.Key(w)
+
+	if err := c.purgeSavedWorkloadData(log, wlKey); err != nil {
+		return err
+	}
+
+	c.workloadAssignedQueues[wlKey] = cq.Name
+	cq.addOrUpdateWorkload(log, w)
+	return nil
+}
+
+func (c *Cache) purgeSavedWorkloadData(log logr.Logger, wlKey workload.Reference) error {
+	assignedCQ, assignded := c.workloadAssignedQueues[wlKey]
+	assumedCQ, assumed := c.assumedWorkloads[wlKey]
+
+	if assumed {
+		if !assignded {
+			return errors.New("workload assumed but not assigned to any queue")
+		}
+		if assumedCQ != assignedCQ {
+			return fmt.Errorf("workload assumed to %s but assigned to %s", assumedCQ, assignedCQ)
+		}
```

  Obsolete since "assumed" has been removed.

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2026-01-12T16:30:04Z

- **pkg/cache/scheduler/cache.go:621** by [@Singularity23x0](https://github.com/Singularity23x0) — 2026-01-12T16:30:04Z
```diff
@@ -584,53 +587,90 @@ func (c *Cache) addOrUpdateWorkload(log logr.Logger, w *kueue.Workload) bool {
 		return false
 	}
 
-	c.cleanupAssumedState(log, w)
-
 	if c.podsReadyTracking {
 		c.podsReadyCond.Broadcast()
 	}
-	clusterQueue.addOrUpdateWorkload(log, w)
-	return true
+
+	err := c.addOrUpdateWorkloadToQueue(log, w, clusterQueue)
+	return err == nil
 }
 
-func (c *Cache) UpdateWorkload(log logr.Logger, oldWl, newWl *kueue.Workload) error {
+func (c *Cache) UpdateWorkload(log logr.Logger, wl *kueue.Workload) error {
 	c.Lock()
 	defer c.Unlock()
-	if workload.HasQuotaReservation(oldWl) {
-		cq := c.hm.ClusterQueue(oldWl.Status.Admission.ClusterQueue)
-		if cq == nil {
-			return errors.New("old ClusterQueue doesn't exist")
-		}
-		cq.deleteWorkload(log, oldWl)
-	}
-	c.cleanupAssumedState(log, oldWl)
 
-	if !workload.HasQuotaReservation(newWl) {
+	if !workload.HasQuotaReservation(wl) {
 		return nil
 	}
-	cq := c.hm.ClusterQueue(newWl.Status.Admission.ClusterQueue)
-	if cq == nil {
-		return errors.New("new ClusterQueue doesn't exist")
+
+	clusterQueue := c.hm.ClusterQueue(wl.Status.Admission.ClusterQueue)
+	if clusterQueue == nil {
+		return ErrCqNotFound
 	}
+
 	if c.podsReadyTracking {
 		c.podsReadyCond.Broadcast()
 	}
-	cq.addOrUpdateWorkload(log, newWl)
+
+	return c.addOrUpdateWorkloadToQueue(log, wl, clusterQueue)
+}
+
+func (c *Cache) addOrUpdateWorkloadToQueue(log logr.Logger, w *kueue.Workload, cq *clusterQueue) error {
+	wlKey := workload.Key(w)
+
+	if err := c.purgeSavedWorkloadData(log, wlKey); err != nil {
```

  Cleaned up, ptal.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-01-13T12:56:11Z

- **pkg/cache/scheduler/cache.go:581** by [@PBundyra](https://github.com/PBundyra) — 2026-01-13T12:56:11Z
```diff
@@ -584,53 +587,90 @@ func (c *Cache) addOrUpdateWorkload(log logr.Logger, w *kueue.Workload) bool {
 		return false
 	}
 
-	c.cleanupAssumedState(log, w)
-
 	if c.podsReadyTracking {
 		c.podsReadyCond.Broadcast()
 	}
-	clusterQueue.addOrUpdateWorkload(log, w)
-	return true
+
+	err := c.addOrUpdateWorkloadToQueue(log, w, clusterQueue)
+	return err == nil
 }
 
-func (c *Cache) UpdateWorkload(log logr.Logger, oldWl, newWl *kueue.Workload) error {
+func (c *Cache) UpdateWorkload(log logr.Logger, wl *kueue.Workload) error {
```

  Could be consolidated if the result function returned both bool and error

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-13T13:02:18Z

LGTM overall, but let me give the tagging to @PBundyra 
/approve
Thank you 👍

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2026-01-13T14:23:19Z

- **pkg/cache/scheduler/cache.go:581** by [@Singularity23x0](https://github.com/Singularity23x0) — 2026-01-13T14:23:18Z
```diff
@@ -584,53 +587,90 @@ func (c *Cache) addOrUpdateWorkload(log logr.Logger, w *kueue.Workload) bool {
 		return false
 	}
 
-	c.cleanupAssumedState(log, w)
-
 	if c.podsReadyTracking {
 		c.podsReadyCond.Broadcast()
 	}
-	clusterQueue.addOrUpdateWorkload(log, w)
-	return true
+
+	err := c.addOrUpdateWorkloadToQueue(log, w, clusterQueue)
+	return err == nil
 }
 
-func (c *Cache) UpdateWorkload(log logr.Logger, oldWl, newWl *kueue.Workload) error {
+func (c *Cache) UpdateWorkload(log logr.Logger, wl *kueue.Workload) error {
```

  Done. In addition fixed a scenario which was misconfigured in initial implementation. It necessitated some refactors to keep the code readable. PTAL if the current arrangement works or if it is too convoluted.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-01-13T15:08:28Z

- **pkg/cache/scheduler/cache.go:581** by [@PBundyra](https://github.com/PBundyra) — 2026-01-13T15:08:27Z
```diff
@@ -584,53 +587,90 @@ func (c *Cache) addOrUpdateWorkload(log logr.Logger, w *kueue.Workload) bool {
 		return false
 	}
 
-	c.cleanupAssumedState(log, w)
-
 	if c.podsReadyTracking {
 		c.podsReadyCond.Broadcast()
 	}
-	clusterQueue.addOrUpdateWorkload(log, w)
-	return true
+
+	err := c.addOrUpdateWorkloadToQueue(log, w, clusterQueue)
+	return err == nil
 }
 
-func (c *Cache) UpdateWorkload(log logr.Logger, oldWl, newWl *kueue.Workload) error {
+func (c *Cache) UpdateWorkload(log logr.Logger, wl *kueue.Workload) error {
```

  What I meant is that we could get replace the `UpdateWorkload` with `AddOrUpdate` completely, but I'm fine with leaving this for a small follow-up

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-01-13T15:12:49Z

- **pkg/cache/scheduler/cache.go:599** by [@PBundyra](https://github.com/PBundyra) — 2026-01-13T15:12:49Z
```diff
@@ -568,64 +571,82 @@ func (c *Cache) UpdateLocalQueue(oldQ, newQ *kueue.LocalQueue) error {
 func (c *Cache) AddOrUpdateWorkload(log logr.Logger, w *kueue.Workload) bool {
 	c.Lock()
 	defer c.Unlock()
-	return c.addOrUpdateWorkload(log, w)
+	updated, _ := c.addOrUpdateWorkloadWithoutLock(log, w)
+	return updated
 }
 
-func (c *Cache) addOrUpdateWorkload(log logr.Logger, w *kueue.Workload) bool {
-	if !workload.HasQuotaReservation(w) {
-		return false
-	}
+func (c *Cache) UpdateWorkload(log logr.Logger, wl *kueue.Workload) error {
+	c.Lock()
+	defer c.Unlock()
+	_, err := c.addOrUpdateWorkloadWithoutLock(log, wl)
+	return err
+}
 
-	clusterQueue := c.hm.ClusterQueue(w.Status.Admission.ClusterQueue)
-	if clusterQueue == nil {
-		return false
+func (c *Cache) addOrUpdateWorkloadWithoutLock(log logr.Logger, wl *kueue.Workload) (bool, error) {
+	if workload.HasQuotaReservation(wl) {
+		cq := c.hm.ClusterQueue(wl.Status.Admission.ClusterQueue)
+		if cq == nil {
+			return false, ErrCqNotFound
+		}
+		c.addOrUpdateWorkloadWithQuota(log, wl, cq)
+		return true, nil
 	}
 
-	if c.podsReadyTracking {
-		c.podsReadyCond.Broadcast()
-	}
-	clusterQueue.addOrUpdateWorkload(log, w)
-	return true
+	anyOperationsPerformed := c.deleteAndUnassign(log, workload.Key(wl))
+	return anyOperationsPerformed, nil
 }
 
-func (c *Cache) UpdateWorkload(log logr.Logger, oldWl, newWl *kueue.Workload) error {
-	c.Lock()
-	defer c.Unlock()
-	if workload.HasQuotaReservation(oldWl) {
-		cq := c.hm.ClusterQueue(oldWl.Status.Admission.ClusterQueue)
-		if cq == nil {
-			return errors.New("old ClusterQueue doesn't exist")
+func (c *Cache) addOrUpdateWorkloadWithQuota(log logr.Logger, wl *kueue.Workload, cq *clusterQueue) {
```

  I think it requires some refactor: we pass `cq` parameter, and then we fetch `assignedCQ` anyway just to delete from it, and add to `cq`

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-01-13T15:23:01Z

- **pkg/cache/scheduler/cache.go:596** by [@PBundyra](https://github.com/PBundyra) — 2026-01-13T15:23:01Z
```diff
@@ -568,64 +571,82 @@ func (c *Cache) UpdateLocalQueue(oldQ, newQ *kueue.LocalQueue) error {
 func (c *Cache) AddOrUpdateWorkload(log logr.Logger, w *kueue.Workload) bool {
 	c.Lock()
 	defer c.Unlock()
-	return c.addOrUpdateWorkload(log, w)
+	updated, _ := c.addOrUpdateWorkloadWithoutLock(log, w)
+	return updated
 }
 
-func (c *Cache) addOrUpdateWorkload(log logr.Logger, w *kueue.Workload) bool {
-	if !workload.HasQuotaReservation(w) {
-		return false
-	}
+func (c *Cache) UpdateWorkload(log logr.Logger, wl *kueue.Workload) error {
+	c.Lock()
+	defer c.Unlock()
+	_, err := c.addOrUpdateWorkloadWithoutLock(log, wl)
+	return err
+}
 
-	clusterQueue := c.hm.ClusterQueue(w.Status.Admission.ClusterQueue)
-	if clusterQueue == nil {
-		return false
+func (c *Cache) addOrUpdateWorkloadWithoutLock(log logr.Logger, wl *kueue.Workload) (bool, error) {
+	if workload.HasQuotaReservation(wl) {
+		cq := c.hm.ClusterQueue(wl.Status.Admission.ClusterQueue)
+		if cq == nil {
+			return false, ErrCqNotFound
+		}
+		c.addOrUpdateWorkloadWithQuota(log, wl, cq)
+		return true, nil
 	}
 
-	if c.podsReadyTracking {
-		c.podsReadyCond.Broadcast()
-	}
-	clusterQueue.addOrUpdateWorkload(log, w)
-	return true
+	anyOperationsPerformed := c.deleteAndUnassign(log, workload.Key(wl))
+	return anyOperationsPerformed, nil
```

  ```suggestion
  	if workload.HasQuotaReservation(wl) {
  	c.deleteAndUnassign(log, workload.Key(wl))
  	return false, nil
  }
  		cq := c.hm.ClusterQueue(wl.Status.Admission.ClusterQueue)
  		if cq == nil {
  			return false, ErrCqNotFound
  		}
  		
  		c.addOrUpdateWorkloadWithQuota(log, wl, cq)
  		return true, nil
  ```
  
  First, I think it reads slightly better as we're not stacking ifs on top of each other and we can easily add another one.
  
  Second, if we delete the workload, I don't think we should return true. The true should be only returned if we indeed added the workload

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-01-13T15:24:12Z

- **pkg/cache/scheduler/cache.go:617** by [@PBundyra](https://github.com/PBundyra) — 2026-01-13T15:24:12Z
```diff
@@ -568,64 +571,82 @@ func (c *Cache) UpdateLocalQueue(oldQ, newQ *kueue.LocalQueue) error {
 func (c *Cache) AddOrUpdateWorkload(log logr.Logger, w *kueue.Workload) bool {
 	c.Lock()
 	defer c.Unlock()
-	return c.addOrUpdateWorkload(log, w)
+	updated, _ := c.addOrUpdateWorkloadWithoutLock(log, w)
+	return updated
 }
 
-func (c *Cache) addOrUpdateWorkload(log logr.Logger, w *kueue.Workload) bool {
-	if !workload.HasQuotaReservation(w) {
-		return false
-	}
+func (c *Cache) UpdateWorkload(log logr.Logger, wl *kueue.Workload) error {
+	c.Lock()
+	defer c.Unlock()
+	_, err := c.addOrUpdateWorkloadWithoutLock(log, wl)
+	return err
+}
 
-	clusterQueue := c.hm.ClusterQueue(w.Status.Admission.ClusterQueue)
-	if clusterQueue == nil {
-		return false
+func (c *Cache) addOrUpdateWorkloadWithoutLock(log logr.Logger, wl *kueue.Workload) (bool, error) {
+	if workload.HasQuotaReservation(wl) {
+		cq := c.hm.ClusterQueue(wl.Status.Admission.ClusterQueue)
+		if cq == nil {
+			return false, ErrCqNotFound
+		}
+		c.addOrUpdateWorkloadWithQuota(log, wl, cq)
+		return true, nil
 	}
 
-	if c.podsReadyTracking {
-		c.podsReadyCond.Broadcast()
-	}
-	clusterQueue.addOrUpdateWorkload(log, w)
-	return true
+	anyOperationsPerformed := c.deleteAndUnassign(log, workload.Key(wl))
+	return anyOperationsPerformed, nil
 }
 
-func (c *Cache) UpdateWorkload(log logr.Logger, oldWl, newWl *kueue.Workload) error {
-	c.Lock()
-	defer c.Unlock()
-	if workload.HasQuotaReservation(oldWl) {
-		cq := c.hm.ClusterQueue(oldWl.Status.Admission.ClusterQueue)
-		if cq == nil {
-			return errors.New("old ClusterQueue doesn't exist")
+func (c *Cache) addOrUpdateWorkloadWithQuota(log logr.Logger, wl *kueue.Workload, cq *clusterQueue) {
+	wlKey := workload.Key(wl)
+	assignedCqName, assigned := c.workloadAssignedQueues[wlKey]
+
+	if assigned && assignedCqName != cq.Name {
+		if assignedCQ := c.hm.ClusterQueue(assignedCqName); assignedCQ != nil {
+			assignedCQ.deleteWorkload(log, wlKey)
 		}
-		cq.deleteWorkload(log, oldWl)
 	}
 
-	if !workload.HasQuotaReservation(newWl) {
-		return nil
-	}
-	cq := c.hm.ClusterQueue(newWl.Status.Admission.ClusterQueue)
-	if cq == nil {
-		return errors.New("new ClusterQueue doesn't exist")
-	}
 	if c.podsReadyTracking {
 		c.podsReadyCond.Broadcast()
 	}
-	cq.addOrUpdateWorkload(log, newWl)
-	return nil
+
+	c.workloadAssignedQueues[wlKey] = cq.Name
+	cq.addOrUpdateWorkload(log, wl)
+}
+
+func (c *Cache) deleteAndUnassign(log logr.Logger, wlKey workload.Reference) bool {
```

  Does this function need to return anything?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-01-13T15:30:21Z

- **pkg/cache/scheduler/cache.go:117** by [@PBundyra](https://github.com/PBundyra) — 2026-01-13T15:30:21Z
```diff
@@ -114,6 +114,8 @@ type Cache struct {
 	workloadInfoOptions  []workload.InfoOption
 	fairSharingEnabled   bool
 	admissionFairSharing *config.AdmissionFairSharing
+	// Tracks Workload's ClusterQueue assignment throughout its presence in the cache.
```

  ```suggestion
  	// Tracks Workload's ClusterQueue assignment throughout its presence in the cache, which is when they reserve quota (`QuotaReserved=True`).
  ```

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2026-01-13T15:33:43Z

- **pkg/cache/scheduler/cache.go:596** by [@Singularity23x0](https://github.com/Singularity23x0) — 2026-01-13T15:33:43Z
```diff
@@ -568,64 +571,82 @@ func (c *Cache) UpdateLocalQueue(oldQ, newQ *kueue.LocalQueue) error {
 func (c *Cache) AddOrUpdateWorkload(log logr.Logger, w *kueue.Workload) bool {
 	c.Lock()
 	defer c.Unlock()
-	return c.addOrUpdateWorkload(log, w)
+	updated, _ := c.addOrUpdateWorkloadWithoutLock(log, w)
+	return updated
 }
 
-func (c *Cache) addOrUpdateWorkload(log logr.Logger, w *kueue.Workload) bool {
-	if !workload.HasQuotaReservation(w) {
-		return false
-	}
+func (c *Cache) UpdateWorkload(log logr.Logger, wl *kueue.Workload) error {
+	c.Lock()
+	defer c.Unlock()
+	_, err := c.addOrUpdateWorkloadWithoutLock(log, wl)
+	return err
+}
 
-	clusterQueue := c.hm.ClusterQueue(w.Status.Admission.ClusterQueue)
-	if clusterQueue == nil {
-		return false
+func (c *Cache) addOrUpdateWorkloadWithoutLock(log logr.Logger, wl *kueue.Workload) (bool, error) {
+	if workload.HasQuotaReservation(wl) {
+		cq := c.hm.ClusterQueue(wl.Status.Admission.ClusterQueue)
+		if cq == nil {
+			return false, ErrCqNotFound
+		}
+		c.addOrUpdateWorkloadWithQuota(log, wl, cq)
+		return true, nil
 	}
 
-	if c.podsReadyTracking {
-		c.podsReadyCond.Broadcast()
-	}
-	clusterQueue.addOrUpdateWorkload(log, w)
-	return true
+	anyOperationsPerformed := c.deleteAndUnassign(log, workload.Key(wl))
+	return anyOperationsPerformed, nil
```

  Applied.

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2026-01-13T15:44:11Z

- **pkg/cache/scheduler/cache.go:599** by [@Singularity23x0](https://github.com/Singularity23x0) — 2026-01-13T15:44:11Z
```diff
@@ -568,64 +571,82 @@ func (c *Cache) UpdateLocalQueue(oldQ, newQ *kueue.LocalQueue) error {
 func (c *Cache) AddOrUpdateWorkload(log logr.Logger, w *kueue.Workload) bool {
 	c.Lock()
 	defer c.Unlock()
-	return c.addOrUpdateWorkload(log, w)
+	updated, _ := c.addOrUpdateWorkloadWithoutLock(log, w)
+	return updated
 }
 
-func (c *Cache) addOrUpdateWorkload(log logr.Logger, w *kueue.Workload) bool {
-	if !workload.HasQuotaReservation(w) {
-		return false
-	}
+func (c *Cache) UpdateWorkload(log logr.Logger, wl *kueue.Workload) error {
+	c.Lock()
+	defer c.Unlock()
+	_, err := c.addOrUpdateWorkloadWithoutLock(log, wl)
+	return err
+}
 
-	clusterQueue := c.hm.ClusterQueue(w.Status.Admission.ClusterQueue)
-	if clusterQueue == nil {
-		return false
+func (c *Cache) addOrUpdateWorkloadWithoutLock(log logr.Logger, wl *kueue.Workload) (bool, error) {
+	if workload.HasQuotaReservation(wl) {
+		cq := c.hm.ClusterQueue(wl.Status.Admission.ClusterQueue)
+		if cq == nil {
+			return false, ErrCqNotFound
+		}
+		c.addOrUpdateWorkloadWithQuota(log, wl, cq)
+		return true, nil
 	}
 
-	if c.podsReadyTracking {
-		c.podsReadyCond.Broadcast()
-	}
-	clusterQueue.addOrUpdateWorkload(log, w)
-	return true
+	anyOperationsPerformed := c.deleteAndUnassign(log, workload.Key(wl))
+	return anyOperationsPerformed, nil
 }
 
-func (c *Cache) UpdateWorkload(log logr.Logger, oldWl, newWl *kueue.Workload) error {
-	c.Lock()
-	defer c.Unlock()
-	if workload.HasQuotaReservation(oldWl) {
-		cq := c.hm.ClusterQueue(oldWl.Status.Admission.ClusterQueue)
-		if cq == nil {
-			return errors.New("old ClusterQueue doesn't exist")
+func (c *Cache) addOrUpdateWorkloadWithQuota(log logr.Logger, wl *kueue.Workload, cq *clusterQueue) {
```

  Removed the need to use sub-functions after other refactor was applied. They were originally added to help with readability but I found a better way to achieve that.

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2026-01-13T15:44:45Z

- **pkg/cache/scheduler/cache.go:617** by [@Singularity23x0](https://github.com/Singularity23x0) — 2026-01-13T15:44:45Z
```diff
@@ -568,64 +571,82 @@ func (c *Cache) UpdateLocalQueue(oldQ, newQ *kueue.LocalQueue) error {
 func (c *Cache) AddOrUpdateWorkload(log logr.Logger, w *kueue.Workload) bool {
 	c.Lock()
 	defer c.Unlock()
-	return c.addOrUpdateWorkload(log, w)
+	updated, _ := c.addOrUpdateWorkloadWithoutLock(log, w)
+	return updated
 }
 
-func (c *Cache) addOrUpdateWorkload(log logr.Logger, w *kueue.Workload) bool {
-	if !workload.HasQuotaReservation(w) {
-		return false
-	}
+func (c *Cache) UpdateWorkload(log logr.Logger, wl *kueue.Workload) error {
+	c.Lock()
+	defer c.Unlock()
+	_, err := c.addOrUpdateWorkloadWithoutLock(log, wl)
+	return err
+}
 
-	clusterQueue := c.hm.ClusterQueue(w.Status.Admission.ClusterQueue)
-	if clusterQueue == nil {
-		return false
+func (c *Cache) addOrUpdateWorkloadWithoutLock(log logr.Logger, wl *kueue.Workload) (bool, error) {
+	if workload.HasQuotaReservation(wl) {
+		cq := c.hm.ClusterQueue(wl.Status.Admission.ClusterQueue)
+		if cq == nil {
+			return false, ErrCqNotFound
+		}
+		c.addOrUpdateWorkloadWithQuota(log, wl, cq)
+		return true, nil
 	}
 
-	if c.podsReadyTracking {
-		c.podsReadyCond.Broadcast()
-	}
-	clusterQueue.addOrUpdateWorkload(log, w)
-	return true
+	anyOperationsPerformed := c.deleteAndUnassign(log, workload.Key(wl))
+	return anyOperationsPerformed, nil
 }
 
-func (c *Cache) UpdateWorkload(log logr.Logger, oldWl, newWl *kueue.Workload) error {
-	c.Lock()
-	defer c.Unlock()
-	if workload.HasQuotaReservation(oldWl) {
-		cq := c.hm.ClusterQueue(oldWl.Status.Admission.ClusterQueue)
-		if cq == nil {
-			return errors.New("old ClusterQueue doesn't exist")
+func (c *Cache) addOrUpdateWorkloadWithQuota(log logr.Logger, wl *kueue.Workload, cq *clusterQueue) {
+	wlKey := workload.Key(wl)
+	assignedCqName, assigned := c.workloadAssignedQueues[wlKey]
+
+	if assigned && assignedCqName != cq.Name {
+		if assignedCQ := c.hm.ClusterQueue(assignedCqName); assignedCQ != nil {
+			assignedCQ.deleteWorkload(log, wlKey)
 		}
-		cq.deleteWorkload(log, oldWl)
 	}
 
-	if !workload.HasQuotaReservation(newWl) {
-		return nil
-	}
-	cq := c.hm.ClusterQueue(newWl.Status.Admission.ClusterQueue)
-	if cq == nil {
-		return errors.New("new ClusterQueue doesn't exist")
-	}
 	if c.podsReadyTracking {
 		c.podsReadyCond.Broadcast()
 	}
-	cq.addOrUpdateWorkload(log, newWl)
-	return nil
+
+	c.workloadAssignedQueues[wlKey] = cq.Name
+	cq.addOrUpdateWorkload(log, wl)
+}
+
+func (c *Cache) deleteAndUnassign(log logr.Logger, wlKey workload.Reference) bool {
```

  Removed altogether.
