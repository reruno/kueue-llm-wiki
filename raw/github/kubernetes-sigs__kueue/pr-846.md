# PR #846: [jobframework] Move `EquivalentToWorkload` functionality into framework

**Summary**: [jobframework] Move `EquivalentToWorkload` functionality into framework

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/846

**Last updated**: 2023-07-11T08:51:26Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `equivalent_in_framework` → `main`
- **Created**: 2023-06-13T05:31:50Z
- **Updated**: 2023-07-11T08:51:26Z
- **Merged**: 2023-06-29T12:07:37Z
- **Closed**: 2023-06-29T12:07:37Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@kerthcet](https://github.com/kerthcet)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 11   **+192 / -306**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Move `EquivalentToWorkload` functionality into the jobframework and simplify the GenericJob interface.
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #782

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-06-13T05:31:55Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c03612ea944b34d06de6724907ac307120ba6e18 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/649d1cce0c4930000847c197 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-846--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-846--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTg0Ni0ta3ViZXJuZXRlcy1zaWdzLWt1ZXVlLm5ldGxpZnkuYXBwIn0.JKDZbpIRunRPMwJyli-Ozg8ZduKDHR_6g1LTnoEl3vU)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-13T20:25:45Z

@kerthcet could you make a pass?

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-06-14T10:14:32Z

/assign

### Comment by [@trasc](https://github.com/trasc) — 2023-06-16T13:55:30Z

/retest

### Comment by [@trasc](https://github.com/trasc) — 2023-06-16T16:05:20Z

/retest

### Comment by [@trasc](https://github.com/trasc) — 2023-06-19T09:45:07Z

> Also, given the current state in which Job suspension is three stages (suspend=true, startTime=nil, reset nodeSelectors)..

I propose #868 to simplify the stop of jobs which don't need a status reset.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-20T19:38:59Z

> I propose https://github.com/kubernetes-sigs/kueue/pull/868 to simplify the stop of jobs which don't need a status reset.

Thanks, although the problem for Job still exists.
But that's beyond the scope of this PR.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-29T11:56:20Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-06-29T11:56:27Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/846#issuecomment-1613008479" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/846#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-06-14T16:02:54Z

- **pkg/util/testing/wrappers.go:221** by [@kerthcet](https://github.com/kerthcet) — 2023-06-14T10:16:24Z
```diff
@@ -217,6 +217,11 @@ func (p *PodSetWrapper) InitContainers(containers ...corev1.Container) *PodSetWr
 	return p
 }
 
+func (p *PodSetWrapper) NodeSelector(kv map[string]string) *PodSetWrapper {
+	p.Template.Spec.NodeSelector = kv
```

  maybe append is better? But we need to allocate at first if it's nil

- **pkg/controller/jobframework/reconciler.go:374** by [@kerthcet](https://github.com/kerthcet) — 2023-06-14T15:52:31Z
```diff
@@ -365,7 +366,24 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
 		return false
 	}
-	return job.EquivalentToWorkload(*wl)
+
+	jobPodSets := job.PodSets()
+	if job.IsSuspended() {
```

  I understand this helps to decrease the code quantity, but it doesn't seem more **maintainable**, like why we don't have to compare the __count__ when job is unsuspended(doesn't jobs except batchJob should also compare the value), and why `jobPodSets[i].Count > wl.Spec.PodSets[i].Count`, I know the reason is related to *partial admission*, but  not for  the future readers. 
  My suggestion is leave the interface `equivalentToWorkload` as it is, and make functions like `ComparePodSetSlices` as util ones.
  
  This is based on my understanding, happy to hear yours ...

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-06-14T19:47:24Z

- **pkg/util/testing/wrappers.go:221** by [@trasc](https://github.com/trasc) — 2023-06-14T19:47:24Z
```diff
@@ -217,6 +217,11 @@ func (p *PodSetWrapper) InitContainers(containers ...corev1.Container) *PodSetWr
 	return p
 }
 
+func (p *PodSetWrapper) NodeSelector(kv map[string]string) *PodSetWrapper {
+	p.Template.Spec.NodeSelector = kv
```

  It's very unlikely to need the append part. it's just for tests. It should be easy to change if/when needed.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-06-14T20:04:14Z

- **pkg/controller/jobframework/reconciler.go:374** by [@trasc](https://github.com/trasc) — 2023-06-14T20:04:14Z
```diff
@@ -365,7 +366,24 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
 		return false
 	}
-	return job.EquivalentToWorkload(*wl)
+
+	jobPodSets := job.PodSets()
+	if job.IsSuspended() {
```

  The main purpose is to decrees the complexity of the `GenericJob` interface, not so much for the Jobs
  we have integrated now but for the potential new ones.
  
  `jobPodSets[i].Count > wl.Spec.PodSets[i].Count` is to ensure that once the workload is admitted, the job will not try to use more resources that  it has initially requested.
  
  I have tried to improve the comments.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-06-16T11:16:01Z

- **pkg/controller/jobframework/reconciler.go:374** by [@kerthcet](https://github.com/kerthcet) — 2023-06-16T11:15:26Z
```diff
@@ -365,7 +366,24 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
 		return false
 	}
-	return job.EquivalentToWorkload(*wl)
+
+	jobPodSets := job.PodSets()
+	if job.IsSuspended() {
```

  Apologize for the delay ... 
  
  So for unsuspended Job, we will not compare the count right? I think this is a bit different than what we're today(for mpiJob and rayJob), isn't that should be counted?

- **pkg/util/testing/wrappers.go:221** by [@kerthcet](https://github.com/kerthcet) — 2023-06-16T11:15:56Z
```diff
@@ -217,6 +217,11 @@ func (p *PodSetWrapper) InitContainers(containers ...corev1.Container) *PodSetWr
 	return p
 }
 
+func (p *PodSetWrapper) NodeSelector(kv map[string]string) *PodSetWrapper {
+	p.Template.Spec.NodeSelector = kv
```

  Not a big deal actually, we can revisit this later if necessary.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-06-16T11:35:58Z

- **pkg/controller/jobframework/reconciler.go:374** by [@trasc](https://github.com/trasc) — 2023-06-16T11:35:58Z
```diff
@@ -365,7 +366,24 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
 		return false
 	}
-	return job.EquivalentToWorkload(*wl)
+
+	jobPodSets := job.PodSets()
+	if job.IsSuspended() {
```

  We are checking that it's not using more that what known during admission. I the future , if we are 100% sure that the job is not supporting Partial admission  or dynamic reclaim we can make a strict check.
  
  For now the trend is to try to add support for partial-admission  and dynamic-reclaim in all the frameworks.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-06-16T14:02:09Z

- **pkg/controller/jobframework/reconciler.go:378** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-15T19:43:40Z
```diff
@@ -365,7 +366,29 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
```

  we should also check the namespace

- **pkg/controller/jobframework/reconciler.go:374** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-15T19:47:25Z
```diff
@@ -365,7 +366,24 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
 		return false
 	}
-	return job.EquivalentToWorkload(*wl)
+
+	jobPodSets := job.PodSets()
+	if job.IsSuspended() {
```

  I agree with @kerthcet here.
  
  Also, given the current state in which Job suspension is three stages (suspend=true, startTime=nil, reset nodeSelectors), there is a high chance of failure and it is possible that we might try to reconcile a Job in an intermediate state where this equivalency check would fail.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-06-16T14:43:19Z

- **pkg/controller/jobframework/reconciler.go:374** by [@kerthcet](https://github.com/kerthcet) — 2023-06-16T14:43:19Z
```diff
@@ -365,7 +366,24 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
 		return false
 	}
-	return job.EquivalentToWorkload(*wl)
+
+	jobPodSets := job.PodSets()
+	if job.IsSuspended() {
```

  em... actually what I learned in the past is we should **be strict at first** then evolve based on the feedbacks, but we're doing the opposite, we change the current behavior based on our assumptions. 
  
  Or we can add a new method to the job interface to know whether the Job supports partial admission, but it seems more complexities then. Glad to hear other's advices here since it's a tradeoff. cc @alculquicondor @tenzen-y

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-16T15:47:34Z

- **pkg/controller/jobframework/reconciler.go:374** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-16T15:47:34Z
```diff
@@ -365,7 +366,24 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
 		return false
 	}
-	return job.EquivalentToWorkload(*wl)
+
+	jobPodSets := job.PodSets()
+	if job.IsSuspended() {
```

  > My suggestion is leave the interface equivalentToWorkload as it is, and make functions like ComparePodSetSlices as util ones.
  
  +1 with @kerthcet 
  
  @alculquicondor @kerthcet @trasc PartialAdmission is an alpha-stage feature. So once the feature graduates to GA, can we re-evaluate whether removing `EquivalentToWorkload ` from `GenericJob` interface is worth it?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-06-19T06:15:11Z

- **pkg/controller/jobframework/reconciler.go:374** by [@trasc](https://github.com/trasc) — 2023-06-19T06:15:11Z
```diff
@@ -365,7 +366,24 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
 		return false
 	}
-	return job.EquivalentToWorkload(*wl)
+
+	jobPodSets := job.PodSets()
+	if job.IsSuspended() {
```

  I've updated this, the count check is skipped only if the job is unsuspended and it accepts partial admission, in which case only the podSets with variable count can have a different count which should be
  in the  minCout .. Count interval.  The check is as **strict** as before for all the integrations.
  
  If in the future, we need a more specific approach for some frameworks we can add an optional interface then.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-06-20T14:35:40Z

- **pkg/controller/jobframework/reconciler.go:378** by [@trasc](https://github.com/trasc) — 2023-06-20T14:35:39Z
```diff
@@ -365,7 +366,29 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
```

  There are two problems with this:
  - this is called only on workload object from within the same namespace
  - there is no namespace in the ownerRef.
  We could change it to check the UID, but it will be harder to follow and the added value will be small in my opinion.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-06-20T19:38:18Z

- **pkg/controller/jobframework/reconciler.go:378** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-20T19:38:18Z
```diff
@@ -365,7 +366,29 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
```

  Valid points.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-06-20T19:40:03Z

- **pkg/controller/jobframework/reconciler.go:377** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-20T19:40:03Z
```diff
@@ -368,7 +369,29 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
 		return false
 	}
-	return job.EquivalentToWorkload(*wl)
+
+	jobPodSets := job.PodSets()
+
+	if !workload.CanBePartiallyAdmitted(wl) || job.IsSuspended() {
+		// the two sets should fully match.
+		return utilequality.ComparePodSetSlices(jobPodSets, wl.Spec.PodSets, true)
```

  I'm still against this simple comparison, because it might not work for a Job that fails in between.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-06-23T06:57:40Z

- **pkg/controller/jobframework/reconciler.go:377** by [@trasc](https://github.com/trasc) — 2023-06-23T06:57:39Z
```diff
@@ -368,7 +369,29 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
 		return false
 	}
-	return job.EquivalentToWorkload(*wl)
+
+	jobPodSets := job.PodSets()
+
+	if !workload.CanBePartiallyAdmitted(wl) || job.IsSuspended() {
+		// the two sets should fully match.
+		return utilequality.ComparePodSetSlices(jobPodSets, wl.Spec.PodSets, true)
```

  In which case?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-06-23T07:08:34Z

- **pkg/controller/jobframework/reconciler.go:377** by [@trasc](https://github.com/trasc) — 2023-06-23T07:08:34Z
```diff
@@ -368,7 +369,29 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
 		return false
 	}
-	return job.EquivalentToWorkload(*wl)
+
+	jobPodSets := job.PodSets()
+
+	if !workload.CanBePartiallyAdmitted(wl) || job.IsSuspended() {
+		// the two sets should fully match.
+		return utilequality.ComparePodSetSlices(jobPodSets, wl.Spec.PodSets, true)
```

  Also the scope of the PR is just to move the common functionality from individual the concrete implementations into the framework. I believe it dose that without any regressions. We can open followup tickets to rework if necessary.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-06-23T12:35:57Z

- **pkg/controller/jobframework/reconciler.go:377** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-23T12:35:57Z
```diff
@@ -368,7 +369,29 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
 		return false
 	}
-	return job.EquivalentToWorkload(*wl)
+
+	jobPodSets := job.PodSets()
+
+	if !workload.CanBePartiallyAdmitted(wl) || job.IsSuspended() {
+		// the two sets should fully match.
+		return utilequality.ComparePodSetSlices(jobPodSets, wl.Spec.PodSets, true)
```

  Yes, that is true, this doesn't seem to introduce regressions.
  However, I'm working on the e2e test with preemption. If we happen to find a bug, merging this PR might make it harder to cherry-pick a fix into the previous release.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-06-23T14:21:55Z

- **pkg/controller/jobframework/reconciler.go:377** by [@trasc](https://github.com/trasc) — 2023-06-23T14:21:54Z
```diff
@@ -368,7 +369,29 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
 		return false
 	}
-	return job.EquivalentToWorkload(*wl)
+
+	jobPodSets := job.PodSets()
+
+	if !workload.CanBePartiallyAdmitted(wl) || job.IsSuspended() {
+		// the two sets should fully match.
+		return utilequality.ComparePodSetSlices(jobPodSets, wl.Spec.PodSets, true)
```

  The same is for any other code changes that go only on one branch.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-06-23T14:48:03Z

- **pkg/controller/jobframework/reconciler.go:377** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-23T14:48:03Z
```diff
@@ -368,7 +369,29 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
 		return false
 	}
-	return job.EquivalentToWorkload(*wl)
+
+	jobPodSets := job.PodSets()
+
+	if !workload.CanBePartiallyAdmitted(wl) || job.IsSuspended() {
+		// the two sets should fully match.
+		return utilequality.ComparePodSetSlices(jobPodSets, wl.Spec.PodSets, true)
```

  Let me get the E2E test first to see how common the problem is.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-06-28T19:29:34Z

- **pkg/util/equality/podset.go:27** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-28T19:17:49Z
```diff
@@ -0,0 +1,55 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package equality
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	"k8s.io/utils/pointer"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+func comperePodTemplate(a, b *corev1.PodSpec) bool {
```

  ```suggestion
  func comparePodTemplate(a, b *corev1.PodSpec) bool {
  ```
  
  Although we are simplifying and ignoring other fields that are not containers. Worth leaving a comment and a TODO to have a stronger coverage of all fields, except for node affinity.

- **pkg/util/equality/podset_test.go:34** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-28T19:19:10Z
```diff
@@ -0,0 +1,101 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package equality
+
+import (
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltestting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestCompareWithMinCoutOnly(t *testing.T) {
+	cases := map[string]struct {
+		a           []kueue.PodSet
+		b           []kueue.PodSet
+		checkCounts bool
+		wnatEqual   bool
```

  ```suggestion
  		wantEqual   bool
  ```

- **pkg/util/equality/podset_test.go:29** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-28T19:19:44Z
```diff
@@ -0,0 +1,101 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package equality
+
+import (
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltestting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestCompareWithMinCoutOnly(t *testing.T) {
```

  ```suggestion
  func TestComparePodSetSlices(t *testing.T) {
  ```

- **pkg/workload/workload.go:99** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-28T19:21:26Z
```diff
@@ -96,6 +96,10 @@ func (i *Info) CanBePartiallyAdmitted() bool {
 	return false
 }
 
+func (i *Info) CanBePartiallyAdmitted() bool {
```

  worth keeping all methods belonging to Info together. So move this before `CanBePartiallyAdmitted`

- **pkg/controller/jobframework/reconciler.go:39** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-28T19:22:29Z
```diff
@@ -36,6 +36,7 @@ import (
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/constants"
 	controllerconsts "sigs.k8s.io/kueue/pkg/controller/constants"
+	utilequality "sigs.k8s.io/kueue/pkg/util/equality"
```

  ```suggestion
  	"sigs.k8s.io/kueue/pkg/util/equality"
  ```
  
  Unless there is a conflict

- **pkg/controller/jobframework/reconciler.go:384** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-28T19:25:07Z
```diff
@@ -368,7 +369,29 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
 		return false
 	}
-	return job.EquivalentToWorkload(*wl)
+
+	jobPodSets := job.PodSets()
+
+	if !workload.CanBePartiallyAdmitted(wl) || job.IsSuspended() {
```

  mental note for self:
  
  Maybe we can change `IsSuspended()` for `batch/Job` to `suspend == true && startTime == nil`

- **pkg/controller/jobframework/reconciler.go:399** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-28T19:29:28Z
```diff
@@ -368,7 +369,29 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
 		return false
 	}
-	return job.EquivalentToWorkload(*wl)
+
+	jobPodSets := job.PodSets()
+
+	if !workload.CanBePartiallyAdmitted(wl) || job.IsSuspended() {
+		// the two sets should fully match.
+		return utilequality.ComparePodSetSlices(jobPodSets, wl.Spec.PodSets, true)
+	}
+
+	// If the job is not suspended, the podSet counts of the job could be lower that
+	// the initial values, in case of partial admission.
+	if !utilequality.ComparePodSetSlices(jobPodSets, wl.Spec.PodSets, false) {
+		return false
+	}
+
+	// Make sure the job podSets don't have higher counts, and use more resources that it originally
+	// requested.
+	// the ComparePodSetSlices fails if the lengths are not equal, don't check it once more
+	for i := range jobPodSets {
+		if jobPodSets[i].Count > wl.Spec.PodSets[i].Count || jobPodSets[i].Count < pointer.Int32Deref(wl.Spec.PodSets[i].MinCount, wl.Spec.PodSets[i].Count) {
```

  can we check that the count is equal to what's in `wl.Status.Admission`?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-28T19:51:26Z

- **pkg/controller/jobframework/reconciler.go:384** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-28T19:47:40Z
```diff
@@ -368,7 +369,29 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
 		return false
 	}
-	return job.EquivalentToWorkload(*wl)
+
+	jobPodSets := job.PodSets()
+
+	if !workload.CanBePartiallyAdmitted(wl) || job.IsSuspended() {
```

  ```suggestion
  	if (features.Enabled(features.PartialAdmission) && !workload.CanBePartiallyAdmitted(wl)) || job.IsSuspended() {
  ```
  
  Should we check if the feature is enabled?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-06-29T05:55:43Z

- **pkg/controller/jobframework/reconciler.go:39** by [@trasc](https://github.com/trasc) — 2023-06-29T04:48:39Z
```diff
@@ -36,6 +36,7 @@ import (
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/constants"
 	controllerconsts "sigs.k8s.io/kueue/pkg/controller/constants"
+	utilequality "sigs.k8s.io/kueue/pkg/util/equality"
```

  done

- **pkg/controller/jobframework/reconciler.go:384** by [@trasc](https://github.com/trasc) — 2023-06-29T04:53:24Z
```diff
@@ -368,7 +369,29 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
 		return false
 	}
-	return job.EquivalentToWorkload(*wl)
+
+	jobPodSets := job.PodSets()
+
+	if !workload.CanBePartiallyAdmitted(wl) || job.IsSuspended() {
```

  `startTime == nil` is just a precondition to be able to restore the selectors ... it might help in `retry stop` but is not sufficient since  it can only cover  2 out of 3  potential failures.

- **pkg/controller/jobframework/reconciler.go:384** by [@trasc](https://github.com/trasc) — 2023-06-29T05:22:28Z
```diff
@@ -368,7 +369,29 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
 		return false
 	}
-	return job.EquivalentToWorkload(*wl)
+
+	jobPodSets := job.PodSets()
+
+	if !workload.CanBePartiallyAdmitted(wl) || job.IsSuspended() {
```

  I make better sense to ensure the minCount is not set when the feature is disabled. I'll do it in #917

- **pkg/controller/jobframework/reconciler.go:399** by [@trasc](https://github.com/trasc) — 2023-06-29T05:25:21Z
```diff
@@ -368,7 +369,29 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
 		return false
 	}
-	return job.EquivalentToWorkload(*wl)
+
+	jobPodSets := job.PodSets()
+
+	if !workload.CanBePartiallyAdmitted(wl) || job.IsSuspended() {
+		// the two sets should fully match.
+		return utilequality.ComparePodSetSlices(jobPodSets, wl.Spec.PodSets, true)
+	}
+
+	// If the job is not suspended, the podSet counts of the job could be lower that
+	// the initial values, in case of partial admission.
+	if !utilequality.ComparePodSetSlices(jobPodSets, wl.Spec.PodSets, false) {
+		return false
+	}
+
+	// Make sure the job podSets don't have higher counts, and use more resources that it originally
+	// requested.
+	// the ComparePodSetSlices fails if the lengths are not equal, don't check it once more
+	for i := range jobPodSets {
+		if jobPodSets[i].Count > wl.Spec.PodSets[i].Count || jobPodSets[i].Count < pointer.Int32Deref(wl.Spec.PodSets[i].MinCount, wl.Spec.PodSets[i].Count) {
```

  The count in admission can be smaller in case of partial reclaim.

- **pkg/util/equality/podset.go:27** by [@trasc](https://github.com/trasc) — 2023-06-29T05:39:13Z
```diff
@@ -0,0 +1,55 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package equality
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	"k8s.io/utils/pointer"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+func comperePodTemplate(a, b *corev1.PodSpec) bool {
```

  Added note to revisit and maybe extend this. 
  
  (the check is not simplified in this PR, it is the same as before for all frameworks)

- **pkg/util/equality/podset_test.go:29** by [@trasc](https://github.com/trasc) — 2023-06-29T05:40:55Z
```diff
@@ -0,0 +1,101 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package equality
+
+import (
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltestting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestCompareWithMinCoutOnly(t *testing.T) {
```

  done

- **pkg/util/equality/podset_test.go:34** by [@trasc](https://github.com/trasc) — 2023-06-29T05:41:25Z
```diff
@@ -0,0 +1,101 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package equality
+
+import (
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltestting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestCompareWithMinCoutOnly(t *testing.T) {
+	cases := map[string]struct {
+		a           []kueue.PodSet
+		b           []kueue.PodSet
+		checkCounts bool
+		wnatEqual   bool
```

  done

- **pkg/workload/workload.go:99** by [@trasc](https://github.com/trasc) — 2023-06-29T05:42:07Z
```diff
@@ -96,6 +96,10 @@ func (i *Info) CanBePartiallyAdmitted() bool {
 	return false
 }
 
+func (i *Info) CanBePartiallyAdmitted() bool {
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-29T06:05:12Z

- **pkg/controller/jobframework/reconciler.go:384** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-29T06:05:12Z
```diff
@@ -368,7 +369,29 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
 		return false
 	}
-	return job.EquivalentToWorkload(*wl)
+
+	jobPodSets := job.PodSets()
+
+	if !workload.CanBePartiallyAdmitted(wl) || job.IsSuspended() {
```

  What happens in the following situation If we check the minCount only in the webhook?
  
  1. Start kueue-controller-manager with `PartialAdmission=enabled`.
  2. Create a job with `minCount`.
  3. Re-start kueue-controller-manager with `PartialAdmission=disabled`.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-06-29T06:54:35Z

- **pkg/controller/jobframework/reconciler.go:384** by [@trasc](https://github.com/trasc) — 2023-06-29T06:54:34Z
```diff
@@ -368,7 +369,29 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
 		return false
 	}
-	return job.EquivalentToWorkload(*wl)
+
+	jobPodSets := job.PodSets()
+
+	if !workload.CanBePartiallyAdmitted(wl) || job.IsSuspended() {
```

  the workload will not be equivalent and will get recreated, which is fine since we no longer accept palatial admission after 3.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-29T06:59:05Z

- **pkg/controller/jobframework/reconciler.go:384** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-29T06:59:04Z
```diff
@@ -368,7 +369,29 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	if owner.Name != object.GetName() {
 		return false
 	}
-	return job.EquivalentToWorkload(*wl)
+
+	jobPodSets := job.PodSets()
+
+	if !workload.CanBePartiallyAdmitted(wl) || job.IsSuspended() {
```

  > which is fine since we no longer accept palatial admission after 3.
  
  Agree.
  Maybe, we need to say the specification in the docs.
