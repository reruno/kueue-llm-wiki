# PR #8477: [Fix] Re-do mk admission after eviction in worker cluster

**Summary**: [Fix] Re-do mk admission after eviction in worker cluster

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8477

**Last updated**: 2026-01-23T11:53:24Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `fix/8302-reset-mk-admission-cluster` → `main`
- **Created**: 2026-01-08T19:22:15Z
- **Updated**: 2026-01-23T11:53:24Z
- **Merged**: 2026-01-15T10:01:37Z
- **Closed**: 2026-01-15T10:01:37Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 6   **+303 / -10**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #8302

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
MultiKueue: trigger workload eviction on the management cluster when the corresponding workload is evicted
on the remote worker cluster. In particular this is fixing the issue with workloads using ProvisioningRequests, 
which could get stuck in a worker cluster which does not have enough capacity to ever admit the workloads.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-08T19:22:18Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-01-08T19:22:21Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | a9ed35d5dc6fca616f60b210f6002961a15de2ac |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6968a63ac0806a0008c94911 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-8477--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-8477--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTg0NzctLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.Pe3iaS4QC3dkCUhrGbXkqNKmJpDZCINywutSTBvFrO8)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-8477--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@mszadkow](https://github.com/mszadkow) — 2026-01-08T19:22:35Z

/ok-to-test

### Comment by [@mszadkow](https://github.com/mszadkow) — 2026-01-09T08:10:27Z

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-13T12:54:25Z

I think this PR will also address https://github.com/kubernetes-sigs/kueue/issues/8089, right? if so then mark that in the issue description
cc @yaroslava-serdiuk

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-13T12:57:11Z

LGTM overall 👍

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-13T13:06:21Z

TBH, I would prefer to categorize this as a bug, because from user perspective the issues https://github.com/kubernetes-sigs/kueue/issues/8302 are https://github.com/kubernetes-sigs/kueue/issues/8089 are already problematic for using MK with ProvReq. If we cherrypick we could fix this user story in 0.15.

### Comment by [@mszadkow](https://github.com/mszadkow) — 2026-01-13T13:47:47Z

> I think this PR will also address #8089, right? if so then mark that in the issue description cc @yaroslava-serdiuk

I am not 100% sure it's addressing that, we check for evicted workloads, depending what is the symptoms of failing ProvisioningRequest.
I can check that with manual tests in GKE

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-13T14:15:51Z

Thanks, this should work fine according to my understanding, but yes, please check.

We may also add a test for ProvReq specifically in a follow up.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-13T15:28:02Z

@mszadkow let me know about the results of your testing, but I think this should be actually fixing the ProvReq issue.
/release-note-edit
```release-note
MultiKueue: trigger workload eviction on the management cluster when the corresponding workload is evicted
on the worker remote cluster. In particular this is fixing the issue with workloads using ProvisioningRequests, 
which could get stuck in a worker clusters which does not have enough capacity to ever admit the workloads.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-13T15:28:54Z

/release-note-edit
```release-note
MultiKueue: trigger workload eviction on the management cluster when the corresponding workload is evicted
on the worker remote cluster. In particular this is fixing the issue with workloads using ProvisioningRequests, 
which could get stuck in a worker cluster which does not have enough capacity to ever admit the workloads.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-15T09:59:39Z

/lgtm
/approve
Thank you 👍 I think we can keep this without cherrypicking for now.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-15T09:59:47Z

LGTM label has been added.  <details>Git tree hash: 11eeea7979e2ed96ab750884583f6b520749a17b</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-15T09:59:50Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8477#issuecomment-3753869794" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8477#" title="Author self-approved">mszadkow</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-23T11:53:20Z

/release-note-edit
```release-note
MultiKueue: trigger workload eviction on the management cluster when the corresponding workload is evicted
on the remote worker cluster. In particular this is fixing the issue with workloads using ProvisioningRequests, 
which could get stuck in a worker cluster which does not have enough capacity to ever admit the workloads.
```

## Reviews

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-01-09T21:37:10Z

Looks good! Just a few minor comments.

- **pkg/controller/admissionchecks/multikueue/workload.go:372** by [@olekzabl](https://github.com/olekzabl) — 2026-01-09T16:12:51Z
```diff
@@ -349,22 +349,53 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 4. Handle workload evicted on manager cluster
+	// 4. Handle workload eviction
 	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
-	if remoteEvictCond != nil && remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+	if remoteEvictCond != nil {
 		remoteCl := group.remoteClients[evictedRemote].client
 		remoteWl := group.remotes[evictedRemote]
 
 		log = log.WithValues("remote", evictedRemote, "remoteWorkload", klog.KObj(remoteWl))
 		ctx = ctrl.LoggerInto(ctx, log)
 
-		if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
-			log.Error(err, "Syncing remote controller object")
-			// We'll retry this in the next reconciling.
+		// workload evicted on manager cluster
+		if remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+			if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
+				log.Error(err, "Syncing remote controller object")
+				// We'll retry this in the next reconciling.
+				return reconcile.Result{}, err
+			}
+			return reconcile.Result{}, nil
+		}
+
+		// workload eviction on worker cluster
+		log.V(5).Info("Workload gets evicted in the remote cluster", "cluster", evictedRemote)
```

  Nit: this present tense feels slightly confusing; IIUC the workload already got evicted.

- **pkg/controller/admissionchecks/multikueue/workload.go:381** by [@olekzabl](https://github.com/olekzabl) — 2026-01-09T16:49:24Z
```diff
@@ -349,22 +349,53 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 4. Handle workload evicted on manager cluster
+	// 4. Handle workload eviction
 	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
-	if remoteEvictCond != nil && remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+	if remoteEvictCond != nil {
 		remoteCl := group.remoteClients[evictedRemote].client
 		remoteWl := group.remotes[evictedRemote]
 
 		log = log.WithValues("remote", evictedRemote, "remoteWorkload", klog.KObj(remoteWl))
 		ctx = ctrl.LoggerInto(ctx, log)
 
-		if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
-			log.Error(err, "Syncing remote controller object")
-			// We'll retry this in the next reconciling.
+		// workload evicted on manager cluster
+		if remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+			if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
+				log.Error(err, "Syncing remote controller object")
+				// We'll retry this in the next reconciling.
+				return reconcile.Result{}, err
+			}
+			return reconcile.Result{}, nil
+		}
+
+		// workload eviction on worker cluster
+		log.V(5).Info("Workload gets evicted in the remote cluster", "cluster", evictedRemote)
+		needsACUpdate := acs.State == kueue.CheckStateReady
+		if err := workload.PatchAdmissionStatus(ctx, w.client, group.local, w.clock, func(wl *kueue.Workload) (bool, error) {
+			if needsACUpdate {
+				acs.State = kueue.CheckStatePending
+				acs.Message = fmt.Sprintf("Workload evicted on worker cluster: %q, resetting for re-admission", *group.local.Status.ClusterName)
+				acs.LastTransitionTime = metav1.NewTime(w.clock.Now())
+				workload.SetAdmissionCheckState(&wl.Status.AdmissionChecks, *acs, w.clock)
+				wl.Status.ClusterName = nil
+				wl.Status.NominatedClusterNames = nil
```

  Out of curiosity - can this have an effect, given [this rule](https://github.com/kubernetes-sigs/kueue/blob/d872725a0e398e29c2ad5a0030a29afdd9ea9237/apis/kueue/v1beta2/workload_types.go#L567)?
  (or is this intended to replace an empty list with `nil` in some edge cases?)

- **pkg/controller/admissionchecks/multikueue/workload.go:394** by [@olekzabl](https://github.com/olekzabl) — 2026-01-09T21:08:32Z
```diff
@@ -349,22 +349,53 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 4. Handle workload evicted on manager cluster
+	// 4. Handle workload eviction
 	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
-	if remoteEvictCond != nil && remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+	if remoteEvictCond != nil {
 		remoteCl := group.remoteClients[evictedRemote].client
 		remoteWl := group.remotes[evictedRemote]
 
 		log = log.WithValues("remote", evictedRemote, "remoteWorkload", klog.KObj(remoteWl))
 		ctx = ctrl.LoggerInto(ctx, log)
 
-		if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
-			log.Error(err, "Syncing remote controller object")
-			// We'll retry this in the next reconciling.
+		// workload evicted on manager cluster
+		if remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+			if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
+				log.Error(err, "Syncing remote controller object")
+				// We'll retry this in the next reconciling.
+				return reconcile.Result{}, err
+			}
+			return reconcile.Result{}, nil
+		}
+
+		// workload eviction on worker cluster
+		log.V(5).Info("Workload gets evicted in the remote cluster", "cluster", evictedRemote)
+		needsACUpdate := acs.State == kueue.CheckStateReady
+		if err := workload.PatchAdmissionStatus(ctx, w.client, group.local, w.clock, func(wl *kueue.Workload) (bool, error) {
+			if needsACUpdate {
+				acs.State = kueue.CheckStatePending
+				acs.Message = fmt.Sprintf("Workload evicted on worker cluster: %q, resetting for re-admission", *group.local.Status.ClusterName)
+				acs.LastTransitionTime = metav1.NewTime(w.clock.Now())
+				workload.SetAdmissionCheckState(&wl.Status.AdmissionChecks, *acs, w.clock)
+				wl.Status.ClusterName = nil
+				wl.Status.NominatedClusterNames = nil
+			}
+			return true, nil
+		}); err != nil {
+			log.Error(err, "Failed to patch workload status")
 			return reconcile.Result{}, err
 		}
 
-		// Wait for QuotaReserved=false in the local job.
+		if needsACUpdate {
+			w.recorder.Eventf(group.local, corev1.EventTypeNormal, "MultiKueue", acs.Message)
+		}
+
+		for cluster := range group.remotes {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, cluster)); err != nil {
```

  In most cases, other calls to `RemoveRemoteObjects` in this file are followed by `group.remotes[cluster] = nil`.
  Should it be done also here?

- **test/e2e/multikueue/e2e_test.go:629** by [@olekzabl](https://github.com/olekzabl) — 2026-01-09T21:19:30Z
```diff
@@ -603,6 +604,93 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 			})
 		})
 
+		ginkgo.It("Should re-do admission process when workload gets evicted in the worker", func() {
+			job := testingjob.MakeJob("job", managerNs.Name).
+				WorkloadPriorityClass(managerLowWPC.Name).
+				Queue(kueue.LocalQueueName(managerLq.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "0.9").
+				RequestAndLimit(corev1.ResourceMemory, "0.5G").
+				Obj()
+			util.MustCreate(ctx, k8sManagerClient, job)
+
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: managerNs.Name}
+			managerWl := &kueue.Workload{}
+			workerWorkload := &kueue.Workload{}
+
+			ginkgo.By("Checking that the workload is created and admitted in the manager cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, wlKey, managerWl)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(managerWl)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdAtWorker := ""
+
+			ginkgo.By("Checking that the workload is created in one of the workers", func() {
```

  Below, you'll modify worker's CQ limits to manipulate what it can fit.
  If so, how about using that trick once more, to get control over where the wl lands initially?
  
  AFAICS the [initial CPU quotas](https://github.com/kubernetes-sigs/kueue/blob/c249417d0f385de9148eb45fea63ec4f51102212/test/e2e/multikueue/e2e_test.go#L152-L168) are: 2 at worker1 and 1 at worker2.
  So if you set 1.5 for your workload, it'll certainly land on worker 1.
  Then, you could swap the quotas (say, first set 2 at w2, then set 1 at w1) and verify if the workload moved to w2.
  
  Compared to what you have now, +1 CQ update but -4 `if` blocks. (If I'm not mistaken).

- **test/e2e/multikueue/e2e_test.go:677** by [@olekzabl](https://github.com/olekzabl) — 2026-01-09T21:22:44Z
```diff
@@ -603,6 +604,93 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 			})
 		})
 
+		ginkgo.It("Should re-do admission process when workload gets evicted in the worker", func() {
+			job := testingjob.MakeJob("job", managerNs.Name).
+				WorkloadPriorityClass(managerLowWPC.Name).
+				Queue(kueue.LocalQueueName(managerLq.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "0.9").
+				RequestAndLimit(corev1.ResourceMemory, "0.5G").
+				Obj()
+			util.MustCreate(ctx, k8sManagerClient, job)
+
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: managerNs.Name}
+			managerWl := &kueue.Workload{}
+			workerWorkload := &kueue.Workload{}
+
+			ginkgo.By("Checking that the workload is created and admitted in the manager cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, wlKey, managerWl)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(managerWl)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdAtWorker := ""
+
+			ginkgo.By("Checking that the workload is created in one of the workers", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					if err := k8sWorker1Client.Get(ctx, wlKey, workerWorkload); err == nil {
+						createdAtWorker = "worker1"
+					} else {
+						g.Expect(k8sWorker2Client.Get(ctx, wlKey, workerWorkload)).To(gomega.Succeed())
+						createdAtWorker = "worker2"
+					}
+					g.Expect(workload.IsAdmitted(workerWorkload)).To(gomega.BeTrue())
+					g.Expect(workerWorkload.Spec).To(gomega.BeComparableTo(managerWl.Spec))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.GinkgoLogr.Info("Workload created at", "cluster", createdAtWorker)
+
+			ginkgo.By("Modifying worker cluster queue to not have enough resources", func() {
+				if createdAtWorker == "worker1" {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sWorker1Client.Get(ctx, client.ObjectKeyFromObject(worker1Cq), worker1Cq)).To(gomega.Succeed())
+						worker1Cq.Spec.ResourceGroups[0].Flavors[0].Resources[0].NominalQuota = resource.MustParse("0.5")
+						g.Expect(k8sWorker1Client.Update(ctx, worker1Cq)).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				} else {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sWorker2Client.Get(ctx, client.ObjectKeyFromObject(worker2Cq), worker2Cq)).To(gomega.Succeed())
+						worker2Cq.Spec.ResourceGroups[0].Flavors[0].Resources[0].NominalQuota = resource.MustParse("0.5")
+						g.Expect(k8sWorker2Client.Update(ctx, worker2Cq)).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Triggering eviction in worker", func() {
+				if createdAtWorker == "worker1" {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sWorker1Client.Get(ctx, wlKey, workerWorkload)).To(gomega.Succeed())
+						g.Expect(workload.SetConditionAndUpdate(ctx, k8sWorker1Client, workerWorkload, kueue.WorkloadEvicted, metav1.ConditionTrue, kueue.WorkloadEvictedByPreemption, "By test", "evict", util.RealClock)).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				} else {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sWorker2Client.Get(ctx, wlKey, workerWorkload)).To(gomega.Succeed())
+						g.Expect(workload.SetConditionAndUpdate(ctx, k8sWorker2Client, workerWorkload, kueue.WorkloadEvicted, metav1.ConditionTrue, kueue.WorkloadEvictedByPreemption, "By test", "evict", util.RealClock)).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Checking that the workload is re-admitted in the other worker cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, wlKey, managerWl)).To(gomega.Succeed())
+					g.Expect(managerWl.Status.ClusterName).NotTo(gomega.HaveValue(gomega.Equal(createdAtWorker)))
```

  You could also check that it's not empty.

- **test/integration/multikueue/jobs_test.go:1889** by [@olekzabl](https://github.com/olekzabl) — 2026-01-09T21:23:18Z
```diff
@@ -1885,6 +1886,91 @@ var _ = ginkgo.Describe("MultiKueue", ginkgo.Label("area:multikueue", "feature:m
 			}, gomega.Equal(completedJobCondition))))
 		})
 	})
+	ginkgo.It("Should redo the admission process once the workload looses Admission in the worker cluster", func() {
```

  ```suggestion
  	ginkgo.It("Should redo the admission process once the workload loses Admission in the worker cluster", func() {
  ```

- **test/integration/multikueue/jobs_test.go:1950** by [@olekzabl](https://github.com/olekzabl) — 2026-01-09T21:28:01Z
```diff
@@ -1885,6 +1886,91 @@ var _ = ginkgo.Describe("MultiKueue", ginkgo.Label("area:multikueue", "feature:m
 			}, gomega.Equal(completedJobCondition))))
 		})
 	})
+	ginkgo.It("Should redo the admission process once the workload looses Admission in the worker cluster", func() {
+		job := testingjob.MakeJob("job", managerNs.Name).
+			ManagedBy(kueue.MultiKueueControllerName).
+			Queue(kueue.LocalQueueName(managerLq.Name)).
+			Obj()
+		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, job)
+
+		createdWorkload := &kueue.Workload{}
+		wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: managerNs.Name}
+
+		ginkgo.By("setting workload reservation in the management cluster", func() {
+			admission := utiltestingapi.MakeAdmission(managerCq.Name).Obj()
+			util.SetQuotaReservation(managerTestCluster.ctx, managerTestCluster.client, wlLookupKey, admission)
+		})
+
+		ginkgo.By("checking the workload creation in the worker clusters", func() {
+			managerWl := &kueue.Workload{}
+			gomega.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, managerWl)).To(gomega.Succeed())
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(createdWorkload.Spec).To(gomega.BeComparableTo(managerWl.Spec))
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(createdWorkload.Spec).To(gomega.BeComparableTo(managerWl.Spec))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("setting workload reservation in worker1, AC state is updated in manager and worker2 wl is removed", func() {
+			admission := utiltestingapi.MakeAdmission(managerCq.Name).Obj()
+			util.SetQuotaReservation(worker1TestCluster.ctx, worker1TestCluster.client, wlLookupKey, admission)
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				acs := admissioncheck.FindAdmissionCheck(createdWorkload.Status.AdmissionChecks, kueue.AdmissionCheckReference(multiKueueAC.Name))
+				g.Expect(acs).NotTo(gomega.BeNil())
+				g.Expect(acs.State).To(gomega.Equal(kueue.CheckStateReady))
+				g.Expect(acs.Message).To(gomega.Equal(`The workload got reservation on "worker1"`))
+				ok, err := utiltesting.HasEventAppeared(managerTestCluster.ctx, managerTestCluster.client, corev1.Event{
+					Reason:  "MultiKueue",
+					Type:    corev1.EventTypeNormal,
+					Message: `The workload got reservation on "worker1"`,
+				})
+				g.Expect(err).NotTo(gomega.HaveOccurred())
+				g.Expect(ok).To(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, wlLookupKey, createdWorkload)).To(utiltesting.BeNotFoundError())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("preempting workload in worker1", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdWorkload := &kueue.Workload{}
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(workload.SetConditionAndUpdate(worker1TestCluster.ctx, worker1TestCluster.client, createdWorkload, kueue.WorkloadEvicted, metav1.ConditionTrue, kueue.WorkloadEvictedByPreemption, "By test", "evict", util.RealClock)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("check manager's workload ClusterName reset", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				managerWl := &kueue.Workload{}
+				g.Expect(managerTestCluster.client.Get(worker1TestCluster.ctx, wlLookupKey, managerWl)).To(gomega.Succeed())
```

  ```suggestion
  				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, managerWl)).To(gomega.Succeed())
  ```
  
  (or are there reasons to use the other context here?)

- **pkg/controller/admissionchecks/multikueue/workload.go:374** by [@olekzabl](https://github.com/olekzabl) — 2026-01-09T21:32:34Z
```diff
@@ -349,22 +349,53 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 4. Handle workload evicted on manager cluster
+	// 4. Handle workload eviction
 	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
-	if remoteEvictCond != nil && remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+	if remoteEvictCond != nil {
 		remoteCl := group.remoteClients[evictedRemote].client
 		remoteWl := group.remotes[evictedRemote]
 
 		log = log.WithValues("remote", evictedRemote, "remoteWorkload", klog.KObj(remoteWl))
 		ctx = ctrl.LoggerInto(ctx, log)
 
-		if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
-			log.Error(err, "Syncing remote controller object")
-			// We'll retry this in the next reconciling.
+		// workload evicted on manager cluster
+		if remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+			if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
+				log.Error(err, "Syncing remote controller object")
+				// We'll retry this in the next reconciling.
+				return reconcile.Result{}, err
+			}
+			return reconcile.Result{}, nil
+		}
+
+		// workload eviction on worker cluster
+		log.V(5).Info("Workload gets evicted in the remote cluster", "cluster", evictedRemote)
+		needsACUpdate := acs.State == kueue.CheckStateReady
+		if err := workload.PatchAdmissionStatus(ctx, w.client, group.local, w.clock, func(wl *kueue.Workload) (bool, error) {
```

  Why would we do it when `needsACUpdate` is false?
  (Currently, the update func does nothing in this case, but it still returns `true` - looks like we'd send an empty patch request to the apiserver?)

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-01-12T13:00:40Z

- **pkg/controller/admissionchecks/multikueue/workload.go:381** by [@mszadkow](https://github.com/mszadkow) — 2026-01-12T13:00:39Z
```diff
@@ -349,22 +349,53 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 4. Handle workload evicted on manager cluster
+	// 4. Handle workload eviction
 	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
-	if remoteEvictCond != nil && remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+	if remoteEvictCond != nil {
 		remoteCl := group.remoteClients[evictedRemote].client
 		remoteWl := group.remotes[evictedRemote]
 
 		log = log.WithValues("remote", evictedRemote, "remoteWorkload", klog.KObj(remoteWl))
 		ctx = ctrl.LoggerInto(ctx, log)
 
-		if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
-			log.Error(err, "Syncing remote controller object")
-			// We'll retry this in the next reconciling.
+		// workload evicted on manager cluster
+		if remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+			if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
+				log.Error(err, "Syncing remote controller object")
+				// We'll retry this in the next reconciling.
+				return reconcile.Result{}, err
+			}
+			return reconcile.Result{}, nil
+		}
+
+		// workload eviction on worker cluster
+		log.V(5).Info("Workload gets evicted in the remote cluster", "cluster", evictedRemote)
+		needsACUpdate := acs.State == kueue.CheckStateReady
+		if err := workload.PatchAdmissionStatus(ctx, w.client, group.local, w.clock, func(wl *kueue.Workload) (bool, error) {
+			if needsACUpdate {
+				acs.State = kueue.CheckStatePending
+				acs.Message = fmt.Sprintf("Workload evicted on worker cluster: %q, resetting for re-admission", *group.local.Status.ClusterName)
+				acs.LastTransitionTime = metav1.NewTime(w.clock.Now())
+				workload.SetAdmissionCheckState(&wl.Status.AdmissionChecks, *acs, w.clock)
+				wl.Status.ClusterName = nil
+				wl.Status.NominatedClusterNames = nil
```

  That's basically what we do when we evict using `resetClusterNomination`, but if that was empty already no need to do that.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-01-12T13:08:05Z

- **pkg/controller/admissionchecks/multikueue/workload.go:394** by [@mszadkow](https://github.com/mszadkow) — 2026-01-12T13:08:05Z
```diff
@@ -349,22 +349,53 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 4. Handle workload evicted on manager cluster
+	// 4. Handle workload eviction
 	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
-	if remoteEvictCond != nil && remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+	if remoteEvictCond != nil {
 		remoteCl := group.remoteClients[evictedRemote].client
 		remoteWl := group.remotes[evictedRemote]
 
 		log = log.WithValues("remote", evictedRemote, "remoteWorkload", klog.KObj(remoteWl))
 		ctx = ctrl.LoggerInto(ctx, log)
 
-		if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
-			log.Error(err, "Syncing remote controller object")
-			// We'll retry this in the next reconciling.
+		// workload evicted on manager cluster
+		if remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+			if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
+				log.Error(err, "Syncing remote controller object")
+				// We'll retry this in the next reconciling.
+				return reconcile.Result{}, err
+			}
+			return reconcile.Result{}, nil
+		}
+
+		// workload eviction on worker cluster
+		log.V(5).Info("Workload gets evicted in the remote cluster", "cluster", evictedRemote)
+		needsACUpdate := acs.State == kueue.CheckStateReady
+		if err := workload.PatchAdmissionStatus(ctx, w.client, group.local, w.clock, func(wl *kueue.Workload) (bool, error) {
+			if needsACUpdate {
+				acs.State = kueue.CheckStatePending
+				acs.Message = fmt.Sprintf("Workload evicted on worker cluster: %q, resetting for re-admission", *group.local.Status.ClusterName)
+				acs.LastTransitionTime = metav1.NewTime(w.clock.Now())
+				workload.SetAdmissionCheckState(&wl.Status.AdmissionChecks, *acs, w.clock)
+				wl.Status.ClusterName = nil
+				wl.Status.NominatedClusterNames = nil
+			}
+			return true, nil
+		}); err != nil {
+			log.Error(err, "Failed to patch workload status")
 			return reconcile.Result{}, err
 		}
 
-		// Wait for QuotaReserved=false in the local job.
+		if needsACUpdate {
+			w.recorder.Eventf(group.local, corev1.EventTypeNormal, "MultiKueue", acs.Message)
+		}
+
+		for cluster := range group.remotes {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, cluster)); err != nil {
```

  I see we are not that consistent with it, but sure let's add it

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-01-12T13:10:08Z

- **test/e2e/multikueue/e2e_test.go:629** by [@mszadkow](https://github.com/mszadkow) — 2026-01-12T13:10:08Z
```diff
@@ -603,6 +604,93 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 			})
 		})
 
+		ginkgo.It("Should re-do admission process when workload gets evicted in the worker", func() {
+			job := testingjob.MakeJob("job", managerNs.Name).
+				WorkloadPriorityClass(managerLowWPC.Name).
+				Queue(kueue.LocalQueueName(managerLq.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "0.9").
+				RequestAndLimit(corev1.ResourceMemory, "0.5G").
+				Obj()
+			util.MustCreate(ctx, k8sManagerClient, job)
+
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: managerNs.Name}
+			managerWl := &kueue.Workload{}
+			workerWorkload := &kueue.Workload{}
+
+			ginkgo.By("Checking that the workload is created and admitted in the manager cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, wlKey, managerWl)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(managerWl)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdAtWorker := ""
+
+			ginkgo.By("Checking that the workload is created in one of the workers", func() {
```

  Yes, that's sounds great, I will definitively go for that.
  I was out of ideas how to get rid of this "if madness", somehow forgot about using CQ once more.
  thank you!

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-01-12T13:17:26Z

- **test/integration/multikueue/jobs_test.go:1950** by [@mszadkow](https://github.com/mszadkow) — 2026-01-12T13:17:26Z
```diff
@@ -1885,6 +1886,91 @@ var _ = ginkgo.Describe("MultiKueue", ginkgo.Label("area:multikueue", "feature:m
 			}, gomega.Equal(completedJobCondition))))
 		})
 	})
+	ginkgo.It("Should redo the admission process once the workload looses Admission in the worker cluster", func() {
+		job := testingjob.MakeJob("job", managerNs.Name).
+			ManagedBy(kueue.MultiKueueControllerName).
+			Queue(kueue.LocalQueueName(managerLq.Name)).
+			Obj()
+		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, job)
+
+		createdWorkload := &kueue.Workload{}
+		wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: managerNs.Name}
+
+		ginkgo.By("setting workload reservation in the management cluster", func() {
+			admission := utiltestingapi.MakeAdmission(managerCq.Name).Obj()
+			util.SetQuotaReservation(managerTestCluster.ctx, managerTestCluster.client, wlLookupKey, admission)
+		})
+
+		ginkgo.By("checking the workload creation in the worker clusters", func() {
+			managerWl := &kueue.Workload{}
+			gomega.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, managerWl)).To(gomega.Succeed())
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(createdWorkload.Spec).To(gomega.BeComparableTo(managerWl.Spec))
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(createdWorkload.Spec).To(gomega.BeComparableTo(managerWl.Spec))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("setting workload reservation in worker1, AC state is updated in manager and worker2 wl is removed", func() {
+			admission := utiltestingapi.MakeAdmission(managerCq.Name).Obj()
+			util.SetQuotaReservation(worker1TestCluster.ctx, worker1TestCluster.client, wlLookupKey, admission)
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				acs := admissioncheck.FindAdmissionCheck(createdWorkload.Status.AdmissionChecks, kueue.AdmissionCheckReference(multiKueueAC.Name))
+				g.Expect(acs).NotTo(gomega.BeNil())
+				g.Expect(acs.State).To(gomega.Equal(kueue.CheckStateReady))
+				g.Expect(acs.Message).To(gomega.Equal(`The workload got reservation on "worker1"`))
+				ok, err := utiltesting.HasEventAppeared(managerTestCluster.ctx, managerTestCluster.client, corev1.Event{
+					Reason:  "MultiKueue",
+					Type:    corev1.EventTypeNormal,
+					Message: `The workload got reservation on "worker1"`,
+				})
+				g.Expect(err).NotTo(gomega.HaveOccurred())
+				g.Expect(ok).To(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, wlLookupKey, createdWorkload)).To(utiltesting.BeNotFoundError())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("preempting workload in worker1", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdWorkload := &kueue.Workload{}
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(workload.SetConditionAndUpdate(worker1TestCluster.ctx, worker1TestCluster.client, createdWorkload, kueue.WorkloadEvicted, metav1.ConditionTrue, kueue.WorkloadEvictedByPreemption, "By test", "evict", util.RealClock)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("check manager's workload ClusterName reset", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				managerWl := &kueue.Workload{}
+				g.Expect(managerTestCluster.client.Get(worker1TestCluster.ctx, wlLookupKey, managerWl)).To(gomega.Succeed())
```

  Copy-Paste syndrome ;)

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-01-12T13:22:36Z

- **pkg/controller/admissionchecks/multikueue/workload.go:374** by [@mszadkow](https://github.com/mszadkow) — 2026-01-12T13:22:36Z
```diff
@@ -349,22 +349,53 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 4. Handle workload evicted on manager cluster
+	// 4. Handle workload eviction
 	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
-	if remoteEvictCond != nil && remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+	if remoteEvictCond != nil {
 		remoteCl := group.remoteClients[evictedRemote].client
 		remoteWl := group.remotes[evictedRemote]
 
 		log = log.WithValues("remote", evictedRemote, "remoteWorkload", klog.KObj(remoteWl))
 		ctx = ctrl.LoggerInto(ctx, log)
 
-		if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
-			log.Error(err, "Syncing remote controller object")
-			// We'll retry this in the next reconciling.
+		// workload evicted on manager cluster
+		if remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+			if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
+				log.Error(err, "Syncing remote controller object")
+				// We'll retry this in the next reconciling.
+				return reconcile.Result{}, err
+			}
+			return reconcile.Result{}, nil
+		}
+
+		// workload eviction on worker cluster
+		log.V(5).Info("Workload gets evicted in the remote cluster", "cluster", evictedRemote)
+		needsACUpdate := acs.State == kueue.CheckStateReady
+		if err := workload.PatchAdmissionStatus(ctx, w.client, group.local, w.clock, func(wl *kueue.Workload) (bool, error) {
```

  Yes, I see what you mean.
  Actually moving needsACUpdate as update return val makes more sense

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-01-12T13:30:44Z

- **test/e2e/multikueue/e2e_test.go:677** by [@mszadkow](https://github.com/mszadkow) — 2026-01-12T13:30:44Z
```diff
@@ -603,6 +604,93 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 			})
 		})
 
+		ginkgo.It("Should re-do admission process when workload gets evicted in the worker", func() {
+			job := testingjob.MakeJob("job", managerNs.Name).
+				WorkloadPriorityClass(managerLowWPC.Name).
+				Queue(kueue.LocalQueueName(managerLq.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "0.9").
+				RequestAndLimit(corev1.ResourceMemory, "0.5G").
+				Obj()
+			util.MustCreate(ctx, k8sManagerClient, job)
+
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: managerNs.Name}
+			managerWl := &kueue.Workload{}
+			workerWorkload := &kueue.Workload{}
+
+			ginkgo.By("Checking that the workload is created and admitted in the manager cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, wlKey, managerWl)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(managerWl)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdAtWorker := ""
+
+			ginkgo.By("Checking that the workload is created in one of the workers", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					if err := k8sWorker1Client.Get(ctx, wlKey, workerWorkload); err == nil {
+						createdAtWorker = "worker1"
+					} else {
+						g.Expect(k8sWorker2Client.Get(ctx, wlKey, workerWorkload)).To(gomega.Succeed())
+						createdAtWorker = "worker2"
+					}
+					g.Expect(workload.IsAdmitted(workerWorkload)).To(gomega.BeTrue())
+					g.Expect(workerWorkload.Spec).To(gomega.BeComparableTo(managerWl.Spec))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.GinkgoLogr.Info("Workload created at", "cluster", createdAtWorker)
+
+			ginkgo.By("Modifying worker cluster queue to not have enough resources", func() {
+				if createdAtWorker == "worker1" {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sWorker1Client.Get(ctx, client.ObjectKeyFromObject(worker1Cq), worker1Cq)).To(gomega.Succeed())
+						worker1Cq.Spec.ResourceGroups[0].Flavors[0].Resources[0].NominalQuota = resource.MustParse("0.5")
+						g.Expect(k8sWorker1Client.Update(ctx, worker1Cq)).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				} else {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sWorker2Client.Get(ctx, client.ObjectKeyFromObject(worker2Cq), worker2Cq)).To(gomega.Succeed())
+						worker2Cq.Spec.ResourceGroups[0].Flavors[0].Resources[0].NominalQuota = resource.MustParse("0.5")
+						g.Expect(k8sWorker2Client.Update(ctx, worker2Cq)).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Triggering eviction in worker", func() {
+				if createdAtWorker == "worker1" {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sWorker1Client.Get(ctx, wlKey, workerWorkload)).To(gomega.Succeed())
+						g.Expect(workload.SetConditionAndUpdate(ctx, k8sWorker1Client, workerWorkload, kueue.WorkloadEvicted, metav1.ConditionTrue, kueue.WorkloadEvictedByPreemption, "By test", "evict", util.RealClock)).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				} else {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sWorker2Client.Get(ctx, wlKey, workerWorkload)).To(gomega.Succeed())
+						g.Expect(workload.SetConditionAndUpdate(ctx, k8sWorker2Client, workerWorkload, kueue.WorkloadEvicted, metav1.ConditionTrue, kueue.WorkloadEvictedByPreemption, "By test", "evict", util.RealClock)).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Checking that the workload is re-admitted in the other worker cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, wlKey, managerWl)).To(gomega.Succeed())
+					g.Expect(managerWl.Status.ClusterName).NotTo(gomega.HaveValue(gomega.Equal(createdAtWorker)))
```

  Yes, but I want to check if it's given value, not any value

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-01-12T13:43:55Z

- **test/e2e/multikueue/e2e_test.go:677** by [@olekzabl](https://github.com/olekzabl) — 2026-01-12T13:43:55Z
```diff
@@ -603,6 +604,93 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 			})
 		})
 
+		ginkgo.It("Should re-do admission process when workload gets evicted in the worker", func() {
+			job := testingjob.MakeJob("job", managerNs.Name).
+				WorkloadPriorityClass(managerLowWPC.Name).
+				Queue(kueue.LocalQueueName(managerLq.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "0.9").
+				RequestAndLimit(corev1.ResourceMemory, "0.5G").
+				Obj()
+			util.MustCreate(ctx, k8sManagerClient, job)
+
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: managerNs.Name}
+			managerWl := &kueue.Workload{}
+			workerWorkload := &kueue.Workload{}
+
+			ginkgo.By("Checking that the workload is created and admitted in the manager cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, wlKey, managerWl)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(managerWl)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdAtWorker := ""
+
+			ginkgo.By("Checking that the workload is created in one of the workers", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					if err := k8sWorker1Client.Get(ctx, wlKey, workerWorkload); err == nil {
+						createdAtWorker = "worker1"
+					} else {
+						g.Expect(k8sWorker2Client.Get(ctx, wlKey, workerWorkload)).To(gomega.Succeed())
+						createdAtWorker = "worker2"
+					}
+					g.Expect(workload.IsAdmitted(workerWorkload)).To(gomega.BeTrue())
+					g.Expect(workerWorkload.Spec).To(gomega.BeComparableTo(managerWl.Spec))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.GinkgoLogr.Info("Workload created at", "cluster", createdAtWorker)
+
+			ginkgo.By("Modifying worker cluster queue to not have enough resources", func() {
+				if createdAtWorker == "worker1" {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sWorker1Client.Get(ctx, client.ObjectKeyFromObject(worker1Cq), worker1Cq)).To(gomega.Succeed())
+						worker1Cq.Spec.ResourceGroups[0].Flavors[0].Resources[0].NominalQuota = resource.MustParse("0.5")
+						g.Expect(k8sWorker1Client.Update(ctx, worker1Cq)).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				} else {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sWorker2Client.Get(ctx, client.ObjectKeyFromObject(worker2Cq), worker2Cq)).To(gomega.Succeed())
+						worker2Cq.Spec.ResourceGroups[0].Flavors[0].Resources[0].NominalQuota = resource.MustParse("0.5")
+						g.Expect(k8sWorker2Client.Update(ctx, worker2Cq)).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Triggering eviction in worker", func() {
+				if createdAtWorker == "worker1" {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sWorker1Client.Get(ctx, wlKey, workerWorkload)).To(gomega.Succeed())
+						g.Expect(workload.SetConditionAndUpdate(ctx, k8sWorker1Client, workerWorkload, kueue.WorkloadEvicted, metav1.ConditionTrue, kueue.WorkloadEvictedByPreemption, "By test", "evict", util.RealClock)).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				} else {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sWorker2Client.Get(ctx, wlKey, workerWorkload)).To(gomega.Succeed())
+						g.Expect(workload.SetConditionAndUpdate(ctx, k8sWorker2Client, workerWorkload, kueue.WorkloadEvicted, metav1.ConditionTrue, kueue.WorkloadEvictedByPreemption, "By test", "evict", util.RealClock)).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Checking that the workload is re-admitted in the other worker cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, wlKey, managerWl)).To(gomega.Succeed())
+					g.Expect(managerWl.Status.ClusterName).NotTo(gomega.HaveValue(gomega.Equal(createdAtWorker)))
```

  Oh, I meant: check that it's distinct from `createdAtWorker` (as currently), _and_ that it's not empty.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-01-12T13:59:20Z

- **pkg/controller/admissionchecks/multikueue/workload.go:374** by [@olekzabl](https://github.com/olekzabl) — 2026-01-12T13:57:30Z
```diff
@@ -349,22 +349,53 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 4. Handle workload evicted on manager cluster
+	// 4. Handle workload eviction
 	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
-	if remoteEvictCond != nil && remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+	if remoteEvictCond != nil {
 		remoteCl := group.remoteClients[evictedRemote].client
 		remoteWl := group.remotes[evictedRemote]
 
 		log = log.WithValues("remote", evictedRemote, "remoteWorkload", klog.KObj(remoteWl))
 		ctx = ctrl.LoggerInto(ctx, log)
 
-		if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
-			log.Error(err, "Syncing remote controller object")
-			// We'll retry this in the next reconciling.
+		// workload evicted on manager cluster
+		if remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+			if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
+				log.Error(err, "Syncing remote controller object")
+				// We'll retry this in the next reconciling.
+				return reconcile.Result{}, err
+			}
+			return reconcile.Result{}, nil
+		}
+
+		// workload eviction on worker cluster
+		log.V(5).Info("Workload gets evicted in the remote cluster", "cluster", evictedRemote)
+		needsACUpdate := acs.State == kueue.CheckStateReady
+		if err := workload.PatchAdmissionStatus(ctx, w.client, group.local, w.clock, func(wl *kueue.Workload) (bool, error) {
```

  I think it'd be still a bit clearer like this:
  ```go
  needsACUpdate := ...
  if needsACUpdate {
    if err := workload.PatchAdmissionStatus(..., func(wl *kueue.Workload) (bool, error) {
      ...
      return true, nil
    }); err != nil {
      ...
    }
  }
  ```
  
  Because in the current version, when `needsACUpdate` is false, we still enter the function, make some changes to `wl`, possibly non-trivial ones (e.g. I imagine we might be changing its status message), and then suddenly "give up" with `return needsACUpdate, nil`. The effect is as intended (no-op), but the way to it is a bit confusing.
  
  WDYT?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-01-12T15:07:01Z

- **pkg/controller/admissionchecks/multikueue/workload.go:374** by [@mszadkow](https://github.com/mszadkow) — 2026-01-12T15:07:01Z
```diff
@@ -349,22 +349,53 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 4. Handle workload evicted on manager cluster
+	// 4. Handle workload eviction
 	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
-	if remoteEvictCond != nil && remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+	if remoteEvictCond != nil {
 		remoteCl := group.remoteClients[evictedRemote].client
 		remoteWl := group.remotes[evictedRemote]
 
 		log = log.WithValues("remote", evictedRemote, "remoteWorkload", klog.KObj(remoteWl))
 		ctx = ctrl.LoggerInto(ctx, log)
 
-		if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
-			log.Error(err, "Syncing remote controller object")
-			// We'll retry this in the next reconciling.
+		// workload evicted on manager cluster
+		if remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+			if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
+				log.Error(err, "Syncing remote controller object")
+				// We'll retry this in the next reconciling.
+				return reconcile.Result{}, err
+			}
+			return reconcile.Result{}, nil
+		}
+
+		// workload eviction on worker cluster
+		log.V(5).Info("Workload gets evicted in the remote cluster", "cluster", evictedRemote)
+		needsACUpdate := acs.State == kueue.CheckStateReady
+		if err := workload.PatchAdmissionStatus(ctx, w.client, group.local, w.clock, func(wl *kueue.Workload) (bool, error) {
```

  Right, it might be little bit too complicated, sure.
  Let's clarify this a bit.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-01-12T15:08:16Z

- **test/e2e/multikueue/e2e_test.go:677** by [@mszadkow](https://github.com/mszadkow) — 2026-01-12T15:08:15Z
```diff
@@ -603,6 +604,93 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 			})
 		})
 
+		ginkgo.It("Should re-do admission process when workload gets evicted in the worker", func() {
+			job := testingjob.MakeJob("job", managerNs.Name).
+				WorkloadPriorityClass(managerLowWPC.Name).
+				Queue(kueue.LocalQueueName(managerLq.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "0.9").
+				RequestAndLimit(corev1.ResourceMemory, "0.5G").
+				Obj()
+			util.MustCreate(ctx, k8sManagerClient, job)
+
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: managerNs.Name}
+			managerWl := &kueue.Workload{}
+			workerWorkload := &kueue.Workload{}
+
+			ginkgo.By("Checking that the workload is created and admitted in the manager cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, wlKey, managerWl)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(managerWl)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdAtWorker := ""
+
+			ginkgo.By("Checking that the workload is created in one of the workers", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					if err := k8sWorker1Client.Get(ctx, wlKey, workerWorkload); err == nil {
+						createdAtWorker = "worker1"
+					} else {
+						g.Expect(k8sWorker2Client.Get(ctx, wlKey, workerWorkload)).To(gomega.Succeed())
+						createdAtWorker = "worker2"
+					}
+					g.Expect(workload.IsAdmitted(workerWorkload)).To(gomega.BeTrue())
+					g.Expect(workerWorkload.Spec).To(gomega.BeComparableTo(managerWl.Spec))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.GinkgoLogr.Info("Workload created at", "cluster", createdAtWorker)
+
+			ginkgo.By("Modifying worker cluster queue to not have enough resources", func() {
+				if createdAtWorker == "worker1" {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sWorker1Client.Get(ctx, client.ObjectKeyFromObject(worker1Cq), worker1Cq)).To(gomega.Succeed())
+						worker1Cq.Spec.ResourceGroups[0].Flavors[0].Resources[0].NominalQuota = resource.MustParse("0.5")
+						g.Expect(k8sWorker1Client.Update(ctx, worker1Cq)).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				} else {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sWorker2Client.Get(ctx, client.ObjectKeyFromObject(worker2Cq), worker2Cq)).To(gomega.Succeed())
+						worker2Cq.Spec.ResourceGroups[0].Flavors[0].Resources[0].NominalQuota = resource.MustParse("0.5")
+						g.Expect(k8sWorker2Client.Update(ctx, worker2Cq)).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Triggering eviction in worker", func() {
+				if createdAtWorker == "worker1" {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sWorker1Client.Get(ctx, wlKey, workerWorkload)).To(gomega.Succeed())
+						g.Expect(workload.SetConditionAndUpdate(ctx, k8sWorker1Client, workerWorkload, kueue.WorkloadEvicted, metav1.ConditionTrue, kueue.WorkloadEvictedByPreemption, "By test", "evict", util.RealClock)).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				} else {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sWorker2Client.Get(ctx, wlKey, workerWorkload)).To(gomega.Succeed())
+						g.Expect(workload.SetConditionAndUpdate(ctx, k8sWorker2Client, workerWorkload, kueue.WorkloadEvicted, metav1.ConditionTrue, kueue.WorkloadEvictedByPreemption, "By test", "evict", util.RealClock)).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Checking that the workload is re-admitted in the other worker cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, wlKey, managerWl)).To(gomega.Succeed())
+					g.Expect(managerWl.Status.ClusterName).NotTo(gomega.HaveValue(gomega.Equal(createdAtWorker)))
```

  `createdAtWorker` was useful in previous version, I have removed it

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-01-12T20:20:42Z

- **test/e2e/multikueue/e2e_test.go:677** by [@olekzabl](https://github.com/olekzabl) — 2026-01-12T20:20:42Z
```diff
@@ -603,6 +604,93 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 			})
 		})
 
+		ginkgo.It("Should re-do admission process when workload gets evicted in the worker", func() {
+			job := testingjob.MakeJob("job", managerNs.Name).
+				WorkloadPriorityClass(managerLowWPC.Name).
+				Queue(kueue.LocalQueueName(managerLq.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "0.9").
+				RequestAndLimit(corev1.ResourceMemory, "0.5G").
+				Obj()
+			util.MustCreate(ctx, k8sManagerClient, job)
+
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: managerNs.Name}
+			managerWl := &kueue.Workload{}
+			workerWorkload := &kueue.Workload{}
+
+			ginkgo.By("Checking that the workload is created and admitted in the manager cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, wlKey, managerWl)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(managerWl)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdAtWorker := ""
+
+			ginkgo.By("Checking that the workload is created in one of the workers", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					if err := k8sWorker1Client.Get(ctx, wlKey, workerWorkload); err == nil {
+						createdAtWorker = "worker1"
+					} else {
+						g.Expect(k8sWorker2Client.Get(ctx, wlKey, workerWorkload)).To(gomega.Succeed())
+						createdAtWorker = "worker2"
+					}
+					g.Expect(workload.IsAdmitted(workerWorkload)).To(gomega.BeTrue())
+					g.Expect(workerWorkload.Spec).To(gomega.BeComparableTo(managerWl.Spec))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.GinkgoLogr.Info("Workload created at", "cluster", createdAtWorker)
+
+			ginkgo.By("Modifying worker cluster queue to not have enough resources", func() {
+				if createdAtWorker == "worker1" {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sWorker1Client.Get(ctx, client.ObjectKeyFromObject(worker1Cq), worker1Cq)).To(gomega.Succeed())
+						worker1Cq.Spec.ResourceGroups[0].Flavors[0].Resources[0].NominalQuota = resource.MustParse("0.5")
+						g.Expect(k8sWorker1Client.Update(ctx, worker1Cq)).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				} else {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sWorker2Client.Get(ctx, client.ObjectKeyFromObject(worker2Cq), worker2Cq)).To(gomega.Succeed())
+						worker2Cq.Spec.ResourceGroups[0].Flavors[0].Resources[0].NominalQuota = resource.MustParse("0.5")
+						g.Expect(k8sWorker2Client.Update(ctx, worker2Cq)).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Triggering eviction in worker", func() {
+				if createdAtWorker == "worker1" {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sWorker1Client.Get(ctx, wlKey, workerWorkload)).To(gomega.Succeed())
+						g.Expect(workload.SetConditionAndUpdate(ctx, k8sWorker1Client, workerWorkload, kueue.WorkloadEvicted, metav1.ConditionTrue, kueue.WorkloadEvictedByPreemption, "By test", "evict", util.RealClock)).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				} else {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sWorker2Client.Get(ctx, wlKey, workerWorkload)).To(gomega.Succeed())
+						g.Expect(workload.SetConditionAndUpdate(ctx, k8sWorker2Client, workerWorkload, kueue.WorkloadEvicted, metav1.ConditionTrue, kueue.WorkloadEvictedByPreemption, "By test", "evict", util.RealClock)).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Checking that the workload is re-admitted in the other worker cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, wlKey, managerWl)).To(gomega.Succeed())
+					g.Expect(managerWl.Status.ClusterName).NotTo(gomega.HaveValue(gomega.Equal(createdAtWorker)))
```

  Oh, ok.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-01-12T20:26:25Z

- **test/e2e/multikueue/e2e_test.go:661** by [@olekzabl](https://github.com/olekzabl) — 2026-01-12T20:24:25Z
```diff
@@ -603,6 +604,75 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 			})
 		})
 
+		ginkgo.It("Should re-do admission process when workload gets evicted in the worker", func() {
+			job := testingjob.MakeJob("job", managerNs.Name).
+				WorkloadPriorityClass(managerLowWPC.Name).
+				Queue(kueue.LocalQueueName(managerLq.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "1.5").
+				RequestAndLimit(corev1.ResourceMemory, "0.5G").
+				Obj()
+			util.MustCreate(ctx, k8sManagerClient, job)
+
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: managerNs.Name}
+			managerWl := &kueue.Workload{}
+			workerWorkload := &kueue.Workload{}
+
+			ginkgo.By("Checking that the workload is created and admitted in the manager cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, wlKey, managerWl)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(managerWl)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the workload is created on worker1", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sWorker1Client.Get(ctx, wlKey, workerWorkload)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(workerWorkload)).To(gomega.BeTrue())
+					g.Expect(workerWorkload.Spec).To(gomega.BeComparableTo(managerWl.Spec))
+
+					g.Expect(k8sWorker2Client.Get(ctx, wlKey, workerWorkload)).To(utiltesting.BeNotFoundError())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Switching worker cluster queues' resources to enforce re-admission on the worker2", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sWorker1Client.Get(ctx, client.ObjectKeyFromObject(worker1Cq), worker1Cq)).To(gomega.Succeed())
+					worker1Cq.Spec.ResourceGroups[0].Flavors[0].Resources[0].NominalQuota = resource.MustParse("1")
+					g.Expect(k8sWorker1Client.Update(ctx, worker1Cq)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sWorker2Client.Get(ctx, client.ObjectKeyFromObject(worker2Cq), worker2Cq)).To(gomega.Succeed())
+					worker2Cq.Spec.ResourceGroups[0].Flavors[0].Resources[0].NominalQuota = resource.MustParse("2")
+					g.Expect(k8sWorker2Client.Update(ctx, worker2Cq)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Triggering eviction in worker1", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sWorker1Client.Get(ctx, wlKey, workerWorkload)).To(gomega.Succeed())
+					g.Expect(workload.SetConditionAndUpdate(ctx, k8sWorker1Client, workerWorkload, kueue.WorkloadEvicted, metav1.ConditionTrue, kueue.WorkloadEvictedByPreemption, "By test", "evict", util.RealClock)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the workload is re-admitted in worker2", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, wlKey, managerWl)).To(gomega.Succeed())
+					g.Expect(managerWl.Status.ClusterName).NotTo(gomega.HaveValue(gomega.Equal("worker2")))
```

  ```suggestion
  					g.Expect(managerWl.Status.ClusterName).To(gomega.HaveValue(gomega.Equal("worker2")))
  ```
  
  (We want it to equal `worker2`, right? And I guess the test currently passes because the current expectation is satisfied _before_ the re-admission happens).

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-01-13T13:44:05Z

- **pkg/controller/admissionchecks/multikueue/workload.go:394** by [@mszadkow](https://github.com/mszadkow) — 2026-01-13T13:44:05Z
```diff
@@ -349,22 +349,53 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 4. Handle workload evicted on manager cluster
+	// 4. Handle workload eviction
 	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
-	if remoteEvictCond != nil && remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+	if remoteEvictCond != nil {
 		remoteCl := group.remoteClients[evictedRemote].client
 		remoteWl := group.remotes[evictedRemote]
 
 		log = log.WithValues("remote", evictedRemote, "remoteWorkload", klog.KObj(remoteWl))
 		ctx = ctrl.LoggerInto(ctx, log)
 
-		if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
-			log.Error(err, "Syncing remote controller object")
-			// We'll retry this in the next reconciling.
+		// workload evicted on manager cluster
+		if remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+			if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
+				log.Error(err, "Syncing remote controller object")
+				// We'll retry this in the next reconciling.
+				return reconcile.Result{}, err
+			}
+			return reconcile.Result{}, nil
+		}
+
+		// workload eviction on worker cluster
+		log.V(5).Info("Workload gets evicted in the remote cluster", "cluster", evictedRemote)
+		needsACUpdate := acs.State == kueue.CheckStateReady
+		if err := workload.PatchAdmissionStatus(ctx, w.client, group.local, w.clock, func(wl *kueue.Workload) (bool, error) {
+			if needsACUpdate {
+				acs.State = kueue.CheckStatePending
+				acs.Message = fmt.Sprintf("Workload evicted on worker cluster: %q, resetting for re-admission", *group.local.Status.ClusterName)
+				acs.LastTransitionTime = metav1.NewTime(w.clock.Now())
+				workload.SetAdmissionCheckState(&wl.Status.AdmissionChecks, *acs, w.clock)
+				wl.Status.ClusterName = nil
+				wl.Status.NominatedClusterNames = nil
+			}
+			return true, nil
+		}); err != nil {
+			log.Error(err, "Failed to patch workload status")
 			return reconcile.Result{}, err
 		}
 
-		// Wait for QuotaReserved=false in the local job.
+		if needsACUpdate {
+			w.recorder.Eventf(group.local, corev1.EventTypeNormal, "MultiKueue", acs.Message)
+		}
+
+		for cluster := range group.remotes {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, cluster)); err != nil {
```

  Added.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-13T15:47:17Z

- **pkg/controller/admissionchecks/multikueue/workload.go:372** by [@mimowo](https://github.com/mimowo) — 2026-01-13T15:47:18Z
```diff
@@ -349,22 +349,53 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 4. Handle workload evicted on manager cluster
+	// 4. Handle workload eviction
 	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
-	if remoteEvictCond != nil && remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+	if remoteEvictCond != nil {
 		remoteCl := group.remoteClients[evictedRemote].client
 		remoteWl := group.remotes[evictedRemote]
 
 		log = log.WithValues("remote", evictedRemote, "remoteWorkload", klog.KObj(remoteWl))
 		ctx = ctrl.LoggerInto(ctx, log)
 
-		if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
-			log.Error(err, "Syncing remote controller object")
-			// We'll retry this in the next reconciling.
-			return reconcile.Result{}, err
+		// workload evicted on manager cluster
+		if remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+			if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
+				log.Error(err, "Syncing remote controller object")
+				// We'll retry this in the next reconciling.
+				return reconcile.Result{}, err
+			}
+			return reconcile.Result{}, nil
 		}
 
-		// Wait for QuotaReserved=false in the local job.
+		// workload evicted on worker cluster
+		log.V(5).Info("Workload was evicted in the remote cluster", "cluster", evictedRemote)
```

  ```suggestion
  		log.V(3).Info("Workload was evicted in the remote cluster", "cluster", evictedRemote)
  ```
  This is quite a significant event, let's increase verbosity

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-13T15:52:58Z

- **pkg/controller/admissionchecks/multikueue/workload.go:376** by [@mimowo](https://github.com/mimowo) — 2026-01-13T15:52:58Z
```diff
@@ -349,22 +349,53 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 4. Handle workload evicted on manager cluster
+	// 4. Handle workload eviction
 	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
-	if remoteEvictCond != nil && remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+	if remoteEvictCond != nil {
 		remoteCl := group.remoteClients[evictedRemote].client
 		remoteWl := group.remotes[evictedRemote]
 
 		log = log.WithValues("remote", evictedRemote, "remoteWorkload", klog.KObj(remoteWl))
 		ctx = ctrl.LoggerInto(ctx, log)
 
-		if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
-			log.Error(err, "Syncing remote controller object")
-			// We'll retry this in the next reconciling.
-			return reconcile.Result{}, err
+		// workload evicted on manager cluster
+		if remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+			if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
+				log.Error(err, "Syncing remote controller object")
+				// We'll retry this in the next reconciling.
+				return reconcile.Result{}, err
+			}
+			return reconcile.Result{}, nil
 		}
 
-		// Wait for QuotaReserved=false in the local job.
+		// workload evicted on worker cluster
+		log.V(5).Info("Workload was evicted in the remote cluster", "cluster", evictedRemote)
+		needsACUpdate := acs.State == kueue.CheckStateReady
+		if needsACUpdate {
+			if err := workload.PatchAdmissionStatus(ctx, w.client, group.local, w.clock, func(wl *kueue.Workload) (bool, error) {
+				acs.State = kueue.CheckStatePending
```

  Shouldn't we set retry here, and let the regular logic handle cleaning of the ClusterName and NominatedClusterNames? Maybe there are also other ACs which are already Ready. I think it is safer to go through full retry.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-13T15:53:34Z

- **pkg/controller/admissionchecks/multikueue/workload.go:392** by [@mimowo](https://github.com/mimowo) — 2026-01-13T15:53:34Z
```diff
@@ -349,22 +349,53 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 4. Handle workload evicted on manager cluster
+	// 4. Handle workload eviction
 	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
-	if remoteEvictCond != nil && remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+	if remoteEvictCond != nil {
 		remoteCl := group.remoteClients[evictedRemote].client
 		remoteWl := group.remotes[evictedRemote]
 
 		log = log.WithValues("remote", evictedRemote, "remoteWorkload", klog.KObj(remoteWl))
 		ctx = ctrl.LoggerInto(ctx, log)
 
-		if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
-			log.Error(err, "Syncing remote controller object")
-			// We'll retry this in the next reconciling.
-			return reconcile.Result{}, err
+		// workload evicted on manager cluster
+		if remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+			if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
+				log.Error(err, "Syncing remote controller object")
+				// We'll retry this in the next reconciling.
+				return reconcile.Result{}, err
+			}
+			return reconcile.Result{}, nil
 		}
 
-		// Wait for QuotaReserved=false in the local job.
+		// workload evicted on worker cluster
+		log.V(5).Info("Workload was evicted in the remote cluster", "cluster", evictedRemote)
+		needsACUpdate := acs.State == kueue.CheckStateReady
+		if needsACUpdate {
+			if err := workload.PatchAdmissionStatus(ctx, w.client, group.local, w.clock, func(wl *kueue.Workload) (bool, error) {
+				acs.State = kueue.CheckStatePending
+				acs.Message = fmt.Sprintf("Workload evicted on worker cluster: %q, resetting for re-admission", *group.local.Status.ClusterName)
+				acs.LastTransitionTime = metav1.NewTime(w.clock.Now())
+				workload.SetAdmissionCheckState(&wl.Status.AdmissionChecks, *acs, w.clock)
+				wl.Status.ClusterName = nil
+				wl.Status.NominatedClusterNames = nil
+
+				return true, nil
+			}); err != nil {
+				log.Error(err, "Failed to patch workload status")
+				return reconcile.Result{}, err
+			}
+
+			w.recorder.Eventf(group.local, corev1.EventTypeNormal, "MultiKueue", acs.Message)
+		}
+
+		for cluster := range group.remotes {
```

  Wouldn't this be handled by pre-existing code if we set the AC state as "Retry"?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-13T15:54:31Z

- **pkg/controller/admissionchecks/multikueue/workload.go:376** by [@mimowo](https://github.com/mimowo) — 2026-01-13T15:54:31Z
```diff
@@ -349,22 +349,53 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 4. Handle workload evicted on manager cluster
+	// 4. Handle workload eviction
 	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
-	if remoteEvictCond != nil && remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+	if remoteEvictCond != nil {
 		remoteCl := group.remoteClients[evictedRemote].client
 		remoteWl := group.remotes[evictedRemote]
 
 		log = log.WithValues("remote", evictedRemote, "remoteWorkload", klog.KObj(remoteWl))
 		ctx = ctrl.LoggerInto(ctx, log)
 
-		if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
-			log.Error(err, "Syncing remote controller object")
-			// We'll retry this in the next reconciling.
-			return reconcile.Result{}, err
+		// workload evicted on manager cluster
+		if remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+			if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
+				log.Error(err, "Syncing remote controller object")
+				// We'll retry this in the next reconciling.
+				return reconcile.Result{}, err
+			}
+			return reconcile.Result{}, nil
 		}
 
-		// Wait for QuotaReserved=false in the local job.
+		// workload evicted on worker cluster
+		log.V(5).Info("Workload was evicted in the remote cluster", "cluster", evictedRemote)
+		needsACUpdate := acs.State == kueue.CheckStateReady
+		if needsACUpdate {
+			if err := workload.PatchAdmissionStatus(ctx, w.client, group.local, w.clock, func(wl *kueue.Workload) (bool, error) {
+				acs.State = kueue.CheckStatePending
```

  Then also the check above `needsACUpdate := acs.State == kueue.CheckStateReady` seems unnecessary

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-13T15:56:59Z

- **pkg/controller/admissionchecks/multikueue/workload.go:375** by [@mimowo](https://github.com/mimowo) — 2026-01-13T15:56:59Z
```diff
@@ -349,22 +349,53 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 4. Handle workload evicted on manager cluster
+	// 4. Handle workload eviction
 	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
-	if remoteEvictCond != nil && remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+	if remoteEvictCond != nil {
 		remoteCl := group.remoteClients[evictedRemote].client
 		remoteWl := group.remotes[evictedRemote]
 
 		log = log.WithValues("remote", evictedRemote, "remoteWorkload", klog.KObj(remoteWl))
 		ctx = ctrl.LoggerInto(ctx, log)
 
-		if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
-			log.Error(err, "Syncing remote controller object")
-			// We'll retry this in the next reconciling.
-			return reconcile.Result{}, err
+		// workload evicted on manager cluster
+		if remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+			if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
+				log.Error(err, "Syncing remote controller object")
+				// We'll retry this in the next reconciling.
+				return reconcile.Result{}, err
+			}
+			return reconcile.Result{}, nil
 		}
 
-		// Wait for QuotaReserved=false in the local job.
+		// workload evicted on worker cluster
+		log.V(5).Info("Workload was evicted in the remote cluster", "cluster", evictedRemote)
+		needsACUpdate := acs.State == kueue.CheckStateReady
+		if needsACUpdate {
+			if err := workload.PatchAdmissionStatus(ctx, w.client, group.local, w.clock, func(wl *kueue.Workload) (bool, error) {
```

  I think this is legitimate bugfix, especially for the ProvReq scenario, but it changes the pre-existing logic, which was underspecified in the KEP. I think it will be safer to have a feature gate like "MultiKueueRedoAdmissionOnEvictionInWorker". This will give us a bailout option in case someone is affected. If noone complains we just graduate in 0.18.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-01-13T19:04:52Z

- **pkg/controller/admissionchecks/multikueue/workload.go:392** by [@mszadkow](https://github.com/mszadkow) — 2026-01-13T19:04:52Z
```diff
@@ -349,22 +349,53 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 4. Handle workload evicted on manager cluster
+	// 4. Handle workload eviction
 	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
-	if remoteEvictCond != nil && remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+	if remoteEvictCond != nil {
 		remoteCl := group.remoteClients[evictedRemote].client
 		remoteWl := group.remotes[evictedRemote]
 
 		log = log.WithValues("remote", evictedRemote, "remoteWorkload", klog.KObj(remoteWl))
 		ctx = ctrl.LoggerInto(ctx, log)
 
-		if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
-			log.Error(err, "Syncing remote controller object")
-			// We'll retry this in the next reconciling.
-			return reconcile.Result{}, err
+		// workload evicted on manager cluster
+		if remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+			if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
+				log.Error(err, "Syncing remote controller object")
+				// We'll retry this in the next reconciling.
+				return reconcile.Result{}, err
+			}
+			return reconcile.Result{}, nil
 		}
 
-		// Wait for QuotaReserved=false in the local job.
+		// workload evicted on worker cluster
+		log.V(5).Info("Workload was evicted in the remote cluster", "cluster", evictedRemote)
+		needsACUpdate := acs.State == kueue.CheckStateReady
+		if needsACUpdate {
+			if err := workload.PatchAdmissionStatus(ctx, w.client, group.local, w.clock, func(wl *kueue.Workload) (bool, error) {
+				acs.State = kueue.CheckStatePending
+				acs.Message = fmt.Sprintf("Workload evicted on worker cluster: %q, resetting for re-admission", *group.local.Status.ClusterName)
+				acs.LastTransitionTime = metav1.NewTime(w.clock.Now())
+				workload.SetAdmissionCheckState(&wl.Status.AdmissionChecks, *acs, w.clock)
+				wl.Status.ClusterName = nil
+				wl.Status.NominatedClusterNames = nil
+
+				return true, nil
+			}); err != nil {
+				log.Error(err, "Failed to patch workload status")
+				return reconcile.Result{}, err
+			}
+
+			w.recorder.Eventf(group.local, corev1.EventTypeNormal, "MultiKueue", acs.Message)
+		}
+
+		for cluster := range group.remotes {
```

  It has the same effect, so let's stick to Retry

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-01-13T19:05:11Z

- **pkg/controller/admissionchecks/multikueue/workload.go:376** by [@mszadkow](https://github.com/mszadkow) — 2026-01-13T19:05:11Z
```diff
@@ -349,22 +349,53 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 4. Handle workload evicted on manager cluster
+	// 4. Handle workload eviction
 	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
-	if remoteEvictCond != nil && remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+	if remoteEvictCond != nil {
 		remoteCl := group.remoteClients[evictedRemote].client
 		remoteWl := group.remotes[evictedRemote]
 
 		log = log.WithValues("remote", evictedRemote, "remoteWorkload", klog.KObj(remoteWl))
 		ctx = ctrl.LoggerInto(ctx, log)
 
-		if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
-			log.Error(err, "Syncing remote controller object")
-			// We'll retry this in the next reconciling.
-			return reconcile.Result{}, err
+		// workload evicted on manager cluster
+		if remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+			if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
+				log.Error(err, "Syncing remote controller object")
+				// We'll retry this in the next reconciling.
+				return reconcile.Result{}, err
+			}
+			return reconcile.Result{}, nil
 		}
 
-		// Wait for QuotaReserved=false in the local job.
+		// workload evicted on worker cluster
+		log.V(5).Info("Workload was evicted in the remote cluster", "cluster", evictedRemote)
+		needsACUpdate := acs.State == kueue.CheckStateReady
+		if needsACUpdate {
+			if err := workload.PatchAdmissionStatus(ctx, w.client, group.local, w.clock, func(wl *kueue.Workload) (bool, error) {
+				acs.State = kueue.CheckStatePending
```

  Yes, Retry works even better, less code

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-13T19:12:19Z

- **pkg/controller/admissionchecks/multikueue/workload.go:392** by [@mimowo](https://github.com/mimowo) — 2026-01-13T19:12:19Z
```diff
@@ -349,22 +349,53 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 4. Handle workload evicted on manager cluster
+	// 4. Handle workload eviction
 	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
-	if remoteEvictCond != nil && remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+	if remoteEvictCond != nil {
 		remoteCl := group.remoteClients[evictedRemote].client
 		remoteWl := group.remotes[evictedRemote]
 
 		log = log.WithValues("remote", evictedRemote, "remoteWorkload", klog.KObj(remoteWl))
 		ctx = ctrl.LoggerInto(ctx, log)
 
-		if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
-			log.Error(err, "Syncing remote controller object")
-			// We'll retry this in the next reconciling.
-			return reconcile.Result{}, err
+		// workload evicted on manager cluster
+		if remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+			if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
+				log.Error(err, "Syncing remote controller object")
+				// We'll retry this in the next reconciling.
+				return reconcile.Result{}, err
+			}
+			return reconcile.Result{}, nil
 		}
 
-		// Wait for QuotaReserved=false in the local job.
+		// workload evicted on worker cluster
+		log.V(5).Info("Workload was evicted in the remote cluster", "cluster", evictedRemote)
+		needsACUpdate := acs.State == kueue.CheckStateReady
+		if needsACUpdate {
+			if err := workload.PatchAdmissionStatus(ctx, w.client, group.local, w.clock, func(wl *kueue.Workload) (bool, error) {
+				acs.State = kueue.CheckStatePending
+				acs.Message = fmt.Sprintf("Workload evicted on worker cluster: %q, resetting for re-admission", *group.local.Status.ClusterName)
+				acs.LastTransitionTime = metav1.NewTime(w.clock.Now())
+				workload.SetAdmissionCheckState(&wl.Status.AdmissionChecks, *acs, w.clock)
+				wl.Status.ClusterName = nil
+				wl.Status.NominatedClusterNames = nil
+
+				return true, nil
+			}); err != nil {
+				log.Error(err, "Failed to patch workload status")
+				return reconcile.Result{}, err
+			}
+
+			w.recorder.Eventf(group.local, corev1.EventTypeNormal, "MultiKueue", acs.Message)
+		}
+
+		for cluster := range group.remotes {
```

  maybe not exactly the same, because this way we will retry other AC too which I think is desired

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-14T07:45:46Z

- **pkg/controller/admissionchecks/multikueue/workload.go:375** by [@mimowo](https://github.com/mimowo) — 2026-01-14T07:45:46Z
```diff
@@ -349,22 +349,47 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 4. Handle workload evicted on manager cluster
+	// 4. Handle workload eviction
 	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
-	if remoteEvictCond != nil && remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+	if remoteEvictCond != nil {
 		remoteCl := group.remoteClients[evictedRemote].client
 		remoteWl := group.remotes[evictedRemote]
 
 		log = log.WithValues("remote", evictedRemote, "remoteWorkload", klog.KObj(remoteWl))
 		ctx = ctrl.LoggerInto(ctx, log)
 
-		if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
-			log.Error(err, "Syncing remote controller object")
-			// We'll retry this in the next reconciling.
+		// workload evicted on manager cluster
+		if remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+			if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
+				log.Error(err, "Syncing remote controller object")
+				// We'll retry this in the next reconciling.
+				return reconcile.Result{}, err
+			}
+			return reconcile.Result{}, nil
+		}
+
+		// workload evicted on worker cluster
+		log.V(3).Info("Workload was evicted in the remote cluster", "cluster", evictedRemote)
+		if err := workload.PatchAdmissionStatus(ctx, w.client, group.local, w.clock, func(wl *kueue.Workload) (bool, error) {
+			acs.State = kueue.CheckStateRetry
+			acs.Message = fmt.Sprintf("Workload evicted on worker cluster: %q, resetting for re-admission", *group.local.Status.ClusterName)
```

  Please include in the message the previous state. Maybe add something like "...resetting for re-admission. Previously: Ready".

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-01-14T08:31:14Z

- **pkg/controller/admissionchecks/multikueue/workload.go:392** by [@mszadkow](https://github.com/mszadkow) — 2026-01-14T08:31:14Z
```diff
@@ -349,22 +349,53 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 4. Handle workload evicted on manager cluster
+	// 4. Handle workload eviction
 	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
-	if remoteEvictCond != nil && remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+	if remoteEvictCond != nil {
 		remoteCl := group.remoteClients[evictedRemote].client
 		remoteWl := group.remotes[evictedRemote]
 
 		log = log.WithValues("remote", evictedRemote, "remoteWorkload", klog.KObj(remoteWl))
 		ctx = ctrl.LoggerInto(ctx, log)
 
-		if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
-			log.Error(err, "Syncing remote controller object")
-			// We'll retry this in the next reconciling.
-			return reconcile.Result{}, err
+		// workload evicted on manager cluster
+		if remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+			if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
+				log.Error(err, "Syncing remote controller object")
+				// We'll retry this in the next reconciling.
+				return reconcile.Result{}, err
+			}
+			return reconcile.Result{}, nil
 		}
 
-		// Wait for QuotaReserved=false in the local job.
+		// workload evicted on worker cluster
+		log.V(5).Info("Workload was evicted in the remote cluster", "cluster", evictedRemote)
+		needsACUpdate := acs.State == kueue.CheckStateReady
+		if needsACUpdate {
+			if err := workload.PatchAdmissionStatus(ctx, w.client, group.local, w.clock, func(wl *kueue.Workload) (bool, error) {
+				acs.State = kueue.CheckStatePending
+				acs.Message = fmt.Sprintf("Workload evicted on worker cluster: %q, resetting for re-admission", *group.local.Status.ClusterName)
+				acs.LastTransitionTime = metav1.NewTime(w.clock.Now())
+				workload.SetAdmissionCheckState(&wl.Status.AdmissionChecks, *acs, w.clock)
+				wl.Status.ClusterName = nil
+				wl.Status.NominatedClusterNames = nil
+
+				return true, nil
+			}); err != nil {
+				log.Error(err, "Failed to patch workload status")
+				return reconcile.Result{}, err
+			}
+
+			w.recorder.Eventf(group.local, corev1.EventTypeNormal, "MultiKueue", acs.Message)
+		}
+
+		for cluster := range group.remotes {
```

  I meant it has the same effect on remote clients, but yes we should reset it all AC too

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-14T10:54:57Z

- **pkg/controller/admissionchecks/multikueue/workload.go:378** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-14T10:54:57Z
```diff
@@ -349,22 +349,39 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 4. Handle workload evicted on manager cluster
+	// 4. Handle workload eviction
 	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
-	if remoteEvictCond != nil && remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+	if remoteEvictCond != nil {
 		remoteCl := group.remoteClients[evictedRemote].client
 		remoteWl := group.remotes[evictedRemote]
 
 		log = log.WithValues("remote", evictedRemote, "remoteWorkload", klog.KObj(remoteWl))
 		ctx = ctrl.LoggerInto(ctx, log)
 
-		if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
-			log.Error(err, "Syncing remote controller object")
-			// We'll retry this in the next reconciling.
+		// workload evicted on manager cluster
+		if remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+			if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
+				log.Error(err, "Syncing remote controller object")
+				// We'll retry this in the next reconciling.
+				return reconcile.Result{}, err
+			}
+			return reconcile.Result{}, nil
+		}
+
+		// workload evicted on worker cluster
+		log.V(3).Info("Workload was evicted in the remote cluster", "cluster", evictedRemote)
+		if err := workload.PatchAdmissionStatus(ctx, w.client, group.local, w.clock, func(wl *kueue.Workload) (bool, error) {
+			acs.Message = fmt.Sprintf("Workload evicted on worker cluster: %q, resetting for re-admission. Previously: %q", *group.local.Status.ClusterName, acs.State)
+			acs.State = kueue.CheckStateRetry
+			acs.LastTransitionTime = metav1.NewTime(w.clock.Now())
+			workload.SetAdmissionCheckState(&wl.Status.AdmissionChecks, *acs, w.clock)
+			return true, nil
```

  ```suggestion
  			return workload.SetAdmissionCheckState(&wl.Status.AdmissionChecks, *acs, w.clock), nil
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-14T15:28:59Z

- **pkg/features/kube_features.go:362** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-14T15:28:59Z
```diff
@@ -341,7 +347,10 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 		{Version: version.MustParse("0.15"), Default: false, PreRelease: featuregate.Alpha},
 	},
 	SkipFinalizersForPodsSuspendedByParent: {
-		{Version: version.MustParse("0.16"), Default: true, PreRelease: featuregate.Beta}, // GA in 0.18
+		{Version: version.MustParse("0.16"), Default: true, PreRelease: featuregate.Beta},
+	},
+	MultiKueueRedoAdmissionOnEvictionInWorker: {
```

  Please add to this table:
  
  https://kueue.sigs.k8s.io/docs/installation/#feature-gates-for-alpha-and-beta-features

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-14T15:35:21Z

- **pkg/features/kube_features.go:353** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-14T15:35:21Z
```diff
@@ -341,7 +347,10 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 		{Version: version.MustParse("0.15"), Default: false, PreRelease: featuregate.Alpha},
 	},
 	SkipFinalizersForPodsSuspendedByParent: {
-		{Version: version.MustParse("0.16"), Default: true, PreRelease: featuregate.Beta}, // GA in 0.18
+		{Version: version.MustParse("0.16"), Default: true, PreRelease: featuregate.Beta},
+	},
+	MultiKueueRedoAdmissionOnEvictionInWorker: {
+		{Version: version.MustParse("0.16"), Default: false, PreRelease: featuregate.Alpha},
```

  ```suggestion
  		{Version: version.MustParse("0.16"), Default: true, PreRelease: featuregate.Beta}, // GA in 0.18
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-14T17:12:32Z

- **pkg/controller/admissionchecks/multikueue/workload.go:373** by [@mimowo](https://github.com/mimowo) — 2026-01-14T17:12:32Z
```diff
@@ -349,23 +350,43 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		return reconcile.Result{}, workload.Finish(ctx, w.client, group.local, remoteFinishedCond.Reason, remoteFinishedCond.Message, w.clock)
 	}
 
-	// 4. Handle workload evicted on manager cluster
+	// 4. Handle workload eviction
 	remoteEvictCond, evictedRemote := group.bestMatchByCondition(kueue.WorkloadEvicted)
-	if remoteEvictCond != nil && remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+	if remoteEvictCond != nil {
 		remoteCl := group.remoteClients[evictedRemote].client
 		remoteWl := group.remotes[evictedRemote]
 
 		log = log.WithValues("remote", evictedRemote, "remoteWorkload", klog.KObj(remoteWl))
 		ctx = ctrl.LoggerInto(ctx, log)
 
-		if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
-			log.Error(err, "Syncing remote controller object")
-			// We'll retry this in the next reconciling.
-			return reconcile.Result{}, err
+		// workload evicted on manager cluster
+		if remoteEvictCond.Reason == workload.ReasonWithCause(kueue.WorkloadDeactivated, kueue.WorkloadEvictedOnManagerCluster) {
+			if err := group.jobAdapter.SyncJob(ctx, w.client, remoteCl, group.controllerKey, group.local.Name, w.origin); err != nil {
+				log.Error(err, "Syncing remote controller object")
+				// We'll retry this in the next reconciling.
+				return reconcile.Result{}, err
+			}
+			return reconcile.Result{}, nil
 		}
 
-		// Wait for QuotaReserved=false in the local job.
-		return reconcile.Result{}, nil
+		if features.Enabled(features.MultiKueueRedoAdmissionOnEvictionInWorker) {
+			if acs.State == kueue.CheckStateReady {
```

  nit, but we can inline the checks `features.Enabled(features.MultiKueueRedoAdmissionOnEvictionInWorker)  && acs.State == kueue.CheckStateReady` to save one level of indentention

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-14T17:14:37Z

LGTM, I will just wait for the remaining comments
/approve

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-15T10:10:47Z

- **test/e2e/multikueue/e2e_test.go:634** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-15T10:10:47Z
```diff
@@ -626,6 +627,84 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 			})
 		})
 
+		ginkgo.It("Should re-do admission process when workload gets evicted in the worker", func() {
+			defaultManagerKueueCfg := util.GetKueueConfiguration(ctx, k8sManagerClient)
+			util.UpdateKueueConfiguration(ctx, k8sManagerClient, defaultManagerKueueCfg, managerClusterName, func(cfg *kueueconfig.Configuration) {
+				cfg.FeatureGates[string(features.MultiKueueRedoAdmissionOnEvictionInWorker)] = true
+			})
```

  I think this changes no need. We already have enabled true. Could you please prepare follow-up PR to remove this?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-15T10:11:07Z

- **test/e2e/multikueue/e2e_test.go:637** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-15T10:11:07Z
```diff
@@ -626,6 +627,84 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 			})
 		})
 
+		ginkgo.It("Should re-do admission process when workload gets evicted in the worker", func() {
+			defaultManagerKueueCfg := util.GetKueueConfiguration(ctx, k8sManagerClient)
+			util.UpdateKueueConfiguration(ctx, k8sManagerClient, defaultManagerKueueCfg, managerClusterName, func(cfg *kueueconfig.Configuration) {
+				cfg.FeatureGates[string(features.MultiKueueRedoAdmissionOnEvictionInWorker)] = true
+			})
+
+			ginkgo.DeferCleanup(func() {
+				util.UpdateKueueConfiguration(ctx, k8sManagerClient, defaultManagerKueueCfg, managerClusterName, func(cfg *kueueconfig.Configuration) {})
```

  Also this.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-15T10:11:29Z

- **test/integration/multikueue/jobs_test.go:1878** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-15T10:11:29Z
```diff
@@ -1873,6 +1874,103 @@ var _ = ginkgo.Describe("MultiKueue", ginkgo.Label("area:multikueue", "feature:m
 			}, gomega.Equal(completedJobCondition))))
 		})
 	})
+	ginkgo.It("Should redo the admission process once the workload loses Admission in the worker cluster", func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.MultiKueueRedoAdmissionOnEvictionInWorker, true)
```

  Also here.
