# PR #8480: add priority class to wl none -> some

**Summary**: add priority class to wl none -> some

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8480

**Last updated**: 2026-03-22T08:26:53Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@andrewseif](https://github.com/andrewseif)
- **Branch**: `add-wpc-to-suspended-wl` → `main`
- **Created**: 2026-01-08T23:53:35Z
- **Updated**: 2026-03-22T08:26:53Z
- **Merged**: 2026-01-13T08:05:38Z
- **Closed**: 2026-01-13T08:05:38Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi), [@olekzabl](https://github.com/olekzabl)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92)
- **Changed files**: 7   **+192 / -6**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Adding a priority class to a workload (none -> some) is now reconciled, and behaving as explained/expected in the ticket

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #8320 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
yes
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Scheduling: fix the bug that setting (none -> some) a workload priority class label (kueue.x-k8s.io/priority-class) was ignored.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-01-08T23:53:41Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 74c3d8ed1c7456b3c91143a27c31b2f707a438aa |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69650d93b7f80200084b6cb7 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-08T23:53:45Z

Hi @andrewseif. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-09T06:52:13Z

/ok-to-test

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-09T06:58:11Z

@andrewseif could you please rebase PR?

### Comment by [@andrewseif](https://github.com/andrewseif) — 2026-01-11T17:09:44Z

/retest

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-12T10:51:47Z

Awesome! Thank you so much! :pray:
 
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-12T10:51:55Z

LGTM label has been added.  <details>Git tree hash: 23c572e89ee4b78534c60b3657347bf7e0f93874</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-12T10:51:55Z

/cc @mimowo

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-12T10:52:20Z

/cc @olekzabl

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-12T11:57:05Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-12T11:57:12Z

LGTM label has been added.  <details>Git tree hash: 45037497ec25c671a30bd4551d1f29dd80ac3e24</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-12T14:50:13Z

@andrewseif this requires a release note, please set it

### Comment by [@andrewseif](https://github.com/andrewseif) — 2026-01-12T15:01:37Z

> @andrewseif this requires a release note, please set it

not sure its correct like that, as it will be my first time adding a release note

### Comment by [@olekzabl](https://github.com/olekzabl) — 2026-01-12T22:33:42Z

Thank you!

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-12T22:33:53Z

LGTM label has been added.  <details>Git tree hash: 45037497ec25c671a30bd4551d1f29dd80ac3e24</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-13T07:37:44Z

Thank you 👍 
/lgtm
/approve
/cherrypick release-0.15

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-01-13T07:37:47Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.15` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8480#issuecomment-3742531426):

>Thank you 👍 
>/lgtm
>/approve
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-13T07:37:52Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8480#" title="Author self-approved">andrewseif</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8480#issuecomment-3742531426" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8480#pullrequestreview-3652950907" title="Approved">olekzabl</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-13T07:41:01Z

/release-note-edit
```release-note
Scheduling: fix the bug that setting a workload priority class label (kueue.x-k8s.io/priority-class) was ignored.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-13T07:41:20Z

/release-note-edit
```release-note
Scheduling: fix the bug that setting (none -> some) a workload priority class label (kueue.x-k8s.io/priority-class) was ignored.
```

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-01-13T08:06:25Z

@mimowo: #8480 failed to apply on top of branch "release-0.15":
```
Applying: bugfix and add unit test
Applying: add e2e testing
Applying: update e2e testing
Applying: update function name
Using index info to reconstruct a base tree...
M	pkg/controller/core/workload_controller.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/controller/core/workload_controller.go
Applying: fix function name in testing
Applying: revert to old logic but removing empty string for old
Applying: add validation for old wl
Applying: fix linter issue
Using index info to reconstruct a base tree...
M	pkg/controller/core/workload_controller.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/controller/core/workload_controller.go
Applying: Update pkg/controller/core/workload_controller.go
Applying: remove redundant validation
Applying: Update pkg/workload/workload.go
Applying: clean tests
Using index info to reconstruct a base tree...
M	test/integration/singlecluster/controller/jobs/job/job_controller_test.go
Falling back to patching base and 3-way merge...
Auto-merging test/integration/singlecluster/controller/jobs/job/job_controller_test.go
Applying: update reconciler UpdateWorkloadPriority method
Applying: update methods to be more expressive
Applying: revert comment for simplicity
Applying: update workload to use IsPodPriorityClass, to avoid complexity
Applying: update IsPodPriorityClass function
Applying: add value based tests
Applying: Update pkg/workload/workload.go
Applying: Update test/integration/singlecluster/controller/jobs/job/job_controller_test.go
Applying: Update test/integration/singlecluster/controller/jobs/job/job_controller_test.go
Applying: Update pkg/controller/core/workload_controller_test.go
Applying: Update pkg/controller/core/workload_controller.go
Applying: add updated tests and fix log
Applying: fix test
Applying: remove redundant test in preemption_test.go
Applying: remove white spaces in preemption_test.go
Applying: add comment on UpdateWorkloadPriority
Applying: add new util func to use wl key, and update job int test
Using index info to reconstruct a base tree...
M	test/integration/singlecluster/controller/jobs/job/job_controller_test.go
M	test/util/util.go
Falling back to patching base and 3-way merge...
Auto-merging test/util/util.go
Auto-merging test/integration/singlecluster/controller/jobs/job/job_controller_test.go
Applying: fix test to use correct func
Applying: revert changes to test
Using index info to reconstruct a base tree...
M	test/integration/singlecluster/controller/jobs/job/job_controller_test.go
M	test/util/util.go
Falling back to patching base and 3-way merge...
Auto-merging test/util/util.go
Auto-merging test/integration/singlecluster/controller/jobs/job/job_controller_test.go
Applying: add new function with wrapper to use in test
Using index info to reconstruct a base tree...
M	test/util/util.go
Falling back to patching base and 3-way merge...
Auto-merging test/util/util.go
CONFLICT (content): Merge conflict in test/util/util.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0032 add new function with wrapper to use in test

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8480#issuecomment-3742531426):

>Thank you 👍 
>/lgtm
>/approve
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-13T17:47:21Z

@andrewseif thank you for working on this issue, could you manually cherry-pick this PR into release-0.14 and release-0.15 branches?
You can use https://github.com/kubernetes-sigs/kueue/blob/main/hack/cherry_pick_pull.sh this script.

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-09T07:00:22Z

- **pkg/controller/jobframework/validation.go:157** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-09T07:00:22Z
```diff
@@ -154,11 +154,13 @@ func validatedUpdateForEnabledWorkloadSlice(oldJob, newJob GenericJob) field.Err
 }
 
 func ValidateUpdateForWorkloadPriorityClassName(isSuspended bool, oldObj, newObj client.Object) field.ErrorList {
+	// Cannot ADD a priority class to a NON-suspended (running) workload && wpc is empty
```

  If we want to support adding a priority class, I think we should allow it.

### Review by [@andrewseif](https://github.com/andrewseif) (commented) — 2026-01-09T08:07:55Z

- **pkg/controller/jobframework/validation.go:157** by [@andrewseif](https://github.com/andrewseif) — 2026-01-09T08:07:55Z
```diff
@@ -154,11 +154,13 @@ func validatedUpdateForEnabledWorkloadSlice(oldJob, newJob GenericJob) field.Err
 }
 
 func ValidateUpdateForWorkloadPriorityClassName(isSuspended bool, oldObj, newObj client.Object) field.ErrorList {
+	// Cannot ADD a priority class to a NON-suspended (running) workload && wpc is empty
```

  as far as I understand the issue mentioned suspended workloads, this piece of code is only affecting running on.
  
  If we change this if condition, it means we allow priority change to running workloads (if I get the code right?)

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-09T08:20:00Z

- **pkg/controller/jobframework/validation.go:157** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-09T08:20:00Z
```diff
@@ -154,11 +154,13 @@ func validatedUpdateForEnabledWorkloadSlice(oldJob, newJob GenericJob) field.Err
 }
 
 func ValidateUpdateForWorkloadPriorityClassName(isSuspended bool, oldObj, newObj client.Object) field.ErrorList {
+	// Cannot ADD a priority class to a NON-suspended (running) workload && wpc is empty
```

  Ah, I see, you right, we want to fix only suspended workloads.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-09T08:22:22Z

- **pkg/controller/core/workload_controller.go:1000** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-09T08:22:22Z
```diff
@@ -991,17 +990,14 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 }
 
 func workloadPriorityChanged(old, new *kueue.Workload) bool {
-	// Updates to Pod Priority are not supported.
-	if !workload.IsWorkloadPriorityClass(old) || !workload.IsWorkloadPriorityClass(new) {
+	if !workload.IsWorkloadPriorityClass(new) {
 		return false
 	}
-	// Check if priority class reference changed.
-	if workload.PriorityClassName(old) != "" && workload.PriorityClassName(new) != "" &&
-		workload.PriorityClassName(old) != workload.PriorityClassName(new) {
-		return true
-	}
-	// Check if priority value changed (for WorkloadPriorityClass value updates).
-	return priority.Priority(old) != priority.Priority(new)
+
+	oldName := workload.PriorityClassName(old)
+	newName := workload.PriorityClassName(new)
+
+	return newName != "" && oldName != newName
```

  You are checking only name here. What about priority?

### Review by [@andrewseif](https://github.com/andrewseif) (commented) — 2026-01-09T08:27:42Z

- **pkg/controller/core/workload_controller.go:1000** by [@andrewseif](https://github.com/andrewseif) — 2026-01-09T08:27:42Z
```diff
@@ -991,17 +990,14 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 }
 
 func workloadPriorityChanged(old, new *kueue.Workload) bool {
-	// Updates to Pod Priority are not supported.
-	if !workload.IsWorkloadPriorityClass(old) || !workload.IsWorkloadPriorityClass(new) {
+	if !workload.IsWorkloadPriorityClass(new) {
 		return false
 	}
-	// Check if priority class reference changed.
-	if workload.PriorityClassName(old) != "" && workload.PriorityClassName(new) != "" &&
-		workload.PriorityClassName(old) != workload.PriorityClassName(new) {
-		return true
-	}
-	// Check if priority value changed (for WorkloadPriorityClass value updates).
-	return priority.Priority(old) != priority.Priority(new)
+
+	oldName := workload.PriorityClassName(old)
+	newName := workload.PriorityClassName(new)
+
+	return newName != "" && oldName != newName
```

  in this code block we check that the new wl has a wpc
  
  `
  if !workload.IsWorkloadPriorityClass(new) {
  		return false
  	}
  	`
  and that's what we care about since we want to allow none wpc -> some wpc

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-09T09:06:05Z

- **pkg/controller/core/workload_controller.go:1004** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-09T09:06:05Z
```diff
@@ -991,17 +990,14 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 }
 
 func workloadPriorityChanged(old, new *kueue.Workload) bool {
-	// Updates to Pod Priority are not supported.
-	if !workload.IsWorkloadPriorityClass(old) || !workload.IsWorkloadPriorityClass(new) {
+	if !workload.IsWorkloadPriorityClass(new) {
 		return false
 	}
-	// Check if priority class reference changed.
-	if workload.PriorityClassName(old) != "" && workload.PriorityClassName(new) != "" &&
-		workload.PriorityClassName(old) != workload.PriorityClassName(new) {
-		return true
-	}
-	// Check if priority value changed (for WorkloadPriorityClass value updates).
-	return priority.Priority(old) != priority.Priority(new)
```

  What about this?

### Review by [@andrewseif](https://github.com/andrewseif) (commented) — 2026-01-09T09:13:09Z

- **pkg/controller/core/workload_controller.go:1004** by [@andrewseif](https://github.com/andrewseif) — 2026-01-09T09:13:08Z
```diff
@@ -991,17 +990,14 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 }
 
 func workloadPriorityChanged(old, new *kueue.Workload) bool {
-	// Updates to Pod Priority are not supported.
-	if !workload.IsWorkloadPriorityClass(old) || !workload.IsWorkloadPriorityClass(new) {
+	if !workload.IsWorkloadPriorityClass(new) {
 		return false
 	}
-	// Check if priority class reference changed.
-	if workload.PriorityClassName(old) != "" && workload.PriorityClassName(new) != "" &&
-		workload.PriorityClassName(old) != workload.PriorityClassName(new) {
-		return true
-	}
-	// Check if priority value changed (for WorkloadPriorityClass value updates).
-	return priority.Priority(old) != priority.Priority(new)
```

  I think this is more restrictive? its checking (as far as I understand) the priority itself (low or high...etc), but I don't think it would work in a none ->some scenario? 
  
  in my case we are checking if old not equal new, and if new isn't empty. so no (some -> none scenario).

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-09T09:19:03Z

- **pkg/controller/core/workload_controller.go:1004** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-09T09:19:03Z
```diff
@@ -991,17 +990,14 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 }
 
 func workloadPriorityChanged(old, new *kueue.Workload) bool {
-	// Updates to Pod Priority are not supported.
-	if !workload.IsWorkloadPriorityClass(old) || !workload.IsWorkloadPriorityClass(new) {
+	if !workload.IsWorkloadPriorityClass(new) {
 		return false
 	}
-	// Check if priority class reference changed.
-	if workload.PriorityClassName(old) != "" && workload.PriorityClassName(new) != "" &&
-		workload.PriorityClassName(old) != workload.PriorityClassName(new) {
-		return true
-	}
-	// Check if priority value changed (for WorkloadPriorityClass value updates).
-	return priority.Priority(old) != priority.Priority(new)
```

  Please don't remove this logic.

### Review by [@andrewseif](https://github.com/andrewseif) (commented) — 2026-01-09T09:26:56Z

- **pkg/controller/core/workload_controller.go:1004** by [@andrewseif](https://github.com/andrewseif) — 2026-01-09T09:26:56Z
```diff
@@ -991,17 +990,14 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 }
 
 func workloadPriorityChanged(old, new *kueue.Workload) bool {
-	// Updates to Pod Priority are not supported.
-	if !workload.IsWorkloadPriorityClass(old) || !workload.IsWorkloadPriorityClass(new) {
+	if !workload.IsWorkloadPriorityClass(new) {
 		return false
 	}
-	// Check if priority class reference changed.
-	if workload.PriorityClassName(old) != "" && workload.PriorityClassName(new) != "" &&
-		workload.PriorityClassName(old) != workload.PriorityClassName(new) {
-		return true
-	}
-	// Check if priority value changed (for WorkloadPriorityClass value updates).
-	return priority.Priority(old) != priority.Priority(new)
```

  I reverted the changes to this logic, but removed the culprit which is checking old prio, please check and tell me if that's ok now 😄

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-09T09:54:28Z

- **pkg/controller/core/workload_controller.go:995** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-09T09:54:28Z
```diff
@@ -992,11 +992,11 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 
 func workloadPriorityChanged(old, new *kueue.Workload) bool {
 	// Updates to Pod Priority are not supported.
-	if !workload.IsWorkloadPriorityClass(old) || !workload.IsWorkloadPriorityClass(new) {
+	if !workload.IsWorkloadPriorityClass(new) {
```

  Can we check if previous one is not a PodPriority?

### Review by [@andrewseif](https://github.com/andrewseif) (commented) — 2026-01-10T12:20:32Z

- **pkg/controller/core/workload_controller.go:995** by [@andrewseif](https://github.com/andrewseif) — 2026-01-10T12:20:32Z
```diff
@@ -992,11 +992,11 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 
 func workloadPriorityChanged(old, new *kueue.Workload) bool {
 	// Updates to Pod Priority are not supported.
-	if !workload.IsWorkloadPriorityClass(old) || !workload.IsWorkloadPriorityClass(new) {
+	if !workload.IsWorkloadPriorityClass(new) {
```

  done, I added a specific method for that because workload.IsWorkloadPriorityClass was returning false for old wl

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-10T14:53:03Z

- **pkg/controller/core/workload_controller.go:995** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-10T14:53:03Z
```diff
@@ -992,11 +992,11 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 
 func workloadPriorityChanged(old, new *kueue.Workload) bool {
 	// Updates to Pod Priority are not supported.
-	if !workload.IsWorkloadPriorityClass(old) || !workload.IsWorkloadPriorityClass(new) {
+	if !workload.IsWorkloadPriorityClass(new) {
```

  ```suggestion
  	if workload.IsPodPriorityClass(old) || !workload.IsWorkloadPriorityClass(new) {
  ```
  
  I think we can add an or case for this instead of creating a separate if clause. That would also reduce changes.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-10T15:04:53Z

- **pkg/controller/core/workload_controller.go:1002** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-10T15:04:53Z
```diff
@@ -991,12 +991,17 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 }
 
 func workloadPriorityChanged(old, new *kueue.Workload) bool {
-	// Updates to Pod Priority are not supported.
-	if !workload.IsWorkloadPriorityClass(old) || !workload.IsWorkloadPriorityClass(new) {
+	// Updates to Pod Priority are not supported, validate that prority class is of type kueue.WorkloadPriorityClass.
+	if workload.IsPodPriorityClass(old) || !workload.IsWorkloadPriorityClass(new) {
+		return false
+	}
+
+	// Check that old wl priority class is of type kueue.WorkloadPriorityClass.
+	if workload.IsPodPriorityClass(old) {
 		return false
 	}
```

  So now this no need, right?

### Review by [@andrewseif](https://github.com/andrewseif) (commented) — 2026-01-10T15:07:43Z

- **pkg/controller/core/workload_controller.go:1002** by [@andrewseif](https://github.com/andrewseif) — 2026-01-10T15:07:43Z
```diff
@@ -991,12 +991,17 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 }
 
 func workloadPriorityChanged(old, new *kueue.Workload) bool {
-	// Updates to Pod Priority are not supported.
-	if !workload.IsWorkloadPriorityClass(old) || !workload.IsWorkloadPriorityClass(new) {
+	// Updates to Pod Priority are not supported, validate that prority class is of type kueue.WorkloadPriorityClass.
+	if workload.IsPodPriorityClass(old) || !workload.IsWorkloadPriorityClass(new) {
+		return false
+	}
+
+	// Check that old wl priority class is of type kueue.WorkloadPriorityClass.
+	if workload.IsPodPriorityClass(old) {
 		return false
 	}
```

  for one sec, I thought that your suggestion removed it, my bad, fixed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-10T15:10:41Z

- **pkg/controller/core/workload_controller_test.go:2791** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-10T15:10:41Z
```diff
@@ -2734,3 +2734,77 @@ func TestReconcile(t *testing.T) {
 		}
 	}
 }
+
+// TestWorkloadPriorityClassChanged tests the workloadPriorityClassChanged function.
+// (none -> some) should be detected as a change and trigger reconciliation.
+func TestWorkloadPriorityClassChanged(t *testing.T) {
+	testCases := map[string]struct {
+		oldWorkload *kueue.Workload
+		newWorkload *kueue.Workload
+		wantChanged bool
+	}{
+		"no priority class on either workload": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").Obj(),
+			wantChanged: false,
+		},
+		"same priority class on both workloads": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Obj(),
+			wantChanged: false,
+		},
+		"priority class changed from one to another": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-2").
+				Obj(),
+			wantChanged: true,
+		},
+		"priority class added (none -> some)": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Obj(),
+			wantChanged: true,
+		},
+		"priority class removed (some -> none) - blocked by validation": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").Obj(),
+			// Removal is blocked by validation, so we don't need to detect it
+			wantChanged: false,
+		},
+		"PodPriorityClass (not WorkloadPriorityClass) changed - should not trigger": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				PodPriorityClassRef("pod-priority-1").
+				Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				PodPriorityClassRef("pod-priority-2").
+				Obj(),
+			wantChanged: false,
+		},
+		"PodPriorityClass added (none -> some) - should not trigger": {
```

  It would also be great to add a test case where we change the priority value. It should work only for WorkloadPriorityClass.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-10T15:12:05Z

- **pkg/workload/workload.go:1447** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-10T15:12:05Z
```diff
@@ -1441,6 +1441,12 @@ func IsWorkloadPriorityClass(wl *kueue.Workload) bool {
 		wl.Spec.PriorityClassRef.Group == kueue.WorkloadPriorityClassGroup
 }
 
+func IsPodPriorityClass(wl *kueue.Workload) bool {
+	return wl.Spec.PriorityClassRef != nil &&
+		wl.Spec.PriorityClassRef.Kind == "PriorityClass" &&
+		(wl.Spec.PriorityClassRef.Group == "" || wl.Spec.PriorityClassRef.Group == "scheduling.k8s.io")
```

  ```suggestion
  		(wl.Spec.PriorityClassRef.Group == "" || wl.Spec.PriorityClassRef.Group == kueue.PodPriorityClassGroup)
  ```
  
  Can we use PodPriorityClassGroup constant?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-10T15:24:49Z

- **test/integration/singlecluster/scheduler/preemption_test.go:289** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-10T15:24:49Z
```diff
@@ -259,6 +259,82 @@ var _ = ginkgo.Describe("Preemption", func() {
 			cQPath := "/" + cq.Name
 			util.ExpectPreemptedCondition(ctx, k8sClient, kueue.InClusterQueueReason, metav1.ConditionTrue, lowWl, highWl, string(highWl.UID), "job-uid", cQPath, cQPath)
 		})
+
+		ginkgo.It("Should trigger preemption when WorkloadPriorityClass is added to suspended workload", func() {
+			ginkgo.By("Creating a normal priority workload that gets admitted and uses all quota")
+
+			normalPrioClass := utiltestingapi.MakeWorkloadPriorityClass("normal-priority").
+				PriorityValue(0).
+				Obj()
+			util.MustCreate(ctx, k8sClient, normalPrioClass)
+			defer func() {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, normalPrioClass, true)
+			}()
+
+			normalWl := utiltestingapi.MakeWorkload("normal-wl", ns.Name).
+				Queue(kueue.LocalQueueName(q.Name)).
+				Priority(0).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+
+			normalWl.Spec.PriorityClassRef = &kueue.PriorityClassRef{
+				Group: kueue.WorkloadPriorityClassGroup,
+				Kind:  "WorkloadPriorityClass",
+				Name:  "normal-priority",
+			}
+
+			util.MustCreate(ctx, k8sClient, normalWl)
+			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, normalWl)
+
+			ginkgo.By("Creating a suspended workload WITHOUT priority class")
```

  I think you mean deactivated workload, right?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-10T15:25:13Z

- **test/integration/singlecluster/scheduler/preemption_test.go:289** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-10T15:25:13Z
```diff
@@ -259,6 +259,82 @@ var _ = ginkgo.Describe("Preemption", func() {
 			cQPath := "/" + cq.Name
 			util.ExpectPreemptedCondition(ctx, k8sClient, kueue.InClusterQueueReason, metav1.ConditionTrue, lowWl, highWl, string(highWl.UID), "job-uid", cQPath, cQPath)
 		})
+
+		ginkgo.It("Should trigger preemption when WorkloadPriorityClass is added to suspended workload", func() {
+			ginkgo.By("Creating a normal priority workload that gets admitted and uses all quota")
+
+			normalPrioClass := utiltestingapi.MakeWorkloadPriorityClass("normal-priority").
+				PriorityValue(0).
+				Obj()
+			util.MustCreate(ctx, k8sClient, normalPrioClass)
+			defer func() {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, normalPrioClass, true)
+			}()
+
+			normalWl := utiltestingapi.MakeWorkload("normal-wl", ns.Name).
+				Queue(kueue.LocalQueueName(q.Name)).
+				Priority(0).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+
+			normalWl.Spec.PriorityClassRef = &kueue.PriorityClassRef{
+				Group: kueue.WorkloadPriorityClassGroup,
+				Kind:  "WorkloadPriorityClass",
+				Name:  "normal-priority",
+			}
+
+			util.MustCreate(ctx, k8sClient, normalWl)
+			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, normalWl)
+
+			ginkgo.By("Creating a suspended workload WITHOUT priority class")
```

  Can we add test case [here](https://github.com/andrewseif/kueue/blob/1e72145c6b814d8fe34ff00d6422106a5c032bfb/test/integration/singlecluster/controller/jobs/job/job_controller_test.go) instead? And also apply case from issue.

### Review by [@andrewseif](https://github.com/andrewseif) (commented) — 2026-01-10T15:25:40Z

- **pkg/controller/core/workload_controller_test.go:2791** by [@andrewseif](https://github.com/andrewseif) — 2026-01-10T15:25:39Z
```diff
@@ -2734,3 +2734,77 @@ func TestReconcile(t *testing.T) {
 		}
 	}
 }
+
+// TestWorkloadPriorityClassChanged tests the workloadPriorityClassChanged function.
+// (none -> some) should be detected as a change and trigger reconciliation.
+func TestWorkloadPriorityClassChanged(t *testing.T) {
+	testCases := map[string]struct {
+		oldWorkload *kueue.Workload
+		newWorkload *kueue.Workload
+		wantChanged bool
+	}{
+		"no priority class on either workload": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").Obj(),
+			wantChanged: false,
+		},
+		"same priority class on both workloads": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Obj(),
+			wantChanged: false,
+		},
+		"priority class changed from one to another": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-2").
+				Obj(),
+			wantChanged: true,
+		},
+		"priority class added (none -> some)": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Obj(),
+			wantChanged: true,
+		},
+		"priority class removed (some -> none) - blocked by validation": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").Obj(),
+			// Removal is blocked by validation, so we don't need to detect it
+			wantChanged: false,
+		},
+		"PodPriorityClass (not WorkloadPriorityClass) changed - should not trigger": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				PodPriorityClassRef("pod-priority-1").
+				Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				PodPriorityClassRef("pod-priority-2").
+				Obj(),
+			wantChanged: false,
+		},
+		"PodPriorityClass added (none -> some) - should not trigger": {
```

  this got me thinking, is there a case where Kind changed from PodPriority to WorkloadPriority but uses same name? do we need to cover that?

### Review by [@andrewseif](https://github.com/andrewseif) (commented) — 2026-01-10T15:31:24Z

- **test/integration/singlecluster/scheduler/preemption_test.go:289** by [@andrewseif](https://github.com/andrewseif) — 2026-01-10T15:31:23Z
```diff
@@ -259,6 +259,82 @@ var _ = ginkgo.Describe("Preemption", func() {
 			cQPath := "/" + cq.Name
 			util.ExpectPreemptedCondition(ctx, k8sClient, kueue.InClusterQueueReason, metav1.ConditionTrue, lowWl, highWl, string(highWl.UID), "job-uid", cQPath, cQPath)
 		})
+
+		ginkgo.It("Should trigger preemption when WorkloadPriorityClass is added to suspended workload", func() {
+			ginkgo.By("Creating a normal priority workload that gets admitted and uses all quota")
+
+			normalPrioClass := utiltestingapi.MakeWorkloadPriorityClass("normal-priority").
+				PriorityValue(0).
+				Obj()
+			util.MustCreate(ctx, k8sClient, normalPrioClass)
+			defer func() {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, normalPrioClass, true)
+			}()
+
+			normalWl := utiltestingapi.MakeWorkload("normal-wl", ns.Name).
+				Queue(kueue.LocalQueueName(q.Name)).
+				Priority(0).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+
+			normalWl.Spec.PriorityClassRef = &kueue.PriorityClassRef{
+				Group: kueue.WorkloadPriorityClassGroup,
+				Kind:  "WorkloadPriorityClass",
+				Name:  "normal-priority",
+			}
+
+			util.MustCreate(ctx, k8sClient, normalWl)
+			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, normalWl)
+
+			ginkgo.By("Creating a suspended workload WITHOUT priority class")
```

  > I think you mean deactivated workload, right?
  
  if I am guessing correctly, in this context you mean:
  if I am using .spec.active = true it means deactivated.
  if I am using .spec.suspended = true it means suspended?
  
  if so then its deactivated.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-10T15:36:17Z

- **pkg/controller/core/workload_controller_test.go:2791** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-10T15:36:17Z
```diff
@@ -2734,3 +2734,77 @@ func TestReconcile(t *testing.T) {
 		}
 	}
 }
+
+// TestWorkloadPriorityClassChanged tests the workloadPriorityClassChanged function.
+// (none -> some) should be detected as a change and trigger reconciliation.
+func TestWorkloadPriorityClassChanged(t *testing.T) {
+	testCases := map[string]struct {
+		oldWorkload *kueue.Workload
+		newWorkload *kueue.Workload
+		wantChanged bool
+	}{
+		"no priority class on either workload": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").Obj(),
+			wantChanged: false,
+		},
+		"same priority class on both workloads": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Obj(),
+			wantChanged: false,
+		},
+		"priority class changed from one to another": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-2").
+				Obj(),
+			wantChanged: true,
+		},
+		"priority class added (none -> some)": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Obj(),
+			wantChanged: true,
+		},
+		"priority class removed (some -> none) - blocked by validation": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").Obj(),
+			// Removal is blocked by validation, so we don't need to detect it
+			wantChanged: false,
+		},
+		"PodPriorityClass (not WorkloadPriorityClass) changed - should not trigger": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				PodPriorityClassRef("pod-priority-1").
+				Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				PodPriorityClassRef("pod-priority-2").
+				Obj(),
+			wantChanged: false,
+		},
+		"PodPriorityClass added (none -> some) - should not trigger": {
```

  Yeah, it would be great to cover this case as well.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-10T15:38:37Z

- **test/integration/singlecluster/scheduler/preemption_test.go:289** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-10T15:38:37Z
```diff
@@ -259,6 +259,82 @@ var _ = ginkgo.Describe("Preemption", func() {
 			cQPath := "/" + cq.Name
 			util.ExpectPreemptedCondition(ctx, k8sClient, kueue.InClusterQueueReason, metav1.ConditionTrue, lowWl, highWl, string(highWl.UID), "job-uid", cQPath, cQPath)
 		})
+
+		ginkgo.It("Should trigger preemption when WorkloadPriorityClass is added to suspended workload", func() {
+			ginkgo.By("Creating a normal priority workload that gets admitted and uses all quota")
+
+			normalPrioClass := utiltestingapi.MakeWorkloadPriorityClass("normal-priority").
+				PriorityValue(0).
+				Obj()
+			util.MustCreate(ctx, k8sClient, normalPrioClass)
+			defer func() {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, normalPrioClass, true)
+			}()
+
+			normalWl := utiltestingapi.MakeWorkload("normal-wl", ns.Name).
+				Queue(kueue.LocalQueueName(q.Name)).
+				Priority(0).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+
+			normalWl.Spec.PriorityClassRef = &kueue.PriorityClassRef{
+				Group: kueue.WorkloadPriorityClassGroup,
+				Kind:  "WorkloadPriorityClass",
+				Name:  "normal-priority",
+			}
+
+			util.MustCreate(ctx, k8sClient, normalWl)
+			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, normalWl)
+
+			ginkgo.By("Creating a suspended workload WITHOUT priority class")
```

  > if I am using .spec.suspended = true it means suspended?
  
  Yes, but this case applies only to jobs. Only jobs can be suspended. Deactivation is not the same as suspension.

### Review by [@andrewseif](https://github.com/andrewseif) (commented) — 2026-01-10T16:49:15Z

- **test/integration/singlecluster/scheduler/preemption_test.go:289** by [@andrewseif](https://github.com/andrewseif) — 2026-01-10T16:49:14Z
```diff
@@ -259,6 +259,82 @@ var _ = ginkgo.Describe("Preemption", func() {
 			cQPath := "/" + cq.Name
 			util.ExpectPreemptedCondition(ctx, k8sClient, kueue.InClusterQueueReason, metav1.ConditionTrue, lowWl, highWl, string(highWl.UID), "job-uid", cQPath, cQPath)
 		})
+
+		ginkgo.It("Should trigger preemption when WorkloadPriorityClass is added to suspended workload", func() {
+			ginkgo.By("Creating a normal priority workload that gets admitted and uses all quota")
+
+			normalPrioClass := utiltestingapi.MakeWorkloadPriorityClass("normal-priority").
+				PriorityValue(0).
+				Obj()
+			util.MustCreate(ctx, k8sClient, normalPrioClass)
+			defer func() {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, normalPrioClass, true)
+			}()
+
+			normalWl := utiltestingapi.MakeWorkload("normal-wl", ns.Name).
+				Queue(kueue.LocalQueueName(q.Name)).
+				Priority(0).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+
+			normalWl.Spec.PriorityClassRef = &kueue.PriorityClassRef{
+				Group: kueue.WorkloadPriorityClassGroup,
+				Kind:  "WorkloadPriorityClass",
+				Name:  "normal-priority",
+			}
+
+			util.MustCreate(ctx, k8sClient, normalWl)
+			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, normalWl)
+
+			ginkgo.By("Creating a suspended workload WITHOUT priority class")
```

  I think we might have a bug [here](https://github.com/kubernetes-sigs/kueue/blob/6daefefb76cc7b5de60342a95d7694d15f0e9cb6/pkg/controller/jobframework/reconciler.go#L970)
  
  I am not sure how exactly everything ties together yet, but it seems this function uses the problematic `workload.IsWorkloadPriorityClass(wl)`, which when the wl is old and doesn't have a wpc ("") it will throw an error and not update the wpc.
  
  I think we might need to change it, but I think someone with more experience and knowledge should also validate my understanding.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-10T16:57:08Z

- **test/integration/singlecluster/scheduler/preemption_test.go:289** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-10T16:57:08Z
```diff
@@ -259,6 +259,82 @@ var _ = ginkgo.Describe("Preemption", func() {
 			cQPath := "/" + cq.Name
 			util.ExpectPreemptedCondition(ctx, k8sClient, kueue.InClusterQueueReason, metav1.ConditionTrue, lowWl, highWl, string(highWl.UID), "job-uid", cQPath, cQPath)
 		})
+
+		ginkgo.It("Should trigger preemption when WorkloadPriorityClass is added to suspended workload", func() {
+			ginkgo.By("Creating a normal priority workload that gets admitted and uses all quota")
+
+			normalPrioClass := utiltestingapi.MakeWorkloadPriorityClass("normal-priority").
+				PriorityValue(0).
+				Obj()
+			util.MustCreate(ctx, k8sClient, normalPrioClass)
+			defer func() {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, normalPrioClass, true)
+			}()
+
+			normalWl := utiltestingapi.MakeWorkload("normal-wl", ns.Name).
+				Queue(kueue.LocalQueueName(q.Name)).
+				Priority(0).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+
+			normalWl.Spec.PriorityClassRef = &kueue.PriorityClassRef{
+				Group: kueue.WorkloadPriorityClassGroup,
+				Kind:  "WorkloadPriorityClass",
+				Name:  "normal-priority",
+			}
+
+			util.MustCreate(ctx, k8sClient, normalWl)
+			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, normalWl)
+
+			ginkgo.By("Creating a suspended workload WITHOUT priority class")
```

  Not sure I got what's wrong with `workload.IsWorkloadPriorityClass(wl)`?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-10T16:58:58Z

- **test/integration/singlecluster/scheduler/preemption_test.go:289** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-10T16:58:58Z
```diff
@@ -259,6 +259,82 @@ var _ = ginkgo.Describe("Preemption", func() {
 			cQPath := "/" + cq.Name
 			util.ExpectPreemptedCondition(ctx, k8sClient, kueue.InClusterQueueReason, metav1.ConditionTrue, lowWl, highWl, string(highWl.UID), "job-uid", cQPath, cQPath)
 		})
+
+		ginkgo.It("Should trigger preemption when WorkloadPriorityClass is added to suspended workload", func() {
+			ginkgo.By("Creating a normal priority workload that gets admitted and uses all quota")
+
+			normalPrioClass := utiltestingapi.MakeWorkloadPriorityClass("normal-priority").
+				PriorityValue(0).
+				Obj()
+			util.MustCreate(ctx, k8sClient, normalPrioClass)
+			defer func() {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, normalPrioClass, true)
+			}()
+
+			normalWl := utiltestingapi.MakeWorkload("normal-wl", ns.Name).
+				Queue(kueue.LocalQueueName(q.Name)).
+				Priority(0).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+
+			normalWl.Spec.PriorityClassRef = &kueue.PriorityClassRef{
+				Group: kueue.WorkloadPriorityClassGroup,
+				Kind:  "WorkloadPriorityClass",
+				Name:  "normal-priority",
+			}
+
+			util.MustCreate(ctx, k8sClient, normalWl)
+			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, normalWl)
+
+			ginkgo.By("Creating a suspended workload WITHOUT priority class")
```

  Could you please explain in which case you have a bug?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-10T17:55:10Z

- **pkg/workload/workload.go:1446** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-10T17:55:10Z
```diff
@@ -1441,6 +1441,16 @@ func IsWorkloadPriorityClass(wl *kueue.Workload) bool {
 		wl.Spec.PriorityClassRef.Group == kueue.WorkloadPriorityClassGroup
 }
 
+func IsPodPriorityClass(wl *kueue.Workload) bool {
+	return wl.Spec.PriorityClassRef != nil &&
+		wl.Spec.PriorityClassRef.Kind == "PriorityClass" &&
```

  ```suggestion
  		wl.Spec.PriorityClassRef.Kind == kueue.PodPriorityClassKind &&
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-10T17:55:22Z

- **pkg/workload/workload.go:1447** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-10T17:55:22Z
```diff
@@ -1441,6 +1441,16 @@ func IsWorkloadPriorityClass(wl *kueue.Workload) bool {
 		wl.Spec.PriorityClassRef.Group == kueue.WorkloadPriorityClassGroup
 }
 
+func IsPodPriorityClass(wl *kueue.Workload) bool {
+	return wl.Spec.PriorityClassRef != nil &&
+		wl.Spec.PriorityClassRef.Kind == "PriorityClass" &&
+		(wl.Spec.PriorityClassRef.Group == "" || wl.Spec.PriorityClassRef.Group == kueue.PodPriorityClassGroup)
```

  ```suggestion
  		wl.Spec.PriorityClassRef.Group == kueue.PodPriorityClassGroup
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-10T18:02:29Z

- **pkg/workload/workload.go:1447** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-10T18:02:29Z
```diff
@@ -1441,6 +1441,16 @@ func IsWorkloadPriorityClass(wl *kueue.Workload) bool {
 		wl.Spec.PriorityClassRef.Group == kueue.WorkloadPriorityClassGroup
 }
 
+func IsPodPriorityClass(wl *kueue.Workload) bool {
+	return wl.Spec.PriorityClassRef != nil &&
+		wl.Spec.PriorityClassRef.Kind == kueue.PodPriorityClassKind &&
+		(wl.Spec.PriorityClassRef.Group == kueue.PodPriorityClassGroup)
```

  ```suggestion
  		wl.Spec.PriorityClassRef.Group == kueue.PodPriorityClassGroup
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-10T18:11:12Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:1604** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-10T18:11:13Z
```diff
@@ -1545,6 +1545,64 @@ var _ = ginkgo.Describe("Interacting with scheduler", ginkgo.Ordered, ginkgo.Con
 				}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
 			})
 		})
+
+		ginkgo.It("should trigger preemption when WorkloadPriorityClass is added to a pending workload that had no priority class", framework.SlowSpec, func() {
+			ginkgo.By("Creating a low-priority job that gets admitted on spot-untainted flavor")
+			jobLow := testingjob.MakeJob(jobName+"-low", ns.Name).
+				WorkloadPriorityClass(lowWorkloadPriorityClass.Name).
+				Queue(kueue.LocalQueueName(devLocalQ.Name)).
+				Request(corev1.ResourceCPU, "5").
+				NodeSelector(instanceKey, "spot-untainted").
+				Obj()
+			util.MustCreate(ctx, k8sClient, jobLow)
+
+			lowWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobLow.Name, jobLow.UID), Namespace: ns.Name}
+
+			ginkgo.By("Verifying the low-priority workload is admitted")
+			util.ExpectWorkloadsToHaveQuotaReservationByKey(ctx, k8sClient, devClusterQ.Name, lowWlKey)
+
+			ginkgo.By("Creating a job WITHOUT any WorkloadPriorityClass on the SAME flavor (will be pending)")
+			jobNoPriority := testingjob.MakeJob(jobName+"-no-priority", ns.Name).
+				Queue(kueue.LocalQueueName(devLocalQ.Name)).
+				Request(corev1.ResourceCPU, "5").
+				NodeSelector(instanceKey, "spot-untainted").
+				Obj()
+			util.MustCreate(ctx, k8sClient, jobNoPriority)
+
+			noPriorityWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobNoPriority.Name, jobNoPriority.UID), Namespace: ns.Name}
+
+			ginkgo.By("Verifying the no-priority workload is pending (not admitted)")
+			gomega.Eventually(func(g gomega.Gomega) {
+				wl := &kueue.Workload{}
+				g.Expect(k8sClient.Get(ctx, noPriorityWlKey, wl)).To(gomega.Succeed())
+				g.Expect(wl.Status.Conditions).Should(utiltesting.HaveConditionStatusFalseAndReason(kueue.WorkloadQuotaReserved, "Pending"))
+				g.Expect(wl.Spec.PriorityClassRef).To(gomega.BeNil())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Adding high WorkloadPriorityClass to the pending job via label update")
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdJob := &batchv1.Job{}
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(jobNoPriority), createdJob)).To(gomega.Succeed())
+				if createdJob.Labels == nil {
+					createdJob.Labels = make(map[string]string)
+				}
+				createdJob.Labels[constants.WorkloadPriorityClassLabel] = highWorkloadPriorityClass.Name
+				g.Expect(k8sClient.Update(ctx, createdJob)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Verifying the workload's priority is updated")
+			util.ExpectWorkloadsWithWorkloadPriority(ctx, k8sClient, highWorkloadPriorityClass.Name, highWorkloadPriorityClass.Value, noPriorityWlKey)
+
+			ginkgo.By("Verifying the low-priority workload gets preempted")
+			lowWl := &kueue.Workload{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, lowWlKey, lowWl)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			util.ExpectWorkloadsToBePreempted(ctx, k8sClient, lowWl)
+
+			ginkgo.By("Verifying the high-priority workload gets admitted")
+			util.ExpectWorkloadsToHaveQuotaReservationByKey(ctx, k8sClient, devClusterQ.Name, noPriorityWlKey)
```

  ```suggestion
  			util.ExpectWorkloadsToBeAdmittedByKey(ctx, k8sClient, devClusterQ.Name, noPriorityWlKey)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-10T18:12:57Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:1601** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-10T18:12:57Z
```diff
@@ -1545,6 +1545,64 @@ var _ = ginkgo.Describe("Interacting with scheduler", ginkgo.Ordered, ginkgo.Con
 				}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
 			})
 		})
+
+		ginkgo.It("should trigger preemption when WorkloadPriorityClass is added to a pending workload that had no priority class", framework.SlowSpec, func() {
+			ginkgo.By("Creating a low-priority job that gets admitted on spot-untainted flavor")
+			jobLow := testingjob.MakeJob(jobName+"-low", ns.Name).
+				WorkloadPriorityClass(lowWorkloadPriorityClass.Name).
+				Queue(kueue.LocalQueueName(devLocalQ.Name)).
+				Request(corev1.ResourceCPU, "5").
+				NodeSelector(instanceKey, "spot-untainted").
+				Obj()
+			util.MustCreate(ctx, k8sClient, jobLow)
+
+			lowWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobLow.Name, jobLow.UID), Namespace: ns.Name}
+
+			ginkgo.By("Verifying the low-priority workload is admitted")
+			util.ExpectWorkloadsToHaveQuotaReservationByKey(ctx, k8sClient, devClusterQ.Name, lowWlKey)
+
+			ginkgo.By("Creating a job WITHOUT any WorkloadPriorityClass on the SAME flavor (will be pending)")
+			jobNoPriority := testingjob.MakeJob(jobName+"-no-priority", ns.Name).
+				Queue(kueue.LocalQueueName(devLocalQ.Name)).
+				Request(corev1.ResourceCPU, "5").
+				NodeSelector(instanceKey, "spot-untainted").
+				Obj()
+			util.MustCreate(ctx, k8sClient, jobNoPriority)
+
+			noPriorityWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobNoPriority.Name, jobNoPriority.UID), Namespace: ns.Name}
+
+			ginkgo.By("Verifying the no-priority workload is pending (not admitted)")
+			gomega.Eventually(func(g gomega.Gomega) {
+				wl := &kueue.Workload{}
+				g.Expect(k8sClient.Get(ctx, noPriorityWlKey, wl)).To(gomega.Succeed())
+				g.Expect(wl.Status.Conditions).Should(utiltesting.HaveConditionStatusFalseAndReason(kueue.WorkloadQuotaReserved, "Pending"))
+				g.Expect(wl.Spec.PriorityClassRef).To(gomega.BeNil())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Adding high WorkloadPriorityClass to the pending job via label update")
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdJob := &batchv1.Job{}
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(jobNoPriority), createdJob)).To(gomega.Succeed())
+				if createdJob.Labels == nil {
+					createdJob.Labels = make(map[string]string)
+				}
+				createdJob.Labels[constants.WorkloadPriorityClassLabel] = highWorkloadPriorityClass.Name
+				g.Expect(k8sClient.Update(ctx, createdJob)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Verifying the workload's priority is updated")
+			util.ExpectWorkloadsWithWorkloadPriority(ctx, k8sClient, highWorkloadPriorityClass.Name, highWorkloadPriorityClass.Value, noPriorityWlKey)
+
+			ginkgo.By("Verifying the low-priority workload gets preempted")
+			lowWl := &kueue.Workload{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, lowWlKey, lowWl)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			util.ExpectWorkloadsToBePreempted(ctx, k8sClient, lowWl)
```

  ```suggestion
  			util.ExpectWorkloadsToBePreemptedByKey(ctx, k8sClient, lowWlKey)
  ```
  Maybe easier to do like this?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-10T18:20:53Z

- **pkg/controller/core/workload_controller_test.go:2739** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-10T18:20:53Z
```diff
@@ -2734,3 +2734,77 @@ func TestReconcile(t *testing.T) {
 		}
 	}
 }
+
+// TestWorkloadPriorityClassChanged tests the workloadPriorityClassChanged function.
+// (none -> some) should be detected as a change and trigger reconciliation.
```

  ```suggestion
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-10T18:20:59Z

- **pkg/controller/core/workload_controller.go:998** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-10T18:20:59Z
```diff
@@ -992,11 +992,12 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 
 func workloadPriorityChanged(old, new *kueue.Workload) bool {
 	// Updates to Pod Priority are not supported.
-	if !workload.IsWorkloadPriorityClass(old) || !workload.IsWorkloadPriorityClass(new) {
+	if workload.IsPodPriorityClass(old) || !workload.IsWorkloadPriorityClass(new) {
 		return false
 	}
+
```

  ```suggestion
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-10T18:23:31Z

- **pkg/controller/core/workload_controller_test.go:2806** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-10T18:23:31Z
```diff
@@ -2734,3 +2734,77 @@ func TestReconcile(t *testing.T) {
 		}
 	}
 }
+
+// TestWorkloadPriorityClassChanged tests the workloadPriorityClassChanged function.
+// (none -> some) should be detected as a change and trigger reconciliation.
+func TestWorkloadPriorityClassChanged(t *testing.T) {
+	testCases := map[string]struct {
+		oldWorkload *kueue.Workload
+		newWorkload *kueue.Workload
+		wantChanged bool
+	}{
+		"no priority class on either workload": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").Obj(),
+			wantChanged: false,
+		},
+		"same priority class on both workloads": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Obj(),
+			wantChanged: false,
+		},
+		"priority class changed from one to another": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-2").
+				Obj(),
+			wantChanged: true,
+		},
+		"priority class added (none -> some)": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Obj(),
+			wantChanged: true,
+		},
+		"priority class removed (some -> none) - blocked by validation": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").Obj(),
+			// Removal is blocked by validation, so we don't need to detect it
+			wantChanged: false,
+		},
+		"PodPriorityClass (not WorkloadPriorityClass) changed - should not trigger": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				PodPriorityClassRef("pod-priority-1").
+				Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				PodPriorityClassRef("pod-priority-2").
+				Obj(),
+			wantChanged: false,
+		},
+		"PodPriorityClass added (none -> some) - should not trigger": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				PodPriorityClassRef("pod-priority-1").
+				Obj(),
+			wantChanged: false,
+		},
+	}
+
+	for name, tc := range testCases {
+		t.Run(name, func(t *testing.T) {
+			gotChanged := workloadPriorityChanged(tc.oldWorkload, tc.newWorkload)
+			if gotChanged != tc.wantChanged {
+				t.Errorf("workloadPriorityClassChanged() = %v, want %v", gotChanged, tc.wantChanged)
```

  ```suggestion
  				t.Errorf("workloadPriorityChanged() = %v, want %v", gotChanged, tc.wantChanged)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-12T04:30:40Z

- **test/integration/singlecluster/scheduler/preemption_test.go:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-12T04:30:40Z
  It looks like we already have a test case in job_controller_test.go. The tests seem to cover the same thing. Do we need another one here? Or I missed something?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-12T04:33:04Z

@andrewseif could you please take a look at tests?

### Review by [@andrewseif](https://github.com/andrewseif) (commented) — 2026-01-12T07:52:07Z

- **test/integration/singlecluster/scheduler/preemption_test.go:1** by [@andrewseif](https://github.com/andrewseif) — 2026-01-12T07:52:07Z
  we can definitely remove the preemption one, let me take a look at it again and check if at least one is different

### Review by [@andrewseif](https://github.com/andrewseif) (commented) — 2026-01-12T09:03:20Z

- **test/integration/singlecluster/scheduler/preemption_test.go:1** by [@andrewseif](https://github.com/andrewseif) — 2026-01-12T09:03:20Z
  I have checked both, and basically preemption works on the workload level, while jobs test work on the job level, but since jobs are 1:1 to workloads, we can remove preemption

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-12T09:05:14Z

- **test/integration/singlecluster/scheduler/preemption_test.go:262** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-12T09:05:14Z
```diff
@@ -259,6 +259,7 @@ var _ = ginkgo.Describe("Preemption", func() {
 			cQPath := "/" + cq.Name
 			util.ExpectPreemptedCondition(ctx, k8sClient, kueue.InClusterQueueReason, metav1.ConditionTrue, lowWl, highWl, string(highWl.UID), "job-uid", cQPath, cQPath)
 		})
+
```

  ```suggestion
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-12T09:05:57Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:1601** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-12T09:05:58Z
```diff
@@ -1545,6 +1545,64 @@ var _ = ginkgo.Describe("Interacting with scheduler", ginkgo.Ordered, ginkgo.Con
 				}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
 			})
 		})
+
+		ginkgo.It("should trigger preemption when WorkloadPriorityClass is added to a pending workload that had no priority class", framework.SlowSpec, func() {
+			ginkgo.By("Creating a low-priority job that gets admitted on spot-untainted flavor")
+			jobLow := testingjob.MakeJob(jobName+"-low", ns.Name).
+				WorkloadPriorityClass(lowWorkloadPriorityClass.Name).
+				Queue(kueue.LocalQueueName(devLocalQ.Name)).
+				Request(corev1.ResourceCPU, "5").
+				NodeSelector(instanceKey, "spot-untainted").
+				Obj()
+			util.MustCreate(ctx, k8sClient, jobLow)
+
+			lowWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobLow.Name, jobLow.UID), Namespace: ns.Name}
+
+			ginkgo.By("Verifying the low-priority workload is admitted")
+			util.ExpectWorkloadsToHaveQuotaReservationByKey(ctx, k8sClient, devClusterQ.Name, lowWlKey)
+
+			ginkgo.By("Creating a job WITHOUT any WorkloadPriorityClass on the SAME flavor (will be pending)")
+			jobNoPriority := testingjob.MakeJob(jobName+"-no-priority", ns.Name).
+				Queue(kueue.LocalQueueName(devLocalQ.Name)).
+				Request(corev1.ResourceCPU, "5").
+				NodeSelector(instanceKey, "spot-untainted").
+				Obj()
+			util.MustCreate(ctx, k8sClient, jobNoPriority)
+
+			noPriorityWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobNoPriority.Name, jobNoPriority.UID), Namespace: ns.Name}
+
+			ginkgo.By("Verifying the no-priority workload is pending (not admitted)")
+			gomega.Eventually(func(g gomega.Gomega) {
+				wl := &kueue.Workload{}
+				g.Expect(k8sClient.Get(ctx, noPriorityWlKey, wl)).To(gomega.Succeed())
+				g.Expect(wl.Status.Conditions).Should(utiltesting.HaveConditionStatusFalseAndReason(kueue.WorkloadQuotaReserved, "Pending"))
+				g.Expect(wl.Spec.PriorityClassRef).To(gomega.BeNil())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Adding high WorkloadPriorityClass to the pending job via label update")
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdJob := &batchv1.Job{}
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(jobNoPriority), createdJob)).To(gomega.Succeed())
+				if createdJob.Labels == nil {
+					createdJob.Labels = make(map[string]string)
+				}
+				createdJob.Labels[constants.WorkloadPriorityClassLabel] = highWorkloadPriorityClass.Name
+				g.Expect(k8sClient.Update(ctx, createdJob)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Verifying the workload's priority is updated")
+			util.ExpectWorkloadsWithWorkloadPriority(ctx, k8sClient, highWorkloadPriorityClass.Name, highWorkloadPriorityClass.Value, noPriorityWlKey)
+
+			ginkgo.By("Verifying the low-priority workload gets preempted")
+			lowWl := &kueue.Workload{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, lowWlKey, lowWl)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			util.ExpectWorkloadsToBePreempted(ctx, k8sClient, lowWl)
```

  ```suggestion
  			util.ExpectWorkloadsToBePreemptedByKey(ctx, k8sClient, lowWlKey)
  ```

### Review by [@andrewseif](https://github.com/andrewseif) (commented) — 2026-01-12T09:08:00Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:1601** by [@andrewseif](https://github.com/andrewseif) — 2026-01-12T09:08:00Z
```diff
@@ -1545,6 +1545,64 @@ var _ = ginkgo.Describe("Interacting with scheduler", ginkgo.Ordered, ginkgo.Con
 				}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
 			})
 		})
+
+		ginkgo.It("should trigger preemption when WorkloadPriorityClass is added to a pending workload that had no priority class", framework.SlowSpec, func() {
+			ginkgo.By("Creating a low-priority job that gets admitted on spot-untainted flavor")
+			jobLow := testingjob.MakeJob(jobName+"-low", ns.Name).
+				WorkloadPriorityClass(lowWorkloadPriorityClass.Name).
+				Queue(kueue.LocalQueueName(devLocalQ.Name)).
+				Request(corev1.ResourceCPU, "5").
+				NodeSelector(instanceKey, "spot-untainted").
+				Obj()
+			util.MustCreate(ctx, k8sClient, jobLow)
+
+			lowWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobLow.Name, jobLow.UID), Namespace: ns.Name}
+
+			ginkgo.By("Verifying the low-priority workload is admitted")
+			util.ExpectWorkloadsToHaveQuotaReservationByKey(ctx, k8sClient, devClusterQ.Name, lowWlKey)
+
+			ginkgo.By("Creating a job WITHOUT any WorkloadPriorityClass on the SAME flavor (will be pending)")
+			jobNoPriority := testingjob.MakeJob(jobName+"-no-priority", ns.Name).
+				Queue(kueue.LocalQueueName(devLocalQ.Name)).
+				Request(corev1.ResourceCPU, "5").
+				NodeSelector(instanceKey, "spot-untainted").
+				Obj()
+			util.MustCreate(ctx, k8sClient, jobNoPriority)
+
+			noPriorityWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobNoPriority.Name, jobNoPriority.UID), Namespace: ns.Name}
+
+			ginkgo.By("Verifying the no-priority workload is pending (not admitted)")
+			gomega.Eventually(func(g gomega.Gomega) {
+				wl := &kueue.Workload{}
+				g.Expect(k8sClient.Get(ctx, noPriorityWlKey, wl)).To(gomega.Succeed())
+				g.Expect(wl.Status.Conditions).Should(utiltesting.HaveConditionStatusFalseAndReason(kueue.WorkloadQuotaReserved, "Pending"))
+				g.Expect(wl.Spec.PriorityClassRef).To(gomega.BeNil())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Adding high WorkloadPriorityClass to the pending job via label update")
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdJob := &batchv1.Job{}
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(jobNoPriority), createdJob)).To(gomega.Succeed())
+				if createdJob.Labels == nil {
+					createdJob.Labels = make(map[string]string)
+				}
+				createdJob.Labels[constants.WorkloadPriorityClassLabel] = highWorkloadPriorityClass.Name
+				g.Expect(k8sClient.Update(ctx, createdJob)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Verifying the workload's priority is updated")
+			util.ExpectWorkloadsWithWorkloadPriority(ctx, k8sClient, highWorkloadPriorityClass.Name, highWorkloadPriorityClass.Value, noPriorityWlKey)
+
+			ginkgo.By("Verifying the low-priority workload gets preempted")
+			lowWl := &kueue.Workload{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, lowWlKey, lowWl)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			util.ExpectWorkloadsToBePreempted(ctx, k8sClient, lowWl)
```

  I think this method doesn't exist, I checked in the utils but didn't find it?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-12T09:08:02Z

- **pkg/controller/jobframework/reconciler.go:968** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-12T09:08:02Z
```diff
@@ -965,9 +965,12 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 	return match, nil
 }
 
-// UpdateWorkloadPriority updates workload priority if object's kueue.x-k8s.io/priority-class label changed.
```

  Please revert this

### Review by [@andrewseif](https://github.com/andrewseif) (commented) — 2026-01-12T09:10:29Z

- **pkg/controller/jobframework/reconciler.go:968** by [@andrewseif](https://github.com/andrewseif) — 2026-01-12T09:10:28Z
```diff
@@ -965,9 +965,12 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 	return match, nil
 }
 
-// UpdateWorkloadPriority updates workload priority if object's kueue.x-k8s.io/priority-class label changed.
```

  done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-12T09:19:25Z

- **test/util/util.go:502** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-12T09:19:25Z
```diff
@@ -499,6 +499,23 @@ func ExpectWorkloadsToBePreempted(ctx context.Context, k8sClient client.Client,
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func ExpectWorkloadsToBePreemptedByKey(ctx context.Context, k8sClient client.Client, wlKeys ...client.ObjectKey) {
```

  Please reuse ExpectWorkloadsToBePreempted(). To get keys you can use `wlKeys := uniqueKeys(workloadKeys(wls))`.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-12T10:40:50Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:1587** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-12T10:40:50Z
```diff
@@ -1545,6 +1545,60 @@ var _ = ginkgo.Describe("Interacting with scheduler", ginkgo.Ordered, ginkgo.Con
 				}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
 			})
 		})
+
+		ginkgo.It("should trigger preemption when WorkloadPriorityClass is added to a pending workload that had no priority class", framework.SlowSpec, func() {
+			ginkgo.By("Creating a low-priority job that gets admitted on spot-untainted flavor")
+			jobLow := testingjob.MakeJob(jobName+"-low", ns.Name).
+				WorkloadPriorityClass(lowWorkloadPriorityClass.Name).
+				Queue(kueue.LocalQueueName(devLocalQ.Name)).
+				Request(corev1.ResourceCPU, "5").
+				NodeSelector(instanceKey, "spot-untainted").
+				Obj()
+			util.MustCreate(ctx, k8sClient, jobLow)
+
+			lowWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobLow.Name, jobLow.UID), Namespace: ns.Name}
+
+			ginkgo.By("Verifying the low-priority workload is admitted")
+			util.ExpectWorkloadsToHaveQuotaReservationByKey(ctx, k8sClient, devClusterQ.Name, lowWlKey)
+
+			ginkgo.By("Creating a job WITHOUT any WorkloadPriorityClass on the SAME flavor (will be pending)")
+			jobNoPriority := testingjob.MakeJob(jobName+"-no-priority", ns.Name).
+				Queue(kueue.LocalQueueName(devLocalQ.Name)).
+				Request(corev1.ResourceCPU, "5").
+				NodeSelector(instanceKey, "spot-untainted").
+				Obj()
+			util.MustCreate(ctx, k8sClient, jobNoPriority)
+
+			noPriorityWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobNoPriority.Name, jobNoPriority.UID), Namespace: ns.Name}
+
+			ginkgo.By("Verifying the no-priority workload is pending (not admitted)")
+			gomega.Eventually(func(g gomega.Gomega) {
+				wl := &kueue.Workload{}
+				g.Expect(k8sClient.Get(ctx, noPriorityWlKey, wl)).To(gomega.Succeed())
+				g.Expect(wl.Status.Conditions).Should(utiltesting.HaveConditionStatusFalseAndReason(kueue.WorkloadQuotaReserved, "Pending"))
+				g.Expect(wl.Spec.PriorityClassRef).To(gomega.BeNil())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Adding high WorkloadPriorityClass to the pending job via label update")
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdJob := &batchv1.Job{}
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(jobNoPriority), createdJob)).To(gomega.Succeed())
+				if createdJob.Labels == nil {
+					createdJob.Labels = make(map[string]string)
```

  ```suggestion
  					createdJob.Labels = make(map[string]string, 1)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-12T10:41:40Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:1600** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-12T10:41:40Z
```diff
@@ -1545,6 +1545,60 @@ var _ = ginkgo.Describe("Interacting with scheduler", ginkgo.Ordered, ginkgo.Con
 				}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
 			})
 		})
+
+		ginkgo.It("should trigger preemption when WorkloadPriorityClass is added to a pending workload that had no priority class", framework.SlowSpec, func() {
+			ginkgo.By("Creating a low-priority job that gets admitted on spot-untainted flavor")
+			jobLow := testingjob.MakeJob(jobName+"-low", ns.Name).
+				WorkloadPriorityClass(lowWorkloadPriorityClass.Name).
+				Queue(kueue.LocalQueueName(devLocalQ.Name)).
+				Request(corev1.ResourceCPU, "5").
+				NodeSelector(instanceKey, "spot-untainted").
+				Obj()
+			util.MustCreate(ctx, k8sClient, jobLow)
+
+			lowWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobLow.Name, jobLow.UID), Namespace: ns.Name}
+
+			ginkgo.By("Verifying the low-priority workload is admitted")
+			util.ExpectWorkloadsToHaveQuotaReservationByKey(ctx, k8sClient, devClusterQ.Name, lowWlKey)
+
+			ginkgo.By("Creating a job WITHOUT any WorkloadPriorityClass on the SAME flavor (will be pending)")
+			jobNoPriority := testingjob.MakeJob(jobName+"-no-priority", ns.Name).
+				Queue(kueue.LocalQueueName(devLocalQ.Name)).
+				Request(corev1.ResourceCPU, "5").
+				NodeSelector(instanceKey, "spot-untainted").
+				Obj()
+			util.MustCreate(ctx, k8sClient, jobNoPriority)
+
+			noPriorityWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobNoPriority.Name, jobNoPriority.UID), Namespace: ns.Name}
+
+			ginkgo.By("Verifying the no-priority workload is pending (not admitted)")
+			gomega.Eventually(func(g gomega.Gomega) {
+				wl := &kueue.Workload{}
+				g.Expect(k8sClient.Get(ctx, noPriorityWlKey, wl)).To(gomega.Succeed())
+				g.Expect(wl.Status.Conditions).Should(utiltesting.HaveConditionStatusFalseAndReason(kueue.WorkloadQuotaReserved, "Pending"))
+				g.Expect(wl.Spec.PriorityClassRef).To(gomega.BeNil())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Adding high WorkloadPriorityClass to the pending job via label update")
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdJob := &batchv1.Job{}
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(jobNoPriority), createdJob)).To(gomega.Succeed())
+				if createdJob.Labels == nil {
+					createdJob.Labels = make(map[string]string)
+				}
+				createdJob.Labels[constants.WorkloadPriorityClassLabel] = highWorkloadPriorityClass.Name
+				g.Expect(k8sClient.Update(ctx, createdJob)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Verifying the workload's priority is updated")
+			util.ExpectWorkloadsWithWorkloadPriority(ctx, k8sClient, highWorkloadPriorityClass.Name, highWorkloadPriorityClass.Value, noPriorityWlKey)
+
+			ginkgo.By("Verifying the low-priority workload gets preempted")
+			util.ExpectWorkloadsToBePreemptedByKey(ctx, k8sClient, lowWlKey)
+
+			ginkgo.By("Verifying the high-priority workload gets admitted")
+			util.ExpectWorkloadsToBeAdmittedByKeys(ctx, k8sClient, lowWlKey)
```

  ```suggestion
  			util.ExpectWorkloadsToBeAdmittedByKeys(ctx, k8sClient, noPriorityWlKey)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-12T10:47:40Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:1562** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-12T10:47:40Z
```diff
@@ -1545,6 +1545,60 @@ var _ = ginkgo.Describe("Interacting with scheduler", ginkgo.Ordered, ginkgo.Con
 				}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
 			})
 		})
+
+		ginkgo.It("should trigger preemption when WorkloadPriorityClass is added to a pending workload that had no priority class", framework.SlowSpec, func() {
+			ginkgo.By("Creating a low-priority job that gets admitted on spot-untainted flavor")
+			jobLow := testingjob.MakeJob(jobName+"-low", ns.Name).
+				WorkloadPriorityClass(lowWorkloadPriorityClass.Name).
+				Queue(kueue.LocalQueueName(devLocalQ.Name)).
+				Request(corev1.ResourceCPU, "5").
+				NodeSelector(instanceKey, "spot-untainted").
+				Obj()
+			util.MustCreate(ctx, k8sClient, jobLow)
+
+			lowWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobLow.Name, jobLow.UID), Namespace: ns.Name}
+
+			ginkgo.By("Verifying the low-priority workload is admitted")
+			util.ExpectWorkloadsToHaveQuotaReservationByKey(ctx, k8sClient, devClusterQ.Name, lowWlKey)
```

  ```suggestion
  			util.ExpectWorkloadsToBeAdmittedByKeys(ctx, k8sClient, lowWlKey)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-12T10:49:28Z

LGTM

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-12T15:03:28Z

- **CHANGELOG/CHANGELOG-0.15.md:231** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-12T15:03:28Z
```diff
@@ -228,6 +228,7 @@ Changes since `v0.14.0`:
   sends the ResourceVersion of the workload being admitted for comparing by kube-apiserver. 
   This fixes the race-condition issue that Workload conditions added concurrently by other controllers
   could be removed during scheduling. (#7246, @mszadkow)
+- Adding a priority class to a workload (none -> some) is now reconciled (#8480, @andrewseif)
```

  No, please revert this.
  
  Just update PR description :)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-12T15:04:02Z

- **CHANGELOG/CHANGELOG-0.15.md:231** by [@mimowo](https://github.com/mimowo) — 2026-01-12T15:04:02Z
```diff
@@ -228,6 +228,7 @@ Changes since `v0.14.0`:
   sends the ResourceVersion of the workload being admitted for comparing by kube-apiserver. 
   This fixes the race-condition issue that Workload conditions added concurrently by other controllers
   could be removed during scheduling. (#7246, @mszadkow)
+- Adding a priority class to a workload (none -> some) is now reconciled (#8480, @andrewseif)
```

  revert, we add the release notes into PR descirption, then automation generates the CHANGELOG during release

### Review by [@andrewseif](https://github.com/andrewseif) (commented) — 2026-01-12T15:07:45Z

- **CHANGELOG/CHANGELOG-0.15.md:231** by [@andrewseif](https://github.com/andrewseif) — 2026-01-12T15:07:45Z
```diff
@@ -228,6 +228,7 @@ Changes since `v0.14.0`:
   sends the ResourceVersion of the workload being admitted for comparing by kube-apiserver. 
   This fixes the race-condition issue that Workload conditions added concurrently by other controllers
   could be removed during scheduling. (#7246, @mszadkow)
+- Adding a priority class to a workload (none -> some) is now reconciled (#8480, @andrewseif)
```

  sorry, didn't know about that, should be fixed now

### Review by [@andrewseif](https://github.com/andrewseif) (commented) — 2026-01-12T15:08:00Z

- **CHANGELOG/CHANGELOG-0.15.md:231** by [@andrewseif](https://github.com/andrewseif) — 2026-01-12T15:08:00Z
```diff
@@ -228,6 +228,7 @@ Changes since `v0.14.0`:
   sends the ResourceVersion of the workload being admitted for comparing by kube-apiserver. 
   This fixes the race-condition issue that Workload conditions added concurrently by other controllers
   could be removed during scheduling. (#7246, @mszadkow)
+- Adding a priority class to a workload (none -> some) is now reconciled (#8480, @andrewseif)
```

  done :) tyvm for the help 🙏

### Review by [@olekzabl](https://github.com/olekzabl) (approved) — 2026-01-12T22:32:33Z

- **pkg/controller/core/workload_controller.go:994** by [@olekzabl](https://github.com/olekzabl) — 2026-01-12T22:14:02Z
```diff
@@ -992,11 +992,11 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 
 func workloadPriorityChanged(old, new *kueue.Workload) bool {
 	// Updates to Pod Priority are not supported.
```

  ```suggestion
  	// Updates to Pod Priority and deletions of Workload Priority are not supported.
  ```
  I'm proposing it to clarify (for future readers) that the new (asymmetric) form of the line below is intended, not a bug.

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:1564** by [@olekzabl](https://github.com/olekzabl) — 2026-01-12T22:30:24Z
```diff
@@ -1545,6 +1545,55 @@ var _ = ginkgo.Describe("Interacting with scheduler", ginkgo.Ordered, ginkgo.Con
 				}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
 			})
 		})
+
+		ginkgo.It("should trigger preemption when WorkloadPriorityClass is added to a pending workload that had no priority class", framework.SlowSpec, func() {
+			ginkgo.By("Creating a low-priority job that gets admitted on spot-untainted flavor")
+			jobLow := testingjob.MakeJob(jobName+"-low", ns.Name).
+				WorkloadPriorityClass(lowWorkloadPriorityClass.Name).
+				Queue(kueue.LocalQueueName(devLocalQ.Name)).
+				Request(corev1.ResourceCPU, "5").
+				NodeSelector(instanceKey, "spot-untainted").
+				Obj()
+			util.MustCreate(ctx, k8sClient, jobLow)
+
+			lowWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobLow.Name, jobLow.UID), Namespace: ns.Name}
+
+			ginkgo.By("Verifying the low-priority workload is admitted")
+			util.ExpectWorkloadsToBeAdmittedByKeys(ctx, k8sClient, lowWlKey)
+
+			ginkgo.By("Creating a job WITHOUT any WorkloadPriorityClass on the SAME flavor (will be pending)")
```

  Super-nit: I wouldn't all-cap "SAME" here; for me, "same flavor" feels quite expected, given the overall goal of this test.
  
  (For context, all-capsing seems generally rare in this code; I'd rather keep it for cases where we anticipate _likely_ confusion)
