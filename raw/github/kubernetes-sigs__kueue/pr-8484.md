# PR #8484: [TAS] Cache non-TAS pod usage

**Summary**: [TAS] Cache non-TAS pod usage

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8484

**Last updated**: 2026-03-09T09:25:50Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@gabesaba](https://github.com/gabesaba)
- **Branch**: `cache_non_tas` → `main`
- **Created**: 2026-01-09T03:19:10Z
- **Updated**: 2026-03-09T09:25:50Z
- **Merged**: 2026-01-20T09:02:56Z
- **Closed**: 2026-01-20T09:02:56Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92)
- **Changed files**: 11   **+506 / -82**

## Description

#### What type of PR is this?
/kind bug

#### What this PR does / why we need it:
Improve performance of TAS by caching pods used to compute non-TAS usage.

#### Which issue(s) this PR fixes:
Fixes #8449

See https://github.com/kubernetes-sigs/kueue/issues/8449#issuecomment-3725934428 for alternatives considered

#### Special notes for your reviewer:
Considered WIP until I add an integration test (or verify that one already exists)

#### Does this PR introduce a user-facing change?
```release-note
TAS: significantly improves scheduling performance by replacing Pod listing with an event-driven
cache for non-TAS Pods, thereby avoiding expensive DeepCopy operations during each scheduling cycle.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-09T03:19:22Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8484#" title="Author self-approved">gabesaba</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [gabesaba]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-01-09T03:19:23Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 1cfa47d82a7a2e007b8e67882b878570ec13292a |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/696e4704315de100088d0b0b |

### Comment by [@gabesaba](https://github.com/gabesaba) — 2026-01-10T08:00:23Z

PR is still WIP until I add integration or e2e test(s). I verified that none exist - tests still pass after introducing errors in new reconciler.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-12T11:35:30Z

> PR is still WIP until I add integration or e2e test(s). I verified that none exist - tests still pass after introducing errors in new reconciler.

+1, thank you for checking

EDIT: Seems like we can reliably test this with integration tests which we have a better control over the set of created Pods

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-19T11:10:14Z

@gabesaba I can see many comments resolved about 1h ago, but no changes pushed. Are you still working on the code, or just forgot to push the changes?

### Comment by [@gabesaba](https://github.com/gabesaba) — 2026-01-19T11:14:54Z

> @gabesaba I can see many comments resolved about 1h ago, but no changes pushed. Are you still working on the code, or just forgot to push the changes?

Still working on the code.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-19T12:48:07Z

Proposal to make it a bit more understandable to end users
/release-note-edit
```release-note
TAS: significantly improves scheduling performance by replacing Pod listing with an event-driven
cache for non-TAS Pods, thereby avoiding expensive DeepCopy operations during each scheduling cycle.
```

### Comment by [@gabesaba](https://github.com/gabesaba) — 2026-01-19T13:56:29Z

[Rebase](https://github.com/kubernetes-sigs/kueue/compare/b7048fc7ca201387d30d39d0ad8e9f4f160fc9fc..bad9730f0519d799b392025cacbcc2faad832c11)

[Address comments](https://github.com/kubernetes-sigs/kueue/compare/bad9730f0519d799b392025cacbcc2faad832c11..fda6c64ef0e5e60d06c5882e44b6108ee72ea177)

### Comment by [@gabesaba](https://github.com/gabesaba) — 2026-01-19T14:24:16Z

Will need to cherry-pick manually - just tested out locally and there are merge conflicts

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-20T08:34:21Z

/lgtm
Thank you 👍 LGTM overall, and I haven't found any bugs, all my comments are around readability, testing, and small perf improvements / good practices. We can improve that in a follow up. 
/cherrypick release-0.15
/cherrypick release-0.14

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-01-20T08:34:24Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.14`, `release-0.15` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8484#issuecomment-3771661220):

>/lgtm
>Thank you 👍 LGTM overall, and I haven't found any bugs, all my comments are around readability and testing. We can improve that in a follow up. 
>/cherrypick release-0.15
>/cherrypick release-0.14


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-20T08:34:29Z

LGTM label has been added.  <details>Git tree hash: d0b7f5d5894bb6e7d3f95336ae9255fee852e693</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-01-20T09:03:35Z

@mimowo: #8484 failed to apply on top of branch "release-0.14":
```
Applying: Cache non-TAS pod usage
Using index info to reconstruct a base tree...
M	pkg/cache/scheduler/tas_cache.go
M	pkg/cache/scheduler/tas_cache_test.go
M	pkg/cache/scheduler/tas_flavor.go
M	pkg/cache/scheduler/tas_flavor_snapshot.go
M	pkg/controller/tas/controllers.go
M	pkg/scheduler/scheduler_tas_test.go
M	test/integration/singlecluster/tas/suite_test.go
M	test/integration/singlecluster/tas/tas_test.go
Falling back to patching base and 3-way merge...
Auto-merging test/integration/singlecluster/tas/tas_test.go
CONFLICT (content): Merge conflict in test/integration/singlecluster/tas/tas_test.go
Auto-merging test/integration/singlecluster/tas/suite_test.go
Auto-merging pkg/scheduler/scheduler_tas_test.go
CONFLICT (content): Merge conflict in pkg/scheduler/scheduler_tas_test.go
Auto-merging pkg/controller/tas/controllers.go
Auto-merging pkg/cache/scheduler/tas_flavor_snapshot.go
Auto-merging pkg/cache/scheduler/tas_flavor.go
Auto-merging pkg/cache/scheduler/tas_cache_test.go
Auto-merging pkg/cache/scheduler/tas_cache.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Cache non-TAS pod usage

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8484#issuecomment-3771661220):

>/lgtm
>Thank you 👍 LGTM overall, and I haven't found any bugs, all my comments are around readability, testing, and small perf improvements / good practices. We can improve that in a follow up. 
>/cherrypick release-0.15
>/cherrypick release-0.14


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-01-20T09:04:10Z

@mimowo: #8484 failed to apply on top of branch "release-0.15":
```
Applying: Cache non-TAS pod usage
Using index info to reconstruct a base tree...
M	pkg/cache/scheduler/tas_cache_test.go
M	pkg/cache/scheduler/tas_flavor_snapshot.go
M	pkg/controller/tas/controllers.go
M	pkg/scheduler/scheduler_tas_test.go
M	test/integration/singlecluster/tas/suite_test.go
M	test/integration/singlecluster/tas/tas_test.go
Falling back to patching base and 3-way merge...
Auto-merging test/integration/singlecluster/tas/tas_test.go
Auto-merging test/integration/singlecluster/tas/suite_test.go
Auto-merging pkg/scheduler/scheduler_tas_test.go
CONFLICT (content): Merge conflict in pkg/scheduler/scheduler_tas_test.go
Auto-merging pkg/controller/tas/controllers.go
Auto-merging pkg/cache/scheduler/tas_flavor_snapshot.go
Auto-merging pkg/cache/scheduler/tas_cache_test.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Cache non-TAS pod usage

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8484#issuecomment-3771661220):

>/lgtm
>Thank you 👍 LGTM overall, and I haven't found any bugs, all my comments are around readability, testing, and small perf improvements / good practices. We can improve that in a follow up. 
>/cherrypick release-0.15
>/cherrypick release-0.14


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-09T08:06:00Z

- **pkg/cache/scheduler/tas_flavor.go:147** by [@mimowo](https://github.com/mimowo) — 2026-01-09T08:06:00Z
```diff
@@ -151,20 +144,13 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	for domainID, usage := range c.usage {
 		snapshot.addTASUsage(domainID, usage)
 	}
-	for _, pod := range pods {
-		// skip unscheduled or terminal pods as they don't use any capacity
-		if len(pod.Spec.NodeName) == 0 || utilpod.IsTerminated(&pod) {
-			continue
-		}
-		if domainID, ok := nodeToDomain[pod.Spec.NodeName]; ok {
-			requests := resourcehelpers.PodRequests(&pod, resourcehelpers.PodResourcesOptions{})
-			usage := resources.NewRequests(requests)
-			snapshot.addNonTASUsage(domainID, usage)
+	for _, podUsage := range c.nonTasUsageCache.podUsage {
```

  This still requires iteration over pods, so has the same complexity - O(# of pods) for building the snapshot.
  
  I think it is better to aggregate the usage per node in cache so that we get O(# of nodes) performance when building the snapshot.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-01-09T09:15:11Z

- **pkg/cache/scheduler/tas_flavor.go:147** by [@PBundyra](https://github.com/PBundyra) — 2026-01-09T09:15:11Z
```diff
@@ -151,20 +144,13 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	for domainID, usage := range c.usage {
 		snapshot.addTASUsage(domainID, usage)
 	}
-	for _, pod := range pods {
-		// skip unscheduled or terminal pods as they don't use any capacity
-		if len(pod.Spec.NodeName) == 0 || utilpod.IsTerminated(&pod) {
-			continue
-		}
-		if domainID, ok := nodeToDomain[pod.Spec.NodeName]; ok {
-			requests := resourcehelpers.PodRequests(&pod, resourcehelpers.PodResourcesOptions{})
-			usage := resources.NewRequests(requests)
-			snapshot.addNonTASUsage(domainID, usage)
+	for _, podUsage := range c.nonTasUsageCache.podUsage {
```

  
  That's true the complexity would still be O(pods), but please note pprofiling shows majority of the time is spent on copying the data from the informer and calling PodRequests function on every Pod.
  
  Iterating over Pods and adding their usage to the snapshot was only small fraction of the time. Not leaning towards any of the approaches, just giving some more context based on data.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-09T09:47:01Z

- **pkg/cache/scheduler/tas_flavor.go:147** by [@mimowo](https://github.com/mimowo) — 2026-01-09T09:47:01Z
```diff
@@ -151,20 +144,13 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	for domainID, usage := range c.usage {
 		snapshot.addTASUsage(domainID, usage)
 	}
-	for _, pod := range pods {
-		// skip unscheduled or terminal pods as they don't use any capacity
-		if len(pod.Spec.NodeName) == 0 || utilpod.IsTerminated(&pod) {
-			continue
-		}
-		if domainID, ok := nodeToDomain[pod.Spec.NodeName]; ok {
-			requests := resourcehelpers.PodRequests(&pod, resourcehelpers.PodResourcesOptions{})
-			usage := resources.NewRequests(requests)
-			snapshot.addNonTASUsage(domainID, usage)
+	for _, podUsage := range c.nonTasUsageCache.podUsage {
```

  Ah, got it, thanks for pointing out this would already save on DeepCopy which is the main contributor. 
  
  So, if this indeed simplifies the code I'm ok to defer using NodeName as a key until proven to be needed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-09T09:51:45Z

- **pkg/cache/scheduler/tas_flavor.go:147** by [@mimowo](https://github.com/mimowo) — 2026-01-09T09:51:44Z
```diff
@@ -151,20 +144,13 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	for domainID, usage := range c.usage {
 		snapshot.addTASUsage(domainID, usage)
 	}
-	for _, pod := range pods {
-		// skip unscheduled or terminal pods as they don't use any capacity
-		if len(pod.Spec.NodeName) == 0 || utilpod.IsTerminated(&pod) {
-			continue
-		}
-		if domainID, ok := nodeToDomain[pod.Spec.NodeName]; ok {
-			requests := resourcehelpers.PodRequests(&pod, resourcehelpers.PodResourcesOptions{})
-			usage := resources.NewRequests(requests)
-			snapshot.addNonTASUsage(domainID, usage)
+	for _, podUsage := range c.nonTasUsageCache.podUsage {
```

  Still, I' not totally clear if this simplifies the code, because we anyway need to do this mapping from pods to nodes at some layer, so IIUC this would be just moving the layer of aggregation to the controller.
  
  Now I read https://github.com/kubernetes-sigs/kueue/issues/8449#issuecomment-3725934428, but this looks misleading:
  
  > The risk of the proposal in issue description is the complexity of computing the cached value accurately, as it can change based on on 1) pod 2) node 3) resource flavor 4) topology changes. I think that doing an incremental update on pod changes, and full rebuild of cache on any other change (with appropriate event filters) would the least risky.
  
  because:
  a) resource flavor in TAS is immutable, as well as topology spec. 
  b) building of such a map: NodeName -> non TAS pod usage, isn't dependent on RF or Topology. We could still build it just based on Pods, without event tracking the nodes, just aggregate based on spec.NodeName. 
  
  So I think the controller could still track only Pods exactly as currently, just store the data in cache already aggregated.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-09T17:43:37Z

- **pkg/cache/scheduler/tas_flavor.go:147** by [@gabesaba](https://github.com/gabesaba) — 2026-01-09T17:43:37Z
```diff
@@ -151,20 +144,13 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	for domainID, usage := range c.usage {
 		snapshot.addTASUsage(domainID, usage)
 	}
-	for _, pod := range pods {
-		// skip unscheduled or terminal pods as they don't use any capacity
-		if len(pod.Spec.NodeName) == 0 || utilpod.IsTerminated(&pod) {
-			continue
-		}
-		if domainID, ok := nodeToDomain[pod.Spec.NodeName]; ok {
-			requests := resourcehelpers.PodRequests(&pod, resourcehelpers.PodResourcesOptions{})
-			usage := resources.NewRequests(requests)
-			snapshot.addNonTASUsage(domainID, usage)
+	for _, podUsage := range c.nonTasUsageCache.podUsage {
```

  You're right @mimowo, I was considering the complexity/updates as if it were TAS usage, but it is not.
  
  I think I can make the change, while keeping it relatively simple. However, if this is not a performance hotspot, I still argue to keep the simplest possible code, and compute the node value each scheduling loop.
  
  Which of the options do you prefer: 
  1) we move the aggregation logic next to the caching logic, without yet caching it. if it shows to be a hotspot, we can cache it later.
  
  2) implement aggregation at node level. Probably requires 2 maps, unless we want to rebuild the entire cache upon each pod update (or have O(N pods) update)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-09T17:55:17Z

- **pkg/cache/scheduler/tas_flavor.go:147** by [@mimowo](https://github.com/mimowo) — 2026-01-09T17:55:17Z
```diff
@@ -151,20 +144,13 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	for domainID, usage := range c.usage {
 		snapshot.addTASUsage(domainID, usage)
 	}
-	for _, pod := range pods {
-		// skip unscheduled or terminal pods as they don't use any capacity
-		if len(pod.Spec.NodeName) == 0 || utilpod.IsTerminated(&pod) {
-			continue
-		}
-		if domainID, ok := nodeToDomain[pod.Spec.NodeName]; ok {
-			requests := resourcehelpers.PodRequests(&pod, resourcehelpers.PodResourcesOptions{})
-			usage := resources.NewRequests(requests)
-			snapshot.addNonTASUsage(domainID, usage)
+	for _, podUsage := range c.nonTasUsageCache.podUsage {
```

  I'm totally fine with (1.), I'm just checking if maybe the complexity is the same, just code location is different.
  
  For (2.) I was simply thinking about maintaining a single map nodeName -> resourceUsage. Whenever we observe a non TAS Pod we addOrUpdate, or subtract (if the Pod is terminating / terminated /deleted). If this simple map does not work for some reason, I'm totally ok with (1.)

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-10T05:51:03Z

- **pkg/cache/scheduler/tas_flavor.go:147** by [@gabesaba](https://github.com/gabesaba) — 2026-01-10T05:51:03Z
```diff
@@ -151,20 +144,13 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	for domainID, usage := range c.usage {
 		snapshot.addTASUsage(domainID, usage)
 	}
-	for _, pod := range pods {
-		// skip unscheduled or terminal pods as they don't use any capacity
-		if len(pod.Spec.NodeName) == 0 || utilpod.IsTerminated(&pod) {
-			continue
-		}
-		if domainID, ok := nodeToDomain[pod.Spec.NodeName]; ok {
-			requests := resourcehelpers.PodRequests(&pod, resourcehelpers.PodResourcesOptions{})
-			usage := resources.NewRequests(requests)
-			snapshot.addNonTASUsage(domainID, usage)
+	for _, podUsage := range c.nonTasUsageCache.podUsage {
```

  I think that the simple version of 2 doesn't work, as when a pod is mutated/deleted, we can't guarantee that we subtract the correct usage from the correct node (e.g. suppose that a pod deletion occurs, and by the time we trigger the reconcilation, the get on the pod returns not found. or imagine many other variations on this), without rebuilding the whole cache

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-10T05:52:18Z

- **pkg/cache/scheduler/tas_flavor.go:147** by [@gabesaba](https://github.com/gabesaba) — 2026-01-10T05:52:18Z
```diff
@@ -151,20 +144,13 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	for domainID, usage := range c.usage {
 		snapshot.addTASUsage(domainID, usage)
 	}
-	for _, pod := range pods {
-		// skip unscheduled or terminal pods as they don't use any capacity
-		if len(pod.Spec.NodeName) == 0 || utilpod.IsTerminated(&pod) {
-			continue
-		}
-		if domainID, ok := nodeToDomain[pod.Spec.NodeName]; ok {
-			requests := resourcehelpers.PodRequests(&pod, resourcehelpers.PodResourcesOptions{})
-			usage := resources.NewRequests(requests)
-			snapshot.addNonTASUsage(domainID, usage)
+	for _, podUsage := range c.nonTasUsageCache.podUsage {
```

  > I'm totally fine with (1.), I'm just checking if maybe the complexity is the same, just code location is different.
  
  Yes, in this case, the complexity will be the same. However, I don't envision any more performance issues as we got rid of the deepcopy and CPUs are fast :)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-12T11:29:07Z

- **pkg/cache/scheduler/tas_flavor.go:147** by [@mimowo](https://github.com/mimowo) — 2026-01-12T11:29:07Z
```diff
@@ -151,20 +144,13 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	for domainID, usage := range c.usage {
 		snapshot.addTASUsage(domainID, usage)
 	}
-	for _, pod := range pods {
-		// skip unscheduled or terminal pods as they don't use any capacity
-		if len(pod.Spec.NodeName) == 0 || utilpod.IsTerminated(&pod) {
-			continue
-		}
-		if domainID, ok := nodeToDomain[pod.Spec.NodeName]; ok {
-			requests := resourcehelpers.PodRequests(&pod, resourcehelpers.PodResourcesOptions{})
-			usage := resources.NewRequests(requests)
-			snapshot.addNonTASUsage(domainID, usage)
+	for _, podUsage := range c.nonTasUsageCache.podUsage {
```

  > Yes, in this case, the complexity will be the same. However, I don't envision any more performance issues as we got rid of the deepcopy and CPUs are fast :)
  
  Ok, I'm good with that.
  
  >  (e.g. suppose that a pod deletion occurs, and by the time we trigger the reconcilation, the get on the pod returns not found. or imagine many other variations on this),
  
  Ah I see what you mean. I think it is only a problem if we subtract / delete from within the Reconcile function ( which is delayed). 
  
  I was actually thinking about using just informer and hearing on the "add", "delete" events for pods, without the need for reconcile actually (or reconcile being empty). I was thinking about filtering Pod updates only if transitioning between states: 
  - "pending -> scheduled" (add)
  - "running" -> "finished or deleted" (subtract)
  
  The current implementation triggers reconcile for every update to the Pod (eg. to removing Pod finalizers or scheduling gates which happen at scale a lot). I'm not totally sure this will create a bottleneck, but it feels it can create hickups for accounting the cache.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-12T11:31:17Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:48** by [@mimowo](https://github.com/mimowo) — 2026-01-12T11:31:17Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+}
+
+type podUsageValue struct {
+	node  string
+	usage resources.Requests
+}
+
+// addOrUpdate assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) addOrUpdate(pod corev1.Pod) {
+	// skip unscheduled or terminal pods as they don't use any capacity
+	if len(pod.Spec.NodeName) == 0 || utilpod.IsTerminated(&pod) {
+		n.delete(pod)
+		return
+	}
+	if utiltas.IsTAS(&pod) {
```

  I would suggest moving this pre-check into the filters for the "controller" to skip triggering Reconciles for tas pods.
  
  EDIT: I was thinking about adding the extra check `return !utiltas.IsTAS(&pod)` in filters. On the second, but I'm ok to keep in this PR minimal version (as is)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-12T11:32:03Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:44** by [@mimowo](https://github.com/mimowo) — 2026-01-12T11:32:04Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+}
+
+type podUsageValue struct {
+	node  string
+	usage resources.Requests
+}
+
+// addOrUpdate assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) addOrUpdate(pod corev1.Pod) {
+	// skip unscheduled or terminal pods as they don't use any capacity
+	if len(pod.Spec.NodeName) == 0 || utilpod.IsTerminated(&pod) {
```

  This happens already inside the lock, I would prefer to move it outside in the new controller code (either inside the Reconcile, or even filters. wdyt?
  
  EDIT: similarly here, it is an optimization we could do later if proven to be needed. I'm ok as is.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-13T12:41:55Z

- **pkg/cache/scheduler/tas_flavor.go:147** by [@mimowo](https://github.com/mimowo) — 2026-01-13T12:41:54Z
```diff
@@ -151,20 +144,13 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	for domainID, usage := range c.usage {
 		snapshot.addTASUsage(domainID, usage)
 	}
-	for _, pod := range pods {
-		// skip unscheduled or terminal pods as they don't use any capacity
-		if len(pod.Spec.NodeName) == 0 || utilpod.IsTerminated(&pod) {
-			continue
-		}
-		if domainID, ok := nodeToDomain[pod.Spec.NodeName]; ok {
-			requests := resourcehelpers.PodRequests(&pod, resourcehelpers.PodResourcesOptions{})
-			usage := resources.NewRequests(requests)
-			snapshot.addNonTASUsage(domainID, usage)
+	for _, podUsage := range c.nonTasUsageCache.podUsage {
```

  On the second thought making the filters respond "true" unconditionally is actually what would happen currently inside the controller-runtime, so I'm ok as is. We can revisit the possible optimizations if needed. Still having some integration tests would be very helpful to ensure we can safely refactor if needed in the future.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-13T13:26:54Z

- **pkg/controller/tas/non_tas_usage_controller.go:64** by [@mimowo](https://github.com/mimowo) — 2026-01-13T13:26:54Z
```diff
@@ -0,0 +1,97 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	schdcache "sigs.k8s.io/kueue/pkg/cache/scheduler"
+)
+
+func newNonTasUsageReconciler(client client.Client, cache *schdcache.Cache) *NonTasUsageReconciler {
+	return &NonTasUsageReconciler{
+		Client: client,
+		Cache:  cache,
+	}
+}
+
+// NonTasUsageReconciler monitors pods to update
+// the TAS cache with non-TAS usage.
+type NonTasUsageReconciler struct {
+	client.Client
+	Scheme *runtime.Scheme
+	Cache  *schdcache.Cache
+}
+
+var _ reconcile.Reconciler = (*NonTasUsageReconciler)(nil)
+var _ predicate.TypedPredicate[*corev1.Pod] = (*NonTasUsageReconciler)(nil)
+
+//+kubebuilder:rbac:groups=core,resources=pods,verbs=get;list;watch
+
+func (r *NonTasUsageReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := klog.FromContext(ctx)
+	log.V(3).Info("Non-TAS usage cachd reconciling", "pod", req.NamespacedName)
+
+	var pod corev1.Pod
+	if err := r.Get(ctx, req.NamespacedName, &pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	// delete
+	if !pod.DeletionTimestamp.IsZero() {
```

  Setting deletionTimestamp does not release the node usage immediately, as the  graceful termination period can be arbitrarily long. I think we can call DeletePod  only after the Pod is terminal (phase =Failed or Succeeded), or it is notFound already.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-13T13:31:04Z

- **pkg/controller/tas/non_tas_usage_controller.go:95** by [@mimowo](https://github.com/mimowo) — 2026-01-13T13:31:05Z
```diff
@@ -0,0 +1,97 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	schdcache "sigs.k8s.io/kueue/pkg/cache/scheduler"
+)
+
+func newNonTasUsageReconciler(client client.Client, cache *schdcache.Cache) *NonTasUsageReconciler {
+	return &NonTasUsageReconciler{
+		Client: client,
+		Cache:  cache,
+	}
+}
+
+// NonTasUsageReconciler monitors pods to update
+// the TAS cache with non-TAS usage.
+type NonTasUsageReconciler struct {
+	client.Client
+	Scheme *runtime.Scheme
+	Cache  *schdcache.Cache
+}
+
+var _ reconcile.Reconciler = (*NonTasUsageReconciler)(nil)
+var _ predicate.TypedPredicate[*corev1.Pod] = (*NonTasUsageReconciler)(nil)
+
+//+kubebuilder:rbac:groups=core,resources=pods,verbs=get;list;watch
+
+func (r *NonTasUsageReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := klog.FromContext(ctx)
+	log.V(3).Info("Non-TAS usage cachd reconciling", "pod", req.NamespacedName)
+
+	var pod corev1.Pod
+	if err := r.Get(ctx, req.NamespacedName, &pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	// delete
+	if !pod.DeletionTimestamp.IsZero() {
+		log.V(5).Info("Deleting", "pod", req.NamespacedName)
+		r.Cache.TASCache().DeletePod(pod)
+		return ctrl.Result{}, nil
+	}
+
+	// add or update
+	log.V(5).Info("Adding or updating", "pod", req.NamespacedName)
+	r.Cache.TASCache().AddOrUpdatePod(pod)
+	return ctrl.Result{}, nil
+}
+
+func (r *NonTasUsageReconciler) Create(e event.TypedCreateEvent[*corev1.Pod]) bool {
+	return true
+}
+
+func (r *NonTasUsageReconciler) Update(e event.TypedUpdateEvent[*corev1.Pod]) bool {
+	return true
+}
+
+func (r *NonTasUsageReconciler) Delete(e event.TypedDeleteEvent[*corev1.Pod]) bool {
+	return true
+}
+
+func (r *NonTasUsageReconciler) Generic(event.TypedGenericEvent[*corev1.Pod]) bool {
+	return false
+}
+
+func (r *NonTasUsageReconciler) SetupWithManager(mgr ctrl.Manager) (string, error) {
+	return TASNonTasUsageController, ctrl.NewControllerManagedBy(mgr).
+		Named(TASNonTasUsageController).
+		For(&corev1.Pod{}).
```

  I think it would be more consistent to follow the setup as in https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/failurerecovery/pod_termination_controller.go#L218-L231
  
  Specifically to register the roleTracker, and leader election, and disable the leader election so that all replicas keep track of the usage.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-14T13:34:37Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:48** by [@gabesaba](https://github.com/gabesaba) — 2026-01-14T13:34:36Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+}
+
+type podUsageValue struct {
+	node  string
+	usage resources.Requests
+}
+
+// addOrUpdate assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) addOrUpdate(pod corev1.Pod) {
+	// skip unscheduled or terminal pods as they don't use any capacity
+	if len(pod.Spec.NodeName) == 0 || utilpod.IsTerminated(&pod) {
+		n.delete(pod)
+		return
+	}
+	if utiltas.IsTAS(&pod) {
```

  Is there ever a case where a pod can change from non-TAS to TAS? If yes, then we can't safely filter on !IsTAS

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-14T13:36:23Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:44** by [@gabesaba](https://github.com/gabesaba) — 2026-01-14T13:36:23Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+}
+
+type podUsageValue struct {
+	node  string
+	usage resources.Requests
+}
+
+// addOrUpdate assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) addOrUpdate(pod corev1.Pod) {
+	// skip unscheduled or terminal pods as they don't use any capacity
+	if len(pod.Spec.NodeName) == 0 || utilpod.IsTerminated(&pod) {
```

  Certainly we could but I would rather have the logic encapsulated in this type, until it is proven to be a bottleneck.
  
  I don't think we can move safely to the filter, as we need to still process, and delete pods that enter a terminal condition.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-14T13:48:01Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:48** by [@mimowo](https://github.com/mimowo) — 2026-01-14T13:48:01Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+}
+
+type podUsageValue struct {
+	node  string
+	usage resources.Requests
+}
+
+// addOrUpdate assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) addOrUpdate(pod corev1.Pod) {
+	// skip unscheduled or terminal pods as they don't use any capacity
+	if len(pod.Spec.NodeName) == 0 || utilpod.IsTerminated(&pod) {
+		n.delete(pod)
+		return
+	}
+	if utiltas.IsTAS(&pod) {
```

  It should never happen by Kueue, but indeed a user may manually add or delete a TAS annotation on the Pod, therefore changing its "IsTAS" status.  However, this has no business use-case so I'm not clear we need to care about such manual manipulations.  We could strenghten the validation by webhook to prevent that, wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-14T13:50:25Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:44** by [@mimowo](https://github.com/mimowo) — 2026-01-14T13:50:25Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+}
+
+type podUsageValue struct {
+	node  string
+	usage resources.Requests
+}
+
+// addOrUpdate assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) addOrUpdate(pod corev1.Pod) {
+	// skip unscheduled or terminal pods as they don't use any capacity
+	if len(pod.Spec.NodeName) == 0 || utilpod.IsTerminated(&pod) {
```

  sure, as mentioned above, let's keep as is, but I don't understand that: "we need to still process, and delete pods that enter a terminal condition.". I think when Pod is transitioning to terminal state (failed or succeeded) we could just subtract its usage in the filter. Let me know if I'm missing some scenario, but it remains unclear to me.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-16T14:03:02Z

- **pkg/controller/tas/non_tas_usage_controller.go:95** by [@gabesaba](https://github.com/gabesaba) — 2026-01-16T14:03:02Z
```diff
@@ -0,0 +1,97 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	schdcache "sigs.k8s.io/kueue/pkg/cache/scheduler"
+)
+
+func newNonTasUsageReconciler(client client.Client, cache *schdcache.Cache) *NonTasUsageReconciler {
+	return &NonTasUsageReconciler{
+		Client: client,
+		Cache:  cache,
+	}
+}
+
+// NonTasUsageReconciler monitors pods to update
+// the TAS cache with non-TAS usage.
+type NonTasUsageReconciler struct {
+	client.Client
+	Scheme *runtime.Scheme
+	Cache  *schdcache.Cache
+}
+
+var _ reconcile.Reconciler = (*NonTasUsageReconciler)(nil)
+var _ predicate.TypedPredicate[*corev1.Pod] = (*NonTasUsageReconciler)(nil)
+
+//+kubebuilder:rbac:groups=core,resources=pods,verbs=get;list;watch
+
+func (r *NonTasUsageReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := klog.FromContext(ctx)
+	log.V(3).Info("Non-TAS usage cachd reconciling", "pod", req.NamespacedName)
+
+	var pod corev1.Pod
+	if err := r.Get(ctx, req.NamespacedName, &pod); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	// delete
+	if !pod.DeletionTimestamp.IsZero() {
+		log.V(5).Info("Deleting", "pod", req.NamespacedName)
+		r.Cache.TASCache().DeletePod(pod)
+		return ctrl.Result{}, nil
+	}
+
+	// add or update
+	log.V(5).Info("Adding or updating", "pod", req.NamespacedName)
+	r.Cache.TASCache().AddOrUpdatePod(pod)
+	return ctrl.Result{}, nil
+}
+
+func (r *NonTasUsageReconciler) Create(e event.TypedCreateEvent[*corev1.Pod]) bool {
+	return true
+}
+
+func (r *NonTasUsageReconciler) Update(e event.TypedUpdateEvent[*corev1.Pod]) bool {
+	return true
+}
+
+func (r *NonTasUsageReconciler) Delete(e event.TypedDeleteEvent[*corev1.Pod]) bool {
+	return true
+}
+
+func (r *NonTasUsageReconciler) Generic(event.TypedGenericEvent[*corev1.Pod]) bool {
+	return false
+}
+
+func (r *NonTasUsageReconciler) SetupWithManager(mgr ctrl.Manager) (string, error) {
+	return TASNonTasUsageController, ctrl.NewControllerManagedBy(mgr).
+		Named(TASNonTasUsageController).
+		For(&corev1.Pod{}).
```

  Done.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-16T14:08:34Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:48** by [@gabesaba](https://github.com/gabesaba) — 2026-01-16T14:07:34Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+}
+
+type podUsageValue struct {
+	node  string
+	usage resources.Requests
+}
+
+// addOrUpdate assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) addOrUpdate(pod corev1.Pod) {
+	// skip unscheduled or terminal pods as they don't use any capacity
+	if len(pod.Spec.NodeName) == 0 || utilpod.IsTerminated(&pod) {
+		n.delete(pod)
+		return
+	}
+	if utiltas.IsTAS(&pod) {
```

  > We could strenghten the validation by webhook to prevent that, wdyt?
  
  I don't think such a validation is necessary. 
  
  > I would suggest moving this pre-check into the filters for the "controller" to skip triggering Reconciles for tas pods.
  
  My only objection to this now is practical: existing unit tests in scheduler/cache don't use the new controller, so we would need to do some refactoring of those tests.
  
  Shall we leave this as a follow-up (I can create a new issue), or are you fine with leaving the logic here?

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:85** by [@gabesaba](https://github.com/gabesaba) — 2026-01-16T14:08:06Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+}
+
+type podUsageValue struct {
+	node  string
+	usage resources.Requests
+}
+
+// addOrUpdate assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) addOrUpdate(pod corev1.Pod, log logr.Logger) {
+	// skip unscheduled pods as they don't use any capacity.
+	if len(pod.Spec.NodeName) == 0 {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting unscheduled pod", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+	// delete terminated pods as they no longer use any capacity.
+	if utilpod.IsTerminated(&pod) {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting terminated", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+	// skip pods accounted for by TAS.
+	if utiltas.IsTAS(&pod) {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting pod accounted for by TAS", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+
+	requests := resources.NewRequests(
+		resourcehelpers.PodRequests(&pod, resourcehelpers.PodResourcesOptions{}))
+	n.podUsage[client.ObjectKeyFromObject(&pod)] = podUsageValue{
+		node:  pod.Spec.NodeName,
+		usage: requests,
+	}
+}
+
+// delete assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) delete(key client.ObjectKey) {
+	delete(n.podUsage, key)
+}
+
+func (n *nonTasUsageCache) cached(pod corev1.Pod) bool {
+	_, found := n.podUsage[client.ObjectKeyFromObject(&pod)]
+	return found
+}
```

  note: this is only used to reduce logging verbosity. reviewers: wdyt, is this worth it, or should we just log a little more and do an idempotent delete?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T14:41:56Z

- **test/integration/singlecluster/tas/tas_test.go:262** by [@mimowo](https://github.com/mimowo) — 2026-01-16T14:41:56Z
```diff
@@ -212,6 +213,167 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 
+	ginkgo.When("non-TAS pod exists", func() {
+		var (
+			nodes        []corev1.Node
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueue.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			nodes = []corev1.Node{
+				*testingnode.MakeNode("node1").
+					Label("node-group", "tas").
+					Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+					Label(utiltesting.DefaultRackTopologyLevel, "r1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("3"),
+					}).
+					Ready().
+					Obj(),
+			}
+			util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+			topology = utiltestingapi.MakeDefaultTwoLevelTopology("default")
+			util.MustCreate(ctx, k8sClient, topology)
+
+			tasFlavor = utiltestingapi.MakeResourceFlavor("tas-flavor").
+				NodeLabel("node-group", "tas").
+				TopologyName("default").Obj()
+			util.MustCreate(ctx, k8sClient, tasFlavor)
+
+			clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*utiltestingapi.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "999").Obj()).
+				Obj()
+			util.MustCreate(ctx, k8sClient, clusterQueue)
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			util.MustCreate(ctx, k8sClient, localQueue)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteAllPodsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
```

  nit, but these can probably be already replaced by DeleteNamespace.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T14:46:41Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:49** by [@mimowo](https://github.com/mimowo) — 2026-01-16T14:46:41Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+}
+
+type podUsageValue struct {
+	node  string
+	usage resources.Requests
+}
+
+// addOrUpdate assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) addOrUpdate(pod corev1.Pod, log logr.Logger) {
+	// skip unscheduled pods as they don't use any capacity.
+	if len(pod.Spec.NodeName) == 0 {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting unscheduled pod", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
```

  This should never happen, that a Pod without NodeName got into cache. I think this branch is non-testable outside of unit tests, so I would drop it.
  
  This cannot happend because in Pod spec.nodeName cannot be mutated / deleted  once set.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T14:53:58Z

- **test/util/pod_webhook.go:37** by [@mimowo](https://github.com/mimowo) — 2026-01-16T14:53:58Z
```diff
@@ -0,0 +1,53 @@
+// Copyright The Kubernetes Authors.
+//
+// Licensed under the Apache License, Version 2.0 (the "License");
+// you may not use this file except in compliance with the License.
+// You may obtain a copy of the License at
+//
+//     http://www.apache.org/licenses/LICENSE-2.0
+//
+// Unless required by applicable law or agreed to in writing, software
+// distributed under the License is distributed on an "AS IS" BASIS,
+// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+// See the License for the specific language governing permissions and
+// limitations under the License.
+
+package util
+
+import (
+	"context"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+func SetupPodWebhookForTesting(mgr manager.Manager) error {
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&corev1.Pod{}).
+		WithDefaulter(&podWebhook{}).
+		WithValidator(&podWebhook{}).
+		Complete()
+}
+
+// podWebhook is a dummy webhook, no-op validation and defaulting,
+// for integration tests which need to create pods.
+type podWebhook struct{}
```

  nit, I would prefer to reuse the NoOp webhook we already have: https://github.com/kubernetes-sigs/kueue/blob/3250572ddbdc351dc27f73f70b713822abed1cc5/pkg/controller/jobframework/noop_webhook.go#L31

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T14:58:04Z

- **pkg/controller/tas/non_tas_usage_controller.go:78** by [@mimowo](https://github.com/mimowo) — 2026-01-16T14:58:04Z
```diff
@@ -0,0 +1,108 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	schdcache "sigs.k8s.io/kueue/pkg/cache/scheduler"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+)
+
+func newNonTasUsageReconciler(client client.Client, cache *schdcache.Cache, qcache *qcache.Manager) *NonTasUsageReconciler {
+	return &NonTasUsageReconciler{
+		Client: client,
+		Cache:  cache,
+		QCache: qcache,
+	}
+}
+
+// NonTasUsageReconciler monitors pods to update
+// the TAS cache with non-TAS usage.
+type NonTasUsageReconciler struct {
+	client.Client
+	Scheme      *runtime.Scheme
+	Cache       *schdcache.Cache
+	QCache      *qcache.Manager
+	roleTracker *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*NonTasUsageReconciler)(nil)
+var _ predicate.TypedPredicate[*corev1.Pod] = (*NonTasUsageReconciler)(nil)
+
+//+kubebuilder:rbac:groups=core,resources=pods,verbs=get;list;watch
+
+func (r *NonTasUsageReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := klog.FromContext(ctx)
+	log.V(3).Info("Non-TAS usage cache reconciling", "pod", req.NamespacedName)
+	var pod corev1.Pod
+	err := r.Get(ctx, req.NamespacedName, &pod)
+	if err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return ctrl.Result{}, err
+		}
+		log.V(5).Info("Idempotently deleting not found pod", "pod", req.NamespacedName)
+		r.Cache.TASCache().DeletePodByKey(req.NamespacedName)
+		r.QCache.QueueAllInadmissible(ctx)
+		return ctrl.Result{}, nil
+	}
+
+	r.Cache.TASCache().AddOrUpdatePod(pod, log)
+	r.QCache.QueueAllInadmissible(ctx)
```

  I would prefer to not do that, because this is a pre-existing bug, which is of rather low importance, because: (a) for most environments non-TAS usage does not matter anyway. (b) any update to the CQ would trigger the requeue anyway.
  
  Solving this problem that way exposes us to the constant inflow of reuque which were source of the problem for Nodes. Especially as this QueueAllInadmissible would be called for **all** pods. 
  
  So, I would suggest to just adjust the integration tests to trigger the requeue, eg by creating a low prioriory workload.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T15:06:30Z

- **pkg/controller/tas/non_tas_usage_controller.go:78** by [@mimowo](https://github.com/mimowo) — 2026-01-16T15:06:30Z
```diff
@@ -0,0 +1,108 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	schdcache "sigs.k8s.io/kueue/pkg/cache/scheduler"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+)
+
+func newNonTasUsageReconciler(client client.Client, cache *schdcache.Cache, qcache *qcache.Manager) *NonTasUsageReconciler {
+	return &NonTasUsageReconciler{
+		Client: client,
+		Cache:  cache,
+		QCache: qcache,
+	}
+}
+
+// NonTasUsageReconciler monitors pods to update
+// the TAS cache with non-TAS usage.
+type NonTasUsageReconciler struct {
+	client.Client
+	Scheme      *runtime.Scheme
+	Cache       *schdcache.Cache
+	QCache      *qcache.Manager
+	roleTracker *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*NonTasUsageReconciler)(nil)
+var _ predicate.TypedPredicate[*corev1.Pod] = (*NonTasUsageReconciler)(nil)
+
+//+kubebuilder:rbac:groups=core,resources=pods,verbs=get;list;watch
+
+func (r *NonTasUsageReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := klog.FromContext(ctx)
+	log.V(3).Info("Non-TAS usage cache reconciling", "pod", req.NamespacedName)
+	var pod corev1.Pod
+	err := r.Get(ctx, req.NamespacedName, &pod)
+	if err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return ctrl.Result{}, err
+		}
+		log.V(5).Info("Idempotently deleting not found pod", "pod", req.NamespacedName)
+		r.Cache.TASCache().DeletePodByKey(req.NamespacedName)
+		r.QCache.QueueAllInadmissible(ctx)
+		return ctrl.Result{}, nil
+	}
+
+	r.Cache.TASCache().AddOrUpdatePod(pod, log)
+	r.QCache.QueueAllInadmissible(ctx)
```

  I think we may need to solve this, but let's be careful about it.  I think when we go about fixing that issue we should at least:
  - filter for non-TAS pods (TAS pods are already handled by TAS Workloads)
  - bulk the requests in time, like 1min or so. 
  - probably only requeue for ClusterQueues which are affected by the change (using Flavors matching the affected nodes)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T15:07:33Z

- **pkg/controller/tas/non_tas_usage_controller.go:51** by [@mimowo](https://github.com/mimowo) — 2026-01-16T15:07:33Z
```diff
@@ -0,0 +1,108 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	schdcache "sigs.k8s.io/kueue/pkg/cache/scheduler"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+)
+
+func newNonTasUsageReconciler(client client.Client, cache *schdcache.Cache, qcache *qcache.Manager) *NonTasUsageReconciler {
+	return &NonTasUsageReconciler{
+		Client: client,
+		Cache:  cache,
+		QCache: qcache,
+	}
+}
+
+// NonTasUsageReconciler monitors pods to update
+// the TAS cache with non-TAS usage.
+type NonTasUsageReconciler struct {
+	client.Client
+	Scheme      *runtime.Scheme
```

  it does not seem used

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T15:16:23Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:35** by [@mimowo](https://github.com/mimowo) — 2026-01-16T15:16:23Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
```

  I think I would prefer to add locking to this, or use sync.Map. 
  
  IIUC your assumption is that the access (nodeUsage, cached) and the writes (addOrUpdate, delete) are already guarded by higher level locks, but honestly this is unclear to me by static analysis.
  
  So, I see that AddOrUpdatePod takes the `tasCache` lock, but `nodeUsage` is called from `snapshotForNodes` which takes a read lock on `TASFlavorCache`, and higher up on `Cache`. So I'm afraid there might be some nasty races when we iterate over the map being mutated.
  
  I might be missing something, let me know if this is the case.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T15:17:52Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:65** by [@mimowo](https://github.com/mimowo) — 2026-01-16T15:17:52Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+}
+
+type podUsageValue struct {
+	node  string
+	usage resources.Requests
+}
+
+// addOrUpdate assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) addOrUpdate(pod corev1.Pod, log logr.Logger) {
+	// skip unscheduled pods as they don't use any capacity.
+	if len(pod.Spec.NodeName) == 0 {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting unscheduled pod", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+	// delete terminated pods as they no longer use any capacity.
+	if utilpod.IsTerminated(&pod) {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting terminated", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+	// skip pods accounted for by TAS.
+	if utiltas.IsTAS(&pod) {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting pod accounted for by TAS", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
```

  nit, I wouldn't worry about that, but I'm also ok keeping it. It does not have no business relevance.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T15:18:27Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:65** by [@mimowo](https://github.com/mimowo) — 2026-01-16T15:18:27Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+}
+
+type podUsageValue struct {
+	node  string
+	usage resources.Requests
+}
+
+// addOrUpdate assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) addOrUpdate(pod corev1.Pod, log logr.Logger) {
+	// skip unscheduled pods as they don't use any capacity.
+	if len(pod.Spec.NodeName) == 0 {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting unscheduled pod", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+	// delete terminated pods as they no longer use any capacity.
+	if utilpod.IsTerminated(&pod) {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting terminated", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+	// skip pods accounted for by TAS.
+	if utiltas.IsTAS(&pod) {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting pod accounted for by TAS", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
```

  nit, I wouldn't worry about that, but I'm also ok keeping it. It does not have no business relevance. I would even use it as a filter in the preconditions.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T15:21:11Z

- **test/integration/singlecluster/tas/tas_test.go:306** by [@mimowo](https://github.com/mimowo) — 2026-01-16T15:21:11Z
```diff
@@ -212,6 +213,167 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 
+	ginkgo.When("non-TAS pod exists", func() {
+		var (
+			nodes        []corev1.Node
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueue.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			nodes = []corev1.Node{
+				*testingnode.MakeNode("node1").
+					Label("node-group", "tas").
+					Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+					Label(utiltesting.DefaultRackTopologyLevel, "r1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("3"),
+					}).
+					Ready().
+					Obj(),
+			}
+			util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+			topology = utiltestingapi.MakeDefaultTwoLevelTopology("default")
+			util.MustCreate(ctx, k8sClient, topology)
+
+			tasFlavor = utiltestingapi.MakeResourceFlavor("tas-flavor").
+				NodeLabel("node-group", "tas").
+				TopologyName("default").Obj()
+			util.MustCreate(ctx, k8sClient, tasFlavor)
+
+			clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*utiltestingapi.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "999").Obj()).
+				Obj()
+			util.MustCreate(ctx, k8sClient, clusterQueue)
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			util.MustCreate(ctx, k8sClient, localQueue)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteAllPodsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+			for _, node := range nodes {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+			}
+		})
+
+		ginkgo.It("non-TAS pod terminates, releasing capacity", func() {
+			var wl *kueue.Workload
+			var nonTasPod *corev1.Pod
+
+			ginkgo.By("create a non-TAS pod which consumes the node's capacity", func() {
+				nonTasPod = testingpod.MakePod("pod", ns.Name).
+					Request(corev1.ResourceCPU, "1").
+					NodeName("node1").
+					Obj()
+				nonTasPod.Spec.TerminationGracePeriodSeconds = ptr.To[int64](0)
+				util.MustCreate(ctx, k8sClient, nonTasPod)
+			})
+
+			ginkgo.By("create a workload which requires the node's capacity", func() {
+				wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+					Queue("local-queue").
+					Request(corev1.ResourceCPU, "1").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+
+				ginkgo.By("verify the workload is not admitted", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(workload.IsAdmitted(wl)).To(gomega.BeFalse())
+					}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
+				})
+			})
+
+			ginkgo.By("terminate the non-TAS pod", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(nonTasPod), nonTasPod)).To(gomega.Succeed())
+					util.SetPodsPhase(ctx, k8sClient, corev1.PodSucceeded, nonTasPod)
+					g.Expect(k8sClient.Update(ctx, nonTasPod)).Should(gomega.Succeed())
+				}).Should(gomega.Succeed(), util.Timeout, util.Interval)
```

  nit: `}, util.Timeout, util.Interval).Should(gomega.Succeed())` for concistency with the rest of the codebase. I'm not even clear if the alternative does what we want

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T15:25:44Z

- **test/integration/singlecluster/tas/tas_test.go:323** by [@mimowo](https://github.com/mimowo) — 2026-01-16T15:25:44Z
```diff
@@ -212,6 +213,167 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 
+	ginkgo.When("non-TAS pod exists", func() {
+		var (
+			nodes        []corev1.Node
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueue.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			nodes = []corev1.Node{
+				*testingnode.MakeNode("node1").
+					Label("node-group", "tas").
+					Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+					Label(utiltesting.DefaultRackTopologyLevel, "r1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("3"),
+					}).
+					Ready().
+					Obj(),
+			}
+			util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+			topology = utiltestingapi.MakeDefaultTwoLevelTopology("default")
+			util.MustCreate(ctx, k8sClient, topology)
+
+			tasFlavor = utiltestingapi.MakeResourceFlavor("tas-flavor").
+				NodeLabel("node-group", "tas").
+				TopologyName("default").Obj()
+			util.MustCreate(ctx, k8sClient, tasFlavor)
+
+			clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*utiltestingapi.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "999").Obj()).
+				Obj()
+			util.MustCreate(ctx, k8sClient, clusterQueue)
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			util.MustCreate(ctx, k8sClient, localQueue)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteAllPodsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+			for _, node := range nodes {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+			}
+		})
+
+		ginkgo.It("non-TAS pod terminates, releasing capacity", func() {
+			var wl *kueue.Workload
+			var nonTasPod *corev1.Pod
+
+			ginkgo.By("create a non-TAS pod which consumes the node's capacity", func() {
+				nonTasPod = testingpod.MakePod("pod", ns.Name).
+					Request(corev1.ResourceCPU, "1").
+					NodeName("node1").
+					Obj()
+				nonTasPod.Spec.TerminationGracePeriodSeconds = ptr.To[int64](0)
+				util.MustCreate(ctx, k8sClient, nonTasPod)
+			})
+
+			ginkgo.By("create a workload which requires the node's capacity", func() {
+				wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+					Queue("local-queue").
+					Request(corev1.ResourceCPU, "1").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+
+				ginkgo.By("verify the workload is not admitted", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(workload.IsAdmitted(wl)).To(gomega.BeFalse())
+					}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
+				})
+			})
+
+			ginkgo.By("terminate the non-TAS pod", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(nonTasPod), nonTasPod)).To(gomega.Succeed())
+					util.SetPodsPhase(ctx, k8sClient, corev1.PodSucceeded, nonTasPod)
+					g.Expect(k8sClient.Update(ctx, nonTasPod)).Should(gomega.Succeed())
+				}).Should(gomega.Succeed(), util.Timeout, util.Interval)
+			})
+
+			ginkgo.By("expect TAS pod to admit", func() {
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+				util.ExpectAdmittedWorkloadsTotalMetric(clusterQueue, "", 1)
+			})
+		})
+		ginkgo.It("non-TAS pod is deleted, releasing capacity", func() {
+			var wl *kueue.Workload
+			var nonTasPod *corev1.Pod
+
+			ginkgo.By("creating a non-TAS pod which consumes the node's capacity", func() {
+				nonTasPod = testingpod.MakePod("pod", ns.Name).
+					Request(corev1.ResourceCPU, "1").
+					NodeName("node1").
+					Obj()
+				nonTasPod.Spec.TerminationGracePeriodSeconds = ptr.To[int64](0)
```

  nit, but use the `TerminationGracePeriod` funtion on the PodWrapper

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T15:27:30Z

- **pkg/cache/scheduler/tas_flavor.go:89** by [@mimowo](https://github.com/mimowo) — 2026-01-16T15:27:30Z
```diff
@@ -88,15 +85,18 @@ type TASFlavorCache struct {
 
 	// usage maintains the usage per topology domain
 	usage map[utiltas.TopologyDomainID]resources.Requests
+
+	nonTasUsageCache nonTasUsageCache
```

  ```suggestion
      // nonTasUsageCache maintains the usage coming from non-TAS pods, eg. static Pods or
      // DaemonSet pods.
  	nonTasUsageCache nonTasUsageCache
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T15:30:46Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:88** by [@mimowo](https://github.com/mimowo) — 2026-01-16T15:30:46Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+}
+
+type podUsageValue struct {
+	node  string
+	usage resources.Requests
+}
+
+// addOrUpdate assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) addOrUpdate(pod corev1.Pod, log logr.Logger) {
+	// skip unscheduled pods as they don't use any capacity.
+	if len(pod.Spec.NodeName) == 0 {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting unscheduled pod", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+	// delete terminated pods as they no longer use any capacity.
+	if utilpod.IsTerminated(&pod) {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting terminated", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+	// skip pods accounted for by TAS.
+	if utiltas.IsTAS(&pod) {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting pod accounted for by TAS", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+
+	requests := resources.NewRequests(
+		resourcehelpers.PodRequests(&pod, resourcehelpers.PodResourcesOptions{}))
+	n.podUsage[client.ObjectKeyFromObject(&pod)] = podUsageValue{
+		node:  pod.Spec.NodeName,
+		usage: requests,
+	}
+}
+
+// delete assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) delete(key client.ObjectKey) {
+	delete(n.podUsage, key)
+}
+
+func (n *nonTasUsageCache) cached(pod corev1.Pod) bool {
+	_, found := n.podUsage[client.ObjectKeyFromObject(&pod)]
+	return found
+}
+
+// nodeUsage assumes that tas cache's read lock is held.
+func (n *nonTasUsageCache) nodeUsage() map[string]resources.Requests {
```

  nit, maybe `usagePerNode`. `nodeUsage` to me reads as if `node` was an input parameter

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T15:33:13Z

- **test/integration/singlecluster/tas/tas_test.go:319** by [@mimowo](https://github.com/mimowo) — 2026-01-16T15:33:14Z
```diff
@@ -212,6 +213,167 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 
+	ginkgo.When("non-TAS pod exists", func() {
+		var (
+			nodes        []corev1.Node
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueue.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			nodes = []corev1.Node{
+				*testingnode.MakeNode("node1").
+					Label("node-group", "tas").
+					Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+					Label(utiltesting.DefaultRackTopologyLevel, "r1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("3"),
+					}).
+					Ready().
+					Obj(),
+			}
+			util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+			topology = utiltestingapi.MakeDefaultTwoLevelTopology("default")
+			util.MustCreate(ctx, k8sClient, topology)
+
+			tasFlavor = utiltestingapi.MakeResourceFlavor("tas-flavor").
+				NodeLabel("node-group", "tas").
+				TopologyName("default").Obj()
+			util.MustCreate(ctx, k8sClient, tasFlavor)
+
+			clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*utiltestingapi.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "999").Obj()).
+				Obj()
+			util.MustCreate(ctx, k8sClient, clusterQueue)
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			util.MustCreate(ctx, k8sClient, localQueue)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteAllPodsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+			for _, node := range nodes {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+			}
+		})
+
+		ginkgo.It("non-TAS pod terminates, releasing capacity", func() {
+			var wl *kueue.Workload
+			var nonTasPod *corev1.Pod
+
+			ginkgo.By("create a non-TAS pod which consumes the node's capacity", func() {
+				nonTasPod = testingpod.MakePod("pod", ns.Name).
+					Request(corev1.ResourceCPU, "1").
+					NodeName("node1").
+					Obj()
+				nonTasPod.Spec.TerminationGracePeriodSeconds = ptr.To[int64](0)
+				util.MustCreate(ctx, k8sClient, nonTasPod)
+			})
+
+			ginkgo.By("create a workload which requires the node's capacity", func() {
+				wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+					Queue("local-queue").
+					Request(corev1.ResourceCPU, "1").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+
+				ginkgo.By("verify the workload is not admitted", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(workload.IsAdmitted(wl)).To(gomega.BeFalse())
+					}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
+				})
+			})
+
+			ginkgo.By("terminate the non-TAS pod", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(nonTasPod), nonTasPod)).To(gomega.Succeed())
+					util.SetPodsPhase(ctx, k8sClient, corev1.PodSucceeded, nonTasPod)
+					g.Expect(k8sClient.Update(ctx, nonTasPod)).Should(gomega.Succeed())
+				}).Should(gomega.Succeed(), util.Timeout, util.Interval)
+			})
+
+			ginkgo.By("expect TAS pod to admit", func() {
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+				util.ExpectAdmittedWorkloadsTotalMetric(clusterQueue, "", 1)
+			})
+		})
+		ginkgo.It("non-TAS pod is deleted, releasing capacity", func() {
```

  nit, add newline to separate the "It"s

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T15:33:47Z

- **test/integration/singlecluster/tas/tas_test.go:364** by [@mimowo](https://github.com/mimowo) — 2026-01-16T15:33:47Z
```diff
@@ -212,6 +213,167 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 
+	ginkgo.When("non-TAS pod exists", func() {
+		var (
+			nodes        []corev1.Node
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueue.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			nodes = []corev1.Node{
+				*testingnode.MakeNode("node1").
+					Label("node-group", "tas").
+					Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+					Label(utiltesting.DefaultRackTopologyLevel, "r1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("3"),
+					}).
+					Ready().
+					Obj(),
+			}
+			util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+			topology = utiltestingapi.MakeDefaultTwoLevelTopology("default")
+			util.MustCreate(ctx, k8sClient, topology)
+
+			tasFlavor = utiltestingapi.MakeResourceFlavor("tas-flavor").
+				NodeLabel("node-group", "tas").
+				TopologyName("default").Obj()
+			util.MustCreate(ctx, k8sClient, tasFlavor)
+
+			clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*utiltestingapi.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "999").Obj()).
+				Obj()
+			util.MustCreate(ctx, k8sClient, clusterQueue)
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			util.MustCreate(ctx, k8sClient, localQueue)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteAllPodsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+			for _, node := range nodes {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+			}
+		})
+
+		ginkgo.It("non-TAS pod terminates, releasing capacity", func() {
+			var wl *kueue.Workload
+			var nonTasPod *corev1.Pod
+
+			ginkgo.By("create a non-TAS pod which consumes the node's capacity", func() {
+				nonTasPod = testingpod.MakePod("pod", ns.Name).
+					Request(corev1.ResourceCPU, "1").
+					NodeName("node1").
+					Obj()
+				nonTasPod.Spec.TerminationGracePeriodSeconds = ptr.To[int64](0)
+				util.MustCreate(ctx, k8sClient, nonTasPod)
+			})
+
+			ginkgo.By("create a workload which requires the node's capacity", func() {
+				wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+					Queue("local-queue").
+					Request(corev1.ResourceCPU, "1").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+
+				ginkgo.By("verify the workload is not admitted", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(workload.IsAdmitted(wl)).To(gomega.BeFalse())
+					}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
+				})
+			})
+
+			ginkgo.By("terminate the non-TAS pod", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(nonTasPod), nonTasPod)).To(gomega.Succeed())
+					util.SetPodsPhase(ctx, k8sClient, corev1.PodSucceeded, nonTasPod)
+					g.Expect(k8sClient.Update(ctx, nonTasPod)).Should(gomega.Succeed())
+				}).Should(gomega.Succeed(), util.Timeout, util.Interval)
+			})
+
+			ginkgo.By("expect TAS pod to admit", func() {
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+				util.ExpectAdmittedWorkloadsTotalMetric(clusterQueue, "", 1)
+			})
+		})
+		ginkgo.It("non-TAS pod is deleted, releasing capacity", func() {
+			var wl *kueue.Workload
+			var nonTasPod *corev1.Pod
+
+			ginkgo.By("creating a non-TAS pod which consumes the node's capacity", func() {
+				nonTasPod = testingpod.MakePod("pod", ns.Name).
+					Request(corev1.ResourceCPU, "1").
+					NodeName("node1").
+					Obj()
+				nonTasPod.Spec.TerminationGracePeriodSeconds = ptr.To[int64](0)
+				util.MustCreate(ctx, k8sClient, nonTasPod)
+			})
+
+			ginkgo.By("creating a workload which requires the node's capacity", func() {
+				wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+					Queue("local-queue").
+					Request(corev1.ResourceCPU, "1").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+			})
+
+			ginkgo.By("verify the workload is not admitted", func() {
+				util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+				gomega.Consistently(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(wl)).To(gomega.BeFalse())
+				}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("delete the non-TAS pod", func() {
+				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, nonTasPod, true, 60*time.Second)
+			})
+
+			ginkgo.By("expect TAS pod to admit", func() {
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+				util.ExpectAdmittedWorkloadsTotalMetric(clusterQueue, "", 1)
+			})
+		})
+		ginkgo.It("Non-TAS pod has no node assignment", func() {
```

  nit, add new line to separate the "It"s - for readability

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T15:36:01Z

- **pkg/cache/queue/manager.go:620** by [@mimowo](https://github.com/mimowo) — 2026-01-16T15:36:01Z
```diff
@@ -617,25 +617,34 @@ func (m *Manager) QueueAssociatedInadmissibleWorkloadsAfter(ctx context.Context,
 	}
 }
 
+func (m *Manager) QueueAllInadmissible(ctx context.Context) {
```

  Actually, I would prefer to revert any changes in the manager.go. I think they are unnecessary. I wouldn't modify business logic which is supposed for cherrypicking just for testing.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T15:40:14Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:95** by [@mimowo](https://github.com/mimowo) — 2026-01-16T15:40:14Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+}
+
+type podUsageValue struct {
+	node  string
+	usage resources.Requests
+}
+
+// addOrUpdate assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) addOrUpdate(pod corev1.Pod, log logr.Logger) {
+	// skip unscheduled pods as they don't use any capacity.
+	if len(pod.Spec.NodeName) == 0 {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting unscheduled pod", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+	// delete terminated pods as they no longer use any capacity.
+	if utilpod.IsTerminated(&pod) {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting terminated", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+	// skip pods accounted for by TAS.
+	if utiltas.IsTAS(&pod) {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting pod accounted for by TAS", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+
+	requests := resources.NewRequests(
+		resourcehelpers.PodRequests(&pod, resourcehelpers.PodResourcesOptions{}))
+	n.podUsage[client.ObjectKeyFromObject(&pod)] = podUsageValue{
+		node:  pod.Spec.NodeName,
+		usage: requests,
+	}
+}
+
+// delete assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) delete(key client.ObjectKey) {
+	delete(n.podUsage, key)
+}
+
+func (n *nonTasUsageCache) cached(pod corev1.Pod) bool {
+	_, found := n.podUsage[client.ObjectKeyFromObject(&pod)]
+	return found
+}
+
+// nodeUsage assumes that tas cache's read lock is held.
+func (n *nonTasUsageCache) nodeUsage() map[string]resources.Requests {
+	nodeUsage := make(map[string]resources.Requests)
+	for _, podUsage := range n.podUsage {
+		if _, found := nodeUsage[podUsage.node]; !found {
+			nodeUsage[podUsage.node] = resources.Requests{}
+		}
+		nodeUsage[podUsage.node].Add(podUsage.usage)
+		nodeUsage[podUsage.node].Add(resources.Requests{corev1.ResourcePods: 1})
```

  I'm suspicious about this line because we didn't do it before: https://github.com/kubernetes-sigs/kueue/pull/8484/changes#diff-aa793cfc2528f289b2b59725f714db6e6f6b2c6e9edf4c75925e656e0cc055b7L154-L161
  
  Could you please clarify why needed, and ideally we would have some integration test for it too.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T15:41:54Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:95** by [@mimowo](https://github.com/mimowo) — 2026-01-16T15:41:54Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+}
+
+type podUsageValue struct {
+	node  string
+	usage resources.Requests
+}
+
+// addOrUpdate assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) addOrUpdate(pod corev1.Pod, log logr.Logger) {
+	// skip unscheduled pods as they don't use any capacity.
+	if len(pod.Spec.NodeName) == 0 {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting unscheduled pod", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+	// delete terminated pods as they no longer use any capacity.
+	if utilpod.IsTerminated(&pod) {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting terminated", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+	// skip pods accounted for by TAS.
+	if utiltas.IsTAS(&pod) {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting pod accounted for by TAS", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+
+	requests := resources.NewRequests(
+		resourcehelpers.PodRequests(&pod, resourcehelpers.PodResourcesOptions{}))
+	n.podUsage[client.ObjectKeyFromObject(&pod)] = podUsageValue{
+		node:  pod.Spec.NodeName,
+		usage: requests,
+	}
+}
+
+// delete assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) delete(key client.ObjectKey) {
+	delete(n.podUsage, key)
+}
+
+func (n *nonTasUsageCache) cached(pod corev1.Pod) bool {
+	_, found := n.podUsage[client.ObjectKeyFromObject(&pod)]
+	return found
+}
+
+// nodeUsage assumes that tas cache's read lock is held.
+func (n *nonTasUsageCache) nodeUsage() map[string]resources.Requests {
+	nodeUsage := make(map[string]resources.Requests)
+	for _, podUsage := range n.podUsage {
+		if _, found := nodeUsage[podUsage.node]; !found {
+			nodeUsage[podUsage.node] = resources.Requests{}
+		}
+		nodeUsage[podUsage.node].Add(podUsage.usage)
+		nodeUsage[podUsage.node].Add(resources.Requests{corev1.ResourcePods: 1})
```

  I actually think that hitting the limit for the number of Pods for node is the most likely constraint coming from this controller, so I want to make sure the logic is sound here.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T15:43:50Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:85** by [@mimowo](https://github.com/mimowo) — 2026-01-16T15:43:50Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+}
+
+type podUsageValue struct {
+	node  string
+	usage resources.Requests
+}
+
+// addOrUpdate assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) addOrUpdate(pod corev1.Pod, log logr.Logger) {
+	// skip unscheduled pods as they don't use any capacity.
+	if len(pod.Spec.NodeName) == 0 {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting unscheduled pod", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+	// delete terminated pods as they no longer use any capacity.
+	if utilpod.IsTerminated(&pod) {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting terminated", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+	// skip pods accounted for by TAS.
+	if utiltas.IsTAS(&pod) {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting pod accounted for by TAS", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+
+	requests := resources.NewRequests(
+		resourcehelpers.PodRequests(&pod, resourcehelpers.PodResourcesOptions{}))
+	n.podUsage[client.ObjectKeyFromObject(&pod)] = podUsageValue{
+		node:  pod.Spec.NodeName,
+		usage: requests,
+	}
+}
+
+// delete assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) delete(key client.ObjectKey) {
+	delete(n.podUsage, key)
+}
+
+func (n *nonTasUsageCache) cached(pod corev1.Pod) bool {
+	_, found := n.podUsage[client.ObjectKeyFromObject(&pod)]
+	return found
+}
```

  I would rather do indempontent delete, instead of repeating the "if `cached` then `delete`".

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T15:48:18Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:48** by [@mimowo](https://github.com/mimowo) — 2026-01-16T15:48:18Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+}
+
+type podUsageValue struct {
+	node  string
+	usage resources.Requests
+}
+
+// addOrUpdate assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) addOrUpdate(pod corev1.Pod) {
+	// skip unscheduled or terminal pods as they don't use any capacity
+	if len(pod.Spec.NodeName) == 0 || utilpod.IsTerminated(&pod) {
+		n.delete(pod)
+		return
+	}
+	if utiltas.IsTAS(&pod) {
```

  > My only objection to this now is practical: existing unit tests in scheduler/cache don't use the new controller, so we would need to do some refactoring of those tests.
  
  I imagine the unit tests in scheduler_tas_test.go could have a new parameter like `nonTasPods` and those would be added to the cache for non-TAS Pods, bypassing the controller. Similarly as we add to cache the "node" capacity without the controller for that running in the scheduler unit tests.
  
  Sure, integration is also important, but for that we may have some integration tests (as you already did).
  
  So TBH I'm not clear why checking IsTAS in filters would be some practical issue for testing here.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-16T16:17:24Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:49** by [@gabesaba](https://github.com/gabesaba) — 2026-01-16T16:17:24Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+}
+
+type podUsageValue struct {
+	node  string
+	usage resources.Requests
+}
+
+// addOrUpdate assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) addOrUpdate(pod corev1.Pod, log logr.Logger) {
+	// skip unscheduled pods as they don't use any capacity.
+	if len(pod.Spec.NodeName) == 0 {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting unscheduled pod", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
```

  We still need to exit on this branch right? This condition was copied from old code (though it didn't delete anything, as there was nothing to be deleted, just filtered)

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-16T16:20:46Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:35** by [@gabesaba](https://github.com/gabesaba) — 2026-01-16T16:20:46Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
```

  Great catch, I missed that these were different locks..

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-16T16:23:59Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:95** by [@gabesaba](https://github.com/gabesaba) — 2026-01-16T16:23:59Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+}
+
+type podUsageValue struct {
+	node  string
+	usage resources.Requests
+}
+
+// addOrUpdate assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) addOrUpdate(pod corev1.Pod, log logr.Logger) {
+	// skip unscheduled pods as they don't use any capacity.
+	if len(pod.Spec.NodeName) == 0 {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting unscheduled pod", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+	// delete terminated pods as they no longer use any capacity.
+	if utilpod.IsTerminated(&pod) {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting terminated", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+	// skip pods accounted for by TAS.
+	if utiltas.IsTAS(&pod) {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting pod accounted for by TAS", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+
+	requests := resources.NewRequests(
+		resourcehelpers.PodRequests(&pod, resourcehelpers.PodResourcesOptions{}))
+	n.podUsage[client.ObjectKeyFromObject(&pod)] = podUsageValue{
+		node:  pod.Spec.NodeName,
+		usage: requests,
+	}
+}
+
+// delete assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) delete(key client.ObjectKey) {
+	delete(n.podUsage, key)
+}
+
+func (n *nonTasUsageCache) cached(pod corev1.Pod) bool {
+	_, found := n.podUsage[client.ObjectKeyFromObject(&pod)]
+	return found
+}
+
+// nodeUsage assumes that tas cache's read lock is held.
+func (n *nonTasUsageCache) nodeUsage() map[string]resources.Requests {
+	nodeUsage := make(map[string]resources.Requests)
+	for _, podUsage := range n.podUsage {
+		if _, found := nodeUsage[podUsage.node]; !found {
+			nodeUsage[podUsage.node] = resources.Requests{}
+		}
+		nodeUsage[podUsage.node].Add(podUsage.usage)
+		nodeUsage[podUsage.node].Add(resources.Requests{corev1.ResourcePods: 1})
```

  In the old code, we subtracked pod here, though there was an assumption that this non-TAS usage method was called _per-pod_: https://github.com/kubernetes-sigs/kueue/blob/15f7b19e822992bfe21cc78cefd0e93bf8c1357d/pkg/cache/scheduler/tas_flavor_snapshot.go#L241
  
  I also added a new unit test to check that we're doing this accounting properly (as I initially wrote a bug, since I was using the old method, but calling _per-node_, rather than _per-pod_

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-16T16:24:11Z

- **pkg/cache/queue/manager.go:620** by [@gabesaba](https://github.com/gabesaba) — 2026-01-16T16:24:11Z
```diff
@@ -617,25 +617,34 @@ func (m *Manager) QueueAssociatedInadmissibleWorkloadsAfter(ctx context.Context,
 	}
 }
 
+func (m *Manager) QueueAllInadmissible(ctx context.Context) {
```

  Ack.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T16:35:35Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:49** by [@mimowo](https://github.com/mimowo) — 2026-01-16T16:35:34Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+}
+
+type podUsageValue struct {
+	node  string
+	usage resources.Requests
+}
+
+// addOrUpdate assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) addOrUpdate(pod corev1.Pod, log logr.Logger) {
+	// skip unscheduled pods as they don't use any capacity.
+	if len(pod.Spec.NodeName) == 0 {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting unscheduled pod", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
```

  > We still need to exit on this branch right? 
  
  yes, just calling the delete is unnecessary

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T16:37:54Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:95** by [@mimowo](https://github.com/mimowo) — 2026-01-16T16:37:54Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+}
+
+type podUsageValue struct {
+	node  string
+	usage resources.Requests
+}
+
+// addOrUpdate assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) addOrUpdate(pod corev1.Pod, log logr.Logger) {
+	// skip unscheduled pods as they don't use any capacity.
+	if len(pod.Spec.NodeName) == 0 {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting unscheduled pod", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+	// delete terminated pods as they no longer use any capacity.
+	if utilpod.IsTerminated(&pod) {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting terminated", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+	// skip pods accounted for by TAS.
+	if utiltas.IsTAS(&pod) {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting pod accounted for by TAS", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+
+	requests := resources.NewRequests(
+		resourcehelpers.PodRequests(&pod, resourcehelpers.PodResourcesOptions{}))
+	n.podUsage[client.ObjectKeyFromObject(&pod)] = podUsageValue{
+		node:  pod.Spec.NodeName,
+		usage: requests,
+	}
+}
+
+// delete assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) delete(key client.ObjectKey) {
+	delete(n.podUsage, key)
+}
+
+func (n *nonTasUsageCache) cached(pod corev1.Pod) bool {
+	_, found := n.podUsage[client.ObjectKeyFromObject(&pod)]
+	return found
+}
+
+// nodeUsage assumes that tas cache's read lock is held.
+func (n *nonTasUsageCache) nodeUsage() map[string]resources.Requests {
+	nodeUsage := make(map[string]resources.Requests)
+	for _, podUsage := range n.podUsage {
+		if _, found := nodeUsage[podUsage.node]; !found {
+			nodeUsage[podUsage.node] = resources.Requests{}
+		}
+		nodeUsage[podUsage.node].Add(podUsage.usage)
+		nodeUsage[podUsage.node].Add(resources.Requests{corev1.ResourcePods: 1})
```

  Ah, I see this makes sense. You essentially moved the accounting. So this should be fine.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T16:58:31Z

- **pkg/cache/scheduler/tas_flavor.go:147** by [@mimowo](https://github.com/mimowo) — 2026-01-16T16:58:31Z
```diff
@@ -151,20 +144,13 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	for domainID, usage := range c.usage {
 		snapshot.addTASUsage(domainID, usage)
 	}
-	for _, pod := range pods {
-		// skip unscheduled or terminal pods as they don't use any capacity
-		if len(pod.Spec.NodeName) == 0 || utilpod.IsTerminated(&pod) {
-			continue
-		}
-		if domainID, ok := nodeToDomain[pod.Spec.NodeName]; ok {
-			requests := resourcehelpers.PodRequests(&pod, resourcehelpers.PodResourcesOptions{})
-			usage := resources.NewRequests(requests)
-			snapshot.addNonTASUsage(domainID, usage)
+	for _, podUsage := range c.nonTasUsageCache.podUsage {
```

  So, just to make the idea about using filers a bit more concreate I was thinking about something like (pseudocode, I havent compiled):
  
  ```golang
  func shouldBeAdded(p *corev1.Pod) {
    // account when: (a) non-TAS, (b) scheduled, (c) non-terminal yet
    return !IsTAS(p) && len(p.spec.NodeName) && !podutil.IsTerminal(p) 
  }
  ```
  Then for handling
  
  ```golang
  func Create(p *corev1.Pod) {
   If shouldBeAdded(p) {
     r.nonTasCache.add(p)
   }
  }
  
  func Update(old, new *corev1.Pod) {
     If !shouldBeAdded(old) && shouldBeAdded(new) {
       r.nonTasCache.add(p)
     } 
     if shouldBeAdded(old) && shouldBeAdded(new) {
       r.nonTasCache.delete(p)
     }
  }
  
  func Delete(p *corev1.Pod) {
    If shouldBeAdded(p) {  // it could also be unconditional here
       r.nonTasCache.delete(p)
     }
  }
  ```
  This actually eliminaes the need for Reconciler, so it could be just informer event handler.
  
  Still, we may consider that for the future, I don't see anything wrong with the current approach, just wanted to make sure the idea is specified more concretely. Maybe it can also inspire to some updates or follow ups.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-19T08:40:52Z

- **pkg/cache/scheduler/tas_flavor.go:147** by [@mimowo](https://github.com/mimowo) — 2026-01-19T08:40:52Z
```diff
@@ -151,20 +144,13 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	for domainID, usage := range c.usage {
 		snapshot.addTASUsage(domainID, usage)
 	}
-	for _, pod := range pods {
-		// skip unscheduled or terminal pods as they don't use any capacity
-		if len(pod.Spec.NodeName) == 0 || utilpod.IsTerminated(&pod) {
-			continue
-		}
-		if domainID, ok := nodeToDomain[pod.Spec.NodeName]; ok {
-			requests := resourcehelpers.PodRequests(&pod, resourcehelpers.PodResourcesOptions{})
-			usage := resources.NewRequests(requests)
-			snapshot.addNonTASUsage(domainID, usage)
+	for _, podUsage := range c.nonTasUsageCache.podUsage {
```

  Ok, I can see a small drawback which is taking locks from within filters for frequently changed objects (Pods), and eliminating the locks is non-trivial. What we could do relatively easily is to trigger Reconcile in the scenarios when in my code snippet I update the `r.nonTasCache`. Still, not required, just thinking about the "ideal" approach.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-19T09:51:26Z

- **pkg/cache/queue/manager.go:620** by [@gabesaba](https://github.com/gabesaba) — 2026-01-19T09:51:26Z
```diff
@@ -617,25 +617,34 @@ func (m *Manager) QueueAssociatedInadmissibleWorkloadsAfter(ctx context.Context,
 	}
 }
 
+func (m *Manager) QueueAllInadmissible(ctx context.Context) {
```

  Created a new issue to track this https://github.com/kubernetes-sigs/kueue/issues/8653

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-19T09:59:17Z

- **test/integration/singlecluster/tas/tas_test.go:306** by [@gabesaba](https://github.com/gabesaba) — 2026-01-19T09:59:17Z
```diff
@@ -212,6 +213,167 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 
+	ginkgo.When("non-TAS pod exists", func() {
+		var (
+			nodes        []corev1.Node
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueue.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			nodes = []corev1.Node{
+				*testingnode.MakeNode("node1").
+					Label("node-group", "tas").
+					Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+					Label(utiltesting.DefaultRackTopologyLevel, "r1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("3"),
+					}).
+					Ready().
+					Obj(),
+			}
+			util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+			topology = utiltestingapi.MakeDefaultTwoLevelTopology("default")
+			util.MustCreate(ctx, k8sClient, topology)
+
+			tasFlavor = utiltestingapi.MakeResourceFlavor("tas-flavor").
+				NodeLabel("node-group", "tas").
+				TopologyName("default").Obj()
+			util.MustCreate(ctx, k8sClient, tasFlavor)
+
+			clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*utiltestingapi.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "999").Obj()).
+				Obj()
+			util.MustCreate(ctx, k8sClient, clusterQueue)
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			util.MustCreate(ctx, k8sClient, localQueue)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteAllPodsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+			for _, node := range nodes {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+			}
+		})
+
+		ginkgo.It("non-TAS pod terminates, releasing capacity", func() {
+			var wl *kueue.Workload
+			var nonTasPod *corev1.Pod
+
+			ginkgo.By("create a non-TAS pod which consumes the node's capacity", func() {
+				nonTasPod = testingpod.MakePod("pod", ns.Name).
+					Request(corev1.ResourceCPU, "1").
+					NodeName("node1").
+					Obj()
+				nonTasPod.Spec.TerminationGracePeriodSeconds = ptr.To[int64](0)
+				util.MustCreate(ctx, k8sClient, nonTasPod)
+			})
+
+			ginkgo.By("create a workload which requires the node's capacity", func() {
+				wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+					Queue("local-queue").
+					Request(corev1.ResourceCPU, "1").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+
+				ginkgo.By("verify the workload is not admitted", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(workload.IsAdmitted(wl)).To(gomega.BeFalse())
+					}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
+				})
+			})
+
+			ginkgo.By("terminate the non-TAS pod", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(nonTasPod), nonTasPod)).To(gomega.Succeed())
+					util.SetPodsPhase(ctx, k8sClient, corev1.PodSucceeded, nonTasPod)
+					g.Expect(k8sClient.Update(ctx, nonTasPod)).Should(gomega.Succeed())
+				}).Should(gomega.Succeed(), util.Timeout, util.Interval)
```

  Nice catch.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-19T10:05:45Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:65** by [@gabesaba](https://github.com/gabesaba) — 2026-01-19T10:05:44Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+}
+
+type podUsageValue struct {
+	node  string
+	usage resources.Requests
+}
+
+// addOrUpdate assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) addOrUpdate(pod corev1.Pod, log logr.Logger) {
+	// skip unscheduled pods as they don't use any capacity.
+	if len(pod.Spec.NodeName) == 0 {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting unscheduled pod", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+	// delete terminated pods as they no longer use any capacity.
+	if utilpod.IsTerminated(&pod) {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting terminated", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
+		return
+	}
+	// skip pods accounted for by TAS.
+	if utiltas.IsTAS(&pod) {
+		if n.cached(pod) {
+			log.V(5).Info("Deleting pod accounted for by TAS", "pod", client.ObjectKeyFromObject(&pod))
+			n.delete(client.ObjectKeyFromObject(&pod))
+		}
```

  Deferring to duplicate comment here https://github.com/kubernetes-sigs/kueue/pull/8484#discussion_r2698920941

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-19T10:08:10Z

- **pkg/controller/tas/non_tas_usage_controller.go:78** by [@gabesaba](https://github.com/gabesaba) — 2026-01-19T10:08:10Z
```diff
@@ -0,0 +1,108 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	schdcache "sigs.k8s.io/kueue/pkg/cache/scheduler"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+)
+
+func newNonTasUsageReconciler(client client.Client, cache *schdcache.Cache, qcache *qcache.Manager) *NonTasUsageReconciler {
+	return &NonTasUsageReconciler{
+		Client: client,
+		Cache:  cache,
+		QCache: qcache,
+	}
+}
+
+// NonTasUsageReconciler monitors pods to update
+// the TAS cache with non-TAS usage.
+type NonTasUsageReconciler struct {
+	client.Client
+	Scheme      *runtime.Scheme
+	Cache       *schdcache.Cache
+	QCache      *qcache.Manager
+	roleTracker *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*NonTasUsageReconciler)(nil)
+var _ predicate.TypedPredicate[*corev1.Pod] = (*NonTasUsageReconciler)(nil)
+
+//+kubebuilder:rbac:groups=core,resources=pods,verbs=get;list;watch
+
+func (r *NonTasUsageReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := klog.FromContext(ctx)
+	log.V(3).Info("Non-TAS usage cache reconciling", "pod", req.NamespacedName)
+	var pod corev1.Pod
+	err := r.Get(ctx, req.NamespacedName, &pod)
+	if err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return ctrl.Result{}, err
+		}
+		log.V(5).Info("Idempotently deleting not found pod", "pod", req.NamespacedName)
+		r.Cache.TASCache().DeletePodByKey(req.NamespacedName)
+		r.QCache.QueueAllInadmissible(ctx)
+		return ctrl.Result{}, nil
+	}
+
+	r.Cache.TASCache().AddOrUpdatePod(pod, log)
+	r.QCache.QueueAllInadmissible(ctx)
```

  I agree. For now, I will add a hack to the integration test to requeue the workloads. Tracking resolution in #8653

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-19T10:16:00Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:44** by [@gabesaba](https://github.com/gabesaba) — 2026-01-19T10:16:00Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+}
+
+type podUsageValue struct {
+	node  string
+	usage resources.Requests
+}
+
+// addOrUpdate assumes that tas cache's write lock is held.
+func (n *nonTasUsageCache) addOrUpdate(pod corev1.Pod) {
+	// skip unscheduled or terminal pods as they don't use any capacity
+	if len(pod.Spec.NodeName) == 0 || utilpod.IsTerminated(&pod) {
```

  Related to https://github.com/kubernetes-sigs/kueue/issues/5310, I prefer to keep reconciliation logic outside of the event filter. Let me see if I can move NodeName to the filter, but IsTerminated should stay inside the reconciliation loop.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-19T10:25:54Z

- **pkg/cache/scheduler/tas_flavor.go:147** by [@gabesaba](https://github.com/gabesaba) — 2026-01-19T10:25:54Z
```diff
@@ -151,20 +144,13 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	for domainID, usage := range c.usage {
 		snapshot.addTASUsage(domainID, usage)
 	}
-	for _, pod := range pods {
-		// skip unscheduled or terminal pods as they don't use any capacity
-		if len(pod.Spec.NodeName) == 0 || utilpod.IsTerminated(&pod) {
-			continue
-		}
-		if domainID, ok := nodeToDomain[pod.Spec.NodeName]; ok {
-			requests := resourcehelpers.PodRequests(&pod, resourcehelpers.PodResourcesOptions{})
-			usage := resources.NewRequests(requests)
-			snapshot.addNonTASUsage(domainID, usage)
+	for _, podUsage := range c.nonTasUsageCache.podUsage {
```

  As with #5310, I prefer to keep reconciliation logic outside of the filters (are there strong enough guarantees regarding ordering/delivery/behavior on startup inside the filters?).
  
  I will update the filters to filter out irrelevant pods (non-TAS, NodeName not set)

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-19T11:13:09Z

- **test/util/pod_webhook.go:37** by [@gabesaba](https://github.com/gabesaba) — 2026-01-19T11:13:09Z
```diff
@@ -0,0 +1,53 @@
+// Copyright The Kubernetes Authors.
+//
+// Licensed under the Apache License, Version 2.0 (the "License");
+// you may not use this file except in compliance with the License.
+// You may obtain a copy of the License at
+//
+//     http://www.apache.org/licenses/LICENSE-2.0
+//
+// Unless required by applicable law or agreed to in writing, software
+// distributed under the License is distributed on an "AS IS" BASIS,
+// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+// See the License for the specific language governing permissions and
+// limitations under the License.
+
+package util
+
+import (
+	"context"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+func SetupPodWebhookForTesting(mgr manager.Manager) error {
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&corev1.Pod{}).
+		WithDefaulter(&podWebhook{}).
+		WithValidator(&podWebhook{}).
+		Complete()
+}
+
+// podWebhook is a dummy webhook, no-op validation and defaulting,
+// for integration tests which need to create pods.
+type podWebhook struct{}
```

  Made this a library in #8656

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-19T11:51:39Z

- **pkg/cache/scheduler/tas_flavor.go:147** by [@mimowo](https://github.com/mimowo) — 2026-01-19T11:51:39Z
```diff
@@ -151,20 +144,13 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	for domainID, usage := range c.usage {
 		snapshot.addTASUsage(domainID, usage)
 	}
-	for _, pod := range pods {
-		// skip unscheduled or terminal pods as they don't use any capacity
-		if len(pod.Spec.NodeName) == 0 || utilpod.IsTerminated(&pod) {
-			continue
-		}
-		if domainID, ok := nodeToDomain[pod.Spec.NodeName]; ok {
-			requests := resourcehelpers.PodRequests(&pod, resourcehelpers.PodResourcesOptions{})
-			usage := resources.NewRequests(requests)
-			snapshot.addNonTASUsage(domainID, usage)
+	for _, podUsage := range c.nonTasUsageCache.podUsage {
```

  ok, let's revist the approach only if needed

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-19T14:04:54Z

- **pkg/controller/tas/non_tas_usage_controller.go:95** by [@gabesaba](https://github.com/gabesaba) — 2026-01-19T14:02:13Z
```diff
@@ -0,0 +1,121 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	schdcache "sigs.k8s.io/kueue/pkg/cache/scheduler"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+func newNonTasUsageReconciler(client client.Client, cache *schdcache.Cache, qcache *qcache.Manager) *NonTasUsageReconciler {
+	return &NonTasUsageReconciler{
+		Client: client,
+		Cache:  cache,
+		QCache: qcache,
+	}
+}
+
+// NonTasUsageReconciler monitors pods to update
+// the TAS cache with non-TAS usage.
+type NonTasUsageReconciler struct {
+	client.Client
+	Cache       *schdcache.Cache
+	QCache      *qcache.Manager
+	roleTracker *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*NonTasUsageReconciler)(nil)
+var _ predicate.TypedPredicate[*corev1.Pod] = (*NonTasUsageReconciler)(nil)
+
+//+kubebuilder:rbac:groups=core,resources=pods,verbs=get;list;watch
+
+func (r *NonTasUsageReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := klog.FromContext(ctx).WithValues("pod", req.NamespacedName)
+	log.V(3).Info("Non-TAS usage cache reconciling")
+	var pod corev1.Pod
+	err := r.Get(ctx, req.NamespacedName, &pod)
+	if err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return ctrl.Result{}, err
+		}
+		log.V(5).Info("Idempotently deleting not found pod")
+		r.Cache.TASCache().DeletePodByKey(req.NamespacedName)
+		return ctrl.Result{}, nil
+	}
+
+	r.Cache.TASCache().AddOrUpdatePod(pod, log)
+	return ctrl.Result{}, nil
+}
+
+func filterPod(pod *corev1.Pod) bool {
+	if utiltas.IsTAS(pod) {
+		return false
+	} else if len(pod.Spec.NodeName) == 0 {
+		// skip unscheduled pods as they don't use any capacity.
+		return false
+	}
+	return true
+}
+
+func (r *NonTasUsageReconciler) Create(e event.TypedCreateEvent[*corev1.Pod]) bool {
+	return filterPod(e.Object)
+}
+
+func (r *NonTasUsageReconciler) Update(e event.TypedUpdateEvent[*corev1.Pod]) bool {
+	return filterPod(e.ObjectOld) || filterPod(e.ObjectNew)
```

  should we only base this decision on ObjectNew? As @mimowo pointed out NodeName can be set, but not mutated or unset later

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-19T14:24:03Z

- **pkg/controller/tas/non_tas_usage_controller.go:95** by [@gabesaba](https://github.com/gabesaba) — 2026-01-19T14:24:03Z
```diff
@@ -0,0 +1,121 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	schdcache "sigs.k8s.io/kueue/pkg/cache/scheduler"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+func newNonTasUsageReconciler(client client.Client, cache *schdcache.Cache, qcache *qcache.Manager) *NonTasUsageReconciler {
+	return &NonTasUsageReconciler{
+		Client: client,
+		Cache:  cache,
+		QCache: qcache,
+	}
+}
+
+// NonTasUsageReconciler monitors pods to update
+// the TAS cache with non-TAS usage.
+type NonTasUsageReconciler struct {
+	client.Client
+	Cache       *schdcache.Cache
+	QCache      *qcache.Manager
+	roleTracker *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*NonTasUsageReconciler)(nil)
+var _ predicate.TypedPredicate[*corev1.Pod] = (*NonTasUsageReconciler)(nil)
+
+//+kubebuilder:rbac:groups=core,resources=pods,verbs=get;list;watch
+
+func (r *NonTasUsageReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := klog.FromContext(ctx).WithValues("pod", req.NamespacedName)
+	log.V(3).Info("Non-TAS usage cache reconciling")
+	var pod corev1.Pod
+	err := r.Get(ctx, req.NamespacedName, &pod)
+	if err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return ctrl.Result{}, err
+		}
+		log.V(5).Info("Idempotently deleting not found pod")
+		r.Cache.TASCache().DeletePodByKey(req.NamespacedName)
+		return ctrl.Result{}, nil
+	}
+
+	r.Cache.TASCache().AddOrUpdatePod(pod, log)
+	return ctrl.Result{}, nil
+}
+
+func filterPod(pod *corev1.Pod) bool {
+	if utiltas.IsTAS(pod) {
+		return false
+	} else if len(pod.Spec.NodeName) == 0 {
+		// skip unscheduled pods as they don't use any capacity.
+		return false
+	}
+	return true
+}
+
+func (r *NonTasUsageReconciler) Create(e event.TypedCreateEvent[*corev1.Pod]) bool {
+	return filterPod(e.Object)
+}
+
+func (r *NonTasUsageReconciler) Update(e event.TypedUpdateEvent[*corev1.Pod]) bool {
+	return filterPod(e.ObjectOld) || filterPod(e.ObjectNew)
```

  I updated to do this. Let me know if you have any objections @mimowo

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-19T14:27:02Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:44** by [@mimowo](https://github.com/mimowo) — 2026-01-19T14:27:02Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"sync"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+	lock     *sync.RWMutex
+}
+
+type podUsageValue struct {
+	node  string
+	usage resources.Requests
+}
+
+func (n *nonTasUsageCache) addOrUpdate(pod corev1.Pod, log logr.Logger) {
```

  nit, but I would rather name it "update" since it can result in "add, delete, update" for the cache

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-19T14:28:07Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:36** by [@mimowo](https://github.com/mimowo) — 2026-01-19T14:28:07Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"sync"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+	lock     *sync.RWMutex
```

  nit, but why by ptr? In all other places in code we just extend the base type, eg: https://github.com/kubernetes-sigs/kueue/blob/41c74539c8e9a6035f30bddd49d6202840ce8832/pkg/cache/scheduler/localqueue.go#L29

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-19T14:30:16Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:36** by [@gabesaba](https://github.com/gabesaba) — 2026-01-19T14:30:15Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"sync"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+	lock     *sync.RWMutex
```

  Because I'm passing around nonTasUsageCache by value.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-19T14:31:04Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:44** by [@mimowo](https://github.com/mimowo) — 2026-01-19T14:31:04Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"sync"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+	lock     *sync.RWMutex
+}
+
+type podUsageValue struct {
+	node  string
+	usage resources.Requests
+}
+
+func (n *nonTasUsageCache) addOrUpdate(pod corev1.Pod, log logr.Logger) {
```

  The alternative is to move the handling of terminal directly in the reconciler

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-19T14:34:54Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:44** by [@gabesaba](https://github.com/gabesaba) — 2026-01-19T14:34:54Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"sync"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+	lock     *sync.RWMutex
+}
+
+type podUsageValue struct {
+	node  string
+	usage resources.Requests
+}
+
+func (n *nonTasUsageCache) addOrUpdate(pod corev1.Pod, log logr.Logger) {
```

  I'm happy with the name change to `update`, and to leave the logic encapsulated

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-19T14:45:32Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:36** by [@mimowo](https://github.com/mimowo) — 2026-01-19T14:45:32Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"sync"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+	lock     *sync.RWMutex
```

  I see, I don't have a strong opinion here, but it feels risky to pass by value caches which need to be singletons by definition.Still, I don't see a "bug" here, so up to you if you want to keep as is.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-19T15:00:22Z

- **pkg/cache/scheduler/tas_non_tas_pod_cache.go:36** by [@gabesaba](https://github.com/gabesaba) — 2026-01-19T15:00:21Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"sync"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	resourcehelpers "k8s.io/component-helpers/resource"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+)
+
+// nonTasUsageCache caches pod usage, to avoid
+// the hot path documented in kueue#8449.
+type nonTasUsageCache struct {
+	podUsage map[types.NamespacedName]podUsageValue
+	lock     *sync.RWMutex
```

  Updated to ptr

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-19T15:18:13Z

- **pkg/cache/scheduler/tas_cache.go:123** by [@mimowo](https://github.com/mimowo) — 2026-01-19T15:18:13Z
```diff
@@ -108,3 +117,13 @@ func (t *tasCache) DeleteTopology(name kueue.TopologyReference) {
 		}
 	}
 }
+
+// Update may add a pod to the cache, or
+// delete a terminated pod.
+func (t *tasCache) Update(pod corev1.Pod, log logr.Logger) {
```

  Are you sure about passing it by value? It means deepcopy of Pod which might be big sometimes. We usually pass Pods and Nodes by ptr. This is also the common practice in the core k8s for example check [Job controller](https://github.com/kubernetes/kubernetes/blob/14dec2184bbeb78743095009db3c44e750884351/pkg/controller/job/job_controller.go#L772), or [Deployment controller](https://github.com/kubernetes/kubernetes/blob/14dec2184bbeb78743095009db3c44e750884351/pkg/controller/deployment/deployment_controller.go#L446). 
  
  IIRC in the core k8s there were PRs to specifically fix passing Pods by value to use ptr.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-19T15:25:27Z

- **pkg/controller/tas/non_tas_usage_controller.go:95** by [@mimowo](https://github.com/mimowo) — 2026-01-19T15:25:27Z
```diff
@@ -0,0 +1,121 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	schdcache "sigs.k8s.io/kueue/pkg/cache/scheduler"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+func newNonTasUsageReconciler(client client.Client, cache *schdcache.Cache, qcache *qcache.Manager) *NonTasUsageReconciler {
+	return &NonTasUsageReconciler{
+		Client: client,
+		Cache:  cache,
+		QCache: qcache,
+	}
+}
+
+// NonTasUsageReconciler monitors pods to update
+// the TAS cache with non-TAS usage.
+type NonTasUsageReconciler struct {
+	client.Client
+	Cache       *schdcache.Cache
+	QCache      *qcache.Manager
+	roleTracker *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*NonTasUsageReconciler)(nil)
+var _ predicate.TypedPredicate[*corev1.Pod] = (*NonTasUsageReconciler)(nil)
+
+//+kubebuilder:rbac:groups=core,resources=pods,verbs=get;list;watch
+
+func (r *NonTasUsageReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := klog.FromContext(ctx).WithValues("pod", req.NamespacedName)
+	log.V(3).Info("Non-TAS usage cache reconciling")
+	var pod corev1.Pod
+	err := r.Get(ctx, req.NamespacedName, &pod)
+	if err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return ctrl.Result{}, err
+		}
+		log.V(5).Info("Idempotently deleting not found pod")
+		r.Cache.TASCache().DeletePodByKey(req.NamespacedName)
+		return ctrl.Result{}, nil
+	}
+
+	r.Cache.TASCache().AddOrUpdatePod(pod, log)
+	return ctrl.Result{}, nil
+}
+
+func filterPod(pod *corev1.Pod) bool {
+	if utiltas.IsTAS(pod) {
+		return false
+	} else if len(pod.Spec.NodeName) == 0 {
+		// skip unscheduled pods as they don't use any capacity.
+		return false
+	}
+	return true
+}
+
+func (r *NonTasUsageReconciler) Create(e event.TypedCreateEvent[*corev1.Pod]) bool {
+	return filterPod(e.Object)
+}
+
+func (r *NonTasUsageReconciler) Update(e event.TypedUpdateEvent[*corev1.Pod]) bool {
+	return filterPod(e.ObjectOld) || filterPod(e.ObjectNew)
```

  That is fine either way. If you don't look for the ObjectOld, it means we may sometimes schedule Reconcile for Pod updates which is already running. For example there will be updates when the Pod is transitioning:
  - Pending -> InitContainer running 
  - Pending -> Running (already has nodeName) [we could ignore from accounting PoV)
  - Running -> Runniing (+ Ready) [we could ignore from accounting PoV, because Ready [readiness probe passing does not release resources]
  - Running -> Running (+ deletionTimestamp) [we could ignore from accounting PoV, because deletionTimestamp does not release resources]
  
  Still, this is "just about performance" optimization. I don't have any evidence it would be critical or even showing up in any pprofs.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-19T15:26:49Z

- **pkg/controller/tas/non_tas_usage_controller.go:95** by [@mimowo](https://github.com/mimowo) — 2026-01-19T15:26:49Z
```diff
@@ -0,0 +1,121 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	schdcache "sigs.k8s.io/kueue/pkg/cache/scheduler"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+func newNonTasUsageReconciler(client client.Client, cache *schdcache.Cache, qcache *qcache.Manager) *NonTasUsageReconciler {
+	return &NonTasUsageReconciler{
+		Client: client,
+		Cache:  cache,
+		QCache: qcache,
+	}
+}
+
+// NonTasUsageReconciler monitors pods to update
+// the TAS cache with non-TAS usage.
+type NonTasUsageReconciler struct {
+	client.Client
+	Cache       *schdcache.Cache
+	QCache      *qcache.Manager
+	roleTracker *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*NonTasUsageReconciler)(nil)
+var _ predicate.TypedPredicate[*corev1.Pod] = (*NonTasUsageReconciler)(nil)
+
+//+kubebuilder:rbac:groups=core,resources=pods,verbs=get;list;watch
+
+func (r *NonTasUsageReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := klog.FromContext(ctx).WithValues("pod", req.NamespacedName)
+	log.V(3).Info("Non-TAS usage cache reconciling")
+	var pod corev1.Pod
+	err := r.Get(ctx, req.NamespacedName, &pod)
+	if err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return ctrl.Result{}, err
+		}
+		log.V(5).Info("Idempotently deleting not found pod")
+		r.Cache.TASCache().DeletePodByKey(req.NamespacedName)
+		return ctrl.Result{}, nil
+	}
+
+	r.Cache.TASCache().AddOrUpdatePod(pod, log)
+	return ctrl.Result{}, nil
+}
+
+func filterPod(pod *corev1.Pod) bool {
+	if utiltas.IsTAS(pod) {
+		return false
+	} else if len(pod.Spec.NodeName) == 0 {
+		// skip unscheduled pods as they don't use any capacity.
+		return false
+	}
+	return true
+}
+
+func (r *NonTasUsageReconciler) Create(e event.TypedCreateEvent[*corev1.Pod]) bool {
+	return filterPod(e.Object)
+}
+
+func (r *NonTasUsageReconciler) Update(e event.TypedUpdateEvent[*corev1.Pod]) bool {
+	return filterPod(e.ObjectOld) || filterPod(e.ObjectNew)
```

  So, no objections to keep it as is, just pointing out there are some scenarios (multiple) where the filtering is suboptimal. Still, it is better that originally anyway, because controller-runtime's cache has to keep track of all pods anyway.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-20T07:50:44Z

- **pkg/cache/scheduler/tas_cache_test.go:6017** by [@mimowo](https://github.com/mimowo) — 2026-01-20T07:50:44Z
```diff
@@ -5980,6 +6013,9 @@ func TestFindTopologyAssignments(t *testing.T) {
 				TopologyName: "default",
 				NodeLabels:   tc.nodeLabels,
 			}
+			for _, pod := range tc.pods {
+				tasCache.Update(pod, logr.FromContextOrDiscard(ctx))
```

  non-blocking nit, it would be better to get the log from `ctx, _ := utiltesting.ContextWithLog(t)`, in case we want to reuse it in the file

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-20T07:52:48Z

- **pkg/controller/tas/non_tas_usage_controller.go:53** by [@mimowo](https://github.com/mimowo) — 2026-01-20T07:52:49Z
```diff
@@ -0,0 +1,121 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	schdcache "sigs.k8s.io/kueue/pkg/cache/scheduler"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+func newNonTasUsageReconciler(client client.Client, cache *schdcache.Cache, qcache *qcache.Manager) *NonTasUsageReconciler {
+	return &NonTasUsageReconciler{
+		Client: client,
+		Cache:  cache,
+		QCache: qcache,
+	}
+}
+
+// NonTasUsageReconciler monitors pods to update
+// the TAS cache with non-TAS usage.
+type NonTasUsageReconciler struct {
+	client.Client
+	Cache       *schdcache.Cache
+	QCache      *qcache.Manager
```

  non-blocking nit, but this is unused

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-20T07:53:22Z

- **pkg/controller/tas/non_tas_usage_controller.go:52** by [@mimowo](https://github.com/mimowo) — 2026-01-20T07:53:22Z
```diff
@@ -0,0 +1,121 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	schdcache "sigs.k8s.io/kueue/pkg/cache/scheduler"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+func newNonTasUsageReconciler(client client.Client, cache *schdcache.Cache, qcache *qcache.Manager) *NonTasUsageReconciler {
+	return &NonTasUsageReconciler{
+		Client: client,
+		Cache:  cache,
+		QCache: qcache,
+	}
+}
+
+// NonTasUsageReconciler monitors pods to update
+// the TAS cache with non-TAS usage.
+type NonTasUsageReconciler struct {
+	client.Client
+	Cache       *schdcache.Cache
```

  non-blocking nit: this should not be exported

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-20T08:25:55Z

- **test/integration/singlecluster/tas/tas_test.go:295** by [@mimowo](https://github.com/mimowo) — 2026-01-20T08:25:55Z
```diff
@@ -212,6 +214,219 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 
+	ginkgo.When("non-TAS pod exists", func() {
+		var (
+			nodes        []corev1.Node
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueue.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			nodes = []corev1.Node{
+				*testingnode.MakeNode("node1").
+					Label("node-group", "tas").
+					Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+					Label(utiltesting.DefaultRackTopologyLevel, "r1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("3"),
+					}).
+					Ready().
+					Obj(),
+			}
+			util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+			topology = utiltestingapi.MakeDefaultTwoLevelTopology("default")
+			util.MustCreate(ctx, k8sClient, topology)
+
+			tasFlavor = utiltestingapi.MakeResourceFlavor("tas-flavor").
+				NodeLabel("node-group", "tas").
+				TopologyName("default").Obj()
+			util.MustCreate(ctx, k8sClient, tasFlavor)
+
+			clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*utiltestingapi.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "999").Obj()).
+				Obj()
+			util.MustCreate(ctx, k8sClient, clusterQueue)
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			util.MustCreate(ctx, k8sClient, localQueue)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+			for _, node := range nodes {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+			}
+		})
+
+		ginkgo.It("non-TAS pod terminates, releasing capacity", func() {
+			var wl *kueue.Workload
+			var nonTasPod *corev1.Pod
+
+			ginkgo.By("create a non-TAS pod which consumes the node's capacity", func() {
+				nonTasPod = testingpod.MakePod("pod", ns.Name).
+					Request(corev1.ResourceCPU, "1").
+					NodeName("node1").
+					TerminationGracePeriod(0).
+					Obj()
+				util.MustCreate(ctx, k8sClient, nonTasPod)
+			})
+
+			ginkgo.By("create a workload which requires the node's capacity", func() {
+				wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+					Queue("local-queue").
+					Request(corev1.ResourceCPU, "1").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+
+				ginkgo.By("verify the workload is not admitted", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(workload.IsAdmitted(wl)).To(gomega.BeFalse())
+					}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
```

  nit, I think it is enough to check the metric for the Pending workloads, because we have the Workload in inadmissible, so we know it was already considered by scheduler. Yes, I know checking the Consistently does not rely on this "side effect", but it has a drawback of testing time increased, so I think checking the metric is a sensible compromise. We rely on this in many other tests too.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-20T08:26:18Z

- **test/integration/singlecluster/tas/tas_test.go:345** by [@mimowo](https://github.com/mimowo) — 2026-01-20T08:26:18Z
```diff
@@ -212,6 +214,219 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 
+	ginkgo.When("non-TAS pod exists", func() {
+		var (
+			nodes        []corev1.Node
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueue.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			nodes = []corev1.Node{
+				*testingnode.MakeNode("node1").
+					Label("node-group", "tas").
+					Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+					Label(utiltesting.DefaultRackTopologyLevel, "r1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("3"),
+					}).
+					Ready().
+					Obj(),
+			}
+			util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+			topology = utiltestingapi.MakeDefaultTwoLevelTopology("default")
+			util.MustCreate(ctx, k8sClient, topology)
+
+			tasFlavor = utiltestingapi.MakeResourceFlavor("tas-flavor").
+				NodeLabel("node-group", "tas").
+				TopologyName("default").Obj()
+			util.MustCreate(ctx, k8sClient, tasFlavor)
+
+			clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*utiltestingapi.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "999").Obj()).
+				Obj()
+			util.MustCreate(ctx, k8sClient, clusterQueue)
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			util.MustCreate(ctx, k8sClient, localQueue)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+			for _, node := range nodes {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+			}
+		})
+
+		ginkgo.It("non-TAS pod terminates, releasing capacity", func() {
+			var wl *kueue.Workload
+			var nonTasPod *corev1.Pod
+
+			ginkgo.By("create a non-TAS pod which consumes the node's capacity", func() {
+				nonTasPod = testingpod.MakePod("pod", ns.Name).
+					Request(corev1.ResourceCPU, "1").
+					NodeName("node1").
+					TerminationGracePeriod(0).
+					Obj()
+				util.MustCreate(ctx, k8sClient, nonTasPod)
+			})
+
+			ginkgo.By("create a workload which requires the node's capacity", func() {
+				wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+					Queue("local-queue").
+					Request(corev1.ResourceCPU, "1").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+
+				ginkgo.By("verify the workload is not admitted", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(workload.IsAdmitted(wl)).To(gomega.BeFalse())
+					}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
+				})
+			})
+
+			ginkgo.By("terminate the non-TAS pod", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(nonTasPod), nonTasPod)).To(gomega.Succeed())
+					util.SetPodsPhase(ctx, k8sClient, corev1.PodSucceeded, nonTasPod)
+					g.Expect(k8sClient.Update(ctx, nonTasPod)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			// https://github.com/kubernetes-sigs/kueue/issues/8653
+			ginkgo.By("hack to requeue workload", func() {
+				cqs := sets.New[kueue.ClusterQueueReference]("cluster-queue")
+				qManager.QueueInadmissibleWorkloads(ctx, cqs)
+			})
+
+			ginkgo.By("expect TAS pod to admit", func() {
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+				util.ExpectAdmittedWorkloadsTotalMetric(clusterQueue, "", 1)
+			})
+		})
+
+		ginkgo.It("non-TAS pod is deleted, releasing capacity", func() {
+			var wl *kueue.Workload
+			var nonTasPod *corev1.Pod
+
+			ginkgo.By("creating a non-TAS pod which consumes the node's capacity", func() {
+				nonTasPod = testingpod.MakePod("pod", ns.Name).
+					Request(corev1.ResourceCPU, "1").
+					NodeName("node1").
+					TerminationGracePeriod(0).
+					Obj()
+				util.MustCreate(ctx, k8sClient, nonTasPod)
+			})
+
+			ginkgo.By("creating a workload which requires the node's capacity", func() {
+				wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+					Queue("local-queue").
+					Request(corev1.ResourceCPU, "1").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+			})
+
+			ginkgo.By("verify the workload is not admitted", func() {
+				util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+				gomega.Consistently(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(wl)).To(gomega.BeFalse())
+				}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
```

  nit, as above, I think relying on the metric is enough

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-20T08:27:41Z

- **test/integration/singlecluster/tas/tas_test.go:295** by [@mimowo](https://github.com/mimowo) — 2026-01-20T08:27:41Z
```diff
@@ -212,6 +214,219 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 
+	ginkgo.When("non-TAS pod exists", func() {
+		var (
+			nodes        []corev1.Node
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueue.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			nodes = []corev1.Node{
+				*testingnode.MakeNode("node1").
+					Label("node-group", "tas").
+					Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+					Label(utiltesting.DefaultRackTopologyLevel, "r1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("3"),
+					}).
+					Ready().
+					Obj(),
+			}
+			util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+			topology = utiltestingapi.MakeDefaultTwoLevelTopology("default")
+			util.MustCreate(ctx, k8sClient, topology)
+
+			tasFlavor = utiltestingapi.MakeResourceFlavor("tas-flavor").
+				NodeLabel("node-group", "tas").
+				TopologyName("default").Obj()
+			util.MustCreate(ctx, k8sClient, tasFlavor)
+
+			clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*utiltestingapi.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "999").Obj()).
+				Obj()
+			util.MustCreate(ctx, k8sClient, clusterQueue)
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			util.MustCreate(ctx, k8sClient, localQueue)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+			for _, node := range nodes {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+			}
+		})
+
+		ginkgo.It("non-TAS pod terminates, releasing capacity", func() {
+			var wl *kueue.Workload
+			var nonTasPod *corev1.Pod
+
+			ginkgo.By("create a non-TAS pod which consumes the node's capacity", func() {
+				nonTasPod = testingpod.MakePod("pod", ns.Name).
+					Request(corev1.ResourceCPU, "1").
+					NodeName("node1").
+					TerminationGracePeriod(0).
+					Obj()
+				util.MustCreate(ctx, k8sClient, nonTasPod)
+			})
+
+			ginkgo.By("create a workload which requires the node's capacity", func() {
+				wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+					Queue("local-queue").
+					Request(corev1.ResourceCPU, "1").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+
+				ginkgo.By("verify the workload is not admitted", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(workload.IsAdmitted(wl)).To(gomega.BeFalse())
+					}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
```

  Or if you want Consistently, then I would use some dedicated ShortConsistentlyTimeout being 10ms. This should in most cases be enough time to detect incorrect admission.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-20T08:31:44Z

- **test/integration/singlecluster/tas/tas_test.go:319** by [@mimowo](https://github.com/mimowo) — 2026-01-20T08:31:44Z
```diff
@@ -212,6 +214,219 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 
+	ginkgo.When("non-TAS pod exists", func() {
+		var (
+			nodes        []corev1.Node
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueue.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			nodes = []corev1.Node{
+				*testingnode.MakeNode("node1").
+					Label("node-group", "tas").
+					Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+					Label(utiltesting.DefaultRackTopologyLevel, "r1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("3"),
+					}).
+					Ready().
+					Obj(),
+			}
+			util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+			topology = utiltestingapi.MakeDefaultTwoLevelTopology("default")
+			util.MustCreate(ctx, k8sClient, topology)
+
+			tasFlavor = utiltestingapi.MakeResourceFlavor("tas-flavor").
+				NodeLabel("node-group", "tas").
+				TopologyName("default").Obj()
+			util.MustCreate(ctx, k8sClient, tasFlavor)
+
+			clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*utiltestingapi.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "999").Obj()).
+				Obj()
+			util.MustCreate(ctx, k8sClient, clusterQueue)
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			util.MustCreate(ctx, k8sClient, localQueue)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+			for _, node := range nodes {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+			}
+		})
+
+		ginkgo.It("non-TAS pod terminates, releasing capacity", func() {
+			var wl *kueue.Workload
+			var nonTasPod *corev1.Pod
+
+			ginkgo.By("create a non-TAS pod which consumes the node's capacity", func() {
+				nonTasPod = testingpod.MakePod("pod", ns.Name).
+					Request(corev1.ResourceCPU, "1").
+					NodeName("node1").
+					TerminationGracePeriod(0).
+					Obj()
+				util.MustCreate(ctx, k8sClient, nonTasPod)
+			})
+
+			ginkgo.By("create a workload which requires the node's capacity", func() {
+				wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+					Queue("local-queue").
+					Request(corev1.ResourceCPU, "1").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+
+				ginkgo.By("verify the workload is not admitted", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(workload.IsAdmitted(wl)).To(gomega.BeFalse())
+					}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
+				})
+			})
+
+			ginkgo.By("terminate the non-TAS pod", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(nonTasPod), nonTasPod)).To(gomega.Succeed())
+					util.SetPodsPhase(ctx, k8sClient, corev1.PodSucceeded, nonTasPod)
+					g.Expect(k8sClient.Update(ctx, nonTasPod)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			// https://github.com/kubernetes-sigs/kueue/issues/8653
+			ginkgo.By("hack to requeue workload", func() {
+				cqs := sets.New[kueue.ClusterQueueReference]("cluster-queue")
+				qManager.QueueInadmissibleWorkloads(ctx, cqs)
+			})
+
+			ginkgo.By("expect TAS pod to admit", func() {
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+				util.ExpectAdmittedWorkloadsTotalMetric(clusterQueue, "", 1)
+			})
+		})
+
+		ginkgo.It("non-TAS pod is deleted, releasing capacity", func() {
```

  nit: One common case (and actually a bug in the initial implementation) is to show that releasing capacity is deferred while the pod is terminating (nodeName != nil, phase=Running, deletionTimestamp != nil)

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-20T10:33:14Z

- **test/integration/singlecluster/tas/tas_test.go:319** by [@gabesaba](https://github.com/gabesaba) — 2026-01-20T10:33:14Z
```diff
@@ -212,6 +214,219 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 
+	ginkgo.When("non-TAS pod exists", func() {
+		var (
+			nodes        []corev1.Node
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueue.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			nodes = []corev1.Node{
+				*testingnode.MakeNode("node1").
+					Label("node-group", "tas").
+					Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+					Label(utiltesting.DefaultRackTopologyLevel, "r1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("3"),
+					}).
+					Ready().
+					Obj(),
+			}
+			util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+			topology = utiltestingapi.MakeDefaultTwoLevelTopology("default")
+			util.MustCreate(ctx, k8sClient, topology)
+
+			tasFlavor = utiltestingapi.MakeResourceFlavor("tas-flavor").
+				NodeLabel("node-group", "tas").
+				TopologyName("default").Obj()
+			util.MustCreate(ctx, k8sClient, tasFlavor)
+
+			clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*utiltestingapi.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "999").Obj()).
+				Obj()
+			util.MustCreate(ctx, k8sClient, clusterQueue)
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			util.MustCreate(ctx, k8sClient, localQueue)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+			for _, node := range nodes {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+			}
+		})
+
+		ginkgo.It("non-TAS pod terminates, releasing capacity", func() {
+			var wl *kueue.Workload
+			var nonTasPod *corev1.Pod
+
+			ginkgo.By("create a non-TAS pod which consumes the node's capacity", func() {
+				nonTasPod = testingpod.MakePod("pod", ns.Name).
+					Request(corev1.ResourceCPU, "1").
+					NodeName("node1").
+					TerminationGracePeriod(0).
+					Obj()
+				util.MustCreate(ctx, k8sClient, nonTasPod)
+			})
+
+			ginkgo.By("create a workload which requires the node's capacity", func() {
+				wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+					Queue("local-queue").
+					Request(corev1.ResourceCPU, "1").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+
+				ginkgo.By("verify the workload is not admitted", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(workload.IsAdmitted(wl)).To(gomega.BeFalse())
+					}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
+				})
+			})
+
+			ginkgo.By("terminate the non-TAS pod", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(nonTasPod), nonTasPod)).To(gomega.Succeed())
+					util.SetPodsPhase(ctx, k8sClient, corev1.PodSucceeded, nonTasPod)
+					g.Expect(k8sClient.Update(ctx, nonTasPod)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			// https://github.com/kubernetes-sigs/kueue/issues/8653
+			ginkgo.By("hack to requeue workload", func() {
+				cqs := sets.New[kueue.ClusterQueueReference]("cluster-queue")
+				qManager.QueueInadmissibleWorkloads(ctx, cqs)
+			})
+
+			ginkgo.By("expect TAS pod to admit", func() {
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+				util.ExpectAdmittedWorkloadsTotalMetric(clusterQueue, "", 1)
+			})
+		})
+
+		ginkgo.It("non-TAS pod is deleted, releasing capacity", func() {
```

  #8686

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-20T10:33:36Z

- **test/integration/singlecluster/tas/tas_test.go:345** by [@gabesaba](https://github.com/gabesaba) — 2026-01-20T10:33:36Z
```diff
@@ -212,6 +214,219 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 
+	ginkgo.When("non-TAS pod exists", func() {
+		var (
+			nodes        []corev1.Node
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueue.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			nodes = []corev1.Node{
+				*testingnode.MakeNode("node1").
+					Label("node-group", "tas").
+					Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+					Label(utiltesting.DefaultRackTopologyLevel, "r1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("3"),
+					}).
+					Ready().
+					Obj(),
+			}
+			util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+			topology = utiltestingapi.MakeDefaultTwoLevelTopology("default")
+			util.MustCreate(ctx, k8sClient, topology)
+
+			tasFlavor = utiltestingapi.MakeResourceFlavor("tas-flavor").
+				NodeLabel("node-group", "tas").
+				TopologyName("default").Obj()
+			util.MustCreate(ctx, k8sClient, tasFlavor)
+
+			clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*utiltestingapi.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "999").Obj()).
+				Obj()
+			util.MustCreate(ctx, k8sClient, clusterQueue)
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			util.MustCreate(ctx, k8sClient, localQueue)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+			for _, node := range nodes {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+			}
+		})
+
+		ginkgo.It("non-TAS pod terminates, releasing capacity", func() {
+			var wl *kueue.Workload
+			var nonTasPod *corev1.Pod
+
+			ginkgo.By("create a non-TAS pod which consumes the node's capacity", func() {
+				nonTasPod = testingpod.MakePod("pod", ns.Name).
+					Request(corev1.ResourceCPU, "1").
+					NodeName("node1").
+					TerminationGracePeriod(0).
+					Obj()
+				util.MustCreate(ctx, k8sClient, nonTasPod)
+			})
+
+			ginkgo.By("create a workload which requires the node's capacity", func() {
+				wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+					Queue("local-queue").
+					Request(corev1.ResourceCPU, "1").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+
+				ginkgo.By("verify the workload is not admitted", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(workload.IsAdmitted(wl)).To(gomega.BeFalse())
+					}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
+				})
+			})
+
+			ginkgo.By("terminate the non-TAS pod", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(nonTasPod), nonTasPod)).To(gomega.Succeed())
+					util.SetPodsPhase(ctx, k8sClient, corev1.PodSucceeded, nonTasPod)
+					g.Expect(k8sClient.Update(ctx, nonTasPod)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			// https://github.com/kubernetes-sigs/kueue/issues/8653
+			ginkgo.By("hack to requeue workload", func() {
+				cqs := sets.New[kueue.ClusterQueueReference]("cluster-queue")
+				qManager.QueueInadmissibleWorkloads(ctx, cqs)
+			})
+
+			ginkgo.By("expect TAS pod to admit", func() {
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+				util.ExpectAdmittedWorkloadsTotalMetric(clusterQueue, "", 1)
+			})
+		})
+
+		ginkgo.It("non-TAS pod is deleted, releasing capacity", func() {
+			var wl *kueue.Workload
+			var nonTasPod *corev1.Pod
+
+			ginkgo.By("creating a non-TAS pod which consumes the node's capacity", func() {
+				nonTasPod = testingpod.MakePod("pod", ns.Name).
+					Request(corev1.ResourceCPU, "1").
+					NodeName("node1").
+					TerminationGracePeriod(0).
+					Obj()
+				util.MustCreate(ctx, k8sClient, nonTasPod)
+			})
+
+			ginkgo.By("creating a workload which requires the node's capacity", func() {
+				wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+					Queue("local-queue").
+					Request(corev1.ResourceCPU, "1").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+			})
+
+			ginkgo.By("verify the workload is not admitted", func() {
+				util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+				gomega.Consistently(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(wl)).To(gomega.BeFalse())
+				}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
```

  I went with ShortConsistentDuration #8686

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-20T10:33:39Z

- **test/integration/singlecluster/tas/tas_test.go:295** by [@gabesaba](https://github.com/gabesaba) — 2026-01-20T10:33:38Z
```diff
@@ -212,6 +214,219 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 
+	ginkgo.When("non-TAS pod exists", func() {
+		var (
+			nodes        []corev1.Node
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueue.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			nodes = []corev1.Node{
+				*testingnode.MakeNode("node1").
+					Label("node-group", "tas").
+					Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+					Label(utiltesting.DefaultRackTopologyLevel, "r1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("3"),
+					}).
+					Ready().
+					Obj(),
+			}
+			util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+			topology = utiltestingapi.MakeDefaultTwoLevelTopology("default")
+			util.MustCreate(ctx, k8sClient, topology)
+
+			tasFlavor = utiltestingapi.MakeResourceFlavor("tas-flavor").
+				NodeLabel("node-group", "tas").
+				TopologyName("default").Obj()
+			util.MustCreate(ctx, k8sClient, tasFlavor)
+
+			clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*utiltestingapi.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "999").Obj()).
+				Obj()
+			util.MustCreate(ctx, k8sClient, clusterQueue)
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			util.MustCreate(ctx, k8sClient, localQueue)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+			for _, node := range nodes {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+			}
+		})
+
+		ginkgo.It("non-TAS pod terminates, releasing capacity", func() {
+			var wl *kueue.Workload
+			var nonTasPod *corev1.Pod
+
+			ginkgo.By("create a non-TAS pod which consumes the node's capacity", func() {
+				nonTasPod = testingpod.MakePod("pod", ns.Name).
+					Request(corev1.ResourceCPU, "1").
+					NodeName("node1").
+					TerminationGracePeriod(0).
+					Obj()
+				util.MustCreate(ctx, k8sClient, nonTasPod)
+			})
+
+			ginkgo.By("create a workload which requires the node's capacity", func() {
+				wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+					Queue("local-queue").
+					Request(corev1.ResourceCPU, "1").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+
+				ginkgo.By("verify the workload is not admitted", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(workload.IsAdmitted(wl)).To(gomega.BeFalse())
+					}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
```

  #8686

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-20T10:33:41Z

- **pkg/controller/tas/non_tas_usage_controller.go:52** by [@gabesaba](https://github.com/gabesaba) — 2026-01-20T10:33:41Z
```diff
@@ -0,0 +1,121 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	schdcache "sigs.k8s.io/kueue/pkg/cache/scheduler"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+func newNonTasUsageReconciler(client client.Client, cache *schdcache.Cache, qcache *qcache.Manager) *NonTasUsageReconciler {
+	return &NonTasUsageReconciler{
+		Client: client,
+		Cache:  cache,
+		QCache: qcache,
+	}
+}
+
+// NonTasUsageReconciler monitors pods to update
+// the TAS cache with non-TAS usage.
+type NonTasUsageReconciler struct {
+	client.Client
+	Cache       *schdcache.Cache
```

  #8686

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-20T10:33:44Z

- **pkg/controller/tas/non_tas_usage_controller.go:53** by [@gabesaba](https://github.com/gabesaba) — 2026-01-20T10:33:43Z
```diff
@@ -0,0 +1,121 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	schdcache "sigs.k8s.io/kueue/pkg/cache/scheduler"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+func newNonTasUsageReconciler(client client.Client, cache *schdcache.Cache, qcache *qcache.Manager) *NonTasUsageReconciler {
+	return &NonTasUsageReconciler{
+		Client: client,
+		Cache:  cache,
+		QCache: qcache,
+	}
+}
+
+// NonTasUsageReconciler monitors pods to update
+// the TAS cache with non-TAS usage.
+type NonTasUsageReconciler struct {
+	client.Client
+	Cache       *schdcache.Cache
+	QCache      *qcache.Manager
```

  #8686

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-20T10:33:46Z

- **pkg/cache/scheduler/tas_cache_test.go:6017** by [@gabesaba](https://github.com/gabesaba) — 2026-01-20T10:33:46Z
```diff
@@ -5980,6 +6013,9 @@ func TestFindTopologyAssignments(t *testing.T) {
 				TopologyName: "default",
 				NodeLabels:   tc.nodeLabels,
 			}
+			for _, pod := range tc.pods {
+				tasCache.Update(pod, logr.FromContextOrDiscard(ctx))
```

  #8686

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-20T10:34:09Z

- **pkg/cache/scheduler/tas_cache.go:123** by [@gabesaba](https://github.com/gabesaba) — 2026-01-20T10:34:09Z
```diff
@@ -108,3 +117,13 @@ func (t *tasCache) DeleteTopology(name kueue.TopologyReference) {
 		}
 	}
 }
+
+// Update may add a pod to the cache, or
+// delete a terminated pod.
+func (t *tasCache) Update(pod corev1.Pod, log logr.Logger) {
```

  I'm not worried about performance of a shallow-copy in practice. In principle, I agree with you. Changed in #8686

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-09T08:41:43Z

- **test/integration/singlecluster/tas/tas_test.go:295** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-09T08:41:43Z
```diff
@@ -212,6 +214,219 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 
+	ginkgo.When("non-TAS pod exists", func() {
+		var (
+			nodes        []corev1.Node
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueue.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			nodes = []corev1.Node{
+				*testingnode.MakeNode("node1").
+					Label("node-group", "tas").
+					Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+					Label(utiltesting.DefaultRackTopologyLevel, "r1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("3"),
+					}).
+					Ready().
+					Obj(),
+			}
+			util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+			topology = utiltestingapi.MakeDefaultTwoLevelTopology("default")
+			util.MustCreate(ctx, k8sClient, topology)
+
+			tasFlavor = utiltestingapi.MakeResourceFlavor("tas-flavor").
+				NodeLabel("node-group", "tas").
+				TopologyName("default").Obj()
+			util.MustCreate(ctx, k8sClient, tasFlavor)
+
+			clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*utiltestingapi.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "999").Obj()).
+				Obj()
+			util.MustCreate(ctx, k8sClient, clusterQueue)
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			util.MustCreate(ctx, k8sClient, localQueue)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+			for _, node := range nodes {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+			}
+		})
+
+		ginkgo.It("non-TAS pod terminates, releasing capacity", func() {
+			var wl *kueue.Workload
+			var nonTasPod *corev1.Pod
+
+			ginkgo.By("create a non-TAS pod which consumes the node's capacity", func() {
+				nonTasPod = testingpod.MakePod("pod", ns.Name).
+					Request(corev1.ResourceCPU, "1").
+					NodeName("node1").
+					TerminationGracePeriod(0).
+					Obj()
+				util.MustCreate(ctx, k8sClient, nonTasPod)
+			})
+
+			ginkgo.By("create a workload which requires the node's capacity", func() {
+				wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+					Queue("local-queue").
+					Request(corev1.ResourceCPU, "1").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+
+				ginkgo.By("verify the workload is not admitted", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(workload.IsAdmitted(wl)).To(gomega.BeFalse())
+					}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
```

  Actually, I don’t quite understand the idea behind ShortConsistentlyTimeout. What should the interval be in this case?
  Currently, I see that the timeout is 10ms, but the interval is 250ms, so it runs only once. In that case, why are we using Consistently?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-09T09:08:12Z

- **test/integration/singlecluster/tas/tas_test.go:295** by [@mimowo](https://github.com/mimowo) — 2026-03-09T09:08:12Z
```diff
@@ -212,6 +214,219 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 
+	ginkgo.When("non-TAS pod exists", func() {
+		var (
+			nodes        []corev1.Node
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueue.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			nodes = []corev1.Node{
+				*testingnode.MakeNode("node1").
+					Label("node-group", "tas").
+					Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+					Label(utiltesting.DefaultRackTopologyLevel, "r1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("3"),
+					}).
+					Ready().
+					Obj(),
+			}
+			util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+			topology = utiltestingapi.MakeDefaultTwoLevelTopology("default")
+			util.MustCreate(ctx, k8sClient, topology)
+
+			tasFlavor = utiltestingapi.MakeResourceFlavor("tas-flavor").
+				NodeLabel("node-group", "tas").
+				TopologyName("default").Obj()
+			util.MustCreate(ctx, k8sClient, tasFlavor)
+
+			clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*utiltestingapi.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "999").Obj()).
+				Obj()
+			util.MustCreate(ctx, k8sClient, clusterQueue)
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			util.MustCreate(ctx, k8sClient, localQueue)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+			for _, node := range nodes {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+			}
+		})
+
+		ginkgo.It("non-TAS pod terminates, releasing capacity", func() {
+			var wl *kueue.Workload
+			var nonTasPod *corev1.Pod
+
+			ginkgo.By("create a non-TAS pod which consumes the node's capacity", func() {
+				nonTasPod = testingpod.MakePod("pod", ns.Name).
+					Request(corev1.ResourceCPU, "1").
+					NodeName("node1").
+					TerminationGracePeriod(0).
+					Obj()
+				util.MustCreate(ctx, k8sClient, nonTasPod)
+			})
+
+			ginkgo.By("create a workload which requires the node's capacity", func() {
+				wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+					Queue("local-queue").
+					Request(corev1.ResourceCPU, "1").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+
+				ginkgo.By("verify the workload is not admitted", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(workload.IsAdmitted(wl)).To(gomega.BeFalse())
+					}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
```

  +1, Let's continue discussion on the PR

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-09T09:25:50Z

- **test/integration/singlecluster/tas/tas_test.go:295** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-09T09:25:50Z
```diff
@@ -212,6 +214,219 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 
+	ginkgo.When("non-TAS pod exists", func() {
+		var (
+			nodes        []corev1.Node
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueue.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			nodes = []corev1.Node{
+				*testingnode.MakeNode("node1").
+					Label("node-group", "tas").
+					Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+					Label(utiltesting.DefaultRackTopologyLevel, "r1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("3"),
+					}).
+					Ready().
+					Obj(),
+			}
+			util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+			topology = utiltestingapi.MakeDefaultTwoLevelTopology("default")
+			util.MustCreate(ctx, k8sClient, topology)
+
+			tasFlavor = utiltestingapi.MakeResourceFlavor("tas-flavor").
+				NodeLabel("node-group", "tas").
+				TopologyName("default").Obj()
+			util.MustCreate(ctx, k8sClient, tasFlavor)
+
+			clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*utiltestingapi.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "999").Obj()).
+				Obj()
+			util.MustCreate(ctx, k8sClient, clusterQueue)
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			util.MustCreate(ctx, k8sClient, localQueue)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+			for _, node := range nodes {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+			}
+		})
+
+		ginkgo.It("non-TAS pod terminates, releasing capacity", func() {
+			var wl *kueue.Workload
+			var nonTasPod *corev1.Pod
+
+			ginkgo.By("create a non-TAS pod which consumes the node's capacity", func() {
+				nonTasPod = testingpod.MakePod("pod", ns.Name).
+					Request(corev1.ResourceCPU, "1").
+					NodeName("node1").
+					TerminationGracePeriod(0).
+					Obj()
+				util.MustCreate(ctx, k8sClient, nonTasPod)
+			})
+
+			ginkgo.By("create a workload which requires the node's capacity", func() {
+				wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+					Queue("local-queue").
+					Request(corev1.ResourceCPU, "1").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+
+				ginkgo.By("verify the workload is not admitted", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(workload.IsAdmitted(wl)).To(gomega.BeFalse())
+					}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
```

  Ref https://github.com/kubernetes-sigs/kueue/pull/9749.
