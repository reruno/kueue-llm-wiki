# PR #849: Enable preemption and borrowing based on flavor

**Summary**: Enable preemption and borrowing based on flavor

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/849

**Last updated**: 2023-10-09T08:42:39Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@KunWuLuan](https://github.com/KunWuLuan)
- **Branch**: `feature/pbnf` → `main`
- **Created**: 2023-06-13T10:01:29Z
- **Updated**: 2023-10-09T08:42:39Z
- **Merged**: 2023-10-09T08:42:37Z
- **Closed**: 2023-10-09T08:42:37Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@trasc](https://github.com/trasc), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 26   **+1529 / -172**

## Description

/kind feature

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #582 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Options to allow workloads to borrow quota or preempt other workloads before trying the next flavor in the list
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-06-13T10:01:39Z

Hi @KunWuLuan. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-06-13T10:01:39Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 50475bd7a620c09a963763aba482bd428393aead |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6521150b822cc90008391b19 |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-13T11:56:30Z

/ok-to-test

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-13T11:57:07Z

/assign @trasc for a first pass

### Comment by [@trasc](https://github.com/trasc) — 2023-06-13T12:43:30Z

/assign

### Comment by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-06-28T12:11:37Z

/test all

### Comment by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-07-04T08:01:26Z

/retest

### Comment by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-07-04T12:14:07Z

/retest

### Comment by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-07-10T03:47:20Z

/retest

### Comment by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-07-10T03:52:09Z

@alculquicondor 🙇 Spent a lot of time fixing tests that couldn't pass. If you have time, please take a look, thanks!

### Comment by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-07-11T02:19:13Z

> Is the new functionality tested?

Not yet, I will add some tests this week

### Comment by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-07-17T11:38:14Z

@trasc Hi, I have add some tests for this new func, if you have time, please take a look. Thanks very much!

### Comment by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-07-18T13:18:40Z

> The tests are fine, but I don't think that the case in which the preemption cannot be done is covered.

OK, will work on adding test to cover this case

### Comment by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-07-20T01:38:53Z

> The tests are fine, but I don't think that the case in which the preemption cannot be done is covered.

@trasc Hi, I have add a unit test in scheduler's tests for this case. In this test, if we don't start where preempting failed, workload `default/preemptor` will always try to preempt in the first flavor. So if the workload was admitted and current workload is not been preempted, we can say the scheduler's behavior is not wrong

### Comment by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-08-07T11:53:05Z

@trasc Hi, if you have time, please take a look at this pr again, thanks. :D

### Comment by [@trasc](https://github.com/trasc) — 2023-08-08T09:19:43Z

> @trasc Hi, if you have time, please take a look at this pr again, thanks. :D

@KunWuLuan can you mark the resolved points accordingly and maybe retitle the PR if it is no longer WIP?

### Comment by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-08-08T11:36:00Z

> > @trasc Hi, if you have time, please take a look at this pr again, thanks. :D
> 
> @KunWuLuan can you mark the resolved points accordingly and maybe retitle the PR if it is no longer WIP?

@trasc  Done

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-08T16:59:21Z

I've been meaning to get back to this but I haven't had the chance.

Could you also update the related KEP to match the implementation so I can more easily get a high level idea of how you are trying to achieve this feature?

Also FYI on #994, which might greatly affect this

### Comment by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-08-11T09:03:40Z

> I've been meaning to get back to this but I haven't had the chance.
> 
> Could you also update the related KEP to match the implementation so I can more easily get a high level idea of how you are trying to achieve this feature?
> 
> Also FYI on #994, which might greatly affect this

@alculquicondor Done. I think this pr is nothing to do with https://github.com/kubernetes-sigs/kueue/pull/994, cause we will only decide whether a flavor should be considered and will not influence the preemption and admission any more.

### Comment by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-08-22T03:47:18Z

> I'm overall happy with the approach.
> 
> Still, it might be worth adding a feature gate to disable the feature in case we find problems. Could you do that?

Have added a feature gate

### Comment by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-08-23T01:17:39Z

@alculquicondor @trasc Hi, I think this pr is ready for review again. Thanks very much if u have time to take a look.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-01T19:42:38Z

/release-note-edit
```release-note
Options to allow workloads to borrow quota or preempt other workloads before trying the next flavor in the list
```

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-13T17:50:14Z

The integration test is failing

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-21T16:44:59Z

To clarify, I'm leaving the LGTM to @tenzen-y.

@KunWuLuan thanks a lot for your great effort! This was high quality work, which is even more impressive as this is your first contribution to the project. Kudos!

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-21T20:14:45Z

> To clarify, I'm leaving the LGTM to @tenzen-y.
> 
> @KunWuLuan thanks a lot for your great effort! This was high quality work, which is even more impressive as this is your first contribution to the project. Kudos!

Sure. Let me check this PR.

### Comment by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-22T03:27:07Z

> To clarify, I'm leaving the LGTM to @tenzen-y.
> 
> @KunWuLuan thanks a lot for your great effort! This was high quality work, which is even more impressive as this is your first contribution to the project. Kudos!

😆 It's really great to contribute to the Kueue community. thanks

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-29T08:04:11Z

I'm still waiting for some comments.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-03T14:54:01Z

hi @KunWuLuan we are approaching the time for the next release. Can you follow up on the last suggestions?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-03T15:04:10Z

> hi @KunWuLuan we are approaching the time for the next release. Can you follow up on the last suggestions?

I'm still waiting for the following:

- https://github.com/kubernetes-sigs/kueue/pull/849#discussion_r1333997290
- https://github.com/kubernetes-sigs/kueue/pull/849#discussion_r1334055881
- https://github.com/kubernetes-sigs/kueue/pull/849#discussion_r1334145099
- https://github.com/kubernetes-sigs/kueue/pull/849#discussion_r1341047332

### Comment by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-10-07T05:54:54Z

> > hi @KunWuLuan we are approaching the time for the next release. Can you follow up on the last suggestions?
> 
> I'm still waiting for the following:
> 
> * [Enable preemption and borrowing based on flavor #849 (comment)](https://github.com/kubernetes-sigs/kueue/pull/849#discussion_r1333997290)
> * [Enable preemption and borrowing based on flavor #849 (comment)](https://github.com/kubernetes-sigs/kueue/pull/849#discussion_r1334055881)
> * [Enable preemption and borrowing based on flavor #849 (comment)](https://github.com/kubernetes-sigs/kueue/pull/849#discussion_r1334145099)
> * [Enable preemption and borrowing based on flavor #849 (comment)](https://github.com/kubernetes-sigs/kueue/pull/849#discussion_r1341047332)

Sure, for replying late. I am on vacation this week. I will work on these comments this weekend.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-07T05:56:29Z

> > > hi @KunWuLuan we are approaching the time for the next release. Can you follow up on the last suggestions?
> > 
> > 
> > I'm still waiting for the following:
> > 
> > * [Enable preemption and borrowing based on flavor #849 (comment)](https://github.com/kubernetes-sigs/kueue/pull/849#discussion_r1333997290)
> > * [Enable preemption and borrowing based on flavor #849 (comment)](https://github.com/kubernetes-sigs/kueue/pull/849#discussion_r1334055881)
> > * [Enable preemption and borrowing based on flavor #849 (comment)](https://github.com/kubernetes-sigs/kueue/pull/849#discussion_r1334145099)
> > * [Enable preemption and borrowing based on flavor #849 (comment)](https://github.com/kubernetes-sigs/kueue/pull/849#discussion_r1341047332)
> 
> Sure, for replying late. I am on vacation this week. I will work on these comments this weekend.

Thank you!

### Comment by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-10-07T09:00:05Z

> > > > hi @KunWuLuan we are approaching the time for the next release. Can you follow up on the last suggestions?
> > > 
> > > 
> > > I'm still waiting for the following:
> > > 
> > > * [Enable preemption and borrowing based on flavor #849 (comment)](https://github.com/kubernetes-sigs/kueue/pull/849#discussion_r1333997290)
> > > * [Enable preemption and borrowing based on flavor #849 (comment)](https://github.com/kubernetes-sigs/kueue/pull/849#discussion_r1334055881)
> > > * [Enable preemption and borrowing based on flavor #849 (comment)](https://github.com/kubernetes-sigs/kueue/pull/849#discussion_r1334145099)
> > > * [Enable preemption and borrowing based on flavor #849 (comment)](https://github.com/kubernetes-sigs/kueue/pull/849#discussion_r1341047332)
> > 
> > 
> > Sure, for replying late. I am on vacation this week. I will work on these comments this weekend.
> 
> Thank you!

Hi, I have addressed these comments. Please take a look if u have time. Thanks very much.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-09T08:37:17Z

LGTM label has been added.  <details>Git tree hash: 89adafb5589fa6c9b5453691effe6d04c4a343a0</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-09T08:37:20Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/849#pullrequestreview-1638301278" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/849#" title="Author self-approved">KunWuLuan</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/849#pullrequestreview-1663963086" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-09T08:37:36Z

/hold cancel

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-06-13T13:06:29Z

Please try to fix the tests.

- **apis/kueue/v1beta1/clusterqueue_types.go:314** by [@trasc](https://github.com/trasc) — 2023-06-13T13:00:32Z
```diff
@@ -243,6 +243,21 @@ const (
 	PreemptionPolicyLowerOrNewerEqualPriority PreemptionPolicy = "LowerOrNewerEqualPriority"
 )
 
+type FlavorFungibilityPolicy string
+
+const (
+	Borrow        FlavorFungibilityPolicy = "Borrow"
+	Preempt       FlavorFungibilityPolicy = "Preempt"
+	TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
+)
+
+type FlavorFungibility struct {
+	// +kubebuilder:validation:Enum="Borrow,TryNextFlavor"
+	WhenCanBorrow FlavorFungibilityPolicy `json:"whenCanBorrow"`
+	// +kubebuilder:validation:Enum="Preempt,TryNextFlavor"
+	WhenCanPreempt FlavorFungibilityPolicy `json:"whenCanPreempt"`
+}
+
```

  Run 
  ```
  make manifests generate update-helm-crd
  ```
  It will also resolve some liter issues.

- **pkg/cache/cache.go:144** by [@trasc](https://github.com/trasc) — 2023-06-13T13:02:29Z
```diff
@@ -140,6 +141,7 @@ type ClusterQueue struct {
 	NamespaceSelector labels.Selector
 	Preemption        kueue.ClusterQueuePreemption
 	Status            metrics.ClusterQueueStatus
+	FlavorFungibility v1beta1.FlavorFungibility
```

  Where is this populated?

- **pkg/scheduler/flavorassigner/flavorassigner.go:395** by [@trasc](https://github.com/trasc) — 2023-06-13T13:03:43Z
```diff
@@ -389,18 +392,39 @@ func (a *Assignment) findFlavorForResourceGroup(
 			}
 		}
 
+		if shouldTryNextFlavor(representativeMode, cq.FlavorFungibility, whetherNeedBorrowing) {
```

  should this be  `!shouldTryNextFlavor(...`

- **pkg/scheduler/flavorassigner/flavorassigner.go:404** by [@trasc](https://github.com/trasc) — 2023-06-13T13:05:45Z
```diff
@@ -389,18 +392,39 @@ func (a *Assignment) findFlavorForResourceGroup(
 			}
 		}
 
+		if shouldTryNextFlavor(representativeMode, cq.FlavorFungibility, whetherNeedBorrowing) {
+			return assignments, nil
+		}
 		if representativeMode > bestAssignmentMode {
 			bestAssignment = assignments
 			bestAssignmentMode = representativeMode
-			if bestAssignmentMode == Fit {
-				// All the resources fit in the cohort, no need to check more flavors.
-				return bestAssignment, nil
-			}
+			// if bestAssignmentMode == Fit {
+			// 	// All the resources fit in the cohort, no need to check more flavors.
+			// 	return bestAssignment, nil
+			// }
```

  ```suggestion
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-06-15T19:58:00Z

- **apis/kueue/v1beta1/clusterqueue_types.go:255** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-15T19:49:53Z
```diff
@@ -243,6 +243,21 @@ const (
 	PreemptionPolicyLowerOrNewerEqualPriority PreemptionPolicy = "LowerOrNewerEqualPriority"
 )
 
+type FlavorFungibilityPolicy string
+
+const (
+	Borrow        FlavorFungibilityPolicy = "Borrow"
+	Preempt       FlavorFungibilityPolicy = "Preempt"
+	TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
+)
+
+type FlavorFungibility struct {
+	// +kubebuilder:validation:Enum="Borrow,TryNextFlavor"
```

  set defaults

- **apis/kueue/v1beta1/clusterqueue_types.go:306** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-15T19:50:06Z
```diff
@@ -276,6 +291,8 @@ type ClusterQueuePreemption struct {
 	// +kubebuilder:default=Never
 	// +kubebuilder:validation:Enum=Never;LowerPriority;LowerOrNewerEqualPriority
 	WithinClusterQueue PreemptionPolicy `json:"withinClusterQueue,omitempty"`
+
+	FlavorFungibility FlavorFungibility `json:"flavorFungibility"`
```

  beyond prototype: needs an explanation comment.

- **pkg/scheduler/flavorassigner/flavorassigner.go:395** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-15T19:54:29Z
```diff
@@ -389,18 +392,39 @@ func (a *Assignment) findFlavorForResourceGroup(
 			}
 		}
 
+		if shouldTryNextFlavor(representativeMode, cq.FlavorFungibility, whetherNeedBorrowing) {
+			return assignments, nil
+		}
 		if representativeMode > bestAssignmentMode {
 			bestAssignment = assignments
 			bestAssignmentMode = representativeMode
-			if bestAssignmentMode == Fit {
```

  why is it ok to remove this?

- **pkg/scheduler/flavorassigner/flavorassigner.go:414** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-15T19:55:10Z
```diff
@@ -389,18 +392,39 @@ func (a *Assignment) findFlavorForResourceGroup(
 			}
 		}
 
+		if shouldTryNextFlavor(representativeMode, cq.FlavorFungibility, whetherNeedBorrowing) {
+			return assignments, nil
+		}
 		if representativeMode > bestAssignmentMode {
 			bestAssignment = assignments
 			bestAssignmentMode = representativeMode
-			if bestAssignmentMode == Fit {
-				// All the resources fit in the cohort, no need to check more flavors.
-				return bestAssignment, nil
-			}
+			// if bestAssignmentMode == Fit {
+			// 	// All the resources fit in the cohort, no need to check more flavors.
+			// 	return bestAssignment, nil
+			// }
 		}
 	}
 	return bestAssignment, status
 }
 
+func shouldTryNextFlavor(representativeMode FlavorAssignmentMode, flavorFungibility v1beta1.FlavorFungibility, whetherNeedBorrowing bool) bool {
+	policyPreempt := flavorFungibility.WhenCanPreempt
+	policyBorrow := flavorFungibility.WhenCanBorrow
+	if representativeMode == Preempt && policyPreempt == v1beta1.Preempt {
+		return true
```

  false?

- **pkg/scheduler/flavorassigner/flavorassigner.go:475** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-15T19:57:44Z
```diff
@@ -389,18 +392,39 @@ func (a *Assignment) findFlavorForResourceGroup(
 			}
 		}
 
+		if shouldTryNextFlavor(representativeMode, cq.FlavorFungibility, whetherNeedBorrowing) {
+			return assignments, nil
+		}
 		if representativeMode > bestAssignmentMode {
 			bestAssignment = assignments
 			bestAssignmentMode = representativeMode
-			if bestAssignmentMode == Fit {
-				// All the resources fit in the cohort, no need to check more flavors.
-				return bestAssignment, nil
-			}
+			// if bestAssignmentMode == Fit {
+			// 	// All the resources fit in the cohort, no need to check more flavors.
+			// 	return bestAssignment, nil
+			// }
 		}
 	}
 	return bestAssignment, status
 }
 
+func shouldTryNextFlavor(representativeMode FlavorAssignmentMode, flavorFungibility v1beta1.FlavorFungibility, whetherNeedBorrowing bool) bool {
+	policyPreempt := flavorFungibility.WhenCanPreempt
+	policyBorrow := flavorFungibility.WhenCanBorrow
+	if representativeMode == Preempt && policyPreempt == v1beta1.Preempt {
```

  at this point, we don't know if preemption would be possible (there might not be enough candidates for preemption)
  
  So how would we "try the next flavor" if preemption doesn't help.
  
  Another thought that comes to mind is whether we should prioritize partial admission over "trying the next flavor".
  
  I think we need more thinking about policies overall.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-06-16T14:51:17Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:475** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-16T14:51:16Z
```diff
@@ -389,18 +392,39 @@ func (a *Assignment) findFlavorForResourceGroup(
 			}
 		}
 
+		if shouldTryNextFlavor(representativeMode, cq.FlavorFungibility, whetherNeedBorrowing) {
+			return assignments, nil
+		}
 		if representativeMode > bestAssignmentMode {
 			bestAssignment = assignments
 			bestAssignmentMode = representativeMode
-			if bestAssignmentMode == Fit {
-				// All the resources fit in the cohort, no need to check more flavors.
-				return bestAssignment, nil
-			}
+			// if bestAssignmentMode == Fit {
+			// 	// All the resources fit in the cohort, no need to check more flavors.
+			// 	return bestAssignment, nil
+			// }
 		}
 	}
 	return bestAssignment, status
 }
 
+func shouldTryNextFlavor(representativeMode FlavorAssignmentMode, flavorFungibility v1beta1.FlavorFungibility, whetherNeedBorrowing bool) bool {
+	policyPreempt := flavorFungibility.WhenCanPreempt
+	policyBorrow := flavorFungibility.WhenCanBorrow
+	if representativeMode == Preempt && policyPreempt == v1beta1.Preempt {
```

  Another possible implementation path is that we somehow keep some information in memory about the last flavors and modes we used. This way, in a future scheduling attempt, we can try the next flavor if preemption didn't work.

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-06-20T11:54:42Z

- **pkg/cache/cache.go:144** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-06-20T11:54:41Z
```diff
@@ -140,6 +141,7 @@ type ClusterQueue struct {
 	NamespaceSelector labels.Selector
 	Preemption        kueue.ClusterQueuePreemption
 	Status            metrics.ClusterQueueStatus
+	FlavorFungibility v1beta1.FlavorFungibility
```

  I removed this field, we can get this from cq.Preemption

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-06-20T11:54:59Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:395** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-06-20T11:54:59Z
```diff
@@ -389,18 +392,39 @@ func (a *Assignment) findFlavorForResourceGroup(
 			}
 		}
 
+		if shouldTryNextFlavor(representativeMode, cq.FlavorFungibility, whetherNeedBorrowing) {
```

  yes, thanks

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-06-20T11:56:44Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:404** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-06-20T11:56:44Z
```diff
@@ -389,18 +392,39 @@ func (a *Assignment) findFlavorForResourceGroup(
 			}
 		}
 
+		if shouldTryNextFlavor(representativeMode, cq.FlavorFungibility, whetherNeedBorrowing) {
+			return assignments, nil
+		}
 		if representativeMode > bestAssignmentMode {
 			bestAssignment = assignments
 			bestAssignmentMode = representativeMode
-			if bestAssignmentMode == Fit {
-				// All the resources fit in the cohort, no need to check more flavors.
-				return bestAssignment, nil
-			}
+			// if bestAssignmentMode == Fit {
+			// 	// All the resources fit in the cohort, no need to check more flavors.
+			// 	return bestAssignment, nil
+			// }
```

  Done, thanks

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-06-20T11:59:39Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:395** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-06-20T11:59:38Z
```diff
@@ -389,18 +392,39 @@ func (a *Assignment) findFlavorForResourceGroup(
 			}
 		}
 
+		if shouldTryNextFlavor(representativeMode, cq.FlavorFungibility, whetherNeedBorrowing) {
+			return assignments, nil
+		}
 		if representativeMode > bestAssignmentMode {
 			bestAssignment = assignments
 			bestAssignmentMode = representativeMode
-			if bestAssignmentMode == Fit {
```

  we will handle this case in shouldTryNextFlavor like:
  ```
  	if representativeMode == Fit && whetherNeedBorrowing && policyBorrow == v1beta1.Borrow {
  		return false
  	}
  
  	if representativeMode == Fit && !whetherNeedBorrowing {
  		return false
  	}
  ```

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-06-20T12:18:19Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:475** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-06-20T12:18:18Z
```diff
@@ -389,18 +392,39 @@ func (a *Assignment) findFlavorForResourceGroup(
 			}
 		}
 
+		if shouldTryNextFlavor(representativeMode, cq.FlavorFungibility, whetherNeedBorrowing) {
+			return assignments, nil
+		}
 		if representativeMode > bestAssignmentMode {
 			bestAssignment = assignments
 			bestAssignmentMode = representativeMode
-			if bestAssignmentMode == Fit {
-				// All the resources fit in the cohort, no need to check more flavors.
-				return bestAssignment, nil
-			}
+			// if bestAssignmentMode == Fit {
+			// 	// All the resources fit in the cohort, no need to check more flavors.
+			// 	return bestAssignment, nil
+			// }
 		}
 	}
 	return bestAssignment, status
 }
 
+func shouldTryNextFlavor(representativeMode FlavorAssignmentMode, flavorFungibility v1beta1.FlavorFungibility, whetherNeedBorrowing bool) bool {
+	policyPreempt := flavorFungibility.WhenCanPreempt
+	policyBorrow := flavorFungibility.WhenCanBorrow
+	if representativeMode == Preempt && policyPreempt == v1beta1.Preempt {
```

  Yes, it is better to try next flavor the next time workload been scheduled. I will try to give some implementation for this.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-10T12:02:18Z

Is the new functionality tested?

- **pkg/scheduler/scheduler_test.go:199** by [@trasc](https://github.com/trasc) — 2023-07-10T10:29:53Z
```diff
@@ -197,97 +196,97 @@ func TestSchedule(t *testing.T) {
 		// enable partial admission
 		enablePartialAdmission bool
 	}{
-		"workload fits in single clusterQueue": {
-			workloads: []kueue.Workload{
-				*utiltesting.MakeWorkload("foo", "sales").
-					Queue("main").
-					PodSets(*utiltesting.MakePodSet("one", 10).
-						Request(corev1.ResourceCPU, "1").
-						Obj()).
-					Obj(),
-			},
-			wantAssignments: map[string]kueue.Admission{
-				"sales/foo": {
-					ClusterQueue: "sales",
-					PodSetAssignments: []kueue.PodSetAssignment{
-						{
-							Name: "one",
-							Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
-								corev1.ResourceCPU: "default",
-							},
-							ResourceUsage: corev1.ResourceList{
-								corev1.ResourceCPU: resource.MustParse("10000m"),
-							},
-							Count: pointer.Int32(10),
-						},
-					},
-				},
-			},
-			wantScheduled: []string{"sales/foo"},
-		},
-		"error during admission": {
-			workloads: []kueue.Workload{
-				*utiltesting.MakeWorkload("foo", "sales").
-					Queue("main").
-					PodSets(*utiltesting.MakePodSet("one", 10).
-						Request(corev1.ResourceCPU, "1").
-						Obj()).
-					Obj(),
-			},
-			admissionError: errors.New("admission"),
-			wantLeft: map[string]sets.Set[string]{
-				"sales": sets.New("sales/foo"),
-			},
-		},
-		"single clusterQueue full": {
-			workloads: []kueue.Workload{
-				*utiltesting.MakeWorkload("new", "sales").
-					Queue("main").
-					PodSets(*utiltesting.MakePodSet("one", 11).
-						Request(corev1.ResourceCPU, "1").
-						Obj()).
-					Obj(),
-				*utiltesting.MakeWorkload("assigned", "sales").
-					PodSets(*utiltesting.MakePodSet("one", 40).
-						Request(corev1.ResourceCPU, "1").
-						Obj()).
-					Admit(utiltesting.MakeAdmission("sales", "one").Assignment(corev1.ResourceCPU, "default", "40000m").AssignmentPodCount(40).Obj()).
-					Obj(),
-			},
-			wantAssignments: map[string]kueue.Admission{
-				"sales/assigned": {
-					ClusterQueue: "sales",
-					PodSetAssignments: []kueue.PodSetAssignment{
-						{
-							Name: "one",
-							Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
-								corev1.ResourceCPU: "default",
-							},
-							ResourceUsage: corev1.ResourceList{
-								corev1.ResourceCPU: resource.MustParse("40000m"),
-							},
-							Count: pointer.Int32(40),
-						},
-					},
-				},
-			},
-			wantLeft: map[string]sets.Set[string]{
-				"sales": sets.New("sales/new"),
-			},
-		},
-		"failed to match clusterQueue selector": {
-			workloads: []kueue.Workload{
-				*utiltesting.MakeWorkload("new", "sales").
-					Queue("blocked").
-					PodSets(*utiltesting.MakePodSet("one", 1).
-						Request(corev1.ResourceCPU, "1").
-						Obj()).
-					Obj(),
-			},
-			wantInadmissibleLeft: map[string]sets.Set[string]{
-				"eng-alpha": sets.New("sales/new"),
-			},
-		},
+		// "workload fits in single clusterQueue": {
```

  Why comment these tests?

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-07-11T02:18:08Z

- **pkg/scheduler/scheduler_test.go:199** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-07-11T02:18:08Z
```diff
@@ -197,97 +196,97 @@ func TestSchedule(t *testing.T) {
 		// enable partial admission
 		enablePartialAdmission bool
 	}{
-		"workload fits in single clusterQueue": {
-			workloads: []kueue.Workload{
-				*utiltesting.MakeWorkload("foo", "sales").
-					Queue("main").
-					PodSets(*utiltesting.MakePodSet("one", 10).
-						Request(corev1.ResourceCPU, "1").
-						Obj()).
-					Obj(),
-			},
-			wantAssignments: map[string]kueue.Admission{
-				"sales/foo": {
-					ClusterQueue: "sales",
-					PodSetAssignments: []kueue.PodSetAssignment{
-						{
-							Name: "one",
-							Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
-								corev1.ResourceCPU: "default",
-							},
-							ResourceUsage: corev1.ResourceList{
-								corev1.ResourceCPU: resource.MustParse("10000m"),
-							},
-							Count: pointer.Int32(10),
-						},
-					},
-				},
-			},
-			wantScheduled: []string{"sales/foo"},
-		},
-		"error during admission": {
-			workloads: []kueue.Workload{
-				*utiltesting.MakeWorkload("foo", "sales").
-					Queue("main").
-					PodSets(*utiltesting.MakePodSet("one", 10).
-						Request(corev1.ResourceCPU, "1").
-						Obj()).
-					Obj(),
-			},
-			admissionError: errors.New("admission"),
-			wantLeft: map[string]sets.Set[string]{
-				"sales": sets.New("sales/foo"),
-			},
-		},
-		"single clusterQueue full": {
-			workloads: []kueue.Workload{
-				*utiltesting.MakeWorkload("new", "sales").
-					Queue("main").
-					PodSets(*utiltesting.MakePodSet("one", 11).
-						Request(corev1.ResourceCPU, "1").
-						Obj()).
-					Obj(),
-				*utiltesting.MakeWorkload("assigned", "sales").
-					PodSets(*utiltesting.MakePodSet("one", 40).
-						Request(corev1.ResourceCPU, "1").
-						Obj()).
-					Admit(utiltesting.MakeAdmission("sales", "one").Assignment(corev1.ResourceCPU, "default", "40000m").AssignmentPodCount(40).Obj()).
-					Obj(),
-			},
-			wantAssignments: map[string]kueue.Admission{
-				"sales/assigned": {
-					ClusterQueue: "sales",
-					PodSetAssignments: []kueue.PodSetAssignment{
-						{
-							Name: "one",
-							Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
-								corev1.ResourceCPU: "default",
-							},
-							ResourceUsage: corev1.ResourceList{
-								corev1.ResourceCPU: resource.MustParse("40000m"),
-							},
-							Count: pointer.Int32(40),
-						},
-					},
-				},
-			},
-			wantLeft: map[string]sets.Set[string]{
-				"sales": sets.New("sales/new"),
-			},
-		},
-		"failed to match clusterQueue selector": {
-			workloads: []kueue.Workload{
-				*utiltesting.MakeWorkload("new", "sales").
-					Queue("blocked").
-					PodSets(*utiltesting.MakePodSet("one", 1).
-						Request(corev1.ResourceCPU, "1").
-						Obj()).
-					Obj(),
-			},
-			wantInadmissibleLeft: map[string]sets.Set[string]{
-				"eng-alpha": sets.New("sales/new"),
-			},
-		},
+		// "workload fits in single clusterQueue": {
```

  I forgot to uncomment these, sorry. I have fixed this.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-18T12:47:48Z

The tests are fine, but I  don't think that the case in which the preemption cannot be done is covered.

- **pkg/scheduler/flavorassigner/flavorassigner.go:475** by [@trasc](https://github.com/trasc) — 2023-07-18T12:35:44Z
```diff
@@ -389,18 +392,39 @@ func (a *Assignment) findFlavorForResourceGroup(
 			}
 		}
 
+		if shouldTryNextFlavor(representativeMode, cq.FlavorFungibility, whetherNeedBorrowing) {
+			return assignments, nil
+		}
 		if representativeMode > bestAssignmentMode {
 			bestAssignment = assignments
 			bestAssignmentMode = representativeMode
-			if bestAssignmentMode == Fit {
-				// All the resources fit in the cohort, no need to check more flavors.
-				return bestAssignment, nil
-			}
+			// if bestAssignmentMode == Fit {
+			// 	// All the resources fit in the cohort, no need to check more flavors.
+			// 	return bestAssignment, nil
+			// }
 		}
 	}
 	return bestAssignment, status
 }
 
+func shouldTryNextFlavor(representativeMode FlavorAssignmentMode, flavorFungibility v1beta1.FlavorFungibility, whetherNeedBorrowing bool) bool {
+	policyPreempt := flavorFungibility.WhenCanPreempt
+	policyBorrow := flavorFungibility.WhenCanBorrow
+	if representativeMode == Preempt && policyPreempt == v1beta1.Preempt {
```

  Has this changed?

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-07-18T13:19:55Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:475** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-07-18T13:19:54Z
```diff
@@ -389,18 +392,39 @@ func (a *Assignment) findFlavorForResourceGroup(
 			}
 		}
 
+		if shouldTryNextFlavor(representativeMode, cq.FlavorFungibility, whetherNeedBorrowing) {
+			return assignments, nil
+		}
 		if representativeMode > bestAssignmentMode {
 			bestAssignment = assignments
 			bestAssignmentMode = representativeMode
-			if bestAssignmentMode == Fit {
-				// All the resources fit in the cohort, no need to check more flavors.
-				return bestAssignment, nil
-			}
+			// if bestAssignmentMode == Fit {
+			// 	// All the resources fit in the cohort, no need to check more flavors.
+			// 	return bestAssignment, nil
+			// }
 		}
 	}
 	return bestAssignment, status
 }
 
+func shouldTryNextFlavor(representativeMode FlavorAssignmentMode, flavorFungibility v1beta1.FlavorFungibility, whetherNeedBorrowing bool) bool {
+	policyPreempt := flavorFungibility.WhenCanPreempt
+	policyBorrow := flavorFungibility.WhenCanBorrow
+	if representativeMode == Preempt && policyPreempt == v1beta1.Preempt {
```

  Has changed to false

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-20T08:41:07Z

- **pkg/scheduler/scheduler_test.go:1266** by [@trasc](https://github.com/trasc) — 2023-07-20T08:39:58Z
```diff
@@ -1070,6 +1070,177 @@ func TestEntryOrdering(t *testing.T) {
 	}
 }
 
+func TestLastSchedulingContext(t *testing.T) {
```

  Can you add more test scenarios?
  
  With more than  two flavors.
  When the admission is not possible (get to the end of the flavor list) 
  What happens when the queue changes configuration (adds / removes flavors) between admission attempts.

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-07-26T02:23:03Z

- **pkg/scheduler/scheduler_test.go:1266** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-07-26T02:23:03Z
```diff
@@ -1070,6 +1070,177 @@ func TestEntryOrdering(t *testing.T) {
 	}
 }
 
+func TestLastSchedulingContext(t *testing.T) {
```

  @trasc Hi, I have add some more test cases, including some test cases for scheduling context changed and for borrow before next flavor and borrow after all flavors. Please take a look, thank you!

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-08-08T11:26:53Z

- **apis/kueue/v1beta1/clusterqueue_types.go:314** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-08-08T11:26:53Z
```diff
@@ -243,6 +243,21 @@ const (
 	PreemptionPolicyLowerOrNewerEqualPriority PreemptionPolicy = "LowerOrNewerEqualPriority"
 )
 
+type FlavorFungibilityPolicy string
+
+const (
+	Borrow        FlavorFungibilityPolicy = "Borrow"
+	Preempt       FlavorFungibilityPolicy = "Preempt"
+	TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
+)
+
+type FlavorFungibility struct {
+	// +kubebuilder:validation:Enum="Borrow,TryNextFlavor"
+	WhenCanBorrow FlavorFungibilityPolicy `json:"whenCanBorrow"`
+	// +kubebuilder:validation:Enum="Preempt,TryNextFlavor"
+	WhenCanPreempt FlavorFungibilityPolicy `json:"whenCanPreempt"`
+}
+
```

  done

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-08-08T11:27:01Z

- **apis/kueue/v1beta1/clusterqueue_types.go:255** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-08-08T11:27:01Z
```diff
@@ -243,6 +243,21 @@ const (
 	PreemptionPolicyLowerOrNewerEqualPriority PreemptionPolicy = "LowerOrNewerEqualPriority"
 )
 
+type FlavorFungibilityPolicy string
+
+const (
+	Borrow        FlavorFungibilityPolicy = "Borrow"
+	Preempt       FlavorFungibilityPolicy = "Preempt"
+	TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
+)
+
+type FlavorFungibility struct {
+	// +kubebuilder:validation:Enum="Borrow,TryNextFlavor"
```

  done

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-08-08T11:27:29Z

- **apis/kueue/v1beta1/clusterqueue_types.go:306** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-08-08T11:27:28Z
```diff
@@ -276,6 +291,8 @@ type ClusterQueuePreemption struct {
 	// +kubebuilder:default=Never
 	// +kubebuilder:validation:Enum=Never;LowerPriority;LowerOrNewerEqualPriority
 	WithinClusterQueue PreemptionPolicy `json:"withinClusterQueue,omitempty"`
+
+	FlavorFungibility FlavorFungibility `json:"flavorFungibility"`
```

  done

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-08-08T11:27:53Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:414** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-08-08T11:27:53Z
```diff
@@ -389,18 +392,39 @@ func (a *Assignment) findFlavorForResourceGroup(
 			}
 		}
 
+		if shouldTryNextFlavor(representativeMode, cq.FlavorFungibility, whetherNeedBorrowing) {
+			return assignments, nil
+		}
 		if representativeMode > bestAssignmentMode {
 			bestAssignment = assignments
 			bestAssignmentMode = representativeMode
-			if bestAssignmentMode == Fit {
-				// All the resources fit in the cohort, no need to check more flavors.
-				return bestAssignment, nil
-			}
+			// if bestAssignmentMode == Fit {
+			// 	// All the resources fit in the cohort, no need to check more flavors.
+			// 	return bestAssignment, nil
+			// }
 		}
 	}
 	return bestAssignment, status
 }
 
+func shouldTryNextFlavor(representativeMode FlavorAssignmentMode, flavorFungibility v1beta1.FlavorFungibility, whetherNeedBorrowing bool) bool {
+	policyPreempt := flavorFungibility.WhenCanPreempt
+	policyBorrow := flavorFungibility.WhenCanBorrow
+	if representativeMode == Preempt && policyPreempt == v1beta1.Preempt {
+		return true
```

  fixed

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-08-08T11:32:52Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:395** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-08-08T11:32:51Z
```diff
@@ -389,18 +392,39 @@ func (a *Assignment) findFlavorForResourceGroup(
 			}
 		}
 
+		if shouldTryNextFlavor(representativeMode, cq.FlavorFungibility, whetherNeedBorrowing) {
+			return assignments, nil
+		}
 		if representativeMode > bestAssignmentMode {
 			bestAssignment = assignments
 			bestAssignmentMode = representativeMode
-			if bestAssignmentMode == Fit {
```

  When representativeMode == Fit and whetherNeedBorrowing == true and policyBorrow == v1beta1.Borrow, we will visit next flavor so bestAssignmentMode == Fit should not be break the loop

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-15T15:51:52Z

I'm overall happy with the approach.

Still, it might be worth adding a feature gate to disable the feature in case we find problems. Could you do that?

- **apis/kueue/v1beta1/clusterqueue_types.go:299** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T17:08:46Z
```diff
@@ -276,6 +295,17 @@ type ClusterQueuePreemption struct {
 	// +kubebuilder:default=Never
 	// +kubebuilder:validation:Enum=Never;LowerPriority;LowerOrNewerEqualPriority
 	WithinClusterQueue PreemptionPolicy `json:"withinClusterQueue,omitempty"`
+
+	// flagorFungibility determines whether a workload should try the next flavor
```

  ```suggestion
  	// flavorFungibility determines whether a workload should try the next flavor
  ```

- **apis/kueue/v1beta1/clusterqueue_types.go:302** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T17:09:40Z
```diff
@@ -276,6 +295,17 @@ type ClusterQueuePreemption struct {
 	// +kubebuilder:default=Never
 	// +kubebuilder:validation:Enum=Never;LowerPriority;LowerOrNewerEqualPriority
 	WithinClusterQueue PreemptionPolicy `json:"withinClusterQueue,omitempty"`
+
+	// flagorFungibility determines whether a workload should try the next flavor
+	// before borrowing or preempting in current flavor. The possible values are:
+	//
+	// - `Borrow` (default for WhenCanBorrow): allocate in current flavor if borrowing
```

  The possible values should be listed per field, not for the struct.

- **apis/kueue/v1beta1/clusterqueue_types.go:308** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T17:11:32Z
```diff
@@ -276,6 +295,17 @@ type ClusterQueuePreemption struct {
 	// +kubebuilder:default=Never
 	// +kubebuilder:validation:Enum=Never;LowerPriority;LowerOrNewerEqualPriority
 	WithinClusterQueue PreemptionPolicy `json:"withinClusterQueue,omitempty"`
+
+	// flagorFungibility determines whether a workload should try the next flavor
+	// before borrowing or preempting in current flavor. The possible values are:
+	//
+	// - `Borrow` (default for WhenCanBorrow): allocate in current flavor if borrowing
+	//   can help. only effective for WhenCanBorrow
+	// - `Preempt`: allocate in current flavor if preempt might help. only effective
+	//   for WhenCanPreempt
+	// - `TryNextFlavor` (default for WhenCanPreempt): try next flavor if the current
+	//   flavor has no enough resource. effective for WhenCanBorrow and WhenCanPreempt.
+	FlavorFungibility FlavorFungibility `json:"flavorFungibility,omitempty"`
```

  I'm not sure if this should live inside the preemption struct, but we can defer this decision for now.

- **pkg/workload/workload.go:44** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T14:43:24Z
```diff
@@ -39,6 +40,40 @@ var (
 	admissionManagedConditions = []string{kueue.WorkloadAdmitted, kueue.WorkloadEvicted}
 )
 
+type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
+type LastScheduleClusterQueueState struct {
```

  ```suggestion
  type AssigmentClusterQueueState struct {
  ```

- **pkg/workload/workload.go:57** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T14:45:19Z
```diff
@@ -39,6 +40,40 @@ var (
 	admissionManagedConditions = []string{kueue.WorkloadAdmitted, kueue.WorkloadEvicted}
 )
 
+type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
+type LastScheduleClusterQueueState struct {
+	LastScheduledFlavorIdx map[string]map[corev1.ResourceName]int
+	ResourceFlavors        map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor
+	ClusterQueueUsage      FlavorResourceQuantities
+	CohortUsage            FlavorResourceQuantities
+}
+
+func (s *LastScheduleClusterQueueState) Equal(resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, clusterQueueUsage, cohortUsage FlavorResourceQuantities) bool {
+	return reflect.DeepEqual(s.ResourceFlavors, resourceFlavors) && reflect.DeepEqual(s.ClusterQueueUsage, clusterQueueUsage) && reflect.DeepEqual(s.CohortUsage, cohortUsage)
+}
+
+func (s *LastScheduleClusterQueueState) Clone() *LastScheduleClusterQueueState {
+	c := LastScheduleClusterQueueState{
+		LastScheduledFlavorIdx: make(map[string]map[corev1.ResourceName]int),
```

  ```suggestion
  		LastScheduledFlavorIdx: make(map[string]map[corev1.ResourceName]int, len(s.LastScheduledFlavorIdx)),
  ```
  
  Similarly for the rest of the values.

- **pkg/workload/workload.go:43** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T14:46:58Z
```diff
@@ -39,6 +40,40 @@ var (
 	admissionManagedConditions = []string{kueue.WorkloadAdmitted, kueue.WorkloadEvicted}
 )
 
+type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
```

  add a new line after this

- **pkg/workload/workload.go:85** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T14:47:13Z
```diff
@@ -47,6 +82,7 @@ type Info struct {
 	// Populated from the queue during admission or from the admission field if
 	// already admitted.
 	ClusterQueue string
+	LastSchedule *LastScheduleClusterQueueState
```

  ```suggestion
  	LastAssignment *AssigmentClusterQueueState
  ```

- **pkg/workload/workload.go:45** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T14:47:35Z
```diff
@@ -39,6 +40,40 @@ var (
 	admissionManagedConditions = []string{kueue.WorkloadAdmitted, kueue.WorkloadEvicted}
 )
 
+type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
+type LastScheduleClusterQueueState struct {
+	LastScheduledFlavorIdx map[string]map[corev1.ResourceName]int
```

  ```suggestion
  	LastAssignedFlavorIdx map[string]map[corev1.ResourceName]int
  ```

- **pkg/workload/workload.go:45** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T14:48:23Z
```diff
@@ -39,6 +40,40 @@ var (
 	admissionManagedConditions = []string{kueue.WorkloadAdmitted, kueue.WorkloadEvicted}
 )
 
+type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
+type LastScheduleClusterQueueState struct {
+	LastScheduledFlavorIdx map[string]map[corev1.ResourceName]int
```

  Could it be `[]map[corev1.ResourceName]int`? The podsets order shouldn't change.

- **apis/kueue/v1beta1/clusterqueue_types.go:299** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T14:50:21Z
```diff
@@ -276,6 +295,17 @@ type ClusterQueuePreemption struct {
 	// +kubebuilder:default=Never
 	// +kubebuilder:validation:Enum=Never;LowerPriority;LowerOrNewerEqualPriority
 	WithinClusterQueue PreemptionPolicy `json:"withinClusterQueue,omitempty"`
+
+	// flagorFungibility determines whether a workload should try the next flavor
```

  Since this is also about borrowing, it shouldn't be inside ClusterQueuePreemption.

- **pkg/util/testing/wrappers.go:366** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T14:51:28Z
```diff
@@ -318,6 +318,14 @@ func MakeClusterQueue(name string) *ClusterQueueWrapper {
 		Spec: kueue.ClusterQueueSpec{
 			NamespaceSelector: &metav1.LabelSelector{},
 			QueueingStrategy:  kueue.BestEffortFIFO,
+			Preemption: &kueue.ClusterQueuePreemption{
```

  I fear that this might hide scenarios where Preemption and/or Fungibility are nil.
  Only add it in the test cases that need it.

- **pkg/scheduler/flavorassigner/flavorassigner.go:44** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T14:54:29Z
```diff
@@ -32,14 +32,16 @@ import (
 	"k8s.io/component-helpers/scheduling/corev1/nodeaffinity"
 	"k8s.io/utils/pointer"
 
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/cache"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
 type Assignment struct {
-	PodSets     []PodSetAssignment
-	TotalBorrow cache.FlavorResourceQuantities
+	PodSets       []PodSetAssignment
+	TotalBorrow   cache.FlavorResourceQuantities
+	ScheduleState workload.LastScheduleClusterQueueState
```

  ```suggestion
  	LastState workload.AssignmentClusterQueueState
  ```
  ?

- **pkg/scheduler/flavorassigner/flavorassigner.go:225** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T14:56:53Z
```diff
@@ -218,33 +220,67 @@ func (m FlavorAssignmentMode) String() string {
 }
 
 type FlavorAssignment struct {
-	Name   kueue.ResourceFlavorReference
-	Mode   FlavorAssignmentMode
-	borrow int64
+	Name                    kueue.ResourceFlavorReference
+	Mode                    FlavorAssignmentMode
+	LastScheduleFlavorIndex int
```

  ```suggestion
  	LastFlavorIndex int
  ```

- **pkg/scheduler/flavorassigner/flavorassigner.go:235** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T14:59:20Z
```diff
@@ -218,33 +220,67 @@ func (m FlavorAssignmentMode) String() string {
 }
 
 type FlavorAssignment struct {
-	Name   kueue.ResourceFlavorReference
-	Mode   FlavorAssignmentMode
-	borrow int64
+	Name                    kueue.ResourceFlavorReference
+	Mode                    FlavorAssignmentMode
+	LastScheduleFlavorIndex int
+	borrow                  int64
 }
 
 // AssignFlavors assigns flavors for each of the resources requested in each pod set.
 // The result for each pod set is accompanied with reasons why the flavor can't
 // be assigned immediately. Each assigned flavor is accompanied with a
 // FlavorAssignmentMode.
 func AssignFlavors(log logr.Logger, wl *workload.Info, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, counts []int32) Assignment {
+	if wl.LastSchedule != nil {
+		if cq.Cohort == nil && !wl.LastSchedule.Equal(resourceFlavors, workload.FlavorResourceQuantities(cq.Usage), make(workload.FlavorResourceQuantities)) {
```

  why not this?
  ```suggestion
  		if cq.Cohort == nil && !wl.LastSchedule.Equal(resourceFlavors, workload.FlavorResourceQuantities(cq.Usage), nil) {
  ```

- **pkg/scheduler/flavorassigner/flavorassigner.go:262** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T15:02:36Z
```diff
@@ -218,33 +220,67 @@ func (m FlavorAssignmentMode) String() string {
 }
 
 type FlavorAssignment struct {
-	Name   kueue.ResourceFlavorReference
-	Mode   FlavorAssignmentMode
-	borrow int64
+	Name                    kueue.ResourceFlavorReference
+	Mode                    FlavorAssignmentMode
+	LastScheduleFlavorIndex int
+	borrow                  int64
 }
 
 // AssignFlavors assigns flavors for each of the resources requested in each pod set.
 // The result for each pod set is accompanied with reasons why the flavor can't
 // be assigned immediately. Each assigned flavor is accompanied with a
 // FlavorAssignmentMode.
 func AssignFlavors(log logr.Logger, wl *workload.Info, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, counts []int32) Assignment {
+	if wl.LastSchedule != nil {
+		if cq.Cohort == nil && !wl.LastSchedule.Equal(resourceFlavors, workload.FlavorResourceQuantities(cq.Usage), make(workload.FlavorResourceQuantities)) {
+			wl.LastSchedule = nil
+		}
+		if cq.Cohort != nil && !wl.LastSchedule.Equal(resourceFlavors, workload.FlavorResourceQuantities(cq.Usage), workload.FlavorResourceQuantities(cq.Cohort.Usage)) {
+			wl.LastSchedule = nil
+		}
+	}
+
 	if len(counts) == 0 {
-		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastSchedule)
 	}
 
 	currentResources := make([]workload.PodSetResources, len(wl.TotalRequests))
 	for i := range wl.TotalRequests {
 		currentResources[i] = *wl.TotalRequests[i].ScaledTo(counts[i])
 	}
-	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastSchedule)
 }
 
-func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue) Assignment {
+func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, lastSchedule *workload.LastScheduleClusterQueueState) Assignment {
 	assignment := Assignment{
 		TotalBorrow: make(cache.FlavorResourceQuantities),
 		PodSets:     make([]PodSetAssignment, 0, len(requests)),
-		usage:       make(cache.FlavorResourceQuantities),
+		ScheduleState: workload.LastScheduleClusterQueueState{
+			LastScheduledFlavorIdx: make(map[string]map[corev1.ResourceName]int),
+			ResourceFlavors:        make(map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor),
+			ClusterQueueUsage:      make(workload.FlavorResourceQuantities),
+			CohortUsage:            make(workload.FlavorResourceQuantities),
```

  leave nil, in case there's no cohort.

- **pkg/scheduler/flavorassigner/flavorassigner.go:261** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T15:04:24Z
```diff
@@ -218,33 +220,67 @@ func (m FlavorAssignmentMode) String() string {
 }
 
 type FlavorAssignment struct {
-	Name   kueue.ResourceFlavorReference
-	Mode   FlavorAssignmentMode
-	borrow int64
+	Name                    kueue.ResourceFlavorReference
+	Mode                    FlavorAssignmentMode
+	LastScheduleFlavorIndex int
+	borrow                  int64
 }
 
 // AssignFlavors assigns flavors for each of the resources requested in each pod set.
 // The result for each pod set is accompanied with reasons why the flavor can't
 // be assigned immediately. Each assigned flavor is accompanied with a
 // FlavorAssignmentMode.
 func AssignFlavors(log logr.Logger, wl *workload.Info, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, counts []int32) Assignment {
+	if wl.LastSchedule != nil {
+		if cq.Cohort == nil && !wl.LastSchedule.Equal(resourceFlavors, workload.FlavorResourceQuantities(cq.Usage), make(workload.FlavorResourceQuantities)) {
+			wl.LastSchedule = nil
+		}
+		if cq.Cohort != nil && !wl.LastSchedule.Equal(resourceFlavors, workload.FlavorResourceQuantities(cq.Usage), workload.FlavorResourceQuantities(cq.Cohort.Usage)) {
+			wl.LastSchedule = nil
+		}
+	}
+
 	if len(counts) == 0 {
-		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastSchedule)
 	}
 
 	currentResources := make([]workload.PodSetResources, len(wl.TotalRequests))
 	for i := range wl.TotalRequests {
 		currentResources[i] = *wl.TotalRequests[i].ScaledTo(counts[i])
 	}
-	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastSchedule)
 }
 
-func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue) Assignment {
+func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, lastSchedule *workload.LastScheduleClusterQueueState) Assignment {
 	assignment := Assignment{
 		TotalBorrow: make(cache.FlavorResourceQuantities),
 		PodSets:     make([]PodSetAssignment, 0, len(requests)),
-		usage:       make(cache.FlavorResourceQuantities),
+		ScheduleState: workload.LastScheduleClusterQueueState{
+			LastScheduledFlavorIdx: make(map[string]map[corev1.ResourceName]int),
+			ResourceFlavors:        make(map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor),
+			ClusterQueueUsage:      make(workload.FlavorResourceQuantities),
+			CohortUsage:            make(workload.FlavorResourceQuantities),
+		},
+		usage: make(cache.FlavorResourceQuantities),
+	}
+	for res, flavor := range resourceFlavors {
+		assignment.ScheduleState.ResourceFlavors[res] = flavor
+	}
+	for flavor, flavorusage := range cq.Usage {
+		assignment.ScheduleState.ClusterQueueUsage[flavor] = make(map[corev1.ResourceName]int64)
+		for res, usage := range flavorusage {
+			assignment.ScheduleState.ClusterQueueUsage[flavor][res] = usage
+		}
```

  can you use a single maps.Clone?

- **pkg/scheduler/flavorassigner/flavorassigner.go:270** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T15:05:14Z
```diff
@@ -218,33 +220,67 @@ func (m FlavorAssignmentMode) String() string {
 }
 
 type FlavorAssignment struct {
-	Name   kueue.ResourceFlavorReference
-	Mode   FlavorAssignmentMode
-	borrow int64
+	Name                    kueue.ResourceFlavorReference
+	Mode                    FlavorAssignmentMode
+	LastScheduleFlavorIndex int
+	borrow                  int64
 }
 
 // AssignFlavors assigns flavors for each of the resources requested in each pod set.
 // The result for each pod set is accompanied with reasons why the flavor can't
 // be assigned immediately. Each assigned flavor is accompanied with a
 // FlavorAssignmentMode.
 func AssignFlavors(log logr.Logger, wl *workload.Info, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, counts []int32) Assignment {
+	if wl.LastSchedule != nil {
+		if cq.Cohort == nil && !wl.LastSchedule.Equal(resourceFlavors, workload.FlavorResourceQuantities(cq.Usage), make(workload.FlavorResourceQuantities)) {
+			wl.LastSchedule = nil
+		}
+		if cq.Cohort != nil && !wl.LastSchedule.Equal(resourceFlavors, workload.FlavorResourceQuantities(cq.Usage), workload.FlavorResourceQuantities(cq.Cohort.Usage)) {
+			wl.LastSchedule = nil
+		}
+	}
+
 	if len(counts) == 0 {
-		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastSchedule)
 	}
 
 	currentResources := make([]workload.PodSetResources, len(wl.TotalRequests))
 	for i := range wl.TotalRequests {
 		currentResources[i] = *wl.TotalRequests[i].ScaledTo(counts[i])
 	}
-	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastSchedule)
 }
 
-func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue) Assignment {
+func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, lastSchedule *workload.LastScheduleClusterQueueState) Assignment {
 	assignment := Assignment{
 		TotalBorrow: make(cache.FlavorResourceQuantities),
 		PodSets:     make([]PodSetAssignment, 0, len(requests)),
-		usage:       make(cache.FlavorResourceQuantities),
+		ScheduleState: workload.LastScheduleClusterQueueState{
+			LastScheduledFlavorIdx: make(map[string]map[corev1.ResourceName]int),
+			ResourceFlavors:        make(map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor),
+			ClusterQueueUsage:      make(workload.FlavorResourceQuantities),
+			CohortUsage:            make(workload.FlavorResourceQuantities),
+		},
+		usage: make(cache.FlavorResourceQuantities),
+	}
+	for res, flavor := range resourceFlavors {
+		assignment.ScheduleState.ResourceFlavors[res] = flavor
+	}
+	for flavor, flavorusage := range cq.Usage {
+		assignment.ScheduleState.ClusterQueueUsage[flavor] = make(map[corev1.ResourceName]int64)
```

  ```suggestion
  		assignment.ScheduleState.ClusterQueueUsage[flavor] = make(map[corev1.ResourceName]int64, len(flavorusage))
  ```

- **pkg/scheduler/flavorassigner/flavorassigner.go:277** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T15:05:35Z
```diff
@@ -218,33 +220,67 @@ func (m FlavorAssignmentMode) String() string {
 }
 
 type FlavorAssignment struct {
-	Name   kueue.ResourceFlavorReference
-	Mode   FlavorAssignmentMode
-	borrow int64
+	Name                    kueue.ResourceFlavorReference
+	Mode                    FlavorAssignmentMode
+	LastScheduleFlavorIndex int
+	borrow                  int64
 }
 
 // AssignFlavors assigns flavors for each of the resources requested in each pod set.
 // The result for each pod set is accompanied with reasons why the flavor can't
 // be assigned immediately. Each assigned flavor is accompanied with a
 // FlavorAssignmentMode.
 func AssignFlavors(log logr.Logger, wl *workload.Info, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, counts []int32) Assignment {
+	if wl.LastSchedule != nil {
+		if cq.Cohort == nil && !wl.LastSchedule.Equal(resourceFlavors, workload.FlavorResourceQuantities(cq.Usage), make(workload.FlavorResourceQuantities)) {
+			wl.LastSchedule = nil
+		}
+		if cq.Cohort != nil && !wl.LastSchedule.Equal(resourceFlavors, workload.FlavorResourceQuantities(cq.Usage), workload.FlavorResourceQuantities(cq.Cohort.Usage)) {
+			wl.LastSchedule = nil
+		}
+	}
+
 	if len(counts) == 0 {
-		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastSchedule)
 	}
 
 	currentResources := make([]workload.PodSetResources, len(wl.TotalRequests))
 	for i := range wl.TotalRequests {
 		currentResources[i] = *wl.TotalRequests[i].ScaledTo(counts[i])
 	}
-	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastSchedule)
 }
 
-func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue) Assignment {
+func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, lastSchedule *workload.LastScheduleClusterQueueState) Assignment {
 	assignment := Assignment{
 		TotalBorrow: make(cache.FlavorResourceQuantities),
 		PodSets:     make([]PodSetAssignment, 0, len(requests)),
-		usage:       make(cache.FlavorResourceQuantities),
+		ScheduleState: workload.LastScheduleClusterQueueState{
+			LastScheduledFlavorIdx: make(map[string]map[corev1.ResourceName]int),
+			ResourceFlavors:        make(map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor),
+			ClusterQueueUsage:      make(workload.FlavorResourceQuantities),
+			CohortUsage:            make(workload.FlavorResourceQuantities),
+		},
+		usage: make(cache.FlavorResourceQuantities),
+	}
+	for res, flavor := range resourceFlavors {
+		assignment.ScheduleState.ResourceFlavors[res] = flavor
+	}
+	for flavor, flavorusage := range cq.Usage {
+		assignment.ScheduleState.ClusterQueueUsage[flavor] = make(map[corev1.ResourceName]int64)
+		for res, usage := range flavorusage {
+			assignment.ScheduleState.ClusterQueueUsage[flavor][res] = usage
+		}
+	}
+	if cq.Cohort != nil {
+		for flavor, flavorusage := range cq.Cohort.Usage {
+			assignment.ScheduleState.CohortUsage[flavor] = make(map[corev1.ResourceName]int64)
```

  Same recommendations as above.

- **pkg/scheduler/flavorassigner/flavorassigner.go:258** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T15:07:43Z
```diff
@@ -218,33 +220,67 @@ func (m FlavorAssignmentMode) String() string {
 }
 
 type FlavorAssignment struct {
-	Name   kueue.ResourceFlavorReference
-	Mode   FlavorAssignmentMode
-	borrow int64
+	Name                    kueue.ResourceFlavorReference
+	Mode                    FlavorAssignmentMode
+	LastScheduleFlavorIndex int
+	borrow                  int64
 }
 
 // AssignFlavors assigns flavors for each of the resources requested in each pod set.
 // The result for each pod set is accompanied with reasons why the flavor can't
 // be assigned immediately. Each assigned flavor is accompanied with a
 // FlavorAssignmentMode.
 func AssignFlavors(log logr.Logger, wl *workload.Info, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, counts []int32) Assignment {
+	if wl.LastSchedule != nil {
+		if cq.Cohort == nil && !wl.LastSchedule.Equal(resourceFlavors, workload.FlavorResourceQuantities(cq.Usage), make(workload.FlavorResourceQuantities)) {
+			wl.LastSchedule = nil
+		}
+		if cq.Cohort != nil && !wl.LastSchedule.Equal(resourceFlavors, workload.FlavorResourceQuantities(cq.Usage), workload.FlavorResourceQuantities(cq.Cohort.Usage)) {
+			wl.LastSchedule = nil
+		}
+	}
+
 	if len(counts) == 0 {
-		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastSchedule)
 	}
 
 	currentResources := make([]workload.PodSetResources, len(wl.TotalRequests))
 	for i := range wl.TotalRequests {
 		currentResources[i] = *wl.TotalRequests[i].ScaledTo(counts[i])
 	}
-	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastSchedule)
 }
 
-func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue) Assignment {
+func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, lastSchedule *workload.LastScheduleClusterQueueState) Assignment {
 	assignment := Assignment{
 		TotalBorrow: make(cache.FlavorResourceQuantities),
 		PodSets:     make([]PodSetAssignment, 0, len(requests)),
-		usage:       make(cache.FlavorResourceQuantities),
+		ScheduleState: workload.LastScheduleClusterQueueState{
```

  if there is a previous state, we already verified that it matches the previous state of the CQ and cohort. So in that case, we shouldn't need to create new maps, we can reuse the existing ones.

- **pkg/workload/workload.go:62** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T15:18:53Z
```diff
@@ -39,6 +40,40 @@ var (
 	admissionManagedConditions = []string{kueue.WorkloadAdmitted, kueue.WorkloadEvicted}
 )
 
+type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
+type LastScheduleClusterQueueState struct {
+	LastScheduledFlavorIdx map[string]map[corev1.ResourceName]int
+	ResourceFlavors        map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor
+	ClusterQueueUsage      FlavorResourceQuantities
+	CohortUsage            FlavorResourceQuantities
+}
+
+func (s *LastScheduleClusterQueueState) Equal(resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, clusterQueueUsage, cohortUsage FlavorResourceQuantities) bool {
+	return reflect.DeepEqual(s.ResourceFlavors, resourceFlavors) && reflect.DeepEqual(s.ClusterQueueUsage, clusterQueueUsage) && reflect.DeepEqual(s.CohortUsage, cohortUsage)
+}
+
+func (s *LastScheduleClusterQueueState) Clone() *LastScheduleClusterQueueState {
+	c := LastScheduleClusterQueueState{
+		LastScheduledFlavorIdx: make(map[string]map[corev1.ResourceName]int),
+		ResourceFlavors:        make(map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor),
+		ClusterQueueUsage:      make(FlavorResourceQuantities),
+		CohortUsage:            make(FlavorResourceQuantities),
+	}
+	for ps, flavorIdx := range s.LastScheduledFlavorIdx {
+		c.LastScheduledFlavorIdx[ps] = maps.Clone(flavorIdx)
+	}
+	for res, flavor := range s.ResourceFlavors {
+		c.ResourceFlavors[res] = flavor
+	}
+	for flavor, flavorusage := range s.ClusterQueueUsage {
+		c.ClusterQueueUsage[flavor] = maps.Clone(flavorusage)
+	}
+	for flavor, flavorusage := range s.CohortUsage {
+		c.CohortUsage[flavor] = maps.Clone(flavorusage)
+	}
+	return &c
+}
+
 // Info holds a Workload object and some pre-processing.
 type Info struct {
```

  Not sure if this will be relevant, but worth checking.
  
  When a admission attempt fails, we usually send a status update to the workload with an Admitted=False condition
  
  https://github.com/kubernetes-sigs/kueue/blob/3d1dc9a4fb78c52bdb8ca53d8c94139a6f241304/pkg/scheduler/scheduler.go#L470-L471
  
  The update to the object would cause an update into the queue:
  
  https://github.com/kubernetes-sigs/kueue/blob/3d1dc9a4fb78c52bdb8ca53d8c94139a6f241304/pkg/controller/core/workload_controller.go#L297
  
  The problem is that in there we always call `workload.NewInfo`, which could wipe the saved status.
  
  Note that requeuing happens before sending the update https://github.com/kubernetes-sigs/kueue/blob/3d1dc9a4fb78c52bdb8ca53d8c94139a6f241304/pkg/scheduler/scheduler.go#L466-L471
  
  But with enough retries, it is possible that a Workload update comes while we are doing a scheduling attempt. Then, we might need to change `requeueIfNotPresent` to still update the lastState.
  
  https://github.com/kubernetes-sigs/kueue/blob/3d1dc9a4fb78c52bdb8ca53d8c94139a6f241304/pkg/queue/cluster_queue_impl.go#L130

- **pkg/scheduler/flavorassigner/flavorassigner.go:310** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T15:29:21Z
```diff
@@ -271,7 +307,11 @@ func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets
 				}
 				break
 			}
-			flavors, status := assignment.findFlavorForResourceGroup(log, rg, podSet.Requests, resourceFlavors, cq, &podSets[i].Template.Spec)
+			var lastScheduleFlavorInfo map[corev1.ResourceName]int
```

  ```suggestion
  			var lastFlavorAssignment map[corev1.ResourceName]int
  ```

- **pkg/scheduler/flavorassigner/flavorassigner.go:312** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T15:29:53Z
```diff
@@ -271,7 +307,11 @@ func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets
 				}
 				break
 			}
-			flavors, status := assignment.findFlavorForResourceGroup(log, rg, podSet.Requests, resourceFlavors, cq, &podSets[i].Template.Spec)
+			var lastScheduleFlavorInfo map[corev1.ResourceName]int
+			if lastSchedule != nil {
+				lastScheduleFlavorInfo = lastSchedule.LastScheduledFlavorIdx[podSet.Name]
```

  ```suggestion
  				lastScheduleFlavorInfo = lastSchedule.LastScheduledFlavorIdx[i]
  ```
  
  Better to use slices, as they are faster to allocate and query.

- **pkg/scheduler/flavorassigner/flavorassigner.go:348** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T15:31:21Z
```diff
@@ -305,6 +345,7 @@ func (psa *PodSetAssignment) append(flavors ResourceAssignment, status *Status)
 }
 
 func (a *Assignment) append(requests workload.Requests, psAssignment *PodSetAssignment) {
+	var flavorIdx = map[corev1.ResourceName]int{}
```

  ```suggestion
  	var flavorIdx = make(map[corev1.ResourceName]int, len(psAssignment.Flavors))
  ```

- **pkg/scheduler/flavorassigner/flavorassigner.go:413** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T15:41:57Z
```diff
@@ -364,10 +410,17 @@ func (a *Assignment) findFlavorForResourceGroup(
 			continue
 		}
 
+		whetherNeedBorrowing := false
```

  ```suggestion
  		needsBorrowing := false
  ```

- **pkg/scheduler/flavorassigner/flavorassigner.go:388** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T15:42:11Z
```diff
@@ -341,7 +385,9 @@ func (a *Assignment) findFlavorForResourceGroup(
 
 	// We will only check against the flavors' labels for the resource.
 	selector := flavorSelector(spec, rg.LabelKeys)
-	for _, flvQuotas := range rg.Flavors {
+	lastScheduledFlavorIdx := 0
```

  Within a resource group, all the resources get assigned the same flavor.
  So at this point you can find the last flavor used for any of the resources in this resource group, and start the iteration from there.
  
  It would be the same behavior you have below, but I think it will be easier to read.

- **pkg/scheduler/flavorassigner/flavorassigner.go:480** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T15:46:32Z
```diff
@@ -388,19 +442,52 @@ func (a *Assignment) findFlavorForResourceGroup(
 				borrow: borrow,
 			}
 		}
+		if skipFlavor {
+			continue
+		}
 
+		if !shouldTryNextFlavor(representativeMode, cq.Preemption.FlavorFungibility, whetherNeedBorrowing) {
+			bestAssignment = assignments
+			bestAssignmentMode = representativeMode
+			break
+		}
 		if representativeMode > bestAssignmentMode {
 			bestAssignment = assignments
 			bestAssignmentMode = representativeMode
-			if bestAssignmentMode == Fit {
-				// All the resources fit in the cohort, no need to check more flavors.
-				return bestAssignment, nil
-			}
 		}
 	}
+
+	for _, assignment := range bestAssignment {
+		if lastScheduledFlavorIdx == len(rg.Flavors)-1 {
+			assignment.LastScheduleFlavorIndex = -1
+		} else {
+			assignment.LastScheduleFlavorIndex = lastScheduledFlavorIdx
+		}
+	}
+	if bestAssignmentMode == Fit {
+		return bestAssignment, nil
+	}
 	return bestAssignment, status
 }
 
+func shouldTryNextFlavor(representativeMode FlavorAssignmentMode, flavorFungibility v1beta1.FlavorFungibility, whetherNeedBorrowing bool) bool {
+	policyPreempt := flavorFungibility.WhenCanPreempt
+	policyBorrow := flavorFungibility.WhenCanBorrow
+	if representativeMode == Preempt && policyPreempt == v1beta1.Preempt {
+		return false
+	}
+
+	if representativeMode == Fit && whetherNeedBorrowing && policyBorrow == v1beta1.Borrow {
```

  Is the code any simpler if we make Borrow be a mode?

- **pkg/scheduler/scheduler.go:266** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T15:51:01Z
```diff
@@ -263,6 +263,7 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 		} else {
 			e.assignment, e.preemptionTargets = s.getAssignments(log, &e.Info, &snap)
 			e.inadmissibleMsg = e.assignment.Message()
+			e.Info.LastSchedule = e.assignment.ScheduleState.Clone()
```

  Why do we need to clone?
  
  I don't think there are other users of these maps, so it should be safe to use the same references.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-18T15:04:07Z

- **apis/kueue/v1beta1/clusterqueue_types.go:299** by [@trasc](https://github.com/trasc) — 2023-08-11T10:51:48Z
```diff
@@ -276,6 +295,17 @@ type ClusterQueuePreemption struct {
 	// +kubebuilder:default=Never
 	// +kubebuilder:validation:Enum=Never;LowerPriority;LowerOrNewerEqualPriority
 	WithinClusterQueue PreemptionPolicy `json:"withinClusterQueue,omitempty"`
+
+	// flagorFungibility determines whether a workload should try the next flavor
```

  ```suggestion
  	// flavorFungibility determines whether a workload should try the next flavor
  ```

- **pkg/workload/workload.go:43** by [@trasc](https://github.com/trasc) — 2023-08-11T11:45:22Z
```diff
@@ -39,6 +40,40 @@ var (
 	admissionManagedConditions = []string{kueue.WorkloadAdmitted, kueue.WorkloadEvicted}
 )
 
+type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
```

  why this duplicate type (we have one in cache)

- **pkg/workload/workload.go:48** by [@trasc](https://github.com/trasc) — 2023-08-18T14:55:33Z
```diff
@@ -39,6 +40,40 @@ var (
 	admissionManagedConditions = []string{kueue.WorkloadAdmitted, kueue.WorkloadEvicted}
 )
 
+type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
+type LastScheduleClusterQueueState struct {
+	LastScheduledFlavorIdx map[string]map[corev1.ResourceName]int
+	ResourceFlavors        map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor
+	ClusterQueueUsage      FlavorResourceQuantities
+	CohortUsage            FlavorResourceQuantities
```

  Is this the only way we can do this, keeping snapshots of the queue/cohort usage looks "expensive" to me.

- **pkg/workload/workload.go:51** by [@trasc](https://github.com/trasc) — 2023-08-18T15:03:21Z
```diff
@@ -39,6 +40,40 @@ var (
 	admissionManagedConditions = []string{kueue.WorkloadAdmitted, kueue.WorkloadEvicted}
 )
 
+type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
+type LastScheduleClusterQueueState struct {
+	LastScheduledFlavorIdx map[string]map[corev1.ResourceName]int
+	ResourceFlavors        map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor
+	ClusterQueueUsage      FlavorResourceQuantities
+	CohortUsage            FlavorResourceQuantities
+}
+
+func (s *LastScheduleClusterQueueState) Equal(resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, clusterQueueUsage, cohortUsage FlavorResourceQuantities) bool {
```

  If going with this full snapshot approach, can't we do more with that data, so the assignment from the beginning is triggered only the current state of the queue/cohort gives better chances to fit in a "better" flavor.

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-08-21T07:59:17Z

- **apis/kueue/v1beta1/clusterqueue_types.go:299** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-08-21T07:59:17Z
```diff
@@ -276,6 +295,17 @@ type ClusterQueuePreemption struct {
 	// +kubebuilder:default=Never
 	// +kubebuilder:validation:Enum=Never;LowerPriority;LowerOrNewerEqualPriority
 	WithinClusterQueue PreemptionPolicy `json:"withinClusterQueue,omitempty"`
+
+	// flagorFungibility determines whether a workload should try the next flavor
```

  done

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-08-21T08:04:42Z

- **pkg/workload/workload.go:44** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-08-21T08:04:42Z
```diff
@@ -39,6 +40,40 @@ var (
 	admissionManagedConditions = []string{kueue.WorkloadAdmitted, kueue.WorkloadEvicted}
 )
 
+type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
+type LastScheduleClusterQueueState struct {
```

  done

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-08-21T08:20:19Z

- **pkg/workload/workload.go:45** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-08-21T08:20:19Z
```diff
@@ -39,6 +40,40 @@ var (
 	admissionManagedConditions = []string{kueue.WorkloadAdmitted, kueue.WorkloadEvicted}
 )
 
+type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
+type LastScheduleClusterQueueState struct {
+	LastScheduledFlavorIdx map[string]map[corev1.ResourceName]int
```

  done

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-08-21T09:20:36Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:235** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-08-21T09:20:36Z
```diff
@@ -218,33 +220,67 @@ func (m FlavorAssignmentMode) String() string {
 }
 
 type FlavorAssignment struct {
-	Name   kueue.ResourceFlavorReference
-	Mode   FlavorAssignmentMode
-	borrow int64
+	Name                    kueue.ResourceFlavorReference
+	Mode                    FlavorAssignmentMode
+	LastScheduleFlavorIndex int
+	borrow                  int64
 }
 
 // AssignFlavors assigns flavors for each of the resources requested in each pod set.
 // The result for each pod set is accompanied with reasons why the flavor can't
 // be assigned immediately. Each assigned flavor is accompanied with a
 // FlavorAssignmentMode.
 func AssignFlavors(log logr.Logger, wl *workload.Info, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, counts []int32) Assignment {
+	if wl.LastSchedule != nil {
+		if cq.Cohort == nil && !wl.LastSchedule.Equal(resourceFlavors, workload.FlavorResourceQuantities(cq.Usage), make(workload.FlavorResourceQuantities)) {
```

  done

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-08-21T09:38:19Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:258** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-08-21T09:38:19Z
```diff
@@ -218,33 +220,67 @@ func (m FlavorAssignmentMode) String() string {
 }
 
 type FlavorAssignment struct {
-	Name   kueue.ResourceFlavorReference
-	Mode   FlavorAssignmentMode
-	borrow int64
+	Name                    kueue.ResourceFlavorReference
+	Mode                    FlavorAssignmentMode
+	LastScheduleFlavorIndex int
+	borrow                  int64
 }
 
 // AssignFlavors assigns flavors for each of the resources requested in each pod set.
 // The result for each pod set is accompanied with reasons why the flavor can't
 // be assigned immediately. Each assigned flavor is accompanied with a
 // FlavorAssignmentMode.
 func AssignFlavors(log logr.Logger, wl *workload.Info, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, counts []int32) Assignment {
+	if wl.LastSchedule != nil {
+		if cq.Cohort == nil && !wl.LastSchedule.Equal(resourceFlavors, workload.FlavorResourceQuantities(cq.Usage), make(workload.FlavorResourceQuantities)) {
+			wl.LastSchedule = nil
+		}
+		if cq.Cohort != nil && !wl.LastSchedule.Equal(resourceFlavors, workload.FlavorResourceQuantities(cq.Usage), workload.FlavorResourceQuantities(cq.Cohort.Usage)) {
+			wl.LastSchedule = nil
+		}
+	}
+
 	if len(counts) == 0 {
-		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastSchedule)
 	}
 
 	currentResources := make([]workload.PodSetResources, len(wl.TotalRequests))
 	for i := range wl.TotalRequests {
 		currentResources[i] = *wl.TotalRequests[i].ScaledTo(counts[i])
 	}
-	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastSchedule)
 }
 
-func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue) Assignment {
+func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, lastSchedule *workload.LastScheduleClusterQueueState) Assignment {
 	assignment := Assignment{
 		TotalBorrow: make(cache.FlavorResourceQuantities),
 		PodSets:     make([]PodSetAssignment, 0, len(requests)),
-		usage:       make(cache.FlavorResourceQuantities),
+		ScheduleState: workload.LastScheduleClusterQueueState{
```

  done

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-08-21T09:39:37Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:312** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-08-21T09:39:36Z
```diff
@@ -271,7 +307,11 @@ func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets
 				}
 				break
 			}
-			flavors, status := assignment.findFlavorForResourceGroup(log, rg, podSet.Requests, resourceFlavors, cq, &podSets[i].Template.Spec)
+			var lastScheduleFlavorInfo map[corev1.ResourceName]int
+			if lastSchedule != nil {
+				lastScheduleFlavorInfo = lastSchedule.LastScheduledFlavorIdx[podSet.Name]
```

  done, thanks

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-08-21T09:49:53Z

- **pkg/scheduler/scheduler.go:266** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-08-21T09:49:53Z
```diff
@@ -263,6 +263,7 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 		} else {
 			e.assignment, e.preemptionTargets = s.getAssignments(log, &e.Info, &snap)
 			e.inadmissibleMsg = e.assignment.Message()
+			e.Info.LastSchedule = e.assignment.ScheduleState.Clone()
```

  done,thanks

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-08-21T10:03:08Z

- **pkg/workload/workload.go:43** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-08-21T10:03:08Z
```diff
@@ -39,6 +40,40 @@ var (
 	admissionManagedConditions = []string{kueue.WorkloadAdmitted, kueue.WorkloadEvicted}
 )
 
+type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
```

  done, there will an import cycle if using cache.FlavorResourceQuantities.
  I have move definition from package cache to package workload

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-08-21T11:28:42Z

- **pkg/workload/workload.go:62** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-08-21T11:28:42Z
```diff
@@ -39,6 +40,40 @@ var (
 	admissionManagedConditions = []string{kueue.WorkloadAdmitted, kueue.WorkloadEvicted}
 )
 
+type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
+type LastScheduleClusterQueueState struct {
+	LastScheduledFlavorIdx map[string]map[corev1.ResourceName]int
+	ResourceFlavors        map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor
+	ClusterQueueUsage      FlavorResourceQuantities
+	CohortUsage            FlavorResourceQuantities
+}
+
+func (s *LastScheduleClusterQueueState) Equal(resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, clusterQueueUsage, cohortUsage FlavorResourceQuantities) bool {
+	return reflect.DeepEqual(s.ResourceFlavors, resourceFlavors) && reflect.DeepEqual(s.ClusterQueueUsage, clusterQueueUsage) && reflect.DeepEqual(s.CohortUsage, cohortUsage)
+}
+
+func (s *LastScheduleClusterQueueState) Clone() *LastScheduleClusterQueueState {
+	c := LastScheduleClusterQueueState{
+		LastScheduledFlavorIdx: make(map[string]map[corev1.ResourceName]int),
+		ResourceFlavors:        make(map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor),
+		ClusterQueueUsage:      make(FlavorResourceQuantities),
+		CohortUsage:            make(FlavorResourceQuantities),
+	}
+	for ps, flavorIdx := range s.LastScheduledFlavorIdx {
+		c.LastScheduledFlavorIdx[ps] = maps.Clone(flavorIdx)
+	}
+	for res, flavor := range s.ResourceFlavors {
+		c.ResourceFlavors[res] = flavor
+	}
+	for flavor, flavorusage := range s.ClusterQueueUsage {
+		c.ClusterQueueUsage[flavor] = maps.Clone(flavorusage)
+	}
+	for flavor, flavorusage := range s.CohortUsage {
+		c.CohortUsage[flavor] = maps.Clone(flavorusage)
+	}
+	return &c
+}
+
 // Info holds a Workload object and some pre-processing.
 type Info struct {
```

  I will add some check in `Manager.addOrUpdateWorkload()` to judge whether to copy lastState

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-08-21T11:39:38Z

- **pkg/workload/workload.go:48** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-08-21T11:39:38Z
```diff
@@ -39,6 +40,40 @@ var (
 	admissionManagedConditions = []string{kueue.WorkloadAdmitted, kueue.WorkloadEvicted}
 )
 
+type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
+type LastScheduleClusterQueueState struct {
+	LastScheduledFlavorIdx map[string]map[corev1.ResourceName]int
+	ResourceFlavors        map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor
+	ClusterQueueUsage      FlavorResourceQuantities
+	CohortUsage            FlavorResourceQuantities
```

  let me try to use generation to solve this.
  we can record the generation of the Cohort and cluster queue, and increase the generation when more resources were add to cohort or cluster queue (either some workloads were released or cluster queue/cohort was changed)

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-08-22T02:53:24Z

- **pkg/workload/workload.go:51** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-08-22T02:53:24Z
```diff
@@ -39,6 +40,40 @@ var (
 	admissionManagedConditions = []string{kueue.WorkloadAdmitted, kueue.WorkloadEvicted}
 )
 
+type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
+type LastScheduleClusterQueueState struct {
+	LastScheduledFlavorIdx map[string]map[corev1.ResourceName]int
+	ResourceFlavors        map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor
+	ClusterQueueUsage      FlavorResourceQuantities
+	CohortUsage            FlavorResourceQuantities
+}
+
+func (s *LastScheduleClusterQueueState) Equal(resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, clusterQueueUsage, cohortUsage FlavorResourceQuantities) bool {
```

  I will record the generation of the Cohort and cluster queue, and increase the generation when more resources were add to cohort or cluster queue (either some workloads were released or cluster queue/cohort was changed)

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-08-22T07:44:03Z

- **pkg/workload/workload.go:48** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-08-22T07:44:03Z
```diff
@@ -39,6 +40,40 @@ var (
 	admissionManagedConditions = []string{kueue.WorkloadAdmitted, kueue.WorkloadEvicted}
 )
 
+type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
+type LastScheduleClusterQueueState struct {
+	LastScheduledFlavorIdx map[string]map[corev1.ResourceName]int
+	ResourceFlavors        map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor
+	ClusterQueueUsage      FlavorResourceQuantities
+	CohortUsage            FlavorResourceQuantities
```

  done

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-08-22T07:44:14Z

- **pkg/workload/workload.go:51** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-08-22T07:44:14Z
```diff
@@ -39,6 +40,40 @@ var (
 	admissionManagedConditions = []string{kueue.WorkloadAdmitted, kueue.WorkloadEvicted}
 )
 
+type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
+type LastScheduleClusterQueueState struct {
+	LastScheduledFlavorIdx map[string]map[corev1.ResourceName]int
+	ResourceFlavors        map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor
+	ClusterQueueUsage      FlavorResourceQuantities
+	CohortUsage            FlavorResourceQuantities
+}
+
+func (s *LastScheduleClusterQueueState) Equal(resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, clusterQueueUsage, cohortUsage FlavorResourceQuantities) bool {
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-01T20:07:20Z

I gave a partial review.
I'll try to finish early next week.

- **apis/kueue/v1beta1/clusterqueue_types.go:76** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-01T19:44:32Z
```diff
@@ -72,6 +72,10 @@ type ClusterQueueSpec struct {
 	// If set to an empty selector `{}`, then all namespaces are eligible.
 	NamespaceSelector *metav1.LabelSelector `json:"namespaceSelector,omitempty"`
 
+	// flavorFungibility defaines whether a workload should try the next flavor
+	// before borrowing in current flavor.
```

  ```suggestion
  	// flavorFungibility defines whether a workload should try the next flavor
  	// before borrowing or preempting in the flavor being evaluated.
  ```

- **apis/kueue/v1beta1/clusterqueue_types.go:278** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-01T19:46:22Z
```diff
@@ -243,6 +247,41 @@ const (
 	PreemptionPolicyLowerOrNewerEqualPriority PreemptionPolicy = "LowerOrNewerEqualPriority"
 )
 
+type FlavorFungibilityPolicy string
+
+const (
+	Borrow        FlavorFungibilityPolicy = "Borrow"
+	Preempt       FlavorFungibilityPolicy = "Preempt"
+	TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
+)
+
+// FlavorFungibility determines whether a workload should try the next flavor
+// before borrowing or preempting in current flavor.
+type FlavorFungibility struct {
+	// whenCanBorrow determines whether a workload should try the next flavor
+	// before borrowing in current flavor. The possible values are:
+	//
+	// - `Borrow` (default for WhenCanBorrow): allocate in current flavor if borrowing
+	//   can help. only effective for WhenCanBorrow
+	// - `TryNextFlavor` (default for WhenCanPreempt): try next flavor if the current
+	//   flavor has no enough resource. effective for WhenCanBorrow and WhenCanPreempt.
+	//
+	// +kubebuilder:validation:Enum={Borrow,TryNextFlavor}
+	// +kubebuilder:default="Borrow"
+	WhenCanBorrow FlavorFungibilityPolicy `json:"whenCanBorrow,omitempty"`
+	// whenCanPreempt determines whether a workload should try the next flavor
+	// before borrowing in current flavor. The possible values are:
+	//
+	// - `Preempt`: allocate in current flavor if preempt might help. only effective
+	//   for WhenCanPreempt
+	// - `TryNextFlavor` (default for WhenCanPreempt): try next flavor if the current
+	//   flavor has no enough resource. effective for WhenCanBorrow and WhenCanPreempt.
```

  ```suggestion
  	// - `TryNextFlavor` (default): try next flavor if the current
  	//   flavor has not enough resources and there not enough candidates for preemption.
  ```

- **apis/kueue/v1beta1/clusterqueue_types.go:276** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-01T19:46:55Z
```diff
@@ -243,6 +247,41 @@ const (
 	PreemptionPolicyLowerOrNewerEqualPriority PreemptionPolicy = "LowerOrNewerEqualPriority"
 )
 
+type FlavorFungibilityPolicy string
+
+const (
+	Borrow        FlavorFungibilityPolicy = "Borrow"
+	Preempt       FlavorFungibilityPolicy = "Preempt"
+	TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
+)
+
+// FlavorFungibility determines whether a workload should try the next flavor
+// before borrowing or preempting in current flavor.
+type FlavorFungibility struct {
+	// whenCanBorrow determines whether a workload should try the next flavor
+	// before borrowing in current flavor. The possible values are:
+	//
+	// - `Borrow` (default for WhenCanBorrow): allocate in current flavor if borrowing
+	//   can help. only effective for WhenCanBorrow
+	// - `TryNextFlavor` (default for WhenCanPreempt): try next flavor if the current
+	//   flavor has no enough resource. effective for WhenCanBorrow and WhenCanPreempt.
+	//
+	// +kubebuilder:validation:Enum={Borrow,TryNextFlavor}
+	// +kubebuilder:default="Borrow"
+	WhenCanBorrow FlavorFungibilityPolicy `json:"whenCanBorrow,omitempty"`
+	// whenCanPreempt determines whether a workload should try the next flavor
+	// before borrowing in current flavor. The possible values are:
+	//
+	// - `Preempt`: allocate in current flavor if preempt might help. only effective
+	//   for WhenCanPreempt
```

  ```suggestion
  	// - `Preempt`: allocate in current flavor if it's possible to preempt some workloads.
  ```

- **apis/kueue/v1beta1/clusterqueue_types.go:265** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-01T19:47:36Z
```diff
@@ -243,6 +247,41 @@ const (
 	PreemptionPolicyLowerOrNewerEqualPriority PreemptionPolicy = "LowerOrNewerEqualPriority"
 )
 
+type FlavorFungibilityPolicy string
+
+const (
+	Borrow        FlavorFungibilityPolicy = "Borrow"
+	Preempt       FlavorFungibilityPolicy = "Preempt"
+	TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
+)
+
+// FlavorFungibility determines whether a workload should try the next flavor
+// before borrowing or preempting in current flavor.
+type FlavorFungibility struct {
+	// whenCanBorrow determines whether a workload should try the next flavor
+	// before borrowing in current flavor. The possible values are:
+	//
+	// - `Borrow` (default for WhenCanBorrow): allocate in current flavor if borrowing
+	//   can help. only effective for WhenCanBorrow
```

  ```suggestion
  	// - `Borrow` (default): allocate in current flavor if borrowing
  	//   is possible.
  ```

- **apis/kueue/v1beta1/clusterqueue_types.go:267** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-01T19:48:05Z
```diff
@@ -243,6 +247,41 @@ const (
 	PreemptionPolicyLowerOrNewerEqualPriority PreemptionPolicy = "LowerOrNewerEqualPriority"
 )
 
+type FlavorFungibilityPolicy string
+
+const (
+	Borrow        FlavorFungibilityPolicy = "Borrow"
+	Preempt       FlavorFungibilityPolicy = "Preempt"
+	TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
+)
+
+// FlavorFungibility determines whether a workload should try the next flavor
+// before borrowing or preempting in current flavor.
+type FlavorFungibility struct {
+	// whenCanBorrow determines whether a workload should try the next flavor
+	// before borrowing in current flavor. The possible values are:
+	//
+	// - `Borrow` (default for WhenCanBorrow): allocate in current flavor if borrowing
+	//   can help. only effective for WhenCanBorrow
+	// - `TryNextFlavor` (default for WhenCanPreempt): try next flavor if the current
+	//   flavor has no enough resource. effective for WhenCanBorrow and WhenCanPreempt.
```

  ```suggestion
  	// - `TryNextFlavor` (default): try next flavor if the current
  	//   flavor has not enough resources to borrow.
  ```

- **pkg/cache/clusterqueue.go:55** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-01T19:50:36Z
```diff
@@ -47,8 +50,9 @@ type Cohort struct {
 	Members sets.Set[*ClusterQueue]
 
 	// These fields are only populated for a snapshot.
-	RequestableResources FlavorResourceQuantities
-	Usage                FlavorResourceQuantities
+	RequestableResources workload.FlavorResourceQuantities
+	Usage                workload.FlavorResourceQuantities
+	Generation           int64
```

  Is the comment `These fields are only populated for a snapshot.` accurate here?
  I ask because ClusterQueue.Generation is in the section that should be part of the snapshot, whereas Cohort.Generation is not.

- **pkg/cache/clusterqueue.go:78** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-01T19:50:55Z
```diff
@@ -71,12 +75,12 @@ type ResourceQuota struct {
 	BorrowingLimit *int64
 }
 
-type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
+// type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
```

  delete this line

- **pkg/cache/clusterqueue.go:210** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-01T19:54:00Z
```diff
@@ -189,6 +207,7 @@ func (c *ClusterQueue) updateResourceGroups(in []kueue.ResourceGroup) {
 			rg.Flavors = append(rg.Flavors, fQuotas)
 		}
 	}
+	atomic.AddInt64(&c.Generation, 1)
```

  do we need atomic? This function is already called inside a lock

- **pkg/cache/clusterqueue.go:303** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-01T19:56:18Z
```diff
@@ -281,9 +300,12 @@ func (c *ClusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
 		updateUsage(wi, c.localQueues[qKey].usage, m)
 		c.localQueues[qKey].admittedWorkloads += int(m)
 	}
+	if m < 0 {
```

  why only if `m < 0`?

- **pkg/cache/snapshot.go:41** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-01T19:58:20Z
```diff
@@ -36,8 +38,10 @@ func (s *Snapshot) RemoveWorkload(wl *workload.Info) {
 	cq := s.ClusterQueues[wl.ClusterQueue]
 	delete(cq.Workloads, workload.Key(wl.Obj))
 	updateUsage(wl, cq.Usage, -1)
+	atomic.AddInt64(&cq.Generation, 1)
```

  I don't think atomic is necessary here either. But are you sure you need to change the generation of a Snapshot?
  The only case where we modify a snapshot is when simulating preemption. But, after the simulation, we restore the Snapshot to its original state, so the usage would be the same.

- **pkg/cache/snapshot_test.go:40** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-01T20:01:29Z
```diff
@@ -28,19 +28,21 @@ import (
 	"k8s.io/apimachinery/pkg/util/sets"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/features"
 	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
 var snapCmpOpts = []cmp.Option{
 	cmpopts.EquateEmpty(),
 	cmpopts.IgnoreUnexported(ClusterQueue{}),
-	cmpopts.IgnoreFields(ClusterQueue{}, "RGByResource"),
-	cmpopts.IgnoreFields(Cohort{}, "Members"), // avoid recursion.
+	cmpopts.IgnoreFields(ClusterQueue{}, "RGByResource", "Generation"),
+	cmpopts.IgnoreFields(Cohort{}, "Members", "Generation"), // avoid recursion.
```

  maybe it's worth testing that the generation has the value that we expect

- **pkg/features/kube_features.go:39** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-01T20:02:00Z
```diff
@@ -33,6 +33,13 @@ const (
 	//
 	// Enables partial admission.
 	PartialAdmission featuregate.Feature = "PartialAdmission"
+
+	// owner: @KunWuLuan
+	// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/582-preempt-based-on-flavor-order
+	// alpha: v0.6
```

  I think we can start at beta, enabled by default

- **pkg/features/kube_features.go:41** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-01T20:02:12Z
```diff
@@ -33,6 +33,13 @@ const (
 	//
 	// Enables partial admission.
 	PartialAdmission featuregate.Feature = "PartialAdmission"
+
+	// owner: @KunWuLuan
+	// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/582-preempt-based-on-flavor-order
+	// alpha: v0.6
+	//
+	// Enables partial admission.
```

  copy paste error :)

- **pkg/features/kube_features_test.go:28** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-01T20:03:13Z
```diff
@@ -25,8 +25,12 @@ import (
 
 func TestFeatureGate(t *testing.T) {
 	defer featuregatetesting.SetFeatureGateDuringTest(t, utilfeature.DefaultFeatureGate, PartialAdmission, true)()
+	defer featuregatetesting.SetFeatureGateDuringTest(t, utilfeature.DefaultFeatureGate, FlavorFungibility, true)()
```

  No need to test this for every single feature

- **test/integration/scheduler/scheduler_test.go:1079** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-01T20:04:34Z
```diff
@@ -1075,6 +1075,43 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.FinishWorkloads(ctx, k8sClient, pWl3)
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, devCQ.Name, dWl1)
 		})
+
+		ginkgo.It("Should schedule when fungibility is set", func() {
```

  try to be a more descriptive

- **test/integration/scheduler/scheduler_test.go:1110** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-01T20:06:13Z
```diff
@@ -1075,6 +1075,43 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.FinishWorkloads(ctx, k8sClient, pWl3)
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, devCQ.Name, dWl1)
 		})
+
+		ginkgo.It("Should schedule when fungibility is set", func() {
+			prodCQ = testing.MakeClusterQueue("prod-cq").
+				Cohort("all").
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodCQ)).Should(gomega.Succeed())
+
+			devCQ = testing.MakeClusterQueue("dev-cq").
+				Cohort("all").
+				FlavorFungibility(kueue.FlavorFungibility{WhenCanBorrow: kueue.TryNextFlavor}).
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj(), *testing.MakeFlavorQuotas("spot-tainted").Resource(corev1.ResourceCPU, "10").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, devCQ)).Should(gomega.Succeed())
+
+			prodQueue := testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodQueue)).Should(gomega.Succeed())
+
+			podQueue := testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, podQueue)).Should(gomega.Succeed())
+
+			ginkgo.By("Creating one workload")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "9").Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodCQ.Name, wl1)
+			util.ExpectPendingWorkloadsMetric(prodCQ, 0, 0)
+			util.ExpectAdmittedActiveWorkloadsMetric(prodCQ, 1)
+			util.ExpectAdmittedWorkloadsTotalMetric(prodCQ, 1)
+
+			ginkgo.By("Creating another workload")
+			wl2 := testing.MakeWorkload("wl-2", ns.Name).Queue(podQueue.Name).Request(corev1.ResourceCPU, "9").Toleration(spotToleration).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, devCQ.Name, wl2)
```

  We should probably check that it's admitted in the flavor that we expect.
  You can use `ExpectWorkloadToBeAdmittedAs`

- **test/integration/scheduler/scheduler_test.go:1092** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-01T20:06:49Z
```diff
@@ -1075,6 +1075,43 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.FinishWorkloads(ctx, k8sClient, pWl3)
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, devCQ.Name, dWl1)
 		})
+
+		ginkgo.It("Should schedule when fungibility is set", func() {
+			prodCQ = testing.MakeClusterQueue("prod-cq").
+				Cohort("all").
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodCQ)).Should(gomega.Succeed())
+
+			devCQ = testing.MakeClusterQueue("dev-cq").
+				Cohort("all").
+				FlavorFungibility(kueue.FlavorFungibility{WhenCanBorrow: kueue.TryNextFlavor}).
```

  Add another test for the other preemption policy.

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-04T12:30:22Z

- **test/integration/scheduler/scheduler_test.go:1079** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-04T12:30:22Z
```diff
@@ -1075,6 +1075,43 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.FinishWorkloads(ctx, k8sClient, pWl3)
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, devCQ.Name, dWl1)
 		})
+
+		ginkgo.It("Should schedule when fungibility is set", func() {
```

  Is "Should try next flavor instead of borrowing" ok?

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-04T12:30:35Z

- **test/integration/scheduler/scheduler_test.go:1110** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-04T12:30:35Z
```diff
@@ -1075,6 +1075,43 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.FinishWorkloads(ctx, k8sClient, pWl3)
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, devCQ.Name, dWl1)
 		})
+
+		ginkgo.It("Should schedule when fungibility is set", func() {
+			prodCQ = testing.MakeClusterQueue("prod-cq").
+				Cohort("all").
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodCQ)).Should(gomega.Succeed())
+
+			devCQ = testing.MakeClusterQueue("dev-cq").
+				Cohort("all").
+				FlavorFungibility(kueue.FlavorFungibility{WhenCanBorrow: kueue.TryNextFlavor}).
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj(), *testing.MakeFlavorQuotas("spot-tainted").Resource(corev1.ResourceCPU, "10").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, devCQ)).Should(gomega.Succeed())
+
+			prodQueue := testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodQueue)).Should(gomega.Succeed())
+
+			podQueue := testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, podQueue)).Should(gomega.Succeed())
+
+			ginkgo.By("Creating one workload")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "9").Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodCQ.Name, wl1)
+			util.ExpectPendingWorkloadsMetric(prodCQ, 0, 0)
+			util.ExpectAdmittedActiveWorkloadsMetric(prodCQ, 1)
+			util.ExpectAdmittedWorkloadsTotalMetric(prodCQ, 1)
+
+			ginkgo.By("Creating another workload")
+			wl2 := testing.MakeWorkload("wl-2", ns.Name).Queue(podQueue.Name).Request(corev1.ResourceCPU, "9").Toleration(spotToleration).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, devCQ.Name, wl2)
```

  Done

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-04T12:51:34Z

- **test/integration/scheduler/scheduler_test.go:1092** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-04T12:51:33Z
```diff
@@ -1075,6 +1075,43 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.FinishWorkloads(ctx, k8sClient, pWl3)
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, devCQ.Name, dWl1)
 		})
+
+		ginkgo.It("Should schedule when fungibility is set", func() {
+			prodCQ = testing.MakeClusterQueue("prod-cq").
+				Cohort("all").
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodCQ)).Should(gomega.Succeed())
+
+			devCQ = testing.MakeClusterQueue("dev-cq").
+				Cohort("all").
+				FlavorFungibility(kueue.FlavorFungibility{WhenCanBorrow: kueue.TryNextFlavor}).
```

  sure. done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-05T20:10:53Z

A few more comments, but I see that there are a few comments still open.

- **apis/kueue/v1beta1/clusterqueue_types.go:271** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-05T19:43:34Z
```diff
@@ -247,6 +251,40 @@ const (
 	PreemptionPolicyLowerOrNewerEqualPriority PreemptionPolicy = "LowerOrNewerEqualPriority"
 )
 
+type FlavorFungibilityPolicy string
+
+const (
+	Borrow        FlavorFungibilityPolicy = "Borrow"
+	Preempt       FlavorFungibilityPolicy = "Preempt"
+	TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
+)
+
+// FlavorFungibility determines whether a workload should try the next flavor
+// before borrowing or preempting in current flavor.
+type FlavorFungibility struct {
+	// whenCanBorrow determines whether a workload should try the next flavor
+	// before borrowing in current flavor. The possible values are:
+	//
+	// - `Borrow` (default): allocate in current flavor if borrowing
+	//   is possible.
+	// - `TryNextFlavor`: try next flavor if the current
+	//   flavor has not enough resources to borrow.
```

  ```suggestion
  	// - `TryNextFlavor`: try next flavor even if the current
  	//   flavor has enough resources to borrow.
  ```
  
  I realized my previous suggestion was wrong.

- **apis/kueue/v1beta1/clusterqueue_types.go:281** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-05T19:44:31Z
```diff
@@ -247,6 +251,40 @@ const (
 	PreemptionPolicyLowerOrNewerEqualPriority PreemptionPolicy = "LowerOrNewerEqualPriority"
 )
 
+type FlavorFungibilityPolicy string
+
+const (
+	Borrow        FlavorFungibilityPolicy = "Borrow"
+	Preempt       FlavorFungibilityPolicy = "Preempt"
+	TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
+)
+
+// FlavorFungibility determines whether a workload should try the next flavor
+// before borrowing or preempting in current flavor.
+type FlavorFungibility struct {
+	// whenCanBorrow determines whether a workload should try the next flavor
+	// before borrowing in current flavor. The possible values are:
+	//
+	// - `Borrow` (default): allocate in current flavor if borrowing
+	//   is possible.
+	// - `TryNextFlavor`: try next flavor if the current
+	//   flavor has not enough resources to borrow.
+	//
+	// +kubebuilder:validation:Enum={Borrow,TryNextFlavor}
+	// +kubebuilder:default="Borrow"
+	WhenCanBorrow FlavorFungibilityPolicy `json:"whenCanBorrow,omitempty"`
+	// whenCanPreempt determines whether a workload should try the next flavor
+	// before borrowing in current flavor. The possible values are:
+	//
+	// - `Preempt`: allocate in current flavor if it's possible to preempt some workloads.
+	// - `TryNextFlavor` (default): try next flavor if the current
+	//   flavor has not enough resources and there not enough candidates for preemption.
```

  ```suggestion
  	// - `TryNextFlavor` (default): try next flavor even if there are enough
  	//   candidates for preemption in the current flavor.
  ```

- **pkg/scheduler/flavorassigner/flavorassigner.go:237** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-05T20:00:50Z
```diff
@@ -218,33 +221,60 @@ func (m FlavorAssignmentMode) String() string {
 }
 
 type FlavorAssignment struct {
-	Name   kueue.ResourceFlavorReference
-	Mode   FlavorAssignmentMode
-	borrow int64
+	Name                  kueue.ResourceFlavorReference
+	Mode                  FlavorAssignmentMode
+	LastAssignedFlavorIdx int
+	borrow                int64
 }
 
 // AssignFlavors assigns flavors for each of the resources requested in each pod set.
 // The result for each pod set is accompanied with reasons why the flavor can't
 // be assigned immediately. Each assigned flavor is accompanied with a
 // FlavorAssignmentMode.
 func AssignFlavors(log logr.Logger, wl *workload.Info, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, counts []int32) Assignment {
+	if wl.LastAssignment != nil {
+		if cq.Generation > wl.LastAssignment.ClusterQueueGeneration &&
+			(cq.Cohort == nil || cq.Cohort.Generation > wl.LastAssignment.CohortGeneration) {
```

  Shouldn't it be like this?
  ```suggestion
  		if cq.Generation > wl.LastAssignment.ClusterQueueGeneration ||
  			(cq.Cohort != nil && cq.Cohort.Generation > wl.LastAssignment.CohortGeneration) {
  ```
  
  Please make sure to have a unit test for this.

- **pkg/scheduler/flavorassigner/flavorassigner.go:271** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-05T20:03:50Z
```diff
@@ -218,33 +221,60 @@ func (m FlavorAssignmentMode) String() string {
 }
 
 type FlavorAssignment struct {
-	Name   kueue.ResourceFlavorReference
-	Mode   FlavorAssignmentMode
-	borrow int64
+	Name                  kueue.ResourceFlavorReference
+	Mode                  FlavorAssignmentMode
+	LastAssignedFlavorIdx int
+	borrow                int64
 }
 
 // AssignFlavors assigns flavors for each of the resources requested in each pod set.
 // The result for each pod set is accompanied with reasons why the flavor can't
 // be assigned immediately. Each assigned flavor is accompanied with a
 // FlavorAssignmentMode.
 func AssignFlavors(log logr.Logger, wl *workload.Info, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, counts []int32) Assignment {
+	if wl.LastAssignment != nil {
+		if cq.Generation > wl.LastAssignment.ClusterQueueGeneration &&
+			(cq.Cohort == nil || cq.Cohort.Generation > wl.LastAssignment.CohortGeneration) {
+			wl.LastAssignment = nil
+		}
+	}
+
 	if len(counts) == 0 {
-		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastAssignment)
 	}
 
 	currentResources := make([]workload.PodSetResources, len(wl.TotalRequests))
 	for i := range wl.TotalRequests {
 		currentResources[i] = *wl.TotalRequests[i].ScaledTo(counts[i])
 	}
-	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastAssignment)
 }
 
-func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue) Assignment {
-	assignment := Assignment{
-		TotalBorrow: make(cache.FlavorResourceQuantities),
-		PodSets:     make([]PodSetAssignment, 0, len(requests)),
-		Usage:       make(cache.FlavorResourceQuantities),
+func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, lastAssignment *workload.AssigmentClusterQueueState) Assignment {
+	var assignment Assignment
+	if lastAssignment != nil {
+		assignment = Assignment{
+			TotalBorrow: make(workload.FlavorResourceQuantities),
+			PodSets:     make([]PodSetAssignment, 0, len(requests)),
+			LastState:   *lastAssignment,
+			Usage:       make(workload.FlavorResourceQuantities),
+		}
+	} else {
+		assignment = Assignment{
+			TotalBorrow: make(workload.FlavorResourceQuantities),
+			PodSets:     make([]PodSetAssignment, 0, len(requests)),
+			LastState: workload.AssigmentClusterQueueState{
+				LastAssignedFlavorIdx:  make([]map[corev1.ResourceName]int, 0),
+				CohortGeneration:       0,
+				ClusterQueueGeneration: cq.Generation,
+			},
+			Usage: make(workload.FlavorResourceQuantities),
+		}
+		if cq.Cohort != nil {
+			assignment.LastState.CohortGeneration = cq.Cohort.Generation
+		}
 	}
```

  why not keep the code as is, and add
  ```golang
  if lastAssignment != nil {
    assigment.LastState = ...
  } else {
    assignment.LastState = ...
  }
  ```
  
  it's less repeated code and more clear what is the actual difference.

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-06T06:05:23Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:237** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-06T06:05:22Z
```diff
@@ -218,33 +221,60 @@ func (m FlavorAssignmentMode) String() string {
 }
 
 type FlavorAssignment struct {
-	Name   kueue.ResourceFlavorReference
-	Mode   FlavorAssignmentMode
-	borrow int64
+	Name                  kueue.ResourceFlavorReference
+	Mode                  FlavorAssignmentMode
+	LastAssignedFlavorIdx int
+	borrow                int64
 }
 
 // AssignFlavors assigns flavors for each of the resources requested in each pod set.
 // The result for each pod set is accompanied with reasons why the flavor can't
 // be assigned immediately. Each assigned flavor is accompanied with a
 // FlavorAssignmentMode.
 func AssignFlavors(log logr.Logger, wl *workload.Info, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, counts []int32) Assignment {
+	if wl.LastAssignment != nil {
+		if cq.Generation > wl.LastAssignment.ClusterQueueGeneration &&
+			(cq.Cohort == nil || cq.Cohort.Generation > wl.LastAssignment.CohortGeneration) {
```

  Sure. I will add a unit test for this.

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-06T06:18:41Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:271** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-06T06:18:41Z
```diff
@@ -218,33 +221,60 @@ func (m FlavorAssignmentMode) String() string {
 }
 
 type FlavorAssignment struct {
-	Name   kueue.ResourceFlavorReference
-	Mode   FlavorAssignmentMode
-	borrow int64
+	Name                  kueue.ResourceFlavorReference
+	Mode                  FlavorAssignmentMode
+	LastAssignedFlavorIdx int
+	borrow                int64
 }
 
 // AssignFlavors assigns flavors for each of the resources requested in each pod set.
 // The result for each pod set is accompanied with reasons why the flavor can't
 // be assigned immediately. Each assigned flavor is accompanied with a
 // FlavorAssignmentMode.
 func AssignFlavors(log logr.Logger, wl *workload.Info, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, counts []int32) Assignment {
+	if wl.LastAssignment != nil {
+		if cq.Generation > wl.LastAssignment.ClusterQueueGeneration &&
+			(cq.Cohort == nil || cq.Cohort.Generation > wl.LastAssignment.CohortGeneration) {
+			wl.LastAssignment = nil
+		}
+	}
+
 	if len(counts) == 0 {
-		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastAssignment)
 	}
 
 	currentResources := make([]workload.PodSetResources, len(wl.TotalRequests))
 	for i := range wl.TotalRequests {
 		currentResources[i] = *wl.TotalRequests[i].ScaledTo(counts[i])
 	}
-	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastAssignment)
 }
 
-func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue) Assignment {
-	assignment := Assignment{
-		TotalBorrow: make(cache.FlavorResourceQuantities),
-		PodSets:     make([]PodSetAssignment, 0, len(requests)),
-		Usage:       make(cache.FlavorResourceQuantities),
+func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, lastAssignment *workload.AssigmentClusterQueueState) Assignment {
+	var assignment Assignment
+	if lastAssignment != nil {
+		assignment = Assignment{
+			TotalBorrow: make(workload.FlavorResourceQuantities),
+			PodSets:     make([]PodSetAssignment, 0, len(requests)),
+			LastState:   *lastAssignment,
+			Usage:       make(workload.FlavorResourceQuantities),
+		}
+	} else {
+		assignment = Assignment{
+			TotalBorrow: make(workload.FlavorResourceQuantities),
+			PodSets:     make([]PodSetAssignment, 0, len(requests)),
+			LastState: workload.AssigmentClusterQueueState{
+				LastAssignedFlavorIdx:  make([]map[corev1.ResourceName]int, 0),
+				CohortGeneration:       0,
+				ClusterQueueGeneration: cq.Generation,
+			},
+			Usage: make(workload.FlavorResourceQuantities),
+		}
+		if cq.Cohort != nil {
+			assignment.LastState.CohortGeneration = cq.Cohort.Generation
+		}
 	}
```

  Fixed

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-12T10:59:25Z

- **pkg/cache/clusterqueue.go:55** by [@trasc](https://github.com/trasc) — 2023-09-12T09:35:32Z
```diff
@@ -47,8 +50,9 @@ type Cohort struct {
 	Members sets.Set[*ClusterQueue]
 
 	// These fields are only populated for a snapshot.
-	RequestableResources FlavorResourceQuantities
-	Usage                FlavorResourceQuantities
+	RequestableResources workload.FlavorResourceQuantities
+	Usage                workload.FlavorResourceQuantities
+	Generation           int64
```

  +1, this is not snapshot speciffic

- **pkg/cache/clusterqueue.go:78** by [@trasc](https://github.com/trasc) — 2023-09-12T09:38:10Z
```diff
@@ -71,12 +75,12 @@ type ResourceQuota struct {
 	BorrowingLimit *int64
 }
 
-type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
+// type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
```

  +1

- **pkg/cache/clusterqueue.go:210** by [@trasc](https://github.com/trasc) — 2023-09-12T09:38:45Z
```diff
@@ -189,6 +207,7 @@ func (c *ClusterQueue) updateResourceGroups(in []kueue.ResourceGroup) {
 			rg.Flavors = append(rg.Flavors, fQuotas)
 		}
 	}
+	atomic.AddInt64(&c.Generation, 1)
```

  +1

- **pkg/cache/clusterqueue.go:303** by [@trasc](https://github.com/trasc) — 2023-09-12T10:40:13Z
```diff
@@ -281,9 +300,12 @@ func (c *ClusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
 		updateUsage(wi, c.localQueues[qKey].usage, m)
 		c.localQueues[qKey].admittedWorkloads += int(m)
 	}
+	if m < 0 {
```

  I think that the idea is change the generation only when a workload is removed, therefore well have better chances to fit in a previously checked flavor. If this is the case maybe the increment should be moved in `func (c *ClusterQueue) deleteWorkload(w *kueue.Workload) {` , and rename `Generation`  to something that describes this.

- **pkg/cache/snapshot.go:41** by [@trasc](https://github.com/trasc) — 2023-09-12T10:42:02Z
```diff
@@ -36,8 +38,10 @@ func (s *Snapshot) RemoveWorkload(wl *workload.Info) {
 	cq := s.ClusterQueues[wl.ClusterQueue]
 	delete(cq.Workloads, workload.Key(wl.Obj))
 	updateUsage(wl, cq.Usage, -1)
+	atomic.AddInt64(&cq.Generation, 1)
```

  +1 on not changing the generation in the snapshot

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1926** by [@trasc](https://github.com/trasc) — 2023-09-12T10:52:58Z
```diff
@@ -1585,16 +1805,81 @@ func TestAssignFlavors(t *testing.T) {
 					ReclaimablePods: tc.wlReclaimablePods,
 				},
 			})
+			if tc.clusterQueue.FlavorFungibility.WhenCanBorrow == "" {
+				tc.clusterQueue.FlavorFungibility.WhenCanBorrow = kueue.Borrow
+			}
+			if tc.clusterQueue.FlavorFungibility.WhenCanPreempt == "" {
+				tc.clusterQueue.FlavorFungibility.WhenCanPreempt = kueue.TryNextFlavor
+			}
 			tc.clusterQueue.UpdateWithFlavors(resourceFlavors)
 			tc.clusterQueue.UpdateRGByResource()
 			assignment := AssignFlavors(log, wlInfo, resourceFlavors, &tc.clusterQueue, nil)
 			if repMode := assignment.RepresentativeMode(); repMode != tc.wantRepMode {
 				t.Errorf("e.assignFlavors(_).RepresentativeMode()=%s, want %s", repMode, tc.wantRepMode)
 			}
 
-			if diff := cmp.Diff(tc.wantAssignment, assignment, cmpopts.IgnoreUnexported(Assignment{}, FlavorAssignment{})); diff != "" {
+			// assignment.LastState = workload.AssigmentClusterQueueState{
+			// 	LastAssignedFlavorIdx: nil,
+			// 	ResourceFlavors:        nil,
+			// 	ClusterQueueUsage:      nil,
+			// 	CohortUsage:            nil,
+			// }
+			if diff := cmp.Diff(tc.wantAssignment, assignment, cmpopts.IgnoreUnexported(Assignment{}, FlavorAssignment{}), cmpopts.IgnoreFields(Assignment{}, "LastState"), cmpopts.IgnoreFields(FlavorAssignment{}, "LastAssignedFlavorIdx")); diff != "" {
 				t.Errorf("Unexpected assignment (-want,+got):\n%s", diff)
 			}
 		})
 	}
 }
+
+func TestLastAssignmentOutdated(t *testing.T) {
+	type args struct {
+		wl *workload.Info
+		cq *cache.ClusterQueue
+	}
+	tests := []struct {
+		name string
+		args args
+		want bool
+	}{
+		{
+			name: "Cluster queue generation increased",
+			args: args{
+				wl: &workload.Info{
+					LastAssignment: &workload.AssigmentClusterQueueState{
+						ClusterQueueGeneration: 0,
+					},
+				},
+				cq: &cache.ClusterQueue{
+					Cohort:     nil,
+					Generation: 1,
+				},
+			},
+			want: true,
+		},
+		{
+			name: "Cohort generation increased",
+			args: args{
+				wl: &workload.Info{
+					LastAssignment: &workload.AssigmentClusterQueueState{
+						ClusterQueueGeneration: 0,
+						CohortGeneration:       0,
+					},
+				},
+				cq: &cache.ClusterQueue{
+					Cohort: &cache.Cohort{
+						Generation: 1,
+					},
+					Generation: 0,
+				},
+			},
+			want: true,
+		},
```

  add a `want=false` test

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1826** by [@trasc](https://github.com/trasc) — 2023-09-12T10:54:25Z
```diff
@@ -1585,16 +1805,81 @@ func TestAssignFlavors(t *testing.T) {
 					ReclaimablePods: tc.wlReclaimablePods,
 				},
 			})
+			if tc.clusterQueue.FlavorFungibility.WhenCanBorrow == "" {
+				tc.clusterQueue.FlavorFungibility.WhenCanBorrow = kueue.Borrow
+			}
+			if tc.clusterQueue.FlavorFungibility.WhenCanPreempt == "" {
+				tc.clusterQueue.FlavorFungibility.WhenCanPreempt = kueue.TryNextFlavor
+			}
 			tc.clusterQueue.UpdateWithFlavors(resourceFlavors)
 			tc.clusterQueue.UpdateRGByResource()
 			assignment := AssignFlavors(log, wlInfo, resourceFlavors, &tc.clusterQueue, nil)
 			if repMode := assignment.RepresentativeMode(); repMode != tc.wantRepMode {
 				t.Errorf("e.assignFlavors(_).RepresentativeMode()=%s, want %s", repMode, tc.wantRepMode)
 			}
 
-			if diff := cmp.Diff(tc.wantAssignment, assignment, cmpopts.IgnoreUnexported(Assignment{}, FlavorAssignment{})); diff != "" {
+			// assignment.LastState = workload.AssigmentClusterQueueState{
+			// 	LastAssignedFlavorIdx: nil,
+			// 	ResourceFlavors:        nil,
+			// 	ClusterQueueUsage:      nil,
+			// 	CohortUsage:            nil,
+			// }
```

  remove this

- **pkg/scheduler/scheduler_test.go:1619** by [@trasc](https://github.com/trasc) — 2023-09-12T10:56:34Z
```diff
@@ -1263,6 +1264,361 @@ func TestEntryOrdering(t *testing.T) {
 	}
 }
 
+func TestLastSchedulingContext(t *testing.T) {
+	defer features.SetFeatureGateDuringTest(t, features.FlavorFungibility, true)()
+	resourceFlavors := []*kueue.ResourceFlavor{
+		{ObjectMeta: metav1.ObjectMeta{Name: "on-demand"}},
+		{ObjectMeta: metav1.ObjectMeta{Name: "spot"}},
+	}
+	clusterQueue := []kueue.ClusterQueue{
+		*utiltesting.MakeClusterQueue("eng-alpha").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+	}
+	clusterQueue_cohort := []kueue.ClusterQueue{
+		*utiltesting.MakeClusterQueue("eng-cohort-alpha").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+				WhenCanBorrow:  kueue.Borrow,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+		*utiltesting.MakeClusterQueue("eng-cohort-beta").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+				WhenCanBorrow:  kueue.Borrow,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+		*utiltesting.MakeClusterQueue("eng-cohort-theta").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.TryNextFlavor,
+				WhenCanBorrow:  kueue.TryNextFlavor,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+	}
+
+	queues := []kueue.LocalQueue{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-alpha",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-alpha",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-alpha",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-beta",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-beta",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-theta",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-theta",
+			},
+		},
+	}
+	wl := utiltesting.MakeWorkload("low-1", "default").
+		Request(corev1.ResourceCPU, "50").
+		ReserveQuota(utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+		Admitted(true).
+		Obj()
+	cases := []struct {
+		name                        string
+		cqs                         []kueue.ClusterQueue
+		admittedWorkloads           []kueue.Workload
+		workloads                   []kueue.Workload
+		deletedWorkloads            []kueue.Workload
+		wantPreempted               sets.Set[string]
+		wantAdmissionsOnceSchedule  map[string]kueue.Admission
+		wantAdmissionsTwiceSchedule map[string]kueue.Admission
+	}{
+		{
+			name: "not changed",
+			cqs:  clusterQueue,
+			admittedWorkloads: []kueue.Workload{
+				*wl,
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("preemptor", "default").
+					Queue("main").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads:           []kueue.Workload{},
+			wantPreempted:              sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/preemptor": *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "spot", "20").Obj(),
+				"default/low-1":     *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+			},
+		},
+		{
+			name: "changed",
+			cqs:  clusterQueue,
+			admittedWorkloads: []kueue.Workload{
+				*wl,
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("preemptor", "default").
+					Queue("main").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads: []kueue.Workload{
+				*wl,
+			},
+			wantPreempted:              sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/preemptor": *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+		},
+		{
+			name: "borrow before next flavor",
+			cqs:  clusterQueue_cohort,
+			admittedWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("placeholder", "default").
+					Request(corev1.ResourceCPU, "50").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("borrower", "default").
+					Queue("main-alpha").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+				*utiltesting.MakeWorkload("workload1", "default").
+					Queue("main-beta").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads: []kueue.Workload{},
+			wantPreempted:    sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{
+				"default/workload1": *utiltesting.MakeAdmission("eng-cohort-beta").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+				"default/borrower":  *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/placeholder": *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+				"default/workload1":   *utiltesting.MakeAdmission("eng-cohort-beta").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+				"default/borrower":    *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+		},
+		{
+			name: "borrow after all flavors",
+			cqs:  clusterQueue_cohort,
+			admittedWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("placeholder", "default").
+					Request(corev1.ResourceCPU, "50").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+					Admitted(true).
+					Obj(),
+				*utiltesting.MakeWorkload("placeholder1", "default").
+					Request(corev1.ResourceCPU, "50").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("workload", "default").
+					Queue("main-theta").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads: []kueue.Workload{},
+			wantPreempted:    sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{
+				"default/workload": *utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "spot", "20").Obj(),
+			},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/placeholder":  *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+				"default/placeholder1": *utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+				"default/workload":     *utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "spot", "20").Obj(),
+			},
+		},
+	}
+
+	for _, tc := range cases {
+		t.Run(tc.name, func(t *testing.T) {
+			ctx, _ := utiltesting.ContextWithLog(t)
+			scheme := runtime.NewScheme()
+
+			clientBuilder := utiltesting.NewClientBuilder().
+				WithLists(&kueue.WorkloadList{Items: tc.admittedWorkloads},
+					&kueue.WorkloadList{Items: tc.workloads},
+					&kueue.ClusterQueueList{Items: tc.cqs},
+					&kueue.LocalQueueList{Items: queues}).
+				WithObjects(
+					&corev1.Namespace{ObjectMeta: metav1.ObjectMeta{Name: "default"}},
+				)
+			cl := clientBuilder.Build()
+			broadcaster := record.NewBroadcaster()
+			recorder := broadcaster.NewRecorder(scheme,
+				corev1.EventSource{Component: constants.AdmissionName})
+			cqCache := cache.New(cl)
+			qManager := queue.NewManager(cl, cqCache)
+			// Workloads are loaded into queues or clusterQueues as we add them.
+			for _, q := range queues {
+				if err := qManager.AddLocalQueue(ctx, &q); err != nil {
+					t.Fatalf("Inserting queue %s/%s in manager: %v", q.Namespace, q.Name, err)
+				}
+			}
+			for i := range resourceFlavors {
+				cqCache.AddOrUpdateResourceFlavor(resourceFlavors[i])
+			}
+			for _, cq := range tc.cqs {
+				if err := cqCache.AddClusterQueue(ctx, &cq); err != nil {
+					t.Fatalf("Inserting clusterQueue %s in cache: %v", cq.Name, err)
+				}
+				if err := qManager.AddClusterQueue(ctx, &cq); err != nil {
+					t.Fatalf("Inserting clusterQueue %s in manager: %v", cq.Name, err)
+				}
+			}
+			scheduler := New(qManager, cqCache, cl, recorder)
+			gotScheduled := make(map[string]kueue.Admission)
+			var mu sync.Mutex
+			scheduler.applyAdmission = func(ctx context.Context, w *kueue.Workload) error {
+				mu.Lock()
+				gotScheduled[workload.Key(w)] = *w.Status.Admission
+				mu.Unlock()
+				return nil
+			}
+			wg := sync.WaitGroup{}
+			scheduler.setAdmissionRoutineWrapper(routine.NewWrapper(
+				func() { wg.Add(1) },
+				func() { wg.Done() },
+			))
+			gotPreempted := sets.New[string]()
+			scheduler.preemptor.OverrideApply(func(_ context.Context, w *kueue.Workload) error {
+				mu.Lock()
+				gotPreempted.Insert(workload.Key(w))
+				mu.Unlock()
+				return nil
+			})
+
+			ctx, cancel := context.WithTimeout(ctx, queueingTimeout)
+			go qManager.CleanUpOnContext(ctx)
+			defer cancel()
+
+			scheduler.schedule(ctx)
+			wg.Wait()
+
+			if diff := cmp.Diff(tc.wantPreempted, gotPreempted); diff != "" {
+				t.Errorf("Unexpected preemptions (-want,+got):\n%s", diff)
+			}
+			if diff := cmp.Diff(tc.wantAdmissionsOnceSchedule, gotScheduled); diff != "" {
+				t.Errorf("Unexpected scheduled workloads (-want,+got):\n%s", diff)
+			}
+
+			// should schedule from where preempting failed last time
+			ctx, cancel = context.WithTimeout(ctx, queueingTimeout)
+			go qManager.CleanUpOnContext(ctx)
+			defer cancel()
+
+			for _, wl := range tc.deletedWorkloads {
+				err := cl.Delete(ctx, &wl)
+				if err != nil {
+					t.Errorf("Delete workload failed: %v", err)
+				}
+				err = cqCache.DeleteWorkload(&wl)
+				if err != nil {
+					t.Errorf("Delete workload failed: %v", err)
+				}
+			}
+
+			scheduler.schedule(ctx)
+			wg.Wait()
+
+			if diff := cmp.Diff(tc.wantPreempted, gotPreempted); diff != "" {
+				t.Errorf("Unexpected preemptions (-want,+got):\n%s", diff)
+			}
+			// Verify assignments in cache.
+			gotAssignments := make(map[string]kueue.Admission)
+			snapshot := cqCache.Snapshot()
+			for cqName, c := range snapshot.ClusterQueues {
+				for name, w := range c.Workloads {
+					if !workload.IsAdmitted(w.Obj) {
+						t.Errorf("Workload %s is not admitted by a clusterQueue, but it is found as member of clusterQueue %s in the cache", name, cqName)
+					} else if string(w.Obj.Status.Admission.ClusterQueue) != cqName {
+						t.Errorf("Workload %s is admitted by clusterQueue %s, but it is found as member of clusterQueue %s in the cache", name, w.Obj.Status.Admission.ClusterQueue, cqName)
+					} else {
+						gotAssignments[name] = *w.Obj.Status.Admission
+					}
+				}
+			}
+			if len(gotAssignments) == 0 {
+				gotAssignments = nil
+			}
+			if diff := cmp.Diff(tc.wantAdmissionsTwiceSchedule, gotAssignments); diff != "" {
+				t.Errorf("Unexpected assigned clusterQueues in cache (-want,+got):\n%s", diff)
+			}
+		})
+	}
+
```

  rm

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-13T02:01:03Z

- **pkg/cache/clusterqueue.go:303** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-13T02:01:02Z
```diff
@@ -281,9 +300,12 @@ func (c *ClusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
 		updateUsage(wi, c.localQueues[qKey].usage, m)
 		c.localQueues[qKey].admittedWorkloads += int(m)
 	}
+	if m < 0 {
```

  yes, thanks for explain. I will update the field name later

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-13T03:07:32Z

- **pkg/cache/snapshot_test.go:40** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-13T03:07:32Z
```diff
@@ -28,19 +28,21 @@ import (
 	"k8s.io/apimachinery/pkg/util/sets"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/features"
 	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
 var snapCmpOpts = []cmp.Option{
 	cmpopts.EquateEmpty(),
 	cmpopts.IgnoreUnexported(ClusterQueue{}),
-	cmpopts.IgnoreFields(ClusterQueue{}, "RGByResource"),
-	cmpopts.IgnoreFields(Cohort{}, "Members"), // avoid recursion.
+	cmpopts.IgnoreFields(ClusterQueue{}, "RGByResource", "Generation"),
+	cmpopts.IgnoreFields(Cohort{}, "Members", "Generation"), // avoid recursion.
```

  we will only add this in snapshot cmpopts for preemption. this field will not be changed because we will not modified this field in snapshot.
  we have add test to ensure this field not change in snapshot test

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-13T19:07:53Z

- **pkg/cache/clusterqueue.go:335** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-13T17:57:38Z
```diff
@@ -316,6 +332,7 @@ func (c *ClusterQueue) deleteWorkload(w *kueue.Workload) {
 	if c.podsReadyTracking && !apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadPodsReady) {
 		c.WorkloadsNotReady.Delete(k)
 	}
+	c.AllocatableResourceIncreasedGen++
```

  why is this only called on `deleteWorkload`?

- **pkg/cache/clusterqueue.go:31** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-13T18:01:26Z
```diff
@@ -23,16 +23,18 @@ var (
 // ClusterQueue is the internal implementation of kueue.ClusterQueue that
 // holds admitted workloads.
 type ClusterQueue struct {
-	Name              string
-	Cohort            *Cohort
-	ResourceGroups    []ResourceGroup
-	RGByResource      map[corev1.ResourceName]*ResourceGroup
-	Usage             FlavorResourceQuantities
-	Workloads         map[string]*workload.Info
-	WorkloadsNotReady sets.Set[string]
-	NamespaceSelector labels.Selector
-	Preemption        kueue.ClusterQueuePreemption
-	Status            metrics.ClusterQueueStatus
+	Name                            string
+	Cohort                          *Cohort
+	ResourceGroups                  []ResourceGroup
+	RGByResource                    map[corev1.ResourceName]*ResourceGroup
+	Usage                           workload.FlavorResourceQuantities
+	AllocatableResourceIncreasedGen int64
```

  not sure if this was suggested by someone else, but I would simplify to: `AllocatableResourceGeneration`.
  
  and add a comment that explain what this is or when it changes, as it might not be immediately obvious from the name.

- **pkg/scheduler/flavorassigner/flavorassigner.go:230** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-13T18:05:20Z
```diff
@@ -218,33 +221,56 @@ func (m FlavorAssignmentMode) String() string {
 }
 
 type FlavorAssignment struct {
-	Name   kueue.ResourceFlavorReference
-	Mode   FlavorAssignmentMode
-	borrow int64
+	Name                  kueue.ResourceFlavorReference
+	Mode                  FlavorAssignmentMode
+	LastAssignedFlavorIdx int
+	borrow                int64
+}
+
+func LastAssignmentOutdated(wl *workload.Info, cq *cache.ClusterQueue) bool {
```

  ```suggestion
  func lastAssignmentOutdated(wl *workload.Info, cq *cache.ClusterQueue) bool {
  ```

- **pkg/scheduler/flavorassigner/flavorassigner.go:256** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-13T18:07:18Z
```diff
@@ -218,33 +221,56 @@ func (m FlavorAssignmentMode) String() string {
 }
 
 type FlavorAssignment struct {
-	Name   kueue.ResourceFlavorReference
-	Mode   FlavorAssignmentMode
-	borrow int64
+	Name                  kueue.ResourceFlavorReference
+	Mode                  FlavorAssignmentMode
+	LastAssignedFlavorIdx int
+	borrow                int64
+}
+
+func LastAssignmentOutdated(wl *workload.Info, cq *cache.ClusterQueue) bool {
+	return cq.AllocatableResourceIncreasedGen > wl.LastAssignment.ClusterQueueGeneration ||
+		(cq.Cohort != nil && cq.Cohort.AllocatableResourceIncreasedGen > wl.LastAssignment.CohortGeneration)
 }
 
 // AssignFlavors assigns flavors for each of the resources requested in each pod set.
 // The result for each pod set is accompanied with reasons why the flavor can't
 // be assigned immediately. Each assigned flavor is accompanied with a
 // FlavorAssignmentMode.
 func AssignFlavors(log logr.Logger, wl *workload.Info, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, counts []int32) Assignment {
+	if wl.LastAssignment != nil && LastAssignmentOutdated(wl, cq) {
+		wl.LastAssignment = nil
+	}
+
 	if len(counts) == 0 {
-		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastAssignment)
 	}
 
 	currentResources := make([]workload.PodSetResources, len(wl.TotalRequests))
 	for i := range wl.TotalRequests {
 		currentResources[i] = *wl.TotalRequests[i].ScaledTo(counts[i])
 	}
-	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastAssignment)
 }
 
-func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue) Assignment {
-	assignment := Assignment{
-		TotalBorrow: make(cache.FlavorResourceQuantities),
+func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, lastAssignment *workload.AssigmentClusterQueueState) Assignment {
+	var assignment Assignment = Assignment{
```

  why change this line?

- **pkg/scheduler/flavorassigner/flavorassigner.go:300** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-13T18:10:27Z
```diff
@@ -271,7 +297,14 @@ func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets
 				}
 				break
 			}
-			flavors, status := assignment.findFlavorForResourceGroup(log, rg, podSet.Requests, resourceFlavors, cq, &podSets[i].Template.Spec)
+			var lastFlavorAssignment int = -1
```

  ```suggestion
  			lastFlavorAssignment := -1
  ```

- **pkg/scheduler/flavorassigner/flavorassigner.go:265** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-13T18:12:46Z
```diff
@@ -218,33 +221,56 @@ func (m FlavorAssignmentMode) String() string {
 }
 
 type FlavorAssignment struct {
-	Name   kueue.ResourceFlavorReference
-	Mode   FlavorAssignmentMode
-	borrow int64
+	Name                  kueue.ResourceFlavorReference
+	Mode                  FlavorAssignmentMode
+	LastAssignedFlavorIdx int
+	borrow                int64
+}
+
+func LastAssignmentOutdated(wl *workload.Info, cq *cache.ClusterQueue) bool {
+	return cq.AllocatableResourceIncreasedGen > wl.LastAssignment.ClusterQueueGeneration ||
+		(cq.Cohort != nil && cq.Cohort.AllocatableResourceIncreasedGen > wl.LastAssignment.CohortGeneration)
 }
 
 // AssignFlavors assigns flavors for each of the resources requested in each pod set.
 // The result for each pod set is accompanied with reasons why the flavor can't
 // be assigned immediately. Each assigned flavor is accompanied with a
 // FlavorAssignmentMode.
 func AssignFlavors(log logr.Logger, wl *workload.Info, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, counts []int32) Assignment {
+	if wl.LastAssignment != nil && LastAssignmentOutdated(wl, cq) {
+		wl.LastAssignment = nil
+	}
+
 	if len(counts) == 0 {
-		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastAssignment)
 	}
 
 	currentResources := make([]workload.PodSetResources, len(wl.TotalRequests))
 	for i := range wl.TotalRequests {
 		currentResources[i] = *wl.TotalRequests[i].ScaledTo(counts[i])
 	}
-	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastAssignment)
 }
 
-func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue) Assignment {
-	assignment := Assignment{
-		TotalBorrow: make(cache.FlavorResourceQuantities),
+func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, lastAssignment *workload.AssigmentClusterQueueState) Assignment {
+	var assignment Assignment = Assignment{
+		TotalBorrow: make(workload.FlavorResourceQuantities),
 		PodSets:     make([]PodSetAssignment, 0, len(requests)),
-		Usage:       make(cache.FlavorResourceQuantities),
+		Usage:       make(workload.FlavorResourceQuantities),
+	}
+	if lastAssignment != nil {
+		assignment.LastState = *lastAssignment
+	} else {
+		assignment.LastState = workload.AssigmentClusterQueueState{
+			LastAssignedFlavorIdx:  make([]map[corev1.ResourceName]int, 0),
```

  ```suggestion
  			LastAssignedFlavorIdx:  make([]map[corev1.ResourceName]int, 0, len(podSets)),
  ```

- **pkg/scheduler/flavorassigner/flavorassigner.go:341** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-13T18:17:47Z
```diff
@@ -305,6 +338,7 @@ func (psa *PodSetAssignment) append(flavors ResourceAssignment, status *Status)
 }
 
 func (a *Assignment) append(requests workload.Requests, psAssignment *PodSetAssignment) {
+	var flavorIdx = make(map[corev1.ResourceName]int, len(psAssignment.Flavors))
```

  ```suggestion
  	flavorIdx := make(map[corev1.ResourceName]int, len(psAssignment.Flavors))
  ```

- **pkg/scheduler/flavorassigner/flavorassigner.go:226** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-13T18:23:06Z
```diff
@@ -218,33 +221,56 @@ func (m FlavorAssignmentMode) String() string {
 }
 
 type FlavorAssignment struct {
-	Name   kueue.ResourceFlavorReference
-	Mode   FlavorAssignmentMode
-	borrow int64
+	Name                  kueue.ResourceFlavorReference
+	Mode                  FlavorAssignmentMode
+	LastAssignedFlavorIdx int
```

  ```suggestion
  	FlavorIdx int
  ```

- **pkg/scheduler/flavorassigner/flavorassigner.go:462** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-13T18:39:54Z
```diff
@@ -389,18 +433,62 @@ func (a *Assignment) findFlavorForResourceGroup(
 			}
 		}
 
-		if representativeMode > bestAssignmentMode {
-			bestAssignment = assignments
-			bestAssignmentMode = representativeMode
-			if bestAssignmentMode == Fit {
-				// All the resources fit in the cohort, no need to check more flavors.
-				return bestAssignment, nil
+		if features.Enabled(features.FlavorFungibility) {
+			if !shouldTryNextFlavor(representativeMode, cq.FlavorFungibility, needsBorrowing) {
+				bestAssignment = assignments
+				bestAssignmentMode = representativeMode
+				break
+			}
+			if representativeMode > bestAssignmentMode {
+				bestAssignment = assignments
+				bestAssignmentMode = representativeMode
+			}
+		} else {
+			if representativeMode > bestAssignmentMode {
+				bestAssignment = assignments
+				bestAssignmentMode = representativeMode
+				if bestAssignmentMode == Fit {
+					// All the resources fit in the cohort, no need to check more flavors.
+					return bestAssignment, nil
+				}
 			}
 		}
 	}
+
+	for _, assignment := range bestAssignment {
+		// When the workload needborrowing and there is another workload in cohort doesn't
+		// need borrowing, the workload needborrowing will come again. In this case we should
+		// not skip the previous flavors.
+		if flavorIdx == len(rg.Flavors)-1 || assignment.Mode == Fit {
```

  It doesn't look like the comment matches the check. Can you explain in other words?
  
  Going by what the comment says: should we set workload.LastAssigment to nil when a workload is skipped in scheduler.go?

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1675** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-13T18:44:47Z
```diff
@@ -1571,6 +1573,224 @@ func TestAssignFlavors(t *testing.T) {
 			},
 			wantRepMode: Fit,
 		},
+		"preempt before try next flavor": {
+			wlPods: []kueue.PodSet{
+				*utiltesting.MakePodSet("main", 1).
+					Request(corev1.ResourceCPU, "9").
+					Obj(),
+			},
+			clusterQueue: cache.ClusterQueue{
+				FlavorFungibility: kueue.FlavorFungibility{
+					WhenCanBorrow:  kueue.Borrow,
+					WhenCanPreempt: kueue.Preempt,
+				},
+				ResourceGroups: []cache.ResourceGroup{{
+					CoveredResources: sets.New(corev1.ResourceCPU, corev1.ResourcePods),
+					Flavors: []cache.FlavorQuotas{{
+						Name: "one",
+						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
+							corev1.ResourcePods: {Nominal: 10},
+							corev1.ResourceCPU:  {Nominal: 10000},
+						},
+					}, {
+						Name: "two",
+						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
+							corev1.ResourcePods: {Nominal: 10},
+							corev1.ResourceCPU:  {Nominal: 10000},
+						},
+					}},
+				}},
+				Usage: workload.FlavorResourceQuantities{
+					"one": {corev1.ResourceCPU: 2000},
+				},
+			},
+			wantRepMode: Preempt,
+			wantAssignment: Assignment{
+				PodSets: []PodSetAssignment{{
+					Name: "main",
+					Flavors: ResourceAssignment{
+						corev1.ResourceCPU:  {Name: "one", Mode: Preempt},
+						corev1.ResourcePods: {Name: "one", Mode: Fit},
+					},
+					Requests: corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("9000m"),
+						corev1.ResourcePods: resource.MustParse("1"),
+					},
+					Status: &Status{
+						reasons: []string{"insufficient unused quota for cpu in flavor one, 1 more needed"},
+					},
+					Count: 1,
+				}},
+				Usage: workload.FlavorResourceQuantities{"one": {"cpu": 9000, "pods": 1}},
+			},
+		},
+		"preempt try next flavor": {
+			wlPods: []kueue.PodSet{
+				*utiltesting.MakePodSet("main", 1).
+					Request(corev1.ResourceCPU, "9").
+					Obj(),
+			},
+			clusterQueue: cache.ClusterQueue{
+				FlavorFungibility: kueue.FlavorFungibility{
+					WhenCanBorrow:  kueue.Borrow,
+					WhenCanPreempt: kueue.TryNextFlavor,
+				},
+				ResourceGroups: []cache.ResourceGroup{{
+					CoveredResources: sets.New(corev1.ResourceCPU, corev1.ResourcePods),
+					Flavors: []cache.FlavorQuotas{{
+						Name: "one",
+						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
+							corev1.ResourcePods: {Nominal: 10},
+							corev1.ResourceCPU:  {Nominal: 10000},
+						},
+					}, {
+						Name: "two",
+						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
+							corev1.ResourcePods: {Nominal: 10},
+							corev1.ResourceCPU:  {Nominal: 10000},
+						},
+					}},
+				}},
+				Usage: workload.FlavorResourceQuantities{
+					"one": {corev1.ResourceCPU: 2000},
+				},
+			},
+			wantRepMode: Fit,
+			wantAssignment: Assignment{
+				PodSets: []PodSetAssignment{{
+					Name: "main",
+					Flavors: ResourceAssignment{
+						corev1.ResourceCPU:  {Name: "two", Mode: Fit},
+						corev1.ResourcePods: {Name: "two", Mode: Fit},
+					},
+					Requests: corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("9000m"),
+						corev1.ResourcePods: resource.MustParse("1"),
+					},
+					Count: 1,
+				}},
+				Usage: workload.FlavorResourceQuantities{"two": {"cpu": 9000, "pods": 1}},
+			},
+		},
+		"borrow try next flavor": {
```

  there should be another test case where we try the next flavor, but it doesn't fit, so we end up borrowing in the first flavor.

- **pkg/util/testing/wrappers.go:366** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-13T18:48:38Z
```diff
@@ -318,6 +318,14 @@ func MakeClusterQueue(name string) *ClusterQueueWrapper {
 		Spec: kueue.ClusterQueueSpec{
 			NamespaceSelector: &metav1.LabelSelector{},
 			QueueingStrategy:  kueue.BestEffortFIFO,
+			Preemption: &kueue.ClusterQueuePreemption{
```

  Please address this

- **pkg/workload/workload.go:43** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-13T18:49:59Z
```diff
@@ -39,6 +40,40 @@ var (
 	admissionManagedConditions = []string{kueue.WorkloadAdmitted, kueue.WorkloadEvicted}
 )
 
+type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
```

  It looks like this move is no longer needed after you switch to the implementation based on Generation? Can you revert?

- **test/integration/scheduler/scheduler_test.go:1123** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-13T19:00:10Z
```diff
@@ -1075,6 +1077,87 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.FinishWorkloads(ctx, k8sClient, pWl3)
 			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, devCQ.Name, dWl1)
 		})
+
+		ginkgo.It("Should try next flavor instead of borrowing", func() {
+			prodCQ = testing.MakeClusterQueue("prod-cq").
+				Cohort("all").
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodCQ)).Should(gomega.Succeed())
+
+			devCQ = testing.MakeClusterQueue("dev-cq").
+				Cohort("all").
+				FlavorFungibility(kueue.FlavorFungibility{WhenCanBorrow: kueue.TryNextFlavor}).
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj(), *testing.MakeFlavorQuotas("spot-tainted").Resource(corev1.ResourceCPU, "11").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, devCQ)).Should(gomega.Succeed())
+
+			prodQueue := testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodQueue)).Should(gomega.Succeed())
+
+			podQueue := testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, podQueue)).Should(gomega.Succeed())
+
+			ginkgo.By("Creating one workload")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "9").Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+			prodWl1Admission := testing.MakeAdmission(prodCQ.Name).Assignment(corev1.ResourceCPU, "on-demand", "9").Obj()
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, wl1, prodWl1Admission)
+			util.ExpectPendingWorkloadsMetric(prodCQ, 0, 0)
+			util.ExpectAdmittedActiveWorkloadsMetric(prodCQ, 1)
+			util.ExpectAdmittedWorkloadsTotalMetric(prodCQ, 1)
+
+			ginkgo.By("Creating another workload")
+			wl2 := testing.MakeWorkload("wl-2", ns.Name).Queue(podQueue.Name).Request(corev1.ResourceCPU, "11").Toleration(spotToleration).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+			prodWl2Admission := testing.MakeAdmission(devCQ.Name).Assignment(corev1.ResourceCPU, "spot-tainted", "11").Obj()
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, wl2, prodWl2Admission)
+			util.ExpectPendingWorkloadsMetric(devCQ, 0, 0)
+			util.ExpectAdmittedActiveWorkloadsMetric(devCQ, 1)
+			util.ExpectAdmittedWorkloadsTotalMetric(devCQ, 1)
+		})
+
+		ginkgo.It("Should preempt before try next flavor", func() {
+			prodCQ = testing.MakeClusterQueue("prod-cq").
+				Cohort("all").
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj(), *testing.MakeFlavorQuotas("spot-tainted").Resource(corev1.ResourceCPU, "11").Obj()).
```

  ```suggestion
  				ResourceGroup(
  					*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj(),
  					*testing.MakeFlavorQuotas("spot-tainted").Resource(corev1.ResourceCPU, "11").Obj()).
  ```
  
  But maybe we can simplify and use spot-untainted? So that we don't need the toleration.

- **test/integration/scheduler/scheduler_test.go:1144** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-13T19:00:46Z
```diff
@@ -1075,6 +1077,87 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.FinishWorkloads(ctx, k8sClient, pWl3)
 			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, devCQ.Name, dWl1)
 		})
+
+		ginkgo.It("Should try next flavor instead of borrowing", func() {
+			prodCQ = testing.MakeClusterQueue("prod-cq").
+				Cohort("all").
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodCQ)).Should(gomega.Succeed())
+
+			devCQ = testing.MakeClusterQueue("dev-cq").
+				Cohort("all").
+				FlavorFungibility(kueue.FlavorFungibility{WhenCanBorrow: kueue.TryNextFlavor}).
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj(), *testing.MakeFlavorQuotas("spot-tainted").Resource(corev1.ResourceCPU, "11").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, devCQ)).Should(gomega.Succeed())
+
+			prodQueue := testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodQueue)).Should(gomega.Succeed())
+
+			podQueue := testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, podQueue)).Should(gomega.Succeed())
+
+			ginkgo.By("Creating one workload")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "9").Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+			prodWl1Admission := testing.MakeAdmission(prodCQ.Name).Assignment(corev1.ResourceCPU, "on-demand", "9").Obj()
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, wl1, prodWl1Admission)
+			util.ExpectPendingWorkloadsMetric(prodCQ, 0, 0)
+			util.ExpectAdmittedActiveWorkloadsMetric(prodCQ, 1)
+			util.ExpectAdmittedWorkloadsTotalMetric(prodCQ, 1)
+
+			ginkgo.By("Creating another workload")
+			wl2 := testing.MakeWorkload("wl-2", ns.Name).Queue(podQueue.Name).Request(corev1.ResourceCPU, "11").Toleration(spotToleration).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+			prodWl2Admission := testing.MakeAdmission(devCQ.Name).Assignment(corev1.ResourceCPU, "spot-tainted", "11").Obj()
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, wl2, prodWl2Admission)
+			util.ExpectPendingWorkloadsMetric(devCQ, 0, 0)
+			util.ExpectAdmittedActiveWorkloadsMetric(devCQ, 1)
+			util.ExpectAdmittedWorkloadsTotalMetric(devCQ, 1)
+		})
+
+		ginkgo.It("Should preempt before try next flavor", func() {
+			prodCQ = testing.MakeClusterQueue("prod-cq").
+				Cohort("all").
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj(), *testing.MakeFlavorQuotas("spot-tainted").Resource(corev1.ResourceCPU, "11").Obj()).
+				Preemption(kueue.ClusterQueuePreemption{
+					ReclaimWithinCohort: kueue.PreemptionPolicyAny,
+				}).
+				FlavorFungibility(kueue.FlavorFungibility{WhenCanPreempt: kueue.Preempt}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodCQ)).Should(gomega.Succeed())
+
+			devCQ = testing.MakeClusterQueue("dev-cq").
+				Cohort("all").
+				FlavorFungibility(kueue.FlavorFungibility{WhenCanBorrow: kueue.TryNextFlavor}).
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, devCQ)).Should(gomega.Succeed())
+
+			prodQueue := testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodQueue)).Should(gomega.Succeed())
+
+			podQueue := testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, podQueue)).Should(gomega.Succeed())
+
+			ginkgo.By("Creating one workload")
```

  there are more than one :)

- **test/integration/scheduler/scheduler_test.go:1162** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-13T19:02:47Z
```diff
@@ -1075,6 +1077,87 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.FinishWorkloads(ctx, k8sClient, pWl3)
 			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, devCQ.Name, dWl1)
 		})
+
+		ginkgo.It("Should try next flavor instead of borrowing", func() {
+			prodCQ = testing.MakeClusterQueue("prod-cq").
+				Cohort("all").
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodCQ)).Should(gomega.Succeed())
+
+			devCQ = testing.MakeClusterQueue("dev-cq").
+				Cohort("all").
+				FlavorFungibility(kueue.FlavorFungibility{WhenCanBorrow: kueue.TryNextFlavor}).
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj(), *testing.MakeFlavorQuotas("spot-tainted").Resource(corev1.ResourceCPU, "11").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, devCQ)).Should(gomega.Succeed())
+
+			prodQueue := testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodQueue)).Should(gomega.Succeed())
+
+			podQueue := testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, podQueue)).Should(gomega.Succeed())
+
+			ginkgo.By("Creating one workload")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "9").Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+			prodWl1Admission := testing.MakeAdmission(prodCQ.Name).Assignment(corev1.ResourceCPU, "on-demand", "9").Obj()
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, wl1, prodWl1Admission)
+			util.ExpectPendingWorkloadsMetric(prodCQ, 0, 0)
+			util.ExpectAdmittedActiveWorkloadsMetric(prodCQ, 1)
+			util.ExpectAdmittedWorkloadsTotalMetric(prodCQ, 1)
+
+			ginkgo.By("Creating another workload")
+			wl2 := testing.MakeWorkload("wl-2", ns.Name).Queue(podQueue.Name).Request(corev1.ResourceCPU, "11").Toleration(spotToleration).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+			prodWl2Admission := testing.MakeAdmission(devCQ.Name).Assignment(corev1.ResourceCPU, "spot-tainted", "11").Obj()
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, wl2, prodWl2Admission)
+			util.ExpectPendingWorkloadsMetric(devCQ, 0, 0)
+			util.ExpectAdmittedActiveWorkloadsMetric(devCQ, 1)
+			util.ExpectAdmittedWorkloadsTotalMetric(devCQ, 1)
+		})
+
+		ginkgo.It("Should preempt before try next flavor", func() {
+			prodCQ = testing.MakeClusterQueue("prod-cq").
+				Cohort("all").
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj(), *testing.MakeFlavorQuotas("spot-tainted").Resource(corev1.ResourceCPU, "11").Obj()).
+				Preemption(kueue.ClusterQueuePreemption{
+					ReclaimWithinCohort: kueue.PreemptionPolicyAny,
+				}).
+				FlavorFungibility(kueue.FlavorFungibility{WhenCanPreempt: kueue.Preempt}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodCQ)).Should(gomega.Succeed())
+
+			devCQ = testing.MakeClusterQueue("dev-cq").
+				Cohort("all").
+				FlavorFungibility(kueue.FlavorFungibility{WhenCanBorrow: kueue.TryNextFlavor}).
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, devCQ)).Should(gomega.Succeed())
+
+			prodQueue := testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodQueue)).Should(gomega.Succeed())
+
+			podQueue := testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, podQueue)).Should(gomega.Succeed())
+
+			ginkgo.By("Creating one workload")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).Queue(podQueue.Name).Request(corev1.ResourceCPU, "9").Obj()
+			wl2 := testing.MakeWorkload("wl-2", ns.Name).Queue(podQueue.Name).Request(corev1.ResourceCPU, "9").Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+			util.ExpectPendingWorkloadsMetric(devCQ, 0, 0)
+			util.ExpectAdmittedActiveWorkloadsMetric(devCQ, 2)
+			util.ExpectAdmittedWorkloadsTotalMetric(devCQ, 2)
+
+			ginkgo.By("Creating another workload")
+			wl3 := testing.MakeWorkload("wl-3", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Toleration(spotToleration).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+			util.ExpectWorkloadsToBePreempted(ctx, k8sClient, wl1)
```

  are there any guarantees that wl1 would be preempted, instead of wl2? Maybe you can play with priorities to give some guarantees.

- **test/integration/scheduler/scheduler_test.go:1091** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-13T19:03:42Z
```diff
@@ -1075,6 +1077,87 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.FinishWorkloads(ctx, k8sClient, pWl3)
 			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, devCQ.Name, dWl1)
 		})
+
+		ginkgo.It("Should try next flavor instead of borrowing", func() {
+			prodCQ = testing.MakeClusterQueue("prod-cq").
+				Cohort("all").
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodCQ)).Should(gomega.Succeed())
+
+			devCQ = testing.MakeClusterQueue("dev-cq").
+				Cohort("all").
+				FlavorFungibility(kueue.FlavorFungibility{WhenCanBorrow: kueue.TryNextFlavor}).
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj(), *testing.MakeFlavorQuotas("spot-tainted").Resource(corev1.ResourceCPU, "11").Obj()).
```

  ```suggestion
  				ResourceGroup(
  					*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj(),
  					*testing.MakeFlavorQuotas("spot-tainted").Resource(corev1.ResourceCPU, "11").Obj()).
  ```

- **test/integration/scheduler/scheduler_test.go:1098** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-13T19:04:49Z
```diff
@@ -1075,6 +1077,87 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.FinishWorkloads(ctx, k8sClient, pWl3)
 			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, devCQ.Name, dWl1)
 		})
+
+		ginkgo.It("Should try next flavor instead of borrowing", func() {
+			prodCQ = testing.MakeClusterQueue("prod-cq").
+				Cohort("all").
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodCQ)).Should(gomega.Succeed())
+
+			devCQ = testing.MakeClusterQueue("dev-cq").
+				Cohort("all").
+				FlavorFungibility(kueue.FlavorFungibility{WhenCanBorrow: kueue.TryNextFlavor}).
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj(), *testing.MakeFlavorQuotas("spot-tainted").Resource(corev1.ResourceCPU, "11").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, devCQ)).Should(gomega.Succeed())
+
+			prodQueue := testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodQueue)).Should(gomega.Succeed())
+
+			podQueue := testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devCQ.Name).Obj()
```

  ```suggestion
  			devQueue := testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devCQ.Name).Obj()
  ```

- **test/integration/scheduler/scheduler_test.go:1141** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-13T19:05:00Z
```diff
@@ -1075,6 +1077,87 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.FinishWorkloads(ctx, k8sClient, pWl3)
 			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, devCQ.Name, dWl1)
 		})
+
+		ginkgo.It("Should try next flavor instead of borrowing", func() {
+			prodCQ = testing.MakeClusterQueue("prod-cq").
+				Cohort("all").
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodCQ)).Should(gomega.Succeed())
+
+			devCQ = testing.MakeClusterQueue("dev-cq").
+				Cohort("all").
+				FlavorFungibility(kueue.FlavorFungibility{WhenCanBorrow: kueue.TryNextFlavor}).
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj(), *testing.MakeFlavorQuotas("spot-tainted").Resource(corev1.ResourceCPU, "11").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, devCQ)).Should(gomega.Succeed())
+
+			prodQueue := testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodQueue)).Should(gomega.Succeed())
+
+			podQueue := testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, podQueue)).Should(gomega.Succeed())
+
+			ginkgo.By("Creating one workload")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "9").Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+			prodWl1Admission := testing.MakeAdmission(prodCQ.Name).Assignment(corev1.ResourceCPU, "on-demand", "9").Obj()
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, wl1, prodWl1Admission)
+			util.ExpectPendingWorkloadsMetric(prodCQ, 0, 0)
+			util.ExpectAdmittedActiveWorkloadsMetric(prodCQ, 1)
+			util.ExpectAdmittedWorkloadsTotalMetric(prodCQ, 1)
+
+			ginkgo.By("Creating another workload")
+			wl2 := testing.MakeWorkload("wl-2", ns.Name).Queue(podQueue.Name).Request(corev1.ResourceCPU, "11").Toleration(spotToleration).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+			prodWl2Admission := testing.MakeAdmission(devCQ.Name).Assignment(corev1.ResourceCPU, "spot-tainted", "11").Obj()
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, wl2, prodWl2Admission)
+			util.ExpectPendingWorkloadsMetric(devCQ, 0, 0)
+			util.ExpectAdmittedActiveWorkloadsMetric(devCQ, 1)
+			util.ExpectAdmittedWorkloadsTotalMetric(devCQ, 1)
+		})
+
+		ginkgo.It("Should preempt before try next flavor", func() {
+			prodCQ = testing.MakeClusterQueue("prod-cq").
+				Cohort("all").
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj(), *testing.MakeFlavorQuotas("spot-tainted").Resource(corev1.ResourceCPU, "11").Obj()).
+				Preemption(kueue.ClusterQueuePreemption{
+					ReclaimWithinCohort: kueue.PreemptionPolicyAny,
+				}).
+				FlavorFungibility(kueue.FlavorFungibility{WhenCanPreempt: kueue.Preempt}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodCQ)).Should(gomega.Succeed())
+
+			devCQ = testing.MakeClusterQueue("dev-cq").
+				Cohort("all").
+				FlavorFungibility(kueue.FlavorFungibility{WhenCanBorrow: kueue.TryNextFlavor}).
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, devCQ)).Should(gomega.Succeed())
+
+			prodQueue := testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodQueue)).Should(gomega.Succeed())
+
+			podQueue := testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devCQ.Name).Obj()
```

  ```suggestion
  			devQueue := testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devCQ.Name).Obj()
  ```

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-14T01:32:33Z

- **pkg/cache/clusterqueue.go:335** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-14T01:32:33Z
```diff
@@ -316,6 +332,7 @@ func (c *ClusterQueue) deleteWorkload(w *kueue.Workload) {
 	if c.podsReadyTracking && !apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadPodsReady) {
 		c.WorkloadsNotReady.Delete(k)
 	}
+	c.AllocatableResourceIncreasedGen++
```

  I think we can only admit more workloads when some other workloads were deleted.
  Also, m == -1 only happen when `updateWorkloadUsage` was call in `deleteWorkload`.
  So I move the generation increasing to `deleteWorkload`

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-14T02:34:27Z

- **pkg/cache/clusterqueue.go:31** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-14T02:34:26Z
```diff
@@ -23,16 +23,18 @@ var (
 // ClusterQueue is the internal implementation of kueue.ClusterQueue that
 // holds admitted workloads.
 type ClusterQueue struct {
-	Name              string
-	Cohort            *Cohort
-	ResourceGroups    []ResourceGroup
-	RGByResource      map[corev1.ResourceName]*ResourceGroup
-	Usage             FlavorResourceQuantities
-	Workloads         map[string]*workload.Info
-	WorkloadsNotReady sets.Set[string]
-	NamespaceSelector labels.Selector
-	Preemption        kueue.ClusterQueuePreemption
-	Status            metrics.ClusterQueueStatus
+	Name                            string
+	Cohort                          *Cohort
+	ResourceGroups                  []ResourceGroup
+	RGByResource                    map[corev1.ResourceName]*ResourceGroup
+	Usage                           workload.FlavorResourceQuantities
+	AllocatableResourceIncreasedGen int64
```

  done

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-14T02:35:20Z

- **test/integration/scheduler/scheduler_test.go:1144** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-14T02:35:19Z
```diff
@@ -1075,6 +1077,87 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.FinishWorkloads(ctx, k8sClient, pWl3)
 			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, devCQ.Name, dWl1)
 		})
+
+		ginkgo.It("Should try next flavor instead of borrowing", func() {
+			prodCQ = testing.MakeClusterQueue("prod-cq").
+				Cohort("all").
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodCQ)).Should(gomega.Succeed())
+
+			devCQ = testing.MakeClusterQueue("dev-cq").
+				Cohort("all").
+				FlavorFungibility(kueue.FlavorFungibility{WhenCanBorrow: kueue.TryNextFlavor}).
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj(), *testing.MakeFlavorQuotas("spot-tainted").Resource(corev1.ResourceCPU, "11").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, devCQ)).Should(gomega.Succeed())
+
+			prodQueue := testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodQueue)).Should(gomega.Succeed())
+
+			podQueue := testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, podQueue)).Should(gomega.Succeed())
+
+			ginkgo.By("Creating one workload")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "9").Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+			prodWl1Admission := testing.MakeAdmission(prodCQ.Name).Assignment(corev1.ResourceCPU, "on-demand", "9").Obj()
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, wl1, prodWl1Admission)
+			util.ExpectPendingWorkloadsMetric(prodCQ, 0, 0)
+			util.ExpectAdmittedActiveWorkloadsMetric(prodCQ, 1)
+			util.ExpectAdmittedWorkloadsTotalMetric(prodCQ, 1)
+
+			ginkgo.By("Creating another workload")
+			wl2 := testing.MakeWorkload("wl-2", ns.Name).Queue(podQueue.Name).Request(corev1.ResourceCPU, "11").Toleration(spotToleration).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+			prodWl2Admission := testing.MakeAdmission(devCQ.Name).Assignment(corev1.ResourceCPU, "spot-tainted", "11").Obj()
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, wl2, prodWl2Admission)
+			util.ExpectPendingWorkloadsMetric(devCQ, 0, 0)
+			util.ExpectAdmittedActiveWorkloadsMetric(devCQ, 1)
+			util.ExpectAdmittedWorkloadsTotalMetric(devCQ, 1)
+		})
+
+		ginkgo.It("Should preempt before try next flavor", func() {
+			prodCQ = testing.MakeClusterQueue("prod-cq").
+				Cohort("all").
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj(), *testing.MakeFlavorQuotas("spot-tainted").Resource(corev1.ResourceCPU, "11").Obj()).
+				Preemption(kueue.ClusterQueuePreemption{
+					ReclaimWithinCohort: kueue.PreemptionPolicyAny,
+				}).
+				FlavorFungibility(kueue.FlavorFungibility{WhenCanPreempt: kueue.Preempt}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodCQ)).Should(gomega.Succeed())
+
+			devCQ = testing.MakeClusterQueue("dev-cq").
+				Cohort("all").
+				FlavorFungibility(kueue.FlavorFungibility{WhenCanBorrow: kueue.TryNextFlavor}).
+				ResourceGroup(*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "10", "10").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, devCQ)).Should(gomega.Succeed())
+
+			prodQueue := testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodQueue)).Should(gomega.Succeed())
+
+			podQueue := testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, podQueue)).Should(gomega.Succeed())
+
+			ginkgo.By("Creating one workload")
```

  Yes, thanks for reminding me.

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-14T02:58:33Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:462** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-14T02:58:33Z
```diff
@@ -389,18 +433,62 @@ func (a *Assignment) findFlavorForResourceGroup(
 			}
 		}
 
-		if representativeMode > bestAssignmentMode {
-			bestAssignment = assignments
-			bestAssignmentMode = representativeMode
-			if bestAssignmentMode == Fit {
-				// All the resources fit in the cohort, no need to check more flavors.
-				return bestAssignment, nil
+		if features.Enabled(features.FlavorFungibility) {
+			if !shouldTryNextFlavor(representativeMode, cq.FlavorFungibility, needsBorrowing) {
+				bestAssignment = assignments
+				bestAssignmentMode = representativeMode
+				break
+			}
+			if representativeMode > bestAssignmentMode {
+				bestAssignment = assignments
+				bestAssignmentMode = representativeMode
+			}
+		} else {
+			if representativeMode > bestAssignmentMode {
+				bestAssignment = assignments
+				bestAssignmentMode = representativeMode
+				if bestAssignmentMode == Fit {
+					// All the resources fit in the cohort, no need to check more flavors.
+					return bestAssignment, nil
+				}
 			}
 		}
 	}
+
+	for _, assignment := range bestAssignment {
+		// When the workload needborrowing and there is another workload in cohort doesn't
+		// need borrowing, the workload needborrowing will come again. In this case we should
+		// not skip the previous flavors.
+		if flavorIdx == len(rg.Flavors)-1 || assignment.Mode == Fit {
```

  I set `assignment.FlavorIdx = -1` when `assignment.Mode == Fit` so that any workload need borrowing or fit comes again, it will not skip any flavors. (`assignment.FlavorIdx = -1` means no flavor would be skipped.)
  
  set workload.LastAssigment to nil when a workload is skipped in scheduler.go should do the same thing, I think.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-14T06:43:14Z

- **pkg/cache/clusterqueue.go:58** by [@trasc](https://github.com/trasc) — 2023-09-14T06:23:56Z
```diff
@@ -51,11 +53,13 @@ type ClusterQueue struct {
 type Cohort struct {
 	Name    string
 	Members sets.Set[*ClusterQueue]
+	// This field will only be set in snapshot. This field equal to the maximum
+	// allocatable generation among its members.
+	AllocatableResourceGeneration int64
```

  The problem with this approach is that if one of the non maximum gen is changing, the event will not be visible at cohort level.  Summing the might work better, Also making sure that the Gen of a queue starts from 1 will change the cohort Gen when new queues are added to it. 
  
  Since the way the value is computed is described , it makes sense to keep this in the `These fields are only populated for a snapshot` section.

- **pkg/cache/snapshot.go:86** by [@trasc](https://github.com/trasc) — 2023-09-14T06:30:03Z
```diff
@@ -78,13 +74,16 @@ func (c *Cache) Snapshot() Snapshot {
 	}
 	for _, cohort := range c.cohorts {
 		cohortCopy := newCohort(cohort.Name, cohort.Members.Len())
-		cohortCopy.Generation = cohort.Generation
+		cohortCopy.AllocatableResourceGeneration = 0
 		for cq := range cohort.Members {
 			if cq.Active() {
 				cqCopy := snap.ClusterQueues[cq.Name]
 				cqCopy.accumulateResources(cohortCopy)
 				cqCopy.Cohort = cohortCopy
 				cohortCopy.Members.Insert(cqCopy)
+				if cqCopy.AllocatableResourceGeneration > cohortCopy.AllocatableResourceGeneration {
+					cohortCopy.AllocatableResourceGeneration = cqCopy.AllocatableResourceGeneration
+				}
```

  ```suggestion
  				cohortCopy.AllocatableResourceGeneration += cqCopy.AllocatableResourceGeneration
  ```

- **pkg/features/kube_features.go:57** by [@trasc](https://github.com/trasc) — 2023-09-14T06:38:22Z
```diff
@@ -54,7 +54,7 @@ func init() {
 // when adding or removing one entry.
 var defaultFeatureGates = map[featuregate.Feature]featuregate.FeatureSpec{
 	PartialAdmission:  {Default: false, PreRelease: featuregate.Alpha},
-	FlavorFungibility: {Default: false, PreRelease: featuregate.Alpha},
+	FlavorFungibility: {Default: true, PreRelease: featuregate.Alpha},
```

  ```suggestion
  	FlavorFungibility: {Default: true, PreRelease: featuregate.Beta},
  ```

- **pkg/features/kube_features.go:39** by [@trasc](https://github.com/trasc) — 2023-09-14T06:39:04Z
```diff
@@ -33,6 +33,13 @@ const (
 	//
 	// Enables partial admission.
 	PartialAdmission featuregate.Feature = "PartialAdmission"
+
+	// owner: @KunWuLuan
+	// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/582-preempt-based-on-flavor-order
+	// alpha: v0.5
```

  ```suggestion
  	// beta: v0.5
  ```

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-15T07:14:40Z

- **pkg/cache/clusterqueue.go:58** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-15T07:14:40Z
```diff
@@ -51,11 +53,13 @@ type ClusterQueue struct {
 type Cohort struct {
 	Name    string
 	Members sets.Set[*ClusterQueue]
+	// This field will only be set in snapshot. This field equal to the maximum
+	// allocatable generation among its members.
+	AllocatableResourceGeneration int64
```

  Done. Because cache will call `updateResourceGroups`, Gen of a queue starts from 1

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-18T19:52:39Z

- **pkg/cache/clusterqueue.go:335** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-18T19:52:38Z
```diff
@@ -316,6 +332,7 @@ func (c *ClusterQueue) deleteWorkload(w *kueue.Workload) {
 	if c.podsReadyTracking && !apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadPodsReady) {
 		c.WorkloadsNotReady.Delete(k)
 	}
+	c.AllocatableResourceIncreasedGen++
```

  Are you saying that if the only change that happen in the snapshot was a workload add, then there is no reason to try the previous flavors again, because we know the workload won't fit?
  
  Regardless of whether my assumption is correct, please add a comment in the code to explain the rationale.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-18T19:55:12Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:462** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-18T19:55:11Z
```diff
@@ -389,18 +433,62 @@ func (a *Assignment) findFlavorForResourceGroup(
 			}
 		}
 
-		if representativeMode > bestAssignmentMode {
-			bestAssignment = assignments
-			bestAssignmentMode = representativeMode
-			if bestAssignmentMode == Fit {
-				// All the resources fit in the cohort, no need to check more flavors.
-				return bestAssignment, nil
+		if features.Enabled(features.FlavorFungibility) {
+			if !shouldTryNextFlavor(representativeMode, cq.FlavorFungibility, needsBorrowing) {
+				bestAssignment = assignments
+				bestAssignmentMode = representativeMode
+				break
+			}
+			if representativeMode > bestAssignmentMode {
+				bestAssignment = assignments
+				bestAssignmentMode = representativeMode
+			}
+		} else {
+			if representativeMode > bestAssignmentMode {
+				bestAssignment = assignments
+				bestAssignmentMode = representativeMode
+				if bestAssignmentMode == Fit {
+					// All the resources fit in the cohort, no need to check more flavors.
+					return bestAssignment, nil
+				}
 			}
 		}
 	}
+
+	for _, assignment := range bestAssignment {
+		// When the workload needborrowing and there is another workload in cohort doesn't
+		// need borrowing, the workload needborrowing will come again. In this case we should
+		// not skip the previous flavors.
+		if flavorIdx == len(rg.Flavors)-1 || assignment.Mode == Fit {
```

  I prefer we do the changes in scheduler.go, just for separation of concerns (the flavor assigner shouldn't know about skips).

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-19T11:33:01Z

Otherwise LGTM

- **pkg/cache/clusterqueue.go:60** by [@trasc](https://github.com/trasc) — 2023-09-19T11:32:39Z
```diff
@@ -50,8 +55,11 @@ type Cohort struct {
 	Members sets.Set[*ClusterQueue]
 
 	// These fields are only populated for a snapshot.
-	RequestableResources FlavorResourceQuantities
-	Usage                FlavorResourceQuantities
+	RequestableResources workload.FlavorResourceQuantities
+	Usage                workload.FlavorResourceQuantities
+	// This field will only be set in snapshot. This field equal to the maximum
+	// allocatable generation among its members.
```

  ```suggestion
  	// This field will only be set in snapshot. This field equal to the sum of
  	// allocatable generation among its members.
  ```

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-19T11:39:20Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:462** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-19T11:39:20Z
```diff
@@ -389,18 +433,62 @@ func (a *Assignment) findFlavorForResourceGroup(
 			}
 		}
 
-		if representativeMode > bestAssignmentMode {
-			bestAssignment = assignments
-			bestAssignmentMode = representativeMode
-			if bestAssignmentMode == Fit {
-				// All the resources fit in the cohort, no need to check more flavors.
-				return bestAssignment, nil
+		if features.Enabled(features.FlavorFungibility) {
+			if !shouldTryNextFlavor(representativeMode, cq.FlavorFungibility, needsBorrowing) {
+				bestAssignment = assignments
+				bestAssignmentMode = representativeMode
+				break
+			}
+			if representativeMode > bestAssignmentMode {
+				bestAssignment = assignments
+				bestAssignmentMode = representativeMode
+			}
+		} else {
+			if representativeMode > bestAssignmentMode {
+				bestAssignment = assignments
+				bestAssignmentMode = representativeMode
+				if bestAssignmentMode == Fit {
+					// All the resources fit in the cohort, no need to check more flavors.
+					return bestAssignment, nil
+				}
 			}
 		}
 	}
+
+	for _, assignment := range bestAssignment {
+		// When the workload needborrowing and there is another workload in cohort doesn't
+		// need borrowing, the workload needborrowing will come again. In this case we should
+		// not skip the previous flavors.
+		if flavorIdx == len(rg.Flavors)-1 || assignment.Mode == Fit {
```

  Ok, I understand. Have moved it to scheduler.go

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-19T11:54:11Z

- **pkg/cache/clusterqueue.go:335** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-19T11:54:11Z
```diff
@@ -316,6 +332,7 @@ func (c *ClusterQueue) deleteWorkload(w *kueue.Workload) {
 	if c.podsReadyTracking && !apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadPodsReady) {
 		c.WorkloadsNotReady.Delete(k)
 	}
+	c.AllocatableResourceIncreasedGen++
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-19T16:43:44Z

I apologize. I forgot to send my pending comments yesterday.

- **pkg/cache/clusterqueue.go:39** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-18T19:58:30Z
```diff
@@ -23,18 +23,21 @@ var (
 // ClusterQueue is the internal implementation of kueue.ClusterQueue that
 // holds admitted workloads.
 type ClusterQueue struct {
-	Name                            string
-	Cohort                          *Cohort
-	ResourceGroups                  []ResourceGroup
-	RGByResource                    map[corev1.ResourceName]*ResourceGroup
-	Usage                           workload.FlavorResourceQuantities
-	AllocatableResourceIncreasedGen int64
-	Workloads                       map[string]*workload.Info
-	WorkloadsNotReady               sets.Set[string]
-	NamespaceSelector               labels.Selector
-	Preemption                      kueue.ClusterQueuePreemption
-	FlavorFungibility               kueue.FlavorFungibility
-	Status                          metrics.ClusterQueueStatus
+	Name              string
+	Cohort            *Cohort
+	ResourceGroups    []ResourceGroup
+	RGByResource      map[corev1.ResourceName]*ResourceGroup
+	Usage             workload.FlavorResourceQuantities
+	Workloads         map[string]*workload.Info
+	WorkloadsNotReady sets.Set[string]
+	NamespaceSelector labels.Selector
+	Preemption        kueue.ClusterQueuePreemption
+	FlavorFungibility kueue.FlavorFungibility
+	Status            metrics.ClusterQueueStatus
+	// AllocatableResourceGeneration will be increased when some admitted workloads are
+	// deleted, or the resource groups are changed. After this field is increased, all
+	// lastState in assignments will be outdated.
```

  ```suggestion
  	// deleted, or the resource groups are changed.
  ```
  
  the cache or snapshot is independent of assignments.

- **pkg/scheduler/flavorassigner/flavorassigner.go:462** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-18T20:06:48Z
```diff
@@ -389,18 +433,62 @@ func (a *Assignment) findFlavorForResourceGroup(
 			}
 		}
 
-		if representativeMode > bestAssignmentMode {
-			bestAssignment = assignments
-			bestAssignmentMode = representativeMode
-			if bestAssignmentMode == Fit {
-				// All the resources fit in the cohort, no need to check more flavors.
-				return bestAssignment, nil
+		if features.Enabled(features.FlavorFungibility) {
+			if !shouldTryNextFlavor(representativeMode, cq.FlavorFungibility, needsBorrowing) {
+				bestAssignment = assignments
+				bestAssignmentMode = representativeMode
+				break
+			}
+			if representativeMode > bestAssignmentMode {
+				bestAssignment = assignments
+				bestAssignmentMode = representativeMode
+			}
+		} else {
+			if representativeMode > bestAssignmentMode {
+				bestAssignment = assignments
+				bestAssignmentMode = representativeMode
+				if bestAssignmentMode == Fit {
+					// All the resources fit in the cohort, no need to check more flavors.
+					return bestAssignment, nil
+				}
 			}
 		}
 	}
+
+	for _, assignment := range bestAssignment {
+		// When the workload needborrowing and there is another workload in cohort doesn't
+		// need borrowing, the workload needborrowing will come again. In this case we should
+		// not skip the previous flavors.
+		if flavorIdx == len(rg.Flavors)-1 || assignment.Mode == Fit {
```

  I see you did that change. Can you clear this check?

- **pkg/scheduler/scheduler_test.go:1394** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-18T20:10:05Z
```diff
@@ -1263,6 +1264,360 @@ func TestEntryOrdering(t *testing.T) {
 	}
 }
 
+func TestLastSchedulingContext(t *testing.T) {
+	defer features.SetFeatureGateDuringTest(t, features.FlavorFungibility, true)()
+	resourceFlavors := []*kueue.ResourceFlavor{
+		{ObjectMeta: metav1.ObjectMeta{Name: "on-demand"}},
+		{ObjectMeta: metav1.ObjectMeta{Name: "spot"}},
+	}
+	clusterQueue := []kueue.ClusterQueue{
+		*utiltesting.MakeClusterQueue("eng-alpha").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+	}
+	clusterQueue_cohort := []kueue.ClusterQueue{
+		*utiltesting.MakeClusterQueue("eng-cohort-alpha").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+				WhenCanBorrow:  kueue.Borrow,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+		*utiltesting.MakeClusterQueue("eng-cohort-beta").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+				WhenCanBorrow:  kueue.Borrow,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+		*utiltesting.MakeClusterQueue("eng-cohort-theta").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.TryNextFlavor,
+				WhenCanBorrow:  kueue.TryNextFlavor,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+	}
+
+	queues := []kueue.LocalQueue{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-alpha",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-alpha",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-alpha",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-beta",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-beta",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-theta",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-theta",
+			},
+		},
+	}
+	wl := utiltesting.MakeWorkload("low-1", "default").
+		Request(corev1.ResourceCPU, "50").
+		ReserveQuota(utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+		Admitted(true).
+		Obj()
+	cases := []struct {
+		name                        string
+		cqs                         []kueue.ClusterQueue
+		admittedWorkloads           []kueue.Workload
+		workloads                   []kueue.Workload
+		deletedWorkloads            []kueue.Workload
+		wantPreempted               sets.Set[string]
+		wantAdmissionsOnceSchedule  map[string]kueue.Admission
+		wantAdmissionsTwiceSchedule map[string]kueue.Admission
```

  ```suggestion
  		wantAdmissionsOnFirstSchedule  map[string]kueue.Admission
  		wantAdmissionsOnSecondSchedule map[string]kueue.Admission
  ```

- **pkg/scheduler/scheduler_test.go:1578** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-18T20:11:59Z
```diff
@@ -1263,6 +1264,360 @@ func TestEntryOrdering(t *testing.T) {
 	}
 }
 
+func TestLastSchedulingContext(t *testing.T) {
+	defer features.SetFeatureGateDuringTest(t, features.FlavorFungibility, true)()
+	resourceFlavors := []*kueue.ResourceFlavor{
+		{ObjectMeta: metav1.ObjectMeta{Name: "on-demand"}},
+		{ObjectMeta: metav1.ObjectMeta{Name: "spot"}},
+	}
+	clusterQueue := []kueue.ClusterQueue{
+		*utiltesting.MakeClusterQueue("eng-alpha").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+	}
+	clusterQueue_cohort := []kueue.ClusterQueue{
+		*utiltesting.MakeClusterQueue("eng-cohort-alpha").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+				WhenCanBorrow:  kueue.Borrow,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+		*utiltesting.MakeClusterQueue("eng-cohort-beta").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+				WhenCanBorrow:  kueue.Borrow,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+		*utiltesting.MakeClusterQueue("eng-cohort-theta").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.TryNextFlavor,
+				WhenCanBorrow:  kueue.TryNextFlavor,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+	}
+
+	queues := []kueue.LocalQueue{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-alpha",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-alpha",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-alpha",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-beta",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-beta",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-theta",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-theta",
+			},
+		},
+	}
+	wl := utiltesting.MakeWorkload("low-1", "default").
+		Request(corev1.ResourceCPU, "50").
+		ReserveQuota(utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+		Admitted(true).
+		Obj()
+	cases := []struct {
+		name                        string
+		cqs                         []kueue.ClusterQueue
+		admittedWorkloads           []kueue.Workload
+		workloads                   []kueue.Workload
+		deletedWorkloads            []kueue.Workload
+		wantPreempted               sets.Set[string]
+		wantAdmissionsOnceSchedule  map[string]kueue.Admission
+		wantAdmissionsTwiceSchedule map[string]kueue.Admission
+	}{
+		{
+			name: "not changed",
+			cqs:  clusterQueue,
+			admittedWorkloads: []kueue.Workload{
+				*wl,
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("preemptor", "default").
+					Queue("main").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads:           []kueue.Workload{},
+			wantPreempted:              sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/preemptor": *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "spot", "20").Obj(),
+				"default/low-1":     *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+			},
+		},
+		{
+			name: "changed",
+			cqs:  clusterQueue,
+			admittedWorkloads: []kueue.Workload{
+				*wl,
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("preemptor", "default").
+					Queue("main").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads: []kueue.Workload{
+				*wl,
+			},
+			wantPreempted:              sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/preemptor": *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+		},
+		{
+			name: "borrow before next flavor",
+			cqs:  clusterQueue_cohort,
+			admittedWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("placeholder", "default").
+					Request(corev1.ResourceCPU, "50").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("borrower", "default").
+					Queue("main-alpha").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+				*utiltesting.MakeWorkload("workload1", "default").
+					Queue("main-beta").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads: []kueue.Workload{},
+			wantPreempted:    sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{
+				"default/workload1": *utiltesting.MakeAdmission("eng-cohort-beta").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+				"default/borrower":  *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/placeholder": *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+				"default/workload1":   *utiltesting.MakeAdmission("eng-cohort-beta").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+				"default/borrower":    *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+		},
+		{
+			name: "borrow after all flavors",
+			cqs:  clusterQueue_cohort,
+			admittedWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("placeholder", "default").
+					Request(corev1.ResourceCPU, "50").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+					Admitted(true).
+					Obj(),
+				*utiltesting.MakeWorkload("placeholder1", "default").
+					Request(corev1.ResourceCPU, "50").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("workload", "default").
+					Queue("main-theta").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads: []kueue.Workload{},
+			wantPreempted:    sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{
+				"default/workload": *utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "spot", "20").Obj(),
+			},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/placeholder":  *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+				"default/placeholder1": *utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+				"default/workload":     *utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "spot", "20").Obj(),
+			},
+		},
+	}
+
+	for _, tc := range cases {
+		t.Run(tc.name, func(t *testing.T) {
+			ctx, _ := utiltesting.ContextWithLog(t)
+			scheme := runtime.NewScheme()
+
+			clientBuilder := utiltesting.NewClientBuilder().
+				WithLists(&kueue.WorkloadList{Items: tc.admittedWorkloads},
+					&kueue.WorkloadList{Items: tc.workloads},
+					&kueue.ClusterQueueList{Items: tc.cqs},
+					&kueue.LocalQueueList{Items: queues}).
+				WithObjects(
+					&corev1.Namespace{ObjectMeta: metav1.ObjectMeta{Name: "default"}},
+				)
+			cl := clientBuilder.Build()
+			broadcaster := record.NewBroadcaster()
+			recorder := broadcaster.NewRecorder(scheme,
+				corev1.EventSource{Component: constants.AdmissionName})
+			cqCache := cache.New(cl)
+			qManager := queue.NewManager(cl, cqCache)
+			// Workloads are loaded into queues or clusterQueues as we add them.
+			for _, q := range queues {
+				if err := qManager.AddLocalQueue(ctx, &q); err != nil {
+					t.Fatalf("Inserting queue %s/%s in manager: %v", q.Namespace, q.Name, err)
+				}
+			}
+			for i := range resourceFlavors {
+				cqCache.AddOrUpdateResourceFlavor(resourceFlavors[i])
+			}
+			for _, cq := range tc.cqs {
+				if err := cqCache.AddClusterQueue(ctx, &cq); err != nil {
+					t.Fatalf("Inserting clusterQueue %s in cache: %v", cq.Name, err)
+				}
+				if err := qManager.AddClusterQueue(ctx, &cq); err != nil {
+					t.Fatalf("Inserting clusterQueue %s in manager: %v", cq.Name, err)
+				}
+			}
+			scheduler := New(qManager, cqCache, cl, recorder)
+			gotScheduled := make(map[string]kueue.Admission)
+			var mu sync.Mutex
+			scheduler.applyAdmission = func(ctx context.Context, w *kueue.Workload) error {
+				mu.Lock()
+				gotScheduled[workload.Key(w)] = *w.Status.Admission
+				mu.Unlock()
+				return nil
+			}
+			wg := sync.WaitGroup{}
+			scheduler.setAdmissionRoutineWrapper(routine.NewWrapper(
+				func() { wg.Add(1) },
+				func() { wg.Done() },
+			))
+			gotPreempted := sets.New[string]()
+			scheduler.preemptor.OverrideApply(func(_ context.Context, w *kueue.Workload) error {
+				mu.Lock()
+				gotPreempted.Insert(workload.Key(w))
+				mu.Unlock()
+				return nil
+			})
+
+			ctx, cancel := context.WithTimeout(ctx, queueingTimeout)
+			go qManager.CleanUpOnContext(ctx)
+			defer cancel()
+
+			scheduler.schedule(ctx)
+			wg.Wait()
+
+			if diff := cmp.Diff(tc.wantPreempted, gotPreempted); diff != "" {
+				t.Errorf("Unexpected preemptions (-want,+got):\n%s", diff)
+			}
+			if diff := cmp.Diff(tc.wantAdmissionsOnceSchedule, gotScheduled); diff != "" {
+				t.Errorf("Unexpected scheduled workloads (-want,+got):\n%s", diff)
+			}
+
+			// should schedule from where preempting failed last time
+			ctx, cancel = context.WithTimeout(ctx, queueingTimeout)
+			go qManager.CleanUpOnContext(ctx)
+			defer cancel()
```

  why are you calling this again?

- **pkg/scheduler/scheduler_test.go:1417** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-18T20:18:05Z
```diff
@@ -1263,6 +1264,360 @@ func TestEntryOrdering(t *testing.T) {
 	}
 }
 
+func TestLastSchedulingContext(t *testing.T) {
+	defer features.SetFeatureGateDuringTest(t, features.FlavorFungibility, true)()
+	resourceFlavors := []*kueue.ResourceFlavor{
+		{ObjectMeta: metav1.ObjectMeta{Name: "on-demand"}},
+		{ObjectMeta: metav1.ObjectMeta{Name: "spot"}},
+	}
+	clusterQueue := []kueue.ClusterQueue{
+		*utiltesting.MakeClusterQueue("eng-alpha").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+	}
+	clusterQueue_cohort := []kueue.ClusterQueue{
+		*utiltesting.MakeClusterQueue("eng-cohort-alpha").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+				WhenCanBorrow:  kueue.Borrow,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+		*utiltesting.MakeClusterQueue("eng-cohort-beta").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+				WhenCanBorrow:  kueue.Borrow,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+		*utiltesting.MakeClusterQueue("eng-cohort-theta").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.TryNextFlavor,
+				WhenCanBorrow:  kueue.TryNextFlavor,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+	}
+
+	queues := []kueue.LocalQueue{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-alpha",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-alpha",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-alpha",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-beta",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-beta",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-theta",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-theta",
+			},
+		},
+	}
+	wl := utiltesting.MakeWorkload("low-1", "default").
+		Request(corev1.ResourceCPU, "50").
+		ReserveQuota(utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+		Admitted(true).
+		Obj()
+	cases := []struct {
+		name                        string
+		cqs                         []kueue.ClusterQueue
+		admittedWorkloads           []kueue.Workload
+		workloads                   []kueue.Workload
+		deletedWorkloads            []kueue.Workload
+		wantPreempted               sets.Set[string]
+		wantAdmissionsOnceSchedule  map[string]kueue.Admission
+		wantAdmissionsTwiceSchedule map[string]kueue.Admission
+	}{
+		{
+			name: "not changed",
+			cqs:  clusterQueue,
+			admittedWorkloads: []kueue.Workload{
+				*wl,
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("preemptor", "default").
+					Queue("main").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads:           []kueue.Workload{},
+			wantPreempted:              sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/preemptor": *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "spot", "20").Obj(),
+				"default/low-1":     *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+			},
+		},
+		{
+			name: "changed",
```

  add some context into what changed.

- **pkg/scheduler/scheduler_test.go:1614** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-18T20:24:13Z
```diff
@@ -1263,6 +1264,360 @@ func TestEntryOrdering(t *testing.T) {
 	}
 }
 
+func TestLastSchedulingContext(t *testing.T) {
+	defer features.SetFeatureGateDuringTest(t, features.FlavorFungibility, true)()
+	resourceFlavors := []*kueue.ResourceFlavor{
+		{ObjectMeta: metav1.ObjectMeta{Name: "on-demand"}},
+		{ObjectMeta: metav1.ObjectMeta{Name: "spot"}},
+	}
+	clusterQueue := []kueue.ClusterQueue{
+		*utiltesting.MakeClusterQueue("eng-alpha").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+	}
+	clusterQueue_cohort := []kueue.ClusterQueue{
+		*utiltesting.MakeClusterQueue("eng-cohort-alpha").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+				WhenCanBorrow:  kueue.Borrow,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+		*utiltesting.MakeClusterQueue("eng-cohort-beta").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+				WhenCanBorrow:  kueue.Borrow,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+		*utiltesting.MakeClusterQueue("eng-cohort-theta").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.TryNextFlavor,
+				WhenCanBorrow:  kueue.TryNextFlavor,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+	}
+
+	queues := []kueue.LocalQueue{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-alpha",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-alpha",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-alpha",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-beta",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-beta",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-theta",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-theta",
+			},
+		},
+	}
+	wl := utiltesting.MakeWorkload("low-1", "default").
+		Request(corev1.ResourceCPU, "50").
+		ReserveQuota(utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+		Admitted(true).
+		Obj()
+	cases := []struct {
+		name                        string
+		cqs                         []kueue.ClusterQueue
+		admittedWorkloads           []kueue.Workload
+		workloads                   []kueue.Workload
+		deletedWorkloads            []kueue.Workload
+		wantPreempted               sets.Set[string]
+		wantAdmissionsOnceSchedule  map[string]kueue.Admission
+		wantAdmissionsTwiceSchedule map[string]kueue.Admission
+	}{
+		{
+			name: "not changed",
+			cqs:  clusterQueue,
+			admittedWorkloads: []kueue.Workload{
+				*wl,
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("preemptor", "default").
+					Queue("main").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads:           []kueue.Workload{},
+			wantPreempted:              sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/preemptor": *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "spot", "20").Obj(),
+				"default/low-1":     *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+			},
+		},
+		{
+			name: "changed",
+			cqs:  clusterQueue,
+			admittedWorkloads: []kueue.Workload{
+				*wl,
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("preemptor", "default").
+					Queue("main").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads: []kueue.Workload{
+				*wl,
+			},
+			wantPreempted:              sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/preemptor": *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+		},
+		{
+			name: "borrow before next flavor",
+			cqs:  clusterQueue_cohort,
+			admittedWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("placeholder", "default").
+					Request(corev1.ResourceCPU, "50").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("borrower", "default").
+					Queue("main-alpha").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+				*utiltesting.MakeWorkload("workload1", "default").
+					Queue("main-beta").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads: []kueue.Workload{},
+			wantPreempted:    sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{
+				"default/workload1": *utiltesting.MakeAdmission("eng-cohort-beta").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+				"default/borrower":  *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/placeholder": *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+				"default/workload1":   *utiltesting.MakeAdmission("eng-cohort-beta").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+				"default/borrower":    *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+		},
+		{
+			name: "borrow after all flavors",
+			cqs:  clusterQueue_cohort,
+			admittedWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("placeholder", "default").
+					Request(corev1.ResourceCPU, "50").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+					Admitted(true).
+					Obj(),
+				*utiltesting.MakeWorkload("placeholder1", "default").
+					Request(corev1.ResourceCPU, "50").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("workload", "default").
+					Queue("main-theta").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads: []kueue.Workload{},
+			wantPreempted:    sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{
+				"default/workload": *utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "spot", "20").Obj(),
+			},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/placeholder":  *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+				"default/placeholder1": *utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+				"default/workload":     *utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "spot", "20").Obj(),
+			},
+		},
+	}
+
+	for _, tc := range cases {
+		t.Run(tc.name, func(t *testing.T) {
+			ctx, _ := utiltesting.ContextWithLog(t)
+			scheme := runtime.NewScheme()
+
+			clientBuilder := utiltesting.NewClientBuilder().
+				WithLists(&kueue.WorkloadList{Items: tc.admittedWorkloads},
+					&kueue.WorkloadList{Items: tc.workloads},
+					&kueue.ClusterQueueList{Items: tc.cqs},
+					&kueue.LocalQueueList{Items: queues}).
+				WithObjects(
+					&corev1.Namespace{ObjectMeta: metav1.ObjectMeta{Name: "default"}},
+				)
+			cl := clientBuilder.Build()
+			broadcaster := record.NewBroadcaster()
+			recorder := broadcaster.NewRecorder(scheme,
+				corev1.EventSource{Component: constants.AdmissionName})
+			cqCache := cache.New(cl)
+			qManager := queue.NewManager(cl, cqCache)
+			// Workloads are loaded into queues or clusterQueues as we add them.
+			for _, q := range queues {
+				if err := qManager.AddLocalQueue(ctx, &q); err != nil {
+					t.Fatalf("Inserting queue %s/%s in manager: %v", q.Namespace, q.Name, err)
+				}
+			}
+			for i := range resourceFlavors {
+				cqCache.AddOrUpdateResourceFlavor(resourceFlavors[i])
+			}
+			for _, cq := range tc.cqs {
+				if err := cqCache.AddClusterQueue(ctx, &cq); err != nil {
+					t.Fatalf("Inserting clusterQueue %s in cache: %v", cq.Name, err)
+				}
+				if err := qManager.AddClusterQueue(ctx, &cq); err != nil {
+					t.Fatalf("Inserting clusterQueue %s in manager: %v", cq.Name, err)
+				}
+			}
+			scheduler := New(qManager, cqCache, cl, recorder)
+			gotScheduled := make(map[string]kueue.Admission)
+			var mu sync.Mutex
+			scheduler.applyAdmission = func(ctx context.Context, w *kueue.Workload) error {
+				mu.Lock()
+				gotScheduled[workload.Key(w)] = *w.Status.Admission
+				mu.Unlock()
+				return nil
+			}
+			wg := sync.WaitGroup{}
+			scheduler.setAdmissionRoutineWrapper(routine.NewWrapper(
+				func() { wg.Add(1) },
+				func() { wg.Done() },
+			))
+			gotPreempted := sets.New[string]()
+			scheduler.preemptor.OverrideApply(func(_ context.Context, w *kueue.Workload) error {
+				mu.Lock()
+				gotPreempted.Insert(workload.Key(w))
+				mu.Unlock()
+				return nil
+			})
+
+			ctx, cancel := context.WithTimeout(ctx, queueingTimeout)
+			go qManager.CleanUpOnContext(ctx)
+			defer cancel()
+
+			scheduler.schedule(ctx)
+			wg.Wait()
+
+			if diff := cmp.Diff(tc.wantPreempted, gotPreempted); diff != "" {
+				t.Errorf("Unexpected preemptions (-want,+got):\n%s", diff)
+			}
+			if diff := cmp.Diff(tc.wantAdmissionsOnceSchedule, gotScheduled); diff != "" {
+				t.Errorf("Unexpected scheduled workloads (-want,+got):\n%s", diff)
+			}
+
+			// should schedule from where preempting failed last time
+			ctx, cancel = context.WithTimeout(ctx, queueingTimeout)
+			go qManager.CleanUpOnContext(ctx)
+			defer cancel()
+
+			for _, wl := range tc.deletedWorkloads {
+				err := cl.Delete(ctx, &wl)
+				if err != nil {
+					t.Errorf("Delete workload failed: %v", err)
+				}
+				err = cqCache.DeleteWorkload(&wl)
+				if err != nil {
+					t.Errorf("Delete workload failed: %v", err)
+				}
+			}
+
+			scheduler.schedule(ctx)
+			wg.Wait()
+
+			if diff := cmp.Diff(tc.wantPreempted, gotPreempted); diff != "" {
+				t.Errorf("Unexpected preemptions (-want,+got):\n%s", diff)
+			}
+			// Verify assignments in cache.
+			gotAssignments := make(map[string]kueue.Admission)
+			snapshot := cqCache.Snapshot()
+			for cqName, c := range snapshot.ClusterQueues {
+				for name, w := range c.Workloads {
+					if !workload.IsAdmitted(w.Obj) {
+						t.Errorf("Workload %s is not admitted by a clusterQueue, but it is found as member of clusterQueue %s in the cache", name, cqName)
+					} else if string(w.Obj.Status.Admission.ClusterQueue) != cqName {
+						t.Errorf("Workload %s is admitted by clusterQueue %s, but it is found as member of clusterQueue %s in the cache", name, w.Obj.Status.Admission.ClusterQueue, cqName)
+					} else {
+						gotAssignments[name] = *w.Obj.Status.Admission
+					}
+				}
+			}
+			if len(gotAssignments) == 0 {
+				gotAssignments = nil
+			}
+			if diff := cmp.Diff(tc.wantAdmissionsTwiceSchedule, gotAssignments); diff != "" {
```

  why do you compare against `gotScheduled` after the first schedule attempt, but here against `gotAssignments`?
  
  For consistency, I would just do `gotScheduled`.
  
  The other test suite should be enough to guarantee that the results from `gotSchedule` match the assignments in the cache.

- **pkg/scheduler/scheduler_test.go:1491** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-18T20:25:15Z
```diff
@@ -1263,6 +1264,360 @@ func TestEntryOrdering(t *testing.T) {
 	}
 }
 
+func TestLastSchedulingContext(t *testing.T) {
+	defer features.SetFeatureGateDuringTest(t, features.FlavorFungibility, true)()
+	resourceFlavors := []*kueue.ResourceFlavor{
+		{ObjectMeta: metav1.ObjectMeta{Name: "on-demand"}},
+		{ObjectMeta: metav1.ObjectMeta{Name: "spot"}},
+	}
+	clusterQueue := []kueue.ClusterQueue{
+		*utiltesting.MakeClusterQueue("eng-alpha").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+	}
+	clusterQueue_cohort := []kueue.ClusterQueue{
+		*utiltesting.MakeClusterQueue("eng-cohort-alpha").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+				WhenCanBorrow:  kueue.Borrow,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+		*utiltesting.MakeClusterQueue("eng-cohort-beta").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+				WhenCanBorrow:  kueue.Borrow,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+		*utiltesting.MakeClusterQueue("eng-cohort-theta").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.TryNextFlavor,
+				WhenCanBorrow:  kueue.TryNextFlavor,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+	}
+
+	queues := []kueue.LocalQueue{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-alpha",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-alpha",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-alpha",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-beta",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-beta",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-theta",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-theta",
+			},
+		},
+	}
+	wl := utiltesting.MakeWorkload("low-1", "default").
+		Request(corev1.ResourceCPU, "50").
+		ReserveQuota(utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+		Admitted(true).
+		Obj()
+	cases := []struct {
+		name                        string
+		cqs                         []kueue.ClusterQueue
+		admittedWorkloads           []kueue.Workload
+		workloads                   []kueue.Workload
+		deletedWorkloads            []kueue.Workload
+		wantPreempted               sets.Set[string]
+		wantAdmissionsOnceSchedule  map[string]kueue.Admission
+		wantAdmissionsTwiceSchedule map[string]kueue.Admission
+	}{
+		{
+			name: "not changed",
+			cqs:  clusterQueue,
+			admittedWorkloads: []kueue.Workload{
+				*wl,
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("preemptor", "default").
+					Queue("main").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads:           []kueue.Workload{},
+			wantPreempted:              sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/preemptor": *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "spot", "20").Obj(),
+				"default/low-1":     *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+			},
+		},
+		{
+			name: "changed",
+			cqs:  clusterQueue,
+			admittedWorkloads: []kueue.Workload{
+				*wl,
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("preemptor", "default").
+					Queue("main").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads: []kueue.Workload{
+				*wl,
+			},
+			wantPreempted:              sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/preemptor": *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+		},
+		{
+			name: "borrow before next flavor",
+			cqs:  clusterQueue_cohort,
+			admittedWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("placeholder", "default").
+					Request(corev1.ResourceCPU, "50").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("borrower", "default").
+					Queue("main-alpha").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+				*utiltesting.MakeWorkload("workload1", "default").
+					Queue("main-beta").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads: []kueue.Workload{},
+			wantPreempted:    sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{
+				"default/workload1": *utiltesting.MakeAdmission("eng-cohort-beta").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+				"default/borrower":  *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/placeholder": *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+				"default/workload1":   *utiltesting.MakeAdmission("eng-cohort-beta").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+				"default/borrower":    *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+		},
+		{
+			name: "borrow after all flavors",
+			cqs:  clusterQueue_cohort,
+			admittedWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("placeholder", "default").
+					Request(corev1.ResourceCPU, "50").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+					Admitted(true).
+					Obj(),
+				*utiltesting.MakeWorkload("placeholder1", "default").
+					Request(corev1.ResourceCPU, "50").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("workload", "default").
+					Queue("main-theta").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads: []kueue.Workload{},
+			wantPreempted:    sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{
+				"default/workload": *utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "spot", "20").Obj(),
```

  Can you add a test where you configure `borrow: TryNextFlavor`, but the next flavor is full, so you have to borrow on the first flavor?

- **pkg/scheduler/scheduler.go:195** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-19T16:43:22Z
```diff
@@ -192,6 +192,9 @@ func (s *Scheduler) schedule(ctx context.Context) {
 			if cycleCohortsUsage.hasCommonFlavorResources(cq.Cohort.Name, e.assignment.Usage) && !cq.Cohort.CanFit(sum) {
 				e.status = skipped
 				e.inadmissibleMsg = "other workloads in the cohort were prioritized"
+				// When the workload needborrowing and there is another workload in cohort doesn't
```

  ```suggestion
  				// When the workload needs borrowing and there is another workload in cohort doesn't
  ```

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-21T12:25:21Z

- **pkg/scheduler/scheduler_test.go:1491** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-21T12:25:21Z
```diff
@@ -1263,6 +1264,360 @@ func TestEntryOrdering(t *testing.T) {
 	}
 }
 
+func TestLastSchedulingContext(t *testing.T) {
+	defer features.SetFeatureGateDuringTest(t, features.FlavorFungibility, true)()
+	resourceFlavors := []*kueue.ResourceFlavor{
+		{ObjectMeta: metav1.ObjectMeta{Name: "on-demand"}},
+		{ObjectMeta: metav1.ObjectMeta{Name: "spot"}},
+	}
+	clusterQueue := []kueue.ClusterQueue{
+		*utiltesting.MakeClusterQueue("eng-alpha").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+	}
+	clusterQueue_cohort := []kueue.ClusterQueue{
+		*utiltesting.MakeClusterQueue("eng-cohort-alpha").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+				WhenCanBorrow:  kueue.Borrow,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+		*utiltesting.MakeClusterQueue("eng-cohort-beta").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+				WhenCanBorrow:  kueue.Borrow,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+		*utiltesting.MakeClusterQueue("eng-cohort-theta").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.TryNextFlavor,
+				WhenCanBorrow:  kueue.TryNextFlavor,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+	}
+
+	queues := []kueue.LocalQueue{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-alpha",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-alpha",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-alpha",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-beta",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-beta",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-theta",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-theta",
+			},
+		},
+	}
+	wl := utiltesting.MakeWorkload("low-1", "default").
+		Request(corev1.ResourceCPU, "50").
+		ReserveQuota(utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+		Admitted(true).
+		Obj()
+	cases := []struct {
+		name                        string
+		cqs                         []kueue.ClusterQueue
+		admittedWorkloads           []kueue.Workload
+		workloads                   []kueue.Workload
+		deletedWorkloads            []kueue.Workload
+		wantPreempted               sets.Set[string]
+		wantAdmissionsOnceSchedule  map[string]kueue.Admission
+		wantAdmissionsTwiceSchedule map[string]kueue.Admission
+	}{
+		{
+			name: "not changed",
+			cqs:  clusterQueue,
+			admittedWorkloads: []kueue.Workload{
+				*wl,
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("preemptor", "default").
+					Queue("main").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads:           []kueue.Workload{},
+			wantPreempted:              sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/preemptor": *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "spot", "20").Obj(),
+				"default/low-1":     *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+			},
+		},
+		{
+			name: "changed",
+			cqs:  clusterQueue,
+			admittedWorkloads: []kueue.Workload{
+				*wl,
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("preemptor", "default").
+					Queue("main").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads: []kueue.Workload{
+				*wl,
+			},
+			wantPreempted:              sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/preemptor": *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+		},
+		{
+			name: "borrow before next flavor",
+			cqs:  clusterQueue_cohort,
+			admittedWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("placeholder", "default").
+					Request(corev1.ResourceCPU, "50").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("borrower", "default").
+					Queue("main-alpha").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+				*utiltesting.MakeWorkload("workload1", "default").
+					Queue("main-beta").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads: []kueue.Workload{},
+			wantPreempted:    sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{
+				"default/workload1": *utiltesting.MakeAdmission("eng-cohort-beta").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+				"default/borrower":  *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/placeholder": *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+				"default/workload1":   *utiltesting.MakeAdmission("eng-cohort-beta").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+				"default/borrower":    *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+		},
+		{
+			name: "borrow after all flavors",
+			cqs:  clusterQueue_cohort,
+			admittedWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("placeholder", "default").
+					Request(corev1.ResourceCPU, "50").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+					Admitted(true).
+					Obj(),
+				*utiltesting.MakeWorkload("placeholder1", "default").
+					Request(corev1.ResourceCPU, "50").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("workload", "default").
+					Queue("main-theta").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads: []kueue.Workload{},
+			wantPreempted:    sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{
+				"default/workload": *utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "spot", "20").Obj(),
```

  done

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-21T12:26:28Z

- **pkg/scheduler/scheduler_test.go:1614** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-21T12:26:27Z
```diff
@@ -1263,6 +1264,360 @@ func TestEntryOrdering(t *testing.T) {
 	}
 }
 
+func TestLastSchedulingContext(t *testing.T) {
+	defer features.SetFeatureGateDuringTest(t, features.FlavorFungibility, true)()
+	resourceFlavors := []*kueue.ResourceFlavor{
+		{ObjectMeta: metav1.ObjectMeta{Name: "on-demand"}},
+		{ObjectMeta: metav1.ObjectMeta{Name: "spot"}},
+	}
+	clusterQueue := []kueue.ClusterQueue{
+		*utiltesting.MakeClusterQueue("eng-alpha").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+	}
+	clusterQueue_cohort := []kueue.ClusterQueue{
+		*utiltesting.MakeClusterQueue("eng-cohort-alpha").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+				WhenCanBorrow:  kueue.Borrow,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+		*utiltesting.MakeClusterQueue("eng-cohort-beta").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+				WhenCanBorrow:  kueue.Borrow,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+		*utiltesting.MakeClusterQueue("eng-cohort-theta").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.TryNextFlavor,
+				WhenCanBorrow:  kueue.TryNextFlavor,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+	}
+
+	queues := []kueue.LocalQueue{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-alpha",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-alpha",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-alpha",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-beta",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-beta",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-theta",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-theta",
+			},
+		},
+	}
+	wl := utiltesting.MakeWorkload("low-1", "default").
+		Request(corev1.ResourceCPU, "50").
+		ReserveQuota(utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+		Admitted(true).
+		Obj()
+	cases := []struct {
+		name                        string
+		cqs                         []kueue.ClusterQueue
+		admittedWorkloads           []kueue.Workload
+		workloads                   []kueue.Workload
+		deletedWorkloads            []kueue.Workload
+		wantPreempted               sets.Set[string]
+		wantAdmissionsOnceSchedule  map[string]kueue.Admission
+		wantAdmissionsTwiceSchedule map[string]kueue.Admission
+	}{
+		{
+			name: "not changed",
+			cqs:  clusterQueue,
+			admittedWorkloads: []kueue.Workload{
+				*wl,
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("preemptor", "default").
+					Queue("main").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads:           []kueue.Workload{},
+			wantPreempted:              sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/preemptor": *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "spot", "20").Obj(),
+				"default/low-1":     *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+			},
+		},
+		{
+			name: "changed",
+			cqs:  clusterQueue,
+			admittedWorkloads: []kueue.Workload{
+				*wl,
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("preemptor", "default").
+					Queue("main").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads: []kueue.Workload{
+				*wl,
+			},
+			wantPreempted:              sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/preemptor": *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+		},
+		{
+			name: "borrow before next flavor",
+			cqs:  clusterQueue_cohort,
+			admittedWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("placeholder", "default").
+					Request(corev1.ResourceCPU, "50").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("borrower", "default").
+					Queue("main-alpha").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+				*utiltesting.MakeWorkload("workload1", "default").
+					Queue("main-beta").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads: []kueue.Workload{},
+			wantPreempted:    sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{
+				"default/workload1": *utiltesting.MakeAdmission("eng-cohort-beta").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+				"default/borrower":  *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/placeholder": *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+				"default/workload1":   *utiltesting.MakeAdmission("eng-cohort-beta").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+				"default/borrower":    *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+		},
+		{
+			name: "borrow after all flavors",
+			cqs:  clusterQueue_cohort,
+			admittedWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("placeholder", "default").
+					Request(corev1.ResourceCPU, "50").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+					Admitted(true).
+					Obj(),
+				*utiltesting.MakeWorkload("placeholder1", "default").
+					Request(corev1.ResourceCPU, "50").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("workload", "default").
+					Queue("main-theta").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads: []kueue.Workload{},
+			wantPreempted:    sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{
+				"default/workload": *utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "spot", "20").Obj(),
+			},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/placeholder":  *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+				"default/placeholder1": *utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+				"default/workload":     *utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "spot", "20").Obj(),
+			},
+		},
+	}
+
+	for _, tc := range cases {
+		t.Run(tc.name, func(t *testing.T) {
+			ctx, _ := utiltesting.ContextWithLog(t)
+			scheme := runtime.NewScheme()
+
+			clientBuilder := utiltesting.NewClientBuilder().
+				WithLists(&kueue.WorkloadList{Items: tc.admittedWorkloads},
+					&kueue.WorkloadList{Items: tc.workloads},
+					&kueue.ClusterQueueList{Items: tc.cqs},
+					&kueue.LocalQueueList{Items: queues}).
+				WithObjects(
+					&corev1.Namespace{ObjectMeta: metav1.ObjectMeta{Name: "default"}},
+				)
+			cl := clientBuilder.Build()
+			broadcaster := record.NewBroadcaster()
+			recorder := broadcaster.NewRecorder(scheme,
+				corev1.EventSource{Component: constants.AdmissionName})
+			cqCache := cache.New(cl)
+			qManager := queue.NewManager(cl, cqCache)
+			// Workloads are loaded into queues or clusterQueues as we add them.
+			for _, q := range queues {
+				if err := qManager.AddLocalQueue(ctx, &q); err != nil {
+					t.Fatalf("Inserting queue %s/%s in manager: %v", q.Namespace, q.Name, err)
+				}
+			}
+			for i := range resourceFlavors {
+				cqCache.AddOrUpdateResourceFlavor(resourceFlavors[i])
+			}
+			for _, cq := range tc.cqs {
+				if err := cqCache.AddClusterQueue(ctx, &cq); err != nil {
+					t.Fatalf("Inserting clusterQueue %s in cache: %v", cq.Name, err)
+				}
+				if err := qManager.AddClusterQueue(ctx, &cq); err != nil {
+					t.Fatalf("Inserting clusterQueue %s in manager: %v", cq.Name, err)
+				}
+			}
+			scheduler := New(qManager, cqCache, cl, recorder)
+			gotScheduled := make(map[string]kueue.Admission)
+			var mu sync.Mutex
+			scheduler.applyAdmission = func(ctx context.Context, w *kueue.Workload) error {
+				mu.Lock()
+				gotScheduled[workload.Key(w)] = *w.Status.Admission
+				mu.Unlock()
+				return nil
+			}
+			wg := sync.WaitGroup{}
+			scheduler.setAdmissionRoutineWrapper(routine.NewWrapper(
+				func() { wg.Add(1) },
+				func() { wg.Done() },
+			))
+			gotPreempted := sets.New[string]()
+			scheduler.preemptor.OverrideApply(func(_ context.Context, w *kueue.Workload) error {
+				mu.Lock()
+				gotPreempted.Insert(workload.Key(w))
+				mu.Unlock()
+				return nil
+			})
+
+			ctx, cancel := context.WithTimeout(ctx, queueingTimeout)
+			go qManager.CleanUpOnContext(ctx)
+			defer cancel()
+
+			scheduler.schedule(ctx)
+			wg.Wait()
+
+			if diff := cmp.Diff(tc.wantPreempted, gotPreempted); diff != "" {
+				t.Errorf("Unexpected preemptions (-want,+got):\n%s", diff)
+			}
+			if diff := cmp.Diff(tc.wantAdmissionsOnceSchedule, gotScheduled); diff != "" {
+				t.Errorf("Unexpected scheduled workloads (-want,+got):\n%s", diff)
+			}
+
+			// should schedule from where preempting failed last time
+			ctx, cancel = context.WithTimeout(ctx, queueingTimeout)
+			go qManager.CleanUpOnContext(ctx)
+			defer cancel()
+
+			for _, wl := range tc.deletedWorkloads {
+				err := cl.Delete(ctx, &wl)
+				if err != nil {
+					t.Errorf("Delete workload failed: %v", err)
+				}
+				err = cqCache.DeleteWorkload(&wl)
+				if err != nil {
+					t.Errorf("Delete workload failed: %v", err)
+				}
+			}
+
+			scheduler.schedule(ctx)
+			wg.Wait()
+
+			if diff := cmp.Diff(tc.wantPreempted, gotPreempted); diff != "" {
+				t.Errorf("Unexpected preemptions (-want,+got):\n%s", diff)
+			}
+			// Verify assignments in cache.
+			gotAssignments := make(map[string]kueue.Admission)
+			snapshot := cqCache.Snapshot()
+			for cqName, c := range snapshot.ClusterQueues {
+				for name, w := range c.Workloads {
+					if !workload.IsAdmitted(w.Obj) {
+						t.Errorf("Workload %s is not admitted by a clusterQueue, but it is found as member of clusterQueue %s in the cache", name, cqName)
+					} else if string(w.Obj.Status.Admission.ClusterQueue) != cqName {
+						t.Errorf("Workload %s is admitted by clusterQueue %s, but it is found as member of clusterQueue %s in the cache", name, w.Obj.Status.Admission.ClusterQueue, cqName)
+					} else {
+						gotAssignments[name] = *w.Obj.Status.Admission
+					}
+				}
+			}
+			if len(gotAssignments) == 0 {
+				gotAssignments = nil
+			}
+			if diff := cmp.Diff(tc.wantAdmissionsTwiceSchedule, gotAssignments); diff != "" {
```

  got assignment will contains all admitted workloads, right? for the cases have workloads to be deleted, it may be useful I think.

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-21T12:26:53Z

- **pkg/scheduler/scheduler_test.go:1578** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-21T12:26:53Z
```diff
@@ -1263,6 +1264,360 @@ func TestEntryOrdering(t *testing.T) {
 	}
 }
 
+func TestLastSchedulingContext(t *testing.T) {
+	defer features.SetFeatureGateDuringTest(t, features.FlavorFungibility, true)()
+	resourceFlavors := []*kueue.ResourceFlavor{
+		{ObjectMeta: metav1.ObjectMeta{Name: "on-demand"}},
+		{ObjectMeta: metav1.ObjectMeta{Name: "spot"}},
+	}
+	clusterQueue := []kueue.ClusterQueue{
+		*utiltesting.MakeClusterQueue("eng-alpha").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+	}
+	clusterQueue_cohort := []kueue.ClusterQueue{
+		*utiltesting.MakeClusterQueue("eng-cohort-alpha").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+				WhenCanBorrow:  kueue.Borrow,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+		*utiltesting.MakeClusterQueue("eng-cohort-beta").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+				WhenCanBorrow:  kueue.Borrow,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+		*utiltesting.MakeClusterQueue("eng-cohort-theta").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.TryNextFlavor,
+				WhenCanBorrow:  kueue.TryNextFlavor,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+	}
+
+	queues := []kueue.LocalQueue{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-alpha",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-alpha",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-alpha",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-beta",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-beta",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-theta",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-theta",
+			},
+		},
+	}
+	wl := utiltesting.MakeWorkload("low-1", "default").
+		Request(corev1.ResourceCPU, "50").
+		ReserveQuota(utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+		Admitted(true).
+		Obj()
+	cases := []struct {
+		name                        string
+		cqs                         []kueue.ClusterQueue
+		admittedWorkloads           []kueue.Workload
+		workloads                   []kueue.Workload
+		deletedWorkloads            []kueue.Workload
+		wantPreempted               sets.Set[string]
+		wantAdmissionsOnceSchedule  map[string]kueue.Admission
+		wantAdmissionsTwiceSchedule map[string]kueue.Admission
+	}{
+		{
+			name: "not changed",
+			cqs:  clusterQueue,
+			admittedWorkloads: []kueue.Workload{
+				*wl,
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("preemptor", "default").
+					Queue("main").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads:           []kueue.Workload{},
+			wantPreempted:              sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/preemptor": *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "spot", "20").Obj(),
+				"default/low-1":     *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+			},
+		},
+		{
+			name: "changed",
+			cqs:  clusterQueue,
+			admittedWorkloads: []kueue.Workload{
+				*wl,
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("preemptor", "default").
+					Queue("main").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads: []kueue.Workload{
+				*wl,
+			},
+			wantPreempted:              sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/preemptor": *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+		},
+		{
+			name: "borrow before next flavor",
+			cqs:  clusterQueue_cohort,
+			admittedWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("placeholder", "default").
+					Request(corev1.ResourceCPU, "50").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("borrower", "default").
+					Queue("main-alpha").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+				*utiltesting.MakeWorkload("workload1", "default").
+					Queue("main-beta").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads: []kueue.Workload{},
+			wantPreempted:    sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{
+				"default/workload1": *utiltesting.MakeAdmission("eng-cohort-beta").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+				"default/borrower":  *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/placeholder": *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+				"default/workload1":   *utiltesting.MakeAdmission("eng-cohort-beta").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+				"default/borrower":    *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+		},
+		{
+			name: "borrow after all flavors",
+			cqs:  clusterQueue_cohort,
+			admittedWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("placeholder", "default").
+					Request(corev1.ResourceCPU, "50").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+					Admitted(true).
+					Obj(),
+				*utiltesting.MakeWorkload("placeholder1", "default").
+					Request(corev1.ResourceCPU, "50").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("workload", "default").
+					Queue("main-theta").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads: []kueue.Workload{},
+			wantPreempted:    sets.Set[string]{},
+			wantAdmissionsOnceSchedule: map[string]kueue.Admission{
+				"default/workload": *utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "spot", "20").Obj(),
+			},
+			wantAdmissionsTwiceSchedule: map[string]kueue.Admission{
+				"default/placeholder":  *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+				"default/placeholder1": *utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+				"default/workload":     *utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "spot", "20").Obj(),
+			},
+		},
+	}
+
+	for _, tc := range cases {
+		t.Run(tc.name, func(t *testing.T) {
+			ctx, _ := utiltesting.ContextWithLog(t)
+			scheme := runtime.NewScheme()
+
+			clientBuilder := utiltesting.NewClientBuilder().
+				WithLists(&kueue.WorkloadList{Items: tc.admittedWorkloads},
+					&kueue.WorkloadList{Items: tc.workloads},
+					&kueue.ClusterQueueList{Items: tc.cqs},
+					&kueue.LocalQueueList{Items: queues}).
+				WithObjects(
+					&corev1.Namespace{ObjectMeta: metav1.ObjectMeta{Name: "default"}},
+				)
+			cl := clientBuilder.Build()
+			broadcaster := record.NewBroadcaster()
+			recorder := broadcaster.NewRecorder(scheme,
+				corev1.EventSource{Component: constants.AdmissionName})
+			cqCache := cache.New(cl)
+			qManager := queue.NewManager(cl, cqCache)
+			// Workloads are loaded into queues or clusterQueues as we add them.
+			for _, q := range queues {
+				if err := qManager.AddLocalQueue(ctx, &q); err != nil {
+					t.Fatalf("Inserting queue %s/%s in manager: %v", q.Namespace, q.Name, err)
+				}
+			}
+			for i := range resourceFlavors {
+				cqCache.AddOrUpdateResourceFlavor(resourceFlavors[i])
+			}
+			for _, cq := range tc.cqs {
+				if err := cqCache.AddClusterQueue(ctx, &cq); err != nil {
+					t.Fatalf("Inserting clusterQueue %s in cache: %v", cq.Name, err)
+				}
+				if err := qManager.AddClusterQueue(ctx, &cq); err != nil {
+					t.Fatalf("Inserting clusterQueue %s in manager: %v", cq.Name, err)
+				}
+			}
+			scheduler := New(qManager, cqCache, cl, recorder)
+			gotScheduled := make(map[string]kueue.Admission)
+			var mu sync.Mutex
+			scheduler.applyAdmission = func(ctx context.Context, w *kueue.Workload) error {
+				mu.Lock()
+				gotScheduled[workload.Key(w)] = *w.Status.Admission
+				mu.Unlock()
+				return nil
+			}
+			wg := sync.WaitGroup{}
+			scheduler.setAdmissionRoutineWrapper(routine.NewWrapper(
+				func() { wg.Add(1) },
+				func() { wg.Done() },
+			))
+			gotPreempted := sets.New[string]()
+			scheduler.preemptor.OverrideApply(func(_ context.Context, w *kueue.Workload) error {
+				mu.Lock()
+				gotPreempted.Insert(workload.Key(w))
+				mu.Unlock()
+				return nil
+			})
+
+			ctx, cancel := context.WithTimeout(ctx, queueingTimeout)
+			go qManager.CleanUpOnContext(ctx)
+			defer cancel()
+
+			scheduler.schedule(ctx)
+			wg.Wait()
+
+			if diff := cmp.Diff(tc.wantPreempted, gotPreempted); diff != "" {
+				t.Errorf("Unexpected preemptions (-want,+got):\n%s", diff)
+			}
+			if diff := cmp.Diff(tc.wantAdmissionsOnceSchedule, gotScheduled); diff != "" {
+				t.Errorf("Unexpected scheduled workloads (-want,+got):\n%s", diff)
+			}
+
+			// should schedule from where preempting failed last time
+			ctx, cancel = context.WithTimeout(ctx, queueingTimeout)
+			go qManager.CleanUpOnContext(ctx)
+			defer cancel()
```

  have removed this

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-21T16:39:14Z

- **pkg/scheduler/scheduler_test.go:1392** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-21T16:39:14Z
```diff
@@ -1263,6 +1264,393 @@ func TestEntryOrdering(t *testing.T) {
 	}
 }
 
+func TestLastSchedulingContext(t *testing.T) {
+	defer features.SetFeatureGateDuringTest(t, features.FlavorFungibility, true)()
+	resourceFlavors := []*kueue.ResourceFlavor{
+		{ObjectMeta: metav1.ObjectMeta{Name: "on-demand"}},
+		{ObjectMeta: metav1.ObjectMeta{Name: "spot"}},
+	}
+	clusterQueue := []kueue.ClusterQueue{
+		*utiltesting.MakeClusterQueue("eng-alpha").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+	}
+	clusterQueue_cohort := []kueue.ClusterQueue{
+		*utiltesting.MakeClusterQueue("eng-cohort-alpha").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+				WhenCanBorrow:  kueue.Borrow,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+		*utiltesting.MakeClusterQueue("eng-cohort-beta").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+				WhenCanBorrow:  kueue.Borrow,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+		*utiltesting.MakeClusterQueue("eng-cohort-theta").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.TryNextFlavor,
+				WhenCanBorrow:  kueue.TryNextFlavor,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+	}
+
+	queues := []kueue.LocalQueue{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-alpha",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-alpha",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-alpha",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-beta",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-beta",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-theta",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-theta",
+			},
+		},
+	}
+	wl := utiltesting.MakeWorkload("low-1", "default").
+		Request(corev1.ResourceCPU, "50").
+		ReserveQuota(utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+		Admitted(true).
+		Obj()
+	cases := []struct {
+		name                           string
+		cqs                            []kueue.ClusterQueue
+		admittedWorkloads              []kueue.Workload
+		workloads                      []kueue.Workload
+		deletedWorkloads               []kueue.Workload
+		wantPreempted                  sets.Set[string]
+		wantAdmissionsOnFirstSchedule  map[string]kueue.Admission
+		wantAdmissionsOnSecondSchedule map[string]kueue.Admission
```

  Based on what we are comparing below
  ```suggestion
  		wantScheduledOnFirstSchedule  map[string]kueue.Admission
  		wantAdmissionsAfterSecondSchedule map[string]kueue.Admission
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-21T16:40:35Z

- **pkg/workload/workload.go:43** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-21T16:40:35Z
```diff
@@ -39,6 +40,40 @@ var (
 	admissionManagedConditions = []string{kueue.WorkloadAdmitted, kueue.WorkloadEvicted}
 )
 
+type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
```

  Did you think about the possibility of reverting this?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-21T16:42:03Z

/approve
/hold for 2 nits

@tenzen-y do you want to give a sanity check or are you ok with this?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-22T10:11:35Z

@KunWuLuan Thank you for the great work! I left a few comments for the first pass.

Also, could you update the featuregate table?

https://github.com/kubernetes-sigs/kueue/blob/main/site/content/en/docs/installation/_index.md#change-the-feature-gates-configuration

- **apis/kueue/v1beta1/clusterqueue_types.go:311** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-22T07:21:14Z
```diff
@@ -274,6 +278,40 @@ const (
 	PreemptionPolicyLowerOrNewerEqualPriority PreemptionPolicy = "LowerOrNewerEqualPriority"
 )
 
+type FlavorFungibilityPolicy string
+
+const (
+	Borrow        FlavorFungibilityPolicy = "Borrow"
+	Preempt       FlavorFungibilityPolicy = "Preempt"
+	TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
+)
+
+// FlavorFungibility determines whether a workload should try the next flavor
+// before borrowing or preempting in current flavor.
+type FlavorFungibility struct {
+	// whenCanBorrow determines whether a workload should try the next flavor
+	// before borrowing in current flavor. The possible values are:
+	//
+	// - `Borrow` (default): allocate in current flavor if borrowing
+	//   is possible.
+	// - `TryNextFlavor`: try next flavor even if the current
+	//   flavor has enough resources to borrow.
+	//
+	// +kubebuilder:validation:Enum={Borrow,TryNextFlavor}
+	// +kubebuilder:default="Borrow"
+	WhenCanBorrow FlavorFungibilityPolicy `json:"whenCanBorrow,omitempty"`
+	// whenCanPreempt determines whether a workload should try the next flavor
+	// before borrowing in current flavor. The possible values are:
+	//
+	// - `Preempt`: allocate in current flavor if it's possible to preempt some workloads.
+	// - `TryNextFlavor` (default): try next flavor even if there are enough
+	//   candidates for preemption in the current flavor.
+	//
+	// +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
+	// +kubebuilder:default="TryNextFlavor"
```

  IIUC, the existing behavior for preemption is `whenCanPreempt=Preempt`.  So by this PR, default behavior is changed?
  
  Is there any reason to change the default behavior? If it is possible, keeping the default behavior might be better.
  
  @alculquicondor @KunWuLuan  WDYT?

- **pkg/cache/clusterqueue.go:180** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-22T07:30:51Z
```diff
@@ -167,6 +174,18 @@ func (c *ClusterQueue) update(in *kueue.ClusterQueue, resourceFlavors map[kueue.
 		c.Preemption = defaultPreemption
 	}
 
+	if in.Spec.FlavorFungibility != nil {
+		c.FlavorFungibility = *in.Spec.FlavorFungibility
+		if c.FlavorFungibility.WhenCanBorrow == "" {
+			c.FlavorFungibility.WhenCanBorrow = kueue.Borrow
```

  ```suggestion
  			c.FlavorFungibility.WhenCanBorrow = defaultFlavorFungibility.WhenCanBorrow
  ```
  
  For more clarity declare that we set the default value here.

- **pkg/cache/clusterqueue.go:183** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-22T07:31:30Z
```diff
@@ -167,6 +174,18 @@ func (c *ClusterQueue) update(in *kueue.ClusterQueue, resourceFlavors map[kueue.
 		c.Preemption = defaultPreemption
 	}
 
+	if in.Spec.FlavorFungibility != nil {
+		c.FlavorFungibility = *in.Spec.FlavorFungibility
+		if c.FlavorFungibility.WhenCanBorrow == "" {
+			c.FlavorFungibility.WhenCanBorrow = kueue.Borrow
+		}
+		if c.FlavorFungibility.WhenCanPreempt == "" {
+			c.FlavorFungibility.WhenCanPreempt = kueue.TryNextFlavor
```

  ```suggestion
  			c.FlavorFungibility.WhenCanPreempt = defaultFlavorFungibility.WhenCanPreempt
  ```
  
  ditto.

- **pkg/workload/workload.go:43** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-22T07:35:56Z
```diff
@@ -39,6 +40,40 @@ var (
 	admissionManagedConditions = []string{kueue.WorkloadAdmitted, kueue.WorkloadEvicted}
 )
 
+type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
```

  @KunWuLuan Is this change still needed?

- **pkg/cache/cache_test.go:747** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-22T08:16:11Z
```diff
@@ -686,10 +736,15 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 			},
 			wantClusterQueues: map[string]*ClusterQueue{
 				"foo": {
-					Name:              "foo",
-					NamespaceSelector: labels.Everything(),
-					Status:            pending,
-					Preemption:        defaultPreemption,
+					Name:                          "foo",
+					NamespaceSelector:             labels.Everything(),
+					Status:                        pending,
+					Preemption:                    defaultPreemption,
+					AllocatableResourceGeneration: 1,
+					FlavorFungibility: kueue.FlavorFungibility{
+						WhenCanBorrow:  kueue.Borrow,
+						WhenCanPreempt: kueue.TryNextFlavor,
+					},
```

  ```suggestion
  					FlavorFungibility: defaultFlavorFungibility,
  ```

- **pkg/cache/cache_test.go:777** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-22T08:16:32Z
```diff
@@ -711,10 +766,15 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 			},
 			wantClusterQueues: map[string]*ClusterQueue{
 				"foo": {
-					Name:              "foo",
-					NamespaceSelector: labels.Everything(),
-					Status:            active,
-					Preemption:        defaultPreemption,
+					Name:                          "foo",
+					NamespaceSelector:             labels.Everything(),
+					Status:                        active,
+					Preemption:                    defaultPreemption,
+					AllocatableResourceGeneration: 1,
+					FlavorFungibility: kueue.FlavorFungibility{
+						WhenCanBorrow:  kueue.Borrow,
+						WhenCanPreempt: kueue.TryNextFlavor,
+					},
```

  ```suggestion
  					FlavorFungibility: defaultFlavorFungibility,
  ```

- **pkg/cache/cache_test.go:808** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-22T08:16:49Z
```diff
@@ -737,10 +797,15 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 			},
 			wantClusterQueues: map[string]*ClusterQueue{
 				"foo": {
-					Name:              "foo",
-					NamespaceSelector: labels.Everything(),
-					Status:            pending,
-					Preemption:        defaultPreemption,
+					Name:                          "foo",
+					NamespaceSelector:             labels.Everything(),
+					Status:                        pending,
+					Preemption:                    defaultPreemption,
+					AllocatableResourceGeneration: 1,
+					FlavorFungibility: kueue.FlavorFungibility{
+						WhenCanBorrow:  kueue.Borrow,
+						WhenCanPreempt: kueue.TryNextFlavor,
+					},
```

  ```suggestion
  					FlavorFungibility: defaultFlavorFungibility,
  ```

- **pkg/cache/cache_test.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-22T08:21:04Z
  It would be better to add clusterQueue with non-default flavorFungibility (adding clusterQueue, `f`) or replace existing clusterQueue (a-e) with clusterQueue with non-default flavorFungibility.

- **pkg/cache/snapshot_test.go:45** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-22T09:23:10Z
```diff
@@ -41,6 +42,7 @@ var snapCmpOpts = []cmp.Option{
 }
 
 func TestSnapshot(t *testing.T) {
+	defer features.SetFeatureGateDuringTest(t, features.FlavorFungibility, true)()
```

  Why do we need to enable featuregate? In the cache, It seems that we process related to FlavorFungibility even though the feature is enabled or disabled.

- **pkg/util/testing/wrappers.go:366** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-22T09:35:58Z
```diff
@@ -318,6 +318,14 @@ func MakeClusterQueue(name string) *ClusterQueueWrapper {
 		Spec: kueue.ClusterQueueSpec{
 			NamespaceSelector: &metav1.LabelSelector{},
 			QueueingStrategy:  kueue.BestEffortFIFO,
+			Preemption: &kueue.ClusterQueuePreemption{
```

  Please address this comment.

- **pkg/scheduler/flavorassigner/flavorassigner.go:242** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-22T09:38:17Z
```diff
@@ -218,33 +221,56 @@ func (m FlavorAssignmentMode) String() string {
 }
 
 type FlavorAssignment struct {
-	Name   kueue.ResourceFlavorReference
-	Mode   FlavorAssignmentMode
-	borrow int64
+	Name      kueue.ResourceFlavorReference
+	Mode      FlavorAssignmentMode
+	FlavorIdx int
+	borrow    int64
+}
+
+func lastAssignmentOutdated(wl *workload.Info, cq *cache.ClusterQueue) bool {
+	return cq.AllocatableResourceGeneration > wl.LastAssignment.ClusterQueueGeneration ||
+		(cq.Cohort != nil && cq.Cohort.AllocatableResourceGeneration > wl.LastAssignment.CohortGeneration)
 }
 
 // AssignFlavors assigns flavors for each of the resources requested in each pod set.
 // The result for each pod set is accompanied with reasons why the flavor can't
 // be assigned immediately. Each assigned flavor is accompanied with a
 // FlavorAssignmentMode.
 func AssignFlavors(log logr.Logger, wl *workload.Info, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, counts []int32) Assignment {
+	if wl.LastAssignment != nil && lastAssignmentOutdated(wl, cq) {
+		wl.LastAssignment = nil
+	}
+
```

  What happens if `cq.AllocatebleResourceGeneration` is outdated (`cq.AllocatableResourceGeneration < wl.LastAssignment.ClusterQueueGeneration`)?

- **pkg/scheduler/flavorassigner/flavorassigner.go:270** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-22T09:42:43Z
```diff
@@ -218,33 +221,56 @@ func (m FlavorAssignmentMode) String() string {
 }
 
 type FlavorAssignment struct {
-	Name   kueue.ResourceFlavorReference
-	Mode   FlavorAssignmentMode
-	borrow int64
+	Name      kueue.ResourceFlavorReference
+	Mode      FlavorAssignmentMode
+	FlavorIdx int
+	borrow    int64
+}
+
+func lastAssignmentOutdated(wl *workload.Info, cq *cache.ClusterQueue) bool {
+	return cq.AllocatableResourceGeneration > wl.LastAssignment.ClusterQueueGeneration ||
+		(cq.Cohort != nil && cq.Cohort.AllocatableResourceGeneration > wl.LastAssignment.CohortGeneration)
 }
 
 // AssignFlavors assigns flavors for each of the resources requested in each pod set.
 // The result for each pod set is accompanied with reasons why the flavor can't
 // be assigned immediately. Each assigned flavor is accompanied with a
 // FlavorAssignmentMode.
 func AssignFlavors(log logr.Logger, wl *workload.Info, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, counts []int32) Assignment {
+	if wl.LastAssignment != nil && lastAssignmentOutdated(wl, cq) {
+		wl.LastAssignment = nil
+	}
+
 	if len(counts) == 0 {
-		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastAssignment)
 	}
 
 	currentResources := make([]workload.PodSetResources, len(wl.TotalRequests))
 	for i := range wl.TotalRequests {
 		currentResources[i] = *wl.TotalRequests[i].ScaledTo(counts[i])
 	}
-	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastAssignment)
 }
 
-func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue) Assignment {
+func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, lastAssignment *workload.AssigmentClusterQueueState) Assignment {
 	assignment := Assignment{
-		TotalBorrow: make(cache.FlavorResourceQuantities),
+		TotalBorrow: make(workload.FlavorResourceQuantities),
 		PodSets:     make([]PodSetAssignment, 0, len(requests)),
-		Usage:       make(cache.FlavorResourceQuantities),
+		Usage:       make(workload.FlavorResourceQuantities),
+	}
+	if lastAssignment != nil {
+		assignment.LastState = *lastAssignment
+	} else {
+		assignment.LastState = workload.AssigmentClusterQueueState{
+			LastAssignedFlavorIdx:  make([]map[corev1.ResourceName]int, 0, len(podSets)),
+			CohortGeneration:       0,
+			ClusterQueueGeneration: cq.AllocatableResourceGeneration,
+		}
+		if cq.Cohort != nil {
+			assignment.LastState.CohortGeneration = cq.Cohort.AllocatableResourceGeneration
+		}
```

  ```suggestion
  			CohortGeneration:       cq.Cohort.AllocatableResourceGeneration,
  			ClusterQueueGeneration: cq.AllocatableResourceGeneration,
  		}
  ```
  
  Why do we need to confirm if the clusterQueue has a cohort? I think the cache still confirms this.

- **pkg/scheduler/flavorassigner/flavorassigner.go:35** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-22T09:58:01Z
```diff
@@ -32,18 +32,21 @@ import (
 	"k8s.io/component-helpers/scheduling/corev1/nodeaffinity"
 	"k8s.io/utils/ptr"
 
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
```

  ```suggestion
  	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
  ```

- **pkg/scheduler/flavorassigner/flavorassigner.go:469** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-22T10:10:46Z
```diff
@@ -389,18 +433,60 @@ func (a *Assignment) findFlavorForResourceGroup(
 			}
 		}
 
-		if representativeMode > bestAssignmentMode {
-			bestAssignment = assignments
-			bestAssignmentMode = representativeMode
-			if bestAssignmentMode == Fit {
-				// All the resources fit in the cohort, no need to check more flavors.
-				return bestAssignment, nil
+		if features.Enabled(features.FlavorFungibility) {
+			if !shouldTryNextFlavor(representativeMode, cq.FlavorFungibility, needsBorrowing) {
+				bestAssignment = assignments
+				bestAssignmentMode = representativeMode
+				break
+			}
+			if representativeMode > bestAssignmentMode {
+				bestAssignment = assignments
+				bestAssignmentMode = representativeMode
+			}
+		} else {
+			if representativeMode > bestAssignmentMode {
+				bestAssignment = assignments
+				bestAssignmentMode = representativeMode
+				if bestAssignmentMode == Fit {
+					// All the resources fit in the cohort, no need to check more flavors.
+					return bestAssignment, nil
+				}
 			}
 		}
 	}
+
+	for _, assignment := range bestAssignment {
+		if flavorIdx == len(rg.Flavors)-1 {
+			// we have reach the last flavor, try from the first flavor next time
+			assignment.FlavorIdx = -1
+		} else {
+			assignment.FlavorIdx = flavorIdx
+		}
+	}
+	if bestAssignmentMode == Fit {
+		return bestAssignment, nil
+	}
```

  Are these needed if the `FlavorFungibility` featuregate is disabled?

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-25T03:00:37Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:242** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-25T03:00:36Z
```diff
@@ -218,33 +221,56 @@ func (m FlavorAssignmentMode) String() string {
 }
 
 type FlavorAssignment struct {
-	Name   kueue.ResourceFlavorReference
-	Mode   FlavorAssignmentMode
-	borrow int64
+	Name      kueue.ResourceFlavorReference
+	Mode      FlavorAssignmentMode
+	FlavorIdx int
+	borrow    int64
+}
+
+func lastAssignmentOutdated(wl *workload.Info, cq *cache.ClusterQueue) bool {
+	return cq.AllocatableResourceGeneration > wl.LastAssignment.ClusterQueueGeneration ||
+		(cq.Cohort != nil && cq.Cohort.AllocatableResourceGeneration > wl.LastAssignment.CohortGeneration)
 }
 
 // AssignFlavors assigns flavors for each of the resources requested in each pod set.
 // The result for each pod set is accompanied with reasons why the flavor can't
 // be assigned immediately. Each assigned flavor is accompanied with a
 // FlavorAssignmentMode.
 func AssignFlavors(log logr.Logger, wl *workload.Info, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, counts []int32) Assignment {
+	if wl.LastAssignment != nil && lastAssignmentOutdated(wl, cq) {
+		wl.LastAssignment = nil
+	}
+
```

  We will record the last flavor index in `wl.LastAssignment` and start from there next time. When `cq.AllocatebleResourceGeneration` is outdated, we set `wl.LastAssignment = nil` so that we will start from the first flavor.

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-25T03:06:35Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:270** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-25T03:06:35Z
```diff
@@ -218,33 +221,56 @@ func (m FlavorAssignmentMode) String() string {
 }
 
 type FlavorAssignment struct {
-	Name   kueue.ResourceFlavorReference
-	Mode   FlavorAssignmentMode
-	borrow int64
+	Name      kueue.ResourceFlavorReference
+	Mode      FlavorAssignmentMode
+	FlavorIdx int
+	borrow    int64
+}
+
+func lastAssignmentOutdated(wl *workload.Info, cq *cache.ClusterQueue) bool {
+	return cq.AllocatableResourceGeneration > wl.LastAssignment.ClusterQueueGeneration ||
+		(cq.Cohort != nil && cq.Cohort.AllocatableResourceGeneration > wl.LastAssignment.CohortGeneration)
 }
 
 // AssignFlavors assigns flavors for each of the resources requested in each pod set.
 // The result for each pod set is accompanied with reasons why the flavor can't
 // be assigned immediately. Each assigned flavor is accompanied with a
 // FlavorAssignmentMode.
 func AssignFlavors(log logr.Logger, wl *workload.Info, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, counts []int32) Assignment {
+	if wl.LastAssignment != nil && lastAssignmentOutdated(wl, cq) {
+		wl.LastAssignment = nil
+	}
+
 	if len(counts) == 0 {
-		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastAssignment)
 	}
 
 	currentResources := make([]workload.PodSetResources, len(wl.TotalRequests))
 	for i := range wl.TotalRequests {
 		currentResources[i] = *wl.TotalRequests[i].ScaledTo(counts[i])
 	}
-	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastAssignment)
 }
 
-func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue) Assignment {
+func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, lastAssignment *workload.AssigmentClusterQueueState) Assignment {
 	assignment := Assignment{
-		TotalBorrow: make(cache.FlavorResourceQuantities),
+		TotalBorrow: make(workload.FlavorResourceQuantities),
 		PodSets:     make([]PodSetAssignment, 0, len(requests)),
-		Usage:       make(cache.FlavorResourceQuantities),
+		Usage:       make(workload.FlavorResourceQuantities),
+	}
+	if lastAssignment != nil {
+		assignment.LastState = *lastAssignment
+	} else {
+		assignment.LastState = workload.AssigmentClusterQueueState{
+			LastAssignedFlavorIdx:  make([]map[corev1.ResourceName]int, 0, len(podSets)),
+			CohortGeneration:       0,
+			ClusterQueueGeneration: cq.AllocatableResourceGeneration,
+		}
+		if cq.Cohort != nil {
+			assignment.LastState.CohortGeneration = cq.Cohort.AllocatableResourceGeneration
+		}
```

  `cq.Cohort` is set in `cache.SnapShot()`, if the cluster queue is not the member of any cohort, `cq.Cohort` would be nil

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-25T03:44:16Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:469** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-25T03:44:15Z
```diff
@@ -389,18 +433,60 @@ func (a *Assignment) findFlavorForResourceGroup(
 			}
 		}
 
-		if representativeMode > bestAssignmentMode {
-			bestAssignment = assignments
-			bestAssignmentMode = representativeMode
-			if bestAssignmentMode == Fit {
-				// All the resources fit in the cohort, no need to check more flavors.
-				return bestAssignment, nil
+		if features.Enabled(features.FlavorFungibility) {
+			if !shouldTryNextFlavor(representativeMode, cq.FlavorFungibility, needsBorrowing) {
+				bestAssignment = assignments
+				bestAssignmentMode = representativeMode
+				break
+			}
+			if representativeMode > bestAssignmentMode {
+				bestAssignment = assignments
+				bestAssignmentMode = representativeMode
+			}
+		} else {
+			if representativeMode > bestAssignmentMode {
+				bestAssignment = assignments
+				bestAssignmentMode = representativeMode
+				if bestAssignmentMode == Fit {
+					// All the resources fit in the cohort, no need to check more flavors.
+					return bestAssignment, nil
+				}
 			}
 		}
 	}
+
+	for _, assignment := range bestAssignment {
+		if flavorIdx == len(rg.Flavors)-1 {
+			// we have reach the last flavor, try from the first flavor next time
+			assignment.FlavorIdx = -1
+		} else {
+			assignment.FlavorIdx = flavorIdx
+		}
+	}
+	if bestAssignmentMode == Fit {
+		return bestAssignment, nil
+	}
```

  done

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-25T03:44:17Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:35** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-25T03:44:17Z
```diff
@@ -32,18 +32,21 @@ import (
 	"k8s.io/component-helpers/scheduling/corev1/nodeaffinity"
 	"k8s.io/utils/ptr"
 
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
```

  done

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-25T03:44:32Z

- **pkg/cache/snapshot_test.go:45** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-25T03:44:32Z
```diff
@@ -41,6 +42,7 @@ var snapCmpOpts = []cmp.Option{
 }
 
 func TestSnapshot(t *testing.T) {
+	defer features.SetFeatureGateDuringTest(t, features.FlavorFungibility, true)()
```

  this can be removed

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-25T03:44:40Z

- **pkg/cache/cache_test.go:808** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-25T03:44:40Z
```diff
@@ -737,10 +797,15 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 			},
 			wantClusterQueues: map[string]*ClusterQueue{
 				"foo": {
-					Name:              "foo",
-					NamespaceSelector: labels.Everything(),
-					Status:            pending,
-					Preemption:        defaultPreemption,
+					Name:                          "foo",
+					NamespaceSelector:             labels.Everything(),
+					Status:                        pending,
+					Preemption:                    defaultPreemption,
+					AllocatableResourceGeneration: 1,
+					FlavorFungibility: kueue.FlavorFungibility{
+						WhenCanBorrow:  kueue.Borrow,
+						WhenCanPreempt: kueue.TryNextFlavor,
+					},
```

  done

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-25T03:44:47Z

- **pkg/cache/cache_test.go:777** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-25T03:44:47Z
```diff
@@ -711,10 +766,15 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 			},
 			wantClusterQueues: map[string]*ClusterQueue{
 				"foo": {
-					Name:              "foo",
-					NamespaceSelector: labels.Everything(),
-					Status:            active,
-					Preemption:        defaultPreemption,
+					Name:                          "foo",
+					NamespaceSelector:             labels.Everything(),
+					Status:                        active,
+					Preemption:                    defaultPreemption,
+					AllocatableResourceGeneration: 1,
+					FlavorFungibility: kueue.FlavorFungibility{
+						WhenCanBorrow:  kueue.Borrow,
+						WhenCanPreempt: kueue.TryNextFlavor,
+					},
```

  done

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-25T03:44:56Z

- **pkg/cache/cache_test.go:747** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-25T03:44:55Z
```diff
@@ -686,10 +736,15 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 			},
 			wantClusterQueues: map[string]*ClusterQueue{
 				"foo": {
-					Name:              "foo",
-					NamespaceSelector: labels.Everything(),
-					Status:            pending,
-					Preemption:        defaultPreemption,
+					Name:                          "foo",
+					NamespaceSelector:             labels.Everything(),
+					Status:                        pending,
+					Preemption:                    defaultPreemption,
+					AllocatableResourceGeneration: 1,
+					FlavorFungibility: kueue.FlavorFungibility{
+						WhenCanBorrow:  kueue.Borrow,
+						WhenCanPreempt: kueue.TryNextFlavor,
+					},
```

  done

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-25T03:45:21Z

- **pkg/cache/clusterqueue.go:183** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-25T03:45:21Z
```diff
@@ -167,6 +174,18 @@ func (c *ClusterQueue) update(in *kueue.ClusterQueue, resourceFlavors map[kueue.
 		c.Preemption = defaultPreemption
 	}
 
+	if in.Spec.FlavorFungibility != nil {
+		c.FlavorFungibility = *in.Spec.FlavorFungibility
+		if c.FlavorFungibility.WhenCanBorrow == "" {
+			c.FlavorFungibility.WhenCanBorrow = kueue.Borrow
+		}
+		if c.FlavorFungibility.WhenCanPreempt == "" {
+			c.FlavorFungibility.WhenCanPreempt = kueue.TryNextFlavor
```

  done

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-25T03:45:27Z

- **pkg/cache/clusterqueue.go:180** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-25T03:45:27Z
```diff
@@ -167,6 +174,18 @@ func (c *ClusterQueue) update(in *kueue.ClusterQueue, resourceFlavors map[kueue.
 		c.Preemption = defaultPreemption
 	}
 
+	if in.Spec.FlavorFungibility != nil {
+		c.FlavorFungibility = *in.Spec.FlavorFungibility
+		if c.FlavorFungibility.WhenCanBorrow == "" {
+			c.FlavorFungibility.WhenCanBorrow = kueue.Borrow
```

  done, thanks

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-09-25T03:45:32Z

- **apis/kueue/v1beta1/clusterqueue_types.go:311** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-09-25T03:45:31Z
```diff
@@ -274,6 +278,40 @@ const (
 	PreemptionPolicyLowerOrNewerEqualPriority PreemptionPolicy = "LowerOrNewerEqualPriority"
 )
 
+type FlavorFungibilityPolicy string
+
+const (
+	Borrow        FlavorFungibilityPolicy = "Borrow"
+	Preempt       FlavorFungibilityPolicy = "Preempt"
+	TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
+)
+
+// FlavorFungibility determines whether a workload should try the next flavor
+// before borrowing or preempting in current flavor.
+type FlavorFungibility struct {
+	// whenCanBorrow determines whether a workload should try the next flavor
+	// before borrowing in current flavor. The possible values are:
+	//
+	// - `Borrow` (default): allocate in current flavor if borrowing
+	//   is possible.
+	// - `TryNextFlavor`: try next flavor even if the current
+	//   flavor has enough resources to borrow.
+	//
+	// +kubebuilder:validation:Enum={Borrow,TryNextFlavor}
+	// +kubebuilder:default="Borrow"
+	WhenCanBorrow FlavorFungibilityPolicy `json:"whenCanBorrow,omitempty"`
+	// whenCanPreempt determines whether a workload should try the next flavor
+	// before borrowing in current flavor. The possible values are:
+	//
+	// - `Preempt`: allocate in current flavor if it's possible to preempt some workloads.
+	// - `TryNextFlavor` (default): try next flavor even if there are enough
+	//   candidates for preemption in the current flavor.
+	//
+	// +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
+	// +kubebuilder:default="TryNextFlavor"
```

  When `whenCanPreempt=Preempt`, it means that we will pause at each flavor where usage+request<nominal+borrowingLimit and attempt to preempt. When `whenCanPreempt=tryNextFlavor`, it means that we will try to preempt after traversing all flavors and when mode=Preempt. `whenCanPreempt=tryNextFlavor` is the current default behavior because we will traverse all flavors and break the loop until the best mode is `Fit` or all flavors have been .

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-27T09:43:28Z

- **apis/kueue/v1beta1/clusterqueue_types.go:311** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-27T09:28:17Z
```diff
@@ -274,6 +278,40 @@ const (
 	PreemptionPolicyLowerOrNewerEqualPriority PreemptionPolicy = "LowerOrNewerEqualPriority"
 )
 
+type FlavorFungibilityPolicy string
+
+const (
+	Borrow        FlavorFungibilityPolicy = "Borrow"
+	Preempt       FlavorFungibilityPolicy = "Preempt"
+	TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
+)
+
+// FlavorFungibility determines whether a workload should try the next flavor
+// before borrowing or preempting in current flavor.
+type FlavorFungibility struct {
+	// whenCanBorrow determines whether a workload should try the next flavor
+	// before borrowing in current flavor. The possible values are:
+	//
+	// - `Borrow` (default): allocate in current flavor if borrowing
+	//   is possible.
+	// - `TryNextFlavor`: try next flavor even if the current
+	//   flavor has enough resources to borrow.
+	//
+	// +kubebuilder:validation:Enum={Borrow,TryNextFlavor}
+	// +kubebuilder:default="Borrow"
+	WhenCanBorrow FlavorFungibilityPolicy `json:"whenCanBorrow,omitempty"`
+	// whenCanPreempt determines whether a workload should try the next flavor
+	// before borrowing in current flavor. The possible values are:
+	//
+	// - `Preempt`: allocate in current flavor if it's possible to preempt some workloads.
+	// - `TryNextFlavor` (default): try next flavor even if there are enough
+	//   candidates for preemption in the current flavor.
+	//
+	// +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
+	// +kubebuilder:default="TryNextFlavor"
```

  Ah, right. The flavorassigner traverses all flavors if the mode is `Preempt`.
  Thank you for the clarifications, and Sorry for the confusion.
  
  https://github.com/kubernetes-sigs/kueue/blob/c4197436d17e6241cd71d880c68d4329fa2f65c8/pkg/scheduler/flavorassigner/flavorassigner.go#L392-L399

- **pkg/cache/cache_test.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-27T09:28:32Z
  I'm still waiting for this.

- **pkg/scheduler/flavorassigner/flavorassigner.go:242** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-27T09:33:13Z
```diff
@@ -218,33 +221,56 @@ func (m FlavorAssignmentMode) String() string {
 }
 
 type FlavorAssignment struct {
-	Name   kueue.ResourceFlavorReference
-	Mode   FlavorAssignmentMode
-	borrow int64
+	Name      kueue.ResourceFlavorReference
+	Mode      FlavorAssignmentMode
+	FlavorIdx int
+	borrow    int64
+}
+
+func lastAssignmentOutdated(wl *workload.Info, cq *cache.ClusterQueue) bool {
+	return cq.AllocatableResourceGeneration > wl.LastAssignment.ClusterQueueGeneration ||
+		(cq.Cohort != nil && cq.Cohort.AllocatableResourceGeneration > wl.LastAssignment.CohortGeneration)
 }
 
 // AssignFlavors assigns flavors for each of the resources requested in each pod set.
 // The result for each pod set is accompanied with reasons why the flavor can't
 // be assigned immediately. Each assigned flavor is accompanied with a
 // FlavorAssignmentMode.
 func AssignFlavors(log logr.Logger, wl *workload.Info, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, counts []int32) Assignment {
+	if wl.LastAssignment != nil && lastAssignmentOutdated(wl, cq) {
+		wl.LastAssignment = nil
+	}
+
```

  That makes sense. Thank you for clarifying :)

- **pkg/scheduler/flavorassigner/flavorassigner.go:270** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-27T09:42:14Z
```diff
@@ -218,33 +221,56 @@ func (m FlavorAssignmentMode) String() string {
 }
 
 type FlavorAssignment struct {
-	Name   kueue.ResourceFlavorReference
-	Mode   FlavorAssignmentMode
-	borrow int64
+	Name      kueue.ResourceFlavorReference
+	Mode      FlavorAssignmentMode
+	FlavorIdx int
+	borrow    int64
+}
+
+func lastAssignmentOutdated(wl *workload.Info, cq *cache.ClusterQueue) bool {
+	return cq.AllocatableResourceGeneration > wl.LastAssignment.ClusterQueueGeneration ||
+		(cq.Cohort != nil && cq.Cohort.AllocatableResourceGeneration > wl.LastAssignment.CohortGeneration)
 }
 
 // AssignFlavors assigns flavors for each of the resources requested in each pod set.
 // The result for each pod set is accompanied with reasons why the flavor can't
 // be assigned immediately. Each assigned flavor is accompanied with a
 // FlavorAssignmentMode.
 func AssignFlavors(log logr.Logger, wl *workload.Info, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, counts []int32) Assignment {
+	if wl.LastAssignment != nil && lastAssignmentOutdated(wl, cq) {
+		wl.LastAssignment = nil
+	}
+
 	if len(counts) == 0 {
-		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+		return assignFlavors(log, wl.TotalRequests, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastAssignment)
 	}
 
 	currentResources := make([]workload.PodSetResources, len(wl.TotalRequests))
 	for i := range wl.TotalRequests {
 		currentResources[i] = *wl.TotalRequests[i].ScaledTo(counts[i])
 	}
-	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq)
+	return assignFlavors(log, currentResources, wl.Obj.Spec.PodSets, resourceFlavors, cq, wl.LastAssignment)
 }
 
-func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue) Assignment {
+func assignFlavors(log logr.Logger, requests []workload.PodSetResources, podSets []kueue.PodSet, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, cq *cache.ClusterQueue, lastAssignment *workload.AssigmentClusterQueueState) Assignment {
 	assignment := Assignment{
-		TotalBorrow: make(cache.FlavorResourceQuantities),
+		TotalBorrow: make(workload.FlavorResourceQuantities),
 		PodSets:     make([]PodSetAssignment, 0, len(requests)),
-		Usage:       make(cache.FlavorResourceQuantities),
+		Usage:       make(workload.FlavorResourceQuantities),
+	}
+	if lastAssignment != nil {
+		assignment.LastState = *lastAssignment
+	} else {
+		assignment.LastState = workload.AssigmentClusterQueueState{
+			LastAssignedFlavorIdx:  make([]map[corev1.ResourceName]int, 0, len(podSets)),
+			CohortGeneration:       0,
+			ClusterQueueGeneration: cq.AllocatableResourceGeneration,
+		}
+		if cq.Cohort != nil {
+			assignment.LastState.CohortGeneration = cq.Cohort.AllocatableResourceGeneration
+		}
```

  Ah, it's correct. I was confused `cq.AllocatableResourceGeneration` with `cq.Cohort.AllocatableResourceGeneration`.

- **pkg/util/testing/wrappers.go:366** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-27T09:43:26Z
```diff
@@ -318,6 +318,14 @@ func MakeClusterQueue(name string) *ClusterQueueWrapper {
 		Spec: kueue.ClusterQueueSpec{
 			NamespaceSelector: &metav1.LabelSelector{},
 			QueueingStrategy:  kueue.BestEffortFIFO,
+			Preemption: &kueue.ClusterQueuePreemption{
```

  I'm still waiting for this.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-27T11:54:24Z

I reviewed everything.

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1700** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-27T10:14:42Z
```diff
@@ -1571,6 +1575,278 @@ func TestAssignFlavors(t *testing.T) {
 			},
 			wantRepMode: Fit,
 		},
+		"preempt before try next flavor": {
+			wlPods: []kueue.PodSet{
+				*utiltesting.MakePodSet("main", 1).
+					Request(corev1.ResourceCPU, "9").
+					Obj(),
+			},
+			clusterQueue: cache.ClusterQueue{
+				FlavorFungibility: kueue.FlavorFungibility{
+					WhenCanBorrow:  kueue.Borrow,
+					WhenCanPreempt: kueue.Preempt,
+				},
+				ResourceGroups: []cache.ResourceGroup{{
+					CoveredResources: sets.New(corev1.ResourceCPU, corev1.ResourcePods),
+					Flavors: []cache.FlavorQuotas{{
+						Name: "one",
+						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
+							corev1.ResourcePods: {Nominal: 10},
+							corev1.ResourceCPU:  {Nominal: 10000},
+						},
+					}, {
+						Name: "two",
+						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
+							corev1.ResourcePods: {Nominal: 10},
+							corev1.ResourceCPU:  {Nominal: 10000},
+						},
+					}},
+				}},
+				Usage: workload.FlavorResourceQuantities{
+					"one": {corev1.ResourceCPU: 2000},
+				},
+			},
+			wantRepMode: Preempt,
+			wantAssignment: Assignment{
+				PodSets: []PodSetAssignment{{
+					Name: "main",
+					Flavors: ResourceAssignment{
+						corev1.ResourceCPU:  {Name: "one", Mode: Preempt},
+						corev1.ResourcePods: {Name: "one", Mode: Fit},
+					},
+					Requests: corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("9000m"),
+						corev1.ResourcePods: resource.MustParse("1"),
+					},
+					Status: &Status{
+						reasons: []string{"insufficient unused quota for cpu in flavor one, 1 more needed"},
+					},
+					Count: 1,
+				}},
+				Usage: workload.FlavorResourceQuantities{"one": {"cpu": 9000, "pods": 1}},
+			},
+		},
+		"preempt try next flavor": {
+			wlPods: []kueue.PodSet{
+				*utiltesting.MakePodSet("main", 1).
+					Request(corev1.ResourceCPU, "9").
+					Obj(),
+			},
+			clusterQueue: cache.ClusterQueue{
+				FlavorFungibility: defaultFlavorFungibility,
+				ResourceGroups: []cache.ResourceGroup{{
+					CoveredResources: sets.New(corev1.ResourceCPU, corev1.ResourcePods),
+					Flavors: []cache.FlavorQuotas{{
+						Name: "one",
+						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
+							corev1.ResourcePods: {Nominal: 10},
+							corev1.ResourceCPU:  {Nominal: 10000},
+						},
+					}, {
+						Name: "two",
+						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
+							corev1.ResourcePods: {Nominal: 10},
+							corev1.ResourceCPU:  {Nominal: 10000},
+						},
+					}},
+				}},
+				Usage: workload.FlavorResourceQuantities{
+					"one": {corev1.ResourceCPU: 2000},
+				},
+			},
+			wantRepMode: Fit,
+			wantAssignment: Assignment{
+				PodSets: []PodSetAssignment{{
+					Name: "main",
+					Flavors: ResourceAssignment{
+						corev1.ResourceCPU:  {Name: "two", Mode: Fit},
+						corev1.ResourcePods: {Name: "two", Mode: Fit},
+					},
+					Requests: corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("9000m"),
+						corev1.ResourcePods: resource.MustParse("1"),
+					},
+					Count: 1,
+				}},
+				Usage: workload.FlavorResourceQuantities{"two": {"cpu": 9000, "pods": 1}},
+			},
+		},
+		"borrow try next flavor, found the first flavor": {
+			wlPods: []kueue.PodSet{
+				*utiltesting.MakePodSet("main", 1).
+					Request(corev1.ResourceCPU, "9").
+					Obj(),
+			},
+			clusterQueue: cache.ClusterQueue{
+				Cohort: &cache.Cohort{
+					Usage: workload.FlavorResourceQuantities{
+						"one": {corev1.ResourceCPU: 2000},
+					},
+					RequestableResources: workload.FlavorResourceQuantities{
+						"one": {corev1.ResourceCPU: 11000, corev1.ResourcePods: 10},
+						"two": {corev1.ResourceCPU: 1000, corev1.ResourcePods: 10},
+					},
+				},
+				FlavorFungibility: kueue.FlavorFungibility{
+					WhenCanBorrow:  kueue.TryNextFlavor,
+					WhenCanPreempt: kueue.TryNextFlavor,
+				},
+				ResourceGroups: []cache.ResourceGroup{{
+					CoveredResources: sets.New(corev1.ResourceCPU, corev1.ResourcePods),
+					Flavors: []cache.FlavorQuotas{{
+						Name: "one",
+						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
+							corev1.ResourcePods: {Nominal: 10},
+							corev1.ResourceCPU:  {Nominal: 10000, BorrowingLimit: ptr.To(int64(1000))},
```

  ```suggestion
  							corev1.ResourceCPU:  {Nominal: 10000, BorrowingLimit: ptr.To[int64](1000)},
  ```
  
  Using type argument instead of casting would be better.

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1760** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-27T10:16:52Z
```diff
@@ -1571,6 +1575,278 @@ func TestAssignFlavors(t *testing.T) {
 			},
 			wantRepMode: Fit,
 		},
+		"preempt before try next flavor": {
+			wlPods: []kueue.PodSet{
+				*utiltesting.MakePodSet("main", 1).
+					Request(corev1.ResourceCPU, "9").
+					Obj(),
+			},
+			clusterQueue: cache.ClusterQueue{
+				FlavorFungibility: kueue.FlavorFungibility{
+					WhenCanBorrow:  kueue.Borrow,
+					WhenCanPreempt: kueue.Preempt,
+				},
+				ResourceGroups: []cache.ResourceGroup{{
+					CoveredResources: sets.New(corev1.ResourceCPU, corev1.ResourcePods),
+					Flavors: []cache.FlavorQuotas{{
+						Name: "one",
+						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
+							corev1.ResourcePods: {Nominal: 10},
+							corev1.ResourceCPU:  {Nominal: 10000},
+						},
+					}, {
+						Name: "two",
+						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
+							corev1.ResourcePods: {Nominal: 10},
+							corev1.ResourceCPU:  {Nominal: 10000},
+						},
+					}},
+				}},
+				Usage: workload.FlavorResourceQuantities{
+					"one": {corev1.ResourceCPU: 2000},
+				},
+			},
+			wantRepMode: Preempt,
+			wantAssignment: Assignment{
+				PodSets: []PodSetAssignment{{
+					Name: "main",
+					Flavors: ResourceAssignment{
+						corev1.ResourceCPU:  {Name: "one", Mode: Preempt},
+						corev1.ResourcePods: {Name: "one", Mode: Fit},
+					},
+					Requests: corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("9000m"),
+						corev1.ResourcePods: resource.MustParse("1"),
+					},
+					Status: &Status{
+						reasons: []string{"insufficient unused quota for cpu in flavor one, 1 more needed"},
+					},
+					Count: 1,
+				}},
+				Usage: workload.FlavorResourceQuantities{"one": {"cpu": 9000, "pods": 1}},
+			},
+		},
+		"preempt try next flavor": {
+			wlPods: []kueue.PodSet{
+				*utiltesting.MakePodSet("main", 1).
+					Request(corev1.ResourceCPU, "9").
+					Obj(),
+			},
+			clusterQueue: cache.ClusterQueue{
+				FlavorFungibility: defaultFlavorFungibility,
+				ResourceGroups: []cache.ResourceGroup{{
+					CoveredResources: sets.New(corev1.ResourceCPU, corev1.ResourcePods),
+					Flavors: []cache.FlavorQuotas{{
+						Name: "one",
+						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
+							corev1.ResourcePods: {Nominal: 10},
+							corev1.ResourceCPU:  {Nominal: 10000},
+						},
+					}, {
+						Name: "two",
+						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
+							corev1.ResourcePods: {Nominal: 10},
+							corev1.ResourceCPU:  {Nominal: 10000},
+						},
+					}},
+				}},
+				Usage: workload.FlavorResourceQuantities{
+					"one": {corev1.ResourceCPU: 2000},
+				},
+			},
+			wantRepMode: Fit,
+			wantAssignment: Assignment{
+				PodSets: []PodSetAssignment{{
+					Name: "main",
+					Flavors: ResourceAssignment{
+						corev1.ResourceCPU:  {Name: "two", Mode: Fit},
+						corev1.ResourcePods: {Name: "two", Mode: Fit},
+					},
+					Requests: corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("9000m"),
+						corev1.ResourcePods: resource.MustParse("1"),
+					},
+					Count: 1,
+				}},
+				Usage: workload.FlavorResourceQuantities{"two": {"cpu": 9000, "pods": 1}},
+			},
+		},
+		"borrow try next flavor, found the first flavor": {
+			wlPods: []kueue.PodSet{
+				*utiltesting.MakePodSet("main", 1).
+					Request(corev1.ResourceCPU, "9").
+					Obj(),
+			},
+			clusterQueue: cache.ClusterQueue{
+				Cohort: &cache.Cohort{
+					Usage: workload.FlavorResourceQuantities{
+						"one": {corev1.ResourceCPU: 2000},
+					},
+					RequestableResources: workload.FlavorResourceQuantities{
+						"one": {corev1.ResourceCPU: 11000, corev1.ResourcePods: 10},
+						"two": {corev1.ResourceCPU: 1000, corev1.ResourcePods: 10},
+					},
+				},
+				FlavorFungibility: kueue.FlavorFungibility{
+					WhenCanBorrow:  kueue.TryNextFlavor,
+					WhenCanPreempt: kueue.TryNextFlavor,
+				},
+				ResourceGroups: []cache.ResourceGroup{{
+					CoveredResources: sets.New(corev1.ResourceCPU, corev1.ResourcePods),
+					Flavors: []cache.FlavorQuotas{{
+						Name: "one",
+						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
+							corev1.ResourcePods: {Nominal: 10},
+							corev1.ResourceCPU:  {Nominal: 10000, BorrowingLimit: ptr.To(int64(1000))},
+						},
+					}, {
+						Name: "two",
+						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
+							corev1.ResourcePods: {Nominal: 10},
+							corev1.ResourceCPU:  {Nominal: 1000},
+						},
+					}},
+				}},
+				Usage: workload.FlavorResourceQuantities{
+					"one": {corev1.ResourceCPU: 2000},
+				},
+			},
+			wantRepMode: Fit,
+			wantAssignment: Assignment{
+				TotalBorrow: workload.FlavorResourceQuantities{"one": {"cpu": 1000}},
+				PodSets: []PodSetAssignment{{
+					Name: "main",
+					Flavors: ResourceAssignment{
+						corev1.ResourceCPU:  {Name: "one", Mode: Fit},
+						corev1.ResourcePods: {Name: "one", Mode: Fit},
+					},
+					Requests: corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("9000m"),
+						corev1.ResourcePods: resource.MustParse("1"),
+					},
+					Count: 1,
+				}},
+				Usage: workload.FlavorResourceQuantities{
+					"one": {corev1.ResourceCPU: 9000, corev1.ResourcePods: 1},
+				},
+			},
+		},
+		"borrow try next flavor, found the second flavor": {
+			wlPods: []kueue.PodSet{
+				*utiltesting.MakePodSet("main", 1).
+					Request(corev1.ResourceCPU, "9").
+					Obj(),
+			},
+			clusterQueue: cache.ClusterQueue{
+				Cohort: &cache.Cohort{
+					Usage: workload.FlavorResourceQuantities{
+						"one": {corev1.ResourceCPU: 2000},
+					},
+					RequestableResources: workload.FlavorResourceQuantities{
+						"one": {corev1.ResourceCPU: 11000, corev1.ResourcePods: 10},
+						"two": {corev1.ResourceCPU: 10000, corev1.ResourcePods: 10},
+					},
+				},
+				FlavorFungibility: kueue.FlavorFungibility{
+					WhenCanBorrow:  kueue.TryNextFlavor,
+					WhenCanPreempt: kueue.TryNextFlavor,
+				},
+				ResourceGroups: []cache.ResourceGroup{{
+					CoveredResources: sets.New(corev1.ResourceCPU, corev1.ResourcePods),
+					Flavors: []cache.FlavorQuotas{{
+						Name: "one",
+						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
+							corev1.ResourcePods: {Nominal: 10},
+							corev1.ResourceCPU:  {Nominal: 10000, BorrowingLimit: ptr.To(int64(1000))},
```

  ```suggestion
  							corev1.ResourceCPU:  {Nominal: 10000, BorrowingLimit: ptr.To[int64](1000)},
  ```
  
  ditto.

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1816** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-27T10:17:37Z
```diff
@@ -1571,6 +1575,278 @@ func TestAssignFlavors(t *testing.T) {
 			},
 			wantRepMode: Fit,
 		},
+		"preempt before try next flavor": {
+			wlPods: []kueue.PodSet{
+				*utiltesting.MakePodSet("main", 1).
+					Request(corev1.ResourceCPU, "9").
+					Obj(),
+			},
+			clusterQueue: cache.ClusterQueue{
+				FlavorFungibility: kueue.FlavorFungibility{
+					WhenCanBorrow:  kueue.Borrow,
+					WhenCanPreempt: kueue.Preempt,
+				},
+				ResourceGroups: []cache.ResourceGroup{{
+					CoveredResources: sets.New(corev1.ResourceCPU, corev1.ResourcePods),
+					Flavors: []cache.FlavorQuotas{{
+						Name: "one",
+						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
+							corev1.ResourcePods: {Nominal: 10},
+							corev1.ResourceCPU:  {Nominal: 10000},
+						},
+					}, {
+						Name: "two",
+						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
+							corev1.ResourcePods: {Nominal: 10},
+							corev1.ResourceCPU:  {Nominal: 10000},
+						},
+					}},
+				}},
+				Usage: workload.FlavorResourceQuantities{
+					"one": {corev1.ResourceCPU: 2000},
+				},
+			},
+			wantRepMode: Preempt,
+			wantAssignment: Assignment{
+				PodSets: []PodSetAssignment{{
+					Name: "main",
+					Flavors: ResourceAssignment{
+						corev1.ResourceCPU:  {Name: "one", Mode: Preempt},
+						corev1.ResourcePods: {Name: "one", Mode: Fit},
+					},
+					Requests: corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("9000m"),
+						corev1.ResourcePods: resource.MustParse("1"),
+					},
+					Status: &Status{
+						reasons: []string{"insufficient unused quota for cpu in flavor one, 1 more needed"},
+					},
+					Count: 1,
+				}},
+				Usage: workload.FlavorResourceQuantities{"one": {"cpu": 9000, "pods": 1}},
+			},
+		},
+		"preempt try next flavor": {
+			wlPods: []kueue.PodSet{
+				*utiltesting.MakePodSet("main", 1).
+					Request(corev1.ResourceCPU, "9").
+					Obj(),
+			},
+			clusterQueue: cache.ClusterQueue{
+				FlavorFungibility: defaultFlavorFungibility,
+				ResourceGroups: []cache.ResourceGroup{{
+					CoveredResources: sets.New(corev1.ResourceCPU, corev1.ResourcePods),
+					Flavors: []cache.FlavorQuotas{{
+						Name: "one",
+						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
+							corev1.ResourcePods: {Nominal: 10},
+							corev1.ResourceCPU:  {Nominal: 10000},
+						},
+					}, {
+						Name: "two",
+						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
+							corev1.ResourcePods: {Nominal: 10},
+							corev1.ResourceCPU:  {Nominal: 10000},
+						},
+					}},
+				}},
+				Usage: workload.FlavorResourceQuantities{
+					"one": {corev1.ResourceCPU: 2000},
+				},
+			},
+			wantRepMode: Fit,
+			wantAssignment: Assignment{
+				PodSets: []PodSetAssignment{{
+					Name: "main",
+					Flavors: ResourceAssignment{
+						corev1.ResourceCPU:  {Name: "two", Mode: Fit},
+						corev1.ResourcePods: {Name: "two", Mode: Fit},
+					},
+					Requests: corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("9000m"),
+						corev1.ResourcePods: resource.MustParse("1"),
+					},
+					Count: 1,
+				}},
+				Usage: workload.FlavorResourceQuantities{"two": {"cpu": 9000, "pods": 1}},
+			},
+		},
+		"borrow try next flavor, found the first flavor": {
+			wlPods: []kueue.PodSet{
+				*utiltesting.MakePodSet("main", 1).
+					Request(corev1.ResourceCPU, "9").
+					Obj(),
+			},
+			clusterQueue: cache.ClusterQueue{
+				Cohort: &cache.Cohort{
+					Usage: workload.FlavorResourceQuantities{
+						"one": {corev1.ResourceCPU: 2000},
+					},
+					RequestableResources: workload.FlavorResourceQuantities{
+						"one": {corev1.ResourceCPU: 11000, corev1.ResourcePods: 10},
+						"two": {corev1.ResourceCPU: 1000, corev1.ResourcePods: 10},
+					},
+				},
+				FlavorFungibility: kueue.FlavorFungibility{
+					WhenCanBorrow:  kueue.TryNextFlavor,
+					WhenCanPreempt: kueue.TryNextFlavor,
+				},
+				ResourceGroups: []cache.ResourceGroup{{
+					CoveredResources: sets.New(corev1.ResourceCPU, corev1.ResourcePods),
+					Flavors: []cache.FlavorQuotas{{
+						Name: "one",
+						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
+							corev1.ResourcePods: {Nominal: 10},
+							corev1.ResourceCPU:  {Nominal: 10000, BorrowingLimit: ptr.To(int64(1000))},
+						},
+					}, {
+						Name: "two",
+						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
+							corev1.ResourcePods: {Nominal: 10},
+							corev1.ResourceCPU:  {Nominal: 1000},
+						},
+					}},
+				}},
+				Usage: workload.FlavorResourceQuantities{
+					"one": {corev1.ResourceCPU: 2000},
+				},
+			},
+			wantRepMode: Fit,
+			wantAssignment: Assignment{
+				TotalBorrow: workload.FlavorResourceQuantities{"one": {"cpu": 1000}},
+				PodSets: []PodSetAssignment{{
+					Name: "main",
+					Flavors: ResourceAssignment{
+						corev1.ResourceCPU:  {Name: "one", Mode: Fit},
+						corev1.ResourcePods: {Name: "one", Mode: Fit},
+					},
+					Requests: corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("9000m"),
+						corev1.ResourcePods: resource.MustParse("1"),
+					},
+					Count: 1,
+				}},
+				Usage: workload.FlavorResourceQuantities{
+					"one": {corev1.ResourceCPU: 9000, corev1.ResourcePods: 1},
+				},
+			},
+		},
+		"borrow try next flavor, found the second flavor": {
+			wlPods: []kueue.PodSet{
+				*utiltesting.MakePodSet("main", 1).
+					Request(corev1.ResourceCPU, "9").
+					Obj(),
+			},
+			clusterQueue: cache.ClusterQueue{
+				Cohort: &cache.Cohort{
+					Usage: workload.FlavorResourceQuantities{
+						"one": {corev1.ResourceCPU: 2000},
+					},
+					RequestableResources: workload.FlavorResourceQuantities{
+						"one": {corev1.ResourceCPU: 11000, corev1.ResourcePods: 10},
+						"two": {corev1.ResourceCPU: 10000, corev1.ResourcePods: 10},
+					},
+				},
+				FlavorFungibility: kueue.FlavorFungibility{
+					WhenCanBorrow:  kueue.TryNextFlavor,
+					WhenCanPreempt: kueue.TryNextFlavor,
+				},
+				ResourceGroups: []cache.ResourceGroup{{
+					CoveredResources: sets.New(corev1.ResourceCPU, corev1.ResourcePods),
+					Flavors: []cache.FlavorQuotas{{
+						Name: "one",
+						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
+							corev1.ResourcePods: {Nominal: 10},
+							corev1.ResourceCPU:  {Nominal: 10000, BorrowingLimit: ptr.To(int64(1000))},
+						},
+					}, {
+						Name: "two",
+						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
+							corev1.ResourcePods: {Nominal: 10},
+							corev1.ResourceCPU:  {Nominal: 10000},
+						},
+					}},
+				}},
+				Usage: workload.FlavorResourceQuantities{
+					"one": {corev1.ResourceCPU: 2000},
+				},
+			},
+			wantRepMode: Fit,
+			wantAssignment: Assignment{
+				PodSets: []PodSetAssignment{{
+					Name: "main",
+					Flavors: ResourceAssignment{
+						corev1.ResourceCPU:  {Name: "two", Mode: Fit},
+						corev1.ResourcePods: {Name: "two", Mode: Fit},
+					},
+					Requests: corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("9000m"),
+						corev1.ResourcePods: resource.MustParse("1"),
+					},
+					Count: 1,
+				}},
+				Usage: workload.FlavorResourceQuantities{
+					"two": {corev1.ResourceCPU: 9000, corev1.ResourcePods: 1},
+				},
+			},
+		},
+		"borrow before try next flavor": {
+			wlPods: []kueue.PodSet{
+				*utiltesting.MakePodSet("main", 1).
+					Request(corev1.ResourceCPU, "9").
+					Obj(),
+			},
+			clusterQueue: cache.ClusterQueue{
+				Cohort: &cache.Cohort{
+					Usage: workload.FlavorResourceQuantities{
+						"one": {corev1.ResourceCPU: 2000},
+					},
+					RequestableResources: workload.FlavorResourceQuantities{
+						"one": {corev1.ResourceCPU: 11000, corev1.ResourcePods: 10},
+						"two": {corev1.ResourceCPU: 10000, corev1.ResourcePods: 10},
+					},
+				},
+				FlavorFungibility: defaultFlavorFungibility,
+				ResourceGroups: []cache.ResourceGroup{{
+					CoveredResources: sets.New(corev1.ResourceCPU, corev1.ResourcePods),
+					Flavors: []cache.FlavorQuotas{{
+						Name: "one",
+						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
+							corev1.ResourcePods: {Nominal: 10},
+							corev1.ResourceCPU:  {Nominal: 10000, BorrowingLimit: ptr.To(int64(1000))},
```

  ```suggestion
  							corev1.ResourceCPU:  {Nominal: 10000, BorrowingLimit: ptr.To[int64](1000)},
  ```

- **pkg/scheduler/scheduler_test.go:1644** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-27T10:54:36Z
```diff
@@ -1263,6 +1263,392 @@ func TestEntryOrdering(t *testing.T) {
 	}
 }
 
+func TestLastSchedulingContext(t *testing.T) {
+	resourceFlavors := []*kueue.ResourceFlavor{
+		{ObjectMeta: metav1.ObjectMeta{Name: "on-demand"}},
+		{ObjectMeta: metav1.ObjectMeta{Name: "spot"}},
+	}
+	clusterQueue := []kueue.ClusterQueue{
+		*utiltesting.MakeClusterQueue("eng-alpha").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+	}
+	clusterQueue_cohort := []kueue.ClusterQueue{
+		*utiltesting.MakeClusterQueue("eng-cohort-alpha").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+				WhenCanBorrow:  kueue.Borrow,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+		*utiltesting.MakeClusterQueue("eng-cohort-beta").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.Preempt,
+				WhenCanBorrow:  kueue.Borrow,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+		*utiltesting.MakeClusterQueue("eng-cohort-theta").
+			Cohort("cohort").
+			QueueingStrategy(kueue.StrictFIFO).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue:  kueue.PreemptionPolicyNever,
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}).
+			FlavorFungibility(kueue.FlavorFungibility{
+				WhenCanPreempt: kueue.TryNextFlavor,
+				WhenCanBorrow:  kueue.TryNextFlavor,
+			}).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					Resource(corev1.ResourceCPU, "50", "50").Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					Resource(corev1.ResourceCPU, "100", "0").Obj(),
+			).Obj(),
+	}
+
+	queues := []kueue.LocalQueue{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-alpha",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-alpha",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-alpha",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-beta",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-beta",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Namespace: "default",
+				Name:      "main-theta",
+			},
+			Spec: kueue.LocalQueueSpec{
+				ClusterQueue: "eng-cohort-theta",
+			},
+		},
+	}
+	wl := utiltesting.MakeWorkload("low-1", "default").
+		Request(corev1.ResourceCPU, "50").
+		ReserveQuota(utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+		Admitted(true).
+		Obj()
+	cases := []struct {
+		name                           string
+		cqs                            []kueue.ClusterQueue
+		admittedWorkloads              []kueue.Workload
+		workloads                      []kueue.Workload
+		deletedWorkloads               []kueue.Workload
+		wantPreempted                  sets.Set[string]
+		wantAdmissionsOnFirstSchedule  map[string]kueue.Admission
+		wantAdmissionsOnSecondSchedule map[string]kueue.Admission
+	}{
+		{
+			name: "scheduling context not changed",
+			cqs:  clusterQueue,
+			admittedWorkloads: []kueue.Workload{
+				*wl,
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("preemptor", "default").
+					Queue("main").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads:              []kueue.Workload{},
+			wantPreempted:                 sets.Set[string]{},
+			wantAdmissionsOnFirstSchedule: map[string]kueue.Admission{},
+			wantAdmissionsOnSecondSchedule: map[string]kueue.Admission{
+				"default/preemptor": *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "spot", "20").Obj(),
+				"default/low-1":     *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+			},
+		},
+		{
+			name: "some workloads were deleted",
+			cqs:  clusterQueue,
+			admittedWorkloads: []kueue.Workload{
+				*wl,
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("preemptor", "default").
+					Queue("main").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads: []kueue.Workload{
+				*wl,
+			},
+			wantPreempted:                 sets.Set[string]{},
+			wantAdmissionsOnFirstSchedule: map[string]kueue.Admission{},
+			wantAdmissionsOnSecondSchedule: map[string]kueue.Admission{
+				"default/preemptor": *utiltesting.MakeAdmission("eng-alpha").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+		},
+		{
+			name: "borrow before next flavor",
+			cqs:  clusterQueue_cohort,
+			admittedWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("placeholder", "default").
+					Request(corev1.ResourceCPU, "50").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("borrower", "default").
+					Queue("main-alpha").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+				*utiltesting.MakeWorkload("workload1", "default").
+					Queue("main-beta").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads: []kueue.Workload{},
+			wantPreempted:    sets.Set[string]{},
+			wantAdmissionsOnFirstSchedule: map[string]kueue.Admission{
+				"default/workload1": *utiltesting.MakeAdmission("eng-cohort-beta").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+				"default/borrower":  *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+			wantAdmissionsOnSecondSchedule: map[string]kueue.Admission{
+				"default/placeholder": *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+				"default/workload1":   *utiltesting.MakeAdmission("eng-cohort-beta").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+				"default/borrower":    *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+		},
+		{
+			name: "borrow after all flavors",
+			cqs:  clusterQueue_cohort,
+			admittedWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("placeholder", "default").
+					Request(corev1.ResourceCPU, "50").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+					Admitted(true).
+					Obj(),
+				*utiltesting.MakeWorkload("placeholder1", "default").
+					Request(corev1.ResourceCPU, "50").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("workload", "default").
+					Queue("main-theta").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads: []kueue.Workload{},
+			wantPreempted:    sets.Set[string]{},
+			wantAdmissionsOnFirstSchedule: map[string]kueue.Admission{
+				"default/workload": *utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "spot", "20").Obj(),
+			},
+			wantAdmissionsOnSecondSchedule: map[string]kueue.Admission{
+				"default/placeholder":  *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+				"default/placeholder1": *utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "on-demand", "50").Obj(),
+				"default/workload":     *utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "spot", "20").Obj(),
+			},
+		},
+		{
+			name: "when the next flavor is full",
+			cqs:  clusterQueue_cohort,
+			admittedWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("placeholder", "default").
+					Request(corev1.ResourceCPU, "40").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "40").Obj()).
+					Admitted(true).
+					Obj(),
+				*utiltesting.MakeWorkload("placeholder1", "default").
+					Request(corev1.ResourceCPU, "40").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "on-demand", "40").Obj()).
+					Admitted(true).
+					Obj(),
+				*utiltesting.MakeWorkload("placeholder2", "default").
+					Request(corev1.ResourceCPU, "100").
+					ReserveQuota(utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "spot", "100").Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("workload", "default").
+					Queue("main-theta").
+					Request(corev1.ResourceCPU, "20").
+					Obj(),
+			},
+			deletedWorkloads: []kueue.Workload{},
+			wantPreempted:    sets.Set[string]{},
+			wantAdmissionsOnFirstSchedule: map[string]kueue.Admission{
+				"default/workload": *utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+			wantAdmissionsOnSecondSchedule: map[string]kueue.Admission{
+				"default/placeholder":  *utiltesting.MakeAdmission("eng-cohort-alpha").Assignment(corev1.ResourceCPU, "on-demand", "40").Obj(),
+				"default/placeholder1": *utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "on-demand", "40").Obj(),
+				"default/placeholder2": *utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "spot", "100").Obj(),
+				"default/workload":     *utiltesting.MakeAdmission("eng-cohort-theta").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
+			},
+		},
+	}
+
+	for _, tc := range cases {
+		t.Run(tc.name, func(t *testing.T) {
+			ctx, _ := utiltesting.ContextWithLog(t)
+			scheme := runtime.NewScheme()
+
+			clientBuilder := utiltesting.NewClientBuilder().
+				WithLists(&kueue.WorkloadList{Items: tc.admittedWorkloads},
+					&kueue.WorkloadList{Items: tc.workloads},
+					&kueue.ClusterQueueList{Items: tc.cqs},
+					&kueue.LocalQueueList{Items: queues}).
+				WithObjects(
+					&corev1.Namespace{ObjectMeta: metav1.ObjectMeta{Name: "default"}},
+				)
+			cl := clientBuilder.Build()
+			broadcaster := record.NewBroadcaster()
+			recorder := broadcaster.NewRecorder(scheme,
+				corev1.EventSource{Component: constants.AdmissionName})
+			cqCache := cache.New(cl)
+			qManager := queue.NewManager(cl, cqCache)
+			// Workloads are loaded into queues or clusterQueues as we add them.
+			for _, q := range queues {
+				if err := qManager.AddLocalQueue(ctx, &q); err != nil {
+					t.Fatalf("Inserting queue %s/%s in manager: %v", q.Namespace, q.Name, err)
+				}
+			}
+			for i := range resourceFlavors {
+				cqCache.AddOrUpdateResourceFlavor(resourceFlavors[i])
+			}
+			for _, cq := range tc.cqs {
+				if err := cqCache.AddClusterQueue(ctx, &cq); err != nil {
+					t.Fatalf("Inserting clusterQueue %s in cache: %v", cq.Name, err)
+				}
+				if err := qManager.AddClusterQueue(ctx, &cq); err != nil {
+					t.Fatalf("Inserting clusterQueue %s in manager: %v", cq.Name, err)
+				}
+			}
+			scheduler := New(qManager, cqCache, cl, recorder)
+			gotScheduled := make(map[string]kueue.Admission)
+			var mu sync.Mutex
+			scheduler.applyAdmission = func(ctx context.Context, w *kueue.Workload) error {
+				mu.Lock()
+				gotScheduled[workload.Key(w)] = *w.Status.Admission
+				mu.Unlock()
+				return nil
+			}
+			wg := sync.WaitGroup{}
+			scheduler.setAdmissionRoutineWrapper(routine.NewWrapper(
+				func() { wg.Add(1) },
+				func() { wg.Done() },
+			))
+			gotPreempted := sets.New[string]()
+			scheduler.preemptor.OverrideApply(func(_ context.Context, w *kueue.Workload) error {
+				mu.Lock()
+				gotPreempted.Insert(workload.Key(w))
+				mu.Unlock()
+				return nil
+			})
+
+			ctx, cancel := context.WithTimeout(ctx, queueingTimeout)
+			go qManager.CleanUpOnContext(ctx)
+			defer cancel()
+
+			scheduler.schedule(ctx)
+			wg.Wait()
+
+			if diff := cmp.Diff(tc.wantPreempted, gotPreempted); diff != "" {
+				t.Errorf("Unexpected preemptions (-want,+got):\n%s", diff)
+			}
+			if diff := cmp.Diff(tc.wantAdmissionsOnFirstSchedule, gotScheduled); diff != "" {
+				t.Errorf("Unexpected scheduled workloads (-want,+got):\n%s", diff)
+			}
+
+			for _, wl := range tc.deletedWorkloads {
+				err := cl.Delete(ctx, &wl)
+				if err != nil {
+					t.Errorf("Delete workload failed: %v", err)
+				}
+				err = cqCache.DeleteWorkload(&wl)
+				if err != nil {
+					t.Errorf("Delete workload failed: %v", err)
+				}
+			}
+
+			scheduler.schedule(ctx)
+			wg.Wait()
+
+			if diff := cmp.Diff(tc.wantPreempted, gotPreempted); diff != "" {
+				t.Errorf("Unexpected preemptions (-want,+got):\n%s", diff)
+			}
+			// Verify assignments in cache.
+			gotAssignments := make(map[string]kueue.Admission)
+			snapshot := cqCache.Snapshot()
+			for cqName, c := range snapshot.ClusterQueues {
+				for name, w := range c.Workloads {
+					if !workload.IsAdmitted(w.Obj) {
+						t.Errorf("Workload %s is not admitted by a clusterQueue, but it is found as member of clusterQueue %s in the cache", name, cqName)
+					} else if string(w.Obj.Status.Admission.ClusterQueue) != cqName {
+						t.Errorf("Workload %s is admitted by clusterQueue %s, but it is found as member of clusterQueue %s in the cache", name, w.Obj.Status.Admission.ClusterQueue, cqName)
+					} else {
+						gotAssignments[name] = *w.Obj.Status.Admission
+					}
+				}
+			}
+			if len(gotAssignments) == 0 {
+				gotAssignments = nil
+			}
```

  Why do we need this?

- **pkg/webhooks/clusterqueue_webhook.go:81** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-27T11:34:15Z
```diff
@@ -67,6 +67,18 @@ func (w *ClusterQueueWebhook) Default(ctx context.Context, obj runtime.Object) e
 			ReclaimWithinCohort: kueue.PreemptionPolicyNever,
 		}
 	}
+	if cq.Spec.FlavorFungibility == nil {
+		cq.Spec.FlavorFungibility = &kueue.FlavorFungibility{
+			WhenCanBorrow:  kueue.Borrow,
+			WhenCanPreempt: kueue.TryNextFlavor,
+		}
+	}
+	if cq.Spec.FlavorFungibility.WhenCanBorrow == "" {
+		cq.Spec.FlavorFungibility.WhenCanBorrow = kueue.Borrow
+	}
+	if cq.Spec.FlavorFungibility.WhenCanPreempt == "" {
+		cq.Spec.FlavorFungibility.WhenCanPreempt = kueue.TryNextFlavor
+	}
```

  I don't think we need these defaulters since the crd defaulters inject these default values into these, right?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-29T08:06:39Z

- **pkg/cache/cache_test.go:802** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-29T08:06:38Z
```diff
@@ -739,11 +793,13 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 			},
 			wantClusterQueues: map[string]*ClusterQueue{
 				"foo": {
-					Name:              "foo",
-					NamespaceSelector: labels.Everything(),
-					Status:            pending,
-					Preemption:        defaultPreemption,
-					AdmissionChecks:   sets.New("check1", "check2"),
+					Name:                          "foo",
+					NamespaceSelector:             labels.Everything(),
+					Status:                        pending,
+					Preemption:                    defaultPreemption,
+					AllocatableResourceGeneration: 1,
+					FlavorFungibility:             defaultFlavorFungibility,
+					AdmissionChecks:               sets.Set[string]{"check1": {}, "check2": {}},
```

  ```suggestion
  					AdmissionChecks:               sets.New("check1", "check2"),
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-29T16:45:42Z

- **pkg/workload/workload.go:43** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-29T16:45:42Z
```diff
@@ -39,6 +40,40 @@ var (
 	admissionManagedConditions = []string{kueue.WorkloadAdmitted, kueue.WorkloadEvicted}
 )
 
+type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
```

  @KunWuLuan ping

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-10-07T07:13:11Z

- **pkg/workload/workload.go:43** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-10-07T07:13:11Z
```diff
@@ -39,6 +40,40 @@ var (
 	admissionManagedConditions = []string{kueue.WorkloadAdmitted, kueue.WorkloadEvicted}
 )
 
+type FlavorResourceQuantities map[kueue.ResourceFlavorReference]map[corev1.ResourceName]int64
```

  done. thanks for reminding me

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-10-07T08:15:17Z

- **pkg/cache/cache_test.go:1** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-10-07T08:15:17Z
  done

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-10-07T08:19:19Z

- **pkg/cache/cache_test.go:802** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-10-07T08:19:19Z
```diff
@@ -739,11 +793,13 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 			},
 			wantClusterQueues: map[string]*ClusterQueue{
 				"foo": {
-					Name:              "foo",
-					NamespaceSelector: labels.Everything(),
-					Status:            pending,
-					Preemption:        defaultPreemption,
-					AdmissionChecks:   sets.New("check1", "check2"),
+					Name:                          "foo",
+					NamespaceSelector:             labels.Everything(),
+					Status:                        pending,
+					Preemption:                    defaultPreemption,
+					AllocatableResourceGeneration: 1,
+					FlavorFungibility:             defaultFlavorFungibility,
+					AdmissionChecks:               sets.Set[string]{"check1": {}, "check2": {}},
```

  done

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-10-07T08:21:35Z

- **pkg/util/testing/wrappers.go:366** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-10-07T08:21:35Z
```diff
@@ -318,6 +318,14 @@ func MakeClusterQueue(name string) *ClusterQueueWrapper {
 		Spec: kueue.ClusterQueueSpec{
 			NamespaceSelector: &metav1.LabelSelector{},
 			QueueingStrategy:  kueue.BestEffortFIFO,
+			Preemption: &kueue.ClusterQueuePreemption{
```

  done. thanks

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-09T08:37:10Z

@KunWuLuan Thank you for this great work!

/lgtm
/approve
