# PR #8493: Use timeout as fallback when recoveryTimeout is not specified

**Summary**: Use timeout as fallback when recoveryTimeout is not specified

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8493

**Last updated**: 2026-01-19T17:26:52Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@IrvingMg](https://github.com/IrvingMg)
- **Branch**: `feature/wait-for-pods-ready-timeout-fallback` → `main`
- **Created**: 2026-01-09T13:11:12Z
- **Updated**: 2026-01-19T17:26:52Z
- **Merged**: 2026-01-19T17:25:48Z
- **Closed**: 2026-01-19T17:25:48Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@pajakd](https://github.com/pajakd), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@pajakd](https://github.com/pajakd), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 11   **+86 / -13**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind feature

#### What this PR does / why we need it:
Makes `waitForPodsReady.timeout` the fallback value when `waitForPodsReady.recoveryTimeout` is not specified.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #8300

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
waitForPodsReady.recoveryTimeout now defaults to the value of waitForPodsReady.timeout when not specified.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-01-09T13:11:19Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 0d4de845fc05b31105091056083f9d6a25d24ba6 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/696e62b78ca85d00083ee9cf |

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2026-01-13T08:39:39Z

I’ve updated the PR to allow disabling `recoveryTimeout` by setting it to 0, as discussed in https://github.com/kubernetes-sigs/kueue/issues/8300#issuecomment-3737601424.

### Comment by [@pajakd](https://github.com/pajakd) — 2026-01-19T13:30:56Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-19T13:31:07Z

LGTM label has been added.  <details>Git tree hash: bf0c618e705ecdad45b070808e16f1ff7b29216a</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-19T17:00:16Z

LGTM label has been added.  <details>Git tree hash: e14cbebda4d3c1677727a7a676772199401fa9f7</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-19T17:00:18Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8493#" title="Author self-approved">IrvingMg</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8493#pullrequestreview-3679010916" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-01-19T11:30:45Z

- **site/content/en/docs/reference/kueue-config.v1beta2.md:942** by [@pajakd](https://github.com/pajakd) — 2026-01-19T11:30:44Z
```diff
@@ -933,12 +933,13 @@ Defaults to false.</p>
 <a href="https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.28/#duration-v1-meta"><code>k8s.io/apimachinery/pkg/apis/meta/v1.Duration</code></a>
 </td>
 <td>
-   <p>RecoveryTimeout defines an opt-in timeout, measured since the
+   <p>RecoveryTimeout defines a timeout, measured since the
 last transition to the PodsReady=false condition after a Workload is Admitted and running.
 Such a transition may happen when a Pod failed and the replacement Pod
 is awaited to be scheduled.
 After exceeding the timeout the corresponding job gets suspended again
-and requeued after the backoff delay.</p>
+and requeued after the backoff delay.
+Defaults to the value of Timeout. Setting to 0 disables recovery timeout checking.</p>
```

  nit: Timeout -- in most places we write `timeout`

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-01-19T16:26:31Z

- **site/content/en/docs/reference/kueue-config.v1beta2.md:942** by [@IrvingMg](https://github.com/IrvingMg) — 2026-01-19T16:26:31Z
```diff
@@ -933,12 +933,13 @@ Defaults to false.</p>
 <a href="https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.28/#duration-v1-meta"><code>k8s.io/apimachinery/pkg/apis/meta/v1.Duration</code></a>
 </td>
 <td>
-   <p>RecoveryTimeout defines an opt-in timeout, measured since the
+   <p>RecoveryTimeout defines a timeout, measured since the
 last transition to the PodsReady=false condition after a Workload is Admitted and running.
 Such a transition may happen when a Pod failed and the replacement Pod
 is awaited to be scheduled.
 After exceeding the timeout the corresponding job gets suspended again
-and requeued after the backoff delay.</p>
+and requeued after the backoff delay.
+Defaults to the value of Timeout. Setting to 0 disables recovery timeout checking.</p>
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-01-19T16:33:38Z

Overall LGTM, thank you 👍

- **keps/349-all-or-nothing/README.md:351** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-19T16:30:07Z
```diff
@@ -348,6 +348,8 @@ condition, so the corresponding job is unsuspended without further waiting.
 
 We introduce two timeouts defined in the `waitForPodsReady.timeout` and `waitForPodsReady.recoveryTimeout` fields.
 
+The `recoveryTimeout` defaults to the value of `timeout` when not specified. Setting `recoveryTimeout` to `0` explicitly disables recovery timeout checking.
```

  ```suggestion
  Since Kueue v0.16.0, the `recoveryTimeout` defaults to the value of `timeout` when not specified. Setting `recoveryTimeout` to `0` explicitly disables recovery timeout checking.
  ```

- **apis/config/v1beta1/configuration_types.go:252** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-19T16:32:18Z
```diff
@@ -243,13 +243,13 @@ type WaitForPodsReady struct {
 	// +optional
 	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
 
-	// RecoveryTimeout defines an opt-in timeout, measured since the
+	// RecoveryTimeout defines a timeout, measured since the
 	// last transition to the PodsReady=false condition after a Workload is Admitted and running.
 	// Such a transition may happen when a Pod failed and the replacement Pod
 	// is awaited to be scheduled.
 	// After exceeding the timeout the corresponding job gets suspended again
 	// and requeued after the backoff delay. The timeout is enforced only if waitForPodsReady.enable=true.
-	// If not set, there is no timeout.
+	// Defaults to the value of timeout. Setting to 0 disables recovery timeout checking.
```

  ```suggestion
  	// Defaults to the value of timeout. Setting to "0s" disables recovery timeout checking.
  ```
  
  Isn't this "0s" rather than "0"?

- **apis/config/v1beta2/configuration_types.go:240** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-19T16:32:36Z
```diff
@@ -231,12 +231,13 @@ type WaitForPodsReady struct {
 	// +optional
 	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
 
-	// RecoveryTimeout defines an opt-in timeout, measured since the
+	// RecoveryTimeout defines a timeout, measured since the
 	// last transition to the PodsReady=false condition after a Workload is Admitted and running.
 	// Such a transition may happen when a Pod failed and the replacement Pod
 	// is awaited to be scheduled.
 	// After exceeding the timeout the corresponding job gets suspended again
 	// and requeued after the backoff delay.
+	// Defaults to the value of timeout. Setting to 0 disables recovery timeout checking.
```

  ```suggestion
  	// Defaults to the value of timeout. Setting to "0s" disables recovery timeout checking.
  ```

- **keps/349-all-or-nothing/README.md:254** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-19T16:33:02Z
```diff
@@ -245,13 +245,13 @@ type WaitForPodsReady struct {
 	// +optional
 	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
 
-	// RecoveryTimeout defines an opt-in timeout, measured since the
+	// RecoveryTimeout defines a timeout, measured since the
 	// last transition to the PodsReady=false condition after a Workload is Admitted and running.
 	// Such a transition may happen when a Pod failed and the replacement Pod
 	// is awaited to be scheduled.
 	// After exceeding the timeout the corresponding job gets suspended again
 	// and requeued after the backoff delay. The timeout is enforced only if waitForPodsReady.enable=true.
-	// If not set, there is no timeout.
+	// Defaults to the value of timeout. Setting to 0 disables recovery timeout checking.
```

  ```suggestion
  	// Defaults to the value of timeout. Setting to "0s" disables recovery timeout checking.
  ```

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-01-19T16:59:10Z

- **apis/config/v1beta1/configuration_types.go:252** by [@IrvingMg](https://github.com/IrvingMg) — 2026-01-19T16:59:10Z
```diff
@@ -243,13 +243,13 @@ type WaitForPodsReady struct {
 	// +optional
 	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
 
-	// RecoveryTimeout defines an opt-in timeout, measured since the
+	// RecoveryTimeout defines a timeout, measured since the
 	// last transition to the PodsReady=false condition after a Workload is Admitted and running.
 	// Such a transition may happen when a Pod failed and the replacement Pod
 	// is awaited to be scheduled.
 	// After exceeding the timeout the corresponding job gets suspended again
 	// and requeued after the backoff delay. The timeout is enforced only if waitForPodsReady.enable=true.
-	// If not set, there is no timeout.
+	// Defaults to the value of timeout. Setting to 0 disables recovery timeout checking.
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-01-19T17:00:04Z

Awesome, thank you!
/lgtm
/approve

Assuming CI passes
