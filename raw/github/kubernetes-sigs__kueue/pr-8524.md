# PR #8524: Easy to use running e2e test for developers

**Summary**: Easy to use running e2e test for developers

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8524

**Last updated**: 2026-03-04T07:57:51Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@vladikkuzn](https://github.com/vladikkuzn)
- **Branch**: `dev-mode` → `main`
- **Created**: 2026-01-10T19:58:10Z
- **Updated**: 2026-03-04T07:57:51Z
- **Merged**: 2026-01-19T13:47:15Z
- **Closed**: 2026-01-19T13:47:15Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@kshalot](https://github.com/kshalot), [@vladikkuzn](https://github.com/vladikkuzn)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mszadkow](https://github.com/mszadkow), [@kshalot](https://github.com/kshalot), [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra), [@windsonsea](https://github.com/windsonsea)
- **Changed files**: 7   **+175 / -62**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
As a developer I want the cluster to stay around for (1.) manual investigation after tests, (2) quick re-run.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #8093

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-10T19:58:12Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-01-10T19:58:16Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | e1b50e7c1e8af33026a5fcdf40a3d4e63f53b30a |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/696a72b06d111f0007459ae4 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-8524--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-8524--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTg1MjQtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.Db4dN-sRkd4yWTITFzyyE2JpuxL5CYuEJptMsAyQPxE)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-8524--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-01-10T19:58:21Z

/ok-to-test

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-01-12T16:14:19Z

/cc @mimowo @tenzen-y @kannon92 @mszadkow

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-01-12T16:14:35Z

/assign

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-13T14:11:29Z

/approve
Leaving lgtm to @kshalot who is already reviewing this one

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-13T14:11:34Z

please rebase

### Comment by [@kshalot](https://github.com/kshalot) — 2026-01-16T08:48:00Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-16T08:48:08Z

LGTM label has been added.  <details>Git tree hash: 8773396242a9b680b39bb6aab7f547b924d3473f</details>

### Comment by [@kshalot](https://github.com/kshalot) — 2026-01-16T11:12:36Z

I noticed one thing - there's an error in the multikueue e2e CI runs (https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8524/pull-kueue-test-e2e-multikueue-main/2011995240482738176)

```
ERROR: failed to delete cluster "kind-manager": failed to lock config file: open /logs/artifacts/run-test-multikueue-e2e-1.34.3/kubeconfig-kind-manager.lock: file exists
```

My first guess was that we are running `cluster_cleanup ... &` for each cluster concurrently, but the name of the file suggests that there are separate files for each cluster. So maybe it's a stale lock file from a crashed cleanup from some previous run? Which would be interesting because I thought prow runs jobs in new pods.

@mimowo Did you see this error before outside of this PR?

### Comment by [@kshalot](https://github.com/kshalot) — 2026-01-16T11:13:41Z

/hold

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-16T11:18:17Z

> @mimowo Did you see this error before outside of this PR?

I haven't but that should be easy to check in artifacts for other builds. My go to place for healthy buids is the testgrid, for example https://testgrid.k8s.io/sig-scheduling#periodic-kueue-test-e2e-multikueue-main

there are periodic builds from the main branch. FYI you can find the configurations for the builds in the test-infra project: https://github.com/kubernetes/test-infra/blob/master/config/jobs/kubernetes-sigs/kueue/kueue-periodics-main.yaml

ptal.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-16T11:19:39Z

And yes, let's make sure the PR does not introduce a regression for debuggability

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-01-16T15:57:32Z

As appeared cluster is deleted without kubeconfig, but kubeconfig is still needed to delete the lock file. Fixed

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-16T17:04:18Z

/approve
Leaving final tagging (lgtm & unhold) to @kshalot . Thank you folks 👍

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-16T17:04:28Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8524#issuecomment-3760980092" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8524#" title="Author self-approved">vladikkuzn</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@kshalot](https://github.com/kshalot) — 2026-01-19T09:48:42Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-19T09:48:50Z

LGTM label has been added.  <details>Git tree hash: bc3e660c20e4fa4c4cd20e6dab8424eb1dc554f0</details>

### Comment by [@kshalot](https://github.com/kshalot) — 2026-01-19T09:49:01Z

/hold cancel

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-19T13:17:23Z

This label looks stale, there is no WIP, not hold.
/unhold

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-19T13:17:34Z

/remove-label do-not-merge/work-in-progress

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-19T13:17:37Z

@mimowo: The label(s) `/remove-label do-not-merge/work-in-progress` cannot be applied. These labels are supported: `api-review, tide/merge-method-merge, tide/merge-method-rebase, tide/merge-method-squash, team/katacoda, refactor, ci-short, ci-extended, ci-full`. Is this label configured under `labels -> additional_labels` or `labels -> restricted_labels` in `plugin.yaml`?

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8524#issuecomment-3768294768):

>/remove-label do-not-merge/work-in-progress


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-19T13:18:22Z

/remove do-not-merge/work-in-progress

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-19T13:19:53Z

removed manually, I don't know what was the reason

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-09T07:36:06Z

/cherrypick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-02-09T07:36:50Z

@mimowo: #8524 failed to apply on top of branch "website":
```
Applying: Easy to use running e2e test for developers #8093
Using index info to reconstruct a base tree...
M	Makefile-test.mk
M	hack/e2e-common.sh
M	hack/e2e-test.sh
M	hack/multikueue-e2e-test.sh
M	site/content/en/docs/contribution_guidelines/testing.md
M	site/content/zh-CN/docs/contribution_guidelines/testing.md
Falling back to patching base and 3-way merge...
Auto-merging site/content/zh-CN/docs/contribution_guidelines/testing.md
CONFLICT (content): Merge conflict in site/content/zh-CN/docs/contribution_guidelines/testing.md
Auto-merging site/content/en/docs/contribution_guidelines/testing.md
CONFLICT (content): Merge conflict in site/content/en/docs/contribution_guidelines/testing.md
Auto-merging hack/multikueue-e2e-test.sh
CONFLICT (content): Merge conflict in hack/multikueue-e2e-test.sh
Auto-merging hack/e2e-test.sh
CONFLICT (content): Merge conflict in hack/e2e-test.sh
Auto-merging hack/e2e-common.sh
CONFLICT (content): Merge conflict in hack/e2e-common.sh
Auto-merging Makefile-test.mk
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Easy to use running e2e test for developers #8093

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8524#issuecomment-3869859266):

>/cherrypick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-09T07:58:31Z

oh, this is already merged into website so no need

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-04T07:57:13Z

/cherry-pick release-0.15

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-04T07:57:51Z

@mbobrovskyi: #8524 failed to apply on top of branch "release-0.15":
```
Applying: Easy to use running e2e test for developers #8093
Using index info to reconstruct a base tree...
M	Makefile-test.mk
A	hack/e2e-common.sh
A	hack/e2e-test.sh
A	hack/multikueue-e2e-test.sh
M	site/content/en/docs/contribution_guidelines/testing.md
Falling back to patching base and 3-way merge...
Auto-merging site/content/en/docs/contribution_guidelines/testing.md
Auto-merging hack/testing/e2e-test.sh
Auto-merging hack/testing/e2e-multikueue-test.sh
Auto-merging hack/testing/e2e-common.sh
CONFLICT (content): Merge conflict in hack/testing/e2e-common.sh
Auto-merging Makefile-test.mk
CONFLICT (content): Merge conflict in Makefile-test.mk
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Easy to use running e2e test for developers #8093

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8524#issuecomment-3995893821):

>/cherry-pick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-11T06:54:25Z

- **Makefile-test.mk:129** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-11T06:54:25Z
```diff
@@ -123,13 +124,21 @@ test-e2e: E2E_NPROCS := 2
 .PHONY: test-e2e
 test-e2e: setup-e2e-env kueuectl kind-ray-project-mini-image-build run-test-e2e-singlecluster-$(E2E_KIND_VERSION:kindest/node:v%=%)
 
+.PHONY: test-e2e-dev
+test-e2e-dev: E2E_MODE=dev
+test-e2e-dev: kind-image-build test-e2e
```

  I don't think so we need separate targets. Why don't use just `make test-e2e E2E_MODE=dev`?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-11T06:57:41Z

- **hack/e2e-common.sh:184** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-11T06:57:41Z
```diff
@@ -94,12 +103,85 @@ export E2E_TEST_AGNHOST_IMAGE=${E2E_TEST_AGNHOST_IMAGE_WITH_SHA%%@*}
 # $1 cluster name
 # $2 kubeconfig
 function cluster_cleanup {
-    kubectl config --kubeconfig="$2" use-context "kind-$1"
-    
-    $KIND export logs "$ARTIFACTS" --name "$1" || true
-    kubectl describe pods --kubeconfig="$2" -n kueue-system > "$ARTIFACTS/$1-kueue-system-pods.log" || true
-    kubectl describe pods --kubeconfig="$2" > "$ARTIFACTS/$1-default-pods.log" || true
-    $KIND delete cluster --name "$1"
+    local name=$1
+    local kubeconfig=$2
+
+    cluster_collect_artifacts "$name" "$kubeconfig"
+
+    if e2e_should_delete_cluster "$name"; then
+        $KIND delete cluster --name "$name"
+        return 0
+    fi
+
+    echo "Keeping kind cluster: $name (E2E_MODE=${E2E_MODE})"
+    echo "Cleanup manually with:"
+    echo "  kind delete cluster --name $name"
+}
+
+# $1 cluster name
+# $2 kubeconfig
+function cluster_collect_artifacts {
+    local name=$1
+    local kubeconfig=${2:-}
+
+    local kubeconfig_args=()
+    if [[ -n "${kubeconfig}" ]]; then
+        kubeconfig_args=(--kubeconfig="${kubeconfig}")
+    fi
+
+    kubectl config "${kubeconfig_args[@]}" use-context "kind-${name}" || true
+    $KIND export logs "$ARTIFACTS" --name "$name" || true
+    kubectl describe pods "${kubeconfig_args[@]}" -n kueue-system > "$ARTIFACTS/${name}-kueue-system-pods.log" || true
+    kubectl describe pods "${kubeconfig_args[@]}" > "$ARTIFACTS/${name}-default-pods.log" || true
+}
+
+# $1 cluster name
+function kind_cluster_exists {
+    $KIND get clusters 2>/dev/null | grep -Fxq "$1"
+}
+
+# $1 cluster name
+# $2 kubeconfig file path (optional)
+function kind_write_kubeconfig {
+    if [[ -n "${2:-}" ]]; then
+        mkdir -p "$(dirname "$2")"
+        $KIND get kubeconfig --name "$1" > "$2"
+    fi
+}
+
+# $1 cluster name
+# $2 cluster kind config
+# $3 kubeconfig file path (optional)
+function ensure_kind_cluster {
+    local name=$1
+    local cfg=$2
+    local kubeconfig=$3
+
+    if kind_cluster_exists "$name"; then
+        echo "Reusing kind cluster: $name"
+        kind_write_kubeconfig "$name" "$kubeconfig"
+        return 0
+    fi
+
+    if [[ "${E2E_MODE}" == "dev" ]]; then
+        echo "Creating kind cluster (dev mode): $name"
+        cluster_create "$name" "$cfg" "$kubeconfig"
+        return 0
+    fi
+
+    # CI mode behavior matches existing semantics: only create if enabled.
+    if [[ "${CREATE_KIND_CLUSTER}" == "true" ]]; then
+        cluster_create "$name" "$cfg" "$kubeconfig"
+    else
+        echo "CREATE_KIND_CLUSTER=false and kind cluster '$name' does not exist."
+        echo "Either set CREATE_KIND_CLUSTER=true or create a cluster named '$name' first."
+        return 1
+    fi
+}
+
+# $1 cluster name
+function e2e_should_delete_cluster {
+    [[ "${E2E_MODE}" != "dev" && "${CREATE_KIND_CLUSTER}" == "true" ]]
```

  Do we still need CREATE_KIND_CLUSTER? I thought we only create the cluster if it doesn’t already exist.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-11T07:05:44Z

- **hack/multikueue-e2e-test.sh:55** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-11T07:05:44Z
```diff
@@ -32,42 +32,49 @@ export WORKER2_KUBECONFIG="${ARTIFACTS}/kubeconfig-$WORKER2_KIND_CLUSTER_NAME"
 source "${SOURCE_DIR}/e2e-common.sh"
 
 function cleanup {
-    if [ "$CREATE_KIND_CLUSTER" == 'true' ]
-    then
-        if [ ! -d "$ARTIFACTS" ]; then
-            mkdir -p "$ARTIFACTS"
-        fi
+    if [ ! -d "$ARTIFACTS" ]; then
+        mkdir -p "$ARTIFACTS"
+    fi
 
+    if e2e_should_delete_cluster; then
         cluster_cleanup "$MANAGER_KIND_CLUSTER_NAME" "$MANAGER_KUBECONFIG" &
         cluster_cleanup "$WORKER1_KIND_CLUSTER_NAME" "$WORKER1_KUBECONFIG" &
         cluster_cleanup "$WORKER2_KIND_CLUSTER_NAME" "$WORKER2_KUBECONFIG" &
+        wait
+        return 0
     fi
+
+    cluster_collect_artifacts "$MANAGER_KIND_CLUSTER_NAME" "$MANAGER_KUBECONFIG" &
+    cluster_collect_artifacts "$WORKER1_KIND_CLUSTER_NAME" "$WORKER1_KUBECONFIG" &
+    cluster_collect_artifacts "$WORKER2_KIND_CLUSTER_NAME" "$WORKER2_KUBECONFIG" &
     wait
+    echo "Keeping kind clusters (E2E_MODE=${E2E_MODE})."
+    echo "To delete them:"
+    echo "  kind delete cluster --name $MANAGER_KIND_CLUSTER_NAME"
+    echo "  kind delete cluster --name $WORKER1_KIND_CLUSTER_NAME"
+    echo "  kind delete cluster --name $WORKER2_KIND_CLUSTER_NAME"
```

  ```suggestion
      echo "  kind delete cluster $MANAGER_KIND_CLUSTER_NAME $WORKER1_KIND_CLUSTER_NAME $WORKER2_KIND_CLUSTER_NAME"
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-11T07:14:53Z

- **hack/multikueue-e2e-test.sh:111** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-11T07:14:53Z
```diff
@@ -94,6 +101,15 @@ export KUBECONFIG="$MANAGER_KUBECONFIG:$WORKER1_KUBECONFIG:$WORKER2_KUBECONFIG"
 kueue_deploy
 
 if [ "$E2E_RUN_ONLY_ENV" = "true" ]; then
+  if [[ "${E2E_MODE:-ci}" == "dev" ]]; then
+    trap - EXIT
+    echo "E2E_RUN_ONLY_ENV=true (E2E_MODE=dev): leaving kind clusters running."
+    echo -e "\nManager kind cluster cleanup:\n  kind delete cluster --name $MANAGER_KIND_CLUSTER_NAME"
+    echo -e "\nWorker1 kind cluster cleanup:\n  kind delete cluster --name $WORKER1_KIND_CLUSTER_NAME"
+    echo -e "\nWorker2 kind cluster cleanup:\n  kind delete cluster --name $WORKER2_KIND_CLUSTER_NAME"
+    exit 0
+  fi
```

  I think this is no longer needed, right?

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-12T14:20:55Z

- **hack/e2e-common.sh:114** by [@kshalot](https://github.com/kshalot) — 2026-01-12T14:15:31Z
```diff
@@ -94,12 +103,85 @@ export E2E_TEST_AGNHOST_IMAGE=${E2E_TEST_AGNHOST_IMAGE_WITH_SHA%%@*}
 # $1 cluster name
 # $2 kubeconfig
 function cluster_cleanup {
-    kubectl config --kubeconfig="$2" use-context "kind-$1"
-    
-    $KIND export logs "$ARTIFACTS" --name "$1" || true
-    kubectl describe pods --kubeconfig="$2" -n kueue-system > "$ARTIFACTS/$1-kueue-system-pods.log" || true
-    kubectl describe pods --kubeconfig="$2" > "$ARTIFACTS/$1-default-pods.log" || true
-    $KIND delete cluster --name "$1"
+    local name=$1
+    local kubeconfig=$2
+
+    cluster_collect_artifacts "$name" "$kubeconfig"
+
+    if e2e_should_delete_cluster "$name"; then
+        $KIND delete cluster --name "$name"
+        return 0
+    fi
```

  This check seems to be already performed in the  `cleanup` functions before calling `cluster_cleanup`. Here (`hack/e2e-test.sh`):
  ```sh
      if e2e_should_delete_cluster; then # <---- HERE
          cluster_cleanup "$KIND_CLUSTER_NAME" ""
      else
          cluster_collect_artifacts "$KIND_CLUSTER_NAME" ""
          echo "Keeping kind cluster '$KIND_CLUSTER_NAME' (E2E_MODE=${E2E_MODE})."
          echo "To delete it: kind delete cluster --name $KIND_CLUSTER_NAME"
      fi
  ```
  and here (`hack/multikueue-e2e-test.sh`):
  ```sh
      if e2e_should_delete_cluster; then
          cluster_cleanup "$MANAGER_KIND_CLUSTER_NAME" "$MANAGER_KUBECONFIG" &
          cluster_cleanup "$WORKER1_KIND_CLUSTER_NAME" "$WORKER1_KUBECONFIG" &
          cluster_cleanup "$WORKER2_KIND_CLUSTER_NAME" "$WORKER2_KUBECONFIG" &
          wait
          return 0
      fi
  ```
  
  I know this is a draft PR, but just pointing it out.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-13T14:25:58Z

- **site/content/en/docs/contribution_guidelines/testing.md:143** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-13T14:25:58Z
```diff
@@ -120,7 +120,31 @@ and then you can use GUI of the Ginkgo Test Explorer to run individual tests, pr
 You can use the following approach to start up a kind cluster and then run e2e tests from commandline or VSCode,
 attaching them to the existing cluster. For example, suppose you want to test some of the multikueue-e2e tests.
 
-Run `E2E_RUN_ONLY_ENV=true make kind-image-build test-multikueue-e2e` and wait for the `Do you want to cleanup? [Y/n] ` to appear.
+### DEV mode (recommended)
+Use `E2E_MODE=dev` to create-or-reuse a kind cluster, rebuild/redeploy Kueue, run tests, and keep the cluster running for fast reruns and post-test investigation:
+
+```shell
+# Create if missing, otherwise reuse. Rebuild image, run tests, keep the cluster.
+E2E_MODE=dev make test-e2e
+
+# MultiKueue dev mode
+E2E_MODE=dev make test-multikueue-e2e
+
+# Loop a suite (until it fails) while keeping the cluster
+E2E_MODE=dev GINKGO_ARGS="--until-it-fails" make test-e2e
+```
+
+To delete the kept cluster(s) afterwards:
+
+```shell
+kind delete cluster --name kind
+kind delete cluster --name kind-manager
+kind delete cluster --name kind-worker1
+kind delete cluster --name kind-worker2
```

  ```suggestion
  kind delete clusters kind-manager kind-worker1 kind-worker2
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-13T14:27:20Z

- **site/content/zh-CN/docs/contribution_guidelines/testing.md:143** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-13T14:27:20Z
```diff
@@ -120,7 +120,31 @@ func TestValidateClusterQueue(t *testing.T) {
 你可以使用以下方法来启动 kind 集群，然后从命令行或 VSCode 运行 e2e 测试，
 将它们附加到现有集群。例如，假设你想测试一些 multikueue-e2e 测试。
 
-运行 `E2E_RUN_ONLY_ENV=true make kind-image-build test-multikueue-e2e` 并等待 `Do you want to cleanup? [Y/n] ` 出现。
+### DEV 模式（推荐）
+使用 `E2E_MODE=dev` 来创建或复用 kind 集群、重新构建/重新部署 Kueue、运行测试，并在测试结束后保留集群以便快速重跑或人工排查：
+
+```shell
+# 若不存在则创建；若已存在则复用。构建镜像、运行测试，并保留集群。
+E2E_MODE=dev make test-e2e
+
+# MultiKueue 的 dev 模式
+E2E_MODE=dev make test-multikueue-e2e
+
+# 循环运行（直到失败），同时保留集群
+E2E_MODE=dev GINKGO_ARGS="--until-it-fails" make test-e2e
+```
+
+测试结束后如需删除保留的集群：
+
+```shell
+kind delete cluster --name kind
+kind delete cluster --name kind-manager
+kind delete cluster --name kind-worker1
+kind delete cluster --name kind-worker2
```

  ```suggestion
  kind delete cluster --name kind-manager kind-worker1 kind-worker2
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-13T14:28:48Z

- **site/content/en/docs/contribution_guidelines/testing.md:140** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-13T14:28:48Z
```diff
@@ -120,7 +120,31 @@ and then you can use GUI of the Ginkgo Test Explorer to run individual tests, pr
 You can use the following approach to start up a kind cluster and then run e2e tests from commandline or VSCode,
 attaching them to the existing cluster. For example, suppose you want to test some of the multikueue-e2e tests.
 
-Run `E2E_RUN_ONLY_ENV=true make kind-image-build test-multikueue-e2e` and wait for the `Do you want to cleanup? [Y/n] ` to appear.
+### DEV mode (recommended)
+Use `E2E_MODE=dev` to create-or-reuse a kind cluster, rebuild/redeploy Kueue, run tests, and keep the cluster running for fast reruns and post-test investigation:
+
+```shell
+# Create if missing, otherwise reuse. Rebuild image, run tests, keep the cluster.
+E2E_MODE=dev make test-e2e
+
+# MultiKueue dev mode
+E2E_MODE=dev make test-multikueue-e2e
+
+# Loop a suite (until it fails) while keeping the cluster
+E2E_MODE=dev GINKGO_ARGS="--until-it-fails" make test-e2e
+```
+
+To delete the kept cluster(s) afterwards:
+
+```shell
+kind delete cluster --name kind
```

  Write a comment that this is for single cluster.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-13T16:15:01Z

- **hack/e2e-test.sh:37** by [@kshalot](https://github.com/kshalot) — 2026-01-13T13:33:00Z
```diff
@@ -25,23 +25,24 @@ ROOT_DIR="$SOURCE_DIR/.."
 source "${SOURCE_DIR}/e2e-common.sh"
 
 function cleanup {
-    if [ "$CREATE_KIND_CLUSTER" == 'true' ]
-    then
-        if [ ! -d "$ARTIFACTS" ]; then
-            mkdir -p "$ARTIFACTS"
-        fi
+    if [ ! -d "$ARTIFACTS" ]; then
+        mkdir -p "$ARTIFACTS"
+    fi
+
+    if e2e_should_delete_cluster; then
         cluster_cleanup "$KIND_CLUSTER_NAME" ""
+    else
+        cluster_collect_artifacts "$KIND_CLUSTER_NAME" ""
+        echo "Keeping kind cluster '$KIND_CLUSTER_NAME' (E2E_MODE=${E2E_MODE})."
+        echo "To delete it: kind delete cluster --name $KIND_CLUSTER_NAME"
```

  Nit: This message and the message for multikueue are slightly inconsistent.
  
  Here, it's:
  ```sh
  echo "Keeping kind cluster '$KIND_CLUSTER_NAME' (E2E_MODE=${E2E_MODE})."
  echo "To delete it: kind delete cluster --name $KIND_CLUSTER_NAME"
  ```
  In multikueue, it's:
  ```sh
  echo "Keeping kind clusters (E2E_MODE=${E2E_MODE})."
  echo "To delete them:"
  echo "  kind delete cluster $MANAGER_KIND_CLUSTER_NAME $WORKER1_KIND_CLUSTER_NAME $WORKER2_KIND_CLUSTER_NAME"
  ```
  
  Maybe a function to echo this message would be useful?

- **hack/e2e-common.sh:169** by [@kshalot](https://github.com/kshalot) — 2026-01-13T16:00:08Z
```diff
@@ -94,12 +103,77 @@ export E2E_TEST_AGNHOST_IMAGE=${E2E_TEST_AGNHOST_IMAGE_WITH_SHA%%@*}
 # $1 cluster name
 # $2 kubeconfig
 function cluster_cleanup {
-    kubectl config --kubeconfig="$2" use-context "kind-$1"
-    
-    $KIND export logs "$ARTIFACTS" --name "$1" || true
-    kubectl describe pods --kubeconfig="$2" -n kueue-system > "$ARTIFACTS/$1-kueue-system-pods.log" || true
-    kubectl describe pods --kubeconfig="$2" > "$ARTIFACTS/$1-default-pods.log" || true
-    $KIND delete cluster --name "$1"
+    local name=$1
+    local kubeconfig=$2
+
+    cluster_collect_artifacts "$name" "$kubeconfig"
+
+    $KIND delete cluster --name "$name"
+    return 0
+}
+
+# $1 cluster name
+# $2 kubeconfig
+function cluster_collect_artifacts {
+    local name=$1
+    local kubeconfig=${2:-}
+
+    local kubeconfig_args=()
+    if [[ -n "${kubeconfig}" ]]; then
+        kubeconfig_args=(--kubeconfig="${kubeconfig}")
+    fi
+
+    kubectl config "${kubeconfig_args[@]}" use-context "kind-${name}" || true
+    $KIND export logs "$ARTIFACTS" --name "$name" || true
+    kubectl describe pods "${kubeconfig_args[@]}" -n kueue-system > "$ARTIFACTS/${name}-kueue-system-pods.log" || true
+    kubectl describe pods "${kubeconfig_args[@]}" > "$ARTIFACTS/${name}-default-pods.log" || true
+}
+
+# $1 cluster name
+function kind_cluster_exists {
+    $KIND get clusters 2>/dev/null | grep -Fxq "$1"
+}
+
+# $1 cluster name
+# $2 kubeconfig file path (optional)
+function kind_write_kubeconfig {
+    if [[ -n "${2:-}" ]]; then
+        mkdir -p "$(dirname "$2")"
+        $KIND get kubeconfig --name "$1" > "$2"
+    fi
+}
+
+# $1 cluster name
+# $2 cluster kind config
+# $3 kubeconfig file path (optional)
+function ensure_kind_cluster {
+    local name=$1
+    local cfg=$2
+    local kubeconfig=$3
+
+    if [[ "${E2E_MODE}" == "dev" ]]; then
+        if kind_cluster_exists "$name"; then
+            echo "Reusing kind cluster: $name (E2E_MODE=dev)"
+            kind_write_kubeconfig "$name" "$kubeconfig"
+            return 0
+        fi
+
+        echo "Creating kind cluster (E2E_MODE=dev): $name"
+        cluster_create "$name" "$cfg" "$kubeconfig"
+        return 0
+    fi
+
+    # CI mode: always start from a clean cluster.
+    if kind_cluster_exists "$name"; then
+        echo "Deleting existing kind cluster for a clean CI run: $name"
+        $KIND delete cluster --name "$name" || true
```

  QQ: If cluster deletion fails and the CI requires a clean cluster, shouldn't the script fail at this point? IIUC `|| true` will make it proceed since we are using `set -o errexit`.

- **hack/multikueue-e2e-test.sh:49** by [@kshalot](https://github.com/kshalot) — 2026-01-13T16:04:18Z
```diff
@@ -32,42 +32,47 @@ export WORKER2_KUBECONFIG="${ARTIFACTS}/kubeconfig-$WORKER2_KIND_CLUSTER_NAME"
 source "${SOURCE_DIR}/e2e-common.sh"
 
 function cleanup {
-    if [ "$CREATE_KIND_CLUSTER" == 'true' ]
-    then
-        if [ ! -d "$ARTIFACTS" ]; then
-            mkdir -p "$ARTIFACTS"
-        fi
+    if [ ! -d "$ARTIFACTS" ]; then
+        mkdir -p "$ARTIFACTS"
+    fi
 
+    if e2e_should_delete_cluster; then
         cluster_cleanup "$MANAGER_KIND_CLUSTER_NAME" "$MANAGER_KUBECONFIG" &
         cluster_cleanup "$WORKER1_KIND_CLUSTER_NAME" "$WORKER1_KUBECONFIG" &
         cluster_cleanup "$WORKER2_KIND_CLUSTER_NAME" "$WORKER2_KUBECONFIG" &
+        wait
+        return 0
     fi
+
+    cluster_collect_artifacts "$MANAGER_KIND_CLUSTER_NAME" "$MANAGER_KUBECONFIG" &
+    cluster_collect_artifacts "$WORKER1_KIND_CLUSTER_NAME" "$WORKER1_KUBECONFIG" &
+    cluster_collect_artifacts "$WORKER2_KIND_CLUSTER_NAME" "$WORKER2_KUBECONFIG" &
```

  `cluster_collect_artifacts` is also called in the `cluster_cleanup` function before the cluster is deleted.
  
  We could collect the artifacts from a single place if we swap the order here, i.e.:
  ```suggestion
      cluster_collect_artifacts "$MANAGER_KIND_CLUSTER_NAME" "$MANAGER_KUBECONFIG" &
      cluster_collect_artifacts "$WORKER1_KIND_CLUSTER_NAME" "$WORKER1_KUBECONFIG" &
      cluster_collect_artifacts "$WORKER2_KIND_CLUSTER_NAME" "$WORKER2_KUBECONFIG" &
      wait
      
      if e2e_should_delete_cluster; then
  cluster_cleanup "$MANAGER_KIND_CLUSTER_NAME" "$MANAGER_KUBECONFIG" &
  cluster_cleanup "$WORKER1_KIND_CLUSTER_NAME" "$WORKER1_KUBECONFIG" &
  cluster_cleanup "$WORKER2_KIND_CLUSTER_NAME" "$WORKER2_KUBECONFIG" &
          wait
          return 0
  fi
  ```
  
  It also makes it clearer that artifact collection always happens before the cluster is considered for deletion (right now this reads kind of as "delete cluster if you should delete, otherwise collect artifacts").
  
  Same comment applies to `cleanup` in `e2e-test.sh`.

- **hack/e2e-common.sh:147** by [@kshalot](https://github.com/kshalot) — 2026-01-13T16:05:55Z
```diff
@@ -94,12 +103,77 @@ export E2E_TEST_AGNHOST_IMAGE=${E2E_TEST_AGNHOST_IMAGE_WITH_SHA%%@*}
 # $1 cluster name
 # $2 kubeconfig
 function cluster_cleanup {
-    kubectl config --kubeconfig="$2" use-context "kind-$1"
-    
-    $KIND export logs "$ARTIFACTS" --name "$1" || true
-    kubectl describe pods --kubeconfig="$2" -n kueue-system > "$ARTIFACTS/$1-kueue-system-pods.log" || true
-    kubectl describe pods --kubeconfig="$2" > "$ARTIFACTS/$1-default-pods.log" || true
-    $KIND delete cluster --name "$1"
+    local name=$1
+    local kubeconfig=$2
+
+    cluster_collect_artifacts "$name" "$kubeconfig"
+
+    $KIND delete cluster --name "$name"
+    return 0
+}
+
+# $1 cluster name
+# $2 kubeconfig
+function cluster_collect_artifacts {
+    local name=$1
+    local kubeconfig=${2:-}
+
+    local kubeconfig_args=()
+    if [[ -n "${kubeconfig}" ]]; then
+        kubeconfig_args=(--kubeconfig="${kubeconfig}")
+    fi
+
+    kubectl config "${kubeconfig_args[@]}" use-context "kind-${name}" || true
+    $KIND export logs "$ARTIFACTS" --name "$name" || true
+    kubectl describe pods "${kubeconfig_args[@]}" -n kueue-system > "$ARTIFACTS/${name}-kueue-system-pods.log" || true
+    kubectl describe pods "${kubeconfig_args[@]}" > "$ARTIFACTS/${name}-default-pods.log" || true
+}
+
+# $1 cluster name
+function kind_cluster_exists {
+    $KIND get clusters 2>/dev/null | grep -Fxq "$1"
+}
+
+# $1 cluster name
+# $2 kubeconfig file path (optional)
+function kind_write_kubeconfig {
+    if [[ -n "${2:-}" ]]; then
+        mkdir -p "$(dirname "$2")"
+        $KIND get kubeconfig --name "$1" > "$2"
+    fi
+}
+
+# $1 cluster name
+# $2 cluster kind config
+# $3 kubeconfig file path (optional)
+function ensure_kind_cluster {
```

  Nit: This would sound more natural to me:
  ```suggestion
  function ensure_kind_cluster_exists {
  ```
  But I don't feel that strongly about it.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-13T16:16:55Z

- **hack/e2e-test.sh:37** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-13T16:16:55Z
```diff
@@ -25,23 +25,24 @@ ROOT_DIR="$SOURCE_DIR/.."
 source "${SOURCE_DIR}/e2e-common.sh"
 
 function cleanup {
-    if [ "$CREATE_KIND_CLUSTER" == 'true' ]
-    then
-        if [ ! -d "$ARTIFACTS" ]; then
-            mkdir -p "$ARTIFACTS"
-        fi
+    if [ ! -d "$ARTIFACTS" ]; then
+        mkdir -p "$ARTIFACTS"
+    fi
+
+    if e2e_should_delete_cluster; then
         cluster_cleanup "$KIND_CLUSTER_NAME" ""
+    else
+        cluster_collect_artifacts "$KIND_CLUSTER_NAME" ""
+        echo "Keeping kind cluster '$KIND_CLUSTER_NAME' (E2E_MODE=${E2E_MODE})."
+        echo "To delete it: kind delete cluster --name $KIND_CLUSTER_NAME"
```

  ```suggestion
          echo "To delete it: kind delete clusters --name $KIND_CLUSTER_NAME"
  ```

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-14T11:25:25Z

- **site/content/en/docs/contribution_guidelines/testing.md:128** by [@kshalot](https://github.com/kshalot) — 2026-01-14T11:25:25Z
```diff
@@ -120,7 +120,31 @@ and then you can use GUI of the Ginkgo Test Explorer to run individual tests, pr
 You can use the following approach to start up a kind cluster and then run e2e tests from commandline or VSCode,
 attaching them to the existing cluster. For example, suppose you want to test some of the multikueue-e2e tests.
 
-Run `E2E_RUN_ONLY_ENV=true make kind-image-build test-multikueue-e2e` and wait for the `Do you want to cleanup? [Y/n] ` to appear.
+### DEV mode (recommended)
+Use `E2E_MODE=dev` to create-or-reuse a kind cluster, rebuild/redeploy Kueue, run tests, and keep the cluster running for fast reruns and post-test investigation:
+
+```shell
+# Create if missing, otherwise reuse. Rebuild image, run tests, keep the cluster.
+E2E_MODE=dev make test-e2e
```

  "Create if missing, otherwise reuse." - I'd mention what this reuses, like:
  
  ```suggestion
  # Create if missing, otherwise reuse cluster. Rebuild image, run tests, keep the cluster.
  E2E_MODE=dev make test-e2e
  ```
  
  "Rebuild image" - it does rebuild some images, but not the main Kueue one (I think). I think you have to manually run `make kind-image-build`, otherwise this will fail with:
  ```
  Error response from daemon: reference does not exist
  ```
  when trying to load the image ref into kind.
  
  This is true in this PR and on main. So this should probably be included in this document.

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-01-16T02:29:17Z

- **hack/e2e-common.sh:169** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-01-16T02:29:17Z
```diff
@@ -94,12 +103,77 @@ export E2E_TEST_AGNHOST_IMAGE=${E2E_TEST_AGNHOST_IMAGE_WITH_SHA%%@*}
 # $1 cluster name
 # $2 kubeconfig
 function cluster_cleanup {
-    kubectl config --kubeconfig="$2" use-context "kind-$1"
-    
-    $KIND export logs "$ARTIFACTS" --name "$1" || true
-    kubectl describe pods --kubeconfig="$2" -n kueue-system > "$ARTIFACTS/$1-kueue-system-pods.log" || true
-    kubectl describe pods --kubeconfig="$2" > "$ARTIFACTS/$1-default-pods.log" || true
-    $KIND delete cluster --name "$1"
+    local name=$1
+    local kubeconfig=$2
+
+    cluster_collect_artifacts "$name" "$kubeconfig"
+
+    $KIND delete cluster --name "$name"
+    return 0
+}
+
+# $1 cluster name
+# $2 kubeconfig
+function cluster_collect_artifacts {
+    local name=$1
+    local kubeconfig=${2:-}
+
+    local kubeconfig_args=()
+    if [[ -n "${kubeconfig}" ]]; then
+        kubeconfig_args=(--kubeconfig="${kubeconfig}")
+    fi
+
+    kubectl config "${kubeconfig_args[@]}" use-context "kind-${name}" || true
+    $KIND export logs "$ARTIFACTS" --name "$name" || true
+    kubectl describe pods "${kubeconfig_args[@]}" -n kueue-system > "$ARTIFACTS/${name}-kueue-system-pods.log" || true
+    kubectl describe pods "${kubeconfig_args[@]}" > "$ARTIFACTS/${name}-default-pods.log" || true
+}
+
+# $1 cluster name
+function kind_cluster_exists {
+    $KIND get clusters 2>/dev/null | grep -Fxq "$1"
+}
+
+# $1 cluster name
+# $2 kubeconfig file path (optional)
+function kind_write_kubeconfig {
+    if [[ -n "${2:-}" ]]; then
+        mkdir -p "$(dirname "$2")"
+        $KIND get kubeconfig --name "$1" > "$2"
+    fi
+}
+
+# $1 cluster name
+# $2 cluster kind config
+# $3 kubeconfig file path (optional)
+function ensure_kind_cluster {
+    local name=$1
+    local cfg=$2
+    local kubeconfig=$3
+
+    if [[ "${E2E_MODE}" == "dev" ]]; then
+        if kind_cluster_exists "$name"; then
+            echo "Reusing kind cluster: $name (E2E_MODE=dev)"
+            kind_write_kubeconfig "$name" "$kubeconfig"
+            return 0
+        fi
+
+        echo "Creating kind cluster (E2E_MODE=dev): $name"
+        cluster_create "$name" "$cfg" "$kubeconfig"
+        return 0
+    fi
+
+    # CI mode: always start from a clean cluster.
+    if kind_cluster_exists "$name"; then
+        echo "Deleting existing kind cluster for a clean CI run: $name"
+        $KIND delete cluster --name "$name" || true
```

  Agree, this should fail the run, since ["Errors will only occur if the cluster resources exist and are not able to be deleted."](https://github.com/kubernetes-sigs/kind/blob/744d4d12b4d7560de9009a66de0e8003a7e69926/pkg/cmd/kind/delete/cluster/deletecluster.go#L50C1-L50C86)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T15:59:53Z

- **site/content/zh-CN/docs/contribution_guidelines/testing.md:123** by [@mimowo](https://github.com/mimowo) — 2026-01-16T15:59:53Z
```diff
@@ -120,7 +120,28 @@ func TestValidateClusterQueue(t *testing.T) {
 你可以使用以下方法来启动 kind 集群，然后从命令行或 VSCode 运行 e2e 测试，
 将它们附加到现有集群。例如，假设你想测试一些 multikueue-e2e 测试。
 
-运行 `E2E_RUN_ONLY_ENV=true make kind-image-build test-multikueue-e2e` 并等待 `Do you want to cleanup? [Y/n] ` 出现。
+### DEV 模式（推荐）
```

  nit, lets revert that translation

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T16:01:26Z

- **site/content/zh-CN/docs/contribution_guidelines/testing.md:123** by [@mimowo](https://github.com/mimowo) — 2026-01-16T16:01:26Z
```diff
@@ -120,7 +120,28 @@ func TestValidateClusterQueue(t *testing.T) {
 你可以使用以下方法来启动 kind 集群，然后从命令行或 VSCode 运行 e2e 测试，
 将它们附加到现有集群。例如，假设你想测试一些 multikueue-e2e 测试。
 
-运行 `E2E_RUN_ONLY_ENV=true make kind-image-build test-multikueue-e2e` 并等待 `Do you want to cleanup? [Y/n] ` 出现。
+### DEV 模式（推荐）
```

  we can follow up and ask @windsonsea or @my-git9 for review

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-01-16T17:00:34Z

- **site/content/zh-CN/docs/contribution_guidelines/testing.md:123** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-01-16T17:00:33Z
```diff
@@ -120,7 +120,28 @@ func TestValidateClusterQueue(t *testing.T) {
 你可以使用以下方法来启动 kind 集群，然后从命令行或 VSCode 运行 e2e 测试，
 将它们附加到现有集群。例如，假设你想测试一些 multikueue-e2e 测试。
 
-运行 `E2E_RUN_ONLY_ENV=true make kind-image-build test-multikueue-e2e` 并等待 `Do you want to cleanup? [Y/n] ` 出现。
+### DEV 模式（推荐）
```

  Do you mean here or better create separate PR just with translations?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T17:03:14Z

- **site/content/zh-CN/docs/contribution_guidelines/testing.md:123** by [@mimowo](https://github.com/mimowo) — 2026-01-16T17:03:14Z
```diff
@@ -120,7 +120,28 @@ func TestValidateClusterQueue(t *testing.T) {
 你可以使用以下方法来启动 kind 集群，然后从命令行或 VSCode 运行 e2e 测试，
 将它们附加到现有集群。例如，假设你想测试一些 multikueue-e2e 测试。
 
-运行 `E2E_RUN_ONLY_ENV=true make kind-image-build test-multikueue-e2e` 并等待 `Do you want to cleanup? [Y/n] ` 出现。
+### DEV 模式（推荐）
```

  separete PR

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-16T17:05:49Z

- **hack/e2e-common.sh:110** by [@kshalot](https://github.com/kshalot) — 2026-01-16T17:05:49Z
```diff
@@ -95,12 +103,75 @@ export E2E_TEST_AGNHOST_IMAGE=${E2E_TEST_AGNHOST_IMAGE_WITH_SHA%%@*}
 # $1 cluster name
 # $2 kubeconfig
 function cluster_cleanup {
-    kubectl config --kubeconfig="$2" use-context "kind-$1"
-    
-    $KIND export logs "$ARTIFACTS" --name "$1" || true
-    kubectl describe pods --kubeconfig="$2" -n kueue-system > "$ARTIFACTS/$1-kueue-system-pods.log" || true
-    kubectl describe pods --kubeconfig="$2" > "$ARTIFACTS/$1-default-pods.log" || true
-    $KIND delete cluster --name "$1"
+    local name=$1
+    local kubeconfig=$2
+
+    kubectl config --kubeconfig="$kubeconfig" use-context "kind-${name}"
+    $KIND delete cluster --name "$name"
```

  Just to make sure I understand, what does the first line do to fix the issue with locked kubeconfig? The multikueue script cleans up with:
  ```sh
          cluster_cleanup "$MANAGER_KIND_CLUSTER_NAME" "$MANAGER_KUBECONFIG" &
          cluster_cleanup "$WORKER1_KIND_CLUSTER_NAME" "$WORKER1_KUBECONFIG" &
          cluster_cleanup "$WORKER2_KIND_CLUSTER_NAME" "$WORKER2_KUBECONFIG" &
  ```
  Since it's executed in parallel, wouldn't there be a race condition since all 3 functions race to change the context? Why do we do the switch?

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-16T17:09:33Z

- **hack/e2e-common.sh:110** by [@kshalot](https://github.com/kshalot) — 2026-01-16T17:09:33Z
```diff
@@ -95,12 +103,75 @@ export E2E_TEST_AGNHOST_IMAGE=${E2E_TEST_AGNHOST_IMAGE_WITH_SHA%%@*}
 # $1 cluster name
 # $2 kubeconfig
 function cluster_cleanup {
-    kubectl config --kubeconfig="$2" use-context "kind-$1"
-    
-    $KIND export logs "$ARTIFACTS" --name "$1" || true
-    kubectl describe pods --kubeconfig="$2" -n kueue-system > "$ARTIFACTS/$1-kueue-system-pods.log" || true
-    kubectl describe pods --kubeconfig="$2" > "$ARTIFACTS/$1-default-pods.log" || true
-    $KIND delete cluster --name "$1"
+    local name=$1
+    local kubeconfig=$2
+
+    kubectl config --kubeconfig="$kubeconfig" use-context "kind-${name}"
+    $KIND delete cluster --name "$name"
```

  Nevermind the race part - IIUC the `--kubeconfig="$kubeconfig"` part is the secret sauce here. But still I'd like to understand how does this help with the lock issue

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-01-16T17:13:07Z

- **hack/e2e-common.sh:110** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-01-16T17:13:07Z
```diff
@@ -95,12 +103,75 @@ export E2E_TEST_AGNHOST_IMAGE=${E2E_TEST_AGNHOST_IMAGE_WITH_SHA%%@*}
 # $1 cluster name
 # $2 kubeconfig
 function cluster_cleanup {
-    kubectl config --kubeconfig="$2" use-context "kind-$1"
-    
-    $KIND export logs "$ARTIFACTS" --name "$1" || true
-    kubectl describe pods --kubeconfig="$2" -n kueue-system > "$ARTIFACTS/$1-kueue-system-pods.log" || true
-    kubectl describe pods --kubeconfig="$2" > "$ARTIFACTS/$1-default-pods.log" || true
-    $KIND delete cluster --name "$1"
+    local name=$1
+    local kubeconfig=$2
+
+    kubectl config --kubeconfig="$kubeconfig" use-context "kind-${name}"
+    $KIND delete cluster --name "$name"
```

  What that first line actually does
  kubectl config --kubeconfig="$kubeconfig" use-context "kind-${name}" writes current-context: kind-${name} into the specific kubeconfig file passed in. It does not change any “global” context (it doesn’t touch ~/.kube/config) because of the explicit --kubeconfig=....
  Is there a race when cleanup runs in parallel?
  In the multikueue script, no: each background call uses a different file:
  ${ARTIFACTS}/kubeconfig-$MANAGER_KIND_CLUSTER_NAME
  ${ARTIFACTS}/kubeconfig-$WORKER1_KIND_CLUSTER_NAME
  ${ARTIFACTS}/kubeconfig-$WORKER2_KIND_CLUSTER_NAME
  So the three kubectl config ... use-context ... calls are writing to different files, and don’t race with each other.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-16T17:17:22Z

- **hack/e2e-common.sh:110** by [@kshalot](https://github.com/kshalot) — 2026-01-16T17:17:22Z
```diff
@@ -95,12 +103,75 @@ export E2E_TEST_AGNHOST_IMAGE=${E2E_TEST_AGNHOST_IMAGE_WITH_SHA%%@*}
 # $1 cluster name
 # $2 kubeconfig
 function cluster_cleanup {
-    kubectl config --kubeconfig="$2" use-context "kind-$1"
-    
-    $KIND export logs "$ARTIFACTS" --name "$1" || true
-    kubectl describe pods --kubeconfig="$2" -n kueue-system > "$ARTIFACTS/$1-kueue-system-pods.log" || true
-    kubectl describe pods --kubeconfig="$2" > "$ARTIFACTS/$1-default-pods.log" || true
-    $KIND delete cluster --name "$1"
+    local name=$1
+    local kubeconfig=$2
+
+    kubectl config --kubeconfig="$kubeconfig" use-context "kind-${name}"
+    $KIND delete cluster --name "$name"
```

  Yeah I got that, but what exactly is different when current context is changed in those files?
  
  If for some reason the current context is unassigned by default and kind needs it for deletion then I'm wondering why it only failed for manager and not for all 3 in the previous CI run.
  
  
  Maybe I'm digging too deep here, but I'd like to make sure I understand

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-01-16T18:21:24Z

- **site/content/zh-CN/docs/contribution_guidelines/testing.md:123** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-01-16T18:21:24Z
```diff
@@ -120,7 +120,28 @@ func TestValidateClusterQueue(t *testing.T) {
 你可以使用以下方法来启动 kind 集群，然后从命令行或 VSCode 运行 e2e 测试，
 将它们附加到现有集群。例如，假设你想测试一些 multikueue-e2e 测试。
 
-运行 `E2E_RUN_ONLY_ENV=true make kind-image-build test-multikueue-e2e` 并等待 `Do you want to cleanup? [Y/n] ` 出现。
+### DEV 模式（推荐）
```

  https://github.com/kubernetes-sigs/kueue/pull/8640

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-01-16T18:22:54Z

- **hack/e2e-common.sh:110** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-01-16T18:22:54Z
```diff
@@ -95,12 +103,75 @@ export E2E_TEST_AGNHOST_IMAGE=${E2E_TEST_AGNHOST_IMAGE_WITH_SHA%%@*}
 # $1 cluster name
 # $2 kubeconfig
 function cluster_cleanup {
-    kubectl config --kubeconfig="$2" use-context "kind-$1"
-    
-    $KIND export logs "$ARTIFACTS" --name "$1" || true
-    kubectl describe pods --kubeconfig="$2" -n kueue-system > "$ARTIFACTS/$1-kueue-system-pods.log" || true
-    kubectl describe pods --kubeconfig="$2" > "$ARTIFACTS/$1-default-pods.log" || true
-    $KIND delete cluster --name "$1"
+    local name=$1
+    local kubeconfig=$2
+
+    kubectl config --kubeconfig="$kubeconfig" use-context "kind-${name}"
+    $KIND delete cluster --name "$name"
```

  I think it failed for the first one and stopped the execution, since it's running in parallel the order is not deterministic

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-19T08:56:22Z

- **hack/e2e-common.sh:110** by [@kshalot](https://github.com/kshalot) — 2026-01-19T08:56:21Z
```diff
@@ -95,12 +103,75 @@ export E2E_TEST_AGNHOST_IMAGE=${E2E_TEST_AGNHOST_IMAGE_WITH_SHA%%@*}
 # $1 cluster name
 # $2 kubeconfig
 function cluster_cleanup {
-    kubectl config --kubeconfig="$2" use-context "kind-$1"
-    
-    $KIND export logs "$ARTIFACTS" --name "$1" || true
-    kubectl describe pods --kubeconfig="$2" -n kueue-system > "$ARTIFACTS/$1-kueue-system-pods.log" || true
-    kubectl describe pods --kubeconfig="$2" > "$ARTIFACTS/$1-default-pods.log" || true
-    $KIND delete cluster --name "$1"
+    local name=$1
+    local kubeconfig=$2
+
+    kubectl config --kubeconfig="$kubeconfig" use-context "kind-${name}"
+    $KIND delete cluster --name "$name"
```

  Since it's ran in the background and `wait`ed without specific pids, I don't think failures would stop execution.
  From the [manual](https://www.man7.org/linux/man-pages/man1/wait.1p.html):
  > If the wait utility is invoked with no operands, it shall wait
         until all process IDs known to the invoking shell have terminated
         and exit with a zero exit status.
  
  The logs in that run are consistent with this:
  ```
  Deleting cluster "kind-manager" ...
  Deleting cluster "kind-worker2" ...
  Deleting cluster "kind-worker1" ...
  failed to update kubeconfig: failed to lock config file: open /logs/artifacts/run-test-multikueue-e2e-1.34.3/kubeconfig-kind-manager.lock: file exists
  Deleted nodes: ["kind-manager-worker" "kind-manager-control-plane"]
  ERROR: failed to delete cluster "kind-manager": failed to lock config file: open /logs/artifacts/run-test-multikueue-e2e-1.34.3/kubeconfig-kind-manager.lock: file exists
  Deleted nodes: ["kind-worker1-worker" "kind-worker1-control-plane"]
  Deleted nodes: ["kind-worker2-control-plane" "kind-worker2-worker"]
  + EXIT_VALUE=0
  ```
  
  I'm trying to understand whether we actually need the `kubectl config --kubeconfig="$kubeconfig" use-context "kind-${name}"` line or did it just worked after it was added because the error is rare. It might even be unrelated to this PR, but then we shouldn't keep the "fix".
  
  Thoughts? Also @mimowo if you want to chip in

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-01-19T09:45:39Z

- **hack/e2e-common.sh:110** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-01-19T09:45:39Z
```diff
@@ -95,12 +103,75 @@ export E2E_TEST_AGNHOST_IMAGE=${E2E_TEST_AGNHOST_IMAGE_WITH_SHA%%@*}
 # $1 cluster name
 # $2 kubeconfig
 function cluster_cleanup {
-    kubectl config --kubeconfig="$2" use-context "kind-$1"
-    
-    $KIND export logs "$ARTIFACTS" --name "$1" || true
-    kubectl describe pods --kubeconfig="$2" -n kueue-system > "$ARTIFACTS/$1-kueue-system-pods.log" || true
-    kubectl describe pods --kubeconfig="$2" > "$ARTIFACTS/$1-default-pods.log" || true
-    $KIND delete cluster --name "$1"
+    local name=$1
+    local kubeconfig=$2
+
+    kubectl config --kubeconfig="$kubeconfig" use-context "kind-${name}"
+    $KIND delete cluster --name "$name"
```

  `kind cluster delete` requires proper kubeconfig(while cluster is removed without it, kubeconfig to removed cluster persists if kubecofig not passed and this why this error happens), after trying without it - it errors, so I reverted the change because context set up was present there before my change

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-19T09:48:31Z

- **hack/e2e-common.sh:110** by [@kshalot](https://github.com/kshalot) — 2026-01-19T09:48:31Z
```diff
@@ -95,12 +103,75 @@ export E2E_TEST_AGNHOST_IMAGE=${E2E_TEST_AGNHOST_IMAGE_WITH_SHA%%@*}
 # $1 cluster name
 # $2 kubeconfig
 function cluster_cleanup {
-    kubectl config --kubeconfig="$2" use-context "kind-$1"
-    
-    $KIND export logs "$ARTIFACTS" --name "$1" || true
-    kubectl describe pods --kubeconfig="$2" -n kueue-system > "$ARTIFACTS/$1-kueue-system-pods.log" || true
-    kubectl describe pods --kubeconfig="$2" > "$ARTIFACTS/$1-default-pods.log" || true
-    $KIND delete cluster --name "$1"
+    local name=$1
+    local kubeconfig=$2
+
+    kubectl config --kubeconfig="$kubeconfig" use-context "kind-${name}"
+    $KIND delete cluster --name "$name"
```

  Okay, I see it.
