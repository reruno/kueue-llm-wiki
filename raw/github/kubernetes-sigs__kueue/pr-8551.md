# PR #8551: [KEP] Add a mechanism to consider preemption cost when finding preemption candidates

**Summary**: [KEP] Add a mechanism to consider preemption cost when finding preemption candidates

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8551

**Last updated**: 2026-03-04T16:13:31Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@vladikkuzn](https://github.com/vladikkuzn)
- **Branch**: `preemption-cost` → `main`
- **Created**: 2026-01-12T17:23:38Z
- **Updated**: 2026-03-04T16:13:31Z
- **Merged**: 2026-03-04T14:46:19Z
- **Closed**: 2026-03-04T14:46:19Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`, `kind/kep`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@vladikkuzn](https://github.com/vladikkuzn), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@amy](https://github.com/amy), [@mimowo](https://github.com/mimowo), [@pajakd](https://github.com/pajakd), [@sohankunkerkar](https://github.com/sohankunkerkar), [@sanposhiho](https://github.com/sanposhiho)
- **Changed files**: 2   **+410 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #7990

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-01-12T17:23:45Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 82583f1e1b4bd45ee05aecead1774efd743e86d5 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69a7eba86cef8f000878f581 |

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-01-12T17:23:45Z

/assign

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-01-12T17:24:32Z

/cc @mimowo

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-13T18:18:57Z

@mimowo @mwysokin Is this targeting v0.16.0?

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-01-13T18:57:44Z

@tenzen-y No, for the 0.17

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-14T17:47:35Z

> @tenzen-y No, for the 0.17

Thank you for checking that. Let me revisit here after 0.16.

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-20T03:25:16Z

/cc @sanposhiho @amy

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-25T03:47:37Z

Revisiting here right now.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-27T16:38:49Z

/kind kep
Adding new label for the ease of filtering PRs which are KEPs  / designs are require special handling

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-03T18:15:44Z

LGTM label has been added.  <details>Git tree hash: 066a7986a6f93b97d682a0ab9937216f615120e3</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-04T14:29:12Z

LGTM label has been added.  <details>Git tree hash: 65c4c8143551bfefb961c2107c6b7a4c427adf39</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-04T14:29:13Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8551#pullrequestreview-3884321891" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8551#pullrequestreview-3889784878" title="Approved">tenzen-y</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8551#" title="Author self-approved">vladikkuzn</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-01-12T19:06:14Z

- **keps/7990-preemption-cost/README.md:173** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-12T19:06:12Z
```diff
@@ -0,0 +1,190 @@
+# KEP-7990: Preemption cost signal for workload preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Ordering semantics](#ordering-semantics)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional, externally-populated **preemption cost signal** on `Workload`
+and make Kueue consider this signal when ordering preemption candidates.
+
+When multiple preemption candidates have the **same workload priority**, Kueue
+should prefer preempting the candidate with the **lower** preemption cost.
+
+This enables cluster operators to steer preemption toward workloads for which
+preemption is expected to be less disruptive (e.g. based on checkpointing, phase
+of execution, or prior preemptions), without hardcoding cost policies into Kueue.
+
+## Motivation
+
+The “cost” of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate these runtime cost signals.
+
+This KEP adds a generic mechanism to accept an external signal and use it to
+reduce wasted work and improve overall system efficiency.
+
+### Goals
+
+- Add a new optional `Workload` field that can be set by an external controller
+  to express preemption cost.
+- Use preemption cost as a **tie-breaker among equal-priority preemption
+  candidates**.
+- Provide a reference implementation of an external controller that populates
+  the field (simple, policy-neutral example).
+
+### Non-Goals
+
+- Define a universal “correct” cost model inside Kueue.
+- Change preemption eligibility semantics (preemption policies, reclaim rules,
+  priority comparisons) beyond candidate ordering tie-breaks.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional preemption cost signal on `Workload.status` and incorporate it
+into the preemption candidate ordering logic.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt when multiple candidates have the same priority.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware) to compute preemption cost and have Kueue respect it without
+  modifying the scheduler.
+
+### Ordering semantics
+
+For preemption candidate ordering, Kueue currently compares candidates by:
+
+- whether already evicted
+- whether in a different ClusterQueue than the preemptor
+- (optional) local queue fair-sharing usage
+- **workload priority**
+- quota reservation time (more recent first)
+- UID for deterministic ordering
+
+This KEP adds one step:
+
+- If (and only if) two candidates have **equal priority**, compare their
+  preemption cost and prefer **lower** cost first.
+
+### Defaulting and backward compatibility
+
+- If a workload has no preemption cost set, it is treated as **zero** cost.
+  - This preserves existing behavior when the field is unused (all costs equal).
+- The feature is “opt-in by signal”: behavior changes only when an external
+  controller populates the field.
+
+### Risks and Mitigations
+
+- **Risk**: External controller writes unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering; keep priority as primary signal.
+- **Risk**: Multiple writers to the same status field could conflict.
+  - **Mitigation**: Document that a single controller should own the field; use
+    merge patch on status updates; recommend a dedicated field manager.
+
+## Design Details
+
+### API changes
```

  @vladikkuzn Could you propose a Go and YAML API scheme at least here, the same as other KEPs?

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-01-12T22:19:01Z

- **keps/7990-preemption-cost/README.md:137** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-01-12T22:19:01Z
```diff
@@ -0,0 +1,222 @@
+# KEP-7990: Preemption cost signal for workload preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Ordering semantics](#ordering-semantics)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional, externally-populated **preemption cost signal** on `Workload`
+and make Kueue consider this signal when ordering preemption candidates.
+
+When multiple preemption candidates have the **same workload priority**, Kueue
+should prefer preempting the candidate with the **lower** preemption cost.
+
+This enables cluster operators to steer preemption toward workloads for which
+preemption is expected to be less disruptive (e.g. based on checkpointing, phase
+of execution, or prior preemptions), without hardcoding cost policies into Kueue.
+
+## Motivation
+
+The “cost” of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate these runtime cost signals.
+
+This KEP adds a generic mechanism to accept an external signal and use it to
+reduce wasted work and improve overall system efficiency.
+
+### Goals
+
+- Add a new optional `Workload` field that can be set by an external controller
+  to express preemption cost.
+- Use preemption cost as a **tie-breaker among equal-priority preemption
+  candidates**.
+- Provide a reference implementation of an external controller that populates
+  the field (simple, policy-neutral example).
+
+### Non-Goals
+
+- Define a universal “correct” cost model inside Kueue.
+- Change preemption eligibility semantics (preemption policies, reclaim rules,
+  priority comparisons) beyond candidate ordering tie-breaks.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional preemption cost signal on `Workload.status` and incorporate it
+into the preemption candidate ordering logic.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt when multiple candidates have the same priority.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware) to compute preemption cost and have Kueue respect it without
+  modifying the scheduler.
+
+### Ordering semantics
+
+For preemption candidate ordering, Kueue currently compares candidates by:
+
+- whether already evicted
+- whether in a different ClusterQueue than the preemptor
+- (optional) local queue fair-sharing usage
+- **workload priority**
+- quota reservation time (more recent first)
+- UID for deterministic ordering
+
+This KEP adds one step:
+
+- If (and only if) two candidates have **equal priority**, compare their
+  preemption cost and prefer **lower** cost first.
+
+### Defaulting and backward compatibility
+
+- If a workload has no preemption cost set, it is treated as **zero** cost.
+  - This preserves existing behavior when the field is unused (all costs equal).
+- The feature is “opt-in by signal”: behavior changes only when an external
+  controller populates the field.
+
+### Risks and Mitigations
+
+- **Risk**: External controller writes unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering; keep priority as primary signal.
+- **Risk**: Multiple writers to the same status field could conflict.
+  - **Mitigation**: Document that a single controller should own the field; use
+    merge patch on status updates; recommend a dedicated field manager.
+
+## Design Details
+
+### API changes
+
+Add a new optional field to `WorkloadStatus`:
+
+- `status.preemptionCost`: `resource.Quantity`
+  - **Semantics**: unitless score, higher means more expensive to preempt.
+  - **Unset**: equivalent to zero.
+
+Go API (illustrative):
+
+```golang
+import "k8s.io/apimachinery/pkg/api/resource"
+
+type WorkloadStatus struct {
+  // ... existing fields ...
+
+  // preemptionCost is an optional, externally populated signal that indicates
+  // how expensive it is to preempt this Workload.
+  //
+  // Higher values mean more expensive to preempt.
+  // If unset, it is treated as 0.
+  //
+  // +optional
+  PreemptionCost *resource.Quantity `json:"preemptionCost,omitempty"`
```

  Should it be `resource.Quantity`, which can support something like seconds, or a plain int would be enough?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-01-14T17:48:57Z

- **keps/7990-preemption-cost/README.md:137** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-14T17:48:57Z
```diff
@@ -0,0 +1,222 @@
+# KEP-7990: Preemption cost signal for workload preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Ordering semantics](#ordering-semantics)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional, externally-populated **preemption cost signal** on `Workload`
+and make Kueue consider this signal when ordering preemption candidates.
+
+When multiple preemption candidates have the **same workload priority**, Kueue
+should prefer preempting the candidate with the **lower** preemption cost.
+
+This enables cluster operators to steer preemption toward workloads for which
+preemption is expected to be less disruptive (e.g. based on checkpointing, phase
+of execution, or prior preemptions), without hardcoding cost policies into Kueue.
+
+## Motivation
+
+The “cost” of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate these runtime cost signals.
+
+This KEP adds a generic mechanism to accept an external signal and use it to
+reduce wasted work and improve overall system efficiency.
+
+### Goals
+
+- Add a new optional `Workload` field that can be set by an external controller
+  to express preemption cost.
+- Use preemption cost as a **tie-breaker among equal-priority preemption
+  candidates**.
+- Provide a reference implementation of an external controller that populates
+  the field (simple, policy-neutral example).
+
+### Non-Goals
+
+- Define a universal “correct” cost model inside Kueue.
+- Change preemption eligibility semantics (preemption policies, reclaim rules,
+  priority comparisons) beyond candidate ordering tie-breaks.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional preemption cost signal on `Workload.status` and incorporate it
+into the preemption candidate ordering logic.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt when multiple candidates have the same priority.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware) to compute preemption cost and have Kueue respect it without
+  modifying the scheduler.
+
+### Ordering semantics
+
+For preemption candidate ordering, Kueue currently compares candidates by:
+
+- whether already evicted
+- whether in a different ClusterQueue than the preemptor
+- (optional) local queue fair-sharing usage
+- **workload priority**
+- quota reservation time (more recent first)
+- UID for deterministic ordering
+
+This KEP adds one step:
+
+- If (and only if) two candidates have **equal priority**, compare their
+  preemption cost and prefer **lower** cost first.
+
+### Defaulting and backward compatibility
+
+- If a workload has no preemption cost set, it is treated as **zero** cost.
+  - This preserves existing behavior when the field is unused (all costs equal).
+- The feature is “opt-in by signal”: behavior changes only when an external
+  controller populates the field.
+
+### Risks and Mitigations
+
+- **Risk**: External controller writes unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering; keep priority as primary signal.
+- **Risk**: Multiple writers to the same status field could conflict.
+  - **Mitigation**: Document that a single controller should own the field; use
+    merge patch on status updates; recommend a dedicated field manager.
+
+## Design Details
+
+### API changes
+
+Add a new optional field to `WorkloadStatus`:
+
+- `status.preemptionCost`: `resource.Quantity`
+  - **Semantics**: unitless score, higher means more expensive to preempt.
+  - **Unset**: equivalent to zero.
+
+Go API (illustrative):
+
+```golang
+import "k8s.io/apimachinery/pkg/api/resource"
+
+type WorkloadStatus struct {
+  // ... existing fields ...
+
+  // preemptionCost is an optional, externally populated signal that indicates
+  // how expensive it is to preempt this Workload.
+  //
+  // Higher values mean more expensive to preempt.
+  // If unset, it is treated as 0.
+  //
+  // +optional
+  PreemptionCost *resource.Quantity `json:"preemptionCost,omitempty"`
```

  Which data type do you assume? Integer? Float? or something?
  If we assume float, we should select resource.Quantity.
  Or we might be able to use int64 and custom scale in case of float representation.

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-01-15T12:09:56Z

- **keps/7990-preemption-cost/README.md:137** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-01-15T12:09:55Z
```diff
@@ -0,0 +1,222 @@
+# KEP-7990: Preemption cost signal for workload preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Ordering semantics](#ordering-semantics)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional, externally-populated **preemption cost signal** on `Workload`
+and make Kueue consider this signal when ordering preemption candidates.
+
+When multiple preemption candidates have the **same workload priority**, Kueue
+should prefer preempting the candidate with the **lower** preemption cost.
+
+This enables cluster operators to steer preemption toward workloads for which
+preemption is expected to be less disruptive (e.g. based on checkpointing, phase
+of execution, or prior preemptions), without hardcoding cost policies into Kueue.
+
+## Motivation
+
+The “cost” of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate these runtime cost signals.
+
+This KEP adds a generic mechanism to accept an external signal and use it to
+reduce wasted work and improve overall system efficiency.
+
+### Goals
+
+- Add a new optional `Workload` field that can be set by an external controller
+  to express preemption cost.
+- Use preemption cost as a **tie-breaker among equal-priority preemption
+  candidates**.
+- Provide a reference implementation of an external controller that populates
+  the field (simple, policy-neutral example).
+
+### Non-Goals
+
+- Define a universal “correct” cost model inside Kueue.
+- Change preemption eligibility semantics (preemption policies, reclaim rules,
+  priority comparisons) beyond candidate ordering tie-breaks.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional preemption cost signal on `Workload.status` and incorporate it
+into the preemption candidate ordering logic.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt when multiple candidates have the same priority.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware) to compute preemption cost and have Kueue respect it without
+  modifying the scheduler.
+
+### Ordering semantics
+
+For preemption candidate ordering, Kueue currently compares candidates by:
+
+- whether already evicted
+- whether in a different ClusterQueue than the preemptor
+- (optional) local queue fair-sharing usage
+- **workload priority**
+- quota reservation time (more recent first)
+- UID for deterministic ordering
+
+This KEP adds one step:
+
+- If (and only if) two candidates have **equal priority**, compare their
+  preemption cost and prefer **lower** cost first.
+
+### Defaulting and backward compatibility
+
+- If a workload has no preemption cost set, it is treated as **zero** cost.
+  - This preserves existing behavior when the field is unused (all costs equal).
+- The feature is “opt-in by signal”: behavior changes only when an external
+  controller populates the field.
+
+### Risks and Mitigations
+
+- **Risk**: External controller writes unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering; keep priority as primary signal.
+- **Risk**: Multiple writers to the same status field could conflict.
+  - **Mitigation**: Document that a single controller should own the field; use
+    merge patch on status updates; recommend a dedicated field manager.
+
+## Design Details
+
+### API changes
+
+Add a new optional field to `WorkloadStatus`:
+
+- `status.preemptionCost`: `resource.Quantity`
+  - **Semantics**: unitless score, higher means more expensive to preempt.
+  - **Unset**: equivalent to zero.
+
+Go API (illustrative):
+
+```golang
+import "k8s.io/apimachinery/pkg/api/resource"
+
+type WorkloadStatus struct {
+  // ... existing fields ...
+
+  // preemptionCost is an optional, externally populated signal that indicates
+  // how expensive it is to preempt this Workload.
+  //
+  // Higher values mean more expensive to preempt.
+  // If unset, it is treated as 0.
+  //
+  // +optional
+  PreemptionCost *resource.Quantity `json:"preemptionCost,omitempty"`
```

  From issue description:
  > Also I don't think that Kueue itself should include some well defined policies how to calculate the cost of preemption but rather should allow external controllers to populate this information and Kueue should consider it during identifying and sorting the preemption candidates. We could offer a reference implementation of such controller that'd be a good example for folks trying to implement their own accordingly to the policies at their organizations and their business goals. I think a good referential example would be a controller that increases the preemption cost for every preemption that already happened.
  
  As I imagine this reference implementation would have just an increasing counter of preemptions, but I can think of another implementation, when preemption cost is the number of seconds the last preemption took, this way I want to keep this field as flexible as possible for the custom implementations

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T19:13:38Z

- **keps/7990-preemption-cost/kep.yaml:8** by [@mimowo](https://github.com/mimowo) — 2026-02-06T19:13:39Z
```diff
@@ -0,0 +1,24 @@
+title: Preemption Cost Signal for Workload Preemption
+kep-number: 7990
+authors:
+  - TBD
+status: implementable
+creation-date: 2026-01-12
+reviewers:
+  - TBD
```

  add me here

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T19:13:48Z

- **keps/7990-preemption-cost/kep.yaml:4** by [@mimowo](https://github.com/mimowo) — 2026-02-06T19:13:48Z
```diff
@@ -0,0 +1,24 @@
+title: Preemption Cost Signal for Workload Preemption
+kep-number: 7990
+authors:
+  - TBD
```

  put yourself

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T19:13:59Z

- **keps/7990-preemption-cost/kep.yaml:18** by [@mimowo](https://github.com/mimowo) — 2026-02-06T19:13:59Z
```diff
@@ -0,0 +1,24 @@
+title: Preemption Cost Signal for Workload Preemption
+kep-number: 7990
+authors:
+  - TBD
+status: implementable
+creation-date: 2026-01-12
+reviewers:
+  - TBD
+approvers:
+  - TBD
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.16"
```

  bump to 0.17 now

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T19:14:08Z

- **keps/7990-preemption-cost/kep.yaml:22** by [@mimowo](https://github.com/mimowo) — 2026-02-06T19:14:08Z
```diff
@@ -0,0 +1,24 @@
+title: Preemption Cost Signal for Workload Preemption
+kep-number: 7990
+authors:
+  - TBD
+status: implementable
+creation-date: 2026-01-12
+reviewers:
+  - TBD
+approvers:
+  - TBD
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.16"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.16"
```

  bump here too

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T19:21:59Z

- **keps/7990-preemption-cost/README.md:137** by [@mimowo](https://github.com/mimowo) — 2026-02-06T19:21:59Z
```diff
@@ -0,0 +1,222 @@
+# KEP-7990: Preemption cost signal for workload preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Ordering semantics](#ordering-semantics)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional, externally-populated **preemption cost signal** on `Workload`
+and make Kueue consider this signal when ordering preemption candidates.
+
+When multiple preemption candidates have the **same workload priority**, Kueue
+should prefer preempting the candidate with the **lower** preemption cost.
+
+This enables cluster operators to steer preemption toward workloads for which
+preemption is expected to be less disruptive (e.g. based on checkpointing, phase
+of execution, or prior preemptions), without hardcoding cost policies into Kueue.
+
+## Motivation
+
+The “cost” of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate these runtime cost signals.
+
+This KEP adds a generic mechanism to accept an external signal and use it to
+reduce wasted work and improve overall system efficiency.
+
+### Goals
+
+- Add a new optional `Workload` field that can be set by an external controller
+  to express preemption cost.
+- Use preemption cost as a **tie-breaker among equal-priority preemption
+  candidates**.
+- Provide a reference implementation of an external controller that populates
+  the field (simple, policy-neutral example).
+
+### Non-Goals
+
+- Define a universal “correct” cost model inside Kueue.
+- Change preemption eligibility semantics (preemption policies, reclaim rules,
+  priority comparisons) beyond candidate ordering tie-breaks.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional preemption cost signal on `Workload.status` and incorporate it
+into the preemption candidate ordering logic.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt when multiple candidates have the same priority.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware) to compute preemption cost and have Kueue respect it without
+  modifying the scheduler.
+
+### Ordering semantics
+
+For preemption candidate ordering, Kueue currently compares candidates by:
+
+- whether already evicted
+- whether in a different ClusterQueue than the preemptor
+- (optional) local queue fair-sharing usage
+- **workload priority**
+- quota reservation time (more recent first)
+- UID for deterministic ordering
+
+This KEP adds one step:
+
+- If (and only if) two candidates have **equal priority**, compare their
+  preemption cost and prefer **lower** cost first.
+
+### Defaulting and backward compatibility
+
+- If a workload has no preemption cost set, it is treated as **zero** cost.
+  - This preserves existing behavior when the field is unused (all costs equal).
+- The feature is “opt-in by signal”: behavior changes only when an external
+  controller populates the field.
+
+### Risks and Mitigations
+
+- **Risk**: External controller writes unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering; keep priority as primary signal.
+- **Risk**: Multiple writers to the same status field could conflict.
+  - **Mitigation**: Document that a single controller should own the field; use
+    merge patch on status updates; recommend a dedicated field manager.
+
+## Design Details
+
+### API changes
+
+Add a new optional field to `WorkloadStatus`:
+
+- `status.preemptionCost`: `resource.Quantity`
+  - **Semantics**: unitless score, higher means more expensive to preempt.
+  - **Unset**: equivalent to zero.
+
+Go API (illustrative):
+
+```golang
+import "k8s.io/apimachinery/pkg/api/resource"
+
+type WorkloadStatus struct {
+  // ... existing fields ...
+
+  // preemptionCost is an optional, externally populated signal that indicates
+  // how expensive it is to preempt this Workload.
+  //
+  // Higher values mean more expensive to preempt.
+  // If unset, it is treated as 0.
+  //
+  // +optional
+  PreemptionCost *resource.Quantity `json:"preemptionCost,omitempty"`
```

  I think both integer and `resource.Quantity` have some merit. 
  
  "priority" is already integer, then "preemption oost" makes sense as integer to me too. Then we don't need to change much code, and just compute "effective-priority-for-preemption" as "priority+preemptionCost".
  
   `resource.Quantity` OTOH makes the model more flexible giving as fractional values. This might be relevant for the cases like tie-breaking where we want to use fractional values just to break the tie, as mentioned in the comment https://github.com/kubernetes-sigs/kueue/issues/7990#issuecomment-3594319816
  
  I'm indeed leaning for (2.) to make it more flexible as a tie-breaking mechanism

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T19:27:25Z

- **keps/7990-preemption-cost/README.md:69** by [@mimowo](https://github.com/mimowo) — 2026-02-06T19:27:25Z
```diff
@@ -0,0 +1,222 @@
+# KEP-7990: Preemption cost signal for workload preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Ordering semantics](#ordering-semantics)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional, externally-populated **preemption cost signal** on `Workload`
+and make Kueue consider this signal when ordering preemption candidates.
+
+When multiple preemption candidates have the **same workload priority**, Kueue
+should prefer preempting the candidate with the **lower** preemption cost.
+
+This enables cluster operators to steer preemption toward workloads for which
+preemption is expected to be less disruptive (e.g. based on checkpointing, phase
+of execution, or prior preemptions), without hardcoding cost policies into Kueue.
+
+## Motivation
+
+The “cost” of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate these runtime cost signals.
+
+This KEP adds a generic mechanism to accept an external signal and use it to
+reduce wasted work and improve overall system efficiency.
+
+### Goals
+
+- Add a new optional `Workload` field that can be set by an external controller
+  to express preemption cost.
+- Use preemption cost as a **tie-breaker among equal-priority preemption
+  candidates**.
+- Provide a reference implementation of an external controller that populates
+  the field (simple, policy-neutral example).
+
+### Non-Goals
+
+- Define a universal “correct” cost model inside Kueue.
+- Change preemption eligibility semantics (preemption policies, reclaim rules,
+  priority comparisons) beyond candidate ordering tie-breaks.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional preemption cost signal on `Workload.status` and incorporate it
+into the preemption candidate ordering logic.
```

  I think this should be rather an annotation on the Job object, which is mapped onto the Workload object, because we consider Job as the user-facing API.  So, maybe `kueue.x-k8s.io/preemption-cost` annotation, wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T19:33:56Z

- **keps/7990-preemption-cost/README.md:93** by [@mimowo](https://github.com/mimowo) — 2026-02-06T19:33:56Z
```diff
@@ -0,0 +1,222 @@
+# KEP-7990: Preemption cost signal for workload preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Ordering semantics](#ordering-semantics)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional, externally-populated **preemption cost signal** on `Workload`
+and make Kueue consider this signal when ordering preemption candidates.
+
+When multiple preemption candidates have the **same workload priority**, Kueue
+should prefer preempting the candidate with the **lower** preemption cost.
+
+This enables cluster operators to steer preemption toward workloads for which
+preemption is expected to be less disruptive (e.g. based on checkpointing, phase
+of execution, or prior preemptions), without hardcoding cost policies into Kueue.
+
+## Motivation
+
+The “cost” of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate these runtime cost signals.
+
+This KEP adds a generic mechanism to accept an external signal and use it to
+reduce wasted work and improve overall system efficiency.
+
+### Goals
+
+- Add a new optional `Workload` field that can be set by an external controller
+  to express preemption cost.
+- Use preemption cost as a **tie-breaker among equal-priority preemption
+  candidates**.
+- Provide a reference implementation of an external controller that populates
+  the field (simple, policy-neutral example).
+
+### Non-Goals
+
+- Define a universal “correct” cost model inside Kueue.
+- Change preemption eligibility semantics (preemption policies, reclaim rules,
+  priority comparisons) beyond candidate ordering tie-breaks.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional preemption cost signal on `Workload.status` and incorporate it
+into the preemption candidate ordering logic.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt when multiple candidates have the same priority.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware) to compute preemption cost and have Kueue respect it without
+  modifying the scheduler.
+
+### Ordering semantics
+
+For preemption candidate ordering, Kueue currently compares candidates by:
+
+- whether already evicted
+- whether in a different ClusterQueue than the preemptor
+- (optional) local queue fair-sharing usage
+- **workload priority**
+- quota reservation time (more recent first)
+- UID for deterministic ordering
+
+This KEP adds one step:
+
+- If (and only if) two candidates have **equal priority**, compare their
+  preemption cost and prefer **lower** cost first.
```

  The preemption candidates are ordered by "priorities", use "effective priority for preemption" expressed as: `priority + preemptionCost`. 
  
  Because we aren't really introducing a "new step", we are instead introducing the concept of "effective priority for preemption".

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T19:37:20Z

- **keps/7990-preemption-cost/README.md:120** by [@mimowo](https://github.com/mimowo) — 2026-02-06T19:37:20Z
```diff
@@ -0,0 +1,222 @@
+# KEP-7990: Preemption cost signal for workload preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Ordering semantics](#ordering-semantics)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional, externally-populated **preemption cost signal** on `Workload`
+and make Kueue consider this signal when ordering preemption candidates.
+
+When multiple preemption candidates have the **same workload priority**, Kueue
+should prefer preempting the candidate with the **lower** preemption cost.
+
+This enables cluster operators to steer preemption toward workloads for which
+preemption is expected to be less disruptive (e.g. based on checkpointing, phase
+of execution, or prior preemptions), without hardcoding cost policies into Kueue.
+
+## Motivation
+
+The “cost” of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate these runtime cost signals.
+
+This KEP adds a generic mechanism to accept an external signal and use it to
+reduce wasted work and improve overall system efficiency.
+
+### Goals
+
+- Add a new optional `Workload` field that can be set by an external controller
+  to express preemption cost.
+- Use preemption cost as a **tie-breaker among equal-priority preemption
+  candidates**.
+- Provide a reference implementation of an external controller that populates
+  the field (simple, policy-neutral example).
+
+### Non-Goals
+
+- Define a universal “correct” cost model inside Kueue.
+- Change preemption eligibility semantics (preemption policies, reclaim rules,
+  priority comparisons) beyond candidate ordering tie-breaks.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional preemption cost signal on `Workload.status` and incorporate it
+into the preemption candidate ordering logic.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt when multiple candidates have the same priority.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware) to compute preemption cost and have Kueue respect it without
+  modifying the scheduler.
+
+### Ordering semantics
+
+For preemption candidate ordering, Kueue currently compares candidates by:
+
+- whether already evicted
+- whether in a different ClusterQueue than the preemptor
+- (optional) local queue fair-sharing usage
+- **workload priority**
+- quota reservation time (more recent first)
+- UID for deterministic ordering
+
+This KEP adds one step:
+
+- If (and only if) two candidates have **equal priority**, compare their
+  preemption cost and prefer **lower** cost first.
+
+### Defaulting and backward compatibility
+
+- If a workload has no preemption cost set, it is treated as **zero** cost.
+  - This preserves existing behavior when the field is unused (all costs equal).
+- The feature is “opt-in by signal”: behavior changes only when an external
+  controller populates the field.
+
+### Risks and Mitigations
+
+- **Risk**: External controller writes unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering; keep priority as primary signal.
+- **Risk**: Multiple writers to the same status field could conflict.
+  - **Mitigation**: Document that a single controller should own the field; use
+    merge patch on status updates; recommend a dedicated field manager.
+
+## Design Details
+
+### API changes
+
+Add a new optional field to `WorkloadStatus`:
+
+- `status.preemptionCost`: `resource.Quantity`
+  - **Semantics**: unitless score, higher means more expensive to preempt.
+  - **Unset**: equivalent to zero.
```

  We have many unknowns about the feature, so I prefer not to introduce a status field in alpha. Let's build it based on the Job-level annotation: `kueue.x-k8s.io/preemption-cost` which is copied to the Workload object. The value from the workload object is then used by scheduler during preemption.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T19:38:06Z

- **keps/7990-preemption-cost/README.md:88** by [@mimowo](https://github.com/mimowo) — 2026-02-06T19:38:06Z
```diff
@@ -0,0 +1,222 @@
+# KEP-7990: Preemption cost signal for workload preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Ordering semantics](#ordering-semantics)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional, externally-populated **preemption cost signal** on `Workload`
+and make Kueue consider this signal when ordering preemption candidates.
+
+When multiple preemption candidates have the **same workload priority**, Kueue
+should prefer preempting the candidate with the **lower** preemption cost.
+
+This enables cluster operators to steer preemption toward workloads for which
+preemption is expected to be less disruptive (e.g. based on checkpointing, phase
+of execution, or prior preemptions), without hardcoding cost policies into Kueue.
+
+## Motivation
+
+The “cost” of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate these runtime cost signals.
+
+This KEP adds a generic mechanism to accept an external signal and use it to
+reduce wasted work and improve overall system efficiency.
+
+### Goals
+
+- Add a new optional `Workload` field that can be set by an external controller
+  to express preemption cost.
+- Use preemption cost as a **tie-breaker among equal-priority preemption
+  candidates**.
+- Provide a reference implementation of an external controller that populates
+  the field (simple, policy-neutral example).
+
+### Non-Goals
+
+- Define a universal “correct” cost model inside Kueue.
+- Change preemption eligibility semantics (preemption policies, reclaim rules,
+  priority comparisons) beyond candidate ordering tie-breaks.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional preemption cost signal on `Workload.status` and incorporate it
+into the preemption candidate ordering logic.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt when multiple candidates have the same priority.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware) to compute preemption cost and have Kueue respect it without
+  modifying the scheduler.
+
+### Ordering semantics
+
+For preemption candidate ordering, Kueue currently compares candidates by:
+
+- whether already evicted
+- whether in a different ClusterQueue than the preemptor
+- (optional) local queue fair-sharing usage
+- **workload priority**
+- quota reservation time (more recent first)
+- UID for deterministic ordering
```

  I would just drop this section and replace with the sentence as proposed in https://github.com/kubernetes-sigs/kueue/pull/8551/changes#r2775702179

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-09T10:35:25Z

- **keps/7990-preemption-cost/README.md:69** by [@mimowo](https://github.com/mimowo) — 2026-02-09T10:35:25Z
```diff
@@ -0,0 +1,222 @@
+# KEP-7990: Preemption cost signal for workload preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Ordering semantics](#ordering-semantics)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional, externally-populated **preemption cost signal** on `Workload`
+and make Kueue consider this signal when ordering preemption candidates.
+
+When multiple preemption candidates have the **same workload priority**, Kueue
+should prefer preempting the candidate with the **lower** preemption cost.
+
+This enables cluster operators to steer preemption toward workloads for which
+preemption is expected to be less disruptive (e.g. based on checkpointing, phase
+of execution, or prior preemptions), without hardcoding cost policies into Kueue.
+
+## Motivation
+
+The “cost” of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate these runtime cost signals.
+
+This KEP adds a generic mechanism to accept an external signal and use it to
+reduce wasted work and improve overall system efficiency.
+
+### Goals
+
+- Add a new optional `Workload` field that can be set by an external controller
+  to express preemption cost.
+- Use preemption cost as a **tie-breaker among equal-priority preemption
+  candidates**.
+- Provide a reference implementation of an external controller that populates
+  the field (simple, policy-neutral example).
+
+### Non-Goals
+
+- Define a universal “correct” cost model inside Kueue.
+- Change preemption eligibility semantics (preemption policies, reclaim rules,
+  priority comparisons) beyond candidate ordering tie-breaks.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional preemption cost signal on `Workload.status` and incorporate it
+into the preemption candidate ordering logic.
```

  Actually, we may park it only at the Workload level per this comment: https://github.com/kubernetes-sigs/kueue/issues/7990#issuecomment-3604784336. At least for Alpha, and add a graduation point for beta to re-evaluate the user-facing API.

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-02-11T11:47:27Z

- **keps/7990-preemption-cost/README.md:69** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-11T11:47:27Z
```diff
@@ -0,0 +1,222 @@
+# KEP-7990: Preemption cost signal for workload preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Ordering semantics](#ordering-semantics)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional, externally-populated **preemption cost signal** on `Workload`
+and make Kueue consider this signal when ordering preemption candidates.
+
+When multiple preemption candidates have the **same workload priority**, Kueue
+should prefer preempting the candidate with the **lower** preemption cost.
+
+This enables cluster operators to steer preemption toward workloads for which
+preemption is expected to be less disruptive (e.g. based on checkpointing, phase
+of execution, or prior preemptions), without hardcoding cost policies into Kueue.
+
+## Motivation
+
+The “cost” of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate these runtime cost signals.
+
+This KEP adds a generic mechanism to accept an external signal and use it to
+reduce wasted work and improve overall system efficiency.
+
+### Goals
+
+- Add a new optional `Workload` field that can be set by an external controller
+  to express preemption cost.
+- Use preemption cost as a **tie-breaker among equal-priority preemption
+  candidates**.
+- Provide a reference implementation of an external controller that populates
+  the field (simple, policy-neutral example).
+
+### Non-Goals
+
+- Define a universal “correct” cost model inside Kueue.
+- Change preemption eligibility semantics (preemption policies, reclaim rules,
+  priority comparisons) beyond candidate ordering tie-breaks.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional preemption cost signal on `Workload.status` and incorporate it
+into the preemption candidate ordering logic.
```

  Yes, I agree that for alpha it's better to have annotation

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-02-11T12:05:21Z

- **keps/7990-preemption-cost/README.md:137** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-11T12:05:21Z
```diff
@@ -0,0 +1,222 @@
+# KEP-7990: Preemption cost signal for workload preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Ordering semantics](#ordering-semantics)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional, externally-populated **preemption cost signal** on `Workload`
+and make Kueue consider this signal when ordering preemption candidates.
+
+When multiple preemption candidates have the **same workload priority**, Kueue
+should prefer preempting the candidate with the **lower** preemption cost.
+
+This enables cluster operators to steer preemption toward workloads for which
+preemption is expected to be less disruptive (e.g. based on checkpointing, phase
+of execution, or prior preemptions), without hardcoding cost policies into Kueue.
+
+## Motivation
+
+The “cost” of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate these runtime cost signals.
+
+This KEP adds a generic mechanism to accept an external signal and use it to
+reduce wasted work and improve overall system efficiency.
+
+### Goals
+
+- Add a new optional `Workload` field that can be set by an external controller
+  to express preemption cost.
+- Use preemption cost as a **tie-breaker among equal-priority preemption
+  candidates**.
+- Provide a reference implementation of an external controller that populates
+  the field (simple, policy-neutral example).
+
+### Non-Goals
+
+- Define a universal “correct” cost model inside Kueue.
+- Change preemption eligibility semantics (preemption policies, reclaim rules,
+  priority comparisons) beyond candidate ordering tie-breaks.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional preemption cost signal on `Workload.status` and incorporate it
+into the preemption candidate ordering logic.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt when multiple candidates have the same priority.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware) to compute preemption cost and have Kueue respect it without
+  modifying the scheduler.
+
+### Ordering semantics
+
+For preemption candidate ordering, Kueue currently compares candidates by:
+
+- whether already evicted
+- whether in a different ClusterQueue than the preemptor
+- (optional) local queue fair-sharing usage
+- **workload priority**
+- quota reservation time (more recent first)
+- UID for deterministic ordering
+
+This KEP adds one step:
+
+- If (and only if) two candidates have **equal priority**, compare their
+  preemption cost and prefer **lower** cost first.
+
+### Defaulting and backward compatibility
+
+- If a workload has no preemption cost set, it is treated as **zero** cost.
+  - This preserves existing behavior when the field is unused (all costs equal).
+- The feature is “opt-in by signal”: behavior changes only when an external
+  controller populates the field.
+
+### Risks and Mitigations
+
+- **Risk**: External controller writes unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering; keep priority as primary signal.
+- **Risk**: Multiple writers to the same status field could conflict.
+  - **Mitigation**: Document that a single controller should own the field; use
+    merge patch on status updates; recommend a dedicated field manager.
+
+## Design Details
+
+### API changes
+
+Add a new optional field to `WorkloadStatus`:
+
+- `status.preemptionCost`: `resource.Quantity`
+  - **Semantics**: unitless score, higher means more expensive to preempt.
+  - **Unset**: equivalent to zero.
+
+Go API (illustrative):
+
+```golang
+import "k8s.io/apimachinery/pkg/api/resource"
+
+type WorkloadStatus struct {
+  // ... existing fields ...
+
+  // preemptionCost is an optional, externally populated signal that indicates
+  // how expensive it is to preempt this Workload.
+  //
+  // Higher values mean more expensive to preempt.
+  // If unset, it is treated as 0.
+  //
+  // +optional
+  PreemptionCost *resource.Quantity `json:"preemptionCost,omitempty"`
```

  Since it's an annotation, I would stick to <int> value for now

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-18T14:43:22Z

- **keps/7990-preemption-cost/kep.yaml:1** by [@mimowo](https://github.com/mimowo) — 2026-02-18T14:43:22Z
```diff
@@ -0,0 +1,26 @@
+title: Preemption Cost Signal for Workload Preemption
```

  ```suggestion
  title: Support Workload Priority Boost By External Controllers
  ```
  let's turn the KEP to match the recent decisions about using `kueue.x-k8s.io/priority-boost` annotation, rather than the preemption cost.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-18T14:44:40Z

- **keps/7990-preemption-cost/README.md:59** by [@mimowo](https://github.com/mimowo) — 2026-02-18T14:44:40Z
```diff
@@ -0,0 +1,232 @@
+# KEP-7990: Preemption cost signal for workload preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Ordering semantics](#ordering-semantics)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional, user-provided preemption cost signal via
+`Job` annotation `kueue.x-k8s.io/preemption-cost`, copied onto `Workload`,
+and make Kueue consider this signal when ordering preemption candidates.
+
+Preemption ordering uses an **effective priority for preemption**:
+`workloadPriority - preemptionCost`.
+
+This enables cluster operators to steer preemption toward workloads for which
+preemption is expected to be less disruptive (e.g. based on checkpointing, phase
+of execution, or prior preemptions), without hardcoding cost policies into Kueue.
+
+## Motivation
+
+The “cost” of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate these runtime cost signals.
+
+This KEP adds a generic mechanism to accept a user-facing signal and use it to
+reduce wasted work and improve overall system efficiency, with an optional
+reference external controller that can populate the signal automatically.
+
+### Goals
+
+- Add a new optional `Job` annotation (`kueue.x-k8s.io/preemption-cost`) that
+  is copied to `Workload` and used to express preemption cost.
+- Use preemption cost in an **effective priority for preemption**
+  (`workloadPriority - preemptionCost`) for candidate ordering.
+- Provide a reference external controller that automatically computes and sets
+  `kueue.x-k8s.io/preemption-cost` on `Job`.
```

  ```suggestion
  - Add a new optional `Workload` annotation which allows external controllers to boost priority for some workloads.
  ```

### Review by [@sanposhiho](https://github.com/sanposhiho) (commented) — 2026-02-20T06:54:30Z

- **keps/7990-preemption-cost/README.md:1** by [@sanposhiho](https://github.com/sanposhiho) — 2026-02-20T06:53:51Z
  One concern is that it will be harder for platform owners to explain why jobA is preempted by jobB because the priority could be dynamically/frequently changed.
  We need to make sure, at the very least, leaving logs for what boost numbers were when the preemption happened, and ideally making it visible on somewhere on Kubernetes API (events?)

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-02-20T16:53:44Z

- **keps/7990-preemption-cost/README.md:1** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-20T16:53:44Z
  Added in the implementation, not sure if I should update the kep for this

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-02-20T16:55:19Z

- **keps/7990-preemption-cost/README.md:1** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-20T16:55:19Z
  https://github.com/kubernetes-sigs/kueue/pull/9120/changes/59df8ad1547a6bf08b880a3c5b18695915aa27e0

### Review by [@sanposhiho](https://github.com/sanposhiho) (commented) — 2026-02-22T03:07:34Z

- **keps/7990-preemption-cost/README.md:1** by [@sanposhiho](https://github.com/sanposhiho) — 2026-02-22T03:07:33Z
  great

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-23T07:29:27Z

- **keps/7990-preemption-cost/README.md:37** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-23T06:56:24Z
```diff
@@ -0,0 +1,326 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used for ordering within a
+ClusterQueue (both scheduling and preemption candidate ordering).
```

  Can we clarify which one is the intended behavior? If boost should cross boundaries (which I believe is the consensus from the issue), we should say something like `wherever workload priority is currently used within a ClusterQueue, including preemption eligibility checks` and drop the exclusion on line 206

- **keps/7990-preemption-cost/README.md:142** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-23T07:04:53Z
```diff
@@ -0,0 +1,326 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used for ordering within a
+ClusterQueue (both scheduling and preemption candidate ordering).
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - **Safeguard**: As an additional safety measure, Kueue may skip recently
```

  Is this in scope for Alpha or not? As-is it reads like we're acknowledging the risk without committing to a solution. I'd prefer one of:
    - For Alpha, Kueue does not implement admission cooldown. This will be evaluated for Beta based on real-world feedback (clear non-goal).
    - An actual design with a duration/mechanism (clear goal)
  
  The current wording makes it hard to know what to expect from the implementation.

- **keps/7990-preemption-cost/README.md:159** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-23T07:07:07Z
```diff
@@ -0,0 +1,326 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used for ordering within a
+ClusterQueue (both scheduling and preemption candidate ordering).
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - **Safeguard**: As an additional safety measure, Kueue may skip recently
+    admitted workloads when selecting preemption candidates.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero and emit a warning
```

  This silently treating invalid values as zero feels risky. If an external controller has a bug and starts writing garbage into the annotation, workloads lose their boost protection with no visible signal. Could we at least emit a K8s Event on the Workload in addition to the log?

- **keps/7990-preemption-cost/README.md:205** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-23T07:11:27Z
```diff
@@ -0,0 +1,326 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used for ordering within a
+ClusterQueue (both scheduling and preemption candidate ordering).
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - **Safeguard**: As an additional safety measure, Kueue may skip recently
+    admitted workloads when selecting preemption candidates.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero and emit a warning
+    event/log.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero and a warning is logged.
+
+The annotation is set directly on the `Workload` object by an external
+controller (not propagated from Jobs).
+
+YAML example (`Workload`):
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: example-workload
+  namespace: default
+  annotations:
+    # Positive values increase effective priority. Unset means 0.
+    kueue.x-k8s.io/priority-boost: "100"
+spec:
+  priorityClassName: medium
+  # ... rest of spec
+```
+
+### Scheduler changes
+
+Update workload ordering and preemption candidate ordering to use effective
+priority:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+This affects ordering within a ClusterQueue for both scheduling and preemption
```

  This contradicts the starvation prevention use case from the issue.
  If eligibility still uses raw priority, a low job with a massive boost is still preemptable by any medium job under LowerPriority policy,   which is exactly the cost-only approach rejected in the Alternatives section.                                                                 
                                                                                                                                                   Should we clarify that effective priority is used for preemption policy evaluation too?

- **keps/7990-preemption-cost/README.md:261** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-23T07:13:41Z
```diff
@@ -0,0 +1,326 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used for ordering within a
+ClusterQueue (both scheduling and preemption candidate ordering).
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - **Safeguard**: As an additional safety measure, Kueue may skip recently
+    admitted workloads when selecting preemption candidates.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero and emit a warning
+    event/log.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero and a warning is logged.
+
+The annotation is set directly on the `Workload` object by an external
+controller (not propagated from Jobs).
+
+YAML example (`Workload`):
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: example-workload
+  namespace: default
+  annotations:
+    # Positive values increase effective priority. Unset means 0.
+    kueue.x-k8s.io/priority-boost: "100"
+spec:
+  priorityClassName: medium
+  # ... rest of spec
+```
+
+### Scheduler changes
+
+Update workload ordering and preemption candidate ordering to use effective
+priority:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+This affects ordering within a ClusterQueue for both scheduling and preemption
+candidate selection. It does not change DRS calculations or preemption
+eligibility rules (preemption policies, reclaim rules).
+
+### Reference controller
+
+Provide a reference external controller that:
+
+- Watches `Workload` objects managed by Kueue.
+- Computes priority boost using a simple, policy-neutral heuristic.
+- Writes/updates `metadata.annotations["kueue.x-k8s.io/priority-boost"]` on
+  the `Workload`.
+
+Reference algorithm (example):
+
+- Start with base boost `0`.
+- Add a bounded increment for each prior eviction with reason `Preempted`
+  observed in the Workload's `schedulingStats`. To avoid preemption flapping,
+  use a threshold-based approach: increase the boost only after every N
+  preemptions (e.g. every 2nd preemption), so that in the worst case a single
+  extra preemption can occur but the cycle does not recur.
+- Optionally add a bounded phase-based increment (for example, running
+  workloads can receive a higher boost than initializing workloads).
+- The controller can combine multiple factors (preemption count, job phase,
+  checkpoint status, crash count) into a single boost value using configurable
+  weights.
+
+The reference implementation is intentionally simple and demonstrates how
+platform teams can plug in richer, organization-specific logic.
+
+### Observability
+
+No new scheduler metrics are required initially.
```

  when a preemption happens, we should log the effective priority (base + boost) of both the preemptor and the victim. This is going to be hard to debug otherwise, especially when boost values change between scheduling cycles.

- **keps/7990-preemption-cost/README.md:310** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-23T07:16:08Z
```diff
@@ -0,0 +1,326 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used for ordering within a
+ClusterQueue (both scheduling and preemption candidate ordering).
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - **Safeguard**: As an additional safety measure, Kueue may skip recently
+    admitted workloads when selecting preemption candidates.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero and emit a warning
+    event/log.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero and a warning is logged.
+
+The annotation is set directly on the `Workload` object by an external
+controller (not propagated from Jobs).
+
+YAML example (`Workload`):
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: example-workload
+  namespace: default
+  annotations:
+    # Positive values increase effective priority. Unset means 0.
+    kueue.x-k8s.io/priority-boost: "100"
+spec:
+  priorityClassName: medium
+  # ... rest of spec
+```
+
+### Scheduler changes
+
+Update workload ordering and preemption candidate ordering to use effective
+priority:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+This affects ordering within a ClusterQueue for both scheduling and preemption
+candidate selection. It does not change DRS calculations or preemption
+eligibility rules (preemption policies, reclaim rules).
+
+### Reference controller
+
+Provide a reference external controller that:
+
+- Watches `Workload` objects managed by Kueue.
+- Computes priority boost using a simple, policy-neutral heuristic.
+- Writes/updates `metadata.annotations["kueue.x-k8s.io/priority-boost"]` on
+  the `Workload`.
+
+Reference algorithm (example):
+
+- Start with base boost `0`.
+- Add a bounded increment for each prior eviction with reason `Preempted`
+  observed in the Workload's `schedulingStats`. To avoid preemption flapping,
+  use a threshold-based approach: increase the boost only after every N
+  preemptions (e.g. every 2nd preemption), so that in the worst case a single
+  extra preemption can occur but the cycle does not recur.
+- Optionally add a bounded phase-based increment (for example, running
+  workloads can receive a higher boost than initializing workloads).
+- The controller can combine multiple factors (preemption count, job phase,
+  checkpoint status, crash count) into a single boost value using configurable
+  weights.
+
+The reference implementation is intentionally simple and demonstrates how
+platform teams can plug in richer, organization-specific logic.
+
+### Observability
+
+No new scheduler metrics are required initially.
+
+- Kueue should log invalid `kueue.x-k8s.io/priority-boost` annotation values
+  at a low verbosity level.
+- The reference controller should log annotation updates and expose
+  reconciliation errors.
+
+### Security, Privacy, and RBAC
+
+Scheduler side: no additional RBAC is required beyond existing permissions for
+reading `Workload` objects.
+
+Reference controller requires permissions to:
+
+- `get`, `list`, `watch` `workloads.kueue.x-k8s.io`
+- `patch` `workloads.kueue.x-k8s.io`
+
+Cluster administrators should ensure that only trusted controllers have `patch`
+access to `Workload` objects to prevent abuse of the `priority-boost`
+annotation.
+
+### Test Plan
+
+#### Unit tests
+
+- Extend existing unit tests for workload ordering and preemption candidate
+  ordering to validate:
+  - effective priority uses `priority + priorityBoost`
+  - missing or invalid annotation values are treated as zero
+  - boost can cross priority class boundaries
+- Add unit tests for the reference controller:
+  - computes expected boost for representative Workload states
+  - writes `kueue.x-k8s.io/priority-boost` annotation idempotently
+  - handles update conflicts/retries
+
+#### Integration/E2E tests
+
+- Run a small integration scenario with two same-priority Workloads and
+  different `kueue.x-k8s.io/priority-boost` values, and confirm preemption
+  selection order.
+- Run a scenario where boost crosses priority class boundaries and confirm that
```

  Can we add a test case specifically for preemption eligibility with boost? Something like: `With LowerPriority policy, a low-priority workload with boost giving it effective priority above medium cannot be preempted by medium-priority workloads.`

- **keps/7990-preemption-cost/README.md:318** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-23T07:18:28Z
```diff
@@ -0,0 +1,326 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used for ordering within a
+ClusterQueue (both scheduling and preemption candidate ordering).
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - **Safeguard**: As an additional safety measure, Kueue may skip recently
+    admitted workloads when selecting preemption candidates.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero and emit a warning
+    event/log.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero and a warning is logged.
+
+The annotation is set directly on the `Workload` object by an external
+controller (not propagated from Jobs).
+
+YAML example (`Workload`):
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: example-workload
+  namespace: default
+  annotations:
+    # Positive values increase effective priority. Unset means 0.
+    kueue.x-k8s.io/priority-boost: "100"
+spec:
+  priorityClassName: medium
+  # ... rest of spec
+```
+
+### Scheduler changes
+
+Update workload ordering and preemption candidate ordering to use effective
+priority:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+This affects ordering within a ClusterQueue for both scheduling and preemption
+candidate selection. It does not change DRS calculations or preemption
+eligibility rules (preemption policies, reclaim rules).
+
+### Reference controller
+
+Provide a reference external controller that:
+
+- Watches `Workload` objects managed by Kueue.
+- Computes priority boost using a simple, policy-neutral heuristic.
+- Writes/updates `metadata.annotations["kueue.x-k8s.io/priority-boost"]` on
+  the `Workload`.
+
+Reference algorithm (example):
+
+- Start with base boost `0`.
+- Add a bounded increment for each prior eviction with reason `Preempted`
+  observed in the Workload's `schedulingStats`. To avoid preemption flapping,
+  use a threshold-based approach: increase the boost only after every N
+  preemptions (e.g. every 2nd preemption), so that in the worst case a single
+  extra preemption can occur but the cycle does not recur.
+- Optionally add a bounded phase-based increment (for example, running
+  workloads can receive a higher boost than initializing workloads).
+- The controller can combine multiple factors (preemption count, job phase,
+  checkpoint status, crash count) into a single boost value using configurable
+  weights.
+
+The reference implementation is intentionally simple and demonstrates how
+platform teams can plug in richer, organization-specific logic.
+
+### Observability
+
+No new scheduler metrics are required initially.
+
+- Kueue should log invalid `kueue.x-k8s.io/priority-boost` annotation values
+  at a low verbosity level.
+- The reference controller should log annotation updates and expose
+  reconciliation errors.
+
+### Security, Privacy, and RBAC
+
+Scheduler side: no additional RBAC is required beyond existing permissions for
+reading `Workload` objects.
+
+Reference controller requires permissions to:
+
+- `get`, `list`, `watch` `workloads.kueue.x-k8s.io`
+- `patch` `workloads.kueue.x-k8s.io`
+
+Cluster administrators should ensure that only trusted controllers have `patch`
+access to `Workload` objects to prevent abuse of the `priority-boost`
+annotation.
+
+### Test Plan
+
+#### Unit tests
+
+- Extend existing unit tests for workload ordering and preemption candidate
+  ordering to validate:
+  - effective priority uses `priority + priorityBoost`
+  - missing or invalid annotation values are treated as zero
+  - boost can cross priority class boundaries
+- Add unit tests for the reference controller:
+  - computes expected boost for representative Workload states
+  - writes `kueue.x-k8s.io/priority-boost` annotation idempotently
+  - handles update conflicts/retries
+
+#### Integration/E2E tests
+
+- Run a small integration scenario with two same-priority Workloads and
+  different `kueue.x-k8s.io/priority-boost` values, and confirm preemption
+  selection order.
+- Run a scenario where boost crosses priority class boundaries and confirm that
+  the boosted low-priority workload is ordered above a medium-priority workload.
+- Add integration coverage where the reference controller updates Workload
+  annotations and preemption order changes accordingly.
+
+### Graduation Criteria
```

  I'd add to the following things to the graduation criteria:
  
    - Interaction with LowerOrNewerEqualPriority`: if two workloads have different base priorities but the same effective priority after boost, are they equal priority for this policy? This should be validated.
   
    - Whether a `maxPriorityBoost` cap on ClusterQueue is needed as a safety valve against runaway controllers.

- **keps/7990-preemption-cost/kep.yaml:1** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-23T07:19:23Z
```diff
@@ -0,0 +1,26 @@
+title: Support Workload Priority Boost By External Controllers
```

  I think we need to add a feature gate here.

- **keps/7990-preemption-cost/README.md:245** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-23T07:21:32Z
```diff
@@ -0,0 +1,326 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used for ordering within a
+ClusterQueue (both scheduling and preemption candidate ordering).
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - **Safeguard**: As an additional safety measure, Kueue may skip recently
+    admitted workloads when selecting preemption candidates.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero and emit a warning
+    event/log.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero and a warning is logged.
+
+The annotation is set directly on the `Workload` object by an external
+controller (not propagated from Jobs).
+
+YAML example (`Workload`):
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: example-workload
+  namespace: default
+  annotations:
+    # Positive values increase effective priority. Unset means 0.
+    kueue.x-k8s.io/priority-boost: "100"
+spec:
+  priorityClassName: medium
+  # ... rest of spec
+```
+
+### Scheduler changes
+
+Update workload ordering and preemption candidate ordering to use effective
+priority:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+This affects ordering within a ClusterQueue for both scheduling and preemption
+candidate selection. It does not change DRS calculations or preemption
+eligibility rules (preemption policies, reclaim rules).
+
+### Reference controller
+
+Provide a reference external controller that:
+
+- Watches `Workload` objects managed by Kueue.
+- Computes priority boost using a simple, policy-neutral heuristic.
+- Writes/updates `metadata.annotations["kueue.x-k8s.io/priority-boost"]` on
+  the `Workload`.
+
+Reference algorithm (example):
+
+- Start with base boost `0`.
+- Add a bounded increment for each prior eviction with reason `Preempted`
+  observed in the Workload's `schedulingStats`. To avoid preemption flapping,
```

  The `every Nth preemption` threshold approach bounds the frequency of flapping but doesn't eliminate it. Might be worth noting this limitation explicitly.

- **keps/7990-preemption-cost/kep.yaml:1** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-23T07:25:42Z
```diff
@@ -0,0 +1,26 @@
+title: Support Workload Priority Boost By External Controllers
```

  nit: consider adding see-also: - "/keps/1714-fair-sharing" since the issue discussion explicitly scoped out FairSharing interaction and that context is useful for future readers.

- **keps/7990-preemption-cost/README.md:182** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-23T07:28:55Z
```diff
@@ -0,0 +1,326 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used for ordering within a
+ClusterQueue (both scheduling and preemption candidate ordering).
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - **Safeguard**: As an additional safety measure, Kueue may skip recently
+    admitted workloads when selecting preemption candidates.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero and emit a warning
+    event/log.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero and a warning is logged.
+
+The annotation is set directly on the `Workload` object by an external
+controller (not propagated from Jobs).
+
+YAML example (`Workload`):
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
```

  nit: `kueue.x-k8s.io/v1beta2`

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-02-24T20:51:20Z

- **keps/7990-preemption-cost/README.md:182** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-24T20:51:20Z
```diff
@@ -0,0 +1,326 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used for ordering within a
+ClusterQueue (both scheduling and preemption candidate ordering).
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - **Safeguard**: As an additional safety measure, Kueue may skip recently
+    admitted workloads when selecting preemption candidates.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero and emit a warning
+    event/log.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero and a warning is logged.
+
+The annotation is set directly on the `Workload` object by an external
+controller (not propagated from Jobs).
+
+YAML example (`Workload`):
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
```

  https://github.com/kubernetes-sigs/kueue/pull/8551/commits/d0ff8ca3382843f6dca099d9d3e6b55548d0dbee

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-02-24T20:51:24Z

- **keps/7990-preemption-cost/README.md:245** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-24T20:51:23Z
```diff
@@ -0,0 +1,326 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used for ordering within a
+ClusterQueue (both scheduling and preemption candidate ordering).
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - **Safeguard**: As an additional safety measure, Kueue may skip recently
+    admitted workloads when selecting preemption candidates.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero and emit a warning
+    event/log.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero and a warning is logged.
+
+The annotation is set directly on the `Workload` object by an external
+controller (not propagated from Jobs).
+
+YAML example (`Workload`):
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: example-workload
+  namespace: default
+  annotations:
+    # Positive values increase effective priority. Unset means 0.
+    kueue.x-k8s.io/priority-boost: "100"
+spec:
+  priorityClassName: medium
+  # ... rest of spec
+```
+
+### Scheduler changes
+
+Update workload ordering and preemption candidate ordering to use effective
+priority:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+This affects ordering within a ClusterQueue for both scheduling and preemption
+candidate selection. It does not change DRS calculations or preemption
+eligibility rules (preemption policies, reclaim rules).
+
+### Reference controller
+
+Provide a reference external controller that:
+
+- Watches `Workload` objects managed by Kueue.
+- Computes priority boost using a simple, policy-neutral heuristic.
+- Writes/updates `metadata.annotations["kueue.x-k8s.io/priority-boost"]` on
+  the `Workload`.
+
+Reference algorithm (example):
+
+- Start with base boost `0`.
+- Add a bounded increment for each prior eviction with reason `Preempted`
+  observed in the Workload's `schedulingStats`. To avoid preemption flapping,
```

  https://github.com/kubernetes-sigs/kueue/pull/8551/commits/d0ff8ca3382843f6dca099d9d3e6b55548d0dbee

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-02-24T20:51:36Z

- **keps/7990-preemption-cost/kep.yaml:1** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-24T20:51:36Z
```diff
@@ -0,0 +1,26 @@
+title: Support Workload Priority Boost By External Controllers
```

  https://github.com/kubernetes-sigs/kueue/pull/8551/commits/d0ff8ca3382843f6dca099d9d3e6b55548d0dbee

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-02-24T20:51:43Z

- **keps/7990-preemption-cost/README.md:318** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-24T20:51:42Z
```diff
@@ -0,0 +1,326 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used for ordering within a
+ClusterQueue (both scheduling and preemption candidate ordering).
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - **Safeguard**: As an additional safety measure, Kueue may skip recently
+    admitted workloads when selecting preemption candidates.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero and emit a warning
+    event/log.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero and a warning is logged.
+
+The annotation is set directly on the `Workload` object by an external
+controller (not propagated from Jobs).
+
+YAML example (`Workload`):
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: example-workload
+  namespace: default
+  annotations:
+    # Positive values increase effective priority. Unset means 0.
+    kueue.x-k8s.io/priority-boost: "100"
+spec:
+  priorityClassName: medium
+  # ... rest of spec
+```
+
+### Scheduler changes
+
+Update workload ordering and preemption candidate ordering to use effective
+priority:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+This affects ordering within a ClusterQueue for both scheduling and preemption
+candidate selection. It does not change DRS calculations or preemption
+eligibility rules (preemption policies, reclaim rules).
+
+### Reference controller
+
+Provide a reference external controller that:
+
+- Watches `Workload` objects managed by Kueue.
+- Computes priority boost using a simple, policy-neutral heuristic.
+- Writes/updates `metadata.annotations["kueue.x-k8s.io/priority-boost"]` on
+  the `Workload`.
+
+Reference algorithm (example):
+
+- Start with base boost `0`.
+- Add a bounded increment for each prior eviction with reason `Preempted`
+  observed in the Workload's `schedulingStats`. To avoid preemption flapping,
+  use a threshold-based approach: increase the boost only after every N
+  preemptions (e.g. every 2nd preemption), so that in the worst case a single
+  extra preemption can occur but the cycle does not recur.
+- Optionally add a bounded phase-based increment (for example, running
+  workloads can receive a higher boost than initializing workloads).
+- The controller can combine multiple factors (preemption count, job phase,
+  checkpoint status, crash count) into a single boost value using configurable
+  weights.
+
+The reference implementation is intentionally simple and demonstrates how
+platform teams can plug in richer, organization-specific logic.
+
+### Observability
+
+No new scheduler metrics are required initially.
+
+- Kueue should log invalid `kueue.x-k8s.io/priority-boost` annotation values
+  at a low verbosity level.
+- The reference controller should log annotation updates and expose
+  reconciliation errors.
+
+### Security, Privacy, and RBAC
+
+Scheduler side: no additional RBAC is required beyond existing permissions for
+reading `Workload` objects.
+
+Reference controller requires permissions to:
+
+- `get`, `list`, `watch` `workloads.kueue.x-k8s.io`
+- `patch` `workloads.kueue.x-k8s.io`
+
+Cluster administrators should ensure that only trusted controllers have `patch`
+access to `Workload` objects to prevent abuse of the `priority-boost`
+annotation.
+
+### Test Plan
+
+#### Unit tests
+
+- Extend existing unit tests for workload ordering and preemption candidate
+  ordering to validate:
+  - effective priority uses `priority + priorityBoost`
+  - missing or invalid annotation values are treated as zero
+  - boost can cross priority class boundaries
+- Add unit tests for the reference controller:
+  - computes expected boost for representative Workload states
+  - writes `kueue.x-k8s.io/priority-boost` annotation idempotently
+  - handles update conflicts/retries
+
+#### Integration/E2E tests
+
+- Run a small integration scenario with two same-priority Workloads and
+  different `kueue.x-k8s.io/priority-boost` values, and confirm preemption
+  selection order.
+- Run a scenario where boost crosses priority class boundaries and confirm that
+  the boosted low-priority workload is ordered above a medium-priority workload.
+- Add integration coverage where the reference controller updates Workload
+  annotations and preemption order changes accordingly.
+
+### Graduation Criteria
```

  https://github.com/kubernetes-sigs/kueue/pull/8551/commits/d0ff8ca3382843f6dca099d9d3e6b55548d0dbee

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-02-24T20:51:46Z

- **keps/7990-preemption-cost/README.md:310** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-24T20:51:46Z
```diff
@@ -0,0 +1,326 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used for ordering within a
+ClusterQueue (both scheduling and preemption candidate ordering).
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - **Safeguard**: As an additional safety measure, Kueue may skip recently
+    admitted workloads when selecting preemption candidates.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero and emit a warning
+    event/log.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero and a warning is logged.
+
+The annotation is set directly on the `Workload` object by an external
+controller (not propagated from Jobs).
+
+YAML example (`Workload`):
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: example-workload
+  namespace: default
+  annotations:
+    # Positive values increase effective priority. Unset means 0.
+    kueue.x-k8s.io/priority-boost: "100"
+spec:
+  priorityClassName: medium
+  # ... rest of spec
+```
+
+### Scheduler changes
+
+Update workload ordering and preemption candidate ordering to use effective
+priority:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+This affects ordering within a ClusterQueue for both scheduling and preemption
+candidate selection. It does not change DRS calculations or preemption
+eligibility rules (preemption policies, reclaim rules).
+
+### Reference controller
+
+Provide a reference external controller that:
+
+- Watches `Workload` objects managed by Kueue.
+- Computes priority boost using a simple, policy-neutral heuristic.
+- Writes/updates `metadata.annotations["kueue.x-k8s.io/priority-boost"]` on
+  the `Workload`.
+
+Reference algorithm (example):
+
+- Start with base boost `0`.
+- Add a bounded increment for each prior eviction with reason `Preempted`
+  observed in the Workload's `schedulingStats`. To avoid preemption flapping,
+  use a threshold-based approach: increase the boost only after every N
+  preemptions (e.g. every 2nd preemption), so that in the worst case a single
+  extra preemption can occur but the cycle does not recur.
+- Optionally add a bounded phase-based increment (for example, running
+  workloads can receive a higher boost than initializing workloads).
+- The controller can combine multiple factors (preemption count, job phase,
+  checkpoint status, crash count) into a single boost value using configurable
+  weights.
+
+The reference implementation is intentionally simple and demonstrates how
+platform teams can plug in richer, organization-specific logic.
+
+### Observability
+
+No new scheduler metrics are required initially.
+
+- Kueue should log invalid `kueue.x-k8s.io/priority-boost` annotation values
+  at a low verbosity level.
+- The reference controller should log annotation updates and expose
+  reconciliation errors.
+
+### Security, Privacy, and RBAC
+
+Scheduler side: no additional RBAC is required beyond existing permissions for
+reading `Workload` objects.
+
+Reference controller requires permissions to:
+
+- `get`, `list`, `watch` `workloads.kueue.x-k8s.io`
+- `patch` `workloads.kueue.x-k8s.io`
+
+Cluster administrators should ensure that only trusted controllers have `patch`
+access to `Workload` objects to prevent abuse of the `priority-boost`
+annotation.
+
+### Test Plan
+
+#### Unit tests
+
+- Extend existing unit tests for workload ordering and preemption candidate
+  ordering to validate:
+  - effective priority uses `priority + priorityBoost`
+  - missing or invalid annotation values are treated as zero
+  - boost can cross priority class boundaries
+- Add unit tests for the reference controller:
+  - computes expected boost for representative Workload states
+  - writes `kueue.x-k8s.io/priority-boost` annotation idempotently
+  - handles update conflicts/retries
+
+#### Integration/E2E tests
+
+- Run a small integration scenario with two same-priority Workloads and
+  different `kueue.x-k8s.io/priority-boost` values, and confirm preemption
+  selection order.
+- Run a scenario where boost crosses priority class boundaries and confirm that
```

  https://github.com/kubernetes-sigs/kueue/pull/8551/commits/d0ff8ca3382843f6dca099d9d3e6b55548d0dbee

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-02-24T20:51:52Z

- **keps/7990-preemption-cost/README.md:261** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-24T20:51:52Z
```diff
@@ -0,0 +1,326 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used for ordering within a
+ClusterQueue (both scheduling and preemption candidate ordering).
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - **Safeguard**: As an additional safety measure, Kueue may skip recently
+    admitted workloads when selecting preemption candidates.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero and emit a warning
+    event/log.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero and a warning is logged.
+
+The annotation is set directly on the `Workload` object by an external
+controller (not propagated from Jobs).
+
+YAML example (`Workload`):
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: example-workload
+  namespace: default
+  annotations:
+    # Positive values increase effective priority. Unset means 0.
+    kueue.x-k8s.io/priority-boost: "100"
+spec:
+  priorityClassName: medium
+  # ... rest of spec
+```
+
+### Scheduler changes
+
+Update workload ordering and preemption candidate ordering to use effective
+priority:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+This affects ordering within a ClusterQueue for both scheduling and preemption
+candidate selection. It does not change DRS calculations or preemption
+eligibility rules (preemption policies, reclaim rules).
+
+### Reference controller
+
+Provide a reference external controller that:
+
+- Watches `Workload` objects managed by Kueue.
+- Computes priority boost using a simple, policy-neutral heuristic.
+- Writes/updates `metadata.annotations["kueue.x-k8s.io/priority-boost"]` on
+  the `Workload`.
+
+Reference algorithm (example):
+
+- Start with base boost `0`.
+- Add a bounded increment for each prior eviction with reason `Preempted`
+  observed in the Workload's `schedulingStats`. To avoid preemption flapping,
+  use a threshold-based approach: increase the boost only after every N
+  preemptions (e.g. every 2nd preemption), so that in the worst case a single
+  extra preemption can occur but the cycle does not recur.
+- Optionally add a bounded phase-based increment (for example, running
+  workloads can receive a higher boost than initializing workloads).
+- The controller can combine multiple factors (preemption count, job phase,
+  checkpoint status, crash count) into a single boost value using configurable
+  weights.
+
+The reference implementation is intentionally simple and demonstrates how
+platform teams can plug in richer, organization-specific logic.
+
+### Observability
+
+No new scheduler metrics are required initially.
```

  https://github.com/kubernetes-sigs/kueue/pull/8551/commits/d0ff8ca3382843f6dca099d9d3e6b55548d0dbee

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-02-24T20:51:56Z

- **keps/7990-preemption-cost/README.md:205** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-24T20:51:55Z
```diff
@@ -0,0 +1,326 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used for ordering within a
+ClusterQueue (both scheduling and preemption candidate ordering).
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - **Safeguard**: As an additional safety measure, Kueue may skip recently
+    admitted workloads when selecting preemption candidates.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero and emit a warning
+    event/log.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero and a warning is logged.
+
+The annotation is set directly on the `Workload` object by an external
+controller (not propagated from Jobs).
+
+YAML example (`Workload`):
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: example-workload
+  namespace: default
+  annotations:
+    # Positive values increase effective priority. Unset means 0.
+    kueue.x-k8s.io/priority-boost: "100"
+spec:
+  priorityClassName: medium
+  # ... rest of spec
+```
+
+### Scheduler changes
+
+Update workload ordering and preemption candidate ordering to use effective
+priority:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+This affects ordering within a ClusterQueue for both scheduling and preemption
```

  https://github.com/kubernetes-sigs/kueue/pull/8551/commits/d0ff8ca3382843f6dca099d9d3e6b55548d0dbee

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-02-24T20:51:59Z

- **keps/7990-preemption-cost/README.md:159** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-24T20:51:59Z
```diff
@@ -0,0 +1,326 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used for ordering within a
+ClusterQueue (both scheduling and preemption candidate ordering).
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - **Safeguard**: As an additional safety measure, Kueue may skip recently
+    admitted workloads when selecting preemption candidates.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero and emit a warning
```

  https://github.com/kubernetes-sigs/kueue/pull/8551/commits/d0ff8ca3382843f6dca099d9d3e6b55548d0dbee

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-02-24T20:52:04Z

- **keps/7990-preemption-cost/README.md:142** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-24T20:52:04Z
```diff
@@ -0,0 +1,326 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used for ordering within a
+ClusterQueue (both scheduling and preemption candidate ordering).
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - **Safeguard**: As an additional safety measure, Kueue may skip recently
```

  https://github.com/kubernetes-sigs/kueue/pull/8551/commits/d0ff8ca3382843f6dca099d9d3e6b55548d0dbee

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-02-24T20:52:08Z

- **keps/7990-preemption-cost/README.md:37** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-24T20:52:08Z
```diff
@@ -0,0 +1,326 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used for ordering within a
+ClusterQueue (both scheduling and preemption candidate ordering).
```

  https://github.com/kubernetes-sigs/kueue/pull/8551/commits/d0ff8ca3382843f6dca099d9d3e6b55548d0dbee

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-27T04:22:42Z

Thank you for moving this forward.
I left some feedback and questions.

- **keps/7990-preemption-cost/README.md:93** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-27T03:57:50Z
```diff
@@ -0,0 +1,339 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
```

  ```suggestion
  - Provide Job level APIs (annotations)
  
  ```
  
  I'd like to emphasize that we will not expose APIs to batch users so that we can avoid evil users boosting their workloads by themselves.

- **keps/7990-preemption-cost/README.md:152** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-27T04:08:34Z
```diff
@@ -0,0 +1,339 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - For Alpha, Kueue does not implement admission cooldown. This will be
+    evaluated for Beta based on real-world feedback.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
```

  ```suggestion
      creates Workloads). RBAC on the `Workload`
      resource should be configured to allow only trusted controllers to patch it.
  ```
  
  Even after beta and ga graduationI don't think that we should expose Job label annotations, which are very dangerous.
  The evil users easily boost their Jobs and can preempt others.

- **keps/7990-preemption-cost/README.md:161** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-27T04:10:21Z
```diff
@@ -0,0 +1,339 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - For Alpha, Kueue does not implement admission cooldown. This will be
+    evaluated for Beta based on real-world feedback.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero, emit a warning
+    Event on the Workload, and log the issue.
```

  Why not introducing Workload level validations?
  Could we validate Workload resource when boost annotaion is added or updated?

- **keps/7990-preemption-cost/README.md:175** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-27T04:11:01Z
```diff
@@ -0,0 +1,339 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - For Alpha, Kueue does not implement admission cooldown. This will be
+    evaluated for Beta based on real-world feedback.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero, emit a warning
+    Event on the Workload, and log the issue.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero, a warning Event is emitted on the Workload, and the issue is logged.
```

  As I mentioned, why not validation?

- **keps/7990-preemption-cost/README.md:207** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-27T04:13:50Z
```diff
@@ -0,0 +1,339 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - For Alpha, Kueue does not implement admission cooldown. This will be
+    evaluated for Beta based on real-world feedback.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero, emit a warning
+    Event on the Workload, and log the issue.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero, a warning Event is emitted on the Workload, and the issue is logged.
+
+The annotation is set directly on the `Workload` object by an external
+controller (not propagated from Jobs).
+
+YAML example (`Workload`):
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: Workload
+metadata:
+  name: example-workload
+  namespace: default
+  annotations:
+    # Positive values increase effective priority. Unset means 0.
+    kueue.x-k8s.io/priority-boost: "100"
+spec:
+  priorityClassName: medium
+  # ... rest of spec
+```
+
+### Scheduler changes
+
+Update workload ordering and preemption candidate ordering to use effective
+priority:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+This affects ordering within a ClusterQueue for both scheduling and preemption
+candidate selection. For preemption policies such as `LowerPriority` and
```

  Does this mean kueue-scheduler will not evaluate boost when it sorts head of workloads during each scheduling-cycle?

- **keps/7990-preemption-cost/README.md:257** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-27T04:15:19Z
```diff
@@ -0,0 +1,339 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - For Alpha, Kueue does not implement admission cooldown. This will be
+    evaluated for Beta based on real-world feedback.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero, emit a warning
+    Event on the Workload, and log the issue.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero, a warning Event is emitted on the Workload, and the issue is logged.
+
+The annotation is set directly on the `Workload` object by an external
+controller (not propagated from Jobs).
+
+YAML example (`Workload`):
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: Workload
+metadata:
+  name: example-workload
+  namespace: default
+  annotations:
+    # Positive values increase effective priority. Unset means 0.
+    kueue.x-k8s.io/priority-boost: "100"
+spec:
+  priorityClassName: medium
+  # ... rest of spec
+```
+
+### Scheduler changes
+
+Update workload ordering and preemption candidate ordering to use effective
+priority:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+This affects ordering within a ClusterQueue for both scheduling and preemption
+candidate selection. For preemption policies such as `LowerPriority` and
+`LowerOrNewerEqualPriority`, Kueue uses effective priority (instead of raw
+priority) when evaluating eligibility. It does not change DRS calculations.
+
+### Reference controller
+
+Provide a reference external controller that:
+
+- Watches `Workload` objects managed by Kueue.
+- Computes priority boost using a simple, policy-neutral heuristic.
+- Writes/updates `metadata.annotations["kueue.x-k8s.io/priority-boost"]` on
+  the `Workload`.
+
+Reference algorithm (example):
+
+- Start with base boost `0`.
+- Add a bounded increment for each prior eviction with reason `Preempted`
+  observed in the Workload's `schedulingStats`. To avoid preemption flapping,
+  use a threshold-based approach: increase the boost only after every N
+  preemptions (e.g. every 2nd preemption), so that in the worst case a single
+  extra preemption can occur but the cycle does not recur. Note: this approach
+  bounds the frequency of flapping but does not eliminate it entirely.
+- Optionally add a bounded phase-based increment (for example, running
+  workloads can receive a higher boost than initializing workloads).
+- The controller can combine multiple factors (preemption count, job phase,
+  checkpoint status, crash count) into a single boost value using configurable
+  weights.
+
+The reference implementation is intentionally simple and demonstrates how
+platform teams can plug in richer, organization-specific logic.
+
+### Observability
+
+No new scheduler metrics are required initially.
+
+- Kueue should emit a warning Event on the Workload and log invalid
+  `kueue.x-k8s.io/priority-boost` annotation values at a low verbosity level.
+- When a preemption happens, Kueue should log the effective priority (base +
+  boost) of both the preemptor and the victim.
+- The reference controller should log annotation updates and expose
+  reconciliation errors.
+
+### Security, Privacy, and RBAC
+
+Scheduler side: no additional RBAC is required beyond existing permissions for
+reading `Workload` objects.
+
+Reference controller requires permissions to:
+
+- `get`, `list`, `watch` `workloads.kueue.x-k8s.io`
+- `patch` `workloads.kueue.x-k8s.io`
```

  ```suggestion
  - `workloads.kueue.x-k8s.io`
    - `get`, `list`, `watch`
  - `workloads.kueue.x-k8s.io`
    - `patch`
  ```
  
  For readability

- **keps/7990-preemption-cost/README.md:334** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-27T04:17:11Z
```diff
@@ -0,0 +1,339 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - For Alpha, Kueue does not implement admission cooldown. This will be
+    evaluated for Beta based on real-world feedback.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero, emit a warning
+    Event on the Workload, and log the issue.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero, a warning Event is emitted on the Workload, and the issue is logged.
+
+The annotation is set directly on the `Workload` object by an external
+controller (not propagated from Jobs).
+
+YAML example (`Workload`):
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: Workload
+metadata:
+  name: example-workload
+  namespace: default
+  annotations:
+    # Positive values increase effective priority. Unset means 0.
+    kueue.x-k8s.io/priority-boost: "100"
+spec:
+  priorityClassName: medium
+  # ... rest of spec
+```
+
+### Scheduler changes
+
+Update workload ordering and preemption candidate ordering to use effective
+priority:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+This affects ordering within a ClusterQueue for both scheduling and preemption
+candidate selection. For preemption policies such as `LowerPriority` and
+`LowerOrNewerEqualPriority`, Kueue uses effective priority (instead of raw
+priority) when evaluating eligibility. It does not change DRS calculations.
+
+### Reference controller
+
+Provide a reference external controller that:
+
+- Watches `Workload` objects managed by Kueue.
+- Computes priority boost using a simple, policy-neutral heuristic.
+- Writes/updates `metadata.annotations["kueue.x-k8s.io/priority-boost"]` on
+  the `Workload`.
+
+Reference algorithm (example):
+
+- Start with base boost `0`.
+- Add a bounded increment for each prior eviction with reason `Preempted`
+  observed in the Workload's `schedulingStats`. To avoid preemption flapping,
+  use a threshold-based approach: increase the boost only after every N
+  preemptions (e.g. every 2nd preemption), so that in the worst case a single
+  extra preemption can occur but the cycle does not recur. Note: this approach
+  bounds the frequency of flapping but does not eliminate it entirely.
+- Optionally add a bounded phase-based increment (for example, running
+  workloads can receive a higher boost than initializing workloads).
+- The controller can combine multiple factors (preemption count, job phase,
+  checkpoint status, crash count) into a single boost value using configurable
+  weights.
+
+The reference implementation is intentionally simple and demonstrates how
+platform teams can plug in richer, organization-specific logic.
+
+### Observability
+
+No new scheduler metrics are required initially.
+
+- Kueue should emit a warning Event on the Workload and log invalid
+  `kueue.x-k8s.io/priority-boost` annotation values at a low verbosity level.
+- When a preemption happens, Kueue should log the effective priority (base +
+  boost) of both the preemptor and the victim.
+- The reference controller should log annotation updates and expose
+  reconciliation errors.
+
+### Security, Privacy, and RBAC
+
+Scheduler side: no additional RBAC is required beyond existing permissions for
+reading `Workload` objects.
+
+Reference controller requires permissions to:
+
+- `get`, `list`, `watch` `workloads.kueue.x-k8s.io`
+- `patch` `workloads.kueue.x-k8s.io`
+
+Cluster administrators should ensure that only trusted controllers have `patch`
+access to `Workload` objects to prevent abuse of the `priority-boost`
+annotation.
+
+### Test Plan
+
+#### Unit tests
+
+- Extend existing unit tests for workload ordering and preemption candidate
+  ordering to validate:
+  - effective priority uses `priority + priorityBoost`
+  - missing or invalid annotation values are treated as zero
+  - boost can cross priority class boundaries
+- Add unit tests for the reference controller:
+  - computes expected boost for representative Workload states
+  - writes `kueue.x-k8s.io/priority-boost` annotation idempotently
+  - handles update conflicts/retries
+
+#### Integration/E2E tests
+
+- Run a small integration scenario with two same-priority Workloads and
+  different `kueue.x-k8s.io/priority-boost` values, and confirm preemption
+  selection order.
+- Run a scenario where boost crosses priority class boundaries and confirm that
+  the boosted low-priority workload is ordered above a medium-priority workload.
+- With `LowerPriority` policy, confirm that a low-priority workload with boost
+  giving it effective priority above medium cannot be preempted by
+  medium-priority workloads.
+- Add integration coverage where the reference controller updates Workload
+  annotations and preemption order changes accordingly.
+
+### Graduation Criteria
+
+Alpha -> Beta:
+
+- Re-evaluate user-facing API: consider whether to add a Job-level annotation
+  that Kueue propagates to `Workload`, or keep it Workload-only.
+- Gather feedback from real-world adoption on the `priority-boost` semantics.
+- Evaluate whether an additional "preemption-cost" mechanism (affecting only
+  preemption candidate ordering, not scheduling) is needed alongside
+  `priority-boost`.
+- Validate anti-flapping safeguards are sufficient.
+- Validate interaction with `LowerOrNewerEqualPriority`: if two workloads have
+  different base priorities but the same effective priority after boost, confirm
+  the policy treats them as equal-priority.
+- Evaluate whether a `maxPriorityBoost` cap on ClusterQueue is needed as a
+  safety valve against runaway controllers.
+
```

  @mimowo Shouldn't we consider the combination of DRS and priority-boost in the beta graduation?

- **keps/7990-preemption-cost/README.md:318** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-27T04:17:48Z
```diff
@@ -0,0 +1,339 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - For Alpha, Kueue does not implement admission cooldown. This will be
+    evaluated for Beta based on real-world feedback.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero, emit a warning
+    Event on the Workload, and log the issue.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero, a warning Event is emitted on the Workload, and the issue is logged.
+
+The annotation is set directly on the `Workload` object by an external
+controller (not propagated from Jobs).
+
+YAML example (`Workload`):
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: Workload
+metadata:
+  name: example-workload
+  namespace: default
+  annotations:
+    # Positive values increase effective priority. Unset means 0.
+    kueue.x-k8s.io/priority-boost: "100"
+spec:
+  priorityClassName: medium
+  # ... rest of spec
+```
+
+### Scheduler changes
+
+Update workload ordering and preemption candidate ordering to use effective
+priority:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+This affects ordering within a ClusterQueue for both scheduling and preemption
+candidate selection. For preemption policies such as `LowerPriority` and
+`LowerOrNewerEqualPriority`, Kueue uses effective priority (instead of raw
+priority) when evaluating eligibility. It does not change DRS calculations.
+
+### Reference controller
+
+Provide a reference external controller that:
+
+- Watches `Workload` objects managed by Kueue.
+- Computes priority boost using a simple, policy-neutral heuristic.
+- Writes/updates `metadata.annotations["kueue.x-k8s.io/priority-boost"]` on
+  the `Workload`.
+
+Reference algorithm (example):
+
+- Start with base boost `0`.
+- Add a bounded increment for each prior eviction with reason `Preempted`
+  observed in the Workload's `schedulingStats`. To avoid preemption flapping,
+  use a threshold-based approach: increase the boost only after every N
+  preemptions (e.g. every 2nd preemption), so that in the worst case a single
+  extra preemption can occur but the cycle does not recur. Note: this approach
+  bounds the frequency of flapping but does not eliminate it entirely.
+- Optionally add a bounded phase-based increment (for example, running
+  workloads can receive a higher boost than initializing workloads).
+- The controller can combine multiple factors (preemption count, job phase,
+  checkpoint status, crash count) into a single boost value using configurable
+  weights.
+
+The reference implementation is intentionally simple and demonstrates how
+platform teams can plug in richer, organization-specific logic.
+
+### Observability
+
+No new scheduler metrics are required initially.
+
+- Kueue should emit a warning Event on the Workload and log invalid
+  `kueue.x-k8s.io/priority-boost` annotation values at a low verbosity level.
+- When a preemption happens, Kueue should log the effective priority (base +
+  boost) of both the preemptor and the victim.
+- The reference controller should log annotation updates and expose
+  reconciliation errors.
+
+### Security, Privacy, and RBAC
+
+Scheduler side: no additional RBAC is required beyond existing permissions for
+reading `Workload` objects.
+
+Reference controller requires permissions to:
+
+- `get`, `list`, `watch` `workloads.kueue.x-k8s.io`
+- `patch` `workloads.kueue.x-k8s.io`
+
+Cluster administrators should ensure that only trusted controllers have `patch`
+access to `Workload` objects to prevent abuse of the `priority-boost`
+annotation.
+
+### Test Plan
+
+#### Unit tests
+
+- Extend existing unit tests for workload ordering and preemption candidate
+  ordering to validate:
+  - effective priority uses `priority + priorityBoost`
+  - missing or invalid annotation values are treated as zero
+  - boost can cross priority class boundaries
+- Add unit tests for the reference controller:
+  - computes expected boost for representative Workload states
+  - writes `kueue.x-k8s.io/priority-boost` annotation idempotently
+  - handles update conflicts/retries
+
+#### Integration/E2E tests
+
+- Run a small integration scenario with two same-priority Workloads and
+  different `kueue.x-k8s.io/priority-boost` values, and confirm preemption
+  selection order.
+- Run a scenario where boost crosses priority class boundaries and confirm that
+  the boosted low-priority workload is ordered above a medium-priority workload.
+- With `LowerPriority` policy, confirm that a low-priority workload with boost
+  giving it effective priority above medium cannot be preempted by
+  medium-priority workloads.
+- Add integration coverage where the reference controller updates Workload
+  annotations and preemption order changes accordingly.
+
+### Graduation Criteria
```

  Does this mean we never graduate this to GA?

- **keps/7990-preemption-cost/README.md:245** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-27T04:20:11Z
```diff
@@ -0,0 +1,339 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - For Alpha, Kueue does not implement admission cooldown. This will be
+    evaluated for Beta based on real-world feedback.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero, emit a warning
+    Event on the Workload, and log the issue.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero, a warning Event is emitted on the Workload, and the issue is logged.
+
+The annotation is set directly on the `Workload` object by an external
+controller (not propagated from Jobs).
+
+YAML example (`Workload`):
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: Workload
+metadata:
+  name: example-workload
+  namespace: default
+  annotations:
+    # Positive values increase effective priority. Unset means 0.
+    kueue.x-k8s.io/priority-boost: "100"
+spec:
+  priorityClassName: medium
+  # ... rest of spec
+```
+
+### Scheduler changes
+
+Update workload ordering and preemption candidate ordering to use effective
+priority:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+This affects ordering within a ClusterQueue for both scheduling and preemption
+candidate selection. For preemption policies such as `LowerPriority` and
+`LowerOrNewerEqualPriority`, Kueue uses effective priority (instead of raw
+priority) when evaluating eligibility. It does not change DRS calculations.
+
+### Reference controller
+
+Provide a reference external controller that:
+
+- Watches `Workload` objects managed by Kueue.
+- Computes priority boost using a simple, policy-neutral heuristic.
+- Writes/updates `metadata.annotations["kueue.x-k8s.io/priority-boost"]` on
+  the `Workload`.
+
+Reference algorithm (example):
+
+- Start with base boost `0`.
+- Add a bounded increment for each prior eviction with reason `Preempted`
+  observed in the Workload's `schedulingStats`. To avoid preemption flapping,
+  use a threshold-based approach: increase the boost only after every N
+  preemptions (e.g. every 2nd preemption), so that in the worst case a single
+  extra preemption can occur but the cycle does not recur. Note: this approach
+  bounds the frequency of flapping but does not eliminate it entirely.
+- Optionally add a bounded phase-based increment (for example, running
+  workloads can receive a higher boost than initializing workloads).
+- The controller can combine multiple factors (preemption count, job phase,
+  checkpoint status, crash count) into a single boost value using configurable
+  weights.
+
+The reference implementation is intentionally simple and demonstrates how
+platform teams can plug in richer, organization-specific logic.
+
+### Observability
+
+No new scheduler metrics are required initially.
+
+- Kueue should emit a warning Event on the Workload and log invalid
+  `kueue.x-k8s.io/priority-boost` annotation values at a low verbosity level.
+- When a preemption happens, Kueue should log the effective priority (base +
+  boost) of both the preemptor and the victim.
```

  ```suggestion
    boost) of both the preemptor and the preemptees.
  ```
  
  Let's use consistent terminology: https://kueue.sigs.k8s.io/docs/concepts/preemption/

- **keps/7990-preemption-cost/README.md:171** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-27T04:22:08Z
```diff
@@ -0,0 +1,339 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - For Alpha, Kueue does not implement admission cooldown. This will be
+    evaluated for Beta based on real-world feedback.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero, emit a warning
+    Event on the Workload, and log the issue.
+
+## Design Details
```

  1. Can we boost Workloads even when Workload uses Pod PriorityClass?
  2. Can we boost Workloads even when both Pod PriorityClass and WorkloadPriorityClass are not tied to the Workload? The unspecified PriorityClass means 0 priority.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-27T17:14:08Z

- **keps/7990-preemption-cost/kep.yaml:8** by [@mimowo](https://github.com/mimowo) — 2026-02-27T17:14:08Z
```diff
@@ -0,0 +1,33 @@
+title: Support Workload Priority Boost By External Controllers
+kep-number: 7990
+authors:
+  - "@vladikkuzn"
+status: implementable
+creation-date: 2026-01-12
+reviewers:
+  - "@mimowo"
```

  please add @sanposhiho

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-27T17:20:23Z

- **keps/7990-preemption-cost/README.md:93** by [@mimowo](https://github.com/mimowo) — 2026-02-27T17:20:23Z
```diff
@@ -0,0 +1,339 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
```

  Sure, We  can add that. 
  
  However, saying **we can avoid evil users boosting their workloads by themselves** is a bit too strong - we cannot really prevent it, because:
  1. users can modify Workload API typically anyway, as they have access to the namespace
  2. users could modify the Job-level API to bump at the  `kueue.x-k8s.io/priority-class` level
  
  So:
  a) this mechanism to be secure still requires some "good will" or in-house ValidationAdmissionPolicies configured by the admin
  b) FairSharing can be used to mitigate bumping priority too high across teams

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-27T17:23:46Z

- **keps/7990-preemption-cost/README.md:171** by [@mimowo](https://github.com/mimowo) — 2026-02-27T17:23:46Z
```diff
@@ -0,0 +1,339 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - For Alpha, Kueue does not implement admission cooldown. This will be
+    evaluated for Beta based on real-world feedback.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero, emit a warning
+    Event on the Workload, and log the issue.
+
+## Design Details
```

  1. Yes, this mechanism just adds "boost" to the Workload's priority. Regardless of the"source" of the priority for the Workload (Pod PriorityClass or WorkloadPriorityClass or custom logic in the integration helper)
  2. yes, this is othogonal to how the "base priority" is derived

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-27T17:24:38Z

- **keps/7990-preemption-cost/README.md:161** by [@mimowo](https://github.com/mimowo) — 2026-02-27T17:24:38Z
```diff
@@ -0,0 +1,339 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - For Alpha, Kueue does not implement admission cooldown. This will be
+    evaluated for Beta based on real-world feedback.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero, emit a warning
+    Event on the Workload, and log the issue.
```

  Sure, I propose a small subsection under Design Details about the validation. It will be very simple - just check if integer.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-27T17:26:07Z

- **keps/7990-preemption-cost/README.md:152** by [@mimowo](https://github.com/mimowo) — 2026-02-27T17:26:07Z
```diff
@@ -0,0 +1,339 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - For Alpha, Kueue does not implement admission cooldown. This will be
+    evaluated for Beta based on real-world feedback.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
```

  Ok, we can drop that, but I feel there is some misconception about the risk here. Discussed a bit more in here: https://github.com/kubernetes-sigs/kueue/pull/8551/changes#r2865379251. Let me know if I'm missing something. Still, I'm ok to exclude it from the plans.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-27T17:29:02Z

- **keps/7990-preemption-cost/README.md:207** by [@mimowo](https://github.com/mimowo) — 2026-02-27T17:29:02Z
```diff
@@ -0,0 +1,339 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - For Alpha, Kueue does not implement admission cooldown. This will be
+    evaluated for Beta based on real-world feedback.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero, emit a warning
+    Event on the Workload, and log the issue.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero, a warning Event is emitted on the Workload, and the issue is logged.
+
+The annotation is set directly on the `Workload` object by an external
+controller (not propagated from Jobs).
+
+YAML example (`Workload`):
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: Workload
+metadata:
+  name: example-workload
+  namespace: default
+  annotations:
+    # Positive values increase effective priority. Unset means 0.
+    kueue.x-k8s.io/priority-boost: "100"
+spec:
+  priorityClassName: medium
+  # ... rest of spec
+```
+
+### Scheduler changes
+
+Update workload ordering and preemption candidate ordering to use effective
+priority:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+This affects ordering within a ClusterQueue for both scheduling and preemption
+candidate selection. For preemption policies such as `LowerPriority` and
```

  Why would that follow from the sentence? Kueue-scheduler is going to use the "effective priority" for all the ordering and operations for which the base "priority" is used currently.
  
  Rather than listing all places we could say this sentence, and then provide examples. This will make it clear.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-27T17:30:29Z

- **keps/7990-preemption-cost/README.md:334** by [@mimowo](https://github.com/mimowo) — 2026-02-27T17:30:29Z
```diff
@@ -0,0 +1,339 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - For Alpha, Kueue does not implement admission cooldown. This will be
+    evaluated for Beta based on real-world feedback.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero, emit a warning
+    Event on the Workload, and log the issue.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero, a warning Event is emitted on the Workload, and the issue is logged.
+
+The annotation is set directly on the `Workload` object by an external
+controller (not propagated from Jobs).
+
+YAML example (`Workload`):
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: Workload
+metadata:
+  name: example-workload
+  namespace: default
+  annotations:
+    # Positive values increase effective priority. Unset means 0.
+    kueue.x-k8s.io/priority-boost: "100"
+spec:
+  priorityClassName: medium
+  # ... rest of spec
+```
+
+### Scheduler changes
+
+Update workload ordering and preemption candidate ordering to use effective
+priority:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+This affects ordering within a ClusterQueue for both scheduling and preemption
+candidate selection. For preemption policies such as `LowerPriority` and
+`LowerOrNewerEqualPriority`, Kueue uses effective priority (instead of raw
+priority) when evaluating eligibility. It does not change DRS calculations.
+
+### Reference controller
+
+Provide a reference external controller that:
+
+- Watches `Workload` objects managed by Kueue.
+- Computes priority boost using a simple, policy-neutral heuristic.
+- Writes/updates `metadata.annotations["kueue.x-k8s.io/priority-boost"]` on
+  the `Workload`.
+
+Reference algorithm (example):
+
+- Start with base boost `0`.
+- Add a bounded increment for each prior eviction with reason `Preempted`
+  observed in the Workload's `schedulingStats`. To avoid preemption flapping,
+  use a threshold-based approach: increase the boost only after every N
+  preemptions (e.g. every 2nd preemption), so that in the worst case a single
+  extra preemption can occur but the cycle does not recur. Note: this approach
+  bounds the frequency of flapping but does not eliminate it entirely.
+- Optionally add a bounded phase-based increment (for example, running
+  workloads can receive a higher boost than initializing workloads).
+- The controller can combine multiple factors (preemption count, job phase,
+  checkpoint status, crash count) into a single boost value using configurable
+  weights.
+
+The reference implementation is intentionally simple and demonstrates how
+platform teams can plug in richer, organization-specific logic.
+
+### Observability
+
+No new scheduler metrics are required initially.
+
+- Kueue should emit a warning Event on the Workload and log invalid
+  `kueue.x-k8s.io/priority-boost` annotation values at a low verbosity level.
+- When a preemption happens, Kueue should log the effective priority (base +
+  boost) of both the preemptor and the victim.
+- The reference controller should log annotation updates and expose
+  reconciliation errors.
+
+### Security, Privacy, and RBAC
+
+Scheduler side: no additional RBAC is required beyond existing permissions for
+reading `Workload` objects.
+
+Reference controller requires permissions to:
+
+- `get`, `list`, `watch` `workloads.kueue.x-k8s.io`
+- `patch` `workloads.kueue.x-k8s.io`
+
+Cluster administrators should ensure that only trusted controllers have `patch`
+access to `Workload` objects to prevent abuse of the `priority-boost`
+annotation.
+
+### Test Plan
+
+#### Unit tests
+
+- Extend existing unit tests for workload ordering and preemption candidate
+  ordering to validate:
+  - effective priority uses `priority + priorityBoost`
+  - missing or invalid annotation values are treated as zero
+  - boost can cross priority class boundaries
+- Add unit tests for the reference controller:
+  - computes expected boost for representative Workload states
+  - writes `kueue.x-k8s.io/priority-boost` annotation idempotently
+  - handles update conflicts/retries
+
+#### Integration/E2E tests
+
+- Run a small integration scenario with two same-priority Workloads and
+  different `kueue.x-k8s.io/priority-boost` values, and confirm preemption
+  selection order.
+- Run a scenario where boost crosses priority class boundaries and confirm that
+  the boosted low-priority workload is ordered above a medium-priority workload.
+- With `LowerPriority` policy, confirm that a low-priority workload with boost
+  giving it effective priority above medium cannot be preempted by
+  medium-priority workloads.
+- Add integration coverage where the reference controller updates Workload
+  annotations and preemption order changes accordingly.
+
+### Graduation Criteria
+
+Alpha -> Beta:
+
+- Re-evaluate user-facing API: consider whether to add a Job-level annotation
+  that Kueue propagates to `Workload`, or keep it Workload-only.
+- Gather feedback from real-world adoption on the `priority-boost` semantics.
+- Evaluate whether an additional "preemption-cost" mechanism (affecting only
+  preemption candidate ordering, not scheduling) is needed alongside
+  `priority-boost`.
+- Validate anti-flapping safeguards are sufficient.
+- Validate interaction with `LowerOrNewerEqualPriority`: if two workloads have
+  different base priorities but the same effective priority after boost, confirm
+  the policy treats them as equal-priority.
+- Evaluate whether a `maxPriorityBoost` cap on ClusterQueue is needed as a
+  safety valve against runaway controllers.
+
```

  I wouldn't like to overload the KEP. This topic is way more complex, and I think deserves a separate Issue / KEP. We could consider adding it to be re-evaluated for GA maybe.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-27T17:31:02Z

- **keps/7990-preemption-cost/kep.yaml:8** by [@mimowo](https://github.com/mimowo) — 2026-02-27T17:31:02Z
```diff
@@ -0,0 +1,33 @@
+title: Support Workload Priority Boost By External Controllers
+kep-number: 7990
+authors:
+  - "@vladikkuzn"
+status: implementable
+creation-date: 2026-01-12
+reviewers:
+  - "@mimowo"
```

  and @sohankunkerkar

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-03-01T05:58:48Z

- **keps/7990-preemption-cost/README.md:93** by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-01T05:58:48Z
```diff
@@ -0,0 +1,339 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
```

  > 1. users can modify Workload API typically anyway, as they have access to the namespace
  
  Is this really common way? AFAIK, the Workload is primary for the Kueue internal usage and cluster admins.
  Surely, in some platforms, the batch users might have writable persmissions. But, I think that giving writable permission to batch users is not recommended way.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-03-01T06:00:02Z

- **keps/7990-preemption-cost/README.md:152** by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-01T06:00:02Z
```diff
@@ -0,0 +1,339 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - For Alpha, Kueue does not implement admission cooldown. This will be
+    evaluated for Beta based on real-world feedback.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
```

  Sure, let's continue discussion in  https://github.com/kubernetes-sigs/kueue/pull/8551/changes#r2865379251

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-03-01T06:00:30Z

- **keps/7990-preemption-cost/README.md:161** by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-01T06:00:30Z
```diff
@@ -0,0 +1,339 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - For Alpha, Kueue does not implement admission cooldown. This will be
+    evaluated for Beta based on real-world feedback.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero, emit a warning
+    Event on the Workload, and log the issue.
```

  That sounds reasonable.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-03-01T06:02:16Z

- **keps/7990-preemption-cost/README.md:207** by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-01T06:02:16Z
```diff
@@ -0,0 +1,339 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - For Alpha, Kueue does not implement admission cooldown. This will be
+    evaluated for Beta based on real-world feedback.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero, emit a warning
+    Event on the Workload, and log the issue.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero, a warning Event is emitted on the Workload, and the issue is logged.
+
+The annotation is set directly on the `Workload` object by an external
+controller (not propagated from Jobs).
+
+YAML example (`Workload`):
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: Workload
+metadata:
+  name: example-workload
+  namespace: default
+  annotations:
+    # Positive values increase effective priority. Unset means 0.
+    kueue.x-k8s.io/priority-boost: "100"
+spec:
+  priorityClassName: medium
+  # ... rest of spec
+```
+
+### Scheduler changes
+
+Update workload ordering and preemption candidate ordering to use effective
+priority:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+This affects ordering within a ClusterQueue for both scheduling and preemption
+candidate selection. For preemption policies such as `LowerPriority` and
```

  Just checking, @mimowo do you send your message to @vladikkuzn, right?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-03-01T06:03:36Z

- **keps/7990-preemption-cost/README.md:334** by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-01T06:03:35Z
```diff
@@ -0,0 +1,339 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - For Alpha, Kueue does not implement admission cooldown. This will be
+    evaluated for Beta based on real-world feedback.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero, emit a warning
+    Event on the Workload, and log the issue.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero, a warning Event is emitted on the Workload, and the issue is logged.
+
+The annotation is set directly on the `Workload` object by an external
+controller (not propagated from Jobs).
+
+YAML example (`Workload`):
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: Workload
+metadata:
+  name: example-workload
+  namespace: default
+  annotations:
+    # Positive values increase effective priority. Unset means 0.
+    kueue.x-k8s.io/priority-boost: "100"
+spec:
+  priorityClassName: medium
+  # ... rest of spec
+```
+
+### Scheduler changes
+
+Update workload ordering and preemption candidate ordering to use effective
+priority:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+This affects ordering within a ClusterQueue for both scheduling and preemption
+candidate selection. For preemption policies such as `LowerPriority` and
+`LowerOrNewerEqualPriority`, Kueue uses effective priority (instead of raw
+priority) when evaluating eligibility. It does not change DRS calculations.
+
+### Reference controller
+
+Provide a reference external controller that:
+
+- Watches `Workload` objects managed by Kueue.
+- Computes priority boost using a simple, policy-neutral heuristic.
+- Writes/updates `metadata.annotations["kueue.x-k8s.io/priority-boost"]` on
+  the `Workload`.
+
+Reference algorithm (example):
+
+- Start with base boost `0`.
+- Add a bounded increment for each prior eviction with reason `Preempted`
+  observed in the Workload's `schedulingStats`. To avoid preemption flapping,
+  use a threshold-based approach: increase the boost only after every N
+  preemptions (e.g. every 2nd preemption), so that in the worst case a single
+  extra preemption can occur but the cycle does not recur. Note: this approach
+  bounds the frequency of flapping but does not eliminate it entirely.
+- Optionally add a bounded phase-based increment (for example, running
+  workloads can receive a higher boost than initializing workloads).
+- The controller can combine multiple factors (preemption count, job phase,
+  checkpoint status, crash count) into a single boost value using configurable
+  weights.
+
+The reference implementation is intentionally simple and demonstrates how
+platform teams can plug in richer, organization-specific logic.
+
+### Observability
+
+No new scheduler metrics are required initially.
+
+- Kueue should emit a warning Event on the Workload and log invalid
+  `kueue.x-k8s.io/priority-boost` annotation values at a low verbosity level.
+- When a preemption happens, Kueue should log the effective priority (base +
+  boost) of both the preemptor and the victim.
+- The reference controller should log annotation updates and expose
+  reconciliation errors.
+
+### Security, Privacy, and RBAC
+
+Scheduler side: no additional RBAC is required beyond existing permissions for
+reading `Workload` objects.
+
+Reference controller requires permissions to:
+
+- `get`, `list`, `watch` `workloads.kueue.x-k8s.io`
+- `patch` `workloads.kueue.x-k8s.io`
+
+Cluster administrators should ensure that only trusted controllers have `patch`
+access to `Workload` objects to prevent abuse of the `priority-boost`
+annotation.
+
+### Test Plan
+
+#### Unit tests
+
+- Extend existing unit tests for workload ordering and preemption candidate
+  ordering to validate:
+  - effective priority uses `priority + priorityBoost`
+  - missing or invalid annotation values are treated as zero
+  - boost can cross priority class boundaries
+- Add unit tests for the reference controller:
+  - computes expected boost for representative Workload states
+  - writes `kueue.x-k8s.io/priority-boost` annotation idempotently
+  - handles update conflicts/retries
+
+#### Integration/E2E tests
+
+- Run a small integration scenario with two same-priority Workloads and
+  different `kueue.x-k8s.io/priority-boost` values, and confirm preemption
+  selection order.
+- Run a scenario where boost crosses priority class boundaries and confirm that
+  the boosted low-priority workload is ordered above a medium-priority workload.
+- With `LowerPriority` policy, confirm that a low-priority workload with boost
+  giving it effective priority above medium cannot be preempted by
+  medium-priority workloads.
+- Add integration coverage where the reference controller updates Workload
+  annotations and preemption order changes accordingly.
+
+### Graduation Criteria
+
+Alpha -> Beta:
+
+- Re-evaluate user-facing API: consider whether to add a Job-level annotation
+  that Kueue propagates to `Workload`, or keep it Workload-only.
+- Gather feedback from real-world adoption on the `priority-boost` semantics.
+- Evaluate whether an additional "preemption-cost" mechanism (affecting only
+  preemption candidate ordering, not scheduling) is needed alongside
+  `priority-boost`.
+- Validate anti-flapping safeguards are sufficient.
+- Validate interaction with `LowerOrNewerEqualPriority`: if two workloads have
+  different base priorities but the same effective priority after boost, confirm
+  the policy treats them as equal-priority.
+- Evaluate whether a `maxPriorityBoost` cap on ClusterQueue is needed as a
+  safety valve against runaway controllers.
+
```

  > I wouldn't like to overload the KEP. This topic is way more complex, and I think deserves a separate Issue / KEP. We could consider adding it to be re-evaluated for GA maybe.
  
  Yeah, that could be. As we discussed in https://github.com/kubernetes-sigs/kueue/issues/7990#issuecomment-3978905691, we can reevaluate DRS combination when GA graduation, then we might handle DRS combination as a seeparate feature.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-03-01T06:04:19Z

- **keps/7990-preemption-cost/README.md:171** by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-01T06:04:19Z
```diff
@@ -0,0 +1,339 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - For Alpha, Kueue does not implement admission cooldown. This will be
+    evaluated for Beta based on real-world feedback.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero, emit a warning
+    Event on the Workload, and log the issue.
+
+## Design Details
```

  Both sound good to me, thank you!
  @vladikkuzn Could you clarify the above points in your KEP?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-02T09:32:37Z

- **keps/7990-preemption-cost/README.md:207** by [@mimowo](https://github.com/mimowo) — 2026-03-02T09:32:37Z
```diff
@@ -0,0 +1,339 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - For Alpha, Kueue does not implement admission cooldown. This will be
+    evaluated for Beta based on real-world feedback.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero, emit a warning
+    Event on the Workload, and log the issue.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero, a warning Event is emitted on the Workload, and the issue is logged.
+
+The annotation is set directly on the `Workload` object by an external
+controller (not propagated from Jobs).
+
+YAML example (`Workload`):
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: Workload
+metadata:
+  name: example-workload
+  namespace: default
+  annotations:
+    # Positive values increase effective priority. Unset means 0.
+    kueue.x-k8s.io/priority-boost: "100"
+spec:
+  priorityClassName: medium
+  # ... rest of spec
+```
+
+### Scheduler changes
+
+Update workload ordering and preemption candidate ordering to use effective
+priority:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+This affects ordering within a ClusterQueue for both scheduling and preemption
+candidate selection. For preemption policies such as `LowerPriority` and
```

  Oh, sure let me clarify that.
  
  The first part was referring to your question @tenzen-y, where you asked about a specific part of the scheduler's logic, when sorking the heads. So I let myself answer that the "boost" will also impact this part of the algo.
  
  The second part was a suggestion to @vladikkuzn to rephrase the paragraph to avoid such confusions. Rather than listing all places we could say "Kueue-scheduler is going to use the "effective priority" for all mechanics where the base "priority" is currently used. In particular, it will be used to:
  1. ordering of heads for scheduling
  2. selection of candidates
  
  By saying "in particular" we clearly indicate the list does not need to cover all places - it is focused on the most imprtant onces.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-03T18:15:35Z

Thank you for working on this 👍 
/lgtm
/approve
/hold
in case @tenzen-y has some extra remarks, but I think you can already start coding the solution, because we already align on most points.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-03-03T18:18:33Z

- **keps/7990-preemption-cost/README.md:207** by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-03T18:18:32Z
```diff
@@ -0,0 +1,339 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - For Alpha, Kueue does not implement admission cooldown. This will be
+    evaluated for Beta based on real-world feedback.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). For Alpha, restricting the annotation to `Workload`
+    prevents batch users from exploiting the mechanism. RBAC on the `Workload`
+    resource should be configured to allow only trusted controllers to patch it.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero, emit a warning
+    Event on the Workload, and log the issue.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero, a warning Event is emitted on the Workload, and the issue is logged.
+
+The annotation is set directly on the `Workload` object by an external
+controller (not propagated from Jobs).
+
+YAML example (`Workload`):
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: Workload
+metadata:
+  name: example-workload
+  namespace: default
+  annotations:
+    # Positive values increase effective priority. Unset means 0.
+    kueue.x-k8s.io/priority-boost: "100"
+spec:
+  priorityClassName: medium
+  # ... rest of spec
+```
+
+### Scheduler changes
+
+Update workload ordering and preemption candidate ordering to use effective
+priority:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+This affects ordering within a ClusterQueue for both scheduling and preemption
+candidate selection. For preemption policies such as `LowerPriority` and
```

  > The first part was referring to your question @tenzen-y, where you asked about a specific part of the scheduler's logic, when sorking the heads. So I let myself answer that the "boost" will also impact this part of the algo.
  
  I see, thank you for describing the boost algo. That sounds good to me.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-03-03T18:26:16Z

I left just one comment.
Otherwise, LGTM.

- **keps/7990-preemption-cost/README.md:214** by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-03T18:26:04Z
```diff
@@ -0,0 +1,373 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Validation](#validation)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+- Expose Job-level APIs (annotations) for priority boost; the mechanism stays
+  Workload-only so that batch users cannot set boost on their Jobs.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - For Alpha, Kueue does not implement admission cooldown. This will be
+    evaluated for Beta based on real-world feedback.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). We do not plan to expose Job-level annotations for
+    priority boost at beta or GA, to avoid giving batch users direct control of
+    boost. In typical setups, `Workload` is used internally by Kueue and
+    cluster admins; giving batch users writable access to `Workload` is not
+    recommended. Security in practice relies on: (a) restricting who can patch
+    `Workload` (RBAC so only trusted controllers can patch it) and optionally
+    admin-configured validation (e.g. ValidationAdmissionPolicies), and (b)
+    FairSharing can help mitigate excessive priority bumping across teams.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero, emit a warning
+    Event on the Workload, and log the issue.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Applicability**: Priority boost applies regardless of how the Workload's
+    base priority is derived (Pod PriorityClass, WorkloadPriorityClass, or
+    custom logic in an integration helper). Boost also applies when no
+    PriorityClass is specified (base priority 0); effective priority is then
+    `0 + priorityBoost`.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero, a warning Event is emitted on the Workload, and the issue is logged.
+
+The annotation is set directly on the `Workload` object by an external
+controller (not propagated from Jobs).
+
+YAML example (`Workload`):
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: Workload
+metadata:
+  name: example-workload
+  namespace: default
+  annotations:
+    # Positive values increase effective priority. Unset means 0.
+    kueue.x-k8s.io/priority-boost: "100"
+spec:
+  priorityClassName: medium
+  # ... rest of spec
+```
+
+### Validation
+
+When the `kueue.x-k8s.io/priority-boost` annotation is set or updated on a
+Workload, Kueue validates that the value is a valid signed integer. Invalid or
+unparsable values are treated as zero, a warning Event is emitted on the
+Workload, and the issue is logged (see API changes above).
```

  @vladikkuzn @mimowo Instead of warnings, I think that we should reject Workload creation or update when an invalid boost value is used.
  
  Any reason for just logging?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-03T18:34:02Z

- **keps/7990-preemption-cost/README.md:214** by [@mimowo](https://github.com/mimowo) — 2026-03-03T18:34:02Z
```diff
@@ -0,0 +1,373 @@
+# KEP-7990: Support workload priority boost by external controllers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Effective priority](#effective-priority)
+  - [Defaulting and backward compatibility](#defaulting-and-backward-compatibility)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API changes](#api-changes)
+  - [Validation](#validation)
+  - [Scheduler changes](#scheduler-changes)
+  - [Reference controller](#reference-controller)
+  - [Observability](#observability)
+  - [Security, Privacy, and RBAC](#security-privacy-and-rbac)
+  - [Test Plan](#test-plan)
+    - [Unit tests](#unit-tests)
+    - [Integration/E2E tests](#integratione2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce an optional `Workload` annotation `kueue.x-k8s.io/priority-boost`
+that allows external controllers to boost (or reduce) a workload's effective
+priority. Kueue computes the **effective priority** as:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+and uses it wherever workload priority is currently used within a ClusterQueue,
+including scheduling order, preemption candidate ordering, and preemption
+eligibility evaluation.
+
+This enables platform teams to dynamically adjust workload priority based on
+runtime signals (e.g. checkpoint status, execution phase, number of prior
+preemptions), without hardcoding cost policies into Kueue and without requiring
+a proliferation of WorkloadPriority classes.
+
+## Motivation
+
+The "cost" of preempting a workload is not uniform. Some workloads are cheap to
+interrupt (e.g. still initializing), while others are expensive (e.g. mid-epoch
+without checkpoint). Kueue today makes preemption decisions primarily based on
+cluster queue, fairness configuration, workload priority, and recency; it has no
+mechanism to incorporate runtime cost signals from external systems.
+
+Existing priority classes are static: a workload receives a priority at creation
+time and keeps it for its lifetime. In practice, platform teams need a way to
+adjust effective priority dynamically:
+
+- **Visibility**: an explicit annotation makes it clear to both users and
+  external controllers that the priority was boosted, avoiding repeated or
+  conflicting adjustments.
+- **Fine-grained priority without class proliferation**: with a numeric boost,
+  controllers can express priorities like "150" or "156" without creating a zoo
+  of WorkloadPriority classes for every combination of base priority and runtime
+  state.
+- **Controller convenience**: the annotation lives on the `Workload` object, so
+  the external controller only needs to operate on a single Kind rather than
+  every schedulable object type (Job, JobSet, Deployment, etc.).
+
+An important use case is preventing starvation of low-priority workloads: if a
+low-priority workload has been preempted too many times and has never completed,
+an external controller can boost it above medium-priority workloads so it
+eventually makes progress.
+
+### Goals
+
+- Add a new optional `Workload` annotation (`kueue.x-k8s.io/priority-boost`)
+  which allows external controllers to boost priority for some workloads.
+- Define effective priority as `workload.priority + priorityBoost` and use it
+  for workload ordering within a ClusterQueue (scheduling and preemption).
+- Provide a reference external controller that demonstrates how to compute and
+  set `kueue.x-k8s.io/priority-boost` on `Workload`.
+
+### Non-Goals
+
+- Define a universal "correct" cost or boost model inside Kueue.
+- Impact DRS (Dominant Resource Share) calculations; the boost only affects
+  priority-based ordering within a ClusterQueue.
+- Define anti-flapping policies inside Kueue; it is the responsibility of the
+  external controller to avoid pathological update patterns.
+- Provide guarantees about graceful termination or checkpointing.
+- Expose Job-level APIs (annotations) for priority boost; the mechanism stays
+  Workload-only so that batch users cannot set boost on their Jobs.
+
+## Proposal
+
+Add an optional `Workload` annotation `kueue.x-k8s.io/priority-boost` that
+external controllers can set to dynamically adjust a workload's effective
+priority. Kueue incorporates this signal into both scheduling order and
+preemption candidate ordering within a ClusterQueue.
+
+### User Stories
+
+- As a cluster administrator, I want Kueue to preferentially preempt workloads
+  that are cheaper to interrupt, even when multiple candidates share the same
+  WorkloadPriority class.
+- As a platform team, I want to implement organization-specific logic (e.g.
+  checkpoint-aware, phase-aware, starvation-prevention) that dynamically adjusts
+  workload priority and have Kueue respect it without modifying the scheduler.
+- As a platform team, I want to boost a low-priority workload that has been
+  preempted too many times so that it can cross priority class boundaries and
+  eventually make progress.
+
+### Effective priority
+
+Wherever Kueue currently uses workload priority for ordering within a
+ClusterQueue, it uses instead:
+
+`effectivePriority = workload.priority + priorityBoost`
+
+where `priorityBoost` is read from the `Workload` annotation
+`kueue.x-k8s.io/priority-boost` (default `0`).
+
+The boost can cross priority class boundaries. For example, if priorities are
+`low=100`, `mid=200`, `high=300`, a low-priority workload with
+`priority-boost: "150"` would have effective priority 250, placing it between
+mid and high.
+
+### Defaulting and backward compatibility
+
+- If a workload has no `kueue.x-k8s.io/priority-boost` annotation set, it is
+  treated as **zero** boost.
+  - This preserves existing behavior when the annotation is unused.
+- The feature is "opt-in by signal": behavior changes only when an external
+  controller (or administrator) sets the annotation.
+
+### Risks and Mitigations
+
+- **Risk**: Infinite preemption cycles. Introducing a dynamic signal that
+  changes ordering creates the possibility of workloads repeatedly preempting
+  each other (A preempts B, B's boost increases, B preempts A, ...).
+  - **Mitigation**: Kueue uses the boost value as a point-in-time snapshot
+    during a scheduling cycle. The responsibility for avoiding flapping lies
+    with the external controller. The reference controller (see below) is
+    designed to limit oscillation by using bounded, threshold-based increments
+    rather than reacting to every preemption.
+  - For Alpha, Kueue does not implement admission cooldown. This will be
+    evaluated for Beta based on real-world feedback.
+
+- **Risk**: Malicious or misconfigured users set extreme boost values on their
+  workloads to avoid preemption.
+  - **Mitigation**: The annotation is on the `Workload` object, which is not
+    directly user-editable in typical deployments (users submit Jobs; Kueue
+    creates Workloads). We do not plan to expose Job-level annotations for
+    priority boost at beta or GA, to avoid giving batch users direct control of
+    boost. In typical setups, `Workload` is used internally by Kueue and
+    cluster admins; giving batch users writable access to `Workload` is not
+    recommended. Security in practice relies on: (a) restricting who can patch
+    `Workload` (RBAC so only trusted controllers can patch it) and optionally
+    admin-configured validation (e.g. ValidationAdmissionPolicies), and (b)
+    FairSharing can help mitigate excessive priority bumping across teams.
+
+- **Risk**: Users/platform automation set unexpected values, causing undesirable
+  ordering.
+  - **Mitigation**: Document semantics clearly; recommend bounded ranges;
+    preserve deterministic ordering.
+
+- **Risk**: Annotation value is invalid or out of expected range.
+  - **Mitigation**: Treat invalid/unparsable values as zero, emit a warning
+    Event on the Workload, and log the issue.
+
+## Design Details
+
+### API changes
+
+Add a new optional annotation on `Workload`:
+
+- `kueue.x-k8s.io/priority-boost: "<int>"`
+  - **Semantics**: signed integer that adjusts effective priority. Positive
+    values increase priority (harder to preempt, scheduled sooner); negative
+    values decrease it.
+  - **Unset**: equivalent to zero.
+  - **Applicability**: Priority boost applies regardless of how the Workload's
+    base priority is derived (Pod PriorityClass, WorkloadPriorityClass, or
+    custom logic in an integration helper). Boost also applies when no
+    PriorityClass is specified (base priority 0); effective priority is then
+    `0 + priorityBoost`.
+  - **Alpha behavior**: if the value is invalid/unparsable, it is treated as
+    zero, a warning Event is emitted on the Workload, and the issue is logged.
+
+The annotation is set directly on the `Workload` object by an external
+controller (not propagated from Jobs).
+
+YAML example (`Workload`):
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: Workload
+metadata:
+  name: example-workload
+  namespace: default
+  annotations:
+    # Positive values increase effective priority. Unset means 0.
+    kueue.x-k8s.io/priority-boost: "100"
+spec:
+  priorityClassName: medium
+  # ... rest of spec
+```
+
+### Validation
+
+When the `kueue.x-k8s.io/priority-boost` annotation is set or updated on a
+Workload, Kueue validates that the value is a valid signed integer. Invalid or
+unparsable values are treated as zero, a warning Event is emitted on the
+Workload, and the issue is logged (see API changes above).
```

  Let's see, the annotation will be set to an already existing Workload object, but I agree we could just reject the request. Say someone wants to change "10" -> "Abc". We can just reject the request rather than setting to "0".

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-03-04T14:29:03Z

LGTM, Thank you!
/lgtm
/approve

/hold cancel
