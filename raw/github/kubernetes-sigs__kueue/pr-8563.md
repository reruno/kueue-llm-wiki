# PR #8563: implementation of tls min version with string api and validation from k8s

**Summary**: implementation of tls min version with string api and validation from k8s

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8563

**Last updated**: 2026-01-26T12:23:25Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kannon92](https://github.com/kannon92)
- **Branch**: `tls-min-version-string-api` → `main`
- **Created**: 2026-01-13T16:20:41Z
- **Updated**: 2026-01-26T12:23:25Z
- **Merged**: 2026-01-23T15:25:28Z
- **Closed**: 2026-01-23T15:25:28Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@kshalot](https://github.com/kshalot), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi), [@gabesaba](https://github.com/gabesaba), [@kshalot](https://github.com/kshalot), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 19   **+1269 / -106**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add support for TLSMinVersion and CipherSuites to Kueue.
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #8190 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Security: Support customization (TLSMinVersion and CipherSuites) for TLS used by the Kueue's webhooks server,
and the visibility server.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-01-13T16:20:48Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | aef39ef2022511a19d1a7b5efbc4b855486f8e2b |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69738bb5ea1dd500089ab17b |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-8563--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-8563--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTg1NjMtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.ZXx5vYVnfymxGhcHCVDApyy-EwD-4Y5fYLmBStnWL4E)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-8563--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@kannon92](https://github.com/kannon92) — 2026-01-16T18:21:11Z

/assign @tenzen-y @mimowo @kshalot 

This is ready for review given that the KEP merged.

### Comment by [@kannon92](https://github.com/kannon92) — 2026-01-19T20:40:56Z

@kshalot @mimowo 

I consolidated the config parsing, generated a single struct and passed that to metrics and visibility.

Right now webhooks is still parsed separately as that seems to be handled differently than metrics/visibility right now.

I also outputted the preffered TLS settings.

I am still torn on logging if an insecure ciphersuite is used. Its a valid setting for Kubernetes so maybe someone has a reason why they use it.

### Comment by [@kshalot](https://github.com/kshalot) — 2026-01-22T10:09:11Z

/lgtm

Thanks for the changes! I left some last comments after consulting with @mimowo, but the core functionality is solid IMO.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-22T10:09:19Z

LGTM label has been added.  <details>Git tree hash: f7f50764bf2e89ebcf93229689b7bc32bb6252b4</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-22T10:39:31Z

I think the PR is almost ready to go 0.16.0 (planned for Monday next week). I would like to just wait a bit to hopefully get the remaining comments addressed

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-23T08:20:18Z

LGTM label has been added.  <details>Git tree hash: a225c06cc49b8a0a728e1e64b6d2328577173126</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-23T08:20:21Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8563#" title="Author self-approved">kannon92</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8563#pullrequestreview-3696257832" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-23T08:23:02Z

Slightly tweaking, proposal:
/release-note-edit
```release-note
Security: Support customization (TLSMinVersion and CipherSuites) for TLS used by the Kueue's webhooks server,
and the visibility server. 
```
Feel free to adjust

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-23T08:59:29Z

@kannon92 please re-genearete the information about feature gates after https://github.com/kubernetes-sigs/kueue/pull/7927 is merged

### Comment by [@kannon92](https://github.com/kannon92) — 2026-01-23T14:58:23Z

> @kannon92 please re-genearete the information about feature gates after #7927 is merged

Updated.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-23T15:23:23Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-23T15:23:31Z

LGTM label has been added.  <details>Git tree hash: 9010cff11a67b91afa825be85a7137a57bfe348e</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-26T12:23:23Z

/kind feature

## Reviews

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-14T15:02:27Z

- **apis/config/v1beta2/configuration_types.go:382** by [@kshalot](https://github.com/kshalot) — 2026-01-14T14:39:52Z
```diff
@@ -370,6 +375,22 @@ type InternalCertManagement struct {
 	WebhookSecretName *string `json:"webhookSecretName,omitempty"`
 }
 
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// tlsMinVersion is the minimum TLS version supported.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// Default: ""
```

  While the value itself will default to "", maybe it would be good to explain what the default _behaviour_ will be as well, i.e. here it would be TLS 1.2.

- **pkg/util/tlsconfig/tlsconfig.go:44** by [@kshalot](https://github.com/kshalot) — 2026-01-14T14:44:51Z
```diff
@@ -0,0 +1,87 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tlsconfig
+
+import (
+	"crypto/tls"
+	"fmt"
+
+	cliflag "k8s.io/component-base/cli/flag"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+)
+
+// BuildTLSOptions converts TLSOptions from the configuration to controller-runtime TLSOpts
+func BuildTLSOptions(cfg *config.TLSOptions) ([]func(*tls.Config), error) {
+	if cfg == nil {
+		return nil, nil
+	}
+
+	var tlsOpts []func(*tls.Config)
+
+	tlsOpts = append(tlsOpts, func(c *tls.Config) {
+		// Set minimum TLS version
+		if cfg.TLSMinVersion != "" {
+			version, err := ConvertTLSMinVersion(cfg.TLSMinVersion)
+			if err == nil {
+				c.MinVersion = version
+			} else {
+				// Default to TLS 1.2 on error
+				c.MinVersion = tls.VersionTLS12
```

  There are 4 places where a default is set to 1.2 - we can consider making it a const `DefaultTLSVersion`/`defaultTLSVersion`.

- **pkg/util/tlsconfig/tlsconfig.go:44** by [@kshalot](https://github.com/kshalot) — 2026-01-14T14:56:20Z
```diff
@@ -0,0 +1,87 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tlsconfig
+
+import (
+	"crypto/tls"
+	"fmt"
+
+	cliflag "k8s.io/component-base/cli/flag"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+)
+
+// BuildTLSOptions converts TLSOptions from the configuration to controller-runtime TLSOpts
+func BuildTLSOptions(cfg *config.TLSOptions) ([]func(*tls.Config), error) {
+	if cfg == nil {
+		return nil, nil
+	}
+
+	var tlsOpts []func(*tls.Config)
+
+	tlsOpts = append(tlsOpts, func(c *tls.Config) {
+		// Set minimum TLS version
+		if cfg.TLSMinVersion != "" {
+			version, err := ConvertTLSMinVersion(cfg.TLSMinVersion)
+			if err == nil {
+				c.MinVersion = version
+			} else {
+				// Default to TLS 1.2 on error
+				c.MinVersion = tls.VersionTLS12
```

  Or in general, we could have a single function which converts the API `config.TLSOptions` into a structure like:
  ```go
  type Config struct {
      MinVersion uint16
      CipherSuites []uint16
  }
  
  func ParseTLSOptions(cfg *config.TLSOptions) (Config, error)
  ```
  
  Then validation and setting the defaults would happen inside `ParseTLSOptions`. The visibility server could use that directly, while the metrics and webhook would require a function to convert `Config` into `[]func(*tls.Config)`.

- **cmd/kueue/main.go:321** by [@kshalot](https://github.com/kshalot) — 2026-01-14T15:01:52Z
```diff
@@ -307,7 +318,7 @@ func main() {
 
 	if features.Enabled(features.VisibilityOnDemand) {
 		go func() {
-			if err := visibility.CreateAndStartVisibilityServer(ctx, queues, *cfg.InternalCertManagement.Enable, kubeConfig); err != nil {
+			if err := visibility.CreateAndStartVisibilityServer(ctx, queues, *cfg.InternalCertManagement.Enable, kubeConfig, cfg.TLS); err != nil {
```

  Instead of passing `cfg.TLS` we could pass TLS configuration which is already parsed and validated. Then we have one place where the conversion happens in `main` and flows further down.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-14T16:20:57Z

- **apis/config/v1beta2/configuration_types.go:382** by [@kannon92](https://github.com/kannon92) — 2026-01-14T16:20:57Z
```diff
@@ -370,6 +375,22 @@ type InternalCertManagement struct {
 	WebhookSecretName *string `json:"webhookSecretName,omitempty"`
 }
 
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// tlsMinVersion is the minimum TLS version supported.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// Default: ""
```

  I'll update this but the default may change on future golang versions. So in reality it is set to whatever the golang version we build on.
  
  Right now it is TLS 1.2 but who knows what it will be in 5 years.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-14T16:21:24Z

- **apis/config/v1beta2/configuration_types.go:382** by [@kannon92](https://github.com/kannon92) — 2026-01-14T16:21:24Z
```diff
@@ -370,6 +375,22 @@ type InternalCertManagement struct {
 	WebhookSecretName *string `json:"webhookSecretName,omitempty"`
 }
 
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// tlsMinVersion is the minimum TLS version supported.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// Default: ""
```

  By nil I was meaning that we would not set TLSOptions explictly and just rely on golang defaults for these settings.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-16T01:12:51Z

- **test/e2e/config/default/controller_manager_config.yaml:42** by [@kannon92](https://github.com/kannon92) — 2026-01-16T01:12:51Z
```diff
@@ -38,3 +38,13 @@ integrations:
 featureGates:
   LocalQueueMetrics: true
   ElasticJobsViaWorkloadSlices: true
+tls:
+  tlsMinVersion: 1.2
```

  This may be controversial.
  
  I was thinking I can exercise this via a e2e test by setting these values to match the golang defaults.
  
  I want the full e2e suite to run because I want to verify visibility/metrics/controllers. So I don't know if custom config is a great fit as I have to replicate all the singlecluster tests. 
  
  I can do that if need be though.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-16T01:15:21Z

- **cmd/kueue/main.go:321** by [@kannon92](https://github.com/kannon92) — 2026-01-16T01:15:21Z
```diff
@@ -307,7 +318,7 @@ func main() {
 
 	if features.Enabled(features.VisibilityOnDemand) {
 		go func() {
-			if err := visibility.CreateAndStartVisibilityServer(ctx, queues, *cfg.InternalCertManagement.Enable, kubeConfig); err != nil {
+			if err := visibility.CreateAndStartVisibilityServer(ctx, queues, *cfg.InternalCertManagement.Enable, kubeConfig, cfg.TLS); err != nil {
```

  I don't really know if this buys us much.
  
  For metrics we need a function of tls options.
  
  For visibility I have to parse and get each value to pass into visibilityServer. I still have two different ways to get this info so I prefer the change I have.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-16T01:16:14Z

- **apis/config/v1beta2/configuration_types.go:382** by [@kannon92](https://github.com/kannon92) — 2026-01-16T01:16:14Z
```diff
@@ -370,6 +375,22 @@ type InternalCertManagement struct {
 	WebhookSecretName *string `json:"webhookSecretName,omitempty"`
 }
 
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// tlsMinVersion is the minimum TLS version supported.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// Default: ""
```

  Pushed an update.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-16T01:16:54Z

- **pkg/util/tlsconfig/tlsconfig.go:44** by [@kannon92](https://github.com/kannon92) — 2026-01-16T01:16:54Z
```diff
@@ -0,0 +1,87 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tlsconfig
+
+import (
+	"crypto/tls"
+	"fmt"
+
+	cliflag "k8s.io/component-base/cli/flag"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+)
+
+// BuildTLSOptions converts TLSOptions from the configuration to controller-runtime TLSOpts
+func BuildTLSOptions(cfg *config.TLSOptions) ([]func(*tls.Config), error) {
+	if cfg == nil {
+		return nil, nil
+	}
+
+	var tlsOpts []func(*tls.Config)
+
+	tlsOpts = append(tlsOpts, func(c *tls.Config) {
+		// Set minimum TLS version
+		if cfg.TLSMinVersion != "" {
+			version, err := ConvertTLSMinVersion(cfg.TLSMinVersion)
+			if err == nil {
+				c.MinVersion = version
+			} else {
+				// Default to TLS 1.2 on error
+				c.MinVersion = tls.VersionTLS12
```

  I made this a constant.
  
  I still don't know if https://github.com/kubernetes-sigs/kueue/pull/8563#discussion_r2690793248 is worth it. We have two places we need to do this and each place has a different shape for the TLS options.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-19T10:43:43Z

- **apis/config/v1beta1/configuration_types.go:402** by [@kshalot](https://github.com/kshalot) — 2026-01-16T11:18:23Z
```diff
@@ -379,6 +384,23 @@ type InternalCertManagement struct {
 	WebhookSecretName *string `json:"webhookSecretName,omitempty"`
 }
 
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// tlsMinVersion is the minimum TLS version supported.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// This field is only valid when TLSOptions is set to true.
+	// The default would be to not set this value and inherit golang settings.
+	// +optional
+	TLSMinVersion string `json:"tlsMinVersion,omitempty"`
+
+	// tlsCipherSuites is the list of allowed cipher suites for the server.
+	// Note that TLS 1.3 ciphersuites are not configurable.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// The default would be to not set this value and inherit golang settings.
+	// +optional
+	TLSCipherSuites []string `json:"tlsCipherSuites,omitempty"`
+}
```

  Now that I look at it, do the fields need the "TLS" prefix? It should be clear from the context of being inside a struct called `TLSOptions`. Now we have to write:
  ```go
  c.TLS.TLSCipherSuites
  ```
  versus:
  ```go
  c.TLS.CipherSuites
  ```

- **pkg/config/validation.go:535** by [@kshalot](https://github.com/kshalot) — 2026-01-19T10:09:42Z
```diff
@@ -528,3 +530,17 @@ func validateObjectRetentionPolicies(c *configapi.Configuration) field.ErrorList
 	}
 	return allErrs
 }
+
+func validateTLS(c *configapi.Configuration) field.ErrorList {
```

  Logging a warning when using an insecure cipher suite is a pretty low hanging fruit since `InsecureTLSCiphers` is in `cliflag`.

- **pkg/util/tlsconfig/tlsconfig.go:37** by [@kshalot](https://github.com/kshalot) — 2026-01-19T10:34:10Z
```diff
@@ -0,0 +1,96 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tlsconfig
+
+import (
+	"crypto/tls"
+	"fmt"
+
+	cliflag "k8s.io/component-base/cli/flag"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	"sigs.k8s.io/kueue/pkg/features"
+)
+
+const TLS12 = tls.VersionTLS12
+
+// BuildTLSOptions converts TLSOptions from the configuration to controller-runtime TLSOpts
+// If the TLSOptions feature gate is disabled, it returns nil.
+func BuildTLSOptions(cfg *config.TLSOptions) ([]func(*tls.Config), error) {
+	// If feature gate is disabled, return nil to use golang defaults
+	if !features.Enabled(features.TLSOptions) {
+		return nil, nil
+	}
```

  Nit: I'd consider moving the feature gate check to the `if` statements that are parsing the config. It will completely shut off any new logic from being executed when the gate is disabled (all the `if cfg.TLS != nil` checks would become `if cfg.TLS != nil && features.Enabled(...)`).

- **cmd/kueue/main.go:321** by [@kshalot](https://github.com/kshalot) — 2026-01-19T10:41:18Z
```diff
@@ -307,7 +318,7 @@ func main() {
 
 	if features.Enabled(features.VisibilityOnDemand) {
 		go func() {
-			if err := visibility.CreateAndStartVisibilityServer(ctx, queues, *cfg.InternalCertManagement.Enable, kubeConfig); err != nil {
+			if err := visibility.CreateAndStartVisibilityServer(ctx, queues, *cfg.InternalCertManagement.Enable, kubeConfig, cfg.TLS); err != nil {
```

  It would keep the error handling in a single place. For example, right now in the main function there program exits if the parsing fails:
  ```go
  		tlsOpts, err := tlsconfig.BuildTLSOptions(cfg.TLS)
  		if err != nil {
  			setupLog.Error(err, "Unable to build TLS options from configuration")
  			os.Exit(1)
  		}
  ```
  in the webhook server it does not:
  ```go
  			tlsOpts, err := tlsconfig.BuildTLSOptions(cfg.TLS)
  			if err == nil {
  				wo.TLSOpts = append(wo.TLSOpts, tlsOpts...)
  			}
  ```
  and the visibility config returns an error. So the TLS options are parsed three times in the code and the error is handled every time in some way.
  
  It works consistently because the `os.Exit(1)` is executed first if parsing fails, so assuming that `tlsconfig.BuildTLSOptions` is deterministic (which it is), then it will always just fail at that point. But it makes it a bit harder to reason about IMO.
  
  Parsing once hinges on adding a new structure though (https://github.com/kubernetes-sigs/kueue/pull/8563#discussion_r2690793248) for parsed TLS, which would be the 4th TLS specific struct in the code so there is some tradeoff.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-19T14:44:13Z

- **apis/config/v1beta1/configuration_types.go:402** by [@kannon92](https://github.com/kannon92) — 2026-01-19T14:44:13Z
```diff
@@ -379,6 +384,23 @@ type InternalCertManagement struct {
 	WebhookSecretName *string `json:"webhookSecretName,omitempty"`
 }
 
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// tlsMinVersion is the minimum TLS version supported.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// This field is only valid when TLSOptions is set to true.
+	// The default would be to not set this value and inherit golang settings.
+	// +optional
+	TLSMinVersion string `json:"tlsMinVersion,omitempty"`
+
+	// tlsCipherSuites is the list of allowed cipher suites for the server.
+	// Note that TLS 1.3 ciphersuites are not configurable.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// The default would be to not set this value and inherit golang settings.
+	// +optional
+	TLSCipherSuites []string `json:"tlsCipherSuites,omitempty"`
+}
```

  I took the name from our kubernetes configs actually.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-19T14:45:02Z

- **apis/config/v1beta1/configuration_types.go:402** by [@kannon92](https://github.com/kannon92) — 2026-01-19T14:45:02Z
```diff
@@ -379,6 +384,23 @@ type InternalCertManagement struct {
 	WebhookSecretName *string `json:"webhookSecretName,omitempty"`
 }
 
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// tlsMinVersion is the minimum TLS version supported.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// This field is only valid when TLSOptions is set to true.
+	// The default would be to not set this value and inherit golang settings.
+	// +optional
+	TLSMinVersion string `json:"tlsMinVersion,omitempty"`
+
+	// tlsCipherSuites is the list of allowed cipher suites for the server.
+	// Note that TLS 1.3 ciphersuites are not configurable.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// The default would be to not set this value and inherit golang settings.
+	// +optional
+	TLSCipherSuites []string `json:"tlsCipherSuites,omitempty"`
+}
```

  https://github.com/kubernetes/kubernetes/blob/master/staging/src/k8s.io/kubelet/config/v1beta1/types.go#L182

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-19T15:11:34Z

- **apis/config/v1beta1/configuration_types.go:402** by [@kshalot](https://github.com/kshalot) — 2026-01-19T15:11:34Z
```diff
@@ -379,6 +384,23 @@ type InternalCertManagement struct {
 	WebhookSecretName *string `json:"webhookSecretName,omitempty"`
 }
 
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// tlsMinVersion is the minimum TLS version supported.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// This field is only valid when TLSOptions is set to true.
+	// The default would be to not set this value and inherit golang settings.
+	// +optional
+	TLSMinVersion string `json:"tlsMinVersion,omitempty"`
+
+	// tlsCipherSuites is the list of allowed cipher suites for the server.
+	// Note that TLS 1.3 ciphersuites are not configurable.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// The default would be to not set this value and inherit golang settings.
+	// +optional
+	TLSCipherSuites []string `json:"tlsCipherSuites,omitempty"`
+}
```

  True. On the other hand the difference is that the kubelet structure is "flat" and ours has the "tls: {...}" nesting

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-19T16:03:50Z

- **pkg/util/tlsconfig/tlsconfig_test.go:187** by [@mimowo](https://github.com/mimowo) — 2026-01-19T16:03:50Z
```diff
@@ -0,0 +1,291 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tlsconfig
+
+import (
+	"crypto/tls"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	"sigs.k8s.io/kueue/pkg/features"
+)
+
+func TestBuildTLSOptions(t *testing.T) {
+	tests := []struct {
+		name             string
+		cfg              *config.TLSOptions
+		featureGateValue *bool
+		expectedMinVer   uint16
+		expectedCipher   []uint16
+		expectNil        bool
+		wantErr          bool
+	}{
+		{
+			name:           "nil config",
+			cfg:            nil,
+			expectedMinVer: 0,
+			expectedCipher: nil,
+			expectNil:      true,
+			wantErr:        false,
+		},
+		{
+			name:           "empty config",
+			cfg:            &config.TLSOptions{},
+			expectedMinVer: tls.VersionTLS12,
+			expectedCipher: nil,
+			wantErr:        false,
+		},
+		{
+			name: "TLS 1.2",
+			cfg: &config.TLSOptions{
+				TLSMinVersion: "VersionTLS12",
+			},
+			expectedMinVer: tls.VersionTLS12,
+			expectedCipher: nil,
+			wantErr:        false,
+		},
+		{
+			name: "TLS 1.3",
+			cfg: &config.TLSOptions{
+				TLSMinVersion: "VersionTLS13",
+			},
+			expectedMinVer: tls.VersionTLS13,
+			expectedCipher: nil,
+			wantErr:        false,
+		},
+		{
+			name: "with cipher suites",
+			cfg: &config.TLSOptions{
+				TLSMinVersion: "VersionTLS12",
+				TLSCipherSuites: []string{
+					"TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256",
+					"TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384",
+				},
+			},
+			expectedMinVer: tls.VersionTLS12,
+			expectedCipher: []uint16{
+				tls.TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256,
+				tls.TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384,
+			},
+			wantErr: false,
+		},
+		{
+			name: "full config",
+			cfg: &config.TLSOptions{
+				TLSMinVersion: "VersionTLS13",
+				TLSCipherSuites: []string{
+					"TLS_AES_128_GCM_SHA256",
+					"TLS_AES_256_GCM_SHA384",
+				},
+			},
+			expectedMinVer: tls.VersionTLS13,
+			expectedCipher: []uint16{
+				tls.TLS_AES_128_GCM_SHA256,
+				tls.TLS_AES_256_GCM_SHA384,
+			},
+			wantErr: false,
+		},
+		{
+			name:             "feature gate disabled with nil config",
+			cfg:              nil,
+			featureGateValue: ptrBool(false),
+			expectNil:        true,
+			wantErr:          false,
+		},
+		{
+			name: "feature gate disabled with valid config",
+			cfg: &config.TLSOptions{
+				TLSMinVersion: "VersionTLS13",
+				TLSCipherSuites: []string{
+					"TLS_AES_128_GCM_SHA256",
+				},
+			},
+			featureGateValue: ptrBool(false),
+			expectNil:        true,
+			wantErr:          false,
+		},
+		{
+			name:             "feature gate enabled with nil config",
+			cfg:              nil,
+			featureGateValue: ptrBool(true),
+			expectNil:        true,
+			wantErr:          false,
+		},
+		{
+			name:             "feature gate enabled with empty config",
+			cfg:              &config.TLSOptions{},
+			featureGateValue: ptrBool(true),
+			expectedMinVer:   tls.VersionTLS12,
+			expectedCipher:   nil,
+			wantErr:          false,
+		},
+		{
+			name: "feature gate enabled with TLS 1.3",
+			cfg: &config.TLSOptions{
+				TLSMinVersion: "VersionTLS13",
+			},
+			featureGateValue: ptrBool(true),
+			expectedMinVer:   tls.VersionTLS13,
+			expectedCipher:   nil,
+			wantErr:          false,
+		},
+	}
+
+	for _, tt := range tests {
+		t.Run(tt.name, func(t *testing.T) {
+			// Set the feature gate if specified
+			if tt.featureGateValue != nil {
+				features.SetFeatureGateDuringTest(t, features.TLSOptions, *tt.featureGateValue)
+			}
+
+			tlsOpts, err := BuildTLSOptions(tt.cfg)
+			if (err != nil) != tt.wantErr {
+				t.Errorf("BuildTLSOptions() error = %v, wantErr %v", err, tt.wantErr)
+				return
+			}
+
+			if tt.expectNil {
+				if tlsOpts != nil {
+					t.Errorf("BuildTLSOptions() returned non-nil, want nil")
+				}
+				return
+			}
+
+			// Apply the options to a test TLS config
+			testConfig := &tls.Config{}
+			for _, opt := range tlsOpts {
+				opt(testConfig)
+			}
+
+			if testConfig.MinVersion != tt.expectedMinVer {
+				t.Errorf("MinVersion = %v, want %v", testConfig.MinVersion, tt.expectedMinVer)
+			}
+
+			if !cmp.Equal(testConfig.CipherSuites, tt.expectedCipher) {
+				t.Errorf("CipherSuites diff: %s", cmp.Diff(tt.expectedCipher, testConfig.CipherSuites))
+			}
+		})
+	}
+}
+
+func ptrBool(b bool) *bool {
```

  nit, but you could replace that ptr.To(true/false)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-19T16:05:58Z

- **pkg/features/kube_features.go:368** by [@mimowo](https://github.com/mimowo) — 2026-01-19T16:05:58Z
```diff
@@ -359,9 +365,14 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	MultiKueueWaitForWorkloadAdmitted: {
 		{Version: version.MustParse("0.16"), Default: true, PreRelease: featuregate.Beta}, // GA in 0.18
 	},
+
```

  nit, revert

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-19T16:06:14Z

- **pkg/features/kube_features.go:237** by [@mimowo](https://github.com/mimowo) — 2026-01-19T16:06:14Z
```diff
@@ -231,6 +231,12 @@ const (
 	// issue: https://github.com/kubernetes-sigs/kueue/issues/8302
 	// Redo admission on eviction in worker cluster.
 	MultiKueueRedoAdmissionOnEvictionInWorker featuregate.Feature = "MultiKueueRedoAdmissionOnEvictionInWorker"
+
+	// owner: @kannon92
+	//
+	// issue:https://github.com/kubernetes-sigs/kueue/issues/8190
```

  ```suggestion
  	// issue: https://github.com/kubernetes-sigs/kueue/issues/8190
  ```
  nit

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-19T16:07:02Z

- **cmd/kueue/main.go:175** by [@mimowo](https://github.com/mimowo) — 2026-01-19T16:06:59Z
```diff
@@ -170,6 +171,16 @@ func main() {
 		FilterProvider: filters.WithAuthenticationAndAuthorization,
 	}
 
+	// Apply TLS configuration from config
+	if cfg.TLS != nil {
```

  Check the TLSOptions feature gate here too

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-19T16:09:41Z

- **pkg/config/config.go:103** by [@mimowo](https://github.com/mimowo) — 2026-01-19T16:09:42Z
```diff
@@ -96,6 +97,15 @@ func addTo(o *ctrl.Options, cfg *configapi.Configuration) {
 		if cfg.Webhook.CertDir != "" {
 			wo.CertDir = cfg.Webhook.CertDir
 		}
+
+		// Apply TLS configuration if provided
+		if cfg.TLS != nil {
+			tlsOpts, err := tlsconfig.BuildTLSOptions(cfg.TLS)
```

  Interesting, I see this PR is checking the FG inside BuildTLSOptions, but I think it will not be respected, because the feature gates can be set by the config itself. Are you sure this is working fine? 
  
  I would expect this conversion happens after the config itself is parsed, and be done once.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-19T16:10:37Z

- **pkg/visibility/server.go:122** by [@mimowo](https://github.com/mimowo) — 2026-01-19T16:10:37Z
```diff
@@ -113,7 +116,26 @@ func applyVisibilityServerOptions(config *genericapiserver.RecommendedConfig, en
 	if err := o.SecureServing.MaybeDefaultWithSelfSignedCerts("localhost", nil, []net.IP{net.ParseIP("127.0.0.1")}); err != nil {
 		return fmt.Errorf("error creating self-signed certificates: %v", err)
 	}
-	return o.ApplyTo(config)
+
+	if err := o.ApplyTo(config); err != nil {
+		return err
+	}
+
+	// Only apply TLS options if the feature gate is enabled
```

  This parsing code seems like done second time. Could we parse and validate once, and then pass around already parsed TLS config?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-19T16:12:56Z

- **pkg/util/tlsconfig/tlsconfig.go:93** by [@mimowo](https://github.com/mimowo) — 2026-01-19T16:12:56Z
```diff
@@ -0,0 +1,96 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tlsconfig
+
+import (
+	"crypto/tls"
+	"fmt"
+
+	cliflag "k8s.io/component-base/cli/flag"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	"sigs.k8s.io/kueue/pkg/features"
+)
+
+const TLS12 = tls.VersionTLS12
+
+// BuildTLSOptions converts TLSOptions from the configuration to controller-runtime TLSOpts
+// If the TLSOptions feature gate is disabled, it returns nil.
+func BuildTLSOptions(cfg *config.TLSOptions) ([]func(*tls.Config), error) {
+	// If feature gate is disabled, return nil to use golang defaults
+	if !features.Enabled(features.TLSOptions) {
+		return nil, nil
+	}
+
+	if cfg == nil {
+		return nil, nil
+	}
+
+	var tlsOpts []func(*tls.Config)
+
+	tlsOpts = append(tlsOpts, func(c *tls.Config) {
+		// Set minimum TLS version
+		if cfg.TLSMinVersion != "" {
+			version, err := ConvertTLSMinVersion(cfg.TLSMinVersion)
+			if err == nil {
+				c.MinVersion = version
+			} else {
+				// Default to TLS 1.2 on error
+				c.MinVersion = TLS12
+			}
+		} else {
+			// Default to TLS 1.2
+			c.MinVersion = TLS12
+		}
+
+		// Set cipher suites
+		if len(cfg.TLSCipherSuites) > 0 {
+			cipherSuites, err := ConvertCipherSuites(cfg.TLSCipherSuites)
+			if err == nil && len(cipherSuites) > 0 {
+				c.CipherSuites = cipherSuites
+			}
+		}
+	})
+
+	return tlsOpts, nil
+}
+
+// ConvertTLSMinVersion converts a TLS version string to the corresponding uint16 constant
+// using k8s.io/component-base/cli/flag for validation
+func ConvertTLSMinVersion(tlsMinVersion string) (uint16, error) {
+	if tlsMinVersion == "" {
+		return TLS12, nil
+	}
+	version, err := cliflag.TLSVersion(tlsMinVersion)
+	if err != nil {
+		return TLS12, fmt.Errorf("invalid TLS version: %w", err)
+	}
+	return version, nil
+}
+
+// ConvertCipherSuites converts cipher suite names to their crypto/tls constants
+// using k8s.io/component-base/cli/flag for validation
+func ConvertCipherSuites(cipherSuites []string) ([]uint16, error) {
+	if len(cipherSuites) == 0 {
+		return nil, nil
+	}
+	suites, err := cliflag.TLSCipherSuites(cipherSuites)
+	if err != nil {
+		return nil, fmt.Errorf("invalid cipher suites: %w", err)
```

  Would you like to maybe output the list of supported and full list of ciphers when the error is throws? THis could be helpful to guide a user, and it seems we already have helpers to do so in https://github.com/kubernetes/kubernetes/blob/master/staging/src/k8s.io/component-base/cli/flag/ciphersuites_flag.go, eg TLSCipherPossibleValues and PreferredTLSCipherNames

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-19T19:57:38Z

- **apis/config/v1beta1/configuration_types.go:402** by [@kannon92](https://github.com/kannon92) — 2026-01-19T19:57:38Z
```diff
@@ -379,6 +384,23 @@ type InternalCertManagement struct {
 	WebhookSecretName *string `json:"webhookSecretName,omitempty"`
 }
 
+// TLSOptions defines TLS security settings for Kueue servers
+type TLSOptions struct {
+	// tlsMinVersion is the minimum TLS version supported.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// This field is only valid when TLSOptions is set to true.
+	// The default would be to not set this value and inherit golang settings.
+	// +optional
+	TLSMinVersion string `json:"tlsMinVersion,omitempty"`
+
+	// tlsCipherSuites is the list of allowed cipher suites for the server.
+	// Note that TLS 1.3 ciphersuites are not configurable.
+	// Values are from tls package constants (https://golang.org/pkg/crypto/tls/#pkg-constants).
+	// The default would be to not set this value and inherit golang settings.
+	// +optional
+	TLSCipherSuites []string `json:"tlsCipherSuites,omitempty"`
+}
```

  Updated this.
  
  Now we have tls as top level and I dropped tls as a prefix.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-19T19:58:18Z

- **pkg/util/tlsconfig/tlsconfig.go:37** by [@kannon92](https://github.com/kannon92) — 2026-01-19T19:58:18Z
```diff
@@ -0,0 +1,96 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tlsconfig
+
+import (
+	"crypto/tls"
+	"fmt"
+
+	cliflag "k8s.io/component-base/cli/flag"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	"sigs.k8s.io/kueue/pkg/features"
+)
+
+const TLS12 = tls.VersionTLS12
+
+// BuildTLSOptions converts TLSOptions from the configuration to controller-runtime TLSOpts
+// If the TLSOptions feature gate is disabled, it returns nil.
+func BuildTLSOptions(cfg *config.TLSOptions) ([]func(*tls.Config), error) {
+	// If feature gate is disabled, return nil to use golang defaults
+	if !features.Enabled(features.TLSOptions) {
+		return nil, nil
+	}
```

  I believe I addressed this.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-19T19:58:37Z

- **pkg/util/tlsconfig/tlsconfig_test.go:187** by [@kannon92](https://github.com/kannon92) — 2026-01-19T19:58:37Z
```diff
@@ -0,0 +1,291 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tlsconfig
+
+import (
+	"crypto/tls"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	"sigs.k8s.io/kueue/pkg/features"
+)
+
+func TestBuildTLSOptions(t *testing.T) {
+	tests := []struct {
+		name             string
+		cfg              *config.TLSOptions
+		featureGateValue *bool
+		expectedMinVer   uint16
+		expectedCipher   []uint16
+		expectNil        bool
+		wantErr          bool
+	}{
+		{
+			name:           "nil config",
+			cfg:            nil,
+			expectedMinVer: 0,
+			expectedCipher: nil,
+			expectNil:      true,
+			wantErr:        false,
+		},
+		{
+			name:           "empty config",
+			cfg:            &config.TLSOptions{},
+			expectedMinVer: tls.VersionTLS12,
+			expectedCipher: nil,
+			wantErr:        false,
+		},
+		{
+			name: "TLS 1.2",
+			cfg: &config.TLSOptions{
+				TLSMinVersion: "VersionTLS12",
+			},
+			expectedMinVer: tls.VersionTLS12,
+			expectedCipher: nil,
+			wantErr:        false,
+		},
+		{
+			name: "TLS 1.3",
+			cfg: &config.TLSOptions{
+				TLSMinVersion: "VersionTLS13",
+			},
+			expectedMinVer: tls.VersionTLS13,
+			expectedCipher: nil,
+			wantErr:        false,
+		},
+		{
+			name: "with cipher suites",
+			cfg: &config.TLSOptions{
+				TLSMinVersion: "VersionTLS12",
+				TLSCipherSuites: []string{
+					"TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256",
+					"TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384",
+				},
+			},
+			expectedMinVer: tls.VersionTLS12,
+			expectedCipher: []uint16{
+				tls.TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256,
+				tls.TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384,
+			},
+			wantErr: false,
+		},
+		{
+			name: "full config",
+			cfg: &config.TLSOptions{
+				TLSMinVersion: "VersionTLS13",
+				TLSCipherSuites: []string{
+					"TLS_AES_128_GCM_SHA256",
+					"TLS_AES_256_GCM_SHA384",
+				},
+			},
+			expectedMinVer: tls.VersionTLS13,
+			expectedCipher: []uint16{
+				tls.TLS_AES_128_GCM_SHA256,
+				tls.TLS_AES_256_GCM_SHA384,
+			},
+			wantErr: false,
+		},
+		{
+			name:             "feature gate disabled with nil config",
+			cfg:              nil,
+			featureGateValue: ptrBool(false),
+			expectNil:        true,
+			wantErr:          false,
+		},
+		{
+			name: "feature gate disabled with valid config",
+			cfg: &config.TLSOptions{
+				TLSMinVersion: "VersionTLS13",
+				TLSCipherSuites: []string{
+					"TLS_AES_128_GCM_SHA256",
+				},
+			},
+			featureGateValue: ptrBool(false),
+			expectNil:        true,
+			wantErr:          false,
+		},
+		{
+			name:             "feature gate enabled with nil config",
+			cfg:              nil,
+			featureGateValue: ptrBool(true),
+			expectNil:        true,
+			wantErr:          false,
+		},
+		{
+			name:             "feature gate enabled with empty config",
+			cfg:              &config.TLSOptions{},
+			featureGateValue: ptrBool(true),
+			expectedMinVer:   tls.VersionTLS12,
+			expectedCipher:   nil,
+			wantErr:          false,
+		},
+		{
+			name: "feature gate enabled with TLS 1.3",
+			cfg: &config.TLSOptions{
+				TLSMinVersion: "VersionTLS13",
+			},
+			featureGateValue: ptrBool(true),
+			expectedMinVer:   tls.VersionTLS13,
+			expectedCipher:   nil,
+			wantErr:          false,
+		},
+	}
+
+	for _, tt := range tests {
+		t.Run(tt.name, func(t *testing.T) {
+			// Set the feature gate if specified
+			if tt.featureGateValue != nil {
+				features.SetFeatureGateDuringTest(t, features.TLSOptions, *tt.featureGateValue)
+			}
+
+			tlsOpts, err := BuildTLSOptions(tt.cfg)
+			if (err != nil) != tt.wantErr {
+				t.Errorf("BuildTLSOptions() error = %v, wantErr %v", err, tt.wantErr)
+				return
+			}
+
+			if tt.expectNil {
+				if tlsOpts != nil {
+					t.Errorf("BuildTLSOptions() returned non-nil, want nil")
+				}
+				return
+			}
+
+			// Apply the options to a test TLS config
+			testConfig := &tls.Config{}
+			for _, opt := range tlsOpts {
+				opt(testConfig)
+			}
+
+			if testConfig.MinVersion != tt.expectedMinVer {
+				t.Errorf("MinVersion = %v, want %v", testConfig.MinVersion, tt.expectedMinVer)
+			}
+
+			if !cmp.Equal(testConfig.CipherSuites, tt.expectedCipher) {
+				t.Errorf("CipherSuites diff: %s", cmp.Diff(tt.expectedCipher, testConfig.CipherSuites))
+			}
+		})
+	}
+}
+
+func ptrBool(b bool) *bool {
```

  removed this actually.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-19T19:58:52Z

- **pkg/features/kube_features.go:368** by [@kannon92](https://github.com/kannon92) — 2026-01-19T19:58:52Z
```diff
@@ -359,9 +365,14 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	MultiKueueWaitForWorkloadAdmitted: {
 		{Version: version.MustParse("0.16"), Default: true, PreRelease: featuregate.Beta}, // GA in 0.18
 	},
+
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-19T19:59:06Z

- **pkg/features/kube_features.go:237** by [@kannon92](https://github.com/kannon92) — 2026-01-19T19:59:05Z
```diff
@@ -231,6 +231,12 @@ const (
 	// issue: https://github.com/kubernetes-sigs/kueue/issues/8302
 	// Redo admission on eviction in worker cluster.
 	MultiKueueRedoAdmissionOnEvictionInWorker featuregate.Feature = "MultiKueueRedoAdmissionOnEvictionInWorker"
+
+	// owner: @kannon92
+	//
+	// issue:https://github.com/kubernetes-sigs/kueue/issues/8190
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-19T19:59:18Z

- **cmd/kueue/main.go:175** by [@kannon92](https://github.com/kannon92) — 2026-01-19T19:59:18Z
```diff
@@ -170,6 +171,16 @@ func main() {
 		FilterProvider: filters.WithAuthenticationAndAuthorization,
 	}
 
+	// Apply TLS configuration from config
+	if cfg.TLS != nil {
```

  updated this.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-19T20:00:26Z

- **pkg/config/config.go:103** by [@kannon92](https://github.com/kannon92) — 2026-01-19T20:00:26Z
```diff
@@ -96,6 +97,15 @@ func addTo(o *ctrl.Options, cfg *configapi.Configuration) {
 		if cfg.Webhook.CertDir != "" {
 			wo.CertDir = cfg.Webhook.CertDir
 		}
+
+		// Apply TLS configuration if provided
+		if cfg.TLS != nil {
+			tlsOpts, err := tlsconfig.BuildTLSOptions(cfg.TLS)
```

  I believe this is working.
  
  I tried to address the config parsing once. 
  
  Technically we have parsing twice as config is parsed and then passed into webhooks.
  
  And then we pass those config options to Metrics/Visibility but I think this is the best I can do without changing out we pass in webhook options.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-19T20:00:42Z

- **pkg/visibility/server.go:122** by [@kannon92](https://github.com/kannon92) — 2026-01-19T20:00:41Z
```diff
@@ -113,7 +116,26 @@ func applyVisibilityServerOptions(config *genericapiserver.RecommendedConfig, en
 	if err := o.SecureServing.MaybeDefaultWithSelfSignedCerts("localhost", nil, []net.IP{net.ParseIP("127.0.0.1")}); err != nil {
 		return fmt.Errorf("error creating self-signed certificates: %v", err)
 	}
-	return o.ApplyTo(config)
+
+	if err := o.ApplyTo(config); err != nil {
+		return err
+	}
+
+	// Only apply TLS options if the feature gate is enabled
```

  Yea I applied your suggestions to this file.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-19T20:39:18Z

- **pkg/config/validation.go:535** by [@kannon92](https://github.com/kannon92) — 2026-01-19T20:39:18Z
```diff
@@ -528,3 +530,17 @@ func validateObjectRetentionPolicies(c *configapi.Configuration) field.ErrorList
 	}
 	return allErrs
 }
+
+func validateTLS(c *configapi.Configuration) field.ErrorList {
```

  We could add this.
  
  its just that we don't pass the context to these functions so not sure we can easily log anything in this code.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-19T20:39:42Z

- **cmd/kueue/main.go:321** by [@kannon92](https://github.com/kannon92) — 2026-01-19T20:39:42Z
```diff
@@ -307,7 +318,7 @@ func main() {
 
 	if features.Enabled(features.VisibilityOnDemand) {
 		go func() {
-			if err := visibility.CreateAndStartVisibilityServer(ctx, queues, *cfg.InternalCertManagement.Enable, kubeConfig); err != nil {
+			if err := visibility.CreateAndStartVisibilityServer(ctx, queues, *cfg.InternalCertManagement.Enable, kubeConfig, cfg.TLS); err != nil {
```

  I updated all this.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-20T08:45:03Z

I will finish review later today.

- **cmd/kueue/main.go:174** by [@kshalot](https://github.com/kshalot) — 2026-01-20T08:38:39Z
```diff
@@ -170,6 +171,22 @@ func main() {
 		FilterProvider: filters.WithAuthenticationAndAuthorization,
 	}
 
+	tlsOptions := &tlsconfig.TLS{}
```

  Naming is slightly confusing "opts" vs "options". Maybe `tlsOptions` could become `parsedTlsConfig`

- **pkg/util/tlsconfig/tlsconfig.go:42** by [@kshalot](https://github.com/kshalot) — 2026-01-20T08:44:36Z
```diff
@@ -0,0 +1,106 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tlsconfig
+
+import (
+	"crypto/tls"
+	"fmt"
+
+	cliflag "k8s.io/component-base/cli/flag"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+)
+
+const TLS12 = tls.VersionTLS12
+
+type TLS struct {
+	MinVersion   uint16
+	CipherSuites []uint16
+}
+
+// ParseTLSOptions
+func ParseTLSOptions(cfg *config.TLSOptions) (*TLS, error) {
+	ret := &TLS{}
+	if cfg == nil {
+		return nil, nil
+	}
+	if cfg.MinVersion != "" {
+		version, err := ConvertTLSMinVersion(cfg.MinVersion)
```

  I feel like this package could be simplified now that we don't need to export `ConvertTLSMinVersion` and `ConvertCipherSuites` for the visibility server. From what I can see now, some checks are being performed twice. For example:
  
  ```go
  	if cfg.MinVersion != "" {  // <- checks ""
  		version, err := ConvertTLSMinVersion(cfg.MinVersion) // <- also checks ""
  ```
  
  Maybe version parsing in `ParseTLSOptions` could just call something like:
  ```go
  version := parseMinVersionWithDefault()
  ```

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-20T11:30:24Z

- **pkg/config/validation.go:535** by [@kshalot](https://github.com/kshalot) — 2026-01-20T11:15:32Z
```diff
@@ -528,3 +530,17 @@ func validateObjectRetentionPolicies(c *configapi.Configuration) field.ErrorList
 	}
 	return allErrs
 }
+
+func validateTLS(c *configapi.Configuration) field.ErrorList {
```

  It would probably force us to "parse" the TLS config multiple times, but if we check whether the values are correct in the validator here then it would surface the error in a more natural place and reference the invalid fields in the error message.

- **pkg/config/config.go:108** by [@kshalot](https://github.com/kshalot) — 2026-01-20T11:22:31Z
```diff
@@ -96,6 +97,16 @@ func addTo(o *ctrl.Options, cfg *configapi.Configuration) {
 		if cfg.Webhook.CertDir != "" {
 			wo.CertDir = cfg.Webhook.CertDir
 		}
+
+		// Apply TLS configuration if provided
+		if cfg.TLS != nil {
+			tlsOpts, err := tlsconfig.ParseTLSOptions(cfg.TLS)
+			if err == nil {
+				tlsOpts := tlsconfig.BuildTLSOptions(tlsOpts)
+				wo.TLSOpts = append(wo.TLSOpts, tlsOpts...)
+			}
+		}
```

  If we parse the TLS config inside the config's `Load` function we could pass it to `addTo` and return it to be used in the main function. Question is whether the `Load` function is the correct place to do this. It would surface the error in a more natural place, but probably worse than inside the validator (see my other comment, [link](https://github.com/kubernetes-sigs/kueue/pull/8563/changes#r2707905981)).
  
  Food for thought.

- **pkg/util/tlsconfig/tlsconfig_test.go:87** by [@kshalot](https://github.com/kshalot) — 2026-01-20T11:25:24Z
```diff
@@ -0,0 +1,398 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tlsconfig
+
+import (
+	"crypto/tls"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+)
+
+func TestParseTLSOptions(t *testing.T) {
+	tests := []struct {
+		name           string
+		cfg            *config.TLSOptions
+		expectedMinVer uint16
+		expectedCipher []uint16
+		expectNil      bool
+		wantErr        bool
+	}{
+		{
+			name:           "nil config",
+			cfg:            nil,
+			expectedMinVer: 0,
+			expectedCipher: nil,
+			expectNil:      true,
+			wantErr:        false,
+		},
+		{
+			name:           "empty config",
+			cfg:            &config.TLSOptions{},
+			expectedMinVer: tls.VersionTLS12,
+			expectedCipher: nil,
+			wantErr:        false,
+		},
+		{
+			name: "TLS 1.2",
+			cfg: &config.TLSOptions{
+				MinVersion: "VersionTLS12",
+			},
+			expectedMinVer: tls.VersionTLS12,
+			expectedCipher: nil,
+			wantErr:        false,
+		},
+		{
+			name: "TLS 1.3",
+			cfg: &config.TLSOptions{
+				MinVersion: "VersionTLS13",
+			},
+			expectedMinVer: tls.VersionTLS13,
+			expectedCipher: nil,
+			wantErr:        false,
+		},
+		{
+			name: "with cipher suites",
+			cfg: &config.TLSOptions{
+				MinVersion: "VersionTLS12",
+				CipherSuites: []string{
+					"TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256",
+					"TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384",
+				},
+			},
+			expectedMinVer: tls.VersionTLS12,
+			expectedCipher: []uint16{
+				tls.TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256,
+				tls.TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384,
+			},
+			wantErr: false,
+		},
+		{
+			name: "full config",
```

  To me it seems that:
  - "full config" = "TLS 1.3 with cipher suites"
  - "with cipher suites" = "TLS 1.2 with cipher suites" (the test case above this one)
  
  I don't see how this one is "fuller"

- **pkg/util/tlsconfig/tlsconfig_test.go:49** by [@kshalot](https://github.com/kshalot) — 2026-01-20T11:27:56Z
```diff
@@ -0,0 +1,398 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tlsconfig
+
+import (
+	"crypto/tls"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+)
+
+func TestParseTLSOptions(t *testing.T) {
+	tests := []struct {
```

  For completeness I'd add a test case where min version is not defined, but the cipher suites are. Currently we have it tested only the other way around

- **pkg/features/kube_features.go:238** by [@kshalot](https://github.com/kshalot) — 2026-01-20T11:29:34Z
```diff
@@ -231,6 +231,12 @@ const (
 	// issue: https://github.com/kubernetes-sigs/kueue/issues/8302
 	// Redo admission on eviction in worker cluster.
 	MultiKueueRedoAdmissionOnEvictionInWorker featuregate.Feature = "MultiKueueRedoAdmissionOnEvictionInWorker"
+
+	// owner: @kannon92
+	//
+	// issue: https://github.com/kubernetes-sigs/kueue/issues/8190
+	// Enables TLSOptions for TLSMinVersion and CipherSuites for kueue servers
```

  Nit:
  ```suggestion
  	// Enables TLSOptions for TLS MinVersion and CipherSuites for Kueue servers.
  ```

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-20T20:36:33Z

- **pkg/config/validation.go:535** by [@kannon92](https://github.com/kannon92) — 2026-01-20T20:36:33Z
```diff
@@ -528,3 +530,17 @@ func validateObjectRetentionPolicies(c *configapi.Configuration) field.ErrorList
 	}
 	return allErrs
 }
+
+func validateTLS(c *configapi.Configuration) field.ErrorList {
```

  I think we do this already.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-20T20:39:16Z

- **pkg/config/config.go:108** by [@kannon92](https://github.com/kannon92) — 2026-01-20T20:39:15Z
```diff
@@ -96,6 +97,16 @@ func addTo(o *ctrl.Options, cfg *configapi.Configuration) {
 		if cfg.Webhook.CertDir != "" {
 			wo.CertDir = cfg.Webhook.CertDir
 		}
+
+		// Apply TLS configuration if provided
+		if cfg.TLS != nil {
+			tlsOpts, err := tlsconfig.ParseTLSOptions(cfg.TLS)
+			if err == nil {
+				tlsOpts := tlsconfig.BuildTLSOptions(tlsOpts)
+				wo.TLSOpts = append(wo.TLSOpts, tlsOpts...)
+			}
+		}
```

  I don't think this would be very clean.
  
  The internal API we are using is different from the user API someone is expected to put into a config API.
  
  It looks like `Load` marshales configs into the configuration api. And we add some defaults.
  
  Our types are strings and we convert to a "internal" representation that matches crypto.tls.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-20T21:43:56Z

- **pkg/config/config.go:108** by [@kannon92](https://github.com/kannon92) — 2026-01-20T21:43:56Z
```diff
@@ -96,6 +97,16 @@ func addTo(o *ctrl.Options, cfg *configapi.Configuration) {
 		if cfg.Webhook.CertDir != "" {
 			wo.CertDir = cfg.Webhook.CertDir
 		}
+
+		// Apply TLS configuration if provided
+		if cfg.TLS != nil {
+			tlsOpts, err := tlsconfig.ParseTLSOptions(cfg.TLS)
+			if err == nil {
+				tlsOpts := tlsconfig.BuildTLSOptions(tlsOpts)
+				wo.TLSOpts = append(wo.TLSOpts, tlsOpts...)
+			}
+		}
```

  Validation provides a cleaner error message imo.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-20T21:44:35Z

- **pkg/config/validation.go:535** by [@kannon92](https://github.com/kannon92) — 2026-01-20T21:44:35Z
```diff
@@ -528,3 +530,17 @@ func validateObjectRetentionPolicies(c *configapi.Configuration) field.ErrorList
 	}
 	return allErrs
 }
+
+func validateTLS(c *configapi.Configuration) field.ErrorList {
```

  I pushed a bunch of validation.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-20T22:29:19Z

- **pkg/config/validation.go:535** by [@kshalot](https://github.com/kshalot) — 2026-01-20T22:29:19Z
```diff
@@ -528,3 +530,17 @@ func validateObjectRetentionPolicies(c *configapi.Configuration) field.ErrorList
 	}
 	return allErrs
 }
+
+func validateTLS(c *configapi.Configuration) field.ErrorList {
```

  Maybe I missed this or got confused, but yeah it's there. Resolving!

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-20T22:30:32Z

- **pkg/features/kube_features.go:238** by [@kannon92](https://github.com/kannon92) — 2026-01-20T22:30:32Z
```diff
@@ -231,6 +231,12 @@ const (
 	// issue: https://github.com/kubernetes-sigs/kueue/issues/8302
 	// Redo admission on eviction in worker cluster.
 	MultiKueueRedoAdmissionOnEvictionInWorker featuregate.Feature = "MultiKueueRedoAdmissionOnEvictionInWorker"
+
+	// owner: @kannon92
+	//
+	// issue: https://github.com/kubernetes-sigs/kueue/issues/8190
+	// Enables TLSOptions for TLSMinVersion and CipherSuites for kueue servers
```

  I believe I addressed this.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-20T22:32:47Z

- **pkg/util/tlsconfig/tlsconfig_test.go:87** by [@kannon92](https://github.com/kannon92) — 2026-01-20T22:32:47Z
```diff
@@ -0,0 +1,398 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tlsconfig
+
+import (
+	"crypto/tls"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+)
+
+func TestParseTLSOptions(t *testing.T) {
+	tests := []struct {
+		name           string
+		cfg            *config.TLSOptions
+		expectedMinVer uint16
+		expectedCipher []uint16
+		expectNil      bool
+		wantErr        bool
+	}{
+		{
+			name:           "nil config",
+			cfg:            nil,
+			expectedMinVer: 0,
+			expectedCipher: nil,
+			expectNil:      true,
+			wantErr:        false,
+		},
+		{
+			name:           "empty config",
+			cfg:            &config.TLSOptions{},
+			expectedMinVer: tls.VersionTLS12,
+			expectedCipher: nil,
+			wantErr:        false,
+		},
+		{
+			name: "TLS 1.2",
+			cfg: &config.TLSOptions{
+				MinVersion: "VersionTLS12",
+			},
+			expectedMinVer: tls.VersionTLS12,
+			expectedCipher: nil,
+			wantErr:        false,
+		},
+		{
+			name: "TLS 1.3",
+			cfg: &config.TLSOptions{
+				MinVersion: "VersionTLS13",
+			},
+			expectedMinVer: tls.VersionTLS13,
+			expectedCipher: nil,
+			wantErr:        false,
+		},
+		{
+			name: "with cipher suites",
+			cfg: &config.TLSOptions{
+				MinVersion: "VersionTLS12",
+				CipherSuites: []string{
+					"TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256",
+					"TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384",
+				},
+			},
+			expectedMinVer: tls.VersionTLS12,
+			expectedCipher: []uint16{
+				tls.TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256,
+				tls.TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384,
+			},
+			wantErr: false,
+		},
+		{
+			name: "full config",
```

  Good idea! I pushed a commit to address this.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-20T22:32:57Z

- **pkg/util/tlsconfig/tlsconfig_test.go:49** by [@kannon92](https://github.com/kannon92) — 2026-01-20T22:32:57Z
```diff
@@ -0,0 +1,398 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tlsconfig
+
+import (
+	"crypto/tls"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+)
+
+func TestParseTLSOptions(t *testing.T) {
+	tests := []struct {
```

  Added.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-21T09:38:52Z

- **pkg/config/validation.go:535** by [@kshalot](https://github.com/kshalot) — 2026-01-21T09:22:25Z
```diff
@@ -528,3 +530,17 @@ func validateObjectRetentionPolicies(c *configapi.Configuration) field.ErrorList
 	}
 	return allErrs
 }
+
+func validateTLS(c *configapi.Configuration) field.ErrorList {
```

  Ok, I see the new code. I added some comments there

- **pkg/config/validation.go:544** by [@kshalot](https://github.com/kshalot) — 2026-01-21T09:25:37Z
```diff
@@ -528,3 +531,28 @@ func validateObjectRetentionPolicies(c *configapi.Configuration) field.ErrorList
 	}
 	return allErrs
 }
+
+func validateTLS(c *configapi.Configuration) field.ErrorList {
+	var allErrs field.ErrorList
+	if c.TLS == nil {
+		return allErrs
+	}
+	// TLS 1.3 cipher suites are not configurable in Go's crypto/tls package.
+	// When TLS 1.3 is set as the minimum version, cipher suites must not be specified.
+	if c.TLS.MinVersion == "VersionTLS13" && len(c.TLS.CipherSuites) > 0 {
+		allErrs = append(allErrs, field.Invalid(tlsPath.Child("cipherSuites"),
+			c.TLS.CipherSuites, "cipher suites cannot be configured when minVersion is VersionTLS13"))
```

  Looking at the [API conventions](https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md#error-messages), this should be:
  
  ```suggestion
  			c.TLS.CipherSuites, "may not be specified when `minVersion` is 'VersionTLS13'"))
  ```

- **pkg/config/validation.go:550** by [@kshalot](https://github.com/kshalot) — 2026-01-21T09:27:19Z
```diff
@@ -528,3 +531,28 @@ func validateObjectRetentionPolicies(c *configapi.Configuration) field.ErrorList
 	}
 	return allErrs
 }
+
+func validateTLS(c *configapi.Configuration) field.ErrorList {
+	var allErrs field.ErrorList
+	if c.TLS == nil {
+		return allErrs
+	}
+	// TLS 1.3 cipher suites are not configurable in Go's crypto/tls package.
+	// When TLS 1.3 is set as the minimum version, cipher suites must not be specified.
+	if c.TLS.MinVersion == "VersionTLS13" && len(c.TLS.CipherSuites) > 0 {
+		allErrs = append(allErrs, field.Invalid(tlsPath.Child("cipherSuites"),
+			c.TLS.CipherSuites, "cipher suites cannot be configured when minVersion is VersionTLS13"))
+	}
+	tls, err := tlsconfig.ParseTLSOptions(c.TLS)
+	if err != nil {
+		if tls.MinVersion == 0 {
+			allErrs = append(allErrs, field.Invalid(tlsPath.Child("minVersion"),
+				c.TLS.MinVersion, "Min Version is invalid. Allowed values are VersionTLS13 or VersionTLS12"))
```

  ```suggestion
  				c.TLS.MinVersion, "must be either 'VersionTLS12' or 'VersionTL13'"))
  ```

- **pkg/config/validation.go:550** by [@kshalot](https://github.com/kshalot) — 2026-01-21T09:38:34Z
```diff
@@ -528,3 +531,28 @@ func validateObjectRetentionPolicies(c *configapi.Configuration) field.ErrorList
 	}
 	return allErrs
 }
+
+func validateTLS(c *configapi.Configuration) field.ErrorList {
+	var allErrs field.ErrorList
+	if c.TLS == nil {
+		return allErrs
+	}
+	// TLS 1.3 cipher suites are not configurable in Go's crypto/tls package.
+	// When TLS 1.3 is set as the minimum version, cipher suites must not be specified.
+	if c.TLS.MinVersion == "VersionTLS13" && len(c.TLS.CipherSuites) > 0 {
+		allErrs = append(allErrs, field.Invalid(tlsPath.Child("cipherSuites"),
+			c.TLS.CipherSuites, "cipher suites cannot be configured when minVersion is VersionTLS13"))
+	}
+	tls, err := tlsconfig.ParseTLSOptions(c.TLS)
+	if err != nil {
+		if tls.MinVersion == 0 {
+			allErrs = append(allErrs, field.Invalid(tlsPath.Child("minVersion"),
+				c.TLS.MinVersion, "Min Version is invalid. Allowed values are VersionTLS13 or VersionTLS12"))
```

  Wouldn't it be more intuitive to have those validations against the unparsed `c.TLS`?
  I.e. first check whether min version has the right value, cipher suites are not defined if TLS 1.3 etc. and then parsing and returning the parsing error if it fails?
  
  For example now if parsing the cipher suites fails, the only error the user will see is "cipher suites is invalid". If you made a typo or used an unsupported suite, you wouldn't have an easy way to tell (especially given our discussions about documenting the supported values for users, using enums etc.).

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-21T16:55:23Z

- **pkg/config/validation.go:550** by [@kannon92](https://github.com/kannon92) — 2026-01-21T16:55:23Z
```diff
@@ -528,3 +531,28 @@ func validateObjectRetentionPolicies(c *configapi.Configuration) field.ErrorList
 	}
 	return allErrs
 }
+
+func validateTLS(c *configapi.Configuration) field.ErrorList {
+	var allErrs field.ErrorList
+	if c.TLS == nil {
+		return allErrs
+	}
+	// TLS 1.3 cipher suites are not configurable in Go's crypto/tls package.
+	// When TLS 1.3 is set as the minimum version, cipher suites must not be specified.
+	if c.TLS.MinVersion == "VersionTLS13" && len(c.TLS.CipherSuites) > 0 {
+		allErrs = append(allErrs, field.Invalid(tlsPath.Child("cipherSuites"),
+			c.TLS.CipherSuites, "cipher suites cannot be configured when minVersion is VersionTLS13"))
+	}
+	tls, err := tlsconfig.ParseTLSOptions(c.TLS)
+	if err != nil {
+		if tls.MinVersion == 0 {
+			allErrs = append(allErrs, field.Invalid(tlsPath.Child("minVersion"),
+				c.TLS.MinVersion, "Min Version is invalid. Allowed values are VersionTLS13 or VersionTLS12"))
```

  I am appending the errors and linking to each component in c.TLS.MinVersion or c.TLS.CipherSuites.
  
  Yea this is the downside of using a string api is that we don't really have a list of allowed values.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-21T18:32:33Z

- **pkg/config/validation.go:544** by [@kannon92](https://github.com/kannon92) — 2026-01-21T18:32:32Z
```diff
@@ -528,3 +531,28 @@ func validateObjectRetentionPolicies(c *configapi.Configuration) field.ErrorList
 	}
 	return allErrs
 }
+
+func validateTLS(c *configapi.Configuration) field.ErrorList {
+	var allErrs field.ErrorList
+	if c.TLS == nil {
+		return allErrs
+	}
+	// TLS 1.3 cipher suites are not configurable in Go's crypto/tls package.
+	// When TLS 1.3 is set as the minimum version, cipher suites must not be specified.
+	if c.TLS.MinVersion == "VersionTLS13" && len(c.TLS.CipherSuites) > 0 {
+		allErrs = append(allErrs, field.Invalid(tlsPath.Child("cipherSuites"),
+			c.TLS.CipherSuites, "cipher suites cannot be configured when minVersion is VersionTLS13"))
```

  updated.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-21T18:32:39Z

- **pkg/config/validation.go:550** by [@kannon92](https://github.com/kannon92) — 2026-01-21T18:32:39Z
```diff
@@ -528,3 +531,28 @@ func validateObjectRetentionPolicies(c *configapi.Configuration) field.ErrorList
 	}
 	return allErrs
 }
+
+func validateTLS(c *configapi.Configuration) field.ErrorList {
+	var allErrs field.ErrorList
+	if c.TLS == nil {
+		return allErrs
+	}
+	// TLS 1.3 cipher suites are not configurable in Go's crypto/tls package.
+	// When TLS 1.3 is set as the minimum version, cipher suites must not be specified.
+	if c.TLS.MinVersion == "VersionTLS13" && len(c.TLS.CipherSuites) > 0 {
+		allErrs = append(allErrs, field.Invalid(tlsPath.Child("cipherSuites"),
+			c.TLS.CipherSuites, "cipher suites cannot be configured when minVersion is VersionTLS13"))
+	}
+	tls, err := tlsconfig.ParseTLSOptions(c.TLS)
+	if err != nil {
+		if tls.MinVersion == 0 {
+			allErrs = append(allErrs, field.Invalid(tlsPath.Child("minVersion"),
+				c.TLS.MinVersion, "Min Version is invalid. Allowed values are VersionTLS13 or VersionTLS12"))
```

  updated.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-21T18:33:14Z

- **pkg/config/validation.go:550** by [@kannon92](https://github.com/kannon92) — 2026-01-21T18:33:14Z
```diff
@@ -528,3 +531,28 @@ func validateObjectRetentionPolicies(c *configapi.Configuration) field.ErrorList
 	}
 	return allErrs
 }
+
+func validateTLS(c *configapi.Configuration) field.ErrorList {
+	var allErrs field.ErrorList
+	if c.TLS == nil {
+		return allErrs
+	}
+	// TLS 1.3 cipher suites are not configurable in Go's crypto/tls package.
+	// When TLS 1.3 is set as the minimum version, cipher suites must not be specified.
+	if c.TLS.MinVersion == "VersionTLS13" && len(c.TLS.CipherSuites) > 0 {
+		allErrs = append(allErrs, field.Invalid(tlsPath.Child("cipherSuites"),
+			c.TLS.CipherSuites, "cipher suites cannot be configured when minVersion is VersionTLS13"))
+	}
+	tls, err := tlsconfig.ParseTLSOptions(c.TLS)
+	if err != nil {
+		if tls.MinVersion == 0 {
+			allErrs = append(allErrs, field.Invalid(tlsPath.Child("minVersion"),
+				c.TLS.MinVersion, "Min Version is invalid. Allowed values are VersionTLS13 or VersionTLS12"))
```

  I pushed a change trying to think through what you meant.
  
  I will validate the fields first and then move the first check to the end.
  
  Is that what you want?

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-21T19:53:26Z

- **pkg/config/validation.go:560** by [@kshalot](https://github.com/kshalot) — 2026-01-21T19:31:03Z
```diff
@@ -528,3 +531,33 @@ func validateObjectRetentionPolicies(c *configapi.Configuration) field.ErrorList
 	}
 	return allErrs
 }
+
+func validateTLS(c *configapi.Configuration) field.ErrorList {
+	var allErrs field.ErrorList
+	if c.TLS == nil {
+		return allErrs
+	}
+
+	// Validate unparsed values first, then parse.
+	// This provides clearer error messages for invalid input.
+	tls, err := tlsconfig.ParseTLSOptions(c.TLS)
+	if err != nil {
+		if tls.MinVersion == 0 {
+			allErrs = append(allErrs, field.Invalid(tlsPath.Child("minVersion"),
+				c.TLS.MinVersion, "must be either 'VersionTLS12' or 'VersionTLS13'"))
+		}
+		if tls.CipherSuites == nil && len(c.TLS.CipherSuites) > 0 {
+			allErrs = append(allErrs, field.Invalid(tlsPath.Child("cipherSuites"),
+				c.TLS.CipherSuites, "cipher suites is invalid"))
+		}
+		return allErrs
+	}
+
+	// TLS 1.3 cipher suites are not configurable in Go's crypto/tls package.
+	// When TLS 1.3 is set as the minimum version, cipher suites must not be specified.
+	if c.TLS.MinVersion == "VersionTLS13" && len(c.TLS.CipherSuites) > 0 {
+		allErrs = append(allErrs, field.Invalid(tlsPath.Child("cipherSuites"),
+			c.TLS.CipherSuites, "may not be specified when 'minVersion' is 'VersionTLS13'"))
```

  ```suggestion
  			c.TLS.CipherSuites, "may not be specified when `minVersion` is 'VersionTLS13'"))
  ```
  
  AFAIR fields are referenced with backticks according to the guidelines

- **pkg/config/validation.go:550** by [@kshalot](https://github.com/kshalot) — 2026-01-21T19:51:17Z
```diff
@@ -528,3 +531,28 @@ func validateObjectRetentionPolicies(c *configapi.Configuration) field.ErrorList
 	}
 	return allErrs
 }
+
+func validateTLS(c *configapi.Configuration) field.ErrorList {
+	var allErrs field.ErrorList
+	if c.TLS == nil {
+		return allErrs
+	}
+	// TLS 1.3 cipher suites are not configurable in Go's crypto/tls package.
+	// When TLS 1.3 is set as the minimum version, cipher suites must not be specified.
+	if c.TLS.MinVersion == "VersionTLS13" && len(c.TLS.CipherSuites) > 0 {
+		allErrs = append(allErrs, field.Invalid(tlsPath.Child("cipherSuites"),
+			c.TLS.CipherSuites, "cipher suites cannot be configured when minVersion is VersionTLS13"))
+	}
+	tls, err := tlsconfig.ParseTLSOptions(c.TLS)
+	if err != nil {
+		if tls.MinVersion == 0 {
+			allErrs = append(allErrs, field.Invalid(tlsPath.Child("minVersion"),
+				c.TLS.MinVersion, "Min Version is invalid. Allowed values are VersionTLS13 or VersionTLS12"))
```

  Sorry if my initial comment wasn't clear. I was thinking about parsing like this:
  ```go
  if !slices.Contains(allowedVersions, c.TLS.MinVersion) {}
  
  // or as you are doing in the parser now
  if tlsMinVersion == "VersionTLS11" || tlsMinVersion == "VersionTLS10" {}
  ```
  instead of
  ```go
  	tls, err := tlsconfig.ParseTLSOptions(c.TLS)
  	if err != nil {
  		if tls.MinVersion == 0 {
  		...
  ```
  The way we rely on the "unparsed" values is not very clean to me, e.g. `if tls.MinVersion == 0` suggesting that the value is invalid. Also, right now if I pass "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA25" (missing "6" at the end) to the `cipherSuites` I'll get an error:
  ```
  cipher suites is invalid
  ```
  while I would expect something like:
  ```
  invalid value 'TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA25'. Supported cipher suites: {suites}
  ```
  This way I know exactly what went wrong.
  
  As an alternative, we could fully rely on the parser to provide the error message (it is returning an error now) and use it in a "parse-not-validate" fashion:
  ```go
  parsedTls, err := parse()
  if err != nil {
      allErrs = append(allErrs, field.Invalid(tlsPath, c.TLS, err))
  }
  ```
  or something along those lines. This has the advantage of ensuring that there is no drift between our validation logic and the parsing in the actual kueue-controller pods.
  
  WDYT?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-22T01:54:10Z

- **pkg/config/validation.go:550** by [@kannon92](https://github.com/kannon92) — 2026-01-22T01:54:10Z
```diff
@@ -528,3 +531,28 @@ func validateObjectRetentionPolicies(c *configapi.Configuration) field.ErrorList
 	}
 	return allErrs
 }
+
+func validateTLS(c *configapi.Configuration) field.ErrorList {
+	var allErrs field.ErrorList
+	if c.TLS == nil {
+		return allErrs
+	}
+	// TLS 1.3 cipher suites are not configurable in Go's crypto/tls package.
+	// When TLS 1.3 is set as the minimum version, cipher suites must not be specified.
+	if c.TLS.MinVersion == "VersionTLS13" && len(c.TLS.CipherSuites) > 0 {
+		allErrs = append(allErrs, field.Invalid(tlsPath.Child("cipherSuites"),
+			c.TLS.CipherSuites, "cipher suites cannot be configured when minVersion is VersionTLS13"))
+	}
+	tls, err := tlsconfig.ParseTLSOptions(c.TLS)
+	if err != nil {
+		if tls.MinVersion == 0 {
+			allErrs = append(allErrs, field.Invalid(tlsPath.Child("minVersion"),
+				c.TLS.MinVersion, "Min Version is invalid. Allowed values are VersionTLS13 or VersionTLS12"))
```

  > As an alternative, we could fully rely on the parser to provide the error message (it is returning an error now) and use it in a "parse-not-validate" fashion:
  
  I think this is probably the cleanest solution.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-22T02:03:00Z

- **pkg/config/validation.go:550** by [@kannon92](https://github.com/kannon92) — 2026-01-22T02:03:00Z
```diff
@@ -528,3 +531,28 @@ func validateObjectRetentionPolicies(c *configapi.Configuration) field.ErrorList
 	}
 	return allErrs
 }
+
+func validateTLS(c *configapi.Configuration) field.ErrorList {
+	var allErrs field.ErrorList
+	if c.TLS == nil {
+		return allErrs
+	}
+	// TLS 1.3 cipher suites are not configurable in Go's crypto/tls package.
+	// When TLS 1.3 is set as the minimum version, cipher suites must not be specified.
+	if c.TLS.MinVersion == "VersionTLS13" && len(c.TLS.CipherSuites) > 0 {
+		allErrs = append(allErrs, field.Invalid(tlsPath.Child("cipherSuites"),
+			c.TLS.CipherSuites, "cipher suites cannot be configured when minVersion is VersionTLS13"))
+	}
+	tls, err := tlsconfig.ParseTLSOptions(c.TLS)
+	if err != nil {
+		if tls.MinVersion == 0 {
+			allErrs = append(allErrs, field.Invalid(tlsPath.Child("minVersion"),
+				c.TLS.MinVersion, "Min Version is invalid. Allowed values are VersionTLS13 or VersionTLS12"))
```

  Pushed up a fix to use your "parse-not-validate" and use the error message so we have 1 place to get back better error messages.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-22T02:03:14Z

- **pkg/config/validation.go:560** by [@kannon92](https://github.com/kannon92) — 2026-01-22T02:03:14Z
```diff
@@ -528,3 +531,33 @@ func validateObjectRetentionPolicies(c *configapi.Configuration) field.ErrorList
 	}
 	return allErrs
 }
+
+func validateTLS(c *configapi.Configuration) field.ErrorList {
+	var allErrs field.ErrorList
+	if c.TLS == nil {
+		return allErrs
+	}
+
+	// Validate unparsed values first, then parse.
+	// This provides clearer error messages for invalid input.
+	tls, err := tlsconfig.ParseTLSOptions(c.TLS)
+	if err != nil {
+		if tls.MinVersion == 0 {
+			allErrs = append(allErrs, field.Invalid(tlsPath.Child("minVersion"),
+				c.TLS.MinVersion, "must be either 'VersionTLS12' or 'VersionTLS13'"))
+		}
+		if tls.CipherSuites == nil && len(c.TLS.CipherSuites) > 0 {
+			allErrs = append(allErrs, field.Invalid(tlsPath.Child("cipherSuites"),
+				c.TLS.CipherSuites, "cipher suites is invalid"))
+		}
+		return allErrs
+	}
+
+	// TLS 1.3 cipher suites are not configurable in Go's crypto/tls package.
+	// When TLS 1.3 is set as the minimum version, cipher suites must not be specified.
+	if c.TLS.MinVersion == "VersionTLS13" && len(c.TLS.CipherSuites) > 0 {
+		allErrs = append(allErrs, field.Invalid(tlsPath.Child("cipherSuites"),
+			c.TLS.CipherSuites, "may not be specified when 'minVersion' is 'VersionTLS13'"))
```

  updated this.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-22T02:03:47Z

- **pkg/config/config.go:103** by [@kannon92](https://github.com/kannon92) — 2026-01-22T02:03:47Z
```diff
@@ -96,6 +97,15 @@ func addTo(o *ctrl.Options, cfg *configapi.Configuration) {
 		if cfg.Webhook.CertDir != "" {
 			wo.CertDir = cfg.Webhook.CertDir
 		}
+
+		// Apply TLS configuration if provided
+		if cfg.TLS != nil {
+			tlsOpts, err := tlsconfig.BuildTLSOptions(cfg.TLS)
```

  fixed this.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-22T09:57:23Z

- **pkg/config/config.go:112** by [@kshalot](https://github.com/kshalot) — 2026-01-22T09:57:23Z
```diff
@@ -96,6 +98,18 @@ func addTo(o *ctrl.Options, cfg *configapi.Configuration) {
 		if cfg.Webhook.CertDir != "" {
 			wo.CertDir = cfg.Webhook.CertDir
 		}
+
+		// Apply TLS configuration if provided
+		if features.Enabled(features.TLSOptions) {
+			if cfg.TLS != nil {
+				tlsOpts, err := tlsconfig.ParseTLSOptions(cfg.TLS)
+				if err == nil {
+					tlsOpts := tlsconfig.BuildTLSOptions(tlsOpts)
+					wo.TLSOpts = append(wo.TLSOpts, tlsOpts...)
+				}
+			}
+		}
+
```

  This was noticed by @mimowo, but I'll flag it again - checking feature gates here might not do anything since they are set **after** this is executed.
  
  `apply` calls `Load` which calls `addTo`. Feature gates are set after it.
  https://github.com/kubernetes-sigs/kueue/blob/7e288e040e7feeb6578bc92bc6e766eb964d85cf/cmd/kueue/main.go#L136-L151
  So won't this flag always be unset?
  
  We can eliminate this issue and make the integration cleaner by moving this block to the `main` function. We could try to initialize the webhook server in `main`, like the metrics server:
  https://github.com/kubernetes-sigs/kueue/blob/7e288e040e7feeb6578bc92bc6e766eb964d85cf/cmd/kueue/main.go#L192

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-01-22T10:08:11Z

- **pkg/util/tlsconfig/tlsconfig_test.go:35** by [@kshalot](https://github.com/kshalot) — 2026-01-22T10:08:12Z
```diff
@@ -0,0 +1,467 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tlsconfig
+
+import (
+	"crypto/tls"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+)
+
+func TestParseTLSOptions(t *testing.T) {
+	tests := []struct {
+		name           string
+		cfg            *config.TLSOptions
+		expectedMinVer uint16
+		expectedCipher []uint16
+		expectNil      bool
+		wantErr        bool
```

  It would be preferable to have a `wantErr string` or `wantErr error` to verify the cause of the error and check whether they match. To avoid having to write and maintain long string values, we can also have `wantErrs` slice and check for substrings ("invalid minVersion", "invalid cipher suites") etc.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-22T10:38:28Z

- **pkg/util/tlsconfig/tlsconfig_test.go:35** by [@mimowo](https://github.com/mimowo) — 2026-01-22T10:38:28Z
```diff
@@ -0,0 +1,467 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tlsconfig
+
+import (
+	"crypto/tls"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+)
+
+func TestParseTLSOptions(t *testing.T) {
+	tests := []struct {
+		name           string
+		cfg            *config.TLSOptions
+		expectedMinVer uint16
+		expectedCipher []uint16
+		expectNil      bool
+		wantErr        bool
```

  +1

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-22T10:38:31Z

- **pkg/config/config.go:112** by [@mimowo](https://github.com/mimowo) — 2026-01-22T10:38:31Z
```diff
@@ -96,6 +98,18 @@ func addTo(o *ctrl.Options, cfg *configapi.Configuration) {
 		if cfg.Webhook.CertDir != "" {
 			wo.CertDir = cfg.Webhook.CertDir
 		}
+
+		// Apply TLS configuration if provided
+		if features.Enabled(features.TLSOptions) {
+			if cfg.TLS != nil {
+				tlsOpts, err := tlsconfig.ParseTLSOptions(cfg.TLS)
+				if err == nil {
+					tlsOpts := tlsconfig.BuildTLSOptions(tlsOpts)
+					wo.TLSOpts = append(wo.TLSOpts, tlsOpts...)
+				}
+			}
+		}
+
```

  +1

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-22T16:47:26Z

- **pkg/config/config.go:112** by [@kannon92](https://github.com/kannon92) — 2026-01-22T16:47:26Z
```diff
@@ -96,6 +98,18 @@ func addTo(o *ctrl.Options, cfg *configapi.Configuration) {
 		if cfg.Webhook.CertDir != "" {
 			wo.CertDir = cfg.Webhook.CertDir
 		}
+
+		// Apply TLS configuration if provided
+		if features.Enabled(features.TLSOptions) {
+			if cfg.TLS != nil {
+				tlsOpts, err := tlsconfig.ParseTLSOptions(cfg.TLS)
+				if err == nil {
+					tlsOpts := tlsconfig.BuildTLSOptions(tlsOpts)
+					wo.TLSOpts = append(wo.TLSOpts, tlsOpts...)
+				}
+			}
+		}
+
```

  sounds good. I'm working on this now.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-22T21:05:30Z

- **pkg/config/config.go:112** by [@kannon92](https://github.com/kannon92) — 2026-01-22T21:05:30Z
```diff
@@ -96,6 +98,18 @@ func addTo(o *ctrl.Options, cfg *configapi.Configuration) {
 		if cfg.Webhook.CertDir != "" {
 			wo.CertDir = cfg.Webhook.CertDir
 		}
+
+		// Apply TLS configuration if provided
+		if features.Enabled(features.TLSOptions) {
+			if cfg.TLS != nil {
+				tlsOpts, err := tlsconfig.ParseTLSOptions(cfg.TLS)
+				if err == nil {
+					tlsOpts := tlsconfig.BuildTLSOptions(tlsOpts)
+					wo.TLSOpts = append(wo.TLSOpts, tlsOpts...)
+				}
+			}
+		}
+
```

  Pushed a fix for this.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-22T21:05:44Z

- **pkg/util/tlsconfig/tlsconfig_test.go:35** by [@kannon92](https://github.com/kannon92) — 2026-01-22T21:05:44Z
```diff
@@ -0,0 +1,467 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tlsconfig
+
+import (
+	"crypto/tls"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+)
+
+func TestParseTLSOptions(t *testing.T) {
+	tests := []struct {
+		name           string
+		cfg            *config.TLSOptions
+		expectedMinVer uint16
+		expectedCipher []uint16
+		expectNil      bool
+		wantErr        bool
```

  Fixed this.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-23T08:20:11Z

/lgtm
/approve
Thank you 👍
