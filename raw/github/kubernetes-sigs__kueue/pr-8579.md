# PR #8579: Ray Serve autoscaling: Skip gates for redis-cleanup

**Summary**: Ray Serve autoscaling: Skip gates for redis-cleanup

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8579

**Last updated**: 2026-06-04T09:56:12Z

---

## Metadata

- **State**: open
- **Author**: [@ns-sundar](https://github.com/ns-sundar)
- **Branch**: `sn1-rayscale-oss-main` → `main`
- **Created**: 2026-01-14T00:51:39Z
- **Updated**: 2026-06-04T09:56:12Z
- **Merged**: —
- **Closed**: —
- **Labels**: `kind/bug`, `release-note-none`, `cncf-cla: no`, `size/XL`, `needs-rebase`, `ok-to-test`
- **Assignees**: [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk)
- **Requested reviewers**: [@gabesaba](https://github.com/gabesaba), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 9   **+555 / -6**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug 

#### What this PR does / why we need it:
See the issue https://github.com/kubernetes-sigs/kueue/issues/8443 .

TL;DR
**Goal**: Get Ray Serve autoscaling to work with Kueue.

**Configuration**:
  - KubeRay 1.4 and Kueue 0.14.3 (with ElasticJobsViaWorkloadSlices feature gate).
  - The Ray Service CR (and the Ray Cluster CR it creates) have spec.enableInTreeAutoscaling: true and the annotation kueue.x-k8s.io/elastic-job: "true"

**Behavior**:
  - When the Ray Serve workload terminates (and the associated Ray Cluster is terminated), KubeRay launches a redis-cleanup batch job, which runs a pod to clean up the redis-cleanup config used by the Ray Cluster (RC), which was launched by Ray Serve.
  - With Kueue, the redis-cleanup pod remains schedule-gated and in Pending state. Some logic in KubeRay eventually kills it but the cleanup never happens.
 - This is because Kueue does not remove the pod scheduling gate in the redis-cleanup pod.

**Analysis**:
Kueue does not to handle autoscaling for the redis-cleanup pod; so, it need not be treated as an elastic job.

**Solution**:
Modify the batch job webhook in Kueue to remove the scheduling gate from the job if (a) it is owned by a ray.io/v1/RayCluster, AND (b) has the label "ray.io/node-type = redis-cleanup"

#### Which issue(s) this PR fixes:
Fixes #8443 (https://github.com/kubernetes-sigs/kueue/issues/8443)

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@linux-foundation-easycla[bot]](https://github.com/apps/linux-foundation-easycla) — 2026-01-14T00:51:45Z

 <a href="https://api.easycla.lfx.linuxfoundation.org/v2/repository-provider/github/sign/20677499/459870310/8579/#/?version=2"><img src="https://s3.amazonaws.com/cla-project-logo-prod/cla-missing-id.svg?v=2" alt="CLA Missing ID" align="left" height="28" width="328"></a> <a href="https://api.easycla.lfx.linuxfoundation.org/v2/repository-provider/github/sign/20677499/459870310/8579/#/?version=2"><img src="https://s3.amazonaws.com/cla-project-logo-prod/cla-not-signed.svg?v=2" alt="CLA Not Signed" align="left" height="28" width="328"></a><br/><br /><ul><li>:white_check_mark: login: ns-sundar / name: Sundar Nadathur  (08e53e1a6eb90d29919e3aa451137e452a81339c, 1e8e10d9a645032bebf122d4c3e501eef2ec3dc6, 27063ac009d719539b439dab7d901069a8bfc0ef, 6653117bbd7243c0e18b3e4e6bfbc0d4f1f0ada6, 9d1d66be9de08bb53ba77db2ee9cd57d056a2915, a663b2fcaede59330fa36de1cf422239be6ca71a, c424012877bbef32aa0dd2fc9ed30511d0eddb79, e86653a640a0323cbdf7f5cbd0339a887393400a)</li><li> :x: The email address for the commit (084516113f7c689c80d96b45bc05a1ddf1056740) is not linked to the GitHub account, preventing the EasyCLA check. Consult <a href='https://linuxfoundation.atlassian.net/wiki/spaces/LP/pages/160923756/Missing+ID+on+Commit+but+I+have+an+agreement+on+file' target='_blank'>this Help Article</a> and <a href='https://help.github.com/en/github/committing-changes-to-your-project/why-are-my-commits-linked-to-the-wrong-user' target='_blank'>GitHub Help</a> to resolve. (To view the commit's email address, add .patch at the end of this PR page's URL.) For further assistance with EasyCLA, <a href='https://easycla.lfx.linuxfoundation.org/' target='_blank'>please visit our EasyCLA portal</a> and chat with our support bot.</li></ul>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-01-14T00:51:46Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 6653117bbd7243c0e18b3e4e6bfbc0d4f1f0ada6 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69fbb13c3cefb700086b8cfa |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-14T00:51:49Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8579#" title="Author self-approved">ns-sundar</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [gabesaba](https://github.com/gabesaba) for approval. For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["gabesaba"]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-14T00:51:50Z

Hi @ns-sundar. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-14T00:51:50Z

Welcome @ns-sundar! <br><br>It looks like this is your first PR to <a href='https://github.com/kubernetes-sigs/kueue'>kubernetes-sigs/kueue</a> 🎉. Please refer to our [pull request process documentation](https://www.kubernetes.dev/docs/guide/pull-requests/) to help your PR have a smooth ride to approval. <br><br>You will be prompted by a bot to use commands during the review process. Do not be afraid to follow the prompts! It is okay to experiment. [Here is the bot commands documentation](https://go.k8s.io/bot-commands). <br><br>You can also check if kubernetes-sigs/kueue has [its own contribution guidelines](https://github.com/kubernetes-sigs/kueue/tree/master/CONTRIBUTING.md). <br><br>You may want to refer to our [testing guide](https://git.k8s.io/community/contributors/devel/sig-testing/testing.md) if you run into trouble with your tests not passing. <br><br>If you are having difficulty getting your pull request seen, please follow the [recommended escalation practices](https://www.kubernetes.dev/docs/guide/pull-requests/#why-is-my-pull-request-not-getting-reviewed). Also, for tips and tricks in the contribution process you may want to read the [Kubernetes contributor cheat sheet](https://www.kubernetes.dev/docs/contributor-cheatsheet/). We want to make sure your contribution gets all the attention it needs! <br><br>Thank you, and welcome to Kubernetes. :smiley:

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-14T08:03:16Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-02T10:05:24Z

/assign @yaroslava-serdiuk 
for review

### Comment by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2026-02-17T08:22:37Z

In general lgtm, let's fix the tests.

### Comment by [@ns-sundar](https://github.com/ns-sundar) — 2026-02-21T18:11:22Z

@yaroslava-serdiuk @mimowo 
Thank you for your patience. I have rebased to top of main, and that fixed most test failures.

I have added an e2e test in test/e2e/singlecluster/kuberay_test.go for Redis cleanup pods. I thought Kuberay would automatically spawn redis-cleanup pods but that's not happening for some reason. That causes 4 checks to fail. While I dig into that, any tips or suggestions would be welcome.

On a different note, this issue happens readily with Kueue 0.15. It was originally observed in 0.14, but it could not be reproduced easily in our environment after we changed the version of KubeRay, YuniKorn and other components. Now, we have upgraded to 0.15 and see it again.

### Comment by [@ns-sundar](https://github.com/ns-sundar) — 2026-04-17T01:53:44Z

Recap:
 * The logic of the fix has been reviewed. The e2e test is the only one that is open. 
 * The newly added e2e test aims to verify that, when a Ray Cluster (such as one spawned by a Ray Service) is terminated, the redis-cleanup job that is created does not have Kueue's scheduling gates. 

KubeRay creates a redis-cleanup job for a Ray Cluster under the following conditions:
* KubeRay was deployed with the `ENABLE_GCS_FT_REDIS_CLEANUP` env var.
* The Ray Cluster has the annotation `ray.io/ft-enabled: true` and the finalizer `ray.io/gcs-ft-redis-cleanup-finalizer`. 
* The Ray Cluster's head pod has the env vars relating to REDIS. The redis-cleanup job inherits from the head pod. 

However, the e2e tests time out because the redis-cleanup job does not get created.
 * By running the e2e tests locally, I've ensured that the above conditions are met.
 * This PR's fix has been applied internally and validated as well.

However, the redis-cleanup job still does not get created in the e2e tests for some reason -- in fact, the e2e-test kuberay logs don't even reference redis, unlike the real-world deployments where we do see log.Info for redis-cleanup job creation. This needs further investigation.
cc: @mimowo @sohankunkerkar

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-17T07:58:22Z

> The logic of the fix has been reviewed. The e2e test is the only one that is open.

Actually, I haven't yet studied the code well. I was hoping with the test I can undestand the issue better, and so the code changes. Let me see if maybe @yaroslava-serdiuk has some ideas why the issue fails to be reproduced in e2e tests.

### Comment by [@ns-sundar](https://github.com/ns-sundar) — 2026-04-28T16:57:26Z

@mimowo @sohankunkerkar @yaroslava-serdiuk , all the checks are passing now (only tide is pending). Could you review the PR now? Thanks.

### Comment by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2026-05-06T09:27:21Z

@ns-sundar I run the test without the change and it passed, so it does't test the actual introduced changes

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-20T02:41:58Z

PR needs rebase.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-06-04T09:56:12Z

@ns-sundar: The following tests **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:

Test name | Commit | Details | Required | Rerun command
--- | --- | --- | --- | ---
pull-kueue-test-e2e-main-1-32 | 2030b8859d993e5e91d651b571857ebf7f47a9b3 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8579/pull-kueue-test-e2e-main-1-32/2026714866772873216) | true | `/test pull-kueue-test-e2e-main-1-32`
pull-kueue-priority-booster-test-integration-main | 6653117bbd7243c0e18b3e4e6bfbc0d4f1f0ada6 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8579/pull-kueue-priority-booster-test-integration-main/2056927366436884480) | true | `/test pull-kueue-priority-booster-test-integration-main`
pull-kueue-test-e2e-main-1-36 | 6653117bbd7243c0e18b3e4e6bfbc0d4f1f0ada6 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8579/pull-kueue-test-e2e-main-1-36/2057935791199358976) | true | `/test pull-kueue-test-e2e-main-1-36`
pull-kueue-test-e2e-baseline-main-1-36 | 6653117bbd7243c0e18b3e4e6bfbc0d4f1f0ada6 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8579/pull-kueue-test-e2e-baseline-main-1-36/2059119139045249024) | true | `/test pull-kueue-test-e2e-baseline-main-1-36`
pull-kueue-test-e2e-extended-main-1-36 | 6653117bbd7243c0e18b3e4e6bfbc0d4f1f0ada6 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8579/pull-kueue-test-e2e-extended-main-1-36/2059119139137523712) | true | `/test pull-kueue-test-e2e-extended-main-1-36`
pull-kueue-test-integration-shard-1-main | 6653117bbd7243c0e18b3e4e6bfbc0d4f1f0ada6 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8579/pull-kueue-test-integration-shard-1-main/2059286693315350528) | true | `/test pull-kueue-test-integration-shard-1-main`
pull-kueue-test-integration-shard-2-main | 6653117bbd7243c0e18b3e4e6bfbc0d4f1f0ada6 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8579/pull-kueue-test-integration-shard-2-main/2059286693420208128) | true | `/test pull-kueue-test-integration-shard-2-main`
pull-kueue-test-integration-shard-0-main | 6653117bbd7243c0e18b3e4e6bfbc0d4f1f0ada6 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8579/pull-kueue-test-integration-shard-0-main/2059286693235658752) | true | `/test pull-kueue-test-integration-shard-0-main`
pull-kueue-test-e2e-sequential-baseline-shard-1-main | 6653117bbd7243c0e18b3e4e6bfbc0d4f1f0ada6 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8579/pull-kueue-test-e2e-sequential-baseline-shard-1-main/2059628596317655040) | true | `/test pull-kueue-test-e2e-sequential-baseline-shard-1-main`
pull-kueue-test-e2e-sequential-baseline-shard-0-main | 6653117bbd7243c0e18b3e4e6bfbc0d4f1f0ada6 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8579/pull-kueue-test-e2e-sequential-baseline-shard-0-main/2059628596170854400) | true | `/test pull-kueue-test-e2e-sequential-baseline-shard-0-main`
pull-kueue-test-e2e-multikueue-sequential-shard-0-main | 6653117bbd7243c0e18b3e4e6bfbc0d4f1f0ada6 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8579/pull-kueue-test-e2e-multikueue-sequential-shard-0-main/2059861127818383360) | true | `/test pull-kueue-test-e2e-multikueue-sequential-shard-0-main`
pull-kueue-test-e2e-multikueue-sequential-shard-1-main | 6653117bbd7243c0e18b3e4e6bfbc0d4f1f0ada6 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8579/pull-kueue-test-e2e-multikueue-sequential-shard-1-main/2059861127923240960) | true | `/test pull-kueue-test-e2e-multikueue-sequential-shard-1-main`
pull-kueue-test-e2e-multikueue-baseline-main | 6653117bbd7243c0e18b3e4e6bfbc0d4f1f0ada6 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8579/pull-kueue-test-e2e-multikueue-baseline-main/2060760302751846400) | true | `/test pull-kueue-test-e2e-multikueue-baseline-main`
pull-kueue-test-e2e-multikueue-extended-main | 6653117bbd7243c0e18b3e4e6bfbc0d4f1f0ada6 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8579/pull-kueue-test-e2e-multikueue-extended-main/2060760302839926784) | true | `/test pull-kueue-test-e2e-multikueue-extended-main`
pull-kueue-test-e2e-extended-shard-1-main-1-36 | 6653117bbd7243c0e18b3e4e6bfbc0d4f1f0ada6 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8579/pull-kueue-test-e2e-extended-shard-1-main-1-36/2062204771082702848) | true | `/test pull-kueue-test-e2e-extended-shard-1-main-1-36`
pull-kueue-test-e2e-extended-shard-1-main-1-33 | 6653117bbd7243c0e18b3e4e6bfbc0d4f1f0ada6 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8579/pull-kueue-test-e2e-extended-shard-1-main-1-33/2062204770470334464) | true | `/test pull-kueue-test-e2e-extended-shard-1-main-1-33`
pull-kueue-test-e2e-extended-shard-0-main-1-33 | 6653117bbd7243c0e18b3e4e6bfbc0d4f1f0ada6 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8579/pull-kueue-test-e2e-extended-shard-0-main-1-33/2062204770369671168) | true | `/test pull-kueue-test-e2e-extended-shard-0-main-1-33`
pull-kueue-test-e2e-extended-shard-1-main-1-34 | 6653117bbd7243c0e18b3e4e6bfbc0d4f1f0ada6 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8579/pull-kueue-test-e2e-extended-shard-1-main-1-34/2062204770659078144) | true | `/test pull-kueue-test-e2e-extended-shard-1-main-1-34`
pull-kueue-test-e2e-extended-shard-0-main-1-34 | 6653117bbd7243c0e18b3e4e6bfbc0d4f1f0ada6 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8579/pull-kueue-test-e2e-extended-shard-0-main-1-34/2062204770550026240) | true | `/test pull-kueue-test-e2e-extended-shard-0-main-1-34`
pull-kueue-test-e2e-extended-shard-1-main-1-35 | 6653117bbd7243c0e18b3e4e6bfbc0d4f1f0ada6 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8579/pull-kueue-test-e2e-extended-shard-1-main-1-35/2062204770864599040) | true | `/test pull-kueue-test-e2e-extended-shard-1-main-1-35`
pull-kueue-test-e2e-extended-shard-0-main-1-35 | 6653117bbd7243c0e18b3e4e6bfbc0d4f1f0ada6 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8579/pull-kueue-test-e2e-extended-shard-0-main-1-35/2062204770768130048) | true | `/test pull-kueue-test-e2e-extended-shard-0-main-1-35`
pull-kueue-test-e2e-extended-shard-0-main-1-36 | 6653117bbd7243c0e18b3e4e6bfbc0d4f1f0ada6 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8579/pull-kueue-test-e2e-extended-shard-0-main-1-36/2062204770977845248) | true | `/test pull-kueue-test-e2e-extended-shard-0-main-1-36`
pull-kueue-test-e2e-tas-extended-shard-0-main | 6653117bbd7243c0e18b3e4e6bfbc0d4f1f0ada6 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8579/pull-kueue-test-e2e-tas-extended-shard-0-main/2062473389909479424) | true | `/test pull-kueue-test-e2e-tas-extended-shard-0-main`
pull-kueue-test-e2e-tas-extended-shard-1-main | 6653117bbd7243c0e18b3e4e6bfbc0d4f1f0ada6 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8579/pull-kueue-test-e2e-tas-extended-shard-1-main/2062473389989171200) | true | `/test pull-kueue-test-e2e-tas-extended-shard-1-main`

[Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=8579). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3Ans-sundar). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository. I understand the commands that are listed [here](https://go.k8s.io/bot-commands).
</details>
<!-- test report -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-14T09:15:50Z

- **pkg/controller/jobframework/reconciler.go:1171** by [@mimowo](https://github.com/mimowo) — 2026-01-14T09:15:50Z
```diff
@@ -1164,6 +1164,14 @@ func (r *JobReconciler) startJob(ctx context.Context, job GenericJob, object cli
 		}
 	}
 
+	// Log job unsuspend event with GVK information
+	log = ctrl.LoggerFrom(ctx)
+	log.V(3).Info("Unsuspending job for admission",
+		"name", object.GetName(),
+		"namespace", object.GetNamespace(),
```

  I think these should not be needed, because controller runtime puts that information already for each reconciled object. In any case I would suggest to drop the logging changes in reconciler.go from this PR. We can consider them in a separate PR focused on debuggability.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-14T09:36:02Z

- **pkg/controller/jobs/job/job_webhook.go:129** by [@mimowo](https://github.com/mimowo) — 2026-01-14T09:36:02Z
```diff
@@ -55,17 +56,128 @@ var (
 // behavior in Kueue-managed Jobs, allowing for fine-grained control over
 // resource allocation and scheduling decisions.
 //
+// Exception: Batch jobs owned by ray.io/v1/RayCluster with the label "ray.io/node-type: redis-cleanup"
+// are excluded from getting scheduling gates because (a) Kueue workload podsets do not account for
+// redis cleanup pods, and (b) redis-cleanup jobs do not need autoscaling (elastic job).
+//
 // Parameters:
+//   - ctx: Context for logging
 //   - job: Pointer to the Job object to be modified.
 //
 // Note: This function modifies the Job's pod template in-place.
-func applyWorkloadSliceSchedulingGate(job *Job) {
-	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) || !workloadslicing.Enabled(job.Object()) {
+func applyWorkloadSliceSchedulingGate(ctx context.Context, job *Job) {
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+
+	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) {
+		log.V(3).Info("ElasticJobsViaWorkloadSlices feature disabled, skipping scheduling gate",
+			"jobName", job.Name,
+			"jobNamespace", job.Namespace)
 		return
 	}
+
+	// For redis_cleanup jobs generated by KubeRay's RayCluster, the workload slicing
+	// annotation will not be set. So, workloadslicing.Enabled(job.Object()) will
+	// return false. Still, we should rm the elastic job scheduling gate.
+
+	if isRayRedisCleanupJob(ctx, job) {
+		log.V(3).Info("Detected Ray redis-cleanup job - removing any inherited elastic scheduling gates",
+			"jobName", job.Name,
+			"jobNamespace", job.Namespace)
+
+		// Safely remove the elastic job scheduling gate if it exists (inherited from RayCluster template)
+		// Use reverse iteration to avoid index shifting issues when removing elements
+		// Use slices.Delete for safe removal
+		updated := false
+		for i := len(job.Spec.Template.Spec.SchedulingGates) - 1; i >= 0; i-- {
+			gate := job.Spec.Template.Spec.SchedulingGates[i]
+			if gate.Name == kueue.ElasticJobSchedulingGate {
+				job.Spec.Template.Spec.SchedulingGates = slices.Delete(job.Spec.Template.Spec.SchedulingGates, i, i+1)
+				updated = true
+				break // Exit after first match since there should only be one elastic gate
+			}
+		}
+		if updated {
+			log.V(3).Info("Successfully removed inherited elastic scheduling gate from redis-cleanup job",
+				"jobName", job.Name,
+				"jobNamespace", job.Namespace)
+		} else {
+			log.V(3).Info("No elastic scheduling gate found to remove from redis-cleanup job",
+				"jobName", job.Name,
+				"jobNamespace", job.Namespace)
+		}
+		return
+	}
+
+	// Only apply scheduling gate if the job is explicitly opted in via workload slicing annotation
+	if !workloadslicing.Enabled(job.Object()) {
+		log.V(3).Info("Job is not opted in for workload slicing, skipping scheduling gate",
+			"jobName", job.Name,
+			"jobNamespace", job.Namespace)
+		return
+	}
+
+	log.V(10).Info("Job is opted in for workload slicing - applying scheduling gate",
+		"jobName", job.Name,
+		"jobNamespace", job.Namespace,
+		"gate", kueue.ElasticJobSchedulingGate)
+
 	utilpod.GateTemplate(&job.Spec.Template, kueue.ElasticJobSchedulingGate)
 }
 
+// isRayRedisCleanupJob checks if the batch job is owned by a RayCluster and has the redis-cleanup label.
+// Returns true if the job should be excluded from getting scheduling gates.
+func isRayRedisCleanupJob(ctx context.Context, job *Job) bool {
```

  Let's move the code to ray_utils.go and commonize with isRaySubmitterJobWithAutoScaling.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-14T09:42:16Z

- **pkg/controller/jobs/job/job_webhook.go:108** by [@mimowo](https://github.com/mimowo) — 2026-01-14T09:42:16Z
```diff
@@ -55,17 +56,128 @@ var (
 // behavior in Kueue-managed Jobs, allowing for fine-grained control over
 // resource allocation and scheduling decisions.
 //
+// Exception: Batch jobs owned by ray.io/v1/RayCluster with the label "ray.io/node-type: redis-cleanup"
+// are excluded from getting scheduling gates because (a) Kueue workload podsets do not account for
+// redis cleanup pods, and (b) redis-cleanup jobs do not need autoscaling (elastic job).
+//
 // Parameters:
+//   - ctx: Context for logging
 //   - job: Pointer to the Job object to be modified.
 //
 // Note: This function modifies the Job's pod template in-place.
-func applyWorkloadSliceSchedulingGate(job *Job) {
-	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) || !workloadslicing.Enabled(job.Object()) {
+func applyWorkloadSliceSchedulingGate(ctx context.Context, job *Job) {
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+
+	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) {
+		log.V(3).Info("ElasticJobsViaWorkloadSlices feature disabled, skipping scheduling gate",
+			"jobName", job.Name,
+			"jobNamespace", job.Namespace)
 		return
 	}
+
+	// For redis_cleanup jobs generated by KubeRay's RayCluster, the workload slicing
+	// annotation will not be set. So, workloadslicing.Enabled(job.Object()) will
+	// return false. Still, we should rm the elastic job scheduling gate.
+
+	if isRayRedisCleanupJob(ctx, job) {
+		log.V(3).Info("Detected Ray redis-cleanup job - removing any inherited elastic scheduling gates",
+			"jobName", job.Name,
+			"jobNamespace", job.Namespace)
+
+		// Safely remove the elastic job scheduling gate if it exists (inherited from RayCluster template)
+		// Use reverse iteration to avoid index shifting issues when removing elements
+		// Use slices.Delete for safe removal
+		updated := false
+		for i := len(job.Spec.Template.Spec.SchedulingGates) - 1; i >= 0; i-- {
+			gate := job.Spec.Template.Spec.SchedulingGates[i]
+			if gate.Name == kueue.ElasticJobSchedulingGate {
+				job.Spec.Template.Spec.SchedulingGates = slices.Delete(job.Spec.Template.Spec.SchedulingGates, i, i+1)
+				updated = true
+				break // Exit after first match since there should only be one elastic gate
+			}
+		}
+		if updated {
+			log.V(3).Info("Successfully removed inherited elastic scheduling gate from redis-cleanup job",
+				"jobName", job.Name,
+				"jobNamespace", job.Namespace)
+		} else {
+			log.V(3).Info("No elastic scheduling gate found to remove from redis-cleanup job",
+				"jobName", job.Name,
+				"jobNamespace", job.Namespace)
+		}
+		return
```

  Instead of this code let's just call `podutil.UngateTemplate`, and add the function analogously as here: https://github.com/kubernetes-sigs/kueue/blob/main/pkg/util/pod/pod.go#L54-L58

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-14T09:44:40Z

- **pkg/controller/jobs/job/job_webhook.go:59** by [@mimowo](https://github.com/mimowo) — 2026-01-14T09:44:40Z
```diff
@@ -55,17 +56,128 @@ var (
 // behavior in Kueue-managed Jobs, allowing for fine-grained control over
 // resource allocation and scheduling decisions.
 //
+// Exception: Batch jobs owned by ray.io/v1/RayCluster with the label "ray.io/node-type: redis-cleanup"
+// are excluded from getting scheduling gates because (a) Kueue workload podsets do not account for
+// redis cleanup pods, and (b) redis-cleanup jobs do not need autoscaling (elastic job).
```

  Are these Ray redis cleanup jobs also created for non-autoscaling clusters?
  
  If so we may need to also adjust (separete PR) the IsTopLevel function so that Kueue will create the Workload object properly, similarly as we do it for the Ray submitter job.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-14T09:46:00Z

- **pkg/controller/jobs/raycluster/raycluster_webhook.go:96** by [@mimowo](https://github.com/mimowo) — 2026-01-14T09:46:00Z
```diff
@@ -87,23 +90,88 @@ func (w *RayClusterWebhook) Default(ctx context.Context, obj runtime.Object) err
 	job := fromObject(obj)
 	log := ctrl.LoggerFrom(ctx).WithName("raycluster-webhook")
 	log.V(10).Info("Applying defaults")
+
 	jobframework.ApplyDefaultLocalQueue(job.Object(), w.queues.DefaultLocalQueueExist)
+
+	oldSuspendState := job.IsSuspended()
```

  IIUC all this code here is just for logging, I would prefer a seprate PR for debuggability only, to make the reviewing easier, and so that we can focus here on the bug.

### Review by [@ns-sundar](https://github.com/ns-sundar) (commented) — 2026-01-14T15:46:44Z

- **pkg/controller/jobframework/reconciler.go:1171** by [@ns-sundar](https://github.com/ns-sundar) — 2026-01-14T15:46:44Z
```diff
@@ -1164,6 +1164,14 @@ func (r *JobReconciler) startJob(ctx context.Context, job GenericJob, object cli
 		}
 	}
 
+	// Log job unsuspend event with GVK information
+	log = ctrl.LoggerFrom(ctx)
+	log.V(3).Info("Unsuspending job for admission",
+		"name", object.GetName(),
+		"namespace", object.GetNamespace(),
```

  I am fine with dropping the logging changes, though I found them useful. How about the K8s events through recording framework? It is really useful to watch events from KubeRay and Kueue together IMHO.

### Review by [@ns-sundar](https://github.com/ns-sundar) (commented) — 2026-01-14T15:52:51Z

- **pkg/controller/jobs/job/job_webhook.go:59** by [@ns-sundar](https://github.com/ns-sundar) — 2026-01-14T15:52:51Z
```diff
@@ -55,17 +56,128 @@ var (
 // behavior in Kueue-managed Jobs, allowing for fine-grained control over
 // resource allocation and scheduling decisions.
 //
+// Exception: Batch jobs owned by ray.io/v1/RayCluster with the label "ray.io/node-type: redis-cleanup"
+// are excluded from getting scheduling gates because (a) Kueue workload podsets do not account for
+// redis cleanup pods, and (b) redis-cleanup jobs do not need autoscaling (elastic job).
```

  Yes, they also get created for non-autoscaling clusters. That's a good point. 
  
  Since these redis-cleanup jobs are never autoscaled and generally take little CPU/memory (and no GPUs, etc.), is it worth representing it as a workload? Just a question - not pushing back.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-14T16:40:04Z

- **pkg/controller/jobs/job/job_webhook.go:59** by [@mimowo](https://github.com/mimowo) — 2026-01-14T16:40:03Z
```diff
@@ -55,17 +56,128 @@ var (
 // behavior in Kueue-managed Jobs, allowing for fine-grained control over
 // resource allocation and scheduling decisions.
 //
+// Exception: Batch jobs owned by ray.io/v1/RayCluster with the label "ray.io/node-type: redis-cleanup"
+// are excluded from getting scheduling gates because (a) Kueue workload podsets do not account for
+// redis cleanup pods, and (b) redis-cleanup jobs do not need autoscaling (elastic job).
```

  I think so, one is for consistency, but two is also about correctness - I think the RedisCleanup Job otherwise may get stuck if `manageJobWithoutQueuename` is enabled in the configuration, because this results in suspending all jobs without the queue-name.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-22T16:17:13Z

Please also add an e2e which would demonstrate the need for the fix, and that the fix it helping

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-23T05:32:49Z

Thanks for the PR. I did a quick review and added a few comments.

- **pkg/controller/jobs/job/job_webhook.go:66** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-23T05:07:39Z
```diff
@@ -53,14 +53,60 @@ var (
 // behavior in Kueue-managed Jobs, allowing for fine-grained control over
 // resource allocation and scheduling decisions.
 //
+// Exception: Batch jobs owned by ray.io/v1/RayCluster with the label "ray.io/node-type: redis-cleanup"
+// are excluded from getting scheduling gates because (a) Kueue workload podsets do not account for
+// redis cleanup pods, and (b) redis-cleanup jobs do not need autoscaling (elastic job).
+//
 // Parameters:
+//   - ctx: Context for logging
 //   - job: Pointer to the Job object to be modified.
 //
 // Note: This function modifies the Job's pod template in-place.
-func applyWorkloadSliceSchedulingGate(job *Job) {
-	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) || !workloadslicing.Enabled(job.Object()) {
+func applyWorkloadSliceSchedulingGate(ctx context.Context, job *Job) {
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
```

  The logger from ctx already has the webhook name from controller-runtime. Adding .WithName("job-webhook") creates a nested name like `raycluster-webhook.job-webhook`. Drop it.

- **pkg/controller/jobs/job/job_webhook.go:69** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-23T05:08:45Z
```diff
@@ -53,14 +53,60 @@ var (
 // behavior in Kueue-managed Jobs, allowing for fine-grained control over
 // resource allocation and scheduling decisions.
 //
+// Exception: Batch jobs owned by ray.io/v1/RayCluster with the label "ray.io/node-type: redis-cleanup"
+// are excluded from getting scheduling gates because (a) Kueue workload podsets do not account for
+// redis cleanup pods, and (b) redis-cleanup jobs do not need autoscaling (elastic job).
+//
 // Parameters:
+//   - ctx: Context for logging
 //   - job: Pointer to the Job object to be modified.
 //
 // Note: This function modifies the Job's pod template in-place.
-func applyWorkloadSliceSchedulingGate(job *Job) {
-	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) || !workloadslicing.Enabled(job.Object()) {
+func applyWorkloadSliceSchedulingGate(ctx context.Context, job *Job) {
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+
+	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) {
+		log.V(3).Info("ElasticJobsViaWorkloadSlices feature disabled, skipping scheduling gate",
```

  Drop this log line entirely. Early return is self-explanatory.

- **pkg/controller/jobs/job/job_webhook.go:80** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-23T05:10:15Z
```diff
@@ -53,14 +53,60 @@ var (
 // behavior in Kueue-managed Jobs, allowing for fine-grained control over
 // resource allocation and scheduling decisions.
 //
+// Exception: Batch jobs owned by ray.io/v1/RayCluster with the label "ray.io/node-type: redis-cleanup"
+// are excluded from getting scheduling gates because (a) Kueue workload podsets do not account for
+// redis cleanup pods, and (b) redis-cleanup jobs do not need autoscaling (elastic job).
+//
 // Parameters:
+//   - ctx: Context for logging
 //   - job: Pointer to the Job object to be modified.
 //
 // Note: This function modifies the Job's pod template in-place.
-func applyWorkloadSliceSchedulingGate(job *Job) {
-	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) || !workloadslicing.Enabled(job.Object()) {
+func applyWorkloadSliceSchedulingGate(ctx context.Context, job *Job) {
+	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
+
+	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) {
+		log.V(3).Info("ElasticJobsViaWorkloadSlices feature disabled, skipping scheduling gate",
+			"jobName", job.Name,
+			"jobNamespace", job.Namespace)
+		return
+	}
+
+	// For redis_cleanup jobs generated by KubeRay's RayCluster, the workload slicing
+	// annotation will not be set. So, workloadslicing.Enabled(job.Object()) will
+	// return false. Still, we should rm the elastic job scheduling gate.
+
+	if isRayRedisCleanupJob(ctx, job) {
+		log.V(3).Info("Detected Ray redis-cleanup job - removing any inherited elastic scheduling gates",
```

  `s/redis-cleanup/redis_cleanup/g`

- **pkg/controller/jobs/job/job_webhook.go:66** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-23T05:13:28Z
```diff
@@ -53,14 +53,60 @@ var (
 // behavior in Kueue-managed Jobs, allowing for fine-grained control over
 // resource allocation and scheduling decisions.
 //
+// Exception: Batch jobs owned by ray.io/v1/RayCluster with the label "ray.io/node-type: redis-cleanup"
+// are excluded from getting scheduling gates because (a) Kueue workload podsets do not account for
+// redis cleanup pods, and (b) redis-cleanup jobs do not need autoscaling (elastic job).
+//
 // Parameters:
+//   - ctx: Context for logging
 //   - job: Pointer to the Job object to be modified.
 //
 // Note: This function modifies the Job's pod template in-place.
-func applyWorkloadSliceSchedulingGate(job *Job) {
-	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) || !workloadslicing.Enabled(job.Object()) {
+func applyWorkloadSliceSchedulingGate(ctx context.Context, job *Job) {
```

  I would suggest reducing the logging to only the necessary places to reduce noise.

- **pkg/controller/jobs/job/ray_utils.go:56** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-23T05:14:55Z
```diff
@@ -0,0 +1,168 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"context"
+
+	rayv1 "github.com/ray-project/kuberay/ray-operator/apis/ray/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+// getRayOwnerReference finds an owner reference with the specified Ray kind and API version.
+// For controller owners (like RayJob), it uses metav1.GetControllerOf.
+// For non-controller owners (like RayCluster), it searches through all owner references.
+func getRayOwnerReference(jobObj client.Object, kind, apiVersion string, controllerOnly bool) *metav1.OwnerReference {
+	if controllerOnly {
+		// For controller owners like RayJob
+		owner := metav1.GetControllerOf(jobObj)
+		if owner != nil && owner.APIVersion == apiVersion && owner.Kind == kind {
+			return owner
+		}
+		return nil
+	}
+
+	// For any owner reference like RayCluster
+	for i, owner := range jobObj.GetOwnerReferences() {
+		if owner.Kind == kind && owner.APIVersion == apiVersion {
+			return &jobObj.GetOwnerReferences()[i]
+		}
+	}
+	return nil
+}
+
+//lint:ignore U1000 Ignore unused function
+func isRaySubmitterJobWithAutoScaling(ctx context.Context, jobObj client.Object, k8sClient client.Client) (bool, *rayv1.RayJob, error) {
```

  This is unused dead code. Don't check in dead code. Add it when it's needed

- **pkg/controller/jobs/job/ray_utils.go:96** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-23T05:15:12Z
```diff
@@ -0,0 +1,168 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"context"
+
+	rayv1 "github.com/ray-project/kuberay/ray-operator/apis/ray/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+// getRayOwnerReference finds an owner reference with the specified Ray kind and API version.
+// For controller owners (like RayJob), it uses metav1.GetControllerOf.
+// For non-controller owners (like RayCluster), it searches through all owner references.
+func getRayOwnerReference(jobObj client.Object, kind, apiVersion string, controllerOnly bool) *metav1.OwnerReference {
+	if controllerOnly {
+		// For controller owners like RayJob
+		owner := metav1.GetControllerOf(jobObj)
+		if owner != nil && owner.APIVersion == apiVersion && owner.Kind == kind {
+			return owner
+		}
+		return nil
+	}
+
+	// For any owner reference like RayCluster
+	for i, owner := range jobObj.GetOwnerReferences() {
+		if owner.Kind == kind && owner.APIVersion == apiVersion {
+			return &jobObj.GetOwnerReferences()[i]
+		}
+	}
+	return nil
+}
+
+//lint:ignore U1000 Ignore unused function
+func isRaySubmitterJobWithAutoScaling(ctx context.Context, jobObj client.Object, k8sClient client.Client) (bool, *rayv1.RayJob, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("jobName", jobObj.GetName(), "jobNamespace", jobObj.GetNamespace())
+
+	owner := getRayOwnerReference(jobObj, "RayJob", rayv1.GroupVersion.String(), true)
+	if owner == nil {
+		log.V(5).Info("Did not find RayJob owner for job")
+		return false, nil, nil
+	}
+
+	parentObj := jobframework.GetEmptyOwnerObject(owner)
+	if parentObj == nil {
+		log.V(5).Info("Did not get empty owner object for job", "ownerKind", owner.Kind, "ownerName", owner.Name)
+		return false, nil, nil
+	}
+
+	err := k8sClient.Get(ctx, client.ObjectKey{Name: owner.Name, Namespace: jobObj.GetNamespace()}, parentObj)
+	if err != nil {
+		log.Error(err, "Failed to get owner object from k8s", "ownerKind", owner.Kind, "ownerName", owner.Name)
+		return false, nil, err
+	}
+	log.V(5).Info("Got owner object for job", "ownerKind", owner.Kind, "ownerName", owner.Name)
+
+	rayJob, ok := parentObj.(*rayv1.RayJob)
+	if !ok {
+		log.V(5).Info("Owner object cannot be converted to RayJob", "ownerKind", owner.Kind, "ownerName", owner.Name)
+		return false, nil, nil
+	}
+
+	if rayJob.Spec.RayClusterSpec == nil ||
+		!ptr.Deref(rayJob.Spec.RayClusterSpec.EnableInTreeAutoscaling, false) {
+		return false, nil, nil
+	}
+
+	return true, rayJob, nil
+}
+
+// copyRaySubmitterJobMetadata checks whether the job is Ray submitter job, if it is, copy queue label
+// and workload slicing annotation from the owner RayJob to the submitter job.
+//
+//lint:ignore U1000 Ignore unused function
+func copyRaySubmitterJobMetadata(ctx context.Context, jobObj client.Object, k8sClient client.Client) error {
```

  same https://github.com/kubernetes-sigs/kueue/pull/8579/changes#r2839110040

- **pkg/controller/jobs/job/ray_utils.go:36** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-23T05:26:30Z
```diff
@@ -0,0 +1,168 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"context"
+
+	rayv1 "github.com/ray-project/kuberay/ray-operator/apis/ray/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+// getRayOwnerReference finds an owner reference with the specified Ray kind and API version.
+// For controller owners (like RayJob), it uses metav1.GetControllerOf.
+// For non-controller owners (like RayCluster), it searches through all owner references.
+func getRayOwnerReference(jobObj client.Object, kind, apiVersion string, controllerOnly bool) *metav1.OwnerReference {
```

  The `controllerOnly` bool parameter makes this generic but only `isRayRedisCleanupJob` uses it with `controllerOnly=true`. And the unused functions use it with both values. Since the unused functions should be removed, simplify this to just check controller owner via `metav1.GetControllerOf`.

- **pkg/controller/jobs/job/ray_utils.go:135** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-23T05:28:58Z
```diff
@@ -0,0 +1,168 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"context"
+
+	rayv1 "github.com/ray-project/kuberay/ray-operator/apis/ray/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+// getRayOwnerReference finds an owner reference with the specified Ray kind and API version.
+// For controller owners (like RayJob), it uses metav1.GetControllerOf.
+// For non-controller owners (like RayCluster), it searches through all owner references.
+func getRayOwnerReference(jobObj client.Object, kind, apiVersion string, controllerOnly bool) *metav1.OwnerReference {
+	if controllerOnly {
+		// For controller owners like RayJob
+		owner := metav1.GetControllerOf(jobObj)
+		if owner != nil && owner.APIVersion == apiVersion && owner.Kind == kind {
+			return owner
+		}
+		return nil
+	}
+
+	// For any owner reference like RayCluster
+	for i, owner := range jobObj.GetOwnerReferences() {
+		if owner.Kind == kind && owner.APIVersion == apiVersion {
+			return &jobObj.GetOwnerReferences()[i]
+		}
+	}
+	return nil
+}
+
+//lint:ignore U1000 Ignore unused function
+func isRaySubmitterJobWithAutoScaling(ctx context.Context, jobObj client.Object, k8sClient client.Client) (bool, *rayv1.RayJob, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("jobName", jobObj.GetName(), "jobNamespace", jobObj.GetNamespace())
+
+	owner := getRayOwnerReference(jobObj, "RayJob", rayv1.GroupVersion.String(), true)
+	if owner == nil {
+		log.V(5).Info("Did not find RayJob owner for job")
+		return false, nil, nil
+	}
+
+	parentObj := jobframework.GetEmptyOwnerObject(owner)
+	if parentObj == nil {
+		log.V(5).Info("Did not get empty owner object for job", "ownerKind", owner.Kind, "ownerName", owner.Name)
+		return false, nil, nil
+	}
+
+	err := k8sClient.Get(ctx, client.ObjectKey{Name: owner.Name, Namespace: jobObj.GetNamespace()}, parentObj)
+	if err != nil {
+		log.Error(err, "Failed to get owner object from k8s", "ownerKind", owner.Kind, "ownerName", owner.Name)
+		return false, nil, err
+	}
+	log.V(5).Info("Got owner object for job", "ownerKind", owner.Kind, "ownerName", owner.Name)
+
+	rayJob, ok := parentObj.(*rayv1.RayJob)
+	if !ok {
+		log.V(5).Info("Owner object cannot be converted to RayJob", "ownerKind", owner.Kind, "ownerName", owner.Name)
+		return false, nil, nil
+	}
+
+	if rayJob.Spec.RayClusterSpec == nil ||
+		!ptr.Deref(rayJob.Spec.RayClusterSpec.EnableInTreeAutoscaling, false) {
+		return false, nil, nil
+	}
+
+	return true, rayJob, nil
+}
+
+// copyRaySubmitterJobMetadata checks whether the job is Ray submitter job, if it is, copy queue label
+// and workload slicing annotation from the owner RayJob to the submitter job.
+//
+//lint:ignore U1000 Ignore unused function
+func copyRaySubmitterJobMetadata(ctx context.Context, jobObj client.Object, k8sClient client.Client) error {
+	if jobframework.QueueNameForObject(jobObj) != "" {
+		return nil
+	}
+
+	// Copy label and annotation for Ray submitter job with autoscaling
+	// See https://github.com/kubernetes-sigs/kueue/pull/8082
+	isRaySubmitterJob, parentObj, err := isRaySubmitterJobWithAutoScaling(ctx, jobObj, k8sClient)
+	if err != nil {
+		return err
+	}
+	if !isRaySubmitterJob {
+		return nil
+	}
+
+	queueName := parentObj.GetLabels()[constants.QueueLabel]
+	if queueName != "" {
+		jobLabels := jobObj.GetLabels()
+		if jobLabels == nil {
+			jobLabels = make(map[string]string, 1)
+		}
+		jobLabels[constants.QueueLabel] = queueName
+		jobObj.SetLabels(jobLabels)
+	}
+
+	workloadslicingAnnotationValue := parentObj.GetAnnotations()[workloadslicing.EnabledAnnotationKey]
+	if workloadslicingAnnotationValue != "" {
+		jobAnnotations := jobObj.GetAnnotations()
+		if jobAnnotations == nil {
+			jobAnnotations = make(map[string]string, 1)
+		}
+		jobAnnotations[workloadslicing.EnabledAnnotationKey] = workloadslicingAnnotationValue
+		jobObj.SetAnnotations(jobAnnotations)
+	}
+	return nil
+}
+
+// isRayRedisCleanupJob checks if the batch job is owned by a RayCluster and has the redis-cleanup label.
+// Returns true if the job should be excluded from getting scheduling gates.
+func isRayRedisCleanupJob(ctx context.Context, job *Job) bool {
```

  Consider dropping ctx and the V(5) logs. Also, this function shouldn't need to call `job.Object()` as the Job type already embeds batchv1.Job so `job.GetLabels()` and `job.GetOwnerReferences()` work directly.

- **pkg/controller/jobs/raycluster/raycluster_webhook.go:92** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-23T05:30:15Z
```diff
@@ -89,12 +89,15 @@ func (w *RayClusterWebhook) Default(ctx context.Context, obj *rayv1.RayCluster)
 	log := ctrl.LoggerFrom(ctx).WithName("raycluster-webhook")
 	log.V(10).Info("Applying defaults")
 	jobframework.ApplyDefaultLocalQueue(job.Object(), w.queues.DefaultLocalQueueExist)
+
```

  Stray line, drop this change.

- **pkg/controller/jobs/raycluster/raycluster_webhook.go:98** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-23T05:31:18Z
```diff
@@ -89,12 +89,15 @@ func (w *RayClusterWebhook) Default(ctx context.Context, obj *rayv1.RayCluster)
 	log := ctrl.LoggerFrom(ctx).WithName("raycluster-webhook")
 	log.V(10).Info("Applying defaults")
 	jobframework.ApplyDefaultLocalQueue(job.Object(), w.queues.DefaultLocalQueueExist)
+
 	if err := jobframework.ApplyDefaultForSuspend(ctx, job, w.client, w.manageJobsWithoutQueueName, w.managedJobsNamespaceSelector); err != nil {
 		return err
 	}
 	jobframework.ApplyDefaultForManagedBy(job, w.queues, w.cache, log)
 
-	if isAnElasticJob(obj) {
+	elasticJobEnabled := isAnElasticJob(obj)
```

  Not sure about the intention here but you can drop this change.

### Review by [@ns-sundar](https://github.com/ns-sundar) (commented) — 2026-02-25T16:29:04Z

- **pkg/controller/jobs/job/job_webhook.go:66** by [@ns-sundar](https://github.com/ns-sundar) — 2026-02-25T06:55:54Z
```diff
@@ -53,14 +53,60 @@ var (
 // behavior in Kueue-managed Jobs, allowing for fine-grained control over
 // resource allocation and scheduling decisions.
 //
+// Exception: Batch jobs owned by ray.io/v1/RayCluster with the label "ray.io/node-type: redis-cleanup"
+// are excluded from getting scheduling gates because (a) Kueue workload podsets do not account for
+// redis cleanup pods, and (b) redis-cleanup jobs do not need autoscaling (elastic job).
+//
 // Parameters:
+//   - ctx: Context for logging
 //   - job: Pointer to the Job object to be modified.
 //
 // Note: This function modifies the Job's pod template in-place.
-func applyWorkloadSliceSchedulingGate(job *Job) {
-	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) || !workloadslicing.Enabled(job.Object()) {
+func applyWorkloadSliceSchedulingGate(ctx context.Context, job *Job) {
```

  OK. I found some logs useful and I have reduced their log level. Applying your specific log-related comments below.

- **pkg/controller/jobs/raycluster/raycluster_webhook.go:98** by [@ns-sundar](https://github.com/ns-sundar) — 2026-02-25T16:28:50Z
```diff
@@ -89,12 +89,15 @@ func (w *RayClusterWebhook) Default(ctx context.Context, obj *rayv1.RayCluster)
 	log := ctrl.LoggerFrom(ctx).WithName("raycluster-webhook")
 	log.V(10).Info("Applying defaults")
 	jobframework.ApplyDefaultLocalQueue(job.Object(), w.queues.DefaultLocalQueueExist)
+
 	if err := jobframework.ApplyDefaultForSuspend(ctx, job, w.client, w.manageJobsWithoutQueueName, w.managedJobsNamespaceSelector); err != nil {
 		return err
 	}
 	jobframework.ApplyDefaultForManagedBy(job, w.queues, w.cache, log)
 
-	if isAnElasticJob(obj) {
+	elasticJobEnabled := isAnElasticJob(obj)
```

  It was originally introduced for logging. Dropped it now.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2026-04-17T14:06:32Z

- **test/e2e/singlecluster/kuberay_test.go:832** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2026-04-17T14:06:32Z
```diff
@@ -777,4 +778,137 @@ app = HelloWorld.bind()`,
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("Should ensure redis-cleanup pods do not have scheduling gates for Ray Cluster with GCS FT", func() {
+		kuberayTestImage := util.GetKuberayTestImage()
+
+		// Environment variables for GCS fault tolerance (applied to Ray pods)
+		env := []corev1.EnvVar{
+			{
+				Name:  "RAY_external_storage_namespace",
+				Value: ns.Name, // Use current test namespace as storage namespace
+			},
+			{
+				Name:  "RAY_REDIS_ADDRESS",
+				Value: "redis://my-redis-service:6379",
+			},
+		}
+
+		// Create a RayCluster with GCS fault tolerance enabled
+		rayCluster := testingraycluster.MakeCluster("raycluster-gcs-ft", ns.Name).
+			Suspend(true).
+			Queue(localQueueName).
+			RequestAndLimit(rayv1.HeadNode, corev1.ResourceCPU, "300m").
+			RequestAndLimit(rayv1.WorkerNode, corev1.ResourceCPU, "300m").
+			Image(rayv1.HeadNode, kuberayTestImage, []string{}).
+			Image(rayv1.WorkerNode, kuberayTestImage, []string{}).
+			Env(rayv1.HeadNode, env).
+			Env(rayv1.WorkerNode, env).
+			SetAnnotation("ray.io/ft-enabled", "true").
+			Obj()
+
+		// Add GCS fault tolerance finalizer to trigger redis-cleanup job creation on deletion
+		rayCluster.Finalizers = append(rayCluster.Finalizers, "ray.io/gcs-ft-redis-cleanup-finalizer")
+
+		ginkgo.By("Creating the RayCluster with GCS FT finalizer", func() {
+			gomega.Expect(k8sClient.Create(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		wlLookupKey := types.NamespacedName{Name: workloadraycluster.GetWorkloadNameForRayCluster(rayCluster.Name, rayCluster.UID), Namespace: ns.Name}
+		createdWorkload := &kueue.Workload{}
+		ginkgo.By("Checking workload is created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking workload is admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, createdWorkload)
+		})
+
+		ginkgo.By("Checking the RayCluster is ready", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdRayCluster := &rayv1.RayCluster{}
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(rayCluster), createdRayCluster)).To(gomega.Succeed())
```

  Could you verify that the head node is ready before checking the worker node?

### Review by [@ns-sundar](https://github.com/ns-sundar) (commented) — 2026-04-17T16:08:22Z

- **test/e2e/singlecluster/kuberay_test.go:832** by [@ns-sundar](https://github.com/ns-sundar) — 2026-04-17T16:08:22Z
```diff
@@ -777,4 +778,137 @@ app = HelloWorld.bind()`,
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("Should ensure redis-cleanup pods do not have scheduling gates for Ray Cluster with GCS FT", func() {
+		kuberayTestImage := util.GetKuberayTestImage()
+
+		// Environment variables for GCS fault tolerance (applied to Ray pods)
+		env := []corev1.EnvVar{
+			{
+				Name:  "RAY_external_storage_namespace",
+				Value: ns.Name, // Use current test namespace as storage namespace
+			},
+			{
+				Name:  "RAY_REDIS_ADDRESS",
+				Value: "redis://my-redis-service:6379",
+			},
+		}
+
+		// Create a RayCluster with GCS fault tolerance enabled
+		rayCluster := testingraycluster.MakeCluster("raycluster-gcs-ft", ns.Name).
+			Suspend(true).
+			Queue(localQueueName).
+			RequestAndLimit(rayv1.HeadNode, corev1.ResourceCPU, "300m").
+			RequestAndLimit(rayv1.WorkerNode, corev1.ResourceCPU, "300m").
+			Image(rayv1.HeadNode, kuberayTestImage, []string{}).
+			Image(rayv1.WorkerNode, kuberayTestImage, []string{}).
+			Env(rayv1.HeadNode, env).
+			Env(rayv1.WorkerNode, env).
+			SetAnnotation("ray.io/ft-enabled", "true").
+			Obj()
+
+		// Add GCS fault tolerance finalizer to trigger redis-cleanup job creation on deletion
+		rayCluster.Finalizers = append(rayCluster.Finalizers, "ray.io/gcs-ft-redis-cleanup-finalizer")
+
+		ginkgo.By("Creating the RayCluster with GCS FT finalizer", func() {
+			gomega.Expect(k8sClient.Create(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		wlLookupKey := types.NamespacedName{Name: workloadraycluster.GetWorkloadNameForRayCluster(rayCluster.Name, rayCluster.UID), Namespace: ns.Name}
+		createdWorkload := &kueue.Workload{}
+		ginkgo.By("Checking workload is created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking workload is admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, createdWorkload)
+		})
+
+		ginkgo.By("Checking the RayCluster is ready", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdRayCluster := &rayv1.RayCluster{}
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(rayCluster), createdRayCluster)).To(gomega.Succeed())
```

  The Ray Cluster comes up fine. All the steps through "Deleting the RayCluster to trigger redis-cleanup job creation" work fine. The "Waiting for redis-cleanup jobs to be created" step times out, however, after 300 seconds.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2026-04-20T12:50:18Z

- **test/e2e/singlecluster/kuberay_test.go:832** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2026-04-20T12:50:18Z
```diff
@@ -777,4 +778,137 @@ app = HelloWorld.bind()`,
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("Should ensure redis-cleanup pods do not have scheduling gates for Ray Cluster with GCS FT", func() {
+		kuberayTestImage := util.GetKuberayTestImage()
+
+		// Environment variables for GCS fault tolerance (applied to Ray pods)
+		env := []corev1.EnvVar{
+			{
+				Name:  "RAY_external_storage_namespace",
+				Value: ns.Name, // Use current test namespace as storage namespace
+			},
+			{
+				Name:  "RAY_REDIS_ADDRESS",
+				Value: "redis://my-redis-service:6379",
+			},
+		}
+
+		// Create a RayCluster with GCS fault tolerance enabled
+		rayCluster := testingraycluster.MakeCluster("raycluster-gcs-ft", ns.Name).
+			Suspend(true).
+			Queue(localQueueName).
+			RequestAndLimit(rayv1.HeadNode, corev1.ResourceCPU, "300m").
+			RequestAndLimit(rayv1.WorkerNode, corev1.ResourceCPU, "300m").
+			Image(rayv1.HeadNode, kuberayTestImage, []string{}).
+			Image(rayv1.WorkerNode, kuberayTestImage, []string{}).
+			Env(rayv1.HeadNode, env).
+			Env(rayv1.WorkerNode, env).
+			SetAnnotation("ray.io/ft-enabled", "true").
+			Obj()
+
+		// Add GCS fault tolerance finalizer to trigger redis-cleanup job creation on deletion
+		rayCluster.Finalizers = append(rayCluster.Finalizers, "ray.io/gcs-ft-redis-cleanup-finalizer")
+
+		ginkgo.By("Creating the RayCluster with GCS FT finalizer", func() {
+			gomega.Expect(k8sClient.Create(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		wlLookupKey := types.NamespacedName{Name: workloadraycluster.GetWorkloadNameForRayCluster(rayCluster.Name, rayCluster.UID), Namespace: ns.Name}
+		createdWorkload := &kueue.Workload{}
+		ginkgo.By("Checking workload is created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking workload is admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, createdWorkload)
+		})
+
+		ginkgo.By("Checking the RayCluster is ready", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdRayCluster := &rayv1.RayCluster{}
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(rayCluster), createdRayCluster)).To(gomega.Succeed())
```

  I checked the failed tests and the [pull-kueue-test-e2e-main-1-33](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/8579/pull-kueue-test-e2e-main-1-33/2044844116151373824) failed when checking ReadyWorkerReplicas 
  
  ```
  The function passed to Eventually /home/prow/go/src/sigs.k8s.io/kueue/test/e2e/singlecluster/kuberay_test.go:834 with:
  Expected
      <int32>: 0
  to equal
      <int32>: 1
  ```

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2026-05-04T18:06:07Z

- **pkg/controller/jobs/job/job_webhook.go:83** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2026-05-04T17:53:43Z
```diff
@@ -54,14 +54,57 @@ var (
 // behavior in Kueue-managed Jobs, allowing for fine-grained control over
 // resource allocation and scheduling decisions.
 //
+// Exception: Batch jobs owned by ray.io/v1/RayCluster with the label "ray.io/node-type: redis-cleanup"
+// are excluded from getting scheduling gates because (a) Kueue workload podsets do not account for
+// redis cleanup pods, and (b) redis-cleanup jobs do not need autoscaling (elastic job).
+//
 // Parameters:
+//   - ctx: Context for logging
 //   - job: Pointer to the Job object to be modified.
 //
 // Note: This function modifies the Job's pod template in-place.
-func applyWorkloadSliceSchedulingGate(job *Job) {
-	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) || !workloadslicing.Enabled(job.Object()) {
+func applyWorkloadSliceSchedulingGate(ctx context.Context, job *Job) {
+	log := ctrl.LoggerFrom(ctx)
+
+	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) {
+		return
+	}
+
+	// For redis_cleanup jobs generated by KubeRay's RayCluster, the workload slicing
+	// annotation will not be set. So, workloadslicing.Enabled(job.Object()) will
+	// return false. Still, we should rm the elastic job scheduling gate.
+
+	if isRayRedisCleanupJob(job) {
+		log.V(3).Info("Detected Ray redis_cleanup job - removing any inherited elastic scheduling gates",
+			"jobName", job.Name,
+			"jobNamespace", job.Namespace)
+
+		// Safely remove the elastic job scheduling gate if it exists (inherited from RayCluster template)
+		if utilpod.UngateTemplate(&job.Spec.Template, kueue.ElasticJobSchedulingGate) {
```

  Why we need this part and cannot just return here?

- **pkg/controller/jobs/job/job_webhook.go:96** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2026-05-04T18:02:01Z
```diff
@@ -54,14 +54,57 @@ var (
 // behavior in Kueue-managed Jobs, allowing for fine-grained control over
 // resource allocation and scheduling decisions.
 //
+// Exception: Batch jobs owned by ray.io/v1/RayCluster with the label "ray.io/node-type: redis-cleanup"
+// are excluded from getting scheduling gates because (a) Kueue workload podsets do not account for
+// redis cleanup pods, and (b) redis-cleanup jobs do not need autoscaling (elastic job).
+//
 // Parameters:
+//   - ctx: Context for logging
 //   - job: Pointer to the Job object to be modified.
 //
 // Note: This function modifies the Job's pod template in-place.
-func applyWorkloadSliceSchedulingGate(job *Job) {
-	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) || !workloadslicing.Enabled(job.Object()) {
+func applyWorkloadSliceSchedulingGate(ctx context.Context, job *Job) {
+	log := ctrl.LoggerFrom(ctx)
+
+	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) {
+		return
+	}
+
+	// For redis_cleanup jobs generated by KubeRay's RayCluster, the workload slicing
+	// annotation will not be set. So, workloadslicing.Enabled(job.Object()) will
+	// return false. Still, we should rm the elastic job scheduling gate.
+
+	if isRayRedisCleanupJob(job) {
+		log.V(3).Info("Detected Ray redis_cleanup job - removing any inherited elastic scheduling gates",
+			"jobName", job.Name,
+			"jobNamespace", job.Namespace)
+
+		// Safely remove the elastic job scheduling gate if it exists (inherited from RayCluster template)
+		if utilpod.UngateTemplate(&job.Spec.Template, kueue.ElasticJobSchedulingGate) {
+			log.V(3).Info("Successfully removed inherited elastic scheduling gate from redis-cleanup job",
+				"jobName", job.Name,
+				"jobNamespace", job.Namespace)
+		} else {
+			log.V(3).Info("No elastic scheduling gate found to remove from redis-cleanup job",
+				"jobName", job.Name,
+				"jobNamespace", job.Namespace)
+		}
 		return
 	}
+
+	// Only apply scheduling gate if the job is explicitly opted in via workload slicing annotation
+	if !workloadslicing.Enabled(job.Object()) {
```

  I suggest to leave the original version in line 69:  `if !features.Enabled(features.ElasticJobsViaWorkloadSlices) || !workloadslicing.Enabled(job.Object())` and remove the if statement here.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2026-05-05T06:16:14Z

- **test/e2e/singlecluster/kuberay_test.go:1030** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2026-05-05T06:09:22Z
```diff
@@ -789,4 +778,112 @@ app = HelloWorld.bind()`,
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("Should ensure redis-cleanup pods do not have scheduling gates for Ray Cluster with GCS FT", func() {
+		kuberayTestImage := util.GetKuberayTestImage()
+
+		rayCluster := testingraycluster.MakeCluster("raycluster-gcs-ft", ns.Name).
+			Suspend(true).
+			Queue(localQueueName).
+			RequestAndLimit(rayv1.HeadNode, corev1.ResourceCPU, "300m").
+			RequestAndLimit(rayv1.WorkerNode, corev1.ResourceCPU, "300m").
+			Image(rayv1.HeadNode, kuberayTestImage, []string{}).
+			Image(rayv1.WorkerNode, kuberayTestImage, []string{}).
+			SetAnnotation("ray.io/ft-enabled", "true").
+			Obj()
+
+		// Add GCS fault tolerance finalizer to trigger redis-cleanup job creation on deletion
+		rayCluster.Finalizers = append(rayCluster.Finalizers, "ray.io/gcs-ft-redis-cleanup-finalizer")
+
+		ginkgo.By("Creating the RayCluster with GCS FT finalizer", func() {
+			gomega.Expect(k8sClient.Create(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		wlLookupKey := types.NamespacedName{Name: workloadraycluster.GetWorkloadNameForRayCluster(rayCluster.Name, rayCluster.UID), Namespace: ns.Name}
+		createdWorkload := &kueue.Workload{}
+		ginkgo.By("Checking workload is created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking workload is admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, createdWorkload)
+		})
+
+		ginkgo.By("Deleting the RayCluster to trigger redis-cleanup job creation", func() {
+			gomega.Expect(k8sClient.Delete(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for redis-cleanup jobs to be created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				jobList := &batchv1.JobList{}
+				g.Expect(k8sClient.List(ctx, jobList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+				g.Expect(jobList.Items).ToNot(gomega.BeEmpty(), "Expected at least one redis-cleanup job to be created")
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Verifying redis-cleanup job pods do not have scheduling gates", func() {
+			jobList := &batchv1.JobList{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.List(ctx, jobList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+
+				for _, job := range jobList.Items {
+					// Verify the job itself doesn't have scheduling gates in its pod template
+					schedulingGates := job.Spec.Template.Spec.SchedulingGates
+					for _, gate := range schedulingGates {
+						g.Expect(gate.Name).NotTo(gomega.Equal(kueue.ElasticJobSchedulingGate),
+							"Redis-cleanup job should not have ElasticJobSchedulingGate")
+					}
+
+					// Also check actual pods created by the job
+					podList := &corev1.PodList{}
+					g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name),
+						client.MatchingLabels{
+							"ray.io/node-type": "redis-cleanup",
+							"job-name":         job.Name,
+						})).To(gomega.Succeed())
+
+					for _, pod := range podList.Items {
+						for _, gate := range pod.Spec.SchedulingGates {
+							g.Expect(gate.Name).NotTo(gomega.Equal(kueue.ElasticJobSchedulingGate),
+								"Redis-cleanup pod should not have ElasticJobSchedulingGate")
+						}
+					}
+				}
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
```

  Since the job is already created we don't need to wait for a long time. Please, use shorter timeout, I think just Timeout should be fine. 
  ```suggestion
  			}, util.Timeout, util.Interval).Should(gomega.Succeed())
  ```

- **test/e2e/singlecluster/kuberay_test.go:1060** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2026-05-05T06:11:22Z
```diff
@@ -789,4 +778,112 @@ app = HelloWorld.bind()`,
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("Should ensure redis-cleanup pods do not have scheduling gates for Ray Cluster with GCS FT", func() {
+		kuberayTestImage := util.GetKuberayTestImage()
+
+		rayCluster := testingraycluster.MakeCluster("raycluster-gcs-ft", ns.Name).
+			Suspend(true).
+			Queue(localQueueName).
+			RequestAndLimit(rayv1.HeadNode, corev1.ResourceCPU, "300m").
+			RequestAndLimit(rayv1.WorkerNode, corev1.ResourceCPU, "300m").
+			Image(rayv1.HeadNode, kuberayTestImage, []string{}).
+			Image(rayv1.WorkerNode, kuberayTestImage, []string{}).
+			SetAnnotation("ray.io/ft-enabled", "true").
+			Obj()
+
+		// Add GCS fault tolerance finalizer to trigger redis-cleanup job creation on deletion
+		rayCluster.Finalizers = append(rayCluster.Finalizers, "ray.io/gcs-ft-redis-cleanup-finalizer")
+
+		ginkgo.By("Creating the RayCluster with GCS FT finalizer", func() {
+			gomega.Expect(k8sClient.Create(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		wlLookupKey := types.NamespacedName{Name: workloadraycluster.GetWorkloadNameForRayCluster(rayCluster.Name, rayCluster.UID), Namespace: ns.Name}
+		createdWorkload := &kueue.Workload{}
+		ginkgo.By("Checking workload is created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking workload is admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, createdWorkload)
+		})
+
+		ginkgo.By("Deleting the RayCluster to trigger redis-cleanup job creation", func() {
+			gomega.Expect(k8sClient.Delete(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for redis-cleanup jobs to be created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				jobList := &batchv1.JobList{}
+				g.Expect(k8sClient.List(ctx, jobList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+				g.Expect(jobList.Items).ToNot(gomega.BeEmpty(), "Expected at least one redis-cleanup job to be created")
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Verifying redis-cleanup job pods do not have scheduling gates", func() {
+			jobList := &batchv1.JobList{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.List(ctx, jobList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+
+				for _, job := range jobList.Items {
+					// Verify the job itself doesn't have scheduling gates in its pod template
+					schedulingGates := job.Spec.Template.Spec.SchedulingGates
+					for _, gate := range schedulingGates {
+						g.Expect(gate.Name).NotTo(gomega.Equal(kueue.ElasticJobSchedulingGate),
+							"Redis-cleanup job should not have ElasticJobSchedulingGate")
+					}
+
+					// Also check actual pods created by the job
+					podList := &corev1.PodList{}
+					g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name),
+						client.MatchingLabels{
+							"ray.io/node-type": "redis-cleanup",
+							"job-name":         job.Name,
+						})).To(gomega.Succeed())
+
+					for _, pod := range podList.Items {
+						for _, gate := range pod.Spec.SchedulingGates {
+							g.Expect(gate.Name).NotTo(gomega.Equal(kueue.ElasticJobSchedulingGate),
+								"Redis-cleanup pod should not have ElasticJobSchedulingGate")
+						}
+					}
+				}
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Verifying redis-cleanup pods can be scheduled immediately", func() {
+			podList := &corev1.PodList{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+
+				// Verify at least one redis-cleanup pod exists and is not blocked by scheduling gates
+				g.Expect(podList.Items).ToNot(gomega.BeEmpty(), "Expected at least one redis-cleanup pod")
+
+				// Check that pods are not stuck in Pending phase due to scheduling gates
+				for _, pod := range podList.Items {
+					// Pod should either be Running/Succeeded or Pending with a reason other than scheduling gates
+					if pod.Status.Phase == corev1.PodPending {
+						// If pending, it should not be due to scheduling gates
+						hasSchedulingGate := false
+						for _, condition := range pod.Status.Conditions {
+							if condition.Type == corev1.PodScheduled && condition.Status == corev1.ConditionFalse {
+								if condition.Reason == "SchedulingGated" {
+									hasSchedulingGate = true
+									break
+								}
+							}
+						}
+						g.Expect(hasSchedulingGate).To(gomega.BeFalse(),
+							"Redis-cleanup pod should not be gated by scheduling gates")
+					}
+				}
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
```

  Same here
  ```suggestion
  			}, util.Timeout, util.Interval).Should(gomega.Succeed())
  ```

- **test/e2e/singlecluster/kuberay_test.go:1033** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2026-05-05T06:13:53Z
```diff
@@ -789,4 +778,112 @@ app = HelloWorld.bind()`,
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("Should ensure redis-cleanup pods do not have scheduling gates for Ray Cluster with GCS FT", func() {
+		kuberayTestImage := util.GetKuberayTestImage()
+
+		rayCluster := testingraycluster.MakeCluster("raycluster-gcs-ft", ns.Name).
+			Suspend(true).
+			Queue(localQueueName).
+			RequestAndLimit(rayv1.HeadNode, corev1.ResourceCPU, "300m").
+			RequestAndLimit(rayv1.WorkerNode, corev1.ResourceCPU, "300m").
+			Image(rayv1.HeadNode, kuberayTestImage, []string{}).
+			Image(rayv1.WorkerNode, kuberayTestImage, []string{}).
+			SetAnnotation("ray.io/ft-enabled", "true").
+			Obj()
+
+		// Add GCS fault tolerance finalizer to trigger redis-cleanup job creation on deletion
+		rayCluster.Finalizers = append(rayCluster.Finalizers, "ray.io/gcs-ft-redis-cleanup-finalizer")
+
+		ginkgo.By("Creating the RayCluster with GCS FT finalizer", func() {
+			gomega.Expect(k8sClient.Create(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		wlLookupKey := types.NamespacedName{Name: workloadraycluster.GetWorkloadNameForRayCluster(rayCluster.Name, rayCluster.UID), Namespace: ns.Name}
+		createdWorkload := &kueue.Workload{}
+		ginkgo.By("Checking workload is created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking workload is admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, createdWorkload)
+		})
+
+		ginkgo.By("Deleting the RayCluster to trigger redis-cleanup job creation", func() {
+			gomega.Expect(k8sClient.Delete(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for redis-cleanup jobs to be created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				jobList := &batchv1.JobList{}
+				g.Expect(k8sClient.List(ctx, jobList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+				g.Expect(jobList.Items).ToNot(gomega.BeEmpty(), "Expected at least one redis-cleanup job to be created")
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Verifying redis-cleanup job pods do not have scheduling gates", func() {
+			jobList := &batchv1.JobList{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.List(ctx, jobList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+
+				for _, job := range jobList.Items {
+					// Verify the job itself doesn't have scheduling gates in its pod template
+					schedulingGates := job.Spec.Template.Spec.SchedulingGates
+					for _, gate := range schedulingGates {
+						g.Expect(gate.Name).NotTo(gomega.Equal(kueue.ElasticJobSchedulingGate),
+							"Redis-cleanup job should not have ElasticJobSchedulingGate")
+					}
+
+					// Also check actual pods created by the job
+					podList := &corev1.PodList{}
+					g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name),
+						client.MatchingLabels{
+							"ray.io/node-type": "redis-cleanup",
+							"job-name":         job.Name,
+						})).To(gomega.Succeed())
+
+					for _, pod := range podList.Items {
+						for _, gate := range pod.Spec.SchedulingGates {
+							g.Expect(gate.Name).NotTo(gomega.Equal(kueue.ElasticJobSchedulingGate),
+								"Redis-cleanup pod should not have ElasticJobSchedulingGate")
+						}
+					}
+				}
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Verifying redis-cleanup pods can be scheduled immediately", func() {
```

  I suggest to remove this phase, because we already checked that the pods are not scheduled gated.

- **test/e2e/singlecluster/kuberay_test.go:959** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2026-05-05T06:16:06Z
```diff
@@ -789,4 +778,112 @@ app = HelloWorld.bind()`,
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("Should ensure redis-cleanup pods do not have scheduling gates for Ray Cluster with GCS FT", func() {
+		kuberayTestImage := util.GetKuberayTestImage()
+
+		rayCluster := testingraycluster.MakeCluster("raycluster-gcs-ft", ns.Name).
```

  Let's add TODO to use RayService instead of RayCluster when https://github.com/kubernetes-sigs/kueue/issues/10795 is fixed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-05T06:21:08Z

- **test/e2e/singlecluster/kuberay_test.go:1055** by [@mimowo](https://github.com/mimowo) — 2026-05-05T06:21:08Z
```diff
@@ -789,4 +778,112 @@ app = HelloWorld.bind()`,
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("Should ensure redis-cleanup pods do not have scheduling gates for Ray Cluster with GCS FT", func() {
+		kuberayTestImage := util.GetKuberayTestImage()
+
+		rayCluster := testingraycluster.MakeCluster("raycluster-gcs-ft", ns.Name).
+			Suspend(true).
+			Queue(localQueueName).
+			RequestAndLimit(rayv1.HeadNode, corev1.ResourceCPU, "300m").
+			RequestAndLimit(rayv1.WorkerNode, corev1.ResourceCPU, "300m").
+			Image(rayv1.HeadNode, kuberayTestImage, []string{}).
+			Image(rayv1.WorkerNode, kuberayTestImage, []string{}).
+			SetAnnotation("ray.io/ft-enabled", "true").
+			Obj()
+
+		// Add GCS fault tolerance finalizer to trigger redis-cleanup job creation on deletion
+		rayCluster.Finalizers = append(rayCluster.Finalizers, "ray.io/gcs-ft-redis-cleanup-finalizer")
+
+		ginkgo.By("Creating the RayCluster with GCS FT finalizer", func() {
+			gomega.Expect(k8sClient.Create(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		wlLookupKey := types.NamespacedName{Name: workloadraycluster.GetWorkloadNameForRayCluster(rayCluster.Name, rayCluster.UID), Namespace: ns.Name}
+		createdWorkload := &kueue.Workload{}
+		ginkgo.By("Checking workload is created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking workload is admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, createdWorkload)
+		})
+
+		ginkgo.By("Deleting the RayCluster to trigger redis-cleanup job creation", func() {
+			gomega.Expect(k8sClient.Delete(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for redis-cleanup jobs to be created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				jobList := &batchv1.JobList{}
+				g.Expect(k8sClient.List(ctx, jobList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+				g.Expect(jobList.Items).ToNot(gomega.BeEmpty(), "Expected at least one redis-cleanup job to be created")
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Verifying redis-cleanup job pods do not have scheduling gates", func() {
+			jobList := &batchv1.JobList{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.List(ctx, jobList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+
+				for _, job := range jobList.Items {
+					// Verify the job itself doesn't have scheduling gates in its pod template
+					schedulingGates := job.Spec.Template.Spec.SchedulingGates
+					for _, gate := range schedulingGates {
+						g.Expect(gate.Name).NotTo(gomega.Equal(kueue.ElasticJobSchedulingGate),
+							"Redis-cleanup job should not have ElasticJobSchedulingGate")
+					}
+
+					// Also check actual pods created by the job
+					podList := &corev1.PodList{}
+					g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name),
+						client.MatchingLabels{
+							"ray.io/node-type": "redis-cleanup",
+							"job-name":         job.Name,
+						})).To(gomega.Succeed())
+
+					for _, pod := range podList.Items {
+						for _, gate := range pod.Spec.SchedulingGates {
+							g.Expect(gate.Name).NotTo(gomega.Equal(kueue.ElasticJobSchedulingGate),
+								"Redis-cleanup pod should not have ElasticJobSchedulingGate")
+						}
+					}
+				}
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Verifying redis-cleanup pods can be scheduled immediately", func() {
+			podList := &corev1.PodList{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+
+				// Verify at least one redis-cleanup pod exists and is not blocked by scheduling gates
+				g.Expect(podList.Items).ToNot(gomega.BeEmpty(), "Expected at least one redis-cleanup pod")
+
+				// Check that pods are not stuck in Pending phase due to scheduling gates
+				for _, pod := range podList.Items {
+					// Pod should either be Running/Succeeded or Pending with a reason other than scheduling gates
+					if pod.Status.Phase == corev1.PodPending {
+						// If pending, it should not be due to scheduling gates
+						hasSchedulingGate := false
+						for _, condition := range pod.Status.Conditions {
+							if condition.Type == corev1.PodScheduled && condition.Status == corev1.ConditionFalse {
+								if condition.Reason == "SchedulingGated" {
+									hasSchedulingGate = true
+									break
+								}
+							}
+						}
```

  Here introduce a helper which collects all Pods which have scheduling gates, and assert that the list of pods is empty. This way we will have a much more useful diff when the test fails - we will see the names of the Pods violating the assert.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-05T06:21:41Z

- **test/e2e/singlecluster/kuberay_test.go:1045** by [@mimowo](https://github.com/mimowo) — 2026-05-05T06:21:41Z
```diff
@@ -789,4 +778,112 @@ app = HelloWorld.bind()`,
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("Should ensure redis-cleanup pods do not have scheduling gates for Ray Cluster with GCS FT", func() {
+		kuberayTestImage := util.GetKuberayTestImage()
+
+		rayCluster := testingraycluster.MakeCluster("raycluster-gcs-ft", ns.Name).
+			Suspend(true).
+			Queue(localQueueName).
+			RequestAndLimit(rayv1.HeadNode, corev1.ResourceCPU, "300m").
+			RequestAndLimit(rayv1.WorkerNode, corev1.ResourceCPU, "300m").
+			Image(rayv1.HeadNode, kuberayTestImage, []string{}).
+			Image(rayv1.WorkerNode, kuberayTestImage, []string{}).
+			SetAnnotation("ray.io/ft-enabled", "true").
+			Obj()
+
+		// Add GCS fault tolerance finalizer to trigger redis-cleanup job creation on deletion
+		rayCluster.Finalizers = append(rayCluster.Finalizers, "ray.io/gcs-ft-redis-cleanup-finalizer")
+
+		ginkgo.By("Creating the RayCluster with GCS FT finalizer", func() {
+			gomega.Expect(k8sClient.Create(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		wlLookupKey := types.NamespacedName{Name: workloadraycluster.GetWorkloadNameForRayCluster(rayCluster.Name, rayCluster.UID), Namespace: ns.Name}
+		createdWorkload := &kueue.Workload{}
+		ginkgo.By("Checking workload is created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking workload is admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, createdWorkload)
+		})
+
+		ginkgo.By("Deleting the RayCluster to trigger redis-cleanup job creation", func() {
+			gomega.Expect(k8sClient.Delete(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for redis-cleanup jobs to be created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				jobList := &batchv1.JobList{}
+				g.Expect(k8sClient.List(ctx, jobList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+				g.Expect(jobList.Items).ToNot(gomega.BeEmpty(), "Expected at least one redis-cleanup job to be created")
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Verifying redis-cleanup job pods do not have scheduling gates", func() {
+			jobList := &batchv1.JobList{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.List(ctx, jobList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+
+				for _, job := range jobList.Items {
+					// Verify the job itself doesn't have scheduling gates in its pod template
+					schedulingGates := job.Spec.Template.Spec.SchedulingGates
+					for _, gate := range schedulingGates {
+						g.Expect(gate.Name).NotTo(gomega.Equal(kueue.ElasticJobSchedulingGate),
+							"Redis-cleanup job should not have ElasticJobSchedulingGate")
+					}
+
+					// Also check actual pods created by the job
+					podList := &corev1.PodList{}
+					g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name),
+						client.MatchingLabels{
+							"ray.io/node-type": "redis-cleanup",
+							"job-name":         job.Name,
+						})).To(gomega.Succeed())
+
+					for _, pod := range podList.Items {
+						for _, gate := range pod.Spec.SchedulingGates {
+							g.Expect(gate.Name).NotTo(gomega.Equal(kueue.ElasticJobSchedulingGate),
+								"Redis-cleanup pod should not have ElasticJobSchedulingGate")
+						}
+					}
+				}
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Verifying redis-cleanup pods can be scheduled immediately", func() {
+			podList := &corev1.PodList{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+
+				// Verify at least one redis-cleanup pod exists and is not blocked by scheduling gates
+				g.Expect(podList.Items).ToNot(gomega.BeEmpty(), "Expected at least one redis-cleanup pod")
+
+				// Check that pods are not stuck in Pending phase due to scheduling gates
+				for _, pod := range podList.Items {
+					// Pod should either be Running/Succeeded or Pending with a reason other than scheduling gates
+					if pod.Status.Phase == corev1.PodPending {
```

  No need to filter by phase, because Pods with schedulinggates will always be Pending.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-05T06:28:17Z

- **test/e2e/singlecluster/kuberay_test.go:1001** by [@mimowo](https://github.com/mimowo) — 2026-05-05T06:28:18Z
```diff
@@ -789,4 +778,112 @@ app = HelloWorld.bind()`,
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("Should ensure redis-cleanup pods do not have scheduling gates for Ray Cluster with GCS FT", func() {
+		kuberayTestImage := util.GetKuberayTestImage()
+
+		rayCluster := testingraycluster.MakeCluster("raycluster-gcs-ft", ns.Name).
+			Suspend(true).
+			Queue(localQueueName).
+			RequestAndLimit(rayv1.HeadNode, corev1.ResourceCPU, "300m").
+			RequestAndLimit(rayv1.WorkerNode, corev1.ResourceCPU, "300m").
+			Image(rayv1.HeadNode, kuberayTestImage, []string{}).
+			Image(rayv1.WorkerNode, kuberayTestImage, []string{}).
+			SetAnnotation("ray.io/ft-enabled", "true").
+			Obj()
+
+		// Add GCS fault tolerance finalizer to trigger redis-cleanup job creation on deletion
+		rayCluster.Finalizers = append(rayCluster.Finalizers, "ray.io/gcs-ft-redis-cleanup-finalizer")
+
+		ginkgo.By("Creating the RayCluster with GCS FT finalizer", func() {
+			gomega.Expect(k8sClient.Create(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		wlLookupKey := types.NamespacedName{Name: workloadraycluster.GetWorkloadNameForRayCluster(rayCluster.Name, rayCluster.UID), Namespace: ns.Name}
+		createdWorkload := &kueue.Workload{}
+		ginkgo.By("Checking workload is created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking workload is admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, createdWorkload)
+		})
+
+		ginkgo.By("Deleting the RayCluster to trigger redis-cleanup job creation", func() {
+			gomega.Expect(k8sClient.Delete(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for redis-cleanup jobs to be created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				jobList := &batchv1.JobList{}
+				g.Expect(k8sClient.List(ctx, jobList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+				g.Expect(jobList.Items).ToNot(gomega.BeEmpty(), "Expected at least one redis-cleanup job to be created")
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Verifying redis-cleanup job pods do not have scheduling gates", func() {
```

  I'm not sure if that matters actually for e2e testing. I think what matters is that the Pods can run eventually. If we gate them, but remove the gate it seems ok to me .

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-05T06:35:59Z

- **hack/testing/ray/kuberay-gcs-ft-patch.yaml:14** by [@mimowo](https://github.com/mimowo) — 2026-05-05T06:35:59Z
```diff
@@ -0,0 +1,15 @@
+# hack/testing/ray/kuberay-gcs-ft-patch.yaml
+apiVersion: apps/v1
+kind: Deployment
+metadata:
+  name: kuberay-operator
+  namespace: default
+spec:
+  template:
+    spec:
+      containers:
+      - name: kuberay-operator
+        env:
+        - name: ENABLE_GCS_FT_REDIS_CLEANUP
+          value: "true"
```

  I would prefer to avoid customizing the KubeRay deployment, so that our tests are testing the OOTB configuration. Could you explore using spec.GcsFaultToleranceOptions to enable that per RayCluster rather than globally, this seems possible looking here: https://github.com/ray-project/kuberay/blob/master/ray-operator/apis/ray/v1/raycluster_types.go#L42-L43

### Review by [@ns-sundar](https://github.com/ns-sundar) (commented) — 2026-05-06T08:17:34Z

- **pkg/controller/jobs/job/job_webhook.go:83** by [@ns-sundar](https://github.com/ns-sundar) — 2026-05-06T02:33:41Z
```diff
@@ -54,14 +54,57 @@ var (
 // behavior in Kueue-managed Jobs, allowing for fine-grained control over
 // resource allocation and scheduling decisions.
 //
+// Exception: Batch jobs owned by ray.io/v1/RayCluster with the label "ray.io/node-type: redis-cleanup"
+// are excluded from getting scheduling gates because (a) Kueue workload podsets do not account for
+// redis cleanup pods, and (b) redis-cleanup jobs do not need autoscaling (elastic job).
+//
 // Parameters:
+//   - ctx: Context for logging
 //   - job: Pointer to the Job object to be modified.
 //
 // Note: This function modifies the Job's pod template in-place.
-func applyWorkloadSliceSchedulingGate(job *Job) {
-	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) || !workloadslicing.Enabled(job.Object()) {
+func applyWorkloadSliceSchedulingGate(ctx context.Context, job *Job) {
+	log := ctrl.LoggerFrom(ctx)
+
+	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) {
+		return
+	}
+
+	// For redis_cleanup jobs generated by KubeRay's RayCluster, the workload slicing
+	// annotation will not be set. So, workloadslicing.Enabled(job.Object()) will
+	// return false. Still, we should rm the elastic job scheduling gate.
+
+	if isRayRedisCleanupJob(job) {
+		log.V(3).Info("Detected Ray redis_cleanup job - removing any inherited elastic scheduling gates",
+			"jobName", job.Name,
+			"jobNamespace", job.Namespace)
+
+		// Safely remove the elastic job scheduling gate if it exists (inherited from RayCluster template)
+		if utilpod.UngateTemplate(&job.Spec.Template, kueue.ElasticJobSchedulingGate) {
```

  This part adds low-level logging for debugging purposes.

- **pkg/controller/jobs/job/job_webhook.go:96** by [@ns-sundar](https://github.com/ns-sundar) — 2026-05-06T02:46:39Z
```diff
@@ -54,14 +54,57 @@ var (
 // behavior in Kueue-managed Jobs, allowing for fine-grained control over
 // resource allocation and scheduling decisions.
 //
+// Exception: Batch jobs owned by ray.io/v1/RayCluster with the label "ray.io/node-type: redis-cleanup"
+// are excluded from getting scheduling gates because (a) Kueue workload podsets do not account for
+// redis cleanup pods, and (b) redis-cleanup jobs do not need autoscaling (elastic job).
+//
 // Parameters:
+//   - ctx: Context for logging
 //   - job: Pointer to the Job object to be modified.
 //
 // Note: This function modifies the Job's pod template in-place.
-func applyWorkloadSliceSchedulingGate(job *Job) {
-	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) || !workloadslicing.Enabled(job.Object()) {
+func applyWorkloadSliceSchedulingGate(ctx context.Context, job *Job) {
+	log := ctrl.LoggerFrom(ctx)
+
+	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) {
+		return
+	}
+
+	// For redis_cleanup jobs generated by KubeRay's RayCluster, the workload slicing
+	// annotation will not be set. So, workloadslicing.Enabled(job.Object()) will
+	// return false. Still, we should rm the elastic job scheduling gate.
+
+	if isRayRedisCleanupJob(job) {
+		log.V(3).Info("Detected Ray redis_cleanup job - removing any inherited elastic scheduling gates",
+			"jobName", job.Name,
+			"jobNamespace", job.Namespace)
+
+		// Safely remove the elastic job scheduling gate if it exists (inherited from RayCluster template)
+		if utilpod.UngateTemplate(&job.Spec.Template, kueue.ElasticJobSchedulingGate) {
+			log.V(3).Info("Successfully removed inherited elastic scheduling gate from redis-cleanup job",
+				"jobName", job.Name,
+				"jobNamespace", job.Namespace)
+		} else {
+			log.V(3).Info("No elastic scheduling gate found to remove from redis-cleanup job",
+				"jobName", job.Name,
+				"jobNamespace", job.Namespace)
+		}
 		return
 	}
+
+	// Only apply scheduling gate if the job is explicitly opted in via workload slicing annotation
+	if !workloadslicing.Enabled(job.Object()) {
```

  For redis_cleanup jobs generated by KubeRay's RayCluster, the workload slicing annotation will not be set. So, if we do:
  ```
  if !features.Enabled(features.ElasticJobsViaWorkloadSlices) || !workloadslicing.Enabled(job.Object()) {
  		return
  	}
  ```
  it will return before removing the scheduling gates.

- **test/e2e/singlecluster/kuberay_test.go:1001** by [@ns-sundar](https://github.com/ns-sundar) — 2026-05-06T02:48:07Z
```diff
@@ -789,4 +778,112 @@ app = HelloWorld.bind()`,
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("Should ensure redis-cleanup pods do not have scheduling gates for Ray Cluster with GCS FT", func() {
+		kuberayTestImage := util.GetKuberayTestImage()
+
+		rayCluster := testingraycluster.MakeCluster("raycluster-gcs-ft", ns.Name).
+			Suspend(true).
+			Queue(localQueueName).
+			RequestAndLimit(rayv1.HeadNode, corev1.ResourceCPU, "300m").
+			RequestAndLimit(rayv1.WorkerNode, corev1.ResourceCPU, "300m").
+			Image(rayv1.HeadNode, kuberayTestImage, []string{}).
+			Image(rayv1.WorkerNode, kuberayTestImage, []string{}).
+			SetAnnotation("ray.io/ft-enabled", "true").
+			Obj()
+
+		// Add GCS fault tolerance finalizer to trigger redis-cleanup job creation on deletion
+		rayCluster.Finalizers = append(rayCluster.Finalizers, "ray.io/gcs-ft-redis-cleanup-finalizer")
+
+		ginkgo.By("Creating the RayCluster with GCS FT finalizer", func() {
+			gomega.Expect(k8sClient.Create(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		wlLookupKey := types.NamespacedName{Name: workloadraycluster.GetWorkloadNameForRayCluster(rayCluster.Name, rayCluster.UID), Namespace: ns.Name}
+		createdWorkload := &kueue.Workload{}
+		ginkgo.By("Checking workload is created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking workload is admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, createdWorkload)
+		})
+
+		ginkgo.By("Deleting the RayCluster to trigger redis-cleanup job creation", func() {
+			gomega.Expect(k8sClient.Delete(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for redis-cleanup jobs to be created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				jobList := &batchv1.JobList{}
+				g.Expect(k8sClient.List(ctx, jobList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+				g.Expect(jobList.Items).ToNot(gomega.BeEmpty(), "Expected at least one redis-cleanup job to be created")
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Verifying redis-cleanup job pods do not have scheduling gates", func() {
```

  The core of the e2e test is to ensure that the change we made -- to rm scheduling gates -- actually works, right?

- **test/e2e/singlecluster/kuberay_test.go:1033** by [@ns-sundar](https://github.com/ns-sundar) — 2026-05-06T08:15:37Z
```diff
@@ -789,4 +778,112 @@ app = HelloWorld.bind()`,
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("Should ensure redis-cleanup pods do not have scheduling gates for Ray Cluster with GCS FT", func() {
+		kuberayTestImage := util.GetKuberayTestImage()
+
+		rayCluster := testingraycluster.MakeCluster("raycluster-gcs-ft", ns.Name).
+			Suspend(true).
+			Queue(localQueueName).
+			RequestAndLimit(rayv1.HeadNode, corev1.ResourceCPU, "300m").
+			RequestAndLimit(rayv1.WorkerNode, corev1.ResourceCPU, "300m").
+			Image(rayv1.HeadNode, kuberayTestImage, []string{}).
+			Image(rayv1.WorkerNode, kuberayTestImage, []string{}).
+			SetAnnotation("ray.io/ft-enabled", "true").
+			Obj()
+
+		// Add GCS fault tolerance finalizer to trigger redis-cleanup job creation on deletion
+		rayCluster.Finalizers = append(rayCluster.Finalizers, "ray.io/gcs-ft-redis-cleanup-finalizer")
+
+		ginkgo.By("Creating the RayCluster with GCS FT finalizer", func() {
+			gomega.Expect(k8sClient.Create(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		wlLookupKey := types.NamespacedName{Name: workloadraycluster.GetWorkloadNameForRayCluster(rayCluster.Name, rayCluster.UID), Namespace: ns.Name}
+		createdWorkload := &kueue.Workload{}
+		ginkgo.By("Checking workload is created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking workload is admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, createdWorkload)
+		})
+
+		ginkgo.By("Deleting the RayCluster to trigger redis-cleanup job creation", func() {
+			gomega.Expect(k8sClient.Delete(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for redis-cleanup jobs to be created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				jobList := &batchv1.JobList{}
+				g.Expect(k8sClient.List(ctx, jobList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+				g.Expect(jobList.Items).ToNot(gomega.BeEmpty(), "Expected at least one redis-cleanup job to be created")
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Verifying redis-cleanup job pods do not have scheduling gates", func() {
+			jobList := &batchv1.JobList{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.List(ctx, jobList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+
+				for _, job := range jobList.Items {
+					// Verify the job itself doesn't have scheduling gates in its pod template
+					schedulingGates := job.Spec.Template.Spec.SchedulingGates
+					for _, gate := range schedulingGates {
+						g.Expect(gate.Name).NotTo(gomega.Equal(kueue.ElasticJobSchedulingGate),
+							"Redis-cleanup job should not have ElasticJobSchedulingGate")
+					}
+
+					// Also check actual pods created by the job
+					podList := &corev1.PodList{}
+					g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name),
+						client.MatchingLabels{
+							"ray.io/node-type": "redis-cleanup",
+							"job-name":         job.Name,
+						})).To(gomega.Succeed())
+
+					for _, pod := range podList.Items {
+						for _, gate := range pod.Spec.SchedulingGates {
+							g.Expect(gate.Name).NotTo(gomega.Equal(kueue.ElasticJobSchedulingGate),
+								"Redis-cleanup pod should not have ElasticJobSchedulingGate")
+						}
+					}
+				}
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Verifying redis-cleanup pods can be scheduled immediately", func() {
```

  OK

- **test/e2e/singlecluster/kuberay_test.go:1045** by [@ns-sundar](https://github.com/ns-sundar) — 2026-05-06T08:16:50Z
```diff
@@ -789,4 +778,112 @@ app = HelloWorld.bind()`,
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("Should ensure redis-cleanup pods do not have scheduling gates for Ray Cluster with GCS FT", func() {
+		kuberayTestImage := util.GetKuberayTestImage()
+
+		rayCluster := testingraycluster.MakeCluster("raycluster-gcs-ft", ns.Name).
+			Suspend(true).
+			Queue(localQueueName).
+			RequestAndLimit(rayv1.HeadNode, corev1.ResourceCPU, "300m").
+			RequestAndLimit(rayv1.WorkerNode, corev1.ResourceCPU, "300m").
+			Image(rayv1.HeadNode, kuberayTestImage, []string{}).
+			Image(rayv1.WorkerNode, kuberayTestImage, []string{}).
+			SetAnnotation("ray.io/ft-enabled", "true").
+			Obj()
+
+		// Add GCS fault tolerance finalizer to trigger redis-cleanup job creation on deletion
+		rayCluster.Finalizers = append(rayCluster.Finalizers, "ray.io/gcs-ft-redis-cleanup-finalizer")
+
+		ginkgo.By("Creating the RayCluster with GCS FT finalizer", func() {
+			gomega.Expect(k8sClient.Create(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		wlLookupKey := types.NamespacedName{Name: workloadraycluster.GetWorkloadNameForRayCluster(rayCluster.Name, rayCluster.UID), Namespace: ns.Name}
+		createdWorkload := &kueue.Workload{}
+		ginkgo.By("Checking workload is created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking workload is admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, createdWorkload)
+		})
+
+		ginkgo.By("Deleting the RayCluster to trigger redis-cleanup job creation", func() {
+			gomega.Expect(k8sClient.Delete(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for redis-cleanup jobs to be created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				jobList := &batchv1.JobList{}
+				g.Expect(k8sClient.List(ctx, jobList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+				g.Expect(jobList.Items).ToNot(gomega.BeEmpty(), "Expected at least one redis-cleanup job to be created")
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Verifying redis-cleanup job pods do not have scheduling gates", func() {
+			jobList := &batchv1.JobList{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.List(ctx, jobList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+
+				for _, job := range jobList.Items {
+					// Verify the job itself doesn't have scheduling gates in its pod template
+					schedulingGates := job.Spec.Template.Spec.SchedulingGates
+					for _, gate := range schedulingGates {
+						g.Expect(gate.Name).NotTo(gomega.Equal(kueue.ElasticJobSchedulingGate),
+							"Redis-cleanup job should not have ElasticJobSchedulingGate")
+					}
+
+					// Also check actual pods created by the job
+					podList := &corev1.PodList{}
+					g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name),
+						client.MatchingLabels{
+							"ray.io/node-type": "redis-cleanup",
+							"job-name":         job.Name,
+						})).To(gomega.Succeed())
+
+					for _, pod := range podList.Items {
+						for _, gate := range pod.Spec.SchedulingGates {
+							g.Expect(gate.Name).NotTo(gomega.Equal(kueue.ElasticJobSchedulingGate),
+								"Redis-cleanup pod should not have ElasticJobSchedulingGate")
+						}
+					}
+				}
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Verifying redis-cleanup pods can be scheduled immediately", func() {
+			podList := &corev1.PodList{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+
+				// Verify at least one redis-cleanup pod exists and is not blocked by scheduling gates
+				g.Expect(podList.Items).ToNot(gomega.BeEmpty(), "Expected at least one redis-cleanup pod")
+
+				// Check that pods are not stuck in Pending phase due to scheduling gates
+				for _, pod := range podList.Items {
+					// Pod should either be Running/Succeeded or Pending with a reason other than scheduling gates
+					if pod.Status.Phase == corev1.PodPending {
```

  We can drop this test per previous comment https://github.com/kubernetes-sigs/kueue/pull/8579/changes/BASE..26328dd5f931fd1e0104890310bef692c4450e79#r3186376955

- **test/e2e/singlecluster/kuberay_test.go:1055** by [@ns-sundar](https://github.com/ns-sundar) — 2026-05-06T08:17:03Z
```diff
@@ -789,4 +778,112 @@ app = HelloWorld.bind()`,
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("Should ensure redis-cleanup pods do not have scheduling gates for Ray Cluster with GCS FT", func() {
+		kuberayTestImage := util.GetKuberayTestImage()
+
+		rayCluster := testingraycluster.MakeCluster("raycluster-gcs-ft", ns.Name).
+			Suspend(true).
+			Queue(localQueueName).
+			RequestAndLimit(rayv1.HeadNode, corev1.ResourceCPU, "300m").
+			RequestAndLimit(rayv1.WorkerNode, corev1.ResourceCPU, "300m").
+			Image(rayv1.HeadNode, kuberayTestImage, []string{}).
+			Image(rayv1.WorkerNode, kuberayTestImage, []string{}).
+			SetAnnotation("ray.io/ft-enabled", "true").
+			Obj()
+
+		// Add GCS fault tolerance finalizer to trigger redis-cleanup job creation on deletion
+		rayCluster.Finalizers = append(rayCluster.Finalizers, "ray.io/gcs-ft-redis-cleanup-finalizer")
+
+		ginkgo.By("Creating the RayCluster with GCS FT finalizer", func() {
+			gomega.Expect(k8sClient.Create(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		wlLookupKey := types.NamespacedName{Name: workloadraycluster.GetWorkloadNameForRayCluster(rayCluster.Name, rayCluster.UID), Namespace: ns.Name}
+		createdWorkload := &kueue.Workload{}
+		ginkgo.By("Checking workload is created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking workload is admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, createdWorkload)
+		})
+
+		ginkgo.By("Deleting the RayCluster to trigger redis-cleanup job creation", func() {
+			gomega.Expect(k8sClient.Delete(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for redis-cleanup jobs to be created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				jobList := &batchv1.JobList{}
+				g.Expect(k8sClient.List(ctx, jobList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+				g.Expect(jobList.Items).ToNot(gomega.BeEmpty(), "Expected at least one redis-cleanup job to be created")
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Verifying redis-cleanup job pods do not have scheduling gates", func() {
+			jobList := &batchv1.JobList{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.List(ctx, jobList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+
+				for _, job := range jobList.Items {
+					// Verify the job itself doesn't have scheduling gates in its pod template
+					schedulingGates := job.Spec.Template.Spec.SchedulingGates
+					for _, gate := range schedulingGates {
+						g.Expect(gate.Name).NotTo(gomega.Equal(kueue.ElasticJobSchedulingGate),
+							"Redis-cleanup job should not have ElasticJobSchedulingGate")
+					}
+
+					// Also check actual pods created by the job
+					podList := &corev1.PodList{}
+					g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name),
+						client.MatchingLabels{
+							"ray.io/node-type": "redis-cleanup",
+							"job-name":         job.Name,
+						})).To(gomega.Succeed())
+
+					for _, pod := range podList.Items {
+						for _, gate := range pod.Spec.SchedulingGates {
+							g.Expect(gate.Name).NotTo(gomega.Equal(kueue.ElasticJobSchedulingGate),
+								"Redis-cleanup pod should not have ElasticJobSchedulingGate")
+						}
+					}
+				}
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Verifying redis-cleanup pods can be scheduled immediately", func() {
+			podList := &corev1.PodList{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+
+				// Verify at least one redis-cleanup pod exists and is not blocked by scheduling gates
+				g.Expect(podList.Items).ToNot(gomega.BeEmpty(), "Expected at least one redis-cleanup pod")
+
+				// Check that pods are not stuck in Pending phase due to scheduling gates
+				for _, pod := range podList.Items {
+					// Pod should either be Running/Succeeded or Pending with a reason other than scheduling gates
+					if pod.Status.Phase == corev1.PodPending {
+						// If pending, it should not be due to scheduling gates
+						hasSchedulingGate := false
+						for _, condition := range pod.Status.Conditions {
+							if condition.Type == corev1.PodScheduled && condition.Status == corev1.ConditionFalse {
+								if condition.Reason == "SchedulingGated" {
+									hasSchedulingGate = true
+									break
+								}
+							}
+						}
```

  Same here: we can drop this test

- **test/e2e/singlecluster/kuberay_test.go:1060** by [@ns-sundar](https://github.com/ns-sundar) — 2026-05-06T08:17:12Z
```diff
@@ -789,4 +778,112 @@ app = HelloWorld.bind()`,
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("Should ensure redis-cleanup pods do not have scheduling gates for Ray Cluster with GCS FT", func() {
+		kuberayTestImage := util.GetKuberayTestImage()
+
+		rayCluster := testingraycluster.MakeCluster("raycluster-gcs-ft", ns.Name).
+			Suspend(true).
+			Queue(localQueueName).
+			RequestAndLimit(rayv1.HeadNode, corev1.ResourceCPU, "300m").
+			RequestAndLimit(rayv1.WorkerNode, corev1.ResourceCPU, "300m").
+			Image(rayv1.HeadNode, kuberayTestImage, []string{}).
+			Image(rayv1.WorkerNode, kuberayTestImage, []string{}).
+			SetAnnotation("ray.io/ft-enabled", "true").
+			Obj()
+
+		// Add GCS fault tolerance finalizer to trigger redis-cleanup job creation on deletion
+		rayCluster.Finalizers = append(rayCluster.Finalizers, "ray.io/gcs-ft-redis-cleanup-finalizer")
+
+		ginkgo.By("Creating the RayCluster with GCS FT finalizer", func() {
+			gomega.Expect(k8sClient.Create(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		wlLookupKey := types.NamespacedName{Name: workloadraycluster.GetWorkloadNameForRayCluster(rayCluster.Name, rayCluster.UID), Namespace: ns.Name}
+		createdWorkload := &kueue.Workload{}
+		ginkgo.By("Checking workload is created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking workload is admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, createdWorkload)
+		})
+
+		ginkgo.By("Deleting the RayCluster to trigger redis-cleanup job creation", func() {
+			gomega.Expect(k8sClient.Delete(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for redis-cleanup jobs to be created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				jobList := &batchv1.JobList{}
+				g.Expect(k8sClient.List(ctx, jobList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+				g.Expect(jobList.Items).ToNot(gomega.BeEmpty(), "Expected at least one redis-cleanup job to be created")
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Verifying redis-cleanup job pods do not have scheduling gates", func() {
+			jobList := &batchv1.JobList{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.List(ctx, jobList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+
+				for _, job := range jobList.Items {
+					// Verify the job itself doesn't have scheduling gates in its pod template
+					schedulingGates := job.Spec.Template.Spec.SchedulingGates
+					for _, gate := range schedulingGates {
+						g.Expect(gate.Name).NotTo(gomega.Equal(kueue.ElasticJobSchedulingGate),
+							"Redis-cleanup job should not have ElasticJobSchedulingGate")
+					}
+
+					// Also check actual pods created by the job
+					podList := &corev1.PodList{}
+					g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name),
+						client.MatchingLabels{
+							"ray.io/node-type": "redis-cleanup",
+							"job-name":         job.Name,
+						})).To(gomega.Succeed())
+
+					for _, pod := range podList.Items {
+						for _, gate := range pod.Spec.SchedulingGates {
+							g.Expect(gate.Name).NotTo(gomega.Equal(kueue.ElasticJobSchedulingGate),
+								"Redis-cleanup pod should not have ElasticJobSchedulingGate")
+						}
+					}
+				}
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Verifying redis-cleanup pods can be scheduled immediately", func() {
+			podList := &corev1.PodList{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+
+				// Verify at least one redis-cleanup pod exists and is not blocked by scheduling gates
+				g.Expect(podList.Items).ToNot(gomega.BeEmpty(), "Expected at least one redis-cleanup pod")
+
+				// Check that pods are not stuck in Pending phase due to scheduling gates
+				for _, pod := range podList.Items {
+					// Pod should either be Running/Succeeded or Pending with a reason other than scheduling gates
+					if pod.Status.Phase == corev1.PodPending {
+						// If pending, it should not be due to scheduling gates
+						hasSchedulingGate := false
+						for _, condition := range pod.Status.Conditions {
+							if condition.Type == corev1.PodScheduled && condition.Status == corev1.ConditionFalse {
+								if condition.Reason == "SchedulingGated" {
+									hasSchedulingGate = true
+									break
+								}
+							}
+						}
+						g.Expect(hasSchedulingGate).To(gomega.BeFalse(),
+							"Redis-cleanup pod should not be gated by scheduling gates")
+					}
+				}
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
```

  OK

- **test/e2e/singlecluster/kuberay_test.go:1030** by [@ns-sundar](https://github.com/ns-sundar) — 2026-05-06T08:17:28Z
```diff
@@ -789,4 +778,112 @@ app = HelloWorld.bind()`,
 			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("Should ensure redis-cleanup pods do not have scheduling gates for Ray Cluster with GCS FT", func() {
+		kuberayTestImage := util.GetKuberayTestImage()
+
+		rayCluster := testingraycluster.MakeCluster("raycluster-gcs-ft", ns.Name).
+			Suspend(true).
+			Queue(localQueueName).
+			RequestAndLimit(rayv1.HeadNode, corev1.ResourceCPU, "300m").
+			RequestAndLimit(rayv1.WorkerNode, corev1.ResourceCPU, "300m").
+			Image(rayv1.HeadNode, kuberayTestImage, []string{}).
+			Image(rayv1.WorkerNode, kuberayTestImage, []string{}).
+			SetAnnotation("ray.io/ft-enabled", "true").
+			Obj()
+
+		// Add GCS fault tolerance finalizer to trigger redis-cleanup job creation on deletion
+		rayCluster.Finalizers = append(rayCluster.Finalizers, "ray.io/gcs-ft-redis-cleanup-finalizer")
+
+		ginkgo.By("Creating the RayCluster with GCS FT finalizer", func() {
+			gomega.Expect(k8sClient.Create(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		wlLookupKey := types.NamespacedName{Name: workloadraycluster.GetWorkloadNameForRayCluster(rayCluster.Name, rayCluster.UID), Namespace: ns.Name}
+		createdWorkload := &kueue.Workload{}
+		ginkgo.By("Checking workload is created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking workload is admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, createdWorkload)
+		})
+
+		ginkgo.By("Deleting the RayCluster to trigger redis-cleanup job creation", func() {
+			gomega.Expect(k8sClient.Delete(ctx, rayCluster)).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Waiting for redis-cleanup jobs to be created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				jobList := &batchv1.JobList{}
+				g.Expect(k8sClient.List(ctx, jobList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+				g.Expect(jobList.Items).ToNot(gomega.BeEmpty(), "Expected at least one redis-cleanup job to be created")
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Verifying redis-cleanup job pods do not have scheduling gates", func() {
+			jobList := &batchv1.JobList{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.List(ctx, jobList, client.InNamespace(ns.Name),
+					client.MatchingLabels{"ray.io/node-type": "redis-cleanup"})).To(gomega.Succeed())
+
+				for _, job := range jobList.Items {
+					// Verify the job itself doesn't have scheduling gates in its pod template
+					schedulingGates := job.Spec.Template.Spec.SchedulingGates
+					for _, gate := range schedulingGates {
+						g.Expect(gate.Name).NotTo(gomega.Equal(kueue.ElasticJobSchedulingGate),
+							"Redis-cleanup job should not have ElasticJobSchedulingGate")
+					}
+
+					// Also check actual pods created by the job
+					podList := &corev1.PodList{}
+					g.Expect(k8sClient.List(ctx, podList, client.InNamespace(ns.Name),
+						client.MatchingLabels{
+							"ray.io/node-type": "redis-cleanup",
+							"job-name":         job.Name,
+						})).To(gomega.Succeed())
+
+					for _, pod := range podList.Items {
+						for _, gate := range pod.Spec.SchedulingGates {
+							g.Expect(gate.Name).NotTo(gomega.Equal(kueue.ElasticJobSchedulingGate),
+								"Redis-cleanup pod should not have ElasticJobSchedulingGate")
+						}
+					}
+				}
+			}, util.VeryLongTimeout, util.Interval).Should(gomega.Succeed())
```

  OK

### Review by [@ns-sundar](https://github.com/ns-sundar) (commented) — 2026-05-06T17:35:02Z

- **hack/testing/ray/kuberay-gcs-ft-patch.yaml:14** by [@ns-sundar](https://github.com/ns-sundar) — 2026-05-06T17:35:02Z
```diff
@@ -0,0 +1,15 @@
+# hack/testing/ray/kuberay-gcs-ft-patch.yaml
+apiVersion: apps/v1
+kind: Deployment
+metadata:
+  name: kuberay-operator
+  namespace: default
+spec:
+  template:
+    spec:
+      containers:
+      - name: kuberay-operator
+        env:
+        - name: ENABLE_GCS_FT_REDIS_CLEANUP
+          value: "true"
```

  Both the operator-level env var and the per-cluster annotation are needed for redis-cleanup jobs to be created, as per this code segment: 
  
  https://github.com/ray-project/kuberay/blob/master/ray-operator/controllers/ray/raycluster_controller.go#L190-L196
