# PR #8580: Add support for TAS + Elasticworkloads

**Summary**: Add support for TAS + Elasticworkloads

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8580

**Last updated**: 2026-02-17T09:27:06Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@sohankunkerkar](https://github.com/sohankunkerkar)
- **Branch**: `support-elasticworkloads` → `main`
- **Created**: 2026-01-14T06:11:53Z
- **Updated**: 2026-02-17T09:27:06Z
- **Merged**: 2026-02-17T09:27:04Z
- **Closed**: 2026-02-17T09:27:04Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 26   **+1652 / -101**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes https://github.com/kubernetes-sigs/kueue/issues/8160

#### Special notes for your reviewer:
When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled, Kueue now preserves topology locality across workload slice transitions during job scaling.
#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
ElasticWorkloads & TAS: enable the integration with the ElasticJobsViaWorkloadSlicesWithTAS feature gate (alpha)
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-01-14T06:11:59Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | ac9cf06e63c22094307b68f1b51f4528ecb001dc |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6993dd7515727d000825a2b6 |

### Comment by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-16T20:22:20Z

> I think for readability I would suggest to actually de-couple the KEP update and the implementation to separate PRs. These can be also reviewed by other folks.

https://github.com/kubernetes-sigs/kueue/pull/8642

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-29T13:50:20Z

@sohankunkerkar please mark all addressed comments as resolved to make clear which are done

### Comment by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-29T14:55:33Z

> @sohankunkerkar please mark all addressed comments as resolved to make clear which are done

Done! There are a few comments that are stale since we changed the implementation, but I made sure everything relevant is resolved.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-17T08:39:42Z

/lgtm
/approve
Thank you 👍 I'm happy with the implementation as for the alpha. The new code is well isolated by the use of the ElasticJobsViaWorkloadSlicesWithTAS feature gate.

I think the next step will be to add a sanity e2e test (one scale up, one scale down) and verify it is not flaky.

please also follow up with documentation

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-17T08:39:50Z

LGTM label has been added.  <details>Git tree hash: ff7cb33d4a60955e9a2c4afc9d8a7c5df3e89cb9</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-17T08:39:53Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8580#issuecomment-3913098469" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8580#" title="Author self-approved">sohankunkerkar</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-17T08:44:04Z

/release-note-edit
```release-note
ElasticWorkloads & TAS: enable the integration with the ElasticJobsViaWorkloadSlicesWithTAS feature gate (alpha)
```

## Reviews

### Review by [@copilot-pull-request-reviewer[bot]](https://github.com/apps/copilot-pull-request-reviewer) (commented) — 2026-01-14T06:22:01Z

## Pull request overview

This PR adds support for integrating Topology Aware Scheduling (TAS) with ElasticJobsViaWorkloadSlices. When both feature gates are enabled, Kueue preserves topology locality across workload slice transitions during job scaling operations.

**Changes:**
- Adds `JobNameAnnotation` constant to provide stable pod identification across workload slice transitions
- Implements topology assignment preservation logic in the scheduler to place scaled workloads in the same topology domain
- Extends pod indexing with a new `JobNameKey` index for finding pods by parent job name
- Updates controllers (TopologyUngater, NodeFailureReconciler) to use job-name-based pod lookups when both features are enabled

### Reviewed changes

Copilot reviewed 12 out of 12 changed files in this pull request and generated 2 comments.

<details>
<summary>Show a summary per file</summary>

| File | Description |
| ---- | ----------- |
| apis/kueue/v1beta2/topology_types.go | Adds JobNameAnnotation constant for stable pod identification |
| apis/kueue/v1beta1/topology_types.go | Adds JobNameAnnotation constant (v1beta1 version) |
| pkg/scheduler/flavorassigner/tas_flavorassigner.go | Extracts previous topology assignment from replaced workload slice |
| pkg/cache/scheduler/tas_flavor_snapshot.go | Implements topology domain preservation and freed capacity calculation for replacement workloads |
| pkg/controller/tas/topology_ungater.go | Updates pod lookup to use job-name index when ElasticJobsViaWorkloadSlices is enabled |
| pkg/controller/tas/node_failure_controller.go | Refactors to track workload info with job names for pod lookups |
| pkg/controller/tas/indexer/indexer.go | Adds new job-name index for pods |
| pkg/controller/jobframework/reconciler.go | Injects JobNameAnnotation on pods when both TAS and ElasticJobsViaWorkloadSlices are enabled |
| test/integration/singlecluster/tas/tas_test.go | Adds integration test for TAS with elastic workload slices |
| test/integration/singlecluster/tas/suite_test.go | Configures pod webhook in test suite |
| keps/77-dynamically-sized-jobs/README.md | Documents TAS integration with elastic jobs |
| keps/2724-topology-aware-scheduling/README.md | Documents elastic workload support in TAS |
</details>






---

💡 <a href="/kubernetes-sigs/kueue/new/main/.github/instructions?filename=*.instructions.md" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Add Copilot custom instructions</a> for smarter, more guided reviews. <a href="https://docs.github.com/en/copilot/customizing-copilot/adding-repository-custom-instructions-for-github-copilot" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Learn how to get started</a>.

- **test/integration/singlecluster/tas/tas_test.go:4567** by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer) — 2026-01-14T06:22:00Z
```diff
@@ -3772,6 +3774,197 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 			})
 		})
 	})
+
+	ginkgo.When("TAS with ElasticJobsViaWorkloadSlices", func() {
+		var (
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueue.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+			nodes        []corev1.Node
+		)
+
+		ginkgo.BeforeEach(func() {
+			features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.ElasticJobsViaWorkloadSlices, true)
+
+			// Create two blocks with two racks each to test topology placement
+			nodes = []corev1.Node{
+				*testingnode.MakeNode("b1-r1").
+					Label("node-group", "tas").
+					Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+					Label(utiltesting.DefaultRackTopologyLevel, "r1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:    resource.MustParse("4"),
+						corev1.ResourceMemory: resource.MustParse("4Gi"),
+						corev1.ResourcePods:   resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b1-r2").
+					Label("node-group", "tas").
+					Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+					Label(utiltesting.DefaultRackTopologyLevel, "r2").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:    resource.MustParse("4"),
+						corev1.ResourceMemory: resource.MustParse("4Gi"),
+						corev1.ResourcePods:   resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b2-r1").
+					Label("node-group", "tas").
+					Label(utiltesting.DefaultBlockTopologyLevel, "b2").
+					Label(utiltesting.DefaultRackTopologyLevel, "r1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:    resource.MustParse("4"),
+						corev1.ResourceMemory: resource.MustParse("4Gi"),
+						corev1.ResourcePods:   resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+			}
+			util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+			topology = utiltestingapi.MakeDefaultTwoLevelTopology("default")
+			util.MustCreate(ctx, k8sClient, topology)
+
+			tasFlavor = utiltestingapi.MakeResourceFlavor("tas-flavor").
+				NodeLabel("node-group", "tas").
+				TopologyName("default").Obj()
+			util.MustCreate(ctx, k8sClient, tasFlavor)
+
+			clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*utiltestingapi.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "12").Obj()).
+				Obj()
+			util.MustCreate(ctx, k8sClient, clusterQueue)
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			util.MustCreate(ctx, k8sClient, localQueue)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+			for _, node := range nodes {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+			}
+		})
+
+		ginkgo.It("should place scaled-up workload in same topology domain as original", func() {
+			var wl1 *kueue.Workload
+			const jobName = "elastic-job"
+
+			// ownerRef is needed so that TopologyUngater uses GetControllerOf(wl)
+			// to get the job name and exercise the job-name index path.
+			jobOwnerRef := metav1.OwnerReference{
+				APIVersion: "batch/v1",
+				Kind:       "Job",
+				Name:       jobName,
+				UID:        "test-uid",
+				Controller: ptr.To(true),
+			}
+
+			ginkgo.By("create initial workload with 2 pods requiring a block topology", func() {
+				wl1 = utiltestingapi.MakeWorkload("wl1", ns.Name).
+					Queue(kueue.LocalQueueName(localQueue.Name)).
+					Annotation(kueue.JobNameAnnotation, jobName).
+					Obj()
+				wl1.OwnerReferences = []metav1.OwnerReference{jobOwnerRef}
+				wl1.Spec.PodSets[0] = *utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 2).
+					Request(corev1.ResourceCPU, "1").
+					RequiredTopologyRequest(utiltesting.DefaultBlockTopologyLevel).
+					Image("image").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl1)
+			})
+
+			ginkgo.By("verify the workload is admitted", func() {
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
+			})
+
+			var originalAssignment *kueue.TopologyAssignment
+			ginkgo.By("verify admission for the workload1 and record the assignment", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+					g.Expect(wl1.Status.Admission).ShouldNot(gomega.BeNil())
+					g.Expect(wl1.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
+					g.Expect(wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment).ShouldNot(gomega.BeNil())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				originalAssignment = wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment
+			})
+
+			ginkgo.By("verify the original assignment is in block b1 or b2", func() {
+				// Extract the block from the original assignment
+				gomega.Expect(originalAssignment).ShouldNot(gomega.BeNil())
+				gomega.Expect(originalAssignment.Slices).ShouldNot(gomega.BeEmpty())
+			})
+
+			ginkgo.By("create pods with JobNameAnnotation simulating running pods", func() {
+				// Create pods with the job name annotation - this is what the job controller would do
+				for i := range 2 {
+					pod := testingpod.MakePod(fmt.Sprintf("pod-%d", i), ns.Name).
+						Annotation(kueue.WorkloadAnnotation, wl1.Name).
+						Annotation(kueue.JobNameAnnotation, jobName).
+						Request(corev1.ResourceCPU, "1").
+						Obj()
+					util.MustCreate(ctx, k8sClient, pod)
+				}
+			})
+
+			var wl2 *kueue.Workload
+			ginkgo.By("create a replacement workload slice with more pods", func() {
+				wl2 = utiltestingapi.MakeWorkload("wl2", ns.Name).
+					Queue(kueue.LocalQueueName(localQueue.Name)).
+					Annotation(kueue.JobNameAnnotation, jobName).
+					Annotation(workloadslicing.WorkloadSliceReplacementFor, string(workload.Key(wl1))).
+					Obj()
+				wl2.OwnerReferences = []metav1.OwnerReference{jobOwnerRef}
+				wl2.Spec.PodSets[0] = *utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 4).
+					Request(corev1.ResourceCPU, "1").
+					RequiredTopologyRequest(utiltesting.DefaultBlockTopologyLevel).
+					Image("image").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl2)
+			})
+
+			ginkgo.By("verify the replacement workload is admitted", func() {
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl2)
+			})
+
+			ginkgo.By("verify the replacement workload is placed in the same topology domain", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl2), wl2)).To(gomega.Succeed())
+					g.Expect(wl2.Status.Admission).ShouldNot(gomega.BeNil())
+					g.Expect(wl2.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
+					g.Expect(wl2.Status.Admission.PodSetAssignments[0].TopologyAssignment).ShouldNot(gomega.BeNil())
+
+					newAssignment := wl2.Status.Admission.PodSetAssignments[0].TopologyAssignment
+					g.Expect(newAssignment.Slices).ShouldNot(gomega.BeEmpty())
+					g.Expect(originalAssignment.Slices).ShouldNot(gomega.BeEmpty())
+
+					// Verify the replacement workload is placed in the same block as the original.
+					blockIdx := slices.Index(originalAssignment.Levels, utiltesting.DefaultBlockTopologyLevel)
+					g.Expect(blockIdx).To(gomega.BeNumerically(">=", 0), "block topology level should be present in assignment levels")
+
+					originalBlocks := map[string]struct{}{}
+					for _, dom := range slices.Collect(utiltas.InternalSeqFrom(originalAssignment)) {
+						originalBlocks[dom.Values[blockIdx]] = struct{}{}
+					}
+					newBlocks := map[string]struct{}{}
+					for _, dom := range slices.Collect(utiltas.InternalSeqFrom(newAssignment)) {
+						newBlocks[dom.Values[blockIdx]] = struct{}{}
+					}
+					g.Expect(len(originalBlocks)).To(gomega.Equal(1), "original workload should be placed within a single block")
+					g.Expect(newBlocks).To(gomega.Equal(originalBlocks), "replacement workload should be placed in the same block as original")
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+	})
```

  The test suite for TAS with ElasticJobsViaWorkloadSlices only covers the scale-up scenario. Consider adding test coverage for the scale-down scenario where a workload slice with fewer pods replaces one with more pods to ensure topology locality is properly maintained in both directions.

- **test/integration/singlecluster/tas/tas_test.go:3912** by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer) — 2026-01-14T06:22:00Z
```diff
@@ -3772,6 +3774,197 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 			})
 		})
 	})
+
+	ginkgo.When("TAS with ElasticJobsViaWorkloadSlices", func() {
+		var (
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueue.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+			nodes        []corev1.Node
+		)
+
+		ginkgo.BeforeEach(func() {
+			features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.ElasticJobsViaWorkloadSlices, true)
+
+			// Create two blocks with two racks each to test topology placement
+			nodes = []corev1.Node{
+				*testingnode.MakeNode("b1-r1").
+					Label("node-group", "tas").
+					Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+					Label(utiltesting.DefaultRackTopologyLevel, "r1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:    resource.MustParse("4"),
+						corev1.ResourceMemory: resource.MustParse("4Gi"),
+						corev1.ResourcePods:   resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b1-r2").
+					Label("node-group", "tas").
+					Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+					Label(utiltesting.DefaultRackTopologyLevel, "r2").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:    resource.MustParse("4"),
+						corev1.ResourceMemory: resource.MustParse("4Gi"),
+						corev1.ResourcePods:   resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b2-r1").
+					Label("node-group", "tas").
+					Label(utiltesting.DefaultBlockTopologyLevel, "b2").
+					Label(utiltesting.DefaultRackTopologyLevel, "r1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:    resource.MustParse("4"),
+						corev1.ResourceMemory: resource.MustParse("4Gi"),
+						corev1.ResourcePods:   resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+			}
+			util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+			topology = utiltestingapi.MakeDefaultTwoLevelTopology("default")
+			util.MustCreate(ctx, k8sClient, topology)
+
+			tasFlavor = utiltestingapi.MakeResourceFlavor("tas-flavor").
+				NodeLabel("node-group", "tas").
+				TopologyName("default").Obj()
+			util.MustCreate(ctx, k8sClient, tasFlavor)
+
+			clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*utiltestingapi.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "12").Obj()).
+				Obj()
+			util.MustCreate(ctx, k8sClient, clusterQueue)
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			util.MustCreate(ctx, k8sClient, localQueue)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+			for _, node := range nodes {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+			}
+		})
+
+		ginkgo.It("should place scaled-up workload in same topology domain as original", func() {
+			var wl1 *kueue.Workload
+			const jobName = "elastic-job"
+
+			// ownerRef is needed so that TopologyUngater uses GetControllerOf(wl)
+			// to get the job name and exercise the job-name index path.
+			jobOwnerRef := metav1.OwnerReference{
+				APIVersion: "batch/v1",
+				Kind:       "Job",
+				Name:       jobName,
+				UID:        "test-uid",
+				Controller: ptr.To(true),
+			}
+
+			ginkgo.By("create initial workload with 2 pods requiring a block topology", func() {
+				wl1 = utiltestingapi.MakeWorkload("wl1", ns.Name).
+					Queue(kueue.LocalQueueName(localQueue.Name)).
+					Annotation(kueue.JobNameAnnotation, jobName).
+					Obj()
+				wl1.OwnerReferences = []metav1.OwnerReference{jobOwnerRef}
+				wl1.Spec.PodSets[0] = *utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 2).
+					Request(corev1.ResourceCPU, "1").
+					RequiredTopologyRequest(utiltesting.DefaultBlockTopologyLevel).
+					Image("image").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl1)
+			})
+
+			ginkgo.By("verify the workload is admitted", func() {
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
+			})
+
+			var originalAssignment *kueue.TopologyAssignment
+			ginkgo.By("verify admission for the workload1 and record the assignment", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+					g.Expect(wl1.Status.Admission).ShouldNot(gomega.BeNil())
+					g.Expect(wl1.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
+					g.Expect(wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment).ShouldNot(gomega.BeNil())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				originalAssignment = wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment
+			})
+
+			ginkgo.By("verify the original assignment is in block b1 or b2", func() {
+				// Extract the block from the original assignment
+				gomega.Expect(originalAssignment).ShouldNot(gomega.BeNil())
+				gomega.Expect(originalAssignment.Slices).ShouldNot(gomega.BeEmpty())
+			})
+
+			ginkgo.By("create pods with JobNameAnnotation simulating running pods", func() {
+				// Create pods with the job name annotation - this is what the job controller would do
+				for i := range 2 {
+					pod := testingpod.MakePod(fmt.Sprintf("pod-%d", i), ns.Name).
+						Annotation(kueue.WorkloadAnnotation, wl1.Name).
+						Annotation(kueue.JobNameAnnotation, jobName).
```

  The test pods created to simulate running pods are missing the required PodSetLabel. The TopologyUngater's podsForPodSet function filters pods by this label, so these test pods won't be found during reconciliation. Add the PodSetLabel to the test pods to properly simulate the job controller's behavior.
  ```suggestion
  						Annotation(kueue.JobNameAnnotation, jobName).
  						Label(workloadslicing.PodSetLabel, kueue.DefaultPodSetName).
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-14T06:35:45Z

- **apis/kueue/v1beta1/topology_types.go:96** by [@mimowo](https://github.com/mimowo) — 2026-01-14T06:35:41Z
```diff
@@ -90,6 +90,10 @@ const (
 	// PodSetGroupName is an annotation indicating the name of the group of PodSets. PodSet Group
 	// is a unit flavor assignment and topology domain fitting.
 	PodSetGroupName = "kueue.x-k8s.io/podset-group-name"
+
+	// JobNameAnnotation is an annotation set on the Job's PodTemplate to indicate
+	// the name of the parent Job.
+	JobNameAnnotation = "kueue.x-k8s.io/job-name"
```

  Im wondering if the name is enough here, because maybe there are in the system two jobs with the same name but in different namespaces or belonging to different framework. Probably for namespaces we can use another filter, but for framwworks, maybe we should also have an annotation with the framework name, or directly Group and Version, wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-14T06:48:38Z

- **apis/kueue/v1beta1/topology_types.go:96** by [@mimowo](https://github.com/mimowo) — 2026-01-14T06:48:38Z
```diff
@@ -90,6 +90,10 @@ const (
 	// PodSetGroupName is an annotation indicating the name of the group of PodSets. PodSet Group
 	// is a unit flavor assignment and topology domain fitting.
 	PodSetGroupName = "kueue.x-k8s.io/podset-group-name"
+
+	// JobNameAnnotation is an annotation set on the Job's PodTemplate to indicate
+	// the name of the parent Job.
+	JobNameAnnotation = "kueue.x-k8s.io/job-name"
```

  In any case we should test such scenarios (two or more ElasticJobs with the same name but different namespace or framework) in integration or e2e tests

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-14T06:52:27Z

- **apis/kueue/v1beta1/topology_types.go:96** by [@mimowo](https://github.com/mimowo) — 2026-01-14T06:52:27Z
```diff
@@ -90,6 +90,10 @@ const (
 	// PodSetGroupName is an annotation indicating the name of the group of PodSets. PodSet Group
 	// is a unit flavor assignment and topology domain fitting.
 	PodSetGroupName = "kueue.x-k8s.io/podset-group-name"
+
+	// JobNameAnnotation is an annotation set on the Job's PodTemplate to indicate
+	// the name of the parent Job.
+	JobNameAnnotation = "kueue.x-k8s.io/job-name"
```

  Maybe an alternative design worth considering is to put on the workloads an extra label like "original-name" then when looking for the workload just identify those with the matching original-name and use already existing logic to choose the newer, WDYT?

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-14T07:13:53Z

- **apis/kueue/v1beta1/topology_types.go:96** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-14T07:13:52Z
```diff
@@ -90,6 +90,10 @@ const (
 	// PodSetGroupName is an annotation indicating the name of the group of PodSets. PodSet Group
 	// is a unit flavor assignment and topology domain fitting.
 	PodSetGroupName = "kueue.x-k8s.io/podset-group-name"
+
+	// JobNameAnnotation is an annotation set on the Job's PodTemplate to indicate
+	// the name of the parent Job.
+	JobNameAnnotation = "kueue.x-k8s.io/job-name"
```

  The job UID approach feels more direct for this problem. When a workload slice is replaced, the `kueue.x-k8s.io/workload` annotation on pods changes, which makes it hard for TopologyUngater to consistently find all pods for a job across slice transitions. 
  Using the job UID avoids this by relying on a stable identifier already available via the owner reference, allowing pods to be indexed and retrieved directly. The workload `original-name` approach adds an extra level of burden by requiring resolution from job name to workloads and then to pods, along with namespace scoping. Since the job UID is globally unique and requires no additional storage, it fits naturally here.
  
  That said, if you see benefits to the workload-label approach I might be missing, happy to discuss further!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-14T08:59:35Z

- **apis/kueue/v1beta1/topology_types.go:96** by [@mimowo](https://github.com/mimowo) — 2026-01-14T08:59:35Z
```diff
@@ -90,6 +90,10 @@ const (
 	// PodSetGroupName is an annotation indicating the name of the group of PodSets. PodSet Group
 	// is a unit flavor assignment and topology domain fitting.
 	PodSetGroupName = "kueue.x-k8s.io/podset-group-name"
+
+	// JobNameAnnotation is an annotation set on the Job's PodTemplate to indicate
+	// the name of the parent Job.
+	JobNameAnnotation = "kueue.x-k8s.io/job-name"
```

  > When a workload slice is replaced, the kueue.x-k8s.io/workload annotation on pods changes, which makes it hard for TopologyUngater to consistently find all pods for a job across slice transitions.
  
  I was thinking to just slightly change the samantic of the `kueue.x-k8s.io/workload` annotation to always contain the "origin name" for the workload. Then, each of the replacement Workloads would always have in their label the "origin name", which can be used to identify them.  In fact we could probably skip the setting of the new "original name" for the workloads if we used indexer. 
  
  >  The workload original-name approach adds an extra level of burden by requiring resolution from job name to workloads and then to pods, along with namespace scoping.
  
  Actually, I find this is the other way round. Ok, let's consider that:
  
  In TopologyUngater we ungate Pods based on the state of the Workload, so we want to find the workload. So we currently do the direct jump: **Pod -> Workload by workload name**. This no longer works for elastic Jobs, but we could do **Pod -> Workloads sharing the same "original-name"**.  The namespace filtering is already in place. The filtering of ElasticWorkloads to find the active is already there.
  
  Now, the alternative approach based on Job UID will also work, but it requires an extra jump: **Pod -> Job by UID -> Workloads**. So it means an extra Get/List request in the process at least.
  
  Wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-14T14:28:54Z

- **pkg/controller/jobframework/reconciler.go:1063** by [@mimowo](https://github.com/mimowo) — 2026-01-14T14:28:54Z
```diff
@@ -1059,7 +1055,12 @@ func expectedRunningPodSets(ctx context.Context, c client.Client, wl *kueue.Work
 	if !workload.HasQuotaReservation(wl) {
 		return nil
 	}
-	info, err := getPodSetsInfoFromStatus(ctx, c, wl)
+	// Extract job name from owner reference for TAS + ElasticJobs annotation
+	var jobName string
+	if owner := metav1.GetControllerOf(wl); owner != nil {
+		jobName = owner.Name
+	}
+	info, err := getPodSetsInfoFromStatus(ctx, c, wl, jobName)
```

  This will probably not work OOTB for integrations which have multiple owners but no controller, like PodGroups,. Currently we don't have elastic PodGroups, but I'm wondering that using the "origin workload" id, would allow us to skip that step.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-14T14:55:45Z

- **pkg/controller/jobframework/reconciler.go:1063** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-14T14:55:45Z
```diff
@@ -1059,7 +1055,12 @@ func expectedRunningPodSets(ctx context.Context, c client.Client, wl *kueue.Work
 	if !workload.HasQuotaReservation(wl) {
 		return nil
 	}
-	info, err := getPodSetsInfoFromStatus(ctx, c, wl)
+	// Extract job name from owner reference for TAS + ElasticJobs annotation
+	var jobName string
+	if owner := metav1.GetControllerOf(wl); owner != nil {
+		jobName = owner.Name
+	}
+	info, err := getPodSetsInfoFromStatus(ctx, c, wl, jobName)
```

  Yeah, good catch!

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-14T16:47:05Z

- **apis/kueue/v1beta1/topology_types.go:96** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-14T16:47:04Z
```diff
@@ -90,6 +90,10 @@ const (
 	// PodSetGroupName is an annotation indicating the name of the group of PodSets. PodSet Group
 	// is a unit flavor assignment and topology domain fitting.
 	PodSetGroupName = "kueue.x-k8s.io/podset-group-name"
+
+	// JobNameAnnotation is an annotation set on the Job's PodTemplate to indicate
+	// the name of the parent Job.
+	JobNameAnnotation = "kueue.x-k8s.io/job-name"
```

  Yeah, we discussed about this offline and have updated the implementation accordingly.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-15T15:57:47Z

- **pkg/controller/jobframework/reconciler.go:1434** by [@mimowo](https://github.com/mimowo) — 2026-01-15T15:57:41Z
```diff
@@ -1416,6 +1430,26 @@ func getPodSetsInfoFromStatus(ctx context.Context, c client.Client, w *kueue.Wor
 	return podSetsInfo, nil
 }
 
+// getWorkloadSliceOriginName returns the original workload name in a replacement chain.
+func getWorkloadSliceOriginName(ctx context.Context, c client.Client, wl *kueue.Workload) string {
```

  This function cannot depend on getting back the first workload in the chain, as it may be already deleted. I think we need to carry that information on all workloads

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-15T17:19:48Z

- **pkg/controller/tas/node_failure_controller.go:184** by [@mimowo](https://github.com/mimowo) — 2026-01-15T17:19:48Z
```diff
@@ -176,19 +180,33 @@ func (r *nodeFailureReconciler) SetupWithManager(mgr ctrl.Manager, cfg *config.C
 		Complete(core.WithLeadingManager(mgr, r, &corev1.Node{}, cfg))
 }
 
-// getWorkloadsOnNode gets all workloads that have the given node assigned in TAS topology assignment
-func (r *nodeFailureReconciler) getWorkloadsOnNode(ctx context.Context, nodeName string) (sets.Set[types.NamespacedName], error) {
```

  I think you could keep the previous interface (returning sets.Set[types.NamespacedName]) to avoid the diff. This would improve readability of the PR.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-15T17:23:18Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:523** by [@mimowo](https://github.com/mimowo) — 2026-01-15T17:23:19Z
```diff
@@ -512,7 +516,21 @@ func (s *TASFlavorSnapshot) FindTopologyAssignmentsForFlavor(flavorTASRequests F
 		} else {
 			leader, workers := findLeaderAndWorkers(trs)
 
-			assignments, reason := s.findTopologyAssignment(workers, leader, assumedUsage, opts.simulateEmpty, "")
+			// For elastic workload slices with a previous assignment, try to place
+			// new pods in the same topology domain as existing pods
+			var requiredDomain utiltas.TopologyDomainID
+			var freedUsage map[utiltas.TopologyDomainID]resources.Requests
+			if workers.PreviousTopologyAssignment != nil {
```

  Please wrap the code checking for the ElasticJobsViaWorkloadSlices feature gate. I know this is true that currently it means that `workers.PreviousTopologyAssignment != nil`, but code evolves and over checking that will be harder. using the feature gate will also give a clear indication why this was introduced.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-15T17:24:48Z

- **pkg/controller/jobframework/reconciler.go:1151** by [@mimowo](https://github.com/mimowo) — 2026-01-15T17:24:48Z
```diff
@@ -1139,7 +1144,11 @@ func (r *JobReconciler) updateWorkloadToMatchJob(ctx context.Context, job Generi
 
 // startJob will unsuspend the job, and also inject the node affinity.
 func (r *JobReconciler) startJob(ctx context.Context, job GenericJob, object client.Object, wl *kueue.Workload) error {
-	info, err := getPodSetsInfoFromStatus(ctx, r.client, wl)
+	var workloadSliceName string
+	if features.Enabled(features.ElasticJobsViaWorkloadSlices) {
+		workloadSliceName = getWorkloadSliceOriginName(ctx, r.client, wl)
+	}
+	info, err := getPodSetsInfoFromStatus(ctx, r.client, wl, workloadSliceName)
```

  This looks like code duplication with https://github.com/kubernetes-sigs/kueue/pull/8580/changes#diff-552ab657667de70900b502e2617bcb405fbf4203ff08b2a35d7c87f2d637a75bR1062-R1067. Can we please wrap this under the a common function, or even better just under pre-existing `getPodSetsInfoFromStatus`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-15T17:25:27Z

- **pkg/controller/tas/indexer/indexer.go:111** by [@mimowo](https://github.com/mimowo) — 2026-01-15T17:25:27Z
```diff
@@ -94,6 +108,10 @@ func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
 		return fmt.Errorf("setting index pod workload: %w", err)
 	}
 
+	if err := indexer.IndexField(ctx, &corev1.Pod{}, WorkloadSliceNameKey, indexPodWorkloadSliceName); err != nil {
```

  Only register the indexer if ElasticJobViaWorkloadSlices is enabled

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-15T17:26:25Z

- **pkg/controller/tas/node_failure_controller.go:262** by [@mimowo](https://github.com/mimowo) — 2026-01-15T17:26:25Z
```diff
@@ -219,25 +235,42 @@ func (r *nodeFailureReconciler) getWorkloadsForImmediateReplacement(ctx context.
 	}
 
 	affectedWorkloads := sets.New[types.NamespacedName]()
-	for wlKey := range tasWorkloadsOnNode {
-		var podsForWl corev1.PodList
-		if err := r.client.List(ctx, &podsForWl, client.InNamespace(wlKey.Namespace), client.MatchingFields{indexer.WorkloadNameKey: wlKey.Name}); err != nil {
-			return nil, fmt.Errorf("failed to list pods for workload %s: %w", wlKey, err)
+	for _, wlInfo := range tasWorkloadsOnNode {
+		podsForWl, err := r.listPodsForWorkload(ctx, wlInfo)
+		if err != nil {
+			return nil, fmt.Errorf("failed to list pods for workload %s: %w", wlInfo.key, err)
 		}
 		allPodsTerminate := true
-		for _, pod := range podsForWl.Items {
+		for _, pod := range podsForWl {
 			if pod.Spec.NodeName == nodeName && pod.DeletionTimestamp.IsZero() && !utilpod.IsTerminated(&pod) {
 				allPodsTerminate = false
 				break
 			}
 		}
 		if allPodsTerminate {
-			affectedWorkloads.Insert(wlKey)
+			affectedWorkloads.Insert(wlInfo.key)
 		}
 	}
 	return affectedWorkloads, nil
 }
 
+// listPodsForWorkload returns pods belonging to a workload.
+func (r *nodeFailureReconciler) listPodsForWorkload(ctx context.Context, wlInfo workloadInfo) ([]corev1.Pod, error) {
+	indexKey := indexer.WorkloadNameKey
+	indexValue := wlInfo.key.Name
+
+	if wlInfo.workloadSliceName != "" {
```

  wrap that into checking for the ElasticJobsViaWorklaodSlices FG

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-15T17:26:49Z

- **pkg/controller/tas/topology_ungater.go:302** by [@mimowo](https://github.com/mimowo) — 2026-01-15T17:26:50Z
```diff
@@ -283,12 +291,20 @@ func (r *topologyUngater) Generic(event.TypedGenericEvent[*kueue.Workload]) bool
 	return false
 }
 
-func (r *topologyUngater) podsForPodSet(ctx context.Context, ns, wlName string, psName kueue.PodSetReference) ([]*corev1.Pod, error) {
+func (r *topologyUngater) podsForPodSet(ctx context.Context, ns, wlName, workloadSliceName string, psName kueue.PodSetReference) ([]*corev1.Pod, error) {
 	var pods corev1.PodList
+
+	indexKey := indexer.WorkloadNameKey
+	indexValue := wlName
+	if workloadSliceName != "" {
+		indexKey = indexer.WorkloadSliceNameKey
+		indexValue = workloadSliceName
+	}
```

  This looks like code duplication, please de-duplicate

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-15T17:27:21Z

- **pkg/controller/tas/topology_ungater.go:500** by [@mimowo](https://github.com/mimowo) — 2026-01-15T17:27:21Z
```diff
@@ -479,3 +495,23 @@ func isAdmittedByTAS(w *kueue.Workload) bool {
 				return psa.TopologyAssignment != nil
 			})
 }
+
+// getWorkloadSliceOrigin returns the original workload name in a replacement chain.
+func getWorkloadSliceOrigin(ctx context.Context, c client.Client, wl *kueue.Workload) string {
```

  This looks duplicated, please de-duplicate

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-15T17:28:24Z

- **test/integration/singlecluster/tas/tas_test.go:3964** by [@mimowo](https://github.com/mimowo) — 2026-01-15T17:28:24Z
```diff
@@ -3772,6 +3774,335 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 			})
 		})
 	})
+
+	ginkgo.When("TAS with ElasticJobsViaWorkloadSlices", func() {
+		var (
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueue.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+			nodes        []corev1.Node
+		)
+
+		ginkgo.BeforeEach(func() {
+			features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.ElasticJobsViaWorkloadSlices, true)
+
+			// Create two blocks with two racks each to test topology placement
+			nodes = []corev1.Node{
+				*testingnode.MakeNode("b1-r1").
+					Label("node-group", "tas").
+					Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+					Label(utiltesting.DefaultRackTopologyLevel, "r1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:    resource.MustParse("8"),
+						corev1.ResourceMemory: resource.MustParse("8Gi"),
+						corev1.ResourcePods:   resource.MustParse("20"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b1-r2").
+					Label("node-group", "tas").
+					Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+					Label(utiltesting.DefaultRackTopologyLevel, "r2").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:    resource.MustParse("8"),
+						corev1.ResourceMemory: resource.MustParse("8Gi"),
+						corev1.ResourcePods:   resource.MustParse("20"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b2-r1").
+					Label("node-group", "tas").
+					Label(utiltesting.DefaultBlockTopologyLevel, "b2").
+					Label(utiltesting.DefaultRackTopologyLevel, "r1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:    resource.MustParse("8"),
+						corev1.ResourceMemory: resource.MustParse("8Gi"),
+						corev1.ResourcePods:   resource.MustParse("20"),
+					}).
+					Ready().
+					Obj(),
+			}
+			util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+			topology = utiltestingapi.MakeDefaultTwoLevelTopology("default")
+			util.MustCreate(ctx, k8sClient, topology)
+
+			tasFlavor = utiltestingapi.MakeResourceFlavor("tas-flavor").
+				NodeLabel("node-group", "tas").
+				TopologyName("default").Obj()
+			util.MustCreate(ctx, k8sClient, tasFlavor)
+
+			clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*utiltestingapi.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "24").Obj()).
+				Obj()
+			util.MustCreate(ctx, k8sClient, clusterQueue)
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			util.MustCreate(ctx, k8sClient, localQueue)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+			for _, node := range nodes {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+			}
+		})
+
+		ginkgo.It("should place scaled-up workload in same topology domain as original", func() {
+			var wl1 *kueue.Workload
+
+			ginkgo.By("create initial workload with 2 pods requiring a block topology", func() {
+				wl1 = utiltestingapi.MakeWorkload("wl1", ns.Name).
+					Queue(kueue.LocalQueueName(localQueue.Name)).
+					Obj()
+				wl1.Spec.PodSets[0] = *utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 2).
+					Request(corev1.ResourceCPU, "1").
+					RequiredTopologyRequest(utiltesting.DefaultBlockTopologyLevel).
+					Image("image").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl1)
+			})
+
+			ginkgo.By("verify the workload is admitted", func() {
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
+			})
+
+			var originalAssignment *kueue.TopologyAssignment
+			ginkgo.By("verify admission for the workload1 and record the assignment", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+					g.Expect(wl1.Status.Admission).ShouldNot(gomega.BeNil())
+					g.Expect(wl1.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
+					g.Expect(wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment).ShouldNot(gomega.BeNil())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				originalAssignment = wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment
+			})
+
+			ginkgo.By("verify the original assignment is in block b1 or b2", func() {
+				// Extract the block from the original assignment
+				gomega.Expect(originalAssignment).ShouldNot(gomega.BeNil())
+				gomega.Expect(originalAssignment.Slices).ShouldNot(gomega.BeEmpty())
+			})
+
+			ginkgo.By("create pods simulating running pods", func() {
+				// Create pods with the workload slice name annotation - this is set by the reconciler
+				for i := range 2 {
+					pod := testingpod.MakePod(fmt.Sprintf("pod-%d", i), ns.Name).
+						Annotation(kueue.WorkloadAnnotation, wl1.Name).
+						Annotation(kueue.WorkloadSliceNameAnnotation, wl1.Name).
+						Request(corev1.ResourceCPU, "1").
+						Obj()
+					util.MustCreate(ctx, k8sClient, pod)
+				}
+			})
+
+			var wl2 *kueue.Workload
+			ginkgo.By("create a replacement workload slice with more pods", func() {
+				wl2 = utiltestingapi.MakeWorkload("wl2", ns.Name).
+					Queue(kueue.LocalQueueName(localQueue.Name)).
+					Annotation(workloadslicing.WorkloadSliceReplacementFor, string(workload.Key(wl1))).
+					Obj()
+				wl2.Spec.PodSets[0] = *utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 4).
+					Request(corev1.ResourceCPU, "1").
+					RequiredTopologyRequest(utiltesting.DefaultBlockTopologyLevel).
+					Image("image").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl2)
+			})
+
+			ginkgo.By("verify the replacement workload is admitted", func() {
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl2)
+			})
+
+			ginkgo.By("verify the replacement workload is placed in the same topology domain", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl2), wl2)).To(gomega.Succeed())
+					g.Expect(wl2.Status.Admission).ShouldNot(gomega.BeNil())
+					g.Expect(wl2.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
+					g.Expect(wl2.Status.Admission.PodSetAssignments[0].TopologyAssignment).ShouldNot(gomega.BeNil())
+
+					newAssignment := wl2.Status.Admission.PodSetAssignments[0].TopologyAssignment
+					g.Expect(newAssignment.Slices).ShouldNot(gomega.BeEmpty())
+					g.Expect(originalAssignment.Slices).ShouldNot(gomega.BeEmpty())
+
+					// Verify the replacement workload is placed in the same block as the original.
+					blockIdx := slices.Index(originalAssignment.Levels, utiltesting.DefaultBlockTopologyLevel)
+					g.Expect(blockIdx).To(gomega.BeNumerically(">=", 0), "block topology level should be present in assignment levels")
+
+					originalBlocks := map[string]struct{}{}
+					for _, dom := range slices.Collect(utiltas.InternalSeqFrom(originalAssignment)) {
+						originalBlocks[dom.Values[blockIdx]] = struct{}{}
+					}
+					newBlocks := map[string]struct{}{}
+					for _, dom := range slices.Collect(utiltas.InternalSeqFrom(newAssignment)) {
+						newBlocks[dom.Values[blockIdx]] = struct{}{}
+					}
+					g.Expect(originalBlocks).To(gomega.HaveLen(1), "original workload should be placed within a single block")
+					g.Expect(newBlocks).To(gomega.Equal(originalBlocks), "replacement workload should be placed in the same block as original")
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("should isolate workloads with same name in different namespaces", func() {
+			ns2 := &corev1.Namespace{ObjectMeta: metav1.ObjectMeta{GenerateName: "tas-elastic-ns2-"}}
+			gomega.Expect(k8sClient.Create(ctx, ns2)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns2)).To(gomega.Succeed())
+			}()
+
+			localQueue2 := utiltestingapi.MakeLocalQueue("local-queue", ns2.Name).ClusterQueue(clusterQueue.Name).Obj()
+			util.MustCreate(ctx, k8sClient, localQueue2)
+			defer func() {
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue2)).To(gomega.Succeed())
+			}()
```

  Instead of that Please cleanup all tests in AfterEach by calling DeleteNamespace

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-15T17:29:34Z

- **apis/kueue/v1beta1/topology_types.go:90** by [@mimowo](https://github.com/mimowo) — 2026-01-15T17:29:34Z
```diff
@@ -90,6 +90,9 @@ const (
 	// PodSetGroupName is an annotation indicating the name of the group of PodSets. PodSet Group
 	// is a unit flavor assignment and topology domain fitting.
 	PodSetGroupName = "kueue.x-k8s.io/podset-group-name"
+
+	// WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+	WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
```

  ```suggestion
  	// WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
  	// This annotation is alpha-level for the ElasticJobViaWorkloadSlices feature gate.
  	WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-15T17:29:48Z

- **apis/kueue/v1beta2/topology_types.go:95** by [@mimowo](https://github.com/mimowo) — 2026-01-15T17:29:48Z
```diff
@@ -90,6 +90,9 @@ const (
 	// PodSetGroupName is an annotation indicating the name of the group of PodSets. PodSet Group
 	// is a unit flavor assignment and topology domain fitting.
 	PodSetGroupName = "kueue.x-k8s.io/podset-group-name"
+
+	// WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+	WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
```

  ```suggestion
  	// WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
  	// It is set on every Workload created in the chain of the workloads, as well as on the Pods
  	// associated with that Workload.
      // This annotation is alpha-level for the ElasticJobViaWorkloadSlices feature gate.
  	WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-15T17:35:34Z

Thank you 👍 This looks very well. The comments are mostly around:
1. fixing the getWorkloadSliceOrigin to avoid relying on all workloads to stay
2. removing code duplication
3. removing unnecessary changes to minimize the diff for readabiility
3. making sure all places modified are behind the FG

Other than that the changes look straightforward, and the feature is super valueable, while still in alpha. So I'm supportive to include to 0.16.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-15T18:40:29Z

- **apis/kueue/v1beta1/topology_types.go:90** by [@mimowo](https://github.com/mimowo) — 2026-01-15T18:40:29Z
```diff
@@ -90,6 +90,10 @@ const (
 	// PodSetGroupName is an annotation indicating the name of the group of PodSets. PodSet Group
 	// is a unit flavor assignment and topology domain fitting.
 	PodSetGroupName = "kueue.x-k8s.io/podset-group-name"
+
+	// WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+	// This annotation is alpha-level for the ElasticJobViaWorkloadSlices feature gate.
+	WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
```

  ```suggestion
  	// WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
  	// It is set on every Workload created in the chain of the workloads, as well as on the Pods
  	// associated with that Workload.
  	// This annotation is alpha-level for the ElasticJobViaWorkloadSlices feature gate.
  	WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
  ```
  nit, to align the comments

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-15T18:46:11Z

- **apis/kueue/v1beta1/topology_types.go:90** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-15T18:46:11Z
```diff
@@ -90,6 +90,10 @@ const (
 	// PodSetGroupName is an annotation indicating the name of the group of PodSets. PodSet Group
 	// is a unit flavor assignment and topology domain fitting.
 	PodSetGroupName = "kueue.x-k8s.io/podset-group-name"
+
+	// WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+	// This annotation is alpha-level for the ElasticJobViaWorkloadSlices feature gate.
+	WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
```

  Fixed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-15T18:54:45Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:52** by [@mimowo](https://github.com/mimowo) — 2026-01-15T18:54:45Z
```diff
@@ -46,7 +46,19 @@ func (a *Assignment) WorkloadsTopologyRequests(wl *workload.Info, cq *schdcache.
 				continue
 			}
 			isTASImplied := isTASImplied(&podSet, cq)
-			psTASRequest, err := podSetTopologyRequest(psAssignment, wl, cq, isTASImplied, i)
+
+			// Get the previous TopologyAssignment from the old workload slice if replacing
+			var previousTopologyAssignment *kueue.TopologyAssignment
+			if a.replaceWorkloadSlice != nil && a.replaceWorkloadSlice.Obj.Status.Admission != nil {
```

  wrap with the ElasticJobViaWorkloadSlices FG

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-15T18:55:22Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:127** by [@mimowo](https://github.com/mimowo) — 2026-01-15T18:55:22Z
```diff
@@ -104,13 +117,14 @@ func podSetTopologyRequest(psAssignment *PodSetAssignment,
 	}
 
 	return &schdcache.TASPodSetRequests{
-		Count:             podCount,
-		SinglePodRequests: singlePodRequests,
-		PodSet:            podSet,
-		PodSetUpdates:     podSetUpdates,
-		Flavor:            *tasFlvr,
-		Implied:           isTASImplied,
-		PodSetGroupName:   podSetGroupName,
+		Count:                      podCount,
+		SinglePodRequests:          singlePodRequests,
+		PodSet:                     podSet,
+		PodSetUpdates:              podSetUpdates,
+		Flavor:                     *tasFlvr,
+		Implied:                    isTASImplied,
+		PodSetGroupName:            podSetGroupName,
+		PreviousTopologyAssignment: previousTopologyAssignment,
```

  ```suggestion
  		PreviousAssignment: previousAssignment,
  ```
  "Topology" is clear from the context

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-15T19:03:27Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:127** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-15T19:03:27Z
```diff
@@ -104,13 +117,14 @@ func podSetTopologyRequest(psAssignment *PodSetAssignment,
 	}
 
 	return &schdcache.TASPodSetRequests{
-		Count:             podCount,
-		SinglePodRequests: singlePodRequests,
-		PodSet:            podSet,
-		PodSetUpdates:     podSetUpdates,
-		Flavor:            *tasFlvr,
-		Implied:           isTASImplied,
-		PodSetGroupName:   podSetGroupName,
+		Count:                      podCount,
+		SinglePodRequests:          singlePodRequests,
+		PodSet:                     podSet,
+		PodSetUpdates:              podSetUpdates,
+		Flavor:                     *tasFlvr,
+		Implied:                    isTASImplied,
+		PodSetGroupName:            podSetGroupName,
+		PreviousTopologyAssignment: previousTopologyAssignment,
```

  Done!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-15T19:10:16Z

I think there are a couple of potential issues here. First, I think we cannot move already running Pods, so we need to ensure the new assignment is a superset of the previous assignment. Looking at the code this is not necessarily the case.

Let's list some of the possible cases:
1. `unconstrained` annotation used, looks like simplest, we basically run the LeastCapacity algo for the delta of Pods
2. `preferred` annotation used - this looks generally harder, but because ideally we would place the Pods close to the previous, but since this is "best effort" we could probably use "BestFit" for the new delta of Pods
3. `required` annotation used, say `required-topology=block` - this might be tricky to implement, really. This is similar problem as we solve in Node Hot Swap, but for multiple Pods. We could probably readily use the algo for one Pod, allowing scale ups per Pod, but this is generally tricky.

We don't need to solve them all while in alpha. TBH I prefer the PR to just give direction and be smaller, rather than big, but breaking :). I would rather just validate against the unsupported cases. We can easily gradually keep adding support while in alpha. Plus we test the scale down. Ideally both with e2e test.

So I'm thinking for simplicity maybe we just start with "unconstained" in 0.16. This could be already powerful to combine ElasticJobs with TAS to eliminate the quota fragmentation issues. WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-15T19:16:10Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:528** by [@mimowo](https://github.com/mimowo) — 2026-01-15T19:16:10Z
```diff
@@ -512,7 +515,21 @@ func (s *TASFlavorSnapshot) FindTopologyAssignmentsForFlavor(flavorTASRequests F
 		} else {
 			leader, workers := findLeaderAndWorkers(trs)
 
-			assignments, reason := s.findTopologyAssignment(workers, leader, assumedUsage, opts.simulateEmpty, "")
+			// For elastic workload slices with a previous assignment, try to place
+			// new pods in the same topology domain as existing pods
+			var requiredDomain utiltas.TopologyDomainID
+			var freedUsage map[utiltas.TopologyDomainID]resources.Requests
+			if features.Enabled(features.ElasticJobsViaWorkloadSlices) && workers.PreviousAssignment != nil {
+				prevAssignment := utiltas.InternalFrom(workers.PreviousAssignment)
+				if isStale, _ := s.IsTopologyAssignmentStale(prevAssignment); !isStale {
+					requiredDomain = s.requiredReplacementDomain(&workers, prevAssignment)
+					// Calculate the usage that will be freed when the replaced workload
+					// is evicted, so the replacement can reuse that capacity.
+					freedUsage = computeFreedUsage(prevAssignment, workers.SinglePodRequests)
```

  I'm actually sceptical of the approach of freeing the capacity for the previous assignment and searching from scratch, because then I cannot imagine how we ensure that the new assignment is a super set of the previous, see also comment here: https://github.com/kubernetes-sigs/kueue/pull/8580#pullrequestreview-3667158255
  
  I think a more robust approach will be to just try to find new assignment for the delta pods, similarly as we do in Node Hot Swap. This should be reasonably simple for "unconstrained" mode.
  
  As for other modes - required and preferred, we already have the algo to put one Pod next to others. Can we just loop the algo to put all delta pods? As the simple first step?

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-15T20:18:53Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:528** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-15T20:18:52Z
```diff
@@ -512,7 +515,21 @@ func (s *TASFlavorSnapshot) FindTopologyAssignmentsForFlavor(flavorTASRequests F
 		} else {
 			leader, workers := findLeaderAndWorkers(trs)
 
-			assignments, reason := s.findTopologyAssignment(workers, leader, assumedUsage, opts.simulateEmpty, "")
+			// For elastic workload slices with a previous assignment, try to place
+			// new pods in the same topology domain as existing pods
+			var requiredDomain utiltas.TopologyDomainID
+			var freedUsage map[utiltas.TopologyDomainID]resources.Requests
+			if features.Enabled(features.ElasticJobsViaWorkloadSlices) && workers.PreviousAssignment != nil {
+				prevAssignment := utiltas.InternalFrom(workers.PreviousAssignment)
+				if isStale, _ := s.IsTopologyAssignmentStale(prevAssignment); !isStale {
+					requiredDomain = s.requiredReplacementDomain(&workers, prevAssignment)
+					// Calculate the usage that will be freed when the replaced workload
+					// is evicted, so the replacement can reuse that capacity.
+					freedUsage = computeFreedUsage(prevAssignment, workers.SinglePodRequests)
```

  Looking at this more carefully, the required topology constraint is at the domain level (e.g., same block), not the node level. This implementation preserves this via `requiredReplacementDomain`, which constrains the new assignment to the same topology domain as the old.
  The "superset of nodes" property that delta-only provides is stricter than what `required` actually requires. During transition, both old and new pods are in the same domain (the constraint is satisfied).
  The delta-only approach is cleaner conceptually, but the current approach is correct for the guarantees these annotations provide. The `freedUsage` mechanism makes old nodes attractive, naturally tending toward node reuse without requiring it.
  
  That said, I'm happy to limit to `unconstrained` only for 0.16 if you prefer the smaller scope, but wanted to clarify the technical basis first.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-15T20:29:55Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:528** by [@mimowo](https://github.com/mimowo) — 2026-01-15T20:29:55Z
```diff
@@ -512,7 +515,21 @@ func (s *TASFlavorSnapshot) FindTopologyAssignmentsForFlavor(flavorTASRequests F
 		} else {
 			leader, workers := findLeaderAndWorkers(trs)
 
-			assignments, reason := s.findTopologyAssignment(workers, leader, assumedUsage, opts.simulateEmpty, "")
+			// For elastic workload slices with a previous assignment, try to place
+			// new pods in the same topology domain as existing pods
+			var requiredDomain utiltas.TopologyDomainID
+			var freedUsage map[utiltas.TopologyDomainID]resources.Requests
+			if features.Enabled(features.ElasticJobsViaWorkloadSlices) && workers.PreviousAssignment != nil {
+				prevAssignment := utiltas.InternalFrom(workers.PreviousAssignment)
+				if isStale, _ := s.IsTopologyAssignmentStale(prevAssignment); !isStale {
+					requiredDomain = s.requiredReplacementDomain(&workers, prevAssignment)
+					// Calculate the usage that will be freed when the replaced workload
+					// is evicted, so the replacement can reuse that capacity.
+					freedUsage = computeFreedUsage(prevAssignment, workers.SinglePodRequests)
```

  Ok thank you for wxplainibg. I need time to dijest and think about it tomorrow.
  
  In the meanwhile I suggest to indeed restrict the PR to the unconstrained mode first. So that the scope is focused, and easy to review. So in this PR rather than focusing on corner cases of the algo I would prefer to have also an e2e test for job demonstrating the scale up works. 
  The n2xt step for me is scale down, also with good test coverage.
  
  Now Im not saying this is only PR for 0.16. but we need to get there iteratively, otherwise we will forget many scenarios for sure. 
  
  Once the sceleton for the solution is merged with this PR, we will focus on the algorithmic side of things to support preferred and required well. 
  
  Eventually, .aybe we can support Node Hot Swap and extend to replacing multiple pods.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T16:42:08Z

- **keps/2724-topology-aware-scheduling/README.md:1365** by [@mimowo](https://github.com/mimowo) — 2026-01-16T16:42:08Z
```diff
@@ -1353,6 +1354,19 @@ to ungate a Pod. The expectation is fulfilled if the Pod is observed as ungated
 or the ungating request fails. We hold ungating if there are pending ungatings
 within the PodSet.
 
+### Support for Elastic Workloads
+
+TAS supports ElasticJobsViaWorkloadSlices. When both feature gates are enabled, TAS attempts to
+preserve topology locality across workload slice transitions during job scaling for workloads with
+**required** topology requests. Pods are indexed by a stable `kueue.x-k8s.io/workload-slice-name`
+annotation (using the origin workload name in the slice chain), and the `TopologyAssignment` from
+the old slice is used to constrain placement of new pods to the same topology domain. If the
+previous assignment is stale (e.g., node unhealthy), TAS falls back to normal placement. Preferred
+topology requests do not currently benefit from this preservation.
```

  Let's just refer to the ElasticWorkloads KEP, and discuss the algo etc. under one place.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T16:42:53Z

- **keps/77-dynamically-sized-jobs/README.md:306** by [@mimowo](https://github.com/mimowo) — 2026-01-16T16:42:53Z
```diff
@@ -275,6 +278,40 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations for workloads with required topology requests. If the previous assignment is stale
+(e.g., node failure), a new assignment is calculated. Preferred topology requests do not currently
+benefit from this preservation.
+
+#### Pod Identification Across Slices
+
+The `kueue.x-k8s.io/workload` annotation changes with each workload slice, which prevents
+TAS from finding all pods belonging to a job across slice transitions. To address this,
+a `kueue.x-k8s.io/workload-slice-name` annotation is added to pods when both features are
+enabled. This annotation contains the origin workload name in the slice chain (following
+`WorkloadSliceReplacementFor` annotations to find the root) and remains stable across slice
+transitions, allowing `TopologyUngater` to find all pods belonging to the job. The namespace
+scope in pod lookups ensures uniqueness across different namespaces.
+
+```golang
+const (
+    // WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+    WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
+)
+```
+
+#### Topology Assignment Preservation
```

  ```suggestion
  #### Scale Up
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T16:44:47Z

- **keps/77-dynamically-sized-jobs/README.md:330** by [@mimowo](https://github.com/mimowo) — 2026-01-16T16:44:47Z
```diff
@@ -275,6 +278,40 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations for workloads with required topology requests. If the previous assignment is stale
+(e.g., node failure), a new assignment is calculated. Preferred topology requests do not currently
+benefit from this preservation.
+
+#### Pod Identification Across Slices
+
+The `kueue.x-k8s.io/workload` annotation changes with each workload slice, which prevents
+TAS from finding all pods belonging to a job across slice transitions. To address this,
+a `kueue.x-k8s.io/workload-slice-name` annotation is added to pods when both features are
+enabled. This annotation contains the origin workload name in the slice chain (following
+`WorkloadSliceReplacementFor` annotations to find the root) and remains stable across slice
+transitions, allowing `TopologyUngater` to find all pods belonging to the job. The namespace
+scope in pod lookups ensures uniqueness across different namespaces.
+
+```golang
+const (
+    // WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+    WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
+)
+```
+
+#### Topology Assignment Preservation
+
+When a new workload slice replaces an old one during scale-up, the scheduler extracts
+the `TopologyAssignment` from the old slice and passes it to TAS. TAS attempts to place
+new pods in the same topology domain as existing pods. If the previous assignment is
+stale (e.g., nodes are unhealthy), TAS falls back to normal placement.
+
```

  Let's clarify that for the first interation of the integration in 0.16 we will focus on supporting only the "unconstrained" TAS for scale up. Supporting scale down and other modes "preferred" and "required" will be subject for future iterations of the feature.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T16:45:45Z

- **keps/77-dynamically-sized-jobs/README.md:330** by [@mimowo](https://github.com/mimowo) — 2026-01-16T16:45:45Z
```diff
@@ -275,6 +278,40 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations for workloads with required topology requests. If the previous assignment is stale
+(e.g., node failure), a new assignment is calculated. Preferred topology requests do not currently
+benefit from this preservation.
+
+#### Pod Identification Across Slices
+
+The `kueue.x-k8s.io/workload` annotation changes with each workload slice, which prevents
+TAS from finding all pods belonging to a job across slice transitions. To address this,
+a `kueue.x-k8s.io/workload-slice-name` annotation is added to pods when both features are
+enabled. This annotation contains the origin workload name in the slice chain (following
+`WorkloadSliceReplacementFor` annotations to find the root) and remains stable across slice
+transitions, allowing `TopologyUngater` to find all pods belonging to the job. The namespace
+scope in pod lookups ensures uniqueness across different namespaces.
+
+```golang
+const (
+    // WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+    WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
+)
+```
+
+#### Topology Assignment Preservation
+
+When a new workload slice replaces an old one during scale-up, the scheduler extracts
+the `TopologyAssignment` from the old slice and passes it to TAS. TAS attempts to place
+new pods in the same topology domain as existing pods. If the previous assignment is
+stale (e.g., nodes are unhealthy), TAS falls back to normal placement.
+
```

  Please also add the "Full integration with Topology-Aware Scheduling to graduation criteria for Beta, or clear validation against unsupported options"

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T16:46:49Z

- **keps/77-dynamically-sized-jobs/README.md:331** by [@mimowo](https://github.com/mimowo) — 2026-01-16T16:46:49Z
```diff
@@ -275,6 +278,40 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations for workloads with required topology requests. If the previous assignment is stale
+(e.g., node failure), a new assignment is calculated. Preferred topology requests do not currently
+benefit from this preservation.
+
+#### Pod Identification Across Slices
+
+The `kueue.x-k8s.io/workload` annotation changes with each workload slice, which prevents
+TAS from finding all pods belonging to a job across slice transitions. To address this,
+a `kueue.x-k8s.io/workload-slice-name` annotation is added to pods when both features are
+enabled. This annotation contains the origin workload name in the slice chain (following
+`WorkloadSliceReplacementFor` annotations to find the root) and remains stable across slice
+transitions, allowing `TopologyUngater` to find all pods belonging to the job. The namespace
+scope in pod lookups ensures uniqueness across different namespaces.
+
+```golang
+const (
+    // WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+    WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
+)
+```
+
+#### Topology Assignment Preservation
+
+When a new workload slice replaces an old one during scale-up, the scheduler extracts
+the `TopologyAssignment` from the old slice and passes it to TAS. TAS attempts to place
+new pods in the same topology domain as existing pods. If the previous assignment is
+stale (e.g., nodes are unhealthy), TAS falls back to normal placement.
+
+See [KEP-2724: Topology Aware Scheduling](../2724-topology-aware-scheduling#support-for-elastic-workloads) for TAS details.
```

  Please also cross reference the KEPs in the see-also sections of the kep.yaml files: https://github.com/kubernetes-sigs/kueue/blob/15f7b19e822992bfe21cc78cefd0e93bf8c1357d/keps/2724-topology-aware-scheduling/kep.yaml#L18-L19

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-16T16:47:55Z

I think for readability I would suggest to actually de-couple the KEP update and the implementation to separate PRs. These can be also reviewed by other folks.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-22T20:02:55Z

- **pkg/controller/tas/topology_ungater.go:318** by [@mimowo](https://github.com/mimowo) — 2026-01-22T20:02:55Z
```diff
@@ -300,12 +307,15 @@ func (r *topologyUngater) Generic(event.TypedGenericEvent[*kueue.Workload]) bool
 	return false
 }
 
-func (r *topologyUngater) podsForPodSet(ctx context.Context, ns, wlName string, psName kueue.PodSetReference) ([]*corev1.Pod, error) {
+func (r *topologyUngater) podsForPodSet(ctx context.Context, ns, wlName, workloadSliceName string, psName kueue.PodSetReference) ([]*corev1.Pod, error) {
 	var pods corev1.PodList
+
+	indexKey, indexValue := indexer.PodIndexKeyValue(wlName, workloadSliceName)
+
 	if err := r.client.List(ctx, &pods, client.InNamespace(ns), client.MatchingLabels{
 		constants.PodSetLabel: string(psName),
 	}, client.MatchingFields{
-		indexer.WorkloadNameKey: wlName,
+		indexKey: indexValue,
```

  I would rather just have one key: workloadSliceName, but it isn't a strong opnion. Let me know what you think.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-22T20:05:24Z

- **pkg/controller/tas/node_failure_controller.go:230** by [@mimowo](https://github.com/mimowo) — 2026-01-22T20:05:24Z
```diff
@@ -220,12 +217,16 @@ func (r *nodeFailureReconciler) getWorkloadsForImmediateReplacement(ctx context.
 
 	affectedWorkloads := sets.New[types.NamespacedName]()
 	for wlKey := range tasWorkloadsOnNode {
-		var podsForWl corev1.PodList
-		if err := r.client.List(ctx, &podsForWl, client.InNamespace(wlKey.Namespace), client.MatchingFields{indexer.WorkloadNameKey: wlKey.Name}); err != nil {
+		var wl kueue.Workload
+		if err := r.client.Get(ctx, wlKey, &wl); err != nil {
+			return nil, fmt.Errorf("failed to get workload %s: %w", wlKey, err)
+		}
```

  Please avoid the extra get normally, only do when ElasticWorkloads are enabled

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-22T20:11:12Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:524** by [@mimowo](https://github.com/mimowo) — 2026-01-22T20:11:12Z
```diff
@@ -511,7 +514,21 @@ func (s *TASFlavorSnapshot) FindTopologyAssignmentsForFlavor(flavorTASRequests F
 		} else {
 			leader, workers := findLeaderAndWorkers(trs)
 
-			assignments, reason := s.findTopologyAssignment(workers, leader, assumedUsage, opts.simulateEmpty, "")
+			// For elastic workload slices with a previous assignment, try to place
+			// new pods in the same topology domain as existing pods
+			var requiredDomain utiltas.TopologyDomainID
+			var freedUsage map[utiltas.TopologyDomainID]resources.Requests
+			if features.Enabled(features.ElasticJobsViaWorkloadSlices) && workers.PreviousAssignment != nil {
+				prevAssignment := utiltas.InternalFrom(workers.PreviousAssignment)
+				if isStale, _ := s.IsTopologyAssignmentStale(prevAssignment); !isStale {
+					requiredDomain = s.requiredReplacementDomain(&workers, prevAssignment)
```

  TBH I don't understand how this works (if it does). If we are using "unconstrained", then there is no requiredDomain really - the pods can spread effectively across the cluster (as long as they confrom to the topology). I think what is needed is some unit tests which would allow us to test this easily with debugger. I think we could have them in scheduler_tas_test.go.
  
  IIUC your algorithm assumes all Pods belong to the same domain, like one block and rack, but this assumption will be definitely too strong.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-22T20:13:25Z

- **apis/kueue/v1beta2/topology_types.go:95** by [@mimowo](https://github.com/mimowo) — 2026-01-22T20:13:25Z
```diff
@@ -95,6 +95,12 @@ const (
 	// PodSetGroupName is an annotation indicating the name of the group of PodSets. PodSet Group
 	// is a unit flavor assignment and topology domain fitting.
 	PodSetGroupName = "kueue.x-k8s.io/podset-group-name"
+
+	// WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+	// It is set on every Workload created in the chain of the workloads, as well as on the Pods
+	// associated with that Workload.
+	// This annotation is alpha-level for the ElasticJobViaWorkloadSlices feature gate.
+	WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
```

  I think this idea is good and we could try to decouple it in a preparatory PR for simply ungating Pods for elastic Workloads. Currently we do this: https://github.com/kubernetes-sigs/kueue/blob/main/pkg/workloadslicing/workloadslicing.go#L278-L293
  
  but this assumes the pods are immediate children on the Job. This will not be the case for JobSet etc. So, I'm wondering maybe a better next step instead of supporting TAS would be do support JobSet.
  
  I imagine the ungating could be refactored as we do in topology_ungater.go, but in any case the key is to lookup Pods directly by workload slice name regardless on the Jobs on the way of the ownership chain.
  
  Let me know what you think.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-22T20:16:21Z

- **pkg/workloadslicing/workloadslicing.go:113** by [@mimowo](https://github.com/mimowo) — 2026-01-22T20:16:21Z
```diff
@@ -91,6 +91,33 @@ func ReplacementForKey(wl *kueue.Workload) *workload.Reference {
 	return &ref
 }
 
+// GetOriginName returns the original workload name in a replacement chain.
+func GetOriginName(_ context.Context, _ client.Client, wl *kueue.Workload) string {
+	if originName, found := wl.Annotations[kueue.WorkloadSliceNameAnnotation]; found {
+		return originName
+	}
+	return wl.Name
+}
+
+// Finish updates the status of a workload slice by applying the "Finished" condition.
+// The function checks if the "Finished" condition is already applied, and if so, does nothing (NOOP).
+// If the "Finished" condition is not present, it applies the condition with the provided `reason` and `message`.
+//
+// This function performs the following:
+// 1. It checks if the "Finished" condition is already applied. If true, it returns immediately, doing nothing.
+// 2. If the "Finished" condition is not set, it patches the workload slice's status to add the "Finished" condition.
+// 3. If the patch fails, it returns an error.
+func Finish(ctx context.Context, clnt client.Client, clk clock.Clock, workloadSlice *kueue.Workload, reason, message string) error {
+	// NOOP if the workload already has "Finished" condition (irrespective of reason and message values).
+	if workload.IsFinished(workloadSlice) {
+		return nil
```

  Finish already does that: https://github.com/kubernetes-sigs/kueue/blob/main/pkg/workload/workload.go#L1421-L1424

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-22T20:16:41Z

- **pkg/workloadslicing/workloadslicing.go:95** by [@mimowo](https://github.com/mimowo) — 2026-01-22T20:16:41Z
```diff
@@ -91,6 +91,33 @@ func ReplacementForKey(wl *kueue.Workload) *workload.Reference {
 	return &ref
 }
 
+// GetOriginName returns the original workload name in a replacement chain.
+func GetOriginName(_ context.Context, _ client.Client, wl *kueue.Workload) string {
```

  ```suggestion
  func SliceName(_ context.Context, _ client.Client, wl *kueue.Workload) string {
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-22T20:17:47Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:3617** by [@mimowo](https://github.com/mimowo) — 2026-01-22T20:17:47Z
```diff
@@ -3558,6 +3558,155 @@ var _ = ginkgo.Describe("Job controller with TopologyAwareScheduling", ginkgo.Or
 	})
 })
 
+var _ = ginkgo.Describe("Job controller with TAS and ElasticJobsViaWorkloadSlices", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	const (
+		nodeGroupLabel = "node-group"
+		tasBlockLabel  = "cloud.com/topology-block"
+	)
+
+	var (
+		ns           *corev1.Namespace
+		nodes        []corev1.Node
+		topology     *kueue.Topology
+		tasFlavor    *kueue.ResourceFlavor
+		clusterQueue *kueue.ClusterQueue
+		localQueue   *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		gomega.Expect(utilfeature.DefaultMutableFeatureGate.SetFromMap(map[string]bool{
+			string(features.ElasticJobsViaWorkloadSlices): true,
+		})).Should(gomega.Succeed())
+		fwk.StartManager(ctx, cfg, managerAndControllersSetup(true, true, nil))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "tas-elastic-")
+
+		nodes = []corev1.Node{
+			*testingnode.MakeNode("b1").
+				Label(nodeGroupLabel, "tas").
+				Label(tasBlockLabel, "b1").
+				StatusAllocatable(corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+					corev1.ResourcePods:   resource.MustParse("10"),
+				}).
+				Ready().
+				Obj(),
+			*testingnode.MakeNode("b2").
+				Label(nodeGroupLabel, "tas").
+				Label(tasBlockLabel, "b2").
+				StatusAllocatable(corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+					corev1.ResourcePods:   resource.MustParse("10"),
+				}).
+				Ready().
+				Obj(),
+		}
+		for i := range nodes {
+			util.MustCreate(ctx, k8sClient, &nodes[i])
+		}
+
+		topology = utiltestingapi.MakeTopology("default").Levels(tasBlockLabel).Obj()
+		util.MustCreate(ctx, k8sClient, topology)
```

  Yes, I think this assumption is too strong. The typical use for TAS is with kubernetes.io/hostname specified at the lowest level. Looking at the current algo this will not work well in this case.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-23T06:38:38Z

- **pkg/workloadslicing/workloadslicing.go:113** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-23T06:38:38Z
```diff
@@ -91,6 +91,33 @@ func ReplacementForKey(wl *kueue.Workload) *workload.Reference {
 	return &ref
 }
 
+// GetOriginName returns the original workload name in a replacement chain.
+func GetOriginName(_ context.Context, _ client.Client, wl *kueue.Workload) string {
+	if originName, found := wl.Annotations[kueue.WorkloadSliceNameAnnotation]; found {
+		return originName
+	}
+	return wl.Name
+}
+
+// Finish updates the status of a workload slice by applying the "Finished" condition.
+// The function checks if the "Finished" condition is already applied, and if so, does nothing (NOOP).
+// If the "Finished" condition is not present, it applies the condition with the provided `reason` and `message`.
+//
+// This function performs the following:
+// 1. It checks if the "Finished" condition is already applied. If true, it returns immediately, doing nothing.
+// 2. If the "Finished" condition is not set, it patches the workload slice's status to add the "Finished" condition.
+// 3. If the patch fails, it returns an error.
+func Finish(ctx context.Context, clnt client.Client, clk clock.Clock, workloadSlice *kueue.Workload, reason, message string) error {
+	// NOOP if the workload already has "Finished" condition (irrespective of reason and message values).
+	if workload.IsFinished(workloadSlice) {
+		return nil
```

  Good catch! removed that part.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-23T06:41:20Z

- **pkg/controller/tas/topology_ungater.go:318** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-23T06:41:20Z
```diff
@@ -300,12 +307,15 @@ func (r *topologyUngater) Generic(event.TypedGenericEvent[*kueue.Workload]) bool
 	return false
 }
 
-func (r *topologyUngater) podsForPodSet(ctx context.Context, ns, wlName string, psName kueue.PodSetReference) ([]*corev1.Pod, error) {
+func (r *topologyUngater) podsForPodSet(ctx context.Context, ns, wlName, workloadSliceName string, psName kueue.PodSetReference) ([]*corev1.Pod, error) {
 	var pods corev1.PodList
+
+	indexKey, indexValue := indexer.PodIndexKeyValue(wlName, workloadSliceName)
+
 	if err := r.client.List(ctx, &pods, client.InNamespace(ns), client.MatchingLabels{
 		constants.PodSetLabel: string(psName),
 	}, client.MatchingFields{
-		indexer.WorkloadNameKey: wlName,
+		indexKey: indexValue,
```

  The current implementation uses `PodIndexKeyValue()` to dynamically select between `WorkloadNameKey` and `WorkloadSliceNameKey`. Using a single key would require changing the indexing approach to always use the workload slice name (which falls back to workload name for non-elastic workloads). Do you want me to tackle this in a follow-up PR or as part of this one?

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-23T06:52:40Z

- **pkg/workloadslicing/workloadslicing.go:95** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-23T06:52:40Z
```diff
@@ -91,6 +91,33 @@ func ReplacementForKey(wl *kueue.Workload) *workload.Reference {
 	return &ref
 }
 
+// GetOriginName returns the original workload name in a replacement chain.
+func GetOriginName(_ context.Context, _ client.Client, wl *kueue.Workload) string {
```

  Done! renamed to `SliceName` and simplified the signature to remove unused `ctx` and client parameters

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-23T07:04:38Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:524** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-23T07:04:38Z
```diff
@@ -511,7 +514,21 @@ func (s *TASFlavorSnapshot) FindTopologyAssignmentsForFlavor(flavorTASRequests F
 		} else {
 			leader, workers := findLeaderAndWorkers(trs)
 
-			assignments, reason := s.findTopologyAssignment(workers, leader, assumedUsage, opts.simulateEmpty, "")
+			// For elastic workload slices with a previous assignment, try to place
+			// new pods in the same topology domain as existing pods
+			var requiredDomain utiltas.TopologyDomainID
+			var freedUsage map[utiltas.TopologyDomainID]resources.Requests
+			if features.Enabled(features.ElasticJobsViaWorkloadSlices) && workers.PreviousAssignment != nil {
+				prevAssignment := utiltas.InternalFrom(workers.PreviousAssignment)
+				if isStale, _ := s.IsTopologyAssignmentStale(prevAssignment); !isStale {
+					requiredDomain = s.requiredReplacementDomain(&workers, prevAssignment)
```

  You're right that the previous approach had issues with unconstrained mode. I'll rework the algorithm to use delta-only placement instead. Rather than re-running placement for all pods (which could choose entirely different nodes), we now preserve the exact previous assignment as-is and only run the placement algorithm for the delta pods. This should handle unconstrained mode correctly since we're not making any assumptions about domain membership, we're just keeping existing pods where they are.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-23T07:05:49Z

- **apis/kueue/v1beta2/topology_types.go:95** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-23T07:05:49Z
```diff
@@ -95,6 +95,12 @@ const (
 	// PodSetGroupName is an annotation indicating the name of the group of PodSets. PodSet Group
 	// is a unit flavor assignment and topology domain fitting.
 	PodSetGroupName = "kueue.x-k8s.io/podset-group-name"
+
+	// WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+	// It is set on every Workload created in the chain of the workloads, as well as on the Pods
+	// associated with that Workload.
+	// This annotation is alpha-level for the ElasticJobViaWorkloadSlices feature gate.
+	WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
```

  That's a good point about the pod ownership chain. The current ungating does assume pods are direct Job children. For TAS specifically, we're already using the workloadslice name annotation for pod lookups via the indexer (WorkloadSliceNameKey), which should work regardless of ownership depth. But I agree this could be cleaner as a  separate concern. Would it make sense to track the JobSet ungating refactor as a follow-up issue?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-23T16:16:43Z

- **apis/kueue/v1beta2/topology_types.go:95** by [@mimowo](https://github.com/mimowo) — 2026-01-23T16:16:42Z
```diff
@@ -95,6 +95,12 @@ const (
 	// PodSetGroupName is an annotation indicating the name of the group of PodSets. PodSet Group
 	// is a unit flavor assignment and topology domain fitting.
 	PodSetGroupName = "kueue.x-k8s.io/podset-group-name"
+
+	// WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+	// It is set on every Workload created in the chain of the workloads, as well as on the Pods
+	// associated with that Workload.
+	// This annotation is alpha-level for the ElasticJobViaWorkloadSlices feature gate.
+	WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
```

  What about making it just a preparetory PR to introduce the workload-slice annot, and refactor how we ungate Pods / find them, wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-23T16:17:29Z

- **apis/kueue/v1beta2/topology_types.go:95** by [@mimowo](https://github.com/mimowo) — 2026-01-23T16:17:29Z
```diff
@@ -95,6 +95,12 @@ const (
 	// PodSetGroupName is an annotation indicating the name of the group of PodSets. PodSet Group
 	// is a unit flavor assignment and topology domain fitting.
 	PodSetGroupName = "kueue.x-k8s.io/podset-group-name"
+
+	// WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+	// It is set on every Workload created in the chain of the workloads, as well as on the Pods
+	// associated with that Workload.
+	// This annotation is alpha-level for the ElasticJobViaWorkloadSlices feature gate.
+	WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
```

  Yes, we could make support for JobSet as a tangible new feature based on that, but I'm ok just with the prep refactor.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-23T16:28:18Z

- **apis/kueue/v1beta2/topology_types.go:95** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-23T16:28:18Z
```diff
@@ -95,6 +95,12 @@ const (
 	// PodSetGroupName is an annotation indicating the name of the group of PodSets. PodSet Group
 	// is a unit flavor assignment and topology domain fitting.
 	PodSetGroupName = "kueue.x-k8s.io/podset-group-name"
+
+	// WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+	// It is set on every Workload created in the chain of the workloads, as well as on the Pods
+	// associated with that Workload.
+	// This annotation is alpha-level for the ElasticJobViaWorkloadSlices feature gate.
+	WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
```

  Yeah, that makes sense!

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-23T17:14:52Z

- **apis/kueue/v1beta2/topology_types.go:95** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-23T17:14:52Z
```diff
@@ -95,6 +95,12 @@ const (
 	// PodSetGroupName is an annotation indicating the name of the group of PodSets. PodSet Group
 	// is a unit flavor assignment and topology domain fitting.
 	PodSetGroupName = "kueue.x-k8s.io/podset-group-name"
+
+	// WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+	// It is set on every Workload created in the chain of the workloads, as well as on the Pods
+	// associated with that Workload.
+	// This annotation is alpha-level for the ElasticJobViaWorkloadSlices feature gate.
+	WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
```

  https://github.com/kubernetes-sigs/kueue/pull/8768

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-23T17:24:00Z

- **apis/kueue/v1beta2/topology_types.go:95** by [@mimowo](https://github.com/mimowo) — 2026-01-23T17:24:00Z
```diff
@@ -95,6 +95,12 @@ const (
 	// PodSetGroupName is an annotation indicating the name of the group of PodSets. PodSet Group
 	// is a unit flavor assignment and topology domain fitting.
 	PodSetGroupName = "kueue.x-k8s.io/podset-group-name"
+
+	// WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+	// It is set on every Workload created in the chain of the workloads, as well as on the Pods
+	// associated with that Workload.
+	// This annotation is alpha-level for the ElasticJobViaWorkloadSlices feature gate.
+	WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
```

  Thank you, your PR looks great overally, I will move reviewing there now. I think we can try to include this one in 0.16.0, but the algo work move for later.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-23T18:30:29Z

- **apis/kueue/v1beta2/topology_types.go:95** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-23T18:30:29Z
```diff
@@ -95,6 +95,12 @@ const (
 	// PodSetGroupName is an annotation indicating the name of the group of PodSets. PodSet Group
 	// is a unit flavor assignment and topology domain fitting.
 	PodSetGroupName = "kueue.x-k8s.io/podset-group-name"
+
+	// WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+	// It is set on every Workload created in the chain of the workloads, as well as on the Pods
+	// associated with that Workload.
+	// This annotation is alpha-level for the ElasticJobViaWorkloadSlices feature gate.
+	WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
```

   Thanks! I'd love to get the algo work into 0.16 as well if possible.                                                                                                   The best part is that unconstrained mode is actually the simplest case to handle correctly. Since there are no topology constraints to satisfy, we just need to ensure the new assignment is a superset of the old one. The existing pods stay where they are (they're already running), and we only place the delta pods using the same              `LeastFreeCapacity` packing. The previous nodes get marked as "in use" so the algorithm doesn't try to pack more pods there than will fit.                                   
  
  The tricky part with `required/preferred` modes is that the combined assignment needs to satisfy topology constraints across all pods. That's why we're explicitly rejecting those for alpha. But unconstrained doesn't have that problem IMO.   Thoughts?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-23T18:35:26Z

- **apis/kueue/v1beta2/topology_types.go:95** by [@mimowo](https://github.com/mimowo) — 2026-01-23T18:35:25Z
```diff
@@ -95,6 +95,12 @@ const (
 	// PodSetGroupName is an annotation indicating the name of the group of PodSets. PodSet Group
 	// is a unit flavor assignment and topology domain fitting.
 	PodSetGroupName = "kueue.x-k8s.io/podset-group-name"
+
+	// WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+	// It is set on every Workload created in the chain of the workloads, as well as on the Pods
+	// associated with that Workload.
+	// This annotation is alpha-level for the ElasticJobViaWorkloadSlices feature gate.
+	WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
```

  sgtm, actually preferred is in grey zone. We just try to bin pack, but there is no strong restriction. In any case, I want to consider it as a follow up.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-23T21:48:29Z

- **pkg/controller/tas/topology_ungater.go:318** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-23T21:48:29Z
```diff
@@ -300,12 +307,15 @@ func (r *topologyUngater) Generic(event.TypedGenericEvent[*kueue.Workload]) bool
 	return false
 }
 
-func (r *topologyUngater) podsForPodSet(ctx context.Context, ns, wlName string, psName kueue.PodSetReference) ([]*corev1.Pod, error) {
+func (r *topologyUngater) podsForPodSet(ctx context.Context, ns, wlName, workloadSliceName string, psName kueue.PodSetReference) ([]*corev1.Pod, error) {
 	var pods corev1.PodList
+
+	indexKey, indexValue := indexer.PodIndexKeyValue(wlName, workloadSliceName)
+
 	if err := r.client.List(ctx, &pods, client.InNamespace(ns), client.MatchingLabels{
 		constants.PodSetLabel: string(psName),
 	}, client.MatchingFields{
-		indexer.WorkloadNameKey: wlName,
+		indexKey: indexValue,
```

  Alright, I fixed that part in this PR.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-26T09:12:22Z

- **pkg/controller/tas/indexer/indexer.go:62** by [@mimowo](https://github.com/mimowo) — 2026-01-26T09:12:22Z
```diff
@@ -56,6 +57,24 @@ func indexPodWorkload(o client.Object) []string {
 	return []string{value}
 }
 
+// indexPodWorkloadSliceName indexes pods by their workload slice name.
+// Uses WorkloadSliceNameAnnotation if present, otherwise falls back to WorkloadAnnotation.
+func indexPodWorkloadSliceName(o client.Object) []string {
```

  This indexer looks almost like the one introduced recently. I think we could consolidate them, and keep only one in the core.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-26T09:13:16Z

- **test/integration/singlecluster/tas/tas_test.go:853** by [@mimowo](https://github.com/mimowo) — 2026-01-26T09:13:16Z
```diff
@@ -849,7 +850,7 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 				ginkgo.By("creating a workload which requires block and can fit", func() {
 					wl = utiltestingapi.MakeWorkload("wl", ns.Name).
 						Queue(kueue.LocalQueueName(localQueue.Name)).PodSets(*utiltestingapi.MakePodSet("worker", 2).
-						RequiredTopologyRequest(utiltesting.DefaultBlockTopologyLevel).
+						UnconstrainedTopologyRequest().
```

  Why updating the pre-existing tests?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-26T09:14:06Z

- **pkg/controller/tas/node_failure_controller.go:76** by [@mimowo](https://github.com/mimowo) — 2026-01-26T09:14:06Z
```diff
@@ -73,8 +74,6 @@ func (r *nodeFailureReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	log.V(2).Info("Reconcile Node")
 
 	var node corev1.Node
-	var affectedWorkloads sets.Set[types.NamespacedName]
```

  These changes lgtm, but please revert for the sake of keeping PRs small and readeable. Feel free to open a dedicated cleanup PR.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-26T09:16:33Z

- **pkg/controller/tas/indexer/indexer.go:116** by [@mimowo](https://github.com/mimowo) — 2026-01-26T09:16:33Z
```diff
@@ -94,6 +113,10 @@ func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
 		return fmt.Errorf("setting index pod workload: %w", err)
 	}
 
+	if err := indexer.IndexField(ctx, &corev1.Pod{}, WorkloadSliceNameKey, indexPodWorkloadSliceName); err != nil {
```

  Actually, I think this indexer will collide on the field name on the preexisting once, so it will not work e2e. Let's try to consolidate both indexers. Alternatively, use different field names for indexing using both of them.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-26T15:25:38Z

- **pkg/controller/tas/indexer/indexer.go:62** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-26T15:25:38Z
```diff
@@ -56,6 +57,24 @@ func indexPodWorkload(o client.Object) []string {
 	return []string{value}
 }
 
+// indexPodWorkloadSliceName indexes pods by their workload slice name.
+// Uses WorkloadSliceNameAnnotation if present, otherwise falls back to WorkloadAnnotation.
+func indexPodWorkloadSliceName(o client.Object) []string {
```

  Yeah, that makes sense!

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-26T15:25:58Z

- **test/integration/singlecluster/tas/tas_test.go:853** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-26T15:25:58Z
```diff
@@ -849,7 +850,7 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 				ginkgo.By("creating a workload which requires block and can fit", func() {
 					wl = utiltestingapi.MakeWorkload("wl", ns.Name).
 						Queue(kueue.LocalQueueName(localQueue.Name)).PodSets(*utiltestingapi.MakePodSet("worker", 2).
-						RequiredTopologyRequest(utiltesting.DefaultBlockTopologyLevel).
+						UnconstrainedTopologyRequest().
```

  whoops, let me update that (testing residue)

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-26T15:26:47Z

- **pkg/controller/tas/node_failure_controller.go:76** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-26T15:26:47Z
```diff
@@ -73,8 +74,6 @@ func (r *nodeFailureReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	log.V(2).Info("Reconcile Node")
 
 	var node corev1.Node
-	var affectedWorkloads sets.Set[types.NamespacedName]
```

  I will create a separate commit for this as the change is related to the overall fix. Are you okay with that?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-03T17:02:06Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:3699** by [@mimowo](https://github.com/mimowo) — 2026-02-03T17:02:06Z
```diff
@@ -3558,6 +3558,155 @@ var _ = ginkgo.Describe("Job controller with TopologyAwareScheduling", ginkgo.Or
 	})
 })
 
+var _ = ginkgo.Describe("Job controller with TAS and ElasticJobsViaWorkloadSlices", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	const (
+		nodeGroupLabel = "node-group"
+		tasBlockLabel  = "cloud.com/topology-block"
+	)
+
+	var (
+		ns           *corev1.Namespace
+		nodes        []corev1.Node
+		topology     *kueue.Topology
+		tasFlavor    *kueue.ResourceFlavor
+		clusterQueue *kueue.ClusterQueue
+		localQueue   *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		gomega.Expect(utilfeature.DefaultMutableFeatureGate.SetFromMap(map[string]bool{
+			string(features.ElasticJobsViaWorkloadSlices): true,
+		})).Should(gomega.Succeed())
+		fwk.StartManager(ctx, cfg, managerAndControllersSetup(true, true, nil))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "tas-elastic-")
+
+		nodes = []corev1.Node{
+			*testingnode.MakeNode("b1").
+				Label(nodeGroupLabel, "tas").
+				Label(tasBlockLabel, "b1").
+				StatusAllocatable(corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+					corev1.ResourcePods:   resource.MustParse("10"),
+				}).
+				Ready().
+				Obj(),
+			*testingnode.MakeNode("b2").
+				Label(nodeGroupLabel, "tas").
+				Label(tasBlockLabel, "b2").
+				StatusAllocatable(corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+					corev1.ResourcePods:   resource.MustParse("10"),
+				}).
+				Ready().
+				Obj(),
+		}
+		for i := range nodes {
+			util.MustCreate(ctx, k8sClient, &nodes[i])
+		}
+
+		topology = utiltestingapi.MakeTopology("default").Levels(tasBlockLabel).Obj()
+		util.MustCreate(ctx, k8sClient, topology)
+
+		tasFlavor = utiltestingapi.MakeResourceFlavor("tas-flavor").
+			NodeLabel(nodeGroupLabel, "tas").
+			TopologyName(topology.Name).Obj()
+		util.MustCreate(ctx, k8sClient, tasFlavor)
+
+		clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas(tasFlavor.Name).
+					Resource(corev1.ResourceCPU, "5").
+					Resource(corev1.ResourceMemory, "5Gi").
+					Obj(),
+			).Obj()
+		util.MustCreate(ctx, k8sClient, clusterQueue)
+
+		localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+		util.MustCreate(ctx, k8sClient, localQueue)
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+		for i := range nodes {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, &nodes[i], true)
+		}
+	})
+
+	ginkgo.It("should scale up an elastic job with TAS unconstrained topology", func() {
+		job := testingjob.MakeJob("elastic-job", ns.Name).
+			Queue(kueue.LocalQueueName(localQueue.Name)).
+			PodAnnotation(kueue.PodSetUnconstrainedTopologyAnnotation, "true").
+			SetAnnotation(workloadslicing.EnabledAnnotationKey, workloadslicing.EnabledAnnotationValue).
+			Parallelism(1).
+			Completions(10).
+			Request(corev1.ResourceCPU, "100m").
+			Obj()
+
+		ginkgo.By("creating an elastic job with unconstrained topology", func() {
+			util.MustCreate(ctx, k8sClient, job)
+		})
+
+		var originalWorkload *kueue.Workload
+		ginkgo.By("verify the workload is created and admitted with topology assignment", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				workloads := &kueue.WorkloadList{}
+				g.Expect(k8sClient.List(ctx, workloads, client.InNamespace(ns.Name))).Should(gomega.Succeed())
+				g.Expect(workloads.Items).Should(gomega.HaveLen(1))
+				originalWorkload = &workloads.Items[0]
+				g.Expect(originalWorkload.Spec.PodSets).Should(gomega.HaveLen(1))
+				g.Expect(originalWorkload.Spec.PodSets[0].TopologyRequest).ShouldNot(gomega.BeNil())
+				g.Expect(originalWorkload.Spec.PodSets[0].TopologyRequest.Unconstrained).Should(gomega.Equal(ptr.To(true)))
+				g.Expect(originalWorkload.Status.Admission).ShouldNot(gomega.BeNil())
+				g.Expect(originalWorkload.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
+				g.Expect(originalWorkload.Status.Admission.PodSetAssignments[0].TopologyAssignment).ShouldNot(gomega.BeNil())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		originalWorkloadName := originalWorkload.Name
+
+		ginkgo.By("scale up the job by increasing parallelism", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(job), job)).Should(gomega.Succeed())
+				job.Spec.Parallelism = ptr.To(int32(2))
+				g.Expect(k8sClient.Update(ctx, job)).Should(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("verify a new workload slice is created and admitted with topology assignment", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				workloadList := &kueue.WorkloadList{}
+				g.Expect(k8sClient.List(ctx, workloadList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+
+				var newWorkload *kueue.Workload
+				for i := range workloadList.Items {
+					wl := &workloadList.Items[i]
+					if wl.Name != originalWorkloadName && !apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
+						newWorkload = wl
+						break
+					}
+				}
```

  Hm I think we need to have some helper in the workloadslicing package for finding the latest workload, because this is what we also do in our controllers - find the "newest" workload. Maybe we need to expose it better as a helper.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-03T17:03:34Z

- **pkg/controller/tas/node_failure_controller.go:92** by [@mimowo](https://github.com/mimowo) — 2026-02-03T17:03:34Z
```diff
@@ -90,19 +89,19 @@ func (r *nodeFailureReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 
 	if errMsg != "" {
 		log.V(3).Info(fmt.Sprintf("%s. Marking as failed immediately", errMsg))
-		affectedWorkloads, err = r.getWorkloadsOnNode(ctx, req.Name)
+		workloads, err := r.getWorkloadsOnNode(ctx, req.Name)
```

  nit, but I think we can revert this changes just to keep the diff minimal

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-03T17:05:35Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:518** by [@mimowo](https://github.com/mimowo) — 2026-02-03T17:05:35Z
```diff
@@ -511,6 +514,69 @@ func (s *TASFlavorSnapshot) FindTopologyAssignmentsForFlavor(flavorTASRequests F
 		} else {
 			leader, workers := findLeaderAndWorkers(trs)
 
+			// Delta-only placement for elastic workloads: keep previous pods fixed, place only new pods.
+			if features.Enabled(features.ElasticJobsViaWorkloadSlices) && workers.PreviousAssignment != nil {
```

  This function is getting too long, I would like to extract handling ElasticWorkloads to a helper, maybe in `tas_elastic_workloads.go` wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-03T17:07:21Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:523** by [@mimowo](https://github.com/mimowo) — 2026-02-03T17:07:21Z
```diff
@@ -511,6 +514,69 @@ func (s *TASFlavorSnapshot) FindTopologyAssignmentsForFlavor(flavorTASRequests F
 		} else {
 			leader, workers := findLeaderAndWorkers(trs)
 
+			// Delta-only placement for elastic workloads: keep previous pods fixed, place only new pods.
+			if features.Enabled(features.ElasticJobsViaWorkloadSlices) && workers.PreviousAssignment != nil {
+				prevAssignment := utiltas.InternalFrom(workers.PreviousAssignment)
+
+				if isStale, staleDomain := s.IsTopologyAssignmentStale(prevAssignment); isStale {
+					s.log.V(3).Info("previous TAS assignment is stale, doing fresh placement",
+						"staleDomain", staleDomain)
```

  Can you remind me here what "stale" means? I'm wondering we can truly recover from that. I'm wondering it might be better to just evict the workload and let it get re-scheduled if something is going wrong.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-03T17:09:21Z

- **pkg/cache/scheduler/tas_flavor_snapshot_test.go:520** by [@mimowo](https://github.com/mimowo) — 2026-02-03T17:09:21Z
```diff
@@ -516,3 +516,209 @@ func TestSortedDomainsWithLeader(t *testing.T) {
 		})
 	}
 }
+
+func TestCountPodsInAssignment(t *testing.T) {
```

  I would like to cover these functions with unit tests because they are internal. Unless we can move them to the `pkg/utl/tas/tas_assignment.go`, and there we export them and unit test.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-03T17:09:47Z

- **pkg/cache/scheduler/tas_flavor_snapshot_test.go:520** by [@mimowo](https://github.com/mimowo) — 2026-02-03T17:09:47Z
```diff
@@ -516,3 +516,209 @@ func TestSortedDomainsWithLeader(t *testing.T) {
 		})
 	}
 }
+
+func TestCountPodsInAssignment(t *testing.T) {
```

  The functions look generic enough to land in utils, so I'm leaning to this option.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-03T17:11:40Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:4142** by [@mimowo](https://github.com/mimowo) — 2026-02-03T17:11:40Z
```diff
@@ -4021,6 +4021,147 @@ func TestWorkloadsTopologyRequests_ErrorBranches(t *testing.T) {
 	}
 }
 
+func TestWorkloadsTopologyRequests_ElasticJobsValidation(t *testing.T) {
+	features.SetFeatureGateDuringTest(t, features.ElasticJobsViaWorkloadSlices, true)
+
+	// Create a mock TAS flavor snapshot
+	tasFlavor := &schdcache.TASFlavorSnapshot{}
+
+	cases := map[string]struct {
+		cq         schdcache.ClusterQueueSnapshot
+		assignment Assignment
+		workload   workload.Info
+		wantErr    string
+	}{
+		"required topology is rejected with ElasticJobsViaWorkloadSlices": {
+			cq: schdcache.ClusterQueueSnapshot{
+				TASFlavors: map[kueue.ResourceFlavorReference]*schdcache.TASFlavorSnapshot{"tas": tasFlavor},
+			},
+			assignment: Assignment{
+				PodSets: []PodSetAssignment{{
+					Name: kueue.DefaultPodSetName,
+					Flavors: ResourceAssignment{
+						corev1.ResourceCPU: {Name: "tas", Mode: Fit, TriedFlavorIdx: -1},
+					},
+					Count:  2,
+					Status: *NewStatus(),
+				}},
+				replaceWorkloadSlice: workload.NewInfo(&kueue.Workload{
+					Status: kueue.WorkloadStatus{
+						Admission: &kueue.Admission{
+							PodSetAssignments: []kueue.PodSetAssignment{{
+								Name:               kueue.DefaultPodSetName,
+								TopologyAssignment: &kueue.TopologyAssignment{},
+							}},
+						},
+					},
+				}),
+			},
+			workload: *workload.NewInfo(&kueue.Workload{
+				Spec: kueue.WorkloadSpec{
+					PodSets: []kueue.PodSet{
+						*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 2).
+							Request(corev1.ResourceCPU, "1").
+							RequiredTopologyRequest(corev1.LabelHostname).
+							Obj(),
+					},
+				},
+			}),
+			wantErr: "required topology is not supported with ElasticJobsViaWorkloadSlices",
+		},
+		"preferred topology is rejected with ElasticJobsViaWorkloadSlices": {
+			cq: schdcache.ClusterQueueSnapshot{
+				TASFlavors: map[kueue.ResourceFlavorReference]*schdcache.TASFlavorSnapshot{"tas": tasFlavor},
+			},
+			assignment: Assignment{
+				PodSets: []PodSetAssignment{{
+					Name: kueue.DefaultPodSetName,
+					Flavors: ResourceAssignment{
+						corev1.ResourceCPU: {Name: "tas", Mode: Fit, TriedFlavorIdx: -1},
+					},
+					Count:  2,
+					Status: *NewStatus(),
+				}},
+				replaceWorkloadSlice: workload.NewInfo(&kueue.Workload{
+					Status: kueue.WorkloadStatus{
+						Admission: &kueue.Admission{
+							PodSetAssignments: []kueue.PodSetAssignment{{
+								Name:               kueue.DefaultPodSetName,
+								TopologyAssignment: &kueue.TopologyAssignment{},
+							}},
+						},
+					},
+				}),
+			},
+			workload: *workload.NewInfo(&kueue.Workload{
+				Spec: kueue.WorkloadSpec{
+					PodSets: []kueue.PodSet{
+						*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 2).
+							Request(corev1.ResourceCPU, "1").
+							PreferredTopologyRequest(corev1.LabelHostname).
+							Obj(),
+					},
+				},
+			}),
+			wantErr: "preferred topology is not supported with ElasticJobsViaWorkloadSlices",
+		},
+		"unconstrained topology is accepted with ElasticJobsViaWorkloadSlices": {
+			cq: schdcache.ClusterQueueSnapshot{
+				TASFlavors: map[kueue.ResourceFlavorReference]*schdcache.TASFlavorSnapshot{"tas": tasFlavor},
+			},
+			assignment: Assignment{
+				PodSets: []PodSetAssignment{{
+					Name: kueue.DefaultPodSetName,
+					Flavors: ResourceAssignment{
+						corev1.ResourceCPU: {Name: "tas", Mode: Fit, TriedFlavorIdx: -1},
+					},
+					Count:  2,
+					Status: *NewStatus(),
+				}},
+				replaceWorkloadSlice: workload.NewInfo(&kueue.Workload{
+					Status: kueue.WorkloadStatus{
+						Admission: &kueue.Admission{
+							PodSetAssignments: []kueue.PodSetAssignment{{
+								Name:               kueue.DefaultPodSetName,
+								TopologyAssignment: &kueue.TopologyAssignment{},
+							}},
+						},
+					},
+				}),
+			},
+			workload: *workload.NewInfo(&kueue.Workload{
+				Spec: kueue.WorkloadSpec{
+					PodSets: []kueue.PodSet{
+						*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 2).
+							Request(corev1.ResourceCPU, "1").
+							UnconstrainedTopologyRequest().
+							Obj(),
+					},
+				},
+			}),
+			wantErr: "", // No error expected
```

  nit, you should be able to drop this line

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-03T17:12:42Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:518** by [@mimowo](https://github.com/mimowo) — 2026-02-03T17:12:42Z
```diff
@@ -511,6 +514,69 @@ func (s *TASFlavorSnapshot) FindTopologyAssignmentsForFlavor(flavorTASRequests F
 		} else {
 			leader, workers := findLeaderAndWorkers(trs)
 
+			// Delta-only placement for elastic workloads: keep previous pods fixed, place only new pods.
+			if features.Enabled(features.ElasticJobsViaWorkloadSlices) && workers.PreviousAssignment != nil {
```

  What is the unit test coverage by unit tests running the exported functions, like those in flavorassigner_test.go?

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-10T05:44:01Z

- **pkg/controller/tas/node_failure_controller.go:92** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-10T05:44:01Z
```diff
@@ -90,19 +89,19 @@ func (r *nodeFailureReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 
 	if errMsg != "" {
 		log.V(3).Info(fmt.Sprintf("%s. Marking as failed immediately", errMsg))
-		affectedWorkloads, err = r.getWorkloadsOnNode(ctx, req.Name)
+		workloads, err := r.getWorkloadsOnNode(ctx, req.Name)
```

  I added a separate commit for this change.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-12T15:51:21Z

- **pkg/cache/scheduler/tas_flavor_snapshot_test.go:665** by [@mimowo](https://github.com/mimowo) — 2026-02-12T15:51:21Z
```diff
@@ -516,3 +516,209 @@ func TestSortedDomainsWithLeader(t *testing.T) {
 		})
 	}
 }
+
+func TestCountPodsInAssignment(t *testing.T) {
+	cases := map[string]struct {
+		assignment *tas.TopologyAssignment
+		want       int32
+	}{
+		"empty assignment": {
+			assignment: &tas.TopologyAssignment{
+				Levels:  []string{"hostname"},
+				Domains: nil,
+			},
+			want: 0,
+		},
+		"single domain": {
+			assignment: &tas.TopologyAssignment{
+				Levels: []string{"hostname"},
+				Domains: []tas.TopologyDomainAssignment{
+					{Values: []string{"node-a"}, Count: 3},
+				},
+			},
+			want: 3,
+		},
+		"multiple domains": {
+			assignment: &tas.TopologyAssignment{
+				Levels: []string{"hostname"},
+				Domains: []tas.TopologyDomainAssignment{
+					{Values: []string{"node-a"}, Count: 2},
+					{Values: []string{"node-b"}, Count: 3},
+					{Values: []string{"node-c"}, Count: 1},
+				},
+			},
+			want: 6,
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			got := tas.CountPodsInAssignment(tc.assignment)
+			if got != tc.want {
+				t.Errorf("CountPodsInAssignment() = %d, want %d", got, tc.want)
+			}
+		})
+	}
+}
+
+func TestComputeAssumedUsageFromAssignment(t *testing.T) {
+	singlePodRequests := resources.Requests{
+		corev1.ResourceCPU:    1000,
+		corev1.ResourceMemory: 1024,
+	}
+
+	cases := map[string]struct {
+		assignment *tas.TopologyAssignment
+		want       map[tas.TopologyDomainID]resources.Requests
+	}{
+		"empty assignment": {
+			assignment: &tas.TopologyAssignment{
+				Levels:  []string{"hostname"},
+				Domains: nil,
+			},
+			want: map[tas.TopologyDomainID]resources.Requests{},
+		},
+		"single domain with one pod": {
+			assignment: &tas.TopologyAssignment{
+				Levels: []string{"hostname"},
+				Domains: []tas.TopologyDomainAssignment{
+					{Values: []string{"node-a"}, Count: 1},
+				},
+			},
+			want: map[tas.TopologyDomainID]resources.Requests{
+				"node-a": {
+					corev1.ResourceCPU:    1000,
+					corev1.ResourceMemory: 1024,
+					corev1.ResourcePods:   1,
+				},
+			},
+		},
+		"multiple domains": {
+			assignment: &tas.TopologyAssignment{
+				Levels: []string{"hostname"},
+				Domains: []tas.TopologyDomainAssignment{
+					{Values: []string{"node-a"}, Count: 2},
+					{Values: []string{"node-b"}, Count: 3},
+				},
+			},
+			want: map[tas.TopologyDomainID]resources.Requests{
+				"node-a": {
+					corev1.ResourceCPU:    2000,
+					corev1.ResourceMemory: 2048,
+					corev1.ResourcePods:   2,
+				},
+				"node-b": {
+					corev1.ResourceCPU:    3000,
+					corev1.ResourceMemory: 3072,
+					corev1.ResourcePods:   3,
+				},
+			},
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			got := computeAssumedUsageFromAssignment(tc.assignment, singlePodRequests)
+			if diff := cmp.Diff(tc.want, got); diff != "" {
+				t.Errorf("computeAssumedUsageFromAssignment() mismatch (-want +got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestTruncateAssignment(t *testing.T) {
+	cases := map[string]struct {
+		assignment *tas.TopologyAssignment
+		newCount   int32
+		want       *tas.TopologyAssignment
+	}{
+		"truncate to zero": {
+			assignment: &tas.TopologyAssignment{
+				Levels: []string{"hostname"},
+				Domains: []tas.TopologyDomainAssignment{
+					{Values: []string{"node-a"}, Count: 2},
+				},
+			},
+			newCount: 0,
+			want: &tas.TopologyAssignment{
+				Levels:  []string{"hostname"},
+				Domains: nil,
+			},
+		},
+		"no truncation needed": {
+			assignment: &tas.TopologyAssignment{
+				Levels: []string{"hostname"},
+				Domains: []tas.TopologyDomainAssignment{
+					{Values: []string{"node-a"}, Count: 2},
+					{Values: []string{"node-b"}, Count: 1},
+				},
+			},
+			newCount: 3,
+			want: &tas.TopologyAssignment{
+				Levels: []string{"hostname"},
+				Domains: []tas.TopologyDomainAssignment{
+					{Values: []string{"node-a"}, Count: 2},
+					{Values: []string{"node-b"}, Count: 1},
+				},
+			},
+		},
+		"truncate to single domain": {
```

  Please add a test case to demonstrate the trunctation does not depend on lex ordering. Just in case.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-12T18:48:51Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:74** by [@mimowo](https://github.com/mimowo) — 2026-02-12T18:48:51Z
```diff
@@ -49,7 +50,30 @@ func (a *Assignment) WorkloadsTopologyRequests(wl *workload.Info, cq *schdcache.
 				continue
 			}
 			isTASImplied := isTASImplied(&podSet, cq)
-			psTASRequest, err := podSetTopologyRequest(psAssignment, wl, cq, isTASImplied, i)
+
+			// Get the previous TopologyAssignment from the old workload slice if replacing
+			var previousAssignment *kueue.TopologyAssignment
+			if features.Enabled(features.ElasticJobsViaWorkloadSlicesWithTAS) &&
+				a.replaceWorkloadSlice != nil && a.replaceWorkloadSlice.Obj.Status.Admission != nil {
+				// Only unconstrained topology is supported with elastic workload slices.
+				// Required and preferred modes are not yet supported.
+				if podSet.TopologyRequest != nil && podSet.TopologyRequest.Required != nil {
+					psAssignment.error(errors.New("required topology is not supported with ElasticJobsViaWorkloadSlices"))
+					continue
+				}
+				if podSet.TopologyRequest != nil && podSet.TopologyRequest.Preferred != nil {
+					psAssignment.error(errors.New("preferred topology is not supported with ElasticJobsViaWorkloadSlices"))
+					continue
+				}
+				for _, oldPSA := range a.replaceWorkloadSlice.Obj.Status.Admission.PodSetAssignments {
+					if oldPSA.Name == podSet.Name && oldPSA.TopologyAssignment != nil {
+						previousAssignment = oldPSA.TopologyAssignment
+						break
+					}
+				}
+			}
```

  please ,move it to a helper in `tas_elastic.go` to offload this function

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-12T18:51:50Z

- **pkg/controller/tas/node_failure_controller.go:247** by [@mimowo](https://github.com/mimowo) — 2026-02-12T18:51:50Z
```diff
@@ -242,6 +243,16 @@ func (r *nodeFailureReconciler) getWorkloadsForImmediateReplacement(ctx context.
 	return affectedWorkloads, nil
 }
 
+// listPodsForWorkload returns pods belonging to a workload.
+func (r *nodeFailureReconciler) listPodsForWorkload(ctx context.Context, wl *kueue.Workload) ([]corev1.Pod, error) {
```

  IIRC we already have a similar function for listing pods when ungating an elastic Job. I would love to commonize them. Probably a helper in a new package like `workloads` , but I'm not sure where yet, feel free to propose some good place and commonize.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-12T18:52:58Z

- **pkg/controller/tas/node_failure_controller.go:188** by [@mimowo](https://github.com/mimowo) — 2026-02-12T18:52:58Z
```diff
@@ -180,19 +179,19 @@ func (r *nodeFailureReconciler) SetupWithManager(mgr ctrl.Manager, cfg *config.C
 		Complete(core.WithLeadingManager(mgr, r, &corev1.Node{}, cfg))
 }
 
-// getWorkloadsOnNode gets all workloads that have the given node assigned in TAS topology assignment
+// getWorkloadsOnNode gets all workloads that have the given node assigned in TAS topology assignment.
 func (r *nodeFailureReconciler) getWorkloadsOnNode(ctx context.Context, nodeName string) (sets.Set[types.NamespacedName], error) {
 	var allWorkloads kueue.WorkloadList
 	if err := r.client.List(ctx, &allWorkloads); err != nil {
 		return nil, fmt.Errorf("failed to list workloads: %w", err)
 	}
-	tasWorkloadsOnNode := sets.New[types.NamespacedName]()
+	result := sets.New[types.NamespacedName]()
```

  Don't do drive by changes, they distract during review. If you think this can be improved, fair, We can merge in a separate PR.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-12T18:53:15Z

- **pkg/controller/tas/node_failure_controller.go:92** by [@mimowo](https://github.com/mimowo) — 2026-02-12T18:53:15Z
```diff
@@ -90,19 +89,19 @@ func (r *nodeFailureReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 
 	if errMsg != "" {
 		log.V(3).Info(fmt.Sprintf("%s. Marking as failed immediately", errMsg))
-		affectedWorkloads, err = r.getWorkloadsOnNode(ctx, req.Name)
+		workloads, err := r.getWorkloadsOnNode(ctx, req.Name)
```

  Also, avoid drive-by changes in the PR here.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-12T18:54:31Z

- **pkg/controller/jobframework/reconciler.go:1425** by [@mimowo](https://github.com/mimowo) — 2026-02-12T18:54:31Z
```diff
@@ -1422,12 +1422,12 @@ func getPodSetsInfoFromStatus(ctx context.Context, c client.Client, w *kueue.Wor
 		}
 		if features.Enabled(features.TopologyAwareScheduling) {
 			info.Annotations[kueue.WorkloadAnnotation] = w.Name
-		}
-		// Set workload slice name annotation on pods for elastic workloads.
-		// This enables pod lookup by annotation instead of owner reference,
-		// supporting JobSet and other workloads where pods are not immediate children.
-		if features.Enabled(features.ElasticJobsViaWorkloadSlices) {
-			info.Annotations[kueue.WorkloadSliceNameAnnotation] = workloadslicing.SliceName(w)
+			// Set workload slice name annotation on pods for elastic workloads with TAS.
```

  Hm, why is that changed? This new annotation is useful also outside of TAS - to ungate Pods for elastic RayJob for example.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-12T18:56:46Z

- **pkg/cache/scheduler/tas_elastic_workloads.go:145** by [@mimowo](https://github.com/mimowo) — 2026-02-12T18:56:47Z
```diff
@@ -0,0 +1,175 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"cmp"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// elasticWorkloadResult holds the result of elastic workload placement.
+type elasticWorkloadResult struct {
+	handled bool
+	result  map[kueue.PodSetReference]tasPodSetAssignmentResult
+}
+
+// handleElasticWorkload processes delta-only placement for elastic workloads.
+// It keeps previous pods fixed and places only new pods during scale-up,
+// or truncates the assignment during scale-down.
+func (s *TASFlavorSnapshot) handleElasticWorkload(
+	workers TASPodSetRequests,
+	leader *TASPodSetRequests,
+	assumedUsage map[utiltas.TopologyDomainID]resources.Requests,
+	opts *findTopologyAssignmentsOption,
+) elasticWorkloadResult {
+	if workers.PreviousAssignment == nil {
+		return elasticWorkloadResult{handled: false}
+	}
+
+	prevAssignment := utiltas.InternalFrom(workers.PreviousAssignment)
+
+	if isStale, staleDomain := s.IsTopologyAssignmentStale(prevAssignment); isStale {
+		s.log.V(3).Info("previous TAS assignment is stale, doing fresh placement",
+			"staleDomain", staleDomain)
+		return elasticWorkloadResult{handled: false}
+	}
+
+	previousCount := utiltas.CountPodsInAssignment(prevAssignment)
+	result := make(map[kueue.PodSetReference]tasPodSetAssignmentResult)
+
+	switch {
+	case workers.Count > previousCount:
+		return s.handleScaleUp(workers, leader, prevAssignment, previousCount, assumedUsage, opts)
+	case workers.Count < previousCount:
+		return s.handleScaleDown(workers, prevAssignment, assumedUsage)
+	default:
+		// Same count: reuse previous assignment
+		result[workers.PodSet.Name] = tasPodSetAssignmentResult{TopologyAssignment: prevAssignment}
+		addAssumedUsage(assumedUsage, prevAssignment, &workers)
+		return elasticWorkloadResult{handled: true, result: result}
+	}
+}
+
+// handleScaleUp places only delta pods while keeping previous pods fixed.
+func (s *TASFlavorSnapshot) handleScaleUp(
+	workers TASPodSetRequests,
+	leader *TASPodSetRequests,
+	prevAssignment *utiltas.TopologyAssignment,
+	previousCount int32,
+	assumedUsage map[utiltas.TopologyDomainID]resources.Requests,
+	opts *findTopologyAssignmentsOption,
+) elasticWorkloadResult {
+	result := make(map[kueue.PodSetReference]tasPodSetAssignmentResult)
+
+	deltaCount := workers.Count - previousCount
+	deltaRequest := workers
+	deltaRequest.Count = deltaCount
+	deltaRequest.PreviousAssignment = nil
+
+	// Previous pods consume capacity
+	prevAssumedUsage := computeAssumedUsageFromAssignment(prevAssignment, workers.SinglePodRequests)
+	for domainID, usage := range prevAssumedUsage {
+		if assumedUsage[domainID] == nil {
+			assumedUsage[domainID] = resources.Requests{}
+		}
+		assumedUsage[domainID].Add(usage)
+	}
+
+	deltaAssignments, reason := s.findTopologyAssignment(deltaRequest, leader, assumedUsage, opts.simulateEmpty, "")
+	if reason != "" {
+		result[workers.PodSet.Name] = tasPodSetAssignmentResult{FailureReason: reason}
+		return elasticWorkloadResult{handled: true, result: result}
+	}
+
+	deltaAssignment := deltaAssignments[workers.PodSet.Name]
+	finalAssignment := s.mergeTopologyAssignments(deltaAssignment, prevAssignment)
+	result[workers.PodSet.Name] = tasPodSetAssignmentResult{TopologyAssignment: finalAssignment}
+
+	if leader != nil {
+		result[leader.PodSet.Name] = tasPodSetAssignmentResult{TopologyAssignment: deltaAssignments[leader.PodSet.Name]}
+		addAssumedUsage(assumedUsage, deltaAssignments[leader.PodSet.Name], leader)
+	}
+
+	// Add only delta to avoid double-counting previous pods.
+	addAssumedUsage(assumedUsage, deltaAssignment, &workers)
+	return elasticWorkloadResult{handled: true, result: result}
+}
+
+// handleScaleDown truncates the previous assignment to fit fewer pods.
+func (s *TASFlavorSnapshot) handleScaleDown(
+	workers TASPodSetRequests,
+	prevAssignment *utiltas.TopologyAssignment,
+	assumedUsage map[utiltas.TopologyDomainID]resources.Requests,
+) elasticWorkloadResult {
+	result := make(map[kueue.PodSetReference]tasPodSetAssignmentResult)
+
+	truncatedAssignment := utiltas.TruncateAssignment(prevAssignment, workers.Count)
+	result[workers.PodSet.Name] = tasPodSetAssignmentResult{TopologyAssignment: truncatedAssignment}
+	addAssumedUsage(assumedUsage, truncatedAssignment, &workers)
+	return elasticWorkloadResult{handled: true, result: result}
+}
+
+// computeAssumedUsageFromAssignment calculates the usage map from an assignment.
+func computeAssumedUsageFromAssignment(ta *utiltas.TopologyAssignment, singlePodRequests resources.Requests) map[utiltas.TopologyDomainID]resources.Requests {
+	usage := make(map[utiltas.TopologyDomainID]resources.Requests)
+	for _, domain := range ta.Domains {
+		domainID := utiltas.DomainID(domain.Values)
+		domainUsage := singlePodRequests.ScaledUp(int64(domain.Count))
+		domainUsage.Add(resources.Requests{corev1.ResourcePods: int64(domain.Count)})
+		usage[domainID] = domainUsage
+	}
+	return usage
+}
+
+// mergeTopologyAssignments merges two topology assignments keeping the lexicographical order of levelValues.
+func (s *TASFlavorSnapshot) mergeTopologyAssignments(a, b *utiltas.TopologyAssignment) *utiltas.TopologyAssignment {
```

  Hm, this function is not just for elasticWorklaods, but hot swap too.  would move it to a shared file, maybe like `tas_helpers.go` for now (for the lack of a better place). Same applies to other functions in this file potentially which are used both by elasticWorkloads, and core tas.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-12T18:59:24Z

- **test/integration/singlecluster/tas/tas_test.go:4281** by [@mimowo](https://github.com/mimowo) — 2026-02-12T18:59:24Z
```diff
@@ -4198,6 +4199,294 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 			})
 		})
 	})
+
+	ginkgo.When("TAS with ElasticJobsViaWorkloadSlices", func() {
```

  Could you please also add a sanity e2e test for a Job? It could be a follow up PR, but I would like to already see a prototype of that test to play with it.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-13T00:21:09Z

- **test/integration/singlecluster/tas/tas_test.go:4281** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-13T00:21:08Z
```diff
@@ -4198,6 +4199,294 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 			})
 		})
 	})
+
+	ginkgo.When("TAS with ElasticJobsViaWorkloadSlices", func() {
```

  I added an initial e2e test under the existing tas workflow. Let me know your thoughts.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-13T00:21:39Z

- **pkg/cache/scheduler/tas_elastic_workloads.go:145** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-13T00:21:39Z
```diff
@@ -0,0 +1,175 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"cmp"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// elasticWorkloadResult holds the result of elastic workload placement.
+type elasticWorkloadResult struct {
+	handled bool
+	result  map[kueue.PodSetReference]tasPodSetAssignmentResult
+}
+
+// handleElasticWorkload processes delta-only placement for elastic workloads.
+// It keeps previous pods fixed and places only new pods during scale-up,
+// or truncates the assignment during scale-down.
+func (s *TASFlavorSnapshot) handleElasticWorkload(
+	workers TASPodSetRequests,
+	leader *TASPodSetRequests,
+	assumedUsage map[utiltas.TopologyDomainID]resources.Requests,
+	opts *findTopologyAssignmentsOption,
+) elasticWorkloadResult {
+	if workers.PreviousAssignment == nil {
+		return elasticWorkloadResult{handled: false}
+	}
+
+	prevAssignment := utiltas.InternalFrom(workers.PreviousAssignment)
+
+	if isStale, staleDomain := s.IsTopologyAssignmentStale(prevAssignment); isStale {
+		s.log.V(3).Info("previous TAS assignment is stale, doing fresh placement",
+			"staleDomain", staleDomain)
+		return elasticWorkloadResult{handled: false}
+	}
+
+	previousCount := utiltas.CountPodsInAssignment(prevAssignment)
+	result := make(map[kueue.PodSetReference]tasPodSetAssignmentResult)
+
+	switch {
+	case workers.Count > previousCount:
+		return s.handleScaleUp(workers, leader, prevAssignment, previousCount, assumedUsage, opts)
+	case workers.Count < previousCount:
+		return s.handleScaleDown(workers, prevAssignment, assumedUsage)
+	default:
+		// Same count: reuse previous assignment
+		result[workers.PodSet.Name] = tasPodSetAssignmentResult{TopologyAssignment: prevAssignment}
+		addAssumedUsage(assumedUsage, prevAssignment, &workers)
+		return elasticWorkloadResult{handled: true, result: result}
+	}
+}
+
+// handleScaleUp places only delta pods while keeping previous pods fixed.
+func (s *TASFlavorSnapshot) handleScaleUp(
+	workers TASPodSetRequests,
+	leader *TASPodSetRequests,
+	prevAssignment *utiltas.TopologyAssignment,
+	previousCount int32,
+	assumedUsage map[utiltas.TopologyDomainID]resources.Requests,
+	opts *findTopologyAssignmentsOption,
+) elasticWorkloadResult {
+	result := make(map[kueue.PodSetReference]tasPodSetAssignmentResult)
+
+	deltaCount := workers.Count - previousCount
+	deltaRequest := workers
+	deltaRequest.Count = deltaCount
+	deltaRequest.PreviousAssignment = nil
+
+	// Previous pods consume capacity
+	prevAssumedUsage := computeAssumedUsageFromAssignment(prevAssignment, workers.SinglePodRequests)
+	for domainID, usage := range prevAssumedUsage {
+		if assumedUsage[domainID] == nil {
+			assumedUsage[domainID] = resources.Requests{}
+		}
+		assumedUsage[domainID].Add(usage)
+	}
+
+	deltaAssignments, reason := s.findTopologyAssignment(deltaRequest, leader, assumedUsage, opts.simulateEmpty, "")
+	if reason != "" {
+		result[workers.PodSet.Name] = tasPodSetAssignmentResult{FailureReason: reason}
+		return elasticWorkloadResult{handled: true, result: result}
+	}
+
+	deltaAssignment := deltaAssignments[workers.PodSet.Name]
+	finalAssignment := s.mergeTopologyAssignments(deltaAssignment, prevAssignment)
+	result[workers.PodSet.Name] = tasPodSetAssignmentResult{TopologyAssignment: finalAssignment}
+
+	if leader != nil {
+		result[leader.PodSet.Name] = tasPodSetAssignmentResult{TopologyAssignment: deltaAssignments[leader.PodSet.Name]}
+		addAssumedUsage(assumedUsage, deltaAssignments[leader.PodSet.Name], leader)
+	}
+
+	// Add only delta to avoid double-counting previous pods.
+	addAssumedUsage(assumedUsage, deltaAssignment, &workers)
+	return elasticWorkloadResult{handled: true, result: result}
+}
+
+// handleScaleDown truncates the previous assignment to fit fewer pods.
+func (s *TASFlavorSnapshot) handleScaleDown(
+	workers TASPodSetRequests,
+	prevAssignment *utiltas.TopologyAssignment,
+	assumedUsage map[utiltas.TopologyDomainID]resources.Requests,
+) elasticWorkloadResult {
+	result := make(map[kueue.PodSetReference]tasPodSetAssignmentResult)
+
+	truncatedAssignment := utiltas.TruncateAssignment(prevAssignment, workers.Count)
+	result[workers.PodSet.Name] = tasPodSetAssignmentResult{TopologyAssignment: truncatedAssignment}
+	addAssumedUsage(assumedUsage, truncatedAssignment, &workers)
+	return elasticWorkloadResult{handled: true, result: result}
+}
+
+// computeAssumedUsageFromAssignment calculates the usage map from an assignment.
+func computeAssumedUsageFromAssignment(ta *utiltas.TopologyAssignment, singlePodRequests resources.Requests) map[utiltas.TopologyDomainID]resources.Requests {
+	usage := make(map[utiltas.TopologyDomainID]resources.Requests)
+	for _, domain := range ta.Domains {
+		domainID := utiltas.DomainID(domain.Values)
+		domainUsage := singlePodRequests.ScaledUp(int64(domain.Count))
+		domainUsage.Add(resources.Requests{corev1.ResourcePods: int64(domain.Count)})
+		usage[domainID] = domainUsage
+	}
+	return usage
+}
+
+// mergeTopologyAssignments merges two topology assignments keeping the lexicographical order of levelValues.
+func (s *TASFlavorSnapshot) mergeTopologyAssignments(a, b *utiltas.TopologyAssignment) *utiltas.TopologyAssignment {
```

  Made the necessary changes.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-13T00:22:07Z

- **pkg/controller/tas/node_failure_controller.go:188** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-13T00:22:07Z
```diff
@@ -180,19 +179,19 @@ func (r *nodeFailureReconciler) SetupWithManager(mgr ctrl.Manager, cfg *config.C
 		Complete(core.WithLeadingManager(mgr, r, &corev1.Node{}, cfg))
 }
 
-// getWorkloadsOnNode gets all workloads that have the given node assigned in TAS topology assignment
+// getWorkloadsOnNode gets all workloads that have the given node assigned in TAS topology assignment.
 func (r *nodeFailureReconciler) getWorkloadsOnNode(ctx context.Context, nodeName string) (sets.Set[types.NamespacedName], error) {
 	var allWorkloads kueue.WorkloadList
 	if err := r.client.List(ctx, &allWorkloads); err != nil {
 		return nil, fmt.Errorf("failed to list workloads: %w", err)
 	}
-	tasWorkloadsOnNode := sets.New[types.NamespacedName]()
+	result := sets.New[types.NamespacedName]()
```

  Dropped that commit from this commit.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-13T00:22:17Z

- **pkg/controller/tas/node_failure_controller.go:92** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-13T00:22:17Z
```diff
@@ -90,19 +89,19 @@ func (r *nodeFailureReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 
 	if errMsg != "" {
 		log.V(3).Info(fmt.Sprintf("%s. Marking as failed immediately", errMsg))
-		affectedWorkloads, err = r.getWorkloadsOnNode(ctx, req.Name)
+		workloads, err := r.getWorkloadsOnNode(ctx, req.Name)
```

  same https://github.com/kubernetes-sigs/kueue/pull/8580#discussion_r2801668897

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-13T00:22:36Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:74** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-13T00:22:36Z
```diff
@@ -49,7 +50,30 @@ func (a *Assignment) WorkloadsTopologyRequests(wl *workload.Info, cq *schdcache.
 				continue
 			}
 			isTASImplied := isTASImplied(&podSet, cq)
-			psTASRequest, err := podSetTopologyRequest(psAssignment, wl, cq, isTASImplied, i)
+
+			// Get the previous TopologyAssignment from the old workload slice if replacing
+			var previousAssignment *kueue.TopologyAssignment
+			if features.Enabled(features.ElasticJobsViaWorkloadSlicesWithTAS) &&
+				a.replaceWorkloadSlice != nil && a.replaceWorkloadSlice.Obj.Status.Admission != nil {
+				// Only unconstrained topology is supported with elastic workload slices.
+				// Required and preferred modes are not yet supported.
+				if podSet.TopologyRequest != nil && podSet.TopologyRequest.Required != nil {
+					psAssignment.error(errors.New("required topology is not supported with ElasticJobsViaWorkloadSlices"))
+					continue
+				}
+				if podSet.TopologyRequest != nil && podSet.TopologyRequest.Preferred != nil {
+					psAssignment.error(errors.New("preferred topology is not supported with ElasticJobsViaWorkloadSlices"))
+					continue
+				}
+				for _, oldPSA := range a.replaceWorkloadSlice.Obj.Status.Admission.PodSetAssignments {
+					if oldPSA.Name == podSet.Name && oldPSA.TopologyAssignment != nil {
+						previousAssignment = oldPSA.TopologyAssignment
+						break
+					}
+				}
+			}
```

  Fixed

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-13T00:23:31Z

- **pkg/controller/tas/node_failure_controller.go:247** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-13T00:23:30Z
```diff
@@ -242,6 +243,16 @@ func (r *nodeFailureReconciler) getWorkloadsForImmediateReplacement(ctx context.
 	return affectedWorkloads, nil
 }
 
+// listPodsForWorkload returns pods belonging to a workload.
+func (r *nodeFailureReconciler) listPodsForWorkload(ctx context.Context, wl *kueue.Workload) ([]corev1.Pod, error) {
```

  I made an attempt to find the commonality. Let me know your thoughts.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-13T00:37:26Z

- **pkg/controller/jobframework/reconciler.go:1425** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-13T00:37:26Z
```diff
@@ -1422,12 +1422,12 @@ func getPodSetsInfoFromStatus(ctx context.Context, c client.Client, w *kueue.Wor
 		}
 		if features.Enabled(features.TopologyAwareScheduling) {
 			info.Annotations[kueue.WorkloadAnnotation] = w.Name
-		}
-		// Set workload slice name annotation on pods for elastic workloads.
-		// This enables pod lookup by annotation instead of owner reference,
-		// supporting JobSet and other workloads where pods are not immediate children.
-		if features.Enabled(features.ElasticJobsViaWorkloadSlices) {
-			info.Annotations[kueue.WorkloadSliceNameAnnotation] = workloadslicing.SliceName(w)
+			// Set workload slice name annotation on pods for elastic workloads with TAS.
```

  ah, good catch! fixed it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-16T16:04:24Z

- **pkg/controller/tas/indexer/indexer.go:101** by [@mimowo](https://github.com/mimowo) — 2026-02-16T16:04:24Z
```diff
@@ -94,6 +96,14 @@ func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
 		return fmt.Errorf("setting index pod workload: %w", err)
 	}
 
+	// Register workload slice name index only if ElasticJobsViaWorkloadSlices is disabled;
+	// otherwise the core indexer registers it.
+	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) {
```

  Would it be simpler if the core indexer registered if ElasticJobsViaWorkloadSlices enabled OR TopologyAwareScheduling enabled? Making this exclusion is conceptually hard I think.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-16T16:07:08Z

- **pkg/controller/tas/topology_ungater.go:312** by [@mimowo](https://github.com/mimowo) — 2026-02-16T16:07:08Z
```diff
@@ -300,23 +302,22 @@ func (r *topologyUngater) Generic(event.TypedGenericEvent[*kueue.Workload]) bool
 	return false
 }
 
-func (r *topologyUngater) podsForPodSet(ctx context.Context, ns, wlName string, psName kueue.PodSetReference) ([]*corev1.Pod, error) {
-	var pods corev1.PodList
-	if err := r.client.List(ctx, &pods, client.InNamespace(ns), client.MatchingLabels{
-		constants.PodSetLabel: string(psName),
-	}, client.MatchingFields{
-		indexer.WorkloadNameKey: wlName,
-	}); err != nil {
+func (r *topologyUngater) podsForPodSet(ctx context.Context, ns, workloadSliceName string, psName kueue.PodSetReference) ([]*corev1.Pod, error) {
+	pods, err := ListPodsForWorkloadSlice(ctx, r.client, ns, workloadSliceName)
+	if err != nil {
 		return nil, err
 	}
-	result := make([]*corev1.Pod, 0, len(pods.Items))
-	for i := range pods.Items {
-		if utilpod.IsTerminated(&pods.Items[i]) {
+	result := make([]*corev1.Pod, 0, len(pods))
+	for _, pod := range pods {
+		if pod.Labels[constants.PodSetLabel] != string(psName) {
```

  This might be a little bit of performance hit, could we instead pass a custom selector into `ListPodsForWorkloadSlice`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-16T16:09:10Z

- **test/e2e/singlecluster/tas_test.go:650** by [@mimowo](https://github.com/mimowo) — 2026-02-16T16:09:10Z
```diff
@@ -556,4 +556,115 @@ var _ = ginkgo.Describe("TopologyAwareScheduling", ginkgo.Label("area:singleclus
 			})
 		})
 	})
+
+	ginkgo.When("Scaling a Job requesting TAS via workload slices", func() {
+		var (
+			topology     *kueue.Topology
+			onDemandRF   *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			topology = utiltestingapi.MakeDefaultOneLevelTopology("hostname-" + ns.Name)
+			util.MustCreate(ctx, k8sClient, topology)
+
+			onDemandRF = utiltestingapi.MakeResourceFlavor("on-demand-"+ns.Name).
+				NodeLabel("instance-type", "on-demand").TopologyName(topology.Name).Obj()
+			util.MustCreate(ctx, k8sClient, onDemandRF)
+			clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue-" + ns.Name).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(onDemandRF.Name).
+						Resource(corev1.ResourceCPU, "4").
+						Resource(corev1.ResourceMemory, "4Gi").
+						Obj(),
+				).
+				Obj()
+			util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("main", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, localQueue)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			// Force remove workloads to be sure that cluster queue can be removed.
+			gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, onDemandRF, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+		})
+
+		ginkgo.It("should preserve topology assignment during scale-up", func() {
+			sampleJob := testingjob.MakeJob("test-job", ns.Name).
+				Queue(kueue.LocalQueueName(localQueue.Name)).
+				Parallelism(1).
+				Completions(10).
+				RequestAndLimit(corev1.ResourceCPU, "100m").
+				RequestAndLimit(corev1.ResourceMemory, "20Mi").
+				PodAnnotation(kueue.PodSetUnconstrainedTopologyAnnotation, "true").
+				Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+				Obj()
+			util.MustCreate(ctx, k8sClient, sampleJob)
+
+			createdWorkload := &kueue.Workload{}
+			wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(sampleJob.Name, sampleJob.UID), Namespace: ns.Name}
+
+			var originalNodeName string
+			ginkgo.By(fmt.Sprintf("await for admission of workload %q and record topology", wlLookupKey), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
+					g.Expect(createdWorkload.Status.Admission).ShouldNot(gomega.BeNil())
+					g.Expect(createdWorkload.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
+					g.Expect(createdWorkload.Status.Admission.PodSetAssignments[0].TopologyAssignment).ShouldNot(gomega.BeNil())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+
+				ta := tas.InternalFrom(createdWorkload.Status.Admission.PodSetAssignments[0].TopologyAssignment)
+				gomega.Expect(ta.Domains).ShouldNot(gomega.BeEmpty())
+				originalNodeName = ta.Domains[0].Values[0]
+			})
+
+			ginkgo.By("scale up the job parallelism", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(sampleJob), sampleJob)).To(gomega.Succeed())
+					sampleJob.Spec.Parallelism = ptr.To[int32](2)
+					g.Expect(k8sClient.Update(ctx, sampleJob)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("verify scaled workload preserves original topology assignment", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					var workloads kueue.WorkloadList
+					g.Expect(k8sClient.List(ctx, &workloads, client.InNamespace(ns.Name))).To(gomega.Succeed())
+
+					var scaledWorkload *kueue.Workload
+					for i := range workloads.Items {
+						wl := &workloads.Items[i]
+						if wl.Status.Admission != nil &&
+							len(wl.Spec.PodSets) > 0 &&
+							wl.Spec.PodSets[0].Count == 2 {
+							scaledWorkload = wl
+							break
+						}
+					}
```

  This is very low level (and with magic numbers), can we somehow abstract it our.?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-16T16:09:39Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:3699** by [@mimowo](https://github.com/mimowo) — 2026-02-16T16:09:39Z
```diff
@@ -3558,6 +3558,155 @@ var _ = ginkgo.Describe("Job controller with TopologyAwareScheduling", ginkgo.Or
 	})
 })
 
+var _ = ginkgo.Describe("Job controller with TAS and ElasticJobsViaWorkloadSlices", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	const (
+		nodeGroupLabel = "node-group"
+		tasBlockLabel  = "cloud.com/topology-block"
+	)
+
+	var (
+		ns           *corev1.Namespace
+		nodes        []corev1.Node
+		topology     *kueue.Topology
+		tasFlavor    *kueue.ResourceFlavor
+		clusterQueue *kueue.ClusterQueue
+		localQueue   *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		gomega.Expect(utilfeature.DefaultMutableFeatureGate.SetFromMap(map[string]bool{
+			string(features.ElasticJobsViaWorkloadSlices): true,
+		})).Should(gomega.Succeed())
+		fwk.StartManager(ctx, cfg, managerAndControllersSetup(true, true, nil))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "tas-elastic-")
+
+		nodes = []corev1.Node{
+			*testingnode.MakeNode("b1").
+				Label(nodeGroupLabel, "tas").
+				Label(tasBlockLabel, "b1").
+				StatusAllocatable(corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+					corev1.ResourcePods:   resource.MustParse("10"),
+				}).
+				Ready().
+				Obj(),
+			*testingnode.MakeNode("b2").
+				Label(nodeGroupLabel, "tas").
+				Label(tasBlockLabel, "b2").
+				StatusAllocatable(corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+					corev1.ResourcePods:   resource.MustParse("10"),
+				}).
+				Ready().
+				Obj(),
+		}
+		for i := range nodes {
+			util.MustCreate(ctx, k8sClient, &nodes[i])
+		}
+
+		topology = utiltestingapi.MakeTopology("default").Levels(tasBlockLabel).Obj()
+		util.MustCreate(ctx, k8sClient, topology)
+
+		tasFlavor = utiltestingapi.MakeResourceFlavor("tas-flavor").
+			NodeLabel(nodeGroupLabel, "tas").
+			TopologyName(topology.Name).Obj()
+		util.MustCreate(ctx, k8sClient, tasFlavor)
+
+		clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas(tasFlavor.Name).
+					Resource(corev1.ResourceCPU, "5").
+					Resource(corev1.ResourceMemory, "5Gi").
+					Obj(),
+			).Obj()
+		util.MustCreate(ctx, k8sClient, clusterQueue)
+
+		localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+		util.MustCreate(ctx, k8sClient, localQueue)
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+		for i := range nodes {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, &nodes[i], true)
+		}
+	})
+
+	ginkgo.It("should scale up an elastic job with TAS unconstrained topology", func() {
+		job := testingjob.MakeJob("elastic-job", ns.Name).
+			Queue(kueue.LocalQueueName(localQueue.Name)).
+			PodAnnotation(kueue.PodSetUnconstrainedTopologyAnnotation, "true").
+			SetAnnotation(workloadslicing.EnabledAnnotationKey, workloadslicing.EnabledAnnotationValue).
+			Parallelism(1).
+			Completions(10).
+			Request(corev1.ResourceCPU, "100m").
+			Obj()
+
+		ginkgo.By("creating an elastic job with unconstrained topology", func() {
+			util.MustCreate(ctx, k8sClient, job)
+		})
+
+		var originalWorkload *kueue.Workload
+		ginkgo.By("verify the workload is created and admitted with topology assignment", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				workloads := &kueue.WorkloadList{}
+				g.Expect(k8sClient.List(ctx, workloads, client.InNamespace(ns.Name))).Should(gomega.Succeed())
+				g.Expect(workloads.Items).Should(gomega.HaveLen(1))
+				originalWorkload = &workloads.Items[0]
+				g.Expect(originalWorkload.Spec.PodSets).Should(gomega.HaveLen(1))
+				g.Expect(originalWorkload.Spec.PodSets[0].TopologyRequest).ShouldNot(gomega.BeNil())
+				g.Expect(originalWorkload.Spec.PodSets[0].TopologyRequest.Unconstrained).Should(gomega.Equal(ptr.To(true)))
+				g.Expect(originalWorkload.Status.Admission).ShouldNot(gomega.BeNil())
+				g.Expect(originalWorkload.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
+				g.Expect(originalWorkload.Status.Admission.PodSetAssignments[0].TopologyAssignment).ShouldNot(gomega.BeNil())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		originalWorkloadName := originalWorkload.Name
+
+		ginkgo.By("scale up the job by increasing parallelism", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(job), job)).Should(gomega.Succeed())
+				job.Spec.Parallelism = ptr.To(int32(2))
+				g.Expect(k8sClient.Update(ctx, job)).Should(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("verify a new workload slice is created and admitted with topology assignment", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				workloadList := &kueue.WorkloadList{}
+				g.Expect(k8sClient.List(ctx, workloadList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+
+				var newWorkload *kueue.Workload
+				for i := range workloadList.Items {
+					wl := &workloadList.Items[i]
+					if wl.Name != originalWorkloadName && !apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
+						newWorkload = wl
+						break
+					}
+				}
```

  This is too low level, please abstract it out somehow with helpers.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-16T16:12:43Z

- **pkg/cache/scheduler/tas_elastic_workloads.go:130** by [@mimowo](https://github.com/mimowo) — 2026-02-16T16:12:43Z
```diff
@@ -0,0 +1,139 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// elasticWorkloadResult holds the result of elastic workload placement.
+type elasticWorkloadResult struct {
+	handled bool
+	result  map[kueue.PodSetReference]tasPodSetAssignmentResult
+}
+
+// handleElasticWorkload processes delta-only placement for elastic workloads.
+// It keeps previous pods fixed and places only new pods during scale-up,
+// or truncates the assignment during scale-down.
+func (s *TASFlavorSnapshot) handleElasticWorkload(
+	workers TASPodSetRequests,
+	leader *TASPodSetRequests,
+	assumedUsage map[utiltas.TopologyDomainID]resources.Requests,
+	opts *findTopologyAssignmentsOption,
+) elasticWorkloadResult {
+	if workers.PreviousAssignment == nil {
+		return elasticWorkloadResult{handled: false}
+	}
+
+	prevAssignment := utiltas.InternalFrom(workers.PreviousAssignment)
+
+	if isStale, staleDomain := s.IsTopologyAssignmentStale(prevAssignment); isStale {
+		s.log.V(3).Info("previous TAS assignment is stale, doing fresh placement",
+			"staleDomain", staleDomain)
+		return elasticWorkloadResult{handled: false}
+	}
+
+	previousCount := utiltas.CountPodsInAssignment(prevAssignment)
+	result := make(map[kueue.PodSetReference]tasPodSetAssignmentResult)
+
+	switch {
+	case workers.Count > previousCount:
+		return s.handleScaleUp(workers, leader, prevAssignment, previousCount, assumedUsage, opts)
+	case workers.Count < previousCount:
+		return s.handleScaleDown(workers, prevAssignment, assumedUsage)
+	default:
+		// Same count: reuse previous assignment
+		result[workers.PodSet.Name] = tasPodSetAssignmentResult{TopologyAssignment: prevAssignment}
+		addAssumedUsage(assumedUsage, prevAssignment, &workers)
+		return elasticWorkloadResult{handled: true, result: result}
+	}
+}
+
+// handleScaleUp places only delta pods while keeping previous pods fixed.
+func (s *TASFlavorSnapshot) handleScaleUp(
+	workers TASPodSetRequests,
+	leader *TASPodSetRequests,
+	prevAssignment *utiltas.TopologyAssignment,
+	previousCount int32,
+	assumedUsage map[utiltas.TopologyDomainID]resources.Requests,
+	opts *findTopologyAssignmentsOption,
+) elasticWorkloadResult {
+	result := make(map[kueue.PodSetReference]tasPodSetAssignmentResult)
+
+	deltaCount := workers.Count - previousCount
+	deltaRequest := workers
+	deltaRequest.Count = deltaCount
+	deltaRequest.PreviousAssignment = nil
+
+	// Previous pods consume capacity
+	prevAssumedUsage := computeAssumedUsageFromAssignment(prevAssignment, workers.SinglePodRequests)
+	for domainID, usage := range prevAssumedUsage {
+		if assumedUsage[domainID] == nil {
+			assumedUsage[domainID] = resources.Requests{}
+		}
+		assumedUsage[domainID].Add(usage)
+	}
+
+	deltaAssignments, reason := s.findTopologyAssignment(deltaRequest, leader, assumedUsage, opts.simulateEmpty, "")
+	if reason != "" {
+		result[workers.PodSet.Name] = tasPodSetAssignmentResult{FailureReason: reason}
+		return elasticWorkloadResult{handled: true, result: result}
+	}
+
+	deltaAssignment := deltaAssignments[workers.PodSet.Name]
+	finalAssignment := s.mergeTopologyAssignments(deltaAssignment, prevAssignment)
+	result[workers.PodSet.Name] = tasPodSetAssignmentResult{TopologyAssignment: finalAssignment}
+
+	if leader != nil {
+		result[leader.PodSet.Name] = tasPodSetAssignmentResult{TopologyAssignment: deltaAssignments[leader.PodSet.Name]}
+		addAssumedUsage(assumedUsage, deltaAssignments[leader.PodSet.Name], leader)
+	}
+
+	// Add only delta to avoid double-counting previous pods.
+	addAssumedUsage(assumedUsage, deltaAssignment, &workers)
+	return elasticWorkloadResult{handled: true, result: result}
+}
+
+// handleScaleDown truncates the previous assignment to fit fewer pods.
+func (s *TASFlavorSnapshot) handleScaleDown(
+	workers TASPodSetRequests,
+	prevAssignment *utiltas.TopologyAssignment,
+	assumedUsage map[utiltas.TopologyDomainID]resources.Requests,
+) elasticWorkloadResult {
+	result := make(map[kueue.PodSetReference]tasPodSetAssignmentResult)
+
+	truncatedAssignment := utiltas.TruncateAssignment(prevAssignment, workers.Count)
+	result[workers.PodSet.Name] = tasPodSetAssignmentResult{TopologyAssignment: truncatedAssignment}
+	addAssumedUsage(assumedUsage, truncatedAssignment, &workers)
+	return elasticWorkloadResult{handled: true, result: result}
+}
+
+// computeAssumedUsageFromAssignment calculates the usage map from an assignment.
+func computeAssumedUsageFromAssignment(ta *utiltas.TopologyAssignment, singlePodRequests resources.Requests) map[utiltas.TopologyDomainID]resources.Requests {
```

  ```suggestion
  func computeUsagePerDomain(ta *utiltas.TopologyAssignment, singlePodRequests resources.Requests) map[utiltas.TopologyDomainID]resources.Requests {
  ```
  It depends on the caller if this is "assumed usage" or not. Then we can move the function to tas_assignment helpers.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-16T16:14:43Z

- **pkg/cache/scheduler/tas_elastic_workloads.go:30** by [@mimowo](https://github.com/mimowo) — 2026-02-16T16:14:43Z
```diff
@@ -0,0 +1,139 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// elasticWorkloadResult holds the result of elastic workload placement.
+type elasticWorkloadResult struct {
+	handled bool
+	result  map[kueue.PodSetReference]tasPodSetAssignmentResult
```

  Please revisit the naming of the fields so that it is more self-explanatory. For example sub-field "result" in the type "Result" feels redundant - not extra useful information, Also "handled" is returned by handleElasticWorkload, but it is not clear what happened. Please also add comments to the fields.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-16T22:47:51Z

- **pkg/cache/scheduler/tas_elastic_workloads.go:30** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-16T22:47:50Z
```diff
@@ -0,0 +1,139 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// elasticWorkloadResult holds the result of elastic workload placement.
+type elasticWorkloadResult struct {
+	handled bool
+	result  map[kueue.PodSetReference]tasPodSetAssignmentResult
```

  Good call, I have pushed the latest changes. Let me know your thoughts.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-16T22:48:05Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:3699** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-16T22:48:05Z
```diff
@@ -3558,6 +3558,155 @@ var _ = ginkgo.Describe("Job controller with TopologyAwareScheduling", ginkgo.Or
 	})
 })
 
+var _ = ginkgo.Describe("Job controller with TAS and ElasticJobsViaWorkloadSlices", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	const (
+		nodeGroupLabel = "node-group"
+		tasBlockLabel  = "cloud.com/topology-block"
+	)
+
+	var (
+		ns           *corev1.Namespace
+		nodes        []corev1.Node
+		topology     *kueue.Topology
+		tasFlavor    *kueue.ResourceFlavor
+		clusterQueue *kueue.ClusterQueue
+		localQueue   *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		gomega.Expect(utilfeature.DefaultMutableFeatureGate.SetFromMap(map[string]bool{
+			string(features.ElasticJobsViaWorkloadSlices): true,
+		})).Should(gomega.Succeed())
+		fwk.StartManager(ctx, cfg, managerAndControllersSetup(true, true, nil))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "tas-elastic-")
+
+		nodes = []corev1.Node{
+			*testingnode.MakeNode("b1").
+				Label(nodeGroupLabel, "tas").
+				Label(tasBlockLabel, "b1").
+				StatusAllocatable(corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+					corev1.ResourcePods:   resource.MustParse("10"),
+				}).
+				Ready().
+				Obj(),
+			*testingnode.MakeNode("b2").
+				Label(nodeGroupLabel, "tas").
+				Label(tasBlockLabel, "b2").
+				StatusAllocatable(corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+					corev1.ResourcePods:   resource.MustParse("10"),
+				}).
+				Ready().
+				Obj(),
+		}
+		for i := range nodes {
+			util.MustCreate(ctx, k8sClient, &nodes[i])
+		}
+
+		topology = utiltestingapi.MakeTopology("default").Levels(tasBlockLabel).Obj()
+		util.MustCreate(ctx, k8sClient, topology)
+
+		tasFlavor = utiltestingapi.MakeResourceFlavor("tas-flavor").
+			NodeLabel(nodeGroupLabel, "tas").
+			TopologyName(topology.Name).Obj()
+		util.MustCreate(ctx, k8sClient, tasFlavor)
+
+		clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas(tasFlavor.Name).
+					Resource(corev1.ResourceCPU, "5").
+					Resource(corev1.ResourceMemory, "5Gi").
+					Obj(),
+			).Obj()
+		util.MustCreate(ctx, k8sClient, clusterQueue)
+
+		localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+		util.MustCreate(ctx, k8sClient, localQueue)
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+		for i := range nodes {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, &nodes[i], true)
+		}
+	})
+
+	ginkgo.It("should scale up an elastic job with TAS unconstrained topology", func() {
+		job := testingjob.MakeJob("elastic-job", ns.Name).
+			Queue(kueue.LocalQueueName(localQueue.Name)).
+			PodAnnotation(kueue.PodSetUnconstrainedTopologyAnnotation, "true").
+			SetAnnotation(workloadslicing.EnabledAnnotationKey, workloadslicing.EnabledAnnotationValue).
+			Parallelism(1).
+			Completions(10).
+			Request(corev1.ResourceCPU, "100m").
+			Obj()
+
+		ginkgo.By("creating an elastic job with unconstrained topology", func() {
+			util.MustCreate(ctx, k8sClient, job)
+		})
+
+		var originalWorkload *kueue.Workload
+		ginkgo.By("verify the workload is created and admitted with topology assignment", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				workloads := &kueue.WorkloadList{}
+				g.Expect(k8sClient.List(ctx, workloads, client.InNamespace(ns.Name))).Should(gomega.Succeed())
+				g.Expect(workloads.Items).Should(gomega.HaveLen(1))
+				originalWorkload = &workloads.Items[0]
+				g.Expect(originalWorkload.Spec.PodSets).Should(gomega.HaveLen(1))
+				g.Expect(originalWorkload.Spec.PodSets[0].TopologyRequest).ShouldNot(gomega.BeNil())
+				g.Expect(originalWorkload.Spec.PodSets[0].TopologyRequest.Unconstrained).Should(gomega.Equal(ptr.To(true)))
+				g.Expect(originalWorkload.Status.Admission).ShouldNot(gomega.BeNil())
+				g.Expect(originalWorkload.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
+				g.Expect(originalWorkload.Status.Admission.PodSetAssignments[0].TopologyAssignment).ShouldNot(gomega.BeNil())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		originalWorkloadName := originalWorkload.Name
+
+		ginkgo.By("scale up the job by increasing parallelism", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(job), job)).Should(gomega.Succeed())
+				job.Spec.Parallelism = ptr.To(int32(2))
+				g.Expect(k8sClient.Update(ctx, job)).Should(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("verify a new workload slice is created and admitted with topology assignment", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				workloadList := &kueue.WorkloadList{}
+				g.Expect(k8sClient.List(ctx, workloadList, client.InNamespace(ns.Name))).To(gomega.Succeed())
+
+				var newWorkload *kueue.Workload
+				for i := range workloadList.Items {
+					wl := &workloadList.Items[i]
+					if wl.Name != originalWorkloadName && !apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
+						newWorkload = wl
+						break
+					}
+				}
```

  Done!

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-16T22:48:12Z

- **test/e2e/singlecluster/tas_test.go:650** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-16T22:48:12Z
```diff
@@ -556,4 +556,115 @@ var _ = ginkgo.Describe("TopologyAwareScheduling", ginkgo.Label("area:singleclus
 			})
 		})
 	})
+
+	ginkgo.When("Scaling a Job requesting TAS via workload slices", func() {
+		var (
+			topology     *kueue.Topology
+			onDemandRF   *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			topology = utiltestingapi.MakeDefaultOneLevelTopology("hostname-" + ns.Name)
+			util.MustCreate(ctx, k8sClient, topology)
+
+			onDemandRF = utiltestingapi.MakeResourceFlavor("on-demand-"+ns.Name).
+				NodeLabel("instance-type", "on-demand").TopologyName(topology.Name).Obj()
+			util.MustCreate(ctx, k8sClient, onDemandRF)
+			clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue-" + ns.Name).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(onDemandRF.Name).
+						Resource(corev1.ResourceCPU, "4").
+						Resource(corev1.ResourceMemory, "4Gi").
+						Obj(),
+				).
+				Obj()
+			util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("main", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, localQueue)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			// Force remove workloads to be sure that cluster queue can be removed.
+			gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, onDemandRF, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+		})
+
+		ginkgo.It("should preserve topology assignment during scale-up", func() {
+			sampleJob := testingjob.MakeJob("test-job", ns.Name).
+				Queue(kueue.LocalQueueName(localQueue.Name)).
+				Parallelism(1).
+				Completions(10).
+				RequestAndLimit(corev1.ResourceCPU, "100m").
+				RequestAndLimit(corev1.ResourceMemory, "20Mi").
+				PodAnnotation(kueue.PodSetUnconstrainedTopologyAnnotation, "true").
+				Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+				Obj()
+			util.MustCreate(ctx, k8sClient, sampleJob)
+
+			createdWorkload := &kueue.Workload{}
+			wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(sampleJob.Name, sampleJob.UID), Namespace: ns.Name}
+
+			var originalNodeName string
+			ginkgo.By(fmt.Sprintf("await for admission of workload %q and record topology", wlLookupKey), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
+					g.Expect(createdWorkload.Status.Admission).ShouldNot(gomega.BeNil())
+					g.Expect(createdWorkload.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
+					g.Expect(createdWorkload.Status.Admission.PodSetAssignments[0].TopologyAssignment).ShouldNot(gomega.BeNil())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+
+				ta := tas.InternalFrom(createdWorkload.Status.Admission.PodSetAssignments[0].TopologyAssignment)
+				gomega.Expect(ta.Domains).ShouldNot(gomega.BeEmpty())
+				originalNodeName = ta.Domains[0].Values[0]
+			})
+
+			ginkgo.By("scale up the job parallelism", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(sampleJob), sampleJob)).To(gomega.Succeed())
+					sampleJob.Spec.Parallelism = ptr.To[int32](2)
+					g.Expect(k8sClient.Update(ctx, sampleJob)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("verify scaled workload preserves original topology assignment", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					var workloads kueue.WorkloadList
+					g.Expect(k8sClient.List(ctx, &workloads, client.InNamespace(ns.Name))).To(gomega.Succeed())
+
+					var scaledWorkload *kueue.Workload
+					for i := range workloads.Items {
+						wl := &workloads.Items[i]
+						if wl.Status.Admission != nil &&
+							len(wl.Spec.PodSets) > 0 &&
+							wl.Spec.PodSets[0].Count == 2 {
+							scaledWorkload = wl
+							break
+						}
+					}
```

  Done!

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-16T22:48:38Z

- **pkg/controller/tas/indexer/indexer.go:101** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-16T22:48:38Z
```diff
@@ -94,6 +96,14 @@ func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
 		return fmt.Errorf("setting index pod workload: %w", err)
 	}
 
+	// Register workload slice name index only if ElasticJobsViaWorkloadSlices is disabled;
+	// otherwise the core indexer registers it.
+	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) {
```

  Yeah, added that check.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-16T22:50:57Z

- **pkg/controller/tas/topology_ungater.go:312** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-16T22:50:57Z
```diff
@@ -300,23 +302,22 @@ func (r *topologyUngater) Generic(event.TypedGenericEvent[*kueue.Workload]) bool
 	return false
 }
 
-func (r *topologyUngater) podsForPodSet(ctx context.Context, ns, wlName string, psName kueue.PodSetReference) ([]*corev1.Pod, error) {
-	var pods corev1.PodList
-	if err := r.client.List(ctx, &pods, client.InNamespace(ns), client.MatchingLabels{
-		constants.PodSetLabel: string(psName),
-	}, client.MatchingFields{
-		indexer.WorkloadNameKey: wlName,
-	}); err != nil {
+func (r *topologyUngater) podsForPodSet(ctx context.Context, ns, workloadSliceName string, psName kueue.PodSetReference) ([]*corev1.Pod, error) {
+	pods, err := ListPodsForWorkloadSlice(ctx, r.client, ns, workloadSliceName)
+	if err != nil {
 		return nil, err
 	}
-	result := make([]*corev1.Pod, 0, len(pods.Items))
-	for i := range pods.Items {
-		if utilpod.IsTerminated(&pods.Items[i]) {
+	result := make([]*corev1.Pod, 0, len(pods))
+	for _, pod := range pods {
+		if pod.Labels[constants.PodSetLabel] != string(psName) {
```

  Addressed that in the latest push
