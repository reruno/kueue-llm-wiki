# PR #8584: Automated cherry pick of #8480: add priority class to wl none -> some

**Summary**: Automated cherry pick of #8480: add priority class to wl none -> some

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8584

**Last updated**: 2026-03-22T08:26:42Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@andrewseif](https://github.com/andrewseif)
- **Branch**: `automated-cherry-pick-of-#8480-upstream-release-0.15` → `release-0.15`
- **Created**: 2026-01-14T12:03:43Z
- **Updated**: 2026-03-22T08:26:42Z
- **Merged**: 2026-01-14T16:51:37Z
- **Closed**: 2026-01-14T16:51:37Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `approved`, `size/M`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92)
- **Changed files**: 6   **+88 / -4**

## Description

Cherry pick of #8480 on release-0.15.

#8480: add priority class to wl none -> some

For details on the cherry pick process, see the [cherry pick requests](https://git.k8s.io/community/contributors/devel/sig-release/cherry-picks.md) page.

```release-note
Scheduling: fix the bug that setting (none -> some) a workload priority class label (kueue.x-k8s.io/priority-class) was ignored.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-01-14T12:03:49Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 3745608b874c0681cae6db0be10a3ec43f174043 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6967bed3905ca30008e315cf |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-14T12:03:53Z

Hi @andrewseif. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-14T12:13:30Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-14T12:13:40Z

/kind bug

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-14T12:14:21Z

cc @mbobrovskyi ptal

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-14T15:18:15Z

@andrewseif could you please rebase?

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-14T15:42:33Z

/kind bug

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-14T16:08:41Z

Thank you so much! And sorry for this mess!

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-14T16:08:51Z

LGTM label has been added.  <details>Git tree hash: ffd37e00ed3f12984f1e6891bf2fa135fa68a8da</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-14T16:30:36Z

@mimowo I think we ready to merge.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-14T16:50:45Z

/approve
Thank you folks 👍

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-14T16:50:56Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8584#" title="Author self-approved">andrewseif</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8584#issuecomment-3750517851" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/release-0.15/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-14T12:15:32Z

- **pkg/workload/workload.go:1420** by [@mimowo](https://github.com/mimowo) — 2026-01-14T12:15:32Z
```diff
@@ -1417,6 +1417,12 @@ func IsWorkloadPriorityClass(wl *kueue.Workload) bool {
 		wl.Spec.PriorityClassRef.Group == kueue.WorkloadPriorityClassGroup
 }
 
+func IsPodPriorityClass(wl *kueue.Workload) bool {
```

  I think we should also add this test case: https://github.com/kubernetes-sigs/kueue/pull/8574#discussion_r2689750504 in workload_test.go
  
  It is not present when doing the cherrypick because the order of cherrypicking was different

### Review by [@andrewseif](https://github.com/andrewseif) (commented) — 2026-01-14T12:34:51Z

- **pkg/workload/workload.go:1420** by [@andrewseif](https://github.com/andrewseif) — 2026-01-14T12:34:51Z
```diff
@@ -1417,6 +1417,12 @@ func IsWorkloadPriorityClass(wl *kueue.Workload) bool {
 		wl.Spec.PriorityClassRef.Group == kueue.WorkloadPriorityClassGroup
 }
 
+func IsPodPriorityClass(wl *kueue.Workload) bool {
```

  interesting, I thought cherry picking would have picked it up?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-14T12:56:35Z

- **pkg/controller/core/workload_controller.go:50** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-14T12:56:35Z
```diff
@@ -47,7 +47,6 @@ import (
 	"sigs.k8s.io/controller-runtime/pkg/predicate"
 	"sigs.k8s.io/controller-runtime/pkg/reconcile"
 	"sigs.k8s.io/controller-runtime/pkg/source"
-
```

  Why this removed?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-14T12:56:59Z

- **pkg/workload/workload.go:1420** by [@mimowo](https://github.com/mimowo) — 2026-01-14T12:56:59Z
```diff
@@ -1417,6 +1417,12 @@ func IsWorkloadPriorityClass(wl *kueue.Workload) bool {
 		wl.Spec.PriorityClassRef.Group == kueue.WorkloadPriorityClassGroup
 }
 
+func IsPodPriorityClass(wl *kueue.Workload) bool {
```

  This test case was not added in the PR which is being cherrypicked.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-14T12:58:30Z

- **pkg/controller/core/workload_controller_test.go:2833** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-14T12:58:30Z
```diff
@@ -2734,3 +2734,106 @@ func TestReconcile(t *testing.T) {
 		}
 	}
 }
+
+// TestWorkloadPriorityClassChanged tests the workloadPriorityClassChanged function.
+func TestWorkloadPriorityClassChanged(t *testing.T) {
+	testCases := map[string]struct {
+		oldWorkload *kueue.Workload
+		newWorkload *kueue.Workload
+		wantChanged bool
+	}{
+		"no priority class on either workload": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").Obj(),
+			wantChanged: false,
+		},
+		"same priority class on both workloads": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Obj(),
+			wantChanged: false,
+		},
+		"priority class changed from one to another": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-2").
+				Obj(),
+			wantChanged: true,
+		},
+		"priority class added (none -> some)": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Obj(),
+			wantChanged: true,
+		},
+		"priority class removed (some -> none) - blocked by validation": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").Obj(),
+			wantChanged: false,
+		},
+		"PodPriorityClass (not WorkloadPriorityClass) changed - should not trigger": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				PodPriorityClassRef("pod-priority-1").
+				Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				PodPriorityClassRef("pod-priority-2").
+				Obj(),
+			wantChanged: false,
+		},
+		"PodPriorityClass added (none -> some) - should not trigger": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				PodPriorityClassRef("pod-priority-1").
+				Obj(),
+			wantChanged: false,
+		},
+		"priority value decreased": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				Priority(500).
+				Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				Priority(600).
+				Obj(),
+			wantChanged: false,
+		},
+		"priority value decreased with WPC": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Priority(500).
+				Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				WorkloadPriorityClassRef("priority-1").
+				Priority(100).
+				Obj(),
+			wantChanged: true,
+		},
+		"priority value decreased with PPC": {
+			oldWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				PodPriorityClassRef("pod-priority").
+				Priority(500).
+				Obj(),
+			newWorkload: utiltestingapi.MakeWorkload("wl", "ns").
+				PodPriorityClassRef("pod-priority").
+				Priority(100).
+				Obj(),
+			wantChanged: false,
+		},
+	}
+
+	for name, tc := range testCases {
+		t.Run(name, func(t *testing.T) {
+			gotChanged := workloadPriorityChanged(tc.oldWorkload, tc.newWorkload)
```

  ```suggestion
  			gotChanged := PriorityChanged(tc.oldWorkload, tc.newWorkload)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-14T12:58:58Z

- **pkg/controller/core/workload_controller_test.go:2739** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-14T12:58:58Z
```diff
@@ -2734,3 +2734,106 @@ func TestReconcile(t *testing.T) {
 		}
 	}
 }
+
+// TestWorkloadPriorityClassChanged tests the workloadPriorityClassChanged function.
+func TestWorkloadPriorityClassChanged(t *testing.T) {
```

  This tests moved to workload/workload_test.go

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-14T13:01:17Z

- **test/util/util.go:408** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-14T13:01:17Z
```diff
@@ -379,21 +380,33 @@ func ExpectWorkloadsToBeAdmitted(ctx context.Context, k8sClient client.Client, w
 				admitted.Insert(client.ObjectKeyFromObject(wl))
 			}
 		}
+	}
+	return admitted, nil
+}
+
+func ExpectWorkloadsToBeAdmitted(ctx context.Context, k8sClient client.Client, wls ...*kueue.Workload) {
+	ginkgo.GinkgoHelper()
+	wlKeys := workloadKeys(wls)
+	ExpectWorkloadsToBeAdmittedByKeys(ctx, k8sClient, wlKeys...)
+}
+
+func ExpectWorkloadsToBeAdmittedByKeys(ctx context.Context, k8sClient client.Client, wlKeys ...client.ObjectKey) {
+	ginkgo.GinkgoHelper()
+	wlKeys = uniqueKeys(wlKeys)
+	gomega.Eventually(func(g gomega.Gomega) {
+		admitted, err := admittedWorkloadKeys(ctx, k8sClient, wlKeys)
+		g.Expect(err).NotTo(gomega.HaveOccurred())
 		g.Expect(admitted).Should(gomega.Equal(wlKeys), "Unexpected workloads are admitted")
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
 func ExpectWorkloadsToBeAdmittedCount(ctx context.Context, k8sClient client.Client, count int, wls ...*kueue.Workload) {
-	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) {
-		admitted := 0
-		var updatedWorkload kueue.Workload
-		for _, wl := range wls {
-			g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWorkload)).To(gomega.Succeed())
-			if apimeta.IsStatusConditionTrue(updatedWorkload.Status.Conditions, kueue.WorkloadAdmitted) {
-				admitted++
-			}
-		}
-		g.Expect(admitted).Should(gomega.Equal(count), "Not enough workloads are admitted")
+	ginkgo.GinkgoHelper()
+	wlKeys := uniqueKeys(workloadKeys(wls))
+	gomega.Eventually(func(g gomega.Gomega) {
+		admitted, err := admittedWorkloadKeys(ctx, k8sClient, wlKeys)
+		g.Expect(err).NotTo(gomega.HaveOccurred())
+		g.Expect(admitted).Should(gomega.HaveLen(count), "Not enough workloads are admitted from the list: %v", wlKeys)
```

  Is it your changes?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-14T13:01:25Z

- **test/util/util.go:408** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-14T13:01:25Z
```diff
@@ -379,21 +380,33 @@ func ExpectWorkloadsToBeAdmitted(ctx context.Context, k8sClient client.Client, w
 				admitted.Insert(client.ObjectKeyFromObject(wl))
 			}
 		}
+	}
+	return admitted, nil
+}
+
+func ExpectWorkloadsToBeAdmitted(ctx context.Context, k8sClient client.Client, wls ...*kueue.Workload) {
+	ginkgo.GinkgoHelper()
+	wlKeys := workloadKeys(wls)
+	ExpectWorkloadsToBeAdmittedByKeys(ctx, k8sClient, wlKeys...)
+}
+
+func ExpectWorkloadsToBeAdmittedByKeys(ctx context.Context, k8sClient client.Client, wlKeys ...client.ObjectKey) {
+	ginkgo.GinkgoHelper()
+	wlKeys = uniqueKeys(wlKeys)
+	gomega.Eventually(func(g gomega.Gomega) {
+		admitted, err := admittedWorkloadKeys(ctx, k8sClient, wlKeys)
+		g.Expect(err).NotTo(gomega.HaveOccurred())
 		g.Expect(admitted).Should(gomega.Equal(wlKeys), "Unexpected workloads are admitted")
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
 func ExpectWorkloadsToBeAdmittedCount(ctx context.Context, k8sClient client.Client, count int, wls ...*kueue.Workload) {
-	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) {
-		admitted := 0
-		var updatedWorkload kueue.Workload
-		for _, wl := range wls {
-			g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWorkload)).To(gomega.Succeed())
-			if apimeta.IsStatusConditionTrue(updatedWorkload.Status.Conditions, kueue.WorkloadAdmitted) {
-				admitted++
-			}
-		}
-		g.Expect(admitted).Should(gomega.Equal(count), "Not enough workloads are admitted")
+	ginkgo.GinkgoHelper()
+	wlKeys := uniqueKeys(workloadKeys(wls))
+	gomega.Eventually(func(g gomega.Gomega) {
+		admitted, err := admittedWorkloadKeys(ctx, k8sClient, wlKeys)
+		g.Expect(err).NotTo(gomega.HaveOccurred())
+		g.Expect(admitted).Should(gomega.HaveLen(count), "Not enough workloads are admitted from the list: %v", wlKeys)
```

  If not revert please

### Review by [@andrewseif](https://github.com/andrewseif) (commented) — 2026-01-14T15:46:43Z

- **test/util/util.go:408** by [@andrewseif](https://github.com/andrewseif) — 2026-01-14T15:46:43Z
```diff
@@ -379,21 +380,33 @@ func ExpectWorkloadsToBeAdmitted(ctx context.Context, k8sClient client.Client, w
 				admitted.Insert(client.ObjectKeyFromObject(wl))
 			}
 		}
+	}
+	return admitted, nil
+}
+
+func ExpectWorkloadsToBeAdmitted(ctx context.Context, k8sClient client.Client, wls ...*kueue.Workload) {
+	ginkgo.GinkgoHelper()
+	wlKeys := workloadKeys(wls)
+	ExpectWorkloadsToBeAdmittedByKeys(ctx, k8sClient, wlKeys...)
+}
+
+func ExpectWorkloadsToBeAdmittedByKeys(ctx context.Context, k8sClient client.Client, wlKeys ...client.ObjectKey) {
+	ginkgo.GinkgoHelper()
+	wlKeys = uniqueKeys(wlKeys)
+	gomega.Eventually(func(g gomega.Gomega) {
+		admitted, err := admittedWorkloadKeys(ctx, k8sClient, wlKeys)
+		g.Expect(err).NotTo(gomega.HaveOccurred())
 		g.Expect(admitted).Should(gomega.Equal(wlKeys), "Unexpected workloads are admitted")
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
 func ExpectWorkloadsToBeAdmittedCount(ctx context.Context, k8sClient client.Client, count int, wls ...*kueue.Workload) {
-	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) {
-		admitted := 0
-		var updatedWorkload kueue.Workload
-		for _, wl := range wls {
-			g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWorkload)).To(gomega.Succeed())
-			if apimeta.IsStatusConditionTrue(updatedWorkload.Status.Conditions, kueue.WorkloadAdmitted) {
-				admitted++
-			}
-		}
-		g.Expect(admitted).Should(gomega.Equal(count), "Not enough workloads are admitted")
+	ginkgo.GinkgoHelper()
+	wlKeys := uniqueKeys(workloadKeys(wls))
+	gomega.Eventually(func(g gomega.Gomega) {
+		admitted, err := admittedWorkloadKeys(ctx, k8sClient, wlKeys)
+		g.Expect(err).NotTo(gomega.HaveOccurred())
+		g.Expect(admitted).Should(gomega.HaveLen(count), "Not enough workloads are admitted from the list: %v", wlKeys)
```

  it was in one of my PRs, I changed it by accident back then, and then reverted the change

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-14T15:48:33Z

- **pkg/controller/core/workload_controller_test.go:41** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-14T15:48:33Z
```diff
@@ -38,7 +38,6 @@ import (
 	"sigs.k8s.io/controller-runtime/pkg/client"
 	"sigs.k8s.io/controller-runtime/pkg/client/interceptor"
 	"sigs.k8s.io/controller-runtime/pkg/reconcile"
-
```

  Why this changes?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-14T15:49:08Z

- **pkg/controller/jobframework/reconciler.go:953** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-14T15:49:08Z
```diff
@@ -946,7 +946,11 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 
 // UpdateWorkloadPriority updates workload priority if object's kueue.x-k8s.io/priority-class label changed.
 func UpdateWorkloadPriority(ctx context.Context, c client.Client, r record.EventRecorder, obj client.Object, wl *kueue.Workload, customPriorityClassFunc func() string) error {
-	if workload.IsWorkloadPriorityClass(wl) && WorkloadPriorityClassName(obj) != workload.PriorityClassName(wl) {
+	// This handles both: changing priority (old -> new) AND adding priority (none -> new)
+	jobPriorityClassName := WorkloadPriorityClassName(obj)
+	wlPriorityClassName := workload.PriorityClassName(wl)
+
+	if jobPriorityClassName != "" && jobPriorityClassName != wlPriorityClassName {
```

  I think we missed something here. Please double check.

### Review by [@andrewseif](https://github.com/andrewseif) (commented) — 2026-01-14T15:59:17Z

- **pkg/controller/core/workload_controller_test.go:41** by [@andrewseif](https://github.com/andrewseif) — 2026-01-14T15:59:17Z
```diff
@@ -38,7 +38,6 @@ import (
 	"sigs.k8s.io/controller-runtime/pkg/client"
 	"sigs.k8s.io/controller-runtime/pkg/client/interceptor"
 	"sigs.k8s.io/controller-runtime/pkg/reconcile"
-
```

  probably linter, do I put it back?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-14T16:00:14Z

- **pkg/controller/core/workload_controller_test.go:41** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-14T16:00:14Z
```diff
@@ -38,7 +38,6 @@ import (
 	"sigs.k8s.io/controller-runtime/pkg/client"
 	"sigs.k8s.io/controller-runtime/pkg/client/interceptor"
 	"sigs.k8s.io/controller-runtime/pkg/reconcile"
-
```

  Yes, please, revert.

### Review by [@andrewseif](https://github.com/andrewseif) (commented) — 2026-01-14T16:03:10Z

- **pkg/controller/jobframework/reconciler.go:953** by [@andrewseif](https://github.com/andrewseif) — 2026-01-14T16:03:10Z
```diff
@@ -946,7 +946,11 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 
 // UpdateWorkloadPriority updates workload priority if object's kueue.x-k8s.io/priority-class label changed.
 func UpdateWorkloadPriority(ctx context.Context, c client.Client, r record.EventRecorder, obj client.Object, wl *kueue.Workload, customPriorityClassFunc func() string) error {
-	if workload.IsWorkloadPriorityClass(wl) && WorkloadPriorityClassName(obj) != workload.PriorityClassName(wl) {
+	// This handles both: changing priority (old -> new) AND adding priority (none -> new)
+	jobPriorityClassName := WorkloadPriorityClassName(obj)
+	wlPriorityClassName := workload.PriorityClassName(wl)
+
+	if jobPriorityClassName != "" && jobPriorityClassName != wlPriorityClassName {
```

  yes function is different than main, ill update it

### Review by [@andrewseif](https://github.com/andrewseif) (commented) — 2026-01-14T16:05:38Z

- **pkg/controller/core/workload_controller.go:50** by [@andrewseif](https://github.com/andrewseif) — 2026-01-14T16:05:37Z
```diff
@@ -47,7 +47,6 @@ import (
 	"sigs.k8s.io/controller-runtime/pkg/predicate"
 	"sigs.k8s.io/controller-runtime/pkg/reconcile"
 	"sigs.k8s.io/controller-runtime/pkg/source"
-
```

  done
