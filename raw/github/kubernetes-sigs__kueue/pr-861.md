# PR #861: Workload finalizer

**Summary**: Workload finalizer

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/861

**Last updated**: 2023-08-18T13:14:31Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `workload-finalizer` → `main`
- **Created**: 2023-06-16T06:00:18Z
- **Updated**: 2023-08-18T13:14:31Z
- **Merged**: 2023-08-18T13:14:29Z
- **Closed**: 2023-08-18T13:14:29Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `tide/merge-method-squash`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 4   **+238 / -36**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Add and manage resourceInUse finalizer for workloads, to add the posibility to restore the podSets Info upon workload deletion.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #518

#### Special notes for your reviewer:

This PR is split of #834 ,  the code was reviewed there.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-06-16T06:00:41Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | e08d53d8238e2abb9bda58ffdf24a675177d5a3c |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/64defd5510175b0008ebcded |

### Comment by [@trasc](https://github.com/trasc) — 2023-06-16T06:01:31Z

/hold 
for #834

### Comment by [@trasc](https://github.com/trasc) — 2023-06-16T06:18:52Z

/retest

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-16T15:12:35Z

Let's park this for the next release

/hold

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-31T15:19:17Z

/assign @mimowo
for a review pass

### Comment by [@trasc](https://github.com/trasc) — 2023-08-03T09:21:17Z

/reopen

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-08-03T09:21:21Z

@trasc: Failed to re-open PR: state cannot be changed. The workload-finalizer branch was force-pushed or recreated.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/861#issuecomment-1663610692):

>/reopen
>
>
>
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@trasc](https://github.com/trasc) — 2023-08-03T09:26:41Z

reopened as #1037

### Comment by [@trasc](https://github.com/trasc) — 2023-08-03T09:33:53Z

was unintentionally closed

### Comment by [@mimowo](https://github.com/mimowo) — 2023-08-03T09:57:19Z

I'm wondering if we can reopen this, so there is not need to transfer the discussion. We probably hit this https://github.com/isaacs/github/issues/361 based on the message `Failed to re-open PR: state cannot be changed. The workload-finalizer branch was force-pushed or recreated.`. Could you try if this works: https://github.com/isaacs/github/issues/361#issuecomment-219088644. If it doesn't it is fine for me, but I've seen this also somewhere else, so it would be nice to have a working solution / workaround.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-03T13:42:43Z

/reopen

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-08-03T13:42:47Z

@alculquicondor: Failed to re-open PR: state cannot be changed. The workload-finalizer branch was force-pushed or recreated.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/861#issuecomment-1664008918):

>/reopen


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@trasc](https://github.com/trasc) — 2023-08-04T05:45:12Z

/reopen

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-08-04T05:45:16Z

@trasc: Reopened this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/861#issuecomment-1665044502):

>/reopen
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@trasc](https://github.com/trasc) — 2023-08-04T18:48:05Z

/unhold

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-18T13:00:46Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-08-18T13:00:54Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/861#issuecomment-1683886398" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/861#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-18T13:01:10Z

Let's hope the new default merge method works

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-18T13:10:20Z

/hold

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-18T13:10:34Z

/label tide/merge-method-squash
/hold cancel
(The PR to change the merge method didn't merge yet)

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-08-02T15:39:30Z

Mostly concerns about complications with handling mid stopped jobs. I hope this can be now simplified after https://github.com/kubernetes-sigs/kueue/pull/1005. Also, wondering if the code can be simpler, if it was workload_controller that removes the workload. Finally, there is an extra API call now to add the finalizer, I would like to add it when creating the workload.

- **pkg/controller/jobframework/reconciler.go:126** by [@mimowo](https://github.com/mimowo) — 2023-08-02T15:20:19Z
```diff
@@ -103,14 +104,31 @@ func NewReconciler(
 
 func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Request, job GenericJob) (ctrl.Result, error) {
 	object := job.Object()
-	if err := r.client.Get(ctx, req.NamespacedName, object); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	log := ctrl.LoggerFrom(ctx).WithValues("job", req.String())
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	err := r.client.Get(ctx, req.NamespacedName, object)
+
+	if apierrors.IsNotFound(err) || !object.GetDeletionTimestamp().IsZero() {
```

  Could you provide maybe first some context for what is the scenario we are fixing here? I was thinking the scenario is that a user deletes a workload, thus losing the node selectors. However, this if appears to be handling deletion of a Job, so I'm not sure how this is connected.
  
  In particular, this code has user-facing impact. Please explain in the release note the impact, the fixed scenario.
  
  Initially, I was thinking there would be a finalizer added to workloads, at workloads creation. Then, there would be a check in the workload_controller to see if the OwnerReference (the Job) exists. If the Job exists, then we can patch the workload to remove the finalizer.

- **pkg/controller/jobframework/reconciler.go:158** by [@mimowo](https://github.com/mimowo) — 2023-08-02T15:20:56Z
```diff
@@ -123,7 +141,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 				"queueName", QueueName(job), "parentWorkload", ParentWorkloadName(job))
 			return ctrl.Result{}, nil
 		}
-		isParentJobManaged, err := r.IsParentJobManaged(ctx, job.Object(), namespacedName.Namespace)
+		isParentJobManaged, err := r.IsParentJobManaged(ctx, job.Object(), req.Namespace)
```

  why this change?

- **pkg/controller/jobframework/reconciler.go:197** by [@mimowo](https://github.com/mimowo) — 2023-08-02T15:24:20Z
```diff
@@ -164,6 +182,32 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, err
 	}
 
+	// 1.1 If the workload is pending deletion, suspend the job if needed
+	// and drop the finalizer.
+	if wl != nil && !wl.DeletionTimestamp.IsZero() {
+		log.V(2).Info("The workload is marked for deletion")
+		if !job.IsSuspended() {
+			err := r.stopJob(ctx, job, object, wl, "Workload is deleted")
+			if err != nil {
+				log.Error(err, "Suspending job with deleted workload")
+			}
+			return ctrl.Result{}, err
+		} else {
+			// make sure the finalizer is removed
+			err := r.setFinalizer(ctx, wl, false)
```

  I would like to have a dedicated function, such as  `removeFinalizer`. Also, would we need to remove the finalizer here, if we added the finalizer removal to the workload_controller, that deletes the finalizer, if the job is suspended?

- **pkg/controller/jobframework/reconciler.go:189** by [@mimowo](https://github.com/mimowo) — 2023-08-02T15:27:59Z
```diff
@@ -164,6 +182,32 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, err
 	}
 
+	// 1.1 If the workload is pending deletion, suspend the job if needed
+	// and drop the finalizer.
+	if wl != nil && !wl.DeletionTimestamp.IsZero() {
+		log.V(2).Info("The workload is marked for deletion")
+		if !job.IsSuspended() {
```

  I'm not sure we want this check, as it was a source of issue in a recently fixed bug: https://github.com/kubernetes-sigs/kueue/pull/1005

- **pkg/controller/jobframework/reconciler.go:203** by [@mimowo](https://github.com/mimowo) — 2023-08-02T15:30:47Z
```diff
@@ -164,6 +182,32 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, err
 	}
 
+	// 1.1 If the workload is pending deletion, suspend the job if needed
+	// and drop the finalizer.
+	if wl != nil && !wl.DeletionTimestamp.IsZero() {
+		log.V(2).Info("The workload is marked for deletion")
+		if !job.IsSuspended() {
+			err := r.stopJob(ctx, job, object, wl, "Workload is deleted")
+			if err != nil {
+				log.Error(err, "Suspending job with deleted workload")
+			}
+			return ctrl.Result{}, err
+		} else {
+			// make sure the finalizer is removed
+			err := r.setFinalizer(ctx, wl, false)
+			return ctrl.Result{}, err
+		}
+	}
+
+	// 1.2 if a job stop failure is suspected
+	if suspectJobStopFailure(job, wl) {
```

  This looks rather suspicious. First, why do we need this , and do we need this given https://github.com/kubernetes-sigs/kueue/pull/1005. Second, if really needed add a comment to explain a scenario in which this is needed.

- **pkg/controller/jobframework/reconciler.go:379** by [@mimowo](https://github.com/mimowo) — 2023-08-02T15:31:57Z
```diff
@@ -328,6 +372,22 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 		}
 	}
 
+	// the job and workload might not be equivalent if the previous
+	// reconcile failed mid stopJob, in which case the job is IsSuspended
+	// the workload is not Admitted but it has the finalizer set
+	// search again
+	if match == nil && job.IsSuspended() {
```

  Similarly here, is this needed after https://github.com/kubernetes-sigs/kueue/pull/1005?

- **pkg/controller/jobframework/reconciler.go:477** by [@mimowo](https://github.com/mimowo) — 2023-08-02T15:34:26Z
```diff
@@ -409,6 +474,11 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 
 // startJob will unsuspend the job, and also inject the node affinity.
 func (r *JobReconciler) startJob(ctx context.Context, job GenericJob, object client.Object, wl *kueue.Workload) error {
+	err := r.setFinalizer(ctx, wl, true)
```

  This will require an extra API request. Why not add the finalizer during workload creation in ConstructWorkload?

- **pkg/controller/jobframework/reconciler.go:512** by [@mimowo](https://github.com/mimowo) — 2023-08-02T15:35:51Z
```diff
@@ -435,23 +505,47 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object clie
 		if stoppedNow {
 			r.record.Eventf(object, corev1.EventTypeNormal, "Stopped", eventMsg)
 		}
-		return err
+		if err != nil {
+			return err
+		}
+	} else {
+		if job.IsSuspended() {
```

  This `if` is fairly surprising. Again, I suspect it might not be needed after https://github.com/kubernetes-sigs/kueue/pull/1005. Please explain if it is really needed.

- **pkg/controller/jobframework/reconciler.go:536** by [@mimowo](https://github.com/mimowo) — 2023-08-02T15:36:44Z
```diff
@@ -435,23 +505,47 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object clie
 		if stoppedNow {
 			r.record.Eventf(object, corev1.EventTypeNormal, "Stopped", eventMsg)
 		}
-		return err
+		if err != nil {
+			return err
+		}
+	} else {
+		if job.IsSuspended() {
+			return nil
+		}
+
+		job.Suspend()
+		if info != nil {
+			job.RestorePodSetsInfo(info)
+		}
+		if err := r.client.Update(ctx, object); err != nil {
+			return err
+		}
+		r.record.Eventf(object, corev1.EventTypeNormal, "Stopped", eventMsg)
 	}
 
-	if job.IsSuspended() {
-		return nil
+	if wl != nil {
+		return r.setFinalizer(ctx, wl, false)
 	}
+	return nil
+}
+
+func suspectJobStopFailure(job GenericJob, wl *kueue.Workload) bool {
+	return wl != nil && job.IsSuspended() && !workload.IsAdmitted(wl) && controllerutil.ContainsFinalizer(wl, kueue.ResourceInUseFinalizerName)
+}
 
-	job.Suspend()
-	if info != nil {
-		job.RestorePodSetsInfo(info)
+func (r *JobReconciler) setFinalizer(ctx context.Context, wl *kueue.Workload, on bool) error {
```

  I would actually prefer to have 2 functions: addFinalizer and removeFinalizer.

- **test/integration/controller/job/job_controller_test.go:849** by [@mimowo](https://github.com/mimowo) — 2023-08-02T15:37:43Z
```diff
@@ -845,7 +846,7 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 		util.ExpectAdmittedActiveWorkloadsMetric(prodClusterQ, 1)
 	})
 
-	ginkgo.It("Should unsuspend job iff localQueue is in the same namespace", func() {
+	ginkgo.It("Should unsuspend job if localQueue is in the same namespace", func() {
```

  Is it a drive by fix, or a legitimate change in semantics. If the former, please do a small separate PR.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-03T09:24:36Z

- **pkg/controller/jobframework/reconciler.go:126** by [@trasc](https://github.com/trasc) — 2023-08-03T05:55:08Z
```diff
@@ -103,14 +104,31 @@ func NewReconciler(
 
 func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Request, job GenericJob) (ctrl.Result, error) {
 	object := job.Object()
-	if err := r.client.Get(ctx, req.NamespacedName, object); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	log := ctrl.LoggerFrom(ctx).WithValues("job", req.String())
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	err := r.client.Get(ctx, req.NamespacedName, object)
+
+	if apierrors.IsNotFound(err) || !object.GetDeletionTimestamp().IsZero() {
```

  This part , just makes sure that when a job gets deleted , it's potential workloads are not left with the finalizer set.
  
  >Initially, I was thinking there would be a finalizer added to workloads, at workloads creation. Then, there would be a check in the workload_controller to see if the OwnerReference (the Job) exists. If the Job exists, then we can patch the workload to remove the finalizer.
  
  We only need to keep the finalizer set until the podsetsInfo gets restored not until the deletion of the job.

- **pkg/controller/jobframework/reconciler.go:158** by [@trasc](https://github.com/trasc) — 2023-08-03T06:04:58Z
```diff
@@ -123,7 +141,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 				"queueName", QueueName(job), "parentWorkload", ParentWorkloadName(job))
 			return ctrl.Result{}, nil
 		}
-		isParentJobManaged, err := r.IsParentJobManaged(ctx, job.Object(), namespacedName.Namespace)
+		isParentJobManaged, err := r.IsParentJobManaged(ctx, job.Object(), req.Namespace)
```

  Just a small clean-up.  `namespacedName` was only used to decorate the logs and in this call.
  The log decoration needed to be clanged since now we can theoretically emit logs for  missing objects.
  The content of `namespacedName` was always the same as the one of the request.

- **pkg/controller/jobframework/reconciler.go:197** by [@trasc](https://github.com/trasc) — 2023-08-03T06:41:33Z
```diff
@@ -164,6 +182,32 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, err
 	}
 
+	// 1.1 If the workload is pending deletion, suspend the job if needed
+	// and drop the finalizer.
+	if wl != nil && !wl.DeletionTimestamp.IsZero() {
+		log.V(2).Info("The workload is marked for deletion")
+		if !job.IsSuspended() {
+			err := r.stopJob(ctx, job, object, wl, "Workload is deleted")
+			if err != nil {
+				log.Error(err, "Suspending job with deleted workload")
+			}
+			return ctrl.Result{}, err
+		} else {
+			// make sure the finalizer is removed
+			err := r.setFinalizer(ctx, wl, false)
```

  > I would like to have a dedicated function, such as removeFinalizer
  
  done
  
  >Also, would we need to remove the finalizer here, if we added the finalizer removal to the workload_controller, that deletes the finalizer, if the job is suspended?
  
  The finalizer along with set/restore podsetInfo are jobframework specific concepts.

- **pkg/controller/jobframework/reconciler.go:379** by [@trasc](https://github.com/trasc) — 2023-08-03T07:42:34Z
```diff
@@ -328,6 +372,22 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 		}
 	}
 
+	// the job and workload might not be equivalent if the previous
+	// reconcile failed mid stopJob, in which case the job is IsSuspended
+	// the workload is not Admitted but it has the finalizer set
+	// search again
+	if match == nil && job.IsSuspended() {
```

  this is related to the eviction...  but is not covered by #1005, to simplify this pr and keep the focus on workload deletion, let's drop it for now, we can revisit this in another issue.

- **pkg/controller/jobframework/reconciler.go:536** by [@trasc](https://github.com/trasc) — 2023-08-03T07:47:00Z
```diff
@@ -435,23 +505,47 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object clie
 		if stoppedNow {
 			r.record.Eventf(object, corev1.EventTypeNormal, "Stopped", eventMsg)
 		}
-		return err
+		if err != nil {
+			return err
+		}
+	} else {
+		if job.IsSuspended() {
+			return nil
+		}
+
+		job.Suspend()
+		if info != nil {
+			job.RestorePodSetsInfo(info)
+		}
+		if err := r.client.Update(ctx, object); err != nil {
+			return err
+		}
+		r.record.Eventf(object, corev1.EventTypeNormal, "Stopped", eventMsg)
 	}
 
-	if job.IsSuspended() {
-		return nil
+	if wl != nil {
+		return r.setFinalizer(ctx, wl, false)
 	}
+	return nil
+}
+
+func suspectJobStopFailure(job GenericJob, wl *kueue.Workload) bool {
+	return wl != nil && job.IsSuspended() && !workload.IsAdmitted(wl) && controllerutil.ContainsFinalizer(wl, kueue.ResourceInUseFinalizerName)
+}
 
-	job.Suspend()
-	if info != nil {
-		job.RestorePodSetsInfo(info)
+func (r *JobReconciler) setFinalizer(ctx context.Context, wl *kueue.Workload, on bool) error {
```

  done

- **test/integration/controller/job/job_controller_test.go:849** by [@trasc](https://github.com/trasc) — 2023-08-03T07:47:39Z
```diff
@@ -845,7 +846,7 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 		util.ExpectAdmittedActiveWorkloadsMetric(prodClusterQ, 1)
 	})
 
-	ginkgo.It("Should unsuspend job iff localQueue is in the same namespace", func() {
+	ginkgo.It("Should unsuspend job if localQueue is in the same namespace", func() {
```

  dropped

- **pkg/controller/jobframework/reconciler.go:512** by [@trasc](https://github.com/trasc) — 2023-08-03T07:55:16Z
```diff
@@ -435,23 +505,47 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object clie
 		if stoppedNow {
 			r.record.Eventf(object, corev1.EventTypeNormal, "Stopped", eventMsg)
 		}
-		return err
+		if err != nil {
+			return err
+		}
+	} else {
+		if job.IsSuspended() {
```

  this is not added in this pr , however reworked

- **pkg/controller/jobframework/reconciler.go:477** by [@trasc](https://github.com/trasc) — 2023-08-03T07:56:53Z
```diff
@@ -409,6 +474,11 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 
 // startJob will unsuspend the job, and also inject the node affinity.
 func (r *JobReconciler) startJob(ctx context.Context, job GenericJob, object client.Object, wl *kueue.Workload) error {
+	err := r.setFinalizer(ctx, wl, true)
```

  I prefer to keep the finalizer in effect only for the duration of the workload admission.

- **pkg/controller/jobframework/reconciler.go:203** by [@trasc](https://github.com/trasc) — 2023-08-03T07:57:01Z
```diff
@@ -164,6 +182,32 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, err
 	}
 
+	// 1.1 If the workload is pending deletion, suspend the job if needed
+	// and drop the finalizer.
+	if wl != nil && !wl.DeletionTimestamp.IsZero() {
+		log.V(2).Info("The workload is marked for deletion")
+		if !job.IsSuspended() {
+			err := r.stopJob(ctx, job, object, wl, "Workload is deleted")
+			if err != nil {
+				log.Error(err, "Suspending job with deleted workload")
+			}
+			return ctrl.Result{}, err
+		} else {
+			// make sure the finalizer is removed
+			err := r.setFinalizer(ctx, wl, false)
+			return ctrl.Result{}, err
+		}
+	}
+
+	// 1.2 if a job stop failure is suspected
+	if suspectJobStopFailure(job, wl) {
```

  indeed #1005 covers the restore failure in case of eviction.

- **pkg/controller/jobframework/reconciler.go:189** by [@trasc](https://github.com/trasc) — 2023-08-03T08:00:07Z
```diff
@@ -164,6 +182,32 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, err
 	}
 
+	// 1.1 If the workload is pending deletion, suspend the job if needed
+	// and drop the finalizer.
+	if wl != nil && !wl.DeletionTimestamp.IsZero() {
+		log.V(2).Info("The workload is marked for deletion")
+		if !job.IsSuspended() {
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-08-03T09:39:10Z

- **pkg/controller/jobframework/reconciler.go:126** by [@mimowo](https://github.com/mimowo) — 2023-08-03T09:39:09Z
```diff
@@ -103,14 +104,31 @@ func NewReconciler(
 
 func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Request, job GenericJob) (ctrl.Result, error) {
 	object := job.Object()
-	if err := r.client.Get(ctx, req.NamespacedName, object); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	log := ctrl.LoggerFrom(ctx).WithValues("job", req.String())
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	err := r.client.Get(ctx, req.NamespacedName, object)
+
+	if apierrors.IsNotFound(err) || !object.GetDeletionTimestamp().IsZero() {
```

  I see. Still, we could check if the owner Job exists or is being deleted (as here) in the workload_controller. Needing to deal with only a single owner job seems simpler.
  
  To the second part, keeping the finalizer for the duration of the job existence seems simpler conceptually, and fewer API requests on suspend / unsuspend, so I'm trying to understand the motivation to delete the workload on suspend. Also, I'm not sure about keeping the workload until the  nodeSelectors are restored. How do we know if this happened? I guess we would need to have some condition to indicate that the nodeSelectors are restored. Otherwise relying just on suspend=true may give wrong results. WDYT?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-03T13:50:12Z

- **pkg/controller/jobframework/reconciler.go:126** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-03T13:50:12Z
```diff
@@ -103,14 +104,31 @@ func NewReconciler(
 
 func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Request, job GenericJob) (ctrl.Result, error) {
 	object := job.Object()
-	if err := r.client.Get(ctx, req.NamespacedName, object); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	log := ctrl.LoggerFrom(ctx).WithValues("job", req.String())
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	err := r.client.Get(ctx, req.NamespacedName, object)
+
+	if apierrors.IsNotFound(err) || !object.GetDeletionTimestamp().IsZero() {
```

  > I guess we would need to have some condition to indicate that the nodeSelectors are restored. Otherwise relying just on suspend=true may give wrong results. 
  
  Given how Job requires 3 different API calls to suspend, it's indeed not enough to rely on the suspend field.
  It's cleaner to just keep the finalizer until the Job is deleted (has a deletionTimestamp) or the Workload has a Finished condition.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-09T05:29:22Z

- **pkg/controller/jobframework/reconciler.go:126** by [@trasc](https://github.com/trasc) — 2023-08-09T05:29:22Z
```diff
@@ -103,14 +104,31 @@ func NewReconciler(
 
 func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Request, job GenericJob) (ctrl.Result, error) {
 	object := job.Object()
-	if err := r.client.Get(ctx, req.NamespacedName, object); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	log := ctrl.LoggerFrom(ctx).WithValues("job", req.String())
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	err := r.client.Get(ctx, req.NamespacedName, object)
+
+	if apierrors.IsNotFound(err) || !object.GetDeletionTimestamp().IsZero() {
```

  Initially , the presence of the finalizer for in the workload of a suspended job was an indicator that `stopJob` failed somewhere mid execution. However #1005 has another approach.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-08-09T09:16:24Z

- **pkg/controller/jobframework/reconciler.go:126** by [@mimowo](https://github.com/mimowo) — 2023-08-09T09:16:24Z
```diff
@@ -103,14 +104,31 @@ func NewReconciler(
 
 func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Request, job GenericJob) (ctrl.Result, error) {
 	object := job.Object()
-	if err := r.client.Get(ctx, req.NamespacedName, object); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	log := ctrl.LoggerFrom(ctx).WithValues("job", req.String())
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	err := r.client.Get(ctx, req.NamespacedName, object)
+
+	if apierrors.IsNotFound(err) || !object.GetDeletionTimestamp().IsZero() {
```

  I'm wondering what are the user-facing differences between:
  1. workload lifetime is just for the running job
  2. workload lifetime tied to the Job lifetime, for as long as they are equivalent
  
  In (1.) we need two additional calls to add and remove the finalizer, so the performance is degraded (it can be argued how much). Are there drawbacks of (2.)?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-09T10:10:51Z

- **pkg/controller/jobframework/reconciler.go:126** by [@trasc](https://github.com/trasc) — 2023-08-09T10:10:51Z
```diff
@@ -103,14 +104,31 @@ func NewReconciler(
 
 func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Request, job GenericJob) (ctrl.Result, error) {
 	object := job.Object()
-	if err := r.client.Get(ctx, req.NamespacedName, object); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	log := ctrl.LoggerFrom(ctx).WithValues("job", req.String())
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	err := r.client.Get(ctx, req.NamespacedName, object)
+
+	if apierrors.IsNotFound(err) || !object.GetDeletionTimestamp().IsZero() {
```

  with 2, deleting a workload while the job is still present requires the job controller to remove the finalizer (one additional reconcile cycle, if triggered from outside the job controller)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-11T13:36:50Z

- **pkg/controller/jobframework/reconciler.go:126** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T13:36:50Z
```diff
@@ -103,14 +104,31 @@ func NewReconciler(
 
 func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Request, job GenericJob) (ctrl.Result, error) {
 	object := job.Object()
-	if err := r.client.Get(ctx, req.NamespacedName, object); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	log := ctrl.LoggerFrom(ctx).WithValues("job", req.String())
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	err := r.client.Get(ctx, req.NamespacedName, object)
+
+	if apierrors.IsNotFound(err) || !object.GetDeletionTimestamp().IsZero() {
```

  We could remove the finalizer from Workload in 2 scenarios:
  - The Workload is already marked as finished.
  - The Job is deleted.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-11T15:18:25Z

- **pkg/controller/jobframework/reconciler.go:126** by [@trasc](https://github.com/trasc) — 2023-08-11T15:18:25Z
```diff
@@ -103,14 +104,31 @@ func NewReconciler(
 
 func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Request, job GenericJob) (ctrl.Result, error) {
 	object := job.Object()
-	if err := r.client.Get(ctx, req.NamespacedName, object); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	log := ctrl.LoggerFrom(ctx).WithValues("job", req.String())
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	err := r.client.Get(ctx, req.NamespacedName, object)
+
+	if apierrors.IsNotFound(err) || !object.GetDeletionTimestamp().IsZero() {
```

  But still need to be removed if not finished and the job is suspended if the workload gets deleted.
  
  Still the current implementation is the best fit, in my mind.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-08-11T15:57:33Z

- **pkg/controller/jobframework/reconciler.go:126** by [@mimowo](https://github.com/mimowo) — 2023-08-11T15:57:33Z
```diff
@@ -103,14 +104,31 @@ func NewReconciler(
 
 func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Request, job GenericJob) (ctrl.Result, error) {
 	object := job.Object()
-	if err := r.client.Get(ctx, req.NamespacedName, object); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	log := ctrl.LoggerFrom(ctx).WithValues("job", req.String())
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	err := r.client.Get(ctx, req.NamespacedName, object)
+
+	if apierrors.IsNotFound(err) || !object.GetDeletionTimestamp().IsZero() {
```

  > Still the current implementation is the best fit, in my mind.
  
  Could you explain why? As pointed out it requires more API calls than the alternative. So far, other potential benefits to the user are not clear to me.
  
  In particular, in the scenario of manual workload deletion (after suspending) it requires 2 API calls (one to add and one to delete the finalizer). Additionally, two extra API calls to add / remove the finalizer for the replacement workload. So total 4 extra API calls, vs. 2 extra API calls in the alternative (in the alternative we need one extra call to remove the finalizer for the first workload, and one for the replacement workload). I cannot think of a scenario when it requires fewer API calls.
  
  I accept I might be missing something, but some supportive explanation would be useful.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-11T16:55:23Z

- **pkg/controller/jobframework/reconciler.go:126** by [@trasc](https://github.com/trasc) — 2023-08-11T16:55:23Z
```diff
@@ -103,14 +104,31 @@ func NewReconciler(
 
 func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Request, job GenericJob) (ctrl.Result, error) {
 	object := job.Object()
-	if err := r.client.Get(ctx, req.NamespacedName, object); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	log := ctrl.LoggerFrom(ctx).WithValues("job", req.String())
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	err := r.client.Get(ctx, req.NamespacedName, object)
+
+	if apierrors.IsNotFound(err) || !object.GetDeletionTimestamp().IsZero() {
```

  > In particular, in the scenario of manual workload deletion (after suspending) it requires 2 API calls (one to add and one to delete the finalizer).
  
  I don't follow.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-08-11T17:12:55Z

- **pkg/controller/jobframework/reconciler.go:126** by [@mimowo](https://github.com/mimowo) — 2023-08-11T17:12:54Z
```diff
@@ -103,14 +104,31 @@ func NewReconciler(
 
 func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Request, job GenericJob) (ctrl.Result, error) {
 	object := job.Object()
-	if err := r.client.Get(ctx, req.NamespacedName, object); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	log := ctrl.LoggerFrom(ctx).WithValues("job", req.String())
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	err := r.client.Get(ctx, req.NamespacedName, object)
+
+	if apierrors.IsNotFound(err) || !object.GetDeletionTimestamp().IsZero() {
```

  I had in mind that you require 2 API calls [here](https://github.com/kubernetes-sigs/kueue/blob/46980b39077eb773a08f48c4712c2fac622b0c41/pkg/controller/jobframework/reconciler.go#L474) and [here](https://github.com/kubernetes-sigs/kueue/blob/46980b39077eb773a08f48c4712c2fac622b0c41/pkg/controller/jobframework/reconciler.go#L522).
  So, when you mentioned workload deletion I was thinking about the scenario:
  1. Job created [here is the first call to add the finalizer]
  2. job started
  3. job suspended, workload unadmitted [+ API  call to remove the finalizer]
  4. workload manually deleted by user, and replacement workload created [+ API call to add the finalizer, and +API call to eventually delete it]

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-08-11T17:13:17Z

- **pkg/controller/jobframework/reconciler.go:126** by [@mimowo](https://github.com/mimowo) — 2023-08-11T17:13:17Z
```diff
@@ -103,14 +104,31 @@ func NewReconciler(
 
 func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Request, job GenericJob) (ctrl.Result, error) {
 	object := job.Object()
-	if err := r.client.Get(ctx, req.NamespacedName, object); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	log := ctrl.LoggerFrom(ctx).WithValues("job", req.String())
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	err := r.client.Get(ctx, req.NamespacedName, object)
+
+	if apierrors.IsNotFound(err) || !object.GetDeletionTimestamp().IsZero() {
```

  I guess, you had a different scenario, that the Job is never started, and the workload is manually deleted, right? I see, in this case the method you propose would not require any extra calls vs. one in the alternative. Still, the scenario seems much less important. Anyway, I will defer to @alculquicondor for the decision.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-11T17:41:03Z

- **pkg/controller/jobframework/reconciler.go:126** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T17:41:03Z
```diff
@@ -103,14 +104,31 @@ func NewReconciler(
 
 func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Request, job GenericJob) (ctrl.Result, error) {
 	object := job.Object()
-	if err := r.client.Get(ctx, req.NamespacedName, object); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	log := ctrl.LoggerFrom(ctx).WithValues("job", req.String())
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	err := r.client.Get(ctx, req.NamespacedName, object)
+
+	if apierrors.IsNotFound(err) || !object.GetDeletionTimestamp().IsZero() {
```

  To me, performance (in this case, less API calls in the happy path) and simplicity of the implementation takes higher priority than covering an edge use case.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-16T10:23:00Z

- **pkg/controller/jobframework/reconciler.go:126** by [@trasc](https://github.com/trasc) — 2023-08-16T10:23:00Z
```diff
@@ -103,14 +104,31 @@ func NewReconciler(
 
 func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Request, job GenericJob) (ctrl.Result, error) {
 	object := job.Object()
-	if err := r.client.Get(ctx, req.NamespacedName, object); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	log := ctrl.LoggerFrom(ctx).WithValues("job", req.String())
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	err := r.client.Get(ctx, req.NamespacedName, object)
+
+	if apierrors.IsNotFound(err) || !object.GetDeletionTimestamp().IsZero() {
```

  The main purpose of this change is to make sure that when a job is unsuspended and its workload gets removed we still get the chance to restore it's node selectors. That is why I choose to "lock" the deletion for the time the job is unsuspended only. (In the previous iterations, before #1005, this approach was also useful in detecting mid stop errors ).
  
  In an ideal life-cycle of a job/workload  (the workload is created once, is admitted and the job finishes) this will have one extra API call then the case when we set the finalizer at the time of creating the workload.  The number of API calls will change in favor of this approach if the workload gets recreated and against is if the workload gets preempted.
  
  However changed so the finalizer is set from the workloads creation until it gets to `Finished`.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-16T15:49:15Z

- **pkg/controller/jobframework/reconciler.go:126** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-16T15:49:14Z
```diff
@@ -103,14 +104,31 @@ func NewReconciler(
 
 func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Request, job GenericJob) (ctrl.Result, error) {
 	object := job.Object()
-	if err := r.client.Get(ctx, req.NamespacedName, object); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	log := ctrl.LoggerFrom(ctx).WithValues("job", req.String())
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	err := r.client.Get(ctx, req.NamespacedName, object)
+
+	if apierrors.IsNotFound(err) || !object.GetDeletionTimestamp().IsZero() {
```

  > this will have one extra API call then the case when we set the finalizer at the time of creating the workload.
  
  Not really. The finalizer can be included in the initial spec sent to create the Workload. This is not only one less API call, but overall less prone to race conditions.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-16T16:17:22Z

- **pkg/controller/jobframework/reconciler.go:121** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-16T15:52:27Z
```diff
@@ -117,14 +118,31 @@ func NewReconciler(
 
 func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Request, job GenericJob) (ctrl.Result, error) {
 	object := job.Object()
-	if err := r.client.Get(ctx, req.NamespacedName, object); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	log := ctrl.LoggerFrom(ctx).WithValues("job", req.String())
```

  can you also include the API? perhaps from `job.GetGVK`?

- **pkg/controller/jobframework/reconciler.go:412** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-16T16:08:20Z
```diff
@@ -373,14 +406,20 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 	// Delete duplicate workload instances.
 	existedWls := 0
 	for i := range toDelete {
-		err := r.client.Delete(ctx, toDelete[i])
+		wlKey := workload.Key(toDelete[i])
+		err := r.removeFinalizer(ctx, toDelete[i])
+		if err != nil && !apierrors.IsNotFound(err) {
+			log.Error(err, "Failed to remove workload finalizer", "wl", wlKey)
```

  we should return here. Otherwise the workload object wouldn't be deleted.

- **pkg/controller/jobframework/reconciler.go:508** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-16T16:14:22Z
```diff
@@ -464,22 +503,26 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object clie
 		if stoppedNow {
 			r.record.Eventf(object, corev1.EventTypeNormal, "Stopped", eventMsg)
 		}
-		return err
-	}
-
-	if job.IsSuspended() {
-		return nil
+		if err != nil {
+			return err
+		}
```

  revert this change to avoid unnecessary indentation.

- **pkg/controller/jobs/job/job_controller_test.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-16T16:16:37Z
  Add new test cases to cover the reconciler changes

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-16T16:19:01Z

- **pkg/controller/jobframework/reconciler.go:126** by [@trasc](https://github.com/trasc) — 2023-08-16T16:19:01Z
```diff
@@ -103,14 +104,31 @@ func NewReconciler(
 
 func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Request, job GenericJob) (ctrl.Result, error) {
 	object := job.Object()
-	if err := r.client.Get(ctx, req.NamespacedName, object); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	log := ctrl.LoggerFrom(ctx).WithValues("job", req.String())
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	err := r.client.Get(ctx, req.NamespacedName, object)
+
+	if apierrors.IsNotFound(err) || !object.GetDeletionTimestamp().IsZero() {
```

  > Not really. The finalizer can be included in the initial spec sent to create the Workload. This is not only one less API call, but overall less prone to race conditions.
  
  That is the one I'm talking about, the removal needs to be done in a different call. (if it gets to finish, or if it gets deleted before that).

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-16T17:01:05Z

- **pkg/controller/jobframework/reconciler.go:126** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-16T17:01:04Z
```diff
@@ -103,14 +104,31 @@ func NewReconciler(
 
 func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Request, job GenericJob) (ctrl.Result, error) {
 	object := job.Object()
-	if err := r.client.Get(ctx, req.NamespacedName, object); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	log := ctrl.LoggerFrom(ctx).WithValues("job", req.String())
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	err := r.client.Get(ctx, req.NamespacedName, object)
+
+	if apierrors.IsNotFound(err) || !object.GetDeletionTimestamp().IsZero() {
```

  Right. The point is that in this approach we no longer need an extra API call when admitting a workload.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-16T17:20:29Z

- **pkg/controller/jobframework/reconciler.go:126** by [@trasc](https://github.com/trasc) — 2023-08-16T17:20:29Z
```diff
@@ -103,14 +104,31 @@ func NewReconciler(
 
 func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Request, job GenericJob) (ctrl.Result, error) {
 	object := job.Object()
-	if err := r.client.Get(ctx, req.NamespacedName, object); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	log := ctrl.LoggerFrom(ctx).WithValues("job", req.String())
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	err := r.client.Get(ctx, req.NamespacedName, object)
+
+	if apierrors.IsNotFound(err) || !object.GetDeletionTimestamp().IsZero() {
```

  But need one if/when the workload gets deleted before being admitted (if the job spec changes). anyway....

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-17T08:19:12Z

- **pkg/controller/jobframework/reconciler.go:121** by [@trasc](https://github.com/trasc) — 2023-08-17T06:45:08Z
```diff
@@ -117,14 +118,31 @@ func NewReconciler(
 
 func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Request, job GenericJob) (ctrl.Result, error) {
 	object := job.Object()
-	if err := r.client.Get(ctx, req.NamespacedName, object); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	log := ctrl.LoggerFrom(ctx).WithValues("job", req.String())
```

  done

- **pkg/controller/jobframework/reconciler.go:412** by [@trasc](https://github.com/trasc) — 2023-08-17T06:52:12Z
```diff
@@ -373,14 +406,20 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 	// Delete duplicate workload instances.
 	existedWls := 0
 	for i := range toDelete {
-		err := r.client.Delete(ctx, toDelete[i])
+		wlKey := workload.Key(toDelete[i])
+		err := r.removeFinalizer(ctx, toDelete[i])
+		if err != nil && !apierrors.IsNotFound(err) {
+			log.Error(err, "Failed to remove workload finalizer", "wl", wlKey)
```

  done

- **pkg/controller/jobs/job/job_controller_test.go:1** by [@trasc](https://github.com/trasc) — 2023-08-17T08:05:34Z
  done, for removal on finished, the removal on workload and job deletion is done in integration.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-17T08:19:51Z

- **pkg/controller/jobframework/reconciler.go:508** by [@trasc](https://github.com/trasc) — 2023-08-17T08:19:51Z
```diff
@@ -464,22 +503,26 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object clie
 		if stoppedNow {
 			r.record.Eventf(object, corev1.EventTypeNormal, "Stopped", eventMsg)
 		}
-		return err
-	}
-
-	if job.IsSuspended() {
-		return nil
+		if err != nil {
+			return err
+		}
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-17T16:43:50Z

- **test/integration/controller/job/job_controller_test.go:892** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-17T16:39:03Z
```diff
@@ -875,15 +876,30 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 			return createdProdJob.Spec.Suspend
 		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
 
+		originalSelector := maps.Clone(createdProdJob.Spec.Template.Spec.NodeSelector)
+
 		ginkgo.By("creating another localQueue of the same name and in the same namespace as the job")
 		prodLocalQ = testing.MakeLocalQueue(localQueue.Name, ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
 		gomega.Expect(k8sClient.Create(ctx, prodLocalQ)).Should(gomega.Succeed())
 
-		ginkgo.By("job should be unsuspended")
+		ginkgo.By("job should be unsuspended and NodeSelector properly set")
 		gomega.Eventually(func() *bool {
 			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdProdJob)).Should(gomega.Succeed())
 			return createdProdJob.Spec.Suspend
 		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+
+		runningSelector := maps.Clone(createdProdJob.Spec.Template.Spec.NodeSelector)
+		gomega.Expect(runningSelector).NotTo(gomega.Equal(originalSelector))
```

  I would just remove this check, as it's redundant given the one below.

- **test/integration/controller/job/job_controller_test.go:901** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-17T16:40:26Z
```diff
@@ -875,15 +876,30 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 			return createdProdJob.Spec.Suspend
 		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
 
+		originalSelector := maps.Clone(createdProdJob.Spec.Template.Spec.NodeSelector)
+
 		ginkgo.By("creating another localQueue of the same name and in the same namespace as the job")
 		prodLocalQ = testing.MakeLocalQueue(localQueue.Name, ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
 		gomega.Expect(k8sClient.Create(ctx, prodLocalQ)).Should(gomega.Succeed())
 
-		ginkgo.By("job should be unsuspended")
+		ginkgo.By("job should be unsuspended and NodeSelector properly set")
 		gomega.Eventually(func() *bool {
 			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdProdJob)).Should(gomega.Succeed())
 			return createdProdJob.Spec.Suspend
 		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+
+		runningSelector := maps.Clone(createdProdJob.Spec.Template.Spec.NodeSelector)
+		gomega.Expect(runningSelector).NotTo(gomega.Equal(originalSelector))
+
+		expectedSelector := maps.Clone(originalSelector)
+		if expectedSelector == nil {
+			expectedSelector = onDemandFlavor.Spec.NodeLabels
+		} else {
+			for k, v := range onDemandFlavor.Spec.NodeLabels {
+				expectedSelector[k] = v
+			}
+		}
```

  avoid generating expected values in tests. hard-coding it is more readable https://enterprisecraftsmanship.com/posts/dry-damp-unit-tests/

- **test/integration/controller/job/job_controller_test.go:1065** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-17T16:43:46Z
```diff
@@ -945,6 +961,123 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 		})
 	})
 
+	ginkgo.When("The workload is deleted while admitted", func() {
+		ginkgo.It("Should restore the original node selectors", func() {
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			job := testingjob.MakeJob(jobName, ns.Name).Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Suspend(false).Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &batchv1.Job{}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To[bool](true)))
+			})
+
+			// backup the podSet's node selector
+			originalNodeSelector := createdJob.Spec.Template.Spec.NodeSelector
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To[bool](false)))
+			})
+
+			ginkgo.By("the node selector should be updated", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).ShouldNot(gomega.Equal(originalNodeSelector))
+			})
+
+			ginkgo.By("delete the localQueue to prevent readmission", func() {
+				gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("deleting the workload", func() {
+				wl := &kueue.Workload{}
+				wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name), Namespace: job.Namespace}
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				gomega.Expect(k8sClient.Delete(ctx, wl)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("the node selector should be restored", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).Should(gomega.Equal(originalNodeSelector))
+			})
+		})
+	})
+
+	ginkgo.When("The job is deleted while admitted", func() {
+		ginkgo.It("Its workload should also be deleted", func() {
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			job := testingjob.MakeJob(jobName, ns.Name).Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Suspend(false).Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &batchv1.Job{}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To[bool](true)))
+			})
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To[bool](false)))
+			})
+
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name), Namespace: job.Namespace}
+			ginkgo.By("checking the finalizer is set", func() {
+				gomega.Eventually(func() []string {
+					wl := &kueue.Workload{}
+					gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+					return wl.Finalizers
+				}, util.Timeout, util.Interval).Should(gomega.ContainElement(kueue.ResourceInUseFinalizerName))
+			})
+
+			ginkgo.By("deleting the job", func() {
+				gomega.Expect(k8sClient.Delete(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("checking the workload is removed or the finalizer is no longer set", func() {
```

  ```suggestion
  			ginkgo.By("checking the workload is removed", func() {
  ```
  
  The finalizer is an implementation detail. The Eventually should check that the Workload is removed.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-18T05:12:39Z

- **test/integration/controller/job/job_controller_test.go:892** by [@trasc](https://github.com/trasc) — 2023-08-18T05:03:12Z
```diff
@@ -875,15 +876,30 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 			return createdProdJob.Spec.Suspend
 		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
 
+		originalSelector := maps.Clone(createdProdJob.Spec.Template.Spec.NodeSelector)
+
 		ginkgo.By("creating another localQueue of the same name and in the same namespace as the job")
 		prodLocalQ = testing.MakeLocalQueue(localQueue.Name, ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
 		gomega.Expect(k8sClient.Create(ctx, prodLocalQ)).Should(gomega.Succeed())
 
-		ginkgo.By("job should be unsuspended")
+		ginkgo.By("job should be unsuspended and NodeSelector properly set")
 		gomega.Eventually(func() *bool {
 			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdProdJob)).Should(gomega.Succeed())
 			return createdProdJob.Spec.Suspend
 		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+
+		runningSelector := maps.Clone(createdProdJob.Spec.Template.Spec.NodeSelector)
+		gomega.Expect(runningSelector).NotTo(gomega.Equal(originalSelector))
```

  done

- **test/integration/controller/job/job_controller_test.go:901** by [@trasc](https://github.com/trasc) — 2023-08-18T05:03:14Z
```diff
@@ -875,15 +876,30 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 			return createdProdJob.Spec.Suspend
 		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
 
+		originalSelector := maps.Clone(createdProdJob.Spec.Template.Spec.NodeSelector)
+
 		ginkgo.By("creating another localQueue of the same name and in the same namespace as the job")
 		prodLocalQ = testing.MakeLocalQueue(localQueue.Name, ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
 		gomega.Expect(k8sClient.Create(ctx, prodLocalQ)).Should(gomega.Succeed())
 
-		ginkgo.By("job should be unsuspended")
+		ginkgo.By("job should be unsuspended and NodeSelector properly set")
 		gomega.Eventually(func() *bool {
 			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdProdJob)).Should(gomega.Succeed())
 			return createdProdJob.Spec.Suspend
 		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+
+		runningSelector := maps.Clone(createdProdJob.Spec.Template.Spec.NodeSelector)
+		gomega.Expect(runningSelector).NotTo(gomega.Equal(originalSelector))
+
+		expectedSelector := maps.Clone(originalSelector)
+		if expectedSelector == nil {
+			expectedSelector = onDemandFlavor.Spec.NodeLabels
+		} else {
+			for k, v := range onDemandFlavor.Spec.NodeLabels {
+				expectedSelector[k] = v
+			}
+		}
```

  done

- **test/integration/controller/job/job_controller_test.go:1065** by [@trasc](https://github.com/trasc) — 2023-08-18T05:10:22Z
```diff
@@ -945,6 +961,123 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 		})
 	})
 
+	ginkgo.When("The workload is deleted while admitted", func() {
+		ginkgo.It("Should restore the original node selectors", func() {
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			job := testingjob.MakeJob(jobName, ns.Name).Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Suspend(false).Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &batchv1.Job{}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To[bool](true)))
+			})
+
+			// backup the podSet's node selector
+			originalNodeSelector := createdJob.Spec.Template.Spec.NodeSelector
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To[bool](false)))
+			})
+
+			ginkgo.By("the node selector should be updated", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).ShouldNot(gomega.Equal(originalNodeSelector))
+			})
+
+			ginkgo.By("delete the localQueue to prevent readmission", func() {
+				gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("deleting the workload", func() {
+				wl := &kueue.Workload{}
+				wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name), Namespace: job.Namespace}
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				gomega.Expect(k8sClient.Delete(ctx, wl)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("the node selector should be restored", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).Should(gomega.Equal(originalNodeSelector))
+			})
+		})
+	})
+
+	ginkgo.When("The job is deleted while admitted", func() {
+		ginkgo.It("Its workload should also be deleted", func() {
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			job := testingjob.MakeJob(jobName, ns.Name).Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Suspend(false).Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &batchv1.Job{}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To[bool](true)))
+			})
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To[bool](false)))
+			})
+
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name), Namespace: job.Namespace}
+			ginkgo.By("checking the finalizer is set", func() {
+				gomega.Eventually(func() []string {
+					wl := &kueue.Workload{}
+					gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+					return wl.Finalizers
+				}, util.Timeout, util.Interval).Should(gomega.ContainElement(kueue.ResourceInUseFinalizerName))
+			})
+
+			ginkgo.By("deleting the job", func() {
+				gomega.Expect(k8sClient.Delete(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("checking the workload is removed or the finalizer is no longer set", func() {
```

  In fact I think we should focus on the finalizer being removed, since the deletion of the workload may or may not be trigger based on the `DeletePropagationPolicy`.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-18T13:00:29Z

- **test/integration/controller/job/job_controller_test.go:1065** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-18T13:00:28Z
```diff
@@ -945,6 +961,123 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 		})
 	})
 
+	ginkgo.When("The workload is deleted while admitted", func() {
+		ginkgo.It("Should restore the original node selectors", func() {
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			job := testingjob.MakeJob(jobName, ns.Name).Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Suspend(false).Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &batchv1.Job{}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To[bool](true)))
+			})
+
+			// backup the podSet's node selector
+			originalNodeSelector := createdJob.Spec.Template.Spec.NodeSelector
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To[bool](false)))
+			})
+
+			ginkgo.By("the node selector should be updated", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).ShouldNot(gomega.Equal(originalNodeSelector))
+			})
+
+			ginkgo.By("delete the localQueue to prevent readmission", func() {
+				gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("deleting the workload", func() {
+				wl := &kueue.Workload{}
+				wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name), Namespace: job.Namespace}
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				gomega.Expect(k8sClient.Delete(ctx, wl)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("the node selector should be restored", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).Should(gomega.Equal(originalNodeSelector))
+			})
+		})
+	})
+
+	ginkgo.When("The job is deleted while admitted", func() {
+		ginkgo.It("Its workload should also be deleted", func() {
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			job := testingjob.MakeJob(jobName, ns.Name).Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Suspend(false).Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &batchv1.Job{}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To[bool](true)))
+			})
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To[bool](false)))
+			})
+
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name), Namespace: job.Namespace}
+			ginkgo.By("checking the finalizer is set", func() {
+				gomega.Eventually(func() []string {
+					wl := &kueue.Workload{}
+					gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+					return wl.Finalizers
+				}, util.Timeout, util.Interval).Should(gomega.ContainElement(kueue.ResourceInUseFinalizerName))
+			})
+
+			ginkgo.By("deleting the job", func() {
+				gomega.Expect(k8sClient.Delete(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("checking the workload is removed or the finalizer is no longer set", func() {
```

  ah right, the default propagation policy for Job is orphan.
