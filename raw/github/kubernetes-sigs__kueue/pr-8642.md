# PR #8642: keps: Update information regarding TAS + ElasticJobsViaWorkloadSlices

**Summary**: keps: Update information regarding TAS + ElasticJobsViaWorkloadSlices

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8642

**Last updated**: 2026-02-09T21:26:01Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@sohankunkerkar](https://github.com/sohankunkerkar)
- **Branch**: `tas-elastic-kep` → `main`
- **Created**: 2026-01-16T20:21:30Z
- **Updated**: 2026-02-09T21:26:01Z
- **Merged**: 2026-02-09T21:25:59Z
- **Closed**: 2026-02-09T21:25:59Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@kannon92](https://github.com/kannon92), [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@pajakd](https://github.com/pajakd)
- **Changed files**: 4   **+84 / -7**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Related to https://github.com/kubernetes-sigs/kueue/issues/8160

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
None
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-01-16T20:21:36Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 21a52334b211be6b37e947c6e1f76c0f6ee00ff5 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6989f684df2c7e0008a5f4a2 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-8642--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-8642--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTg2NDItLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.BkM2jOODZpoJorkTJGSMx3r5wFM_2yZq7HiAiLpOtqM)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-8642--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-16T20:24:12Z

You should be listed in https://github.com/sohankunkerkar/kueue/blob/3b982f49a072bb88ae125448add28eec902e8b58/keps/77-dynamically-sized-jobs/kep.yaml#L3-L5

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-22T02:26:04Z

LGTM label has been added.  <details>Git tree hash: f66e1149ccadf7fda61fc7524d95e6802c9f3d0f</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-03T16:53:07Z

LGTM, yes there are open questions about the "preferred" and "required" modes, I think we can update the design as we go about supporting them

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-03T16:54:16Z

I think there are only nits form me, let me know @tenzen-y if you see some major points which require more work here.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-06T12:12:40Z

LGTM, just minor comments to make things more explicit.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-09T15:46:32Z

LGTM label has been added.  <details>Git tree hash: d5f557244af1e2143c4a5e2775ce18979c84918f</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-09T18:43:46Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8642#pullrequestreview-3774070511" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8642#" title="Author self-approved">sohankunkerkar</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8642#pullrequestreview-3774799648" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-09T21:05:05Z

/test pull-kueue-verify-main

infra error

## Reviews

### Review by [@copilot-pull-request-reviewer[bot]](https://github.com/apps/copilot-pull-request-reviewer) (commented) — 2026-01-16T20:24:04Z

## Pull request overview

This PR documents the integration between Topology Aware Scheduling (TAS) and ElasticJobsViaWorkloadSlices features in Kueue. It adds bidirectional cross-references between KEP-77 (Dynamically Sized Jobs) and KEP-2724 (Topology Aware Scheduling), along with detailed documentation of how these features work together.

**Changes:**
- Added cross-references in both KEP YAML files linking KEP-77 and KEP-2724
- Documented TAS integration in KEP-77, including pod identification across slices and topology assignment preservation
- Added a reference section in KEP-2724 pointing to the detailed documentation in KEP-77
- Updated table of contents in both KEPs to reflect the new sections

### Reviewed changes

Copilot reviewed 4 out of 4 changed files in this pull request and generated no comments.

| File | Description |
| ---- | ----------- |
| keps/77-dynamically-sized-jobs/kep.yaml | Added see-also reference to KEP-2724 |
| keps/77-dynamically-sized-jobs/README.md | Added comprehensive "Topology Aware Scheduling Integration" section with implementation details and graduation criteria |
| keps/2724-topology-aware-scheduling/kep.yaml | Added see-also reference to KEP-77 |
| keps/2724-topology-aware-scheduling/README.md | Added "Support for Elastic Workloads" section with cross-reference to KEP-77 |


<details>
<summary>Comments suppressed due to low confidence (2)</summary>

**keps/2724-topology-aware-scheduling/README.md:1373**
* Corrected spelling of 'implemetation' to 'implementation' and 'consequtive' to 'consecutive'.
```
In the first implemetation, we propose to perform the balanced algorithm only on two consequtive levels indicated
```
**keps/2724-topology-aware-scheduling/README.md:1373**
* Corrected spelling of 'implemetation' to 'implementation' and 'consequtive' to 'consecutive'.
```
In the first implemetation, we propose to perform the balanced algorithm only on two consequtive levels indicated
```
</details>



---

💡 <a href="/kubernetes-sigs/kueue/new/main/.github/instructions?filename=*.instructions.md" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Add Copilot custom instructions</a> for smarter, more guided reviews. <a href="https://docs.github.com/en/copilot/customizing-copilot/adding-repository-custom-instructions-for-github-copilot" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Learn how to get started</a>.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-01-22T02:25:57Z

/lgtm

/assign @tenzen-y @mimowo

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-22T19:49:28Z

- **keps/2724-topology-aware-scheduling/kep.yaml:28** by [@mimowo](https://github.com/mimowo) — 2026-01-22T19:49:28Z
  Please update that to 0.16

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-22T19:49:51Z

- **keps/77-dynamically-sized-jobs/kep.yaml:26** by [@mimowo](https://github.com/mimowo) — 2026-01-22T19:49:51Z
  Let's also add `latest-milestone: "v0.16"`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-22T19:56:11Z

- **keps/77-dynamically-sized-jobs/README.md:315** by [@mimowo](https://github.com/mimowo) — 2026-01-22T19:56:11Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.16), only **unconstrained** topology mode is supported for scale-up
+operations. Required and preferred topology modes are not yet supported and will be rejected
+during scheduling. Support for scale-down topology preservation and other topology modes (required,
+preferred) will be added in future iterations. If the previous assignment is stale (e.g., node
+failure), a new assignment is calculated.
+
+#### Pod Identification Across Slices
+
+The `kueue.x-k8s.io/workload` annotation changes with each workload slice, which prevents
+TAS from finding all pods belonging to a job across slice transitions. To address this,
+a `kueue.x-k8s.io/workload-slice-name` annotation is added to pods when both features are
+enabled. This annotation contains the origin workload name in the slice chain (following
+`WorkloadSliceReplacementFor` annotations to find the root) and remains stable across slice
+transitions, allowing `TopologyUngater` to find all pods belonging to the job. The namespace
+scope in pod lookups ensures uniqueness across different namespaces.
+
+```golang
+const (
+    // WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+    WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
+)
+```
+
+#### Scale Up
+
+When a new workload slice replaces an old one during scale-up, the scheduler extracts
+the `TopologyAssignment` from the old slice and passes it to TAS. TAS attempts to place
+new pods in the same topology domain as existing pods. If the previous assignment is
+stale (e.g., nodes are unhealthy), TAS falls back to normal placement.
```

  What is "normal placement" in this context? I would say we just don't scale while the assignment has unhealthy nodes. However, I think we may just decide whatever is simpler for alpha.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-23T17:25:04Z

- **keps/77-dynamically-sized-jobs/README.md:315** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-23T17:25:04Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.16), only **unconstrained** topology mode is supported for scale-up
+operations. Required and preferred topology modes are not yet supported and will be rejected
+during scheduling. Support for scale-down topology preservation and other topology modes (required,
+preferred) will be added in future iterations. If the previous assignment is stale (e.g., node
+failure), a new assignment is calculated.
+
+#### Pod Identification Across Slices
+
+The `kueue.x-k8s.io/workload` annotation changes with each workload slice, which prevents
+TAS from finding all pods belonging to a job across slice transitions. To address this,
+a `kueue.x-k8s.io/workload-slice-name` annotation is added to pods when both features are
+enabled. This annotation contains the origin workload name in the slice chain (following
+`WorkloadSliceReplacementFor` annotations to find the root) and remains stable across slice
+transitions, allowing `TopologyUngater` to find all pods belonging to the job. The namespace
+scope in pod lookups ensures uniqueness across different namespaces.
+
+```golang
+const (
+    // WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+    WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
+)
+```
+
+#### Scale Up
+
+When a new workload slice replaces an old one during scale-up, the scheduler extracts
+the `TopologyAssignment` from the old slice and passes it to TAS. TAS attempts to place
+new pods in the same topology domain as existing pods. If the previous assignment is
+stale (e.g., nodes are unhealthy), TAS falls back to normal placement.
```

  I made the change. Let me know how does it feel. We can revisit this when we transition this feature to beta

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-01-23T17:38:13Z

- **keps/77-dynamically-sized-jobs/README.md:289** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-23T17:28:36Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.16), only **unconstrained** topology mode is supported for scale-up
+operations. Required and preferred topology modes are not yet supported and will be rejected
+during scheduling. Support for scale-down topology preservation and other topology modes (required,
```

  Rather than rejecting during scheduling, could we reject the scaling request when users scale against the workload with the required and preferred topology?

- **keps/77-dynamically-sized-jobs/README.md:310** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-23T17:35:39Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.16), only **unconstrained** topology mode is supported for scale-up
+operations. Required and preferred topology modes are not yet supported and will be rejected
+during scheduling. Support for scale-down topology preservation and other topology modes (required,
+preferred) will be added in future iterations. If the previous assignment is stale (e.g., node
+failure), a new assignment is calculated.
+
+#### Pod Identification Across Slices
+
+The `kueue.x-k8s.io/workload` annotation changes with each workload slice, which prevents
+TAS from finding all pods belonging to a job across slice transitions. To address this,
+a `kueue.x-k8s.io/workload-slice-name` annotation is added to pods when both features are
+enabled. This annotation contains the origin workload name in the slice chain (following
+`WorkloadSliceReplacementFor` annotations to find the root) and remains stable across slice
+transitions, allowing `TopologyUngater` to find all pods belonging to the job. The namespace
+scope in pod lookups ensures uniqueness across different namespaces.
+
+```golang
+const (
+    // WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+    WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
+)
+```
```

  These are one of my primary concerns to graduate workload slice to beta and GA.
  This feature conceptually has a risk of adding more and more annotations to track something when we support Workload Slice × X features. 
  
  The root cause is that this feature doesn't have a specific field and Resources.
  
  But we can discuss the Workload Slice graduation separate from this KEP.

- **keps/77-dynamically-sized-jobs/README.md:282** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-23T17:38:11Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
```

  What if scaling up request is issued when the node is repairing?
  Kueue TAS tracks unhealthyNode and waits for a while until the unhealthyNode is alive, then Kueue tries to reschedule the Pod on unhealthyNodes to a new healthy node.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-23T17:44:29Z

- **keps/77-dynamically-sized-jobs/README.md:310** by [@mimowo](https://github.com/mimowo) — 2026-01-23T17:44:29Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.16), only **unconstrained** topology mode is supported for scale-up
+operations. Required and preferred topology modes are not yet supported and will be rejected
+during scheduling. Support for scale-down topology preservation and other topology modes (required,
+preferred) will be added in future iterations. If the previous assignment is stale (e.g., node
+failure), a new assignment is calculated.
+
+#### Pod Identification Across Slices
+
+The `kueue.x-k8s.io/workload` annotation changes with each workload slice, which prevents
+TAS from finding all pods belonging to a job across slice transitions. To address this,
+a `kueue.x-k8s.io/workload-slice-name` annotation is added to pods when both features are
+enabled. This annotation contains the origin workload name in the slice chain (following
+`WorkloadSliceReplacementFor` annotations to find the root) and remains stable across slice
+transitions, allowing `TopologyUngater` to find all pods belonging to the job. The namespace
+scope in pod lookups ensures uniqueness across different namespaces.
+
+```golang
+const (
+    // WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+    WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
+)
+```
```

  Indeed, but this annotation is needed for the ElasticJobsViaWorkloadSlices feature as designed. 
  
  The alternative design of Workload Resize field is very demanding too. I dont think I will be able to work on it in Q1 or Q2, so I think we can still continue making progress for ElasticJobsViaWorkloadSlices.
  
  We generally observe signifficant interest in Kueue supporting elastic workloads, so we may need to reach beta with ElasticJobsViaWorkloadSlices.
  
  My hope is that if we do WorkloadResizeRequest once day we will be able to deprecate ElasticJobsViaWorkloadSlices, because from user perspective it should behave the same.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-23T17:48:07Z

- **keps/77-dynamically-sized-jobs/README.md:282** by [@mimowo](https://github.com/mimowo) — 2026-01-23T17:48:07Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
```

  I think for the first iteration it is fair to just wait for the repairing to complete. We will certainly need to revisit this topic in the future. I would add it to graduation criteria for Beta.
  
  Maybe we introduce a dedicated feature gate like ElasticWorkloadsForTAS. This would allow us to incubate ElasticWorkloadsForTAS in Alpha for long, and graduate just ElasticJobsViaWorkloadSlices

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-23T17:58:56Z

- **keps/77-dynamically-sized-jobs/README.md:289** by [@mimowo](https://github.com/mimowo) — 2026-01-23T17:58:55Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.16), only **unconstrained** topology mode is supported for scale-up
+operations. Required and preferred topology modes are not yet supported and will be rejected
+during scheduling. Support for scale-down topology preservation and other topology modes (required,
```

  I would rather reject early even creation of ElasticJobs using TAS preferred or required. We will relax that as we start supporting it. wdyt? We could just reject them in webhooks if TAS preferred/required annoattion is used on PodSet, and elastic-job annotation on the Job metadata.
  
  It will make validation much simpler, than late validation on resize. While the feature is alpha we have a freedom to start with strong validation and gradually relax.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-01-23T18:08:34Z

- **keps/77-dynamically-sized-jobs/README.md:289** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-23T18:08:34Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.16), only **unconstrained** topology mode is supported for scale-up
+operations. Required and preferred topology modes are not yet supported and will be rejected
+during scheduling. Support for scale-down topology preservation and other topology modes (required,
```

  Thank you for expanding my request. I totally agree with you.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-01-23T18:11:05Z

- **keps/77-dynamically-sized-jobs/README.md:310** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-23T18:11:05Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.16), only **unconstrained** topology mode is supported for scale-up
+operations. Required and preferred topology modes are not yet supported and will be rejected
+during scheduling. Support for scale-down topology preservation and other topology modes (required,
+preferred) will be added in future iterations. If the previous assignment is stale (e.g., node
+failure), a new assignment is calculated.
+
+#### Pod Identification Across Slices
+
+The `kueue.x-k8s.io/workload` annotation changes with each workload slice, which prevents
+TAS from finding all pods belonging to a job across slice transitions. To address this,
+a `kueue.x-k8s.io/workload-slice-name` annotation is added to pods when both features are
+enabled. This annotation contains the origin workload name in the slice chain (following
+`WorkloadSliceReplacementFor` annotations to find the root) and remains stable across slice
+transitions, allowing `TopologyUngater` to find all pods belonging to the job. The namespace
+scope in pod lookups ensures uniqueness across different namespaces.
+
+```golang
+const (
+    // WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+    WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
+)
+```
```

  I don't say to prevent introducing this annotation.
  I understand this annotation is needed to ElasticJob via WorkloadSlice.
  
  So, as I mentioned, we should revisit how to standardize Elastic Workload support in Kueue, separate from this KEP.
  I understand that many users are interested in elastic workloads support, and the We kueue community has a limited development capacity.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-01-23T18:13:18Z

- **keps/77-dynamically-sized-jobs/README.md:282** by [@tenzen-y](https://github.com/tenzen-y) — 2026-01-23T18:13:18Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
```

  That sounds better. 
  @sohankunkerkar, could you expand this KEP to align with our suggestion?
  
  
  SIDE NOTE: I will check this KEP more carefully after v0.16.0 release (next Monday) if we have other missing problems with feature combinations.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-23T18:23:15Z

- **keps/77-dynamically-sized-jobs/README.md:282** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-23T18:23:14Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
```

   Thanks for the feedback! Before updating, I want to confirm I understood correctly:                                                                                        
                                                                                                                                                                               
    1. Webhook validation for Alpha: Reject job creation when both `kueue.x-k8s.io/elastic-job: "true"` and TAS preferred/required annotations are present on the same job. Is this the right validation point, or should it be at workload creation time?                                                                                                
    2. Node repair handling: For Alpha, we block scale-up while the previous TAS assignment has unhealthy nodes (already documented). Should I add to Beta criteria something like `Support graceful handling of scale-up when nodes are repairing (e.g., reschedule to healthy nodes)`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-03T16:48:41Z

- **keps/2724-topology-aware-scheduling/kep.yaml:28** by [@mimowo](https://github.com/mimowo) — 2026-02-03T16:48:41Z
```diff
@@ -17,14 +17,15 @@ approvers:
 
 see-also:
   - "/keps/1136-provisioning-request-support"
+  - "/keps/77-dynamically-sized-jobs"
 
 # The target maturity stage in the current dev cycle for this KEP.
 stage: beta
 
 # The most recent milestone for which work toward delivery of this KEP has been
 # done. This can be the current (upcoming) milestone, if it is being actively
 # worked on.
-latest-milestone: "v0.15"
+latest-milestone: "v0.16"
```

  I guess we need to bump to 0.17, unless you have some need to cherrypick that @sohankunkerkar ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-03T16:49:07Z

- **keps/77-dynamically-sized-jobs/kep.yaml:22** by [@mimowo](https://github.com/mimowo) — 2026-02-03T16:49:07Z
```diff
@@ -18,9 +19,16 @@ approvers:
 # The target maturity stage in the current dev cycle for this KEP.
 stage: alpha
 
+# The most recent milestone for which work toward delivery of this KEP has been
```

  Let's also here bump to 0.17

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-03T16:49:46Z

- **keps/77-dynamically-sized-jobs/README.md:287** by [@mimowo](https://github.com/mimowo) — 2026-02-03T16:49:46Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.16), only **unconstrained** topology mode is supported for scale-up
```

  ```suggestion
  For the initial iteration, only **unconstrained** topology mode is supported for scale-up
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-03T16:51:00Z

- **keps/77-dynamically-sized-jobs/README.md:312** by [@mimowo](https://github.com/mimowo) — 2026-02-03T16:51:00Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.16), only **unconstrained** topology mode is supported for scale-up
+operations. Jobs with `kueue.x-k8s.io/elastic-job: "true"` that also specify required or preferred
+topology annotations are rejected at job creation via webhook validation. Support for scale-down
+and required/preferred modes will be added in future iterations. If the previous assignment is
+stale (e.g., node failure), a new assignment is calculated.
+
+#### Pod Identification Across Slices
+
+The `kueue.x-k8s.io/workload` annotation changes with each workload slice, which prevents
+TAS from finding all pods belonging to a job across slice transitions. To address this,
+a `kueue.x-k8s.io/workload-slice-name` annotation is added to pods when both features are
+enabled. This annotation contains the origin workload name in the slice chain (following
+`WorkloadSliceReplacementFor` annotations to find the root) and remains stable across slice
+transitions, allowing `TopologyUngater` to find all pods belonging to the job. The namespace
+scope in pod lookups ensures uniqueness across different namespaces.
+
+```golang
+const (
+    // WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+    WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
+)
+```
+
+#### Scale Up
```

  Please also add a section for Scale Down

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-05T15:44:40Z

- **keps/77-dynamically-sized-jobs/README.md:287** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-05T15:44:40Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.16), only **unconstrained** topology mode is supported for scale-up
```

  ```suggestion
  For the initial iteration (v0.17), only **unconstrained** topology mode is supported for scale-up
  ```
  
  I think that this is better than removal of the version.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-05T15:55:05Z

- **keps/77-dynamically-sized-jobs/README.md:282** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-05T15:55:05Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
```

  > Webhook validation for Alpha: Reject job creation when both kueue.x-k8s.io/elastic-job: "true" and TAS preferred/required annotations are present on the same job. Is this the right validation point, or should it be at workload creation time?
  
  Yes, that is my understanding.
  
  > Node repair handling: For Alpha, we block scale-up while the previous TAS assignment has unhealthy nodes (already documented). Should I add to Beta criteria something like Support graceful handling of scale-up when nodes are repairing (e.g., reschedule to healthy nodes)?
  
  Yes, we should block scale-up request under the situation in the fist iteration.
  Additionally, we should block scale-down requests as well in the first iteration because scaling down the workload with a repairing state could cause violating the expected minimum required replicas.
  
  Let's say the Job has 3 replicas and a minimum required number of replicas. After a while, one of the nodes was down, which means active replicas 2, while the TAS Node Repair controller doesn't behave in topology reassignment. In this situation, if the user changes the replicas to 2, the active replicas will be 1.
  
  I understand that scale-down is out of scope in the first iteration, but we should clarify that we should not allow scaling operations both in scale-up and scale-down, and those should be mentioned in Beta graduation criteria.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-05T15:59:18Z

- **keps/2724-topology-aware-scheduling/kep.yaml:1** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-05T15:59:18Z
  @sohankunkerkar @mimowo As we discussed, what about a new separated FeatureGate?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-05T16:04:21Z

- **keps/77-dynamically-sized-jobs/README.md:295** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-05T16:04:13Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.16), only **unconstrained** topology mode is supported for scale-up
+operations. Jobs with `kueue.x-k8s.io/elastic-job: "true"` that also specify required or preferred
+topology annotations are rejected at job creation via webhook validation. Support for scale-down
+and required/preferred modes will be added in future iterations. If the previous assignment is
+stale (e.g., node failure), a new assignment is calculated.
+
+#### Pod Identification Across Slices
```

  What does "Pod Integration" mean?
  Does this indicate expanding batch/v1 Job, RayCluster, RayJob integrations?
  
  If this indicates to "Pod Integration?, why do we need to expand the "Pod Integration" because ElasicJob supports only Job and RayCluster, IIUC, isn't it?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-05T16:38:35Z

- **keps/2724-topology-aware-scheduling/kep.yaml:1** by [@mimowo](https://github.com/mimowo) — 2026-02-05T16:38:35Z
  +1, then we could graduate ElasticJobs (or not) independently

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-05T16:38:44Z

- **keps/77-dynamically-sized-jobs/README.md:282** by [@mimowo](https://github.com/mimowo) — 2026-02-05T16:38:44Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
```

  > I understand that scale-down is out of scope in the first iteration, but we should clarify that we should not allow scaling operations both in scale-up and scale-down, and those should be mentioned in Beta graduation criteria.
  
  I think  the initial PR already delivers scale down actually. I would like to move forward with that, but we can add this for criteria to Beta. Then we can deliver it in first alpha or second alpha.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-05T17:25:53Z

- **keps/2724-topology-aware-scheduling/kep.yaml:1** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-05T17:25:53Z
  I think this will be a follow up, right?

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-05T17:31:29Z

- **keps/77-dynamically-sized-jobs/README.md:295** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-05T17:31:29Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.16), only **unconstrained** topology mode is supported for scale-up
+operations. Jobs with `kueue.x-k8s.io/elastic-job: "true"` that also specify required or preferred
+topology annotations are rejected at job creation via webhook validation. Support for scale-down
+and required/preferred modes will be added in future iterations. If the previous assignment is
+stale (e.g., node failure), a new assignment is calculated.
+
+#### Pod Identification Across Slices
```

  Not sure if I understood your question. The section actually talks about `Pod Identification Across Slices`, which explains how TAS identifies pods across slice transitions.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-05T17:31:50Z

- **keps/2724-topology-aware-scheduling/kep.yaml:28** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-05T17:31:50Z
```diff
@@ -17,14 +17,15 @@ approvers:
 
 see-also:
   - "/keps/1136-provisioning-request-support"
+  - "/keps/77-dynamically-sized-jobs"
 
 # The target maturity stage in the current dev cycle for this KEP.
 stage: beta
 
 # The most recent milestone for which work toward delivery of this KEP has been
 # done. This can be the current (upcoming) milestone, if it is being actively
 # worked on.
-latest-milestone: "v0.15"
+latest-milestone: "v0.16"
```

  I'm okay with 0.17

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-05T17:32:56Z

- **keps/77-dynamically-sized-jobs/README.md:287** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-05T17:32:56Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.16), only **unconstrained** topology mode is supported for scale-up
```

  Fixed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-05T18:19:58Z

- **keps/2724-topology-aware-scheduling/kep.yaml:1** by [@mimowo](https://github.com/mimowo) — 2026-02-05T18:19:58Z
  I'm ok either with follow up if we see such a need, but I'm also ok to introduce it proactively in this KEP update. `ElasticJobsViaWorkloadSliceWithTAS` seems reasonable, wdyt?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-05T18:46:10Z

- **keps/77-dynamically-sized-jobs/README.md:282** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-05T18:46:10Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
```

  > I think the initial PR already delivers scale down actually. I would like to move forward with that, but we can add this for criteria to Beta. Then we can deliver it in first alpha or second alpha.
  
  I see, I'm ok with including scale-down in the first iteration, but as least, I think that we should not support scaling under the repairing situations at this time.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-05T18:55:32Z

- **keps/77-dynamically-sized-jobs/README.md:295** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-05T18:55:32Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.16), only **unconstrained** topology mode is supported for scale-up
+operations. Jobs with `kueue.x-k8s.io/elastic-job: "true"` that also specify required or preferred
+topology annotations are rejected at job creation via webhook validation. Support for scale-down
+and required/preferred modes will be added in future iterations. If the previous assignment is
+stale (e.g., node failure), a new assignment is calculated.
+
+#### Pod Identification Across Slices
```

  My "Pod Integration" indicates https://github.com/kubernetes-sigs/kueue/tree/main/pkg/controller/jobs/pod.
  Does your "Pod Integration" indicate to it as well or just mention how to handle Pod in TAS?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-05T18:59:04Z

- **keps/2724-topology-aware-scheduling/kep.yaml:1** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-05T18:59:04Z
  +1 on introducing the `ElasticJobsViaWorkloadSliceWithTAS` in this KEP.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-05T21:51:14Z

- **keps/2724-topology-aware-scheduling/kep.yaml:1** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-05T21:51:14Z
  Fixed

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-05T21:51:39Z

- **keps/77-dynamically-sized-jobs/README.md:312** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-05T21:51:39Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.16), only **unconstrained** topology mode is supported for scale-up
+operations. Jobs with `kueue.x-k8s.io/elastic-job: "true"` that also specify required or preferred
+topology annotations are rejected at job creation via webhook validation. Support for scale-down
+and required/preferred modes will be added in future iterations. If the previous assignment is
+stale (e.g., node failure), a new assignment is calculated.
+
+#### Pod Identification Across Slices
+
+The `kueue.x-k8s.io/workload` annotation changes with each workload slice, which prevents
+TAS from finding all pods belonging to a job across slice transitions. To address this,
+a `kueue.x-k8s.io/workload-slice-name` annotation is added to pods when both features are
+enabled. This annotation contains the origin workload name in the slice chain (following
+`WorkloadSliceReplacementFor` annotations to find the root) and remains stable across slice
+transitions, allowing `TopologyUngater` to find all pods belonging to the job. The namespace
+scope in pod lookups ensures uniqueness across different namespaces.
+
+```golang
+const (
+    // WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+    WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
+)
+```
+
+#### Scale Up
```

  Fixed

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-05T22:01:03Z

- **keps/77-dynamically-sized-jobs/README.md:295** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-05T22:01:03Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.16), only **unconstrained** topology mode is supported for scale-up
+operations. Jobs with `kueue.x-k8s.io/elastic-job: "true"` that also specify required or preferred
+topology annotations are rejected at job creation via webhook validation. Support for scale-down
+and required/preferred modes will be added in future iterations. If the previous assignment is
+stale (e.g., node failure), a new assignment is calculated.
+
+#### Pod Identification Across Slices
```

  It's not about adding standalone Pod (pkg/controller/jobs/pod) support for ElasticJobs. ElasticJobs scope remains Job and RayCluster only. Does that answer your question?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T12:09:25Z

- **keps/77-dynamically-sized-jobs/README.md:565** by [@mimowo](https://github.com/mimowo) — 2026-02-06T12:09:25Z
```diff
@@ -509,6 +561,8 @@ Here’s a structured and detailed **Graduation Criteria** section for KEP-77: *
 * [ ] Re-evaluate the approach for removing PodSchedulingReadiness gate for admitted workload slices to use a dedicated controller rather than calling from Job reconciler (see 3. in [comment](https://github.com/kubernetes-sigs/kueue/pull/5510#issuecomment-3060737465)).
 * [ ] Re-evaluate integration frameworks leveraging `ElasticJobsViaWorkloadSlices`.
 * [ ] Re-evaluate `ElasticJobsViaWorkloadSlices` by leveraging the MultiKueue JobAdapter’s `Sync` routine for ElasticJobs-specific functionality, particularly in detecting `JobUpdate` events as described in [issue #7065](https://github.com/kubernetes-sigs/kueue/issues/7065).
+* [ ] TAS integration validated in production-like environments with scale-up/scale-down cycles.
+* [ ] Full integration with Topology-Aware Scheduling (support for required/preferred topology modes and scale-down), or clear validation/rejection of unsupported options.
```

  Please also add a point to re-evaluate the approach to handling scale-ups and downs during the node reparing

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T12:10:25Z

- **keps/77-dynamically-sized-jobs/README.md:324** by [@mimowo](https://github.com/mimowo) — 2026-02-06T12:10:25Z
```diff
@@ -275,6 +279,53 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When the ElasticJobsViaWorkloadSlicesWithTAS feature gate is enabled (requires
+both ElasticJobsViaWorkloadSlices and TopologyAwareScheduling), Kueue attempts
+to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.17), only **unconstrained** topology mode is supported for scaling
+operations. Jobs with `kueue.x-k8s.io/elastic-job: "true"` that also specify required or preferred
+topology annotations are rejected at job creation via webhook validation. Support for
+required/preferred modes will be added in future iterations. During node repair (unhealthy nodes),
+both scale-up and scale-down operations are blocked until the nodes recover.
+
+#### Pod Identification Across Slices
+
+The `kueue.x-k8s.io/workload` annotation changes with each workload slice, which prevents
+TAS from finding all pods belonging to a job across slice transitions. To address this,
+a `kueue.x-k8s.io/workload-slice-name` annotation is added to pods when both features are
+enabled. This annotation contains the origin workload name in the slice chain (following
+`WorkloadSliceReplacementFor` annotations to find the root) and remains stable across slice
+transitions, allowing `TopologyUngater` to find all pods belonging to the job. The namespace
+scope in pod lookups ensures uniqueness across different namespaces.
+
+```golang
+const (
+    // WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+    WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
+)
+```
+
+#### Scale Up
+
+When a new workload slice replaces an old one during scale-up, the scheduler extracts
+the `TopologyAssignment` from the old slice and passes it to TAS. TAS attempts to place
+new pods in the same topology domain as existing pods. If the previous assignment is
+stale (e.g., nodes are unhealthy), TAS blocks the scale-up operation until the nodes recover.
+
+#### Scale Down
+
+During scale-down, the workload slice retains the existing topology assignment for the
+remaining pods. If unhealthy nodes are detected during the scale-down operation, the
```

  Let's call it a tentative plan to be re-evaluated before Beta, I'm not sure this is ideal, and add a point about that before Beta, see also https://github.com/kubernetes-sigs/kueue/pull/8642/changes#r2773819934

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T12:11:22Z

- **keps/77-dynamically-sized-jobs/README.md:317** by [@mimowo](https://github.com/mimowo) — 2026-02-06T12:11:22Z
```diff
@@ -275,6 +279,53 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When the ElasticJobsViaWorkloadSlicesWithTAS feature gate is enabled (requires
+both ElasticJobsViaWorkloadSlices and TopologyAwareScheduling), Kueue attempts
+to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.17), only **unconstrained** topology mode is supported for scaling
+operations. Jobs with `kueue.x-k8s.io/elastic-job: "true"` that also specify required or preferred
+topology annotations are rejected at job creation via webhook validation. Support for
+required/preferred modes will be added in future iterations. During node repair (unhealthy nodes),
+both scale-up and scale-down operations are blocked until the nodes recover.
+
+#### Pod Identification Across Slices
+
+The `kueue.x-k8s.io/workload` annotation changes with each workload slice, which prevents
+TAS from finding all pods belonging to a job across slice transitions. To address this,
+a `kueue.x-k8s.io/workload-slice-name` annotation is added to pods when both features are
+enabled. This annotation contains the origin workload name in the slice chain (following
+`WorkloadSliceReplacementFor` annotations to find the root) and remains stable across slice
+transitions, allowing `TopologyUngater` to find all pods belonging to the job. The namespace
+scope in pod lookups ensures uniqueness across different namespaces.
+
+```golang
+const (
+    // WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+    WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
+)
+```
+
+#### Scale Up
+
+When a new workload slice replaces an old one during scale-up, the scheduler extracts
+the `TopologyAssignment` from the old slice and passes it to TAS. TAS attempts to place
+new pods in the same topology domain as existing pods. If the previous assignment is
+stale (e.g., nodes are unhealthy), TAS blocks the scale-up operation until the nodes recover.
```

  Please also here mention that this is the tentative plan, and will be re-evaluated before beta. I don't want to commit to this long-term, as suggested here: https://github.com/kubernetes-sigs/kueue/pull/8642/changes#r2773819934

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T12:12:15Z

- **keps/77-dynamically-sized-jobs/README.md:565** by [@mimowo](https://github.com/mimowo) — 2026-02-06T12:12:15Z
```diff
@@ -509,6 +561,8 @@ Here’s a structured and detailed **Graduation Criteria** section for KEP-77: *
 * [ ] Re-evaluate the approach for removing PodSchedulingReadiness gate for admitted workload slices to use a dedicated controller rather than calling from Job reconciler (see 3. in [comment](https://github.com/kubernetes-sigs/kueue/pull/5510#issuecomment-3060737465)).
 * [ ] Re-evaluate integration frameworks leveraging `ElasticJobsViaWorkloadSlices`.
 * [ ] Re-evaluate `ElasticJobsViaWorkloadSlices` by leveraging the MultiKueue JobAdapter’s `Sync` routine for ElasticJobs-specific functionality, particularly in detecting `JobUpdate` events as described in [issue #7065](https://github.com/kubernetes-sigs/kueue/issues/7065).
+* [ ] TAS integration validated in production-like environments with scale-up/scale-down cycles.
+* [ ] Full integration with Topology-Aware Scheduling (support for required/preferred topology modes and scale-down), or clear validation/rejection of unsupported options.
```

  Please add a common prefix for the Points about TAS integration like `TAS-integration:`  since we are tracking them with a dedicated feature gate

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-06T16:03:36Z

- **keps/77-dynamically-sized-jobs/README.md:295** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-06T16:03:36Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.16), only **unconstrained** topology mode is supported for scale-up
+operations. Jobs with `kueue.x-k8s.io/elastic-job: "true"` that also specify required or preferred
+topology annotations are rejected at job creation via webhook validation. Support for scale-down
+and required/preferred modes will be added in future iterations. If the previous assignment is
+stale (e.g., node failure), a new assignment is calculated.
+
+#### Pod Identification Across Slices
```

  > It's not about adding standalone Pod (pkg/controller/jobs/pod) support for ElasticJobs. ElasticJobs scope remains Job and RayCluster only. Does that answer your question?
  
  Yes, that is a right answer for my question. In that case, I'm ok with that.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-06T16:05:17Z

- **keps/77-dynamically-sized-jobs/README.md:295** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-06T16:05:17Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.16), only **unconstrained** topology mode is supported for scale-up
+operations. Jobs with `kueue.x-k8s.io/elastic-job: "true"` that also specify required or preferred
+topology annotations are rejected at job creation via webhook validation. Support for scale-down
+and required/preferred modes will be added in future iterations. If the previous assignment is
+stale (e.g., node failure), a new assignment is calculated.
+
+#### Pod Identification Across Slices
```

  Thank you for describing that, I'm ok with closing this thread.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-06T16:07:06Z

- **keps/2724-topology-aware-scheduling/kep.yaml:40** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-06T16:07:06Z
```diff
@@ -36,6 +37,7 @@ milestone:
 # List the feature gate name and the components for which it must be enabled
 feature-gates:
   - name: TopologyAwareScheduling
+  - name: ElasticJobsViaWorkloadSlicesWithTAS
```

  @mimowo  If we define this FG at this TAS enhancement, shouldn't we use "TASWithElasticJobsViaWorkloadSlices"?
  Because TAS is a center of this enhancement instead of ElasticJobsViaWorkloadSlices.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-06T16:07:52Z

- **keps/77-dynamically-sized-jobs/README.md:565** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-06T16:07:52Z
```diff
@@ -509,6 +561,8 @@ Here’s a structured and detailed **Graduation Criteria** section for KEP-77: *
 * [ ] Re-evaluate the approach for removing PodSchedulingReadiness gate for admitted workload slices to use a dedicated controller rather than calling from Job reconciler (see 3. in [comment](https://github.com/kubernetes-sigs/kueue/pull/5510#issuecomment-3060737465)).
 * [ ] Re-evaluate integration frameworks leveraging `ElasticJobsViaWorkloadSlices`.
 * [ ] Re-evaluate `ElasticJobsViaWorkloadSlices` by leveraging the MultiKueue JobAdapter’s `Sync` routine for ElasticJobs-specific functionality, particularly in detecting `JobUpdate` events as described in [issue #7065](https://github.com/kubernetes-sigs/kueue/issues/7065).
+* [ ] TAS integration validated in production-like environments with scale-up/scale-down cycles.
+* [ ] Full integration with Topology-Aware Scheduling (support for required/preferred topology modes and scale-down), or clear validation/rejection of unsupported options.
```

  +1

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-06T16:09:55Z

- **keps/2724-topology-aware-scheduling/kep.yaml:40** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-06T16:09:55Z
```diff
@@ -36,6 +37,7 @@ milestone:
 # List the feature gate name and the components for which it must be enabled
 feature-gates:
   - name: TopologyAwareScheduling
+  - name: ElasticJobsViaWorkloadSlicesWithTAS
```

  Oh, NVM. We have this FG in KEP-77 level as well.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-06T16:10:48Z

- **keps/2724-topology-aware-scheduling/kep.yaml:40** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-06T16:10:48Z
```diff
@@ -36,6 +37,7 @@ milestone:
 # List the feature gate name and the components for which it must be enabled
 feature-gates:
   - name: TopologyAwareScheduling
+  - name: ElasticJobsViaWorkloadSlicesWithTAS
```

  In that case, we probably can remove this FG from kep-2724, then keep that only in kep-77.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-06T16:15:19Z

- **keps/77-dynamically-sized-jobs/README.md:293** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-06T16:15:20Z
```diff
@@ -275,6 +279,53 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When the ElasticJobsViaWorkloadSlicesWithTAS feature gate is enabled (requires
+both ElasticJobsViaWorkloadSlices and TopologyAwareScheduling), Kueue attempts
+to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.17), only **unconstrained** topology mode is supported for scaling
+operations. Jobs with `kueue.x-k8s.io/elastic-job: "true"` that also specify required or preferred
+topology annotations are rejected at job creation via webhook validation. Support for
+required/preferred modes will be added in future iterations. During node repair (unhealthy nodes),
+both scale-up and scale-down operations are blocked until the nodes recover.
```

  Thank you 👍

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-06T16:21:10Z

- **keps/77-dynamically-sized-jobs/README.md:1** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-06T16:21:10Z
  @mimowo  This is just a comment and out of scope in this KEP discussion, but let me mention that here.
  While I review this KEP, I'm wondering if there is any problem in case of ElasticJobsViaWorkloadSlices and WaitForPodsReady timeout and recoveryTimeout by the same reason as node unhealthy situation.
  
  Additionally, I guess that we might have the same conflict problem between ElasticJobsViaWorkloadSlices and ProvReq (or AdmissionCheck) retry or timeout mechanism. We might need to track those problem as other ones.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-06T16:21:35Z

Otherwise LGTM, thank you!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T16:39:26Z

- **keps/77-dynamically-sized-jobs/README.md:1** by [@mimowo](https://github.com/mimowo) — 2026-02-06T16:39:26Z
  Well, yeah, I think there are problems related to the `ElasticJobsViaWorkloadSlices` due to the "multiple workloads" design, as I recently mentioned here: https://github.com/kubernetes-sigs/kueue/issues/5897.
  
  For, example we don't currently copy the WorkloadStatus (information about the past requeueState), and so me lose track of the number of WaitForPodsReady retries, or ProvisioningRequest retries. cc @ichekrygin @yaroslava-serdiuk . On top of the "problems" it also complicates the mental model, complicates MultiKueue etc. cc @olekzabl @kshalot as MultiKueue leads at Google's side now.
  
  Still, these problems aren't specific to this KEP (TAS + ElasticJobsViaWorkloadSlices).
  
   I think, we may need to consider them if we go with `ElasticJobsViaWorkloadSlices` or look for the single-workload approach. I think we could still replace the approach even if this goes to Beta, because from the user-perspective, aside from corner cases, it should behave the same. The main issue is that changing the approach requires investment and it is hard to get at my end at this point. OTOH we observe growing user interest into elastic workloads. So, my idea is to continue with the design until we find someone who could invest time to research the single-workload approach.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-06T16:52:17Z

- **keps/77-dynamically-sized-jobs/README.md:1** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-06T16:52:17Z
  > Well, yeah, I think there are problems related to the ElasticJobsViaWorkloadSlices due to the "multiple workloads" design, as I recently mentioned here: https://github.com/kubernetes-sigs/kueue/issues/5897.
  
  yes, good call. I am with you. Additionally, I'm thinking about the problem situation where scaling-down operation during workload with temporary not-ready Pod.
  
  In that situation, the scale-down could be an unexpected one from a user PoV as I mentioned in the node repairing situation issue.
  
  > OTOH we observe growing user interest into elastic workloads. So, my idea is to continue with the design until we find someone who could invest time to research the single-workload approach.
  
  Yes, I guess that it will be the only way to grow Elastic Workload support.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2026-02-06T17:09:32Z

- **keps/77-dynamically-sized-jobs/README.md:1** by [@ichekrygin](https://github.com/ichekrygin) — 2026-02-06T17:09:32Z
  > For, example we don't currently copy the WorkloadStatus (information about the past requeueState), and so me lose track of the number of WaitForPodsReady retries, or ProvisioningRequest retries. cc @ichekrygin @yaroslava-serdiuk . On top of the "problems" it also complicates the mental model, complicates MultiKueue etc. cc @olekzabl @kshalot as MultiKueue leads at Google's side now.
  
  I think “status copying” between slices is one of the easier problems to tackle. I don’t see any inherent design blockers that would prevent us from doing this.
  
  As you’re pointing out, the current workload slice implementation has edge cases around slice replacement. That’s largely inherent to the design and to the fact that Kubernetes has no notion of transactions (multiple updates). That said, the current implementation represents one of the trade-offs I recently summarized [here](https://github.com/kubernetes-sigs/kueue/issues/9015#issuecomment-3857140563)
  
  And yes, I’m 100% in agreement that having multiple workloads per job is a mental model overload 🙂

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2026-02-06T17:21:20Z

- **keps/77-dynamically-sized-jobs/README.md:295** by [@ichekrygin](https://github.com/ichekrygin) — 2026-02-06T17:21:20Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.16), only **unconstrained** topology mode is supported for scale-up
+operations. Jobs with `kueue.x-k8s.io/elastic-job: "true"` that also specify required or preferred
+topology annotations are rejected at job creation via webhook validation. Support for scale-down
+and required/preferred modes will be added in future iterations. If the previous assignment is
+stale (e.g., node failure), a new assignment is calculated.
+
+#### Pod Identification Across Slices
```

  Commented in wrong place :/  - pls ignore here, and see below.
  
  I agree with the concern around “expanding” the API via annotations in this context. I don’t see this as a blocker, but rather as a symptom of a larger underlying problem.
  
  Have we considered the alternative? Technically, we don’t need to encode the original or previous workload name in a pod annotation. We should be able to retrieve and map the pods directly. It’s also worth remembering that during a scale-up event there should only be two slices in play at any given time, and both should be present concurrently.
  
  The new workload slice is already annotated with `"kueue.x-k8s.io/workload-slice-replacement-for"`, which we could leverage to discover and correlate pods across slices in the scale-up context.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2026-02-06T17:23:13Z

- **keps/77-dynamically-sized-jobs/README.md:310** by [@ichekrygin](https://github.com/ichekrygin) — 2026-02-06T17:23:12Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.16), only **unconstrained** topology mode is supported for scale-up
+operations. Required and preferred topology modes are not yet supported and will be rejected
+during scheduling. Support for scale-down topology preservation and other topology modes (required,
+preferred) will be added in future iterations. If the previous assignment is stale (e.g., node
+failure), a new assignment is calculated.
+
+#### Pod Identification Across Slices
+
+The `kueue.x-k8s.io/workload` annotation changes with each workload slice, which prevents
+TAS from finding all pods belonging to a job across slice transitions. To address this,
+a `kueue.x-k8s.io/workload-slice-name` annotation is added to pods when both features are
+enabled. This annotation contains the origin workload name in the slice chain (following
+`WorkloadSliceReplacementFor` annotations to find the root) and remains stable across slice
+transitions, allowing `TopologyUngater` to find all pods belonging to the job. The namespace
+scope in pod lookups ensures uniqueness across different namespaces.
+
+```golang
+const (
+    // WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+    WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
+)
+```
```

  I agree with the concern around “expanding” the API via annotations in this context. I don’t see this as a blocker, but rather as a symptom of a larger underlying problem.
  
  Have we considered the alternative? Technically, we don’t need to encode the original or previous workload name in a pod annotation. We should be able to retrieve and map the pods directly. It’s also worth remembering that during a scale-up event there should only be two slices in play at any given time, and both should be present concurrently.
  
  The new workload slice is already annotated with "kueue.x-k8s.io/workload-slice-replacement-for", which we could leverage to discover and correlate pods across slices in the scale-up context.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2026-02-06T17:25:36Z

- **keps/77-dynamically-sized-jobs/README.md:1** by [@ichekrygin](https://github.com/ichekrygin) — 2026-02-06T17:25:36Z
  I want to call out one thing. The current WorkloadSlice implementation has a *sticky* flavor assignment, meaning the new slice **must** be assigned to the same flavor, even if that results in the new slice being blocked or remaining pending.
  
  Does this have any impact in the TAS context?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T17:29:49Z

- **keps/77-dynamically-sized-jobs/README.md:295** by [@mimowo](https://github.com/mimowo) — 2026-02-06T17:29:49Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.16), only **unconstrained** topology mode is supported for scale-up
+operations. Jobs with `kueue.x-k8s.io/elastic-job: "true"` that also specify required or preferred
+topology annotations are rejected at job creation via webhook validation. Support for scale-down
+and required/preferred modes will be added in future iterations. If the previous assignment is
+stale (e.g., node failure), a new assignment is calculated.
+
+#### Pod Identification Across Slices
```

  >  Have we considered the alternative? 
  
  Sure, but I couldn't find an alternative way. Maybe I didn't try hard enough, but this is already an indicator it is not easy.
  
  The "workload-slice" seems very simple conceptually, it is carried from workload to workload. So, all Pods know the "slice-name" and can quickly find all workloads belonging to the slice.
  
  > Technically, we don’t need to encode the original or previous workload name in a pod annotation. We should be able to retrieve and map the pods directly. It’s also worth remembering that during a scale-up event there should only be two slices in play at any given time, and both should be present concurrently.
  
  Well, but if we did 10 scaleup is in the row, then the first workload no longer exists, but the Pods use the annotation pointing to that workload.
  
  > The new workload slice is already annotated with "kueue.x-k8s.io/workload-slice-replacement-for", which we could leverage to discover and correlate pods across slices in the scale-up context.
  
  Yes, we use it to determine the "new workload", but first of all the problem is, for a given pod, find the set of "current workloads". This is not always easy, or can be performance costly, for "deep hierarchies", With the "workload-slice" annotation we can for each pod quickly find the currently active workload slices.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2026-02-06T17:44:33Z

- **keps/77-dynamically-sized-jobs/README.md:295** by [@ichekrygin](https://github.com/ichekrygin) — 2026-02-06T17:44:32Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.16), only **unconstrained** topology mode is supported for scale-up
+operations. Jobs with `kueue.x-k8s.io/elastic-job: "true"` that also specify required or preferred
+topology annotations are rejected at job creation via webhook validation. Support for scale-down
+and required/preferred modes will be added in future iterations. If the previous assignment is
+stale (e.g., node failure), a new assignment is calculated.
+
+#### Pod Identification Across Slices
```

  Since my original comment was in wrong place - I respond in the thread below.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2026-02-06T19:18:21Z

- **keps/77-dynamically-sized-jobs/README.md:301** by [@ichekrygin](https://github.com/ichekrygin) — 2026-02-06T19:18:21Z
```diff
@@ -275,6 +279,53 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When the ElasticJobsViaWorkloadSlicesWithTAS feature gate is enabled (requires
+both ElasticJobsViaWorkloadSlices and TopologyAwareScheduling), Kueue attempts
+to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.17), only **unconstrained** topology mode is supported for scaling
+operations. Jobs with `kueue.x-k8s.io/elastic-job: "true"` that also specify required or preferred
+topology annotations are rejected at job creation via webhook validation. Support for
+required/preferred modes will be added in future iterations. During node repair (unhealthy nodes),
+both scale-up and scale-down operations are blocked until the nodes recover.
+
+#### Pod Identification Across Slices
+
+The `kueue.x-k8s.io/workload` annotation changes with each workload slice, which prevents
+TAS from finding all pods belonging to a job across slice transitions. To address this,
+a `kueue.x-k8s.io/workload-slice-name` annotation is added to pods when both features are
+enabled. This annotation contains the origin workload name in the slice chain (following
+`WorkloadSliceReplacementFor` annotations to find the root) and remains stable across slice
```

  Today, every new workload slice has a pointer to its predecessor. However, there is no guarantee that all predecessors in the chain are still present or accessible, as @mimowo correctly pointed out.
  
  Alternatively, we could treat the original workload as the stable anchor that tracks all pods belonging to the logical “group.” In that case, the origin workload identity would need to be preserved explicitly in all subsequent slices, for example via an annotation like `kueue.x-k8s.io/workload-slice-origin` (or similar).
  
  This approach would address two issues:
  * It removes the need to walk the slice chain to discover the root workload.
  * It avoids relying on the continued existence of the root workload in the API.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T19:44:13Z

- **keps/77-dynamically-sized-jobs/README.md:301** by [@mimowo](https://github.com/mimowo) — 2026-02-06T19:44:12Z
```diff
@@ -275,6 +279,53 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When the ElasticJobsViaWorkloadSlicesWithTAS feature gate is enabled (requires
+both ElasticJobsViaWorkloadSlices and TopologyAwareScheduling), Kueue attempts
+to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.17), only **unconstrained** topology mode is supported for scaling
+operations. Jobs with `kueue.x-k8s.io/elastic-job: "true"` that also specify required or preferred
+topology annotations are rejected at job creation via webhook validation. Support for
+required/preferred modes will be added in future iterations. During node repair (unhealthy nodes),
+both scale-up and scale-down operations are blocked until the nodes recover.
+
+#### Pod Identification Across Slices
+
+The `kueue.x-k8s.io/workload` annotation changes with each workload slice, which prevents
+TAS from finding all pods belonging to a job across slice transitions. To address this,
+a `kueue.x-k8s.io/workload-slice-name` annotation is added to pods when both features are
+enabled. This annotation contains the origin workload name in the slice chain (following
+`WorkloadSliceReplacementFor` annotations to find the root) and remains stable across slice
```

  Yes, this is what we are already doing with the annotation already introduced in a preparatory PR: https://github.com/kubernetes-sigs/kueue/pull/8768

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T20:03:54Z

- **keps/77-dynamically-sized-jobs/README.md:295** by [@mimowo](https://github.com/mimowo) — 2026-02-06T20:03:54Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.16), only **unconstrained** topology mode is supported for scale-up
+operations. Jobs with `kueue.x-k8s.io/elastic-job: "true"` that also specify required or preferred
+topology annotations are rejected at job creation via webhook validation. Support for scale-down
+and required/preferred modes will be added in future iterations. If the previous assignment is
+stale (e.g., node failure), a new assignment is calculated.
+
+#### Pod Identification Across Slices
```

  ups, let me x-reference

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T20:05:02Z

- **keps/77-dynamically-sized-jobs/README.md:310** by [@mimowo](https://github.com/mimowo) — 2026-02-06T20:05:02Z
```diff
@@ -275,6 +278,44 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When both TopologyAwareScheduling and ElasticJobsViaWorkloadSlices feature gates are enabled,
+Kueue attempts to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.16), only **unconstrained** topology mode is supported for scale-up
+operations. Required and preferred topology modes are not yet supported and will be rejected
+during scheduling. Support for scale-down topology preservation and other topology modes (required,
+preferred) will be added in future iterations. If the previous assignment is stale (e.g., node
+failure), a new assignment is calculated.
+
+#### Pod Identification Across Slices
+
+The `kueue.x-k8s.io/workload` annotation changes with each workload slice, which prevents
+TAS from finding all pods belonging to a job across slice transitions. To address this,
+a `kueue.x-k8s.io/workload-slice-name` annotation is added to pods when both features are
+enabled. This annotation contains the origin workload name in the slice chain (following
+`WorkloadSliceReplacementFor` annotations to find the root) and remains stable across slice
+transitions, allowing `TopologyUngater` to find all pods belonging to the job. The namespace
+scope in pod lookups ensures uniqueness across different namespaces.
+
+```golang
+const (
+    // WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+    WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
+)
+```
```

  My answer is here: https://github.com/kubernetes-sigs/kueue/pull/8642#discussion_r2775259708
  
  FYI this annotation has already been introduced in 0.16 in this preparatory work: https://github.com/kubernetes-sigs/kueue/pull/8768

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T20:06:16Z

- **keps/77-dynamically-sized-jobs/README.md:1** by [@mimowo](https://github.com/mimowo) — 2026-02-06T20:06:16Z
  In the context of TAS we need to land on the same flavor, and even more the set of "new nodes" needs to be a superset (for scale up) or subset (for scale down) or old nodes. So this property (stick flavor) is a strict requirement for TAS integration to work.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2026-02-06T20:12:28Z

- **keps/77-dynamically-sized-jobs/README.md:301** by [@ichekrygin](https://github.com/ichekrygin) — 2026-02-06T20:12:27Z
```diff
@@ -275,6 +279,53 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When the ElasticJobsViaWorkloadSlicesWithTAS feature gate is enabled (requires
+both ElasticJobsViaWorkloadSlices and TopologyAwareScheduling), Kueue attempts
+to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.17), only **unconstrained** topology mode is supported for scaling
+operations. Jobs with `kueue.x-k8s.io/elastic-job: "true"` that also specify required or preferred
+topology annotations are rejected at job creation via webhook validation. Support for
+required/preferred modes will be added in future iterations. During node repair (unhealthy nodes),
+both scale-up and scale-down operations are blocked until the nodes recover.
+
+#### Pod Identification Across Slices
+
+The `kueue.x-k8s.io/workload` annotation changes with each workload slice, which prevents
+TAS from finding all pods belonging to a job across slice transitions. To address this,
+a `kueue.x-k8s.io/workload-slice-name` annotation is added to pods when both features are
+enabled. This annotation contains the origin workload name in the slice chain (following
+`WorkloadSliceReplacementFor` annotations to find the root) and remains stable across slice
```

  Missed that PR.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2026-02-06T20:13:01Z

- **keps/77-dynamically-sized-jobs/README.md:1** by [@ichekrygin](https://github.com/ichekrygin) — 2026-02-06T20:13:00Z
  Nice, ty, for clarifying.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2026-02-06T20:15:42Z

Nice addition, LGTM!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-09T13:24:36Z

- **keps/77-dynamically-sized-jobs/README.md:565** by [@mimowo](https://github.com/mimowo) — 2026-02-09T13:24:36Z
```diff
@@ -509,6 +561,8 @@ Here’s a structured and detailed **Graduation Criteria** section for KEP-77: *
 * [ ] Re-evaluate the approach for removing PodSchedulingReadiness gate for admitted workload slices to use a dedicated controller rather than calling from Job reconciler (see 3. in [comment](https://github.com/kubernetes-sigs/kueue/pull/5510#issuecomment-3060737465)).
 * [ ] Re-evaluate integration frameworks leveraging `ElasticJobsViaWorkloadSlices`.
 * [ ] Re-evaluate `ElasticJobsViaWorkloadSlices` by leveraging the MultiKueue JobAdapter’s `Sync` routine for ElasticJobs-specific functionality, particularly in detecting `JobUpdate` events as described in [issue #7065](https://github.com/kubernetes-sigs/kueue/issues/7065).
+* [ ] TAS integration validated in production-like environments with scale-up/scale-down cycles.
+* [ ] Full integration with Topology-Aware Scheduling (support for required/preferred topology modes and scale-down), or clear validation/rejection of unsupported options.
```

  @sohankunkerkar please address this by adding: "re-evaluate the approach to handling scale-ups and downs during the node reparing"

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-09T13:25:11Z

- **keps/77-dynamically-sized-jobs/README.md:565** by [@mimowo](https://github.com/mimowo) — 2026-02-09T13:25:11Z
```diff
@@ -509,6 +561,8 @@ Here’s a structured and detailed **Graduation Criteria** section for KEP-77: *
 * [ ] Re-evaluate the approach for removing PodSchedulingReadiness gate for admitted workload slices to use a dedicated controller rather than calling from Job reconciler (see 3. in [comment](https://github.com/kubernetes-sigs/kueue/pull/5510#issuecomment-3060737465)).
 * [ ] Re-evaluate integration frameworks leveraging `ElasticJobsViaWorkloadSlices`.
 * [ ] Re-evaluate `ElasticJobsViaWorkloadSlices` by leveraging the MultiKueue JobAdapter’s `Sync` routine for ElasticJobs-specific functionality, particularly in detecting `JobUpdate` events as described in [issue #7065](https://github.com/kubernetes-sigs/kueue/issues/7065).
+* [ ] TAS integration validated in production-like environments with scale-up/scale-down cycles.
+* [ ] Full integration with Topology-Aware Scheduling (support for required/preferred topology modes and scale-down), or clear validation/rejection of unsupported options.
```

  nit, please address this

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-09T15:00:31Z

- **keps/2724-topology-aware-scheduling/kep.yaml:40** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-09T15:00:30Z
```diff
@@ -36,6 +37,7 @@ milestone:
 # List the feature gate name and the components for which it must be enabled
 feature-gates:
   - name: TopologyAwareScheduling
+  - name: ElasticJobsViaWorkloadSlicesWithTAS
```

  Yeah, I removed that from kep-2724

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-09T15:01:24Z

- **keps/77-dynamically-sized-jobs/README.md:565** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-09T15:01:23Z
```diff
@@ -509,6 +561,8 @@ Here’s a structured and detailed **Graduation Criteria** section for KEP-77: *
 * [ ] Re-evaluate the approach for removing PodSchedulingReadiness gate for admitted workload slices to use a dedicated controller rather than calling from Job reconciler (see 3. in [comment](https://github.com/kubernetes-sigs/kueue/pull/5510#issuecomment-3060737465)).
 * [ ] Re-evaluate integration frameworks leveraging `ElasticJobsViaWorkloadSlices`.
 * [ ] Re-evaluate `ElasticJobsViaWorkloadSlices` by leveraging the MultiKueue JobAdapter’s `Sync` routine for ElasticJobs-specific functionality, particularly in detecting `JobUpdate` events as described in [issue #7065](https://github.com/kubernetes-sigs/kueue/issues/7065).
+* [ ] TAS integration validated in production-like environments with scale-up/scale-down cycles.
+* [ ] Full integration with Topology-Aware Scheduling (support for required/preferred topology modes and scale-down), or clear validation/rejection of unsupported options.
```

  Done!

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-09T15:01:37Z

- **keps/77-dynamically-sized-jobs/README.md:317** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-09T15:01:37Z
```diff
@@ -275,6 +279,53 @@ const (
 )
 ```
 
+### Topology Aware Scheduling Integration
+
+When the ElasticJobsViaWorkloadSlicesWithTAS feature gate is enabled (requires
+both ElasticJobsViaWorkloadSlices and TopologyAwareScheduling), Kueue attempts
+to preserve topology locality across workload slice transitions during scaling
+operations.
+
+For the initial iteration (v0.17), only **unconstrained** topology mode is supported for scaling
+operations. Jobs with `kueue.x-k8s.io/elastic-job: "true"` that also specify required or preferred
+topology annotations are rejected at job creation via webhook validation. Support for
+required/preferred modes will be added in future iterations. During node repair (unhealthy nodes),
+both scale-up and scale-down operations are blocked until the nodes recover.
+
+#### Pod Identification Across Slices
+
+The `kueue.x-k8s.io/workload` annotation changes with each workload slice, which prevents
+TAS from finding all pods belonging to a job across slice transitions. To address this,
+a `kueue.x-k8s.io/workload-slice-name` annotation is added to pods when both features are
+enabled. This annotation contains the origin workload name in the slice chain (following
+`WorkloadSliceReplacementFor` annotations to find the root) and remains stable across slice
+transitions, allowing `TopologyUngater` to find all pods belonging to the job. The namespace
+scope in pod lookups ensures uniqueness across different namespaces.
+
+```golang
+const (
+    // WorkloadSliceNameAnnotation identifies the original workload name in a slice chain.
+    WorkloadSliceNameAnnotation = "kueue.x-k8s.io/workload-slice-name"
+)
+```
+
+#### Scale Up
+
+When a new workload slice replaces an old one during scale-up, the scheduler extracts
+the `TopologyAssignment` from the old slice and passes it to TAS. TAS attempts to place
+new pods in the same topology domain as existing pods. If the previous assignment is
+stale (e.g., nodes are unhealthy), TAS blocks the scale-up operation until the nodes recover.
```

  Fixed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-09T15:46:24Z

Thank  you 👍 
/lgtm
/approve
/hold
In case @tenzen-y has some more comments

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-09T18:43:34Z

@sohankunkerkar This looks great, thank you!
/lgtm
/approve

/hold cancel
