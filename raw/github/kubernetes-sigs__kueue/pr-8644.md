# PR #8644: Improve migrate-to-v1beta2.sh.

**Summary**: Improve migrate-to-v1beta2.sh.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8644

**Last updated**: 2026-01-19T09:47:06Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `cleanup/improve-migrate-to-v1beta2-script` → `main`
- **Created**: 2026-01-16T20:51:38Z
- **Updated**: 2026-01-19T09:47:06Z
- **Merged**: 2026-01-19T09:45:13Z
- **Closed**: 2026-01-19T09:45:13Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 1   **+117 / -15**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Improve `migrate-to-v1beta2.sh`:
- Use progress
- Add `--verbose` flag
- Add `--dry-run` flag

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #8018

#### Special notes for your reviewer:

---

#### Execute `./hack/migrate-to-v1beta2.sh`:

```
$ ./hack/migrate-to-v1beta2.sh    
         
Migrating cohorts.kueue.x-k8s.io...
  → No cohorts found, skipping.

Migrating workloadpriorityclasses.kueue.x-k8s.io...
  → No workloadpriorityclasses found, skipping.

Migrating localqueues.kueue.x-k8s.io...
  → Found: 1 object(s)
  → Progress: 100% (1/1)

Migrating clusterqueues.kueue.x-k8s.io...
  → Found: 1 object(s)
  → Progress: 100% (1/1)

Migrating resourceflavors.kueue.x-k8s.io...
  → Found: 1 object(s)
  → Progress: 100% (1/1)

Migrating admissionchecks.kueue.x-k8s.io...
  → No admissionchecks found, skipping.

Migrating provisioningrequestconfigs.kueue.x-k8s.io...
  → No provisioningrequestconfigs found, skipping.

Migrating multikueueclusters.kueue.x-k8s.io...
  → No multikueueclusters found, skipping.

Migrating multikueueconfigs.kueue.x-k8s.io...
  → No multikueueconfigs found, skipping.

Migrating topologies.kueue.x-k8s.io...
  → No topologies found, skipping.

Migrating workloads.kueue.x-k8s.io...
  → Found: 5 object(s)
  → Progress: 100% (5/5)

All migrations finished successfully!
```

---

#### Execute `./hack/migrate-to-v1beta2.sh` with `--verbose` flag:

```
$ ./hack/migrate-to-v1beta2.sh --verbose        
                                                                       
Migrating cohorts.kueue.x-k8s.io...
  → kubectl get "cohorts.kueue.x-k8s.io" -A -o jsonpath='{range .items[*]}{.metadata.namespace}{"/"}{.metadata.name}{"\n"}{end}'
  → No cohorts found, skipping.

Migrating workloadpriorityclasses.kueue.x-k8s.io...
  → kubectl get "workloadpriorityclasses.kueue.x-k8s.io" -A -o jsonpath='{range .items[*]}{.metadata.namespace}{"/"}{.metadata.name}{"\n"}{end}'
  → No workloadpriorityclasses found, skipping.

Migrating localqueues.kueue.x-k8s.io...
  → kubectl get "localqueues.kueue.x-k8s.io" -A -o jsonpath='{range .items[*]}{.metadata.namespace}{"/"}{.metadata.name}{"\n"}{end}'
  → Found: 1 object(s)
    └─ kubectl patch localqueues.kueue.x-k8s.io user-queue -n default --type=merge -p {"apiVersion":"kueue.x-k8s.io/v1beta2"}
       localqueue.kueue.x-k8s.io/user-queue patched (no change)
  → Progress: 100% (1/1)

Migrating clusterqueues.kueue.x-k8s.io...
  → kubectl get "clusterqueues.kueue.x-k8s.io" -A -o jsonpath='{range .items[*]}{.metadata.namespace}{"/"}{.metadata.name}{"\n"}{end}'
  → Found: 1 object(s)
    └─ kubectl patch clusterqueues.kueue.x-k8s.io cluster-queue --type=merge -p {"apiVersion":"kueue.x-k8s.io/v1beta2"}
       clusterqueue.kueue.x-k8s.io/cluster-queue patched (no change)
  → Progress: 100% (1/1)

Migrating resourceflavors.kueue.x-k8s.io...
  → kubectl get "resourceflavors.kueue.x-k8s.io" -A -o jsonpath='{range .items[*]}{.metadata.namespace}{"/"}{.metadata.name}{"\n"}{end}'
  → Found: 1 object(s)
    └─ kubectl patch resourceflavors.kueue.x-k8s.io default-flavor --type=merge -p {"apiVersion":"kueue.x-k8s.io/v1beta2"}
       resourceflavor.kueue.x-k8s.io/default-flavor patched (no change)
  → Progress: 100% (1/1)

Migrating admissionchecks.kueue.x-k8s.io...
  → kubectl get "admissionchecks.kueue.x-k8s.io" -A -o jsonpath='{range .items[*]}{.metadata.namespace}{"/"}{.metadata.name}{"\n"}{end}'
  → No admissionchecks found, skipping.

Migrating provisioningrequestconfigs.kueue.x-k8s.io...
  → kubectl get "provisioningrequestconfigs.kueue.x-k8s.io" -A -o jsonpath='{range .items[*]}{.metadata.namespace}{"/"}{.metadata.name}{"\n"}{end}'
  → No provisioningrequestconfigs found, skipping.

Migrating multikueueclusters.kueue.x-k8s.io...
  → kubectl get "multikueueclusters.kueue.x-k8s.io" -A -o jsonpath='{range .items[*]}{.metadata.namespace}{"/"}{.metadata.name}{"\n"}{end}'
  → No multikueueclusters found, skipping.

Migrating multikueueconfigs.kueue.x-k8s.io...
  → kubectl get "multikueueconfigs.kueue.x-k8s.io" -A -o jsonpath='{range .items[*]}{.metadata.namespace}{"/"}{.metadata.name}{"\n"}{end}'
  → No multikueueconfigs found, skipping.

Migrating topologies.kueue.x-k8s.io...
  → kubectl get "topologies.kueue.x-k8s.io" -A -o jsonpath='{range .items[*]}{.metadata.namespace}{"/"}{.metadata.name}{"\n"}{end}'
  → No topologies found, skipping.

Migrating workloads.kueue.x-k8s.io...
  → kubectl get "workloads.kueue.x-k8s.io" -A -o jsonpath='{range .items[*]}{.metadata.namespace}{"/"}{.metadata.name}{"\n"}{end}'
  → Found: 5 object(s)
    └─ kubectl patch workloads.kueue.x-k8s.io job-sample-job-gm76w-69234 -n default --type=merge -p {"apiVersion":"kueue.x-k8s.io/v1beta2"}
       workload.kueue.x-k8s.io/job-sample-job-gm76w-69234 patched (no change)
    └─ kubectl patch workloads.kueue.x-k8s.io job-sample-job-gmtv5-8607c -n default --type=merge -p {"apiVersion":"kueue.x-k8s.io/v1beta2"}
       workload.kueue.x-k8s.io/job-sample-job-gmtv5-8607c patched (no change)
    └─ kubectl patch workloads.kueue.x-k8s.io job-sample-job-vccl7-4a634 -n default --type=merge -p {"apiVersion":"kueue.x-k8s.io/v1beta2"}
       workload.kueue.x-k8s.io/job-sample-job-vccl7-4a634 patched (no change)
    └─ kubectl patch workloads.kueue.x-k8s.io job-sample-job-vlgsn-e1e6e -n default --type=merge -p {"apiVersion":"kueue.x-k8s.io/v1beta2"}
       workload.kueue.x-k8s.io/job-sample-job-vlgsn-e1e6e patched (no change)
    └─ kubectl patch workloads.kueue.x-k8s.io job-sample-job-zsjfm-09a1b -n default --type=merge -p {"apiVersion":"kueue.x-k8s.io/v1beta2"}
       workload.kueue.x-k8s.io/job-sample-job-zsjfm-09a1b patched (no change)
  → Progress: 100% (5/5)

All migrations finished successfully!
```

---

#### Execute `./hack/migrate-to-v1beta2.sh` in cluster without Kueue:

```
$ ./hack/migrate-to-v1beta2.sh          

Migrating cohorts.kueue.x-k8s.io...
error: the server doesn't have a resource type "cohorts"

Migrating workloadpriorityclasses.kueue.x-k8s.io...
error: the server doesn't have a resource type "workloadpriorityclasses"

Migrating localqueues.kueue.x-k8s.io...
error: the server doesn't have a resource type "localqueues"

Migrating clusterqueues.kueue.x-k8s.io...
error: the server doesn't have a resource type "clusterqueues"

Migrating resourceflavors.kueue.x-k8s.io...
error: the server doesn't have a resource type "resourceflavors"

Migrating admissionchecks.kueue.x-k8s.io...
error: the server doesn't have a resource type "admissionchecks"

Migrating provisioningrequestconfigs.kueue.x-k8s.io...
error: the server doesn't have a resource type "provisioningrequestconfigs"

Migrating multikueueclusters.kueue.x-k8s.io...
error: the server doesn't have a resource type "multikueueclusters"

Migrating multikueueconfigs.kueue.x-k8s.io...
error: the server doesn't have a resource type "multikueueconfigs"

Migrating topologies.kueue.x-k8s.io...
error: the server doesn't have a resource type "topologies"

Migrating workloads.kueue.x-k8s.io...
error: the server doesn't have a resource type "workloads"

Error: Migration completed with issues!
```

---

#### Execute `./hack/migrate-to-v1beta2.sh` in cluster with Kueue < v0.15:

```
./hack/migrate-to-v1beta2.sh             
Migrating cohorts.kueue.x-k8s.io...
  → No cohorts found, skipping.

Migrating workloadpriorityclasses.kueue.x-k8s.io...
  → No workloadpriorityclasses found, skipping.

Migrating localqueues.kueue.x-k8s.io...
  → Found: 1 object(s)
Warning: unknown field "spec"
Warning: unknown field "status"
The LocalQueue "user-queue" is invalid: apiVersion: Invalid value: "kueue.x-k8s.io/v1beta2": must be kueue.x-k8s.io/v1beta1
  → Progress:   0% (0/1)
  → WARNING: only 0/1 object(s) patched successfully (1 failure(s))!

Migrating clusterqueues.kueue.x-k8s.io...
  → Found: 1 object(s)
Warning: unknown field "spec"
Warning: unknown field "status"
The ClusterQueue "cluster-queue" is invalid: apiVersion: Invalid value: "kueue.x-k8s.io/v1beta2": must be kueue.x-k8s.io/v1beta1
  → Progress:   0% (0/1)
  → WARNING: only 0/1 object(s) patched successfully (1 failure(s))!

Migrating resourceflavors.kueue.x-k8s.io...
  → Found: 1 object(s)
Warning: unknown field "spec"
The ResourceFlavor "default-flavor" is invalid: apiVersion: Invalid value: "kueue.x-k8s.io/v1beta2": must be kueue.x-k8s.io/v1beta1
  → Progress:   0% (0/1)
  → WARNING: only 0/1 object(s) patched successfully (1 failure(s))!

Migrating admissionchecks.kueue.x-k8s.io...
  → No admissionchecks found, skipping.

Migrating provisioningrequestconfigs.kueue.x-k8s.io...
  → No provisioningrequestconfigs found, skipping.

Migrating multikueueclusters.kueue.x-k8s.io...
  → No multikueueclusters found, skipping.

Migrating multikueueconfigs.kueue.x-k8s.io...
  → No multikueueconfigs found, skipping.

Migrating topologies.kueue.x-k8s.io...
  → No topologies found, skipping.

Migrating workloads.kueue.x-k8s.io...
  → Found: 5 object(s)
Warning: unknown field "spec"
Warning: unknown field "status"
The Workload "job-sample-job-446jw-f612c" is invalid: 
* apiVersion: Invalid value: "kueue.x-k8s.io/v1beta2": must be kueue.x-k8s.io/v1beta1
* <nil>: Invalid value: "object": no such key: spec evaluating rule: podSetAssignments must have the same number of podSets as the spec
* <nil>: Invalid value: "object": no such key: spec evaluating rule: field is immutable
* <nil>: Invalid value: "object": no such key: spec evaluating rule: maximumExecutionTimeSeconds is immutable while admitted
Warning: unknown field "spec"
Warning: unknown field "status"
The Workload "job-sample-job-ctf7p-9529c" is invalid: 
* apiVersion: Invalid value: "kueue.x-k8s.io/v1beta2": must be kueue.x-k8s.io/v1beta1
* <nil>: Invalid value: "object": no such key: spec evaluating rule: podSetAssignments must have the same number of podSets as the spec
* <nil>: Invalid value: "object": no such key: spec evaluating rule: field is immutable
* <nil>: Invalid value: "object": no such key: spec evaluating rule: maximumExecutionTimeSeconds is immutable while admitted
Warning: unknown field "spec"
Warning: unknown field "status"
The Workload "job-sample-job-hlmxb-810f1" is invalid: apiVersion: Invalid value: "kueue.x-k8s.io/v1beta2": must be kueue.x-k8s.io/v1beta1
Warning: unknown field "spec"
Warning: unknown field "status"
The Workload "job-sample-job-kxt2n-9a2c5" is invalid: apiVersion: Invalid value: "kueue.x-k8s.io/v1beta2": must be kueue.x-k8s.io/v1beta1
Warning: unknown field "spec"
Warning: unknown field "status"
The Workload "job-sample-job-ngcvz-3682b" is invalid: 
* apiVersion: Invalid value: "kueue.x-k8s.io/v1beta2": must be kueue.x-k8s.io/v1beta1
* <nil>: Invalid value: "object": no such key: spec evaluating rule: podSetAssignments must have the same number of podSets as the spec
* <nil>: Invalid value: "object": no such key: spec evaluating rule: field is immutable
* <nil>: Invalid value: "object": no such key: spec evaluating rule: maximumExecutionTimeSeconds is immutable while admitted
  → Progress:   0% (0/5)
  → Error: only 0/5 object(s) patched successfully (5 failure(s))!

Error: Migration completed with issues!
```

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-01-16T20:51:45Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 5a5a3165070bf3cae0368825790c5d3057d0ca12 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/696df6ab0ab5800008192a7c |

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-19T09:36:40Z

/lgtm 
/approve
Thank you!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-19T09:36:48Z

LGTM label has been added.  <details>Git tree hash: 1cd76b4c87ecf0b32c14a7b15988400dff3db549</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-19T09:36:50Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8644#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8644#issuecomment-3767353746" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-19T08:12:01Z

- **hack/migrate-to-v1beta2.sh:139** by [@mimowo](https://github.com/mimowo) — 2026-01-19T08:12:01Z
```diff
@@ -35,33 +64,94 @@ kinds=(
   workloads
 )
 
+patch_payload='{"apiVersion":"kueue.x-k8s.io/v1beta2"}'
+
+exit_code=0
+
 for kind in "${kinds[@]}"; do
-  echo "Migrating $kind.kueue.x-k8s.io ..."
+  echo "Migrating $kind.kueue.x-k8s.io..."
+
+  resources_cmd="kubectl get \"${kind}.kueue.x-k8s.io\" -A -o jsonpath='{range .items[*]}{.metadata.namespace}{\"/\"}{.metadata.name}{\"\n\"}{end}'"
+  if $VERBOSE; then
+    echo "  → ${resources_cmd}"
+  fi
 
   # Get resources in namespace/name format
-  resources=$(kubectl get "$kind.kueue.x-k8s.io" -A -o jsonpath='{range .items[*]}{.metadata.namespace}{"/"}{.metadata.name}{"\n"}{end}')
+  resources=$(eval "$resources_cmd")
+  # shellcheck disable=SC2181
+  if [ $? -ne 0 ]; then
+    echo "" >&2
+    exit_code=1
+    continue
+  fi
+
+  if [[ -z "${resources}" ]]; then
+    echo "  → No $kind found, skipping."
+    echo ""
+    continue
+  fi
+
+  filtered=$(echo "${resources}" | grep -v '^$' | grep -v '^/$')
+  total=$(echo "${filtered}" | wc -l | tr -d '[:space:]')
 
-  if [[ -z "$resources" ]]; then
-    echo "  → No $kind found, skipping"
+  if [[ -z "${total}" ]] || (( total == 0 )); then
+    echo "  → No valid ${kind} found, skipping."
+    echo ""
     continue
   fi
 
-  echo "$resources" | while read -r entry; do
-    [[ -z "$entry" || "$entry" == "/" ]] && continue
+  echo "  → Found: ${total} object(s)"
 
+  patched=0
+  percent=0
+
+  while read -r entry; do
     ns="${entry%/*}"
     name="${entry#*/}"
 
-    if [[ -z "$ns" ]]; then
-      # Cluster-scoped resource
-      kubectl patch "$kind.kueue.x-k8s.io" "$name" --type=merge -p '{"apiVersion":"kueue.x-k8s.io/v1beta2"}'
-    else
+    patch_cmd=(kubectl patch "$kind.kueue.x-k8s.io" "$name")
+    if [[ -n "$ns" ]]; then
       # Namespaced resource
-      kubectl patch "$kind.kueue.x-k8s.io" "$name" -n "$ns" --type=merge -p '{"apiVersion":"kueue.x-k8s.io/v1beta2"}'
+      patch_cmd+=(-n "$ns")
+    fi
+
+    # shellcheck disable=SC2206
+    patch_cmd+=(--type=merge -p "$patch_payload" $DRY_RUN)
+
+    if $VERBOSE; then
+        echo "    └─ ${patch_cmd[*]}"
+    fi
+
+    output=$("${patch_cmd[@]}")
+    # shellcheck disable=SC2181
+    if [ $? -eq 0 ]; then
+      patched=$((patched + 1))
+    else
+      exit_code=1
+      continue
+    fi
+
+    if $VERBOSE; then
+      echo "       $output" >&2
     fi
-  done
 
-  echo "  → $kind migration complete"
+    percent=$(( (patched * 100 + total / 2) / total ))
+    printf "  → Progress: %3d%% (%d/%d)\r" "${percent}" "${patched}" "${total}"
```

  nit, I'm wondering if on some environments which don't handle the carriage return `\r` this might bloat the output. I'm thinking about just doing:
  
  ```sh
  patched=0
  percent=0
  last_percent=-1
  
  while read -r entry; do
    # ... [your existing patch logic] ...
    # Calculate new percentage
    percent=$(( (patched * 100 + total / 2) / total ))
    if [[ $percent -ne $last_percent ]]; then
      printf "  → Progress: %3d%% (%d/%d)\r" "${percent}" "${patched}" "${total}"
      last_percent=$percent
    fi
  done <<< "$filtered"
  ```
  it doesn't seem much complexity, wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-19T08:12:13Z

LGTM overall, just one nit / discussion

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-19T09:17:39Z

- **hack/migrate-to-v1beta2.sh:139** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-19T09:17:39Z
```diff
@@ -35,33 +64,94 @@ kinds=(
   workloads
 )
 
+patch_payload='{"apiVersion":"kueue.x-k8s.io/v1beta2"}'
+
+exit_code=0
+
 for kind in "${kinds[@]}"; do
-  echo "Migrating $kind.kueue.x-k8s.io ..."
+  echo "Migrating $kind.kueue.x-k8s.io..."
+
+  resources_cmd="kubectl get \"${kind}.kueue.x-k8s.io\" -A -o jsonpath='{range .items[*]}{.metadata.namespace}{\"/\"}{.metadata.name}{\"\n\"}{end}'"
+  if $VERBOSE; then
+    echo "  → ${resources_cmd}"
+  fi
 
   # Get resources in namespace/name format
-  resources=$(kubectl get "$kind.kueue.x-k8s.io" -A -o jsonpath='{range .items[*]}{.metadata.namespace}{"/"}{.metadata.name}{"\n"}{end}')
+  resources=$(eval "$resources_cmd")
+  # shellcheck disable=SC2181
+  if [ $? -ne 0 ]; then
+    echo "" >&2
+    exit_code=1
+    continue
+  fi
+
+  if [[ -z "${resources}" ]]; then
+    echo "  → No $kind found, skipping."
+    echo ""
+    continue
+  fi
+
+  filtered=$(echo "${resources}" | grep -v '^$' | grep -v '^/$')
+  total=$(echo "${filtered}" | wc -l | tr -d '[:space:]')
 
-  if [[ -z "$resources" ]]; then
-    echo "  → No $kind found, skipping"
+  if [[ -z "${total}" ]] || (( total == 0 )); then
+    echo "  → No valid ${kind} found, skipping."
+    echo ""
     continue
   fi
 
-  echo "$resources" | while read -r entry; do
-    [[ -z "$entry" || "$entry" == "/" ]] && continue
+  echo "  → Found: ${total} object(s)"
 
+  patched=0
+  percent=0
+
+  while read -r entry; do
     ns="${entry%/*}"
     name="${entry#*/}"
 
-    if [[ -z "$ns" ]]; then
-      # Cluster-scoped resource
-      kubectl patch "$kind.kueue.x-k8s.io" "$name" --type=merge -p '{"apiVersion":"kueue.x-k8s.io/v1beta2"}'
-    else
+    patch_cmd=(kubectl patch "$kind.kueue.x-k8s.io" "$name")
+    if [[ -n "$ns" ]]; then
       # Namespaced resource
-      kubectl patch "$kind.kueue.x-k8s.io" "$name" -n "$ns" --type=merge -p '{"apiVersion":"kueue.x-k8s.io/v1beta2"}'
+      patch_cmd+=(-n "$ns")
+    fi
+
+    # shellcheck disable=SC2206
+    patch_cmd+=(--type=merge -p "$patch_payload" $DRY_RUN)
+
+    if $VERBOSE; then
+        echo "    └─ ${patch_cmd[*]}"
+    fi
+
+    output=$("${patch_cmd[@]}")
+    # shellcheck disable=SC2181
+    if [ $? -eq 0 ]; then
+      patched=$((patched + 1))
+    else
+      exit_code=1
+      continue
+    fi
+
+    if $VERBOSE; then
+      echo "       $output" >&2
     fi
-  done
 
-  echo "  → $kind migration complete"
+    percent=$(( (patched * 100 + total / 2) / total ))
+    printf "  → Progress: %3d%% (%d/%d)\r" "${percent}" "${patched}" "${total}"
```

  Added solution for both cases.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-19T09:18:42Z

- **hack/migrate-to-v1beta2.sh:139** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-19T09:18:41Z
```diff
@@ -35,33 +64,94 @@ kinds=(
   workloads
 )
 
+patch_payload='{"apiVersion":"kueue.x-k8s.io/v1beta2"}'
+
+exit_code=0
+
 for kind in "${kinds[@]}"; do
-  echo "Migrating $kind.kueue.x-k8s.io ..."
+  echo "Migrating $kind.kueue.x-k8s.io..."
+
+  resources_cmd="kubectl get \"${kind}.kueue.x-k8s.io\" -A -o jsonpath='{range .items[*]}{.metadata.namespace}{\"/\"}{.metadata.name}{\"\n\"}{end}'"
+  if $VERBOSE; then
+    echo "  → ${resources_cmd}"
+  fi
 
   # Get resources in namespace/name format
-  resources=$(kubectl get "$kind.kueue.x-k8s.io" -A -o jsonpath='{range .items[*]}{.metadata.namespace}{"/"}{.metadata.name}{"\n"}{end}')
+  resources=$(eval "$resources_cmd")
+  # shellcheck disable=SC2181
+  if [ $? -ne 0 ]; then
+    echo "" >&2
+    exit_code=1
+    continue
+  fi
+
+  if [[ -z "${resources}" ]]; then
+    echo "  → No $kind found, skipping."
+    echo ""
+    continue
+  fi
+
+  filtered=$(echo "${resources}" | grep -v '^$' | grep -v '^/$')
+  total=$(echo "${filtered}" | wc -l | tr -d '[:space:]')
 
-  if [[ -z "$resources" ]]; then
-    echo "  → No $kind found, skipping"
+  if [[ -z "${total}" ]] || (( total == 0 )); then
+    echo "  → No valid ${kind} found, skipping."
+    echo ""
     continue
   fi
 
-  echo "$resources" | while read -r entry; do
-    [[ -z "$entry" || "$entry" == "/" ]] && continue
+  echo "  → Found: ${total} object(s)"
 
+  patched=0
+  percent=0
+
+  while read -r entry; do
     ns="${entry%/*}"
     name="${entry#*/}"
 
-    if [[ -z "$ns" ]]; then
-      # Cluster-scoped resource
-      kubectl patch "$kind.kueue.x-k8s.io" "$name" --type=merge -p '{"apiVersion":"kueue.x-k8s.io/v1beta2"}'
-    else
+    patch_cmd=(kubectl patch "$kind.kueue.x-k8s.io" "$name")
+    if [[ -n "$ns" ]]; then
       # Namespaced resource
-      kubectl patch "$kind.kueue.x-k8s.io" "$name" -n "$ns" --type=merge -p '{"apiVersion":"kueue.x-k8s.io/v1beta2"}'
+      patch_cmd+=(-n "$ns")
+    fi
+
+    # shellcheck disable=SC2206
+    patch_cmd+=(--type=merge -p "$patch_payload" $DRY_RUN)
+
+    if $VERBOSE; then
+        echo "    └─ ${patch_cmd[*]}"
+    fi
+
+    output=$("${patch_cmd[@]}")
+    # shellcheck disable=SC2181
+    if [ $? -eq 0 ]; then
+      patched=$((patched + 1))
+    else
+      exit_code=1
+      continue
+    fi
+
+    if $VERBOSE; then
+      echo "       $output" >&2
     fi
-  done
 
-  echo "  → $kind migration complete"
+    percent=$(( (patched * 100 + total / 2) / total ))
+    printf "  → Progress: %3d%% (%d/%d)\r" "${percent}" "${patched}" "${total}"
```

  Now, when running `./hack/migrate-to-v1beta2.sh > progress.log`, you can see logs only for each percentage.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-19T09:37:00Z

- **hack/migrate-to-v1beta2.sh:139** by [@mimowo](https://github.com/mimowo) — 2026-01-19T09:37:00Z
```diff
@@ -35,33 +64,94 @@ kinds=(
   workloads
 )
 
+patch_payload='{"apiVersion":"kueue.x-k8s.io/v1beta2"}'
+
+exit_code=0
+
 for kind in "${kinds[@]}"; do
-  echo "Migrating $kind.kueue.x-k8s.io ..."
+  echo "Migrating $kind.kueue.x-k8s.io..."
+
+  resources_cmd="kubectl get \"${kind}.kueue.x-k8s.io\" -A -o jsonpath='{range .items[*]}{.metadata.namespace}{\"/\"}{.metadata.name}{\"\n\"}{end}'"
+  if $VERBOSE; then
+    echo "  → ${resources_cmd}"
+  fi
 
   # Get resources in namespace/name format
-  resources=$(kubectl get "$kind.kueue.x-k8s.io" -A -o jsonpath='{range .items[*]}{.metadata.namespace}{"/"}{.metadata.name}{"\n"}{end}')
+  resources=$(eval "$resources_cmd")
+  # shellcheck disable=SC2181
+  if [ $? -ne 0 ]; then
+    echo "" >&2
+    exit_code=1
+    continue
+  fi
+
+  if [[ -z "${resources}" ]]; then
+    echo "  → No $kind found, skipping."
+    echo ""
+    continue
+  fi
+
+  filtered=$(echo "${resources}" | grep -v '^$' | grep -v '^/$')
+  total=$(echo "${filtered}" | wc -l | tr -d '[:space:]')
 
-  if [[ -z "$resources" ]]; then
-    echo "  → No $kind found, skipping"
+  if [[ -z "${total}" ]] || (( total == 0 )); then
+    echo "  → No valid ${kind} found, skipping."
+    echo ""
     continue
   fi
 
-  echo "$resources" | while read -r entry; do
-    [[ -z "$entry" || "$entry" == "/" ]] && continue
+  echo "  → Found: ${total} object(s)"
 
+  patched=0
+  percent=0
+
+  while read -r entry; do
     ns="${entry%/*}"
     name="${entry#*/}"
 
-    if [[ -z "$ns" ]]; then
-      # Cluster-scoped resource
-      kubectl patch "$kind.kueue.x-k8s.io" "$name" --type=merge -p '{"apiVersion":"kueue.x-k8s.io/v1beta2"}'
-    else
+    patch_cmd=(kubectl patch "$kind.kueue.x-k8s.io" "$name")
+    if [[ -n "$ns" ]]; then
       # Namespaced resource
-      kubectl patch "$kind.kueue.x-k8s.io" "$name" -n "$ns" --type=merge -p '{"apiVersion":"kueue.x-k8s.io/v1beta2"}'
+      patch_cmd+=(-n "$ns")
+    fi
+
+    # shellcheck disable=SC2206
+    patch_cmd+=(--type=merge -p "$patch_payload" $DRY_RUN)
+
+    if $VERBOSE; then
+        echo "    └─ ${patch_cmd[*]}"
+    fi
+
+    output=$("${patch_cmd[@]}")
+    # shellcheck disable=SC2181
+    if [ $? -eq 0 ]; then
+      patched=$((patched + 1))
+    else
+      exit_code=1
+      continue
+    fi
+
+    if $VERBOSE; then
+      echo "       $output" >&2
     fi
-  done
 
-  echo "  → $kind migration complete"
+    percent=$(( (patched * 100 + total / 2) / total ))
+    printf "  → Progress: %3d%% (%d/%d)\r" "${percent}" "${patched}" "${total}"
```

  Yes it will be useful if somwone wants to redirect to file for example
