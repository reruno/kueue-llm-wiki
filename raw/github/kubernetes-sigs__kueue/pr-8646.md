# PR #8646: KEP-8522: Add time-based same-priority preemption proposal

**Summary**: KEP-8522: Add time-based same-priority preemption proposal

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8646

**Last updated**: 2026-04-08T20:29:35Z

---

## Metadata

- **State**: open
- **Author**: [@sohankunkerkar](https://github.com/sohankunkerkar)
- **Branch**: `kep-8522` → `main`
- **Created**: 2026-01-16T22:01:15Z
- **Updated**: 2026-04-08T20:29:35Z
- **Merged**: —
- **Closed**: —
- **Labels**: `kind/feature`, `size/L`, `release-note-none`, `kind/documentation`, `cncf-cla: yes`, `kind/kep`
- **Assignees**: _none_
- **Requested reviewers**: _none_
- **Changed files**: 2   **+392 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation
/kind feature

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Related to #8522 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-16T22:01:24Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8646#" title="Author self-approved">sohankunkerkar</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [gabesaba](https://github.com/gabesaba) for approval. For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["gabesaba"]} -->

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-01-16T22:01:28Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 01508081e1f8252faf28aaf58ac0f5a5f3f244b9 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69a750e089429200084dfe71 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-8646--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-8646--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTg2NDYtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.9DVoWSeJvOaS-a-KbSG1W_NUJPiL4cVlW3832CJnvn4)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-8646--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-18T17:45:18Z

cc @Ladicle @dddwsd @mimowo

### Comment by [@Ladicle](https://github.com/Ladicle) — 2026-01-26T07:41:06Z

Thanks for the PR! I have a few questions regarding the behavior when Hierarchical Cohorts (HC) are enabled.

I'm curious if the following scenarios are covered in the current scope:
- Cross-CQ Reclaim: Can a workload in CQ-A preempt a workload of the same priority in CQ-B, provided the target has exceeded its minAdmitDuration?
- Within-CQ Protection: To avoid unnecessary churn within the same team, can we protect a new workload that does not preempt an existing one within the same CQ (e.g., CQ-B), even if minAdmitDuration has passed?

### Comment by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-27T03:34:20Z

>Cross-CQ Reclaim: Can a workload in CQ-A preempt a workload of the same priority in CQ-B, provided the target has exceeded its minAdmitDuration?

No, this is explicitly out of scope for this KEP. The `minAdmitDuration` configuration only applies to `withinClusterQueue` preemption. Cross-CQ preemption (including reclaim scenarios in Hierarchical Cohorts) continues to be operated by existing Fair Sharing algorithms and the `reclaimWithinCohort`/`borrowWithinCohort` policies.

 If cross-CQ time-based protection is needed, the semantics would require careful design. A few open questions:                                                             
                                                                                                                                                                             
1. Whose `minAdmitDuration` applies? If CQ-A (minAdmitDuration: 2h) is reclaiming from CQ-B (minAdmitDuration: 4h), which threshold governs whether CQ-B's workload is protected?                                                                                                                                                                 
2. Should reclaim of own quota be subject to `minAdmitDuration`? If CQ-A lent resources to CQ-B, should CQ-A have to wait before reclaiming its own nominal quota?           
3. Where would this be configured? On the Cohort? On the borrowing CQ? On reclaimWithinCohort?                                                                             
                                                                                                                                                                             
If you have a concrete use case for cross-CQ time-based protection, I'd love to hear more details.

>Within-CQ Protection: To avoid unnecessary churn within the same team, can we protect a new workload that does not preempt an existing one within the same CQ (e.g., CQ-B), even if minAdmitDuration has passed?

 Yes, the new workload is protected by its own `minAdmitDuration`. Each workload's protection is based on its own quota reservation time, not on how other workloads in the CQ are doing. So the same-priority workload can be preempted if either:                                                                                                                                                       
  1. It was admitted after the preemptor was created (existing LowerOrNewerEqualPriority behavior)                                                                           
  2. It has exceeded `minAdmitDuration` (new behavior)

### Comment by [@Ladicle](https://github.com/Ladicle) — 2026-01-27T08:03:21Z

Thanks for the reply!

In our environment, each project has its own ClusterQueue (CQ), which shares resources via a Hierarchical Cohort. Here's our scenarios:
- Cross-CQ (Fairness): To ensure fairness, we want to prevent a single project from monopolizing shared resources for too long. Therefore, we would like to allow cross-CQ preemption for same-priority workloads once they have exceeded their `minAdmitDuration`.
- Within-CQ (Stability/FIFO): On the other hand, within a single project (the same CQ), we prefer FIFO-like behavior. We want to avoid preempting workloads within the same CQ to minimize unnecessary stops, even if they have already passed the `minAdmitDuration` threshold.

> 3. Where would this be configured? On the Cohort? On the borrowing CQ? On reclaimWithinCohort?

For our specific use case, sharing `minAdmitDuration` at the Cohort level would be enough.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-27T16:38:43Z

/kind kep
Adding new label for the ease of filtering PRs which are KEPs  / designs are require special handling

### Comment by [@kannon92](https://github.com/kannon92) — 2026-03-01T18:51:55Z

https://github.com/kubernetes-sigs/kueue/pull/9120

I'm thinking we should consider this KEP for the design of this feature.

Or at least figure out what is Kueue's plan for configurable preemtpion features. It seems we want to encourage external controllers to change priority of preemption.

### Comment by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-03T22:13:41Z

> #9120
> 
> I'm thinking we should consider this KEP for the design of this feature.
> 
> Or at least figure out what is Kueue's plan for configurable preemtpion features. It seems we want to encourage external controllers to change priority of preemption.

 Thanks for flagging this. I looked at #9120 and here's my findings:                                                                     
  - minAdmitDuration determines which same-priority workloads become eligible for preemption (the eligibility set)                                
  - Preemption cost (#9120) determines which candidate gets preempted first among eligible ones (the ordering)

They operate at different stages of the preemption algorithm and don't conflict. A workload that has exceeded `minAdmitDuration` enters the candidate pool, and if preemption cost is also configured, the cost ordering applies within that pool.
I added a `Future Extensibility` section to the KEP to make it clear that `WithinClusterQueueConfig` is designed to accommodate additional policies (like the max-duration immunity you mentioned [here](https://github.com/kubernetes-sigs/kueue/pull/8646#discussion_r2856473825)) without API breakage.

## Reviews

### Review by [@copilot-pull-request-reviewer[bot]](https://github.com/apps/copilot-pull-request-reviewer) (commented) — 2026-01-16T22:03:53Z

## Pull request overview

This KEP proposes time-based same-priority preemption to address resource monopolization in Kueue. The enhancement allows workloads to preempt other equal-priority workloads after they have been admitted for a minimum duration (`minAdmitDuration`), preventing indefinite resource blocking by the first admitted workload.

**Changes:**
- Adds KEP-8522 documentation describing the time-based same-priority preemption feature
- Introduces a new `withinClusterQueueConfig` configuration structure with a `minAdmitDuration` field
- Proposes a new metric reason label `InClusterQueueTimeBased` to track time-based preemptions

### Reviewed changes

Copilot reviewed 2 out of 2 changed files in this pull request and generated 1 comment.

| File | Description |
| ---- | ----------- |
| keps/8522-time-based-same-priority-preemption/kep.yaml | KEP metadata defining the feature's lifecycle, maturity stage (beta), milestones, feature gate name, and associated metrics |
| keps/8522-time-based-same-priority-preemption/README.md | Complete KEP document detailing motivation, proposal, API changes, test plan, and graduation criteria for time-based same-priority preemption |





---

💡 <a href="/kubernetes-sigs/kueue/new/main/.github/instructions?filename=*.instructions.md" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Add Copilot custom instructions</a> for smarter, more guided reviews. <a href="https://docs.github.com/en/copilot/customizing-copilot/adding-repository-custom-instructions-for-github-copilot" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Learn how to get started</a>.

- **keps/8522-time-based-same-priority-preemption/README.md:195** by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer) — 2026-01-16T22:03:53Z
```diff
@@ -0,0 +1,336 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative 1: Dynamic Priority Decay](#alternative-1-dynamic-priority-decay)
+  - [Alternative 2: Rely on Admission Fair Sharing Alone](#alternative-2-rely-on-admission-fair-sharing-alone)
+  - [Alternative 3: Usage-Based Preemption](#alternative-3-usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
+resource monopolization problem where the first workload admitted to a
+ClusterQueue continues to run indefinitely while same-priority workloads
+remain pending.
+
+**Terminology**: Throughout this KEP, "quota reservation time" refers to the
+`LastTransitionTime` of the workload's `QuotaReserved` condition (when
+`Status=True`). This is the moment when quota was reserved for the workload.
+
+## Motivation
+
+Currently, Kueue offers two preemption policies for `withinClusterQueue`:
+
+1. **`LowerPriority`**: Only preempt workloads with strictly lower priority
+2. **`LowerOrNewerEqualPriority`**: Preempt workloads with lower priority OR
+   workloads with equal priority that were admitted more recently
+
+The `LowerOrNewerEqualPriority` policy was designed to prevent queue starvation
+by allowing newly arrived high-demand workloads to preempt recently admitted
+same-priority workloads. However, this creates a subtle but significant
+problem: **the first workload admitted monopolizes resources indefinitely**.
+
+Consider this scenario:
+- WorkloadA (priority=10) is admitted at T=0
+- WorkloadB (priority=10) arrives at T=5min
+- WorkloadB cannot preempt WorkloadA because WorkloadA is older
+- WorkloadA runs for 24 hours while WorkloadB waits
+
+This behavior contradicts the expectation that same-priority workloads should
+have fair access to cluster resources over time. In multi-tenant environments,
+this can lead to:
+
+- One team's long-running workload blocking other teams indefinitely
+- Users submitting artificially low resource requests to get admitted first,
+  then consuming resources for extended periods
+- Reduced cluster utilization efficiency due to suboptimal scheduling
+
+### Goals
+
+- Extend the `LowerOrNewerEqualPriority` preemption policy with a
+  `minAdmitDuration` configuration that allows preemption of same-priority
+  workloads after they have been admitted for a specified duration
+- Maintain backward compatibility: existing configurations without
+  `minAdmitDuration` behave exactly as before
+
+### Non-Goals
+
+- Dynamic priority that decays over time
+- Per-workload or per-LocalQueue `minAdmitDuration` overrides
+- Precise CPU/GPU time accounting (wall-clock time from admission is used)
+- Cross-ClusterQueue time-based preemption (this feature is scoped to
+  `withinClusterQueue` only)
+
+## Proposal
+
+Introduce a new configuration structure `WithinClusterQueueConfig` that extends
+the `withinClusterQueue` preemption policy with a `minAdmitDuration` field.
+When `withinClusterQueue` is set to `LowerOrNewerEqualPriority` and
+`minAdmitDuration` is configured, workloads become eligible for same-priority
+preemption only after they have been admitted for at least the specified
+duration.
+
+### User Stories
+
+#### Story 1: Fair GPU Time-Sharing Among Data Scientists
+
+As a cluster administrator managing GPU resources for a team of 20 data
+scientists, I want to ensure that each team member gets fair access to GPU
+resources for their training jobs, all of which have the same priority.
+
+Currently, if one data scientist submits a job that starts training and runs
+for days, other team members' jobs remain pending indefinitely. With
+`minAdmitDuration: 4h`, each job is guaranteed 4 hours of runtime before it
+can be preempted by a waiting job.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: ml-training
+spec:
+  preemption:
+    withinClusterQueue: LowerOrNewerEqualPriority
+    withinClusterQueueConfig:
+      minAdmitDuration: 4h
+```
+
+#### Story 2: Preventing Resource Monopolization in Shared Clusters
+
+As a platform team managing a shared Kubernetes cluster for multiple teams,
+I want to prevent any single team from monopolizing cluster resources with
+long-running jobs while ensuring jobs have enough time to make meaningful
+progress.
+
+By setting `minAdmitDuration: 2h`, we ensure that:
+- Jobs have at least 2 hours to run and checkpoint
+- No single job can block others indefinitely
+- Resource access rotates fairly among all pending workloads
+
+### Notes/Constraints/Caveats (Optional)
+
+The `minAdmitDuration` field modifies the behavior of the
+`LowerOrNewerEqualPriority` policy. Without `minAdmitDuration`, the policy
+only preempts workloads that are newer than the preempting workload. With
+`minAdmitDuration`, it additionally preempts workloads that have exceeded
+the minimum duration, regardless of relative age.
+
+This feature is complementary to Fair Sharing (KEP-1714). Fair Sharing
+addresses resource distribution across different ClusterQueues/Cohorts via
+preemption, while this feature addresses time-sharing among workloads with
+identical priority within the same ClusterQueue.
+
+**Interaction with Fair Sharing**: When both Fair Sharing and time-based
+preemption are enabled, time-based preemption only applies to the
+`withinClusterQueue` policy. Cross-ClusterQueue preemption continues to be
+governed by Fair Sharing algorithms. The two mechanisms operate independently
+and do not conflict.
+
+This is similar to Slurm's `PreemptExemptTime` which provides a guaranteed
+minimum runtime before a job becomes preemptible.
+
+### Risks and Mitigations
+
+**Rapid Preemption Cycles**: If `minAdmitDuration` is set too low, workloads
+might be preempted before making meaningful progress. Mitigated by webhook
+validation requiring a minimum of 1 minute.
+
+**Checkpoint Dependency**: Workloads that don't implement checkpointing lose
+progress when preempted. This is not new; workloads already need to handle
+preemption gracefully.
+
+**Controller Restart**: If the Kueue controller restarts, all timing is based
+on the persisted `QuotaReserved` condition timestamps stored in workload
+status, so workloads maintain their accurate admission time across restarts.
+No special handling is required.
+
+## Design Details
+
+### API Changes
+
+Add a new optional field `withinClusterQueueConfig` to `ClusterQueuePreemption`:
+
+```go
+type ClusterQueuePreemption struct {
+	ReclaimWithinCohort PreemptionPolicy `json:"reclaimWithinCohort,omitempty"`
+	BorrowWithinCohort  *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+	WithinClusterQueue  PreemptionPolicy `json:"withinClusterQueue,omitempty"`
+
+	// withinClusterQueueConfig provides additional configuration for the
+	// withinClusterQueue preemption policy. Only valid when withinClusterQueue
+	// is set to LowerOrNewerEqualPriority.
+	// +optional
+	WithinClusterQueueConfig *WithinClusterQueueConfig `json:"withinClusterQueueConfig,omitempty"`
+}
+
+type WithinClusterQueueConfig struct {
+	// minAdmitDuration specifies the minimum duration that a workload must be
+	// admitted before it becomes eligible for same-priority preemption.
+	// The duration is measured from the workload's quota reservation time.
+	// Minimum allowed value is 1 minute.
```

  The documentation should clarify what happens when minAdmitDuration is nil versus explicitly set to 0 or omitted, since the webhook validation section mentions that nil is valid but has no effect.
  ```suggestion
  	// If this field is nil or omitted, time-based same-priority preemption
  	// (based on admit duration) is disabled and only the existing behavior
  	// applies. When set, the minimum allowed value is 1 minute; values less
  	// than 1 minute (including 0) are rejected by validation.
  ```

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-02-26T01:59:31Z

- **keps/8522-time-based-same-priority-preemption/README.md:206** by [@kannon92](https://github.com/kannon92) — 2026-02-26T01:59:31Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative 1: Dynamic Priority Decay](#alternative-1-dynamic-priority-decay)
+  - [Alternative 2: Rely on Admission Fair Sharing Alone](#alternative-2-rely-on-admission-fair-sharing-alone)
+  - [Alternative 3: Usage-Based Preemption](#alternative-3-usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
+resource monopolization problem where the first workload admitted to a
+ClusterQueue continues to run indefinitely while same-priority workloads
+remain pending.
+
+**Terminology**: Throughout this KEP, "quota reservation time" refers to the
+`LastTransitionTime` of the workload's `QuotaReserved` condition (when
+`Status=True`). This is the moment when quota was reserved for the workload.
+
+## Motivation
+
+Currently, Kueue offers two preemption policies for `withinClusterQueue`:
+
+1. **`LowerPriority`**: Only preempt workloads with strictly lower priority
+2. **`LowerOrNewerEqualPriority`**: Preempt workloads with lower priority OR
+   workloads with equal priority that were admitted more recently
+
+The `LowerOrNewerEqualPriority` policy was designed to prevent queue starvation
+by allowing newly arrived high-demand workloads to preempt recently admitted
+same-priority workloads. However, this creates a subtle but significant
+problem: **the first workload admitted monopolizes resources indefinitely**.
+
+Consider this scenario:
+- WorkloadA (priority=10) is admitted at T=0
+- WorkloadB (priority=10) arrives at T=5min
+- WorkloadB cannot preempt WorkloadA because WorkloadA is older
+- WorkloadA runs for 24 hours while WorkloadB waits
+
+This behavior contradicts the expectation that same-priority workloads should
+have fair access to cluster resources over time. In multi-tenant environments,
+this can lead to:
+
+- One team's long-running workload blocking other teams indefinitely
+- Users submitting artificially low resource requests to get admitted first,
+  then consuming resources for extended periods
+- Reduced cluster utilization efficiency due to suboptimal scheduling
+
+### Goals
+
+- Extend the `LowerOrNewerEqualPriority` preemption policy with a
+  `minAdmitDuration` configuration that allows preemption of same-priority
+  workloads after they have been admitted for a specified duration
+- Maintain backward compatibility: existing configurations without
+  `minAdmitDuration` behave exactly as before
+
+### Non-Goals
+
+- Dynamic priority that decays over time
+- Per-workload or per-LocalQueue `minAdmitDuration` overrides
+- Precise CPU/GPU time accounting (wall-clock time from admission is used)
+- Cross-ClusterQueue time-based preemption (this feature is scoped to
+  `withinClusterQueue` only)
+
+## Proposal
+
+Introduce a new configuration structure `WithinClusterQueueConfig` that extends
+the `withinClusterQueue` preemption policy with a `minAdmitDuration` field.
+When `withinClusterQueue` is set to `LowerOrNewerEqualPriority` and
+`minAdmitDuration` is configured, workloads become eligible for same-priority
+preemption only after they have been admitted for at least the specified
+duration.
+
+### User Stories
+
+#### Story 1: Fair GPU Time-Sharing Among Data Scientists
+
+As a cluster administrator managing GPU resources for a team of 20 data
+scientists, I want to ensure that each team member gets fair access to GPU
+resources for their training jobs, all of which have the same priority.
+
+Currently, if one data scientist submits a job that starts training and runs
+for days, other team members' jobs remain pending indefinitely. With
+`minAdmitDuration: 4h`, each job is guaranteed 4 hours of runtime before it
+can be preempted by a waiting job.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: ml-training
+spec:
+  preemption:
+    withinClusterQueue: LowerOrNewerEqualPriority
+    withinClusterQueueConfig:
+      minAdmitDuration: 4h
+```
+
+#### Story 2: Preventing Resource Monopolization in Shared Clusters
+
+As a platform team managing a shared Kubernetes cluster for multiple teams,
+I want to prevent any single team from monopolizing cluster resources with
+long-running jobs while ensuring jobs have enough time to make meaningful
+progress.
+
+By setting `minAdmitDuration: 2h`, we ensure that:
+- Jobs have at least 2 hours to run and checkpoint
+- No single job can block others indefinitely
+- Resource access rotates fairly among all pending workloads
+
+### Notes/Constraints/Caveats (Optional)
+
+The `minAdmitDuration` field modifies the behavior of the
+`LowerOrNewerEqualPriority` policy. Without `minAdmitDuration`, the policy
+only preempts workloads that are newer than the preempting workload. With
+`minAdmitDuration`, it additionally preempts workloads that have exceeded
+the minimum duration, regardless of relative age.
+
+This feature is complementary to Fair Sharing (KEP-1714). Fair Sharing
+addresses resource distribution across different ClusterQueues/Cohorts via
+preemption, while this feature addresses time-sharing among workloads with
+identical priority within the same ClusterQueue.
+
+**Interaction with Fair Sharing**: When both Fair Sharing and time-based
+preemption are enabled, time-based preemption only applies to the
+`withinClusterQueue` policy. Cross-ClusterQueue preemption continues to be
+governed by Fair Sharing algorithms. The two mechanisms operate independently
+and do not conflict.
+
+This is similar to Slurm's `PreemptExemptTime` which provides a guaranteed
+minimum runtime before a job becomes preemptible.
+
+### Risks and Mitigations
+
+**Rapid Preemption Cycles**: If `minAdmitDuration` is set too low, workloads
+might be preempted before making meaningful progress. Mitigated by webhook
+validation requiring a minimum of 1 minute.
+
+**Checkpoint Dependency**: Workloads that don't implement checkpointing lose
+progress when preempted. This is not new; workloads already need to handle
+preemption gracefully.
+
+**Controller Restart**: If the Kueue controller restarts, all timing is based
+on the persisted `QuotaReserved` condition timestamps stored in workload
+status, so workloads maintain their accurate admission time across restarts.
+No special handling is required.
+
+**Scheduler Re-evaluation**: When a workload crosses `minAdmitDuration` while
+the scheduler is idle, preemption happens on the next scheduling cycle.
+For typical `minAdmitDuration` values (30m+), scheduler cycles are usually
+frequent enough due to other cluster events. If tighter responsiveness is
+required, a future enhancement could schedule re-evaluation at the earliest
+expiration time.
+
+**Requeue Position**: Preempted workloads follow the existing eviction path.
+With `BestEffortFIFO` and `Eviction` requeue timestamp (in default/common
+configuration), the workload moves to the back of the queue, enabling
+round-robin rotation. When re-admitted, the `QuotaReserved` timestamp resets,
+restarting the `minAdmitDuration` timer.
+
+## Design Details
+
+### API Changes
+
+Add a new optional field `withinClusterQueueConfig` to `ClusterQueuePreemption`:
+
+```go
+type ClusterQueuePreemption struct {
+    ReclaimWithinCohort PreemptionPolicy    `json:"reclaimWithinCohort,omitempty"`
+    BorrowWithinCohort  *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+    WithinClusterQueue  PreemptionPolicy    `json:"withinClusterQueue,omitempty"`
+
+    // withinClusterQueueConfig provides additional configuration for the
+    // withinClusterQueue preemption policy. Only valid when withinClusterQueue
+    // is set to LowerOrNewerEqualPriority.
+    // +optional
+    WithinClusterQueueConfig *WithinClusterQueueConfig `json:"withinClusterQueueConfig,omitempty"`
+}
+
+type WithinClusterQueueConfig struct {
```

  I don't think this needs to be solved for this KEP but I have another use case actually.
  
  I was discussing with a customer that they would like a way to control preemption based on how long it has been admitted.
  
  Say if a job has run for 24 hours it should no longer be considered with preemption after it hits 24 hours.
  
  Do you think it could be possible to consider this in design or at least think through how these policies could work together?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-02-26T02:00:20Z

I had one minor item but I think this is a well thought through proposal and a good addition to the project.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-06T20:17:39Z

It is an important feature, but also complex, and the timeline for the timeline for 0.17 is very close. I would prefer to move it 0.18. You are free to start prototyping to de-risk the 0.18 release.

### Review by [@mukund-wayve](https://github.com/mukund-wayve) (commented) — 2026-03-14T07:23:46Z

- **keps/8522-time-based-same-priority-preemption/README.md:77** by [@mukund-wayve](https://github.com/mukund-wayve) — 2026-03-14T07:23:19Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Future Extensibility](#future-extensibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dynamic Priority Decay](#dynamic-priority-decay)
+  - [Rely on Admission Fair Sharing Alone](#rely-on-admission-fair-sharing-alone)
+  - [Usage-Based Preemption](#usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
+resource monopolization problem where the first workload admitted to a
+ClusterQueue continues to run indefinitely while same-priority workloads
+remain pending.
+
+**Terminology**: Throughout this KEP, "quota reservation time" refers to the
+`LastTransitionTime` of the workload's `QuotaReserved` condition (when
+`Status=True`). This is the moment when quota was reserved for the workload.
+
+## Motivation
+
+Currently, Kueue offers two preemption policies for `withinClusterQueue`:
+
+1. **`LowerPriority`**: Only preempt workloads with strictly lower priority
+2. **`LowerOrNewerEqualPriority`**: Preempt workloads with lower priority OR
+   workloads with equal priority that were admitted more recently
+
+The `LowerOrNewerEqualPriority` policy was designed to prevent queue starvation
+by allowing newly arrived high-demand workloads to preempt recently admitted
+same-priority workloads. However, this creates a subtle but significant
+problem: **the first workload admitted monopolizes resources indefinitely**.
+
+Consider this scenario:
+- WorkloadA (priority=10) is admitted at T=0
+- WorkloadB (priority=10) arrives at T=5min
+- WorkloadB cannot preempt WorkloadA because WorkloadA is older
+- WorkloadA runs for 24 hours while WorkloadB waits
+
+This behavior contradicts the expectation that same-priority workloads should
+have fair access to cluster resources over time. In multi-tenant environments,
+this can lead to:
+
+- One team's long-running workload blocking other teams indefinitely
+- Users submitting artificially low resource requests to get admitted first,
+  then consuming resources for extended periods
+- Reduced cluster utilization efficiency due to suboptimal scheduling
+
+### Goals
+
+- Extend the `LowerOrNewerEqualPriority` preemption policy with a
```

  Hi Sohan, this is really useful for us and an important feature we are after. Thanks for writing this KEP!
  
  But we have a use case where it would help to have two separate durations instead of one, because there are two different situations:
  
  `minAdmitDuration` — for workloads that were already in the queue BEFORE the pending workload arrived. They got in first, so they should get a longer guaranteed runtime (e.g., 4h).
  
  `opportunisticMinAdmitDuration` — for workloads that arrived AFTER the pending workload but got admitted first because they were small enough to fit. Today these can be preempted right away; this field would let them run for a short time first (e.g., 20m) so they can finish or make progress.
  
  With a single duration, we can't handle both cases well: if we set it high, small jobs that jumped the queue are protected for too long; if we set it low, jobs that were rightfully first don't get enough time.
  
  Example:
  
  ```
  withinClusterQueueConfig:
    minAdmitDuration: 2h
    opportunisticMinAdmitDuration: 20m
  ```
  WorkloadA (3 CPU) is admitted at T=0. WorkloadB (3 CPU) arrives at T=1m but can't fit, so it waits. WorkloadC (1 CPU) arrives at T=2m and fits in the remaining space, so it gets admitted. With this config, C can be preempted after 20m (it jumped the queue) and A after 2h (it was there first).
  
  ---
  
  An user story to capture this could be:
  
  **Story 3: Different Guarantees for Queue-Jumpers vs First-in-Line Jobs**
  
  As a cluster administrator, I want workloads that were first in line to get a longer guaranteed runtime than workloads that jumped the queue by fitting into gaps.
  
  Our ML team shares a queue with 8 GPUs. Large training jobs (8 GPUs) often wait while smaller experiment jobs (1-2 GPUs) get admitted into remaining capacity. With a single minAdmitDuration, we can't treat these cases differently. With two durations, we give first-in-line jobs 2 hours of guaranteed runtime while small jobs that jumped ahead only get 20 minutes — enough to finish or checkpoint, but not long enough to block the queue.

### Review by [@mukund-wayve](https://github.com/mukund-wayve) (commented) — 2026-03-14T07:36:08Z

- **keps/8522-time-based-same-priority-preemption/README.md:77** by [@mukund-wayve](https://github.com/mukund-wayve) — 2026-03-14T07:36:08Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Future Extensibility](#future-extensibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dynamic Priority Decay](#dynamic-priority-decay)
+  - [Rely on Admission Fair Sharing Alone](#rely-on-admission-fair-sharing-alone)
+  - [Usage-Based Preemption](#usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
+resource monopolization problem where the first workload admitted to a
+ClusterQueue continues to run indefinitely while same-priority workloads
+remain pending.
+
+**Terminology**: Throughout this KEP, "quota reservation time" refers to the
+`LastTransitionTime` of the workload's `QuotaReserved` condition (when
+`Status=True`). This is the moment when quota was reserved for the workload.
+
+## Motivation
+
+Currently, Kueue offers two preemption policies for `withinClusterQueue`:
+
+1. **`LowerPriority`**: Only preempt workloads with strictly lower priority
+2. **`LowerOrNewerEqualPriority`**: Preempt workloads with lower priority OR
+   workloads with equal priority that were admitted more recently
+
+The `LowerOrNewerEqualPriority` policy was designed to prevent queue starvation
+by allowing newly arrived high-demand workloads to preempt recently admitted
+same-priority workloads. However, this creates a subtle but significant
+problem: **the first workload admitted monopolizes resources indefinitely**.
+
+Consider this scenario:
+- WorkloadA (priority=10) is admitted at T=0
+- WorkloadB (priority=10) arrives at T=5min
+- WorkloadB cannot preempt WorkloadA because WorkloadA is older
+- WorkloadA runs for 24 hours while WorkloadB waits
+
+This behavior contradicts the expectation that same-priority workloads should
+have fair access to cluster resources over time. In multi-tenant environments,
+this can lead to:
+
+- One team's long-running workload blocking other teams indefinitely
+- Users submitting artificially low resource requests to get admitted first,
+  then consuming resources for extended periods
+- Reduced cluster utilization efficiency due to suboptimal scheduling
+
+### Goals
+
+- Extend the `LowerOrNewerEqualPriority` preemption policy with a
```

  This differentiation allows `minAdmitDuration` to be extended to lower priority preemption as well which is a requirement for us.
  
  So, essentially, rather than differentiating between equal priority or lower priority, the differentiation comes from whether preemption is for workloads were admitted AFTER the HOL workload or BEFORE and it applies to lower or same priority. 
  
  Maybe this KEP only targets the AFTER case and the BEFORE case is a new requirement I'm adding?

### Review by [@mukund-wayve](https://github.com/mukund-wayve) (commented) — 2026-03-14T09:27:42Z

- **keps/8522-time-based-same-priority-preemption/README.md:77** by [@mukund-wayve](https://github.com/mukund-wayve) — 2026-03-14T09:27:42Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Future Extensibility](#future-extensibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dynamic Priority Decay](#dynamic-priority-decay)
+  - [Rely on Admission Fair Sharing Alone](#rely-on-admission-fair-sharing-alone)
+  - [Usage-Based Preemption](#usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
+resource monopolization problem where the first workload admitted to a
+ClusterQueue continues to run indefinitely while same-priority workloads
+remain pending.
+
+**Terminology**: Throughout this KEP, "quota reservation time" refers to the
+`LastTransitionTime` of the workload's `QuotaReserved` condition (when
+`Status=True`). This is the moment when quota was reserved for the workload.
+
+## Motivation
+
+Currently, Kueue offers two preemption policies for `withinClusterQueue`:
+
+1. **`LowerPriority`**: Only preempt workloads with strictly lower priority
+2. **`LowerOrNewerEqualPriority`**: Preempt workloads with lower priority OR
+   workloads with equal priority that were admitted more recently
+
+The `LowerOrNewerEqualPriority` policy was designed to prevent queue starvation
+by allowing newly arrived high-demand workloads to preempt recently admitted
+same-priority workloads. However, this creates a subtle but significant
+problem: **the first workload admitted monopolizes resources indefinitely**.
+
+Consider this scenario:
+- WorkloadA (priority=10) is admitted at T=0
+- WorkloadB (priority=10) arrives at T=5min
+- WorkloadB cannot preempt WorkloadA because WorkloadA is older
+- WorkloadA runs for 24 hours while WorkloadB waits
+
+This behavior contradicts the expectation that same-priority workloads should
+have fair access to cluster resources over time. In multi-tenant environments,
+this can lead to:
+
+- One team's long-running workload blocking other teams indefinitely
+- Users submitting artificially low resource requests to get admitted first,
+  then consuming resources for extended periods
+- Reduced cluster utilization efficiency due to suboptimal scheduling
+
+### Goals
+
+- Extend the `LowerOrNewerEqualPriority` preemption policy with a
```

  I've created a [prototype PR](https://github.com/kubernetes-sigs/kueue/pull/9874/changes#diff-42d1c79488d61febf1bcdea04e7e78ff45c666a98325a309e17ec32f1d40b147R70).
  
  Two differences from the KEP as written:
  
  1. Two durations instead of one. Instead of a single minAdmitDuration, the prototype has minAdmitDurationSeconds for incumbent workloads (queued before the preemptor) and opportunisticMinAdmitDurationSeconds for workloads that jumped ahead (queued after the preemptor but admitted first, e.g. via BestEffortFIFO). This lets you give a longer guaranteed runtime to workloads that legitimately earned their spot vs. ones that fit opportunistically.
  
  2. Works with **both policies**, not just LowerOrNewerEqualPriority. The KEP scopes withinClusterQueueConfig to LowerOrNewerEqualPriority only. The prototype applies the time check as a generic guard on top of both LowerPriority and LowerOrNewerEqualPriority -- since opportunistic admissions can happen under LowerPriority too with BestEffortFIFO. Each policy still decides which candidates are eligible using its existing rules; the time check just adds a minimum runtime before an eligible candidate can actually be preempted.
  
  The alternative would be separate min-runtime settings per priority relationship (equal vs. lower), but that feels like too many options. The incumbent/opportunistic split covers the main use case more naturally.
  
  I can raise a separate KEP for this if you want to avoid scope creep. But just want to highlight that this is a valid requirement for us and we might want to consider this generic setting.
  
  Let me know what you think.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-03-16T09:53:32Z

This seems to noticeably overlap with #9835 ...
At first glance, it feels that both efforts could be somehow merged?

cc @mbobrovskyi

- **keps/8522-time-based-same-priority-preemption/README.md:100** by [@olekzabl](https://github.com/olekzabl) — 2026-03-16T08:41:21Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Future Extensibility](#future-extensibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dynamic Priority Decay](#dynamic-priority-decay)
+  - [Rely on Admission Fair Sharing Alone](#rely-on-admission-fair-sharing-alone)
+  - [Usage-Based Preemption](#usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
+resource monopolization problem where the first workload admitted to a
+ClusterQueue continues to run indefinitely while same-priority workloads
+remain pending.
+
+**Terminology**: Throughout this KEP, "quota reservation time" refers to the
+`LastTransitionTime` of the workload's `QuotaReserved` condition (when
+`Status=True`). This is the moment when quota was reserved for the workload.
+
+## Motivation
+
+Currently, Kueue offers two preemption policies for `withinClusterQueue`:
+
+1. **`LowerPriority`**: Only preempt workloads with strictly lower priority
+2. **`LowerOrNewerEqualPriority`**: Preempt workloads with lower priority OR
+   workloads with equal priority that were admitted more recently
+
+The `LowerOrNewerEqualPriority` policy was designed to prevent queue starvation
+by allowing newly arrived high-demand workloads to preempt recently admitted
+same-priority workloads. However, this creates a subtle but significant
+problem: **the first workload admitted monopolizes resources indefinitely**.
+
+Consider this scenario:
+- WorkloadA (priority=10) is admitted at T=0
+- WorkloadB (priority=10) arrives at T=5min
+- WorkloadB cannot preempt WorkloadA because WorkloadA is older
+- WorkloadA runs for 24 hours while WorkloadB waits
+
+This behavior contradicts the expectation that same-priority workloads should
+have fair access to cluster resources over time. In multi-tenant environments,
+this can lead to:
+
+- One team's long-running workload blocking other teams indefinitely
+- Users submitting artificially low resource requests to get admitted first,
+  then consuming resources for extended periods
+- Reduced cluster utilization efficiency due to suboptimal scheduling
+
+### Goals
+
+- Extend the `LowerOrNewerEqualPriority` preemption policy with a
+  `minAdmitDuration` configuration that allows preemption of same-priority
+  workloads after they have been admitted for a specified duration
+- Maintain backward compatibility: existing configurations without
+  `minAdmitDuration` behave exactly as before
+
+### Non-Goals
+
+- Dynamic priority that decays over time
+- Per-workload or per-LocalQueue `minAdmitDuration` overrides
+- Precise CPU/GPU time accounting (wall-clock time from admission is used)
+- Cross-ClusterQueue time-based preemption (this feature is scoped to
+  `withinClusterQueue` only)
+
+## Proposal
+
+Introduce a new configuration structure `WithinClusterQueueConfig` that extends
+the `withinClusterQueue` preemption policy with a `minAdmitDuration` field.
+When `withinClusterQueue` is set to `LowerOrNewerEqualPriority` and
+`minAdmitDuration` is configured, workloads become eligible for same-priority
+preemption only after they have been admitted for at least the specified
+duration.
+
+### User Stories
```

  I can't help feeling that Story 1 is just a special case of Story 2.
  (Where "resource" boils down to "GPU", "team" boils down to a "single researcher" etc.)
  
  I'm curious, are there some differences or nuances which I've missed?

- **keps/8522-time-based-same-priority-preemption/README.md:143** by [@olekzabl](https://github.com/olekzabl) — 2026-03-16T08:43:42Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Future Extensibility](#future-extensibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dynamic Priority Decay](#dynamic-priority-decay)
+  - [Rely on Admission Fair Sharing Alone](#rely-on-admission-fair-sharing-alone)
+  - [Usage-Based Preemption](#usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
+resource monopolization problem where the first workload admitted to a
+ClusterQueue continues to run indefinitely while same-priority workloads
+remain pending.
+
+**Terminology**: Throughout this KEP, "quota reservation time" refers to the
+`LastTransitionTime` of the workload's `QuotaReserved` condition (when
+`Status=True`). This is the moment when quota was reserved for the workload.
+
+## Motivation
+
+Currently, Kueue offers two preemption policies for `withinClusterQueue`:
+
+1. **`LowerPriority`**: Only preempt workloads with strictly lower priority
+2. **`LowerOrNewerEqualPriority`**: Preempt workloads with lower priority OR
+   workloads with equal priority that were admitted more recently
+
+The `LowerOrNewerEqualPriority` policy was designed to prevent queue starvation
+by allowing newly arrived high-demand workloads to preempt recently admitted
+same-priority workloads. However, this creates a subtle but significant
+problem: **the first workload admitted monopolizes resources indefinitely**.
+
+Consider this scenario:
+- WorkloadA (priority=10) is admitted at T=0
+- WorkloadB (priority=10) arrives at T=5min
+- WorkloadB cannot preempt WorkloadA because WorkloadA is older
+- WorkloadA runs for 24 hours while WorkloadB waits
+
+This behavior contradicts the expectation that same-priority workloads should
+have fair access to cluster resources over time. In multi-tenant environments,
+this can lead to:
+
+- One team's long-running workload blocking other teams indefinitely
+- Users submitting artificially low resource requests to get admitted first,
+  then consuming resources for extended periods
+- Reduced cluster utilization efficiency due to suboptimal scheduling
+
+### Goals
+
+- Extend the `LowerOrNewerEqualPriority` preemption policy with a
+  `minAdmitDuration` configuration that allows preemption of same-priority
+  workloads after they have been admitted for a specified duration
+- Maintain backward compatibility: existing configurations without
+  `minAdmitDuration` behave exactly as before
+
+### Non-Goals
+
+- Dynamic priority that decays over time
+- Per-workload or per-LocalQueue `minAdmitDuration` overrides
+- Precise CPU/GPU time accounting (wall-clock time from admission is used)
+- Cross-ClusterQueue time-based preemption (this feature is scoped to
+  `withinClusterQueue` only)
+
+## Proposal
+
+Introduce a new configuration structure `WithinClusterQueueConfig` that extends
+the `withinClusterQueue` preemption policy with a `minAdmitDuration` field.
+When `withinClusterQueue` is set to `LowerOrNewerEqualPriority` and
+`minAdmitDuration` is configured, workloads become eligible for same-priority
+preemption only after they have been admitted for at least the specified
+duration.
+
+### User Stories
+
+#### Story 1: Fair GPU Time-Sharing Among Data Scientists
+
+As a cluster administrator managing GPU resources for a team of 20 data
+scientists, I want to ensure that each team member gets fair access to GPU
+resources for their training jobs, all of which have the same priority.
+
+Currently, if one data scientist submits a job that starts training and runs
+for days, other team members' jobs remain pending indefinitely. With
+`minAdmitDuration: 4h`, each job is guaranteed 4 hours of runtime before it
+can be preempted by a waiting job.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: ml-training
+spec:
+  preemption:
+    withinClusterQueue: LowerOrNewerEqualPriority
+    withinClusterQueueConfig:
+      minAdmitDuration: 4h
+```
+
+#### Story 2: Preventing Resource Monopolization in Shared Clusters
+
+As a platform team managing a shared Kubernetes cluster for multiple teams,
+I want to prevent any single team from monopolizing cluster resources with
+long-running jobs while ensuring jobs have enough time to make meaningful
+progress.
+
+By setting `minAdmitDuration: 2h`, we ensure that:
+- Jobs have at least 2 hours to run and checkpoint
+- No single job can block others indefinitely
+- Resource access rotates fairly among all pending workloads
+
+### Notes/Constraints/Caveats (Optional)
+
+The `minAdmitDuration` field modifies the behavior of the
+`LowerOrNewerEqualPriority` policy. Without `minAdmitDuration`, the policy
+only preempts workloads that are newer than the preempting workload. With
+`minAdmitDuration`, it additionally preempts workloads that have exceeded
+the minimum duration, regardless of relative age.
```

  Nit, optional.
  I understand it might feel like "irritating repetition", though I'm feeling for me it still helps.
  
  ```suggestion
  The `minAdmitDuration` field modifies the behavior of the
  `LowerOrNewerEqualPriority` policy. Without `minAdmitDuration`, the policy
  only preempts (within same priority) workloads that are newer than the preempting workload. With
  `minAdmitDuration`, it additionally preempts same-priority workloads that have exceeded
  the minimum duration, regardless of relative age.
  ```

- **keps/8522-time-based-same-priority-preemption/README.md:154** by [@olekzabl](https://github.com/olekzabl) — 2026-03-16T08:47:03Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Future Extensibility](#future-extensibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dynamic Priority Decay](#dynamic-priority-decay)
+  - [Rely on Admission Fair Sharing Alone](#rely-on-admission-fair-sharing-alone)
+  - [Usage-Based Preemption](#usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
+resource monopolization problem where the first workload admitted to a
+ClusterQueue continues to run indefinitely while same-priority workloads
+remain pending.
+
+**Terminology**: Throughout this KEP, "quota reservation time" refers to the
+`LastTransitionTime` of the workload's `QuotaReserved` condition (when
+`Status=True`). This is the moment when quota was reserved for the workload.
+
+## Motivation
+
+Currently, Kueue offers two preemption policies for `withinClusterQueue`:
+
+1. **`LowerPriority`**: Only preempt workloads with strictly lower priority
+2. **`LowerOrNewerEqualPriority`**: Preempt workloads with lower priority OR
+   workloads with equal priority that were admitted more recently
+
+The `LowerOrNewerEqualPriority` policy was designed to prevent queue starvation
+by allowing newly arrived high-demand workloads to preempt recently admitted
+same-priority workloads. However, this creates a subtle but significant
+problem: **the first workload admitted monopolizes resources indefinitely**.
+
+Consider this scenario:
+- WorkloadA (priority=10) is admitted at T=0
+- WorkloadB (priority=10) arrives at T=5min
+- WorkloadB cannot preempt WorkloadA because WorkloadA is older
+- WorkloadA runs for 24 hours while WorkloadB waits
+
+This behavior contradicts the expectation that same-priority workloads should
+have fair access to cluster resources over time. In multi-tenant environments,
+this can lead to:
+
+- One team's long-running workload blocking other teams indefinitely
+- Users submitting artificially low resource requests to get admitted first,
+  then consuming resources for extended periods
+- Reduced cluster utilization efficiency due to suboptimal scheduling
+
+### Goals
+
+- Extend the `LowerOrNewerEqualPriority` preemption policy with a
+  `minAdmitDuration` configuration that allows preemption of same-priority
+  workloads after they have been admitted for a specified duration
+- Maintain backward compatibility: existing configurations without
+  `minAdmitDuration` behave exactly as before
+
+### Non-Goals
+
+- Dynamic priority that decays over time
+- Per-workload or per-LocalQueue `minAdmitDuration` overrides
+- Precise CPU/GPU time accounting (wall-clock time from admission is used)
+- Cross-ClusterQueue time-based preemption (this feature is scoped to
+  `withinClusterQueue` only)
+
+## Proposal
+
+Introduce a new configuration structure `WithinClusterQueueConfig` that extends
+the `withinClusterQueue` preemption policy with a `minAdmitDuration` field.
+When `withinClusterQueue` is set to `LowerOrNewerEqualPriority` and
+`minAdmitDuration` is configured, workloads become eligible for same-priority
+preemption only after they have been admitted for at least the specified
+duration.
+
+### User Stories
+
+#### Story 1: Fair GPU Time-Sharing Among Data Scientists
+
+As a cluster administrator managing GPU resources for a team of 20 data
+scientists, I want to ensure that each team member gets fair access to GPU
+resources for their training jobs, all of which have the same priority.
+
+Currently, if one data scientist submits a job that starts training and runs
+for days, other team members' jobs remain pending indefinitely. With
+`minAdmitDuration: 4h`, each job is guaranteed 4 hours of runtime before it
+can be preempted by a waiting job.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: ml-training
+spec:
+  preemption:
+    withinClusterQueue: LowerOrNewerEqualPriority
+    withinClusterQueueConfig:
+      minAdmitDuration: 4h
+```
+
+#### Story 2: Preventing Resource Monopolization in Shared Clusters
+
+As a platform team managing a shared Kubernetes cluster for multiple teams,
+I want to prevent any single team from monopolizing cluster resources with
+long-running jobs while ensuring jobs have enough time to make meaningful
+progress.
+
+By setting `minAdmitDuration: 2h`, we ensure that:
+- Jobs have at least 2 hours to run and checkpoint
+- No single job can block others indefinitely
+- Resource access rotates fairly among all pending workloads
+
+### Notes/Constraints/Caveats (Optional)
+
+The `minAdmitDuration` field modifies the behavior of the
+`LowerOrNewerEqualPriority` policy. Without `minAdmitDuration`, the policy
+only preempts workloads that are newer than the preempting workload. With
+`minAdmitDuration`, it additionally preempts workloads that have exceeded
+the minimum duration, regardless of relative age.
+
+This feature is complementary to Fair Sharing (KEP-1714). Fair Sharing
+addresses resource distribution across different ClusterQueues/Cohorts via
+preemption, while this feature addresses time-sharing among workloads with
+identical priority within the same ClusterQueue.
+
+**Interaction with Fair Sharing**: When both Fair Sharing and time-based
+preemption are enabled, time-based preemption only applies to the
+`withinClusterQueue` policy. Cross-ClusterQueue preemption continues to be
+governed by Fair Sharing algorithms. The two mechanisms operate independently
+and do not conflict.
```

  Does it feel like a good place to mention the proposed #9877 ?

- **keps/8522-time-based-same-priority-preemption/README.md:38** by [@olekzabl](https://github.com/olekzabl) — 2026-03-16T08:59:21Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Future Extensibility](#future-extensibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dynamic Priority Decay](#dynamic-priority-decay)
+  - [Rely on Admission Fair Sharing Alone](#rely-on-admission-fair-sharing-alone)
+  - [Usage-Based Preemption](#usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
```

  I'm sensing a clash of concepts:
  
  - on one hand, you have `minAdmitDuration`, you also say "_admitted_ for a minimum duration", "_guaranteed runtime_" etc.
  - OTOH, you propose "_quota reservation_ time" as the moment of "starting the watch".
  
  
  IIUC this may significantly diverge e.g. if the CQ has multiple ResourceFlavors, _some_ of them based on a long-running AdmissionCheck (e.g. ProvisioningRequest; I heard these may take _hours_).
  
  So in such case, we could set `minAdmitDuration: 4h` but still have a (same-priority) workload preempted e.g. after 3h from becoming _admitted_.
  
  WDYT?

- **keps/8522-time-based-same-priority-preemption/README.md:208** by [@olekzabl](https://github.com/olekzabl) — 2026-03-16T09:12:40Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Future Extensibility](#future-extensibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dynamic Priority Decay](#dynamic-priority-decay)
+  - [Rely on Admission Fair Sharing Alone](#rely-on-admission-fair-sharing-alone)
+  - [Usage-Based Preemption](#usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
+resource monopolization problem where the first workload admitted to a
+ClusterQueue continues to run indefinitely while same-priority workloads
+remain pending.
+
+**Terminology**: Throughout this KEP, "quota reservation time" refers to the
+`LastTransitionTime` of the workload's `QuotaReserved` condition (when
+`Status=True`). This is the moment when quota was reserved for the workload.
+
+## Motivation
+
+Currently, Kueue offers two preemption policies for `withinClusterQueue`:
+
+1. **`LowerPriority`**: Only preempt workloads with strictly lower priority
+2. **`LowerOrNewerEqualPriority`**: Preempt workloads with lower priority OR
+   workloads with equal priority that were admitted more recently
+
+The `LowerOrNewerEqualPriority` policy was designed to prevent queue starvation
+by allowing newly arrived high-demand workloads to preempt recently admitted
+same-priority workloads. However, this creates a subtle but significant
+problem: **the first workload admitted monopolizes resources indefinitely**.
+
+Consider this scenario:
+- WorkloadA (priority=10) is admitted at T=0
+- WorkloadB (priority=10) arrives at T=5min
+- WorkloadB cannot preempt WorkloadA because WorkloadA is older
+- WorkloadA runs for 24 hours while WorkloadB waits
+
+This behavior contradicts the expectation that same-priority workloads should
+have fair access to cluster resources over time. In multi-tenant environments,
+this can lead to:
+
+- One team's long-running workload blocking other teams indefinitely
+- Users submitting artificially low resource requests to get admitted first,
+  then consuming resources for extended periods
+- Reduced cluster utilization efficiency due to suboptimal scheduling
+
+### Goals
+
+- Extend the `LowerOrNewerEqualPriority` preemption policy with a
+  `minAdmitDuration` configuration that allows preemption of same-priority
+  workloads after they have been admitted for a specified duration
+- Maintain backward compatibility: existing configurations without
+  `minAdmitDuration` behave exactly as before
+
+### Non-Goals
+
+- Dynamic priority that decays over time
+- Per-workload or per-LocalQueue `minAdmitDuration` overrides
+- Precise CPU/GPU time accounting (wall-clock time from admission is used)
+- Cross-ClusterQueue time-based preemption (this feature is scoped to
+  `withinClusterQueue` only)
+
+## Proposal
+
+Introduce a new configuration structure `WithinClusterQueueConfig` that extends
+the `withinClusterQueue` preemption policy with a `minAdmitDuration` field.
+When `withinClusterQueue` is set to `LowerOrNewerEqualPriority` and
+`minAdmitDuration` is configured, workloads become eligible for same-priority
+preemption only after they have been admitted for at least the specified
+duration.
+
+### User Stories
+
+#### Story 1: Fair GPU Time-Sharing Among Data Scientists
+
+As a cluster administrator managing GPU resources for a team of 20 data
+scientists, I want to ensure that each team member gets fair access to GPU
+resources for their training jobs, all of which have the same priority.
+
+Currently, if one data scientist submits a job that starts training and runs
+for days, other team members' jobs remain pending indefinitely. With
+`minAdmitDuration: 4h`, each job is guaranteed 4 hours of runtime before it
+can be preempted by a waiting job.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: ml-training
+spec:
+  preemption:
+    withinClusterQueue: LowerOrNewerEqualPriority
+    withinClusterQueueConfig:
+      minAdmitDuration: 4h
+```
+
+#### Story 2: Preventing Resource Monopolization in Shared Clusters
+
+As a platform team managing a shared Kubernetes cluster for multiple teams,
+I want to prevent any single team from monopolizing cluster resources with
+long-running jobs while ensuring jobs have enough time to make meaningful
+progress.
+
+By setting `minAdmitDuration: 2h`, we ensure that:
+- Jobs have at least 2 hours to run and checkpoint
+- No single job can block others indefinitely
+- Resource access rotates fairly among all pending workloads
+
+### Notes/Constraints/Caveats (Optional)
+
+The `minAdmitDuration` field modifies the behavior of the
+`LowerOrNewerEqualPriority` policy. Without `minAdmitDuration`, the policy
+only preempts workloads that are newer than the preempting workload. With
+`minAdmitDuration`, it additionally preempts workloads that have exceeded
+the minimum duration, regardless of relative age.
+
+This feature is complementary to Fair Sharing (KEP-1714). Fair Sharing
+addresses resource distribution across different ClusterQueues/Cohorts via
+preemption, while this feature addresses time-sharing among workloads with
+identical priority within the same ClusterQueue.
+
+**Interaction with Fair Sharing**: When both Fair Sharing and time-based
+preemption are enabled, time-based preemption only applies to the
+`withinClusterQueue` policy. Cross-ClusterQueue preemption continues to be
+governed by Fair Sharing algorithms. The two mechanisms operate independently
+and do not conflict.
+
+This is similar to Slurm's `PreemptExemptTime` which provides a guaranteed
+minimum runtime before a job becomes preemptible.
+
+### Risks and Mitigations
+
+**Rapid Preemption Cycles**: If `minAdmitDuration` is set too low, workloads
+might be preempted before making meaningful progress. Mitigated by webhook
+validation requiring a minimum of 1 minute.
+
+**Checkpoint Dependency**: Workloads that don't implement checkpointing lose
+progress when preempted. This is not new; workloads already need to handle
+preemption gracefully.
+
+**Controller Restart**: If the Kueue controller restarts, all timing is based
+on the persisted `QuotaReserved` condition timestamps stored in workload
+status, so workloads maintain their accurate admission time across restarts.
+No special handling is required.
+
+**Scheduler Re-evaluation**: When a workload crosses `minAdmitDuration` while
+the scheduler is idle, preemption happens on the next scheduling cycle.
+For typical `minAdmitDuration` values (30m+), scheduler cycles are usually
+frequent enough due to other cluster events. If tighter responsiveness is
+required, a future enhancement could schedule re-evaluation at the earliest
+expiration time.
+
+**Requeue Position**: Preempted workloads follow the existing eviction path.
+With `BestEffortFIFO` and `Eviction` requeue timestamp (in default/common
+configuration), the workload moves to the back of the queue, enabling
+round-robin rotation. When re-admitted, the `QuotaReserved` timestamp resets,
+restarting the `minAdmitDuration` timer.
+
+## Design Details
+
+### API Changes
+
+Add a new optional field `withinClusterQueueConfig` to `ClusterQueuePreemption`:
+
+```go
+type ClusterQueuePreemption struct {
+    ReclaimWithinCohort PreemptionPolicy    `json:"reclaimWithinCohort,omitempty"`
+    BorrowWithinCohort  *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+    WithinClusterQueue  PreemptionPolicy    `json:"withinClusterQueue,omitempty"`
+
+    // withinClusterQueueConfig provides additional configuration for the
+    // withinClusterQueue preemption policy. Only valid when withinClusterQueue
+    // is set to LowerOrNewerEqualPriority.
+    // +optional
+    WithinClusterQueueConfig *WithinClusterQueueConfig `json:"withinClusterQueueConfig,omitempty"`
+}
+
+type WithinClusterQueueConfig struct {
+    // minAdmitDuration specifies the minimum duration a workload must be
+    // admitted before it becomes eligible for same-priority preemption.
```

  IIUC this sentence (especially with "_must_") is misleading because you're going to still allow preempting **newer** same-priority workloads, **even if** their "time since admission" (or even "time since quota reservation") is **below** `minAdmitDuration`. Am I right?

- **keps/8522-time-based-same-priority-preemption/README.md:284** by [@olekzabl](https://github.com/olekzabl) — 2026-03-16T09:28:26Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Future Extensibility](#future-extensibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dynamic Priority Decay](#dynamic-priority-decay)
+  - [Rely on Admission Fair Sharing Alone](#rely-on-admission-fair-sharing-alone)
+  - [Usage-Based Preemption](#usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
+resource monopolization problem where the first workload admitted to a
+ClusterQueue continues to run indefinitely while same-priority workloads
+remain pending.
+
+**Terminology**: Throughout this KEP, "quota reservation time" refers to the
+`LastTransitionTime` of the workload's `QuotaReserved` condition (when
+`Status=True`). This is the moment when quota was reserved for the workload.
+
+## Motivation
+
+Currently, Kueue offers two preemption policies for `withinClusterQueue`:
+
+1. **`LowerPriority`**: Only preempt workloads with strictly lower priority
+2. **`LowerOrNewerEqualPriority`**: Preempt workloads with lower priority OR
+   workloads with equal priority that were admitted more recently
+
+The `LowerOrNewerEqualPriority` policy was designed to prevent queue starvation
+by allowing newly arrived high-demand workloads to preempt recently admitted
+same-priority workloads. However, this creates a subtle but significant
+problem: **the first workload admitted monopolizes resources indefinitely**.
+
+Consider this scenario:
+- WorkloadA (priority=10) is admitted at T=0
+- WorkloadB (priority=10) arrives at T=5min
+- WorkloadB cannot preempt WorkloadA because WorkloadA is older
+- WorkloadA runs for 24 hours while WorkloadB waits
+
+This behavior contradicts the expectation that same-priority workloads should
+have fair access to cluster resources over time. In multi-tenant environments,
+this can lead to:
+
+- One team's long-running workload blocking other teams indefinitely
+- Users submitting artificially low resource requests to get admitted first,
+  then consuming resources for extended periods
+- Reduced cluster utilization efficiency due to suboptimal scheduling
+
+### Goals
+
+- Extend the `LowerOrNewerEqualPriority` preemption policy with a
+  `minAdmitDuration` configuration that allows preemption of same-priority
+  workloads after they have been admitted for a specified duration
+- Maintain backward compatibility: existing configurations without
+  `minAdmitDuration` behave exactly as before
+
+### Non-Goals
+
+- Dynamic priority that decays over time
+- Per-workload or per-LocalQueue `minAdmitDuration` overrides
+- Precise CPU/GPU time accounting (wall-clock time from admission is used)
+- Cross-ClusterQueue time-based preemption (this feature is scoped to
+  `withinClusterQueue` only)
+
+## Proposal
+
+Introduce a new configuration structure `WithinClusterQueueConfig` that extends
+the `withinClusterQueue` preemption policy with a `minAdmitDuration` field.
+When `withinClusterQueue` is set to `LowerOrNewerEqualPriority` and
+`minAdmitDuration` is configured, workloads become eligible for same-priority
+preemption only after they have been admitted for at least the specified
+duration.
+
+### User Stories
+
+#### Story 1: Fair GPU Time-Sharing Among Data Scientists
+
+As a cluster administrator managing GPU resources for a team of 20 data
+scientists, I want to ensure that each team member gets fair access to GPU
+resources for their training jobs, all of which have the same priority.
+
+Currently, if one data scientist submits a job that starts training and runs
+for days, other team members' jobs remain pending indefinitely. With
+`minAdmitDuration: 4h`, each job is guaranteed 4 hours of runtime before it
+can be preempted by a waiting job.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: ml-training
+spec:
+  preemption:
+    withinClusterQueue: LowerOrNewerEqualPriority
+    withinClusterQueueConfig:
+      minAdmitDuration: 4h
+```
+
+#### Story 2: Preventing Resource Monopolization in Shared Clusters
+
+As a platform team managing a shared Kubernetes cluster for multiple teams,
+I want to prevent any single team from monopolizing cluster resources with
+long-running jobs while ensuring jobs have enough time to make meaningful
+progress.
+
+By setting `minAdmitDuration: 2h`, we ensure that:
+- Jobs have at least 2 hours to run and checkpoint
+- No single job can block others indefinitely
+- Resource access rotates fairly among all pending workloads
+
+### Notes/Constraints/Caveats (Optional)
+
+The `minAdmitDuration` field modifies the behavior of the
+`LowerOrNewerEqualPriority` policy. Without `minAdmitDuration`, the policy
+only preempts workloads that are newer than the preempting workload. With
+`minAdmitDuration`, it additionally preempts workloads that have exceeded
+the minimum duration, regardless of relative age.
+
+This feature is complementary to Fair Sharing (KEP-1714). Fair Sharing
+addresses resource distribution across different ClusterQueues/Cohorts via
+preemption, while this feature addresses time-sharing among workloads with
+identical priority within the same ClusterQueue.
+
+**Interaction with Fair Sharing**: When both Fair Sharing and time-based
+preemption are enabled, time-based preemption only applies to the
+`withinClusterQueue` policy. Cross-ClusterQueue preemption continues to be
+governed by Fair Sharing algorithms. The two mechanisms operate independently
+and do not conflict.
+
+This is similar to Slurm's `PreemptExemptTime` which provides a guaranteed
+minimum runtime before a job becomes preemptible.
+
+### Risks and Mitigations
+
+**Rapid Preemption Cycles**: If `minAdmitDuration` is set too low, workloads
+might be preempted before making meaningful progress. Mitigated by webhook
+validation requiring a minimum of 1 minute.
+
+**Checkpoint Dependency**: Workloads that don't implement checkpointing lose
+progress when preempted. This is not new; workloads already need to handle
+preemption gracefully.
+
+**Controller Restart**: If the Kueue controller restarts, all timing is based
+on the persisted `QuotaReserved` condition timestamps stored in workload
+status, so workloads maintain their accurate admission time across restarts.
+No special handling is required.
+
+**Scheduler Re-evaluation**: When a workload crosses `minAdmitDuration` while
+the scheduler is idle, preemption happens on the next scheduling cycle.
+For typical `minAdmitDuration` values (30m+), scheduler cycles are usually
+frequent enough due to other cluster events. If tighter responsiveness is
+required, a future enhancement could schedule re-evaluation at the earliest
+expiration time.
+
+**Requeue Position**: Preempted workloads follow the existing eviction path.
+With `BestEffortFIFO` and `Eviction` requeue timestamp (in default/common
+configuration), the workload moves to the back of the queue, enabling
+round-robin rotation. When re-admitted, the `QuotaReserved` timestamp resets,
+restarting the `minAdmitDuration` timer.
+
+## Design Details
+
+### API Changes
+
+Add a new optional field `withinClusterQueueConfig` to `ClusterQueuePreemption`:
+
+```go
+type ClusterQueuePreemption struct {
+    ReclaimWithinCohort PreemptionPolicy    `json:"reclaimWithinCohort,omitempty"`
+    BorrowWithinCohort  *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+    WithinClusterQueue  PreemptionPolicy    `json:"withinClusterQueue,omitempty"`
+
+    // withinClusterQueueConfig provides additional configuration for the
+    // withinClusterQueue preemption policy. Only valid when withinClusterQueue
+    // is set to LowerOrNewerEqualPriority.
+    // +optional
+    WithinClusterQueueConfig *WithinClusterQueueConfig `json:"withinClusterQueueConfig,omitempty"`
+}
+
+type WithinClusterQueueConfig struct {
+    // minAdmitDuration specifies the minimum duration a workload must be
+    // admitted before it becomes eligible for same-priority preemption.
+    // The duration is measured from the workload's quota reservation time.
+    // If nil or omitted, time-based same-priority preemption is disabled
+    // and only the existing behavior applies. When set, the minimum
+    // allowed value is 1 minute; values less than 1 minute (including 0)
+    // are rejected by validation.
+    // +optional
+    MinAdmitDuration *metav1.Duration `json:"minAdmitDuration,omitempty"`
+}
+```
+
+### Preemption Eligibility
+
+Preemption only occurs when there is a pending workload that cannot be
+admitted due to insufficient quota. A pending workload can preempt an
+admitted same-priority workload if **either** of these conditions is true:
+
+1. **Existing behavior**: The target's quota reservation time is more recent
+   than the preemptor's creation time (i.e., the target was admitted after the
+   preemptor entered the queue)
+2. **New behavior**: The target has exceeded `minAdmitDuration` (i.e.,
+   `now - target.quotaReservationTime > minAdmitDuration`)
+
+Note: Condition 1 preserves backward compatibility with the existing
+`LowerOrNewerEqualPriority` behavior where a workload that has been waiting
+longer in the queue has priority over one that was admitted after the waiting
+workload was created.
+
+**Preemption order among same-priority candidates**:
+1. Time-expired workloads (exceeded `minAdmitDuration`) are preempted before
+   "newer" workloads (admitted after preemptor was created)
+2. Among time-expired workloads, the longest-running is preempted first
+3. Among "newer" workloads, the most recently admitted is preempted first
+
+This ordering ensures workloads that have had their guaranteed runtime are
+preempted before those still within their protected period.
+
+### Future Extensibility
+
+The `WithinClusterQueueConfig` struct is designed to accommodate additional
+preemption policies. For example, a complementary use case has been raised
+where workloads should become immune to lower-priority preemption after a
+maximum duration. Such a field could be added to the same struct without
+API breakage and would operate independently of `minAdmitDuration`.
+
+### Webhook Validation
+
+1. `minAdmitDuration` must be at least 1 minute. Values less than 1 minute
+   (including explicit `0s` and negative values) are rejected with an error.
+2. If `withinClusterQueueConfig` is specified but `minAdmitDuration` is `nil`,
+   the configuration is valid but has no effect (time-based preemption is
+   disabled, only the existing `LowerOrNewerEqualPriority` behavior applies).
+3. `withinClusterQueueConfig` is only valid when `withinClusterQueue` is
+   `LowerOrNewerEqualPriority`. Setting it with `Never` or `LowerPriority`
+   is rejected with an error indicating the incompatible configuration.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+#### Unit Tests
+
+- `pkg/scheduler/preemption`: Test preemption candidate selection with
+  `minAdmitDuration`
+- `apis/kueue/v1beta2/clusterqueue_webhook`: Test validation rules
+
+#### Integration tests
+
+1. Two same-priority workloads competing for quota, first admitted workload
+   is preempted after `minAdmitDuration`
+2. Multiple workloads queued, fair rotation occurs as each exceeds threshold
+3. Mixed priorities: lower priority always preempted before same-priority
+4. `minAdmitDuration` not set: existing behavior preserved
+5. Edge case: workload exactly at `minAdmitDuration` threshold (should NOT
+   be preempted, as comparison requires strictly greater than)
```

  Is it worth it? I'm afraid this might be noticeable effort with minor value.
  
  * Why minor value: given that the logic formula is
    `now - target.quotaReservationTime > minAdmitDuration`,
    where `now` is (I guess) some timestamp inside Kueue scheduler, it seems a super-rare event to hit `now` being _exactly equal_ to `quotaReservationTime + minAdmitDuration`.
    And, even if it happens, it seems super-irrelevant if we apply `>` or `>=` in that case :)
    Because, alternatively, we could easily have hit a timestamp 1ms earlier or later - so "either outcome feels legit" anyway.
  
  * Why noticeable effort: to write a test for handling the `==` case, you'd need to inject some fake clock into the scheduler code.
    I currently see it done in unit tests but not in integration tests. (There, I only see fake clock injected into `kueuectl`).
    This _may_ be doable - but I'd blindly guess some effort will hide in that.

- **keps/8522-time-based-same-priority-preemption/README.md:288** by [@olekzabl](https://github.com/olekzabl) — 2026-03-16T09:34:10Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Future Extensibility](#future-extensibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dynamic Priority Decay](#dynamic-priority-decay)
+  - [Rely on Admission Fair Sharing Alone](#rely-on-admission-fair-sharing-alone)
+  - [Usage-Based Preemption](#usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
+resource monopolization problem where the first workload admitted to a
+ClusterQueue continues to run indefinitely while same-priority workloads
+remain pending.
+
+**Terminology**: Throughout this KEP, "quota reservation time" refers to the
+`LastTransitionTime` of the workload's `QuotaReserved` condition (when
+`Status=True`). This is the moment when quota was reserved for the workload.
+
+## Motivation
+
+Currently, Kueue offers two preemption policies for `withinClusterQueue`:
+
+1. **`LowerPriority`**: Only preempt workloads with strictly lower priority
+2. **`LowerOrNewerEqualPriority`**: Preempt workloads with lower priority OR
+   workloads with equal priority that were admitted more recently
+
+The `LowerOrNewerEqualPriority` policy was designed to prevent queue starvation
+by allowing newly arrived high-demand workloads to preempt recently admitted
+same-priority workloads. However, this creates a subtle but significant
+problem: **the first workload admitted monopolizes resources indefinitely**.
+
+Consider this scenario:
+- WorkloadA (priority=10) is admitted at T=0
+- WorkloadB (priority=10) arrives at T=5min
+- WorkloadB cannot preempt WorkloadA because WorkloadA is older
+- WorkloadA runs for 24 hours while WorkloadB waits
+
+This behavior contradicts the expectation that same-priority workloads should
+have fair access to cluster resources over time. In multi-tenant environments,
+this can lead to:
+
+- One team's long-running workload blocking other teams indefinitely
+- Users submitting artificially low resource requests to get admitted first,
+  then consuming resources for extended periods
+- Reduced cluster utilization efficiency due to suboptimal scheduling
+
+### Goals
+
+- Extend the `LowerOrNewerEqualPriority` preemption policy with a
+  `minAdmitDuration` configuration that allows preemption of same-priority
+  workloads after they have been admitted for a specified duration
+- Maintain backward compatibility: existing configurations without
+  `minAdmitDuration` behave exactly as before
+
+### Non-Goals
+
+- Dynamic priority that decays over time
+- Per-workload or per-LocalQueue `minAdmitDuration` overrides
+- Precise CPU/GPU time accounting (wall-clock time from admission is used)
+- Cross-ClusterQueue time-based preemption (this feature is scoped to
+  `withinClusterQueue` only)
+
+## Proposal
+
+Introduce a new configuration structure `WithinClusterQueueConfig` that extends
+the `withinClusterQueue` preemption policy with a `minAdmitDuration` field.
+When `withinClusterQueue` is set to `LowerOrNewerEqualPriority` and
+`minAdmitDuration` is configured, workloads become eligible for same-priority
+preemption only after they have been admitted for at least the specified
+duration.
+
+### User Stories
+
+#### Story 1: Fair GPU Time-Sharing Among Data Scientists
+
+As a cluster administrator managing GPU resources for a team of 20 data
+scientists, I want to ensure that each team member gets fair access to GPU
+resources for their training jobs, all of which have the same priority.
+
+Currently, if one data scientist submits a job that starts training and runs
+for days, other team members' jobs remain pending indefinitely. With
+`minAdmitDuration: 4h`, each job is guaranteed 4 hours of runtime before it
+can be preempted by a waiting job.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: ml-training
+spec:
+  preemption:
+    withinClusterQueue: LowerOrNewerEqualPriority
+    withinClusterQueueConfig:
+      minAdmitDuration: 4h
+```
+
+#### Story 2: Preventing Resource Monopolization in Shared Clusters
+
+As a platform team managing a shared Kubernetes cluster for multiple teams,
+I want to prevent any single team from monopolizing cluster resources with
+long-running jobs while ensuring jobs have enough time to make meaningful
+progress.
+
+By setting `minAdmitDuration: 2h`, we ensure that:
+- Jobs have at least 2 hours to run and checkpoint
+- No single job can block others indefinitely
+- Resource access rotates fairly among all pending workloads
+
+### Notes/Constraints/Caveats (Optional)
+
+The `minAdmitDuration` field modifies the behavior of the
+`LowerOrNewerEqualPriority` policy. Without `minAdmitDuration`, the policy
+only preempts workloads that are newer than the preempting workload. With
+`minAdmitDuration`, it additionally preempts workloads that have exceeded
+the minimum duration, regardless of relative age.
+
+This feature is complementary to Fair Sharing (KEP-1714). Fair Sharing
+addresses resource distribution across different ClusterQueues/Cohorts via
+preemption, while this feature addresses time-sharing among workloads with
+identical priority within the same ClusterQueue.
+
+**Interaction with Fair Sharing**: When both Fair Sharing and time-based
+preemption are enabled, time-based preemption only applies to the
+`withinClusterQueue` policy. Cross-ClusterQueue preemption continues to be
+governed by Fair Sharing algorithms. The two mechanisms operate independently
+and do not conflict.
+
+This is similar to Slurm's `PreemptExemptTime` which provides a guaranteed
+minimum runtime before a job becomes preemptible.
+
+### Risks and Mitigations
+
+**Rapid Preemption Cycles**: If `minAdmitDuration` is set too low, workloads
+might be preempted before making meaningful progress. Mitigated by webhook
+validation requiring a minimum of 1 minute.
+
+**Checkpoint Dependency**: Workloads that don't implement checkpointing lose
+progress when preempted. This is not new; workloads already need to handle
+preemption gracefully.
+
+**Controller Restart**: If the Kueue controller restarts, all timing is based
+on the persisted `QuotaReserved` condition timestamps stored in workload
+status, so workloads maintain their accurate admission time across restarts.
+No special handling is required.
+
+**Scheduler Re-evaluation**: When a workload crosses `minAdmitDuration` while
+the scheduler is idle, preemption happens on the next scheduling cycle.
+For typical `minAdmitDuration` values (30m+), scheduler cycles are usually
+frequent enough due to other cluster events. If tighter responsiveness is
+required, a future enhancement could schedule re-evaluation at the earliest
+expiration time.
+
+**Requeue Position**: Preempted workloads follow the existing eviction path.
+With `BestEffortFIFO` and `Eviction` requeue timestamp (in default/common
+configuration), the workload moves to the back of the queue, enabling
+round-robin rotation. When re-admitted, the `QuotaReserved` timestamp resets,
+restarting the `minAdmitDuration` timer.
+
+## Design Details
+
+### API Changes
+
+Add a new optional field `withinClusterQueueConfig` to `ClusterQueuePreemption`:
+
+```go
+type ClusterQueuePreemption struct {
+    ReclaimWithinCohort PreemptionPolicy    `json:"reclaimWithinCohort,omitempty"`
+    BorrowWithinCohort  *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+    WithinClusterQueue  PreemptionPolicy    `json:"withinClusterQueue,omitempty"`
+
+    // withinClusterQueueConfig provides additional configuration for the
+    // withinClusterQueue preemption policy. Only valid when withinClusterQueue
+    // is set to LowerOrNewerEqualPriority.
+    // +optional
+    WithinClusterQueueConfig *WithinClusterQueueConfig `json:"withinClusterQueueConfig,omitempty"`
+}
+
+type WithinClusterQueueConfig struct {
+    // minAdmitDuration specifies the minimum duration a workload must be
+    // admitted before it becomes eligible for same-priority preemption.
+    // The duration is measured from the workload's quota reservation time.
+    // If nil or omitted, time-based same-priority preemption is disabled
+    // and only the existing behavior applies. When set, the minimum
+    // allowed value is 1 minute; values less than 1 minute (including 0)
+    // are rejected by validation.
+    // +optional
+    MinAdmitDuration *metav1.Duration `json:"minAdmitDuration,omitempty"`
+}
+```
+
+### Preemption Eligibility
+
+Preemption only occurs when there is a pending workload that cannot be
+admitted due to insufficient quota. A pending workload can preempt an
+admitted same-priority workload if **either** of these conditions is true:
+
+1. **Existing behavior**: The target's quota reservation time is more recent
+   than the preemptor's creation time (i.e., the target was admitted after the
+   preemptor entered the queue)
+2. **New behavior**: The target has exceeded `minAdmitDuration` (i.e.,
+   `now - target.quotaReservationTime > minAdmitDuration`)
+
+Note: Condition 1 preserves backward compatibility with the existing
+`LowerOrNewerEqualPriority` behavior where a workload that has been waiting
+longer in the queue has priority over one that was admitted after the waiting
+workload was created.
+
+**Preemption order among same-priority candidates**:
+1. Time-expired workloads (exceeded `minAdmitDuration`) are preempted before
+   "newer" workloads (admitted after preemptor was created)
+2. Among time-expired workloads, the longest-running is preempted first
+3. Among "newer" workloads, the most recently admitted is preempted first
+
+This ordering ensures workloads that have had their guaranteed runtime are
+preempted before those still within their protected period.
+
+### Future Extensibility
+
+The `WithinClusterQueueConfig` struct is designed to accommodate additional
+preemption policies. For example, a complementary use case has been raised
+where workloads should become immune to lower-priority preemption after a
+maximum duration. Such a field could be added to the same struct without
+API breakage and would operate independently of `minAdmitDuration`.
+
+### Webhook Validation
+
+1. `minAdmitDuration` must be at least 1 minute. Values less than 1 minute
+   (including explicit `0s` and negative values) are rejected with an error.
+2. If `withinClusterQueueConfig` is specified but `minAdmitDuration` is `nil`,
+   the configuration is valid but has no effect (time-based preemption is
+   disabled, only the existing `LowerOrNewerEqualPriority` behavior applies).
+3. `withinClusterQueueConfig` is only valid when `withinClusterQueue` is
+   `LowerOrNewerEqualPriority`. Setting it with `Never` or `LowerPriority`
+   is rejected with an error indicating the incompatible configuration.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+#### Unit Tests
+
+- `pkg/scheduler/preemption`: Test preemption candidate selection with
+  `minAdmitDuration`
+- `apis/kueue/v1beta2/clusterqueue_webhook`: Test validation rules
+
+#### Integration tests
+
+1. Two same-priority workloads competing for quota, first admitted workload
+   is preempted after `minAdmitDuration`
+2. Multiple workloads queued, fair rotation occurs as each exceeds threshold
+3. Mixed priorities: lower priority always preempted before same-priority
+4. `minAdmitDuration` not set: existing behavior preserved
+5. Edge case: workload exactly at `minAdmitDuration` threshold (should NOT
+   be preempted, as comparison requires strictly greater than)
+6. Controller restart: workload timestamps persist correctly and preemption
+   timing remains accurate
+7. Multiple workloads expiring simultaneously: deterministic ordering based
+   on longest-running first
```

  I don't understand.
  Is "moment of expiration" defined as "start time + `minAdmitDuration`", the latter summand independent of the workload?
  If so, "expiring _simultaneously_" implies "they're all equally long-running".
  
  My guess is that you don't need them to _expire simultaneously_; perhaps you just need a group of _expired_ workloads to be _simultaneously considered_ as preemption candidates for a single newly-incoming workload.
  
  WDYT?

- **keps/8522-time-based-same-priority-preemption/README.md:306** by [@olekzabl](https://github.com/olekzabl) — 2026-03-16T09:37:04Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Future Extensibility](#future-extensibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dynamic Priority Decay](#dynamic-priority-decay)
+  - [Rely on Admission Fair Sharing Alone](#rely-on-admission-fair-sharing-alone)
+  - [Usage-Based Preemption](#usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
+resource monopolization problem where the first workload admitted to a
+ClusterQueue continues to run indefinitely while same-priority workloads
+remain pending.
+
+**Terminology**: Throughout this KEP, "quota reservation time" refers to the
+`LastTransitionTime` of the workload's `QuotaReserved` condition (when
+`Status=True`). This is the moment when quota was reserved for the workload.
+
+## Motivation
+
+Currently, Kueue offers two preemption policies for `withinClusterQueue`:
+
+1. **`LowerPriority`**: Only preempt workloads with strictly lower priority
+2. **`LowerOrNewerEqualPriority`**: Preempt workloads with lower priority OR
+   workloads with equal priority that were admitted more recently
+
+The `LowerOrNewerEqualPriority` policy was designed to prevent queue starvation
+by allowing newly arrived high-demand workloads to preempt recently admitted
+same-priority workloads. However, this creates a subtle but significant
+problem: **the first workload admitted monopolizes resources indefinitely**.
+
+Consider this scenario:
+- WorkloadA (priority=10) is admitted at T=0
+- WorkloadB (priority=10) arrives at T=5min
+- WorkloadB cannot preempt WorkloadA because WorkloadA is older
+- WorkloadA runs for 24 hours while WorkloadB waits
+
+This behavior contradicts the expectation that same-priority workloads should
+have fair access to cluster resources over time. In multi-tenant environments,
+this can lead to:
+
+- One team's long-running workload blocking other teams indefinitely
+- Users submitting artificially low resource requests to get admitted first,
+  then consuming resources for extended periods
+- Reduced cluster utilization efficiency due to suboptimal scheduling
+
+### Goals
+
+- Extend the `LowerOrNewerEqualPriority` preemption policy with a
+  `minAdmitDuration` configuration that allows preemption of same-priority
+  workloads after they have been admitted for a specified duration
+- Maintain backward compatibility: existing configurations without
+  `minAdmitDuration` behave exactly as before
+
+### Non-Goals
+
+- Dynamic priority that decays over time
+- Per-workload or per-LocalQueue `minAdmitDuration` overrides
+- Precise CPU/GPU time accounting (wall-clock time from admission is used)
+- Cross-ClusterQueue time-based preemption (this feature is scoped to
+  `withinClusterQueue` only)
+
+## Proposal
+
+Introduce a new configuration structure `WithinClusterQueueConfig` that extends
+the `withinClusterQueue` preemption policy with a `minAdmitDuration` field.
+When `withinClusterQueue` is set to `LowerOrNewerEqualPriority` and
+`minAdmitDuration` is configured, workloads become eligible for same-priority
+preemption only after they have been admitted for at least the specified
+duration.
+
+### User Stories
+
+#### Story 1: Fair GPU Time-Sharing Among Data Scientists
+
+As a cluster administrator managing GPU resources for a team of 20 data
+scientists, I want to ensure that each team member gets fair access to GPU
+resources for their training jobs, all of which have the same priority.
+
+Currently, if one data scientist submits a job that starts training and runs
+for days, other team members' jobs remain pending indefinitely. With
+`minAdmitDuration: 4h`, each job is guaranteed 4 hours of runtime before it
+can be preempted by a waiting job.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: ml-training
+spec:
+  preemption:
+    withinClusterQueue: LowerOrNewerEqualPriority
+    withinClusterQueueConfig:
+      minAdmitDuration: 4h
+```
+
+#### Story 2: Preventing Resource Monopolization in Shared Clusters
+
+As a platform team managing a shared Kubernetes cluster for multiple teams,
+I want to prevent any single team from monopolizing cluster resources with
+long-running jobs while ensuring jobs have enough time to make meaningful
+progress.
+
+By setting `minAdmitDuration: 2h`, we ensure that:
+- Jobs have at least 2 hours to run and checkpoint
+- No single job can block others indefinitely
+- Resource access rotates fairly among all pending workloads
+
+### Notes/Constraints/Caveats (Optional)
+
+The `minAdmitDuration` field modifies the behavior of the
+`LowerOrNewerEqualPriority` policy. Without `minAdmitDuration`, the policy
+only preempts workloads that are newer than the preempting workload. With
+`minAdmitDuration`, it additionally preempts workloads that have exceeded
+the minimum duration, regardless of relative age.
+
+This feature is complementary to Fair Sharing (KEP-1714). Fair Sharing
+addresses resource distribution across different ClusterQueues/Cohorts via
+preemption, while this feature addresses time-sharing among workloads with
+identical priority within the same ClusterQueue.
+
+**Interaction with Fair Sharing**: When both Fair Sharing and time-based
+preemption are enabled, time-based preemption only applies to the
+`withinClusterQueue` policy. Cross-ClusterQueue preemption continues to be
+governed by Fair Sharing algorithms. The two mechanisms operate independently
+and do not conflict.
+
+This is similar to Slurm's `PreemptExemptTime` which provides a guaranteed
+minimum runtime before a job becomes preemptible.
+
+### Risks and Mitigations
+
+**Rapid Preemption Cycles**: If `minAdmitDuration` is set too low, workloads
+might be preempted before making meaningful progress. Mitigated by webhook
+validation requiring a minimum of 1 minute.
+
+**Checkpoint Dependency**: Workloads that don't implement checkpointing lose
+progress when preempted. This is not new; workloads already need to handle
+preemption gracefully.
+
+**Controller Restart**: If the Kueue controller restarts, all timing is based
+on the persisted `QuotaReserved` condition timestamps stored in workload
+status, so workloads maintain their accurate admission time across restarts.
+No special handling is required.
+
+**Scheduler Re-evaluation**: When a workload crosses `minAdmitDuration` while
+the scheduler is idle, preemption happens on the next scheduling cycle.
+For typical `minAdmitDuration` values (30m+), scheduler cycles are usually
+frequent enough due to other cluster events. If tighter responsiveness is
+required, a future enhancement could schedule re-evaluation at the earliest
+expiration time.
+
+**Requeue Position**: Preempted workloads follow the existing eviction path.
+With `BestEffortFIFO` and `Eviction` requeue timestamp (in default/common
+configuration), the workload moves to the back of the queue, enabling
+round-robin rotation. When re-admitted, the `QuotaReserved` timestamp resets,
+restarting the `minAdmitDuration` timer.
+
+## Design Details
+
+### API Changes
+
+Add a new optional field `withinClusterQueueConfig` to `ClusterQueuePreemption`:
+
+```go
+type ClusterQueuePreemption struct {
+    ReclaimWithinCohort PreemptionPolicy    `json:"reclaimWithinCohort,omitempty"`
+    BorrowWithinCohort  *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+    WithinClusterQueue  PreemptionPolicy    `json:"withinClusterQueue,omitempty"`
+
+    // withinClusterQueueConfig provides additional configuration for the
+    // withinClusterQueue preemption policy. Only valid when withinClusterQueue
+    // is set to LowerOrNewerEqualPriority.
+    // +optional
+    WithinClusterQueueConfig *WithinClusterQueueConfig `json:"withinClusterQueueConfig,omitempty"`
+}
+
+type WithinClusterQueueConfig struct {
+    // minAdmitDuration specifies the minimum duration a workload must be
+    // admitted before it becomes eligible for same-priority preemption.
+    // The duration is measured from the workload's quota reservation time.
+    // If nil or omitted, time-based same-priority preemption is disabled
+    // and only the existing behavior applies. When set, the minimum
+    // allowed value is 1 minute; values less than 1 minute (including 0)
+    // are rejected by validation.
+    // +optional
+    MinAdmitDuration *metav1.Duration `json:"minAdmitDuration,omitempty"`
+}
+```
+
+### Preemption Eligibility
+
+Preemption only occurs when there is a pending workload that cannot be
+admitted due to insufficient quota. A pending workload can preempt an
+admitted same-priority workload if **either** of these conditions is true:
+
+1. **Existing behavior**: The target's quota reservation time is more recent
+   than the preemptor's creation time (i.e., the target was admitted after the
+   preemptor entered the queue)
+2. **New behavior**: The target has exceeded `minAdmitDuration` (i.e.,
+   `now - target.quotaReservationTime > minAdmitDuration`)
+
+Note: Condition 1 preserves backward compatibility with the existing
+`LowerOrNewerEqualPriority` behavior where a workload that has been waiting
+longer in the queue has priority over one that was admitted after the waiting
+workload was created.
+
+**Preemption order among same-priority candidates**:
+1. Time-expired workloads (exceeded `minAdmitDuration`) are preempted before
+   "newer" workloads (admitted after preemptor was created)
+2. Among time-expired workloads, the longest-running is preempted first
+3. Among "newer" workloads, the most recently admitted is preempted first
+
+This ordering ensures workloads that have had their guaranteed runtime are
+preempted before those still within their protected period.
+
+### Future Extensibility
+
+The `WithinClusterQueueConfig` struct is designed to accommodate additional
+preemption policies. For example, a complementary use case has been raised
+where workloads should become immune to lower-priority preemption after a
+maximum duration. Such a field could be added to the same struct without
+API breakage and would operate independently of `minAdmitDuration`.
+
+### Webhook Validation
+
+1. `minAdmitDuration` must be at least 1 minute. Values less than 1 minute
+   (including explicit `0s` and negative values) are rejected with an error.
+2. If `withinClusterQueueConfig` is specified but `minAdmitDuration` is `nil`,
+   the configuration is valid but has no effect (time-based preemption is
+   disabled, only the existing `LowerOrNewerEqualPriority` behavior applies).
+3. `withinClusterQueueConfig` is only valid when `withinClusterQueue` is
+   `LowerOrNewerEqualPriority`. Setting it with `Never` or `LowerPriority`
+   is rejected with an error indicating the incompatible configuration.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+#### Unit Tests
+
+- `pkg/scheduler/preemption`: Test preemption candidate selection with
+  `minAdmitDuration`
+- `apis/kueue/v1beta2/clusterqueue_webhook`: Test validation rules
+
+#### Integration tests
+
+1. Two same-priority workloads competing for quota, first admitted workload
+   is preempted after `minAdmitDuration`
+2. Multiple workloads queued, fair rotation occurs as each exceeds threshold
+3. Mixed priorities: lower priority always preempted before same-priority
+4. `minAdmitDuration` not set: existing behavior preserved
+5. Edge case: workload exactly at `minAdmitDuration` threshold (should NOT
+   be preempted, as comparison requires strictly greater than)
+6. Controller restart: workload timestamps persist correctly and preemption
+   timing remains accurate
+7. Multiple workloads expiring simultaneously: deterministic ordering based
+   on longest-running first
+8. Interaction with Fair Sharing: time-based preemption only affects
+   `withinClusterQueue`, cross-queue preemption follows Fair Sharing rules
+9. Webhook validation: reject `minAdmitDuration` < 1 minute, reject config
+   when `withinClusterQueue` is `Never` or `LowerPriority`
+
+### Graduation Criteria
+
+#### Alpha
+
+- Feature behind `TimeBasedSamePriorityPreemption` feature gate (disabled by
+  default)
+- `withinClusterQueueConfig.minAdmitDuration` API field implemented
+- Preemption logic updated to consider time-expired workloads
+- Webhook validation for minimum duration and policy compatibility
+- Unit tests for preemption candidate selection
+- Integration tests for time-based preemption scenarios
+- Add a new reason value `InClusterQueueTimeBased` to the existing `reason`
+  label on `kueue_preempted_workloads_total` metric
```

  BTW how about some info in WorkloadStatus (I guess, specifically Conditions?)
  
  Basically: how are we going to help a user understand "why was this workload preempted, even though the rules for `LowerOrNewerEqualPriority` which I used to know did not allow that"?

- **keps/8522-time-based-same-priority-preemption/README.md:227** by [@olekzabl](https://github.com/olekzabl) — 2026-03-16T09:47:44Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Future Extensibility](#future-extensibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dynamic Priority Decay](#dynamic-priority-decay)
+  - [Rely on Admission Fair Sharing Alone](#rely-on-admission-fair-sharing-alone)
+  - [Usage-Based Preemption](#usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
+resource monopolization problem where the first workload admitted to a
+ClusterQueue continues to run indefinitely while same-priority workloads
+remain pending.
+
+**Terminology**: Throughout this KEP, "quota reservation time" refers to the
+`LastTransitionTime` of the workload's `QuotaReserved` condition (when
+`Status=True`). This is the moment when quota was reserved for the workload.
+
+## Motivation
+
+Currently, Kueue offers two preemption policies for `withinClusterQueue`:
+
+1. **`LowerPriority`**: Only preempt workloads with strictly lower priority
+2. **`LowerOrNewerEqualPriority`**: Preempt workloads with lower priority OR
+   workloads with equal priority that were admitted more recently
+
+The `LowerOrNewerEqualPriority` policy was designed to prevent queue starvation
+by allowing newly arrived high-demand workloads to preempt recently admitted
+same-priority workloads. However, this creates a subtle but significant
+problem: **the first workload admitted monopolizes resources indefinitely**.
+
+Consider this scenario:
+- WorkloadA (priority=10) is admitted at T=0
+- WorkloadB (priority=10) arrives at T=5min
+- WorkloadB cannot preempt WorkloadA because WorkloadA is older
+- WorkloadA runs for 24 hours while WorkloadB waits
+
+This behavior contradicts the expectation that same-priority workloads should
+have fair access to cluster resources over time. In multi-tenant environments,
+this can lead to:
+
+- One team's long-running workload blocking other teams indefinitely
+- Users submitting artificially low resource requests to get admitted first,
+  then consuming resources for extended periods
+- Reduced cluster utilization efficiency due to suboptimal scheduling
+
+### Goals
+
+- Extend the `LowerOrNewerEqualPriority` preemption policy with a
+  `minAdmitDuration` configuration that allows preemption of same-priority
+  workloads after they have been admitted for a specified duration
+- Maintain backward compatibility: existing configurations without
+  `minAdmitDuration` behave exactly as before
+
+### Non-Goals
+
+- Dynamic priority that decays over time
+- Per-workload or per-LocalQueue `minAdmitDuration` overrides
+- Precise CPU/GPU time accounting (wall-clock time from admission is used)
+- Cross-ClusterQueue time-based preemption (this feature is scoped to
+  `withinClusterQueue` only)
+
+## Proposal
+
+Introduce a new configuration structure `WithinClusterQueueConfig` that extends
+the `withinClusterQueue` preemption policy with a `minAdmitDuration` field.
+When `withinClusterQueue` is set to `LowerOrNewerEqualPriority` and
+`minAdmitDuration` is configured, workloads become eligible for same-priority
+preemption only after they have been admitted for at least the specified
+duration.
+
+### User Stories
+
+#### Story 1: Fair GPU Time-Sharing Among Data Scientists
+
+As a cluster administrator managing GPU resources for a team of 20 data
+scientists, I want to ensure that each team member gets fair access to GPU
+resources for their training jobs, all of which have the same priority.
+
+Currently, if one data scientist submits a job that starts training and runs
+for days, other team members' jobs remain pending indefinitely. With
+`minAdmitDuration: 4h`, each job is guaranteed 4 hours of runtime before it
+can be preempted by a waiting job.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: ml-training
+spec:
+  preemption:
+    withinClusterQueue: LowerOrNewerEqualPriority
+    withinClusterQueueConfig:
+      minAdmitDuration: 4h
+```
+
+#### Story 2: Preventing Resource Monopolization in Shared Clusters
+
+As a platform team managing a shared Kubernetes cluster for multiple teams,
+I want to prevent any single team from monopolizing cluster resources with
+long-running jobs while ensuring jobs have enough time to make meaningful
+progress.
+
+By setting `minAdmitDuration: 2h`, we ensure that:
+- Jobs have at least 2 hours to run and checkpoint
+- No single job can block others indefinitely
+- Resource access rotates fairly among all pending workloads
+
+### Notes/Constraints/Caveats (Optional)
+
+The `minAdmitDuration` field modifies the behavior of the
+`LowerOrNewerEqualPriority` policy. Without `minAdmitDuration`, the policy
+only preempts workloads that are newer than the preempting workload. With
+`minAdmitDuration`, it additionally preempts workloads that have exceeded
+the minimum duration, regardless of relative age.
+
+This feature is complementary to Fair Sharing (KEP-1714). Fair Sharing
+addresses resource distribution across different ClusterQueues/Cohorts via
+preemption, while this feature addresses time-sharing among workloads with
+identical priority within the same ClusterQueue.
+
+**Interaction with Fair Sharing**: When both Fair Sharing and time-based
+preemption are enabled, time-based preemption only applies to the
+`withinClusterQueue` policy. Cross-ClusterQueue preemption continues to be
+governed by Fair Sharing algorithms. The two mechanisms operate independently
+and do not conflict.
+
+This is similar to Slurm's `PreemptExemptTime` which provides a guaranteed
+minimum runtime before a job becomes preemptible.
+
+### Risks and Mitigations
+
+**Rapid Preemption Cycles**: If `minAdmitDuration` is set too low, workloads
+might be preempted before making meaningful progress. Mitigated by webhook
+validation requiring a minimum of 1 minute.
+
+**Checkpoint Dependency**: Workloads that don't implement checkpointing lose
+progress when preempted. This is not new; workloads already need to handle
+preemption gracefully.
+
+**Controller Restart**: If the Kueue controller restarts, all timing is based
+on the persisted `QuotaReserved` condition timestamps stored in workload
+status, so workloads maintain their accurate admission time across restarts.
+No special handling is required.
+
+**Scheduler Re-evaluation**: When a workload crosses `minAdmitDuration` while
+the scheduler is idle, preemption happens on the next scheduling cycle.
+For typical `minAdmitDuration` values (30m+), scheduler cycles are usually
+frequent enough due to other cluster events. If tighter responsiveness is
+required, a future enhancement could schedule re-evaluation at the earliest
+expiration time.
+
+**Requeue Position**: Preempted workloads follow the existing eviction path.
+With `BestEffortFIFO` and `Eviction` requeue timestamp (in default/common
+configuration), the workload moves to the back of the queue, enabling
+round-robin rotation. When re-admitted, the `QuotaReserved` timestamp resets,
+restarting the `minAdmitDuration` timer.
+
+## Design Details
+
+### API Changes
+
+Add a new optional field `withinClusterQueueConfig` to `ClusterQueuePreemption`:
+
+```go
+type ClusterQueuePreemption struct {
+    ReclaimWithinCohort PreemptionPolicy    `json:"reclaimWithinCohort,omitempty"`
+    BorrowWithinCohort  *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+    WithinClusterQueue  PreemptionPolicy    `json:"withinClusterQueue,omitempty"`
+
+    // withinClusterQueueConfig provides additional configuration for the
+    // withinClusterQueue preemption policy. Only valid when withinClusterQueue
+    // is set to LowerOrNewerEqualPriority.
+    // +optional
+    WithinClusterQueueConfig *WithinClusterQueueConfig `json:"withinClusterQueueConfig,omitempty"`
+}
+
+type WithinClusterQueueConfig struct {
+    // minAdmitDuration specifies the minimum duration a workload must be
+    // admitted before it becomes eligible for same-priority preemption.
+    // The duration is measured from the workload's quota reservation time.
+    // If nil or omitted, time-based same-priority preemption is disabled
+    // and only the existing behavior applies. When set, the minimum
+    // allowed value is 1 minute; values less than 1 minute (including 0)
+    // are rejected by validation.
+    // +optional
+    MinAdmitDuration *metav1.Duration `json:"minAdmitDuration,omitempty"`
+}
+```
+
+### Preemption Eligibility
+
+Preemption only occurs when there is a pending workload that cannot be
+admitted due to insufficient quota. A pending workload can preempt an
+admitted same-priority workload if **either** of these conditions is true:
+
+1. **Existing behavior**: The target's quota reservation time is more recent
+   than the preemptor's creation time (i.e., the target was admitted after the
+   preemptor entered the queue)
```

  I don't think this description is accurate.
  By comparing target's _quota reservation_ time with preemptor's _creation_ time, you postulate some asymmetry between target & preemptor which I don't see in the code.
  (Neither [now](https://github.com/kubernetes-sigs/kueue/blob/baf8a94160fe65077c17a1387c57055c6309ac01/pkg/scheduler/preemption/common/preemption_policy.go#L39-L41) nor [before #9835](https://github.com/kubernetes-sigs/kueue/blob/22f2c7de247ff88fd14a7d88deaff57e068a0df6/pkg/scheduler/preemption/common/preemption_policy.go#L34-L36)).

- **keps/8522-time-based-same-priority-preemption/README.md:238** by [@olekzabl](https://github.com/olekzabl) — 2026-03-16T09:53:27Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Future Extensibility](#future-extensibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dynamic Priority Decay](#dynamic-priority-decay)
+  - [Rely on Admission Fair Sharing Alone](#rely-on-admission-fair-sharing-alone)
+  - [Usage-Based Preemption](#usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
+resource monopolization problem where the first workload admitted to a
+ClusterQueue continues to run indefinitely while same-priority workloads
+remain pending.
+
+**Terminology**: Throughout this KEP, "quota reservation time" refers to the
+`LastTransitionTime` of the workload's `QuotaReserved` condition (when
+`Status=True`). This is the moment when quota was reserved for the workload.
+
+## Motivation
+
+Currently, Kueue offers two preemption policies for `withinClusterQueue`:
+
+1. **`LowerPriority`**: Only preempt workloads with strictly lower priority
+2. **`LowerOrNewerEqualPriority`**: Preempt workloads with lower priority OR
+   workloads with equal priority that were admitted more recently
+
+The `LowerOrNewerEqualPriority` policy was designed to prevent queue starvation
+by allowing newly arrived high-demand workloads to preempt recently admitted
+same-priority workloads. However, this creates a subtle but significant
+problem: **the first workload admitted monopolizes resources indefinitely**.
+
+Consider this scenario:
+- WorkloadA (priority=10) is admitted at T=0
+- WorkloadB (priority=10) arrives at T=5min
+- WorkloadB cannot preempt WorkloadA because WorkloadA is older
+- WorkloadA runs for 24 hours while WorkloadB waits
+
+This behavior contradicts the expectation that same-priority workloads should
+have fair access to cluster resources over time. In multi-tenant environments,
+this can lead to:
+
+- One team's long-running workload blocking other teams indefinitely
+- Users submitting artificially low resource requests to get admitted first,
+  then consuming resources for extended periods
+- Reduced cluster utilization efficiency due to suboptimal scheduling
+
+### Goals
+
+- Extend the `LowerOrNewerEqualPriority` preemption policy with a
+  `minAdmitDuration` configuration that allows preemption of same-priority
+  workloads after they have been admitted for a specified duration
+- Maintain backward compatibility: existing configurations without
+  `minAdmitDuration` behave exactly as before
+
+### Non-Goals
+
+- Dynamic priority that decays over time
+- Per-workload or per-LocalQueue `minAdmitDuration` overrides
+- Precise CPU/GPU time accounting (wall-clock time from admission is used)
+- Cross-ClusterQueue time-based preemption (this feature is scoped to
+  `withinClusterQueue` only)
+
+## Proposal
+
+Introduce a new configuration structure `WithinClusterQueueConfig` that extends
+the `withinClusterQueue` preemption policy with a `minAdmitDuration` field.
+When `withinClusterQueue` is set to `LowerOrNewerEqualPriority` and
+`minAdmitDuration` is configured, workloads become eligible for same-priority
+preemption only after they have been admitted for at least the specified
+duration.
+
+### User Stories
+
+#### Story 1: Fair GPU Time-Sharing Among Data Scientists
+
+As a cluster administrator managing GPU resources for a team of 20 data
+scientists, I want to ensure that each team member gets fair access to GPU
+resources for their training jobs, all of which have the same priority.
+
+Currently, if one data scientist submits a job that starts training and runs
+for days, other team members' jobs remain pending indefinitely. With
+`minAdmitDuration: 4h`, each job is guaranteed 4 hours of runtime before it
+can be preempted by a waiting job.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: ml-training
+spec:
+  preemption:
+    withinClusterQueue: LowerOrNewerEqualPriority
+    withinClusterQueueConfig:
+      minAdmitDuration: 4h
+```
+
+#### Story 2: Preventing Resource Monopolization in Shared Clusters
+
+As a platform team managing a shared Kubernetes cluster for multiple teams,
+I want to prevent any single team from monopolizing cluster resources with
+long-running jobs while ensuring jobs have enough time to make meaningful
+progress.
+
+By setting `minAdmitDuration: 2h`, we ensure that:
+- Jobs have at least 2 hours to run and checkpoint
+- No single job can block others indefinitely
+- Resource access rotates fairly among all pending workloads
+
+### Notes/Constraints/Caveats (Optional)
+
+The `minAdmitDuration` field modifies the behavior of the
+`LowerOrNewerEqualPriority` policy. Without `minAdmitDuration`, the policy
+only preempts workloads that are newer than the preempting workload. With
+`minAdmitDuration`, it additionally preempts workloads that have exceeded
+the minimum duration, regardless of relative age.
+
+This feature is complementary to Fair Sharing (KEP-1714). Fair Sharing
+addresses resource distribution across different ClusterQueues/Cohorts via
+preemption, while this feature addresses time-sharing among workloads with
+identical priority within the same ClusterQueue.
+
+**Interaction with Fair Sharing**: When both Fair Sharing and time-based
+preemption are enabled, time-based preemption only applies to the
+`withinClusterQueue` policy. Cross-ClusterQueue preemption continues to be
+governed by Fair Sharing algorithms. The two mechanisms operate independently
+and do not conflict.
+
+This is similar to Slurm's `PreemptExemptTime` which provides a guaranteed
+minimum runtime before a job becomes preemptible.
+
+### Risks and Mitigations
+
+**Rapid Preemption Cycles**: If `minAdmitDuration` is set too low, workloads
+might be preempted before making meaningful progress. Mitigated by webhook
+validation requiring a minimum of 1 minute.
+
+**Checkpoint Dependency**: Workloads that don't implement checkpointing lose
+progress when preempted. This is not new; workloads already need to handle
+preemption gracefully.
+
+**Controller Restart**: If the Kueue controller restarts, all timing is based
+on the persisted `QuotaReserved` condition timestamps stored in workload
+status, so workloads maintain their accurate admission time across restarts.
+No special handling is required.
+
+**Scheduler Re-evaluation**: When a workload crosses `minAdmitDuration` while
+the scheduler is idle, preemption happens on the next scheduling cycle.
+For typical `minAdmitDuration` values (30m+), scheduler cycles are usually
+frequent enough due to other cluster events. If tighter responsiveness is
+required, a future enhancement could schedule re-evaluation at the earliest
+expiration time.
+
+**Requeue Position**: Preempted workloads follow the existing eviction path.
+With `BestEffortFIFO` and `Eviction` requeue timestamp (in default/common
+configuration), the workload moves to the back of the queue, enabling
+round-robin rotation. When re-admitted, the `QuotaReserved` timestamp resets,
+restarting the `minAdmitDuration` timer.
+
+## Design Details
+
+### API Changes
+
+Add a new optional field `withinClusterQueueConfig` to `ClusterQueuePreemption`:
+
+```go
+type ClusterQueuePreemption struct {
+    ReclaimWithinCohort PreemptionPolicy    `json:"reclaimWithinCohort,omitempty"`
+    BorrowWithinCohort  *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+    WithinClusterQueue  PreemptionPolicy    `json:"withinClusterQueue,omitempty"`
+
+    // withinClusterQueueConfig provides additional configuration for the
+    // withinClusterQueue preemption policy. Only valid when withinClusterQueue
+    // is set to LowerOrNewerEqualPriority.
+    // +optional
+    WithinClusterQueueConfig *WithinClusterQueueConfig `json:"withinClusterQueueConfig,omitempty"`
+}
+
+type WithinClusterQueueConfig struct {
+    // minAdmitDuration specifies the minimum duration a workload must be
+    // admitted before it becomes eligible for same-priority preemption.
+    // The duration is measured from the workload's quota reservation time.
+    // If nil or omitted, time-based same-priority preemption is disabled
+    // and only the existing behavior applies. When set, the minimum
+    // allowed value is 1 minute; values less than 1 minute (including 0)
+    // are rejected by validation.
+    // +optional
+    MinAdmitDuration *metav1.Duration `json:"minAdmitDuration,omitempty"`
+}
+```
+
+### Preemption Eligibility
+
+Preemption only occurs when there is a pending workload that cannot be
+admitted due to insufficient quota. A pending workload can preempt an
+admitted same-priority workload if **either** of these conditions is true:
+
+1. **Existing behavior**: The target's quota reservation time is more recent
+   than the preemptor's creation time (i.e., the target was admitted after the
+   preemptor entered the queue)
+2. **New behavior**: The target has exceeded `minAdmitDuration` (i.e.,
+   `now - target.quotaReservationTime > minAdmitDuration`)
+
+Note: Condition 1 preserves backward compatibility with the existing
+`LowerOrNewerEqualPriority` behavior where a workload that has been waiting
+longer in the queue has priority over one that was admitted after the waiting
+workload was created.
+
+**Preemption order among same-priority candidates**:
+1. Time-expired workloads (exceeded `minAdmitDuration`) are preempted before
+   "newer" workloads (admitted after preemptor was created)
```

  I'd like to ask about this choice.
  It doesn't feel _wrong_ to me - but it feels _not obvious_.
  
  IIUC if we chose the other way (preempt "newer" before "time-expired" ones), we would also achieve the goals as outlined in the User Stories (i.e. avoid starvation of newer workloads), just in a more "conservative" way (i.e. remaining closer to the current semantics).
  
  I'm sensing there should be noticeable difference between the two choices once we consider more complex scenarios, more workloads queueing over time etc.
  
  I'd be happy to learn your thoughts on this.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-04-07T05:41:24Z

- **keps/8522-time-based-same-priority-preemption/README.md:100** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-07T04:00:07Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Future Extensibility](#future-extensibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dynamic Priority Decay](#dynamic-priority-decay)
+  - [Rely on Admission Fair Sharing Alone](#rely-on-admission-fair-sharing-alone)
+  - [Usage-Based Preemption](#usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
+resource monopolization problem where the first workload admitted to a
+ClusterQueue continues to run indefinitely while same-priority workloads
+remain pending.
+
+**Terminology**: Throughout this KEP, "quota reservation time" refers to the
+`LastTransitionTime` of the workload's `QuotaReserved` condition (when
+`Status=True`). This is the moment when quota was reserved for the workload.
+
+## Motivation
+
+Currently, Kueue offers two preemption policies for `withinClusterQueue`:
+
+1. **`LowerPriority`**: Only preempt workloads with strictly lower priority
+2. **`LowerOrNewerEqualPriority`**: Preempt workloads with lower priority OR
+   workloads with equal priority that were admitted more recently
+
+The `LowerOrNewerEqualPriority` policy was designed to prevent queue starvation
+by allowing newly arrived high-demand workloads to preempt recently admitted
+same-priority workloads. However, this creates a subtle but significant
+problem: **the first workload admitted monopolizes resources indefinitely**.
+
+Consider this scenario:
+- WorkloadA (priority=10) is admitted at T=0
+- WorkloadB (priority=10) arrives at T=5min
+- WorkloadB cannot preempt WorkloadA because WorkloadA is older
+- WorkloadA runs for 24 hours while WorkloadB waits
+
+This behavior contradicts the expectation that same-priority workloads should
+have fair access to cluster resources over time. In multi-tenant environments,
+this can lead to:
+
+- One team's long-running workload blocking other teams indefinitely
+- Users submitting artificially low resource requests to get admitted first,
+  then consuming resources for extended periods
+- Reduced cluster utilization efficiency due to suboptimal scheduling
+
+### Goals
+
+- Extend the `LowerOrNewerEqualPriority` preemption policy with a
+  `minAdmitDuration` configuration that allows preemption of same-priority
+  workloads after they have been admitted for a specified duration
+- Maintain backward compatibility: existing configurations without
+  `minAdmitDuration` behave exactly as before
+
+### Non-Goals
+
+- Dynamic priority that decays over time
+- Per-workload or per-LocalQueue `minAdmitDuration` overrides
+- Precise CPU/GPU time accounting (wall-clock time from admission is used)
+- Cross-ClusterQueue time-based preemption (this feature is scoped to
+  `withinClusterQueue` only)
+
+## Proposal
+
+Introduce a new configuration structure `WithinClusterQueueConfig` that extends
+the `withinClusterQueue` preemption policy with a `minAdmitDuration` field.
+When `withinClusterQueue` is set to `LowerOrNewerEqualPriority` and
+`minAdmitDuration` is configured, workloads become eligible for same-priority
+preemption only after they have been admitted for at least the specified
+duration.
+
+### User Stories
```

  They serve different audiences. Story 1 is a single-team GPU sharing scenario, Story 2 is a multi-team platform scenario. Keeping both for clarity.

- **keps/8522-time-based-same-priority-preemption/README.md:38** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-07T04:07:47Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Future Extensibility](#future-extensibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dynamic Priority Decay](#dynamic-priority-decay)
+  - [Rely on Admission Fair Sharing Alone](#rely-on-admission-fair-sharing-alone)
+  - [Usage-Based Preemption](#usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
```

  @olekzabl Good catch! Looking at the code, the existing `LowerOrNewerEqualPriority`comparison uses `GetQueueOrderTimestamp()` for both preemptor and candidate, which returns `CreationTimestamp` by default, not `QuotaReserved` time. The KEP's use of "quota reservation time" in the terminology section was inaccurate about the existing behavior.
                                                                                                                                                                                                                For the new time-based path, the question is what timestamp to use for measuring "how long has this workload been running." I'll use the Admitted condition's `LastTransitionTime` because that's when pods actually started. This is a new comparison that doesn't interact with the existing ordering timestamp. Will fix the terminology section.

- **keps/8522-time-based-same-priority-preemption/README.md:208** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-07T04:08:58Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Future Extensibility](#future-extensibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dynamic Priority Decay](#dynamic-priority-decay)
+  - [Rely on Admission Fair Sharing Alone](#rely-on-admission-fair-sharing-alone)
+  - [Usage-Based Preemption](#usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
+resource monopolization problem where the first workload admitted to a
+ClusterQueue continues to run indefinitely while same-priority workloads
+remain pending.
+
+**Terminology**: Throughout this KEP, "quota reservation time" refers to the
+`LastTransitionTime` of the workload's `QuotaReserved` condition (when
+`Status=True`). This is the moment when quota was reserved for the workload.
+
+## Motivation
+
+Currently, Kueue offers two preemption policies for `withinClusterQueue`:
+
+1. **`LowerPriority`**: Only preempt workloads with strictly lower priority
+2. **`LowerOrNewerEqualPriority`**: Preempt workloads with lower priority OR
+   workloads with equal priority that were admitted more recently
+
+The `LowerOrNewerEqualPriority` policy was designed to prevent queue starvation
+by allowing newly arrived high-demand workloads to preempt recently admitted
+same-priority workloads. However, this creates a subtle but significant
+problem: **the first workload admitted monopolizes resources indefinitely**.
+
+Consider this scenario:
+- WorkloadA (priority=10) is admitted at T=0
+- WorkloadB (priority=10) arrives at T=5min
+- WorkloadB cannot preempt WorkloadA because WorkloadA is older
+- WorkloadA runs for 24 hours while WorkloadB waits
+
+This behavior contradicts the expectation that same-priority workloads should
+have fair access to cluster resources over time. In multi-tenant environments,
+this can lead to:
+
+- One team's long-running workload blocking other teams indefinitely
+- Users submitting artificially low resource requests to get admitted first,
+  then consuming resources for extended periods
+- Reduced cluster utilization efficiency due to suboptimal scheduling
+
+### Goals
+
+- Extend the `LowerOrNewerEqualPriority` preemption policy with a
+  `minAdmitDuration` configuration that allows preemption of same-priority
+  workloads after they have been admitted for a specified duration
+- Maintain backward compatibility: existing configurations without
+  `minAdmitDuration` behave exactly as before
+
+### Non-Goals
+
+- Dynamic priority that decays over time
+- Per-workload or per-LocalQueue `minAdmitDuration` overrides
+- Precise CPU/GPU time accounting (wall-clock time from admission is used)
+- Cross-ClusterQueue time-based preemption (this feature is scoped to
+  `withinClusterQueue` only)
+
+## Proposal
+
+Introduce a new configuration structure `WithinClusterQueueConfig` that extends
+the `withinClusterQueue` preemption policy with a `minAdmitDuration` field.
+When `withinClusterQueue` is set to `LowerOrNewerEqualPriority` and
+`minAdmitDuration` is configured, workloads become eligible for same-priority
+preemption only after they have been admitted for at least the specified
+duration.
+
+### User Stories
+
+#### Story 1: Fair GPU Time-Sharing Among Data Scientists
+
+As a cluster administrator managing GPU resources for a team of 20 data
+scientists, I want to ensure that each team member gets fair access to GPU
+resources for their training jobs, all of which have the same priority.
+
+Currently, if one data scientist submits a job that starts training and runs
+for days, other team members' jobs remain pending indefinitely. With
+`minAdmitDuration: 4h`, each job is guaranteed 4 hours of runtime before it
+can be preempted by a waiting job.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: ml-training
+spec:
+  preemption:
+    withinClusterQueue: LowerOrNewerEqualPriority
+    withinClusterQueueConfig:
+      minAdmitDuration: 4h
+```
+
+#### Story 2: Preventing Resource Monopolization in Shared Clusters
+
+As a platform team managing a shared Kubernetes cluster for multiple teams,
+I want to prevent any single team from monopolizing cluster resources with
+long-running jobs while ensuring jobs have enough time to make meaningful
+progress.
+
+By setting `minAdmitDuration: 2h`, we ensure that:
+- Jobs have at least 2 hours to run and checkpoint
+- No single job can block others indefinitely
+- Resource access rotates fairly among all pending workloads
+
+### Notes/Constraints/Caveats (Optional)
+
+The `minAdmitDuration` field modifies the behavior of the
+`LowerOrNewerEqualPriority` policy. Without `minAdmitDuration`, the policy
+only preempts workloads that are newer than the preempting workload. With
+`minAdmitDuration`, it additionally preempts workloads that have exceeded
+the minimum duration, regardless of relative age.
+
+This feature is complementary to Fair Sharing (KEP-1714). Fair Sharing
+addresses resource distribution across different ClusterQueues/Cohorts via
+preemption, while this feature addresses time-sharing among workloads with
+identical priority within the same ClusterQueue.
+
+**Interaction with Fair Sharing**: When both Fair Sharing and time-based
+preemption are enabled, time-based preemption only applies to the
+`withinClusterQueue` policy. Cross-ClusterQueue preemption continues to be
+governed by Fair Sharing algorithms. The two mechanisms operate independently
+and do not conflict.
+
+This is similar to Slurm's `PreemptExemptTime` which provides a guaranteed
+minimum runtime before a job becomes preemptible.
+
+### Risks and Mitigations
+
+**Rapid Preemption Cycles**: If `minAdmitDuration` is set too low, workloads
+might be preempted before making meaningful progress. Mitigated by webhook
+validation requiring a minimum of 1 minute.
+
+**Checkpoint Dependency**: Workloads that don't implement checkpointing lose
+progress when preempted. This is not new; workloads already need to handle
+preemption gracefully.
+
+**Controller Restart**: If the Kueue controller restarts, all timing is based
+on the persisted `QuotaReserved` condition timestamps stored in workload
+status, so workloads maintain their accurate admission time across restarts.
+No special handling is required.
+
+**Scheduler Re-evaluation**: When a workload crosses `minAdmitDuration` while
+the scheduler is idle, preemption happens on the next scheduling cycle.
+For typical `minAdmitDuration` values (30m+), scheduler cycles are usually
+frequent enough due to other cluster events. If tighter responsiveness is
+required, a future enhancement could schedule re-evaluation at the earliest
+expiration time.
+
+**Requeue Position**: Preempted workloads follow the existing eviction path.
+With `BestEffortFIFO` and `Eviction` requeue timestamp (in default/common
+configuration), the workload moves to the back of the queue, enabling
+round-robin rotation. When re-admitted, the `QuotaReserved` timestamp resets,
+restarting the `minAdmitDuration` timer.
+
+## Design Details
+
+### API Changes
+
+Add a new optional field `withinClusterQueueConfig` to `ClusterQueuePreemption`:
+
+```go
+type ClusterQueuePreemption struct {
+    ReclaimWithinCohort PreemptionPolicy    `json:"reclaimWithinCohort,omitempty"`
+    BorrowWithinCohort  *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+    WithinClusterQueue  PreemptionPolicy    `json:"withinClusterQueue,omitempty"`
+
+    // withinClusterQueueConfig provides additional configuration for the
+    // withinClusterQueue preemption policy. Only valid when withinClusterQueue
+    // is set to LowerOrNewerEqualPriority.
+    // +optional
+    WithinClusterQueueConfig *WithinClusterQueueConfig `json:"withinClusterQueueConfig,omitempty"`
+}
+
+type WithinClusterQueueConfig struct {
+    // minAdmitDuration specifies the minimum duration a workload must be
+    // admitted before it becomes eligible for same-priority preemption.
```

  Will fix. `minAdmitDuration` adds a second eligibility path for older workloads. The existing newer path (creation-time-based, not reservation-time-based as the KEP incorrectly stated) is unchanged.

- **keps/8522-time-based-same-priority-preemption/README.md:284** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-07T04:12:13Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Future Extensibility](#future-extensibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dynamic Priority Decay](#dynamic-priority-decay)
+  - [Rely on Admission Fair Sharing Alone](#rely-on-admission-fair-sharing-alone)
+  - [Usage-Based Preemption](#usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
+resource monopolization problem where the first workload admitted to a
+ClusterQueue continues to run indefinitely while same-priority workloads
+remain pending.
+
+**Terminology**: Throughout this KEP, "quota reservation time" refers to the
+`LastTransitionTime` of the workload's `QuotaReserved` condition (when
+`Status=True`). This is the moment when quota was reserved for the workload.
+
+## Motivation
+
+Currently, Kueue offers two preemption policies for `withinClusterQueue`:
+
+1. **`LowerPriority`**: Only preempt workloads with strictly lower priority
+2. **`LowerOrNewerEqualPriority`**: Preempt workloads with lower priority OR
+   workloads with equal priority that were admitted more recently
+
+The `LowerOrNewerEqualPriority` policy was designed to prevent queue starvation
+by allowing newly arrived high-demand workloads to preempt recently admitted
+same-priority workloads. However, this creates a subtle but significant
+problem: **the first workload admitted monopolizes resources indefinitely**.
+
+Consider this scenario:
+- WorkloadA (priority=10) is admitted at T=0
+- WorkloadB (priority=10) arrives at T=5min
+- WorkloadB cannot preempt WorkloadA because WorkloadA is older
+- WorkloadA runs for 24 hours while WorkloadB waits
+
+This behavior contradicts the expectation that same-priority workloads should
+have fair access to cluster resources over time. In multi-tenant environments,
+this can lead to:
+
+- One team's long-running workload blocking other teams indefinitely
+- Users submitting artificially low resource requests to get admitted first,
+  then consuming resources for extended periods
+- Reduced cluster utilization efficiency due to suboptimal scheduling
+
+### Goals
+
+- Extend the `LowerOrNewerEqualPriority` preemption policy with a
+  `minAdmitDuration` configuration that allows preemption of same-priority
+  workloads after they have been admitted for a specified duration
+- Maintain backward compatibility: existing configurations without
+  `minAdmitDuration` behave exactly as before
+
+### Non-Goals
+
+- Dynamic priority that decays over time
+- Per-workload or per-LocalQueue `minAdmitDuration` overrides
+- Precise CPU/GPU time accounting (wall-clock time from admission is used)
+- Cross-ClusterQueue time-based preemption (this feature is scoped to
+  `withinClusterQueue` only)
+
+## Proposal
+
+Introduce a new configuration structure `WithinClusterQueueConfig` that extends
+the `withinClusterQueue` preemption policy with a `minAdmitDuration` field.
+When `withinClusterQueue` is set to `LowerOrNewerEqualPriority` and
+`minAdmitDuration` is configured, workloads become eligible for same-priority
+preemption only after they have been admitted for at least the specified
+duration.
+
+### User Stories
+
+#### Story 1: Fair GPU Time-Sharing Among Data Scientists
+
+As a cluster administrator managing GPU resources for a team of 20 data
+scientists, I want to ensure that each team member gets fair access to GPU
+resources for their training jobs, all of which have the same priority.
+
+Currently, if one data scientist submits a job that starts training and runs
+for days, other team members' jobs remain pending indefinitely. With
+`minAdmitDuration: 4h`, each job is guaranteed 4 hours of runtime before it
+can be preempted by a waiting job.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: ml-training
+spec:
+  preemption:
+    withinClusterQueue: LowerOrNewerEqualPriority
+    withinClusterQueueConfig:
+      minAdmitDuration: 4h
+```
+
+#### Story 2: Preventing Resource Monopolization in Shared Clusters
+
+As a platform team managing a shared Kubernetes cluster for multiple teams,
+I want to prevent any single team from monopolizing cluster resources with
+long-running jobs while ensuring jobs have enough time to make meaningful
+progress.
+
+By setting `minAdmitDuration: 2h`, we ensure that:
+- Jobs have at least 2 hours to run and checkpoint
+- No single job can block others indefinitely
+- Resource access rotates fairly among all pending workloads
+
+### Notes/Constraints/Caveats (Optional)
+
+The `minAdmitDuration` field modifies the behavior of the
+`LowerOrNewerEqualPriority` policy. Without `minAdmitDuration`, the policy
+only preempts workloads that are newer than the preempting workload. With
+`minAdmitDuration`, it additionally preempts workloads that have exceeded
+the minimum duration, regardless of relative age.
+
+This feature is complementary to Fair Sharing (KEP-1714). Fair Sharing
+addresses resource distribution across different ClusterQueues/Cohorts via
+preemption, while this feature addresses time-sharing among workloads with
+identical priority within the same ClusterQueue.
+
+**Interaction with Fair Sharing**: When both Fair Sharing and time-based
+preemption are enabled, time-based preemption only applies to the
+`withinClusterQueue` policy. Cross-ClusterQueue preemption continues to be
+governed by Fair Sharing algorithms. The two mechanisms operate independently
+and do not conflict.
+
+This is similar to Slurm's `PreemptExemptTime` which provides a guaranteed
+minimum runtime before a job becomes preemptible.
+
+### Risks and Mitigations
+
+**Rapid Preemption Cycles**: If `minAdmitDuration` is set too low, workloads
+might be preempted before making meaningful progress. Mitigated by webhook
+validation requiring a minimum of 1 minute.
+
+**Checkpoint Dependency**: Workloads that don't implement checkpointing lose
+progress when preempted. This is not new; workloads already need to handle
+preemption gracefully.
+
+**Controller Restart**: If the Kueue controller restarts, all timing is based
+on the persisted `QuotaReserved` condition timestamps stored in workload
+status, so workloads maintain their accurate admission time across restarts.
+No special handling is required.
+
+**Scheduler Re-evaluation**: When a workload crosses `minAdmitDuration` while
+the scheduler is idle, preemption happens on the next scheduling cycle.
+For typical `minAdmitDuration` values (30m+), scheduler cycles are usually
+frequent enough due to other cluster events. If tighter responsiveness is
+required, a future enhancement could schedule re-evaluation at the earliest
+expiration time.
+
+**Requeue Position**: Preempted workloads follow the existing eviction path.
+With `BestEffortFIFO` and `Eviction` requeue timestamp (in default/common
+configuration), the workload moves to the back of the queue, enabling
+round-robin rotation. When re-admitted, the `QuotaReserved` timestamp resets,
+restarting the `minAdmitDuration` timer.
+
+## Design Details
+
+### API Changes
+
+Add a new optional field `withinClusterQueueConfig` to `ClusterQueuePreemption`:
+
+```go
+type ClusterQueuePreemption struct {
+    ReclaimWithinCohort PreemptionPolicy    `json:"reclaimWithinCohort,omitempty"`
+    BorrowWithinCohort  *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+    WithinClusterQueue  PreemptionPolicy    `json:"withinClusterQueue,omitempty"`
+
+    // withinClusterQueueConfig provides additional configuration for the
+    // withinClusterQueue preemption policy. Only valid when withinClusterQueue
+    // is set to LowerOrNewerEqualPriority.
+    // +optional
+    WithinClusterQueueConfig *WithinClusterQueueConfig `json:"withinClusterQueueConfig,omitempty"`
+}
+
+type WithinClusterQueueConfig struct {
+    // minAdmitDuration specifies the minimum duration a workload must be
+    // admitted before it becomes eligible for same-priority preemption.
+    // The duration is measured from the workload's quota reservation time.
+    // If nil or omitted, time-based same-priority preemption is disabled
+    // and only the existing behavior applies. When set, the minimum
+    // allowed value is 1 minute; values less than 1 minute (including 0)
+    // are rejected by validation.
+    // +optional
+    MinAdmitDuration *metav1.Duration `json:"minAdmitDuration,omitempty"`
+}
+```
+
+### Preemption Eligibility
+
+Preemption only occurs when there is a pending workload that cannot be
+admitted due to insufficient quota. A pending workload can preempt an
+admitted same-priority workload if **either** of these conditions is true:
+
+1. **Existing behavior**: The target's quota reservation time is more recent
+   than the preemptor's creation time (i.e., the target was admitted after the
+   preemptor entered the queue)
+2. **New behavior**: The target has exceeded `minAdmitDuration` (i.e.,
+   `now - target.quotaReservationTime > minAdmitDuration`)
+
+Note: Condition 1 preserves backward compatibility with the existing
+`LowerOrNewerEqualPriority` behavior where a workload that has been waiting
+longer in the queue has priority over one that was admitted after the waiting
+workload was created.
+
+**Preemption order among same-priority candidates**:
+1. Time-expired workloads (exceeded `minAdmitDuration`) are preempted before
+   "newer" workloads (admitted after preemptor was created)
+2. Among time-expired workloads, the longest-running is preempted first
+3. Among "newer" workloads, the most recently admitted is preempted first
+
+This ordering ensures workloads that have had their guaranteed runtime are
+preempted before those still within their protected period.
+
+### Future Extensibility
+
+The `WithinClusterQueueConfig` struct is designed to accommodate additional
+preemption policies. For example, a complementary use case has been raised
+where workloads should become immune to lower-priority preemption after a
+maximum duration. Such a field could be added to the same struct without
+API breakage and would operate independently of `minAdmitDuration`.
+
+### Webhook Validation
+
+1. `minAdmitDuration` must be at least 1 minute. Values less than 1 minute
+   (including explicit `0s` and negative values) are rejected with an error.
+2. If `withinClusterQueueConfig` is specified but `minAdmitDuration` is `nil`,
+   the configuration is valid but has no effect (time-based preemption is
+   disabled, only the existing `LowerOrNewerEqualPriority` behavior applies).
+3. `withinClusterQueueConfig` is only valid when `withinClusterQueue` is
+   `LowerOrNewerEqualPriority`. Setting it with `Never` or `LowerPriority`
+   is rejected with an error indicating the incompatible configuration.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+#### Unit Tests
+
+- `pkg/scheduler/preemption`: Test preemption candidate selection with
+  `minAdmitDuration`
+- `apis/kueue/v1beta2/clusterqueue_webhook`: Test validation rules
+
+#### Integration tests
+
+1. Two same-priority workloads competing for quota, first admitted workload
+   is preempted after `minAdmitDuration`
+2. Multiple workloads queued, fair rotation occurs as each exceeds threshold
+3. Mixed priorities: lower priority always preempted before same-priority
+4. `minAdmitDuration` not set: existing behavior preserved
+5. Edge case: workload exactly at `minAdmitDuration` threshold (should NOT
+   be preempted, as comparison requires strictly greater than)
```

  Agreed with your own assessment. Using >=, dropping the edge case test.

- **keps/8522-time-based-same-priority-preemption/README.md:227** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-07T04:38:25Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Future Extensibility](#future-extensibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dynamic Priority Decay](#dynamic-priority-decay)
+  - [Rely on Admission Fair Sharing Alone](#rely-on-admission-fair-sharing-alone)
+  - [Usage-Based Preemption](#usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
+resource monopolization problem where the first workload admitted to a
+ClusterQueue continues to run indefinitely while same-priority workloads
+remain pending.
+
+**Terminology**: Throughout this KEP, "quota reservation time" refers to the
+`LastTransitionTime` of the workload's `QuotaReserved` condition (when
+`Status=True`). This is the moment when quota was reserved for the workload.
+
+## Motivation
+
+Currently, Kueue offers two preemption policies for `withinClusterQueue`:
+
+1. **`LowerPriority`**: Only preempt workloads with strictly lower priority
+2. **`LowerOrNewerEqualPriority`**: Preempt workloads with lower priority OR
+   workloads with equal priority that were admitted more recently
+
+The `LowerOrNewerEqualPriority` policy was designed to prevent queue starvation
+by allowing newly arrived high-demand workloads to preempt recently admitted
+same-priority workloads. However, this creates a subtle but significant
+problem: **the first workload admitted monopolizes resources indefinitely**.
+
+Consider this scenario:
+- WorkloadA (priority=10) is admitted at T=0
+- WorkloadB (priority=10) arrives at T=5min
+- WorkloadB cannot preempt WorkloadA because WorkloadA is older
+- WorkloadA runs for 24 hours while WorkloadB waits
+
+This behavior contradicts the expectation that same-priority workloads should
+have fair access to cluster resources over time. In multi-tenant environments,
+this can lead to:
+
+- One team's long-running workload blocking other teams indefinitely
+- Users submitting artificially low resource requests to get admitted first,
+  then consuming resources for extended periods
+- Reduced cluster utilization efficiency due to suboptimal scheduling
+
+### Goals
+
+- Extend the `LowerOrNewerEqualPriority` preemption policy with a
+  `minAdmitDuration` configuration that allows preemption of same-priority
+  workloads after they have been admitted for a specified duration
+- Maintain backward compatibility: existing configurations without
+  `minAdmitDuration` behave exactly as before
+
+### Non-Goals
+
+- Dynamic priority that decays over time
+- Per-workload or per-LocalQueue `minAdmitDuration` overrides
+- Precise CPU/GPU time accounting (wall-clock time from admission is used)
+- Cross-ClusterQueue time-based preemption (this feature is scoped to
+  `withinClusterQueue` only)
+
+## Proposal
+
+Introduce a new configuration structure `WithinClusterQueueConfig` that extends
+the `withinClusterQueue` preemption policy with a `minAdmitDuration` field.
+When `withinClusterQueue` is set to `LowerOrNewerEqualPriority` and
+`minAdmitDuration` is configured, workloads become eligible for same-priority
+preemption only after they have been admitted for at least the specified
+duration.
+
+### User Stories
+
+#### Story 1: Fair GPU Time-Sharing Among Data Scientists
+
+As a cluster administrator managing GPU resources for a team of 20 data
+scientists, I want to ensure that each team member gets fair access to GPU
+resources for their training jobs, all of which have the same priority.
+
+Currently, if one data scientist submits a job that starts training and runs
+for days, other team members' jobs remain pending indefinitely. With
+`minAdmitDuration: 4h`, each job is guaranteed 4 hours of runtime before it
+can be preempted by a waiting job.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: ml-training
+spec:
+  preemption:
+    withinClusterQueue: LowerOrNewerEqualPriority
+    withinClusterQueueConfig:
+      minAdmitDuration: 4h
+```
+
+#### Story 2: Preventing Resource Monopolization in Shared Clusters
+
+As a platform team managing a shared Kubernetes cluster for multiple teams,
+I want to prevent any single team from monopolizing cluster resources with
+long-running jobs while ensuring jobs have enough time to make meaningful
+progress.
+
+By setting `minAdmitDuration: 2h`, we ensure that:
+- Jobs have at least 2 hours to run and checkpoint
+- No single job can block others indefinitely
+- Resource access rotates fairly among all pending workloads
+
+### Notes/Constraints/Caveats (Optional)
+
+The `minAdmitDuration` field modifies the behavior of the
+`LowerOrNewerEqualPriority` policy. Without `minAdmitDuration`, the policy
+only preempts workloads that are newer than the preempting workload. With
+`minAdmitDuration`, it additionally preempts workloads that have exceeded
+the minimum duration, regardless of relative age.
+
+This feature is complementary to Fair Sharing (KEP-1714). Fair Sharing
+addresses resource distribution across different ClusterQueues/Cohorts via
+preemption, while this feature addresses time-sharing among workloads with
+identical priority within the same ClusterQueue.
+
+**Interaction with Fair Sharing**: When both Fair Sharing and time-based
+preemption are enabled, time-based preemption only applies to the
+`withinClusterQueue` policy. Cross-ClusterQueue preemption continues to be
+governed by Fair Sharing algorithms. The two mechanisms operate independently
+and do not conflict.
+
+This is similar to Slurm's `PreemptExemptTime` which provides a guaranteed
+minimum runtime before a job becomes preemptible.
+
+### Risks and Mitigations
+
+**Rapid Preemption Cycles**: If `minAdmitDuration` is set too low, workloads
+might be preempted before making meaningful progress. Mitigated by webhook
+validation requiring a minimum of 1 minute.
+
+**Checkpoint Dependency**: Workloads that don't implement checkpointing lose
+progress when preempted. This is not new; workloads already need to handle
+preemption gracefully.
+
+**Controller Restart**: If the Kueue controller restarts, all timing is based
+on the persisted `QuotaReserved` condition timestamps stored in workload
+status, so workloads maintain their accurate admission time across restarts.
+No special handling is required.
+
+**Scheduler Re-evaluation**: When a workload crosses `minAdmitDuration` while
+the scheduler is idle, preemption happens on the next scheduling cycle.
+For typical `minAdmitDuration` values (30m+), scheduler cycles are usually
+frequent enough due to other cluster events. If tighter responsiveness is
+required, a future enhancement could schedule re-evaluation at the earliest
+expiration time.
+
+**Requeue Position**: Preempted workloads follow the existing eviction path.
+With `BestEffortFIFO` and `Eviction` requeue timestamp (in default/common
+configuration), the workload moves to the back of the queue, enabling
+round-robin rotation. When re-admitted, the `QuotaReserved` timestamp resets,
+restarting the `minAdmitDuration` timer.
+
+## Design Details
+
+### API Changes
+
+Add a new optional field `withinClusterQueueConfig` to `ClusterQueuePreemption`:
+
+```go
+type ClusterQueuePreemption struct {
+    ReclaimWithinCohort PreemptionPolicy    `json:"reclaimWithinCohort,omitempty"`
+    BorrowWithinCohort  *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+    WithinClusterQueue  PreemptionPolicy    `json:"withinClusterQueue,omitempty"`
+
+    // withinClusterQueueConfig provides additional configuration for the
+    // withinClusterQueue preemption policy. Only valid when withinClusterQueue
+    // is set to LowerOrNewerEqualPriority.
+    // +optional
+    WithinClusterQueueConfig *WithinClusterQueueConfig `json:"withinClusterQueueConfig,omitempty"`
+}
+
+type WithinClusterQueueConfig struct {
+    // minAdmitDuration specifies the minimum duration a workload must be
+    // admitted before it becomes eligible for same-priority preemption.
+    // The duration is measured from the workload's quota reservation time.
+    // If nil or omitted, time-based same-priority preemption is disabled
+    // and only the existing behavior applies. When set, the minimum
+    // allowed value is 1 minute; values less than 1 minute (including 0)
+    // are rejected by validation.
+    // +optional
+    MinAdmitDuration *metav1.Duration `json:"minAdmitDuration,omitempty"`
+}
+```
+
+### Preemption Eligibility
+
+Preemption only occurs when there is a pending workload that cannot be
+admitted due to insufficient quota. A pending workload can preempt an
+admitted same-priority workload if **either** of these conditions is true:
+
+1. **Existing behavior**: The target's quota reservation time is more recent
+   than the preemptor's creation time (i.e., the target was admitted after the
+   preemptor entered the queue)
```

  Yes, will fix that.

- **keps/8522-time-based-same-priority-preemption/README.md:238** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-07T04:45:43Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Future Extensibility](#future-extensibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dynamic Priority Decay](#dynamic-priority-decay)
+  - [Rely on Admission Fair Sharing Alone](#rely-on-admission-fair-sharing-alone)
+  - [Usage-Based Preemption](#usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
+resource monopolization problem where the first workload admitted to a
+ClusterQueue continues to run indefinitely while same-priority workloads
+remain pending.
+
+**Terminology**: Throughout this KEP, "quota reservation time" refers to the
+`LastTransitionTime` of the workload's `QuotaReserved` condition (when
+`Status=True`). This is the moment when quota was reserved for the workload.
+
+## Motivation
+
+Currently, Kueue offers two preemption policies for `withinClusterQueue`:
+
+1. **`LowerPriority`**: Only preempt workloads with strictly lower priority
+2. **`LowerOrNewerEqualPriority`**: Preempt workloads with lower priority OR
+   workloads with equal priority that were admitted more recently
+
+The `LowerOrNewerEqualPriority` policy was designed to prevent queue starvation
+by allowing newly arrived high-demand workloads to preempt recently admitted
+same-priority workloads. However, this creates a subtle but significant
+problem: **the first workload admitted monopolizes resources indefinitely**.
+
+Consider this scenario:
+- WorkloadA (priority=10) is admitted at T=0
+- WorkloadB (priority=10) arrives at T=5min
+- WorkloadB cannot preempt WorkloadA because WorkloadA is older
+- WorkloadA runs for 24 hours while WorkloadB waits
+
+This behavior contradicts the expectation that same-priority workloads should
+have fair access to cluster resources over time. In multi-tenant environments,
+this can lead to:
+
+- One team's long-running workload blocking other teams indefinitely
+- Users submitting artificially low resource requests to get admitted first,
+  then consuming resources for extended periods
+- Reduced cluster utilization efficiency due to suboptimal scheduling
+
+### Goals
+
+- Extend the `LowerOrNewerEqualPriority` preemption policy with a
+  `minAdmitDuration` configuration that allows preemption of same-priority
+  workloads after they have been admitted for a specified duration
+- Maintain backward compatibility: existing configurations without
+  `minAdmitDuration` behave exactly as before
+
+### Non-Goals
+
+- Dynamic priority that decays over time
+- Per-workload or per-LocalQueue `minAdmitDuration` overrides
+- Precise CPU/GPU time accounting (wall-clock time from admission is used)
+- Cross-ClusterQueue time-based preemption (this feature is scoped to
+  `withinClusterQueue` only)
+
+## Proposal
+
+Introduce a new configuration structure `WithinClusterQueueConfig` that extends
+the `withinClusterQueue` preemption policy with a `minAdmitDuration` field.
+When `withinClusterQueue` is set to `LowerOrNewerEqualPriority` and
+`minAdmitDuration` is configured, workloads become eligible for same-priority
+preemption only after they have been admitted for at least the specified
+duration.
+
+### User Stories
+
+#### Story 1: Fair GPU Time-Sharing Among Data Scientists
+
+As a cluster administrator managing GPU resources for a team of 20 data
+scientists, I want to ensure that each team member gets fair access to GPU
+resources for their training jobs, all of which have the same priority.
+
+Currently, if one data scientist submits a job that starts training and runs
+for days, other team members' jobs remain pending indefinitely. With
+`minAdmitDuration: 4h`, each job is guaranteed 4 hours of runtime before it
+can be preempted by a waiting job.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: ml-training
+spec:
+  preemption:
+    withinClusterQueue: LowerOrNewerEqualPriority
+    withinClusterQueueConfig:
+      minAdmitDuration: 4h
+```
+
+#### Story 2: Preventing Resource Monopolization in Shared Clusters
+
+As a platform team managing a shared Kubernetes cluster for multiple teams,
+I want to prevent any single team from monopolizing cluster resources with
+long-running jobs while ensuring jobs have enough time to make meaningful
+progress.
+
+By setting `minAdmitDuration: 2h`, we ensure that:
+- Jobs have at least 2 hours to run and checkpoint
+- No single job can block others indefinitely
+- Resource access rotates fairly among all pending workloads
+
+### Notes/Constraints/Caveats (Optional)
+
+The `minAdmitDuration` field modifies the behavior of the
+`LowerOrNewerEqualPriority` policy. Without `minAdmitDuration`, the policy
+only preempts workloads that are newer than the preempting workload. With
+`minAdmitDuration`, it additionally preempts workloads that have exceeded
+the minimum duration, regardless of relative age.
+
+This feature is complementary to Fair Sharing (KEP-1714). Fair Sharing
+addresses resource distribution across different ClusterQueues/Cohorts via
+preemption, while this feature addresses time-sharing among workloads with
+identical priority within the same ClusterQueue.
+
+**Interaction with Fair Sharing**: When both Fair Sharing and time-based
+preemption are enabled, time-based preemption only applies to the
+`withinClusterQueue` policy. Cross-ClusterQueue preemption continues to be
+governed by Fair Sharing algorithms. The two mechanisms operate independently
+and do not conflict.
+
+This is similar to Slurm's `PreemptExemptTime` which provides a guaranteed
+minimum runtime before a job becomes preemptible.
+
+### Risks and Mitigations
+
+**Rapid Preemption Cycles**: If `minAdmitDuration` is set too low, workloads
+might be preempted before making meaningful progress. Mitigated by webhook
+validation requiring a minimum of 1 minute.
+
+**Checkpoint Dependency**: Workloads that don't implement checkpointing lose
+progress when preempted. This is not new; workloads already need to handle
+preemption gracefully.
+
+**Controller Restart**: If the Kueue controller restarts, all timing is based
+on the persisted `QuotaReserved` condition timestamps stored in workload
+status, so workloads maintain their accurate admission time across restarts.
+No special handling is required.
+
+**Scheduler Re-evaluation**: When a workload crosses `minAdmitDuration` while
+the scheduler is idle, preemption happens on the next scheduling cycle.
+For typical `minAdmitDuration` values (30m+), scheduler cycles are usually
+frequent enough due to other cluster events. If tighter responsiveness is
+required, a future enhancement could schedule re-evaluation at the earliest
+expiration time.
+
+**Requeue Position**: Preempted workloads follow the existing eviction path.
+With `BestEffortFIFO` and `Eviction` requeue timestamp (in default/common
+configuration), the workload moves to the back of the queue, enabling
+round-robin rotation. When re-admitted, the `QuotaReserved` timestamp resets,
+restarting the `minAdmitDuration` timer.
+
+## Design Details
+
+### API Changes
+
+Add a new optional field `withinClusterQueueConfig` to `ClusterQueuePreemption`:
+
+```go
+type ClusterQueuePreemption struct {
+    ReclaimWithinCohort PreemptionPolicy    `json:"reclaimWithinCohort,omitempty"`
+    BorrowWithinCohort  *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+    WithinClusterQueue  PreemptionPolicy    `json:"withinClusterQueue,omitempty"`
+
+    // withinClusterQueueConfig provides additional configuration for the
+    // withinClusterQueue preemption policy. Only valid when withinClusterQueue
+    // is set to LowerOrNewerEqualPriority.
+    // +optional
+    WithinClusterQueueConfig *WithinClusterQueueConfig `json:"withinClusterQueueConfig,omitempty"`
+}
+
+type WithinClusterQueueConfig struct {
+    // minAdmitDuration specifies the minimum duration a workload must be
+    // admitted before it becomes eligible for same-priority preemption.
+    // The duration is measured from the workload's quota reservation time.
+    // If nil or omitted, time-based same-priority preemption is disabled
+    // and only the existing behavior applies. When set, the minimum
+    // allowed value is 1 minute; values less than 1 minute (including 0)
+    // are rejected by validation.
+    // +optional
+    MinAdmitDuration *metav1.Duration `json:"minAdmitDuration,omitempty"`
+}
+```
+
+### Preemption Eligibility
+
+Preemption only occurs when there is a pending workload that cannot be
+admitted due to insufficient quota. A pending workload can preempt an
+admitted same-priority workload if **either** of these conditions is true:
+
+1. **Existing behavior**: The target's quota reservation time is more recent
+   than the preemptor's creation time (i.e., the target was admitted after the
+   preemptor entered the queue)
+2. **New behavior**: The target has exceeded `minAdmitDuration` (i.e.,
+   `now - target.quotaReservationTime > minAdmitDuration`)
+
+Note: Condition 1 preserves backward compatibility with the existing
+`LowerOrNewerEqualPriority` behavior where a workload that has been waiting
+longer in the queue has priority over one that was admitted after the waiting
+workload was created.
+
+**Preemption order among same-priority candidates**:
+1. Time-expired workloads (exceeded `minAdmitDuration`) are preempted before
+   "newer" workloads (admitted after preemptor was created)
```

  If we preempted newer first, time-expired workloads would never rotate out as there's almost always a newer candidate available. The long-running monopolizer keeps running, which is the exact problem this KEP solves. Preempting expired first ensures workloads that had their guaranteed runtime rotate out before those still within their protected period.

- **keps/8522-time-based-same-priority-preemption/README.md:288** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-07T04:46:41Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Future Extensibility](#future-extensibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dynamic Priority Decay](#dynamic-priority-decay)
+  - [Rely on Admission Fair Sharing Alone](#rely-on-admission-fair-sharing-alone)
+  - [Usage-Based Preemption](#usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
+resource monopolization problem where the first workload admitted to a
+ClusterQueue continues to run indefinitely while same-priority workloads
+remain pending.
+
+**Terminology**: Throughout this KEP, "quota reservation time" refers to the
+`LastTransitionTime` of the workload's `QuotaReserved` condition (when
+`Status=True`). This is the moment when quota was reserved for the workload.
+
+## Motivation
+
+Currently, Kueue offers two preemption policies for `withinClusterQueue`:
+
+1. **`LowerPriority`**: Only preempt workloads with strictly lower priority
+2. **`LowerOrNewerEqualPriority`**: Preempt workloads with lower priority OR
+   workloads with equal priority that were admitted more recently
+
+The `LowerOrNewerEqualPriority` policy was designed to prevent queue starvation
+by allowing newly arrived high-demand workloads to preempt recently admitted
+same-priority workloads. However, this creates a subtle but significant
+problem: **the first workload admitted monopolizes resources indefinitely**.
+
+Consider this scenario:
+- WorkloadA (priority=10) is admitted at T=0
+- WorkloadB (priority=10) arrives at T=5min
+- WorkloadB cannot preempt WorkloadA because WorkloadA is older
+- WorkloadA runs for 24 hours while WorkloadB waits
+
+This behavior contradicts the expectation that same-priority workloads should
+have fair access to cluster resources over time. In multi-tenant environments,
+this can lead to:
+
+- One team's long-running workload blocking other teams indefinitely
+- Users submitting artificially low resource requests to get admitted first,
+  then consuming resources for extended periods
+- Reduced cluster utilization efficiency due to suboptimal scheduling
+
+### Goals
+
+- Extend the `LowerOrNewerEqualPriority` preemption policy with a
+  `minAdmitDuration` configuration that allows preemption of same-priority
+  workloads after they have been admitted for a specified duration
+- Maintain backward compatibility: existing configurations without
+  `minAdmitDuration` behave exactly as before
+
+### Non-Goals
+
+- Dynamic priority that decays over time
+- Per-workload or per-LocalQueue `minAdmitDuration` overrides
+- Precise CPU/GPU time accounting (wall-clock time from admission is used)
+- Cross-ClusterQueue time-based preemption (this feature is scoped to
+  `withinClusterQueue` only)
+
+## Proposal
+
+Introduce a new configuration structure `WithinClusterQueueConfig` that extends
+the `withinClusterQueue` preemption policy with a `minAdmitDuration` field.
+When `withinClusterQueue` is set to `LowerOrNewerEqualPriority` and
+`minAdmitDuration` is configured, workloads become eligible for same-priority
+preemption only after they have been admitted for at least the specified
+duration.
+
+### User Stories
+
+#### Story 1: Fair GPU Time-Sharing Among Data Scientists
+
+As a cluster administrator managing GPU resources for a team of 20 data
+scientists, I want to ensure that each team member gets fair access to GPU
+resources for their training jobs, all of which have the same priority.
+
+Currently, if one data scientist submits a job that starts training and runs
+for days, other team members' jobs remain pending indefinitely. With
+`minAdmitDuration: 4h`, each job is guaranteed 4 hours of runtime before it
+can be preempted by a waiting job.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: ml-training
+spec:
+  preemption:
+    withinClusterQueue: LowerOrNewerEqualPriority
+    withinClusterQueueConfig:
+      minAdmitDuration: 4h
+```
+
+#### Story 2: Preventing Resource Monopolization in Shared Clusters
+
+As a platform team managing a shared Kubernetes cluster for multiple teams,
+I want to prevent any single team from monopolizing cluster resources with
+long-running jobs while ensuring jobs have enough time to make meaningful
+progress.
+
+By setting `minAdmitDuration: 2h`, we ensure that:
+- Jobs have at least 2 hours to run and checkpoint
+- No single job can block others indefinitely
+- Resource access rotates fairly among all pending workloads
+
+### Notes/Constraints/Caveats (Optional)
+
+The `minAdmitDuration` field modifies the behavior of the
+`LowerOrNewerEqualPriority` policy. Without `minAdmitDuration`, the policy
+only preempts workloads that are newer than the preempting workload. With
+`minAdmitDuration`, it additionally preempts workloads that have exceeded
+the minimum duration, regardless of relative age.
+
+This feature is complementary to Fair Sharing (KEP-1714). Fair Sharing
+addresses resource distribution across different ClusterQueues/Cohorts via
+preemption, while this feature addresses time-sharing among workloads with
+identical priority within the same ClusterQueue.
+
+**Interaction with Fair Sharing**: When both Fair Sharing and time-based
+preemption are enabled, time-based preemption only applies to the
+`withinClusterQueue` policy. Cross-ClusterQueue preemption continues to be
+governed by Fair Sharing algorithms. The two mechanisms operate independently
+and do not conflict.
+
+This is similar to Slurm's `PreemptExemptTime` which provides a guaranteed
+minimum runtime before a job becomes preemptible.
+
+### Risks and Mitigations
+
+**Rapid Preemption Cycles**: If `minAdmitDuration` is set too low, workloads
+might be preempted before making meaningful progress. Mitigated by webhook
+validation requiring a minimum of 1 minute.
+
+**Checkpoint Dependency**: Workloads that don't implement checkpointing lose
+progress when preempted. This is not new; workloads already need to handle
+preemption gracefully.
+
+**Controller Restart**: If the Kueue controller restarts, all timing is based
+on the persisted `QuotaReserved` condition timestamps stored in workload
+status, so workloads maintain their accurate admission time across restarts.
+No special handling is required.
+
+**Scheduler Re-evaluation**: When a workload crosses `minAdmitDuration` while
+the scheduler is idle, preemption happens on the next scheduling cycle.
+For typical `minAdmitDuration` values (30m+), scheduler cycles are usually
+frequent enough due to other cluster events. If tighter responsiveness is
+required, a future enhancement could schedule re-evaluation at the earliest
+expiration time.
+
+**Requeue Position**: Preempted workloads follow the existing eviction path.
+With `BestEffortFIFO` and `Eviction` requeue timestamp (in default/common
+configuration), the workload moves to the back of the queue, enabling
+round-robin rotation. When re-admitted, the `QuotaReserved` timestamp resets,
+restarting the `minAdmitDuration` timer.
+
+## Design Details
+
+### API Changes
+
+Add a new optional field `withinClusterQueueConfig` to `ClusterQueuePreemption`:
+
+```go
+type ClusterQueuePreemption struct {
+    ReclaimWithinCohort PreemptionPolicy    `json:"reclaimWithinCohort,omitempty"`
+    BorrowWithinCohort  *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+    WithinClusterQueue  PreemptionPolicy    `json:"withinClusterQueue,omitempty"`
+
+    // withinClusterQueueConfig provides additional configuration for the
+    // withinClusterQueue preemption policy. Only valid when withinClusterQueue
+    // is set to LowerOrNewerEqualPriority.
+    // +optional
+    WithinClusterQueueConfig *WithinClusterQueueConfig `json:"withinClusterQueueConfig,omitempty"`
+}
+
+type WithinClusterQueueConfig struct {
+    // minAdmitDuration specifies the minimum duration a workload must be
+    // admitted before it becomes eligible for same-priority preemption.
+    // The duration is measured from the workload's quota reservation time.
+    // If nil or omitted, time-based same-priority preemption is disabled
+    // and only the existing behavior applies. When set, the minimum
+    // allowed value is 1 minute; values less than 1 minute (including 0)
+    // are rejected by validation.
+    // +optional
+    MinAdmitDuration *metav1.Duration `json:"minAdmitDuration,omitempty"`
+}
+```
+
+### Preemption Eligibility
+
+Preemption only occurs when there is a pending workload that cannot be
+admitted due to insufficient quota. A pending workload can preempt an
+admitted same-priority workload if **either** of these conditions is true:
+
+1. **Existing behavior**: The target's quota reservation time is more recent
+   than the preemptor's creation time (i.e., the target was admitted after the
+   preemptor entered the queue)
+2. **New behavior**: The target has exceeded `minAdmitDuration` (i.e.,
+   `now - target.quotaReservationTime > minAdmitDuration`)
+
+Note: Condition 1 preserves backward compatibility with the existing
+`LowerOrNewerEqualPriority` behavior where a workload that has been waiting
+longer in the queue has priority over one that was admitted after the waiting
+workload was created.
+
+**Preemption order among same-priority candidates**:
+1. Time-expired workloads (exceeded `minAdmitDuration`) are preempted before
+   "newer" workloads (admitted after preemptor was created)
+2. Among time-expired workloads, the longest-running is preempted first
+3. Among "newer" workloads, the most recently admitted is preempted first
+
+This ordering ensures workloads that have had their guaranteed runtime are
+preempted before those still within their protected period.
+
+### Future Extensibility
+
+The `WithinClusterQueueConfig` struct is designed to accommodate additional
+preemption policies. For example, a complementary use case has been raised
+where workloads should become immune to lower-priority preemption after a
+maximum duration. Such a field could be added to the same struct without
+API breakage and would operate independently of `minAdmitDuration`.
+
+### Webhook Validation
+
+1. `minAdmitDuration` must be at least 1 minute. Values less than 1 minute
+   (including explicit `0s` and negative values) are rejected with an error.
+2. If `withinClusterQueueConfig` is specified but `minAdmitDuration` is `nil`,
+   the configuration is valid but has no effect (time-based preemption is
+   disabled, only the existing `LowerOrNewerEqualPriority` behavior applies).
+3. `withinClusterQueueConfig` is only valid when `withinClusterQueue` is
+   `LowerOrNewerEqualPriority`. Setting it with `Never` or `LowerPriority`
+   is rejected with an error indicating the incompatible configuration.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+#### Unit Tests
+
+- `pkg/scheduler/preemption`: Test preemption candidate selection with
+  `minAdmitDuration`
+- `apis/kueue/v1beta2/clusterqueue_webhook`: Test validation rules
+
+#### Integration tests
+
+1. Two same-priority workloads competing for quota, first admitted workload
+   is preempted after `minAdmitDuration`
+2. Multiple workloads queued, fair rotation occurs as each exceeds threshold
+3. Mixed priorities: lower priority always preempted before same-priority
+4. `minAdmitDuration` not set: existing behavior preserved
+5. Edge case: workload exactly at `minAdmitDuration` threshold (should NOT
+   be preempted, as comparison requires strictly greater than)
+6. Controller restart: workload timestamps persist correctly and preemption
+   timing remains accurate
+7. Multiple workloads expiring simultaneously: deterministic ordering based
+   on longest-running first
```

  I will rewrite to `multiple expired candidates in the same scheduling cycle`.

- **keps/8522-time-based-same-priority-preemption/README.md:306** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-07T04:47:14Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Future Extensibility](#future-extensibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dynamic Priority Decay](#dynamic-priority-decay)
+  - [Rely on Admission Fair Sharing Alone](#rely-on-admission-fair-sharing-alone)
+  - [Usage-Based Preemption](#usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
+resource monopolization problem where the first workload admitted to a
+ClusterQueue continues to run indefinitely while same-priority workloads
+remain pending.
+
+**Terminology**: Throughout this KEP, "quota reservation time" refers to the
+`LastTransitionTime` of the workload's `QuotaReserved` condition (when
+`Status=True`). This is the moment when quota was reserved for the workload.
+
+## Motivation
+
+Currently, Kueue offers two preemption policies for `withinClusterQueue`:
+
+1. **`LowerPriority`**: Only preempt workloads with strictly lower priority
+2. **`LowerOrNewerEqualPriority`**: Preempt workloads with lower priority OR
+   workloads with equal priority that were admitted more recently
+
+The `LowerOrNewerEqualPriority` policy was designed to prevent queue starvation
+by allowing newly arrived high-demand workloads to preempt recently admitted
+same-priority workloads. However, this creates a subtle but significant
+problem: **the first workload admitted monopolizes resources indefinitely**.
+
+Consider this scenario:
+- WorkloadA (priority=10) is admitted at T=0
+- WorkloadB (priority=10) arrives at T=5min
+- WorkloadB cannot preempt WorkloadA because WorkloadA is older
+- WorkloadA runs for 24 hours while WorkloadB waits
+
+This behavior contradicts the expectation that same-priority workloads should
+have fair access to cluster resources over time. In multi-tenant environments,
+this can lead to:
+
+- One team's long-running workload blocking other teams indefinitely
+- Users submitting artificially low resource requests to get admitted first,
+  then consuming resources for extended periods
+- Reduced cluster utilization efficiency due to suboptimal scheduling
+
+### Goals
+
+- Extend the `LowerOrNewerEqualPriority` preemption policy with a
+  `minAdmitDuration` configuration that allows preemption of same-priority
+  workloads after they have been admitted for a specified duration
+- Maintain backward compatibility: existing configurations without
+  `minAdmitDuration` behave exactly as before
+
+### Non-Goals
+
+- Dynamic priority that decays over time
+- Per-workload or per-LocalQueue `minAdmitDuration` overrides
+- Precise CPU/GPU time accounting (wall-clock time from admission is used)
+- Cross-ClusterQueue time-based preemption (this feature is scoped to
+  `withinClusterQueue` only)
+
+## Proposal
+
+Introduce a new configuration structure `WithinClusterQueueConfig` that extends
+the `withinClusterQueue` preemption policy with a `minAdmitDuration` field.
+When `withinClusterQueue` is set to `LowerOrNewerEqualPriority` and
+`minAdmitDuration` is configured, workloads become eligible for same-priority
+preemption only after they have been admitted for at least the specified
+duration.
+
+### User Stories
+
+#### Story 1: Fair GPU Time-Sharing Among Data Scientists
+
+As a cluster administrator managing GPU resources for a team of 20 data
+scientists, I want to ensure that each team member gets fair access to GPU
+resources for their training jobs, all of which have the same priority.
+
+Currently, if one data scientist submits a job that starts training and runs
+for days, other team members' jobs remain pending indefinitely. With
+`minAdmitDuration: 4h`, each job is guaranteed 4 hours of runtime before it
+can be preempted by a waiting job.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: ml-training
+spec:
+  preemption:
+    withinClusterQueue: LowerOrNewerEqualPriority
+    withinClusterQueueConfig:
+      minAdmitDuration: 4h
+```
+
+#### Story 2: Preventing Resource Monopolization in Shared Clusters
+
+As a platform team managing a shared Kubernetes cluster for multiple teams,
+I want to prevent any single team from monopolizing cluster resources with
+long-running jobs while ensuring jobs have enough time to make meaningful
+progress.
+
+By setting `minAdmitDuration: 2h`, we ensure that:
+- Jobs have at least 2 hours to run and checkpoint
+- No single job can block others indefinitely
+- Resource access rotates fairly among all pending workloads
+
+### Notes/Constraints/Caveats (Optional)
+
+The `minAdmitDuration` field modifies the behavior of the
+`LowerOrNewerEqualPriority` policy. Without `minAdmitDuration`, the policy
+only preempts workloads that are newer than the preempting workload. With
+`minAdmitDuration`, it additionally preempts workloads that have exceeded
+the minimum duration, regardless of relative age.
+
+This feature is complementary to Fair Sharing (KEP-1714). Fair Sharing
+addresses resource distribution across different ClusterQueues/Cohorts via
+preemption, while this feature addresses time-sharing among workloads with
+identical priority within the same ClusterQueue.
+
+**Interaction with Fair Sharing**: When both Fair Sharing and time-based
+preemption are enabled, time-based preemption only applies to the
+`withinClusterQueue` policy. Cross-ClusterQueue preemption continues to be
+governed by Fair Sharing algorithms. The two mechanisms operate independently
+and do not conflict.
+
+This is similar to Slurm's `PreemptExemptTime` which provides a guaranteed
+minimum runtime before a job becomes preemptible.
+
+### Risks and Mitigations
+
+**Rapid Preemption Cycles**: If `minAdmitDuration` is set too low, workloads
+might be preempted before making meaningful progress. Mitigated by webhook
+validation requiring a minimum of 1 minute.
+
+**Checkpoint Dependency**: Workloads that don't implement checkpointing lose
+progress when preempted. This is not new; workloads already need to handle
+preemption gracefully.
+
+**Controller Restart**: If the Kueue controller restarts, all timing is based
+on the persisted `QuotaReserved` condition timestamps stored in workload
+status, so workloads maintain their accurate admission time across restarts.
+No special handling is required.
+
+**Scheduler Re-evaluation**: When a workload crosses `minAdmitDuration` while
+the scheduler is idle, preemption happens on the next scheduling cycle.
+For typical `minAdmitDuration` values (30m+), scheduler cycles are usually
+frequent enough due to other cluster events. If tighter responsiveness is
+required, a future enhancement could schedule re-evaluation at the earliest
+expiration time.
+
+**Requeue Position**: Preempted workloads follow the existing eviction path.
+With `BestEffortFIFO` and `Eviction` requeue timestamp (in default/common
+configuration), the workload moves to the back of the queue, enabling
+round-robin rotation. When re-admitted, the `QuotaReserved` timestamp resets,
+restarting the `minAdmitDuration` timer.
+
+## Design Details
+
+### API Changes
+
+Add a new optional field `withinClusterQueueConfig` to `ClusterQueuePreemption`:
+
+```go
+type ClusterQueuePreemption struct {
+    ReclaimWithinCohort PreemptionPolicy    `json:"reclaimWithinCohort,omitempty"`
+    BorrowWithinCohort  *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+    WithinClusterQueue  PreemptionPolicy    `json:"withinClusterQueue,omitempty"`
+
+    // withinClusterQueueConfig provides additional configuration for the
+    // withinClusterQueue preemption policy. Only valid when withinClusterQueue
+    // is set to LowerOrNewerEqualPriority.
+    // +optional
+    WithinClusterQueueConfig *WithinClusterQueueConfig `json:"withinClusterQueueConfig,omitempty"`
+}
+
+type WithinClusterQueueConfig struct {
+    // minAdmitDuration specifies the minimum duration a workload must be
+    // admitted before it becomes eligible for same-priority preemption.
+    // The duration is measured from the workload's quota reservation time.
+    // If nil or omitted, time-based same-priority preemption is disabled
+    // and only the existing behavior applies. When set, the minimum
+    // allowed value is 1 minute; values less than 1 minute (including 0)
+    // are rejected by validation.
+    // +optional
+    MinAdmitDuration *metav1.Duration `json:"minAdmitDuration,omitempty"`
+}
+```
+
+### Preemption Eligibility
+
+Preemption only occurs when there is a pending workload that cannot be
+admitted due to insufficient quota. A pending workload can preempt an
+admitted same-priority workload if **either** of these conditions is true:
+
+1. **Existing behavior**: The target's quota reservation time is more recent
+   than the preemptor's creation time (i.e., the target was admitted after the
+   preemptor entered the queue)
+2. **New behavior**: The target has exceeded `minAdmitDuration` (i.e.,
+   `now - target.quotaReservationTime > minAdmitDuration`)
+
+Note: Condition 1 preserves backward compatibility with the existing
+`LowerOrNewerEqualPriority` behavior where a workload that has been waiting
+longer in the queue has priority over one that was admitted after the waiting
+workload was created.
+
+**Preemption order among same-priority candidates**:
+1. Time-expired workloads (exceeded `minAdmitDuration`) are preempted before
+   "newer" workloads (admitted after preemptor was created)
+2. Among time-expired workloads, the longest-running is preempted first
+3. Among "newer" workloads, the most recently admitted is preempted first
+
+This ordering ensures workloads that have had their guaranteed runtime are
+preempted before those still within their protected period.
+
+### Future Extensibility
+
+The `WithinClusterQueueConfig` struct is designed to accommodate additional
+preemption policies. For example, a complementary use case has been raised
+where workloads should become immune to lower-priority preemption after a
+maximum duration. Such a field could be added to the same struct without
+API breakage and would operate independently of `minAdmitDuration`.
+
+### Webhook Validation
+
+1. `minAdmitDuration` must be at least 1 minute. Values less than 1 minute
+   (including explicit `0s` and negative values) are rejected with an error.
+2. If `withinClusterQueueConfig` is specified but `minAdmitDuration` is `nil`,
+   the configuration is valid but has no effect (time-based preemption is
+   disabled, only the existing `LowerOrNewerEqualPriority` behavior applies).
+3. `withinClusterQueueConfig` is only valid when `withinClusterQueue` is
+   `LowerOrNewerEqualPriority`. Setting it with `Never` or `LowerPriority`
+   is rejected with an error indicating the incompatible configuration.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+#### Unit Tests
+
+- `pkg/scheduler/preemption`: Test preemption candidate selection with
+  `minAdmitDuration`
+- `apis/kueue/v1beta2/clusterqueue_webhook`: Test validation rules
+
+#### Integration tests
+
+1. Two same-priority workloads competing for quota, first admitted workload
+   is preempted after `minAdmitDuration`
+2. Multiple workloads queued, fair rotation occurs as each exceeds threshold
+3. Mixed priorities: lower priority always preempted before same-priority
+4. `minAdmitDuration` not set: existing behavior preserved
+5. Edge case: workload exactly at `minAdmitDuration` threshold (should NOT
+   be preempted, as comparison requires strictly greater than)
+6. Controller restart: workload timestamps persist correctly and preemption
+   timing remains accurate
+7. Multiple workloads expiring simultaneously: deterministic ordering based
+   on longest-running first
+8. Interaction with Fair Sharing: time-based preemption only affects
+   `withinClusterQueue`, cross-queue preemption follows Fair Sharing rules
+9. Webhook validation: reject `minAdmitDuration` < 1 minute, reject config
+   when `withinClusterQueue` is `Never` or `LowerPriority`
+
+### Graduation Criteria
+
+#### Alpha
+
+- Feature behind `TimeBasedSamePriorityPreemption` feature gate (disabled by
+  default)
+- `withinClusterQueueConfig.minAdmitDuration` API field implemented
+- Preemption logic updated to consider time-expired workloads
+- Webhook validation for minimum duration and policy compatibility
+- Unit tests for preemption candidate selection
+- Integration tests for time-based preemption scenarios
+- Add a new reason value `InClusterQueueTimeBased` to the existing `reason`
+  label on `kueue_preempted_workloads_total` metric
```

  Will add `InClusterQueueTimeBased` reason to the Evicted condition.

- **keps/8522-time-based-same-priority-preemption/README.md:77** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-07T05:41:13Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Future Extensibility](#future-extensibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dynamic Priority Decay](#dynamic-priority-decay)
+  - [Rely on Admission Fair Sharing Alone](#rely-on-admission-fair-sharing-alone)
+  - [Usage-Based Preemption](#usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
+resource monopolization problem where the first workload admitted to a
+ClusterQueue continues to run indefinitely while same-priority workloads
+remain pending.
+
+**Terminology**: Throughout this KEP, "quota reservation time" refers to the
+`LastTransitionTime` of the workload's `QuotaReserved` condition (when
+`Status=True`). This is the moment when quota was reserved for the workload.
+
+## Motivation
+
+Currently, Kueue offers two preemption policies for `withinClusterQueue`:
+
+1. **`LowerPriority`**: Only preempt workloads with strictly lower priority
+2. **`LowerOrNewerEqualPriority`**: Preempt workloads with lower priority OR
+   workloads with equal priority that were admitted more recently
+
+The `LowerOrNewerEqualPriority` policy was designed to prevent queue starvation
+by allowing newly arrived high-demand workloads to preempt recently admitted
+same-priority workloads. However, this creates a subtle but significant
+problem: **the first workload admitted monopolizes resources indefinitely**.
+
+Consider this scenario:
+- WorkloadA (priority=10) is admitted at T=0
+- WorkloadB (priority=10) arrives at T=5min
+- WorkloadB cannot preempt WorkloadA because WorkloadA is older
+- WorkloadA runs for 24 hours while WorkloadB waits
+
+This behavior contradicts the expectation that same-priority workloads should
+have fair access to cluster resources over time. In multi-tenant environments,
+this can lead to:
+
+- One team's long-running workload blocking other teams indefinitely
+- Users submitting artificially low resource requests to get admitted first,
+  then consuming resources for extended periods
+- Reduced cluster utilization efficiency due to suboptimal scheduling
+
+### Goals
+
+- Extend the `LowerOrNewerEqualPriority` preemption policy with a
```

  @mukund-wayve Agreed on including the two-duration model (minAdmitDuration + opportunisticMinAdmitDuration). The incumbent vs opportunistic distinction makes sense and the implementation is minimal. One thing to flag: @mimowo suggested on #8522 that the priority boost mechanism (PR #9120, merged) could achieve this via an external controller. PR #9959 is an example controller for it. However, priority  
    boost is per-workload and async. It can't do pairwise decisions (incumbent vs opportunistic), and lowering effective priority breaks out of same-priority semantics into the lowerPriority path in SatisfiesPreemptionPolicy, losing the fairness guarantee. Guaranteed minimum runtime needs a native check in the preemption eligibility path.                                                                                            
                                                                                                                                                                                                                                                               I still think the native minAdmitDuration approach is cleaner for admins (one config field vs deploying a controller).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-07T11:28:29Z

- **keps/8522-time-based-same-priority-preemption/README.md:77** by [@mimowo](https://github.com/mimowo) — 2026-04-07T11:28:29Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Future Extensibility](#future-extensibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dynamic Priority Decay](#dynamic-priority-decay)
+  - [Rely on Admission Fair Sharing Alone](#rely-on-admission-fair-sharing-alone)
+  - [Usage-Based Preemption](#usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
+resource monopolization problem where the first workload admitted to a
+ClusterQueue continues to run indefinitely while same-priority workloads
+remain pending.
+
+**Terminology**: Throughout this KEP, "quota reservation time" refers to the
+`LastTransitionTime` of the workload's `QuotaReserved` condition (when
+`Status=True`). This is the moment when quota was reserved for the workload.
+
+## Motivation
+
+Currently, Kueue offers two preemption policies for `withinClusterQueue`:
+
+1. **`LowerPriority`**: Only preempt workloads with strictly lower priority
+2. **`LowerOrNewerEqualPriority`**: Preempt workloads with lower priority OR
+   workloads with equal priority that were admitted more recently
+
+The `LowerOrNewerEqualPriority` policy was designed to prevent queue starvation
+by allowing newly arrived high-demand workloads to preempt recently admitted
+same-priority workloads. However, this creates a subtle but significant
+problem: **the first workload admitted monopolizes resources indefinitely**.
+
+Consider this scenario:
+- WorkloadA (priority=10) is admitted at T=0
+- WorkloadB (priority=10) arrives at T=5min
+- WorkloadB cannot preempt WorkloadA because WorkloadA is older
+- WorkloadA runs for 24 hours while WorkloadB waits
+
+This behavior contradicts the expectation that same-priority workloads should
+have fair access to cluster resources over time. In multi-tenant environments,
+this can lead to:
+
+- One team's long-running workload blocking other teams indefinitely
+- Users submitting artificially low resource requests to get admitted first,
+  then consuming resources for extended periods
+- Reduced cluster utilization efficiency due to suboptimal scheduling
+
+### Goals
+
+- Extend the `LowerOrNewerEqualPriority` preemption policy with a
```

  > However, priority
  boost is per-workload and async. It can't do pairwise decisions (incumbent vs opportunistic), and lowering effective priority breaks out of same-priority semantics into the lowerPriority path in SatisfiesPreemptionPolicy, losing the fairness guarantee. Guaranteed minimum runtime needs a native check in the preemption eligibility path.
  
  @sohankunkerkar can you elaborate a bit more on that? I would like to understand what are the issues with the priority-boost round-robin before we go into full blown KEP for time-sharing. 
  
  Yes, it is async, but it does not seem like a blocker on its own. The potential millisecond or even second delay for the "leader" swap does not seem prohibitive, because IIUC the time sharing we are talking here is 30min units anyway.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-04-07T12:19:54Z

- **keps/8522-time-based-same-priority-preemption/README.md:77** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-07T12:19:54Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Future Extensibility](#future-extensibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dynamic Priority Decay](#dynamic-priority-decay)
+  - [Rely on Admission Fair Sharing Alone](#rely-on-admission-fair-sharing-alone)
+  - [Usage-Based Preemption](#usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
+resource monopolization problem where the first workload admitted to a
+ClusterQueue continues to run indefinitely while same-priority workloads
+remain pending.
+
+**Terminology**: Throughout this KEP, "quota reservation time" refers to the
+`LastTransitionTime` of the workload's `QuotaReserved` condition (when
+`Status=True`). This is the moment when quota was reserved for the workload.
+
+## Motivation
+
+Currently, Kueue offers two preemption policies for `withinClusterQueue`:
+
+1. **`LowerPriority`**: Only preempt workloads with strictly lower priority
+2. **`LowerOrNewerEqualPriority`**: Preempt workloads with lower priority OR
+   workloads with equal priority that were admitted more recently
+
+The `LowerOrNewerEqualPriority` policy was designed to prevent queue starvation
+by allowing newly arrived high-demand workloads to preempt recently admitted
+same-priority workloads. However, this creates a subtle but significant
+problem: **the first workload admitted monopolizes resources indefinitely**.
+
+Consider this scenario:
+- WorkloadA (priority=10) is admitted at T=0
+- WorkloadB (priority=10) arrives at T=5min
+- WorkloadB cannot preempt WorkloadA because WorkloadA is older
+- WorkloadA runs for 24 hours while WorkloadB waits
+
+This behavior contradicts the expectation that same-priority workloads should
+have fair access to cluster resources over time. In multi-tenant environments,
+this can lead to:
+
+- One team's long-running workload blocking other teams indefinitely
+- Users submitting artificially low resource requests to get admitted first,
+  then consuming resources for extended periods
+- Reduced cluster utilization efficiency due to suboptimal scheduling
+
+### Goals
+
+- Extend the `LowerOrNewerEqualPriority` preemption policy with a
```

  @mimowo You're right on the async part. It's not a concern for 30m+ windows. And I want to correct my earlier comment: the example controller uses a positive boost during the protection window, so it does work  for same-priority protection in the single-duration case.
                                                                                                                                                                                                                     Where the controller gets awkward is @mukund-wayve's use case: different protection for incumbents (2h) vs queue-jumpers (20m). To do that, the controller would need to list pending workloads, compare each  admitted workload's admission time against each pending workload's creation time, and classify incumbent vs opportunistic on every reconcile. It's reconstructing pairwise queue history that already exists inside `SatisfiesPreemptionPolicy` at preemption time. So for the single-duration case, the controller works, agreed. For the two-duration case with pairwise semantics, a native eligibility check is a more natural fit. @mukund-wayve can fill in more information here.

### Review by [@mukund-wayve](https://github.com/mukund-wayve) (commented) — 2026-04-08T20:29:35Z

- **keps/8522-time-based-same-priority-preemption/README.md:77** by [@mukund-wayve](https://github.com/mukund-wayve) — 2026-04-08T20:29:35Z
```diff
@@ -0,0 +1,353 @@
+# KEP-8522: Time-Based Same-Priority Preemption
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Fair GPU Time-Sharing Among Data Scientists](#story-1-fair-gpu-time-sharing-among-data-scientists)
+    - [Story 2: Preventing Resource Monopolization in Shared Clusters](#story-2-preventing-resource-monopolization-in-shared-clusters)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Preemption Eligibility](#preemption-eligibility)
+  - [Future Extensibility](#future-extensibility)
+  - [Webhook Validation](#webhook-validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dynamic Priority Decay](#dynamic-priority-decay)
+  - [Rely on Admission Fair Sharing Alone](#rely-on-admission-fair-sharing-alone)
+  - [Usage-Based Preemption](#usage-based-preemption)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces time-based same-priority preemption, allowing workloads to
+preempt other workloads of equal priority after the target workloads have been
+admitted for a minimum duration (`minAdmitDuration`). This addresses the
+resource monopolization problem where the first workload admitted to a
+ClusterQueue continues to run indefinitely while same-priority workloads
+remain pending.
+
+**Terminology**: Throughout this KEP, "quota reservation time" refers to the
+`LastTransitionTime` of the workload's `QuotaReserved` condition (when
+`Status=True`). This is the moment when quota was reserved for the workload.
+
+## Motivation
+
+Currently, Kueue offers two preemption policies for `withinClusterQueue`:
+
+1. **`LowerPriority`**: Only preempt workloads with strictly lower priority
+2. **`LowerOrNewerEqualPriority`**: Preempt workloads with lower priority OR
+   workloads with equal priority that were admitted more recently
+
+The `LowerOrNewerEqualPriority` policy was designed to prevent queue starvation
+by allowing newly arrived high-demand workloads to preempt recently admitted
+same-priority workloads. However, this creates a subtle but significant
+problem: **the first workload admitted monopolizes resources indefinitely**.
+
+Consider this scenario:
+- WorkloadA (priority=10) is admitted at T=0
+- WorkloadB (priority=10) arrives at T=5min
+- WorkloadB cannot preempt WorkloadA because WorkloadA is older
+- WorkloadA runs for 24 hours while WorkloadB waits
+
+This behavior contradicts the expectation that same-priority workloads should
+have fair access to cluster resources over time. In multi-tenant environments,
+this can lead to:
+
+- One team's long-running workload blocking other teams indefinitely
+- Users submitting artificially low resource requests to get admitted first,
+  then consuming resources for extended periods
+- Reduced cluster utilization efficiency due to suboptimal scheduling
+
+### Goals
+
+- Extend the `LowerOrNewerEqualPriority` preemption policy with a
```

  Let me go back to one of our [use-cases](https://github.com/kubernetes-sigs/kueue/pull/8646/changes#r2934869206):
  
  ---
  
  Story 3: Different Guarantees for Queue-Jumpers vs First-in-Line Jobs
  
  As a cluster administrator, I want workloads that were first in line to get a longer guaranteed runtime than workloads that jumped the queue by fitting into gaps.
  
  Our ML team shares a queue with 8 GPUs. Large training jobs (8 GPUs) often wait while smaller experiment jobs (1-2 GPUs) get admitted into available capacity via BestEffortFIFO. With a single minAdmitDuration, we can't treat these cases differently. With two durations, we give first-in-line jobs 2 hours of guaranteed runtime while small jobs that jumped ahead only get 20 minutes — enough to finish or checkpoint, but not long enough to block the queue.
  
  ---
  
  Why I would prefer clear minRuntime semantics over priority boost (as a cluster admin):
  
  1. It's much easier to reason about for users and cluster admins. Once a workloads reaches a certain threshold, it becomes eligible for preemption. This is easier to understand than relative priority boosts.
  2. When one/more controllers are trying to change priorities for different reasons - time sharing, checkpointing, etc, it gets even more hard to reason how priorities are affecting preemptions.
  3. No additional controller to manage
  
  
  For these reasons, I would argue that min-runtime should be a first-class feature within Kueue.
