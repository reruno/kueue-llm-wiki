# PR #8686: address follow-up comments to #8484

**Summary**: address follow-up comments to #8484

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8686

**Last updated**: 2026-01-21T14:38:31Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@gabesaba](https://github.com/gabesaba)
- **Branch**: `8484_followup` → `main`
- **Created**: 2026-01-20T10:32:21Z
- **Updated**: 2026-01-21T14:38:31Z
- **Merged**: 2026-01-20T11:10:51Z
- **Closed**: 2026-01-20T11:10:51Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 8   **+78 / -29**

## Description

#### What type of PR is this?
/kind cleanup

Followup comments in #8484

```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-01-20T10:32:28Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 7bfbd64fcac8fa6bd3300e7447bffba54417ba3e |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/696f5c877674080008628b3b |

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-20T10:59:26Z

/lgtm
/approve
please make sure it is cherrypick eventually too

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-20T10:59:35Z

LGTM label has been added.  <details>Git tree hash: 5bf66c6bb6d2dfa2c13aeccfca1279fa3d12f8f5</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-20T10:59:37Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8686#" title="Author self-approved">gabesaba</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8686#issuecomment-3772285797" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [gabesaba,mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-20T11:15:23Z

/cherrypick release-0.15
/cherrypick release-0.14

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-01-20T11:16:02Z

@mimowo: #8686 failed to apply on top of branch "release-0.14":
```
Applying: address follow-up comments to #8484
Using index info to reconstruct a base tree...
M	pkg/cache/scheduler/tas_cache.go
M	pkg/cache/scheduler/tas_cache_test.go
A	pkg/cache/scheduler/tas_non_tas_pod_cache.go
M	pkg/controller/tas/controllers.go
A	pkg/controller/tas/non_tas_usage_controller.go
M	pkg/scheduler/scheduler_tas_test.go
M	test/integration/singlecluster/tas/tas_test.go
M	test/util/constants.go
Falling back to patching base and 3-way merge...
Auto-merging test/util/constants.go
Auto-merging test/integration/singlecluster/tas/tas_test.go
CONFLICT (content): Merge conflict in test/integration/singlecluster/tas/tas_test.go
Auto-merging pkg/scheduler/scheduler_tas_test.go
CONFLICT (content): Merge conflict in pkg/scheduler/scheduler_tas_test.go
CONFLICT (modify/delete): pkg/controller/tas/non_tas_usage_controller.go deleted in HEAD and modified in address follow-up comments to #8484. Version address follow-up comments to #8484 of pkg/controller/tas/non_tas_usage_controller.go left in tree.
Auto-merging pkg/controller/tas/controllers.go
CONFLICT (content): Merge conflict in pkg/controller/tas/controllers.go
CONFLICT (modify/delete): pkg/cache/scheduler/tas_non_tas_pod_cache.go deleted in HEAD and modified in address follow-up comments to #8484. Version address follow-up comments to #8484 of pkg/cache/scheduler/tas_non_tas_pod_cache.go left in tree.
Auto-merging pkg/cache/scheduler/tas_cache_test.go
CONFLICT (content): Merge conflict in pkg/cache/scheduler/tas_cache_test.go
Auto-merging pkg/cache/scheduler/tas_cache.go
CONFLICT (content): Merge conflict in pkg/cache/scheduler/tas_cache.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 address follow-up comments to #8484

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8686#issuecomment-3772353859):

>/cherrypick release-0.15
>/cherrypick release-0.14


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-01-20T11:16:38Z

@mimowo: #8686 failed to apply on top of branch "release-0.15":
```
Applying: address follow-up comments to #8484
Using index info to reconstruct a base tree...
M	pkg/cache/scheduler/tas_cache.go
M	pkg/cache/scheduler/tas_cache_test.go
A	pkg/cache/scheduler/tas_non_tas_pod_cache.go
M	pkg/controller/tas/controllers.go
A	pkg/controller/tas/non_tas_usage_controller.go
M	pkg/scheduler/scheduler_tas_test.go
M	test/integration/singlecluster/tas/tas_test.go
Falling back to patching base and 3-way merge...
Auto-merging test/integration/singlecluster/tas/tas_test.go
CONFLICT (content): Merge conflict in test/integration/singlecluster/tas/tas_test.go
Auto-merging pkg/scheduler/scheduler_tas_test.go
CONFLICT (content): Merge conflict in pkg/scheduler/scheduler_tas_test.go
CONFLICT (modify/delete): pkg/controller/tas/non_tas_usage_controller.go deleted in HEAD and modified in address follow-up comments to #8484. Version address follow-up comments to #8484 of pkg/controller/tas/non_tas_usage_controller.go left in tree.
Auto-merging pkg/controller/tas/controllers.go
CONFLICT (content): Merge conflict in pkg/controller/tas/controllers.go
CONFLICT (modify/delete): pkg/cache/scheduler/tas_non_tas_pod_cache.go deleted in HEAD and modified in address follow-up comments to #8484. Version address follow-up comments to #8484 of pkg/cache/scheduler/tas_non_tas_pod_cache.go left in tree.
Auto-merging pkg/cache/scheduler/tas_cache_test.go
CONFLICT (content): Merge conflict in pkg/cache/scheduler/tas_cache_test.go
Auto-merging pkg/cache/scheduler/tas_cache.go
CONFLICT (content): Merge conflict in pkg/cache/scheduler/tas_cache.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 address follow-up comments to #8484

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8686#issuecomment-3772353859):

>/cherrypick release-0.15
>/cherrypick release-0.14


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-20T10:41:30Z

- **test/integration/singlecluster/tas/tas_test.go:475** by [@mimowo](https://github.com/mimowo) — 2026-01-20T10:41:30Z
```diff
@@ -425,6 +425,59 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 				util.ExpectAdmittedWorkloadsTotalMetric(clusterQueue, "", 2)
 			})
 		})
+
+		ginkgo.It("non-TAS pod terminating; capacity not released", func() {
+			var wl *kueue.Workload
+			var nonTasPod *corev1.Pod
+
+			ginkgo.By("create a non-TAS pod which consumes the node's capacity", func() {
+				nonTasPod = testingpod.MakePod("pod", ns.Name).
+					Request(corev1.ResourceCPU, "1").
+					NodeName("node1").
+					StatusPhase(corev1.PodRunning).
+					Finalizer("kueue.sigs.k8s.io/test-finalizer").
+					Obj()
+				util.MustCreate(ctx, k8sClient, nonTasPod)
+			})
+
+			ginkgo.By("create a workload which requires the node's capacity", func() {
+				wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+					Queue("local-queue").
+					Request(corev1.ResourceCPU, "1").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+
+				ginkgo.By("verify the workload is not admitted", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(workload.IsAdmitted(wl)).To(gomega.BeFalse())
+					}, util.ShortConsistentDuration, util.Interval).Should(gomega.Succeed())
+				})
+			})
+
+			ginkgo.By("delete the non-TAS pod", func() {
+				gomega.Expect(k8sClient.Delete(ctx, nonTasPod)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("verify the non-TAS pod has deletionTimestamp", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(nonTasPod), nonTasPod)).To(gomega.Succeed())
+					g.Expect(nonTasPod.DeletionTimestamp).NotTo(gomega.BeNil())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify that the TAS-workload doesn't admit", func() {
+				gomega.Consistently(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(wl)).To(gomega.BeFalse())
+				}, util.ShortConsistentDuration, util.Interval).Should(gomega.Succeed())
```

  nit, what about using ShortConsistentDuration in the sibling tests?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-20T10:42:31Z

- **test/integration/singlecluster/tas/tas_test.go:475** by [@mimowo](https://github.com/mimowo) — 2026-01-20T10:42:31Z
```diff
@@ -425,6 +425,59 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 				util.ExpectAdmittedWorkloadsTotalMetric(clusterQueue, "", 2)
 			})
 		})
+
+		ginkgo.It("non-TAS pod terminating; capacity not released", func() {
+			var wl *kueue.Workload
+			var nonTasPod *corev1.Pod
+
+			ginkgo.By("create a non-TAS pod which consumes the node's capacity", func() {
+				nonTasPod = testingpod.MakePod("pod", ns.Name).
+					Request(corev1.ResourceCPU, "1").
+					NodeName("node1").
+					StatusPhase(corev1.PodRunning).
+					Finalizer("kueue.sigs.k8s.io/test-finalizer").
+					Obj()
+				util.MustCreate(ctx, k8sClient, nonTasPod)
+			})
+
+			ginkgo.By("create a workload which requires the node's capacity", func() {
+				wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+					Queue("local-queue").
+					Request(corev1.ResourceCPU, "1").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+
+				ginkgo.By("verify the workload is not admitted", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(workload.IsAdmitted(wl)).To(gomega.BeFalse())
+					}, util.ShortConsistentDuration, util.Interval).Should(gomega.Succeed())
+				})
+			})
+
+			ginkgo.By("delete the non-TAS pod", func() {
+				gomega.Expect(k8sClient.Delete(ctx, nonTasPod)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("verify the non-TAS pod has deletionTimestamp", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(nonTasPod), nonTasPod)).To(gomega.Succeed())
+					g.Expect(nonTasPod.DeletionTimestamp).NotTo(gomega.BeNil())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify that the TAS-workload doesn't admit", func() {
+				gomega.Consistently(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(wl)).To(gomega.BeFalse())
+				}, util.ShortConsistentDuration, util.Interval).Should(gomega.Succeed())
```

  eg https://github.com/kubernetes-sigs/kueue/pull/8686/changes#diff-95ef2b4868044a4fbcc96fcda6df858f3b79548d2e4548fafcd9280114928dfcR345

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-20T10:42:34Z

- **test/integration/singlecluster/tas/tas_test.go:475** by [@gabesaba](https://github.com/gabesaba) — 2026-01-20T10:42:34Z
```diff
@@ -425,6 +425,59 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 				util.ExpectAdmittedWorkloadsTotalMetric(clusterQueue, "", 2)
 			})
 		})
+
+		ginkgo.It("non-TAS pod terminating; capacity not released", func() {
+			var wl *kueue.Workload
+			var nonTasPod *corev1.Pod
+
+			ginkgo.By("create a non-TAS pod which consumes the node's capacity", func() {
+				nonTasPod = testingpod.MakePod("pod", ns.Name).
+					Request(corev1.ResourceCPU, "1").
+					NodeName("node1").
+					StatusPhase(corev1.PodRunning).
+					Finalizer("kueue.sigs.k8s.io/test-finalizer").
+					Obj()
+				util.MustCreate(ctx, k8sClient, nonTasPod)
+			})
+
+			ginkgo.By("create a workload which requires the node's capacity", func() {
+				wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+					Queue("local-queue").
+					Request(corev1.ResourceCPU, "1").
+					Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+
+				ginkgo.By("verify the workload is not admitted", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(workload.IsAdmitted(wl)).To(gomega.BeFalse())
+					}, util.ShortConsistentDuration, util.Interval).Should(gomega.Succeed())
+				})
+			})
+
+			ginkgo.By("delete the non-TAS pod", func() {
+				gomega.Expect(k8sClient.Delete(ctx, nonTasPod)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("verify the non-TAS pod has deletionTimestamp", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(nonTasPod), nonTasPod)).To(gomega.Succeed())
+					g.Expect(nonTasPod.DeletionTimestamp).NotTo(gomega.BeNil())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify that the TAS-workload doesn't admit", func() {
+				gomega.Consistently(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(wl)).To(gomega.BeFalse())
+				}, util.ShortConsistentDuration, util.Interval).Should(gomega.Succeed())
```

  Ahh sorry, I put it in but reverted it accidentally during some debugging/changes
