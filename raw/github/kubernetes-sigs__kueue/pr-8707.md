# PR #8707: feat(kueueviz/backend): drop polling and switch to informers

**Summary**: feat(kueueviz/backend): drop polling and switch to informers

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8707

**Last updated**: 2026-03-10T03:35:12Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@thejoeejoee](https://github.com/thejoeejoee)
- **Branch**: `main` → `main`
- **Created**: 2026-01-20T16:45:41Z
- **Updated**: 2026-03-10T03:35:12Z
- **Merged**: 2026-03-10T03:35:10Z
- **Closed**: 2026-03-10T03:35:10Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XL`, `approved`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`, `area/dashboard`
- **Assignees**: [@samzong](https://github.com/samzong)
- **Requested reviewers**: [@akram](https://github.com/akram), [@mimowo](https://github.com/mimowo), [@pajakd](https://github.com/pajakd), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 15   **+641 / -96**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
/kind cleanup

#### What this PR does / why we need it:

Replace polling with informers in kueueviz backend WebSocket handlers.

- Drop polling, use informers for real-time updates
- Support multi-resource watching (variadic GVKs in GenericWebSocketHandler)
- Addressed nits from #7663

#### Special notes for your reviewer:

No polling fallback. WebSocket protocol unchanged.

#### Does this PR introduce a user-facing change?

```release-note
kueueviz: use informers instead of polling for WebSocket updates
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-01-20T16:45:50Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 52f9d26fbbaecd4a65a2e10115c0afe71158a0f3 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69ad35d6c113ae0008663e57 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-20T16:45:52Z

Hi @thejoeejoee. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-20T17:25:02Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-20T17:25:10Z

cc @mbobrovskyi ptal

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-22T20:20:25Z

Thank you folks for the progress here!

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-23T16:52:17Z

@thejoeejoee have you had a chance to test it e2e manually? Also, do you know what is the confidence we get from our automated e2e tests here?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-05T09:18:10Z

@david-gang maybe you could help to review / confirming this looks good

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-05T09:21:47Z

The PR lgtm, but I would like to have some confirmation it works e2e as requested in https://github.com/kubernetes-sigs/kueue/pull/8707#issuecomment-3791229627

@thejoeejoee what about the confirmation

cc @mbobrovskyi

### Comment by [@david-gang](https://github.com/david-gang) — 2026-02-05T15:21:29Z

> @david-gang maybe you could help to review / confirming this looks good

added review.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-17T11:11:04Z

@thejoeejoee do you have cycles to address the remaining comments?

### Comment by [@thejoeejoee](https://github.com/thejoeejoee) — 2026-02-27T21:36:44Z

> @thejoeejoee have you had a chance to test it e2e manually? Also, do you know what is the confidence we get from our automated e2e tests here?

yep, the changes are instant now


https://github.com/user-attachments/assets/4451d961-d7a8-4c66-8f68-51cf41fb0275

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-06T17:06:36Z

Thank you 👍 
/approve
I'm wondering we could probably cherrypick this change, if I get such a signal from reviewers, maybe @samzong, @
david-gang, wdyt?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-06T17:07:33Z

@thejoeejoee please resolve the conflicts. I will prioritize the PR to avoid future conflicts.

### Comment by [@samzong](https://github.com/samzong) — 2026-03-06T17:21:26Z

@mimowo for me. I think this change itself is sound and deserves to be merged into the main branch. 

However, I don't think cherry-pick is necessary; the polling method in the stable version already works correctly, and this improvement addresses user experience latency rather than correctness issues.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-09T19:34:40Z

Thank  you 👍 
/area dashboard
/approve
Leaving lgtm to @samzong

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-09T19:34:49Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8707#issuecomment-4026252062" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8707#" title="Author self-approved">thejoeejoee</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@samzong](https://github.com/samzong) — 2026-03-10T03:02:39Z

/lgtm

Thanks for your contribution!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-10T03:02:47Z

LGTM label has been added.  <details>Git tree hash: 0e017ec8ccb1ce097287876991724d74e1317d38</details>

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-22T16:05:00Z

- **cmd/kueueviz/backend/config/server.go:21** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-22T16:05:00Z
```diff
@@ -18,7 +18,7 @@ package config
 
 import (
 	"fmt"
-	"log"
+	log "log/slog"
```

  Can we create prepare PR for this?  to reduce changed.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-22T16:06:02Z

- **cmd/kueueviz/backend/main.go:23** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-22T16:06:02Z
```diff
@@ -19,7 +19,8 @@ package main
 import (
 	"context"
 	"errors"
-	"log"
+	stdlog "log"
+	log "log/slog"
```

  Can we avoid using both loggers?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-22T16:06:23Z

- **cmd/kueueviz/backend/main.go:48** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-22T16:06:23Z
```diff
@@ -43,13 +44,15 @@ func main() {
 	// Create Kubernetes client
 	dynamicClient, manager, err := createK8sClient(ctx)
 	if err != nil {
-		log.Fatalf("Error creating Kubernetes client: %v", err)
+		log.Error("Error creating Kubernetes client", "error", err)
+		return
```

  ```suggestion
  		os.Exit(1)
  ```
  ?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-22T16:06:31Z

- **cmd/kueueviz/backend/main.go:48** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-22T16:06:31Z
```diff
@@ -43,13 +44,15 @@ func main() {
 	// Create Kubernetes client
 	dynamicClient, manager, err := createK8sClient(ctx)
 	if err != nil {
-		log.Fatalf("Error creating Kubernetes client: %v", err)
+		log.Error("Error creating Kubernetes client", "error", err)
+		return
```

  Also other places.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-22T16:07:09Z

- **cmd/kueueviz/backend/main.go:104** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-22T16:07:09Z
```diff
@@ -63,27 +66,29 @@ func main() {
 	h.InitializeWebSocketRoutes(r)
 	h.InitializeAPIRoutes(r, dynamicClient)
 
-	ctrllog.SetLogger(stdr.New(log.Default()))
+	ctrllog.SetLogger(stdr.New(stdlog.Default()))
 
 	// Start manager in a separate goroutine
 	go func() {
 		if err = manager.Start(ctx); err != nil {
-			log.Fatalf("Failed to start manager: %v", err)
+			log.Error("Failed to start manager", "error", err)
+			cancel()
 		}
 	}()
 
 	// Start HTTP server in a separate goroutine
 	go func() {
-		log.Printf("Starting server on %s", srv.Addr)
+		log.Info("Starting server", "address", srv.Addr)
 		if err := srv.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
-			log.Fatalf("Failed to start HTTP server: %v", err)
+			log.Error("Failed to start HTTP server", "error", err)
+			cancel()
 		}
 	}()
 
 	<-ctx.Done()
 
 	// Shutdown the server gracefully
 	if err := srv.Shutdown(context.Background()); err != nil {
```

  ```suggestion
  	if err := srv.Shutdown(ctx); err != nil {
  ```
  ?

### Review by [@thejoeejoee](https://github.com/thejoeejoee) (commented) — 2026-01-22T20:03:06Z

- **cmd/kueueviz/backend/config/server.go:21** by [@thejoeejoee](https://github.com/thejoeejoee) — 2026-01-22T20:03:06Z
```diff
@@ -18,7 +18,7 @@ package config
 
 import (
 	"fmt"
-	"log"
+	log "log/slog"
```

  prepared #8750

### Review by [@thejoeejoee](https://github.com/thejoeejoee) (commented) — 2026-01-22T20:17:30Z

- **cmd/kueueviz/backend/main.go:23** by [@thejoeejoee](https://github.com/thejoeejoee) — 2026-01-22T20:17:30Z
```diff
@@ -19,7 +19,8 @@ package main
 import (
 	"context"
 	"errors"
-	"log"
+	stdlog "log"
+	log "log/slog"
```

  yep, moved to https://github.com/kubernetes-sigs/kueue/pull/8750

### Review by [@thejoeejoee](https://github.com/thejoeejoee) (commented) — 2026-01-22T20:27:47Z

- **cmd/kueueviz/backend/main.go:104** by [@thejoeejoee](https://github.com/thejoeejoee) — 2026-01-22T20:27:47Z
```diff
@@ -63,27 +66,29 @@ func main() {
 	h.InitializeWebSocketRoutes(r)
 	h.InitializeAPIRoutes(r, dynamicClient)
 
-	ctrllog.SetLogger(stdr.New(log.Default()))
+	ctrllog.SetLogger(stdr.New(stdlog.Default()))
 
 	// Start manager in a separate goroutine
 	go func() {
 		if err = manager.Start(ctx); err != nil {
-			log.Fatalf("Failed to start manager: %v", err)
+			log.Error("Failed to start manager", "error", err)
+			cancel()
 		}
 	}()
 
 	// Start HTTP server in a separate goroutine
 	go func() {
-		log.Printf("Starting server on %s", srv.Addr)
+		log.Info("Starting server", "address", srv.Addr)
 		if err := srv.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
-			log.Fatalf("Failed to start HTTP server: %v", err)
+			log.Error("Failed to start HTTP server", "error", err)
+			cancel()
 		}
 	}()
 
 	<-ctx.Done()
 
 	// Shutdown the server gracefully
 	if err := srv.Shutdown(context.Background()); err != nil {
```

  `ctx` is technically already cancelled here, since we passed the `<-ctx.Done()`; doesn't make much sense to me

### Review by [@thejoeejoee](https://github.com/thejoeejoee) (commented) — 2026-01-22T20:30:45Z

- **cmd/kueueviz/backend/main.go:48** by [@thejoeejoee](https://github.com/thejoeejoee) — 2026-01-22T20:30:45Z
```diff
@@ -43,13 +44,15 @@ func main() {
 	// Create Kubernetes client
 	dynamicClient, manager, err := createK8sClient(ctx)
 	if err != nil {
-		log.Fatalf("Error creating Kubernetes client: %v", err)
+		log.Error("Error creating Kubernetes client", "error", err)
+		return
```

  yep, switched to printf+os.exit; this log does not have error, waits for #8750 for slog.error

### Review by [@david-gang](https://github.com/david-gang) (commented) — 2026-02-05T15:20:47Z

 Left comments inline. Also worth considering: a small debounce delay (100-200ms) would help batch rapid changes and avoid back-to-back updates when multiple resources change in quick succession.

- **cmd/kueueviz/backend/handlers/websocket_handler.go:125** by [@david-gang](https://github.com/david-gang) — 2026-02-05T15:08:52Z
```diff
@@ -50,82 +54,124 @@ func (h *Handlers) GenericWebSocketHandler(dataFetcher func(ctx context.Context)
 		defer conn.Close()
 		slog.Debug("WebSocket connection established took %v", "duration", time.Since(connStart))
 
-		// Fetch the initial data to send it immediately
-		fetchStart := time.Now()
-		data, err := dataFetcher(c.Request.Context())
-		if err != nil {
-			slog.Error("Error fetching data %v", "error", err)
+		ctx, cancel := context.WithCancel(c.Request.Context())
+		defer cancel()
+
+		// Send initial data
+		if err := h.sendData(ctx, conn, dataFetcher); err != nil {
+			slog.Error("Error sending initial data: %v", "error", err)
 			return
 		}
-		slog.Debug("Data fetched took %v", "duration", time.Since(fetchStart))
 
-		// Marshal the fetched data into JSON
-		marshalStart := time.Now()
-		jsonData, err := json.Marshal(data)
-		if err != nil {
-			slog.Error("Error marshaling data : %v", "error", err)
-			return
+		// Use informer-based updates for real-time streaming
+		h.handleInformerUpdates(ctx, conn, dataFetcher, gvks...)
+
+		slog.Debug("WebSocket handler completed, total time: %v", "duration", time.Since(startTime))
+	}
+}
+
+// sendData fetches and sends data through the WebSocket connection
+func (h *Handlers) sendData(ctx context.Context, conn *websocket.Conn, dataFetcher func(ctx context.Context) (any, error)) error {
+	fetchStart := time.Now()
+	data, err := dataFetcher(ctx)
+	if err != nil {
+		return err
+	}
+	slog.Debug("Data fetched took %v", "duration", time.Since(fetchStart))
+
+	marshalStart := time.Now()
+	jsonData, err := json.Marshal(data)
+	if err != nil {
+		return err
+	}
+	slog.Debug("Data marshaled into JSON took %v", "duration", time.Since(marshalStart))
+
+	if err := conn.SetWriteDeadline(time.Now().Add(5 * time.Second)); err != nil {
+		return err
+	}
+
+	writeStart := time.Now()
+	err = conn.WriteMessage(websocket.TextMessage, jsonData)
+	if err != nil {
+		return err
+	}
+	slog.Debug("Message sent to client took %v", "duration", time.Since(writeStart))
+
+	return nil
+}
+
+// handleInformerUpdates uses Kubernetes informers to stream real-time changes
+// Supports watching multiple resource types by registering handlers for all provided GVKs
+func (h *Handlers) handleInformerUpdates(ctx context.Context, conn *websocket.Conn, dataFetcher func(ctx context.Context) (any, error), gvks ...schema.GroupVersionKind) {
+	// Channel to signal when data needs to be sent
+	updateChan := make(chan struct{}, 1)
+	var updateMu sync.Mutex
+
+	// Helper to trigger update (non-blocking)
+	triggerUpdate := func() {
+		updateMu.Lock()
+		defer updateMu.Unlock()
+		select {
+		case updateChan <- struct{}{}:
+		default:
+			// Channel already has a pending update
 		}
-		slog.Debug("Data marshaled into JSON at took %v", "duration", time.Since(marshalStart))
+	}
 
-		// Set write deadline to avoid blocking indefinitely
-		if err := conn.SetWriteDeadline(time.Now().Add(5 * time.Second)); err != nil {
-			slog.Error("Error setting write deadline: %v", "error", err)
-			return
+	// Track all registrations for cleanup
+	var registrations []cache.ResourceEventHandlerRegistration
+	defer func() {
+		for i, reg := range registrations {
+			if i < len(gvks) && gvks[i] != (schema.GroupVersionKind{}) {
```

   The cleanup logic assumes registrations[i] corresponds to gvks[i],  but if any GVK fails, they get out of sync
  
  ```
  if err != nil {
  			slog.Error("Error adding event handler for %v: %v", "gvk", gvk, "error", err)
  			continue
  		}
  		registrations = append(registrations, registration)
  ```
  
  wouldn't it be better to store both of them together ? one possibility would be:
  
  ```
  type registrationInfo struct {
        gvk schema.GroupVersionKind
        reg cache.ResourceEventHandlerRegistration
    }
    var registrations []registrationInfo
  ```

- **cmd/kueueviz/backend/handlers/websocket_handler.go:129** by [@david-gang](https://github.com/david-gang) — 2026-02-05T15:14:15Z
```diff
@@ -50,82 +54,124 @@ func (h *Handlers) GenericWebSocketHandler(dataFetcher func(ctx context.Context)
 		defer conn.Close()
 		slog.Debug("WebSocket connection established took %v", "duration", time.Since(connStart))
 
-		// Fetch the initial data to send it immediately
-		fetchStart := time.Now()
-		data, err := dataFetcher(c.Request.Context())
-		if err != nil {
-			slog.Error("Error fetching data %v", "error", err)
+		ctx, cancel := context.WithCancel(c.Request.Context())
+		defer cancel()
+
+		// Send initial data
+		if err := h.sendData(ctx, conn, dataFetcher); err != nil {
+			slog.Error("Error sending initial data: %v", "error", err)
 			return
 		}
-		slog.Debug("Data fetched took %v", "duration", time.Since(fetchStart))
 
-		// Marshal the fetched data into JSON
-		marshalStart := time.Now()
-		jsonData, err := json.Marshal(data)
-		if err != nil {
-			slog.Error("Error marshaling data : %v", "error", err)
-			return
+		// Use informer-based updates for real-time streaming
+		h.handleInformerUpdates(ctx, conn, dataFetcher, gvks...)
+
+		slog.Debug("WebSocket handler completed, total time: %v", "duration", time.Since(startTime))
+	}
+}
+
+// sendData fetches and sends data through the WebSocket connection
+func (h *Handlers) sendData(ctx context.Context, conn *websocket.Conn, dataFetcher func(ctx context.Context) (any, error)) error {
+	fetchStart := time.Now()
+	data, err := dataFetcher(ctx)
+	if err != nil {
+		return err
+	}
+	slog.Debug("Data fetched took %v", "duration", time.Since(fetchStart))
+
+	marshalStart := time.Now()
+	jsonData, err := json.Marshal(data)
+	if err != nil {
+		return err
+	}
+	slog.Debug("Data marshaled into JSON took %v", "duration", time.Since(marshalStart))
+
+	if err := conn.SetWriteDeadline(time.Now().Add(5 * time.Second)); err != nil {
+		return err
+	}
+
+	writeStart := time.Now()
+	err = conn.WriteMessage(websocket.TextMessage, jsonData)
+	if err != nil {
+		return err
+	}
+	slog.Debug("Message sent to client took %v", "duration", time.Since(writeStart))
+
+	return nil
+}
+
+// handleInformerUpdates uses Kubernetes informers to stream real-time changes
+// Supports watching multiple resource types by registering handlers for all provided GVKs
+func (h *Handlers) handleInformerUpdates(ctx context.Context, conn *websocket.Conn, dataFetcher func(ctx context.Context) (any, error), gvks ...schema.GroupVersionKind) {
+	// Channel to signal when data needs to be sent
+	updateChan := make(chan struct{}, 1)
+	var updateMu sync.Mutex
+
+	// Helper to trigger update (non-blocking)
+	triggerUpdate := func() {
+		updateMu.Lock()
+		defer updateMu.Unlock()
+		select {
+		case updateChan <- struct{}{}:
+		default:
+			// Channel already has a pending update
 		}
-		slog.Debug("Data marshaled into JSON at took %v", "duration", time.Since(marshalStart))
+	}
 
-		// Set write deadline to avoid blocking indefinitely
-		if err := conn.SetWriteDeadline(time.Now().Add(5 * time.Second)); err != nil {
-			slog.Error("Error setting write deadline: %v", "error", err)
-			return
+	// Track all registrations for cleanup
+	var registrations []cache.ResourceEventHandlerRegistration
+	defer func() {
+		for i, reg := range registrations {
+			if i < len(gvks) && gvks[i] != (schema.GroupVersionKind{}) {
+				informer, err := h.client.GetInformerForKind(ctx, gvks[i])
+				if err == nil {
+					if err := informer.RemoveEventHandler(reg); err != nil {
+						slog.Error("Error removing event handler: %v", "error", err)
```

   The slog package uses structured key-value logging, not printf-style formatting. so better
    slog.Error("Error removing event handler", "error", err) or something similar along the lines.

- **cmd/kueueviz/backend/handlers/websocket_handler.go:79** by [@david-gang](https://github.com/david-gang) — 2026-02-05T15:20:41Z
```diff
@@ -50,82 +54,124 @@ func (h *Handlers) GenericWebSocketHandler(dataFetcher func(ctx context.Context)
 		defer conn.Close()
 		slog.Debug("WebSocket connection established took %v", "duration", time.Since(connStart))
 
-		// Fetch the initial data to send it immediately
-		fetchStart := time.Now()
-		data, err := dataFetcher(c.Request.Context())
-		if err != nil {
-			slog.Error("Error fetching data %v", "error", err)
+		ctx, cancel := context.WithCancel(c.Request.Context())
+		defer cancel()
+
+		// Send initial data
+		if err := h.sendData(ctx, conn, dataFetcher); err != nil {
+			slog.Error("Error sending initial data: %v", "error", err)
 			return
 		}
-		slog.Debug("Data fetched took %v", "duration", time.Since(fetchStart))
 
```

  There's no goroutine reading from the WebSocket connection. Without it, client disconnects won't be detected promptly and ping/pong frames won't be handled. Typical pattern:
  
  ```
    go func() {
        for {
            if _, _, err := conn.ReadMessage(); err != nil {
                cancel()
                return
            }
        }
    }()
  ```

### Review by [@thejoeejoee](https://github.com/thejoeejoee) (commented) — 2026-02-27T20:51:07Z

- **cmd/kueueviz/backend/handlers/websocket_handler.go:125** by [@thejoeejoee](https://github.com/thejoeejoee) — 2026-02-27T20:51:07Z
```diff
@@ -50,82 +54,124 @@ func (h *Handlers) GenericWebSocketHandler(dataFetcher func(ctx context.Context)
 		defer conn.Close()
 		slog.Debug("WebSocket connection established took %v", "duration", time.Since(connStart))
 
-		// Fetch the initial data to send it immediately
-		fetchStart := time.Now()
-		data, err := dataFetcher(c.Request.Context())
-		if err != nil {
-			slog.Error("Error fetching data %v", "error", err)
+		ctx, cancel := context.WithCancel(c.Request.Context())
+		defer cancel()
+
+		// Send initial data
+		if err := h.sendData(ctx, conn, dataFetcher); err != nil {
+			slog.Error("Error sending initial data: %v", "error", err)
 			return
 		}
-		slog.Debug("Data fetched took %v", "duration", time.Since(fetchStart))
 
-		// Marshal the fetched data into JSON
-		marshalStart := time.Now()
-		jsonData, err := json.Marshal(data)
-		if err != nil {
-			slog.Error("Error marshaling data : %v", "error", err)
-			return
+		// Use informer-based updates for real-time streaming
+		h.handleInformerUpdates(ctx, conn, dataFetcher, gvks...)
+
+		slog.Debug("WebSocket handler completed, total time: %v", "duration", time.Since(startTime))
+	}
+}
+
+// sendData fetches and sends data through the WebSocket connection
+func (h *Handlers) sendData(ctx context.Context, conn *websocket.Conn, dataFetcher func(ctx context.Context) (any, error)) error {
+	fetchStart := time.Now()
+	data, err := dataFetcher(ctx)
+	if err != nil {
+		return err
+	}
+	slog.Debug("Data fetched took %v", "duration", time.Since(fetchStart))
+
+	marshalStart := time.Now()
+	jsonData, err := json.Marshal(data)
+	if err != nil {
+		return err
+	}
+	slog.Debug("Data marshaled into JSON took %v", "duration", time.Since(marshalStart))
+
+	if err := conn.SetWriteDeadline(time.Now().Add(5 * time.Second)); err != nil {
+		return err
+	}
+
+	writeStart := time.Now()
+	err = conn.WriteMessage(websocket.TextMessage, jsonData)
+	if err != nil {
+		return err
+	}
+	slog.Debug("Message sent to client took %v", "duration", time.Since(writeStart))
+
+	return nil
+}
+
+// handleInformerUpdates uses Kubernetes informers to stream real-time changes
+// Supports watching multiple resource types by registering handlers for all provided GVKs
+func (h *Handlers) handleInformerUpdates(ctx context.Context, conn *websocket.Conn, dataFetcher func(ctx context.Context) (any, error), gvks ...schema.GroupVersionKind) {
+	// Channel to signal when data needs to be sent
+	updateChan := make(chan struct{}, 1)
+	var updateMu sync.Mutex
+
+	// Helper to trigger update (non-blocking)
+	triggerUpdate := func() {
+		updateMu.Lock()
+		defer updateMu.Unlock()
+		select {
+		case updateChan <- struct{}{}:
+		default:
+			// Channel already has a pending update
 		}
-		slog.Debug("Data marshaled into JSON at took %v", "duration", time.Since(marshalStart))
+	}
 
-		// Set write deadline to avoid blocking indefinitely
-		if err := conn.SetWriteDeadline(time.Now().Add(5 * time.Second)); err != nil {
-			slog.Error("Error setting write deadline: %v", "error", err)
-			return
+	// Track all registrations for cleanup
+	var registrations []cache.ResourceEventHandlerRegistration
+	defer func() {
+		for i, reg := range registrations {
+			if i < len(gvks) && gvks[i] != (schema.GroupVersionKind{}) {
```

  seems better, fixed

### Review by [@thejoeejoee](https://github.com/thejoeejoee) (commented) — 2026-02-27T20:51:32Z

- **cmd/kueueviz/backend/handlers/websocket_handler.go:129** by [@thejoeejoee](https://github.com/thejoeejoee) — 2026-02-27T20:51:32Z
```diff
@@ -50,82 +54,124 @@ func (h *Handlers) GenericWebSocketHandler(dataFetcher func(ctx context.Context)
 		defer conn.Close()
 		slog.Debug("WebSocket connection established took %v", "duration", time.Since(connStart))
 
-		// Fetch the initial data to send it immediately
-		fetchStart := time.Now()
-		data, err := dataFetcher(c.Request.Context())
-		if err != nil {
-			slog.Error("Error fetching data %v", "error", err)
+		ctx, cancel := context.WithCancel(c.Request.Context())
+		defer cancel()
+
+		// Send initial data
+		if err := h.sendData(ctx, conn, dataFetcher); err != nil {
+			slog.Error("Error sending initial data: %v", "error", err)
 			return
 		}
-		slog.Debug("Data fetched took %v", "duration", time.Since(fetchStart))
 
-		// Marshal the fetched data into JSON
-		marshalStart := time.Now()
-		jsonData, err := json.Marshal(data)
-		if err != nil {
-			slog.Error("Error marshaling data : %v", "error", err)
-			return
+		// Use informer-based updates for real-time streaming
+		h.handleInformerUpdates(ctx, conn, dataFetcher, gvks...)
+
+		slog.Debug("WebSocket handler completed, total time: %v", "duration", time.Since(startTime))
+	}
+}
+
+// sendData fetches and sends data through the WebSocket connection
+func (h *Handlers) sendData(ctx context.Context, conn *websocket.Conn, dataFetcher func(ctx context.Context) (any, error)) error {
+	fetchStart := time.Now()
+	data, err := dataFetcher(ctx)
+	if err != nil {
+		return err
+	}
+	slog.Debug("Data fetched took %v", "duration", time.Since(fetchStart))
+
+	marshalStart := time.Now()
+	jsonData, err := json.Marshal(data)
+	if err != nil {
+		return err
+	}
+	slog.Debug("Data marshaled into JSON took %v", "duration", time.Since(marshalStart))
+
+	if err := conn.SetWriteDeadline(time.Now().Add(5 * time.Second)); err != nil {
+		return err
+	}
+
+	writeStart := time.Now()
+	err = conn.WriteMessage(websocket.TextMessage, jsonData)
+	if err != nil {
+		return err
+	}
+	slog.Debug("Message sent to client took %v", "duration", time.Since(writeStart))
+
+	return nil
+}
+
+// handleInformerUpdates uses Kubernetes informers to stream real-time changes
+// Supports watching multiple resource types by registering handlers for all provided GVKs
+func (h *Handlers) handleInformerUpdates(ctx context.Context, conn *websocket.Conn, dataFetcher func(ctx context.Context) (any, error), gvks ...schema.GroupVersionKind) {
+	// Channel to signal when data needs to be sent
+	updateChan := make(chan struct{}, 1)
+	var updateMu sync.Mutex
+
+	// Helper to trigger update (non-blocking)
+	triggerUpdate := func() {
+		updateMu.Lock()
+		defer updateMu.Unlock()
+		select {
+		case updateChan <- struct{}{}:
+		default:
+			// Channel already has a pending update
 		}
-		slog.Debug("Data marshaled into JSON at took %v", "duration", time.Since(marshalStart))
+	}
 
-		// Set write deadline to avoid blocking indefinitely
-		if err := conn.SetWriteDeadline(time.Now().Add(5 * time.Second)); err != nil {
-			slog.Error("Error setting write deadline: %v", "error", err)
-			return
+	// Track all registrations for cleanup
+	var registrations []cache.ResourceEventHandlerRegistration
+	defer func() {
+		for i, reg := range registrations {
+			if i < len(gvks) && gvks[i] != (schema.GroupVersionKind{}) {
+				informer, err := h.client.GetInformerForKind(ctx, gvks[i])
+				if err == nil {
+					if err := informer.RemoveEventHandler(reg); err != nil {
+						slog.Error("Error removing event handler: %v", "error", err)
```

  switched all of them to key=values

### Review by [@thejoeejoee](https://github.com/thejoeejoee) (commented) — 2026-02-27T20:51:45Z

- **cmd/kueueviz/backend/handlers/websocket_handler.go:79** by [@thejoeejoee](https://github.com/thejoeejoee) — 2026-02-27T20:51:45Z
```diff
@@ -50,82 +54,124 @@ func (h *Handlers) GenericWebSocketHandler(dataFetcher func(ctx context.Context)
 		defer conn.Close()
 		slog.Debug("WebSocket connection established took %v", "duration", time.Since(connStart))
 
-		// Fetch the initial data to send it immediately
-		fetchStart := time.Now()
-		data, err := dataFetcher(c.Request.Context())
-		if err != nil {
-			slog.Error("Error fetching data %v", "error", err)
+		ctx, cancel := context.WithCancel(c.Request.Context())
+		defer cancel()
+
+		// Send initial data
+		if err := h.sendData(ctx, conn, dataFetcher); err != nil {
+			slog.Error("Error sending initial data: %v", "error", err)
 			return
 		}
-		slog.Debug("Data fetched took %v", "duration", time.Since(fetchStart))
 
```

  fixed

### Review by [@samzong](https://github.com/samzong) (commented) — 2026-03-06T17:36:11Z

- **cmd/kueueviz/backend/handlers/gvr.go:70** by [@samzong](https://github.com/samzong) — 2026-03-06T17:25:17Z
```diff
@@ -43,6 +43,73 @@ func NewClientFromManager(manager ctrlmanager.Manager) Client {
 	}
 }
 
+// GVK helper functions for informers
+
+// ClusterQueuesGVK returns the GroupVersionKind for ClusterQueues
+func ClusterQueuesGVK() schema.GroupVersionKind {
+	return schema.GroupVersionKind{
+		Group:   "kueue.x-k8s.io",
+		Version: "v1beta1",
+		Kind:    "ClusterQueue",
+	}
+}
+
+// WorkloadsGVK returns the GroupVersionKind for Workloads
+func WorkloadsGVK() schema.GroupVersionKind {
+	return schema.GroupVersionKind{
+		Group:   "kueue.x-k8s.io",
+		Version: "v1beta1",
+		Kind:    "Workload",
+	}
+}
+
+// LocalQueuesGVK returns the GroupVersionKind for LocalQueues
+func LocalQueuesGVK() schema.GroupVersionKind {
+	return schema.GroupVersionKind{
+		Group:   "kueue.x-k8s.io",
+		Version: "v1beta1",
```

  I think it's should be v1beta2

- **cmd/kueueviz/backend/handlers/websocket_handler.go:138** by [@samzong](https://github.com/samzong) — 2026-03-06T17:27:08Z
```diff
@@ -44,88 +47,142 @@ func (h *Handlers) GenericWebSocketHandler(dataFetcher func(ctx context.Context)
 		connStart := time.Now()
 		conn, err := upgrader.Upgrade(c.Writer, c.Request, nil)
 		if err != nil {
-			slog.Debug("Failed to upgrade to WebSocket: %v", "error", err)
+			slog.Debug("Failed to upgrade to WebSocket", "error", err)
 			return
 		}
 		defer conn.Close()
-		slog.Debug("WebSocket connection established took %v", "duration", time.Since(connStart))
+		slog.Debug("WebSocket connection established", "duration", time.Since(connStart))
+
+		ctx, cancel := context.WithCancel(c.Request.Context())
+		defer cancel()
+
+		// Read pump: gorilla/websocket requires an active reader to process control
+		// frames and detect client disconnects. On any read error, cancel the context
+		// to unblock handleInformerUpdates and trigger informer cleanup.
+		go func() {
+			defer cancel()
+			for {
+				if _, _, err := conn.ReadMessage(); err != nil {
+					return
+				}
+			}
+		}()
 
-		// Fetch the initial data to send it immediately
-		fetchStart := time.Now()
-		data, err := dataFetcher(c.Request.Context())
-		if err != nil {
-			slog.Error("Error fetching data %v", "error", err)
+		// Send initial data
+		if err := h.sendData(ctx, conn, dataFetcher); err != nil {
+			slog.Error("Error sending initial data", "error", err)
 			return
 		}
-		slog.Debug("Data fetched took %v", "duration", time.Since(fetchStart))
 
-		// Marshal the fetched data into JSON
-		marshalStart := time.Now()
-		jsonData, err := json.Marshal(data)
-		if err != nil {
-			slog.Error("Error marshaling data : %v", "error", err)
-			return
+		// Use informer-based updates for real-time streaming
+		h.handleInformerUpdates(ctx, conn, dataFetcher, gvks...)
+
+		slog.Debug("WebSocket handler completed", "duration", time.Since(startTime))
+	}
+}
+
+// sendData fetches and sends data through the WebSocket connection
+func (h *Handlers) sendData(ctx context.Context, conn *websocket.Conn, dataFetcher func(ctx context.Context) (any, error)) error {
+	fetchStart := time.Now()
+	data, err := dataFetcher(ctx)
+	if err != nil {
+		return err
+	}
+	slog.Debug("Data fetched", "duration", time.Since(fetchStart))
+
+	marshalStart := time.Now()
+	jsonData, err := json.Marshal(data)
+	if err != nil {
+		return err
+	}
+	slog.Debug("Data marshaled into JSON", "duration", time.Since(marshalStart))
+
+	if err := conn.SetWriteDeadline(time.Now().Add(5 * time.Second)); err != nil {
+		return err
+	}
+
+	writeStart := time.Now()
+	err = conn.WriteMessage(websocket.TextMessage, jsonData)
+	if err != nil {
+		return err
+	}
+	slog.Debug("Message sent to client", "duration", time.Since(writeStart))
+
+	return nil
+}
+
+// handleInformerUpdates uses Kubernetes informers to stream real-time changes
+// Supports watching multiple resource types by registering handlers for all provided GVKs
+func (h *Handlers) handleInformerUpdates(ctx context.Context, conn *websocket.Conn, dataFetcher func(ctx context.Context) (any, error), gvks ...schema.GroupVersionKind) {
+	// Channel to signal when data needs to be sent
+	updateChan := make(chan struct{}, 1)
+
+	// Helper to trigger update (non-blocking)
+	triggerUpdate := func() {
+		select {
+		case updateChan <- struct{}{}:
+		default:
+			// Channel already has a pending update
 		}
-		slog.Debug("Data marshaled into JSON at took %v", "duration", time.Since(marshalStart))
+	}
 
-		// Set write deadline to avoid blocking indefinitely
-		if err := conn.SetWriteDeadline(time.Now().Add(5 * time.Second)); err != nil {
-			slog.Error("Error setting write deadline: %v", "error", err)
-			return
+	type registrationInfo struct {
+		gvk schema.GroupVersionKind
+		reg cache.ResourceEventHandlerRegistration
+	}
+
+	// Track all registrations for cleanup
+	var registrations []registrationInfo
+	defer func() {
+		for _, r := range registrations {
+			informer, err := h.client.GetInformerForKind(ctx, r.gvk)
```

  `ctx` has bedd canceled, it's should be `context.Background()`

- **cmd/kueueviz/backend/handlers/workloads_dashboard.go:47** by [@samzong](https://github.com/samzong) — 2026-03-06T17:30:12Z
```diff
@@ -41,7 +42,13 @@ func (h *Handlers) WorkloadsDashboardWebSocketHandler() gin.HandlerFunc {
 			return h.fetchDashboardData(ctx, namespace)
 		}
 
-		h.GenericWebSocketHandler(dataFetcher)(c)
+		h.GenericWebSocketHandler(dataFetcher,
+			WorkloadsGVK(),
+			PodsGVK(),
```

  Watching `PodsGVK()` cluster-wide with no debounce means every pod event triggers, this will be noisier than 5s polling in busy clusters.

- **cmd/kueueviz/backend/handlers/cluster_queues.go:43** by [@samzong](https://github.com/samzong) — 2026-03-06T17:33:08Z
```diff
@@ -30,31 +30,32 @@ import (
 func (h *Handlers) ClusterQueuesWebSocketHandler() gin.HandlerFunc {
 	return h.GenericWebSocketHandler(func(ctx context.Context) (any, error) {
 		return h.fetchClusterQueues(ctx)
-	})
+	}, ClusterQueuesGVK())
 }
 
 // ClusterQueueDetailsWebSocketHandler streams details for a specific cluster queue
 func (h *Handlers) ClusterQueueDetailsWebSocketHandler() gin.HandlerFunc {
 	return func(c *gin.Context) {
 		clusterQueueName := c.Param("cluster_queue_name")
+
 		h.GenericWebSocketHandler(func(ctx context.Context) (any, error) {
 			return h.fetchClusterQueueDetails(ctx, clusterQueueName)
-		})(c)
+		}, ClusterQueuesGVK())(c)
```

  `fetchClusterQueueDetails` reads both `ClusterQueue` and `LocalQueueList, but this handler only watches cluster_queue, Needs `LocalQueuesGVK()` here too.

- **cmd/kueueviz/backend/handlers/websocket_handler.go:119** by [@samzong](https://github.com/samzong) — 2026-03-06T17:36:05Z
```diff
@@ -44,88 +47,142 @@ func (h *Handlers) GenericWebSocketHandler(dataFetcher func(ctx context.Context)
 		connStart := time.Now()
 		conn, err := upgrader.Upgrade(c.Writer, c.Request, nil)
 		if err != nil {
-			slog.Debug("Failed to upgrade to WebSocket: %v", "error", err)
+			slog.Debug("Failed to upgrade to WebSocket", "error", err)
 			return
 		}
 		defer conn.Close()
-		slog.Debug("WebSocket connection established took %v", "duration", time.Since(connStart))
+		slog.Debug("WebSocket connection established", "duration", time.Since(connStart))
+
+		ctx, cancel := context.WithCancel(c.Request.Context())
+		defer cancel()
+
+		// Read pump: gorilla/websocket requires an active reader to process control
+		// frames and detect client disconnects. On any read error, cancel the context
+		// to unblock handleInformerUpdates and trigger informer cleanup.
+		go func() {
+			defer cancel()
+			for {
+				if _, _, err := conn.ReadMessage(); err != nil {
+					return
+				}
+			}
+		}()
 
-		// Fetch the initial data to send it immediately
-		fetchStart := time.Now()
-		data, err := dataFetcher(c.Request.Context())
-		if err != nil {
-			slog.Error("Error fetching data %v", "error", err)
+		// Send initial data
+		if err := h.sendData(ctx, conn, dataFetcher); err != nil {
+			slog.Error("Error sending initial data", "error", err)
 			return
 		}
-		slog.Debug("Data fetched took %v", "duration", time.Since(fetchStart))
 
-		// Marshal the fetched data into JSON
-		marshalStart := time.Now()
-		jsonData, err := json.Marshal(data)
-		if err != nil {
-			slog.Error("Error marshaling data : %v", "error", err)
-			return
+		// Use informer-based updates for real-time streaming
+		h.handleInformerUpdates(ctx, conn, dataFetcher, gvks...)
+
+		slog.Debug("WebSocket handler completed", "duration", time.Since(startTime))
+	}
+}
+
+// sendData fetches and sends data through the WebSocket connection
+func (h *Handlers) sendData(ctx context.Context, conn *websocket.Conn, dataFetcher func(ctx context.Context) (any, error)) error {
+	fetchStart := time.Now()
+	data, err := dataFetcher(ctx)
+	if err != nil {
+		return err
+	}
+	slog.Debug("Data fetched", "duration", time.Since(fetchStart))
+
+	marshalStart := time.Now()
+	jsonData, err := json.Marshal(data)
+	if err != nil {
+		return err
+	}
+	slog.Debug("Data marshaled into JSON", "duration", time.Since(marshalStart))
+
+	if err := conn.SetWriteDeadline(time.Now().Add(5 * time.Second)); err != nil {
+		return err
+	}
+
+	writeStart := time.Now()
+	err = conn.WriteMessage(websocket.TextMessage, jsonData)
+	if err != nil {
+		return err
+	}
+	slog.Debug("Message sent to client", "duration", time.Since(writeStart))
+
+	return nil
+}
+
+// handleInformerUpdates uses Kubernetes informers to stream real-time changes
+// Supports watching multiple resource types by registering handlers for all provided GVKs
+func (h *Handlers) handleInformerUpdates(ctx context.Context, conn *websocket.Conn, dataFetcher func(ctx context.Context) (any, error), gvks ...schema.GroupVersionKind) {
```

  should we need add test of new informer changes(registration/cleanup, etc.) ?

### Review by [@thejoeejoee](https://github.com/thejoeejoee) (commented) — 2026-03-08T08:19:16Z

- **cmd/kueueviz/backend/handlers/workloads_dashboard.go:47** by [@thejoeejoee](https://github.com/thejoeejoee) — 2026-03-08T08:19:16Z
```diff
@@ -41,7 +42,13 @@ func (h *Handlers) WorkloadsDashboardWebSocketHandler() gin.HandlerFunc {
 			return h.fetchDashboardData(ctx, namespace)
 		}
 
-		h.GenericWebSocketHandler(dataFetcher)(c)
+		h.GenericWebSocketHandler(dataFetcher,
+			WorkloadsGVK(),
+			PodsGVK(),
```

  dded generic debounce to the handler with 200 ms

### Review by [@thejoeejoee](https://github.com/thejoeejoee) (commented) — 2026-03-08T10:46:35Z

- **cmd/kueueviz/backend/handlers/cluster_queues.go:43** by [@thejoeejoee](https://github.com/thejoeejoee) — 2026-03-08T10:46:35Z
```diff
@@ -30,31 +30,32 @@ import (
 func (h *Handlers) ClusterQueuesWebSocketHandler() gin.HandlerFunc {
 	return h.GenericWebSocketHandler(func(ctx context.Context) (any, error) {
 		return h.fetchClusterQueues(ctx)
-	})
+	}, ClusterQueuesGVK())
 }
 
 // ClusterQueueDetailsWebSocketHandler streams details for a specific cluster queue
 func (h *Handlers) ClusterQueueDetailsWebSocketHandler() gin.HandlerFunc {
 	return func(c *gin.Context) {
 		clusterQueueName := c.Param("cluster_queue_name")
+
 		h.GenericWebSocketHandler(func(ctx context.Context) (any, error) {
 			return h.fetchClusterQueueDetails(ctx, clusterQueueName)
-		})(c)
+		}, ClusterQueuesGVK())(c)
```

  fixed

### Review by [@thejoeejoee](https://github.com/thejoeejoee) (commented) — 2026-03-08T10:46:56Z

- **cmd/kueueviz/backend/handlers/websocket_handler.go:119** by [@thejoeejoee](https://github.com/thejoeejoee) — 2026-03-08T10:46:56Z
```diff
@@ -44,88 +47,142 @@ func (h *Handlers) GenericWebSocketHandler(dataFetcher func(ctx context.Context)
 		connStart := time.Now()
 		conn, err := upgrader.Upgrade(c.Writer, c.Request, nil)
 		if err != nil {
-			slog.Debug("Failed to upgrade to WebSocket: %v", "error", err)
+			slog.Debug("Failed to upgrade to WebSocket", "error", err)
 			return
 		}
 		defer conn.Close()
-		slog.Debug("WebSocket connection established took %v", "duration", time.Since(connStart))
+		slog.Debug("WebSocket connection established", "duration", time.Since(connStart))
+
+		ctx, cancel := context.WithCancel(c.Request.Context())
+		defer cancel()
+
+		// Read pump: gorilla/websocket requires an active reader to process control
+		// frames and detect client disconnects. On any read error, cancel the context
+		// to unblock handleInformerUpdates and trigger informer cleanup.
+		go func() {
+			defer cancel()
+			for {
+				if _, _, err := conn.ReadMessage(); err != nil {
+					return
+				}
+			}
+		}()
 
-		// Fetch the initial data to send it immediately
-		fetchStart := time.Now()
-		data, err := dataFetcher(c.Request.Context())
-		if err != nil {
-			slog.Error("Error fetching data %v", "error", err)
+		// Send initial data
+		if err := h.sendData(ctx, conn, dataFetcher); err != nil {
+			slog.Error("Error sending initial data", "error", err)
 			return
 		}
-		slog.Debug("Data fetched took %v", "duration", time.Since(fetchStart))
 
-		// Marshal the fetched data into JSON
-		marshalStart := time.Now()
-		jsonData, err := json.Marshal(data)
-		if err != nil {
-			slog.Error("Error marshaling data : %v", "error", err)
-			return
+		// Use informer-based updates for real-time streaming
+		h.handleInformerUpdates(ctx, conn, dataFetcher, gvks...)
+
+		slog.Debug("WebSocket handler completed", "duration", time.Since(startTime))
+	}
+}
+
+// sendData fetches and sends data through the WebSocket connection
+func (h *Handlers) sendData(ctx context.Context, conn *websocket.Conn, dataFetcher func(ctx context.Context) (any, error)) error {
+	fetchStart := time.Now()
+	data, err := dataFetcher(ctx)
+	if err != nil {
+		return err
+	}
+	slog.Debug("Data fetched", "duration", time.Since(fetchStart))
+
+	marshalStart := time.Now()
+	jsonData, err := json.Marshal(data)
+	if err != nil {
+		return err
+	}
+	slog.Debug("Data marshaled into JSON", "duration", time.Since(marshalStart))
+
+	if err := conn.SetWriteDeadline(time.Now().Add(5 * time.Second)); err != nil {
+		return err
+	}
+
+	writeStart := time.Now()
+	err = conn.WriteMessage(websocket.TextMessage, jsonData)
+	if err != nil {
+		return err
+	}
+	slog.Debug("Message sent to client", "duration", time.Since(writeStart))
+
+	return nil
+}
+
+// handleInformerUpdates uses Kubernetes informers to stream real-time changes
+// Supports watching multiple resource types by registering handlers for all provided GVKs
+func (h *Handlers) handleInformerUpdates(ctx context.Context, conn *websocket.Conn, dataFetcher func(ctx context.Context) (any, error), gvks ...schema.GroupVersionKind) {
```

  added test including debounce case

### Review by [@thejoeejoee](https://github.com/thejoeejoee) (commented) — 2026-03-08T10:47:03Z

- **cmd/kueueviz/backend/handlers/websocket_handler.go:138** by [@thejoeejoee](https://github.com/thejoeejoee) — 2026-03-08T10:47:03Z
```diff
@@ -44,88 +47,142 @@ func (h *Handlers) GenericWebSocketHandler(dataFetcher func(ctx context.Context)
 		connStart := time.Now()
 		conn, err := upgrader.Upgrade(c.Writer, c.Request, nil)
 		if err != nil {
-			slog.Debug("Failed to upgrade to WebSocket: %v", "error", err)
+			slog.Debug("Failed to upgrade to WebSocket", "error", err)
 			return
 		}
 		defer conn.Close()
-		slog.Debug("WebSocket connection established took %v", "duration", time.Since(connStart))
+		slog.Debug("WebSocket connection established", "duration", time.Since(connStart))
+
+		ctx, cancel := context.WithCancel(c.Request.Context())
+		defer cancel()
+
+		// Read pump: gorilla/websocket requires an active reader to process control
+		// frames and detect client disconnects. On any read error, cancel the context
+		// to unblock handleInformerUpdates and trigger informer cleanup.
+		go func() {
+			defer cancel()
+			for {
+				if _, _, err := conn.ReadMessage(); err != nil {
+					return
+				}
+			}
+		}()
 
-		// Fetch the initial data to send it immediately
-		fetchStart := time.Now()
-		data, err := dataFetcher(c.Request.Context())
-		if err != nil {
-			slog.Error("Error fetching data %v", "error", err)
+		// Send initial data
+		if err := h.sendData(ctx, conn, dataFetcher); err != nil {
+			slog.Error("Error sending initial data", "error", err)
 			return
 		}
-		slog.Debug("Data fetched took %v", "duration", time.Since(fetchStart))
 
-		// Marshal the fetched data into JSON
-		marshalStart := time.Now()
-		jsonData, err := json.Marshal(data)
-		if err != nil {
-			slog.Error("Error marshaling data : %v", "error", err)
-			return
+		// Use informer-based updates for real-time streaming
+		h.handleInformerUpdates(ctx, conn, dataFetcher, gvks...)
+
+		slog.Debug("WebSocket handler completed", "duration", time.Since(startTime))
+	}
+}
+
+// sendData fetches and sends data through the WebSocket connection
+func (h *Handlers) sendData(ctx context.Context, conn *websocket.Conn, dataFetcher func(ctx context.Context) (any, error)) error {
+	fetchStart := time.Now()
+	data, err := dataFetcher(ctx)
+	if err != nil {
+		return err
+	}
+	slog.Debug("Data fetched", "duration", time.Since(fetchStart))
+
+	marshalStart := time.Now()
+	jsonData, err := json.Marshal(data)
+	if err != nil {
+		return err
+	}
+	slog.Debug("Data marshaled into JSON", "duration", time.Since(marshalStart))
+
+	if err := conn.SetWriteDeadline(time.Now().Add(5 * time.Second)); err != nil {
+		return err
+	}
+
+	writeStart := time.Now()
+	err = conn.WriteMessage(websocket.TextMessage, jsonData)
+	if err != nil {
+		return err
+	}
+	slog.Debug("Message sent to client", "duration", time.Since(writeStart))
+
+	return nil
+}
+
+// handleInformerUpdates uses Kubernetes informers to stream real-time changes
+// Supports watching multiple resource types by registering handlers for all provided GVKs
+func (h *Handlers) handleInformerUpdates(ctx context.Context, conn *websocket.Conn, dataFetcher func(ctx context.Context) (any, error), gvks ...schema.GroupVersionKind) {
+	// Channel to signal when data needs to be sent
+	updateChan := make(chan struct{}, 1)
+
+	// Helper to trigger update (non-blocking)
+	triggerUpdate := func() {
+		select {
+		case updateChan <- struct{}{}:
+		default:
+			// Channel already has a pending update
 		}
-		slog.Debug("Data marshaled into JSON at took %v", "duration", time.Since(marshalStart))
+	}
 
-		// Set write deadline to avoid blocking indefinitely
-		if err := conn.SetWriteDeadline(time.Now().Add(5 * time.Second)); err != nil {
-			slog.Error("Error setting write deadline: %v", "error", err)
-			return
+	type registrationInfo struct {
+		gvk schema.GroupVersionKind
+		reg cache.ResourceEventHandlerRegistration
+	}
+
+	// Track all registrations for cleanup
+	var registrations []registrationInfo
+	defer func() {
+		for _, r := range registrations {
+			informer, err := h.client.GetInformerForKind(ctx, r.gvk)
```

  fixed

### Review by [@thejoeejoee](https://github.com/thejoeejoee) (commented) — 2026-03-08T10:47:10Z

- **cmd/kueueviz/backend/handlers/gvr.go:70** by [@thejoeejoee](https://github.com/thejoeejoee) — 2026-03-08T10:47:10Z
```diff
@@ -43,6 +43,73 @@ func NewClientFromManager(manager ctrlmanager.Manager) Client {
 	}
 }
 
+// GVK helper functions for informers
+
+// ClusterQueuesGVK returns the GroupVersionKind for ClusterQueues
+func ClusterQueuesGVK() schema.GroupVersionKind {
+	return schema.GroupVersionKind{
+		Group:   "kueue.x-k8s.io",
+		Version: "v1beta1",
+		Kind:    "ClusterQueue",
+	}
+}
+
+// WorkloadsGVK returns the GroupVersionKind for Workloads
+func WorkloadsGVK() schema.GroupVersionKind {
+	return schema.GroupVersionKind{
+		Group:   "kueue.x-k8s.io",
+		Version: "v1beta1",
+		Kind:    "Workload",
+	}
+}
+
+// LocalQueuesGVK returns the GroupVersionKind for LocalQueues
+func LocalQueuesGVK() schema.GroupVersionKind {
+	return schema.GroupVersionKind{
+		Group:   "kueue.x-k8s.io",
+		Version: "v1beta1",
```

  fixed
