# PR #8724: Introduce FinishedWorkloads and LocalQueueFinishedWorkloads metrics.

**Summary**: Introduce FinishedWorkloads and LocalQueueFinishedWorkloads metrics.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8724

**Last updated**: 2026-01-26T12:32:39Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `feature/finished-workloads-metrics` → `main`
- **Created**: 2026-01-21T15:03:59Z
- **Updated**: 2026-01-26T12:32:39Z
- **Merged**: 2026-01-26T12:31:56Z
- **Closed**: 2026-01-26T12:31:56Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@gabesaba](https://github.com/gabesaba)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 10   **+187 / -17**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Introduce FinishedWorkloads and LocalQueueFinishedWorkloads metrics.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #6993

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Observability: Introduce the gauge metrics for finished workloads: kueue_finished_workloads and kueue_local_queue_finished_workloads.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-01-21T15:04:05Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 5def9a853cf45d74c09dca808958aba66ec902c6 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69774853fcbfb30008464307 |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-26T07:58:02Z

/cc @mimowo

### Comment by [@gabesaba](https://github.com/gabesaba) — 2026-01-26T10:59:23Z

/lgtm
/approve

thank you!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-26T10:59:32Z

LGTM label has been added.  <details>Git tree hash: b8480981606bf3b96d6c93323e7b2d7207c58189</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-26T10:59:36Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8724#issuecomment-3798995809" title="Approved">gabesaba</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8724#" title="Author self-approved">mbobrovskyi</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [gabesaba,mbobrovskyi]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-26T11:20:07Z

/test pull-kueue-test-e2e-main-1-32
This is a known flake, we should prioritize fixing as this is common now

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-26T11:22:21Z

Just to ref https://github.com/kubernetes-sigs/kueue/issues/8752.

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-23T08:28:19Z

- **apis/kueue/v1beta1/clusterqueue_types.go:310** by [@mimowo](https://github.com/mimowo) — 2026-01-23T08:28:19Z
```diff
@@ -305,6 +305,10 @@ type ClusterQueueStatus struct {
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
 
+	// finishedWorkloads is the number of finished workloads in this clusterQueue.
+	// +optional
+	FinishedWorkloads int32 `json:"finishedWorkloads"`
```

  It would be great to have the metric in 0.16.x.
  
  This means API change, which also means performance penalty for updating the status on removal of finished workloads. I'm wondering it could be better to base it purely on cache. Would that be feasible? 
  
  I'm not saying the API is bad, but maybe unnecessary. If we don't change API we could backport to 0.16.1 for example.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-23T08:28:37Z

- **apis/kueue/v1beta1/clusterqueue_types.go:310** by [@mimowo](https://github.com/mimowo) — 2026-01-23T08:28:37Z
```diff
@@ -305,6 +305,10 @@ type ClusterQueueStatus struct {
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
 
+	// finishedWorkloads is the number of finished workloads in this clusterQueue.
+	// +optional
+	FinishedWorkloads int32 `json:"finishedWorkloads"`
```

  Any opinion @tenzen-y @gabesaba ?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-23T14:28:03Z

- **apis/kueue/v1beta1/clusterqueue_types.go:310** by [@gabesaba](https://github.com/gabesaba) — 2026-01-23T14:28:02Z
```diff
@@ -305,6 +305,10 @@ type ClusterQueueStatus struct {
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
 
+	// finishedWorkloads is the number of finished workloads in this clusterQueue.
+	// +optional
+	FinishedWorkloads int32 `json:"finishedWorkloads"`
```

  Since the goal of this PR is new metrics, if we can do it just in the cache without any API change, I would lean towards that option. I would avoid changing the API until users are explicitly asking for this new field/status

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-23T17:15:52Z

- **pkg/controller/core/workload_controller.go:287** by [@mimowo](https://github.com/mimowo) — 2026-01-23T17:15:48Z
```diff
@@ -284,7 +284,7 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			queueOptions = append(queueOptions, workload.WithPreprocessedDRAResources(draResources))
 		}
 
-		if workload.IsAdmissible(&wl) {
+		if workload.IsActive(&wl) && !workload.HasQuotaReservation(&wl) {
```

  hmmm, Im not very happy about it. I think finished workloads shoukd be accounted by separate path, not by addOrUpdate. If you really need that, hopefullky not, then use IsAdmissbleOrFinished helper

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-23T18:04:50Z

- **pkg/controller/core/workload_controller.go:287** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-23T18:04:50Z
```diff
@@ -284,7 +284,7 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			queueOptions = append(queueOptions, workload.WithPreprocessedDRAResources(draResources))
 		}
 
-		if workload.IsAdmissible(&wl) {
+		if workload.IsActive(&wl) && !workload.HasQuotaReservation(&wl) {
```

  Agree. Here no need to revert.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-26T07:57:45Z

- **apis/kueue/v1beta1/clusterqueue_types.go:310** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-26T07:57:45Z
```diff
@@ -305,6 +305,10 @@ type ClusterQueueStatus struct {
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
 
+	// finishedWorkloads is the number of finished workloads in this clusterQueue.
+	// +optional
+	FinishedWorkloads int32 `json:"finishedWorkloads"`
```

  Reverted.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-26T08:07:24Z

- **pkg/cache/queue/manager.go:190** by [@mimowo](https://github.com/mimowo) — 2026-01-26T08:07:24Z
```diff
@@ -174,6 +176,76 @@ func (m *Manager) NotifyTopologyUpdateWatchers(oldTopology, newTopology *kueue.T
 	}
 }
 
+func (m *Manager) AddFinishedWorkload(wl *kueue.Workload) {
+	m.Lock()
+	defer m.Unlock()
+	m.addFinishedWorkloadWithoutLock(wl)
+}
+
+func (m *Manager) addFinishedWorkloadWithoutLock(wl *kueue.Workload) {
+	wlKey := workload.Key(wl)
+	m.deleteFinishedWorkloadWithoutLock(wlKey)
+
+	// We should manage only finished and assigned workloads.
+	if !workload.IsFinished(wl) || wl.Status.Admission == nil {
```

  Why checking `wl.Status.Admission == nil`? It seems unrelated. We also want to track finished workloads which lost admission (for whatever reason).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-26T08:09:31Z

- **pkg/cache/queue/manager.go:646** by [@mimowo](https://github.com/mimowo) — 2026-01-26T08:09:31Z
```diff
@@ -565,8 +643,14 @@ func (m *Manager) assignWorkload(wlKey workload.Reference, qKey queue.LocalQueue
 	m.workloadAssignedQueues[wlKey] = qKey
 }
 
-func (m *Manager) deleteWorkloadWithoutLock(log logr.Logger, wlKey workload.Reference) {
-	qKey := m.workloadAssignedQueues[wlKey]
+func (m *Manager) deleteWorkloadWithoutLock(log logr.Logger, wl *kueue.Workload) {
```

  I think you should not need to change the interface, right? We can just take it from`workloadAssignedQueues`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-26T08:11:51Z

- **pkg/cache/queue/manager.go:623** by [@mimowo](https://github.com/mimowo) — 2026-01-26T08:11:51Z
```diff
@@ -536,24 +612,26 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, reas
 
 // Delete the workload from queue or cluster queue.
 // Does not remove the queue assignment caching.
-func (m *Manager) DeleteWorkload(log logr.Logger, wlKey workload.Reference) {
+func (m *Manager) DeleteWorkload(log logr.Logger, wl *kueue.Workload) {
 	m.Lock()
 	defer m.Unlock()
-	m.deleteWorkloadWithoutLock(log, wlKey)
+	m.deleteWorkloadWithoutLock(log, wl)
 }
 
 // Deletes the workload from assigned queue and purges the assigment caching.
 // Uses a lock to ensure operation safety.
-func (m *Manager) DeleteAndForgetWorkload(log logr.Logger, wlKey workload.Reference) {
+func (m *Manager) DeleteAndForgetWorkload(log logr.Logger, wl *kueue.Workload) {
```

  Why changing the interface? It is probably ok-ish for a time-being, but ultimately we would like the functions to accept just the key, so that we can move the logic from event handles to reconcile. So, if we don't change that

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-26T08:12:56Z

- **pkg/cache/queue/manager.go:646** by [@mimowo](https://github.com/mimowo) — 2026-01-26T08:12:56Z
```diff
@@ -565,8 +643,14 @@ func (m *Manager) assignWorkload(wlKey workload.Reference, qKey queue.LocalQueue
 	m.workloadAssignedQueues[wlKey] = qKey
 }
 
-func (m *Manager) deleteWorkloadWithoutLock(log logr.Logger, wlKey workload.Reference) {
-	qKey := m.workloadAssignedQueues[wlKey]
+func (m *Manager) deleteWorkloadWithoutLock(log logr.Logger, wl *kueue.Workload) {
```

  Maybe we could achieve that if you kept `finishedWorkloads` to be a set rather than just a count?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-26T08:14:37Z

- **pkg/cache/queue/manager.go:646** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-26T08:14:37Z
```diff
@@ -565,8 +643,14 @@ func (m *Manager) assignWorkload(wlKey workload.Reference, qKey queue.LocalQueue
 	m.workloadAssignedQueues[wlKey] = qKey
 }
 
-func (m *Manager) deleteWorkloadWithoutLock(log logr.Logger, wlKey workload.Reference) {
-	qKey := m.workloadAssignedQueues[wlKey]
+func (m *Manager) deleteWorkloadWithoutLock(log logr.Logger, wl *kueue.Workload) {
```

  > Maybe we could achieve that if you kept finishedWorkloads to be a set rather than just a count?
  
  Yeah, I thought about using a set, but it would consume a lot of unnecessary memory.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-26T08:27:00Z

- **pkg/cache/queue/manager.go:646** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-26T08:27:00Z
```diff
@@ -565,8 +643,14 @@ func (m *Manager) assignWorkload(wlKey workload.Reference, qKey queue.LocalQueue
 	m.workloadAssignedQueues[wlKey] = qKey
 }
 
-func (m *Manager) deleteWorkloadWithoutLock(log logr.Logger, wlKey workload.Reference) {
-	qKey := m.workloadAssignedQueues[wlKey]
+func (m *Manager) deleteWorkloadWithoutLock(log logr.Logger, wl *kueue.Workload) {
```

  > I think you should not need to change the interface, right? We can just take it fromworkloadAssignedQueues
  
  No, because in deleteWorkloadWithoutLock we should add the workload to the cache. Only finished workloads should be added, and fromWorkloadAssignedQueues does not provide this information.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-26T08:31:31Z

- **pkg/cache/queue/manager.go:623** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-26T08:31:31Z
```diff
@@ -536,24 +612,26 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, reas
 
 // Delete the workload from queue or cluster queue.
 // Does not remove the queue assignment caching.
-func (m *Manager) DeleteWorkload(log logr.Logger, wlKey workload.Reference) {
+func (m *Manager) DeleteWorkload(log logr.Logger, wl *kueue.Workload) {
 	m.Lock()
 	defer m.Unlock()
-	m.deleteWorkloadWithoutLock(log, wlKey)
+	m.deleteWorkloadWithoutLock(log, wl)
 }
 
 // Deletes the workload from assigned queue and purges the assigment caching.
 // Uses a lock to ensure operation safety.
-func (m *Manager) DeleteAndForgetWorkload(log logr.Logger, wlKey workload.Reference) {
+func (m *Manager) DeleteAndForgetWorkload(log logr.Logger, wl *kueue.Workload) {
```

  Since deleteWorkloadWithoutLock changed its interface, we now need to propagate wl instead of wlKey.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-26T08:35:18Z

- **pkg/cache/queue/manager.go:623** by [@mimowo](https://github.com/mimowo) — 2026-01-26T08:35:18Z
```diff
@@ -536,24 +612,26 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, reas
 
 // Delete the workload from queue or cluster queue.
 // Does not remove the queue assignment caching.
-func (m *Manager) DeleteWorkload(log logr.Logger, wlKey workload.Reference) {
+func (m *Manager) DeleteWorkload(log logr.Logger, wl *kueue.Workload) {
 	m.Lock()
 	defer m.Unlock()
-	m.deleteWorkloadWithoutLock(log, wlKey)
+	m.deleteWorkloadWithoutLock(log, wl)
 }
 
 // Deletes the workload from assigned queue and purges the assigment caching.
 // Uses a lock to ensure operation safety.
-func (m *Manager) DeleteAndForgetWorkload(log logr.Logger, wlKey workload.Reference) {
+func (m *Manager) DeleteAndForgetWorkload(log logr.Logger, wl *kueue.Workload) {
```

  Hm, I don't think so, if you stored wlKeys for finished workloads instad of count `finishedWorkloads`  then I think `DeleteAndForgetWorkload` could be just deleting from both places - the queues and the `finishedWorkloads`, becasue then the deletion from the set `finishedWorkloads` would be idempotent.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-26T08:37:26Z

- **pkg/cache/queue/manager.go:646** by [@mimowo](https://github.com/mimowo) — 2026-01-26T08:37:26Z
```diff
@@ -565,8 +643,14 @@ func (m *Manager) assignWorkload(wlKey workload.Reference, qKey queue.LocalQueue
 	m.workloadAssignedQueues[wlKey] = qKey
 }
 
-func (m *Manager) deleteWorkloadWithoutLock(log logr.Logger, wlKey workload.Reference) {
-	qKey := m.workloadAssignedQueues[wlKey]
+func (m *Manager) deleteWorkloadWithoutLock(log logr.Logger, wl *kueue.Workload) {
```

  > Yeah, I thought about using a set, but it would consume a lot of unnecessary memory.
  
  Tracking workload keys is not a lot of memory. Informers (part of Kueue binary) anyway remember deep copy of entire Workload structure present in the system (finished or not). 
  
  Let's avoid premature optimization.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-26T08:47:27Z

- **pkg/cache/queue/manager.go:623** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-26T08:47:27Z
```diff
@@ -536,24 +612,26 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, reas
 
 // Delete the workload from queue or cluster queue.
 // Does not remove the queue assignment caching.
-func (m *Manager) DeleteWorkload(log logr.Logger, wlKey workload.Reference) {
+func (m *Manager) DeleteWorkload(log logr.Logger, wl *kueue.Workload) {
 	m.Lock()
 	defer m.Unlock()
-	m.deleteWorkloadWithoutLock(log, wlKey)
+	m.deleteWorkloadWithoutLock(log, wl)
 }
 
 // Deletes the workload from assigned queue and purges the assigment caching.
 // Uses a lock to ensure operation safety.
-func (m *Manager) DeleteAndForgetWorkload(log logr.Logger, wlKey workload.Reference) {
+func (m *Manager) DeleteAndForgetWorkload(log logr.Logger, wl *kueue.Workload) {
```

  But we need to check is it finished or not. Only if finished we can move it to finishedWorkloads set in deleteWorkloadWithoutLock(). workloadAssignedQueues doesn't provide this information.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-26T08:54:11Z

- **pkg/cache/queue/manager.go:623** by [@mimowo](https://github.com/mimowo) — 2026-01-26T08:54:10Z
```diff
@@ -536,24 +612,26 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, reas
 
 // Delete the workload from queue or cluster queue.
 // Does not remove the queue assignment caching.
-func (m *Manager) DeleteWorkload(log logr.Logger, wlKey workload.Reference) {
+func (m *Manager) DeleteWorkload(log logr.Logger, wl *kueue.Workload) {
 	m.Lock()
 	defer m.Unlock()
-	m.deleteWorkloadWithoutLock(log, wlKey)
+	m.deleteWorkloadWithoutLock(log, wl)
 }
 
 // Deletes the workload from assigned queue and purges the assigment caching.
 // Uses a lock to ensure operation safety.
-func (m *Manager) DeleteAndForgetWorkload(log logr.Logger, wlKey workload.Reference) {
+func (m *Manager) DeleteAndForgetWorkload(log logr.Logger, wl *kueue.Workload) {
```

  So, let's see. 
  
  We need to call `AddFinishedWorkload` when:
  1. Create event, already done here: https://github.com/kubernetes-sigs/kueue/pull/8724/changes#diff-60dd240c20adbd6a189d018d1c216c2d296730f446c341d8bf449fa6657964ffR811
  2. Update event (missing)
  
  Then we call DeleteFinishedWorkload on Delete event (or within DeleteAndForgetWorkload).

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-26T08:59:47Z

- **pkg/cache/queue/manager.go:623** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-26T08:59:47Z
```diff
@@ -536,24 +612,26 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, reas
 
 // Delete the workload from queue or cluster queue.
 // Does not remove the queue assignment caching.
-func (m *Manager) DeleteWorkload(log logr.Logger, wlKey workload.Reference) {
+func (m *Manager) DeleteWorkload(log logr.Logger, wl *kueue.Workload) {
 	m.Lock()
 	defer m.Unlock()
-	m.deleteWorkloadWithoutLock(log, wlKey)
+	m.deleteWorkloadWithoutLock(log, wl)
 }
 
 // Deletes the workload from assigned queue and purges the assigment caching.
 // Uses a lock to ensure operation safety.
-func (m *Manager) DeleteAndForgetWorkload(log logr.Logger, wlKey workload.Reference) {
+func (m *Manager) DeleteAndForgetWorkload(log logr.Logger, wl *kueue.Workload) {
```

  > Update event (missing)
  
  Actually it's not missing. I added it inside deleteWorkloadWithoutLock().

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-26T09:04:33Z

- **pkg/cache/queue/manager.go:646** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-26T09:04:33Z
```diff
@@ -565,8 +643,14 @@ func (m *Manager) assignWorkload(wlKey workload.Reference, qKey queue.LocalQueue
 	m.workloadAssignedQueues[wlKey] = qKey
 }
 
-func (m *Manager) deleteWorkloadWithoutLock(log logr.Logger, wlKey workload.Reference) {
-	qKey := m.workloadAssignedQueues[wlKey]
+func (m *Manager) deleteWorkloadWithoutLock(log logr.Logger, wl *kueue.Workload) {
```

  > Tracking workload keys is not a lot of memory.
  
  Actually, it's a lot if we compare integer and set of References.
  
  > Let's avoid premature optimization.
  
  I didn't got why we need to keep set of References if we can just keep integer.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-26T09:08:35Z

- **pkg/cache/queue/manager.go:623** by [@mimowo](https://github.com/mimowo) — 2026-01-26T09:08:35Z
```diff
@@ -536,24 +612,26 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, reas
 
 // Delete the workload from queue or cluster queue.
 // Does not remove the queue assignment caching.
-func (m *Manager) DeleteWorkload(log logr.Logger, wlKey workload.Reference) {
+func (m *Manager) DeleteWorkload(log logr.Logger, wl *kueue.Workload) {
 	m.Lock()
 	defer m.Unlock()
-	m.deleteWorkloadWithoutLock(log, wlKey)
+	m.deleteWorkloadWithoutLock(log, wl)
 }
 
 // Deletes the workload from assigned queue and purges the assigment caching.
 // Uses a lock to ensure operation safety.
-func (m *Manager) DeleteAndForgetWorkload(log logr.Logger, wlKey workload.Reference) {
+func (m *Manager) DeleteAndForgetWorkload(log logr.Logger, wl *kueue.Workload) {
```

  Yes, but it required the interface changes I'm trying to avoid. So, if called from `Update` then I wouldn't see a need to change interfaces. That is my point, to make the changes minimal.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-26T09:12:47Z

- **pkg/cache/queue/manager.go:623** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-26T09:12:47Z
```diff
@@ -536,24 +612,26 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, reas
 
 // Delete the workload from queue or cluster queue.
 // Does not remove the queue assignment caching.
-func (m *Manager) DeleteWorkload(log logr.Logger, wlKey workload.Reference) {
+func (m *Manager) DeleteWorkload(log logr.Logger, wl *kueue.Workload) {
 	m.Lock()
 	defer m.Unlock()
-	m.deleteWorkloadWithoutLock(log, wlKey)
+	m.deleteWorkloadWithoutLock(log, wl)
 }
 
 // Deletes the workload from assigned queue and purges the assigment caching.
 // Uses a lock to ensure operation safety.
-func (m *Manager) DeleteAndForgetWorkload(log logr.Logger, wlKey workload.Reference) {
+func (m *Manager) DeleteAndForgetWorkload(log logr.Logger, wl *kueue.Workload) {
```

  Otherwise, we would need to move it to the Update event and probably other places, but I think we might forget to update it in the future.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-26T09:21:43Z

- **pkg/cache/queue/manager.go:623** by [@mimowo](https://github.com/mimowo) — 2026-01-26T09:21:43Z
```diff
@@ -536,24 +612,26 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, reas
 
 // Delete the workload from queue or cluster queue.
 // Does not remove the queue assignment caching.
-func (m *Manager) DeleteWorkload(log logr.Logger, wlKey workload.Reference) {
+func (m *Manager) DeleteWorkload(log logr.Logger, wl *kueue.Workload) {
 	m.Lock()
 	defer m.Unlock()
-	m.deleteWorkloadWithoutLock(log, wlKey)
+	m.deleteWorkloadWithoutLock(log, wl)
 }
 
 // Deletes the workload from assigned queue and purges the assigment caching.
 // Uses a lock to ensure operation safety.
-func (m *Manager) DeleteAndForgetWorkload(log logr.Logger, wlKey workload.Reference) {
+func (m *Manager) DeleteAndForgetWorkload(log logr.Logger, wl *kueue.Workload) {
```

  I think it is ok to care mostly about the pre-existing code, as this is the main functionality we have. 
  
  I think long term I would prefer to have something like `finishedWorkloadQueues: map[Workload]LocalQueue`, and move finished workload out from `assignedWorkloadQueues`, but I'm not yet sure this is a good idea.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-26T09:23:11Z

- **pkg/cache/queue/manager.go:623** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-26T09:23:11Z
```diff
@@ -536,24 +612,26 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, reas
 
 // Delete the workload from queue or cluster queue.
 // Does not remove the queue assignment caching.
-func (m *Manager) DeleteWorkload(log logr.Logger, wlKey workload.Reference) {
+func (m *Manager) DeleteWorkload(log logr.Logger, wl *kueue.Workload) {
 	m.Lock()
 	defer m.Unlock()
-	m.deleteWorkloadWithoutLock(log, wlKey)
+	m.deleteWorkloadWithoutLock(log, wl)
 }
 
 // Deletes the workload from assigned queue and purges the assigment caching.
 // Uses a lock to ensure operation safety.
-func (m *Manager) DeleteAndForgetWorkload(log logr.Logger, wlKey workload.Reference) {
+func (m *Manager) DeleteAndForgetWorkload(log logr.Logger, wl *kueue.Workload) {
```

  > I think long term I would prefer to have something like finishedWorkloadQueues: map[Workload]LocalQueue, and move finished workload out from assignedWorkloadQueues, but I'm not yet sure this is a good idea.
  
  Yes, but still we need Workload to check is it finished or not.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-26T09:47:59Z

- **pkg/cache/queue/manager.go:623** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-26T09:47:59Z
```diff
@@ -536,24 +612,26 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, reas
 
 // Delete the workload from queue or cluster queue.
 // Does not remove the queue assignment caching.
-func (m *Manager) DeleteWorkload(log logr.Logger, wlKey workload.Reference) {
+func (m *Manager) DeleteWorkload(log logr.Logger, wl *kueue.Workload) {
 	m.Lock()
 	defer m.Unlock()
-	m.deleteWorkloadWithoutLock(log, wlKey)
+	m.deleteWorkloadWithoutLock(log, wl)
 }
 
 // Deletes the workload from assigned queue and purges the assigment caching.
 // Uses a lock to ensure operation safety.
-func (m *Manager) DeleteAndForgetWorkload(log logr.Logger, wlKey workload.Reference) {
+func (m *Manager) DeleteAndForgetWorkload(log logr.Logger, wl *kueue.Workload) {
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-26T10:03:04Z

- **pkg/cache/queue/manager.go:646** by [@mimowo](https://github.com/mimowo) — 2026-01-26T10:03:03Z
```diff
@@ -565,8 +643,14 @@ func (m *Manager) assignWorkload(wlKey workload.Reference, qKey queue.LocalQueue
 	m.workloadAssignedQueues[wlKey] = qKey
 }
 
-func (m *Manager) deleteWorkloadWithoutLock(log logr.Logger, wlKey workload.Reference) {
-	qKey := m.workloadAssignedQueues[wlKey]
+func (m *Manager) deleteWorkloadWithoutLock(log logr.Logger, wl *kueue.Workload) {
```

  > Actually, it's a lot if we compare integer and set of References.
  
  Sure, but it does not change the asymptotic memory usage, only constant, and only by a small fraction. 
  
  Each finished workload is already tracked in informer and is probably 10k bytes (including PodTemplates it can easily be more). Plus, we already keep workload in cache in a couple of fields, Finally, in a typical healthy environment finished workloads are a small fraction of all workloads. So, I'm not worried about memory coming from the extra field.
  
  > I didn't got why we need to keep set of References if we can just keep integer.
  
  To avoid the risk of double counting, or double subtracting. For example, when we have LQ added/deleted, etc.
  
  If you feel confident there is no such risk then we can try with just counter.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-26T10:07:32Z

- **pkg/cache/queue/manager.go:190** by [@mimowo](https://github.com/mimowo) — 2026-01-26T10:07:32Z
```diff
@@ -174,6 +176,76 @@ func (m *Manager) NotifyTopologyUpdateWatchers(oldTopology, newTopology *kueue.T
 	}
 }
 
+func (m *Manager) AddFinishedWorkload(wl *kueue.Workload) {
+	m.Lock()
+	defer m.Unlock()
+	m.addFinishedWorkloadWithoutLock(wl)
+}
+
+func (m *Manager) addFinishedWorkloadWithoutLock(wl *kueue.Workload) {
+	wlKey := workload.Key(wl)
+	m.deleteFinishedWorkloadWithoutLock(wlKey)
+
+	// We should manage only finished and assigned workloads.
+	if !workload.IsFinished(wl) || wl.Status.Admission == nil {
```

  I see this code changed, so please resolve

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-26T10:14:09Z

lgtm, just a couple readability nits (since we have two extremely similar metrics)

- **pkg/metrics/metrics.go:703** by [@gabesaba](https://github.com/gabesaba) — 2026-01-26T10:10:55Z
```diff
@@ -682,6 +692,26 @@ func ReportLocalQueuePendingWorkloads(lq LocalQueueReference, active, inadmissib
 	LocalQueuePendingWorkloads.WithLabelValues(string(lq.Name), lq.Namespace, PendingStatusInadmissible, role).Set(float64(inadmissible))
 }
 
+func FinishedWorkload(cqName kueue.ClusterQueueReference, priorityClass string, tracker *roletracker.RoleTracker) {
+	role := roletracker.GetRole(tracker)
+	FinishedWorkloadsTotal.WithLabelValues(string(cqName), priorityClass, role).Inc()
+}
+
+func LocalQueueFinishedWorkload(lq LocalQueueReference, priorityClass string, tracker *roletracker.RoleTracker) {
+	role := roletracker.GetRole(tracker)
+	LocalQueueFinishedWorkloadsTotal.WithLabelValues(string(lq.Name), lq.Namespace, priorityClass, role).Inc()
+}
```

  Can we add total to the names? `FinishedWorkloadTotal`, `LocalQueueFinishedWorkloadTotal`
  
  Perhaps add a short comment above each of these 4 functions, just indicating that the first 2 are counters and latter 2 are gauge?

- **test/integration/singlecluster/controller/core/workload_controller_test.go:916** by [@gabesaba](https://github.com/gabesaba) — 2026-01-26T10:12:26Z
```diff
@@ -909,6 +911,9 @@ var _ = ginkgo.Describe("Workload controller with resource retention", ginkgo.Or
 					return k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &createdWorkload)
 				}, util.Timeout, util.Interval).ShouldNot(gomega.Succeed())
 			})
+
+			util.ExpectFinishedWorkloadsMetric(clusterQueue, 0)
+			util.ExpectLQFinishedWorkloadsMetric(localQueue, 0)
```

  Do we also want an assertion for the total metric here, to show that the count maintains? or do we cover that elsewhere?

- **test/util/util.go:639** by [@gabesaba](https://github.com/gabesaba) — 2026-01-26T10:13:11Z
```diff
@@ -636,6 +636,26 @@ func ExpectPendingWorkloadsMetric(cq *kueue.ClusterQueue, active, inadmissible i
 	}
 }
 
+func ExpectFinishedWorkloadsMetric(cq *kueue.ClusterQueue, count int) {
```

  For test readability, even though it is a little explicit, how about ExpectFinishedWorkloadsGaugeMetric?

- **pkg/metrics/metrics.go:705** by [@gabesaba](https://github.com/gabesaba) — 2026-01-26T10:13:46Z
```diff
@@ -682,6 +692,26 @@ func ReportLocalQueuePendingWorkloads(lq LocalQueueReference, active, inadmissib
 	LocalQueuePendingWorkloads.WithLabelValues(string(lq.Name), lq.Namespace, PendingStatusInadmissible, role).Set(float64(inadmissible))
 }
 
+func FinishedWorkload(cqName kueue.ClusterQueueReference, priorityClass string, tracker *roletracker.RoleTracker) {
+	role := roletracker.GetRole(tracker)
+	FinishedWorkloadsTotal.WithLabelValues(string(cqName), priorityClass, role).Inc()
+}
+
+func LocalQueueFinishedWorkload(lq LocalQueueReference, priorityClass string, tracker *roletracker.RoleTracker) {
+	role := roletracker.GetRole(tracker)
+	LocalQueueFinishedWorkloadsTotal.WithLabelValues(string(lq.Name), lq.Namespace, priorityClass, role).Inc()
+}
+
+func ReportFinishedWorkloads(cqName kueue.ClusterQueueReference, count int, tracker *roletracker.RoleTracker) {
```

  Also here, since it is not user facing, including either a comment or Gauge in the name?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-26T10:31:44Z

- **pkg/metrics/metrics.go:703** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-26T10:31:44Z
```diff
@@ -682,6 +692,26 @@ func ReportLocalQueuePendingWorkloads(lq LocalQueueReference, active, inadmissib
 	LocalQueuePendingWorkloads.WithLabelValues(string(lq.Name), lq.Namespace, PendingStatusInadmissible, role).Set(float64(inadmissible))
 }
 
+func FinishedWorkload(cqName kueue.ClusterQueueReference, priorityClass string, tracker *roletracker.RoleTracker) {
+	role := roletracker.GetRole(tracker)
+	FinishedWorkloadsTotal.WithLabelValues(string(cqName), priorityClass, role).Inc()
+}
+
+func LocalQueueFinishedWorkload(lq LocalQueueReference, priorityClass string, tracker *roletracker.RoleTracker) {
+	role := roletracker.GetRole(tracker)
+	LocalQueueFinishedWorkloadsTotal.WithLabelValues(string(lq.Name), lq.Namespace, priorityClass, role).Inc()
+}
```

  What do you think to rename to `IncFinishedWorkloadsTotal` and `IncLocalQueueFinishedWorkloadsTotal`?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-26T10:34:48Z

- **pkg/metrics/metrics.go:703** by [@gabesaba](https://github.com/gabesaba) — 2026-01-26T10:34:48Z
```diff
@@ -682,6 +692,26 @@ func ReportLocalQueuePendingWorkloads(lq LocalQueueReference, active, inadmissib
 	LocalQueuePendingWorkloads.WithLabelValues(string(lq.Name), lq.Namespace, PendingStatusInadmissible, role).Set(float64(inadmissible))
 }
 
+func FinishedWorkload(cqName kueue.ClusterQueueReference, priorityClass string, tracker *roletracker.RoleTracker) {
+	role := roletracker.GetRole(tracker)
+	FinishedWorkloadsTotal.WithLabelValues(string(cqName), priorityClass, role).Inc()
+}
+
+func LocalQueueFinishedWorkload(lq LocalQueueReference, priorityClass string, tracker *roletracker.RoleTracker) {
+	role := roletracker.GetRole(tracker)
+	LocalQueueFinishedWorkloadsTotal.WithLabelValues(string(lq.Name), lq.Namespace, priorityClass, role).Inc()
+}
```

  Sounds good, though let's expand it: ```IncrementFinishedWorkloadsTotal```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-26T10:41:04Z

- **pkg/cache/queue/manager.go:646** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-26T10:41:04Z
```diff
@@ -565,8 +643,14 @@ func (m *Manager) assignWorkload(wlKey workload.Reference, qKey queue.LocalQueue
 	m.workloadAssignedQueues[wlKey] = qKey
 }
 
-func (m *Manager) deleteWorkloadWithoutLock(log logr.Logger, wlKey workload.Reference) {
-	qKey := m.workloadAssignedQueues[wlKey]
+func (m *Manager) deleteWorkloadWithoutLock(log logr.Logger, wl *kueue.Workload) {
```

  > To avoid the risk of double counting, or double subtracting.
  
  Currently, it’s safe to use as is. However, it’s possible we might forget to increment/decrement it in some places in the future. If we’re not concerned about memory usage, I’m fine with using a set.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-26T10:47:08Z

- **test/integration/singlecluster/controller/core/workload_controller_test.go:916** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-26T10:47:08Z
```diff
@@ -909,6 +911,9 @@ var _ = ginkgo.Describe("Workload controller with resource retention", ginkgo.Or
 					return k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &createdWorkload)
 				}, util.Timeout, util.Interval).ShouldNot(gomega.Succeed())
 			})
+
+			util.ExpectFinishedWorkloadsMetric(clusterQueue, 0)
+			util.ExpectLQFinishedWorkloadsMetric(localQueue, 0)
```

  Yes, to make sure we decrement finishedWorkloads after the workload is deleted.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-26T10:51:54Z

- **pkg/metrics/metrics.go:703** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-26T10:51:54Z
```diff
@@ -682,6 +692,26 @@ func ReportLocalQueuePendingWorkloads(lq LocalQueueReference, active, inadmissib
 	LocalQueuePendingWorkloads.WithLabelValues(string(lq.Name), lq.Namespace, PendingStatusInadmissible, role).Set(float64(inadmissible))
 }
 
+func FinishedWorkload(cqName kueue.ClusterQueueReference, priorityClass string, tracker *roletracker.RoleTracker) {
+	role := roletracker.GetRole(tracker)
+	FinishedWorkloadsTotal.WithLabelValues(string(cqName), priorityClass, role).Inc()
+}
+
+func LocalQueueFinishedWorkload(lq LocalQueueReference, priorityClass string, tracker *roletracker.RoleTracker) {
+	role := roletracker.GetRole(tracker)
+	LocalQueueFinishedWorkloadsTotal.WithLabelValues(string(lq.Name), lq.Namespace, priorityClass, role).Inc()
+}
```

  Done

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2026-01-26T10:53:17Z

- **test/integration/singlecluster/controller/core/workload_controller_test.go:916** by [@gabesaba](https://github.com/gabesaba) — 2026-01-26T10:53:16Z
```diff
@@ -909,6 +911,9 @@ var _ = ginkgo.Describe("Workload controller with resource retention", ginkgo.Or
 					return k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &createdWorkload)
 				}, util.Timeout, util.Interval).ShouldNot(gomega.Succeed())
 			})
+
+			util.ExpectFinishedWorkloadsMetric(clusterQueue, 0)
+			util.ExpectLQFinishedWorkloadsMetric(localQueue, 0)
```

  I meant that: in addition to the gauge metric, do we want to assert the counter metric here too? we can leave that as a follow-up

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-26T10:56:16Z

- **test/util/util.go:639** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-26T10:56:16Z
```diff
@@ -636,6 +636,26 @@ func ExpectPendingWorkloadsMetric(cq *kueue.ClusterQueue, active, inadmissible i
 	}
 }
 
+func ExpectFinishedWorkloadsMetric(cq *kueue.ClusterQueue, count int) {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-26T10:56:27Z

- **pkg/metrics/metrics.go:705** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-26T10:56:27Z
```diff
@@ -682,6 +692,26 @@ func ReportLocalQueuePendingWorkloads(lq LocalQueueReference, active, inadmissib
 	LocalQueuePendingWorkloads.WithLabelValues(string(lq.Name), lq.Namespace, PendingStatusInadmissible, role).Set(float64(inadmissible))
 }
 
+func FinishedWorkload(cqName kueue.ClusterQueueReference, priorityClass string, tracker *roletracker.RoleTracker) {
+	role := roletracker.GetRole(tracker)
+	FinishedWorkloadsTotal.WithLabelValues(string(cqName), priorityClass, role).Inc()
+}
+
+func LocalQueueFinishedWorkload(lq LocalQueueReference, priorityClass string, tracker *roletracker.RoleTracker) {
+	role := roletracker.GetRole(tracker)
+	LocalQueueFinishedWorkloadsTotal.WithLabelValues(string(lq.Name), lq.Namespace, priorityClass, role).Inc()
+}
+
+func ReportFinishedWorkloads(cqName kueue.ClusterQueueReference, count int, tracker *roletracker.RoleTracker) {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-26T10:57:04Z

- **test/integration/singlecluster/controller/core/workload_controller_test.go:916** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-26T10:57:04Z
```diff
@@ -909,6 +911,9 @@ var _ = ginkgo.Describe("Workload controller with resource retention", ginkgo.Or
 					return k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &createdWorkload)
 				}, util.Timeout, util.Interval).ShouldNot(gomega.Succeed())
 			})
+
+			util.ExpectFinishedWorkloadsMetric(clusterQueue, 0)
+			util.ExpectLQFinishedWorkloadsMetric(localQueue, 0)
```

  Ah, I see. Sure, I will do that in follow-up.
