# PR #8745: test: add clusterprofile integration tests

**Summary**: test: add clusterprofile integration tests

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8745

**Last updated**: 2026-01-26T16:15:55Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mykysha](https://github.com/mykysha)
- **Branch**: `test/integration-clusterprofiles` → `main`
- **Created**: 2026-01-22T09:27:10Z
- **Updated**: 2026-01-26T16:15:55Z
- **Merged**: 2026-01-26T11:41:17Z
- **Closed**: 2026-01-26T11:41:18Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 1   **+386 / -46**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add integration tests for ClusterProfiles

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #7850 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-01-22T09:27:17Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 5ab4b28acea06f4fb5e92a4fb9dc423a4f127fb5 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69772fc10a88a20008106408 |

### Comment by [@mykysha](https://github.com/mykysha) — 2026-01-22T09:52:00Z

@mbobrovskyi @mszadkow PTAL

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-26T09:50:02Z

@mykysha have you had a chance to test locally the tests aren't flaking? Eg. repeat them 100 times or so.

### Comment by [@mykysha](https://github.com/mykysha) — 2026-01-26T09:51:04Z

I have tested the previous version, and am currently running the test on this one

### Comment by [@mykysha](https://github.com/mykysha) — 2026-01-26T10:54:55Z

No issues after 117 local runs on the current iteration

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-26T11:06:38Z

/lgtm 
/approve
/cherrypick release-0.15

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-01-26T11:06:42Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.15` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8745#issuecomment-3799022213):

>/lgtm 
>/approve
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-26T11:06:47Z

LGTM label has been added.  <details>Git tree hash: 33bb2141ffd20d3d13afcc4cdb2e8c9c7e44a900</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-26T11:06:49Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8745#issuecomment-3799022213" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8745#" title="Author self-approved">mykysha</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-01-26T11:42:00Z

@mimowo: new pull request created: #8783

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8745#issuecomment-3799022213):

>/lgtm 
>/approve
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-22T15:04:55Z

- **test/integration/multikueue/setup_test.go:779** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-22T15:04:55Z
```diff
@@ -762,55 +763,443 @@ var _ = ginkgo.Describe("MultiKueue", ginkgo.Label("area:multikueue", "feature:m
 })
 
 var _ = ginkgo.Describe("MultiKueue with ClusterProfile", ginkgo.Label("area:multikueue", "feature:multikueue"), ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
-	ginkgo.BeforeAll(func() {
-		// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
-		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.MultiKueueClusterProfile, true)
-		managerTestCluster.fwk.StartManager(managerTestCluster.ctx, managerTestCluster.cfg, func(ctx context.Context, mgr manager.Manager) {
-			managerAndMultiKueueSetup(ctx, mgr, 2*time.Second, defaultEnabledIntegrations, config.MultiKueueDispatcherModeAllAtOnce)
+	ginkgo.Context("Feature gate enabled", ginkgo.Ordered, func() {
+		ginkgo.BeforeAll(func() {
+			// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
+			gomega.Expect(features.SetEnable(features.MultiKueueClusterProfile, true)).To(gomega.Succeed())
+
+			managerTestCluster.fwk.StartManager(managerTestCluster.ctx, managerTestCluster.cfg, func(ctx context.Context, mgr manager.Manager) {
+				managerAndMultiKueueSetup(ctx, mgr, 2*time.Second, defaultEnabledIntegrations, config.MultiKueueDispatcherModeAllAtOnce)
+			})
+		})
+		ginkgo.AfterAll(func() {
+			managerTestCluster.fwk.StopManager(managerTestCluster.ctx)
+
+			// Restore default to avoid leaking the setting to other specs/packages.
+			gomega.Expect(features.SetEnable(features.MultiKueueClusterProfile, false)).To(gomega.Succeed())
```

  ```suggestion
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-22T15:05:41Z

- **test/integration/multikueue/setup_test.go:1147** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-22T15:05:42Z
```diff
@@ -762,55 +763,443 @@ var _ = ginkgo.Describe("MultiKueue", ginkgo.Label("area:multikueue", "feature:m
 })
 
 var _ = ginkgo.Describe("MultiKueue with ClusterProfile", ginkgo.Label("area:multikueue", "feature:multikueue"), ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
-	ginkgo.BeforeAll(func() {
-		// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
-		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.MultiKueueClusterProfile, true)
-		managerTestCluster.fwk.StartManager(managerTestCluster.ctx, managerTestCluster.cfg, func(ctx context.Context, mgr manager.Manager) {
-			managerAndMultiKueueSetup(ctx, mgr, 2*time.Second, defaultEnabledIntegrations, config.MultiKueueDispatcherModeAllAtOnce)
+	ginkgo.Context("Feature gate enabled", ginkgo.Ordered, func() {
+		ginkgo.BeforeAll(func() {
+			// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
+			gomega.Expect(features.SetEnable(features.MultiKueueClusterProfile, true)).To(gomega.Succeed())
+
+			managerTestCluster.fwk.StartManager(managerTestCluster.ctx, managerTestCluster.cfg, func(ctx context.Context, mgr manager.Manager) {
+				managerAndMultiKueueSetup(ctx, mgr, 2*time.Second, defaultEnabledIntegrations, config.MultiKueueDispatcherModeAllAtOnce)
+			})
+		})
+		ginkgo.AfterAll(func() {
+			managerTestCluster.fwk.StopManager(managerTestCluster.ctx)
+
+			// Restore default to avoid leaking the setting to other specs/packages.
+			gomega.Expect(features.SetEnable(features.MultiKueueClusterProfile, false)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should report no cluster providers configured", func() {
+			var workerCluster3 *kueue.MultiKueueCluster
+			ginkgo.By("Create a MultiKueueCluster with ClusterProfile", func() {
+				workerCluster3 = utiltestingapi.MakeMultiKueueCluster("worker3").ClusterProfile("test-profile").Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, workerCluster3)).To(gomega.Succeed())
+			})
+
+			workerCluster3Key := client.ObjectKeyFromObject(workerCluster3)
+			ginkgo.By("Verify status conditions of the MultiKueueCluster", func() {
+				mkc := &kueue.MultiKueueCluster{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, workerCluster3Key, mkc)).To(gomega.Succeed())
+					activeCondition := apimeta.FindStatusCondition(mkc.Status.Conditions, kueue.MultiKueueClusterActive)
+					g.Expect(activeCondition).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.MultiKueueClusterActive,
+						Status:  metav1.ConditionFalse,
+						Reason:  "BadClusterProfile",
+						Message: "load client config failed: ClusterProfile.multicluster.x-k8s.io \"test-profile\" not found",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Create ClusterProfile and trigger MultiKueueCluster reconciliation", func() {
+				clusterProfile := utiltestingapi.MakeClusterProfile("test-profile", config.DefaultNamespace).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, clusterProfile)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify status of the MultiKueueCluster", func() {
+				mkc := &kueue.MultiKueueCluster{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, workerCluster3Key, mkc)).To(gomega.Succeed())
+					g.Expect(mkc.Status.Conditions).To(gomega.ContainElement(gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.MultiKueueClusterActive,
+						Status:  metav1.ConditionFalse,
+						Reason:  "BadClusterProfile",
+						Message: "load client config failed: no credentials provider configured",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration)))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("Should reconcile a MultiKueueCluster when the referenced ClusterProfile is deleted and re-created", func() {
+			cpName := "cp-delete-recreate"
+			clusterName := "cp-worker-delete-recreate"
+
+			ginkgo.By("Create a MultiKueueCluster referencing a missing ClusterProfile", func() {
+				mkc := utiltestingapi.MakeMultiKueueCluster(clusterName).ClusterProfile(cpName).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, mkc)).To(gomega.Succeed())
+			})
+			clusterKey := client.ObjectKey{Name: clusterName}
+			ginkgo.DeferCleanup(func() {
+				util.ExpectObjectToBeDeletedWithTimeout(managerTestCluster.ctx, managerTestCluster.client, &kueue.MultiKueueCluster{
+					ObjectMeta: metav1.ObjectMeta{Name: clusterName},
+				}, true, util.Timeout)
+			})
+
+			ginkgo.By("Wait for status to indicate ClusterProfile is missing", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					mkc := &kueue.MultiKueueCluster{}
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, clusterKey, mkc)).To(gomega.Succeed())
+
+					active := apimeta.FindStatusCondition(mkc.Status.Conditions, kueue.MultiKueueClusterActive)
+					g.Expect(active).NotTo(gomega.BeNil())
+					g.Expect(active.Status).To(gomega.Equal(metav1.ConditionFalse))
+					g.Expect(active.Reason).To(gomega.Equal("BadClusterProfile"))
+					g.Expect(active.Message).To(gomega.ContainSubstring("not found"))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Create the referenced ClusterProfile", func() {
+				cp := utiltestingapi.MakeClusterProfile(cpName, managersConfigNamespace.Name).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, cp)).To(gomega.Succeed())
+				ginkgo.DeferCleanup(func() {
+					util.ExpectObjectToBeDeletedWithTimeout(managerTestCluster.ctx, managerTestCluster.client, &inventoryv1alpha1.ClusterProfile{
+						ObjectMeta: metav1.ObjectMeta{Namespace: managersConfigNamespace.Name, Name: cpName},
+					}, true, util.Timeout)
+				})
+			})
+
+			ginkgo.By("Wait for status to indicate 'no credentials provider configured'", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					mkc := &kueue.MultiKueueCluster{}
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, clusterKey, mkc)).To(gomega.Succeed())
+
+					active := apimeta.FindStatusCondition(mkc.Status.Conditions, kueue.MultiKueueClusterActive)
+					g.Expect(active).NotTo(gomega.BeNil())
+					g.Expect(active.Status).To(gomega.Equal(metav1.ConditionFalse))
+					g.Expect(active.Reason).To(gomega.Equal("BadClusterProfile"))
+					g.Expect(active.Message).To(gomega.ContainSubstring("no credentials provider configured"))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Delete the referenced ClusterProfile", func() {
+				util.ExpectObjectToBeDeletedWithTimeout(managerTestCluster.ctx, managerTestCluster.client, &inventoryv1alpha1.ClusterProfile{
+					ObjectMeta: metav1.ObjectMeta{Namespace: managersConfigNamespace.Name, Name: cpName},
+				}, true, util.Timeout)
+			})
+
+			ginkgo.By("Wait for status to go back to 'not found'", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					mkc := &kueue.MultiKueueCluster{}
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, clusterKey, mkc)).To(gomega.Succeed())
+
+					active := apimeta.FindStatusCondition(mkc.Status.Conditions, kueue.MultiKueueClusterActive)
+					g.Expect(active).NotTo(gomega.BeNil())
+					g.Expect(active.Status).To(gomega.Equal(metav1.ConditionFalse))
+					g.Expect(active.Reason).To(gomega.Equal("BadClusterProfile"))
+					g.Expect(active.Message).To(gomega.ContainSubstring("not found"))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Re-create the referenced ClusterProfile", func() {
+				cp := utiltestingapi.MakeClusterProfile(cpName, managersConfigNamespace.Name).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, cp)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("Wait for status again to indicate 'no credentials provider configured'", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					mkc := &kueue.MultiKueueCluster{}
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, clusterKey, mkc)).To(gomega.Succeed())
+
+					active := apimeta.FindStatusCondition(mkc.Status.Conditions, kueue.MultiKueueClusterActive)
+					g.Expect(active).NotTo(gomega.BeNil())
+					g.Expect(active.Status).To(gomega.Equal(metav1.ConditionFalse))
+					g.Expect(active.Reason).To(gomega.Equal("BadClusterProfile"))
+					g.Expect(active.Message).To(gomega.ContainSubstring("no credentials provider configured"))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("Should reconcile all MultiKueueClusters that reference the same ClusterProfile", func() {
+			cpName := "cp-shared"
+			clusterNames := []string{"cp-worker-shared-1", "cp-worker-shared-2"}
+
+			ginkgo.By("Create two MultiKueueClusters referencing the same missing ClusterProfile", func() {
+				for _, n := range clusterNames {
+					mkc := utiltestingapi.MakeMultiKueueCluster(n).ClusterProfile(cpName).Obj()
+					gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, mkc)).To(gomega.Succeed())
+
+					clusterName := n
+					ginkgo.DeferCleanup(func() {
+						util.ExpectObjectToBeDeletedWithTimeout(managerTestCluster.ctx, managerTestCluster.client, &kueue.MultiKueueCluster{
+							ObjectMeta: metav1.ObjectMeta{Name: clusterName},
+						}, true, util.Timeout)
+					})
+				}
+			})
+
+			ginkgo.By("Both clusters should report ClusterProfile missing", func() {
+				for _, n := range clusterNames {
+					clusterKey := client.ObjectKey{Name: n}
+					gomega.Eventually(func(g gomega.Gomega) {
+						mkc := &kueue.MultiKueueCluster{}
+						g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, clusterKey, mkc)).To(gomega.Succeed())
+
+						active := apimeta.FindStatusCondition(mkc.Status.Conditions, kueue.MultiKueueClusterActive)
+						g.Expect(active).NotTo(gomega.BeNil())
+						g.Expect(active.Status).To(gomega.Equal(metav1.ConditionFalse))
+						g.Expect(active.Reason).To(gomega.Equal("BadClusterProfile"))
+						g.Expect(active.Message).To(gomega.ContainSubstring("not found"))
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Create the shared ClusterProfile", func() {
+				cp := utiltestingapi.MakeClusterProfile(cpName, managersConfigNamespace.Name).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, cp)).To(gomega.Succeed())
+
+				ginkgo.DeferCleanup(func() {
+					util.ExpectObjectToBeDeletedWithTimeout(managerTestCluster.ctx, managerTestCluster.client, &inventoryv1alpha1.ClusterProfile{
+						ObjectMeta: metav1.ObjectMeta{Namespace: managersConfigNamespace.Name, Name: cpName},
+					}, true, util.Timeout)
+				})
+			})
+
+			ginkgo.By("Both clusters should move to 'no credentials provider configured'", func() {
+				for _, n := range clusterNames {
+					clusterKey := client.ObjectKey{Name: n}
+					gomega.Eventually(func(g gomega.Gomega) {
+						mkc := &kueue.MultiKueueCluster{}
+						g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, clusterKey, mkc)).To(gomega.Succeed())
+
+						active := apimeta.FindStatusCondition(mkc.Status.Conditions, kueue.MultiKueueClusterActive)
+						g.Expect(active).NotTo(gomega.BeNil())
+						g.Expect(active.Status).To(gomega.Equal(metav1.ConditionFalse))
+						g.Expect(active.Reason).To(gomega.Equal("BadClusterProfile"))
+						g.Expect(active.Message).To(gomega.ContainSubstring("no credentials provider configured"))
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				}
+			})
+		})
+
+		ginkgo.It("Should require ClusterProfile to exist in the configured namespace", func() {
+			cpName := "cp-wrong-namespace"
+			clusterName := "cp-worker-wrong-namespace"
+
+			otherNS := utiltesting.MakeNamespace("cp-other-ns")
+			ginkgo.By("Create a non-system namespace", func() {
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, otherNS)).To(gomega.Succeed())
+				ginkgo.DeferCleanup(func() {
+					gomega.Expect(util.DeleteNamespace(managerTestCluster.ctx, managerTestCluster.client, otherNS)).To(gomega.Succeed())
+				})
+			})
+
+			ginkgo.By("Create MultiKueueCluster referencing ClusterProfile", func() {
+				mkc := utiltestingapi.MakeMultiKueueCluster(clusterName).ClusterProfile(cpName).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, mkc)).To(gomega.Succeed())
+				ginkgo.DeferCleanup(func() {
+					util.ExpectObjectToBeDeletedWithTimeout(managerTestCluster.ctx, managerTestCluster.client, &kueue.MultiKueueCluster{
+						ObjectMeta: metav1.ObjectMeta{Name: clusterName},
+					}, true, util.Timeout)
+				})
+			})
+
+			clusterKey := client.ObjectKey{Name: clusterName}
+			ginkgo.By("Wait for status to indicate ClusterProfile missing", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					mkc := &kueue.MultiKueueCluster{}
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, clusterKey, mkc)).To(gomega.Succeed())
+
+					active := apimeta.FindStatusCondition(mkc.Status.Conditions, kueue.MultiKueueClusterActive)
+					g.Expect(active).NotTo(gomega.BeNil())
+					g.Expect(active.Reason).To(gomega.Equal("BadClusterProfile"))
+					g.Expect(active.Message).To(gomega.ContainSubstring("not found"))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Create ClusterProfile with the same name in a different namespace", func() {
+				cpOther := utiltestingapi.MakeClusterProfile(cpName, otherNS.Name).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, cpOther)).To(gomega.Succeed())
+				ginkgo.DeferCleanup(func() {
+					util.ExpectObjectToBeDeletedWithTimeout(managerTestCluster.ctx, managerTestCluster.client, &inventoryv1alpha1.ClusterProfile{
+						ObjectMeta: metav1.ObjectMeta{Namespace: otherNS.Name, Name: cpName},
+					}, true, util.Timeout)
+				})
+			})
+
+			ginkgo.By("Status should remain as 'not found'", func() {
+				gomega.Consistently(func(g gomega.Gomega) {
+					mkc := &kueue.MultiKueueCluster{}
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, clusterKey, mkc)).To(gomega.Succeed())
+
+					active := apimeta.FindStatusCondition(mkc.Status.Conditions, kueue.MultiKueueClusterActive)
+					g.Expect(active).NotTo(gomega.BeNil())
+					g.Expect(active.Reason).To(gomega.Equal("BadClusterProfile"))
+					g.Expect(active.Message).To(gomega.ContainSubstring("not found"))
+				}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Create ClusterProfile in the configured namespace", func() {
+				cp := utiltestingapi.MakeClusterProfile(cpName, managersConfigNamespace.Name).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, cp)).To(gomega.Succeed())
+				ginkgo.DeferCleanup(func() {
+					util.ExpectObjectToBeDeletedWithTimeout(managerTestCluster.ctx, managerTestCluster.client, &inventoryv1alpha1.ClusterProfile{
+						ObjectMeta: metav1.ObjectMeta{Namespace: managersConfigNamespace.Name, Name: cpName},
+					}, true, util.Timeout)
+				})
+			})
+
+			ginkgo.By("Status should move to 'no credentials provider configured'", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					mkc := &kueue.MultiKueueCluster{}
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, clusterKey, mkc)).To(gomega.Succeed())
+
+					active := apimeta.FindStatusCondition(mkc.Status.Conditions, kueue.MultiKueueClusterActive)
+					g.Expect(active).NotTo(gomega.BeNil())
+					g.Expect(active.Reason).To(gomega.Equal("BadClusterProfile"))
+					g.Expect(active.Message).To(gomega.ContainSubstring("no credentials provider configured"))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("Should update status when switching ClusterProfileRef to another name", func() {
+			clusterName := "cp-worker-switch"
+			cp1 := "cp-switch-1"
+			cp2 := "cp-switch-2"
+
+			ginkgo.By("Create a MultiKueueCluster referencing cp1 (initially missing)", func() {
+				mkc := utiltestingapi.MakeMultiKueueCluster(clusterName).ClusterProfile(cp1).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, mkc)).To(gomega.Succeed())
+				ginkgo.DeferCleanup(func() {
+					util.ExpectObjectToBeDeletedWithTimeout(managerTestCluster.ctx, managerTestCluster.client, &kueue.MultiKueueCluster{
+						ObjectMeta: metav1.ObjectMeta{Name: clusterName},
+					}, true, util.Timeout)
+				})
+			})
+			clusterKey := client.ObjectKey{Name: clusterName}
+
+			ginkgo.By("Wait for status to indicate cp1 missing", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					mkc := &kueue.MultiKueueCluster{}
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, clusterKey, mkc)).To(gomega.Succeed())
+
+					active := apimeta.FindStatusCondition(mkc.Status.Conditions, kueue.MultiKueueClusterActive)
+					g.Expect(active).NotTo(gomega.BeNil())
+					g.Expect(active.Reason).To(gomega.Equal("BadClusterProfile"))
+					g.Expect(active.Message).To(gomega.ContainSubstring("not found"))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Create cp1 and wait for 'no credentials provider configured'", func() {
+				cpObj := utiltestingapi.MakeClusterProfile(cp1, managersConfigNamespace.Name).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, cpObj)).To(gomega.Succeed())
+				ginkgo.DeferCleanup(func() {
+					util.ExpectObjectToBeDeletedWithTimeout(managerTestCluster.ctx, managerTestCluster.client, &inventoryv1alpha1.ClusterProfile{
+						ObjectMeta: metav1.ObjectMeta{Namespace: managersConfigNamespace.Name, Name: cp1},
+					}, true, util.Timeout)
+				})
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					mkc := &kueue.MultiKueueCluster{}
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, clusterKey, mkc)).To(gomega.Succeed())
+
+					active := apimeta.FindStatusCondition(mkc.Status.Conditions, kueue.MultiKueueClusterActive)
+					g.Expect(active).NotTo(gomega.BeNil())
+					g.Expect(active.Reason).To(gomega.Equal("BadClusterProfile"))
+					g.Expect(active.Message).To(gomega.ContainSubstring("no credentials provider configured"))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Switch cluster to reference cp2 (missing) and wait for 'not found'", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					mkc := &kueue.MultiKueueCluster{}
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, clusterKey, mkc)).To(gomega.Succeed())
+					mkc.Spec.ClusterSource.KubeConfig = nil
+					mkc.Spec.ClusterSource.ClusterProfileRef = &kueue.ClusterProfileReference{Name: cp2}
+					g.Expect(managerTestCluster.client.Update(managerTestCluster.ctx, mkc)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					mkc := &kueue.MultiKueueCluster{}
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, clusterKey, mkc)).To(gomega.Succeed())
+
+					active := apimeta.FindStatusCondition(mkc.Status.Conditions, kueue.MultiKueueClusterActive)
+					g.Expect(active).NotTo(gomega.BeNil())
+					g.Expect(active.Reason).To(gomega.Equal("BadClusterProfile"))
+					g.Expect(active.Message).To(gomega.ContainSubstring("not found"))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Create cp2 and wait for 'no credentials provider configured'", func() {
+				cpObj := utiltestingapi.MakeClusterProfile(cp2, managersConfigNamespace.Name).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, cpObj)).To(gomega.Succeed())
+				ginkgo.DeferCleanup(func() {
+					util.ExpectObjectToBeDeletedWithTimeout(managerTestCluster.ctx, managerTestCluster.client, &inventoryv1alpha1.ClusterProfile{
+						ObjectMeta: metav1.ObjectMeta{Namespace: managersConfigNamespace.Name, Name: cp2},
+					}, true, util.Timeout)
+				})
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					mkc := &kueue.MultiKueueCluster{}
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, clusterKey, mkc)).To(gomega.Succeed())
+
+					active := apimeta.FindStatusCondition(mkc.Status.Conditions, kueue.MultiKueueClusterActive)
+					g.Expect(active).NotTo(gomega.BeNil())
+					g.Expect(active.Reason).To(gomega.Equal("BadClusterProfile"))
+					g.Expect(active.Message).To(gomega.ContainSubstring("no credentials provider configured"))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
 		})
 	})
-	ginkgo.AfterAll(func() {
-		managerTestCluster.fwk.StopManager(managerTestCluster.ctx)
-	})
 
-	ginkgo.It("Should report no cluster providers configured", func() {
-		var workerCluster3 *kueue.MultiKueueCluster
-		ginkgo.By("Create a MultiKueueCluster with ClusterProfile", func() {
-			workerCluster3 = utiltestingapi.MakeMultiKueueCluster("worker3").ClusterProfile("test-profile").Obj()
-			gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, workerCluster3)).To(gomega.Succeed())
-		})
-
-		workerCluster3Key := client.ObjectKeyFromObject(workerCluster3)
-		ginkgo.By("Verify status conditions of the MultiKueueCluster", func() {
-			mkc := &kueue.MultiKueueCluster{}
-			gomega.Eventually(func(g gomega.Gomega) {
-				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, workerCluster3Key, mkc)).To(gomega.Succeed())
-				activeCondition := apimeta.FindStatusCondition(mkc.Status.Conditions, kueue.MultiKueueClusterActive)
-				g.Expect(activeCondition).To(gomega.BeComparableTo(&metav1.Condition{
-					Type:    kueue.MultiKueueClusterActive,
-					Status:  metav1.ConditionFalse,
-					Reason:  "BadClusterProfile",
-					Message: "load client config failed: ClusterProfile.multicluster.x-k8s.io \"test-profile\" not found",
-				}, util.IgnoreConditionTimestampsAndObservedGeneration))
-			}, util.Timeout, util.Interval).Should(gomega.Succeed())
-		})
-
-		ginkgo.By("Create ClusterProfile and trigger MultiKueueCluster reconciliation", func() {
-			clusterProfile := utiltestingapi.MakeClusterProfile("test-profile", config.DefaultNamespace).Obj()
-			gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, clusterProfile)).To(gomega.Succeed())
-		})
-
-		ginkgo.By("Verify status of the MultiKueueCluster", func() {
-			mkc := &kueue.MultiKueueCluster{}
-			gomega.Eventually(func(g gomega.Gomega) {
-				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, workerCluster3Key, mkc)).To(gomega.Succeed())
-				g.Expect(mkc.Status.Conditions).To(gomega.ContainElement(gomega.BeComparableTo(metav1.Condition{
-					Type:    kueue.MultiKueueClusterActive,
-					Status:  metav1.ConditionFalse,
-					Reason:  "BadClusterProfile",
-					Message: "load client config failed: no credentials provider configured",
-				}, util.IgnoreConditionTimestampsAndObservedGeneration)))
-			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	ginkgo.Context("Feature gate disabled", ginkgo.Ordered, func() {
+		ginkgo.BeforeAll(func() {
+			// Gate must be set before StartManager.
+			gomega.Expect(features.SetEnable(features.MultiKueueClusterProfile, false)).To(gomega.Succeed())
```

  ```suggestion
  			features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.MultiKueueClusterProfile, false)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-22T15:11:19Z

- **test/integration/multikueue/setup_test.go:769** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-22T15:11:19Z
```diff
@@ -762,55 +763,443 @@ var _ = ginkgo.Describe("MultiKueue", ginkgo.Label("area:multikueue", "feature:m
 })
 
 var _ = ginkgo.Describe("MultiKueue with ClusterProfile", ginkgo.Label("area:multikueue", "feature:multikueue"), ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
-	ginkgo.BeforeAll(func() {
-		// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
-		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.MultiKueueClusterProfile, true)
-		managerTestCluster.fwk.StartManager(managerTestCluster.ctx, managerTestCluster.cfg, func(ctx context.Context, mgr manager.Manager) {
-			managerAndMultiKueueSetup(ctx, mgr, 2*time.Second, defaultEnabledIntegrations, config.MultiKueueDispatcherModeAllAtOnce)
+	ginkgo.Context("Feature gate enabled", ginkgo.Ordered, func() {
+		ginkgo.BeforeAll(func() {
+			// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
+			gomega.Expect(features.SetEnable(features.MultiKueueClusterProfile, true)).To(gomega.Succeed())
```

  ```suggestion
  			features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.MultiKueueClusterProfile, true)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-22T15:24:09Z

- **test/integration/multikueue/setup_test.go:836** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-22T15:24:09Z
```diff
@@ -762,55 +763,443 @@ var _ = ginkgo.Describe("MultiKueue", ginkgo.Label("area:multikueue", "feature:m
 })
 
 var _ = ginkgo.Describe("MultiKueue with ClusterProfile", ginkgo.Label("area:multikueue", "feature:multikueue"), ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
-	ginkgo.BeforeAll(func() {
-		// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
-		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.MultiKueueClusterProfile, true)
-		managerTestCluster.fwk.StartManager(managerTestCluster.ctx, managerTestCluster.cfg, func(ctx context.Context, mgr manager.Manager) {
-			managerAndMultiKueueSetup(ctx, mgr, 2*time.Second, defaultEnabledIntegrations, config.MultiKueueDispatcherModeAllAtOnce)
+	ginkgo.Context("Feature gate enabled", ginkgo.Ordered, func() {
+		ginkgo.BeforeAll(func() {
+			// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
+			gomega.Expect(features.SetEnable(features.MultiKueueClusterProfile, true)).To(gomega.Succeed())
+
+			managerTestCluster.fwk.StartManager(managerTestCluster.ctx, managerTestCluster.cfg, func(ctx context.Context, mgr manager.Manager) {
+				managerAndMultiKueueSetup(ctx, mgr, 2*time.Second, defaultEnabledIntegrations, config.MultiKueueDispatcherModeAllAtOnce)
+			})
+		})
+		ginkgo.AfterAll(func() {
+			managerTestCluster.fwk.StopManager(managerTestCluster.ctx)
+
+			// Restore default to avoid leaking the setting to other specs/packages.
+			gomega.Expect(features.SetEnable(features.MultiKueueClusterProfile, false)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should report no cluster providers configured", func() {
+			var workerCluster3 *kueue.MultiKueueCluster
+			ginkgo.By("Create a MultiKueueCluster with ClusterProfile", func() {
+				workerCluster3 = utiltestingapi.MakeMultiKueueCluster("worker3").ClusterProfile("test-profile").Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, workerCluster3)).To(gomega.Succeed())
+			})
+
+			workerCluster3Key := client.ObjectKeyFromObject(workerCluster3)
+			ginkgo.By("Verify status conditions of the MultiKueueCluster", func() {
+				mkc := &kueue.MultiKueueCluster{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, workerCluster3Key, mkc)).To(gomega.Succeed())
+					activeCondition := apimeta.FindStatusCondition(mkc.Status.Conditions, kueue.MultiKueueClusterActive)
+					g.Expect(activeCondition).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.MultiKueueClusterActive,
+						Status:  metav1.ConditionFalse,
+						Reason:  "BadClusterProfile",
+						Message: "load client config failed: ClusterProfile.multicluster.x-k8s.io \"test-profile\" not found",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Create ClusterProfile and trigger MultiKueueCluster reconciliation", func() {
+				clusterProfile := utiltestingapi.MakeClusterProfile("test-profile", config.DefaultNamespace).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, clusterProfile)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify status of the MultiKueueCluster", func() {
+				mkc := &kueue.MultiKueueCluster{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, workerCluster3Key, mkc)).To(gomega.Succeed())
+					g.Expect(mkc.Status.Conditions).To(gomega.ContainElement(gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.MultiKueueClusterActive,
+						Status:  metav1.ConditionFalse,
+						Reason:  "BadClusterProfile",
+						Message: "load client config failed: no credentials provider configured",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration)))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("Should reconcile a MultiKueueCluster when the referenced ClusterProfile is deleted and re-created", func() {
+			cpName := "cp-delete-recreate"
+			clusterName := "cp-worker-delete-recreate"
+
+			ginkgo.By("Create a MultiKueueCluster referencing a missing ClusterProfile", func() {
+				mkc := utiltestingapi.MakeMultiKueueCluster(clusterName).ClusterProfile(cpName).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, mkc)).To(gomega.Succeed())
+			})
+			clusterKey := client.ObjectKey{Name: clusterName}
+			ginkgo.DeferCleanup(func() {
+				util.ExpectObjectToBeDeletedWithTimeout(managerTestCluster.ctx, managerTestCluster.client, &kueue.MultiKueueCluster{
+					ObjectMeta: metav1.ObjectMeta{Name: clusterName},
+				}, true, util.Timeout)
+			})
```

  ```suggestion
  				util.ExpectObjectToBeDeleted(managerTestCluster.ctx, managerTestCluster.client, &kueue.MultiKueueCluster{
  					ObjectMeta: metav1.ObjectMeta{Name: clusterName},
  				}, true)
  			})
  ```
  Also other places.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-22T15:25:23Z

- **test/integration/multikueue/setup_test.go:786** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-22T15:25:23Z
```diff
@@ -762,55 +763,443 @@ var _ = ginkgo.Describe("MultiKueue", ginkgo.Label("area:multikueue", "feature:m
 })
 
 var _ = ginkgo.Describe("MultiKueue with ClusterProfile", ginkgo.Label("area:multikueue", "feature:multikueue"), ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
-	ginkgo.BeforeAll(func() {
-		// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
-		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.MultiKueueClusterProfile, true)
-		managerTestCluster.fwk.StartManager(managerTestCluster.ctx, managerTestCluster.cfg, func(ctx context.Context, mgr manager.Manager) {
-			managerAndMultiKueueSetup(ctx, mgr, 2*time.Second, defaultEnabledIntegrations, config.MultiKueueDispatcherModeAllAtOnce)
+	ginkgo.Context("Feature gate enabled", ginkgo.Ordered, func() {
+		ginkgo.BeforeAll(func() {
+			// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
+			gomega.Expect(features.SetEnable(features.MultiKueueClusterProfile, true)).To(gomega.Succeed())
+
+			managerTestCluster.fwk.StartManager(managerTestCluster.ctx, managerTestCluster.cfg, func(ctx context.Context, mgr manager.Manager) {
+				managerAndMultiKueueSetup(ctx, mgr, 2*time.Second, defaultEnabledIntegrations, config.MultiKueueDispatcherModeAllAtOnce)
+			})
+		})
+		ginkgo.AfterAll(func() {
+			managerTestCluster.fwk.StopManager(managerTestCluster.ctx)
+
+			// Restore default to avoid leaking the setting to other specs/packages.
+			gomega.Expect(features.SetEnable(features.MultiKueueClusterProfile, false)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should report no cluster providers configured", func() {
+			var workerCluster3 *kueue.MultiKueueCluster
+			ginkgo.By("Create a MultiKueueCluster with ClusterProfile", func() {
+				workerCluster3 = utiltestingapi.MakeMultiKueueCluster("worker3").ClusterProfile("test-profile").Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, workerCluster3)).To(gomega.Succeed())
```

  I think we missed to cleanup in AfterEach().

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-22T15:26:40Z

- **test/integration/multikueue/setup_test.go:786** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-22T15:26:40Z
```diff
@@ -762,55 +763,443 @@ var _ = ginkgo.Describe("MultiKueue", ginkgo.Label("area:multikueue", "feature:m
 })
 
 var _ = ginkgo.Describe("MultiKueue with ClusterProfile", ginkgo.Label("area:multikueue", "feature:multikueue"), ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
-	ginkgo.BeforeAll(func() {
-		// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
-		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.MultiKueueClusterProfile, true)
-		managerTestCluster.fwk.StartManager(managerTestCluster.ctx, managerTestCluster.cfg, func(ctx context.Context, mgr manager.Manager) {
-			managerAndMultiKueueSetup(ctx, mgr, 2*time.Second, defaultEnabledIntegrations, config.MultiKueueDispatcherModeAllAtOnce)
+	ginkgo.Context("Feature gate enabled", ginkgo.Ordered, func() {
+		ginkgo.BeforeAll(func() {
+			// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
+			gomega.Expect(features.SetEnable(features.MultiKueueClusterProfile, true)).To(gomega.Succeed())
+
+			managerTestCluster.fwk.StartManager(managerTestCluster.ctx, managerTestCluster.cfg, func(ctx context.Context, mgr manager.Manager) {
+				managerAndMultiKueueSetup(ctx, mgr, 2*time.Second, defaultEnabledIntegrations, config.MultiKueueDispatcherModeAllAtOnce)
+			})
+		})
+		ginkgo.AfterAll(func() {
+			managerTestCluster.fwk.StopManager(managerTestCluster.ctx)
+
+			// Restore default to avoid leaking the setting to other specs/packages.
+			gomega.Expect(features.SetEnable(features.MultiKueueClusterProfile, false)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should report no cluster providers configured", func() {
+			var workerCluster3 *kueue.MultiKueueCluster
+			ginkgo.By("Create a MultiKueueCluster with ClusterProfile", func() {
+				workerCluster3 = utiltestingapi.MakeMultiKueueCluster("worker3").ClusterProfile("test-profile").Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, workerCluster3)).To(gomega.Succeed())
```

  Please check also other places.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-22T15:28:33Z

- **test/integration/multikueue/setup_test.go:832** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-22T15:28:33Z
```diff
@@ -762,55 +763,443 @@ var _ = ginkgo.Describe("MultiKueue", ginkgo.Label("area:multikueue", "feature:m
 })
 
 var _ = ginkgo.Describe("MultiKueue with ClusterProfile", ginkgo.Label("area:multikueue", "feature:multikueue"), ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
-	ginkgo.BeforeAll(func() {
-		// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
-		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.MultiKueueClusterProfile, true)
-		managerTestCluster.fwk.StartManager(managerTestCluster.ctx, managerTestCluster.cfg, func(ctx context.Context, mgr manager.Manager) {
-			managerAndMultiKueueSetup(ctx, mgr, 2*time.Second, defaultEnabledIntegrations, config.MultiKueueDispatcherModeAllAtOnce)
+	ginkgo.Context("Feature gate enabled", ginkgo.Ordered, func() {
+		ginkgo.BeforeAll(func() {
+			// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
+			gomega.Expect(features.SetEnable(features.MultiKueueClusterProfile, true)).To(gomega.Succeed())
+
+			managerTestCluster.fwk.StartManager(managerTestCluster.ctx, managerTestCluster.cfg, func(ctx context.Context, mgr manager.Manager) {
+				managerAndMultiKueueSetup(ctx, mgr, 2*time.Second, defaultEnabledIntegrations, config.MultiKueueDispatcherModeAllAtOnce)
+			})
+		})
+		ginkgo.AfterAll(func() {
+			managerTestCluster.fwk.StopManager(managerTestCluster.ctx)
+
+			// Restore default to avoid leaking the setting to other specs/packages.
+			gomega.Expect(features.SetEnable(features.MultiKueueClusterProfile, false)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should report no cluster providers configured", func() {
+			var workerCluster3 *kueue.MultiKueueCluster
+			ginkgo.By("Create a MultiKueueCluster with ClusterProfile", func() {
+				workerCluster3 = utiltestingapi.MakeMultiKueueCluster("worker3").ClusterProfile("test-profile").Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, workerCluster3)).To(gomega.Succeed())
+			})
+
+			workerCluster3Key := client.ObjectKeyFromObject(workerCluster3)
+			ginkgo.By("Verify status conditions of the MultiKueueCluster", func() {
+				mkc := &kueue.MultiKueueCluster{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, workerCluster3Key, mkc)).To(gomega.Succeed())
+					activeCondition := apimeta.FindStatusCondition(mkc.Status.Conditions, kueue.MultiKueueClusterActive)
+					g.Expect(activeCondition).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.MultiKueueClusterActive,
+						Status:  metav1.ConditionFalse,
+						Reason:  "BadClusterProfile",
+						Message: "load client config failed: ClusterProfile.multicluster.x-k8s.io \"test-profile\" not found",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Create ClusterProfile and trigger MultiKueueCluster reconciliation", func() {
+				clusterProfile := utiltestingapi.MakeClusterProfile("test-profile", config.DefaultNamespace).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, clusterProfile)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify status of the MultiKueueCluster", func() {
+				mkc := &kueue.MultiKueueCluster{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, workerCluster3Key, mkc)).To(gomega.Succeed())
+					g.Expect(mkc.Status.Conditions).To(gomega.ContainElement(gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.MultiKueueClusterActive,
+						Status:  metav1.ConditionFalse,
+						Reason:  "BadClusterProfile",
+						Message: "load client config failed: no credentials provider configured",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration)))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("Should reconcile a MultiKueueCluster when the referenced ClusterProfile is deleted and re-created", func() {
+			cpName := "cp-delete-recreate"
+			clusterName := "cp-worker-delete-recreate"
+
+			ginkgo.By("Create a MultiKueueCluster referencing a missing ClusterProfile", func() {
+				mkc := utiltestingapi.MakeMultiKueueCluster(clusterName).ClusterProfile(cpName).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, mkc)).To(gomega.Succeed())
+			})
+			clusterKey := client.ObjectKey{Name: clusterName}
+			ginkgo.DeferCleanup(func() {
```

  Please move to AfterEach().

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-22T15:37:30Z

- **test/integration/multikueue/setup_test.go:859** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-22T15:37:30Z
```diff
@@ -762,55 +763,443 @@ var _ = ginkgo.Describe("MultiKueue", ginkgo.Label("area:multikueue", "feature:m
 })
 
 var _ = ginkgo.Describe("MultiKueue with ClusterProfile", ginkgo.Label("area:multikueue", "feature:multikueue"), ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
-	ginkgo.BeforeAll(func() {
-		// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
-		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.MultiKueueClusterProfile, true)
-		managerTestCluster.fwk.StartManager(managerTestCluster.ctx, managerTestCluster.cfg, func(ctx context.Context, mgr manager.Manager) {
-			managerAndMultiKueueSetup(ctx, mgr, 2*time.Second, defaultEnabledIntegrations, config.MultiKueueDispatcherModeAllAtOnce)
+	ginkgo.Context("Feature gate enabled", ginkgo.Ordered, func() {
+		ginkgo.BeforeAll(func() {
+			// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
+			gomega.Expect(features.SetEnable(features.MultiKueueClusterProfile, true)).To(gomega.Succeed())
+
+			managerTestCluster.fwk.StartManager(managerTestCluster.ctx, managerTestCluster.cfg, func(ctx context.Context, mgr manager.Manager) {
+				managerAndMultiKueueSetup(ctx, mgr, 2*time.Second, defaultEnabledIntegrations, config.MultiKueueDispatcherModeAllAtOnce)
+			})
+		})
+		ginkgo.AfterAll(func() {
+			managerTestCluster.fwk.StopManager(managerTestCluster.ctx)
+
+			// Restore default to avoid leaking the setting to other specs/packages.
+			gomega.Expect(features.SetEnable(features.MultiKueueClusterProfile, false)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should report no cluster providers configured", func() {
+			var workerCluster3 *kueue.MultiKueueCluster
+			ginkgo.By("Create a MultiKueueCluster with ClusterProfile", func() {
+				workerCluster3 = utiltestingapi.MakeMultiKueueCluster("worker3").ClusterProfile("test-profile").Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, workerCluster3)).To(gomega.Succeed())
+			})
+
+			workerCluster3Key := client.ObjectKeyFromObject(workerCluster3)
+			ginkgo.By("Verify status conditions of the MultiKueueCluster", func() {
+				mkc := &kueue.MultiKueueCluster{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, workerCluster3Key, mkc)).To(gomega.Succeed())
+					activeCondition := apimeta.FindStatusCondition(mkc.Status.Conditions, kueue.MultiKueueClusterActive)
+					g.Expect(activeCondition).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.MultiKueueClusterActive,
+						Status:  metav1.ConditionFalse,
+						Reason:  "BadClusterProfile",
+						Message: "load client config failed: ClusterProfile.multicluster.x-k8s.io \"test-profile\" not found",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Create ClusterProfile and trigger MultiKueueCluster reconciliation", func() {
+				clusterProfile := utiltestingapi.MakeClusterProfile("test-profile", config.DefaultNamespace).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, clusterProfile)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify status of the MultiKueueCluster", func() {
+				mkc := &kueue.MultiKueueCluster{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, workerCluster3Key, mkc)).To(gomega.Succeed())
+					g.Expect(mkc.Status.Conditions).To(gomega.ContainElement(gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.MultiKueueClusterActive,
+						Status:  metav1.ConditionFalse,
+						Reason:  "BadClusterProfile",
+						Message: "load client config failed: no credentials provider configured",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration)))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("Should reconcile a MultiKueueCluster when the referenced ClusterProfile is deleted and re-created", func() {
+			cpName := "cp-delete-recreate"
+			clusterName := "cp-worker-delete-recreate"
+
+			ginkgo.By("Create a MultiKueueCluster referencing a missing ClusterProfile", func() {
+				mkc := utiltestingapi.MakeMultiKueueCluster(clusterName).ClusterProfile(cpName).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, mkc)).To(gomega.Succeed())
+			})
+			clusterKey := client.ObjectKey{Name: clusterName}
+			ginkgo.DeferCleanup(func() {
+				util.ExpectObjectToBeDeletedWithTimeout(managerTestCluster.ctx, managerTestCluster.client, &kueue.MultiKueueCluster{
+					ObjectMeta: metav1.ObjectMeta{Name: clusterName},
+				}, true, util.Timeout)
+			})
+
+			ginkgo.By("Wait for status to indicate ClusterProfile is missing", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					mkc := &kueue.MultiKueueCluster{}
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, clusterKey, mkc)).To(gomega.Succeed())
+
+					active := apimeta.FindStatusCondition(mkc.Status.Conditions, kueue.MultiKueueClusterActive)
+					g.Expect(active).NotTo(gomega.BeNil())
+					g.Expect(active.Status).To(gomega.Equal(metav1.ConditionFalse))
+					g.Expect(active.Reason).To(gomega.Equal("BadClusterProfile"))
+					g.Expect(active.Message).To(gomega.ContainSubstring("not found"))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Create the referenced ClusterProfile", func() {
+				cp := utiltestingapi.MakeClusterProfile(cpName, managersConfigNamespace.Name).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, cp)).To(gomega.Succeed())
+				ginkgo.DeferCleanup(func() {
+					util.ExpectObjectToBeDeletedWithTimeout(managerTestCluster.ctx, managerTestCluster.client, &inventoryv1alpha1.ClusterProfile{
+						ObjectMeta: metav1.ObjectMeta{Namespace: managersConfigNamespace.Name, Name: cpName},
+					}, true, util.Timeout)
+				})
+			})
```

  ```suggestion
  					util.ExpectObjectToBeDeleted(managerTestCluster.ctx, managerTestCluster.client, cp, true)
  ```
  Not sure I got why not just like this?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-22T15:38:18Z

- **test/integration/multikueue/setup_test.go:862** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-22T15:38:18Z
```diff
@@ -762,55 +763,443 @@ var _ = ginkgo.Describe("MultiKueue", ginkgo.Label("area:multikueue", "feature:m
 })
 
 var _ = ginkgo.Describe("MultiKueue with ClusterProfile", ginkgo.Label("area:multikueue", "feature:multikueue"), ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
-	ginkgo.BeforeAll(func() {
-		// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
-		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.MultiKueueClusterProfile, true)
-		managerTestCluster.fwk.StartManager(managerTestCluster.ctx, managerTestCluster.cfg, func(ctx context.Context, mgr manager.Manager) {
-			managerAndMultiKueueSetup(ctx, mgr, 2*time.Second, defaultEnabledIntegrations, config.MultiKueueDispatcherModeAllAtOnce)
+	ginkgo.Context("Feature gate enabled", ginkgo.Ordered, func() {
+		ginkgo.BeforeAll(func() {
+			// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
+			gomega.Expect(features.SetEnable(features.MultiKueueClusterProfile, true)).To(gomega.Succeed())
+
+			managerTestCluster.fwk.StartManager(managerTestCluster.ctx, managerTestCluster.cfg, func(ctx context.Context, mgr manager.Manager) {
+				managerAndMultiKueueSetup(ctx, mgr, 2*time.Second, defaultEnabledIntegrations, config.MultiKueueDispatcherModeAllAtOnce)
+			})
+		})
+		ginkgo.AfterAll(func() {
+			managerTestCluster.fwk.StopManager(managerTestCluster.ctx)
+
+			// Restore default to avoid leaking the setting to other specs/packages.
+			gomega.Expect(features.SetEnable(features.MultiKueueClusterProfile, false)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should report no cluster providers configured", func() {
+			var workerCluster3 *kueue.MultiKueueCluster
+			ginkgo.By("Create a MultiKueueCluster with ClusterProfile", func() {
+				workerCluster3 = utiltestingapi.MakeMultiKueueCluster("worker3").ClusterProfile("test-profile").Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, workerCluster3)).To(gomega.Succeed())
+			})
+
+			workerCluster3Key := client.ObjectKeyFromObject(workerCluster3)
+			ginkgo.By("Verify status conditions of the MultiKueueCluster", func() {
+				mkc := &kueue.MultiKueueCluster{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, workerCluster3Key, mkc)).To(gomega.Succeed())
+					activeCondition := apimeta.FindStatusCondition(mkc.Status.Conditions, kueue.MultiKueueClusterActive)
+					g.Expect(activeCondition).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.MultiKueueClusterActive,
+						Status:  metav1.ConditionFalse,
+						Reason:  "BadClusterProfile",
+						Message: "load client config failed: ClusterProfile.multicluster.x-k8s.io \"test-profile\" not found",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Create ClusterProfile and trigger MultiKueueCluster reconciliation", func() {
+				clusterProfile := utiltestingapi.MakeClusterProfile("test-profile", config.DefaultNamespace).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, clusterProfile)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify status of the MultiKueueCluster", func() {
+				mkc := &kueue.MultiKueueCluster{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, workerCluster3Key, mkc)).To(gomega.Succeed())
+					g.Expect(mkc.Status.Conditions).To(gomega.ContainElement(gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.MultiKueueClusterActive,
+						Status:  metav1.ConditionFalse,
+						Reason:  "BadClusterProfile",
+						Message: "load client config failed: no credentials provider configured",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration)))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("Should reconcile a MultiKueueCluster when the referenced ClusterProfile is deleted and re-created", func() {
+			cpName := "cp-delete-recreate"
+			clusterName := "cp-worker-delete-recreate"
+
+			ginkgo.By("Create a MultiKueueCluster referencing a missing ClusterProfile", func() {
+				mkc := utiltestingapi.MakeMultiKueueCluster(clusterName).ClusterProfile(cpName).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, mkc)).To(gomega.Succeed())
+			})
+			clusterKey := client.ObjectKey{Name: clusterName}
+			ginkgo.DeferCleanup(func() {
+				util.ExpectObjectToBeDeletedWithTimeout(managerTestCluster.ctx, managerTestCluster.client, &kueue.MultiKueueCluster{
+					ObjectMeta: metav1.ObjectMeta{Name: clusterName},
+				}, true, util.Timeout)
+			})
+
+			ginkgo.By("Wait for status to indicate ClusterProfile is missing", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					mkc := &kueue.MultiKueueCluster{}
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, clusterKey, mkc)).To(gomega.Succeed())
+
+					active := apimeta.FindStatusCondition(mkc.Status.Conditions, kueue.MultiKueueClusterActive)
+					g.Expect(active).NotTo(gomega.BeNil())
+					g.Expect(active.Status).To(gomega.Equal(metav1.ConditionFalse))
+					g.Expect(active.Reason).To(gomega.Equal("BadClusterProfile"))
+					g.Expect(active.Message).To(gomega.ContainSubstring("not found"))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Create the referenced ClusterProfile", func() {
+				cp := utiltestingapi.MakeClusterProfile(cpName, managersConfigNamespace.Name).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, cp)).To(gomega.Succeed())
+				ginkgo.DeferCleanup(func() {
+					util.ExpectObjectToBeDeletedWithTimeout(managerTestCluster.ctx, managerTestCluster.client, &inventoryv1alpha1.ClusterProfile{
+						ObjectMeta: metav1.ObjectMeta{Namespace: managersConfigNamespace.Name, Name: cpName},
+					}, true, util.Timeout)
+				})
+			})
+
+			ginkgo.By("Wait for status to indicate 'no credentials provider configured'", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					mkc := &kueue.MultiKueueCluster{}
```

  Please make this variable globally and delete in AfterEach().

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-01-26T09:12:44Z

- **test/integration/multikueue/setup_test.go:836** by [@mykysha](https://github.com/mykysha) — 2026-01-26T09:12:44Z
```diff
@@ -762,55 +763,443 @@ var _ = ginkgo.Describe("MultiKueue", ginkgo.Label("area:multikueue", "feature:m
 })
 
 var _ = ginkgo.Describe("MultiKueue with ClusterProfile", ginkgo.Label("area:multikueue", "feature:multikueue"), ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
-	ginkgo.BeforeAll(func() {
-		// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
-		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.MultiKueueClusterProfile, true)
-		managerTestCluster.fwk.StartManager(managerTestCluster.ctx, managerTestCluster.cfg, func(ctx context.Context, mgr manager.Manager) {
-			managerAndMultiKueueSetup(ctx, mgr, 2*time.Second, defaultEnabledIntegrations, config.MultiKueueDispatcherModeAllAtOnce)
+	ginkgo.Context("Feature gate enabled", ginkgo.Ordered, func() {
+		ginkgo.BeforeAll(func() {
+			// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
+			gomega.Expect(features.SetEnable(features.MultiKueueClusterProfile, true)).To(gomega.Succeed())
+
+			managerTestCluster.fwk.StartManager(managerTestCluster.ctx, managerTestCluster.cfg, func(ctx context.Context, mgr manager.Manager) {
+				managerAndMultiKueueSetup(ctx, mgr, 2*time.Second, defaultEnabledIntegrations, config.MultiKueueDispatcherModeAllAtOnce)
+			})
+		})
+		ginkgo.AfterAll(func() {
+			managerTestCluster.fwk.StopManager(managerTestCluster.ctx)
+
+			// Restore default to avoid leaking the setting to other specs/packages.
+			gomega.Expect(features.SetEnable(features.MultiKueueClusterProfile, false)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should report no cluster providers configured", func() {
+			var workerCluster3 *kueue.MultiKueueCluster
+			ginkgo.By("Create a MultiKueueCluster with ClusterProfile", func() {
+				workerCluster3 = utiltestingapi.MakeMultiKueueCluster("worker3").ClusterProfile("test-profile").Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, workerCluster3)).To(gomega.Succeed())
+			})
+
+			workerCluster3Key := client.ObjectKeyFromObject(workerCluster3)
+			ginkgo.By("Verify status conditions of the MultiKueueCluster", func() {
+				mkc := &kueue.MultiKueueCluster{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, workerCluster3Key, mkc)).To(gomega.Succeed())
+					activeCondition := apimeta.FindStatusCondition(mkc.Status.Conditions, kueue.MultiKueueClusterActive)
+					g.Expect(activeCondition).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.MultiKueueClusterActive,
+						Status:  metav1.ConditionFalse,
+						Reason:  "BadClusterProfile",
+						Message: "load client config failed: ClusterProfile.multicluster.x-k8s.io \"test-profile\" not found",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Create ClusterProfile and trigger MultiKueueCluster reconciliation", func() {
+				clusterProfile := utiltestingapi.MakeClusterProfile("test-profile", config.DefaultNamespace).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, clusterProfile)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify status of the MultiKueueCluster", func() {
+				mkc := &kueue.MultiKueueCluster{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, workerCluster3Key, mkc)).To(gomega.Succeed())
+					g.Expect(mkc.Status.Conditions).To(gomega.ContainElement(gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.MultiKueueClusterActive,
+						Status:  metav1.ConditionFalse,
+						Reason:  "BadClusterProfile",
+						Message: "load client config failed: no credentials provider configured",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration)))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("Should reconcile a MultiKueueCluster when the referenced ClusterProfile is deleted and re-created", func() {
+			cpName := "cp-delete-recreate"
+			clusterName := "cp-worker-delete-recreate"
+
+			ginkgo.By("Create a MultiKueueCluster referencing a missing ClusterProfile", func() {
+				mkc := utiltestingapi.MakeMultiKueueCluster(clusterName).ClusterProfile(cpName).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, mkc)).To(gomega.Succeed())
+			})
+			clusterKey := client.ObjectKey{Name: clusterName}
+			ginkgo.DeferCleanup(func() {
+				util.ExpectObjectToBeDeletedWithTimeout(managerTestCluster.ctx, managerTestCluster.client, &kueue.MultiKueueCluster{
+					ObjectMeta: metav1.ObjectMeta{Name: clusterName},
+				}, true, util.Timeout)
+			})
```

  Fixed

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-01-26T09:12:57Z

- **test/integration/multikueue/setup_test.go:832** by [@mykysha](https://github.com/mykysha) — 2026-01-26T09:12:57Z
```diff
@@ -762,55 +763,443 @@ var _ = ginkgo.Describe("MultiKueue", ginkgo.Label("area:multikueue", "feature:m
 })
 
 var _ = ginkgo.Describe("MultiKueue with ClusterProfile", ginkgo.Label("area:multikueue", "feature:multikueue"), ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
-	ginkgo.BeforeAll(func() {
-		// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
-		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.MultiKueueClusterProfile, true)
-		managerTestCluster.fwk.StartManager(managerTestCluster.ctx, managerTestCluster.cfg, func(ctx context.Context, mgr manager.Manager) {
-			managerAndMultiKueueSetup(ctx, mgr, 2*time.Second, defaultEnabledIntegrations, config.MultiKueueDispatcherModeAllAtOnce)
+	ginkgo.Context("Feature gate enabled", ginkgo.Ordered, func() {
+		ginkgo.BeforeAll(func() {
+			// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
+			gomega.Expect(features.SetEnable(features.MultiKueueClusterProfile, true)).To(gomega.Succeed())
+
+			managerTestCluster.fwk.StartManager(managerTestCluster.ctx, managerTestCluster.cfg, func(ctx context.Context, mgr manager.Manager) {
+				managerAndMultiKueueSetup(ctx, mgr, 2*time.Second, defaultEnabledIntegrations, config.MultiKueueDispatcherModeAllAtOnce)
+			})
+		})
+		ginkgo.AfterAll(func() {
+			managerTestCluster.fwk.StopManager(managerTestCluster.ctx)
+
+			// Restore default to avoid leaking the setting to other specs/packages.
+			gomega.Expect(features.SetEnable(features.MultiKueueClusterProfile, false)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should report no cluster providers configured", func() {
+			var workerCluster3 *kueue.MultiKueueCluster
+			ginkgo.By("Create a MultiKueueCluster with ClusterProfile", func() {
+				workerCluster3 = utiltestingapi.MakeMultiKueueCluster("worker3").ClusterProfile("test-profile").Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, workerCluster3)).To(gomega.Succeed())
+			})
+
+			workerCluster3Key := client.ObjectKeyFromObject(workerCluster3)
+			ginkgo.By("Verify status conditions of the MultiKueueCluster", func() {
+				mkc := &kueue.MultiKueueCluster{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, workerCluster3Key, mkc)).To(gomega.Succeed())
+					activeCondition := apimeta.FindStatusCondition(mkc.Status.Conditions, kueue.MultiKueueClusterActive)
+					g.Expect(activeCondition).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.MultiKueueClusterActive,
+						Status:  metav1.ConditionFalse,
+						Reason:  "BadClusterProfile",
+						Message: "load client config failed: ClusterProfile.multicluster.x-k8s.io \"test-profile\" not found",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Create ClusterProfile and trigger MultiKueueCluster reconciliation", func() {
+				clusterProfile := utiltestingapi.MakeClusterProfile("test-profile", config.DefaultNamespace).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, clusterProfile)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify status of the MultiKueueCluster", func() {
+				mkc := &kueue.MultiKueueCluster{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, workerCluster3Key, mkc)).To(gomega.Succeed())
+					g.Expect(mkc.Status.Conditions).To(gomega.ContainElement(gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.MultiKueueClusterActive,
+						Status:  metav1.ConditionFalse,
+						Reason:  "BadClusterProfile",
+						Message: "load client config failed: no credentials provider configured",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration)))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("Should reconcile a MultiKueueCluster when the referenced ClusterProfile is deleted and re-created", func() {
+			cpName := "cp-delete-recreate"
+			clusterName := "cp-worker-delete-recreate"
+
+			ginkgo.By("Create a MultiKueueCluster referencing a missing ClusterProfile", func() {
+				mkc := utiltestingapi.MakeMultiKueueCluster(clusterName).ClusterProfile(cpName).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, mkc)).To(gomega.Succeed())
+			})
+			clusterKey := client.ObjectKey{Name: clusterName}
+			ginkgo.DeferCleanup(func() {
```

  Moved cleanups

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-01-26T09:13:45Z

- **test/integration/multikueue/setup_test.go:859** by [@mykysha](https://github.com/mykysha) — 2026-01-26T09:13:45Z
```diff
@@ -762,55 +763,443 @@ var _ = ginkgo.Describe("MultiKueue", ginkgo.Label("area:multikueue", "feature:m
 })
 
 var _ = ginkgo.Describe("MultiKueue with ClusterProfile", ginkgo.Label("area:multikueue", "feature:multikueue"), ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
-	ginkgo.BeforeAll(func() {
-		// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
-		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.MultiKueueClusterProfile, true)
-		managerTestCluster.fwk.StartManager(managerTestCluster.ctx, managerTestCluster.cfg, func(ctx context.Context, mgr manager.Manager) {
-			managerAndMultiKueueSetup(ctx, mgr, 2*time.Second, defaultEnabledIntegrations, config.MultiKueueDispatcherModeAllAtOnce)
+	ginkgo.Context("Feature gate enabled", ginkgo.Ordered, func() {
+		ginkgo.BeforeAll(func() {
+			// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
+			gomega.Expect(features.SetEnable(features.MultiKueueClusterProfile, true)).To(gomega.Succeed())
+
+			managerTestCluster.fwk.StartManager(managerTestCluster.ctx, managerTestCluster.cfg, func(ctx context.Context, mgr manager.Manager) {
+				managerAndMultiKueueSetup(ctx, mgr, 2*time.Second, defaultEnabledIntegrations, config.MultiKueueDispatcherModeAllAtOnce)
+			})
+		})
+		ginkgo.AfterAll(func() {
+			managerTestCluster.fwk.StopManager(managerTestCluster.ctx)
+
+			// Restore default to avoid leaking the setting to other specs/packages.
+			gomega.Expect(features.SetEnable(features.MultiKueueClusterProfile, false)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should report no cluster providers configured", func() {
+			var workerCluster3 *kueue.MultiKueueCluster
+			ginkgo.By("Create a MultiKueueCluster with ClusterProfile", func() {
+				workerCluster3 = utiltestingapi.MakeMultiKueueCluster("worker3").ClusterProfile("test-profile").Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, workerCluster3)).To(gomega.Succeed())
+			})
+
+			workerCluster3Key := client.ObjectKeyFromObject(workerCluster3)
+			ginkgo.By("Verify status conditions of the MultiKueueCluster", func() {
+				mkc := &kueue.MultiKueueCluster{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, workerCluster3Key, mkc)).To(gomega.Succeed())
+					activeCondition := apimeta.FindStatusCondition(mkc.Status.Conditions, kueue.MultiKueueClusterActive)
+					g.Expect(activeCondition).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.MultiKueueClusterActive,
+						Status:  metav1.ConditionFalse,
+						Reason:  "BadClusterProfile",
+						Message: "load client config failed: ClusterProfile.multicluster.x-k8s.io \"test-profile\" not found",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Create ClusterProfile and trigger MultiKueueCluster reconciliation", func() {
+				clusterProfile := utiltestingapi.MakeClusterProfile("test-profile", config.DefaultNamespace).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, clusterProfile)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify status of the MultiKueueCluster", func() {
+				mkc := &kueue.MultiKueueCluster{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, workerCluster3Key, mkc)).To(gomega.Succeed())
+					g.Expect(mkc.Status.Conditions).To(gomega.ContainElement(gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.MultiKueueClusterActive,
+						Status:  metav1.ConditionFalse,
+						Reason:  "BadClusterProfile",
+						Message: "load client config failed: no credentials provider configured",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration)))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("Should reconcile a MultiKueueCluster when the referenced ClusterProfile is deleted and re-created", func() {
+			cpName := "cp-delete-recreate"
+			clusterName := "cp-worker-delete-recreate"
+
+			ginkgo.By("Create a MultiKueueCluster referencing a missing ClusterProfile", func() {
+				mkc := utiltestingapi.MakeMultiKueueCluster(clusterName).ClusterProfile(cpName).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, mkc)).To(gomega.Succeed())
+			})
+			clusterKey := client.ObjectKey{Name: clusterName}
+			ginkgo.DeferCleanup(func() {
+				util.ExpectObjectToBeDeletedWithTimeout(managerTestCluster.ctx, managerTestCluster.client, &kueue.MultiKueueCluster{
+					ObjectMeta: metav1.ObjectMeta{Name: clusterName},
+				}, true, util.Timeout)
+			})
+
+			ginkgo.By("Wait for status to indicate ClusterProfile is missing", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					mkc := &kueue.MultiKueueCluster{}
+					g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, clusterKey, mkc)).To(gomega.Succeed())
+
+					active := apimeta.FindStatusCondition(mkc.Status.Conditions, kueue.MultiKueueClusterActive)
+					g.Expect(active).NotTo(gomega.BeNil())
+					g.Expect(active.Status).To(gomega.Equal(metav1.ConditionFalse))
+					g.Expect(active.Reason).To(gomega.Equal("BadClusterProfile"))
+					g.Expect(active.Message).To(gomega.ContainSubstring("not found"))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Create the referenced ClusterProfile", func() {
+				cp := utiltestingapi.MakeClusterProfile(cpName, managersConfigNamespace.Name).Obj()
+				gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, cp)).To(gomega.Succeed())
+				ginkgo.DeferCleanup(func() {
+					util.ExpectObjectToBeDeletedWithTimeout(managerTestCluster.ctx, managerTestCluster.client, &inventoryv1alpha1.ClusterProfile{
+						ObjectMeta: metav1.ObjectMeta{Namespace: managersConfigNamespace.Name, Name: cpName},
+					}, true, util.Timeout)
+				})
+			})
```

  Updated and moved to AfterEach

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-01-26T09:32:34Z

- **test/integration/multikueue/setup_test.go:769** by [@mykysha](https://github.com/mykysha) — 2026-01-26T09:32:34Z
```diff
@@ -762,55 +763,443 @@ var _ = ginkgo.Describe("MultiKueue", ginkgo.Label("area:multikueue", "feature:m
 })
 
 var _ = ginkgo.Describe("MultiKueue with ClusterProfile", ginkgo.Label("area:multikueue", "feature:multikueue"), ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
-	ginkgo.BeforeAll(func() {
-		// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
-		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.MultiKueueClusterProfile, true)
-		managerTestCluster.fwk.StartManager(managerTestCluster.ctx, managerTestCluster.cfg, func(ctx context.Context, mgr manager.Manager) {
-			managerAndMultiKueueSetup(ctx, mgr, 2*time.Second, defaultEnabledIntegrations, config.MultiKueueDispatcherModeAllAtOnce)
+	ginkgo.Context("Feature gate enabled", ginkgo.Ordered, func() {
+		ginkgo.BeforeAll(func() {
+			// The flag must be called before multikueue.SetupIndexer so that MultiKueueCluster ClusterProfile indexer is registered.
+			gomega.Expect(features.SetEnable(features.MultiKueueClusterProfile, true)).To(gomega.Succeed())
```

  Replaced feature gate setup with features.SetFeatureGateDuringTest
