# PR #88: Enhance Kueue.Queue.Status test suite

**Summary**: Enhance Kueue.Queue.Status test suite

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/88

**Last updated**: 2022-03-11T12:30:35Z

---

## Metadata

- **State**: closed
- **Author**: [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Branch**: `devel/queue_status` → `main`
- **Created**: 2022-03-02T21:32:10Z
- **Updated**: 2022-03-11T12:30:35Z
- **Merged**: —
- **Closed**: 2022-03-11T12:30:34Z
- **Labels**: `kind/feature`, `size/L`, `kind/api-change`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@denkensk](https://github.com/denkensk), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 11   **+112 / -48**

## Description

Kueue.Queue.Status report queuedWorkloads with assigned capacity
 QueuedWorkloadConditionType = "Assigned"

Signed-off-by: Carlos Eduardo Arango Gutierrez <carangog@redhat.com>

#### What type of PR is this?
/kind feature
/kind api-change
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Follow up on #5

#### Special notes for your reviewer:

## Discussion

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-02T21:37:55Z

/assign @alculquicondor

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-02T21:44:09Z

Can you work on an integration test first?

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-07T19:47:34Z

friendly ping, can we please make progress on this item?

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-07T21:07:38Z

> friendly ping, can we please make progress on this item?

Hi, sorry have been busy, will focus on @alculquicondor ask of more integration testing for this area this week

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-07T23:02:33Z

rebased

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-03-07T23:02:40Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/88#" title="Author self-approved">ArangoGutierrez</a>*
To complete the [pull request process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process), please ask for approval from **alculquicondor** after the PR has been reviewed.

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["alculquicondor"]} -->

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-08T00:52:05Z

hold on #98 
/hold

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-08T01:54:40Z

/unhold

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-08T01:54:54Z

> Can you work on an integration test first?

Ready for review

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-08T21:12:04Z

updated to WIP just so we know which PRs are actually waiting to be reviewed

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-09T13:46:49Z

Once https://github.com/kubernetes-sigs/kueue/pull/105 is in, I will retake work here

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-11T12:05:12Z

/retitle Enhance Kueue.Queue.Status test suite

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-11T12:05:12Z

/retitle Enhance Kueue.Queue.Status test suite

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-11T12:09:16Z

/retest-required

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-03-11T12:30:14Z

@ArangoGutierrez: The following tests **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:

Test name | Commit | Details | Required | Rerun command
--- | --- | --- | --- | ---
pull-kueue-test-integration-main | bdb1b1de3532c7daa271d7806beb0489da7e2057 | [link](https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/88/pull-kueue-test-integration-main/1502260375322628096) | true | `/test pull-kueue-test-integration-main`
pull-kueue-test-unit-main | bdb1b1de3532c7daa271d7806beb0489da7e2057 | [link](https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/88/pull-kueue-test-unit-main/1502260375284879360) | true | `/test pull-kueue-test-unit-main`
pull-kueue-verify-main | bdb1b1de3532c7daa271d7806beb0489da7e2057 | [link](https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/88/pull-kueue-verify-main/1502260375372959744) | true | `/test pull-kueue-verify-main`

[Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=88). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3AArangoGutierrez). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository. I understand the commands that are listed [here](https://go.k8s.io/bot-commands).
</details>
<!-- test report -->

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-11T12:30:23Z

This PR is broken will re-create 
/close

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-03-11T12:30:34Z

@ArangoGutierrez: Closed this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/88#issuecomment-1065071205):

>This PR is broken will re-create 
>/close


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-08T02:04:35Z

- **pkg/queue/manager.go:120** by [@ahg-g](https://github.com/ahg-g) — 2022-03-08T02:04:35Z
```diff
@@ -105,13 +105,28 @@ func (m *Manager) DeleteQueue(q *kueue.Queue) {
 	delete(m.queues, key)
 }
 
-func (m *Manager) Status(q *kueue.Queue) (int, error) {
+func (m *Manager) Status(q *kueue.Queue) (map[string]int32, error) {
 	m.RLock()
 	defer m.RUnlock()
-	if q := m.queues[Key(q)]; q != nil {
-		return q.heap.Len(), nil
+
+	jobs := make(map[string]int32)
+	queue, ok := m.queues[Key(q)]
+	if !ok {
+		return jobs, errQueueDoesntExist
 	}
-	return 0, errQueueDoesntExist
+	jobs["pending"] = int32(queue.heap.Len())
+
+	var admitted int32
+	for _, hi := range queue.heap.items {
```

  An admitted workload wouldn't exist in the queue at all. We will need to list the workloads using the index and count the assigned ones (unless we somehow keep track of them in the Queue data structure).

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-08T02:22:03Z

- **pkg/queue/manager.go:120** by [@ahg-g](https://github.com/ahg-g) — 2022-03-08T02:22:02Z
```diff
@@ -105,13 +105,28 @@ func (m *Manager) DeleteQueue(q *kueue.Queue) {
 	delete(m.queues, key)
 }
 
-func (m *Manager) Status(q *kueue.Queue) (int, error) {
+func (m *Manager) Status(q *kueue.Queue) (map[string]int32, error) {
 	m.RLock()
 	defer m.RUnlock()
-	if q := m.queues[Key(q)]; q != nil {
-		return q.heap.Len(), nil
+
+	jobs := make(map[string]int32)
+	queue, ok := m.queues[Key(q)]
+	if !ok {
+		return jobs, errQueueDoesntExist
 	}
-	return 0, errQueueDoesntExist
+	jobs["pending"] = int32(queue.heap.Len())
+
+	var admitted int32
+	for _, hi := range queue.heap.items {
```

  and listing is going to have perf implications, I don't think we should do it. Tracking number of admitted workloads at the clusterQueue level will probably be good enough for now.
  
  How about we use this PR to only update the test as discussed in the other PR?

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-08T14:59:13Z

- **pkg/queue/manager.go:120** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-08T14:59:13Z
```diff
@@ -105,13 +105,28 @@ func (m *Manager) DeleteQueue(q *kueue.Queue) {
 	delete(m.queues, key)
 }
 
-func (m *Manager) Status(q *kueue.Queue) (int, error) {
+func (m *Manager) Status(q *kueue.Queue) (map[string]int32, error) {
 	m.RLock()
 	defer m.RUnlock()
-	if q := m.queues[Key(q)]; q != nil {
-		return q.heap.Len(), nil
+
+	jobs := make(map[string]int32)
+	queue, ok := m.queues[Key(q)]
+	if !ok {
+		return jobs, errQueueDoesntExist
 	}
-	return 0, errQueueDoesntExist
+	jobs["pending"] = int32(queue.heap.Len())
+
+	var admitted int32
+	for _, hi := range queue.heap.items {
```

  Good point, I am a bit worried on the nested loop as well, but we do need something to query the queue it self, and not at the cluster lvl. 
  users might want to submit several workloads, and watch for their progress

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-08T15:04:30Z

- **pkg/queue/manager.go:120** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-08T15:04:30Z
```diff
@@ -105,13 +105,28 @@ func (m *Manager) DeleteQueue(q *kueue.Queue) {
 	delete(m.queues, key)
 }
 
-func (m *Manager) Status(q *kueue.Queue) (int, error) {
+func (m *Manager) Status(q *kueue.Queue) (map[string]int32, error) {
 	m.RLock()
 	defer m.RUnlock()
-	if q := m.queues[Key(q)]; q != nil {
-		return q.heap.Len(), nil
+
+	jobs := make(map[string]int32)
+	queue, ok := m.queues[Key(q)]
+	if !ok {
+		return jobs, errQueueDoesntExist
 	}
-	return 0, errQueueDoesntExist
+	jobs["pending"] = int32(queue.heap.Len())
+
+	var admitted int32
+	for _, hi := range queue.heap.items {
```

  Also I don't like the idea of users querying at the ClusterQueue lvl
  users should only be able to query at their lvl

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-08T15:17:08Z

- **pkg/queue/manager.go:120** by [@ahg-g](https://github.com/ahg-g) — 2022-03-08T15:17:08Z
```diff
@@ -105,13 +105,28 @@ func (m *Manager) DeleteQueue(q *kueue.Queue) {
 	delete(m.queues, key)
 }
 
-func (m *Manager) Status(q *kueue.Queue) (int, error) {
+func (m *Manager) Status(q *kueue.Queue) (map[string]int32, error) {
 	m.RLock()
 	defer m.RUnlock()
-	if q := m.queues[Key(q)]; q != nil {
-		return q.heap.Len(), nil
+
+	jobs := make(map[string]int32)
+	queue, ok := m.queues[Key(q)]
+	if !ok {
+		return jobs, errQueueDoesntExist
 	}
-	return 0, errQueueDoesntExist
+	jobs["pending"] = int32(queue.heap.Len())
+
+	var admitted int32
+	for _, hi := range queue.heap.items {
```

  It is useful in both places, admins would be interested in checking the number of workloads admitted to a ClusterQueue. 
  
  > users might want to submit several workloads, and watch for their progress
  
  right, but only if we can do that in an efficient way

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-08T15:25:44Z

- **pkg/queue/manager.go:120** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-08T15:25:43Z
```diff
@@ -105,13 +105,28 @@ func (m *Manager) DeleteQueue(q *kueue.Queue) {
 	delete(m.queues, key)
 }
 
-func (m *Manager) Status(q *kueue.Queue) (int, error) {
+func (m *Manager) Status(q *kueue.Queue) (map[string]int32, error) {
 	m.RLock()
 	defer m.RUnlock()
-	if q := m.queues[Key(q)]; q != nil {
-		return q.heap.Len(), nil
+
+	jobs := make(map[string]int32)
+	queue, ok := m.queues[Key(q)]
+	if !ok {
+		return jobs, errQueueDoesntExist
 	}
-	return 0, errQueueDoesntExist
+	jobs["pending"] = int32(queue.heap.Len())
+
+	var admitted int32
+	for _, hi := range queue.heap.items {
```

  oh for admins I agree 100%.
  
  @alculquicondor maybe you have an idea on how to query this without nesting?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-08T15:27:30Z

- **pkg/queue/manager.go:120** by [@ahg-g](https://github.com/ahg-g) — 2022-03-08T15:27:30Z
```diff
@@ -105,13 +105,28 @@ func (m *Manager) DeleteQueue(q *kueue.Queue) {
 	delete(m.queues, key)
 }
 
-func (m *Manager) Status(q *kueue.Queue) (int, error) {
+func (m *Manager) Status(q *kueue.Queue) (map[string]int32, error) {
 	m.RLock()
 	defer m.RUnlock()
-	if q := m.queues[Key(q)]; q != nil {
-		return q.heap.Len(), nil
+
+	jobs := make(map[string]int32)
+	queue, ok := m.queues[Key(q)]
+	if !ok {
+		return jobs, errQueueDoesntExist
 	}
-	return 0, errQueueDoesntExist
+	jobs["pending"] = int32(queue.heap.Len())
+
+	var admitted int32
+	for _, hi := range queue.heap.items {
```

  I am not sure what you mean by nesting, but as I mentioned above, admitted workloads don't exist in `queue.heap.items` at all; the queue only have the workloads to be scheduled (pending)

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-08T15:28:40Z

- **pkg/queue/manager.go:120** by [@ahg-g](https://github.com/ahg-g) — 2022-03-08T15:28:39Z
```diff
@@ -105,13 +105,28 @@ func (m *Manager) DeleteQueue(q *kueue.Queue) {
 	delete(m.queues, key)
 }
 
-func (m *Manager) Status(q *kueue.Queue) (int, error) {
+func (m *Manager) Status(q *kueue.Queue) (map[string]int32, error) {
 	m.RLock()
 	defer m.RUnlock()
-	if q := m.queues[Key(q)]; q != nil {
-		return q.heap.Len(), nil
+
+	jobs := make(map[string]int32)
+	queue, ok := m.queues[Key(q)]
+	if !ok {
+		return jobs, errQueueDoesntExist
 	}
-	return 0, errQueueDoesntExist
+	jobs["pending"] = int32(queue.heap.Len())
+
+	var admitted int32
+	for _, hi := range queue.heap.items {
```

  what we need to do is perhaps organize the admitted workloads in each CQ broken by Q name, that is the most efficient way I believe.

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-08T16:00:11Z

- **pkg/queue/manager.go:120** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-08T16:00:11Z
```diff
@@ -105,13 +105,28 @@ func (m *Manager) DeleteQueue(q *kueue.Queue) {
 	delete(m.queues, key)
 }
 
-func (m *Manager) Status(q *kueue.Queue) (int, error) {
+func (m *Manager) Status(q *kueue.Queue) (map[string]int32, error) {
 	m.RLock()
 	defer m.RUnlock()
-	if q := m.queues[Key(q)]; q != nil {
-		return q.heap.Len(), nil
+
+	jobs := make(map[string]int32)
+	queue, ok := m.queues[Key(q)]
+	if !ok {
+		return jobs, errQueueDoesntExist
 	}
-	return 0, errQueueDoesntExist
+	jobs["pending"] = int32(queue.heap.Len())
+
+	var admitted int32
+	for _, hi := range queue.heap.items {
```

  Side thought: I am getting very conflicted on the layers here
  
  QW -> Q -> CQ
  
  we seem to be skipping Q,  CQ's should track Q's, and Q's track QW , if not, this will open questions for security implications.

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-08T16:01:56Z

- **pkg/queue/manager.go:120** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-08T16:01:56Z
```diff
@@ -105,13 +105,28 @@ func (m *Manager) DeleteQueue(q *kueue.Queue) {
 	delete(m.queues, key)
 }
 
-func (m *Manager) Status(q *kueue.Queue) (int, error) {
+func (m *Manager) Status(q *kueue.Queue) (map[string]int32, error) {
 	m.RLock()
 	defer m.RUnlock()
-	if q := m.queues[Key(q)]; q != nil {
-		return q.heap.Len(), nil
+
+	jobs := make(map[string]int32)
+	queue, ok := m.queues[Key(q)]
+	if !ok {
+		return jobs, errQueueDoesntExist
 	}
-	return 0, errQueueDoesntExist
+	jobs["pending"] = int32(queue.heap.Len())
+
+	var admitted int32
+	for _, hi := range queue.heap.items {
```

  If Q's are going to be namespaced, users need a way to fully manage their QW and Q's at their permission lvl 
  never ever have to interact with CQ's

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-08T16:37:30Z

- **pkg/queue/manager.go:120** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-08T16:37:30Z
```diff
@@ -105,13 +105,28 @@ func (m *Manager) DeleteQueue(q *kueue.Queue) {
 	delete(m.queues, key)
 }
 
-func (m *Manager) Status(q *kueue.Queue) (int, error) {
+func (m *Manager) Status(q *kueue.Queue) (map[string]int32, error) {
 	m.RLock()
 	defer m.RUnlock()
-	if q := m.queues[Key(q)]; q != nil {
-		return q.heap.Len(), nil
+
+	jobs := make(map[string]int32)
+	queue, ok := m.queues[Key(q)]
+	if !ok {
+		return jobs, errQueueDoesntExist
 	}
-	return 0, errQueueDoesntExist
+	jobs["pending"] = int32(queue.heap.Len())
+
+	var admitted int32
+	for _, hi := range queue.heap.items {
```

  working on a PoC based on this discussion and your last comment 
  
  > organize the admitted workloads

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-08T17:21:56Z

- **pkg/queue/manager.go:120** by [@ahg-g](https://github.com/ahg-g) — 2022-03-08T17:21:56Z
```diff
@@ -105,13 +105,28 @@ func (m *Manager) DeleteQueue(q *kueue.Queue) {
 	delete(m.queues, key)
 }
 
-func (m *Manager) Status(q *kueue.Queue) (int, error) {
+func (m *Manager) Status(q *kueue.Queue) (map[string]int32, error) {
 	m.RLock()
 	defer m.RUnlock()
-	if q := m.queues[Key(q)]; q != nil {
-		return q.heap.Len(), nil
+
+	jobs := make(map[string]int32)
+	queue, ok := m.queues[Key(q)]
+	if !ok {
+		return jobs, errQueueDoesntExist
 	}
-	return 0, errQueueDoesntExist
+	jobs["pending"] = int32(queue.heap.Len())
+
+	var admitted int32
+	for _, hi := range queue.heap.items {
```

  sg, please open an issue regarding the confusion on how the dependencies between the resource types might be confusing and your concern regarding security, lets have a discussion on that.

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-11T12:06:51Z

- **pkg/queue/manager.go:120** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-11T12:06:50Z
```diff
@@ -105,13 +105,28 @@ func (m *Manager) DeleteQueue(q *kueue.Queue) {
 	delete(m.queues, key)
 }
 
-func (m *Manager) Status(q *kueue.Queue) (int, error) {
+func (m *Manager) Status(q *kueue.Queue) (map[string]int32, error) {
 	m.RLock()
 	defer m.RUnlock()
-	if q := m.queues[Key(q)]; q != nil {
-		return q.heap.Len(), nil
+
+	jobs := make(map[string]int32)
+	queue, ok := m.queues[Key(q)]
+	if !ok {
+		return jobs, errQueueDoesntExist
 	}
-	return 0, errQueueDoesntExist
+	jobs["pending"] = int32(queue.heap.Len())
+
+	var admitted int32
+	for _, hi := range queue.heap.items {
```

  >How about we use this PR to only update the test as discussed in the other PR?
  
  Agree, done

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-11T12:06:51Z

- **pkg/queue/manager.go:120** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-11T12:06:50Z
```diff
@@ -105,13 +105,28 @@ func (m *Manager) DeleteQueue(q *kueue.Queue) {
 	delete(m.queues, key)
 }
 
-func (m *Manager) Status(q *kueue.Queue) (int, error) {
+func (m *Manager) Status(q *kueue.Queue) (map[string]int32, error) {
 	m.RLock()
 	defer m.RUnlock()
-	if q := m.queues[Key(q)]; q != nil {
-		return q.heap.Len(), nil
+
+	jobs := make(map[string]int32)
+	queue, ok := m.queues[Key(q)]
+	if !ok {
+		return jobs, errQueueDoesntExist
 	}
-	return 0, errQueueDoesntExist
+	jobs["pending"] = int32(queue.heap.Len())
+
+	var admitted int32
+	for _, hi := range queue.heap.items {
```

  >How about we use this PR to only update the test as discussed in the other PR?
  
  Agree, done
