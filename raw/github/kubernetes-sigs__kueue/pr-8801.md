# PR #8801: Handle surge replicas during rolling updates LWS

**Summary**: Handle surge replicas during rolling updates LWS

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8801

**Last updated**: 2026-02-11T07:49:35Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PannagaRao](https://github.com/PannagaRao)
- **Branch**: `lws-surge` → `main`
- **Created**: 2026-01-26T21:38:40Z
- **Updated**: 2026-02-11T07:49:35Z
- **Merged**: 2026-01-30T07:45:49Z
- **Closed**: 2026-01-30T07:45:49Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mimowo](https://github.com/mimowo)
- **Changed files**: 2   **+120 / -0**

## Description

Signed-off-by: Pannaga Rao Bhoja Ramamanohara

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes # https://github.com/kubernetes-sigs/kueue/issues/8769


#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
LeaderWorkerSet: Fix the bug where rolling updates with maxSurge could get stuck.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-01-26T21:38:46Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 23256267a8943b51e43350be5273ac18103606ed |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/697c086c6c23ab0008420bf4 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-26T21:38:50Z

Hi @PannagaRao. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@kannon92](https://github.com/kannon92) — 2026-01-26T21:42:46Z

/ok-to-test

### Comment by [@kannon92](https://github.com/kannon92) — 2026-01-26T21:43:32Z

Please add a release note since this is a potential bug.

/kind bug

cc @mbobrovskyi

I believe you've been looking at some LWS integration work lately so would appreciate your review.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-28T08:43:58Z

/cc @mimowo

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-30T06:05:44Z

LGTM label has been added.  <details>Git tree hash: 073170f0b13b546a01950f6d0efd8b0492558510</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-30T07:15:49Z

Thank you 👍 
/approve
/cherrypick release-0.16
/cherrypick release-0.15

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-01-30T07:15:52Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.15`, `release-0.16` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8801#issuecomment-3822235469):

>Thank you 👍 
>/approve
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-30T07:15:59Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8801#pullrequestreview-3726528837" title="LGTM">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8801#issuecomment-3822235469" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8801#" title="Author self-approved">PannagaRao</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-01-30T07:46:28Z

@mimowo: new pull request created: #8886

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8801#issuecomment-3822235469):

>Thank you 👍 
>/approve
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-01-30T07:47:05Z

@mimowo: new pull request created: #8887

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8801#issuecomment-3822235469):

>Thank you 👍 
>/approve
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-11T07:49:32Z

/release-note-edit
```release-note
LeaderWorkerSet: Fix the bug where rolling updates with maxSurge could get stuck.
```

## Reviews

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-27T14:28:15Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:183** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-27T14:28:16Z
```diff
@@ -152,22 +156,44 @@ func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reco
 // filterWorkloads compares the desired state of a LeaderWorkerSet with existing workloads,
 // determining which workloads need to be created, updated, or deleted.
 //
-// It accepts a LeaderWorkerSet and a slice of existing Workload objects as input and returns:
+// It accepts a LeaderWorkerSet, a slice of existing Workload objects, and a slice of existing
+// Pod objects as input and returns:
 // 1. A slice of workload names to be created
 // 2. A slice of workloads that may require updates
 // 3. A slice of Workload pointers to be deleted
-func (r *Reconciler) filterWorkloads(lws *leaderworkersetv1.LeaderWorkerSet, existingWorkloads []kueue.Workload) ([]string, []*kueue.Workload, []*kueue.Workload) {
+func (r *Reconciler) filterWorkloads(lws *leaderworkersetv1.LeaderWorkerSet, existingWorkloads []kueue.Workload, pods []corev1.Pod) ([]string, []*kueue.Workload, []*kueue.Workload) {
 	var (
 		toCreate []string
 		toUpdate []*kueue.Workload
 		toDelete = utilslices.ToRefMap(existingWorkloads, func(e *kueue.Workload) string {
 			return e.Name
 		})
-		replicas = ptr.Deref(lws.Spec.Replicas, 1)
+		groupIndices = make(map[string]bool)
+		replicas     = ptr.Deref(lws.Spec.Replicas, 1)
 	)
 
 	for i := range replicas {
-		workloadName := GetWorkloadName(lws.UID, lws.Name, fmt.Sprint(i))
+		groupIndices[fmt.Sprint(i)] = true
+	}
+
+	// Additionally, ensure workloads exist for any surge replicas during rolling updates.
+	// Surge pods only exist when updatedReplicas < replicas (rolling update in progress).
+	// During scale-down, updatedReplicas == replicas, so surge pods are not added.
+	// DeletionTimestamp check prevents keeping workloads for surge pods being cleaned up.
+	if lws.Status.UpdatedReplicas < lws.Status.Replicas {
```

  The guard `UpdatedReplicas < Replicas` might skip surge groups in a window where all groups are updated but `Status.Replicas > spec.Replicas` (surge pods not yet GC'd).
  
  For example:                                                                                                                                                        
    - spec.Replicas = 4, maxSurge = 2                                                                                                                                          
    - Phase 1: 4 old + 2 surge → Replicas=6, UpdatedReplicas=2 → guard is true                                                                                             
    - Phase 2: 2 new + 2 old + 2 surge → Replicas=6, UpdatedReplicas=4 → guard is true                                                                                       
    - Phase 3: 4 new + 2 surge (terminating) → Replicas=6, UpdatedReplicas=6 → guard is false

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-27T14:30:40Z

- **test/e2e/singlecluster/leaderworkerset_test.go:776** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-27T14:30:40Z
```diff
@@ -693,6 +696,132 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(4).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
+				TerminationGracePeriod(1).
+				Obj()
+
+			lws.Spec.RolloutStrategy = leaderworkersetv1.RolloutStrategy{
+				Type: leaderworkersetv1.RollingUpdateStrategyType,
+				RollingUpdateConfiguration: &leaderworkersetv1.RollingUpdateConfiguration{
+					MaxUnavailable: intstr.FromInt(2),
+					MaxSurge:       intstr.FromInt(2),
+				},
+			}
+
+			ginkgo.By("Create LeaderWorkerSet with rolling update strategy", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+
+			ginkgo.By("Wait for initial 4 replicas to be ready", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(4)))
+					g.Expect(createdLeaderWorkerSet.Status.Conditions).To(utiltesting.HaveConditionStatusTrueAndReason("Available", "AllGroupsReady"))
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify workloads exist for initial 4 groups", func() {
+				for i := range 4 {
+					wl := &kueue.Workload{}
+					wlKey := types.NamespacedName{
+						Name:      leaderworkerset.GetWorkloadName(lws.UID, lws.Name, strconv.Itoa(i)),
+						Namespace: ns.Name,
+					}
+					gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Trigger rolling update by changing image", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					createdLeaderWorkerSet.Spec.LeaderWorkerTemplate.WorkerTemplate.Spec.Containers[0].Image = util.GetAgnHostImage()
+					g.Expect(k8sClient.Update(ctx, createdLeaderWorkerSet)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Wait for surge pods to be created (maxSurge=2)", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					pods := &corev1.PodList{}
+					g.Expect(k8sClient.List(ctx, pods, client.InNamespace(ns.Name),
+						client.MatchingLabels{leaderworkersetv1.SetNameLabelKey: lws.Name})).To(gomega.Succeed())
+					g.Expect(pods.Items).To(gomega.HaveLen(6))
```

  Based on https://github.com/kubernetes-sigs/kueue/pull/8801/changes#r2732283531, I think this could be a time-sensitive during rollout. It may flake at some point.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-27T15:54:03Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:124** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-27T15:54:03Z
```diff
@@ -119,7 +116,14 @@ func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reco
 		return ctrl.Result{}, err
 	}
 
-	toCreate, toUpdate, toDelete := r.filterWorkloads(lws, wlList.Items)
+	podList := &corev1.PodList{}
+	if err := r.client.List(ctx, podList, client.InNamespace(lws.GetNamespace()),
+		client.MatchingLabels{leaderworkersetv1.SetNameLabelKey: lws.Name},
+	); err != nil {
+		return ctrl.Result{}, err
+	}
```

  Can we avoid fetching Pods? Is it possible to get this from the LWS status instead?

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-01-27T17:35:42Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:183** by [@PannagaRao](https://github.com/PannagaRao) — 2026-01-27T17:35:42Z
```diff
@@ -152,22 +156,44 @@ func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reco
 // filterWorkloads compares the desired state of a LeaderWorkerSet with existing workloads,
 // determining which workloads need to be created, updated, or deleted.
 //
-// It accepts a LeaderWorkerSet and a slice of existing Workload objects as input and returns:
+// It accepts a LeaderWorkerSet, a slice of existing Workload objects, and a slice of existing
+// Pod objects as input and returns:
 // 1. A slice of workload names to be created
 // 2. A slice of workloads that may require updates
 // 3. A slice of Workload pointers to be deleted
-func (r *Reconciler) filterWorkloads(lws *leaderworkersetv1.LeaderWorkerSet, existingWorkloads []kueue.Workload) ([]string, []*kueue.Workload, []*kueue.Workload) {
+func (r *Reconciler) filterWorkloads(lws *leaderworkersetv1.LeaderWorkerSet, existingWorkloads []kueue.Workload, pods []corev1.Pod) ([]string, []*kueue.Workload, []*kueue.Workload) {
 	var (
 		toCreate []string
 		toUpdate []*kueue.Workload
 		toDelete = utilslices.ToRefMap(existingWorkloads, func(e *kueue.Workload) string {
 			return e.Name
 		})
-		replicas = ptr.Deref(lws.Spec.Replicas, 1)
+		groupIndices = make(map[string]bool)
+		replicas     = ptr.Deref(lws.Spec.Replicas, 1)
 	)
 
 	for i := range replicas {
-		workloadName := GetWorkloadName(lws.UID, lws.Name, fmt.Sprint(i))
+		groupIndices[fmt.Sprint(i)] = true
+	}
+
+	// Additionally, ensure workloads exist for any surge replicas during rolling updates.
+	// Surge pods only exist when updatedReplicas < replicas (rolling update in progress).
+	// During scale-down, updatedReplicas == replicas, so surge pods are not added.
+	// DeletionTimestamp check prevents keeping workloads for surge pods being cleaned up.
+	if lws.Status.UpdatedReplicas < lws.Status.Replicas {
```

  Thanks for the detailed analysis! You're right that there's a window in Phase 3 where UpdatedReplicas == Replicas but surge pods haven't been GC'd yet. But in this case, surge pods have already been admitted and LWS can delete them even without workloads right? Is there a reason we need to preserve the workloads in this case?

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-01-27T19:41:30Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:124** by [@PannagaRao](https://github.com/PannagaRao) — 2026-01-27T19:41:30Z
```diff
@@ -119,7 +116,14 @@ func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reco
 		return ctrl.Result{}, err
 	}
 
-	toCreate, toUpdate, toDelete := r.filterWorkloads(lws, wlList.Items)
+	podList := &corev1.PodList{}
+	if err := r.client.List(ctx, podList, client.InNamespace(lws.GetNamespace()),
+		client.MatchingLabels{leaderworkersetv1.SetNameLabelKey: lws.Name},
+	); err != nil {
+		return ctrl.Result{}, err
+	}
```

  I have updated the logic to use spec.replicas and status.replicas instead of fetching pods. PTAL!

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-01-27T19:42:34Z

- **test/e2e/singlecluster/leaderworkerset_test.go:776** by [@PannagaRao](https://github.com/PannagaRao) — 2026-01-27T19:42:34Z
```diff
@@ -693,6 +696,132 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(4).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
+				TerminationGracePeriod(1).
+				Obj()
+
+			lws.Spec.RolloutStrategy = leaderworkersetv1.RolloutStrategy{
+				Type: leaderworkersetv1.RollingUpdateStrategyType,
+				RollingUpdateConfiguration: &leaderworkersetv1.RollingUpdateConfiguration{
+					MaxUnavailable: intstr.FromInt(2),
+					MaxSurge:       intstr.FromInt(2),
+				},
+			}
+
+			ginkgo.By("Create LeaderWorkerSet with rolling update strategy", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+
+			ginkgo.By("Wait for initial 4 replicas to be ready", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(4)))
+					g.Expect(createdLeaderWorkerSet.Status.Conditions).To(utiltesting.HaveConditionStatusTrueAndReason("Available", "AllGroupsReady"))
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify workloads exist for initial 4 groups", func() {
+				for i := range 4 {
+					wl := &kueue.Workload{}
+					wlKey := types.NamespacedName{
+						Name:      leaderworkerset.GetWorkloadName(lws.UID, lws.Name, strconv.Itoa(i)),
+						Namespace: ns.Name,
+					}
+					gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Trigger rolling update by changing image", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					createdLeaderWorkerSet.Spec.LeaderWorkerTemplate.WorkerTemplate.Spec.Containers[0].Image = util.GetAgnHostImage()
+					g.Expect(k8sClient.Update(ctx, createdLeaderWorkerSet)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Wait for surge pods to be created (maxSurge=2)", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					pods := &corev1.PodList{}
+					g.Expect(k8sClient.List(ctx, pods, client.InNamespace(ns.Name),
+						client.MatchingLabels{leaderworkersetv1.SetNameLabelKey: lws.Name})).To(gomega.Succeed())
+					g.Expect(pods.Items).To(gomega.HaveLen(6))
```

  I have updated the test to see if workloads are created for surge pods instead of checking the length of pods to reduce flake. Please let me know your thoughts.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-28T04:40:25Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:166** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-28T04:40:25Z
```diff
@@ -156,18 +152,30 @@ func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reco
 // 1. A slice of workload names to be created
 // 2. A slice of workloads that may require updates
 // 3. A slice of Workload pointers to be deleted
+//
+// During rolling updates with maxSurge, status.Replicas may temporarily exceed spec.Replicas.
+// This function ensures workloads exist for all groups including surge replicas.
 func (r *Reconciler) filterWorkloads(lws *leaderworkersetv1.LeaderWorkerSet, existingWorkloads []kueue.Workload) ([]string, []*kueue.Workload, []*kueue.Workload) {
 	var (
 		toCreate []string
 		toUpdate []*kueue.Workload
 		toDelete = utilslices.ToRefMap(existingWorkloads, func(e *kueue.Workload) string {
 			return e.Name
 		})
-		replicas = ptr.Deref(lws.Spec.Replicas, 1)
+		replicas     = ptr.Deref(lws.Spec.Replicas, 1)
+		groupIndices = make(map[string]bool)
```

  Now I think we don't need groupIndices, right?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-28T04:43:02Z

- **test/e2e/singlecluster/leaderworkerset_test.go:733** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-28T04:43:02Z
```diff
@@ -693,6 +696,114 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(4).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
+				TerminationGracePeriod(1).
+				Obj()
+
+			lws.Spec.RolloutStrategy = leaderworkersetv1.RolloutStrategy{
+				Type: leaderworkersetv1.RollingUpdateStrategyType,
+				RollingUpdateConfiguration: &leaderworkersetv1.RollingUpdateConfiguration{
+					MaxUnavailable: intstr.FromInt(2),
```

  ```suggestion
  					MaxUnavailable: intstr.FromInt32(2),
  ```
  Looks like intstr.FromInt deprecated.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-28T04:43:10Z

- **test/e2e/singlecluster/leaderworkerset_test.go:734** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-28T04:43:10Z
```diff
@@ -693,6 +696,114 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(4).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
+				TerminationGracePeriod(1).
+				Obj()
+
+			lws.Spec.RolloutStrategy = leaderworkersetv1.RolloutStrategy{
+				Type: leaderworkersetv1.RollingUpdateStrategyType,
+				RollingUpdateConfiguration: &leaderworkersetv1.RollingUpdateConfiguration{
+					MaxUnavailable: intstr.FromInt(2),
+					MaxSurge:       intstr.FromInt(2),
```

  ```suggestion
  					MaxSurge:       intstr.FromInt32(2),
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-28T04:59:15Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:175** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-28T04:59:15Z
```diff
@@ -156,18 +152,30 @@ func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reco
 // 1. A slice of workload names to be created
 // 2. A slice of workloads that may require updates
 // 3. A slice of Workload pointers to be deleted
+//
+// During rolling updates with maxSurge, status.Replicas may temporarily exceed spec.Replicas.
+// This function ensures workloads exist for all groups including surge replicas.
 func (r *Reconciler) filterWorkloads(lws *leaderworkersetv1.LeaderWorkerSet, existingWorkloads []kueue.Workload) ([]string, []*kueue.Workload, []*kueue.Workload) {
 	var (
 		toCreate []string
 		toUpdate []*kueue.Workload
 		toDelete = utilslices.ToRefMap(existingWorkloads, func(e *kueue.Workload) string {
 			return e.Name
 		})
-		replicas = ptr.Deref(lws.Spec.Replicas, 1)
+		replicas     = ptr.Deref(lws.Spec.Replicas, 1)
+		groupIndices = make(map[string]bool)
 	)
 
-	for i := range replicas {
-		workloadName := GetWorkloadName(lws.UID, lws.Name, fmt.Sprint(i))
+	totalReplicas := lws.Status.Replicas
+	if totalReplicas == 0 {
+		totalReplicas = replicas
+	}
+	for i := range totalReplicas {
+		groupIndices[fmt.Sprint(i)] = true
+	}
```

  ```suggestion
  	if lws.Status.Replicas > replicas {
  		replicas = lws.Status.Replicas
  	}
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-28T04:59:57Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:177** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-28T04:59:57Z
```diff
@@ -156,18 +152,30 @@ func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reco
 // 1. A slice of workload names to be created
 // 2. A slice of workloads that may require updates
 // 3. A slice of Workload pointers to be deleted
+//
+// During rolling updates with maxSurge, status.Replicas may temporarily exceed spec.Replicas.
+// This function ensures workloads exist for all groups including surge replicas.
 func (r *Reconciler) filterWorkloads(lws *leaderworkersetv1.LeaderWorkerSet, existingWorkloads []kueue.Workload) ([]string, []*kueue.Workload, []*kueue.Workload) {
 	var (
 		toCreate []string
 		toUpdate []*kueue.Workload
 		toDelete = utilslices.ToRefMap(existingWorkloads, func(e *kueue.Workload) string {
 			return e.Name
 		})
-		replicas = ptr.Deref(lws.Spec.Replicas, 1)
+		replicas     = ptr.Deref(lws.Spec.Replicas, 1)
+		groupIndices = make(map[string]bool)
 	)
 
-	for i := range replicas {
-		workloadName := GetWorkloadName(lws.UID, lws.Name, fmt.Sprint(i))
+	totalReplicas := lws.Status.Replicas
+	if totalReplicas == 0 {
+		totalReplicas = replicas
+	}
+	for i := range totalReplicas {
+		groupIndices[fmt.Sprint(i)] = true
+	}
+
+	for groupIndex := range groupIndices {
```

  I think we can revert it.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-28T05:02:45Z

- **test/e2e/singlecluster/leaderworkerset_test.go:726** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-28T05:02:45Z
```diff
@@ -693,6 +696,114 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(4).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
```

  ```suggestion
  ```
  
  I think this no need, right?

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-01-28T05:10:36Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:177** by [@PannagaRao](https://github.com/PannagaRao) — 2026-01-28T05:10:36Z
```diff
@@ -156,18 +152,30 @@ func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reco
 // 1. A slice of workload names to be created
 // 2. A slice of workloads that may require updates
 // 3. A slice of Workload pointers to be deleted
+//
+// During rolling updates with maxSurge, status.Replicas may temporarily exceed spec.Replicas.
+// This function ensures workloads exist for all groups including surge replicas.
 func (r *Reconciler) filterWorkloads(lws *leaderworkersetv1.LeaderWorkerSet, existingWorkloads []kueue.Workload) ([]string, []*kueue.Workload, []*kueue.Workload) {
 	var (
 		toCreate []string
 		toUpdate []*kueue.Workload
 		toDelete = utilslices.ToRefMap(existingWorkloads, func(e *kueue.Workload) string {
 			return e.Name
 		})
-		replicas = ptr.Deref(lws.Spec.Replicas, 1)
+		replicas     = ptr.Deref(lws.Spec.Replicas, 1)
+		groupIndices = make(map[string]bool)
 	)
 
-	for i := range replicas {
-		workloadName := GetWorkloadName(lws.UID, lws.Name, fmt.Sprint(i))
+	totalReplicas := lws.Status.Replicas
+	if totalReplicas == 0 {
+		totalReplicas = replicas
+	}
+	for i := range totalReplicas {
+		groupIndices[fmt.Sprint(i)] = true
+	}
+
+	for groupIndex := range groupIndices {
```

  Done

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-01-28T05:14:42Z

- **test/e2e/singlecluster/leaderworkerset_test.go:726** by [@PannagaRao](https://github.com/PannagaRao) — 2026-01-28T05:14:42Z
```diff
@@ -693,6 +696,114 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(4).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
```

  Umm I'm not sure I understand the requirement here. I'm just creating LWS using the same template as some other tests in the file. It will be needed to verify right?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-28T05:17:03Z

- **test/e2e/singlecluster/leaderworkerset_test.go:753** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-28T05:17:04Z
```diff
@@ -693,6 +696,114 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(4).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
+				TerminationGracePeriod(1).
+				Obj()
+
+			lws.Spec.RolloutStrategy = leaderworkersetv1.RolloutStrategy{
+				Type: leaderworkersetv1.RollingUpdateStrategyType,
+				RollingUpdateConfiguration: &leaderworkersetv1.RollingUpdateConfiguration{
+					MaxUnavailable: intstr.FromInt32(2),
+					MaxSurge:       intstr.FromInt32(2),
+				},
+			}
+
+			ginkgo.By("Create LeaderWorkerSet with rolling update strategy", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+
+			ginkgo.By("Wait for initial 4 replicas to be ready", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(4)))
+					g.Expect(createdLeaderWorkerSet.Status.Conditions).To(utiltesting.HaveConditionStatusTrueAndReason("Available", "AllGroupsReady"))
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify workloads exist for initial 4 groups", func() {
+				for i := range 4 {
```

  ```suggestion
  			ginkgo.By(fmt.Sprintf("Verify workloads exist for initial %d groups", ptr.Deref(lws.Spec.Replicas, 1)), func() {
  				for i := range ptr.Deref(lws.Spec.Replicas, 1) {
  ```
  nit

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-28T05:18:21Z

- **test/e2e/singlecluster/leaderworkerset_test.go:774** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-28T05:18:21Z
```diff
@@ -693,6 +696,114 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(4).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
+				TerminationGracePeriod(1).
+				Obj()
+
+			lws.Spec.RolloutStrategy = leaderworkersetv1.RolloutStrategy{
+				Type: leaderworkersetv1.RollingUpdateStrategyType,
+				RollingUpdateConfiguration: &leaderworkersetv1.RollingUpdateConfiguration{
+					MaxUnavailable: intstr.FromInt32(2),
+					MaxSurge:       intstr.FromInt32(2),
+				},
+			}
+
+			ginkgo.By("Create LeaderWorkerSet with rolling update strategy", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+
+			ginkgo.By("Wait for initial 4 replicas to be ready", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(4)))
+					g.Expect(createdLeaderWorkerSet.Status.Conditions).To(utiltesting.HaveConditionStatusTrueAndReason("Available", "AllGroupsReady"))
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify workloads exist for initial 4 groups", func() {
+				for i := range 4 {
+					wl := &kueue.Workload{}
+					wlKey := types.NamespacedName{
+						Name:      leaderworkerset.GetWorkloadName(lws.UID, lws.Name, strconv.Itoa(i)),
+						Namespace: ns.Name,
+					}
+					gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Trigger rolling update by changing image", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					createdLeaderWorkerSet.Spec.LeaderWorkerTemplate.WorkerTemplate.Spec.Containers[0].Image = util.GetAgnHostImage()
+					g.Expect(k8sClient.Update(ctx, createdLeaderWorkerSet)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Wait for surge workloads to be created (maxSurge=2)", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					surgeGroups := []int{4, 5}
+					for _, groupIndex := range surgeGroups {
```

  ```suggestion
  					for i := range surgeGroups {
  ```
  
  Can we make a constant here? Like const surgeGroups = 2 and use it in lws.Spec.RolloutStrategy.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-28T05:20:04Z

- **test/e2e/singlecluster/leaderworkerset_test.go:726** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-28T05:20:04Z
```diff
@@ -693,6 +696,114 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(4).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
```

  Actually, no – [here](https://github.com/PannagaRao/kueue/blob/0d5ad40d1e9aeb22796e54fe48f17b5149a1d533/test/e2e/singlecluster/leaderworkerset_test.go#L88-L95) we are using only worker template.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-28T05:21:39Z

- **test/e2e/singlecluster/leaderworkerset_test.go:726** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-28T05:21:39Z
```diff
@@ -693,6 +696,114 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(4).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
```

  LWS should use the worker template if the leader template is not set. I think this should be sufficient, as I don’t see any difference.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-28T05:22:26Z

- **test/e2e/singlecluster/leaderworkerset_test.go:771** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-28T05:22:26Z
```diff
@@ -693,6 +696,114 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(4).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
+				TerminationGracePeriod(1).
+				Obj()
+
+			lws.Spec.RolloutStrategy = leaderworkersetv1.RolloutStrategy{
+				Type: leaderworkersetv1.RollingUpdateStrategyType,
+				RollingUpdateConfiguration: &leaderworkersetv1.RollingUpdateConfiguration{
+					MaxUnavailable: intstr.FromInt32(2),
+					MaxSurge:       intstr.FromInt32(2),
+				},
+			}
+
+			ginkgo.By("Create LeaderWorkerSet with rolling update strategy", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+
+			ginkgo.By("Wait for initial 4 replicas to be ready", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(4)))
+					g.Expect(createdLeaderWorkerSet.Status.Conditions).To(utiltesting.HaveConditionStatusTrueAndReason("Available", "AllGroupsReady"))
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify workloads exist for initial 4 groups", func() {
+				for i := range 4 {
+					wl := &kueue.Workload{}
+					wlKey := types.NamespacedName{
+						Name:      leaderworkerset.GetWorkloadName(lws.UID, lws.Name, strconv.Itoa(i)),
+						Namespace: ns.Name,
+					}
+					gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Trigger rolling update by changing image", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					createdLeaderWorkerSet.Spec.LeaderWorkerTemplate.WorkerTemplate.Spec.Containers[0].Image = util.GetAgnHostImage()
+					g.Expect(k8sClient.Update(ctx, createdLeaderWorkerSet)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Wait for surge workloads to be created (maxSurge=2)", func() {
```

  ```suggestion
  			ginkgo.By(fmt.Sprintf("Wait for surge workloads to be created (maxSurge=%d), surgeGroups)", func() {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-28T05:23:31Z

- **test/e2e/singlecluster/leaderworkerset_test.go:790** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-28T05:23:31Z
```diff
@@ -693,6 +696,114 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(4).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
+				TerminationGracePeriod(1).
+				Obj()
+
+			lws.Spec.RolloutStrategy = leaderworkersetv1.RolloutStrategy{
+				Type: leaderworkersetv1.RollingUpdateStrategyType,
+				RollingUpdateConfiguration: &leaderworkersetv1.RollingUpdateConfiguration{
+					MaxUnavailable: intstr.FromInt32(2),
+					MaxSurge:       intstr.FromInt32(2),
+				},
+			}
+
+			ginkgo.By("Create LeaderWorkerSet with rolling update strategy", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+
+			ginkgo.By("Wait for initial 4 replicas to be ready", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(4)))
+					g.Expect(createdLeaderWorkerSet.Status.Conditions).To(utiltesting.HaveConditionStatusTrueAndReason("Available", "AllGroupsReady"))
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify workloads exist for initial 4 groups", func() {
+				for i := range 4 {
+					wl := &kueue.Workload{}
+					wlKey := types.NamespacedName{
+						Name:      leaderworkerset.GetWorkloadName(lws.UID, lws.Name, strconv.Itoa(i)),
+						Namespace: ns.Name,
+					}
+					gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Trigger rolling update by changing image", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					createdLeaderWorkerSet.Spec.LeaderWorkerTemplate.WorkerTemplate.Spec.Containers[0].Image = util.GetAgnHostImage()
+					g.Expect(k8sClient.Update(ctx, createdLeaderWorkerSet)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Wait for surge workloads to be created (maxSurge=2)", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					surgeGroups := []int{4, 5}
+					for _, groupIndex := range surgeGroups {
+						wl := &kueue.Workload{}
+						wlKey := types.NamespacedName{
+							Name:      leaderworkerset.GetWorkloadName(lws.UID, lws.Name, strconv.Itoa(groupIndex)),
+							Namespace: ns.Name,
+						}
+						g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed(),
+							"workload for surge group %d should exist", groupIndex)
+					}
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify rolling update completes successfully with 4 pods running", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.UpdatedReplicas).To(gomega.Equal(int32(4)))
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(4)))
```

  nit: also here would be great to use constant instead of just 4.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-28T05:24:09Z

Overall LGTM.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-01-28T05:43:35Z

- **test/e2e/singlecluster/leaderworkerset_test.go:753** by [@PannagaRao](https://github.com/PannagaRao) — 2026-01-28T05:43:35Z
```diff
@@ -693,6 +696,114 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(4).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
+				TerminationGracePeriod(1).
+				Obj()
+
+			lws.Spec.RolloutStrategy = leaderworkersetv1.RolloutStrategy{
+				Type: leaderworkersetv1.RollingUpdateStrategyType,
+				RollingUpdateConfiguration: &leaderworkersetv1.RollingUpdateConfiguration{
+					MaxUnavailable: intstr.FromInt32(2),
+					MaxSurge:       intstr.FromInt32(2),
+				},
+			}
+
+			ginkgo.By("Create LeaderWorkerSet with rolling update strategy", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+
+			ginkgo.By("Wait for initial 4 replicas to be ready", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(4)))
+					g.Expect(createdLeaderWorkerSet.Status.Conditions).To(utiltesting.HaveConditionStatusTrueAndReason("Available", "AllGroupsReady"))
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify workloads exist for initial 4 groups", func() {
+				for i := range 4 {
```

  Done

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-01-28T05:43:56Z

- **test/e2e/singlecluster/leaderworkerset_test.go:790** by [@PannagaRao](https://github.com/PannagaRao) — 2026-01-28T05:43:56Z
```diff
@@ -693,6 +696,114 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(4).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
+				TerminationGracePeriod(1).
+				Obj()
+
+			lws.Spec.RolloutStrategy = leaderworkersetv1.RolloutStrategy{
+				Type: leaderworkersetv1.RollingUpdateStrategyType,
+				RollingUpdateConfiguration: &leaderworkersetv1.RollingUpdateConfiguration{
+					MaxUnavailable: intstr.FromInt32(2),
+					MaxSurge:       intstr.FromInt32(2),
+				},
+			}
+
+			ginkgo.By("Create LeaderWorkerSet with rolling update strategy", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+
+			ginkgo.By("Wait for initial 4 replicas to be ready", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(4)))
+					g.Expect(createdLeaderWorkerSet.Status.Conditions).To(utiltesting.HaveConditionStatusTrueAndReason("Available", "AllGroupsReady"))
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify workloads exist for initial 4 groups", func() {
+				for i := range 4 {
+					wl := &kueue.Workload{}
+					wlKey := types.NamespacedName{
+						Name:      leaderworkerset.GetWorkloadName(lws.UID, lws.Name, strconv.Itoa(i)),
+						Namespace: ns.Name,
+					}
+					gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Trigger rolling update by changing image", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					createdLeaderWorkerSet.Spec.LeaderWorkerTemplate.WorkerTemplate.Spec.Containers[0].Image = util.GetAgnHostImage()
+					g.Expect(k8sClient.Update(ctx, createdLeaderWorkerSet)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Wait for surge workloads to be created (maxSurge=2)", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					surgeGroups := []int{4, 5}
+					for _, groupIndex := range surgeGroups {
+						wl := &kueue.Workload{}
+						wlKey := types.NamespacedName{
+							Name:      leaderworkerset.GetWorkloadName(lws.UID, lws.Name, strconv.Itoa(groupIndex)),
+							Namespace: ns.Name,
+						}
+						g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed(),
+							"workload for surge group %d should exist", groupIndex)
+					}
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify rolling update completes successfully with 4 pods running", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.UpdatedReplicas).To(gomega.Equal(int32(4)))
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(4)))
```

  Done

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-01-28T05:47:19Z

- **test/e2e/singlecluster/leaderworkerset_test.go:774** by [@PannagaRao](https://github.com/PannagaRao) — 2026-01-28T05:47:19Z
```diff
@@ -693,6 +696,114 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(4).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
+				TerminationGracePeriod(1).
+				Obj()
+
+			lws.Spec.RolloutStrategy = leaderworkersetv1.RolloutStrategy{
+				Type: leaderworkersetv1.RollingUpdateStrategyType,
+				RollingUpdateConfiguration: &leaderworkersetv1.RollingUpdateConfiguration{
+					MaxUnavailable: intstr.FromInt32(2),
+					MaxSurge:       intstr.FromInt32(2),
+				},
+			}
+
+			ginkgo.By("Create LeaderWorkerSet with rolling update strategy", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+
+			ginkgo.By("Wait for initial 4 replicas to be ready", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(4)))
+					g.Expect(createdLeaderWorkerSet.Status.Conditions).To(utiltesting.HaveConditionStatusTrueAndReason("Available", "AllGroupsReady"))
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify workloads exist for initial 4 groups", func() {
+				for i := range 4 {
+					wl := &kueue.Workload{}
+					wlKey := types.NamespacedName{
+						Name:      leaderworkerset.GetWorkloadName(lws.UID, lws.Name, strconv.Itoa(i)),
+						Namespace: ns.Name,
+					}
+					gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Trigger rolling update by changing image", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					createdLeaderWorkerSet.Spec.LeaderWorkerTemplate.WorkerTemplate.Spec.Containers[0].Image = util.GetAgnHostImage()
+					g.Expect(k8sClient.Update(ctx, createdLeaderWorkerSet)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Wait for surge workloads to be created (maxSurge=2)", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					surgeGroups := []int{4, 5}
+					for _, groupIndex := range surgeGroups {
```

  I've added const maxSurge = 2 and used it in lws.Spec.RolloutStrategy. However, surgeGroups is a slice of group indices (e.g., [4, 5]) if spec.replicas=4 so I will calculate this dynamically as surgeGroups[i] = lwsReplicas + i to generate the correct indices for workload verification.
  
  Does this sound okay to you?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-28T05:55:55Z

- **test/e2e/singlecluster/leaderworkerset_test.go:799** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-28T05:52:11Z
```diff
@@ -693,6 +697,123 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			const (
+				maxSurge       = 2
+				maxUnavailable = 2
+			)
+
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(4).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
+				TerminationGracePeriod(1).
+				Obj()
+
+			lws.Spec.RolloutStrategy = leaderworkersetv1.RolloutStrategy{
+				Type: leaderworkersetv1.RollingUpdateStrategyType,
+				RollingUpdateConfiguration: &leaderworkersetv1.RollingUpdateConfiguration{
+					MaxUnavailable: intstr.FromInt32(maxUnavailable),
+					MaxSurge:       intstr.FromInt32(maxSurge),
+				},
+			}
+
+			ginkgo.By("Create LeaderWorkerSet with rolling update strategy", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+
+			ginkgo.By(fmt.Sprintf("Wait for initial %d replicas to be ready", ptr.Deref(lws.Spec.Replicas, 1)), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(ptr.Deref(lws.Spec.Replicas, 1))))
+					g.Expect(createdLeaderWorkerSet.Status.Conditions).To(utiltesting.HaveConditionStatusTrueAndReason("Available", "AllGroupsReady"))
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Verify workloads exist for initial %d groups", ptr.Deref(lws.Spec.Replicas, 1)), func() {
+				for i := range ptr.Deref(lws.Spec.Replicas, 1) {
+					wl := &kueue.Workload{}
+					wlKey := types.NamespacedName{
+						Name:      leaderworkerset.GetWorkloadName(lws.UID, lws.Name, strconv.Itoa(int(i))),
+						Namespace: ns.Name,
+					}
+					gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Trigger rolling update by changing image", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					createdLeaderWorkerSet.Spec.LeaderWorkerTemplate.WorkerTemplate.Spec.Containers[0].Image = util.GetAgnHostImage()
+					g.Expect(k8sClient.Update(ctx, createdLeaderWorkerSet)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Wait for the surge workloads to be created with maxSurge=%d", maxSurge), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					lwsReplicas := int(ptr.Deref(lws.Spec.Replicas, 1))
+					surgeGroups := make([]int, maxSurge)
+					for i := range surgeGroups {
+						surgeGroups[i] = lwsReplicas + i
+					}
+					for i := range surgeGroups {
+						wl := &kueue.Workload{}
+						wlKey := types.NamespacedName{
+							Name:      leaderworkerset.GetWorkloadName(lws.UID, lws.Name, strconv.Itoa(surgeGroups[i])),
+							Namespace: ns.Name,
+						}
+						g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed(),
+							"workload for surge group %d should exist", surgeGroups[i])
+					}
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Verify rolling update completes successfully with %d replicas running", ptr.Deref(lws.Spec.Replicas, 1)), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.UpdatedReplicas).To(gomega.Equal(int32(ptr.Deref(lws.Spec.Replicas, 1))))
```

  ```suggestion
  					g.Expect(createdLeaderWorkerSet.Status.UpdatedReplicas).To(gomega.Equal(ptr.Deref(lws.Spec.Replicas, 1)))
  ```

- **test/e2e/singlecluster/leaderworkerset_test.go:800** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-28T05:52:22Z
```diff
@@ -693,6 +697,123 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			const (
+				maxSurge       = 2
+				maxUnavailable = 2
+			)
+
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(4).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
+				TerminationGracePeriod(1).
+				Obj()
+
+			lws.Spec.RolloutStrategy = leaderworkersetv1.RolloutStrategy{
+				Type: leaderworkersetv1.RollingUpdateStrategyType,
+				RollingUpdateConfiguration: &leaderworkersetv1.RollingUpdateConfiguration{
+					MaxUnavailable: intstr.FromInt32(maxUnavailable),
+					MaxSurge:       intstr.FromInt32(maxSurge),
+				},
+			}
+
+			ginkgo.By("Create LeaderWorkerSet with rolling update strategy", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+
+			ginkgo.By(fmt.Sprintf("Wait for initial %d replicas to be ready", ptr.Deref(lws.Spec.Replicas, 1)), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(ptr.Deref(lws.Spec.Replicas, 1))))
+					g.Expect(createdLeaderWorkerSet.Status.Conditions).To(utiltesting.HaveConditionStatusTrueAndReason("Available", "AllGroupsReady"))
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Verify workloads exist for initial %d groups", ptr.Deref(lws.Spec.Replicas, 1)), func() {
+				for i := range ptr.Deref(lws.Spec.Replicas, 1) {
+					wl := &kueue.Workload{}
+					wlKey := types.NamespacedName{
+						Name:      leaderworkerset.GetWorkloadName(lws.UID, lws.Name, strconv.Itoa(int(i))),
+						Namespace: ns.Name,
+					}
+					gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Trigger rolling update by changing image", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					createdLeaderWorkerSet.Spec.LeaderWorkerTemplate.WorkerTemplate.Spec.Containers[0].Image = util.GetAgnHostImage()
+					g.Expect(k8sClient.Update(ctx, createdLeaderWorkerSet)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Wait for the surge workloads to be created with maxSurge=%d", maxSurge), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					lwsReplicas := int(ptr.Deref(lws.Spec.Replicas, 1))
+					surgeGroups := make([]int, maxSurge)
+					for i := range surgeGroups {
+						surgeGroups[i] = lwsReplicas + i
+					}
+					for i := range surgeGroups {
+						wl := &kueue.Workload{}
+						wlKey := types.NamespacedName{
+							Name:      leaderworkerset.GetWorkloadName(lws.UID, lws.Name, strconv.Itoa(surgeGroups[i])),
+							Namespace: ns.Name,
+						}
+						g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed(),
+							"workload for surge group %d should exist", surgeGroups[i])
+					}
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Verify rolling update completes successfully with %d replicas running", ptr.Deref(lws.Spec.Replicas, 1)), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.UpdatedReplicas).To(gomega.Equal(int32(ptr.Deref(lws.Spec.Replicas, 1))))
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(ptr.Deref(lws.Spec.Replicas, 1))))
```

  ```suggestion
  					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(ptr.Deref(lws.Spec.Replicas, 1)))
  ```

- **test/e2e/singlecluster/leaderworkerset_test.go:753** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-28T05:53:14Z
```diff
@@ -693,6 +697,123 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			const (
+				maxSurge       = 2
+				maxUnavailable = 2
+			)
+
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(4).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
+				TerminationGracePeriod(1).
+				Obj()
+
+			lws.Spec.RolloutStrategy = leaderworkersetv1.RolloutStrategy{
+				Type: leaderworkersetv1.RollingUpdateStrategyType,
+				RollingUpdateConfiguration: &leaderworkersetv1.RollingUpdateConfiguration{
+					MaxUnavailable: intstr.FromInt32(maxUnavailable),
+					MaxSurge:       intstr.FromInt32(maxSurge),
+				},
+			}
+
+			ginkgo.By("Create LeaderWorkerSet with rolling update strategy", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+
+			ginkgo.By(fmt.Sprintf("Wait for initial %d replicas to be ready", ptr.Deref(lws.Spec.Replicas, 1)), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(ptr.Deref(lws.Spec.Replicas, 1))))
```

  ```suggestion
  					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(ptr.Deref(lws.Spec.Replicas, 1)))
  ```

- **test/e2e/singlecluster/leaderworkerset_test.go:753** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-28T05:53:42Z
```diff
@@ -693,6 +697,123 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			const (
+				maxSurge       = 2
+				maxUnavailable = 2
+			)
+
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(4).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
+				TerminationGracePeriod(1).
+				Obj()
+
+			lws.Spec.RolloutStrategy = leaderworkersetv1.RolloutStrategy{
+				Type: leaderworkersetv1.RollingUpdateStrategyType,
+				RollingUpdateConfiguration: &leaderworkersetv1.RollingUpdateConfiguration{
+					MaxUnavailable: intstr.FromInt32(maxUnavailable),
+					MaxSurge:       intstr.FromInt32(maxSurge),
+				},
+			}
+
+			ginkgo.By("Create LeaderWorkerSet with rolling update strategy", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+
+			ginkgo.By(fmt.Sprintf("Wait for initial %d replicas to be ready", ptr.Deref(lws.Spec.Replicas, 1)), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(ptr.Deref(lws.Spec.Replicas, 1))))
```

  But maybe it's much easier to use some constant here. WDYT?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-28T05:57:59Z

- **test/e2e/singlecluster/leaderworkerset_test.go:784** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-28T05:57:59Z
```diff
@@ -693,6 +697,123 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			const (
+				maxSurge       = 2
+				maxUnavailable = 2
+			)
+
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(4).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
+				TerminationGracePeriod(1).
+				Obj()
+
+			lws.Spec.RolloutStrategy = leaderworkersetv1.RolloutStrategy{
+				Type: leaderworkersetv1.RollingUpdateStrategyType,
+				RollingUpdateConfiguration: &leaderworkersetv1.RollingUpdateConfiguration{
+					MaxUnavailable: intstr.FromInt32(maxUnavailable),
+					MaxSurge:       intstr.FromInt32(maxSurge),
+				},
+			}
+
+			ginkgo.By("Create LeaderWorkerSet with rolling update strategy", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+
+			ginkgo.By(fmt.Sprintf("Wait for initial %d replicas to be ready", ptr.Deref(lws.Spec.Replicas, 1)), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(ptr.Deref(lws.Spec.Replicas, 1))))
+					g.Expect(createdLeaderWorkerSet.Status.Conditions).To(utiltesting.HaveConditionStatusTrueAndReason("Available", "AllGroupsReady"))
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Verify workloads exist for initial %d groups", ptr.Deref(lws.Spec.Replicas, 1)), func() {
+				for i := range ptr.Deref(lws.Spec.Replicas, 1) {
+					wl := &kueue.Workload{}
+					wlKey := types.NamespacedName{
+						Name:      leaderworkerset.GetWorkloadName(lws.UID, lws.Name, strconv.Itoa(int(i))),
+						Namespace: ns.Name,
+					}
+					gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Trigger rolling update by changing image", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					createdLeaderWorkerSet.Spec.LeaderWorkerTemplate.WorkerTemplate.Spec.Containers[0].Image = util.GetAgnHostImage()
+					g.Expect(k8sClient.Update(ctx, createdLeaderWorkerSet)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Wait for the surge workloads to be created with maxSurge=%d", maxSurge), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					lwsReplicas := int(ptr.Deref(lws.Spec.Replicas, 1))
+					surgeGroups := make([]int, maxSurge)
+					for i := range surgeGroups {
+						surgeGroups[i] = lwsReplicas + i
+					}
+					for i := range surgeGroups {
```

  ```suggestion
  					for i := replicas; i < replicas+maxSurge; i++ {
  ```
  Maybe like this?

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-01-28T05:59:00Z

- **test/e2e/singlecluster/leaderworkerset_test.go:753** by [@PannagaRao](https://github.com/PannagaRao) — 2026-01-28T05:59:00Z
```diff
@@ -693,6 +697,123 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			const (
+				maxSurge       = 2
+				maxUnavailable = 2
+			)
+
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(4).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
+				TerminationGracePeriod(1).
+				Obj()
+
+			lws.Spec.RolloutStrategy = leaderworkersetv1.RolloutStrategy{
+				Type: leaderworkersetv1.RollingUpdateStrategyType,
+				RollingUpdateConfiguration: &leaderworkersetv1.RollingUpdateConfiguration{
+					MaxUnavailable: intstr.FromInt32(maxUnavailable),
+					MaxSurge:       intstr.FromInt32(maxSurge),
+				},
+			}
+
+			ginkgo.By("Create LeaderWorkerSet with rolling update strategy", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+
+			ginkgo.By(fmt.Sprintf("Wait for initial %d replicas to be ready", ptr.Deref(lws.Spec.Replicas, 1)), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(ptr.Deref(lws.Spec.Replicas, 1))))
```

  Yes, creating lwsReplicas const will be easier. I thought we wanted to fetch values from spec. Happy to change it to a constant

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-01-28T06:15:49Z

- **test/e2e/singlecluster/leaderworkerset_test.go:753** by [@PannagaRao](https://github.com/PannagaRao) — 2026-01-28T06:15:49Z
```diff
@@ -693,6 +697,123 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			const (
+				maxSurge       = 2
+				maxUnavailable = 2
+			)
+
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(4).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
+				TerminationGracePeriod(1).
+				Obj()
+
+			lws.Spec.RolloutStrategy = leaderworkersetv1.RolloutStrategy{
+				Type: leaderworkersetv1.RollingUpdateStrategyType,
+				RollingUpdateConfiguration: &leaderworkersetv1.RollingUpdateConfiguration{
+					MaxUnavailable: intstr.FromInt32(maxUnavailable),
+					MaxSurge:       intstr.FromInt32(maxSurge),
+				},
+			}
+
+			ginkgo.By("Create LeaderWorkerSet with rolling update strategy", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+
+			ginkgo.By(fmt.Sprintf("Wait for initial %d replicas to be ready", ptr.Deref(lws.Spec.Replicas, 1)), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(ptr.Deref(lws.Spec.Replicas, 1))))
```

  Changed to use lwsReplicas as an int constant, with explicit int32() conversions only when comparing against int32 status fields.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-01-28T06:16:43Z

- **test/e2e/singlecluster/leaderworkerset_test.go:800** by [@PannagaRao](https://github.com/PannagaRao) — 2026-01-28T06:16:43Z
```diff
@@ -693,6 +697,123 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			const (
+				maxSurge       = 2
+				maxUnavailable = 2
+			)
+
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(4).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
+				TerminationGracePeriod(1).
+				Obj()
+
+			lws.Spec.RolloutStrategy = leaderworkersetv1.RolloutStrategy{
+				Type: leaderworkersetv1.RollingUpdateStrategyType,
+				RollingUpdateConfiguration: &leaderworkersetv1.RollingUpdateConfiguration{
+					MaxUnavailable: intstr.FromInt32(maxUnavailable),
+					MaxSurge:       intstr.FromInt32(maxSurge),
+				},
+			}
+
+			ginkgo.By("Create LeaderWorkerSet with rolling update strategy", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+
+			ginkgo.By(fmt.Sprintf("Wait for initial %d replicas to be ready", ptr.Deref(lws.Spec.Replicas, 1)), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(ptr.Deref(lws.Spec.Replicas, 1))))
+					g.Expect(createdLeaderWorkerSet.Status.Conditions).To(utiltesting.HaveConditionStatusTrueAndReason("Available", "AllGroupsReady"))
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Verify workloads exist for initial %d groups", ptr.Deref(lws.Spec.Replicas, 1)), func() {
+				for i := range ptr.Deref(lws.Spec.Replicas, 1) {
+					wl := &kueue.Workload{}
+					wlKey := types.NamespacedName{
+						Name:      leaderworkerset.GetWorkloadName(lws.UID, lws.Name, strconv.Itoa(int(i))),
+						Namespace: ns.Name,
+					}
+					gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Trigger rolling update by changing image", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					createdLeaderWorkerSet.Spec.LeaderWorkerTemplate.WorkerTemplate.Spec.Containers[0].Image = util.GetAgnHostImage()
+					g.Expect(k8sClient.Update(ctx, createdLeaderWorkerSet)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Wait for the surge workloads to be created with maxSurge=%d", maxSurge), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					lwsReplicas := int(ptr.Deref(lws.Spec.Replicas, 1))
+					surgeGroups := make([]int, maxSurge)
+					for i := range surgeGroups {
+						surgeGroups[i] = lwsReplicas + i
+					}
+					for i := range surgeGroups {
+						wl := &kueue.Workload{}
+						wlKey := types.NamespacedName{
+							Name:      leaderworkerset.GetWorkloadName(lws.UID, lws.Name, strconv.Itoa(surgeGroups[i])),
+							Namespace: ns.Name,
+						}
+						g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed(),
+							"workload for surge group %d should exist", surgeGroups[i])
+					}
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Verify rolling update completes successfully with %d replicas running", ptr.Deref(lws.Spec.Replicas, 1)), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.UpdatedReplicas).To(gomega.Equal(int32(ptr.Deref(lws.Spec.Replicas, 1))))
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(ptr.Deref(lws.Spec.Replicas, 1))))
```

  Changed to use lwsReplicas as an int constant, with explicit int32() conversions only when comparing against int32 status fields.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-01-28T06:16:47Z

- **test/e2e/singlecluster/leaderworkerset_test.go:799** by [@PannagaRao](https://github.com/PannagaRao) — 2026-01-28T06:16:46Z
```diff
@@ -693,6 +697,123 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			const (
+				maxSurge       = 2
+				maxUnavailable = 2
+			)
+
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(4).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
+				TerminationGracePeriod(1).
+				Obj()
+
+			lws.Spec.RolloutStrategy = leaderworkersetv1.RolloutStrategy{
+				Type: leaderworkersetv1.RollingUpdateStrategyType,
+				RollingUpdateConfiguration: &leaderworkersetv1.RollingUpdateConfiguration{
+					MaxUnavailable: intstr.FromInt32(maxUnavailable),
+					MaxSurge:       intstr.FromInt32(maxSurge),
+				},
+			}
+
+			ginkgo.By("Create LeaderWorkerSet with rolling update strategy", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+
+			ginkgo.By(fmt.Sprintf("Wait for initial %d replicas to be ready", ptr.Deref(lws.Spec.Replicas, 1)), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(ptr.Deref(lws.Spec.Replicas, 1))))
+					g.Expect(createdLeaderWorkerSet.Status.Conditions).To(utiltesting.HaveConditionStatusTrueAndReason("Available", "AllGroupsReady"))
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Verify workloads exist for initial %d groups", ptr.Deref(lws.Spec.Replicas, 1)), func() {
+				for i := range ptr.Deref(lws.Spec.Replicas, 1) {
+					wl := &kueue.Workload{}
+					wlKey := types.NamespacedName{
+						Name:      leaderworkerset.GetWorkloadName(lws.UID, lws.Name, strconv.Itoa(int(i))),
+						Namespace: ns.Name,
+					}
+					gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Trigger rolling update by changing image", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					createdLeaderWorkerSet.Spec.LeaderWorkerTemplate.WorkerTemplate.Spec.Containers[0].Image = util.GetAgnHostImage()
+					g.Expect(k8sClient.Update(ctx, createdLeaderWorkerSet)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Wait for the surge workloads to be created with maxSurge=%d", maxSurge), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					lwsReplicas := int(ptr.Deref(lws.Spec.Replicas, 1))
+					surgeGroups := make([]int, maxSurge)
+					for i := range surgeGroups {
+						surgeGroups[i] = lwsReplicas + i
+					}
+					for i := range surgeGroups {
+						wl := &kueue.Workload{}
+						wlKey := types.NamespacedName{
+							Name:      leaderworkerset.GetWorkloadName(lws.UID, lws.Name, strconv.Itoa(surgeGroups[i])),
+							Namespace: ns.Name,
+						}
+						g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed(),
+							"workload for surge group %d should exist", surgeGroups[i])
+					}
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Verify rolling update completes successfully with %d replicas running", ptr.Deref(lws.Spec.Replicas, 1)), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.UpdatedReplicas).To(gomega.Equal(int32(ptr.Deref(lws.Spec.Replicas, 1))))
```

  Changed to use lwsReplicas as an int constant, with explicit int32() conversions only when comparing against int32 status fields.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-01-28T06:17:27Z

- **test/e2e/singlecluster/leaderworkerset_test.go:784** by [@PannagaRao](https://github.com/PannagaRao) — 2026-01-28T06:17:27Z
```diff
@@ -693,6 +697,123 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			const (
+				maxSurge       = 2
+				maxUnavailable = 2
+			)
+
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(4).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
+				TerminationGracePeriod(1).
+				Obj()
+
+			lws.Spec.RolloutStrategy = leaderworkersetv1.RolloutStrategy{
+				Type: leaderworkersetv1.RollingUpdateStrategyType,
+				RollingUpdateConfiguration: &leaderworkersetv1.RollingUpdateConfiguration{
+					MaxUnavailable: intstr.FromInt32(maxUnavailable),
+					MaxSurge:       intstr.FromInt32(maxSurge),
+				},
+			}
+
+			ginkgo.By("Create LeaderWorkerSet with rolling update strategy", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+
+			ginkgo.By(fmt.Sprintf("Wait for initial %d replicas to be ready", ptr.Deref(lws.Spec.Replicas, 1)), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(ptr.Deref(lws.Spec.Replicas, 1))))
+					g.Expect(createdLeaderWorkerSet.Status.Conditions).To(utiltesting.HaveConditionStatusTrueAndReason("Available", "AllGroupsReady"))
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Verify workloads exist for initial %d groups", ptr.Deref(lws.Spec.Replicas, 1)), func() {
+				for i := range ptr.Deref(lws.Spec.Replicas, 1) {
+					wl := &kueue.Workload{}
+					wlKey := types.NamespacedName{
+						Name:      leaderworkerset.GetWorkloadName(lws.UID, lws.Name, strconv.Itoa(int(i))),
+						Namespace: ns.Name,
+					}
+					gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Trigger rolling update by changing image", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					createdLeaderWorkerSet.Spec.LeaderWorkerTemplate.WorkerTemplate.Spec.Containers[0].Image = util.GetAgnHostImage()
+					g.Expect(k8sClient.Update(ctx, createdLeaderWorkerSet)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Wait for the surge workloads to be created with maxSurge=%d", maxSurge), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					lwsReplicas := int(ptr.Deref(lws.Spec.Replicas, 1))
+					surgeGroups := make([]int, maxSurge)
+					for i := range surgeGroups {
+						surgeGroups[i] = lwsReplicas + i
+					}
+					for i := range surgeGroups {
```

  I have applied this suggestion. Waiting to see if linter complains.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-28T06:37:12Z

- **test/e2e/singlecluster/leaderworkerset_test.go:714** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-28T06:37:12Z
```diff
@@ -693,6 +697,119 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			const (
+				lwsReplicas    = 4
+				maxSurge       = 2
+				maxUnavailable = 2
+			)
+
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(lwsReplicas).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
```

  I still think we can remove this. Did you try removing it?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-28T07:04:57Z

- **test/e2e/singlecluster/leaderworkerset_test.go:714** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-28T07:04:57Z
```diff
@@ -693,6 +697,119 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			const (
+				lwsReplicas    = 4
+				maxSurge       = 2
+				maxUnavailable = 2
+			)
+
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(lwsReplicas).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
```

  As I can see from this comment: https://github.com/kubernetes-sigs/lws/blob/fc19b9ae9b0113d228ba15f4b62b06bea434b510/api/leaderworkerset/v1/leaderworkerset_types.go#L143-L149, we fall back to the worker template when the leader template is not specified. Currently, the leader template is the same as the worker template, so I’m pretty sure we can remove it here.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-01-29T20:31:42Z

- **test/e2e/singlecluster/leaderworkerset_test.go:714** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-29T20:27:53Z
```diff
@@ -693,6 +697,119 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			const (
+				lwsReplicas    = 4
+				maxSurge       = 2
+				maxUnavailable = 2
+			)
+
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(lwsReplicas).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
```

  I agree with @mbobrovskyi's suggestion. The explicit LeaderTemplate can be removed since it's identical to the WorkerTemplate and LWS handles the fallback.

- **test/e2e/singlecluster/leaderworkerset_test.go:786** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-01-29T20:31:24Z
```diff
@@ -693,6 +697,119 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			const (
+				lwsReplicas    = 4
+				maxSurge       = 2
+				maxUnavailable = 2
+			)
+
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(lwsReplicas).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
+				TerminationGracePeriod(1).
+				Obj()
+
+			lws.Spec.RolloutStrategy = leaderworkersetv1.RolloutStrategy{
+				Type: leaderworkersetv1.RollingUpdateStrategyType,
+				RollingUpdateConfiguration: &leaderworkersetv1.RollingUpdateConfiguration{
+					MaxUnavailable: intstr.FromInt32(maxUnavailable),
+					MaxSurge:       intstr.FromInt32(maxSurge),
+				},
+			}
+
+			ginkgo.By("Create LeaderWorkerSet with rolling update strategy", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+
+			ginkgo.By(fmt.Sprintf("Wait for initial %d replicas to be ready", lwsReplicas), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(lwsReplicas)))
+					g.Expect(createdLeaderWorkerSet.Status.Conditions).To(utiltesting.HaveConditionStatusTrueAndReason("Available", "AllGroupsReady"))
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Verify workloads exist for initial %d groups", lwsReplicas), func() {
+				for i := range lwsReplicas {
+					wl := &kueue.Workload{}
+					wlKey := types.NamespacedName{
+						Name:      leaderworkerset.GetWorkloadName(lws.UID, lws.Name, strconv.Itoa(i)),
+						Namespace: ns.Name,
+					}
+					gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Trigger rolling update by changing image", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					createdLeaderWorkerSet.Spec.LeaderWorkerTemplate.WorkerTemplate.Spec.Containers[0].Image = util.GetAgnHostImage()
+					g.Expect(k8sClient.Update(ctx, createdLeaderWorkerSet)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Wait for the surge workloads to be created with maxSurge=%d", maxSurge), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					for i := lwsReplicas; i < lwsReplicas+maxSurge; i++ {
+						wl := &kueue.Workload{}
+						wlKey := types.NamespacedName{
+							Name:      leaderworkerset.GetWorkloadName(lws.UID, lws.Name, strconv.Itoa(i)),
+							Namespace: ns.Name,
+						}
+						g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed(),
+							"workload for surge group %d should exist", i)
+					}
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Verify rolling update completes successfully with %d replicas running", lwsReplicas), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.UpdatedReplicas).To(gomega.Equal(int32(lwsReplicas)))
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(lwsReplicas)))
+					g.Expect(createdLeaderWorkerSet.Status.Conditions).To(utiltesting.HaveConditionStatusTrueAndReason("Available", "AllGroupsReady"))
+
+					pods := &corev1.PodList{}
+					g.Expect(k8sClient.List(ctx, pods, client.InNamespace(ns.Name),
+						client.MatchingLabels{leaderworkersetv1.SetNameLabelKey: lws.Name})).To(gomega.Succeed())
+
+					for _, pod := range pods.Items {
+						g.Expect(pod.Status.Phase).To(gomega.Equal(corev1.PodRunning))
+					}
```

  not a blocker but it will be good to verify surge workloads are cleaned up after rollout. This would verify the cleanup path works when status.Replicas drops back to spec.Replicas

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-01-30T01:13:49Z

- **test/e2e/singlecluster/leaderworkerset_test.go:786** by [@PannagaRao](https://github.com/PannagaRao) — 2026-01-30T01:13:49Z
```diff
@@ -693,6 +697,119 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			const (
+				lwsReplicas    = 4
+				maxSurge       = 2
+				maxUnavailable = 2
+			)
+
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(lwsReplicas).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
+				TerminationGracePeriod(1).
+				Obj()
+
+			lws.Spec.RolloutStrategy = leaderworkersetv1.RolloutStrategy{
+				Type: leaderworkersetv1.RollingUpdateStrategyType,
+				RollingUpdateConfiguration: &leaderworkersetv1.RollingUpdateConfiguration{
+					MaxUnavailable: intstr.FromInt32(maxUnavailable),
+					MaxSurge:       intstr.FromInt32(maxSurge),
+				},
+			}
+
+			ginkgo.By("Create LeaderWorkerSet with rolling update strategy", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+
+			ginkgo.By(fmt.Sprintf("Wait for initial %d replicas to be ready", lwsReplicas), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(lwsReplicas)))
+					g.Expect(createdLeaderWorkerSet.Status.Conditions).To(utiltesting.HaveConditionStatusTrueAndReason("Available", "AllGroupsReady"))
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Verify workloads exist for initial %d groups", lwsReplicas), func() {
+				for i := range lwsReplicas {
+					wl := &kueue.Workload{}
+					wlKey := types.NamespacedName{
+						Name:      leaderworkerset.GetWorkloadName(lws.UID, lws.Name, strconv.Itoa(i)),
+						Namespace: ns.Name,
+					}
+					gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Trigger rolling update by changing image", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					createdLeaderWorkerSet.Spec.LeaderWorkerTemplate.WorkerTemplate.Spec.Containers[0].Image = util.GetAgnHostImage()
+					g.Expect(k8sClient.Update(ctx, createdLeaderWorkerSet)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Wait for the surge workloads to be created with maxSurge=%d", maxSurge), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					for i := lwsReplicas; i < lwsReplicas+maxSurge; i++ {
+						wl := &kueue.Workload{}
+						wlKey := types.NamespacedName{
+							Name:      leaderworkerset.GetWorkloadName(lws.UID, lws.Name, strconv.Itoa(i)),
+							Namespace: ns.Name,
+						}
+						g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed(),
+							"workload for surge group %d should exist", i)
+					}
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Verify rolling update completes successfully with %d replicas running", lwsReplicas), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.UpdatedReplicas).To(gomega.Equal(int32(lwsReplicas)))
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(lwsReplicas)))
+					g.Expect(createdLeaderWorkerSet.Status.Conditions).To(utiltesting.HaveConditionStatusTrueAndReason("Available", "AllGroupsReady"))
+
+					pods := &corev1.PodList{}
+					g.Expect(k8sClient.List(ctx, pods, client.InNamespace(ns.Name),
+						client.MatchingLabels{leaderworkersetv1.SetNameLabelKey: lws.Name})).To(gomega.Succeed())
+
+					for _, pod := range pods.Items {
+						g.Expect(pod.Status.Phase).To(gomega.Equal(corev1.PodRunning))
+					}
```

  I have added a check to verify surge workloads are deleted. PTAL!

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-01-30T01:14:11Z

- **test/e2e/singlecluster/leaderworkerset_test.go:714** by [@PannagaRao](https://github.com/PannagaRao) — 2026-01-30T01:14:11Z
```diff
@@ -693,6 +697,119 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			const (
+				lwsReplicas    = 4
+				maxSurge       = 2
+				maxUnavailable = 2
+			)
+
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(lwsReplicas).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
```

  I have removed the leader template. PTAL!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (approved) — 2026-01-30T06:05:36Z

Awesome! Thank you!

/lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-30T07:15:26Z

- **test/e2e/singlecluster/leaderworkerset_test.go:786** by [@mimowo](https://github.com/mimowo) — 2026-01-30T07:15:26Z
```diff
@@ -693,6 +697,119 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 				util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, createdWorkload2, false, util.LongTimeout)
 			})
 		})
+
+		ginkgo.It("Rolling update with maxSurge creates workloads for surge pods and completes successfully", func() {
+			const (
+				lwsReplicas    = 4
+				maxSurge       = 2
+				maxUnavailable = 2
+			)
+
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws-rollout", ns.Name).
+				Image(util.GetAgnHostImageOld(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(lwsReplicas).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Queue(lq.Name).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "c",
+								Args:  util.BehaviorWaitForDeletion,
+								Image: util.GetAgnHostImageOld(),
+								Resources: corev1.ResourceRequirements{
+									Requests: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+									Limits: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("200m"),
+									},
+								},
+							},
+						},
+						NodeSelector: map[string]string{},
+					},
+				}).
+				TerminationGracePeriod(1).
+				Obj()
+
+			lws.Spec.RolloutStrategy = leaderworkersetv1.RolloutStrategy{
+				Type: leaderworkersetv1.RollingUpdateStrategyType,
+				RollingUpdateConfiguration: &leaderworkersetv1.RollingUpdateConfiguration{
+					MaxUnavailable: intstr.FromInt32(maxUnavailable),
+					MaxSurge:       intstr.FromInt32(maxSurge),
+				},
+			}
+
+			ginkgo.By("Create LeaderWorkerSet with rolling update strategy", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+
+			ginkgo.By(fmt.Sprintf("Wait for initial %d replicas to be ready", lwsReplicas), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(lwsReplicas)))
+					g.Expect(createdLeaderWorkerSet.Status.Conditions).To(utiltesting.HaveConditionStatusTrueAndReason("Available", "AllGroupsReady"))
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Verify workloads exist for initial %d groups", lwsReplicas), func() {
+				for i := range lwsReplicas {
+					wl := &kueue.Workload{}
+					wlKey := types.NamespacedName{
+						Name:      leaderworkerset.GetWorkloadName(lws.UID, lws.Name, strconv.Itoa(i)),
+						Namespace: ns.Name,
+					}
+					gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Trigger rolling update by changing image", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					createdLeaderWorkerSet.Spec.LeaderWorkerTemplate.WorkerTemplate.Spec.Containers[0].Image = util.GetAgnHostImage()
+					g.Expect(k8sClient.Update(ctx, createdLeaderWorkerSet)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Wait for the surge workloads to be created with maxSurge=%d", maxSurge), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					for i := lwsReplicas; i < lwsReplicas+maxSurge; i++ {
+						wl := &kueue.Workload{}
+						wlKey := types.NamespacedName{
+							Name:      leaderworkerset.GetWorkloadName(lws.UID, lws.Name, strconv.Itoa(i)),
+							Namespace: ns.Name,
+						}
+						g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed(),
+							"workload for surge group %d should exist", i)
+					}
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Verify rolling update completes successfully with %d replicas running", lwsReplicas), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.UpdatedReplicas).To(gomega.Equal(int32(lwsReplicas)))
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(lwsReplicas)))
+					g.Expect(createdLeaderWorkerSet.Status.Conditions).To(utiltesting.HaveConditionStatusTrueAndReason("Available", "AllGroupsReady"))
+
+					pods := &corev1.PodList{}
+					g.Expect(k8sClient.List(ctx, pods, client.InNamespace(ns.Name),
+						client.MatchingLabels{leaderworkersetv1.SetNameLabelKey: lws.Name})).To(gomega.Succeed())
+
+					for _, pod := range pods.Items {
+						g.Expect(pod.Status.Phase).To(gomega.Equal(corev1.PodRunning))
+					}
```

  Thank you for the test to make sure the workloads are not leaking!
