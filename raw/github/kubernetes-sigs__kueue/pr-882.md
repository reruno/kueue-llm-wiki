# PR #882: [client-go] Generate from outside GOPATH

**Summary**: [client-go] Generate from outside GOPATH

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/882

**Last updated**: 2023-06-22T13:10:01Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `client-go-generate-anyware` → `main`
- **Created**: 2023-06-21T11:01:42Z
- **Updated**: 2023-06-22T13:10:01Z
- **Merged**: 2023-06-22T08:01:40Z
- **Closed**: 2023-06-22T08:01:40Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 5   **+55 / -19**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Make client-go generate work from outside GOPATH and a reference to k8s.io/code-generator in go.mod

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-06-21T11:01:53Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c8ab21f4e21c8edb99e11954a3172dc672172662 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6493527e76a66c000887f340 |

### Comment by [@trasc](https://github.com/trasc) — 2023-06-21T11:29:53Z

/cc @tenzen-y

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-21T11:40:23Z

NOTE: Even if using the main branch, I could generate the client-go code if I put the kueue repository the under GOPATH.

### Comment by [@trasc](https://github.com/trasc) — 2023-06-21T12:10:02Z

The scope is not to move the code in GOROOT.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-21T16:49:57Z

> The scope is not to move the code in GOROOT.

I meant I could generate codes in GOPATH without this improvement.

### Comment by [@trasc](https://github.com/trasc) — 2023-06-21T17:05:24Z

> > The scope is not to move the code in GOROOT.
> 
> I meant I could generate codes in GOPATH without this improvement.
> 

I know, I need it to work outside GOPATH.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-21T17:08:33Z

> > > The scope is not to move the code in GOROOT.
> > 
> > 
> > I meant I could generate codes in GOPATH without this improvement.
> 
> I know, I need it to work outside GOPATH.

I'm a bit confused. Which do you mean GOROOT or GOPATH?

### Comment by [@trasc](https://github.com/trasc) — 2023-06-21T17:40:06Z

GOPATH, sorry for the confusion. (It's been some time since I needed to use this term (before  go 1.11))

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-21T17:52:57Z

I'm ok with this change, if we decide that we will move all tools to use the same approach as this PR, instead of `go install`

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-21T17:58:50Z

> I'm ok with this change, if we decide that we will move all tools to use the same approach as this PR, instead of `go install`

+1
Because we should use consistent ways to manage tools.

### Comment by [@trasc](https://github.com/trasc) — 2023-06-21T18:04:01Z

The go install step cannot be removed, but we can have the same way of managing the versions.(via go.mod)

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-06-21T18:08:48Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/882#pullrequestreview-1491398785" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/882#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-21T18:08:58Z

Leaving LGTM to @tenzen-y

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-22T07:50:41Z

/lgtm

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-21T11:35:19Z

- **hack/internal/tools/pinversion.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-21T11:35:19Z
  I remember that we decided not to use tools after discussing it with @alculquicondor.
  
  cc: @alculquicondor

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-06-21T15:15:27Z

- **hack/update-codegen.sh:45** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-21T15:13:46Z
```diff
@@ -18,16 +18,28 @@ set -o errexit
 set -o nounset
 set -o pipefail
 
-cd "$(dirname "${0}")"
 GO_CMD=${1:-go}
-CODEGEN_PKG=${2:-../bin}
+KUEUE_ROOT=$(realpath $(dirname ${BASH_SOURCE[0]})/..)
+CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator )
 
-bash "${CODEGEN_PKG}/generate-groups.sh" \
+cd $(dirname ${BASH_SOURCE[0]})/..
+
+source "${CODEGEN_PKG}/generate-groups.sh" \
   "all" \
   sigs.k8s.io/kueue/client-go \
   sigs.k8s.io/kueue/apis \
   kueue:v1beta1 \
-  --go-header-file ./boilerplate.go.txt
+  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt \
 
-# We need to clean up the go.mod file since code-generator adds temporary library to the go.mod file.
-"${GO_CMD}" mod tidy
+# Future releases of of code-generator add better support for out of GOPATH generation,
+# check https://github.com/kubernetes/code-generator/blob/master/examples/hack/update-codegen.sh for details,
+# for now we should just move the generated code to `client-go`.
+if  [ -d sigs.k8s.io/kueue/client-go ]
+then
+  echo "Out of GOPATH generation, move the generated files to ../client-go"
+  if  [ -d ${KUEUE_ROOT}/client-go ]
+  then
+    rm -r ${KUEUE_ROOT}/client-go
+  fi
+  mv sigs.k8s.io/kueue/client-go ${KUEUE_ROOT}/client-go
+fi
```

  why is this necessary now but it wasn't before?

- **hack/internal/tools/pinversion.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-21T15:15:16Z
  I guess in this case both options are not pretty.
  
  We can't use `go install` because it wouldn't include the `.sh` file.
  
  Using the `tools` package is not consistent with the rest of the tools.
  
  Is there one approach that would allow us for every tool to be installed the same way?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-06-21T15:30:57Z

- **hack/update-codegen.sh:45** by [@trasc](https://github.com/trasc) — 2023-06-21T15:30:57Z
```diff
@@ -18,16 +18,28 @@ set -o errexit
 set -o nounset
 set -o pipefail
 
-cd "$(dirname "${0}")"
 GO_CMD=${1:-go}
-CODEGEN_PKG=${2:-../bin}
+KUEUE_ROOT=$(realpath $(dirname ${BASH_SOURCE[0]})/..)
+CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator )
 
-bash "${CODEGEN_PKG}/generate-groups.sh" \
+cd $(dirname ${BASH_SOURCE[0]})/..
+
+source "${CODEGEN_PKG}/generate-groups.sh" \
   "all" \
   sigs.k8s.io/kueue/client-go \
   sigs.k8s.io/kueue/apis \
   kueue:v1beta1 \
-  --go-header-file ./boilerplate.go.txt
+  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt \
 
-# We need to clean up the go.mod file since code-generator adds temporary library to the go.mod file.
-"${GO_CMD}" mod tidy
+# Future releases of of code-generator add better support for out of GOPATH generation,
+# check https://github.com/kubernetes/code-generator/blob/master/examples/hack/update-codegen.sh for details,
+# for now we should just move the generated code to `client-go`.
+if  [ -d sigs.k8s.io/kueue/client-go ]
+then
+  echo "Out of GOPATH generation, move the generated files to ../client-go"
+  if  [ -d ${KUEUE_ROOT}/client-go ]
+  then
+    rm -r ${KUEUE_ROOT}/client-go
+  fi
+  mv sigs.k8s.io/kueue/client-go ${KUEUE_ROOT}/client-go
+fi
```

  I guess I'm the only one not keeping my code in  GOROOT , and only today I had the time to do it :)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-06-21T15:33:25Z

- **hack/update-codegen.sh:45** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-21T15:33:25Z
```diff
@@ -18,16 +18,28 @@ set -o errexit
 set -o nounset
 set -o pipefail
 
-cd "$(dirname "${0}")"
 GO_CMD=${1:-go}
-CODEGEN_PKG=${2:-../bin}
+KUEUE_ROOT=$(realpath $(dirname ${BASH_SOURCE[0]})/..)
+CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator )
 
-bash "${CODEGEN_PKG}/generate-groups.sh" \
+cd $(dirname ${BASH_SOURCE[0]})/..
+
+source "${CODEGEN_PKG}/generate-groups.sh" \
   "all" \
   sigs.k8s.io/kueue/client-go \
   sigs.k8s.io/kueue/apis \
   kueue:v1beta1 \
-  --go-header-file ./boilerplate.go.txt
+  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt \
 
-# We need to clean up the go.mod file since code-generator adds temporary library to the go.mod file.
-"${GO_CMD}" mod tidy
+# Future releases of of code-generator add better support for out of GOPATH generation,
+# check https://github.com/kubernetes/code-generator/blob/master/examples/hack/update-codegen.sh for details,
+# for now we should just move the generated code to `client-go`.
+if  [ -d sigs.k8s.io/kueue/client-go ]
+then
+  echo "Out of GOPATH generation, move the generated files to ../client-go"
+  if  [ -d ${KUEUE_ROOT}/client-go ]
+  then
+    rm -r ${KUEUE_ROOT}/client-go
+  fi
+  mv sigs.k8s.io/kueue/client-go ${KUEUE_ROOT}/client-go
+fi
```

  You mean GOPATH?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-06-21T18:08:42Z

/approve

Please follow up by moving every tool we use to the `tools` package.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-21T18:13:22Z

I left comments for nits.

- **hack/update-codegen.sh:23** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-21T18:12:21Z
```diff
@@ -18,16 +18,28 @@ set -o errexit
 set -o nounset
 set -o pipefail
 
-cd "$(dirname "${0}")"
 GO_CMD=${1:-go}
-CODEGEN_PKG=${2:-../bin}
+KUEUE_ROOT=$(realpath $(dirname ${BASH_SOURCE[0]})/..)
+CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator )
```

  ```suggestion
  CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
  ```

- **hack/update-codegen.sh:32** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-21T18:12:33Z
```diff
@@ -18,16 +18,28 @@ set -o errexit
 set -o nounset
 set -o pipefail
 
-cd "$(dirname "${0}")"
 GO_CMD=${1:-go}
-CODEGEN_PKG=${2:-../bin}
+KUEUE_ROOT=$(realpath $(dirname ${BASH_SOURCE[0]})/..)
+CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator )
 
-bash "${CODEGEN_PKG}/generate-groups.sh" \
+cd $(dirname ${BASH_SOURCE[0]})/..
+
+source "${CODEGEN_PKG}/generate-groups.sh" \
   "all" \
   sigs.k8s.io/kueue/client-go \
   sigs.k8s.io/kueue/apis \
   kueue:v1beta1 \
-  --go-header-file ./boilerplate.go.txt
+  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt \
```

  ```suggestion
    --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt
  ```
