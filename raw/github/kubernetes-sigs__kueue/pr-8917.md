# PR #8917: Add TAS performance test

**Summary**: Add TAS performance test

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8917

**Last updated**: 2026-02-04T22:15:27Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ASverdlov](https://github.com/ASverdlov)
- **Branch**: `tas-performance-testing` → `main`
- **Created**: 2026-01-30T16:52:04Z
- **Updated**: 2026-02-04T22:15:27Z
- **Merged**: 2026-02-04T22:04:32Z
- **Closed**: 2026-02-04T22:04:32Z
- **Labels**: `kind/feature`, `lgtm`, `size/XXL`, `release-note-none`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@kshalot](https://github.com/kshalot)
- **Requested reviewers**: [@mwielgus](https://github.com/mwielgus), [@kshalot](https://github.com/kshalot), [@vladikkuzn](https://github.com/vladikkuzn), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 15   **+1002 / -205**

## Description

#### What type of PR is this?
/kind feature                                                                                                                                                                                           
/area testing

#### What this PR does / why we need it:
Implements a performance testing framework for Topology Aware Scheduling (TAS) to measure and track TAS assignment performance over time.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #4634 

#### Special notes for your reviewer:
Example test run ([script to produce a Gist](https://gist.github.com/ASverdlov/ee7d40568d7c6dea6ca1d204e6c392fd)): https://gist.github.com/ASverdlov/0eca3a4b109201bfbccae2e9eb7f5e7a

The thresholds are chosen by running the test 10 times on my local MacBook:
```
      Model Name: MacBook Air
      Model Identifier: Mac14,2
      Model Number: Z1600011KN/A
      Chip: Apple M2
      Total Number of Cores: 8 (4 performance and 4 efficiency)
      Memory: 16 GB
      System Firmware Version: 13822.61.10
      OS Loader Version: 13822.61.10
      Serial Number (system): HR6G27X5WK
      Hardware UUID: 0BD77642-1EF0-50CC-8CFC-621B9FACB577
      Provisioning UDID: 00008112-001924D02E68A01E
      Activation Lock Status: Enabled
``` 

Some limitations:
* No mixing of TAS and non-TAS workloads, so the recent [performance improvement](https://github.com/kubernetes-sigs/kueue/pull/8484) won't be seen by this performance test
* No workload size randomisation. We could easily add it, and it would help test the fragmentation, but not sure if it should mix the performance testing with fragmentation testing, maybe there could be a separate fragmentation test?
* Not part of the CI yet. I can make a separate PR to reduce the size of the changes to be reviewed.

A corresponding PR in test-infra: https://github.com/kubernetes/test-infra/pull/36375

#### Does this PR introduce a user-facing change?
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-30T16:52:08Z

@ASverdlov: The label(s) `area/testing` cannot be applied, because the repository doesn't have them.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8917):

>#### What type of PR is this?
>/kind feature                                                                                                                                                                                           
>/area testing
>
>#### What this PR does / why we need it:
>Implements a performance testing framework for Topology Aware Scheduling (TAS) to measure and track TAS assignment performance over time.
>
>#### Which issue(s) this PR fixes:
>
>Fixes #4634 
>
>#### Special notes for your reviewer:
>Example test run ([script to produce a Gist](https://gist.github.com/ASverdlov/ee7d40568d7c6dea6ca1d204e6c392fd)): https://gist.github.com/ASverdlov/0eca3a4b109201bfbccae2e9eb7f5e7a
>
>The thresholds are chosen by running the test 10 times on my local MacBook:
>```
>      Model Name: MacBook Air
>      Model Identifier: Mac14,2
>      Model Number: Z1600011KN/A
>      Chip: Apple M2
>      Total Number of Cores: 8 (4 performance and 4 efficiency)
>      Memory: 16 GB
>      System Firmware Version: 13822.61.10
>      OS Loader Version: 13822.61.10
>      Serial Number (system): HR6G27X5WK
>      Hardware UUID: 0BD77642-1EF0-50CC-8CFC-621B9FACB577
>      Provisioning UDID: 00008112-001924D02E68A01E
>      Activation Lock Status: Enabled
>``` 
>
>Some limitations:
>* No mixing of TAS and non-TAS workloads, so the recent [performance improvement](https://github.com/kubernetes-sigs/kueue/pull/8484) won't be seen by this performance test
>* No workload size randomisation. We could easily add it, and it would help test the fragmentation, but not sure if it should mix the performance testing with fragmentation testing, maybe there could be a separate fragmentation test?
>* Not part of the CI yet. I can make a separate PR to reduce the size of the changes to be reviewed.
>
>#### Does this PR introduce a user-facing change?
>-->
>```release-note
>NONE
>```


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-01-30T16:52:15Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 0d62307ff8fc22705e12a56d6322bbc0f47dd8ae |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6983a0b978ec66000826fb04 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-30T16:52:15Z

Hi @ASverdlov. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-30T16:55:33Z

/ok-to-test

### Comment by [@ASverdlov](https://github.com/ASverdlov) — 2026-01-30T16:55:36Z

/hold

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-30T17:08:20Z

Thank you for working on that! This will be super useful to compare performance of quota only vs. TAS.

FYI Once we have the suite we can add a CI job in the https://github.com/kubernetes/test-infra repo, similarly as currently we have it for `pull-kueue-test-scheduling-perf-main`

I will leave first passes of review to others.

### Comment by [@ASverdlov](https://github.com/ASverdlov) — 2026-02-01T16:33:36Z

/hold cancel

### Comment by [@kshalot](https://github.com/kshalot) — 2026-02-04T09:46:58Z

/lgtm

Thank you @ASverdlov! I posted a few more remarks, see whether there's anything actionable in there.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-04T09:47:06Z

LGTM label has been added.  <details>Git tree hash: 8fdcc3fb023a8802aad620388868f69ce9ab4d84</details>

### Comment by [@kshalot](https://github.com/kshalot) — 2026-02-04T18:55:41Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-04T18:55:49Z

LGTM label has been added.  <details>Git tree hash: 972962d192647c43cc03c3a601fd8d1076a6bf45</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-04T19:09:50Z

Thank you!
/approve
This is just tests so cherrypicking is safe, it will also give us useful data points
/cherrypick release-0.16
/cherrypick release-0.15

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-02-04T19:09:53Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.15`, `release-0.16` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8917#issuecomment-3849249957):

>Thank you!
>/approve
>This is just tests so cherrypicking is safe, it will also give us useful data points
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-04T19:10:02Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8917#" title="Author self-approved">ASverdlov</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8917#issuecomment-3849249957" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@ASverdlov](https://github.com/ASverdlov) — 2026-02-04T19:35:00Z

Thanks for reviewing the PR! Sorry I had pushed a delayed fix for one of the comments, so it removed the LGTM, I didn't expect you to review the previous ones so fast :)

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-04T19:35:15Z

LGTM label has been added.  <details>Git tree hash: 3cab0ea748798b12e1a82ed5a9e9e1413e3a6202</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-04T19:36:15Z

/hold
Could you please squeeze the commits, it will be much easier to cherrypick in case of conflicts

### Comment by [@ASverdlov](https://github.com/ASverdlov) — 2026-02-04T19:41:26Z

@mimowo squashed the commits together.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-04T21:32:51Z

/unhold
/lgtm
Thank you 👍

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-02-04T22:05:12Z

@mimowo: new pull request created: #8994

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8917#issuecomment-3849249957):

>Thank you!
>/approve
>This is just tests so cherrypicking is safe, it will also give us useful data points
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-02-04T22:05:48Z

@mimowo: #8917 failed to apply on top of branch "release-0.15":
```
Applying: Implement TAS performance test
Using index info to reconstruct a base tree...
M	Makefile-test.mk
M	pkg/util/testing/v1beta2/wrappers.go
M	test/performance/scheduler/minimalkueue/main.go
M	test/performance/scheduler/runner/generator/generator.go
Falling back to patching base and 3-way merge...
Auto-merging test/performance/scheduler/runner/generator/generator.go
CONFLICT (content): Merge conflict in test/performance/scheduler/runner/generator/generator.go
Auto-merging test/performance/scheduler/minimalkueue/main.go
CONFLICT (content): Merge conflict in test/performance/scheduler/minimalkueue/main.go
Removing test/performance/scheduler/default_generator_config.yaml
Auto-merging pkg/util/testing/v1beta2/wrappers.go
Auto-merging Makefile-test.mk
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Implement TAS performance test

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8917#issuecomment-3849249957):

>Thank you!
>/approve
>This is just tests so cherrypicking is safe, it will also give us useful data points
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-04T22:15:27Z

Please prepare the cherrypick for 0.15 manually. I like to use the ./hack/cherry_pick_pull.sh script

## Reviews

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-02-01T10:21:48Z

- **test/performance/tas/runner/generator/generator.go:194** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-01T10:21:49Z
```diff
@@ -0,0 +1,289 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package generator
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"os"
+	"sync"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/yaml"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+)
+
+const (
+	RunningTimeLabel = "kueue.x-k8s.io/performance-scheduler-running-ms"
+	ClassLabel       = "kueue.x-k8s.io/performance-scheduler-class"
+	CleanupLabel     = "kueue.x-k8s.io/performance-scheduler-cleanup"
+)
+
+// WorkloadTemplate defines a workload to generate (extended for TAS)
+type WorkloadTemplate struct {
+	ClassName     string `json:"className"`
+	RuntimeMs     uint   `json:"runtimeMs"`
+	Priority      int32  `json:"priority"`
+	Request       string `json:"request"`
+	PodCount      int    `json:"podCount"`
+	TASConstraint string `json:"tasConstraint"` // "required", "preferred", "balanced"
+	TASLevel      string `json:"tasLevel"`
+	SliceSize     int32  `json:"sliceSize"` // for balanced placement - pods per slice
+}
+
+// WorkloadsSet defines a set of workloads to generate
+type WorkloadsSet struct {
+	Count              int                `json:"count"`
+	CreationIntervalMs uint               `json:"creationIntervalMs"`
+	Workloads          []WorkloadTemplate `json:"workloads"`
+}
+
+// QueuesSet defines a set of queues to generate
+type QueuesSet struct {
+	ClassName           string                 `json:"className"`
+	Count               int                    `json:"count"`
+	NominalQuota        string                 `json:"nominalQuota"`
+	BorrowingLimit      string                 `json:"borrowingLimit"`
+	ReclaimWithinCohort kueue.PreemptionPolicy `json:"reclaimWithinCohort"`
+	WithinClusterQueue  kueue.PreemptionPolicy `json:"withinClusterQueue"`
+	WorkloadsSets       []WorkloadsSet         `json:"workloadsSets"`
+}
+
+// CohortSet defines a set of cohorts to generate
+type CohortSet struct {
+	ClassName  string      `json:"className"`
+	Count      int         `json:"count"`
+	QueuesSets []QueuesSet `json:"queuesSets"`
+}
+
+// Config represents the full generator configuration
+type Config struct {
+	Topology       TopologyConfig       `json:"topology"`
+	ResourceFlavor ResourceFlavorConfig `json:"resourceFlavor"`
+	Cohorts        []CohortSet          `json:"cohorts"`
+}
+
+// LoadConfig loads the generator configuration from file
+func LoadConfig(configFile string) (*Config, error) {
+	yamlFile, err := os.ReadFile(configFile)
+	if err != nil {
+		return nil, fmt.Errorf("reading config file: %w", err)
+	}
+
+	config := &Config{}
+	if err := yaml.Unmarshal(yamlFile, config); err != nil {
+		return nil, fmt.Errorf("parsing config: %w", err)
+	}
+
+	return config, nil
+}
+
+// Generate creates all resources (topology, nodes, queues, workloads)
+func Generate(ctx context.Context, c client.Client, config *Config) error {
+	log := ctrl.LoggerFrom(ctx).WithName("generate")
+	log.Info("Start generation")
+	defer log.Info("End generation")
+
+	// 1. Generate topology infrastructure
+	if err := GenerateTopologyNodes(ctx, c, config.Topology); err != nil {
+		return fmt.Errorf("generating topology nodes: %w", err)
+	}
+
+	if err := GenerateTopologyResource(ctx, c, config.Topology); err != nil {
+		return fmt.Errorf("generating topology resource: %w", err)
+	}
+
+	if err := GenerateResourceFlavor(ctx, c, config.ResourceFlavor, config.Topology.Name); err != nil {
+		return fmt.Errorf("generating resource flavor: %w", err)
+	}
+
+	// 2. Generate cohorts, queues, and workloads
+	return concurrent(config.Cohorts, func(cs []CohortSet) int { return len(cs) }, func(idx int) error {
+		return generateCohortSet(ctx, c, config.Cohorts[idx], config.ResourceFlavor.Name)
+	})
+}
+
+func concurrent[T any](set T, count func(T) int, call func(int) error) error {
+	wg := sync.WaitGroup{}
+	errCh := make(chan error)
+	var errs []error
+	go func() {
+		for err := range errCh {
+			errs = append(errs, err)
+		}
+	}()
+	total := count(set)
+	wg.Add(total)
+	for i := range total {
+		go func() {
+			defer wg.Done()
+			err := call(i)
+			if err != nil {
+				errCh <- err
+			}
+		}()
+	}
+	wg.Wait()
+	close(errCh)
+	return errors.Join(errs...)
+}
+
+func generateWlSet(ctx context.Context, c client.Client, wlSet WorkloadsSet, namespace string, localQueue kueue.LocalQueueName, wlSetIdx int) error {
+	delay := time.Duration(wlSet.CreationIntervalMs) * time.Millisecond
+	log := ctrl.LoggerFrom(ctx).WithName("generate workload group").WithValues("namespace", namespace, "localQueue", localQueue, "delay", delay)
+	log.Info("Start generation")
+	defer log.Info("End generation")
+
+	for si := range wlSet.Count {
+		for i, wlt := range wlSet.Workloads {
+			<-time.After(delay)
+
+			// Create workload with TAS support
+			wlName := fmt.Sprintf("%s-%d-%d-%d", wlt.ClassName, wlSetIdx, si, i)
+
+			// Use podCount if specified, otherwise default to 1
+			podCount := wlt.PodCount
+			if podCount == 0 {
+				podCount = 1
+			}
+
+			podSetBuilder := utiltestingapi.MakePodSet("main", podCount).
+				Request(corev1.ResourceCPU, wlt.Request)
+
+			// Add TAS topology request if specified
+			if wlt.TASLevel != "" {
+				switch wlt.TASConstraint {
+				case "required":
+					podSetBuilder = podSetBuilder.RequiredTopologyRequest(wlt.TASLevel)
+				case "preferred":
+					podSetBuilder = podSetBuilder.PreferredTopologyRequest(wlt.TASLevel)
+				case "balanced":
+					// Balanced placement uses slice-based topology
+					podSetBuilder = podSetBuilder.SliceRequiredTopologyRequest(wlt.TASLevel)
+					if wlt.SliceSize > 0 {
+						podSetBuilder = podSetBuilder.SliceSizeTopologyRequest(wlt.SliceSize)
+					}
+				}
+			}
+
+			wl := utiltestingapi.MakeWorkload(wlName, namespace).
+				Queue(localQueue).
+				PodSets(*podSetBuilder.Obj()).
+				Request(corev1.ResourceCPU, wlt.Request).
+				Label(RunningTimeLabel, fmt.Sprintf("%d", wlt.RuntimeMs)).
+				Label(ClassLabel, wlt.ClassName).
```

  ```suggestion
  				Label(ClassLabel, wlt.ClassName).
  				Label(CleanupLabel, "true").
  ```

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-02-01T10:23:24Z

- **test/performance/tas/runner/generator/generator.go:149** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-01T10:23:24Z
```diff
@@ -0,0 +1,289 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package generator
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"os"
+	"sync"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/yaml"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+)
+
+const (
+	RunningTimeLabel = "kueue.x-k8s.io/performance-scheduler-running-ms"
+	ClassLabel       = "kueue.x-k8s.io/performance-scheduler-class"
+	CleanupLabel     = "kueue.x-k8s.io/performance-scheduler-cleanup"
+)
+
+// WorkloadTemplate defines a workload to generate (extended for TAS)
+type WorkloadTemplate struct {
+	ClassName     string `json:"className"`
+	RuntimeMs     uint   `json:"runtimeMs"`
+	Priority      int32  `json:"priority"`
+	Request       string `json:"request"`
+	PodCount      int    `json:"podCount"`
+	TASConstraint string `json:"tasConstraint"` // "required", "preferred", "balanced"
+	TASLevel      string `json:"tasLevel"`
+	SliceSize     int32  `json:"sliceSize"` // for balanced placement - pods per slice
+}
+
+// WorkloadsSet defines a set of workloads to generate
+type WorkloadsSet struct {
+	Count              int                `json:"count"`
+	CreationIntervalMs uint               `json:"creationIntervalMs"`
+	Workloads          []WorkloadTemplate `json:"workloads"`
+}
+
+// QueuesSet defines a set of queues to generate
+type QueuesSet struct {
+	ClassName           string                 `json:"className"`
+	Count               int                    `json:"count"`
+	NominalQuota        string                 `json:"nominalQuota"`
+	BorrowingLimit      string                 `json:"borrowingLimit"`
+	ReclaimWithinCohort kueue.PreemptionPolicy `json:"reclaimWithinCohort"`
+	WithinClusterQueue  kueue.PreemptionPolicy `json:"withinClusterQueue"`
+	WorkloadsSets       []WorkloadsSet         `json:"workloadsSets"`
+}
+
+// CohortSet defines a set of cohorts to generate
+type CohortSet struct {
+	ClassName  string      `json:"className"`
+	Count      int         `json:"count"`
+	QueuesSets []QueuesSet `json:"queuesSets"`
+}
+
+// Config represents the full generator configuration
+type Config struct {
+	Topology       TopologyConfig       `json:"topology"`
+	ResourceFlavor ResourceFlavorConfig `json:"resourceFlavor"`
+	Cohorts        []CohortSet          `json:"cohorts"`
+}
+
+// LoadConfig loads the generator configuration from file
+func LoadConfig(configFile string) (*Config, error) {
+	yamlFile, err := os.ReadFile(configFile)
+	if err != nil {
+		return nil, fmt.Errorf("reading config file: %w", err)
+	}
+
+	config := &Config{}
+	if err := yaml.Unmarshal(yamlFile, config); err != nil {
+		return nil, fmt.Errorf("parsing config: %w", err)
+	}
+
+	return config, nil
+}
+
+// Generate creates all resources (topology, nodes, queues, workloads)
+func Generate(ctx context.Context, c client.Client, config *Config) error {
+	log := ctrl.LoggerFrom(ctx).WithName("generate")
+	log.Info("Start generation")
+	defer log.Info("End generation")
+
+	// 1. Generate topology infrastructure
+	if err := GenerateTopologyNodes(ctx, c, config.Topology); err != nil {
+		return fmt.Errorf("generating topology nodes: %w", err)
+	}
+
+	if err := GenerateTopologyResource(ctx, c, config.Topology); err != nil {
+		return fmt.Errorf("generating topology resource: %w", err)
+	}
+
+	if err := GenerateResourceFlavor(ctx, c, config.ResourceFlavor, config.Topology.Name); err != nil {
+		return fmt.Errorf("generating resource flavor: %w", err)
+	}
+
+	// 2. Generate cohorts, queues, and workloads
+	return concurrent(config.Cohorts, func(cs []CohortSet) int { return len(cs) }, func(idx int) error {
+		return generateCohortSet(ctx, c, config.Cohorts[idx], config.ResourceFlavor.Name)
+	})
+}
+
+func concurrent[T any](set T, count func(T) int, call func(int) error) error {
+	wg := sync.WaitGroup{}
+	errCh := make(chan error)
+	var errs []error
+	go func() {
+		for err := range errCh {
+			errs = append(errs, err)
+		}
+	}()
+	total := count(set)
+	wg.Add(total)
+	for i := range total {
+		go func() {
+			defer wg.Done()
+			err := call(i)
+			if err != nil {
+				errCh <- err
+			}
+		}()
+	}
+	wg.Wait()
+	close(errCh)
+	return errors.Join(errs...)
+}
```

  Let's collect all errors
  ```suggestion
  func concurrent[T any](set T, count func(T) int, call func(int) error) error {
  	wg := sync.WaitGroup{}
  	errCh := make(chan error)
  	var errs []error
  	done := make(chan struct{})
  	go func() {
  		for err := range errCh {
  			errs = append(errs, err)
  		}
  		close(done)
  	}()
  	total := count(set)
  	wg.Add(total)
  	for i := range total {
  		go func() {
  			defer wg.Done()
  			err := call(i)
  			if err != nil {
  				errCh <- err
  			}
  		}()
  	}
  	wg.Wait()
  	close(errCh)
  	<-done
  	return errors.Join(errs...)
  }
  ```

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-02-01T10:31:46Z

- **test/performance/tas/runner/main.go:290** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-01T10:31:46Z
```diff
@@ -0,0 +1,448 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package main
+
+import (
+	"context"
+	"flag"
+	"fmt"
+	"os"
+	"os/exec"
+	"os/signal"
+	"path"
+	"path/filepath"
+	"strconv"
+	"sync"
+	"syscall"
+	"time"
+
+	"github.com/onsi/gomega"
+	zaplog "go.uber.org/zap"
+	"go.uber.org/zap/zapcore"
+	"k8s.io/apimachinery/pkg/runtime"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	clientgoscheme "k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/client-go/rest"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	crconfig "sigs.k8s.io/controller-runtime/pkg/config"
+	"sigs.k8s.io/controller-runtime/pkg/envtest"
+	"sigs.k8s.io/controller-runtime/pkg/log/zap"
+	metricsserver "sigs.k8s.io/controller-runtime/pkg/metrics/server"
+	"sigs.k8s.io/yaml"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/performance/scheduler/runner/controller"
+	"sigs.k8s.io/kueue/test/performance/scheduler/runner/recorder"
+	"sigs.k8s.io/kueue/test/performance/scheduler/runner/scraper"
+	"sigs.k8s.io/kueue/test/performance/scheduler/runner/stats"
+	"sigs.k8s.io/kueue/test/performance/tas/runner/generator"
+)
+
+var (
+	outputDir       = flag.String("o", "", "output directory")
+	crdsPath        = flag.String("crds", "", "crds path")
+	generatorConfig = flag.String("generatorConfig", "", "generator config file")
+	timeout         = flag.Duration("timeout", 10*time.Minute, "maximum record time")
+	qps             = flag.Float64("qps", 0, "qps used by the runner clients, use default if 0")
+	burst           = flag.Int("burst", 0, "burst used by the runner clients, use default if 0")
+
+	// metrics scraping
+	metricsScrapeInterval = flag.Duration("metricsScrapeInterval", 0, "the duration between two metrics scraping, if 0 the metrics scraping is disabled")
+	metricsScrapeURL      = flag.String("metricsScrapeURL", "", "the URL to scrape metrics from, ignored when minimal kueue is used")
+
+	// related to minimalkueue
+	minimalKueuePath = flag.String("minimalKueue", "", "path to minimalkueue, run in the hosts default cluster if empty")
+	withCPUProfile   = flag.Bool("withCPUProfile", false, "generate a CPU profile for minimalkueue")
+	withLogs         = flag.Bool("withLogs", false, "capture minimalkueue logs")
+	logLevel         = flag.Int("withLogsLevel", 2, "set minimalkueue logs level")
+	logToFile        = flag.Bool("logToFile", false, "capture minimalkueue logs to files")
+)
+
+var (
+	scheme = runtime.NewScheme()
+)
+
+func init() {
+	utilruntime.Must(clientgoscheme.AddToScheme(scheme))
+	utilruntime.Must(kueue.AddToScheme(scheme))
+	utilruntime.Must(kueuealpha.AddToScheme(scheme))
+	utilruntime.Must(configapi.AddToScheme(scheme))
+}
+
+func main() {
+	opts := zap.Options{
+		TimeEncoder: zapcore.RFC3339NanoTimeEncoder,
+		ZapOpts:     []zaplog.Option{zaplog.AddCaller()},
+		Development: true,
+		Level:       zaplog.NewAtomicLevelAt(-2),
+	}
+	opts.BindFlags(flag.CommandLine)
+	flag.Parse()
+	log := zap.New(zap.UseFlagOptions(&opts))
+
+	ctrl.SetLogger(log)
+
+	// Register Gomega fail handler to allow test utilities to work in standalone binary
+	gomega.RegisterFailHandler(func(message string, callerSkip ...int) {
+		log.Error(nil, message)
+		os.Exit(1)
+	})
+
+	log.Info("Start TAS performance test runner", "outputDir", outputDir, "crdsPath", crdsPath)
+	errCh := make(chan error, 3)
+	wg := &sync.WaitGroup{}
+	ctx, ctxCancel := context.WithCancel(ctrl.LoggerInto(context.Background(), log))
+	var cfg *rest.Config
+
+	if *minimalKueuePath != "" {
+		testEnv := &envtest.Environment{
+			CRDDirectoryPaths:     []string{*crdsPath},
+			ErrorIfCRDPathMissing: true,
+		}
+
+		// Increase API server request limits to handle high load from TAS performance tests.
+		testEnv.ControlPlane.GetAPIServer().Configure().
+			Append("max-requests-inflight", "800").
+			Append("max-mutating-requests-inflight", "400")
+
+		var err error
+		cfg, err = testEnv.Start()
+		if err != nil {
+			log.Error(err, "Starting test env")
+			os.Exit(1)
+		}
+		defer func() {
+			err := testEnv.Stop()
+			if err != nil {
+				log.Error(err, "Stopping test env")
+			}
+		}()
+
+		// prepare the kubeconfig
+		kubeconfig, err := utiltesting.RestConfigToKubeConfig(cfg)
+		if err != nil {
+			log.Error(err, "Generate kubeconfig")
+			os.Exit(1)
+		}
+
+		kubeconfigPath := filepath.Join(*outputDir, "kubeconfig")
+		err = os.WriteFile(kubeconfigPath, kubeconfig, 00660)
+		if err != nil {
+			log.Error(err, "Write kubeconfig")
+			os.Exit(1)
+		}
+
+		metricsPort := 0
+		if *metricsScrapeInterval != 0 {
+			metricsPort, err = scraper.GetFreePort()
+			if err != nil {
+				log.Error(err, "getting a free port, metrics scraping disabled")
+			}
+			metricsScrapeURL = ptr.To(fmt.Sprintf("http://localhost:%d/metrics", metricsPort))
+		}
+
+		// start the minimal kueue manager process
+		err = runCommand(ctx, *outputDir, *minimalKueuePath, "kubeconfig", *withCPUProfile, *withLogs, *logToFile, *logLevel, errCh, wg, metricsPort)
+		if err != nil {
+			log.Error(err, "MinimalKueue start")
+			os.Exit(1)
+		}
+	} else {
+		var err error
+		cfg, err = ctrl.GetConfig()
+		if err != nil {
+			log.Error(err, "Get config")
+			os.Exit(1)
+		}
+	}
+
+	if *qps > 0 {
+		cfg.QPS = float32(*qps)
+	}
+
+	if *burst > 0 {
+		cfg.Burst = *burst
+	}
+
+	generationDoneCh := make(chan struct{})
+	err := runGenerator(ctx, cfg, *generatorConfig, errCh, wg, generationDoneCh)
+	if err != nil {
+		log.Error(err, "Generator start")
+		os.Exit(1)
+	}
+
+	recorder, err := startRecorder(ctx, errCh, wg, generationDoneCh, *timeout)
+	if err != nil {
+		log.Error(err, "Recorder start")
+		os.Exit(1)
+	}
+
+	if *metricsScrapeInterval != 0 && *metricsScrapeURL != "" {
+		dumpTar := filepath.Join(*outputDir, "metricsDump.tgz")
+		err := runScraper(ctx, *metricsScrapeInterval, dumpTar, *metricsScrapeURL, errCh, wg)
+		if err != nil {
+			log.Error(err, "Scraper start")
+			os.Exit(1)
+		}
+	}
+
+	err = runManager(ctx, cfg, errCh, wg, recorder)
+	if err != nil {
+		log.Error(err, "Failed to start manager")
+		os.Exit(1)
+	}
+
+	done := make(chan os.Signal, 1)
+	signal.Notify(done, syscall.SIGINT, syscall.SIGTERM)
+	endWithError := false
+	select {
+	case <-done:
+		log.Info("Done")
+	case err := <-errCh:
+		if err != nil {
+			log.Error(err, "Error")
+			endWithError = true
+		}
+	}
+	ctxCancel()
+	wg.Wait()
+
+	if endWithError {
+		os.Exit(1)
+	}
+
+	err = recorder.WriteSummary(filepath.Join(*outputDir, "summary.yaml"))
+	if err != nil {
+		log.Error(err, "Writing summary")
+		os.Exit(1)
+	}
+	err = recorder.WriteCQCsv(filepath.Join(*outputDir, "cqStates.csv"))
+	if err != nil {
+		log.Error(err, "Writing cq csv")
+		os.Exit(1)
+	}
+	err = recorder.WriteWLCsv(filepath.Join(*outputDir, "wlStates.csv"))
+	if err != nil {
+		log.Error(err, "Writing wl csv")
+		os.Exit(1)
+	}
+
+	if *minimalKueuePath == "" {
+		c, err := client.New(cfg, client.Options{Scheme: scheme})
+		if err != nil {
+			log.Error(err, "Create cleanup client")
+		} else {
+			generator.Cleanup(context.Background(), c)
+		}
+	}
+}
+
+func runCommand(ctx context.Context, workDir, cmdPath, kubeconfig string, withCPUProf, withLogs, logToFile bool, logLevel int, errCh chan<- error, wg *sync.WaitGroup, metricsPort int) error {
+	log := ctrl.LoggerFrom(ctx).WithName("Run command")
+
+	cmd := exec.CommandContext(ctx, cmdPath, "--kubeconfig", filepath.Join(workDir, kubeconfig))
+	cmd.Cancel = func() error {
+		log.Info("Stop the command")
+		return cmd.Process.Signal(syscall.SIGINT)
+	}
+
+	exe := path.Base(cmdPath)
+
+	if withCPUProf {
+		cmd.Args = append(cmd.Args, "--cpuprofile", filepath.Join(workDir, fmt.Sprintf("%s.cpu.prof", exe)))
+	}
+
+	if withLogs {
+		cmd.Args = append(cmd.Args, fmt.Sprintf("--zap-log-level=%d", logLevel))
+		outWriter := os.Stdout
+		errWriter := os.Stderr
+		if logToFile {
+			var err error
+			outWriter, err = os.Create(filepath.Join(workDir, fmt.Sprintf("%s.out.log", exe)))
+			if err != nil {
+				return err
+			}
+			defer outWriter.Close()
+
+			errWriter, err = os.Create(filepath.Join(workDir, fmt.Sprintf("%s.err.log", exe)))
+			if err != nil {
+				return err
+			}
+			defer errWriter.Close()
```

  Move outWriter.Close() and errWriter.Close() to wg.Go below
  These file handles will be closed while the subprocess is still writing to them, causing writes to fail or panic.

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-02-01T10:32:37Z

- **test/performance/tas/checker/checker_test.go:96** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-01T10:32:37Z
```diff
@@ -0,0 +1,122 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package checker
+
+import (
+	"flag"
+	"os"
+	"testing"
+
+	"sigs.k8s.io/yaml"
+
+	"sigs.k8s.io/kueue/test/performance/scheduler/runner/recorder"
+	"sigs.k8s.io/kueue/test/performance/scheduler/runner/stats"
+)
+
+var (
+	summaryFile  = flag.String("summary", "", "the runner summary report")
+	cmdStatsFile = flag.String("cmdStats", "", "command stats yaml file")
+	rangeFile    = flag.String("range", "", "expectations range file")
+)
+
+type RangeSpec struct {
+	Cmd struct {
+		MaxWallMs int64 `json:"maxWallMs"`
+	} `json:"cmd"`
+	ClusterQueueClassesMinUsage      map[string]float64 `json:"clusterQueueClassesMinUsage"`
+	WlClassesMaxAvgTimeToAdmissionMs map[string]int64   `json:"wlClassesMaxAvgTimeToAdmissionMs"`
+}
+
+func TestPerformanceTAS(t *testing.T) {
+	summaryBytes, err := os.ReadFile(*summaryFile)
+	if err != nil {
+		t.Fatalf("Unable to read summary: %s", err)
+	}
+
+	summary := recorder.Summary{}
+
+	err = yaml.UnmarshalStrict(summaryBytes, &summary)
+	if err != nil {
+		t.Fatalf("Unable to unmarshal summary: %s", err)
+	}
+
+	cmdStatsBytes, err := os.ReadFile(*cmdStatsFile)
+	if err != nil {
+		t.Fatalf("Unable to read command stats: %s", err)
+	}
+
+	cmdStats := stats.CmdStats{}
+	err = yaml.UnmarshalStrict(cmdStatsBytes, &cmdStats)
+	if err != nil {
+		t.Fatalf("Unable to unmarshal command stats: %s", err)
+	}
+
+	rangeBytes, err := os.ReadFile(*rangeFile)
+	if err != nil {
+		t.Fatalf("Unable to read range spec: %s", err)
+	}
+
+	rangeSpec := RangeSpec{}
+	err = yaml.UnmarshalStrict(rangeBytes, &rangeSpec)
+	if err != nil {
+		t.Fatalf("Unable to unmarshal range spec: %s", err)
+	}
+
+	t.Run("CommandStats", func(t *testing.T) {
+		if cmdStats.WallMs > rangeSpec.Cmd.MaxWallMs {
+			t.Errorf("Wall time %dms is greater than maximum expected %dms", cmdStats.WallMs, rangeSpec.Cmd.MaxWallMs)
+		}
+		t.Logf("Wall time: %dms (max: %dms)", cmdStats.WallMs, rangeSpec.Cmd.MaxWallMs)
+		t.Logf("User time: %dms", cmdStats.UserMs)
+		t.Logf("System time: %dms", cmdStats.SysMs)
+		t.Logf("Max RSS: %d", cmdStats.Maxrss)
+	})
+
+	t.Run("ClusterQueueClasses", func(t *testing.T) {
+		for c, cqcSummary := range summary.ClusterQueueClasses {
+			t.Run(c, func(t *testing.T) {
+				expected, found := rangeSpec.ClusterQueueClassesMinUsage[c]
+				if !found {
+					t.Fatalf("Unexpected class")
+				}
+				actual := float64(cqcSummary.CPUUsed) * 100 / (float64(cqcSummary.NominalQuota) * float64(cqcSummary.LastEventTime.Sub(cqcSummary.FirstEventTime).Milliseconds()))
```

  ```suggestion
  				elapsedMs := cqcSummary.LastEventTime.Sub(cqcSummary.FirstEventTime).Milliseconds()
  			if cqcSummary.NominalQuota == 0 || elapsedMs == 0 {
  				t.Fatalf("Invalid data: NominalQuota=%d, elapsedMs=%d", cqcSummary.NominalQuota, elapsedMs)
  			}
  			actual := float64(cqcSummary.CPUUsed) * 100 / (float64(cqcSummary.NominalQuota) * float64(elapsedMs))
  ```

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-02-01T10:34:02Z

- **test/performance/tas/minimalkueue/main.go:99** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-01T10:34:02Z
```diff
@@ -0,0 +1,209 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package main
+
+import (
+	"context"
+	"flag"
+	"fmt"
+	"os"
+	"os/signal"
+	"runtime/pprof"
+	"syscall"
+
+	zaplog "go.uber.org/zap"
+	"go.uber.org/zap/zapcore"
+	"k8s.io/apimachinery/pkg/runtime"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	clientgoscheme "k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	crconfig "sigs.k8s.io/controller-runtime/pkg/config"
+	"sigs.k8s.io/controller-runtime/pkg/log/zap"
+	metricsserver "sigs.k8s.io/controller-runtime/pkg/metrics/server"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	schdcache "sigs.k8s.io/kueue/pkg/cache/scheduler"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	"sigs.k8s.io/kueue/pkg/controller/tas"
+	tasindexer "sigs.k8s.io/kueue/pkg/controller/tas/indexer"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/scheduler"
+)
+
+var (
+	cpuprofile = flag.String("cpuprofile", "", "write cpu profile to `file`")
+
+	metricsPort = flag.Int("metricsPort", 0, "metrics serving port")
+)
+
+var (
+	scheme = runtime.NewScheme()
+)
+
+func init() {
+	utilruntime.Must(clientgoscheme.AddToScheme(scheme))
+	utilruntime.Must(kueue.AddToScheme(scheme))
+	utilruntime.Must(kueuealpha.AddToScheme(scheme))
+	utilruntime.Must(configapi.AddToScheme(scheme))
+}
+
+func main() {
+	initFlags()
+	flag.Parse()
+	os.Exit(run())
+}
+
+var logOptions = zap.Options{
+	TimeEncoder: zapcore.RFC3339NanoTimeEncoder,
+	ZapOpts:     []zaplog.Option{zaplog.AddCaller()},
+	Development: true,
+	Level:       zaplog.NewAtomicLevelAt(zapcore.ErrorLevel),
+}
+
+func initFlags() {
+	logOptions.BindFlags(flag.CommandLine)
+}
+
+func run() int {
+	log := zap.New(zap.UseFlagOptions(&logOptions))
+	ctrl.SetLogger(log)
+	log.Info("Start minimalkueue-tas")
+
+	if *cpuprofile != "" {
+		f, err := os.Create(*cpuprofile)
+		if err != nil {
+			log.Error(err, "Could not create CPU profile")
+		}
+		defer f.Close() // error handling omitted for example
+		if err := pprof.StartCPUProfile(f); err != nil {
+			log.Error(err, "Could not start CPU profile")
```

  ```suggestion
  			log.Error(err, "Could not start CPU profile")
  			return 1
  ```

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-02-01T10:34:09Z

- **test/performance/tas/minimalkueue/main.go:95** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-01T10:34:09Z
```diff
@@ -0,0 +1,209 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package main
+
+import (
+	"context"
+	"flag"
+	"fmt"
+	"os"
+	"os/signal"
+	"runtime/pprof"
+	"syscall"
+
+	zaplog "go.uber.org/zap"
+	"go.uber.org/zap/zapcore"
+	"k8s.io/apimachinery/pkg/runtime"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	clientgoscheme "k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	crconfig "sigs.k8s.io/controller-runtime/pkg/config"
+	"sigs.k8s.io/controller-runtime/pkg/log/zap"
+	metricsserver "sigs.k8s.io/controller-runtime/pkg/metrics/server"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	schdcache "sigs.k8s.io/kueue/pkg/cache/scheduler"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	"sigs.k8s.io/kueue/pkg/controller/tas"
+	tasindexer "sigs.k8s.io/kueue/pkg/controller/tas/indexer"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/scheduler"
+)
+
+var (
+	cpuprofile = flag.String("cpuprofile", "", "write cpu profile to `file`")
+
+	metricsPort = flag.Int("metricsPort", 0, "metrics serving port")
+)
+
+var (
+	scheme = runtime.NewScheme()
+)
+
+func init() {
+	utilruntime.Must(clientgoscheme.AddToScheme(scheme))
+	utilruntime.Must(kueue.AddToScheme(scheme))
+	utilruntime.Must(kueuealpha.AddToScheme(scheme))
+	utilruntime.Must(configapi.AddToScheme(scheme))
+}
+
+func main() {
+	initFlags()
+	flag.Parse()
+	os.Exit(run())
+}
+
+var logOptions = zap.Options{
+	TimeEncoder: zapcore.RFC3339NanoTimeEncoder,
+	ZapOpts:     []zaplog.Option{zaplog.AddCaller()},
+	Development: true,
+	Level:       zaplog.NewAtomicLevelAt(zapcore.ErrorLevel),
+}
+
+func initFlags() {
+	logOptions.BindFlags(flag.CommandLine)
+}
+
+func run() int {
+	log := zap.New(zap.UseFlagOptions(&logOptions))
+	ctrl.SetLogger(log)
+	log.Info("Start minimalkueue-tas")
+
+	if *cpuprofile != "" {
+		f, err := os.Create(*cpuprofile)
+		if err != nil {
+			log.Error(err, "Could not create CPU profile")
```

  ```suggestion
  			log.Error(err, "Could not create CPU profile")
  			return 1
  ```

### Review by [@ASverdlov](https://github.com/ASverdlov) (commented) — 2026-02-01T16:27:14Z

- **test/performance/tas/minimalkueue/main.go:95** by [@ASverdlov](https://github.com/ASverdlov) — 2026-02-01T16:27:14Z
```diff
@@ -0,0 +1,209 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package main
+
+import (
+	"context"
+	"flag"
+	"fmt"
+	"os"
+	"os/signal"
+	"runtime/pprof"
+	"syscall"
+
+	zaplog "go.uber.org/zap"
+	"go.uber.org/zap/zapcore"
+	"k8s.io/apimachinery/pkg/runtime"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	clientgoscheme "k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	crconfig "sigs.k8s.io/controller-runtime/pkg/config"
+	"sigs.k8s.io/controller-runtime/pkg/log/zap"
+	metricsserver "sigs.k8s.io/controller-runtime/pkg/metrics/server"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	schdcache "sigs.k8s.io/kueue/pkg/cache/scheduler"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	"sigs.k8s.io/kueue/pkg/controller/tas"
+	tasindexer "sigs.k8s.io/kueue/pkg/controller/tas/indexer"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/scheduler"
+)
+
+var (
+	cpuprofile = flag.String("cpuprofile", "", "write cpu profile to `file`")
+
+	metricsPort = flag.Int("metricsPort", 0, "metrics serving port")
+)
+
+var (
+	scheme = runtime.NewScheme()
+)
+
+func init() {
+	utilruntime.Must(clientgoscheme.AddToScheme(scheme))
+	utilruntime.Must(kueue.AddToScheme(scheme))
+	utilruntime.Must(kueuealpha.AddToScheme(scheme))
+	utilruntime.Must(configapi.AddToScheme(scheme))
+}
+
+func main() {
+	initFlags()
+	flag.Parse()
+	os.Exit(run())
+}
+
+var logOptions = zap.Options{
+	TimeEncoder: zapcore.RFC3339NanoTimeEncoder,
+	ZapOpts:     []zaplog.Option{zaplog.AddCaller()},
+	Development: true,
+	Level:       zaplog.NewAtomicLevelAt(zapcore.ErrorLevel),
+}
+
+func initFlags() {
+	logOptions.BindFlags(flag.CommandLine)
+}
+
+func run() int {
+	log := zap.New(zap.UseFlagOptions(&logOptions))
+	ctrl.SetLogger(log)
+	log.Info("Start minimalkueue-tas")
+
+	if *cpuprofile != "" {
+		f, err := os.Create(*cpuprofile)
+		if err != nil {
+			log.Error(err, "Could not create CPU profile")
```

  Done here and in `test/performance/integration`

### Review by [@ASverdlov](https://github.com/ASverdlov) (commented) — 2026-02-01T16:27:36Z

- **test/performance/tas/minimalkueue/main.go:99** by [@ASverdlov](https://github.com/ASverdlov) — 2026-02-01T16:27:36Z
```diff
@@ -0,0 +1,209 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package main
+
+import (
+	"context"
+	"flag"
+	"fmt"
+	"os"
+	"os/signal"
+	"runtime/pprof"
+	"syscall"
+
+	zaplog "go.uber.org/zap"
+	"go.uber.org/zap/zapcore"
+	"k8s.io/apimachinery/pkg/runtime"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	clientgoscheme "k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	crconfig "sigs.k8s.io/controller-runtime/pkg/config"
+	"sigs.k8s.io/controller-runtime/pkg/log/zap"
+	metricsserver "sigs.k8s.io/controller-runtime/pkg/metrics/server"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	schdcache "sigs.k8s.io/kueue/pkg/cache/scheduler"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	"sigs.k8s.io/kueue/pkg/controller/tas"
+	tasindexer "sigs.k8s.io/kueue/pkg/controller/tas/indexer"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/scheduler"
+)
+
+var (
+	cpuprofile = flag.String("cpuprofile", "", "write cpu profile to `file`")
+
+	metricsPort = flag.Int("metricsPort", 0, "metrics serving port")
+)
+
+var (
+	scheme = runtime.NewScheme()
+)
+
+func init() {
+	utilruntime.Must(clientgoscheme.AddToScheme(scheme))
+	utilruntime.Must(kueue.AddToScheme(scheme))
+	utilruntime.Must(kueuealpha.AddToScheme(scheme))
+	utilruntime.Must(configapi.AddToScheme(scheme))
+}
+
+func main() {
+	initFlags()
+	flag.Parse()
+	os.Exit(run())
+}
+
+var logOptions = zap.Options{
+	TimeEncoder: zapcore.RFC3339NanoTimeEncoder,
+	ZapOpts:     []zaplog.Option{zaplog.AddCaller()},
+	Development: true,
+	Level:       zaplog.NewAtomicLevelAt(zapcore.ErrorLevel),
+}
+
+func initFlags() {
+	logOptions.BindFlags(flag.CommandLine)
+}
+
+func run() int {
+	log := zap.New(zap.UseFlagOptions(&logOptions))
+	ctrl.SetLogger(log)
+	log.Info("Start minimalkueue-tas")
+
+	if *cpuprofile != "" {
+		f, err := os.Create(*cpuprofile)
+		if err != nil {
+			log.Error(err, "Could not create CPU profile")
+		}
+		defer f.Close() // error handling omitted for example
+		if err := pprof.StartCPUProfile(f); err != nil {
+			log.Error(err, "Could not start CPU profile")
```

  Done here and in `test/performance/integration`

### Review by [@ASverdlov](https://github.com/ASverdlov) (commented) — 2026-02-01T16:27:45Z

- **test/performance/tas/checker/checker_test.go:96** by [@ASverdlov](https://github.com/ASverdlov) — 2026-02-01T16:27:44Z
```diff
@@ -0,0 +1,122 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package checker
+
+import (
+	"flag"
+	"os"
+	"testing"
+
+	"sigs.k8s.io/yaml"
+
+	"sigs.k8s.io/kueue/test/performance/scheduler/runner/recorder"
+	"sigs.k8s.io/kueue/test/performance/scheduler/runner/stats"
+)
+
+var (
+	summaryFile  = flag.String("summary", "", "the runner summary report")
+	cmdStatsFile = flag.String("cmdStats", "", "command stats yaml file")
+	rangeFile    = flag.String("range", "", "expectations range file")
+)
+
+type RangeSpec struct {
+	Cmd struct {
+		MaxWallMs int64 `json:"maxWallMs"`
+	} `json:"cmd"`
+	ClusterQueueClassesMinUsage      map[string]float64 `json:"clusterQueueClassesMinUsage"`
+	WlClassesMaxAvgTimeToAdmissionMs map[string]int64   `json:"wlClassesMaxAvgTimeToAdmissionMs"`
+}
+
+func TestPerformanceTAS(t *testing.T) {
+	summaryBytes, err := os.ReadFile(*summaryFile)
+	if err != nil {
+		t.Fatalf("Unable to read summary: %s", err)
+	}
+
+	summary := recorder.Summary{}
+
+	err = yaml.UnmarshalStrict(summaryBytes, &summary)
+	if err != nil {
+		t.Fatalf("Unable to unmarshal summary: %s", err)
+	}
+
+	cmdStatsBytes, err := os.ReadFile(*cmdStatsFile)
+	if err != nil {
+		t.Fatalf("Unable to read command stats: %s", err)
+	}
+
+	cmdStats := stats.CmdStats{}
+	err = yaml.UnmarshalStrict(cmdStatsBytes, &cmdStats)
+	if err != nil {
+		t.Fatalf("Unable to unmarshal command stats: %s", err)
+	}
+
+	rangeBytes, err := os.ReadFile(*rangeFile)
+	if err != nil {
+		t.Fatalf("Unable to read range spec: %s", err)
+	}
+
+	rangeSpec := RangeSpec{}
+	err = yaml.UnmarshalStrict(rangeBytes, &rangeSpec)
+	if err != nil {
+		t.Fatalf("Unable to unmarshal range spec: %s", err)
+	}
+
+	t.Run("CommandStats", func(t *testing.T) {
+		if cmdStats.WallMs > rangeSpec.Cmd.MaxWallMs {
+			t.Errorf("Wall time %dms is greater than maximum expected %dms", cmdStats.WallMs, rangeSpec.Cmd.MaxWallMs)
+		}
+		t.Logf("Wall time: %dms (max: %dms)", cmdStats.WallMs, rangeSpec.Cmd.MaxWallMs)
+		t.Logf("User time: %dms", cmdStats.UserMs)
+		t.Logf("System time: %dms", cmdStats.SysMs)
+		t.Logf("Max RSS: %d", cmdStats.Maxrss)
+	})
+
+	t.Run("ClusterQueueClasses", func(t *testing.T) {
+		for c, cqcSummary := range summary.ClusterQueueClasses {
+			t.Run(c, func(t *testing.T) {
+				expected, found := rangeSpec.ClusterQueueClassesMinUsage[c]
+				if !found {
+					t.Fatalf("Unexpected class")
+				}
+				actual := float64(cqcSummary.CPUUsed) * 100 / (float64(cqcSummary.NominalQuota) * float64(cqcSummary.LastEventTime.Sub(cqcSummary.FirstEventTime).Milliseconds()))
```

  Done here and in `test/performance/integration`

### Review by [@ASverdlov](https://github.com/ASverdlov) (commented) — 2026-02-01T16:31:09Z

- **test/performance/tas/runner/main.go:290** by [@ASverdlov](https://github.com/ASverdlov) — 2026-02-01T16:31:09Z
```diff
@@ -0,0 +1,448 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package main
+
+import (
+	"context"
+	"flag"
+	"fmt"
+	"os"
+	"os/exec"
+	"os/signal"
+	"path"
+	"path/filepath"
+	"strconv"
+	"sync"
+	"syscall"
+	"time"
+
+	"github.com/onsi/gomega"
+	zaplog "go.uber.org/zap"
+	"go.uber.org/zap/zapcore"
+	"k8s.io/apimachinery/pkg/runtime"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	clientgoscheme "k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/client-go/rest"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	crconfig "sigs.k8s.io/controller-runtime/pkg/config"
+	"sigs.k8s.io/controller-runtime/pkg/envtest"
+	"sigs.k8s.io/controller-runtime/pkg/log/zap"
+	metricsserver "sigs.k8s.io/controller-runtime/pkg/metrics/server"
+	"sigs.k8s.io/yaml"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/performance/scheduler/runner/controller"
+	"sigs.k8s.io/kueue/test/performance/scheduler/runner/recorder"
+	"sigs.k8s.io/kueue/test/performance/scheduler/runner/scraper"
+	"sigs.k8s.io/kueue/test/performance/scheduler/runner/stats"
+	"sigs.k8s.io/kueue/test/performance/tas/runner/generator"
+)
+
+var (
+	outputDir       = flag.String("o", "", "output directory")
+	crdsPath        = flag.String("crds", "", "crds path")
+	generatorConfig = flag.String("generatorConfig", "", "generator config file")
+	timeout         = flag.Duration("timeout", 10*time.Minute, "maximum record time")
+	qps             = flag.Float64("qps", 0, "qps used by the runner clients, use default if 0")
+	burst           = flag.Int("burst", 0, "burst used by the runner clients, use default if 0")
+
+	// metrics scraping
+	metricsScrapeInterval = flag.Duration("metricsScrapeInterval", 0, "the duration between two metrics scraping, if 0 the metrics scraping is disabled")
+	metricsScrapeURL      = flag.String("metricsScrapeURL", "", "the URL to scrape metrics from, ignored when minimal kueue is used")
+
+	// related to minimalkueue
+	minimalKueuePath = flag.String("minimalKueue", "", "path to minimalkueue, run in the hosts default cluster if empty")
+	withCPUProfile   = flag.Bool("withCPUProfile", false, "generate a CPU profile for minimalkueue")
+	withLogs         = flag.Bool("withLogs", false, "capture minimalkueue logs")
+	logLevel         = flag.Int("withLogsLevel", 2, "set minimalkueue logs level")
+	logToFile        = flag.Bool("logToFile", false, "capture minimalkueue logs to files")
+)
+
+var (
+	scheme = runtime.NewScheme()
+)
+
+func init() {
+	utilruntime.Must(clientgoscheme.AddToScheme(scheme))
+	utilruntime.Must(kueue.AddToScheme(scheme))
+	utilruntime.Must(kueuealpha.AddToScheme(scheme))
+	utilruntime.Must(configapi.AddToScheme(scheme))
+}
+
+func main() {
+	opts := zap.Options{
+		TimeEncoder: zapcore.RFC3339NanoTimeEncoder,
+		ZapOpts:     []zaplog.Option{zaplog.AddCaller()},
+		Development: true,
+		Level:       zaplog.NewAtomicLevelAt(-2),
+	}
+	opts.BindFlags(flag.CommandLine)
+	flag.Parse()
+	log := zap.New(zap.UseFlagOptions(&opts))
+
+	ctrl.SetLogger(log)
+
+	// Register Gomega fail handler to allow test utilities to work in standalone binary
+	gomega.RegisterFailHandler(func(message string, callerSkip ...int) {
+		log.Error(nil, message)
+		os.Exit(1)
+	})
+
+	log.Info("Start TAS performance test runner", "outputDir", outputDir, "crdsPath", crdsPath)
+	errCh := make(chan error, 3)
+	wg := &sync.WaitGroup{}
+	ctx, ctxCancel := context.WithCancel(ctrl.LoggerInto(context.Background(), log))
+	var cfg *rest.Config
+
+	if *minimalKueuePath != "" {
+		testEnv := &envtest.Environment{
+			CRDDirectoryPaths:     []string{*crdsPath},
+			ErrorIfCRDPathMissing: true,
+		}
+
+		// Increase API server request limits to handle high load from TAS performance tests.
+		testEnv.ControlPlane.GetAPIServer().Configure().
+			Append("max-requests-inflight", "800").
+			Append("max-mutating-requests-inflight", "400")
+
+		var err error
+		cfg, err = testEnv.Start()
+		if err != nil {
+			log.Error(err, "Starting test env")
+			os.Exit(1)
+		}
+		defer func() {
+			err := testEnv.Stop()
+			if err != nil {
+				log.Error(err, "Stopping test env")
+			}
+		}()
+
+		// prepare the kubeconfig
+		kubeconfig, err := utiltesting.RestConfigToKubeConfig(cfg)
+		if err != nil {
+			log.Error(err, "Generate kubeconfig")
+			os.Exit(1)
+		}
+
+		kubeconfigPath := filepath.Join(*outputDir, "kubeconfig")
+		err = os.WriteFile(kubeconfigPath, kubeconfig, 00660)
+		if err != nil {
+			log.Error(err, "Write kubeconfig")
+			os.Exit(1)
+		}
+
+		metricsPort := 0
+		if *metricsScrapeInterval != 0 {
+			metricsPort, err = scraper.GetFreePort()
+			if err != nil {
+				log.Error(err, "getting a free port, metrics scraping disabled")
+			}
+			metricsScrapeURL = ptr.To(fmt.Sprintf("http://localhost:%d/metrics", metricsPort))
+		}
+
+		// start the minimal kueue manager process
+		err = runCommand(ctx, *outputDir, *minimalKueuePath, "kubeconfig", *withCPUProfile, *withLogs, *logToFile, *logLevel, errCh, wg, metricsPort)
+		if err != nil {
+			log.Error(err, "MinimalKueue start")
+			os.Exit(1)
+		}
+	} else {
+		var err error
+		cfg, err = ctrl.GetConfig()
+		if err != nil {
+			log.Error(err, "Get config")
+			os.Exit(1)
+		}
+	}
+
+	if *qps > 0 {
+		cfg.QPS = float32(*qps)
+	}
+
+	if *burst > 0 {
+		cfg.Burst = *burst
+	}
+
+	generationDoneCh := make(chan struct{})
+	err := runGenerator(ctx, cfg, *generatorConfig, errCh, wg, generationDoneCh)
+	if err != nil {
+		log.Error(err, "Generator start")
+		os.Exit(1)
+	}
+
+	recorder, err := startRecorder(ctx, errCh, wg, generationDoneCh, *timeout)
+	if err != nil {
+		log.Error(err, "Recorder start")
+		os.Exit(1)
+	}
+
+	if *metricsScrapeInterval != 0 && *metricsScrapeURL != "" {
+		dumpTar := filepath.Join(*outputDir, "metricsDump.tgz")
+		err := runScraper(ctx, *metricsScrapeInterval, dumpTar, *metricsScrapeURL, errCh, wg)
+		if err != nil {
+			log.Error(err, "Scraper start")
+			os.Exit(1)
+		}
+	}
+
+	err = runManager(ctx, cfg, errCh, wg, recorder)
+	if err != nil {
+		log.Error(err, "Failed to start manager")
+		os.Exit(1)
+	}
+
+	done := make(chan os.Signal, 1)
+	signal.Notify(done, syscall.SIGINT, syscall.SIGTERM)
+	endWithError := false
+	select {
+	case <-done:
+		log.Info("Done")
+	case err := <-errCh:
+		if err != nil {
+			log.Error(err, "Error")
+			endWithError = true
+		}
+	}
+	ctxCancel()
+	wg.Wait()
+
+	if endWithError {
+		os.Exit(1)
+	}
+
+	err = recorder.WriteSummary(filepath.Join(*outputDir, "summary.yaml"))
+	if err != nil {
+		log.Error(err, "Writing summary")
+		os.Exit(1)
+	}
+	err = recorder.WriteCQCsv(filepath.Join(*outputDir, "cqStates.csv"))
+	if err != nil {
+		log.Error(err, "Writing cq csv")
+		os.Exit(1)
+	}
+	err = recorder.WriteWLCsv(filepath.Join(*outputDir, "wlStates.csv"))
+	if err != nil {
+		log.Error(err, "Writing wl csv")
+		os.Exit(1)
+	}
+
+	if *minimalKueuePath == "" {
+		c, err := client.New(cfg, client.Options{Scheme: scheme})
+		if err != nil {
+			log.Error(err, "Create cleanup client")
+		} else {
+			generator.Cleanup(context.Background(), c)
+		}
+	}
+}
+
+func runCommand(ctx context.Context, workDir, cmdPath, kubeconfig string, withCPUProf, withLogs, logToFile bool, logLevel int, errCh chan<- error, wg *sync.WaitGroup, metricsPort int) error {
+	log := ctrl.LoggerFrom(ctx).WithName("Run command")
+
+	cmd := exec.CommandContext(ctx, cmdPath, "--kubeconfig", filepath.Join(workDir, kubeconfig))
+	cmd.Cancel = func() error {
+		log.Info("Stop the command")
+		return cmd.Process.Signal(syscall.SIGINT)
+	}
+
+	exe := path.Base(cmdPath)
+
+	if withCPUProf {
+		cmd.Args = append(cmd.Args, "--cpuprofile", filepath.Join(workDir, fmt.Sprintf("%s.cpu.prof", exe)))
+	}
+
+	if withLogs {
+		cmd.Args = append(cmd.Args, fmt.Sprintf("--zap-log-level=%d", logLevel))
+		outWriter := os.Stdout
+		errWriter := os.Stderr
+		if logToFile {
+			var err error
+			outWriter, err = os.Create(filepath.Join(workDir, fmt.Sprintf("%s.out.log", exe)))
+			if err != nil {
+				return err
+			}
+			defer outWriter.Close()
+
+			errWriter, err = os.Create(filepath.Join(workDir, fmt.Sprintf("%s.err.log", exe)))
+			if err != nil {
+				return err
+			}
+			defer errWriter.Close()
```

  Good catch! The process for the command duplicates these file descriptors on the OS level for the command before they are closed, that's why it worked, but fixed it here and in the `test/performance/integration`

### Review by [@ASverdlov](https://github.com/ASverdlov) (commented) — 2026-02-01T16:31:36Z

- **test/performance/tas/runner/generator/generator.go:149** by [@ASverdlov](https://github.com/ASverdlov) — 2026-02-01T16:31:35Z
```diff
@@ -0,0 +1,289 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package generator
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"os"
+	"sync"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/yaml"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+)
+
+const (
+	RunningTimeLabel = "kueue.x-k8s.io/performance-scheduler-running-ms"
+	ClassLabel       = "kueue.x-k8s.io/performance-scheduler-class"
+	CleanupLabel     = "kueue.x-k8s.io/performance-scheduler-cleanup"
+)
+
+// WorkloadTemplate defines a workload to generate (extended for TAS)
+type WorkloadTemplate struct {
+	ClassName     string `json:"className"`
+	RuntimeMs     uint   `json:"runtimeMs"`
+	Priority      int32  `json:"priority"`
+	Request       string `json:"request"`
+	PodCount      int    `json:"podCount"`
+	TASConstraint string `json:"tasConstraint"` // "required", "preferred", "balanced"
+	TASLevel      string `json:"tasLevel"`
+	SliceSize     int32  `json:"sliceSize"` // for balanced placement - pods per slice
+}
+
+// WorkloadsSet defines a set of workloads to generate
+type WorkloadsSet struct {
+	Count              int                `json:"count"`
+	CreationIntervalMs uint               `json:"creationIntervalMs"`
+	Workloads          []WorkloadTemplate `json:"workloads"`
+}
+
+// QueuesSet defines a set of queues to generate
+type QueuesSet struct {
+	ClassName           string                 `json:"className"`
+	Count               int                    `json:"count"`
+	NominalQuota        string                 `json:"nominalQuota"`
+	BorrowingLimit      string                 `json:"borrowingLimit"`
+	ReclaimWithinCohort kueue.PreemptionPolicy `json:"reclaimWithinCohort"`
+	WithinClusterQueue  kueue.PreemptionPolicy `json:"withinClusterQueue"`
+	WorkloadsSets       []WorkloadsSet         `json:"workloadsSets"`
+}
+
+// CohortSet defines a set of cohorts to generate
+type CohortSet struct {
+	ClassName  string      `json:"className"`
+	Count      int         `json:"count"`
+	QueuesSets []QueuesSet `json:"queuesSets"`
+}
+
+// Config represents the full generator configuration
+type Config struct {
+	Topology       TopologyConfig       `json:"topology"`
+	ResourceFlavor ResourceFlavorConfig `json:"resourceFlavor"`
+	Cohorts        []CohortSet          `json:"cohorts"`
+}
+
+// LoadConfig loads the generator configuration from file
+func LoadConfig(configFile string) (*Config, error) {
+	yamlFile, err := os.ReadFile(configFile)
+	if err != nil {
+		return nil, fmt.Errorf("reading config file: %w", err)
+	}
+
+	config := &Config{}
+	if err := yaml.Unmarshal(yamlFile, config); err != nil {
+		return nil, fmt.Errorf("parsing config: %w", err)
+	}
+
+	return config, nil
+}
+
+// Generate creates all resources (topology, nodes, queues, workloads)
+func Generate(ctx context.Context, c client.Client, config *Config) error {
+	log := ctrl.LoggerFrom(ctx).WithName("generate")
+	log.Info("Start generation")
+	defer log.Info("End generation")
+
+	// 1. Generate topology infrastructure
+	if err := GenerateTopologyNodes(ctx, c, config.Topology); err != nil {
+		return fmt.Errorf("generating topology nodes: %w", err)
+	}
+
+	if err := GenerateTopologyResource(ctx, c, config.Topology); err != nil {
+		return fmt.Errorf("generating topology resource: %w", err)
+	}
+
+	if err := GenerateResourceFlavor(ctx, c, config.ResourceFlavor, config.Topology.Name); err != nil {
+		return fmt.Errorf("generating resource flavor: %w", err)
+	}
+
+	// 2. Generate cohorts, queues, and workloads
+	return concurrent(config.Cohorts, func(cs []CohortSet) int { return len(cs) }, func(idx int) error {
+		return generateCohortSet(ctx, c, config.Cohorts[idx], config.ResourceFlavor.Name)
+	})
+}
+
+func concurrent[T any](set T, count func(T) int, call func(int) error) error {
+	wg := sync.WaitGroup{}
+	errCh := make(chan error)
+	var errs []error
+	go func() {
+		for err := range errCh {
+			errs = append(errs, err)
+		}
+	}()
+	total := count(set)
+	wg.Add(total)
+	for i := range total {
+		go func() {
+			defer wg.Done()
+			err := call(i)
+			if err != nil {
+				errCh <- err
+			}
+		}()
+	}
+	wg.Wait()
+	close(errCh)
+	return errors.Join(errs...)
+}
```

  Good catch! Fixed it here and in the `test/performance/integration`

### Review by [@ASverdlov](https://github.com/ASverdlov) (commented) — 2026-02-01T16:32:32Z

- **test/performance/tas/runner/generator/generator.go:194** by [@ASverdlov](https://github.com/ASverdlov) — 2026-02-01T16:32:32Z
```diff
@@ -0,0 +1,289 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package generator
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"os"
+	"sync"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/yaml"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+)
+
+const (
+	RunningTimeLabel = "kueue.x-k8s.io/performance-scheduler-running-ms"
+	ClassLabel       = "kueue.x-k8s.io/performance-scheduler-class"
+	CleanupLabel     = "kueue.x-k8s.io/performance-scheduler-cleanup"
+)
+
+// WorkloadTemplate defines a workload to generate (extended for TAS)
+type WorkloadTemplate struct {
+	ClassName     string `json:"className"`
+	RuntimeMs     uint   `json:"runtimeMs"`
+	Priority      int32  `json:"priority"`
+	Request       string `json:"request"`
+	PodCount      int    `json:"podCount"`
+	TASConstraint string `json:"tasConstraint"` // "required", "preferred", "balanced"
+	TASLevel      string `json:"tasLevel"`
+	SliceSize     int32  `json:"sliceSize"` // for balanced placement - pods per slice
+}
+
+// WorkloadsSet defines a set of workloads to generate
+type WorkloadsSet struct {
+	Count              int                `json:"count"`
+	CreationIntervalMs uint               `json:"creationIntervalMs"`
+	Workloads          []WorkloadTemplate `json:"workloads"`
+}
+
+// QueuesSet defines a set of queues to generate
+type QueuesSet struct {
+	ClassName           string                 `json:"className"`
+	Count               int                    `json:"count"`
+	NominalQuota        string                 `json:"nominalQuota"`
+	BorrowingLimit      string                 `json:"borrowingLimit"`
+	ReclaimWithinCohort kueue.PreemptionPolicy `json:"reclaimWithinCohort"`
+	WithinClusterQueue  kueue.PreemptionPolicy `json:"withinClusterQueue"`
+	WorkloadsSets       []WorkloadsSet         `json:"workloadsSets"`
+}
+
+// CohortSet defines a set of cohorts to generate
+type CohortSet struct {
+	ClassName  string      `json:"className"`
+	Count      int         `json:"count"`
+	QueuesSets []QueuesSet `json:"queuesSets"`
+}
+
+// Config represents the full generator configuration
+type Config struct {
+	Topology       TopologyConfig       `json:"topology"`
+	ResourceFlavor ResourceFlavorConfig `json:"resourceFlavor"`
+	Cohorts        []CohortSet          `json:"cohorts"`
+}
+
+// LoadConfig loads the generator configuration from file
+func LoadConfig(configFile string) (*Config, error) {
+	yamlFile, err := os.ReadFile(configFile)
+	if err != nil {
+		return nil, fmt.Errorf("reading config file: %w", err)
+	}
+
+	config := &Config{}
+	if err := yaml.Unmarshal(yamlFile, config); err != nil {
+		return nil, fmt.Errorf("parsing config: %w", err)
+	}
+
+	return config, nil
+}
+
+// Generate creates all resources (topology, nodes, queues, workloads)
+func Generate(ctx context.Context, c client.Client, config *Config) error {
+	log := ctrl.LoggerFrom(ctx).WithName("generate")
+	log.Info("Start generation")
+	defer log.Info("End generation")
+
+	// 1. Generate topology infrastructure
+	if err := GenerateTopologyNodes(ctx, c, config.Topology); err != nil {
+		return fmt.Errorf("generating topology nodes: %w", err)
+	}
+
+	if err := GenerateTopologyResource(ctx, c, config.Topology); err != nil {
+		return fmt.Errorf("generating topology resource: %w", err)
+	}
+
+	if err := GenerateResourceFlavor(ctx, c, config.ResourceFlavor, config.Topology.Name); err != nil {
+		return fmt.Errorf("generating resource flavor: %w", err)
+	}
+
+	// 2. Generate cohorts, queues, and workloads
+	return concurrent(config.Cohorts, func(cs []CohortSet) int { return len(cs) }, func(idx int) error {
+		return generateCohortSet(ctx, c, config.Cohorts[idx], config.ResourceFlavor.Name)
+	})
+}
+
+func concurrent[T any](set T, count func(T) int, call func(int) error) error {
+	wg := sync.WaitGroup{}
+	errCh := make(chan error)
+	var errs []error
+	go func() {
+		for err := range errCh {
+			errs = append(errs, err)
+		}
+	}()
+	total := count(set)
+	wg.Add(total)
+	for i := range total {
+		go func() {
+			defer wg.Done()
+			err := call(i)
+			if err != nil {
+				errCh <- err
+			}
+		}()
+	}
+	wg.Wait()
+	close(errCh)
+	return errors.Join(errs...)
+}
+
+func generateWlSet(ctx context.Context, c client.Client, wlSet WorkloadsSet, namespace string, localQueue kueue.LocalQueueName, wlSetIdx int) error {
+	delay := time.Duration(wlSet.CreationIntervalMs) * time.Millisecond
+	log := ctrl.LoggerFrom(ctx).WithName("generate workload group").WithValues("namespace", namespace, "localQueue", localQueue, "delay", delay)
+	log.Info("Start generation")
+	defer log.Info("End generation")
+
+	for si := range wlSet.Count {
+		for i, wlt := range wlSet.Workloads {
+			<-time.After(delay)
+
+			// Create workload with TAS support
+			wlName := fmt.Sprintf("%s-%d-%d-%d", wlt.ClassName, wlSetIdx, si, i)
+
+			// Use podCount if specified, otherwise default to 1
+			podCount := wlt.PodCount
+			if podCount == 0 {
+				podCount = 1
+			}
+
+			podSetBuilder := utiltestingapi.MakePodSet("main", podCount).
+				Request(corev1.ResourceCPU, wlt.Request)
+
+			// Add TAS topology request if specified
+			if wlt.TASLevel != "" {
+				switch wlt.TASConstraint {
+				case "required":
+					podSetBuilder = podSetBuilder.RequiredTopologyRequest(wlt.TASLevel)
+				case "preferred":
+					podSetBuilder = podSetBuilder.PreferredTopologyRequest(wlt.TASLevel)
+				case "balanced":
+					// Balanced placement uses slice-based topology
+					podSetBuilder = podSetBuilder.SliceRequiredTopologyRequest(wlt.TASLevel)
+					if wlt.SliceSize > 0 {
+						podSetBuilder = podSetBuilder.SliceSizeTopologyRequest(wlt.SliceSize)
+					}
+				}
+			}
+
+			wl := utiltestingapi.MakeWorkload(wlName, namespace).
+				Queue(localQueue).
+				PodSets(*podSetBuilder.Obj()).
+				Request(corev1.ResourceCPU, wlt.Request).
+				Label(RunningTimeLabel, fmt.Sprintf("%d", wlt.RuntimeMs)).
+				Label(ClassLabel, wlt.ClassName).
```

  Good catch! Fixed this for topologies, localqueues, and nodes as well.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2026-02-02T09:17:35Z

- **test/performance/tas/README.md:25** by [@mwielgus](https://github.com/mwielgus) — 2026-02-02T09:17:35Z
```diff
@@ -0,0 +1,199 @@
+# TAS Performance Test
+
+Performance test framework for TAS (Topology Aware Scheduling) in Kueue.
+
+## Overview
+
+This test framework measures the performance of Kueue's TAS feature by:
+- Generating a hierarchical topology (blocks → racks → nodes)
+- Creating nodes with topology labels
+- Submitting workloads with topology constraints
+- Measuring time-to-admission and resource utilization
+- Validating performance against expected thresholds
+
+## Components
+
+### Runner
+Main application that orchestrates the performance test:
+- Generates topology nodes with hierarchical labels
+- Creates Topology and ResourceFlavor resources
+- Generates cohorts, queues, and workloads
+- Monitors workload admission and completion
+- Collects performance statistics
+
+### MinimalKueue
+Lightweight Kueue controller manager with:
```

  Can you write what, in particular, MinimalKueue does NOT have?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2026-02-02T09:19:24Z

- **test/performance/tas/README.md:29** by [@mwielgus](https://github.com/mwielgus) — 2026-02-02T09:19:24Z
```diff
@@ -0,0 +1,199 @@
+# TAS Performance Test
+
+Performance test framework for TAS (Topology Aware Scheduling) in Kueue.
+
+## Overview
+
+This test framework measures the performance of Kueue's TAS feature by:
+- Generating a hierarchical topology (blocks → racks → nodes)
+- Creating nodes with topology labels
+- Submitting workloads with topology constraints
+- Measuring time-to-admission and resource utilization
+- Validating performance against expected thresholds
+
+## Components
+
+### Runner
+Main application that orchestrates the performance test:
+- Generates topology nodes with hierarchical labels
+- Creates Topology and ResourceFlavor resources
+- Generates cohorts, queues, and workloads
+- Monitors workload admission and completion
+- Collects performance statistics
+
+### MinimalKueue
+Lightweight Kueue controller manager with:
+- Core controllers (Workload, LocalQueue, ClusterQueue, ResourceFlavor)
+- TAS controllers (Topology, Resource Flavor, Topology Ungater)
+- Scheduler with TAS support
+- Optional CPU profiling and metrics
```

  What about job* API support? If not, please mention that it only works on Workloads.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-02-02T11:44:23Z

- **test/performance/tas/minimalkueue/main.go:1** by [@kshalot](https://github.com/kshalot) — 2026-02-02T11:44:24Z
  Instead of having two minimalkueues for TAS and non-TAS, couldn't we have one implementation with parameters?

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-02-02T13:10:28Z

- **test/performance/tas/runner/generator/generator.go:1** by [@kshalot](https://github.com/kshalot) — 2026-02-02T13:10:28Z
  Similarly to consolidating the two minimalkueues into one, wouldn't it be more future-proof to have a single generator that can consume a configuration with or without topologies?

### Review by [@ASverdlov](https://github.com/ASverdlov) (commented) — 2026-02-03T03:06:09Z

- **test/performance/tas/README.md:29** by [@ASverdlov](https://github.com/ASverdlov) — 2026-02-03T03:06:09Z
```diff
@@ -0,0 +1,199 @@
+# TAS Performance Test
+
+Performance test framework for TAS (Topology Aware Scheduling) in Kueue.
+
+## Overview
+
+This test framework measures the performance of Kueue's TAS feature by:
+- Generating a hierarchical topology (blocks → racks → nodes)
+- Creating nodes with topology labels
+- Submitting workloads with topology constraints
+- Measuring time-to-admission and resource utilization
+- Validating performance against expected thresholds
+
+## Components
+
+### Runner
+Main application that orchestrates the performance test:
+- Generates topology nodes with hierarchical labels
+- Creates Topology and ResourceFlavor resources
+- Generates cohorts, queues, and workloads
+- Monitors workload admission and completion
+- Collects performance statistics
+
+### MinimalKueue
+Lightweight Kueue controller manager with:
+- Core controllers (Workload, LocalQueue, ClusterQueue, ResourceFlavor)
+- TAS controllers (Topology, Resource Flavor, Topology Ungater)
+- Scheduler with TAS support
+- Optional CPU profiling and metrics
```

  Yeah, it only works on Workloads. I have merged (with support for TAS configurations) `test/performance/tas` into an existing `test/performance/scheduler` framework based on the feedback from @kshalot below, so this `README.md` is no longer there. Let me know if you have suggestions for `test/performance/scheduler/README.md`.

### Review by [@ASverdlov](https://github.com/ASverdlov) (commented) — 2026-02-03T03:06:27Z

- **test/performance/tas/README.md:25** by [@ASverdlov](https://github.com/ASverdlov) — 2026-02-03T03:06:27Z
```diff
@@ -0,0 +1,199 @@
+# TAS Performance Test
+
+Performance test framework for TAS (Topology Aware Scheduling) in Kueue.
+
+## Overview
+
+This test framework measures the performance of Kueue's TAS feature by:
+- Generating a hierarchical topology (blocks → racks → nodes)
+- Creating nodes with topology labels
+- Submitting workloads with topology constraints
+- Measuring time-to-admission and resource utilization
+- Validating performance against expected thresholds
+
+## Components
+
+### Runner
+Main application that orchestrates the performance test:
+- Generates topology nodes with hierarchical labels
+- Creates Topology and ResourceFlavor resources
+- Generates cohorts, queues, and workloads
+- Monitors workload admission and completion
+- Collects performance statistics
+
+### MinimalKueue
+Lightweight Kueue controller manager with:
```

  I have merged (with support for TAS configurations) `test/performance/tas` into an existing `test/performance/scheduler` framework based on the feedback from @kshalot below, so this `README.md` is no longer there. Let me know if you have suggestions for `test/performance/scheduler/README.md`.

### Review by [@ASverdlov](https://github.com/ASverdlov) (commented) — 2026-02-03T03:08:37Z

- **test/performance/tas/minimalkueue/main.go:1** by [@ASverdlov](https://github.com/ASverdlov) — 2026-02-03T03:08:37Z
  Implemented your suggestion by merging TAS and non-TAS performance test into 1 (at `test/performance/scheduler`): https://github.com/kubernetes-sigs/kueue/pull/8917/commits/b6d64634dbd13f7cfd2b4e41ee19faaf367c75d0.
  
  PTAL

### Review by [@ASverdlov](https://github.com/ASverdlov) (commented) — 2026-02-03T03:08:42Z

- **test/performance/tas/runner/generator/generator.go:1** by [@ASverdlov](https://github.com/ASverdlov) — 2026-02-03T03:08:42Z
  Implemented your suggestion by merging TAS and non-TAS performance test into 1 (at `test/performance/scheduler`): https://github.com/kubernetes-sigs/kueue/pull/8917/commits/b6d64634dbd13f7cfd2b4e41ee19faaf367c75d0.
  
  PTAL

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-03T08:37:11Z

- **Makefile-test.mk:358** by [@mimowo](https://github.com/mimowo) — 2026-02-03T08:37:12Z
```diff
@@ -348,6 +348,43 @@ run-performance-scheduler-in-cluster: envtest performance-scheduler-runner
 		--generatorConfig=$(SCALABILITY_GENERATOR_CONFIG) \
 		--qps=1000 --burst=2000 --timeout=15m $(SCALABILITY_SCRAPE_ARGS)
 
+##@ TAS Performance Testing
+
+SCALABILITY_TAS_GENERATOR_CONFIG ?= $(PROJECT_DIR)/test/performance/scheduler/configs/tas/generator.yaml
+SCALABILITY_TAS_RANGE_FILE ?= $(PROJECT_DIR)/test/performance/scheduler/configs/tas/rangespec.yaml
+
+SCALABILITY_TAS_RUN_DIR := $(ARTIFACTS)/run-performance-tas
+.PHONY: run-performance-tas
+run-performance-tas: envtest performance-scheduler-runner minimalkueue
```

  Could you please already PR a CI job in the test-infra and put it on hold until this PR is merged, just so that we see all steps are already prepared.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-03T08:40:10Z

- **test/performance/scheduler/configs/baseline/generator.yaml:1** by [@mimowo](https://github.com/mimowo) — 2026-02-03T08:40:10Z
```diff
@@ -0,0 +1,35 @@
+# Scheduler Performance Test Configuration
```

  I would rather avoid naming the config / folder as "scheduler", because "tas" is also done by the Kueue scheduler. I'm thinking rather about "baseline" or "quota-only" names, wdyt?

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-02-03T08:56:57Z

- **test/performance/scheduler/runner/generator/generator.go:270** by [@kshalot](https://github.com/kshalot) — 2026-02-03T08:53:30Z
```diff
@@ -170,44 +231,70 @@ func generateQueue(ctx context.Context, c client.Client, qSet QueuesSet, cohortN
 	})
 }
 
-func generateQueueSet(ctx context.Context, c client.Client, qSet QueuesSet, cohortName kueue.CohortReference, queueSetIdx int) error {
+func generateQueueSet(ctx context.Context, c client.Client, qSet QueuesSet, cohortName kueue.CohortReference, flavorName string, queueSetIdx int) error {
 	log := ctrl.LoggerFrom(ctx).WithName("generate queue set").WithValues("count", qSet.Count, "prefix", qSet.ClassName)
 	log.Info("Start generation")
 	defer log.Info("End generation")
 	return concurrent(qSet, func(qs QueuesSet) int { return qs.Count }, func(idx int) error {
-		return generateQueue(ctx, c, qSet, cohortName, queueSetIdx, idx)
+		return generateQueue(ctx, c, qSet, cohortName, flavorName, queueSetIdx, idx)
 	})
 }
 
-func generateCohort(ctx context.Context, c client.Client, cSet CohortSet, cohortIdx int) error {
+func generateCohort(ctx context.Context, c client.Client, cSet CohortSet, flavorName string, cohortIdx int) error {
 	log := ctrl.LoggerFrom(ctx).WithName("generate cohort").WithValues("idx", cohortIdx, "prefix", cSet.ClassName)
 	log.Info("Start generation")
 	defer log.Info("End generation")
 	cohortName := fmt.Sprintf("%s-%d", cSet.ClassName, cohortIdx)
 	return concurrent(cSet, func(cs CohortSet) int { return len(cs.QueuesSets) }, func(idx int) error {
-		return generateQueueSet(ctx, c, cSet.QueuesSets[idx], kueue.CohortReference(cohortName), idx)
+		return generateQueueSet(ctx, c, cSet.QueuesSets[idx], kueue.CohortReference(cohortName), flavorName, idx)
 	})
 }
 
-func generateCohortSet(ctx context.Context, c client.Client, cSet CohortSet) error {
+func generateCohortSet(ctx context.Context, c client.Client, cSet CohortSet, flavorName string) error {
 	log := ctrl.LoggerFrom(ctx).WithName("generate cohort set").WithValues("count", cSet.Count, "prefix", cSet.ClassName)
 	log.Info("Start generation")
 	defer log.Info("End generation")
 	return concurrent(cSet, func(cs CohortSet) int { return cs.Count }, func(idx int) error {
-		return generateCohort(ctx, c, cSet, idx)
+		return generateCohort(ctx, c, cSet, flavorName, idx)
 	})
 }
 
-func Generate(ctx context.Context, c client.Client, cSets []CohortSet) error {
-	log := ctrl.LoggerFrom(ctx).WithName("generate cohort sets").WithValues("numSets", len(cSets))
+func Generate(ctx context.Context, c client.Client, config *Config) error {
+	log := ctrl.LoggerFrom(ctx).WithName("generate").WithValues("numCohortSets", len(config.Cohorts))
 	log.Info("Start generation")
 	defer log.Info("End generation")
-	rf := utiltestingapi.MakeResourceFlavor(resourceFlavorName).Label(CleanupLabel, "true").Obj()
-	err := c.Create(ctx, rf)
-	if err != nil {
-		return err
+
+	flavorName := defaultResourceFlavorName
+	if config.ResourceFlavor != nil && config.ResourceFlavor.Name != "" {
+		flavorName = config.ResourceFlavor.Name
+	}
```

  You are already defaulting to the `defaultResourceFlavorName` in the `LoadConfig` func:
  ```
  	// Set default resource flavor if not provided (for scheduler configs without TAS)
  	if config.ResourceFlavor == nil {
  		config.ResourceFlavor = &ResourceFlavorConfig{
  			Name: defaultResourceFlavorName,
  		}
  	}
  ```
  So maybe there is no need to do it here again?

- **test/performance/scheduler/runner/generator/generator.go:289** by [@kshalot](https://github.com/kshalot) — 2026-02-03T08:56:55Z
```diff
@@ -170,44 +231,70 @@ func generateQueue(ctx context.Context, c client.Client, qSet QueuesSet, cohortN
 	})
 }
 
-func generateQueueSet(ctx context.Context, c client.Client, qSet QueuesSet, cohortName kueue.CohortReference, queueSetIdx int) error {
+func generateQueueSet(ctx context.Context, c client.Client, qSet QueuesSet, cohortName kueue.CohortReference, flavorName string, queueSetIdx int) error {
 	log := ctrl.LoggerFrom(ctx).WithName("generate queue set").WithValues("count", qSet.Count, "prefix", qSet.ClassName)
 	log.Info("Start generation")
 	defer log.Info("End generation")
 	return concurrent(qSet, func(qs QueuesSet) int { return qs.Count }, func(idx int) error {
-		return generateQueue(ctx, c, qSet, cohortName, queueSetIdx, idx)
+		return generateQueue(ctx, c, qSet, cohortName, flavorName, queueSetIdx, idx)
 	})
 }
 
-func generateCohort(ctx context.Context, c client.Client, cSet CohortSet, cohortIdx int) error {
+func generateCohort(ctx context.Context, c client.Client, cSet CohortSet, flavorName string, cohortIdx int) error {
 	log := ctrl.LoggerFrom(ctx).WithName("generate cohort").WithValues("idx", cohortIdx, "prefix", cSet.ClassName)
 	log.Info("Start generation")
 	defer log.Info("End generation")
 	cohortName := fmt.Sprintf("%s-%d", cSet.ClassName, cohortIdx)
 	return concurrent(cSet, func(cs CohortSet) int { return len(cs.QueuesSets) }, func(idx int) error {
-		return generateQueueSet(ctx, c, cSet.QueuesSets[idx], kueue.CohortReference(cohortName), idx)
+		return generateQueueSet(ctx, c, cSet.QueuesSets[idx], kueue.CohortReference(cohortName), flavorName, idx)
 	})
 }
 
-func generateCohortSet(ctx context.Context, c client.Client, cSet CohortSet) error {
+func generateCohortSet(ctx context.Context, c client.Client, cSet CohortSet, flavorName string) error {
 	log := ctrl.LoggerFrom(ctx).WithName("generate cohort set").WithValues("count", cSet.Count, "prefix", cSet.ClassName)
 	log.Info("Start generation")
 	defer log.Info("End generation")
 	return concurrent(cSet, func(cs CohortSet) int { return cs.Count }, func(idx int) error {
-		return generateCohort(ctx, c, cSet, idx)
+		return generateCohort(ctx, c, cSet, flavorName, idx)
 	})
 }
 
-func Generate(ctx context.Context, c client.Client, cSets []CohortSet) error {
-	log := ctrl.LoggerFrom(ctx).WithName("generate cohort sets").WithValues("numSets", len(cSets))
+func Generate(ctx context.Context, c client.Client, config *Config) error {
+	log := ctrl.LoggerFrom(ctx).WithName("generate").WithValues("numCohortSets", len(config.Cohorts))
 	log.Info("Start generation")
 	defer log.Info("End generation")
-	rf := utiltestingapi.MakeResourceFlavor(resourceFlavorName).Label(CleanupLabel, "true").Obj()
-	err := c.Create(ctx, rf)
-	if err != nil {
-		return err
+
+	flavorName := defaultResourceFlavorName
+	if config.ResourceFlavor != nil && config.ResourceFlavor.Name != "" {
+		flavorName = config.ResourceFlavor.Name
+	}
+
+	if config.Topology != nil {
+		// TAS mode
+		log.Info("Generating TAS topology infrastructure")
+		if err := generateTopologyNodes(ctx, c, *config.Topology); err != nil {
+			return fmt.Errorf("generating topology nodes: %w", err)
+		}
+
+		if err := generateTopologyResource(ctx, c, *config.Topology); err != nil {
+			return fmt.Errorf("generating topology resource: %w", err)
+		}
+
+		if err := generateResourceFlavor(ctx, c, *config.ResourceFlavor, config.Topology.Name); err != nil {
+			return fmt.Errorf("generating resource flavor: %w", err)
+		}
+	} else {
+		// Standard mode: create simple resource flavor
+		log.Info("Generating standard resource flavor")
+		rf := utiltestingapi.MakeResourceFlavor(flavorName).Label(CleanupLabel, "true").Obj()
```

  Maybe there is no need to branch out here and instead have a single place where the resource flavor is created? For example, the `ResourceFlavorConfig` could have an optional `topologyName` field and just create a flavor according to this config (i.e. the tas tests will define this field which will create a flavor that links to the topology).
  
  `LoadConfig` created a default `ResourceFlavorConfig` if it's not in the YAML, so we can expect the struct to be there. If we use `generateResourceFlavor` in both places we'll also automatically get all the logging and error handling that you added in that function.

### Review by [@ASverdlov](https://github.com/ASverdlov) (commented) — 2026-02-03T21:04:06Z

- **test/performance/scheduler/runner/generator/generator.go:289** by [@ASverdlov](https://github.com/ASverdlov) — 2026-02-03T21:04:06Z
```diff
@@ -170,44 +231,70 @@ func generateQueue(ctx context.Context, c client.Client, qSet QueuesSet, cohortN
 	})
 }
 
-func generateQueueSet(ctx context.Context, c client.Client, qSet QueuesSet, cohortName kueue.CohortReference, queueSetIdx int) error {
+func generateQueueSet(ctx context.Context, c client.Client, qSet QueuesSet, cohortName kueue.CohortReference, flavorName string, queueSetIdx int) error {
 	log := ctrl.LoggerFrom(ctx).WithName("generate queue set").WithValues("count", qSet.Count, "prefix", qSet.ClassName)
 	log.Info("Start generation")
 	defer log.Info("End generation")
 	return concurrent(qSet, func(qs QueuesSet) int { return qs.Count }, func(idx int) error {
-		return generateQueue(ctx, c, qSet, cohortName, queueSetIdx, idx)
+		return generateQueue(ctx, c, qSet, cohortName, flavorName, queueSetIdx, idx)
 	})
 }
 
-func generateCohort(ctx context.Context, c client.Client, cSet CohortSet, cohortIdx int) error {
+func generateCohort(ctx context.Context, c client.Client, cSet CohortSet, flavorName string, cohortIdx int) error {
 	log := ctrl.LoggerFrom(ctx).WithName("generate cohort").WithValues("idx", cohortIdx, "prefix", cSet.ClassName)
 	log.Info("Start generation")
 	defer log.Info("End generation")
 	cohortName := fmt.Sprintf("%s-%d", cSet.ClassName, cohortIdx)
 	return concurrent(cSet, func(cs CohortSet) int { return len(cs.QueuesSets) }, func(idx int) error {
-		return generateQueueSet(ctx, c, cSet.QueuesSets[idx], kueue.CohortReference(cohortName), idx)
+		return generateQueueSet(ctx, c, cSet.QueuesSets[idx], kueue.CohortReference(cohortName), flavorName, idx)
 	})
 }
 
-func generateCohortSet(ctx context.Context, c client.Client, cSet CohortSet) error {
+func generateCohortSet(ctx context.Context, c client.Client, cSet CohortSet, flavorName string) error {
 	log := ctrl.LoggerFrom(ctx).WithName("generate cohort set").WithValues("count", cSet.Count, "prefix", cSet.ClassName)
 	log.Info("Start generation")
 	defer log.Info("End generation")
 	return concurrent(cSet, func(cs CohortSet) int { return cs.Count }, func(idx int) error {
-		return generateCohort(ctx, c, cSet, idx)
+		return generateCohort(ctx, c, cSet, flavorName, idx)
 	})
 }
 
-func Generate(ctx context.Context, c client.Client, cSets []CohortSet) error {
-	log := ctrl.LoggerFrom(ctx).WithName("generate cohort sets").WithValues("numSets", len(cSets))
+func Generate(ctx context.Context, c client.Client, config *Config) error {
+	log := ctrl.LoggerFrom(ctx).WithName("generate").WithValues("numCohortSets", len(config.Cohorts))
 	log.Info("Start generation")
 	defer log.Info("End generation")
-	rf := utiltestingapi.MakeResourceFlavor(resourceFlavorName).Label(CleanupLabel, "true").Obj()
-	err := c.Create(ctx, rf)
-	if err != nil {
-		return err
+
+	flavorName := defaultResourceFlavorName
+	if config.ResourceFlavor != nil && config.ResourceFlavor.Name != "" {
+		flavorName = config.ResourceFlavor.Name
+	}
+
+	if config.Topology != nil {
+		// TAS mode
+		log.Info("Generating TAS topology infrastructure")
+		if err := generateTopologyNodes(ctx, c, *config.Topology); err != nil {
+			return fmt.Errorf("generating topology nodes: %w", err)
+		}
+
+		if err := generateTopologyResource(ctx, c, *config.Topology); err != nil {
+			return fmt.Errorf("generating topology resource: %w", err)
+		}
+
+		if err := generateResourceFlavor(ctx, c, *config.ResourceFlavor, config.Topology.Name); err != nil {
+			return fmt.Errorf("generating resource flavor: %w", err)
+		}
+	} else {
+		// Standard mode: create simple resource flavor
+		log.Info("Generating standard resource flavor")
+		rf := utiltestingapi.MakeResourceFlavor(flavorName).Label(CleanupLabel, "true").Obj()
```

  Great idea! I unified the creation of the resource flavor in the latest commit.

### Review by [@ASverdlov](https://github.com/ASverdlov) (commented) — 2026-02-04T00:15:45Z

- **Makefile-test.mk:358** by [@ASverdlov](https://github.com/ASverdlov) — 2026-02-04T00:15:45Z
```diff
@@ -348,6 +348,43 @@ run-performance-scheduler-in-cluster: envtest performance-scheduler-runner
 		--generatorConfig=$(SCALABILITY_GENERATOR_CONFIG) \
 		--qps=1000 --burst=2000 --timeout=15m $(SCALABILITY_SCRAPE_ARGS)
 
+##@ TAS Performance Testing
+
+SCALABILITY_TAS_GENERATOR_CONFIG ?= $(PROJECT_DIR)/test/performance/scheduler/configs/tas/generator.yaml
+SCALABILITY_TAS_RANGE_FILE ?= $(PROJECT_DIR)/test/performance/scheduler/configs/tas/rangespec.yaml
+
+SCALABILITY_TAS_RUN_DIR := $(ARTIFACTS)/run-performance-tas
+.PHONY: run-performance-tas
+run-performance-tas: envtest performance-scheduler-runner minimalkueue
```

  Here you go: https://github.com/kubernetes/test-infra/pull/36375

### Review by [@ASverdlov](https://github.com/ASverdlov) (commented) — 2026-02-04T00:21:46Z

- **test/performance/scheduler/configs/baseline/generator.yaml:1** by [@ASverdlov](https://github.com/ASverdlov) — 2026-02-04T00:21:46Z
```diff
@@ -0,0 +1,35 @@
+# Scheduler Performance Test Configuration
```

  I like `baseline`, I pushed some commits to make the configurations: `configs/{baseline,tas}`.
  
  I also renamed the Makefile targets to be:
  - `run-performance-scheduler-with-tas` (for baseline, unchanged: `run-performance-scheduler`)
  - `test-performance-scheduler-with-tas` (for baseline, unchanged: `test-performance-scheduler`)
  
  I don't like how long the name is, alternative would be: `{run,test}-performance-{baseline,tas}`, but that would require migrating the target (baseline old target name is already used in test-infra).

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-02-04T09:31:31Z

- **hack/performance-retry.sh:35** by [@kshalot](https://github.com/kshalot) — 2026-02-04T08:54:21Z
```diff
@@ -18,23 +18,40 @@ set -o errexit
 set -o nounset
 set -o pipefail
 
-if [[ "$#" -lt 1 ]]; then
-	echo "Usage: ${0} <num-retries>"
+if [[ "$#" -lt 2 ]]; then
+	echo "Usage: ${0} <num-retries> <target-name>"
 	exit 1
 fi
 
+TARGET_NAME="$2"
+
+# Map target name to junit file and run directory
+case "$TARGET_NAME" in
+	test-performance-scheduler-once)
+		JUNIT_FILE="junit.xml"
+		RUN_DIR="run-performance-scheduler"
+		;;
+	test-performance-scheduler-with-tas-once)
+		JUNIT_FILE="junit-tas.xml"
```

  I think some other test targets add the target name (`$@`) to the artifacts dir, like:
  https://github.com/kubernetes-sigs/kueue/blob/94809908f9a2fe75a554aa7a95aee37e27157b31/Makefile-test.mk#L177
  
  Can we do this for performance tests as well? This way there won't be any need for unique naming etc.

- **test/performance/scheduler/configs/baseline/generator.yaml:1** by [@kshalot](https://github.com/kshalot) — 2026-02-04T09:01:49Z
```diff
@@ -0,0 +1,35 @@
+# Scheduler Performance Test Configuration
```

  Having "scheduler" in the target name makes it easier to distinguish this from E2E performance tests, where the pods are actually running on a cluster. We have this in the codebase ([link](https://github.com/kubernetes-sigs/kueue/tree/main/test/performance/e2e)), although it is not used at the moment.
  
  I think we could drop the "with" though. Looking at other tests, for example "test-e2e" vs "test-tas-e2e", we could have:
  * "test-performance-scheduler" <- remains the same
  * "test-tas-performance-scheduler"

- **test/performance/scheduler/runner/main.go:310** by [@kshalot](https://github.com/kshalot) — 2026-02-04T09:31:10Z
```diff
@@ -295,6 +301,10 @@ func runCommand(ctx context.Context, workDir, cmdPath, kubeconfig string, withCP
 		cmd.Args = append(cmd.Args, "--metricsPort", strconv.Itoa(metricsPort))
 	}
 
+	if enableTAS {
+		cmd.Args = append(cmd.Args, "--enableTAS")
+	}
+
 	log.Info("Starting process", "path", cmd.Path, "args", cmd.Args)
 	err := cmd.Start()
 	if err != nil {
```

  In theory, if this error occurs then the files created in `logToFile` will not be closed. I'm only mentioning it because I see you added deferred closing in this PR.
  
  Anyway, this shouldn't be a problem in this particular case. Not actionable.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-02-04T09:41:16Z

- **test/performance/scheduler/configs/tas/rangespec.yaml:1** by [@kshalot](https://github.com/kshalot) — 2026-02-04T09:41:16Z
  Those are the thresholds from the MacBook runs, right?
  
  Just noting that we should circle back after this is merged to establish a baseline from the CI runs

### Review by [@ASverdlov](https://github.com/ASverdlov) (commented) — 2026-02-04T17:28:10Z

- **test/performance/scheduler/configs/baseline/generator.yaml:1** by [@ASverdlov](https://github.com/ASverdlov) — 2026-02-04T17:28:10Z
```diff
@@ -0,0 +1,35 @@
+# Scheduler Performance Test Configuration
```

  Done.

### Review by [@ASverdlov](https://github.com/ASverdlov) (commented) — 2026-02-04T17:28:46Z

- **test/performance/scheduler/configs/tas/rangespec.yaml:1** by [@ASverdlov](https://github.com/ASverdlov) — 2026-02-04T17:28:45Z
  Added the provisional "always passing" thresholds: https://github.com/kubernetes-sigs/kueue/commit/fccd94e57098fc15d5a9caf05dedad5baea8ba53
  
  We can have a follow-up PR after this is merged to figure out the thresholds.

### Review by [@ASverdlov](https://github.com/ASverdlov) (commented) — 2026-02-04T19:32:19Z

- **hack/performance-retry.sh:35** by [@ASverdlov](https://github.com/ASverdlov) — 2026-02-04T19:32:19Z
```diff
@@ -18,23 +18,40 @@ set -o errexit
 set -o nounset
 set -o pipefail
 
-if [[ "$#" -lt 1 ]]; then
-	echo "Usage: ${0} <num-retries>"
+if [[ "$#" -lt 2 ]]; then
+	echo "Usage: ${0} <num-retries> <target-name>"
 	exit 1
 fi
 
+TARGET_NAME="$2"
+
+# Map target name to junit file and run directory
+case "$TARGET_NAME" in
+	test-performance-scheduler-once)
+		JUNIT_FILE="junit.xml"
+		RUN_DIR="run-performance-scheduler"
+		;;
+	test-performance-scheduler-with-tas-once)
+		JUNIT_FILE="junit-tas.xml"
```

  Good idea. Done: https://github.com/kubernetes-sigs/kueue/pull/8917/commits/f82e15f1177f9935c0b4a348926a594864a9ff37

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-04T19:35:06Z

/lgtm
