# PR #8939: Fix flaky restart - ensure the Ready Pods match the desired replicas

**Summary**: Fix flaky restart - ensure the Ready Pods match the desired replicas

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8939

**Last updated**: 2026-02-21T15:46:44Z

---

## Metadata

- **State**: closed
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `fix-flaky-restart-kueue` → `main`
- **Created**: 2026-02-02T16:10:43Z
- **Updated**: 2026-02-21T15:46:44Z
- **Merged**: —
- **Closed**: 2026-02-03T07:41:50Z
- **Labels**: `kind/bug`, `kind/flake`, `release-note-none`, `approved`, `size/M`, `do-not-merge/hold`, `cncf-cla: yes`
- **Assignees**: _none_
- **Requested reviewers**: [@kannon92](https://github.com/kannon92)
- **Changed files**: 1   **+30 / -1**

## Description

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind flake
/kind bug

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #8937

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-02-02T16:10:53Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 86096169adf2a5932a981ae1343cd78385cb984d |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6980d3280c4566000841d3ad |

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-02T16:10:56Z

cc @sohankunkerkar @mbobrovskyi

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-02T16:10:56Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8939#" title="Author self-approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-02T18:13:34Z

> Just throwing an idea out here: I’m trying to understand if there’s an opportunity to consolidate some of the wait functions in a meaningful way. I worry that we’re bloating the wait functions mainly to appease CI, and it might be worth thinking about this from a broader, longer-term perspective. That said, this PR looks fine to me.

I know, one alternative would be to stop checking Pods, and just check if the number of Endpoints equals to deployment.spec.replicas, but it would slightly relax the condition, because currently we can assert on the two sets of IPs being equal: https://github.com/kubernetes-sigs/kueue/blob/e2bb1d95b78738c3322a2f353745adca60efbb37/test/util/e2e.go#L444

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-02T19:08:31Z

/hold
I think this PR https://github.com/kubernetes-sigs/kueue/pull/8941 is also fixing the issue, so let me focus on reviewing that one.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-03T07:41:45Z

/close

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-03T07:41:51Z

@mimowo: Closed this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8939#issuecomment-3839627356):

>/close


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-02T16:34:17Z

- **test/util/e2e.go:467** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-02T16:34:17Z
```diff
@@ -457,6 +460,31 @@ func waitForDeploymentWithOnlyAvailableReplicas(ctx context.Context, k8sClient c
 	ginkgo.GinkgoLogr.Info("Deployment has only available replicas in the cluster", "deployment", key, "waitingTime", time.Since(waitForAvailableStart))
 }
 
+func waitForDeploymentReadyPodsToMatchDesiredReplicas(ctx context.Context, k8sClient client.Client, key types.NamespacedName) {
+	deployment := &appsv1.Deployment{}
+	waitForAvailableStart := time.Now()
+	ginkgo.By(fmt.Sprintf("Waiting for the number of ready Pods to match the expected number of replicas: %q", key))
+	gomega.EventuallyWithOffset(2, func(g gomega.Gomega) {
```

  Use ginkgo.GinkgoHelper() instead of Offset.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-02T16:41:13Z

- **test/util/e2e.go:467** by [@mimowo](https://github.com/mimowo) — 2026-02-02T16:41:13Z
```diff
@@ -457,6 +460,31 @@ func waitForDeploymentWithOnlyAvailableReplicas(ctx context.Context, k8sClient c
 	ginkgo.GinkgoLogr.Info("Deployment has only available replicas in the cluster", "deployment", key, "waitingTime", time.Since(waitForAvailableStart))
 }
 
+func waitForDeploymentReadyPodsToMatchDesiredReplicas(ctx context.Context, k8sClient client.Client, key types.NamespacedName) {
+	deployment := &appsv1.Deployment{}
+	waitForAvailableStart := time.Now()
+	ginkgo.By(fmt.Sprintf("Waiting for the number of ready Pods to match the expected number of replicas: %q", key))
+	gomega.EventuallyWithOffset(2, func(g gomega.Gomega) {
```

  done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-02T16:45:11Z

- **test/util/e2e.go:463** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-02T16:45:11Z
```diff
@@ -457,6 +460,32 @@ func waitForDeploymentWithOnlyAvailableReplicas(ctx context.Context, k8sClient c
 	ginkgo.GinkgoLogr.Info("Deployment has only available replicas in the cluster", "deployment", key, "waitingTime", time.Since(waitForAvailableStart))
 }
 
+func waitForDeploymentReadyPodsToMatchDesiredReplicas(ctx context.Context, k8sClient client.Client, key types.NamespacedName) {
```

  I'm wondering if we can check TerminatingReplicas on Deployment status instead of pod count.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-02T16:46:38Z

- **test/util/e2e.go:463** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-02T16:46:38Z
```diff
@@ -457,6 +460,32 @@ func waitForDeploymentWithOnlyAvailableReplicas(ctx context.Context, k8sClient c
 	ginkgo.GinkgoLogr.Info("Deployment has only available replicas in the cluster", "deployment", key, "waitingTime", time.Since(waitForAvailableStart))
 }
 
+func waitForDeploymentReadyPodsToMatchDesiredReplicas(ctx context.Context, k8sClient client.Client, key types.NamespacedName) {
```

  Or maybe we can check for DeploymentProgressing condition?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-02T16:46:51Z

- **test/util/e2e.go:463** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-02T16:46:50Z
```diff
@@ -457,6 +460,32 @@ func waitForDeploymentWithOnlyAvailableReplicas(ctx context.Context, k8sClient c
 	ginkgo.GinkgoLogr.Info("Deployment has only available replicas in the cluster", "deployment", key, "waitingTime", time.Since(waitForAvailableStart))
 }
 
+func waitForDeploymentReadyPodsToMatchDesiredReplicas(ctx context.Context, k8sClient client.Client, key types.NamespacedName) {
```

  WDYT?

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-02T17:14:19Z

Just throwing an idea out here: I’m trying to understand if there’s an opportunity to consolidate some of the wait functions in a meaningful way. I worry that we’re bloating the wait functions mainly to appease CI, and it might be worth thinking about this from a broader, longer-term perspective. That said, this PR looks fine to me.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-02T18:12:03Z

- **test/util/e2e.go:463** by [@mimowo](https://github.com/mimowo) — 2026-02-02T18:12:03Z
```diff
@@ -457,6 +460,32 @@ func waitForDeploymentWithOnlyAvailableReplicas(ctx context.Context, k8sClient c
 	ginkgo.GinkgoLogr.Info("Deployment has only available replicas in the cluster", "deployment", key, "waitingTime", time.Since(waitForAvailableStart))
 }
 
+func waitForDeploymentReadyPodsToMatchDesiredReplicas(ctx context.Context, k8sClient client.Client, key types.NamespacedName) {
```

  I think the status of Deployment is already ok - 1 replica. I think the issue is that later we check Pods in order to get the set of IPs to make sure the number of Endpoints is ok.
  
  The thing about event propagation in k8s is that the informer in the test code can see Deployment.status.Replicas=1, while the number of Pods is still 2, because there is no order on the event propagation for different types.
  
  Alternative is not to rely on the IPs found in Pods, here: https://github.com/kubernetes-sigs/kueue/blob/e2bb1d95b78738c3322a2f353745adca60efbb37/test/util/e2e.go#L421-L427

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-03T05:22:46Z

- **test/util/e2e.go:463** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-03T05:22:46Z
```diff
@@ -457,6 +460,32 @@ func waitForDeploymentWithOnlyAvailableReplicas(ctx context.Context, k8sClient c
 	ginkgo.GinkgoLogr.Info("Deployment has only available replicas in the cluster", "deployment", key, "waitingTime", time.Since(waitForAvailableStart))
 }
 
+func waitForDeploymentReadyPodsToMatchDesiredReplicas(ctx context.Context, k8sClient client.Client, key types.NamespacedName) {
```

  Not exactly. All of [these](https://github.com/kubernetes/api/blob/release-1.33/apps/v1/types.go#L490-L504) status fields represent non-terminating Pods.
  
  At first, I also considered checking [this](https://github.com/kubernetes/api/blob/fd489ea10e70d7ea2926c58431153438f6408d80/apps/v1/types.go#L514-L519) status, but as @sohankunkerkar mentioned, it’s a beta field and won’t work in 1.33 or 1.34.
  
  As an alternative, I think we can rely on [this](https://github.com/kubernetes/api/blob/release-1.33/apps/v1/types.go#L540-L544) condition instead.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-03T05:23:49Z

- **test/util/e2e.go:463** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-03T05:23:49Z
```diff
@@ -457,6 +460,32 @@ func waitForDeploymentWithOnlyAvailableReplicas(ctx context.Context, k8sClient c
 	ginkgo.GinkgoLogr.Info("Deployment has only available replicas in the cluster", "deployment", key, "waitingTime", time.Since(waitForAvailableStart))
 }
 
+func waitForDeploymentReadyPodsToMatchDesiredReplicas(ctx context.Context, k8sClient client.Client, key types.NamespacedName) {
```

  > I think the status of Deployment is already ok - 1 replica.
  
  Not exactly. All of [these](https://github.com/kubernetes/api/blob/release-1.33/apps/v1/types.go#L490-L504) status fields represent non-terminating Pods.
  
  At first, I also considered checking [this](https://github.com/kubernetes/api/blob/fd489ea10e70d7ea2926c58431153438f6408d80/apps/v1/types.go#L514-L519) status, but as @sohankunkerkar mentioned, it’s an beta field and won’t work in 1.33 or 1.34 without DeploymentReplicaSetTerminatingReplicas feature.
  
  As an alternative, I think we can rely on [this](https://github.com/kubernetes/api/blob/release-1.33/apps/v1/types.go#L540-L544) condition instead.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-03T06:43:41Z

- **test/util/e2e.go:463** by [@mimowo](https://github.com/mimowo) — 2026-02-03T06:43:41Z
```diff
@@ -457,6 +460,32 @@ func waitForDeploymentWithOnlyAvailableReplicas(ctx context.Context, k8sClient c
 	ginkgo.GinkgoLogr.Info("Deployment has only available replicas in the cluster", "deployment", key, "waitingTime", time.Since(waitForAvailableStart))
 }
 
+func waitForDeploymentReadyPodsToMatchDesiredReplicas(ctx context.Context, k8sClient client.Client, key types.NamespacedName) {
```

  > Not exactly. All of [these](https://github.com/kubernetes/api/blob/release-1.33/apps/v1/types.go#L490-L504) status fields represent non-terminating Pods.
  
  Yes, but I don't think the problem is here.  As I mentioned the gap is the event propagation for different types, consider the scenario:
  1. Kueue Pod gets deletetionTimestamp <- request to API server
  2. the event about the deletionTimestamp is received by kube-controller manager, the Deployment controller decreses status.Replicas to 1
  3. the code is progressing and checks the set of IPs for ready pods, but it still sees 2 ready pods, because the event about deletionTimestamp is not yet received by the test code
  4. the informer in the test code receives the event about the deletionTimestamp but it is too late
  
  
  > At first, I also considered checking [this](https://github.com/kubernetes/api/blob/fd489ea10e70d7ea2926c58431153438f6408d80/apps/v1/types.go#L514-L519) status, but as @sohankunkerkar mentioned, it’s an beta field and won’t work in 1.33 or 1.34 without DeploymentReplicaSetTerminatingReplicas feature.
  
  Check `status.terminatingReplicas` is a valid thing to do, but we should do `ptr.Deref( status.terminatingReplicas, 0) == 0`. So, this will stenghten the asserts, but I don't think the gap is here. It is good idea to add either in this PR or follow up.
  
  > As an alternative, I think we can rely on [this](https://github.com/kubernetes/api/blob/release-1.33/apps/v1/types.go#L540-L544) condition instead.
  
  I don't understand how this would help for the scenario described above. I still think this PR is properly fixing the gap.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-03T07:41:38Z

- **test/util/e2e.go:463** by [@mimowo](https://github.com/mimowo) — 2026-02-03T07:41:38Z
```diff
@@ -457,6 +460,32 @@ func waitForDeploymentWithOnlyAvailableReplicas(ctx context.Context, k8sClient c
 	ginkgo.GinkgoLogr.Info("Deployment has only available replicas in the cluster", "deployment", key, "waitingTime", time.Since(waitForAvailableStart))
 }
 
+func waitForDeploymentReadyPodsToMatchDesiredReplicas(ctx context.Context, k8sClient client.Client, key types.NamespacedName) {
```

  Let's continue the discussion on the other PR https://github.com/kubernetes-sigs/kueue/pull/8941/changes#r2757297506 which essentially does the same, but more thoroughly.
