# PR #8963: TAS: support ResourceTransformations

**Summary**: TAS: support ResourceTransformations

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8963

**Last updated**: 2026-03-05T11:38:53Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `feature/tas-support-ResourceTransformations` → `main`
- **Created**: 2026-02-03T13:59:51Z
- **Updated**: 2026-03-05T11:38:53Z
- **Merged**: 2026-03-05T10:28:20Z
- **Closed**: 2026-03-05T10:28:20Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@pajakd](https://github.com/pajakd), [@gabesaba](https://github.com/gabesaba), [@sohankunkerkar](https://github.com/sohankunkerkar), [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 7   **+396 / -75**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
TAS: support ResourceTransformations

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #8860

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
TAS: support ResourceTransformations to define "virtual" resources which allow putting a cap on
some "virtual" credits across multiple-flavors, see [sharing quotas](https://kueue.sigs.k8s.io/docs/tasks/manage/share_quotas_across_flavors/) for quota-only resources.
This is considered a bug since there was no validation preventing such configuration before.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-02-03T14:00:00Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 6309c2405176dced7c39b9c53ba09325e67a22ec |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69a93ac2c9fa7e00081105f8 |

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-23T14:20:33Z

LGTM, I think we could call it a bugfix since the use of ResourceTransformations wasn't blocked for TAS by validation, it just didn't work.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-23T14:26:17Z

/release-note-edit
```release-note
TAS: support ResourceTransformations to define "virtual" resources which allow putting a cap on
some "virtual" credits across multiple-flavors, see [sharing quotas](https://kueue.sigs.k8s.io/docs/tasks/manage/share_quotas_across_flavors/) for quota-only resources.
This is considered a bug since there was no validation preventing such configuration before.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-23T14:26:28Z

/remove-kind feature
/kind bug

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-23T14:27:17Z

/cherrypick release-0.16
/cherrypick release-0.15
cc @tenzen-y @gabesaba in case you have a different opinion on the categorization here.

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-02-23T14:27:20Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.15`, `release-0.16` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8963#issuecomment-3945084366):

>/cherrypick release-0.16
>/cherrypick release-0.15
>cc @tenzen-y @gabesaba in case you have a different opinion on the categorization here.


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-04T12:13:25Z

/cc @sohankunkerkar

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-05T09:54:17Z

Thank you 👍 
/lgtm
/approve 
/cherrypick release-0.16
/cherrypick release-0.15

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-05T09:54:20Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.15`, `release-0.16` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8963#issuecomment-4003825691):

>Thank you 👍 
>/lgtm
>/approve 
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-05T09:54:27Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8963#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8963#issuecomment-4003825691" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mbobrovskyi,mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-05T09:54:29Z

LGTM label has been added.  <details>Git tree hash: 424b97b7d93ed4074845b2933af568eec91a02fb</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-05T10:28:58Z

@mimowo: #8963 failed to apply on top of branch "release-0.16":
```
Applying: Create integration test.
Using index info to reconstruct a base tree...
M	test/integration/singlecluster/tas/suite_test.go
M	test/integration/singlecluster/tas/tas_test.go
Falling back to patching base and 3-way merge...
Auto-merging test/integration/singlecluster/tas/tas_test.go
Auto-merging test/integration/singlecluster/tas/suite_test.go
CONFLICT (content): Merge conflict in test/integration/singlecluster/tas/suite_test.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Create integration test.

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8963#issuecomment-4003825691):

>Thank you 👍 
>/lgtm
>/approve 
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-05T10:29:33Z

@mimowo: #8963 failed to apply on top of branch "release-0.15":
```
Applying: Create integration test.
Using index info to reconstruct a base tree...
M	test/integration/singlecluster/tas/suite_test.go
M	test/integration/singlecluster/tas/tas_test.go
Falling back to patching base and 3-way merge...
Auto-merging test/integration/singlecluster/tas/tas_test.go
Auto-merging test/integration/singlecluster/tas/suite_test.go
CONFLICT (content): Merge conflict in test/integration/singlecluster/tas/suite_test.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Create integration test.

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8963#issuecomment-4003825691):

>Thank you 👍 
>/lgtm
>/approve 
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-05T10:38:28Z

@mbobrovskyi could you prepare the cherrypicks?

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-17T10:42:58Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:110** by [@mimowo](https://github.com/mimowo) — 2026-02-17T10:42:58Z
```diff
@@ -91,8 +91,20 @@ func (a *Assignment) ComputeTASNetUsage(prevAdmission *kueue.Admission) workload
 			if prevAdmission != nil && prevAdmission.PodSetAssignments[i].TopologyAssignment != nil {
 				continue
 			}
-			singlePodRequests := resources.NewRequests(psa.Requests).ScaledDown(int64(psa.Count))
+			tasRequests := make(corev1.ResourceList, len(psa.Requests))
+			for resourceName, quantity := range psa.Requests {
+				// TODO: Check is it virtual resource.
+				if resourceName == "cpu_credits" {
+					continue
+				}
+				tasRequests[resourceName] = quantity
+			}
+			singlePodRequests := resources.NewRequests(tasRequests).ScaledDown(int64(psa.Count))
 			for _, flv := range psa.Flavors {
+				// TODO: Check is it virtual flavor.
+				if flv.Name == "credits" {
+					continue
+				}
```

  This looks very similar to the issue we were solving here: https://github.com/kubernetes-sigs/kueue/pull/8865. In particular check for this fix line: https://github.com/kubernetes-sigs/kueue/pull/8865/changes#diff-5a39cea8316e94ece1214aef72728e4476f4531d9714f30c2b00f33b00b47e4fR90-R91
  
  I think here we should to the analogous change: TAS manages resources declared in Pod requests. All other resources should be quota based, and only managed by non-TAS flavors.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-17T10:43:26Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:104** by [@mimowo](https://github.com/mimowo) — 2026-02-17T10:43:26Z
```diff
@@ -91,8 +91,20 @@ func (a *Assignment) ComputeTASNetUsage(prevAdmission *kueue.Admission) workload
 			if prevAdmission != nil && prevAdmission.PodSetAssignments[i].TopologyAssignment != nil {
 				continue
 			}
-			singlePodRequests := resources.NewRequests(psa.Requests).ScaledDown(int64(psa.Count))
+			tasRequests := make(corev1.ResourceList, len(psa.Requests))
+			for resourceName, quantity := range psa.Requests {
+				// TODO: Check is it virtual resource.
+				if resourceName == "cpu_credits" {
+					continue
+				}
+				tasRequests[resourceName] = quantity
+			}
+			singlePodRequests := resources.NewRequests(tasRequests).ScaledDown(int64(psa.Count))
 			for _, flv := range psa.Flavors {
+				// TODO: Check is it virtual flavor.
```

  I think here "virtual flavor" - "quota-only flavor"

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-17T10:45:19Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:149** by [@mimowo](https://github.com/mimowo) — 2026-02-17T10:45:19Z
```diff
@@ -136,10 +139,15 @@ func onlyFlavor(ra ResourceAssignment) (*kueue.ResourceFlavorReference, error) {
 	for i, n := range list {
 		names[i] = string(n)
 	}
-	return nil, fmt.Errorf("more than one flavor assigned: %s", strings.Join(names, ", "))
+	return nil, fmt.Errorf("more than one TAS flavor assigned: %s", strings.Join(names, ", "))
 }
 
 func checkPodSetAndFlavorMatchForTAS(cq *schdcache.ClusterQueueSnapshot, ps *kueue.PodSet, flavor *kueue.ResourceFlavor) *string {
+	// TODO: Check is it virtual flavor.
+	if flavor.Name == "credits" {
+		return nil
+	}
```

  Check if quota-only (non-TAS).ie. no topologyName specified

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-17T10:47:23Z

- **test/integration/singlecluster/tas/tas_test.go:64** by [@mimowo](https://github.com/mimowo) — 2026-02-17T10:47:23Z
```diff
@@ -60,7 +61,7 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 	)
 
 	ginkgo.BeforeAll(func() {
-		fwk.StartManager(ctx, cfg, managerSetup)
+		fwk.StartManager(ctx, cfg, managerSetup(nil))
```

  Maybe add variadic options to  `managerSetup`, so that we can avoid changing pre-existing code?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-17T10:52:18Z

- **test/integration/singlecluster/tas/tas_test.go:4414** by [@mimowo](https://github.com/mimowo) — 2026-02-17T10:52:19Z
```diff
@@ -4278,6 +4279,143 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 	})
 })
 
+var _ = ginkgo.Describe("Topology Aware Scheduling - Resource Transformation", ginkgo.Ordered, func() {
+	const cpuCredits = "cpu_credits"
+
+	var (
+		resourceTransformations []config.ResourceTransformation
+
+		nodes        []corev1.Node
+		ns           *corev1.Namespace
+		topology     *kueue.Topology
+		onDemand     *kueue.ResourceFlavor
+		spot         *kueue.ResourceFlavor
+		credits      *kueue.ResourceFlavor
+		clusterQueue *kueue.ClusterQueue
+		localQueue   *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		fwk.StartManager(ctx, cfg, managerSetup(resourceTransformations))
+
+		nodes = []corev1.Node{
+			*testingnode.MakeNode("on-demand").
+				Label("node-type", "on-demand").
+				Label(corev1.LabelHostname, "on-demand").
+				StatusAllocatable(corev1.ResourceList{
+					corev1.ResourceCPU:  resource.MustParse("9"),
+					corev1.ResourcePods: resource.MustParse("10"),
+				}).
+				Ready().
+				Obj(),
+			*testingnode.MakeNode("spot").
+				Label("node-type", "spot").
+				Label(corev1.LabelHostname, "spot").
+				StatusAllocatable(corev1.ResourceList{
+					corev1.ResourceCPU:  resource.MustParse("9"),
+					corev1.ResourcePods: resource.MustParse("10"),
+				}).
+				Ready().
+				Obj(),
+		}
+		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "tas-")
+
+		topology = utiltestingapi.MakeDefaultOneLevelTopology("topology")
+		util.MustCreate(ctx, k8sClient, topology)
+
+		onDemand = utiltestingapi.MakeResourceFlavor("on-demand").
+			NodeLabel("node-type", "on-demand").
+			TopologyName(topology.Name).
+			Obj()
+		util.MustCreate(ctx, k8sClient, onDemand)
+
+		spot = utiltestingapi.MakeResourceFlavor("spot").
+			NodeLabel("node-type", "spot").
+			TopologyName(topology.Name).
+			Obj()
+		util.MustCreate(ctx, k8sClient, spot)
+
+		credits = utiltestingapi.MakeResourceFlavor("credits").Obj()
+		util.MustCreate(ctx, k8sClient, credits)
+
+		clusterQueue = utiltestingapi.MakeClusterQueue("team-cluster-queue").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas(onDemand.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+				*utiltestingapi.MakeFlavorQuotas(spot.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+			).
+			ResourceGroup(*utiltestingapi.MakeFlavorQuotas(credits.Name).Resource(cpuCredits, "14").Obj()).
+			Obj()
+		util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, clusterQueue)
+
+		localQueue = utiltestingapi.MakeLocalQueue("team-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+		util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, localQueue)
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, onDemand, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, spot, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, credits, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+		for i := range nodes {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, &nodes[i], true)
+		}
+
+		resourceTransformations = nil
+
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.When("ResourceTransformation is enabled (cpu -> cpu_credits)", func() {
+		ginkgo.BeforeEach(func() {
+			resourceTransformations = []config.ResourceTransformation{{
+				Input:    corev1.ResourceCPU,
+				Strategy: ptr.To(config.Retain),
+				Outputs:  corev1.ResourceList{cpuCredits: resource.MustParse("1")},
+			}}
+		})
+
+		ginkgo.It("transforms cpu requests and admits workloads up to transformed capacity", func() {
+			const (
+				workloadsToCreate = 5
+				expectedAdmitted  = 4
+				cpuRequest        = "3" // 14 credits / 3 -> 4 workloads fit
+			)
+
+			workloads := make([]*kueue.Workload, 0, workloadsToCreate)
+
+			ginkgo.By(fmt.Sprintf("Creating %d workloads each requesting %s CPU", workloadsToCreate, cpuRequest), func() {
+				for i := range workloadsToCreate {
+					wl := utiltestingapi.MakeWorkload(fmt.Sprintf("wl-%d", i+1), ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).
+						PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+							Request(corev1.ResourceCPU, "3").
+							RequiredTopologyRequest(corev1.LabelHostname).
+							Obj()).
+						Obj()
+					util.MustCreate(ctx, k8sClient, wl)
+					workloads = append(workloads, wl)
+				}
+			})
+
+			ginkgo.By(fmt.Sprintf("Eventually exactly %d workloads should be admitted", expectedAdmitted), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(util.CountAdmittedWorkloads(ctx, k8sClient, g, ns.Name)).Should(gomega.Equal(expectedAdmitted))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Verifying that exactly %d workloads remain admitted", expectedAdmitted), func() {
+				gomega.Consistently(func(g gomega.Gomega) {
+					g.Expect(util.CountAdmittedWorkloads(ctx, k8sClient, g, ns.Name)).Should(gomega.Equal(expectedAdmitted))
+				}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
+			})
```

  nit, I would rather make the test deterministic- first create 4 workloads and assert they are all admitted, by their full name. Then create one more and check it is not admitted, just pending. 
  
  This will eliminete the need for helper like `CountAdmittedWorkloads`, and failure messages will be better.
  
  Later, finish one of the workloads, and demonstrate the pending is admitted - so that we show the quota is correctly released on finishing / deleting the workload.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-17T10:53:02Z

- **test/integration/singlecluster/tas/tas_test.go:4372** by [@mimowo](https://github.com/mimowo) — 2026-02-17T10:53:02Z
```diff
@@ -4278,6 +4279,143 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 	})
 })
 
+var _ = ginkgo.Describe("Topology Aware Scheduling - Resource Transformation", ginkgo.Ordered, func() {
+	const cpuCredits = "cpu_credits"
+
+	var (
+		resourceTransformations []config.ResourceTransformation
+
+		nodes        []corev1.Node
+		ns           *corev1.Namespace
+		topology     *kueue.Topology
+		onDemand     *kueue.ResourceFlavor
+		spot         *kueue.ResourceFlavor
+		credits      *kueue.ResourceFlavor
+		clusterQueue *kueue.ClusterQueue
+		localQueue   *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		fwk.StartManager(ctx, cfg, managerSetup(resourceTransformations))
+
+		nodes = []corev1.Node{
+			*testingnode.MakeNode("on-demand").
+				Label("node-type", "on-demand").
+				Label(corev1.LabelHostname, "on-demand").
+				StatusAllocatable(corev1.ResourceList{
+					corev1.ResourceCPU:  resource.MustParse("9"),
+					corev1.ResourcePods: resource.MustParse("10"),
+				}).
+				Ready().
+				Obj(),
+			*testingnode.MakeNode("spot").
+				Label("node-type", "spot").
+				Label(corev1.LabelHostname, "spot").
+				StatusAllocatable(corev1.ResourceList{
+					corev1.ResourceCPU:  resource.MustParse("9"),
+					corev1.ResourcePods: resource.MustParse("10"),
+				}).
+				Ready().
+				Obj(),
+		}
+		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "tas-")
+
+		topology = utiltestingapi.MakeDefaultOneLevelTopology("topology")
+		util.MustCreate(ctx, k8sClient, topology)
+
+		onDemand = utiltestingapi.MakeResourceFlavor("on-demand").
+			NodeLabel("node-type", "on-demand").
+			TopologyName(topology.Name).
+			Obj()
+		util.MustCreate(ctx, k8sClient, onDemand)
+
+		spot = utiltestingapi.MakeResourceFlavor("spot").
+			NodeLabel("node-type", "spot").
+			TopologyName(topology.Name).
+			Obj()
+		util.MustCreate(ctx, k8sClient, spot)
+
+		credits = utiltestingapi.MakeResourceFlavor("credits").Obj()
+		util.MustCreate(ctx, k8sClient, credits)
+
+		clusterQueue = utiltestingapi.MakeClusterQueue("team-cluster-queue").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas(onDemand.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+				*utiltestingapi.MakeFlavorQuotas(spot.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+			).
+			ResourceGroup(*utiltestingapi.MakeFlavorQuotas(credits.Name).Resource(cpuCredits, "14").Obj()).
+			Obj()
+		util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, clusterQueue)
+
+		localQueue = utiltestingapi.MakeLocalQueue("team-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+		util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, localQueue)
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, onDemand, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, spot, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, credits, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+		for i := range nodes {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, &nodes[i], true)
+		}
+
+		resourceTransformations = nil
+
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.When("ResourceTransformation is enabled (cpu -> cpu_credits)", func() {
```

  Please also add one test case to demonstrate the preemption works well in this case.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-17T10:53:28Z

- **test/integration/singlecluster/tas/tas_test.go:4414** by [@mimowo](https://github.com/mimowo) — 2026-02-17T10:53:28Z
```diff
@@ -4278,6 +4279,143 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 	})
 })
 
+var _ = ginkgo.Describe("Topology Aware Scheduling - Resource Transformation", ginkgo.Ordered, func() {
+	const cpuCredits = "cpu_credits"
+
+	var (
+		resourceTransformations []config.ResourceTransformation
+
+		nodes        []corev1.Node
+		ns           *corev1.Namespace
+		topology     *kueue.Topology
+		onDemand     *kueue.ResourceFlavor
+		spot         *kueue.ResourceFlavor
+		credits      *kueue.ResourceFlavor
+		clusterQueue *kueue.ClusterQueue
+		localQueue   *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		fwk.StartManager(ctx, cfg, managerSetup(resourceTransformations))
+
+		nodes = []corev1.Node{
+			*testingnode.MakeNode("on-demand").
+				Label("node-type", "on-demand").
+				Label(corev1.LabelHostname, "on-demand").
+				StatusAllocatable(corev1.ResourceList{
+					corev1.ResourceCPU:  resource.MustParse("9"),
+					corev1.ResourcePods: resource.MustParse("10"),
+				}).
+				Ready().
+				Obj(),
+			*testingnode.MakeNode("spot").
+				Label("node-type", "spot").
+				Label(corev1.LabelHostname, "spot").
+				StatusAllocatable(corev1.ResourceList{
+					corev1.ResourceCPU:  resource.MustParse("9"),
+					corev1.ResourcePods: resource.MustParse("10"),
+				}).
+				Ready().
+				Obj(),
+		}
+		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "tas-")
+
+		topology = utiltestingapi.MakeDefaultOneLevelTopology("topology")
+		util.MustCreate(ctx, k8sClient, topology)
+
+		onDemand = utiltestingapi.MakeResourceFlavor("on-demand").
+			NodeLabel("node-type", "on-demand").
+			TopologyName(topology.Name).
+			Obj()
+		util.MustCreate(ctx, k8sClient, onDemand)
+
+		spot = utiltestingapi.MakeResourceFlavor("spot").
+			NodeLabel("node-type", "spot").
+			TopologyName(topology.Name).
+			Obj()
+		util.MustCreate(ctx, k8sClient, spot)
+
+		credits = utiltestingapi.MakeResourceFlavor("credits").Obj()
+		util.MustCreate(ctx, k8sClient, credits)
+
+		clusterQueue = utiltestingapi.MakeClusterQueue("team-cluster-queue").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas(onDemand.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+				*utiltestingapi.MakeFlavorQuotas(spot.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+			).
+			ResourceGroup(*utiltestingapi.MakeFlavorQuotas(credits.Name).Resource(cpuCredits, "14").Obj()).
+			Obj()
+		util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, clusterQueue)
+
+		localQueue = utiltestingapi.MakeLocalQueue("team-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+		util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, localQueue)
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, onDemand, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, spot, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, credits, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+		for i := range nodes {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, &nodes[i], true)
+		}
+
+		resourceTransformations = nil
+
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.When("ResourceTransformation is enabled (cpu -> cpu_credits)", func() {
+		ginkgo.BeforeEach(func() {
+			resourceTransformations = []config.ResourceTransformation{{
+				Input:    corev1.ResourceCPU,
+				Strategy: ptr.To(config.Retain),
+				Outputs:  corev1.ResourceList{cpuCredits: resource.MustParse("1")},
+			}}
+		})
+
+		ginkgo.It("transforms cpu requests and admits workloads up to transformed capacity", func() {
+			const (
+				workloadsToCreate = 5
+				expectedAdmitted  = 4
+				cpuRequest        = "3" // 14 credits / 3 -> 4 workloads fit
+			)
+
+			workloads := make([]*kueue.Workload, 0, workloadsToCreate)
+
+			ginkgo.By(fmt.Sprintf("Creating %d workloads each requesting %s CPU", workloadsToCreate, cpuRequest), func() {
+				for i := range workloadsToCreate {
+					wl := utiltestingapi.MakeWorkload(fmt.Sprintf("wl-%d", i+1), ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).
+						PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+							Request(corev1.ResourceCPU, "3").
+							RequiredTopologyRequest(corev1.LabelHostname).
+							Obj()).
+						Obj()
+					util.MustCreate(ctx, k8sClient, wl)
+					workloads = append(workloads, wl)
+				}
+			})
+
+			ginkgo.By(fmt.Sprintf("Eventually exactly %d workloads should be admitted", expectedAdmitted), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(util.CountAdmittedWorkloads(ctx, k8sClient, g, ns.Name)).Should(gomega.Equal(expectedAdmitted))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Verifying that exactly %d workloads remain admitted", expectedAdmitted), func() {
+				gomega.Consistently(func(g gomega.Gomega) {
+					g.Expect(util.CountAdmittedWorkloads(ctx, k8sClient, g, ns.Name)).Should(gomega.Equal(expectedAdmitted))
+				}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
+			})
```

  Actually, I think it is safe to reduce the number of workloads to just 3 - two admitted and 1 pending.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-17T11:56:29Z

- **test/integration/singlecluster/tas/tas_test.go:64** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-17T11:56:29Z
```diff
@@ -60,7 +61,7 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 	)
 
 	ginkgo.BeforeAll(func() {
-		fwk.StartManager(ctx, cfg, managerSetup)
+		fwk.StartManager(ctx, cfg, managerSetup(nil))
```

  We still need to wrap this function because we must implement exactly `func(ctx context.Context, mgr manager.Manager)`.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-18T08:31:26Z

- **test/integration/singlecluster/tas/tas_test.go:4414** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-18T08:31:26Z
```diff
@@ -4278,6 +4279,143 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 	})
 })
 
+var _ = ginkgo.Describe("Topology Aware Scheduling - Resource Transformation", ginkgo.Ordered, func() {
+	const cpuCredits = "cpu_credits"
+
+	var (
+		resourceTransformations []config.ResourceTransformation
+
+		nodes        []corev1.Node
+		ns           *corev1.Namespace
+		topology     *kueue.Topology
+		onDemand     *kueue.ResourceFlavor
+		spot         *kueue.ResourceFlavor
+		credits      *kueue.ResourceFlavor
+		clusterQueue *kueue.ClusterQueue
+		localQueue   *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		fwk.StartManager(ctx, cfg, managerSetup(resourceTransformations))
+
+		nodes = []corev1.Node{
+			*testingnode.MakeNode("on-demand").
+				Label("node-type", "on-demand").
+				Label(corev1.LabelHostname, "on-demand").
+				StatusAllocatable(corev1.ResourceList{
+					corev1.ResourceCPU:  resource.MustParse("9"),
+					corev1.ResourcePods: resource.MustParse("10"),
+				}).
+				Ready().
+				Obj(),
+			*testingnode.MakeNode("spot").
+				Label("node-type", "spot").
+				Label(corev1.LabelHostname, "spot").
+				StatusAllocatable(corev1.ResourceList{
+					corev1.ResourceCPU:  resource.MustParse("9"),
+					corev1.ResourcePods: resource.MustParse("10"),
+				}).
+				Ready().
+				Obj(),
+		}
+		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "tas-")
+
+		topology = utiltestingapi.MakeDefaultOneLevelTopology("topology")
+		util.MustCreate(ctx, k8sClient, topology)
+
+		onDemand = utiltestingapi.MakeResourceFlavor("on-demand").
+			NodeLabel("node-type", "on-demand").
+			TopologyName(topology.Name).
+			Obj()
+		util.MustCreate(ctx, k8sClient, onDemand)
+
+		spot = utiltestingapi.MakeResourceFlavor("spot").
+			NodeLabel("node-type", "spot").
+			TopologyName(topology.Name).
+			Obj()
+		util.MustCreate(ctx, k8sClient, spot)
+
+		credits = utiltestingapi.MakeResourceFlavor("credits").Obj()
+		util.MustCreate(ctx, k8sClient, credits)
+
+		clusterQueue = utiltestingapi.MakeClusterQueue("team-cluster-queue").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas(onDemand.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+				*utiltestingapi.MakeFlavorQuotas(spot.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+			).
+			ResourceGroup(*utiltestingapi.MakeFlavorQuotas(credits.Name).Resource(cpuCredits, "14").Obj()).
+			Obj()
+		util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, clusterQueue)
+
+		localQueue = utiltestingapi.MakeLocalQueue("team-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+		util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, localQueue)
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, onDemand, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, spot, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, credits, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+		for i := range nodes {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, &nodes[i], true)
+		}
+
+		resourceTransformations = nil
+
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.When("ResourceTransformation is enabled (cpu -> cpu_credits)", func() {
+		ginkgo.BeforeEach(func() {
+			resourceTransformations = []config.ResourceTransformation{{
+				Input:    corev1.ResourceCPU,
+				Strategy: ptr.To(config.Retain),
+				Outputs:  corev1.ResourceList{cpuCredits: resource.MustParse("1")},
+			}}
+		})
+
+		ginkgo.It("transforms cpu requests and admits workloads up to transformed capacity", func() {
+			const (
+				workloadsToCreate = 5
+				expectedAdmitted  = 4
+				cpuRequest        = "3" // 14 credits / 3 -> 4 workloads fit
+			)
+
+			workloads := make([]*kueue.Workload, 0, workloadsToCreate)
+
+			ginkgo.By(fmt.Sprintf("Creating %d workloads each requesting %s CPU", workloadsToCreate, cpuRequest), func() {
+				for i := range workloadsToCreate {
+					wl := utiltestingapi.MakeWorkload(fmt.Sprintf("wl-%d", i+1), ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).
+						PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+							Request(corev1.ResourceCPU, "3").
+							RequiredTopologyRequest(corev1.LabelHostname).
+							Obj()).
+						Obj()
+					util.MustCreate(ctx, k8sClient, wl)
+					workloads = append(workloads, wl)
+				}
+			})
+
+			ginkgo.By(fmt.Sprintf("Eventually exactly %d workloads should be admitted", expectedAdmitted), func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(util.CountAdmittedWorkloads(ctx, k8sClient, g, ns.Name)).Should(gomega.Equal(expectedAdmitted))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By(fmt.Sprintf("Verifying that exactly %d workloads remain admitted", expectedAdmitted), func() {
+				gomega.Consistently(func(g gomega.Gomega) {
+					g.Expect(util.CountAdmittedWorkloads(ctx, k8sClient, g, ns.Name)).Should(gomega.Equal(expectedAdmitted))
+				}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
+			})
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-19T13:20:21Z

- **test/integration/singlecluster/tas/tas_test.go:4372** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-19T13:20:21Z
```diff
@@ -4278,6 +4279,143 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 	})
 })
 
+var _ = ginkgo.Describe("Topology Aware Scheduling - Resource Transformation", ginkgo.Ordered, func() {
+	const cpuCredits = "cpu_credits"
+
+	var (
+		resourceTransformations []config.ResourceTransformation
+
+		nodes        []corev1.Node
+		ns           *corev1.Namespace
+		topology     *kueue.Topology
+		onDemand     *kueue.ResourceFlavor
+		spot         *kueue.ResourceFlavor
+		credits      *kueue.ResourceFlavor
+		clusterQueue *kueue.ClusterQueue
+		localQueue   *kueue.LocalQueue
+	)
+
+	ginkgo.JustBeforeEach(func() {
+		fwk.StartManager(ctx, cfg, managerSetup(resourceTransformations))
+
+		nodes = []corev1.Node{
+			*testingnode.MakeNode("on-demand").
+				Label("node-type", "on-demand").
+				Label(corev1.LabelHostname, "on-demand").
+				StatusAllocatable(corev1.ResourceList{
+					corev1.ResourceCPU:  resource.MustParse("9"),
+					corev1.ResourcePods: resource.MustParse("10"),
+				}).
+				Ready().
+				Obj(),
+			*testingnode.MakeNode("spot").
+				Label("node-type", "spot").
+				Label(corev1.LabelHostname, "spot").
+				StatusAllocatable(corev1.ResourceList{
+					corev1.ResourceCPU:  resource.MustParse("9"),
+					corev1.ResourcePods: resource.MustParse("10"),
+				}).
+				Ready().
+				Obj(),
+		}
+		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "tas-")
+
+		topology = utiltestingapi.MakeDefaultOneLevelTopology("topology")
+		util.MustCreate(ctx, k8sClient, topology)
+
+		onDemand = utiltestingapi.MakeResourceFlavor("on-demand").
+			NodeLabel("node-type", "on-demand").
+			TopologyName(topology.Name).
+			Obj()
+		util.MustCreate(ctx, k8sClient, onDemand)
+
+		spot = utiltestingapi.MakeResourceFlavor("spot").
+			NodeLabel("node-type", "spot").
+			TopologyName(topology.Name).
+			Obj()
+		util.MustCreate(ctx, k8sClient, spot)
+
+		credits = utiltestingapi.MakeResourceFlavor("credits").Obj()
+		util.MustCreate(ctx, k8sClient, credits)
+
+		clusterQueue = utiltestingapi.MakeClusterQueue("team-cluster-queue").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas(onDemand.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+				*utiltestingapi.MakeFlavorQuotas(spot.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+			).
+			ResourceGroup(*utiltestingapi.MakeFlavorQuotas(credits.Name).Resource(cpuCredits, "14").Obj()).
+			Obj()
+		util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, clusterQueue)
+
+		localQueue = utiltestingapi.MakeLocalQueue("team-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+		util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, localQueue)
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, onDemand, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, spot, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, credits, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+		for i := range nodes {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, &nodes[i], true)
+		}
+
+		resourceTransformations = nil
+
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.When("ResourceTransformation is enabled (cpu -> cpu_credits)", func() {
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-20T11:14:39Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:76** by [@mimowo](https://github.com/mimowo) — 2026-02-20T11:14:39Z
```diff
@@ -71,6 +71,9 @@ type Assignment struct {
 	// In these scenarios, flavor assignment proceeds as in the original flow—i.e., as for regular,
 	// non-sliced workloads.
 	replaceWorkloadSlice *workload.Info
+
+	// virtualResources are resources from resourceTransformation with Retain types
+	virtualResources sets.Set[corev1.ResourceName]
```

  Hm, I'm not sure this is the good direction to define them by resourceTransformation. Certainly, this is othogonal to Retain / Replace options,
  
  Can't we define them for the new task, just as any resources which are not covered by the onlyTASFlavor?

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-02-20T14:23:26Z

- **test/integration/singlecluster/tas/tas_test.go:64** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-20T14:23:26Z
```diff
@@ -60,7 +61,7 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 	)
 
 	ginkgo.BeforeAll(func() {
-		fwk.StartManager(ctx, cfg, managerSetup)
+		fwk.StartManager(ctx, cfg, managerSetup(nil))
```

  Maybe provide WithResourceTransformations as option, this way there's no need to pass nil

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-20T14:29:20Z

- **test/integration/singlecluster/tas/tas_test.go:64** by [@mimowo](https://github.com/mimowo) — 2026-02-20T14:29:20Z
```diff
@@ -60,7 +61,7 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 	)
 
 	ginkgo.BeforeAll(func() {
-		fwk.StartManager(ctx, cfg, managerSetup)
+		fwk.StartManager(ctx, cfg, managerSetup(nil))
```

  > We still need to wrap this function because we must implement exactly func(ctx context.Context, mgr manager.Manager).
  
  sure we can have another wrapper, I just don't see  a need to modify the pre-exising code

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-23T14:17:00Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:76** by [@mimowo](https://github.com/mimowo) — 2026-02-23T14:17:00Z
```diff
@@ -71,6 +71,9 @@ type Assignment struct {
 	// In these scenarios, flavor assignment proceeds as in the original flow—i.e., as for regular,
 	// non-sliced workloads.
 	replaceWorkloadSlice *workload.Info
+
+	// virtualResources are resources from resourceTransformation with Retain types
+	virtualResources sets.Set[corev1.ResourceName]
```

  IIUC this is addressed now

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-23T14:19:01Z

- **pkg/scheduler/scheduler_tas_test.go:2787** by [@mimowo](https://github.com/mimowo) — 2026-02-23T14:19:02Z
```diff
@@ -2784,138 +2784,135 @@ func TestScheduleForTAS(t *testing.T) {
 		},
 	}
 	for name, tc := range cases {
-		for _, enabled := range []bool{false, true} {
-			t.Run(fmt.Sprintf("%s WorkloadRequestUseMergePatch enabled: %t", name, enabled), func(t *testing.T) {
-				features.SetFeatureGateDuringTest(t, features.WorkloadRequestUseMergePatch, enabled)
-				for fg, enable := range tc.featureGates {
-					features.SetFeatureGateDuringTest(t, fg, enable)
-				}
-				ctx, log := utiltesting.ContextWithLog(t)
-				testWls := make([]kueue.Workload, 0, len(tc.workloads))
-				for _, wl := range tc.workloads {
-					testWls = append(testWls, *wl.DeepCopy())
-				}
+		t.Run(name, func(t *testing.T) {
```

  Hm, it seems like this change removing of `for _, enabled := range []bool{false, true} {` should be reverted

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-03T08:47:00Z

- **pkg/scheduler/scheduler_tas_test.go:2787** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-03T08:47:00Z
```diff
@@ -2784,138 +2784,135 @@ func TestScheduleForTAS(t *testing.T) {
 		},
 	}
 	for name, tc := range cases {
-		for _, enabled := range []bool{false, true} {
-			t.Run(fmt.Sprintf("%s WorkloadRequestUseMergePatch enabled: %t", name, enabled), func(t *testing.T) {
-				features.SetFeatureGateDuringTest(t, features.WorkloadRequestUseMergePatch, enabled)
-				for fg, enable := range tc.featureGates {
-					features.SetFeatureGateDuringTest(t, fg, enable)
-				}
-				ctx, log := utiltesting.ContextWithLog(t)
-				testWls := make([]kueue.Workload, 0, len(tc.workloads))
-				for _, wl := range tc.workloads {
-					testWls = append(testWls, *wl.DeepCopy())
-				}
+		t.Run(name, func(t *testing.T) {
```

  Fixed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-03T08:47:19Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:76** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-03T08:47:19Z
```diff
@@ -71,6 +71,9 @@ type Assignment struct {
 	// In these scenarios, flavor assignment proceeds as in the original flow—i.e., as for regular,
 	// non-sliced workloads.
 	replaceWorkloadSlice *workload.Info
+
+	// virtualResources are resources from resourceTransformation with Retain types
+	virtualResources sets.Set[corev1.ResourceName]
```

  Yes, it's done.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-03T08:53:52Z

- **test/integration/singlecluster/tas/tas_test.go:64** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-03T08:53:52Z
```diff
@@ -60,7 +61,7 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 	)
 
 	ginkgo.BeforeAll(func() {
-		fwk.StartManager(ctx, cfg, managerSetup)
+		fwk.StartManager(ctx, cfg, managerSetup(nil))
```

  > Maybe provide WithResourceTransformations as option, this way there's no need to pass nil
  
  I don’t think we need this for just one option. Otherwise, we would need to create a struct for options, a separate function, and so on. In the end, we would still need to change this line from `fwk.StartManager(ctx, cfg, managerSetup)` to `fwk.StartManager(ctx, cfg, managerSetup())`.
  
  > sure we can have another wrapper, I just don't see a need to modify the pre-exising code
  
  How do you propose propagate ResourceTransformations?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-03T09:05:58Z

- **test/integration/singlecluster/tas/tas_test.go:64** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-03T09:05:58Z
```diff
@@ -60,7 +61,7 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 	)
 
 	ginkgo.BeforeAll(func() {
-		fwk.StartManager(ctx, cfg, managerSetup)
+		fwk.StartManager(ctx, cfg, managerSetup(nil))
```

  > Maybe provide WithResourceTransformations as option, this way there's no need to pass nil
  
  I have another idea to use variadic parameters instead.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-03-04T14:57:39Z

Minor nits but overall LGTM

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:208** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-04T13:42:47Z
```diff
@@ -191,6 +205,12 @@ func checkPodSetAndFlavorMatchForTAS(cq *schdcache.ClusterQueueSnapshot, ps *kue
 	return nil
 }
 
+func coversOnlyNonTASResources(ps *kueue.PodSet, flavorResources sets.Set[corev1.ResourceName]) bool {
```

  nit: Can you add a godoc here explaining the intent?

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:210** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-04T13:44:07Z
```diff
@@ -191,6 +205,12 @@ func checkPodSetAndFlavorMatchForTAS(cq *schdcache.ClusterQueueSnapshot, ps *kue
 	return nil
 }
 
+func coversOnlyNonTASResources(ps *kueue.PodSet, flavorResources sets.Set[corev1.ResourceName]) bool {
+	requests := resources.NewRequestsFromPodSpec(&ps.Template.Spec)
+	tasResources := maps.Keys(requests)
```

  IIUC, these are pod-requested resources and not TAS resources. I think it would be clearer if the name of the function would be something like `podRequestedResources`.

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:208** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-04T13:52:31Z
```diff
@@ -191,6 +205,12 @@ func checkPodSetAndFlavorMatchForTAS(cq *schdcache.ClusterQueueSnapshot, ps *kue
 	return nil
 }
 
+func coversOnlyNonTASResources(ps *kueue.PodSet, flavorResources sets.Set[corev1.ResourceName]) bool {
```

  Not a blocker but worth adding a unit test for this.

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:175** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-04T13:54:45Z
```diff
@@ -167,6 +172,15 @@ func checkPodSetAndFlavorMatchForTAS(cq *schdcache.ClusterQueueSnapshot, ps *kue
 		}
 		// PodSet explicitly requires TAS, so we need to check if the flavor supports it.
 		if flavor.Spec.TopologyName == nil {
+			if cq.TASFlavors[kueue.ResourceFlavorReference(flavor.Name)] == nil {
```

  This looks redundant. If `flavor.Spec.TopologyName == nil`, then the flavor should never be in TASFlavors (the cache is built from flavors with TopologyName). So the inner check `cq.TASFlavors[...] == nil` is always true when the outer check is true.

- **pkg/scheduler/flavorassigner/flavorassigner.go:100** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-04T14:57:24Z
```diff
@@ -74,27 +74,40 @@ type Assignment struct {
 }
 
 // UpdateForTASResult updates the Assignment with the TAS result
-func (a *Assignment) UpdateForTASResult(result schdcache.TASAssignmentsResult) {
+func (a *Assignment) UpdateForTASResult(cq *schdcache.ClusterQueueSnapshot, result schdcache.TASAssignmentsResult) {
 	for psName, psResult := range result {
 		psAssignment := a.podSetAssignmentByName(psName)
 		psAssignment.TopologyAssignment = psResult.TopologyAssignment
 		if psResult.TopologyAssignment != nil && psAssignment.DelayedTopologyRequest != nil {
 			psAssignment.DelayedTopologyRequest = ptr.To(kueue.DelayedTopologyRequestStateReady)
 		}
 	}
-	a.Usage.TAS = a.ComputeTASNetUsage(nil)
+	a.Usage.TAS = a.ComputeTASNetUsage(cq, nil)
 }
 
 // ComputeTASNetUsage computes the net TAS usage for the assignment
-func (a *Assignment) ComputeTASNetUsage(prevAdmission *kueue.Admission) workload.TASUsage {
+func (a *Assignment) ComputeTASNetUsage(cq *schdcache.ClusterQueueSnapshot, prevAdmission *kueue.Admission) workload.TASUsage {
 	result := make(workload.TASUsage)
 	for i, psa := range a.PodSets {
 		if psa.TopologyAssignment != nil {
 			if prevAdmission != nil && prevAdmission.PodSetAssignments[i].TopologyAssignment != nil {
 				continue
 			}
-			singlePodRequests := resources.NewRequests(psa.Requests).ScaledDown(int64(psa.Count))
+			tasRequests := make(corev1.ResourceList, len(psa.Requests))
+			for resourceName, quantity := range psa.Requests {
+				flv := psa.Flavors[resourceName]
+				if flv == nil || cq.TASFlavors[flv.Name] == nil {
+					// skip any virtual resource, skip when computing TAS usage
```

  Should this be `quota-only` instead of `virtual`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-04T18:41:27Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:100** by [@mimowo](https://github.com/mimowo) — 2026-03-04T18:41:27Z
```diff
@@ -74,27 +74,40 @@ type Assignment struct {
 }
 
 // UpdateForTASResult updates the Assignment with the TAS result
-func (a *Assignment) UpdateForTASResult(result schdcache.TASAssignmentsResult) {
+func (a *Assignment) UpdateForTASResult(cq *schdcache.ClusterQueueSnapshot, result schdcache.TASAssignmentsResult) {
 	for psName, psResult := range result {
 		psAssignment := a.podSetAssignmentByName(psName)
 		psAssignment.TopologyAssignment = psResult.TopologyAssignment
 		if psResult.TopologyAssignment != nil && psAssignment.DelayedTopologyRequest != nil {
 			psAssignment.DelayedTopologyRequest = ptr.To(kueue.DelayedTopologyRequestStateReady)
 		}
 	}
-	a.Usage.TAS = a.ComputeTASNetUsage(nil)
+	a.Usage.TAS = a.ComputeTASNetUsage(cq, nil)
 }
 
 // ComputeTASNetUsage computes the net TAS usage for the assignment
-func (a *Assignment) ComputeTASNetUsage(prevAdmission *kueue.Admission) workload.TASUsage {
+func (a *Assignment) ComputeTASNetUsage(cq *schdcache.ClusterQueueSnapshot, prevAdmission *kueue.Admission) workload.TASUsage {
 	result := make(workload.TASUsage)
 	for i, psa := range a.PodSets {
 		if psa.TopologyAssignment != nil {
 			if prevAdmission != nil && prevAdmission.PodSetAssignments[i].TopologyAssignment != nil {
 				continue
 			}
-			singlePodRequests := resources.NewRequests(psa.Requests).ScaledDown(int64(psa.Count))
+			tasRequests := make(corev1.ResourceList, len(psa.Requests))
+			for resourceName, quantity := range psa.Requests {
+				flv := psa.Flavors[resourceName]
+				if flv == nil || cq.TASFlavors[flv.Name] == nil {
+					// skip any virtual resource, skip when computing TAS usage
```

  Yeah, I think quota-only is more suggestive rather than a new terminology.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-04T18:48:50Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:210** by [@mimowo](https://github.com/mimowo) — 2026-03-04T18:48:50Z
```diff
@@ -191,6 +205,12 @@ func checkPodSetAndFlavorMatchForTAS(cq *schdcache.ClusterQueueSnapshot, ps *kue
 	return nil
 }
 
+func coversOnlyNonTASResources(ps *kueue.PodSet, flavorResources sets.Set[corev1.ResourceName]) bool {
+	requests := resources.NewRequestsFromPodSpec(&ps.Template.Spec)
+	tasResources := maps.Keys(requests)
```

  This is a bit tricky, I think some examples could be helpful to demonstrate the intention. You may think of this check as "is this flavor for gpu_credits", ie. covering the "virtual" resources.
  
  I think an alternative name could be `coversOnlyVirtualResources` - this is true if the intersection of the "PodRequestedResources" and the Flavor's resources is empty.
  
  Or `hasOverlapWithPodRequestedResources`, and check if false

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-05T07:38:59Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:208** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-05T07:38:59Z
```diff
@@ -191,6 +205,12 @@ func checkPodSetAndFlavorMatchForTAS(cq *schdcache.ClusterQueueSnapshot, ps *kue
 	return nil
 }
 
+func coversOnlyNonTASResources(ps *kueue.PodSet, flavorResources sets.Set[corev1.ResourceName]) bool {
```

  > Not a blocker but worth adding a unit test for this.
  
  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-05T07:40:33Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:210** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-05T07:40:33Z
```diff
@@ -191,6 +205,12 @@ func checkPodSetAndFlavorMatchForTAS(cq *schdcache.ClusterQueueSnapshot, ps *kue
 	return nil
 }
 
+func coversOnlyNonTASResources(ps *kueue.PodSet, flavorResources sets.Set[corev1.ResourceName]) bool {
+	requests := resources.NewRequestsFromPodSpec(&ps.Template.Spec)
+	tasResources := maps.Keys(requests)
```

  > Or hasOverlapWithPodRequestedResources, and check if false
  
  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-05T07:40:44Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:100** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-05T07:40:44Z
```diff
@@ -74,27 +74,40 @@ type Assignment struct {
 }
 
 // UpdateForTASResult updates the Assignment with the TAS result
-func (a *Assignment) UpdateForTASResult(result schdcache.TASAssignmentsResult) {
+func (a *Assignment) UpdateForTASResult(cq *schdcache.ClusterQueueSnapshot, result schdcache.TASAssignmentsResult) {
 	for psName, psResult := range result {
 		psAssignment := a.podSetAssignmentByName(psName)
 		psAssignment.TopologyAssignment = psResult.TopologyAssignment
 		if psResult.TopologyAssignment != nil && psAssignment.DelayedTopologyRequest != nil {
 			psAssignment.DelayedTopologyRequest = ptr.To(kueue.DelayedTopologyRequestStateReady)
 		}
 	}
-	a.Usage.TAS = a.ComputeTASNetUsage(nil)
+	a.Usage.TAS = a.ComputeTASNetUsage(cq, nil)
 }
 
 // ComputeTASNetUsage computes the net TAS usage for the assignment
-func (a *Assignment) ComputeTASNetUsage(prevAdmission *kueue.Admission) workload.TASUsage {
+func (a *Assignment) ComputeTASNetUsage(cq *schdcache.ClusterQueueSnapshot, prevAdmission *kueue.Admission) workload.TASUsage {
 	result := make(workload.TASUsage)
 	for i, psa := range a.PodSets {
 		if psa.TopologyAssignment != nil {
 			if prevAdmission != nil && prevAdmission.PodSetAssignments[i].TopologyAssignment != nil {
 				continue
 			}
-			singlePodRequests := resources.NewRequests(psa.Requests).ScaledDown(int64(psa.Count))
+			tasRequests := make(corev1.ResourceList, len(psa.Requests))
+			for resourceName, quantity := range psa.Requests {
+				flv := psa.Flavors[resourceName]
+				if flv == nil || cq.TASFlavors[flv.Name] == nil {
+					// skip any virtual resource, skip when computing TAS usage
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-05T07:44:10Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:175** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-05T07:44:10Z
```diff
@@ -167,6 +172,15 @@ func checkPodSetAndFlavorMatchForTAS(cq *schdcache.ClusterQueueSnapshot, ps *kue
 		}
 		// PodSet explicitly requires TAS, so we need to check if the flavor supports it.
 		if flavor.Spec.TopologyName == nil {
+			if cq.TASFlavors[kueue.ResourceFlavorReference(flavor.Name)] == nil {
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T09:46:17Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:99** by [@mimowo](https://github.com/mimowo) — 2026-03-05T09:46:17Z
```diff
@@ -74,27 +74,40 @@ type Assignment struct {
 }
 
 // UpdateForTASResult updates the Assignment with the TAS result
-func (a *Assignment) UpdateForTASResult(result schdcache.TASAssignmentsResult) {
+func (a *Assignment) UpdateForTASResult(cq *schdcache.ClusterQueueSnapshot, result schdcache.TASAssignmentsResult) {
 	for psName, psResult := range result {
 		psAssignment := a.podSetAssignmentByName(psName)
 		psAssignment.TopologyAssignment = psResult.TopologyAssignment
 		if psResult.TopologyAssignment != nil && psAssignment.DelayedTopologyRequest != nil {
 			psAssignment.DelayedTopologyRequest = ptr.To(kueue.DelayedTopologyRequestStateReady)
 		}
 	}
-	a.Usage.TAS = a.ComputeTASNetUsage(nil)
+	a.Usage.TAS = a.ComputeTASNetUsage(cq, nil)
 }
 
 // ComputeTASNetUsage computes the net TAS usage for the assignment
-func (a *Assignment) ComputeTASNetUsage(prevAdmission *kueue.Admission) workload.TASUsage {
+func (a *Assignment) ComputeTASNetUsage(cq *schdcache.ClusterQueueSnapshot, prevAdmission *kueue.Admission) workload.TASUsage {
 	result := make(workload.TASUsage)
 	for i, psa := range a.PodSets {
 		if psa.TopologyAssignment != nil {
 			if prevAdmission != nil && prevAdmission.PodSetAssignments[i].TopologyAssignment != nil {
 				continue
 			}
-			singlePodRequests := resources.NewRequests(psa.Requests).ScaledDown(int64(psa.Count))
+			tasRequests := make(corev1.ResourceList, len(psa.Requests))
+			for resourceName, quantity := range psa.Requests {
+				flv := psa.Flavors[resourceName]
+				if flv == nil || cq.TASFlavors[flv.Name] == nil {
```

  nit, I'm ok with the nil check. However, let m check if we have a unit test case which would demonstrate the need?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-05T09:53:11Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:99** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-05T09:53:10Z
```diff
@@ -74,27 +74,40 @@ type Assignment struct {
 }
 
 // UpdateForTASResult updates the Assignment with the TAS result
-func (a *Assignment) UpdateForTASResult(result schdcache.TASAssignmentsResult) {
+func (a *Assignment) UpdateForTASResult(cq *schdcache.ClusterQueueSnapshot, result schdcache.TASAssignmentsResult) {
 	for psName, psResult := range result {
 		psAssignment := a.podSetAssignmentByName(psName)
 		psAssignment.TopologyAssignment = psResult.TopologyAssignment
 		if psResult.TopologyAssignment != nil && psAssignment.DelayedTopologyRequest != nil {
 			psAssignment.DelayedTopologyRequest = ptr.To(kueue.DelayedTopologyRequestStateReady)
 		}
 	}
-	a.Usage.TAS = a.ComputeTASNetUsage(nil)
+	a.Usage.TAS = a.ComputeTASNetUsage(cq, nil)
 }
 
 // ComputeTASNetUsage computes the net TAS usage for the assignment
-func (a *Assignment) ComputeTASNetUsage(prevAdmission *kueue.Admission) workload.TASUsage {
+func (a *Assignment) ComputeTASNetUsage(cq *schdcache.ClusterQueueSnapshot, prevAdmission *kueue.Admission) workload.TASUsage {
 	result := make(workload.TASUsage)
 	for i, psa := range a.PodSets {
 		if psa.TopologyAssignment != nil {
 			if prevAdmission != nil && prevAdmission.PodSetAssignments[i].TopologyAssignment != nil {
 				continue
 			}
-			singlePodRequests := resources.NewRequests(psa.Requests).ScaledDown(int64(psa.Count))
+			tasRequests := make(corev1.ResourceList, len(psa.Requests))
+			for resourceName, quantity := range psa.Requests {
+				flv := psa.Flavors[resourceName]
+				if flv == nil || cq.TASFlavors[flv.Name] == nil {
```

  We have an unit test which failed without this check:
  
  ```
  === Failed
  === FAIL: pkg/scheduler TestScheduleForTAS/admits_workload_with_zero-quantity_request_for_resource_not_in_ClusterQueue_with_TAS_WorkloadRequestUseMergePatch_enabled:_false (0.02s)
      clusterqueue.go:245: "level"=2 "msg"="Syncing TAS usage initilized TAS cache" "workloads"=0
      clusterqueue.go:226: "level"=3 "msg"="Updating status in cache" "clusterQueue"="tas-main" "newStatus"="active" "oldStatus"=""
      resources.go:128: "msg"="Failed adjusting requests for LimitRanges" "error"="List on GroupVersionKind /v1, Kind=LimitRange specifies selector on field spec.hasContainerType, but no index with name spec.hasContainerType has been registered for GroupVersionKind /v1, Kind=LimitRange"
      manager.go:694: "level"=3 "msg"="Obtained ClusterQueue heads" "replica-role"="standalone" "schedulingCycle"=1 "count"=1
      tas_flavor.go:143: "level"=3 "msg"="Constructing TAS snapshot" "replica-role"="standalone" "schedulingCycle"=1 "nodeLabels"={"tas-node"="true"} "levels"=["kubernetes.io/hostname"] "nodeCount"=1
  
  === FAIL: pkg/scheduler TestScheduleForTAS (0.10s)
  panic: runtime error: invalid memory address or nil pointer dereference [recovered, repanicked]
  [signal SIGSEGV: segmentation violation code=0x2 addr=0x0 pc=0x10260def4]
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T09:53:49Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:99** by [@mimowo](https://github.com/mimowo) — 2026-03-05T09:53:48Z
```diff
@@ -74,27 +74,40 @@ type Assignment struct {
 }
 
 // UpdateForTASResult updates the Assignment with the TAS result
-func (a *Assignment) UpdateForTASResult(result schdcache.TASAssignmentsResult) {
+func (a *Assignment) UpdateForTASResult(cq *schdcache.ClusterQueueSnapshot, result schdcache.TASAssignmentsResult) {
 	for psName, psResult := range result {
 		psAssignment := a.podSetAssignmentByName(psName)
 		psAssignment.TopologyAssignment = psResult.TopologyAssignment
 		if psResult.TopologyAssignment != nil && psAssignment.DelayedTopologyRequest != nil {
 			psAssignment.DelayedTopologyRequest = ptr.To(kueue.DelayedTopologyRequestStateReady)
 		}
 	}
-	a.Usage.TAS = a.ComputeTASNetUsage(nil)
+	a.Usage.TAS = a.ComputeTASNetUsage(cq, nil)
 }
 
 // ComputeTASNetUsage computes the net TAS usage for the assignment
-func (a *Assignment) ComputeTASNetUsage(prevAdmission *kueue.Admission) workload.TASUsage {
+func (a *Assignment) ComputeTASNetUsage(cq *schdcache.ClusterQueueSnapshot, prevAdmission *kueue.Admission) workload.TASUsage {
 	result := make(workload.TASUsage)
 	for i, psa := range a.PodSets {
 		if psa.TopologyAssignment != nil {
 			if prevAdmission != nil && prevAdmission.PodSetAssignments[i].TopologyAssignment != nil {
 				continue
 			}
-			singlePodRequests := resources.NewRequests(psa.Requests).ScaledDown(int64(psa.Count))
+			tasRequests := make(corev1.ResourceList, len(psa.Requests))
+			for resourceName, quantity := range psa.Requests {
+				flv := psa.Flavors[resourceName]
+				if flv == nil || cq.TASFlavors[flv.Name] == nil {
```

  Thank you for confirming this is needed, and we have a unit test guarding against regression here.
