# PR #8971: KEP: add kueueviz auth

**Summary**: KEP: add kueueviz auth

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8971

**Last updated**: 2026-03-06T16:44:23Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@samzong](https://github.com/samzong)
- **Branch**: `kep-5993-kueueviz-authentication` → `main`
- **Created**: 2026-02-04T01:28:55Z
- **Updated**: 2026-03-06T16:44:23Z
- **Merged**: 2026-03-06T16:44:20Z
- **Closed**: 2026-03-06T16:44:20Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`, `kind/kep`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 2   **+356 / -0**

## Description

#### What type of PR is this?

/kind feature
/kind documentation

#### What this PR does / why we need it:

Adds KEP for KueueViz auth using Kubernetes TokenReview API.

KueueViz currently has no auth, limiting it to `kubectl port-forward`. 
This KEP proposes adding optional bearer token auth so KueueViz can be 
safely exposed via Ingress.

#### Which issue(s) this PR fixes:

Fixes #5993

#### Special notes for your reviewer:

This is a KEP proposal only, no code changes. Implementation will follow in 
separate PRs after KEP approval.

#### Does this PR introduce a user-facing change?

```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-02-04T01:29:04Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | df0f9dc8374525dfe32a8bb6cd3156cc07f3b784 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69aa00ec3473f80008ed17ac |

### Comment by [@samzong](https://github.com/samzong) — 2026-02-09T08:04:15Z

@mimowo @mwielgus I think we can move on

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-18T07:30:58Z

cc @samzong please address the remaining comments. Do you maybe have some prototype of that?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-27T16:38:16Z

/kind kep
Adding new label for the ease of filtering PRs which are KEPs  / designs are require special handling

### Comment by [@samzong](https://github.com/samzong) — 2026-03-03T07:19:09Z

@mimowo Okay, I will take it at this week.

### Comment by [@samzong](https://github.com/samzong) — 2026-03-05T03:20:59Z

@mimowo all comments addressed. prototype is still in progress.

### Comment by [@samzong](https://github.com/samzong) — 2026-03-05T08:29:50Z

@mimowo @tenzen-y basically completed, I added a demo video.

https://github.com/user-attachments/assets/ce07c067-e719-4dfc-9916-2d338577726f

And a question, should I split code and KEP doc to different PR?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-05T08:37:35Z

Nice demo @samzong, should we also show a command to generate tokens with expiration, say 7 days by default?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-05T08:45:34Z

> And a question, should I split code and KEP doc to different PR?

Yes, let's keep the implementation and the KEP separate PRs, but feel free to already open the implementation PR so maybe some folks can start reviewing in parallel.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-06T16:06:45Z

/lgtm
/approve
Thank  you 👍

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-06T16:06:54Z

LGTM label has been added.  <details>Git tree hash: 87e198b3d57ca084d9eaba5f7d11526ebf54bcec</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-06T16:06:56Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8971#issuecomment-4012616068" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8971#" title="Author self-approved">samzong</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-04T18:13:05Z

- **keps/5993-kueueviz-authentication/kep.yaml:28** by [@mimowo](https://github.com/mimowo) — 2026-02-04T18:13:05Z
```diff
@@ -0,0 +1,28 @@
+title: KueueViz Authentication
+kep-number: 5993
+authors:
+  - "@samzong"
+status: provisional
+creation-date: 2026-02-04
+reviewers:
+  - TBD
+  - "@mimowo"
+approvers:
+  - TBD
+
+see-also:
+  - "https://github.com/kubernetes-sigs/kueue/issues/5993"
+  - "https://github.com/kubernetes-sigs/kueue/issues/8969"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: beta
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.18"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  beta: "v0.18"
```

  should we rather aim for 0.17?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-04T18:18:31Z

- **keps/5993-kueueviz-authentication/README.md:35** by [@mimowo](https://github.com/mimowo) — 2026-02-04T18:18:32Z
```diff
@@ -0,0 +1,272 @@
+# KEP-5993: KueueViz Authentication
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Authentication Flow](#authentication-flow)
+  - [Backend Implementation](#backend-implementation)
+  - [WebSocket Authentication](#websocket-authentication)
+  - [RBAC Updates](#rbac-updates)
+  - [Helm Configuration](#helm-configuration)
+  - [Frontend Considerations](#frontend-considerations)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [OAuth2/OIDC Proxy](#oauth2oidc-proxy)
+  - [mTLS Client Certificates](#mtls-client-certificates)
+  - [Basic Authentication](#basic-authentication)
+<!-- /toc -->
+
+## Summary
+
+KueueViz currently exposes all Kueue resoruces without authentication.
+This KEP adds optional bearer token authentication using the Kubernetes
+TokenReview API, the same mechanism Kubernetes Dashboard uses.
+
+Authentication is disabled by defualt for backward compatibility.
```

  I'm not sure this is desired as the end result. I would love if KueueViz can be secure by default. 
  
  Could we introduce the TokenReview mechanism so that it is transparent for the users? For example, the frontend can generate the token and would use it without user intervention.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-04T18:20:24Z

cc @kannon92 @akram

### Review by [@samzong](https://github.com/samzong) (commented) — 2026-02-05T02:03:26Z

- **keps/5993-kueueviz-authentication/kep.yaml:28** by [@samzong](https://github.com/samzong) — 2026-02-05T02:03:26Z
```diff
@@ -0,0 +1,28 @@
+title: KueueViz Authentication
+kep-number: 5993
+authors:
+  - "@samzong"
+status: provisional
+creation-date: 2026-02-04
+reviewers:
+  - TBD
+  - "@mimowo"
+approvers:
+  - TBD
+
+see-also:
+  - "https://github.com/kubernetes-sigs/kueue/issues/5993"
+  - "https://github.com/kubernetes-sigs/kueue/issues/8969"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: beta
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.18"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  beta: "v0.18"
```

  OK, I'll try finish this at current milestone.
  I have check v0.17 release date at 16th March.  #8829
  
  > next week I plan to traving until end of this month.

### Review by [@samzong](https://github.com/samzong) (commented) — 2026-02-05T02:21:04Z

- **keps/5993-kueueviz-authentication/README.md:35** by [@samzong](https://github.com/samzong) — 2026-02-05T02:21:04Z
```diff
@@ -0,0 +1,272 @@
+# KEP-5993: KueueViz Authentication
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Authentication Flow](#authentication-flow)
+  - [Backend Implementation](#backend-implementation)
+  - [WebSocket Authentication](#websocket-authentication)
+  - [RBAC Updates](#rbac-updates)
+  - [Helm Configuration](#helm-configuration)
+  - [Frontend Considerations](#frontend-considerations)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [OAuth2/OIDC Proxy](#oauth2oidc-proxy)
+  - [mTLS Client Certificates](#mtls-client-certificates)
+  - [Basic Authentication](#basic-authentication)
+<!-- /toc -->
+
+## Summary
+
+KueueViz currently exposes all Kueue resoruces without authentication.
+This KEP adds optional bearer token authentication using the Kubernetes
+TokenReview API, the same mechanism Kubernetes Dashboard uses.
+
+Authentication is disabled by defualt for backward compatibility.
```

  great idea! if enable by default, it's will be more secure. 
  
  but it means `break change` of exist users. so how create the first token is important.
  
  I will look at similar projects are handled.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-05T08:27:13Z

- **keps/5993-kueueviz-authentication/kep.yaml:28** by [@mimowo](https://github.com/mimowo) — 2026-02-05T08:27:13Z
```diff
@@ -0,0 +1,28 @@
+title: KueueViz Authentication
+kep-number: 5993
+authors:
+  - "@samzong"
+status: provisional
+creation-date: 2026-02-04
+reviewers:
+  - TBD
+  - "@mimowo"
+approvers:
+  - TBD
+
+see-also:
+  - "https://github.com/kubernetes-sigs/kueue/issues/5993"
+  - "https://github.com/kubernetes-sigs/kueue/issues/8969"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: beta
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.18"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  beta: "v0.18"
```

  I'm ok with 0.18 too if this is not urgent for you, just wanted to ask to clarify your expectations here.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-05T08:30:04Z

- **keps/5993-kueueviz-authentication/README.md:35** by [@mimowo](https://github.com/mimowo) — 2026-02-05T08:30:04Z
```diff
@@ -0,0 +1,272 @@
+# KEP-5993: KueueViz Authentication
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Authentication Flow](#authentication-flow)
+  - [Backend Implementation](#backend-implementation)
+  - [WebSocket Authentication](#websocket-authentication)
+  - [RBAC Updates](#rbac-updates)
+  - [Helm Configuration](#helm-configuration)
+  - [Frontend Considerations](#frontend-considerations)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [OAuth2/OIDC Proxy](#oauth2oidc-proxy)
+  - [mTLS Client Certificates](#mtls-client-certificates)
+  - [Basic Authentication](#basic-authentication)
+<!-- /toc -->
+
+## Summary
+
+KueueViz currently exposes all Kueue resoruces without authentication.
+This KEP adds optional bearer token authentication using the Kubernetes
+TokenReview API, the same mechanism Kubernetes Dashboard uses.
+
+Authentication is disabled by defualt for backward compatibility.
```

  Thank you, this is exactly what I'm looking for. I would love KueueViz to be secure by default, but have some doubts: how much effort it will be on users to "get going", can the token creation be fully automated? If this is not too bad, then I think we could swallow the pill and describe the migration procedure in release notes for upgrade.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-05T09:53:45Z

- **keps/5993-kueueviz-authentication/kep.yaml:28** by [@mimowo](https://github.com/mimowo) — 2026-02-05T09:53:45Z
```diff
@@ -0,0 +1,28 @@
+title: KueueViz Authentication
+kep-number: 5993
+authors:
+  - "@samzong"
+status: provisional
+creation-date: 2026-02-04
+reviewers:
+  - TBD
+  - "@mimowo"
+approvers:
+  - TBD
+
+see-also:
+  - "https://github.com/kubernetes-sigs/kueue/issues/5993"
+  - "https://github.com/kubernetes-sigs/kueue/issues/8969"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: beta
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.18"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  beta: "v0.18"
```

  Basically, we can try to aim for 0.17, but probably 0.18 is safer indeed, as we already have a big inflow of work.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2026-02-05T20:22:00Z

- **keps/5993-kueueviz-authentication/README.md:193** by [@mwielgus](https://github.com/mwielgus) — 2026-02-05T20:22:00Z
```diff
@@ -0,0 +1,272 @@
+# KEP-5993: KueueViz Authentication
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Authentication Flow](#authentication-flow)
+  - [Backend Implementation](#backend-implementation)
+  - [WebSocket Authentication](#websocket-authentication)
+  - [RBAC Updates](#rbac-updates)
+  - [Helm Configuration](#helm-configuration)
+  - [Frontend Considerations](#frontend-considerations)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [OAuth2/OIDC Proxy](#oauth2oidc-proxy)
+  - [mTLS Client Certificates](#mtls-client-certificates)
+  - [Basic Authentication](#basic-authentication)
+<!-- /toc -->
+
+## Summary
+
+KueueViz currently exposes all Kueue resoruces without authentication.
+This KEP adds optional bearer token authentication using the Kubernetes
+TokenReview API, the same mechanism Kubernetes Dashboard uses.
+
+Authentication is disabled by defualt for backward compatibility.
+
+## Motivation
+
+From [Issue #5993](https://github.com/kubernetes-sigs/kueue/issues/5993):
+
+> "The current KueueViz has no authentication, which makes it risky to expose externally.
+> We would need auth to be added before we can safely use it."
+> — david-gang, Issue #8969
+
+Without authentication, KueueViz is limited to `kubectl port-foward`.
+There is no way to safely expose it via Ingress, and no audit trail.
+
+### Goals
+
+1. Add optional authentication using Kubernetes TokenReview API
+2. Support bearer token via `Authorization: Bearer <token>` header
+3. Authenticate WebSocket connections at upgrade time
+4. Keep authentication disabled by default
+5. Document how to obtain and use toekns
+6. Add metrics for authentication attempts
+
+### Non-Goals
+
+1. Authorization (SubjectAccessReview), may be added in a future KEP
+2. Server-side sessions
+3. OAuth2/OIDC integration, users can deploy oauth2-proxy separately
+4. Frontend login UI, out of scope for intial release
+5. Token refresh, client resposibility
+
+## Proposal
+
+Add authentication middleware to KueueViz backend:
+
+1. Extract bearer token from request
+2. Validate via Kubernetes TokenReview API
+3. Return HTTP 401 for invalid or missing tokens
+4. Store user info in request context
+
+### User Stories
+
+**Cluster Admin**: Enable authentication via Helm so only users with valid
+Kubernetes tokens can access KueueViz.
+
+**Multi-Tenant Operator**: Expose KueueViz via Ingress to multiple teams,
+with per-user authentication and audit logging.
+
+**Developer**: Access KueueViz using existing Kubernetes credentials
+(ServiceAccount token or kubeconfig token).
+
+### Risks and Mitigations
+
+| Risk | Mitigation |
+|------|------------|
+| Breaking existing deployments | Disabled by default |
+| Token exposure in logs | Never log tokens |
+| TokenReview latency | Cache with configurable TTL |
+| WebSocket complexity | Authenticate at connection time only |
+| Expired tokens on long connections | Document token lifetime |
+
+## Design Details
+
+### API Changes
+
+None. Changes limited to environment variables and Helm values.
+
+### Authentication Flow
+
+```text
+Client                    KueueViz Backend         K8s API Server
+  │                             │                        │
+  │ GET /ws/workloads           │                        │
+  │ Authorization: Bearer <t>   │                        │
+  │────────────────────────────>│                        │
+  │                             │ POST tokenreviews      │
+  │                             │───────────────────────>│
+  │                             │ authenticated: true    │
+  │                             │<───────────────────────│
+  │ 200 OK / WS Upgrade         │                        │
+  │<────────────────────────────│                        │
+```
+
+### Backend Implementation
+
+**New file:** `cmd/kueueviz/backend/middleware/auth.go`
+
+Gin middleware that extracts bearer token from `Authorization` header or
+`?token=` query param, validates via TokenReview API, caches successful
+authentications, and returns HTTP 401 with `WWW-Authenticate: Bearer` on
+failure.
+
+**Modified:** `cmd/kueueviz/backend/config/server.go`
+
+Add fields to `ServerConfig`:
+
+- `AuthEnabled bool`
+- `AuthAudiences []string`
+- `AuthCacheTTL time.Duration`
+
+Load from environment variables:
+
+- `KUEUEVIZ_AUTH_ENABLED`
+- `KUEUEVIZ_AUTH_AUDIENCES`
+- `KUEUEVIZ_AUTH_CACHE_TTL`
+
+**Modified:** `cmd/kueueviz/backend/main.go`
+
+- Create `kubernetes.Clientset` for TokenReview calls
+- Register auth middleware when `AuthEnabled` is true
+
+### WebSocket Authentication
+
+Tokens can be provided via:
+
+1. `Authorization: Bearer <token>` header (preferred)
+2. `?token=<token>` query parameter (browser fallback)
+
+Authentication happens at the HTTP upgrade request. No per-message auth.
+
+Future versions may use `Sec-WebSocket-Protocol` header instead of query
+parameter to avoid URL logging exposure.
+
+### RBAC Updates
+
+Add to `charts/kueue/templates/kueueviz/clusterrole.yaml`:
+
+```yaml
+- apiGroups: ["authentication.k8s.io"]
+  resources: ["tokenreviews"]
+  verbs: ["create"]
```

  Why just create?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2026-02-05T20:24:25Z

- **keps/5993-kueueviz-authentication/README.md:105** by [@mwielgus](https://github.com/mwielgus) — 2026-02-05T20:24:25Z
```diff
@@ -0,0 +1,272 @@
+# KEP-5993: KueueViz Authentication
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Authentication Flow](#authentication-flow)
+  - [Backend Implementation](#backend-implementation)
+  - [WebSocket Authentication](#websocket-authentication)
+  - [RBAC Updates](#rbac-updates)
+  - [Helm Configuration](#helm-configuration)
+  - [Frontend Considerations](#frontend-considerations)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [OAuth2/OIDC Proxy](#oauth2oidc-proxy)
+  - [mTLS Client Certificates](#mtls-client-certificates)
+  - [Basic Authentication](#basic-authentication)
+<!-- /toc -->
+
+## Summary
+
+KueueViz currently exposes all Kueue resoruces without authentication.
+This KEP adds optional bearer token authentication using the Kubernetes
+TokenReview API, the same mechanism Kubernetes Dashboard uses.
+
+Authentication is disabled by defualt for backward compatibility.
+
+## Motivation
+
+From [Issue #5993](https://github.com/kubernetes-sigs/kueue/issues/5993):
+
+> "The current KueueViz has no authentication, which makes it risky to expose externally.
+> We would need auth to be added before we can safely use it."
+> — david-gang, Issue #8969
+
+Without authentication, KueueViz is limited to `kubectl port-foward`.
+There is no way to safely expose it via Ingress, and no audit trail.
+
+### Goals
+
+1. Add optional authentication using Kubernetes TokenReview API
+2. Support bearer token via `Authorization: Bearer <token>` header
+3. Authenticate WebSocket connections at upgrade time
+4. Keep authentication disabled by default
+5. Document how to obtain and use toekns
+6. Add metrics for authentication attempts
+
+### Non-Goals
+
+1. Authorization (SubjectAccessReview), may be added in a future KEP
+2. Server-side sessions
+3. OAuth2/OIDC integration, users can deploy oauth2-proxy separately
+4. Frontend login UI, out of scope for intial release
+5. Token refresh, client resposibility
+
+## Proposal
+
+Add authentication middleware to KueueViz backend:
+
+1. Extract bearer token from request
+2. Validate via Kubernetes TokenReview API
+3. Return HTTP 401 for invalid or missing tokens
+4. Store user info in request context
+
+### User Stories
+
+**Cluster Admin**: Enable authentication via Helm so only users with valid
+Kubernetes tokens can access KueueViz.
+
+**Multi-Tenant Operator**: Expose KueueViz via Ingress to multiple teams,
+with per-user authentication and audit logging.
+
+**Developer**: Access KueueViz using existing Kubernetes credentials
+(ServiceAccount token or kubeconfig token).
+
+### Risks and Mitigations
+
+| Risk | Mitigation |
+|------|------------|
+| Breaking existing deployments | Disabled by default |
+| Token exposure in logs | Never log tokens |
+| TokenReview latency | Cache with configurable TTL |
+| WebSocket complexity | Authenticate at connection time only |
+| Expired tokens on long connections | Document token lifetime |
+
+## Design Details
+
+### API Changes
+
+None. Changes limited to environment variables and Helm values.
+
+### Authentication Flow
+
+```text
```

  How to prevent DDOS attacks on K8S API server?

### Review by [@samzong](https://github.com/samzong) (commented) — 2026-02-06T00:55:50Z

- **keps/5993-kueueviz-authentication/README.md:193** by [@samzong](https://github.com/samzong) — 2026-02-06T00:55:50Z
```diff
@@ -0,0 +1,272 @@
+# KEP-5993: KueueViz Authentication
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Authentication Flow](#authentication-flow)
+  - [Backend Implementation](#backend-implementation)
+  - [WebSocket Authentication](#websocket-authentication)
+  - [RBAC Updates](#rbac-updates)
+  - [Helm Configuration](#helm-configuration)
+  - [Frontend Considerations](#frontend-considerations)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [OAuth2/OIDC Proxy](#oauth2oidc-proxy)
+  - [mTLS Client Certificates](#mtls-client-certificates)
+  - [Basic Authentication](#basic-authentication)
+<!-- /toc -->
+
+## Summary
+
+KueueViz currently exposes all Kueue resoruces without authentication.
+This KEP adds optional bearer token authentication using the Kubernetes
+TokenReview API, the same mechanism Kubernetes Dashboard uses.
+
+Authentication is disabled by defualt for backward compatibility.
+
+## Motivation
+
+From [Issue #5993](https://github.com/kubernetes-sigs/kueue/issues/5993):
+
+> "The current KueueViz has no authentication, which makes it risky to expose externally.
+> We would need auth to be added before we can safely use it."
+> — david-gang, Issue #8969
+
+Without authentication, KueueViz is limited to `kubectl port-foward`.
+There is no way to safely expose it via Ingress, and no audit trail.
+
+### Goals
+
+1. Add optional authentication using Kubernetes TokenReview API
+2. Support bearer token via `Authorization: Bearer <token>` header
+3. Authenticate WebSocket connections at upgrade time
+4. Keep authentication disabled by default
+5. Document how to obtain and use toekns
+6. Add metrics for authentication attempts
+
+### Non-Goals
+
+1. Authorization (SubjectAccessReview), may be added in a future KEP
+2. Server-side sessions
+3. OAuth2/OIDC integration, users can deploy oauth2-proxy separately
+4. Frontend login UI, out of scope for intial release
+5. Token refresh, client resposibility
+
+## Proposal
+
+Add authentication middleware to KueueViz backend:
+
+1. Extract bearer token from request
+2. Validate via Kubernetes TokenReview API
+3. Return HTTP 401 for invalid or missing tokens
+4. Store user info in request context
+
+### User Stories
+
+**Cluster Admin**: Enable authentication via Helm so only users with valid
+Kubernetes tokens can access KueueViz.
+
+**Multi-Tenant Operator**: Expose KueueViz via Ingress to multiple teams,
+with per-user authentication and audit logging.
+
+**Developer**: Access KueueViz using existing Kubernetes credentials
+(ServiceAccount token or kubeconfig token).
+
+### Risks and Mitigations
+
+| Risk | Mitigation |
+|------|------------|
+| Breaking existing deployments | Disabled by default |
+| Token exposure in logs | Never log tokens |
+| TokenReview latency | Cache with configurable TTL |
+| WebSocket complexity | Authenticate at connection time only |
+| Expired tokens on long connections | Document token lifetime |
+
+## Design Details
+
+### API Changes
+
+None. Changes limited to environment variables and Helm values.
+
+### Authentication Flow
+
+```text
+Client                    KueueViz Backend         K8s API Server
+  │                             │                        │
+  │ GET /ws/workloads           │                        │
+  │ Authorization: Bearer <t>   │                        │
+  │────────────────────────────>│                        │
+  │                             │ POST tokenreviews      │
+  │                             │───────────────────────>│
+  │                             │ authenticated: true    │
+  │                             │<───────────────────────│
+  │ 200 OK / WS Upgrade         │                        │
+  │<────────────────────────────│                        │
+```
+
+### Backend Implementation
+
+**New file:** `cmd/kueueviz/backend/middleware/auth.go`
+
+Gin middleware that extracts bearer token from `Authorization` header or
+`?token=` query param, validates via TokenReview API, caches successful
+authentications, and returns HTTP 401 with `WWW-Authenticate: Bearer` on
+failure.
+
+**Modified:** `cmd/kueueviz/backend/config/server.go`
+
+Add fields to `ServerConfig`:
+
+- `AuthEnabled bool`
+- `AuthAudiences []string`
+- `AuthCacheTTL time.Duration`
+
+Load from environment variables:
+
+- `KUEUEVIZ_AUTH_ENABLED`
+- `KUEUEVIZ_AUTH_AUDIENCES`
+- `KUEUEVIZ_AUTH_CACHE_TTL`
+
+**Modified:** `cmd/kueueviz/backend/main.go`
+
+- Create `kubernetes.Clientset` for TokenReview calls
+- Register auth middleware when `AuthEnabled` is true
+
+### WebSocket Authentication
+
+Tokens can be provided via:
+
+1. `Authorization: Bearer <token>` header (preferred)
+2. `?token=<token>` query parameter (browser fallback)
+
+Authentication happens at the HTTP upgrade request. No per-message auth.
+
+Future versions may use `Sec-WebSocket-Protocol` header instead of query
+parameter to avoid URL logging exposure.
+
+### RBAC Updates
+
+Add to `charts/kueue/templates/kueueviz/clusterrole.yaml`:
+
+```yaml
+- apiGroups: ["authentication.k8s.io"]
+  resources: ["tokenreviews"]
+  verbs: ["create"]
```

  TokenReview is a create-only api in k8s, no get/list/watch/delete.
  
  just repsonse auth result when post to api server.

### Review by [@samzong](https://github.com/samzong) (commented) — 2026-02-06T01:09:10Z

- **keps/5993-kueueviz-authentication/README.md:105** by [@samzong](https://github.com/samzong) — 2026-02-06T01:09:10Z
```diff
@@ -0,0 +1,272 @@
+# KEP-5993: KueueViz Authentication
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Authentication Flow](#authentication-flow)
+  - [Backend Implementation](#backend-implementation)
+  - [WebSocket Authentication](#websocket-authentication)
+  - [RBAC Updates](#rbac-updates)
+  - [Helm Configuration](#helm-configuration)
+  - [Frontend Considerations](#frontend-considerations)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [OAuth2/OIDC Proxy](#oauth2oidc-proxy)
+  - [mTLS Client Certificates](#mtls-client-certificates)
+  - [Basic Authentication](#basic-authentication)
+<!-- /toc -->
+
+## Summary
+
+KueueViz currently exposes all Kueue resoruces without authentication.
+This KEP adds optional bearer token authentication using the Kubernetes
+TokenReview API, the same mechanism Kubernetes Dashboard uses.
+
+Authentication is disabled by defualt for backward compatibility.
+
+## Motivation
+
+From [Issue #5993](https://github.com/kubernetes-sigs/kueue/issues/5993):
+
+> "The current KueueViz has no authentication, which makes it risky to expose externally.
+> We would need auth to be added before we can safely use it."
+> — david-gang, Issue #8969
+
+Without authentication, KueueViz is limited to `kubectl port-foward`.
+There is no way to safely expose it via Ingress, and no audit trail.
+
+### Goals
+
+1. Add optional authentication using Kubernetes TokenReview API
+2. Support bearer token via `Authorization: Bearer <token>` header
+3. Authenticate WebSocket connections at upgrade time
+4. Keep authentication disabled by default
+5. Document how to obtain and use toekns
+6. Add metrics for authentication attempts
+
+### Non-Goals
+
+1. Authorization (SubjectAccessReview), may be added in a future KEP
+2. Server-side sessions
+3. OAuth2/OIDC integration, users can deploy oauth2-proxy separately
+4. Frontend login UI, out of scope for intial release
+5. Token refresh, client resposibility
+
+## Proposal
+
+Add authentication middleware to KueueViz backend:
+
+1. Extract bearer token from request
+2. Validate via Kubernetes TokenReview API
+3. Return HTTP 401 for invalid or missing tokens
+4. Store user info in request context
+
+### User Stories
+
+**Cluster Admin**: Enable authentication via Helm so only users with valid
+Kubernetes tokens can access KueueViz.
+
+**Multi-Tenant Operator**: Expose KueueViz via Ingress to multiple teams,
+with per-user authentication and audit logging.
+
+**Developer**: Access KueueViz using existing Kubernetes credentials
+(ServiceAccount token or kubeconfig token).
+
+### Risks and Mitigations
+
+| Risk | Mitigation |
+|------|------------|
+| Breaking existing deployments | Disabled by default |
+| Token exposure in logs | Never log tokens |
+| TokenReview latency | Cache with configurable TTL |
+| WebSocket complexity | Authenticate at connection time only |
+| Expired tokens on long connections | Document token lifetime |
+
+## Design Details
+
+### API Changes
+
+None. Changes limited to environment variables and Helm values.
+
+### Authentication Flow
+
+```text
```

  Thanks for review, it's good question.
  
  I think k8s api server already has built-in protection this(API Priority and Fairness, `--max-requests-inflight`). and DDOS at the network level(ingress can do it), not do again at application-layer.
  
  In KEP, it's will cache successful auth (configurable TTL), which reduces API server load for normal usage.
  
  maybe we can also **cache failed authentication results**, to prevent the same invalid token from triggering repeated TokenReview calls.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-11T18:17:38Z

- **keps/5993-kueueviz-authentication/README.md:257** by [@mimowo](https://github.com/mimowo) — 2026-02-11T18:17:38Z
```diff
@@ -0,0 +1,301 @@
+# KEP-5993: KueueViz Authentication
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Authentication Flow](#authentication-flow)
+  - [Backend Implementation](#backend-implementation)
+  - [WebSocket Authentication](#websocket-authentication)
+  - [Login Page](#login-page)
+  - [RBAC Updates](#rbac-updates)
+  - [Helm Configuration](#helm-configuration)
+  - [Frontend Considerations](#frontend-considerations)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [OAuth2/OIDC Proxy](#oauth2oidc-proxy)
+  - [mTLS Client Certificates](#mtls-client-certificates)
+  - [Basic Authentication](#basic-authentication)
+<!-- /toc -->
+
+## Summary
+
+KueueViz currently exposes all Kueue resoruces without authentication.
+This KEP adds optional bearer token authentication using the Kubernetes
+TokenReview API, the same mechanism Kubernetes Dashboard uses.
+
+Authentication is enabled by default, and can be disabled with
+`auth.enabled: false`.
+
+## Motivation
+
+From [Issue #5993](https://github.com/kubernetes-sigs/kueue/issues/5993):
+
+> "The current KueueViz has no authentication, which makes it risky to expose externally.
+> We would need auth to be added before we can safely use it."
+> — david-gang, Issue #8969
+
+Without authentication, KueueViz is limited to `kubectl port-foward`.
+There is no way to safely expose it via Ingress, and no audit trail.
+
+### Goals
+
+1. Add optional authentication using Kubernetes TokenReview API
+2. Support bearer token via `Authorization: Bearer <token>` header
+3. Authenticate WebSocket connections at upgrade time
+4. Enable authentication by default
+5. Provide a login page with token input and validation flow
+6. Add metrics for authentication attempts
+
+### Non-Goals
+
+1. Authorization (SubjectAccessReview), may be added in a future KEP
+2. Server-side sessions
+3. OAuth2/OIDC integration, users can deploy oauth2-proxy separately
+4. Token refresh, client resposibility
+
+## Proposal
+
+Add authentication middleware to KueueViz backend:
+
+1. Extract bearer token from request
+2. Validate via Kubernetes TokenReview API
+3. Return HTTP 401 for invalid or missing tokens
+4. Store user info in request context
+
+### User Stories
+
+**Cluster Admin**: Enable authentication via Helm so only users with valid
+Kubernetes tokens can access KueueViz.
+
+**Multi-Tenant Operator**: Expose KueueViz via Ingress to multiple teams,
+with per-user authentication and audit logging.
+
+**Developer**: Access KueueViz using existing Kubernetes credentials
+(ServiceAccount token or kubeconfig token).
+
+### Risks and Mitigations
+
+| Risk | Mitigation |
+|------|------------|
+| Breaking existing deployments | Users can set `auth.enabled: false` |
+| Token exposure in logs | Never log tokens |
+| TokenReview latency | Cache with configurable TTL |
+| API server load from invalid tokens | Negative caching (5s TTL) for failed auth results |
+| WebSocket complexity | Authenticate at connection time only |
+| Expired tokens on long connections | Document token lifetime |
+
+## Design Details
+
+### API Changes
+
+None. Changes limited to environment variables and Helm values.
+
+### Authentication Flow
+
+```text
+Client                    KueueViz Backend         K8s API Server
+  │                             │                        │
+  │ GET /ws/workloads           │                        │
+  │ Authorization: Bearer <t>   │                        │
+  │────────────────────────────>│                        │
+  │                             │ POST tokenreviews      │
+  │                             │───────────────────────>│
+  │                             │ authenticated: true    │
+  │                             │<───────────────────────│
+  │ 200 OK / WS Upgrade         │                        │
+  │<────────────────────────────│                        │
+```
+
+### Backend Implementation
+
+**New file:** `cmd/kueueviz/backend/middleware/auth.go`
+
+Gin middleware that extracts bearer token from `Authorization` header or
+`?token=` query param, validates via TokenReview API, caches successful
+authentications, and returns HTTP 401 with `WWW-Authenticate: Bearer` on
+failure.
+
+**Modified:** `cmd/kueueviz/backend/config/server.go`
+
+Add fields to `ServerConfig`:
+
+- `AuthEnabled bool`
+- `AuthAudiences []string`
+- `AuthCacheTTL time.Duration`
+- `AuthNegativeCacheTTL time.Duration`
+
+Load from environment variables:
+
+- `KUEUEVIZ_AUTH_ENABLED`
+- `KUEUEVIZ_AUTH_AUDIENCES`
+- `KUEUEVIZ_AUTH_CACHE_TTL`
+- `KUEUEVIZ_AUTH_NEGATIVE_CACHE_TTL`
+
+**Modified:** `cmd/kueueviz/backend/main.go`
+
+- Create `kubernetes.Clientset` for TokenReview calls
+- Register auth middleware when `AuthEnabled` is true
+
+### WebSocket Authentication
+
+Tokens can be provided via:
+
+1. `Authorization: Bearer <token>` header (preferred)
+2. `?token=<token>` query parameter (browser fallback)
+
+Authentication happens at the HTTP upgrade request. No per-message auth.
+
+Future versions may use `Sec-WebSocket-Protocol` header instead of query
+parameter to avoid URL logging exposure.
+
+### Login Page
+
+The frontend provides a login page at `/login` with:
+
+1. A token input field
+2. A "Sign In" action that stores the token for subsequent requests
+3. Instructions for obtaining a token:
+
+```text
+kubectl create token <service-account> -n <namespace>
+```
+
+Authentication flow on the frontend:
+
+1. Access protected API endpoints with bearer token when available.
+2. On HTTP 401, clear token and redirect to `/login`.
+3. User provides a token and signs in.
+4. Retry protected requests with bearer token for HTTP APIs and WebSockets.
+5. On later HTTP 401, clear token and redirect back to `/login`.
+
+### RBAC Updates
+
+Add to `charts/kueue/templates/kueueviz/clusterrole.yaml`:
+
+```yaml
+- apiGroups: ["authentication.k8s.io"]
+  resources: ["tokenreviews"]
+  verbs: ["create"] # TokenReview is a create-only API
+```
+
+### Helm Configuration
+
+`charts/kueue/values.yaml`:
+
+```yaml
+kueueViz:
+  backend:
+    auth:
+      enabled: true           # enabled by default
+      audiences: ""           # optional, comma-separated
+      cacheTTL: "60s"
+      negativeCacheTTL: "5s"  # cache failed auth results
+```
+
+`charts/kueue/templates/kueueviz/backend-deployment.yaml`:
+
+```yaml
+{{- if .Values.kueueViz.backend.auth.enabled }}
+- name: KUEUEVIZ_AUTH_ENABLED
+  value: "true"
+{{- if .Values.kueueViz.backend.auth.audiences }}
+- name: KUEUEVIZ_AUTH_AUDIENCES
+  value: {{ .Values.kueueViz.backend.auth.audiences | quote }}
+{{- end }}
+- name: KUEUEVIZ_AUTH_CACHE_TTL
+  value: {{ .Values.kueueViz.backend.auth.cacheTTL | default "60s" | quote }}
+{{- end }}
+```
+
+### Frontend Considerations
+
+Initial release: out of scope. Users can use `kubectl proxy` or add headers
+manually.
+
+Future: may add login dialog with token input and localStorage persistence.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+Unit tests (`middleware/auth_test.go`):
+
+- Token extraction from header and query param
+- Cache hit/miss
+- Expired cache entries
+- Disabled auth passthrough
+
+Coverage target: >80%
+
+Integration tests:
+
+- Valid token → 200
+- Invalid token → 401
+- Missing token → 401
+- Disabled auth → pass all
+
+E2E tests (`test/e2e/kueueviz/`):
+
+- Deploy with auth enabled
+- Test with ServiceAccount token
+- Test rejection of unauthenticated requests
+
+### Graduation Criteria
+
+Beta (v0.17):
```

  I would propose to add Alpha v0.17 with the feature disabled by default, and in graduation criteria for Beta let's say  "re-evaluate enabling by default based on user feedback".

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-11T18:17:53Z

- **keps/5993-kueueviz-authentication/kep.yaml:11** by [@mimowo](https://github.com/mimowo) — 2026-02-11T18:17:53Z
```diff
@@ -0,0 +1,28 @@
+title: KueueViz Authentication
+kep-number: 5993
+authors:
+  - "@samzong"
+status: provisional
+creation-date: 2026-02-04
+reviewers:
+  - TBD
+  - "@mimowo"
+approvers:
+  - TBD
```

  add me and @tenzen-y

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-11T18:18:21Z

- **keps/5993-kueueviz-authentication/kep.yaml:18** by [@mimowo](https://github.com/mimowo) — 2026-02-11T18:18:21Z
```diff
@@ -0,0 +1,28 @@
+title: KueueViz Authentication
+kep-number: 5993
+authors:
+  - "@samzong"
+status: provisional
+creation-date: 2026-02-04
+reviewers:
+  - TBD
+  - "@mimowo"
+approvers:
+  - TBD
+
+see-also:
+  - "https://github.com/kubernetes-sigs/kueue/issues/5993"
+  - "https://github.com/kubernetes-sigs/kueue/issues/8969"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: beta
```

  Can we first alpha with the authentication disabled, and then we re-evaluate for beta, wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-11T18:19:14Z

Basically LGTM, just left comments about the graduation steps. I think starting in Alpha disabled by default would be safer. Once we hear positive feedback and adoption we can consider enabled by default.

cc @tenzen-y

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T08:40:19Z

- **keps/5993-kueueviz-authentication/README.md:141** by [@mimowo](https://github.com/mimowo) — 2026-03-05T08:40:19Z
```diff
@@ -0,0 +1,328 @@
+# KEP-5993: KueueViz Authentication
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Authentication Flow](#authentication-flow)
+  - [Backend Implementation](#backend-implementation)
+  - [WebSocket Authentication](#websocket-authentication)
+  - [Login Page](#login-page)
+  - [RBAC Updates](#rbac-updates)
+  - [Helm Configuration](#helm-configuration)
+  - [Frontend Considerations](#frontend-considerations)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [OAuth2/OIDC Proxy](#oauth2oidc-proxy)
+  - [mTLS Client Certificates](#mtls-client-certificates)
+  - [Basic Authentication](#basic-authentication)
+<!-- /toc -->
+
+## Summary
+
+KueueViz currently exposes all Kueue resources without authentication.
+This KEP adds optional bearer token authentication using the Kubernetes
+TokenReview API, the same mechanism Kubernetes Dashboard uses.
+
+Authentication is controlled by a feature gate (`KueueVizAuth`), disabled
+by default in Alpha. Users opt in via Helm values (`auth.enabled: true`).
+
+## Motivation
+
+From [Issue #5993](https://github.com/kubernetes-sigs/kueue/issues/5993):
+
+> "The current KueueViz has no authentication, which makes it risky to expose externally.
+> We would need auth to be added before we can safely use it."
+> — david-gang, Issue #8969
+
+Without authentication, KueueViz is limited to `kubectl port-foward`.
+There is no way to safely expose it via Ingress, and no audit trail.
+
+### Goals
+
+1. Add optional authentication using Kubernetes TokenReview API
+2. Support bearer token via `Authorization: Bearer <token>` header
+3. Authenticate WebSocket connections at upgrade time
+4. Provide a login page with token input and validation flow
+5. Add metrics for authentication attempts
+6. Minimize manual steps for users when authenticating
+
+### Non-Goals
+
+1. Authorization (SubjectAccessReview), may be added in a future KEP
+2. Server-side sessions
+3. OAuth2/OIDC integration, users can deploy oauth2-proxy separately
+4. Token refresh, client resposibility
+
+## Proposal
+
+Add authentication middleware to KueueViz backend:
+
+1. Extract bearer token from request
+2. Validate via Kubernetes TokenReview API
+3. Return HTTP 401 for invalid or missing tokens
+4. Store user info in request context
+
+### User Stories
+
+**Cluster Admin**: Enable authentication via Helm so only users with valid
+Kubernetes tokens can access KueueViz.
+
+**Multi-Tenant Operator**: Expose KueueViz via Ingress to multiple teams,
+with per-user authentication and audit logging.
+
+**Developer**: Access KueueViz using existing Kubernetes credentials
+(ServiceAccount token or kubeconfig token).
+
+### Risks and Mitigations
+
+| Risk | Mitigation |
+|------|------------|
+| Breaking existing deployments | Disabled by default in Alpha; opt-in only |
+| Token exposure in logs | Never log tokens |
+| TokenReview latency | Cache with configurable TTL |
+| API server load from invalid tokens | Negative caching (5s TTL) for failed auth results |
+| WebSocket complexity | Authenticate at connection time only |
+| Expired tokens on long connections | Document token lifetime |
+
+## Design Details
+
+### API Changes
+
+None. Changes limited to environment variables and Helm values.
+
+### Authentication Flow
+
+```text
+Client                    KueueViz Backend         K8s API Server
+  │                             │                        │
+  │ GET /ws/workloads           │                        │
+  │ Authorization: Bearer <t>   │                        │
+  │────────────────────────────>│                        │
+  │                             │ POST tokenreviews      │
+  │                             │───────────────────────>│
+  │                             │ authenticated: true    │
+  │                             │<───────────────────────│
+  │ 200 OK / WS Upgrade         │                        │
+  │<────────────────────────────│                        │
+```
+
+### Backend Implementation
+
+**New file:** `cmd/kueueviz/backend/middleware/auth.go`
+
+Gin middleware that extracts bearer token from `Authorization` header or
+`?token=` query param, validates via TokenReview API, and returns HTTP 401
+with `WWW-Authenticate: Bearer` on failure.
+
+Caching protects the API server from excessive TokenReview calls:
+- Successful auth results are cached with configurable TTL (default 60s).
+- Failed auth results are cached with a short TTL (default 5s) so the same
+  invalid token does not trigger repeated calls.
+- The API server's own API Priority and Fairness (`--max-requests-inflight`)
+  provides additional protection; application-layer caching is a second line.
+
+**Modified:** `cmd/kueueviz/backend/config/server.go`
+
+Add fields to `ServerConfig`:
+
+- `AuthEnabled bool`
+- `AuthAudiences []string`
+- `AuthCacheTTL time.Duration`
+- `AuthNegativeCacheTTL time.Duration`
```

  Please add comments above them in the KEP to describe their semantics

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T08:43:25Z

- **keps/5993-kueueviz-authentication/README.md:210** by [@mimowo](https://github.com/mimowo) — 2026-03-05T08:43:25Z
```diff
@@ -0,0 +1,328 @@
+# KEP-5993: KueueViz Authentication
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Authentication Flow](#authentication-flow)
+  - [Backend Implementation](#backend-implementation)
+  - [WebSocket Authentication](#websocket-authentication)
+  - [Login Page](#login-page)
+  - [RBAC Updates](#rbac-updates)
+  - [Helm Configuration](#helm-configuration)
+  - [Frontend Considerations](#frontend-considerations)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [OAuth2/OIDC Proxy](#oauth2oidc-proxy)
+  - [mTLS Client Certificates](#mtls-client-certificates)
+  - [Basic Authentication](#basic-authentication)
+<!-- /toc -->
+
+## Summary
+
+KueueViz currently exposes all Kueue resources without authentication.
+This KEP adds optional bearer token authentication using the Kubernetes
+TokenReview API, the same mechanism Kubernetes Dashboard uses.
+
+Authentication is controlled by a feature gate (`KueueVizAuth`), disabled
+by default in Alpha. Users opt in via Helm values (`auth.enabled: true`).
+
+## Motivation
+
+From [Issue #5993](https://github.com/kubernetes-sigs/kueue/issues/5993):
+
+> "The current KueueViz has no authentication, which makes it risky to expose externally.
+> We would need auth to be added before we can safely use it."
+> — david-gang, Issue #8969
+
+Without authentication, KueueViz is limited to `kubectl port-foward`.
+There is no way to safely expose it via Ingress, and no audit trail.
+
+### Goals
+
+1. Add optional authentication using Kubernetes TokenReview API
+2. Support bearer token via `Authorization: Bearer <token>` header
+3. Authenticate WebSocket connections at upgrade time
+4. Provide a login page with token input and validation flow
+5. Add metrics for authentication attempts
+6. Minimize manual steps for users when authenticating
+
+### Non-Goals
+
+1. Authorization (SubjectAccessReview), may be added in a future KEP
+2. Server-side sessions
+3. OAuth2/OIDC integration, users can deploy oauth2-proxy separately
+4. Token refresh, client resposibility
+
+## Proposal
+
+Add authentication middleware to KueueViz backend:
+
+1. Extract bearer token from request
+2. Validate via Kubernetes TokenReview API
+3. Return HTTP 401 for invalid or missing tokens
+4. Store user info in request context
+
+### User Stories
+
+**Cluster Admin**: Enable authentication via Helm so only users with valid
+Kubernetes tokens can access KueueViz.
+
+**Multi-Tenant Operator**: Expose KueueViz via Ingress to multiple teams,
+with per-user authentication and audit logging.
+
+**Developer**: Access KueueViz using existing Kubernetes credentials
+(ServiceAccount token or kubeconfig token).
+
+### Risks and Mitigations
+
+| Risk | Mitigation |
+|------|------------|
+| Breaking existing deployments | Disabled by default in Alpha; opt-in only |
+| Token exposure in logs | Never log tokens |
+| TokenReview latency | Cache with configurable TTL |
+| API server load from invalid tokens | Negative caching (5s TTL) for failed auth results |
+| WebSocket complexity | Authenticate at connection time only |
+| Expired tokens on long connections | Document token lifetime |
+
+## Design Details
+
+### API Changes
+
+None. Changes limited to environment variables and Helm values.
+
+### Authentication Flow
+
+```text
+Client                    KueueViz Backend         K8s API Server
+  │                             │                        │
+  │ GET /ws/workloads           │                        │
+  │ Authorization: Bearer <t>   │                        │
+  │────────────────────────────>│                        │
+  │                             │ POST tokenreviews      │
+  │                             │───────────────────────>│
+  │                             │ authenticated: true    │
+  │                             │<───────────────────────│
+  │ 200 OK / WS Upgrade         │                        │
+  │<────────────────────────────│                        │
+```
+
+### Backend Implementation
+
+**New file:** `cmd/kueueviz/backend/middleware/auth.go`
+
+Gin middleware that extracts bearer token from `Authorization` header or
+`?token=` query param, validates via TokenReview API, and returns HTTP 401
+with `WWW-Authenticate: Bearer` on failure.
+
+Caching protects the API server from excessive TokenReview calls:
+- Successful auth results are cached with configurable TTL (default 60s).
+- Failed auth results are cached with a short TTL (default 5s) so the same
+  invalid token does not trigger repeated calls.
+- The API server's own API Priority and Fairness (`--max-requests-inflight`)
+  provides additional protection; application-layer caching is a second line.
+
+**Modified:** `cmd/kueueviz/backend/config/server.go`
+
+Add fields to `ServerConfig`:
+
+- `AuthEnabled bool`
+- `AuthAudiences []string`
+- `AuthCacheTTL time.Duration`
+- `AuthNegativeCacheTTL time.Duration`
+
+Load from environment variables:
+
+- `KUEUEVIZ_AUTH_ENABLED`
+- `KUEUEVIZ_AUTH_AUDIENCES`
+- `KUEUEVIZ_AUTH_CACHE_TTL`
+- `KUEUEVIZ_AUTH_NEGATIVE_CACHE_TTL`
+
+**Modified:** `cmd/kueueviz/backend/main.go`
+
+- Create `kubernetes.Clientset` for TokenReview calls
+- Register auth middleware when `AuthEnabled` is true
+
+### WebSocket Authentication
+
+Tokens can be provided via:
+
+1. `Authorization: Bearer <token>` header (preferred)
+2. `?token=<token>` query parameter (browser fallback)
+
+Authentication happens at the HTTP upgrade request. No per-message auth.
+
+Future versions may use `Sec-WebSocket-Protocol` header instead of query
+parameter to avoid URL logging exposure.
+
+### Login Page
+
+The frontend provides a login page at `/login` with:
+
+1. A token input field
+2. A "Sign In" action that stores the token for subsequent requests
+3. Instructions for obtaining a token:
+
+```text
+kubectl create token <service-account> -n <namespace>
+```
+
+Authentication flow on the frontend:
+
+1. Access protected API endpoints with bearer token when available.
+2. On HTTP 401, clear token and redirect to `/login`.
+3. User provides a token and signs in.
+4. Retry protected requests with bearer token for HTTP APIs and WebSockets.
+5. On later HTTP 401, clear token and redirect back to `/login`.
+
+### RBAC Updates
+
+Add to `charts/kueue/templates/kueueviz/clusterrole.yaml`:
+
+```yaml
+- apiGroups: ["authentication.k8s.io"]
+  resources: ["tokenreviews"]
+  # TokenReview is a create-only API in Kubernetes (no get/list/watch/delete).
+  # The client POSTs a token and receives an authentication result.
+  verbs: ["create"]
+```
+
+### Helm Configuration
+
+`charts/kueue/values.yaml`:
+
+```yaml
+kueueViz:
+  backend:
+    auth:
+      enabled: false          # disabled by default in Alpha
+      audiences: ""           # optional, comma-separated
+      cacheTTL: "60s"
+      negativeCacheTTL: "5s"  # cache failed auth results to prevent API server abuse
```

  Are these specific to the currrent mechanism? I'm wondering if we should nest them under the name for the current mechanism, say `tokenReviewConfig`. Maybe in the future we will have more mechanisms like oauth, etc with their own config. wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T08:44:41Z

- **keps/5993-kueueviz-authentication/README.md:207** by [@mimowo](https://github.com/mimowo) — 2026-03-05T08:44:41Z
```diff
@@ -0,0 +1,328 @@
+# KEP-5993: KueueViz Authentication
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Authentication Flow](#authentication-flow)
+  - [Backend Implementation](#backend-implementation)
+  - [WebSocket Authentication](#websocket-authentication)
+  - [Login Page](#login-page)
+  - [RBAC Updates](#rbac-updates)
+  - [Helm Configuration](#helm-configuration)
+  - [Frontend Considerations](#frontend-considerations)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [OAuth2/OIDC Proxy](#oauth2oidc-proxy)
+  - [mTLS Client Certificates](#mtls-client-certificates)
+  - [Basic Authentication](#basic-authentication)
+<!-- /toc -->
+
+## Summary
+
+KueueViz currently exposes all Kueue resources without authentication.
+This KEP adds optional bearer token authentication using the Kubernetes
+TokenReview API, the same mechanism Kubernetes Dashboard uses.
+
+Authentication is controlled by a feature gate (`KueueVizAuth`), disabled
+by default in Alpha. Users opt in via Helm values (`auth.enabled: true`).
+
+## Motivation
+
+From [Issue #5993](https://github.com/kubernetes-sigs/kueue/issues/5993):
+
+> "The current KueueViz has no authentication, which makes it risky to expose externally.
+> We would need auth to be added before we can safely use it."
+> — david-gang, Issue #8969
+
+Without authentication, KueueViz is limited to `kubectl port-foward`.
+There is no way to safely expose it via Ingress, and no audit trail.
+
+### Goals
+
+1. Add optional authentication using Kubernetes TokenReview API
+2. Support bearer token via `Authorization: Bearer <token>` header
+3. Authenticate WebSocket connections at upgrade time
+4. Provide a login page with token input and validation flow
+5. Add metrics for authentication attempts
+6. Minimize manual steps for users when authenticating
+
+### Non-Goals
+
+1. Authorization (SubjectAccessReview), may be added in a future KEP
+2. Server-side sessions
+3. OAuth2/OIDC integration, users can deploy oauth2-proxy separately
+4. Token refresh, client resposibility
+
+## Proposal
+
+Add authentication middleware to KueueViz backend:
+
+1. Extract bearer token from request
+2. Validate via Kubernetes TokenReview API
+3. Return HTTP 401 for invalid or missing tokens
+4. Store user info in request context
+
+### User Stories
+
+**Cluster Admin**: Enable authentication via Helm so only users with valid
+Kubernetes tokens can access KueueViz.
+
+**Multi-Tenant Operator**: Expose KueueViz via Ingress to multiple teams,
+with per-user authentication and audit logging.
+
+**Developer**: Access KueueViz using existing Kubernetes credentials
+(ServiceAccount token or kubeconfig token).
+
+### Risks and Mitigations
+
+| Risk | Mitigation |
+|------|------------|
+| Breaking existing deployments | Disabled by default in Alpha; opt-in only |
+| Token exposure in logs | Never log tokens |
+| TokenReview latency | Cache with configurable TTL |
+| API server load from invalid tokens | Negative caching (5s TTL) for failed auth results |
+| WebSocket complexity | Authenticate at connection time only |
+| Expired tokens on long connections | Document token lifetime |
+
+## Design Details
+
+### API Changes
+
+None. Changes limited to environment variables and Helm values.
+
+### Authentication Flow
+
+```text
+Client                    KueueViz Backend         K8s API Server
+  │                             │                        │
+  │ GET /ws/workloads           │                        │
+  │ Authorization: Bearer <t>   │                        │
+  │────────────────────────────>│                        │
+  │                             │ POST tokenreviews      │
+  │                             │───────────────────────>│
+  │                             │ authenticated: true    │
+  │                             │<───────────────────────│
+  │ 200 OK / WS Upgrade         │                        │
+  │<────────────────────────────│                        │
+```
+
+### Backend Implementation
+
+**New file:** `cmd/kueueviz/backend/middleware/auth.go`
+
+Gin middleware that extracts bearer token from `Authorization` header or
+`?token=` query param, validates via TokenReview API, and returns HTTP 401
+with `WWW-Authenticate: Bearer` on failure.
+
+Caching protects the API server from excessive TokenReview calls:
+- Successful auth results are cached with configurable TTL (default 60s).
+- Failed auth results are cached with a short TTL (default 5s) so the same
+  invalid token does not trigger repeated calls.
+- The API server's own API Priority and Fairness (`--max-requests-inflight`)
+  provides additional protection; application-layer caching is a second line.
+
+**Modified:** `cmd/kueueviz/backend/config/server.go`
+
+Add fields to `ServerConfig`:
+
+- `AuthEnabled bool`
+- `AuthAudiences []string`
+- `AuthCacheTTL time.Duration`
+- `AuthNegativeCacheTTL time.Duration`
+
+Load from environment variables:
+
+- `KUEUEVIZ_AUTH_ENABLED`
+- `KUEUEVIZ_AUTH_AUDIENCES`
+- `KUEUEVIZ_AUTH_CACHE_TTL`
+- `KUEUEVIZ_AUTH_NEGATIVE_CACHE_TTL`
+
+**Modified:** `cmd/kueueviz/backend/main.go`
+
+- Create `kubernetes.Clientset` for TokenReview calls
+- Register auth middleware when `AuthEnabled` is true
+
+### WebSocket Authentication
+
+Tokens can be provided via:
+
+1. `Authorization: Bearer <token>` header (preferred)
+2. `?token=<token>` query parameter (browser fallback)
+
+Authentication happens at the HTTP upgrade request. No per-message auth.
+
+Future versions may use `Sec-WebSocket-Protocol` header instead of query
+parameter to avoid URL logging exposure.
+
+### Login Page
+
+The frontend provides a login page at `/login` with:
+
+1. A token input field
+2. A "Sign In" action that stores the token for subsequent requests
+3. Instructions for obtaining a token:
+
+```text
+kubectl create token <service-account> -n <namespace>
+```
+
+Authentication flow on the frontend:
+
+1. Access protected API endpoints with bearer token when available.
+2. On HTTP 401, clear token and redirect to `/login`.
+3. User provides a token and signs in.
+4. Retry protected requests with bearer token for HTTP APIs and WebSockets.
+5. On later HTTP 401, clear token and redirect back to `/login`.
+
+### RBAC Updates
+
+Add to `charts/kueue/templates/kueueviz/clusterrole.yaml`:
+
+```yaml
+- apiGroups: ["authentication.k8s.io"]
+  resources: ["tokenreviews"]
+  # TokenReview is a create-only API in Kubernetes (no get/list/watch/delete).
+  # The client POSTs a token and receives an authentication result.
+  verbs: ["create"]
+```
+
+### Helm Configuration
+
+`charts/kueue/values.yaml`:
+
+```yaml
+kueueViz:
+  backend:
+    auth:
+      enabled: false          # disabled by default in Alpha
```

  Here I'm also wondering, maybe instead of `boolean` we could have something more generic like enum `mode` with options like `Disabled` or `TokenReview`, we could then naturally extend to support other authentication mechanisms, wdyt?

### Review by [@samzong](https://github.com/samzong) (commented) — 2026-03-05T09:42:03Z

- **keps/5993-kueueviz-authentication/README.md:141** by [@samzong](https://github.com/samzong) — 2026-03-05T09:42:03Z
```diff
@@ -0,0 +1,328 @@
+# KEP-5993: KueueViz Authentication
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Authentication Flow](#authentication-flow)
+  - [Backend Implementation](#backend-implementation)
+  - [WebSocket Authentication](#websocket-authentication)
+  - [Login Page](#login-page)
+  - [RBAC Updates](#rbac-updates)
+  - [Helm Configuration](#helm-configuration)
+  - [Frontend Considerations](#frontend-considerations)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [OAuth2/OIDC Proxy](#oauth2oidc-proxy)
+  - [mTLS Client Certificates](#mtls-client-certificates)
+  - [Basic Authentication](#basic-authentication)
+<!-- /toc -->
+
+## Summary
+
+KueueViz currently exposes all Kueue resources without authentication.
+This KEP adds optional bearer token authentication using the Kubernetes
+TokenReview API, the same mechanism Kubernetes Dashboard uses.
+
+Authentication is controlled by a feature gate (`KueueVizAuth`), disabled
+by default in Alpha. Users opt in via Helm values (`auth.enabled: true`).
+
+## Motivation
+
+From [Issue #5993](https://github.com/kubernetes-sigs/kueue/issues/5993):
+
+> "The current KueueViz has no authentication, which makes it risky to expose externally.
+> We would need auth to be added before we can safely use it."
+> — david-gang, Issue #8969
+
+Without authentication, KueueViz is limited to `kubectl port-foward`.
+There is no way to safely expose it via Ingress, and no audit trail.
+
+### Goals
+
+1. Add optional authentication using Kubernetes TokenReview API
+2. Support bearer token via `Authorization: Bearer <token>` header
+3. Authenticate WebSocket connections at upgrade time
+4. Provide a login page with token input and validation flow
+5. Add metrics for authentication attempts
+6. Minimize manual steps for users when authenticating
+
+### Non-Goals
+
+1. Authorization (SubjectAccessReview), may be added in a future KEP
+2. Server-side sessions
+3. OAuth2/OIDC integration, users can deploy oauth2-proxy separately
+4. Token refresh, client resposibility
+
+## Proposal
+
+Add authentication middleware to KueueViz backend:
+
+1. Extract bearer token from request
+2. Validate via Kubernetes TokenReview API
+3. Return HTTP 401 for invalid or missing tokens
+4. Store user info in request context
+
+### User Stories
+
+**Cluster Admin**: Enable authentication via Helm so only users with valid
+Kubernetes tokens can access KueueViz.
+
+**Multi-Tenant Operator**: Expose KueueViz via Ingress to multiple teams,
+with per-user authentication and audit logging.
+
+**Developer**: Access KueueViz using existing Kubernetes credentials
+(ServiceAccount token or kubeconfig token).
+
+### Risks and Mitigations
+
+| Risk | Mitigation |
+|------|------------|
+| Breaking existing deployments | Disabled by default in Alpha; opt-in only |
+| Token exposure in logs | Never log tokens |
+| TokenReview latency | Cache with configurable TTL |
+| API server load from invalid tokens | Negative caching (5s TTL) for failed auth results |
+| WebSocket complexity | Authenticate at connection time only |
+| Expired tokens on long connections | Document token lifetime |
+
+## Design Details
+
+### API Changes
+
+None. Changes limited to environment variables and Helm values.
+
+### Authentication Flow
+
+```text
+Client                    KueueViz Backend         K8s API Server
+  │                             │                        │
+  │ GET /ws/workloads           │                        │
+  │ Authorization: Bearer <t>   │                        │
+  │────────────────────────────>│                        │
+  │                             │ POST tokenreviews      │
+  │                             │───────────────────────>│
+  │                             │ authenticated: true    │
+  │                             │<───────────────────────│
+  │ 200 OK / WS Upgrade         │                        │
+  │<────────────────────────────│                        │
+```
+
+### Backend Implementation
+
+**New file:** `cmd/kueueviz/backend/middleware/auth.go`
+
+Gin middleware that extracts bearer token from `Authorization` header or
+`?token=` query param, validates via TokenReview API, and returns HTTP 401
+with `WWW-Authenticate: Bearer` on failure.
+
+Caching protects the API server from excessive TokenReview calls:
+- Successful auth results are cached with configurable TTL (default 60s).
+- Failed auth results are cached with a short TTL (default 5s) so the same
+  invalid token does not trigger repeated calls.
+- The API server's own API Priority and Fairness (`--max-requests-inflight`)
+  provides additional protection; application-layer caching is a second line.
+
+**Modified:** `cmd/kueueviz/backend/config/server.go`
+
+Add fields to `ServerConfig`:
+
+- `AuthEnabled bool`
+- `AuthAudiences []string`
+- `AuthCacheTTL time.Duration`
+- `AuthNegativeCacheTTL time.Duration`
```

  All addressed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T09:44:39Z

- **keps/5993-kueueviz-authentication/kep.yaml:11** by [@mimowo](https://github.com/mimowo) — 2026-03-05T09:44:39Z
```diff
@@ -1,20 +1,18 @@
-title: MultiKueue Orchestrated Preemption
-kep-number: 8303
+title: KueueViz Authentication
+kep-number: 5993
 authors:
-  - "@kshalot"
+  - "@samzong"
 status: provisional
 creation-date: 2026-02-04
 reviewers:
-  - "@ichekrygin"
-  - "@olekzabl"
-approvers:
   - "@mimowo"
-  - "@mwielgus"
   - "@tenzen-y"
+approvers:
+  - TBD
```

  add mimowo here

### Review by [@samzong](https://github.com/samzong) (commented) — 2026-03-05T10:04:57Z

- **keps/5993-kueueviz-authentication/kep.yaml:11** by [@samzong](https://github.com/samzong) — 2026-03-05T10:04:57Z
```diff
@@ -1,20 +1,18 @@
-title: MultiKueue Orchestrated Preemption
-kep-number: 8303
+title: KueueViz Authentication
+kep-number: 5993
 authors:
-  - "@kshalot"
+  - "@samzong"
 status: provisional
 creation-date: 2026-02-04
 reviewers:
-  - "@ichekrygin"
-  - "@olekzabl"
-approvers:
   - "@mimowo"
-  - "@mwielgus"
   - "@tenzen-y"
+approvers:
+  - TBD
```

  done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T10:20:22Z

- **keps/5993-kueueviz-authentication/README.md:275** by [@mimowo](https://github.com/mimowo) — 2026-03-05T10:20:22Z
```diff
@@ -0,0 +1,328 @@
+# KEP-5993: KueueViz Authentication
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Authentication Flow](#authentication-flow)
+  - [Backend Implementation](#backend-implementation)
+  - [WebSocket Authentication](#websocket-authentication)
+  - [Login Page](#login-page)
+  - [RBAC Updates](#rbac-updates)
+  - [Helm Configuration](#helm-configuration)
+  - [Frontend Considerations](#frontend-considerations)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [OAuth2/OIDC Proxy](#oauth2oidc-proxy)
+  - [mTLS Client Certificates](#mtls-client-certificates)
+  - [Basic Authentication](#basic-authentication)
+<!-- /toc -->
+
+## Summary
+
+KueueViz currently exposes all Kueue resources without authentication.
+This KEP adds optional bearer token authentication using the Kubernetes
+TokenReview API, the same mechanism Kubernetes Dashboard uses.
+
+Authentication is controlled by a feature gate (`KueueVizAuth`), disabled
+by default in Alpha. Users opt in via Helm values (`auth.mode: TokenReview`).
+
+## Motivation
+
+From [Issue #5993](https://github.com/kubernetes-sigs/kueue/issues/5993):
+
+> "The current KueueViz has no authentication, which makes it risky to expose externally.
+> We would need auth to be added before we can safely use it."
+> — david-gang, Issue #8969
+
+Without authentication, KueueViz is limited to `kubectl port-foward`.
+There is no way to safely expose it via Ingress, and no audit trail.
+
+### Goals
+
+1. Add optional authentication using Kubernetes TokenReview API
+2. Support bearer token via `Authorization: Bearer <token>` header
+3. Authenticate WebSocket connections at upgrade time
+4. Provide a login page with token input and validation flow
+5. Add metrics for authentication attempts
+6. Minimize manual steps for users when authenticating
+
+### Non-Goals
+
+1. Authorization (SubjectAccessReview), may be added in a future KEP
+2. Server-side sessions
+3. OAuth2/OIDC integration, users can deploy oauth2-proxy separately
+4. Token refresh, client resposibility
+
+## Proposal
+
+Add authentication middleware to KueueViz backend:
+
+1. Extract bearer token from request
+2. Validate via Kubernetes TokenReview API
+3. Return HTTP 401 for invalid or missing tokens
+4. Store user info in request context
+
+### User Stories
+
+**Cluster Admin**: Enable authentication via Helm so only users with valid
+Kubernetes tokens can access KueueViz.
+
+**Multi-Tenant Operator**: Expose KueueViz via Ingress to multiple teams,
+with per-user authentication and audit logging.
+
+**Developer**: Access KueueViz using existing Kubernetes credentials
+(ServiceAccount token or kubeconfig token).
+
+### Risks and Mitigations
+
+| Risk | Mitigation |
+|------|------------|
+| Breaking existing deployments | Disabled by default in Alpha; opt-in via `auth.mode: TokenReview` |
+| Token exposure in logs | Never log tokens |
+| TokenReview latency | Cache with configurable TTL |
+| API server load from invalid tokens | Negative caching (5s TTL) for failed auth results |
+| WebSocket complexity | Authenticate at connection time only |
+| Expired tokens on long connections | Document token lifetime |
+
+## Design Details
+
+### API Changes
+
+None. Changes limited to environment variables and Helm values.
+
+### Authentication Flow
+
+```text
+Client                    KueueViz Backend         K8s API Server
+  │                             │                        │
+  │ GET /ws/workloads           │                        │
+  │ Authorization: Bearer <t>   │                        │
+  │────────────────────────────>│                        │
+  │                             │ POST tokenreviews      │
+  │                             │───────────────────────>│
+  │                             │ authenticated: true    │
+  │                             │<───────────────────────│
+  │ 200 OK / WS Upgrade         │                        │
+  │<────────────────────────────│                        │
+```
+
+### Backend Implementation
+
+**New file:** `cmd/kueueviz/backend/middleware/auth.go`
+
+Gin middleware that extracts bearer token from `Authorization` header or
+`Sec-WebSocket-Protocol` subprotocol, validates via TokenReview API, and returns HTTP 401
+with `WWW-Authenticate: Bearer` on failure.
+
+Caching protects the API server from excessive TokenReview calls:
+- Successful auth results are cached with configurable TTL (default 60s).
+- Failed auth results are cached with a short TTL (default 5s) so the same
+  invalid token does not trigger repeated calls.
+- The API server's own API Priority and Fairness (`--max-requests-inflight`)
+  provides additional protection; application-layer caching is a second line.
+
+**Modified:** `cmd/kueueviz/backend/config/server.go`
+
+Add fields to `ServerConfig`:
+
+- `AuthMode string` — authentication mode; one of `Disabled` or `TokenReview`
+- `AuthTokenReviewAudiences []string` — optional token audiences to validate against; empty means any audience is accepted
+- `AuthTokenReviewCacheTTL time.Duration` — TTL for caching successful TokenReview results (default 60s)
+- `AuthTokenReviewNegativeCacheTTL time.Duration` — TTL for caching failed TokenReview results (default 5s)
+
+Load from environment variables:
+
+- `KUEUEVIZ_AUTH_MODE`
+- `KUEUEVIZ_AUTH_TOKEN_REVIEW_AUDIENCES`
+- `KUEUEVIZ_AUTH_TOKEN_REVIEW_CACHE_TTL`
+- `KUEUEVIZ_AUTH_TOKEN_REVIEW_NEGATIVE_CACHE_TTL`
+
+**Modified:** `cmd/kueueviz/backend/main.go`
+
+- Create `kubernetes.Clientset` for TokenReview calls
+- Register auth middleware when `AuthMode` is `TokenReview`
+
+### WebSocket Authentication
+
+Tokens can be provided via:
+
+1. `Authorization: Bearer <token>` header (preferred)
+2. `Sec-WebSocket-Protocol: kueueviz.auth.<base64-token>` subprotocol (browser fallback)
+
+Authentication happens at the HTTP upgrade request. No per-message auth.
+
+### Login Page
+
+The frontend provides a login page at `/login` with:
+
+1. A token input field
+2. A "Sign In" action that stores the token for subsequent requests
+3. Instructions for obtaining a token:
+
+```text
+kubectl create token <service-account> -n <namespace> --duration=168h
+```
+
+Authentication flow on the frontend:
+
+1. Access protected API endpoints with bearer token when available.
+2. On HTTP 401, clear token and redirect to `/login`.
+3. User provides a token and signs in.
+4. Retry protected requests with bearer token for HTTP APIs and WebSockets.
+5. On later HTTP 401, clear token and redirect back to `/login`.
+
+### RBAC Updates
+
+Add to `charts/kueue/templates/kueueviz/clusterrole.yaml`:
+
+```yaml
+- apiGroups: ["authentication.k8s.io"]
+  resources: ["tokenreviews"]
+  # TokenReview is a create-only API in Kubernetes (no get/list/watch/delete).
+  # The client POSTs a token and receives an authentication result.
+  verbs: ["create"]
+```
+
+### Helm Configuration
+
+`charts/kueue/values.yaml`:
+
+```yaml
+kueueViz:
+  backend:
+    auth:
+      mode: "Disabled"          # Disabled | TokenReview
+      tokenReviewConfig:
+        audiences: ""           # optional, comma-separated; empty means any audience
+        cacheTTL: "60s"         # how long a successful auth result is cached
+        negativeCacheTTL: "5s"  # how long a failed auth result is cached to prevent API server abuse
+```
+
+`charts/kueue/templates/kueueviz/backend-deployment.yaml`:
+
+```yaml
+{{- if eq .Values.kueueViz.backend.auth.mode "TokenReview" }}
+- name: KUEUEVIZ_AUTH_MODE
+  value: "TokenReview"
+{{- if .Values.kueueViz.backend.auth.tokenReviewConfig.audiences }}
+- name: KUEUEVIZ_AUTH_TOKEN_REVIEW_AUDIENCES
+  value: {{ .Values.kueueViz.backend.auth.tokenReviewConfig.audiences | quote }}
+{{- end }}
+- name: KUEUEVIZ_AUTH_TOKEN_REVIEW_CACHE_TTL
+  value: {{ .Values.kueueViz.backend.auth.tokenReviewConfig.cacheTTL | default "60s" | quote }}
+- name: KUEUEVIZ_AUTH_TOKEN_REVIEW_NEGATIVE_CACHE_TTL
+  value: {{ .Values.kueueViz.backend.auth.tokenReviewConfig.negativeCacheTTL | default "5s" | quote }}
+{{- end }}
+```
+
+### Frontend Considerations
+
+The frontend handles authentication with minimal user friction:
+
+1. When auth is enabled, the frontend detects HTTP 401 and redirects to `/login`.
+2. The login page provides a token input field and a helper command:
+   `kubectl create token <service-account> -n <namespace> --duration=168h`.
+3. Once entered, the token is stored in sessionStorage (scoped to the browser
+   tab, cleared on tab close) and attached to all subsequent HTTP and WebSocket
+   requests automatically.
+4. On token expiry (HTTP 401), the user is prompted to re-authenticate.
+
+Future: automatic token acquisition via `kubectl proxy` or browser-based
+OIDC, removing the need to copy-paste tokens.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+Unit tests (`middleware/auth_test.go`):
+
+- Token extraction from header and WebSocket subprotocol
+- Cache hit/miss
+- Expired cache entries
+- Disabled auth passthrough
+
+Coverage target: >80%
+
+Integration tests:
+
+- Valid token → 200
+- Invalid token → 401
+- Missing token → 401
+- Disabled auth → pass all
+
+E2E tests (`test/e2e/kueueviz/`):
+
+- Deploy with auth enabled
+- Test with ServiceAccount token
+- Test rejection of unauthenticated requests
+
+### Graduation Criteria
+
+Alpha (v0.17, disabled by default):
+
+- [ ] Feature gate `KueueVizAuth` added (default: false)
```

  Actually, I'm not sure we need a feature gate in the Kueue main code, right? The role of the feature gate could play rather the configuration with `mode: Disabled`, wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T10:20:52Z

- **keps/5993-kueueviz-authentication/README.md:275** by [@mimowo](https://github.com/mimowo) — 2026-03-05T10:20:52Z
```diff
@@ -0,0 +1,328 @@
+# KEP-5993: KueueViz Authentication
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Authentication Flow](#authentication-flow)
+  - [Backend Implementation](#backend-implementation)
+  - [WebSocket Authentication](#websocket-authentication)
+  - [Login Page](#login-page)
+  - [RBAC Updates](#rbac-updates)
+  - [Helm Configuration](#helm-configuration)
+  - [Frontend Considerations](#frontend-considerations)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [OAuth2/OIDC Proxy](#oauth2oidc-proxy)
+  - [mTLS Client Certificates](#mtls-client-certificates)
+  - [Basic Authentication](#basic-authentication)
+<!-- /toc -->
+
+## Summary
+
+KueueViz currently exposes all Kueue resources without authentication.
+This KEP adds optional bearer token authentication using the Kubernetes
+TokenReview API, the same mechanism Kubernetes Dashboard uses.
+
+Authentication is controlled by a feature gate (`KueueVizAuth`), disabled
+by default in Alpha. Users opt in via Helm values (`auth.mode: TokenReview`).
+
+## Motivation
+
+From [Issue #5993](https://github.com/kubernetes-sigs/kueue/issues/5993):
+
+> "The current KueueViz has no authentication, which makes it risky to expose externally.
+> We would need auth to be added before we can safely use it."
+> — david-gang, Issue #8969
+
+Without authentication, KueueViz is limited to `kubectl port-foward`.
+There is no way to safely expose it via Ingress, and no audit trail.
+
+### Goals
+
+1. Add optional authentication using Kubernetes TokenReview API
+2. Support bearer token via `Authorization: Bearer <token>` header
+3. Authenticate WebSocket connections at upgrade time
+4. Provide a login page with token input and validation flow
+5. Add metrics for authentication attempts
+6. Minimize manual steps for users when authenticating
+
+### Non-Goals
+
+1. Authorization (SubjectAccessReview), may be added in a future KEP
+2. Server-side sessions
+3. OAuth2/OIDC integration, users can deploy oauth2-proxy separately
+4. Token refresh, client resposibility
+
+## Proposal
+
+Add authentication middleware to KueueViz backend:
+
+1. Extract bearer token from request
+2. Validate via Kubernetes TokenReview API
+3. Return HTTP 401 for invalid or missing tokens
+4. Store user info in request context
+
+### User Stories
+
+**Cluster Admin**: Enable authentication via Helm so only users with valid
+Kubernetes tokens can access KueueViz.
+
+**Multi-Tenant Operator**: Expose KueueViz via Ingress to multiple teams,
+with per-user authentication and audit logging.
+
+**Developer**: Access KueueViz using existing Kubernetes credentials
+(ServiceAccount token or kubeconfig token).
+
+### Risks and Mitigations
+
+| Risk | Mitigation |
+|------|------------|
+| Breaking existing deployments | Disabled by default in Alpha; opt-in via `auth.mode: TokenReview` |
+| Token exposure in logs | Never log tokens |
+| TokenReview latency | Cache with configurable TTL |
+| API server load from invalid tokens | Negative caching (5s TTL) for failed auth results |
+| WebSocket complexity | Authenticate at connection time only |
+| Expired tokens on long connections | Document token lifetime |
+
+## Design Details
+
+### API Changes
+
+None. Changes limited to environment variables and Helm values.
+
+### Authentication Flow
+
+```text
+Client                    KueueViz Backend         K8s API Server
+  │                             │                        │
+  │ GET /ws/workloads           │                        │
+  │ Authorization: Bearer <t>   │                        │
+  │────────────────────────────>│                        │
+  │                             │ POST tokenreviews      │
+  │                             │───────────────────────>│
+  │                             │ authenticated: true    │
+  │                             │<───────────────────────│
+  │ 200 OK / WS Upgrade         │                        │
+  │<────────────────────────────│                        │
+```
+
+### Backend Implementation
+
+**New file:** `cmd/kueueviz/backend/middleware/auth.go`
+
+Gin middleware that extracts bearer token from `Authorization` header or
+`Sec-WebSocket-Protocol` subprotocol, validates via TokenReview API, and returns HTTP 401
+with `WWW-Authenticate: Bearer` on failure.
+
+Caching protects the API server from excessive TokenReview calls:
+- Successful auth results are cached with configurable TTL (default 60s).
+- Failed auth results are cached with a short TTL (default 5s) so the same
+  invalid token does not trigger repeated calls.
+- The API server's own API Priority and Fairness (`--max-requests-inflight`)
+  provides additional protection; application-layer caching is a second line.
+
+**Modified:** `cmd/kueueviz/backend/config/server.go`
+
+Add fields to `ServerConfig`:
+
+- `AuthMode string` — authentication mode; one of `Disabled` or `TokenReview`
+- `AuthTokenReviewAudiences []string` — optional token audiences to validate against; empty means any audience is accepted
+- `AuthTokenReviewCacheTTL time.Duration` — TTL for caching successful TokenReview results (default 60s)
+- `AuthTokenReviewNegativeCacheTTL time.Duration` — TTL for caching failed TokenReview results (default 5s)
+
+Load from environment variables:
+
+- `KUEUEVIZ_AUTH_MODE`
+- `KUEUEVIZ_AUTH_TOKEN_REVIEW_AUDIENCES`
+- `KUEUEVIZ_AUTH_TOKEN_REVIEW_CACHE_TTL`
+- `KUEUEVIZ_AUTH_TOKEN_REVIEW_NEGATIVE_CACHE_TTL`
+
+**Modified:** `cmd/kueueviz/backend/main.go`
+
+- Create `kubernetes.Clientset` for TokenReview calls
+- Register auth middleware when `AuthMode` is `TokenReview`
+
+### WebSocket Authentication
+
+Tokens can be provided via:
+
+1. `Authorization: Bearer <token>` header (preferred)
+2. `Sec-WebSocket-Protocol: kueueviz.auth.<base64-token>` subprotocol (browser fallback)
+
+Authentication happens at the HTTP upgrade request. No per-message auth.
+
+### Login Page
+
+The frontend provides a login page at `/login` with:
+
+1. A token input field
+2. A "Sign In" action that stores the token for subsequent requests
+3. Instructions for obtaining a token:
+
+```text
+kubectl create token <service-account> -n <namespace> --duration=168h
+```
+
+Authentication flow on the frontend:
+
+1. Access protected API endpoints with bearer token when available.
+2. On HTTP 401, clear token and redirect to `/login`.
+3. User provides a token and signs in.
+4. Retry protected requests with bearer token for HTTP APIs and WebSockets.
+5. On later HTTP 401, clear token and redirect back to `/login`.
+
+### RBAC Updates
+
+Add to `charts/kueue/templates/kueueviz/clusterrole.yaml`:
+
+```yaml
+- apiGroups: ["authentication.k8s.io"]
+  resources: ["tokenreviews"]
+  # TokenReview is a create-only API in Kubernetes (no get/list/watch/delete).
+  # The client POSTs a token and receives an authentication result.
+  verbs: ["create"]
+```
+
+### Helm Configuration
+
+`charts/kueue/values.yaml`:
+
+```yaml
+kueueViz:
+  backend:
+    auth:
+      mode: "Disabled"          # Disabled | TokenReview
+      tokenReviewConfig:
+        audiences: ""           # optional, comma-separated; empty means any audience
+        cacheTTL: "60s"         # how long a successful auth result is cached
+        negativeCacheTTL: "5s"  # how long a failed auth result is cached to prevent API server abuse
+```
+
+`charts/kueue/templates/kueueviz/backend-deployment.yaml`:
+
+```yaml
+{{- if eq .Values.kueueViz.backend.auth.mode "TokenReview" }}
+- name: KUEUEVIZ_AUTH_MODE
+  value: "TokenReview"
+{{- if .Values.kueueViz.backend.auth.tokenReviewConfig.audiences }}
+- name: KUEUEVIZ_AUTH_TOKEN_REVIEW_AUDIENCES
+  value: {{ .Values.kueueViz.backend.auth.tokenReviewConfig.audiences | quote }}
+{{- end }}
+- name: KUEUEVIZ_AUTH_TOKEN_REVIEW_CACHE_TTL
+  value: {{ .Values.kueueViz.backend.auth.tokenReviewConfig.cacheTTL | default "60s" | quote }}
+- name: KUEUEVIZ_AUTH_TOKEN_REVIEW_NEGATIVE_CACHE_TTL
+  value: {{ .Values.kueueViz.backend.auth.tokenReviewConfig.negativeCacheTTL | default "5s" | quote }}
+{{- end }}
+```
+
+### Frontend Considerations
+
+The frontend handles authentication with minimal user friction:
+
+1. When auth is enabled, the frontend detects HTTP 401 and redirects to `/login`.
+2. The login page provides a token input field and a helper command:
+   `kubectl create token <service-account> -n <namespace> --duration=168h`.
+3. Once entered, the token is stored in sessionStorage (scoped to the browser
+   tab, cleared on tab close) and attached to all subsequent HTTP and WebSocket
+   requests automatically.
+4. On token expiry (HTTP 401), the user is prompted to re-authenticate.
+
+Future: automatic token acquisition via `kubectl proxy` or browser-based
+OIDC, removing the need to copy-paste tokens.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+Unit tests (`middleware/auth_test.go`):
+
+- Token extraction from header and WebSocket subprotocol
+- Cache hit/miss
+- Expired cache entries
+- Disabled auth passthrough
+
+Coverage target: >80%
+
+Integration tests:
+
+- Valid token → 200
+- Invalid token → 401
+- Missing token → 401
+- Disabled auth → pass all
+
+E2E tests (`test/e2e/kueueviz/`):
+
+- Deploy with auth enabled
+- Test with ServiceAccount token
+- Test rejection of unauthenticated requests
+
+### Graduation Criteria
+
+Alpha (v0.17, disabled by default):
+
+- [ ] Feature gate `KueueVizAuth` added (default: false)
```

  Because AFAIK no changes to the main Kueue are needed based on https://github.com/kubernetes-sigs/kueue/pull/9684

### Review by [@samzong](https://github.com/samzong) (commented) — 2026-03-05T10:24:23Z

- **keps/5993-kueueviz-authentication/README.md:275** by [@samzong](https://github.com/samzong) — 2026-03-05T10:24:23Z
```diff
@@ -0,0 +1,328 @@
+# KEP-5993: KueueViz Authentication
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Authentication Flow](#authentication-flow)
+  - [Backend Implementation](#backend-implementation)
+  - [WebSocket Authentication](#websocket-authentication)
+  - [Login Page](#login-page)
+  - [RBAC Updates](#rbac-updates)
+  - [Helm Configuration](#helm-configuration)
+  - [Frontend Considerations](#frontend-considerations)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [OAuth2/OIDC Proxy](#oauth2oidc-proxy)
+  - [mTLS Client Certificates](#mtls-client-certificates)
+  - [Basic Authentication](#basic-authentication)
+<!-- /toc -->
+
+## Summary
+
+KueueViz currently exposes all Kueue resources without authentication.
+This KEP adds optional bearer token authentication using the Kubernetes
+TokenReview API, the same mechanism Kubernetes Dashboard uses.
+
+Authentication is controlled by a feature gate (`KueueVizAuth`), disabled
+by default in Alpha. Users opt in via Helm values (`auth.mode: TokenReview`).
+
+## Motivation
+
+From [Issue #5993](https://github.com/kubernetes-sigs/kueue/issues/5993):
+
+> "The current KueueViz has no authentication, which makes it risky to expose externally.
+> We would need auth to be added before we can safely use it."
+> — david-gang, Issue #8969
+
+Without authentication, KueueViz is limited to `kubectl port-foward`.
+There is no way to safely expose it via Ingress, and no audit trail.
+
+### Goals
+
+1. Add optional authentication using Kubernetes TokenReview API
+2. Support bearer token via `Authorization: Bearer <token>` header
+3. Authenticate WebSocket connections at upgrade time
+4. Provide a login page with token input and validation flow
+5. Add metrics for authentication attempts
+6. Minimize manual steps for users when authenticating
+
+### Non-Goals
+
+1. Authorization (SubjectAccessReview), may be added in a future KEP
+2. Server-side sessions
+3. OAuth2/OIDC integration, users can deploy oauth2-proxy separately
+4. Token refresh, client resposibility
+
+## Proposal
+
+Add authentication middleware to KueueViz backend:
+
+1. Extract bearer token from request
+2. Validate via Kubernetes TokenReview API
+3. Return HTTP 401 for invalid or missing tokens
+4. Store user info in request context
+
+### User Stories
+
+**Cluster Admin**: Enable authentication via Helm so only users with valid
+Kubernetes tokens can access KueueViz.
+
+**Multi-Tenant Operator**: Expose KueueViz via Ingress to multiple teams,
+with per-user authentication and audit logging.
+
+**Developer**: Access KueueViz using existing Kubernetes credentials
+(ServiceAccount token or kubeconfig token).
+
+### Risks and Mitigations
+
+| Risk | Mitigation |
+|------|------------|
+| Breaking existing deployments | Disabled by default in Alpha; opt-in via `auth.mode: TokenReview` |
+| Token exposure in logs | Never log tokens |
+| TokenReview latency | Cache with configurable TTL |
+| API server load from invalid tokens | Negative caching (5s TTL) for failed auth results |
+| WebSocket complexity | Authenticate at connection time only |
+| Expired tokens on long connections | Document token lifetime |
+
+## Design Details
+
+### API Changes
+
+None. Changes limited to environment variables and Helm values.
+
+### Authentication Flow
+
+```text
+Client                    KueueViz Backend         K8s API Server
+  │                             │                        │
+  │ GET /ws/workloads           │                        │
+  │ Authorization: Bearer <t>   │                        │
+  │────────────────────────────>│                        │
+  │                             │ POST tokenreviews      │
+  │                             │───────────────────────>│
+  │                             │ authenticated: true    │
+  │                             │<───────────────────────│
+  │ 200 OK / WS Upgrade         │                        │
+  │<────────────────────────────│                        │
+```
+
+### Backend Implementation
+
+**New file:** `cmd/kueueviz/backend/middleware/auth.go`
+
+Gin middleware that extracts bearer token from `Authorization` header or
+`Sec-WebSocket-Protocol` subprotocol, validates via TokenReview API, and returns HTTP 401
+with `WWW-Authenticate: Bearer` on failure.
+
+Caching protects the API server from excessive TokenReview calls:
+- Successful auth results are cached with configurable TTL (default 60s).
+- Failed auth results are cached with a short TTL (default 5s) so the same
+  invalid token does not trigger repeated calls.
+- The API server's own API Priority and Fairness (`--max-requests-inflight`)
+  provides additional protection; application-layer caching is a second line.
+
+**Modified:** `cmd/kueueviz/backend/config/server.go`
+
+Add fields to `ServerConfig`:
+
+- `AuthMode string` — authentication mode; one of `Disabled` or `TokenReview`
+- `AuthTokenReviewAudiences []string` — optional token audiences to validate against; empty means any audience is accepted
+- `AuthTokenReviewCacheTTL time.Duration` — TTL for caching successful TokenReview results (default 60s)
+- `AuthTokenReviewNegativeCacheTTL time.Duration` — TTL for caching failed TokenReview results (default 5s)
+
+Load from environment variables:
+
+- `KUEUEVIZ_AUTH_MODE`
+- `KUEUEVIZ_AUTH_TOKEN_REVIEW_AUDIENCES`
+- `KUEUEVIZ_AUTH_TOKEN_REVIEW_CACHE_TTL`
+- `KUEUEVIZ_AUTH_TOKEN_REVIEW_NEGATIVE_CACHE_TTL`
+
+**Modified:** `cmd/kueueviz/backend/main.go`
+
+- Create `kubernetes.Clientset` for TokenReview calls
+- Register auth middleware when `AuthMode` is `TokenReview`
+
+### WebSocket Authentication
+
+Tokens can be provided via:
+
+1. `Authorization: Bearer <token>` header (preferred)
+2. `Sec-WebSocket-Protocol: kueueviz.auth.<base64-token>` subprotocol (browser fallback)
+
+Authentication happens at the HTTP upgrade request. No per-message auth.
+
+### Login Page
+
+The frontend provides a login page at `/login` with:
+
+1. A token input field
+2. A "Sign In" action that stores the token for subsequent requests
+3. Instructions for obtaining a token:
+
+```text
+kubectl create token <service-account> -n <namespace> --duration=168h
+```
+
+Authentication flow on the frontend:
+
+1. Access protected API endpoints with bearer token when available.
+2. On HTTP 401, clear token and redirect to `/login`.
+3. User provides a token and signs in.
+4. Retry protected requests with bearer token for HTTP APIs and WebSockets.
+5. On later HTTP 401, clear token and redirect back to `/login`.
+
+### RBAC Updates
+
+Add to `charts/kueue/templates/kueueviz/clusterrole.yaml`:
+
+```yaml
+- apiGroups: ["authentication.k8s.io"]
+  resources: ["tokenreviews"]
+  # TokenReview is a create-only API in Kubernetes (no get/list/watch/delete).
+  # The client POSTs a token and receives an authentication result.
+  verbs: ["create"]
+```
+
+### Helm Configuration
+
+`charts/kueue/values.yaml`:
+
+```yaml
+kueueViz:
+  backend:
+    auth:
+      mode: "Disabled"          # Disabled | TokenReview
+      tokenReviewConfig:
+        audiences: ""           # optional, comma-separated; empty means any audience
+        cacheTTL: "60s"         # how long a successful auth result is cached
+        negativeCacheTTL: "5s"  # how long a failed auth result is cached to prevent API server abuse
+```
+
+`charts/kueue/templates/kueueviz/backend-deployment.yaml`:
+
+```yaml
+{{- if eq .Values.kueueViz.backend.auth.mode "TokenReview" }}
+- name: KUEUEVIZ_AUTH_MODE
+  value: "TokenReview"
+{{- if .Values.kueueViz.backend.auth.tokenReviewConfig.audiences }}
+- name: KUEUEVIZ_AUTH_TOKEN_REVIEW_AUDIENCES
+  value: {{ .Values.kueueViz.backend.auth.tokenReviewConfig.audiences | quote }}
+{{- end }}
+- name: KUEUEVIZ_AUTH_TOKEN_REVIEW_CACHE_TTL
+  value: {{ .Values.kueueViz.backend.auth.tokenReviewConfig.cacheTTL | default "60s" | quote }}
+- name: KUEUEVIZ_AUTH_TOKEN_REVIEW_NEGATIVE_CACHE_TTL
+  value: {{ .Values.kueueViz.backend.auth.tokenReviewConfig.negativeCacheTTL | default "5s" | quote }}
+{{- end }}
+```
+
+### Frontend Considerations
+
+The frontend handles authentication with minimal user friction:
+
+1. When auth is enabled, the frontend detects HTTP 401 and redirects to `/login`.
+2. The login page provides a token input field and a helper command:
+   `kubectl create token <service-account> -n <namespace> --duration=168h`.
+3. Once entered, the token is stored in sessionStorage (scoped to the browser
+   tab, cleared on tab close) and attached to all subsequent HTTP and WebSocket
+   requests automatically.
+4. On token expiry (HTTP 401), the user is prompted to re-authenticate.
+
+Future: automatic token acquisition via `kubectl proxy` or browser-based
+OIDC, removing the need to copy-paste tokens.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+Unit tests (`middleware/auth_test.go`):
+
+- Token extraction from header and WebSocket subprotocol
+- Cache hit/miss
+- Expired cache entries
+- Disabled auth passthrough
+
+Coverage target: >80%
+
+Integration tests:
+
+- Valid token → 200
+- Invalid token → 401
+- Missing token → 401
+- Disabled auth → pass all
+
+E2E tests (`test/e2e/kueueviz/`):
+
+- Deploy with auth enabled
+- Test with ServiceAccount token
+- Test rejection of unauthenticated requests
+
+### Graduation Criteria
+
+Alpha (v0.17, disabled by default):
+
+- [ ] Feature gate `KueueVizAuth` added (default: false)
```

  yes. ur right, I not use it feature gateway

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T10:48:16Z

- **keps/5993-kueueviz-authentication/README.md:275** by [@mimowo](https://github.com/mimowo) — 2026-03-05T10:48:16Z
```diff
@@ -0,0 +1,328 @@
+# KEP-5993: KueueViz Authentication
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Authentication Flow](#authentication-flow)
+  - [Backend Implementation](#backend-implementation)
+  - [WebSocket Authentication](#websocket-authentication)
+  - [Login Page](#login-page)
+  - [RBAC Updates](#rbac-updates)
+  - [Helm Configuration](#helm-configuration)
+  - [Frontend Considerations](#frontend-considerations)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [OAuth2/OIDC Proxy](#oauth2oidc-proxy)
+  - [mTLS Client Certificates](#mtls-client-certificates)
+  - [Basic Authentication](#basic-authentication)
+<!-- /toc -->
+
+## Summary
+
+KueueViz currently exposes all Kueue resources without authentication.
+This KEP adds optional bearer token authentication using the Kubernetes
+TokenReview API, the same mechanism Kubernetes Dashboard uses.
+
+Authentication is controlled by a feature gate (`KueueVizAuth`), disabled
+by default in Alpha. Users opt in via Helm values (`auth.mode: TokenReview`).
+
+## Motivation
+
+From [Issue #5993](https://github.com/kubernetes-sigs/kueue/issues/5993):
+
+> "The current KueueViz has no authentication, which makes it risky to expose externally.
+> We would need auth to be added before we can safely use it."
+> — david-gang, Issue #8969
+
+Without authentication, KueueViz is limited to `kubectl port-foward`.
+There is no way to safely expose it via Ingress, and no audit trail.
+
+### Goals
+
+1. Add optional authentication using Kubernetes TokenReview API
+2. Support bearer token via `Authorization: Bearer <token>` header
+3. Authenticate WebSocket connections at upgrade time
+4. Provide a login page with token input and validation flow
+5. Add metrics for authentication attempts
+6. Minimize manual steps for users when authenticating
+
+### Non-Goals
+
+1. Authorization (SubjectAccessReview), may be added in a future KEP
+2. Server-side sessions
+3. OAuth2/OIDC integration, users can deploy oauth2-proxy separately
+4. Token refresh, client resposibility
+
+## Proposal
+
+Add authentication middleware to KueueViz backend:
+
+1. Extract bearer token from request
+2. Validate via Kubernetes TokenReview API
+3. Return HTTP 401 for invalid or missing tokens
+4. Store user info in request context
+
+### User Stories
+
+**Cluster Admin**: Enable authentication via Helm so only users with valid
+Kubernetes tokens can access KueueViz.
+
+**Multi-Tenant Operator**: Expose KueueViz via Ingress to multiple teams,
+with per-user authentication and audit logging.
+
+**Developer**: Access KueueViz using existing Kubernetes credentials
+(ServiceAccount token or kubeconfig token).
+
+### Risks and Mitigations
+
+| Risk | Mitigation |
+|------|------------|
+| Breaking existing deployments | Disabled by default in Alpha; opt-in via `auth.mode: TokenReview` |
+| Token exposure in logs | Never log tokens |
+| TokenReview latency | Cache with configurable TTL |
+| API server load from invalid tokens | Negative caching (5s TTL) for failed auth results |
+| WebSocket complexity | Authenticate at connection time only |
+| Expired tokens on long connections | Document token lifetime |
+
+## Design Details
+
+### API Changes
+
+None. Changes limited to environment variables and Helm values.
+
+### Authentication Flow
+
+```text
+Client                    KueueViz Backend         K8s API Server
+  │                             │                        │
+  │ GET /ws/workloads           │                        │
+  │ Authorization: Bearer <t>   │                        │
+  │────────────────────────────>│                        │
+  │                             │ POST tokenreviews      │
+  │                             │───────────────────────>│
+  │                             │ authenticated: true    │
+  │                             │<───────────────────────│
+  │ 200 OK / WS Upgrade         │                        │
+  │<────────────────────────────│                        │
+```
+
+### Backend Implementation
+
+**New file:** `cmd/kueueviz/backend/middleware/auth.go`
+
+Gin middleware that extracts bearer token from `Authorization` header or
+`Sec-WebSocket-Protocol` subprotocol, validates via TokenReview API, and returns HTTP 401
+with `WWW-Authenticate: Bearer` on failure.
+
+Caching protects the API server from excessive TokenReview calls:
+- Successful auth results are cached with configurable TTL (default 60s).
+- Failed auth results are cached with a short TTL (default 5s) so the same
+  invalid token does not trigger repeated calls.
+- The API server's own API Priority and Fairness (`--max-requests-inflight`)
+  provides additional protection; application-layer caching is a second line.
+
+**Modified:** `cmd/kueueviz/backend/config/server.go`
+
+Add fields to `ServerConfig`:
+
+- `AuthMode string` — authentication mode; one of `Disabled` or `TokenReview`
+- `AuthTokenReviewAudiences []string` — optional token audiences to validate against; empty means any audience is accepted
+- `AuthTokenReviewCacheTTL time.Duration` — TTL for caching successful TokenReview results (default 60s)
+- `AuthTokenReviewNegativeCacheTTL time.Duration` — TTL for caching failed TokenReview results (default 5s)
+
+Load from environment variables:
+
+- `KUEUEVIZ_AUTH_MODE`
+- `KUEUEVIZ_AUTH_TOKEN_REVIEW_AUDIENCES`
+- `KUEUEVIZ_AUTH_TOKEN_REVIEW_CACHE_TTL`
+- `KUEUEVIZ_AUTH_TOKEN_REVIEW_NEGATIVE_CACHE_TTL`
+
+**Modified:** `cmd/kueueviz/backend/main.go`
+
+- Create `kubernetes.Clientset` for TokenReview calls
+- Register auth middleware when `AuthMode` is `TokenReview`
+
+### WebSocket Authentication
+
+Tokens can be provided via:
+
+1. `Authorization: Bearer <token>` header (preferred)
+2. `Sec-WebSocket-Protocol: kueueviz.auth.<base64-token>` subprotocol (browser fallback)
+
+Authentication happens at the HTTP upgrade request. No per-message auth.
+
+### Login Page
+
+The frontend provides a login page at `/login` with:
+
+1. A token input field
+2. A "Sign In" action that stores the token for subsequent requests
+3. Instructions for obtaining a token:
+
+```text
+kubectl create token <service-account> -n <namespace> --duration=168h
+```
+
+Authentication flow on the frontend:
+
+1. Access protected API endpoints with bearer token when available.
+2. On HTTP 401, clear token and redirect to `/login`.
+3. User provides a token and signs in.
+4. Retry protected requests with bearer token for HTTP APIs and WebSockets.
+5. On later HTTP 401, clear token and redirect back to `/login`.
+
+### RBAC Updates
+
+Add to `charts/kueue/templates/kueueviz/clusterrole.yaml`:
+
+```yaml
+- apiGroups: ["authentication.k8s.io"]
+  resources: ["tokenreviews"]
+  # TokenReview is a create-only API in Kubernetes (no get/list/watch/delete).
+  # The client POSTs a token and receives an authentication result.
+  verbs: ["create"]
+```
+
+### Helm Configuration
+
+`charts/kueue/values.yaml`:
+
+```yaml
+kueueViz:
+  backend:
+    auth:
+      mode: "Disabled"          # Disabled | TokenReview
+      tokenReviewConfig:
+        audiences: ""           # optional, comma-separated; empty means any audience
+        cacheTTL: "60s"         # how long a successful auth result is cached
+        negativeCacheTTL: "5s"  # how long a failed auth result is cached to prevent API server abuse
+```
+
+`charts/kueue/templates/kueueviz/backend-deployment.yaml`:
+
+```yaml
+{{- if eq .Values.kueueViz.backend.auth.mode "TokenReview" }}
+- name: KUEUEVIZ_AUTH_MODE
+  value: "TokenReview"
+{{- if .Values.kueueViz.backend.auth.tokenReviewConfig.audiences }}
+- name: KUEUEVIZ_AUTH_TOKEN_REVIEW_AUDIENCES
+  value: {{ .Values.kueueViz.backend.auth.tokenReviewConfig.audiences | quote }}
+{{- end }}
+- name: KUEUEVIZ_AUTH_TOKEN_REVIEW_CACHE_TTL
+  value: {{ .Values.kueueViz.backend.auth.tokenReviewConfig.cacheTTL | default "60s" | quote }}
+- name: KUEUEVIZ_AUTH_TOKEN_REVIEW_NEGATIVE_CACHE_TTL
+  value: {{ .Values.kueueViz.backend.auth.tokenReviewConfig.negativeCacheTTL | default "5s" | quote }}
+{{- end }}
+```
+
+### Frontend Considerations
+
+The frontend handles authentication with minimal user friction:
+
+1. When auth is enabled, the frontend detects HTTP 401 and redirects to `/login`.
+2. The login page provides a token input field and a helper command:
+   `kubectl create token <service-account> -n <namespace> --duration=168h`.
+3. Once entered, the token is stored in sessionStorage (scoped to the browser
+   tab, cleared on tab close) and attached to all subsequent HTTP and WebSocket
+   requests automatically.
+4. On token expiry (HTTP 401), the user is prompted to re-authenticate.
+
+Future: automatic token acquisition via `kubectl proxy` or browser-based
+OIDC, removing the need to copy-paste tokens.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+Unit tests (`middleware/auth_test.go`):
+
+- Token extraction from header and WebSocket subprotocol
+- Cache hit/miss
+- Expired cache entries
+- Disabled auth passthrough
+
+Coverage target: >80%
+
+Integration tests:
+
+- Valid token → 200
+- Invalid token → 401
+- Missing token → 401
+- Disabled auth → pass all
+
+E2E tests (`test/e2e/kueueviz/`):
+
+- Deploy with auth enabled
+- Test with ServiceAccount token
+- Test rejection of unauthenticated requests
+
+### Graduation Criteria
+
+Alpha (v0.17, disabled by default):
+
+- [ ] Feature gate `KueueVizAuth` added (default: false)
```

  Ok, so please drop from the KEP

### Review by [@samzong](https://github.com/samzong) (commented) — 2026-03-05T22:17:26Z

- **keps/5993-kueueviz-authentication/README.md:275** by [@samzong](https://github.com/samzong) — 2026-03-05T22:17:26Z
```diff
@@ -0,0 +1,328 @@
+# KEP-5993: KueueViz Authentication
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Authentication Flow](#authentication-flow)
+  - [Backend Implementation](#backend-implementation)
+  - [WebSocket Authentication](#websocket-authentication)
+  - [Login Page](#login-page)
+  - [RBAC Updates](#rbac-updates)
+  - [Helm Configuration](#helm-configuration)
+  - [Frontend Considerations](#frontend-considerations)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [OAuth2/OIDC Proxy](#oauth2oidc-proxy)
+  - [mTLS Client Certificates](#mtls-client-certificates)
+  - [Basic Authentication](#basic-authentication)
+<!-- /toc -->
+
+## Summary
+
+KueueViz currently exposes all Kueue resources without authentication.
+This KEP adds optional bearer token authentication using the Kubernetes
+TokenReview API, the same mechanism Kubernetes Dashboard uses.
+
+Authentication is controlled by a feature gate (`KueueVizAuth`), disabled
+by default in Alpha. Users opt in via Helm values (`auth.mode: TokenReview`).
+
+## Motivation
+
+From [Issue #5993](https://github.com/kubernetes-sigs/kueue/issues/5993):
+
+> "The current KueueViz has no authentication, which makes it risky to expose externally.
+> We would need auth to be added before we can safely use it."
+> — david-gang, Issue #8969
+
+Without authentication, KueueViz is limited to `kubectl port-foward`.
+There is no way to safely expose it via Ingress, and no audit trail.
+
+### Goals
+
+1. Add optional authentication using Kubernetes TokenReview API
+2. Support bearer token via `Authorization: Bearer <token>` header
+3. Authenticate WebSocket connections at upgrade time
+4. Provide a login page with token input and validation flow
+5. Add metrics for authentication attempts
+6. Minimize manual steps for users when authenticating
+
+### Non-Goals
+
+1. Authorization (SubjectAccessReview), may be added in a future KEP
+2. Server-side sessions
+3. OAuth2/OIDC integration, users can deploy oauth2-proxy separately
+4. Token refresh, client resposibility
+
+## Proposal
+
+Add authentication middleware to KueueViz backend:
+
+1. Extract bearer token from request
+2. Validate via Kubernetes TokenReview API
+3. Return HTTP 401 for invalid or missing tokens
+4. Store user info in request context
+
+### User Stories
+
+**Cluster Admin**: Enable authentication via Helm so only users with valid
+Kubernetes tokens can access KueueViz.
+
+**Multi-Tenant Operator**: Expose KueueViz via Ingress to multiple teams,
+with per-user authentication and audit logging.
+
+**Developer**: Access KueueViz using existing Kubernetes credentials
+(ServiceAccount token or kubeconfig token).
+
+### Risks and Mitigations
+
+| Risk | Mitigation |
+|------|------------|
+| Breaking existing deployments | Disabled by default in Alpha; opt-in via `auth.mode: TokenReview` |
+| Token exposure in logs | Never log tokens |
+| TokenReview latency | Cache with configurable TTL |
+| API server load from invalid tokens | Negative caching (5s TTL) for failed auth results |
+| WebSocket complexity | Authenticate at connection time only |
+| Expired tokens on long connections | Document token lifetime |
+
+## Design Details
+
+### API Changes
+
+None. Changes limited to environment variables and Helm values.
+
+### Authentication Flow
+
+```text
+Client                    KueueViz Backend         K8s API Server
+  │                             │                        │
+  │ GET /ws/workloads           │                        │
+  │ Authorization: Bearer <t>   │                        │
+  │────────────────────────────>│                        │
+  │                             │ POST tokenreviews      │
+  │                             │───────────────────────>│
+  │                             │ authenticated: true    │
+  │                             │<───────────────────────│
+  │ 200 OK / WS Upgrade         │                        │
+  │<────────────────────────────│                        │
+```
+
+### Backend Implementation
+
+**New file:** `cmd/kueueviz/backend/middleware/auth.go`
+
+Gin middleware that extracts bearer token from `Authorization` header or
+`Sec-WebSocket-Protocol` subprotocol, validates via TokenReview API, and returns HTTP 401
+with `WWW-Authenticate: Bearer` on failure.
+
+Caching protects the API server from excessive TokenReview calls:
+- Successful auth results are cached with configurable TTL (default 60s).
+- Failed auth results are cached with a short TTL (default 5s) so the same
+  invalid token does not trigger repeated calls.
+- The API server's own API Priority and Fairness (`--max-requests-inflight`)
+  provides additional protection; application-layer caching is a second line.
+
+**Modified:** `cmd/kueueviz/backend/config/server.go`
+
+Add fields to `ServerConfig`:
+
+- `AuthMode string` — authentication mode; one of `Disabled` or `TokenReview`
+- `AuthTokenReviewAudiences []string` — optional token audiences to validate against; empty means any audience is accepted
+- `AuthTokenReviewCacheTTL time.Duration` — TTL for caching successful TokenReview results (default 60s)
+- `AuthTokenReviewNegativeCacheTTL time.Duration` — TTL for caching failed TokenReview results (default 5s)
+
+Load from environment variables:
+
+- `KUEUEVIZ_AUTH_MODE`
+- `KUEUEVIZ_AUTH_TOKEN_REVIEW_AUDIENCES`
+- `KUEUEVIZ_AUTH_TOKEN_REVIEW_CACHE_TTL`
+- `KUEUEVIZ_AUTH_TOKEN_REVIEW_NEGATIVE_CACHE_TTL`
+
+**Modified:** `cmd/kueueviz/backend/main.go`
+
+- Create `kubernetes.Clientset` for TokenReview calls
+- Register auth middleware when `AuthMode` is `TokenReview`
+
+### WebSocket Authentication
+
+Tokens can be provided via:
+
+1. `Authorization: Bearer <token>` header (preferred)
+2. `Sec-WebSocket-Protocol: kueueviz.auth.<base64-token>` subprotocol (browser fallback)
+
+Authentication happens at the HTTP upgrade request. No per-message auth.
+
+### Login Page
+
+The frontend provides a login page at `/login` with:
+
+1. A token input field
+2. A "Sign In" action that stores the token for subsequent requests
+3. Instructions for obtaining a token:
+
+```text
+kubectl create token <service-account> -n <namespace> --duration=168h
+```
+
+Authentication flow on the frontend:
+
+1. Access protected API endpoints with bearer token when available.
+2. On HTTP 401, clear token and redirect to `/login`.
+3. User provides a token and signs in.
+4. Retry protected requests with bearer token for HTTP APIs and WebSockets.
+5. On later HTTP 401, clear token and redirect back to `/login`.
+
+### RBAC Updates
+
+Add to `charts/kueue/templates/kueueviz/clusterrole.yaml`:
+
+```yaml
+- apiGroups: ["authentication.k8s.io"]
+  resources: ["tokenreviews"]
+  # TokenReview is a create-only API in Kubernetes (no get/list/watch/delete).
+  # The client POSTs a token and receives an authentication result.
+  verbs: ["create"]
+```
+
+### Helm Configuration
+
+`charts/kueue/values.yaml`:
+
+```yaml
+kueueViz:
+  backend:
+    auth:
+      mode: "Disabled"          # Disabled | TokenReview
+      tokenReviewConfig:
+        audiences: ""           # optional, comma-separated; empty means any audience
+        cacheTTL: "60s"         # how long a successful auth result is cached
+        negativeCacheTTL: "5s"  # how long a failed auth result is cached to prevent API server abuse
+```
+
+`charts/kueue/templates/kueueviz/backend-deployment.yaml`:
+
+```yaml
+{{- if eq .Values.kueueViz.backend.auth.mode "TokenReview" }}
+- name: KUEUEVIZ_AUTH_MODE
+  value: "TokenReview"
+{{- if .Values.kueueViz.backend.auth.tokenReviewConfig.audiences }}
+- name: KUEUEVIZ_AUTH_TOKEN_REVIEW_AUDIENCES
+  value: {{ .Values.kueueViz.backend.auth.tokenReviewConfig.audiences | quote }}
+{{- end }}
+- name: KUEUEVIZ_AUTH_TOKEN_REVIEW_CACHE_TTL
+  value: {{ .Values.kueueViz.backend.auth.tokenReviewConfig.cacheTTL | default "60s" | quote }}
+- name: KUEUEVIZ_AUTH_TOKEN_REVIEW_NEGATIVE_CACHE_TTL
+  value: {{ .Values.kueueViz.backend.auth.tokenReviewConfig.negativeCacheTTL | default "5s" | quote }}
+{{- end }}
+```
+
+### Frontend Considerations
+
+The frontend handles authentication with minimal user friction:
+
+1. When auth is enabled, the frontend detects HTTP 401 and redirects to `/login`.
+2. The login page provides a token input field and a helper command:
+   `kubectl create token <service-account> -n <namespace> --duration=168h`.
+3. Once entered, the token is stored in sessionStorage (scoped to the browser
+   tab, cleared on tab close) and attached to all subsequent HTTP and WebSocket
+   requests automatically.
+4. On token expiry (HTTP 401), the user is prompted to re-authenticate.
+
+Future: automatic token acquisition via `kubectl proxy` or browser-based
+OIDC, removing the need to copy-paste tokens.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+Unit tests (`middleware/auth_test.go`):
+
+- Token extraction from header and WebSocket subprotocol
+- Cache hit/miss
+- Expired cache entries
+- Disabled auth passthrough
+
+Coverage target: >80%
+
+Integration tests:
+
+- Valid token → 200
+- Invalid token → 401
+- Missing token → 401
+- Disabled auth → pass all
+
+E2E tests (`test/e2e/kueueviz/`):
+
+- Deploy with auth enabled
+- Test with ServiceAccount token
+- Test rejection of unauthenticated requests
+
+### Graduation Criteria
+
+Alpha (v0.17, disabled by default):
+
+- [ ] Feature gate `KueueVizAuth` added (default: false)
```

  DONE.
