# PR #8997: Handle NoSchedule taint as an unhealthy node

**Summary**: Handle NoSchedule taint as an unhealthy node

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/8997

**Last updated**: 2026-03-19T13:45:05Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@j-skiba](https://github.com/j-skiba)
- **Branch**: `tas-taint-no-schedule-handling` → `main`
- **Created**: 2026-02-05T06:34:32Z
- **Updated**: 2026-03-19T13:45:05Z
- **Merged**: 2026-03-19T10:06:32Z
- **Closed**: 2026-03-19T10:06:33Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@gabesaba](https://github.com/gabesaba), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 4   **+458 / -21**

## Description

#### What type of PR is this?
/kind feature

#### What this PR does / why we need it:
This PR extends the `TASReplaceNodeOnNodeTaints ` feature gate to handle nodes tainted with `NoSchedule`.

This is a follow-up to #8964 which introduced handling for `NoExecute` taints.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #8828

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
```release-note
TAS: Extend the support for handling NoSchedule taints when the TASReplaceNodeOnNodeTaints feature gate is enabled.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-02-05T06:34:39Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 55bd5c85ef37ce5c0bfc325d2a623a2881f92ed7 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69bbbc82f1fc540008e6db86 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-05T06:34:43Z

Hi @j-skiba. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@j-skiba](https://github.com/j-skiba) — 2026-02-05T08:32:37Z

I'm also thinking about making the node_failure_controller act on pod change instead of polling but that can be added later wdyt?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-05T08:38:20Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-06T10:40:20Z

/hold
for kep update first, and the comment  https://github.com/kubernetes-sigs/kueue/pull/8997#discussion_r2773453046

### Comment by [@j-skiba](https://github.com/j-skiba) — 2026-02-09T14:27:42Z

/retitle Handle NoSchedule taint as an unhealthy node

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-09T14:27:51Z

@j-skiba: Re-titling can only be requested by trusted users, like repository collaborators.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8997#issuecomment-3872051558):

>/retitle Handle NoSchedule taint as an unhealthy node


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@j-skiba](https://github.com/j-skiba) — 2026-02-25T09:54:27Z

/test pull-kueue-test-unit-main

it seems to be unrelated
```
=== Failed
=== FAIL: pkg/controller/admissionchecks/multikueue  (0.00s)
FAIL
coverage: 5.0% of statements in ./...
FAIL	sigs.k8s.io/kueue/pkg/controller/admissionchecks/multikueue	97.317s
DONE 12919 tests, 9 skipped, 1 failure in 546.874s
make: *** [Makefile-test.mk:77: test] Error 1
```

### Comment by [@j-skiba](https://github.com/j-skiba) — 2026-02-26T09:00:29Z

/test pull-kueue-populator-test-integration-main
503 error
```bash
unable to download requested version: unable fetch envtest-v1.35.0-linux-amd64.tar.gz (https://github.com/kubernetes-sigs/controller-tools/releases/download/envtest-v1.35.0/envtest-v1.35.0-linux-amd64.tar.gz) -- got status "503 Service Unavailable"
```

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-19T09:57:32Z

> /hold for kep update first, and the comment [#8997 (comment)](https://github.com/kubernetes-sigs/kueue/pull/8997#discussion_r2773453046)

The KEP is already updated
/unhold

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-19T10:04:41Z

LGTM label has been added.  <details>Git tree hash: 1299792c7bb15b481f0cf709660657058b7e95eb</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-19T10:04:43Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/8997#" title="Author self-approved">j-skiba</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/8997#pullrequestreview-3974023396" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-19T10:15:35Z

/cherrypick release-0.16
/cherrypick release-0.15

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-19T10:16:15Z

@mimowo: new pull request created: #10002

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8997#issuecomment-4089089289):

>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-19T10:16:52Z

@mimowo: new pull request created: #10003

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/8997#issuecomment-4089089289):

>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-19T12:28:47Z

@j-skiba please address @pajakd comments. They don't seem blockers for the patch releases, but nice cleanups.

### Comment by [@j-skiba](https://github.com/j-skiba) — 2026-03-19T13:03:49Z

@mimowo @pajakd follow up is here https://github.com/kubernetes-sigs/kueue/pull/10012

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-19T13:08:33Z

/release-note-edit
```release-note
TAS NodeHotSwap: Extend the support for handling NoSchedule taints when the TASReplaceNodeOnNodeTaints feature gate is enabled.
```

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-19T13:45:04Z

/release-note-edit
```release-note
TAS: Extend the support for handling NoSchedule taints when the TASReplaceNodeOnNodeTaints feature gate is enabled.
```

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T10:38:26Z

- **pkg/controller/tas/node_failure_controller.go:348** by [@mimowo](https://github.com/mimowo) — 2026-02-06T10:38:26Z
```diff
@@ -330,22 +345,176 @@ func (r *nodeFailureReconciler) handleHealthyNode(ctx context.Context, nodeName
 			continue
 		}
 
-		if !slices.Contains(wl.Status.UnhealthyNodes, kueue.UnhealthyNode{Name: nodeName}) {
+		isUnhealthy, podsToDelete, needsMonitoring, err := r.isNodeUnhealthyForWorkload(ctx, node, &wl)
```

  I'm not yet sure we need to do this - would need to verify, but certainly we should not delete the Pods.
  
  We can consider marking them as finished with the dedicated Pod condition like:
  ```
  type: TerminatedByKueue
  status: True
  reason: UnschedulableDueToUntoleratedNoScheduleTaint
  ```
  This will effectively make them re-created such as deletion, but will be well better for debuggabiiity.
  
  let's discuss the need and the details in the KEP update.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T11:09:28Z

- **pkg/controller/tas/node_failure_controller.go:348** by [@mimowo](https://github.com/mimowo) — 2026-02-06T11:09:27Z
```diff
@@ -330,22 +345,176 @@ func (r *nodeFailureReconciler) handleHealthyNode(ctx context.Context, nodeName
 			continue
 		}
 
-		if !slices.Contains(wl.Status.UnhealthyNodes, kueue.UnhealthyNode{Name: nodeName}) {
+		isUnhealthy, podsToDelete, needsMonitoring, err := r.isNodeUnhealthyForWorkload(ctx, node, &wl)
```

  Ok, so in my underestanding PodDeletion or termination should not be necessary, because we already handle that by TASReplaceNodeOnPodTermination. 
  
  Look at this code: https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/tas/node_failure_controller.go#L219-L243
  
  Here, if we have Pending Pods which aren't scheduled yet, then `allPodsTerminate` will be true , because such pending Pods don't have nodeName set, and thus the workload will be considered for node replacement.
  
  I might be missing something, I'm happy to sync, but it requires a better justification why we are changing that.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T11:09:46Z

- **pkg/controller/tas/node_failure_controller.go:348** by [@mimowo](https://github.com/mimowo) — 2026-02-06T11:09:46Z
```diff
@@ -330,22 +345,176 @@ func (r *nodeFailureReconciler) handleHealthyNode(ctx context.Context, nodeName
 			continue
 		}
 
-		if !slices.Contains(wl.Status.UnhealthyNodes, kueue.UnhealthyNode{Name: nodeName}) {
+		isUnhealthy, podsToDelete, needsMonitoring, err := r.isNodeUnhealthyForWorkload(ctx, node, &wl)
```

  cc @pajakd @PBundyra who also worked with this code closely

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-02-06T11:17:04Z

- **pkg/controller/tas/node_failure_controller.go:348** by [@j-skiba](https://github.com/j-skiba) — 2026-02-06T11:17:03Z
```diff
@@ -330,22 +345,176 @@ func (r *nodeFailureReconciler) handleHealthyNode(ctx context.Context, nodeName
 			continue
 		}
 
-		if !slices.Contains(wl.Status.UnhealthyNodes, kueue.UnhealthyNode{Name: nodeName}) {
+		isUnhealthy, podsToDelete, needsMonitoring, err := r.isNodeUnhealthyForWorkload(ctx, node, &wl)
```

  > This will effectively make them re-created such as deletion, but will be well better for debuggabiiity.
  > let's discuss the need and the details in the KEP update.
  
  I updated the KEP with Failed+Condition approach
  
  > Here, if we have Pending Pods which aren't scheduled yet, then allPodsTerminate will be true , because such pending Pods don't have nodeName set, and thus the workload will be considered for node replacement.
  
  yes, the node replacement will be triggered and new topology assignment will be calculated too but the pending pods will be still there and they will be blocking creation of new pods that should replace the pending ones.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T11:22:02Z

- **pkg/controller/tas/node_failure_controller.go:348** by [@mimowo](https://github.com/mimowo) — 2026-02-06T11:22:01Z
```diff
@@ -330,22 +345,176 @@ func (r *nodeFailureReconciler) handleHealthyNode(ctx context.Context, nodeName
 			continue
 		}
 
-		if !slices.Contains(wl.Status.UnhealthyNodes, kueue.UnhealthyNode{Name: nodeName}) {
+		isUnhealthy, podsToDelete, needsMonitoring, err := r.isNodeUnhealthyForWorkload(ctx, node, &wl)
```

  Oh I see, thank you for explaining! IIUC this is only needed in case the replacement is actually needed - only one node to be replaced? Otherwise we will evict the entire workload, and so the Job will delete all the Pods. So this is only needed if exactly one node has the NoSchedule taint, right?

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-02-06T11:24:36Z

- **pkg/controller/tas/node_failure_controller.go:348** by [@j-skiba](https://github.com/j-skiba) — 2026-02-06T11:24:36Z
```diff
@@ -330,22 +345,176 @@ func (r *nodeFailureReconciler) handleHealthyNode(ctx context.Context, nodeName
 			continue
 		}
 
-		if !slices.Contains(wl.Status.UnhealthyNodes, kueue.UnhealthyNode{Name: nodeName}) {
+		isUnhealthy, podsToDelete, needsMonitoring, err := r.isNodeUnhealthyForWorkload(ctx, node, &wl)
```

  That's correct, it's just for a single node

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-02-06T11:25:52Z

- **pkg/controller/tas/node_failure_controller.go:348** by [@j-skiba](https://github.com/j-skiba) — 2026-02-06T11:25:52Z
```diff
@@ -330,22 +345,176 @@ func (r *nodeFailureReconciler) handleHealthyNode(ctx context.Context, nodeName
 			continue
 		}
 
-		if !slices.Contains(wl.Status.UnhealthyNodes, kueue.UnhealthyNode{Name: nodeName}) {
+		isUnhealthy, podsToDelete, needsMonitoring, err := r.isNodeUnhealthyForWorkload(ctx, node, &wl)
```

  if there is more than one unhealthy node then we evict the workload

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T11:27:48Z

- **pkg/controller/tas/node_failure_controller.go:348** by [@mimowo](https://github.com/mimowo) — 2026-02-06T11:27:48Z
```diff
@@ -330,22 +345,176 @@ func (r *nodeFailureReconciler) handleHealthyNode(ctx context.Context, nodeName
 			continue
 		}
 
-		if !slices.Contains(wl.Status.UnhealthyNodes, kueue.UnhealthyNode{Name: nodeName}) {
+		isUnhealthy, podsToDelete, needsMonitoring, err := r.isNodeUnhealthyForWorkload(ctx, node, &wl)
```

  Ok, this makes all sense to me, please make sure that is put in KEP. I will try to get there today, but it sounds quite reasonable

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-25T11:38:59Z

cc @pajakd ptal

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-02-25T14:01:32Z

- **pkg/controller/tas/node_failure_controller.go:586** by [@pajakd](https://github.com/pajakd) — 2026-02-25T14:01:32Z
```diff
@@ -453,13 +516,79 @@ func (r *nodeFailureReconciler) addUnhealthyNode(ctx context.Context, wl *kueue.
 	return nil
 }
 
+// isPodAssignedToNode checks if the pod is assigned to the node, either by being scheduled there
+// (spec.nodeName) or by having a TAS assignment that maps to this node (via nodeSelector).
+func isPodAssignedToNode(pod *corev1.Pod, node *corev1.Node, levels []string) bool {
+	if pod.Spec.NodeName != "" {
+		return pod.Spec.NodeName == node.Name
+	}
+	if len(pod.Spec.NodeSelector) == 0 || len(levels) == 0 {
+		return false
+	}
+	key := levels[len(levels)-1]
+	val, ok := pod.Spec.NodeSelector[key]
+	if !ok {
+		return false
+	}
+	if nodeVal, ok := node.Labels[key]; !ok || nodeVal != val {
+		return false
+	}
+	return true
+}
+
+func getTopologyAssignmentLevelsForPod(pod *corev1.Pod, wl *kueue.Workload, psaMap map[kueue.PodSetReference]*kueue.PodSetAssignment) []string {
+	psName := kueue.PodSetReference(pod.Labels[constants.PodSetLabel])
+	psa, found := psaMap[psName]
+
+	// Fallback to the first assignment if the label is missing (common for simple/plain Pods)
+	if !found && len(wl.Status.Admission.PodSetAssignments) == 1 {
+		psa = &wl.Status.Admission.PodSetAssignments[0]
+		found = true
+	}
+
+	if found && psa.TopologyAssignment != nil {
+		return psa.TopologyAssignment.Levels
+	}
+
+	return nil
+}
+
+// getPodsAssignedToNode returns a list of pods that are assigned to the node (via spec.nodeName)
+// or are assigned by topology assignment but are not scheduled to the node (e.g. because of a taint).
+// It also returns a boolean indicating if there are any pods assigned to the Workload's topology
+// that are still gated by TopologySchedulingGate (missing nodeSelector).
+func (r *nodeFailureReconciler) getPodsAssignedToNode(ctx context.Context, wl *kueue.Workload, node *corev1.Node) ([]*corev1.Pod, bool, error) {
+	podsForWl, err := ListPodsForWorkloadSlice(ctx, r.client, wl.Namespace, workloadslicing.SliceName(wl))
+	if err != nil {
+		return nil, false, fmt.Errorf("list pods: %w", err)
+	}
+
+	psaMap := make(map[kueue.PodSetReference]*kueue.PodSetAssignment)
+	for i := range wl.Status.Admission.PodSetAssignments {
+		psaMap[wl.Status.Admission.PodSetAssignments[i].Name] = &wl.Status.Admission.PodSetAssignments[i]
+	}
+
+	var podsAssignedToNode []*corev1.Pod
+	hasGatedPods := false
+	for _, pod := range podsForWl {
+		levels := getTopologyAssignmentLevelsForPod(pod, wl, psaMap)
+		if isPodAssignedToNode(pod, node, levels) {
+			podsAssignedToNode = append(podsAssignedToNode, pod)
+		}
+		if utilpod.HasGate(pod, kueue.TopologySchedulingGate) {
+			hasGatedPods = true
+		}
+	}
+	return podsAssignedToNode, hasGatedPods, nil
+}
+
 func checkTaintTolerations(logger logr.Logger, taint *corev1.Taint, podSets []kueue.PodSet) taintToleration {
-	isUntolerated := true
+	isTolerated := true
```

  This is just rename? So, previously the variable name was misleading?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-02-25T14:04:50Z

- **pkg/controller/tas/node_failure_controller.go:489** by [@pajakd](https://github.com/pajakd) — 2026-02-25T14:04:50Z
```diff
@@ -431,6 +450,50 @@ func (r *nodeFailureReconciler) handleHealthyNode(ctx context.Context, nodeName
 	return nil
 }
 
+func classifyPodsForReplacement(pods []*corev1.Pod) (podsToTerminate []*corev1.Pod, hasPodsToRetain bool) {
+	for _, pod := range pods {
+		if !pod.DeletionTimestamp.IsZero() || utilpod.IsTerminated(pod) {
+			continue
+		}
+		if pod.Status.Phase == corev1.PodPending && !utilpod.HasGate(pod, kueue.TopologySchedulingGate) {
+			podsToTerminate = append(podsToTerminate, pod)
+		} else {
+			hasPodsToRetain = true
+		}
+	}
+	return
+}
+
+func (r *nodeFailureReconciler) terminatePods(ctx context.Context, pods []*corev1.Pod) {
+	log := ctrl.LoggerFrom(ctx)
+	for _, p := range pods {
+		pod := r.makePodStatusPatch(p)
+		//nolint:staticcheck //SA1019: client.Apply is deprecated
+		if err := r.client.Status().Patch(ctx, pod, client.Apply, client.FieldOwner(TASNodeFailureController), client.ForceOwnership); err != nil {
+			if !apierrors.IsNotFound(err) {
+				log.Error(err, "Failed to patch pending pod", "pod", klog.KObj(p))
+			}
+		} else {
+			r.recorder.Eventf(pod, corev1.EventTypeNormal, "PodTerminatedByKueue", "Pod terminated by Kueue NodeFailureController due to node taint")
+		}
+	}
+}
+
+func (r *nodeFailureReconciler) makePodStatusPatch(p *corev1.Pod) *corev1.Pod {
+	pod := utilpod.BaseSSAPod(p)
+	pod.Status.Phase = corev1.PodFailed
+	pod.Status.Conditions = []corev1.PodCondition{
+		{
+			Type:               "TerminatedByKueue",
+			Status:             corev1.ConditionTrue,
+			Reason:             "UnschedulableOnAssignedNode",
```

  Lets declare it as const somewhere (can be in this file).

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-02-25T14:12:59Z

- **pkg/controller/tas/node_failure_controller.go:528** by [@pajakd](https://github.com/pajakd) — 2026-02-25T14:12:59Z
```diff
@@ -453,13 +516,79 @@ func (r *nodeFailureReconciler) addUnhealthyNode(ctx context.Context, wl *kueue.
 	return nil
 }
 
+// isPodAssignedToNode checks if the pod is assigned to the node, either by being scheduled there
+// (spec.nodeName) or by having a TAS assignment that maps to this node (via nodeSelector).
+func isPodAssignedToNode(pod *corev1.Pod, node *corev1.Node, levels []string) bool {
+	if pod.Spec.NodeName != "" {
+		return pod.Spec.NodeName == node.Name
+	}
+	if len(pod.Spec.NodeSelector) == 0 || len(levels) == 0 {
+		return false
+	}
+	key := levels[len(levels)-1]
```

  is it sufficient to check only the lowest level? we could perhaps have same values of eq `cloud.provider.com/topology-rack` in two different `cloud.provider.com/topology-block`
  
  it works if the lowest level is `kubernetes.io/hostname` but this function wants to work in general case, right?

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-02-26T08:25:17Z

- **pkg/controller/tas/node_failure_controller.go:586** by [@j-skiba](https://github.com/j-skiba) — 2026-02-26T08:25:17Z
```diff
@@ -453,13 +516,79 @@ func (r *nodeFailureReconciler) addUnhealthyNode(ctx context.Context, wl *kueue.
 	return nil
 }
 
+// isPodAssignedToNode checks if the pod is assigned to the node, either by being scheduled there
+// (spec.nodeName) or by having a TAS assignment that maps to this node (via nodeSelector).
+func isPodAssignedToNode(pod *corev1.Pod, node *corev1.Node, levels []string) bool {
+	if pod.Spec.NodeName != "" {
+		return pod.Spec.NodeName == node.Name
+	}
+	if len(pod.Spec.NodeSelector) == 0 || len(levels) == 0 {
+		return false
+	}
+	key := levels[len(levels)-1]
+	val, ok := pod.Spec.NodeSelector[key]
+	if !ok {
+		return false
+	}
+	if nodeVal, ok := node.Labels[key]; !ok || nodeVal != val {
+		return false
+	}
+	return true
+}
+
+func getTopologyAssignmentLevelsForPod(pod *corev1.Pod, wl *kueue.Workload, psaMap map[kueue.PodSetReference]*kueue.PodSetAssignment) []string {
+	psName := kueue.PodSetReference(pod.Labels[constants.PodSetLabel])
+	psa, found := psaMap[psName]
+
+	// Fallback to the first assignment if the label is missing (common for simple/plain Pods)
+	if !found && len(wl.Status.Admission.PodSetAssignments) == 1 {
+		psa = &wl.Status.Admission.PodSetAssignments[0]
+		found = true
+	}
+
+	if found && psa.TopologyAssignment != nil {
+		return psa.TopologyAssignment.Levels
+	}
+
+	return nil
+}
+
+// getPodsAssignedToNode returns a list of pods that are assigned to the node (via spec.nodeName)
+// or are assigned by topology assignment but are not scheduled to the node (e.g. because of a taint).
+// It also returns a boolean indicating if there are any pods assigned to the Workload's topology
+// that are still gated by TopologySchedulingGate (missing nodeSelector).
+func (r *nodeFailureReconciler) getPodsAssignedToNode(ctx context.Context, wl *kueue.Workload, node *corev1.Node) ([]*corev1.Pod, bool, error) {
+	podsForWl, err := ListPodsForWorkloadSlice(ctx, r.client, wl.Namespace, workloadslicing.SliceName(wl))
+	if err != nil {
+		return nil, false, fmt.Errorf("list pods: %w", err)
+	}
+
+	psaMap := make(map[kueue.PodSetReference]*kueue.PodSetAssignment)
+	for i := range wl.Status.Admission.PodSetAssignments {
+		psaMap[wl.Status.Admission.PodSetAssignments[i].Name] = &wl.Status.Admission.PodSetAssignments[i]
+	}
+
+	var podsAssignedToNode []*corev1.Pod
+	hasGatedPods := false
+	for _, pod := range podsForWl {
+		levels := getTopologyAssignmentLevelsForPod(pod, wl, psaMap)
+		if isPodAssignedToNode(pod, node, levels) {
+			podsAssignedToNode = append(podsAssignedToNode, pod)
+		}
+		if utilpod.HasGate(pod, kueue.TopologySchedulingGate) {
+			hasGatedPods = true
+		}
+	}
+	return podsAssignedToNode, hasGatedPods, nil
+}
+
 func checkTaintTolerations(logger logr.Logger, taint *corev1.Taint, podSets []kueue.PodSet) taintToleration {
-	isUntolerated := true
+	isTolerated := true
```

  yes, it's just a rename. We check
  ```go
  if !corev1helpers.TolerationsTolerateTaint(logger, ps.Template.Spec.Tolerations, taint, false) {
  	isTolerated = false
  	break
  }
  ```
  if tolerations do not tolerate taint so I changed the name to be more accurate

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-26T08:30:07Z

- **pkg/controller/tas/node_failure_controller.go:528** by [@mimowo](https://github.com/mimowo) — 2026-02-26T08:30:07Z
```diff
@@ -453,13 +516,79 @@ func (r *nodeFailureReconciler) addUnhealthyNode(ctx context.Context, wl *kueue.
 	return nil
 }
 
+// isPodAssignedToNode checks if the pod is assigned to the node, either by being scheduled there
+// (spec.nodeName) or by having a TAS assignment that maps to this node (via nodeSelector).
+func isPodAssignedToNode(pod *corev1.Pod, node *corev1.Node, levels []string) bool {
+	if pod.Spec.NodeName != "" {
+		return pod.Spec.NodeName == node.Name
+	}
+	if len(pod.Spec.NodeSelector) == 0 || len(levels) == 0 {
+		return false
+	}
+	key := levels[len(levels)-1]
```

  Hmm, good point. I think the NodeHotSwap should only work when `kubernetes.io/hostname` is the lowest level. Otherwise it is really tricky, certainly out of scope for the PR / KEP update. 
  
  Please make sure it does not panic in such cases (when hostname is not the lowest level).

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-02-26T08:55:16Z

- **pkg/controller/tas/node_failure_controller.go:489** by [@j-skiba](https://github.com/j-skiba) — 2026-02-26T08:55:16Z
```diff
@@ -431,6 +450,50 @@ func (r *nodeFailureReconciler) handleHealthyNode(ctx context.Context, nodeName
 	return nil
 }
 
+func classifyPodsForReplacement(pods []*corev1.Pod) (podsToTerminate []*corev1.Pod, hasPodsToRetain bool) {
+	for _, pod := range pods {
+		if !pod.DeletionTimestamp.IsZero() || utilpod.IsTerminated(pod) {
+			continue
+		}
+		if pod.Status.Phase == corev1.PodPending && !utilpod.HasGate(pod, kueue.TopologySchedulingGate) {
+			podsToTerminate = append(podsToTerminate, pod)
+		} else {
+			hasPodsToRetain = true
+		}
+	}
+	return
+}
+
+func (r *nodeFailureReconciler) terminatePods(ctx context.Context, pods []*corev1.Pod) {
+	log := ctrl.LoggerFrom(ctx)
+	for _, p := range pods {
+		pod := r.makePodStatusPatch(p)
+		//nolint:staticcheck //SA1019: client.Apply is deprecated
+		if err := r.client.Status().Patch(ctx, pod, client.Apply, client.FieldOwner(TASNodeFailureController), client.ForceOwnership); err != nil {
+			if !apierrors.IsNotFound(err) {
+				log.Error(err, "Failed to patch pending pod", "pod", klog.KObj(p))
+			}
+		} else {
+			r.recorder.Eventf(pod, corev1.EventTypeNormal, "PodTerminatedByKueue", "Pod terminated by Kueue NodeFailureController due to node taint")
+		}
+	}
+}
+
+func (r *nodeFailureReconciler) makePodStatusPatch(p *corev1.Pod) *corev1.Pod {
+	pod := utilpod.BaseSSAPod(p)
+	pod.Status.Phase = corev1.PodFailed
+	pod.Status.Conditions = []corev1.PodCondition{
+		{
+			Type:               "TerminatedByKueue",
+			Status:             corev1.ConditionTrue,
+			Reason:             "UnschedulableOnAssignedNode",
```

  done

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-02-26T08:58:50Z

- **pkg/controller/tas/node_failure_controller.go:528** by [@j-skiba](https://github.com/j-skiba) — 2026-02-26T08:58:50Z
```diff
@@ -453,13 +516,79 @@ func (r *nodeFailureReconciler) addUnhealthyNode(ctx context.Context, wl *kueue.
 	return nil
 }
 
+// isPodAssignedToNode checks if the pod is assigned to the node, either by being scheduled there
+// (spec.nodeName) or by having a TAS assignment that maps to this node (via nodeSelector).
+func isPodAssignedToNode(pod *corev1.Pod, node *corev1.Node, levels []string) bool {
+	if pod.Spec.NodeName != "" {
+		return pod.Spec.NodeName == node.Name
+	}
+	if len(pod.Spec.NodeSelector) == 0 || len(levels) == 0 {
+		return false
+	}
+	key := levels[len(levels)-1]
```

  I added a few test cases for `isPodAssignedToNode`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-26T09:20:44Z

- **pkg/controller/tas/node_failure_controller.go:480** by [@mimowo](https://github.com/mimowo) — 2026-02-26T09:20:44Z
```diff
@@ -431,6 +455,50 @@ func (r *nodeFailureReconciler) handleHealthyNode(ctx context.Context, nodeName
 	return nil
 }
 
+func classifyPodsForReplacement(pods []*corev1.Pod) (podsToTerminate []*corev1.Pod, hasPodsToRetain bool) {
+	for _, pod := range pods {
+		if !pod.DeletionTimestamp.IsZero() || utilpod.IsTerminated(pod) {
+			continue
+		}
+		if pod.Status.Phase == corev1.PodPending && !utilpod.HasGate(pod, kueue.TopologySchedulingGate) {
+			podsToTerminate = append(podsToTerminate, pod)
+		} else {
+			hasPodsToRetain = true
+		}
+	}
+	return
+}
+
+func (r *nodeFailureReconciler) terminatePods(ctx context.Context, pods []*corev1.Pod) {
+	log := ctrl.LoggerFrom(ctx)
+	for _, p := range pods {
+		pod := r.makePodStatusPatch(p)
+		//nolint:staticcheck //SA1019: client.Apply is deprecated
+		if err := r.client.Status().Patch(ctx, pod, client.Apply, client.FieldOwner(TASNodeFailureController), client.ForceOwnership); err != nil {
+			if !apierrors.IsNotFound(err) {
+				log.Error(err, "Failed to patch pending pod", "pod", klog.KObj(p))
+			}
```

  We should retry on errors

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-26T09:47:25Z

- **pkg/controller/tas/node_failure_controller.go:475** by [@mimowo](https://github.com/mimowo) — 2026-02-26T09:47:25Z
```diff
@@ -431,6 +455,50 @@ func (r *nodeFailureReconciler) handleHealthyNode(ctx context.Context, nodeName
 	return nil
 }
 
+func classifyPodsForReplacement(pods []*corev1.Pod) (podsToTerminate []*corev1.Pod, hasPodsToRetain bool) {
+	for _, pod := range pods {
+		if !pod.DeletionTimestamp.IsZero() || utilpod.IsTerminated(pod) {
+			continue
+		}
+		if pod.Status.Phase == corev1.PodPending && !utilpod.HasGate(pod, kueue.TopologySchedulingGate) {
+			podsToTerminate = append(podsToTerminate, pod)
+		} else {
+			hasPodsToRetain = true
+		}
+	}
+	return
+}
+
+func (r *nodeFailureReconciler) terminatePods(ctx context.Context, pods []*corev1.Pod) {
+	log := ctrl.LoggerFrom(ctx)
+	for _, p := range pods {
+		pod := r.makePodStatusPatch(p)
```

  Why using SSA here? I would prefer to make it consistent with how the FailureRecovery controller is doing that (adding conditio and changing phase). We could commonize in follow ups.

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-02-26T09:59:52Z

- **pkg/controller/tas/node_failure_controller.go:480** by [@j-skiba](https://github.com/j-skiba) — 2026-02-26T09:59:52Z
```diff
@@ -431,6 +455,50 @@ func (r *nodeFailureReconciler) handleHealthyNode(ctx context.Context, nodeName
 	return nil
 }
 
+func classifyPodsForReplacement(pods []*corev1.Pod) (podsToTerminate []*corev1.Pod, hasPodsToRetain bool) {
+	for _, pod := range pods {
+		if !pod.DeletionTimestamp.IsZero() || utilpod.IsTerminated(pod) {
+			continue
+		}
+		if pod.Status.Phase == corev1.PodPending && !utilpod.HasGate(pod, kueue.TopologySchedulingGate) {
+			podsToTerminate = append(podsToTerminate, pod)
+		} else {
+			hasPodsToRetain = true
+		}
+	}
+	return
+}
+
+func (r *nodeFailureReconciler) terminatePods(ctx context.Context, pods []*corev1.Pod) {
+	log := ctrl.LoggerFrom(ctx)
+	for _, p := range pods {
+		pod := r.makePodStatusPatch(p)
+		//nolint:staticcheck //SA1019: client.Apply is deprecated
+		if err := r.client.Status().Patch(ctx, pod, client.Apply, client.FieldOwner(TASNodeFailureController), client.ForceOwnership); err != nil {
+			if !apierrors.IsNotFound(err) {
+				log.Error(err, "Failed to patch pending pod", "pod", klog.KObj(p))
+			}
```

  done

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-02-26T10:06:23Z

- **pkg/controller/tas/node_failure_controller.go:475** by [@j-skiba](https://github.com/j-skiba) — 2026-02-26T10:06:23Z
```diff
@@ -431,6 +455,50 @@ func (r *nodeFailureReconciler) handleHealthyNode(ctx context.Context, nodeName
 	return nil
 }
 
+func classifyPodsForReplacement(pods []*corev1.Pod) (podsToTerminate []*corev1.Pod, hasPodsToRetain bool) {
+	for _, pod := range pods {
+		if !pod.DeletionTimestamp.IsZero() || utilpod.IsTerminated(pod) {
+			continue
+		}
+		if pod.Status.Phase == corev1.PodPending && !utilpod.HasGate(pod, kueue.TopologySchedulingGate) {
+			podsToTerminate = append(podsToTerminate, pod)
+		} else {
+			hasPodsToRetain = true
+		}
+	}
+	return
+}
+
+func (r *nodeFailureReconciler) terminatePods(ctx context.Context, pods []*corev1.Pod) {
+	log := ctrl.LoggerFrom(ctx)
+	for _, p := range pods {
+		pod := r.makePodStatusPatch(p)
```

  👍  added `utilclient.PatchStatus`, it limits other necessary code additions

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-27T09:46:50Z

- **pkg/controller/tas/node_failure_controller.go:263** by [@mimowo](https://github.com/mimowo) — 2026-02-27T09:46:50Z
```diff
@@ -234,63 +249,69 @@ func hasTASAssignmentOnNode(wl *kueue.Workload, nodeName string) bool {
 	return false
 }
 
-func (r *nodeFailureReconciler) getWorkloadStatus(ctx context.Context, nodeName string, node *corev1.Node, wlKey types.NamespacedName, wl *kueue.Workload) (workloadStatus, error) {
+func (r *nodeFailureReconciler) checkWorkloadHealthOnNode(ctx context.Context, nodeName string, node *corev1.Node, wlKey types.NamespacedName, wl *kueue.Workload) (workloadHealthCheck, error) {
 	if err := r.client.Get(ctx, wlKey, wl); err != nil {
 		if apierrors.IsNotFound(err) {
-			return workloadHealthy, nil
+			return workloadHealthCheck{status: workloadHealthy}, nil
 		}
-		return workloadHealthy, err
+		return workloadHealthCheck{}, err
 	}
 
-	shouldWait := false
 	ready := utiltas.IsNodeStatusConditionTrue(node.Status.Conditions, corev1.NodeReady)
 
+	var untolerated, temporarilyTolerated []corev1.Taint
+	shouldCheckPods := false
```

  I'm not sure we need that at all. If `if len(untolerated) == 0 && len(temporarilyTolerated) == 0 {` then IIUC we could just short-circuit

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-27T09:53:14Z

- **charts/kueue/templates/rbac/role.yaml:70** by [@mimowo](https://github.com/mimowo) — 2026-02-27T09:53:14Z
```diff
@@ -67,6 +67,7 @@ rules:
     verbs:
       - get
       - patch
+      - update
```

  I think we can skip it now as we moved to Patch rather than SSA

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-27T09:54:28Z

- **pkg/controller/tas/node_failure_controller.go:182** by [@mimowo](https://github.com/mimowo) — 2026-02-27T09:54:29Z
```diff
@@ -164,7 +178,8 @@ func (r *nodeFailureReconciler) Delete(e event.TypedDeleteEvent[*corev1.Node]) b
 }
 
 //+kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
-//+kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+//+kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update
+//+kubebuilder:rbac:groups="",resources=pods/status,verbs=patch;update
```

  This can be also dropped now AFAIK

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-27T11:28:23Z

- **pkg/controller/tas/node_failure_controller.go:295** by [@mimowo](https://github.com/mimowo) — 2026-02-27T11:28:23Z
```diff
@@ -234,63 +249,69 @@ func hasTASAssignmentOnNode(wl *kueue.Workload, nodeName string) bool {
 	return false
 }
 
-func (r *nodeFailureReconciler) getWorkloadStatus(ctx context.Context, nodeName string, node *corev1.Node, wlKey types.NamespacedName, wl *kueue.Workload) (workloadStatus, error) {
+func (r *nodeFailureReconciler) checkWorkloadHealthOnNode(ctx context.Context, nodeName string, node *corev1.Node, wlKey types.NamespacedName, wl *kueue.Workload) (workloadHealthCheck, error) {
 	if err := r.client.Get(ctx, wlKey, wl); err != nil {
 		if apierrors.IsNotFound(err) {
-			return workloadHealthy, nil
+			return workloadHealthCheck{status: workloadHealthy}, nil
 		}
-		return workloadHealthy, err
+		return workloadHealthCheck{}, err
 	}
 
-	shouldWait := false
 	ready := utiltas.IsNodeStatusConditionTrue(node.Status.Conditions, corev1.NodeReady)
 
+	var untolerated, temporarilyTolerated []corev1.Taint
+	shouldCheckPods := false
+
 	if ready && features.Enabled(features.TASReplaceNodeOnNodeTaints) {
 		// Ready node, check for taints
-		untolerated, temporarilyTolerated := classifyNoExecuteTaints(ctx, node.Spec.Taints, workload.PodSetsOnNode(wl, nodeName))
-		switch {
-		case len(untolerated) > 0:
-			if !features.Enabled(features.TASReplaceNodeOnPodTermination) {
-				return workloadUnhealthy, nil
-			}
-			shouldWait = true
-		case len(temporarilyTolerated) > 0:
-			shouldWait = true
-		default:
-			return workloadHealthy, nil
+		untolerated, temporarilyTolerated = classifyTaints(ctx, node.Spec.Taints, workload.PodSetsOnNode(wl, nodeName))
+		if len(untolerated) > 0 || len(temporarilyTolerated) > 0 {
+			shouldCheckPods = true
+		} else {
+			return workloadHealthCheck{status: workloadHealthy}, nil
 		}
 	} else if !ready && features.Enabled(features.TASReplaceNodeOnPodTermination) {
 		// NotReady node
-		shouldWait = true
+		shouldCheckPods = true
 	}
 
-	if shouldWait {
-		var wl kueue.Workload
-		if err := r.client.Get(ctx, wlKey, &wl); err != nil {
-			return workloadHealthy, fmt.Errorf("failed to get workload %s: %w", wlKey, err)
-		}
-		podsForWl, err := r.listPodsForWorkload(ctx, &wl)
+	if shouldCheckPods {
+		podsAssigned, hasGatedPods, err := r.getPodsAssignedToNode(ctx, wl, node)
 		if err != nil {
-			return workloadHealthy, fmt.Errorf("failed to list pods for workload %s: %w", wlKey, err)
+			return workloadHealthCheck{}, fmt.Errorf("failed to list pods for workload %s: %w", wlKey, err)
 		}
-		allPodsTerminated := true
-		for _, pod := range podsForWl {
-			if pod.Spec.NodeName == nodeName && pod.DeletionTimestamp.IsZero() && !utilpod.IsTerminated(pod) {
-				allPodsTerminated = false
-				break
+
+		if hasGatedPods && len(podsAssigned) == 0 {
+			// We consider the workload temporarily healthy if there are pods assigned to the node via Workload assignment,
+			// but they are still pending nodeSelector injection by TopologyUngater.
+			log := ctrl.LoggerFrom(ctx).WithValues("workload", klog.KRef(wlKey.Namespace, wlKey.Name))
+			log.V(2).Info("Found gated pods for assigned workload, waiting for TopologyUngater to inject nodeSelector")
+			return workloadHealthCheck{status: workloadTemporarilyHealthy}, nil
+		}
+
+		// Identify pods that need to be terminated (pending pods on tainted nodes)
+		// and check if there are any pods that should be retained (e.g. running or gated).
+		podsToTerminate, hasPodsToRetain := classifyPodsForReplacement(podsAssigned)
+		if len(untolerated) > 0 && len(podsToTerminate) > 0 {
```

  This piece of code extends the scope of pod termination beyond just handling to NoSchedule as originally planned in the KEP to NoExecute.
  As I synced with @j-skiba this is needed to prevent race condition for Pods getting stuck in between Kueue ungating and scheduling by Kube scheduler.
  From our analysis this seems a pre-existing bug in handling both NoExecute done before) and NotReady nodes. So, I would propose to take a step back and prepare a small bugfix PR first which fixes the pre-existing bug with the PodTermination mechanism.
  
  Plus a dedicated integration or e2e test for the scenario.
  
  cc @tenzen-y

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-02T09:45:16Z

- **charts/kueue/templates/rbac/role.yaml:70** by [@j-skiba](https://github.com/j-skiba) — 2026-03-02T09:45:16Z
```diff
@@ -67,6 +67,7 @@ rules:
     verbs:
       - get
       - patch
+      - update
```

  removed

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-02T09:45:22Z

- **pkg/controller/tas/node_failure_controller.go:182** by [@j-skiba](https://github.com/j-skiba) — 2026-03-02T09:45:22Z
```diff
@@ -164,7 +178,8 @@ func (r *nodeFailureReconciler) Delete(e event.TypedDeleteEvent[*corev1.Node]) b
 }
 
 //+kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
-//+kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch
+//+kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update
+//+kubebuilder:rbac:groups="",resources=pods/status,verbs=patch;update
```

  removed

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-19T08:27:46Z

- **pkg/controller/tas/node_failure_controller.go:295** by [@j-skiba](https://github.com/j-skiba) — 2026-03-19T08:27:46Z
```diff
@@ -234,63 +249,69 @@ func hasTASAssignmentOnNode(wl *kueue.Workload, nodeName string) bool {
 	return false
 }
 
-func (r *nodeFailureReconciler) getWorkloadStatus(ctx context.Context, nodeName string, node *corev1.Node, wlKey types.NamespacedName, wl *kueue.Workload) (workloadStatus, error) {
+func (r *nodeFailureReconciler) checkWorkloadHealthOnNode(ctx context.Context, nodeName string, node *corev1.Node, wlKey types.NamespacedName, wl *kueue.Workload) (workloadHealthCheck, error) {
 	if err := r.client.Get(ctx, wlKey, wl); err != nil {
 		if apierrors.IsNotFound(err) {
-			return workloadHealthy, nil
+			return workloadHealthCheck{status: workloadHealthy}, nil
 		}
-		return workloadHealthy, err
+		return workloadHealthCheck{}, err
 	}
 
-	shouldWait := false
 	ready := utiltas.IsNodeStatusConditionTrue(node.Status.Conditions, corev1.NodeReady)
 
+	var untolerated, temporarilyTolerated []corev1.Taint
+	shouldCheckPods := false
+
 	if ready && features.Enabled(features.TASReplaceNodeOnNodeTaints) {
 		// Ready node, check for taints
-		untolerated, temporarilyTolerated := classifyNoExecuteTaints(ctx, node.Spec.Taints, workload.PodSetsOnNode(wl, nodeName))
-		switch {
-		case len(untolerated) > 0:
-			if !features.Enabled(features.TASReplaceNodeOnPodTermination) {
-				return workloadUnhealthy, nil
-			}
-			shouldWait = true
-		case len(temporarilyTolerated) > 0:
-			shouldWait = true
-		default:
-			return workloadHealthy, nil
+		untolerated, temporarilyTolerated = classifyTaints(ctx, node.Spec.Taints, workload.PodSetsOnNode(wl, nodeName))
+		if len(untolerated) > 0 || len(temporarilyTolerated) > 0 {
+			shouldCheckPods = true
+		} else {
+			return workloadHealthCheck{status: workloadHealthy}, nil
 		}
 	} else if !ready && features.Enabled(features.TASReplaceNodeOnPodTermination) {
 		// NotReady node
-		shouldWait = true
+		shouldCheckPods = true
 	}
 
-	if shouldWait {
-		var wl kueue.Workload
-		if err := r.client.Get(ctx, wlKey, &wl); err != nil {
-			return workloadHealthy, fmt.Errorf("failed to get workload %s: %w", wlKey, err)
-		}
-		podsForWl, err := r.listPodsForWorkload(ctx, &wl)
+	if shouldCheckPods {
+		podsAssigned, hasGatedPods, err := r.getPodsAssignedToNode(ctx, wl, node)
 		if err != nil {
-			return workloadHealthy, fmt.Errorf("failed to list pods for workload %s: %w", wlKey, err)
+			return workloadHealthCheck{}, fmt.Errorf("failed to list pods for workload %s: %w", wlKey, err)
 		}
-		allPodsTerminated := true
-		for _, pod := range podsForWl {
-			if pod.Spec.NodeName == nodeName && pod.DeletionTimestamp.IsZero() && !utilpod.IsTerminated(pod) {
-				allPodsTerminated = false
-				break
+
+		if hasGatedPods && len(podsAssigned) == 0 {
+			// We consider the workload temporarily healthy if there are pods assigned to the node via Workload assignment,
+			// but they are still pending nodeSelector injection by TopologyUngater.
+			log := ctrl.LoggerFrom(ctx).WithValues("workload", klog.KRef(wlKey.Namespace, wlKey.Name))
+			log.V(2).Info("Found gated pods for assigned workload, waiting for TopologyUngater to inject nodeSelector")
+			return workloadHealthCheck{status: workloadTemporarilyHealthy}, nil
+		}
+
+		// Identify pods that need to be terminated (pending pods on tainted nodes)
+		// and check if there are any pods that should be retained (e.g. running or gated).
+		podsToTerminate, hasPodsToRetain := classifyPodsForReplacement(podsAssigned)
+		if len(untolerated) > 0 && len(podsToTerminate) > 0 {
```

  fixed in https://github.com/kubernetes-sigs/kueue/pull/9615

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-19T08:28:03Z

- **pkg/controller/tas/node_failure_controller.go:263** by [@j-skiba](https://github.com/j-skiba) — 2026-03-19T08:28:03Z
```diff
@@ -234,63 +249,69 @@ func hasTASAssignmentOnNode(wl *kueue.Workload, nodeName string) bool {
 	return false
 }
 
-func (r *nodeFailureReconciler) getWorkloadStatus(ctx context.Context, nodeName string, node *corev1.Node, wlKey types.NamespacedName, wl *kueue.Workload) (workloadStatus, error) {
+func (r *nodeFailureReconciler) checkWorkloadHealthOnNode(ctx context.Context, nodeName string, node *corev1.Node, wlKey types.NamespacedName, wl *kueue.Workload) (workloadHealthCheck, error) {
 	if err := r.client.Get(ctx, wlKey, wl); err != nil {
 		if apierrors.IsNotFound(err) {
-			return workloadHealthy, nil
+			return workloadHealthCheck{status: workloadHealthy}, nil
 		}
-		return workloadHealthy, err
+		return workloadHealthCheck{}, err
 	}
 
-	shouldWait := false
 	ready := utiltas.IsNodeStatusConditionTrue(node.Status.Conditions, corev1.NodeReady)
 
+	var untolerated, temporarilyTolerated []corev1.Taint
+	shouldCheckPods := false
```

  outdated

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-19T08:59:10Z

- **test/integration/singlecluster/tas/tas_test.go:2576** by [@mimowo](https://github.com/mimowo) — 2026-03-19T08:59:10Z
```diff
@@ -2423,6 +2485,108 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
 				})
 			})
+
+			ginkgo.It("should evict workload when node has NoSchedule taint and no pods are present", framework.SlowSpec, func() {
+				features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.TASReplaceNodeOnNodeTaints, true)
+				features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.TASReplaceNodeOnPodTermination, false)
+
+				var wl1 *kueue.Workload
+				nodeName := nodes[0].Name
+
+				ginkgo.By("creating a workload", func() {
+					wl1 = utiltestingapi.MakeWorkload("wl1", ns.Name).
+						PodSets(*utiltestingapi.MakePodSet("worker", 2).
+							RequiredTopologyRequest(utiltesting.DefaultBlockTopologyLevel).
+							Obj()).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					util.MustCreate(ctx, k8sClient, wl1)
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
+				})
+
+				ginkgo.By("applying NoSchedule taint to the node", func() {
+					nodeToUpdate := &corev1.Node{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: nodeName}, nodeToUpdate)).Should(gomega.Succeed())
+					nodeToUpdate.Spec.Taints = append(nodeToUpdate.Spec.Taints, corev1.Taint{
+						Key:    "example.com/unschedulable",
+						Value:  "true",
+						Effect: corev1.TaintEffectNoSchedule,
+					})
+					gomega.Expect(k8sClient.Update(ctx, nodeToUpdate)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload eventually gets an entry in unhealthyNodes list", func() {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+						g.Expect(wl1.Status.UnhealthyNodes).To(gomega.ContainElement(kueue.UnhealthyNode{Name: nodeName}))
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+			})
+
+			ginkgo.It("should NOT update UnhealthyNodes immediately when node has NoSchedule taint and a pod is running", framework.SlowSpec, func() {
+				features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.TASReplaceNodeOnNodeTaints, true)
+				features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.TASReplaceNodeOnPodTermination, true)
+
+				var wl1 *kueue.Workload
+				nodeName := nodes[0].Name
+
+				ginkgo.By("creating a workload", func() {
+					wl1 = utiltestingapi.MakeWorkload("wl1", ns.Name).
+						PodSets(*utiltestingapi.MakePodSet("worker", 2).
+							RequiredTopologyRequest(utiltesting.DefaultBlockTopologyLevel).
+							Obj()).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					util.MustCreate(ctx, k8sClient, wl1)
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
+				})
+
+				var pod *corev1.Pod
+				ginkgo.By("creating a running pod for the workload assigned to nodeName", func() {
+					pod = testingpod.MakePod("pod1", ns.Name).
+						Annotation(kueue.WorkloadAnnotation, "wl1").
+						Annotation(kueue.PodSetUnconstrainedTopologyAnnotation, "true").
+						NodeName(nodeName).
+						Obj()
+					util.MustCreate(ctx, k8sClient, pod)
+
+					pod.Status.Phase = corev1.PodRunning
+					gomega.Expect(k8sClient.Status().Update(ctx, pod)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("applying NoSchedule taint to the node", func() {
+					nodeToUpdate := &corev1.Node{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: nodeName}, nodeToUpdate)).Should(gomega.Succeed())
+					nodeToUpdate.Spec.Taints = append(nodeToUpdate.Spec.Taints, corev1.Taint{
+						Key:    "example.com/unschedulable",
+						Value:  "true",
+						Effect: corev1.TaintEffectNoSchedule,
+					})
+					gomega.Expect(k8sClient.Update(ctx, nodeToUpdate)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify UnhealthyNodes is NOT updated while pod is running", func() {
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+						g.Expect(wl1.Status.UnhealthyNodes).NotTo(gomega.ContainElement(kueue.UnhealthyNode{Name: nodeName}))
+					}, 2*time.Second, util.Interval).Should(gomega.Succeed())
```

  Don't use magic timestamps, I think just regular interval of 1s should be enough

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-19T09:06:39Z

- **test/integration/singlecluster/tas/tas_test.go:2576** by [@j-skiba](https://github.com/j-skiba) — 2026-03-19T09:06:39Z
```diff
@@ -2423,6 +2485,108 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
 				})
 			})
+
+			ginkgo.It("should evict workload when node has NoSchedule taint and no pods are present", framework.SlowSpec, func() {
+				features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.TASReplaceNodeOnNodeTaints, true)
+				features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.TASReplaceNodeOnPodTermination, false)
+
+				var wl1 *kueue.Workload
+				nodeName := nodes[0].Name
+
+				ginkgo.By("creating a workload", func() {
+					wl1 = utiltestingapi.MakeWorkload("wl1", ns.Name).
+						PodSets(*utiltestingapi.MakePodSet("worker", 2).
+							RequiredTopologyRequest(utiltesting.DefaultBlockTopologyLevel).
+							Obj()).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					util.MustCreate(ctx, k8sClient, wl1)
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
+				})
+
+				ginkgo.By("applying NoSchedule taint to the node", func() {
+					nodeToUpdate := &corev1.Node{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: nodeName}, nodeToUpdate)).Should(gomega.Succeed())
+					nodeToUpdate.Spec.Taints = append(nodeToUpdate.Spec.Taints, corev1.Taint{
+						Key:    "example.com/unschedulable",
+						Value:  "true",
+						Effect: corev1.TaintEffectNoSchedule,
+					})
+					gomega.Expect(k8sClient.Update(ctx, nodeToUpdate)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload eventually gets an entry in unhealthyNodes list", func() {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+						g.Expect(wl1.Status.UnhealthyNodes).To(gomega.ContainElement(kueue.UnhealthyNode{Name: nodeName}))
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+			})
+
+			ginkgo.It("should NOT update UnhealthyNodes immediately when node has NoSchedule taint and a pod is running", framework.SlowSpec, func() {
+				features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.TASReplaceNodeOnNodeTaints, true)
+				features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.TASReplaceNodeOnPodTermination, true)
+
+				var wl1 *kueue.Workload
+				nodeName := nodes[0].Name
+
+				ginkgo.By("creating a workload", func() {
+					wl1 = utiltestingapi.MakeWorkload("wl1", ns.Name).
+						PodSets(*utiltestingapi.MakePodSet("worker", 2).
+							RequiredTopologyRequest(utiltesting.DefaultBlockTopologyLevel).
+							Obj()).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					util.MustCreate(ctx, k8sClient, wl1)
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
+				})
+
+				var pod *corev1.Pod
+				ginkgo.By("creating a running pod for the workload assigned to nodeName", func() {
+					pod = testingpod.MakePod("pod1", ns.Name).
+						Annotation(kueue.WorkloadAnnotation, "wl1").
+						Annotation(kueue.PodSetUnconstrainedTopologyAnnotation, "true").
+						NodeName(nodeName).
+						Obj()
+					util.MustCreate(ctx, k8sClient, pod)
+
+					pod.Status.Phase = corev1.PodRunning
+					gomega.Expect(k8sClient.Status().Update(ctx, pod)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("applying NoSchedule taint to the node", func() {
+					nodeToUpdate := &corev1.Node{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: nodeName}, nodeToUpdate)).Should(gomega.Succeed())
+					nodeToUpdate.Spec.Taints = append(nodeToUpdate.Spec.Taints, corev1.Taint{
+						Key:    "example.com/unschedulable",
+						Value:  "true",
+						Effect: corev1.TaintEffectNoSchedule,
+					})
+					gomega.Expect(k8sClient.Update(ctx, nodeToUpdate)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify UnhealthyNodes is NOT updated while pod is running", func() {
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+						g.Expect(wl1.Status.UnhealthyNodes).NotTo(gomega.ContainElement(kueue.UnhealthyNode{Name: nodeName}))
+					}, 2*time.Second, util.Interval).Should(gomega.Succeed())
```

  changed to `ConsistentDuration`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-19T10:02:37Z

- **pkg/controller/tas/node_controller.go:661** by [@mimowo](https://github.com/mimowo) — 2026-03-19T10:02:37Z
```diff
@@ -655,10 +655,10 @@ func checkTaintTolerations(logger logr.Logger, taint *corev1.Taint, podSets []ku
 	return toleratedPermanently
 }
 
-func classifyNoExecuteTaints(ctx context.Context, taints []corev1.Taint, podSets []kueue.PodSet) (untolerated, temporarilyTolerated []corev1.Taint) {
+func classifyTaints(ctx context.Context, taints []corev1.Taint, podSets []kueue.PodSet) (untolerated, temporarilyTolerated []corev1.Taint) {
 	logger := ctrl.LoggerFrom(ctx)
 	for _, taint := range taints {
-		if taint.Effect != corev1.TaintEffectNoExecute {
+		if taint.Effect != corev1.TaintEffectNoExecute && taint.Effect != corev1.TaintEffectNoSchedule {
```

  Nice, so virtually there is now no difference handling NoExecute and NoSchedule 👍 
  
  So iic for NoExecute we rely on kubelet to start killing Pods, and thus triggering the workload eviction. For NoSchedule the Pods will continue running and no eviction is triggered.
  
  One potential corner case is when NoSchedule is used, and there are 2 Pods on a node, one is crashed, then currently IIUC we will not evict, we will just keep replacing the Pod. So we may I think trigger the eviction as soon as any (not necessarily all) Pods are terminating. We may double check that scenario as a follow up since the functionality remains alpha.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-19T10:04:33Z

/lgtm
/approve
Thank you 👍 
/cherrypick  release-0.16
/cherrypick  release-0.15

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-03-19T10:06:21Z

- **pkg/controller/tas/node_controller_test.go:602** by [@pajakd](https://github.com/pajakd) — 2026-03-19T10:06:21Z
```diff
@@ -586,6 +585,74 @@ func TestNodeFailureReconciler(t *testing.T) {
 				features.TASReplaceNodeOnNodeTaints: true,
 			},
 		},
+		"Node has NoSchedule taint, ReplaceNodeOnPodTermination on, pod running -> Healthy": {
+			initObjs: []client.Object{
+				baseNode.Clone().StatusConditions(corev1.NodeCondition{
+					Type:               corev1.NodeReady,
+					Status:             corev1.ConditionTrue,
+					LastTransitionTime: now}).
+					Taints(corev1.Taint{Key: "foo", Effect: corev1.TaintEffectNoSchedule}).Obj(),
+				baseWorkload.DeepCopy(),
+				basePod.DeepCopy(),
+			},
+			reconcileRequests:  []reconcile.Request{{NamespacedName: types.NamespacedName{Name: nodeName}}},
+			wantUnhealthyNodes: nil,
+			featureGates: map[featuregate.Feature]bool{
+				features.TASReplaceNodeOnNodeTaints:     true,
+				features.TASReplaceNodeOnPodTermination: true,
```

  Isn't TASReplaceNodeOnPodTermination true by default since 0.14?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-03-19T10:10:16Z

- **test/e2e/tas/hotswap_test.go:300** by [@pajakd](https://github.com/pajakd) — 2026-03-19T10:10:16Z
```diff
@@ -296,7 +296,7 @@ var _ = ginkgo.Describe("Hotswap for Topology Aware Scheduling", ginkgo.Ordered,
 				util.UpdateKueueConfigurationAndRestart(ctx, k8sClient, defaultKueueCfg, kindClusterName)
 			})
 
-			// In this test we use a jobset with SliceSize = 2 and SliceRequiredTopology = Rack
+			// In this test we use a job with SliceSize = 2 and SliceRequiredTopology = Rack
 			// Each pod requires 1 "extraResource" so the jobSet will use both nodes from a Rack.
```

  ```suggestion
  			// Each pod requires 1 "extraResource" so the job will use both nodes from a Rack.
  ```

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-03-19T10:16:12Z

- **test/e2e/tas/hotswap_test.go:302** by [@pajakd](https://github.com/pajakd) — 2026-03-19T10:16:12Z
```diff
@@ -296,7 +296,7 @@ var _ = ginkgo.Describe("Hotswap for Topology Aware Scheduling", ginkgo.Ordered,
 				util.UpdateKueueConfigurationAndRestart(ctx, k8sClient, defaultKueueCfg, kindClusterName)
 			})
 
-			// In this test we use a jobset with SliceSize = 2 and SliceRequiredTopology = Rack
+			// In this test we use a job with SliceSize = 2 and SliceRequiredTopology = Rack
 			// Each pod requires 1 "extraResource" so the jobSet will use both nodes from a Rack.
 			// We simulate a node failure by tainting it with NoExecute.
 			// The replacement mechanism seeks a replacement within the same Rack, but fails as no implementation is available.
```

  I know its a test on NoExecute but the word "implementation" bothers me a bit. Shouldn't it be  "no replacement is available"?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-03-19T10:20:21Z

- **test/e2e/tas/hotswap_test.go:639** by [@pajakd](https://github.com/pajakd) — 2026-03-19T10:20:21Z
```diff
@@ -614,6 +614,98 @@ var _ = ginkgo.Describe("Hotswap for Topology Aware Scheduling", ginkgo.Ordered,
 					expectPodsOnNodes(ctx, k8sClient, ns.Name, jobName, numPods, expectedNodes)
 				})
 			})
+
+			// In this test we use a job with Parallelism = 3 and SliceSize = 3, Block topology.
+			// Each pod requires 1 "extraResource", so the job will use three nodes from a Block.
+			// Once running, one node gets tainted with NoSchedule.
+			// The NoSchedule taint doesn't evict running pods, so the node is initially considered temporarily healthy.
+			// When the pod on this tainted node fails (is deleted), Kueue recognizes the active pods no longer tolerate the node,
+			// and triggers a replacement to find an available node within the same block.
+			ginkgo.It("Should replace a node when it becomes tainted with NoSchedule and pod fails", func() {
+				parallelism := 3
+				numPods := parallelism
+				jobName := "noschedule-job"
+				sampleJob := testingjob.MakeJob(jobName, ns.Name).
+					Queue(kueue.LocalQueueName(localQueue.Name)).
+					Parallelism(int32(parallelism)).
+					Completions(int32(parallelism)).
+					Suspend(true).
+					PodLabel("job-name", jobName).
+					Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletionFailOnExit).
+					RequestAndLimit(corev1.ResourceCPU, "200m").
+					RequestAndLimit(extraResource, "1").
+					PodAnnotation(kueue.PodSetPreferredTopologyAnnotation, utiltesting.DefaultBlockTopologyLevel).
+					PodAnnotation(kueue.PodSetSliceRequiredTopologyAnnotation, utiltesting.DefaultBlockTopologyLevel).
+					PodAnnotation(kueue.PodSetSliceSizeAnnotation, "3").
```

  ```suggestion
  					PodAnnotation(kueue.PodSetRequiredTopologyAnnotation, utiltesting.DefaultBlockTopologyLevel).
  ```
  
  Unless I'm missing something, this is equivalent to setting Block as `required` level.

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-19T13:19:24Z

- **test/e2e/tas/hotswap_test.go:639** by [@j-skiba](https://github.com/j-skiba) — 2026-03-19T13:19:24Z
```diff
@@ -614,6 +614,98 @@ var _ = ginkgo.Describe("Hotswap for Topology Aware Scheduling", ginkgo.Ordered,
 					expectPodsOnNodes(ctx, k8sClient, ns.Name, jobName, numPods, expectedNodes)
 				})
 			})
+
+			// In this test we use a job with Parallelism = 3 and SliceSize = 3, Block topology.
+			// Each pod requires 1 "extraResource", so the job will use three nodes from a Block.
+			// Once running, one node gets tainted with NoSchedule.
+			// The NoSchedule taint doesn't evict running pods, so the node is initially considered temporarily healthy.
+			// When the pod on this tainted node fails (is deleted), Kueue recognizes the active pods no longer tolerate the node,
+			// and triggers a replacement to find an available node within the same block.
+			ginkgo.It("Should replace a node when it becomes tainted with NoSchedule and pod fails", func() {
+				parallelism := 3
+				numPods := parallelism
+				jobName := "noschedule-job"
+				sampleJob := testingjob.MakeJob(jobName, ns.Name).
+					Queue(kueue.LocalQueueName(localQueue.Name)).
+					Parallelism(int32(parallelism)).
+					Completions(int32(parallelism)).
+					Suspend(true).
+					PodLabel("job-name", jobName).
+					Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletionFailOnExit).
+					RequestAndLimit(corev1.ResourceCPU, "200m").
+					RequestAndLimit(extraResource, "1").
+					PodAnnotation(kueue.PodSetPreferredTopologyAnnotation, utiltesting.DefaultBlockTopologyLevel).
+					PodAnnotation(kueue.PodSetSliceRequiredTopologyAnnotation, utiltesting.DefaultBlockTopologyLevel).
+					PodAnnotation(kueue.PodSetSliceSizeAnnotation, "3").
```

  addressed in https://github.com/kubernetes-sigs/kueue/pull/10012

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-19T13:19:32Z

- **test/e2e/tas/hotswap_test.go:302** by [@j-skiba](https://github.com/j-skiba) — 2026-03-19T13:19:32Z
```diff
@@ -296,7 +296,7 @@ var _ = ginkgo.Describe("Hotswap for Topology Aware Scheduling", ginkgo.Ordered,
 				util.UpdateKueueConfigurationAndRestart(ctx, k8sClient, defaultKueueCfg, kindClusterName)
 			})
 
-			// In this test we use a jobset with SliceSize = 2 and SliceRequiredTopology = Rack
+			// In this test we use a job with SliceSize = 2 and SliceRequiredTopology = Rack
 			// Each pod requires 1 "extraResource" so the jobSet will use both nodes from a Rack.
 			// We simulate a node failure by tainting it with NoExecute.
 			// The replacement mechanism seeks a replacement within the same Rack, but fails as no implementation is available.
```

  addressed in https://github.com/kubernetes-sigs/kueue/pull/10012

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-19T13:19:36Z

- **test/e2e/tas/hotswap_test.go:300** by [@j-skiba](https://github.com/j-skiba) — 2026-03-19T13:19:35Z
```diff
@@ -296,7 +296,7 @@ var _ = ginkgo.Describe("Hotswap for Topology Aware Scheduling", ginkgo.Ordered,
 				util.UpdateKueueConfigurationAndRestart(ctx, k8sClient, defaultKueueCfg, kindClusterName)
 			})
 
-			// In this test we use a jobset with SliceSize = 2 and SliceRequiredTopology = Rack
+			// In this test we use a job with SliceSize = 2 and SliceRequiredTopology = Rack
 			// Each pod requires 1 "extraResource" so the jobSet will use both nodes from a Rack.
```

  addressed in https://github.com/kubernetes-sigs/kueue/pull/10012

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-19T13:19:38Z

- **pkg/controller/tas/node_controller_test.go:602** by [@j-skiba](https://github.com/j-skiba) — 2026-03-19T13:19:38Z
```diff
@@ -586,6 +585,74 @@ func TestNodeFailureReconciler(t *testing.T) {
 				features.TASReplaceNodeOnNodeTaints: true,
 			},
 		},
+		"Node has NoSchedule taint, ReplaceNodeOnPodTermination on, pod running -> Healthy": {
+			initObjs: []client.Object{
+				baseNode.Clone().StatusConditions(corev1.NodeCondition{
+					Type:               corev1.NodeReady,
+					Status:             corev1.ConditionTrue,
+					LastTransitionTime: now}).
+					Taints(corev1.Taint{Key: "foo", Effect: corev1.TaintEffectNoSchedule}).Obj(),
+				baseWorkload.DeepCopy(),
+				basePod.DeepCopy(),
+			},
+			reconcileRequests:  []reconcile.Request{{NamespacedName: types.NamespacedName{Name: nodeName}}},
+			wantUnhealthyNodes: nil,
+			featureGates: map[featuregate.Feature]bool{
+				features.TASReplaceNodeOnNodeTaints:     true,
+				features.TASReplaceNodeOnPodTermination: true,
```

  addressed in https://github.com/kubernetes-sigs/kueue/pull/10012
