# PR #90: NamespaceSelector for ClusterQueue

**Summary**: NamespaceSelector for ClusterQueue

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/90

**Last updated**: 2022-03-08T19:20:51Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ahg-g](https://github.com/ahg-g)
- **Branch**: `ahg-ns` → `main`
- **Created**: 2022-03-03T01:30:35Z
- **Updated**: 2022-03-08T19:20:51Z
- **Merged**: 2022-03-08T19:20:50Z
- **Closed**: 2022-03-08T19:20:50Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: _none_
- **Changed files**: 12   **+415 / -73**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

Implements NamespaceSelector for clusterQueue

#### Which issue(s) this PR fixes:
Fixes #4 

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-03-03T01:30:47Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/90#" title="Author self-approved">ahg-g</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [ahg-g]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-03T21:02:12Z

/assign @alculquicondor 
/hold 

I will send a separate PR for integration tests

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-07T21:36:10Z

![Screenshot from 2022-03-07 16-35-36](https://user-images.githubusercontent.com/15933089/157121657-b556027e-7060-4782-a318-3f4930f04484.png)
A small nit, maybe not even for this PR, can we use caps for the first letter of commits? ;)

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-08T01:43:30Z

/retest

### Comment by [@denkensk](https://github.com/denkensk) — 2022-03-08T03:10:14Z

/retest

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-08T14:28:35Z

I added the integration test in this PR now that we have the refactor to ClusterQueue merged.

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-08T15:07:02Z

Can you squash, thanks

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-08T19:12:38Z

/lgtm

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-08T19:19:13Z

/hold cancel

## Reviews

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-04T03:53:09Z

- **pkg/capacity/capacity.go:140** by [@denkensk](https://github.com/denkensk) — 2022-03-04T03:35:44Z
```diff
@@ -99,39 +102,61 @@ type FlavorInfo struct {
 	Labels     map[string]string
 }
 
-func NewCapacity(cap *kueue.Capacity) *Capacity {
+func NewCapacity(cap *kueue.Capacity) (*Capacity, error) {
+	usedResources := make(Resources, len(cap.Spec.RequestableResources))
+	for _, r := range cap.Spec.RequestableResources {
+		if len(r.Flavors) == 0 {
+			continue
+		}
+
+		ts := make(map[string]int64, len(r.Flavors))
+		for _, t := range r.Flavors {
+			ts[t.Name] = 0
+		}
+		usedResources[r.Name] = ts
+	}
+
 	c := &Capacity{
-		Name:                 cap.Name,
-		RequestableResources: resourcesByName(cap.Spec.RequestableResources),
-		UsedResources:        make(Resources, len(cap.Spec.RequestableResources)),
-		Workloads:            map[string]*workload.Info{},
-		QueueingStrategy:     cap.Spec.QueueingStrategy,
+		Name:             cap.Name,
+		UsedResources:    usedResources,
+		Workloads:        map[string]*workload.Info{},
+		QueueingStrategy: cap.Spec.QueueingStrategy,
+	}
+	if err := c.update(cap); err != nil {
+		return nil, err
 	}
 
+	return c, nil
+}
+
+func (c *Capacity) update(in *kueue.Capacity) error {
+	c.RequestableResources = resourcesByName(in.Spec.RequestableResources)
+	nsSelector, err := metav1.LabelSelectorAsSelector(in.Spec.NamespaceSelector)
+	if err != nil {
+		return err
+	}
+	c.NamespaceSelector = nsSelector
 	labelKeys := map[corev1.ResourceName]sets.String{}
-	for _, r := range cap.Spec.RequestableResources {
+	for _, r := range in.Spec.RequestableResources {
```

  Is it possible that the new flavor is added? Then we need to add this items in the  `c.UsedResources` ?
  Because it's also invoked in `UpdateCapacity`.

- **pkg/scheduler/scheduler.go:150** by [@denkensk](https://github.com/denkensk) — 2022-03-04T03:52:27Z
```diff
@@ -146,6 +146,15 @@ func calculateRequirementsForAssignments(log logr.Logger, workloads []workload.I
 			log.V(3).Info("Capacity not found when calculating workload assignments")
 			continue
 		}
+		ns := corev1.Namespace{}
+		if err := s.client.Get(ctx, types.NamespacedName{Name: w.Obj.Namespace}, &ns); err != nil {
```

  I'm curious when it would return an error here,  is the namespace not found? 
  Ns will be deleted only after all object are deleted.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-04T14:44:37Z

- **pkg/scheduler/scheduler.go:150** by [@ahg-g](https://github.com/ahg-g) — 2022-03-04T14:44:37Z
```diff
@@ -146,6 +146,15 @@ func calculateRequirementsForAssignments(log logr.Logger, workloads []workload.I
 			log.V(3).Info("Capacity not found when calculating workload assignments")
 			continue
 		}
+		ns := corev1.Namespace{}
+		if err := s.client.Get(ctx, types.NamespacedName{Name: w.Obj.Namespace}, &ns); err != nil {
```

  may be connection errors as well, does the nature of the error matter here?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-04T15:08:36Z

- **pkg/capacity/capacity.go:140** by [@ahg-g](https://github.com/ahg-g) — 2022-03-04T15:08:35Z
```diff
@@ -99,39 +102,61 @@ type FlavorInfo struct {
 	Labels     map[string]string
 }
 
-func NewCapacity(cap *kueue.Capacity) *Capacity {
+func NewCapacity(cap *kueue.Capacity) (*Capacity, error) {
+	usedResources := make(Resources, len(cap.Spec.RequestableResources))
+	for _, r := range cap.Spec.RequestableResources {
+		if len(r.Flavors) == 0 {
+			continue
+		}
+
+		ts := make(map[string]int64, len(r.Flavors))
+		for _, t := range r.Flavors {
+			ts[t.Name] = 0
+		}
+		usedResources[r.Name] = ts
+	}
+
 	c := &Capacity{
-		Name:                 cap.Name,
-		RequestableResources: resourcesByName(cap.Spec.RequestableResources),
-		UsedResources:        make(Resources, len(cap.Spec.RequestableResources)),
-		Workloads:            map[string]*workload.Info{},
-		QueueingStrategy:     cap.Spec.QueueingStrategy,
+		Name:             cap.Name,
+		UsedResources:    usedResources,
+		Workloads:        map[string]*workload.Info{},
+		QueueingStrategy: cap.Spec.QueueingStrategy,
+	}
+	if err := c.update(cap); err != nil {
+		return nil, err
 	}
 
+	return c, nil
+}
+
+func (c *Capacity) update(in *kueue.Capacity) error {
+	c.RequestableResources = resourcesByName(in.Spec.RequestableResources)
+	nsSelector, err := metav1.LabelSelectorAsSelector(in.Spec.NamespaceSelector)
+	if err != nil {
+		return err
+	}
+	c.NamespaceSelector = nsSelector
 	labelKeys := map[corev1.ResourceName]sets.String{}
-	for _, r := range cap.Spec.RequestableResources {
+	for _, r := range in.Spec.RequestableResources {
```

  good catch, updated

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-07T19:30:59Z

- **pkg/cache/cache.go:36** by [@ahg-g](https://github.com/ahg-g) — 2022-03-07T19:30:59Z
```diff
@@ -31,7 +33,7 @@ import (
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-const workloadCapacityKey = "spec.assignedCapacity"
+const workloadClusterQueueKey = "spec.admission.clusterQueue"
```

  @alculquicondor how can we add a test for this bug?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-07T20:02:28Z

- **pkg/cache/cache.go:36** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-07T20:02:28Z
```diff
@@ -31,7 +33,7 @@ import (
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-const workloadCapacityKey = "spec.assignedCapacity"
+const workloadClusterQueueKey = "spec.admission.clusterQueue"
```

  It actually might not be a bug, because we use the same string when setting up the index and when querying.
  
  But this is the kind of magic in controller-runtime that I don't think we can test.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-07T20:29:58Z

- **pkg/cache/cache.go:36** by [@ahg-g](https://github.com/ahg-g) — 2022-03-07T20:29:57Z
```diff
@@ -31,7 +33,7 @@ import (
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-const workloadCapacityKey = "spec.assignedCapacity"
+const workloadClusterQueueKey = "spec.admission.clusterQueue"
```

  it is not about the string though, it is about the value; the string is the key to lookup the value and it should be correct to have a correct index, otherwise what would it be indexing on?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-07T20:31:11Z

- **pkg/cache/cache.go:136** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-07T20:13:57Z
```diff
@@ -101,39 +104,59 @@ type FlavorInfo struct {
 	Labels     map[string]string
 }
 
-func NewClusterQueue(cap *kueue.ClusterQueue) *ClusterQueue {
+func NewClusterQueue(cap *kueue.ClusterQueue) (*ClusterQueue, error) {
 	c := &ClusterQueue{
-		Name:                 cap.Name,
-		RequestableResources: resourcesByName(cap.Spec.RequestableResources),
-		UsedResources:        make(Resources, len(cap.Spec.RequestableResources)),
-		Workloads:            map[string]*workload.Info{},
-		QueueingStrategy:     cap.Spec.QueueingStrategy,
+		Name:             cap.Name,
+		Workloads:        map[string]*workload.Info{},
+		QueueingStrategy: cap.Spec.QueueingStrategy,
 	}
+	if err := c.update(cap); err != nil {
+		return nil, err
+	}
+
+	return c, nil
+}
 
+func (c *ClusterQueue) update(in *kueue.ClusterQueue) error {
+	c.RequestableResources = resourcesByName(in.Spec.RequestableResources)
+	nsSelector, err := metav1.LabelSelectorAsSelector(in.Spec.NamespaceSelector)
+	if err != nil {
+		return err
+	}
+	c.NamespaceSelector = nsSelector
 	labelKeys := map[corev1.ResourceName]sets.String{}
-	for _, r := range cap.Spec.RequestableResources {
+	if c.UsedResources == nil {
+		c.UsedResources = make(Resources, len(in.Spec.RequestableResources))
+	}
+	for _, r := range in.Spec.RequestableResources {
 		if len(r.Flavors) == 0 {
 			continue
 		}
 
+		ts, exist := c.UsedResources[r.Name]
```

  s/ts/usedFlavors

- **pkg/cache/cache.go:143** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-07T20:15:15Z
```diff
@@ -101,39 +104,59 @@ type FlavorInfo struct {
 	Labels     map[string]string
 }
 
-func NewClusterQueue(cap *kueue.ClusterQueue) *ClusterQueue {
+func NewClusterQueue(cap *kueue.ClusterQueue) (*ClusterQueue, error) {
 	c := &ClusterQueue{
-		Name:                 cap.Name,
-		RequestableResources: resourcesByName(cap.Spec.RequestableResources),
-		UsedResources:        make(Resources, len(cap.Spec.RequestableResources)),
-		Workloads:            map[string]*workload.Info{},
-		QueueingStrategy:     cap.Spec.QueueingStrategy,
+		Name:             cap.Name,
+		Workloads:        map[string]*workload.Info{},
+		QueueingStrategy: cap.Spec.QueueingStrategy,
 	}
+	if err := c.update(cap); err != nil {
+		return nil, err
+	}
+
+	return c, nil
+}
 
+func (c *ClusterQueue) update(in *kueue.ClusterQueue) error {
+	c.RequestableResources = resourcesByName(in.Spec.RequestableResources)
+	nsSelector, err := metav1.LabelSelectorAsSelector(in.Spec.NamespaceSelector)
+	if err != nil {
+		return err
+	}
+	c.NamespaceSelector = nsSelector
 	labelKeys := map[corev1.ResourceName]sets.String{}
-	for _, r := range cap.Spec.RequestableResources {
+	if c.UsedResources == nil {
+		c.UsedResources = make(Resources, len(in.Spec.RequestableResources))
+	}
+	for _, r := range in.Spec.RequestableResources {
 		if len(r.Flavors) == 0 {
 			continue
 		}
 
+		ts, exist := c.UsedResources[r.Name]
+		if !exist {
+			ts = make(map[string]int64, len(r.Flavors))
+		}
 		resKeys := sets.NewString()
-		ts := make(map[string]int64, len(r.Flavors))
 		for _, t := range r.Flavors {
+			if _, exist := ts[t.Name]; !exist {
+				ts[t.Name] = 0
```

  we need to cleanup the flavors that got removed

- **api/v1alpha1/clusterqueue_types.go:146** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-07T20:20:04Z
```diff
@@ -134,14 +134,19 @@ type ClusterQueueSpec struct {
 	Cohort string `json:"cohort,omitempty"`
 
 	// QueueingStrategy indicates the queueing strategy of the workloads
-	// across the queues in this Capacity. This field is immutable.
+	// across the queues in this ClusterQueue. This field is immutable.
 	// Current Supported Strategies:
 	//
 	// - StrictFIFO: workloads are sorted strictly by creation time.
 	//
 	// +kubebuilder:default=StrictFIFO
 	// +kubebuilder:validation:Enum=StrictFIFO
 	QueueingStrategy QueueingStrategy `json:"queueingStrategy,omitempty"`
+
+	// namespaceSelector defines which namespaces are allowed to submit workloads to
```

  What's the behavior if `nil`?

- **pkg/cache/cache_test.go:243** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-07T20:22:58Z
```diff
@@ -108,36 +199,48 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 			name: "delete",
 			operation: func() {
 				clusterQueues := []kueue.ClusterQueue{
-					{ObjectMeta: metav1.ObjectMeta{Name: "b"}},
+					{ObjectMeta: metav1.ObjectMeta{Name: "a"}},
 					{ObjectMeta: metav1.ObjectMeta{Name: "d"}},
 				}
 				for _, c := range clusterQueues {
 					cache.DeleteClusterQueue(&c)
 				}
 			},
-			wantClusterQueues: sets.NewString("a", "c"),
+			wantClusterQueues: map[string]*ClusterQueue{
+				"b": {
+					Name:                 "b",
+					RequestableResources: map[corev1.ResourceName][]FlavorInfo{},
+					NamespaceSelector:    labels.Everything(),
+				},
+				"c": {
+					Name:                 "c",
+					RequestableResources: map[corev1.ResourceName][]FlavorInfo{},
+					NamespaceSelector:    labels.Nothing(),
+				},
+			},
 			wantCohorts: map[string]sets.String{
-				"two": sets.NewString("a", "c"),
+				"one": sets.NewString("b"),
+				"two": sets.NewString("c"),
 			},
 		},
 	}
 	for _, step := range steps {
 		t.Run(step.name, func(t *testing.T) {
 			step.operation()
-			gotclusterQueues := sets.NewString()
-			nameForCapacity := make(map[*ClusterQueue]string)
-			gotCohorts := make(map[string]sets.String)
-			for name, cq := range cache.clusterQueues {
-				gotclusterQueues.Insert(name)
-				nameForCapacity[cq] = name
-			}
-			if diff := cmp.Diff(step.wantClusterQueues, gotclusterQueues); diff != "" {
+			if diff := cmp.Diff(step.wantClusterQueues, cache.clusterQueues,
+				cmpopts.IgnoreFields(ClusterQueue{}, "Cohort", "UsedResources", "Workloads")); diff != "" {
 				t.Errorf("Unexpected clusterQueues (-want,+got):\n%s", diff)
 			}
+
+			nameForClusterQueue := make(map[*ClusterQueue]string)
+			for name, cq := range cache.clusterQueues {
+				nameForClusterQueue[cq] = name
+			}
+			gotCohorts := map[string]sets.String{}
 			for name, cohort := range cache.cohorts {
 				gotCohort := sets.NewString()
 				for clusterQueues := range cohort.members {
-					gotCohort.Insert(nameForCapacity[clusterQueues])
+					gotCohort.Insert(nameForClusterQueue[clusterQueues])
```

  ```suggestion
  					gotCohort.Insert(clusterQueue.Name)
  ```

- **pkg/cache/cache_test.go:242** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-07T20:23:48Z
```diff
@@ -108,36 +199,48 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 			name: "delete",
 			operation: func() {
 				clusterQueues := []kueue.ClusterQueue{
-					{ObjectMeta: metav1.ObjectMeta{Name: "b"}},
+					{ObjectMeta: metav1.ObjectMeta{Name: "a"}},
 					{ObjectMeta: metav1.ObjectMeta{Name: "d"}},
 				}
 				for _, c := range clusterQueues {
 					cache.DeleteClusterQueue(&c)
 				}
 			},
-			wantClusterQueues: sets.NewString("a", "c"),
+			wantClusterQueues: map[string]*ClusterQueue{
+				"b": {
+					Name:                 "b",
+					RequestableResources: map[corev1.ResourceName][]FlavorInfo{},
+					NamespaceSelector:    labels.Everything(),
+				},
+				"c": {
+					Name:                 "c",
+					RequestableResources: map[corev1.ResourceName][]FlavorInfo{},
+					NamespaceSelector:    labels.Nothing(),
+				},
+			},
 			wantCohorts: map[string]sets.String{
-				"two": sets.NewString("a", "c"),
+				"one": sets.NewString("b"),
+				"two": sets.NewString("c"),
 			},
 		},
 	}
 	for _, step := range steps {
 		t.Run(step.name, func(t *testing.T) {
 			step.operation()
-			gotclusterQueues := sets.NewString()
-			nameForCapacity := make(map[*ClusterQueue]string)
-			gotCohorts := make(map[string]sets.String)
-			for name, cq := range cache.clusterQueues {
-				gotclusterQueues.Insert(name)
-				nameForCapacity[cq] = name
-			}
-			if diff := cmp.Diff(step.wantClusterQueues, gotclusterQueues); diff != "" {
+			if diff := cmp.Diff(step.wantClusterQueues, cache.clusterQueues,
+				cmpopts.IgnoreFields(ClusterQueue{}, "Cohort", "UsedResources", "Workloads")); diff != "" {
 				t.Errorf("Unexpected clusterQueues (-want,+got):\n%s", diff)
 			}
+
+			nameForClusterQueue := make(map[*ClusterQueue]string)
+			for name, cq := range cache.clusterQueues {
+				nameForClusterQueue[cq] = name
+			}
+			gotCohorts := map[string]sets.String{}
 			for name, cohort := range cache.cohorts {
 				gotCohort := sets.NewString()
 				for clusterQueues := range cohort.members {
```

  s/clusterQueues/cq

- **pkg/scheduler/scheduler.go:150** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-07T20:30:11Z
```diff
@@ -146,6 +146,15 @@ func calculateRequirementsForAssignments(log logr.Logger, workloads []workload.I
 			log.V(3).Info("Capacity not found when calculating workload assignments")
 			continue
 		}
+		ns := corev1.Namespace{}
+		if err := s.client.Get(ctx, types.NamespacedName{Name: w.Obj.Namespace}, &ns); err != nil {
```

  We might need a `+kubebuilder:rbac` directive to be able to query namespaces.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-07T20:47:09Z

- **pkg/cache/cache.go:36** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-07T20:47:09Z
```diff
@@ -31,7 +33,7 @@ import (
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-const workloadCapacityKey = "spec.assignedCapacity"
+const workloadClusterQueueKey = "spec.admission.clusterQueue"
```

  but we extract the value explicitly in the index

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-07T20:54:09Z

- **pkg/cache/cache.go:36** by [@ahg-g](https://github.com/ahg-g) — 2022-03-07T20:54:09Z
```diff
@@ -31,7 +33,7 @@ import (
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-const workloadCapacityKey = "spec.assignedCapacity"
+const workloadClusterQueueKey = "spec.admission.clusterQueue"
```

  ah, right

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-07T21:13:59Z

- **api/v1alpha1/clusterqueue_types.go:146** by [@ahg-g](https://github.com/ahg-g) — 2022-03-07T21:11:59Z
```diff
@@ -134,14 +134,19 @@ type ClusterQueueSpec struct {
 	Cohort string `json:"cohort,omitempty"`
 
 	// QueueingStrategy indicates the queueing strategy of the workloads
-	// across the queues in this Capacity. This field is immutable.
+	// across the queues in this ClusterQueue. This field is immutable.
 	// Current Supported Strategies:
 	//
 	// - StrictFIFO: workloads are sorted strictly by creation time.
 	//
 	// +kubebuilder:default=StrictFIFO
 	// +kubebuilder:validation:Enum=StrictFIFO
 	QueueingStrategy QueueingStrategy `json:"queueingStrategy,omitempty"`
+
+	// namespaceSelector defines which namespaces are allowed to submit workloads to
```

  selects nothing, which is the default.

- **pkg/cache/cache.go:143** by [@ahg-g](https://github.com/ahg-g) — 2022-03-07T21:13:41Z
```diff
@@ -101,39 +104,59 @@ type FlavorInfo struct {
 	Labels     map[string]string
 }
 
-func NewClusterQueue(cap *kueue.ClusterQueue) *ClusterQueue {
+func NewClusterQueue(cap *kueue.ClusterQueue) (*ClusterQueue, error) {
 	c := &ClusterQueue{
-		Name:                 cap.Name,
-		RequestableResources: resourcesByName(cap.Spec.RequestableResources),
-		UsedResources:        make(Resources, len(cap.Spec.RequestableResources)),
-		Workloads:            map[string]*workload.Info{},
-		QueueingStrategy:     cap.Spec.QueueingStrategy,
+		Name:             cap.Name,
+		Workloads:        map[string]*workload.Info{},
+		QueueingStrategy: cap.Spec.QueueingStrategy,
 	}
+	if err := c.update(cap); err != nil {
+		return nil, err
+	}
+
+	return c, nil
+}
 
+func (c *ClusterQueue) update(in *kueue.ClusterQueue) error {
+	c.RequestableResources = resourcesByName(in.Spec.RequestableResources)
+	nsSelector, err := metav1.LabelSelectorAsSelector(in.Spec.NamespaceSelector)
+	if err != nil {
+		return err
+	}
+	c.NamespaceSelector = nsSelector
 	labelKeys := map[corev1.ResourceName]sets.String{}
-	for _, r := range cap.Spec.RequestableResources {
+	if c.UsedResources == nil {
+		c.UsedResources = make(Resources, len(in.Spec.RequestableResources))
+	}
+	for _, r := range in.Spec.RequestableResources {
 		if len(r.Flavors) == 0 {
 			continue
 		}
 
+		ts, exist := c.UsedResources[r.Name]
+		if !exist {
+			ts = make(map[string]int64, len(r.Flavors))
+		}
 		resKeys := sets.NewString()
-		ts := make(map[string]int64, len(r.Flavors))
 		for _, t := range r.Flavors {
+			if _, exist := ts[t.Name]; !exist {
+				ts[t.Name] = 0
```

  done

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-07T21:19:56Z

looking good, just a couple nits

- **pkg/scheduler/scheduler.go:139** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-07T21:19:11Z
```diff
@@ -136,7 +137,8 @@ type entry struct {
 // calculateRequirementsForAdmission returns the workloads with their
 // requirements (resource flavors, borrowing) if they were admitted by the
 // clusterQueues in the snapshot.
-func calculateRequirementsForAdmission(log logr.Logger, workloads []workload.Info, snap cache.Snapshot) []entry {
+func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, snap cache.Snapshot) []entry {
```

  also edit comment for `nominate`

- **pkg/util/testing/wrappers.go:189** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-07T21:19:35Z
```diff
@@ -180,7 +180,7 @@ func (q *QueueWrapper) Obj() *kueue.Queue {
 }
 
 // Capacity updates the capacity the queue points to.
-func (q *QueueWrapper) Capacity(c string) *QueueWrapper {
+func (q *QueueWrapper) ClusterQueue(c string) *QueueWrapper {
```

  edit func comment

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-07T21:26:07Z

- **pkg/util/testing/wrappers.go:189** by [@ahg-g](https://github.com/ahg-g) — 2022-03-07T21:26:07Z
```diff
@@ -180,7 +180,7 @@ func (q *QueueWrapper) Obj() *kueue.Queue {
 }
 
 // Capacity updates the capacity the queue points to.
-func (q *QueueWrapper) Capacity(c string) *QueueWrapper {
+func (q *QueueWrapper) ClusterQueue(c string) *QueueWrapper {
```

  done.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-07T21:26:12Z

- **pkg/scheduler/scheduler.go:139** by [@ahg-g](https://github.com/ahg-g) — 2022-03-07T21:26:11Z
```diff
@@ -136,7 +137,8 @@ type entry struct {
 // calculateRequirementsForAdmission returns the workloads with their
 // requirements (resource flavors, borrowing) if they were admitted by the
 // clusterQueues in the snapshot.
-func calculateRequirementsForAdmission(log logr.Logger, workloads []workload.Info, snap cache.Snapshot) []entry {
+func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, snap cache.Snapshot) []entry {
```

  done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-07T22:12:50Z

- **pkg/scheduler/scheduler.go:154** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-07T22:12:02Z
```diff
@@ -145,6 +146,15 @@ func calculateRequirementsForAdmission(log logr.Logger, workloads []workload.Inf
 			log.V(3).Info("ClusterQueue not found when calculating workload admission requirements")
 			continue
 		}
+		ns := corev1.Namespace{}
+		if err := s.client.Get(ctx, types.NamespacedName{Name: w.Obj.Namespace}, &ns); err != nil {
+			log.Error(err, "Looking up namespace")
+			continue
+		}
+		if !c.NamespaceSelector.Matches(labels.Set(ns.Labels)) {
```

  Let's replace `c` with `cq`.

- **api/v1alpha1/clusterqueue_types.go:146** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-07T22:12:42Z
```diff
@@ -134,14 +134,19 @@ type ClusterQueueSpec struct {
 	Cohort string `json:"cohort,omitempty"`
 
 	// QueueingStrategy indicates the queueing strategy of the workloads
-	// across the queues in this Capacity. This field is immutable.
+	// across the queues in this ClusterQueue. This field is immutable.
 	// Current Supported Strategies:
 	//
 	// - StrictFIFO: workloads are sorted strictly by creation time.
 	//
 	// +kubebuilder:default=StrictFIFO
 	// +kubebuilder:validation:Enum=StrictFIFO
 	QueueingStrategy QueueingStrategy `json:"queueingStrategy,omitempty"`
+
+	// namespaceSelector defines which namespaces are allowed to submit workloads to
```

  So if it's not set, then all the workloads are rejected? How come our integration tests didn't need to be updated to include a selector?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-07T22:43:07Z

- **api/v1alpha1/clusterqueue_types.go:146** by [@ahg-g](https://github.com/ahg-g) — 2022-03-07T22:43:07Z
```diff
@@ -134,14 +134,19 @@ type ClusterQueueSpec struct {
 	Cohort string `json:"cohort,omitempty"`
 
 	// QueueingStrategy indicates the queueing strategy of the workloads
-	// across the queues in this Capacity. This field is immutable.
+	// across the queues in this ClusterQueue. This field is immutable.
 	// Current Supported Strategies:
 	//
 	// - StrictFIFO: workloads are sorted strictly by creation time.
 	//
 	// +kubebuilder:default=StrictFIFO
 	// +kubebuilder:validation:Enum=StrictFIFO
 	QueueingStrategy QueueingStrategy `json:"queueingStrategy,omitempty"`
+
+	// namespaceSelector defines which namespaces are allowed to submit workloads to
```

  It is set to an empty selector in the wrapper, which selects everything.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-07T22:45:46Z

- **api/v1alpha1/clusterqueue_types.go:146** by [@ahg-g](https://github.com/ahg-g) — 2022-03-07T22:45:46Z
```diff
@@ -134,14 +134,19 @@ type ClusterQueueSpec struct {
 	Cohort string `json:"cohort,omitempty"`
 
 	// QueueingStrategy indicates the queueing strategy of the workloads
-	// across the queues in this Capacity. This field is immutable.
+	// across the queues in this ClusterQueue. This field is immutable.
 	// Current Supported Strategies:
 	//
 	// - StrictFIFO: workloads are sorted strictly by creation time.
 	//
 	// +kubebuilder:default=StrictFIFO
 	// +kubebuilder:validation:Enum=StrictFIFO
 	QueueingStrategy QueueingStrategy `json:"queueingStrategy,omitempty"`
+
+	// namespaceSelector defines which namespaces are allowed to submit workloads to
```

  I updated the comment to clarify that empty selector is allowed.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-08T16:36:11Z

- **api/v1alpha1/clusterqueue_types.go:149** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-08T15:48:26Z
```diff
@@ -134,14 +134,21 @@ type ClusterQueueSpec struct {
 	Cohort string `json:"cohort,omitempty"`
 
 	// QueueingStrategy indicates the queueing strategy of the workloads
-	// across the queues in this Capacity. This field is immutable.
+	// across the queues in this ClusterQueue. This field is immutable.
 	// Current Supported Strategies:
 	//
 	// - StrictFIFO: workloads are sorted strictly by creation time.
 	//
 	// +kubebuilder:default=StrictFIFO
 	// +kubebuilder:validation:Enum=StrictFIFO
 	QueueingStrategy QueueingStrategy `json:"queueingStrategy,omitempty"`
+
+	// namespaceSelector defines which namespaces are allowed to submit workloads to
+	// this clusterQueue. Beyond this basic support for policy, an policy agent like
+	// Gatekeeper should be used to enforce more advanced policies.
+	// Defaults to nil which is a nothing selector (no namespaces eligible).
```

  s/nil/null which is the yaml/json name.

- **pkg/cache/cache.go:136** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-08T15:57:29Z
```diff
@@ -101,39 +104,58 @@ type FlavorInfo struct {
 	Labels     map[string]string
 }
 
-func NewClusterQueue(cap *kueue.ClusterQueue) *ClusterQueue {
+func NewClusterQueue(cap *kueue.ClusterQueue) (*ClusterQueue, error) {
 	c := &ClusterQueue{
-		Name:                 cap.Name,
-		RequestableResources: resourcesByName(cap.Spec.RequestableResources),
-		UsedResources:        make(Resources, len(cap.Spec.RequestableResources)),
-		Workloads:            map[string]*workload.Info{},
-		QueueingStrategy:     cap.Spec.QueueingStrategy,
+		Name:             cap.Name,
+		Workloads:        map[string]*workload.Info{},
+		QueueingStrategy: cap.Spec.QueueingStrategy,
 	}
+	if err := c.update(cap); err != nil {
+		return nil, err
+	}
+
+	return c, nil
+}
 
+func (c *ClusterQueue) update(in *kueue.ClusterQueue) error {
+	c.RequestableResources = resourcesByName(in.Spec.RequestableResources)
+	nsSelector, err := metav1.LabelSelectorAsSelector(in.Spec.NamespaceSelector)
+	if err != nil {
+		return err
+	}
+	c.NamespaceSelector = nsSelector
 	labelKeys := map[corev1.ResourceName]sets.String{}
-	for _, r := range cap.Spec.RequestableResources {
+	usedResources := make(Resources, len(in.Spec.RequestableResources))
+	for _, r := range in.Spec.RequestableResources {
 		if len(r.Flavors) == 0 {
 			continue
 		}
 
+		usedFlavors, exist := c.UsedResources[r.Name]
+		if !exist {
+			usedFlavors = make(map[string]int64, len(r.Flavors))
```

  We should create a new map in all cases, so that we can get rid of the flavors that got removed.

- **pkg/util/testing/wrappers.go:205** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-08T15:59:12Z
```diff
@@ -200,6 +200,9 @@ func MakeClusterQueue(name string) *ClusterQueueWrapper {
 		ObjectMeta: metav1.ObjectMeta{
 			Name: name,
 		},
+		Spec: kueue.ClusterQueueSpec{
+			NamespaceSelector: &metav1.LabelSelector{},
```

  document this in the comment.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-08T17:19:05Z

- **pkg/cache/cache.go:136** by [@ahg-g](https://github.com/ahg-g) — 2022-03-08T17:05:27Z
```diff
@@ -101,39 +104,58 @@ type FlavorInfo struct {
 	Labels     map[string]string
 }
 
-func NewClusterQueue(cap *kueue.ClusterQueue) *ClusterQueue {
+func NewClusterQueue(cap *kueue.ClusterQueue) (*ClusterQueue, error) {
 	c := &ClusterQueue{
-		Name:                 cap.Name,
-		RequestableResources: resourcesByName(cap.Spec.RequestableResources),
-		UsedResources:        make(Resources, len(cap.Spec.RequestableResources)),
-		Workloads:            map[string]*workload.Info{},
-		QueueingStrategy:     cap.Spec.QueueingStrategy,
+		Name:             cap.Name,
+		Workloads:        map[string]*workload.Info{},
+		QueueingStrategy: cap.Spec.QueueingStrategy,
 	}
+	if err := c.update(cap); err != nil {
+		return nil, err
+	}
+
+	return c, nil
+}
 
+func (c *ClusterQueue) update(in *kueue.ClusterQueue) error {
+	c.RequestableResources = resourcesByName(in.Spec.RequestableResources)
+	nsSelector, err := metav1.LabelSelectorAsSelector(in.Spec.NamespaceSelector)
+	if err != nil {
+		return err
+	}
+	c.NamespaceSelector = nsSelector
 	labelKeys := map[corev1.ResourceName]sets.String{}
-	for _, r := range cap.Spec.RequestableResources {
+	usedResources := make(Resources, len(in.Spec.RequestableResources))
+	for _, r := range in.Spec.RequestableResources {
 		if len(r.Flavors) == 0 {
 			continue
 		}
 
+		usedFlavors, exist := c.UsedResources[r.Name]
+		if !exist {
+			usedFlavors = make(map[string]int64, len(r.Flavors))
```

  yup!

- **pkg/util/testing/wrappers.go:205** by [@ahg-g](https://github.com/ahg-g) — 2022-03-08T17:07:23Z
```diff
@@ -200,6 +200,9 @@ func MakeClusterQueue(name string) *ClusterQueueWrapper {
 		ObjectMeta: metav1.ObjectMeta{
 			Name: name,
 		},
+		Spec: kueue.ClusterQueueSpec{
+			NamespaceSelector: &metav1.LabelSelector{},
```

  done.

- **api/v1alpha1/clusterqueue_types.go:149** by [@ahg-g](https://github.com/ahg-g) — 2022-03-08T17:08:36Z
```diff
@@ -134,14 +134,21 @@ type ClusterQueueSpec struct {
 	Cohort string `json:"cohort,omitempty"`
 
 	// QueueingStrategy indicates the queueing strategy of the workloads
-	// across the queues in this Capacity. This field is immutable.
+	// across the queues in this ClusterQueue. This field is immutable.
 	// Current Supported Strategies:
 	//
 	// - StrictFIFO: workloads are sorted strictly by creation time.
 	//
 	// +kubebuilder:default=StrictFIFO
 	// +kubebuilder:validation:Enum=StrictFIFO
 	QueueingStrategy QueueingStrategy `json:"queueingStrategy,omitempty"`
+
+	// namespaceSelector defines which namespaces are allowed to submit workloads to
+	// this clusterQueue. Beyond this basic support for policy, an policy agent like
+	// Gatekeeper should be used to enforce more advanced policies.
+	// Defaults to nil which is a nothing selector (no namespaces eligible).
```

  done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-08T18:28:46Z

- **pkg/cache/cache.go:136** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-08T18:27:35Z
```diff
@@ -131,15 +131,14 @@ func (c *ClusterQueue) update(in *kueue.ClusterQueue) error {
 			continue
 		}
 
-		usedFlavors, exist := c.UsedResources[r.Name]
-		if !exist {
-			usedFlavors = make(map[string]int64, len(r.Flavors))
+		existingUsedFlavors := map[string]int64{}
+		if existing, ok := c.UsedResources[r.Name]; ok {
+			existingUsedFlavors = existing
```

  why not just?
  
  ```golang
  existingUsedFlavors := c.UsedResources[r.Name]
  ```

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-08T18:40:11Z

- **pkg/cache/cache.go:136** by [@ahg-g](https://github.com/ahg-g) — 2022-03-08T18:40:11Z
```diff
@@ -131,15 +131,14 @@ func (c *ClusterQueue) update(in *kueue.ClusterQueue) error {
 			continue
 		}
 
-		usedFlavors, exist := c.UsedResources[r.Name]
-		if !exist {
-			usedFlavors = make(map[string]int64, len(r.Flavors))
+		existingUsedFlavors := map[string]int64{}
+		if existing, ok := c.UsedResources[r.Name]; ok {
+			existingUsedFlavors = existing
```

  wouldn't that return a nil if not exist?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-08T18:48:15Z

- **pkg/cache/cache.go:136** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-08T18:48:15Z
```diff
@@ -131,15 +131,14 @@ func (c *ClusterQueue) update(in *kueue.ClusterQueue) error {
 			continue
 		}
 
-		usedFlavors, exist := c.UsedResources[r.Name]
-		if !exist {
-			usedFlavors = make(map[string]int64, len(r.Flavors))
+		existingUsedFlavors := map[string]int64{}
+		if existing, ok := c.UsedResources[r.Name]; ok {
+			existingUsedFlavors = existing
```

  yes, and that's fine https://go.dev/play/p/ArI2F_FzrhA 
  Sometimes golang feels like python :upside_down_face:

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-08T19:07:24Z

- **pkg/cache/cache.go:136** by [@ahg-g](https://github.com/ahg-g) — 2022-03-08T19:07:23Z
```diff
@@ -131,15 +131,14 @@ func (c *ClusterQueue) update(in *kueue.ClusterQueue) error {
 			continue
 		}
 
-		usedFlavors, exist := c.UsedResources[r.Name]
-		if !exist {
-			usedFlavors = make(map[string]int64, len(r.Flavors))
+		existingUsedFlavors := map[string]int64{}
+		if existing, ok := c.UsedResources[r.Name]; ok {
+			existingUsedFlavors = existing
```

  sg, updated and squashed.
