# PR #9029: TAS: Broadcast scheduler when Topology activates ClusterQueues

**Summary**: TAS: Broadcast scheduler when Topology activates ClusterQueues

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9029

**Last updated**: 2026-02-09T11:36:41Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@sohankunkerkar](https://github.com/sohankunkerkar)
- **Branch**: `fix-tas-usage-bug` → `main`
- **Created**: 2026-02-06T13:29:51Z
- **Updated**: 2026-02-09T11:36:41Z
- **Merged**: 2026-02-09T11:35:24Z
- **Closed**: 2026-02-09T11:35:24Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `kind/flake`, `approved`, `size/XS`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@pajakd](https://github.com/pajakd)
- **Changed files**: 1   **+8 / -1**

## Description

The topology controller was ignoring the return value of AddOrUpdateTopology(), missing the broadcast when CQs transition from pending to active. This caused flaky behavior after controller restart when informer events fire in certain orders.

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug
/kind flake

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes https://github.com/kubernetes-sigs/kueue/issues/9004

#### Special notes for your reviewer:
After controller restart, informer cache repopulation fires Create events for existing resources. The order of these events is non-deterministic. When CQ events fire before Topology events, the CQ initially enters "pending" state (waiting for TAS). When the Topology event fires, the CQ transitions to "active", but the topology controller was not calling Broadcast() to wake up the scheduler.                                                                   

This left pending workloads stuck indefinitely because the scheduler was waiting on cond.Wait() and never notified that CQs became active. The test was flaky because the bug only reproducible when informer events fire in a specific order (ResourceFlavor → ClusterQueue → Topology).

One note here:
When Topology is deleted, CQs become inactive. The scheduler doesn't need to wake up because there's no new capacity to use:                                                                                                                                             
  - Create: CQ pending → active (can admit more) → Broadcast to wake scheduler
  - Delete: CQ active → pending (can't admit) → just requeue inadmissible workloads, no new work possible.
  
Ran the failed test 200 times locally and it works w/o any failure.                                     

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
TAS: Fixed a bug that pending workloads could be stuck, not being considered by the Kueue's scheduler,
after the restart of Kueue. The workloads would be considered for scheduling again after any update to their 
ClusterQueue.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-02-06T13:30:14Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 32438ea2c096fe9d9931d60b2066fe4a85a56ad1 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6989830a928e5f0008e26711 |

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-06T14:00:49Z

Summarized the context, ptal https://github.com/kubernetes-sigs/kueue/issues/9004#issuecomment-3860648878

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-09T11:04:21Z

/release-note-edit
```release-note
TAS: Fixed a bug that pending workloads could be stuck, not being considered by the Kueue's scheduler,
after the restart of Kueue. The workloads would be considered for scheduling again after any update to their 
ClusterQueue.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-09T11:06:19Z

Please remove the hack about wl3 as described in https://github.com/kubernetes-sigs/kueue/issues/9004#issuecomment-3860648878, can we a follow up.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-09T11:07:00Z

/lgtm
/approve
/cherrypick release-0.16
/cherrypick release-0.15

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-02-09T11:07:04Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.15`, `release-0.16` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9029#issuecomment-3871066354):

>/lgtm
>/approve
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-09T11:07:09Z

LGTM label has been added.  <details>Git tree hash: 2e5c803a6ff3e391b695b987b6661a692d0c6b5c</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-09T11:07:11Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9029#issuecomment-3871066354" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9029#" title="Author self-approved">sohankunkerkar</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-02-09T11:36:04Z

@mimowo: new pull request created: #9056

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9029#issuecomment-3871066354):

>/lgtm
>/approve
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-02-09T11:36:41Z

@mimowo: new pull request created: #9057

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9029#issuecomment-3871066354):

>/lgtm
>/approve
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T13:36:46Z

- **pkg/controller/tas/topology_controller.go:166** by [@mimowo](https://github.com/mimowo) — 2026-02-06T13:36:46Z
```diff
@@ -156,7 +161,9 @@ func (r *topologyReconciler) Delete(e event.TypedDeleteEvent[*kueue.Topology]) b
 	log.V(2).Info("Topology delete event")
 
 	defer r.queues.NotifyTopologyUpdateWatchers(e.Object, nil)
-	r.cache.DeleteTopology(log, kueue.TopologyReference(e.Object.Name))
+	if cqNames := r.cache.DeleteTopology(log, kueue.TopologyReference(e.Object.Name)); len(cqNames) > 0 {
+		r.queues.QueueInadmissibleWorkloads(context.Background(), cqNames)
+	}
```

  I'm pretty sure this is not needed. I cannot see a scenario when Deleting a topology unblocks schedulng.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T13:38:12Z

- **pkg/controller/tas/topology_controller.go:150** by [@mimowo](https://github.com/mimowo) — 2026-02-06T13:38:12Z
```diff
@@ -143,7 +143,12 @@ func (r *topologyReconciler) Create(e event.TypedCreateEvent[*kueue.Topology]) b
 	log.V(2).Info("Topology create event")
 
 	defer r.queues.NotifyTopologyUpdateWatchers(nil, e.Object)
-	r.cache.AddOrUpdateTopology(log, e.Object)
+	// If at least one CQ becomes active after the topology is created,
+	// those CQs should now get evaluated by the scheduler.
+	if cqNames := r.cache.AddOrUpdateTopology(log, e.Object); len(cqNames) > 0 {
+		r.queues.QueueInadmissibleWorkloads(context.Background(), cqNames)
+		r.queues.Broadcast()
```

  I'm don't know why would that be needed. This is why we call `NotifyTopologyUpdateWatchers`, which should trigger the `QueueInadmissibleWorkloads` from without ClusterQueue controller. Maybe we are missing the Boardcast there?

### Review by [@copilot-pull-request-reviewer[bot]](https://github.com/apps/copilot-pull-request-reviewer) (commented) — 2026-02-06T13:39:32Z

## Pull request overview

This PR fixes a flaky test failure caused by the topology controller not properly broadcasting scheduler events when ClusterQueues (CQs) transition from pending to active state. After controller restart, when Topology create events occur after ClusterQueue create events, CQs become active but the scheduler never receives notification to process pending workloads.

**Changes:**
- Updated topology Create handler to check AddOrUpdateTopology() return value and call Broadcast() when CQs activate
- Updated topology Delete handler to check DeleteTopology() return value and requeue inadmissible workloads
- Added explanatory comments documenting the activation/broadcast behavior





---

💡 <a href="/kubernetes-sigs/kueue/new/main/.github/instructions?filename=*.instructions.md" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Add Copilot custom instructions</a> for smarter, more guided reviews. <a href="https://docs.github.com/en/copilot/customizing-copilot/adding-repository-custom-instructions-for-github-copilot" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Learn how to get started</a>.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-06T16:40:55Z

- **pkg/controller/tas/topology_controller.go:166** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-06T16:40:55Z
```diff
@@ -156,7 +161,9 @@ func (r *topologyReconciler) Delete(e event.TypedDeleteEvent[*kueue.Topology]) b
 	log.V(2).Info("Topology delete event")
 
 	defer r.queues.NotifyTopologyUpdateWatchers(e.Object, nil)
-	r.cache.DeleteTopology(log, kueue.TopologyReference(e.Object.Name))
+	if cqNames := r.cache.DeleteTopology(log, kueue.TopologyReference(e.Object.Name)); len(cqNames) > 0 {
+		r.queues.QueueInadmissibleWorkloads(context.Background(), cqNames)
+	}
```

  Yeah, good call!

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-06T16:41:09Z

- **pkg/controller/tas/topology_controller.go:150** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-06T16:41:09Z
```diff
@@ -143,7 +143,12 @@ func (r *topologyReconciler) Create(e event.TypedCreateEvent[*kueue.Topology]) b
 	log.V(2).Info("Topology create event")
 
 	defer r.queues.NotifyTopologyUpdateWatchers(nil, e.Object)
-	r.cache.AddOrUpdateTopology(log, e.Object)
+	// If at least one CQ becomes active after the topology is created,
+	// those CQs should now get evaluated by the scheduler.
+	if cqNames := r.cache.AddOrUpdateTopology(log, e.Object); len(cqNames) > 0 {
+		r.queues.QueueInadmissibleWorkloads(context.Background(), cqNames)
+		r.queues.Broadcast()
```

  The `NotifyTopologyUpdateWatchers` path fails after restart because: (1) the API CQ already has `Active: True`, so `updateCqStatusIfChanged` finds no diff and skips the update, and (2) even if it did update, `qManager.UpdateClusterQueue` won't broadcast because the queue manager CQ was already created with active=true from the API condition during `AddClusterQueue`.
  I followed the `ResourceFlavorReconciler.Create` pattern, but I agree it could be cleaner to keep queue management in the CQ controller. Happy to move the Broadcast() into NotifyTopologyUpdate for topology create events instead. Let me know your preference.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T16:44:32Z

- **pkg/controller/tas/topology_controller.go:150** by [@mimowo](https://github.com/mimowo) — 2026-02-06T16:44:32Z
```diff
@@ -143,7 +143,12 @@ func (r *topologyReconciler) Create(e event.TypedCreateEvent[*kueue.Topology]) b
 	log.V(2).Info("Topology create event")
 
 	defer r.queues.NotifyTopologyUpdateWatchers(nil, e.Object)
-	r.cache.AddOrUpdateTopology(log, e.Object)
+	// If at least one CQ becomes active after the topology is created,
+	// those CQs should now get evaluated by the scheduler.
+	if cqNames := r.cache.AddOrUpdateTopology(log, e.Object); len(cqNames) > 0 {
+		r.queues.QueueInadmissibleWorkloads(context.Background(), cqNames)
+		r.queues.Broadcast()
```

  > Let me know your preference.
  
  Could you please prototype how this could look like, potentially on another PR? How to tell without actually seeing the code.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-06T19:33:57Z

- **pkg/controller/tas/topology_controller.go:150** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-06T19:33:56Z
```diff
@@ -143,7 +143,12 @@ func (r *topologyReconciler) Create(e event.TypedCreateEvent[*kueue.Topology]) b
 	log.V(2).Info("Topology create event")
 
 	defer r.queues.NotifyTopologyUpdateWatchers(nil, e.Object)
-	r.cache.AddOrUpdateTopology(log, e.Object)
+	// If at least one CQ becomes active after the topology is created,
+	// those CQs should now get evaluated by the scheduler.
+	if cqNames := r.cache.AddOrUpdateTopology(log, e.Object); len(cqNames) > 0 {
+		r.queues.QueueInadmissibleWorkloads(context.Background(), cqNames)
+		r.queues.Broadcast()
```

  So the idea is to remove wl3 and use the following snippet here: https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/core/clusterqueue_controller.go#L200
  
  ```go
  func (r *ClusterQueueReconciler) NotifyTopologyUpdate(oldTopology, newTopology *kueue.Topology) {
  	var topology *kueue.Topology
  	switch {
  	case oldTopology == nil:
  		topology = newTopology
  	case newTopology == nil:
  		topology = oldTopology
  	default:
  		return
  	}
  	cqNames := r.cache.ClusterQueuesUsingTopology(kueue.TopologyReference(topology.Name))
  	r.nonCQObjectUpdateCh <- event.TypedGenericEvent[iter.Seq[kueue.ClusterQueueReference]]{
  		Object: slices.Values(cqNames),
  	}
  	// Broadcast to ensure the scheduler re-evaluates pending workloads
  	// for CQs that may have become active after the topology is created.
  	if oldTopology == nil {
  		r.qManager.QueueInadmissibleWorkloads(context.Background(), sets.New(cqNames...))
  		r.qManager.Broadcast()
  	}
  }
  ```
  I tested this in a loop for 100 times. I haven't seen any issues.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T19:45:42Z

- **pkg/controller/tas/topology_controller.go:150** by [@mimowo](https://github.com/mimowo) — 2026-02-06T19:45:42Z
```diff
@@ -143,7 +143,12 @@ func (r *topologyReconciler) Create(e event.TypedCreateEvent[*kueue.Topology]) b
 	log.V(2).Info("Topology create event")
 
 	defer r.queues.NotifyTopologyUpdateWatchers(nil, e.Object)
-	r.cache.AddOrUpdateTopology(log, e.Object)
+	// If at least one CQ becomes active after the topology is created,
+	// those CQs should now get evaluated by the scheduler.
+	if cqNames := r.cache.AddOrUpdateTopology(log, e.Object); len(cqNames) > 0 {
+		r.queues.QueueInadmissibleWorkloads(context.Background(), cqNames)
+		r.queues.Broadcast()
```

  And without the fix, how many retries fails for you per 100 (for comparison)?

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-06T19:49:43Z

- **pkg/controller/tas/topology_controller.go:150** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-06T19:49:43Z
```diff
@@ -143,7 +143,12 @@ func (r *topologyReconciler) Create(e event.TypedCreateEvent[*kueue.Topology]) b
 	log.V(2).Info("Topology create event")
 
 	defer r.queues.NotifyTopologyUpdateWatchers(nil, e.Object)
-	r.cache.AddOrUpdateTopology(log, e.Object)
+	// If at least one CQ becomes active after the topology is created,
+	// those CQs should now get evaluated by the scheduler.
+	if cqNames := r.cache.AddOrUpdateTopology(log, e.Object); len(cqNames) > 0 {
+		r.queues.QueueInadmissibleWorkloads(context.Background(), cqNames)
+		r.queues.Broadcast()
```

  Do you mean just removing wl3 and not adding any fix?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T19:56:26Z

- **pkg/controller/tas/topology_controller.go:150** by [@mimowo](https://github.com/mimowo) — 2026-02-06T19:56:26Z
```diff
@@ -143,7 +143,12 @@ func (r *topologyReconciler) Create(e event.TypedCreateEvent[*kueue.Topology]) b
 	log.V(2).Info("Topology create event")
 
 	defer r.queues.NotifyTopologyUpdateWatchers(nil, e.Object)
-	r.cache.AddOrUpdateTopology(log, e.Object)
+	// If at least one CQ becomes active after the topology is created,
+	// those CQs should now get evaluated by the scheduler.
+	if cqNames := r.cache.AddOrUpdateTopology(log, e.Object); len(cqNames) > 0 {
+		r.queues.QueueInadmissibleWorkloads(context.Background(), cqNames)
+		r.queues.Broadcast()
```

  yes

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-06T20:00:59Z

- **pkg/controller/tas/topology_controller.go:150** by [@mimowo](https://github.com/mimowo) — 2026-02-06T20:00:58Z
```diff
@@ -143,7 +143,12 @@ func (r *topologyReconciler) Create(e event.TypedCreateEvent[*kueue.Topology]) b
 	log.V(2).Info("Topology create event")
 
 	defer r.queues.NotifyTopologyUpdateWatchers(nil, e.Object)
-	r.cache.AddOrUpdateTopology(log, e.Object)
+	// If at least one CQ becomes active after the topology is created,
+	// those CQs should now get evaluated by the scheduler.
+	if cqNames := r.cache.AddOrUpdateTopology(log, e.Object); len(cqNames) > 0 {
+		r.queues.QueueInadmissibleWorkloads(context.Background(), cqNames)
+		r.queues.Broadcast()
```

  Ok, so overall I'm positive about the fix, but let me ask you to consider: instead of `context.Background()` could we defer calling `QueueInadmissibleWorkloads` and `Broadcast` in reaction to new generic event in `nonCQObjectUpdateCh`. Then we would have a better context. Still, I see we already use in the similar situation Background context from [resourceflavor reconciler](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/core/resourceflavor_controller.go#L145-L152)  so I don't consider this as a regression, but something we may re-think long term.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-06T21:31:37Z

- **pkg/controller/tas/topology_controller.go:150** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-06T21:31:36Z
```diff
@@ -143,7 +143,12 @@ func (r *topologyReconciler) Create(e event.TypedCreateEvent[*kueue.Topology]) b
 	log.V(2).Info("Topology create event")
 
 	defer r.queues.NotifyTopologyUpdateWatchers(nil, e.Object)
-	r.cache.AddOrUpdateTopology(log, e.Object)
+	// If at least one CQ becomes active after the topology is created,
+	// those CQs should now get evaluated by the scheduler.
+	if cqNames := r.cache.AddOrUpdateTopology(log, e.Object); len(cqNames) > 0 {
+		r.queues.QueueInadmissibleWorkloads(context.Background(), cqNames)
+		r.queues.Broadcast()
```

  >And without the fix, how many retries fails for you per 100 (for comparison)?
  
  So the test failed after 30+ runs.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-06T21:34:24Z

- **pkg/controller/tas/topology_controller.go:150** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-06T21:34:24Z
```diff
@@ -143,7 +143,12 @@ func (r *topologyReconciler) Create(e event.TypedCreateEvent[*kueue.Topology]) b
 	log.V(2).Info("Topology create event")
 
 	defer r.queues.NotifyTopologyUpdateWatchers(nil, e.Object)
-	r.cache.AddOrUpdateTopology(log, e.Object)
+	// If at least one CQ becomes active after the topology is created,
+	// those CQs should now get evaluated by the scheduler.
+	if cqNames := r.cache.AddOrUpdateTopology(log, e.Object); len(cqNames) > 0 {
+		r.queues.QueueInadmissibleWorkloads(context.Background(), cqNames)
+		r.queues.Broadcast()
```

  > Ok, so overall I'm positive about the fix, but let me ask you to consider: instead of `context.Background()` could we defer calling `QueueInadmissibleWorkloads` and `Broadcast` in reaction to new generic event in `nonCQObjectUpdateCh`. Then we would have a better context. Still, I see we already use in the similar situation Background context from [resourceflavor reconciler](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/core/resourceflavor_controller.go#L145-L152) so I don't consider this as a regression, but something we may re-think long term.
  
  Agreed, that would be cleaner long-term. For now I'll keep it consistent with the ResourceFlavor pattern to avoid over-complicating this PR, but happy to revisit in the future.
