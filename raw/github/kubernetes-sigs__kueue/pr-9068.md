# PR #9068: Semi-automated cherry pick of #8768: Add WorkloadSliceNameAnnotation and refactor pod ungating

**Summary**: Semi-automated cherry pick of #8768: Add WorkloadSliceNameAnnotation and refactor pod ungating

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9068

**Last updated**: 2026-02-21T15:51:17Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `automated-cherry-pick-of-#8768-release-0.15` → `release-0.15`
- **Created**: 2026-02-09T18:43:11Z
- **Updated**: 2026-02-21T15:51:17Z
- **Merged**: 2026-02-10T07:38:00Z
- **Closed**: 2026-02-10T07:38:00Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 7   **+180 / -55**

## Description

Cherry pick of #8768 on release-0.15.

#8768: Add WorkloadSliceNameAnnotation and refactor pod ungating 

For details on the cherry pick process, see the [cherry pick requests](https://git.k8s.io/community/contributors/devel/sig-release/cherry-picks.md) page.

```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-02-09T18:43:19Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 1f6b5c71c122c039062ce8b7880c386f244fdf8e |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/698a2ac383d7fb00084cb308 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-9068--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-9068--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTkwNjgtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.1DRzuuj73Ckio7wvAuldUETXgpm48b7UwbEeOmLy7ok)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-9068--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-09T19:09:39Z

/test pull-kueue-verify-release-0-15

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-09T19:29:55Z

LGTM label has been added.  <details>Git tree hash: 62de6ce3d2319c7088353ff8819e6c20d708228d</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-09T19:29:57Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9068#" title="Author self-approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9068#pullrequestreview-3774926830" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/release-0.15/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-09T19:35:57Z

> Once you check the new behavior, feel free to unhold this PR.

Any particular test you would like to conduct beyond what we already have in out automated tests? Do you mean upgrade?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-09T19:43:29Z

> > Once you check the new behavior, feel free to unhold this PR.
> 
> Any particular test you would like to conduct beyond what we already have in out automated tests? Do you mean upgrade?

Hopefully, manually testings sounds better... But, I'm hesitating if we should pay much verification cost for alpha feature...

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-10T07:09:39Z

> Hopefully, manually testings sounds better... But, I'm hesitating if we should pay much verification cost for alpha feature...

FYI @hiboyang is doing manual verifications for every PR against main:
- https://github.com/kubernetes-sigs/kueue/pull/8341#issuecomment-3849968423
- https://github.com/kubernetes-sigs/kueue/pull/9077#issuecomment-3875712632

So, I expect it is unlikely there would be bugs here. There was no difficult conflict to resolve.

So, I suggest to merge all the related PRs and I will also test 0.15 manually before the release tomorrow. Let me add it do the release issue.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-10T07:09:45Z

/unhold

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-10T09:33:47Z

> > Hopefully, manually testings sounds better... But, I'm hesitating if we should pay much verification cost for alpha feature...
> 
> FYI @hiboyang is doing manual verifications for every PR against main:
> 
> * [Make RayJob top level for autoscaling #8341 (comment)](https://github.com/kubernetes-sigs/kueue/pull/8341#issuecomment-3849968423)
> * [Update RayJob.PodSets method to use RayCluster to update podSets #9077 (comment)](https://github.com/kubernetes-sigs/kueue/pull/9077#issuecomment-3875712632)
> 
> So, I expect it is unlikely there would be bugs here. There was no difficult conflict to resolve.
> 
> So, I suggest to merge all the related PRs and I will also test 0.15 manually before the release tomorrow. Let me add it do the release issue.

That sounds reasonable, thank you!

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-12T19:52:55Z

/kind feature

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-09T18:49:54Z

- **pkg/workloadslicing/workloadslicing.go:114** by [@mimowo](https://github.com/mimowo) — 2026-02-09T18:49:54Z
```diff
@@ -111,6 +111,17 @@ func Finish(ctx context.Context, clnt client.Client, clk clock.Clock, workloadSl
 	return nil
 }
 
+// SliceName returns the workload slice name for the given workload.
```

  I only had a trivial conflict here with neighbouring function `Finish` (as it was deleted on main)

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-09T19:29:46Z

Changes look great to me, I left a question.

/lgtm
/approve

/hold
Once you check the new behavior, feel free to unhold this PR.

- **pkg/workloadslicing/workloadslicing.go:294** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-09T19:28:55Z
```diff
@@ -260,24 +271,40 @@ func EnsureWorkloadSlices(ctx context.Context, clnt client.Client, clk clock.Clo
 	}
 }
 
-// StartWorkloadSlicePods identifies pods associated with the provided parent object
+// StartWorkloadSlicePods identifies pods associated with the provided workload
 // that are gated by the ElasticJobSchedulingGate scheduling gate and removes this gate,
 // allowing them to be considered for scheduling.
 //
 // This function performs the following steps:
-// 1. Lists all pods in the same namespace with an OwnerReference UID matching the parent object.
-// 2. For each pod, removes the ElasticJobSchedulingGate scheduling gate if present.
+//  1. Lists all pods in the same namespace with the WorkloadSliceNameAnnotation matching
+//     the workload slice name, falling back to OwnerReference UID for backwards compatibility.
+//  2. For each pod, removes the ElasticJobSchedulingGate scheduling gate if present.
 //
 // Returns:
 // - An error if any of the operations fail; otherwise, nil.
-func StartWorkloadSlicePods(ctx context.Context, clnt client.Client, object client.Object) error {
+func StartWorkloadSlicePods(ctx context.Context, clnt client.Client, wl *kueue.Workload) error {
 	log := ctrl.LoggerFrom(ctx)
 	list := &corev1.PodList{}
-	if err := clnt.List(ctx, list, client.InNamespace(object.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(object.GetUID())}); err != nil {
-		return fmt.Errorf("failed to list job pods: %w", err)
+	sliceName := SliceName(wl)
+
+	// First try annotation-based lookup (supports JobSet and other workloads where pods
+	// are not immediate children of the job).
+	if err := clnt.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{indexer.WorkloadSliceNameKey: sliceName}); err != nil {
+		return fmt.Errorf("failed to list workload slice pods: %w", err)
 	}
```

  It seems that we use wl slice name to identify wl slice pods from this PR.
  Is there any non-working autoscale problem for the existing Workload?
  
  If yes, I believe that we should expand the release notes.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-09T19:35:15Z

- **pkg/workloadslicing/workloadslicing.go:294** by [@mimowo](https://github.com/mimowo) — 2026-02-09T19:35:15Z
```diff
@@ -260,24 +271,40 @@ func EnsureWorkloadSlices(ctx context.Context, clnt client.Client, clk clock.Clo
 	}
 }
 
-// StartWorkloadSlicePods identifies pods associated with the provided parent object
+// StartWorkloadSlicePods identifies pods associated with the provided workload
 // that are gated by the ElasticJobSchedulingGate scheduling gate and removes this gate,
 // allowing them to be considered for scheduling.
 //
 // This function performs the following steps:
-// 1. Lists all pods in the same namespace with an OwnerReference UID matching the parent object.
-// 2. For each pod, removes the ElasticJobSchedulingGate scheduling gate if present.
+//  1. Lists all pods in the same namespace with the WorkloadSliceNameAnnotation matching
+//     the workload slice name, falling back to OwnerReference UID for backwards compatibility.
+//  2. For each pod, removes the ElasticJobSchedulingGate scheduling gate if present.
 //
 // Returns:
 // - An error if any of the operations fail; otherwise, nil.
-func StartWorkloadSlicePods(ctx context.Context, clnt client.Client, object client.Object) error {
+func StartWorkloadSlicePods(ctx context.Context, clnt client.Client, wl *kueue.Workload) error {
 	log := ctrl.LoggerFrom(ctx)
 	list := &corev1.PodList{}
-	if err := clnt.List(ctx, list, client.InNamespace(object.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(object.GetUID())}); err != nil {
-		return fmt.Errorf("failed to list job pods: %w", err)
+	sliceName := SliceName(wl)
+
+	// First try annotation-based lookup (supports JobSet and other workloads where pods
+	// are not immediate children of the job).
+	if err := clnt.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{indexer.WorkloadSliceNameKey: sliceName}); err != nil {
+		return fmt.Errorf("failed to list workload slice pods: %w", err)
 	}
```

  > It seems that we use wl slice name to identify wl slice pods from this PR.
  
  Without workload-slice annotation, the PR cherrypick does not work: https://github.com/kubernetes-sigs/kueue/pull/9040
  
  > Is there any non-working autoscale problem for the existing Workload?
  
  The follow up PR https://github.com/kubernetes-sigs/kueue/pull/9040 makes the RayJob top-level. I don't know if there is any user-observable issue. However, aligning the 0.15 branch with main will help us debug / fix issues. Otherwise bugfixing 0.15 will not be possible. 
  
  We may say this is fixing the consistency issue, when we always create Workloads for top-level Job objects. RayJob was an exception.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-09T19:42:32Z

- **pkg/workloadslicing/workloadslicing.go:294** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-09T19:42:32Z
```diff
@@ -260,24 +271,40 @@ func EnsureWorkloadSlices(ctx context.Context, clnt client.Client, clk clock.Clo
 	}
 }
 
-// StartWorkloadSlicePods identifies pods associated with the provided parent object
+// StartWorkloadSlicePods identifies pods associated with the provided workload
 // that are gated by the ElasticJobSchedulingGate scheduling gate and removes this gate,
 // allowing them to be considered for scheduling.
 //
 // This function performs the following steps:
-// 1. Lists all pods in the same namespace with an OwnerReference UID matching the parent object.
-// 2. For each pod, removes the ElasticJobSchedulingGate scheduling gate if present.
+//  1. Lists all pods in the same namespace with the WorkloadSliceNameAnnotation matching
+//     the workload slice name, falling back to OwnerReference UID for backwards compatibility.
+//  2. For each pod, removes the ElasticJobSchedulingGate scheduling gate if present.
 //
 // Returns:
 // - An error if any of the operations fail; otherwise, nil.
-func StartWorkloadSlicePods(ctx context.Context, clnt client.Client, object client.Object) error {
+func StartWorkloadSlicePods(ctx context.Context, clnt client.Client, wl *kueue.Workload) error {
 	log := ctrl.LoggerFrom(ctx)
 	list := &corev1.PodList{}
-	if err := clnt.List(ctx, list, client.InNamespace(object.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(object.GetUID())}); err != nil {
-		return fmt.Errorf("failed to list job pods: %w", err)
+	sliceName := SliceName(wl)
+
+	// First try annotation-based lookup (supports JobSet and other workloads where pods
+	// are not immediate children of the job).
+	if err := clnt.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{indexer.WorkloadSliceNameKey: sliceName}); err != nil {
+		return fmt.Errorf("failed to list workload slice pods: %w", err)
 	}
```

  > The follow up PR https://github.com/kubernetes-sigs/kueue/pull/9040 makes the RayJob top-level. I don't know if there is any user-observable issue.
  
  If you don't see any problem, I think that we can merge this w/o any operation.
