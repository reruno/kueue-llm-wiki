# PR #9091: Fix generateName for StatefulSets by including UID in workload name

**Summary**: Fix generateName for StatefulSets by including UID in workload name

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9091

**Last updated**: 2026-03-05T15:17:23Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@IrvingMg](https://github.com/IrvingMg)
- **Branch**: `fix/statefulsets-generated-name` → `main`
- **Created**: 2026-02-10T12:03:19Z
- **Updated**: 2026-03-05T15:17:23Z
- **Merged**: 2026-03-05T14:30:23Z
- **Closed**: 2026-03-05T14:30:23Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@pajakd](https://github.com/pajakd), [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 16   **+612 / -108**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind bug

#### What this PR does / why we need it:

`GetWorkloadName` previously used only the StatefulSet name with an empty UID, causing workload name collisions when multiple StatefulSets shared a `generateName` prefix. This PR now includes the UID in the workload name hash.

Since the UID is not available at webhook time, labels that depend on the workload name have been moved from the webhook to the reconciler.

For MultiKueue compatibility, this PR adds `GetOwnerUID` (following the LWS pattern) so that worker-cluster copies use the management cluster’s UID via `MultiKueueOriginUIDAnnotation`.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #8687

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
StatefulSet integration: fix the bug that when using `generateName` the Workload names generated
for two different StatefulSets would conflict, not allowing to run the second StatefulSet.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-02-10T12:03:28Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 18f4277808001032c3660ec758f49388a45586eb |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69a98cb543d4980008a234c8 |

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-10T12:07:44Z

@DavideRutigliano @mbobrovskyi PTAL

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-17T11:04:07Z

@IrvingMg do you understand the issue behind the e2e tests failing https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/9091/pull-kueue-test-e2e-multikueue-main/2023423325627224064? Do we have a path forward for fixing it?

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-17T11:12:24Z

> @IrvingMg do you understand the issue behind the e2e tests failing https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/9091/pull-kueue-test-e2e-multikueue-main/2023423325627224064? Do we have a path forward for fixing it?

Yes, I’ve been looking into it and am currently working on the fix. The issue seems to be related to splitting the reconciliation process after adding the StatefulSet pod reconciler, which is causing the workload to miss the StatefulSet owner reference.

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-18T13:46:10Z

The main problem with MultiKueue tests comes from a bug in how StatefulSets were integrated into MultiKueue. (https://github.com/kubernetes-sigs/kueue/pull/8611). The issue is that MultiKueue uses the pod adapter instead of the StatefulSet adapter, so it treats the StatefulSet as just a group of pods. Because of this, no StatefulSet is created in the worker cluster.

I have created a new PR to fix this issue: https://github.com/kubernetes-sigs/kueue/pull/9344

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-03T11:28:58Z

/release-note-edit
```release-note
StatefulSet integration: fix the bug that when using `generateName` the Workload names generated
for two different StatefulSets would conflict, not allowing to run the second StatefulSet. 
```

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-03T11:32:24Z

Looks great, but I have a doubt about pre-existing StatefulSets. Can you verify what will happen with them? 
In particular, I think we may create another Workload resulting in double-counting the quota. Ideally, we can handle that gracefully and continue to operate using the old Workloads, or re-create them with minimal downtime. Let me know what you find out.

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-04T15:06:22Z

> Looks great, but I have a doubt about pre-existing StatefulSets. Can you verify what will happen with them? In particular, I think we may create another Workload resulting in double-counting the quota. Ideally, we can handle that gracefully and continue to operate using the old Workloads, or re-create them with minimal downtime. Let me know what you find out.

I manually checked the behavior and confirmed that workload duplication and double-counted quota do happen.

To fix this, I added a fallback for existing STS. During reconciliation, if the workload is not found with the new UID-based name, the controller checks the old (non-UID) name. If it finds an existing workload with the old name, it reuses it instead of creating a new one. This prevents duplicate workloads and avoids double-counting quota, without downtime.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-04T15:14:20Z

Thank you. I would like two things still:
1. annotate the fallback code with a comment that it can be deleted in 0.20+
2. do we have an integration test to demonstrate the fallback works? Please annotate the test to be removed

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-04T17:48:40Z

> Thank you. I would like two things still:
> 
> 1. annotate the fallback code with a comment that it can be deleted in 0.20+
> 2. do we have an integration test to demonstrate the fallback works? Please annotate the test to be removed

Done. I've updated the TODO comments and added integration tests for STS.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-05T13:37:53Z

LGTM label has been added.  <details>Git tree hash: 73a7f3d7068812258c40faada9573bcedd778562</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-05T13:37:55Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9091#" title="Author self-approved">IrvingMg</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9091#pullrequestreview-3896551003" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-05T14:15:45Z

/hold cancel
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-05T14:15:52Z

LGTM label has been added.  <details>Git tree hash: ea32ee7230c2d4b75bb1316be18aee71e8d2ded9</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-05T15:16:08Z

/cherrypick release-0.16
/cherrypick release-0.15

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-05T15:16:21Z

@IrvingMg please prepare cherrypicks if the robot fails

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-05T15:16:48Z

@mimowo: #9091 failed to apply on top of branch "release-0.15":
```
Applying: Fix generateName for StatefulSets by including UID in workload name
Using index info to reconstruct a base tree...
M	pkg/controller/jobs/statefulset/statefulset_controller.go
A	pkg/controller/jobs/statefulset/statefulset_multikueue_adapter.go
A	pkg/controller/jobs/statefulset/statefulset_multikueue_adapter_test.go
A	pkg/controller/jobs/statefulset/statefulset_pod_reconciler.go
A	pkg/controller/jobs/statefulset/statefulset_pod_reconciler_test.go
M	pkg/controller/jobs/statefulset/statefulset_reconciler.go
M	pkg/controller/jobs/statefulset/statefulset_reconciler_test.go
M	pkg/controller/jobs/statefulset/statefulset_webhook.go
M	pkg/controller/jobs/statefulset/statefulset_webhook_test.go
M	test/e2e/customconfigs/managejobswithoutqueuename_test.go
M	test/e2e/multikueue/e2e_test.go
M	test/e2e/singlecluster/statefulset_test.go
Falling back to patching base and 3-way merge...
Auto-merging test/e2e/singlecluster/statefulset_test.go
Auto-merging test/e2e/multikueue/e2e_test.go
CONFLICT (content): Merge conflict in test/e2e/multikueue/e2e_test.go
Auto-merging test/e2e/customconfigs/managejobswithoutqueuename_test.go
Auto-merging pkg/controller/jobs/statefulset/statefulset_webhook_test.go
Auto-merging pkg/controller/jobs/statefulset/statefulset_webhook.go
CONFLICT (content): Merge conflict in pkg/controller/jobs/statefulset/statefulset_webhook.go
Auto-merging pkg/controller/jobs/statefulset/statefulset_reconciler_test.go
CONFLICT (content): Merge conflict in pkg/controller/jobs/statefulset/statefulset_reconciler_test.go
Auto-merging pkg/controller/jobs/statefulset/statefulset_reconciler.go
CONFLICT (content): Merge conflict in pkg/controller/jobs/statefulset/statefulset_reconciler.go
CONFLICT (modify/delete): pkg/controller/jobs/statefulset/statefulset_pod_reconciler_test.go deleted in HEAD and modified in Fix generateName for StatefulSets by including UID in workload name. Version Fix generateName for StatefulSets by including UID in workload name of pkg/controller/jobs/statefulset/statefulset_pod_reconciler_test.go left in tree.
CONFLICT (modify/delete): pkg/controller/jobs/statefulset/statefulset_multikueue_adapter_test.go deleted in HEAD and modified in Fix generateName for StatefulSets by including UID in workload name. Version Fix generateName for StatefulSets by including UID in workload name of pkg/controller/jobs/statefulset/statefulset_multikueue_adapter_test.go left in tree.
CONFLICT (modify/delete): pkg/controller/jobs/statefulset/statefulset_multikueue_adapter.go deleted in HEAD and modified in Fix generateName for StatefulSets by including UID in workload name. Version Fix generateName for StatefulSets by including UID in workload name of pkg/controller/jobs/statefulset/statefulset_multikueue_adapter.go left in tree.
Auto-merging pkg/controller/jobs/statefulset/statefulset_controller.go
Auto-merging pkg/controller/jobs/leaderworkerset/leaderworkerset_pod_reconciler.go
CONFLICT (content): Merge conflict in pkg/controller/jobs/leaderworkerset/leaderworkerset_pod_reconciler.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Fix generateName for StatefulSets by including UID in workload name

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9091#issuecomment-4005805677):

>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-05T15:17:23Z

@mimowo: #9091 failed to apply on top of branch "release-0.16":
```
Applying: Fix generateName for StatefulSets by including UID in workload name
Using index info to reconstruct a base tree...
M	pkg/controller/jobs/statefulset/statefulset_multikueue_adapter.go
M	pkg/controller/jobs/statefulset/statefulset_reconciler.go
M	pkg/controller/jobs/statefulset/statefulset_webhook.go
M	pkg/controller/jobs/statefulset/statefulset_webhook_test.go
M	test/e2e/customconfigs/managejobswithoutqueuename_test.go
M	test/e2e/multikueue/e2e_test.go
Falling back to patching base and 3-way merge...
Auto-merging test/e2e/multikueue/e2e_test.go
Auto-merging test/e2e/customconfigs/managejobswithoutqueuename_test.go
Auto-merging pkg/controller/jobs/statefulset/statefulset_webhook_test.go
Auto-merging pkg/controller/jobs/statefulset/statefulset_webhook.go
CONFLICT (content): Merge conflict in pkg/controller/jobs/statefulset/statefulset_webhook.go
Auto-merging pkg/controller/jobs/statefulset/statefulset_reconciler.go
Auto-merging pkg/controller/jobs/statefulset/statefulset_multikueue_adapter.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Fix generateName for StatefulSets by including UID in workload name

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9091#issuecomment-4005805677):

>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-11T16:14:02Z

- **pkg/controller/jobs/statefulset/statefulset_reconciler.go:128** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-11T16:14:02Z
```diff
@@ -104,6 +115,61 @@ func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	return ctrl.Result{}, err
 }
 
+func (r *Reconciler) listPods(ctx context.Context, req reconcile.Request, sts *appsv1.StatefulSet) ([]corev1.Pod, error) {
+	podList := &corev1.PodList{}
+	if sts != nil {
+		if err := r.client.List(ctx, podList,
+			client.InNamespace(req.Namespace),
+			client.MatchingLabels(sts.Spec.Selector.MatchLabels),
+		); err != nil {
+			return nil, err
+		}
+		return slices.DeleteFunc(podList.Items, func(pod corev1.Pod) bool {
+			return !isManagedByStatefulSet(&pod, sts.UID)
```

  I don’t think we need this check. We already select the pods using matchLabels, which gives us exactly the pods we need.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-11T16:17:00Z

- **pkg/controller/jobs/statefulset/statefulset_reconciler.go:140** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-11T16:17:00Z
```diff
@@ -104,6 +115,61 @@ func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	return ctrl.Result{}, err
 }
 
+func (r *Reconciler) listPods(ctx context.Context, req reconcile.Request, sts *appsv1.StatefulSet) ([]corev1.Pod, error) {
+	podList := &corev1.PodList{}
+	if sts != nil {
+		if err := r.client.List(ctx, podList,
+			client.InNamespace(req.Namespace),
+			client.MatchingLabels(sts.Spec.Selector.MatchLabels),
+		); err != nil {
+			return nil, err
+		}
+		return slices.DeleteFunc(podList.Items, func(pod corev1.Pod) bool {
+			return !isManagedByStatefulSet(&pod, sts.UID)
+		}), nil
+	}
+
+	if err := r.client.List(ctx, podList,
+		client.InNamespace(req.Namespace),
+		client.MatchingLabels{constants.ManagedByKueueLabelKey: constants.ManagedByKueueLabelValue},
+	); err != nil {
+		return nil, err
+	}
+	return slices.DeleteFunc(podList.Items, func(pod corev1.Pod) bool {
+		return !isOwnedByStatefulSetName(&pod, req.Name)
+	}), nil
```

  This is not good. There could be thousands of pods in this namespace. We need to add indexer for this.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-11T16:18:24Z

- **pkg/controller/jobs/statefulset/statefulset_reconciler.go:48** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-11T16:18:24Z
```diff
@@ -41,8 +45,10 @@ import (
 	"sigs.k8s.io/controller-runtime/pkg/reconcile"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
+	controllerconstants "sigs.k8s.io/kueue/pkg/controller/constants"
 	"sigs.k8s.io/kueue/pkg/controller/jobframework"
-	podcontroller "sigs.k8s.io/kueue/pkg/controller/jobs/pod/constants"
+	podconstants "sigs.k8s.io/kueue/pkg/controller/jobs/pod/constants"
```

  Could you please create prepare PR to reduce changes?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-11T16:23:02Z

- **pkg/controller/jobs/statefulset/statefulset_reconciler.go:137** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-11T16:23:02Z
```diff
@@ -104,6 +115,61 @@ func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	return ctrl.Result{}, err
 }
 
+func (r *Reconciler) listPods(ctx context.Context, req reconcile.Request, sts *appsv1.StatefulSet) ([]corev1.Pod, error) {
+	podList := &corev1.PodList{}
+	if sts != nil {
+		if err := r.client.List(ctx, podList,
+			client.InNamespace(req.Namespace),
+			client.MatchingLabels(sts.Spec.Selector.MatchLabels),
+		); err != nil {
+			return nil, err
+		}
+		return slices.DeleteFunc(podList.Items, func(pod corev1.Pod) bool {
+			return !isManagedByStatefulSet(&pod, sts.UID)
+		}), nil
+	}
+
+	if err := r.client.List(ctx, podList,
+		client.InNamespace(req.Namespace),
+		client.MatchingLabels{constants.ManagedByKueueLabelKey: constants.ManagedByKueueLabelValue},
+	); err != nil {
+		return nil, err
+	}
```

  I also thinking about case when we don't have statefulset and don't have ownerReference. In that case it could be possible zombie pods.
  
  [Here](https://github.com/epam/kubernetes-kueue/blob/d1344c5dc1fadd280af0499077dce85d4cc0d438/pkg/controller/jobs/statefulset/statefulset_reconciler.go#L360-L363) we catch ADDED event. But still we skipping pods without ownerReference [here](https://github.com/epam/kubernetes-kueue/blob/d1344c5dc1fadd280af0499077dce85d4cc0d438/pkg/controller/jobs/statefulset/statefulset_reconciler.go#L376-L387).
  
  I would propose creating a separate statufelset_pod_controller for this, similar to what we did in LWS, to prevent this case.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-11T16:24:23Z

- **pkg/controller/jobs/statefulset/statefulset_reconciler.go:145** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-11T16:24:23Z
```diff
@@ -104,6 +115,61 @@ func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	return ctrl.Result{}, err
 }
 
+func (r *Reconciler) listPods(ctx context.Context, req reconcile.Request, sts *appsv1.StatefulSet) ([]corev1.Pod, error) {
+	podList := &corev1.PodList{}
+	if sts != nil {
+		if err := r.client.List(ctx, podList,
+			client.InNamespace(req.Namespace),
+			client.MatchingLabels(sts.Spec.Selector.MatchLabels),
+		); err != nil {
+			return nil, err
+		}
+		return slices.DeleteFunc(podList.Items, func(pod corev1.Pod) bool {
+			return !isManagedByStatefulSet(&pod, sts.UID)
+		}), nil
+	}
+
+	if err := r.client.List(ctx, podList,
+		client.InNamespace(req.Namespace),
+		client.MatchingLabels{constants.ManagedByKueueLabelKey: constants.ManagedByKueueLabelValue},
+	); err != nil {
+		return nil, err
+	}
+	return slices.DeleteFunc(podList.Items, func(pod corev1.Pod) bool {
+		return !isOwnedByStatefulSetName(&pod, req.Name)
+	}), nil
+}
+
+func (r *Reconciler) labelPods(ctx context.Context, workloadName string, sts *appsv1.StatefulSet, pods []corev1.Pod) error {
+	log := ctrl.LoggerFrom(ctx)
+	queueName := string(jobframework.QueueNameForObject(sts))
```

  ```suggestion
  	queueName := jobframework.QueueNameForObject(sts)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-11T16:28:47Z

- **pkg/controller/jobs/statefulset/statefulset_reconciler_test.go:40** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-11T16:28:47Z
```diff
@@ -36,6 +37,8 @@ import (
 	statefulsettesting "sigs.k8s.io/kueue/pkg/util/testingjobs/statefulset"
 )
 
+const testUID = types.UID("sts")
```

  ```suggestion
  const testUID = "sts"
  ```

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-02-16T09:11:10Z

- **pkg/controller/jobs/statefulset/statefulset_reconciler.go:48** by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-16T09:11:10Z
```diff
@@ -41,8 +45,10 @@ import (
 	"sigs.k8s.io/controller-runtime/pkg/reconcile"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
+	controllerconstants "sigs.k8s.io/kueue/pkg/controller/constants"
 	"sigs.k8s.io/kueue/pkg/controller/jobframework"
-	podcontroller "sigs.k8s.io/kueue/pkg/controller/jobs/pod/constants"
+	podconstants "sigs.k8s.io/kueue/pkg/controller/jobs/pod/constants"
```

  Opened PR: https://github.com/kubernetes-sigs/kueue/pull/9274

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-02-16T15:45:29Z

- **pkg/controller/jobs/statefulset/statefulset_reconciler_test.go:40** by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-16T15:45:29Z
```diff
@@ -36,6 +37,8 @@ import (
 	statefulsettesting "sigs.k8s.io/kueue/pkg/util/testingjobs/statefulset"
 )
 
+const testUID = types.UID("sts")
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-02-16T15:45:44Z

- **pkg/controller/jobs/statefulset/statefulset_reconciler.go:145** by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-16T15:45:44Z
```diff
@@ -104,6 +115,61 @@ func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	return ctrl.Result{}, err
 }
 
+func (r *Reconciler) listPods(ctx context.Context, req reconcile.Request, sts *appsv1.StatefulSet) ([]corev1.Pod, error) {
+	podList := &corev1.PodList{}
+	if sts != nil {
+		if err := r.client.List(ctx, podList,
+			client.InNamespace(req.Namespace),
+			client.MatchingLabels(sts.Spec.Selector.MatchLabels),
+		); err != nil {
+			return nil, err
+		}
+		return slices.DeleteFunc(podList.Items, func(pod corev1.Pod) bool {
+			return !isManagedByStatefulSet(&pod, sts.UID)
+		}), nil
+	}
+
+	if err := r.client.List(ctx, podList,
+		client.InNamespace(req.Namespace),
+		client.MatchingLabels{constants.ManagedByKueueLabelKey: constants.ManagedByKueueLabelValue},
+	); err != nil {
+		return nil, err
+	}
+	return slices.DeleteFunc(podList.Items, func(pod corev1.Pod) bool {
+		return !isOwnedByStatefulSetName(&pod, req.Name)
+	}), nil
+}
+
+func (r *Reconciler) labelPods(ctx context.Context, workloadName string, sts *appsv1.StatefulSet, pods []corev1.Pod) error {
+	log := ctrl.LoggerFrom(ctx)
+	queueName := string(jobframework.QueueNameForObject(sts))
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-02-16T15:47:40Z

- **pkg/controller/jobs/statefulset/statefulset_reconciler.go:128** by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-16T15:47:40Z
```diff
@@ -104,6 +115,61 @@ func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	return ctrl.Result{}, err
 }
 
+func (r *Reconciler) listPods(ctx context.Context, req reconcile.Request, sts *appsv1.StatefulSet) ([]corev1.Pod, error) {
+	podList := &corev1.PodList{}
+	if sts != nil {
+		if err := r.client.List(ctx, podList,
+			client.InNamespace(req.Namespace),
+			client.MatchingLabels(sts.Spec.Selector.MatchLabels),
+		); err != nil {
+			return nil, err
+		}
+		return slices.DeleteFunc(podList.Items, func(pod corev1.Pod) bool {
+			return !isManagedByStatefulSet(&pod, sts.UID)
```

  Dropped it.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-02-16T15:49:57Z

- **pkg/controller/jobs/statefulset/statefulset_reconciler.go:140** by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-16T15:49:56Z
```diff
@@ -104,6 +115,61 @@ func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	return ctrl.Result{}, err
 }
 
+func (r *Reconciler) listPods(ctx context.Context, req reconcile.Request, sts *appsv1.StatefulSet) ([]corev1.Pod, error) {
+	podList := &corev1.PodList{}
+	if sts != nil {
+		if err := r.client.List(ctx, podList,
+			client.InNamespace(req.Namespace),
+			client.MatchingLabels(sts.Spec.Selector.MatchLabels),
+		); err != nil {
+			return nil, err
+		}
+		return slices.DeleteFunc(podList.Items, func(pod corev1.Pod) bool {
+			return !isManagedByStatefulSet(&pod, sts.UID)
+		}), nil
+	}
+
+	if err := r.client.List(ctx, podList,
+		client.InNamespace(req.Namespace),
+		client.MatchingLabels{constants.ManagedByKueueLabelKey: constants.ManagedByKueueLabelValue},
+	); err != nil {
+		return nil, err
+	}
+	return slices.DeleteFunc(podList.Items, func(pod corev1.Pod) bool {
+		return !isOwnedByStatefulSetName(&pod, req.Name)
+	}), nil
```

  As the pod reconciler has been added, this logic was removed.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-02-16T15:50:39Z

- **pkg/controller/jobs/statefulset/statefulset_reconciler.go:137** by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-16T15:50:39Z
```diff
@@ -104,6 +115,61 @@ func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	return ctrl.Result{}, err
 }
 
+func (r *Reconciler) listPods(ctx context.Context, req reconcile.Request, sts *appsv1.StatefulSet) ([]corev1.Pod, error) {
+	podList := &corev1.PodList{}
+	if sts != nil {
+		if err := r.client.List(ctx, podList,
+			client.InNamespace(req.Namespace),
+			client.MatchingLabels(sts.Spec.Selector.MatchLabels),
+		); err != nil {
+			return nil, err
+		}
+		return slices.DeleteFunc(podList.Items, func(pod corev1.Pod) bool {
+			return !isManagedByStatefulSet(&pod, sts.UID)
+		}), nil
+	}
+
+	if err := r.client.List(ctx, podList,
+		client.InNamespace(req.Namespace),
+		client.MatchingLabels{constants.ManagedByKueueLabelKey: constants.ManagedByKueueLabelValue},
+	); err != nil {
+		return nil, err
+	}
```

  Done. I've added `pkg/controller/jobs/statefulset/statefulset_pod_reconciler.go`.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-16T16:11:22Z

- **pkg/controller/jobs/statefulset/statefulset_multikueue_adapter.go:76** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-16T16:11:22Z
```diff
@@ -72,6 +72,11 @@ func (b *multiKueueAdapter) SyncJob(ctx context.Context, localClient client.Clie
 	remoteStatefulSet.Labels[constants.PrebuiltWorkloadLabel] = workloadName
 	remoteStatefulSet.Labels[kueue.MultiKueueOriginLabel] = origin
 
+	if remoteStatefulSet.Annotations == nil {
+		remoteStatefulSet.Annotations = map[string]string{}
```

  ```suggestion
  		remoteStatefulSet.Annotations = make(map[string]string, 1)
  ```

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-02-24T11:19:50Z

- **pkg/controller/jobs/statefulset/statefulset_pod_reconciler.go:4** by [@mszadkow](https://github.com/mszadkow) — 2026-02-24T11:19:51Z
```diff
@@ -0,0 +1,261 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
```

  This file seems to be un-relevant refactoring, can we extract this change to a separate PR?

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-02-24T12:04:00Z

- **pkg/controller/jobs/statefulset/statefulset_pod_reconciler.go:4** by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-24T12:04:00Z
```diff
@@ -0,0 +1,261 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
```

  Yeah, this PR will be based, once https://github.com/kubernetes-sigs/kueue/pull/9344 is merged.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-02-27T15:55:00Z

- **pkg/controller/jobs/statefulset/statefulset_pod_reconciler.go:4** by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-27T15:55:00Z
```diff
@@ -0,0 +1,261 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
```

  Done. PR rebased.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-04T18:37:33Z

- **pkg/controller/jobs/statefulset/statefulset_webhook.go:201** by [@mimowo](https://github.com/mimowo) — 2026-03-04T18:37:33Z
```diff
@@ -185,14 +186,26 @@ func (wh *Webhook) ValidateUpdate(ctx context.Context, oldSTSObj, newSTSObj *app
 				allErrs = append(allErrs, field.Forbidden(replicasPath, "scaling down is still in progress"))
 			} else {
 				// Block if workload is still being deleted
-				workloadName := GetWorkloadName(oldStatefulSet.GetName())
-				wlKey := client.ObjectKey{Namespace: oldStatefulSet.GetNamespace(), Name: workloadName}
+				workloadName := GetWorkloadName(oldStatefulSet.GetUID(), oldStatefulSet.GetName())
+				ns := oldStatefulSet.GetNamespace()
 				var wl kueue.Workload
-				err := wh.client.Get(ctx, wlKey, &wl)
-				if client.IgnoreNotFound(err) != nil {
+				err := wh.client.Get(ctx, client.ObjectKey{Namespace: ns, Name: workloadName}, &wl)
+				switch {
+				case client.IgnoreNotFound(err) != nil:
 					return nil, err
-				} else if err == nil {
+				case err == nil:
 					allErrs = append(allErrs, field.Forbidden(replicasPath, "workload from previous scale-down is still being deleted"))
+				default:
+					// TODO(v0.20): Remove legacy fallback.
+					legacyName := GetWorkloadName("", oldStatefulSet.GetName())
+					if legacyName != workloadName {
```

  Why the check ` legacyName != workloadName`. IIUC this is just a micro optimization to skip extra Get which will happen once in a million: `16^5`. Unless I'm missing something we can just save on complexity (and also cover the code better in tests, I doubt both branches of the check are covered in tests currently).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-04T18:39:05Z

- **pkg/controller/jobs/statefulset/statefulset_reconciler.go:92** by [@mimowo](https://github.com/mimowo) — 2026-03-04T18:39:05Z
```diff
@@ -79,21 +79,31 @@ func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	log := ctrl.LoggerFrom(ctx)
 	log.V(2).Info("Reconcile StatefulSet")
 
-	podList := &corev1.PodList{}
-	if err := r.client.List(ctx, podList, client.InNamespace(req.Namespace), client.MatchingLabels{
-		podconstants.GroupNameLabel: GetWorkloadName(req.Name),
-	}); err != nil {
-		return ctrl.Result{}, err
+	sts := &appsv1.StatefulSet{}
+	if err := r.client.Get(ctx, req.NamespacedName, sts); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
-	sts := &appsv1.StatefulSet{}
-	err := r.client.Get(ctx, req.NamespacedName, sts)
-	if client.IgnoreNotFound(err) != nil {
-		return ctrl.Result{}, err
+	wlName := GetWorkloadName(GetOwnerUID(sts), sts.Name)
+
+	// TODO(v0.20): Remove legacy fallback.
+	// Fall back to pre-v0.17 workload name (without UID) for upgrade compatibility.
+	legacyName := GetWorkloadName("", sts.Name)
+	if legacyName != wlName {
```

  This check looks extra as commented in :  https://github.com/kubernetes-sigs/kueue/pull/9091/changes#r2885425499
  
  However, I'm also thinking if we could commonize the code by a helper like `determineWorkloadName`, and the fallback would only be there.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-04T18:39:43Z

LGTM, just nits to deduplicate the checks for the legacy wlName fallback

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-05T09:04:51Z

- **pkg/controller/jobs/statefulset/statefulset_webhook.go:201** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-05T09:04:51Z
```diff
@@ -185,14 +186,26 @@ func (wh *Webhook) ValidateUpdate(ctx context.Context, oldSTSObj, newSTSObj *app
 				allErrs = append(allErrs, field.Forbidden(replicasPath, "scaling down is still in progress"))
 			} else {
 				// Block if workload is still being deleted
-				workloadName := GetWorkloadName(oldStatefulSet.GetName())
-				wlKey := client.ObjectKey{Namespace: oldStatefulSet.GetNamespace(), Name: workloadName}
+				workloadName := GetWorkloadName(oldStatefulSet.GetUID(), oldStatefulSet.GetName())
+				ns := oldStatefulSet.GetNamespace()
 				var wl kueue.Workload
-				err := wh.client.Get(ctx, wlKey, &wl)
-				if client.IgnoreNotFound(err) != nil {
+				err := wh.client.Get(ctx, client.ObjectKey{Namespace: ns, Name: workloadName}, &wl)
+				switch {
+				case client.IgnoreNotFound(err) != nil:
 					return nil, err
-				} else if err == nil {
+				case err == nil:
 					allErrs = append(allErrs, field.Forbidden(replicasPath, "workload from previous scale-down is still being deleted"))
+				default:
+					// TODO(v0.20): Remove legacy fallback.
+					legacyName := GetWorkloadName("", oldStatefulSet.GetName())
+					if legacyName != workloadName {
```

  Yes, the idea was to skip an extra get, but I've dropped it.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-05T09:05:51Z

- **pkg/controller/jobs/statefulset/statefulset_reconciler.go:92** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-05T09:05:51Z
```diff
@@ -79,21 +79,31 @@ func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	log := ctrl.LoggerFrom(ctx)
 	log.V(2).Info("Reconcile StatefulSet")
 
-	podList := &corev1.PodList{}
-	if err := r.client.List(ctx, podList, client.InNamespace(req.Namespace), client.MatchingLabels{
-		podconstants.GroupNameLabel: GetWorkloadName(req.Name),
-	}); err != nil {
-		return ctrl.Result{}, err
+	sts := &appsv1.StatefulSet{}
+	if err := r.client.Get(ctx, req.NamespacedName, sts); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
-	sts := &appsv1.StatefulSet{}
-	err := r.client.Get(ctx, req.NamespacedName, sts)
-	if client.IgnoreNotFound(err) != nil {
-		return ctrl.Result{}, err
+	wlName := GetWorkloadName(GetOwnerUID(sts), sts.Name)
+
+	// TODO(v0.20): Remove legacy fallback.
+	// Fall back to pre-v0.17 workload name (without UID) for upgrade compatibility.
+	legacyName := GetWorkloadName("", sts.Name)
+	if legacyName != wlName {
```

  Done. I've commonized it in the `findWorkloadName` func.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T09:51:59Z

- **pkg/controller/jobs/statefulset/statefulset_webhook.go:205** by [@mimowo](https://github.com/mimowo) — 2026-03-05T09:51:59Z
```diff
@@ -185,14 +186,24 @@ func (wh *Webhook) ValidateUpdate(ctx context.Context, oldSTSObj, newSTSObj *app
 				allErrs = append(allErrs, field.Forbidden(replicasPath, "scaling down is still in progress"))
 			} else {
 				// Block if workload is still being deleted
-				workloadName := GetWorkloadName(oldStatefulSet.GetName())
-				wlKey := client.ObjectKey{Namespace: oldStatefulSet.GetNamespace(), Name: workloadName}
+				workloadName := GetWorkloadName(oldStatefulSet.GetUID(), oldStatefulSet.GetName())
+				ns := oldStatefulSet.GetNamespace()
 				var wl kueue.Workload
-				err := wh.client.Get(ctx, wlKey, &wl)
-				if client.IgnoreNotFound(err) != nil {
+				err := wh.client.Get(ctx, client.ObjectKey{Namespace: ns, Name: workloadName}, &wl)
+				switch {
+				case client.IgnoreNotFound(err) != nil:
 					return nil, err
-				} else if err == nil {
+				case err == nil:
 					allErrs = append(allErrs, field.Forbidden(replicasPath, "workload from previous scale-down is still being deleted"))
+				default:
+					// TODO(v0.20): Remove legacy fallback.
+					legacyName := GetWorkloadName("", oldStatefulSet.GetName())
+					err = wh.client.Get(ctx, client.ObjectKey{Namespace: ns, Name: legacyName}, &wl)
+					if client.IgnoreNotFound(err) != nil {
+						return nil, err
+					} else if err == nil {
+						allErrs = append(allErrs, field.Forbidden(replicasPath, "workload from previous scale-down is still being deleted"))
```

  Hm, could we restructure the code to use the findWorkloadName helper?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T09:52:33Z

- **pkg/controller/jobs/statefulset/statefulset_pod_reconciler.go:147** by [@mimowo](https://github.com/mimowo) — 2026-03-05T09:52:33Z
```diff
@@ -129,7 +130,21 @@ func (r *PodReconciler) setDefault(ctx context.Context, pod *corev1.Pod) (bool,
 	}
 
 	queueName := jobframework.QueueNameForObject(sts)
-	wlName := GetWorkloadName(sts.Name)
+	wlName := GetWorkloadName(GetOwnerUID(sts), sts.Name)
+
+	// TODO(v0.20): Remove legacy fallback.
+	// Fall back to pre-v0.17 workload name (without UID) for upgrade compatibility.
+	if pod.Labels[podconstants.GroupNameLabel] != wlName {
+		legacyName := GetWorkloadName("", sts.Name)
+		if legacyName != wlName {
+			wl := &kueue.Workload{}
+			if err := r.client.Get(ctx, client.ObjectKey{Namespace: pod.Namespace, Name: legacyName}, wl); err == nil {
+				wlName = legacyName
+			} else if !apierrors.IsNotFound(err) {
+				return false, err
+			}
+		}
+	}
```

  Could we restructure the code to us use the findWorkloadName helper?

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-05T10:54:20Z

- **pkg/controller/jobs/statefulset/statefulset_webhook.go:205** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-05T10:54:20Z
```diff
@@ -185,14 +186,24 @@ func (wh *Webhook) ValidateUpdate(ctx context.Context, oldSTSObj, newSTSObj *app
 				allErrs = append(allErrs, field.Forbidden(replicasPath, "scaling down is still in progress"))
 			} else {
 				// Block if workload is still being deleted
-				workloadName := GetWorkloadName(oldStatefulSet.GetName())
-				wlKey := client.ObjectKey{Namespace: oldStatefulSet.GetNamespace(), Name: workloadName}
+				workloadName := GetWorkloadName(oldStatefulSet.GetUID(), oldStatefulSet.GetName())
+				ns := oldStatefulSet.GetNamespace()
 				var wl kueue.Workload
-				err := wh.client.Get(ctx, wlKey, &wl)
-				if client.IgnoreNotFound(err) != nil {
+				err := wh.client.Get(ctx, client.ObjectKey{Namespace: ns, Name: workloadName}, &wl)
+				switch {
+				case client.IgnoreNotFound(err) != nil:
 					return nil, err
-				} else if err == nil {
+				case err == nil:
 					allErrs = append(allErrs, field.Forbidden(replicasPath, "workload from previous scale-down is still being deleted"))
+				default:
+					// TODO(v0.20): Remove legacy fallback.
+					legacyName := GetWorkloadName("", oldStatefulSet.GetName())
+					err = wh.client.Get(ctx, client.ObjectKey{Namespace: ns, Name: legacyName}, &wl)
+					if client.IgnoreNotFound(err) != nil {
+						return nil, err
+					} else if err == nil {
+						allErrs = append(allErrs, field.Forbidden(replicasPath, "workload from previous scale-down is still being deleted"))
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-05T10:54:28Z

- **pkg/controller/jobs/statefulset/statefulset_pod_reconciler.go:147** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-05T10:54:28Z
```diff
@@ -129,7 +130,21 @@ func (r *PodReconciler) setDefault(ctx context.Context, pod *corev1.Pod) (bool,
 	}
 
 	queueName := jobframework.QueueNameForObject(sts)
-	wlName := GetWorkloadName(sts.Name)
+	wlName := GetWorkloadName(GetOwnerUID(sts), sts.Name)
+
+	// TODO(v0.20): Remove legacy fallback.
+	// Fall back to pre-v0.17 workload name (without UID) for upgrade compatibility.
+	if pod.Labels[podconstants.GroupNameLabel] != wlName {
+		legacyName := GetWorkloadName("", sts.Name)
+		if legacyName != wlName {
+			wl := &kueue.Workload{}
+			if err := r.client.Get(ctx, client.ObjectKey{Namespace: pod.Namespace, Name: legacyName}, wl); err == nil {
+				wlName = legacyName
+			} else if !apierrors.IsNotFound(err) {
+				return false, err
+			}
+		}
+	}
```

  Done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T13:37:44Z

Thank you 👍 
/lgtm
/approve
/cherrypick release-0.16
/cherrypick release-0.15

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T13:43:23Z

- **pkg/controller/jobs/statefulset/statefulset_reconciler.go:191** by [@mimowo](https://github.com/mimowo) — 2026-03-05T13:43:23Z
```diff
@@ -181,6 +177,22 @@ func (r *Reconciler) syncQueueLabel(ctx context.Context, sts *appsv1.StatefulSet
 	})
 }
 
+// findWorkloadName returns the workload name for the given StatefulSet,
+// falling back to the legacy name (without UID) if no workload exists under the new name.
+// TODO(#9497, v0.20): Remove legacy fallback.
+func findWorkloadName(ctx context.Context, c client.Client, sts *appsv1.StatefulSet) string {
+	wlName := GetWorkloadName(GetOwnerUID(sts), sts.Name)
+	wl := &kueue.Workload{}
+	if apierrors.IsNotFound(c.Get(ctx, client.ObjectKey{Namespace: sts.Namespace, Name: wlName}, wl)) {
+		legacyName := GetWorkloadName("", sts.Name)
+		if c.Get(ctx, client.ObjectKey{Namespace: sts.Namespace, Name: legacyName}, wl) == nil {
+			ctrl.LoggerFrom(ctx).V(3).Info("Using legacy workload name", "legacyName", legacyName, "newName", wlName)
+			return legacyName
+		}
```

  /hold

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T13:44:52Z

- **pkg/controller/jobs/statefulset/statefulset_reconciler.go:191** by [@mimowo](https://github.com/mimowo) — 2026-03-05T13:44:52Z
```diff
@@ -181,6 +177,22 @@ func (r *Reconciler) syncQueueLabel(ctx context.Context, sts *appsv1.StatefulSet
 	})
 }
 
+// findWorkloadName returns the workload name for the given StatefulSet,
+// falling back to the legacy name (without UID) if no workload exists under the new name.
+// TODO(#9497, v0.20): Remove legacy fallback.
+func findWorkloadName(ctx context.Context, c client.Client, sts *appsv1.StatefulSet) string {
+	wlName := GetWorkloadName(GetOwnerUID(sts), sts.Name)
+	wl := &kueue.Workload{}
+	if apierrors.IsNotFound(c.Get(ctx, client.ObjectKey{Namespace: sts.Namespace, Name: wlName}, wl)) {
+		legacyName := GetWorkloadName("", sts.Name)
+		if c.Get(ctx, client.ObjectKey{Namespace: sts.Namespace, Name: legacyName}, wl) == nil {
+			ctrl.LoggerFrom(ctx).V(3).Info("Using legacy workload name", "legacyName", legacyName, "newName", wlName)
+			return legacyName
+		}
```

  Let's propagate any non-NotFound errors and return, to avoid silencing some errors.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-05T14:01:32Z

- **pkg/controller/jobs/statefulset/statefulset_reconciler.go:191** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-05T14:01:32Z
```diff
@@ -181,6 +177,22 @@ func (r *Reconciler) syncQueueLabel(ctx context.Context, sts *appsv1.StatefulSet
 	})
 }
 
+// findWorkloadName returns the workload name for the given StatefulSet,
+// falling back to the legacy name (without UID) if no workload exists under the new name.
+// TODO(#9497, v0.20): Remove legacy fallback.
+func findWorkloadName(ctx context.Context, c client.Client, sts *appsv1.StatefulSet) string {
+	wlName := GetWorkloadName(GetOwnerUID(sts), sts.Name)
+	wl := &kueue.Workload{}
+	if apierrors.IsNotFound(c.Get(ctx, client.ObjectKey{Namespace: sts.Namespace, Name: wlName}, wl)) {
+		legacyName := GetWorkloadName("", sts.Name)
+		if c.Get(ctx, client.ObjectKey{Namespace: sts.Namespace, Name: legacyName}, wl) == nil {
+			ctrl.LoggerFrom(ctx).V(3).Info("Using legacy workload name", "legacyName", legacyName, "newName", wlName)
+			return legacyName
+		}
```

  Done.
