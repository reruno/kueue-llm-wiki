# PR #9120: Add a mechanism to consider preemption cost when finding preemption candidates

**Summary**: Add a mechanism to consider preemption cost when finding preemption candidates

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9120

**Last updated**: 2026-03-19T09:50:29Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@vladikkuzn](https://github.com/vladikkuzn)
- **Branch**: `preemption-cost-imp` → `main`
- **Created**: 2026-02-11T12:56:19Z
- **Updated**: 2026-03-19T09:50:29Z
- **Merged**: 2026-03-17T17:43:42Z
- **Closed**: 2026-03-17T17:43:42Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@vladikkuzn](https://github.com/vladikkuzn)
- **Requested reviewers**: [@amy](https://github.com/amy), [@sanposhiho](https://github.com/sanposhiho), [@tenzen-y](https://github.com/tenzen-y), [@windsonsea](https://github.com/windsonsea)
- **Changed files**: 26   **+798 / -78**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #7990

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Scheduling: support the kueue.k8s-x.io/priority-boost annotation to increase the effective priority of a workload.
The annotation is meant for the use of external controllers on the Workload object.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-11T12:56:22Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-02-11T12:56:26Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | f3832ab3d4de4a4e961ae52c8bc722aa04379054 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69b98a4ead7e640008c2c309 |

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-20T03:24:43Z

/cc @mbobrovskyi @mimowo

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-20T03:25:21Z

/cc @sanposhiho @amy

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-20T03:25:35Z

/assign

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-20T16:53:08Z

As per https://github.com/kubernetes-sigs/kueue/pull/8551#discussion_r2831685461:
Added priorityInfo() helper — computes (effectivePriority, basePriority, boost) for a workload in one call, using the priority package.
Enhanced V(3) structured log — Added four new key-value pairs to the existing "Preempted" log line: preemptorEffectivePriority, preemptorBoost, targetEffectivePriority, targetBoost. This lets operators filter/search logs by priority values.
Enhanced preemptee Preempted event — The existing event on the preemptee now appends priority details: ; preemptor effective priority: X (base: Y, boost: Z); preemptee effective priority: X (base: Y, boost: Z).
New PreemptedWorkload event on the preemptor — A new event is emitted on the preemptor workload, providing bidirectional traceability. Platform owners can now see preemption decisions from both sides via kubectl describe workload.

### Comment by [@kannon92](https://github.com/kannon92) — 2026-03-01T18:42:50Z

can you separate the KEP from the implementation?

### Comment by [@kannon92](https://github.com/kannon92) — 2026-03-01T18:46:03Z

cc @sohankunkerkar

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-03T03:39:59Z

@kannon92 [The KEP](https://github.com/kubernetes-sigs/kueue/pull/8551) is separate; it won't be in this PR after KEP is merged

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-10T12:10:49Z

/cc @mszadkow

### Comment by [@mszadkow](https://github.com/mszadkow) — 2026-03-10T12:28:30Z

`computeSchedulingHash` still uses "raw" priority not effective priority to decide on fit.
If we have 2 workloads with the same hash (= same priority + same podSet shape) they will both go to inadmissible, even though one might be eligible for preemption.
I am not sure how possible that is, but still worth considering.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-10T18:39:27Z

> `computeSchedulingHash` still uses "raw" priority not effective priority to decide on fit.

Yes, we need to either put the "priority" as already boosted, or have another field "priorityBoost" for the boost value.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-10T18:52:53Z

@vladikkuzn the release-note was empty, I set some proposal, ptal

### Comment by [@mszadkow](https://github.com/mszadkow) — 2026-03-13T12:01:52Z

Overall looks good, can you embed it in some integration test - an extra step in one test would be fine imho

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-16T10:19:46Z

LGTM overall, thank  you 👍

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-17T17:42:04Z

LGTM label has been added.  <details>Git tree hash: 1d8f9de48f8da52965b025301dd0e02a44ac43e6</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-17T17:42:07Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9120#pullrequestreview-3962593539" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9120#" title="Author self-approved">vladikkuzn</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-19T09:50:25Z

/kind feature

## Reviews

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-10T12:14:34Z

- **pkg/util/priority/priority.go:70** by [@mszadkow](https://github.com/mszadkow) — 2026-03-10T12:14:34Z
```diff
@@ -36,6 +39,36 @@ func Priority(w *kueue.Workload) int32 {
 	return ptr.Deref(w.Spec.Priority, constants.DefaultPriority)
 }
 
+// PriorityBoost returns the priority boost read from the workload annotation.
+// Missing annotation is treated as 0. When the PriorityBoost feature gate is
+// disabled, the annotation is ignored and 0 is returned.
+// Invalid values cause the Workload webhook to reject create/update (KEP-7990); at use-time callers treat as 0 and emit Event + log as defense-in-depth.
+func PriorityBoost(w *kueue.Workload) (int32, error) {
+	if !features.Enabled(features.PriorityBoost) {
+		return 0, nil
+	}
+	value, found := w.Annotations[controllerconstants.PriorityBoostAnnotationKey]
+	if !found || value == "" {
+		return 0, nil
+	}
+	boost, err := strconv.ParseInt(value, 10, 32)
+	if err != nil {
+		return 0, err
+	}
+	return int32(boost), nil
+}
+
+// EffectivePriority returns priority adjusted by the priority boost annotation.
+// effectivePriority = workload.priority + priorityBoost
+// Invalid boost is rejected at admission (KEP-7990); at use-time returns base priority and non-nil error, callers emit Event + log as defense-in-depth.
+func EffectivePriority(w *kueue.Workload) (int32, error) {
+	boost, err := PriorityBoost(w)
+	if err != nil {
+		return Priority(w), err
+	}
+	return Priority(w) + boost, nil
+}
```

  As low as this possibility is maybe let's maybe return int64, as addition can overflow int32.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-10T18:40:01Z

- **cmd/experimental/priority-boost-controller/charts/priority-boost-controller/templates/manager.yaml:1** by [@mimowo](https://github.com/mimowo) — 2026-03-10T18:40:02Z
```diff
@@ -0,0 +1,122 @@
+apiVersion: v1
```

  Let's move the example priority-boost-controller to another PR.
  
  This is important to get better quality review by smaller pieces of work.

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-03-12T19:13:17Z

- **pkg/util/priority/priority.go:70** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-12T19:13:17Z
```diff
@@ -36,6 +39,36 @@ func Priority(w *kueue.Workload) int32 {
 	return ptr.Deref(w.Spec.Priority, constants.DefaultPriority)
 }
 
+// PriorityBoost returns the priority boost read from the workload annotation.
+// Missing annotation is treated as 0. When the PriorityBoost feature gate is
+// disabled, the annotation is ignored and 0 is returned.
+// Invalid values cause the Workload webhook to reject create/update (KEP-7990); at use-time callers treat as 0 and emit Event + log as defense-in-depth.
+func PriorityBoost(w *kueue.Workload) (int32, error) {
+	if !features.Enabled(features.PriorityBoost) {
+		return 0, nil
+	}
+	value, found := w.Annotations[controllerconstants.PriorityBoostAnnotationKey]
+	if !found || value == "" {
+		return 0, nil
+	}
+	boost, err := strconv.ParseInt(value, 10, 32)
+	if err != nil {
+		return 0, err
+	}
+	return int32(boost), nil
+}
+
+// EffectivePriority returns priority adjusted by the priority boost annotation.
+// effectivePriority = workload.priority + priorityBoost
+// Invalid boost is rejected at admission (KEP-7990); at use-time returns base priority and non-nil error, callers emit Event + log as defense-in-depth.
+func EffectivePriority(w *kueue.Workload) (int32, error) {
+	boost, err := PriorityBoost(w)
+	if err != nil {
+		return Priority(w), err
+	}
+	return Priority(w) + boost, nil
+}
```

  I updated the implementation, but please check if it makes sense with Priority being int32

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-12T19:36:42Z

- **pkg/controller/core/workload_controller.go:208** by [@mimowo](https://github.com/mimowo) — 2026-03-12T19:36:42Z
```diff
@@ -196,6 +198,16 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
+	// KEP-7990: admission normally rejects invalid priority-boost; here we treat as 0 and emit Event + log as defense-in-depth.
+	if _, err := priority.EffectivePriority(&wl); err != nil {
+		log.V(2).Info("Invalid priority-boost annotation, treating as zero",
+			"annotation", wl.Annotations[controllerconstants.PriorityBoostAnnotationKey], "error", err)
+		r.recorder.Eventf(&wl, corev1.EventTypeWarning, "InvalidPriorityBoost",
+			"Invalid value %q for annotation %s, treating as zero: %v",
+			wl.Annotations[controllerconstants.PriorityBoostAnnotationKey],
+			controllerconstants.PriorityBoostAnnotationKey, err)
```

  no need to record here. This should never happen if we add validation

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-12T19:36:49Z

- **pkg/controller/core/workload_controller.go:203** by [@mimowo](https://github.com/mimowo) — 2026-03-12T19:36:49Z
```diff
@@ -196,6 +198,16 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
+	// KEP-7990: admission normally rejects invalid priority-boost; here we treat as 0 and emit Event + log as defense-in-depth.
+	if _, err := priority.EffectivePriority(&wl); err != nil {
+		log.V(2).Info("Invalid priority-boost annotation, treating as zero",
```

  ```suggestion
  		log.Error("Invalid priority-boost annotation, treating as zero",
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-12T19:37:56Z

- **pkg/scheduler/preemption/classical/hierarchical_preemption.go:108** by [@mimowo](https://github.com/mimowo) — 2026-03-12T19:37:56Z
```diff
@@ -104,22 +104,22 @@ func classifyPreemptionVariant(ctx *HierarchicalPreemptionCtx, wl *workload.Info
 	if borrowWithinCohortForbidden {
 		return ReclaimWithoutBorrowing
 	}
-	candidatePriority := priority.Priority(wl.Obj)
-	incomingPriority := priority.Priority(ctx.Wl)
+	candidatePriority, _ := priority.EffectivePriority(wl.Obj)
+	incomingPriority, _ := priority.EffectivePriority(ctx.Wl)
```

  Don't slicence errors like that. We can rely on validation, and so introduce a helper like `EffectivePriority` which will assume the annotation is already validated. It will just log on error.
  
  Then you may have "ParseEffectivePriority" which would return error and is used by the validation webhook

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-12T19:38:36Z

- **pkg/scheduler/preemption/common/ordering.go:79** by [@mimowo](https://github.com/mimowo) — 2026-03-12T19:38:36Z
```diff
@@ -64,9 +64,19 @@ func CandidatesOrdering(log logr.Logger, afsEnabled bool, a, b *workload.Info, c
 			return 0
 		},
 		func() int {
+			priorityA, errA := priority.EffectivePriority(a.Obj)
+			if errA != nil {
+				log.V(5).Info("Invalid priority-boost annotation, defaulting to zero",
+					"workload", klog.KObj(a.Obj), "annotation", a.Obj.Annotations)
+			}
+			priorityB, errB := priority.EffectivePriority(b.Obj)
+			if errB != nil {
+				log.V(5).Info("Invalid priority-boost annotation, defaulting to zero",
+					"workload", klog.KObj(b.Obj), "annotation", b.Obj.Annotations)
+			}
 			return cmp.Compare(
-				priority.Priority(a.Obj),
-				priority.Priority(b.Obj),
+				priorityA,
+				priorityB,
```

  Yeah, this is too much error handling. Let's introduce a helper which already assumes the value is valid

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-13T11:42:55Z

- **pkg/controller/jobframework/interface.go:228** by [@mszadkow](https://github.com/mszadkow) — 2026-03-13T11:42:55Z
```diff
@@ -217,13 +217,15 @@ func PrebuiltWorkloadFor(job GenericJob) (string, bool) {
 }
 
 func NewWorkload(name string, obj client.Object, podSets []kueue.PodSet, labelKeysToCopy []string) *kueue.Workload {
+	annotations := admissioncheck.FilterProvReqAnnotations(obj.GetAnnotations())
+
 	return &kueue.Workload{
 		ObjectMeta: metav1.ObjectMeta{
 			Name:        name,
 			Namespace:   obj.GetNamespace(),
 			Labels:      maps.FilterKeys(obj.GetLabels(), labelKeysToCopy),
 			Finalizers:  []string{kueue.ResourceInUseFinalizerName},
-			Annotations: admissioncheck.FilterProvReqAnnotations(obj.GetAnnotations()),
+			Annotations: annotations,
```

  seems unrelated

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-13T11:59:28Z

- **pkg/workload/workload.go:1691** by [@mszadkow](https://github.com/mszadkow) — 2026-03-13T11:59:28Z
```diff
@@ -1688,5 +1689,7 @@ func PriorityChanged(old, new *kueue.Workload) bool {
 		return true
 	}
 	// Check if priority value changed (for WorkloadPriorityClass value updates).
```

  effective priority

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-13T12:00:09Z

- **site/content/zh-CN/docs/reference/labels-and-annotations.md:251** by [@mszadkow](https://github.com/mszadkow) — 2026-03-13T12:00:09Z
```diff
@@ -234,6 +234,21 @@ Used on: [Plain Pods](/docs/tasks/run/plain_pods/).
 
 The annotation key is used to indicate the integration name of the Pod owner.
 
+### kueue.x-k8s.io/priority-boost
+
+Type: Annotation
+
+Example: `kueue.x-k8s.io/priority-boost: "10"`
+
+Used on: [Workload](/docs/concepts/workload/).
+
+可选的有符号整数注解，用于调整工作负载的有效优先级：
+`effectivePriority = workloadPriority + priorityBoost`。
+正值提高优先级，负值降低优先级。
+有效优先级同时用于调度排序和抢占候选排序。
+此注解由外部控制器直接设置在 Workload 上（不从 Job 传播）。
+如果值缺失或非法，会按 `0` 处理。
+
```

  can't review that ;)

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-13T12:10:00Z

- **pkg/controller/jobframework/interface.go:220** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-13T12:10:00Z
```diff
@@ -217,13 +217,15 @@ func PrebuiltWorkloadFor(job GenericJob) (string, bool) {
 }
 
 func NewWorkload(name string, obj client.Object, podSets []kueue.PodSet, labelKeysToCopy []string) *kueue.Workload {
+	annotations := admissioncheck.FilterProvReqAnnotations(obj.GetAnnotations())
```

  Why this changes?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-13T13:01:40Z

- **site/content/en/docs/reference/labels-and-annotations.md:254** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-13T13:01:40Z
```diff
@@ -251,6 +251,21 @@ Used on: [Plain Pods](/docs/tasks/run/plain_pods/).
 
 The annotation key is used to indicate the integration name of the Pod owner.
 
+### kueue.x-k8s.io/priority-boost
```

  We are trying to keep this labels list in order. Could you please move this after `kueue.x-k8s.io/prebuilt-workload-name`?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-13T13:08:58Z

- **pkg/util/priority/priority.go:52** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-13T13:08:58Z
```diff
@@ -36,6 +40,50 @@ func Priority(w *kueue.Workload) int32 {
 	return ptr.Deref(w.Spec.Priority, constants.DefaultPriority)
 }
 
+// PriorityBoost returns the priority boost read from the workload annotation.
+// Missing annotation is treated as 0. When the PriorityBoost feature gate is
+// disabled, the annotation is ignored and 0 is returned.
+// Invalid values cause the Workload webhook to reject create/update.
+func PriorityBoost(w *kueue.Workload) (int32, error) {
+	if !features.Enabled(features.PriorityBoost) {
+		return 0, nil
+	}
+	value, found := w.Annotations[controllerconstants.PriorityBoostAnnotationKey]
+	if !found || value == "" {
```

  ```suggestion
  	value := w.Annotations[controllerconstants.PriorityBoostAnnotationKey]
  	if value == "" {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-13T13:10:03Z

- **pkg/util/priority/priority.go:79** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-13T13:10:03Z
```diff
@@ -36,6 +40,50 @@ func Priority(w *kueue.Workload) int32 {
 	return ptr.Deref(w.Spec.Priority, constants.DefaultPriority)
 }
 
+// PriorityBoost returns the priority boost read from the workload annotation.
+// Missing annotation is treated as 0. When the PriorityBoost feature gate is
+// disabled, the annotation is ignored and 0 is returned.
+// Invalid values cause the Workload webhook to reject create/update.
+func PriorityBoost(w *kueue.Workload) (int32, error) {
+	if !features.Enabled(features.PriorityBoost) {
+		return 0, nil
+	}
+	value, found := w.Annotations[controllerconstants.PriorityBoostAnnotationKey]
+	if !found || value == "" {
+		return 0, nil
+	}
+	boost, err := strconv.ParseInt(value, 10, 32)
+	if err != nil {
+		return 0, err
+	}
+	return int32(boost), nil
+}
+
+// ParseEffectivePriority returns priority adjusted by the priority boost
+// annotation and reports an error if the annotation cannot be parsed.
+func ParseEffectivePriority(w *kueue.Workload) (int64, error) {
+	basePriority := int64(Priority(w))
+	boost, err := PriorityBoost(w)
+	if err != nil {
+		return basePriority, err
+	}
+	return basePriority + int64(boost), nil
+}
+
+// EffectivePriority returns priority adjusted by the priority boost
+// annotation. Invalid values should be rejected at admission; as
+// defense-in-depth, invalid values are logged and treated as zero here.
+func EffectivePriority(w *kueue.Workload) int64 {
+	effectivePriority, err := ParseEffectivePriority(w)
+	if err != nil {
+		klog.V(5).InfoS("Invalid priority-boost annotation, defaulting to base priority",
```

  Shouldn't it be error?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-13T13:10:59Z

- **pkg/util/priority/priority.go:79** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-13T13:10:59Z
```diff
@@ -36,6 +40,50 @@ func Priority(w *kueue.Workload) int32 {
 	return ptr.Deref(w.Spec.Priority, constants.DefaultPriority)
 }
 
+// PriorityBoost returns the priority boost read from the workload annotation.
+// Missing annotation is treated as 0. When the PriorityBoost feature gate is
+// disabled, the annotation is ignored and 0 is returned.
+// Invalid values cause the Workload webhook to reject create/update.
+func PriorityBoost(w *kueue.Workload) (int32, error) {
+	if !features.Enabled(features.PriorityBoost) {
+		return 0, nil
+	}
+	value, found := w.Annotations[controllerconstants.PriorityBoostAnnotationKey]
+	if !found || value == "" {
+		return 0, nil
+	}
+	boost, err := strconv.ParseInt(value, 10, 32)
+	if err != nil {
+		return 0, err
+	}
+	return int32(boost), nil
+}
+
+// ParseEffectivePriority returns priority adjusted by the priority boost
+// annotation and reports an error if the annotation cannot be parsed.
+func ParseEffectivePriority(w *kueue.Workload) (int64, error) {
+	basePriority := int64(Priority(w))
+	boost, err := PriorityBoost(w)
+	if err != nil {
+		return basePriority, err
+	}
+	return basePriority + int64(boost), nil
+}
+
+// EffectivePriority returns priority adjusted by the priority boost
+// annotation. Invalid values should be rejected at admission; as
+// defense-in-depth, invalid values are logged and treated as zero here.
+func EffectivePriority(w *kueue.Workload) int64 {
+	effectivePriority, err := ParseEffectivePriority(w)
+	if err != nil {
+		klog.V(5).InfoS("Invalid priority-boost annotation, defaulting to base priority",
```

  Also why we are uing klog? Can we propagate log?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-13T13:22:06Z

- **pkg/scheduler/preemption/common/ordering.go:68** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-13T13:22:06Z
```diff
@@ -64,9 +64,11 @@ func CandidatesOrdering(log logr.Logger, afsEnabled bool, a, b *workload.Info, c
 			return 0
 		},
 		func() int {
+			priorityA := priority.EffectivePriority(a.Obj)
+			priorityB := priority.EffectivePriority(b.Obj)
```

  Can we avoid variable allocation?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-13T13:25:39Z

- **pkg/scheduler/preemption/preemption_test.go:4527** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-13T13:25:39Z
```diff
@@ -4523,6 +4524,7 @@ func targetKeyReason(key workload.Reference, reason string) string {
 	return fmt.Sprintf("%s:%s", key, reason)
 }
 func TestCandidatesOrdering(t *testing.T) {
+	features.SetFeatureGateDuringTest(t, features.PriorityBoost, true)
```

  Can we enable it only in case where we really need it?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-13T13:27:35Z

- **pkg/scheduler/preemption/preemption_test.go:4737** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-13T13:27:35Z
```diff
@@ -4686,3 +4732,88 @@ func TestPreemptionMessage(t *testing.T) {
 		}
 	}
 }
+
+func TestPriorityInfo(t *testing.T) {
+	features.SetFeatureGateDuringTest(t, features.PriorityBoost, true)
```

  Move it inside t.Run().

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-13T13:28:01Z

- **pkg/scheduler/preemption/preemption_test.go:4737** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-13T13:28:01Z
```diff
@@ -4686,3 +4732,88 @@ func TestPreemptionMessage(t *testing.T) {
 		}
 	}
 }
+
+func TestPriorityInfo(t *testing.T) {
+	features.SetFeatureGateDuringTest(t, features.PriorityBoost, true)
```

  Also add test case if feature disabled.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-13T13:28:53Z

- **pkg/scheduler/scheduler_tas_test.go:3358** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-13T13:28:53Z
```diff
@@ -3352,7 +3352,10 @@ func TestScheduleForTASPreemption(t *testing.T) {
 					Message("Preempted to accommodate a workload (UID: wl-foo, JobUID: job-foo) due to prioritization in the ClusterQueue; preemptor path: /tas-main; preemptee path: /tas-main").
 					Obj(),
 				utiltesting.MakeEventRecord("default", "low-priority-admitted", "Preempted", "Normal").
-					Message("Preempted to accommodate a workload (UID: wl-foo, JobUID: job-foo) due to prioritization in the ClusterQueue; preemptor path: /tas-main; preemptee path: /tas-main").
+					Message("Preempted to accommodate a workload (UID: wl-foo, JobUID: job-foo) due to prioritization in the ClusterQueue; preemptor path: /tas-main; preemptee path: /tas-main; preemptor effective priority: 3 (base: 3, boost: 0); preemptee effective priority: 1 (base: 1, boost: 0)").
+					Obj(),
+				utiltesting.MakeEventRecord("default", "foo", "PreemptedWorkload", "Normal").
+					Message("Preempted workload default/low-priority-admitted (UID: ) in ClusterQueue tas-main; preemptor effective priority: 3 (base: 3, boost: 0); preemptee effective priority: 1 (base: 1, boost: 0)").
```

  Why we don't have UID?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-13T13:29:41Z

- **pkg/util/priority/priority_test.go:220** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-13T13:29:41Z
```diff
@@ -212,3 +215,149 @@ func TestGetPriorityFromWorkloadPriorityClass(t *testing.T) {
 		})
 	}
 }
+
+func TestPriorityBoost(t *testing.T) {
+	features.SetFeatureGateDuringTest(t, features.PriorityBoost, true)
```

  The same,

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-13T13:31:21Z

- **pkg/webhooks/workload_webhook.go:126** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-13T13:31:21Z
```diff
@@ -119,6 +121,15 @@ func ValidateWorkload(obj *kueue.Workload) field.ErrorList {
 	allErrs = append(allErrs, validateReclaimablePods(obj, statusPath.Child("reclaimablePods"))...)
 	allErrs = append(allErrs, validateAdmissionChecks(obj, statusPath.Child("admissionChecks"))...)
 
+	// KEP-7990: when priority-boost annotation is set, it must be a valid signed integer; invalid values cause rejection.
+	if features.Enabled(features.PriorityBoost) {
+		if _, err := priority.ParseEffectivePriority(obj); err != nil {
```

  Actually, is it valid case with empty string? As far I remember we don't have error for empty string.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-13T13:33:33Z

- **pkg/webhooks/workload_webhook.go:127** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-13T13:33:34Z
```diff
@@ -119,6 +121,15 @@ func ValidateWorkload(obj *kueue.Workload) field.ErrorList {
 	allErrs = append(allErrs, validateReclaimablePods(obj, statusPath.Child("reclaimablePods"))...)
 	allErrs = append(allErrs, validateAdmissionChecks(obj, statusPath.Child("admissionChecks"))...)
 
+	// KEP-7990: when priority-boost annotation is set, it must be a valid signed integer; invalid values cause rejection.
+	if features.Enabled(features.PriorityBoost) {
+		if _, err := priority.ParseEffectivePriority(obj); err != nil {
+			path := field.NewPath("metadata", "annotations").Key(controllerconstants.PriorityBoostAnnotationKey)
```

  Can we move it as a package variable?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-13T13:34:45Z

- **pkg/webhooks/workload_webhook_test.go:55** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-13T13:34:46Z
```diff
@@ -49,9 +50,11 @@ func TestValidateWorkload(t *testing.T) {
 	firstAdmissionChecksPath := statusPath.Child("admissionChecks").Index(0)
 	podSetUpdatePath := firstAdmissionChecksPath.Child("podSetUpdates")
 	firstPodSetSpecPath := podSetsPath.Index(0).Child("template", "spec")
+	priorityBoostPath := field.NewPath("metadata", "annotations").Key(controllerconstants.PriorityBoostAnnotationKey)
 	testCases := map[string]struct {
-		workload *kueue.Workload
-		wantErr  field.ErrorList
+		enablePriorityBoost *bool
```

  ```suggestion
  		features map[featuregate.Feature]bool
  ```
  I think this pattern is much better.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-13T13:36:09Z

- **pkg/workload/workload.go:1694** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-13T13:36:09Z
```diff
@@ -1688,5 +1689,7 @@ func PriorityChanged(old, new *kueue.Workload) bool {
 		return true
 	}
 	// Check if priority value changed (for WorkloadPriorityClass value updates).
-	return priority.Priority(old) != priority.Priority(new)
+	p1 := priority.EffectivePriority(old)
+	p2 := priority.EffectivePriority(new)
+	return p1 != p2
```

  ```suggestion
  	return priority.EffectivePriority(old) != priority.EffectivePriority(new)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-13T13:37:06Z

- **pkg/workload/workload_test.go:2573** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-13T13:37:06Z
```diff
@@ -2570,6 +2570,8 @@ func TestGetLocalQueueFromWorkload(t *testing.T) {
 }
 
 func TestSchedulingHash(t *testing.T) {
+	features.SetFeatureGateDuringTest(t, features.SchedulingEquivalenceHashing, true)
```

  Please move it to t.Run() and execute in cases where we really need it.

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-03-13T19:35:45Z

- **pkg/util/priority/priority.go:79** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-13T19:35:45Z
```diff
@@ -36,6 +40,50 @@ func Priority(w *kueue.Workload) int32 {
 	return ptr.Deref(w.Spec.Priority, constants.DefaultPriority)
 }
 
+// PriorityBoost returns the priority boost read from the workload annotation.
+// Missing annotation is treated as 0. When the PriorityBoost feature gate is
+// disabled, the annotation is ignored and 0 is returned.
+// Invalid values cause the Workload webhook to reject create/update.
+func PriorityBoost(w *kueue.Workload) (int32, error) {
+	if !features.Enabled(features.PriorityBoost) {
+		return 0, nil
+	}
+	value, found := w.Annotations[controllerconstants.PriorityBoostAnnotationKey]
+	if !found || value == "" {
+		return 0, nil
+	}
+	boost, err := strconv.ParseInt(value, 10, 32)
+	if err != nil {
+		return 0, err
+	}
+	return int32(boost), nil
+}
+
+// ParseEffectivePriority returns priority adjusted by the priority boost
+// annotation and reports an error if the annotation cannot be parsed.
+func ParseEffectivePriority(w *kueue.Workload) (int64, error) {
+	basePriority := int64(Priority(w))
+	boost, err := PriorityBoost(w)
+	if err != nil {
+		return basePriority, err
+	}
+	return basePriority + int64(boost), nil
+}
+
+// EffectivePriority returns priority adjusted by the priority boost
+// annotation. Invalid values should be rejected at admission; as
+// defense-in-depth, invalid values are logged and treated as zero here.
+func EffectivePriority(w *kueue.Workload) int64 {
+	effectivePriority, err := ParseEffectivePriority(w)
+	if err != nil {
+		klog.V(5).InfoS("Invalid priority-boost annotation, defaulting to base priority",
```

  This is no error wrapper as it's called after ParseEffectivePriority, we need it to simplify code where there could be no error. I think it's not the best place to propagate log to, maybe it's better to silence the error here, wdyt?
  See https://github.com/kubernetes-sigs/kueue/pull/9120/changes#discussion_r2926946677

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-03-13T19:39:25Z

- **site/content/en/docs/reference/labels-and-annotations.md:254** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-13T19:39:24Z
```diff
@@ -251,6 +251,21 @@ Used on: [Plain Pods](/docs/tasks/run/plain_pods/).
 
 The annotation key is used to indicate the integration name of the Pod owner.
 
+### kueue.x-k8s.io/priority-boost
```

  If we really are, maybe as a follow-up create an issue to use template here with sorting, wdyt? It can be good first issue

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-03-13T19:41:50Z

- **pkg/webhooks/workload_webhook.go:126** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-13T19:41:49Z
```diff
@@ -119,6 +121,15 @@ func ValidateWorkload(obj *kueue.Workload) field.ErrorList {
 	allErrs = append(allErrs, validateReclaimablePods(obj, statusPath.Child("reclaimablePods"))...)
 	allErrs = append(allErrs, validateAdmissionChecks(obj, statusPath.Child("admissionChecks"))...)
 
+	// KEP-7990: when priority-boost annotation is set, it must be a valid signed integer; invalid values cause rejection.
+	if features.Enabled(features.PriorityBoost) {
+		if _, err := priority.ParseEffectivePriority(obj); err != nil {
```

  Empty string is treated as 0
  https://github.com/kubernetes-sigs/kueue/blob/af7d67bc213cf0e734140f09c8a5a81b2ef5c181/pkg/util/priority/priority.go#L52-L54

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-16T09:56:41Z

- **site/content/en/docs/reference/labels-and-annotations.md:254** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-16T09:56:41Z
```diff
@@ -251,6 +251,21 @@ Used on: [Plain Pods](/docs/tasks/run/plain_pods/).
 
 The annotation key is used to indicate the integration name of the Pod owner.
 
+### kueue.x-k8s.io/priority-boost
```

  > If we really are, maybe as a follow-up create an issue to use template here with sorting, wdyt? It can be good first issue
  
  I like this idea!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-16T09:57:57Z

- **pkg/util/priority/priority.go:79** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-16T09:57:57Z
```diff
@@ -36,6 +40,50 @@ func Priority(w *kueue.Workload) int32 {
 	return ptr.Deref(w.Spec.Priority, constants.DefaultPriority)
 }
 
+// PriorityBoost returns the priority boost read from the workload annotation.
+// Missing annotation is treated as 0. When the PriorityBoost feature gate is
+// disabled, the annotation is ignored and 0 is returned.
+// Invalid values cause the Workload webhook to reject create/update.
+func PriorityBoost(w *kueue.Workload) (int32, error) {
+	if !features.Enabled(features.PriorityBoost) {
+		return 0, nil
+	}
+	value, found := w.Annotations[controllerconstants.PriorityBoostAnnotationKey]
+	if !found || value == "" {
+		return 0, nil
+	}
+	boost, err := strconv.ParseInt(value, 10, 32)
+	if err != nil {
+		return 0, err
+	}
+	return int32(boost), nil
+}
+
+// ParseEffectivePriority returns priority adjusted by the priority boost
+// annotation and reports an error if the annotation cannot be parsed.
+func ParseEffectivePriority(w *kueue.Workload) (int64, error) {
+	basePriority := int64(Priority(w))
+	boost, err := PriorityBoost(w)
+	if err != nil {
+		return basePriority, err
+	}
+	return basePriority + int64(boost), nil
+}
+
+// EffectivePriority returns priority adjusted by the priority boost
+// annotation. Invalid values should be rejected at admission; as
+// defense-in-depth, invalid values are logged and treated as zero here.
+func EffectivePriority(w *kueue.Workload) int64 {
+	effectivePriority, err := ParseEffectivePriority(w)
+	if err != nil {
+		klog.V(5).InfoS("Invalid priority-boost annotation, defaulting to base priority",
```

  Actually we have validation for workload, right? So yeah I think we can skip it.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-16T09:59:05Z

- **pkg/webhooks/workload_webhook.go:126** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-16T09:59:05Z
```diff
@@ -119,6 +121,15 @@ func ValidateWorkload(obj *kueue.Workload) field.ErrorList {
 	allErrs = append(allErrs, validateReclaimablePods(obj, statusPath.Child("reclaimablePods"))...)
 	allErrs = append(allErrs, validateAdmissionChecks(obj, statusPath.Child("admissionChecks"))...)
 
+	// KEP-7990: when priority-boost annotation is set, it must be a valid signed integer; invalid values cause rejection.
+	if features.Enabled(features.PriorityBoost) {
+		if _, err := priority.ParseEffectivePriority(obj); err != nil {
```

  Yeah, but shouldn’t we validate this case and require the user to set it to 0?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-16T09:59:58Z

- **pkg/cache/queue/inadmissible_workloads_test.go:34** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-16T09:59:58Z
```diff
@@ -31,8 +31,8 @@ import (
 
 func TestNewRequeuer(t *testing.T) {
 	type args struct {
-		features map[featuregate.Feature]bool
-		opts     []RequeuerOption
+		featureGates map[featuregate.Feature]bool
```

  nit: can we avoid this changes? It looks like not related.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-16T10:00:14Z

- **pkg/controller/admissionchecks/multikueue/workload_test.go:77** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-16T10:00:14Z
```diff
@@ -74,7 +74,7 @@ func TestWlReconcile(t *testing.T) {
 	baseJobManagedByKueueBuilder := baseJobBuilder.Clone().ManagedBy(kueue.MultiKueueControllerName)
 
 	cases := map[string]struct {
-		features map[featuregate.Feature]bool
+		featureGates map[featuregate.Feature]bool
```

  Also here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-16T10:00:40Z

- **pkg/controller/jobs/job/job_multikueue_adapter_test.go:266** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-16T10:00:41Z
```diff
@@ -263,7 +263,7 @@ func TestMultiKueueAdapter(t *testing.T) {
 
 func Test_multiKueueAdapter_SyncJob(t *testing.T) {
 	type fields struct {
-		features map[featuregate.Feature]bool
+		featureGates map[featuregate.Feature]bool
```

  Also here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-16T10:01:08Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler_test.go:60** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-16T10:01:08Z
```diff
@@ -57,7 +57,7 @@ var (
 
 func TestReconciler(t *testing.T) {
 	cases := map[string]struct {
-		features                map[featuregate.Feature]bool
+		featureGates            map[featuregate.Feature]bool
```

  Please also revert other places.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-16T10:04:39Z

- **pkg/util/priority/priority.go:79** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-16T10:04:39Z
```diff
@@ -36,6 +40,50 @@ func Priority(w *kueue.Workload) int32 {
 	return ptr.Deref(w.Spec.Priority, constants.DefaultPriority)
 }
 
+// PriorityBoost returns the priority boost read from the workload annotation.
+// Missing annotation is treated as 0. When the PriorityBoost feature gate is
+// disabled, the annotation is ignored and 0 is returned.
+// Invalid values cause the Workload webhook to reject create/update.
+func PriorityBoost(w *kueue.Workload) (int32, error) {
+	if !features.Enabled(features.PriorityBoost) {
+		return 0, nil
+	}
+	value, found := w.Annotations[controllerconstants.PriorityBoostAnnotationKey]
+	if !found || value == "" {
+		return 0, nil
+	}
+	boost, err := strconv.ParseInt(value, 10, 32)
+	if err != nil {
+		return 0, err
+	}
+	return int32(boost), nil
+}
+
+// ParseEffectivePriority returns priority adjusted by the priority boost
+// annotation and reports an error if the annotation cannot be parsed.
+func ParseEffectivePriority(w *kueue.Workload) (int64, error) {
+	basePriority := int64(Priority(w))
+	boost, err := PriorityBoost(w)
+	if err != nil {
+		return basePriority, err
+	}
+	return basePriority + int64(boost), nil
+}
+
+// EffectivePriority returns priority adjusted by the priority boost
+// annotation. Invalid values should be rejected at admission; as
+// defense-in-depth, invalid values are logged and treated as zero here.
+func EffectivePriority(w *kueue.Workload) int64 {
+	effectivePriority, err := ParseEffectivePriority(w)
+	if err != nil {
+		klog.V(5).InfoS("Invalid priority-boost annotation, defaulting to base priority",
```

  @mimowo WDYT?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-16T10:05:54Z

- **pkg/util/priority/priority_test.go:276** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-16T10:05:54Z
```diff
@@ -212,3 +216,175 @@ func TestGetPriorityFromWorkloadPriorityClass(t *testing.T) {
 		})
 	}
 }
+
+func TestPriorityBoost(t *testing.T) {
+	tests := map[string]struct {
+		workload     *kueue.Workload
+		wantBoost    int32
+		wantErr      bool
+		featureGates map[featuregate.Feature]bool
+	}{
+		"annotation not set": {
+			workload:     utiltestingapi.MakeWorkload("name", "ns").Obj(),
+			wantBoost:    0,
+			featureGates: map[featuregate.Feature]bool{features.PriorityBoost: true},
+		},
+		"positive boost": {
+			workload:     utiltestingapi.MakeWorkload("name", "ns").Annotation(controllerconstants.PriorityBoostAnnotationKey, "100").Obj(),
+			wantBoost:    100,
+			featureGates: map[featuregate.Feature]bool{features.PriorityBoost: true},
+		},
+		"negative boost": {
+			workload:     utiltestingapi.MakeWorkload("name", "ns").Annotation(controllerconstants.PriorityBoostAnnotationKey, "-50").Obj(),
+			wantBoost:    -50,
+			featureGates: map[featuregate.Feature]bool{features.PriorityBoost: true},
+		},
+		"zero boost": {
+			workload:     utiltestingapi.MakeWorkload("name", "ns").Annotation(controllerconstants.PriorityBoostAnnotationKey, "0").Obj(),
+			wantBoost:    0,
+			featureGates: map[featuregate.Feature]bool{features.PriorityBoost: true},
+		},
+		"empty string": {
+			workload:     utiltestingapi.MakeWorkload("name", "ns").Annotation(controllerconstants.PriorityBoostAnnotationKey, "").Obj(),
+			wantBoost:    0,
+			featureGates: map[featuregate.Feature]bool{features.PriorityBoost: true},
+		},
+		"invalid value": {
+			workload:     utiltestingapi.MakeWorkload("name", "ns").Annotation(controllerconstants.PriorityBoostAnnotationKey, "invalid").Obj(),
+			wantBoost:    0,
+			wantErr:      true,
+			featureGates: map[featuregate.Feature]bool{features.PriorityBoost: true},
+		},
+	}
+
+	for desc, tt := range tests {
+		t.Run(desc, func(t *testing.T) {
+			for fg, enable := range tt.featureGates {
+				features.SetFeatureGateDuringTest(t, fg, enable)
+			}
+			got, err := PriorityBoost(tt.workload)
+			if (err != nil) != tt.wantErr {
+				t.Errorf("PriorityBoost() error = %v, wantErr %v", err, tt.wantErr)
+			}
+			if got != tt.wantBoost {
+				t.Errorf("PriorityBoost() = %d, want %d", got, tt.wantBoost)
+			}
+		})
+	}
+}
+
+func TestPriorityBoostFeatureGateDisabled(t *testing.T) {
```

  Why separate one? Can we add it as a case for TestPriorityBoost?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-16T10:07:31Z

- **test/integration/singlecluster/scheduler/scheduler_test.go:647** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-16T10:07:31Z
```diff
@@ -641,6 +642,26 @@ var _ = ginkgo.Describe("Scheduler", func() {
 				util.ExpectReservingActiveWorkloadsMetric(prodClusterQ, 2)
 				util.ExpectQuotaReservedWorkloadsTotalMetric(prodClusterQ, "", 4)
 				util.ExpectAdmittedWorkloadsTotalMetric(prodClusterQ, "", 4)
+
+				ginkgo.By("when PriorityBoost is enabled, workload with effective priority is admitted over a lower-priority pending workload", func() {
+					if !features.Enabled(features.PriorityBoost) {
```

  I think we can skip this check.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-16T10:16:42Z

- **pkg/util/priority/priority.go:79** by [@mimowo](https://github.com/mimowo) — 2026-03-16T10:16:42Z
```diff
@@ -36,6 +40,50 @@ func Priority(w *kueue.Workload) int32 {
 	return ptr.Deref(w.Spec.Priority, constants.DefaultPriority)
 }
 
+// PriorityBoost returns the priority boost read from the workload annotation.
+// Missing annotation is treated as 0. When the PriorityBoost feature gate is
+// disabled, the annotation is ignored and 0 is returned.
+// Invalid values cause the Workload webhook to reject create/update.
+func PriorityBoost(w *kueue.Workload) (int32, error) {
+	if !features.Enabled(features.PriorityBoost) {
+		return 0, nil
+	}
+	value, found := w.Annotations[controllerconstants.PriorityBoostAnnotationKey]
+	if !found || value == "" {
+		return 0, nil
+	}
+	boost, err := strconv.ParseInt(value, 10, 32)
+	if err != nil {
+		return 0, err
+	}
+	return int32(boost), nil
+}
+
+// ParseEffectivePriority returns priority adjusted by the priority boost
+// annotation and reports an error if the annotation cannot be parsed.
+func ParseEffectivePriority(w *kueue.Workload) (int64, error) {
+	basePriority := int64(Priority(w))
+	boost, err := PriorityBoost(w)
+	if err != nil {
+		return basePriority, err
+	}
+	return basePriority + int64(boost), nil
+}
+
+// EffectivePriority returns priority adjusted by the priority boost
+// annotation. Invalid values should be rejected at admission; as
+// defense-in-depth, invalid values are logged and treated as zero here.
+func EffectivePriority(w *kueue.Workload) int64 {
+	effectivePriority, err := ParseEffectivePriority(w)
+	if err != nil {
+		klog.V(5).InfoS("Invalid priority-boost annotation, defaulting to base priority",
```

  I would still log in case we missed some scenario in validation, but don't use klog. Propagete the full logger as the first param.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-16T10:19:19Z

- **pkg/controller/jobs/job/job_multikueue_adapter_test.go:266** by [@mimowo](https://github.com/mimowo) — 2026-03-16T10:19:20Z
```diff
@@ -263,7 +263,7 @@ func TestMultiKueueAdapter(t *testing.T) {
 
 func Test_multiKueueAdapter_SyncJob(t *testing.T) {
 	type fields struct {
-		features map[featuregate.Feature]bool
+		featureGates map[featuregate.Feature]bool
```

  I fully support the rename,  but this blows the diff for no reason. Please submit a dedicated prep PR.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-16T16:48:39Z

- **pkg/controller/jobs/job/job_multikueue_adapter_test.go:266** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-16T16:48:39Z
```diff
@@ -263,7 +263,7 @@ func TestMultiKueueAdapter(t *testing.T) {
 
 func Test_multiKueueAdapter_SyncJob(t *testing.T) {
 	type fields struct {
-		features map[featuregate.Feature]bool
+		featureGates map[featuregate.Feature]bool
```

  +1

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-17T03:40:43Z

- **pkg/cache/queue/inadmissible_workloads_test.go:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-17T03:40:43Z
  Why this changes?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-17T03:42:11Z

- **pkg/controller/admissionchecks/multikueue/workload_test.go:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-17T03:42:12Z
  Why this changes?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-17T03:42:42Z

Please revert not related changes.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T08:12:40Z

- **pkg/cache/queue/inadmissible_workloads_test.go:1** by [@mimowo](https://github.com/mimowo) — 2026-03-17T08:12:40Z
  +1, I'm happy with the changes like these to be done as a separete PR and backported, because they would be only for tests.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T08:18:18Z

- **test/integration/singlecluster/scheduler/scheduler_test.go:661** by [@mimowo](https://github.com/mimowo) — 2026-03-17T08:18:18Z
```diff
@@ -641,6 +642,24 @@ var _ = ginkgo.Describe("Scheduler", func() {
 				util.ExpectReservingActiveWorkloadsMetric(prodClusterQ, 2)
 				util.ExpectQuotaReservedWorkloadsTotalMetric(prodClusterQ, "", 4)
 				util.ExpectAdmittedWorkloadsTotalMetric(prodClusterQ, "", 4)
+
+				ginkgo.By("when PriorityBoost is enabled, workload with effective priority is admitted over a lower-priority pending workload", func() {
+					features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.PriorityBoost, true)
+					wlEffectivePrio := utiltestingapi.MakeWorkload("wl-effective-priority", ns.Name).
+						Queue(kueue.LocalQueueName(prodQueue.Name)).
+						Request(corev1.ResourceCPU, "2").
+						Priority(0).
+						Annotation(constants.PriorityBoostAnnotationKey, "50").
+						Obj()
+					// Create the boosted workload first; it cannot fit yet (only 1 CPU free) so it
+					// goes inadmissible alongside wlLow.  Finishing wlMid1 frees 2 CPU, causing
+					// both wlLow and wlEffectivePrio to be re-queued.  Because the scheduler heap
+					// now orders by EffectivePriority, wlEffectivePrio (eff=50) is picked before
+					// wlLow (eff=0) and gets the admission slot.
+					util.MustCreate(ctx, k8sClient, wlEffectivePrio)
+					util.FinishWorkloads(ctx, k8sClient, wlMid1)
+					util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, prodClusterQ.Name, wlEffectivePrio)
```

  I think this could flake, also it is not very realistic to create workload with boost already, let's adjust the scenario like this:
  1. create first "mid" priority workload and check it gets admitted
  2. create second "mid" priority workload and check it is pending
  3. create third "low" priority workload and check it is pending (2 pending workloads)
  4. boost the "low" priority workload to be high, and verify it preempts the running workload.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T08:19:28Z

- **pkg/scheduler/scheduler_tas_test.go:3267** by [@mimowo](https://github.com/mimowo) — 2026-03-17T08:19:28Z
```diff
@@ -3264,6 +3264,7 @@ func TestScheduleForTASPreemption(t *testing.T) {
 						Obj()).
 					Obj(),
 				*utiltestingapi.MakeWorkload("low-priority-admitted", "default").
+					UID("low-priority-admitted-uid").
```

  Please revert changes in pre-existing tests which are unrelated

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-03-17T09:48:18Z

- **pkg/scheduler/scheduler_tas_test.go:3267** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-17T09:48:18Z
```diff
@@ -3264,6 +3264,7 @@ func TestScheduleForTASPreemption(t *testing.T) {
 						Obj()).
 					Obj(),
 				*utiltestingapi.MakeWorkload("low-priority-admitted", "default").
+					UID("low-priority-admitted-uid").
```

  This is needed as it now emits PreemptedWorkload event, and we don't want to have an empty uid in message of the event

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T09:53:50Z

- **pkg/cache/queue/inadmissible_workloads_test.go:42** by [@mimowo](https://github.com/mimowo) — 2026-03-17T09:53:50Z
```diff
@@ -31,34 +31,28 @@ import (
 
 func TestNewRequeuer(t *testing.T) {
 	type args struct {
-		features map[featuregate.Feature]bool
-		opts     []RequeuerOption
+		opts []RequeuerOption
 	}
 
 	type want struct {
 		batchPeriod time.Duration
 	}
 
 	testCases := map[string]struct {
-		args args
-		want want
+		enableSchedulerLongRequeueInterval *bool
```

  Hold, why is this changed?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T09:54:05Z

- **pkg/controller/admissionchecks/multikueue/workload_test.go:77** by [@mimowo](https://github.com/mimowo) — 2026-03-17T09:54:06Z
```diff
@@ -74,7 +73,8 @@ func TestWlReconcile(t *testing.T) {
 	baseJobManagedByKueueBuilder := baseJobBuilder.Clone().ManagedBy(kueue.MultiKueueControllerName)
 
 	cases := map[string]struct {
-		features map[featuregate.Feature]bool
+		enableMultiKueueWaitForWorkloadAdmitted *bool
+		enableElasticJobsViaWorkloadSlices      bool
```

  Why is that  changed?

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-03-17T11:34:09Z

- **site/content/en/docs/reference/labels-and-annotations.md:254** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-17T11:34:09Z
```diff
@@ -251,6 +251,21 @@ Used on: [Plain Pods](/docs/tasks/run/plain_pods/).
 
 The annotation key is used to indicate the integration name of the Pod owner.
 
+### kueue.x-k8s.io/priority-boost
```

  https://github.com/kubernetes-sigs/kueue/issues/9936

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T12:53:32Z

- **test/integration/singlecluster/scheduler/scheduler_test.go:695** by [@mimowo](https://github.com/mimowo) — 2026-03-17T12:53:32Z
```diff
@@ -686,9 +686,12 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			wl.Annotations[constants.PriorityBoostAnnotationKey] = "100"
 			gomega.Expect(k8sClient.Patch(ctx, &wl, client.MergeFrom(original))).To(gomega.Succeed())
 
-			ginkgo.By("boosted workload is admitted and preempts the running workload")
-			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, preemptionClusterQ.Name, wlLow)
+			ginkgo.By("boosted workload preempts the running workload")
 			util.ExpectWorkloadsToBePreempted(ctx, k8sClient, wlMid1)
+			ginkgo.By("finish eviction so the slot is freed and boosted workload can be admitted")
+			util.FinishEvictionForWorkloads(ctx, k8sClient, wlMid1)
+			ginkgo.By("boosted workload is admitted")
+			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, preemptionClusterQ.Name, wlLow)
```

  verify also that mid2 remains pending

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T12:55:49Z

- **pkg/cache/queue/cluster_queue.go:266** by [@mimowo](https://github.com/mimowo) — 2026-03-17T12:55:50Z
```diff
@@ -263,13 +263,16 @@ func (c *ClusterQueue) PushOrUpdate(wInfo *workload.Info) {
 	if c.inflight != nil && workload.Key(c.inflight.Obj) == key {
 		return
 	}
+	specChangedWhileInadmissible := false
```

  Is it needed? Isn't enough to check the annotation in the below

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T12:57:14Z

- **pkg/cache/queue/cluster_queue.go:290** by [@mimowo](https://github.com/mimowo) — 2026-03-17T12:57:14Z
```diff
@@ -279,9 +282,12 @@ func (c *ClusterQueue) PushOrUpdate(wInfo *workload.Info) {
 			return
 		}
 		// otherwise move or update in place in the queue.
+		specChangedWhileInadmissible = true
 		c.inadmissibleWorkloads.delete(key)
 	}
-	if c.heap.GetByKey(key) == nil && !c.backoffWaitingTimeExpired(wInfo) {
+	// When spec changed while inadmissible (e.g. priority boost), allow immediate re-queue
+	// so the scheduler can re-evaluate with the new spec.
+	if c.heap.GetByKey(key) == nil && !specChangedWhileInadmissible && !c.backoffWaitingTimeExpired(wInfo) {
```

  tbh, I don't understand the comment, I'm not sure we need the code change also. If we need the code change it looks like it should be preparetory PR, because there should be some scenarios which is already affects. 
  
  Somehow it wasn't needed with the priority mutations by @mbobrovskyi , so I think it should also not be needed here

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T12:57:51Z

- **pkg/cache/queue/cluster_queue.go:275** by [@mimowo](https://github.com/mimowo) — 2026-03-17T12:57:51Z
```diff
@@ -263,13 +263,16 @@ func (c *ClusterQueue) PushOrUpdate(wInfo *workload.Info) {
 	if c.inflight != nil && workload.Key(c.inflight.Obj) == key {
 		return
 	}
+	specChangedWhileInadmissible := false
 	if oldInfo := c.inadmissibleWorkloads.get(key); oldInfo != nil {
 		specChangedSinceEval := oldInfo.LastEvaluatedGeneration != 0 &&
 			wInfo.Obj.Generation != oldInfo.LastEvaluatedGeneration
 
 		// Update in place if the workload didn't change to potentially become admissible.
+		// Annotations (e.g. priority boost) can change effective priority, so treat annotation changes as requiring re-evaluation.
 		if !specChangedSinceEval &&
 			equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) &&
+			equality.Semantic.DeepEqual(oldInfo.Obj.Annotations, wInfo.Obj.Annotations) &&
```

  Don't assert all annotations, instead check if the EffectivePriority changed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-17T14:16:43Z

- **pkg/cache/queue/cluster_queue.go:276** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-17T14:16:43Z
```diff
@@ -268,8 +269,11 @@ func (c *ClusterQueue) PushOrUpdate(wInfo *workload.Info) {
 			wInfo.Obj.Generation != oldInfo.LastEvaluatedGeneration
 
 		// Update in place if the workload didn't change to potentially become admissible.
+		// Annotations (e.g. priority boost) can change effective priority, so treat annotation changes as requiring re-evaluation.
 		if !specChangedSinceEval &&
 			equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) &&
+			// compare effective priority annotations, not the whole annotation map, to avoid unnecessary re-evaluation due to unrelated annotation changes
+			equality.Semantic.DeepEqual(oldInfo.Obj.Annotations[controllerconstants.PriorityBoostAnnotationKey], wInfo.Obj.Annotations[controllerconstants.PriorityBoostAnnotationKey]) &&
```

  ```suggestion
  			oldInfo.Obj.Annotations[controllerconstants.PriorityBoostAnnotationKey] != wInfo.Obj.Annotations[controllerconstants.PriorityBoostAnnotationKey] &&
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-17T14:17:11Z

- **pkg/cache/queue/cluster_queue.go:272** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-17T14:17:12Z
```diff
@@ -268,8 +269,11 @@ func (c *ClusterQueue) PushOrUpdate(wInfo *workload.Info) {
 			wInfo.Obj.Generation != oldInfo.LastEvaluatedGeneration
 
 		// Update in place if the workload didn't change to potentially become admissible.
+		// Annotations (e.g. priority boost) can change effective priority, so treat annotation changes as requiring re-evaluation.
```

  ```suggestion
  ```
  Do we need this?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-17T14:19:27Z

- **pkg/cache/queue/cluster_queue.go:276** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-17T14:19:28Z
```diff
@@ -268,8 +269,11 @@ func (c *ClusterQueue) PushOrUpdate(wInfo *workload.Info) {
 			wInfo.Obj.Generation != oldInfo.LastEvaluatedGeneration
 
 		// Update in place if the workload didn't change to potentially become admissible.
+		// Annotations (e.g. priority boost) can change effective priority, so treat annotation changes as requiring re-evaluation.
 		if !specChangedSinceEval &&
 			equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) &&
+			// compare effective priority annotations, not the whole annotation map, to avoid unnecessary re-evaluation due to unrelated annotation changes
+			equality.Semantic.DeepEqual(oldInfo.Obj.Annotations[controllerconstants.PriorityBoostAnnotationKey], wInfo.Obj.Annotations[controllerconstants.PriorityBoostAnnotationKey]) &&
```

  What if feature gate disabled?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-17T14:21:25Z

- **pkg/scheduler/preemption/common/preemption_policy_test.go:171** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-17T14:21:25Z
```diff
@@ -104,15 +106,69 @@ func TestSatisfiesPreemptionPolicy(t *testing.T) {
 			policy:    kueue.PreemptionPolicyNever,
 			want:      false,
 		},
+		"LowerPriority: boost makes low-priority immune to medium-priority preemption": {
+			featureGates: map[featuregate.Feature]bool{
+				features.PriorityBoost: true,
+			},
+			preemptor: utiltestingapi.MakeWorkload("preemptor", "ns").Priority(200).Obj(),
+			candidate: utiltestingapi.MakeWorkload("candidate", "ns").Priority(100).
+				Annotation(controllerconstants.PriorityBoostAnnotationKey, "150").Obj(),
+			policy: kueue.PreemptionPolicyLowerPriority,
+			want:   false,
+		},
+		"LowerPriority: boost on preemptor enables preemption of higher raw priority": {
+			featureGates: map[featuregate.Feature]bool{
+				features.PriorityBoost: true,
+			},
+			preemptor: utiltestingapi.MakeWorkload("preemptor", "ns").Priority(100).
+				Annotation(controllerconstants.PriorityBoostAnnotationKey, "200").Obj(),
+			candidate: utiltestingapi.MakeWorkload("candidate", "ns").Priority(200).Obj(),
+			policy:    kueue.PreemptionPolicyLowerPriority,
+			want:      true,
+		},
+		"LowerOrNewerEqualPriority: boost equalizes priorities, older preemptor can preempt newer candidate": {
+			featureGates: map[featuregate.Feature]bool{
+				features.PriorityBoost: true,
+			},
+			preemptor: utiltestingapi.MakeWorkload("preemptor", "ns").Priority(100).
+				Annotation(controllerconstants.PriorityBoostAnnotationKey, "100").
+				Creation(now).Obj(),
+			candidate: utiltestingapi.MakeWorkload("candidate", "ns").Priority(200).
+				Creation(now.Add(time.Second)).Obj(),
+			policy: kueue.PreemptionPolicyLowerOrNewerEqualPriority,
+			want:   true,
+		},
+		"LowerOrNewerEqualPriority: equal effective priority, newer preemptor cannot preempt older candidate": {
+			featureGates: map[featuregate.Feature]bool{
+				features.PriorityBoost: true,
+			},
+			preemptor: utiltestingapi.MakeWorkload("preemptor", "ns").Priority(100).
+				Annotation(controllerconstants.PriorityBoostAnnotationKey, "100").
+				Creation(now.Add(time.Second)).Obj(),
+			candidate: utiltestingapi.MakeWorkload("candidate", "ns").Priority(200).
+				Creation(now).Obj(),
+			policy: kueue.PreemptionPolicyLowerOrNewerEqualPriority,
+			want:   false,
+		},
+		"Any: always satisfies regardless of priority or boost": {
+			featureGates: map[featuregate.Feature]bool{
+				features.PriorityBoost: true,
+			},
+			preemptor: utiltestingapi.MakeWorkload("preemptor", "ns").Priority(50).Obj(),
+			candidate: utiltestingapi.MakeWorkload("candidate", "ns").Priority(100).
+				Annotation(controllerconstants.PriorityBoostAnnotationKey, "200").Obj(),
+			policy: kueue.PreemptionPolicyAny,
+			want:   true,
+		},
 	}
 
 	for name, tc := range testCases {
 		t.Run(name, func(t *testing.T) {
-			for feature, enabled := range tc.features {
+			for feature, enabled := range tc.featureGates {
 				features.SetFeatureGateDuringTest(t, feature, enabled)
 			}
 			ordering := workload.Ordering{}
-			got := SatisfiesPreemptionPolicy(tc.preemptor, tc.candidate, ordering, tc.policy)
+			got := SatisfiesPreemptionPolicy(logr.Discard(), tc.preemptor, tc.candidate, ordering, tc.policy)
```

  Use `ctx, log := utiltesting.ContextWithLog(t)`.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-17T14:22:43Z

- **pkg/scheduler/preemption/common/preemption_policy_test.go:113** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-17T14:22:43Z
```diff
@@ -104,15 +106,69 @@ func TestSatisfiesPreemptionPolicy(t *testing.T) {
 			policy:    kueue.PreemptionPolicyNever,
 			want:      false,
 		},
+		"LowerPriority: boost makes low-priority immune to medium-priority preemption": {
+			featureGates: map[featuregate.Feature]bool{
+				features.PriorityBoost: true,
+			},
+			preemptor: utiltestingapi.MakeWorkload("preemptor", "ns").Priority(200).Obj(),
```

  Can we clone the preemptor object like we did in the previous test cases?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-17T14:23:12Z

- **pkg/scheduler/preemption/common/preemption_policy_test.go:113** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-17T14:23:11Z
```diff
@@ -104,15 +106,69 @@ func TestSatisfiesPreemptionPolicy(t *testing.T) {
 			policy:    kueue.PreemptionPolicyNever,
 			want:      false,
 		},
+		"LowerPriority: boost makes low-priority immune to medium-priority preemption": {
+			featureGates: map[featuregate.Feature]bool{
+				features.PriorityBoost: true,
+			},
+			preemptor: utiltestingapi.MakeWorkload("preemptor", "ns").Priority(200).Obj(),
```

  Also other test cases.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-17T14:25:58Z

- **pkg/scheduler/preemption/preemption_test.go:4685** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-17T14:25:58Z
```diff
@@ -4614,23 +4663,26 @@ func TestCandidatesOrdering(t *testing.T) {
 				*wlLowUsageLq,
 				*wlMidUsageLq,
 			},
-			wantCandidates:              []workload.Reference{"mid_lq_usage", "low_lq_usage"},
-			admissionFairSharingEnabled: true,
+			wantCandidates: []workload.Reference{"mid_lq_usage", "low_lq_usage"},
+			featureGates:   map[featuregate.Feature]bool{features.AdmissionFairSharing: true},
 		},
 		"workloads from different CQ are sorted based on priority and timestamp": {
 			candidates: []workload.Info{
 				*wlMidUsageLq,
 				*wlHighUsageLqDifCQ,
 			},
-			wantCandidates:              []workload.Reference{"high_lq_usage_different_cq", "mid_lq_usage"},
-			admissionFairSharingEnabled: true,
+			wantCandidates: []workload.Reference{"high_lq_usage_different_cq", "mid_lq_usage"},
+			featureGates:   map[featuregate.Feature]bool{features.AdmissionFairSharing: true},
 		}}
 
 	_, log := utiltesting.ContextWithLog(t)
 	for _, tc := range cases {
-		features.SetFeatureGateDuringTest(t, features.AdmissionFairSharing, tc.admissionFairSharingEnabled)
+		for fg, enable := range tc.featureGates {
+			features.SetFeatureGateDuringTest(t, fg, enable)
+		}
+		admissionFairSharingEnabled := tc.featureGates != nil && tc.featureGates[features.AdmissionFairSharing]
 		slices.SortFunc(tc.candidates, func(a, b workload.Info) int {
-			return preemptioncommon.CandidatesOrdering(log, tc.admissionFairSharingEnabled, &a, &b, kueue.ClusterQueueReference(preemptorCq), now)
+			return preemptioncommon.CandidatesOrdering(log, admissionFairSharingEnabled, &a, &b, kueue.ClusterQueueReference(preemptorCq), now)
```

  ```suggestion
  			return preemptioncommon.CandidatesOrdering(log, tc.featureGates[features.AdmissionFairSharing], &a, &b, kueue.ClusterQueueReference(preemptorCq), now)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-17T14:33:50Z

- **pkg/webhooks/workload_webhook_test.go:261** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-17T14:33:50Z
```diff
@@ -225,9 +228,61 @@ func TestValidateWorkload(t *testing.T) {
 				field.Invalid(podSetsPath, nil, ""),
 			},
 		},
+		"valid priority-boost": {
+			featureGates: map[featuregate.Feature]bool{features.PriorityBoost: true},
+			workload: utiltestingapi.MakeWorkload(testWorkloadName, testWorkloadNamespace).
+				PodSets(*utiltestingapi.MakePodSet("main", 1).Obj()).
+				Annotation(controllerconstants.PriorityBoostAnnotationKey, "10").
+				Obj(),
+			wantErr: nil,
+		},
+		"valid priority-boost zero": {
+			featureGates: map[featuregate.Feature]bool{features.PriorityBoost: true},
+			workload: utiltestingapi.MakeWorkload(testWorkloadName, testWorkloadNamespace).
+				PodSets(*utiltestingapi.MakePodSet("main", 1).Obj()).
+				Annotation(controllerconstants.PriorityBoostAnnotationKey, "0").
+				Obj(),
+			wantErr: nil,
+		},
+		"valid priority-boost negative": {
+			featureGates: map[featuregate.Feature]bool{features.PriorityBoost: true},
+			workload: utiltestingapi.MakeWorkload(testWorkloadName, testWorkloadNamespace).
+				PodSets(*utiltestingapi.MakePodSet("main", 1).Obj()).
+				Annotation(controllerconstants.PriorityBoostAnnotationKey, "-5").
+				Obj(),
+			wantErr: nil,
+		},
+		"invalid priority-boost": {
+			featureGates: map[featuregate.Feature]bool{features.PriorityBoost: true},
+			workload: utiltestingapi.MakeWorkload(testWorkloadName, testWorkloadNamespace).
+				PodSets(*utiltestingapi.MakePodSet("main", 1).Obj()).
+				Annotation(controllerconstants.PriorityBoostAnnotationKey, "invalid").
+				Obj(),
+			wantErr: field.ErrorList{
```

  Please also add test case with empty string.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-17T14:34:43Z

- **pkg/workload/workload.go:1697** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-17T14:34:43Z
```diff
@@ -1685,6 +1693,6 @@ func PriorityChanged(old, new *kueue.Workload) bool {
 		PriorityClassName(old) != PriorityClassName(new) {
 		return true
 	}
-	// Check if priority value changed (for WorkloadPriorityClass value updates).
-	return priority.Priority(old) != priority.Priority(new)
+	// Check if effective priority changed (for WorkloadPriorityClass value updates or priority-boost annotation).
+	return priority.EffectivePriority(logr.Discard(), old) != priority.EffectivePriority(logr.Discard(), new)
```

  Please propagate logger instead.

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-03-17T14:36:48Z

- **pkg/cache/queue/cluster_queue.go:276** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-17T14:36:48Z
```diff
@@ -268,8 +269,11 @@ func (c *ClusterQueue) PushOrUpdate(wInfo *workload.Info) {
 			wInfo.Obj.Generation != oldInfo.LastEvaluatedGeneration
 
 		// Update in place if the workload didn't change to potentially become admissible.
+		// Annotations (e.g. priority boost) can change effective priority, so treat annotation changes as requiring re-evaluation.
 		if !specChangedSinceEval &&
 			equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) &&
+			// compare effective priority annotations, not the whole annotation map, to avoid unnecessary re-evaluation due to unrelated annotation changes
+			equality.Semantic.DeepEqual(oldInfo.Obj.Annotations[controllerconstants.PriorityBoostAnnotationKey], wInfo.Obj.Annotations[controllerconstants.PriorityBoostAnnotationKey]) &&
```

  I think == should be here

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-17T14:36:53Z

- **test/integration/singlecluster/scheduler/scheduler_test.go:687** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-17T14:36:53Z
```diff
@@ -644,6 +645,57 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			})
 		})
 
+		ginkgo.It("when PriorityBoost is enabled, boosting a low-priority pending workload preempts the running workload", func() {
+			features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.PriorityBoost, true)
+			const lowPrio, midPrio = 0, 10
+			// preemptionClusterQ has 3 CPU and allows within-cluster preemption by effective priority
+			wlMid1 := utiltestingapi.MakeWorkload("wl-mid-1", ns.Name).
+				Queue(kueue.LocalQueueName(preemptionQueue.Name)).
+				Request(corev1.ResourceCPU, "2").
+				Priority(midPrio).
+				Obj()
+			util.MustCreate(ctx, k8sClient, wlMid1)
+			ginkgo.By("first mid priority workload gets admitted")
+			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, preemptionClusterQ.Name, wlMid1)
+
+			wlMid2 := utiltestingapi.MakeWorkload("wl-mid-2", ns.Name).
+				Queue(kueue.LocalQueueName(preemptionQueue.Name)).
+				Request(corev1.ResourceCPU, "2").
+				Priority(midPrio).
+				Obj()
+			util.MustCreate(ctx, k8sClient, wlMid2)
+			ginkgo.By("second mid priority workload is pending")
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wlMid2)
+
+			wlLow := utiltestingapi.MakeWorkload("wl-low", ns.Name).
+				Queue(kueue.LocalQueueName(preemptionQueue.Name)).
+				Request(corev1.ResourceCPU, "2").
+				Priority(lowPrio).
+				Obj()
+			util.MustCreate(ctx, k8sClient, wlLow)
+			ginkgo.By("low priority workload is pending (two pending workloads)")
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wlMid2, wlLow)
+
+			ginkgo.By("boost the low priority workload to be high")
+			var wl kueue.Workload
+			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wlLow), &wl)).To(gomega.Succeed())
+			original := wl.DeepCopy()
+			if wl.Annotations == nil {
+				wl.Annotations = make(map[string]string)
+			}
+			wl.Annotations[constants.PriorityBoostAnnotationKey] = "100"
+			gomega.Expect(k8sClient.Patch(ctx, &wl, client.MergeFrom(original))).To(gomega.Succeed())
```

  Use Eventually to make sure we will not catch flakes.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-17T14:38:05Z

- **test/integration/singlecluster/scheduler/scheduler_test.go:687** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-17T14:38:05Z
```diff
@@ -644,6 +645,57 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			})
 		})
 
+		ginkgo.It("when PriorityBoost is enabled, boosting a low-priority pending workload preempts the running workload", func() {
+			features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.PriorityBoost, true)
+			const lowPrio, midPrio = 0, 10
+			// preemptionClusterQ has 3 CPU and allows within-cluster preemption by effective priority
+			wlMid1 := utiltestingapi.MakeWorkload("wl-mid-1", ns.Name).
+				Queue(kueue.LocalQueueName(preemptionQueue.Name)).
+				Request(corev1.ResourceCPU, "2").
+				Priority(midPrio).
+				Obj()
+			util.MustCreate(ctx, k8sClient, wlMid1)
+			ginkgo.By("first mid priority workload gets admitted")
+			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, preemptionClusterQ.Name, wlMid1)
+
+			wlMid2 := utiltestingapi.MakeWorkload("wl-mid-2", ns.Name).
+				Queue(kueue.LocalQueueName(preemptionQueue.Name)).
+				Request(corev1.ResourceCPU, "2").
+				Priority(midPrio).
+				Obj()
+			util.MustCreate(ctx, k8sClient, wlMid2)
+			ginkgo.By("second mid priority workload is pending")
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wlMid2)
+
+			wlLow := utiltestingapi.MakeWorkload("wl-low", ns.Name).
+				Queue(kueue.LocalQueueName(preemptionQueue.Name)).
+				Request(corev1.ResourceCPU, "2").
+				Priority(lowPrio).
+				Obj()
+			util.MustCreate(ctx, k8sClient, wlLow)
+			ginkgo.By("low priority workload is pending (two pending workloads)")
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wlMid2, wlLow)
+
+			ginkgo.By("boost the low priority workload to be high")
+			var wl kueue.Workload
+			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wlLow), &wl)).To(gomega.Succeed())
+			original := wl.DeepCopy()
+			if wl.Annotations == nil {
+				wl.Annotations = make(map[string]string)
+			}
+			wl.Annotations[constants.PriorityBoostAnnotationKey] = "100"
+			gomega.Expect(k8sClient.Patch(ctx, &wl, client.MergeFrom(original))).To(gomega.Succeed())
```

  Use Update instead.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-17T14:39:09Z

- **pkg/cache/queue/cluster_queue.go:276** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-17T14:39:09Z
```diff
@@ -268,8 +269,11 @@ func (c *ClusterQueue) PushOrUpdate(wInfo *workload.Info) {
 			wInfo.Obj.Generation != oldInfo.LastEvaluatedGeneration
 
 		// Update in place if the workload didn't change to potentially become admissible.
+		// Annotations (e.g. priority boost) can change effective priority, so treat annotation changes as requiring re-evaluation.
 		if !specChangedSinceEval &&
 			equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) &&
+			// compare effective priority annotations, not the whole annotation map, to avoid unnecessary re-evaluation due to unrelated annotation changes
+			equality.Semantic.DeepEqual(oldInfo.Obj.Annotations[controllerconstants.PriorityBoostAnnotationKey], wInfo.Obj.Annotations[controllerconstants.PriorityBoostAnnotationKey]) &&
```

  Yeah, my bad.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T16:54:18Z

- **pkg/cache/queue/cluster_queue.go:275** by [@mimowo](https://github.com/mimowo) — 2026-03-17T16:54:18Z
```diff
@@ -278,14 +270,17 @@ func (c *ClusterQueue) PushOrUpdate(wInfo *workload.Info) {
 			wInfo.Obj.Generation != oldInfo.LastEvaluatedGeneration
 
 		// Update in place if the workload didn't change to potentially become admissible.
+		// When PriorityBoost is enabled, compare the annotation so we don't skip re-evaluation when only effective priority (boost) changed.
+		priorityBoostUnchanged := !features.Enabled(features.PriorityBoost) ||
+			oldInfo.Obj.Annotations[controllerconstants.PriorityBoostAnnotationKey] == wInfo.Obj.Annotations[controllerconstants.PriorityBoostAnnotationKey]
```

  Can you move it to a helper function? In order to skip evaluation eagerly. Then we will only resolve if spec is the same

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T17:41:56Z

/lgtm
/approve
Thank you 👍
