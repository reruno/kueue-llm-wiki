# PR #9135: Fix flake in scale-down logic for LeaderWorkerSet

**Summary**: Fix flake in scale-down logic for LeaderWorkerSet

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9135

**Last updated**: 2026-02-17T12:52:11Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PannagaRao](https://github.com/PannagaRao)
- **Branch**: `fix-flake-scale-down-lws` → `main`
- **Created**: 2026-02-11T21:54:43Z
- **Updated**: 2026-02-17T12:52:11Z
- **Merged**: 2026-02-17T10:47:05Z
- **Closed**: 2026-02-17T10:47:05Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `kind/flake`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@sohankunkerkar](https://github.com/sohankunkerkar)
- **Requested reviewers**: [@pajakd](https://github.com/pajakd), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 2   **+179 / -1**

## Description

Signed-off-by: Pannaga Rao Bhoja Ramamanohara

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug
/kind flake
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->


#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes # https://github.com/kubernetes-sigs/kueue/issues/9034

#### Special notes for your reviewer:

Ran the flaky test 300 times with the fix applied — all passed.
Reference : https://github.com/kubernetes-sigs/kueue/pull/9050

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
LeaderWorkerSet: fix an occasional race condition resulting in workload deletion getting stuck during scale down.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-02-11T21:54:50Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | ca3d211fdb1dee5e1ae8c9d7a7015e8c7ffb8f3d |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/699380fe4d82060008e2c407 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-11T21:54:53Z

Hi @PannagaRao. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-11T21:55:38Z

/ok-to-test

### Comment by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-11T22:41:40Z

/test pull-kueue-test-e2e-certmanager-main

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-16T21:39:24Z

LGTM label has been added.  <details>Git tree hash: f9da77686fe0d04f5d37a8293f48889b7f1493f8</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-17T10:13:56Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9135#pullrequestreview-3812980938" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9135#" title="Author self-approved">PannagaRao</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9135#pullrequestreview-3810606692" title="LGTM">sohankunkerkar</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-17T10:20:23Z

/release-note-edit
```release-note
LeaderWorkerSet: fix occasional race condition resulting in workload deletion getting stuck during scale down.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-17T10:20:37Z

/release-note-edit
```release-note
LeaderWorkerSet: fix an occasional race condition resulting in workload deletion getting stuck during scale down.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-17T12:50:45Z

/cherrypick release-0.16
/cherrypick release-0.15

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-02-17T12:51:22Z

@mimowo: #9135 failed to apply on top of branch "release-0.15":
```
Applying: Fix flake in scale-down logic for LeaderWorkerSet
Using index info to reconstruct a base tree...
M	pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go
CONFLICT (content): Merge conflict in pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Fix flake in scale-down logic for LeaderWorkerSet

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9135#issuecomment-3914538146):

>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-02-17T12:51:57Z

@mimowo: #9135 failed to apply on top of branch "release-0.16":
```
Applying: Fix flake in scale-down logic for LeaderWorkerSet
Using index info to reconstruct a base tree...
M	pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go
CONFLICT (content): Merge conflict in pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Fix flake in scale-down logic for LeaderWorkerSet

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9135#issuecomment-3914538146):

>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-17T12:52:11Z

@PannagaRao please prepare the cherry-picks

cc @sohankunkerkar

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-12T06:26:15Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:150** by [@mimowo](https://github.com/mimowo) — 2026-02-12T06:26:16Z
```diff
@@ -145,6 +145,11 @@ func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reco
 
 	eg.Go(func() error {
 		return parallelize.Until(ctx, len(toDelete), func(i int) error {
+			// Remove the finalizer before deleting to ensure prompt cleanup,
+			// consistent with how the job framework reconciler deletes workloads.
+			if err := workload.RemoveFinalizer(ctx, r.client, toDelete[i]); err != nil {
```

  I'm wondering why this would be needed, because the workload_controller already removes finalizers for workloads being deleted: https://github.com/kubernetes-sigs/kueue/blob/8358e71fca91b564fb945204b338026801055cdb/pkg/controller/core/workload_controller.go#L193-L194
  
  Is the extra check for no owner reference the causing the issue? In that case I think it makes sense, but I would like to confirm if this is really needed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-12T06:27:52Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:150** by [@mimowo](https://github.com/mimowo) — 2026-02-12T06:27:52Z
```diff
@@ -145,6 +145,11 @@ func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reco
 
 	eg.Go(func() error {
 		return parallelize.Until(ctx, len(toDelete), func(i int) error {
+			// Remove the finalizer before deleting to ensure prompt cleanup,
+			// consistent with how the job framework reconciler deletes workloads.
+			if err := workload.RemoveFinalizer(ctx, r.client, toDelete[i]); err != nil {
```

  Ah, regardless it would be good to make the conde consistent with  https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/reconciler.go#L956-L964
  
  It would be good to commonize that in a follow up.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-12T06:29:22Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:213** by [@mimowo](https://github.com/mimowo) — 2026-02-12T06:29:22Z
```diff
@@ -197,6 +204,14 @@ func (r *Reconciler) filterWorkloads(lws *leaderworkersetv1.LeaderWorkerSet, exi
 	return toCreate, toUpdate, slices.Collect(maps.Values(toDelete))
 }
 
+func isRollingUpdateWithSurge(lws *leaderworkersetv1.LeaderWorkerSet) bool {
+	if lws.Spec.RolloutStrategy.RollingUpdateConfiguration == nil {
+		return false
+	}
+	maxSurge := int32(lws.Spec.RolloutStrategy.RollingUpdateConfiguration.MaxSurge.IntValue())
+	return maxSurge > 0 && lws.Status.UpdatedReplicas < ptr.Deref(lws.Spec.Replicas, 1)
```

  Is the extra check `lws.Status.UpdatedReplicas < ptr.Deref(lws.Spec.Replicas, 1)` needed?

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-02-12T07:19:38Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:150** by [@PannagaRao](https://github.com/PannagaRao) — 2026-02-12T07:19:38Z
```diff
@@ -145,6 +145,11 @@ func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reco
 
 	eg.Go(func() error {
 		return parallelize.Until(ctx, len(toDelete), func(i int) error {
+			// Remove the finalizer before deleting to ensure prompt cleanup,
+			// consistent with how the job framework reconciler deletes workloads.
+			if err := workload.RemoveFinalizer(ctx, r.client, toDelete[i]); err != nil {
```

  Yes, LWS workloads always have the LeaderWorkerSet as an owner reference, so the workload controller's finalizer removal path (which requires len(wl.OwnerReferences) == 0) never triggers for them.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-02-12T07:23:13Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:213** by [@PannagaRao](https://github.com/PannagaRao) — 2026-02-12T07:23:13Z
```diff
@@ -197,6 +204,14 @@ func (r *Reconciler) filterWorkloads(lws *leaderworkersetv1.LeaderWorkerSet, exi
 	return toCreate, toUpdate, slices.Collect(maps.Values(toDelete))
 }
 
+func isRollingUpdateWithSurge(lws *leaderworkersetv1.LeaderWorkerSet) bool {
+	if lws.Spec.RolloutStrategy.RollingUpdateConfiguration == nil {
+		return false
+	}
+	maxSurge := int32(lws.Spec.RolloutStrategy.RollingUpdateConfiguration.MaxSurge.IntValue())
+	return maxSurge > 0 && lws.Status.UpdatedReplicas < ptr.Deref(lws.Spec.Replicas, 1)
```

  Since maxSurge > 0 is a static config value and it's true even when no rolling update is happening. Without the UpdatedReplicas check, a normal scale-down on an LWS that happens to have maxSurge configured would still use stale status.Replicas. So I thought this check would be needed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-12T07:30:42Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:213** by [@mimowo](https://github.com/mimowo) — 2026-02-12T07:30:42Z
```diff
@@ -197,6 +204,14 @@ func (r *Reconciler) filterWorkloads(lws *leaderworkersetv1.LeaderWorkerSet, exi
 	return toCreate, toUpdate, slices.Collect(maps.Values(toDelete))
 }
 
+func isRollingUpdateWithSurge(lws *leaderworkersetv1.LeaderWorkerSet) bool {
+	if lws.Spec.RolloutStrategy.RollingUpdateConfiguration == nil {
+		return false
+	}
+	maxSurge := int32(lws.Spec.RolloutStrategy.RollingUpdateConfiguration.MaxSurge.IntValue())
+	return maxSurge > 0 && lws.Status.UpdatedReplicas < ptr.Deref(lws.Spec.Replicas, 1)
```

  maybe this is a useful check, but I want to understand what is the underlying reason for the flake and what is the minimal fix, bacause this PR is changing both Workload removal and the max surge handling

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-12T12:02:36Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:213** by [@mimowo](https://github.com/mimowo) — 2026-02-12T12:02:36Z
```diff
@@ -197,6 +204,14 @@ func (r *Reconciler) filterWorkloads(lws *leaderworkersetv1.LeaderWorkerSet, exi
 	return toCreate, toUpdate, slices.Collect(maps.Values(toDelete))
 }
 
+func isRollingUpdateWithSurge(lws *leaderworkersetv1.LeaderWorkerSet) bool {
+	if lws.Spec.RolloutStrategy.RollingUpdateConfiguration == nil {
+		return false
+	}
+	maxSurge := int32(lws.Spec.RolloutStrategy.RollingUpdateConfiguration.MaxSurge.IntValue())
+	return maxSurge > 0 && lws.Status.UpdatedReplicas < ptr.Deref(lws.Spec.Replicas, 1)
```

  > Without the UpdatedReplicas check, a normal scale-down on an LWS that happens to have maxSurge configured would still use stale status.Replicas. 
  
  After the second thought this looks reasonable, but the question is if we could verify this works well in a dedicated test maybe?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-12T19:04:09Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:213** by [@mimowo](https://github.com/mimowo) — 2026-02-12T19:04:08Z
```diff
@@ -197,6 +204,14 @@ func (r *Reconciler) filterWorkloads(lws *leaderworkersetv1.LeaderWorkerSet, exi
 	return toCreate, toUpdate, slices.Collect(maps.Values(toDelete))
 }
 
+func isRollingUpdateWithSurge(lws *leaderworkersetv1.LeaderWorkerSet) bool {
+	if lws.Spec.RolloutStrategy.RollingUpdateConfiguration == nil {
+		return false
+	}
+	maxSurge := int32(lws.Spec.RolloutStrategy.RollingUpdateConfiguration.MaxSurge.IntValue())
+	return maxSurge > 0 && lws.Status.UpdatedReplicas < ptr.Deref(lws.Spec.Replicas, 1)
```

  It would be great to also open an experimental PR which shows there is no flake when looped 200 times, as it was done here: https://github.com/kubernetes-sigs/kueue/pull/9181

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-02-12T19:07:15Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:213** by [@PannagaRao](https://github.com/PannagaRao) — 2026-02-12T19:07:15Z
```diff
@@ -197,6 +204,14 @@ func (r *Reconciler) filterWorkloads(lws *leaderworkersetv1.LeaderWorkerSet, exi
 	return toCreate, toUpdate, slices.Collect(maps.Values(toDelete))
 }
 
+func isRollingUpdateWithSurge(lws *leaderworkersetv1.LeaderWorkerSet) bool {
+	if lws.Spec.RolloutStrategy.RollingUpdateConfiguration == nil {
+		return false
+	}
+	maxSurge := int32(lws.Spec.RolloutStrategy.RollingUpdateConfiguration.MaxSurge.IntValue())
+	return maxSurge > 0 && lws.Status.UpdatedReplicas < ptr.Deref(lws.Spec.Replicas, 1)
```

  Do you mean only with this change and without finalizer change? I already have one expiremental PR for both of them together. I have attached it in the current PR description. https://github.com/kubernetes-sigs/kueue/pull/9050

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-12T19:11:43Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:213** by [@mimowo](https://github.com/mimowo) — 2026-02-12T19:11:42Z
```diff
@@ -197,6 +204,14 @@ func (r *Reconciler) filterWorkloads(lws *leaderworkersetv1.LeaderWorkerSet, exi
 	return toCreate, toUpdate, slices.Collect(maps.Values(toDelete))
 }
 
+func isRollingUpdateWithSurge(lws *leaderworkersetv1.LeaderWorkerSet) bool {
+	if lws.Spec.RolloutStrategy.RollingUpdateConfiguration == nil {
+		return false
+	}
+	maxSurge := int32(lws.Spec.RolloutStrategy.RollingUpdateConfiguration.MaxSurge.IntValue())
+	return maxSurge > 0 && lws.Status.UpdatedReplicas < ptr.Deref(lws.Spec.Replicas, 1)
```

  Oh, I missed that. I think the combined PR makes sense.
  
  So I'm happy to merge it as scale-down fix, but I'm still wondering about this case `lws.Status.UpdatedReplicas < ptr.Deref(lws.Spec.Replicas, 1)` which is probably not detectable by any existing test. It would be great to have a test as you described: "Without the UpdatedReplicas check, a normal scale-down on an LWS that happens to have maxSurge configured would still use stale status.Replicas. So I thought this check would be needed."
  
  wdyt?

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-02-12T19:13:49Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:213** by [@PannagaRao](https://github.com/PannagaRao) — 2026-02-12T19:13:49Z
```diff
@@ -197,6 +204,14 @@ func (r *Reconciler) filterWorkloads(lws *leaderworkersetv1.LeaderWorkerSet, exi
 	return toCreate, toUpdate, slices.Collect(maps.Values(toDelete))
 }
 
+func isRollingUpdateWithSurge(lws *leaderworkersetv1.LeaderWorkerSet) bool {
+	if lws.Spec.RolloutStrategy.RollingUpdateConfiguration == nil {
+		return false
+	}
+	maxSurge := int32(lws.Spec.RolloutStrategy.RollingUpdateConfiguration.MaxSurge.IntValue())
+	return maxSurge > 0 && lws.Status.UpdatedReplicas < ptr.Deref(lws.Spec.Replicas, 1)
```

  Yes, I will add a test to check scale down with maxSurge configured.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-02-13T23:34:54Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:213** by [@PannagaRao](https://github.com/PannagaRao) — 2026-02-13T23:34:54Z
```diff
@@ -197,6 +204,14 @@ func (r *Reconciler) filterWorkloads(lws *leaderworkersetv1.LeaderWorkerSet, exi
 	return toCreate, toUpdate, slices.Collect(maps.Values(toDelete))
 }
 
+func isRollingUpdateWithSurge(lws *leaderworkersetv1.LeaderWorkerSet) bool {
+	if lws.Spec.RolloutStrategy.RollingUpdateConfiguration == nil {
+		return false
+	}
+	maxSurge := int32(lws.Spec.RolloutStrategy.RollingUpdateConfiguration.MaxSurge.IntValue())
+	return maxSurge > 0 && lws.Status.UpdatedReplicas < ptr.Deref(lws.Spec.Replicas, 1)
```

  I have added the tests. PTAL!

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-02-16T19:43:58Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:150** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-02-16T19:43:52Z
```diff
@@ -145,6 +145,11 @@ func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reco
 
 	eg.Go(func() error {
 		return parallelize.Until(ctx, len(toDelete), func(i int) error {
+			// Remove the finalizer before deleting to ensure prompt cleanup,
+			// consistent with how the job framework reconciler deletes workloads.
+			if err := workload.RemoveFinalizer(ctx, r.client, toDelete[i]); err != nil {
```

  nit: just to be consistent with jobframework
  should we also check `if err != nil && !apierrors.IsNotFound(err) `? Might be useful to avoid spurious errors in case workload is deleted between List and Delete.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (approved) — 2026-02-16T21:39:17Z

/lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-17T10:13:47Z

Thank you 👍 
/approve
/cherrypick release-0.16
/cherrypick release-0.15
