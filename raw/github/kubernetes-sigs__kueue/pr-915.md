# PR #915: Changed logic of pods ready cond

**Summary**: Changed logic of pods ready cond

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/915

**Last updated**: 2024-03-12T08:57:25Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@stuton](https://github.com/stuton)
- **Branch**: `flaky-test` → `main`
- **Created**: 2023-06-28T15:15:44Z
- **Updated**: 2024-03-12T08:57:25Z
- **Merged**: 2023-07-06T16:23:04Z
- **Closed**: 2023-07-06T16:23:04Z
- **Labels**: `kind/bug`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `cncf-cla: yes`, `size/S`
- **Assignees**: [@trasc](https://github.com/trasc)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 3   **+13 / -9**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Trying to solve race condition when context is done

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #886

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-06-28T15:15:49Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 841197146b36f2658d8fc36d5839deeee62d619d |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/64a6d086a9a9920007eea99a |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-06-28T15:15:53Z

Hi @stuton. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-28T16:13:42Z

/cc @tenzen-y

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-29T12:17:30Z

/ok-to-test

### Comment by [@trasc](https://github.com/trasc) — 2023-07-06T14:30:38Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-07-06T14:57:08Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/915#pullrequestreview-1516747833" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/915#" title="Author self-approved">stuton</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@trasc](https://github.com/trasc) — 2023-07-06T16:22:04Z

/lgtm

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-06-29T13:43:19Z

This looks very complicated :)

I think the bug is that `CleanUpOnContext` doesn't obtain the lock before calling broadcast.

- **pkg/cache/cache.go:235** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-29T13:37:57Z
```diff
@@ -220,18 +220,35 @@ func (c *Cache) WaitForPodsReady(ctx context.Context) {
 	defer c.Unlock()
 
 	log := ctrl.LoggerFrom(ctx)
+
+	podsReadyCond := func(ch chan any) {
+		c.Lock()
+		defer c.Unlock()
+		defer close(ch)
+		c.podsReadyCond.Wait()
+		ch <- struct{}{}
+	}
+
+	podsReadyCondCh := make(chan any)
+
 	for {
 		if c.podsReadyForAllAdmittedWorkloads(ctx) {
```

  it doesn't look like this function needs the `ctx`. We can pass a the `log` instead.
  You can do it in this PR.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-29T15:45:46Z

- **pkg/cache/cache.go:232** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-29T15:45:13Z
```diff
@@ -220,18 +220,35 @@ func (c *Cache) WaitForPodsReady(ctx context.Context) {
 	defer c.Unlock()
 
 	log := ctrl.LoggerFrom(ctx)
+
+	podsReadyCond := func(ch chan any) {
+		c.Lock()
+		defer c.Unlock()
+		defer close(ch)
+		c.podsReadyCond.Wait()
+		ch <- struct{}{}
+	}
+
+	podsReadyCondCh := make(chan any)
```

  ```suggestion
  	podsReadyCondCh := make(chan struct{})
  	defer close(podsReadyCondCh)
  ```
  
  Can we use `struct{}` typed to reduce the amount of memory?
  Also, should we close this channel?

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-07-03T08:19:08Z

- **pkg/cache/cache.go:232** by [@stuton](https://github.com/stuton) — 2023-07-03T08:19:08Z
```diff
@@ -220,18 +220,35 @@ func (c *Cache) WaitForPodsReady(ctx context.Context) {
 	defer c.Unlock()
 
 	log := ctrl.LoggerFrom(ctx)
+
+	podsReadyCond := func(ch chan any) {
+		c.Lock()
+		defer c.Unlock()
+		defer close(ch)
+		c.podsReadyCond.Wait()
+		ch <- struct{}{}
+	}
+
+	podsReadyCondCh := make(chan any)
```

  sure, changed to struct type. Yeah, the channel is already closed [inside goroutine](https://github.com/kubernetes-sigs/kueue/pull/915/files#diff-964e351ee2375d359c78d69e514c4edc42577219761c4475f391ed2daf715e51R127)

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-07-04T10:44:14Z

- **pkg/cache/cache.go:232** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-04T10:44:14Z
```diff
@@ -220,18 +220,35 @@ func (c *Cache) WaitForPodsReady(ctx context.Context) {
 	defer c.Unlock()
 
 	log := ctrl.LoggerFrom(ctx)
+
+	podsReadyCond := func(ch chan any) {
+		c.Lock()
+		defer c.Unlock()
+		defer close(ch)
+		c.podsReadyCond.Wait()
+		ch <- struct{}{}
+	}
+
+	podsReadyCondCh := make(chan any)
```

  > the channel is already closed inside goroutine
  
  Ah, I see. I missed the line. Also, we shouldn't call `defer` inside the infinity loop since the memory leak will be caused by the `defer`.
  
  So, your code looks good, thanks!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-07-04T10:51:20Z

- **pkg/cache/cache.go:127** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-04T10:50:38Z
```diff
@@ -117,38 +118,46 @@ func (c *Cache) WaitForPodsReady(ctx context.Context) {
 		return
 	}
 
-	c.Lock()
-	defer c.Unlock()
-
 	log := ctrl.LoggerFrom(ctx)
+
+	podsReadyCond := func(ch chan struct{}) {
+		c.Lock()
+		c.podsReadyCond.Wait()
+		c.Unlock()
+		close(ch)
```

  ```suggestion
  		c.Lock()
  		defer c.Unlock()
  		defer close(ch)		
  		c.podsReadyCond.Wait()
  ```
  
  @stuton Should we use `defer` to catch panic?

- **pkg/cache/cache.go:121** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-04T10:51:08Z
```diff
@@ -117,38 +118,46 @@ func (c *Cache) WaitForPodsReady(ctx context.Context) {
 		return
 	}
 
-	c.Lock()
-	defer c.Unlock()
-
 	log := ctrl.LoggerFrom(ctx)
```

  Can you address @alculquicondor's comment?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-07-04T10:52:52Z

- **pkg/cache/cache.go:121** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-04T10:52:52Z
```diff
@@ -117,38 +118,46 @@ func (c *Cache) WaitForPodsReady(ctx context.Context) {
 		return
 	}
 
-	c.Lock()
-	defer c.Unlock()
-
 	log := ctrl.LoggerFrom(ctx)
```

  nvm
  Sorry, this is my misunderstanding.

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-07-04T12:41:34Z

- **pkg/cache/cache.go:127** by [@stuton](https://github.com/stuton) — 2023-07-04T12:41:34Z
```diff
@@ -117,38 +118,46 @@ func (c *Cache) WaitForPodsReady(ctx context.Context) {
 		return
 	}
 
-	c.Lock()
-	defer c.Unlock()
-
 	log := ctrl.LoggerFrom(ctx)
+
+	podsReadyCond := func(ch chan struct{}) {
+		c.Lock()
+		c.podsReadyCond.Wait()
+		c.Unlock()
+		close(ch)
```

  Yeah, you right

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-04T14:49:29Z

- **pkg/cache/cache.go:123** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-04T14:48:38Z
```diff
@@ -117,38 +118,46 @@ func (c *Cache) WaitForPodsReady(ctx context.Context) {
 		return
 	}
 
-	c.Lock()
-	defer c.Unlock()
-
 	log := ctrl.LoggerFrom(ctx)
+
+	podsReadyCond := func(ch chan struct{}) {
```

  Are you sure we still need these changes after fixing `CleanUpOnContext` to grab the lock?

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-07-05T10:38:02Z

- **pkg/cache/cache.go:123** by [@stuton](https://github.com/stuton) — 2023-07-05T10:38:02Z
```diff
@@ -117,38 +118,46 @@ func (c *Cache) WaitForPodsReady(ctx context.Context) {
 		return
 	}
 
-	c.Lock()
-	defer c.Unlock()
-
 	log := ctrl.LoggerFrom(ctx)
+
+	podsReadyCond := func(ch chan struct{}) {
```

  yes, the main problem is in the default case in a select statement, it becomes non-blocking and when context is canceled exactly after of passing the first condition of select, but before calling the function 'Wait'. If we put the channel instead of default case we'll wait until it could receive 'close' from podsReadyCondCh.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-05T14:00:18Z

- **pkg/cache/cache.go:123** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-05T14:00:18Z
```diff
@@ -117,38 +118,46 @@ func (c *Cache) WaitForPodsReady(ctx context.Context) {
 		return
 	}
 
-	c.Lock()
-	defer c.Unlock()
-
 	log := ctrl.LoggerFrom(ctx)
+
+	podsReadyCond := func(ch chan struct{}) {
```

  > the main problem is in the default case in a select statement, it becomes non-blocking and when context is canceled exactly after of passing the first condition of select, but before calling the function 'Wait'.
  
  While that could still happen, it's no longer a problem, precisely because `CleanUpOnContext` obtains a lock.
  
  This function (`WaitForPodsReady`) also has the lock. The lock is only freed when calling `c.podsReadyCond.Wait`. At that point, `CleanUpOnContext` will be granted the lock and it will call `Broadcast`.
  
  Then this function will be able to proceed. Perhaps the only thing missing is a check for whether the context is already closed.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-06T14:30:23Z

- **pkg/cache/cache.go:125** by [@trasc](https://github.com/trasc) — 2023-07-06T14:29:42Z
```diff
@@ -121,11 +122,13 @@ func (c *Cache) WaitForPodsReady(ctx context.Context) {
 	defer c.Unlock()
 
 	log := ctrl.LoggerFrom(ctx)
+
```

  nit: remove

- **pkg/cache/cache.go:131** by [@trasc](https://github.com/trasc) — 2023-07-06T14:29:54Z
```diff
@@ -121,11 +122,13 @@ func (c *Cache) WaitForPodsReady(ctx context.Context) {
 	defer c.Unlock()
 
 	log := ctrl.LoggerFrom(ctx)
+
 	for {
-		if c.podsReadyForAllAdmittedWorkloads(ctx) {
+		if c.podsReadyForAllAdmittedWorkloads(log) {
 			return
 		}
 		log.V(3).Info("Blocking admission as not all workloads are in the PodsReady condition")
+
```

  nit: remove

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-06T14:57:00Z

/approve
/hold cancel
