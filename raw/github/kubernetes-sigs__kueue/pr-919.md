# PR #919: [e2e] Make kind verbose

**Summary**: [e2e] Make kind verbose

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/919

**Last updated**: 2023-06-29T16:21:37Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `e2e_kind_verbose` → `main`
- **Created**: 2023-06-29T06:51:36Z
- **Updated**: 2023-06-29T16:21:37Z
- **Merged**: 2023-06-29T14:09:37Z
- **Closed**: 2023-06-29T14:09:37Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `size/XS`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 1   **+6 / -2**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
[e2e] Make kind verbose

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-06-29T06:51:45Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 3435658677f20923145164f3331caf69a361dd83 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/649d8decf26fa500081c5b69 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-06-29T14:01:12Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/919#pullrequestreview-1505367655" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/919#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[hack/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/hack/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-29T07:35:14Z

- **hack/e2e-test.sh:37** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-29T07:35:14Z
```diff
@@ -34,15 +34,18 @@ function cleanup {
         kubectl logs -n kube-system kube-controller-manager-kind-control-plane > $ARTIFACTS/kube-controller-manager.log || true
         kubectl logs -n kueue-system deployment/kueue-controller-manager > $ARTIFACTS/kueue-controller-manager.log || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/kueue-system-pods.log || true
-        $KIND delete cluster --name $KIND_CLUSTER_NAME || { echo "You need to run make kind-image-build before this script"; exit -1; }
+        $KIND delete cluster --name $KIND_CLUSTER_NAME -v 5
```

  Why do we need to increase verbose? Do you have any cases you want to increase this verbose?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-29T07:40:16Z

- **hack/e2e-test.sh:37** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-29T07:40:16Z
```diff
@@ -34,15 +34,18 @@ function cleanup {
         kubectl logs -n kube-system kube-controller-manager-kind-control-plane > $ARTIFACTS/kube-controller-manager.log || true
         kubectl logs -n kueue-system deployment/kueue-controller-manager > $ARTIFACTS/kueue-controller-manager.log || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/kueue-system-pods.log || true
-        $KIND delete cluster --name $KIND_CLUSTER_NAME || { echo "You need to run make kind-image-build before this script"; exit -1; }
+        $KIND delete cluster --name $KIND_CLUSTER_NAME -v 5
```

  My concern is to overlook important logs by this increase.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-06-29T07:47:09Z

- **hack/e2e-test.sh:37** by [@trasc](https://github.com/trasc) — 2023-06-29T07:47:04Z
```diff
@@ -34,15 +34,18 @@ function cleanup {
         kubectl logs -n kube-system kube-controller-manager-kind-control-plane > $ARTIFACTS/kube-controller-manager.log || true
         kubectl logs -n kueue-system deployment/kueue-controller-manager > $ARTIFACTS/kueue-controller-manager.log || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/kueue-system-pods.log || true
-        $KIND delete cluster --name $KIND_CLUSTER_NAME || { echo "You need to run make kind-image-build before this script"; exit -1; }
+        $KIND delete cluster --name $KIND_CLUSTER_NAME -v 5
```

  there war some cases when the kind cluster did not start. https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/913/pull-kueue-test-e2e-main-1-25/1674051550193913856

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-06-29T12:13:11Z

- **hack/e2e-test.sh:37** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-29T12:13:11Z
```diff
@@ -34,15 +34,18 @@ function cleanup {
         kubectl logs -n kube-system kube-controller-manager-kind-control-plane > $ARTIFACTS/kube-controller-manager.log || true
         kubectl logs -n kueue-system deployment/kueue-controller-manager > $ARTIFACTS/kueue-controller-manager.log || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/kueue-system-pods.log || true
-        $KIND delete cluster --name $KIND_CLUSTER_NAME || { echo "You need to run make kind-image-build before this script"; exit -1; }
+        $KIND delete cluster --name $KIND_CLUSTER_NAME -v 5
```

  I guess it's not worth during delete, just create?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-06-29T12:13:55Z

- **hack/e2e-test.sh:45** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-29T12:13:54Z
```diff
@@ -34,15 +34,18 @@ function cleanup {
         kubectl logs -n kube-system kube-controller-manager-kind-control-plane > $ARTIFACTS/kube-controller-manager.log || true
         kubectl logs -n kueue-system deployment/kueue-controller-manager > $ARTIFACTS/kueue-controller-manager.log || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/kueue-system-pods.log || true
-        $KIND delete cluster --name $KIND_CLUSTER_NAME || { echo "You need to run make kind-image-build before this script"; exit -1; }
+        $KIND delete cluster --name $KIND_CLUSTER_NAME -v 5
     fi
     (cd config/components/manager && $KUSTOMIZE edit set image controller=gcr.io/k8s-staging-kueue/kueue:main)
 }
 
 function startup {
     if [ $CREATE_KIND_CLUSTER == 'true' ]
     then
-        $KIND create cluster --name $KIND_CLUSTER_NAME --image $E2E_KIND_VERSION --config $SOURCE_DIR/kind-cluster.yaml --wait 1m
+        $KIND create cluster --name $KIND_CLUSTER_NAME --image $E2E_KIND_VERSION --config $SOURCE_DIR/kind-cluster.yaml --wait 1m -v 5
```

  Can we start with something like v2?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-06-29T12:50:56Z

- **hack/e2e-test.sh:45** by [@trasc](https://github.com/trasc) — 2023-06-29T12:50:56Z
```diff
@@ -34,15 +34,18 @@ function cleanup {
         kubectl logs -n kube-system kube-controller-manager-kind-control-plane > $ARTIFACTS/kube-controller-manager.log || true
         kubectl logs -n kueue-system deployment/kueue-controller-manager > $ARTIFACTS/kueue-controller-manager.log || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/kueue-system-pods.log || true
-        $KIND delete cluster --name $KIND_CLUSTER_NAME || { echo "You need to run make kind-image-build before this script"; exit -1; }
+        $KIND delete cluster --name $KIND_CLUSTER_NAME -v 5
     fi
     (cd config/components/manager && $KUSTOMIZE edit set image controller=gcr.io/k8s-staging-kueue/kueue:main)
 }
 
 function startup {
     if [ $CREATE_KIND_CLUSTER == 'true' ]
     then
-        $KIND create cluster --name $KIND_CLUSTER_NAME --image $E2E_KIND_VERSION --config $SOURCE_DIR/kind-cluster.yaml --wait 1m
+        $KIND create cluster --name $KIND_CLUSTER_NAME --image $E2E_KIND_VERSION --config $SOURCE_DIR/kind-cluster.yaml --wait 1m -v 5
```

  5 is almost random, in the code they only use 1 and 0 , if I recall correctly

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-06-29T12:55:05Z

- **hack/e2e-test.sh:37** by [@trasc](https://github.com/trasc) — 2023-06-29T12:55:05Z
```diff
@@ -34,15 +34,18 @@ function cleanup {
         kubectl logs -n kube-system kube-controller-manager-kind-control-plane > $ARTIFACTS/kube-controller-manager.log || true
         kubectl logs -n kueue-system deployment/kueue-controller-manager > $ARTIFACTS/kueue-controller-manager.log || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/kueue-system-pods.log || true
-        $KIND delete cluster --name $KIND_CLUSTER_NAME || { echo "You need to run make kind-image-build before this script"; exit -1; }
+        $KIND delete cluster --name $KIND_CLUSTER_NAME -v 5
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-06-29T13:12:45Z

- **hack/e2e-test.sh:45** by [@alculquicondor](https://github.com/alculquicondor) — 2023-06-29T13:12:45Z
```diff
@@ -34,15 +34,18 @@ function cleanup {
         kubectl logs -n kube-system kube-controller-manager-kind-control-plane > $ARTIFACTS/kube-controller-manager.log || true
         kubectl logs -n kueue-system deployment/kueue-controller-manager > $ARTIFACTS/kueue-controller-manager.log || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/kueue-system-pods.log || true
-        $KIND delete cluster --name $KIND_CLUSTER_NAME || { echo "You need to run make kind-image-build before this script"; exit -1; }
+        $KIND delete cluster --name $KIND_CLUSTER_NAME -v 5
     fi
     (cd config/components/manager && $KUSTOMIZE edit set image controller=gcr.io/k8s-staging-kueue/kueue:main)
 }
 
 function startup {
     if [ $CREATE_KIND_CLUSTER == 'true' ]
     then
-        $KIND create cluster --name $KIND_CLUSTER_NAME --image $E2E_KIND_VERSION --config $SOURCE_DIR/kind-cluster.yaml --wait 1m
+        $KIND create cluster --name $KIND_CLUSTER_NAME --image $E2E_KIND_VERSION --config $SOURCE_DIR/kind-cluster.yaml --wait 1m -v 5
```

  let's output to another file.... just to reduce clutter from the main logs

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-06-29T13:15:56Z

- **hack/e2e-test.sh:45** by [@tenzen-y](https://github.com/tenzen-y) — 2023-06-29T13:15:56Z
```diff
@@ -34,15 +34,18 @@ function cleanup {
         kubectl logs -n kube-system kube-controller-manager-kind-control-plane > $ARTIFACTS/kube-controller-manager.log || true
         kubectl logs -n kueue-system deployment/kueue-controller-manager > $ARTIFACTS/kueue-controller-manager.log || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/kueue-system-pods.log || true
-        $KIND delete cluster --name $KIND_CLUSTER_NAME || { echo "You need to run make kind-image-build before this script"; exit -1; }
+        $KIND delete cluster --name $KIND_CLUSTER_NAME -v 5
     fi
     (cd config/components/manager && $KUSTOMIZE edit set image controller=gcr.io/k8s-staging-kueue/kueue:main)
 }
 
 function startup {
     if [ $CREATE_KIND_CLUSTER == 'true' ]
     then
-        $KIND create cluster --name $KIND_CLUSTER_NAME --image $E2E_KIND_VERSION --config $SOURCE_DIR/kind-cluster.yaml --wait 1m
+        $KIND create cluster --name $KIND_CLUSTER_NAME --image $E2E_KIND_VERSION --config $SOURCE_DIR/kind-cluster.yaml --wait 1m -v 5
```

  > let's output to another file.... just to reduce clutter from the main logs
  
  +1
  
  Otherwise LGTM

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-06-29T13:59:23Z

- **hack/e2e-test.sh:45** by [@trasc](https://github.com/trasc) — 2023-06-29T13:59:23Z
```diff
@@ -34,15 +34,18 @@ function cleanup {
         kubectl logs -n kube-system kube-controller-manager-kind-control-plane > $ARTIFACTS/kube-controller-manager.log || true
         kubectl logs -n kueue-system deployment/kueue-controller-manager > $ARTIFACTS/kueue-controller-manager.log || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/kueue-system-pods.log || true
-        $KIND delete cluster --name $KIND_CLUSTER_NAME || { echo "You need to run make kind-image-build before this script"; exit -1; }
+        $KIND delete cluster --name $KIND_CLUSTER_NAME -v 5
     fi
     (cd config/components/manager && $KUSTOMIZE edit set image controller=gcr.io/k8s-staging-kueue/kueue:main)
 }
 
 function startup {
     if [ $CREATE_KIND_CLUSTER == 'true' ]
     then
-        $KIND create cluster --name $KIND_CLUSTER_NAME --image $E2E_KIND_VERSION --config $SOURCE_DIR/kind-cluster.yaml --wait 1m
+        $KIND create cluster --name $KIND_CLUSTER_NAME --image $E2E_KIND_VERSION --config $SOURCE_DIR/kind-cluster.yaml --wait 1m -v 5
```

  done, redirect to file, `cat` the file if create fails

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-06-29T14:01:00Z

/lgtm
/approve
